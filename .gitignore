# Build artifacts and dependencies
leanshot/node_modules/
leanshot/dist/
leanshot/.vite/
node_modules/

# Claude Code worktree scratchpads + agent infrastructure (per-agent git worktrees; never committed)
.claude/worktrees/
.claude/agents/
.claude/skills/
.claude/projects/
.playwright-mcp/

# Stray image files from earlier capture experiments
body-no-watermark.png
medlevel-watermark.png

# Reference / scratch
minisite/
leanshot.html
*.log
.DS_Store
.env
.env.local

# Supabase local artifacts + secrets
supabase/.env.secrets
supabase/.branches/
supabase/.temp/

# Deno lockfile (per-function, regenerated by CI/Edge Runtime)
supabase/functions/**/deno.lock
