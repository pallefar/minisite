---
phase: 24-foundation-modular-admin-shell-event-taxonomy-server-side-po
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260518000001_admin_role_enum.sql
  - supabase/migrations/20260518000002_profiles_admin_role_column.sql
  - supabase/migrations/20260518000003_audit_logs_table_rls.sql
  - supabase/migrations/20260518000004_log_admin_action_function.sql
  - supabase/migrations/20260518000005_audit_phi_table_list_triggers.sql
  - supabase/migrations/20260518000006_admin_backup_codes_table.sql
  - supabase/migrations/20260518000007_audit_archive_cron.sql
  - src/lib/admin/__tests__/audit-logs-rls.test.ts
  - src/lib/admin/__tests__/audit-trigger.test.ts
  - src/lib/admin/__tests__/backup-codes.test.ts
autonomous: false
requirements: [ADMIN-02, ADMIN-03]
user_setup:
  - service: supabase
    why: "Provision private Storage bucket for Parquet cold archive (D-16); cron requires bucket present"
    env_vars: []
    dashboard_config:
      - task: "Create private bucket `audit-archive` (file-size-limit 100 MB, allowed-mime types `application/octet-stream`)"
        location: "Supabase Dashboard -> Storage -> New bucket"

must_haves:
  truths:
    - "profiles.admin_role enum exists with three values: staff, admin, superadmin"
    - "audit_logs table exists with append-only RLS (DENY update + delete for ALL roles incl. service_role)"
    - "log_admin_action(action_name, target_user_id, before_jsonb, after_jsonb) is SECURITY DEFINER and inserts into audit_logs"
    - "Per-row INSERT/UPDATE/DELETE triggers attached to the 15 PHI tables write to audit_logs with before/after JSONB"
    - "admin_backup_codes table stores HMAC-hashed codes with used_at single-use enforcement"
    - "audit-archive bucket exists and pg_cron schedules nightly archive (03:00 UTC)"
    - "Cross-tenant impersonation test proves service_role CANNOT update or delete audit_logs rows"
  artifacts:
    - "supabase/migrations/20260518000001_admin_role_enum.sql"
    - "supabase/migrations/20260518000002_profiles_admin_role_column.sql"
    - "supabase/migrations/20260518000003_audit_logs_table_rls.sql"
    - "supabase/migrations/20260518000004_log_admin_action_function.sql"
    - "supabase/migrations/20260518000005_audit_phi_table_list_triggers.sql"
    - "supabase/migrations/20260518000006_admin_backup_codes_table.sql"
    - "supabase/migrations/20260518000007_audit_archive_cron.sql"
    - "src/lib/admin/__tests__/audit-logs-rls.test.ts"
    - "src/lib/admin/__tests__/audit-trigger.test.ts"
    - "src/lib/admin/__tests__/backup-codes.test.ts"
  key_links:
    - "log_admin_action -> audit_logs INSERT (only privileged write path for admin RPCs)"
    - "PHI-table triggers -> audit_logs INSERT (only privileged write path for PHI data changes)"
    - "admin_role enum -> profiles.admin_role column -> minRole gates in src/lib/admin/modules.ts (Plan 24-03)"
---

<objective>
Lock the SQL foundation for the entire phase. Apply 7 migrations that establish: (1) the 3-role admin enum per D-04, (2) audit_logs append-only RLS table per D-14/15/17, (3) log_admin_action SECURITY DEFINER write path per D-14, (4) trigger-based audit capture on the curated 15 PHI tables per D-14, (5) admin_backup_codes for HMAC-hashed single-use recovery codes per D-08, and (6) pg_cron + audit-archive cron entry for nightly Parquet archive per D-16. Wave 1 foundation that Plans 03/04/05/06 depend on.

Purpose: Without the role enum, AdminShell cannot gate modules. Without audit_logs + RLS, admin actions cannot be recorded tamper-proof. Without backup_codes, TOTP recovery has no DB surface. All other plans need these tables to exist.
Output: 7 SQL migrations + 3 RLS/trigger/backup-code test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-CONTEXT.md
@.planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-RESEARCH.md
@supabase/migrations
@src/components/clinic/settings/AuditTab.tsx
@src/lib/admin

<interfaces>
<!-- Existing v1.2 admin surface — Plan 24-03 will refactor AdminLayout to read profiles.admin_role -->
From src/components/admin/AdminLayout.tsx:
- Existing `is_staff` probe via supabase.auth + RPC. Phase 24 augments with admin_role column (existing is_staff users default to 'admin' on migration).

From src/lib/admin/admin-api.ts:
- Existing admin RPC wrappers. Phase 24 plans 03/05/06 add new RPCs that call `log_admin_action(...)` per D-14.

From src/components/clinic/settings/AuditTab.tsx:
- Reference audit-viewer pattern. Plan 24-06 reuses for the new admin Audit Log module.

Memory references (decision rationale):
- `[[reference_supabase_migration_gotchas]]` — SECURITY DEFINER needs `set search_path = extensions, public, pg_temp;`; partial-index expressions must be IMMUTABLE; storage.objects DELETE needs allow_delete_query GUC.
- `[[reference_supabase_migration_filename_regex]]` — `<14-digits>_name.sql` STRICT; letter suffix SILENTLY SKIPPED. All filenames here use the 14-digit form `20260518000001..7`.
- `[[reference_rls_fixture_gotruechient_flake]]` — RLS test fixture pattern; use admin.generateLink + /auth/v1/verify via PLAIN fetch (ES256-compat).
- `[[feedback_realtime_layer_e2e_pattern]]` — DB-level invariant verification > UI traversal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 7 migrations (admin_role enum, profiles column, audit_logs+RLS, log_admin_action, PHI triggers, backup_codes, audit_archive cron)</name>
  <files>
    supabase/migrations/20260518000001_admin_role_enum.sql,
    supabase/migrations/20260518000002_profiles_admin_role_column.sql,
    supabase/migrations/20260518000003_audit_logs_table_rls.sql,
    supabase/migrations/20260518000004_log_admin_action_function.sql,
    supabase/migrations/20260518000005_audit_phi_table_list_triggers.sql,
    supabase/migrations/20260518000006_admin_backup_codes_table.sql,
    supabase/migrations/20260518000007_audit_archive_cron.sql
  </files>
  <read_first>
    supabase/migrations (most recent v1.2 migrations as reference for header conventions),
    src/components/clinic/settings/AuditTab.tsx (existing audit viewer reuse target),
    .planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-CONTEXT.md,
    .planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-RESEARCH.md
  </read_first>
  <action>
    Write SQL migrations as follows. Use 14-digit filenames strictly. Each migration MUST be idempotent (`if not exists`, `or replace`, `do $$ ... if not exists ... end $$`) per v1.2 conventions. Every SECURITY DEFINER function MUST include `set search_path = extensions, public, pg_temp;` per [[reference_supabase_migration_gotchas]].

    20260518000001_admin_role_enum.sql:
    - `create type if not exists public.admin_role as enum ('staff','admin','superadmin');` (use do-block guard since `if not exists` isn't supported on `create type` pre-PG16 — use exception handler).

    20260518000002_profiles_admin_role_column.sql:
    - `alter table public.profiles add column if not exists admin_role public.admin_role;`
    - `alter table public.profiles add column if not exists has_totp boolean not null default false;`
    - Backfill existing `is_staff = true` rows to `admin_role = 'admin'`: `update public.profiles set admin_role = 'admin' where is_staff = true and admin_role is null;` (per D-04 mapping).
    - `create or replace function public.is_admin_at_least(min_role public.admin_role) returns boolean language sql stable security definer set search_path = extensions, public, pg_temp as $$ select case (select admin_role from public.profiles where id = auth.uid()) when 'superadmin' then array['staff','admin','superadmin']::public.admin_role[] when 'admin' then array['staff','admin']::public.admin_role[] when 'staff' then array['staff']::public.admin_role[] else array[]::public.admin_role[] end @> array[min_role] $$;` (ordinal comparator per D-04).
    - `grant execute on function public.is_admin_at_least(public.admin_role) to authenticated, anon;`

    20260518000003_audit_logs_table_rls.sql:
    - `create table if not exists public.audit_logs ( id uuid primary key default gen_random_uuid(), created_at timestamptz not null default now(), actor_user_id uuid references auth.users(id), target_user_id uuid references auth.users(id), action_name text not null, table_name text, row_pk text, before_data jsonb, after_data jsonb, org_id uuid /* P28 forward-compat per [[reference_rls_fixture_gotruechient_flake]] */, source text not null check (source in ('rpc','trigger')) );`
    - Indexes: `create index if not exists audit_logs_created_at_idx on public.audit_logs (created_at desc);`, plus `(actor_user_id, created_at desc)`, `(target_user_id, created_at desc)`, `(table_name, created_at desc)`.
    - `alter table public.audit_logs enable row level security;`
    - SELECT policy: admin-or-higher can read all rows. `create policy "audit_logs_select_admin" on public.audit_logs for select using (public.is_admin_at_least('admin'::public.admin_role));`
    - INSERT policy: REVOKE direct insert — only SECURITY DEFINER funcs + triggers can insert. `revoke insert on public.audit_logs from authenticated, anon, service_role; grant insert on public.audit_logs to postgres;` (triggers run as table owner; SECURITY DEFINER fns run as definer).
    - UPDATE policy: DENY. `create policy "audit_logs_no_update" on public.audit_logs for update using (false) with check (false);` and `revoke update on public.audit_logs from authenticated, anon, service_role;`
    - DELETE policy: DENY for ALL roles. `create policy "audit_logs_no_delete" on public.audit_logs for delete using (false);` and `revoke delete on public.audit_logs from authenticated, anon, service_role;` per D-17.

    20260518000004_log_admin_action_function.sql:
    - `create or replace function public.log_admin_action(p_action_name text, p_target_user_id uuid, p_table_name text default null, p_row_pk text default null, p_before jsonb default null, p_after jsonb default null) returns uuid language plpgsql security definer set search_path = extensions, public, pg_temp as $$ declare v_id uuid; begin if auth.uid() is null or not public.is_admin_at_least('staff'::public.admin_role) then raise exception 'log_admin_action: caller is not admin'; end if; insert into public.audit_logs (actor_user_id, target_user_id, action_name, table_name, row_pk, before_data, after_data, source) values (auth.uid(), p_target_user_id, p_action_name, p_table_name, p_row_pk, p_before, p_after, 'rpc') returning id into v_id; return v_id; end; $$;`
    - `grant execute on function public.log_admin_action(text, uuid, text, text, jsonb, jsonb) to authenticated;`

    20260518000005_audit_phi_table_list_triggers.sql:
    - Define `create or replace function public.fn_audit_phi_trigger() returns trigger language plpgsql security definer set search_path = extensions, public, pg_temp as $$ begin if current_setting('app.suppress_audit', true) = 'on' then return coalesce(new, old); end if; insert into public.audit_logs (actor_user_id, action_name, table_name, row_pk, before_data, after_data, source) values (auth.uid(), TG_OP, TG_TABLE_NAME, coalesce(new.id::text, old.id::text), case when TG_OP in ('UPDATE','DELETE') then to_jsonb(old) else null end, case when TG_OP in ('INSERT','UPDATE') then to_jsonb(new) else null end, 'trigger'); return coalesce(new, old); end; $$;` (suppress GUC per [[reference_supabase_migration_gotchas]]).
    - Attach AFTER INSERT/UPDATE/DELETE triggers to the 15 PHI tables per D-14: injections, weights, meals, workouts, vials, costs, mood_logs, sleep_logs, photos, doctor_shares, clinic_patients, conversations, profiles, affiliate_payouts, audit_logs. (audit_logs trigger is for tamper-detection; it logs deletes that bypass RLS — should never fire if RLS is correct.)
    - For each table: `do $$ begin if not exists (select 1 from pg_trigger where tgname = 'trg_audit_<table>') then create trigger trg_audit_<table> after insert or update or delete on public.<table> for each row execute function public.fn_audit_phi_trigger(); end if; end $$;` Loop over the list explicitly (no dynamic SQL — easier diff review).

    20260518000006_admin_backup_codes_table.sql:
    - `create table if not exists public.admin_backup_codes ( id uuid primary key default gen_random_uuid(), user_id uuid not null references auth.users(id) on delete cascade, code_hash text not null, used_at timestamptz, created_at timestamptz not null default now() );`
    - `create unique index if not exists admin_backup_codes_user_hash on public.admin_backup_codes(user_id, code_hash);`
    - `alter table public.admin_backup_codes enable row level security;`
    - SELECT policy: only the owning user when aal2: `create policy "abc_select_own_aal2" on public.admin_backup_codes for select using (user_id = auth.uid() and (auth.jwt() ->> 'aal') = 'aal2');`
    - INSERT/UPDATE/DELETE: REVOKE; only SECURITY DEFINER funcs (Plan 24-05 ships `admin.issue_backup_codes` and `admin.consume_backup_code`).

    20260518000007_audit_archive_cron.sql:
    - `create extension if not exists pg_cron;` (Supabase Pro already on; per [[project_phase19_pre_plan_state.md]]).
    - `create extension if not exists pg_net;` (for net.http_post).
    - Cron entry: `select cron.schedule('audit-archive-nightly', '0 3 * * *', $$ select net.http_post( url := 'https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/audit-archive', headers := jsonb_build_object('Authorization', 'Bearer ' || (select decrypted_secret from vault.decrypted_secrets where name = 'SUPABASE_SERVICE_ROLE_KEY'), 'Content-Type', 'application/json'), body := jsonb_build_object('source','cron') ) $$);` (vault key MUST exist per Plan 24-06 user_setup).
    - Note: Plan 24-06 owns the audit-archive Edge Function implementation. This migration just registers the cron entry; the cron job becomes a no-op until the Edge Function is deployed.

    All migrations: top-of-file comment with `-- Phase 24: <one-liner>` and a `-- Decision ref: D-XX` line so verifiers can trace.
  </action>
  <verify>
    <automated>
      bash -c '
        cd leanshot && \
        # 1) Strict 14-digit filename check (per [[reference_supabase_migration_filename_regex]])
        for f in supabase/migrations/20260518000001_*.sql supabase/migrations/20260518000002_*.sql supabase/migrations/20260518000003_*.sql supabase/migrations/20260518000004_*.sql supabase/migrations/20260518000005_*.sql supabase/migrations/20260518000006_*.sql supabase/migrations/20260518000007_*.sql; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done && \
        # 2) Every SECURITY DEFINER fn has search_path set (no silent search_path attack)
        ! grep -l "security definer" supabase/migrations/2026051800000[1-7]_*.sql | xargs -I{} sh -c "grep -L \"set search_path\" {} && echo MISSING_SEARCH_PATH:{} && exit 2 || true" && \
        # 3) RLS deny clauses present
        grep -q "audit_logs_no_update" supabase/migrations/20260518000003_audit_logs_table_rls.sql && \
        grep -q "audit_logs_no_delete" supabase/migrations/20260518000003_audit_logs_table_rls.sql && \
        grep -q "revoke delete on public.audit_logs from authenticated, anon, service_role" supabase/migrations/20260518000003_audit_logs_table_rls.sql && \
        # 4) Dry-run apply locally
        supabase db reset && supabase db push --linked --dry-run 2>&1 | tee /tmp/24-01-dry.log && ! grep -i "^Skipping" /tmp/24-01-dry.log
      '
    </automated>
  </verify>
  <acceptance_criteria>
    - All 7 migration files exist with strict 14-digit timestamp filenames.
    - Every SECURITY DEFINER function declares `set search_path = extensions, public, pg_temp;`.
    - `audit_logs` has explicit RLS policies AND explicit REVOKEs covering update + delete for authenticated, anon, AND service_role.
    - `supabase db reset` followed by `supabase db push --linked --dry-run` applies cleanly with NO `Skipping` warnings.
    - `is_admin_at_least` ordinal comparator works for all 6 role pairings (staff/admin/superadmin × required role).
  </acceptance_criteria>
  <done>
    7 migration files written, named with 14-digit timestamps, idempotent guards present, `supabase db push --linked --dry-run` reports zero skips, search_path declared on every SECURITY DEFINER function.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the audit_logs RLS + trigger + backup-codes test suite</name>
  <files>
    src/lib/admin/__tests__/audit-logs-rls.test.ts,
    src/lib/admin/__tests__/audit-trigger.test.ts,
    src/lib/admin/__tests__/backup-codes.test.ts
  </files>
  <read_first>
    src/lib/admin/__tests__ (existing RLS fixture pattern from v1.2),
    src/lib/affiliate/__tests__ (RLS test pattern with admin.generateLink),
    .planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-RESEARCH.md (Pitfall 7 + RLS fixture notes)
  </read_first>
  <behavior>
    - Test 1: `service_role` CANNOT update or delete audit_logs rows (returns RLS error or zero rows affected). MUST cover both UPDATE and DELETE. Insert a row via SECURITY DEFINER `log_admin_action`, then attempt service_role direct delete; assert RLS deny.
    - Test 2: `log_admin_action` inserts a row with correct actor_user_id (auth.uid()), target_user_id, action_name, before/after JSONB, and source='rpc'.
    - Test 3: PHI-table trigger fires on `injections` INSERT — assert new audit_logs row exists with TG_OP='INSERT', table_name='injections', after_data containing the inserted row as JSON, source='trigger'.
    - Test 4: PHI-table trigger fires on `injections` DELETE — assert audit_logs row with TG_OP='DELETE', before_data populated, after_data null.
    - Test 5: `app.suppress_audit` GUC suppresses trigger (set to 'on', delete a row, assert no new audit_logs row inserted).
    - Test 6: admin_backup_codes — issue 10 hashed codes; consume one (mark used_at); attempt re-use; assert second attempt rejected.
    - Test 7: admin_backup_codes RLS — non-owner user CANNOT select another user's codes even with aal2.
  </behavior>
  <action>
    Mirror the RLS fixture pattern from src/lib/affiliate/__tests__/*-rls.test.ts (look at existing RLS suites for the project's preferred bootstrap). Use the [[reference_rls_fixture_gotruechient_flake]] 2026-05-16 fix: `admin.generateLink` + `/auth/v1/verify` via plain fetch (ES256-compat). DO NOT use HS256 mintTestJwt (obsolete on this project's ES256 instance).

    Per [[feedback_rls_per_file_slug_prefix]]: use file-scoped TEST_SLUG_PREFIX (not a shared module) to avoid clobber under vitest file-parallelism.

    Each test file imports the shared RLS bootstrap helper (find the existing one — most likely `src/lib/__tests__/rls-fixture.ts` or `src/lib/affiliate/__tests__/setup.ts`). If a shared helper exists, USE IT — do not duplicate.

    For service_role tests: bootstrap two synthetic users; create service_role client via SUPABASE_SERVICE_ROLE_KEY; attempt UPDATE + DELETE; assert PostgrestError with code matching `42501` or row count == 0.

    For backup-code re-use: insert 10 rows with `code_hash = encode(hmac(plaintext::bytea, 'test-secret'::bytea, 'sha256'), 'hex')`. Test consumes one via the consume RPC (PLAN 24-05 ships it — STUB it in this test file as a direct SQL `update admin_backup_codes set used_at = now() where ...` because the RPC may not exist yet at Wave 1; mark the test with a comment so Plan 24-05 swaps to RPC).

    Per [[feedback_chunked_planning_integration_seam_blindspot]]: who owns wire A→B for backup_codes? Answer: this Plan owns the TABLE; Plan 24-05 owns the issuing + consuming RPCs. Plan 24-05 entry condition asserts this Plan committed.
  </action>
  <verify>
    <automated>cd leanshot && npm test -- src/lib/admin/__tests__/audit-logs-rls.test.ts src/lib/admin/__tests__/audit-trigger.test.ts src/lib/admin/__tests__/backup-codes.test.ts --run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 7 tests pass against a freshly-reset DB seeded with the 7 migrations from Task 1.
    - `npm test -- src/lib/admin/__tests__/audit-logs-rls.test.ts --run` exits 0.
    - Tests use file-scoped TEST_SLUG_PREFIX (no shared global).
    - Tests use admin.generateLink + /auth/v1/verify (ES256-compat) — NOT HS256 mintTestJwt.
    - service_role UPDATE and DELETE both produce explicit RLS denial (not silently 0 rows).
  </acceptance_criteria>
  <done>
    All 7 RLS/trigger/backup-code assertions pass; no `it.fixme` or `test.skip`; tests use the project's ES256-compat fixture pattern.
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: HUMAN — provision audit-archive Storage bucket + vault key</name>
  <files>(no code; vendor UI + Supabase Vault)</files>
  <read_first>
    .planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-CONTEXT.md (D-16)
  </read_first>
  <action>
    Two steps the executor cannot do via code:

    Step A — Create the `audit-archive` private Storage bucket:
    - Supabase Dashboard -> Storage -> "New bucket"
    - Name: `audit-archive`
    - Public: NO
    - File size limit: 100 MB
    - Allowed MIME types: `application/octet-stream`
    - Click Create.

    Step B — Confirm SUPABASE_SERVICE_ROLE_KEY is in Vault (the pg_cron entry uses it to authenticate the http_post):
    - Run via supabase CLI:
      `supabase db query --linked --sql "select name from vault.secrets where name = 'SUPABASE_SERVICE_ROLE_KEY';"`
    - If absent: `supabase db query --linked --sql "select vault.create_secret(<service-role-key>, 'SUPABASE_SERVICE_ROLE_KEY')"` (paste the key from Dashboard -> Settings -> API -> service_role key). Per [[reference_phase19_research_findings]] vault pattern.
    - Reply with: "bucket created; vault key present" OR "bucket created; vault key created".
  </action>
  <verify>User-confirmed reply: "bucket created; vault key present|created".</verify>
  <acceptance_criteria>
    - `audit-archive` bucket visible in Supabase Storage dashboard, private, 100 MB limit.
    - `select name from vault.secrets where name = 'SUPABASE_SERVICE_ROLE_KEY'` returns the row.
  </acceptance_criteria>
  <done>
    Bucket provisioned + vault key confirmed; cron will fire but no-op until Plan 24-06 Edge Function is deployed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role → audit_logs | service_role bypasses RLS by default for grants; D-17 REQUIRES explicit deny |
| authenticated user → log_admin_action | RPC checks is_admin_at_least; non-admin call raises exception |
| PHI table writer → audit_trigger | Every PHI mutation MUST be captured; `app.suppress_audit` GUC explicit-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Tampering | audit_logs UPDATE/DELETE | mitigate | RLS policies DENY + explicit REVOKE update/delete for authenticated/anon/service_role; test asserts service_role denied |
| T-24-03b | Tampering | trigger bypass via direct service_role INSERT to PHI table | mitigate | trigger runs as table owner; service_role inserts FIRE the trigger; suppress_audit GUC must be explicitly set 'on' to bypass (audit-archive cron only) |
| T-24-02 | Elevation of privilege | log_admin_action called by non-admin | mitigate | function checks `is_admin_at_least('staff')`; raises exception otherwise |
| T-24-09 | Information disclosure | audit_logs SELECT by non-admin | mitigate | RLS policy restricts SELECT to is_admin_at_least('admin') |
| T-24-10 | Tampering | backup-code re-use | mitigate | unique (user_id, code_hash) + used_at single-use check in consume RPC (Plan 24-05) |
</threat_model>

<verification>
- `supabase db reset && supabase db push --linked --dry-run` clean (zero `Skipping`).
- `npm test -- src/lib/admin/__tests__ --run` passes all 3 test files.
- `supabase db query --linked --sql "select 1 from cron.job where jobname = 'audit-archive-nightly';"` returns 1 row.
- `supabase db query --linked --sql "select count(*) from pg_trigger where tgname like 'trg_audit_%';"` returns 15.
- Manual: `audit-archive` bucket present in Storage UI.
</verification>

<success_criteria>
- 7 migrations apply cleanly with strict 14-digit timestamps and no `Skipping` warnings.
- All 7 RLS/trigger/backup-code assertions pass.
- service_role explicitly denied UPDATE + DELETE on audit_logs (DB-level invariant test, not UI).
- PHI triggers attached to all 15 D-14 tables.
- audit-archive bucket + vault key present (human checkpoint confirmed).
- Cron entry registered (becomes active when Plan 24-06 Edge Fn deploys).
</success_criteria>

<output>
Create `.planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-01-SUMMARY.md` when done.
</output>
