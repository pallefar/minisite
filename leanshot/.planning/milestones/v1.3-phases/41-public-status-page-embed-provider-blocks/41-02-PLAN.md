---
phase: 41-public-status-page-embed-provider-blocks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20270711000001_p41_iframe_allowlist.sql
  - supabase/migrations/20270711000002_p41_iframe_allowlist_rpcs.sql
  - leanshot/src/lib/page-builder/block-schema.ts
  - leanshot/src/lib/page-builder/embed-src.ts
  - leanshot/src/lib/page-builder/__tests__/embed-src.custom-iframe.test.ts
  - leanshot/src/lib/admin/iframe-allowlist.ts
autonomous: true
requirements:
  - EMBED-04
  - EMBED-07
tags:
  - migration
  - schema
  - rpc
  - audit-log
  - foundation

user_setup: []

must_haves:
  truths:
    - "A new Postgres table `public.iframe_allowlist(id, hostname, added_by_user_id, added_at, last_used_at)` exists with hostname UNIQUE constraint and RLS enabled."
    - "Anon + authenticated roles can SELECT all rows (needed by Plan 41-03 Vercel Edge Middleware fetch); no role has direct INSERT/UPDATE/DELETE policies — writes flow only through SECDEF RPCs."
    - "SECDEF RPCs `add_iframe_allowlist_hostname(p_hostname text) returns uuid` and `remove_iframe_allowlist_hostname(p_id uuid) returns void` exist and are gated by `is_admin_at_least('superadmin'::public.admin_role)` per D-17."
    - "Both RPCs call `log_admin_action` with the canonical 6-arg signature (p_action_name, p_target_user_id, p_table_name, p_row_pk, p_before, p_after) — action names `'iframe_allowlist.add'` and `'iframe_allowlist.remove'`."
    - "RPCs reject malformed hostnames (`::`, `/`, `*`, leading `.`) with errcode '22023' — defensive layer matching UI-SPEC §Surface E client-side validation."
    - "TypeScript `BlockType` union has `'custom_iframe'` literal added as the 13th member."
    - "Pure validator `validateCustomIframeUrl(raw, allowlistHostnames)` enforces D-15 exact-hostname match (no subdomain expansion, no endsWith/includes)."
    - "Pure renderer `buildCustomIframeIframeHtml(content, allowlistHostnames)` emits iframe HTML with FIXED sandbox `allow-scripts allow-same-origin` per D-16 (admin cannot override)."
  artifacts:
    - path: "supabase/migrations/20270711000001_p41_iframe_allowlist.sql"
      provides: "iframe_allowlist table DDL + RLS + index"
      contains: "create table public.iframe_allowlist"
    - path: "supabase/migrations/20270711000002_p41_iframe_allowlist_rpcs.sql"
      provides: "SECDEF add/remove RPCs with audit logging"
      contains: "add_iframe_allowlist_hostname"
    - path: "leanshot/src/lib/page-builder/block-schema.ts"
      provides: "BlockType union extended with 'custom_iframe'"
      contains: "'custom_iframe'"
    - path: "leanshot/src/lib/page-builder/embed-src.ts"
      provides: "validateCustomIframeUrl + buildCustomIframeIframeHtml pure helpers"
      exports: ["validateCustomIframeUrl", "buildCustomIframeIframeHtml"]
    - path: "leanshot/src/lib/admin/iframe-allowlist.ts"
      provides: "Browser-side wrappers that call the RPCs via supabase-js + return typed results for the Plan 41-06 admin UI"
      exports: ["addHostname", "removeHostname", "listHostnames", "AllowlistRow"]
  key_links:
    - from: "supabase/migrations/20270711000002_p41_iframe_allowlist_rpcs.sql"
      to: "public.log_admin_action(text, uuid, text, text, jsonb, jsonb)"
      via: "perform call inside each RPC body"
      pattern: "perform public\\.log_admin_action"
    - from: "leanshot/src/lib/page-builder/embed-src.ts"
      to: "validateCustomIframeUrl"
      via: "buildCustomIframeIframeHtml calls validateCustomIframeUrl and returns empty string on failure"
      pattern: "validateCustomIframeUrl\\("
    - from: "leanshot/src/lib/admin/iframe-allowlist.ts"
      to: "supabase.rpc('add_iframe_allowlist_hostname', ...)"
      via: "supabase-js client wrapper used by Plan 41-06 admin UI"
      pattern: "rpc\\('(add|remove)_iframe_allowlist_hostname'"
---

<objective>
Ship the data + schema foundation for Custom-iframe support: the per-deployment allowlist table, two SECDEF RPCs with audit logging, the `BlockType` union extension, the pure-function URL validator + HTML builder, and the browser-side admin client wrappers.

Purpose: Plan 41-03 (Vercel Edge Middleware) fetches from this table to dynamically augment CSP `frame-src`. Plan 41-05 (CustomIframeBlock) calls `buildCustomIframeIframeHtml` for editor preview. Plan 41-06 (admin allowlist UI) calls `addHostname`/`removeHostname`. Every downstream Wave 2 + 3 plan depends on this plan landing first.

Output: 2 migrations, 4 modified/new TypeScript files, 1 new test file. Strictly schema + pure helpers — no UI, no middleware, no Edge Fns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-CONTEXT.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-RESEARCH.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-PATTERNS.md
@leanshot/src/lib/page-builder/block-schema.ts
@leanshot/src/lib/page-builder/embed-src.ts
@supabase/migrations/20260601000001_audit_logs.sql
@supabase/migrations/20270601000029_log_admin_action_function.sql
@supabase/migrations/20270603000003_p32_04_locale_overrides_audit.sql

<interfaces>
<!-- Canonical 6-arg log_admin_action signature — verified from 20270601000029:16-23 -->
log_admin_action(
  p_action_name    text,
  p_target_user_id uuid,
  p_table_name     text   default null,
  p_row_pk         text   default null,
  p_before         jsonb  default null,
  p_after          jsonb  default null
) returns void

<!-- Existing BlockType union — extend with one literal -->
File: leanshot/src/lib/page-builder/block-schema.ts (lines 36-48)
Current union has 12 literals: 'hero' | 'cta' | 'faq' | 'pricing' | 'testimonial' | 'feature-grid' | 'image-text' | 'footer' | 'calendly' | 'youtube' | 'tally' | 'lead-form'
Add: | 'custom_iframe'

<!-- embed-src.ts reusable helpers (verified lines 86-97, 135-142, 159-165) -->
File: leanshot/src/lib/page-builder/embed-src.ts
- parseAndValidateUrl(raw, expectedHost): existing pattern; mirror for custom-iframe but accept an allowlist array, not single host
- escapeHtmlAttr(value): existing module-local helper; reuse
- commonIframeAttrs(title): existing module-local helper; reuse
- EMBED_IFRAME_TITLES.calendly / youtube / tally: existing exported map; add custom_iframe entry with default title 'Embedded content'

<!-- Admin role enum (verified from existing migration) -->
type: public.admin_role enum
gate function: public.is_admin_at_least(role public.admin_role) returns boolean
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create iframe_allowlist table + RLS migration</name>
  <files>supabase/migrations/20270711000001_p41_iframe_allowlist.sql</files>
  <action>Create the migration at the exact filename `supabase/migrations/20270711000001_p41_iframe_allowlist.sql` (strict 14-digit underscore-prefix regex per `reference_supabase_migration_filename_regex`; brief mandates timestamps strictly greater than the stated remote tail `20270710000006`). Pre-check: `ls supabase/migrations/20270711*.sql` MUST return zero hits before write (collision pre-check per `reference_migration_timestamp_collision_precheck`); abort + renumber to 20270712000001 if a collision is found. Migration body (mirror 20260601000001_audit_logs.sql pattern + 41-PATTERNS lines 380-402): (1) `create table public.iframe_allowlist (id uuid primary key default gen_random_uuid(), hostname text not null unique, added_by_user_id uuid references auth.users(id) on delete set null, added_at timestamptz not null default now(), last_used_at timestamptz);` (2) `create index iframe_allowlist_hostname_idx on public.iframe_allowlist (hostname);` (3) `alter table public.iframe_allowlist enable row level security;` (4) Single SELECT policy `iframe_allowlist_select_public` for `anon, authenticated` with `using (true)` — explicitly required so the Plan 41-03 Vercel Edge Middleware can fetch via the anon key (hostnames are not sensitive; they appear in served HTML CSP headers; per RESEARCH Pattern 3). (5) NO INSERT/UPDATE/DELETE policies — writes must flow only through SECDEF RPCs (matches `audit_logs` default-deny pattern). Add a SQL comment block at the top of the file referencing `Phase 41 / D-14 / D-17 / EMBED-07` for git archeology. Do NOT add a comment that says "v1" or "future enhancement" — table is the v1.3 deliverable per D-17.</action>
  <verify>
    <automated>test -f supabase/migrations/20270711000001_p41_iframe_allowlist.sql &amp;&amp; grep -q "create table public.iframe_allowlist" supabase/migrations/20270711000001_p41_iframe_allowlist.sql &amp;&amp; grep -q "enable row level security" supabase/migrations/20270711000001_p41_iframe_allowlist.sql &amp;&amp; ! ls supabase/migrations/20270711000001*.sql 2&gt;/dev/null | grep -v "p41_iframe_allowlist.sql"</automated>
  </verify>
  <done>Migration file exists at the exact path; contains table DDL, RLS enable, public-SELECT policy, no INSERT/UPDATE/DELETE policies; no timestamp collision with any sibling file.</done>
</task>

<task type="auto">
  <name>Task 2: Create SECDEF RPCs migration with canonical 6-arg audit logging</name>
  <files>supabase/migrations/20270711000002_p41_iframe_allowlist_rpcs.sql</files>
  <action>Create the migration at exact filename `supabase/migrations/20270711000002_p41_iframe_allowlist_rpcs.sql` (collision pre-check first). Per 41-PATTERNS lines 422-457 and RESEARCH §Code Examples lines 699-738, define two SECDEF functions. Function 1 — `public.add_iframe_allowlist_hostname(p_hostname text) returns uuid language plpgsql security definer set search_path = public, extensions, pg_temp` (the `extensions, public, pg_temp` search_path is REQUIRED per `reference_supabase_migration_gotchas`). Body: (a) gate `if not public.is_admin_at_least('superadmin'::public.admin_role) then raise exception 'forbidden' using errcode = '42501'; end if;` (b) reject empty/null hostname with errcode '22023', (c) reject hostnames containing `'://'`, `'/'`, or `'*'` with errcode '22023', (d) `insert into public.iframe_allowlist (hostname, added_by_user_id) values (p_hostname, auth.uid()) returning id into v_id;`, (e) `perform public.log_admin_action('iframe_allowlist.add', null, 'iframe_allowlist', v_id::text, null, jsonb_build_object('hostname', p_hostname));` — note the 6-arg POSITIONAL invocation matches the verified signature in `20270601000029_log_admin_action_function.sql:16-23` (NOT the named-arg form in older docs), (f) return v_id. Function 2 — `public.remove_iframe_allowlist_hostname(p_id uuid) returns void` mirrors the gate; reads the hostname BEFORE delete into a local var, then `delete from public.iframe_allowlist where id = p_id;`, then `perform public.log_admin_action('iframe_allowlist.remove', null, 'iframe_allowlist', p_id::text, jsonb_build_object('hostname', v_hostname), null);` — note p_before is populated, p_after is null per delete-semantics. Both functions `grant execute ... to authenticated;` (RLS gate inside; non-superadmin invocations get the 42501 error). Per `feedback_rpc_auth_uid_vs_service_role_mismatch`: these RPCs reference `auth.uid()` — Plan 41-06 admin UI calls them via supabase-js with the user's JWT, NOT service-role — verified that admin UI uses authenticated client. NO `if exists / drop function` boilerplate at top — these are net-new functions; let the migration fail loudly if a name collision exists.</action>
  <verify>
    <automated>test -f supabase/migrations/20270711000002_p41_iframe_allowlist_rpcs.sql &amp;&amp; grep -c "security definer" supabase/migrations/20270711000002_p41_iframe_allowlist_rpcs.sql | grep -q "^2$" &amp;&amp; grep -q "is_admin_at_least.*superadmin" supabase/migrations/20270711000002_p41_iframe_allowlist_rpcs.sql &amp;&amp; grep -q "log_admin_action.*'iframe_allowlist.add'" supabase/migrations/20270711000002_p41_iframe_allowlist_rpcs.sql &amp;&amp; grep -q "log_admin_action.*'iframe_allowlist.remove'" supabase/migrations/20270711000002_p41_iframe_allowlist_rpcs.sql</automated>
  </verify>
  <done>Two SECDEF functions defined; both gated on `is_admin_at_least('superadmin')`; both call `log_admin_action` with 6 positional args; search_path includes extensions+public+pg_temp; collision pre-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend BlockType union + add embed-src helpers + admin client wrappers</name>
  <files>leanshot/src/lib/page-builder/block-schema.ts, leanshot/src/lib/page-builder/embed-src.ts, leanshot/src/lib/page-builder/__tests__/embed-src.custom-iframe.test.ts, leanshot/src/lib/admin/iframe-allowlist.ts</files>
  <behavior>
    - Test 1 (D-15): `validateCustomIframeUrl('https://meet.example.org/foo', ['meet.example.org'])` returns the URL string; `validateCustomIframeUrl('https://sub.meet.example.org', ['meet.example.org'])` returns null (exact-match only, no subdomain expansion).
    - Test 2 (D-15): `validateCustomIframeUrl('https://calendly.com.evil.com', ['calendly.com'])` returns null (look-alike rejection — `URL` ctor + exact `parsed.hostname` equality).
    - Test 3: `validateCustomIframeUrl('http://meet.example.org', ['meet.example.org'])` returns null (HTTPS required).
    - Test 4: `validateCustomIframeUrl('', anything)`, `validateCustomIframeUrl(null, anything)`, `validateCustomIframeUrl('not-a-url', anything)` all return null.
    - Test 5 (D-16): `buildCustomIframeIframeHtml({embedUrl:'https://meet.example.org', iframeTitle:'My form'}, ['meet.example.org'])` returns a string containing `sandbox="allow-scripts allow-same-origin"` (FIXED — no admin override) and `loading="lazy"` and `title="My form"` and `referrerpolicy="no-referrer"`.
    - Test 6: `buildCustomIframeIframeHtml({embedUrl:'https://bad.com', iframeTitle:'x'}, ['good.com'])` returns empty string (validation fail).
    - Test 7: iframeTitle shorter than 3 chars falls back to `'Embedded content'`.
    - Test 8: TypeScript: `block-schema.ts` accepts `BlockType` value `'custom_iframe'` (compile-time check covered by the test file importing the type).
  </behavior>
  <action>Step A — Open `leanshot/src/lib/page-builder/block-schema.ts`. Locate the `BlockType` literal union at lines 36-48 (12 literals today). Append `| 'custom_iframe'` as the 13th member with an inline comment `// P41 EMBED-07 — superadmin-allowlisted iframe`. Do NOT rename or reorder existing literals (downstream JSONB blocks reference these by string value). Step B — Open `leanshot/src/lib/page-builder/embed-src.ts`. Add (a) `interface CustomIframeContent { embedUrl: string; iframeTitle: string; widthMode?: boolean }` (export), (b) `validateCustomIframeUrl(raw: unknown, allowlistHostnames: ReadonlyArray&lt;string&gt;): string | null` per RESEARCH §Code Examples lines 592-603 — guard non-string, `new URL(raw)` in try/catch, require `parsed.protocol === 'https:'`, gate on `allowlistHostnames.includes(parsed.hostname)` (D-15: exact equality only — NEVER `endsWith`/`includes`/regex; matches the pattern at existing lines 22-29 + 86-97), return `parsed.toString()` or null. (c) `buildCustomIframeIframeHtml(content: CustomIframeContent, allowlistHostnames: ReadonlyArray&lt;string&gt;): string` per RESEARCH §Code Examples lines 605-626 — calls `validateCustomIframeUrl`; on null returns `''`; on success builds the iframe HTML reusing the EXISTING `escapeHtmlAttr()` + `commonIframeAttrs()` module-local helpers (lines 135-142 + 159-165); FIXED sandbox literal `allow-scripts allow-same-origin` per D-16. Extend `EMBED_IFRAME_TITLES` map with `custom_iframe: 'Embedded content'`. Step C — Create `leanshot/src/lib/admin/iframe-allowlist.ts` exporting (a) `interface AllowlistRow { id: string; hostname: string; added_by_user_id: string | null; added_at: string; last_used_at: string | null }` (b) `async function listHostnames(client: SupabaseClient): Promise&lt;AllowlistRow[]&gt;` — selects from `iframe_allowlist` order by `added_at desc`; (c) `async function addHostname(client: SupabaseClient, hostname: string): Promise&lt;{ id: string }&gt;` — `client.rpc('add_iframe_allowlist_hostname', { p_hostname: hostname })` then throws on error else returns `{ id: data }`; (d) `async function removeHostname(client: SupabaseClient, id: string): Promise&lt;void&gt;` — `client.rpc('remove_iframe_allowlist_hostname', { p_id: id })`. Use the existing supabase-js client pattern from sibling files in `src/lib/admin/`. NO authentication logic in this file — caller passes the authenticated client. Step D — Create the test file `leanshot/src/lib/page-builder/__tests__/embed-src.custom-iframe.test.ts` covering all 8 test cases in the `<behavior>` block. Use vitest; import directly from the source module.</action>
  <verify>
    <automated>cd leanshot &amp;&amp; npx vitest run src/lib/page-builder/__tests__/embed-src.custom-iframe.test.ts &amp;&amp; npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <done>All 8 test cases green; tsc strict clean; `grep -c "'custom_iframe'" leanshot/src/lib/page-builder/block-schema.ts` returns at least 1; `grep -c "allow-scripts allow-same-origin" leanshot/src/lib/page-builder/embed-src.ts` returns at least 1 (the FIXED sandbox literal); `iframe-allowlist.ts` exports the four named symbols.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase-js client → SECDEF RPC | Browser-side JWT crosses into elevated privilege via SECDEF. Gate must re-verify role; cannot trust client claims. |
| Editor input (raw URL) → server | Admin-pasted URL crosses into both client-side validator AND server-side audit log; both layers MUST validate independently per D-15. |
| Public anon SELECT → iframe_allowlist | Anyone with the anon key can read hostnames. Accepted exposure (hostnames appear in served CSP headers anyway). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Elevation of Privilege | `add_iframe_allowlist_hostname` RPC called by non-superadmin | mitigate | RPC body first statement: `if not public.is_admin_at_least('superadmin'::public.admin_role) then raise exception 'forbidden' using errcode = '42501';` — SECDEF re-checks server-side regardless of client claim. Matches Phase 24 dual-layer pattern. |
| T-41-02-02 | Tampering | Look-alike hostname `calendly.com.evil.com` slips past validation | mitigate | `validateCustomIframeUrl` uses `URL` ctor + `parsed.hostname === expected` exact equality (verified `embed-src.ts:22-29,86-97`). Tests in Task 3 explicitly cover the look-alike case. |
| T-41-02-03 | Tampering | Subdomain expansion (`sub.meet.example.org` matches allowlisted `meet.example.org`) | mitigate | Per D-15: `allowlistHostnames.includes(parsed.hostname)` — NEVER `endsWith`/`startsWith`. Tested. |
| T-41-02-04 | Information Disclosure | Anon key reads hostname list | accept | Hostnames are not secrets — they appear in served HTML CSP `frame-src` headers. Public-SELECT policy is documented architectural decision (RESEARCH §Pattern + §Pattern 3 fetch path). |
| T-41-02-05 | Tampering | Malformed hostname `meet.com/path` slips into table | mitigate | RPC body second gate: `if p_hostname like '%://%' or p_hostname like '%/%' or p_hostname like '%*%' then raise '22023'` — server-side defense layered with UI-SPEC §Surface E client-side rules. |
| T-41-02-06 | Repudiation | Allowlist mutation not auditable | mitigate | Both RPCs `perform log_admin_action(...)` with `action_name in ('iframe_allowlist.add', 'iframe_allowlist.remove')`. 90d retention is managed by the existing `audit_retention_cron` (no Phase 41 change needed per 41-PATTERNS line 589). |
| T-41-02-07 | Denial of Service | Admin floods table with bogus hostnames | accept | Superadmin-only gate (D-17) — superadmin self-DOS is not a credible threat. Table size assumption (A5 in RESEARCH): <1000 rows is realistic for v1.3 deployment scope. |
| T-41-02-08 | Tampering | iframe sandbox flags bypass (D-16) | mitigate | `buildCustomIframeIframeHtml` writes `sandbox="allow-scripts allow-same-origin"` as a string literal — no parameterization, no override path. Plan 41-05's CustomIframeBlock + Plan 41-03's Deno renderer MUST call this helper; cannot construct iframe attrs independently. |
</threat_model>

<verification>
- `cd leanshot &amp;&amp; npx vitest run src/lib/page-builder/__tests__/embed-src.custom-iframe.test.ts` — all 8 cases green.
- `cd leanshot &amp;&amp; npx tsc -p tsconfig.app.json --noEmit` — strict TS clean (per Supabase v2 AAL gotcha memory: use `-p ... --noEmit`, NOT `tsc -b`).
- `cd leanshot &amp;&amp; npm run lint` clean — including no new violations under ESLint `import-x/no-restricted-paths` zones (D-18: this plan does NOT touch `src/lib/native/ads*.ts`; verify via lint pass).
- `ls supabase/migrations/20270711*.sql` returns exactly 2 files (no collisions).
- `grep -RIn "endsWith\|startsWith.*hostname\|includes.*hostname" leanshot/src/lib/page-builder/embed-src.ts | grep -v '^#' | grep -v "test"` returns ONLY the `allowlistHostnames.includes(parsed.hostname)` line (the safe exact-match call) — no string-pattern hostname matching.
- Migrations are SQL-only and validated at Plan 41-06's `[BLOCKING] supabase db push --linked` closeout step — this plan does NOT push (per phase migration aggregation discipline).
</verification>

<success_criteria>
- Both migration files exist at the exact strict-regex filenames; collision check clean.
- `BlockType` union contains `'custom_iframe'`; tsc strict-clean.
- 8 unit tests green covering D-15 exact-match + D-16 fixed sandbox + edge cases.
- `iframe-allowlist.ts` exports the four named symbols Plan 41-06 depends on.
- No `[BLOCKING] supabase db push` in THIS plan — pushes are aggregated to Plan 41-06 closeout.
</success_criteria>

<output>
After completion, create `.planning/phases/41-public-status-page-embed-provider-blocks/41-02-SUMMARY.md` describing: (1) the two migration filenames (verbatim), (2) the RPC signatures with their exact `log_admin_action` call shape, (3) the new public exports from `embed-src.ts` + `iframe-allowlist.ts` for Plan 41-05/06 consumption, (4) explicit note that `supabase db push` is deferred to Plan 41-06's closeout task.
</output>
