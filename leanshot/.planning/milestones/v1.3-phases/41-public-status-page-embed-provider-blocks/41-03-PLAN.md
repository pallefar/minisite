---
phase: 41-public-status-page-embed-provider-blocks
plan: 03
type: execute
wave: 2
depends_on:
  - 41-02
files_modified:
  - leanshot/middleware.ts
  - leanshot/vercel.json
  - leanshot/tests/csp/csp-snapshot.txt
  - leanshot/tests/integration/csp-middleware.test.ts
  - supabase/functions/page-render/render.ts
  - supabase/functions/page-render/index.ts
autonomous: true
requirements:
  - EMBED-01
  - EMBED-02
  - EMBED-03
  - EMBED-04
  - EMBED-07
tags:
  - middleware
  - csp
  - vercel-edge
  - deno-renderer

user_setup:
  - service: sentry
    why: "CSP violation reporting endpoint URL needed for D-11. Sentry org 'optimizenet' on de.sentry.io per memory reference."
    env_vars:
      - name: VITE_SENTRY_CSP_REPORT_URI
        source: "Sentry → Settings → Security Headers → CSP report URI for leanshot-web project; or assemble manually as https://o<orgid>.ingest.sentry.io/api/<projid>/security/?sentry_key=<key>"

must_haves:
  truths:
    - "`vercel.json` includes Vercel rewrites for `/api/calendly/oauth-start` → Supabase Edge Fn `calendly-oauth-start` and `/api/calendly/oauth-callback` → Supabase Edge Fn `calendly-oauth-callback` (Plan 41-04 Edge Fns require these — middleware passes rewrites through unchanged)."
    - "Static CSP in `vercel.json` includes per-provider entries from D-12: Calendly (`*.calendly.com`, `assets.calendly.com`, `api.calendly.com`), YouTube (`youtube-nocookie.com`, `www.youtube-nocookie.com`, `s.ytimg.com`, `i.ytimg.com`), Tally (`tally.so`, `*.tally.so`)."
    - "Static `vercel.json` CSP contains NO `${VITE_*}` literals — env-var-derived `report-uri` is assembled by the Vercel Edge Middleware at request time (D-11/D-13). Static CSP has a minimal fallback (no report-uri); middleware augments it with `report-uri ${VITE_SENTRY_CSP_REPORT_URI}` + CSP-3 `report-to csp-endpoint` when env is set."
    - "`tests/csp/csp-snapshot.txt` regenerated in the SAME commit as the `vercel.json` edit per Phase 12 D-10 enforcement (`reference_grep_gate_comment_strip` memory + RESEARCH §Pitfall 10)."
    - "Vercel Edge Middleware at `leanshot/middleware.ts` runs on all non-asset routes; reads `iframe_allowlist` hostnames via Supabase REST (anon key) with 60s in-memory cache; appends `https://<hostname>` entries to the `frame-src` directive AFTER the static CSP returns from origin."
    - "Middleware uses regex replacement that APPENDS to `frame-src`; does NOT overwrite the whole CSP (per RESEARCH §Pattern 3 + §Pitfall 11)."
    - "`supabase/functions/page-render/index.ts` (the orchestrator that calls `renderPage` at line 230) fetches `select hostname from public.iframe_allowlist` BEFORE calling `renderPage`, threading `allowlistHostnames: string[]` into the renderPage input shape. The fetch is BLOCKING — on DB error the renderer throws a 500 rather than rendering custom-iframe blocks as inert placeholders (per D-15: fail loud, do not silently no-op)."
    - "`supabase/functions/page-render/render.ts` has a new `case 'custom_iframe':` branch that calls a new `renderEmbedCustomIframe(block, allowlistHostnames)` helper which pre-validates via `validateCustomIframeUrl` and emits the iframe HTML with FIXED `sandbox=\"allow-scripts allow-same-origin\"` per D-16. Custom-iframe blocks render an iframe ONLY when hostname is in `allowlistHostnames`; validation failure → empty iframe HTML → placeholder visible but iframe never hydrates."
    - "`render.ts` retrofit emits a consent-gating placeholder (`<div data-embed-pending data-embed-category data-embed-src ...>`) + one inline `<script>` per page that subscribes to `'leanshot:consent-change'` and hydrates pending iframes on grant (per RESEARCH §Pattern 2)."
  artifacts:
    - path: "leanshot/middleware.ts"
      provides: "Vercel Edge Middleware that augments CSP frame-src per request with iframe_allowlist hostnames"
      contains: "iframe_allowlist"
    - path: "leanshot/vercel.json"
      provides: "Static CSP extended per D-12 + report-uri + report-to directives"
      contains: "calendly"
    - path: "leanshot/tests/csp/csp-snapshot.txt"
      provides: "Snapshot regenerated to include new D-12 entries"
      contains: "tally"
    - path: "leanshot/tests/integration/csp-middleware.test.ts"
      provides: "Unit-level verification that middleware appends hostnames to frame-src correctly (mockable fetch)"
    - path: "supabase/functions/page-render/render.ts"
      provides: "case 'custom_iframe' + retrofit consent-gating placeholders on all 4 embed renderers"
      contains: "custom_iframe"
    - path: "supabase/functions/page-render/index.ts"
      provides: "BLOCKING iframe_allowlist hostname fetch threaded into renderPage input"
      contains: "iframe_allowlist"
  key_links:
    - from: "leanshot/middleware.ts"
      to: "https://ytnsipxxmzgaebkqmokp.supabase.co/rest/v1/iframe_allowlist?select=hostname"
      via: "fetch with SUPABASE_ANON_KEY auth header, 60s cache"
      pattern: "iframe_allowlist\\?select=hostname"
    - from: "supabase/functions/page-render/render.ts"
      to: "leanshot/src/lib/page-builder/embed-src.ts validateCustomIframeUrl"
      via: "Deno renderer imports the shared validator (existing pattern: render.ts already imports from embed-src per 41-PATTERNS line 285)"
      pattern: "validateCustomIframeUrl"
    - from: "leanshot/vercel.json"
      to: "leanshot/tests/csp/csp-snapshot.txt"
      via: "Phase 12 D-10 snapshot test asserts sorted-equal — both files MUST edit in same commit"
      pattern: "frame-src.*calendly"
---

<objective>
Wire the request-time CSP layer + the public-page Deno renderer changes that the embed retrofit depends on. Static-CSP additions live in `vercel.json`; dynamic per-deployment Custom-iframe `frame-src` injection lives in a new Vercel Edge Middleware; the Supabase Edge Fn `page-render/render.ts` gets the `custom_iframe` switch branch + the server-side consent-gating placeholder shape.

Purpose: D-11 mandates day-1 CSP enforcement. D-12 mandates exact-host per-provider entries. D-14 mandates dynamic Custom-iframe injection — Vite SPA on Vercel does NOT support per-request CSP in `vercel.json` (RESEARCH §Pitfall 1), so a Vercel Edge Middleware is the documented path (RESEARCH §Pattern 3 + Assumption A1). The Deno renderer must emit consent-gated placeholders so public pages do NOT load provider scripts before consent (RESEARCH §Pattern 2).

Output: 1 new middleware file, 2 modified config files (vercel.json + csp-snapshot.txt — same commit), 1 new integration test, 1 modified Deno renderer.

Depends on Plan 41-02 (`iframe_allowlist` table + `validateCustomIframeUrl` helper). Same-wave as Plan 41-04 (Calendly OAuth Edge Fns) — zero file overlap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-CONTEXT.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-RESEARCH.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-PATTERNS.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-01-SUMMARY.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-02-SUMMARY.md
@leanshot/vercel.json
@leanshot/tests/csp/csp-snapshot.txt
@leanshot/tests/csp/csp-snapshot.test.ts
@supabase/functions/page-render/render.ts
@leanshot/src/lib/page-builder/embed-src.ts

<interfaces>
<!-- Current static CSP — verified vercel.json:45 -->
default-src 'none';
script-src 'self' https://js.stripe.com;
connect-src 'self' https://*.supabase.co wss://*.supabase.co https://*.ingest.us.sentry.io https://*.ingest.sentry.io https://*.posthog.com https://api.anthropic.com https://api.stripe.com https://m.stripe.network;
style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
img-src 'self' data: blob:;
font-src 'self' data: https://fonts.gstatic.com;
frame-src 'self' https://js.stripe.com https://hooks.stripe.com https://calendly.com https://www.youtube-nocookie.com https://tally.so;
object-src 'none';
base-uri 'self';
form-action 'self';
worker-src 'self' blob:

<!-- D-12 additions (RESEARCH §vercel.json EDIT lines 350-356) -->
script-src   += https://assets.calendly.com https://www.youtube-nocookie.com https://s.ytimg.com https://tally.so
connect-src  += https://api.calendly.com
frame-src    += https://*.calendly.com https://youtube-nocookie.com https://*.tally.so
img-src      += https://i.ytimg.com
+ report-uri https://o<sentryorg>.ingest.sentry.io/api/<projid>/security/?sentry_key=<key>
+ report-to csp-endpoint

<!-- render.ts existing renderer pattern (lines 663-672) -->
function renderEmbedCalendly(block: BlockNode): string {
  const c = block.content;
  const html = buildCalendlyIframeHtml({ ... });
  const wrapStyle = blockWrapperStyle(block.style, 'default', 'center');
  const wrapClass = `block block-cal-wrap${hideOnMobileClass(block.style.hideOnMobile)}`;
  return `<section class="${wrapClass}" style="${wrapStyle}"><div class="block-cal-wrap__inner">${html}</div></section>`;
}

<!-- Vercel Edge Middleware shape — RESEARCH §Pattern 3 lines 397-432 -->
import { next } from '@vercel/edge';
import type { Config } from '@vercel/edge';
export const config: Config = { matcher: ['/((?!api|_next/static|assets|favicon).*)'] };
// 60s in-memory cache; fetch SUPABASE_URL + SUPABASE_ANON_KEY env;
// regex-append to frame-src directive; preserve all other directives unchanged
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend static CSP in vercel.json + add Calendly OAuth rewrites + regenerate snapshot (ATOMIC commit)</name>
  <files>leanshot/vercel.json, leanshot/tests/csp/csp-snapshot.txt</files>
  <action>Open `leanshot/vercel.json`. **Step A — Add Vercel rewrites for Plan 41-04 Calendly OAuth Edge Fns (B2 fix)**: in the top-level `"rewrites"` array (Vercel auto-creates if absent), add two entries: `{ "source": "/api/calendly/oauth-start", "destination": "https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/calendly-oauth-start" }` and `{ "source": "/api/calendly/oauth-callback", "destination": "https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/calendly-oauth-callback" }`. The Edge Middleware matcher in Task 2 EXCLUDES `/api` paths (`matcher: ['/((?!api|_next/static|assets|favicon).*)']`) so these rewrites pass through unaugmented — verified safe. Plan 41-04 hard-codes `CALENDLY_OAUTH_REDIRECT_URI=https://app.leanshot.app/api/calendly/oauth-callback`; without this rewrite the callback 404s.
**Step B — Extend the static CSP `Content-Security-Policy` header value at line ~45** (single string under `"source": "/(.*)"` headers entry). Extend per D-12 (RESEARCH §vercel.json EDIT lines 350-356): (a) `script-src` — add `https://assets.calendly.com https://www.youtube-nocookie.com https://s.ytimg.com https://tally.so` (preserve existing `'self' https://js.stripe.com`); (b) `connect-src` — add `https://api.calendly.com` after the existing Sentry/PostHog entries; (c) `frame-src` — add `https://*.calendly.com https://youtube-nocookie.com https://*.tally.so` (note `https://youtube-nocookie.com` is the bare-domain entry; `https://www.youtube-nocookie.com` is already present); (d) `img-src` — add `https://i.ytimg.com`.
**Step C — `report-uri` lives in middleware, NOT vercel.json (B4 fix; path 1 of two — middleware-assembled, recommended)**: Vercel does NOT interpolate `${VITE_*}` env vars inside `vercel.json` — writing the literal string `${VITE_SENTRY_CSP_REPORT_URI}` into the header value would land the literal characters in the served CSP and the directive would be malformed. Therefore: DO NOT add `report-uri` or `report-to` directives to the static `vercel.json` CSP. Leave the static CSP as the minimal fallback (no report-uri) that serves on the very first SSR response before middleware runs; the Vercel Edge Middleware (Task 2) reads `process.env.VITE_SENTRY_CSP_REPORT_URI` at request time and appends `report-uri <url>; report-to csp-endpoint;` directives (and the `Report-To` JSON response header) when env is set. Do NOT touch the existing `X-Frame-Options: DENY` (kept for older-browser compat per RESEARCH §State-of-the-Art lines 750-752).
**Step D — Regenerate `leanshot/tests/csp/csp-snapshot.txt`** per Phase 12 D-10 contract — the test at `tests/csp/csp-snapshot.test.ts:65` does a sorted-equal comparison of directives. The snapshot reflects the STATIC `vercel.json` CSP (NOT the middleware-augmented one); per B4 it contains the new D-12 host additions but contains NO `report-uri` / `report-to` (those are middleware-only). Read the existing snapshot to learn the canonical sort order (one directive per line, hosts sorted alphabetically within each directive), then rewrite the snapshot to match the new vercel.json verbatim. BOTH files MUST be committed atomically — the snapshot test will BLOCK if they drift. Per memory `reference_grep_gate_comment_strip`: ensure no commented-out lines confuse the snapshot diff.</action>
  <verify>
    <automated>cd leanshot &amp;&amp; npx vitest run tests/csp/csp-snapshot.test.ts &amp;&amp; grep -q "assets.calendly.com" vercel.json &amp;&amp; grep -q "i.ytimg.com" vercel.json &amp;&amp; grep -q "/api/calendly/oauth-start" vercel.json &amp;&amp; grep -q "/api/calendly/oauth-callback" vercel.json &amp;&amp; ! grep -q '\${VITE_' vercel.json &amp;&amp; grep -q "assets.calendly.com" tests/csp/csp-snapshot.txt</automated>
  </verify>
  <done>CSP snapshot test green; vercel.json contains all D-12 additions + 2 Calendly OAuth rewrites; vercel.json contains NO `${VITE_*}` literal substrings (B4 negative gate); snapshot file matches; single atomic commit per Phase 12 enforcement contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vercel Edge Middleware for D-14 dynamic Custom-iframe CSP injection + B4 report-uri assembly</name>
  <files>leanshot/middleware.ts, leanshot/tests/integration/csp-middleware.test.ts</files>
  <behavior>
    - Test 1: When `iframe_allowlist` fetch returns `[{hostname:'meet.example.com'}, {hostname:'forms.acme.org'}]`, the middleware-augmented response CSP `frame-src` directive contains both `https://meet.example.com` and `https://forms.acme.org` APPENDED to existing directive entries (existing `'self' https://js.stripe.com ...` are preserved).
    - Test 2: When fetch fails (network error / 5xx), middleware returns the response with UNAUGMENTED CSP (does NOT throw, does NOT block the request). Failing safe = visitor sees default CSP; Custom-iframe blocks fail gracefully via the in-browser CSP block (acceptable degraded state).
    - Test 3: Second invocation within 60s does NOT re-fetch (cache hit); third invocation after `Date.now()` advances past `expiresAt` re-fetches.
    - Test 4: Middleware does NOT modify any directive other than `frame-src` and the appended `report-uri` / `report-to` directives (regex precision check on non-frame-src directives byte-equal).
    - Test 5: Empty allowlist → CSP `frame-src` unchanged (no trailing whitespace, no orphan semicolons). report-uri / report-to still appended if env set.
    - Test 6 (B4 — report-uri env-driven assembly): When `process.env.VITE_SENTRY_CSP_REPORT_URI = 'https://o123.ingest.sentry.io/api/456/security/?sentry_key=abc'`, the response CSP ends with `; report-uri https://o123.ingest.sentry.io/api/456/security/?sentry_key=abc; report-to csp-endpoint;` AND response headers include `Report-To: {"group":"csp-endpoint","max_age":10886400,"endpoints":[{"url":"https://o123.ingest.sentry.io/api/456/security/?sentry_key=abc"}]}`. When env var is unset, neither directive nor header is appended (clean fallback).
    - Test 7 (W11 — env-var-presence pre-check): When `SUPABASE_URL` or `SUPABASE_ANON_KEY` is missing (Vercel preview without env vars), middleware logs `console.warn('CSP middleware: env vars missing, returning unaugmented CSP')` and returns response without attempting the fetch (no allowlist augmentation; report-uri augmentation independent of this branch).
  </behavior>
  <action>Create `leanshot/middleware.ts` (project-root level — Vercel auto-detects). Follow RESEARCH §Pattern 3 lines 397-432 verbatim as the implementation contract. Key details: (a) `import { next } from '@vercel/edge'; import type { Config } from '@vercel/edge';` (package already available — Vercel runtime injects), (b) `export const config: Config = { matcher: ['/((?!api|_next/static|assets|favicon).*)'] };` — matcher excludes API + static assets + favicon (Vite SPA pattern; importantly this ALSO excludes the new `/api/calendly/oauth-*` rewrites added in Task 1 — Plan 41-04 OAuth Edge Fns are reached without middleware augmentation, which is correct since those endpoints do not need iframe_allowlist CSP injection), (c) module-private `let cache: { hosts: string[]; expiresAt: number } | null = null;` (60s TTL per RESEARCH Open Question 4 — manual purge path is Plan 41-06 admin UI escape hatch; deferred to v1.4 polish), (d) `export default async function middleware(request: Request)` — first calls `const response = await next();` so middleware mutates the response AFTER origin returns (per RESEARCH §Pitfall 11 + Assumption A1: this preserves middleware augmentation when `vercel.json:23` rewrites `/{slug}` to the Supabase `page-render` Edge Fn).
**W11 fix — env-var-presence pre-check (FIRST statement after `await next()`)**: `if (!process.env.SUPABASE_URL || !process.env.SUPABASE_ANON_KEY) { console.warn('CSP middleware: env vars missing, returning unaugmented CSP'); /* still attempt report-uri augmentation below — independent of allowlist fetch */ }` — when env vars are missing, SKIP the allowlist fetch entirely and SKIP frame-src augmentation, but still proceed to the report-uri augmentation step (which has its own env-var gate). This protects against silent un-augmented CSP under Vercel preview without env vars (W11). Test 7 covers.
(e) Allowlist fetch path (only entered when env vars present): cache-miss path fetches `${SUPABASE_URL}/rest/v1/iframe_allowlist?select=hostname` with headers `{ apikey: SUPABASE_ANON_KEY, Authorization: 'Bearer ' + SUPABASE_ANON_KEY }` — env vars read via `process.env.SUPABASE_URL` + `process.env.SUPABASE_ANON_KEY` (Vercel Edge runtime supports these), (f) on fetch failure (any error or non-2xx status), log to console.warn + return UNAUGMENTED response — fail-safe, NOT fail-closed (Test 2 behavior), (g) regex augment: `csp.replace(/frame-src ([^;]+);/, (_, dirs) => 'frame-src ' + dirs.trim() + ' ' + cache.hosts.join(' ') + ';')` — appends to the existing directive value.
**B4 fix — middleware-assembled report-uri (LAST step before setting header)**: read `const reportUri = process.env.VITE_SENTRY_CSP_REPORT_URI;` — if truthy, append to the CSP string: `csp += ' report-uri ' + reportUri + '; report-to csp-endpoint;';` AND set the JSON Report-To header: `response.headers.set('Report-To', JSON.stringify({group: 'csp-endpoint', max_age: 10886400, endpoints: [{url: reportUri}]}));`. If `reportUri` is unset (preview branches without Sentry env), skip both — graceful fallback to no reporting (Test 6 covers).
(h) `response.headers.set('content-security-policy', augmented)`. Create the test file at `leanshot/tests/integration/csp-middleware.test.ts` covering all 7 behavior cases. Mock `next` via vitest mock; mock `fetch` for the Supabase REST call; mock `process.env` per-test via vi.stubEnv. Per `feedback_planner_iter1_anti_patterns`: NO hedge comments like "may need adjustment later" — middleware behavior is locked. Per memory `reference_vite_static_env_inlining`: dynamic `process.env[\`VITE_${x}\`]` does NOT work; use literal env var names directly (VITE_SENTRY_CSP_REPORT_URI is read with literal key here, not template). Per `feedback_state_counter_table_needs_upsert_on_event`: the cache pattern here is a simple TTL replace (not a counter), no upsert concern.</action>
  <verify>
    <automated>cd leanshot &amp;&amp; npx vitest run tests/integration/csp-middleware.test.ts &amp;&amp; test -f middleware.ts &amp;&amp; grep -q "iframe_allowlist" middleware.ts &amp;&amp; grep -q "frame-src" middleware.ts</automated>
  </verify>
  <done>5 unit-level tests green; middleware.ts exists at project root with correct matcher config; fetch endpoint targets Supabase REST iframe_allowlist; regex replaces only frame-src directive; cache TTL 60s; fail-safe behavior on fetch error verified.</done>
</task>

<task type="auto">
  <name>Task 3: Add custom_iframe branch + consent-gating placeholder retrofit to render.ts + BLOCKING allowlist fetch in index.ts orchestrator (B3)</name>
  <files>supabase/functions/page-render/render.ts, supabase/functions/page-render/index.ts</files>
  <action>Open `supabase/functions/page-render/render.ts`. Step A — Add `case 'custom_iframe':` to the switch statement at line ~600+ (no fallthrough; strict TS `noFallthroughCasesInSwitch`). Define `renderEmbedCustomIframe(block: BlockNode, allowlistHostnames: ReadonlyArray<string>): string` mirroring `renderEmbedCalendly` at lines 663-672. Inside: call `buildCustomIframeIframeHtml(block.content, allowlistHostnames)` from the existing Deno-side import of `embed-src.ts` (verify the import is already at top of file per 41-PATTERNS line 285; add if missing — the Deno relative-import pattern is already established in this file). Wrap in `<section class="block block-embed-custom-iframe">...</section>`. Step B — Retrofit the 4 embed renderers (`renderEmbedCalendly`, `renderEmbedYouTube`, `renderEmbedTally`, `renderEmbedCustomIframe`) per RESEARCH §Pattern 2: emit a placeholder `<div class="block-embed-pending" data-embed-pending="true" data-embed-provider="<provider>" data-embed-category="<categories-csv>" data-embed-src="<escaped-src>" data-embed-sandbox="<provider-sandbox>" data-embed-title="<escaped-title>" data-embed-min-height="<provider-height>">...static HTML for State 1 per UI-SPEC §Surface B State 1...</div>` INSTEAD OF the existing iframe HTML directly. Per-provider category mappings (D-07): calendly → `'functional,analytics'`; youtube → `'analytics,marketing'`; tally → `'functional'`; custom_iframe → `'marketing'`. Per-provider sandbox + min-height — pull from existing per-provider constants (Calendly 700 / YouTube 16:9 aspect / Tally 500 / Custom-iframe 400 per UI-SPEC §Surface B). Step C — Add a SINGLE module-private function `renderConsentGatingScript(): string` that returns ONE `<script>...</script>` block containing the inline-JS subscriber (per RESEARCH §Pattern 2 lines 380-387). The script: (i) reads `'leanshot:consent-change'` literal (matches Plan 41-01 contract), (ii) on `DOMContentLoaded`, for each `[data-embed-pending="true"]`, reads the `cc_cookie` localStorage value OR uses vanilla-cookieconsent global `window.CookieConsent.acceptedCategory(...)` if present, decides grant from `data-embed-category` CSV, hydrates iframe immediately if granted, else `window.addEventListener('leanshot:consent-change', handler)` — handler re-checks per-element and replaces placeholder with iframe element. The iframe element is built from `data-embed-src` + `data-embed-sandbox` + `data-embed-title` + `data-embed-min-height` + opacity-0 → opacity-1 transition gated by `matchMedia('(prefers-reduced-motion: reduce)')`. NO module imports inside the script — Deno emits as raw template literal. Call `renderConsentGatingScript()` ONCE per page output (append to the assembled HTML body once, NOT per block).
**Step D — Update `renderPage` input shape**: extend the `RenderPageInput` interface to accept `allowlistHostnames: ReadonlyArray<string>` (required field; non-optional — per B3 fix, the hedge "pass empty array on missing" is REMOVED). `renderEmbedCustomIframe` reads from this input — no in-renderer fetch.
**Step E — B3 fix in `supabase/functions/page-render/index.ts` orchestrator** (the file that calls `renderPage` at line 230). Add a BLOCKING fetch BEFORE the `renderPage` call: `const allowlistRes = await client.from('iframe_allowlist').select('hostname');` — on `.error` truthy → throw a 500 Response (`return new Response('page-render: iframe_allowlist fetch failed: ' + allowlistRes.error.message, { status: 500, headers })`) rather than silently rendering custom-iframe blocks as inert placeholders. On success: `const allowlistHostnames = (allowlistRes.data ?? []).map(r => r.hostname as string);`. Thread `allowlistHostnames` into the `renderPage({...})` call at line 230 alongside `slug, title, seo, blocks, siteSettings`. This makes the allowlist a BLOCKING dependency of every page render — exactly what D-15 + UI-SPEC §Surface C require (custom-iframe behavior must be deterministic; an inert-placeholder failure mode would silently kill EMBED-07). The fetch leverages the existing `client` (service-role admin client per index.ts line 10 commentary) which has full read access; the `iframe_allowlist` table also has anon SELECT policy per Plan 41-02 Task 1 (defense in depth). Per `feedback_handoff_doc_with_embedded_discoveries`: reuse the existing DB connection — no second client construction. Per `feedback_planner_missed_status_enum_widening`: this plan adds NO new SQL constraints — block-type validation in render is by string match, not Postgres enum. Per memory `reference_supabase_edge_function_deploy`: bundler may ignore relative imports — verify the `embed-src` import works (esm.sh fallback for the validator helper is acceptable but prefer the existing relative-import pattern).</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; $HOME/.deno/bin/deno test --no-check --allow-read --allow-env supabase/functions/page-render/ 2&gt;&amp;1 | grep -E "ok|FAILED" &amp;&amp; grep -q "case 'custom_iframe'" supabase/functions/page-render/render.ts &amp;&amp; grep -q "data-embed-pending" supabase/functions/page-render/render.ts &amp;&amp; grep -q "leanshot:consent-change" supabase/functions/page-render/render.ts &amp;&amp; grep -q "iframe_allowlist" supabase/functions/page-render/index.ts &amp;&amp; grep -q "allowlistHostnames" supabase/functions/page-render/render.ts</automated>
  </verify>
  <done>switch has `case 'custom_iframe':` branch; all 4 embed renderers emit `data-embed-pending` placeholder + `data-embed-category` per D-07; single inline consent-gating script emitted per page; `'leanshot:consent-change'` literal hardcoded matches Plan 41-01 contract; `index.ts` orchestrator fetches `iframe_allowlist` BEFORE `renderPage` and threads `allowlistHostnames` into the input; on DB error orchestrator throws 500 (NOT silent empty array); Deno tests green; deno check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Edge runtime → Supabase REST | Middleware fetches with anon key over public network. Anon key is public — no secret crosses here. |
| Vercel rewrite → Supabase Edge Fn (page-render) | `vercel.json:23` rewrites `/{slug}` → `page-render`. Middleware augmentation MUST survive this rewrite (RESEARCH Assumption A1 + §Pitfall 11). |
| Public visitor → iframe (post-consent) | Once consent grants and iframe hydrates, provider scripts execute. CSP `frame-src` + `sandbox` are the active controls. |
| Inline script (Deno-emitted) → DOM | Script runs in page context with full DOM access. Must NOT introduce new XSS sinks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03-01 | Tampering | Iframe loaded BEFORE consent granted | mitigate | Deno renderer emits `<div data-embed-pending>` placeholder — NO `<iframe>` element exists until consent fires the hydration script. Tested implicitly via Plan 41-05 unit tests; reinforced by `data-embed-pending` grep in verify. |
| T-41-03-02 | Information Disclosure | CSP report-uri itself blocked by CSP | mitigate | Sentry ingest hosts already in `connect-src` per `vercel.json:42` (verified). Adding `report-uri` directive does not change connect-src eligibility. RESEARCH §Pitfall 12 covered. |
| T-41-03-03 | Tampering | Middleware regex-replace corrupts other CSP directives | mitigate | Regex `/frame-src ([^;]+);/` anchored to `;` terminator; only the matched directive is replaced. Test 4 in Task 2 explicitly verifies non-frame-src directives are byte-identical post-augmentation. |
| T-41-03-04 | Denial of Service | Supabase REST 5xx blocks all page loads | mitigate | Fail-safe pattern in Task 2: fetch failure returns UNAUGMENTED CSP. Default static CSP is functional for Calendly/YouTube/Tally; only Custom-iframe blocks degrade. |
| T-41-03-05 | Spoofing | Inline-script reads `leanshot:consent-change` event from malicious same-origin script | accept | Same-origin XSS would already have full control. The event-spoof defense (re-read `cc_cookie` at hydration time) is in the inline script per Plan 41-01 T-41-01-04 mitigation. |
| T-41-03-06 | Tampering | Custom-iframe rendered for hostname NOT on current allowlist (race: hostname removed between editor save + page render) | mitigate | `renderEmbedCustomIframe` re-validates via `validateCustomIframeUrl(content, currentAllowlist)` at render time; returns empty iframe HTML on miss → placeholder visible but iframe never hydrates. Editor save-time check is advisory; server render is authoritative. |
| T-41-03-07 | Information Disclosure | Sentry CSP report URI exposes org/project ID in HTML | accept | These IDs appear in every Sentry-instrumented page; not sensitive. Documented in D-13 (monthly review process). |
| T-41-03-08 | Tampering | Stale 60s middleware cache serves blocked-hostname state | accept | Per D-14 + RESEARCH Open Question 4: 60s TTL acceptable. Manual purge button deferred to v1.4 polish (NOT this plan); admin documented in 41-06 SUMMARY. |
</threat_model>

<verification>
- `cd leanshot &amp;&amp; npx vitest run tests/csp/csp-snapshot.test.ts` — Phase 12 snapshot gate green.
- `cd leanshot &amp;&amp; npx vitest run tests/integration/csp-middleware.test.ts` — 5 middleware behaviors verified.
- `cd /Users/karstenhaldan/minisite &amp;&amp; $HOME/.deno/bin/deno check --no-config supabase/functions/page-render/render.ts` — Deno type check clean.
- `! grep -q "\${VITE_" leanshot/vercel.json` — B4 negative gate: zero env-var literals in vercel.json.
- `grep -c "report-uri\|report-to" leanshot/middleware.ts` returns at least 2 — B4: middleware owns the report-uri assembly (NOT vercel.json).
- `grep -c "/api/calendly/oauth-" leanshot/vercel.json` returns 2 — B2: both OAuth rewrites present.
- `grep -c "iframe_allowlist" supabase/functions/page-render/index.ts` returns at least 1 — B3: orchestrator fetches allowlist.
- `grep -RIn "https://www.youtube-nocookie.com\|https://tally.so\|https://\*.calendly.com" leanshot/vercel.json | grep -v '^#' | wc -l` returns the D-12 additions present.
- `grep -c "data-embed-pending" supabase/functions/page-render/render.ts` returns at least 4 (one per embed renderer).
- `grep -c "case 'custom_iframe'" supabase/functions/page-render/render.ts` returns exactly 1.
- `cd leanshot &amp;&amp; npm run build` succeeds — production bundle includes middleware.ts in Vercel's expected location.
</verification>

<success_criteria>
- vercel.json + csp-snapshot.txt edited in same commit; snapshot test green; vercel.json contains 2 Calendly OAuth rewrites (B2) and ZERO `${VITE_*}` literal env-var substrings (B4).
- middleware.ts exists at project root, runs only on non-API/non-asset routes, augments CSP frame-src with iframe_allowlist hostnames (60s cache, fail-safe), assembles `report-uri` from `VITE_SENTRY_CSP_REPORT_URI` env at request time (B4), and short-circuits with logged warning if `SUPABASE_URL` / `SUPABASE_ANON_KEY` missing (W11).
- render.ts has `case 'custom_iframe':` branch + emits `data-embed-pending` placeholder + 1 inline consent-gating script per page; `renderPage` input requires `allowlistHostnames`.
- index.ts orchestrator BLOCKING-fetches `iframe_allowlist` before calling `renderPage`; throws 500 on DB error rather than silent inert-placeholder fallback (B3).
- No new directives in CSP overlap with existing ones (alphabetized sort holds).
- Wave 2 dispatch unblocked: Plan 41-04 (Calendly OAuth) is same-wave; zero file conflict (verified — middleware.ts + vercel.json + render.ts vs. supabase/functions/calendly-oauth-*).
</success_criteria>

<output>
After completion, create `.planning/phases/41-public-status-page-embed-provider-blocks/41-03-SUMMARY.md` describing: (1) verbatim new CSP directives added to vercel.json, (2) middleware.ts behavior contract (matcher, cache TTL, fail-safe semantics), (3) render.ts retrofit shape (placeholder data-attrs + single inline script), (4) confirmation that `supabase db push` of Plan 41-02 migrations is still PENDING — gated to Plan 41-06 closeout, (5) confirmation that the Sentry CSP report URI is in place OR flagged as a TODO for Plan 41-06 to reconcile during HUMAN-UAT.
</output>
