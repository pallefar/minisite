---
phase: 36
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20270710000001_p36_review_prompt_rules.sql
  - supabase/migrations/20270710000002_p36_review_prompt_history.sql
  - supabase/migrations/20270710000003_p36_native_review_prompts.sql
  - supabase/migrations/20270710000004_p36_review_cta_catalog.sql
  - supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql
  - supabase/migrations/__tests__/p36_rules.test.ts
  - supabase/migrations/__tests__/p36_history.test.ts
  - supabase/migrations/__tests__/p36_native_review_prompts.test.ts
  - supabase/migrations/__tests__/p36_cta_catalog.test.ts
  - leanshot/src/lib/analytics/events.ts
  - leanshot/eslint-rules/no-conditional-native-review.test.cjs
  - leanshot/scripts/check-no-conditional-native-review.sh
  - leanshot/eslint.config.js
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, REVIEW-08]
tags: [nps, review-prompt, schema, eslint, v13-3]

must_haves:
  truths:
    - "review_prompt_rules table exists with RLS deny + admin-only SECDEF RPCs"
    - "review_prompt_history table exists, append-only with user-scoped SELECT + service-role INSERT, including a copy_variant text NULL column for B2-direct-read funnel aggregation"
    - "native_review_prompts table exists with (platform IN ('ios','android')) CHECK"
    - "review_cta_catalog seeded with 5 rows (trustpilot/g2/capterra web + apple/google mobile-shell)"
    - "events.ts EventDef type carries optional nps_trigger_eligible boolean; D-01 whitelist events ship the flag set true"
    - "ESLint rule no-conditional-native-review passes RuleTester for P36 fixtures (direct + aliased + shim-method patterns)"
    - "scripts/check-no-conditional-native-review.sh exits 0 on clean tree and 1 on a planted violation"
    - "create_review_prompt_rule SECDEF RPC writes created_by = auth.uid() on INSERT (B1 fix — schema column is NOT NULL)"
    - "_test_seed_review_prompt_history SECDEF helper exists, gated by current_setting('app.test_mode', true) = 'true', for E2E seeding from Wave 5 (W8)"
    - "_test_seed_with_gas SECDEF wrapper exists in 20270710000005 and is the ONLY callable path from clients/test-harness; wrapper body PERFORMs set_config('app.test_mode','true', true) (transaction-local scope) then RETURNS _test_seed_review_prompt_history(...). Test seeding goes through `_test_seed_with_gas` which sets the GUC; bare `_test_seed_review_prompt_history` is never called directly from clients (W8-followup)."
    - "_test_seed_with_gas EXECUTE granted ONLY to service_role (revoked from PUBLIC, anon, authenticated)"
  artifacts:
    - path: "supabase/migrations/20270710000001_p36_review_prompt_rules.sql"
      provides: "rules table + RLS + indexes"
      contains: "CREATE TABLE public.review_prompt_rules"
    - path: "supabase/migrations/20270710000002_p36_review_prompt_history.sql"
      provides: "append-only history + RLS + copy_variant column for funnel by-variant aggregation"
      contains: "copy_variant text"
    - path: "supabase/migrations/20270710000003_p36_native_review_prompts.sql"
      provides: "native fire log"
      contains: "platform IN ('ios','android')"
    - path: "supabase/migrations/20270710000004_p36_review_cta_catalog.sql"
      provides: "CTA catalog table + 5-row seed"
      contains: "INSERT INTO public.review_cta_catalog"
    - path: "supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql"
      provides: "admin SECDEF RPCs: create/update/delete/list_review_prompt_rules + list_review_cta_catalog + _test_seed_review_prompt_history (test-mode-gated) + _test_seed_with_gas (service_role-only wrapper that sets app.test_mode GUC transaction-local before delegating to the bare helper — W8-followup)"
      contains: "SECURITY DEFINER"
    - path: "leanshot/src/lib/analytics/events.ts"
      provides: "nps_trigger_eligible flag + whitelist tagging"
      contains: "nps_trigger_eligible"
    - path: "leanshot/eslint-rules/no-conditional-native-review.test.cjs"
      provides: "extended RuleTester fixtures for P36 (direct + reviewShim.request + aliased re-export)"
      contains: "reviewShim.request"
    - path: "leanshot/scripts/check-no-conditional-native-review.sh"
      provides: "D-04 grep backup with comment-strip + 10-line co-occurrence window"
      contains: "comment-strip"
  key_links:
    - from: "supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql"
      to: "public.review_prompt_rules"
      via: "SECDEF RPC body INSERT/UPDATE/DELETE with admin_role re-check + created_by=auth.uid() on INSERT"
      pattern: "auth.uid()"
    - from: "supabase/migrations/20270710000002_p36_review_prompt_history.sql"
      to: "supabase/migrations/20270710000006_p36_review_funnel_aggregate_rpc.sql (Wave 4)"
      via: "copy_variant column read directly by review_funnel_aggregate by_variant breakdown — no events_mirror fallback (B2 fix)"
      pattern: "copy_variant"
    - from: "leanshot/src/lib/analytics/events.ts"
      to: "review_prompt_rules.trigger_event"
      via: "rule-builder UI sources picker options from events with nps_trigger_eligible=true"
      pattern: "nps_trigger_eligible"
    - from: "leanshot/eslint.config.js"
      to: "leanshot/eslint-rules/no-conditional-native-review.cjs"
      via: "rule already registered Phase 42; this plan only extends test fixtures + adds import-x zone"
      pattern: "no-conditional-native-review"
    - from: "supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql"
      to: "leanshot/e2e/_helpers/with-test-mode.ts (Wave 5 / 36-05 Task 2)"
      via: "_test_seed_with_gas SECDEF wrapper invoked from service-role admin client; wrapper sets app.test_mode GUC transaction-local then delegates to bare helper. Bare helper is NEVER called directly by clients (W8-followup)."
      pattern: "_test_seed_with_gas"
---

<objective>
Land all Phase 36 schema (4 net-new tables + 1 SECDEF RPC migration), extend the event registry with the `nps_trigger_eligible` flag, extend the existing V13-3 ESLint rule's test fixtures, and ship the grep backup gate. This plan is the foundation Wave 2/3/4 build against — no cross-plan file overlap.

Purpose: Unblock all downstream Phase 36 work (Edge Fns, modals, admin module). V13-3 BLOCKER (REVIEW-01) is anchored here via two-gate defense (AST rule fixtures + grep gate).
Output: 5 migrations (including copy_variant column on history + test-mode-gated seed helper + service_role-only wrapper that sets the GUC), events.ts extension, eslint test extension, grep gate script, eslint.config.js import-x zone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/PROJECT.md
@leanshot/.planning/ROADMAP.md
@leanshot/.planning/STATE.md
@leanshot/.planning/phases/36-m3-review-prompt-engine-web-only/36-CONTEXT.md
@leanshot/.planning/phases/36-m3-review-prompt-engine-web-only/36-RESEARCH.md
@leanshot/.planning/phases/36-m3-review-prompt-engine-web-only/36-PATTERNS.md
@supabase/migrations/20270707000009_helpdesk_create_ticket_rpc.sql
@supabase/migrations/20270708000020_p35_streak_warn_helper.sql
@leanshot/eslint-rules/no-conditional-native-review.cjs
@leanshot/eslint-rules/no-conditional-native-review.test.cjs
@leanshot/eslint-rules/additive-only-events.cjs
@leanshot/scripts/check-css-logical-properties.sh
@leanshot/src/lib/analytics/events.ts
@leanshot/eslint.config.js

<interfaces>
From leanshot/src/lib/analytics/events.ts (existing EventDef shape — add ONE field):
```typescript
export type EventDef = {
  readonly name: string;
  readonly version: 1;
  readonly payload: z.ZodObject<z.ZodRawShape>;
  readonly phi: false;
  readonly description: string;
  readonly owner: 'growth' | 'product' | 'platform' | 'billing' | 'admin';
  readonly server_only?: true;
  readonly aem_priority?: 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8;
  readonly aem_dropped?: true;
  // P36 D-01: extend with optional boolean flag.
  readonly nps_trigger_eligible?: true;
};
```

D-01 admissible events to flag `nps_trigger_eligible: true`:
- `activation_completed` (already in registry — owner: product, server_only: true)
- `level_up`, `streak_milestone_30d`, `streak_milestone_60d`, `streak_milestone_90d`, `weekly_challenge_completed` (Phase 35 may not have landed registry entries — defensively add only the flag on entries that already exist; do NOT create P35 entries here)
- `kb_article_helpful_voted` (Phase 37; defensively skip if entry absent)

From supabase/migrations/20270707000009_helpdesk_create_ticket_rpc.sql (analog for SECDEF preamble + search_path):
- `SET search_path = public, extensions;`
- `SECURITY DEFINER`
- Admin gate: `IF NOT EXISTS (SELECT 1 FROM public.profiles WHERE id = auth.uid() AND admin_role IN ('admin','superadmin')) THEN RAISE EXCEPTION 'forbidden'; END IF;`

Migration timestamps (per Pitfall 4 + project memory `reference_supabase_back_dated_migration_blocks_push`): latest on disk is `20270709000008`. Use `20270710000001..05` to stay strictly ahead.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migrations (4 tables) + SECDEF RPCs + SQL test fixtures</name>
  <files>
    supabase/migrations/20270710000001_p36_review_prompt_rules.sql,
    supabase/migrations/20270710000002_p36_review_prompt_history.sql,
    supabase/migrations/20270710000003_p36_native_review_prompts.sql,
    supabase/migrations/20270710000004_p36_review_cta_catalog.sql,
    supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql,
    supabase/migrations/__tests__/p36_rules.test.ts,
    supabase/migrations/__tests__/p36_history.test.ts,
    supabase/migrations/__tests__/p36_native_review_prompts.test.ts,
    supabase/migrations/__tests__/p36_cta_catalog.test.ts
  </files>
  <read_first>
    supabase/migrations/20270707000009_helpdesk_create_ticket_rpc.sql,
    supabase/migrations/20270708000020_p35_streak_warn_helper.sql,
    .planning/phases/36-m3-review-prompt-engine-web-only/36-RESEARCH.md (lines 560-616 schema; lines 323-330 Pattern 6 RLS),
    .planning/phases/36-m3-review-prompt-engine-web-only/36-PATTERNS.md (migration analog table)
  </read_first>
  <behavior>
    - Test: review_prompt_rules accepts a row with cohort_id=NULL (single-condition per D-02); rejects name length 0 or 61+.
    - Test: review_prompt_history INSERT works as service-role; UPDATE/DELETE denied via RLS (append-only per Pattern 6).
    - Test (B2 fix): review_prompt_history has copy_variant text NULL column; service-role INSERT can write 'control' / 'variant_a' / etc.; the column accepts NULL.
    - Test: native_review_prompts rejects platform value outside ('ios','android').
    - Test: review_cta_catalog seed has exactly 5 rows; 3 with requires_mobile_shell=false (trustpilot/g2/capterra); 2 with requires_mobile_shell=true (apple_app_store/google_play). available_for_org_type CHECK enforces ('consumer','clinic','both').
    - Test: SECDEF RPC create_review_prompt_rule raises 'forbidden' for non-admin caller; succeeds for admin_role='admin'.
    - Test (B1 fix): create_review_prompt_rule INSERT writes created_by = auth.uid(); assert post-insert SELECT created_by from review_prompt_rules WHERE id=<returned> equals the admin's user-id (the JWT subject used in the test).
    - Test: list_review_prompt_rules returns all rules to admin; list_review_cta_catalog returns full catalog for admin.
    - Test (W8): _test_seed_review_prompt_history is callable as service-role WHEN current_setting('app.test_mode', true) = 'true'; raises 'forbidden' otherwise. Inserts a row with (user_id, rule_id, fired_at, rating_value, copy_variant) as supplied.
    - Test (W8-followup): _test_seed_with_gas wrapper exists with signature (p_user_id uuid, p_rule_id uuid, p_fired_at timestamptz, p_rating_value smallint, p_copy_variant text DEFAULT NULL) RETURNS uuid. SECURITY DEFINER. Calling _test_seed_with_gas as service-role WITHOUT pre-setting app.test_mode succeeds (the wrapper sets the GUC transaction-local). Calling the bare _test_seed_review_prompt_history without the GUC still raises 'forbidden' (wrapper is the ONLY safe path).
    - Test (W8-followup): pg_proc contains _test_seed_with_gas with proowner=postgres. pg_proc_acl shows EXECUTE granted ONLY to service_role; NOT granted to anon, authenticated, or PUBLIC. Calling _test_seed_with_gas as the anon role raises a permission-denied error.
    - Test (W8-followup): after _test_seed_with_gas returns, the calling session's current_setting('app.test_mode', true) is NOT 'true' (i.e., third arg `true` = local-scope; GUC does not persist beyond the function-call frame).
  </behavior>
  <action>
Per D-05/D-07/D-08, history table is keyed on user_id and append-only. Per D-02, rules table has exactly one trigger_event text column + nullable cohort_id uuid; NO and_clauses / or_tree jsonb columns. Per D-13, CTA catalog includes mobile-shell rows but Wave 3 surface filters them out at query time.

Implement five migrations in strict timestamp order with named-dollar-quote tags (`$p36$ ... $p36$` for any DO blocks per project memory `reference_postgres_dollar_quote_nesting_in_cron_body`):

20270710000001_p36_review_prompt_rules.sql:
- CREATE TABLE public.review_prompt_rules with columns id uuid PK default gen_random_uuid(), name text NOT NULL CHECK (length(name) BETWEEN 1 AND 60), trigger_event text NOT NULL, cohort_id uuid REFERENCES public.cohorts(id) ON DELETE SET NULL, active boolean NOT NULL DEFAULT true, created_at timestamptz NOT NULL DEFAULT now(), created_by uuid NOT NULL REFERENCES auth.users(id), updated_at timestamptz NOT NULL DEFAULT now().
- CREATE INDEX ON (trigger_event) WHERE active = true (partial; expression IMMUTABLE per `reference_supabase_migration_gotchas`).
- ALTER TABLE ENABLE ROW LEVEL SECURITY; policies DENY all direct DML for authenticated role; SELECT allowed only via SECDEF RPC list_review_prompt_rules.

20270710000002_p36_review_prompt_history.sql (B2 — add copy_variant column):
- CREATE TABLE public.review_prompt_history with columns id uuid PK default gen_random_uuid(), user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, rule_id uuid REFERENCES public.review_prompt_rules(id) ON DELETE SET NULL, fired_at timestamptz NOT NULL DEFAULT now(), surface_dismissed_at timestamptz, rating_value int CHECK (rating_value BETWEEN 1 AND 5), copy_variant text NULL.
- copy_variant column: text, nullable (NULL = 'control' / pre-A/B-experiment legacy rows). NO default. Wave 2 step 10 INSERT writes the variant string sourced from PostHog flag resolution (e.g. 'control', 'variant_a'). Wave 4 funnel RPC reads this column directly for the by_variant breakdown — no events_mirror fallback (B2 + W5 fix).
- CREATE INDEX ON (user_id, fired_at DESC).
- CREATE INDEX ON (copy_variant) WHERE copy_variant IS NOT NULL (supports Wave 4 by_variant GROUP BY).
- ALTER TABLE ENABLE ROW LEVEL SECURITY; policy: user can SELECT own (user_id = auth.uid()); service_role can INSERT; UPDATE + DELETE denied for all roles (append-only).

20270710000003_p36_native_review_prompts.sql:
- CREATE TABLE public.native_review_prompts with columns id uuid PK default gen_random_uuid(), user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, platform text NOT NULL CHECK (platform IN ('ios','android')), fired_at timestamptz NOT NULL DEFAULT now().
- CREATE INDEX ON (user_id, platform, fired_at DESC).
- ALTER TABLE ENABLE ROW LEVEL SECURITY; deny all direct DML; service-role-only INSERT (v1.3 ships empty per D-18).

20270710000004_p36_review_cta_catalog.sql:
- CREATE TABLE public.review_cta_catalog with columns slug text PRIMARY KEY, display_name text NOT NULL, url_pattern text NOT NULL CHECK (url_pattern ~ '^(https?://|itms-apps://|market://)'), requires_mobile_shell boolean NOT NULL DEFAULT false, available_for_org_type text NOT NULL CHECK (available_for_org_type IN ('consumer','clinic','both')), claimed boolean NOT NULL DEFAULT false, created_at timestamptz NOT NULL DEFAULT now().
- ALTER TABLE ENABLE ROW LEVEL SECURITY; SELECT-all for authenticated (catalog is non-sensitive); INSERT/UPDATE/DELETE denied for all (seed-only in v1.3 per UI-SPEC Surface F).
- Inline seed INSERT for the 5 rows in the schema synopsis: trustpilot (consumer/false), g2 (clinic/false), capterra (clinic/false), apple_app_store (both/true), google_play (both/true). claimed defaults false; D-16 vendor-claim flips later via admin.

20270710000005_p36_review_secdef_rpcs.sql:
- Functions (all SECURITY DEFINER, SET search_path = public, extensions, RAISE EXCEPTION 'forbidden' if caller is not admin per analog — EXCEPT the test-seed helper + wrapper which use the test_mode GUC gate):
  - create_review_prompt_rule(p_name text, p_trigger_event text, p_cohort_id uuid, p_active boolean) RETURNS uuid — admin gate, then `INSERT INTO public.review_prompt_rules (name, trigger_event, cohort_id, active, created_by) VALUES (p_name, p_trigger_event, p_cohort_id, p_active, auth.uid()) RETURNING id` (B1 fix — explicit created_by from auth.uid() since the column is NOT NULL and there is no DEFAULT).
  - update_review_prompt_rule(p_id uuid, p_name text, p_trigger_event text, p_cohort_id uuid, p_active boolean) RETURNS void — UPDATE row.
  - delete_review_prompt_rule(p_id uuid) RETURNS void — DELETE row (history.rule_id ON DELETE SET NULL preserves history per Q4 RESEARCH).
  - list_review_prompt_rules() RETURNS SETOF public.review_prompt_rules.
  - list_review_cta_catalog() RETURNS SETOF public.review_cta_catalog.
  - _test_seed_review_prompt_history(p_user_id uuid, p_rule_id uuid, p_fired_at timestamptz, p_rating_value int, p_copy_variant text DEFAULT NULL) RETURNS uuid — W8 fix: SECDEF helper for Wave 5 E2E seeding. Gate body: `IF current_setting('app.test_mode', true) IS DISTINCT FROM 'true' THEN RAISE EXCEPTION 'forbidden — test_mode GUC not set'; END IF;`. Then INSERT INTO public.review_prompt_history(user_id, rule_id, fired_at, rating_value, copy_variant) VALUES (p_user_id, p_rule_id, p_fired_at, p_rating_value, p_copy_variant) RETURNING id. The bare helper is NEVER called directly by clients — the wrapper below is the only safe path. EXECUTE granted only to service_role; never to anon/authenticated.
  - _test_seed_with_gas(p_user_id uuid, p_rule_id uuid, p_fired_at timestamptz, p_rating_value smallint, p_copy_variant text DEFAULT NULL) RETURNS uuid — **W8-followup wrapper**. SECURITY DEFINER. SET search_path = public, extensions. Body (plpgsql):
    ```
    DECLARE
      v_id uuid;
    BEGIN
      PERFORM set_config('app.test_mode', 'true', true); -- third arg true = transaction-local scope; GUC does NOT persist beyond this function-call frame.
      v_id := public._test_seed_review_prompt_history(p_user_id, p_rule_id, p_fired_at, p_rating_value, p_copy_variant);
      RETURN v_id;
    END;
    ```
    Permissions for the wrapper (immediately after CREATE FUNCTION):
    ```
    REVOKE EXECUTE ON FUNCTION public._test_seed_with_gas(uuid, uuid, timestamptz, smallint, text) FROM PUBLIC;
    REVOKE EXECUTE ON FUNCTION public._test_seed_with_gas(uuid, uuid, timestamptz, smallint, text) FROM anon;
    REVOKE EXECUTE ON FUNCTION public._test_seed_with_gas(uuid, uuid, timestamptz, smallint, text) FROM authenticated;
    GRANT EXECUTE ON FUNCTION public._test_seed_with_gas(uuid, uuid, timestamptz, smallint, text) TO service_role;
    ```
    Production-call safety: the bare helper still raises 'forbidden' if invoked with `app.test_mode` unset, so even if a future code path tried to call `_test_seed_review_prompt_history` directly from a non-test context, the inner gate prevents inserts. The wrapper is the ONLY callable path for test-harness seeding; client code (Wave 5 with-test-mode.ts) calls `_test_seed_with_gas` exclusively (W8-followup contract — verified in 36-05 Task 2 via greps).
- Each admin function: admin gate check on profiles.admin_role IN ('admin','superadmin').
- GRANT EXECUTE ON FUNCTION ... TO authenticated for admin functions; GRANT EXECUTE ON FUNCTION _test_seed_review_prompt_history TO service_role (NOT authenticated, NOT anon); GRANT EXECUTE ON FUNCTION _test_seed_with_gas TO service_role ONLY (NOT authenticated, NOT anon, NOT PUBLIC — per W8-followup REVOKE/GRANT block above).

Tests (under supabase/migrations/__tests__/) follow the existing vitest-e2e harness pattern used by P37 / P40 — RLS impersonation proof per project rule. Add explicit pg_proc + pg_proc_acl assertions for `_test_seed_with_gas` per the new W8-followup behavior cases (existence, ownership, EXECUTE grants).

Filename hygiene: 14-digit underscore-prefix strict per `reference_supabase_migration_filename_regex`. No letter suffixes.
  </action>
  <verify>
    <automated>cd leanshot && npm run test:unit -- supabase/migrations/__tests__/p36 2>&1 | tail -40</automated>
  </verify>
  <done>
    All 5 migration files exist with correct timestamps; the 4 __tests__/p36_*.test.ts files pass; no UPDATE/DELETE permitted on history table; SECDEF RPCs gate on admin_role; create_review_prompt_rule populates created_by from auth.uid() (B1); review_prompt_history has copy_variant text NULL column (B2); _test_seed_review_prompt_history exists with test_mode GUC gate (W8); _test_seed_with_gas wrapper exists in pg_proc with EXECUTE granted ONLY to service_role (W8-followup — wrapper sets app.test_mode transaction-local then delegates to the bare helper; bare helper still raises 'forbidden' without the GUC, so wrapper is the only safe path).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: events.ts nps_trigger_eligible flag + flag the D-01 whitelist</name>
  <files>leanshot/src/lib/analytics/events.ts</files>
  <read_first>
    leanshot/src/lib/analytics/events.ts (full type + EVENTS const),
    .planning/phases/36-m3-review-prompt-engine-web-only/36-CONTEXT.md (D-01),
    .planning/phases/36-m3-review-prompt-engine-web-only/36-RESEARCH.md (Pitfall 3 — Phase 35 in-flight)
  </read_first>
  <behavior>
    - Test: EventDef type accepts the optional `nps_trigger_eligible?: true` field.
    - Test: `activation_completed` registry entry has `nps_trigger_eligible: true` (always-present per Phase 34 ship).
    - Test: any entry in EVENTS named `level_up`, `streak_milestone_*`, `weekly_challenge_completed`, `kb_article_helpful_voted` that EXISTS at this commit has `nps_trigger_eligible: true`; missing entries do NOT cause type errors (per A1 — Phase 35 may not have landed).
    - Test: `payment_failed`, `ticket_escalated`, `error_thrown` (if present) MUST NOT carry the flag.
  </behavior>
  <action>
Per D-01, the registry is the source of truth for trigger eligibility. Extend `EventDef` type with `readonly nps_trigger_eligible?: true;` directly after `aem_dropped`. Defensively flag the D-01 whitelist:

Add `nps_trigger_eligible: true` to the registry entry for each of these names if and only if the entry already exists in the `EVENTS` const at the time of execution:
- `activation_completed` (verified present today)
- `level_up`
- `streak_milestone_30d`
- `streak_milestone_60d`
- `streak_milestone_90d`
- `weekly_challenge_completed`
- `kb_article_helpful_voted`

Do NOT create new EVENTS entries for events that are not yet present — Phase 35 owns those and adding them here would conflict-on-merge per the planner-iter1 anti-pattern. Per Pitfall 3, this defensive approach lets Phase 35 + Phase 37 add entries later; downstream merge can re-add the flag if needed. Surface this as a code comment near each flagged entry: `// P36 D-01: NPS-eligible trigger event.`

Explicitly verify (greppable in source) that negative-state events do NOT receive the flag.
  </action>
  <verify>
    <automated>cd leanshot && npx tsc -p tsconfig.app.json --noEmit && grep -c "nps_trigger_eligible: true" src/lib/analytics/events.ts</automated>
  </verify>
  <done>
    `tsc` passes; ≥1 occurrences of `nps_trigger_eligible: true` in events.ts (activation_completed at minimum); zero occurrences on negative-state events (payment_failed/ticket_escalated/error_thrown).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: V13-3 BLOCKER — extend ESLint rule test fixtures + ship grep backup + import-x zone</name>
  <files>
    leanshot/eslint-rules/no-conditional-native-review.test.cjs,
    leanshot/scripts/check-no-conditional-native-review.sh,
    leanshot/eslint.config.js
  </files>
  <read_first>
    leanshot/eslint-rules/no-conditional-native-review.cjs (Phase 42 implementation — DO NOT modify the .cjs rule; only extend tests),
    leanshot/eslint-rules/no-conditional-native-review.test.cjs (existing test scaffold),
    leanshot/scripts/check-css-logical-properties.sh (comment-strip pattern analog per `reference_grep_gate_comment_strip`),
    leanshot/eslint.config.js (existing import-x/no-restricted-paths zones)
  </read_first>
  <behavior>
    - RuleTester: a P36-style file containing `if (rating >= 4) requestReview()` FAILS the rule.
    - RuleTester: a P36-style file containing `if (is_promoter) reviewShim.request()` FAILS the rule.
    - RuleTester: a file invoking `useNativeReviewTrigger().request()` unconditionally PASSES.
    - RuleTester: an aliased re-export pattern `const fireReview = requestReview; if (rating >= 4) fireReview();` FAILS (if the rule already supports tracked-alias detection per Pitfall 7) OR is documented as covered by the grep backup if AST detection cannot reach. Add a comment in the test file noting which gate catches it.
    - Shell: `bash leanshot/scripts/check-no-conditional-native-review.sh` exits 0 on clean tree; exits 1 when a planted file contains `requestReview` within 10 lines of `nps_score`.
    - ESLint config: `src/components/nps/**` is forbidden from importing `src/admin/**` and `src/lib/admin/**` (Pitfall 6 bundle isolation).
  </behavior>
  <action>
The ESLint rule file `eslint-rules/no-conditional-native-review.cjs` ALREADY EXISTS (Phase 42 D-20 shipped pre-emptively per RESEARCH Pitfall 1) — DO NOT recreate it. This task only extends the test file and ships the grep backup.

Extend `leanshot/eslint-rules/no-conditional-native-review.test.cjs`:
- Add a `RuleTester` block named `"Phase 36 fixtures (D-03 + D-21)"`.
- Add `invalid` cases covering: direct `if (rating >= 4) requestReview()`, ternary `is_promoter ? requestReview() : null`, conditional on `nps_score`, `review_state`, `is_detractor`. Each case asserts error message references CONTEXT.md D-03.
- Add `invalid` case for shim method: `if (rating >= 4) reviewShim.request()` and `if (rating >= 4) useNativeReviewTrigger().request()`.
- Add `valid` cases: unconditional call site (no NPS identifier in scope), call site that references rating only after the call returns (post-call analytics).
- Add a `valid`-or-`invalid` fixture for aliased re-export with a comment explaining whether AST catches (test runner determines empirically) — if it lands as `valid` (AST blind), add an inline TODO comment noting D-04 grep gate covers it.

Create `leanshot/scripts/check-no-conditional-native-review.sh` modeled exactly on `leanshot/scripts/check-css-logical-properties.sh`:
- `#!/usr/bin/env bash`, `set -u`, `set -o pipefail` (NOT `set -e` — we want to count violations).
- Strip `//` and `/* */` comments via sed first per `reference_grep_gate_comment_strip` (project rule: comment-strip BEFORE grep).
- Scan `leanshot/src/**/*.{ts,tsx}` for files containing both `requestReview|InAppReview\.request|Rating\.request|reviewShim\.request|useNativeReviewTrigger` AND any of `nps_score|rating|review_state|is_promoter|is_detractor` within 10 lines (awk window).
- Exit 0 if zero violations; exit 1 with file:line printout if any.
- Header comment: `# Phase 36 Plan 36-01 (REVIEW-01 D-04) — V13-3 grep backup.`
- File becomes executable (chmod +x via task action).

Extend `leanshot/eslint.config.js`:
- Add a new entry to the existing `import-x/no-restricted-paths` zones block: forbid `src/components/nps/**` from importing `src/admin/**` and `src/lib/admin/**`. Use **glob** patterns (per `reference_eslint_import_x_path_gotcha` — bare file paths silently no-op).
- Do NOT modify the existing `no-conditional-native-review` registration (already wired Phase 42 lines 16-19).

Make the grep gate runnable: `chmod +x leanshot/scripts/check-no-conditional-native-review.sh` post-write.
  </action>
  <verify>
    <automated>cd leanshot && node --test eslint-rules/no-conditional-native-review.test.cjs && bash scripts/check-no-conditional-native-review.sh && npm run lint -- eslint.config.js 2>&1 | tail -10</automated>
  </verify>
  <done>
    RuleTester P36 fixtures pass (all `invalid` cases produce expected errors; all `valid` cases produce 0 errors); grep backup exits 0 on clean working tree; eslint.config.js parses + lint passes; new `import-x` zone glob is present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→service_role | Edge Fn writes (Wave 2) use service-role to bypass RLS for history INSERT. This plan's RLS policies are the only gate. |
| client→admin RPC | Rules CRUD must go through SECDEF RPCs — direct DML is denied (V4 dual-layer per Phase 34 Pattern S1). |
| developer→production code | V13-3 BLOCKER: any future code conditioning native-review on rating value must be caught at PR time (D-03 AST + D-04 grep). |
| test-harness→test-seed RPC | _test_seed_review_prompt_history bypasses RLS (SECDEF) but is gated by the `app.test_mode` GUC. Production callers (GUC unset) get 'forbidden'. EXECUTE granted only to service_role, never to anon/authenticated. The W8-followup wrapper `_test_seed_with_gas` is the SOLE callable path for test-harness seeding; it sets the GUC transaction-local then delegates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Elevation of Privilege | review_prompt_rules SECDEF RPC | mitigate | Pattern S1 dual-layer — RPC re-checks `profiles.admin_role IN ('admin','superadmin')` server-side; RLS denies direct DML so non-admin RPC failure does not silently fall through to RLS bypass. Created_by is server-stamped via auth.uid() (B1 — no client-supplied user attribution). |
| T-36-02 | Tampering | review_prompt_history append-only contract | mitigate | RLS denies UPDATE + DELETE on all rows (Pattern 6); service-role-only INSERT from Wave 2 Edge Fn. |
| T-36-03 | Tampering / Repudiation (vendor policy) | conditional native-review call | mitigate | V13-3 two-gate defense: D-03 ESLint AST rule + D-04 grep backup with comment-strip. This plan extends both gates with P36 fixtures (Task 3). No code path in this plan imports the shim from a rating-aware context. |
| T-36-04 | Information Disclosure | review_cta_catalog url_pattern column | mitigate | CHECK constraint restricts url_pattern to `^(https?://|itms-apps://|market://)` prefixes; catalog is seed-only (v1.3) so no admin paste path exists. |
| T-36-05 | Spoofing | history rows attributed to wrong user | mitigate | user_id derived from JWT in Wave 2 Edge Fn (T-36-NEXT); history table FK to auth.users with ON DELETE CASCADE. This plan: no client-supplied user_id path exists outside the test-mode-gated seed helper. |
| T-36-06 | Tampering | rule.active toggle bypass | mitigate | `nps-trigger-decide` (Wave 2) MUST filter `WHERE active = true` — index on `(trigger_event) WHERE active = true` enforces server-side query shape (Pitfall 9 surfaces this in plan-check). |
| T-36-33 | Elevation of Privilege | _test_seed_review_prompt_history misuse in production | mitigate | SECDEF function checks `current_setting('app.test_mode', true) = 'true'` and raises 'forbidden' otherwise. EXECUTE granted only to service_role. Production application code never invokes this RPC directly — the W8-followup wrapper `_test_seed_with_gas` is the sole client-callable path. |
| T-36-34 | Elevation of Privilege | _test_seed_with_gas wrapper misuse / GUC leak | mitigate | W8-followup wrapper enforces production-call safety: (a) EXECUTE revoked from PUBLIC/anon/authenticated and granted ONLY to service_role; (b) wrapper sets `app.test_mode` transactionally with third arg `true` = local-scope so the GUC does NOT persist beyond the function call frame (no leak to surrounding session); (c) the bare helper still raises 'forbidden' if anyone tries to call it directly with `app.test_mode` unset, so the wrapper is the only safe path even for service-role callers. Test-harness in 36-05 (`with-test-mode.ts`) MUST call `_test_seed_with_gas` exclusively; calling the bare helper is forbidden and verified by grep in 36-05 verification. |

**V13-3 declaration for this plan:** No file modified in this plan imports the native-review shim from a rating-aware code path. Wave 1 owns the gates that catch any such future regression.
</threat_model>

<verification>
- `cd leanshot && npx tsc -p tsconfig.app.json --noEmit` passes
- `cd leanshot && npm run lint` passes
- `cd leanshot && node --test eslint-rules/no-conditional-native-review.test.cjs` — P36 RuleTester block green
- `bash leanshot/scripts/check-no-conditional-native-review.sh` exits 0 on clean tree
- `cd leanshot && npm run test:unit -- supabase/migrations/__tests__/p36` — all 4 SQL test files green
- Migration filenames match strict 14-digit regex (`ls supabase/migrations/20270710000001_*` returns 1 match)
- `grep -c "nps_trigger_eligible: true" leanshot/src/lib/analytics/events.ts` ≥ 1
- `grep -c "copy_variant text" supabase/migrations/20270710000002_p36_review_prompt_history.sql` ≥ 1 (B2 fix verification)
- `grep -c "created_by.*auth.uid()" supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql` ≥ 1 (B1 fix verification)
- `grep -c "_test_seed_review_prompt_history" supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql` ≥ 1 (W8 fix verification)
- `grep -c "app.test_mode" supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql` ≥ 1 (W8 test-mode GUC gate)
- `grep -c "_test_seed_with_gas" supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql` ≥ 1 (W8-followup wrapper exists)
- `grep -cE "GRANT EXECUTE ON FUNCTION public\._test_seed_with_gas.*TO service_role" supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql` ≥ 1 (W8-followup grant)
- `grep -cE "REVOKE EXECUTE ON FUNCTION public\._test_seed_with_gas.*FROM (PUBLIC|anon|authenticated)" supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql` ≥ 1 (W8-followup revokes — pattern should match ≥1 line; ideally 3 separate REVOKE statements present)
- `grep -cE "set_config\('app\.test_mode',\s*'true',\s*true\)" supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql` ≥ 1 (W8-followup wrapper body sets GUC transaction-local)
</verification>

<success_criteria>
- 5 migrations land in correct timestamp order, no collision with existing 20270709000008_* tail
- 4 RLS / schema tests cover REVIEW-02 (rules CRUD via SECDEF), REVIEW-03 (history append-only), REVIEW-08 (catalog seed shape), and native_review_prompts platform CHECK
- V13-3 BLOCKER two-gate defense extended to cover P36 fixtures (D-03 + D-04 + D-21 verified)
- events.ts type carries the new flag; D-01 whitelist entries marked
- B1 fix verified: create_review_prompt_rule INSERT sets created_by = auth.uid()
- B2 fix verified: review_prompt_history has copy_variant text NULL column (consumed by Wave 4 funnel RPC + populated by Wave 2 step 10)
- W8 fix verified: _test_seed_review_prompt_history exists, gated by app.test_mode GUC, service_role-only EXECUTE
- W8-followup fix verified: _test_seed_with_gas wrapper exists in pg_proc, owned by postgres, EXECUTE granted ONLY to service_role (REVOKEd from PUBLIC/anon/authenticated), wrapper body PERFORMs `set_config('app.test_mode','true', true)` (transaction-local) then delegates to the bare helper. Bare helper still raises 'forbidden' if called directly without the GUC. After wrapper returns, GUC does not persist on the caller's session.
- No file modified outside `files_modified` (no leak to main per `feedback_worktree_executor_pwd_drift_leaks_to_main`)
</success_criteria>

<output>
After completion, create `.planning/phases/36-m3-review-prompt-engine-web-only/36-01-SUMMARY.md` per template, including:
- Migration timestamps used (for downstream collision-precheck)
- Confirmation V13-3 RuleTester block name + grep gate exit-code on clean tree
- events.ts flag count for downstream rule-builder picker source-of-truth
- Confirmation of B1 (created_by=auth.uid()), B2 (copy_variant column), W8 (test seed helper), W8-followup (_test_seed_with_gas wrapper + service_role-only grants) presence — grep counts from <verification>
</output>
</content>
</invoke>