---
phase: 36
plan: 05
type: execute
wave: 5
depends_on: [36-01, 36-02, 36-03, 36-04]
files_modified:
  - supabase/functions/nps-trigger-decide/deno.json
  - supabase/functions/nps-feedback-submit/deno.json
  - supabase/functions/nps-cta-click-log/deno.json
  - leanshot/e2e/nps-cooldown-multi-device.spec.ts
  - leanshot/e2e/nps-ab-variant.spec.ts
  - leanshot/e2e/admin/reviews-rule-builder.spec.ts
  - leanshot/e2e/_helpers/with-test-mode.ts
  - leanshot/.planning/phases/36-m3-review-prompt-engine-web-only/36-CARRY-OVER.md
autonomous: false
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, REVIEW-04, REVIEW-05, REVIEW-06, REVIEW-07, REVIEW-08]
tags: [closeout, db-push, e2e, human-uat, posthog, vendor-claim]
user_setup:
  - service: trustpilot
    why: "Promoter consumer CTA target (D-16 vendor pre-req)"
    dashboard_config:
      - task: "Claim Trustpilot business profile for leanshot.app"
        location: "https://business.trustpilot.com/signup"
  - service: g2
    why: "Promoter clinic CTA target (D-16)"
    dashboard_config:
      - task: "Claim G2 vendor profile"
        location: "https://sell.g2.com"
  - service: capterra
    why: "Promoter clinic CTA target (D-16)"
    dashboard_config:
      - task: "Claim Capterra vendor profile"
        location: "https://www.capterra.com/vendors"
  - service: posthog
    why: "A/B variant resolution + Ship-Winner flag-flip (D-19 Claude's Discretion)"
    env_vars:
      - name: POSTHOG_PERSONAL_API_KEY
        source: "PostHog Dashboard → Project Settings → Personal API Keys"
    dashboard_config:
      - task: "Create PostHog Experiment on 'nps_prompt_copy' feature flag with control + variant_a payloads"
        location: "PostHog Experiments dashboard"

must_haves:
  truths:
    - "All 6 Phase 36 migrations are pushed to linked Supabase project (rules, history-with-copy_variant, native, catalog, secdef_rpcs-with-test-seed-helper-and-wrapper, funnel_aggregate)"
    - "3 Edge Fns deployed to linked Supabase project (nps-trigger-decide, nps-feedback-submit, nps-cta-click-log)"
    - "E2E Playwright multi-device cooldown spec passes; history seeding goes through `_test_seed_with_gas` (W8-followup — wrapper shipped in 36-01) called from a service-role admin client; the bare `_test_seed_review_prompt_history` is NEVER called directly from any e2e helper or spec"
    - "E2E A/B variant spec passes (or skipped with PostHog vendor-gate fallback)"
    - "Admin rule-builder E2E passes against the just-deployed admin module"
    - "Bundle budgets respected: consumer NPS chunk ≤6 kB gz, admin chunk delta ≤12 kB gz (counts toward admin-shell 30 kB ceiling)"
    - "Post-push smoke confirms BOTH `_test_seed_review_prompt_history` AND `_test_seed_with_gas` exist in remote pg_proc; the wrapper has EXECUTE granted only to service_role"
    - "CARRY-OVER.md documents any HUMAN-UAT items that did not land at phase close (Trustpilot/G2/Capterra claims, PostHog experiment creation, multi-device E2E if seed-blocked)"
  artifacts:
    - path: "leanshot/e2e/nps-cooldown-multi-device.spec.ts"
      provides: "REVIEW-03 multi-device cooldown E2E (D-08); history seeding via `_test_seed_with_gas` SECDEF wrapper (W8-followup)"
    - path: "leanshot/e2e/_helpers/with-test-mode.ts"
      provides: "service-role admin client + `seedReviewPromptHistory` helper that invokes `_test_seed_with_gas` (W8-followup — wrapper sets app.test_mode GUC transaction-local server-side; helper never references the bare _test_seed_review_prompt_history)"
      contains: "_test_seed_with_gas"
    - path: "leanshot/e2e/nps-ab-variant.spec.ts"
      provides: "REVIEW-06 PostHog variant E2E"
    - path: "leanshot/e2e/admin/reviews-rule-builder.spec.ts"
      provides: "REVIEW-02 admin rule CRUD E2E"
    - path: "leanshot/.planning/phases/36-m3-review-prompt-engine-web-only/36-CARRY-OVER.md"
      provides: "Documented HUMAN-UAT carry-overs (Trustpilot/G2/Capterra claims, PostHog experiment, multi-device E2E status)"
      contains: "CARRY-OVER"
  key_links:
    - from: "supabase/migrations/20270710000001..06_p36_*"
      to: "linked Supabase project"
      via: "supabase db push --linked"
      pattern: "supabase db push"
    - from: "supabase/functions/nps-{trigger-decide,feedback-submit,cta-click-log}"
      to: "linked Supabase project"
      via: "supabase functions deploy <name>"
      pattern: "functions deploy"
    - from: "leanshot/e2e/_helpers/with-test-mode.ts"
      to: "public._test_seed_with_gas (36-01 Task 1 W8-followup wrapper)"
      via: "service-role `admin.rpc('_test_seed_with_gas', {...})`; wrapper server-side PERFORMs `set_config('app.test_mode','true', true)` then delegates to `_test_seed_review_prompt_history`. Client/helper code NEVER calls the bare helper directly."
      pattern: "_test_seed_with_gas"
    - from: "leanshot/e2e/nps-cooldown-multi-device.spec.ts"
      to: "leanshot/e2e/_helpers/with-test-mode.ts"
      via: "import { makeAdminClient, seedReviewPromptHistory } from './_helpers/with-test-mode'"
      pattern: "seedReviewPromptHistory"
---

<objective>
Push all Phase 36 schema + Edge Fns to the linked Supabase project, run the cross-phase smoke E2Es (multi-device cooldown, A/B variant, admin rule CRUD), verify bundle ceilings, and surface any HUMAN-UAT carry-overs (vendor profile claims, PostHog experiment creation).

Purpose: Close the gate from "code merged" to "feature live in production-equivalent environment". Phase cannot pass verification without `supabase db push --linked` and the 3 Edge Fn deploys (per orchestrator `schema_push_requirement`).
Output: 6 migrations applied + 3 Edge Fns deployed + 3 E2E spec files + with-test-mode.ts helper invoking the W8-followup wrapper + CARRY-OVER.md if any deferrals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/36-m3-review-prompt-engine-web-only/36-CONTEXT.md
@leanshot/.planning/phases/36-m3-review-prompt-engine-web-only/36-RESEARCH.md
@leanshot/.planning/phases/36-m3-review-prompt-engine-web-only/36-01-PLAN.md
@leanshot/.planning/phases/36-m3-review-prompt-engine-web-only/36-02-PLAN.md
@leanshot/.planning/phases/36-m3-review-prompt-engine-web-only/36-03-PLAN.md
@leanshot/.planning/phases/36-m3-review-prompt-engine-web-only/36-04-PLAN.md
@leanshot/playwright.config.ts
@supabase/functions/ship-winner-flag/deno.json

<interfaces>
DB push command (per `reference_supabase_db_query_linked` + project memory):
```bash
supabase db push --linked
# Pre-check: ensure SUPABASE_ACCESS_TOKEN env var is set (non-TTY workaround)
# Pre-check: glob 20270710* against remote applied list to catch collisions per [[reference_migration_timestamp_collision_precheck]]
# Pre-check: ls supabase/migrations/20270710*.sql | wc -l should equal 6 (5 from Wave 1 + 1 from Wave 4)
```

Edge Fn deploy command (per `reference_supabase_functions_deploy_no_linked_flag`):
```bash
# CLI v2.100.0+: NO --linked flag (auto-read from supabase/.temp/)
supabase functions deploy nps-trigger-decide --import-map supabase/functions/import_map.json
supabase functions deploy nps-feedback-submit --import-map supabase/functions/import_map.json
supabase functions deploy nps-cta-click-log --import-map supabase/functions/import_map.json
```

Bundle ceiling check (per UI-SPEC Performance + Bundle Budget):
```bash
cd leanshot && npm run build && du -sh dist/assets/*.js | sort -h | tail -5
# Look for the nps chunk (likely in App lazy bundle); confirm consumer modal code ≤6 kB gz.
# Look for admin chunk delta vs pre-Phase-36 baseline; ≤12 kB gz contribution.
```

E2E specs (Playwright):
- `e2e/nps-cooldown-multi-device.spec.ts`: 2 browser contexts as same user; user fires an admissible event in context A → NPS modal appears; dismisses; fires same event in context B within global cooldown window → modal does NOT appear (D-08 server-side cooldown respect).
- `e2e/nps-ab-variant.spec.ts`: stub PostHog flag response → first user sees control variant copy; second user (different distinct_id) sees variant_a copy; admin Ship-Winner → after PATCH, third user sees variant_a as new default.
- `e2e/admin/reviews-rule-builder.spec.ts`: admin logs in (via test admin user fixture) → navigates to /admin/reviews/rules → creates rule with trigger_event + cohort → rule appears in list → edits + saves → deletes via Confirm → rule removed.

W8-followup — History seeding for the multi-device cooldown E2E:
The cooldown spec needs to pre-seed `review_prompt_history` rows BEFORE the test runs (e.g., 4 prior fires for the user to assert global cooldown semantics). The LOCKED path (per 36-01 W8-followup) is the SECDEF wrapper `_test_seed_with_gas`, which is the ONLY callable path for test-harness seeding — it sets `app.test_mode` GUC server-side (transaction-local) then delegates to the bare helper. The bare helper still raises 'forbidden' without the GUC, so even if someone accidentally referenced it, the inner gate prevents inserts. Helper code MUST NOT reference `_test_seed_review_prompt_history` directly.

Reference invocation (single RPC call, no extra GUC plumbing on the client):

```typescript
const adminClient = createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY);
const { data, error } = await adminClient.rpc('_test_seed_with_gas', {
  p_user_id: testUser.id,
  p_rule_id: testRule.id,
  p_fired_at: new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString(),
  p_rating_value: null,
  p_copy_variant: 'control',
});
// The wrapper sets app.test_mode transactionally inside its own body, so no
// pre-call set_config / postgrest header gymnastics are required from the helper.
```

The wrapper file (test-only) lives at `leanshot/e2e/_helpers/with-test-mode.ts` and is shared across this spec set. The wrapper exists in 36-01's 20270710000005_p36_review_secdef_rpcs.sql migration with EXECUTE granted only to service_role (REVOKEd from PUBLIC / anon / authenticated). No "escalate to 36-01" path is needed — 36-01 already ships the wrapper.

CARRY-OVER.md template (when items deferred):
```
# Phase 36 — Carry-Over Items

## HUMAN-UAT pending at phase close
- [ ] Trustpilot profile claim — assigned to founder
- [ ] G2 profile claim — assigned to founder
- [ ] Capterra profile claim — assigned to founder
- [ ] PostHog Experiment 'nps_prompt_copy' creation — Wave 5 task 3 checkpoint
- [ ] Multi-device cooldown E2E — DEFERRED if Phase 35 trigger events not yet seeded

## Auto-verify-only items shipped
- Server-side cooldown verified via Deno unit tests (Wave 2).
- Surface B vendor-gate fallback renders correctly with empty cta_set (Wave 3 unit test).

Per [[milestone_uat_deferral_consolidation]]: roll these into milestone-uat-deferred.md at v1.3 close if any remain unclaimed.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] supabase db push --linked + Edge Fn deploys + post-push verification</name>
  <files>
    supabase/functions/nps-trigger-decide/deno.json,
    supabase/functions/nps-feedback-submit/deno.json,
    supabase/functions/nps-cta-click-log/deno.json
  </files>
  <read_first>
    .planning/phases/36-m3-review-prompt-engine-web-only/36-01-PLAN.md (migration timestamps + W8-followup `_test_seed_with_gas` wrapper),
    .planning/phases/36-m3-review-prompt-engine-web-only/36-04-PLAN.md (funnel-aggregate migration timestamp),
    supabase/functions/ship-winner-flag/deno.json (deno.json template for new Edge Fns),
    supabase/functions/cancellation-feedback-to-ticket/deno.json
  </read_first>
  <acceptance_criteria>
    - Pre-push: `ls supabase/migrations/20270710*.sql | wc -l` returns exactly 6 (5 Wave-1 + 1 Wave-4 funnel RPC).
    - Pre-push: no timestamp collision with existing remote migrations (glob check per `reference_migration_timestamp_collision_precheck`).
    - `supabase db push --linked` completes without error; output shows 6 migrations applied (or "Already in sync" if a prior partial push landed some).
    - 3 deno.json files exist for the new Edge Fns (per ship-winner-flag template).
    - `supabase functions deploy nps-trigger-decide --import-map supabase/functions/import_map.json` succeeds.
    - Same for nps-feedback-submit and nps-cta-click-log.
    - Post-deploy: `supabase functions list` includes all 3 new Fns.
    - Post-push verification: `supabase db query --linked` to assert `review_prompt_rules`, `review_prompt_history`, `native_review_prompts`, `review_cta_catalog` tables exist + `review_cta_catalog` has 5 rows seeded + `review_funnel_aggregate` function exists in pg_proc + BOTH `_test_seed_review_prompt_history` AND `_test_seed_with_gas` functions exist in pg_proc (W8-followup) + `_test_seed_with_gas` EXECUTE grants in pg_proc_acl show only service_role (no anon, no authenticated) + `review_prompt_history` has a `copy_variant` column.
  </acceptance_criteria>
  <action>
This is the [BLOCKING] schema-push task per the orchestrator's `schema_push_requirement`. Phase 36 cannot pass verification without it.

Step 1 — Create deno.json for each new Edge Fn (template per `supabase/functions/ship-winner-flag/deno.json`): each contains `{ "imports": { ... } }` per the project's import-map style OR is empty if the project relies solely on `supabase/functions/import_map.json`. Verify ship-winner-flag's deno.json shape at execute-time and mirror exactly.

Step 2 — Pre-push collision check:
- Run `ls supabase/migrations/20270710*.sql | wc -l` — assert returns 6.
- Run `supabase db query --linked "SELECT version FROM supabase_migrations.schema_migrations WHERE version LIKE '20270710%' ORDER BY version"` — assert returns 0 rows OR matches what's about to be pushed (idempotency).
- Per project memory `reference_supabase_back_dated_migration_blocks_push`: verify all 20270710* timestamps are strictly greater than `max(version)` on remote. Latest at plan-time: `20270709000008_p40_roi_view.sql` — 20270710000001+ is safely ahead.

Step 3 — Push (one-shot):
```
SUPABASE_ACCESS_TOKEN=<token> supabase db push --linked
```
The token comes from the operator's environment OR macOS keychain (per `reference_stripe_legacy_key_and_supabase_token`). The agent should attempt with the existing CLI session first; if it errors with "Cannot find project ref", verify `supabase/.temp/` is present (per `reference_supabase_worktree_temp_state`).

Step 4 — Deploy Edge Fns (one at a time; do not parallelize — Supabase CLI rate-limits):
```
supabase functions deploy nps-trigger-decide --import-map supabase/functions/import_map.json
supabase functions deploy nps-feedback-submit --import-map supabase/functions/import_map.json
supabase functions deploy nps-cta-click-log --import-map supabase/functions/import_map.json
```
Per `reference_supabase_functions_deploy_no_linked_flag`: do NOT pass `--linked` flag (CLI v2.100.0+ rejects it). Per `reference_supabase_functions_deploy_import_map_flag`: KEEP `--import-map` despite the deprecation warning (still required for the `_shared/` aliases).

Step 5 — Post-push smoke (live read-only via `supabase db query --linked`):
- `SELECT count(*) FROM public.review_cta_catalog;` → expect 5.
- `SELECT to_regclass('public.review_prompt_rules');` → not null.
- `SELECT to_regclass('public.review_prompt_history');` → not null.
- `SELECT to_regclass('public.native_review_prompts');` → not null.
- `SELECT 1 FROM pg_proc WHERE proname='review_funnel_aggregate';` → returns row.
- `SELECT 1 FROM pg_proc WHERE proname='_test_seed_review_prompt_history';` → returns row (W8 — confirms bare helper shipped).
- `SELECT 1 FROM pg_proc WHERE proname='_test_seed_with_gas';` → returns row (W8-followup — confirms wrapper shipped).
- `SELECT count(*) FROM pg_proc WHERE proname IN ('_test_seed_review_prompt_history','_test_seed_with_gas');` → returns 2.
- `SELECT proacl FROM pg_proc WHERE proname='_test_seed_with_gas';` → proacl entries include service_role=X (EXECUTE); do NOT include anon or authenticated. Equivalent check via information_schema.routine_privileges: `SELECT grantee FROM information_schema.routine_privileges WHERE routine_name='_test_seed_with_gas' AND privilege_type='EXECUTE'` returns ONLY `service_role` (no `anon`, no `authenticated`, no `PUBLIC`).
- `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='review_prompt_history' AND column_name='copy_variant';` → returns row (B2 — confirms column shipped).
- `supabase functions list` (or curl `/functions/v1/<name>` with OPTIONS to confirm CORS responds) → 3 new fns present.

If ANY step fails: do NOT proceed; halt and write the error to the carry-over doc + STATE.md per the orchestrator's blocker protocol.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/migrations/20270710*.sql | wc -l && supabase db query --linked "SELECT count(*) AS catalog_rows FROM public.review_cta_catalog" 2>&1 | tail -5 && supabase db query --linked "SELECT count(*) AS test_seed_fns FROM pg_proc WHERE proname IN ('_test_seed_review_prompt_history','_test_seed_with_gas')" 2>&1 | tail -5 && supabase db query --linked "SELECT grantee FROM information_schema.routine_privileges WHERE routine_name='_test_seed_with_gas' AND privilege_type='EXECUTE' ORDER BY grantee" 2>&1 | tail -10</automated>
  </verify>
  <done>
    Migration count exactly 6 on disk; catalog_rows == 5 in remote DB; copy_variant column present on review_prompt_history; BOTH `_test_seed_review_prompt_history` AND `_test_seed_with_gas` functions present in pg_proc (count = 2); `_test_seed_with_gas` EXECUTE grant restricted to `service_role` only (information_schema.routine_privileges shows exactly one grantee, `service_role`); 3 Edge Fns visible in `supabase functions list`; no deploy errors logged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3 E2E specs + bundle budget assertion (W8-followup — multi-device spec uses _test_seed_with_gas wrapper exclusively)</name>
  <files>
    leanshot/e2e/nps-cooldown-multi-device.spec.ts,
    leanshot/e2e/nps-ab-variant.spec.ts,
    leanshot/e2e/admin/reviews-rule-builder.spec.ts,
    leanshot/e2e/_helpers/with-test-mode.ts
  </files>
  <read_first>
    leanshot/playwright.config.ts,
    leanshot/e2e/ (existing E2E specs for fixture patterns — e.g., any cancellation or auth flow spec),
    .planning/phases/36-m3-review-prompt-engine-web-only/36-01-PLAN.md (Task 1 — `_test_seed_with_gas` wrapper signature: p_user_id uuid, p_rule_id uuid, p_fired_at timestamptz, p_rating_value smallint, p_copy_variant text DEFAULT NULL — SECDEF wrapper sets `app.test_mode` GUC transaction-local server-side then delegates to the bare helper; EXECUTE granted only to service_role),
    .planning/phases/36-m3-review-prompt-engine-web-only/36-RESEARCH.md (Decision-to-Test Map),
    .planning/phases/36-m3-review-prompt-engine-web-only/36-UI-SPEC.md (Surface A/D selectors)
  </read_first>
  <behavior>
    - Multi-device cooldown spec: 2 browser contexts; same user; pre-seeded with N=4 prior fires via `seedReviewPromptHistory` helper which invokes `_test_seed_with_gas`; fire admissible event in context A → NPSPromptModal does NOT open if cooldown still active; verify global cooldown semantics + lifetime cap path end-to-end.
    - A/B variant spec: stub `nps-trigger-decide` to return copy_variant='variant_a' for user X and 'control' for user Y → modal copy differs by user.
    - Admin rule-builder spec: admin login → /admin/reviews/rules → click "New rule" → fill name, pick trigger, pick cohort → save → rule appears in list → click Edit → modify → save → click Delete → confirm → rule removed.
    - Bundle budget: `npm run build` succeeds; bundle-budget script (existing per `reference_bundle_budget_hash_hyphen`) passes for nps chunk ≤6 kB gz, admin delta ≤12 kB gz.
    - W8-followup — `_helpers/with-test-mode.ts` exists, exports `makeAdminClient()` and `seedReviewPromptHistory()`. `seedReviewPromptHistory` calls `admin.rpc('_test_seed_with_gas', {...})` (the wrapper from 36-01 W8-followup). The helper MUST NOT reference `_test_seed_review_prompt_history` (the bare helper) anywhere — verified by grep.
  </behavior>
  <action>
**W8-followup — _helpers/with-test-mode.ts (NEW)**:

The wrapper `_test_seed_with_gas` ships in 36-01's 20270710000005 migration with EXECUTE granted only to service_role. It sets `app.test_mode` GUC transaction-local server-side, so the helper here makes a single RPC call with no extra GUC / PostgREST header plumbing. The bare `_test_seed_review_prompt_history` MUST NOT appear in this file.

Implementation:
```typescript
// leanshot/e2e/_helpers/with-test-mode.ts
import { createClient, SupabaseClient } from '@supabase/supabase-js';

export function makeAdminClient(): SupabaseClient {
  return createClient(
    process.env.SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { persistSession: false } },
  );
}

/**
 * Seed a row into review_prompt_history via the `_test_seed_with_gas` SECDEF
 * wrapper (shipped in 36-01 Task 1, W8-followup). The wrapper sets the
 * `app.test_mode` GUC transaction-local on the server side and then delegates
 * to the bare `_test_seed_review_prompt_history` helper. Do NOT call the bare
 * helper directly — it raises 'forbidden' without the GUC, and EXECUTE on the
 * wrapper is granted ONLY to service_role.
 */
export async function seedReviewPromptHistory(
  admin: SupabaseClient,
  args: {
    userId: string;
    ruleId: string;
    firedAt: string; // ISO-8601
    ratingValue?: number | null;
    copyVariant?: string | null;
  },
): Promise<string> {
  const { data, error } = await admin.rpc('_test_seed_with_gas', {
    p_user_id: args.userId,
    p_rule_id: args.ruleId,
    p_fired_at: args.firedAt,
    p_rating_value: args.ratingValue ?? null,
    p_copy_variant: args.copyVariant ?? null,
  });
  if (error) throw error;
  return data as string;
}
```

This is the LOCKED path (no path-a/b/c hedging). The wrapper exists in 36-01; no "escalate to 36-01" pivot is needed at execution time.

**nps-cooldown-multi-device.spec.ts** (W8-followup wired):
Use `browser.newContext()` twice to get 2 separate storage states, both authenticated as the same test user. BEFORE either context navigates:
1. `const admin = makeAdminClient(); const ruleId = '<seed-rule-id>';`
2. Seed 4 prior history rows for the test user at `now() - 30/45/60/70 days` via `seedReviewPromptHistory(admin, ...)`. Last fired_at < 60d so global cooldown is active.
3. In context A: navigate, fire the admissible event (`activation_completed` is always-present from Phase 34 — use that as the trigger). Assert NPSPromptModal does NOT appear (locator `getByRole('dialog', { name: /How likely are you to recommend/ })` → `toHaveCount(0)` with timeout).
4. In context B: same. Confirms multi-device respect — both contexts share the server-side cooldown via review_prompt_history.

If for any reason the test user fixture or rule fixture doesn't exist, fall back to `test.skip` with a deferred-tests.md entry per `feedback_defer_then_batch_fix_pattern`.

**nps-ab-variant.spec.ts**: Use Playwright's `page.route('**/functions/v1/nps-trigger-decide', ...)` to mock the Edge Fn response per-user. User X gets `copy_variant: 'variant_a'`; user Y gets `copy_variant: 'control'`. Assert the rendered modal body differs (e.g., title text differs by variant if Wave 3 wired the variant prop, OR a data-variant attribute on the modal root reflects copy_variant). If Wave 3 did NOT wire variant into visible UI yet (only into analytics), assert the variant via `data-copy-variant` attribute the modal sets internally. This spec is a hard E2E and may be deferred if PostHog stub is brittle — surface to CARRY-OVER.md if so.

**reviews-rule-builder.spec.ts**: Standard CRUD spec under `e2e/admin/`. Use the existing admin login fixture (verify it exists — if not, use the test admin email/password from project env). Navigate to `/admin/reviews/rules`. Click "New rule". Fill name. Select trigger from dropdown (use `selectOption` on a `select` if `<select>` is used, OR click open the picker if it's a custom popover). Select a cohort or "All users". Click Save. Assert rule appears in list. Click Edit → modify name → Save → assert update. Click Delete → confirm → assert row removed.

Per `reference_playwright_conditional_project_argv`: if any conditional project is used in playwright config, use an env var (`PLAYWRIGHT_RUN_P36=1`) NOT argv detection.

After spec creation, run the bundle build:
- `cd leanshot && npm run build` should succeed.
- Check bundle sizes against the consumer-NPS ≤6 kB gz and admin-shell ≤30 kB ceilings. Existing `scripts/assert-bundle-budgets.sh` (verify name at execute-time) should pass. If the admin module's chunk pushes admin-shell over 30 kB, surface a BLOCKER and recommend splitting the admin module's lazy imports further per UI-SPEC Performance section.

Per project memory `reference_playwright_mcp_env_gotchas`: do not rely on MCP screenshots — use DOM assertions only.

**W8-followup verification — greps inside the leanshot/e2e/ tree:**
- `grep -rnE "rpc\('_test_seed_with_gas'" leanshot/e2e/_helpers/with-test-mode.ts` returns ≥1 line (helper invokes the wrapper).
- `grep -rnE "rpc\('_test_seed_review_prompt_history'" leanshot/e2e/` returns 0 lines (helper / specs MUST NOT call the bare helper directly).
- `grep -rn "_test_seed_review_prompt_history" leanshot/e2e/` returns 0 lines OR matches only inside comments/docstrings (operator can confirm via diff inspection; the strict gate is the rpc-call grep above).
  </action>
  <verify>
    <automated>cd leanshot && npm run build && (grep -cE "rpc\('_test_seed_with_gas'" e2e/_helpers/with-test-mode.ts) && (! grep -rqE "rpc\('_test_seed_review_prompt_history'" e2e/ || (echo "FAIL: bare helper RPC call found in e2e/" && exit 1)) && npm run test:e2e -- e2e/nps-cooldown-multi-device.spec.ts e2e/nps-ab-variant.spec.ts e2e/admin/reviews-rule-builder.spec.ts 2>&1 | tail -40</automated>
  </verify>
  <done>
    Build succeeds + bundle budget script (if present) exits 0; the 3 E2E spec files exist (either passing OR explicitly skipped via `test.skip` with a deferred-tests.md entry per `feedback_defer_then_batch_fix_pattern` if blocked by missing trigger seed / PostHog stub brittleness); `_helpers/with-test-mode.ts` exists; `grep -cE "rpc\('_test_seed_with_gas'" e2e/_helpers/with-test-mode.ts` returns ≥1 (W8-followup — helper invokes the wrapper); `grep -rE "rpc\('_test_seed_review_prompt_history'" e2e/` returns 0 (W8-followup — bare helper is NEVER called directly from any e2e file); the multi-device spec invokes `seedReviewPromptHistory` (which in turn calls `_test_seed_with_gas`).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-UAT — vendor profile claims + PostHog experiment + multi-signal sign-off</name>
  <what-built>
    All Phase 36 code is shipped + deployed. Server-side cooldown verified via Deno tests. Consumer modals + admin module pass unit + E2E tests. Vendor pre-reqs (Trustpilot/G2/Capterra profile claims, PostHog Experiment creation) are HUMAN-only steps that block live promoter CTA fires + Ship-Winner attribution.
  </what-built>
  <how-to-verify>
    Per `feedback_multi_signal_human_verify_checkpoint_pattern`, this checkpoint has 4 discrete resume signals — approve each independently or defer to milestone close.

    **Signal 1 (CLI-verifiable inline by agent): live deploy smoke**
    Run as the operator (or paste in shell):
    ```
    supabase db query --linked "SELECT count(*) FROM public.review_cta_catalog"
    # Expect: 5
    supabase db query --linked "SELECT count(*) FROM pg_proc WHERE proname IN ('_test_seed_review_prompt_history','_test_seed_with_gas')"
    # Expect: 2 (W8-followup)
    supabase db query --linked "SELECT grantee FROM information_schema.routine_privileges WHERE routine_name='_test_seed_with_gas' AND privilege_type='EXECUTE' ORDER BY grantee"
    # Expect: exactly one row, grantee = service_role
    supabase functions list | grep -E "nps-(trigger-decide|feedback-submit|cta-click-log)"
    # Expect: 3 lines
    curl -sS -o /dev/null -w "%{http_code}" -X OPTIONS https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/nps-trigger-decide
    # Expect: 204 or 200 (CORS preflight)
    ```

    **Signal 2 (vendor — async, founder action): Trustpilot/G2/Capterra profile claims (D-16)**
    For each platform, the founder must:
    1. Trustpilot: https://business.trustpilot.com/signup → claim leanshot.app domain.
    2. G2: https://sell.g2.com → claim LeanShot vendor profile.
    3. Capterra: https://www.capterra.com/vendors → claim LeanShot listing.
    After each claim, update `review_cta_catalog.claimed=true`:
    ```
    supabase db query --linked "UPDATE public.review_cta_catalog SET claimed=true WHERE slug='trustpilot'"
    # Repeat for g2, capterra
    ```
    Until all 3 land, Surface B fallback renders "Thanks for the rating!" no-CTA per UI-SPEC vendor-block fallback. Defer to milestone close if any unclaimed — per `feedback_hitl_walkthrough_deferred_when_fixtures_missing`.

    **Signal 3 (vendor — founder action): PostHog Experiment creation (REVIEW-06 / D-19)**
    1. Log into PostHog dashboard.
    2. Create new Experiment on feature flag `nps_prompt_copy` with payloads `{ "variant": "control" }` and `{ "variant": "variant_a", "title_override": "Help us improve LeanShot" }` (or similar — text TBD).
    3. Confirm `POSTHOG_PERSONAL_API_KEY` is set as a Supabase Function Secret (per `reference_vapid_keypair_supabase_setup`):
    ```
    supabase secrets list | grep POSTHOG_PERSONAL_API_KEY
    ```
    If missing, set:
    ```
    supabase secrets set --project-ref ytnsipxxmzgaebkqmokp POSTHOG_PERSONAL_API_KEY=<value>
    ```
    Until this lands, Ship-Winner button returns 503 with the existing vendor-gated soft banner (OnboardingABPanel precedent) — non-blocking.

    **Signal 4 (browser walkthrough — founder, ~5 min): consumer modal smoke**
    1. Sign in as a test consumer user with no NPS history.
    2. Trigger an admissible event (e.g., complete the activation flow if not yet activated).
    3. NPSPromptModal should appear at App root.
    4. Rate 5★ → PromoterCtaModal opens with Trustpilot button (consumer cohort per D-14) OR fallback copy if Trustpilot not yet claimed.
    5. Click "Not now" → modal dismisses.
    6. (Optional) Sign in as a different test consumer; rate 1★ → DetractorFeedbackModal opens; submit feedback → success state appears; verify ticket lands in /admin/helpdesk inbox with the `nps-feedback` tag (via ticket_tags JOIN-TABLE INSERT — verified server-side per W6 fix in 36-02).

    Per `reference_zustand_persisted_user_blocks_marketing_uat`: clear `localStorage.leanshot_v4` between user-switches to break the persisted-user shortcut.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved all" → all 4 signals confirmed; phase ships clean.
    - "approved signals 1 + 4; signals 2 + 3 deferred to milestone close" → operator confirms deploy + browser smoke; vendor + PostHog deferred. Agent writes CARRY-OVER.md for signals 2 + 3.
    - "approved signal 1 only — automated-verify-only ship" → per `feedback_hitl_walkthrough_deferred_when_fixtures_missing`. Agent writes CARRY-OVER.md for signals 2 + 3 + 4; phase marked complete with HUMAN-UAT deferral.
    - "issue: <description>" → halt; report inline.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→Supabase production | `supabase db push --linked` + `supabase functions deploy` execute against the live linked project. Migration filename regex + timestamp collision pre-check guard against bad pushes. |
| browser→production Edge Fns | Post-deploy, all 3 Fns are reachable at `/functions/v1/<name>`; auth requirements verified by smoke. |
| operator→vendor portals | Trustpilot/G2/Capterra/PostHog interactions are out-of-band; carry-over doc tracks status. |
| E2E test-harness→Supabase | Service-role admin client invokes `_test_seed_with_gas` SECDEF wrapper (W8-followup) which sets `app.test_mode` GUC server-side (transaction-local) and delegates to the bare helper. Wrapper EXECUTE is restricted to service_role; bare helper still raises 'forbidden' without the GUC, so the wrapper is the only safe path even for service-role callers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-28 | Tampering | back-dated migration blocking push | mitigate | Pre-push check on timestamp collisions + strict >max(remote) per `reference_supabase_back_dated_migration_blocks_push`. Operator-fix recipe documented in CARRY-OVER if it triggers. |
| T-36-29 | Repudiation | partial deploy (some Fns up, some down) | mitigate | Serial deploy (not parallel); each verified via `supabase functions list` post-deploy; deploy errors halt the task per blocker protocol. |
| T-36-30 | Information Disclosure | vendor profile spoofing (someone else claims Trustpilot/G2/Capterra first) | accept | Founder claims via business email + domain ownership proof. Out of scope for this code phase. |
| T-36-31 | Tampering | non-authoritative PostHog flag injection | mitigate | Ship-Winner Edge Fn already gates on superadmin role (Phase 34 D-20); no additional surface in this plan exposes flag-flip. |
| T-36-32 | Tampering / vendor policy | post-deploy V13-3 regression smuggle via hot-patch | mitigate | V13-3 ESLint rule + grep gate run on every commit + bundle build. Post-deploy verification includes a final `bash scripts/check-no-conditional-native-review.sh`. |
| T-36-39 | Elevation of Privilege | _test_seed_with_gas wrapper misuse in production | mitigate | Wrapper EXECUTE restricted to service_role (REVOKEd from PUBLIC/anon/authenticated). Wrapper sets `app.test_mode` transactionally with third arg `true` (local scope) so the GUC does not persist beyond the function-call frame. Bare `_test_seed_review_prompt_history` still raises 'forbidden' without the GUC, so the wrapper is the only safe path. Helper code in `e2e/_helpers/with-test-mode.ts` calls the wrapper exclusively — verified by grep (Task 2 verify block); calling the bare helper from any e2e file is forbidden. |

**V13-3 declaration for this plan:** This plan ships no application code that conditions native review on rating. Wave 1 ESLint + grep gates remain in effect for all post-deploy hot-patches.
</threat_model>

<verification>
- `ls /Users/karstenhaldan/minisite/supabase/migrations/20270710*.sql | wc -l` returns 6
- `supabase db query --linked "SELECT count(*) FROM public.review_cta_catalog"` returns 5
- `supabase db query --linked "SELECT to_regclass('public.review_prompt_rules')"` returns non-null
- `supabase db query --linked "SELECT 1 FROM pg_proc WHERE proname='_test_seed_review_prompt_history'"` returns 1 (W8)
- `supabase db query --linked "SELECT 1 FROM pg_proc WHERE proname='_test_seed_with_gas'"` returns 1 (W8-followup wrapper)
- `supabase db query --linked "SELECT count(*) FROM pg_proc WHERE proname IN ('_test_seed_review_prompt_history','_test_seed_with_gas')"` returns 2 (W8-followup — both functions present)
- `supabase db query --linked "SELECT grantee FROM information_schema.routine_privileges WHERE routine_name='_test_seed_with_gas' AND privilege_type='EXECUTE'"` returns ONLY `service_role` (no anon, no authenticated, no PUBLIC) (W8-followup grant restriction)
- `supabase db query --linked "SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='review_prompt_history' AND column_name='copy_variant'"` returns 1 row (B2)
- `supabase functions list` includes nps-trigger-decide, nps-feedback-submit, nps-cta-click-log
- `cd leanshot && npm run build` succeeds + bundle budget script exits 0
- `cd leanshot && bash scripts/check-no-conditional-native-review.sh` exits 0 (final V13-3 gate)
- 3 E2E spec files exist; either pass OR explicitly `test.skip` with a deferred-tests.md entry
- `grep -cE "rpc\('_test_seed_with_gas'" leanshot/e2e/_helpers/with-test-mode.ts` returns ≥1 (W8-followup — helper wires the wrapper)
- `grep -rE "rpc\('_test_seed_review_prompt_history'" leanshot/e2e/` returns 0 lines (W8-followup — bare helper is NEVER called from e2e)
- CARRY-OVER.md created if any HUMAN-UAT signal deferred
</verification>

<success_criteria>
- All 6 Phase 36 migrations applied to linked Supabase project (no errors)
- All 3 Edge Fns deployed and reachable (CORS preflight returns 200/204)
- Bundle ceilings respected (consumer NPS ≤6 kB gz, admin delta ≤12 kB gz)
- 3 E2E specs land (passing OR explicitly deferred with a CARRY-OVER.md entry)
- W8 closed: multi-device cooldown spec uses the locked test-seed path; production callers cannot abuse it (GUC gate inside the bare helper + EXECUTE-restricted wrapper)
- W8-followup closed: `_test_seed_with_gas` SECDEF wrapper present in remote pg_proc with EXECUTE granted only to service_role; helper code (`e2e/_helpers/with-test-mode.ts`) invokes the wrapper exclusively; bare `_test_seed_review_prompt_history` is NEVER referenced from any e2e file (greps return 0)
- HUMAN-UAT 4-signal checkpoint resolved (approved all OR approved subset with CARRY-OVER.md documenting deferrals)
- All 8 REVIEW-NN REQ-IDs trace to at least one task that has shipped or has a documented deferral path
</success_criteria>

<output>
After completion, create `.planning/phases/36-m3-review-prompt-engine-web-only/36-05-SUMMARY.md` per template, including:
- Migration push timestamp + applied list
- Edge Fn deploy URLs (`https://<project>.supabase.co/functions/v1/<name>`)
- Bundle size measurements (consumer NPS chunk + admin delta)
- E2E spec status per file (pass / skip with reason / fail)
- HUMAN-UAT signal status (1/4, 2/4, 3/4, 4/4 approved)
- Iter-1 fix closures: B1 ✓ (created_by set in SECDEF RPC), B2 ✓ (copy_variant column live in remote DB), W3 ✓ (primary_org_id), W4 ✓ (ANALYTICS_TRIGGER_EVENT shipped), W5 ✓ (funnel direct read), W6 ✓ (ticket_tags JOIN), W7 ✓ (sliding 90d), W8 ✓ (test seed helper live in pg_proc), W8-followup ✓ (`_test_seed_with_gas` wrapper live in pg_proc with service_role-only EXECUTE; e2e helper calls wrapper exclusively, bare helper grep-confirmed absent from e2e/), W9 ✓ (cta_set carries url_pattern)
- Carry-over count + link to CARRY-OVER.md if present
</output>
</content>
</invoke>