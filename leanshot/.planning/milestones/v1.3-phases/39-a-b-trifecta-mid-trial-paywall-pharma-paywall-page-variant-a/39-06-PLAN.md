---
phase: 39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-06-SUMMARY.md
  - leanshot/src/lib/admin/modules.ts
  - leanshot/src/components/admin/growth/ExperimentDashboardPage.tsx
  - leanshot/src/components/admin/growth/ExperimentDashboardPage.test.tsx
  - leanshot/src/components/admin/growth/experiment-types.ts
  - leanshot/src/components/admin/__tests__/AdminShell.test.tsx
autonomous: true
requirements:
  - PAYWALL-06
  - PAGEAB-01
  - PAGEAB-07
  - PHARMA-08
must_haves:
  truths:
    - "src/lib/admin/modules.ts has a new ADMIN_MODULES entry with key='growth-experiments', route='growth/experiments', icon=TrendingUpIcon, lazy import to ExperimentDashboardPage, flagKey='admin.growth.experiments.enabled', minRole='admin'"
    - "ExperimentDashboardPage renders 3 Pill tabs: Paywall / Page-Builder / Pharma per UI-SPEC Surface E"
    - "ExperimentDashboardPage uses useEffect+setInterval polling (NO TanStack Query) per UI-SPEC Hard Constraint 5"
    - "Sub-tab rendering is a switch on a 'paywall'|'page'|'pharma' state union; defaults to 'paywall'"
    - "Empty state copy matches UI-SPEC exactly (heading 'No active experiments yet.', body, 'Go to Pages' primary action)"
    - "Vendor-unconfigured soft banner appears when get_experiment_results RPC returns vendor_unconfigured (Pattern A)"
    - "AdminShell.test.tsx is extended to assert the new growth/experiments route resolves via the manifest-driven branch (mitigates [[feedback_admin_module_manifest_vs_router_branch_drift]])"
  artifacts:
    - path: "leanshot/src/lib/admin/modules.ts"
      provides: "ADMIN_MODULES entry for growth/experiments"
      contains: "growth-experiments"
    - path: "leanshot/src/components/admin/growth/ExperimentDashboardPage.tsx"
      provides: "Admin Surface E shell + 3 Pill tabs"
      contains: "ExperimentDashboardPage"
    - path: "leanshot/src/components/admin/growth/experiment-types.ts"
      provides: "Shared inline-types module for Plans 39-07 and 39-08"
      contains: "ExperimentRow"
  key_links:
    - from: "leanshot/src/lib/admin/modules.ts"
      to: "leanshot/src/components/admin/growth/ExperimentDashboardPage.tsx"
      via: "lazy import"
      pattern: "growth/ExperimentDashboardPage"
    - from: "leanshot/src/components/admin/AdminShell.tsx"
      to: "leanshot/src/lib/admin/modules.ts"
      via: "manifest-driven URL-prefix branch (line 116-120)"
      pattern: "pathname.startsWith"
---

<objective>
Wave 4 admin shell — register the growth/experiments admin module and ship the ExperimentDashboardPage shell (Surface E + H) with 3 Pill tabs. This plan is intentionally SMALL so Plans 39-07 and 39-08 can fill in per-tab content with maximum parallelism.

Purpose: H is the router-level entry; E is the chrome that hosts the 3 sub-experiences. Without this plan, Plans 39-07 and 39-08 have no mount point. Per UI-SPEC the chrome mirrors CACDashboardPage.

Output:
- 1 manifest entry in modules.ts (single source of truth — AdminShell.tsx already manifest-driven per PATTERNS Section E)
- ExperimentDashboardPage with 3 Pill tabs + tab-state union
- experiment-types.ts shared inline-types module
- AdminShell test extension to prove manifest+router parity
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CONTEXT.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-UI-SPEC.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-PATTERNS.md

<read_first>
- leanshot/src/components/admin/growth/CACDashboardPage.tsx — full read. Sibling chrome template (PATTERNS Section D). Header structure, Sheet drill-in, polling pattern, Skeleton, EmptyState — all mirrored.
- leanshot/src/components/admin/growth/CACDashboardPage.test.tsx — sibling test shape.
- leanshot/src/lib/admin/modules.ts lines 290-320 — existing growth-cac entry; new entry follows the same shape exactly (PATTERNS Section G).
- leanshot/src/components/admin/AdminShell.tsx — confirm manifest-driven router branch (no per-route addition needed).
- leanshot/src/components/ui/Pill.tsx — primitive contract; aria-pressed inheritance.
- leanshot/src/components/admin/__tests__/AdminShell.test.tsx — extend with the new parity test case.
</read_first>

<interfaces>
ExperimentDashboardPage:
  export function ExperimentDashboardPage(): JSX.Element;
  // Internal state:
  // - activeTab: 'paywall' | 'page' | 'pharma' (default 'paywall')
  // - rows: ExperimentRow[] | null (null = loading, [] = empty, [...] = data)
  // - vendorUnconfigured: boolean
  // - busyKey: string | null (for in-flight Ship-Winner button — Plan 39-07 hooks)

experiment-types.ts shared exports (consumed by 39-07 + 39-08):
  export type ExperimentSurface = 'paywall' | 'page' | 'pharma';

  export interface ExperimentRow {
    variant_id: string;
    variant_name: string;
    surface: ExperimentSurface;
    cohort_id: string | null;
    cohort_label: string | null;
    sample_size: number;
    paid_rate: number | null;
    retention_30d_rate: number | null;
    composite_score: number | null;
    posterior: number; // 0..1
    refund_rate_7d: number | null;
    refund_rate_baseline_30d: number | null;
    warned_at: string | null;
    archived_at: string | null;
    created_at: string;
  }

  export interface PharmaExperimentRow extends ExperimentRow {
    nps_delta: number | null;
    one_star_rate_ratio: number | null;
    safety_categories_in_variant: string[]; // MUST be empty array — invariant proof of D-05
  }

  export interface InvokeError { error?: string }

modules.ts new entry shape (mirror existing growth-cac at lines 297-308):
  key: 'growth-experiments'
  label: 'Experiments'
  route: 'growth/experiments'
  icon: TrendingUpIcon
  lazy: dynamic import of @/components/admin/growth/ExperimentDashboardPage with default = ExperimentDashboardPage
  flagKey: 'admin.growth.experiments.enabled'
  minRole: 'admin' as AdminRole
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: experiment-types module + modules.ts manifest entry + AdminShell parity test</name>
  <files>leanshot/src/components/admin/growth/experiment-types.ts, leanshot/src/lib/admin/modules.ts, leanshot/src/components/admin/__tests__/AdminShell.test.tsx</files>
  <behavior>
    - RED AdminShell parity: a new test case "manifest entry growth-experiments resolves via AdminShell pathname.startsWith branch" mounts AdminShell with pathname=/admin/growth/experiments + admin role; asserts the lazy module placeholder or resolved ExperimentDashboardPage renders.
    - GREEN modules.ts: append the new entry per `<interfaces>`. Place after growth-cac, before nps-quarterly (grouping convention).
    - GREEN experiment-types.ts: emit shared types per `<interfaces>` for Plans 39-07/08 consumption.
  </behavior>
  <action>
Pre-Read modules.ts lines 290-320 to find the insertion point and confirm TrendingUpIcon import is already present (it is, per the growth-cac sibling).

Pre-Read AdminShell.test.tsx full file to learn the existing test scaffolding (auth mock, role mock, router prop shape).

Append the new ADMIN_MODULES entry with a header comment citing PAYWALL-06, PAGEAB-01, PHARMA-08 (the 3 REQ-IDs this admin module serves) plus a reference to `[[feedback_admin_module_manifest_vs_router_branch_drift]]` confirming the AdminShell prefix-branch covers the manifest entry automatically.

Per PATTERNS Pattern S1: minRole='admin' is the UX gate; SECDEF RPCs in Plans 39-07/08 re-check at the data layer.

The AdminShell parity test is the single mitigation for the manifest-router drift footgun — failure here flags any future drift before deploy.

Decision references: PAYWALL-06 (variant_config edited from this admin), PAGEAB-01 (admin creates page variant), PAGEAB-07 (Bayesian badge rendered in 39-07), PHARMA-08 (pharma admin tab rendered in 39-08).
  </action>
  <verify>
    <automated>cd leanshot && grep -q "key: 'growth-experiments'" src/lib/admin/modules.ts && grep -q "route: 'growth/experiments'" src/lib/admin/modules.ts && grep -q 'ExperimentDashboardPage' src/lib/admin/modules.ts && npx vitest run src/components/admin/__tests__/AdminShell.test.tsx 2>&1 | tail -10 && test -f src/components/admin/growth/experiment-types.ts</automated>
  </verify>
  <done>Manifest entry has all 7 fields per CACDashboardPage shape. AdminShell parity test asserts new route resolves. experiment-types.ts exports ExperimentRow + PharmaExperimentRow + ExperimentSurface + InvokeError.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExperimentDashboardPage shell with 3 Pill tabs + polling + EmptyState + vendor banner</name>
  <files>leanshot/src/components/admin/growth/ExperimentDashboardPage.tsx, leanshot/src/components/admin/growth/ExperimentDashboardPage.test.tsx</files>
  <behavior>
    - RED: tests (a) renders 3 Pill tabs labeled Paywall / Page-Builder / Pharma; (b) default activeTab is paywall (first tab aria-pressed); (c) clicking Page-Builder Pill switches activeTab to 'page'; (d) on mount, useEffect calls supabase.rpc('get_experiment_results', { surface: 'paywall' }); (e) loading state renders Skeleton rows; (f) empty result renders EmptyState with heading 'No active experiments yet.', body 'Create a variant from any published page to begin testing.', primary 'Go to Pages'; (g) vendor_unconfigured response renders the soft banner with UI-SPEC copy 'Slack alerts pause until #growth-experiments webhook is configured.'; (h) polling re-fires every 30 seconds via setInterval; (i) tab change re-triggers load with new surface arg.
    - GREEN: mirror CACDashboardPage.tsx imports + chrome. Inline ExperimentRow type imported from experiment-types.ts. Pill tab nav with controlled state union. Body slot per tab is a placeholder div with data-tab attribute + data-testid='experiment-tab-content' — Plans 39-07 and 39-08 fill in the per-tab content.
    - Vendor banner uses Pattern A vendor-unconfigured shape; appears when get_experiment_results returns error.error === 'vendor_unconfigured'.
  </behavior>
  <action>
Pre-read CACDashboardPage.tsx in full. Mirror header + chrome + polling pattern. Replace title to "Experiments"; replace data-fetch RPC name to get_experiment_results (the RPC body ships in Plan 39-07).

Tab nav: 3 Pill components with onClick handlers. Active state via aria-pressed inherited from Pill primitive. Pills sit in a flex row with gap-2 (8px sm token) per UI-SPEC spacing scale.

Per UI-SPEC Hard Constraint 5: NO useQuery / useMutation / QueryClient imports. All async via useEffect + cancellation flag + setInterval at 30_000ms per CACDashboardPage precedent. Cleanup function clears the interval + sets cancelled flag.

Per UI-SPEC Hard Constraints 1-4: typography ramp only text-sm/base/xl/2xl + weights 400/700; spacing tokens only; --color-primary appears only on the active Pill tab indicator + primary CTA buttons; no hex literals (DSv2 semantic tokens only).

Per UI-SPEC Hard Constraint 11: NO new keyframes in this plan.

The "Go to Pages" empty-state CTA navigates to /admin/pages via window.location assignment (router-less admin per UI-SPEC).

Polling triggers re-fetch on activeTab change via the useEffect dependency array.

Decision references: D-04 (Slack ops-hint banner shape), PAYWALL-06, PAGEAB-01/07, PHARMA-08.

Per Pattern M: post-Edit, run `git status --short` to confirm both files dirty (no silent skip).
  </action>
  <verify>
    <automated>cd leanshot && npx vitest run src/components/admin/growth/ExperimentDashboardPage.test.tsx 2>&1 | tail -15 && ! grep -E 'useQuery|useMutation|QueryClient' src/components/admin/growth/ExperimentDashboardPage.tsx && ! grep -E 'text-\[|font-\[' src/components/admin/growth/ExperimentDashboardPage.tsx</automated>
  </verify>
  <done>Vitest GREEN for all 9 test cases. ZERO TanStack Query imports. ZERO inline text-[ or font-[ arbitrary values. 3 Pill tabs with controlled state union.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> get_experiment_results RPC | minRole='admin' UX gate (modules.ts); SECDEF RPC re-checks at data layer (Pattern S1) |
| Manifest entry -> AdminShell router | Manifest-driven URL prefix branch; parity test prevents silent route drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06-01 | Spoofing | Non-admin user navigates directly to /admin/growth/experiments | mitigate | AdminShell evaluates role via existing auth gate before mounting any module; minRole='admin' in manifest filters from the picker; manifest entry uses Pattern S1 (UX gate + data-layer SECDEF re-check) |
| T-39-06-02 | Tampering | Manifest added but route fails to resolve due to drift | mitigate | AdminShell parity test in Task 1 fails on drift before deploy |
| T-39-06-03 | Information disclosure | Vendor-unconfigured banner reveals POSTHOG_API_KEY name | accept | Banner text mentions secret NAME only (no value); operator-visible only via admin role; matches existing Phase 34/35 vendor banner pattern |
| T-39-06-04 | DoS | 30s polling overloads RPC | accept | Single tab open per admin; cancellation flag prevents pileup on tab change; matches CACDashboardPage precedent in production |
</threat_model>

<verification>
- Manifest entry present with all 7 fields
- AdminShell parity test green
- ExperimentDashboardPage renders 3 Pills with correct labels
- Polling pattern uses useEffect+setInterval (NO TanStack Query)
- No hex literals; no inline arbitrary values; accent allowlist honored
- experiment-types.ts exports the types Plans 39-07 and 39-08 import
</verification>

<success_criteria>
- Admin can navigate to /admin/growth/experiments and see the 3-tab chrome
- Plans 39-07 and 39-08 have a stable mount point + shared type surface
- Manifest-router drift impossible without parity test failure
- UI-SPEC Hard Constraints 1-5, 11 all honored
</success_criteria>

<output>
Create `.planning/phases/.../39-06-SUMMARY.md` — list 5 files modified, the 3 Pill labels verbatim, the polling interval (30s), and the AdminShell parity test name so Plans 39-07/08 can wire against the same fixture pattern.
</output>
