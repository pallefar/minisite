---
phase: 39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a
plan: 10
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09]
files_modified:
  - leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-10-SUMMARY.md
  - leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CLOSEOUT-NOTES.md
  - leanshot/.planning/PROJECT.md
autonomous: false
requirements:
  - PAYWALL-01
  - PAYWALL-02
  - PAYWALL-03
  - PAYWALL-04
  - PAYWALL-05
  - PAYWALL-06
  - PAYWALL-07
  - PAGEAB-01
  - PAGEAB-02
  - PAGEAB-03
  - PAGEAB-04
  - PAGEAB-05
  - PAGEAB-06
  - PAGEAB-07
  - PHARMA-01
  - PHARMA-02
  - PHARMA-03
  - PHARMA-04
  - PHARMA-05
  - PHARMA-06
  - PHARMA-07
  - PHARMA-08
must_haves:
  truths:
    - "supabase db push --linked completes without error, applying all 20 Phase 39 migrations (20270714000001..20)"
    - "supabase functions deploy completes for variant-resolver + slack-alert-experiments with --import-map flag"
    - "supabase functions deploy completes for page-render (extension) with --import-map flag"
    - "SLACK_WEBHOOK_EXPERIMENTS_URL Function Secret is set on the linked project (verified via supabase secrets list grep)"
    - "POSTHOG_PROJECT_KEY + POSTHOG_API_KEY Function Secrets already present (verified via supabase secrets list grep) — pre-existing per Phase 34/35"
    - "Vercel deploy + axe-core baseline + lint:safety + tsc --noEmit + full vitest + Deno test sweep all green"
    - "PROJECT.md vendor table updated to record SLACK_WEBHOOK_EXPERIMENTS_URL + #growth-experiments channel ownership"
    - "6 HUMAN-UAT signals discrete + individually resumable per [[feedback_multi_signal_human_verify_checkpoint_pattern]]"
  artifacts:
    - path: "leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CLOSEOUT-NOTES.md"
      provides: "Deploy log + secret-set evidence + post-deploy sanity check results"
      contains: "supabase db push"
    - path: "leanshot/.planning/PROJECT.md"
      provides: "Vendor table append for Slack #growth-experiments + webhook secret"
      contains: "growth-experiments"
  key_links:
    - from: "Operator browser"
      to: "Supabase Function Secrets dashboard"
      via: "Sets SLACK_WEBHOOK_EXPERIMENTS_URL value from Slack admin"
      pattern: "SLACK_WEBHOOK_EXPERIMENTS_URL"
    - from: "supabase db push --linked"
      to: "20 Phase 39 migrations in supabase/migrations/20270714*.sql"
      via: "Applies all schema + cron + RPC migrations atomically"
      pattern: "20270714"
user_setup:
  - service: slack
    why: "PHARMA + PAYWALL + PAGEAB kill-switches + lifecycle alerts fan out via single #growth-experiments channel (D-04)"
    env_vars:
      - name: SLACK_WEBHOOK_EXPERIMENTS_URL
        source: "Slack workspace admin -> Apps -> Incoming Webhooks -> Add to #growth-experiments"
    dashboard_config:
      - task: "Create #growth-experiments channel in Slack workspace (operator action)"
        location: "Slack workspace admin"
      - task: "Add Incoming Webhooks app to #growth-experiments and copy the webhook URL"
        location: "Slack app directory -> Incoming Webhooks"
      - task: "Set the webhook URL as Supabase Function Secret via `supabase secrets set --project-ref ytnsipxxmzgaebkqmokp SLACK_WEBHOOK_EXPERIMENTS_URL=<url>`"
        location: "Local terminal with supabase CLI"
---

<objective>
Wave 6 close-out (BLOCKING + human gates) — ship all 20 Phase 39 migrations to the linked Supabase project, deploy the 2 new Edge Fns + the extended page-render Fn, set the Slack webhook Function Secret, run the full automated verification sweep, then collect 6 HUMAN-UAT signals.

Purpose: Phase 39 is functionally complete after Plans 39-01..09 land in the worktree. This plan converts that worktree state to a deployed, operator-verified phase. Per `[[feedback_phase_close_out_db_push_verification]]`: this plan OWNS the supabase db push step (no per-plan push); per `[[feedback_worktree_executor_pwd_drift_leaks_to_main]]`: db push MUST happen in the primary checkout, not a worktree.

Output:
- All migrations applied to remote (D-NN coverage end-to-end)
- 2 new Edge Fns + 1 extended Edge Fn deployed
- Slack webhook secret set
- Vercel deploy + full automated test sweep green
- 6 HUMAN-UAT signals collected (or deferred to milestone close per [[feedback_milestone_uat_deferral_consolidation]])
- PROJECT.md vendor table updated
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CONTEXT.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-RESEARCH.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-01-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-02-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-03-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-04-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-05-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-06-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-07-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-08-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-09-SUMMARY.md
@leanshot/.planning/PROJECT.md

<read_first>
- leanshot/.planning/PROJECT.md — vendor table for the append target.
- All 9 Plan 39 SUMMARY files — single-pass read to inventory migration timestamps + Edge Fn names for the deploy step.
</read_first>

<interfaces>
Pre-deploy migration timestamp collision check (Pattern H):
  ls supabase/migrations/20270714*.sql
  Expected count: 20 (Plans 39-01 -> 11 migrations; Plan 39-03 -> 4 migrations; Plan 39-07 -> 2 migrations; Plan 39-08 -> 3 migrations).
  If a non-Phase-39 migration has appeared with a 20270714 prefix between plan-time and execute-time, bump suffixes forward.

Deploy commands (verbatim — per [[reference_supabase_functions_deploy_no_linked_flag]] no --linked on deploy; per [[reference_supabase_functions_deploy_import_map_flag]] --import-map required for _shared imports):
  supabase db push --linked
  supabase functions deploy variant-resolver --import-map supabase/functions/import_map.json
  supabase functions deploy slack-alert-experiments --import-map supabase/functions/import_map.json
  supabase functions deploy page-render --import-map supabase/functions/import_map.json

Secret set commands:
  supabase secrets set --project-ref ytnsipxxmzgaebkqmokp SLACK_WEBHOOK_EXPERIMENTS_URL=<from operator>
  supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep SLACK_WEBHOOK_EXPERIMENTS_URL
  supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep POSTHOG_PROJECT_KEY
  supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep POSTHOG_API_KEY

Post-deploy sanity checks:
  - supabase db query --linked "SELECT count(*) FROM pg_matviews WHERE matviewname = 'experiment_results';" -> 1
  - supabase db query --linked "SELECT count(*) FROM cohort_definitions WHERE key IN ('free-user','past-due-3d','trial-day-3','trial-day-7','post-activation');" -> 5
  - supabase db query --linked "SELECT count(*) FROM utm_variant_map WHERE utm_source IN ('lean','transformation','clinical','default');" -> 4
  - supabase db query --linked "SELECT jobname FROM cron.job WHERE jobname LIKE 'p39-%';" -> 3 rows (p39-variant-42day-archive, p39-refund-rate-kill, p39-pharma-nps-kill)

Full automated sweep:
  cd leanshot && npx tsc -p tsconfig.app.json --noEmit
  cd leanshot && npx eslint .
  cd leanshot && bash scripts/check-no-paywall-on-safety-category.sh
  cd leanshot && npx vitest run
  cd /Users/karstenhaldan/minisite && $HOME/.deno/bin/deno test --allow-all --no-check supabase/functions/
  cd leanshot && npx playwright test e2e/paywall-mid-trial.spec.ts e2e/admin/page-variant-create.spec.ts e2e/admin/pharma-variant-disable.spec.ts e2e/admin/pharma-admin-tab.spec.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration push + Edge Fn deploys + Slack secret set + automated sweep</name>
  <files>leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CLOSEOUT-NOTES.md, leanshot/.planning/PROJECT.md</files>
  <action>
Pre-flight per Pattern H + `[[feedback_phase_close_out_db_push_verification]]`:
  1. Run `ls supabase/migrations/20270714*.sql | wc -l` -> assert 20. If less, identify missing plan SUMMARY and HALT.
  2. Run `git status` in primary checkout -> assert clean working tree (all worktrees merged).
  3. Run `git log --oneline origin/main..HEAD | head` -> push if main is ahead per [[feedback_push_origin_before_parallel_worktree_dispatch]].

Database push (BLOCKING):
  - Run `supabase db push --linked` from the primary checkout (NOT a worktree per [[feedback_worktree_executor_pwd_drift_leaks_to_main]]).
  - Capture stdout/stderr into 39-CLOSEOUT-NOTES.md. If push fails with "older than remote tail" per [[reference_supabase_back_dated_migration_blocks_push]], invoke the operator-fix recipe: `mv` offending file to /tmp, push, restore.
  - Post-push: run the 4 post-deploy sanity-check queries from `<interfaces>` and append results to CLOSEOUT-NOTES.

Edge Fn deploys (BLOCKING):
  - Per [[reference_supabase_functions_deploy_no_linked_flag]] + [[reference_supabase_functions_deploy_import_map_flag]]: deploy variant-resolver, slack-alert-experiments, page-render with `--import-map supabase/functions/import_map.json`.
  - Capture deploy URLs + timestamps into CLOSEOUT-NOTES.

Slack secret AUTH GATE (checkpoint pattern):
  - Attempt `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep SLACK_WEBHOOK_EXPERIMENTS_URL`. If absent, create an inline AUTH checkpoint:
      "Operator: create #growth-experiments Slack channel + Incoming Webhook + share webhook URL.
       Then run: supabase secrets set --project-ref ytnsipxxmzgaebkqmokp SLACK_WEBHOOK_EXPERIMENTS_URL=<url>"
  - Verify POSTHOG_PROJECT_KEY + POSTHOG_API_KEY already present (per RESEARCH Environment Availability — should be set from Phase 24/34/35).
  - If POSTHOG keys missing, surface AUTH checkpoint for operator to set them.

Vercel deploy (BLOCKING):
  - Run `cd leanshot && vercel --prod` OR confirm `git push origin main` triggers Vercel auto-deploy.
  - Capture Vercel deploy URL.

Automated sweep (BLOCKING):
  - Run the 6 commands from `<interfaces>` (tsc, eslint, lint:safety, vitest, deno test sweep, playwright critical path).
  - For Deno test sweep per [[feedback_post_merge_deno_sweep_pattern]]: cross-Fn sweep with --no-check.
  - Capture pass/fail per command into CLOSEOUT-NOTES.

PROJECT.md vendor table append:
  - Add row for #growth-experiments Slack channel + SLACK_WEBHOOK_EXPERIMENTS_URL Function Secret + owner = operator.

Per Pattern J: per-commit `git rev-parse --show-toplevel` assertion confirms commits land in primary checkout, not a worktree.

Decision references: D-04 (Slack channel), RESEARCH Environment Availability (Slack webhook BLOCKING), all 14 D-NN end-to-end traceability.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && (supabase migration list --linked 2>&1 | grep -c '20270714' | grep -q '^[2][0-9]$' || supabase db query --linked "SELECT count(*) FROM pg_matviews WHERE matviewname = 'experiment_results';" 2>&1 | grep -q '1') && supabase secrets list --project-ref ytnsipxxmzgaebkqmokp 2>&1 | grep -q SLACK_WEBHOOK_EXPERIMENTS_URL && supabase secrets list --project-ref ytnsipxxmzgaebkqmokp 2>&1 | grep -q POSTHOG_PROJECT_KEY && cd leanshot && npx tsc -p tsconfig.app.json --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>All 20 migrations applied. 3 Edge Fns deployed. Slack + PostHog secrets present. Automated sweep green. CLOSEOUT-NOTES.md captures evidence. PROJECT.md vendor table updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 6-signal HUMAN-UAT close-out (discrete + individually resumable)</name>
  <what-built>
Phase 39 is fully deployed. The following 6 signals require human verification or are operator-only acceptance criteria. Per [[feedback_multi_signal_human_verify_checkpoint_pattern]]: signals are LISTED individually so operator can approve resumable subsets; deferred items consolidate to .planning/v1.3-uat-deferred.md per [[feedback_milestone_uat_deferral_consolidation]].
  </what-built>
  <how-to-verify>
**Signal 1 — Mid-trial paywall renders for activated trialing user (PAYWALL-01)**
Open /admin/users in incognito → impersonate a trialing user post-activation_events row → assert PaywallModal appears on next dashboard navigation. Expected variant_id appears in Network tab variant-resolver response.

**Signal 2 — 6-screen onboarding paywall flow (PAYWALL-06 + D-14)**
Same impersonation context, but set user_experiments.variant_id pointing to a variant_config row with config.flow='onboarding'. Reload. Expected: 6 screens render in fixed order (value-pillar-1..3, social-proof, pricing, final-CTA). Per-screen back/next navigation works. Final-CTA heading is Fraunces serif.

**Signal 3 — Page-variant SEO canonical (PAGEAB-02)**
Open variant URL in browser → View Source → assert `<link rel="canonical" href="/{control-slug}">` is present, pointing to the canonical (NOT the variant) page.

**Signal 4 — Pharma safety-info never paywalled (PHARMA-02 + D-05)**
Open /pharma/{drug-id-with-safety-content} as a free user → Expected: full content visible + 'Safety info — always free' sage badge present. No paywall CTA visible anywhere in the safety_category subtree.

**Signal 5 — Admin Ship-Winner below-95% typed-confirm (D-12)**
Navigate to /admin/growth/experiments → Paywall tab → click Ship-Winner on a variant with posterior < 95% → confirm modal opens → assert confirm button disabled until typed string 'ship-below-95' + reason filled → confirm → assert variant promoted + admin_audit_log row created (verify via `supabase db query --linked "SELECT count(*) FROM admin_audit_log WHERE action='ship_below_95';"`).

**Signal 6 — Pharma 1-click disable + Slack alert (PHARMA-04 + D-04)**
Pharma tab → click Disable on an active variant → Confirm → assert variant_config.archived_at populated → check #growth-experiments Slack channel for the kill alert post within 30 seconds.

For each signal: type "approved <N>" to mark green; type "deferred <N> <reason>" to push to .planning/v1.3-uat-deferred.md; type "blocked <N> <reason>" to flag for rework.
  </how-to-verify>
  <files>leanshot/.planning/v1.3-uat-deferred.md</files>
  <action>Operator verifies the 6 signals listed in <how-to-verify>; each signal is independently approvable/deferrable/blockable per [[feedback_multi_signal_human_verify_checkpoint_pattern]]. CLI-runnable signals can be approved inline; browser/device-dependent signals carry over to milestone-close .planning/v1.3-uat-deferred.md.</action>
  <verify>Each of 6 signals carries a discrete disposition (approved | deferred | blocked); orchestrator records dispositions in 39-10-SUMMARY.md.</verify>
  <done>All 6 signals dispositioned; phase status set to complete or complete + UAT-deferred per the final disposition mix.</done>
  <resume-signal>For each of the 6 signals, respond "approved N", "deferred N: <reason>", or "blocked N: <reason>". When all 6 dispositions are recorded, type "all-signals-resolved" to complete the phase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Primary checkout -> linked Supabase project | supabase db push + functions deploy require operator-authenticated CLI; no automation bypass |
| Operator browser -> Slack workspace | Webhook creation requires Slack admin; Function Secret set requires Supabase project owner role |
| Production deployment -> end user | All 22 REQ-IDs surface ONLY post this plan; partial deploy = partial phase = inconsistent user experience |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-10-01 | Tampering | Migration push from worktree leaks to main | mitigate | Pattern J + [[feedback_worktree_executor_pwd_drift_leaks_to_main]] — push from primary checkout only; per-commit cwd guard |
| T-39-10-02 | DoS | Back-dated migration blocks db push entirely | mitigate | Pattern H pre-flight collision check; operator-fix recipe (mv to /tmp, push, restore) documented inline |
| T-39-10-03 | Information disclosure | SLACK_WEBHOOK_EXPERIMENTS_URL leaked via git commit | mitigate | Secret set via `supabase secrets set` CLI; NEVER committed to repo; PROJECT.md vendor table records ownership but not value |
| T-39-10-04 | Repudiation | Phase deployed without UAT trace | mitigate | 6-signal HUMAN-UAT checkpoint logs disposition per signal; deferred signals consolidate to .planning/v1.3-uat-deferred.md per [[feedback_milestone_uat_deferral_consolidation]] |
| T-39-10-05 | DoS | Crons fire before secrets set -> Slack 404 storm | accept (low severity) | slack-alert-experiments returns 503 vendor_unconfigured when SLACK_WEBHOOK_EXPERIMENTS_URL unset; pg_cron consumes the 503 without retry storm; first alert post-secret-set succeeds |
</threat_model>

<verification>
- `supabase migration list --linked` shows all 20 Phase 39 migrations applied
- `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp` shows SLACK_WEBHOOK_EXPERIMENTS_URL + POSTHOG_PROJECT_KEY + POSTHOG_API_KEY
- 3 cron jobs visible: `SELECT jobname FROM cron.job WHERE jobname LIKE 'p39-%';` -> 3 rows
- 5 cohort_definitions Phase 39 seed rows + 4 utm_variant_map rows + 1 experiment_results matview confirmed
- Vercel deploy URL recorded
- tsc --noEmit + eslint + lint:safety + vitest + deno test + critical-path playwright all green
- 6 HUMAN-UAT signals all dispositioned (approved / deferred / blocked)
- PROJECT.md vendor table includes #growth-experiments + secret name
</verification>

<success_criteria>
- All 22 REQ-IDs (PAYWALL-01..07, PAGEAB-01..07, PHARMA-01..08) covered by deployed code
- All 14 D-NN decisions implemented end-to-end (Phase 39-01..09 plans collectively + this plan deploys them)
- Slack channel #growth-experiments operational
- Phase 39 SUMMARY status = "complete" (or "complete + UAT deferred to v1.3 close" if any of the 6 signals deferred)
</success_criteria>

<output>
Create `.planning/phases/.../39-10-SUMMARY.md` per template — final phase-level summary including:
  - Migration count applied (target 20)
  - Edge Fn deploy URLs (3)
  - Secret-set evidence (3 secrets present)
  - Sweep results (6 commands pass/fail)
  - UAT disposition per signal (1-6)
  - Phase status: complete | complete + UAT-deferred
  - Pointer to .planning/v1.3-uat-deferred.md for any deferred signals

Append to .planning/STATE.md to mark Phase 39 done.
</output>
