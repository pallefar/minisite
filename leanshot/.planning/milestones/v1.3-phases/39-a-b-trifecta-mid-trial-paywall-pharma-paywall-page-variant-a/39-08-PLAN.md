---
phase: 39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a
plan: 08
type: execute
wave: 5
depends_on: [01, 02, 03, 06, 07]
files_modified:
  - leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-08-SUMMARY.md
  - leanshot/src/components/admin/growth/PharmaExperimentTab.tsx
  - leanshot/src/components/admin/growth/PharmaExperimentTab.test.tsx
  - leanshot/src/components/admin/growth/PharmaVersionList.tsx
  - leanshot/src/components/admin/growth/PharmaVersionList.test.tsx
  - leanshot/src/components/admin/growth/PharmaVariantMetricsCard.tsx
  - leanshot/src/components/admin/growth/PharmaVariantMetricsCard.test.tsx
  - leanshot/src/components/admin/growth/ExperimentDashboardPage.tsx
  - supabase/migrations/20270714000018_p39_get_pharma_experiments_rpc.sql
  - supabase/migrations/20270714000019_p39_pharma_content_version_write_rpc.sql
  - supabase/migrations/20270714000020_p39_disable_pharma_variant_rpc.sql
  - leanshot/e2e/admin/pharma-variant-disable.spec.ts
  - leanshot/e2e/admin/pharma-admin-tab.spec.ts
autonomous: true
requirements:
  - PHARMA-03
  - PHARMA-04
  - PHARMA-07
  - PHARMA-08
must_haves:
  truths:
    - "PharmaExperimentTab renders pharma variant rows with nps_delta + one_star_rate_ratio columns (NOT shown for paywall/page tabs)"
    - "PharmaExperimentTab includes 'Disable variant' button per row -> Confirm primitive -> disable_pharma_variant RPC (1-click rollback per PHARMA-04)"
    - "PharmaVersionList renders pharma_content_versions audit history with author + clinical_signoff badge (pending warning / approved success)"
    - "PharmaVariantMetricsCard shows composite metric copy from UI-SPEC: 'Conversion uplift: {N}% · NPS Δ: {N} · 1★ rate: {ratio}× baseline'"
    - "get_pharma_experiments SECDEF RPC returns PharmaExperimentRow[] with safety_categories_in_variant ALWAYS empty array (D-05 invariant)"
    - "pharma_content_version_write SECDEF RPC is the ONLY write path for pharma_content_versions; gates on is_admin_at_least('admin'); writes diff jsonb + author_id + optional clinical_signoff"
    - "disable_pharma_variant SECDEF RPC sets variant_config.archived_at + posts to slack-alert-experiments + writes admin_audit_log"
    - "ExperimentDashboardPage activeTab='pharma' renders PharmaExperimentTab (replaces the Plan 39-06/07 placeholder)"
    - "Playwright e2e proves: admin disables a pharma variant via 1-click; variant_config.archived_at populated; pharma admin tab renders the disabled state"
  artifacts:
    - path: "leanshot/src/components/admin/growth/PharmaExperimentTab.tsx"
      provides: "Pharma sub-tab content for Surface G"
      contains: "Disable variant"
    - path: "leanshot/src/components/admin/growth/PharmaVersionList.tsx"
      provides: "Pharma content version history table"
      contains: "clinical_signoff"
    - path: "leanshot/src/components/admin/growth/PharmaVariantMetricsCard.tsx"
      provides: "NPS + 1-star composite metrics card"
      contains: "nps"
    - path: "supabase/migrations/20270714000018_p39_get_pharma_experiments_rpc.sql"
      provides: "Admin read RPC for pharma experiments"
      contains: "get_pharma_experiments"
    - path: "supabase/migrations/20270714000019_p39_pharma_content_version_write_rpc.sql"
      provides: "Append-only write RPC for pharma_content_versions"
      contains: "pharma_content_version_write"
    - path: "supabase/migrations/20270714000020_p39_disable_pharma_variant_rpc.sql"
      provides: "1-click disable RPC (PHARMA-04 reversibility)"
      contains: "disable_pharma_variant"
  key_links:
    - from: "leanshot/src/components/admin/growth/PharmaExperimentTab.tsx"
      to: "public.disable_pharma_variant RPC"
      via: "supabase.rpc on Confirm primitive accept"
      pattern: "disable_pharma_variant"
    - from: "public.disable_pharma_variant RPC"
      to: "supabase/functions/slack-alert-experiments/index.ts"
      via: "net.http_post inside RPC body (vault decrypted_secrets)"
      pattern: "slack-alert-experiments"
    - from: "leanshot/src/components/admin/growth/PharmaVersionList.tsx"
      to: "public.pharma_content_version_write RPC"
      via: "writes audit row when admin saves new version"
      pattern: "pharma_content_version_write"
---

<objective>
Wave 5 admin slice B — fill in the Pharma sub-tab of the ExperimentDashboardPage (Plan 39-06), ship the audit-log + version-history admin UI (PHARMA-07), ship the 1-click disable flow (PHARMA-04), and ship the 3 supporting SECDEF RPCs.

Purpose: Pharma is the third leg of the trifecta and the most regulator-visible. PHARMA-07 versioning + PHARMA-08 admin metrics + PHARMA-04 1-click disable all converge into this single admin tab. Parallel-friendly with Plan 39-07 (zero file overlap except both edit ExperimentDashboardPage.tsx — coordinated via interface-first ordering: 39-06 ships the placeholder, 39-07 fills paywall/page branches, 39-08 fills pharma branch).

NOTE on shared file ExperimentDashboardPage.tsx: this plan ONLY touches the `activeTab === 'pharma'` branch (which Plans 39-06 and 39-07 leave as a placeholder). Plan 39-07 ONLY touches the paywall + page branches. No conflict in practice — but the executor MUST coordinate via the dispatch order: Plans 39-07 and 39-08 cannot dispatch in the same parallel wave-burst because of this file overlap. The orchestrator should serialize 39-08 AFTER 39-07.

Output:
- PharmaExperimentTab + 2 supporting widgets (version list + metrics card)
- 3 SECDEF RPCs (read + version write + disable)
- 2 Playwright e2e specs covering PHARMA-04 + PHARMA-08
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CONTEXT.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-UI-SPEC.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-PATTERNS.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-06-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-07-SUMMARY.md

<read_first>
- leanshot/src/components/admin/growth/PaywallExperimentTab.tsx (Plan 39-07) — sibling tab pattern; PharmaExperimentTab mirrors structure with pharma-specific columns.
- leanshot/src/components/admin/growth/CACDashboardPage.tsx — sort-table section is the template for PharmaVersionList.
- leanshot/src/components/ui/Confirm.tsx — primitive for 1-click disable confirm dialog (PHARMA-04 reversibility flow).
- leanshot/src/components/dashboard/charts/BaseChart.tsx + leanshot/src/components/ui/Sparkline.tsx — for PharmaVariantMetricsCard trend visualization.
- supabase/migrations/20270714000017_p39_admin_ship_below_95_rpc.sql (Plan 39-07) — sibling SECDEF RPC pattern.
- supabase/migrations/20270714000015_p39_kill_scan_functions.sql (Plan 39-03) — net.http_post + vault decrypted_secrets pattern reused for the disable RPC.
- leanshot/src/components/admin/growth/ExperimentDashboardPage.tsx (Plan 39-07 latest) — confirm the pharma placeholder branch is still present; this plan replaces it.
</read_first>

<interfaces>
PharmaExperimentTab props:
  interface PharmaExperimentTabProps {
    rows: PharmaExperimentRow[];
    onDisable: (variantId: string) => Promise<void>;
    busyKey: string | null;
  }
  // Renders columns: variant_name, sample_size, conversion uplift %, nps_delta, one_star_rate_ratio,
  // PharmaVariantMetricsCard preview, Disable button, clinical-signoff badge

PharmaVersionList props:
  interface PharmaVersionListProps {
    contentId: string;
    versions: PharmaContentVersion[];
  }
  interface PharmaContentVersion {
    id: string;
    content_id: string;
    diff: Record<string, unknown>;
    author_id: string;
    author_name: string;
    clinical_signoff_by: string | null;
    clinical_signoff_at: string | null;
    clinical_signoff_name: string | null;
    created_at: string;
  }

PharmaVariantMetricsCard props:
  interface PharmaVariantMetricsCardProps {
    nps_delta: number | null;
    one_star_rate_ratio: number | null;
    conversion_uplift_pct: number | null;
    trend_series?: number[]; // for Sparkline
  }
  // Renders Card primitive with copy from UI-SPEC:
  //   "Conversion uplift: {N}% · NPS Δ: {N} · 1★ rate: {ratio}× baseline"

get_pharma_experiments RPC:
  create or replace function public.get_pharma_experiments() returns setof public.pharma_experiment_row_t
  language plpgsql security definer set search_path = public, extensions
  -- Gate: is_admin_at_least('admin')
  -- Body: joins experiment_results matview WHERE surface='pharma' with quarterly_nps_responses (nps_delta) + review_submissions (one_star_rate_ratio) + variant_config + cohort_definitions
  -- INVARIANT: safety_categories_in_variant must always be returned as '{}'::text[] (empty array)
  --           — if any variant ever references a safety_category, the RPC raises and slack-alerts (defense-in-depth above D-05)

pharma_content_version_write RPC:
  create or replace function public.pharma_content_version_write(
    p_content_id uuid,
    p_diff jsonb,
    p_clinical_signoff_by uuid default null
  ) returns uuid -- new version id
  language plpgsql security definer set search_path = public, extensions
  -- Gate: is_admin_at_least('admin')
  -- INSERT into pharma_content_versions; author_id = auth.uid()
  -- IF p_clinical_signoff_by IS NOT NULL THEN set clinical_signoff_at = now()
  -- Returns the new row id

disable_pharma_variant RPC:
  create or replace function public.disable_pharma_variant(
    p_variant_id uuid,
    p_reason text
  ) returns void
  language plpgsql security definer set search_path = public, extensions
  -- Gate: is_admin_at_least('admin')
  -- UPDATE variant_config SET archived_at = now() WHERE id = p_variant_id AND surface = 'pharma'
  -- INSERT admin_audit_log row with action='pharma_disable', actor_id = auth.uid()
  -- net.http_post to slack-alert-experiments with kind='variant_kill', context.reason = p_reason
  --   (vault decrypted_secrets pattern verbatim from 20270714000015)
  -- IDEMPOTENT: if archived_at IS NOT NULL, skip silently
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PharmaVariantMetricsCard + PharmaVersionList + 3 SECDEF RPCs</name>
  <files>leanshot/src/components/admin/growth/PharmaVariantMetricsCard.tsx, leanshot/src/components/admin/growth/PharmaVariantMetricsCard.test.tsx, leanshot/src/components/admin/growth/PharmaVersionList.tsx, leanshot/src/components/admin/growth/PharmaVersionList.test.tsx, supabase/migrations/20270714000018_p39_get_pharma_experiments_rpc.sql, supabase/migrations/20270714000019_p39_pharma_content_version_write_rpc.sql, supabase/migrations/20270714000020_p39_disable_pharma_variant_rpc.sql</files>
  <behavior>
    - RED PharmaVariantMetricsCard: tests (a) renders UI-SPEC copy exactly with N/values; (b) null nps_delta renders '—' placeholder; (c) Sparkline renders when trend_series.length > 0; (d) one_star_rate_ratio > 2 triggers danger-tone visual cue.
    - RED PharmaVersionList: tests (a) renders one row per version with author_name + created_at; (b) clinical_signoff null -> 'Pending sign-off' warning badge; (c) clinical_signoff set -> 'Reviewed by {clinician}' success badge; (d) versions sorted descending by created_at.
    - GREEN: implement both per `<interfaces>` and UI-SPEC copy table verbatim.
    - SECDEF RPCs (3): write per `<interfaces>` signatures. Filenames 20270714000018/19/20 (next free slots after Plan 39-07's 17). All MUST use security definer + search_path = public, extensions. The disable_pharma_variant RPC body uses the EXACT same net.http_post + vault decrypted_secrets pattern from 20270714000015 (mirror lines verbatim).
    - get_pharma_experiments invariant: hardcode `'{}'::text[] as safety_categories_in_variant` in the SELECT — D-05 defense-in-depth.
  </behavior>
  <action>
Pre-read 20270714000017 (Plan 39-07 ship_below_95 RPC) as the SECDEF template. Pre-read 20270714000015 (Plan 39-03 kill-scan) for the net.http_post block.

Pre-flight collision check: `ls supabase/migrations/20270714*.sql` to confirm 18/19/20 are free (Plan 39-07 ended at 17).

UI-SPEC Hard Constraints:
- PharmaVariantMetricsCard uses sparkline (existing primitive) + Card primitive; no new chart abstraction.
- Typography ramp only text-sm/base/xl/2xl; weights 400/700.
- Clinical-signoff badge: warning tone for pending, success tone for approved. NEVER teal accent.
- Per UI-SPEC accent NOT-used-for list: Pharma 'always-free' safety badge stays in PharmaContentBlock (Plan 39-05); this plan's clinical-signoff badge uses warning/success palette only.

Decision references: D-03 (NPS + 1★ kill thresholds — surfaced as metrics here), D-05 (safety_categories_in_variant invariant in RPC), D-12 (audit-log on disable), PHARMA-04/07/08.

Per Pattern M: post-Edit, git status --short.
  </action>
  <verify>
    <automated>(cd leanshot && npx vitest run src/components/admin/growth/PharmaVariantMetricsCard.test.tsx src/components/admin/growth/PharmaVersionList.test.tsx 2>&1 | tail -10) && test -f supabase/migrations/20270714000018_p39_get_pharma_experiments_rpc.sql && test -f supabase/migrations/20270714000019_p39_pharma_content_version_write_rpc.sql && test -f supabase/migrations/20270714000020_p39_disable_pharma_variant_rpc.sql && grep -q "safety_categories_in_variant" supabase/migrations/20270714000018_p39_get_pharma_experiments_rpc.sql && grep -q "decrypted_secrets" supabase/migrations/20270714000020_p39_disable_pharma_variant_rpc.sql</automated>
  </verify>
  <done>2 vitest suites GREEN. 3 RPC migrations exist with SECDEF + role gate + vault pattern (where applicable). safety_categories_in_variant invariant present in get_pharma_experiments. No teal accent on clinical-signoff badge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PharmaExperimentTab + ExperimentDashboardPage wiring + 2 Playwright e2e specs</name>
  <files>leanshot/src/components/admin/growth/PharmaExperimentTab.tsx, leanshot/src/components/admin/growth/PharmaExperimentTab.test.tsx, leanshot/src/components/admin/growth/ExperimentDashboardPage.tsx, leanshot/e2e/admin/pharma-variant-disable.spec.ts, leanshot/e2e/admin/pharma-admin-tab.spec.ts</files>
  <behavior>
    - RED PharmaExperimentTab: tests (a) renders one row per PharmaExperimentRow with variant_name + sample_size + nps_delta + one_star_rate_ratio + PharmaVariantMetricsCard preview + Disable button; (b) Disable button click opens Confirm primitive with copy 'Disable pharma variant `{variant_name}`? All traffic returns to control immediately.'; (c) Confirm accept calls onDisable(variantId); (d) archived_at non-null row shows muted/disabled state with no Disable button; (e) per-row PharmaVersionList preview button opens Sheet drawer with version history.
    - GREEN PharmaExperimentTab: implement per `<interfaces>`. Reuse Confirm primitive + Sheet primitive.
    - GREEN ExperimentDashboardPage.tsx (additive edit): replace the pharma placeholder branch with PharmaExperimentTab; rows source from supabase.rpc('get_pharma_experiments'). When activeTab switches to pharma, the polling load fires get_pharma_experiments (not get_experiment_results). Plans 39-06/07 paywall/page branches remain untouched.
    - Playwright pharma-variant-disable.spec.ts: seed admin user with role='admin'; seed 1 pharma variant_config row; navigate to /admin/growth/experiments; click Pharma tab; click Disable; type confirm; assert toast 'Disabled variant'; assert variant_config.archived_at populated via supabase query post-action.
    - Playwright pharma-admin-tab.spec.ts: seed admin user; seed 2 pharma variants (1 active + 1 archived); navigate; click Pharma tab; assert both rows render; assert archived row has muted styling + no Disable button.
  </behavior>
  <action>
Pre-read PaywallExperimentTab.tsx (Plan 39-07) — mirror table + Ship-Winner busyKey pattern + tab wiring.
Pre-read ExperimentDashboardPage.tsx (Plan 39-07 latest version) to find the pharma placeholder branch slot.
Pre-read Confirm.tsx + Sheet.tsx primitive contracts.

ExperimentDashboardPage edit: introduce a separate load function for pharma (calls get_pharma_experiments RPC) — activeTab='pharma' triggers this function instead of get_experiment_results. Use the existing useEffect+setInterval cancellation pattern; add a discriminated state for pharma rows.

The data-overlap with Plan 39-07 on ExperimentDashboardPage.tsx is real but bounded — this plan touches only the pharma branch + load-function dispatch. The orchestrator MUST dispatch Plan 39-08 AFTER Plan 39-07 to avoid worktree merge conflict (sequenced wave assignment).

UI-SPEC Hard Constraint 8 — Pharma Disable does NOT use ship-winner-flag (it's a kill, not a promotion); uses disable_pharma_variant RPC instead. Documented in plan must_haves.

Playwright specs use the project's existing fixture pattern (addInitScript for state seeding per [[reference_playwright_state_seeding]]).

Decision references: D-03 (NPS + 1★ surfaced), D-12 (audit on disable), PHARMA-03/04/07/08.
  </action>
  <verify>
    <automated>(cd leanshot && npx vitest run src/components/admin/growth/PharmaExperimentTab.test.tsx src/components/admin/growth/ExperimentDashboardPage.test.tsx 2>&1 | tail -10) && test -f leanshot/e2e/admin/pharma-variant-disable.spec.ts && test -f leanshot/e2e/admin/pharma-admin-tab.spec.ts && grep -q 'disable_pharma_variant' leanshot/src/components/admin/growth/PharmaExperimentTab.tsx</automated>
  </verify>
  <done>2 vitest suites GREEN (including extended ExperimentDashboardPage test for 3-tab coverage). 2 e2e specs exist. Pharma tab wires to disable_pharma_variant RPC (NOT ship-winner-flag).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> disable_pharma_variant RPC | admin role gate; idempotent; emits Slack alert via vault-secured net.http_post |
| Admin browser -> pharma_content_version_write RPC | append-only write; SECDEF gates; author_id=auth.uid() at write time |
| get_pharma_experiments -> safety_category invariant | safety_categories_in_variant ALWAYS returned as '{}'::text[] (defense-in-depth above D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08-01 | Compliance | get_pharma_experiments leaks variant referencing safety_category to admin UI | mitigate | RPC enforces safety_categories_in_variant='{}' invariant; raises + slack-alerts if any variant ever references a safety_category. 4th layer beyond Plan 39-02 ESLint+runtime+grep |
| T-39-08-02 | Tampering | pharma_content_versions edited retroactively | mitigate | Append-only RLS proven by Plan 39-05 RLS test; pharma_content_version_write is the ONLY write path; admin_audit_log row written by future enhancement (for now: insert-only RLS is sufficient per CONTEXT D-06 wording) |
| T-39-08-03 | Tampering | Admin disables wrong variant due to UI race | mitigate | Confirm primitive requires explicit accept; busyKey prevents double-click; disable_pharma_variant RPC is idempotent (re-call on archived variant = no-op) |
| T-39-08-04 | Repudiation | Variant disabled without audit | mitigate | disable_pharma_variant RPC writes admin_audit_log row + posts Slack BEFORE returning; pgTAP-style proof not added (audit row creation tested at e2e layer) |
| T-39-08-05 | Spoofing | Non-admin user calls pharma_content_version_write | mitigate | is_admin_at_least('admin') gate at top of RPC body |
| T-39-08-06 | Information disclosure | PharmaVersionList exposes clinical_signoff_by user_id to admin | accept | Resolved to clinical_signoff_name (display string) via join in RPC; raw user_id never reaches UI |
| T-39-08-07 | DoS | Slack webhook flapping blocks disable | accept | net.http_post in RPC is async-ish (5s timeout); on failure, variant is still archived; alert is best-effort. Documented inline. |
</threat_model>

<verification>
- 4 vitest test files green (PharmaVariantMetricsCard, PharmaVersionList, PharmaExperimentTab, ExperimentDashboardPage extended)
- 3 RPC migrations exist with proper search_path + SECDEF + role gates
- safety_categories_in_variant='{}'::text[] invariant present in get_pharma_experiments RPC body
- vault decrypted_secrets pattern present in disable_pharma_variant RPC body
- 2 Playwright e2e files exist
- ExperimentDashboardPage now renders 3 functional sub-tabs
</verification>

<success_criteria>
- PHARMA-03/04/07/08 fully wired
- Pharma admin tab live, mirroring UI-SPEC Surface G layout
- 1-click disable + audit + Slack alert path operational
- Plans 39-06/07/08 collectively close the admin half of Phase 39
</success_criteria>

<output>
Create `.planning/phases/.../39-08-SUMMARY.md` — list 12 files, the 3 RPC names + role gates, the safety-category invariant proof, the e2e spec file names for milestone-close UAT.
</output>
