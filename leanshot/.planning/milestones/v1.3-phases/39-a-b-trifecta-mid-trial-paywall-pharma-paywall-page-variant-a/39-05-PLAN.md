---
phase: 39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-05-SUMMARY.md
  - leanshot/src/components/pharma/PharmaContentBlock.tsx
  - leanshot/src/components/pharma/PharmaContentBlock.test.tsx
  - leanshot/src/components/pharma/SafetyInfoBadge.tsx
  - leanshot/src/components/pharma/SafetyInfoBadge.test.tsx
  - leanshot/src/lib/pharma/region-detect.ts
  - leanshot/src/lib/pharma/region-detect.test.ts
  - leanshot/src/lib/pharma/get-content-tier.ts
  - leanshot/src/lib/pharma/get-content-tier.test.ts
  - leanshot/src/test/rls-pharma-content-versions.test.ts
autonomous: true
requirements:
  - PHARMA-01
  - PHARMA-02
  - PHARMA-05
  - PHARMA-06
  - PHARMA-07
must_haves:
  truths:
    - "PharmaContentBlock renders FULL content for Pro users (subscriptions.status='active' + tier='pro' via tier_effective) and SUMMARY-with-inline-CTA for free users"
    - "PharmaContentBlock NEVER paywalls content with safety_category non-null — D-05 enforced by phaCheck call BEFORE render branch"
    - "PharmaContentBlock wraps the tiered render in PaywallGate so all 3 phaCheck layers stack (build-time ESLint rule + runtime helper + render-cascade)"
    - "PharmaContentBlock short-circuits to full content for WA/CT region per D-07 (consults region-detect.ts client signals)"
    - "region-detect.ts reads cookie lt_pharma_blocked (set by Vercel edge middleware OR profile.state_of_residence) and exposes isPharmaRegionBlocked(): boolean"
    - "SafetyInfoBadge renders 'Safety info — always free' sage-tone badge ALWAYS alongside safety_category content per UI-SPEC copy + D-05"
    - "get-content-tier.ts reads tier_effective from Zustand store (Phase 19/43 contract) and returns 'pro' | 'free'"
    - "rls-pharma-content-versions.test.ts PROVES from a Vitest e2e harness that an admin can INSERT a version row via SECDEF RPC but CANNOT UPDATE/DELETE; non-admin gets 0 rows on SELECT for cross-tenant content"
  artifacts:
    - path: "leanshot/src/components/pharma/PharmaContentBlock.tsx"
      provides: "Tiered pharma content render (Surface F)"
      contains: "PaywallGate"
    - path: "leanshot/src/components/pharma/SafetyInfoBadge.tsx"
      provides: "Always-free badge for safety_category content"
      contains: "Safety info"
    - path: "leanshot/src/lib/pharma/region-detect.ts"
      provides: "WA/CT detection (D-07 client-side)"
      contains: "isPharmaRegionBlocked"
    - path: "leanshot/src/lib/pharma/get-content-tier.ts"
      provides: "Pro vs free tier lookup helper"
      contains: "tier_effective"
    - path: "leanshot/src/test/rls-pharma-content-versions.test.ts"
      provides: "Cross-tenant + append-only RLS proof"
      contains: "pharma_content_versions"
  key_links:
    - from: "leanshot/src/components/pharma/PharmaContentBlock.tsx"
      to: "leanshot/src/components/paywall/PaywallGate.tsx"
      via: "wraps tiered render in PaywallGate for 3-layer phaCheck stack"
      pattern: "<PaywallGate"
    - from: "leanshot/src/components/pharma/PharmaContentBlock.tsx"
      to: "leanshot/src/lib/pharma/region-detect.ts"
      via: "isPharmaRegionBlocked() short-circuit to full content"
      pattern: "isPharmaRegionBlocked"
    - from: "leanshot/src/components/pharma/PharmaContentBlock.tsx"
      to: "leanshot/src/lib/pharma/get-content-tier.ts"
      via: "tier resolution for free/Pro branch"
      pattern: "getContentTier"
---

<objective>
Wave 3 consumer slice B (parallel to Plan 39-04) — ship the pharma consumer surface F (PharmaContentBlock + SafetyInfoBadge) plus the region-detect.ts client helper (D-07 layer) and the tier resolution helper.

Purpose: PHARMA-01/02/05 are the tiered-access front end; safety_category content is the regulator-visible NEVER-paywall surface; WA/CT region carveout (D-07) is the WMHMDA/CTDPA compliance hinge. PHARMA-07 RLS proof closes the audit-log defense. Plan is parallel-friendly with 39-04 (zero file overlap; both depend on Plan 39-02's PaywallGate + phaCheck).

Output:
- PharmaContentBlock — Surface F (page-builder block + standalone)
- SafetyInfoBadge — small reusable badge tied to D-05 categories
- region-detect.ts — client-side companion to the Edge Fn WA/CT check
- get-content-tier.ts — Pro vs free lookup
- RLS proof for pharma_content_versions append-only invariant (PHARMA-07)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CONTEXT.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-UI-SPEC.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-PATTERNS.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-01-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-02-SUMMARY.md

<read_first>
- leanshot/src/components/paywall/PaywallGate.tsx — written by Plan 39-04 Task 1; READ to learn the props contract so PharmaContentBlock wraps correctly.
- leanshot/src/lib/pharma/phaCheck.ts — written by Plan 39-02; READ the exported type PharmaContent.
- leanshot/src/components/ui/Badge.tsx — sage success-tone styling source for SafetyInfoBadge.
- leanshot/src/lib/store.ts — find the tier_effective selector / accessor (Phase 19 contract, possibly named `subscriptionTier` or similar). If absent, read the per-user subscription from supabase directly (fallback documented inline).
- leanshot/src/test/rls-*.test.ts — any existing RLS proof file shape (per [[reference_rls_fixture_gotrueclient_flake]] the project uses admin.generateLink + plain fetch verify pattern).
</read_first>

<interfaces>
PharmaContentBlock props (used as page-builder block AND standalone surface F):
  interface PharmaContent {
    id: string;
    slug: string;
    summary: string;
    full_content: string;
    safety_category?: string | null;  // D-05 one of 5 values OR null
    category?: string;                 // e.g. 'drug-interactions', 'dosing', 'contraindications'
  }
  interface PharmaContentBlockProps {
    content: PharmaContent;
  }

PharmaContentBlock render branches (in cascade order):
  1. If isPharmaRegionBlocked() === true -> render FULL content (D-07 WA/CT carveout, silent to user per UI-SPEC)
  2. If content.safety_category != null -> render FULL content + SafetyInfoBadge (D-05 always-free)
  3. If getContentTier() === 'pro' -> render FULL content
  4. Otherwise: render SUMMARY + inline upgrade CTA wrapped in <PaywallGate content={content} surface="pharma">
     The inline CTA copy per UI-SPEC: "Upgrade to Pro for full {category} details"
     Summary suffix: "...(1-2 sentence summary; Pro members see full content)" rendered as text-sm text-text-tertiary

SafetyInfoBadge props:
  interface SafetyInfoBadgeProps { category: string }
  // Renders Badge primitive with tone="success" (sage), text "Safety info — always free"
  // Optional aria-label="Always free safety information ({category})"

region-detect.ts:
  export function isPharmaRegionBlocked(): boolean;
  // Reads document.cookie for lt_pharma_blocked=1 (set by Vercel edge middleware).
  // Also reads user.profile.state_of_residence from Zustand store; returns true if 'WA' or 'CT'.
  // Profile-state wins per D-07.
  // Returns false otherwise.
  // SSR-safe: returns false when document/window undefined.

get-content-tier.ts:
  export function getContentTier(): 'pro' | 'free';
  // Reads from Zustand store tier_effective lookup. Per CLAUDE.md: subscriptions via tier_effective contract.
  // Returns 'pro' if active Pro/Lifetime tier; 'free' otherwise (including trialing).
  // Per Phase 43 contract: uses has_active boolean, NOT string match on tier='paid'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: region-detect + get-content-tier + SafetyInfoBadge</name>
  <files>leanshot/src/lib/pharma/region-detect.ts, leanshot/src/lib/pharma/region-detect.test.ts, leanshot/src/lib/pharma/get-content-tier.ts, leanshot/src/lib/pharma/get-content-tier.test.ts, leanshot/src/components/pharma/SafetyInfoBadge.tsx, leanshot/src/components/pharma/SafetyInfoBadge.test.tsx</files>
  <behavior>
    - RED region-detect: tests (a) cookie lt_pharma_blocked=1 -> true; (b) cookie absent + profile.state='WA' -> true; (c) cookie absent + profile.state='CT' -> true; (d) cookie absent + profile.state='CA' -> false; (e) SSR (document undefined) -> false; (f) profile.state wins over absent cookie (D-07 precedence).
    - GREEN region-detect: parse document.cookie via small regex; read profile.state_of_residence from store via useStore-equivalent or direct getState() call (store contract).
    - RED get-content-tier: tests (a) Zustand store with has_active=true, tier='pro' -> 'pro'; (b) has_active=false -> 'free'; (c) trialing (no subscription) -> 'free'; (d) Lifetime tier per Phase 43 -> 'pro'.
    - GREEN get-content-tier: read store via getState(); apply has_active check NOT string-match on tier (per [[Phase 19 D-04]] memory contract referenced in Phase 43 plans).
    - RED SafetyInfoBadge: tests (a) renders text 'Safety info — always free'; (b) Badge tone=success; (c) aria-label includes the category param value.
    - GREEN SafetyInfoBadge: wrap Badge primitive with the fixed copy + dynamic aria-label.
  </behavior>
  <action>
Pre-read store.ts to confirm tier_effective accessor name (varies — may be `subscription.has_active` or `user.tierEffective`). Then write get-content-tier as a thin wrapper.

For region-detect, do NOT import Zustand subscribe inside the function — read via `useStore.getState()` once per call (not hook usage; this function is called inline inside PharmaContentBlock render).

SafetyInfoBadge MUST use UI-SPEC color: sage success-tone (--color-success-soft bg + --color-success text), NEVER teal accent (per UI-SPEC accent NOT-used-for list).
  </action>
  <verify>
    <automated>cd leanshot && npx vitest run src/lib/pharma/region-detect.test.ts src/lib/pharma/get-content-tier.test.ts src/components/pharma/SafetyInfoBadge.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>3 helpers + 1 badge + 3 test files all GREEN. region-detect honors D-07 profile-wins; get-content-tier uses has_active (NOT tier string match); SafetyInfoBadge uses sage tone NOT teal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PharmaContentBlock + RLS proof for pharma_content_versions append-only</name>
  <files>leanshot/src/components/pharma/PharmaContentBlock.tsx, leanshot/src/components/pharma/PharmaContentBlock.test.tsx, leanshot/src/test/rls-pharma-content-versions.test.ts</files>
  <behavior>
    - RED PharmaContentBlock: tests covering the 4 cascade branches in `<interfaces>`: (a) region-blocked -> full content rendered; (b) safety_category non-null -> full content + SafetyInfoBadge rendered (NO PaywallGate path); (c) Pro tier -> full content rendered (no badge, no CTA); (d) free tier non-safety -> summary + inline CTA + PaywallGate wrapper. Also test (e) Pro tier + safety_category -> full content + SafetyInfoBadge (badge ALWAYS shown alongside safety content per UI-SPEC).
    - GREEN PharmaContentBlock: implement cascade. Wraps free-tier branch in PaywallGate so all 3 phaCheck layers stack. Inline upgrade CTA uses Button variant=primary (NOT link) per UI-SPEC.
    - RLS proof rls-pharma-content-versions.test.ts: vitest e2e test using project RLS fixture pattern. Setup: create admin user + non-admin user via admin.generateLink + plain fetch verify per [[reference_rls_fixture_gotrueclient_flake]]. Test (a) non-admin SELECT pharma_content_versions returns 0 rows (no row-level access); (b) admin SELECT returns rows; (c) admin attempt UPDATE pharma_content_versions raises permission denied (no UPDATE policy); (d) admin attempt DELETE raises permission denied. Per-file slug prefix per [[feedback_rls_per_file_slug_prefix]].
  </behavior>
  <action>
Pre-read PaywallGate.tsx (Plan 39-04) before scaffolding PharmaContentBlock. The wrap pattern is `<PaywallGate content={content} surface="pharma">{summaryRender}</PaywallGate>` — PaywallGate then handles the variant-resolver invoke.

Per UI-SPEC strict typography ramp: PharmaContentBlock uses text-base for summary body, text-xl font-bold for content heading, text-sm text-text-tertiary for the suffix "(1-2 sentence summary...)". NO new sizes.

The inline upgrade CTA text MUST be exactly "Upgrade to Pro for full {category} details" per UI-SPEC copy table.

RLS proof: use the SAME file slug prefix pattern as other RLS tests in src/test/. afterAll cleanup deletes test rows; file-scoped prefix prevents cross-file collision under vitest file-parallelism per memory.

Decision references: D-05 (5 safety categories), D-07 (WA/CT), D-12 (typed audit-write — relevant for the SECDEF RPC that pharma_content_versions writes flow through, ships in Plan 39-08 not here), PHARMA-01/02/05/06/07.
  </action>
  <verify>
    <automated>cd leanshot && npx vitest run src/components/pharma/PharmaContentBlock.test.tsx 2>&1 | tail -10 && test -f src/test/rls-pharma-content-versions.test.ts && grep -q 'pharma_content_versions' src/test/rls-pharma-content-versions.test.ts</automated>
  </verify>
  <done>PharmaContentBlock cascade covers 5 enumerated test cases; RLS proof file exists with append-only invariant tested across admin + non-admin contexts. UI-SPEC accent allowlist honored (SafetyInfoBadge uses sage, NOT teal).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pharma content -> render branch | safety_category column check happens at multiple layers (phaCheck runtime + PaywallGate + PharmaContentBlock cascade) |
| WA/CT user signals -> render branch | Vercel edge cookie (server signal) + Zustand profile state (user-asserted signal); either positive bypasses paywall |
| pharma_content_versions audit log | Append-only RLS proven by RLS test; writes route through SECDEF RPC (Plan 39-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05-01 | Compliance / Repudiation | PharmaContentBlock paywalls safety_category content (regulator visible) | mitigate | 4-layer defense: ESLint rule (Plan 39-02) + phaCheck runtime (Plan 39-02) + PaywallGate cascade (Plan 39-04) + PharmaContentBlock branch order (this plan, branch 2 evaluated BEFORE branch 4 free-tier paywall) |
| T-39-05-02 | Compliance | WA/CT user served pharma paywall (WMHMDA/CTDPA breach) | mitigate | D-07 3-signal check (Vercel header at edge / cookie / profile.state); region-detect.ts reads cookie + store; profile wins. PaywallGate also short-circuits via variant-resolver Edge Fn (Plan 39-03). Defense at edge + client + server. |
| T-39-05-03 | Tampering | User edits profile.state_of_residence to 'WA' to bypass paywall | accept | False-positive cost is low (user voluntarily reduces pharma upsell exposure); regulatory posture is "reasonable effort + best knowledge" per D-07 — covers both edge-detected and self-asserted residents. Documented in CONTEXT D-07. |
| T-39-05-04 | Tampering | pharma_content_versions row tampered with retroactively | mitigate | RLS proven append-only by rls-pharma-content-versions.test.ts; no UPDATE/DELETE policies; writes flow only through Plan 39-08 SECDEF RPC; admin_audit_log row written by RPC for forensic trail |
| T-39-05-05 | Information disclosure | Pro full_content leaked to free user via React DevTools | mitigate | Free-tier branch returns ONLY summary string from the React subtree; full_content is fetched server-side at variant-resolver / RPC only if tier check passes; PharmaContentBlock NEVER receives full_content for free user (data fetcher gates server-side — documented contract for whoever wires PharmaContentBlock to a page-builder block payload) |
| T-39-05-06 | Compliance | SafetyInfoBadge missing on safety_category render | mitigate | Test case (b) and (e) assert badge presence; UI-SPEC mandates "ALWAYS rendered alongside safety-category content" |
</threat_model>

<verification>
- 2 vitest test files green (PharmaContentBlock, helpers)
- RLS proof file exists and references pharma_content_versions
- Cascade ordering verified: region -> safety -> tier -> paywall (test names map 1:1 to branches)
- SafetyInfoBadge uses sage (`--color-success-soft` / `--color-success`) NOT teal
- No inline text-[Npx] in any of the 7 source files
</verification>

<success_criteria>
- PHARMA-01/02/05/06/07 covered
- 3-layer phaCheck stack now end-to-end traceable (ESLint plan 39-02 + runtime plan 39-02 + render cascade plan 39-05)
- WA/CT carveout (D-07) operational via both server (Edge Fn) and client (region-detect.ts) checks
- Append-only RLS for audit log proven by automated test
</success_criteria>

<output>
Create `.planning/phases/.../39-05-SUMMARY.md` — list 9 files, the cascade branch order, the test case count (5 branches + 4 RLS assertions), and the tier_effective accessor name discovered in store.ts.
</output>
