---
phase: 39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a
plan: 09
type: execute
wave: 5
depends_on: [01, 02, 03, 06]
files_modified:
  - leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-09-SUMMARY.md
  - supabase/functions/page-render/render.ts
  - supabase/functions/page-render/render.test.ts
  - leanshot/src/components/admin/pages/PageEditorView.tsx
  - leanshot/src/components/admin/pages/PageEditorView.test.tsx
  - leanshot/src/components/admin/pages/BlockVariantDrawer.tsx
  - leanshot/src/components/admin/pages/BlockVariantDrawer.test.tsx
  - leanshot/e2e/admin/page-variant-create.spec.ts
autonomous: true
requirements:
  - PAGEAB-01
  - PAGEAB-02
  - PAGEAB-04
  - PAGEAB-06
must_haves:
  truths:
    - "supabase/functions/page-render/render.ts is extended to emit Vary: Cookie lt_variant_{page_id} header"
    - "page-render ISR cache key is now ${page_id}:${variant_id} so control + variant do NOT cross-poison (PAGEAB-04)"
    - "page-render emits <link rel=\"canonical\" href=\"/{control-slug}\"> when serving a variant (PAGEAB-02)"
    - "page-render block resolver: when BlockNode.variant_set_id is non-null, calls variant-resolver Edge Fn with body { surface:'page', block_id } and picks the resolved variant blocks before HTML emission (PAGEAB-06 + D-13)"
    - "PageEditorView is extended (Phase 15 self-extend) with 'Create variant' action in the editor toolbar + per-block 'Add variant' affordance"
    - "BlockVariantDrawer is a new Sheet-primitive drawer opened from the per-block 'Add variant' button; lists existing variants for that block + 'Add variant' CTA"
    - "Playwright e2e proves PAGEAB-01: admin creates a variant of a published page via the page-builder editor; page-variants row inserted; <link rel='canonical'> present in variant render"
  artifacts:
    - path: "supabase/functions/page-render/render.ts"
      provides: "Variant-aware page renderer with per-variant cache key + canonical link + per-block resolver"
      contains: "variant_set_id"
    - path: "leanshot/src/components/admin/pages/PageEditorView.tsx"
      provides: "Page editor extended with variant-creation affordances"
      contains: "Create variant"
    - path: "leanshot/src/components/admin/pages/BlockVariantDrawer.tsx"
      provides: "Per-block variant Sheet drawer (Surface D)"
      contains: "variant_set_id"
  key_links:
    - from: "supabase/functions/page-render/render.ts"
      to: "leanshot/src/lib/page-builder/block-schema.ts"
      via: "Deno mirror of BlockNode interface (incl. variant_set_id from Plan 39-02)"
      pattern: "variant_set_id"
    - from: "supabase/functions/page-render/render.ts"
      to: "supabase/functions/variant-resolver/index.ts"
      via: "fetch('/functions/v1/variant-resolver') per-block when variant_set_id present"
      pattern: "variant-resolver"
    - from: "leanshot/src/components/admin/pages/PageEditorView.tsx"
      to: "leanshot/src/components/admin/pages/BlockVariantDrawer.tsx"
      via: "Open drawer on per-block 'Add variant' click"
      pattern: "BlockVariantDrawer"
---

<objective>
Wave 5 admin slice C — extend the Phase 15 page-renderer + page-editor to support variant pages + per-block A/B (PAGEAB-01/02/04/06). Plan 39-07 covered the experiment dashboard's page tab; this plan covers the page-builder editor surfaces C + D where variants are actually authored + rendered.

Purpose: PAGEAB-01 (create variant), PAGEAB-02 (canonical link), PAGEAB-04 (ISR cache key), PAGEAB-06 (per-block A/B) all converge in this plan. The Deno render.ts mirror is the critical seam — must consume the new BlockNode.variant_set_id field added by Plan 39-02.

Output:
- Extended page-render Edge Fn with Vary header + per-variant cache key + canonical link + per-block resolver
- Extended PageEditorView with variant + per-block variant authoring affordances
- BlockVariantDrawer Sheet primitive for per-block variant management
- Playwright e2e for PAGEAB-01
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CONTEXT.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-UI-SPEC.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-PATTERNS.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-02-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-03-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-06-SUMMARY.md

<read_first>
- supabase/functions/page-render/render.ts — full read of existing Phase 15 renderer; find HTML emission point + ETag/cache header section + canonical link emission point.
- supabase/functions/page-render/render.test.ts — existing test shape; new test cases extend this file (NOT a new test file).
- leanshot/src/lib/page-builder/block-schema.ts (extended by Plan 39-02) — BlockNode now has optional variant_set_id; the Deno mirror in render.ts must reflect this.
- leanshot/src/components/admin/pages/PageEditorView.tsx — find toolbar region + per-block edit affordance region for the new buttons.
- leanshot/src/components/ui/Sheet.tsx — primitive contract; drawer slides in from right at 480px width per UI-SPEC.
- supabase/functions/variant-resolver/index.ts (Plan 39-03) — body shape { surface:'page', page_id, block_id } that this plan's render.ts calls with.
</read_first>

<interfaces>
page-render extensions:

Cache key:
  Pre-Phase-39: cache key = ${page_id}
  Post-Phase-39: cache key = ${page_id}:${variant_id ?? 'control'}
  Implementation: compute variant_id from cookie lt_variant_${page_id} OR call variant-resolver if absent; mix into ETag hash; Vary header on the cookie.

Vary header:
  Pre: (none for variant)
  Post: Vary: Cookie    -- specifically the lt_variant_{page_id} cookie partitions the edge cache

Canonical link:
  Pre: <link rel="canonical" href="/{slug}">
  Post: when variant_id != 'control': resolve canonical_page.slug from page_variants.canonical_page_id; emit <link rel="canonical" href="/{canonical_slug}">

Per-block resolver:
  When iterating BlockNode tree to emit HTML:
    if (block.variant_set_id) {
      const resolved = await fetch('/functions/v1/variant-resolver', { method:'POST', body: JSON.stringify({ surface:'page', page_id, block_id: block.id }), headers: { Authorization, Content-Type } });
      const { variant_id, config } = await resolved.json();
      // Replace block.content with config.block_content (admin-edited variant block)
    }
    // Emit block as normal

PageEditorView extensions:
  - Toolbar gets a 'Create variant' button (UI-SPEC copy). On click: opens a Modal that lets admin select canonical_page (current page) + initial traffic_split (default 50%); on save, INSERT page_variants row + navigate to the variant editor.
  - Each BlockNode in the editor canvas gets a small per-block 'Add variant' affordance (icon button in block toolbar). On click: opens BlockVariantDrawer for that block.

BlockVariantDrawer props:
  interface BlockVariantDrawerProps {
    open: boolean;
    onClose: () => void;
    block: BlockNode;
    onSave: (variantBlocks: BlockNode[]) => Promise<void>;
  }
  // Sheet from right, 480px wide
  // Lists existing variants for this block (if block.variant_set_id is set, lookup variant set)
  // 'Add variant' CTA at bottom; opens a mini-editor for the variant block content
  // Focus restoration on close: back to the 'Add variant' icon button that opened it

variant-resolver invocation from render.ts:
  Headers must forward the original page-render request's cookies + a service-role bearer (page-render is server-context, no user JWT; but variant-resolver requires a user JWT for cohort resolution — for anonymous/public page render, pass a synthetic anonymous JWT OR call a dedicated public path that returns variant by cookie alone).

  Implementation note: for public/anonymous page-render, the variant-resolver Edge Fn (Plan 39-03) handles unauthenticated via the lt_utm_source cookie fallback path; for logged-in user-context, the user JWT is in the original request and forwarded. page-render must detect both modes and forward appropriately.

  If variant-resolver returns 401 unauthenticated (anonymous path not supported in Plan 39-03's first cut), page-render falls back to: read lt_variant_${page_id} cookie -> if set, use that variant_id; else 'control'. This degradation is documented inline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend page-render with Vary + per-variant cache key + canonical + per-block resolver</name>
  <files>supabase/functions/page-render/render.ts, supabase/functions/page-render/render.test.ts</files>
  <behavior>
    - RED render.test.ts extensions: (a) request with cookie lt_variant_abc123=variant_x yields Response.headers.has('Vary') === true with value including 'Cookie'; (b) ETag/cache key includes variant_id suffix; (c) page-variants row with non-null canonical_page_id causes emitted HTML to contain <link rel="canonical" href="/{canonical_slug}">; (d) block with non-null variant_set_id triggers a variant-resolver fetch (mocked); (e) variant-resolver returns config.block_content -> emitted HTML contains the variant block content NOT the canonical block content; (f) variant-resolver 401 fallback -> emits canonical block content + no error to user.
    - GREEN: extend render.ts per `<interfaces>`. The per-block resolver runs INSIDE the existing BlockNode tree walk; minimal touch to the surrounding code.
    - REFACTOR: ensure the existing PAGEAB-02 canonical emission for non-variant pages remains intact (regression-test in invariant suite).
  </behavior>
  <action>
Pre-read render.ts in full. Find: cache key construction site; ETag emission; canonical link emission; BlockNode tree walk / switch block.

The cache key change is small (1-2 lines). The Vary header is a single header.set call. The canonical extension reads page_variants table to find canonical_page_id; if found, lookups its slug from landing_pages. The per-block resolver is a small async block inside the tree walk (must NOT block sequential block rendering — Promise.all OR sequential await depending on existing render style).

For the variant-resolver fetch: page-render runs in Deno; use the existing internal-function-invoke pattern from page-render (look for any existing `supabase.functions.invoke` or `fetch('/functions/v1/...')` in render.ts; mirror that).

CRITICAL — the Deno mirror of BlockNode in render.ts: per PATTERNS Section "leanshot/src/lib/page-builder/block-schema.ts" comment at line 17-18 of render.ts (read first to confirm), the Deno-side mirror MUST be updated in the SAME plan that updates block-schema.ts. Plan 39-02 added variant_set_id to the TS interface; this task adds it to the Deno mirror (if a literal mirror exists) OR updates the type-check predicate.

Per `[[reference_supabase_functions_deploy_import_map_flag]]`: any new shared imports require deploy with --import-map flag (Plan 39-10 close-out owns deploy; this plan documents the requirement in the SUMMARY).

Decision references: D-13 (per-block A/B independent), PAGEAB-02/04/06.
  </action>
  <verify>
    <automated>cd supabase && $HOME/.deno/bin/deno test --allow-all --no-check functions/page-render/render.test.ts 2>&1 | tail -15 && grep -q 'variant_set_id' functions/page-render/render.ts && grep -q 'Vary' functions/page-render/render.ts && grep -q 'canonical_page_id' functions/page-render/render.ts</automated>
  </verify>
  <done>render.test.ts GREEN across all new cases. variant_set_id, Vary header, canonical_page_id lookups all present in render.ts. Existing non-variant render path unaffected (regression-tested).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PageEditorView + BlockVariantDrawer + Playwright e2e for PAGEAB-01</name>
  <files>leanshot/src/components/admin/pages/PageEditorView.tsx, leanshot/src/components/admin/pages/PageEditorView.test.tsx, leanshot/src/components/admin/pages/BlockVariantDrawer.tsx, leanshot/src/components/admin/pages/BlockVariantDrawer.test.tsx, leanshot/e2e/admin/page-variant-create.spec.ts</files>
  <behavior>
    - RED PageEditorView extension: (a) toolbar has a 'Create variant' button (per UI-SPEC copy); (b) clicking it opens a Modal with traffic-split TrafficSplitSlider (reused from Plan 39-07) + 'Publish variant' CTA; (c) Publish action calls supabase from('page_variants').insert and navigates to the variant editor; (d) each BlockNode rendered in canvas has a per-block 'Add variant' affordance (icon button in block toolbar); (e) clicking 'Add variant' opens BlockVariantDrawer for that block.
    - RED BlockVariantDrawer: (a) Sheet renders from right at 480px width; (b) shows existing variants when block.variant_set_id is set; (c) 'Add variant' CTA at bottom; (d) Save invokes onSave callback with variantBlocks payload; (e) close restores focus to the triggering 'Add variant' button.
    - GREEN: implement both per `<interfaces>`. Reuse Modal + Sheet primitives.
    - Playwright e2e page-variant-create.spec.ts: seed admin user; seed 1 published landing_page; navigate to /admin/pages/{id}/edit; click 'Create variant'; fill 50% traffic split; click 'Publish variant'; assert page_variants row inserted via direct supabase query; navigate to variant URL; assert <link rel="canonical"> attribute in DOM.
  </behavior>
  <action>
Pre-read PageEditorView.tsx in full to find: toolbar region; block-toolbar / block-edit affordance region; the existing pattern for opening Modal / Sheet.

Pre-read TrafficSplitSlider (Plan 39-07) for the import path.

BlockVariantDrawer focus restoration: store the triggering button's ref/DOM-node on open; .focus() it on close. Test asserts this via document.activeElement check.

UI-SPEC Hard Constraints:
- 'Create variant', 'Add variant', 'Publish variant' copy verbatim from UI-SPEC copy table (Workstream B).
- Drawer 480px = w-[480px] is FORBIDDEN inline arbitrary value; use existing Sheet primitive size prop OR a documented Tailwind size class (lg breakpoint).
- Accent --color-primary only on the primary CTAs ('Create variant', 'Publish variant', 'Save').

Playwright spec uses addInitScript for state seeding (per project convention).

Per Pattern M: post-Edit, git status --short.

Decision references: D-13 (per-block A/B), PAGEAB-01/06.
  </action>
  <verify>
    <automated>cd leanshot && npx vitest run src/components/admin/pages/PageEditorView.test.tsx src/components/admin/pages/BlockVariantDrawer.test.tsx 2>&1 | tail -10 && test -f e2e/admin/page-variant-create.spec.ts && grep -q 'Create variant' src/components/admin/pages/PageEditorView.tsx && grep -q 'BlockVariantDrawer' src/components/admin/pages/PageEditorView.tsx</automated>
  </verify>
  <done>2 vitest suites GREEN. Playwright spec exists. PageEditorView has 'Create variant' + per-block 'Add variant' affordances. BlockVariantDrawer Sheet drawer focus restoration verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor -> page-render Edge Fn | Anonymous page render path; variant_id resolved from cookie OR variant-resolver (fallback to control on 401) |
| Variant cookie -> ISR cache partitioning | Vary: Cookie + cache key = ${page_id}:${variant_id} prevents cross-variant cache poisoning |
| Admin -> page_variants table | INSERT via direct table write (admin RLS); future plans may move this to a SECDEF RPC if writes proliferate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-09-01 | Information disclosure | Variant page cached + served to control-cohort users (cross-cache-poisoning) | mitigate | PAGEAB-04 enforced via Vary header + per-variant cache key; render.test.ts asserts cache key includes variant_id |
| T-39-09-02 | Compliance / SEO | Variant page emits <link rel='canonical'> to itself instead of control -> Google penalty (V13-4) | mitigate | PAGEAB-02 enforced: canonical resolved from page_variants.canonical_page_id -> landing_pages.slug; render.test.ts asserts |
| T-39-09-03 | Tampering | Block-level variant content edited without audit | accept (deferred) | Per-block variant audit deferred to Phase 39 v1.4 — for now, admin_audit_log row written on PUBLISH event (PageEditorView wires this OPTIONALLY; if not wired in this plan, documented in SUMMARY for future enhancement) |
| T-39-09-04 | DoS | Per-block variant-resolver fetch storm during page render | mitigate | variant-resolver short-circuits via existing user_experiments row on repeat call (Plan 39-03 step 1); page-render caches block-level results in the ISR cache key |
| T-39-09-05 | Repudiation | Variant created without traceability to operator | mitigate | page_variants.created_by FK to auth.users; PageEditorView submits via supabase client with user JWT |
</threat_model>

<verification>
- Deno render.test.ts GREEN with 6 new test cases
- 2 vitest suites GREEN
- Playwright e2e file exists
- 'variant_set_id' literal present in render.ts (Deno mirror updated)
- 'Vary' literal present in render.ts response header set
- 'canonical_page_id' lookup present in render.ts
- 'Create variant' literal copy present in PageEditorView (UI-SPEC compliance)
- BlockVariantDrawer wraps Sheet primitive (no new drawer abstraction)
</verification>

<success_criteria>
- PAGEAB-01/02/04/06 all wired
- Existing Phase 15 page-render behavior unchanged for non-variant pages (regression-tested)
- Plan 39-10 close-out can deploy page-render with the --import-map flag and the variant-aware render goes live
</success_criteria>

<output>
Create `.planning/phases/.../39-09-SUMMARY.md` — list 7 files, the 6 new render.test.ts cases, the deploy-time --import-map requirement (Plan 39-10 owns), the deferred per-block-variant audit-log enhancement.
</output>
