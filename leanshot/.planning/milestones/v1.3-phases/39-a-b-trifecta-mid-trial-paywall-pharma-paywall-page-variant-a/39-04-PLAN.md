---
phase: 39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-04-SUMMARY.md
  - leanshot/src/components/paywall/PaywallModal.tsx
  - leanshot/src/components/paywall/PaywallModal.test.tsx
  - leanshot/src/components/paywall/PaywallGate.tsx
  - leanshot/src/components/paywall/PaywallGate.test.tsx
  - leanshot/src/components/paywall/OnboardingFlowPaywall/index.tsx
  - leanshot/src/components/paywall/OnboardingFlowPaywall/Screen1.tsx
  - leanshot/src/components/paywall/OnboardingFlowPaywall/Screen2.tsx
  - leanshot/src/components/paywall/OnboardingFlowPaywall/Screen3.tsx
  - leanshot/src/components/paywall/OnboardingFlowPaywall/Screen4.tsx
  - leanshot/src/components/paywall/OnboardingFlowPaywall/Screen5.tsx
  - leanshot/src/components/paywall/OnboardingFlowPaywall/Screen6.tsx
  - leanshot/src/components/paywall/OnboardingFlowPaywall/__tests__/index.test.tsx
  - leanshot/src/lib/paywall/consent-adapter.ts
  - leanshot/src/lib/paywall/consent-adapter.test.ts
  - leanshot/src/lib/utm/capture-first-touch.ts
  - leanshot/src/lib/utm/capture-first-touch.test.ts
  - leanshot/e2e/paywall-mid-trial.spec.ts
autonomous: true
requirements:
  - PAYWALL-01
  - PAYWALL-02
  - PAYWALL-05
  - PAYWALL-06
  - PAYWALL-07
must_haves:
  truths:
    - "PaywallModal renders only when (a) activation_events row exists for user AND (b) consent-adapter returns tracking=true"
    - "PaywallModal calls supabase.functions.invoke('variant-resolver', { body: { surface: 'paywall' } }) on mount; on resolve, displays variant.config.headline + CTA"
    - "PaywallGate wraps content and short-circuits to free if (a) phaCheck throws not (D-06 layer 2), or (b) content.safety_category is non-null (D-05), or (c) consent-adapter tracking=false"
    - "OnboardingFlowPaywall renders EXACTLY 6 screens in fixed order: value-pillar-1, value-pillar-2, value-pillar-3, social-proof, pricing, final-CTA (D-14)"
    - "Screen 6 final-CTA heading uses .font-display (Fraunces) — single accent moment per UI-SPEC; all other screens use Geist"
    - "Dismiss flow uses inline confirm card (NOT modal-on-modal) per UI-SPEC copy table"
    - "consent-adapter.ts provides getPaywallTrackingConsent(): boolean stable contract regardless of upstream Phase 22 cookieConsent shape (RESEARCH OQ-2 resolution)"
    - "capture-first-touch.ts mounts at the top of the App.tsx component body so it runs once per app load (post-React-mount, but before any paywall surface evaluates the cookie). Mounted via App body — NOT main.tsx — because tests stub window.location after main.tsx init. Sets lt_utm_source cookie from first-visit URL query (PAYWALL-07 / RESEARCH OQ-5)."
    - "Typography ramp strict: only text-sm / text-base / text-xl / text-2xl + font-normal/font-bold (UI-SPEC Hard Constraint 1)"
    - "Accent --color-primary only on primary CTA + active progress dot per UI-SPEC accent allowlist"
    - "e2e/paywall-mid-trial.spec.ts asserts PAYWALL-01 end-to-end: trialing user post-activation sees paywall variant"
  artifacts:
    - path: "leanshot/src/components/paywall/PaywallModal.tsx"
      provides: "Single-screen paywall (Surface A) — wraps Modal primitive"
      contains: "supabase.functions.invoke('variant-resolver'"
    - path: "leanshot/src/components/paywall/PaywallGate.tsx"
      provides: "Generic wrapper that calls phaCheck + consent gate"
      contains: "phaCheck"
    - path: "leanshot/src/components/paywall/OnboardingFlowPaywall/index.tsx"
      provides: "6-screen state machine container (Surface B)"
      contains: "SCREENS"
    - path: "leanshot/src/lib/paywall/consent-adapter.ts"
      provides: "Stable consent contract for paywall mount-gate"
      contains: "getPaywallTrackingConsent"
    - path: "leanshot/src/lib/utm/capture-first-touch.ts"
      provides: "lt_utm_source cookie writer for first-touch UTM capture"
      contains: "lt_utm_source"
    - path: "leanshot/e2e/paywall-mid-trial.spec.ts"
      provides: "Playwright e2e proof of PAYWALL-01"
      contains: "activation"
  key_links:
    - from: "leanshot/src/components/paywall/PaywallModal.tsx"
      to: "supabase/functions/variant-resolver/index.ts"
      via: "supabase.functions.invoke on mount"
      pattern: "variant-resolver"
    - from: "leanshot/src/components/paywall/PaywallGate.tsx"
      to: "leanshot/src/lib/pharma/phaCheck.ts"
      via: "phaCheck(content) before paywall render"
      pattern: "import { phaCheck }"
    - from: "leanshot/src/components/paywall/OnboardingFlowPaywall/index.tsx"
      to: "leanshot/src/lib/paywall/consent-adapter.ts"
      via: "getPaywallTrackingConsent() called at mount; null returned if false"
      pattern: "getPaywallTrackingConsent"
    - from: "leanshot/src/lib/utm/capture-first-touch.ts"
      to: "supabase/functions/variant-resolver/index.ts (cookie read)"
      via: "lt_utm_source cookie set at first visit, read by variant-resolver"
      pattern: "lt_utm_source"
---

<objective>
Wave 3 consumer slice A — ship the mid-trial paywall surfaces (A single-screen modal + B 6-screen onboarding flow) plus the PaywallGate wrapper, the consent-adapter (RESEARCH OQ-2 resolution), and the first-touch UTM cookie writer (RESEARCH OQ-5 resolution).

Purpose: Activation-triggered paywall is the FRONT DOOR of v1.3 monetization. Every variant_config, every cohort decision, every UTM map row from Waves 1-2 manifests here. This plan owns the consumer-visible PAYWALL-01/02/05/06/07 surfaces — five REQ-IDs in one consumer wave-3 plan because all five share the same component tree + variant-resolver invoke + Modal primitive.

Output:
- 2 paywall surfaces (PaywallModal + 6-screen OnboardingFlowPaywall) — 8 component files
- 1 PaywallGate wrapper (consumed by PharmaContentBlock in Plan 39-05)
- 1 consent-adapter shim (decouples paywall from Phase 22 shape per RESEARCH A2/OQ-2)
- 1 UTM capture helper (PAYWALL-07 dependency that Phase 51 will later adopt; see RESEARCH OQ-5)
- 1 Playwright e2e proof of PAYWALL-01
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CONTEXT.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-UI-SPEC.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-PATTERNS.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-02-SUMMARY.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-03-SUMMARY.md

<read_first>
- leanshot/src/lib/consent/consent-defer.ts — HEAD 30 lines to introspect actual API shape (RESEARCH OQ-2). The consent-adapter wraps whatever this exports to provide getPaywallTrackingConsent(): boolean.
- leanshot/src/components/consent/CookieConsentBootstrap.tsx — sibling that shows the consent surface (do NOT modify; adapter is independent).
- leanshot/src/components/onboarding/OnboardingFlow.tsx — pattern source for multi-step state machine (per PATTERNS — read once, mirror the step-machine pattern, do NOT fork the file).
- leanshot/src/components/ui/Modal.tsx — primitive contract (role="dialog", aria-modal, focus trap).
- leanshot/src/components/admin/onboarding-builder/OnboardingABPanel.tsx — Pattern A vendor-gated soft-banner + invoke pattern (lines 56-96 area).
- leanshot/src/lib/supabase.ts — supabase client factory consumed by Edge Fn invocations.
- leanshot/src/App.tsx — find a clean pre-paint mount point for capture-first-touch (mirrors how `applyThemeToDOM` runs in main.tsx).
- supabase/functions/variant-resolver/index.ts (from Plan 39-03) — variant-resolver response shape `{ variant_id, config }` that PaywallModal/OnboardingFlowPaywall consume.
</read_first>

<interfaces>
PaywallModal props:
  interface PaywallModalProps { open: boolean; onClose: () => void; cohortHint?: string }

PaywallGate props:
  interface PaywallGateProps {
    content: { safety_category?: string | null; [k: string]: unknown };
    children: React.ReactNode;
    surface?: 'pharma' | 'paywall';   // default 'paywall'
  }

OnboardingFlowPaywall props:
  interface OnboardingFlowPaywallProps { open: boolean; onClose: () => void }

SCREENS const (D-14):
  const SCREENS = ['value-pillar-1', 'value-pillar-2', 'value-pillar-3', 'social-proof', 'pricing', 'final-CTA'] as const;
  type ScreenKey = typeof SCREENS[number];

Per-screen component prop contract:
  interface ScreenProps {
    config: Record<string, unknown>;   // variant_config.config payload
    onNext: () => void;
    onBack?: () => void;
    onDismiss: () => void;
    stepLabel: string;                  // "Step N of 6"
  }

consent-adapter.ts:
  export function getPaywallTrackingConsent(): boolean;
  // Wraps src/lib/consent/consent-defer.ts; returns true ONLY when tracking is explicitly granted.
  // OQ-2 resolution: regardless of upstream shape (tracking boolean OR analytics+marketing struct),
  // this helper boils down to a single boolean. Used by PaywallModal + OnboardingFlowPaywall + PaywallGate.

capture-first-touch.ts:
  export function captureFirstTouchUtm(): void;
  // Reads window.location.search for utm_source. If lt_utm_source cookie is unset AND a utm_source query exists,
  // sets document.cookie = `lt_utm_source=${encoded}; max-age=31536000; SameSite=Lax; Secure` (1 year).
  // Idempotent: if cookie already present, does nothing.
  // Per OQ-5: Phase 39 OWNS this cookie key; Phase 51 will adopt the same key when shipping.
  // Cookie value is URL-decoded utm_source; HTTPOnly NOT set because document.cookie write requires JS-readable cookie.

variant-resolver invocation pattern (every paywall mount):
  const { data, error } = await supabase.functions.invoke<{ variant_id: string; config: Record<string, unknown> }>(
    'variant-resolver',
    { body: { surface: 'paywall' } }
  );
  // On error or non-200 -> silent skip (paywall does not render); per UI-SPEC Interaction States: NO loading state visible.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: consent-adapter + capture-first-touch + PaywallGate (Wave 3 foundation for both surfaces in this plan and Plan 39-05)</name>
  <files>leanshot/src/lib/paywall/consent-adapter.ts, leanshot/src/lib/paywall/consent-adapter.test.ts, leanshot/src/lib/utm/capture-first-touch.ts, leanshot/src/lib/utm/capture-first-touch.test.ts, leanshot/src/components/paywall/PaywallGate.tsx, leanshot/src/components/paywall/PaywallGate.test.tsx</files>
  <behavior>
    - RED consent-adapter: test that mocking consent-defer to return {tracking: true} yields true; {tracking: false} yields false; missing/undefined yields false. Test covers BOTH possible upstream shapes (boolean vs analytics+marketing struct per OQ-2).
    - GREEN consent-adapter: implement getPaywallTrackingConsent() that reads from the actual consent-defer.ts surface as it exists post-Read. Defensive coding for undefined/null upstream.
    - RED capture-first-touch: tests that window.location.search='?utm_source=lean' sets lt_utm_source cookie to 'lean'; existing cookie is NOT overwritten; no utm_source query = no cookie write.
    - GREEN capture-first-touch: implement per `<interfaces>`. URL-decode utm_source value; SameSite=Lax + Secure + 1y max-age.
    - RED PaywallGate: tests (a) safety_category content renders children directly (no paywall) — D-05; (b) consent=false renders children directly; (c) consent=true + non-safety content + variant-resolver mock returns variant -> renders paywall stub (use a mock variant render slot); (d) phaCheck throws -> caught and renders free content (defense-in-depth).
    - GREEN PaywallGate: implement per `<interfaces>` with the cascade: phaCheck(content) in try/catch -> safety check -> consent check -> variant-resolver invoke -> render.
  </behavior>
  <action>
Pre-read consent-defer.ts (RESEARCH OQ-2: shape NOT verified). Then write consent-adapter as a thin shim that handles either shape gracefully.

PaywallGate test note: mock supabase.functions.invoke via vi.mock at top of test file (project convention from CACDashboardPage.test.tsx). The render-slot for variant content is just a `<div data-testid="paywall-content">` for now; full visual paywall lives in PaywallModal/OnboardingFlowPaywall.

Per UI-SPEC Hard Constraint #5 — NO useQuery/useMutation imports. All async via useEffect + setState + cancellation flag.

Decision references: D-05 (safety carveout), D-06 (phaCheck layer 2 — already from Plan 39-02), D-09 (UTM cookie shape), RESEARCH OQ-2 + OQ-5 resolutions.
  </action>
  <verify>
    <automated>cd leanshot && npx vitest run src/lib/paywall src/lib/utm src/components/paywall/PaywallGate.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>All 4 test files green. consent-adapter handles both upstream shapes. capture-first-touch idempotent + URL-decoded. PaywallGate cascade ordered (safety > consent > variant-resolver).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PaywallModal + OnboardingFlowPaywall 6-screen flow + App.tsx UTM mount + Playwright e2e</name>
  <files>leanshot/src/components/paywall/PaywallModal.tsx, leanshot/src/components/paywall/PaywallModal.test.tsx, leanshot/src/components/paywall/OnboardingFlowPaywall/index.tsx, leanshot/src/components/paywall/OnboardingFlowPaywall/Screen1.tsx, leanshot/src/components/paywall/OnboardingFlowPaywall/Screen2.tsx, leanshot/src/components/paywall/OnboardingFlowPaywall/Screen3.tsx, leanshot/src/components/paywall/OnboardingFlowPaywall/Screen4.tsx, leanshot/src/components/paywall/OnboardingFlowPaywall/Screen5.tsx, leanshot/src/components/paywall/OnboardingFlowPaywall/Screen6.tsx, leanshot/src/components/paywall/OnboardingFlowPaywall/__tests__/index.test.tsx, leanshot/e2e/paywall-mid-trial.spec.ts</files>
  <behavior>
    - RED PaywallModal: tests (a) open=false renders null; (b) open=true + consent=false renders null; (c) open=true + consent=true + variant-resolver mock returns {variant_id:'control', config:{headline:'Start subscription'}} -> renders Modal with that headline + primary CTA "Start subscription" + secondary "Maybe later"; (d) Modal inherits role="dialog" + aria-modal="true" from primitive.
    - GREEN PaywallModal: wrap Modal primitive; useEffect invokes variant-resolver on mount; setState variant. Vendor-unconfigured response renders the soft banner from UI-SPEC (per Pattern A).
    - RED OnboardingFlowPaywall: tests (a) renders exactly 6 screens in fixed order (D-14); (b) progress dot count = 6; (c) sr-only "Step N of 6" updates as user advances; (d) Screen 6 heading uses `.font-display` className (Fraunces accent moment per UI-SPEC); (e) dismiss mid-flow shows inline confirm card (NOT a second Modal); (f) consent=false renders null.
    - GREEN OnboardingFlowPaywall: implement state machine; per-screen component composition; inline dismiss confirm (uses `Card` primitive, NOT `Modal`).
    - Each Screen1..6.tsx is a minimal presentational component with the standardized ScreenProps contract; screen-specific copy comes from variant_config.config (admin-edited per UI-SPEC).
    - Playwright e2e (paywall-mid-trial.spec.ts): seed a test user with consent.tracking=true via addInitScript (per [[reference_playwright_state_seeding]]); navigate to dashboard; trigger record_activation RPC; assert PaywallModal becomes visible.
  </behavior>
  <action>
Pre-read OnboardingFlow.tsx ONCE for the step-machine shape (do NOT copy verbatim — borrow useState<number>(0) + advance/back/dismiss handlers). Pre-read Modal.tsx + Card.tsx primitives.

Per UI-SPEC Hard Constraints:
- Typography: only text-sm/text-base/text-xl/text-2xl + font-normal/font-bold. Screen 5 pricing $-amount uses text-2xl font-bold; Screen 6 final-CTA heading uses text-2xl font-bold .font-display (Fraunces opt-in per ramp); per-screen headings (Screens 1-5) use text-xl font-bold.
- Spacing: only the 7 tokens from UI-SPEC spacing scale.
- Accent --color-primary only on: primary CTA buttons + active progress dot.
- NO inline text-[Npx] / font-[Nnnn] / arbitrary hex literals.

App.tsx wiring: import + invoke captureFirstTouchUtm() at the top of the App component body (runs once per app load; idempotent). DO NOT mount in main.tsx — needs to run after React mount because tests stub window.location after main.tsx initialization.

Per UI-SPEC accessibility: sr-only `<p>Step {step + 1} of 6</p>` + visual 6-dot row marked aria-hidden. Dismiss action button has aria-label.

The 6-screen inline dismiss confirm uses Card primitive with two buttons (Keep watching = primary, Yes skip = ghost). NOT a Modal-on-Modal.

Decision references: D-09 (UTM map), D-14 (6-screen fixed order), PAYWALL-01/02/05/06/07.

Per Pattern M: post-Edit run `git status --short` to confirm all 11 expected files dirty.
  </action>
  <verify>
    <automated>cd leanshot && npx vitest run src/components/paywall/PaywallModal.test.tsx src/components/paywall/OnboardingFlowPaywall/__tests__/index.test.tsx 2>&1 | tail -15 && grep -c "^const SCREENS = \['value-pillar-1'" src/components/paywall/OnboardingFlowPaywall/index.tsx && ls e2e/paywall-mid-trial.spec.ts</automated>
  </verify>
  <done>Vitest GREEN for PaywallModal + OnboardingFlowPaywall; SCREENS constant declared exactly once with the 6 fixed labels in order; e2e file exists. UI-SPEC hard constraints honored (verified by lint:safety from Plan 39-02 + manual grep).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser document.cookie -> variant-resolver server | lt_utm_source cookie carried via standard cookie header; server reads first-touch UTM at variant assignment |
| Phase 22 consent state -> paywall mount | Adapter shim provides stable boolean contract; paywall returns null silently if consent=false |
| User content -> phaCheck guard | PaywallGate calls phaCheck before any paywall render; safety_category short-circuits to free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04-01 | Compliance | Paywall mounts before cookie consent grant -> GDPR breach | mitigate | consent-adapter returns false unless explicitly granted; PaywallModal + OnboardingFlowPaywall return null silently when false; PaywallGate.test asserts this (UI-SPEC Hard Constraint #6) |
| T-39-04-02 | Compliance | Pharma safety content paywalled at runtime | mitigate | PaywallGate calls phaCheck(content) FIRST; content.safety_category short-circuits to render children directly; defense-in-depth above the ESLint rule from Plan 39-02 |
| T-39-04-03 | Tampering | lt_utm_source cookie tampered to claim a different variant | accept (low value) | Cookie influences variant assignment only at first-touch; subsequent calls read user_experiments.utm_variant_id from DB (server-authoritative); tampering only affects one user's own variant, no cross-user impact |
| T-39-04-04 | Information disclosure | Paywall variant fetch leaks cohort_id in client devtools | mitigate | variant-resolver returns {variant_id, config}; cohort_id not in response payload. Inspect Plan 39-03 contract; downstream consumer never sees cohort_id |
| T-39-04-05 | Repudiation | User dismisses paywall, no record of attempt | mitigate | variant-resolver UPSERTS user_experiments + captureServer fires before response returns; even if dismissed, the attempt is logged server-side |
| T-39-04-06 | DoS | Repeat-fire of variant-resolver on every render | mitigate | useEffect with empty dep array (mount only); cancellation flag prevents double-invoke on StrictMode double-render |
</threat_model>

<verification>
- All 3 vitest files green
- Playwright e2e file exists (executed in milestone-close UAT)
- SCREENS constant has exactly 6 entries in fixed D-14 order
- consent-adapter shim is the ONLY file referencing src/lib/consent/* from paywall code (decoupling proof)
- No inline `text-[` or `font-[` patterns in any of the 11 new component files
- No `useQuery` or `useMutation` imports anywhere in paywall/* (UI-SPEC Hard Constraint #5)
</verification>

<success_criteria>
- PAYWALL-01/02/05/06/07 covered by component + helper files
- UI-SPEC Hard Constraints 1, 4, 5, 6, 9 all honored in this plan's files
- variant-resolver is invoked from exactly 2 surfaces (PaywallModal + OnboardingFlowPaywall) with body `{ surface: 'paywall' }`
- PaywallGate becomes the consumer-side base class for Plan 39-05 PharmaContentBlock
- Phase 39 owns the lt_utm_source cookie key per RESEARCH OQ-5
</success_criteria>

<output>
Create `.planning/phases/.../39-04-SUMMARY.md` per template — list 17 files, the 6-screen order verbatim, the consent-adapter resolution of OQ-2 (which shape Phase 22 used), and the Playwright spec name for milestone-close UAT reference.

scope_sanity ack — 17 files in this plan; 6 OnboardingFlowPaywall screen files are homogenous presentational shells (same ScreenProps contract, copy from variant_config), so executor handoff hazard is bounded if context tightens. No split required.
</output>
