---
phase: 39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-02-SUMMARY.md
  - leanshot/eslint-rules/no-paywall-on-safety-category.cjs
  - leanshot/eslint-rules/__tests__/no-paywall-on-safety-category.test.cjs
  - leanshot/scripts/check-no-paywall-on-safety-category.sh
  - leanshot/src/lib/pharma/phaCheck.ts
  - leanshot/src/lib/pharma/__tests__/phaCheck.test.ts
  - leanshot/src/lib/page-builder/block-schema.ts
  - leanshot/src/lib/page-builder/__tests__/block-schema.test.ts
  - leanshot/eslint.config.js
  - leanshot/package.json
autonomous: true
requirements:
  - PHARMA-02
  - PAGEAB-06
must_haves:
  truths:
    - "leanshot/eslint-rules/no-paywall-on-safety-category.cjs exists, mirrors no-conditional-native-review.cjs shape, and reports a JSXElement <Paywall|PaywallGate|PaywallModal> wrapping safety_category"
    - "Rule is REGISTERED in leanshot/eslint.config.js so `npm run lint` triggers it"
    - "leanshot/src/lib/pharma/phaCheck.ts exports phaCheck(content) that throws in dev/test when content.safety_category is non-null and warn-logs in prod"
    - "leanshot/scripts/check-no-paywall-on-safety-category.sh comment-strips before grep and exits non-zero on Paywall proximity to safety_category"
    - "scripts/check-no-paywall-on-safety-category.sh is wired to package.json (lint:safety or similar) so CI can invoke it"
    - "BlockNode interface in src/lib/page-builder/block-schema.ts is extended with optional variant_set_id?: string per D-13 (PAGEAB-06)"
  artifacts:
    - path: "leanshot/eslint-rules/no-paywall-on-safety-category.cjs"
      provides: "Build-time AST rule (layer 1 of D-06)"
      contains: "PAYWALL_COMPONENTS"
    - path: "leanshot/eslint-rules/__tests__/no-paywall-on-safety-category.test.cjs"
      provides: "RuleTester proof"
      contains: "RuleTester"
    - path: "leanshot/src/lib/pharma/phaCheck.ts"
      provides: "Runtime assertion (layer 2 of D-06)"
      contains: "export function phaCheck"
    - path: "leanshot/scripts/check-no-paywall-on-safety-category.sh"
      provides: "CI grep gate (layer 3 of D-06)"
      contains: "safety_category"
    - path: "leanshot/src/lib/page-builder/block-schema.ts"
      provides: "BlockNode interface extended with variant_set_id?: string"
      contains: "variant_set_id"
  key_links:
    - from: "leanshot/eslint.config.js"
      to: "leanshot/eslint-rules/no-paywall-on-safety-category.cjs"
      via: "rule registration"
      pattern: "no-paywall-on-safety-category"
    - from: "leanshot/package.json"
      to: "leanshot/scripts/check-no-paywall-on-safety-category.sh"
      via: "npm script"
      pattern: "check-no-paywall-on-safety-category"
---

<objective>
Wave 1 sibling-of-39-01 — ship the three-layer phaCheck enforcement (D-06) PLUS the BlockNode.variant_set_id extension (D-13 / PAGEAB-06). This plan is dispatchable in parallel with 39-01 (zero file overlap).

Purpose: PHARMA-02 safety-info NEVER-paywalled is the hardest regulatory invariant in the phase; D-06 mandates THREE independent layers (build / runtime / CI grep). All three land in this plan + are registered against the project's lint pipeline so Wave 2/3/4 plans pick up the rule automatically. Simultaneously, the page-builder schema extension is small enough (one optional field) to co-ship without contention.

Output:
- 3 enforcement layers (ESLint AST rule + runtime helper + CI grep)
- 1 BlockNode interface extension
- Test coverage for all four files (RuleTester + Vitest + bash dry-run)
- Rule registration in eslint.config.js + npm script in package.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CONTEXT.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-RESEARCH.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-PATTERNS.md

<read_first>
- leanshot/eslint-rules/no-conditional-native-review.cjs — verbatim shape source for the new rule (Pattern K). Read once, mirror identifier-set + JSXElement visitor + module.exports messages structure.
- leanshot/eslint-rules/no-conditional-native-review.test.cjs — RuleTester test shape; mirror invalid/valid case structure.
- leanshot/eslint-rules/additive-only-events.cjs — sibling .cjs rule for ESLint rule-registration convention (.cjs because package.json declares "type": "module").
- leanshot/src/lib/page-builder/block-schema.ts — read full file to internalize BlockNode shape; the variant_set_id field is APPENDED (not inserted in the middle) to preserve diff readability.
- leanshot/scripts/check-taxo-06-reconciliation.sh — closest analog for the CI grep gate shape (comment-strip + grep with -B/-A context).
- leanshot/eslint.config.js — read to find the existing rule-registration block where the new rule key gets added.
</read_first>

<interfaces>
Expected exports / signatures:

phaCheck.ts:
  export interface PharmaContent { safety_category?: string | null; [k: string]: unknown }
  export function phaCheck(content: PharmaContent): void
  -- throws Error in import.meta.env.DEV or MODE === 'test'
  -- console.warn otherwise (prod = no crash; defense-in-depth)

no-paywall-on-safety-category.cjs (ESLint AST):
  module.exports = {
    meta: { type: 'problem', docs: { ... }, messages: { paywallOnSafety: '...' }, schema: [] },
    create(context) {
      return {
        JSXElement(node) { /* if name in PAYWALL_COMPONENTS && subtree references safety_category => report */ }
      };
    }
  };

block-schema.ts BlockNode extension:
  export interface BlockNode {
    id: string;
    type: BlockType;
    parent_id: string | null;
    order: number;
    content: Record<string, unknown>;
    style?: BlockStyle;
    variant_set_id?: string;   // NEW PHASE 39 D-13 — optional FK to page_variants.id at runtime
  }

check-no-paywall-on-safety-category.sh:
  - set -euo pipefail
  - grep -rn --include='*.tsx' --include='*.ts' -B 10 -A 10 'safety_category' leanshot/src/
  - pipe through `sed -E 's://.*$::; s:/\\*[^*]*\\*+([^/*][^*]*\\*+)*/::g'` (comment strip per [[reference_grep_gate_comment_strip]])
  - grep -E '(Paywall|PaywallGate|PaywallModal)'
  - exit 1 if any hits

package.json script wiring:
  "lint:safety": "bash scripts/check-no-paywall-on-safety-category.sh"
  (Or extend an existing CI script like "lint:phase-gates" if present.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ESLint AST rule + RuleTester proof + eslint.config.js registration</name>
  <files>leanshot/eslint-rules/no-paywall-on-safety-category.cjs, leanshot/eslint-rules/__tests__/no-paywall-on-safety-category.test.cjs, leanshot/eslint.config.js</files>
  <behavior>
    - RED: Add to eslint-rules/__tests__/no-paywall-on-safety-category.test.cjs a RuleTester `invalid` case: a JSX snippet like `<PaywallGate>{content.safety_category}</PaywallGate>` MUST produce the `paywallOnSafety` error message. A `valid` case: `<PaywallGate>{content.other_field}</PaywallGate>` MUST produce zero errors. Run the test — RED.
    - GREEN: Author no-paywall-on-safety-category.cjs mirroring no-conditional-native-review.cjs shape. PAYWALL_COMPONENTS Set('Paywall','PaywallGate','PaywallModal'). Walk subtree of detected JSXElement for any Identifier or MemberExpression accessing `safety_category`. Bail at function boundaries (mirror no-conditional-native-review FUNCTION_BOUNDARY_TYPES). Then register the rule in eslint.config.js so `npm run lint` picks it up. Run RuleTester + `npm run lint` — both GREEN.
    - REFACTOR: Compare against no-conditional-native-review.cjs; ensure identical structure for messages/meta/visitor; consolidate any duplicate walk helpers.
  </behavior>
  <action>
Pre-read no-conditional-native-review.cjs + its test once. Then author the new rule + test. Filename is `.cjs` because package.json declares `"type": "module"` (mirrors sibling rules per PATTERNS Pattern K).

Test cases (in order — covers each PAYWALL_COMPONENTS member + safety vs non-safety subtree):
1. invalid: `<Paywall>{content.safety_category}</Paywall>` -> paywallOnSafety
2. invalid: `<PaywallGate>{x.safety_category && <span/>}</PaywallGate>` -> paywallOnSafety
3. invalid: `<PaywallModal>{ data['safety_category'] }</PaywallModal>` -> paywallOnSafety (MemberExpression form)
4. valid: `<PaywallGate>{content.dosage}</PaywallGate>` -> 0 errors
5. valid: `<div>{content.safety_category}</div>` -> 0 errors (not a Paywall* wrapper)
6. valid: function boundary bail — Paywall containing an inline arrow `() => content.safety_category` (the safety access is inside a child function, not the JSX subtree visible at render) -> 0 errors

Register the rule in `leanshot/eslint.config.js` under the project's local-rules plugin (consult existing additive-only-events / no-conditional-native-review registrations). Severity `error` so CI fails on violation.
  </action>
  <verify>
    <automated>cd leanshot && npx eslint --print-config src/App.tsx 2>/dev/null | grep -q 'no-paywall-on-safety-category' && node --experimental-vm-modules ./node_modules/.bin/mocha eslint-rules/__tests__/no-paywall-on-safety-category.test.cjs 2>&1 | tail -5 || npx vitest run eslint-rules/__tests__/no-paywall-on-safety-category.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <done>Rule file + test file exist; rule appears in eslint --print-config output; RuleTester proves all 6 cases (3 invalid, 3 valid); rule registered as error severity in eslint.config.js.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: phaCheck runtime helper + CI grep gate + BlockNode extension + package.json wiring</name>
  <files>leanshot/src/lib/pharma/phaCheck.ts, leanshot/src/lib/pharma/__tests__/phaCheck.test.ts, leanshot/scripts/check-no-paywall-on-safety-category.sh, leanshot/src/lib/page-builder/block-schema.ts, leanshot/src/lib/page-builder/__tests__/block-schema.test.ts, leanshot/package.json</files>
  <behavior>
    - RED phaCheck: test that phaCheck({safety_category: 'overdose-warning'}) throws in test env, and phaCheck({safety_category: null}) does NOT throw, and phaCheck({}) does NOT throw. Run vitest — RED.
    - GREEN phaCheck: implement runtime helper per `<interfaces>`. Throw when MODE='test' or import.meta.env.DEV truthy; console.warn otherwise.
    - RED BlockNode: extend block-schema.test.ts (existing file) with a type-level assertion that BlockNode supports `{ ..., variant_set_id: 'abc' }` (e.g. via satisfies BlockNode). Run vitest — RED.
    - GREEN BlockNode: add `variant_set_id?: string` optional field to BlockNode interface in block-schema.ts. Update the JSON schema validator (if present in that file) to allow the new optional field without breaking existing rows. Run vitest — GREEN.
    - REFACTOR: ensure variant_set_id is also accepted in any factory function or validator in block-schema.ts; do NOT add it as required anywhere.
  </behavior>
  <action>
Pre-read block-schema.ts in full to find the BlockNode interface line + any factory/validator that needs the new optional field reflected (the file at leanshot/src/lib/page-builder/block-schema.ts already declares BlockType union + BlockNode interface; the field is APPENDED to the interface per PATTERNS Section G).

Author phaCheck.ts per `<interfaces>`. Vitest unit tests cover: throws on non-null safety_category; no-throw on null or absent; one parameterized test across all 5 D-05 safety categories (`overdose-warning`, `contraindication-alert`, `fda-black-box`, `serious-adverse-event-signal`, `pregnancy-lactation-contraindication`).

Author check-no-paywall-on-safety-category.sh per `<interfaces>`. Make executable (`chmod +x`). Wire `lint:safety` npm script into leanshot/package.json scripts block — invocation: `bash scripts/check-no-paywall-on-safety-category.sh`. Use Read on package.json first to find a clean insertion point near existing lint scripts.

Decision references: D-05 (5 safety categories), D-06 (three-layer enforcement), D-13 (per-block A/B contract via variant_set_id).

Per Pattern M (verify file count): after Edit on all 6 files, run `git status --short` to confirm all 6 expected file paths are dirty (no silent skips).
  </action>
  <verify>
    <automated>cd leanshot && npx vitest run src/lib/pharma/__tests__/phaCheck.test.ts src/lib/page-builder/__tests__/block-schema.test.ts 2>&1 | tail -10 && test -x scripts/check-no-paywall-on-safety-category.sh && grep -q 'lint:safety' package.json && grep -q 'variant_set_id' src/lib/page-builder/block-schema.ts</automated>
  </verify>
  <done>phaCheck.test.ts + block-schema.test.ts both pass. CI grep script executable + wired into package.json. BlockNode interface includes optional variant_set_id field. All 5 D-05 safety categories proven via parameterized test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer source code -> build pipeline | ESLint AST rule (layer 1) catches Paywall over safety_category at build time; CI fails the PR |
| Build output -> runtime browser | phaCheck() runtime assertion (layer 2) is defense-in-depth: throws in dev/test, warn-logs in prod (never crashes user) |
| Source repo -> CI gates | Bash grep gate (layer 3) backstops both above; runs in CI per package.json `lint:safety` script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02-01 | Compliance / Repudiation | Paywall wraps FDA-black-box content (regulator-visible breach) | mitigate | 3-layer phaCheck per D-06 (ESLint AST + runtime + CI grep); D-05 enumerates 5 safety categories; each layer fails independently |
| T-39-02-02 | Tampering | Developer disables ESLint rule via eslint-disable-next-line comment | mitigate (partial) | CI grep gate (layer 3) is comment-stripped per [[reference_grep_gate_comment_strip]] BEFORE grep so eslint-disable comments cannot hide the violation |
| T-39-02-03 | Tampering | New paywall component added in future phase bypasses PAYWALL_COMPONENTS set | accept (residual) | Documented in rule meta.docs.description; future plans MUST extend PAYWALL_COMPONENTS when adding any new gate component; cross-checked at Wave 3 plan checker review |
| T-39-02-04 | Information disclosure | BlockNode.variant_set_id reveals admin-only variant existence to public render | mitigate | variant_set_id is OPTIONAL and only resolves to an actual variant at page-render time via Wave 2 resolver Edge Fn; public render emits the resolved variant blocks only, not the field itself |
</threat_model>

<verification>
- `npx eslint --print-config <any tsx>` includes `no-paywall-on-safety-category: error`
- RuleTester proves all 6 test cases for the AST rule
- Vitest passes for phaCheck.test.ts (5 D-05 categories) + block-schema.test.ts (variant_set_id type assertion)
- `bash scripts/check-no-paywall-on-safety-category.sh` exits 0 on current main (no real violations) and exits 1 against a deliberate-violation fixture
- `npm run lint:safety` is a valid npm command
- `variant_set_id` appears in block-schema.ts BlockNode interface
</verification>

<success_criteria>
- 6 net-new files + 3 edits ship under this plan
- All 3 phaCheck layers operational
- BlockNode interface extended with optional variant_set_id field (PAGEAB-06)
- Lint pipeline gated on the new rule
- npm script wired so CI runs the grep gate
</success_criteria>

<output>
Create `.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-02-SUMMARY.md` per template — list 9 files modified, note RuleTester case count (6), Vitest case count, package.json script name.
</output>
