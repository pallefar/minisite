---
phase: 25-hipaa-audit-hardening-vendor-baa-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20270702000001_vendor_baa_chain.sql
  - supabase/migrations/20270702000002_subprocessor_snapshots.sql
  - supabase/migrations/20270702000003_admin_compliance_module_seed.sql
  - supabase/functions/tests/integration/vendor-baa-chain.test.ts
  - .planning/phases/25-hipaa-audit-hardening-vendor-baa-chain/25-01-SUMMARY.md
autonomous: false
requirements: [HIPAA-01, HIPAA-02, HIPAA-03, HIPAA-04, HIPAA-05, HIPAA-06, HIPAA-12, HIPAA-13]
user_setup:
  - service: supabase
    why: "Vault key `service_role_key` is the bearer token used by pg_cron in Plan 25-08; this plan installs the vendor_baa_chain table that the cron will read. Verify Vault key exists before pushing migration so Plan 25-08 cron does not silently emit `Bearer ` literal."
    env_vars: []
    dashboard_config:
      - task: "Verify vault.decrypted_secrets row `service_role_key` exists; if missing, add via Supabase Dashboard → Project Settings → Vault (carry-over from Phase 22 BL-7)."
        location: "Supabase Dashboard → Project Settings → Vault"
must_haves:
  truths:
    - "Table public.vendor_baa_chain exists with columns: vendor_name (text PK), baa_signed_at (timestamptz null), baa_expiry_at (timestamptz null), monthly_cost_usd (numeric), scope_summary (text), subprocessor_list (jsonb default '[]'), subprocessor_last_diff_at (timestamptz null), contact_email (text), status (enum pending/signed/expired)"
    - "vendor_baa_chain RLS: select gated to public.is_admin_at_least('staff'::public.admin_role); insert/update gated to superadmin via Phase 24 admin_role; NO delete policy; service_role REVOKE update,delete (Pattern S2 per RESEARCH Pitfall 6)"
    - "6 vendor rows seeded (Supabase, Vercel, Sentry, Anthropic, AWS SES, PostHog) with status='pending', baa_signed_at=null, monthly_cost_usd populated per D-01"
    - "Table public.subprocessor_snapshots exists (vendor_name, captured_at, payload jsonb, content_hash text) with append-only RLS + service_role REVOKE update,delete"
    - "admin_compliance_module manifest row inserted (per Phase 24 D-01 ADMIN_MODULES manifest extension) keyed `compliance`, minRole=superadmin, label='Compliance', route='/admin/compliance'"
    - "RLS integration test proves: anonymous SELECT denied; non-staff authenticated SELECT denied; staff SELECT allowed; non-superadmin staff UPDATE denied; service_role UPDATE on status column denied"
    - "Status enum transition owners documented in migration header (per [[feedback_status_machine_transition_owner]]): seed writes pending; founder UI in Plan 25-08 writes signed; baa-expiry-check cron in Plan 25-08 writes expired"
  artifacts:
    - path: "supabase/migrations/20270702000001_vendor_baa_chain.sql"
      provides: "vendor_baa_chain table + RLS + 6 seed rows + status enum + writer-ownership header"
      contains: "create table public.vendor_baa_chain"
    - path: "supabase/migrations/20270702000002_subprocessor_snapshots.sql"
      provides: "subprocessor_snapshots append-only table + RLS"
      contains: "create table public.subprocessor_snapshots"
    - path: "supabase/migrations/20270702000003_admin_compliance_module_seed.sql"
      provides: "Admin manifest seed (Compliance module entry for Phase 24 modular shell)"
    - path: "supabase/functions/tests/integration/vendor-baa-chain.test.ts"
      provides: "Cross-tenant impersonation proof for vendor_baa_chain RLS"
  key_links:
    - from: "supabase/migrations/20270702000001_vendor_baa_chain.sql"
      to: "Phase 24 profiles.admin_role enum"
      via: "RLS policy `vendor_baa_chain_modify_superadmin` predicate `exists (select 1 from public.profiles where id = auth.uid() and admin_role = 'superadmin')`"
      pattern: "admin_role.*superadmin"
    - from: "supabase/migrations/20270702000001_vendor_baa_chain.sql"
      to: "Plan 25-08 baa-expiry-check Edge Fn"
      via: "Cron reads baa_expiry_at and writes status='expired' via SECURITY DEFINER RPC (Plan 25-08)"
      pattern: "baa_expiry_at"
---

<objective>
Ship the `vendor_baa_chain` + `subprocessor_snapshots` tables, the admin-shell `Compliance` module manifest row, and the RLS integration test that locks the security boundaries. These are the load-bearing schema artifacts every other Phase 25 plan depends on for vendor BAA state tracking, subprocessor diffing, and the admin Compliance UI (Plan 25-08).

Purpose: Closes HIPAA-12 storage half + HIPAA-13 expiry-tracking half + provides the rows that Plans 25-03/25-04 health checks read for vendor-gated send patterns. Without these tables, no other Phase 25 plan can verify vendor BAA status at runtime.

Output: 3 SQL migrations + 1 RLS integration test + this plan's SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-hipaa-audit-hardening-vendor-baa-chain/25-CONTEXT.md
@.planning/phases/25-hipaa-audit-hardening-vendor-baa-chain/25-RESEARCH.md
@.planning/phases/25-hipaa-audit-hardening-vendor-baa-chain/25-PATTERNS.md
@.planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-01-PLAN.md
@.planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-03-PLAN.md

<interfaces>
<!-- Phase 24 carry-forward shapes the executor needs. Read PHASE 24 24-01-PLAN.md for full audit_logs DDL. -->

From Phase 24 Plan 24-01 (admin_role enum + audit_logs):
- `public.profiles.admin_role` enum with values `'staff' | 'admin' | 'superadmin'` (NULL = non-staff).
- `public.is_admin_at_least(min_role public.admin_role)` SECURITY DEFINER returns `boolean`; pass `'staff'::public.admin_role` for any-staff-tier gating. Defined in Phase 24 Plan 24-01.
- `public.audit_logs` exists with append-only RLS; service_role REVOKE update,delete is the law.

From Phase 24 Plan 24-03 (modular admin manifest):
- `src/lib/admin/modules.ts` exports `ADMIN_MODULES: AdminModule[]` typed `{ key, label, route, icon, lazy, flagKey, minRole }`.
- Manifest is CODE-driven (TypeScript module), NOT a DB seed. **Therefore `20270702000003_admin_compliance_module_seed.sql` is a NO-OP placeholder** documenting that the Compliance module entry will be added to `src/lib/admin/modules.ts` by Plan 25-08; the migration file exists so a future feature-flag-table approach has a stub to grow into. (Keep the file with only a comment header — DO NOT INSERT anything into a non-existent table.)
- This avoids the [[feedback_planner_iter1_anti_patterns]] "VALIDATION flag flip-timing" pitfall: code-vs-DB ownership of the module list is settled in Phase 24, Plan 25-01 must not invent a new DB-driven flag layer.

Phase 22 lifecycle_cron_schedules.sql (analog for Vault bearer in Plan 25-08):
```sql
select decrypted_secret from vault.decrypted_secrets where name = 'service_role_key' limit 1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write vendor_baa_chain migration + 6 seed rows + RLS</name>
  <files>supabase/migrations/20270702000001_vendor_baa_chain.sql</files>
  <action>
Create the 14-digit-prefixed migration (per [[reference_supabase_migration_filename_regex]] — `^\d{14}_[a-z0-9_]+\.sql$`). The file MUST start with a docstring header citing Phase 25 D-01, D-12 and stating writer-ownership for the `status` column (per [[feedback_status_machine_transition_owner]]): the seed in this migration writes `pending`; the founder UI in Plan 25-08 writes `signed`; the baa-expiry-check Edge Fn in Plan 25-08 writes `expired`.

DDL requirements (per Pattern S2 + RESEARCH Pitfall 6):
- `create type public.vendor_baa_status as enum ('pending', 'signed', 'expired')`.
- `create table public.vendor_baa_chain` with columns: `vendor_name text primary key`, `baa_signed_at timestamptz`, `baa_expiry_at timestamptz`, `monthly_cost_usd numeric(10,2) not null default 0`, `scope_summary text`, `subprocessor_list jsonb not null default '[]'::jsonb`, `subprocessor_last_diff_at timestamptz`, `contact_email text`, `status public.vendor_baa_status not null default 'pending'`, `created_at timestamptz not null default now()`, `updated_at timestamptz not null default now()`.
- `alter table ... enable row level security`.
- Three RLS policies: (a) `vendor_baa_chain_select_staff` for select using `public.is_admin_at_least('staff'::public.admin_role)`. (b) `vendor_baa_chain_insert_superadmin` for insert with check `exists (select 1 from public.profiles where id = auth.uid() and admin_role = 'superadmin')`. (c) `vendor_baa_chain_update_superadmin` for update using same predicate. NO delete policy.
- Per RESEARCH Pitfall 6 (RLS does NOT block service_role): `revoke update, delete on public.vendor_baa_chain from service_role;` — keep INSERT for service_role (cron may need it eventually) but compliance UI writes go through a SECURITY DEFINER RPC, not direct service_role. Add a negative-space comment block matching audit_logs.sql lines 118-138 explaining the absence of DELETE.
- `updated_at` trigger: install a BEFORE UPDATE trigger setting `new.updated_at = now()` (use existing `public.set_updated_at()` if it exists in the codebase; otherwise define it once here in `extensions` schema using `set search_path = public, extensions` per Pattern S7).

Seed 6 rows (D-01 cost figures); use `insert into ... values ... on conflict (vendor_name) do nothing;` so re-runs are safe:
- ('Supabase', null, null, 924.00, 'Database + Auth + Storage + Edge Functions (HIPAA add-on on Team plan)', '[]'::jsonb, null, null, 'pending')
- ('Vercel', null, null, 350.00, 'Hosting + Edge Runtime + AI Gateway (HIPAA add-on on Pro plan, self-serve since 2025)', '[]'::jsonb, null, null, 'pending')
- ('Sentry', null, null, 80.00, 'Error monitoring + session replay (Business plan with BAA)', '[]'::jsonb, null, null, 'pending')
- ('Anthropic', null, null, 1500.00, 'Clinical-credential LLM (Enterprise + BAA + ZDR addendum; sales-assisted)', '[]'::jsonb, null, null, 'pending')
- ('AWS SES', null, null, 10.00, 'PHI email delivery (BAA via AWS Artifact, self-serve same-day)', '[]'::jsonb, null, null, 'pending')
- ('PostHog', null, null, 0.00, 'Product analytics + session replay (scrub-only tier per D-04; revisit Boost only if clinician-replay becomes active blocker)', '[]'::jsonb, null, null, 'pending')

STRIDE register (write in migration header):
- T-25-01-01 Tampering: NO INSERT/UPDATE/DELETE policy for authenticated role; only is_admin_at_least('staff') select + superadmin insert/update; service_role REVOKE update,delete prevents Edge-Fn bypass.
- T-25-01-02 Information disclosure: select policy `public.is_admin_at_least('staff'::public.admin_role)` — only staff/admin/superadmin can read vendor names + BAA dates; patient/clinician roles cannot.
- T-25-01-03 Repudiation: append-only-by-policy for delete; updated_at trigger gives change history; consider INSERT trigger writing to audit_logs in Plan 25-08 wire-up.

DO NOT create the `vendor_baa_chain_update` SECURITY DEFINER RPC here — that lives in Plan 25-08 (compliance UI). Per RESEARCH Anti-Patterns line 637, the planner must avoid the "shared-file choreography" anti-pattern by keeping RPC creation in the plan that wires the UI consumer.
  </action>
  <verify>
    <automated>
test -f supabase/migrations/20270702000001_vendor_baa_chain.sql &amp;&amp; \
grep -q "create table public.vendor_baa_chain" supabase/migrations/20270702000001_vendor_baa_chain.sql &amp;&amp; \
grep -q "revoke update, delete on public.vendor_baa_chain from service_role" supabase/migrations/20270702000001_vendor_baa_chain.sql &amp;&amp; \
grep -v '^--' supabase/migrations/20270702000001_vendor_baa_chain.sql | grep -c "on conflict (vendor_name) do nothing" | xargs -I{} test {} -ge 1 &amp;&amp; \
test "$(grep -v '^--' supabase/migrations/20270702000001_vendor_baa_chain.sql | grep -c "'Supabase'\|'Vercel'\|'Sentry'\|'Anthropic'\|'AWS SES'\|'PostHog'")" -ge 6
    </automated>
  </verify>
  <done>Migration file exists with the 6 seed rows, append-only RLS, service_role REVOKE, and a writer-ownership header naming Plans 25-01/25-08 for each status transition.</done>
</task>

<task type="auto">
  <name>Task 2: Write subprocessor_snapshots migration + Compliance manifest no-op</name>
  <files>supabase/migrations/20270702000002_subprocessor_snapshots.sql, supabase/migrations/20270702000003_admin_compliance_module_seed.sql</files>
  <action>
**Migration 002 (subprocessor_snapshots, append-only):** Header docstring cites Phase 25 D-12 + RESEARCH Pattern 2 (subprocessor-diff cron). DDL:
- `create table public.subprocessor_snapshots (id bigserial primary key, vendor_name text not null references public.vendor_baa_chain(vendor_name) on delete restrict, captured_at timestamptz not null default now(), source_url text not null, content_hash text not null, payload jsonb not null)`.
- `create unique index subprocessor_snapshots_vendor_hash_idx on public.subprocessor_snapshots (vendor_name, content_hash)` (idempotency: same content_hash for a vendor = no new snapshot row).
- `create index subprocessor_snapshots_vendor_captured_idx on public.subprocessor_snapshots (vendor_name, captured_at desc)`.
- `alter table ... enable row level security`.
- Select policy: `subprocessor_snapshots_select_staff` using `public.is_admin_at_least('staff'::public.admin_role)` (admin Compliance UI reads).
- NO insert/update/delete policy for authenticated role; service_role inserts (cron is service-role-only).
- `revoke update, delete on public.subprocessor_snapshots from service_role;` (RESEARCH Pitfall 6).
- Negative-space tamper comment block.

STRIDE: T-25-01-04 Tampering (append-only); T-25-01-05 Information disclosure (staff-only select).

**Migration 003 (admin_compliance_module_seed — NO-OP placeholder):** Per Phase 24 Plan 24-03 the admin module manifest is CODE-driven (`src/lib/admin/modules.ts`), NOT a DB seed. This migration file MUST be a header-only placeholder with this exact content (no DDL, no DML — the file exists only to keep the timestamp ordering documented):

```sql
-- Phase 25 Plan 25-01 — admin Compliance module seed placeholder.
--
-- INTENTIONALLY NO-OP. The admin module manifest is the TypeScript module
-- `src/lib/admin/modules.ts` per Phase 24 Plan 24-03 D-01. Adding a
-- `Compliance` entry happens in Plan 25-08 by editing modules.ts directly.
--
-- This migration file exists so a future feature-flag-table refactor has a
-- ready timestamp slot to grow into; today it ships zero schema changes.
--
-- Verify: `select 1` is a deliberate no-op statement that supabase db push
-- accepts without modifying any schema object.
select 1 where false;
```

This avoids the [[feedback_planner_iter1_anti_patterns]] "defensive jsonb contracts" pitfall (do not invent a `admin_modules` table that doesn't exist). It also avoids splitting Phase 24 D-01 ownership by NOT touching `modules.ts` here — that file is owned by Plan 25-08.
  </action>
  <verify>
    <automated>
test -f supabase/migrations/20270702000002_subprocessor_snapshots.sql &amp;&amp; \
test -f supabase/migrations/20270702000003_admin_compliance_module_seed.sql &amp;&amp; \
grep -q "create table public.subprocessor_snapshots" supabase/migrations/20270702000002_subprocessor_snapshots.sql &amp;&amp; \
grep -q "revoke update, delete on public.subprocessor_snapshots from service_role" supabase/migrations/20270702000002_subprocessor_snapshots.sql &amp;&amp; \
grep -q "INTENTIONALLY NO-OP" supabase/migrations/20270702000003_admin_compliance_module_seed.sql &amp;&amp; \
! grep -q "^create table\|^insert into" supabase/migrations/20270702000003_admin_compliance_module_seed.sql
    </automated>
  </verify>
  <done>Two migration files exist; subprocessor_snapshots has append-only RLS + service_role REVOKE; module-seed migration is documented no-op with `select 1 where false`.</done>
</task>

<task type="auto">
  <name>Task 3: Write RLS integration test (cross-tenant impersonation proof)</name>
  <files>supabase/functions/tests/integration/vendor-baa-chain.test.ts</files>
  <action>
Create a vitest integration test under `supabase/functions/tests/integration/` (matches existing project convention; deno tests for Edge Fns live under each fn directory).

Use the RLS fixture pattern from [[reference_rls_fixture_gotruechient_flake]]: `admin.generateLink` + `/auth/v1/verify` via plain fetch (NOT `signInWithPassword`). Apply per-file slug prefix per [[feedback_rls_per_file_slug_prefix]]: `const TEST_SLUG_PREFIX = 'vbc-test-'` (file-scoped const, NOT shared).

Test cases (all run against linked Supabase project via `supabase db query --linked`-equivalent fixture):
1. **Anonymous SELECT denied** — supabase client with anon key gets `data: null, error.code = 'PGRST116'` (or empty array depending on PostgREST default-deny shape; assert no rows returned + no row leaked).
2. **Non-staff authenticated SELECT denied** — create test user with `profiles.admin_role = null`, sign in, SELECT vendor_baa_chain → expect zero rows.
3. **Staff SELECT allowed** — set test user's `profiles.admin_role = 'staff'`, SELECT → expect 6 seed rows (Supabase, Vercel, Sentry, Anthropic, AWS SES, PostHog).
4. **Non-superadmin staff UPDATE denied** — `admin_role = 'admin'`, attempt `update vendor_baa_chain set status='signed' where vendor_name='Sentry'` → expect error code `42501` (insufficient_privilege) per RLS.
5. **Superadmin UPDATE allowed** — set `admin_role = 'superadmin'`, same update → expect row affected = 1.
6. **service_role UPDATE on status DENIED** — using service-role client, attempt `update vendor_baa_chain set status='expired' where vendor_name='AWS SES'` → expect error per the explicit `revoke update from service_role`. This is the load-bearing case proving the RESEARCH Pitfall 6 mitigation.
7. **DELETE denied for all roles** — anon, staff, superadmin, service_role all attempt delete → all rejected.

Cleanup in `afterAll`: revert status changes back to 'pending', remove created test profiles. Use the file-scoped slug prefix on any temp users.

Per [[reference_vitest_skip_fixme]]: use `it.skip(...)` NOT `it.fixme(...)` if any case must be deferred (e.g., test 6 requires service_role key — may need to skip in PR CI and run only in linked-DB integration tier).
  </action>
  <verify>
    <automated>
test -f supabase/functions/tests/integration/vendor-baa-chain.test.ts &amp;&amp; \
grep -q "TEST_SLUG_PREFIX = 'vbc-test-'" supabase/functions/tests/integration/vendor-baa-chain.test.ts &amp;&amp; \
grep -c "describe\|it(" supabase/functions/tests/integration/vendor-baa-chain.test.ts | xargs -I{} test {} -ge 7 &amp;&amp; \
grep -q "service_role UPDATE on status DENIED\|service_role.*denied\|revoke.*service_role" supabase/functions/tests/integration/vendor-baa-chain.test.ts
    </automated>
  </verify>
  <done>Test file exists with 7 cases covering anon/non-staff/staff/non-superadmin/superadmin/service_role write-deny/all-roles-delete-deny; cleanup restores seed status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Supabase db push (schema gate) + Vault key verify</name>
  <what-built>3 migrations (vendor_baa_chain + subprocessor_snapshots + module-seed no-op) ready to push to linked Supabase project `ytnsipxxmzgaebkqmokp`.</what-built>
  <how-to-verify>
1. Verify Vault key exists (Plan 25-08 cron will fail silently without it):
   ```
   supabase db query --linked "select name from vault.decrypted_secrets where name = 'service_role_key' limit 1;"
   ```
   Expected: 1 row. If 0 rows, STOP and add the key via Supabase Dashboard → Project Settings → Vault → Add new secret → name `service_role_key`, value = the project's `service_role` key from Settings → API → Service Role secret. Per [[project_phase16_unblock_plan]] this is the Wave-0 manual step.

2. If running in a git worktree, copy `supabase/.temp/*` from main checkout per [[reference_supabase_worktree_temp_state]].

3. Push the migrations:
   ```
   supabase db push --linked
   ```
   Grep stderr for `^Skipping` per [[reference_supabase_migration_filename_regex]]:
   ```
   supabase db push --linked 2>&amp;1 | grep '^Skipping' &amp;&amp; echo "FAIL: migration silently skipped" || echo "OK"
   ```

4. Verify tables exist + seed rows present:
   ```
   supabase db query --linked "select vendor_name, status, monthly_cost_usd from public.vendor_baa_chain order by vendor_name;"
   ```
   Expected: 6 rows with status='pending' and monthly_cost_usd populated.

5. Run the RLS integration test against the linked project:
   ```
   npm run test -- --run supabase/functions/tests/integration/vendor-baa-chain.test.ts
   ```
   Expected: all 7 cases green.
  </how-to-verify>
  <resume-signal>Reply "pushed" with the output of step 4, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated→DB | Browser/Edge Fn client crosses RLS into vendor_baa_chain reads/writes |
| service_role→DB | Edge Fns (Plan 25-08 cron) cross with elevated privilege; explicit REVOKE blocks status tampering |
| superadmin→DB | Founder UI (Plan 25-08) updates status='signed'; gated by admin_role enum from Phase 24 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Tampering | vendor_baa_chain.status column | mitigate | RLS update policy restricted to superadmin; `revoke update from service_role` (RESEARCH Pitfall 6); status writer-ownership documented in migration header |
| T-25-01-02 | Information disclosure | vendor_baa_chain SELECT | mitigate | `public.is_admin_at_least('staff'::public.admin_role)` predicate on select policy; patient/clinician roles cannot read vendor list or BAA dates |
| T-25-01-03 | Repudiation | vendor_baa_chain row changes | mitigate | `updated_at` trigger + status change rows wired to audit_logs in Plan 25-08; no DELETE policy preserves history |
| T-25-01-04 | Tampering | subprocessor_snapshots integrity | mitigate | Append-only via absence of INSERT/UPDATE/DELETE policies for authenticated; `revoke update,delete from service_role`; unique index on (vendor_name, content_hash) prevents duplicate-content writes |
| T-25-01-05 | Information disclosure | subprocessor_snapshots SELECT | mitigate | Staff-only via `public.is_admin_at_least('staff'::public.admin_role)`; patient/clinician role isolation |
| T-25-01-06 | Elevation of privilege | seed inserting unauthorized vendor rows | accept | Seed runs during migration push as DDL owner; the 6 vendors are a fixed list per D-01; future vendors added via superadmin UI in Plan 25-08, NOT migrations |
</threat_model>

<verification>
- `supabase db push --linked` succeeds with no `^Skipping` lines.
- `select count(*) from public.vendor_baa_chain` returns 6.
- RLS integration test green (7/7 cases).
- Vault `service_role_key` present (verified pre-push).
</verification>

<success_criteria>
- vendor_baa_chain + subprocessor_snapshots tables live on linked Supabase project `ytnsipxxmzgaebkqmokp` with seed rows + append-only RLS + service_role REVOKE.
- Phase 25 Plans 25-03, 25-04, 25-08 can read vendor BAA state to drive vendor-gated send (Pattern S4) + admin Compliance UI.
- HIPAA-12 storage half DONE; HIPAA-13 storage half DONE (cron + alert wiring lands in Plan 25-08).
</success_criteria>

<output>
After completion, create `.planning/phases/25-hipaa-audit-hardening-vendor-baa-chain/25-01-SUMMARY.md` capturing: migration timestamps used, RLS test pass output, Vault key verification result, any deviations from Pattern S2 (none expected).
</output>
