---
phase: 42-v1-3-polish-closeout
plan: "05"
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - supabase/migrations/20270704000001_notification_settings.sql
  - supabase/migrations/20270704000002_notification_category_config.sql
  - supabase/migrations/20270704000003_user_notifications.sql
  - supabase/migrations/20270704000004_notification_dismissal_state.sql
  - supabase/migrations/20270704000005_push_subscriptions.sql
  - supabase/migrations/20270704000006_notification_rls.sql
  - supabase/migrations/20270704000007_notification_category_config_seed.sql
  - supabase/functions/_shared/notification-types.ts
  - supabase/functions/_shared/notification-fire-decision.ts
  - supabase/functions/notification-send/index.ts
  - supabase/functions/notification-send/index.test.ts
  - supabase/functions/notification-dismiss/index.ts
  - leanshot/tests/integration/notification-dismissal-fresh-user.test.ts
  - supabase/functions/notification-snooze/index.ts
  - supabase/functions/push-subscribe/index.ts
  - leanshot/tests/rls/notification-settings-rls.test.ts
  - leanshot/tests/integration/notification-frequency-cap.test.ts
  - leanshot/tests/integration/notification-sentiment-throttle.test.ts
  - leanshot/tests/integration/notification-snooze.test.ts
  - leanshot/tests/integration/notification-urgent.test.ts
  - leanshot/tests/integration/notification-user-cap-downward.test.ts
autonomous: false
requirements:
  - POLISH-05
  - POLISH-06

must_haves:
  truths:
    - "Six new tables exist in live DB with RLS deny-by-default: notification_settings, notification_category_config, user_notifications, notification_dismissal_state, push_subscriptions"
    - "Cross-tenant impersonation test proves user A cannot read user B's notification_settings, user_notifications, push_subscriptions, or notification_dismissal_state"
    - "Frequency caps per D-04 are enforced server-side (3/day for ai-insights; 1/week for billing+marketing; UNLIMITED for dose-reminders+clinic-alerts)"
    - "Dismissal-rate sentiment (D-05): ≥3 consecutive dismissals halves the effective cap for 7 days; transparent via banner data"
    - "Per-category snooze (D-06): 1d/7d/30d options write notification_settings.snoozed_until; fire decision honors snooze"
    - "URGENT escalation (D-03): clinic-alerts push includes urgency='high' header in web-push send"
    - "Email channel routes via supabase/functions/_shared/email-router.ts (P25 D-03): clinic-alerts → SES; others → Resend (D-08)"
    - "User can lower their own cap (DOWNWARD only) but cannot raise above admin default (D-04 user-tunable constraint)"
    - "Sensible per-category defaults seeded per D-02 (dose/ai/clinic/billing/marketing × email/push/in-app matrix)"
    - "VAPID push send uses the import recipe from 42-01-SPIKE-RESULT.md"
  artifacts:
    - path: "supabase/migrations/20270704000001_notification_settings.sql"
      provides: "Table with UNIQUE(user_id, category, channel); snoozed_until timestamptz nullable; user_cap_override int nullable"
    - path: "supabase/migrations/20270704000003_user_notifications.sql"
      provides: "Append-only fire history with category, channel, fired_at, dismissed_at, clicked_at columns + indexes for cap-window queries"
    - path: "supabase/migrations/20270704000005_push_subscriptions.sql"
      provides: "endpoint+p256dh+auth per user with UNIQUE(user_id, endpoint)"
    - path: "supabase/migrations/20270704000006_notification_rls.sql"
      provides: "RLS deny + per-user select/insert/update policies on all five tables; notification_category_config is admin-only write, authenticated read"
    - path: "supabase/functions/_shared/notification-fire-decision.ts"
      provides: "Pure-function shouldFire(req, prefs, cfg, dismissal, firedToday) returning {email, push, in_app, urgent}"
      exports: ["shouldFire", "computeEffectiveCap", "isSnoozeActive"]
    - path: "supabase/functions/notification-send/index.ts"
      provides: "POST handler that loads context + computes fire decision + fans out to email/push/in-app + writes user_notifications row"
  key_links:
    - from: "supabase/functions/notification-send/index.ts"
      to: "supabase/functions/_shared/email-router.ts (P25 D-03)"
      via: "import { sendEmail } from '../_shared/email-router.ts'"
      pattern: "_shared/email-router"
    - from: "supabase/functions/notification-send/index.ts"
      to: "web-push (per 42-01-SPIKE-RESULT.md)"
      via: "either npm:web-push or hand-rolled crypto.subtle path per spike outcome"
      pattern: "web-push|crypto\\.subtle.*ECDSA"
    - from: "user_notifications"
      to: "notification_dismissal_state"
      via: "Trigger on dismissed_at update increments consecutive_dismissals; resets on click; triggers throttle when >=3"
      pattern: "CREATE TRIGGER.*dismissal"

user_setup:
  - service: supabase
    why: "7 DB migrations must be applied to the linked project before notification Edge Fns are usable"
    dashboard_config:
      - task: "Approve and push migrations"
        location: "Human-checkpoint task at end of this plan — supabase db push --linked"
---

<objective>
Build the complete notification backend (POLISH-05 + POLISH-06 server tier): five new tables with RLS, the server-authoritative fire-decision function, the three Edge Functions (notification-send, notification-dismiss, notification-snooze), the push-subscribe endpoint, and a comprehensive RLS + integration test suite (cross-tenant proofs + cap enforcement + sentiment halving + snooze + URGENT). Consumes the VAPID signing recipe from plan 42-01.

Purpose: All firing decisions MUST be server-side (RESEARCH §Anti-Patterns: "Client-side fire decision — Always server-side; client can't be trusted with cap enforcement"). User-tunable caps clamp DOWNWARD only per D-04. PHI-bearing categories (clinic-alerts) route through SES via the existing email-router. Dismissal-rate sentiment (D-05) is computed in SQL on rolling 7d window without Claude calls.

Output: Complete server tier ready for the Wave 3 settings UI (42-08) to consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-01-SPIKE-RESULT.md
@/Users/karstenhaldan/minisite/supabase/functions/_shared/email-router.ts
@/Users/karstenhaldan/minisite/supabase/migrations/20270601300004_p30_matviews_and_cron.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations — five tables + RLS + admin-configurable seed</name>
  <files>supabase/migrations/20270704000001_notification_settings.sql, supabase/migrations/20270704000002_notification_category_config.sql, supabase/migrations/20270704000003_user_notifications.sql, supabase/migrations/20270704000004_notification_dismissal_state.sql, supabase/migrations/20270704000005_push_subscriptions.sql, supabase/migrations/20270704000006_notification_rls.sql, supabase/migrations/20270704000007_notification_category_config_seed.sql</files>
  <behavior>
    - Schema test 1: notification_settings UNIQUE(user_id, category, channel) where category ∈ {dose-reminders, ai-insights, clinic-alerts, billing, marketing} and channel ∈ {email, web-push, in-app}; default `enabled` boolean follows D-02 per-channel matrix (use a CHECK + seeding migration); snoozed_until timestamptz NULL; user_cap_override int NULL with `CHECK (user_cap_override IS NULL OR user_cap_override <= notification_category_config.daily_cap)` enforced at the trigger layer (cross-row constraint via INSERT/UPDATE trigger).
    - Schema test 2: notification_category_config UNIQUE(category); columns daily_cap int (NULL or sentinel value to encode "unlimited" — pick `NULL` consistently; document in column comment), weekly_cap int (NULL for unlimited), urgent_escalation boolean, push_enabled_default boolean, email_enabled_default boolean, in_app_enabled_default boolean. Per D-04, dose-reminders + clinic-alerts ship with daily_cap = NULL (unlimited); ai-insights daily_cap = 3; billing + marketing weekly_cap = 1.
    - Schema test 3: user_notifications append-only — columns id uuid pk, user_id uuid, category text, channel text, payload jsonb, fired_at timestamptz default now(), dismissed_at timestamptz NULL, clicked_at timestamptz NULL, snoozed_at timestamptz NULL. Index on (user_id, category, fired_at DESC).
    - Schema test 4: notification_dismissal_state UNIQUE(user_id, category) — consecutive_dismissals int default 0, throttle_until timestamptz NULL, last_event_at timestamptz.
    - Schema test 5: push_subscriptions UNIQUE(user_id, endpoint) — endpoint text, p256dh text, auth text, user_agent text NULL, created_at timestamptz default now().
    - Schema test 6: CHECK-constraint widening — any status enum used across these tables ships its CHECK / enum widening in the SAME migration that introduces the column (per memory feedback_planner_missed_status_enum_widening).
    - RLS test 1: User A SELECT/INSERT/UPDATE/DELETE on notification_settings WHERE user_id = userA.id succeeds; WHERE user_id = userB.id returns zero rows / fails.
    - RLS test 2: notification_category_config — authenticated SELECT succeeds; non-admin INSERT/UPDATE/DELETE denied; is_admin() RPC required for writes.
    - RLS test 3: user_notifications, notification_dismissal_state, push_subscriptions — same per-user RLS pattern as notification_settings.
    - RLS test 4: Cross-tenant impersonation proof — user A's JWT cannot ever see user B's rows in any of the five tables (PROJECT RULE per memory: every RLS surface gets a live cross-tenant proof).
    - Seed test: After running 20270704000007, SELECT * FROM notification_category_config returns 5 rows matching the D-04 matrix exactly.
  </behavior>
  <action>Write the 7 migrations with timestamps 20270704000001..20270704000007 (avoids Phase 33's 20270703* window per memory reference_migration_timestamp_collision_precheck). Per memory reference_supabase_migration_gotchas: SECURITY DEFINER functions must set `search_path = extensions, public`. Per memory reference_supabase_migration_filename_regex: strict 14-digit_name.sql pattern (no letter suffixes). All migrations idempotent via `CREATE TABLE IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS` / `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$`. The seed migration (20270704000007) writes 5 rows via `INSERT ... ON CONFLICT (category) DO UPDATE` so re-runs are safe. Per D-02 defaults matrix, embed default-enabled values DIRECTLY in the notification_category_config seed (3 per-channel default columns) — when a user first authenticates, a trigger or application code copies these defaults into notification_settings rows. Write the corresponding RLS tests in leanshot/tests/rls/notification-settings-rls.test.ts following the existing P22 RLS fixture pattern. CRITICAL: per memory reference_rls_fixture_gotruteclient_flake, use admin.generateLink + /auth/v1/verify via PLAIN fetch for test JWTs (do NOT use the obsolete mintTestJwt HS256 path). Use file-scoped TEST_SLUG_PREFIX per memory feedback_rls_per_file_slug_prefix. The cross-tenant proof test creates two users with file-scoped slugs and asserts user A's session sees zero of user B's rows. Per memory feedback_partial_phase_carry_over_md_pattern + project rule, RLS surface tests are non-negotiable. Per memory feedback_planner_iter1_anti_patterns: NO defensive jsonb contracts — payload jsonb is intentionally open; downstream Edge Fn validates shape.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && for f in supabase/migrations/20270704000001_*.sql supabase/migrations/20270704000002_*.sql supabase/migrations/20270704000003_*.sql supabase/migrations/20270704000004_*.sql supabase/migrations/20270704000005_*.sql supabase/migrations/20270704000006_*.sql supabase/migrations/20270704000007_*.sql; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done; cd leanshot && npx vitest run --config vitest-e2e.config.ts tests/rls/notification-settings-rls.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All 7 migration files exist with the strict timestamp_name.sql pattern; RLS test passes proving cross-tenant deny on all 5 tables; seed migration produces exactly 5 notification_category_config rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared types + fire-decision pure function + integration tests</name>
  <files>supabase/functions/_shared/notification-types.ts, supabase/functions/_shared/notification-fire-decision.ts, leanshot/tests/integration/notification-frequency-cap.test.ts, leanshot/tests/integration/notification-sentiment-throttle.test.ts, leanshot/tests/integration/notification-snooze.test.ts, leanshot/tests/integration/notification-urgent.test.ts, leanshot/tests/integration/notification-user-cap-downward.test.ts</files>
  <behavior>
    - shouldFire test 1 (frequency cap): With ai-insights daily_cap=3 and firedToday=2 → fire decision allows; firedToday=3 → blocked
    - shouldFire test 2 (sentiment halving): With dismissal.throttle_until in future and ai-insights daily_cap=4 → effective cap is 2; firedToday=2 blocks
    - shouldFire test 3 (snooze): With notification_settings.snoozed_until in future for category → all channels blocked regardless of cap
    - shouldFire test 4 (URGENT escalation): For clinic-alerts category, decision.urgent === true regardless of other state
    - shouldFire test 5 (UNLIMITED cap): For dose-reminders + clinic-alerts where daily_cap IS NULL, fires regardless of firedToday count
    - shouldFire test 6 (user cap downward): User has user_cap_override=1 on ai-insights (admin default 3); firedToday=1 → blocked (user clamp honored); cannot raise (test attempts user_cap_override=5 against admin default 3 → DB CHECK / trigger denies)
    - shouldFire test 7 (per-channel toggle): User disables email channel for billing → decision.email === false; push/in_app remain per their settings
  </behavior>
  <action>Create supabase/functions/_shared/notification-types.ts exporting TypeScript types: `Category = 'dose-reminders' | 'ai-insights' | 'clinic-alerts' | 'billing' | 'marketing'`, `Channel = 'email' | 'web-push' | 'in-app'`, `NotificationSetting`, `CategoryConfig`, `DismissalState`, `FireDecision = {email: boolean, push: boolean, in_app: boolean, urgent: boolean, reasons: Record<string, string>}` (reasons field gives debuggable explanation per channel). Create supabase/functions/_shared/notification-fire-decision.ts as a PURE function (no I/O): `shouldFire({ user_id, category, prefs, cfg, dismissal, firedTodayCount, firedThisWeekCount, now })` → FireDecision. Internal helpers: computeEffectiveCap (handles NULL=unlimited; halves when throttle_until > now); isSnoozeActive (checks any prefs.snoozed_until > now); perChannelDecision (combines enabled flag + cap not exceeded + snooze inactive). The `reasons` map records WHY each channel was/wasn't allowed ('cap_exceeded' | 'snoozed' | 'disabled' | 'throttled' | 'allowed') for downstream audit logging. Write the 5 integration test files in leanshot/tests/integration/. Each test wires the pure function against vitest mock data — no DB calls (those are RLS tests). Use the canonical fixture pattern from supabase/functions/_shared/test-fixtures.ts (or create it if absent — small object with default cfg/prefs/dismissal builders). The user-cap-downward test ALSO exercises the DB constraint by attempting an UPDATE that raises the cap above admin default and expecting failure — this part DOES hit DB, similar to the cross-tenant test.</action>
  <verify>
    <automated>cd leanshot && npx vitest run tests/integration/notification-frequency-cap.test.ts tests/integration/notification-sentiment-throttle.test.ts tests/integration/notification-snooze.test.ts tests/integration/notification-urgent.test.ts tests/integration/notification-user-cap-downward.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 integration test files pass; shouldFire is a pure function (no I/O imports — verify via `grep -E "supabase-js|fetch|createClient" supabase/functions/_shared/notification-fire-decision.ts` returning zero matches).</done>
</task>

<task type="auto">
  <name>Task 3: Four Edge Functions — notification-send, dismiss, snooze, push-subscribe</name>
  <files>supabase/functions/notification-send/index.ts, supabase/functions/notification-send/index.test.ts, supabase/functions/notification-dismiss/index.ts, supabase/functions/notification-snooze/index.ts, supabase/functions/push-subscribe/index.ts</files>
  <action>Implement supabase/functions/notification-send/index.ts per RESEARCH §Pattern 2. Accept POST {user_id, category, payload}. Service-role bearer check via existing _shared/lifecycle-utils.ts pattern (used in Phase 33 functions). Load notification_settings + notification_category_config + notification_dismissal_state + firedTodayCount + firedThisWeekCount via parallel `Promise.all` queries. Call shouldFire(). For each true-channel, fan out: email via `_shared/email-router.ts` `sendEmail({to, subject, body, phi: category === 'clinic-alerts'})` (D-08: PHI routes through SES; non-PHI Resend); push via the import recipe from 42-01-SPIKE-RESULT.md (npm:web-push OR crypto.subtle fallback) — pass `{ urgency: decision.urgent ? 'high' : 'normal' }` per D-03; in-app via INSERT INTO user_notifications (writes the realtime channel that the browser subscribes to in plan 42-08). After fan-out, INSERT INTO user_notifications a single source-of-truth row with fired_at + channels actually fired. Per D-13 HIPAA: notification body content is NON-PHI for v1.3 — subject + deeplink only for PHI-bearing categories (clinic-alerts payload may only contain {subject, deeplink}; no PHI fields). Enforce this with a zod schema validation gate at the Edge Fn entry for clinic-alerts category. Implement supabase/functions/notification-dismiss/index.ts: POST {notification_id}. RLS-scoped UPDATE user_notifications SET dismissed_at = now() WHERE id = $1 AND user_id = auth.uid(). After update, fire the dismissal-state UPSERT (NOT a bare UPDATE — a bare UPDATE silently no-ops if no row exists yet, so consecutive_dismissals would never reach 3 for a fresh user → D-05 sentiment-throttle would never trigger). Use: `INSERT INTO notification_dismissal_state (user_id, category, consecutive_dismissals, last_event_at, throttle_until) VALUES (auth.uid(), (SELECT category FROM user_notifications WHERE id = $1 AND user_id = auth.uid()), 1, now(), NULL) ON CONFLICT (user_id, category) DO UPDATE SET consecutive_dismissals = notification_dismissal_state.consecutive_dismissals + 1, last_event_at = now(), throttle_until = CASE WHEN notification_dismissal_state.consecutive_dismissals + 1 >= 3 THEN now() + interval '7 days' ELSE notification_dismissal_state.throttle_until END;`. Add a corresponding integration test `leanshot/tests/integration/notification-dismissal-fresh-user.test.ts` proving the fresh-user path: dismiss notification → SELECT consecutive_dismissals → expect 1 (not NULL); dismiss 2 more → expect 3 + throttle_until set. Implement supabase/functions/notification-snooze/index.ts: POST {category, duration_days IN (1,7,30)} → UPDATE notification_settings SET snoozed_until = now() + (duration_days * interval '1 day') WHERE user_id = auth.uid() AND category = $1. Implement supabase/functions/push-subscribe/index.ts: POST {endpoint, p256dh, auth, user_agent} → INSERT INTO push_subscriptions ON CONFLICT (user_id, endpoint) DO UPDATE SET p256dh = EXCLUDED.p256dh, auth = EXCLUDED.auth, user_agent = EXCLUDED.user_agent. All four Edge Fns: zod validation on inputs per RESEARCH §Security Domain V5. Per memory reference_supabase_functions_deploy_no_linked_flag: deploy WITHOUT --linked flag. Per memory reference_supabase_functions_deploy_import_map_flag: pass --import-map if any Fn imports from supabase/functions/import_map.json aliases. Per memory feedback_worktree_executor_pwd_drift_leaks_to_main: every commit verified via `git rev-parse --show-toplevel` matching the worktree root. Write supabase/functions/notification-send/index.test.ts using Deno test (per memory reference_deno_test_discovery: filename pattern `<name>.test.ts`). Test the full fire→fan-out path with mocked supabase client + mocked email-router + mocked web-push.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && for fn in notification-send notification-dismiss notification-snooze push-subscribe; do test -f "supabase/functions/$fn/index.ts" || { echo "MISSING $fn"; exit 1; }; done; cd supabase/functions/notification-send && deno test --allow-all index.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All four Edge Fns exist with zod input validation; notification-send Deno test passes; PHI gate on clinic-alerts rejects payloads with non-{subject,deeplink} fields.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN — Push migrations and deploy Edge Fns to live project</name>
  <what-built>7 migrations + 4 Edge Functions ready for the linked Supabase project.</what-built>
  <how-to-verify>
    1. From `/Users/karstenhaldan/minisite/` (NOT the worktree — per memory feedback_worktree_executor_pwd_drift_leaks_to_main, but also: supabase CLI is installed at the monorepo root):
       Pre-flight: `git rev-parse --show-toplevel` MUST print `/Users/karstenhaldan/minisite`.
    2. Push migrations: `supabase db push --linked`. Expect "Applied migrations: 20270704000001..20270704000007".
       If you see "Skipping" lines, abort — that means a filename regex violation (memory reference_supabase_migration_filename_regex). Fix and re-push.
    3. Verify live schema:
       `supabase db query --linked "SELECT count(*) FROM notification_category_config"` → expects 5.
       `supabase db query --linked "SELECT category, daily_cap, weekly_cap FROM notification_category_config ORDER BY category"` → expects matrix per D-04.
    4. Deploy Edge Fns:
       `supabase functions deploy notification-send notification-dismiss notification-snooze push-subscribe --project-ref ytnsipxxmzgaebkqmokp` (NO --linked flag per memory).
    5. Smoke-test notification-send with a service-role bearer:
       `curl -X POST https://ytnsipxxmzgaebkqmokp.functions.supabase.co/notification-send -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" -H "Content-Type: application/json" -d '{"user_id":"<your-user-uuid>","category":"ai-insights","payload":{"subject":"Test","body":"hello"}}'`
       → expects 200; verify a row appears in user_notifications via supabase db query.
  </how-to-verify>
  <resume-signal>Type "migrations-pushed-fns-deployed" once all 7 migrations are applied + 4 Fns deployed + smoke test returns 200</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → push-subscribe Edge Fn | User-scoped auth; subscription stored under user_id |
| Edge Fn → web-push endpoint | VAPID JWT signed server-side; private key in Function Secret |
| Edge Fn → email-router → SES/Resend | PHI routing decision based on category |
| Service role → notification-send | RLS-bypass for fire-decision queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05-01 | Information Disclosure | PHI in notification body cross-channel | mitigate | Zod gate: clinic-alerts payload schema restricts to {subject, deeplink}; HIPAA non-PHI body invariant from D-13 |
| T-42-05-02 | Spoofing | Forged push subscription overwrites another user's | mitigate | RLS on push_subscriptions; UNIQUE(user_id, endpoint); endpoint alone is high-entropy URL |
| T-42-05-03 | Tampering | Client raises own cap | mitigate | DB-level CHECK / trigger on notification_settings.user_cap_override against notification_category_config.daily_cap; explicit integration test |
| T-42-05-04 | Elevation of Privilege | User reads another user's notification_settings | mitigate | RLS per-user policies on all 5 tables; cross-tenant proof test (project rule) |
| T-42-05-05 | Information Disclosure | Cross-user data via SW cache | mitigate | Handled in plan 42-04 by allowlist (no notification API endpoints in PWA cache) |
| T-42-05-06 | Repudiation | User claims notification never sent | mitigate | user_notifications append-only with fired_at; payload retained for audit |
| T-42-05-SC | Tampering | npm install of web-push during Edge Fn deploy | mitigate | Already vetted in 42-01; LEGITIMATE per RESEARCH §Package Legitimacy |
</threat_model>

<verification>
- 7 migrations apply cleanly via `supabase db push --linked`.
- `npx vitest run --config vitest-e2e.config.ts tests/rls/notification-settings-rls.test.ts` proves cross-tenant deny.
- 5 integration tests pass.
- 4 Edge Fns deploy and respond to smoke-test invocations.
</verification>

<success_criteria>
- A fire request for ai-insights with firedToday=3 returns FireDecision { email: false, push: false, in_app: false, reasons: { ... 'cap_exceeded' } }.
- A fire request for clinic-alerts with the user fully opted-in returns FireDecision { urgent: true, push: true, email: true, in_app: true }.
- Three consecutive dismissals on a category trigger throttle_until = now() + 7d in notification_dismissal_state.
</success_criteria>

<output>
After completion, create `.planning/phases/42-v1-3-polish-closeout/42-05-SUMMARY.md`.
</output>
