---
phase: 42-v1-3-polish-closeout
plan: "09"
type: execute
wave: 3
depends_on: ["42-06"]
files_modified:
  - leanshot/src/lib/changelog/changelog-store.ts
  - leanshot/src/lib/changelog/drawer-trigger.ts
  - leanshot/src/components/changelog/WhatsNewDrawer.tsx
  - leanshot/src/components/changelog/WhatsNewDrawer.test.tsx
  - leanshot/src/components/layout/Topbar.tsx
  - leanshot/src/App.tsx
  - leanshot/e2e/whats-new-drawer.spec.ts
autonomous: true
requirements:
  - POLISH-11

must_haves:
  truths:
    - "Header avatar shows a subtle unread-dot indicator when any changelog_entries.published_at > user.last_seen_published_at"
    - "Clicking the avatar opens a Sheet drawer listing all changelog entries newer than last_seen, newest first"
    - "Markdown body is rendered via react-markdown + dompurify (already in stack per HELP-07)"
    - "Clicking 'Got it' calls /changelog-mark-read and the dot disappears"
    - "Drawer is lazy-loaded — does NOT bloat the index chunk per Pitfall 9"
    - "axe-core CI gate from 42-02 passes for /whats-new route with zero NEW violations"
  artifacts:
    - path: "leanshot/src/components/changelog/WhatsNewDrawer.tsx"
      provides: "Sheet primitive with markdown-rendered entry list + Got-it CTA"
      min_lines: 60
    - path: "leanshot/src/lib/changelog/changelog-store.ts"
      provides: "useChangelog() hook returning {entries, hasUnread, markRead}"
    - path: "leanshot/src/lib/changelog/drawer-trigger.ts"
      provides: "Computed flag: hasUnread = entries.some(e => e.published_at > lastSeen)"
  key_links:
    - from: "leanshot/src/components/layout/Topbar.tsx"
      to: "leanshot/src/components/changelog/WhatsNewDrawer.tsx"
      via: "React.lazy import + Suspense boundary; avatar Button onClick opens drawer"
      pattern: "React.lazy.*WhatsNewDrawer"
    - from: "leanshot/src/components/changelog/WhatsNewDrawer.tsx"
      to: "POST /changelog-mark-read"
      via: "markRead() called on Got-it click"
      pattern: "changelog-mark-read"
---

<objective>
Build the "What's New" drawer UI (POLISH-11): unread-dot indicator on the header avatar, lazy-loaded Sheet drawer that lists changelog entries newer than the user's last_seen_published_at, markdown body rendering via react-markdown + dompurify (HELP-07 chain), and the mark-read action wired to /changelog-mark-read Edge Fn from plan 42-06.

Purpose: Per CONTEXT D-11 (Claude's Discretion), planner picks the UI shape — Sheet primitive with the subtle-dot-on-avatar trigger pattern matches RESEARCH §Pattern 6.

Output: Working drawer; lazy-loaded; a11y-clean; unread state per-user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/src/components/layout/Topbar.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Sheet.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Changelog store + drawer trigger</name>
  <files>leanshot/src/lib/changelog/changelog-store.ts, leanshot/src/lib/changelog/drawer-trigger.ts</files>
  <behavior>
    - Test 1: useChangelog() fetches changelog_entries (SELECT * ORDER BY published_at DESC LIMIT 20) and user_changelog_dismissed (own row); returns {entries, lastSeenAt, hasUnread, markRead}.
    - Test 2: hasUnread === true iff entries.some(e => new Date(e.published_at) > new Date(lastSeenAt ?? '1970-01-01')).
    - Test 3: markRead() calls fetch('/functions/v1/changelog-mark-read', {method:'POST', headers: bearer}) and on success refetches user_changelog_dismissed.
    - Test 4: Anonymous (no auth) → hook returns {entries: [], hasUnread: false} gracefully (no error toast).
  </behavior>
  <action>Create leanshot/src/lib/changelog/changelog-store.ts using the same store pattern as 42-08's settings-store (Zustand slice OR React Query — match neighboring lib code). Expose useChangelog() with the 4-behavior shape. Use supabase-js for both reads. Cache the changelog_entries result for ~5 minutes (TanStack Query staleTime if RQ; manual TTL if Zustand). Create leanshot/src/lib/changelog/drawer-trigger.ts: a simple helper exporting `computeHasUnread(entries, lastSeenAt)` for unit testability + reuse from any nav surface. Write inline vitest unit tests covering the 4 behaviors above.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx vitest run src/lib/changelog/ 2>&1 | tail -10 && npx tsc -b --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 hook tests pass; tsc -b --noEmit succeeds; hook returns expected shape with safe anonymous fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WhatsNewDrawer component + Topbar avatar dot indicator + lazy mount</name>
  <files>leanshot/src/components/changelog/WhatsNewDrawer.tsx, leanshot/src/components/changelog/WhatsNewDrawer.test.tsx, leanshot/src/components/layout/Topbar.tsx, leanshot/src/App.tsx</files>
  <behavior>
    - UI test 1: WhatsNewDrawer renders one `<article>` per entry with the title + date + markdown body.
    - UI test 2: Markdown body is sanitized — a fixture entry with body_md containing `<script>alert(1)</script>` does NOT render a script tag in the DOM.
    - UI test 3: Clicking "Got it" calls markRead() and the drawer closes via Sheet's onOpenChange.
    - UI test 4 (Topbar): Avatar Button shows a span with class indicating unread state when hasUnread === true; absent when hasUnread === false.
    - UI test 5 (a11y): Avatar Button has aria-label="What's new — N unread updates" when hasUnread; "Profile menu" when not.
    - UI test 6 (lazy): grep `src/App.tsx` and `src/components/layout/Topbar.tsx` for `React.lazy.*WhatsNewDrawer` — the import path must use React.lazy so the drawer is NOT in the index chunk (Pitfall 9).
  </behavior>
  <action>Create leanshot/src/components/changelog/WhatsNewDrawer.tsx per RESEARCH §Pattern 6 example. Use the project's existing Sheet primitive (leanshot/src/components/ui/Sheet.tsx). Inside the Sheet, render `<SheetHeader title="What's new" />` then `<div>` of `<article>` rows. Each article has `<time dateTime={e.published_at}>` (formatted via existing date helper) + `<h3>{e.title}</h3>` + Markdown component. Use react-markdown + rehype-sanitize (or dompurify via custom processor) — per memory reference_claude_design_bundle_landmines AND per RESEARCH §Don't Hand-Roll, this chain is already in stack from HELP-07 (Phase 37). If Phase 37 has not shipped (check via `grep -r "react-markdown" leanshot/src/` and `grep "react-markdown" leanshot/package.json`), this plan installs them: `npm install react-markdown@^9 rehype-sanitize@^6 dompurify@^3`. Footer: `<Button onClick={() => { markRead(); onClose(); }}>Got it</Button>`. Modify leanshot/src/components/layout/Topbar.tsx: read existing avatar component; add the unread-dot indicator (a small `<span aria-hidden="true">` with appropriate CSS classes for the dot) when useChangelog().hasUnread === true. The avatar Button has aria-label dynamically computed. Update the click handler: previously opened a profile menu — now it routes to a new state that opens the WhatsNewDrawer. The drawer is imported via React.lazy + Suspense from the Topbar OR mounted globally in App.tsx with an open-state prop. Pick the cheaper bundle path — likely App.tsx-mounted to avoid Topbar including the drawer chunk. Write the comprehensive RTL test in WhatsNewDrawer.test.tsx covering the 6 behaviors above + an axe-core scan.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx vitest run src/components/changelog/WhatsNewDrawer.test.tsx 2>&1 | tail -10 && grep -q "React.lazy" src/components/layout/Topbar.tsx src/App.tsx && npm run build 2>&1 | grep -E "whats-new|changelog" | head -3 && npm run check-bundle-budget 2>&1 | tail -10</automated>
  </verify>
  <done>All 6 UI tests pass; XSS fixture proves dompurify works; lazy import confirmed via grep; bundle-budget passes (no index regression).</done>
</task>

<task type="auto">
  <name>Task 3: e2e Playwright test for unread → mark-read flow</name>
  <files>leanshot/e2e/whats-new-drawer.spec.ts</files>
  <action>Create leanshot/e2e/whats-new-drawer.spec.ts. Test scenario: (1) seed an authenticated user with no user_changelog_dismissed row → confirm avatar shows unread dot; (2) click avatar → drawer opens with 3 seeded entries from plan 42-06; (3) click "Got it" → drawer closes; (4) reload page → unread dot is gone (user_changelog_dismissed.last_seen_published_at now > all published_at). Use page.addInitScript() for auth seeding per memory reference_playwright_state_seeding. Use service-role direct DB writes to seed/cleanup the test changelog_entries row(s) if the 3 seeded entries from 42-06 are not sufficient.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx playwright test e2e/whats-new-drawer.spec.ts 2>&1 | tail -10</automated>
  </verify>
  <done>e2e spec passes the full unread → mark-read → no-dot cycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → changelog_entries.body_md | Markdown rendered to authenticated users |
| User → /changelog-mark-read | Auth-scoped POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-09-01 | Injection | Admin XSS via body_md (HTML in markdown) | mitigate | react-markdown + rehype-sanitize (or dompurify) blocks script tags / event handlers / data: URIs |
| T-42-09-02 | Denial of Service | Large entry list bloats Sheet render | mitigate | LIMIT 20 in the changelog_entries query; pagination if list grows (deferred) |
| T-42-09-03 | Information Disclosure | Drawer ships in index chunk (Pitfall 9) | mitigate | React.lazy import; bundle-budget check in CI confirms |
| T-42-09-SC | Tampering | Optional npm install react-markdown + rehype-sanitize | mitigate | Already in stack per HELP-07 if Phase 37 shipped — else LEGITIMATE per RESEARCH §Standard Stack |
</threat_model>

<verification>
- Component tests pass.
- e2e flow passes.
- axe-baseline.test.ts shows zero NEW violations on /whats-new route.
- bundle-budget passes after the new lazy chunk.
</verification>

<success_criteria>
- New users see unread dot on first login (3 v1.3-highlight entries from 42-06 seed).
- Click avatar → drawer opens → Got it → dot disappears → persists across reload.
</success_criteria>

<output>
After completion, create `.planning/phases/42-v1-3-polish-closeout/42-09-SUMMARY.md`.
</output>
