---
phase: 42-v1-3-polish-closeout
plan: "06"
type: execute
wave: 2
depends_on: []
files_modified:
  - supabase/migrations/20270704000010_changelog_entries.sql
  - supabase/migrations/20270704000011_user_changelog_dismissed.sql
  - supabase/migrations/20270704000012_changelog_rls.sql
  - supabase/migrations/20270704000013_changelog_seed.sql
  - supabase/functions/changelog-mark-read/index.ts
  - leanshot/tests/rls/changelog-rls.test.ts
autonomous: false
requirements:
  - POLISH-11

must_haves:
  truths:
    - "changelog_entries table is admin-write authenticated-read with slug UNIQUE + published_at indexed DESC"
    - "user_changelog_dismissed table tracks per-user last_seen_published_at; PK user_id"
    - "Cross-tenant test proves user A cannot read user B's user_changelog_dismissed row"
    - "Markdown body content is rendered with dompurify + react-markdown server-of-truth (admin-pasted HTML XSS-safe)"
    - "Seed migration creates 3 initial changelog entries covering v1.3 highlights"
  artifacts:
    - path: "supabase/migrations/20270704000010_changelog_entries.sql"
      provides: "Table {id uuid pk, slug text UNIQUE, title text, body_md text, published_at timestamptz, created_at, updated_at}"
    - path: "supabase/migrations/20270704000011_user_changelog_dismissed.sql"
      provides: "Table {user_id uuid pk REFERENCES auth.users, last_seen_published_at timestamptz}"
    - path: "supabase/migrations/20270704000012_changelog_rls.sql"
      provides: "RLS: changelog_entries select-authenticated insert/update-admin; user_changelog_dismissed select/upsert own-row"
    - path: "supabase/functions/changelog-mark-read/index.ts"
      provides: "POST handler: UPSERT user_changelog_dismissed SET last_seen_published_at = now()"

user_setup:
  - service: supabase
    why: "4 DB migrations + 1 Edge Function deploy"
    dashboard_config:
      - task: "Approve and push migrations"
        location: "supabase db push --linked at end of plan"
---

<objective>
Build the "What's New" drawer backend (POLISH-11): two tables (changelog_entries, user_changelog_dismissed), RLS, a mark-read Edge Function, and a seed of 3 v1.3-highlight entries. UI is in plan 42-09.

Purpose: Per CONTEXT D-11 (Claude's Discretion section, POLISH-11), entries are admin-curated NOT git-log auto-generated. Per-user dismissal state is the key UX detail: the dot indicator on the avatar reflects `EXISTS(SELECT 1 FROM changelog_entries WHERE published_at > coalesce(user.last_seen_published_at, '1970-01-01'))`.

Output: Server tier ready for the drawer UI (42-09) to consume; minimal scope because the heavy markdown/render lift is shared with HELP-07 dompurify chain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations — changelog_entries + user_changelog_dismissed + RLS + seed</name>
  <files>supabase/migrations/20270704000010_changelog_entries.sql, supabase/migrations/20270704000011_user_changelog_dismissed.sql, supabase/migrations/20270704000012_changelog_rls.sql, supabase/migrations/20270704000013_changelog_seed.sql, leanshot/tests/rls/changelog-rls.test.ts</files>
  <behavior>
    - Schema test 1: changelog_entries has columns id (uuid pk), slug (text UNIQUE NOT NULL), title (text NOT NULL), body_md (text NOT NULL), published_at (timestamptz NOT NULL DEFAULT now()), created_at, updated_at. Index on (published_at DESC).
    - Schema test 2: user_changelog_dismissed has user_id (uuid pk REFERENCES auth.users(id) ON DELETE CASCADE), last_seen_published_at (timestamptz NOT NULL DEFAULT now()), updated_at.
    - RLS test 1: changelog_entries — authenticated SELECT succeeds for any row; non-admin INSERT/UPDATE/DELETE denied; is_admin() required.
    - RLS test 2: user_changelog_dismissed — user A can SELECT/INSERT/UPDATE own row; cannot read user B's row.
    - Cross-tenant test: User A's JWT against `SELECT last_seen_published_at FROM user_changelog_dismissed WHERE user_id = '<userB-uuid>'` returns zero rows.
    - Seed test: After running 20270704000013, SELECT count(*) FROM changelog_entries returns >= 3 with at least one entry referencing v1.3 highlights (Smart Notifications / PWA Offline / Dark Mode).
  </behavior>
  <action>Write 20270704000010..20270704000013 migrations. Per memory reference_supabase_migration_filename_regex: strict 14-digit_name.sql. Per memory reference_supabase_migration_gotchas: SECDEF functions use `SET search_path = extensions, public`. Per memory reference_migration_timestamp_collision_precheck: the 20270704* window is reserved for plan 42-05 (00001..00007) and 42-06 (00010..00013). The 8-9 slots are reserved for plan 42-07 (NPS). The seed in 20270704000013 INSERTs 3 entries via `INSERT INTO changelog_entries (slug, title, body_md, published_at) VALUES (...) ON CONFLICT (slug) DO NOTHING`. Slugs: 'v1-3-smart-notifications', 'v1-3-pwa-offline', 'v1-3-dark-mode'. The body_md uses markdown formatting and DOES NOT include any HTML tags (dompurify in the renderer sanitizes anyway, but seed is hand-curated). Per CLAUDE.md, every PHI-irrelevant table is fine here — changelog is fully public content. Write leanshot/tests/rls/changelog-rls.test.ts using the same admin.generateLink + /auth/v1/verify pattern as plan 42-05 task 1 (per memory reference_rls_fixture_gotrueclient_flake). File-scoped TEST_SLUG_PREFIX per memory feedback_rls_per_file_slug_prefix.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && for f in 20270704000010 20270704000011 20270704000012 20270704000013; do ls supabase/migrations/${f}_*.sql >/dev/null || { echo "MISSING $f"; exit 1; }; done; cd leanshot && npx vitest run --config vitest-e2e.config.ts tests/rls/changelog-rls.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>4 migration files exist with strict naming; RLS test passes; cross-tenant proof passes.</done>
</task>

<task type="auto">
  <name>Task 2: Edge Function changelog-mark-read</name>
  <files>supabase/functions/changelog-mark-read/index.ts</files>
  <action>Implement supabase/functions/changelog-mark-read/index.ts. POST handler, no body required. Reads auth.uid() from the JWT (use the existing lifecycle-utils.ts pattern for JWT extraction). UPSERT user_changelog_dismissed (user_id, last_seen_published_at, updated_at) VALUES (auth.uid(), now(), now()) ON CONFLICT (user_id) DO UPDATE SET last_seen_published_at = EXCLUDED.last_seen_published_at, updated_at = EXCLUDED.updated_at. Returns 200 with { last_seen_published_at }. Per memory reference_supabase_functions_deploy_no_linked_flag: deploy without --linked. Zod gate on inputs (even though empty body — still validate Method = POST). Add a small Deno test asserting the upsert behavior.</action>
  <verify>
    <automated>test -f /Users/karstenhaldan/minisite/supabase/functions/changelog-mark-read/index.ts && grep -E "ON CONFLICT.*user_id.*DO UPDATE" /Users/karstenhaldan/minisite/supabase/functions/changelog-mark-read/index.ts</automated>
  </verify>
  <done>Edge Fn exists with the documented UPSERT signature; idempotent on repeat invocation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN — Push migrations and deploy changelog-mark-read</name>
  <what-built>4 migrations + 1 Edge Function.</what-built>
  <how-to-verify>
    1. `cd /Users/karstenhaldan/minisite && git rev-parse --show-toplevel` must print monorepo root.
    2. `supabase db push --linked`. Expect 20270704000010..00013 applied; NO "Skipping" lines.
    3. `supabase db query --linked "SELECT count(*) FROM changelog_entries"` → expects >= 3.
    4. `supabase db query --linked "SELECT slug, title FROM changelog_entries ORDER BY published_at DESC"` → expects v1.3 highlights.
    5. `supabase functions deploy changelog-mark-read --project-ref ytnsipxxmzgaebkqmokp` (no --linked).
    6. Smoke-test: `curl -X POST https://ytnsipxxmzgaebkqmokp.functions.supabase.co/changelog-mark-read -H "Authorization: Bearer <user-jwt>"` → expects 200 + { last_seen_published_at }.
  </how-to-verify>
  <resume-signal>Type "changelog-deployed" once migrations applied + Fn smoke-tested</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → changelog_entries write | Admin-only via RLS + is_admin() |
| User → user_changelog_dismissed | Per-user RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06-01 | Injection | Admin XSS via body_md | mitigate | UI renderer (42-09) uses react-markdown + dompurify; never raw HTML; no script tags allowed |
| T-42-06-02 | Information Disclosure | User reads another user's dismissal state | mitigate | RLS per-user policy + cross-tenant proof |
| T-42-06-03 | Spoofing | Forged mark-read request | mitigate | Edge Fn checks auth.uid() from JWT |
</threat_model>

<verification>
- 4 migrations apply cleanly.
- Cross-tenant RLS proof passes.
- Edge Fn responds to authenticated POST.
- Seed creates 3 entries.
</verification>

<success_criteria>
- A new authenticated user (with no user_changelog_dismissed row) sees `published_at > '1970-01-01'` = TRUE for all 3 seed entries (unread).
- After POST /changelog-mark-read, the user's last_seen_published_at = now() so unread-count = 0.
</success_criteria>

<output>
After completion, create `.planning/phases/42-v1-3-polish-closeout/42-06-SUMMARY.md`.
</output>
