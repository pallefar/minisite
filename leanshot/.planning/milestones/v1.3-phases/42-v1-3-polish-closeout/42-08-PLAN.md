---
phase: 42-v1-3-polish-closeout
plan: "08"
type: execute
wave: 3
depends_on: ["42-05", "42-04"]
files_modified:
  - leanshot/src/lib/notifications/settings-store.ts
  - leanshot/src/lib/notifications/permission.ts
  - leanshot/src/lib/notifications/realtime.ts
  - leanshot/src/lib/notifications/types.ts
  - leanshot/src/components/dashboard/settings/NotificationsSubtab.tsx
  - leanshot/src/components/dashboard/settings/SettingsPage.tsx
  - leanshot/src/components/dashboard/notifications/InAppNotificationToast.tsx
  - leanshot/src/lib/analytics/events.ts
  - leanshot/src/sw.ts
  - leanshot/src/App.tsx
  - leanshot/src/components/dashboard/settings/NotificationsSubtab.test.tsx
  - leanshot/e2e/notification-settings.spec.ts
autonomous: false
requirements:
  - POLISH-05
  - POLISH-06

must_haves:
  truths:
    - "Self-serve /settings/notifications subtab renders 5×3 matrix (categories × channels) with current toggle state per D-07"
    - "User can toggle each combo; mutation hits notification_settings table via supabase-js + reflects optimistically"
    - "Snooze controls offer 1d/7d/30d options per D-06 with category-level granularity"
    - "Frequency cap controls allow user to LOWER own cap (never raise) per D-04 — UI clamps and server enforces"
    - "Dismissal-rate suppression banner appears when notification_dismissal_state.throttle_until > now() per D-05 with 'Restore?' CTA"
    - "Web Push permission prompt is invoked ONLY inside an explicit user-gesture click handler per Pitfall 3"
    - "After permission grant, push subscription is sent to push-subscribe Edge Fn"
    - "In-app channel subscribes to user_notifications via Supabase Realtime + renders DS Toast per CONTEXT D-07 realtime pipeline"
    - "5 new analytics events fire: notification_sent (server-side via PostHog), notification_dismissed, notification_clicked, notification_snoozed, notification_permission_granted"
    - "axe-core CI gate from 42-02 passes for /settings/notifications (zero NEW violations)"
  artifacts:
    - path: "leanshot/src/components/dashboard/settings/NotificationsSubtab.tsx"
      provides: "5×3 toggle matrix + snooze controls + cap controls + suppression banner; uses DS Card/Toggle primitives"
      min_lines: 120
    - path: "leanshot/src/lib/notifications/permission.ts"
      provides: "requestPushPermission() — wraps Notification.requestPermission(); MUST be called from a user-click handler"
    - path: "leanshot/src/lib/notifications/realtime.ts"
      provides: "subscribeToUserNotifications(user_id, onFire) — Supabase Realtime channel.subscribe() on user_notifications"
    - path: "leanshot/src/sw.ts"
      provides: "Service-worker push event handler that renders system notification on incoming push payload"
  key_links:
    - from: "leanshot/src/components/dashboard/settings/NotificationsSubtab.tsx"
      to: "supabase.from('notification_settings').upsert(...)"
      via: "useNotificationSettings hook + react-query (or direct supabase-js depending on project pattern)"
      pattern: "notification_settings.*upsert"
    - from: "leanshot/src/lib/notifications/permission.ts"
      to: "POST /push-subscribe"
      via: "fetch with auth-bearer after pushManager.subscribe()"
      pattern: "push-subscribe"
    - from: "leanshot/src/lib/notifications/realtime.ts"
      to: "supabase.channel('user_notifications:<user_id>')"
      via: "postgres_changes on INSERT"
      pattern: "channel\\(.*user_notifications"
---

<objective>
Build the user-facing notification surface (POLISH-05 + POLISH-06 UI tier): the self-serve `/settings/notifications` subtab with the 5×3 matrix + snooze + caps + dismissal-rate banner, the web-push permission flow (Pitfall 3 user-gesture gated), the Supabase Realtime subscription that renders incoming in-app notifications as DS Toasts, and the service-worker push handler that renders system notifications outside the tab. Consumes everything from plans 42-05 (backend tables/Fns) and 42-04 (PWA service worker).

Purpose: This is the user-facing closing of POLISH-05 and POLISH-06. The matrix UI must match RESEARCH §Pattern 1. The permission flow must avoid the browser's quiet-permission penalty (Pitfall 3). The realtime channel must scope by user_id (NOT a global channel) so RLS continues to enforce isolation.

Output: Working /settings/notifications page + in-app toast renderer + system push notifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/SettingsPage.tsx
@/Users/karstenhaldan/minisite/leanshot/src/lib/analytics/events.ts
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Toast.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings store + permission + realtime + service worker push handler</name>
  <files>leanshot/src/lib/notifications/settings-store.ts, leanshot/src/lib/notifications/permission.ts, leanshot/src/lib/notifications/realtime.ts, leanshot/src/lib/notifications/types.ts, leanshot/src/sw.ts, leanshot/src/lib/analytics/events.ts</files>
  <behavior>
    - settings-store test 1: useNotificationSettings() fetches notification_settings rows for the current user; returns a Map keyed by `<category>:<channel>` with {enabled, snoozed_until, user_cap_override}.
    - settings-store test 2: updateSetting({category, channel, enabled}) calls supabase.from('notification_settings').upsert(...) with onConflict 'user_id,category,channel'; optimistic update applied first, rolled back on failure.
    - permission test 1: requestPushPermission() throws if called outside a user gesture (use a `__from_user_gesture` parameter that the click handler explicitly passes — runtime check on this flag to enforce Pitfall 3).
    - permission test 2: On Notification.permission === 'granted', pushManager.subscribe is called with applicationServerKey = VITE_VAPID_PUBLIC_KEY; the resulting subscription is POSTed to /push-subscribe Edge Fn.
    - realtime test: subscribeToUserNotifications(user_id, onFire) creates a Supabase channel filtered on user_id; INSERT events on user_notifications fire onFire(row) callback.
  </behavior>
  <action>Create leanshot/src/lib/notifications/types.ts mirroring the server-side types from supabase/functions/_shared/notification-types.ts (do NOT import from server — duplicate at the type boundary; the type definitions are tiny). Create leanshot/src/lib/notifications/settings-store.ts as either a Zustand slice (existing project pattern per CLAUDE.md §Architecture) OR a React Query hook — pick whichever the rest of /settings/* uses (read SettingsPage.tsx + neighbors first). Expose useNotificationSettings() returning {settings: Map, isLoading, error, update(setting)}. Create leanshot/src/lib/notifications/permission.ts with requestPushPermission(opts: {fromUserGesture: true}) — runtime assert opts.fromUserGesture === true (Pitfall 3 enforcement). After Notification.requestPermission() resolves 'granted': await navigator.serviceWorker.ready; const sub = await reg.pushManager.subscribe({userVisibleOnly:true, applicationServerKey:VITE_VAPID_PUBLIC_KEY}); POST sub to /push-subscribe with auth bearer. Per memory reference_vite_static_env_inlining: VITE_VAPID_PUBLIC_KEY must be referenced as a LITERAL `import.meta.env.VITE_VAPID_PUBLIC_KEY` (no dynamic key construction). Create leanshot/src/lib/notifications/realtime.ts: subscribeToUserNotifications(userId, onFire) opens `supabase.channel('user_notifications:' + userId).on('postgres_changes', {event:'INSERT', schema:'public', table:'user_notifications', filter:'user_id=eq.'+userId}, payload => onFire(payload.new)).subscribe()`. Returns unsubscribe function. **EXTEND leanshot/src/sw.ts** (created in plan 42-04 under injectManifest strategy — DO NOT create a separate file in leanshot/public/). Add a push event listener AFTER the existing route registrations and BEFORE the message→SKIP_WAITING listener: `self.addEventListener('push', (event) => { const payload = event.data?.json() as { title:string; body:string; icon?:string; tag?:string; urgency?:'low'|'normal'|'high'; deeplink?:string }; if (!payload) return; event.waitUntil(self.registration.showNotification(payload.title, { body: payload.body, icon: payload.icon ?? '/pwa-192x192.png', tag: payload.tag, requireInteraction: payload.urgency === 'high', data: { deeplink: payload.deeplink } })); }); self.addEventListener('notificationclick', (event) => { event.notification.close(); const url = (event.notification.data as {deeplink?:string})?.deeplink ?? '/'; event.waitUntil(self.clients.matchAll({type:'window'}).then((cs) => { for (const c of cs) { if (c.url.endsWith(url) && 'focus' in c) return c.focus(); } return self.clients.openWindow(url); })); });` — this runs inside the SW context (injectManifest gives us a single SW source — generateSW does NOT support arbitrary event listeners). The payload contract MUST match `supabase/functions/notification-send/index.ts` from plan 42-05 (Wave 2). Extend leanshot/src/lib/analytics/events.ts with 5 new TAXO events: 'notification_sent' (server-side only — but type-define here for client-side log), 'notification_dismissed', 'notification_clicked', 'notification_snoozed', 'notification_permission_granted'. These are CLIENT-fired except notification_sent which is logged server-side via supabase/functions/_shared/posthog-server.ts (already exists per Phase 24).</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx vitest run src/lib/notifications/ 2>&1 | tail -10 && npx tsc -b --noEmit 2>&1 | tail -5 && grep -E "notification_sent|notification_dismissed|notification_clicked|notification_snoozed|notification_permission_granted" src/lib/analytics/events.ts | wc -l | awk '{ if ($1 >= 5) print "OK"; else { print "FAIL"; exit 1 } }'</automated>
  </verify>
  <done>All notification lib tests pass; tsc -b --noEmit succeeds; 5 new TAXO events declared; permission.ts runtime-rejects calls without fromUserGesture flag.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotificationsSubtab UI — matrix + snooze + caps + suppression banner</name>
  <files>leanshot/src/components/dashboard/settings/NotificationsSubtab.tsx, leanshot/src/components/dashboard/settings/SettingsPage.tsx, leanshot/src/components/dashboard/notifications/InAppNotificationToast.tsx, leanshot/src/components/dashboard/settings/NotificationsSubtab.test.tsx</files>
  <behavior>
    - UI test 1: NotificationsSubtab renders 5 category rows × 3 channel columns = 15 toggle cells.
    - UI test 2: Clicking a toggle calls updateSetting({category, channel, enabled}) optimistically.
    - UI test 3: Snooze control offers exactly 1d / 7d / 30d (per D-06).
    - UI test 4: Cap input is type=number, min=0, max=<adminDefault>; attempting to type a value > adminDefault is clamped on blur with a tooltip "Admin default for this category is N" — server-side denial is the source of truth (UI is helper).
    - UI test 5: When notification_dismissal_state.throttle_until > now() for a category, the suppression banner appears above that row with copy "We've reduced <category> frequency because you've dismissed several. <Button>Restore</Button>"; Restore click DELETEs the throttle row (or sets throttle_until = now()).
    - UI test 6: An "Enable push notifications" button at top — clicking calls requestPushPermission({fromUserGesture: true}). If permission denied, copy explains "Browser-level permission denied; enable in browser settings" with a help link.
    - axe scan: The matrix is wrapped in a `<table aria-label="Notification preferences">` with proper `<th scope="row">` per RESEARCH §Pattern 1 example; axe-baseline.test.ts reports 0 NEW blocking violations.
  </behavior>
  <action>Create leanshot/src/components/dashboard/settings/NotificationsSubtab.tsx using the project DS primitives (Card, CardHeader, Toggle, Button, Input). Layout: `<Card>` with `<CardHeader title="Notifications">`; first child is the "Enable push notifications" Button row (only rendered if Notification.permission !== 'granted'); then the `<table>` matrix per RESEARCH §Pattern 1 — `<thead>` with category column + 3 channel headers; `<tbody>` with one row per category, `<th scope="row">` for the category label, three `<td>` with `<Toggle aria-label="<cat> <ch>">` per cell. After the table: a "Snooze a category" section using a `<select>` for category + 3 button group `Snooze 1d`/`Snooze 7d`/`Snooze 30d` → calls /notification-snooze Edge Fn. Below: "Frequency caps" rows showing the admin daily/weekly cap + a "Lower mine to ___" Input (clamped to <= admin default). At top of the page (inside the Card), conditional `<SuppressionBanner>` for any category where dismissal_state.throttle_until > now(). Update leanshot/src/components/dashboard/settings/SettingsPage.tsx to register the new subtab `'notifications'` with label "Notifications" and route '/settings/notifications' per D-07 (reuses P22 settings drawer pattern — read existing subtab registration shape first; do not invent). Create leanshot/src/components/dashboard/notifications/InAppNotificationToast.tsx: a small wrapper around the existing useToast that consumes the realtime subscription's onFire callback and renders Toast with title=payload.subject + onClick=window.location.href = payload.deeplink. Mount the subscription in leanshot/src/App.tsx alongside the existing global toast container (inside an authenticated wrapper — do NOT subscribe before user is authenticated). Write the comprehensive vitest + RTL test in NotificationsSubtab.test.tsx covering the 6 behaviors above + axe-core scan via @axe-core/react adapter.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx vitest run src/components/dashboard/settings/NotificationsSubtab.test.tsx 2>&1 | tail -10 && npx tsc -b --noEmit 2>&1 | tail -5 && npm run test:a11y 2>&1 | grep -E "settings/notifications|FAIL" | head -5</automated>
  </verify>
  <done>All 6 UI tests pass; axe-baseline a11y test passes for /settings/notifications route; matrix + snooze + cap + banner + permission-button all rendered; In-app toast renders on realtime INSERT event.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: e2e Playwright test for full notification settings flow</name>
  <files>leanshot/e2e/notification-settings.spec.ts</files>
  <behavior>
    - e2e-1: Seed an authenticated user; navigate to /settings/notifications; verify 15 toggles render with default state per D-02.
    - e2e-2: Toggle 'marketing'×'email' off; verify supabase row updated; reload page; toggle persists.
    - e2e-3: Click Snooze 7d on 'ai-insights'; verify notification_settings.snoozed_until column updated.
    - e2e-4: Click "Lower mine to" on ai-insights cap, set 1; verify user_cap_override=1; try to set 5 (above admin default 3) → UI clamps + server returns 400.
    - e2e-5: Insert a row into user_notifications for the test user via service-role; the realtime subscription fires; an in-app DS Toast appears with the payload subject.
    - e2e-6: Click "Enable push notifications" → grants permission (use page.context().grantPermissions(['notifications'])) → /push-subscribe receives a POST with valid subscription.
  </behavior>
  <action>Create leanshot/e2e/notification-settings.spec.ts following the existing leanshot/e2e/ pattern. Use page.addInitScript() to seed an authenticated session per memory reference_playwright_state_seeding. For e2e-5, use the service role key to INSERT a row directly into user_notifications then assert the Toast becomes visible. For e2e-6, use page.context().grantPermissions(['notifications'], {origin: ...}) to bypass the browser prompt. Per memory reference_playwright_conditional_project_argv: do NOT argv-sniff for project detection; use PLAYWRIGHT_NOTIFICATION_RUN=1 env var. Per memory feedback_realtime_layer_e2e_pattern: assert the realtime subscription at DB-level by directly subscribing via supabase-js channel inside the test file as well, to prove the subscription path works.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && PLAYWRIGHT_NOTIFICATION_RUN=1 npx playwright test e2e/notification-settings.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <done>All 6 e2e steps pass deterministically; the page renders without errors; realtime subscription delivers the in-app Toast.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN — Verify push notification end-to-end on real browser</name>
  <what-built>Complete user-facing notification flow: settings UI + permission grant + push subscription + in-app realtime toast + system push notification.</what-built>
  <how-to-verify>
    1. Run dev server: `cd leanshot && npm run dev` and open http://localhost:5173 in Chrome.
    2. Sign in as a real test user.
    3. Navigate to /settings/notifications.
    4. Verify the 5×3 matrix renders with defaults per D-02 (e.g. dose-reminders×email = ON, marketing×in-app = OFF).
    5. Click "Enable push notifications" button. Browser native prompt appears. Click Allow.
    6. Verify in DevTools Application → Service Workers that the SW is active.
    7. Verify in DevTools Application → Storage → IndexedDB or via supabase db query that `push_subscriptions` has a row for this user.
    8. From a separate terminal, fire a test notification:
       `curl -X POST https://ytnsipxxmzgaebkqmokp.functions.supabase.co/notification-send -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" -H "Content-Type: application/json" -d '{"user_id":"<your-uuid>","category":"ai-insights","payload":{"subject":"Test from 42-08","deeplink":"/dashboard"}}'`
    9. Confirm: (a) a system-level push notification appears (even with the tab in background); (b) bringing the tab to foreground shows an in-app DS Toast with the subject "Test from 42-08"; (c) supabase db query shows a new row in user_notifications with channel='in-app' and channel='web-push' fired_at.
    10. Test snooze: in /settings/notifications, click Snooze 1d on ai-insights. Re-fire the curl in step 8. Confirm NO notification appears (snoozed).
  </how-to-verify>
  <resume-signal>Type "notifications-verified" once: (a) permission grant works, (b) system push lands, (c) in-app toast renders, (d) snooze blocks fire</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → push subscription endpoint | High-entropy URL; treat as low-PII per RESEARCH security domain |
| User-gesture click → permission prompt | Browser-enforced gate (Pitfall 3) |
| Realtime channel → in-app toast | RLS-scoped to user_id filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-08-01 | Repudiation | Permission denied permanently due to eager prompt (Pitfall 3) | mitigate | requestPushPermission requires fromUserGesture flag; explicit user-click "Enable push notifications" button is the only call site |
| T-42-08-02 | Information Disclosure | Realtime channel leaks other users' notifications | mitigate | channel('user_notifications:'+userId) + filter user_id=eq.; RLS on user_notifications enforces |
| T-42-08-03 | Tampering | Client raises own cap via supabase-js | mitigate | DB trigger from 42-05; UI is helper, server is source of truth |
| T-42-08-04 | Information Disclosure | Push subscription endpoint URL leaks via client-side analytics | mitigate | RESEARCH §Security Domain: do NOT log push endpoint to PostHog; subscription stored server-side only |
| T-42-08-SC | — | No new package installs in this plan | accept | All deps already vetted in 42-02/42-04/42-05 |
</threat_model>

<verification>
- NotificationsSubtab renders + interactive; toggles persist; snooze writes DB.
- e2e spec passes 6 steps.
- Real push notification arrives end-to-end in browser.
- /settings/notifications has zero NEW axe violations in baseline.
</verification>

<success_criteria>
- User can self-serve every aspect of POLISH-06: opt out per category × channel, snooze, lower own cap.
- Snooze blocks fires server-side as verified in human checkpoint step 10.
- Permission request never fires outside an explicit user click (Pitfall 3 invariant).
</success_criteria>

<output>
After completion, create `.planning/phases/42-v1-3-polish-closeout/42-08-SUMMARY.md`.
</output>
