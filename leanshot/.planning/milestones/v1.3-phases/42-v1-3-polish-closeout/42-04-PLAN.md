---
phase: 42-v1-3-polish-closeout
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/package.json
  - leanshot/package-lock.json
  - leanshot/vite.config.ts
  - leanshot/src/sw.ts
  - leanshot/public/pwa-192x192.png
  - leanshot/public/pwa-512x512.png
  - leanshot/public/apple-touch-icon.png
  - leanshot/src/lib/pwa/register.ts
  - leanshot/src/lib/pwa/install-prompt.ts
  - leanshot/src/lib/pwa/offline-store.ts
  - leanshot/src/lib/native/capacitor-shim.ts
  - leanshot/src/hooks/useOfflineState.ts
  - leanshot/src/hooks/useInstallPrompt.ts
  - leanshot/src/components/pwa/InstallPromptCard.tsx
  - leanshot/src/components/pwa/OfflineBanner.tsx
  - leanshot/src/App.tsx
  - leanshot/e2e/pwa-offline.spec.ts
  - leanshot/e2e/pwa-install-prompt.spec.ts
  - leanshot/src/lib/pwa/register.test.ts
autonomous: true
requirements:
  - POLISH-07

must_haves:
  truths:
    - "vite-plugin-pwa@1.3.0 is integrated; npm run build produces a service worker (sw.js) + manifest"
    - "Service worker registration is LAZY-imported AFTER first paint via dynamic import (never in index chunk) per Pitfall 9"
    - "Service worker registration is SKIPPED inside Capacitor (Capacitor.isNativePlatform() returns true) per D-18"
    - "Runtime cache for Supabase API uses NetworkFirst with 3s timeout + 5min TTL; image cache uses CacheFirst with 1d TTL per D-15"
    - "Workbox runtime cache DOES NOT cache authenticated PHI responses (Pitfall 1 / HIPAA): only public reads + assets"
    - "Offline banner appears when navigator.onLine === false AND logging is disabled with clear messaging per D-13"
    - "Install prompt appears as branded card on 3rd dashboard visit AND not yet installed per D-16; Maybe-later snoozes 30d"
    - "SW update notification appears as non-blocking toast with Reload button per D-17 (skipWaiting:false)"
    - "Per-chunk bundle ceiling on index.js NOT exceeded after this plan ships"
  artifacts:
    - path: "leanshot/vite.config.ts"
      provides: "VitePWA plugin registered with strategies:'injectManifest' + autoUpdate + workbox runtimeCaching per D-15/D-17. injectManifest required because plan 42-08 adds a custom push event listener inside the SW context (generateSW does NOT support arbitrary listeners — workbox.additionalManifestEntries is for precache assets, not SW logic)."
    - path: "leanshot/src/sw.ts"
      provides: "Custom service worker source consumed by injectManifest. Owns precaching (self.__WB_MANIFEST), runtime caching (workbox-routing), update lifecycle (skipWaiting:false). 42-08 will extend this file with the push event listener — do NOT create leanshot/public/sw-push-handler.js."
    - path: "leanshot/src/lib/pwa/register.ts"
      provides: "initializePWA(onUpdate) — lazy-loaded; respects Capacitor.isNativePlatform()"
    - path: "leanshot/src/lib/pwa/install-prompt.ts"
      provides: "Branded install prompt orchestrator with beforeinstallprompt state machine (3 states: installed, snoozed, dismissed) per Pitfall 2"
    - path: "leanshot/src/components/pwa/OfflineBanner.tsx"
      provides: "Top banner when offline; disables logging UIs with messaging per D-13"
    - path: "leanshot/src/components/pwa/InstallPromptCard.tsx"
      provides: "Custom-branded deferred install prompt; primary 'Install' + secondary 'Maybe later' (30d snooze)"
  key_links:
    - from: "leanshot/src/App.tsx"
      to: "leanshot/src/lib/pwa/register.ts"
      via: "useEffect lazy import inside App component"
      pattern: "import\\(['\"]@/lib/pwa/register['\"]\\)"
    - from: "leanshot/src/lib/pwa/register.ts"
      to: "leanshot/src/lib/native/capacitor-shim.ts"
      via: "Capacitor.isNativePlatform() check before registerSW"
      pattern: "isNativePlatform\\(\\)"
    - from: "leanshot/vite.config.ts"
      to: "Workbox runtime cache for Supabase API"
      via: "runtimeCaching urlPattern matching supabase.co/rest/v1/ — only PUBLIC reads allowed"
      pattern: "supabase.*rest/v1"
---

<objective>
Stand up the PWA foundation (POLISH-07): vite-plugin-pwa 1.3.0 in generateSW mode with autoUpdate (skipWaiting: false), lazy-loaded service worker registration that respects Capacitor (D-18), HIPAA-safe runtime caching (Pitfall 1: NO authenticated API responses cached unless explicitly public), offline banner that gates logging UIs (D-13), and a branded deferred install prompt with 3-state lifecycle (Pitfall 2). Wire the SW update toast (D-17). No notification triggers in this plan — those are Wave 3 once the firewall plan-set ships.

Purpose: PWA wraps the SAME web bundle that Capacitor consumes (D-18); both delivery wrappers must stay separable. Lazy-loading the SW glue keeps the index chunk inside its bundle ceiling (Pitfall 9). Read-only offline (D-13) avoids the sync-queue complexity deferred to v1.4.

Output: Service worker registers on web (not in Capacitor); offline banner gates logging; install prompt is deferred + branded; bundle ceilings respected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/vite.config.ts
@/Users/karstenhaldan/minisite/leanshot/src/App.tsx
@/Users/karstenhaldan/minisite/leanshot/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vite-plugin-pwa, generate icons, wire into vite.config.ts</name>
  <files>leanshot/package.json, leanshot/package-lock.json, leanshot/vite.config.ts, leanshot/src/sw.ts, leanshot/public/pwa-192x192.png, leanshot/public/pwa-512x512.png, leanshot/public/apple-touch-icon.png</files>
  <action>From leanshot/: `npm install -D vite-plugin-pwa@^1.3.0 workbox-window@^7.4.1 @vite-pwa/assets-generator@^1.0.2`. Generate PWA icon assets from the existing project logo using `npx pwa-assets-generator` — locate the source logo (look in leanshot/src/illustrations/ or leanshot/public/ — pick the highest-resolution SVG/PNG available; fallback to apple-touch-icon if present). Configure with `@vite-pwa/assets-generator/dist/cli.js` to emit 192x192 + 512x512 PNGs + apple-touch-icon (180x180) into leanshot/public/. Then edit leanshot/vite.config.ts: import { VitePWA } from 'vite-plugin-pwa' and add the plugin per RESEARCH §Pattern 3 example, with these LITERAL values (do NOT improvise): `registerType: 'autoUpdate'`, `injectRegister: false` (lazy-register manually per Pitfall 9), `strategies: 'injectManifest'` (NOT generateSW — see artifact note: 42-08 must add a push listener inside the SW context), `srcDir: 'src'`, `filename: 'sw.ts'`, `devOptions: { enabled: false }` (Pitfall 5), workbox.skipWaiting: false (D-17), workbox.clientsClaim: true. Manifest: name 'LeanShot', short_name 'LeanShot', theme_color '#0B1413', background_color '#EFEBE0', display 'standalone', icons referencing the three PNGs. workbox.runtimeCaching MUST follow the HIPAA constraint (Pitfall 1) — DO NOT cache authenticated Supabase API responses. Allow only: (1) Supabase REST URLs matching `/rest/v1/(kb_articles|changelog_entries|status_components)` (public read tables; explicit allowlist — NEVER a broad `supabase.co/rest/v1/.*` glob) with NetworkFirst, networkTimeoutSeconds 3, cacheName 'public-api-cache', expiration {maxEntries 50, maxAgeSeconds 300}; (2) image/font assets matching /\.(png|jpg|jpeg|svg|webp|avif|woff2)$/i with CacheFirst, cacheName 'asset-cache', expiration {maxEntries 200, maxAgeSeconds 86400}. Add an inline comment in vite.config.ts above runtimeCaching explaining the Pitfall 1 / HIPAA constraint and why the URL allowlist is explicit (not pattern-match all).

**ALSO create leanshot/src/sw.ts** (consumed by injectManifest). With injectManifest the workbox helpers (precaching + routing) live INSIDE the SW source, not the plugin config. Template:
```ts
/// <reference lib="webworker" />
import { precacheAndRoute, cleanupOutdatedCaches } from 'workbox-precaching';
import { registerRoute } from 'workbox-routing';
import { NetworkFirst, CacheFirst } from 'workbox-strategies';
import { ExpirationPlugin } from 'workbox-expiration';

declare const self: ServiceWorkerGlobalScope;

cleanupOutdatedCaches();
precacheAndRoute(self.__WB_MANIFEST);

// HIPAA constraint (Pitfall 1): explicit allowlist of public-read endpoints only.
registerRoute(
  ({ url }) => /\/rest\/v1\/(kb_articles|changelog_entries|status_components)/.test(url.pathname),
  new NetworkFirst({ cacheName: 'public-api-cache', networkTimeoutSeconds: 3,
    plugins: [new ExpirationPlugin({ maxEntries: 50, maxAgeSeconds: 300 })] }),
);
registerRoute(
  ({ request, url }) => /\.(png|jpg|jpeg|svg|webp|avif|woff2)$/i.test(url.pathname),
  new CacheFirst({ cacheName: 'asset-cache',
    plugins: [new ExpirationPlugin({ maxEntries: 200, maxAgeSeconds: 86400 })] }),
);

// 42-08 will add: self.addEventListener('push', (event) => { ... showNotification ... }).
// Push listener lives HERE (not leanshot/public/sw-push-handler.js) because injectManifest
// gives us a single SW source file that owns ALL background behavior in the SW context.

// Update lifecycle: do NOT auto-skipWaiting (D-17).
self.addEventListener('message', (event) => {
  if (event.data?.type === 'SKIP_WAITING') self.skipWaiting();
});
```
Install `workbox-precaching`, `workbox-routing`, `workbox-strategies`, `workbox-expiration` as deps of `vite-plugin-pwa` (they ship transitively but pin explicitly for predictability — verify after install that all four resolve).</action>
  <verify>
    <automated>cd leanshot && grep -q "VitePWA" vite.config.ts && grep -q "skipWaiting: false" vite.config.ts && grep -E "kb_articles|changelog_entries|status_components" vite.config.ts && ls public/pwa-192x192.png public/pwa-512x512.png public/apple-touch-icon.png && npm run build 2>&1 | tail -10 && ls leanshot/dist/sw.js 2>/dev/null || ls dist/sw.js</automated>
  </verify>
  <done>vite-plugin-pwa + workbox-window + @vite-pwa/assets-generator at the pinned versions; PWA icons in public/; sw.js generated by build; HIPAA-safe runtimeCaching allowlist (NOT a wildcard supabase glob).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lazy SW registration, Capacitor shim, offline + install hooks, components</name>
  <files>leanshot/src/lib/pwa/register.ts, leanshot/src/lib/pwa/install-prompt.ts, leanshot/src/lib/pwa/offline-store.ts, leanshot/src/lib/native/capacitor-shim.ts, leanshot/src/hooks/useOfflineState.ts, leanshot/src/hooks/useInstallPrompt.ts, leanshot/src/components/pwa/InstallPromptCard.tsx, leanshot/src/components/pwa/OfflineBanner.tsx, leanshot/src/lib/pwa/register.test.ts</files>
  <behavior>
    - Test 1 (register.test.ts): initializePWA returns undefined and DOES NOT call registerSW when Capacitor.isNativePlatform() === true (D-18)
    - Test 2 (register.test.ts): initializePWA calls registerSW with onNeedRefresh handler when Capacitor.isNativePlatform() === false (D-17)
    - Test 3 (register.test.ts): the onUpdate callback fires when registerSW's onNeedRefresh fires
    - Test 4 (install-prompt logic): state machine has THREE states (installed | snoozed | dismissed); 'Maybe later' sets snoozed-until = now + 30d (D-16); appinstalled event sets installed=true (Pitfall 2); cannot transition installed → snoozed
    - Test 5 (install-prompt logic): card displays only when dashboard-visit-count >= 3 AND state !== installed AND state !== dismissed AND (state !== snoozed OR snoozed-until < now)
  </behavior>
  <action>Create leanshot/src/lib/native/capacitor-shim.ts: a tiny module exporting `Capacitor` object with `isNativePlatform(): boolean` that returns `typeof window !== 'undefined' && (window as any).Capacitor?.isNativePlatform?.() === true`. This shim makes Capacitor optional — Capacitor SDK is NOT a dependency of this phase; if it's not installed, isNativePlatform() returns false and PWA registers normally. Per memory feedback_scaffolding_for_deferred_mobile_pattern: this is the architectural seam the v1.4 Capacitor phase will hook into. Create leanshot/src/lib/pwa/register.ts per RESEARCH §Pattern 3 second example: `import { registerSW } from 'virtual:pwa-register'`, `import { Capacitor } from '@/lib/native/capacitor-shim'`, exports `initializePWA(onUpdateAvailable: () => void)` that early-returns if Capacitor.isNativePlatform() else calls registerSW with onNeedRefresh and onOfflineReady. Create leanshot/src/lib/pwa/install-prompt.ts with the 3-state state machine + localStorage-persisted `leanshot_install_state_v1` (per memory reference_minisite_monorepo_layout, namespace v1). State shape: `{ state: 'fresh' | 'snoozed' | 'dismissed' | 'installed', snoozed_until?: ISO, last_event_at: ISO }`. Listen for `beforeinstallprompt` (preventDefault + cache the event), `appinstalled`, and `display-mode: standalone` matchMedia (sets installed=true only on standalone). Dashboard-visit count is a separate localStorage key incremented by `recordDashboardVisit()` called from HomeTab mount. Create leanshot/src/lib/pwa/offline-store.ts: a Zustand SLICE (or standalone store) exposing `{ isOffline: boolean, banner_dismissed_at: ISO | null }` driven by `navigator.onLine` events. Create leanshot/src/hooks/useOfflineState.ts and leanshot/src/hooks/useInstallPrompt.ts as thin React hooks over the stores. Create leanshot/src/components/pwa/OfflineBanner.tsx: top-fixed banner using DS Card / a11y `role="status"` with copy "You're offline — logging resumes when reconnected." (verbatim from D-13). The banner exposes a context value `OfflineGateContext.disableLogging` that logging UIs check before allowing submit; Wave 3 logging surfaces consume this. Create leanshot/src/components/pwa/InstallPromptCard.tsx: branded card matching project DS, primary Button 'Install LeanShot', secondary Button 'Maybe later'. Wire all three components into App.tsx AFTER first paint via useEffect — InstallPromptCard mounted at AppShell level (not inside Suspense fallback). The SW registration: `useEffect(() => { import('@/lib/pwa/register').then(({ initializePWA }) => initializePWA(() => showToast({ title: 'New version available', action: { label: 'Reload', onClick: () => window.location.reload() }, persist: true }))); }, [])` per RESEARCH §Code Examples "Service worker update toast". Write leanshot/src/lib/pwa/register.test.ts that mocks the virtual:pwa-register module and Capacitor shim to assert Tests 1-3 above. Write inline unit tests for install-prompt state-machine logic.</action>
  <verify>
    <automated>cd leanshot && npx vitest run src/lib/pwa/register.test.ts && npx tsc -b --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All five behavior tests pass; tsc -b --noEmit succeeds; App.tsx uses dynamic import for register.ts (verifiable via `grep -c "import('@/lib/pwa/register')" src/App.tsx` >= 1).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: e2e Playwright tests for offline mode + install prompt deferral</name>
  <files>leanshot/e2e/pwa-offline.spec.ts, leanshot/e2e/pwa-install-prompt.spec.ts</files>
  <behavior>
    - Test e2e-1 (pwa-offline.spec.ts): With page online → no offline banner present. After `await context.setOffline(true)` → OfflineBanner appears with the verbatim D-13 copy.
    - Test e2e-2 (pwa-offline.spec.ts): While offline, the dose-log submit button (or any logging form's submit) is disabled OR submission is blocked with the disable-logging message.
    - Test e2e-3 (pwa-install-prompt.spec.ts): Fresh localStorage state + 1 dashboard visit → InstallPromptCard NOT visible. 3 dashboard visits → card visible. Click "Maybe later" → card hidden + localStorage state.snoozed_until set ~30d in future.
    - Test e2e-4 (pwa-install-prompt.spec.ts): If localStorage state === 'installed' OR matchMedia('(display-mode: standalone)') matches → card never shown.
  </behavior>
  <action>Create leanshot/e2e/pwa-offline.spec.ts and leanshot/e2e/pwa-install-prompt.spec.ts following existing leanshot/e2e/ patterns. Use page.addInitScript() to seed localStorage state per reference_playwright_state_seeding. Use context.setOffline() for the offline tests. For the install-prompt test, fake the beforeinstallprompt event via `page.evaluate(() => window.dispatchEvent(new Event('beforeinstallprompt')))` — note that Chromium may not allow synthesizing the real event, so the install-prompt module must accept a debug-injection hook (a global `window.__leanshot_simulate_install_prompt` that the e2e test sets). Document this hook with a `/* TEST HOOK — leanshot e2e/pwa-install-prompt.spec.ts */` comment in install-prompt.ts. Per memory reference_playwright_conditional_project_argv, do not argv-sniff for conditional behavior.</action>
  <verify>
    <automated>cd leanshot && npx playwright test e2e/pwa-offline.spec.ts e2e/pwa-install-prompt.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <done>Both e2e specs pass; OfflineBanner appears on context.setOffline; InstallPromptCard appears after 3 dashboard visits and is gated by the snoozed/installed/dismissed state machine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service worker → cached responses | Workbox cache is shared per PWA install — cross-user surface |
| Browser → install-prompt local state | localStorage; gated by same-origin policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04-01 | Information Disclosure | SW caches authenticated PHI responses cross-user (Pitfall 1) | mitigate | Explicit URL allowlist in workbox.runtimeCaching — only kb_articles, changelog_entries, status_components (all public). PHI-bearing endpoints (e.g. `/rest/v1/injections`, `/rest/v1/clinic_invites`) are NOT in the allowlist; they pass through to network on every request |
| T-42-04-02 | Repudiation | Install prompt suppressed forever (Pitfall 2) | mitigate | 3-state state machine (installed | snoozed | dismissed); snoozed re-shows after 30d; installed only set on matchMedia standalone (not on Maybe-later) |
| T-42-04-03 | Tampering | SW serves stale chunks during HMR (Pitfall 5) | mitigate | devOptions.enabled: false in vite.config.ts |
| T-42-04-04 | Denial of Service | SW glue inflates index chunk past ceiling (Pitfall 9) | mitigate | injectRegister: false + dynamic import in App.tsx useEffect; verified via Wave 4 bundle-budget check |
| T-42-04-SC | Tampering | npm install vite-plugin-pwa + workbox-window + @vite-pwa/assets-generator | mitigate | LEGITIMATE per RESEARCH §Package Legitimacy (vite-pwa org maintainer, Workbox is Google-backed) |
</threat_model>

<verification>
- `cd leanshot && npm run build` produces dist/sw.js + dist/manifest.webmanifest.
- `cd leanshot && npm run check-bundle-budget` passes (index chunk does NOT inflate).
- Disconnecting network in DevTools shows the OfflineBanner.
- Visiting /dashboard 3 times shows the branded install prompt.
- Service worker auto-update flow shows the toast (manual: deploy a new version and observe toast on stale tab).
</verification>

<success_criteria>
- PWA install via "Install LeanShot" launches the app in standalone display mode.
- Going offline shows the banner AND disables every logging form.
- vite.config.ts comment explicitly forbids caching PHI endpoints; the allowlist is exhaustive.
- 4 e2e tests pass deterministically.
</success_criteria>

<output>
After completion, create `.planning/phases/42-v1-3-polish-closeout/42-04-SUMMARY.md`.
</output>
