---
phase: 46
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270725000004_p46_certificates_bucket.sql
  - supabase/migrations/20270725000005_p46_course_resources_bucket.sql
autonomous: true
requirements: [COURSE-04, COURSE-06]
tags: [courses, storage, rls, certificates, resources]
threat_refs: [T-46-06, T-46-08]

must_haves:
  truths:
    - "Storage bucket 'certificates' exists; private; only service-role can INSERT; authenticated users SELECT only objects under their own user_id prefix"
    - "Storage bucket 'course-resources' exists; private; SELECT gated to Pro/Lifetime/Trial via tier_effective; MIME whitelist enforced"
    - "Signed URL TTL: 60 minutes (3600 seconds) for both buckets — pattern matches D-13"
  artifacts:
    - path: "supabase/migrations/20270725000004_p46_certificates_bucket.sql"
      provides: "certificates Storage bucket + RLS (signed-only SELECT 60-min TTL; service-role INSERT only)"
      contains: "'certificates'"
    - path: "supabase/migrations/20270725000005_p46_course_resources_bucket.sql"
      provides: "course-resources Storage bucket + RLS (tier-gated SELECT; admin INSERT via is_staff)"
      contains: "'course-resources'"
  key_links:
    - from: "certificates bucket SELECT policy"
      to: "auth.uid() + storage.foldername(name)[1]"
      via: "object-path-prefix isolation"
      pattern: "storage.foldername.*auth.uid"
    - from: "course-resources bucket SELECT policy"
      to: "tier_effective view (Phase 43)"
      via: "EXISTS subquery on has_active"
      pattern: "tier_effective.*has_active"
---

<objective>
Create the two Storage buckets Phase 46 needs: `certificates` (cert PDFs, per-user signed URLs only) and `course-resources` (downloadable lesson resources, tier-gated). Both buckets are private; signed URLs are the only access path. MIME whitelist on `course-resources` blocks arbitrary upload.

Purpose: Cert PDFs must exist somewhere reachable by a 60-min signed URL (D-13). Lesson resources must be gated to Pro/Lifetime/Trial users (D-16 / COURSE-06). Both buckets must be in place before Plan 46-07 (cert Edge Fn) and Plan 46-08 (consumer UI) try to read/write them.

Output: 2 net-new migrations. No schema overlap with Plan 46-01 (different tables, namespace `storage`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-m4-courses-classroom/46-CONTEXT.md
@.planning/phases/46-m4-courses-classroom/46-PATTERNS.md

# Analog migrations (READ before writing):
@supabase/migrations/20270601000007_dsar_exports_storage_bucket.sql
@supabase/migrations/20270720000003_p44_community_media_bucket.sql

# Phase 43 view referenced by tier-gated bucket:
@supabase/migrations/20270715000002_p43_tier_effective_view_v2.sql

<interfaces>
<!-- D-13: certificates bucket -->
-- Bucket: 'certificates' (private)
-- Object path: '<user_id>/<course_id>-<cert_id>.pdf'
-- SELECT: authenticated where storage.foldername(name)[1] = auth.uid()::text
-- INSERT/UPDATE/DELETE: service_role only (no authenticated policy)

<!-- D-16/COURSE-06: course-resources bucket -->
-- Bucket: 'course-resources' (private), 200 MB cap, MIME whitelist
-- Allowed MIMEs: application/pdf, video/mp4, application/zip
-- Object path: '<course_id>/<lesson_id>/<uuid>.<ext>'
-- SELECT: authenticated with tier_effective.has_active = true
-- INSERT: authenticated with public.is_staff()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create certificates Storage bucket migration</name>
  <read_first>
    - .planning/phases/46-m4-courses-classroom/46-PATTERNS.md §"certificates_bucket.sql" (exact pattern)
    - supabase/migrations/20270601000007_dsar_exports_storage_bucket.sql (analog: private bucket + own-folder SELECT)
  </read_first>
  <files>supabase/migrations/20270725000004_p46_certificates_bucket.sql</files>
  <action>
    Single `begin; ... commit;`.

    1. `insert into storage.buckets (id, name, public) values ('certificates', 'certificates', false) on conflict (id) do nothing;`
    2. `create policy "certificates_select_own_object" on storage.objects for select to authenticated using (bucket_id = 'certificates' and auth.uid()::text = (storage.foldername(name))[1]);` — wrap in `do $$ ... if not exists ... end $$;` guard per p44 pattern.
    3. NO `for insert / update / delete` policies for authenticated. service_role bypasses RLS for INSERT (used by `generate-course-certificate` Edge Fn in Plan 46-07).
    4. Header comment block: D-13 + threat refs T-46-06 (path traversal) + T-46-08 (URL leakage mitigated by 60-min TTL signed URLs).

    Filename: `20270725000004_p46_certificates_bucket.sql` (14-digit prefix per `reference_supabase_migration_filename_regex`).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/supabase &amp;&amp; grep -qE "insert into storage\.buckets.*'certificates'" migrations/20270725000004_p46_certificates_bucket.sql &amp;&amp; grep -qE "storage\.foldername\(name\)\)?\[1\]" migrations/20270725000004_p46_certificates_bucket.sql &amp;&amp; ! grep -qE "for (insert|update|delete) to authenticated" migrations/20270725000004_p46_certificates_bucket.sql</automated>
  </verify>
  <acceptance_criteria>
    - File at exact path.
    - Bucket inserted as private (`public=false`) with `on conflict do nothing`.
    - SELECT policy uses `storage.foldername(name)[1] = auth.uid()::text`.
    - NO authenticated INSERT/UPDATE/DELETE policy exists.
    - File header references D-13 + T-46-06 + T-46-08.
  </acceptance_criteria>
  <done>Migration written; service-role can insert cert PDFs; users can only SELECT their own folder.</done>
</task>

<task type="auto">
  <name>Task 2: Create course-resources Storage bucket migration</name>
  <read_first>
    - .planning/phases/46-m4-courses-classroom/46-PATTERNS.md §"course_resources_bucket.sql" (exact pattern with idempotent guards)
    - supabase/migrations/20270720000003_p44_community_media_bucket.sql (analog: MIME whitelist + tier-gated SELECT)
    - .planning/phases/46-m4-courses-classroom/46-CONTEXT.md D-16 (Pro-gated resources)
  </read_first>
  <files>supabase/migrations/20270725000005_p46_course_resources_bucket.sql</files>
  <action>
    Single `begin; ... commit;`.

    1. `insert into storage.buckets (id, name, public, file_size_limit, allowed_mime_types) values ('course-resources','course-resources',false,209715200, array['application/pdf','video/mp4','application/zip']) on conflict (id) do nothing;`
    2. SELECT policy `objects_select_course_resources` (idempotent `do $$ ... if not exists ... end $$` guard):
       - Bucket id = 'course-resources'
       - auth.uid() is not null
       - EXISTS subquery on tier_effective with `te.has_active = true` (D-07/D-16 tier gate via boolean, not tier_label string list — per the project's preference to gate on `has_active` boolean).
    3. INSERT policy `objects_insert_course_resources` (admin-only via is_staff):
       - Bucket id = 'course-resources'
       - `public.is_staff()`
    4. UPDATE + DELETE policies: same admin-only `public.is_staff()` check.
    5. Header references D-16, COURSE-06, T-46-06.

    Filename: `20270725000005_p46_course_resources_bucket.sql`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/supabase &amp;&amp; grep -qE "'course-resources'" migrations/20270725000005_p46_course_resources_bucket.sql &amp;&amp; grep -qE "209715200" migrations/20270725000005_p46_course_resources_bucket.sql &amp;&amp; grep -qE "application/pdf.*video/mp4.*application/zip" migrations/20270725000005_p46_course_resources_bucket.sql &amp;&amp; grep -qE "te\.has_active\s*=\s*true" migrations/20270725000005_p46_course_resources_bucket.sql &amp;&amp; grep -qE "public\.is_staff\(\)" migrations/20270725000005_p46_course_resources_bucket.sql</automated>
  </verify>
  <acceptance_criteria>
    - File at exact path.
    - Bucket private + 200 MB cap + MIME whitelist [pdf, mp4, zip].
    - SELECT policy gates on `tier_effective.has_active = true`.
    - INSERT/UPDATE/DELETE all gate on `public.is_staff()`.
    - No `staff_users` references.
  </acceptance_criteria>
  <done>Migration written; tier-gated resource downloads enforced at storage layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anon → Storage | NO anon access to either bucket (both private) |
| Authenticated → Storage SELECT | certificates: own folder only; course-resources: requires `has_active=true` |
| Service-role Edge Fn → Storage | INSERT cert PDFs (Plan 46-07) bypasses RLS; signed URLs returned to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-06 | Information Disclosure | Storage path traversal (user A reads user B's cert) | mitigate | RLS uses `storage.foldername(name)[1] = auth.uid()::text` — user_id prefix enforced |
| T-46-08 | Information Disclosure | Cert PDF URL leakage beyond TTL | mitigate | 60-min signed URL TTL (3600s); user re-fetches via Edge Fn after expiry; bucket itself never public |
</threat_model>

<verification>
- Both migrations apply cleanly to linked DB (verified by Plan 46-11 push step).
- Post-push live-DB verify (Plan 46-11): `npx supabase db query --linked "SELECT name, public FROM storage.buckets WHERE name IN ('certificates','course-resources')"` returns 2 rows, both `public=false`.
</verification>

<success_criteria>
- [ ] 2 migrations exist at exact paths.
- [ ] Both buckets are private (`public=false`).
- [ ] certificates bucket has NO authenticated write policy.
- [ ] course-resources bucket gates SELECT on `tier_effective.has_active = true` and writes on `public.is_staff()`.
- [ ] grep gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/46-m4-courses-classroom/46-02-SUMMARY.md` listing the 2 file paths, SHAs, and a one-line note that buckets are created idempotently (`on conflict (id) do nothing`).
</output>
