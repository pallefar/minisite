---
phase: 45
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql
  - leanshot/src/lib/community/community-storage.ts
autonomous: true
requirements:
  - COMMUNITY-08
must_haves:
  truths:
    - "Storage bucket 'dm-attachments' exists; private; 5 MB file_size_limit; MIME whitelist image/jpeg|png|webp"
    - "SELECT policy requires authenticated user to be participant of dm_thread referenced by path[0]"
    - "INSERT policy requires authenticated user to be participant of dm_thread referenced by path[0]"
    - "DELETE policy requires authenticated user to be sender of direct_message referenced by path[1]"
    - "Path convention: {thread_id}/{message_id}.{ext}"
    - "INSERT RLS uses EXISTS dm_threads JOIN (NOT (foldername)[1] = auth.uid() — path segment 0 is thread_id not user_id)"
    - "60-min signed-URL TTL helper exported from community-storage.ts"
  artifacts:
    - path: "supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql"
      provides: "dm-attachments bucket + RLS policies"
      contains: "dm-attachments"
    - path: "leanshot/src/lib/community/community-storage.ts"
      provides: "getDmAttachmentSignedUrl helper"
      exports: ["getDmAttachmentSignedUrl", "DM_ATTACHMENT_MAX_BYTES"]
  key_links:
    - from: "supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql"
      to: "public.dm_threads"
      via: "RLS predicate EXISTS subquery"
      pattern: "EXISTS \\(\\s*SELECT 1 FROM public\\.dm_threads"
---

<objective>
Ship the `dm-attachments` Storage bucket and its RLS — the key divergence from Phase 44's `community-media` is the path shape: `{thread_id}/{message_id}.{ext}`, NOT `{user_id}/...`. The simple `(storage.foldername(name))[1] = auth.uid()::text` pattern from community-media DOES NOT work here. Per RESEARCH Pattern 5 + RESEARCH Pitfall 3, the INSERT/SELECT policies MUST use `EXISTS (SELECT 1 FROM dm_threads ...)` with an explicit `(storage.foldername(name))[1]::uuid` cast.

Purpose: Wave 0 foundation for plan 45-04 (dm-create-thread) and 45-07 (DMMessageComposer attachment upload).
Output: 1 migration file + community-storage.ts extension with signed-URL helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-RESEARCH.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md

# Primary analog — copy bucket + RLS policy shape, REPLACE foldername predicate with dm_threads JOIN
@supabase/migrations/20270720000003_p44_community_media_bucket.sql

# Helper analog
@leanshot/src/lib/community/community-storage.ts
</context>

<interfaces>
<!-- After this plan: -->

From `leanshot/src/lib/community/community-storage.ts` (extended):
```typescript
export const DM_ATTACHMENT_MAX_BYTES = 5 * 1024 * 1024; // 5 MB per D-09 (NOT 10 MB like community-media)
export async function getDmAttachmentSignedUrl(path: string): Promise<{ url: string } | { error: string }>;
// MIME whitelist reuses existing COMMUNITY_MEDIA_MIMES (jpeg, png, webp)
```

Bucket id: `dm-attachments`. Path convention: `{thread_id}/{message_id}.{ext}`. Signed URL TTL: 3600 seconds (60 min per D-09).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dm-attachments bucket migration + 3 RLS policies (thread-participant JOIN)</name>
  <files>supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql</files>
  <read_first>
    - supabase/migrations/20270720000003_p44_community_media_bucket.sql (primary analog — copy bucket INSERT pattern at lines 24-33 + idempotency-guard DO $$ at lines 40-54)
    - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-RESEARCH.md §Pattern 5 (full RLS SQL)
    - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md §"supabase/migrations/<ts>_p45_dm_attachments_bucket.sql" (Key divergence callout)
  </read_first>
  <behavior>
    - INSERT into storage.buckets with id='dm-attachments', public=false, file_size_limit=5242880 (5 MB), allowed_mime_types=array['image/jpeg','image/png','image/webp']; on conflict (id) do nothing
    - INSERT RLS policy `objects_insert_dm_attachments`: bucket_id='dm-attachments' AND EXISTS (SELECT 1 FROM public.dm_threads dt WHERE dt.id = (storage.foldername(name))[1]::uuid AND (dt.creator_user_id = auth.uid() OR dt.recipient_user_id = auth.uid()))
    - SELECT RLS policy `objects_select_dm_attachments`: same predicate as INSERT
    - DELETE RLS policy `objects_delete_dm_attachments`: bucket_id='dm-attachments' AND EXISTS (SELECT 1 FROM public.direct_messages dm WHERE dm.id = (storage.foldername(name))[2]::uuid AND dm.sender_user_id = auth.uid())
    - All 3 policies wrapped in DO $$ begin if not exists (...) idempotency guards (analog pattern)
    - Migration depends on plan 45-01 having created public.dm_threads + public.direct_messages tables (depends_on: [] is correct because Wave 0 plans run in parallel; supabase db push will apply all Wave 0 migrations in timestamp order — 000001 → 000005 — so dm_threads exists by the time 000005 runs)
  </behavior>
  <action>
    Migration filename: `supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql`. Open `20270720000003_p44_community_media_bucket.sql` and copy lines 24-33 (bucket INSERT) — change id/name to 'dm-attachments', file_size_limit to 5242880 (5 MB per D-09; not the 10 MB of community-media), MIME array to `array['image/jpeg','image/png','image/webp']`.

    Then write 3 storage policies using the DO $$ begin if not exists ... then create policy ... end if; end $$; idempotency guard from analog lines 40-54. Replace the analog's `(storage.foldername(name))[1] = auth.uid()::text` predicate with the dm_threads JOIN from 45-RESEARCH.md §Pattern 5 (full SQL) — explicit `::uuid` cast on `(storage.foldername(name))[1]` per RESEARCH Pitfall 3.

    DELETE policy uses `[2]` (message_id, second path segment) joined to `direct_messages.id` checking `sender_user_id = auth.uid()`. This enforces author-only delete (D-09 implication).

    Add `comment on policy objects_insert_dm_attachments on storage.objects is 'Phase 45 Plan 45-03 — thread-participant gate via dm_threads JOIN; path[0] is thread_id (uuid)';` to document the divergence from community-media.
  </action>
  <acceptance_criteria>
    - File exists with name `20270727000005_p45_dm_attachments_bucket.sql`
    - `grep -c "'dm-attachments'" supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql` returns ≥4 (1 bucket INSERT + 3 policy bucket_id checks)
    - `grep -c "5242880" supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql` returns 1 (5 MB cap, NOT 10485760)
    - `grep -cE "EXISTS\\s*\\(\\s*SELECT 1 FROM public\\.dm_threads" supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql` returns ≥2 (INSERT + SELECT policies use JOIN)
    - `grep -c "(storage.foldername(name))\\[1\\]::uuid" supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql` returns ≥2
    - `grep -c "(storage.foldername(name))\\[1\\] = auth.uid()" supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql` returns 0 (the rejected simple pattern; this is the Phase 44 shape, NOT Phase 45)
    - `grep -cE "create policy objects_(insert|select|delete)_dm_attachments" supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql` returns 3
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test $(grep -c "5242880" supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql) -eq 1 && grep -cE "EXISTS\\s*\\(\\s*SELECT 1 FROM public\\.dm_threads" supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql | awk '$1 >= 2 {print "OK"; exit 0} {print "FAIL"; exit 1}' && test $(grep -c "(storage.foldername(name))\\[1\\] = auth.uid()" supabase/migrations/20270727000005_p45_dm_attachments_bucket.sql) -eq 0 && echo "OK"</automated>
  </verify>
  <done>Bucket migration ships with 3 policies using dm_threads JOIN (not the community-media foldername-equals-uid pattern).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: community-storage.ts — getDmAttachmentSignedUrl helper + 5 MB constant</name>
  <files>leanshot/src/lib/community/community-storage.ts</files>
  <read_first>
    - leanshot/src/lib/community/community-storage.ts (extend existing — see lines 113-128 for getCommunityMediaSignedUrl analog)
    - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md §"Storage Signed-URL (60-min TTL)"
  </read_first>
  <behavior>
    - Export `DM_ATTACHMENT_MAX_BYTES = 5 * 1024 * 1024` constant (5 MB per D-09; DIFFERENT from existing COMMUNITY_MEDIA_MAX_BYTES which is 10 MB)
    - Export `getDmAttachmentSignedUrl(path: string): Promise<{ url: string } | { error: string }>` that calls `supabase.storage.from('dm-attachments').createSignedUrl(path, 3600)` (60-min TTL per D-09)
    - Returns `{ error: 'network' }` on any error or null data (consistent with existing community-storage error pattern)
    - Reuses existing `COMMUNITY_MEDIA_MIMES` export — DM attachments use the same MIME whitelist; do NOT duplicate the constant
  </behavior>
  <action>
    In `leanshot/src/lib/community/community-storage.ts`, locate the existing `COMMUNITY_MEDIA_MAX_BYTES` export. Below it, add `export const DM_ATTACHMENT_MAX_BYTES = 5 * 1024 * 1024;` with a comment `// 5 MB per D-09 — DIFFERENT from community-media (10 MB)`.

    Locate the existing `getCommunityMediaSignedUrl` helper (around lines 113-128). Below it, add `getDmAttachmentSignedUrl` following the same Result shape (`{ url: string } | { error: string }`) — only differences: bucket id `'dm-attachments'` and TTL `3600` (vs 60 for media). Reuse the same try/catch + null-data-guard pattern.

    Do NOT introduce a separate DM MIME list — reuse `COMMUNITY_MEDIA_MIMES` for both buckets (per CONTEXT D-09 — same MIME set).
  </action>
  <acceptance_criteria>
    - `grep -c "DM_ATTACHMENT_MAX_BYTES" leanshot/src/lib/community/community-storage.ts` returns ≥1
    - `grep -c "getDmAttachmentSignedUrl" leanshot/src/lib/community/community-storage.ts` returns ≥1
    - `grep -c "'dm-attachments'" leanshot/src/lib/community/community-storage.ts` returns ≥1
    - `cd leanshot && npx tsc -p tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test $(grep -c "DM_ATTACHMENT_MAX_BYTES" leanshot/src/lib/community/community-storage.ts) -ge 1 && test $(grep -c "getDmAttachmentSignedUrl" leanshot/src/lib/community/community-storage.ts) -ge 1 && cd leanshot && npx tsc -p tsconfig.app.json --noEmit && echo "OK"</automated>
  </verify>
  <done>Helper + constant exported; TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Storage RLS | Authenticated user uploads/reads dm-attachments objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Tampering (path traversal) | dm-attachments INSERT RLS | mitigate | EXISTS dm_threads JOIN with `::uuid` cast on path[0] fails closed if path is malformed or uploader is not a thread participant (RESEARCH Pitfall 3) |
| T-45-03 | Information Disclosure | dm-attachments SELECT RLS | mitigate | Same participant-JOIN predicate on SELECT; 60-min signed URL TTL bounds replay window |
</threat_model>

<verification>
- Bucket creation idempotent (ON CONFLICT (id) DO NOTHING)
- 3 RLS policies wrapped in DO $$ if not exists guards
- No use of `(foldername)[1] = auth.uid()` (the community-media shape)
- TypeScript helper compiles
</verification>

<success_criteria>
- Migration creates dm-attachments bucket with 5 MB cap + correct MIME whitelist
- All 3 storage policies use dm_threads JOIN (or direct_messages JOIN for DELETE)
- community-storage.ts exports `getDmAttachmentSignedUrl` + `DM_ATTACHMENT_MAX_BYTES`
</success_criteria>

<output>
After completion, create `.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-03-SUMMARY.md` documenting: bucket config, 3 policy names, helper function signature, and confirmation that path[0]::uuid cast is in place.
</output>
