---
phase: 45
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270727000004_p45_notification_widening.sql
  - supabase/functions/_shared/email-router.ts
  - supabase/functions/_shared/email-templates/community-dm-new.ts
  - supabase/functions/_shared/email-templates/community-admin-report-digest.ts
  - supabase/functions/notification-send/index.ts
  - supabase/functions/notify-community/index.ts
autonomous: true
requirements:
  - COMMUNITY-08
  - COMMUNITY-09
must_haves:
  truths:
    - "Notification CHECK constraints on 4 tables accept 'community-dm' and 'community-admin-report' categories"
    - "notification_category_config has rows for both new categories with correct caps + defaults"
    - "_shared/email-router.ts EmailTemplate union includes 'community_dm_new' and 'community_admin_report_digest'"
    - "notification-send VALID_CATEGORIES includes both new categories"
    - "notify-community Edge Fn handles kind 'dm_new' with single-recipient fan-out"
    - "Two new email template files exist with subject() + render() exports"
    - "ALL of the above land in a SINGLE git commit (atomicity per Phase 44 44-02 lesson + memory feedback_planner_missed_status_enum_widening)"
  artifacts:
    - path: "supabase/migrations/20270727000004_p45_notification_widening.sql"
      provides: "4 CHECK widenings + 2 category_config UPSERTs in one transaction"
      contains: "community-dm"
    - path: "supabase/functions/_shared/email-templates/community-dm-new.ts"
      provides: "DM email template (subject + render)"
      exports: ["subject", "render"]
    - path: "supabase/functions/_shared/email-templates/community-admin-report-digest.ts"
      provides: "Admin digest email template"
      exports: ["subject", "render"]
  key_links:
    - from: "supabase/functions/_shared/email-router.ts"
      to: "community-dm-new.ts + community-admin-report-digest.ts"
      via: "import * as + subjectFor switch arms"
      pattern: "case 'community_dm_new'"
    - from: "supabase/functions/notify-community/index.ts"
      to: "callNotificationSend"
      via: "kind 'dm_new' handler"
      pattern: "kind === 'dm_new'"
---

<objective>
Atomically widen the notification CHECK constraints on 4 tables + ship 2 new email templates + extend email-router + notify-community + notification-send VALID_CATEGORIES — ALL in one plan / one commit. This is the highest-risk silent-failure surface in Phase 45 (per memory `feedback_planner_missed_status_enum_widening` + Phase 44 44-02 lesson + RESEARCH Pitfall 1). Any split = CHECK violation 23514 at production runtime when `dm-create-thread` calls `notify-community kind='dm_new'`.

Purpose: Wave 0 foundation for plan 45-04 (dm-create-thread) and 45-05 (community-admin-report-digest). Both depend on the notification surface being widened to accept the new categories.
Output: 1 migration file + 2 email template files + 3 extended files (email-router, notification-send, notify-community).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-RESEARCH.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md

# Primary analog — copy verbatim, extend the category list
@supabase/migrations/20270720000004_p44_notification_community.sql

# Code analogs
@supabase/functions/_shared/email-router.ts
@supabase/functions/_shared/email-templates/community-mention.ts
@supabase/functions/notification-send/index.ts
@supabase/functions/notify-community/index.ts
</context>

<interfaces>
<!-- After this plan, downstream Wave 1 Edge Fns can: -->

From `supabase/functions/_shared/email-router.ts` (extended):
```typescript
export type EmailTemplate =
  | 'community_mention'
  | 'community_reply'
  | 'community_dm_new'              // NEW
  | 'community_admin_report_digest' // NEW
  | ...;
```

From `supabase/functions/notify-community/index.ts` (extended):
```typescript
interface DmNewBody {
  kind: 'dm_new';
  thread_id: string;
  sender_user_id: string;
  sender_handle: string;
  recipient_user_id: string;
  body_excerpt: string;  // ≤80 chars post-dompurify
}
type NotifyBody = MentionBody | ReplyBody | DmNewBody;
```

From email templates:
- `community-dm-new.ts` exports `subject(vars) → string`, `render(vars) → html string`. Vars: `sender_handle`, `body_excerpt`, `thread_url`.
- `community-admin-report-digest.ts` exports same shape. Vars: `open_count`, `by_type` (array `{target_type, count}`), `digest_date`, `admin_url`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomic CHECK-widening migration + category_config UPSERT</name>
  <files>supabase/migrations/20270727000004_p45_notification_widening.sql</files>
  <read_first>
    - supabase/migrations/20270720000004_p44_notification_community.sql (primary analog — copy VERBATIM begin/commit wrapper + structure)
    - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md §"supabase/migrations/<ts>_p45_notification_widening.sql"
  </read_first>
  <behavior>
    - Single `begin;/commit;` transaction wrapping all DDL + DML
    - 4x `alter table … drop constraint if exists … add constraint … check (category in (...all-existing-plus-new...))` on: notification_settings, notification_category_config, user_notifications, notification_dismissal_state
    - Each ALTER includes existing categories from Phase 44 ('dose-reminders', 'ai-insights', 'clinic-alerts', 'billing', 'marketing', 'community-mentions', 'community-replies') plus the 2 new ones ('community-dm', 'community-admin-report')
    - 2x UPSERT into notification_category_config: ('community-dm', daily_cap=10, weekly_cap=null, urgent_escalation=false, push_default=true, email_default=true, in_app_default=true) and ('community-admin-report', daily_cap=1, weekly_cap=null, urgent_escalation=false, push_default=false, email_default=true, in_app_default=false) — using INSERT … ON CONFLICT (category) DO UPDATE SET … updated_at = now() (memory reference_state_counter_table_needs_upsert_on_event)
  </behavior>
  <action>
    Migration filename: `supabase/migrations/20270727000004_p45_notification_widening.sql`. Open analog `20270720000004_p44_notification_community.sql` and copy its `begin;/commit;` wrapper + 4-table ALTER pattern verbatim. Replace the category list with the full 9-item list (7 existing + 'community-dm' + 'community-admin-report'). The order of categories inside the CHECK must match the analog's order with the 2 new ones appended at the end.

    For category_config seed: use `insert into public.notification_category_config (...) values (...) on conflict (category) do update set ... updated_at = now()` (NOT bare INSERT — memory reference_state_counter_table_needs_upsert_on_event). Set `urgent_escalation=false` for both. `daily_cap` per Pattern 6 in RESEARCH (community-dm=10, community-admin-report=1).

    All 6 statements (4 ALTERs + 2 UPSERTs) MUST be inside the SAME `begin;/commit;` block. Splitting them defeats the atomicity guarantee.
  </action>
  <acceptance_criteria>
    - File exists with name `20270727000004_p45_notification_widening.sql`
    - `grep -c "begin;" supabase/migrations/20270727000004_p45_notification_widening.sql` returns 1
    - `grep -c "commit;" supabase/migrations/20270727000004_p45_notification_widening.sql` returns 1
    - `grep -cE "alter table public\\.(notification_settings|notification_category_config|user_notifications|notification_dismissal_state)" supabase/migrations/20270727000004_p45_notification_widening.sql` returns ≥4
    - `grep -c "'community-dm'" supabase/migrations/20270727000004_p45_notification_widening.sql` returns ≥5 (4 CHECKs + 1 UPSERT)
    - `grep -c "'community-admin-report'" supabase/migrations/20270727000004_p45_notification_widening.sql` returns ≥5
    - `grep -c "on conflict (category) do update" supabase/migrations/20270727000004_p45_notification_widening.sql` returns ≥2
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test $(grep -c "begin;" supabase/migrations/20270727000004_p45_notification_widening.sql) -eq 1 && test $(grep -c "commit;" supabase/migrations/20270727000004_p45_notification_widening.sql) -eq 1 && grep -c "'community-dm'" supabase/migrations/20270727000004_p45_notification_widening.sql | awk '$1 >= 5 {print "OK"; exit 0} {print "FAIL"; exit 1}' && grep -c "'community-admin-report'" supabase/migrations/20270727000004_p45_notification_widening.sql | awk '$1 >= 5 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
  </verify>
  <done>Atomic migration ships 4 CHECK widenings + 2 category_config UPSERTs in one transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two new email template files + email-router union extension</name>
  <files>supabase/functions/_shared/email-templates/community-dm-new.ts, supabase/functions/_shared/email-templates/community-admin-report-digest.ts, supabase/functions/_shared/email-router.ts</files>
  <read_first>
    - supabase/functions/_shared/email-templates/community-mention.ts (primary analog — copy verbatim structure including escapeHtml helper at lines 19-26)
    - supabase/functions/_shared/email-router.ts (extend EmailTemplate union + add 2 imports + 2 subjectFor + 2 renderTemplate arms)
    - 45-PATTERNS.md §"supabase/functions/_shared/email-templates/community-dm-new.ts"
  </read_first>
  <behavior>
    - community-dm-new.ts: subject(vars) returns `New message from @${escapeHtml(vars.sender_handle)}`; render(vars) returns HTML matching the community-mention.ts layout (inline-CSS card + CTA button + notification footer). Vars consumed: sender_handle, body_excerpt, thread_url
    - community-admin-report-digest.ts: subject(vars) returns `[LeanShot Admin] ${vars.open_count} open community reports — ${vars.digest_date}`; render(vars) returns HTML with a small `<table>` rendering `by_type` rows (target_type, count) + a CTA pointing to `admin_url`. Vars consumed: open_count, by_type, digest_date, admin_url
    - email-router.ts: union extended with `'community_dm_new'` and `'community_admin_report_digest'` after `| 'community_reply'`; 2 new `import * as` lines after community-reply import; 2 new `case` arms in `subjectFor` switch; 2 new `case` arms in `renderTemplate` switch
    - escapeHtml ALL user-supplied vars in both templates (XSS defense per RESEARCH §Security Domain)
    - Both templates have `phi: false` semantics (Resend channel, not SES) per CONTEXT canonical_refs §Phase 25
  </behavior>
  <action>
    Create `supabase/functions/_shared/email-templates/community-dm-new.ts` by copying `community-mention.ts` verbatim and replacing: (a) subject template literal to `New message from @${escapeHtml(sender_handle)}`, (b) render() body to swap the mention-specific copy ("mentioned you in a post") for DM copy ("sent you a direct message" + body_excerpt blockquote), (c) CTA button text "View thread" → thread_url.

    Create `supabase/functions/_shared/email-templates/community-admin-report-digest.ts` similarly. Subject: `[LeanShot Admin] ${open_count} open community reports — ${digest_date}`. Render: inline-CSS card containing a small table with header row "Target type | Open count" and body rows iterated from `vars.by_type` (array of `{target_type, count}`). CTA button "Review queue" → admin_url (Phase 48 placeholder `/admin/community/reports`).

    Both templates use the project's existing `escapeHtml()` helper (copy from community-mention.ts lines 19-26 verbatim). Do NOT introduce a new HTML sanitization library.

    Extend `supabase/functions/_shared/email-router.ts` per 45-PATTERNS.md §"Extension: _shared/email-router.ts":
    1. Union extension: add `| 'community_dm_new'` and `| 'community_admin_report_digest'` after `| 'community_reply'`
    2. Imports: add `import * as communityDmNew from './email-templates/community-dm-new.ts';` and `import * as communityAdminDigest from './email-templates/community-admin-report-digest.ts';` after the community-reply import
    3. subjectFor switch arms after the community_reply case: `case 'community_dm_new': return communityDmNew.subject(vars);` and `case 'community_admin_report_digest': return communityAdminDigest.subject(vars);`
    4. renderTemplate switch arms (same pattern)
  </action>
  <acceptance_criteria>
    - File `supabase/functions/_shared/email-templates/community-dm-new.ts` exists; exports `subject` and `render` functions
    - File `supabase/functions/_shared/email-templates/community-admin-report-digest.ts` exists; exports `subject` and `render` functions
    - `grep -c "community_dm_new" supabase/functions/_shared/email-router.ts` returns ≥3 (union + subjectFor + renderTemplate)
    - `grep -c "community_admin_report_digest" supabase/functions/_shared/email-router.ts` returns ≥3
    - `grep -c "escapeHtml" supabase/functions/_shared/email-templates/community-dm-new.ts` returns ≥2 (used inside subject + render)
    - `grep -c "escapeHtml" supabase/functions/_shared/email-templates/community-admin-report-digest.ts` returns ≥2
    - Edge Fn type-check passes: `cd supabase/functions && $HOME/.deno/bin/deno check --no-check=remote _shared/email-router.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test -f supabase/functions/_shared/email-templates/community-dm-new.ts && test -f supabase/functions/_shared/email-templates/community-admin-report-digest.ts && grep -c "community_dm_new" supabase/functions/_shared/email-router.ts | awk '$1 >= 3 {print "OK"; exit 0} {print "FAIL"; exit 1}' && grep -c "community_admin_report_digest" supabase/functions/_shared/email-router.ts | awk '$1 >= 3 {print "OK"; exit 0} {print "FAIL"; exit 1}'</automated>
  </verify>
  <done>Two new templates exist; email-router has union + imports + subjectFor + renderTemplate arms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend notify-community kind 'dm_new' handler + notification-send VALID_CATEGORIES</name>
  <files>supabase/functions/notify-community/index.ts, supabase/functions/notification-send/index.ts</files>
  <read_first>
    - supabase/functions/notify-community/index.ts (the existing handler — extend NotifyBody union + add handler arm)
    - supabase/functions/notification-send/index.ts (lines 169–179 — VALID_CATEGORIES Set extension)
    - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md §"Extension: notify-community/index.ts"
  </read_first>
  <behavior>
    - notify-community: NotifyBody union extended with DmNewBody (fields per <interfaces> block); handler arm for `body.kind === 'dm_new'` calls `callNotificationSend(body.recipient_user_id, 'community-dm', { sender_handle, body_excerpt, thread_url: '/community/dm/${thread_id}' })`; returns `jsonResponse(200, { fanout_count: 1 })`
    - notify-community auth check for dm_new path: sender JWT sub must equal body.sender_user_id (mirrors T-44-08 identity-mismatch pattern)
    - notification-send: VALID_CATEGORIES Set extended with 'community-dm' and 'community-admin-report' (lines 169–179 — append after 'community-replies')
  </behavior>
  <action>
    In `supabase/functions/notify-community/index.ts`: locate the `type NotifyBody = MentionBody | ReplyBody;` declaration and extend to `type NotifyBody = MentionBody | ReplyBody | DmNewBody;`. Add the DmNewBody interface immediately above with exact fields from the `<interfaces>` block. Add a handler arm in the body-switch matching `body.kind === 'dm_new'` per 45-PATTERNS.md §"Dm fan-out handler" — single-recipient, no N-mention loop. The thread_url is constructed server-side: `\`https://app.leanshot.app/community/dm/\${body.thread_id}\``.

    For dual-auth on dm_new: when path B (user JWT), the `body.sender_user_id` claim MUST equal `data.user.id`; otherwise return jsonError(403, 'identity_mismatch'). This mirrors the existing T-44-08 mention path identity check.

    In `supabase/functions/notification-send/index.ts`: locate the VALID_CATEGORIES Set (lines 169–179 per RESEARCH §Pattern 6 #6 + 45-PATTERNS.md §"Extension: notification-send/index.ts"). Append `'community-dm'` and `'community-admin-report'` to the Set entries, after `'community-replies'`. Maintain alphabetic-or-domain-grouped ordering per existing style.

    Do NOT introduce a separate Fn for dm_new — extending notify-community keeps the single-source pattern.
  </action>
  <acceptance_criteria>
    - `grep -c "DmNewBody" supabase/functions/notify-community/index.ts` returns ≥2 (interface + union)
    - `grep -c "kind === 'dm_new'" supabase/functions/notify-community/index.ts` returns ≥1
    - `grep -c "identity_mismatch" supabase/functions/notify-community/index.ts` returns ≥1 (existing or new on dm_new path)
    - `grep -c "'community-dm'" supabase/functions/notification-send/index.ts` returns ≥1
    - `grep -c "'community-admin-report'" supabase/functions/notification-send/index.ts` returns ≥1
    - Existing notify-community Deno tests still pass: `cd supabase/functions/notify-community && $HOME/.deno/bin/deno test --no-check --allow-env index.test.ts` exits 0 (no regressions from extension)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c "DmNewBody" supabase/functions/notify-community/index.ts | awk '$1 >= 2 {print "OK"; exit 0} {print "FAIL"; exit 1}' && test $(grep -c "kind === 'dm_new'" supabase/functions/notify-community/index.ts) -ge 1 && test $(grep -c "'community-dm'" supabase/functions/notification-send/index.ts) -ge 1 && test $(grep -c "'community-admin-report'" supabase/functions/notification-send/index.ts) -ge 1 && echo "OK"</automated>
  </verify>
  <done>notify-community handles dm_new; notification-send accepts the 2 new categories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge Fn→Postgres | notify-community writes to notification tables — CHECK constraints enforce category whitelist |
| client→Edge Fn | notify-community accepts user JWT for dm_new path; identity verified against body.sender_user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02 | Spoofing | notify-community dm_new path | mitigate | Dual-auth: user JWT.sub must equal body.sender_user_id; service-role path bypasses (cron-only) |
| T-45-05 | Tampering (XSS) | email template render() | mitigate | escapeHtml() applied to all user-supplied vars (sender_handle, body_excerpt) before injecting into HTML; reuses community-mention.ts pattern |
</threat_model>

<verification>
- Single git commit contains all 6 file changes (atomic)
- Notification CHECK constraints accept 'community-dm' and 'community-admin-report'
- Email-router union + imports + subjectFor + renderTemplate all extended
- notify-community handles kind='dm_new'; notification-send VALID_CATEGORIES extended
</verification>

<success_criteria>
- 1 migration + 2 email templates + 3 extended files commit together
- No deferred component (atomicity per memory feedback_planner_missed_status_enum_widening + Phase 44 44-02)
</success_criteria>

<output>
After completion, create `.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-02-SUMMARY.md` documenting: migration filename, 2 new template filenames, exact line ranges modified in email-router.ts / notify-community/index.ts / notification-send/index.ts, and confirmation that ALL changes shipped in one git commit (cite commit SHA).
</output>
