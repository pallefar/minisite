---
phase: 31
plan: 05
type: execute
wave: 2
depends_on: [31-00, 31-00b, 31-01, 31-02, 31-03, 31-04]
files_modified:
  - src/components/clinic/settings/ClinicSettingsPage.tsx
  - src/components/clinic/settings/BrandingTab.tsx
  - src/components/clinic/settings/OnboardingTab.tsx
  - src/components/clinic/settings/RoleEditorModal.tsx
  - src/lib/wcag-contrast.ts
  - src/lib/__tests__/wcag-contrast.test.ts
  - src/lib/__tests__/role-editor-modal.test.ts
  # change_member_role SECDEF migration is owned by 31-04 (closes plan-checker BLOCKER 1+3 — push-race risk) — see 31-04 frontmatter files_modified
autonomous: true
requirements: [ORG-11, ORG-12, ORG-13]
tags: [white-label, role-editor, onboarding-builder, wcag, branding-tab, clinic-settings]
must_haves:
  truths:
    - "Clinic owner sees a BrandingTab in /clinic/{slug}/settings/branding that lets them set logo, favicon, 4 oklch colors, 2 fonts, and radius scale."
    - "Save in BrandingTab is disabled while client-side WCAG meter fails on text/bg (≥4.5) or primary/bg (≥3.0); on Save success a 'Branding saved' toast appears."
    - "Server-side WCAG rejection (CONTRAST_TEXT_BG_FAIL / CONTRAST_PRIMARY_BG_FAIL) surfaces inline beside the failing pair, not as a generic toast."
    - "Clinic owner sees an OnboardingTab in /clinic/{slug}/settings/onboarding with a SortableTreePanel<OnboardingStepNode> step list and a patient preview pane."
    - "Owner can reorder steps via drag/keyboard, toggle Skippable on allowed steps, edit welcome+intro_card content via modal, remove non-mandatory steps, and Save → version N inserted with previous active flipped to is_active=false."
    - "Owner can pick a prior version from the Version history Select and click Restore this version → activate_onboarding_flow_version RPC fires + toast 'Flow version N restored'."
    - "Clinician/staff who lack 'onboarding.edit' see a read-only 'Current patient onboarding flow' view (no drag handles, no edit/remove/skip controls)."
    - "Clinician/staff who lack 'branding.edit' do NOT see the Branding nav entry at all."
    - "RoleEditorModal shows a 12-permission × 3-role matrix preview table; the column for the currently-selected role is highlighted."
    - "RoleEditorModal disables Confirm with tooltip 'An organization must have at least one owner' when demoting the final owner."
    - "Confirm calls change_member_role SECDEF; on success toast '{Name}'s role changed to {Role}'."
    - "Server-side change_member_role SECDEF rejects with LAST_OWNER_DEMOTE_DENIED if last owner is demoted even when client guard is bypassed."
    - "All admin mutations (save_org_branding, save_org_onboarding_flow, activate_onboarding_flow_version, change_member_role) write to audit_logs via log_admin_action."
    - "Remove logo / remove favicon / remove step actions use optimistic + 6s undo toast (no blocking confirm dialog) per D-16 UI-SPEC §Destructive-action."
    - "Bundle: clinic chunk stays under CLINIC_CEILING=50000 gz after BrandingTab + OnboardingTab + RoleEditorModal expansion land."
  artifacts:
    - path: "src/components/clinic/settings/BrandingTab.tsx"
      provides: "Branding form + live preview + WCAG meter + upload zones; consumes save_org_branding RPC + branding-asset-upload-url Edge Fn"
      contains: "save_org_branding"
    - path: "src/components/clinic/settings/OnboardingTab.tsx"
      provides: "Step builder + patient preview + version history; consumes save_org_onboarding_flow + activate_onboarding_flow_version"
      contains: "SortableTreePanel"
    - path: "src/components/clinic/settings/RoleEditorModal.tsx"
      provides: "Existing modal expanded with 12-key matrix table + last-owner guard + change_member_role caller"
      contains: "change_member_role"
    - path: "src/components/clinic/settings/ClinicSettingsPage.tsx"
      provides: "NAV array updated to register branding + onboarding tabs with visibleWhen gates"
      contains: "branding"
    - path: "src/lib/wcag-contrast.ts"
      provides: "TypeScript port of SQL _compute_wcag_contrast helper (L^2.2 approximation per RESEARCH Finding 2)"
      exports: ["computeWcagContrast", "parseOklch"]
    # NOTE: change_member_role SECDEF moved to plan 31-04 (closes plan-checker BLOCKER 1+3 — push-race avoidance; single push site at 31-04 covers all P31 server-side mutations). 31-05 consumes the SECDEF via supabase.rpc() in Task 2.
    - path: "src/lib/__tests__/wcag-contrast.test.ts"
      provides: "Parity unit tests between TS port and SQL helper on sample input pairs"
      contains: "computeWcagContrast"
    - path: "src/lib/__tests__/role-editor-modal.test.ts"
      provides: "Tests for last-owner guard client behavior + matrix table render"
      contains: "last owner"
  key_links:
    - from: "src/components/clinic/settings/BrandingTab.tsx"
      to: "supabase Edge Function branding-asset-upload-url (31-02)"
      via: "supabase.functions.invoke('branding-asset-upload-url', { body: { p_org_id, p_kind } }) then PUT to returned presigned URL"
      pattern: "branding-asset-upload-url"
    - from: "src/components/clinic/settings/BrandingTab.tsx"
      to: "save_org_branding SECDEF (31-02)"
      via: "supabase.rpc('save_org_branding', { p_org_id, p_tokens })"
      pattern: "save_org_branding"
    - from: "src/components/clinic/settings/BrandingTab.tsx"
      to: "src/lib/wcag-contrast.ts"
      via: "import { computeWcagContrast } and call on each color field change to gate Save button"
      pattern: "computeWcagContrast"
    - from: "src/components/clinic/settings/OnboardingTab.tsx"
      to: "src/components/ui/SortableTreePanel.tsx (31-00b)"
      via: "import SortableTreePanel and render with items=steps, getId=step.id, renderItem=stepRow, onReorder=setSteps"
      pattern: "SortableTreePanel"
    - from: "src/components/clinic/settings/OnboardingTab.tsx"
      to: "src/types/onboarding-step.ts (31-04)"
      via: "import type { OnboardingStepNode, StepType }"
      pattern: "OnboardingStepNode"
    - from: "src/components/clinic/settings/OnboardingTab.tsx"
      to: "save_org_onboarding_flow + activate_onboarding_flow_version SECDEFs (31-04)"
      via: "supabase.rpc('save_org_onboarding_flow', { p_org_id, p_steps }) / supabase.rpc('activate_onboarding_flow_version', { p_org_id, p_flow_id })"
      pattern: "save_org_onboarding_flow"
    - from: "src/components/clinic/settings/RoleEditorModal.tsx"
      to: "change_member_role SECDEF (owned by plan 31-04 — moved there to close plan-checker BLOCKER 1+3 push race)"
      via: "supabase.rpc('change_member_role', { p_org_id, p_user_id, p_role })"
      pattern: "change_member_role"
    - from: "src/components/clinic/settings/RoleEditorModal.tsx"
      to: "ROLE_PERMISSIONS const in src/lib/org.ts (post-31-01 expansion)"
      via: "import { _ROLE_PERMISSIONS_FOR_TEST as ROLE_PERMISSIONS } or named export to render the 12×3 matrix table"
      pattern: "ROLE_PERMISSIONS"
    - from: "src/components/clinic/settings/ClinicSettingsPage.tsx"
      to: "BrandingTab + OnboardingTab"
      via: "NAV array entries with id, label, Icon, visibleWhen; lazy mount inside route switch"
      pattern: "branding|onboarding"
    - from: "change_member_role SECDEF"
      to: "log_admin_action (Phase 24)"
      via: "perform log_admin_action('org_member.role_changed', jsonb_build_object(...))"
      pattern: "log_admin_action"
    - from: "change_member_role SECDEF"
      to: "has_permission + get_caller_role (31-01)"
      via: "if not has_permission(get_caller_role(p_org_id), 'members.role.edit') then raise insufficient_privilege"
      pattern: "has_permission"
---

<objective>
Land the three new clinic-admin UI surfaces (BrandingTab, OnboardingTab, expanded RoleEditorModal) on top of the server-side foundations shipped by Waves 0–2 (31-00 enum rename, 31-00b SortableTreePanel, 31-01 has_permission matrix, 31-02 branding schema + Edge Fn, 31-03 brand tokens at first paint, 31-04 onboarding schema + SECDEFs + `change_member_role` SECDEF). Consume `change_member_role` SECDEF (owned by 31-04 — moved there to avoid the Wave-2 push race per plan-checker BLOCKER 1+3) via `supabase.rpc()` from the expanded RoleEditorModal; UI ships a client-side last-owner guard as UX sugar on top of 31-04's server-side enforcement. Register both new tabs in `ClinicSettingsPage` NAV with permission-gated visibility. Add a client-side WCAG helper that mirrors the SQL `_compute_wcag_contrast` so Save is never UX-disabled at submit time.

Purpose: This plan is the UI consumer of every other plan in Phase 31. After it lands, an owner can theme their clinic, customize patient onboarding, and reassign roles — completing the clinic B2B story for ORG-11/12/13.

Output: 3 component files (1 NEW `BrandingTab`, 1 NEW `OnboardingTab`, 1 expanded `RoleEditorModal`), 1 NAV update in `ClinicSettingsPage`, 1 NEW `wcag-contrast.ts` helper, 2 NEW vitest files (parity + role-editor-modal). NOTE: `change_member_role` SECDEF migration was moved to plan 31-04 to close plan-checker BLOCKER 1+3 (Wave-2 push race avoidance); 31-05 consumes the SECDEF via `supabase.rpc()` after 31-04's BLOCKING push lands it to live DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-CONTEXT.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-UI-SPEC.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-RESEARCH.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-PLAN-OUTLINE.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-00-PLAN.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-00b-PLAN.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-01-PLAN.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-02-PLAN.md
@src/components/clinic/settings/ClinicSettingsPage.tsx
@src/components/clinic/settings/MembersTab.tsx
@src/components/clinic/settings/RoleEditorModal.tsx
@src/components/clinic/settings/ClinicRankingWeightsForm.tsx
@src/lib/org.ts
@src/lib/supabase.ts
@src/hooks/useReducedMotion.ts
@src/hooks/useToast.ts

<interfaces>
<!-- These are the EXACT contracts the executor consumes. Do NOT re-explore the codebase to discover them. -->

<!-- From src/lib/org.ts (post-31-01 expansion — same exports, expanded internals) -->
```typescript
export type OrgRole = 'owner' | 'clinician' | 'staff';
export function getCurrentOrgId(): string;            // throws if no org in store
export function getCurrentOrgIdOrNull(): string | null;
export function surfaceCheck(permission: string): boolean;   // reads current org role from store; consults ROLE_PERMISSIONS
export function useCurrentOrg(): { orgId: string | null; role: OrgRole | null; slug: string | null };
export const _ROLE_PERMISSIONS_FOR_TEST: Record<OrgRole, ReadonlySet<string>>;   // re-export of internal const
```

<!-- From src/components/ui/SortableTreePanel.tsx (31-00b new) -->
```typescript
export interface SortableTreePanelProps<T> {
  items: T[];
  getId: (item: T) => string;
  renderItem: (item: T, dragHandle: React.ReactNode) => React.ReactNode;
  onReorder: (next: T[]) => void;
  announceItemLabel?: (item: T) => string;
}
export function SortableTreePanel<T>(props: SortableTreePanelProps<T>): JSX.Element;
```

<!-- From src/types/onboarding-step.ts (31-04 new) -->
```typescript
export type StepType =
  | 'welcome' | 'intro_card' | 'medication' | 'goals'
  | 'body_stats' | 'consent' | 'doctor_invite' | 'tour';
export interface OnboardingStepNode {
  id: string;
  type: StepType;
  skip?: boolean;
  custom?: { title?: string; body?: string; image_url?: string };
}
```

<!-- From 31-04 SECDEFs (signatures) -->
```sql
save_org_onboarding_flow(p_org_id uuid, p_steps jsonb) returns uuid       -- returns the NEW FLOW ID (uuid); consumer fetches version via subsequent SELECT or shows toast with id-derived label per UI-SPEC
activate_onboarding_flow_version(p_org_id uuid, p_flow_id uuid) returns void -- no return value; consumer shows "Flow version N restored" toast after fetching version of p_flow_id
mark_onboarding_complete() returns void                                    -- patient-side; not used in this plan
```

<!-- From 31-02 SECDEF + Edge Function -->
```
save_org_branding(p_org_id uuid, p_tokens jsonb) returns void
   raises CONTRAST_TEXT_BG_FAIL or CONTRAST_PRIMARY_BG_FAIL on rejection
   raises insufficient_privilege on permission miss
Edge Fn: POST /functions/v1/branding-asset-upload-url
   body: { p_org_id: string, p_kind: 'logo' | 'favicon' | 'intro_card' }
   returns: { upload_url: string, public_url: string, expires_in: number }
```

<!-- From src/components/ui/Modal.tsx existing primitive -->
```typescript
export interface ModalProps { open: boolean; onClose: () => void; title: string; size?: 'sm'|'md'|'lg'|'xl'; mobileFullscreen?: boolean; children: React.ReactNode; }
```

<!-- From src/components/ui/{Button,Card,Input,Badge,Pill,Select,Toast} existing primitives -->
All consumed via `import { Button, IconButton } from '@/components/ui/Button'` etc. Never restyle at component level.

<!-- From src/lib/supabase.ts -->
```typescript
export const supabase: SupabaseClient; // singleton
```

<!-- From src/hooks/useToast.ts -->
```typescript
export function useToast(): (message: string, tone?: 'success'|'error'|'info') => void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wcag-contrast.ts TS port + parity vitest (change_member_role SECDEF moved to 31-04)</name>
  <files>src/lib/wcag-contrast.ts, src/lib/__tests__/wcag-contrast.test.ts</files>
  <behavior>
    - (change_member_role SECDEF behavior owned by 31-04 — see 31-04 Task 2; this plan CONSUMES it via supabase.rpc('change_member_role', ...) in Task 2)
    - wcag-contrast.ts exports computeWcagContrast(oklchA: string, oklchB: string): number that mirrors the SQL helper from 31-02 using the same L^2.2 approximation. Also exports parseOklch(s: string): { L: number; C: number; H: number } | null for swatch rendering (graceful null on invalid input).
    - Parity test: for at least 6 sample input pairs (covering passing-text/bg, failing-text/bg, passing-primary/bg, failing-primary/bg, identical colors, max-contrast black-on-white), |TS_value - SQL_value| < 0.05 against expected pre-computed values committed inline in the test.
  </behavior>
  <action>The `change_member_role` SECDEF migration has MOVED to plan 31-04 (closes plan-checker BLOCKER 1+3 — push race avoidance). 31-04 Task 2 now owns the SECDEF body (gate via has_permission, last-owner guard, UPDATE, log_admin_action). This plan's Task 2 CONSUMES the SECDEF via supabase.rpc('change_member_role', ...) after 31-04's BLOCKING push has landed it to the live DB.

Create `src/lib/wcag-contrast.ts`: implement TS port of the SQL `_compute_wcag_contrast` from 31-02 (L^2.2 approximation; consume the EXACT formula committed in `supabase/migrations/<ts>_p31_02_branding_expand.sql` — read the migration file as source of truth). Export `computeWcagContrast(oklchA: string, oklchB: string): number` returning the ratio (rounded to 2 decimals). Export `parseOklch(s: string): { L: number; C: number; H: number } | null` using a tight regex `/^oklch\(\s*([0-9.]+)%?\s+([0-9.]+)\s+([0-9.]+)\s*\)$/` (researcher-recommended shape per 31-CONTEXT D-05). Both functions must be pure (no side effects, no DOM access) so they can run in vitest without jsdom. Return number with up to 2 decimal precision.

Create `src/lib/__tests__/wcag-contrast.test.ts`: 6+ test cases asserting parity within 0.05 absolute tolerance vs precomputed SQL values. Sample pairs: ('oklch(1 0 0)', 'oklch(0 0 0)') expects ~21.0, ('oklch(0.5 0 0)', 'oklch(0.5 0 0)') expects ~1.0, etc. Also test parseOklch returns null on rgb()/hex inputs. NEVER use `it.fixme` per reference_vitest_skip_fixme; use `it.skip` with deferral note if needed.

Implementation note for last-owner guard: this is per D-13 (server is floor) and key_decisions #4 above. Client-side guard in RoleEditorModal task is UX sugar; this SECDEF is the security floor (T-31-05-01).</action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/wcag-contrast.test.ts && grep -q "computeWcagContrast" src/lib/wcag-contrast.ts && grep -q "parseOklch" src/lib/wcag-contrast.ts</automated>
  </verify>
  <done>wcag-contrast.ts exports computeWcagContrast + parseOklch as pure functions; parity vitest passes 6+ cases under 0.05 tolerance vs the SQL `_compute_wcag_contrast` helper shipped in 31-02's migration. (change_member_role SECDEF migration is owned by 31-04; no migration file written by this plan.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand RoleEditorModal with 12-key matrix + last-owner guard + change_member_role caller; register both new tabs in ClinicSettingsPage NAV; role-editor-modal vitest</name>
  <files>src/components/clinic/settings/RoleEditorModal.tsx, src/components/clinic/settings/ClinicSettingsPage.tsx, src/lib/__tests__/role-editor-modal.test.ts</files>
  <behavior>
    - RoleEditorModal renders a 12-row × 3-column table (rows: PERMISSION_LABELS keys from 31-01 expanded const; columns: Owner/Clinician/Staff) with Check (granted) / Minus (denied) icons. The currently-selected role's column is highlighted with bg-[var(--color-primary-soft)].
    - Confirm button label is dynamic: "Change to Owner" / "Change to Clinician" / "Change to Staff".
    - Client-side last-owner guard: when editing a member whose current role is 'owner' AND new role is NOT 'owner' AND the org has exactly 1 owner (query org_members with role='owner' count), Confirm is disabled and a tooltip reads "An organization must have at least one owner."
    - On Confirm click (allowed): calls supabase.rpc('change_member_role', { p_org_id, p_user_id, p_role }); on success toast "{Name}'s role changed to {Role}."; on LAST_OWNER_DEMOTE_DENIED error toast "An organization must have at least one owner." (server-floor message).
    - Body contains the line "Changes are logged to your workspace audit log." beneath the matrix.
    - Modal still respects role="dialog" aria-modal="true" via the existing <Modal> primitive.
    - ClinicSettingsPage NAV array gains two entries: { id: 'branding', label: 'Branding', Icon: Palette, visibleWhen: (perms) => perms['branding.edit'] === true } and { id: 'onboarding', label: 'Onboarding', Icon: ListOrdered } (no visibleWhen → always visible; clinician sees read-only view).
    - Route switch handles 'branding' and 'onboarding' tab IDs, lazy-mounting BrandingTab and OnboardingTab respectively (React.lazy to keep clinic chunk under 50 kB gz ceiling).
    - parseRoute regex updated to include 'branding' and 'onboarding' as valid tab strings.
    - permMap in ClinicSettingsPage gains 'branding.edit' and 'onboarding.edit' booleans sourced from surfaceCheck (per 31-01 ROLE_PERMISSIONS 12-key matrix).
    - Vitest covers: (a) matrix renders 12 rows × 3 cols; (b) selecting 'clinician' highlights the Clinician column; (c) last-owner demotion disables Confirm + shows tooltip; (d) non-last-owner demotion enables Confirm; (e) branding nav entry hidden when surfaceCheck('branding.edit') returns false.
  </behavior>
  <action>Expand `src/components/clinic/settings/RoleEditorModal.tsx`: This file currently implements a generic create/edit role modal for the Phase 9 custom-roles surface. For P31 we are EXTENDING the same file to also support the 3-role assignment use case per UI-SPEC §Surface 3 (per D-03). DO NOT delete the existing create/edit flow — that surface still lives. Add a NEW mode `'assign'` to the existing `mode` discriminated union, and add new props: `userId?: string` + `userName?: string` + `currentRole?: OrgRole` (used only in assign mode). When `mode === 'assign'`: render the 12-key matrix preview table sourced from `_ROLE_PERMISSIONS_FOR_TEST` (rename the re-export or add named export `ROLE_PERMISSIONS_MATRIX` in 31-01's `src/lib/org.ts` if not already named — per D-02 the matrix is canonically a Record<OrgRole, ReadonlySet<string>>). Use the PERMISSION_LABELS map from key_decisions #3 above for human labels. Use lucide-react icons Check (size=14, text-success) for granted and Minus (size=14, text-text-tertiary) for denied. Render the 12 rows with key order matching D-03 (members.invite, members.revoke, members.list, members.role.edit, settings.edit, branding.edit, onboarding.edit, roster.view, roster.thresholds.edit, alerts.ack, alerts.snooze, billing.view). Add a Select with options Owner / Clinician / Staff (values 'owner'|'clinician'|'staff'). Add note line "Changes are logged to your workspace audit log." in a 13px secondary-color paragraph.

Client last-owner guard: on modal open (in assign mode) fetch `select count(*) as c from org_members where org_id = {orgId} and role = 'owner'` via supabase-js; store in state `ownerCount`. Compute `isLastOwnerDemote = currentRole === 'owner' && selectedRole !== 'owner' && ownerCount <= 1`. Confirm button: disabled={isLastOwnerDemote || submitting}; if disabled due to last-owner: wrap in a `<span title="An organization must have at least one owner." aria-label="An organization must have at least one owner.">` so AT users hear the reason (Tooltip primitive may not exist; use native title attr + aria-label per CLAUDE.md a11y conventions). Confirm label: `Change to ${selectedRoleLabel}` (e.g., "Change to Clinician").

On Confirm: `const { error } = await supabase.rpc('change_member_role', { p_org_id: orgId, p_user_id: userId, p_role: selectedRole });`. If error.message.includes('LAST_OWNER_DEMOTE_DENIED') → toast("An organization must have at least one owner.", "error"). Else if error → toast generic. Else → toast(`${userName}'s role changed to ${selectedRoleLabel}.`, 'success') + call onSaved({ id: userId, name: userName }) + onClose().

Per anti-patterns: do NOT introduce a new color or font value; consume var(--color-*) tokens. The matrix column highlight uses `bg-[var(--color-primary-soft)]` (per UI-SPEC). All modals already have role="dialog" aria-modal="true" via existing Modal primitive.

Update `src/components/clinic/settings/ClinicSettingsPage.tsx`:
- Add imports: `Palette, ListOrdered` from 'lucide-react'.
- Update TabId union type: add 'branding' | 'onboarding'.
- Update `parseRoute`: include 'branding' and 'onboarding' in the validation list.
- Append two NAV entries: `{ id: 'branding', label: 'Branding', Icon: Palette, visibleWhen: (perms) => perms['branding.edit'] === true }` and `{ id: 'onboarding', label: 'Onboarding', Icon: ListOrdered }` (no visibleWhen — clinician sees read-only view inside the tab).
- Expand permMap: `'branding.edit': surfaceCheck('branding.edit'), 'onboarding.edit': surfaceCheck('onboarding.edit')` (these read via the post-31-01 expanded ROLE_PERMISSIONS matrix; surfaceCheck pulls current org role from the store). Note: surfaceCheck returns boolean, never null — coerce or shape permMap entries to boolean | null consistent with existing keys.
- Add lazy imports near the top: `const BrandingTab = lazy(() => import('./BrandingTab').then(m => ({ default: m.BrandingTab })));` and `const OnboardingTab = lazy(() => import('./OnboardingTab').then(m => ({ default: m.OnboardingTab })));` — keeps clinic-chunk bundle under 50 kB gz ceiling (per 31-01 raise + anti-patterns "Bundle impact").
- Route switch: add `{route.tab === 'branding' && surfaceCheck('branding.edit') && (<Suspense fallback={<ClinicSettingsLoader/>}><BrandingTab orgId={org.id} orgSlug={org.slug}/></Suspense>)}` and `{route.tab === 'onboarding' && (<Suspense fallback={<ClinicSettingsLoader/>}><OnboardingTab orgId={org.id}/></Suspense>)}` (OnboardingTab handles its own owner-vs-clinician branch internally per UI-SPEC §Surface 2 "Clinician read-only view").
- If `Suspense` is not already imported, add `import { lazy, Suspense, useCallback, useEffect, useState } from 'react';`.

Create `src/lib/__tests__/role-editor-modal.test.ts`: vitest with @testing-library/react. Mock `supabase.rpc` and `supabase.from('org_members').select(...).eq('role', 'owner')`. Test cases: (1) opens in assign mode → renders 12 rows × 3 column table; (2) selecting role='clinician' applies `bg-[var(--color-primary-soft)]` className to the Clinician column header; (3) when ownerCount=1 and currentRole='owner' and selectedRole='clinician' → Confirm has disabled attribute + title="An organization must have at least one owner."; (4) when ownerCount=2 → Confirm is enabled; (5) Confirm click in valid state calls rpc('change_member_role', ...) once; (6) server returns LAST_OWNER_DEMOTE_DENIED → toast error fires (mock useToast). NEVER use `it.fixme`. If a render branch requires more setup than feasible, use `it.skip('… [DEFERRED — see deferred-tests.md]', ...)` per reference_vitest_skip_fixme.

Anti-patterns to honor: use `surfaceCheck` not direct role string compares; matrix table uses var(--color-*) tokens; no new font/color values; modal a11y inherited from Modal primitive.</action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/role-editor-modal.test.ts && npx tsc -b --noEmit && grep -q "id: 'branding'" src/components/clinic/settings/ClinicSettingsPage.tsx && grep -q "id: 'onboarding'" src/components/clinic/settings/ClinicSettingsPage.tsx && grep -q "change_member_role" src/components/clinic/settings/RoleEditorModal.tsx && grep -q "LAST_OWNER_DEMOTE_DENIED" src/components/clinic/settings/RoleEditorModal.tsx</automated>
  </verify>
  <done>RoleEditorModal in 'assign' mode renders 12×3 matrix + last-owner guard + change_member_role caller. ClinicSettingsPage NAV registers Branding + Onboarding tabs with permission gates and lazy mounts. role-editor-modal.test.ts ≥6 passing cases. tsc clean. No new color/font literals introduced (grep for hex/rgb).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BrandingTab (form + live WCAG meter + upload zones + live preview + save_org_branding wiring)</name>
  <files>src/components/clinic/settings/BrandingTab.tsx</files>
  <behavior>
    - Component-level gate: returns <NoPermission/> placeholder card if surfaceCheck('branding.edit') is false (defense-in-depth alongside the NAV visibleWhen — per anti-pattern "Do NOT bypass surfaceCheck() gates").
    - Two-column layout (md+ breakpoint): left form column min-width 320px primary anchor; right live preview min-width 280px max-width 420px sticky at md+, collapses below form on mobile. Per UI-SPEC §Surface 1 visual focal point note.
    - Logo upload zone: 160×80px dashed-border dropzone (.png/.jpg/.svg, ≤500 KB). On file selection: invoke supabase.functions.invoke('branding-asset-upload-url', { body: { p_org_id: orgId, p_kind: 'logo' } }) → receive { upload_url, public_url } → PUT file to upload_url → setForm({ ...form, logo_url: public_url }). aria-busy="true" while uploading. On success show img preview + "Remove" ghost button top-right.
    - Favicon upload zone: 64×64px dashed-border dropzone (.ico/.png, ≤500 KB). Same flow but p_kind: 'favicon'.
    - Four oklch color rows (Background → --brand-bg → bg_color; Text → --brand-text → text_color; Primary → --brand-primary → primary_color; Accent → --brand-accent → accent_color). Each row: 32×32px swatch (background-color set via inline style to the live oklch string when parseOklch is non-null, else last valid color) + monospace 13px <Input type="text"> showing the oklch string + (for text/bg and primary/bg only) WCAG indicator Badge.
    - Color string accepts oklch(...) only; parseOklch returns null on rgb/hex/named — on blur render red aria-live="polite" inline error "Enter a valid oklch() color (e.g., oklch(0.65 0.18 240))."
    - Live WCAG meter: two-row summary beneath color section. Row 1 "Text on background: {ratio} : 1" with Check icon text-[var(--color-success)] (passing >=4.5) or AlertCircle icon text-[var(--color-danger)] (failing). Row 2 "Primary on background: {ratio} : 1" (passing >=3.0). Computed live from form state via computeWcagContrast (from Task 1).
    - Font dropdowns: heading_font + body_font as <Select> with options Inter, Fraunces, JetBrains Mono, Lora, IBM Plex Sans. Each option renders its name in its own typeface via inline style.
    - Radius scale: 4-option segmented control rendered as <PillGroup> or 4 <Pill aria-pressed={selected===v}> for sm|md|lg|xl. Selected pill: bg-[var(--color-primary-soft)] text-[var(--color-primary)].
    - Support email: <Input type="email"> with validation on blur.
    - "Preview defaults" toggle: ghost <Button size="sm" leadingIcon={<Eye/>}> "Preview defaults". On click: temporarily applies LeanShot default tokens to the live preview pane (does NOT mutate form values; does NOT mutate global --brand-* on <html>). Button label flips to "Preview clinic theme" with EyeOff icon. Showing-defaults badge "Showing LeanShot defaults" appears in preview top-right.
    - Live preview pane: <Card variant="elevated"> with sticky md+ + 16px padding. role="img" aria-label="Live preview of clinic branding". Renders mini clinic chrome: 32px tall fake sidebar (background: --brand-bg via inline style, accent left-border: --brand-primary, logo slot showing form.logo_url img or LeanShot wordmark fallback), a fake card (background: --color-surface, border-radius mapped from form.radius_scale, heading in form.heading_font at 14px weight 600, body in form.body_font at 13px weight 400, mock primary CTA button using form.primary_color), and a text-on-bg sample "Patient data stays private" in form.text_color over form.bg_color. Preview overrides are LOCAL via inline style — DO NOT mutate document.documentElement (anti-pattern: "preview is isolated").
    - Save button: <Button variant="primary"> "Save branding". Disabled when ANY color is invalid oklch OR WCAG meter fails (text/bg < 4.5 OR primary/bg < 3.0) OR submitting. aria-busy + Spinner while saving. Calls supabase.rpc('save_org_branding', { p_org_id: orgId, p_tokens: { logo_url, favicon_url, primary_color, accent_color, bg_color, text_color, heading_font, body_font, radius_scale, support_email } }).
    - On error.message includes CONTRAST_TEXT_BG_FAIL → render inline error beside Row 1 meter "Text color doesn't have enough contrast against the background. Adjust either color and try again." (NEVER generic toast for WCAG errors).
    - On error.message includes CONTRAST_PRIMARY_BG_FAIL → render inline error beside Row 2 meter analogous message.
    - On other error → toast("Something went wrong. Please try again or contact support.", "error").
    - On success → toast("Branding saved — patients will see this on their next visit.", "success").
    - Destructive Remove logo / Remove favicon: optimistic update (clear logo_url / favicon_url in form state) + 6s undo toast "Logo removed. Undo" / "Favicon removed. Undo" using <Toast tone="info"> primitive with action link. After 6s expires, the cleared URL persists in form state (still requires Save click to write through to server). NEVER a blocking modal (per anti-patterns + UI-SPEC §Destructive).
    - useReducedMotion() respected: when true, no upload zone shimmer animation, no preview fade transitions (anti-pattern: "All forms respect useReducedMotion()").
    - Initial form values loaded from `select * from org_branding where org_id = orgId limit 1` on mount; if no row exists default to empty form (server-side INSERT vs UPDATE handled inside save_org_branding SECDEF).
  </behavior>
  <action>Create `src/components/clinic/settings/BrandingTab.tsx` per the behavior block above and UI-SPEC §Surface 1. Idiom target: model after `src/components/clinic/settings/ClinicRankingWeightsForm.tsx` (controlled form + supabase.rpc on save + useToast + Card wrapper). Props: `interface BrandingTabProps { orgId: string; orgSlug: string }`. Use lucide-react icons: Upload, X (remove), Eye, EyeOff, Check, AlertCircle, Palette. Import computeWcagContrast and parseOklch from `@/lib/wcag-contrast` (Task 1). Import surfaceCheck from `@/lib/org`. Import supabase from `@/lib/supabase`. Import useToast from `@/hooks/useToast`. Import useReducedMotion from `@/hooks/useReducedMotion`.

Component returns the <NoPermission/> placeholder (simple inline component rendering a flat Card with the message "You don't have permission to edit clinic branding.") when `!surfaceCheck('branding.edit')` per anti-pattern "every admin action must be both UI-gated AND server-gated".

Form state shape: `{ logo_url: string | null, favicon_url: string | null, primary_color: string, accent_color: string, bg_color: string, text_color: string, heading_font: string, body_font: string, radius_scale: 'sm'|'md'|'lg'|'xl', support_email: string }`. Defaults sourced from org_branding row; falling back to '' for empty fields. Track also `previewDefaults: boolean` for the toggle and `submitting: boolean` for Save state.

WCAG live meter rendering: compute `textBg = parseOklch(form.text_color) && parseOklch(form.bg_color) ? computeWcagContrast(form.text_color, form.bg_color) : null` and analogous `primaryBg`. Save disabled if (textBg === null || textBg < 4.5 || primaryBg === null || primaryBg < 3.0 || invalidOklchAny || submitting).

Server error mapping: after `const { error } = await supabase.rpc('save_org_branding', ...)`, inspect `error?.message`. If matches `CONTRAST_TEXT_BG_FAIL` set `inlineErrors.textBg = "..."` (rendered beside Row 1 meter). If matches `CONTRAST_PRIMARY_BG_FAIL` set `inlineErrors.primaryBg = "..."`. Clear inlineErrors on next user color change. Per anti-pattern: server is floor but client meter should never let user reach a failing-save state — the inline error path exists for defense-in-depth (e.g., server-side helper drift, edge oklch values).

Live preview pane: implemented as a separate <PreviewPane> sub-component (private to file). Receives `tokens: ResolvedTokens` prop derived from `previewDefaults ? LEANSHOT_DEFAULTS : form`. Uses inline `style={{ '--brand-bg': tokens.bg_color, '--brand-primary': tokens.primary_color, ... } as React.CSSProperties}` on its outer container so its descendants see brand-* CSS vars without polluting global <html>. Per anti-patterns: "preview is isolated".

Upload zone implementation: Pattern for both logo + favicon: handler `async function handleUpload(kind: 'logo' | 'favicon', file: File)`. Steps: (1) `setUploading(kind, true)`, (2) call `const { data, error } = await supabase.functions.invoke('branding-asset-upload-url', { body: { p_org_id: orgId, p_kind: kind } });` (3) on success PUT file to `data.upload_url` with fetch(upload_url, { method: 'PUT', body: file, headers: { 'content-type': file.type } }), (4) `setForm(f => ({ ...f, [kind === 'logo' ? 'logo_url' : 'favicon_url']: data.public_url }))`, (5) `setUploading(kind, false)`. Pre-validation client-side: reject files > 500 KB or wrong mime type with inline error before invoke.

Destructive remove pattern: handler `handleRemove(kind: 'logo' | 'favicon')`: snapshot the current URL, set form field to null, fire `toast({ message: \`${kind === 'logo' ? 'Logo' : 'Favicon'} removed.\`, tone: 'info', action: { label: 'Undo', onClick: () => setForm(f => ({...f, [field]: snapshot})) }, duration: 6000 })`. If the existing useToast hook does not support action+duration, extend the call signature minimally (or wrap with setTimeout for the 6s window) — but DO NOT replace useToast across the codebase. If extension is non-trivial, use the simpler pattern: optimistic clear + a transient `<Toast>` rendered inline below the upload zone with a manual Undo button + 6s setTimeout to dismiss. Choose whichever minimally-invasive shape matches the existing Toast primitive (read `src/components/ui/Toast.tsx` and `src/hooks/useToast.ts` to pick).

Bundle discipline: import lucide icons individually (already established codebase pattern). Use React.lazy at ClinicSettingsPage level (Task 2) to keep this component out of the main clinic chunk. Anti-pattern check: total clinic-chunk add must stay under 50000 gz ceiling (raised by 31-01).

a11y: every input has aria-label or label; upload zones have role="button" aria-label="{field} upload zone"; uploading state aria-busy="true" aria-label="Uploading {field}…"; uploaded state aria-label="{field} uploaded: {filename}. Click Remove to clear." per UI-SPEC §"Upload zone states". Color swatches are decorative (aria-hidden="true") since the text input is the primary control. Modal a11y inherited from Modal primitive when used (this tab does not open modals directly, but if any uploader confirmation appears, role="dialog" aria-modal="true" required).

Copy: use UI-SPEC §"Copywriting Contract / BrandingTab" verbatim for every label/placeholder/toast/error message — tab label "Branding", page heading "Clinic branding", page sub-label "Customize how patients experience your clinic.", primary CTA "Save branding", etc.</action>
  <verify>
    <automated>npx tsc -b --noEmit && npx vitest run src/lib/__tests__/wcag-contrast.test.ts && npx eslint src/components/clinic/settings/BrandingTab.tsx && grep -q "save_org_branding" src/components/clinic/settings/BrandingTab.tsx && grep -q "branding-asset-upload-url" src/components/clinic/settings/BrandingTab.tsx && grep -q "computeWcagContrast" src/components/clinic/settings/BrandingTab.tsx && grep -q "surfaceCheck" src/components/clinic/settings/BrandingTab.tsx && grep -v '^\s*[/*#]' src/components/clinic/settings/BrandingTab.tsx | grep -c "var(--brand-" | xargs -I {} test {} -ge 1</automated>
  </verify>
  <done>BrandingTab.tsx exists with: surfaceCheck gate, two upload zones (logo + favicon) calling branding-asset-upload-url Edge Fn, 4 oklch color rows with live WCAG meter, font + radius selectors, "Preview defaults" toggle, isolated live preview pane, Save calling save_org_branding RPC with structured-error inline mapping, optimistic-undo Remove pattern. tsc + eslint clean. Component-level consumes var(--brand-*) tokens (only in isolated preview). useReducedMotion respected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: OnboardingTab (SortableTreePanel step list + step editor modal + add-step palette + version history + patient preview + clinician read-only branch)</name>
  <files>src/components/clinic/settings/OnboardingTab.tsx</files>
  <behavior>
    - Component-level routing on surfaceCheck('onboarding.edit'): true → owner edit view; false → clinician read-only view per UI-SPEC §Surface 2 "Clinician read-only view".
    - Owner edit view: two-column layout md+ (left: step builder 48% width; right: patient preview 48% width). Mobile: single column with "Preview for patients" toggle button.
    - Step builder (LEFT): uses SortableTreePanel<OnboardingStepNode> from `@/components/ui/SortableTreePanel`. Initial steps loaded from `select id, steps, version from org_onboarding_flows where org_id = orgId and is_active = true limit 1`; if no row exists, initialize from DEFAULT_STEPS (seeded with the 8 StepType canonical sequence: welcome, intro_card, medication, goals, body_stats, consent, doctor_invite, tour — each with a fresh uuid via crypto.randomUUID()).
    - Each step renders as 44px tall row: 40×44px drag handle (GripVertical icon, aria-label="Drag to reorder {step name}", cursor-grab; HIDDEN for consent step: opacity-0 pointer-events-none); 20×20px step type icon (mapping per UI-SPEC: welcome→Sparkles, intro_card→Image, medication→Pill, goals→Target, body_stats→Scale, consent→FileCheck, doctor_invite→Stethoscope, tour→Map); step name (13px font-medium flex-1); "Required" Badge (tone="neutral") for medication + consent; Skippable Pill (aria-pressed={!step.skip}) for steps where skip is allowed (welcome, intro_card, goals, body_stats, doctor_invite, tour); Edit IconButton (Pencil icon) ONLY for welcome + intro_card; Remove IconButton (Trash2 icon, text-danger) HIDDEN for medication + consent.
    - Add step button: <Button variant="secondary" size="sm" leadingIcon={<Plus/>}> "Add step". Click opens inline palette sub-panel with animate-fade-in (skipped when useReducedMotion()). Palette ordering: welcome → intro_card → goals → body_stats → doctor_invite → tour (medication + consent NEVER in palette per UI-SPEC). Each palette card: type icon + name + 1-line description; click appends to step list with fresh uuid.
    - Step editor modal: <Modal role="dialog" aria-modal="true" aria-labelledby="step-editor-title"> opens for welcome + intro_card only. Heading "Edit {step name}". welcome: <Input> "Step title" (maxLength 60) + <Textarea> "Body text" (maxLength 200). intro_card: same inputs + 64×64px image uploader using same Edge Fn pattern as BrandingTab but with p_kind: 'intro_card' (uploaded to org-onboarding-assets bucket per CONTEXT D-09 + Claude's-Discretion separate-bucket recommendation). Buttons: "Cancel" (ghost) + "Apply changes" (primary). "Apply changes" mutates local steps state only (no RPC).
    - Version history Select: label "Version history". Populated from `select id, version, created_at from org_onboarding_flows where org_id = orgId order by version desc limit 5`. Format options as "v{N} — {relative date}". Selecting a non-current version → ghost note "Preview only" + <Button variant="secondary" size="sm"> "Restore this version" → calls supabase.rpc('activate_onboarding_flow_version', { p_org_id: orgId, p_flow_id: selectedFlowId }) → on success toast "Flow version {N} restored — new patients will see this flow." per UI-SPEC copy.
    - Save flow button: <Button variant="primary"> "Save flow". Disabled while local steps fail client-side shape validation (mandatory medication + consent present; no unknown StepType values; custom fields only on welcome/intro_card). Calls supabase.rpc('save_org_onboarding_flow', { p_org_id: orgId, p_steps: steps }). On success toast "Saved version {N} — new patients will see this flow." (N from RPC return).
    - Patient preview (RIGHT): <Card variant="elevated"> sticky md+ top-24. Heading "Patient view" (13px secondary). role="img" aria-label="Preview of patient onboarding flow". Renders mini stepper using existing <ProgressIndicator> from src/components/onboarding/ProgressIndicator.tsx (consumed as-is). Prev/next chevron <IconButton> arrows. "Step {N} of {total}" counter (13px secondary). Step frame shows step.custom.title/body if set or LeanShot default label.
    - Empty state: only if all removable steps are removed (rare); shows EmptyState illustration + "No steps yet" + "Add steps to build your patient onboarding experience." per UI-SPEC copy.
    - Clinician read-only view: single panel; heading "Current patient onboarding flow" (16px semibold); read-only step list (same row layout but drag handles + edit/remove/skip controls hidden); aria-readonly="true" on list container; footer "Only workspace owners can edit the onboarding flow." (13px secondary italic).
    - Remove step destructive pattern: optimistic remove + 6s undo toast "Step removed. Undo" per UI-SPEC §Destructive — NEVER blocking modal.
    - useReducedMotion() respected: when true, palette appearance uses display swap not fade; SortableTreePanel's announcements work but transitions are undefined (the 31-00b primitive should already honor this — verify by props).
  </behavior>
  <action>Create `src/components/clinic/settings/OnboardingTab.tsx` per the behavior block + UI-SPEC §Surface 2. Props: `interface OnboardingTabProps { orgId: string }`. Imports: SortableTreePanel from `@/components/ui/SortableTreePanel`; `import type { OnboardingStepNode, StepType } from '@/types/onboarding-step'`; ProgressIndicator from `@/components/onboarding/ProgressIndicator`; Modal/Button/IconButton/Input/Textarea/Card/Badge/Pill/Select/EmptyState primitives; useToast, useReducedMotion, surfaceCheck, supabase.

At top of file declare:

```ts
const STEP_TYPE_ICONS: Record<StepType, LucideIcon> = { welcome: Sparkles, intro_card: Image, medication: Pill, goals: Target, body_stats: Scale, consent: FileCheck, doctor_invite: Stethoscope, tour: Map };
const STEP_TYPE_LABELS: Record<StepType, string> = { welcome: 'Welcome', intro_card: 'Intro card', medication: 'Medication', goals: 'Goals', body_stats: 'Body stats', consent: 'Consent', doctor_invite: 'Doctor invite', tour: 'Tour' };
const STEP_TYPE_DESCRIPTIONS: Record<StepType, string> = { welcome: 'Personal greeting from your clinic.', intro_card: 'A custom card with optional image.', ... };
const MANDATORY_TYPES: ReadonlySet<StepType> = new Set(['medication', 'consent']);
const SKIPPABLE_TYPES: ReadonlySet<StepType> = new Set(['welcome', 'intro_card', 'goals', 'body_stats', 'doctor_invite', 'tour']);
const EDITABLE_TYPES: ReadonlySet<StepType> = new Set(['welcome', 'intro_card']);
const PALETTE_ORDER: readonly StepType[] = ['welcome', 'intro_card', 'goals', 'body_stats', 'doctor_invite', 'tour'];
const DEFAULT_STEPS_TEMPLATE: readonly StepType[] = ['welcome', 'intro_card', 'medication', 'goals', 'body_stats', 'consent', 'doctor_invite', 'tour'];
```

Top-level component logic:
1. Resolve `canEdit = surfaceCheck('onboarding.edit')`. If false → render <ReadOnlyView orgId={orgId}/> sub-component which performs the same SELECT but renders the static list per UI-SPEC.
2. Owner view loads initial state via `useEffect` hook fetching active flow + version history in parallel. Active flow fetch: `await supabase.from('org_onboarding_flows').select('id, steps, version').eq('org_id', orgId).eq('is_active', true).maybeSingle()`. Version history fetch: `await supabase.from('org_onboarding_flows').select('id, version, created_at').eq('org_id', orgId).order('version', { ascending: false }).limit(5)`.
3. If active row exists set local `steps` state to `row.steps as OnboardingStepNode[]`; else initialize `steps` from DEFAULT_STEPS_TEMPLATE.map(t => ({ id: crypto.randomUUID(), type: t })).
4. Render <SortableTreePanel<OnboardingStepNode> items={steps} getId={s => s.id} onReorder={setSteps} announceItemLabel={s => STEP_TYPE_LABELS[s.type]} renderItem={renderStepRow}/>.
5. renderStepRow callback receives (step, dragHandle) and returns the 44px li row per behavior block. The dragHandle node is the SortableTreePanel-provided handle; for `step.type === 'consent'` render a span with className="opacity-0 pointer-events-none" wrapping the handle to hide it visually but preserve list structure.
6. Skippable Pill onChange: setSteps(steps.map(s => s.id === step.id ? { ...s, skip: !s.skip } : s)). Edit IconButton onClick: setEditingStep(step). Remove IconButton onClick: handleRemoveStep(step.id) which optimistically setSteps(steps.filter(...)) + fires 6s undo toast that restores on Undo click.
7. Add-step palette: inline sub-panel rendered conditionally on `paletteOpen` state. Filter: `PALETTE_ORDER.filter(t => !steps.some(s => s.type === t))` (one instance of each type; researcher recommendation per UI-SPEC since palette appears in canonical patient-journey order). Click handler appends with fresh uuid.
8. Step editor modal: <Modal open={editingStep !== null} onClose={() => setEditingStep(null)} title={`Edit ${STEP_TYPE_LABELS[editingStep?.type ?? 'welcome']}`} size="md" mobileFullscreen>. Internal state stagedTitle + stagedBody + stagedImageUrl. Apply changes: setSteps(steps.map(s => s.id === editingStep.id ? { ...s, custom: { title: stagedTitle, body: stagedBody, ...(editingStep.type === 'intro_card' && stagedImageUrl ? { image_url: stagedImageUrl } : {}) } } : s)) + setEditingStep(null). For intro_card image uploader: reuse the same Edge Fn invoke pattern from BrandingTab Task 3 with `p_kind: 'intro_card'` (Edge Fn from 31-02 supports the third kind per CONTEXT D-09 sibling-bucket recommendation; if 31-02 ships only logo/favicon kinds, fall back to the same bucket with `p_kind: 'logo'` and post-Phase-31 follow-up — but PRIMARY path is the third kind; cross-reference 31-02's Edge Fn implementation before implementing).
9. Client validation: `validateSteps(steps): string | null` returns error message if MANDATORY_TYPES not all present, unknown StepType, or custom on non-EDITABLE_TYPES. Save button disabled unless validateSteps returns null. (Server-side `_validate_onboarding_steps` from 31-04 is the floor; client is UX sugar.)
10. handleSaveFlow: `const { data: newFlowId, error } = await supabase.rpc('save_org_onboarding_flow', { p_org_id: orgId, p_steps: steps });` — RPC returns the new flow's UUID (uuid type). After success, refetch version history (single SELECT ordering by version DESC), look up the new row's version from the refreshed list using newFlowId match, then `toast('Saved version ${newRow.version} — new patients will see this flow.', 'success')`. On error toast generic.
11. handleRestoreVersion(flowId, version): `const { error } = await supabase.rpc('activate_onboarding_flow_version', { p_org_id: orgId, p_flow_id: flowId });`. On success toast(`Flow version ${version} restored — new patients will see this flow.`, 'success') + refetch active flow + version history.
12. Patient preview pane: dedicated <PatientPreviewPane steps={steps}/> sub-component. State: `previewIdx`. Renders ProgressIndicator with total=steps.length and current=previewIdx, prev/next IconButtons, and a Card showing step.custom.title || STEP_TYPE_LABELS[step.type] as heading + step.custom.body or "(LeanShot default content)" as body. No real inputs (read-only mockup).
13. ReadOnlyView sub-component: fetches active flow (same query); renders steps list with the same row layout but drag handles + edit/remove/skip controls hidden; aria-readonly="true" on container; footer note "Only workspace owners can edit the onboarding flow."

Per anti-patterns: all forms respect useReducedMotion (pass to SortableTreePanel if it accepts; otherwise the 31-00b primitive already honors it internally per its contract); modal a11y role="dialog" aria-modal="true" via primitive; no new color/font values; consume var(--color-*) tokens; "Apply" → "Apply changes" per UI-SPEC final copy; no blocking confirmation modals for Remove (optimistic + 6s undo).

Copy: use UI-SPEC §"Copywriting Contract / OnboardingTab" verbatim for every label/placeholder/toast.

Test note: This component is rendering-heavy. We are NOT writing a vitest file for OnboardingTab in this plan (the SortableTreePanel + ProgressIndicator interaction is covered by 31-00b's page-builder neighbor tests + Phase 31's Wave 3 e2e in plan 31-06). If during execution the type-check fails or eslint flags a path, fix in-place rather than adding test scaffold.</action>
  <verify>
    <automated>npx tsc -b --noEmit && npx eslint src/components/clinic/settings/OnboardingTab.tsx && grep -q "SortableTreePanel" src/components/clinic/settings/OnboardingTab.tsx && grep -q "save_org_onboarding_flow" src/components/clinic/settings/OnboardingTab.tsx && grep -q "activate_onboarding_flow_version" src/components/clinic/settings/OnboardingTab.tsx && grep -q "OnboardingStepNode" src/components/clinic/settings/OnboardingTab.tsx && grep -q "surfaceCheck" src/components/clinic/settings/OnboardingTab.tsx && bash scripts/assert-clinic-bundle-budget.sh</automated>
  </verify>
  <done>OnboardingTab.tsx exists with both owner-edit view (SortableTreePanel + step editor modal + add-step palette + version history Select + Save flow button + patient preview pane) and clinician read-only view; consumes SortableTreePanel + OnboardingStepNode from prior plans; calls save_org_onboarding_flow + activate_onboarding_flow_version SECDEFs; respects useReducedMotion + a11y. tsc + eslint clean. Clinic chunk under 50 kB gz ceiling per assert-clinic-bundle-budget.sh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (clinic admin) → SECDEFs | Form submissions cross from authenticated browser into Postgres SECURITY DEFINER functions running with elevated privileges (Phase 24 has_permission + Phase 31-01 12-key matrix gate every entry). |
| Client (uploader) → Edge Function | File metadata + p_org_id cross from browser into Supabase Edge Function `branding-asset-upload-url` which issues a presigned Storage URL. |
| Client → Storage PUT | File bytes uploaded directly to Supabase Storage via presigned URL; storage RLS enforces path-prefix matching. |
| Client → log_admin_action audit trail | Every successful mutation writes a row to audit_logs which is read-only to clinicians and immutable to anyone but the SECDEF. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05-01 | Tampering | RoleEditorModal client last-owner guard | mitigate | Server-side `change_member_role` SECDEF independently enforces `count(*) where role='owner'` ≤ 1 → raises `LAST_OWNER_DEMOTE_DENIED` (P0001). Even if client guard is bypassed via devtools/network replay, the SECDEF is the security floor (per D-13 "DB is floor"). Test path: 31-04 cross-tenant RLS test suite extended to call `change_member_role` directly with last-owner payload and assert raise. |
| T-31-05-02 | Elevation of Privilege | BrandingTab + OnboardingTab + RoleEditorModal surface visibility | mitigate | Defense-in-depth: (a) ClinicSettingsPage NAV `visibleWhen` hides Branding entry for non-owners; (b) BrandingTab + RoleEditorModal also call `surfaceCheck()` at component mount and render `<NoPermission/>` placeholder if false; (c) every SECDEF the components call (`save_org_branding`, `save_org_onboarding_flow`, `activate_onboarding_flow_version`, `change_member_role`) gates on `has_permission(get_caller_role(p_org_id), '<perm>')` via 31-01 helper. Three independent layers. |
| T-31-05-03 | Information Disclosure | RoleEditorModal 12×3 matrix table visible to clinicians | accept | The matrix is informational only (already describes role semantics that any org member can infer from product behavior). No PHI, no secrets. Clinicians benefit from understanding role boundaries when communicating with owners. Disposition accepted with no mitigation. |
| T-31-05-04 | Denial of Service | Save button rapid-click → duplicate save_org_branding / save_org_onboarding_flow RPC fires | mitigate | Save button has `disabled={submitting}` gate; submitting flag set true before await + reset false in finally. supabase-js single-flight is implicit since the Promise resolution completes before the next click can re-enable the button. Per UI-SPEC `aria-busy="true"` + Spinner provides AT-user feedback that the operation is in progress. |
| T-31-05-05 | Tampering | Client-side WCAG meter shows pass while server hard-blocks | mitigate (UX consistency) | `src/lib/wcag-contrast.ts` is a TypeScript port of the SQL `_compute_wcag_contrast` and the parity vitest (Task 1) asserts equality within 0.05 tolerance across 6 sample pairs. Drift between TS port and SQL helper would cause UX confusion (Save enabled then server rejects); test gates against drift. If 31-02 changes the SQL helper, this plan's parity test fails → forces re-sync. Server is floor; client mirror is for UX only. |
| T-31-05-06 | Information Disclosure | Upload presigned URL leak via shoulder surfing / browser history | accept | Presigned URLs from Supabase Storage are short-lived (default 60-second expiry; Edge Fn configures). Leaked URL grants single-PUT upload to one path within one org folder. RLS on storage.objects still enforces `(storage.foldername(name))[1] = org_id::text` AND `org_members.role = 'owner'` (per 31-02). Low impact; standard presigned-URL trust model. |
| T-31-05-07 | Tampering | Optimistic-remove pattern allows client to silently drop org branding without server call | mitigate | Optimistic remove is LOCAL state only; user MUST click Save for the cleared logo_url/favicon_url to persist via save_org_branding. Save itself gates on `has_permission('branding.edit')` server-side. Closing the tab without Save discards the change. No silent drop. |
| T-31-05-08 | Repudiation | Member role change without audit trail | mitigate | `change_member_role` SECDEF calls `log_admin_action('org_member.role_changed', jsonb_build_object('org_id', p_org_id, 'user_id', p_user_id, 'new_role', p_role::text))` per D-13. AuditTab (Phase 9) renders the trail. Append-only audit_logs table (Phase 24) prevents tampering. |
</threat_model>

<verification>
**Per-task verification (automated commands embedded in each task above) plus phase-level checks:**

1. `npx tsc -b --noEmit` — no TS errors across all 6 modified/created files.
2. `npx eslint src/components/clinic/settings/BrandingTab.tsx src/components/clinic/settings/OnboardingTab.tsx src/components/clinic/settings/RoleEditorModal.tsx src/components/clinic/settings/ClinicSettingsPage.tsx src/lib/wcag-contrast.ts` — eslint clean (matches project import-x/order conventions per project_lint_debt_import_x_order baseline).
3. `npx vitest run src/lib/__tests__/wcag-contrast.test.ts src/lib/__tests__/role-editor-modal.test.ts` — both files pass.
4. `npx vitest run` — full unit suite still passes (no regressions to Phase 9 RoleEditorModal create/edit mode tests since we extended via discriminated `mode` union without removing existing mode='create'|'edit' branches).
5. `npm run build && bash scripts/assert-clinic-bundle-budget.sh` — clinic chunk under CLINIC_CEILING=50000 gz (raised by 31-01 as first task). Per reference_bundle_budget_hash_hyphen ensure per-chunk ceilings still enforced.
6. Migration file naming: `ls -1 supabase/migrations | grep -E '^[0-9]{14}_p31_05_' | wc -l | xargs -I {} test {} -eq 1` — exactly one strict-14-digit file landed.
7. Grep gate against scope reduction: `grep -v '^\s*[/*#]' src/components/clinic/settings/BrandingTab.tsx src/components/clinic/settings/OnboardingTab.tsx | grep -ciE '\b(v1|simplified|hardcoded|placeholder|TODO.*phase|will be wired)\b'` returns 0 (per scope_reduction_prohibition + reference_grep_gate_comment_strip).
8. Grep gate for hard-coded colors at component level: `grep -nE '#[0-9a-fA-F]{3,8}|rgb\(' src/components/clinic/settings/BrandingTab.tsx src/components/clinic/settings/OnboardingTab.tsx | grep -v 'oklch'` returns 0 (per anti-patterns: never style at component level except in the isolated preview pane consuming form state). Acceptable exceptions: the upload accept attribute MIME strings.
9. Manual smoke (no e2e in this plan — e2e at Phase 31 Wave 3 plan 31-06 covers patient-side end-to-end): run `npm run dev`, visit `/clinic/test-clinic/settings/branding` as owner → expect Branding form renders with current org_branding values + live preview; visit `/clinic/test-clinic/settings/onboarding` as owner → expect SortableTreePanel + Save flow button; visit same URL as clinician → expect read-only "Current patient onboarding flow" panel.

**NOT in scope of this plan:**
- Live `supabase db push --linked` — owned by Plan 31-04 as the BLOCKING final task; this plan's migration ships file-only and is pushed in the same Wave 2 batch.
- Patient-side OnboardingFlow.tsx render branch — owned by Plan 31-06 (Wave 3).
- Cross-tenant RLS test for `change_member_role` — owned by Plan 31-04 which owns the RLS test fixture pattern for ORG-scoped tables. This plan only ships the SECDEF; 31-04 extends its `rls-org-onboarding-flows.test.ts` neighbor to call `change_member_role` cross-tenant for last-owner enforcement proof.
</verification>

<success_criteria>
1. Three new clinic-admin UI surfaces (`BrandingTab.tsx`, `OnboardingTab.tsx`, `RoleEditorModal.tsx` extended) live and registered in `ClinicSettingsPage` NAV with permission-gated visibility.
2. `change_member_role` SECDEF migration committed (NOT pushed — 31-04 owns push) with both gate + server-side last-owner guard + log_admin_action call + explicit search_path.
3. Client-side WCAG helper (`wcag-contrast.ts`) ships with parity vitest passing against SQL helper from 31-02 across ≥6 sample pairs within 0.05 tolerance.
4. RoleEditorModal in `mode='assign'` renders 12×3 matrix preview table + last-owner guard tooltip + Confirm caller wired to change_member_role; existing `mode='create'|'edit'` flows still pass their original vitests (no regression).
5. ClinicSettingsPage NAV registers Branding (visibleWhen branding.edit) + Onboarding (always visible; clinician sees read-only) tabs with lazy mount.
6. BrandingTab Save button disabled while client WCAG meter fails OR colors invalid; on server WCAG rejection inline error appears beside the failing pair (not generic toast).
7. OnboardingTab Save calls save_org_onboarding_flow → toast with version N; Restore calls activate_onboarding_flow_version → toast "Flow version N restored".
8. All Remove actions (logo, favicon, step) use optimistic + 6s undo toast pattern; zero blocking confirmation modals introduced.
9. Clinic chunk gz under CLINIC_CEILING=50000 (raised by 31-01).
10. tsc + eslint + vitest all green; ≥1 new vitest each for wcag-contrast (parity) and role-editor-modal (last-owner guard + matrix render).
</success_criteria>

<output>
After completion, create `.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-05-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- Files created/modified (paths)
- Migration SHA (after Plan 31-04 pushes the union of Wave-2 migrations)
- Bundle delta (kB gz before/after for clinic chunk)
- Test count delta (vitest count delta + which suites added)
- Server-side last-owner guard proof (snippet from migration showing the count(*) where role='owner' branch)
- Deviation log (if any anti-pattern had to be worked around — e.g., if useToast hook lacked action+duration shape and required inline workaround, note it)
- Open items for downstream plans (e.g., Plan 31-04 extending its RLS test to cover change_member_role cross-tenant + last-owner)
</output>
</content>
</invoke>