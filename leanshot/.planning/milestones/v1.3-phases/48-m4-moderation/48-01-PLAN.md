---
phase: 48-m4-moderation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270901000001_p48_community_reports_extend.sql
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - "community_reports.status CHECK widened from ('open') to ('open','triaged','resolved','dismissed') in single txn"
    - "community_reports has columns triaged_by uuid + triaged_at timestamptz + dismissed_reason text (all nullable)"
    - "partial UNIQUE (reporter_user_id, target_type, target_id) WHERE status IN ('open','triaged') enforces D-02 cooldown"
    - "partial UNIQUE (target_type, target_id) WHERE reason->>'source'='banned_word' supports D-11 idempotent sweep"
  artifacts:
    - path: "supabase/migrations/20270901000001_p48_community_reports_extend.sql"
      provides: "community_reports triage widening + cooldown UNIQUE + banned-word idempotency UNIQUE"
      contains: "alter table public.community_reports"
  key_links:
    - from: "community_reports.status"
      to: "community_reports_status_chk constraint"
      via: "DROP+ADD CHECK in same begin/commit"
      pattern: "check \\(status in \\('open','triaged','resolved','dismissed'\\)\\)"
    - from: "community_reports_active_dedup_uniq partial index"
      to: "report_content RPC (48-02) catches unique_violation"
      via: "partial UNIQUE WHERE status IN ('open','triaged')"
      pattern: "where status in \\('open','triaged'\\)"
---

<objective>
Widen Phase 45 `community_reports` to support D-01 triage workflow + D-02 cooldown + D-11 banned-word idempotent re-sweep.

Purpose: Foundation row for every downstream Wave 0 plan. The partial UNIQUEs are load-bearing — Plan 02's `report_content` RPC depends on the cooldown UNIQUE; Plan 08 (banned-words trigger) + Wave 1 sweep Fn depend on the banned-word UNIQUE for idempotent INSERTs.
Output: 1 migration file. Single `begin; … commit;` widens CHECK + ADD COLUMN + creates both partial UNIQUE indexes atomically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/48-m4-moderation/48-CONTEXT.md
@leanshot/.planning/phases/48-m4-moderation/48-RESEARCH.md
@leanshot/.planning/phases/48-m4-moderation/48-PATTERNS.md
@supabase/migrations/20270720000004_p44_notification_community.sql

**Memory references (load-bearing):**
- `reference_supabase_migration_filename_regex` — strict 14-digit prefix `20270901*`; verify NO `20270901000001*` collision before commit (pre-merge `ls supabase/migrations/20270901*`).
- `reference_minisite_monorepo_layout` — git-root-relative paths (`supabase/migrations/…`, NOT `leanshot/supabase/…`).
- `feedback_planner_missed_status_enum_widening` — DROP+ADD CHECK in same txn.
- `feedback_live_db_precheck_inverts_research_grep` — RESEARCH live-DB pre-check confirmed `community_reports` does NOT yet exist live (Phase 45 not pushed). Phase 48 migrations apply AFTER Phase 45's; ordering enforced by timestamp.
- `reference_supabase_migration_gotchas` — partial-index predicate expressions MUST be IMMUTABLE (literal constants only — both partial UNIQUEs here use `status IN (…)` and `reason->>'source'='banned_word'`; both are IMMUTABLE).
- `reference_postgres_no_insert_on_conflict_do_delete` — cooldown is enforced via UNIQUE+catch-23505 (Plan 02 RPC), NOT via `ON CONFLICT DO DELETE` (syntax does not exist).
- `feedback_negation_grep_defeated_by_comment_string` — do NOT include rejected-alternative strings (e.g., `ON CONFLICT DO DELETE`, `support_admin override`) in committed SQL comments.

**Phase 45 sequencing:** Phase 45's `community_reports` migration ships in `20270727*`. Phase 48 timestamp `20270901*` lands AFTER. Phase 48 EXECUTE cannot start until Phase 45/46/47 EXECUTE merges to main (orchestrator-enforced sequencing, not a plan-time blocker).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: community_reports triage widening + cooldown UNIQUE + banned-word UNIQUE</name>
  <files>supabase/migrations/20270901000001_p48_community_reports_extend.sql</files>
  <behavior>
    - After apply: `pg_get_constraintdef((select oid from pg_constraint where conname='community_reports_status_chk'))` includes all 4 status values.
    - After apply: `\d community_reports` shows triaged_by uuid (nullable, FK to auth.users), triaged_at timestamptz (nullable), dismissed_reason text (nullable).
    - After apply: 2 partial UNIQUE indexes exist on community_reports with predicates `WHERE status IN ('open','triaged')` and `WHERE reason->>'source'='banned_word'`.
    - Idempotent: applying twice does NOT error (use `add column if not exists` + `create unique index if not exists`).
  </behavior>
  <action>
    FORK header style from `supabase/migrations/20270720000004_p44_notification_community.sql` lines 1-18 (begin; … commit; with CHECK DROP+ADD).

    Wrap entire migration in single `begin; … commit;` transaction.

    **D-01 status CHECK widen** (DROP+ADD in same txn per memory `feedback_planner_missed_status_enum_widening`):
    ```sql
    alter table public.community_reports
      drop constraint if exists community_reports_status_chk,
      add  constraint community_reports_status_chk
        check (status in ('open','triaged','resolved','dismissed'));
    ```

    **D-01 ADD COLUMN** (nullable; safe on existing rows):
    ```sql
    alter table public.community_reports
      add column if not exists triaged_by       uuid        references auth.users(id),
      add column if not exists triaged_at       timestamptz,
      add column if not exists dismissed_reason text;
    ```

    **D-02 cooldown partial UNIQUE** (per memory `reference_supabase_migration_gotchas` — predicate IMMUTABLE):
    ```sql
    create unique index if not exists community_reports_active_dedup_uniq
      on public.community_reports (reporter_user_id, target_type, target_id)
      where status in ('open','triaged');
    ```

    **D-11 banned-word idempotency partial UNIQUE** (supports Plan 08 trigger ON CONFLICT DO NOTHING + Wave 1 sweep Fn re-invokes):
    ```sql
    create unique index if not exists community_reports_banned_word_dedup_uniq
      on public.community_reports (target_type, target_id)
      where reason->>'source' = 'banned_word';
    ```

    Add header comments referencing D-01, D-02, D-11 and the analog migration. Do NOT mention rejected alternatives like `ON CONFLICT DO DELETE` (per memory `feedback_negation_grep_defeated_by_comment_string`). Strict 14-digit filename per memory `reference_supabase_migration_filename_regex`.

    Per memory `reference_minisite_monorepo_layout` — write path is `supabase/migrations/...` from git root, NOT inside `leanshot/`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c "check (status in ('open','triaged','resolved','dismissed'))" | xargs test 1 -le && grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c 'community_reports_active_dedup_uniq' | xargs test 1 -le && grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c 'community_reports_banned_word_dedup_uniq' | xargs test 1 -le</automated>
  </verify>
  <done>
    File at canonical path; all 3 grep gates pass; single begin/commit block; no rejected-alternative strings in committed file.
  </done>
</task>

</tasks>

<acceptance_criteria>
- `ls supabase/migrations/20270901000001_p48_community_reports_extend.sql` — exists.
- `grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c "check (status in ('open','triaged','resolved','dismissed'))"` — exactly 1.
- `grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c 'add column if not exists triaged_by'` — exactly 1.
- `grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c 'add column if not exists triaged_at'` — exactly 1.
- `grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c 'add column if not exists dismissed_reason'` — exactly 1.
- `grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c 'community_reports_active_dedup_uniq'` — exactly 1.
- `grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c 'community_reports_banned_word_dedup_uniq'` — exactly 1.
- `grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c "reason->>'source' = 'banned_word'"` — exactly 1.
- `grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c 'begin;'` — exactly 1.
- `grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -c 'commit;'` — exactly 1.
- `grep -v '^--' supabase/migrations/20270901000001_p48_community_reports_extend.sql | grep -ic 'on conflict do delete'` — exactly 0 (rejected-alternative syntax MUST NOT appear in committed file).
- `ls supabase/migrations/20270901000001*.sql | wc -l` — exactly 1 (no timestamp collision).
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reporter → PostgREST | RPC `report_content` (48-02) inserts rows; partial UNIQUE enforces cooldown atomically |
| sweep Fn → PostgREST | banned-words sweep relies on banned-word partial UNIQUE for idempotency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | D (DoS) | report-flood from one reporter | mitigate | partial UNIQUE on (reporter_user_id, target_type, target_id) WHERE status IN ('open','triaged') — RPC catches 23505 |
| T-48-11 | T (Tampering) | sweep Fn re-invoked → duplicate banned-word reports | mitigate | partial UNIQUE on (target_type, target_id) WHERE reason->>'source'='banned_word' — `on conflict do nothing` in trigger + Fn |
</threat_model>

<verification>
- Migration file exists; all grep gates pass.
- Wave 3 close-out (Plan 12) runs `supabase db push --linked`; this migration applies as part of that push.
- RLS proof tests scaffolded in Plan 06 (Wave 0) will reference both partial UNIQUEs; they GREEN after Plan 12 db push.
</verification>

<success_criteria>
- Migration file committed.
- All `<acceptance_criteria>` greps pass.
- No `ON CONFLICT DO DELETE` strings in committed file (per memory `feedback_negation_grep_defeated_by_comment_string`).
- Migration timestamp `20270901000001` confirmed unique within phase via `ls`.
</success_criteria>

<output>
After completion, create `.planning/phases/48-m4-moderation/48-01-SUMMARY.md` per template.
</output>
