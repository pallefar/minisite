---
phase: 29
plan: 06
type: checkpoint:human-verify
wave: 3
depends_on: ["29-04", "29-05"]
files_modified:
  - src/components/clinic/billing/ClinicBillingCard.tsx
  - src/components/clinic/billing/PatientInviteForm.tsx
  - src/components/auth/ConsentAcceptScreen.tsx
  - src/App.tsx
  - e2e/clinic-patient-invite-accept.spec.ts
  - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-06-SUMMARY.md
autonomous: false
requirements: [ORG-08, ORG-10]
tags: [react, ui, realtime, e2e, lazy]
must_haves:
  truths:
    - "ClinicBillingCard.tsx renders: current subscription status + current period dates + active patient count, lazy-loaded via React.lazy in admin routes (per CLAUDE.md bundle constraint)"
    - "PatientInviteForm.tsx renders an inline form (email + consent scope checkboxes) that calls sendPatientInvite from src/lib/clinic-patient-invite.ts"
    - "ClinicBillingCard subscribes to Phase 28 HMAC realtime channel `org-{hmac8}-subscriptions` via src/lib/org-realtime.ts channelNameFor; updates status within 30 seconds of webhook (ORG-08 SC#4)"
    - "ConsentAcceptScreen.tsx renders at route /accept-clinic-invite?token=... — fetches preview, shows {org_name, org_logo_url, scope_summary}, on Accept calls acceptInvite + redirects to magic-link action_url"
    - "Anti-enumeration: 404 from /preview renders the EmptyState 'Invite not found' — identical for invalid OR expired"
    - "App.tsx lazy-loads /accept-clinic-invite route (single conditional path render — App.tsx has no router; per CLAUDE.md derive view from path)"
    - "e2e/clinic-patient-invite-accept.spec.ts Playwright spec proves: admin signs in → sends invite → patient browser opens /accept-clinic-invite?token=... → sees org name → accepts → redirected to /dashboard?welcome=clinic → primary_org_id set in DB"
  artifacts:
    - path: "src/components/clinic/billing/ClinicBillingCard.tsx"
      provides: "Clinic admin billing surface with realtime subscription status"
      contains: "channelNameFor"
    - path: "src/components/clinic/billing/PatientInviteForm.tsx"
      provides: "Inline invite send form"
      contains: "sendPatientInvite"
    - path: "src/components/auth/ConsentAcceptScreen.tsx"
      provides: "Patient consent screen at /accept-clinic-invite"
      contains: "previewInvite"
    - path: "e2e/clinic-patient-invite-accept.spec.ts"
      provides: "End-to-end Playwright proof of full invite → accept flow"
      contains: "test('full invite accept flow"
  key_links:
    - from: "ClinicBillingCard.tsx"
      to: "Plan 29-05 src/lib/clinic-patient-invite.ts + Phase 28 src/lib/org-realtime.ts"
      via: "imports both modules"
      pattern: "import.*clinic-patient-invite|import.*org-realtime"
    - from: "ClinicBillingCard.tsx"
      to: "Realtime channel"
      via: "supabase.channel(channelNameFor(orgId, 'subscriptions')).on(...).subscribe()"
      pattern: "channel.*subscribe"
    - from: "App.tsx"
      to: "ConsentAcceptScreen"
      via: "lazy + conditional render when window.location.pathname === '/accept-clinic-invite'"
      pattern: "accept-clinic-invite"
---

<objective>
Ship the UI surface that consumes Plan 29-04's billing pipeline + Plan 29-05's invite Edge Function:

(1) **ClinicBillingCard** — clinic admin sees their subscription status (active/past_due/canceled) + current period + active patient count; subscribes to Phase 28 HMAC realtime channel so Stripe webhook updates are reflected within 30s (ORG-08 SC#4).

(2) **PatientInviteForm** — inline form on the same admin surface; clinic admin enters patient email + consent scope; calls Plan 29-05 `sendPatientInvite`.

(3) **ConsentAcceptScreen** — patient-side route `/accept-clinic-invite?token=...`; fetches preview, shows consent UI, on Accept redirects to magic-link.

(4) **e2e proof** — Playwright spec drives the full round-trip per ORG-10 SC#3.

Output: 3 React components + App.tsx routing edit + Playwright spec. This is a `checkpoint:human-verify` plan because the realtime 30s reflection invariant requires live two-tab verification per [[feedback_verify_human_uat_via_cli]] — Playwright spec can drive the path but the UI shape needs human eyeballs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-RESEARCH.md
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-04-SUMMARY.md
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-05-SUMMARY.md

<interfaces>
From src/components/ui/Card.tsx + Button.tsx + Input.tsx + EmptyState.tsx (verified via CLAUDE.md inventory):
  - <Card span={4|6|8|12} variant="elevated">; <CardHeader title icon action />; <Button variant="primary|secondary" /> ; <Input aria-invalid /> ; <EmptyState icon title description />

From src/lib/org-realtime.ts (Phase 28 Plan 04 — exported):
  - export function channelNameFor(orgId: string, table: string): string

From src/lib/clinic-patient-invite.ts (Plan 29-05):
  - sendPatientInvite, previewInvite, acceptInvite — all return InviteResult<T>

From src/App.tsx (verified — no router; conditional view by store + window.location):
  - Top-level switch on user presence; lazy-loaded tabs/modals; one-shot Suspense
  - Pattern: `const Component = lazy(() => import('@/components/...'))`

From src/lib/billing.ts (Phase 14 — getActiveTier collapse):
  - export type Tier = 'free' | 'pro_monthly' | 'pro_yearly' | ...
  - getActiveTier(subscription): Tier
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ClinicBillingCard + PatientInviteForm with realtime subscription wire</name>
  <files>
    - src/components/clinic/billing/ClinicBillingCard.tsx
    - src/components/clinic/billing/PatientInviteForm.tsx
  </files>
  <read_first>
    - src/lib/clinic-patient-invite.ts (Plan 29-05 helpers — exact signatures)
    - src/lib/org-realtime.ts (channelNameFor — Phase 28 Plan 04)
    - src/lib/billing.ts (subscription type + getActiveTier)
    - src/components/ui/Card.tsx + Button.tsx + Input.tsx + EmptyState.tsx (primitives)
    - src/hooks/useReducedMotion.ts (use for any animation gating)
    - src/lib/store.ts (Zustand selector pattern — pull org_id, user, etc.)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-EXTENSION-CONTRACT.md §Section 6 (HMAC channel-name registration checklist)
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md §D-05 (30s reflection invariant)
  </read_first>
  <action>
    Create `src/components/clinic/billing/ClinicBillingCard.tsx`:
    1. `import { useEffect, useState } from 'react'` + UI primitives + supabase client + channelNameFor + types.
    2. Component receives `orgId` prop (or pulls from Zustand `useStore(s => s.currentOrgId)`).
    3. Initial state load: fetch `subscriptions` row where `clinic_id = orgId` (use `subscriptions WHERE clinic_id IS NOT NULL` per A1 reconciliation); fetch `count_active_patients(orgId)` via `supabase.rpc('count_active_patients', { p_org_id: orgId })`.
    4. Realtime: `useEffect` subscribes to `supabase.channel(channelNameFor(orgId, 'subscriptions')).on('postgres_changes', {event: 'UPDATE', schema: 'public', table: 'subscriptions', filter: \`clinic_id=eq.\${orgId}\`}, payload => setSubscription(payload.new)).subscribe()`. Cleanup on unmount.
    5. Render: `<Card variant="elevated" span={8}><CardHeader title="Clinic Billing" /><div>Status: {subscription.status}</div><div>Period: {subscription.current_period_start} → {subscription.current_period_end}</div><div>Active patients: {count}</div></Card>`.
    6. Include `data-sentry-mask` on any element rendering patient counts if HIPAA-16 enforces — count is non-PHI so likely not needed but add a comment marker for Phase 25 sweep.
    7. Loading state via `<Skeleton />` primitive; error state via `<EmptyState />`.

    Create `src/components/clinic/billing/PatientInviteForm.tsx`:
    1. State: `email`, `scope`, `submitting`, `result`.
    2. Form fields: email <Input type="email">, consent scope checkboxes (read_activity, read_pharmacology, etc. — match `_validate_consent_scope` shape from Phase 9). For Plan 29-06 MVP: single checkbox "Read patient activity data" → `scope = {read_activity: true}`.
    3. On submit: call `sendPatientInvite(orgId, email, scope)`. Show toast on result.
    4. NEVER render anything from the patient's eventual data (this is pre-link state) — strictly clinic-admin-facing.
    5. Anti-enumeration UX: identical "Invite sent" toast regardless of whether email mapped to existing user (matches Edge Fn W-1 invariant).

    Both files must be `export default function ComponentName()` so they can be `React.lazy`-loaded.

    Run TypeScript check: `npx tsc --noEmit`. No errors in these new files.

    Run lint: `npm run lint -- src/components/clinic/billing/`. No errors.

    Run bundle budget check after wiring up in Task 3: `npm run build && bash scripts/assert-clinic-bundle-budget.sh`. Per [[reference_bundle_budget_hash_hyphen]]: ensure indices ≤ ceiling. Lazy-loading these components keeps them out of index.* chunk.
  </action>
  <verify>
    <automated>test -f src/components/clinic/billing/ClinicBillingCard.tsx && test -f src/components/clinic/billing/PatientInviteForm.tsx && grep -q "channelNameFor" src/components/clinic/billing/ClinicBillingCard.tsx && grep -q "sendPatientInvite" src/components/clinic/billing/PatientInviteForm.tsx && grep -q "export default function" src/components/clinic/billing/ClinicBillingCard.tsx && grep -q "export default function" src/components/clinic/billing/PatientInviteForm.tsx && npx tsc --noEmit 2>&1 | grep -E "src/components/clinic/billing/.*error" | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist as `export default function`.
    - ClinicBillingCard imports `channelNameFor` from `src/lib/org-realtime.ts`.
    - ClinicBillingCard subscribes via `supabase.channel(channelNameFor(orgId, 'subscriptions'))` + `postgres_changes` on `subscriptions` filtered by `clinic_id=eq.${orgId}`.
    - PatientInviteForm imports `sendPatientInvite` from `src/lib/clinic-patient-invite.ts`.
    - TypeScript compiles with no errors in these files.
  </acceptance_criteria>
  <done>Admin billing surface + invite form authored.</done>
</task>

<task type="auto">
  <name>Task 2: Author ConsentAcceptScreen + App.tsx route wire + bundle-budget verify</name>
  <files>
    - src/components/auth/ConsentAcceptScreen.tsx
    - src/App.tsx
  </files>
  <read_first>
    - src/App.tsx (full file — current view-switching logic; how lazy modals/tabs are conditionally rendered)
    - src/lib/clinic-patient-invite.ts (previewInvite + acceptInvite signatures)
    - src/components/ui/Card.tsx + Button.tsx + EmptyState.tsx + Skeleton.tsx
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-RESEARCH.md §Pattern 6 (consent screen recommended primitives)
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md §D-08 (flow steps 1-7)
  </read_first>
  <action>
    Create `src/components/auth/ConsentAcceptScreen.tsx`:
    1. `useState` for: `preview` (null | object), `loading` (boolean), `error` ('not_found' | 'network' | null), `submitting` (boolean), `acceptResult` (null | object).
    2. `useEffect` on mount: read `token` from `new URLSearchParams(window.location.search).get('token')` — if absent or empty, set `error='not_found'`. Otherwise call `previewInvite(token)`.
    3. Loading: render `<Skeleton />`.
    4. Error (not_found): render `<EmptyState icon={AlertCircle} title="Invite not found" description="This invite is invalid or has expired." />` — anti-enumeration: identical UI for invalid/expired.
    5. Preview success: render `<Card variant="elevated"><img src={preview.org_logo_url} alt="" /><h1>{preview.org_name} invites you to LeanShot</h1><p>{summarizeScope(preview.scope_summary)}</p><Button variant="primary" onClick={handleAccept}>Accept invite</Button><Button variant="secondary" onClick={() => window.location.href='/'}>Decline</Button></Card>`.
    6. `handleAccept`:
       - Prompt for patient email (inline input — they may not have it pre-filled if signing up fresh). Per D-08: email is needed to mint magic link.
       - Actually: simplest UX is to pass the email through the invite-link querystring AS WELL (signed via hash). For Plan 29-06: render an inline `<Input type="email" />` before Accept button; require email before Accept enabled.
       - Call `acceptInvite(token, email)`.
       - On `{ok: true, redirect_url}`: `window.location.href = redirect_url` (magic-link action_url → Supabase Auth verifies → /dashboard?welcome=clinic).
       - On `{ok: false, error: 'magic_link_failed', invite_accepted: true}`: render "Invite accepted! Please request a password reset to log in" + link to /auth/reset.
       - On `{ok: false, error: 'invite_not_found'}`: set error='not_found'.

    Edit `src/App.tsx`:
    1. Add `const ConsentAcceptScreen = lazy(() => import('@/components/auth/ConsentAcceptScreen'))` near other lazy imports.
    2. Add a top-level conditional render BEFORE the marketing/onboarding/dashboard switch: `if (window.location.pathname === '/accept-clinic-invite') return <Suspense fallback={<Skeleton />}><ConsentAcceptScreen /></Suspense>;`.
    3. This bypasses normal auth gating (the screen MUST be reachable anonymous-side).

    Run `npm run build && bash scripts/assert-clinic-bundle-budget.sh`. Bundle ceiling unchanged because both ClinicBillingCard and ConsentAcceptScreen are lazy-loaded (not in index chunk).
  </action>
  <verify>
    <automated>test -f src/components/auth/ConsentAcceptScreen.tsx && grep -q "previewInvite" src/components/auth/ConsentAcceptScreen.tsx && grep -q "acceptInvite" src/components/auth/ConsentAcceptScreen.tsx && grep -q "ConsentAcceptScreen" src/App.tsx && grep -q "accept-clinic-invite" src/App.tsx && grep -q "lazy" src/App.tsx && npm run build 2>&1 | tee /tmp/p29_06_build.txt | grep -E "built in" && bash scripts/assert-clinic-bundle-budget.sh 2>&1 | tee /tmp/p29_06_budget.txt | grep -E "(OK|pass|under)"</automated>
  </verify>
  <acceptance_criteria>
    - ConsentAcceptScreen.tsx exists; calls previewInvite + acceptInvite.
    - Anti-enumeration: 404 (`error='not_found'`) renders identical UI for invalid/expired.
    - App.tsx lazy-loads ConsentAcceptScreen + conditionally renders for `/accept-clinic-invite` path.
    - `npm run build` succeeds.
    - `scripts/assert-clinic-bundle-budget.sh` passes (no chunk-ceiling violations).
  </acceptance_criteria>
  <done>Patient consent screen + route wired; production build green.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright e2e spec for full invite→accept round-trip</name>
  <files>
    - e2e/clinic-patient-invite-accept.spec.ts
  </files>
  <read_first>
    - playwright.config.ts (project shape + conditional run flag pattern per [[reference_playwright_conditional_project_argv]])
    - e2e/patient-activity-modal.spec.ts (closest analog — read full file for fixture seeding + addInitScript usage)
    - e2e/<any-existing-clinic-spec>.spec.ts (clinic-side admin auth pattern)
    - .planning/MEMORY references: [[reference_playwright_state_seeding]] (addInitScript not page.evaluate)
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md §D-08 (accept flow steps)
  </read_first>
  <action>
    Create `e2e/clinic-patient-invite-accept.spec.ts`:

    1. Gate: `const SHOULD_RUN = !!process.env.PLAYWRIGHT_RUN_P29;` (per [[reference_playwright_conditional_project_argv]] env-var pattern — argv detection breaks in worker subprocesses).
    2. `test.describe.configure({ mode: SHOULD_RUN ? 'serial' : 'skip' })`.
    3. Fixture: file-scoped TEST_SLUG_PREFIX. Use admin REST API to: create org, create admin user (`admin-${prefix}@example.com`), seed `org_members` row with role='admin'.
    4. Test flow:
       a. Admin browser context: addInitScript to seed session for admin user (per [[reference_playwright_state_seeding]] — addInitScript not page.goto+evaluate). Navigate to `/clinic/${slug}/billing`.
       b. Wait for ClinicBillingCard.
       c. Fill PatientInviteForm with `patient-${prefix}@example.com` + check consent scope.
       d. Click Send. Expect toast "Invite sent".
       e. Query DB admin client to fetch the invite row's raw token: actually impossible — only hash is stored. WORKAROUND for e2e: expose a TEST-ONLY env-gated RPC `_test_get_latest_invite_raw_token(p_org_id, p_patient_email)` that's behind `if (Deno.env.get('TEST_MODE') === '1')` — alternatively, BYPASS via direct INSERT into org_patient_invites with a known token+hash from the test fixture (cleanest).
       f. Patient browser context (new context): `page.goto('/accept-clinic-invite?token=<knownToken>')`.
       g. Wait for org_name to render. Click Accept after entering patient email.
       h. Wait for navigation to `/dashboard*` (magic-link action_url resolves through Supabase Auth → onboarded dashboard).
       i. Query DB admin client: assert `profiles.primary_org_id` = test org_id; assert `org_consent_grants` row exists; assert `org_patient_links` row exists; assert `org_patient_invites.accepted_at IS NOT NULL`.
    5. `afterAll`: cleanup org + user + invite via slug prefix.

    Run gated: `PLAYWRIGHT_RUN_P29=1 npx playwright test e2e/clinic-patient-invite-accept.spec.ts`. Expected pass.

    Skip note: if the test depends on the realtime channel update for ClinicBillingCard, defer that assertion to the human-verify checkpoint (Task 4) — Playwright realtime assertions are flaky per [[feedback_realtime_layer_e2e_pattern]]; the DB-state assertion is the more reliable invariant.
  </action>
  <verify>
    <automated>test -f e2e/clinic-patient-invite-accept.spec.ts && grep -q "PLAYWRIGHT_RUN_P29\|describe.configure" e2e/clinic-patient-invite-accept.spec.ts && grep -q "accept-clinic-invite" e2e/clinic-patient-invite-accept.spec.ts && grep -q "primary_org_id" e2e/clinic-patient-invite-accept.spec.ts && grep -q "addInitScript" e2e/clinic-patient-invite-accept.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `e2e/clinic-patient-invite-accept.spec.ts`.
    - Run gate uses `PLAYWRIGHT_RUN_P29` env var (NOT process.argv detection per [[reference_playwright_conditional_project_argv]]).
    - State seeding uses `page.addInitScript` (NOT goto+evaluate per [[reference_playwright_state_seeding]]).
    - Test asserts DB state post-accept: primary_org_id, org_consent_grants, org_patient_links, invite.accepted_at all populated.
    - When run with `PLAYWRIGHT_RUN_P29=1` env, the test passes.
  </acceptance_criteria>
  <done>e2e spec authored; ORG-10 SC#3 round-trip provable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN-VERIFY — realtime 30s billing-surface reflection (ORG-08 SC#4)</name>
  <files>(no file modification — visual + behavioral verification only)</files>
  <read_first>
    - src/components/clinic/billing/ClinicBillingCard.tsx (the component under test)
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md §D-05 (30s SLA invariant)
  </read_first>
  <action>HUMAN-VERIFY checkpoint — no code modification. Operator performs the steps in <how-to-verify> and reports the outcome via <resume-signal>. The verifier signal becomes the audit trail captured in the SUMMARY (Task 5).</action>
  <what-built>
    Plan 29-06 ships ClinicBillingCard that subscribes to Phase 28 HMAC realtime channel `org-{hmac8}-subscriptions`. When the Stripe webhook UPDATEs the `subscriptions` row (status, period dates), the card should re-render within 30 seconds without page refresh. Playwright realtime assertions are flaky per [[feedback_realtime_layer_e2e_pattern]] — human eyeballs verify the SC#4 invariant directly.
  </what-built>
  <how-to-verify>
    1. Spin up local dev: `npm run dev` + ensure logged in as a clinic admin for a test org with an existing `subscriptions` row.
    2. Navigate to `/clinic/{slug}/billing` — verify ClinicBillingCard renders status + period + count.
    3. Open a second tab to Stripe Dashboard (Test Mode) → Customers → find the test clinic's customer → Subscriptions → open the active subscription → click "Cancel subscription" (Stripe will fire `customer.subscription.updated` webhook).
    4. Watch the LeanShot tab — within 30 seconds, the status field should update from "active" to "canceled" without manual reload.
    5. Verify the timestamp diff between Stripe action (Stripe webhook console: "Sent at HH:MM:SS") and the UI update is ≤ 30 seconds.
    6. Optional: open browser devtools → Network tab → confirm a Realtime websocket frame containing the `subscriptions` UPDATE payload arrived in real time.
  </how-to-verify>
  <resume-signal>Type "approved" (UI reflects within 30s), "approved-with-notes" (works but >30s — flag latency), or describe issues (e.g., "no reflection — websocket disconnected").</resume-signal>
  <verify>
    <automated>echo "HUMAN-VERIFY checkpoint — operator signal captured in 29-06-SUMMARY.md Task 4 row"</automated>
  </verify>
  <done>Operator signal captured + recorded in SUMMARY; SC#4 invariant verified-or-flagged.</done>
</task>

<task type="auto">
  <name>Task 5: Write Plan 06 SUMMARY + commit</name>
  <files>
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-06-SUMMARY.md
  </files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - /tmp/p29_06_build.txt
    - /tmp/p29_06_budget.txt
    - Task 4 human-verify signal capture (record in SUMMARY)
  </read_first>
  <action>
    Write SUMMARY per template:
      - What Shipped: 3 components + App.tsx route + Playwright spec.
      - Verification Evidence: build pass + bundle budget pass + Playwright run output (if executed) + human-verify signal from Task 4.
      - Decisions Honored: D-05 realtime channel wire, D-08 anti-enumeration UX, RESEARCH §Pattern 6 consent screen primitives, CLAUDE.md bundle constraint.
      - Carry-Forward: Plan 29-07 closes the phase with vendor checkpoints + final commits.
      - Risks/Notes: Realtime 30s SLA verified by human (or noted if longer); the e2e spec uses a TEST-MODE bypass for raw-token retrieval — document the test-only RPC approach.

    Commit pathspec: `git add src/components/clinic/billing/ src/components/auth/ConsentAcceptScreen.tsx src/App.tsx e2e/clinic-patient-invite-accept.spec.ts .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-06-SUMMARY.md && git commit -m "feat(29-06): ClinicBillingCard + PatientInviteForm + ConsentAcceptScreen + e2e + realtime wire"`.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-06-SUMMARY.md && grep -q "## Verification Evidence" .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-06-SUMMARY.md && grep -q "human-verify\|HUMAN-VERIFY" .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-06-SUMMARY.md && git log --oneline -1 | grep -q "29-06"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY has Verification Evidence including build + bundle-budget pass-lines + human-verify Task 4 signal.
    - Carry-Forward names Plan 29-07.
    - Commit references all 4 source files + spec + SUMMARY.
  </acceptance_criteria>
  <done>UI shipped + verified; ORG-08 SC#4 satisfied; ORG-10 SC#3 e2e proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clinic admin browser → Edge Fn (send) | JWT-authed via session helper |
| Patient browser (anonymous) → /accept-clinic-invite | Public route; token-bearing query string |
| Browser → Realtime websocket | HMAC-channel-authorized via Phase 28 SECDEF helper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06-01 | Information Disclosure | ConsentAcceptScreen leaks org existence | mitigate | Anti-enumeration: identical EmptyState for invalid/expired token; preview RPC raises P0002 for both. |
| T-29-06-02 | Tampering | XSS via org_name/scope_summary | mitigate | React auto-escapes JSX text content; img src is constrained to https URLs (CSP); no dangerouslySetInnerHTML used. |
| T-29-06-03 | Spoofing | Cross-tenant realtime subscription | mitigate | Phase 28 Plan 04 `realtime_topic_authorized` SECDEF helper rejects HMAC mismatches AND verifies org_id in JWT org_ids claim. |
| T-29-06-04 | Information Disclosure | PHI in client-side billing UI | accept | Subscription status + count + period are non-PHI; data-sentry-mask not required but commented for Phase 25 sweep. |
| T-29-06-05 | Denial of Service | Realtime channel storms | accept | One subscription per ClinicBillingCard mount; useEffect cleanup unsubscribes on unmount; v1.3 scale (<50 admins) trivial. |
</threat_model>

<verification>
- 4 components/file present + TS compiles (Task 1+2 automated).
- Build + bundle budget pass (Task 2 automated).
- Playwright spec exists with env-var gating (Task 3 automated).
- Human-verify signal captured (Task 4 checkpoint).
- SUMMARY committed (Task 5 automated).
</verification>

<success_criteria>
- ORG-08 SC#4: 30-second billing-surface reflection (human-verified).
- ORG-10 SC#3: full invite → accept flow proves DB state via Playwright.
- Bundle ceiling unchanged (per CLAUDE.md constraint).
- Anti-enumeration: invalid + expired tokens render identical 404.
</success_criteria>

<output>
After completion, create `.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-06-SUMMARY.md`.
</output>
