---
phase: 29
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270601200001_p29_reconcile.sql
  - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-00-SUMMARY.md
autonomous: true
requirements: [ORG-08, ORG-09, ORG-10]
tags: [supabase, migration, reconciliation, stripe]
must_haves:
  truths:
    - "public.org_subscriptions table no longer exists (dropped — was P28 deny-all skeleton with zero data)"
    - "public.subscriptions has seats_paid int default 0 and seats_used int default 0 columns"
    - "public.profiles has primary_org_id uuid column referencing organizations(id) on delete set null"
    - "All 10 patient-data event tables (injections, weights, meals, mood, sleep, symptoms, photos, workouts, vials, ai_messages) have (user_id, created_at) composite indexes"
    - "Migration is applied to the linked Supabase project (visible via supabase db query --linked)"
  artifacts:
    - path: "supabase/migrations/20270601200001_p29_reconcile.sql"
      provides: "Wave 0 RECONCILE — DROP org_subscriptions, verify-or-ADD seats_paid/seats_used on subscriptions, ADD primary_org_id to profiles, ADD missing composite indexes on 5 event tables"
      contains: "drop table if exists public.org_subscriptions"
  key_links:
    - from: "supabase/migrations/20270601200001_p29_reconcile.sql"
      to: "linked Supabase project"
      via: "supabase db push --linked"
      pattern: "Applying migration 20270601200001_p29_reconcile.sql"
    - from: "subscriptions.seats_paid / seats_used"
      to: "Phase 14 public.subscriptions table"
      via: "ALTER TABLE ADD COLUMN IF NOT EXISTS"
      pattern: "alter table public.subscriptions add column if not exists seats_paid"
---

<objective>
Plan 29-00 RECONCILE — the mandatory Wave 0 prerequisite for every other Phase 29 plan (per CONTEXT D-10).

Drops the P28 deny-all `org_subscriptions` skeleton (CONTEXT A1) so that Phase 14's unified `public.subscriptions` table becomes canonical for clinic subs. Verifies-or-adds the schema P29 downstream code depends on: `subscriptions.seats_paid`, `subscriptions.seats_used`, `profiles.primary_org_id`. Adds `(user_id, created_at)` composite indexes on the 5 event tables that lack them (mood, sleep, symptoms, photos, vials per RESEARCH Pitfall 6 — verified via file read).

Purpose: lock the schema before any downstream plan writes Edge Function or RPC code that references these columns. Without this plan, downstream plans hit "relation org_subscriptions does not exist" or "column primary_org_id does not exist" runtime errors (RESEARCH Pitfall 2).

Output: single migration file `20270601200001_p29_reconcile.sql` pushed live via `supabase db push --linked`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-RESEARCH.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-EXTENSION-CONTRACT.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-ADDENDUM-orgs-reconciliation.md

<interfaces>
<!-- Live schema baseline (verified via grep at planning time) -->

From supabase/migrations/20270601100008_org_subscriptions_table.sql (P28 SKELETON — TO BE DROPPED):
  - public.org_subscriptions (deny-all RLS, zero rows)
  - Columns: org_id PK, status enum, stripe_subscription_id, stripe_customer_id, seats_paid int, seats_used int, ...

From supabase/migrations/20260601000019_stripe_subscriptions.sql (Phase 14 — KEEP):
  - public.subscriptions (XOR constraint on user_id / clinic_id)
  - public.clinic_stripe_customers (keyed by clinic_id → organizations(id))
  - public.stripe_customers (keyed by user_id)
  - public.subscription_events (Stripe evt_xxx idempotency anchor)
  - public.count_active_patients(p_clinic_id uuid) — Phase 14 obsolete version (memberships-based, 5-table UNION) — REPLACED in Plan 29-01

From supabase/migrations/20260514000004..20260514000009_*.sql (verified via RESEARCH Pitfall 6):
  - mood, sleep, symptoms, photos, vials tables have (user_id, date desc) index BUT NOT (user_id, created_at) — Plan 29-00 adds these.

From supabase/migrations/20260601000020_profiles_table.sql (verified absent — no primary_org_id column exists in any migration as of HEAD a82aff4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight live-schema audit + draft RECONCILE migration</name>
  <files>
    - supabase/migrations/20270601200001_p29_reconcile.sql
  </files>
  <read_first>
    - supabase/migrations/20270601000019_stripe_subscriptions.sql (Phase 14 subscriptions/clinic_stripe_customers/subscription_events baseline)
    - supabase/migrations/20270601100008_org_subscriptions_table.sql (P28 skeleton being dropped)
    - supabase/migrations/20270601100001_p28_reconcile_orgs_to_organizations.sql (direct precedent for RECONCILE migration pattern)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-EXTENSION-CONTRACT.md §Status-Machine Transition Ownership (org_subscriptions transition rows being removed)
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md §A1, D-09, D-10
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-RESEARCH.md §Runtime State Inventory + Pitfall 6
  </read_first>
  <action>
    Run pre-flight live audit (capture into the migration's leading comment block):
      `supabase db query --linked --query "select to_regclass('public.org_subscriptions') as org_subs_table, exists(select 1 from information_schema.columns where table_schema='public' and table_name='subscriptions' and column_name='seats_paid') as has_seats_paid, exists(select 1 from information_schema.columns where table_schema='public' and table_name='subscriptions' and column_name='seats_used') as has_seats_used, exists(select 1 from information_schema.columns where table_schema='public' and table_name='profiles' and column_name='primary_org_id') as has_primary_org_id;"`
    Capture result in a `-- PRE-FLIGHT AUDIT:` header comment so the SUMMARY.md can echo it.

    Create migration file `supabase/migrations/20270601200001_p29_reconcile.sql` (14-digit strict per [[reference_supabase_migration_filename_regex]]) implementing per A1, D-09, D-10:

    1. `BEGIN;`
    2. `DROP TABLE IF EXISTS public.org_subscriptions CASCADE;` — drops the P28 skeleton + any deny-all policies. CASCADE in case the EXTENSION-CONTRACT manifest comment-referenced the row.
    3. `ALTER TABLE public.subscriptions ADD COLUMN IF NOT EXISTS seats_paid integer NOT NULL DEFAULT 0;`
    4. `ALTER TABLE public.subscriptions ADD COLUMN IF NOT EXISTS seats_used integer NOT NULL DEFAULT 0;`
    5. `ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS primary_org_id uuid REFERENCES public.organizations(id) ON DELETE SET NULL;`
    6. `CREATE INDEX IF NOT EXISTS profiles_primary_org_id_idx ON public.profiles(primary_org_id) WHERE primary_org_id IS NOT NULL;` (partial index — predicate is IMMUTABLE per [[reference_supabase_migration_gotchas]]).
    7. For each of the 5 event tables (mood, sleep, symptoms, photos, vials): `CREATE INDEX IF NOT EXISTS <table>_user_id_created_at_idx ON public.<table>(user_id, created_at);` (RESEARCH Pitfall 6 — composite for `count_active_patients` UNION in Plan 29-01).
    8. Document column-level COMMENTs on `subscriptions.seats_paid`/`seats_used`: `'P29 — seat capacity / active-seat usage for clinic metered billing; deprecates org_subscriptions.seats_*'`.
    9. Document COMMENT on `profiles.primary_org_id`: `'P29 D-09 — patient default workspace context. Set by accept_org_patient_invite RPC (Plan 29-05). Null = consumer user (no org).'`
    10. `COMMIT;`

    File header comment block must list: (a) PRE-FLIGHT AUDIT result placeholder, (b) reference to CONTEXT.md A1+D-09+D-10, (c) BREAKING CHANGE note ("org_subscriptions DROPPED — downstream callers MUST use public.subscriptions WHERE clinic_id IS NOT NULL"), (d) rollback note (no rollback — Phase 14 subscriptions table is canonical going forward; if rollback needed, restore from snapshot).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20270601200001_p29_reconcile.sql && grep -c "drop table if exists public.org_subscriptions" supabase/migrations/20270601200001_p29_reconcile.sql | grep -v '^0$' && grep -c "add column if not exists seats_paid" supabase/migrations/20270601200001_p29_reconcile.sql | grep -v '^0$' && grep -c "add column if not exists primary_org_id" supabase/migrations/20270601200001_p29_reconcile.sql | grep -v '^0$' && for t in mood sleep symptoms photos vials; do grep -q "${t}_user_id_created_at_idx" supabase/migrations/20270601200001_p29_reconcile.sql || { echo "missing index for $t"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at exact filename `supabase/migrations/20270601200001_p29_reconcile.sql` (regex `^[0-9]{14}_[a-z_0-9]+\.sql$`).
    - File contains `drop table if exists public.org_subscriptions`.
    - File contains `add column if not exists seats_paid` AND `add column if not exists seats_used` against `public.subscriptions`.
    - File contains `add column if not exists primary_org_id uuid references public.organizations(id) on delete set null` against `public.profiles`.
    - File contains 5 `CREATE INDEX IF NOT EXISTS <table>_user_id_created_at_idx` entries for mood, sleep, symptoms, photos, vials.
    - File begins with PRE-FLIGHT AUDIT result block + reference to A1 / D-09 / D-10 in a header comment.
  </acceptance_criteria>
  <done>Migration drafted, audit captured, filename validates against strict 14-digit regex.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push migration to linked Supabase project</name>
  <files>
    - (no file modification — runtime DDL only)
  </files>
  <read_first>
    - supabase/migrations/20270601200001_p29_reconcile.sql (just-drafted file)
    - .planning/MEMORY references: [[reference_supabase_worktree_temp_state]], [[reference_supabase_migration_filename_regex]], [[reference_worktree_supabase_push_leak]]
  </read_first>
  <action>
    BLOCKING per phase init `schema_push_requirement`.

    1. Verify supabase CLI link state: `supabase status` — if not linked, abort + escalate.
    2. Verify migration timestamp is monotonic vs last applied: `supabase migration list --linked | tail` (last entry should be ≤ 20270601100019 from P28).
    3. Push: `SUPABASE_ACCESS_TOKEN="$(security find-generic-password -s 'SupabaseAccessToken' -w 2>/dev/null || echo "${SUPABASE_ACCESS_TOKEN}")" supabase db push --linked` (non-TTY token extraction per [[reference_supabase_keychain_token_extraction]] — required so the runner doesn't prompt for password). The `security` keychain read fails-soft to env var.
    4. Grep the push output for `^Skipping` — any skipped migrations indicate the strict-14-digit regex was violated (per [[reference_supabase_migration_filename_regex]]). Abort if seen.
    5. Post-push verify: `supabase db query --linked --query "select to_regclass('public.org_subscriptions') is null as dropped, exists(select 1 from information_schema.columns where table_name='subscriptions' and column_name='seats_paid') as has_seats_paid, exists(select 1 from information_schema.columns where table_name='profiles' and column_name='primary_org_id') as has_primary_org_id, (select count(*) from pg_indexes where tablename in ('mood','sleep','symptoms','photos','vials') and indexname like '%_user_id_created_at_idx') as event_index_count;"`
    6. Assert: `dropped=true`, `has_seats_paid=true`, `has_primary_org_id=true`, `event_index_count=5`.
    7. Per [[reference_worktree_supabase_push_leak]]: if this plan runs in a worktree, ensure the .sql file ALSO exists in the main checkout BEFORE the push (Plan 29-00 should NOT run under a worktree — pin to main checkout). If a worktree was used, copy the migration file to main and re-run the push there.
  </action>
  <verify>
    <automated>supabase db query --linked --query "select to_regclass('public.org_subscriptions') is null as a, exists(select 1 from information_schema.columns where table_name='subscriptions' and column_name='seats_paid') as b, exists(select 1 from information_schema.columns where table_name='subscriptions' and column_name='seats_used') as c, exists(select 1 from information_schema.columns where table_name='profiles' and column_name='primary_org_id') as d, (select count(*) from pg_indexes where tablename in ('mood','sleep','symptoms','photos','vials') and indexname like '%_user_id_created_at_idx') as e;" 2>&1 | tee /tmp/p29_00_verify.txt && grep -E 'a +\| b +\| c +\| d +\| e' -A 2 /tmp/p29_00_verify.txt | grep -E 't +\| t +\| t +\| t +\| 5'</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list --linked` shows `20270601200001_p29_reconcile` in the Remote column (i.e., applied).
    - The post-push verify query returns: `a=t, b=t, c=t, d=t, e=5` (org_subscriptions dropped; seats_paid+seats_used present on subscriptions; primary_org_id present on profiles; all 5 event-table composite indexes present).
    - Push stdout contains zero `^Skipping` lines.
    - No worktree split-state: the migration file is byte-identical between any worktree and main checkout.
  </acceptance_criteria>
  <done>Migration applied to linked project; live schema matches CONTEXT D-09/D-10 expectations; Wave 1 downstream plans unblocked.</done>
</task>

<task type="auto">
  <name>Task 3: Write Plan 00 SUMMARY + commit</name>
  <files>
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-00-SUMMARY.md
  </files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template)
    - supabase/migrations/20270601200001_p29_reconcile.sql (the file you just shipped)
    - /tmp/p29_00_verify.txt (post-push query output captured in Task 2)
  </read_first>
  <action>
    Write `.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-00-SUMMARY.md` per the GSD SUMMARY template:
      - Frontmatter: phase=29, plan=00, status=complete, files_modified, tests_added=0 (Wave 0 test scaffolds are in Plan 29-01)
      - Sections: Objective, What Shipped (migration + 5 indexes + drop), Verification Evidence (paste /tmp/p29_00_verify.txt), Decisions Honored (A1, D-09, D-10), Carry-Forward (Plan 29-01 owns count_active_patients v2; downstream plans use `subscriptions WHERE clinic_id IS NOT NULL`), Risks/Notes (org_subscriptions DROP is irreversible — no rollback path).

    Git commit with pathspec: `git add supabase/migrations/20270601200001_p29_reconcile.sql .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-00-SUMMARY.md && git commit -m "feat(29-00): RECONCILE — drop org_subscriptions, extend subscriptions seats, add profiles.primary_org_id"` (per [[feedback_parallel_executor_git_isolation]] use explicit pathspec).
  </action>
  <verify>
    <automated>test -f .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-00-SUMMARY.md && grep -q "## Verification Evidence" .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-00-SUMMARY.md && git log --oneline -1 | grep -q "29-00"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY.md exists with template sections including Verification Evidence quoting the actual post-push query output.
    - Git HEAD message starts with `feat(29-00)` or similar conforming to repo convention.
    - SUMMARY frontmatter has `status: complete` and lists exactly the files this plan modified.
  </acceptance_criteria>
  <done>Plan 29-00 SUMMARY committed; phase metadata reflects Wave 0 complete; downstream plans unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → live DB | Schema mutation crossing into production data plane |
| supabase CLI → keychain | macOS Keychain token extraction for non-TTY push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-00-01 | Tampering | Migration file (drops a table) | mitigate | DROP IF EXISTS + audit-captured pre-flight asserts table is the P28 skeleton (zero rows) before drop; CASCADE limited to known dependent objects (deny-all RLS policies). |
| T-29-00-02 | Denial of Service | (user_id, created_at) composite indexes on 5 large event tables | mitigate | CREATE INDEX IF NOT EXISTS without CONCURRENTLY — acceptable because v1.3 patient scale is <200 patients/org; CONCURRENTLY adds migration-transaction complexity. At larger scale, switch to CONCURRENTLY in a separate migration. |
| T-29-00-03 | Information Disclosure | profiles.primary_org_id FK exposes org membership to anyone with read on profiles | accept | profiles.primary_org_id is a nullable uuid; Phase 9 profiles RLS already restricts SELECT to `auth.uid() = id` for the patient's own row and admin RPCs. No new disclosure surface. |
| T-29-00-04 | Repudiation | Schema change with no audit row | accept | Migrations are version-controlled in git + supabase_migrations.schema_migrations live table; no separate audit needed for DDL per Phase 24 D-17 (audit is for DML, not DDL). |
</threat_model>

<verification>
- Post-push DB query returns expected booleans + index count (Task 2 automated).
- Filename regex (Task 1 automated).
- SUMMARY.md exists + committed (Task 3 automated).
- No silent migration skip (Task 2 grep-gate for `^Skipping`).
- Live schema diff against P28 baseline shows exactly: org_subscriptions dropped, subscriptions +2 columns, profiles +1 column, 5 new indexes.
</verification>

<success_criteria>
- `supabase migration list --linked` lists `20270601200001_p29_reconcile` as applied.
- `select to_regclass('public.org_subscriptions')` returns NULL.
- `\d subscriptions` shows `seats_paid integer default 0` and `seats_used integer default 0`.
- `\d profiles` shows `primary_org_id uuid references organizations(id) on delete set null`.
- `select indexname from pg_indexes where tablename in ('mood','sleep','symptoms','photos','vials') and indexname like '%_user_id_created_at_idx'` returns 5 rows.
- SUMMARY.md committed; Plan 29-01..29-07 dependencies unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-00-SUMMARY.md` with the GSD SUMMARY template (Objective, What Shipped, Verification Evidence, Decisions Honored, Carry-Forward, Risks).
</output>
