---
phase: 29
plan: 01
type: execute
wave: 1
depends_on: ["29-00"]
files_modified:
  - supabase/migrations/20270601200002_count_active_patients_v2.sql
  - src/lib/__tests__/count-active-patients.test.ts
  - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-01-SUMMARY.md
autonomous: true
requirements: [ORG-09]
tags: [supabase, migration, secdef, billing, metered]
must_haves:
  truths:
    - "count_active_patients(org_id uuid) SECDEF function exists and returns an integer count of distinct patient_user_ids with any event in the last 30 days"
    - "Function UNIONs across all 10 patient-data event tables: injections, weights, meals, mood, sleep, symptoms, photos, workouts, vials, ai_messages"
    - "Function uses org_patient_links (Phase 28 schema) — NOT the legacy Phase 9 memberships table"
    - "Function explicit search_path = pg_catalog, public, extensions"
    - "Service-role caller (auth.uid() IS NULL) bypasses the admin check — required for the nightly cron in Plan 29-04"
    - "Non-service-role caller without admin role on the target org raises 42501"
    - "Vitest count-active-patients.test.ts proves: 0 for org with no patients, N for org with N active patients, 0 for inactive patients (>30 days since last event), service-role bypass works, non-admin caller raises 42501"
  artifacts:
    - path: "supabase/migrations/20270601200002_count_active_patients_v2.sql"
      provides: "CREATE OR REPLACE count_active_patients SECDEF function with full 10-table UNION + service_role bypass + admin check"
      contains: "create or replace function public.count_active_patients"
    - path: "src/lib/__tests__/count-active-patients.test.ts"
      provides: "Vitest unit test covering D-01 invariants (count correctness, bypass, RLS)"
      contains: "describe.*count_active_patients"
  key_links:
    - from: "supabase/migrations/20270601200002_count_active_patients_v2.sql"
      to: "Phase 28 org_patient_links table"
      via: "FROM public.org_patient_links opl WHERE opl.org_id = p_org_id AND opl.unlinked_at IS NULL"
      pattern: "from public.org_patient_links"
    - from: "supabase/migrations/20270601200002_count_active_patients_v2.sql"
      to: "10 event tables (injections, weights, meals, mood, sleep, symptoms, photos, workouts, vials, ai_messages)"
      via: "UNION ALL ... WHERE created_at > now() - interval '30 days'"
      pattern: "interval '30 days'"
---

<objective>
Replace Phase 14's obsolete `count_active_patients(p_clinic_id)` (memberships-based, 5-table UNION) with the P29 D-01 specification: switch source to `org_patient_links`, extend UNION to all 10 patient-data event tables, retain service-role bypass for the nightly cron caller (auth.uid() IS NULL).

Purpose: ORG-09 metered billing depends on this function being accurate. The nightly cron (Plan 29-04) calls this function per org to derive the meter event `value`. Wrong source table = zero billing; wrong UNION = silent under/over billing.

Output: single migration `20270601200002_count_active_patients_v2.sql` + vitest unit test `src/lib/__tests__/count-active-patients.test.ts` proving the four invariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-RESEARCH.md
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-00-SUMMARY.md

<interfaces>
From supabase/migrations/20260601000019_stripe_subscriptions.sql (Phase 14 obsolete signature — being replaced):
```sql
create or replace function public.count_active_patients(p_clinic_id uuid) returns integer
  language plpgsql security definer ...
  -- joins memberships + roles.name = 'View-only' (Phase 9 schema)
  -- UNIONs 5 tables only
```

From supabase/migrations/20270601100009_org_patient_links_table.sql (Phase 28):
  - public.org_patient_links (org_id uuid, patient_user_id uuid, linked_by uuid, linked_at timestamptz, unlinked_at timestamptz null, consent_grant_id uuid)
  - Index: org_patient_links_org_id_patient_user_id_idx
  - RLS: deny-all for authenticated; SECDEF RPCs only

From supabase/migrations/20270601100004_org_members_table.sql:
  - public.org_members (org_id, user_id, role org_member_role) where org_member_role = enum('admin','clinician','staff')

From src/lib/__tests__/_fixtures/p28-rls-fixture.ts (verified):
  - exports: SHOULD_RUN, createTwoOrgsTwoUsers, sessionFor, cleanupByPrefix, makeSlugPrefix
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author count_active_patients v2 CREATE OR REPLACE migration</name>
  <files>
    - supabase/migrations/20270601200002_count_active_patients_v2.sql
  </files>
  <read_first>
    - supabase/migrations/20260601000019_stripe_subscriptions.sql §count_active_patients (the obsolete Phase 14 version being replaced — read the full function body to understand the bypass + admin check pattern)
    - supabase/migrations/20270601100009_org_patient_links_table.sql (canonical org_patient_links shape)
    - supabase/migrations/20270601100004_org_members_table.sql (org_members + org_member_role enum)
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md §D-01 (exact UNION spec)
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-RESEARCH.md §Pattern 3 (Pattern 3 code is the verbatim target — replicate exactly)
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-00-SUMMARY.md (Plan 29-00 verified that all 10 event tables have (user_id, created_at) indexes)
  </read_first>
  <behavior>
    - Test 1: count_active_patients(org_with_3_active_patients) returns 3
    - Test 2: count_active_patients(org_with_0_patients) returns 0
    - Test 3: Patient with last event >30 days ago is NOT counted (excluded by `created_at > now() - interval '30 days'`)
    - Test 4: Patient with unlinked_at IS NOT NULL is NOT counted
    - Test 5: Same patient with events in multiple event tables counts as 1 (distinct on patient_user_id)
    - Test 6: Service-role caller (no JWT) succeeds for any org_id (bypass)
    - Test 7: Authenticated non-admin caller raises 42501 "forbidden"
    - Test 8: Authenticated admin of org A querying org A succeeds
    - Test 9: Authenticated admin of org A querying org B raises 42501
  </behavior>
  <action>
    Author `supabase/migrations/20270601200002_count_active_patients_v2.sql` per D-01 + RESEARCH Pattern 3 verbatim, with these exact constraints:

    1. `CREATE OR REPLACE FUNCTION public.count_active_patients(p_org_id uuid) RETURNS integer`
       - Parameter renamed from `p_clinic_id` → `p_org_id` (semantic alignment with Phase 28 nomenclature; the parameter is still a clinic/organization id, this is purely a rename).
       - `LANGUAGE plpgsql`, `SECURITY DEFINER`, `STABLE`.
       - `SET search_path = pg_catalog, public, extensions` (per [[reference_supabase_migration_gotchas]] — required for SECDEF).
    2. Function body — Pattern 3 from 29-RESEARCH.md verbatim:
       - Declare `v_uid uuid := auth.uid()` + `v_count integer`.
       - If `v_uid IS NOT NULL`: query `public.org_members` for (org_id = p_org_id AND user_id = v_uid AND role = 'admin'); if not found, `RAISE EXCEPTION 'forbidden' USING ERRCODE = '42501'`.
       - SELECT count(distinct opl.patient_user_id)::integer INTO v_count FROM public.org_patient_links opl WHERE opl.org_id = p_org_id AND opl.unlinked_at IS NULL AND EXISTS ( <UNION ALL of 10 sub-selects, each `SELECT 1 FROM public.<table> x WHERE x.user_id = opl.patient_user_id AND x.created_at > now() - interval '30 days'`> ).
       - The 10 tables in exact order: injections, weights, meals, mood, sleep, symptoms, photos, workouts, vials, ai_messages.
       - RETURN coalesce(v_count, 0).
    3. `REVOKE ALL ON FUNCTION public.count_active_patients(uuid) FROM public;`
    4. `GRANT EXECUTE ON FUNCTION public.count_active_patients(uuid) TO authenticated;` (admin-callable from clinic billing UI debug surfaces).
    5. `GRANT EXECUTE ON FUNCTION public.count_active_patients(uuid) TO service_role;` (cron caller).
    6. Drop the OLD signature explicitly to avoid ambiguity:
       `DROP FUNCTION IF EXISTS public.count_active_patients(p_clinic_id uuid);` — only effective if Postgres treats them as distinct; if signatures collide on `(uuid)`, CREATE OR REPLACE handles it. Add this DROP defensively at the head — Postgres will skip if absent.
    7. COMMENT ON FUNCTION: `'P29 D-01 — Returns count of distinct patient_user_ids in org_patient_links with any event in last 30 days. Service-role bypass for cron (Plan 29-04). Replaces Phase 14 memberships-based version.'`

    NEVER include PHI in the function body (no patient names, dose values — count only). PHI-lint (Plan 29-07) will grep this file.

    Plan-checker BLOCKER per D-01: if ANY new patient-data table is added in v1.4+, this function MUST be extended via a follow-up migration. Add a `-- FUTURE: extend UNION when new patient-data tables added` comment marker for grep visibility.
  </behavior>
  <verify>
    <automated>test -f supabase/migrations/20270601200002_count_active_patients_v2.sql && grep -c "create or replace function public.count_active_patients" supabase/migrations/20270601200002_count_active_patients_v2.sql | grep -v '^0$' && grep -c "from public.org_patient_links" supabase/migrations/20270601200002_count_active_patients_v2.sql | grep -v '^0$' && for t in injections weights meals mood sleep symptoms photos workouts vials ai_messages; do grep -q "public.${t}" supabase/migrations/20270601200002_count_active_patients_v2.sql || { echo "missing table $t in UNION"; exit 1; }; done && grep -c "set search_path = pg_catalog, public, extensions" supabase/migrations/20270601200002_count_active_patients_v2.sql | grep -v '^0$' && grep -cv "memberships\|roles.name" supabase/migrations/20270601200002_count_active_patients_v2.sql | grep -v '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at `supabase/migrations/20270601200002_count_active_patients_v2.sql`.
    - Contains `create or replace function public.count_active_patients(p_org_id uuid)`.
    - Contains `set search_path = pg_catalog, public, extensions`.
    - Contains all 10 event-table references: `public.injections`, `public.weights`, `public.meals`, `public.mood`, `public.sleep`, `public.symptoms`, `public.photos`, `public.workouts`, `public.vials`, `public.ai_messages` (each at least once).
    - Contains `from public.org_patient_links` (NOT `from public.memberships`).
    - Contains `interval '30 days'`.
    - Contains `auth.uid()` null-check pattern (service-role bypass).
    - Contains `errcode = '42501'`.
    - Does NOT contain `memberships` or `roles.name` (RESEARCH Pitfall 1 negative assertion — strip comments first per [[reference_grep_gate_comment_strip]]: `grep -v '^\s*--' supabase/migrations/20270601200002_count_active_patients_v2.sql | grep -cE 'memberships|roles\.name'` MUST return 0).
  </acceptance_criteria>
  <done>Function migration drafted, plan-checker BLOCKER negative assertions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Push migration + author vitest unit test covering D-01 invariants</name>
  <files>
    - src/lib/__tests__/count-active-patients.test.ts
  </files>
  <read_first>
    - src/lib/__tests__/_fixtures/p28-rls-fixture.ts (createTwoOrgsTwoUsers, sessionFor, cleanupByPrefix, makeSlugPrefix, SHOULD_RUN — verified via file read at planning time)
    - src/lib/__tests__/rls-org-members.test.ts (closest analog file — read full file to copy import shape + describeIfLive pattern + afterAll cleanup)
    - vitest-e2e.config.ts (the config flag this test runs against)
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-RESEARCH.md §Validation Architecture (test command)
  </read_first>
  <action>
    1. Push the migration: `supabase db push --linked` (per [[reference_worktree_supabase_push_leak]] — if running under worktree, ensure file is in main checkout first; this plan's frontmatter implies main-checkout execution). Capture push output.

    2. Verify post-push:
       `supabase db query --linked --query "select proname, pg_get_function_identity_arguments(oid) as args from pg_proc where proname = 'count_active_patients';"`
       Assert: exactly one row, args = `p_org_id uuid`.

    3. Author `src/lib/__tests__/count-active-patients.test.ts` using p28-rls-fixture per the existing rls-org-members.test.ts pattern:
       - `import path from 'node:path'`, file-scoped `TEST_SLUG_PREFIX = makeSlugPrefix(path.basename(__filename))` per [[feedback_rls_per_file_slug_prefix]].
       - `import { afterAll, beforeAll, describe, expect, it } from 'vitest'`.
       - `import { SHOULD_RUN, cleanupByPrefix, createTwoOrgsTwoUsers, makeSlugPrefix, sessionFor } from './_fixtures/p28-rls-fixture'`.
       - `const describeIfLive = SHOULD_RUN ? describe : describe.skip`.
       - Top-level `describeIfLive('P29 count_active_patients — D-01 invariants', () => { ... })`.

    4. Fixture setup in `beforeAll`:
       - Call `createTwoOrgsTwoUsers(TEST_SLUG_PREFIX)` → gives `{orgX, orgY, sessA (admin of X), sessB (admin of Y)}`.
       - Get an admin (service-role) client via the fixture helper (or `_createServiceRoleClientUnsafe` indirectly via raw supabase-js with SUPABASE_SERVICE_ROLE_KEY env var — same pattern as rls-org-members.test.ts).
       - Seed 3 patients in orgX: create auth users via admin API with TEST_SLUG_PREFIX'd emails; insert into `org_patient_links` with `org_id=orgX`, `linked_by=fixture.userA.id`, `linked_at=now()`, `unlinked_at=null`.
       - Seed events: patient1 has 1 injection (created_at = now()); patient2 has 1 weight + 1 mood (both created_at = now()); patient3 has 1 ai_message (created_at = now() - interval '40 days') — i.e., INACTIVE.

    5. Tests (one `it` per behavior row above):
       - it('returns 2 for orgX with 2 active patients out of 3 linked', async () => `const {data} = await admin.rpc('count_active_patients', {p_org_id: fixture.orgX}); expect(data).toBe(2);`)
       - it('returns 0 for orgY with 0 patients', async () => `const {data} = await admin.rpc('count_active_patients', {p_org_id: fixture.orgY}); expect(data).toBe(0);`)
       - it('counts patient with events in multiple tables as 1', `seed extra mood for patient1; expect count still = 2 (not 3)`)
       - it('excludes patient with unlinked_at set', `set patient1.unlinked_at = now(); expect count = 1`)
       - it('service-role bypass works', `admin client → succeeds without admin check`)
       - it('non-admin authenticated user querying own org raises 42501', `userB.client.rpc('count_active_patients', {p_org_id: orgY})` where userB is admin of orgY — wait, that's admin. Use a NON-admin user via fixture extension, OR use sessA querying orgY — sessA is admin of X not Y → expects 42501).
       - it('admin querying foreign org raises 42501', `sessA.client.rpc('count_active_patients', {p_org_id: orgY})` → expect error.code === '42501' OR `error.message.includes('forbidden')`).

    6. `afterAll(() => cleanupByPrefix(TEST_SLUG_PREFIX))`.

    7. Run: `npx vitest run src/lib/__tests__/count-active-patients.test.ts --config vitest-e2e.config.ts` — all tests pass.

    8. Commit (pathspec per [[feedback_parallel_executor_git_isolation]]): `git add supabase/migrations/20270601200002_count_active_patients_v2.sql src/lib/__tests__/count-active-patients.test.ts && git commit -m "feat(29-01): count_active_patients v2 — 10-table UNION + org_patient_links + service-role bypass + tests"`.
  </action>
  <verify>
    <automated>SUPABASE_ACCESS_TOKEN="${SUPABASE_ACCESS_TOKEN}" supabase db query --linked --query "select proname from pg_proc where proname='count_active_patients';" 2>&1 | grep -q count_active_patients && npx vitest run src/lib/__tests__/count-active-patients.test.ts --config vitest-e2e.config.ts --reporter=basic 2>&1 | tee /tmp/p29_01_test.txt | grep -E '^Tests +[0-9]+ passed' && ! grep -E '(failed|FAIL)' /tmp/p29_01_test.txt</automated>
  </verify>
  <acceptance_criteria>
    - Migration applied: `supabase migration list --linked | grep -q 20270601200002_count_active_patients_v2`.
    - Test file exists at `src/lib/__tests__/count-active-patients.test.ts`.
    - Test file imports from `./_fixtures/p28-rls-fixture` with `SHOULD_RUN, cleanupByPrefix, createTwoOrgsTwoUsers, makeSlugPrefix, sessionFor`.
    - Test file declares `TEST_SLUG_PREFIX = makeSlugPrefix(path.basename(__filename))` (file-scoped per [[feedback_rls_per_file_slug_prefix]]).
    - All 7+ test cases pass in vitest output (no "failed" or "FAIL" lines in /tmp/p29_01_test.txt).
    - Git commit landed with both files in the same commit (pathspec discipline per [[feedback_parallel_executor_git_isolation]]).
  </acceptance_criteria>
  <done>Function shipped + verified live; unit tests prove D-01 behavior invariants; ORG-09 count primitive done.</done>
</task>

<task type="auto">
  <name>Task 3: Write Plan 01 SUMMARY + commit</name>
  <files>
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-01-SUMMARY.md
  </files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - /tmp/p29_01_test.txt (vitest output from Task 2)
    - supabase/migrations/20270601200002_count_active_patients_v2.sql
  </read_first>
  <action>
    Write `.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-01-SUMMARY.md` per the GSD SUMMARY template:
      - Frontmatter: phase=29, plan=01, status=complete, files_modified, tests_added=N (count actual `it` blocks)
      - Sections: Objective, What Shipped (migration + UNION extension + service-role bypass + test file), Verification Evidence (paste /tmp/p29_01_test.txt summary line), Decisions Honored (D-01), Carry-Forward (Plan 29-04 cron calls this function; Plan 29-07 PHI-lint will grep this migration), Risks/Notes (D-01 plan-checker BLOCKER: future patient-data tables MUST extend UNION).

    Commit: `git add .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-01-SUMMARY.md && git commit -m "docs(29-01): SUMMARY — count_active_patients v2"`.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-01-SUMMARY.md && grep -q "## Verification Evidence" .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-01-SUMMARY.md && git log --oneline -1 | grep -q "29-01"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY.md exists with template sections including Verification Evidence (paste of vitest pass-line).
    - Frontmatter `tests_added` matches actual `it` count.
    - Carry-Forward names Plan 29-04 + Plan 29-07 explicitly.
  </acceptance_criteria>
  <done>SUMMARY committed; Plan 29-04 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron Edge Function → DB function | Service-role call crossing into PHI-aggregating SQL |
| Clinic admin browser → RPC | Authenticated call to count their own org's patients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Elevation of Privilege | count_active_patients called by non-admin authenticated user | mitigate | SECDEF re-checks org_members.role = 'admin' for caller (Pattern S1 dual-layer); test 9 proves cross-org admin gets 42501. |
| T-29-01-02 | Information Disclosure | Returning a count leaks aggregate org size | accept | Count is non-PHI (no patient identifiers); aggregate counts are routinely surfaced in clinic billing UIs. |
| T-29-01-03 | Tampering | Manipulating `created_at` to inflate counts | accept | created_at columns are server-defaulted; tampering requires direct DB write which RLS prevents for authenticated callers. |
| T-29-01-04 | Denial of Service | UNION across 10 tables × distinct count on large orgs | mitigate | Plan 29-00 ships (user_id, created_at) composite indexes on all 10 tables; STABLE function declaration allows planner to cache within a query. At v1.3 scale (<200 patients/org), expected <200ms per call. |
</threat_model>

<verification>
- Migration filename regex (Task 1 automated).
- Negative assertions (no `memberships`, no `roles.name`) (Task 1 automated).
- All 10 tables present in UNION (Task 1 automated).
- Live DB has the function with correct signature (Task 2 automated).
- 7+ behavior tests pass against live DB (Task 2 automated).
- SUMMARY committed (Task 3 automated).
</verification>

<success_criteria>
- `select count_active_patients('<orgX_with_2_active_patients>')` returns 2.
- Authenticated non-admin caller raises 42501.
- Service-role caller bypasses admin check.
- All UNION sub-selects use (user_id, created_at) indexes (verified by EXPLAIN if needed — not gated in this plan; cron perf checked at Plan 29-04).
- Vitest test file exists with file-scoped slug prefix.
</success_criteria>

<output>
After completion, create `.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-01-SUMMARY.md`.
</output>
