---
phase: 37-m6-helpdesk-core
plan: 09
type: execute
wave: 8
depends_on: [37-01-PLAN.md, 37-02-PLAN.md, 37-03-PLAN.md, 37-04-PLAN.md, 37-05-PLAN.md, 37-06-PLAN.md, 37-07-PLAN.md, 37-08-PLAN.md]
files_modified:
  - leanshot/src/test/rls-helpdesk-tickets.test.ts
  - leanshot/src/test/rls-helpdesk-kb.test.ts
  - leanshot/.planning/phases/37-m6-helpdesk-core/uat-runbook.md
autonomous: false
requirements: [HELP-01, HELP-02, HELP-03, HELP-04, HELP-05, HELP-06, HELP-07, HELP-08, HELP-09, HELP-10, HELP-11, HELP-12, HELP-13]
tags: [helpdesk, rls, e2e, uat, vendor, resend, closeout]

must_haves:
  truths:
    - "RLS cross-tenant impersonation proof tests cover every helpdesk table"
    - "User A cannot SELECT tickets where user_id = User B"
    - "Org A agent cannot SELECT tickets where org_id = Org B"
    - "PHI ticket agent-open fires log_phi_access row (verified end-to-end)"
    - "Function Secrets set for all 4 Phase 37 Edge Fns + helpdesk-agent-reply-send"
    - "Resend Inbound MX records configured for app.leanshot.app (human-UAT checkpoint)"
    - "Resend Inbound webhook endpoint pointed to /functions/v1/helpdesk-inbound"
    - "Resend Webhook Secret (RESEND_WEBHOOK_SECRET) copied from dashboard to Function Secrets"
    - "End-to-end smoke: send test email to support@app.leanshot.app → ticket appears in admin inbox"
  artifacts:
    - path: "leanshot/src/test/rls-helpdesk-tickets.test.ts"
      provides: "Live-DB RLS impersonation matrix for tickets / messages / attachments / ai_suggestions"
      min_lines: 150
    - path: "leanshot/src/test/rls-helpdesk-kb.test.ts"
      provides: "Live-DB RLS proof for kb_articles + kb_article_versions + macros + routing + sla"
      min_lines: 80
    - path: "leanshot/.planning/phases/37-m6-helpdesk-core/uat-runbook.md"
      provides: "Operator UAT runbook for Function Secrets + Resend Inbound MX + e2e smoke"
  key_links:
    - from: "RLS proof tests"
      to: "live Supabase project"
      via: "supabase service-role JWT for fixture creation; admin.generateLink + /auth/v1/verify for user JWTs"
      pattern: "rls-helpdesk"
    - from: "Resend Inbound MX"
      to: "helpdesk-inbound Edge Fn webhook"
      via: "Resend dashboard → Receiving → Webhook URL"
      pattern: "helpdesk-inbound"
---

<objective>
Phase 37 closeout — three things land in this plan:

1. **RLS cross-tenant impersonation proof tests** (per `[[reference_supabase_project.md]]` rule: every RLS surface gets a live cross-tenant impersonation proof test). Two new live-DB test files covering all helpdesk tables.

2. **Operator-facing UAT runbook** documenting the Function Secrets that must be set, the Resend Inbound dashboard config (DNS MX records + webhook endpoint + webhook secret), and the manual end-to-end smoke procedure.

3. **Three blocking human checkpoints**:
   - Function Secrets configuration (operator runs `supabase secrets set` — Claude can prepare the exact commands but secrets values come from external dashboards).
   - Resend Inbound MX + webhook configuration (per RESEARCH Open Question 2 — must be done in Resend dashboard).
   - End-to-end e2e smoke: send real email → confirm ticket created → reply via widget → confirm CSAT delivered post-close.

Output: 2 test files + 1 runbook + 3 human checkpoints. No new code files outside tests.

Per [[feedback_verify_human_uat_via_cli]] — fetch as many secrets as possible via CLI rather than asking the operator to paste. Specifically: `supabase secrets list` to confirm, `supabase db query --linked` for any DB-side verification. Reserve the human checkpoint for actions that genuinely lack CLI paths (Resend dashboard MX setup, email-send smoke).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-m6-helpdesk-core/37-CONTEXT.md
@.planning/phases/37-m6-helpdesk-core/37-RESEARCH.md
@.planning/phases/37-m6-helpdesk-core/37-PATTERNS.md
@.planning/phases/37-m6-helpdesk-core/deferred-items.md
@leanshot/src/lib/rag/__tests__/rls-matrix.test.ts
@leanshot/vitest-e2e.config.ts

<interfaces>
Function Secrets to set (consolidated across all helpdesk Edge Fns):
  HELPDESK_HMAC_SECRET           — copy from `vault.decrypted_secrets` WHERE name='helpdesk_hmac_secret'
  HELPDESK_CSAT_SIGNING_SECRET   — fresh `openssl rand -hex 32`
  RESEND_API_KEY                 — Resend dashboard → API keys (existing key reused; already present for Resend outbound — verify)
  RESEND_WEBHOOK_SECRET          — Resend dashboard → Webhooks → endpoint → "Signing Secret"
  SLA_BREACH_DEFAULT_ONCALL_EMAILS — comma-separated; ops team distribution list
  ANTHROPIC_MODEL_HELPDESK       — `claude-sonnet-4-6`
  ANTHROPIC_API_KEY              — existing consumer credential
  ANTHROPIC_API_KEY_BAA          — existing BAA credential (set in Phase 25)
  ANTHROPIC_API_BASE             — `https://api.anthropic.com` (default; only set if overriding)

CLI commands (run from /Users/karstenhaldan/minisite):
  supabase secrets set HELPDESK_HMAC_SECRET=<value> --project-ref ytnsipxxmzgaebkqmokp
  supabase secrets list --project-ref ytnsipxxmzgaebkqmokp
  supabase db query --linked "select name from vault.secrets where name = 'helpdesk_hmac_secret'"

Resend Inbound dashboard steps (per RESEARCH Open Question 2):
  1. Resend dashboard → Domains → app.leanshot.app → Receiving → Enable
  2. Copy provided MX record values (e.g., `feedback-smtp.us-east-1.resend.com` priority 10)
  3. Add MX records to DNS provider (Vercel domains panel for app.leanshot.app)
  4. Wait for MX verification (~5-15 min)
  5. Resend dashboard → Webhooks → "Add endpoint" → URL = `https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/helpdesk-inbound`
  6. Subscribe to event: `email.received`
  7. Copy "Signing Secret" from the new webhook entry → set as RESEND_WEBHOOK_SECRET via supabase secrets set

End-to-end smoke checklist:
  a. From an external mailbox (not the test sender), send to `support@app.leanshot.app` with subject "Phase 37 smoke" + body "this is a test"
  b. Within 30s: agent inbox at `/admin/helpdesk/inbox` shows new ticket
  c. AI suggestion pane populates within 60s (Claude classify latency)
  d. Agent inserts draft + sends reply → user's mailbox receives reply with `Reply-To: reply+<token>@app.leanshot.app`
  e. User replies to that email → ticket thread shows the user's reply within 60s (Resend Inbound → helpdesk-inbound idempotent path)
  f. Agent clicks "Close ticket" → user's mailbox receives CSAT survey email
  g. Wait 5+ min past p3 SLA (or manually back-date `tickets.created_at` to >7d ago) → confirm sla_breach_alert email arrives at ops list
  h. PHI smoke: as a clinician user, repeat (a)-(c) — verify `phi_access_log` row appears when agent opens the ticket

Pre-existing reference tests for analog:
  leanshot/src/lib/rag/__tests__/rls-matrix.test.ts — cross-tenant pattern; per-file slug prefix; admin.generateLink + /auth/v1/verify fixture (NOT signInWithPassword per [[reference_rls_fixture_gotrueclient_flake]])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RLS impersonation proof test — tickets / messages / attachments / ai_suggestions</name>
  <files>leanshot/src/test/rls-helpdesk-tickets.test.ts</files>
  <action>
Model on `leanshot/src/lib/rag/__tests__/rls-matrix.test.ts`. File-scoped slug prefix (per [[feedback_rls_per_file_slug_prefix]]). Use `admin.generateLink({ type: 'magiclink', ... })` + `/auth/v1/verify?token_hash=...` to mint user JWTs (per [[reference_rls_fixture_gotrueclient_flake]] — NOT `signInWithPassword`).

**File header:**
```typescript
import { describe, beforeAll, afterAll, it, expect } from 'vitest';
import { createClient, type SupabaseClient } from '@supabase/supabase-js';

const TEST_SLUG_PREFIX = `p37-rls-tickets-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 6)}`;

const SUPABASE_URL = process.env.VITE_SUPABASE_URL ?? process.env.SUPABASE_URL;
const SUPABASE_ANON_KEY = process.env.VITE_SUPABASE_ANON_KEY ?? process.env.SUPABASE_ANON_KEY;
const SUPABASE_SERVICE_ROLE_KEY = process.env.SUPABASE_SERVICE_ROLE_KEY;
const SHOULD_RUN = Boolean(SUPABASE_URL && SUPABASE_ANON_KEY && SUPABASE_SERVICE_ROLE_KEY);
const describeIfLive = SHOULD_RUN ? describe : describe.skip;

function getAdmin(): SupabaseClient {
  return createClient(SUPABASE_URL!, SUPABASE_SERVICE_ROLE_KEY!, { auth: { autoRefreshToken: false, persistSession: false }});
}
// User-client mint pattern (from rls-matrix.test.ts) — admin.generateLink + verify via PLAIN fetch.
async function mintUserClient(admin: SupabaseClient, email: string, password: string, storageKey: string): Promise<SupabaseClient> {
  const { data: link } = await admin.auth.admin.generateLink({ type: 'magiclink', email });
  if (!link?.properties?.hashed_token) throw new Error('no magic link token');
  const verifyRes = await fetch(`${SUPABASE_URL}/auth/v1/verify?token=${link.properties.hashed_token}&type=magiclink`, {
    headers: { 'apikey': SUPABASE_ANON_KEY! }, redirect: 'manual',
  });
  // extract access_token from the redirect Location fragment
  const loc = verifyRes.headers.get('location') ?? '';
  const accessToken = new URL(loc).hash.match(/access_token=([^&]+)/)?.[1];
  if (!accessToken) throw new Error('no access token in redirect');
  return createClient(SUPABASE_URL!, SUPABASE_ANON_KEY!, {
    auth: { autoRefreshToken: false, persistSession: false, storageKey },
    global: { headers: { Authorization: `Bearer ${accessToken}` }},
  });
}
```

**Test fixtures** (in `beforeAll`):
- 2 orgs (`org_a`, `org_b`).
- 4 users: `userA_owner`, `userA_patient`, `userB_owner`, `userB_patient`. Each linked to their org via `org_members`.
- 1 ticket per (org × patient), 1 ticket_message per ticket, 1 ticket_attachment per ticket, 1 ticket_ai_suggestion per ticket.
- All slugs/names prefixed with `TEST_SLUG_PREFIX` for cleanup.

**Cleanup** (in `afterAll`):
- DELETE ai_suggestions / attachments / messages / tickets / org_members / org rows WHERE org/user/subject matches `TEST_SLUG_PREFIX`. Use service-role client.

**Test cases** (in `describeIfLive`):

1. **Patient cross-user**: `userA_patient` SELECT tickets returns ONLY their own ticket; cannot see `userB_patient.id` or `userA_owner.id` tickets even within same org.
2. **Patient cross-org**: `userA_patient` cannot see any `org_b` ticket (RLS denies).
3. **Agent within org**: `userA_owner` (org_a member) sees BOTH `userA_patient` ticket AND any other org_a tickets; cannot see org_b tickets.
4. **Agent cross-org**: `userA_owner` cannot SELECT tickets where `org_id = org_b`.
5. **Append-only ticket_messages**: `userA_owner` attempts UPDATE on a ticket_message → fails (no policy grants UPDATE to authenticated).
6. **Append-only ticket_messages**: service-role attempts UPDATE → also fails because `revoke update from service_role` (Plan 01 Task 1).
7. **ticket_attachments cross-tenant**: `userA_owner` cannot SELECT attachments tied to org_b tickets.
8. **ticket_ai_suggestions agent-only**: `userA_patient` cannot SELECT ai_suggestions for their own ticket (user-side has no SELECT policy on this table); `userA_owner` can.
9. **PHI ticket open fires log_phi_access**: as `userA_owner`, call `supabase.rpc('log_phi_access', { ... })` (or simulate the client-side fire-and-forget); after the call, service-role SELECT on `phi_access_log` returns 1 new row.
10. **User cannot transition ticket to 'closed' status**: `userA_patient` UPDATE ticket → status='closed' → fails (RLS policy WHERE clause `status in ('open','pending','waiting_on_customer','resolved')`).

Each `it.skip` if SHOULD_RUN is false; the `describeIfLive` already handles this top-level.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; test -f src/test/rls-helpdesk-tickets.test.ts &amp;&amp; npx tsc -p tsconfig.app.json --noEmit &amp;&amp; npm run test:e2e:rls -- --run --testPathPattern rls-helpdesk-tickets</automated>
  </verify>
  <done>Test file present + tsc passes; live-DB run passes all 10 cases (or skips cleanly if service-role key absent in local env).</done>
</task>

<task type="auto">
  <name>Task 2: RLS impersonation proof — kb_articles / kb_article_versions / macros / routing / sla</name>
  <files>leanshot/src/test/rls-helpdesk-kb.test.ts</files>
  <action>
Companion file to Task 1. Same fixture helpers (factor out a small `src/test/_helpers/helpdesk-fixtures.ts` if it cuts duplication; otherwise inline).

**Test cases:**

1. **Global KB**: Any authenticated user can SELECT `kb_articles WHERE org_id IS NULL AND published_at IS NOT NULL`.
2. **Org-private KB cross-tenant**: `userA_patient` cannot see `kb_articles` WHERE `org_id = org_b`.
3. **Draft KB hidden from non-admin**: `userA_patient` cannot see `kb_articles WHERE published_at IS NULL` even for own org.
4. **Draft KB visible to admin**: `userA_owner` CAN see drafts for `org_a`.
5. **publish_kb_article role gate**: `userA_patient` calls `supabase.rpc('publish_kb_article', ...)` → fails with insufficient_role.
6. **publish_kb_article success**: `userA_owner` calls publish_kb_article → success; `kb_article_versions` row appears.
7. **agent_macros cross-tenant**: `userA_owner` cannot SELECT `agent_macros WHERE org_id = org_b`.
8. **helpdesk_routing_rules role gate**: `userA_patient` cannot INSERT a routing rule (RLS denies).
9. **sla_targets role gate**: `userA_patient` cannot UPDATE sla_targets (RLS denies).
10. **search_kb_articles RPC**: `userA_patient` calls `search_kb_articles('test', 'en', null, 10)` → returns global articles; calling with `p_org_id = org_b` returns 0 rows (RLS cascade).

File-scoped prefix `p37-rls-kb-…`; same cleanup pattern.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; test -f src/test/rls-helpdesk-kb.test.ts &amp;&amp; npm run test:e2e:rls -- --run --testPathPattern rls-helpdesk-kb</automated>
  </verify>
  <done>Test file present; live-DB run passes all 10 cases.</done>
</task>

<task type="auto">
  <name>Task 3: UAT runbook document for operator</name>
  <files>leanshot/.planning/phases/37-m6-helpdesk-core/uat-runbook.md</files>
  <action>
Create `leanshot/.planning/phases/37-m6-helpdesk-core/uat-runbook.md` with the exact operator-facing procedure. Use copy-paste CLI commands wherever possible per [[feedback_verify_human_uat_via_cli]] and [[feedback_cli_over_paste_back]].

Structure:

```markdown
# Phase 37 UAT Runbook

## 1. Function Secrets

Pre-flight:
  supabase login          # if not already
  supabase link --project-ref ytnsipxxmzgaebkqmokp   # if local link missing

Generate fresh secrets where needed:
  HMAC_SECRET=$(supabase db query --linked "select decrypted_secret from vault.decrypted_secrets where name='helpdesk_hmac_secret'" | tail -1 | tr -d ' ')
  CSAT_SECRET=$(openssl rand -hex 32)

Set all helpdesk Edge Fn secrets in one go:
  supabase secrets set --project-ref ytnsipxxmzgaebkqmokp \
    HELPDESK_HMAC_SECRET="$HMAC_SECRET" \
    HELPDESK_CSAT_SIGNING_SECRET="$CSAT_SECRET" \
    ANTHROPIC_MODEL_HELPDESK=claude-sonnet-4-6 \
    SLA_BREACH_DEFAULT_ONCALL_EMAILS="ops@leanshot.app,karsten.haldan@gmail.com"

Verify (do NOT print values — only names):
  supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep -E "HELPDESK_HMAC_SECRET|HELPDESK_CSAT_SIGNING_SECRET|ANTHROPIC_MODEL_HELPDESK|SLA_BREACH_DEFAULT_ONCALL_EMAILS|RESEND_API_KEY|RESEND_WEBHOOK_SECRET|ANTHROPIC_API_KEY|ANTHROPIC_API_KEY_BAA"

Expected: all 8 names present.

If RESEND_WEBHOOK_SECRET is missing, run Section 2 first.

## 2. Resend Inbound dashboard configuration (HUMAN-ONLY)

This section cannot be automated — Resend admin dashboard has no CLI for receiving/webhook setup.

a. Open https://resend.com/domains/app.leanshot.app — confirm domain is verified for OUTBOUND (should be — Phase 16 work).
b. Click "Receiving" tab → enable inbound for this domain.
c. Resend shows MX record values like:
     Type: MX  Host: app.leanshot.app  Value: feedback-smtp.us-east-1.resend.com  Priority: 10
d. Add the MX record at the DNS provider for app.leanshot.app (likely Vercel: https://vercel.com/karstenhaldan-5548/leanshot-marketing/settings/domains).
e. Wait for Resend dashboard to show MX verified (~5-15 min). Refresh until green.
f. Resend dashboard → "Webhooks" → "Add Endpoint":
     URL: https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/helpdesk-inbound
     Events: email.received
g. After creation, click the new endpoint → copy the "Signing Secret" (starts with `whsec_`).
h. Set the secret:
     supabase secrets set --project-ref ytnsipxxmzgaebkqmokp RESEND_WEBHOOK_SECRET=whsec_xxxxxxxxx

## 3. Edge Fn deploys (re-deploy after secrets set)

  supabase functions deploy helpdesk-inbound
  supabase functions deploy helpdesk-ai-assist
  supabase functions deploy helpdesk-csat-send
  supabase functions deploy helpdesk-sla-breach-cron
  supabase functions deploy helpdesk-agent-reply-send

(Per [[reference_supabase_functions_deploy_no_linked_flag]] — no `--linked` flag.)

## 4. End-to-end smoke (HUMAN-ONLY)

a. From an external mailbox (e.g., karsten.haldan@gmail.com), send to support@app.leanshot.app:
     Subject: Phase 37 smoke test
     Body: "this is a test"

b. Within 30s, open https://app.leanshot.app/admin/helpdesk/inbox — confirm a new ticket appears.

c. Within 60s, click into the ticket — confirm AiSuggestionPane shows tags + sentiment + draft reply.

d. Click "Insert draft into composer" → confirm composer populates. Click Send.

e. Confirm reply email arrives at the external mailbox with header `Reply-To: reply+<token>@app.leanshot.app`.

f. Reply to that email with "follow-up question". Within 60s, ticket thread shows the new user message.

g. As agent, click "Close ticket" → within 30s, external mailbox receives CSAT survey email.

h. PHI smoke:
   - Promote `karsten.haldan@gmail.com` to `clinician` role in a test org via `supabase db query --linked`.
   - Repeat (a)-(c).
   - After agent opens the ticket, run:
       supabase db query --linked "select * from public.phi_access_log where reason='agent-inbox-open' order by created_at desc limit 1"
     Confirm 1 row appears with the agent's user_id.

i. SLA breach smoke (deferred — requires ~4h wait OR manual back-date):
     supabase db query --linked "update public.tickets set created_at = now() - interval '5 hours' where subject ilike '%smoke%' returning id, created_at"
   Wait for next 5-min cron tick. Confirm SLA breach email arrives at ops@.

## 5. Acceptance

All 5 sections pass → Phase 37 ships. Document any failures in `.planning/phases/37-m6-helpdesk-core/uat-failures.md`.
```

Save the file under the phase directory.
  </action>
  <verify>
    <automated>test -f /Users/karstenhaldan/minisite/leanshot/.planning/phases/37-m6-helpdesk-core/uat-runbook.md &amp;&amp; grep -q "Phase 37 UAT Runbook" /Users/karstenhaldan/minisite/leanshot/.planning/phases/37-m6-helpdesk-core/uat-runbook.md &amp;&amp; grep -q "RESEND_WEBHOOK_SECRET" /Users/karstenhaldan/minisite/leanshot/.planning/phases/37-m6-helpdesk-core/uat-runbook.md</automated>
  </verify>
  <done>Runbook file exists; covers Function Secrets + Resend dashboard + Edge Fn deploys + e2e smoke + acceptance criteria.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator UAT — Function Secrets + Resend Inbound MX + e2e smoke + PHI audit verify</name>
  <what-built>All 4 Edge Fns deployed, all migrations applied, admin module live, widget chunk shipped, RLS tests passing. Awaiting operator-only steps from `uat-runbook.md`.</what-built>
  <how-to-verify>
Follow `leanshot/.planning/phases/37-m6-helpdesk-core/uat-runbook.md` end-to-end:

1. **Function Secrets** (Section 1) — run the commands listed; report `supabase secrets list` output (names only — DO NOT paste secret values).

2. **Resend Inbound dashboard** (Section 2) — confirm:
   a. MX records added to DNS for app.leanshot.app.
   b. Resend dashboard shows MX verified.
   c. Webhook endpoint created pointing to /functions/v1/helpdesk-inbound, subscribed to `email.received`.
   d. RESEND_WEBHOOK_SECRET copied into Function Secrets.

3. **Edge Fn deploys** (Section 3) — all 5 deploy commands return success.

4. **E2E smoke** (Section 4 steps a-i) — report PASS/FAIL per step. For the PHI audit step (h), paste the SELECT result row (excluding access_token / hashes — just confirm `reason='agent-inbox-open'` and an agent user_id present).

5. **SLA breach** (Section 4 step i) — at least confirm the SQL update + cron-driven email path; the actual delay-based assertion can defer.

Report blocked items individually so the developer can either inline-fix or surface to a follow-up plan.
  </how-to-verify>
  <resume-signal>Type "approved" if Sections 1-4 + spot-check of 5 pass; otherwise list which step failed and the observed behavior. Per multi-signal pattern, individual section signals are also acceptable (e.g., "approved sections 1-3; section 4 step f failed: no reply email received").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator credentials → Function Secrets | Operator pastes secrets into `supabase secrets set`; secrets never appear in repo or logs |
| Resend dashboard → Webhook endpoint | Webhook signing secret distinct from Resend API key; tied to specific endpoint URL |
| External email → support@app.leanshot.app | MX records gate inbound; Svix sig verifies on webhook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-09-01 | Information Disclosure | Secret values pasted into git via runbook | mitigate | Runbook commands use shell variable interpolation; never check in `.env` files; rely on `supabase secrets list` to confirm presence (NOT values) |
| T-37-09-02 | Spoofing | Webhook URL leaked → attacker posts fake email.received | mitigate | Svix signature verification with `RESEND_WEBHOOK_SECRET`; 401 on bad sig |
| T-37-09-03 | Information Disclosure | RLS test fixtures leak across runs | mitigate | File-scoped prefix per [[feedback_rls_per_file_slug_prefix]]; afterAll cleanup |
| T-37-09-04 | Repudiation | PHI access audit doesn't fire end-to-end | mitigate | Smoke step (h) verifies row presence via `supabase db query --linked` |
| T-37-09-05 | Tampering | Operator skips MX verification → ticket creation fails silently | mitigate | E2E smoke step (b) — visible ticket-not-appearing is the failure signal |
</threat_model>

<verification>
- `cd leanshot && npm run test:e2e:rls -- --run --testPathPattern rls-helpdesk` passes (both files).
- `supabase functions list --project-ref ytnsipxxmzgaebkqmokp` shows all 5 helpdesk Edge Fns deployed.
- `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp` shows all required secret names present (per Section 1 of runbook).
- E2E smoke produces a real ticket + reply + CSAT exchange.
</verification>

<success_criteria>
- Every helpdesk RLS surface has a live cross-tenant impersonation proof test.
- Function Secrets verified present via `supabase secrets list`.
- Resend Inbound MX + webhook live; signing secret in place.
- E2E smoke (a)-(h) all pass.
- All 13 HELP-* requirements verified end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/37-m6-helpdesk-core/37-09-SUMMARY.md` capturing:
- Final secret names confirmed in Supabase project (NOT the values).
- Resend dashboard endpoint URL + webhook ID (for future incident response).
- DNS MX record values added (for documentation; not secret).
- E2E smoke completion timestamp.
- Any deferred follow-ups (e.g., SLA breach delay-based test that needs real-time wait).
- Phase 37 → ROADMAP.md update: mark all 13 HELP-* IDs as Pending → In Progress → Completed.
</output>
