---
phase: 44
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270720000001_p44_community_schema.sql
  - supabase/migrations/20270720000002_p44_community_rls.sql
  - supabase/migrations/20270720000003_p44_community_media_bucket.sql
  - supabase/migrations/20270720000005_p44_community_secdef_rpcs.sql
  - leanshot/tests/rls/community-spaces-rls.test.ts
  - leanshot/tests/rls/community-tier-gating-rls.test.ts
  - leanshot/tests/rls/community-reactions-rls.test.ts
autonomous: true
requirements:
  - COMMUNITY-01
  - COMMUNITY-02
  - COMMUNITY-06

must_haves:
  truths:
    - "All 7 community_* tables exist with named, immutable CHECK constraints (body 5000, reactions 5 emojis, min_tier 3-tier)"
    - "RLS is ENABLED on every community_* table and on community_post_media + community_post_mentions + community_comment_mentions"
    - "Free user CANNOT read community_posts in a Pro-only space (SELECT returns 0 rows, not 403)"
    - "Non-org user CANNOT see community_spaces rows where org_id IS NOT NULL (cross-tenant probe returns 0 rows)"
    - "Reaction toggle SECDEF RPC toggle_community_reaction is idempotent (insert→delete on second call)"
    - "community-media Storage bucket exists as private with mime whitelist {image/jpeg,image/png,image/webp} and 10 MB size limit"
    - "community_comments carries denormalized space_id column (RESEARCH Pitfall 5) for Realtime filter efficiency"
  artifacts:
    - path: "supabase/migrations/20270720000001_p44_community_schema.sql"
      provides: "7 community tables + indexes + CHECK constraints"
      contains: "create table if not exists public.community_spaces"
    - path: "supabase/migrations/20270720000002_p44_community_rls.sql"
      provides: "RLS policies (SELECT/INSERT/UPDATE/DELETE) per table"
      contains: "alter table public.community_spaces enable row level security"
    - path: "supabase/migrations/20270720000003_p44_community_media_bucket.sql"
      provides: "community-media bucket + storage.objects RLS"
      contains: "insert into storage.buckets"
    - path: "supabase/migrations/20270720000005_p44_community_secdef_rpcs.sql"
      provides: "toggle_community_reaction SECDEF RPC"
      contains: "create or replace function public.toggle_community_reaction"
    - path: "leanshot/tests/rls/community-spaces-rls.test.ts"
      provides: "Cross-tenant org-private space + tier-gated SELECT proof"
    - path: "leanshot/tests/rls/community-tier-gating-rls.test.ts"
      provides: "Free user blocked from Pro-only space posts proof"
    - path: "leanshot/tests/rls/community-reactions-rls.test.ts"
      provides: "Reaction toggle idempotency proof (T-44-01 + COMMUNITY-02)"
  key_links:
    - from: "community_spaces.org_id"
      to: "org_members table"
      via: "RLS policy cspace_select_org_member EXISTS subquery"
      pattern: "exists \\(select 1 from public.org_members"
    - from: "community_posts SELECT policy"
      to: "tier_effective view"
      via: "RLS policy cpost_select_tier join on auth.uid()"
      pattern: "join public.tier_effective"
    - from: "toggle_community_reaction RPC"
      to: "community_reactions UNIQUE(user_id,target_type,target_id,emoji)"
      via: "ON CONFLICT DO DELETE"
      pattern: "on conflict.*do delete"
---

<objective>
Ship the community schema foundation: 7 tables (spaces, posts, comments, reactions, post_media, post_mentions, comment_mentions), RLS policies that enforce tier gating + org-private isolation, the community-media Storage bucket with mime whitelist, and the toggle_community_reaction SECDEF RPC. Land the three RLS test files that prove cross-tenant isolation, tier gating, and reaction idempotency.

Purpose: This is the bedrock for every other Phase 44 plan. Without schema + RLS, every Edge Fn and React component below fails closed. Phases 45/48/49 schema-extend this without re-keying.

Output: 4 migration files (NOT pushed yet — Wave 3 plan 44-10 runs `supabase db push --linked` once all migrations across all plans are written), 3 vitest-e2e RLS test files (executed AFTER 44-10 pushes; this plan only authors them).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-m4-community-feed-foundation/44-CONTEXT.md
@.planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md
@.planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md
@.planning/phases/44-m4-community-feed-foundation/44-VALIDATION.md

<!-- Analog migrations to fork from -->
@supabase/migrations/20270707000020_helpdesk_rls_policies.sql
@supabase/migrations/20260801000003_org_logos_storage.sql
@supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql
@supabase/migrations/20270715000002_p43_tier_effective_view_v2.sql

<!-- Fixture analog -->
@leanshot/tests/rls/helpdesk-tickets-rls.test.ts

<interfaces>
<!-- Key contracts other Wave 1+ plans consume. Schema columns are LOCKED here. -->

community_spaces (id uuid PK, name text, description text, org_id uuid NULL FK organizations(id) ON DELETE CASCADE,
  min_tier text NOT NULL default 'free' CHECK (min_tier IN ('free','pro','lifetime')),
  created_at timestamptz, updated_at timestamptz)

community_posts (id uuid PK, space_id uuid FK community_spaces(id) ON DELETE CASCADE,
  author_id uuid FK auth.users(id) ON DELETE SET NULL, body text NOT NULL CHECK (char_length(body) <= 5000),
  mux_upload_id text NULL, mux_playback_id text NULL,
  video_status text NULL CHECK (video_status IN ('uploading','processing','ready','rejected')),
  created_at timestamptz, edited_at timestamptz NULL, deleted_at timestamptz NULL)

community_comments (id uuid PK, post_id uuid FK community_posts(id) ON DELETE CASCADE,
  space_id uuid NOT NULL FK community_spaces(id) ON DELETE CASCADE,  -- DENORMALIZED per Pitfall 5
  parent_comment_id uuid NULL FK community_comments(id) ON DELETE CASCADE,  -- nullable per D-01 forward-compat
  author_id uuid FK auth.users(id) ON DELETE SET NULL,
  body text NOT NULL CHECK (char_length(body) <= 5000),
  created_at timestamptz, edited_at timestamptz NULL, deleted_at timestamptz NULL)

community_reactions (id uuid PK, user_id uuid FK auth.users(id) ON DELETE CASCADE,
  target_type text NOT NULL CHECK (target_type IN ('post','comment')),
  target_id uuid NOT NULL,
  emoji text NOT NULL CHECK (emoji IN ('like','heart','target','fire','clap')),
  created_at timestamptz,
  UNIQUE (user_id, target_type, target_id, emoji))

community_post_media (id uuid PK, post_id uuid FK community_posts(id) ON DELETE CASCADE,
  path text NOT NULL, display_order int NOT NULL,
  UNIQUE (post_id, display_order))
-- Trigger BEFORE INSERT enforces D-04 10-image cap: count rows where post_id=NEW.post_id >= 10 → RAISE EXCEPTION

community_post_mentions (post_id uuid FK community_posts(id) ON DELETE CASCADE,
  user_id uuid FK auth.users(id) ON DELETE CASCADE,
  PRIMARY KEY (post_id, user_id))

community_comment_mentions (comment_id uuid FK community_comments(id) ON DELETE CASCADE,
  user_id uuid FK auth.users(id) ON DELETE CASCADE,
  PRIMARY KEY (comment_id, user_id))

-- SECDEF RPC contract
toggle_community_reaction(p_target_type text, p_target_id uuid, p_emoji text)
  RETURNS TABLE(emoji text, count bigint, reacted_by_me boolean)
  -- INSERT ... ON CONFLICT (user_id,target_type,target_id,emoji) DO DELETE
  -- Then RETURN QUERY aggregate counts per emoji for (target_type, target_id)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author community schema + RLS + bucket + RPC migrations</name>
  <files>supabase/migrations/20270720000001_p44_community_schema.sql, supabase/migrations/20270720000002_p44_community_rls.sql, supabase/migrations/20270720000003_p44_community_media_bucket.sql, supabase/migrations/20270720000005_p44_community_secdef_rpcs.sql</files>
  <read_first>
    - supabase/migrations/20270707000020_helpdesk_rls_policies.sql (full RLS policy pattern)
    - supabase/migrations/20260801000003_org_logos_storage.sql (storage bucket pattern lines 24–132)
    - supabase/migrations/20270715000002_p43_tier_effective_view_v2.sql (tier_effective column names)
    - supabase/migrations/20270710000005_p36_review_secdef_rpcs.sql (SECDEF RPC pattern)
    - .planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md (lines 41–217 schema/RLS/bucket excerpts)
    - .planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md (Security Domain table; Pitfall 5)
  </read_first>
  <action>
    Author the four migrations in order. Pre-check: `ls supabase/migrations/20270720*.sql` returns nothing (no collisions in this prefix range — verified 2026-05-23). Use the EXACT 14-digit timestamps in the filenames (`20270720000001`, `20270720000002`, `20270720000003`, `20270720000005`); letter-suffix timestamps are silently skipped by Supabase CLI per reference_supabase_migration_filename_regex. Timestamp `...000004` is reserved for plan 44-02 (notification widening) so order is preserved.

    Migration 1 (community_schema.sql): wrap in `begin; ... commit;`. Create all 7 tables per the interfaces block. Add `create index` on (`space_id`, `created_at desc`) for community_posts and (`post_id`, `created_at`) for community_comments to support cursor pagination per D-12. Add `community_post_media` BEFORE-INSERT trigger function that RAISES EXCEPTION when count(*) where post_id=NEW.post_id is already 10 (D-04 cap; idempotent via `create or replace function`). Use `column comment` markers on every table noting D-XX decision IDs they implement. `community_comments.space_id` is NOT NULL and denormalized per Pitfall 5 (filter efficiency for Realtime).

    Migration 2 (community_rls.sql): wrap in `begin; ... commit;`. Per the patterns in 44-PATTERNS.md:
    - `alter table … enable row level security` for all 7 tables.
    - cspace_select_global (org_id IS NULL → all authenticated), cspace_select_org_member (org_id IS NOT NULL → org_members EXISTS join per D-09).
    - cpost_select_tier joining `tier_effective` per D-08; trial users see Pro content (`te.tier_label IN ('pro','lifetime','trial')` — see Claude's Discretion below).
    - Soft-delete filtering: SELECT policies on community_posts and community_comments append `and deleted_at is null`.
    - cpost_update_author / ccomment_update_author / cpost_delete_author (UPDATE for edit-forever D-15; DELETE here means UPDATE deleted_at — author scoped via with check author_id = auth.uid()). NO hard-delete policy for non-staff (Phase 48 owns admin hard-delete).
    - INSERT policies on community_posts (must satisfy space tier read); community_comments (post must be selectable + author_id = auth.uid()); community_reactions (user_id = auth.uid()); community_post_media (post belongs to caller).
    - Cross-tenant impersonation defense per Phase 28 + reference_supabase_project: every policy keyed on auth.uid() with explicit subquery, no GUC.

    Migration 3 (community_media_bucket.sql): insert into storage.buckets with `community-media`, public=false, file_size_limit=10485760, allowed_mime_types=`{image/jpeg,image/png,image/webp}` (NO SVG per security domain table — T-44-04). Use `on conflict (id) do nothing`. Three storage.objects RLS policies guarded by `do $$ if not exists (select 1 from pg_policies …) then create policy … end if; $$`:
    - objects_select_community_media: bucket_id='community-media' AND auth.uid() IS NOT NULL.
    - objects_insert_community_media: bucket_id='community-media' AND (storage.foldername(name))[1] = auth.uid()::text (T-44-04 path-traversal defense).
    - objects_delete_community_media: same as insert (author-or-staff hard-delete deferred to Phase 48).

    Migration 4 (community_secdef_rpcs.sql): `create or replace function public.toggle_community_reaction(p_target_type text, p_target_id uuid, p_emoji text) returns table(emoji text, count bigint, reacted_by_me boolean) language plpgsql security definer set search_path = public, extensions` (search_path includes extensions per reference_supabase_migration_gotchas). Body uses `insert … on conflict (user_id,target_type,target_id,emoji) do delete` per RESEARCH §Code Examples. RETURN QUERY aggregate per-emoji counts. `grant execute on function … to authenticated`.

    Add `comment on function public.toggle_community_reaction is 'Phase 44 D-02 — idempotent reaction toggle; INSERT...ON CONFLICT DO DELETE pattern (Postgres 15+ Supabase cloud).'`.

    Do NOT run `supabase db push` — plan 44-10 owns the BLOCKING push task.

    Per D-15 trial-tier discretion: include trial in cpost_select_tier 'pro' branch (trial users get Pro features for evaluation per planning_context).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/migrations/20270720000001_p44_community_schema.sql supabase/migrations/20270720000002_p44_community_rls.sql supabase/migrations/20270720000003_p44_community_media_bucket.sql supabase/migrations/20270720000005_p44_community_secdef_rpcs.sql && grep -l 'create table if not exists public.community_spaces' supabase/migrations/20270720000001_p44_community_schema.sql && grep -l 'enable row level security' supabase/migrations/20270720000002_p44_community_rls.sql && grep -c 'tier_effective' supabase/migrations/20270720000002_p44_community_rls.sql | grep -v '^0$' && grep -l 'community-media' supabase/migrations/20270720000003_p44_community_media_bucket.sql && grep -l 'toggle_community_reaction' supabase/migrations/20270720000005_p44_community_secdef_rpcs.sql && grep -c 'on conflict.*do delete' supabase/migrations/20270720000005_p44_community_secdef_rpcs.sql | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - All 4 migration files exist at the exact 14-digit timestamps listed in files_modified.
    - File 1 contains `create table if not exists public.community_spaces` AND `community_posts` AND `community_comments` AND `community_reactions` AND `community_post_media` AND `community_post_mentions` AND `community_comment_mentions` (grep each separately).
    - File 1 community_comments has `space_id uuid not null` column line (Pitfall 5 denorm; grep `community_comments.*space_id uuid not null` after collapse).
    - File 1 community_reactions has `unique (user_id, target_type, target_id, emoji)` literal text.
    - File 1 community_posts has `char_length(body) <= 5000` literal text (D-11).
    - File 1 community_reactions has `emoji in ('like','heart','target','fire','clap')` literal text (D-03).
    - File 2 has `alter table public.community_spaces enable row level security` AND same for 6 other tables.
    - File 2 has `org_members` EXISTS subquery in cspace_select_org_member policy (D-09 cross-tenant defense).
    - File 2 has `tier_effective` join in cpost_select_tier (D-08).
    - File 2 has `and deleted_at is null` in community_posts and community_comments SELECT policies (D-15 soft-delete filter).
    - File 3 has `insert into storage.buckets` with literal `community-media` and `false` (private) and `image/jpeg`,`image/png`,`image/webp` MIMEs, NO `image/svg` literal.
    - File 3 has `(storage.foldername(name))[1] = auth.uid()::text` literal (T-44-04 path defense).
    - File 4 has `security definer set search_path = public, extensions` literal.
    - File 4 has `on conflict (user_id, target_type, target_id, emoji) do delete` literal text.
    - File 4 has `grant execute on function public.toggle_community_reaction` literal text.
  </acceptance_criteria>
  <done>All 4 migration files written, lint-clean, no migration is run yet (push happens in 44-10).</done>
</task>

<task type="auto">
  <name>Task 2: Author Wave 0 RLS test files</name>
  <files>leanshot/tests/rls/community-spaces-rls.test.ts, leanshot/tests/rls/community-tier-gating-rls.test.ts, leanshot/tests/rls/community-reactions-rls.test.ts</files>
  <read_first>
    - leanshot/tests/rls/helpdesk-tickets-rls.test.ts (fixture pattern: admin.generateLink + /auth/v1/verify per reference_rls_fixture_gotrueclient_flake)
    - .planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md (no analog for community fixtures; fork helpdesk-tickets pattern)
    - .planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md §Validation Architecture lines 752–780
  </read_first>
  <action>
    Author three vitest-e2e RLS test files using the project-standard helpdesk-tickets-rls.test.ts pattern. Each test uses `admin.generateLink` + `/auth/v1/verify` via plain fetch (NOT signInWithPassword per reference_rls_fixture_gotrueclient_flake — that flakes on ES256 projects). Use `const TEST_SLUG_PREFIX = \`p44_${new URL(import.meta.url).pathname.split('/').pop()}_${Date.now()}\`` (file-scoped per reference feedback_rls_per_file_slug_prefix; never share across files).

    File 1 (community-spaces-rls.test.ts): two top-level it() blocks:
    - "User A in org_A cannot see community_spaces row owned by org_B" — seed two orgs, two users (each as org_member of their respective org), one community_spaces row with org_id=orgB.id. Assert `await client_A.from('community_spaces').select('*').eq('id', spaceB.id)` returns `data.length === 0`. This is the cross-tenant impersonation proof for T-44-01 + D-09.
    - "Global space (org_id IS NULL) IS visible to both users" — insert a row with org_id NULL, assert both clients see it. Sanity check.

    File 2 (community-tier-gating-rls.test.ts): three top-level it() blocks:
    - "Free user CANNOT read community_posts in a min_tier='pro' space" — seed a Free-tier user (insert into tier_effective via the v2 RPC if SECDEF helper exists, OR direct insert into the underlying subscription tables — verify the seeding approach in helpdesk-tickets-rls.test.ts seeding pattern). Insert a community_spaces row with min_tier='pro', insert a community_posts row. Assert Free-tier client SELECT on community_posts WHERE space_id=… returns 0 rows.
    - "Pro user CAN read community_posts in a min_tier='pro' space" — same setup, but seed user as tier_label='pro'. Assert returns 1 row.
    - "Trial user CAN read min_tier='pro' space" — D-06 + Claude's Discretion. tier_label='trial' SELECT returns 1 row.

    File 3 (community-reactions-rls.test.ts): two top-level it() blocks:
    - "toggle_community_reaction is idempotent — first call inserts, second call deletes" — call `client.rpc('toggle_community_reaction', { p_target_type: 'post', p_target_id: postId, p_emoji: 'like' })` twice. Assert after call 1: count=1, reacted_by_me=true. After call 2: count=0, reacted_by_me=false. This is T-44-01 + COMMUNITY-02 idempotency proof.
    - "Reactions UNIQUE constraint rejects duplicate raw INSERT" — call `client.from('community_reactions').insert({...})` with same (user_id, target_type, target_id, emoji) twice; assert second call returns error with code 23505 (unique_violation).

    Use shared fixtures helper (create at leanshot/tests/rls/fixtures-community.ts inline within this task — small, just `createOrgScopedUser(tier, orgId)` + `seedSpaceAndPost(spaceParams)`). Cleanup in afterAll deletes all rows where the slug-prefix matches.

    These tests CANNOT actually run until 44-10 has pushed the migrations. Mark each with `// REQUIRES: supabase db push --linked completed (Wave 3 plan 44-10)` comment at file top. The `<verify>` for THIS task only confirms files exist and compile via `tsc --noEmit`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && ls tests/rls/community-spaces-rls.test.ts tests/rls/community-tier-gating-rls.test.ts tests/rls/community-reactions-rls.test.ts && grep -l 'admin.generateLink' tests/rls/community-spaces-rls.test.ts && grep -c "it\\(.*'.*cannot see.*org_B" tests/rls/community-spaces-rls.test.ts | grep -v '^0$' && grep -c "tier_label.*pro\\|min_tier.*pro" tests/rls/community-tier-gating-rls.test.ts | grep -v '^0$' && grep -c "toggle_community_reaction" tests/rls/community-reactions-rls.test.ts | grep -v '^0$' && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'community-(spaces|tier-gating|reactions)-rls' | (! grep error)</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test files exist at exact paths in files_modified.
    - Each file uses file-scoped TEST_SLUG_PREFIX (not shared across files; grep `Date.now\(\)` present in each).
    - Each file uses `admin.generateLink` (NOT `signInWithPassword`).
    - community-spaces-rls has at least 2 it() blocks; one mentions `org_B` cross-tenant case.
    - community-tier-gating-rls has 3 it() blocks covering Free/Pro/Trial against a min_tier='pro' space.
    - community-reactions-rls has an it() block calling `toggle_community_reaction` twice and asserting count flips 0→1→0.
    - community-reactions-rls has an it() block asserting duplicate INSERT returns Postgres 23505 unique_violation.
    - tsc --noEmit returns no errors for the three new files.
    - Comment `// REQUIRES: supabase db push --linked completed` appears at top of each file.
  </acceptance_criteria>
  <done>Three RLS test files compile clean; ready to run once 44-10 push completes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase RLS | All community_* reads/writes pass through PostgREST + RLS; auth.uid() is the only trusted identifier |
| Browser → Storage RLS | community-media bucket uploads pass through storage.objects RLS keyed on (storage.foldername(name))[1] = auth.uid()::text |
| Cross-tenant (User A → User B's org) | community_spaces.org_id RLS prevents probing existence of org-private spaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Elevation of Privilege | community_spaces / community_posts | mitigate | RLS policies: cspace_select_org_member with org_members EXISTS join (no GUC); cpost_select_tier with tier_effective join. Cross-tenant proof in tests/rls/community-spaces-rls.test.ts. |
| T-44-04 | Tampering | community-media Storage bucket | mitigate | objects_insert policy: (storage.foldername(name))[1] = auth.uid()::text — leading path segment MUST equal caller's uid. MIME whitelist {image/jpeg,image/png,image/webp} blocks SVG-XSS. No public list (private bucket). |
| T-44-07 | Repudiation (acceptable risk) | community_posts.edited_at + soft-delete | accept | D-15 documents that authors may edit forever; soft-delete preserves rows for moderation queue. Phase 48 (moderation) adds immutable audit log. Document and defer — NOT a Phase 44 blocker. |
</threat_model>

<verification>
- `ls supabase/migrations/20270720000{001,002,003,005}_*.sql` → all 4 exist
- `grep -c 'enable row level security' supabase/migrations/20270720000002_*.sql` → 7
- Run by 44-10 after push: `npx vitest run tests/rls/community-spaces-rls.test.ts tests/rls/community-tier-gating-rls.test.ts tests/rls/community-reactions-rls.test.ts -x`
</verification>

<success_criteria>
- 4 migration files at exact 14-digit timestamps written; lint-clean.
- 3 RLS test files compile under tsc --noEmit.
- Cross-tenant impersonation test, tier-gated SELECT test, and reaction toggle idempotency test all PRESENT as written code (executable once 44-10 pushes).
- Schema is forward-compatible: parent_comment_id nullable (Phase 45 threading), org_id on every community-scoped table (clinic-private), reactions UNIQUE keyed for Phase 45 leaderboards.
</success_criteria>

<output>
After completion, create `.planning/phases/44-m4-community-feed-foundation/44-01-SUMMARY.md` summarizing: tables created, RLS policies count per table, trial-tier discretion (included trial in Pro branch), and Wave 3 dependency on 44-10 push.
</output>
