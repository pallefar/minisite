---
phase: 44
plan: 10
type: execute
wave: 3
depends_on: [44-01, 44-02, 44-03, 44-04, 44-05, 44-06, 44-07, 44-08, 44-09]
files_modified:
  - leanshot/tests/e2e/community/community-feed.spec.ts
  - leanshot/playwright.config.ts
  - .planning/phases/44-m4-community-feed-foundation/44-UAT.md
autonomous: false
requirements:
  - COMMUNITY-01
  - COMMUNITY-02
  - COMMUNITY-03
  - COMMUNITY-04
  - COMMUNITY-05
  - COMMUNITY-06
user_setup:
  - service: mux
    why: "Plan 44-04's mux-create-upload + mux-webhook Edge Fns require Mux API credentials (MUX_TOKEN_ID, MUX_TOKEN_SECRET, MUX_WEBHOOK_SECRET) as Supabase Function Secrets before deploy"
    env_vars:
      - name: MUX_TOKEN_ID
        source: "Mux Dashboard → Settings → Access Tokens → Create New Token (scope: Video)"
      - name: MUX_TOKEN_SECRET
        source: "Same dashboard create-token modal (shown once)"
      - name: MUX_WEBHOOK_SECRET
        source: "Mux Dashboard → Settings → Webhooks → Create Webhook → endpoint = https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/mux-webhook; signing-secret revealed once"
    dashboard_config:
      - task: "Create Mux Video access token (Video scope)"
        location: "https://dashboard.mux.com/settings/access-tokens"
      - task: "Create webhook endpoint pointing to Supabase mux-webhook Edge Fn"
        location: "https://dashboard.mux.com/settings/webhooks"
      - task: "Set 3 secrets on Supabase Function Secrets"
        location: "`cd /Users/karstenhaldan/minisite/supabase && supabase secrets set --project-ref ytnsipxxmzgaebkqmokp MUX_TOKEN_ID=… MUX_TOKEN_SECRET=… MUX_WEBHOOK_SECRET=…`"

must_haves:
  truths:
    - "supabase db push --linked succeeds applying all 6 Phase 44 migrations (community schema + RLS + bucket + notification CHECK widening + SECDEF RPCs + admin policies)"
    - "supabase functions deploy succeeds for mux-create-upload, mux-webhook, notify-community"
    - "scripts/assert-bundle-budget.sh reports community-feed ≤ 20 kB gz AND community-media ≤ 190 kB gz AND community-mentions ≤ 12 kB gz"
    - "All Wave 0/1 vitest unit + RLS + integration tests pass against the live Supabase project"
    - "Playwright e2e spec exercises create-post → comment → react → realtime-broadcast cross-tab path"
    - "44-UAT.md captures 4 discrete HUMAN-UAT resume signals (Mux video upload roundtrip, @mention email delivery, cross-tab realtime, locked-card upgrade flow) per multi-signal human-verify checkpoint pattern"
  artifacts:
    - path: "leanshot/tests/e2e/community/community-feed.spec.ts"
      provides: "Playwright e2e covering post + comment + react + realtime cross-tab"
    - path: "leanshot/playwright.config.ts"
      provides: "Adds community project entry if not already configured"
    - path: ".planning/phases/44-m4-community-feed-foundation/44-UAT.md"
      provides: "HUMAN-UAT checklist with 4 discrete signals"
  key_links:
    - from: "supabase db push --linked"
      to: "all 6 Phase 44 migrations applied"
      via: "BLOCKING task before any verification"
      pattern: "supabase db push"
    - from: "supabase functions deploy"
      to: "3 Edge Fns live"
      via: "BLOCKING task after push + before HUMAN-UAT"
      pattern: "supabase functions deploy"
---

<objective>
Close Phase 44 by running the BLOCKING migration push, deploying Edge Fns, gating on the bundle-budget CI script, executing all authored automated tests, authoring the Playwright e2e cross-tab spec, and surfacing the 4-signal HUMAN-UAT checkpoint that covers the non-automatable behaviors (Mux upload roundtrip, @mention email delivery, cross-tab realtime, tier-locked discovery flow).

This plan has no react/db file authoring of its own besides the e2e spec and the UAT doc — it is the orchestration plan that runs the gates Phase 44 ships behind.

Purpose: Promote Phase 44 from "code committed" to "verified and in production".

Output: 1 Playwright spec + 1 UAT.md + the side effects of supabase db push + supabase functions deploy + bundle-budget CI report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-m4-community-feed-foundation/44-CONTEXT.md
@.planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md
@.planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md
@.planning/phases/44-m4-community-feed-foundation/44-VALIDATION.md

@.planning/phases/44-m4-community-feed-foundation/44-01-PLAN.md
@.planning/phases/44-m4-community-feed-foundation/44-02-PLAN.md
@.planning/phases/44-m4-community-feed-foundation/44-04-PLAN.md
@.planning/phases/44-m4-community-feed-foundation/44-05-PLAN.md
@.planning/phases/44-m4-community-feed-foundation/44-09-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Pre-checks + supabase db push --linked + supabase functions deploy</name>
  <files></files>
  <read_first>
    - .planning/phases/44-m4-community-feed-foundation/44-01-PLAN.md (4 migrations)
    - .planning/phases/44-m4-community-feed-foundation/44-02-PLAN.md (1 migration)
    - .planning/phases/44-m4-community-feed-foundation/44-09-PLAN.md (1 admin-policies migration)
    - reference_supabase_migration_filename_regex (in memory)
    - reference_supabase_back_dated_migration_blocks_push (in memory)
    - reference_supabase_functions_deploy_no_linked_flag (in memory)
    - feedback_phase_close_out_db_push_verification (in memory)
  </read_first>
  <action>
    **[BLOCKING — this task must complete fully before Task 2 runs]**

    Pre-checks before push:
    1. Migration filename collision sweep:
       `cd /Users/karstenhaldan/minisite && ls supabase/migrations/20270720*.sql`
       Expected exactly 6 files: 20270720000001..000006 (community_schema, community_rls, community_media_bucket, p44_notification_community, p44_community_secdef_rpcs, p44_community_spaces_admin_policies).
    2. Letter-suffix scan (per reference_supabase_migration_filename_regex): `ls supabase/migrations/ | grep -E '^[0-9]{14}[a-z]_'` — must return 0 results.
    3. Back-dated migration scan (per reference_supabase_back_dated_migration_blocks_push): `ls supabase/migrations/2026*.sql 2>/dev/null` — should return ONLY already-applied historical migrations (no NEW back-dated files).

    Supabase CLI link check:
    4. `cd /Users/karstenhaldan/minisite/supabase && cat .temp/project-ref` — must match `ytnsipxxmzgaebkqmokp` per memory reference_supabase_project. If missing, run `supabase link --project-ref ytnsipxxmzgaebkqmokp` interactively (HUMAN checkpoint if not linked).

    Mux secret presence check (HUMAN gate if missing):
    5. `cd /Users/karstenhaldan/minisite/supabase && supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep -E '^(MUX_TOKEN_ID|MUX_TOKEN_SECRET|MUX_WEBHOOK_SECRET)\\b'`.
       Expected: all 3 names present (values masked).
       If ANY of the 3 are missing: PAUSE here with a checkpoint:decision tag asking the operator to run the user_setup steps from frontmatter. Don't proceed to deploy without them — mux-create-upload + mux-webhook will 500 at first invocation.

    Push migrations:
    6. `cd /Users/karstenhaldan/minisite/supabase && supabase db push --linked 2>&1 | tee /tmp/p44_push.log`. Expected: 6 new migrations applied. Grep for `^Skipping` in /tmp/p44_push.log — must be empty (per reference_supabase_migration_filename_regex). If any "Skipping" or non-zero exit: STOP, debug filename, do not deploy Fns.

    Deploy Edge Fns (per reference_supabase_functions_deploy_no_linked_flag — DO NOT use --linked flag with v2.100.0+):
    7. `cd /Users/karstenhaldan/minisite/supabase && supabase functions deploy mux-create-upload --project-ref ytnsipxxmzgaebkqmokp 2>&1`
    8. `supabase functions deploy mux-webhook --project-ref ytnsipxxmzgaebkqmokp 2>&1`
    9. `supabase functions deploy notify-community --project-ref ytnsipxxmzgaebkqmokp 2>&1`

    Smoke check (post-deploy):
    10. `curl -s -o /dev/null -w "%{http_code}\\n" https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/mux-webhook` — expected 401 (missing Mux-Signature) NOT 500 (deploy failure).
    11. `curl -s -o /dev/null -w "%{http_code}\\n" -X POST https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/notify-community` — expected 401 (missing bearer).
    12. `curl -s -o /dev/null -w "%{http_code}\\n" -X POST https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/mux-create-upload` — expected 401.

    Run the full automated suite (post-push, Wave 0/1 tests now executable):
    13. `cd /Users/karstenhaldan/minisite/leanshot && npm test -- src/lib/community tests/unit/community-*.test.ts 2>&1 | tail -30`
    14. `cd /Users/karstenhaldan/minisite/leanshot && npx vitest run tests/rls/community-spaces-rls.test.ts tests/rls/community-tier-gating-rls.test.ts tests/rls/community-reactions-rls.test.ts -x 2>&1 | tail -30`
    15. `cd /Users/karstenhaldan/minisite/leanshot && npx vitest run tests/integration/community-mention-notification.test.ts tests/integration/mux-tier-gate.test.ts -x 2>&1 | tail -30`
    16. Edge Fn Deno test sweep per feedback_post_merge_deno_sweep_pattern: `$HOME/.deno/bin/deno test --no-check /Users/karstenhaldan/minisite/supabase/functions/mux-create-upload /Users/karstenhaldan/minisite/supabase/functions/mux-webhook /Users/karstenhaldan/minisite/supabase/functions/notify-community 2>&1 | tail -20`

    If any of steps 6–16 fail, STOP and route to gap-closure planning rather than proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/supabase && supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep -E '^(MUX_TOKEN_ID|MUX_TOKEN_SECRET|MUX_WEBHOOK_SECRET)\\b' | wc -l | grep '3' && supabase db push --linked --dry-run 2>&1 | grep -cE '^Would (apply|push)' | (! grep '^0$') && curl -s -o /dev/null -w "%{http_code}" https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/mux-webhook | grep -E '^(401|405)$' && curl -s -o /dev/null -w "%{http_code}" -X POST https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/notify-community | grep '^401$'</automated>
  </verify>
  <acceptance_criteria>
    - All 3 Mux Function Secrets present (Step 5 returns 3 names).
    - `supabase db push --linked` exits 0; `grep -c '^Skipping' /tmp/p44_push.log` returns 0.
    - All 3 Edge Fn deploys succeed (CLI exit 0).
    - Smoke curls return 401 (not 500 — proves Fns are running and rejecting unauthenticated requests, not crashing).
    - Steps 13–15 (vitest unit + RLS + integration) all green.
    - Step 16 (Deno test sweep across 3 Edge Fns) all green.
    - Cross-tenant impersonation test from 44-01 (community-spaces-rls.test.ts) passes against the LIVE Supabase project (memory: reference_supabase_project: "every RLS surface gets a live cross-tenant impersonation proof test").
  </acceptance_criteria>
  <done>All migrations applied, all Edge Fns deployed, all automated tests green against live project. Phase 44 backend + middleware is live.</done>
</task>

<task type="auto">
  <name>Task 2: Bundle-budget CI gate + author Playwright e2e cross-tab spec</name>
  <files>leanshot/tests/e2e/community/community-feed.spec.ts, leanshot/playwright.config.ts</files>
  <read_first>
    - leanshot/scripts/assert-bundle-budget.sh (current ceilings; community-feed=20, community-media + community-mentions added by 44-09)
    - leanshot/playwright.config.ts (existing projects + addInitScript pattern)
    - .planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md §Pitfall 1
    - reference_playwright_state_seeding (in memory)
    - reference_playwright_conditional_project_argv (in memory)
  </read_first>
  <action>
    Bundle-budget gate (CI script run):
    1. `cd /Users/karstenhaldan/minisite/leanshot && npm run build 2>&1 | tee /tmp/p44_build.log`. Expected: build exits 0.
    2. `bash scripts/assert-bundle-budget.sh dist/assets 2>&1 | tee /tmp/p44_budget.log`. Expected: rows for community-feed, community-media, community-mentions all show `OK` not `FAIL`. The community-feed row must show actual_kb ≤ 20.
    3. Per reference_bundle_budget_hash_hyphen, verify chunk-hash detection: `find dist/assets -regex '.*/community-feed-[A-Za-z0-9_-]\\{8\\}.js$' | wc -l` returns 1.

    Author `leanshot/tests/e2e/community/community-feed.spec.ts`:
    - Use Playwright with the project's standard session-seeding via `page.addInitScript` (NOT page.evaluate per reference_playwright_state_seeding — race condition with supabase-js INITIAL_SESSION).
    - Test 1 (COMMUNITY-01 + COMMUNITY-02 happy path):
      - Sign in as Pro-tier 'alice' via addInitScript-seeded session.
      - Navigate to a Free-tier space.
      - Type body 'Hello world' in composer; submit.
      - Assert the new post card renders with `Hello world` text via locator.
      - Click the 🔥 reaction pill; assert it becomes aria-pressed=true with count 1.
      - Click again; assert count returns to 0 (idempotent toggle).
    - Test 2 (COMMUNITY-05 cross-tab realtime):
      - Open two browser contexts (alice, bob). Both navigate to the same Free-tier space.
      - alice posts 'Realtime test'.
      - Assert bob's page sees the new post within 3 seconds WITHOUT reload (`await bob.getByText('Realtime test').waitFor({ timeout: 3000 });`).
    - Test 3 (COMMUNITY-06 tier-locked card):
      - Sign in as Free-tier 'charlie'.
      - Open the space list page.
      - Assert that a Pro-tier-only space renders as a locked card with an `Upgrade` button.
      - Click Upgrade; assert navigation to `/pricing`.
    - Test 4 (D-10 markdown XSS defense — automation of T-44-05):
      - Sign in as alice; post body `<script>window.__pwned = 1;</script>Plain text`.
      - Reload; assert post body renders as `Plain text` (script stripped).
      - Assert `await page.evaluate(() => window.__pwned)` is undefined.
    - playwright.config.ts: add a `community` project entry if not present. Use env-var detection per reference_playwright_conditional_project_argv (`process.env.PLAYWRIGHT_RUN_COMMUNITY === '1'`), NOT process.argv. Project base URL = local dev server `http://localhost:5173`.
    - These tests REQUIRE the dev server running + the live Supabase backend. Caller can `npm run dev` + `PLAYWRIGHT_RUN_COMMUNITY=1 npx playwright test --project=community --grep community`.

    Mux upload (Test 5) is NOT automated here — it's a HUMAN-UAT signal in 44-UAT.md because Playwright lacks a stable Mux-upload simulation and CI doesn't carry a Mux production token.

    Run the Playwright spec:
    `cd /Users/karstenhaldan/minisite/leanshot && (PLAYWRIGHT_RUN_COMMUNITY=1 npx playwright test --project=community --grep community 2>&1 | tee /tmp/p44_playwright.log) || true`
    Expected: 4 tests pass. If any fail, capture trace + STOP (gap-closure).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && ls tests/e2e/community/community-feed.spec.ts && grep -c "addInitScript" tests/e2e/community/community-feed.spec.ts | grep -v '^0$' && grep -c "Realtime test\\|aria-pressed\\|/pricing\\|window.__pwned" tests/e2e/community/community-feed.spec.ts | grep -E '^[3-9]|^[1-9][0-9]+' && bash scripts/assert-bundle-budget.sh dist/assets 2>&1 | grep -E '^community-feed\\s+20\\s+[0-9.]+\\s+OK$' && bash scripts/assert-bundle-budget.sh dist/assets 2>&1 | grep -E '^community-media\\s' | grep OK && bash scripts/assert-bundle-budget.sh dist/assets 2>&1 | grep -E '^community-mentions\\s' | grep OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/assert-bundle-budget.sh dist/assets` reports OK for community-feed, community-media, community-mentions (≥3 OK rows).
    - community-feed actual_kb ≤ 20.
    - Playwright spec file at exact path with addInitScript usage + 4 distinct test() blocks.
    - Playwright spec contains literal `Realtime test`, `aria-pressed`, `/pricing`, and `window.__pwned` (4 distinct test concerns).
    - playwright.config.ts has a `community` project entry gated on `process.env.PLAYWRIGHT_RUN_COMMUNITY` (NOT process.argv).
    - All 4 Playwright tests pass when run with `PLAYWRIGHT_RUN_COMMUNITY=1`.
  </acceptance_criteria>
  <done>Bundle ceiling held + 4 automated e2e tests green. Mux video upload signal carried to HUMAN-UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Multi-signal HUMAN-UAT — 4 discrete signals per feedback_multi_signal_human_verify_checkpoint_pattern</name>
  <files>.planning/phases/44-m4-community-feed-foundation/44-UAT.md</files>
  <what-built>
    Phase 44 backend (6 migrations applied), 3 Edge Functions deployed (mux-create-upload, mux-webhook, notify-community), 4 lib helpers + 5 read-side + 3 write-side + 4 media + 4 wiring components shipped, vite chunk rules locked, automated tests green (vitest + Deno + Playwright). What remains needs a real device + real Mux key + real Resend inbox.
  </what-built>
  <how-to-verify>
    Author `.planning/phases/44-m4-community-feed-foundation/44-UAT.md` with the following 4 discrete sections, each with its own resume-signal name (per feedback_multi_signal_human_verify_checkpoint_pattern — operator can approve some inline and carry others to a later session without blocking the phase close). For each signal, supply step-by-step instructions including URL, expected timing, expected on-screen text.

    **Signal A: Mux video upload roundtrip (COMMUNITY-04)**
      1. Open https://app.leanshot.app/ as a Pro-tier test user (use `localStorage.leanshot_v4` clear per reference_zustand_persisted_user_blocks_marketing_uat if onboarding is gating).
      2. Navigate to a Free-tier space (any one created via SpaceEditor).
      3. Click "Add video" in the composer.
      4. Upload `.planning/phases/44-m4-community-feed-foundation/fixtures/test-30s.mp4` (operator provides; if missing, any 5–30s mp4 ≤ 100 MB).
      5. Wait ≤90s for the post to flip from `video_status='processing'` → `video_status='ready'` (UI shows thumbnail).
      6. Click the thumbnail; verify MuxPlayer loads and plays.
      Resume-signal: `mux-upload-ok` or describe failure.

    **Signal B: @mention email delivery (COMMUNITY-03)**
      1. As user alice@<your-test-domain>, post a comment with body `Hello @bob` in any space where bob is a member.
      2. Open bob@<your-test-domain> inbox within 60 seconds.
      3. Verify email subject contains "mentioned you" and body links to the post.
      4. Click the link; verify it opens the space + post.
      5. As bob, toggle off `notification_settings.community_mentions` (via Settings UI when wired in Phase 45, OR via `UPDATE public.notification_settings SET email_enabled=false WHERE user_id=$bob.id AND category='community-mentions'` direct SQL).
      6. As alice, post another `@bob` mention.
      7. Verify NO email arrives within 60 seconds (toggle respected).
      Resume-signal: `mention-email-ok` or describe failure.

    **Signal C: Cross-tab realtime broadcast (COMMUNITY-05)**
      1. Open the same space in two browser tabs (Tab A as user A, Tab B as user B).
      2. In Tab A, post a comment.
      3. Verify Tab B's feed shows the new comment within 2 seconds WITHOUT reload.
      4. In Tab A, click 🎯 on a post.
      5. Verify Tab B's reaction count updates within 2 seconds.
      Resume-signal: `realtime-cross-tab-ok` or describe failure.

    **Signal D: Tier-locked discovery card → upgrade CTA (COMMUNITY-06, D-08)**
      1. Sign in as a Free-tier user.
      2. Navigate to the community space list.
      3. Verify any Pro-tier space appears as a locked card (lock icon + "Upgrade to Pro" CTA).
      4. Verify the post body content is NOT visible on the locked card (only name + member count + post count).
      5. Click "Upgrade"; verify navigation to /pricing.
      6. Sign in as a Pro-tier user; verify the same space now opens normally with full feed.
      Resume-signal: `tier-locked-discovery-ok` or describe failure.

    Per feedback_hitl_walkthrough_deferred_when_fixtures_missing: if Mux test fixture is missing OR Resend sandbox domain not yet verified, operator MAY approve "automated-verify-only" — the phase ships with carry-over to a milestone-close UAT batch (per feedback_milestone_uat_deferral_consolidation).
  </how-to-verify>
  <resume-signal>Type 4 status strings on separate lines: `mux-upload-ok|fail`, `mention-email-ok|fail`, `realtime-cross-tab-ok|fail`, `tier-locked-discovery-ok|fail`. OR `approved — automated-verify-only` to defer all 4 to milestone close.</resume-signal>
  <action>Operator executes the 4 signal walkthroughs above. Defer-or-pass per signal is allowed (multi-signal pattern); any deferrals get tracked in STATE.md by Task 4.</action>
  <verify><automated>test -f .planning/phases/44-m4-community-feed-foundation/44-UAT.md</automated></verify>
  <done>4 resume-signals collected (each ok|fail) OR `approved — automated-verify-only` provided; 44-UAT.md written documenting outcomes.</done>
</task>

<task type="auto">
  <name>Task 4: Mark phase complete + roadmap toggle + STATE.md update + VALIDATION.md nyquist toggle</name>
  <files></files>
  <read_first>
    - reference_gsd_sdk_state_complete_phase_cwd_sensitivity (in memory — cd to leanshot/ first)
    - .planning/phases/44-m4-community-feed-foundation/44-VALIDATION.md
  </read_first>
  <action>
    Per reference_gsd_sdk_state_complete_phase_cwd_sensitivity:
    1. `cd /Users/karstenhaldan/minisite/leanshot` (CRITICAL — running state.complete-phase from git root silently overwrites the wrong file).
    2. Toggle 44-VALIDATION.md frontmatter: set `nyquist_compliant: true` and `wave_0_complete: true`. (Also populate the Per-Task Verification Map table by appending one row per task across all 10 plans — fetch each plan's task names + automated verify command + REQ + threat ref. The map should have ≥20 rows when all plans complete.)
    3. Toggle ROADMAP.md `### Phase 44: M4 Community Feed Foundation` task list: change `- [ ]` → `- [x]` for the Phase 44 entry. Use direct sed: `sed -i '' 's|^### Phase 44.*$|### Phase 44: M4 Community Feed Foundation [COMPLETE]|' .planning/ROADMAP.md` (be conservative; check the actual ROADMAP line shape before sed).
    4. Append to STATE.md a `## Phase 44 closeout (2026-05-23)` section with: requirements satisfied count (6/6), Mux-secret carry-over status, any HUMAN-UAT signals deferred via the multi-signal pattern, and a pointer to the milestone v1.3+M4 UAT deferred doc if Signals A–D are partially deferred.
    5. Commit doc updates with message `docs(44): phase complete — community feed foundation`.

    Per CLAUDE.md "Aggressive foundations": the user prefers max coverage on foundation phases. Even if 1–2 HUMAN-UAT signals are deferred, the phase is COMPLETE provided all automated gates are green and the deferred items are visible in STATE.md.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && grep -c 'nyquist_compliant: true' .planning/phases/44-m4-community-feed-foundation/44-VALIDATION.md | grep -v '^0$' && grep -c 'wave_0_complete: true' .planning/phases/44-m4-community-feed-foundation/44-VALIDATION.md | grep -v '^0$' && grep -c 'Phase 44.*COMPLETE\\|- \\[x\\] \\*\\*COMMUNITY-' .planning/ROADMAP.md | grep -v '^0$' && grep -c '## Phase 44 closeout' .planning/STATE.md | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - 44-VALIDATION.md has `nyquist_compliant: true` and `wave_0_complete: true` in frontmatter.
    - Per-Task Verification Map in 44-VALIDATION.md has ≥20 populated rows (one per task across the 10 plans).
    - ROADMAP.md Phase 44 marked complete (one of: `[COMPLETE]` suffix on heading, OR all 6 COMMUNITY-XX REQ-ID checkboxes flipped to `[x]`).
    - STATE.md has a `## Phase 44 closeout` section.
    - Working tree clean OR a single closeout commit pending push.
  </acceptance_criteria>
  <done>Phase 44 marked complete in all artifacts; Phase 45 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI → Supabase production project | `supabase db push --linked` + `supabase functions deploy` operate on live data; Mux secrets are set as Function Secrets (never in code) |
| Browser → live Supabase project | All RLS + tier gating + bucket policies enforced at the database boundary; bundle-budget CI is a client-side hardening gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-CLOSEOUT-01 | Tampering (deploy without secrets) | Deploy step 7–9 | mitigate | Step 5 hard-checks Mux Function Secret presence BEFORE deploy; HUMAN checkpoint if missing. Prevents Edge Fns from going live in a 500-on-first-call state. |
| T-44-CLOSEOUT-02 | Tampering (migration silently skipped) | db push step 6 | mitigate | Pre-check sweeps filenames for letter-suffixes (reference_supabase_migration_filename_regex) and back-dated dupes (reference_supabase_back_dated_migration_blocks_push). Post-push grep for `^Skipping`. |
| T-44-CLOSEOUT-03 | Bypass of automated gates | Sign-off | mitigate | Multi-signal HUMAN-UAT documents 4 discrete signals; even if 1–2 are deferred to milestone close, the deferred items are tracked in STATE.md per feedback_milestone_uat_deferral_consolidation. |
</threat_model>

<verification>
- All 4 task `<verify>` blocks exit 0.
- Smoke curl returns 401 (not 500) for each of 3 Edge Fns.
- Bundle-budget table shows OK for community-feed, community-media, community-mentions.
- ROADMAP Phase 44 marked complete; STATE.md has closeout section.
</verification>

<success_criteria>
- All 6 Phase 44 migrations applied to production Supabase project.
- 3 Edge Functions deployed and responding correctly to unauthenticated probe (401).
- Bundle ceiling held: community-feed ≤ 20 kB gz, community-media ≤ 190 kB, community-mentions ≤ 12 kB.
- All automated tests (vitest unit, RLS, integration; Deno Edge Fn; Playwright e2e) green.
- 4-signal HUMAN-UAT documented; deferrals (if any) tracked in STATE.md.
- Phase 44 marked complete in ROADMAP + VALIDATION + STATE; Phase 45 unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/44-m4-community-feed-foundation/44-10-SUMMARY.md` summarizing migrations applied, Fns deployed, bundle results, test results, HUMAN-UAT outcomes, and Phase 45 readiness. If any signal deferred, link to `.planning/v1.3-uat-deferred.md` (or create one if not present) and add the row.
</output>
