---
phase: 33-hourly-ad-spend-etl-meta-google-tiktok
plan: "04"
type: execute
wave: 2
depends_on:
  - "33-01"
  - "33-02"
files_modified:
  - supabase/functions/meta-capi-relay/index.ts
  - supabase/functions/meta-capi-relay/index.test.ts
  - supabase/functions/cac-alert-cron/index.ts
  - supabase/functions/cac-alert-cron/index.test.ts
autonomous: true
requirements:
  - ADETL-05
  - ADETL-07

must_haves:
  truths:
    - "meta-capi-relay reads events_mirror WHERE event_name IN (aem-priority events), dedupes via event_id, POSTs to Meta CAPI"
    - "meta-capi-relay throws at runtime if eventDef.phi === true (belt+suspenders PHI guardrail per D-08)"
    - "meta-capi-relay advances the meta_capi_relay cursor in etl_cursors after each successful batch"
    - "meta-capi-relay uses SHA-256 via crypto.subtle.digest for email/phone hashing (Web Crypto; no library)"
    - "cac-alert-cron reads ad_revenue_normalized via get_cac_summary() RPC; evaluates against growth_targets; upserts cac_alerts ON CONFLICT idempotency_key"
    - "cac-alert-cron emits cac_target_breached event via captureServer + calls shutdownPostHog in finally"
    - "cac-alert-cron writes admin_notifications row for each breach via writeAdminNotification"
  artifacts:
    - path: "supabase/functions/meta-capi-relay/index.ts"
      provides: "events_mirror cursor reader + Meta CAPI batch poster + PHI runtime check"
    - path: "supabase/functions/cac-alert-cron/index.ts"
      provides: "7d CAC evaluator + alert upsert + captureServer emission"
  key_links:
    - from: "meta-capi-relay/index.ts"
      to: "events_mirror table"
      via: "SELECT WHERE id > cursor AND event_name IN aem_priority_events"
      pattern: "etl_cursors.*meta_capi_relay"
    - from: "meta-capi-relay/index.ts"
      to: "src/lib/analytics/events.ts EVENTS"
      via: "Dynamic import or shared list built at deploy time from EVENTS entries with aem_priority"
      pattern: "aem_priority"
    - from: "cac-alert-cron/index.ts"
      to: "get_cac_summary() SECDEF RPC"
      via: "admin.rpc('get_cac_summary', {...})"
      pattern: "get_cac_summary"
    - from: "cac-alert-cron/index.ts"
      to: "captureServer + shutdownPostHog"
      via: "import from _shared/posthog-server.ts"
      pattern: "shutdownPostHog"
---

<objective>
Implement the meta-capi-relay Edge Function (5-min cron, reads events_mirror AEM events, POSTs to Meta CAPI with PHI guardrail) and the cac-alert-cron Edge Function (daily 00:30 UTC, evaluates 7d CAC against growth_targets, writes alerts + PostHog event).

Purpose: meta-capi-relay closes the loop between the AEM priority register (Plan 33-02) and Meta Conversions API, enabling conversion attribution for Meta campaigns. cac-alert-cron operationalizes the CAC health signal for growth operators.

Output: 2 Edge Function directories (4 files). Both depend on Plan 33-01 tables and Plan 33-02 AEM annotations.
</objective>

<execution_context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/33-hourly-ad-spend-etl-meta-google-tiktok/33-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/33-hourly-ad-spend-etl-meta-google-tiktok/33-RESEARCH.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/ROADMAP.md
@/Users/karstenhaldan/minisite/supabase/functions/_shared/posthog-server.ts
@/Users/karstenhaldan/minisite/supabase/functions/funnel-anomaly-cron/index.ts

<interfaces>
<!-- AEM priority events list (from Plan 33-02 output): -->
<!-- meta-capi-relay must NOT dynamically import from events.ts at runtime — Edge Fn cannot resolve
     leanshot/src/ paths. Instead, build a static list of aem_priority event names in the function.
     This list is the source of truth for what to relay; it must match the events.ts annotations.
     Maintenance: update this list alongside events.ts aem_priority changes. -->

AEM_PRIORITY_EVENTS (hardcoded in meta-capi-relay/index.ts per D-07 implementation choice):
```typescript
// Source: leanshot/src/lib/analytics/events.ts — aem_priority 1-8 annotations from Plan 33-02
// MUST be kept in sync with events.ts AEM annotations.
// If an event is added/removed from top-8, update both events.ts AND this list.
const AEM_PRIORITY_EVENTS = [
  'payment_completed',      // aem_priority: 1
  'signup_completed',       // aem_priority: 2
  'activation_first_log',   // aem_priority: 3
  'payment_initiated',      // aem_priority: 4
  'refund_issued',          // aem_priority: 5
  // Slots 6-7 reserved for Phase 50 rag events if confirmed by plan-checker
] as const;
```

PHI events (from events.phi.ts — NOT in events.ts):
- PHI events are import-blocked from client zones AND from meta-capi-relay
- Runtime check: if any row's event_name IS NOT in AEM_PRIORITY_EVENTS list, skip it (defense in depth)
- The phi:true runtime check must also work: each event in the relay must be validated against a static phi=false assertion. Since we cannot import events.ts at runtime, the static list IS the phi guardrail — any event in AEM_PRIORITY_EVENTS must have been verified phi:false at build time (ESLint rule from Plan 33-02).

From posthog-server.ts:
```typescript
export type CaptureArgs = { userId: string; event: string; properties?: Record<string, unknown> };
export function captureServer(args: CaptureArgs): void;
export async function shutdownPostHog(): Promise<void>;
```

From ad-etl-utils.ts (Plan 33-01):
```typescript
export function writeAdminNotification(admin, payload: {title, body, type}): Promise<void>;
```

get_cac_summary RPC signature (from Plan 33-01 migration 08):
```typescript
admin.rpc('get_cac_summary', {
  p_source: string | null,   // null = all sources
  p_start_date: string | null, // YYYY-MM-DD
  p_end_date: string | null,
})
// Returns: Array<{network, ad_account_id, ad_id, campaign_id, spend_date, spend_usd_at_spend_date, clicks, impressions, attributed_conversions, cac_usd}>
```

events_mirror table shape (from posthog-server.ts dual-write):
```typescript
{id: string, event_name: string, user_id: string, properties: Record<string, unknown>, distinct_id: string, created_at: string}
```

CAPI batch endpoint:
POST https://graph.facebook.com/v25.0/{PIXEL_ID}/events?access_token={TOKEN}
Body: {data: [{event_name, event_time (unix), event_id, action_source:'website', user_data:{em:[sha256_email]}, custom_data:{value?, currency:'USD'}}]}
Max 1000 events per request (batch)
</interfaces>

NOTE for PLAN-CHECKER ITER-1: meta-capi-relay requires `META_PIXEL_ID` as an additional Function Secret not listed in D-03. Add to the user_setup secrets list. The relay cannot POST to CAPI without knowing which pixel to post to.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: meta-capi-relay Edge Function</name>
  <files>
    supabase/functions/meta-capi-relay/index.ts
    supabase/functions/meta-capi-relay/index.test.ts
  </files>
  <behavior>
    - Test 1: No META_ACCESS_TOKEN or META_PIXEL_ID → {ok:true, skipped:'credentials_missing'} (no vendor-gate throw)
    - Test 2: events_mirror has 2 rows with event_names in AEM_PRIORITY_EVENTS + email in properties → POST to CAPI called with hashed em field
    - Test 3: events_mirror has 1 row with event_name NOT in AEM_PRIORITY_EVENTS → row skipped, CAPI not called
    - Test 4: Cursor advances after successful batch (etl_cursors updated to max processed id)
    - Test 5: CAPI POST returns non-200 → error logged, cursor NOT advanced, 500 returned
    - Test 6: events_mirror row has properties.event_id present → event_id included in CAPI payload for dedup
    - Test 7: Non-POST request → 405
  </behavior>
  <action>
  **Source header comment (MANDATORY at top of index.ts):**
  ```
  /**
   * meta-capi-relay — Phase 33 D-07/D-08
   *
   * Reads events_mirror rows for AEM-priority events (aem_priority 1-8 per events.ts),
   * hashes user PII via SHA-256 (Web Crypto — no library), POSTs to Meta Conversions API.
   *
   * DEDUP DEPENDENCY: event_id in events_mirror.properties.event_id must match the
   * browser fbq() 4th argument eventID for Meta's 48h dedup window to work.
   * Without browser fbq pixel (out of Phase 33 scope), dedup will not fire but
   * CAPI send is still valid — just lacks dedup optimization.
   *
   * PHI GUARDRAIL (D-08 belt+suspenders):
   * - Build-time: ESLint rule blocks aem_priority on phi:true events (additive-only-events.cjs)
   * - Runtime: AEM_PRIORITY_EVENTS static list only contains verified-phi:false events
   * - Defense-in-depth: any row whose event_name is not in AEM_PRIORITY_EVENTS is SKIPPED
   *
   * Cursor tracking: etl_cursors WHERE name='meta_capi_relay'. Advances only on
   * successful CAPI POST. If CAPI fails, cursor stays at previous position and
   * next 5-min tick retries the same batch (at-least-once delivery).
   */
  ```

  Implementation:
  1. `AEM_PRIORITY_EVENTS` constant array (as above in interfaces section). These are the ONLY events this function will process — do NOT process any event not in this list.
  2. Vendor-gate on `META_ACCESS_TOKEN` AND `META_PIXEL_ID`. If either absent: return `{ok:true, skipped:'credentials_missing'}`. Note: META_PIXEL_ID is an additional secret beyond D-03 — surface in user_setup (already done in plan frontmatter).
  3. `getCursor(admin): Promise<string>` — selects from etl_cursors WHERE name='meta_capi_relay', returns value ('0' if row missing).
  4. `setCursor(admin, value: string): Promise<void>` — upserts etl_cursors SET value=? WHERE name='meta_capi_relay'.
  5. `hashField(value: string): Promise<string>` — `crypto.subtle.digest('SHA-256', new TextEncoder().encode(value.toLowerCase().trim()))` → hex string.
  6. `fetchPendingEvents(admin, cursor: string): Promise<EventMirrorRow[]>`:
     - `admin.from('events_mirror').select('*').gt('id', cursor).in('event_name', Array.from(AEM_PRIORITY_EVENTS)).order('id', {ascending:true}).limit(500)`
     - Return rows array
  7. `buildCapiPayload(rows)` — for each row:
     - Check row.event_name IN AEM_PRIORITY_EVENTS (defense-in-depth skip)
     - Extract email from row.properties.email (if present), phone from row.properties.phone (if present)
     - Hash fields present: em = SHA-256(email) if email present
     - Build payload object: {event_name: row.event_name, event_time: Math.floor(new Date(row.created_at).getTime()/1000), event_id: row.properties.event_id ?? undefined, action_source: 'website', user_data: {em: email ? [await hashField(email)] : undefined}, custom_data: {value: row.properties.value ?? undefined, currency: 'USD'}}
     - Filter out undefined fields
  8. Batch CAPI POST in chunks of 1000 per request. For each batch:
     - POST to `https://graph.facebook.com/v25.0/${pixelId}/events?access_token=${token}`
     - If response.ok: continue to next batch
     - If not ok: log error + throw (cursor will not advance)
  9. After ALL batches succeed: `setCursor(admin, String(rows[rows.length-1].id))`
  10. Return `{ok:true, events_relayed: rows.length}`

  In try/finally block: no shutdownPostHog (this function doesn't use captureServer).

  `export const __internal = { handleRun, setAdminForTest, resetAdminForTest }`

  Tests mock: admin singleton (setAdminForTest), fetch (global fetch override in test).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && deno test supabase/functions/meta-capi-relay/index.test.ts --allow-env --allow-net 2>&1 | tail -5</automated>
  </verify>
  <done>meta-capi-relay/index.ts exists with source-header comment, AEM_PRIORITY_EVENTS list, SHA-256 hashing via Web Crypto, cursor advance only on success; 7 Deno tests pass; no direct import of events.ts (static list instead).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cac-alert-cron Edge Function</name>
  <files>
    supabase/functions/cac-alert-cron/index.ts
    supabase/functions/cac-alert-cron/index.test.ts
  </files>
  <behavior>
    - Test 1: No ad_revenue_normalized rows → checked:0, fired:0, returns 200 ok
    - Test 2: CAC 7d rolling = $120, target_ltv_usd=$200, cac_multiplier=0.5 → threshold=$100, CAC breaches → cac_alerts upserted
    - Test 3: CAC 7d rolling = $80, target_ltv_usd=$200, cac_multiplier=0.5 → threshold=$100, no breach → no alert inserted
    - Test 4: Breach already exists in cac_alerts for (source, date) → UPSERT ON CONFLICT idempotency_key DO NOTHING fires, no duplicate alert
    - Test 5: captureServer called with event='cac_target_breached' + correct attrs {source, 7d_cac_usd, target_ltv_usd, breach_ratio, date} on breach
    - Test 6: writeAdminNotification called with type='cac_alert' on breach
    - Test 7: shutdownPostHog called in finally block (always, even on error)
    - Test 8: Non-POST → 405
  </behavior>
  <action>
  **cac-alert-cron/index.ts:**
  1. Import `{ captureServer, shutdownPostHog }` from `'../_shared/posthog-server.ts'`
  2. Import `{ writeAdminNotification }` from `'../_shared/ad-etl-utils.ts'`
  3. Import `{ checkServiceRoleBearer, jsonError, jsonResponse, makeLazyAdmin }` from `'../_shared/lifecycle-utils.ts'`
  4. Lazy admin singleton: `const { admin, setAdminForTest, resetAdminForTest } = makeLazyAdmin()`

  `handleRun(req)` function:
  1. Compute date strings: `today` = current UTC date as YYYY-MM-DD, `sevenDaysAgo` = 7 days prior
  2. Fetch growth_targets via `admin.from('growth_targets').select('*').eq('enabled', true)`
  3. For each target row (source, target_ltv_usd, cac_multiplier):
     a. Determine CAC source filter: if target.source === 'all' then p_source=null, else p_source=target.source
     b. Call `admin.rpc('get_cac_summary', {p_source: capiSource, p_start_date: sevenDaysAgo, p_end_date: today})` to get matview rows
     c. Compute 7d rolling CAC: sum(spend_usd_at_spend_date) / sum(attributed_conversions) across all returned rows. If sum(attributed_conversions) === 0, skip (cannot compute meaningful CAC — avoid division by zero)
     d. Compute threshold: `target_ltv_usd * cac_multiplier`
     e. If 7d_cac_usd > threshold:
        - Build idempotency_key: `target.source + '|' + today`
        - Upsert to cac_alerts: `{source: target.source, alert_date: today, cac_7d_usd, target_ltv_usd: target.target_ltv_usd, breach_ratio: cac_7d_usd/threshold}` ON CONFLICT idempotency_key DO NOTHING
        - Check if upsert inserted a new row (data non-null); if so (new alert, not duplicate):
          - `captureServer({userId: 'system', event: 'cac_target_breached', properties: {source: target.source, cac_7d_usd, target_ltv_usd: target.target_ltv_usd, breach_ratio, date: today}})` — NOTE: userId='system' is a reserved non-UUID sentinel; document in comment that CAC alerts are system events not user events; D-13 normally requires auth.users.id but system crons use 'system' as distinct_id per posthog-node convention
          - `writeAdminNotification(admin, {title: 'CAC Alert', body: `${target.source} CAC $${cac_7d_usd.toFixed(2)} exceeds threshold $${threshold.toFixed(2)} (breach ratio ${breach_ratio.toFixed(2)}x)`, type: 'cac_alert'})`
  4. Return `{ok:true, targets_evaluated: N, alerts_fired: M}`

  Deno.serve with try/finally ensuring `await shutdownPostHog()` always runs:
  ```typescript
  Deno.serve(async (req) => {
    if (req.method === 'OPTIONS') return new Response('ok', { headers: corsHeaders });
    if (req.method !== 'POST') return jsonError(405, 'method_not_allowed');
    if (!checkServiceRoleBearer(req)) return jsonError(401, 'unauthorized');
    try {
      return await handleRun(req);
    } catch (e) {
      console.warn('[cac-alert-cron] unhandled', ...);
      return jsonError(500, 'internal');
    } finally {
      await shutdownPostHog();
    }
  });
  ```

  `export const __internal = { handleRun, setAdminForTest, resetAdminForTest }`

  Tests: mock admin via setAdminForTest, mock captureServer import override in test file. Each test case is a Deno.test block.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && deno test supabase/functions/cac-alert-cron/index.test.ts --allow-env --allow-net 2>&1 | tail -5</automated>
  </verify>
  <done>cac-alert-cron/index.ts has shutdownPostHog in finally; captureServer called with event='cac_target_breached'; cac_alerts upserted ON CONFLICT idempotency_key; all 8 Deno tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta-capi-relay → Meta Graph API | outbound PHI-free event data; no PII sent except hashed email/phone |
| events_mirror (Postgres) → meta-capi-relay | internal read; service-role; only AEM_PRIORITY_EVENTS events processed |
| cac-alert-cron → get_cac_summary() RPC | SECDEF function enforces is_admin() inside RPC; cron uses service-role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04-01 | Information Disclosure | PHI event accidentally relayed to Meta CAPI | mitigate | (a) ESLint phi-aem-conflict rule blocks at build time; (b) AEM_PRIORITY_EVENTS static allowlist — only listed events processed; (c) any event_name not in list is SKIPPED at runtime |
| T-33-04-02 | Information Disclosure | Plaintext email in CAPI payload | mitigate | SHA-256 hash via crypto.subtle.digest before transmission; raw email never leaves the function |
| T-33-04-03 | Spoofing | cac-alert-cron produces alerts with system userId | accept | Documented design: system-cron events use 'system' distinct_id per PostHog cron convention; not tied to a real user; no PII involved |
| T-33-04-04 | Denial of Service | meta-capi-relay cursor stuck after persistent CAPI failure | accept | Human-visible: health badge in admin dashboard shows last_success_at stale; admin can reset cursor row manually. Auto-recovery in v1.4. |
| T-33-04-05 | Elevation of Privilege | meta-capi-relay reads events_mirror without org scope | accept | events_mirror is not org-scoped (P27 design); service-role access is intentional for relay; all events are phi:false verified |
</threat_model>

<verification>
- `grep -c "AEM_PRIORITY_EVENTS" supabase/functions/meta-capi-relay/index.ts` → 2+ (declaration + usage)
- `grep "crypto.subtle.digest" supabase/functions/meta-capi-relay/index.ts` → 1 match (SHA-256 hashing)
- `grep "etl_cursors" supabase/functions/meta-capi-relay/index.ts` → 1+ matches (cursor read/write)
- `grep "shutdownPostHog" supabase/functions/cac-alert-cron/index.ts` → 1 match in finally block
- `grep "cac_target_breached" supabase/functions/cac-alert-cron/index.ts` → 1 match (captureServer call)
- All Deno tests pass
</verification>

<success_criteria>
- meta-capi-relay: AEM_PRIORITY_EVENTS static list; SHA-256 Web Crypto hashing; cursor advances only on CAPI success; PHI runtime skip as defense-in-depth; META_PIXEL_ID added to user_setup
- meta-capi-relay source header documents fbq event_id dedup dependency
- cac-alert-cron: reads from growth_targets; evaluates 7d CAC; upserts cac_alerts with idempotency_key; emits cac_target_breached via captureServer; shutdownPostHog in finally
- All 15 Deno tests pass (7 for meta-capi-relay + 8 for cac-alert-cron)
</success_criteria>

<output>
After completion, create `.planning/phases/33-hourly-ad-spend-etl-meta-google-tiktok/33-04-SUMMARY.md`.
</output>
