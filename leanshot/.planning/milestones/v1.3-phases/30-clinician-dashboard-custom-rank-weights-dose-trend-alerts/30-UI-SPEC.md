---
phase: 30
slug: clinician-dashboard-custom-rank-weights-dose-trend-alerts
status: approved
shadcn_initialized: false
preset: none
created: 2026-05-17
reviewed_at: 2026-05-17
checker_iterations: 2
checker_outcome: 6/6 dimensions pass (iter-1: 3 BLOCKERs + 4 FLAGs inline-fixed)
---

# Phase 30 — UI Design Contract

> Visual and interaction contract for Phase 30: Clinician Dashboard + Custom Rank Weights + Dose-Trend Alerts.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (manual CSS tokens) | Codebase scan — no components.json found |
| Preset | not applicable | — |
| Component library | hand-rolled primitives in `src/components/ui/` | Phase 1–10 baseline |
| Icon library | lucide-react 0.460.0 | CLAUDE.md |
| Font (sans) | Geist, -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, sans-serif | `src/index.css --font-sans` |
| Font (mono) | Geist Mono (numeric columns, threshold values, weight percentages) | `src/index.css --font-mono` |

No shadcn initialization — project predates shadcn; design system is Tailwind v4 `@theme` tokens in `src/index.css`. Registry safety gate: not applicable.

---

## Spacing Scale

Standard 4px-base scale from `src/index.css --spacing: 0.25rem`. All spacing uses Tailwind utility multiples. No new tokens introduced.

| Token | Value | Usage in Phase 30 |
|-------|-------|-------------------|
| xs | 4px (`gap-1`, `p-1`) | Icon-to-label gaps, inline badge padding |
| sm | 8px (`gap-2`, `p-2`) | Compact row spacing, alert action button gaps |
| md | 16px (`gap-4`, `p-4`) | Default card inner padding, form field stacking |
| lg | 24px (`gap-6`, `p-6`) | Section padding inside settings tabs, dashboard card padding |
| xl | 32px (`gap-8`, `mt-8`) | Between dashboard stat-card rows |
| 2xl | 48px (`py-12`) | Empty-state vertical centering padding |
| 3xl | 64px | Not used in Phase 30 surfaces |

Exceptions: icon-only touch targets (bell icon, Acknowledge, Snooze buttons) minimum 44px hit area via `min-h-11 min-w-11` per CLAUDE.md accessibility requirement (WCAG 2.5.5 minimum touch target — not a rhythm token; multiple of 4 = 11 × 4px).

---

## Typography

Phase 30 surfaces are clinical/operator-facing. Follows the established 4-size clinic pattern detected in `WorkspaceTab.tsx`, `RosterRow.tsx`, `ScoreChip.tsx`, and `ClinicBillingCard.tsx`. No new font sizes introduced.

| Role | Size | Token | Weight | Line Height | Usage |
|------|------|-------|--------|-------------|-------|
| Label / secondary | 13px | `text-[13px]` | medium (500) | 1.5 | Alert metadata, threshold field labels, stat card sub-labels, empty-state body, **tertiary captions (matview staleness, threshold-override field notes)** |
| Body / data | 15px | `text-[15px]` | medium (500) | 1.5 | Alert summary text, patient name in alerts panel, threshold input values |
| Heading | 16px | `text-[16px]` | semibold (600) | 1.4 | Section headings (Ranking Weights, Alert Thresholds, Dashboard Overview), settings h2 |
| Page heading | 20px | `text-[20px]` | semibold (600) | 1.2 | Page-level h1 (Dashboard, Alerts) — matches WorkspaceTab h1 pattern |

Weights: **medium (500)** for body/data/labels; **semibold (600)** for headings (h1 + h2 + section). Maximum 2 weights system-wide for Phase 30 surfaces. Tertiary/muted differentiation comes from `--color-text-tertiary` (color), not weight.

Monospace: `font-mono` for weight percentage values (e.g. "40%"), threshold numeric inputs (N, M, X), and the snooze duration pill labels — matches ScoreChip precedent.

---

## Color

Source: `src/index.css` semantic tokens. No new color primitives introduced.

| Role | Token | Hex (light) | Usage |
|------|-------|-------------|-------|
| Dominant (60%) | `--color-bg` | `#f2ede0` | Page background for clinic shell, empty alert panel, settings page base |
| Secondary (30%) | `--color-surface` / `--color-surface-elevated` | `#fefcf7` / `#f6f2e8` | Stat cards (`Card variant="elevated"`), alert item rows, weights form card, threshold form card |
| Accent (10%) | `--color-primary` | `#1b4842` | Reserved elements listed below |
| Destructive | `--color-danger` | `#cf5454` | Destructive / critical states listed below |

**Accent (`--color-primary` #1b4842) reserved exclusively for:**
- Save button in `ClinicRankingWeightsForm` and `ClinicDoseTrendThresholdsForm` (primary action)
- Save button in `PatientThresholdOverrideForm`
- Active-state weight slider/input highlight border
- "View patient" link text in alert rows

**Accent (`--color-success` #45b077) reserved for:**
- Auto-resolved alert status badge tone="success"
- "Sum = 100%" validation row when weights are valid

**Destructive (`--color-danger` #cf5454) reserved for:**
- `delivery_failed` alert status badge tone="danger"
- Weights form error state ("Weights must sum to 100%")
- Threshold override destructive reset confirmation text

**Semantic alert status color map:**

| Status | Badge tone | Background | Usage |
|--------|-----------|------------|-------|
| `pending` | `--color-amber` (`#e0af4e`) — NOT `--color-warning` (which resolves to orange `#e37748`) | `--color-amber-soft` (`#f7ebcf`) | Unacknowledged, unacted alert requiring clinician attention. Badge primitive must explicitly use `--color-amber` token; if Badge only exposes `tone="warning"`, add a `tone="amber"` variant rather than re-pointing `warning` (other surfaces depend on orange semantics). |
| `snoozed` | `neutral` | `--color-surface-elevated` | Snoozed alert — visually quieted |
| `acknowledged` | `success` | `--color-sage-soft` | Clinician has acknowledged |
| `auto_resolved` | `success` | `--color-sage-soft` | Auto-resolved after 7 days |
| `delivery_failed` | `danger` | `--color-clay-soft` | Failed after 3 retries; Sentry alerted |

**Severity:** v1.3 collapses to a single severity level. No severity badge rendered. The `severity` column is persisted in DB for v1.4 ML refinement but the UI omits it entirely in Phase 30 to avoid false clinical confidence from a single-rule detector. Rationale: alert-fatigue evidence indicates multi-level severity in rule-based systems increases ignore rates vs single-level with high signal quality.

**Population-level metric stat cards:**
- "# below dosing range": `--color-amber` (#e0af4e) fill on metric number when count > 0; `--color-text-tertiary` when count = 0
- "Ack rate": `--color-success` when ack_rate_pct >= 80; `--color-amber` (#e0af4e) when 50–79; `--color-danger` when < 50

---

## Component Inventory

All Phase 30 components reuse existing UI primitives. No new design system primitives needed.

### Reused primitives (do not reimplement)

| Primitive | File | Usage in Phase 30 |
|-----------|------|--------------------|
| `Card` / `CardHeader` | `@/components/ui/Card` | Stat cards in `ClinicDashboardOverview`, form containers in settings components |
| `Button` | `@/components/ui/Button` | Save, Acknowledge, Snooze, Reset to defaults, View patient |
| `Input` | `@/components/ui/Input` | Threshold knob inputs (N, M, X), weight percentage inputs |
| `Badge` | `@/components/ui/Badge` | Alert status badges, pending-count badge on bell icon |
| `EmptyState` | `@/components/ui/EmptyState` | All 6 component empty states |
| `Skeleton` | `@/components/ui/Skeleton` | Loading states for all 6 components |
| `Spinner` | `@/components/ui/Spinner` | Inline spinner on Acknowledge/Snooze action while SECDEF is in flight |
| `Sheet` | `@/components/ui/Sheet` | `AlertSnoozePopover` on mobile — bottom sheet; on desktop, anchored panel |
| `Modal` | `@/components/ui/Modal` | Threshold override reset confirmation on `PatientThresholdOverrideForm` |
| `ScoreBreakdownPopover` pattern | `@/components/clinic/roster/ScoreBreakdownPopover` | `AlertSnoozePopover` follows same role="dialog" + focus-trap + Escape pattern |

### New components (Phase 30 deliverables)

| Component | Path | Primitive building blocks |
|-----------|------|---------------------------|
| `ClinicRankingWeightsForm` | `src/components/clinic/settings/ClinicRankingWeightsForm.tsx` | `Card`, `CardHeader`, `Input` (×4), `Button`, `Badge` (sum valid/invalid) |
| `ClinicDoseTrendThresholdsForm` | `src/components/clinic/settings/ClinicDoseTrendThresholdsForm.tsx` | `Card`, `CardHeader`, `Input` (×3), `Button` |
| `ClinicianAlertsPanel` | `src/components/clinic/alerts/ClinicianAlertsPanel.tsx` | `Badge` (bell count), `EmptyState`, `Skeleton`, alert row list |
| `AlertSnoozePopover` | `src/components/clinic/alerts/AlertSnoozePopover.tsx` | Anchored `<div role="dialog">` or `Sheet` (mobile), `Button` ×4 |
| `ClinicDashboardOverview` | `src/components/clinic/dashboard/ClinicDashboardOverview.tsx` | `Card` ×N (stat cards), `Skeleton`, `EmptyState` |
| `PatientThresholdOverrideForm` | `src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx` | `Card`, `Input` (×3), `Button`, `Modal` (reset confirm) |

---

## Interaction Contracts

### ClinicRankingWeightsForm (CLIN-01)

**Input primitive:** 4 labeled number inputs (integer 0–100, representing %). Not sliders. Rationale: clinicians configure once with precision; 4 synchronized sliders that auto-normalize are cognitively expensive when exact targets (e.g., "40% dose adherence") matter. Integer percent inputs with live sum feedback give direct control.

**Auto-normalize:** On input blur, auto-scale all 4 values proportionally to sum to 100. Show a live "Total: {N}%" counter row below the inputs — green (`--color-success`) when N=100, amber when N≠100. The Save button is disabled (aria-disabled) when total ≠ 100.

**Save flow:**
1. User edits weights → Total row updates live.
2. User clicks "Save ranking weights" → Button enters loading state (Spinner inline, aria-busy).
3. SECDEF `update_org_ranking_weights` called.
4. On success: Toast "Ranking weights saved. Roster will reorder shortly." + the `org-{hmac8}-settings` HMAC channel broadcast triggers RosterTable refresh within 1s (SC#1).
5. On error: Toast "Save failed. Try again." + button re-enabled.

**Reset to defaults:** Text link "Reset to defaults" (not a button) below Save. No confirmation modal — weights are not destructive (can be re-saved). Clicking resets inputs to equal weights (25/25/25/25).

### ClinicDoseTrendThresholdsForm (CLIN-02)

**3 knobs:** Missed doses N (integer, min 1, max 30), Window days M (integer, min 7, max 90), Variance % X (integer, min 5, max 100). All use `<Input type="number">` with explicit `min`/`max` props.

**Defaults display:** Placeholder text shows v1.3 defaults (N=2, M=14, X=25). Input value is empty until the org has saved custom thresholds; placeholder communicates the effective default.

**Save flow:** Same pattern as ClinicRankingWeightsForm. Toast on success: "Alert thresholds saved. New alerts will use these thresholds."

### ClinicianAlertsPanel (CLIN-03/06) — bell-icon placement

**Decision: bell-icon badge in `ClinicContextBar`, dropdown panel on activation.**

Rationale: a persistent right-rail would require 280px of permanent layout real estate, forcing the RosterTable to shrink on 1280px displays. The `ClinicContextBar` already exists and is present on all clinic surfaces. A `Badge tone="danger" pulse` on the bell icon communicates urgency without consuming layout. This matches the `ScoreBreakdownPopover` on-demand pattern established in Phase 10.

**Bell badge:** `<Badge tone="danger" pulse>` with pending-alert count. Hidden (aria-hidden) when count = 0. Max displayed: "9+" when pending > 9.

**Panel open:** Clicking bell opens an anchored dropdown panel (desktop: fixed-position below the bell, 380px wide, max-height 60vh with overflow-y-auto; mobile: `<Sheet>` bottom-drawer). Panel has `role="region"` `aria-label="Clinician alerts"` and closes on Escape or outside click.

**Alert rows:** Each alert row shows:
- `<Badge tone="{status-tone}">` status label (pending / snoozed / acknowledged / auto_resolved / delivery_failed)
- Alert type label: "Missed doses" or "Dose variance" (human-readable mapping of `alert_type`)
- Created-at relative time (e.g., "2 hours ago")
- "View patient" link text — navigates to `ClinicDrillInPage` for that patient
- Action buttons: "Acknowledge" + "Snooze" (pending only); "Re-open" not available (auto_resolved/acknowledged are terminal in v1.3)

**Alert grouping:** Sorted by `created_at DESC`. Pending alerts appear first, then snoozed (with snooze expiry shown), then older acknowledged/resolved (collapsed behind a "Show resolved" toggle — collapsed by default to reduce noise).

**Visual hierarchy:** Closed-state focal point = the pending-count badge on the bell icon (`Badge tone="danger" pulse`). Open-state focal point = the topmost pending alert row (first DOM child of the panel, rendered with subtly elevated background `--color-amber-soft` to anchor the eye before lower-priority groups).

**Realtime subscription:** Panel subscribes to `org-{hmac8}-alerts` HMAC channel via `channelNameFor`. New alerts received via broadcast append to the top of the pending list with a brief motion-safe enter animation (framer-motion `initial={{ opacity: 0, y: -4 }}` → `animate={{ opacity: 1, y: 0 }}`; gated by `useReducedMotion()`).

**Subscription failure state:** If the HMAC channel fails to subscribe (e.g., Vault secret unavailable), the panel renders a non-fatal inline warning: "Live updates paused. Reload to reconnect." The existing alert list still loads from the DB query. This is not a full error state — alerts are still viewable.

### AlertSnoozePopover

**4 preset duration buttons** in a 2×2 grid: "1 hour" / "4 hours" / "24 hours" / "7 days". No free-form input per D-13. Each button is `<Button variant="secondary">` with the duration label. On click: calls SECDEF `snooze_clinician_alert`, button enters loading state, popover closes on success.

Pattern mirrors `ScoreBreakdownPopover`: `role="dialog"` `aria-modal="true"` `aria-labelledby`, Escape closes, focus trap cycles through the 4 buttons + close button. On desktop: anchored 200px panel below the Snooze trigger button. On mobile: `<Sheet>` bottom-drawer with title "Snooze alert".

### ClinicDashboardOverview (CLIN-05/08)

**Layout:** Responsive CSS grid. Desktop (≥768px): 3-column stat-card row. Mobile: single-column stacked.

**Visual focal point:** The "Pending alerts" card is the primary visual anchor — rendered FIRST in DOM order, given elevated shadow (`shadow-md` vs `shadow-sm` on the other cards), and uses the larger 20px page-heading size for its numeric figure (other cards use the 16px heading size for their figures). This draws the clinician's eye to the action item before historical metrics.

**Stat cards (3–4 cards, in this DOM order):**
1. "Pending alerts" — count of `status='pending'` alerts this week; tone badge `--color-amber` when count > 0 / `--color-success` when 0. **Primary focal point per above.**
2. "Ack rate (7 days)" — from `mv_clinic_alert_metrics.ack_rate_pct`; colored per color section above
3. "Patients below dosing range" — from `mv_clinic_dose_trend_population` where `dosing_range_status='below'`; `--color-amber` fill on metric number when > 0
4. "Alert types" — breakdown by `alert_type` (dose_adherence vs dose_variance count) as two sub-figures on a single card

**Matview staleness:** A small `text-[13px] text-[var(--color-text-tertiary)]` caption below the grid: "Updated every 15 minutes · Last: {relative time}". This is not interactive. Uses the declared Label/secondary size (D4 typography contract — no new sizes).

**No chart.js in Phase 30.** Stat cards with numeric figures suffice for SC#5. chart.js would add bundle weight; defer to Phase 33+ dashboards.

### PatientThresholdOverrideForm (CLIN-07)

**Placement:** New tab or collapsible section within `ClinicDrillInPage`. Tab label: "Dose thresholds". Renders below `ClinicDrillInSubBar`.

**3 inputs:** Same labels and constraints as `ClinicDoseTrendThresholdsForm` (Missed doses N, Window days M, Variance % X). A `text-[13px] text-[var(--color-text-tertiary)]` note below: "Overrides clinic default for this patient only." Shows the effective clinic default in placeholder text. (Reuses declared Label/secondary size — no new font size.)

**Reset confirmation:** "Reset to clinic defaults" button opens a `<Modal>` with title "Reset patient thresholds" and body "This will remove the override for {patient display name} and use the clinic's default thresholds. This cannot be undone." Confirm = `<Button variant="danger">` "Reset to defaults". Cancel = `<Button variant="secondary">` "Keep current thresholds" (binary-choice label per D1 anti-generic-label rule). Modal pattern: `role="dialog"` `aria-modal="true"`.

---

## Copywriting Contract

| Element | Component | Copy |
|---------|-----------|------|
| Ranking weights form heading | `ClinicRankingWeightsForm` | "Ranking weights" |
| Ranking weights form description | `ClinicRankingWeightsForm` | "Adjust how patient signals are weighted when ordering the roster. Weights must sum to 100%." |
| Ranking weights field labels | `ClinicRankingWeightsForm` | "Dose adherence", "Weight loss", "Activity", "Symptoms" (sentence case, no abbreviations) |
| Weights sum valid | `ClinicRankingWeightsForm` | "Total: 100% — ready to save" (in `--color-success`) |
| Weights sum invalid | `ClinicRankingWeightsForm` | "Total: {N}% — must equal 100%" (in `--color-danger`) |
| Primary CTA — save weights | `ClinicRankingWeightsForm` | "Save ranking weights" |
| Reset weights link | `ClinicRankingWeightsForm` | "Reset to equal weights" |
| Save success toast — weights | `ClinicRankingWeightsForm` | "Ranking weights saved. Roster will reorder shortly." |
| Save error toast — weights | `ClinicRankingWeightsForm` | "Save failed — please try again." |
| Weights empty state (first load, null weights) | `ClinicRankingWeightsForm` | Not an empty state — form renders with equal-weight defaults pre-filled |
| Dose thresholds form heading | `ClinicDoseTrendThresholdsForm` | "Alert thresholds" |
| Dose thresholds form description | `ClinicDoseTrendThresholdsForm` | "Alerts fire when a patient misses more doses than the threshold within the observation window, or when injection interval variance exceeds the set percentage." |
| Threshold field labels | `ClinicDoseTrendThresholdsForm` | "Missed doses (N)", "Observation window (days)", "Interval variance (%)" |
| Primary CTA — save thresholds | `ClinicDoseTrendThresholdsForm` | "Save thresholds" |
| Save success toast — thresholds | `ClinicDoseTrendThresholdsForm` | "Alert thresholds saved. New alerts will use these thresholds." |
| Alerts panel heading | `ClinicianAlertsPanel` | "Clinician alerts" |
| Alerts panel empty — no alerts | `ClinicianAlertsPanel` | Heading: "No active alerts" / Body: "Your patients are within the configured thresholds. Check back after the nightly scan." |
| Alerts panel error — load failed | `ClinicianAlertsPanel` | "Could not load alerts. Reload the page to try again." (inline, not toast) |
| Alerts panel — realtime paused | `ClinicianAlertsPanel` | "Live updates paused. Reload to reconnect." (inline warning, non-blocking) |
| Alert type — dose adherence | `ClinicianAlertsPanel` alert row | "Missed doses" |
| Alert type — dose variance | `ClinicianAlertsPanel` alert row | "Dose variance" |
| Acknowledge button | `ClinicianAlertsPanel` alert row | "Acknowledge" (not "Ack" — clinical professionalism) |
| Snooze button | `ClinicianAlertsPanel` alert row | "Snooze" |
| View patient link | `ClinicianAlertsPanel` alert row | "View patient" |
| Snooze popover heading | `AlertSnoozePopover` | "Snooze alert" |
| Snooze duration labels | `AlertSnoozePopover` | "1 hour", "4 hours", "24 hours", "7 days" (spelled out, not abbreviations) |
| Dashboard overview heading | `ClinicDashboardOverview` | "Clinic overview" |
| Dashboard stat — pending alerts | `ClinicDashboardOverview` | Heading: "Pending alerts" |
| Dashboard stat — ack rate | `ClinicDashboardOverview` | Heading: "Ack rate (7 days)" |
| Dashboard stat — below range | `ClinicDashboardOverview` | Heading: "Below dosing range" / Sub: "This week" |
| Dashboard stat — alert types | `ClinicDashboardOverview` | Heading: "Alert types" / Sub-labels: "Missed doses", "Dose variance" |
| Dashboard empty state | `ClinicDashboardOverview` | Heading: "No data yet" / Body: "Metrics appear after the first nightly alert scan. Check back tomorrow." |
| Dashboard matview staleness caption | `ClinicDashboardOverview` | "Updated every 15 minutes · Last: {relative time}" |
| Patient threshold override heading | `PatientThresholdOverrideForm` | "Dose thresholds for this patient" |
| Patient threshold override description | `PatientThresholdOverrideForm` | "Overrides the clinic's default thresholds for this patient only. Leave blank to use the clinic default." |
| Primary CTA — save override | `PatientThresholdOverrideForm` | "Save patient thresholds" |
| Reset override button | `PatientThresholdOverrideForm` | "Reset to clinic defaults" |
| Reset confirm modal title | `PatientThresholdOverrideForm` > `Modal` | "Reset patient thresholds" |
| Reset confirm modal body | `PatientThresholdOverrideForm` > `Modal` | "This will remove the override for this patient and use the clinic's default thresholds." |
| Reset confirm CTA | `PatientThresholdOverrideForm` > `Modal` | "Reset to defaults" (Button variant="danger") |
| Reset cancel | `PatientThresholdOverrideForm` > `Modal` | "Keep current thresholds" |
| Reset confirm modal body | `PatientThresholdOverrideForm` > `Modal` | "This will remove the override for {patient display name} and use the clinic's default thresholds. This cannot be undone." (canonical — overrides any conflicting wording in Interaction Contracts) |
| Patient threshold empty (no override) | `PatientThresholdOverrideForm` | Not an empty state — form renders with clinic-default placeholders; saving creates the override |
| Patient threshold save success toast | `PatientThresholdOverrideForm` | "Patient thresholds saved." |
| Patient threshold reset success toast | `PatientThresholdOverrideForm` | "Thresholds reset to clinic defaults." |

**Destructive actions in Phase 30:**

| Action | Component | Confirmation approach |
|--------|-----------|----------------------|
| Reset patient thresholds to clinic default | `PatientThresholdOverrideForm` | `<Modal role="dialog">` with explicit "Reset to defaults" danger button + Cancel. No inline inline-confirm (destructive action on clinical config). |

**PHI discipline in copy:** No patient names, diagnoses, drug names, or dose values appear in any alert notification copy, email subject, or badge label. Alert rows in the panel identify patients by their `display_name` (non-medical identifier, already stored in `profiles`) — this is consistent with how `RosterRow.tsx` identifies patients. The email delivery (D-02) uses only `{org_name}` and a deep-link URL, no patient data.

---

## Accessibility Requirements

(Inherited from CLAUDE.md; specified per-component for Phase 30 surfaces)

| Requirement | Implementation |
|-------------|---------------|
| Bell icon button | `aria-label="Clinician alerts, {N} pending"` (updates with count); `aria-haspopup="dialog"` |
| Bell badge count | `<span aria-live="polite" aria-atomic="true">` wraps the badge so screen readers announce count changes |
| Alerts panel | `role="region"` `aria-label="Clinician alerts"` on the panel container |
| AlertSnoozePopover | `role="dialog"` `aria-modal="true"` `aria-labelledby` → heading ID; focus trap; Escape closes; return focus to Snooze button on close |
| Reset threshold Modal | `role="dialog"` `aria-modal="true"` (uses existing `Modal` primitive — already compliant) |
| Weight inputs | `aria-label="{signal name} weight"` `aria-describedby` → sum row ID |
| Sum validation row | `role="status"` `aria-live="polite"` so screen reader announces "Total: 100% — ready to save" on input change |
| Acknowledge button | Explicit text "Acknowledge" — no icon-only usage; if icon added later, `aria-label` required |
| Snooze button | Explicit text "Snooze" — same rule |
| Loading states | `aria-busy="true"` on buttons during SECDEF flight |
| Pending-count badge | `aria-hidden="true"` on the visual badge; count also announced via `aria-label` on the bell button |
| Animations | All framer-motion entry animations gated by `useReducedMotion()` — static render when reduced-motion preferred |
| Stat cards | Each card's figure has `aria-label="{heading}: {value}"` for screen-reader comprehension |

---

## Success Criteria Visual Support Map

| SC | Success Criterion | UI Contract that supports it |
|----|-------------------|------------------------------|
| SC#1 | Roster reorders within 1s of weight save | Save button → success toast → HMAC `org-{hmac8}-settings` broadcast → RosterTable refresh. Toast text explicitly says "Roster will reorder shortly" to set expectation. |
| SC#3 | In-app notification within seconds of HMAC broadcast | `ClinicianAlertsPanel` subscribes to `org-{hmac8}-alerts`; new alert appends to top with motion-safe entry animation; Badge pulse count updates via `aria-live`. |
| SC#4 | Acknowledge / snooze / auto-resolve visually distinguished | Status badge color-coding (pending=amber, snoozed=neutral, acknowledged=success, auto_resolved=success, delivery_failed=danger). Snoozed alerts show "Snoozed until {relative time}" sub-label. "Show resolved" toggle collapses terminal states. |
| SC#5 | Aggregate metrics in dashboard | `ClinicDashboardOverview` 3–4 stat cards from `mv_clinic_alert_metrics` + `mv_clinic_dose_trend_population`. Matview staleness caption shown. Color coding on metric values (amber/green/red) communicates clinical significance without requiring chart. |

---

## Bundle Constraint Reminder

All 6 new components must be `React.lazy()`-loaded. They live inside the `/clinic/*` lazy chunk and must not be imported eagerly from any path outside that chunk. No new heavy dependencies (chart.js deferred; no new icon library). Estimated impact: < 8 kB gz for all 6 components combined (no new primitives, no chart, no external libraries).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — project does not use shadcn |
| Third-party | none | not applicable |

No third-party registries declared for Phase 30. All components built from existing in-repo UI primitives.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 30 — Clinician Dashboard + Custom Rank Weights + Dose-Trend Alerts*
*UI-SPEC generated: 2026-05-17*
*Source artifacts: 30-CONTEXT.md (18 decisions D-01..D-18), REQUIREMENTS.md CLIN-01..08, CLAUDE.md, src/index.css @theme tokens, existing clinic component patterns (Phase 10, 28, 29)*
