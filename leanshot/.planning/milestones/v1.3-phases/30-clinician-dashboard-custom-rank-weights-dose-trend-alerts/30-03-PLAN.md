---
phase: 30
plan: 03
type: execute
wave: 1
depends_on: ["30-00"]
files_modified:
  - leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.tsx
  - leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.test.tsx
  - leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx
  - leanshot/src/components/clinic/alerts/AlertSnoozePopover.test.tsx
  - leanshot/src/components/clinic/alerts/use-clinician-alerts-realtime.ts
  - leanshot/src/components/clinic/alerts/use-clinician-alerts.ts
  - leanshot/src/components/clinic/ClinicContextBar.tsx
  - leanshot/src/components/ui/Badge.tsx
autonomous: true
requirements: [CLIN-03, CLIN-04, CLIN-06]
user_setup: []

must_haves:
  truths:
    - "(D-13) ClinicContextBar renders a bell-icon button + `<Badge tone='amber' pulse>` count badge of pending alerts; badge hidden (aria-hidden) when count=0; max display '9+'; aria-label includes count (`Clinician alerts, {N} pending`); aria-haspopup='dialog'"
    - "(D-13) Clicking the bell opens `ClinicianAlertsPanel` as anchored dropdown (desktop, fixed-position 380px) OR `<Sheet>` bottom-drawer (mobile); panel has `role='region' aria-label='Clinician alerts'`; closes on Escape or outside click"
    - "(D-13) Panel rows show status Badge (pending/snoozed/acknowledged/auto_resolved/delivery_failed) using the EXACT color map from 30-UI-SPEC §Color: pending → --color-amber (NOT --color-warning); snoozed → neutral; ack/auto_resolved → success; delivery_failed → danger"
    - "(D-13) Alert row action buttons: 'Acknowledge' + 'Snooze' (pending only); 'View patient' link → ClinicDrillInPage deep link; resolved alerts hidden behind 'Show resolved' toggle (collapsed by default)"
    - "(D-10) Acknowledge calls SECDEF `acknowledge_clinician_alert(p_alert_id)` (Plan 30-00); Snooze opens `AlertSnoozePopover` then calls SECDEF `snooze_clinician_alert(p_alert_id, p_duration)` with one of '1h'|'4h'|'24h'|'7d' literals; both ops use Spinner inline + aria-busy; toast on success/error"
    - "(D-13) `AlertSnoozePopover` renders 4 preset duration buttons in 2×2 grid ('1 hour', '4 hours', '24 hours', '7 days') — NO free-form input; mirrors `ScoreBreakdownPopover` accessibility pattern (role='dialog' aria-modal='true' aria-labelledby + focus trap + Escape closes + returns focus to Snooze trigger)"
    - "(D-13) Panel subscribes to `channelNameFor(orgId, 'alerts')` HMAC channel via `use-clinician-alerts-realtime` hook; new broadcast events ({type:'broadcast', event:'new_alert', payload:{alert_id, alert_type}}) append the new alert to the top of the pending list with motion-safe entry animation (framer-motion gated by useReducedMotion)"
    - "(D-13) Subscription failure state: inline non-blocking warning 'Live updates paused. Reload to reconnect.' (per UI-SPEC); DB query results still load (failsafe)"
    - "(SC#3) Bell badge count updates via aria-live polite span so screen readers announce changes"
    - "Badge primitive gains a `tone='amber'` variant if not already present (UI-SPEC explicitly requires not re-pointing tone='warning' which is orange — must add amber as a distinct tone)"
  artifacts:
    - path: "leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.tsx"
      provides: "Bell-icon dropdown; pending/snoozed list with status badges; ack/snooze actions; realtime subscription"
      contains: "channelNameFor"
      min_lines: 160
    - path: "leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx"
      provides: "4-preset duration popover (role='dialog' with focus trap)"
      contains: "snooze_clinician_alert"
      min_lines: 60
    - path: "leanshot/src/components/clinic/alerts/use-clinician-alerts-realtime.ts"
      provides: "Browser HMAC channel subscription for alerts broadcast"
      contains: "channelNameFor"
    - path: "leanshot/src/components/clinic/alerts/use-clinician-alerts.ts"
      provides: "TanStack-style query hook fetching pending+snoozed+recent alerts for org; refetch on realtime broadcast"
      contains: "supabase.from('clinician_alerts')"
    - path: "leanshot/src/components/clinic/ClinicContextBar.tsx"
      provides: "Bell button + count badge + panel toggle wiring"
      contains: "ClinicianAlertsPanel"
    - path: "leanshot/src/components/ui/Badge.tsx"
      provides: "tone='amber' variant for pending alerts (per UI-SPEC §Color)"
      contains: "amber"
    - path: "leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.test.tsx"
      provides: "RTL tests: panel render, ack action, snooze action, realtime broadcast appending, subscription failure warning"
      contains: "describe"
    - path: "leanshot/src/components/clinic/alerts/AlertSnoozePopover.test.tsx"
      provides: "RTL tests: focus trap, Escape close, 4 preset buttons, SECDEF dispatch with correct duration string"
      contains: "describe"
  key_links:
    - from: "leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.tsx"
      to: "supabase/migrations/20270601300003_p30_secdef_and_triggers.sql"
      via: "supabase.rpc('acknowledge_clinician_alert', { p_alert_id })"
      pattern: "acknowledge_clinician_alert"
    - from: "leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx"
      to: "supabase/migrations/20270601300003_p30_secdef_and_triggers.sql"
      via: "supabase.rpc('snooze_clinician_alert', { p_alert_id, p_duration: '1h'|'4h'|'24h'|'7d' })"
      pattern: "snooze_clinician_alert"
    - from: "leanshot/src/components/clinic/alerts/use-clinician-alerts-realtime.ts"
      to: "leanshot/src/lib/org-realtime.ts"
      via: "channelNameFor(orgId, 'alerts') Phase 28 browser HMAC helper"
      pattern: "channelNameFor"
    - from: "leanshot/src/components/clinic/ClinicContextBar.tsx"
      to: "leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.tsx"
      via: "useState toggle + onClick handler on bell icon"
      pattern: "ClinicianAlertsPanel"
---

<objective>
Build the in-app alerts UX: bell icon + count badge in ClinicContextBar, dropdown panel showing pending/snoozed/recent alerts, per-alert Acknowledge + Snooze actions (4-preset durations), realtime subscription to `org-{hmac8}-alerts` HMAC channel for live append on new alerts.

Purpose: CLIN-03 (in-app notification within seconds of HMAC broadcast — SC#3) + CLIN-06 (clinician acknowledge/snooze workflow; auto-resolve handled by Plan 30-00 cron).

Output: 4 new files under `src/components/clinic/alerts/` + ClinicContextBar bell integration + Badge primitive `tone='amber'` extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-CONTEXT.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-RESEARCH.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-00-PLAN.md
@leanshot/src/components/clinic/ClinicContextBar.tsx
@leanshot/src/components/clinic/roster/ScoreBreakdownPopover.tsx
@leanshot/src/components/clinic/roster/use-roster-realtime.ts
@leanshot/src/components/ui/Badge.tsx
@leanshot/src/components/ui/Sheet.tsx
@leanshot/src/components/ui/Skeleton.tsx
@leanshot/src/components/ui/EmptyState.tsx
@leanshot/src/components/ui/Spinner.tsx
@leanshot/src/hooks/useReducedMotion.ts
@leanshot/src/lib/org-realtime.ts

<interfaces>
From leanshot/src/lib/org-realtime.ts:
  export async function channelNameFor(orgId: string, suffix: string): Promise<string>

From leanshot/src/components/ui/Badge.tsx (existing tones — verify before extending):
  type BadgeTone = 'success' | 'warning' | 'danger' | 'neutral'  // EXTEND: add 'amber' per UI-SPEC §Color (do NOT redirect 'warning' which is orange)
  Props: { tone?: BadgeTone; pulse?: boolean; children: ReactNode }

From leanshot/src/components/clinic/roster/ScoreBreakdownPopover.tsx:
  Pattern: role='dialog' aria-modal aria-labelledby + focus trap + Escape closes + returns focus to trigger

Phase 30 SECDEFs (Plan 30-00 Task 2):
  rpc('acknowledge_clinician_alert', { p_alert_id: string }) → returns void; raises 42501 / 22023
  rpc('snooze_clinician_alert', { p_alert_id: string, p_duration: '1h'|'4h'|'24h'|'7d' }) → returns void

CSS tokens (Tailwind v4 @theme):
  --color-amber          (#e0af4e — exact value per UI-SPEC §Color; NOT --color-warning which is orange #e37748)
  --color-amber-soft     (#f7ebcf — pending alert row background)
  --color-sage-soft      (acknowledged/auto_resolved background)
  --color-clay-soft      (delivery_failed background)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClinicianAlertsPanel + use-clinician-alerts query + realtime hook + ClinicContextBar bell + Badge amber tone</name>
  <files>
    leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.tsx,
    leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.test.tsx,
    leanshot/src/components/clinic/alerts/use-clinician-alerts.ts,
    leanshot/src/components/clinic/alerts/use-clinician-alerts-realtime.ts,
    leanshot/src/components/clinic/ClinicContextBar.tsx,
    leanshot/src/components/ui/Badge.tsx
  </files>
  <read_first>
    - leanshot/src/components/clinic/ClinicContextBar.tsx (insertion point for bell)
    - leanshot/src/components/clinic/roster/use-roster-realtime.ts (realtime hook pattern to mirror — alerts variant uses 'alerts' suffix + 'broadcast' event)
    - leanshot/src/components/ui/Badge.tsx (verify existing tones + extend with 'amber')
    - leanshot/src/components/ui/Sheet.tsx (mobile bottom-drawer pattern)
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md §"ClinicianAlertsPanel — bell-icon placement" (full layout spec)
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md §"Copywriting Contract" (exact alert label strings)
  </read_first>
  <behavior>
    Badge primitive:
    - Test 1: tone='amber' renders with `--color-amber` background per UI-SPEC; `tone='warning'` still resolves to orange (regression check — must not be re-pointed)

    use-clinician-alerts query hook:
    - Test 1: returns pending + snoozed + recent (last 7 days non-resolved) alerts for given orgId
    - Test 2: re-runs query when refetch() is called

    use-clinician-alerts-realtime hook:
    - Test 1: subscribes to channelNameFor(orgId, 'alerts')
    - Test 2: invokes onNewAlert callback when broadcast event 'new_alert' arrives
    - Test 3: subscription failure (CHANNEL_ERROR) sets isSubscribed=false (consumer renders 'Live updates paused' inline warning) WITHOUT throwing

    ClinicianAlertsPanel:
    - Test 1: closed state — bell icon visible; aria-label='Clinician alerts, {N} pending'; badge hidden when count=0
    - Test 2: count > 9 → badge text '9+'
    - Test 3: Clicking bell opens panel with role='region' aria-label='Clinician alerts'
    - Test 4: pending alert row renders amber Badge + 'Missed doses' label + 'Acknowledge' + 'Snooze' + 'View patient' buttons
    - Test 5: Clicking Acknowledge dispatches supabase.rpc('acknowledge_clinician_alert', { p_alert_id }); on success removes the row from pending list (or moves to acknowledged group) + shows toast
    - Test 6: Clicking Snooze opens AlertSnoozePopover anchored to the Snooze button
    - Test 7: Realtime broadcast (event='new_alert') appends new alert to top of pending list with motion animation (gated by useReducedMotion mock = false)
    - Test 8: Subscription failure → inline warning 'Live updates paused. Reload to reconnect.' rendered (alerts list still visible)
    - Test 9: Empty state — heading 'No active alerts', body 'Your patients are within the configured thresholds. Check back after the nightly scan.' (exact UI-SPEC copy)
    - Test 10: 'Show resolved' toggle revealing acknowledged + auto_resolved alerts (collapsed by default)
  </behavior>
  <action>
    Extend `Badge.tsx`: if tone='amber' variant not present, add a new branch in the tone-to-classes map that uses `bg-[var(--color-amber-soft)] text-[var(--color-amber)]` (verify exact pattern by reading the existing tone branches; mirror their structure). DO NOT redirect `tone='warning'` — UI-SPEC explicitly requires amber and warning be distinct (orange vs amber).

    Write `use-clinician-alerts.ts`: returns `{ data: ClinicianAlert[], isLoading, error, refetch }`. Uses `useEffect` + `useState` (matches existing project pattern; project does NOT use @tanstack/react-query in `clinic/roster`). Query: `supabase.from('clinician_alerts').select('id, org_id, patient_user_id, alert_type, status, threshold_snapshot, created_at, snooze_until, ack_at, ack_by').eq('org_id', orgId).in('status', ['pending','snoozed','acknowledged','auto_resolved']).gte('created_at', new Date(Date.now() - 7*24*60*60*1000).toISOString()).order('created_at', { ascending: false })`. Returns the rows + helpers `groupByStatus()` to slice into pending/snoozed/resolved.

    Also resolve patient display_name per alert: a separate sub-query `supabase.from('profiles').select('id, display_name').in('id', patientIds)` and JOIN client-side. Per UI-SPEC: alert rows show display_name (consistent with RosterRow.tsx) — display_name is NOT PHI per UI-SPEC §"PHI discipline".

    Write `use-clinician-alerts-realtime.ts`: mirrors `use-roster-realtime.ts` exactly. Signature `useClinicianAlertsRealtime({ orgId, onNewAlert }: { orgId: string; onNewAlert: (payload: { alert_id: string; alert_type: string }) => void }): { isSubscribed: boolean }`. Subscribe to `channelNameFor(orgId, 'alerts')` via supabase.channel(topic).on('broadcast', { event: 'new_alert' }, (msg) => onNewAlert(msg.payload as ...)).subscribe((status) => setIsSubscribed(status === 'SUBSCRIBED')). Cleanup: supabase.removeChannel.

    Write `ClinicianAlertsPanel.tsx` per 30-UI-SPEC:
    - Props: `{ orgId: string; orgSlug: string; open: boolean; onClose: () => void; anchorRef: React.RefObject<HTMLElement> }`.
    - Uses `useClinicianAlerts({ orgId })` + `useClinicianAlertsRealtime({ orgId, onNewAlert: (payload) => alertsQuery.refetch() })`.
    - Layout (desktop ≥768px): `<div role="region" aria-label="Clinician alerts" className="fixed w-[380px] max-h-[60vh] overflow-y-auto" style={anchored position below anchorRef}>`. Mobile: `<Sheet>` bottom-drawer with same role attribute.
    - Sections (DOM order): subscription warning (conditional, inline `<div role="alert">Live updates paused. Reload to reconnect.</div>`), pending alerts (highest priority, render first with `bg-[var(--color-amber-soft)]` topmost row), snoozed alerts with `Snoozed until {relative time}` sub-label, 'Show resolved' toggle revealing collapsed acknowledged + auto_resolved + delivery_failed rows.
    - Per-alert row: `<Badge tone={statusToTone(alert.status)}>{statusLabel}</Badge>` + alert_type label per UI-SPEC ('Missed doses' for dose_adherence; 'Dose variance' for dose_variance) + relative time + 'View patient' link (`<Link to={`/clinic/${orgSlug}/roster/${alert.patient_user_id}`}>View patient</Link>`) + (pending only) `<Button variant="primary" onClick={ack}>Acknowledge</Button>` + `<Button variant="secondary" onClick={openSnooze}>Snooze</Button>` (Snooze opens AlertSnoozePopover anchored to itself — see Task 2).
    - statusToTone map: pending→'amber' (NOT warning per UI-SPEC), snoozed→'neutral', acknowledged→'success', auto_resolved→'success', delivery_failed→'danger'.
    - statusLabel map: pending→'Pending', snoozed→'Snoozed', acknowledged→'Acknowledged', auto_resolved→'Auto-resolved', delivery_failed→'Delivery failed'.
    - Ack handler: `await supabase.rpc('acknowledge_clinician_alert', { p_alert_id: alert.id })`; on success toast.success per UI-SPEC + alertsQuery.refetch(); on error toast.error.
    - Motion-safe entry animation on new realtime alert: framer-motion `<motion.div initial={{ opacity: 0, y: -4 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.2 }}>` for the topmost pending row; wrap in `useReducedMotion()` check (when true, render static without motion wrapper).
    - Empty state: `<EmptyState heading="No active alerts" body="Your patients are within the configured thresholds. Check back after the nightly scan." />`.
    - Close on Escape (window listener inside useEffect) + outside click (useRef on panel + document click listener).

    Update `ClinicContextBar.tsx`:
    - Add bell icon button: `<button aria-label={`Clinician alerts, ${pendingCount} pending`} aria-haspopup="dialog" aria-expanded={panelOpen} onClick={() => setPanelOpen(o => !o)} className="relative min-h-11 min-w-11">` containing `<Bell />` (lucide-react) + `<span aria-live="polite" aria-atomic="true">{pendingCount > 0 && <Badge tone="amber" pulse aria-hidden>{pendingCount > 9 ? '9+' : pendingCount}</Badge>}</span>`.
    - Lazy-import ClinicianAlertsPanel via `React.lazy(() => import('./alerts/ClinicianAlertsPanel'))` to keep clinic-shell bundle under the 30 kB admin-shell ceiling raised in Plan 29-06 (45 kB current; alerts panel is one of multiple components Phase 30 ships into this chunk — verify post-build via Plan 30-05 phase-close).
    - Read pendingCount from a lightweight `useClinicianAlerts({ orgId, statusFilter: ['pending'] })` query that runs at ClinicContextBar level (small payload — only id + status).

    Write `ClinicianAlertsPanel.test.tsx` with 10 tests per behavior block. Use vitest + RTL + user-event. Mock `@/lib/supabase`, `@/lib/org-realtime` channelNameFor (returns 'org-aaaa1111-alerts'), `@/hooks/useToast`, `@/hooks/useReducedMotion` (default false; toggle to true in one test).
  </action>
  <verify>
    <automated>
      test -f leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.tsx && \
      test -f leanshot/src/components/clinic/alerts/use-clinician-alerts.ts && \
      test -f leanshot/src/components/clinic/alerts/use-clinician-alerts-realtime.ts && \
      grep -q 'channelNameFor' leanshot/src/components/clinic/alerts/use-clinician-alerts-realtime.ts && \
      grep -q "event: 'new_alert'" leanshot/src/components/clinic/alerts/use-clinician-alerts-realtime.ts && \
      grep -q 'acknowledge_clinician_alert' leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.tsx && \
      grep -q "tone=\"amber\"\|tone='amber'" leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.tsx && \
      grep -q "role=\"region\"" leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.tsx && \
      grep -q "aria-label=\"Clinician alerts\"" leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.tsx && \
      grep -q "No active alerts" leanshot/src/components/clinic/alerts/ClinicianAlertsPanel.tsx && \
      grep -q "amber" leanshot/src/components/ui/Badge.tsx && \
      grep -q 'aria-live="polite"' leanshot/src/components/clinic/ClinicContextBar.tsx && \
      grep -q 'aria-haspopup="dialog"' leanshot/src/components/clinic/ClinicContextBar.tsx && \
      grep -q 'ClinicianAlertsPanel' leanshot/src/components/clinic/ClinicContextBar.tsx && \
      cd leanshot && npm run test:unit -- --run src/components/clinic/alerts/ClinicianAlertsPanel.test.tsx 2>&1 | tail -10 | grep -qE "Test Files\\s+1 passed"
    </automated>
  </verify>
  <done>
    Panel renders per UI-SPEC layout + copy; bell badge uses amber not warning; realtime subscription works + survives failure; 10 tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AlertSnoozePopover (4-preset role=dialog with focus trap)</name>
  <files>
    leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx,
    leanshot/src/components/clinic/alerts/AlertSnoozePopover.test.tsx
  </files>
  <read_first>
    - leanshot/src/components/clinic/roster/ScoreBreakdownPopover.tsx (exact role=dialog + focus trap + Escape + return-focus pattern to mirror)
    - leanshot/src/components/ui/Sheet.tsx (mobile bottom-drawer fallback)
    - leanshot/src/components/ui/Button.tsx (variant='secondary' for preset buttons)
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md §"AlertSnoozePopover"
  </read_first>
  <behavior>
    - Test 1: renders 4 buttons labeled "1 hour", "4 hours", "24 hours", "7 days" (exact spellings per UI-SPEC)
    - Test 2: `role="dialog" aria-modal="true" aria-labelledby` set; heading "Snooze alert"
    - Test 3: Escape key closes popover + returns focus to the trigger button
    - Test 4: Tab cycles focus through the 4 buttons + close button (focus trap)
    - Test 5: Clicking "1 hour" dispatches supabase.rpc('snooze_clinician_alert', { p_alert_id, p_duration: '1h' })
    - Test 6: Clicking "4 hours" dispatches with p_duration='4h'; "24 hours" → '24h'; "7 days" → '7d'
    - Test 7: On success popover closes + toast "Alert snoozed"
    - Test 8: On error popover stays open + toast "Snooze failed — please try again."
    - Test 9: Mobile breakpoint (matchMedia mock <768px) → renders inside `<Sheet>` bottom-drawer with title "Snooze alert"
  </behavior>
  <action>
    Write `AlertSnoozePopover.tsx`:
    - Props: `{ alertId: string; triggerRef: React.RefObject<HTMLElement>; open: boolean; onClose: () => void }`.
    - Desktop: anchored 200px panel positioned below triggerRef (use triggerRef.current.getBoundingClientRect to compute top/left); `role="dialog" aria-modal="true" aria-labelledby="snooze-alert-heading"`. Mobile: `<Sheet open={open} onClose={onClose} title="Snooze alert">`.
    - Heading: `<h2 id="snooze-alert-heading">Snooze alert</h2>`.
    - 4 buttons in 2×2 grid (`grid grid-cols-2 gap-2`): `<Button variant="secondary" onClick={() => snooze('1h')}>1 hour</Button>` etc. Each button is in the tab order; focus moves between them via Tab; Shift+Tab + Tab beyond last cycle back to first (focus trap).
    - Focus trap implementation: mirror ScoreBreakdownPopover pattern — useEffect attaches keydown listener; on Tab at last focusable → preventDefault + focus first; Shift+Tab at first → preventDefault + focus last. On Escape → preventDefault + onClose(); useEffect cleanup also returns focus to triggerRef.current.
    - snooze('1h'|'4h'|'24h'|'7d') handler: setLoading; await supabase.rpc('snooze_clinician_alert', { p_alert_id: alertId, p_duration: duration }); on success toast.success('Alert snoozed') + onClose(); on error toast.error('Snooze failed — please try again.') + keep open.
    - Render guard: `if (!open) return null` so unmounted panel doesn't grab focus.

    Write `AlertSnoozePopover.test.tsx` with 9 tests. Mock supabase rpc, useToast, matchMedia for mobile breakpoint test.
  </action>
  <verify>
    <automated>
      test -f leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx && \
      grep -q 'role="dialog"' leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx && \
      grep -q 'aria-modal="true"' leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx && \
      grep -q 'snooze_clinician_alert' leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx && \
      grep -q "'1 hour'\|>1 hour<\|'1 hour'" leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx && \
      grep -q "'4 hours'\|>4 hours<" leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx && \
      grep -q "'24 hours'\|>24 hours<" leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx && \
      grep -q "'7 days'\|>7 days<" leanshot/src/components/clinic/alerts/AlertSnoozePopover.tsx && \
      cd leanshot && npm run test:unit -- --run src/components/clinic/alerts/AlertSnoozePopover.test.tsx 2>&1 | tail -10 | grep -qE "Test Files\\s+1 passed"
    </automated>
  </verify>
  <done>
    Popover ships with role=dialog + focus trap + Escape close + return-focus + 4 preset buttons mapping to '1h'/'4h'/'24h'/'7d' SECDEF arguments; mobile uses Sheet; 9 tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→SECDEF | Ack/Snooze RPCs re-check org-member role for alert.org_id (Plan 30-00 SECDEF body); client gate is convenience only |
| Realtime→client | HMAC channel subscribe — JWT org_ids + HMAC validation by realtime_topic_authorized (Phase 28) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Tampering | Client-side ack/snooze without server validation | mitigate | Pattern S1 dual-layer: client gate + SECDEF re-check on org_member role; ack SECDEF also enforces `status='pending'` transition guard (raises 22023 on stale state) |
| T-30-03-02 | Information disclosure | Alerts panel displays PHI in summary | mitigate | UI-SPEC §"PHI discipline" — display_name only (not PHI per UI-SPEC); no diagnosis / dose / vitals; relative time only |
| T-30-03-03 | Tampering | Realtime broadcast spoofed by attacker | mitigate | HMAC channel name + JWT org_ids claim re-checked on subscribe; sender side broadcast irrelevant to authorization |
| T-30-03-04 | Information disclosure | Bell badge count leaks alert volume to non-clinic users | mitigate | useClinicianAlerts query runs only when ClinicContextBar mounts (clinic routes only); RLS denies SELECT for non-org-members |
| T-30-03-05 | Denial of service | Realtime channel failure stalls UX | accept | Inline 'Live updates paused' warning + DB query still loads alerts on page mount + manual refresh path; degrades gracefully |
</threat_model>

<verification>
- `cd leanshot && npm run test:unit -- --run src/components/clinic/alerts/` all passes
- `cd leanshot && npx tsc --noEmit -p tsconfig.app.json` exits 0
- `cd leanshot && npm run lint -- src/components/clinic/alerts/ src/components/clinic/ClinicContextBar.tsx src/components/ui/Badge.tsx` exits 0
- Visual sanity: open ClinicContextBar with mocked pendingCount=3 → bell badge renders amber (NOT orange — verify color tokens differ)
</verification>

<success_criteria>
- Bell icon + count badge integrated into ClinicContextBar with correct aria-label/aria-haspopup/aria-live
- ClinicianAlertsPanel renders per UI-SPEC layout + copy; status color map honors `--color-amber` not `--color-warning`
- AlertSnoozePopover ships role=dialog + focus trap + 4 preset durations mapping to SECDEF p_duration argument literals
- Realtime subscription survives failure (inline warning surfaced; DB query still loads)
- Badge primitive extended with `tone='amber'` variant without redirecting `tone='warning'`
- Lazy-import keeps ClinicContextBar bundle under raised 45 kB admin-shell ceiling (verified in Plan 30-05)
</success_criteria>

<output>
After completion, create `.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-03-SUMMARY.md` documenting: alerts component file paths + Badge primitive amber-tone addition + ClinicContextBar bell integration point + lazy-load wiring + total LOC delta.
</output>
