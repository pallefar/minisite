---
phase: 40
plan: 01
type: execute
wave: 1
title: "Schema — cancellation_offers_log + save_offer_rules + Stripe coupon seed"
requirements: [POLISH-01, POLISH-02, POLISH-04]
depends_on: []
files_modified:
  - supabase/migrations/20270709000001_p40_cancellation_offers_log.sql
  - supabase/migrations/20270709000002_p40_save_offer_rules.sql
  - supabase/migrations/20270709000003_p40_stripe_coupon_seed.sql
  - supabase/tests/p40_offers_log_rls_proof.sql
  - supabase/tests/p40_save_offer_rules_rls_proof.sql
  - supabase/tests/p40_enum_check.sql
  - supabase/functions/cancellation-seed-coupons/index.ts
  - supabase/functions/cancellation-seed-coupons/index.test.ts
autonomous: true
must_haves:
  truths:
    - "cancellation_offers_log table exists with all 6 status values + all 6 offer_type values + all 7 reason values declared at CHECK-constraint time (no later widening migration needed in Phase 40)"
    - "save_offer_rules table exists with admin-only direct read; INSERT/UPDATE/DELETE revoked from authenticated (writes via 40-05 SECDEF RPCs only)"
    - "cancellation_offers_log INSERT/UPDATE/DELETE revoked from authenticated; only service-role writes; admin (support_admin+) selects"
    - "Defense-in-depth: triggers _p40_cancellation_offers_log_block_update + _block_delete fire RAISE EXCEPTION even if service_role grant leaks"
    - "subscriptions FK from cancellation_offers_log uses TEXT (Stripe sub_* id is TEXT not UUID per reference_minisite_monorepo_layout / 20260601000019_stripe_subscriptions.sql:52)"
    - "user_id FK uses ON DELETE SET NULL (matches xp_ledger / audit_logs precedent — rows survive account deletion per GDPR-audit retention)"
    - "Six Stripe coupons SAVE-{20,25,30}-{2,3}MO exist in Stripe account with duration='repeating' and idempotent seeding (re-run = no-op)"
    - "cross-tenant impersonation cannot SELECT another user's cancellation_offers_log row (live RLS proof passes)"
    - "non-admin authenticated user cannot INSERT/UPDATE/DELETE save_offer_rules (live RLS proof passes)"
  artifacts:
    - path: "supabase/migrations/20270709000001_p40_cancellation_offers_log.sql"
      provides: "table + RLS policies + append-only triggers + indexes"
      contains: "create table public.cancellation_offers_log"
    - path: "supabase/migrations/20270709000002_p40_save_offer_rules.sql"
      provides: "admin-edited rule catalog table"
      contains: "create table public.save_offer_rules"
    - path: "supabase/migrations/20270709000003_p40_stripe_coupon_seed.sql"
      provides: "registration-time marker calling Edge Fn seed (or no-op DO block if seed-via-Fn pattern is picked)"
      contains: "p40_stripe_coupon_seed"
    - path: "supabase/functions/cancellation-seed-coupons/index.ts"
      provides: "Idempotent Stripe coupon seed Fn (callable by orchestrator at deploy-time)"
      exports: ["default Deno.serve"]
    - path: "supabase/tests/p40_offers_log_rls_proof.sql"
      provides: "pgTAP cross-tenant SELECT-denial proof"
    - path: "supabase/tests/p40_save_offer_rules_rls_proof.sql"
      provides: "pgTAP admin-only write proof"
    - path: "supabase/tests/p40_enum_check.sql"
      provides: "pgTAP enum-completeness check for status + offer_type + reason"
  key_links:
    - from: "cancellation_offers_log.user_id"
      to: "profiles.user_id"
      via: "FK ON DELETE SET NULL"
      pattern: "references public\\.profiles\\(user_id\\) on delete set null"
    - from: "cancellation_offers_log.subscription_id"
      to: "subscriptions.id (TEXT)"
      via: "FK ON DELETE SET NULL"
      pattern: "references public\\.subscriptions\\(id\\) on delete set null"
    - from: "save_offer_rules.cohort_id"
      to: "cohort_definitions.id"
      via: "FK ON DELETE CASCADE (cohort deletion drops rules)"
      pattern: "references public\\.cohort_definitions\\(id\\)"
---

<objective>
Ship the data-layer foundation for Phase 40: the append-only `cancellation_offers_log` ledger, the admin-edited `save_offer_rules` catalog (read-side RLS), and the Stripe coupon pool seed (6 fixed combinations).

Purpose: every Wave 2 / Wave 3 plan reads from or writes to these tables. The append-only pattern (per PATTERNS §1) + status enum completeness at table creation (per PATTERNS §13) prevent the two recurring Phase 37/38 hot-fix categories.

Output: 3 migration files + 3 pgTAP proofs + 1 Edge Fn seed-helper + 1 test file. All RLS surfaces have live cross-tenant impersonation proofs (per LeanShot Supabase project rule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-CONTEXT.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-RESEARCH.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-PATTERNS.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-VALIDATION.md
@supabase/migrations/20270708000001_p35_xp_ledger.sql
@supabase/migrations/20270602000010_cohort_definitions.sql
@supabase/migrations/20260601000019_stripe_subscriptions.sql

<interfaces>
<!-- Extracted contracts; downstream plans (40-03, 40-05, 40-06) consume these. -->

cancellation_offers_log columns (executors copy verbatim):
  id uuid pk default gen_random_uuid()
  user_id uuid not null references profiles(user_id) on delete set null
  org_id uuid references orgs(id) on delete set null
  subscription_id text references subscriptions(id) on delete set null
  reason text not null check (reason in ('too_expensive','not_using','found_alternative','health_goals_changed','temporary_break','service_quality_issue','other'))
  reason_other_text text
  offer_type text not null check (offer_type in ('pause','discount','extended_trial','downgrade','contact_csm','none'))
  offer_config jsonb not null default '{}'::jsonb
  cohort_snapshot jsonb not null default '{}'::jsonb
  tenure_bucket text not null check (tenure_bucket in ('<30d','30-180d','>180d'))
  rule_id uuid references save_offer_rules(id) on delete set null   -- defined in 40-02 migration; declare FK in a single ALTER TABLE at end of 40-02 migration to avoid cyclic-dependency at create time
  stacking_pre_clamp_pct numeric(5,2)
  stacking_post_clamp_pct numeric(5,2)
  stacking_clamped boolean not null default false
  prior_takes_count integer not null default 0
  status text not null default 'offered' check (status in ('offered','accepted','declined','expired','ineligible_lifetime_cap','ineligible_cooldown'))
  offered_at timestamptz not null default now()
  taken_at timestamptz
  declined_at timestamptz
  posthog_variant_id text
  created_at timestamptz not null default now()

save_offer_rules columns:
  id uuid pk default gen_random_uuid()
  title text not null
  cohort_id uuid references cohort_definitions(id) on delete cascade
  tenure_buckets text[] not null default '{}'  -- subset of {'<30d','30-180d','>180d'}; empty = any
  reasons text[] not null default '{}'         -- subset of 7 reason codes; empty = any
  org_type text not null default 'any' check (org_type in ('any','consumer','clinic'))
  offer_type text not null check (offer_type in ('pause','discount','extended_trial','downgrade','contact_csm'))
  pause_months integer check (pause_months in (1,2,3))
  coupon_id text                                -- references Stripe coupon e.g. 'SAVE-25-3MO'
  extension_days integer check (extension_days in (7,14,30))
  downgrade_target text                         -- 'price_monthly' canonical
  priority integer not null default 100
  active boolean not null default false
  created_at timestamptz not null default now()
  updated_at timestamptz not null default now()
  created_by uuid references profiles(user_id) on delete set null

Indexes (cancellation_offers_log):
  idx_offers_log_user (user_id, offered_at desc)
  idx_offers_log_status_taken (status, taken_at desc) where status in ('accepted','declined')
  idx_offers_log_cohort_offer USING gin(cohort_snapshot) where status='accepted'

Indexes (save_offer_rules):
  idx_rules_active_priority (priority) where active=true
  idx_rules_cohort (cohort_id) where active=true

Stripe coupon catalog (PATTERNS §"No-analog cases"):
  SAVE-20-2MO, SAVE-20-3MO, SAVE-25-2MO, SAVE-25-3MO, SAVE-30-2MO, SAVE-30-3MO
  All: duration='repeating', metadata.source='phase_40_save_flow'
  Idempotency: try/catch on err.code === 'resource_already_exists'; idempotencyKey: `seed-${id}-v1`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — cancellation_offers_log table + RLS + append-only triggers (per PATTERNS §1, mirror xp_ledger)</name>
  <files>supabase/migrations/20270709000001_p40_cancellation_offers_log.sql, supabase/tests/p40_offers_log_rls_proof.sql, supabase/tests/p40_enum_check.sql</files>
  <action>
    Create migration file `supabase/migrations/20270709000001_p40_cancellation_offers_log.sql` mirroring xp_ledger structure at `supabase/migrations/20270708000001_p35_xp_ledger.sql`. Include: (1) create table with all columns from the &lt;interfaces&gt; block above — declare ALL six status values, ALL six offer_type values, ALL seven reason values, ALL three tenure_bucket values inside the CHECK constraints at create time (per PATTERNS §13 + per D-37-09-1 carryover lesson — no later widening). (2) Three indexes per &lt;interfaces&gt;. (3) `alter table ... enable row level security`. (4) Policy `cancellation_offers_log_select_admin` for select to authenticated using `exists (select 1 from public.admin_roles_view where user_id = auth.uid() and role >= 'support_admin')` — mirror the existing admin_roles_view check used elsewhere; if unsure of the helper name verify against `supabase/migrations/20270602000010_cohort_definitions.sql:50-75`. (5) Policy `cancellation_offers_log_service_insert` for insert to service_role with check (true) — explicit grep-able intent per Phase 19 documentation convention. (6) `revoke insert, update, delete on public.cancellation_offers_log from authenticated`. (7) Two PL/pgSQL trigger functions `_p40_cancellation_offers_log_block_update` and `_p40_cancellation_offers_log_block_delete` with `security definer set search_path = public, pg_catalog` that `raise exception 'append_only_table'` — use named dollar-quote tag `$body$` NEVER bare `$$` (per reference_postgres_dollar_quote_nesting_in_cron_body). (8) Defer the `rule_id` FK to `save_offer_rules` to the END of the 40-02 migration via separate `alter table cancellation_offers_log add constraint p40_offers_rule_fk foreign key (rule_id) references public.save_offer_rules(id) on delete set null` — DO NOT declare it in this migration (cyclic-dependency: 40-02 hasn't run yet); declare `rule_id` as nullable uuid column here only. Use FK `subscriptions(id)` typed TEXT per Phase 40 truth above + reference_minisite_monorepo_layout.

    Also write `supabase/tests/p40_offers_log_rls_proof.sql` — pgTAP test creating two test users (user A + user B), service-role inserts one row for each, asserts user A authenticated-context `select` returns ONLY their row (zero cross-tenant leakage). Mirror P25 RLS-proof test pattern (search `supabase/tests/*rls*` for nearest template — verify via `ls supabase/tests/ | head -20`).

    Also write `supabase/tests/p40_enum_check.sql` — pgTAP assertion that `information_schema.check_constraints` for `cancellation_offers_log.status` lists exactly: offered, accepted, declined, expired, ineligible_lifetime_cap, ineligible_cooldown; for `offer_type` lists exactly: pause, discount, extended_trial, downgrade, contact_csm, none; for `reason` lists all 7 values verbatim from D-18.

    Per feedback_planner_iter1_anti_patterns: no defensive jsonb shape in plpgsql; `offer_config` is opaque jsonb — its shape lives in 40-03's Deno-side `_shared/cancellation-types.ts` and 40-04's `leanshot/src/types/cancellation.ts` (split-owner pattern per PATTERNS §"TS types one-writer rule"). Implements D-01, D-02, D-03, D-15 evidence columns, D-18 reason capture, D-22 posthog_variant_id.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; supabase db reset --linked --no-seed &lt; /dev/null &amp;&amp; supabase db query --linked --file supabase/tests/p40_offers_log_rls_proof.sql &amp;&amp; supabase db query --linked --file supabase/tests/p40_enum_check.sql</automated>
    <manual>Verify migration applies cleanly on top of latest main via `supabase db push --linked --dry-run` — no "Skipping" warnings (per reference_supabase_migration_filename_regex).</manual>
  </verify>
  <done>Migration file present at exact path `supabase/migrations/20270709000001_p40_cancellation_offers_log.sql`; both pgTAP proofs green; `grep -c "check (status in" 20270709000001_p40_cancellation_offers_log.sql` ≥ 1; `grep -c "block_update\|block_delete" 20270709000001_p40_cancellation_offers_log.sql` ≥ 2; bare `$$` count is zero (`grep -E '^\s*\$\$\s*$' migration_file | wc -l` returns 0).</done>
</task>

<task type="auto">
  <name>Task 2: Migration — save_offer_rules table + RLS + deferred FK back to offers_log (per PATTERNS §2)</name>
  <files>supabase/migrations/20270709000002_p40_save_offer_rules.sql, supabase/tests/p40_save_offer_rules_rls_proof.sql</files>
  <action>
    Create migration `supabase/migrations/20270709000002_p40_save_offer_rules.sql` mirroring `supabase/migrations/20270602000010_cohort_definitions.sql` shape: (1) create table with all columns from &lt;interfaces&gt; — declare ALL 5 offer_type values + ALL 3 org_type values at CHECK time (per D-37-09-1 lesson). (2) Two indexes per &lt;interfaces&gt;. (3) `enable row level security`. (4) Policy `save_offer_rules_select_admin` for SELECT to authenticated using the same `is_admin_at_least('support_admin')` helper that cohort_definitions uses — verify helper name exists via `grep -rn 'create or replace function public.is_admin_at_least' supabase/migrations/ | head -1`. (5) `revoke insert, update, delete on public.save_offer_rules from authenticated` — writes flow through 40-05 SECDEF RPCs only. (6) At end of migration: ADD the deferred FK `alter table public.cancellation_offers_log add constraint p40_offers_rule_fk foreign key (rule_id) references public.save_offer_rules(id) on delete set null` (resolves the cyclic-dependency note from Task 1). (7) Per CONTEXT D-04 clinic-org fork: the `org_type` column gates whether a rule is consumer-only vs clinic-only vs any. (8) `updated_at` trigger — mirror `cohort_definitions` updated_at trigger pattern (copy verbatim).

    Write `supabase/tests/p40_save_offer_rules_rls_proof.sql` — pgTAP test: create one non-admin authenticated user; assert `select` from save_offer_rules succeeds (read OK); assert direct `insert` / `update` / `delete` all raise insufficient_privilege (or empty rowcount with no exception, depending on grant style) — match the assertion pattern from `supabase/tests/` cohort tests.

    Implements D-04 (org_type fork), D-05 (cohort+tenure+reason rule shape), D-12 (coupon_id field references the seeded catalog), D-13 (priority column).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; supabase db query --linked --file supabase/tests/p40_save_offer_rules_rls_proof.sql &amp;&amp; supabase db query --linked --command "select column_name from information_schema.columns where table_name='save_offer_rules' order by ordinal_position" | grep -E "cohort_id|tenure_buckets|reasons|org_type|priority|active"</automated>
  </verify>
  <done>Migration file present; pgTAP proof green; `grep -c "constraint p40_offers_rule_fk" 20270709000002_p40_save_offer_rules.sql` == 1; cohort_id FK references `cohort_definitions(id)` on delete cascade.</done>
</task>

<task type="auto">
  <name>Task 3: Stripe coupon seed — idempotent Edge Fn + migration that records "seeded" marker (per PATTERNS §"No-analog cases" + RESEARCH §Code Examples)</name>
  <files>supabase/functions/cancellation-seed-coupons/index.ts, supabase/functions/cancellation-seed-coupons/index.test.ts, supabase/migrations/20270709000003_p40_stripe_coupon_seed.sql</files>
  <action>
    Create Edge Fn `supabase/functions/cancellation-seed-coupons/index.ts`. Skeleton: (1) Import Stripe via `import Stripe from 'https://esm.sh/stripe@19?target=denonext'` and pin `apiVersion: '2026-04-22.dahlia' as Stripe.LatestApiVersion` (per PATTERNS §"Stripe SDK init"). (2) Gate via `checkServiceRoleBearer(req)` from `_shared/lifecycle-utils.ts`. (3) Loop the 6-coupon array from RESEARCH §"Stripe coupon catalog seed" (lines 791-797 verbatim — copy IDs, percent_off, duration_in_months). (4) Per coupon, call `stripe.coupons.create({ id, percent_off, duration: 'repeating', duration_in_months, name: '...', metadata: { source: 'phase_40_save_flow', cohort: 'all' } }, { idempotencyKey: `seed-${id}-v1` })`. (5) Catch `err.code === 'resource_already_exists'` and continue (per A7). (6) Return `{ created: [...], skipped: [...] }` JSON. (7) Wrap entire body in `try { ... } finally { await shutdownPostHog() }` (per PATTERNS §"PostHog flush in finally").

    Create test `supabase/functions/cancellation-seed-coupons/index.test.ts` — mock Stripe coupons.create to (a) succeed for 3 IDs, (b) throw `{ code: 'resource_already_exists' }` for the other 3, (c) verify response body `created.length === 3 && skipped.length === 3`, (d) verify NO exception bubbles. Mirror existing Edge Fn test patterns under `supabase/functions/*/index.test.ts` — find nearest analog via `ls supabase/functions/*/index.test.ts | head -3`.

    Create migration `supabase/migrations/20270709000003_p40_stripe_coupon_seed.sql` — a marker-only migration (no schema mutation). Body: insert a row into `public.migration_markers` if that table exists (search via `grep -rn 'create table public.migration_markers' supabase/migrations/` — if it does NOT exist, replace with a `do $migration_marker$ begin raise notice 'p40_stripe_coupon_seed: invoke supabase functions deploy cancellation-seed-coupons then POST /functions/v1/cancellation-seed-coupons with service-role bearer'; end $migration_marker$;` block). The Fn is invoked at deploy-time by the orchestrator in 40-06 close-out task — this migration ONLY records intent. Per RESEARCH §"Runtime State Inventory" + Assumption A7.

    Implements D-12 (6 fixed Stripe coupons), D-13 (admin assigns from catalog).
  </action>
  <verify>
    <automated>$HOME/.deno/bin/deno test --no-check --allow-env --allow-net supabase/functions/cancellation-seed-coupons/index.test.ts</automated>
    <manual>Deploy task (in 40-06 close-out): `supabase functions deploy cancellation-seed-coupons --import-map supabase/functions/import_map.json` then `curl -X POST -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" "https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/cancellation-seed-coupons"` returns `{ "created": 6, "skipped": 0 }` first invocation, `{ "created": 0, "skipped": 6 }` second invocation. Verify in Stripe Dashboard → Coupons that 6 coupons named `SAVE-{20,25,30}-{2,3}MO` exist.</manual>
  </verify>
  <done>Edge Fn skeleton + test file exist; Deno test green; migration marker file present; bare `$$` count is zero in marker migration (uses `$migration_marker$` named tag).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Edge Fn (cancellation-seed-coupons) | service-role bearer only; users never call this endpoint |
| Edge Fn→Stripe API | TLS + idempotency-key for replay safety |
| Edge Fn→Postgres | service-role admin client; RLS bypass intentional for log insert |
| authenticated user→public.save_offer_rules | RLS allows SELECT only; INSERT/UPDATE/DELETE revoked |
| service_role→public.cancellation_offers_log | INSERT allowed; UPDATE/DELETE blocked by trigger (defense-in-depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | Tampering | cancellation_offers_log row mutation post-insert (e.g., flipping status from declined→accepted) | mitigate | append-only triggers `_p40_cancellation_offers_log_block_update` + `_block_delete` raise exception on any UPDATE/DELETE; tested in pgTAP enum_check + rls_proof |
| T-40-01-02 | Information Disclosure | Patient A reads patient B's reason_other_text | mitigate | RLS policy `cancellation_offers_log_select_admin` denies authenticated unless support_admin+; pgTAP proof asserts cross-tenant SELECT returns zero rows |
| T-40-01-03 | Elevation of Privilege | Non-admin authenticated user writes save_offer_rules to manipulate offers | mitigate | `revoke insert,update,delete from authenticated`; writes only via 40-05 SECDEF RPCs gated at `is_admin_at_least('admin')` |
| T-40-01-04 | Tampering | Stripe coupon seed creates duplicates / wrong values on re-run | mitigate | idempotencyKey `seed-${id}-v1` + try/catch on `resource_already_exists`; A7 mitigation |
| T-40-01-05 | Spoofing | Anonymous caller invokes cancellation-seed-coupons | mitigate | `checkServiceRoleBearer` gates entry; 401 on missing/invalid bearer |
| T-40-01-06 | Information Disclosure | Stripe API error payload contains customer email / card details and leaks via Sentry | mitigate | Catch err, log `err.message + err.code` only (NEVER err.raw); per T-14-03-I1 |
| T-40-01-07 | Repudiation | Admin denies enabling a save_offer_rule | accept | 40-05 RPC layer logs via `log_admin_action` — out of scope for this plan; documented as 40-05 responsibility |
</threat_model>

<verification>
- All three migrations apply cleanly via `supabase db push --linked --dry-run`
- pgTAP RLS proofs both green (user A cannot read user B's row; non-admin cannot write rules)
- pgTAP enum check confirms ALL values declared at table creation (no missing values, no widening required)
- Deno test for seed Fn green (success + already_exists paths both handled)
- `grep -E '^\s*\$\$\s*$' supabase/migrations/2027070900000{1,2,3}_p40_*.sql | wc -l` returns 0 (no bare dollar quotes)
- Migration filename regex strict-compliance (14-digit + underscore + name + .sql) — `ls supabase/migrations/2027070900000{1,2,3}_p40_*.sql | wc -l` returns 3
</verification>

<success_criteria>
- Phase 40 data layer complete: cancellation_offers_log + save_offer_rules + Stripe coupon catalog exist
- Append-only invariant proven by trigger + RLS + pgTAP
- Status enum + offer_type enum + reason enum complete at table-creation (no Phase 37 hot-fix needed downstream)
- Stripe coupon seed Fn deployable and idempotent (A7 verified)
- POLISH-01 / POLISH-02 / POLISH-04 partial credit: tables ready for Wave 2/3 plans to consume
</success_criteria>

<output>
After completion, create `.planning/phases/40-cancellation-save-offers-flow/40-01-SUMMARY.md` capturing: (a) timestamp slot used (20270709000001-3); (b) helper-function name verified (`is_admin_at_least`); (c) any deviation from the &lt;interfaces&gt; column list with rationale; (d) Stripe coupon seed invocation status (deployed or deferred to 40-06).
</output>
