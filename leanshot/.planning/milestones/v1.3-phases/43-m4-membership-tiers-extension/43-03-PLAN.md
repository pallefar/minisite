---
phase: 43-m4-membership-tiers-extension
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20270715000005_p43_entitlement_helpers.sql
  - supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql
  - .planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md
autonomous: true
requirements: [MEMBER-02, MEMBER-04]
must_haves:
  # Wave 1 parallel-dispatched with 43-01, 43-02. Runtime sequencing is timestamp-driven
  # (Supabase CLI applies migrations lexicographically by filename); no compile-time imports
  # cross plan boundaries within Wave 1. Per [[feedback_planner_coordination_in_graph_not_narrative]]
  # — this is timestamp-ordering-as-graph, acceptable for migration-only plans.
  truths:
    - "`current_user_has_pro()` returns true iff `auth.uid()` has a tier_effective row with has_active=true; returns false (not null) for unauthenticated callers (auth.uid() IS NULL)."
    - "`user_has_pro(p_user_id uuid)` returns the same boolean given an explicit user_id — usable from service-role contexts that lack auth.uid()."
    - "`resolve_user_effective_price(p_user_id, p_plan)` returns the grandfathered stripe_price_id for the user's most-recent matching cohort window, falling back to the default per-plan env var resolved via stripe_price_lookup."
    - "All three functions are STABLE + SECURITY DEFINER + `set search_path = public, pg_catalog` per Pattern S1."
    - "43-PRO-GATING-CONTRACT.md exists and contains verbatim ALTER TABLE / CREATE POLICY SQL snippets that Phases 44/46/47 paste into their table-create migrations to add `pro_only` + RLS gating."
  artifacts:
    - path: "supabase/migrations/20270715000005_p43_entitlement_helpers.sql"
      provides: "current_user_has_pro() + user_has_pro(p_user_id) + stripe_price_lookup table seeded with default per-plan prices"
      contains: "create or replace function public.current_user_has_pro"
    - path: "supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql"
      provides: "resolve_user_effective_price(p_user_id, p_plan) SECDEF helper consumed by stripe-checkout in 43-04"
      contains: "create or replace function public.resolve_user_effective_price"
    - path: ".planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md"
      provides: "Verbatim SQL snippets for Phases 44/46/47 to consume — `pro_only` ADD COLUMN + partial index + RLS policy citing current_user_has_pro()"
      contains: "current_user_has_pro"
  key_links:
    - from: "current_user_has_pro()"
      to: "public.tier_effective"
      via: "SELECT has_active FROM tier_effective WHERE user_id = auth.uid()"
      pattern: "from public\\.tier_effective"
    - from: "user_has_pro(p_user_id)"
      to: "public.tier_effective"
      via: "SELECT has_active WHERE user_id = p_user_id (no auth.uid() reference)"
      pattern: "where user_id = p_user_id"
    - from: "resolve_user_effective_price()"
      to: "public.grandfathered_prices + public.cohort_is_member + public.stripe_price_lookup"
      via: "filter active windows × cohort membership; ORDER BY effective_from DESC LIMIT 1; COALESCE with default"
      pattern: "cohort_is_member"
---

<objective>
Ship the entitlement-helper SECDEF functions consumed by RLS predicates (community/courses/events in Phases 44/46/47) and the checkout-create Edge Fn (43-04). Also write the cross-phase contract `43-PRO-GATING-CONTRACT.md` that downstream phases consume verbatim.

Per the auth.uid-vs-service-role trap ([[feedback_rpc_auth_uid_vs_service_role_mismatch]]), ship BOTH `current_user_has_pro()` (authenticated-only, reads auth.uid) AND `user_has_pro(p_user_id uuid)` (service-role-friendly, takes explicit user_id) in the same migration.

Purpose: MEMBER-02 + MEMBER-04 foundations. The resolver is required by 43-04 (checkout-create lifetime + grandfathered branch). The entitlement helpers are referenced by 43-05's PaywallGate variant and by Phases 44/46/47 RLS policies through the contract.

Output:
- 2 migrations (`20270715000005`, `20270715000006`)
- 1 contract artifact (markdown, NOT code)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-m4-membership-tiers-extension/43-CONTEXT.md
@.planning/phases/43-m4-membership-tiers-extension/43-RESEARCH.md
@.planning/phases/43-m4-membership-tiers-extension/43-PATTERNS.md

# Pattern analog for SECDEF function shape
@supabase/migrations/20270602000011_cohort_membership_matview.sql

<interfaces>
<!-- This plan consumes the OUTPUT of 43-01 (tier_effective view) and 43-02 (grandfathered_prices table).
     Same wave but no files_modified overlap → independent files; at runtime,
     migrations are timestamp-ordered so 005/006 run AFTER 001-004 land. -->

From 43-01 (Plan 01 output) — public.tier_effective view, post-extension:
  Columns: user_id uuid, effective_period_end timestamptz, has_active boolean,
           has_past_due boolean, winning_provider text, tier_label text
  Predicate of interest: `has_active = true` (covers pro + lifetime + trialing per P19 D-04)

From 43-02 (Plan 02 output) — public.grandfathered_prices table:
  Columns: id, cohort_id, stripe_price_id, effective_from, effective_until, created_by, created_at

From existing P27 — `public.cohort_is_member(p_user_id uuid, p_cohort_id uuid) RETURNS boolean`:
  SECDEF STABLE, granted to authenticated; returns boolean only (no leak of cohort contents).

stripe_price_lookup (NEW in this plan, per RESEARCH §A3 — small lookup table seeded at deploy):
  Columns: plan_name text PRIMARY KEY, stripe_price_id text NOT NULL, updated_at timestamptz DEFAULT now()
  Seed rows: ('plus_monthly', '$STRIPE_PRICE_PLUS_MONTHLY'-placeholder), ('plus_yearly', ...), ('clinic_base', ...), ('clinic_overage', ...), ('lifetime', ...)
  RLS: admin-only-read (denial-by-default), seeded via admin RPC OR direct INSERT in migration as placeholders.
  Default values: empty strings; admin updates via Supabase Studio post-deploy (vendor-gated-send pattern — Edge Fn gracefully 503s if a row's stripe_price_id is empty).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 05 — entitlement helpers + stripe_price_lookup table</name>
  <files>supabase/migrations/20270715000005_p43_entitlement_helpers.sql</files>
  <action>Create the migration. Header comment names "Phase 43 Plan 03 — MEMBER-04 D-10 entitlement helpers (auth.uid-and-service-role variants per [[feedback_rpc_auth_uid_vs_service_role_mismatch]]) + MEMBER-02 D-03 stripe_price_lookup support table".

Section A — `public.current_user_has_pro()`:
  `create or replace function public.current_user_has_pro() returns boolean language sql security definer stable set search_path = public, pg_catalog as $$ select coalesce((select has_active from public.tier_effective where user_id = auth.uid()), false); $$;`
  Comment: 'P43 MEMBER-04 D-10: RLS predicate. Returns true if auth.uid() has has_active in tier_effective (covers pro+lifetime+trialing per P19 D-04). Marked STABLE so Postgres can inline at policy eval. CANNOT be invoked from service-role context (cron, fire-and-forget) — use user_has_pro(p_user_id) instead. See feedback_rpc_auth_uid_vs_service_role_mismatch.'
  `revoke all on function public.current_user_has_pro() from public; grant execute on function public.current_user_has_pro() to authenticated;`

Section B — `public.user_has_pro(p_user_id uuid)`:
  Same shape, but body reads `select coalesce((select has_active from public.tier_effective where user_id = p_user_id), false);`. No reference to auth.uid().
  Comment: 'P43 MEMBER-04 D-10: service-role-callable sibling of current_user_has_pro(). Takes explicit user_id — usable from cron / admin RPCs / Edge Fn service-role contexts that lack auth.uid().'
  `revoke all ... from public; grant execute ... to authenticated;` (admin/service-role callers reach it via SECDEF; the grant supports cohort_define-style admin RPC chains).

Section C — `public.stripe_price_lookup` table:
  `create table if not exists public.stripe_price_lookup (plan_name text primary key, stripe_price_id text not null default '', updated_at timestamptz not null default now());`
  Seed 5 rows via `insert into public.stripe_price_lookup (plan_name, stripe_price_id) values ('plus_monthly',''), ('plus_yearly',''), ('clinic_base',''), ('clinic_overage',''), ('lifetime','') on conflict (plan_name) do nothing;`. Empty defaults — operator fills via Studio post-deploy (vendor-gated-send pattern).
  RLS: enable + single policy `pol_stripe_price_lookup_admin_read FOR SELECT TO authenticated USING (public.is_admin_at_least('admin'::public.admin_role))`. No write policies; updates via service-role only.
  Comment: 'P43 MEMBER-02 D-03: maps plan_name (plus_monthly/plus_yearly/clinic_base/clinic_overage/lifetime) → stripe_price_id. Used by resolve_user_effective_price() as the fallback when no grandfathered cohort match exists. Empty stripe_price_id = vendor-gated 503 from checkout Edge Fn.'</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -n "create or replace function public.current_user_has_pro" supabase/migrations/20270715000005_p43_entitlement_helpers.sql && grep -n "create or replace function public.user_has_pro(p_user_id uuid)" supabase/migrations/20270715000005_p43_entitlement_helpers.sql && grep -n "create table if not exists public.stripe_price_lookup" supabase/migrations/20270715000005_p43_entitlement_helpers.sql && [ "$(grep -c 'security definer' supabase/migrations/20270715000005_p43_entitlement_helpers.sql)" -ge "2" ] && [ "$(grep -c 'stable' supabase/migrations/20270715000005_p43_entitlement_helpers.sql)" -ge "2" ] && [ "$(grep -c "set search_path = public, pg_catalog" supabase/migrations/20270715000005_p43_entitlement_helpers.sql)" -ge "2" ] && grep -n "where user_id = auth.uid()" supabase/migrations/20270715000005_p43_entitlement_helpers.sql && grep -n "where user_id = p_user_id" supabase/migrations/20270715000005_p43_entitlement_helpers.sql && grep -n "pol_stripe_price_lookup_admin_read" supabase/migrations/20270715000005_p43_entitlement_helpers.sql</automated>
  </verify>
  <done>Both functions exist with correct SECDEF+STABLE+search_path; auth.uid is referenced ONLY in current_user_has_pro (never in user_has_pro); stripe_price_lookup table created + 5 seed rows + admin-read RLS.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 06 — resolve_user_effective_price() SECDEF helper</name>
  <files>supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql</files>
  <action>Create the migration. Header comment names "Phase 43 Plan 03 — MEMBER-02 D-03/D-04 resolver consumed by stripe-checkout Edge Fn BEFORE sessions.create".

Function signature:
  `create or replace function public.resolve_user_effective_price(p_user_id uuid, p_plan text) returns text language plpgsql security definer stable set search_path = public, pg_catalog as $$ ... $$;`

Body:
  DECLARE
    v_default_price_id text;
    v_grandfathered    text;
  BEGIN
    -- Fallback: lookup table seeded in migration 05
    SELECT stripe_price_id INTO v_default_price_id
      FROM public.stripe_price_lookup
      WHERE plan_name = p_plan;
    -- Grandfathered override: MOST-RECENT effective_from wins (operator controls precedence)
    SELECT gp.stripe_price_id INTO v_grandfathered
      FROM public.grandfathered_prices gp
      WHERE public.cohort_is_member(p_user_id, gp.cohort_id)
        AND gp.effective_from <= now()
        AND (gp.effective_until IS NULL OR gp.effective_until > now())
      ORDER BY gp.effective_from DESC
      LIMIT 1;
    RETURN COALESCE(NULLIF(v_grandfathered, ''), NULLIF(v_default_price_id, ''));
  END;

Comment: 'P43 MEMBER-02 D-03/D-04: returns the Stripe price_id the user should be charged for plan p_plan. Grandfathered override (most-recent effective_from per RESEARCH OQ-resolution) wins over stripe_price_lookup default. Returns NULL if neither match — checkout Edge Fn responds 503 (vendor-gated-send pattern). cohort_is_member is the existing P27 SECDEF boolean helper.'

`revoke all on function public.resolve_user_effective_price(uuid, text) from public; grant execute on function public.resolve_user_effective_price(uuid, text) to authenticated;`</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -n "create or replace function public.resolve_user_effective_price" supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql && grep -n "language plpgsql" supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql && grep -n "security definer" supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql && grep -n "stable" supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql && grep -n "set search_path = public, pg_catalog" supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql && grep -n "public.cohort_is_member(p_user_id, gp.cohort_id)" supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql && grep -n "order by gp.effective_from desc" supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql && grep -n "from public.stripe_price_lookup" supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql && grep -n "grant execute on function public.resolve_user_effective_price(uuid, text) to authenticated" supabase/migrations/20270715000006_p43_resolve_user_effective_price.sql</automated>
  </verify>
  <done>Resolver function exists with correct signature; cohort_is_member is the join; MOST-RECENT effective_from wins via ORDER BY DESC LIMIT 1; falls back to stripe_price_lookup; revoke/grant complete.</done>
</task>

<task type="auto">
  <name>Task 3: Write 43-PRO-GATING-CONTRACT.md (cross-phase contract for Phases 44/46/47)</name>
  <files>.planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md</files>
  <action>Create a contract markdown file (NOT code; this file is consumed by Phase 44/46/47 planners verbatim). Structure:

1. **Header**: "Phase 43 → Phases 44/46/47 PRO-Gating Contract — MEMBER-04 D-10/D-11/D-12"
2. **Status**: "Locked — change only via /gsd-discuss-phase on Phase 43 + downstream re-plan"
3. **Consumers**: Phase 44 (community_spaces), Phase 46 (courses), Phase 47 (events)
4. **What Phase 43 ships (already in this plan):**
   - `public.current_user_has_pro()` SECDEF function — for use as RLS policy predicate
   - `public.user_has_pro(p_user_id uuid)` SECDEF function — for service-role contexts
   - `public.tier_effective` view with `tier_label` column for UI surfaces
5. **What Phase 44/46/47 MUST ship in their table-create migrations** — verbatim SQL snippets to paste:

   For each target table TBL in {community_spaces, courses, events}:

   ```sql
   -- A. Column addition (in the same migration that CREATEs the table)
   alter table public.<TBL> add column if not exists pro_only boolean not null default false;

   -- B. Partial index for the RLS predicate hot-path
   create index if not exists <TBL>_pro_only_idx on public.<TBL>(pro_only) where pro_only = true;

   -- C. RLS policy — soft-gate the SELECT (D-11 soft-block for view; the 200+PaywallGate metadata-only response is rendered by the API layer reading this policy)
   create policy pol_<TBL>_pro_only_gate
     on public.<TBL>
     for select
     to authenticated
     using (not pro_only or public.current_user_has_pro());

   -- D. Write-gating happens at the Edge Fn layer, NOT in RLS — per D-12, writes return 403 + {error:'pro_required'}.
   --    Edge Fn that POSTs to <TBL> MUST check `current_user_has_pro()` (or user_has_pro(target.user_id) for service-role) before INSERT.
   ```

6. **Acceptance grep for downstream plan-checkers**: each Phase 44/46/47 plan MUST contain `current_user_has_pro` in its migration files; plan-checker grep `current_user_has_pro` returns ≥1 hit per phase.
7. **Boundary**: Phase 43 does NOT ship `ALTER TABLE community_spaces / courses / events` migrations. Those tables don't exist on main yet. Per RESEARCH OQ-2 RESOLVED.
8. **Soft-block API contract (D-11)**: when a free-tier user queries a `pro_only=true` resource, the API returns:
   - HTTP 200
   - JSON: `{ metadata: { id, name, description, thumbnail }, body: null, paywall: true }`
   - The frontend renders the resource header + replaces body with `<PaywallUpsell gating_reason="pro_only_resource" resource_type="community|course|event" resource_name="..." />` (variant ships in 43-05).
9. **Hard-block write API contract (D-12)**: Edge Fn write endpoints (e.g., POST /community/post, POST /course/enroll, POST /event/rsvp) MUST return:
   - HTTP 403
   - JSON: `{ error: 'pro_required', upgrade_url: '/pricing?upsell=<resource_type>' }`
   - The Edge Fn calls `admin.rpc('user_has_pro', { p_user_id: user.id })` (NOT current_user_has_pro — admin client lacks auth.uid()) to make the determination.
10. **References**: link to 43-CONTEXT.md D-10..D-13 and RESEARCH OQ-2 RESOLVED.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && [ -f .planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md ] && grep -n "current_user_has_pro" .planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md && grep -n "user_has_pro" .planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md && grep -n "community_spaces" .planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md && grep -n "courses" .planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md && grep -n "events" .planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md && grep -n "pro_only boolean not null default false" .planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md && grep -n "pol_.*_pro_only_gate" .planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md && grep -n "pro_required" .planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md</automated>
  </verify>
  <done>Contract file exists with verbatim SQL snippets for 3 target tables, both helper-function names, hard/soft block API contracts, and explicit boundary statement disclaiming any ALTER TABLE shipment in Phase 43.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated caller → current_user_has_pro() | Reads auth.uid(); returns false if NULL |
| service-role caller → user_has_pro(p_user_id) | Explicit user_id param; no auth.uid() dependency |
| public → resolve_user_effective_price | Granted to authenticated; SECDEF reads tables admin-only; result is just a price_id text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03-01 | Elevation of Privilege | service-role Edge Fn calls current_user_has_pro() and gets false (auth.uid IS NULL), incorrectly hiding resources from admin views | mitigate | Ship dual variants: current_user_has_pro() for authenticated RLS use; user_has_pro(p_user_id) for service-role callers. Documented in function comments + 43-PRO-GATING-CONTRACT.md. ([[feedback_rpc_auth_uid_vs_service_role_mismatch]]) |
| T-43-03-02 | Information Disclosure | resolve_user_effective_price leaks cohort membership by returning a non-default price | accept | Returned value is only the stripe_price_id (already-public Stripe identifier); user pays the resolved price, so visibility is intentional. Cohort membership itself is NOT revealed — function returns no metadata about WHICH cohort matched. |
| T-43-03-03 | Tampering | malicious user passes p_user_id != self to user_has_pro to discover another user's tier | mitigate | user_has_pro grant to authenticated is INTENTIONAL because the boolean is admin-controlled gating info (already inferable from "can user access pro-only resources"). For added defense, the only call sites are: (a) SECDEF RPCs (admin), (b) service-role Edge Fns. Plan 43-05 PaywallGate uses the authenticated current_user_has_pro variant (self-only). Plan-checker for Phases 44/46/47 must grep that Edge Fns call user_has_pro ONLY with target-user uuid the caller is authorized to act on. |
| T-43-03-04 | Denial of Service | stripe_price_lookup row missing → checkout 503 cascades | mitigate | Vendor-gated-send pattern: checkout Edge Fn returns 503 with explicit `vendor_unconfigured` error code; frontend shows operator-facing maintenance state. Operator alerted via 43-06 close-out validation step. |
| T-43-03-05 | Information Disclosure | non-admin authenticated user reads stripe_price_lookup directly | mitigate | RLS SELECT requires `is_admin_at_least('admin')`; non-admin sees empty result set. resolve_user_effective_price (SECDEF) is the only public surface. |
</threat_model>

<verification>
- All 3 files exist at their declared paths.
- `current_user_has_pro` and `user_has_pro` BOTH appear in migration 005 (dual-variant per [[feedback_rpc_auth_uid_vs_service_role_mismatch]]).
- `resolve_user_effective_price` is plpgsql STABLE SECDEF with correct search_path.
- `43-PRO-GATING-CONTRACT.md` references all three target tables + both helper function names + verbatim ALTER/CREATE INDEX/CREATE POLICY snippets.
</verification>

<success_criteria>
- 2 migrations + 1 contract doc shipped
- Auth.uid trap mitigated via dual-variant function
- Resolver fallback chain (grandfathered → stripe_price_lookup → NULL → 503) explicit
- Downstream phases have a paste-ready contract; no Phase 43 plan ships `ALTER TABLE community_spaces/courses/events`
</success_criteria>

<output>
After completion, create `.planning/phases/43-m4-membership-tiers-extension/43-03-SUMMARY.md` covering:
- 2 migration files + function signatures
- Contract artifact path + 3 SQL snippet count
- Downstream consumers list (Phases 44/46/47) — these phases' planners must read 43-PRO-GATING-CONTRACT.md before drafting their table-create migrations
- Carry-over: db push deferred to 43-06
</output>
