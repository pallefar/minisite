---
phase: 32-spanish-i18n-parallel-with-clinic-track
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - leanshot/src/**/*.tsx  # bulk extraction sweep across all UI surfaces
  - leanshot/src/lib/helpers.ts
  - leanshot/eslint.config.js
  - leanshot/public/locales/en/common.json
  - leanshot/public/locales/en/nav.json
  - leanshot/public/locales/en/patient.json
  - leanshot/public/locales/en/onboarding.json
  - leanshot/public/locales/en/kb.json
  - leanshot/public/locales/en/admin.json
  - leanshot/public/locales/en/clinic.json
  - leanshot/public/locales/en/settings.json
  - leanshot/public/locales/es/patient.json
  - leanshot/public/locales/es/onboarding.json
  - leanshot/public/locales/es/kb.json
  - leanshot/public/locales/es/admin.json
  - leanshot/public/locales/es/clinic.json
  - leanshot/public/locales/es/settings.json
  - leanshot/.github/workflows/ci.yml
autonomous: true
gap_closure: false
requirements: [I18N-03, I18N-06, I18N-07, I18N-10]
# I18N-05 schema half (kb_articles.locale column + locale-respecting lookup helper)
# moved to Phase 37 (M6 Helpdesk Core) — natural owner. This plan ships the kb.json
# UI-chrome namespace + the .es.md content files (via 32-06); KB article DB binding
# lands when helpdesk does. Plan-checker iter-1 WARN-3 resolution.

must_haves:
  truths:
    - "Every visible JSX string in src/components/** is wrapped in t() or <Trans> with a stable dotted key"
    - "i18next-parser run produces 8 namespaces (common, nav, patient, onboarding, kb, admin, clinic, settings) in public/locales/en/"
    - "ES bootstrap files exist for all 8 namespaces (Plan 32-06 contractor refines to native ES)"
    - "eslint-plugin-i18next rule i18next/no-literal-string blocks future raw JSX text additions"
    - "scripts/check-locale-coverage.sh passes for all 8 namespaces (EN==ES key counts)"
    - "src/lib/helpers.ts date formatters accept an explicit locale arg; non-React callsites pass i18n.language explicitly OR delegate to useLocale() hook"
    - "useTranslation(['ns','common']) wiring uses 'common' as fallback namespace on every surface — no duplicated Save/Cancel keys across page namespaces"
  artifacts:
    - path: "leanshot/public/locales/en/patient.json"
      provides: "250-400 EN strings extracted from src/components/dashboard/** (tabs, cards, charts, greetings)"
    - path: "leanshot/public/locales/en/onboarding.json"
      provides: "150-200 EN strings from src/components/onboarding/** + src/components/marketing/**"
    - path: "leanshot/public/locales/en/kb.json"
      provides: "80-120 EN strings from helpdesk widget chrome + KB index UI"
    - path: "leanshot/public/locales/en/admin.json"
      provides: "200-300 EN strings from src/components/admin/**"
    - path: "leanshot/public/locales/en/clinic.json"
      provides: "150-250 EN strings from src/components/clinic/** + clinic-invite/** + clinic-settings/**"
    - path: "leanshot/public/locales/en/settings.json"
      provides: "60-100 EN strings from src/components/dashboard/settings/**"
    - path: "leanshot/eslint.config.js"
      provides: "Adds eslint-plugin-i18next with rule i18next/no-literal-string mode:'jsx-text-only'; ignore patterns for test files + src/lib/i18n/ + src/lib/analytics/events*.ts"
    - path: "leanshot/.github/workflows/ci.yml"
      provides: "Adds two CI steps: `npm run i18n:check` and `npx i18next-parser --fail-on-update` (D-05/D-06 100% coverage gate)"
  key_links:
    - from: "leanshot/src/components/**"
      to: "leanshot/public/locales/{en,es}/<ns>.json"
      via: "useTranslation(['ns','common']) + t('key') / <Trans i18nKey='ns:key' />"
      pattern: "useTranslation\\(\\["
    - from: "leanshot/.github/workflows/ci.yml"
      to: "leanshot/scripts/check-locale-coverage.sh"
      via: "run: bash scripts/check-locale-coverage.sh"
      pattern: "check-locale-coverage"
    - from: "leanshot/src/lib/helpers.ts"
      to: "leanshot/src/lib/i18n/useLocale.ts"
      via: "Callsites in React components migrate to useLocale() hook; non-React utilities (AI prompts, share cards) accept explicit locale arg"
      pattern: "useLocale|locale\\?: string"
---

<objective>
Extract every visible JSX string across `src/components/**` into `t()` / `<Trans>` calls using the 8-namespace split defined in 32-RESEARCH Open Item #1. Run `i18next-parser` to emit the canonical EN catalog. Bootstrap ES files as English placeholders (Plan 32-06 contractor refines). Migrate `src/lib/helpers.ts` date formatters to accept explicit locale args. Wire `eslint-plugin-i18next` to ban future raw JSX strings. Add the 100%-coverage CI gate.

Purpose: Plan 32-01 ships the runtime; this plan ships the SURFACE — every patient tab, onboarding step, marketing landing block, admin module, clinic operator surface, and settings drawer pane has Spanish-ready key references. After this plan, Plan 32-06's translator delivery is a pure content edit against the ES JSON files (zero source-code churn).

Output: 8 EN namespace catalogs (5-15 kB each) + 8 ES placeholder catalogs; 315-ish component files refactored to `useTranslation` calls; ESLint guard rule active; CI gate enforces parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-CONTEXT.md
@.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-RESEARCH.md
@.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-01-PLAN.md
@leanshot/CLAUDE.md
@leanshot/src/lib/helpers.ts
@leanshot/eslint.config.js
@leanshot/i18next-parser.config.js
@leanshot/scripts/check-locale-coverage.sh

# Project memory the executor MUST honor
@$HOME/.claude/projects/-Users-karstenhaldan/minisite/memory/feedback_planner_iter1_anti_patterns.md
@$HOME/.claude/projects/-Users-karstenhaldan/minisite/memory/feedback_chunked_planning_integration_seam_blindspot.md
@$HOME/.claude/projects/-Users-karstenhaldan/minisite/memory/reference_grep_gate_comment_strip.md

<interfaces>
react-i18next hook signature (consume in components):
```typescript
const { t, i18n } = useTranslation(['patient', 'common']);
t('patient:dose_card.title');              // string with namespace prefix
t('action.save');                           // implicit defaultNS=common fallback
t('patient:streak.day_remaining', { count }); // plural via _one/_other suffix
```

<Trans> for embedded JSX (per 32-RESEARCH lines 768-773):
```tsx
<Trans i18nKey="onboarding:terms_consent" components={{ termsLink: <a href="/terms" />, privacyLink: <a href="/privacy" /> }}>
  By continuing, you agree to our <termsLink>Terms</termsLink> and <privacyLink>Privacy Policy</privacyLink>.
</Trans>
```

Namespace ownership (per 32-RESEARCH Open Item #1 lines 364-373):
| Namespace | Owning component tree |
|-----------|------------------------|
| common | Buttons, modals, toasts, validation, error messages — globally fallback |
| nav | src/components/layout/** (Sidebar, Topbar, MobileNav, FAB) |
| patient | src/components/dashboard/{tabs,cards,charts,modals,share,tour}/** |
| onboarding | src/components/onboarding/** + src/components/marketing/** + src/components/landing/** |
| kb | src/components/dashboard/help/** + helpdesk-widget chunks (helpdesk added later in v1.3 — files may not exist yet; namespace remains empty placeholder) |
| admin | src/components/admin/** |
| clinic | src/components/clinic/** + src/components/clinic-invite/** + src/components/clinic-settings/** |
| settings | src/components/dashboard/settings/** |

Existing helpers.ts date formatter shape (verify exact signatures in leanshot/src/lib/helpers.ts):
```typescript
export function formatShort(date: Date | string): string;   // → toLocaleDateString(undefined, {month:'short', day:'numeric'})
export function formatLong(date: Date | string): string;    // → toLocaleDateString(undefined, {month:'short', day:'numeric', year:'numeric'})
```

After migration:
```typescript
// helpers.ts — non-React callers pass locale explicitly
export function formatShort(date: Date | string, locale: string = 'en'): string;
export function formatLong(date: Date | string, locale: string = 'en'): string;
// React components prefer useLocale() hook
const { dateShort, dateLong } = useLocale();
dateShort.format(new Date(iso));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ESLint guard + extract all strings with i18next-parser sweep</name>
  <files>
    leanshot/eslint.config.js,
    leanshot/src/**/*.tsx,
    leanshot/public/locales/en/common.json,
    leanshot/public/locales/en/nav.json,
    leanshot/public/locales/en/patient.json,
    leanshot/public/locales/en/onboarding.json,
    leanshot/public/locales/en/kb.json,
    leanshot/public/locales/en/admin.json,
    leanshot/public/locales/en/clinic.json,
    leanshot/public/locales/en/settings.json
  </files>
  <action>
    Step 1 — extend `leanshot/eslint.config.js` (flat config) to register `eslint-plugin-i18next`. Use rule `i18next/no-literal-string` with `mode: 'jsx-text-only'` (per 32-RESEARCH Pitfall 2 — avoids false positives in `className`/`aria-*`/`data-*`). Ignore patterns:
    - `src/lib/i18n/**` (the runtime itself is allowed to embed key literals)
    - `src/lib/analytics/events*.ts` (event-name strings are not user-facing copy)
    - `**/*.test.{ts,tsx}` + `**/__tests__/**`
    - `src/illustrations/**` (inline SVG strings)
    - `src/lib/constants.ts` if it contains internal enum-like string literals (verify before excluding)

    Add a rule comment block citing CONTEXT D-06 + Pitfall 2 of RESEARCH.

    Step 2 — manual + parser-assisted extraction sweep. Order of operations (deterministic per `feedback_planner_iter1_anti_patterns.md` "no hedge instructions"):

    a. For each namespace owner directory (table in <interfaces>), open every `.tsx` file and replace:
       - JSX text nodes (`<button>Save</button>`) → `<button>{t('action.save')}</button>`
       - String props that ARE user-visible copy (`aria-label="Close menu"`, `title="Streak"`, `placeholder="Enter email"`) → `t('...')` calls
       - Toast / Modal / Alert strings — these live in `common` namespace as the FALLBACK; per-surface namespace if they're surface-specific.

    b. For embedded markup (a/strong/links inside sentences), use `<Trans i18nKey="ns:key" components={{...}}>...</Trans>` per Pattern in <interfaces>.

    c. For plural strings, use suffixed keys `_one` / `_other` / `_zero` per 32-RESEARCH Open Item #4. Call site: `t('patient:streak.day_remaining', { count: days })`.

    d. For interpolated values, prefer named placeholders `t('patient:greeting.hello_name', { name })` over positional.

    e. Key naming convention (lock in this plan; document in 32-06 TRANSLATOR-WORKFLOW.md):
       - `<namespace>:<surface>.<element>.<action>` e.g. `patient:hero.cta.log_dose`, `clinic:roster.empty_state.title`, `settings:profile.unit.kg`
       - `action.*` and `error.*` live in `common` (cross-cutting)
       - Brand names (Ozempic, Wegovy, Mounjaro, Zepbound, LeanShot) stay HARDCODED (per D-11) — add an ESLint-disable-next-line comment on those lines with `// brand name — see D-11`

    f. Per 32-RESEARCH Pitfall 6: every NEW t() call must include a `defaultValue` arg so the parser emits the EN string AND the ES placeholder. Pattern: `t('patient:dose_card.title', 'Dose')`. The default propagates into BOTH `en/<ns>.json` and `es/<ns>.json` so the coverage gate stays green even before contractor delivery.

    Step 3 — run `npx i18next-parser` from `leanshot/` directory. This:
    - Walks `src/**/*.{ts,tsx}` (excluding tests per config)
    - Emits 8 files under `public/locales/en/<ns>.json`
    - Emits 8 mirror files under `public/locales/es/<ns>.json` (defaults to EN copy because `defaultValue` is provided per Step 2f)

    Step 4 — diff-review the emitted catalogs. Expected key-count ranges per Open Item #1:
    - common: 50-80
    - nav: 30-40
    - patient: 250-400
    - onboarding: 150-200
    - kb: 80-120 (may be lower if helpdesk widget hasn't shipped yet — that's fine; record actual)
    - admin: 200-300
    - clinic: 150-250
    - settings: 60-100

    If a namespace blows past 5 kB gz on its own, RECORD it in SUMMARY but do NOT sub-split in this plan — sub-split is a Plan 32-07 ship-gate optimization if needed.

    Step 5 — verify the parser-emitted EN files include the bootstrap keys created in Plan 32-01 Task 1 Step 13 (greetings, action verbs, etc.). If the parser overwrites them with extracted-from-source keys, MERGE — never delete a key that an existing component actually uses. (Parser does NOT remove keys by default with `createOldCatalogs: false`; verify the run preserves bootstrap-only keys via the `keepRemoved: true` setting if needed.)

    Per `reference_grep_gate_comment_strip.md`: any verification step that uses `grep -c` against a JSON file must filter via `jq` first — bare grep counts comments. We use jq exclusively for JSON validation.

    Critical invariant: after this task, `npm run lint` must NOT regress (current baseline 84 errors per `project_lint_debt_import_x_order.md`). The new `i18next/no-literal-string` rule may add NEW errors for any string not yet wrapped — those errors MUST be at zero before commit.
  </action>
  <verify>
    <automated>cd leanshot &amp;&amp; npx tsc -b &amp;&amp; npm run lint 2>&amp;1 | tee /tmp/lint.txt | tail -10 &amp;&amp; LINT_ERR=$(grep -cE "^\s*[0-9]+:[0-9]+\s+error" /tmp/lint.txt || true) &amp;&amp; echo "Current lint errors: $LINT_ERR (baseline 84)" &amp;&amp; test "$LINT_ERR" -le 84 &amp;&amp; npx i18next-parser --fail-on-update 2>&amp;1 | tail -20 &amp;&amp; bash scripts/check-locale-coverage.sh 2>&amp;1 | tee /tmp/coverage.txt &amp;&amp; ! grep -qE "FAIL" /tmp/coverage.txt</automated>
  </verify>
  <done>
    `i18next/no-literal-string` rule is wired into eslint.config.js with documented ignore patterns; all 8 EN namespaces exist with parser-emitted catalogs; all 8 ES namespaces exist as defaultValue mirrors; coverage gate passes; lint count ≤ 84 (no regression); no `i18next/no-literal-string` errors anywhere in src/components/**.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate src/lib/helpers.ts date formatters + helpers.test.ts + greeting i18n</name>
  <files>
    leanshot/src/lib/helpers.ts,
    leanshot/src/lib/helpers.test.ts,
    leanshot/src/components/dashboard/**/*.tsx,
    leanshot/src/components/dashboard/share/**/*.ts
  </files>
  <behavior>
    - helpers.test.ts: existing tests for `formatShort`, `formatLong`, `relTime` continue to pass with the new optional `locale` parameter (default `'en'` preserves prior behavior).
    - NEW test: `formatShort(date, 'es')` produces a Spanish month abbreviation (`ene` for January, etc.).
    - NEW test: greeting helper returns `'morning'|'afternoon'|'evening'` STRING (not the formatted greeting); the i18n key lookup happens in the component.
    - All existing dashboard components using `formatShort(date)` still render correctly (default arg preserves behavior; React components migrate to `useLocale()` over the same task).
  </behavior>
  <action>
    Step 1 — modify `leanshot/src/lib/helpers.ts`:
    - `formatShort(date, locale: string = 'en')` — pass `locale` to `Intl.DateTimeFormat(locale, {...}).format(date)`. Replace any `.toLocaleDateString(undefined, ...)` calls.
    - `formatLong(date, locale: string = 'en')` — same treatment.
    - `relTime(now, then, locale: string = 'en')` — use `new Intl.RelativeTimeFormat(locale, {numeric: 'auto'})`.
    - Split out `greeting(now: Date): 'morning' | 'afternoon' | 'evening'` from the existing combined helper. The string-returning version becomes `pickGreetingSlot(now)` (per 32-RESEARCH Open Item #3 line 460). Add a deprecation comment on any prior `greeting()` that returned a translated string — components should call `t(\`common:greeting.${pickGreetingSlot(now)}\`)`.

    Step 2 — update `helpers.test.ts` to cover the new signatures:
    - Existing assertions pass with default `'en'` arg.
    - NEW: `formatShort(new Date('2026-01-15'), 'es')` returns a string containing `'ene'` (Spanish January abbrev).
    - NEW: `pickGreetingSlot(date_at_9am)` returns `'morning'`.

    Step 3 — codebase scout shows 38 files use `toLocaleDateString` / `toLocaleString` / `new Intl.*` (per 32-RESEARCH line 433). For each file in `src/components/dashboard/**`:
    - If inside a React component: replace direct `.toLocaleDateString(...)` with the appropriate `useLocale()` formatter from Plan 32-01 (`const { dateShort } = useLocale();` → `dateShort.format(date)`).
    - If inside a pure utility (no React import): pass `i18n.language` explicitly OR keep the explicit `'en'` default (call site decides; usually translatable utilities like share cards belong to the React tree and should use the hook).

    Step 4 — for share-card / PDF / AI prompt generation utilities (`src/components/dashboard/share/template-*.ts`, `src/lib/ai.ts`, `src/lib/insights.ts`):
    - These are NOT React; they accept a `locale` parameter from the caller. Default `'en'` for backward compat.
    - AI prompt: include locale in the prompt context only if the caller wants Spanish coach responses — defer the prompt-localization decision to Plan 32-06 (translator may want to write the system prompt in ES). For NOW, accept the locale param but don't change prompt language (TODO comment).

    Step 5 — sweep `src/components/dashboard/layout/GreetingStrip.tsx` (per existing architecture): replace any hardcoded `'Good morning'` etc. with `t(\`common:greeting.${pickGreetingSlot(now)}\`)`. Verify the keys exist in `common.json` (Plan 32-01 Task 1 Step 13 added them).

    No NEW lint rule added in this task (the JSX-text-only rule from Task 1 already covers the JSX surface; `toLocaleDateString` is a method call, not a literal). A follow-up custom rule banning `new Intl.*` outside `src/lib/i18n/` is documented in 32-RESEARCH Open Item #3 as defer-to-v1.4 — DO NOT add it here.

    Per `feedback_planner_iter1_anti_patterns.md`: no "v1 simplified" hedge — every callsite migrates in this task, no "TODO migrate later" deferrals (except the AI-prompt locale gating documented in Step 4, which is an explicit scope boundary).
  </action>
  <verify>
    <automated>cd leanshot &amp;&amp; npx tsc -b &amp;&amp; npm run test:unit -- src/lib/helpers.test.ts 2>&amp;1 | tail -10 &amp;&amp; npm run test:unit -- src/components/dashboard/ 2>&amp;1 | tail -20 &amp;&amp; ! grep -rn "toLocaleDateString(undefined" leanshot/src/ 2>/dev/null | grep -v "node_modules\|\.test\."</automated>
  </verify>
  <done>
    helpers.ts date formatters accept optional locale; helpers.test.ts green (existing + new ES + greeting); all dashboard components use `useLocale()` for date/number formatting OR pass explicit locale via helpers; zero `toLocaleDateString(undefined,` callsites remain in production source.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire CI workflow for coverage gate + parser drift check</name>
  <files>
    leanshot/.github/workflows/ci.yml
  </files>
  <action>
    Step 1 — open `leanshot/.github/workflows/ci.yml`. Identify the existing lint/test/build job (typically named `quality` or `ci`).

    Step 2 — add two steps to that job, AFTER `npm install` and BEFORE `npm run build` (so a coverage miss fails fast). Use this YAML pattern (adapt step ordering to match the existing file's style):
    ```yaml
    - name: i18n coverage gate (D-05 / D-06 — 100% per namespace)
      run: bash scripts/check-locale-coverage.sh
      working-directory: leanshot

    - name: i18n parser drift check (D-06 — fail on uncommitted catalog updates)
      run: |
        npx i18next-parser --fail-on-update
        # Sanity: parser run should not have produced uncommitted changes to public/locales.
        # If git status shows any dirty file under public/locales/, the developer forgot to
        # run `npm run i18n:extract` after adding new t() calls.
        if ! git diff --quiet -- public/locales; then
          echo "::error::i18n catalog drift detected — run 'npm run i18n:extract' locally and commit the changes." >&2
          git diff -- public/locales | head -50
          exit 1
        fi
      working-directory: leanshot
    ```

    Step 3 — confirm the workflow's `checkout` step uses `fetch-depth: 0` or similar — needed for `git diff` to work. If not, add `fetch-depth: 1` is sufficient since we only compare workdir to HEAD.

    Step 4 — do NOT modify any other CI step. The bundle-budget assertion (Phase 24) already runs and will catch any `i18n-runtime` overrun introduced by this plan.

    Per `feedback_chunked_planning_integration_seam_blindspot.md`: this CI wiring IS the integration seam between Plan 32-02 (catalog producer) and Plan 32-06 (translator delivery consumer). Without this CI step, a future PR could land partial ES translations and the gate would only fire locally.
  </action>
  <verify>
    <automated>cd leanshot &amp;&amp; test -f .github/workflows/ci.yml &amp;&amp; grep -q "check-locale-coverage" .github/workflows/ci.yml &amp;&amp; grep -q "i18next-parser --fail-on-update" .github/workflows/ci.yml &amp;&amp; bash scripts/check-locale-coverage.sh &amp;&amp; npx i18next-parser --fail-on-update 2>&amp;1 | tail -5 &amp;&amp; git diff --quiet -- public/locales</automated>
  </verify>
  <done>
    ci.yml has both new steps in the correct job; running `bash scripts/check-locale-coverage.sh` + `npx i18next-parser --fail-on-update` locally exits 0; `git diff --quiet -- public/locales` is clean (no untracked drift).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer source → public/locales catalogs | Parser is the gate. ESLint rule blocks raw JSX strings. CI script blocks drift. |
| public/locales catalogs → client runtime | Same-origin static fetch. Catalog content is git-audited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-01 | Tampering | translator GitHub PR access to `public/locales/es/*.json` | mitigate | Per CONTEXT I18N-06, contractor PRs require team review before merge. ESLint rule + CI coverage gate catch structural drift. JSON-only PRs cannot inject executable code. |
| T-32-02-02 | Information Disclosure | extracted EN catalogs contain internal-only copy | accept | All extracted strings are already shipped to clients as JSX text. JSON catalog is a re-encoding, not new exposure. |
| T-32-02-03 | Denial of Service | massive parser run blocks CI | accept | i18next-parser is fast on the 315-component surface (~5s); the `--fail-on-update` flag exits early on drift. |
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npm run lint` count ≤ 84 (no regression vs `project_lint_debt_import_x_order.md` baseline)
- Zero `i18next/no-literal-string` errors in `src/components/**`
- `bash scripts/check-locale-coverage.sh` exits 0 for all 8 namespaces
- `npx i18next-parser --fail-on-update` exits 0 (no drift)
- `git diff --quiet -- public/locales` after running the parser
- `npm run test:unit` passes (helpers + components)
- `npm run build` succeeds; bundle ceilings preserved
- `npm run test:e2e -- e2e/i18n-language-switch.spec.ts` passes (catalogs now have real content)
</verification>

<success_criteria>
1. 8 EN namespace catalogs emitted by i18next-parser, each within the key-count ranges in Task 1 Step 4.
2. 8 ES placeholder catalogs exist with matching keys (defaultValue-populated).
3. Every JSX visible-text string in `src/components/**` is wrapped in `t()` / `<Trans>` (lint rule proves this).
4. `src/lib/helpers.ts` date formatters accept explicit locale; React callsites use `useLocale()`; non-React callsites pass `'en'` default or explicit param.
5. CI workflow enforces both the coverage gate and parser drift check — blocking merge on any drift.
6. Greeting strip and other locale-aware UI surfaces render Spanish when `?lang=es` is set (e2e from Plan 32-01 still passes; now exercises real strings).
</success_criteria>

<output>
After completion, create `.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-02-SUMMARY.md` documenting:
- Per-namespace EN key counts (actual vs expected range)
- Per-namespace ES key counts (must equal EN)
- Per-namespace EN catalog gz size (catalog-only; chunk ceiling unchanged)
- Number of `.tsx` files modified
- Number of `t()` / `<Trans>` callsites added (grep count)
- Number of `toLocaleDateString(undefined,` callsites removed
- CI workflow additions (job + step names)
- Open items for Plan 32-06: namespaces where bootstrap defaultValue is EN-text (contractor list)
</output>