---
phase: 32-spanish-i18n-parallel-with-clinic-track
plan: 06
type: execute
wave: 3
depends_on: [32-02, 32-04, 32-05]
files_modified:
  - docs/i18n/TRANSLATOR-WORKFLOW.md
  - docs/i18n/clinical-glossary-es.md
  - leanshot/public/locales/es/common.json
  - leanshot/public/locales/es/nav.json
  - leanshot/public/locales/es/patient.json
  - leanshot/public/locales/es/onboarding.json
  - leanshot/public/locales/es/kb.json
  - leanshot/public/locales/es/admin.json
  - leanshot/public/locales/es/clinic.json
  - leanshot/public/locales/es/settings.json
  - supabase/functions/_shared/locales/es/emails.json
  - leanshot/public/kb-articles/**/*.es.md  # KB content files (paths depend on existing kb-articles dir layout)
autonomous: false
gap_closure: false
requirements: [I18N-04, I18N-05, I18N-06, I18N-09]

must_haves:
  truths:
    - "Bilingual clinical contractor (D-02) engaged; engagement gate captured in commit message + glossary header"
    - "docs/i18n/TRANSLATOR-WORKFLOW.md documents the in-house GitHub PR pipeline (I18N-06)"
    - "docs/i18n/clinical-glossary-es.md exists with brand-name policy (D-11) + Reviewed: date + reviewer name header (I18N-09)"
    - "All 8 ES UI namespaces (common, nav, patient, onboarding, kb, admin, clinic, settings) contain native ES strings (not EN defaultValue placeholders)"
    - "ES emails.json refined to native ES copy (replaces Plan 32-05 bootstrap)"
    - "KB articles for I18N-05 in-scope set ship as {slug}.es.md alongside existing EN files"
    - "scripts/check-locale-coverage.sh continues to pass (key parity unchanged; only values replaced)"
    - "ICU plural fixture from Plan 32-01 still green after contractor pass"
  artifacts:
    - path: "docs/i18n/TRANSLATOR-WORKFLOW.md"
      provides: "End-to-end translator pipeline doc — GitHub PR conventions, key naming policy, defaultValue rule, glossary lookup, locale_overrides emergency-fix lever, review gate"
    - path: "docs/i18n/clinical-glossary-es.md"
      provides: "Versioned glossary of GLP-1 drug brand names (untranslated), generic names (translated semaglutide→semaglutida etc.), dosing terms, symptom names; YAML or markdown table per term; Reviewed: header with date + reviewer signoff (I18N-09)"
      contains: "Reviewed:"
    - path: "leanshot/public/locales/es/*.json"
      provides: "Native Spanish translations for all 8 UI namespaces (contractor edits)"
    - path: "supabase/functions/_shared/locales/es/emails.json"
      provides: "Native Spanish translations for all 7 email templates"
    - path: "leanshot/public/kb-articles/**/*.es.md"
      provides: "ES KB article files for the v1.3 in-scope set (count TBD at Plan kickoff per RESEARCH Open Question #1)"
  key_links:
    - from: "docs/i18n/TRANSLATOR-WORKFLOW.md"
      to: "leanshot/public/locales/es/*.json"
      via: "Documents the PR-edit pattern + defaultValue rule + glossary cross-ref"
      pattern: "public/locales"
    - from: "docs/i18n/clinical-glossary-es.md"
      to: "leanshot/public/locales/es/patient.json"
      via: "Translator references glossary for any patient namespace string mentioning drug names / dosing / symptoms"
      pattern: "Brand names|generic|semaglutida"
---

<objective>
Deliver the contractor-translated Spanish corpus and lock in the I18N-06 process documentation + I18N-09 clinical advisor signoff. This plan is a HUMAN-IN-THE-LOOP delivery checkpoint — the bilingual clinical contractor (per CONTEXT D-02) edits the 8 ES UI namespaces + 1 ES emails.json + N KB article files via GitHub PRs over the engagement window; this plan defines the gates and accepts/rejects the deliverable.

Purpose: Plans 32-01..32-05 ship the runtime + admin surface + email pipeline with BOOTSTRAP / DEFAULTVALUE Spanish strings. Plan 32-06 replaces every bootstrap value with native, clinically-reviewed ES. After this plan, the only ES content drift comes from new EN strings landing in later phases (caught by the coverage CI gate from Plan 32-02 Task 3).

Output: Native ES corpus across all 9 catalogs (8 UI + 1 email); clinical glossary committed with reviewer signoff; KB ES article set committed; translator workflow doc shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-CONTEXT.md
@.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-RESEARCH.md
@.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-01-PLAN.md
@.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-02-PLAN.md
@.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-05-PLAN.md
@leanshot/CLAUDE.md

# Project memory the executor MUST honor
@$HOME/.claude/projects/-Users-karstenhaldan-minisite/memory/feedback_planner_iter1_anti_patterns.md
@$HOME/.claude/projects/-Users-karstenhaldan-minisite/memory/feedback_cli_over_paste_back.md

<interfaces>
ES namespace deliverable inventory (from Plan 32-02 Task 1 Step 4 measured ranges):
- common.json: 50-80 keys
- nav.json: 30-40 keys
- patient.json: 250-400 keys
- onboarding.json: 150-200 keys
- kb.json: 80-120 keys (lower if helpdesk surface not yet shipped)
- admin.json: 200-300 keys (+ ~10 new keys from Plan 32-04 admin module)
- clinic.json: 150-250 keys
- settings.json: 60-100 keys

Email deliverable (from Plan 32-05 Task 1):
- supabase/functions/_shared/locales/es/emails.json: ~18 keys (11 subjects + 7 bodies — final inventory locked by Plan 32-05)

Glossary structure (markdown table — example):
```
| EN term | ES term | Policy | Notes |
|---------|---------|--------|-------|
| Ozempic | Ozempic | DO NOT TRANSLATE (D-11 brand) | Novo Nordisk trademark |
| Wegovy | Wegovy | DO NOT TRANSLATE (D-11 brand) | Novo Nordisk trademark |
| Mounjaro | Mounjaro | DO NOT TRANSLATE (D-11 brand) | Eli Lilly trademark |
| Zepbound | Zepbound | DO NOT TRANSLATE (D-11 brand) | Eli Lilly trademark |
| semaglutide | semaglutida | TRANSLATE generic | |
| tirzepatide | tirzepatida | TRANSLATE generic | |
| injection | inyección | TRANSLATE | feminine |
| dose | dosis | TRANSLATE | feminine; same singular + plural |
| titration | titulación / ajuste de dosis | CONTEXT — prefer ajuste de dosis in patient UI | |
| nausea | náuseas | TRANSLATE | plural by convention in es |
| ...   | ...      | ...    | ...   |

Reviewed: 2026-MM-DD by <Contractor Name>, RN/MD/PharmD <license #>
```

KB article in-scope set: TBD at plan kickoff. Per RESEARCH Open Question #1: planner asks user "How many KB articles are in scope for ES translation in v1.3? (Best-effort count from kb_articles WHERE published = true)". Capture answer in CHECKPOINT 1 below. Article files live at the location the existing EN KB articles live; planner verifies the path at kickoff (search `git ls-files | grep -i "kb-articles\|knowledge-base"`).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 1: Engage bilingual clinical contractor + scope KB article count</name>
  <files>(none — human checkpoint)</files>
  <action>Halt automated execution; surface the user-facing checkpoint content below; wait for the resume-signal before proceeding.</action>
  <verify><automated>true  # Checkpoint completion is human-signaled; no automated probe.</automated></verify>
  <done>User replies per &lt;resume-signal&gt; below; orchestrator records the response in the plan SUMMARY before resuming.</done>
  <what-built>
    Plans 32-01..32-05 are shipped. Catalogs exist with EN canonical + ES defaultValue (placeholder English) text in all 8 UI namespaces + 1 email namespace. The admin shell module + Edge Fn rendering + coverage CI gate are all live.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-02-SUMMARY.md` and `32-05-SUMMARY.md`. Confirm contractor-deliverable inventory: 8 UI namespaces + 1 email namespace + KB article count.
    2. Engage the contractor per CONTEXT D-02 (single bilingual clinical contractor handles UI + 7 emails + KB + clinical glossary in one engagement, ~$3-5k for v1 corpus). Provide them:
       - GitHub PR access to a fork of this repo (or write access if signed NDA + DPA already in place).
       - The 8 UI namespace JSON paths + the 1 email JSON path.
       - The clinical glossary template (Task 2 ships the empty template).
       - The KB EN articles list (run `find leanshot/public/kb-articles -name "*.md" -not -name "*.es.md" 2>/dev/null | wc -l` OR query `select count(*) from kb_articles where published = true and locale='en'` and copy the slugs).
       - Locked policies: D-10 (single 'es' es-419 LatAm neutral), D-11 (brand names untranslated; generics translated), D-12 (kg default for new ES users — relevant if any email body mentions weight units).
    3. Decide KB scope. Per RESEARCH Open Question #1: how many KB articles to translate in v1.3?
       - Option A: ALL published EN articles (highest coverage; highest cost).
       - Option B: Top-N most-viewed (data-driven; needs PostHog query — see `mcp__posthog` if MCP available).
       - Option C: Curated list (operator-picked; lowest scope; fastest delivery).
    4. Provide contractor a target deliverable date and the in-house git-PR pipeline doc (Task 2 ships TRANSLATOR-WORKFLOW.md first so contractor has a process reference).
  </how-to-verify>
  <resume-signal>
    Reply with: (a) contractor name + credentials (for glossary signoff header), (b) chosen KB scope option (A/B/C) and article count, (c) estimated delivery window.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: TRANSLATOR-WORKFLOW.md doc + empty clinical-glossary-es.md template</name>
  <files>
    docs/i18n/TRANSLATOR-WORKFLOW.md,
    docs/i18n/clinical-glossary-es.md
  </files>
  <action>
    Step 1 — create `docs/i18n/TRANSLATOR-WORKFLOW.md` documenting the in-house pipeline (per CONTEXT D-01 + RESEARCH Pitfall 6). Sections:
    - **Overview** — single bilingual clinical contractor; git PR pipeline; no Crowdin/Lokalise.
    - **Repository layout** — paths to UI catalogs (`leanshot/public/locales/{lng}/{ns}.json`), email catalog (`supabase/functions/_shared/locales/{lng}/emails.json`), KB articles (path verified at Checkpoint 1).
    - **Key naming convention** (from Plan 32-02 Task 1 Step 2e): `<namespace>:<surface>.<element>.<action>`.
    - **defaultValue rule** (Pitfall 6): every `t()` call in source includes `t('key', 'EN default')` so the parser emits the EN catalog AND the ES placeholder. Translators OVERWRITE the ES placeholder; never add new ES keys without a corresponding EN key (the coverage CI gate blocks).
    - **Glossary cross-reference rule** — when a string contains a glossary term, the translator MUST honor the policy column (DO NOT TRANSLATE for brand names per D-11; use the canonical ES term for generics).
    - **PR review gate** — every translator PR requires team review by an engineer; CI gate (coverage + i18next-parser drift) must be green.
    - **Emergency hot-patch** — for mistranslations discovered post-deploy, admin uses `Locale Overrides` admin shell module (Plan 32-04) WITHOUT waiting for the next translator PR cycle.
    - **Signed commits / contractor access policy** — per RESEARCH Security Domain (line 895): contractor never gets `main` push access; PRs from a fork (or feature branch on the main repo if NDA + DPA signed).
    - **What NOT to translate**: 
      - Anything in `src/lib/analytics/events.ts` (event names are not user-facing copy)
      - Anything in `src/illustrations/` (decorative SVG)
      - Brand names per glossary D-11

    Step 2 — create `docs/i18n/clinical-glossary-es.md` per RESEARCH Open Item #1 + CONTEXT D-03 + D-11. Structure:
    - Header: `# LeanShot Clinical Glossary (Spanish)\n\n**Reviewed:** TBD by TBD\n**Last updated:** YYYY-MM-DD\n**Scope:** v1.3 corpus\n`
    - Sections:
      - **Brand names (DO NOT TRANSLATE)** — Ozempic, Wegovy, Mounjaro, Zepbound, Saxenda, Trulicity, Rybelsus (verify with contractor)
      - **Generic names (translate)** — semaglutide→semaglutida, tirzepatide→tirzepatida, liraglutide→liraglutida, etc.
      - **Dosing terms** — mg, mcg, IU stay as-is; doses translate (`once weekly → una vez por semana`)
      - **Symptoms** — nausea→náuseas, fatigue→fatiga, dizziness→mareos, etc.
      - **Body metrics** — weight→peso, BMI→IMC (índice de masa corporal), waist circumference→circunferencia de cintura
      - **Treatment terminology** — injection→inyección, injection site→sitio de inyección, titration→ajuste de dosis (or titulación in clinical), maintenance dose→dosis de mantenimiento
    - One blank table row per section labeled `<add more terms here as needed>` for contractor expansion.
    - Bottom: `## Review Log\n\n| Date | Reviewer | Scope | Notes |\n|------|----------|-------|-------|\n`

    The contractor (per Checkpoint 1) fills in:
    - Their name + license # in the Reviewed: header.
    - Additional terms encountered during translation.
    - The Review Log entry on completion.

    Per `feedback_planner_iter1_anti_patterns.md`: no hedge text like "v1 minimal glossary, expand later" — the document IS the glossary; contractor expands it as part of the engagement.
  </action>
  <verify>
    <automated>test -f docs/i18n/TRANSLATOR-WORKFLOW.md &amp;&amp; test -f docs/i18n/clinical-glossary-es.md &amp;&amp; grep -q "Reviewed:" docs/i18n/clinical-glossary-es.md &amp;&amp; grep -q "DO NOT TRANSLATE" docs/i18n/clinical-glossary-es.md &amp;&amp; grep -q "semaglutid" docs/i18n/clinical-glossary-es.md &amp;&amp; grep -q "defaultValue" docs/i18n/TRANSLATOR-WORKFLOW.md &amp;&amp; grep -q "locale_overrides" docs/i18n/TRANSLATOR-WORKFLOW.md</automated>
  </verify>
  <done>
    Both docs exist; glossary has the brand-name section + generic-name section + Reviewed: header (TBD until contractor signoff); workflow doc covers all 8 sections.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: Accept contractor PRs (UI + emails + KB + glossary signoff)</name>
  <files>(none — human checkpoint)</files>
  <action>Halt automated execution; surface the user-facing checkpoint content below; wait for the resume-signal before proceeding.</action>
  <verify><automated>true  # Checkpoint completion is human-signaled; no automated probe.</automated></verify>
  <done>User replies per &lt;resume-signal&gt; below; orchestrator records the response in the plan SUMMARY before resuming.</done>
  <what-built>
    Contractor (engaged at Checkpoint 1) submits one or more GitHub PRs covering:
    - All 8 ES UI namespace JSON files (native ES, replacing EN defaultValue placeholders)
    - The 1 ES emails.json catalog
    - N ES KB article files at the agreed-upon path
    - Glossary file updated with `Reviewed: <date> by <name + credentials>` header + contractor-added terms + Review Log entry
  </what-built>
  <how-to-verify>
    For each contractor PR:
    1. CI must be green:
       - `npm run lint` (no regression vs 84 baseline)
       - `bash leanshot/scripts/check-locale-coverage.sh` (PASS for all 8 namespaces; key parity unchanged)
       - `npx i18next-parser --fail-on-update` (no drift)
       - `npm run build` (bundle ceilings preserved; ES JSON catalog size has no chunk-ceiling impact since catalogs are served as static files)
       - `npm run test:e2e -- e2e/i18n-language-switch.spec.ts` (now exercises real translated strings)
    2. Spot-check 10 random strings per namespace for clinical accuracy:
       - Brand names stay English (search for `Ozempic`/`Wegovy`/`Mounjaro`/`Zepbound` in es/patient.json — must appear as-is, not transliterated).
       - Generic names translated (`semaglutida` not `semaglutide`).
       - No `[MISSING:` markers leaked into ES JSON.
       - No English fallthrough (`Welcome to` should not appear in es/onboarding.json).
    3. Glossary file header:
       - `Reviewed: <date>` populated with a real date.
       - Reviewer name + credentials present (e.g., "Dr. Maria Lopez, MD, License #12345").
       - At least 30 terms (the bootstrap template has ~20; contractor should expand).
    4. KB articles: each `{slug}.es.md` file exists at the agreed path; spot-read 3 for clinical accuracy + glossary consistency.
    5. Manual UAT in browser:
       - Load `/?lang=es` → onboarding flow + patient dashboard renders native ES (not bootstrap placeholders).
       - Send a test welcome email to a profile with `locale='es'` via the lifecycle-welcome-series Edge Fn → subject + body are native ES.
       - Open admin shell → Locale Overrides module → confirm the UI itself is in ES when locale='es'.
    6. PostHog `i18n_missing_key` dashboard check (after the contractor PR has been live for 24h, OR via synthetic key-coverage browse-walk):
       - Top missing keys count should be near zero. If non-zero, those keys reveal Pitfall 3 dynamic-key callsites (`t(\`error.${code}\`)`); planner adds defaultValue comments per Pitfall 3 in a follow-up commit (NOT a new plan).
  </how-to-verify>
  <resume-signal>
    Reply with: (a) "accepted" — all PRs merged, CI green; OR (b) "deferred" — N keys + path list of issues for contractor revision; OR (c) "rejected" — escalation to user for re-engagement.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-merge SUMMARY + STATE update + close I18N-06/I18N-09 in ROADMAP</name>
  <files>
    .planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-06-SUMMARY.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <action>
    Step 1 — write `.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-06-SUMMARY.md` documenting:
    - Contractor name + credentials
    - PR(s) merged with SHA refs
    - Per-namespace ES delivery confirmation (key count + sample-check verdict)
    - Glossary Reviewed: date + term count
    - KB article count delivered (vs Checkpoint 1 scope target)
    - Manual UAT screenshots / curl payloads (welcome email ES sample)
    - Open items (e.g., if any keys were Pitfall 3 dynamic-key callsites needing follow-up)
    - Phase 25 (HIPAA SES split) inheritance note: contractor's emails.json work auto-applies when SES Edge Fns spin up

    Step 2 — update `.planning/STATE.md`:
    - Move I18N-04, I18N-05, I18N-06, I18N-09 from "Pending" to delivery in-progress / completion state.
    - Add contractor engagement record (date, scope, cost estimate) under a "Vendor Engagements" section.

    Step 3 — update `.planning/REQUIREMENTS.md` (the per-REQ table at the bottom):
    - I18N-04 → Complete
    - I18N-05 → Complete (scope per Checkpoint 1)
    - I18N-06 → Complete
    - I18N-09 → Complete

    Per `feedback_cli_over_paste_back.md`: fetch contractor PR SHAs via `gh pr list --state merged --label i18n --json number,title,mergeCommit` — do NOT ask the user to paste SHAs back.

    Do NOT mark I18N-01..03, I18N-07, I18N-08, I18N-10 as Complete in this plan — those are owned by Plans 32-01, 32-02, 32-03, 32-04, and 32-07 respectively.
  </action>
  <verify>
    <automated>test -f .planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-06-SUMMARY.md &amp;&amp; grep -q "Reviewed:" docs/i18n/clinical-glossary-es.md &amp;&amp; ! grep -q "Reviewed: TBD" docs/i18n/clinical-glossary-es.md &amp;&amp; bash leanshot/scripts/check-locale-coverage.sh 2>&amp;1 | tail -10 &amp;&amp; cd leanshot &amp;&amp; npm run build 2>&amp;1 | tail -5 &amp;&amp; grep -E "I18N-04|I18N-05|I18N-06|I18N-09" ../.planning/REQUIREMENTS.md | grep -v "Pending"</automated>
  </verify>
  <done>
    SUMMARY.md exists with contractor PR refs + delivery confirmation; STATE.md updated; I18N-04/05/06/09 marked Complete in REQUIREMENTS.md; glossary Reviewed: date is real (not TBD); coverage CI gate stays green; bundle build succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contractor GitHub fork → main repo PR | Team review + signed-NDA + DPA per RESEARCH Security Domain |
| contractor authors clinical glossary | Reviewed: header captures accountability for clinical accuracy (I18N-09 advisor gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06-01 | Tampering | contractor adds malicious key with unsanitized HTML in ES catalog | mitigate | i18next interpolation.escapeValue:false for UI is React-protected (JSX escapes). Email side escapes via consumer-controlled HTML template construction. CI lint catches gross issues. PR review is the human gate. |
| T-32-06-02 | Information Disclosure | clinical glossary leaks PHI patterns | accept | Glossary is generic clinical terms (drug names, symptoms, dosing); not patient-specific data. Public-facing docs. |
| T-32-06-03 | Repudiation | translator denies authoring a problematic translation | mitigate | Signed commits on contractor's PRs; git blame is the audit trail. Reviewed: header captures clinical signoff explicitly. |
</threat_model>

<verification>
- `docs/i18n/TRANSLATOR-WORKFLOW.md` exists with all 8 sections
- `docs/i18n/clinical-glossary-es.md` exists with Reviewed: <real-date> by <contractor name + creds>
- All 8 ES UI namespace JSON files contain native ES (manual spot-check + no `[MISSING:` markers)
- ES emails.json contains native ES
- KB ES article files exist at agreed scope
- `bash leanshot/scripts/check-locale-coverage.sh` PASS for all 8 namespaces
- `npm run lint` no regression
- `npm run build` succeeds; bundle ceilings preserved
- `npm run test:e2e -- e2e/i18n-language-switch.spec.ts` passes with real ES strings
- PostHog `i18n_missing_key` 24h count after contractor merge is near-zero (Pitfall 3 dynamic-key follow-up captured in SUMMARY if any)
</verification>

<success_criteria>
1. Contractor engaged + delivered all 8 UI namespaces + 1 email namespace + KB ES articles + glossary.
2. Glossary Reviewed: header populated with real date + reviewer credentials (I18N-09 gate cleared).
3. TRANSLATOR-WORKFLOW.md documents the in-house pipeline (I18N-06 gate cleared).
4. CI coverage gate stays green throughout contractor PRs (no key drift).
5. Manual UAT confirms native ES across patient dashboard + onboarding + welcome email (I18N-04 user-facing gate).
6. Plan 32-07 ship-gate inherits a corpus that is contractor-final, not bootstrap.
</success_criteria>

<output>
After completion, create `.planning/phases/32-spanish-i18n-parallel-with-clinic-track/32-06-SUMMARY.md` documenting the contractor delivery, glossary signoff, and REQ-ID closures per Task 3.
</output>
