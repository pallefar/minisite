---
phase: 49-m4-search-email-digests
plan: 05
type: execute
wave: 1
depends_on: [49-01, 49-02, 49-03, 49-04]
files_modified:
  - supabase/migrations/20271001000008_p49_pg_cron_schedules.sql
  - supabase/functions/_shared/__tests__/fts-schema.test.ts
  - supabase/functions/_shared/__tests__/search-content-rpc.test.ts
  - supabase/functions/_shared/__tests__/digest-helpers.test.ts
  - supabase/tests/p49_search_content_rls.sql
  - supabase/tests/p49_digest_send_log_idempotency.sql
autonomous: true
requirements: [DIGEST-01, DIGEST-02, DIGEST-03, DIGEST-04]
must_haves:
  truths:
    - "pg_cron migration registers 2 jobs: phase49-community-daily-digest-hourly-fanout (5 * * * *) + phase49-community-weekly-digest-hourly-fanout (15 * * * *)"
    - "Cron body uses outer $cron$ + inner $daily$/$weekly$/$unschedule$ dollar-quote tags (D-20; distinct from Phase 38/47/48 tags)"
    - "Cron body reads service_role_key from vault.decrypted_secrets WHERE name='service_role_key'; hardcoded fn URLs"
    - "Daily cron predicate: extract(hour from now() at time zone profiles.timezone) = 9; weekly adds extract(dow)=0"
    - "5 test scaffold files exist covering FTS schema, search_content RPC, digest helpers, RLS proof, send_log idempotency"
  artifacts:
    - path: "supabase/migrations/20271001000008_p49_pg_cron_schedules.sql"
      provides: "2 pg_cron jobs (daily + weekly fan-out); idempotent unschedule preamble"
      contains: "cron.schedule('phase49-community-daily-digest-hourly-fanout'"
    - path: "supabase/functions/_shared/__tests__/fts-schema.test.ts"
      provides: "Wave 0 test scaffold — assert search_en/search_es GENERATED column populates on INSERT; GIN index used"
      contains: "Deno.test"
    - path: "supabase/functions/_shared/__tests__/search-content-rpc.test.ts"
      provides: "Wave 0 test scaffold — EN + ES fixtures; ts_headline <b> assert; UNION ALL contract"
      contains: "Deno.test"
    - path: "supabase/functions/_shared/__tests__/digest-helpers.test.ts"
      provides: "Wave 0 test scaffold — per-user filtering for all 6 SECDEF RPCs"
      contains: "Deno.test"
    - path: "supabase/tests/p49_search_content_rls.sql"
      provides: "Cross-tenant impersonation proof"
      contains: "set local request.jwt"
    - path: "supabase/tests/p49_digest_send_log_idempotency.sql"
      provides: "UPSERT conflict-on-(user_id,kind,sent_date) proof"
      contains: "insert into public.digest_send_log"
  key_links:
    - from: "pg_cron jobs"
      to: "community-daily-digest + community-weekly-digest Edge Fns (Wave 1)"
      via: "net.http_post to hardcoded fn URL"
      pattern: "/functions/v1/community-(daily|weekly)-digest"
---

<objective>
Single Wave 0 plan that owns BOTH (a) the pg_cron migration for the 2 hourly fan-out jobs (D-10, D-11, D-20) AND (b) ALL Wave 0 test scaffolds listed in VALIDATION.md.

Purpose: pg_cron migration is the last Wave 0 schema artifact; consolidating the test scaffolds here lets Wave 1 Edge Fn plans (49-06/07/08) consume them immediately rather than create scaffolds inline. Per memory `reference_postgres_dollar_quote_nesting_in_cron_body` — outer `$cron$` + inner `$daily$`/`$weekly$`/`$unschedule$` (NEVER reuse Phase 38 `$digest$`, Phase 47 `$reminders$`, Phase 48 `$restore$`). Per memory `reference_supabase_pg_cron_vault_service_role_pattern` — vault.decrypted_secrets, hardcoded URL.
Output: 1 cron migration + 5 test scaffold files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/49-m4-search-email-digests/49-CONTEXT.md
@leanshot/.planning/phases/49-m4-search-email-digests/49-RESEARCH.md
@leanshot/.planning/phases/49-m4-search-email-digests/49-PATTERNS.md
@leanshot/.planning/phases/49-m4-search-email-digests/49-VALIDATION.md
@supabase/migrations/20270705000030_phase38_pg_cron_schedules.sql

**Memory references (load-bearing):**
- `reference_postgres_dollar_quote_nesting_in_cron_body` — outer `$cron$`, inner `$daily$`/`$weekly$`/`$unschedule$`; distinct from Phase 38 `$digest$`, Phase 47 `$reminders$`, Phase 48 `$restore$`, Phase 38 `$cleanup$`.
- `reference_supabase_pg_cron_vault_service_role_pattern` — `(select decrypted_secret from vault.decrypted_secrets where name='service_role_key')`; hardcoded URL `https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/<fn>`.
- `reference_supabase_migration_filename_regex` — 14-digit `20271001000008`.
- `reference_supabase_service_role_key_format_divergence` — bearer uses live `sb_secret_*` token; vault returns the correct format.
- `reference_deno_test_top_level_serve_trap` — test scaffolds MUST import the Edge Fn `__internal` export (not boot a server).

**Reuse target:** VERBATIM block from `supabase/migrations/20270705000030_phase38_pg_cron_schedules.sql` (Phase 38) for cron shell; swap jobnames + URL + dollar-quote tags + minute offset (5 / 15) + dedup interval.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pg_cron migration — 2 hourly fan-out jobs (daily + weekly)</name>
  <files>supabase/migrations/20271001000008_p49_pg_cron_schedules.sql</files>
  <behavior>
    - `select jobname from cron.job where jobname like 'phase49-community-%-digest-hourly-fanout'` returns 2 rows.
    - `select schedule from cron.job where jobname = 'phase49-community-daily-digest-hourly-fanout'` returns `5 * * * *`.
    - `select schedule from cron.job where jobname = 'phase49-community-weekly-digest-hourly-fanout'` returns `15 * * * *`.
    - Pre-flight `do $unschedule$` block unregisters BOTH jobnames before re-scheduling (idempotent re-apply).
    - Migration body contains `$cron$` outer tag + `$daily$` + `$weekly$` + `$unschedule$` inner tags; NO `$digest$` / `$reminders$` / `$restore$` / `$cleanup$`.
    - Body uses `(select decrypted_secret from vault.decrypted_secrets where name = 'service_role_key' limit 1)`; NO `current_setting('app.service_role_key')`.
  </behavior>
  <action>
    Create `supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` (PATTERNS.md Pattern F lines 400-477 pinned verbatim — adapt jobnames + URLs).

    Header references D-10, D-11, D-20, and memories `reference_postgres_dollar_quote_nesting_in_cron_body` + `reference_supabase_pg_cron_vault_service_role_pattern`.

    Body skeleton (per PATTERNS.md Pattern F):
    ```sql
    create extension if not exists pg_cron;
    create extension if not exists pg_net;

    do $unschedule$
    declare job_name text;
    begin
      for job_name in
        select jobname from cron.job
         where jobname in (
           'phase49-community-daily-digest-hourly-fanout',
           'phase49-community-weekly-digest-hourly-fanout'
         )
      loop
        perform cron.unschedule(job_name);
      end loop;
    exception when others then null;
    end $unschedule$;

    -- DAILY fan-out: minute 5; per-user-TZ predicate hour=9.
    select cron.schedule(
      'phase49-community-daily-digest-hourly-fanout',
      '5 * * * *',
      $cron$
      do $daily$
      declare
        rec record;
        fn_url constant text := 'https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/community-daily-digest';
        service_key text;
      begin
        select decrypted_secret into service_key
          from vault.decrypted_secrets where name = 'service_role_key' limit 1;
        if service_key is null then
          raise notice 'phase49-community-daily-digest-hourly-fanout: service_role_key missing';
          return;
        end if;
        for rec in
          select p.id as user_id
            from public.profiles p
           where extract(hour from (now() at time zone coalesce(p.timezone, 'UTC'))) = 9
             and not exists (
               select 1 from public.digest_send_log dsl
                where dsl.user_id = p.id and dsl.kind = 'daily'
                  and dsl.sent_at > now() - interval '20 hours'
             )
        loop
          perform net.http_post(
            url := fn_url,
            body := jsonb_build_object('user_id', rec.user_id),
            headers := jsonb_build_object(
              'Authorization', 'Bearer ' || service_key,
              'Content-Type', 'application/json'
            ),
            timeout_milliseconds := 60000
          );
        end loop;
      end;
      $daily$;
      $cron$
    );

    -- WEEKLY fan-out: minute 15; predicate dow=0 (Sunday) + hour=9; dedup window 6 days.
    select cron.schedule(
      'phase49-community-weekly-digest-hourly-fanout',
      '15 * * * *',
      $cron$
      do $weekly$
      declare
        rec record;
        fn_url constant text := 'https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/community-weekly-digest';
        service_key text;
      begin
        select decrypted_secret into service_key
          from vault.decrypted_secrets where name = 'service_role_key' limit 1;
        if service_key is null then
          raise notice 'phase49-community-weekly-digest-hourly-fanout: service_role_key missing';
          return;
        end if;
        for rec in
          select p.id as user_id
            from public.profiles p
           where extract(dow from (now() at time zone coalesce(p.timezone, 'UTC'))) = 0
             and extract(hour from (now() at time zone coalesce(p.timezone, 'UTC'))) = 9
             and not exists (
               select 1 from public.digest_send_log dsl
                where dsl.user_id = p.id and dsl.kind = 'weekly'
                  and dsl.sent_at > now() - interval '6 days'
             )
        loop
          perform net.http_post(
            url := fn_url,
            body := jsonb_build_object('user_id', rec.user_id),
            headers := jsonb_build_object(
              'Authorization', 'Bearer ' || service_key,
              'Content-Type', 'application/json'
            ),
            timeout_milliseconds := 60000
          );
        end loop;
      end;
      $weekly$;
      $cron$
    );
    ```

    Per memory `feedback_negation_grep_defeated_by_comment_string` — do NOT mention `$digest$`, `current_setting('app.service_role_key')`, or other rejected-alt strings in committed comments.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c '\$cron\$' supabase/migrations/20271001000008_p49_pg_cron_schedules.sql | xargs test 4 -le && grep -c '\$daily\$' supabase/migrations/20271001000008_p49_pg_cron_schedules.sql | xargs test 2 -le && grep -c '\$weekly\$' supabase/migrations/20271001000008_p49_pg_cron_schedules.sql | xargs test 2 -le && grep -c '\$unschedule\$' supabase/migrations/20271001000008_p49_pg_cron_schedules.sql | xargs test 2 -le && grep -c "'5 \* \* \* \*'" supabase/migrations/20271001000008_p49_pg_cron_schedules.sql | xargs test 1 -le && grep -c "'15 \* \* \* \*'" supabase/migrations/20271001000008_p49_pg_cron_schedules.sql | xargs test 1 -le && test $(grep -c "current_setting('app.service_role_key')" supabase/migrations/20271001000008_p49_pg_cron_schedules.sql) -eq 0 && test $(grep -c '\$digest\$\|\$reminders\$\|\$restore\$\|\$cleanup\$' supabase/migrations/20271001000008_p49_pg_cron_schedules.sql) -eq 0</automated>
  </verify>
  <done>
    Cron migration uses required dollar-quote tags; 2 cron.schedule calls with correct schedules and URLs; vault.decrypted_secrets read; no rejected-tag collisions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 5 Wave 0 test scaffolds (Deno + SQL)</name>
  <files>supabase/functions/_shared/__tests__/fts-schema.test.ts, supabase/functions/_shared/__tests__/search-content-rpc.test.ts, supabase/functions/_shared/__tests__/digest-helpers.test.ts, supabase/tests/p49_search_content_rls.sql, supabase/tests/p49_digest_send_log_idempotency.sql</files>
  <behavior>
    - 3 Deno tests exist + 2 pgTAP/raw-SQL tests; each file at least has a Deno.test() or comment-test block referencing the live DB.
    - fts-schema.test.ts asserts: INSERT into community_posts populates search_en (non-null tsvector); GIN index used in explain.
    - search-content-rpc.test.ts asserts: 'dosing' EN query returns rows with `<b>dosing</b>` in snippet; 'dosis' ES query returns Spanish-tokenized rows; cross-table UNION returns mixed type rows.
    - digest-helpers.test.ts asserts: each of 6 SECDEF RPCs returns the expected shape; per-user filter excludes other users; digest_recent_mentions JOIN to parent.created_at correctly filters 24h.
    - p49_search_content_rls.sql proves: clinic A user impersonation does NOT return clinic B community_posts rows via search_content.
    - p49_digest_send_log_idempotency.sql proves: 2nd INSERT on same (user_id, kind, sent_date) raises 23505 unique_violation, OR UPSERT collapses to single row.
  </behavior>
  <action>
    Create 3 Deno test scaffolds under `supabase/functions/_shared/__tests__/` and 2 SQL files under `supabase/tests/`.

    **fts-schema.test.ts** (FORK from `supabase/functions/_shared/digest-schema.test.ts` idiom):
    ```typescript
    import { assertEquals, assertExists } from 'https://deno.land/std@0.224.0/assert/mod.ts';
    import { createClient } from 'npm:@supabase/supabase-js@2';

    const url = Deno.env.get('SUPABASE_URL')!;
    const serviceKey = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;

    Deno.test('community_posts.search_en populates on INSERT (body-only weight A per D-17)', async () => {
      const sb = createClient(url, serviceKey);
      // Insert fixture post; assert search_en non-null + matches websearch_to_tsquery('english','dosing').
      // Cleanup row at end.
      // TODO: implement live-DB INSERT + SELECT + cleanup. Marked TODO until Wave 0 DB push.
    });

    Deno.test('course_lessons.search_en uses title=A + content_md=B', async () => {
      // TODO
    });

    Deno.test('events.search_en uses title=A + description=B', async () => {
      // TODO
    });
    ```

    **search-content-rpc.test.ts** (FORK from `digest-schema.test.ts`):
    ```typescript
    import { assertEquals } from 'https://deno.land/std@0.224.0/assert/mod.ts';
    import { createClient } from 'npm:@supabase/supabase-js@2';

    Deno.test('search_content RPC: English query returns top-5-per-type', async () => {
      // TODO: invoke supabase.rpc('search_content', {p_query:'dosing', p_lang:'english'})
      // Assert: each `type` (post/lesson/event) has ≤5 rows; snippet wraps match in <b>.
    });

    Deno.test('search_content RPC: Spanish query uses spanish regconfig', async () => {
      // TODO
    });

    Deno.test('search_content RPC: ts_headline runs only over LIMIT-5 survivors (D-21)', async () => {
      // TODO: assert via EXPLAIN that ts_headline executes 15 times (5 per type) max.
    });
    ```

    **digest-helpers.test.ts** (FORK same):
    ```typescript
    Deno.test('digest_top_posts_in_spaces returns top-5 score-ordered', async () => { /* TODO */ });
    Deno.test('digest_new_comments_on_my_posts filters to user-authored posts', async () => { /* TODO */ });
    Deno.test('digest_recent_mentions 24h filter JOINs parent.created_at (D-18)', async () => { /* TODO */ });
    Deno.test('digest_course_progress_delta_7d computes % correctly', async () => { /* TODO */ });
    Deno.test('digest_upcoming_events_7d_rsvpd filters to status=going + 7d window', async () => { /* TODO */ });
    Deno.test('digest_community_top3_7d uses Phase 45 score formula (posts*3 + (reactions+comments)*1)', async () => { /* TODO */ });
    ```

    **p49_search_content_rls.sql** (pgTAP-style or raw SQL):
    ```sql
    -- Phase 49 Plan 05 — cross-tenant RLS proof for search_content RPC.
    -- Per project rule: every RLS surface gets a live cross-tenant impersonation proof test.
    begin;

    -- 1. Create 2 clinic orgs + 2 users (clinic A + clinic B); seed posts in each.
    -- 2. Set local request.jwt.claims to impersonate clinic A user.
    -- 3. Call search_content('GLP'); assert: rows have space_id from clinic A only; NO clinic B rows.
    -- 4. Reverse: impersonate clinic B; assert only clinic B rows.

    -- TODO: implement fixtures + jwt impersonation. Marked TODO until Wave 0 push.

    rollback;
    ```

    **p49_digest_send_log_idempotency.sql**:
    ```sql
    -- Phase 49 Plan 05 — digest_send_log UPSERT idempotency proof.
    begin;

    -- 1. Insert (user_id=test, kind='daily', sent_at=now(), status='sent').
    -- 2. UPSERT same (user_id, kind, sent_date) with status='skipped:no-content' on conflict update.
    -- 3. Assert: exactly 1 row exists for that user_id+kind+sent_date.
    -- 4. Insert 2nd raw without ON CONFLICT — assert 23505 unique_violation.

    -- TODO

    rollback;
    ```

    All scaffolds are TODO-marked but importable; Wave 0 db push fills them in via subsequent commits if not landed inline.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/functions/_shared/__tests__/fts-schema.test.ts supabase/functions/_shared/__tests__/search-content-rpc.test.ts supabase/functions/_shared/__tests__/digest-helpers.test.ts supabase/tests/p49_search_content_rls.sql supabase/tests/p49_digest_send_log_idempotency.sql 2>&1 | grep -v 'cannot access' | wc -l | xargs test 5 -le</automated>
  </verify>
  <done>
    All 5 scaffold files exist at canonical paths; Deno.test() blocks present even if TODO-filled; SQL scaffolds begin/rollback wrapped.
  </done>
</task>

</tasks>

<acceptance_criteria>
- `ls supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` — exists.
- `grep -c '\$cron\$' supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` — at least 4 (2 open + 2 close).
- `grep -c '\$daily\$' supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` — at least 2.
- `grep -c '\$weekly\$' supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` — at least 2.
- `grep -c "'5 \* \* \* \*'" supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` — at least 1.
- `grep -c "'15 \* \* \* \*'" supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` — at least 1.
- `grep -c "current_setting('app.service_role_key')" supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` — exactly 0.
- `grep -c '\$digest\$\|\$reminders\$\|\$restore\$' supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` — exactly 0.
- `grep -c 'phase49-community-daily-digest-hourly-fanout' supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` — at least 2.
- `grep -c 'phase49-community-weekly-digest-hourly-fanout' supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` — at least 2.
- `grep -c "vault.decrypted_secrets" supabase/migrations/20271001000008_p49_pg_cron_schedules.sql` — at least 2.
- `ls supabase/functions/_shared/__tests__/fts-schema.test.ts` — exists.
- `ls supabase/functions/_shared/__tests__/search-content-rpc.test.ts` — exists.
- `ls supabase/functions/_shared/__tests__/digest-helpers.test.ts` — exists.
- `ls supabase/tests/p49_search_content_rls.sql` — exists.
- `ls supabase/tests/p49_digest_send_log_idempotency.sql` — exists.
- Each Deno scaffold: `grep -c 'Deno.test' <file>` — at least 1.
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron → Edge Fn | cron body mints `Bearer ${vault.service_role_key}` for each net.http_post |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-11 | S (Spoofing) | non-cron caller invokes Edge Fns | mitigate | Edge Fns require `checkServiceRoleBearer(req)` — only cron + admin tooling can authenticate |
| T-49-12 | T (Tampering) | cron body modification via dollar-quote escape | mitigate | Named `$cron$` + `$daily$`/`$weekly$` per memory; tag uniqueness prevents accidental closure |
| T-49-13 | D (DoS) | minute-0 collision with Phase 38 cron | mitigate | D-20 offsets (5 / 15) avoid HTTP burst |
</threat_model>

<verification>
- All 6 files exist; grep + ls gates pass.
- Wave 1 plans (49-06, 49-07, 49-08) consume the test scaffolds for HMAC / fixture testing.
- Wave 3 close-out applies cron migration via `supabase db push --linked`; verifies via `select jobname from cron.job where jobname like 'phase49-%'`.
</verification>

<success_criteria>
- 1 cron migration + 5 test scaffolds committed.
- All `<acceptance_criteria>` greps pass.
- Dollar-quote tags unique vs Phase 38/47/48; minute offsets 5/15; vault read pattern correct.
</success_criteria>

<output>
After completion, create `.planning/phases/49-m4-search-email-digests/49-05-SUMMARY.md` per template.
</output>
