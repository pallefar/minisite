---
phase: 49-m4-search-email-digests
plan: 08
type: execute
wave: 1
depends_on: [49-04]
files_modified:
  - supabase/functions/_shared/unsubscribe-token.ts
  - supabase/functions/_shared/unsubscribe-token.test.ts
  - supabase/functions/unsubscribe-handler/index.ts
  - supabase/functions/unsubscribe-handler/deno.json
  - supabase/functions/unsubscribe-handler/index.test.ts
autonomous: true
requirements: [DIGEST-04]
must_haves:
  truths:
    - "_shared/unsubscribe-token.ts exports mintUnsubscribeToken + verifyUnsubscribeToken using HMAC-SHA256 via node:crypto.createHmac + timingSafeEqual"
    - "Token payload = {user_id, category in ('daily_community_digest','weekly_community_digest'), exp unix-seconds}; KEY_ENV='UNSUBSCRIBE_SECRET'"
    - "base64url replace-chain via Postgres-compatible regex per memory reference_base64url_postgres_vercel_mint_verify"
    - "unsubscribe-handler Edge Fn GET+POST (RFC 8058 One-Click POST supported); no Authorization bearer required (token IS the auth)"
    - "Handler UPSERTs notification_settings SET enabled=false WHERE user_id=t.user_id AND category=t.category AND channel='email'; idempotent"
    - "Handler renders confirmation HTML with <meta name=robots content=noindex> + Cache-Control: no-store"
    - "Tampered HMAC → 401; expired exp → 401; missing token → 400; unknown category → 401 (whitelist enforced inside verify)"
    - "Deno.serve guard with UNSUBSCRIBE_HANDLER_DISABLE_SERVE env var"
    - "Per-Fn deno.json present"
  artifacts:
    - path: "supabase/functions/_shared/unsubscribe-token.ts"
      provides: "HMAC-SHA256 mint+verify utility for unsub URLs"
      contains: "mintUnsubscribeToken"
    - path: "supabase/functions/_shared/unsubscribe-token.test.ts"
      provides: "Roundtrip + tamper + expiry + missing-env test scaffolds"
      contains: "Deno.test"
    - path: "supabase/functions/unsubscribe-handler/index.ts"
      provides: "GET/POST endpoint; token-gated; flips notification_settings + renders confirmation HTML"
      contains: "verifyUnsubscribeToken"
    - path: "supabase/functions/unsubscribe-handler/deno.json"
      provides: "Per-Fn import map"
      contains: "imports"
    - path: "supabase/functions/unsubscribe-handler/index.test.ts"
      provides: "Deno test scaffold — valid/tampered/expired/missing-token"
      contains: "Deno.test"
  key_links:
    - from: "Plan 49-06 + 49-07 mintUnsubscribeToken"
      to: "_shared/unsubscribe-token.ts mintUnsubscribeToken export"
      via: "import { mintUnsubscribeToken } from '../_shared/unsubscribe-token.ts'"
      pattern: "import.*mintUnsubscribeToken"
    - from: "unsubscribe-handler verify"
      to: "notification_settings UPSERT (user_id, category, channel='email', enabled=false)"
      via: "admin.from('notification_settings').upsert(...)"
      pattern: "notification_settings.*upsert.*enabled: false"
---

<objective>
Ship the 1-click unsubscribe primitives: (a) `_shared/unsubscribe-token.ts` HMAC mint/verify helper; (b) `unsubscribe-handler` Edge Fn (GET+POST; no Bearer; token-gated). Both Wave 1 digest Fns (49-06, 49-07) import `mintUnsubscribeToken`; orchestrator serializes 49-08 before {49-06, 49-07} within Wave 1.

Purpose: Per CONTEXT D-13 — HMAC-signed token URL + GET endpoint + RFC 8058 List-Unsubscribe + List-Unsubscribe-Post: One-Click. URL shape `?t=<token>` (single param — researcher recommendation; matches Phase 42 NPS shape). Per memory `reference_base64url_postgres_vercel_mint_verify` — base64url replace-chain. Per memory `reference_deno_test_top_level_serve_trap` — Deno.serve env-var guarded. Per CONTEXT Claude-discretion — KEY_ENV = `UNSUBSCRIBE_SECRET` (Supabase Function Secret; vendor pre-flight surfaced at Wave 0 dispatch per memory `feedback_vendor_secret_preflight_surface`).
Output: 5 files (2 shared + 3 Fn).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/49-m4-search-email-digests/49-CONTEXT.md
@leanshot/.planning/phases/49-m4-search-email-digests/49-RESEARCH.md
@leanshot/.planning/phases/49-m4-search-email-digests/49-PATTERNS.md
@supabase/functions/_shared/anthropic-baa-allowlist.test.ts

**Memory references (load-bearing):**
- `reference_base64url_postgres_vercel_mint_verify` — base64url chain `replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')`.
- `reference_deno_test_top_level_serve_trap` — `if (Deno.env.get('UNSUBSCRIBE_HANDLER_DISABLE_SERVE') !== '1')` guard.
- `reference_supabase_functions_deploy_import_map_flag` — per-Fn deno.json.
- `feedback_vendor_secret_preflight_surface` — UNSUBSCRIBE_SECRET is NEW; orchestrator surfaces command `supabase secrets set UNSUBSCRIBE_SECRET=$(openssl rand -base64 32)` BEFORE Wave 1 dispatch.
- `reference_minisite_monorepo_layout` — `supabase/functions/...` from git root.
- `feedback_negation_grep_defeated_by_comment_string` — do NOT include rejected-alt `auth.admin.signOut` or `current_setting('app.unsubscribe_secret')` in committed code/comments.

**Reuse target:** FORK `supabase/functions/_shared/nps-token.ts` (Phase 42 NPS) verbatim — swap payload shape to `{user_id, category, exp}`, swap KEY_ENV to `UNSUBSCRIBE_SECRET`, keep timingSafeEqual + base64url replace-chain.

**Vendor secret pre-flight (per memory):** Operator must run BEFORE Wave 1 dispatch:
```bash
supabase secrets set UNSUBSCRIBE_SECRET=$(openssl rand -base64 32) --project-ref ytnsipxxmzgaebkqmokp
supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep UNSUBSCRIBE_SECRET
```
This is surfaced by orchestrator at Wave 1 dispatch; not a plan-task action.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _shared/unsubscribe-token.ts mint/verify + test scaffold</name>
  <files>supabase/functions/_shared/unsubscribe-token.ts, supabase/functions/_shared/unsubscribe-token.test.ts</files>
  <behavior>
    - mintUnsubscribeToken({user_id, category, exp}): returns `<base64url(json)>.<base64url(hmac_sha256)>` string.
    - verifyUnsubscribeToken(token): returns payload object OR null. Returns null for: missing dot, bad base64url, HMAC mismatch via timingSafeEqual (constant-time), expired (now > exp), unknown category (whitelist enforced).
    - Test seam: optional `key?` parameter on both mint + verify lets tests pass fixtures without setting env.
    - KEY_ENV='UNSUBSCRIBE_SECRET'; readSigningKey() throws if env missing.
    - Test file has 5+ Deno.test blocks: mint+verify roundtrip; tampered MAC → null; expired exp → null; missing payload field → null; unknown category → null.
  </behavior>
  <action>
    Create `supabase/functions/_shared/unsubscribe-token.ts` (FORK `_shared/nps-token.ts` per PATTERNS.md Pattern J — RESEARCH Example 7 lines 911-961 pinned).

    ```typescript
    import { createHmac, timingSafeEqual } from 'node:crypto';

    export interface UnsubscribeTokenPayload {
      user_id: string;
      category: 'daily_community_digest' | 'weekly_community_digest';
      exp: number;
    }

    const KEY_ENV = 'UNSUBSCRIBE_SECRET';

    function readSigningKey(): string {
      const v = (globalThis as any).Deno?.env?.get?.(KEY_ENV);
      if (!v) throw new Error(`unsubscribe-token: ${KEY_ENV} not set`);
      return v;
    }

    function toBase64Url(bytes: Uint8Array | Buffer): string {
      return Buffer.from(bytes).toString('base64')
        .replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
    }

    function fromBase64Url(s: string): Buffer {
      const pad = s.length % 4 === 2 ? '==' : s.length % 4 === 3 ? '=' : '';
      return Buffer.from(s.replace(/-/g, '+').replace(/_/g, '/') + pad, 'base64');
    }

    export function mintUnsubscribeToken(payload: UnsubscribeTokenPayload, key?: string): string {
      const signingKey = key ?? readSigningKey();
      const json = JSON.stringify(payload);
      const payloadEncoded = toBase64Url(Buffer.from(json, 'utf8'));
      const mac = createHmac('sha256', signingKey).update(payloadEncoded).digest();
      return `${payloadEncoded}.${toBase64Url(mac)}`;
    }

    export function verifyUnsubscribeToken(token: string, key?: string): UnsubscribeTokenPayload | null {
      if (!token) return null;
      const [payloadEncoded, macEncoded] = token.split('.');
      if (!payloadEncoded || !macEncoded) return null;
      const signingKey = key ?? readSigningKey();
      const expectedMac = createHmac('sha256', signingKey).update(payloadEncoded).digest();
      let providedMac: Buffer;
      try { providedMac = fromBase64Url(macEncoded); } catch { return null; }
      if (providedMac.length !== expectedMac.length) return null;
      if (!timingSafeEqual(providedMac, expectedMac)) return null;
      let payload: UnsubscribeTokenPayload;
      try { payload = JSON.parse(fromBase64Url(payloadEncoded).toString('utf8')); } catch { return null; }
      if (!payload?.user_id || !payload?.category || typeof payload?.exp !== 'number') return null;
      if (payload.category !== 'daily_community_digest' && payload.category !== 'weekly_community_digest') return null;
      if (Math.floor(Date.now() / 1000) > payload.exp) return null;
      return payload;
    }
    ```

    Create `supabase/functions/_shared/unsubscribe-token.test.ts`:
    ```typescript
    import { assertEquals, assertThrows } from 'https://deno.land/std@0.224.0/assert/mod.ts';
    import { mintUnsubscribeToken, verifyUnsubscribeToken } from './unsubscribe-token.ts';

    const TEST_KEY = 'test-fixture-key';

    Deno.test('roundtrip: mint then verify recovers payload', () => {
      const p = { user_id: '00000000-0000-0000-0000-000000000001', category: 'daily_community_digest' as const, exp: Math.floor(Date.now()/1000) + 3600 };
      const tok = mintUnsubscribeToken(p, TEST_KEY);
      assertEquals(verifyUnsubscribeToken(tok, TEST_KEY), p);
    });

    Deno.test('tampered MAC → null', () => {
      const p = { user_id: 'u', category: 'daily_community_digest' as const, exp: Math.floor(Date.now()/1000) + 3600 };
      const tok = mintUnsubscribeToken(p, TEST_KEY);
      const parts = tok.split('.');
      const tampered = `${parts[0]}.${parts[1].slice(0, -2)}AA`;
      assertEquals(verifyUnsubscribeToken(tampered, TEST_KEY), null);
    });

    Deno.test('expired exp → null', () => {
      const p = { user_id: 'u', category: 'weekly_community_digest' as const, exp: Math.floor(Date.now()/1000) - 1 };
      const tok = mintUnsubscribeToken(p, TEST_KEY);
      assertEquals(verifyUnsubscribeToken(tok, TEST_KEY), null);
    });

    Deno.test('unknown category → null', () => {
      // Mint a token with rejected category, then verify
      const json = JSON.stringify({ user_id: 'u', category: 'rejected_category', exp: Math.floor(Date.now()/1000) + 3600 });
      // Construct manually using mint mechanic but with bad category — should reject in verify.
      // TODO: implement manual mint then verify-rejects.
    });

    Deno.test('readSigningKey throws when UNSUBSCRIBE_SECRET unset', () => {
      assertThrows(() => verifyUnsubscribeToken('any.thing'));
    });
    ```

    Per memory `feedback_negation_grep_defeated_by_comment_string` — do NOT mention `current_setting('app.unsubscribe_secret')` or vault as rejected-alts in committed comments.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/functions/_shared/unsubscribe-token.ts supabase/functions/_shared/unsubscribe-token.test.ts && grep -c 'createHmac' supabase/functions/_shared/unsubscribe-token.ts | xargs test 1 -le && grep -c 'timingSafeEqual' supabase/functions/_shared/unsubscribe-token.ts | xargs test 1 -le && grep -c "KEY_ENV = 'UNSUBSCRIBE_SECRET'" supabase/functions/_shared/unsubscribe-token.ts | xargs test 1 -le && grep -c "daily_community_digest\|weekly_community_digest" supabase/functions/_shared/unsubscribe-token.ts | xargs test 2 -le && grep -c 'Deno.test' supabase/functions/_shared/unsubscribe-token.test.ts | xargs test 5 -le</automated>
  </verify>
  <done>
    2 shared files exist; mint+verify use createHmac + timingSafeEqual; KEY_ENV correct; whitelist categories checked in verify; 5+ Deno.test blocks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: unsubscribe-handler Edge Fn (index.ts + deno.json + test scaffold)</name>
  <files>supabase/functions/unsubscribe-handler/index.ts, supabase/functions/unsubscribe-handler/deno.json, supabase/functions/unsubscribe-handler/index.test.ts</files>
  <behavior>
    - GET + POST both supported (RFC 8058 One-Click POSTs to URL).
    - NO Authorization bearer check; URL `?t=<token>` IS the auth.
    - Valid token → UPSERT notification_settings (user_id, category, channel='email', enabled=false) onConflict (user_id, category, channel) → respond 200 with HTML confirmation page (Content-Type: text/html; charset=utf-8; Cache-Control: no-store; `<meta name="robots" content="noindex">`).
    - Missing token → 400 'missing_token'.
    - Invalid/tampered/expired token → 401 'invalid_token'.
    - Unknown method → 405.
    - Deno.serve guard via UNSUBSCRIBE_HANDLER_DISABLE_SERVE.
    - Test scaffold has 5+ Deno.test blocks: valid GET, valid POST, tampered → 401, expired → 401, missing → 400.
    - deno.json present.
  </behavior>
  <action>
    Create `supabase/functions/unsubscribe-handler/index.ts` (PATTERNS.md Pattern I lines 596-664 pinned):

    ```typescript
    import { corsHeaders, jsonError, makeLazyAdmin } from '../_shared/lifecycle-utils.ts';
    import { verifyUnsubscribeToken } from '../_shared/unsubscribe-token.ts';

    const { admin } = makeLazyAdmin();

    const CONFIRMATION_HTML = (category: string) => `<!doctype html>
    <html><head>
      <meta charset="utf-8">
      <meta name="robots" content="noindex">
      <title>Unsubscribed — LeanShot</title>
      <style>body { font-family: system-ui, -apple-system, sans-serif; max-width: 480px; margin: 80px auto; padding: 24px; }</style>
    </head><body>
      <h1>You've unsubscribed</h1>
      <p>You will no longer receive <strong>${category.replace(/_/g, ' ')}</strong> emails.</p>
      <p><a href="https://leanshot.app/settings/notifications">Manage all preferences</a></p>
    </body></html>`;

    async function handler(req: Request): Promise<Response> {
      if (req.method === 'OPTIONS') return new Response('ok', { headers: corsHeaders });
      if (req.method !== 'GET' && req.method !== 'POST') {
        return jsonError(405, 'method_not_allowed');
      }
      const url = new URL(req.url);
      const token = url.searchParams.get('t');
      if (!token) return jsonError(400, 'missing_token');

      const payload = verifyUnsubscribeToken(token);
      if (!payload) return jsonError(401, 'invalid_token');

      await admin
        .from('notification_settings')
        .upsert(
          { user_id: payload.user_id, category: payload.category, channel: 'email', enabled: false },
          { onConflict: 'user_id,category,channel' }
        );

      return new Response(CONFIRMATION_HTML(payload.category), {
        status: 200,
        headers: { 'Content-Type': 'text/html; charset=utf-8', 'Cache-Control': 'no-store' },
      });
    }

    if (Deno.env.get('UNSUBSCRIBE_HANDLER_DISABLE_SERVE') !== '1') {
      Deno.serve(handler);
    }

    export const __internal = { handler };
    ```

    Create `supabase/functions/unsubscribe-handler/deno.json` (FORK from `supabase/functions/mux-webhook/deno.json` verbatim).

    Create `supabase/functions/unsubscribe-handler/index.test.ts`:
    ```typescript
    import { assertEquals } from 'https://deno.land/std@0.224.0/assert/mod.ts';
    Deno.env.set('UNSUBSCRIBE_HANDLER_DISABLE_SERVE', '1');
    Deno.env.set('UNSUBSCRIBE_SECRET', 'test-fixture-key');
    const { __internal } = await import('./index.ts');

    Deno.test('GET with valid token → 200 HTML + noindex meta', async () => { /* TODO */ });
    Deno.test('POST with valid token (RFC 8058 One-Click) → 200', async () => { /* TODO */ });
    Deno.test('tampered token → 401 invalid_token', async () => { /* TODO */ });
    Deno.test('expired token → 401 invalid_token', async () => { /* TODO */ });
    Deno.test('missing ?t param → 400 missing_token', async () => { /* TODO */ });
    Deno.test('unknown method (PUT) → 405', async () => { /* TODO */ });
    ```

    Per memory `feedback_negation_grep_defeated_by_comment_string` — do NOT mention `auth.admin.signOut` or other rejected-alt patterns in committed comments.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/functions/unsubscribe-handler/index.ts supabase/functions/unsubscribe-handler/deno.json supabase/functions/unsubscribe-handler/index.test.ts && grep -c 'verifyUnsubscribeToken' supabase/functions/unsubscribe-handler/index.ts | xargs test 1 -le && grep -c 'UNSUBSCRIBE_HANDLER_DISABLE_SERVE' supabase/functions/unsubscribe-handler/index.ts | xargs test 1 -le && grep -c 'robots.*noindex' supabase/functions/unsubscribe-handler/index.ts | xargs test 1 -le && grep -c 'Cache-Control.*no-store' supabase/functions/unsubscribe-handler/index.ts | xargs test 1 -le && test $(grep -c 'checkServiceRoleBearer' supabase/functions/unsubscribe-handler/index.ts) -eq 0 && grep -c 'Deno.test' supabase/functions/unsubscribe-handler/index.test.ts | xargs test 5 -le</automated>
  </verify>
  <done>
    3 unsubscribe-handler files exist; verify + UPSERT + noindex + no-store all present; no Bearer check; Deno.serve guarded; 5+ Deno.test blocks.
  </done>
</task>

</tasks>

<acceptance_criteria>
- `ls supabase/functions/_shared/unsubscribe-token.ts` — exists.
- `ls supabase/functions/_shared/unsubscribe-token.test.ts` — exists.
- `ls supabase/functions/unsubscribe-handler/index.ts` — exists.
- `ls supabase/functions/unsubscribe-handler/deno.json` — exists.
- `ls supabase/functions/unsubscribe-handler/index.test.ts` — exists.
- `grep -c 'createHmac' supabase/functions/_shared/unsubscribe-token.ts` — at least 1.
- `grep -c 'timingSafeEqual' supabase/functions/_shared/unsubscribe-token.ts` — at least 1.
- `grep -c "KEY_ENV = 'UNSUBSCRIBE_SECRET'" supabase/functions/_shared/unsubscribe-token.ts` — at least 1.
- `grep -c 'replace(/=+$/' supabase/functions/_shared/unsubscribe-token.ts` — at least 1 (base64url chain).
- `grep -c 'export function mintUnsubscribeToken' supabase/functions/_shared/unsubscribe-token.ts` — at least 1.
- `grep -c 'export function verifyUnsubscribeToken' supabase/functions/_shared/unsubscribe-token.ts` — at least 1.
- `grep -c 'verifyUnsubscribeToken' supabase/functions/unsubscribe-handler/index.ts` — at least 1.
- `grep -c 'UNSUBSCRIBE_HANDLER_DISABLE_SERVE' supabase/functions/unsubscribe-handler/index.ts` — at least 1.
- `grep -c 'checkServiceRoleBearer' supabase/functions/unsubscribe-handler/index.ts` — exactly 0 (token-gated, NO Bearer).
- `grep -c "name=\"robots\" content=\"noindex\"" supabase/functions/unsubscribe-handler/index.ts` — at least 1.
- `grep -c "Cache-Control" supabase/functions/unsubscribe-handler/index.ts` — at least 1.
- `grep -c "onConflict: 'user_id,category,channel'" supabase/functions/unsubscribe-handler/index.ts` — at least 1.
- `grep -c "current_setting" supabase/functions/unsubscribe-handler/index.ts` — exactly 0.
- `grep -c 'Deno.test' supabase/functions/_shared/unsubscribe-token.test.ts` — at least 5.
- `grep -c 'Deno.test' supabase/functions/unsubscribe-handler/index.test.ts` — at least 5.
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click email link → unsubscribe-handler | URL signature IS the auth; HMAC constant-time compare |
| handler → admin client | service-role bypass for UPSERT into notification_settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-21 | S (Spoofing) | attacker forges unsub URL | mitigate | HMAC-SHA256 over payloadEncoded with UNSUBSCRIBE_SECRET; timingSafeEqual constant-time |
| T-49-22 | T (Tampering) | replay old token after user re-subscribes | accept | UPSERT is idempotent-toggle-off; user can re-enable via /settings/notifications |
| T-49-23 | I (Info disclosure) | error response reveals whether user_id exists | mitigate | UPSERT is 0-rows-affected-tolerant; 200 OK regardless of row existence (email enumeration mitigation) |
| T-49-24 | D (DoS) | unauthenticated POST flood | accept | Edge Fn rate-limits per project; URL signing cost is HMAC-verify only |
| T-49-25 | E (Elevation) | URL re-used after sender browser caches | mitigate | Cache-Control: no-store on response |
</threat_model>

<verification>
- 5 files exist; grep gates pass.
- Wave 0 vendor secret pre-flight (orchestrator-surfaced): `supabase secrets set UNSUBSCRIBE_SECRET=$(openssl rand -base64 32) --project-ref ytnsipxxmzgaebkqmokp`.
- Wave 3 deploys via `supabase functions deploy unsubscribe-handler`.
- Cross-Fn Deno sweep at close-out exercises mint+verify roundtrip + handler tampered-token rejection.
- Wave 3 HUMAN-UAT signal-1 (Plan 49-10) tests Gmail RFC 8058 in-inbox unsubscribe button POSTs to this Fn end-to-end.
</verification>

<success_criteria>
- 5 files committed.
- All `<acceptance_criteria>` greps pass.
- HMAC-SHA256 + timingSafeEqual + base64url replace-chain match Phase 42 NPS pattern.
- Handler is GET+POST + no-Bearer + UPSERT + noindex/no-store; Deno.serve guarded.
</success_criteria>

<output>
After completion, create `.planning/phases/49-m4-search-email-digests/49-08-SUMMARY.md` per template.
</output>
