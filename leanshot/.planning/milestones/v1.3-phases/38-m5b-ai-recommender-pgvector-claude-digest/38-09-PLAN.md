---
phase: 38-m5b-ai-recommender-pgvector-claude-digest
plan: 09
type: execute
wave: 3
depends_on: [38-01, 38-02, 38-03, 38-04, 38-05]
files_modified:
  - supabase/migrations/20270705000030_phase38_pg_cron_schedules.sql
  - supabase/migrations/20270705000031_phase38_taxo_events.sql
  - supabase/functions/_shared/posthog-server.ts
  - supabase/functions/track-rec-click/index.ts
  - supabase/functions/track-rec-click/index.test.ts
  - src/components/dashboard/cards/ForYouCard.tsx
  - src/components/dashboard/cards/ForYouCard.test.tsx
  - src/components/kb/RelatedArticlesFooter.tsx
autonomous: true
requirements: [RECOMMEND-06, RECOMMEND-08]
must_haves:
  truths:
    - "pg_cron schedules: weekly-digest-hourly-fanout (0 * * * *), embed-content-nightly (0 3 * * *), winback-scorer-nightly (0 4 * * *), softdelete-cleanup-daily (0 5 * * *)"
    - "Per-user-timezone digest fan-out fires Sunday 09:00 LOCAL with 6h dedup (RESEARCH Pattern 3 + Pitfall #7)"
    - "All cron bodies use named dollar-quote tags ($cron$...$digest$) per memory reference_postgres_dollar_quote_nesting_in_cron_body"
    - "TAXO events registered in Phase 24 catalog: recommendation.shown/.clicked/.dismissed/.404_on_click, digest.sent/.skipped/.validation_failed/.redflag_detected/.baa_scope_resolved, win_back_trigger, hitl_approve/.reject/.edit, eval.judge.score"
    - "Dashboard For You card renders top-3 from recommender Edge Fn; falls back to popular content on error (D-01 dashboard surface)"
    - "KB article footer renders Related Articles via recommender with surface=kb_footer + exclude_id (RECOMMEND-08)"
  artifacts:
    - path: supabase/migrations/20270705000030_phase38_pg_cron_schedules.sql
      provides: "all Phase 38 cron jobs"
      contains: "cron.schedule"
    - path: src/components/dashboard/cards/ForYouCard.tsx
      provides: "dashboard For You bento card"
      min_lines: 50
    - path: src/components/kb/RelatedArticlesFooter.tsx
      provides: "KB article footer related-articles renderer"
      min_lines: 30
  key_links:
    - from: supabase/migrations/20270705000030_phase38_pg_cron_schedules.sql
      to: supabase/functions/weekly-digest/index.ts
      via: "net.http_post invokes weekly-digest per-user from cron body"
      pattern: "weekly-digest"
    - from: src/components/dashboard/cards/ForYouCard.tsx
      to: supabase/functions/recommend-next-best-action/index.ts
      via: "supabase.functions.invoke on mount"
      pattern: "functions\\.invoke.*recommend-next-best-action"
---

<objective>
Wire up cron schedules, telemetry registry, and the two client-side surfaces (dashboard For You card + KB footer Related Articles) that consume the recommender Edge Fn. Cron uses pg_cron + vault.decrypted_secrets pattern (Phase 25). Telemetry registers all Phase 38 events in the Phase 24 TAXO event catalog. CTR tracking via server-side PostHog (no client-side AI events per Phase 24).

Purpose: Powers RECOMMEND-06 telemetry + RECOMMEND-08 multi-surface render. Wave 3 — depends on all Wave 2 Edge Fns existing + Wave 1 schema. Plan 38-07 winback-scorer is in Wave 3 too but no file overlap.
Output: 2 migrations (cron + TAXO) + 1 helper extension + 2 React components + 1 unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-CONTEXT.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-RESEARCH.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-AI-SPEC.md
@supabase/functions/_shared/posthog-server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: pg_cron schedules — 4 jobs (digest hourly fan-out, embed nightly, winback nightly, softdelete cleanup daily)</name>
  <files>supabase/migrations/20270705000030_phase38_pg_cron_schedules.sql</files>
  <action>
Pre-check: `ls supabase/migrations/ | grep 20270705000030` empty (collision-check per memory).

Migration body using named dollar-quote tags per memory `reference_postgres_dollar_quote_nesting_in_cron_body` — outer `$cron$ ... $cron$`, inner `$digest$ ... $digest$` / `$embed$ ... $embed$` etc.:

```sql
-- weekly-digest-hourly-fanout (per-user timezone Sunday 09:00 local + 6h dedup)
SELECT cron.schedule(
  'phase38-weekly-digest-hourly-fanout',
  '0 * * * *',
  $cron$
  DO $digest$
  DECLARE
    rec RECORD;
    fn_url text := 'https://ytnsipxxmzgaebkqmokp.functions.supabase.co/weekly-digest';
    service_key text;
  BEGIN
    SELECT decrypted_secret INTO service_key
    FROM vault.decrypted_secrets
    WHERE name = 'service_role_key';

    FOR rec IN
      SELECT p.id AS user_id
      FROM public.profiles p
      WHERE EXISTS (
              SELECT 1 FROM public.user_preferences up
              WHERE up.user_id = p.id AND up.weekly_digest_opt_in = true
            )
        AND extract(dow FROM (now() AT TIME ZONE p.timezone)) = 0
        AND extract(hour FROM (now() AT TIME ZONE p.timezone)) = 9
        AND NOT EXISTS (
              SELECT 1 FROM public.weekly_digest_sends wds
              WHERE wds.user_id = p.id AND wds.sent_at > now() - interval '6 hours'
            )
    LOOP
      PERFORM net.http_post(
        url := fn_url,
        body := jsonb_build_object('user_id', rec.user_id),
        headers := jsonb_build_object(
          'Authorization', 'Bearer ' || service_key,
          'Content-Type', 'application/json'
        )
      );
    END LOOP;
  END;
  $digest$;
  $cron$
);
```

Add 3 more cron.schedule calls with parallel structure:
- `phase38-embed-content-nightly` cron='0 3 * * *' invoking `/embed-content-nightly`
- `phase38-winback-scorer-nightly` cron='0 4 * * *' invoking `/winback-scorer`
- `phase38-softdelete-cleanup-daily` cron='0 5 * * *' direct SQL `SELECT public.cleanup_content_embeddings_soft_deleted()` (no HTTP)

Each uses a UNIQUE inner dollar-quote tag (`$embed$`, `$winback$`, `$cleanup$`) per memory rule.

CRITICAL: replace `<project-ref>` with the actual Supabase project ref `ytnsipxxmzgaebkqmokp` (from MEMORY `reference_supabase_project`). Hardcode the URL — `current_setting('app.functions_url')` GUC does NOT exist on this project per memory `reference_supabase_pg_cron_vault_service_role_pattern`.

CRITICAL: vault.decrypted_secrets row `name='service_role_key'` MUST exist (Phase 25 populated). Verify via `supabase db query --linked "SELECT count(*) FROM vault.decrypted_secrets WHERE name='service_role_key'"` returns 1.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && supabase db push --linked 2>&1 | tee /tmp/p38-09.log; grep -E "Skipping|ERROR|syntax error" /tmp/p38-09.log && exit 1; supabase db query --linked "SELECT jobname FROM cron.job WHERE jobname LIKE 'phase38-%' ORDER BY jobname" | grep -c "phase38-" | grep -q "^4$"</automated>
  </verify>
  <done>All 4 cron jobs registered in cron.job table. `supabase db query --linked "SELECT cron.unschedule('phase38-noop')"` is a no-op (verifies cron extension functional). DST behavior validated by Plan 38-05 timezone.test.ts.</done>
</task>

<task type="auto">
  <name>Task 2: TAXO event registration migration + posthog-server extension</name>
  <files>supabase/migrations/20270705000031_phase38_taxo_events.sql, supabase/functions/_shared/posthog-server.ts</files>
  <action>
Migration `20270705000031_phase38_taxo_events.sql`:
- If Phase 24 ships a `taxo_events(event_name text PRIMARY KEY, category text, owner text, phi_sensitive boolean, created_at timestamptz)` registry table, INSERT 16 new event rows (idempotent ON CONFLICT DO NOTHING):
  - `recommendation.shown`, `recommendation.clicked`, `recommendation.dismissed`, `recommendation.404_on_click`, `recommendation.refusal_stripped`, `recommendation.embedding_fallback`
  - `digest.sent`, `digest.skipped_optout`, `digest.skipped_dedup`, `digest.validation_failed`, `digest.redflag_detected`, `digest.redflag_rewrite`, `digest.redflag_action_substitution`, `digest.clinical_action_leak`, `digest.baa_scope_resolved`, `digest.baa_scope_violation`
  - `win_back_trigger`, `winback.batch_complete`, `embed.batch_complete`, `embed.dedup_skip`, `embed.rate_limit_429`
  - `hitl_approve`, `hitl_reject`, `hitl_edit`
  - `plan_personalize.hint_returned`
  - `eval.judge.score`
  - `anthropic.prompt_cache.hit`

  All `phi_sensitive=false` (Phase 38 events are PHI-free by design per RECOMMEND-06 + D-04 sanitized narrative — narratives stay in DB, not in PostHog payloads).

- If TAXO registry table does NOT exist (Phase 24 not yet planned this way), create a comment-only migration listing the events and add them to `supabase/functions/_shared/posthog-events.ts` as a typed enum array.

Extend `supabase/functions/_shared/posthog-server.ts` (read first; do not overwrite existing exports):
- Add typed `Phase38Event` union type covering all event names above.
- Add typed payload interfaces for the highest-volume events (recommendation.shown, digest.sent) so TypeScript catches missing fields.
- Verify `client.shutdown()` is exposed; document at top of file that ALL Phase 38 Edge Fns MUST call `await posthog.shutdown()` before Response return per memory `feedback_planner_iter1_anti_patterns`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && supabase db push --linked 2>&1 | grep -E "Skipping|ERROR" && exit 1; npm run tsc --noEmit 2>&1 | grep -E "posthog-server\.ts|phase38" | head -5 || echo OK</automated>
  </verify>
  <done>TAXO migration applied; tsc passes for posthog-server.ts changes; 16+ event names registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ForYouCard (dashboard bento) + RelatedArticlesFooter (KB) — client-side render</name>
  <files>src/components/dashboard/cards/ForYouCard.tsx, src/components/dashboard/cards/ForYouCard.test.tsx, src/components/kb/RelatedArticlesFooter.tsx</files>
  <behavior>
    - Test 1: ForYouCard on mount calls `supabase.functions.invoke('recommend-next-best-action', {body: {user_id, surface: 'dashboard'}})`.
    - Test 2: Renders top-3 recommendations with title + deeplink + score badge.
    - Test 3: On error or 3s timeout, falls back to popular-content placeholder (D-02 graceful degradation).
    - Test 4: Clicks emit `recommendation.clicked` PostHog event via server-side capture (NOT client-side — Phase 24 rule). Use a small POST to a thin `/api/track-click` Edge Fn OR a server action; do NOT use posthog-js client.
    - Test 5: ForYouCard uses Card primitive `span={6}` per CLAUDE.md bento pattern.
    - Test 6: RelatedArticlesFooter accepts `currentKbId` prop, calls recommender with `surface: 'kb_footer', exclude_content_id: currentKbId`.
    - Test 7: Both components honor `useReducedMotion()` for any transitions.
    - Test 8: Lazy-loaded via React.lazy from App.tsx (bundle ceiling per memory `project_phase5_bundle_regression`).
  </behavior>
  <action>
Create `src/components/dashboard/cards/ForYouCard.tsx`:
- Uses Card primitive from `@/components/ui/Card.tsx` with `span={6}` + `variant='elevated'`.
- `useEffect` on mount: calls `supabase.functions.invoke('recommend-next-best-action', {body: {user_id, surface: 'dashboard'}})`.
- 3s timeout via AbortController; on timeout or error, set fallback state (loads 3 hardcoded popular KB slugs as v1 fallback while Edge Fn warms).
- Each rec renders as a Pill-style row with title + arrow icon; onClick fires `fetch('/api/track-rec-click', {method: 'POST', body: JSON.stringify({recommendation_id})})` — server-side click tracker.
- Skeleton state while loading (use existing `<Skeleton>` primitive).

**Track-click server endpoint:** Create a new Edge Fn `supabase/functions/track-rec-click/index.ts` (declared in this plan's `files_modified`) that accepts `{recommendation_id}` body, validates the row belongs to the caller via RLS, UPDATEs `recommendation_events.clicked_at = now()`, emits `recommendation.clicked` server-side (per Phase 24 taxonomy + posthog-server.ts capture), returns HTTP 204. Companion test `supabase/functions/track-rec-click/index.test.ts` covers: (a) authenticated caller updates own row, (b) cross-tenant attempt rejected by RLS, (c) PostHog event fired with `recommendation_id` property, (d) idempotent on repeat clicks (only first updates `clicked_at`).

Create `src/components/kb/RelatedArticlesFooter.tsx`:
- Standalone component rendered at bottom of KB article pages (assume KB page already exists; if not, this is a no-op until KB ships — vendor-gated pattern).
- Same recommender call but `surface: 'kb_footer'` + `exclude_content_id: currentKbId`.
- Renders as 3 cards or list items below article body.

Test file uses Vitest + React Testing Library + `vi.mock('@/lib/supabase')` for `functions.invoke`.

Lazy-load: in `src/App.tsx`, add `const ForYouCard = React.lazy(() => import('@/components/dashboard/cards/ForYouCard'))` if not present (Phase 5 bundle pattern). The HomeTab.tsx imports include this lazy component within Suspense.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && npm run test -- src/components/dashboard/cards/ForYouCard.test.tsx 2>&1 | tail -10; npm run lint 2>&1 | tail -5; npm run build 2>&1 | grep -E "ERROR|error" | head -3 || echo OK</automated>
  </verify>
  <done>ForYouCard renders top-3; falls back gracefully; clicks tracked server-side; bundle size delta <2 kB gz per memory bundle ceiling. RelatedArticlesFooter renders on KB pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron → vault.decrypted_secrets → Edge Fn | service_role bearer pulled at cron-tick time; Edge Fn auth-checks |
| Browser → /api/track-rec-click | user JWT; user_id implied from session; cross-user tampering prevented by RLS on recommendation_events |
| Browser → /recommend-next-best-action | user JWT crosses; RLS on returned content honored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-37 | Tampering | cron body $$ nesting collision | mitigate | Named dollar-tags per memory `reference_postgres_dollar_quote_nesting_in_cron_body` |
| T-38-38 | Information Disclosure | vault.decrypted_secrets exposed in cron logs | accept | Postgres redacts SECDEF function bodies in error logs; vault read happens in BEGIN block |
| T-38-39 | Tampering | client forges recommendation_id in track-click | mitigate | Server validates recommendation_id belongs to caller's user_id before UPDATE clicked_at |
| T-38-40 | Information Disclosure | client-side AI event capture (adblock leak) | mitigate | ALL Phase 38 capture is server-side per Phase 24 rule; client only POSTs to /api/track-rec-click |
</threat_model>

<verification>
After deploy: confirm cron.job table has 4 phase38-* rows. Confirm dashboard For You card renders within 800ms (latency budget). Confirm clicks emit recommendation.clicked event in PostHog within 5s.
</verification>

<success_criteria>
RECOMMEND-06 (CTR telemetry server-side) + RECOMMEND-08 (multi-surface render — dashboard + KB footer; community + course shims wait for M4/Phase 46) covered.
</success_criteria>

<output>
Create `.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-09-SUMMARY.md`.
</output>
