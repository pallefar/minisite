---
phase: 38-m5b-ai-recommender-pgvector-claude-digest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20270705000001_phase38_pgvector_extension.sql
  - supabase/migrations/20270705000002_phase38_content_embeddings_table.sql
  - supabase/migrations/20270705000003_phase38_content_embeddings_hnsw.sql
  - supabase/migrations/20270705000004_phase38_match_content_embeddings_fn.sql
  - supabase/migrations/20270705000005_phase38_recommendation_events.sql
  - supabase/migrations/20270705000006_phase38_weekly_digest_sends.sql
  - supabase/migrations/20270705000007_phase38_win_back_sends.sql
  - supabase/migrations/20270705000008_phase38_ai_suggestion_review.sql
  - supabase/migrations/20270705000009_phase38_profiles_timezone.sql
  - supabase/migrations/20270705000010_phase38_content_stale_trigger.sql
  - supabase/migrations/20270705000011_phase38_content_softdelete_cascade.sql
  - supabase/migrations/20270705000012_phase38_user_preferences_digest_optin.sql
  - supabase/functions/_shared/pgvector-smoke.test.ts
autonomous: true
requirements: [RECOMMEND-01, RECOMMEND-04, RECOMMEND-06, RECOMMEND-07, RECOMMEND-10]
must_haves:
  truths:
    - "pgvector extension enabled; content_embeddings table exists with vector(1536) column"
    - "HNSW index over vector_cosine_ops at m=16, ef_construction=64 exists"
    - "SECURITY INVOKER RPC match_content_embeddings accepts sources[] param (Phase 50 future-proof)"
    - "profiles.timezone column exists, NOT NULL DEFAULT 'America/New_York'"
    - "weekly_digest_sends + win_back_sends + ai_suggestion_review + recommendation_events tables exist with RLS"
    - "Cross-tenant SQL guard: RPC raises EXCEPTION when requesting_user_id IS NULL"
    - "Soft-delete trigger flips content_embeddings.stale=true on content.deleted_at; 7d cleanup honored"
  artifacts:
    - path: supabase/migrations/20270705000001_phase38_pgvector_extension.sql
      provides: "CREATE EXTENSION vector"
      contains: "CREATE EXTENSION IF NOT EXISTS vector"
    - path: supabase/migrations/20270705000002_phase38_content_embeddings_table.sql
      provides: "content_embeddings storage"
      contains: "embedding vector(1536)"
    - path: supabase/migrations/20270705000003_phase38_content_embeddings_hnsw.sql
      provides: "HNSW index for cosine"
      contains: "USING hnsw (embedding vector_cosine_ops)"
    - path: supabase/migrations/20270705000004_phase38_match_content_embeddings_fn.sql
      provides: "match_content_embeddings RPC with sources[] param"
      contains: "sources text[] DEFAULT ARRAY['content_embeddings']"
    - path: supabase/migrations/20270705000009_phase38_profiles_timezone.sql
      provides: "profiles.timezone column"
      contains: "ADD COLUMN timezone text NOT NULL DEFAULT 'America/New_York'"
  key_links:
    - from: supabase/migrations/20270705000004_phase38_match_content_embeddings_fn.sql
      to: supabase/migrations/20270705000003_phase38_content_embeddings_hnsw.sql
      via: "RPC ORDER BY ce.embedding <=> query_embedding uses HNSW"
      pattern: "embedding <=> query_embedding"
    - from: supabase/migrations/20270705000010_phase38_content_stale_trigger.sql
      to: supabase/migrations/20270705000002_phase38_content_embeddings_table.sql
      via: "AFTER UPDATE trigger on content flips stale=true"
      pattern: "stale\\s*=\\s*true"
---

<objective>
Land all Phase 38 schema: pgvector extension, content_embeddings + HNSW index, match_content_embeddings RPC (multi-source Phase 50 future-proof), recommendation_events, weekly_digest_sends, win_back_sends, ai_suggestion_review (HITL), profiles.timezone column, stale-on-update trigger, soft-delete 7d cascade, and user_preferences.weekly_digest_opt_in column.

Purpose: All other Phase 38 plans depend on these tables + RPC + timezone column. This is the foundation wave-1 plan; nothing else can run without it.
Output: 12 migrations in the 20270705* window + 1 Deno smoke test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-CONTEXT.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-RESEARCH.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-AI-SPEC.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md
@supabase/functions/_shared/anthropic-baa-allowlist.ts

<interfaces>
<!-- Multi-source RPC signature locked here for Phase 38 + Phase 50 callers -->

RPC contract (executed via supabase.rpc('match_content_embeddings', {...})):
```
match_content_embeddings(
  query_embedding vector(1536),
  match_count int DEFAULT 10,
  requesting_user_id uuid,                                  -- NOT NULL — raises EXCEPTION
  sources text[] DEFAULT ARRAY['content_embeddings'],       -- Phase 50 will pass ['content_embeddings','external_kb_embeddings']
  source_tier_weights jsonb DEFAULT '{}'                    -- Phase 50 reweighting hook
)
RETURNS TABLE (
  source_table text,
  content_id uuid,
  source_type text,
  title text,
  similarity float,
  weighted_score float
)
```

Migration timestamp window: 20270705000001..20270705000012 (latest existing on main = 20270704000025; verified 2026-05-20 by ls + memory `reference_migration_timestamp_collision_precheck`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migrations — extension, content_embeddings, HNSW, RPC, supporting tables</name>
  <files>supabase/migrations/20270705000001_phase38_pgvector_extension.sql, supabase/migrations/20270705000002_phase38_content_embeddings_table.sql, supabase/migrations/20270705000003_phase38_content_embeddings_hnsw.sql, supabase/migrations/20270705000004_phase38_match_content_embeddings_fn.sql, supabase/migrations/20270705000005_phase38_recommendation_events.sql, supabase/migrations/20270705000006_phase38_weekly_digest_sends.sql, supabase/migrations/20270705000007_phase38_win_back_sends.sql, supabase/migrations/20270705000008_phase38_ai_suggestion_review.sql</files>
  <action>
Pre-check: run `ls supabase/migrations/ | grep 20270705` — must be empty (memory `reference_migration_timestamp_collision_precheck`). If anything exists, halt and rename.

20270705000001 — `CREATE EXTENSION IF NOT EXISTS vector` (RECOMMEND-01).

20270705000002 — `content_embeddings` table per RESEARCH §Code Examples: columns `content_id uuid PK REFERENCES content(id) ON DELETE CASCADE`, `embedding vector(1536) NOT NULL`, `body_sha256 text NOT NULL`, `last_embedded_at timestamptz NOT NULL DEFAULT now()`, `stale boolean NOT NULL DEFAULT false`, `embedding_model_id text NOT NULL DEFAULT 'openai/text-embedding-3-small'`. Enable RLS; policy: super-admin SELECT/INSERT/UPDATE/DELETE; service_role bypass; consumer caller: SELECT only when joined `content` row passes visibility (no direct caller SELECT). NOTE: assumes `public.content` table exists from prior phases (KB articles + blog posts). If not yet, create a minimal `content(id uuid PK, kind text, title text, body_md text, published_at timestamptz, deleted_at timestamptz, visible_to_user_id uuid, org_id uuid, updated_at timestamptz)` here behind `CREATE TABLE IF NOT EXISTS` to unblock Phase 38; downstream phases own enrichment.

20270705000003 — `CREATE INDEX content_embeddings_hnsw_cos ON public.content_embeddings USING hnsw (embedding vector_cosine_ops) WITH (m = 16, ef_construction = 64)` per CONTEXT discretion + RESEARCH Pitfall #2.

20270705000004 — `match_content_embeddings` RPC per RESEARCH Pattern 2 (exactly that body): SECURITY INVOKER (NOT DEFINER per memory `reference_supabase_migration_gotchas`), `SET search_path = public, extensions`, raises EXCEPTION when `requesting_user_id IS NULL` (Guardrail O4), filters `c.deleted_at IS NULL AND ce.stale = false AND ce.last_embedded_at > now() - interval '30 days' AND (c.visible_to_user_id IS NULL OR c.visible_to_user_id = requesting_user_id)`. Accepts `sources text[] DEFAULT ARRAY['content_embeddings']` + `source_tier_weights jsonb DEFAULT '{}'` for Phase 50 (per D-29 of Phase 50). `GRANT EXECUTE ON FUNCTION public.match_content_embeddings(vector, int, uuid, text[], jsonb) TO authenticated, service_role`.

20270705000005 — `recommendation_events` (RECOMMEND-06): columns `id uuid PK DEFAULT gen_random_uuid()`, `recommendation_id uuid NOT NULL`, `user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`, `org_id uuid` (nullable), `source_type text NOT NULL`, `source_id uuid NOT NULL`, `action_id text` (whitelist enforced in Plan 38-02 via Zod + Postgres CHECK below), `surface_target text[] NOT NULL`, `score float NOT NULL`, `shown_at timestamptz NOT NULL DEFAULT now()`, `clicked_at timestamptz`, `dismissed_at timestamptz`, `expires_at timestamptz NOT NULL`. RLS: user can SELECT/INSERT their own row; service_role bypass; super-admin SELECT all. Indexes on `(user_id, shown_at DESC)` + `(recommendation_id)`.

20270705000006 — `weekly_digest_sends`: `id uuid PK DEFAULT gen_random_uuid()`, `user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`, `org_id uuid` (BAA-scope log per Dim 5), `sent_at timestamptz NOT NULL DEFAULT now()`, `narrative text NOT NULL`, `actions_jsonb jsonb NOT NULL DEFAULT '[]'::jsonb`, `status text NOT NULL CHECK (status IN ('pending_review','sent','failed','skipped_redflag','skipped_optout'))`, `model_id text NOT NULL`, `prompt_cache_hit boolean DEFAULT false`. RLS: user SELECT own; service_role bypass. Index `(user_id, sent_at DESC)`. PRIMARY KEY enforces audit/replay.

20270705000007 — `win_back_sends` (RECOMMEND-10, D-10 30d cap): `id uuid PK`, `user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`, `last_sent_at timestamptz NOT NULL DEFAULT now()`, `reason text NOT NULL DEFAULT 'inactive_14d'`, `save_engine_status text NOT NULL DEFAULT 'pending_handoff' CHECK (save_engine_status IN ('pending_handoff','handed_off','delivered','failed'))`. UNIQUE constraint on `(user_id)` is wrong — instead `UNIQUE (user_id, date_trunc('month', last_sent_at))` is also wrong; use partial unique: `CREATE UNIQUE INDEX win_back_sends_30d_cap ON public.win_back_sends (user_id) WHERE last_sent_at > now() - interval '30 days'` — actually `now()` is volatile in index predicate; instead the 30d cap is enforced in the winback-scorer Edge Fn via `NOT EXISTS (SELECT 1 FROM win_back_sends WHERE user_id = $1 AND last_sent_at > now() - interval '30 days')` and a plain `INDEX (user_id, last_sent_at DESC)` for the lookup. RLS: user SELECT own; service_role bypass.

20270705000008 — `ai_suggestion_review` (HITL queue, RECOMMEND-07 + D-12, D-13, D-14): `id uuid PK`, `type text NOT NULL CHECK (type IN ('recommender','digest','win_back'))`, `user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE`, `org_id uuid` (clinic-admin read-only via separate view, not this table), `payload jsonb NOT NULL`, `source_type text`, `action_id text`, `status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','approved','rejected','edited','auto_approved_kb'))`, `decided_by uuid REFERENCES auth.users(id)`, `decided_at timestamptz`, `decision_note text`, `created_at timestamptz NOT NULL DEFAULT now()`. RLS: super-admin only — `CREATE POLICY ai_suggestion_review_super_admin ON public.ai_suggestion_review FOR ALL TO authenticated USING ((SELECT app.is_super_admin(auth.uid())))` (assumes Phase 27/24 ships `app.is_super_admin(uuid)`; if not, gate with `auth.jwt() ->> 'role' = 'super_admin'`). Service_role bypass. Index `(status, created_at)`.

For all migrations: use named dollar-quote tags only if any cron body is embedded (none in this plan); pure DDL otherwise. Each migration single concern, idempotent where possible (`IF NOT EXISTS` on CREATE EXTENSION/TABLE/INDEX where the planner permits).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && supabase db reset --linked --debug 2>&1 | tail -5 || echo "Use staging push"; supabase db push --linked 2>&1 | tee /tmp/p38-01-push.log; grep -E "Skipping|ERROR" /tmp/p38-01-push.log && exit 1 || echo OK</automated>
  </verify>
  <done>All 8 migrations applied to linked Supabase (no Skipping, no error). `supabase db query --linked "SELECT to_regclass('public.content_embeddings'), to_regclass('public.recommendation_events'), to_regclass('public.weekly_digest_sends'), to_regclass('public.win_back_sends'), to_regclass('public.ai_suggestion_review')"` returns 5 non-NULL regclass values. `supabase db query --linked "SELECT proname FROM pg_proc WHERE proname='match_content_embeddings'"` returns 1 row.</done>
</task>

<task type="auto">
  <name>Task 2: Profiles.timezone column + user_preferences.weekly_digest_opt_in + stale-on-update trigger + soft-delete 7d cascade</name>
  <files>supabase/migrations/20270705000009_phase38_profiles_timezone.sql, supabase/migrations/20270705000010_phase38_content_stale_trigger.sql, supabase/migrations/20270705000011_phase38_content_softdelete_cascade.sql, supabase/migrations/20270705000012_phase38_user_preferences_digest_optin.sql</files>
  <action>
20270705000009 (per D-06, RESEARCH §Environment Availability): `ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS timezone text NOT NULL DEFAULT 'America/New_York'`. Also `ADD CONSTRAINT profiles_timezone_iana CHECK (timezone ~ '^[A-Za-z_]+/[A-Za-z_]+(/[A-Za-z_]+)?$')` to reject offset strings (RESEARCH Pitfall #7 — must be IANA name not offset). Backfill no-op (default fills NULLs).

20270705000010 (D-18 re-embed on content edit): `CREATE OR REPLACE FUNCTION public.tg_content_mark_embedding_stale() RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path=public,extensions AS $fn$ BEGIN IF NEW.body_md IS DISTINCT FROM OLD.body_md OR NEW.title IS DISTINCT FROM OLD.title THEN UPDATE public.content_embeddings SET stale = true WHERE content_id = NEW.id; END IF; RETURN NEW; END; $fn$;` then `CREATE TRIGGER content_stale_on_update AFTER UPDATE ON public.content FOR EACH ROW EXECUTE FUNCTION public.tg_content_mark_embedding_stale();`. Per memory `reference_supabase_migration_gotchas`, SECDEF requires explicit `search_path`.

20270705000011 (D-19 soft-delete 7d cascade): `CREATE OR REPLACE FUNCTION public.cleanup_content_embeddings_soft_deleted() RETURNS void LANGUAGE sql SECURITY DEFINER SET search_path=public,extensions AS $fn$ DELETE FROM public.content_embeddings ce WHERE EXISTS (SELECT 1 FROM public.content c WHERE c.id = ce.content_id AND c.deleted_at IS NOT NULL AND c.deleted_at < now() - interval '7 days'); $fn$;` — scheduled in Plan 38-09 pg_cron migration. NO trigger here (cleanup is cron-only — embedding row stays for 7 days audit window per D-19).

20270705000012 — `ALTER TABLE public.user_preferences ADD COLUMN IF NOT EXISTS weekly_digest_opt_in boolean NOT NULL DEFAULT false` (D-04: opt-IN default). If `user_preferences` table doesn't exist, `CREATE TABLE IF NOT EXISTS public.user_preferences (user_id uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE, weekly_digest_opt_in boolean NOT NULL DEFAULT false, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now())` with RLS: user can SELECT/UPDATE own row only.

For Threat T-38-01 (BAA-scope): the credential routing guard lives in code (Plan 38-02 `_shared/baa-scope.ts`); DB layer here just provides the data (`profiles.org_id` already exists in Phase 28; if not, add column under `IF NOT EXISTS`).
  </action>
  <verify>
    <automated>supabase db push --linked 2>&1 | tee /tmp/p38-01b.log; grep -E "Skipping|ERROR" /tmp/p38-01b.log && exit 1; supabase db query --linked "SELECT column_name, data_type, column_default FROM information_schema.columns WHERE table_schema='public' AND table_name='profiles' AND column_name='timezone'" | grep -q 'America/New_York'</automated>
  </verify>
  <done>profiles.timezone exists with IANA CHECK; user_preferences.weekly_digest_opt_in exists default false; tg_content_mark_embedding_stale trigger fires on content UPDATE (verified via `supabase db query --linked "SELECT tgname FROM pg_trigger WHERE tgname='content_stale_on_update'"`); cleanup_content_embeddings_soft_deleted function exists.</done>
</task>

<task type="auto">
  <name>Task 3: pgvector smoke test — Deno test for HNSW + cosine operator</name>
  <files>supabase/functions/_shared/pgvector-smoke.test.ts</files>
  <action>
Create Deno test that (1) inserts 3 known vectors into content_embeddings via service-role client, (2) calls `match_content_embeddings(query_embedding, 10, test_user_id, ARRAY['content_embeddings'])` RPC, (3) asserts results returned ascending by distance (cosine), (4) asserts `requesting_user_id := null` raises EXCEPTION (Guardrail O4), (5) verifies `similarity = 1 - distance`, (6) cleans up. Use file-scoped TEST_SLUG_PREFIX per memory `feedback_rls_per_file_slug_prefix`. Auth: service-role from env; do NOT use signInWithPassword (memory `reference_rls_fixture_gotrueclient_flake`).

Tests are RED at this point — verifies the migrations applied correctly. Run via `deno test supabase/functions/_shared/pgvector-smoke.test.ts --allow-env --allow-net`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && deno test supabase/functions/_shared/pgvector-smoke.test.ts --allow-env --allow-net 2>&1 | tail -10</automated>
  </verify>
  <done>Smoke test passes against linked Supabase: HNSW index used (verified via `EXPLAIN`), cosine ordering correct, cross-tenant guard fires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (Edge Fn) | User JWT crosses; RLS enforces user_id scope downstream |
| pg_cron → Edge Fn | service_role from vault.decrypted_secrets crosses; subsequent Edge Fn must re-impose user/org scope |
| Edge Fn → DB (RPC) | requesting_user_id parameter is the belt-and-suspenders RLS-equivalent guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information Disclosure | match_content_embeddings RPC | mitigate | RAISE EXCEPTION when requesting_user_id IS NULL; SECURITY INVOKER honors caller JWT; `(visible_to_user_id IS NULL OR = requesting_user_id)` filter |
| T-38-02 | Information Disclosure | content_embeddings table | mitigate | RLS enabled; super-admin policy + service_role bypass; no direct caller SELECT |
| T-38-03 | Tampering | content_stale_on_update trigger | mitigate | SECURITY DEFINER + explicit `search_path = public, extensions` per memory `reference_supabase_migration_gotchas` |
| T-38-04 | Elevation of Privilege | ai_suggestion_review HITL queue | mitigate | RLS super-admin only (D-14); service_role bypass; clinic-admin access denied by absence of policy |
| T-38-05 | Repudiation | weekly_digest_sends audit | mitigate | append-only; PRIMARY KEY (id); index (user_id, sent_at DESC); 6h dedup window enforced by cron caller |
| T-38-06 | Denial of Service | content_embeddings runaway | accept | Cost cap enforced by `body_sha256` dedup (Plan 38-04); no DB-level mitigation needed |
</threat_model>

<verification>
After migrations: `supabase db query --linked "SELECT count(*) FROM pg_extension WHERE extname='vector'"` returns 1. Cross-tenant guard verified by Plan 38-03 RLS impersonation test (`tests/rls/recommender-cross-tenant.spec.ts`).
</verification>

<success_criteria>
All 12 migrations applied with zero `Skipping` lines. pgvector smoke test passes. `profiles.timezone` populated. `match_content_embeddings` RPC callable from service-role with `sources[]` parameter.
</success_criteria>

<output>
After completion, create `.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-01-SUMMARY.md` (use absolute project-subdir path per memory `feedback_parallel_executor_autonomy_drift`).
</output>
