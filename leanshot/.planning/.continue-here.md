# Continue Here — LeanShot launch-readiness (paused 2026-06-01)

Paused at ~25% context. **Source of truth: `leanshot/.planning/LAUNCH-READINESS-2026-05-31.md`** (committed). Memory `reference_leanshot_deployed_app_runtime_state.md` is fully current.

## ✅ Shipped & LIVE (verified on https://app.leanshot.app)
- PRs **#14, #18, #20** squash-merged to `main`; **#19 closed** (superseded by #20).
  - #20 = branch `chore/launch-readiness` (all-inclusive): button-bg fix, `--color-text-tertiary` AA darken, `auth.ts` SSR redirect → app.leanshot.app, `vercel.json` CSP (`manifest-src`/`img-src https://*.supabase.co`/`frame-ancestors`), `index.html` font `media="all"`, + 6 UI bug fixes (Nutrition water-grid cap, nav active-state, white-on-white tokens, LegalFooter sidebar offset, Body weight-card contrast, Settings 406→`maybeSingle`).
- `leanshot-app` auto-deployed + **verified live** (build `index-BHjdhKY6.js`; CSP `manifest-src` + `img-src supabase.co` present; font `media="all"`, 0 leftover onload).
- Vercel: **`VITE_SPA_URL` = https://app.leanshot.app** set on `leanshot-marketing` (prod/preview/dev) via CLI → **NEEDS marketing redeploy** to bake in.
- **Supabase MCP connected** (`plugin:supabase:supabase`, OAuth) — durable for SQL/migrations/edge-fns. Has **NO Auth-config write tool**.
- Admin confirmed: **`karsten.haldan@gmail.com` = superadmin** (the operator's account).

## ⏭️ OPEN — next session (priority order)
1. **Supabase Site URL** (fixes post-login redirect AND magic-link domain): Dashboard → Auth → URL Configuration → Site URL = `https://app.leanshot.app`; redirect allowlist += `https://app.leanshot.app/**`, `.../auth/callback`; remove `leanshot-app.vercel.app`. *(MCP can't set it — dashboard or Mgmt-API PAT.)*
2. **Vercel**: set `app.leanshot.app` as **primary** on `leanshot-app`; **redeploy `leanshot-marketing`**.
3. **PURGE `@leanshot.test` accounts** (3822 total / **288 with admin_role**) from prod — **BLOCKED**: deleting `auth.users` cascades `SET user_id=NULL` onto append-only `freeze_tokens_ledger` whose `deny_update` trigger forbids it → whole delete rolls back (**nothing deleted, DB unchanged**). Fix: delete those users' `freeze_tokens_ledger` rows first (verify DELETE allowed — trigger only blocks UPDATE) or alter the FK, then scoped delete (`email like '%@leanshot.test' and admin_role is not null`); **verify `karsten.haldan@gmail.com` survives.**
4. **Google/Facebook OAuth**: app wires **Apple only** (`signInWithOAuthProvider('apple')`/`isAppleEnabled` in SignIn/SignUpForm). Needs new buttons (code) + external OAuth apps (Google Cloud/Facebook/Apple consoles + secrets) + Supabase provider enable. Callback: `https://ytnsipxxmzgaebkqmokp.supabase.co/auth/v1/callback`.
5. **Edge-Fn CORS** (`traffic-attribution-recorder`: wildcard ACAO + credentials → blocked) redeploy; confirm **MFA TOTP** enabled.

## ⚠️ Gotchas
- Branch `chore/launch-readiness` is merged but based off PR #19's branch (dup commits reconciled at merge).
- **Shared checkout**: verify `git status -sb` before any commit/push; stage by explicit path.
- Supabase MCP OAuth state expires fast — complete the callback URL immediately.
- `supabase` CLI NOT installed in-session; local `.env.local` has only anon keys (no service-role).
