# Continue Here — LeanShot launch-readiness (updated 2026-06-01 PM)

Source of truth: `LAUNCH-READINESS-2026-05-31.md`. This session worked the 5 open
items in order. **All code/in-repo work that can be done without console/operator
access is DONE + validated (tsc + tests + lint green).** What remains is operator
console actions + one held destructive step.

## ⚠️ UNCOMMITTED working-tree changes (branch `chore/launch-readiness`)
Made this session, NOT yet committed (verify `git status -sb` before committing —
shared-checkout hazard; stage by explicit path):
- `supabase/config.toml` — site_url→app.leanshot.app, redirect allowlist, MFA TOTP enroll/verify=true.
- `supabase/functions/traffic-attribution-recorder/index.ts` — credentialed CORS (origin-echo + Allow-Credentials).
- `src/lib/auth.ts` — isGoogleEnabled()/isFacebookEnabled() + 'facebook' in provider union.
- `src/components/auth/SignInForm.tsx`, `SignUpForm.tsx` — gated Google+Facebook buttons.
- `src/components/onboarding/ConsumerOnboardingRenderer.tsx` — gated Facebook button.
- `src/lib/analytics/events.ts` — auth_provider enum += 'facebook'.
- `public/locales/{en,es}/onboarding.json` — continue_facebook label.
- 3 test files (mocks + new Google/Facebook tests).
- `.planning/PURGE-LEANSHOT-TEST-VALIDATED.md` (new), this file.

## Item-by-item state
1. **Supabase Site URL/allowlist + MFA** — config.toml aligned (in-repo ✅).
   OPERATOR (no Mgmt-API token / MCP has no auth-config tool): Dashboard → Auth →
   URL Configuration: Site URL=`https://app.leanshot.app`; redirect allowlist per
   readiness §3-A; remove `leanshot-app.vercel.app`. Confirm MFA TOTP enroll+verify
   ENABLED (else admins lock out of AAL2 gate).
2. **Vercel marketing redeploy — ✅ DONE + VERIFIED.** `vercel redeploy` (server-side
   rebuild picks up the new `VITE_SPA_URL`); live bundle `marketing-tGKD7NSO.js` now
   contains ONLY `app.leanshot.app`. Optional operator: set app.leanshot.app primary
   on leanshot-app + drop the `.vercel.app` alias (PR #20 vercel.json host-redirect
   already covers the redirect).
3. **Purge @leanshot.test — VALIDATED but HELD** (user 2026-06-01: "don't delete yet").
   Full plan + proof in `PURGE-LEANSHOT-TEST-VALIDATED.md`. ⚠️ The original handoff
   recipe was WRONG (missed `freeze_tokens_ledger_no_delete` trigger + 2 RESTRICT FKs
   + 2998 orgs). New plan is dry-run-proven; run BATCHED (single txn times out). To
   execute: follow the batched steps in that doc via MCP execute_sql.
4. **Google/Facebook OAuth — CODE DONE + validated.** Buttons gated, dormant until
   OPERATOR: create Google Cloud OAuth + Facebook app, register callback
   `https://ytnsipxxmzgaebkqmokp.supabase.co/auth/v1/callback`, enable providers in
   Supabase dashboard, then set Vercel env `VITE_AUTH_GOOGLE_ENABLED=true` +
   `VITE_AUTH_FACEBOOK_ENABLED=true` (on leanshot-app) and redeploy.
5. **Edge-Fn CORS — CODE DONE.** OPERATOR/deploy: `supabase functions deploy
   traffic-attribution-recorder` (needs supabase CLI + login or SUPABASE_ACCESS_TOKEN
   — none in-session; MCP deploy infeasible due to deep `_shared` deps). Plus MFA
   confirm (same as item 1).

## Capability facts (this session)
- Supabase MCP (`plugin:supabase:supabase`) connected, role `postgres` (owns public
  tables → can DISABLE TRIGGER; NOT superuser → no session_replication_role). No
  auth-config write tool, no Mgmt-API PAT, no service-role key, no supabase CLI.
- Edge-fn source + config.toml live at repo ROOT `/Users/karstenhaldan/minisite/supabase/`
  (NOT under `leanshot/`).
- `vercel` CLI authed; local `.vercel` links to **leanshot-marketing**.
