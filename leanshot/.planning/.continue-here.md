# Continue Here — LeanShot (updated 2026-06-01, end of session)

**All code/in-repo work is DONE, merged to `main`, and documented.** What remains is
operator console/device work + working the launch gate. Resume with `/gsd-progress`
(routes to Phase 70). This file is the index; the runbooks below have the detail.

## Branch / git state
- **`main` = `44ff56c0`** — PRs **#20** (launch UI) · **#21** (OAuth/CORS/config) · **#22**
  (error boundary + a11y/observability quick wins) · **#23** (Android Play-upload CI fixes) all merged.
- **`chore/launch-readiness`** holds the committed `.planning` docs but is **STALE vs main**
  (predates #14/#18/#9). **Do NOT PR/merge that branch** — it's planning-docs only. For any
  new code, branch off `origin/main` (worktree + cherry-pick pattern).

## Done this session
v1.4 launch items 1-5 (config/CORS/OAuth code merged; marketing redeployed live) · **`@leanshot.test`
purge executed + verified** (3869 users + 3036 orgs deleted, 0 remain, operator safe) · best-in-class
audit + v1.5 scaffold · error boundary (the #1 audit finding) · Android CI fixes · Supabase CLI
installed + logged in.

## Committed planning docs (read these to resume)
- `OPERATOR-RUNBOOK-2026-06-01.md` — step-by-step for the 4 operator items.
- `PHASE-70-SIGNOFF-CHECKLIST.md` — tickable launch gate (94 signals / 66 critical / 75 pending).
- `APP-STORE-FIRST-TEST-BUILD-2026-06-01.md` — first test build → Play internal + TestFlight.
- `BEST-IN-CLASS-AUDIT-2026-06-01.md` + `V1.5-BEST-IN-CLASS-MILESTONE.md` — post-launch roadmap (8 phases / 52 reqs).

## OPEN — next session (all operator/console/device; I can't do these from here)
1. **Supabase Auth — MOSTLY DONE 2026-06-01** (applied via Management-API `PATCH` using a temp PAT,
   confirmed via `GET`): Site URL = `https://app.leanshot.app` ✅ · redirect allowlist set (stale
   `leanshot-app.vercel.app` removed) ✅ · **MFA TOTP enroll/verify = true** ✅. **STILL OPEN:**
   (a) **leaked-password protection** is **Pro-plan-only** (API returned 402) → upgrade project to
   Pro, then `password_hibp_enabled:true` (dashboard Attack Protection or the PATCH); (b) **enroll a
   TOTP factor on `karsten.haldan@gmail.com`** in-app (admin AAL2 gate or you lock out). The temp PAT
   should be revoked (Dashboard → Account → Access Tokens). To re-PATCH: `OPERATOR-RUNBOOK §1`.
2. **Edge-fn CORS deploy** — `supabase functions deploy traffic-attribution-recorder --no-verify-jwt
   --project-ref ytnsipxxmzgaebkqmokp` (CLI installed + logged in). `OPERATOR-RUNBOOK §3`.
3. **App-store first test build** — **Android reachable now** (create Play app, mint keystore, build
   AAB, upload to internal track; CI fixes #23 merged + needs `KEYSTORE_BASE64`/`PLAY_SERVICE_ACCOUNT_JSON`
   secrets). **iOS needs signing** (no DEVELOPMENT_TEAM in pbxproj + Automatic-vs-`match` mismatch +
   unseeded match repo `pallefar/leanshot-fastlane-match`; + same Gemfile fix in `mobile-ios.yml`).
   `APP-STORE-FIRST-TEST-BUILD`.
4. **Phase 70 UAT gate** — work `PHASE-70-SIGNOFF-CHECKLIST.md`; plan 08 issues go/no-go + tags
   `v1.4.0-ship`. Items 1-3 clear the vendor-oauth-secrets + browser groups.
5. **v1.5 best-in-class** — `/gsd-new-milestone v1.5` to formalize the roadmap (Phase 72 error-boundary
   starters already merged via #22).

## Capability facts (carry forward)
- Supabase MCP (`plugin:supabase:supabase`) = role `postgres` (owns public tables → DISABLE TRIGGER ok;
  NOT superuser; **no auth-config write tool**). No service-role / no Mgmt-API PAT in session.
- `supabase` CLI installed (2.103.0) + logged in; token in macOS Keychain (NOT extractable for Mgmt API).
- `vercel` CLI authed; `main` auto-deploys `leanshot-app`. Project ref `ytnsipxxmzgaebkqmokp`;
  OAuth callback `https://ytnsipxxmzgaebkqmokp.supabase.co/auth/v1/callback`; Apple Team ID `XCZMRC727Z`.
- Shared checkout: verify `git status -sb` before commit; CI Format check is red on `main`
  (NutritionTab prettier) + full Unit suite is flaky → merge own-green PRs via `--admin` over those.
</content>
