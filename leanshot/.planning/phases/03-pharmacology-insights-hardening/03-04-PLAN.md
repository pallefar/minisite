---
phase: 03-pharmacology-insights-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/components/dashboard/modals/DoctorReport.tsx
  - src/components/dashboard/modals/DoctorReport.test.tsx
  - .planning/phases/02-visible-compliance-public-deploy/02-06-SUMMARY.md
  - .planning/phases/02-visible-compliance-public-deploy/02-HUMAN-UAT.md
autonomous: true
requirements:
  - PK-04
user_setup: []
tags:
  - doctor-report
  - pdf
  - disclaimer
  - rtl

must_haves:
  truths:
    - "DoctorReport modal renders a PK-specific disclaimer aside between the patient-name header and the Summary section on every open — discharging CONTEXT D-10 (DoctorReport PDF must include the same watermark text)"
    - "Disclaimer aside contains the verbatim PK_DISCLAIMER_DOCTOR_REPORT string from src/lib/disclaimers.ts"
    - "The aside survives window.print() — it carries print: utilities so it remains visible in the PDF output"
    - "Phase 2 cross-reference docs (02-06-SUMMARY.md and 02-HUMAN-UAT.md C10/C11) quote the new Phase 3 watermark string with a 'superseded by Phase 3 D-08' note (discharges D-09)"
    - "Vitest + RTL test asserts the disclaimer string is present in DoctorReport rendered output"
  artifacts:
    - path: "src/components/dashboard/modals/DoctorReport.tsx"
      provides: "PK-specific disclaimer aside inserted between header and Summary section (implements CONTEXT D-10)"
      contains: "PK_DISCLAIMER_DOCTOR_REPORT"
    - path: "src/components/dashboard/modals/DoctorReport.test.tsx"
      provides: "RTL test asserting disclaimer string presence in rendered output"
      min_lines: 30
    - path: ".planning/phases/02-visible-compliance-public-deploy/02-06-SUMMARY.md"
      provides: "Updated watermark text reference with Phase 3 supersession note (discharges D-09)"
    - path: ".planning/phases/02-visible-compliance-public-deploy/02-HUMAN-UAT.md"
      provides: "C10/C11 expected text updated to Phase 3 string (discharges D-09)"
  key_links:
    - from: "src/components/dashboard/modals/DoctorReport.tsx"
      to: "src/lib/disclaimers.ts"
      via: "import { PK_DISCLAIMER_DOCTOR_REPORT } from '@/lib/disclaimers'"
      pattern: "PK_DISCLAIMER_DOCTOR_REPORT"
---

<objective>
Implement **CONTEXT D-10** — `DoctorReport.tsx` PDF must include the same watermark text as the chart — by adding a PK-specific disclaimer aside to the DoctorReport (HTML-via-window.print). Wire an RTL test that fails if the disclaimer is removed, and update Phase 2's cross-reference docs to quote the new Phase 3 watermark string per **CONTEXT D-09**.

Purpose: The doctor receiving a printed/saved PDF report sees the PK disclaimer alongside the dose/injection summary — preventing them from mistaking the modeled curve for measured serum levels. Implements PK-04 (PDF side) AND discharges CONTEXT D-10 (DoctorReport PDF watermark parity) AND CONTEXT D-09 (Phase 2 cross-reference docs).

Output: One source file modified, one new test file, two planning docs updated.

**Critical structural finding (RESEARCH §DoctorReport, addresses D-10's implementation-path uncertainty):** `DoctorReport.tsx` does NOT embed the MedLevelChart canvas. It is a Modal of HTML tables that `window.print()` renders to PDF. The chart watermark plugin (Plan 03) does NOT survive print of this modal. D-10 says the report "must include the same watermark text" — researcher confirmed the implementation path is the **footer/aside HTML disclaimer string** (the second of D-10's two branches), NOT canvas re-rendering. Plan 04 Task 1 adds a parallel HTML disclaimer; do NOT attempt to render the chart canvas into the report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pharmacology-insights-hardening/03-CONTEXT.md
@.planning/phases/03-pharmacology-insights-hardening/03-RESEARCH.md
@.planning/phases/03-pharmacology-insights-hardening/03-PATTERNS.md
@.planning/phases/03-pharmacology-insights-hardening/03-UI-SPEC.md
@.planning/phases/03-pharmacology-insights-hardening/03-01-SUMMARY.md

<interfaces>
<!-- Contract from Plan 01 -->
```typescript
// src/lib/disclaimers.ts
export const PK_DISCLAIMER_DOCTOR_REPORT: string;
// = 'Drug-level curve: estimate, not measured serum level — based on population pharmacokinetics. Shows modeled mean with shaded inter-individual variability band (~30%).'
// (em-dash U+2014; tilde ASCII U+007E)
```

<!-- Existing DoctorReport.tsx surface (preserved) -->
The Modal opens with `<div className="space-y-6 leading-relaxed">` containing `<header>` (patient name + generation date) at line 43-48, then a `<section className="rounded-2xl bg-[var(--color-surface-elevated)] border border-[var(--color-border)] p-4">` Summary at line 50. The existing generic footer `Generated by LeanShot. This is a tracking summary…` lives at line 197 inside `<p className="text-[11px] text-[var(--color-text-tertiary)] italic pt-4 border-t border-[var(--color-border)]">`.

The new PK disclaimer aside is inserted BETWEEN line 48 (`</header>`) and line 50 (`<section>`), at the TOP of the report so a doctor scrolling or reading the printed page encounters it before the dose numbers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert PK-specific disclaimer aside into DoctorReport.tsx (implements CONTEXT D-10)</name>
  <files>src/components/dashboard/modals/DoctorReport.tsx</files>
  <read_first>
    - src/components/dashboard/modals/DoctorReport.tsx entire file (~200 lines)
    - src/lib/disclaimers.ts (built by Plan 01)
    - .planning/phases/03-pharmacology-insights-hardening/03-RESEARCH.md lines 651-693 (DoctorReport integration + verbatim copy)
    - .planning/phases/03-pharmacology-insights-hardening/03-UI-SPEC.md line 104 (verbatim PDF footer copy)
    - .planning/phases/03-pharmacology-insights-hardening/03-CONTEXT.md D-10 ("`DoctorReport.tsx` PDF must include the same watermark text")
    - .planning/phases/03-pharmacology-insights-hardening/03-PATTERNS.md §`DoctorReport.tsx`
  </read_first>
  <behavior>
    - On every render of DoctorReport, an `<aside role="note">` element appears in the document body BETWEEN the `<header>` (patient name) and the first `<section>` (Summary) — this is the **PDF-side implementation of CONTEXT D-10's "must include the same watermark text"** requirement
    - The aside contains a `<strong>` element with text `Pharmacokinetic estimate:` and the rest of the text is `PK_DISCLAIMER_DOCTOR_REPORT` imported from `@/lib/disclaimers`
    - The aside has Tailwind classes including `rounded-2xl`, `border`, `border-[var(--color-border)]`, `bg-[var(--color-surface-elevated)]`, `p-3`, `text-[12px]`, `italic`, `text-[var(--color-text-secondary)]`, `print:border-black` — surviving print rendering
    - The existing generic footer at line 197 is unchanged
    - The existing Summary `<section>` and all downstream sections are unchanged
  </behavior>
  <action>
    **Rationale (CONTEXT D-10 traceability):** CONTEXT D-10 states verbatim: "`DoctorReport.tsx` PDF must include the same watermark text — implementation depends on whether `DoctorReport` renders the chart canvas directly into the PDF (in which case the canvas plugin's watermark survives automatically) or builds a separate PDF chart (in which case a footer-style disclaimer string is added). Phase researcher should read `DoctorReport.tsx` to determine which." The researcher resolved that ambiguity: `DoctorReport` does NOT embed the MedLevelChart canvas (RESEARCH §DoctorReport), so the canvas plugin's watermark does NOT survive print. This task implements D-10's second branch — the footer-style HTML disclaimer string, rendered as an `<aside>` so it appears at the TOP of the PDF (before the dose tables) where a doctor reads it first.

    Implementation steps:
    (1) Add an import: `import { PK_DISCLAIMER_DOCTOR_REPORT } from '@/lib/disclaimers';` — alphabetize relative to existing imports per the project's eslint-plugin-import-x order.
    (2) Locate the `</header>` closing tag on line 48 and the `<section className="rounded-2xl ...">` opening tag on line 50 (the Summary block).
    (3) Insert the following JSX BETWEEN line 48 and line 50:
    `<aside role="note" className="rounded-2xl border border-[var(--color-border)] bg-[var(--color-surface-elevated)] p-3 text-[12px] italic text-[var(--color-text-secondary)] print:border-black"><strong className="not-italic font-semibold">Pharmacokinetic estimate:</strong>{' '}{PK_DISCLAIMER_DOCTOR_REPORT}</aside>`
    Add a JSX comment immediately above the aside: `{/* CONTEXT D-10: DoctorReport PDF watermark parity — see .planning/phases/03-pharmacology-insights-hardening/03-CONTEXT.md */}`
    (4) Do NOT modify the existing footer paragraph at line 197 — it stays as the generic medical disclaimer. The new aside is ADDITIVE and PK-specific.
    (5) No new state, no new hooks, no new effects — the component remains a pure render of store data.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "import { PK_DISCLAIMER_DOCTOR_REPORT } from '@/lib/disclaimers'" src/components/dashboard/modals/DoctorReport.tsx` matches
    - `grep -F "<aside role=\"note\"" src/components/dashboard/modals/DoctorReport.tsx` matches exactly 1 time
    - `grep -F "Pharmacokinetic estimate:" src/components/dashboard/modals/DoctorReport.tsx` matches
    - `grep -F "CONTEXT D-10" src/components/dashboard/modals/DoctorReport.tsx` matches at least 1 time (traceability comment per D-10)
    - `grep -F "print:border-black" src/components/dashboard/modals/DoctorReport.tsx` matches
    - `grep -F "{PK_DISCLAIMER_DOCTOR_REPORT}" src/components/dashboard/modals/DoctorReport.tsx` matches
    - `grep -c "Generated by LeanShot" src/components/dashboard/modals/DoctorReport.tsx` returns 1 (existing footer preserved)
    - `npx tsc -p tsconfig.app.json --noEmit` exits 0
    - `npm run lint -- src/components/dashboard/modals/DoctorReport.tsx` exits 0
  </acceptance_criteria>
  <done>
    PK disclaimer aside is rendered between header and Summary (discharges CONTEXT D-10); existing generic footer preserved; build + lint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DoctorReport.test.tsx — RTL test asserting disclaimer presence on render</name>
  <files>src/components/dashboard/modals/DoctorReport.test.tsx</files>
  <read_first>
    - src/components/dashboard/modals/DoctorReport.tsx (post-Task-1 state)
    - src/lib/disclaimers.ts (provides PK_DISCLAIMER_DOCTOR_REPORT)
    - src/lib/store.ts lines 60-110 (initialState shape needed to seed a user before render)
    - src/lib/storage.ts (initialState export — minimum user fixture)
    - any existing component test in src/components (canonical RTL pattern in this codebase — if none, follow Vitest + @testing-library/react idiom from `src/test-setup.ts`)
  </read_first>
  <behavior>
    - `npm run test -- --run src/components/dashboard/modals/DoctorReport.test.tsx` exits 0
    - At least 2 `it` cases run: (a) renders aside with `PK_DISCLAIMER_DOCTOR_REPORT` text in the document; (b) `Pharmacokinetic estimate:` label visible
    - Test seeds `useStore.setState({ user: minimalUser, injections: [] })` before render so the component does not crash on missing user
    - Test uses `@testing-library/react` `render` + `screen.getByText` patterns; cleans up via `afterEach(() => { useStore.setState(initialState); })`
  </behavior>
  <action>
    Create `src/components/dashboard/modals/DoctorReport.test.tsx`. Imports: `import { afterEach, describe, expect, it } from 'vitest';`, `import { render, screen } from '@testing-library/react';`, `import { DoctorReport } from './DoctorReport';`, `import { useStore } from '@/lib/store';`, `import { initialState } from '@/lib/storage';`, `import { PK_DISCLAIMER_DOCTOR_REPORT } from '@/lib/disclaimers';`. Declare a minimal user fixture: `const FIXTURE_USER = { name: 'Test Patient', medication: 'wegovy' as const, dose: '1.0', doseUnit: 'mg' as const, startDate: '2026-01-01', startWeight: 100, units: 'metric' as const, sex: 'female' as const, ageBracket: '30-39' as const, height: 170, goalType: 'weight-loss' as const, activityLevel: 'moderate' as const, liftingLevel: 'novice' as const, proteinTarget: 100, waterTarget: 8, injectionDay: 1, supplements: [] };` — match the actual `User` interface from `@/types`. If the User interface has additional required fields, add them with sensible defaults; the test only needs the component to render without crashing. Use `afterEach(() => { useStore.setState(initialState); });` to reset the store between tests. Add a `describe('DoctorReport — Phase 3 PK disclaimer (PK-04 + D-10)', () => { ... })` block with at least two `it` cases: `it('renders the PK_DISCLAIMER_DOCTOR_REPORT verbatim', () => { useStore.setState({ ...initialState, user: FIXTURE_USER }); render(<DoctorReport open onClose={() => {}} />); expect(screen.getByText(PK_DISCLAIMER_DOCTOR_REPORT, { exact: false })).toBeTruthy(); });` and `it('renders the Pharmacokinetic estimate: label', () => { useStore.setState({ ...initialState, user: FIXTURE_USER }); render(<DoctorReport open onClose={() => {}} />); expect(screen.getByText('Pharmacokinetic estimate:')).toBeTruthy(); });`. If `DoctorReport`'s props differ from `open` + `onClose`, inspect the component signature first and adjust the JSX accordingly — the goal is to render the modal contents. NOTE: if RTL is not yet available in `package.json`, surface that as a blocker (Phase 1 wired Vitest + RTL per `01-04-PLAN.md` — RTL should already be installed).
  </action>
  <verify>
    <automated>npm run test -- --run src/components/dashboard/modals/DoctorReport.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/components/dashboard/modals/DoctorReport.test.tsx` exits 0
    - Vitest reports at least 2 passing tests
    - `grep -F "PK_DISCLAIMER_DOCTOR_REPORT" src/components/dashboard/modals/DoctorReport.test.tsx` matches at least 2 times (import + assertion)
    - `grep -F "Pharmacokinetic estimate:" src/components/dashboard/modals/DoctorReport.test.tsx` matches at least 1 time
    - `grep -c "useStore" src/components/dashboard/modals/DoctorReport.test.tsx` returns at least 2 (setState + import)
  </acceptance_criteria>
  <done>
    RTL test green; disclaimer presence asserted; store cleanup in afterEach prevents test pollution.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update Phase 2 cross-reference docs per CONTEXT D-09 — 02-06-SUMMARY.md and 02-HUMAN-UAT.md C10/C11</name>
  <files>.planning/phases/02-visible-compliance-public-deploy/02-06-SUMMARY.md, .planning/phases/02-visible-compliance-public-deploy/02-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/02-visible-compliance-public-deploy/02-06-SUMMARY.md (find watermark text references)
    - .planning/phases/02-visible-compliance-public-deploy/02-HUMAN-UAT.md lines 80-81 specifically (C10 = line 80, C11 = line 81 — see deterministic edit targets in `<action>`)
    - .planning/phases/03-pharmacology-insights-hardening/03-CONTEXT.md D-08, D-09
    - src/lib/disclaimers.ts (canonical Phase 3 strings)
  </read_first>
  <behavior>
    - 02-06-SUMMARY.md: every occurrence of the Phase 2 watermark string `Estimate — not medical advice` has an adjacent "superseded by Phase 3 D-08" note. The doc accurately documents that the live string is now the two-line Phase 3 disclaimer
    - 02-HUMAN-UAT.md: the C10 line (line 80) and C11 line (line 81) carry a Phase 3 supersession note appended directly beneath them — at the exact deterministic anchor specified in the action
    - Both docs preserve their original frontmatter and structure — additive edits only, no destructive rewrites
  </behavior>
  <action>
    **Step A — 02-06-SUMMARY.md:** Use the Read tool first to inspect the actual contents. Locate every line referring to the watermark text `Estimate — not medical advice` or the plugin id `medLevelWatermark`. After each such line, insert an italicized note: `_Superseded by Phase 3 D-08: live watermark is now the two-line disclaimer `estimate, not measured serum level` / `— based on population pharmacokinetics`, plugin id bumped to `medLevelWatermark-v2`. See `.planning/phases/03-pharmacology-insights-hardening/03-CONTEXT.md` D-08._`. Do not remove the original Phase 2 quotation — it remains historically accurate for the Phase 2 deliverable.

    **Step B — 02-HUMAN-UAT.md (DETERMINISTIC EDIT TARGETS):** Open `.planning/phases/02-visible-compliance-public-deploy/02-HUMAN-UAT.md`. The C10 anchor is **line 80**, which reads verbatim:
    `- [ ] **C10. Watermark survives screenshot** — open MedLevelChart on a tab that displays it (Home or Medication); take a screenshot (system screenshot tool); confirm "Estimate — not medical advice" text is visible in the saved PNG (SC#3 verbatim).`
    The C11 anchor is **line 81**, which reads verbatim:
    `- [ ] **C11. Watermark NOT on other charts** — visit Body tab and Symptoms tab; confirm weight + symptom charts have NO watermark (D-14 — watermark is scoped to the pharmacology projection only).`

    Use the Edit tool to make TWO precise replacements (do NOT use Write — preserves the rest of the file verbatim):

    **Edit 1 — replace line 80 (C10) with the original line PLUS a Phase 3 supersession note on a new line below it:**
    Old string (exact):
    `- [ ] **C10. Watermark survives screenshot** — open MedLevelChart on a tab that displays it (Home or Medication); take a screenshot (system screenshot tool); confirm "Estimate — not medical advice" text is visible in the saved PNG (SC#3 verbatim).`
    New string (exact):
    `- [ ] **C10. Watermark survives screenshot** — open MedLevelChart on a tab that displays it (Home or Medication); take a screenshot (system screenshot tool); confirm "Estimate — not medical advice" text is visible in the saved PNG (SC#3 verbatim).`
    `  - **Note (Phase 3 D-09):** As of Phase 3, the live watermark text is `estimate, not measured serum level — based on population pharmacokinetics` (two lines, plugin id `medLevelWatermark-v2`). The Phase 2 single-line string above is HISTORICAL; subsequent UAT runs MUST verify the Phase 3 two-line string. The em-dash U+2014 byte-verification check still applies (Phase 3 line 2 starts with U+2014).`

    **Edit 2 — replace line 81 (C11) with the original line PLUS a Phase 3 supersession note on a new line below it:**
    Old string (exact):
    `- [ ] **C11. Watermark NOT on other charts** — visit Body tab and Symptoms tab; confirm weight + symptom charts have NO watermark (D-14 — watermark is scoped to the pharmacology projection only).`
    New string (exact):
    `- [ ] **C11. Watermark NOT on other charts** — visit Body tab and Symptoms tab; confirm weight + symptom charts have NO watermark (D-14 — watermark is scoped to the pharmacology projection only).`
    `  - **Note (Phase 3 D-09):** D-14's watermark-scope constraint still holds in Phase 3 — the Phase 3 two-line disclaimer plugin (`medLevelWatermark-v2`) is registered per-instance on MedLevelChart only. C11 verifies the scope rule, not the text.`

    **DO NOT** use any "if not present, append at end" fallback — the C10 and C11 anchors are confirmed at lines 80-81 above. If the Edit tool fails to match the old strings verbatim (e.g. someone edited the file between revisions), surface that as a blocker and STOP; do not append elsewhere.

    Do not modify or remove any other content of either file. Specifically, do not edit any of the other UAT checkboxes (C1-C9, D1-D3, E1-E3, F1-F3) and do not touch the Execution Log section starting at line 134.
  </action>
  <verify>
    <automated>grep -c "Phase 3 D-08\|medLevelWatermark-v2" .planning/phases/02-visible-compliance-public-deploy/02-06-SUMMARY.md .planning/phases/02-visible-compliance-public-deploy/02-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Phase 3 D-08" .planning/phases/02-visible-compliance-public-deploy/02-06-SUMMARY.md` matches at least 1 time
    - `grep -F "medLevelWatermark-v2" .planning/phases/02-visible-compliance-public-deploy/02-06-SUMMARY.md` matches at least 1 time
    - `grep -F "estimate, not measured serum level" .planning/phases/02-visible-compliance-public-deploy/02-HUMAN-UAT.md` matches at least 1 time
    - `grep -F "Note (Phase 3 D-09):" .planning/phases/02-visible-compliance-public-deploy/02-HUMAN-UAT.md` matches at least 2 times (one note appended under C10, one under C11)
    - `grep -c "C10. Watermark survives screenshot" .planning/phases/02-visible-compliance-public-deploy/02-HUMAN-UAT.md` returns 1 (original C10 line preserved verbatim — additive edit only)
    - `grep -c "C11. Watermark NOT on other charts" .planning/phases/02-visible-compliance-public-deploy/02-HUMAN-UAT.md` returns 1 (original C11 line preserved verbatim)
    - Both files still parse as valid markdown (no broken frontmatter — `head -5` of each file shows existing structure intact)
  </acceptance_criteria>
  <done>
    Phase 2 docs accurately reference Phase 3's supersession; C10 (line 80) and C11 (line 81) each have a deterministic Phase 3 supersession note appended immediately below; D-09 discharged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `DoctorReport` HTML → window.print PDF → doctor's printer | The DoctorReport modal becomes a printed/saved PDF that travels to the doctor's environment. The disclaimer is the gatekeeping copy that distinguishes "modeled estimate" from a medical-device claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering / Repudiation | `DoctorReport.tsx` PK disclaimer aside (CONTEXT D-10) | mitigate (HIGH severity — compliance posture, identical to T-03-07) | RTL test in `DoctorReport.test.tsx` asserts `PK_DISCLAIMER_DOCTOR_REPORT` text is present in render output. A PR that removes the aside or mutes the text fails CI. Disclaimer string is sourced from `@/lib/disclaimers` so any single-source edit cascades automatically; same source as the watermark plugin. Implements D-10's PDF-watermark-parity requirement. |
| T-03-12 | Information disclosure | print:border-black class | accept | `print:border-black` ensures the aside remains visible under print rendering where CSS variables can degrade in some browsers. No PII risk. |
</threat_model>

<verification>
- `npm run test -- --run src/components/dashboard/modals/DoctorReport.test.tsx` green
- `npx tsc -p tsconfig.app.json --noEmit` clean
- `npm run lint -- src/components/dashboard/modals/DoctorReport.tsx` exits 0
- Manual UAT (recorded in VALIDATION.md as one of two manual checks): developer opens DoctorReport modal, clicks Print, confirms the new aside appears in the print preview between the patient-name header and the Summary section, and the disclaimer text matches `PK_DISCLAIMER_DOCTOR_REPORT` verbatim
</verification>

<success_criteria>
PK-04 (PDF side) satisfied: `DoctorReport` PDF includes the `estimate, not measured serum level — based on population pharmacokinetics` disclaimer in a visible, print-surviving aside. CONTEXT D-10 satisfied: DoctorReport PDF carries the same watermark text as the chart canvas (via parallel HTML aside, per the second branch of D-10). CONTEXT D-09 satisfied: Phase 2 cross-reference docs updated at deterministic C10/C11 anchors.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pharmacology-insights-hardening/03-04-SUMMARY.md` documenting:
- DoctorReport.tsx insertion site + RTL test coverage (discharges D-10)
- Phase 2 cross-reference doc updates: deterministic C10/C11 anchor edits (discharges D-09)
- Manual UAT note for the print-preview verification (planner records in VALIDATION.md as the second of two manual-only items)
</output>
</output>
