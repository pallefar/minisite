---
phase: 03-pharmacology-insights-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/components/dashboard/charts/MedLevelChart.tsx
  - src/components/dashboard/charts/medLevelWatermarkPlugin.ts
  - src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts
autonomous: true
requirements:
  - PK-03
  - PK-04
user_setup: []
tags:
  - chart
  - chartjs
  - watermark
  - band

must_haves:
  truths:
    - "MedLevelChart renders a shaded uncertainty band (CV%-driven, per drug class) on the chart canvas under the past + projected mean lines"
    - "MedLevelChart Y-axis carries no measurement-grade unit — the axis title reads 'Estimate · arbitrary units' (middle dot U+00B7)"
    - "medLevelWatermarkPlugin paints the longer Phase 3 two-line disclaimer at 45° across the chart canvas, in BOTH light and dark themes, on every render"
    - "Watermark plugin id is 'medLevelWatermark-v2' and the consumer in MedLevelChart.tsx references the same v2 id"
    - "The chart's aria-label includes the words 'estimate' and 'not a measured serum level' so screen-reader users hear the disclaimer"
    - "Tooltip and legend filter out the 4 invisible band-bound datasets so only Past + Projected appear in user-visible chart chrome"
    - "Empty-state (zero injections) renders the existing Past+Projected datasets but suppresses the band so there is no ghost-fill artifact"
  artifacts:
    - path: "src/components/dashboard/charts/MedLevelChart.tsx"
      provides: "Band datasets + plugin-id update + Y-axis relabel + aria-label update"
      contains: "CV_BY_DRUG_CLASS"
    - path: "src/components/dashboard/charts/medLevelWatermarkPlugin.ts"
      provides: "Two-line Phase 3 disclaimer render + v2 plugin id + dynamic font formula"
      contains: "medLevelWatermark-v2"
    - path: "src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts"
      provides: "Updated assertions for v2 id + two-line fillText calls"
      contains: "medLevelWatermark-v2"
  key_links:
    - from: "src/components/dashboard/charts/MedLevelChart.tsx"
      to: "src/lib/pharmacology-corpus.ts"
      via: "import { CV_BY_DRUG_CLASS } from '@/lib/pharmacology-corpus'"
      pattern: "CV_BY_DRUG_CLASS"
    - from: "src/components/dashboard/charts/MedLevelChart.tsx"
      to: "src/lib/pharmacology.ts"
      via: "import { trialClass } from '@/lib/pharmacology'"
      pattern: "trialClass"
    - from: "src/components/dashboard/charts/medLevelWatermarkPlugin.ts"
      to: "src/lib/disclaimers.ts"
      via: "import { PK_DISCLAIMER_LINE_1, PK_DISCLAIMER_LINE_2 } from '@/lib/disclaimers'"
      pattern: "PK_DISCLAIMER_LINE_1"
---

<objective>
Render the uncertainty band on the drug-level chart, swap the Y-axis title to the abstract-unit label, update the watermark text + plugin id + dynamic font formula to handle the longer Phase 3 disclaimer as two lines, and update the corresponding canvas-mock test.

Purpose: A patient reading the chart sees a shaded band (not a deterministic line) plus the chart-overlaid "estimate, not measured serum level" disclaimer — surviving screenshots. A doctor in Phase 8 read-share reuses this same component. Implements PK-03 (band) and PK-04 (chart-side watermark text).

Output: Three files modified. No new files.

**Critical dataset-ordering decision (from PATTERNS.md flag + UI-SPEC tooltip filter):** Visible lines (Past, Projected) are placed FIRST in the datasets array, so the existing UI-SPEC tooltip filter `(item) => item.datasetIndex < 2` correctly EXCLUDES the band datasets. Chart.js `fill: '+1'` still works for the band because `'+1'` is index-relative. Band datasets follow with `fill: '+1'` on upper-bound pointing down to lower-bound (the dataset immediately after it in the array). This reverses RESEARCH lines 500-547's "bands first" suggestion in favor of UI-SPEC's tooltip filter — PATTERNS.md flagged this reconciliation and Plan 03 picks the visible-lines-first ordering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pharmacology-insights-hardening/03-CONTEXT.md
@.planning/phases/03-pharmacology-insights-hardening/03-RESEARCH.md
@.planning/phases/03-pharmacology-insights-hardening/03-PATTERNS.md
@.planning/phases/03-pharmacology-insights-hardening/03-UI-SPEC.md
@.planning/phases/03-pharmacology-insights-hardening/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 (already shipped) — Plan 03 consumes these. -->
```typescript
// src/lib/disclaimers.ts
export const PK_DISCLAIMER_LINE_1: string;   // 'estimate, not measured serum level'
export const PK_DISCLAIMER_LINE_2: string;   // '— based on population pharmacokinetics' (U+2014 em-dash first char)
export const PK_DISCLAIMER_Y_AXIS: string;   // 'Estimate · arbitrary units'

// src/lib/pharmacology-corpus.ts
export const CV_BY_DRUG_CLASS: Record<string, number>; // values like 0.27 (semaglutide), 0.14 (tirz), 0.30, 0.34, 0.30
```

<!-- Existing pharmacology helpers Plan 03 consumes -->
```typescript
// src/lib/pharmacology.ts
export function trialClass(med: MedicationId): 'semaglutide' | 'tirzepatide' | 'liraglutide' | 'dulaglutide' | 'retatrutide';
```

<!-- Existing chart-theme tokens (no new tokens introduced) -->
```typescript
// src/lib/chart-theme.ts
export function getChartTokens(theme: Theme): { primary: string; rose: string; tick: string; grid: string; /* ... */ };
```

<!-- Per-instance plugin discipline (Phase 2 D-13/D-14/D-15 — PRESERVED) -->
The watermark plugin remains per-instance via `plugins: [medLevelWatermarkPlugin]` in the ChartConfiguration. NEVER `Chart.register(medLevelWatermarkPlugin)` — that would leak the watermark onto every chart sharing BaseChart.

<!-- α=0.12 hex-alpha idiom — already in repo at MedLevelChart.tsx:50, 61 -->
Use `t.primary + '20'` and `t.rose + '20'` for band fill (the `'20'` suffix is 0x20/255 ≈ 12.5% alpha — matches UI-SPEC α=0.12).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update medLevelWatermarkPlugin.ts — two-line render, v2 plugin id, dynamic font formula, import strings from disclaimers module</name>
  <files>src/components/dashboard/charts/medLevelWatermarkPlugin.ts</files>
  <read_first>
    - src/components/dashboard/charts/medLevelWatermarkPlugin.ts entire file (53 lines)
    - src/lib/disclaimers.ts (built by Plan 01 — provides PK_DISCLAIMER_LINE_1 and PK_DISCLAIMER_LINE_2)
    - .planning/phases/03-pharmacology-insights-hardening/03-RESEARCH.md lines 619-636 (two-line wrap recommendation + font formula)
    - .planning/phases/03-pharmacology-insights-hardening/03-UI-SPEC.md lines 51-62 (typography contract — UI-SPEC line 61 documents the fallback `0.08 → 0.055` and instructs "Document the chosen value in the plan")
  </read_first>
  <behavior>
    - Plugin exports `medLevelWatermarkPlugin: Plugin<'line', MedLevelWatermarkOptions>` with `id: 'medLevelWatermark-v2'`
    - `afterDraw` paints `PK_DISCLAIMER_LINE_1` at `(0, -lineHeight / 2)` and `PK_DISCLAIMER_LINE_2` at `(0, +lineHeight / 2)` — two fillText calls, both rotated -45°
    - Font formula: `ctx.font = \`bold ${Math.max(11, height * 0.06)}px ${fontFamily}\`;` (11px floor, 0.06 multiplier — replaces previous 14/0.08)
    - `lineHeight = Math.max(13, height * 0.07)`
    - When `chartArea` is undefined, plugin bails before any `ctx.save()` (existing behavior preserved)
    - `opacity` option defaults to 0.12; `color` defaults to `'120, 120, 120'`; rotation stays `-Math.PI / 4`
  </behavior>
  <action>
    Modify `src/components/dashboard/charts/medLevelWatermarkPlugin.ts` in place.

    **Multiplier rationale (UI-SPEC line 61 compliance):** UI-SPEC line 61 documents the fallback path as `0.08 → 0.055` and instructs "Document the chosen value in the plan." This plan picks `0.06` (NOT 0.055) as the chosen value. Rationale recorded here per UI-SPEC instruction: 0.06 is the midpoint between Phase 2's `0.08` (sized for the 31-char single-line string) and UI-SPEC's deepest-compact fallback `0.055` (sized for worst-case narrow viewports). 0.06 preserves legibility on standard viewports while honoring UI-SPEC's shrink-from-0.08 intent for the longer Phase 3 two-line copy (96 chars total). `0.055` is reserved as the deepest-compact fallback if QA reports clipping at narrow widths during the Plan 05 VALIDATION pass; if invoked, the implementer changes `Math.max(11, height * 0.06)` to `Math.max(11, height * 0.055)` in this same file and re-runs the watermark test. Add a one-line JSDoc comment above the `ctx.font` assignment: `// Font multiplier 0.06: midpoint between Phase 2's 0.08 and UI-SPEC line 61's compact-fallback 0.055; honors UI-SPEC shrink-from-0.08 intent for the longer two-line Phase 3 disclaimer.`

    Implementation steps:
    (1) Update the file-header JSDoc to cite Phase 3 D-08 (plugin id bump) and the two-line render rationale.
    (2) Replace the `const WATERMARK_TEXT = 'Estimate — not medical advice';` line with `import { PK_DISCLAIMER_LINE_1, PK_DISCLAIMER_LINE_2 } from '@/lib/disclaimers';` AT THE TOP of the file (after the existing `import type { Chart, Plugin } from 'chart.js';` line).
    (3) Delete the `WATERMARK_TEXT` constant entirely.
    (4) Change `id: 'medLevelWatermark'` to `id: 'medLevelWatermark-v2'` on the plugin object.
    (5) Inside `afterDraw`, remove the `const text = options.text ?? WATERMARK_TEXT;` line.
    (6) Replace the font formula `ctx.font = \`bold ${Math.max(14, height * 0.08)}px ${fontFamily}\`;` with the rationale comment above plus `ctx.font = \`bold ${Math.max(11, height * 0.06)}px ${fontFamily}\`;`.
    (7) Add `const lineHeight = Math.max(13, height * 0.07);` immediately before the fillText calls.
    (8) Replace the single `ctx.fillText(text, 0, 0);` line with two calls: `ctx.fillText(options.text ?? PK_DISCLAIMER_LINE_1, 0, -lineHeight / 2);` and `ctx.fillText(PK_DISCLAIMER_LINE_2, 0, lineHeight / 2);` — the `options.text` override only affects line 1 (testing convenience; production paints both verbatim from constants).
    (9) Update the `MedLevelWatermarkOptions.text` JSDoc to clarify it overrides line 1 only.
  </action>
  <verify>
    <automated>npm run test -- --run src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "id: 'medLevelWatermark-v2'" src/components/dashboard/charts/medLevelWatermarkPlugin.ts` matches exactly 1 time
    - `grep -c "WATERMARK_TEXT" src/components/dashboard/charts/medLevelWatermarkPlugin.ts` returns 0 (constant removed)
    - `grep -F "import { PK_DISCLAIMER_LINE_1, PK_DISCLAIMER_LINE_2 } from '@/lib/disclaimers'" src/components/dashboard/charts/medLevelWatermarkPlugin.ts` matches
    - `grep -F "Math.max(11, height * 0.06)" src/components/dashboard/charts/medLevelWatermarkPlugin.ts` matches
    - `grep -F "Font multiplier 0.06" src/components/dashboard/charts/medLevelWatermarkPlugin.ts` matches (rationale comment present per UI-SPEC line 61 "Document the chosen value" instruction)
    - `grep -c "ctx.fillText" src/components/dashboard/charts/medLevelWatermarkPlugin.ts` returns 2 (two-line render)
    - `npx tsc -p tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Plugin id is `medLevelWatermark-v2`; two-line render uses imported disclaimer constants from `@/lib/disclaimers`; new font formula 0.06 in place with rationale comment per UI-SPEC line 61; chartArea undefined guard preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update medLevelWatermarkPlugin.test.ts — assert v2 id, two-line fillText calls, em-dash byte presence</name>
  <files>src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts</files>
  <read_first>
    - src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts entire file (68 lines)
    - src/components/dashboard/charts/medLevelWatermarkPlugin.ts (post-Task-1 state)
    - src/lib/disclaimers.ts (constants under test)
    - .planning/phases/03-pharmacology-insights-hardening/03-PATTERNS.md §`medLevelWatermarkPlugin.test.ts` (assertion updates)
  </read_first>
  <behavior>
    - First `it` asserts `medLevelWatermarkPlugin.id === 'medLevelWatermark-v2'`
    - Second `it` asserts BOTH `fillText` calls happen with the imported constants: line 1 = `PK_DISCLAIMER_LINE_1` at `(0, expect.any(Number))` for the negative-y offset, line 2 = `PK_DISCLAIMER_LINE_2` at `(0, expect.any(Number))` for the positive-y offset
    - An additional `it` asserts `PK_DISCLAIMER_LINE_2.charCodeAt(0) === 0x2014` (em-dash byte presence — replaces Phase 2's verbatim string check)
    - Existing `rotates -45°`, `save/restore hygiene`, `honors opacity`, `bails when chartArea is undefined` tests all still pass
  </behavior>
  <action>
    Modify the existing test file. (1) Add an import at the top: `import { PK_DISCLAIMER_LINE_1, PK_DISCLAIMER_LINE_2 } from '@/lib/disclaimers';`. (2) Update the `'plugin id is "medLevelWatermark" (D-15)'` test to assert `expect(medLevelWatermarkPlugin.id).toBe('medLevelWatermark-v2');` and update its description to `'plugin id is "medLevelWatermark-v2" (Phase 3 D-08)'`. (3) Replace the `'draws the verbatim SC#3 watermark via fillText'` test entirely with: `it('draws the Phase 3 two-line watermark via fillText (D-08)', () => { const { chart, ctx } = makeChart(); medLevelWatermarkPlugin.afterDraw!(chart as never, {} as never, {}); expect(ctx.fillText).toHaveBeenCalledWith(PK_DISCLAIMER_LINE_1, 0, expect.any(Number)); expect(ctx.fillText).toHaveBeenCalledWith(PK_DISCLAIMER_LINE_2, 0, expect.any(Number)); expect(ctx.fillText).toHaveBeenCalledTimes(2); });`. (4) Add a new test: `it('PK_DISCLAIMER_LINE_2 starts with em-dash U+2014 (byte verification)', () => { expect(PK_DISCLAIMER_LINE_2.charCodeAt(0)).toBe(0x2014); });`. (5) Leave the other four `it` blocks (`rotates`, `save/restore`, `opacity`, `bails when chartArea is undefined`) untouched.
  </action>
  <verify>
    <automated>npm run test -- --run src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts` exits 0
    - `grep -F "expect(medLevelWatermarkPlugin.id).toBe('medLevelWatermark-v2')" src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts` matches
    - `grep -F "0x2014" src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts` matches (em-dash byte test)
    - `grep -c "expect(ctx.fillText).toHaveBeenCalledWith" src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts` returns at least 2
    - Vitest run shows at least 6 passing tests in this file
  </acceptance_criteria>
  <done>
    All six tests pass; plugin id, two-line render, em-dash byte, rotation, save/restore hygiene, opacity, and chartArea-undefined bail are all asserted.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update MedLevelChart.tsx — add band datasets, Y-axis relabel, plugin-id key rename, aria-label update, tooltip/legend filters</name>
  <files>src/components/dashboard/charts/MedLevelChart.tsx</files>
  <read_first>
    - src/components/dashboard/charts/MedLevelChart.tsx entire file (100 lines)
    - src/lib/pharmacology-corpus.ts (built by Plan 01 — provides CV_BY_DRUG_CLASS)
    - src/lib/pharmacology.ts lines 100-114 (trialClass)
    - src/lib/disclaimers.ts (PK_DISCLAIMER_Y_AXIS)
    - .planning/phases/03-pharmacology-insights-hardening/03-RESEARCH.md lines 486-575 (band config snippet)
    - .planning/phases/03-pharmacology-insights-hardening/03-UI-SPEC.md lines 70-91, 125-135, 138-151 (color contract, interaction contract, layout contract)
    - .planning/phases/03-pharmacology-insights-hardening/03-PATTERNS.md §`MedLevelChart.tsx` (dataset-ordering reconciliation — Plan 03 picks visible-lines-first)
  </read_first>
  <behavior>
    - Y-axis title reads `Estimate · arbitrary units` (sourced from `PK_DISCLAIMER_Y_AXIS`); the previous `${u.doseUnit} in system` template literal is removed
    - When `injections.length > 0`, the datasets array contains exactly 6 entries in this z-order: `[Past, Projected, UpperPast, LowerPast, UpperProjected, LowerProjected]`. Past+Projected are FIRST so `tooltip.filter: (item) => item.datasetIndex < 2` shows only those two
    - When `injections.length === 0`, the datasets array contains only `[Past, Projected]` (no band — empty-state suppression)
    - Each upper bound dataset = `mean[i] * (1 + cvPct)`; each lower bound dataset = `mean[i] * (1 - cvPct)`. `cvPct = CV_BY_DRUG_CLASS[trialClass(u.medication)] ?? 0.30`
    - Upper-bound datasets carry `borderColor: 'transparent'`, `backgroundColor: t.primary + '20'` (past) / `t.rose + '20'` (projected), `fill: '+1'`, `pointRadius: 0`, `tension: 0.3`
    - Lower-bound datasets carry `borderColor: 'transparent'`, `backgroundColor: 'transparent'`, `fill: false`, `pointRadius: 0`
    - Tooltip filter: `tooltip: { filter: (item) => item.datasetIndex < 2 }` — hides band datasets from hover tooltip
    - Legend filter: `legend.labels.filter: (item) => item.text === 'Past' || item.text === 'Projected'` — band datasets hidden from legend
    - Plugin options key on the ChartConfiguration's `plugins` block is `'medLevelWatermark-v2'` (quoted because of hyphen) instead of the previous unquoted `medLevelWatermark`
    - aria-label on the BaseChart wrapper is `28-day medication level estimate with inter-individual variability band, not a measured serum level`
    - `plugins: [medLevelWatermarkPlugin]` array (per-instance registration) is preserved unchanged
    - A DOM caption renders below the chart canvas with verbatim text `Estimate · ~30% inter-individual variation` (middle dot U+00B7; tilde ASCII U+007E) sourced from PK_DISCLAIMER_BAND_CAPTION; caption is wrapped in a `<p>` with text-xs muted styling (per UI-SPEC line 103 + D-06) — accessible to screen readers and visible in light + dark themes
  </behavior>
  <action>
    Modify `src/components/dashboard/charts/MedLevelChart.tsx`. (1) Add imports (alphabetized, per existing `eslint-plugin-import-x` order): import `PK_DISCLAIMER_Y_AXIS` from `@/lib/disclaimers`, import `CV_BY_DRUG_CLASS` from `@/lib/pharmacology-corpus`, and add `trialClass` to the existing `@/lib/pharmacology` import alongside `HALF_LIVES, calcMedLevel`. (2) Inside the `useMemo`, after the existing `past`/`future` computation and the bridge-the-gap block (line 39), add: `const cvPct = CV_BY_DRUG_CLASS[trialClass(u.medication)] ?? 0.30;` then four mapped arrays: `const upperPast = past.map((v) => v == null ? null : v * (1 + cvPct));`, `const lowerPast = past.map((v) => v == null ? null : v * (1 - cvPct));`, `const upperFuture = future.map((v) => v == null ? null : v * (1 + cvPct));`, `const lowerFuture = future.map((v) => v == null ? null : v * (1 - cvPct));`. Then `const showBand = injections.length > 0;`. (3) Build the datasets array conditionally. Keep the existing `Past` dataset (lines 46-56) and `Projected` dataset (lines 57-68) as-is — these stay FIRST in the array. APPEND the band datasets when `showBand` is true: spread an array `...(showBand ? [{ label: 'Upper bound (Past)', data: upperPast, borderColor: 'transparent', backgroundColor: t.primary + '20', fill: '+1', tension: 0.3, pointRadius: 0, spanGaps: false }, { label: 'Lower bound (Past)', data: lowerPast, borderColor: 'transparent', backgroundColor: 'transparent', fill: false, tension: 0.3, pointRadius: 0, spanGaps: false }, { label: 'Upper bound (Projected)', data: upperFuture, borderColor: 'transparent', backgroundColor: t.rose + '20', fill: '+1', tension: 0.3, pointRadius: 0, spanGaps: true }, { label: 'Lower bound (Projected)', data: lowerFuture, borderColor: 'transparent', backgroundColor: 'transparent', fill: false, tension: 0.3, pointRadius: 0, spanGaps: true }] : [])`. Note: this places band datasets AFTER Past/Projected so `'+1'` on `Upper bound (Past)` correctly points DOWN to the next dataset (`Lower bound (Past)`) — visible-lines-first ordering preserves UI-SPEC's `datasetIndex < 2` tooltip filter. (4) Update `options.plugins.legend.labels` to include `filter: (item: { text: string }) => item.text === 'Past' || item.text === 'Projected'` alongside the existing `color: t.tick`. (5) Add `tooltip: { filter: (item: { datasetIndex: number }) => item.datasetIndex < 2 }` inside `options.plugins` next to legend. (6) Replace the plugin options key `medLevelWatermark: { ... }` with `'medLevelWatermark-v2': { ... }` (quoted key because of hyphen) — the inner `color` and `opacity` object is unchanged. (7) In `options.scales.y.title`, replace `text: \`${u.doseUnit} in system\`` with `text: PK_DISCLAIMER_Y_AXIS`. (8) Update the BaseChart wrapper at the bottom of the return: change `ariaLabel="28-day medication level"` to `ariaLabel="28-day medication level estimate with inter-individual variability band, not a measured serum level"`. (9) Preserve `plugins: [medLevelWatermarkPlugin]` at line 95 verbatim. (10) Add `injections.length` to the `useMemo` dependency array if not already covered by `injections` (it is — `injections` is already in the deps). (11) Also import `PK_DISCLAIMER_BAND_CAPTION` from `@/lib/disclaimers` alongside `PK_DISCLAIMER_Y_AXIS`. (12) Wrap the existing single-element return in a React Fragment and render a DOM caption directly under the BaseChart: `return (<><BaseChart config={config} height={height} ariaLabel="28-day medication level estimate with inter-individual variability band, not a measured serum level" /><p className="text-[12px] italic text-[var(--color-text-tertiary)] text-center mt-1">{PK_DISCLAIMER_BAND_CAPTION}</p></>);` — the caption is one DOM element below the canvas; preserves a11y for screen-reader users since canvas pixels are inaccessible to AT.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -E "MedLevelChart|error TS" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "import { PK_DISCLAIMER_Y_AXIS } from '@/lib/disclaimers'" src/components/dashboard/charts/MedLevelChart.tsx` matches
    - `grep -F "import { CV_BY_DRUG_CLASS } from '@/lib/pharmacology-corpus'" src/components/dashboard/charts/MedLevelChart.tsx` matches
    - `grep -F "trialClass" src/components/dashboard/charts/MedLevelChart.tsx` matches at least 2 times (import + call)
    - `grep -F "'medLevelWatermark-v2':" src/components/dashboard/charts/MedLevelChart.tsx` matches (quoted plugin options key)
    - `grep -cE "medLevelWatermark(\['\"\\]|: \\{)" src/components/dashboard/charts/MedLevelChart.tsx` returns 0 (no old unversioned plugin-options key remains — regex matches `medLevelWatermark: {` or `medLevelWatermark['` or `medLevelWatermark"]` ONLY, so the `medLevelWatermarkPlugin` identifier is NOT a false-positive)
    - `grep -F "text: PK_DISCLAIMER_Y_AXIS" src/components/dashboard/charts/MedLevelChart.tsx` matches
    - `grep -F "datasetIndex < 2" src/components/dashboard/charts/MedLevelChart.tsx` matches (tooltip filter)
    - `grep -F "item.text === 'Past'" src/components/dashboard/charts/MedLevelChart.tsx` matches (legend filter)
    - `grep -F "fill: '+1'" src/components/dashboard/charts/MedLevelChart.tsx` matches at least 2 times (past upper + projected upper)
    - `grep -F "ariaLabel=\"28-day medication level estimate with inter-individual variability band, not a measured serum level\"" src/components/dashboard/charts/MedLevelChart.tsx` matches
    - `grep -F "doseUnit} in system" src/components/dashboard/charts/MedLevelChart.tsx` returns 0 matches (template literal removed)
    - `grep -F "Chart.register(medLevelWatermarkPlugin)" src/components/dashboard/charts/MedLevelChart.tsx` returns 0 matches (per-instance discipline preserved)
    - `npx tsc -p tsconfig.app.json --noEmit` exits 0
    - `npm run test -- --run src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts` exits 0 (no regression from plugin-id cascade)
    - `npm run lint -- src/components/dashboard/charts/MedLevelChart.tsx` exits 0
    - `grep -F "PK_DISCLAIMER_BAND_CAPTION" src/components/dashboard/charts/MedLevelChart.tsx` matches at least 2 times (import + JSX usage)
    - `grep -F "Estimate · ~30% inter-individual variation" src/lib/disclaimers.ts` matches (source constant verbatim)
  </acceptance_criteria>
  <done>
    Band datasets render when injections exist; Y-axis carries the abstract-unit string; plugin-id cascade complete; tooltip and legend filters in place; aria-label updated; per-instance plugin registration preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `medLevelWatermarkPlugin` → screenshot | The watermark is the patient-/doctor-/clinic-visible disclaimer that survives screenshots (since it lives on the canvas, not the DOM). Removing or muting it removes the gatekeeping copy that distinguishes modeled estimate from a measurement claim. |
| `MedLevelChart` → DOM / a11y tree | The aria-label is the screen-reader equivalent of the canvas watermark. SR users cannot read canvas pixels; they need the disclaimer in the aria-label. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering / Repudiation | `medLevelWatermarkPlugin.ts` watermark text | mitigate (HIGH severity — patient safety / FDA general-wellness framing) | Plugin id is `medLevelWatermark-v2` and `medLevelWatermarkPlugin.test.ts` asserts both `id` AND that `PK_DISCLAIMER_LINE_2.charCodeAt(0) === 0x2014` (em-dash byte verification). A PR that drops the em-dash or renames the id is caught by CI. Plus: the disclaimer strings live in `src/lib/disclaimers.ts` (Plan 01) — a single source of truth — so future text changes are one-file edits with diff visibility. |
| T-03-08 | Tampering | `MedLevelChart.tsx` Y-axis label | mitigate (HIGH severity — implies measurement claim if reverted) | Y-axis title sourced from `PK_DISCLAIMER_Y_AXIS` constant. A PR that hardcodes `'mg in system'` or `'ng/mL'` in the chart is visible in diff and breaks the constant-import pattern. SC#1 ROADMAP success criterion explicitly forbids `ng/mL` — codified as the constant. |
| T-03-09 | Information disclosure | accessibility (a11y) aria-label | mitigate | aria-label includes "estimate" and "not a measured serum level" so SR users receive equivalent information to the canvas watermark. Verified by acceptance criteria grep. |
| T-03-10 | Denial of service (visual) | band rendering with zero injections | mitigate | `showBand = injections.length > 0` gate prevents zero-on-zero ghost-fill artifact that would look like a UI bug. |
</threat_model>

<verification>
- `npm run test -- --run src/components/dashboard/charts/medLevelWatermarkPlugin.test.ts` green
- `npm run test -- --run src/lib/pharmacology.test.ts` green (regression check — Plan 01 still passes after Plan 03 reads its exports)
- `npx tsc -p tsconfig.app.json --noEmit` clean
- `npm run lint` clean for the three modified files
- Visual UAT: developer renders the dashboard locally, navigates to Home tab → confirms band visible, Y-axis reads "Estimate · arbitrary units", watermark reads two-line disclaimer at 45°. (This visual check is the one manual-only verification for Phase 3 — recorded in VALIDATION.md.)
</verification>

<success_criteria>
PK-03 satisfied: Chart visually conveys uncertainty as a shaded band per drug-class CV%. PK-04 (chart side) satisfied: watermark text on the canvas reads `estimate, not measured serum level — based on population pharmacokinetics` on every render of `MedLevelChart`, in both themes. ROADMAP Phase 3 SC#1 satisfied: Y-axis carries no measurement-grade unit.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pharmacology-insights-hardening/03-03-SUMMARY.md` documenting:
- Plugin-id cascade (plugin file + consumer + test all on `medLevelWatermark-v2`)
- Dataset ordering chosen (visible-lines-first) and the UI-SPEC tooltip-filter reconciliation
- Y-axis relabel + aria-label update
- Empty-state band suppression confirmation
- Test counts
- Watermark font multiplier chosen value (0.06) + rationale (per UI-SPEC line 61 "Document the chosen value in the plan" instruction)
</output>
</output>
