---
phase: 51-full-traffic-conversion-tracking-system-unified-dashboard-ut
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/.planning/REQUIREMENTS.md
  - supabase/migrations/20270712000001_user_traffic_attribution.sql
  - supabase/migrations/20270712000002_channel_groups.sql
  - supabase/migrations/20270712000003_referrer_channel_rules_seed.sql
  - supabase/migrations/20270712000004_is_retained_secdef_helper.sql
  - supabase/migrations/20270712000005_upsert_traffic_attribution_rpcs.sql
  - supabase/migrations/20270712000006_traffic_taxonomy_rls.sql
  - leanshot/src/lib/analytics/events.ts
autonomous: true
requirements:
  - TRAFFIC-01
  - TRAFFIC-04
  - TRAFFIC-05
  - TRAFFIC-06
  - TRAFFIC-09
  - TRAFFIC-10
must_haves:
  truths:
    - "12 TRAFFIC-NN REQ-IDs exist in REQUIREMENTS.md and resolve from this plan forward"
    - "user_traffic_attribution table exists with first_touch + last_touch columns + org_id + anon_id UNIQUE index"
    - "channel_groups table exists with 8 seed rows + match_rule_jsonb + attribution_window_days + priority order"
    - "referrer_channel_rules table exists with ~80 well-known seed rows from Snowplow referer-parser JSON"
    - "is_retained(user_id, audience, window_days) SECDEF helper returns boolean per per-audience retention rule"
    - "classify_channel_group SQL helper resolves utm → channel_group label in priority order"
    - "upsert_traffic_attribution + claim_traffic_attribution SECDEF RPCs preserve first-touch immutability via ON CONFLICT"
    - "RLS denies anon role on all 3 new tables; admin sees all; clinic_owner sees only own org rows"
    - "events.ts exports traffic_visit, traffic_signup, traffic_activation, traffic_paid event defs (additive-only)"
  artifacts:
    - path: "leanshot/.planning/REQUIREMENTS.md"
      provides: "TRAFFIC-01..12 entries appended to v1.3 Requirements block"
      contains: "TRAFFIC-12"
    - path: "supabase/migrations/20270712000001_user_traffic_attribution.sql"
      provides: "user_traffic_attribution table + indexes + RLS"
      contains: "create table public.user_traffic_attribution"
    - path: "supabase/migrations/20270712000002_channel_groups.sql"
      provides: "channel_groups table + 8 seed rows + classify_channel_group fn"
      contains: "create or replace function public.classify_channel_group"
    - path: "supabase/migrations/20270712000003_referrer_channel_rules_seed.sql"
      provides: "referrer_channel_rules table + ~80 seed INSERT statements"
      contains: "create table public.referrer_channel_rules"
    - path: "supabase/migrations/20270712000004_is_retained_secdef_helper.sql"
      provides: "is_retained(user_id uuid, audience text, window_days int) SECDEF SQL function"
      contains: "create or replace function public.is_retained"
    - path: "supabase/migrations/20270712000005_upsert_traffic_attribution_rpcs.sql"
      provides: "upsert_traffic_attribution + claim_traffic_attribution SECDEF RPCs"
      contains: "create or replace function public.upsert_traffic_attribution"
    - path: "supabase/migrations/20270712000006_traffic_taxonomy_rls.sql"
      provides: "RLS policies on 3 tables + cross-tenant deny test for user_traffic_attribution"
      contains: "create policy"
    - path: "leanshot/src/lib/analytics/events.ts"
      provides: "4 net-new traffic_* event defs (additive)"
      contains: "traffic_visit"
  key_links:
    - from: "supabase/migrations/20270712000001_user_traffic_attribution.sql"
      to: "_is_org_clinician(org_id, user_id)"
      via: "RLS policy USING clause"
      pattern: "_is_org_clinician"
    - from: "supabase/migrations/20270712000005_upsert_traffic_attribution_rpcs.sql"
      to: "user_traffic_attribution"
      via: "ON CONFLICT (anon_id) DO UPDATE SET last_touch_* = EXCLUDED.last_touch_*"
      pattern: "ON CONFLICT \\(anon_id\\)"
    - from: "supabase/migrations/20270712000002_channel_groups.sql"
      to: "supabase/migrations/20270712000003_referrer_channel_rules_seed.sql"
      via: "classify_channel_group_with_referrer wrapper falls through to referrer_channel_rules"
      pattern: "referrer_channel_rules"
---

<objective>
Land the SQL + REQ-IDs foundation for Phase 51: append the 12 TRAFFIC-NN REQ-IDs to REQUIREMENTS.md so downstream plans can claim them; create `user_traffic_attribution` + `channel_groups` + `referrer_channel_rules` tables; create the `is_retained()` retention helper, the `classify_channel_group()` classifier, and the SECDEF UPSERT RPCs; apply RLS for admin + per-clinic-org scoping; extend `events.ts` with 4 additive traffic_* event defs.

Purpose: every downstream plan in Phase 51 reads from or writes to these tables. This plan is the schema + REQ-ID + classifier foundation — nothing else lands until this does.

Output: 6 SQL migrations + 1 REQUIREMENTS.md edit + 1 events.ts edit, all dispatched in a single commit; ready for Wave 2 to start.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/PROJECT.md
@leanshot/.planning/ROADMAP.md
@leanshot/.planning/STATE.md
@leanshot/.planning/phases/51-full-traffic-conversion-tracking-system-unified-dashboard-ut/51-CONTEXT.md
@leanshot/.planning/phases/51-full-traffic-conversion-tracking-system-unified-dashboard-ut/51-RESEARCH.md
@leanshot/.planning/phases/51-full-traffic-conversion-tracking-system-unified-dashboard-ut/51-PATTERNS.md

# Reference analogs (read before writing migrations)
@supabase/migrations/20270601300004_p30_matviews_and_cron.sql
@supabase/migrations/20270601100011_resolve_clinic_slug_rpc.sql
@supabase/migrations/20270601300100_p31_00_enum_rename_and_secdef_ripple.sql
@supabase/functions/_shared/posthog-server.ts
@leanshot/src/lib/analytics/events.ts

<interfaces>
<!-- SECDEF helpers already on main (do not redeclare; reference only) -->

is_admin() returns boolean — Phase 24 SECDEF helper, gates admin reads.
_is_org_clinician(org_id uuid, user_id uuid) returns boolean — Phase 31 SECDEF helper, gates org-scoped reads.

events_mirror table — Phase 27; columns include event_name text, distinct_id text, properties jsonb, created_at timestamptz.

EventDef factory in leanshot/src/lib/analytics/events.ts — existing additive-only export shape; enforced by eslint-rules/additive-only-events.js.

ad_spend_facts table — Phase 33; LEFT JOIN target by (network → channel_group via fixed mapping) × day. Not touched by this plan, but downstream matviews depend on it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append TRAFFIC-01..12 REQ-IDs to REQUIREMENTS.md</name>
  <files>leanshot/.planning/REQUIREMENTS.md</files>
  <action>
Verify `leanshot/.planning/REQUIREMENTS.md` now contains a `### WS25 — A Revenue: Multi-channel Traffic + Conversion Tracking (TRAFFIC, 12 REQ-IDs)` section with TRAFFIC-01 through TRAFFIC-12 entries inserted before the `## Future Requirements (v1.4+)` heading (the planner-orchestrator step has already inserted this block via `python3 -c` patch — confirm via grep). If the block is missing, re-insert using the canonical text already produced by the planner. Do NOT renumber existing WS-NN sections; WS25 sits between WS24 DIGEST and Future Requirements per source-audit traceability (D-01..D-16 → TRAFFIC-01..12).
  </action>
  <verify>
    <automated>grep -c "TRAFFIC-12" leanshot/.planning/REQUIREMENTS.md | grep -v "^0$"</automated>
  </verify>
  <done>`grep "^- \[ \] \*\*TRAFFIC-" leanshot/.planning/REQUIREMENTS.md | wc -l` returns 12; the `WS25` heading exists; no existing WS24 / Future Requirements text is broken.</done>
</task>

<task type="auto">
  <name>Task 2: Migration — user_traffic_attribution table + RLS + classify_channel_group helper + channel_groups taxonomy + referrer_channel_rules</name>
  <files>supabase/migrations/20270712000001_user_traffic_attribution.sql, supabase/migrations/20270712000002_channel_groups.sql, supabase/migrations/20270712000003_referrer_channel_rules_seed.sql, supabase/migrations/20270712000006_traffic_taxonomy_rls.sql</files>
  <action>
Create four migrations following the verbatim shape of `supabase/migrations/20270601300004_p30_matviews_and_cron.sql` (table + RLS + indexes section) and `supabase/migrations/20270601100011_resolve_clinic_slug_rpc.sql` (SECDEF + `set search_path = pg_catalog, public, extensions`):

**20270712000001_user_traffic_attribution.sql** — per D-13 column list:
- `create table public.user_traffic_attribution` with `anon_id text not null unique`, `user_id uuid references auth.users(id) on delete cascade`, `org_id uuid references public.organizations(id) on delete set null`, all `first_touch_*` columns (source, medium, campaign, term, content, referrer, landing_path, channel_group text, at timestamptz not null), all `last_touch_*` columns (same shape), `updated_at timestamptz default now()`, `created_at timestamptz default now()`.
- Indexes: `create unique index utat_anon_id_uq on public.user_traffic_attribution(anon_id);` `create index utat_user_id_idx on public.user_traffic_attribution(user_id) where user_id is not null;` `create index utat_org_id_last_touch_idx on public.user_traffic_attribution(org_id, last_touch_at) where org_id is not null;`
- `alter table public.user_traffic_attribution enable row level security;` (policies land in migration 6 to keep concerns single).

**20270712000002_channel_groups.sql** — per D-01 + RESEARCH Pattern 3 (lines 348-398):
- `create table public.channel_groups` with `id uuid primary key default gen_random_uuid()`, `label text not null unique`, `priority int not null`, `match_rule_jsonb jsonb not null`, `attribution_window_days int not null default 30 check (attribution_window_days between 1 and 90)`, `is_default_fallback boolean not null default false`, `created_at` + `updated_at`.
- `create unique index channel_groups_default_fallback_uq on public.channel_groups(is_default_fallback) where is_default_fallback;` (partial-index expression IS IMMUTABLE — verified, just a column reference; per `reference_supabase_migration_gotchas`).
- Seed 8 default rows verbatim per RESEARCH §Pattern 3 (Paid Search priority 10, Paid Social 11, Email 20, Affiliate 30, Organic Search 40, Organic Social 41, Referral 50, Direct 99 with `is_default_fallback=true`).
- `create or replace function public.classify_channel_group(p_utm_source text, p_utm_medium text, p_utm_campaign text) returns text language sql stable set search_path = pg_catalog, public, extensions as $$ ... $$;` — body per RESEARCH §Pattern 3 lines 360-381 (AND across keys, OR within key, `order by priority asc limit 1`).
- `alter table public.channel_groups enable row level security;` (policies land in migration 6).

**20270712000003_referrer_channel_rules_seed.sql** — per D-03:
- `create table public.referrer_channel_rules` with `id uuid primary key default gen_random_uuid()`, `referrer_domain text not null unique`, `channel_group_label text not null references public.channel_groups(label) on update cascade`, `priority int not null default 50`, `created_at` + `updated_at`.
- `create unique index rcr_domain_uq on public.referrer_channel_rules(referrer_domain);`
- Seed 80 rows from Snowplow `referer-parser` (https://github.com/snowplow-referer-parser/snowplow-referer-parser/blob/master/resources/referers.yml). Use the canonical labels: Search engines → 'Organic Search' (google.com, bing.com, duckduckgo.com, yahoo.com, baidu.com, yandex.ru, ecosia.org, brave.com, startpage.com, kagi.com, qwant.com — extend to 25 search domains); Social → 'Organic Social' (facebook.com, m.facebook.com, l.facebook.com, instagram.com, l.instagram.com, x.com, t.co, twitter.com, mobile.twitter.com, tiktok.com, linkedin.com, lnkd.in, reddit.com, old.reddit.com, m.reddit.com, pinterest.com, threads.net, mastodon.social, bsky.app — extend to 25 social domains); Email → 'Email' (mail.google.com, mail.yahoo.com, outlook.com, outlook.live.com, mail.ru, m.yandex.ru — extend to 15 mail domains); Other notable → 'Referral' (news.ycombinator.com, producthunt.com, github.com, medium.com — extend to 15 referral domains). Target 80 rows total. Hard-code these as `insert into public.referrer_channel_rules (referrer_domain, channel_group_label, priority) values ...` — one INSERT statement with 80 value tuples (the file may exceed 100 lines).
- Wrapper SQL fn: `create or replace function public.classify_channel_group_with_referrer(p_utm_source text, p_utm_medium text, p_utm_campaign text, p_referrer_host text) returns text language sql stable set search_path = pg_catalog, public, extensions as $$ select coalesce( public.classify_channel_group(p_utm_source, p_utm_medium, p_utm_campaign), (select channel_group_label from public.referrer_channel_rules where referrer_domain = p_referrer_host order by priority asc limit 1), 'Direct' ); $$;`
- `alter table public.referrer_channel_rules enable row level security;` (policies land in migration 6).

**20270712000006_traffic_taxonomy_rls.sql** — per D-12 + project memory `reference_supabase_project`:
- Three policies on `user_traffic_attribution`:
  - `create policy utat_admin_all on public.user_traffic_attribution for all using (public.is_admin()) with check (public.is_admin());`
  - `create policy utat_org_clinician_select on public.user_traffic_attribution for select using (org_id is not null and public._is_org_clinician(org_id, auth.uid()));`
  - `create policy utat_self_user on public.user_traffic_attribution for select using (user_id = auth.uid());`
- Two policies on `channel_groups`: admin-only ALL (`using (public.is_admin())`). No clinic_owner / no anon access.
- Two policies on `referrer_channel_rules`: admin-only ALL (`using (public.is_admin())`). No clinic_owner / no anon access.
- Explicit `revoke all on public.user_traffic_attribution from anon, authenticated;` `grant select on public.user_traffic_attribution to authenticated;` (RLS narrows further per role) — mirror P30 matview-revoke pattern.

**Migration filename strict regex** per project memory `reference_supabase_migration_filename_regex`: 14 digits + underscore + name. The four files above conform.

**Pre-merge collision check** per project memory `reference_migration_timestamp_collision_precheck`: `ls supabase/migrations/20270712*.sql 2>/dev/null | wc -l` — expect 0 before this plan ships; abort and bump timestamps if any exist.

**Do NOT push these migrations yet.** Plan 51-10 owns the BLOCKING `supabase db push --linked` step. This plan only writes files + commits.
  </action>
  <verify>
    <automated>ls supabase/migrations/20270712000001_user_traffic_attribution.sql supabase/migrations/20270712000002_channel_groups.sql supabase/migrations/20270712000003_referrer_channel_rules_seed.sql supabase/migrations/20270712000006_traffic_taxonomy_rls.sql 2>&1 | grep -v "No such file" | wc -l | grep -E "^[[:space:]]*4$" && grep -c "create table public.user_traffic_attribution" supabase/migrations/20270712000001_user_traffic_attribution.sql && grep -c "is_default_fallback" supabase/migrations/20270712000002_channel_groups.sql && grep -v '^--' supabase/migrations/20270712000003_referrer_channel_rules_seed.sql | grep -c "google.com\|facebook.com\|reddit.com"</automated>
  </verify>
  <done>All four migrations exist, table creates compile-clean against a Postgres 15 syntax check (`psql --no-psqlrc -X --echo-errors -f` against a local stub OR Deno-side SQL parse), and seed list contains ≥60 referrer rows.</done>
</task>

<task type="auto">
  <name>Task 3: Migration — is_retained SECDEF helper + UPSERT RPCs + events.ts traffic_* event defs</name>
  <files>supabase/migrations/20270712000004_is_retained_secdef_helper.sql, supabase/migrations/20270712000005_upsert_traffic_attribution_rpcs.sql, leanshot/src/lib/analytics/events.ts</files>
  <action>
**20270712000004_is_retained_secdef_helper.sql** — per D-16 + CONTEXT §Specifics retention semantics:
- `create or replace function public.is_retained(p_user_id uuid, p_audience text, p_window_days int) returns boolean language plpgsql security definer stable set search_path = pg_catalog, public, extensions as $$ declare v_threshold timestamptz := now() - make_interval(days => p_window_days); begin if p_audience = 'consumer' then return exists(select 1 from public.events_mirror where distinct_id = p_user_id::text and created_at >= v_threshold); elsif p_audience = 'clinic-org' then return exists(select 1 from public.org_members om join public.subscriptions s on s.org_id = om.org_id where om.user_id = p_user_id and s.status = 'active' and s.is_paid_seat = true and s.activated_at <= v_threshold); elsif p_audience = 'affiliate' then return exists(select 1 from public.affiliate_referral_conversions where affiliate_user_id = p_user_id and converted_at >= v_threshold); else return false; end if; end; $$;`
- Note: `v_threshold` evaluates inside the function body (NOT inside a partial-index expression) — IMMUTABLE constraint per `reference_supabase_migration_gotchas` does NOT apply here (function is STABLE, used in matview SELECTs at refresh time, NOT in an index).
- `grant execute on function public.is_retained(uuid, text, int) to service_role;` `revoke execute on function public.is_retained(uuid, text, int) from public, anon, authenticated;`

**Schema-existence preflight** (memory `feedback_planner_missed_status_enum_widening`): before authoring this migration the executor must `supabase db query --linked` to verify tables `org_members`, `subscriptions` (with `is_paid_seat` + `activated_at` columns), and `affiliate_referral_conversions` (with `affiliate_user_id` + `converted_at` columns) all exist on the linked project. If any column name differs from what the helper expects, adjust the helper body to match the live schema; do NOT add new columns to other phases' tables. If a required table is entirely missing (e.g., affiliate_referral_conversions not yet shipped), set that audience branch to `return false;` and document the carry-over in `51-CARRY-OVER.md` instead of inventing the table.

**20270712000005_upsert_traffic_attribution_rpcs.sql** — per D-13 + PATTERNS §Edge Fn analog (first-touch immutability via ON CONFLICT):
- RPC 1 — `upsert_traffic_attribution`:
  ```
  create or replace function public.upsert_traffic_attribution(
    p_anon_id text,
    p_org_id uuid,
    p_utm_source text, p_utm_medium text, p_utm_campaign text,
    p_utm_term text, p_utm_content text,
    p_referrer text, p_landing_path text,
    p_channel_group text,
    p_now timestamptz
  ) returns void
  language sql security definer
  set search_path = pg_catalog, public, extensions
  as $$
    insert into public.user_traffic_attribution (
      anon_id, org_id,
      first_touch_source, first_touch_medium, first_touch_campaign, first_touch_term, first_touch_content,
      first_touch_referrer, first_touch_landing_path, first_touch_channel_group, first_touch_at,
      last_touch_source, last_touch_medium, last_touch_campaign, last_touch_term, last_touch_content,
      last_touch_referrer, last_touch_landing_path, last_touch_channel_group, last_touch_at,
      updated_at
    ) values (
      p_anon_id, p_org_id,
      p_utm_source, p_utm_medium, p_utm_campaign, p_utm_term, p_utm_content,
      p_referrer, p_landing_path, p_channel_group, p_now,
      p_utm_source, p_utm_medium, p_utm_campaign, p_utm_term, p_utm_content,
      p_referrer, p_landing_path, p_channel_group, p_now,
      p_now
    )
    on conflict (anon_id) do update set
      org_id = coalesce(public.user_traffic_attribution.org_id, excluded.org_id),
      last_touch_source = excluded.last_touch_source,
      last_touch_medium = excluded.last_touch_medium,
      last_touch_campaign = excluded.last_touch_campaign,
      last_touch_term = excluded.last_touch_term,
      last_touch_content = excluded.last_touch_content,
      last_touch_referrer = excluded.last_touch_referrer,
      last_touch_landing_path = excluded.last_touch_landing_path,
      last_touch_channel_group = excluded.last_touch_channel_group,
      last_touch_at = excluded.last_touch_at,
      updated_at = excluded.updated_at;
  $$;
  ```
  First-touch immutability is preserved: the ON CONFLICT clause only updates last_touch_* + org_id (when previously null) + updated_at. First_touch_* columns are NEVER overwritten.
- RPC 2 — `claim_traffic_attribution(p_anon_id text, p_user_id uuid)` — called from `merge-anon-session` Edge Fn at signup. Sets `user_id = p_user_id` WHERE `anon_id = p_anon_id` AND `user_id IS NULL`. Returns void.
- Grants: `revoke execute on function public.upsert_traffic_attribution(...) from public, anon, authenticated;` `grant execute on function public.upsert_traffic_attribution(...) to service_role;` (same for claim_).

**leanshot/src/lib/analytics/events.ts** — additive event defs per PATTERNS §events.ts:
- Read the existing file first to confirm the `EventDef` / `makeEvent` factory signature and follow it verbatim. Add four new exports: `traffic_visit`, `traffic_signup`, `traffic_activation`, `traffic_paid`. Each carries properties: `{ channel_group: string; audience: 'consumer' | 'clinic-org' | 'affiliate'; landing_path?: string; page_variant_id?: string | null; }` (signup/activation/paid additionally include `user_id?: string`). DO NOT delete or rename existing exports — `eslint-rules/additive-only-events.js` enforces.
- No new ESLint rule needed; the additive-only rule covers traffic_* exports automatically.
  </action>
  <verify>
    <automated>ls supabase/migrations/20270712000004_is_retained_secdef_helper.sql supabase/migrations/20270712000005_upsert_traffic_attribution_rpcs.sql 2>&1 | grep -v "No such file" | wc -l | grep -E "^[[:space:]]*2$" && grep -c "create or replace function public.is_retained" supabase/migrations/20270712000004_is_retained_secdef_helper.sql && grep -c "on conflict (anon_id)" supabase/migrations/20270712000005_upsert_traffic_attribution_rpcs.sql && grep -E "traffic_visit|traffic_signup|traffic_activation|traffic_paid" leanshot/src/lib/analytics/events.ts | wc -l | grep -E "^[[:space:]]*[4-9]"</automated>
  </verify>
  <done>is_retained migration creates the SECDEF helper; upsert_traffic_attribution preserves first-touch via ON CONFLICT excluding first_touch_* columns; events.ts exports all 4 traffic_* defs; `npm run lint -- leanshot/src/lib/analytics/events.ts` returns 0 (additive-only rule passes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon role → user_traffic_attribution | anon must NEVER read this table; only service_role writes |
| clinic_owner role → cross-org rows | clinic_owner must see ONLY their own org_id rows |
| admin role → all rows | is_admin() gates full access |
| service_role → upsert_traffic_attribution | only service_role can call the SECDEF RPC; anon/authenticated denied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Information disclosure | user_traffic_attribution | mitigate | RLS policies in migration 06 + `revoke all from anon, authenticated` + SECDEF RPCs are the only write path |
| T-51-02 | Tampering | first_touch_* columns | mitigate | upsert_traffic_attribution RPC's ON CONFLICT clause excludes all first_touch_* columns from UPDATE; immutable once written |
| T-51-03 | Elevation of privilege | clinic_owner seeing other orgs | mitigate | `_is_org_clinician(org_id, auth.uid())` gate in utat_org_clinician_select policy; cross-tenant deny test required at Wave 5 |
| T-51-04 | Information disclosure | channel_groups / referrer_channel_rules | mitigate | admin-only RLS; clinic_owner sees no taxonomy admin surface (UI denies via role guard) |
| T-51-05 | Repudiation | who edited a channel_group | accept | v1.3 scope ends at RLS; full audit-log for taxonomy edits → v1.4 (would extend P25 audit_logs pattern) |
| T-51-06 | Denial of service | classify_channel_group runs per-row at matview refresh | accept | matview refresh is offline (cron-driven); ~6 rules × matview row count is bounded; no online latency cost |
| T-51-07 | Information disclosure (PHI) | utm_term / utm_content carrying PHI in URL | mitigate | recorder Fn (Plan 51-02) length-redacts + path-redacts before write; this plan's table accepts whatever the recorder writes — defense in depth is at the boundary, not in the table |
</threat_model>

<verification>
- `grep -c "^- \[ \] \*\*TRAFFIC-" leanshot/.planning/REQUIREMENTS.md` returns exactly 12 (all 12 TRAFFIC-NN entries are unchecked).
- All six migrations parse via `psql --no-psqlrc -X --echo-errors -f` against a local Postgres instance OR are reviewed by hand for syntax compliance (no `supabase db push` in this plan — Plan 51-10 pushes).
- `npm run lint -- leanshot/src/lib/analytics/events.ts` exits 0 (additive-only check passes; no removed exports).
- `npm run typecheck` (i.e., `tsc -p leanshot/tsconfig.app.json --noEmit`) exits 0.
- Schema-existence preflight: `supabase db query --linked "select 1 from information_schema.columns where table_name = 'subscriptions' and column_name in ('is_paid_seat', 'activated_at') limit 1"` confirms upstream columns exist before is_retained helper references them; if not, helper branch is set to `return false` and a carry-over note is filed.
</verification>

<success_criteria>
- REQUIREMENTS.md contains 12 TRAFFIC-NN entries, each addressable from a downstream plan.
- 6 migration files exist with correct timestamps (20270712000001..20270712000006), follow the strict 14-digit filename regex, and contain no `$$` nesting issues.
- All RLS policies use existing SECDEF helpers `is_admin()` + `_is_org_clinician()` (no new helper invented).
- `upsert_traffic_attribution` RPC's ON CONFLICT clause preserves first-touch immutability by excluding first_touch_* columns from UPDATE — verified by inspection.
- `events.ts` exports 4 new traffic_* event defs; eslint additive-only rule passes.
- Plan does NOT execute `supabase db push` — Plan 51-10 owns that step.
</success_criteria>

<output>
After completion, create `.planning/phases/51-full-traffic-conversion-tracking-system-unified-dashboard-ut/51-01-SUMMARY.md` summarizing: (a) 12 TRAFFIC-NN REQ-IDs added to REQUIREMENTS.md; (b) 6 migrations written (filenames + one-line role); (c) events.ts +4 additive exports; (d) any schema-existence preflight findings that adjusted the is_retained helper.
</output>
