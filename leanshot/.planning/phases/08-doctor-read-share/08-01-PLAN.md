---
phase: 08-doctor-read-share
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260701000001_audit_logs_share_columns.sql
  - supabase/migrations/20260701000002_shares_table.sql
  - supabase/migrations/20260701000003_share_rpcs.sql
  - supabase/migrations/20260701000004_share_snapshot_view.sql
  - supabase/functions/share/deno.json
  - supabase/functions/share/index.test.ts
  - e2e/rls-shares.test.ts
  - e2e/share-happy-path.spec.ts
  - e2e/share-revocation-drill.spec.ts
  - e2e/active-shares.spec.ts
  - e2e/share-print.spec.ts
  - src/components/share/SharePage.test.tsx
  - src/components/dashboard/settings/ActiveSharesSection.test.tsx
  - src/types/share.ts
autonomous: false  # supabase db push may require interactive prompt
requirements: [SHARE-01, SHARE-02, SHARE-03, SHARE-04, SHARE-05, SHARE-06]
mvp_slice: "Foundation slice — `shares` table + audit extension + snapshot view (no AI history) is the load-bearing primitive. Without this, no doctor view can render and no revocation can be enforced. Vertical: DB schema → RLS gate → Wave 0 test scaffolds. The 'user-visible' outcome on completion: a developer can manually `insert into shares (...)` via SQL and a Playwright fixture can confirm RLS blocks cross-tenant reads."

must_haves:
  truths:
    - "Postgres `shares` table exists with all load-bearing columns (id, user_id, label, token_hash, access_code_hash, expires_at, revoked_at, code_consumed_at, recipient_session_hash, failed_attempts_count, last_attempt_at, created_at)"
    - "`audit_logs` has new columns `actor_type` (enum), `share_id` (nullable FK), `recipient_ua_family`, `recipient_ip_family`; action check constraint extended with `'share_view'`; `recipient_ip_family` carries a CHECK constraint that allows only /16 or /48 buckets (no full dot-quad IPs)"
    - "Postgres view `share_snapshot_view` exists and contains zero references to `ai_messages` (grep-verifiable in migration SQL)"
    - "6 SECURITY DEFINER RPCs exist with `search_path = public, extensions, pg_catalog`: `create_share`, `revoke_share`, `redeem_share`, `verify_share_code`, `log_share_view`, `increment_share_attempt`"
    - "RLS enabled on `shares`; owner can SELECT own rows; NO authenticated INSERT/UPDATE/DELETE policy (RPCs are the only write path); service_role bypasses for Edge Function"
    - "Cross-tenant RLS impersonation proof passes — user B cannot SELECT user A's shares row"
    - "`supabase db push` succeeds against the linked project (`ytnsipxxmzgaebkqmokp`)"
    - "Wave 0 test files exist as scaffolds with failing/skipped tests (file existence verified by grep)"
    - "Shared TypeScript types exported from `src/types/share.ts` (Share, ShareSnapshot, CreateShareRequest, RedeemRequest, SnapshotResponse including the new `share_id` field per D-02(c))"
  artifacts:
    - path: "supabase/migrations/20260701000001_audit_logs_share_columns.sql"
      provides: "audit_logs schema extension (actor_type enum, share_id nullable FK placeholder, recipient_ua_family, recipient_ip_family with CHECK enforcing /16 or /48 only, action check expanded, partial index on actor_type='share_recipient')"
      contains: "create type public.audit_actor_type"
    - path: "supabase/migrations/20260701000002_shares_table.sql"
      provides: "shares table + RLS + indexes + FK back to audit_logs.share_id"
      contains: "create table public.shares"
    - path: "supabase/migrations/20260701000003_share_rpcs.sql"
      provides: "6 SECURITY DEFINER RPCs (create_share, revoke_share, redeem_share, verify_share_code, log_share_view, increment_share_attempt)"
      contains: "create or replace function public.create_share"
    - path: "supabase/migrations/20260701000004_share_snapshot_view.sql"
      provides: "share_snapshot_view (structural ai_messages exclusion per SC#3)"
      contains: "create or replace view public.share_snapshot_view"
    - path: "supabase/functions/share/deno.json"
      provides: "Deno test config for share Edge Function (Wave 0 scaffold)"
    - path: "supabase/functions/share/index.test.ts"
      provides: "Failing Deno test scaffold (test discovery glob compliant — `.test.ts` suffix per memory reference_deno_test_discovery.md)"
    - path: "e2e/rls-shares.test.ts"
      provides: "Cross-tenant RLS impersonation proof — REQUIRED by project rule (reference_supabase_project.md)"
      contains: "shares_select_own"
    - path: "src/types/share.ts"
      provides: "Shared TS types for Share, ShareSnapshot, CreateShareRequest, RedeemRequest, SnapshotResponse (with share_id field)"
  key_links:
    - from: "supabase/migrations/20260701000001_audit_logs_share_columns.sql"
      to: "audit_logs table (existing from Phase 7)"
      via: "ALTER TABLE additive columns"
      pattern: "alter table public.audit_logs add column"
    - from: "supabase/migrations/20260701000002_shares_table.sql"
      to: "audit_logs.share_id"
      via: "FK constraint added AFTER shares table exists (correct migration order)"
      pattern: "foreign key \\(share_id\\) references public.shares"
    - from: "supabase/migrations/20260701000003_share_rpcs.sql"
      to: "shares table"
      via: "SECURITY DEFINER write path"
      pattern: "set search_path = public, extensions, pg_catalog"
    - from: "supabase/migrations/20260701000004_share_snapshot_view.sql"
      to: "Phase 5/6 sync tables (injections, weights, meals, workouts, supplements, mood, sleep, symptoms, vials, settings, photos)"
      via: "SELECT joins"
      pattern: "from public\\.(injections|weights|meals|workouts|supplements|mood|sleep|symptoms|vials|settings|photos)"
    - from: "e2e/rls-shares.test.ts"
      to: "shares table RLS policies"
      via: "supabase-js with two test users"
      pattern: "createClient.*anon"
---

<objective>
Land all Postgres schema for Phase 8 in a single foundation slice — `shares` table, `audit_logs` extension, `share_snapshot_view`, 6 SECURITY DEFINER RPCs, and Wave 0 test scaffolds. Push migrations to the live Supabase project so downstream Edge Function (Plan 08-02) and UI (Plans 08-03/08-04) have a real schema to bind against.

Purpose: SC#3 (`ai_messages` structurally excluded) is enforced by the view definition — a reviewer can grep the migration SQL and verify zero `ai_messages` references. SC#5 (audit log of every doctor view) is enabled by the `log_share_view` RPC. SHARE-01..06 all transit through this schema; without the foundation, no doctor view can render and no revocation can be enforced. Per D-02(c), the snapshot JWT/cookie carries an opaque `share_id` (NOT patient_id), so the snapshot response contract surfaces `share_id` at the type layer.

Output: 4 SQL migrations pushed to `ytnsipxxmzgaebkqmokp`, 6 SECURITY DEFINER RPCs callable, cross-tenant RLS impersonation proof green, shared TS types exported (with `share_id` on `SnapshotResponse`), Wave 0 test files exist as scaffolds (failing or skipped — turn green in Waves 2/3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-doctor-read-share/08-CONTEXT.md
@.planning/phases/08-doctor-read-share/08-RESEARCH.md
@.planning/phases/08-doctor-read-share/08-VALIDATION.md

<!-- Existing Phase 7 migration patterns this plan REUSES verbatim -->
@/Users/karstenhaldan/minisite/supabase/migrations/20260601000001_audit_logs.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20260601000011_initiate_account_deletion_rpc.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20260601000015_account_delete_fix_search_path.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20260601000017_audit_trigger_suppress_guc.sql

<!-- Existing Phase 5/6 sync table list (informs view shape) -->
@/Users/karstenhaldan/minisite/leanshot/e2e/rls-injections.test.ts

<interfaces>
<!-- Shared types this plan creates. Plans 08-02/03/04 import from src/types/share.ts. -->

```typescript
// src/types/share.ts — exported, consumed by Plans 08-02, 08-03, 08-04

export type ShareStatus = 'active' | 'expired' | 'revoked' | 'pending-redeem';

export interface Share {
  id: string;
  user_id: string;
  label: string;
  expires_at: string;     // ISO timestamptz
  revoked_at: string | null;
  code_consumed_at: string | null;
  created_at: string;
  // hashes intentionally not exposed to browser
}

export interface CreateShareRequest {
  label: string;          // 1..80 chars
  expires_at: string;     // ISO timestamptz, must be > now()
}

export interface CreateShareResponse {
  share_id: string;
  raw_token: string;      // returned ONCE
  raw_code: string;       // 6-digit, returned ONCE
}

export interface RedeemRequest {
  token: string;
  code: string;           // 6 digit
}

export type RedeemError =
  | 'invalid-code'
  | 'rate-limited'
  | 'already-consumed'
  | 'not-found'
  | 'revoked'
  | 'expired';

export interface SnapshotResponse {
  snapshot: {
    user_id: string;
    patient_first_name: string;
    injections: Array<{ log_id: string; timestamp: string; medication: string; dose: number; unit: string; site: string }>;
    weights: Array<{ id: string; timestamp: string; weight_kg: number }>;
    symptoms: Array<{ id: string; timestamp: string; symptom: string; severity: number }>;
    photos: Array<{ id: string; timestamp: string; signed_url: string }>;
    // NOTE: ai_messages NEVER appears here (SC#3 structural exclusion)
  };
  expires_at: string;
  share_id: string;       // opaque share_id per D-02(c); used for print footer + display ID; NEVER the patient user_id
}

export type SnapshotError =
  | 'not-found'
  | 'requires-code'
  | 'invalid-session'
  | 'revoked'
  | 'expired';
```
</interfaces>

<schema_push_note>
This plan modifies schema-relevant files (4 new `supabase/migrations/*.sql`). The blocking task at the end of this plan MUST run `supabase db push` against the linked project (`ytnsipxxmzgaebkqmokp`). Per memory `project_worktree_supabase_cli.md` the orchestrator handles the worktree-vs-main-repo migration file mirror.

Set `autonomous: false` because `supabase db push` may prompt for confirmation on destructive operations or first-time auth. If `SUPABASE_ACCESS_TOKEN` is set in the env, the prompt is suppressed.
</schema_push_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 test scaffolds + shared types + Deno test config</name>
  <files>
    src/types/share.ts,
    supabase/functions/share/deno.json,
    supabase/functions/share/index.test.ts,
    e2e/rls-shares.test.ts,
    e2e/share-happy-path.spec.ts,
    e2e/share-revocation-drill.spec.ts,
    e2e/active-shares.spec.ts,
    e2e/share-print.spec.ts,
    src/components/share/SharePage.test.tsx,
    src/components/dashboard/settings/ActiveSharesSection.test.tsx
  </files>
  <read_first>
    .planning/phases/08-doctor-read-share/08-VALIDATION.md (Wave 0 checklist — 10 items),
    .planning/phases/08-doctor-read-share/08-RESEARCH.md §"Wave 0 Gaps",
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-injections.test.ts (RLS proof template),
    /Users/karstenhaldan/minisite/supabase/functions/ai-chat/deno.json (Deno config template — replicate exactly),
    .planning/phases/08-doctor-read-share/08-CONTEXT.md §"Reusable Assets",
    memory reference_deno_test_discovery.md (Deno glob — file MUST be `index.test.ts`, NOT `index-test.ts`),
    memory reference_playwright_state_seeding.md (Playwright fixtures use `addInitScript`, not `page.goto + evaluate`),
    memory reference_supabase_project.md (cross-tenant RLS impersonation proof is REQUIRED)
  </read_first>
  <action>
    Create the Wave 0 test scaffolds and shared types. ALL test files in this task should be failing or skipped — they get implemented in Waves 2/3. Existence is what matters for Wave 0 closure.

    **`src/types/share.ts`** — copy the TypeScript types from the `<interfaces>` block in this plan's `<context>` verbatim. Export every type. **CRITICAL:** `SnapshotResponse` MUST include the `share_id: string` field per D-02(c) — Plan 08-06's print footer reads it, and Plan 08-02's `/snapshot` handler populates it from the redeem/cookie lookup. Place the JSDoc note at the TOP of the file using `/** … */` block-comment syntax (so it survives the `^(//|*|/\*)` grep filter the verify step uses):

    ```typescript
    /**
     * Shared types for Phase 8 doctor read-share.
     * AI conversation history (ai_messages) is structurally excluded per SC#3 —
     * never add fields referencing it here.
     */
    import …
    ```

    **`supabase/functions/share/deno.json`** — mirror `supabase/functions/ai-chat/deno.json` exactly (same `imports`, same `tasks`). This is the Wave 0 scaffold; Plan 08-02 fills out the actual function source.

    **`supabase/functions/share/index.test.ts`** — minimal Deno test that imports nothing yet and asserts `Deno.test('share function scaffold — replaced by Plan 08-02', () => { /* TODO Wave 2 */ });`. Filename MUST be `index.test.ts` (Deno glob `{*_,*.,}test.*` — `.test.ts` matches; `-test.ts` does NOT — per memory `reference_deno_test_discovery.md`).

    **`e2e/rls-shares.test.ts`** — copy structure from `e2e/rls-injections.test.ts`. Implement ONE real test in this task: `'cross-tenant — user B cannot SELECT user A shares row'`. Test setup: create two test users (alice + bob) via supabase admin client (reuse `e2e/fixtures/auth.ts` if it exists, else inline the pattern). Insert a share for alice via `create_share` RPC. Query `shares` as bob — assert zero rows. This test MUST pass before this plan completes (it's the project-rule gate per memory `reference_supabase_project.md`). The remaining RLS tests (audit row write, snapshot view structural exclusion, code single-use) are stubs that throw `new Error('TODO Plan 08-XX')` — to be filled by later plans.

    **`e2e/share-happy-path.spec.ts`** — Playwright spec scaffold with test stubs: `test.skip('create share via Settings', ...)`, `test.skip('enter code → see snapshot', ...)`, `test.skip('snapshot includes chart + injections + symptoms + photos + weight + doctor report', ...)`. Use `test.skip` so CI passes; remove `.skip` in Plans 08-03/08-04.

    **`e2e/share-revocation-drill.spec.ts`** — Playwright spec scaffold with 4 skipped tests (one per failure mode: token cache, HTTP cache, JWT TTL via DB revoke, forwarded link). Plan 08-05 will implement.

    **`e2e/active-shares.spec.ts`** — Playwright spec scaffold for patient-side audit log surface. Plan 08-03 will implement.

    **`e2e/share-print.spec.ts`** — Playwright spec scaffold for print-mode disclaimer survival. Plan 08-06 will implement.

    **`src/components/share/SharePage.test.tsx`** — Vitest scaffold with `it.skip('snapshot contract — no ai_* keys', ...)`. Use `import { describe, it } from 'vitest'`. Plan 08-04 implements.

    **`src/components/dashboard/settings/ActiveSharesSection.test.tsx`** — Vitest scaffold with `it.skip('aggregates view count + last_viewed correctly', ...)`. Plan 08-03 implements.

    Commit using pathspec per memory `feedback_parallel_executor_git_isolation.md` even though this plan is solo (good hygiene): `git commit -- src/types/share.ts supabase/functions/share/deno.json supabase/functions/share/index.test.ts e2e/rls-shares.test.ts e2e/share-happy-path.spec.ts e2e/share-revocation-drill.spec.ts e2e/active-shares.spec.ts e2e/share-print.spec.ts src/components/share/SharePage.test.tsx src/components/dashboard/settings/ActiveSharesSection.test.tsx`.
  </action>
  <verify>
    <automated>test -f src/types/share.ts && test -f supabase/functions/share/deno.json && test -f supabase/functions/share/index.test.ts && test -f e2e/rls-shares.test.ts && test -f e2e/share-happy-path.spec.ts && test -f e2e/share-revocation-drill.spec.ts && test -f e2e/active-shares.spec.ts && test -f e2e/share-print.spec.ts && test -f src/components/share/SharePage.test.tsx && test -f src/components/dashboard/settings/ActiveSharesSection.test.tsx && grep -Ev '^(//|\*|/\*)' src/types/share.ts | grep -c 'ai_messages\|aiHistory\|ai_history' | grep -q '^0$' && grep -q 'share_id: string' src/types/share.ts</automated>
  </verify>
  <acceptance_criteria>
    All 10 Wave 0 files exist. `src/types/share.ts` exports Share, CreateShareRequest, CreateShareResponse, RedeemRequest, RedeemError, SnapshotResponse (including `share_id: string`), SnapshotError, ShareStatus. `supabase/functions/share/deno.json` is valid JSON and mirrors ai-chat shape. `supabase/functions/share/index.test.ts` filename matches Deno discovery glob. Type file contains ZERO `ai_messages` / `aiHistory` references in non-comment lines (structural exclusion at the type layer). The `share_id` field on `SnapshotResponse` is present (BL-1).
  </acceptance_criteria>
  <done>Type definitions compile cleanly; test scaffolds detected by their respective runners (`npm run test -- --run --listFiles | grep share` returns matches); Deno glob picks up `index.test.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Schema migrations — audit_logs extension + shares table + 6 RPCs + snapshot view</name>
  <files>
    supabase/migrations/20260701000001_audit_logs_share_columns.sql,
    supabase/migrations/20260701000002_shares_table.sql,
    supabase/migrations/20260701000003_share_rpcs.sql,
    supabase/migrations/20260701000004_share_snapshot_view.sql
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/supabase/migrations/20260601000001_audit_logs.sql (BASE schema being extended — read columns + RLS policies + index pattern),
    /Users/karstenhaldan/minisite/supabase/migrations/20260601000002_audit_triggers.sql (existing audit_trigger function — note `share_view` action MUST be added to its allowed list if it has one),
    /Users/karstenhaldan/minisite/supabase/migrations/20260601000011_initiate_account_deletion_rpc.sql (SECURITY DEFINER pattern — VERBATIM template),
    /Users/karstenhaldan/minisite/supabase/migrations/20260601000015_account_delete_fix_search_path.sql (exact search_path string `public, extensions, pg_catalog` per Pitfall 1),
    /Users/karstenhaldan/minisite/supabase/migrations/20260601000017_audit_trigger_suppress_guc.sql (GUC bypass — NOT used for share_view writes but understand the mechanism),
    .planning/phases/08-doctor-read-share/08-RESEARCH.md §"Code Examples" (4 SQL snippets — adapt, do NOT copy verbatim per A5 random() warning),
    memory reference_supabase_migration_gotchas.md (4 deviations to apply preventively)
  </read_first>
  <action>
    Create 4 sequential migrations. ORDER IS LOAD-BEARING — audit_logs columns must exist BEFORE shares FK references them, but `shares` table must exist BEFORE the FK constraint on `audit_logs.share_id` resolves. Solution per RESEARCH.md: add column WITHOUT FK in migration 01, add FK in migration 02 after `shares` table created.

    **`20260701000001_audit_logs_share_columns.sql`**:
    - Create enum `public.audit_actor_type` with values `'user'`, `'share_recipient'`, `'system'`. Use `create type if not exists` pattern OR `do $$ begin ... exception when duplicate_object then null; end $$;` (mirror existing migration idioms).
    - `alter table public.audit_logs add column actor_type public.audit_actor_type not null default 'user'`
    - `alter table public.audit_logs add column share_id uuid` (NO FK yet — added in migration 02 after shares exists)
    - `alter table public.audit_logs add column recipient_ua_family text`
    - `alter table public.audit_logs add column recipient_ip_family text`
    - **ME-2 CHECK constraint:** `alter table public.audit_logs add constraint audit_logs_recipient_ip_family_bucketed_chk check (recipient_ip_family is null or recipient_ip_family ~ '^([0-9]{1,3}\.[0-9]{1,3}\.0\.0/16|[0-9a-fA-F:]+::/48|unknown)$')` — explicitly forbid full dot-quad IPs; allow only /16 IPv4 buckets, /48 IPv6 buckets, or the sentinel `unknown`. Document inline: this prevents PII-grade IP precision from ever landing in audit rows even if upstream parsing regresses.
    - Drop existing `audit_logs_action_check` constraint; add new one allowing `'insert', 'update', 'delete', 'account_deleted_initiated', 'account_deleted_finalized', 'share_view'`. READ the existing constraint first to preserve all currently-allowed values.
    - Partial index on `(share_id, timestamp desc) where actor_type = 'share_recipient'` — IMMUTABLE-safe (literal + column comparisons only — per Pitfall 2).
    - If existing `audit_trigger()` function in `20260601000002_audit_triggers.sql` has a hard-coded action whitelist, extend it with `'share_view'` — but `share_view` rows are written directly via `log_share_view` RPC, NOT via trigger, so the trigger function may not need changes. Verify by reading `20260601000002_audit_triggers.sql` first.

    **`20260701000002_shares_table.sql`**:
    - `create table public.shares` with all columns from must_haves truths line 1, plus types from RESEARCH §"Code Examples" `shares table` snippet. Constraints: `check (char_length(label) between 1 and 80)`, `check (expires_at > created_at)`.
    - `create unique index shares_token_hash_idx on public.shares (token_hash)`.
    - `create index shares_user_active_idx on public.shares (user_id, expires_at) where revoked_at is null` (IMMUTABLE-safe — no `now()` in WHERE).
    - `alter table public.shares enable row level security`.
    - `create policy shares_select_own on public.shares for select using (auth.uid() = user_id)`.
    - DO NOT add INSERT/UPDATE/DELETE policies — RPCs are the only write path; service_role bypasses RLS for Edge Function reads.
    - `alter table public.audit_logs add constraint audit_logs_share_id_fkey foreign key (share_id) references public.shares(id) on delete set null` (FK added HERE after shares table exists).
    - **`audit_logs` cascade-safety note**: when a share row is deleted (via `on delete cascade` from `auth.users` if patient account deleted, OR via `on delete set null` here), audit_logs.share_id is set to NULL — preserves audit history per Phase 7 retention. Memory `reference_supabase_migration_gotchas.md` cascade-DELETE-audit-logs GUC suppression NOT needed here because `set null` doesn't fire DELETE cascade on audit_logs rows.

    **`20260701000003_share_rpcs.sql`**: 6 SECURITY DEFINER plpgsql functions. ALL include `set search_path = public, extensions, pg_catalog` (per Pitfall 1; this is non-negotiable for `digest`/`crypt`/`gen_random_bytes` resolution on managed Supabase).

    1. `public.create_share(p_label text, p_expires_at timestamptz) returns table (share_id uuid, raw_token text, raw_code text)`:
       - Read `auth.uid()`; raise `28000` if null.
       - Raise `22023` if `p_expires_at <= now()` or `char_length(p_label) NOT between 1 and 80`.
       - Generate `v_token`: `replace(replace(encode(gen_random_bytes(16), 'base64'), '+', '-'), '/', '_')` — strip `=` padding too: `rtrim(..., '=')`.
       - Generate `v_code`: **DO NOT use `random()`** (per RESEARCH Assumption A5). Use CSPRNG: `lpad(((get_byte(gen_random_bytes(4), 0) * 16777216 + get_byte(gen_random_bytes(4), 1) * 65536 + get_byte(gen_random_bytes(4), 2) * 256 + get_byte(gen_random_bytes(4), 3)) % 1000000)::text, 6, '0')` OR simpler: `lpad((('x' || encode(gen_random_bytes(4), 'hex'))::bit(32)::int % 1000000 + 1000000) % 1000000)::text, 6, '0')`. Pick one and document inline why.
       - INSERT into shares with `token_hash = encode(digest(v_token, 'sha256'), 'hex')`, `access_code_hash = crypt(v_code, gen_salt('bf', 10))`.
       - `return query select v_id, v_token, v_code` — ONCE.
       - `revoke all on function ... from public; grant execute ... to authenticated`.

    2. `public.revoke_share(p_share_id uuid) returns void`:
       - Reads `auth.uid()`; raises 28000 if null.
       - `update public.shares set revoked_at = now(), recipient_session_hash = null where id = p_share_id and user_id = auth.uid() and revoked_at is null` (Open Question 3 — null recipient_session_hash for defense-in-depth).
       - If 0 rows affected, raise `P0002` ('share not found or not owned or already revoked').
       - grant execute to authenticated.

    3. `public.redeem_share(p_share_id uuid, p_recipient_session_hash text, p_ua_family text, p_ip_family text) returns void`:
       - service_role-only (no `authenticated` grant). Edge Function calls this with admin client.
       - `update public.shares set code_consumed_at = now(), recipient_session_hash = p_recipient_session_hash where id = p_share_id and code_consumed_at is null and revoked_at is null and expires_at > now()`.
       - If 0 rows affected, raise `P0002` ('share unredeemable').
       - revoke from public; grant execute to service_role only.

    4. **`public.verify_share_code(p_share_id uuid, p_code text) returns boolean` (BL-2 — was previously called out as a mid-Wave 2 invention; lives here in Wave 1):**
       - service_role-only.
       - `select (access_code_hash = crypt(p_code, access_code_hash)) into v_ok from public.shares where id = p_share_id;` — bcrypt compare via `crypt()` against the stored `access_code_hash`. Returns `true` on match, `false` on mismatch, NULL → `false` if row not found.
       - **MUST** include `set search_path = public, extensions, pg_catalog` (so `crypt()` resolves on managed Supabase — Pitfall 1).
       - revoke from public; grant execute to service_role only.

    5. `public.log_share_view(p_share_id uuid, p_ua_family text, p_ip_family text) returns void`:
       - service_role-only.
       - Resolve `v_owner` from `select user_id from shares where id = p_share_id`; raise P0002 if null.
       - `insert into public.audit_logs (user_id, user_id_hash, table_name, row_id, action, actor_type, share_id, recipient_ua_family, recipient_ip_family) values (v_owner, encode(digest(v_owner::text, 'sha256'), 'hex'), 'shares', p_share_id::text, 'share_view', 'share_recipient', p_share_id, p_ua_family, p_ip_family)`. NOTE: `user_id_hash` column name MUST match existing `audit_logs` schema — read `20260601000001_audit_logs.sql` first to verify exact column name (may be `user_id_hash` or `actor_hash`). NOTE on `p_ip_family`: callers MUST pass a bucketed value (/16 or /48 or `unknown`); the ME-2 CHECK constraint on `audit_logs.recipient_ip_family` will reject full dot-quad IPs.
       - revoke from public; grant execute to service_role only.

    6. `public.increment_share_attempt(p_share_id uuid) returns int` (per Pitfall 5 — per-share rate limit, NOT per-user):
       - `update public.shares set failed_attempts_count = failed_attempts_count + 1, last_attempt_at = now() where id = p_share_id returning failed_attempts_count` → return the new count.
       - service_role-only; called from Edge Function on each failed `verify_share_code` compare.

    **`20260701000004_share_snapshot_view.sql`**:
    - `create or replace view public.share_snapshot_view as ...` joining the following tables ONLY: `injections`, `weights`, `meals`, `workouts`, `supplements`, `mood_logs` (or `mood`), `sleep_logs` (or `sleep`), `symptoms`, `vials`, `settings`, `photos`. EXACT table names: read `supabase/migrations/` to confirm; Phase 6 plans 06-03 and 06-04 created these — find them in `supabase/migrations/` and use the exact table names declared there.
    - **DO NOT join `public.ai_messages`** — this is the SC#3 structural-exclusion contract. The migration MUST be grep-verifiable: `grep ai_messages 20260701000004_share_snapshot_view.sql` returns ZERO matches.
    - View shape: 11 LATERAL JOINs returning JSON arrays per entity type, or a single UNION ALL — planner's discretion on SQL shape. Recommendation: use `jsonb_agg` per table grouped by `user_id` so the Edge Function receives one row per user with arrays of entities, simplifying the snapshot JSON contract in `SnapshotResponse`.
    - Example shape (adapt):
      ```sql
      create or replace view public.share_snapshot_view as
      select
        u.id as user_id,
        u.raw_user_meta_data->>'first_name' as patient_first_name,
        (select jsonb_agg(row_to_json(i.*)) from public.injections i where i.user_id = u.id) as injections,
        (select jsonb_agg(row_to_json(w.*)) from public.weights w where w.user_id = u.id) as weights,
        -- ... 9 more entity arrays ...
        -- ai_messages INTENTIONALLY OMITTED per SC#3
      from auth.users u;
      ```
    - Grant SELECT on the view to `service_role` only (Edge Function bypass). DO NOT grant to authenticated.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260701000001_audit_logs_share_columns.sql && test -f supabase/migrations/20260701000002_shares_table.sql && test -f supabase/migrations/20260701000003_share_rpcs.sql && test -f supabase/migrations/20260701000004_share_snapshot_view.sql && grep -c 'ai_messages' supabase/migrations/20260701000004_share_snapshot_view.sql | grep -q '^0$' && grep -c 'set search_path = public, extensions, pg_catalog' supabase/migrations/20260701000003_share_rpcs.sql | awk '{ if ($1 >= 6) exit 0; else exit 1 }' && grep -c 'random()' supabase/migrations/20260701000003_share_rpcs.sql | grep -q '^0$' && grep -q 'verify_share_code' supabase/migrations/20260701000003_share_rpcs.sql && grep -q 'audit_logs_recipient_ip_family_bucketed_chk' supabase/migrations/20260701000001_audit_logs_share_columns.sql</automated>
  </verify>
  <acceptance_criteria>
    All 4 migration files exist. `share_snapshot_view.sql` has ZERO occurrences of `ai_messages` (structural SC#3 enforcement). All **6** RPCs in `share_rpcs.sql` carry `set search_path = public, extensions, pg_catalog` (Pitfall 1 — verified by count ≥ 6). NO `random()` call in `share_rpcs.sql` (CSPRNG only, per Assumption A5). All partial indexes use only column-vs-literal comparisons (Pitfall 2). `audit_logs.share_id` FK is declared in migration 02, not 01 (ordering correctness). `audit_logs.recipient_ip_family` carries the ME-2 CHECK constraint forbidding full dot-quad IPs. `verify_share_code` (BL-2) is one of the 6 RPCs.
  </acceptance_criteria>
  <done>All 4 migration SQL files validate via `npx supabase db lint` if available, otherwise `psql --variable=ON_ERROR_STOP=1` dry-parse (no syntax errors). Constraints + indexes + RLS policies + grants present per spec above.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 [BLOCKING]: Push migrations to live Supabase + verify RLS + types regen</name>
  <files>
    supabase/migrations/20260701000001_audit_logs_share_columns.sql,
    supabase/migrations/20260701000002_shares_table.sql,
    supabase/migrations/20260701000003_share_rpcs.sql,
    supabase/migrations/20260701000004_share_snapshot_view.sql,
    src/types/supabase.ts
  </files>
  <read_first>
    memory project_worktree_supabase_cli.md (orchestrator handles worktree/main migration mirror — `supabase db push` needs file in main repo tree),
    memory reference_supabase_project.md (project ref `ytnsipxxmzgaebkqmokp`),
    .planning/phases/08-doctor-read-share/08-RESEARCH.md §"Environment Availability" (Supabase CLI 2.98.2, project ref confirmed)
  </read_first>
  <what-built>
    4 migration files ready to push: audit_logs columns + ip-family CHECK constraint, shares table + RLS + FK, 6 SECURITY DEFINER RPCs (create_share, revoke_share, redeem_share, verify_share_code, log_share_view, increment_share_attempt), share_snapshot_view.
  </what-built>
  <how-to-verify>
    1. Ensure `SUPABASE_ACCESS_TOKEN` env var is set (if not in current shell, source from `.env` or run `supabase login`).
    2. Run `supabase db push --linked` from repo root (`/Users/karstenhaldan/minisite/`). If prompted, confirm.
    3. **If push errors with `function digest does not exist` or `function crypt does not exist`** — Pitfall 1 hit. Fix: verify every SECURITY DEFINER has `set search_path = public, extensions, pg_catalog` (NOT just `public, pg_catalog`). Re-push.
    4. **If push errors with `42P17 must be marked IMMUTABLE`** — Pitfall 2 hit. Fix: remove `now()` or `current_timestamp` from any partial index WHERE clause. Re-push.
    5. Regenerate types: `supabase gen types typescript --linked > src/types/supabase.ts` (or `--project-id ytnsipxxmzgaebkqmokp`).
    6. Run `npm run typecheck` — must pass clean.
    7. Run `e2e/rls-shares.test.ts`: `npx vitest run --config vitest-e2e.config.ts e2e/rls-shares.test.ts` — the cross-tenant test MUST pass.
    8. Manual verification via psql or Supabase Studio SQL editor:
       - `select count(*) from public.shares` → 0 (table exists, empty)
       - `select column_name from information_schema.columns where table_name = 'audit_logs' and column_name in ('actor_type', 'share_id', 'recipient_ua_family', 'recipient_ip_family')` → 4 rows
       - `select count(*) from pg_proc where proname in ('create_share', 'revoke_share', 'redeem_share', 'verify_share_code', 'log_share_view', 'increment_share_attempt')` → **6**
       - `select pg_get_viewdef('public.share_snapshot_view'::regclass)` → contains `from auth.users` (or similar), does NOT contain `ai_messages`
       - `select conname from pg_constraint where conrelid = 'public.audit_logs'::regclass and conname = 'audit_logs_recipient_ip_family_bucketed_chk'` → 1 row (ME-2 CHECK applied)
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 verification steps pass. If push fails, share the error message and resume with "blocked: <error>".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Patient browser ↔ Postgres (RPC) | Patient JWT crosses; SECURITY DEFINER `create_share` / `revoke_share` enforce auth.uid() filter |
| Edge Function ↔ Postgres (service-role) | Service-role bypasses RLS; SECURITY DEFINER RPCs `redeem_share`, `verify_share_code`, `log_share_view`, `increment_share_attempt` are the only mutation surface |
| Untrusted recipient ↔ Postgres | No direct path; all access intermediated by Edge Function (Plan 08-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-T1 | Tampering | `shares` table direct UPDATE to extend `expires_at` | mitigate | NO update policy on `shares` for authenticated role; only `revoke_share` RPC (sets `revoked_at` only — cannot extend expiry) writes. Service-role bypass is scoped to 4 RPCs only. |
| T-08-T2 | Tampering | Direct INSERT into `audit_logs` with falsified `share_id` to hide a doctor view | mitigate | `audit_logs` already has no INSERT policy for authenticated role (Phase 7); only `log_share_view` SECURITY DEFINER RPC writes share_view rows; RPC is service_role-only grant. |
| T-08-I1 | Information disclosure | Cross-tenant token enumeration via base `shares` table SELECT | mitigate | RLS `shares_select_own` scopes SELECT to `auth.uid() = user_id`; service-role token-hash lookups happen only inside Edge Function (Plan 08-02). Cross-tenant impersonation proof test in Task 1 enforces. |
| T-08-I2 | Information disclosure | AI conversation history leaks via snapshot view | mitigate | `share_snapshot_view` definition MUST NOT reference `ai_messages` — structural exclusion. Verified by grep gate in Task 2 verify command (`grep -c 'ai_messages' ... | grep -q '^0$'`). |
| T-08-I3 | Information disclosure | `shares.access_code_hash` or `token_hash` leaks via SELECT | mitigate | Patient owner RLS allows SELECT on own shares, but hash columns can be inspected; mitigation: Active shares UI (Plan 08-03) reads via RPC `list_active_shares` that returns only safe columns. (Defense-in-depth — even if patient can SELECT the hashes, they can't reverse them.) |
| T-08-I4 | Information disclosure | Full IPv4/IPv6 addresses land in audit_logs as PII | mitigate | ME-2 CHECK constraint `audit_logs_recipient_ip_family_bucketed_chk` rejects any value that is not /16 IPv4 bucket, /48 IPv6 bucket, or sentinel `unknown`. Defense-in-depth at the DB layer regardless of Edge Function parsing. |
| T-08-R1 | Repudiation | Audit row not written because RPC errored (Pitfall 6) | mitigate | `log_share_view` is called BEFORE snapshot SELECT in Plan 08-02 Edge Function; on RPC error, Edge Function MUST respond 500 — never silently proceed. Documented in Plan 08-02 acceptance criteria. |
| T-08-S1 | Spoofing | `create_share` called with falsified `user_id` parameter | mitigate | RPC has NO `user_id` parameter; reads `auth.uid()` only. |
| T-08-E1 | Elevation of privilege | Authenticated user calls service-role-only RPC | mitigate | `revoke all from public; grant execute to service_role only` on `redeem_share`, `verify_share_code`, `log_share_view`, `increment_share_attempt`. `create_share` and `revoke_share` granted to `authenticated` (correct — those need patient context). |
</threat_model>

<verification>
After all 3 tasks complete:
- `git diff --stat` shows exactly 14 new files (4 migrations + 10 test/type scaffolds — the actual `src/types/supabase.ts` is regenerated, may or may not show in diff depending on Phase 7 baseline).
- `npm run typecheck` passes.
- `npx vitest run --config vitest-e2e.config.ts e2e/rls-shares.test.ts -t 'cross-tenant'` passes (the one real test).
- Live DB has `shares` table, **6 RPCs**, snapshot view, ME-2 CHECK constraint; verified via Supabase Studio SQL editor or `psql`.
- `grep -rn "ai_messages\|aiHistory" supabase/migrations/20260701*` returns ZERO matches (SC#3 structural enforcement at the schema layer).
</verification>

<success_criteria>
1. All 4 SQL migrations pushed to `ytnsipxxmzgaebkqmokp` successfully.
2. Cross-tenant RLS impersonation proof (`e2e/rls-shares.test.ts -t 'cross-tenant'`) GREEN — project-rule satisfied per memory `reference_supabase_project.md`.
3. `src/types/share.ts` exported and consumed-ready by downstream plans 08-02/03/04; `SnapshotResponse` includes the new `share_id: string` field (BL-1).
4. All 10 Wave 0 scaffold files exist (validates `08-VALIDATION.md` Wave 0 closure).
5. `share_snapshot_view` definition contains ZERO `ai_messages` references — SC#3 structural enforcement.
6. `supabase gen types typescript --linked` regenerated `src/types/supabase.ts` reflects new tables/RPCs; `npm run typecheck` clean.
7. **Six** SECURITY DEFINER RPCs (`create_share`, `revoke_share`, `redeem_share`, `verify_share_code`, `log_share_view`, `increment_share_attempt`) all carry `search_path = public, extensions, pg_catalog` (Pitfall 1 preventive — grep gate in Task 2 verify).
8. No `random()` calls in any new RPC (CSPRNG only — Assumption A5).
9. `audit_logs.recipient_ip_family` carries the ME-2 CHECK constraint forbidding full dot-quad IPs.
</success_criteria>

<output>
After completion, create `.planning/phases/08-doctor-read-share/08-01-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Highlight:
- Migration push outcome + any deviation patterns encountered (Pitfalls 1/2 if hit).
- Exact column names in `audit_logs` (esp. `user_id_hash` vs `actor_hash`) so Plan 08-02 doesn't guess.
- Exact table names for Phase 5/6 sync tables (`mood` vs `mood_logs`, `sleep` vs `sleep_logs`) so Plan 08-02 + the view definition match exactly.
- Wave 0 closure status (10/10 files present).
- Confirmation that `verify_share_code` shipped as part of this plan (BL-2) so Plan 08-02 can call it directly without a follow-up migration.
- Confirmation that `SnapshotResponse.share_id` is exported from `src/types/share.ts` (BL-1) for Plan 08-02's handler + Plan 08-06's print footer.
</output>
</output>
