---
phase: 10-clinic-operator-surface
plan: 09
type: execute
wave: 4
depends_on: [01]
files_modified:
  - supabase/functions/patient-activity/index.ts
  - supabase/functions/patient-activity/deno.json
  - supabase/functions/patient-activity/cors.ts
  - supabase/functions/patient-activity/index.test.ts
  - src/components/dashboard/settings/PatientActivityModal.tsx
  - src/components/dashboard/settings/PatientActivityRow.tsx
  - src/components/dashboard/settings/use-patient-activity.ts
  - src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx
  - src/components/dashboard/settings/PatientActivityModal.test.tsx
autonomous: true
requirements: [CLINIC-07]

must_haves:
  truths:
    - "`patient-activity` Edge Function deployed; validates patient JWT; returns audit_logs rows where target_user_id=auth.uid() AND org_id={query org_id}; supports `?tab=operator_views|ranking_events&offset=N&limit=25`"
    - "Edge Function enriches each row with actor_display_name fallback per D-20 ('a clinic member' when raw_user_meta_data.display_name is empty)"
    - "Cross-tenant proof in Deno test: Patient B's JWT querying Patient A's org → returns 0 rows (RLS does the filter)"
    - "PatientActivityModal renders two tabs (Operator views / Ranking events) with 25-row pagination per tab"
    - "Operator views tab renders rows: `{relative-time} — {actor display name} ({role badge}) viewed your {section name}` per UI-SPEC L562-565"
    - "Ranking events tab renders threshold-cross rows with `Why?` link → expands to show breakdown_snapshot top-3 signals from D-18 metadata"
    - "Modal NEVER captures PostHog events (D-19 patient-side surface — no meta-audit per the explicit decision in 10-EVENTS.md)"
    - "Active Organizations row 'View activity' IconButton (Phase 9 D-15 stub) NOW opens this modal — fills the stub by overwriting `ActiveOrganizationsSection.tsx` to wire the click handler"
    - "Modal close returns focus to the View activity IconButton in the originating row"
    - "HBNR/WMHMDA defensible footer always visible at modal bottom"
  artifacts:
    - path: "supabase/functions/patient-activity/index.ts"
      provides: "Edge Function returning per-org audit rows scoped to authenticated patient (RLS-safe)"
    - path: "supabase/functions/patient-activity/index.test.ts"
      provides: "Deno test (`.test.ts` per memory) covering RLS isolation + actor display fallback + tab filter"
    - path: "src/components/dashboard/settings/PatientActivityModal.tsx"
      provides: "Two-tab modal (Operator views / Ranking events) with pagination"
  key_links:
    - from: "src/components/dashboard/settings/PatientActivityModal.tsx"
      to: "supabase/functions/patient-activity/index.ts"
      via: "fetch via authorized URL"
      pattern: "patient-activity"
    - from: "src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx"
      to: "src/components/dashboard/settings/PatientActivityModal.tsx"
      via: "View activity IconButton click handler opens modal scoped to (org_id, patient_id)"
      pattern: "PatientActivityModal"
---

<objective>
Land the patient-side mirror modal that fulfills the Phase 9 D-15 stub ('View activity' on Active Organizations rows). Read-only audit-row surface scoped to (current patient × selected org). Two tabs: Operator views (per-section drill-in audits) + Ranking events (threshold-crossings). Edge Function enforces RLS (target_user_id = auth.uid()).

Purpose: D-19 + D-20 — patient-facing transparency. HBNR/WMHMDA defensible: patients can request the breakdown of any flag decision. Mirror modal is read-only — no PostHog events here per intentional D-19 decision.

Output: 4 Edge Function files + 4 frontend files + 1 update to ActiveOrganizationsSection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-clinic-operator-surface/10-CONTEXT.md
@.planning/phases/10-clinic-operator-surface/10-RESEARCH.md
@.planning/phases/10-clinic-operator-surface/10-UI-SPEC.md
@.planning/phases/10-clinic-operator-surface/10-EVENTS.md
@src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx
@src/components/ui/Modal.tsx
@src/components/ui/Pill.tsx
@src/lib/supabase.ts
@supabase/functions/clinic-photo/index.ts
@supabase/functions/clinic-snapshot/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author patient-activity Edge Function + Deno test</name>
  <read_first>
    - supabase/functions/clinic-photo/index.ts (Phase 9 — patient-JWT validation pattern)
    - supabase/functions/clinic-snapshot/index.ts (Plan 10-04 — Cache-Control + CORS pattern; mirror)
    - .planning/phases/10-clinic-operator-surface/10-RESEARCH.md §"Architecture Patterns" #7
    - .planning/phases/10-clinic-operator-surface/10-CONTEXT.md D-19 (two-tab structure) + D-20 (actor display fallback)
  </read_first>
  <behavior>
    - Test 1 (no JWT): 401 + Cache-Control set.
    - Test 2 (valid JWT, missing org_id): 400.
    - Test 3 (valid JWT, query own audit rows for own org): returns rows; each enriched with actor_display_name; rows where target_user_id != auth.uid() are absent (RLS).
    - Test 4 (cross-tenant): Patient B's JWT querying Patient A's org_id → returns 0 rows (RLS gate).
    - Test 5 (tab=operator_views filter): only rows with action IN ('section_view', 'patient_data.read', 'patient_photos.read') return.
    - Test 6 (tab=ranking_events filter): only rows with action='rank_threshold_crossed' return.
    - Test 7 (actor display fallback): row whose actor's raw_user_meta_data.display_name is null → actor_display_name='a clinic member'.
    - Test 8 (pagination): offset=25 returns next 25 rows; has_more=true when more exist.
    - Test 9 (Cache-Control on every status): assert header on each above.
  </behavior>
  <action>
    Mirror Plan 10-04's Edge Function structure. Files: `cors.ts` (echo-Origin), `deno.json` (mirror clinic-snapshot), `index.ts` per RESEARCH.md skeleton, `index.test.ts` per behaviors 1-9.

    Deploy via `supabase functions deploy patient-activity`.

    **Use `git commit -- <pathspec>`.**
  </action>
  <verify>
    <automated>test -f supabase/functions/patient-activity/index.ts && (cd supabase/functions/patient-activity && deno test --allow-all) && supabase functions deploy patient-activity</automated>
  </verify>
  <done>
    - Edge Function deployed; Deno test passes 9 assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author PatientActivityModal + row + hook + wire ActiveOrganizationsSection</name>
  <read_first>
    - src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx (Phase 9 — has the View activity IconButton stub click handler)
    - .planning/phases/10-clinic-operator-surface/10-UI-SPEC.md §"Patient-side: View activity modal" (full copywriting + tab structure)
    - src/components/ui/Modal.tsx (focus trap; close on Escape)
    - src/components/ui/Pill.tsx + Badge.tsx (actor role badge)
  </read_first>
  <behavior>
    - Test 1 (Vitest, modal opens): click View activity IconButton on an Active Orgs row → PatientActivityModal renders with org name in title.
    - Test 2 (Vitest, two tabs): tab nav has `Operator views` + `Ranking events`; defaults to Operator views.
    - Test 3 (Vitest, fetch): each tab triggers separate fetch with `?tab=...`; rows render with actor display + role badge + canonical row format.
    - Test 4 (Vitest, ranking-event Why? expander): click `Why?` link on ranking row → details block renders with breakdown_snapshot top-3 signals; click again → collapses.
    - Test 5 (Vitest, empty states): per UI-SPEC copy, when API returns 0 rows for the active tab.
    - Test 6 (Vitest, pagination): Previous/Next render; click Next → fetches with offset=25.
    - Test 7 (Vitest, focus return): close modal → focus returns to View activity IconButton in originating row.
    - Test 8 (Vitest, NO PostHog): assert that during modal mount + interaction, posthog.capture is NEVER called (D-19 explicit decision).
    - Test 9 (Vitest, HBNR footer always visible): footer text per UI-SPEC L582 visible regardless of active tab.
  </behavior>
  <action>
    1. **`src/components/dashboard/settings/use-patient-activity.ts`:** Hook signature: `usePatientActivity({ orgId, tab, offset, limit }) → { rows, loading, error, hasMore, refresh }`. Fetches via Edge Function; parses response into typed rows.

    2. **`src/components/dashboard/settings/PatientActivityRow.tsx`:** Renders one row. Operator-views format: `{relative-time} — {actor display name} ({role badge}) viewed your {section name}`. Ranking-events format: includes `Why?` link with `<button aria-expanded={open} aria-controls="ranking-{id}-details">` toggle + breakdown details block.

    3. **`src/components/dashboard/settings/PatientActivityModal.tsx`:** Composition root. Modal primitive wraps tab nav + tab panel + pagination + HBNR footer. Tab change calls usePatientActivity with new tab + reset offset to 0. Focus management per Modal primitive convention (Phase 9 carry-forward).

    4. **Update `src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx`:** wire the View activity IconButton click handler to open PatientActivityModal scoped to that row's org_id. Replace the Phase 9 stub TODO/console.warn with the modal mount.

    5. **`src/components/dashboard/settings/PatientActivityModal.test.tsx`:** 9 Vitest assertions per behaviors 1-9. Mock Edge Function fetch; assert posthog.capture NEVER called via jest.spyOn pattern.

    **Use `git commit -- <pathspec>`.**
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/components/dashboard/settings/PatientActivityModal.tsx','src/components/dashboard/settings/PatientActivityRow.tsx','src/components/dashboard/settings/use-patient-activity.ts','src/components/dashboard/settings/PatientActivityModal.test.tsx']; for (const f of files) if (!fs.existsSync(f)) { console.error('MISSING '+f); process.exit(1); } const aos=fs.readFileSync('src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx','utf8'); if (!aos.includes('PatientActivityModal')) { console.error('AOS not wired to modal'); process.exit(1); } console.log('OK');" && npm run typecheck && npm run test -- --run settings/PatientActivityModal</automated>
  </verify>
  <done>
    - 4 files exist; AOS wired; Vitest passes 9 assertions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patient client → patient-activity Edge Function | JWT validates auth.uid(); RLS on audit_logs filters target_user_id |
| Edge Function → audit_logs SELECT | RLS-guarded by target_user_id = auth.uid() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09-01 | Information disclosure | patient sees audit rows for other patients | mitigate | RLS on audit_logs target_user_id; cross-tenant Deno test verifies |
| T-10-09-02 | Information disclosure | meta-audit (PostHog event captures patient viewing modal) | mitigate | NO posthog.capture in modal code; Vitest test 8 asserts |
| T-10-09-03 | Information disclosure | actor IP/UA leaks via audit row metadata | accept | metadata schema does NOT include IP/UA; D-20 invariant |
</threat_model>

<verification>
- Edge Function deployed; 9 Deno assertions pass.
- 4 frontend files exist; 9 Vitest assertions pass.
- AOS wired to modal.
- ZERO posthog.capture calls in modal code path.
</verification>

<success_criteria>
1. Edge Function deployed; cross-tenant Deno test asserts isolation.
2. `npm run test -- --run settings/PatientActivityModal` passes 9 assertions.
3. Manual: as patient, open Settings → Active organizations → click View activity → modal opens with rows scoped to selected org → switch tabs → expand a Ranking event → see breakdown.
</success_criteria>

<output>
After completion, create `.planning/phases/10-clinic-operator-surface/10-09-SUMMARY.md` with: Edge Function deployment + Deno test + Vitest results + bundle-size delta on existing settings chunk.
</output>
