---
phase: 10-clinic-operator-surface
plan: 10
type: execute
wave: 5
depends_on: [04, 06, 07]
files_modified:
  - supabase/migrations/20260901000006_log_bulk_export_inclusion_rpc.sql
  - supabase/functions/bulk-csv-export/index.ts
  - supabase/functions/bulk-csv-export/deno.json
  - supabase/functions/bulk-csv-export/cors.ts
  - supabase/functions/bulk-csv-export/index.test.ts
  - src/components/clinic/roster/RosterBulkSelectionBar.tsx
  - src/components/clinic/roster/BulkExportPDFFlow.tsx
  - src/components/clinic/roster/BulkExportCSVFlow.tsx
  - src/components/clinic/roster/BulkOpenInTabsFlow.tsx
  - src/components/clinic/roster/use-roster-selection.ts
  - src/components/clinic/roster/RosterTable.tsx
  - src/components/clinic/roster/BulkExport.test.tsx
  - e2e/clinic-bulk-pdf.spec.ts
  - e2e/rls-bulk-export.test.ts
autonomous: true
requirements: [CLINIC-04, CLINIC-07]

must_haves:
  truths:
    - "`log_bulk_export_inclusion(p_org_id uuid, p_target_user_id uuid, p_export_type text)` SECURITY DEFINER RPC exists with `set search_path = public, extensions, pg_catalog`; gates on `has_permission(uid, p_org_id, 'patient_data.read')` and that target patient is active member; writes audit_logs row with action='bulk_pdf_export' OR 'bulk_csv_export' per p_export_type"
    - "`bulk-csv-export` Edge Function deployed; validates operator JWT → membership → permission; returns CSV (Content-Type: text/csv; Content-Disposition: attachment; filename=...); CSV contains symptoms from last 30d for selected patients with consent_scope.symptoms=true; per-included-patient calls log_bulk_export_inclusion RPC inline"
    - "RosterBulkSelectionBar renders when ≥1 patient selected; shows count + Clear + Actions menu (3 items: PDF / CSV / Open tabs); persists selection across sort + pagination via use-roster-selection hook (sessionStorage round-trip)"
    - "BulkExportPDFFlow uses `await import('jspdf')` (DYNAMIC ONLY — static import is forbidden by bundle-size CI guard); assembles per-patient pages from clinic-snapshot Edge Function fetches + chart-to-image; on completion, downloads via blob; writes one log_bulk_export_inclusion row per included patient; Fraunces success heading when N≥10"
    - "BulkExportCSVFlow calls bulk-csv-export Edge Function; downloads response stream"
    - "BulkOpenInTabsFlow opens up to 5 tabs via window.open() with 100ms stagger; tab cap warning when N>5"
    - "Mobile long-press enters selection mode; tap-to-drill remains primary; selection-bar slides up from bottom"
    - "PostHog events `clinic_bulk_selected` + `clinic_bulk_action_executed` fire per 10-EVENTS.md (count + action enum, no patient ids)"
    - "Cross-tenant pgTAP proof on log_bulk_export_inclusion: cross-tenant call → access_denied; cross-tenant patient → patient_not_in_org"
    - "Bundle invariant verified by CI: `! grep -rE \"^import .* from ['\\\"]jspdf['\\\"]\" src/ | grep -v test` returns zero matches"
    - "RosterTable.tsx is updated to render RosterBulkSelectionBar above the table when selection.length > 0; RosterRow + RosterMobileCard receive `isSelected` + `onToggleSelect` props"
  artifacts:
    - path: "supabase/migrations/20260901000006_log_bulk_export_inclusion_rpc.sql"
      provides: "SECURITY DEFINER RPC for per-included-patient audit row writes"
    - path: "supabase/functions/bulk-csv-export/index.ts"
      provides: "Server-side CSV assembly Edge Function"
    - path: "src/components/clinic/roster/{6 net-new files}"
      provides: "Bulk-selection UI + 3 action flows + selection hook"
    - path: "e2e/clinic-bulk-pdf.spec.ts"
      provides: "End-to-end bulk PDF flow"
    - path: "e2e/rls-bulk-export.test.ts"
      provides: "Cross-tenant pgTAP proof"
  key_links:
    - from: "src/components/clinic/roster/BulkExportPDFFlow.tsx"
      to: "jspdf (DYNAMIC import)"
      via: "await import('jspdf')"
      pattern: "await import\\('jspdf'\\)"
    - from: "src/components/clinic/roster/BulkExportPDFFlow.tsx"
      to: "supabase/functions/clinic-snapshot/index.ts (Plan 10-04)"
      via: "per-patient snapshot fetch loop for PDF assembly"
      pattern: "clinic-snapshot"
    - from: "supabase/functions/bulk-csv-export/index.ts"
      to: "supabase/migrations/20260901000006_log_bulk_export_inclusion_rpc.sql"
      via: "supabase.rpc('log_bulk_export_inclusion', { p_org_id, p_target_user_id, p_export_type: 'csv' }) per included patient"
      pattern: "log_bulk_export_inclusion"
---

<objective>
Land the operator's bulk affordances: multi-select infrastructure (checkbox per row, header select-all-visible, persistence across sort + pagination), 3 bulk actions (PDF dynamic-import + CSV server-side + Open in tabs), per-included-patient audit RPC, cross-tenant pgTAP proof, mobile long-press gesture. PostHog events PHI-safe. Bundle invariant: jsPDF dynamic-only.

Purpose: D-22 + D-23 + memory `project_phase5_bundle_regression.md` (jsPDF dynamic-import). Bulk affordances were locked in v1 by the user's CONTEXT D-22 override of the "defer bulk" recommendation — operator UX is END-USER per memory `feedback_regulator_vs_user_audience_pattern.md`.

Output: 1 migration + 4 Edge Function files + 6 frontend files + 1 RosterTable update + Vitest + Playwright + pgTAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-clinic-operator-surface/10-CONTEXT.md
@.planning/phases/10-clinic-operator-surface/10-RESEARCH.md
@.planning/phases/10-clinic-operator-surface/10-UI-SPEC.md
@.planning/phases/10-clinic-operator-surface/10-EVENTS.md
@src/types/snapshot.ts
@src/components/clinic/roster/RosterTable.tsx
@src/components/clinic/roster/RosterRow.tsx
@src/components/clinic/roster/RosterMobileCard.tsx
@src/components/ui/Modal.tsx
@src/lib/clinic-events.ts
@supabase/functions/clinic-snapshot/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author log_bulk_export_inclusion RPC + cross-tenant pgTAP + bulk-csv-export Edge Function + Deno test</name>
  <read_first>
    - supabase/migrations/20260901000005_log_clinic_view_rpc.sql (Plan 10-04 — mirror RPC pattern)
    - supabase/functions/clinic-snapshot/index.ts (Plan 10-04 — mirror Edge Function auth pattern)
    - .planning/phases/10-clinic-operator-surface/10-CONTEXT.md D-22 (per-included-patient audit row)
  </read_first>
  <behavior>
    - Test 1 (RPC): Operator A in Org 1 calls log_bulk_export_inclusion(org_1_id, patient_in_org_1, 'pdf') → succeeds, audit row written with action='bulk_pdf_export'.
    - Test 2 (RPC cross-tenant): Operator A calls for Org 2 → access_denied.
    - Test 3 (RPC patient cross-tenant): Operator A in Org 1 calls for patient in Org 2 → patient_not_in_org.
    - Test 4 (RPC export type): p_export_type='csv' writes 'bulk_csv_export'; 'pdf' writes 'bulk_pdf_export'; other → invalid_export_type.
    - Test 5 (Edge Function CSV no JWT): 401.
    - Test 6 (Edge Function CSV happy path): operator with permission, 3 patients, all consent_scope.symptoms=true → returns CSV with rows for each patient × symptom log; Content-Type text/csv; Content-Disposition attachment.
    - Test 7 (Edge Function CSV consent filter): one patient has consent_scope.symptoms=false → that patient's rows are absent from CSV; per-included-patient audit row NOT written for that patient.
    - Test 8 (Edge Function CSV per-patient audit): 3 included patients → 3 audit_logs rows with action='bulk_csv_export'.
  </behavior>
  <action>
    1. **`supabase/migrations/20260901000006_log_bulk_export_inclusion_rpc.sql`:** Mirror Plan 10-04's log_clinic_view RPC structure. SECURITY DEFINER, search_path, REVOKE/GRANT. Validates p_export_type IN ('pdf', 'csv'). Writes audit row with action='bulk_pdf_export' or 'bulk_csv_export' depending on p_export_type.

    2. **`e2e/rls-bulk-export.test.ts`:** Cross-tenant proof per behaviors 1-4.

    3. **`supabase/functions/bulk-csv-export/{index.ts, deno.json, cors.ts, index.test.ts}`:** Mirror Plan 10-04 Edge Function structure. POST endpoint accepting `{ org_id, patient_ids: string[] }`. For each patient: validate active membership + consent_scope.symptoms. Build CSV: header `patient_display_name,date,symptom_name,severity`, rows from `symptoms` table last 30d. Per included patient call `supabase.rpc('log_bulk_export_inclusion', { p_org_id, p_target_user_id: patientId, p_export_type: 'csv' })`. Response Content-Type text/csv, Content-Disposition `attachment; filename="symptoms-{org_slug}-{date}.csv"`, Cache-Control private no-store.

    Deploy via `supabase functions deploy bulk-csv-export`.

    **Use `git commit -- <pathspec>`.**
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260901000006_log_bulk_export_inclusion_rpc.sql && test -f supabase/functions/bulk-csv-export/index.ts && supabase db push --linked && npm run test:rls -- 'rls-bulk-export\\.test\\.ts' && (cd supabase/functions/bulk-csv-export && deno test --allow-all) && supabase functions deploy bulk-csv-export</automated>
  </verify>
  <done>
    - Migration applied; cross-tenant proof passes 4 assertions; Edge Function passes 4 assertions; deployed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author 6 bulk frontend files + update RosterTable + Vitest + Playwright</name>
  <read_first>
    - src/components/clinic/roster/RosterTable.tsx (Plan 10-06 — extend with selection state + bulk-bar render)
    - src/components/clinic/roster/RosterRow.tsx + RosterMobileCard.tsx (extend with isSelected + onToggleSelect props)
    - src/components/ui/Modal.tsx (confirmation modals)
    - .planning/phases/10-clinic-operator-surface/10-UI-SPEC.md §"Operator-side: Roster bulk affordances" (full copywriting)
    - .planning/phases/10-clinic-operator-surface/10-EVENTS.md §8-9 (PHI-safe bulk events)
    - memory `project_phase5_bundle_regression.md` (jsPDF dynamic-import invariant)
  </read_first>
  <behavior>
    - Test 1 (Vitest, selection persistence): select 2 rows on page 1 → click Next → page 2 selection-bar still shows `2 selected` (sessionStorage round-trip).
    - Test 2 (Vitest, header indeterminate): 1 of 50 visible rows selected → header checkbox shows `aria-checked="mixed"`.
    - Test 3 (Vitest, bulk PDF dynamic import): mock `import('jspdf')` resolution; click Generate PDF → assert dynamic import was called; assert NO static import statement for jspdf in source files.
    - Test 4 (Vitest, per-included-patient audit): 3 patients selected → BulkExportPDFFlow loops over them, calls supabase.rpc('log_bulk_export_inclusion', {p_export_type: 'pdf'}) ONCE per patient.
    - Test 5 (Vitest, CSV flow): click CSV → fetch bulk-csv-export Edge Function with selected ids; on response, browser download triggered.
    - Test 6 (Vitest, Open tabs N≤5): 3 patients selected → click Open tabs → confirm → window.open called 3 times with the drill-in URLs.
    - Test 7 (Vitest, Open tabs cap): 8 patients selected → confirmation body warns about cap; on confirm, only 5 tabs open; toast warns.
    - Test 8 (Vitest, mobile long-press): touchstart held 500ms → enters selection mode (selection bar visible).
    - Test 9 (Vitest, PostHog PHI-safe): clinic_bulk_selected captured with `{ count, action_planned }`; clinic_bulk_action_executed captured with `{ action, patient_count }`. NO patient ids, NO names in any captured event.
    - Test 10 (Playwright e2e clinic-bulk-pdf.spec.ts): seed 3 patients with full data; sign in as operator; select all 3; click Generate PDF; assert download triggered (mock the blob save); assert 3 audit_logs rows with action='bulk_pdf_export'.
    - Test 11 (bundle invariant): `grep -rE "^import .* from ['\"]jspdf['\"]" src/` returns zero matches outside test files.
  </behavior>
  <action>
    1. **`src/components/clinic/roster/use-roster-selection.ts`:** Hook signature: `useRosterSelection({ orgId }) → { selected: Set<string>, toggle: (userId: string) => void, toggleAll: (userIds: string[]) => void, clear: () => void, isSelected: (userId: string) => boolean }`. Persists to sessionStorage key `clinic_roster_selection_{orgId}`.

    2. **`src/components/clinic/roster/RosterBulkSelectionBar.tsx`:** Renders `{N} selected · Clear · Actions ▾` pill at top-right of roster card (or fixed-bottom bar on mobile). Action menu has 3 items per UI-SPEC. Each action opens its respective Flow component.

    3. **`src/components/clinic/roster/BulkExportPDFFlow.tsx`:**
       - Confirmation modal first.
       - On confirm: `const { jsPDF } = await import('jspdf');` (DYNAMIC, never static).
       - Loop selected patient ids: fetch clinic-snapshot Edge Function for each; assemble per-patient PDF page (chart image via existing chart-to-image helper from Phase 7 doctor-report; section text per snapshot); call `supabase.rpc('log_bulk_export_inclusion', { p_org_id, p_target_user_id, p_export_type: 'pdf' })`.
       - On completion: `doc.output('blob')` → `saveAs(blob, ...)` (use existing FileSaver pattern from Phase 7 or `URL.createObjectURL` + anchor click).
       - Toast in-progress + success.
       - Fraunces success heading modal when N≥10.
       - Honor permission_map per patient: skip sections that operator's role can't access; show partial-failure summary if ANY patient was fully blocked.

    4. **`src/components/clinic/roster/BulkExportCSVFlow.tsx`:** Confirmation modal → POST to bulk-csv-export Edge Function with selected ids → browser triggers download via Content-Disposition response → toast success.

    5. **`src/components/clinic/roster/BulkOpenInTabsFlow.tsx`:** Confirmation modal (with cap warning if N>5) → loop window.open with 100ms stagger; cap at 5; toast warns.

    6. **Update `src/components/clinic/roster/RosterTable.tsx`:** integrate use-roster-selection hook; render RosterBulkSelectionBar when selected.size > 0; pass isSelected + onToggleSelect props to RosterRow + RosterMobileCard. Header `Select all visible` checkbox uses indeterminate state. PostHog `clinic_bulk_selected` fires on selection-count change.

    7. **Update RosterRow + RosterMobileCard:** add bulk-select checkbox cell (40×40 hit target, 20×20 visible glyph per UI-SPEC); onClick stops propagation to prevent drill-in. On mobile, long-press 500ms enters selection mode.

    8. **`src/components/clinic/roster/BulkExport.test.tsx`:** 9 Vitest assertions per behaviors 1-9.

    9. **`e2e/clinic-bulk-pdf.spec.ts`:** behavior 10.

    10. **Bundle CI guard config:** verify `scripts/bundle-size-check.cjs` (or wherever) FAILS the PR if `jspdf` appears in any always-loaded chunk's manifest. If the existing guard doesn't have this rule, ADD it: `for (const chunk of staticChunks) { if (chunk.includes('jspdf')) throw new Error('jspdf must be dynamic') }`.

    **Use `git commit -- <pathspec>` per logical batch.**
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/components/clinic/roster/RosterBulkSelectionBar.tsx','src/components/clinic/roster/BulkExportPDFFlow.tsx','src/components/clinic/roster/BulkExportCSVFlow.tsx','src/components/clinic/roster/BulkOpenInTabsFlow.tsx','src/components/clinic/roster/use-roster-selection.ts','src/components/clinic/roster/BulkExport.test.tsx','e2e/clinic-bulk-pdf.spec.ts']; for (const f of files) if (!fs.existsSync(f)) { console.error('MISSING '+f); process.exit(1); } const pdf=fs.readFileSync('src/components/clinic/roster/BulkExportPDFFlow.tsx','utf8'); if (!pdf.match(/await import\\(['\"]jspdf['\"]\\)/)) { console.error('PDF flow missing dynamic import'); process.exit(1); } console.log('OK 7 files + dynamic import');" && npm run typecheck && ! grep -rE "^import .* from ['\"]jspdf['\"]" src/ | grep -v test && npm run test -- --run roster/BulkExport && npx playwright test e2e/clinic-bulk-pdf.spec.ts</automated>
  </verify>
  <done>
    - 7 files exist; dynamic-import invariant verified; Vitest (9) + Playwright (1) all pass.
    - RosterTable extended with selection.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator client → bulk-csv-export Edge Function | JWT validates membership + permission |
| Edge Function → log_bulk_export_inclusion RPC | RPC re-validates auth + membership server-side |
| dynamic jspdf import | bundle CI guard prevents accidental static import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10-01 | Information disclosure | bulk PDF includes patients with consent_scope.section=false | mitigate | clinic-snapshot Edge Function (Plan 10-04) already omits sections; PDF assembly uses snapshot output verbatim |
| T-10-10-02 | Information disclosure | CSV exposes patient PHI to operator without permission | mitigate | bulk-csv-export gates on operator's patient_data.read; consent_scope.symptoms filter applied per patient |
| T-10-10-03 | Bundle regression | jspdf static-imported into clinic chunk | mitigate | CI bundle guard fails PR; behavior 11 grep assertion |
| T-10-10-04 | Information disclosure | PostHog event leaks patient ids in bulk_action_executed | mitigate | event payload schema is count + action enum only; Vitest test 9 asserts |
| T-10-10-05 | DoS | Open tabs spawns N tabs, browser blocks | mitigate | cap at 5 with toast warning; explicit confirmation modal at confirmation step |
</threat_model>

<verification>
- 1 migration + 4 Edge Function files + 7 frontend files all exist.
- 4 pgTAP + 8 Deno + 9 Vitest + 1 Playwright + 1 bundle-grep all pass.
- jspdf NEVER static-imported.
- PostHog events PHI-safe.
</verification>

<success_criteria>
1. `supabase db push --linked` applies migration 6.
2. `npm run test:rls -- 'rls-bulk-export\\.test\\.ts'` passes.
3. `cd supabase/functions/bulk-csv-export && deno test --allow-all` passes.
4. `npm run test -- --run roster/BulkExport` passes 9 assertions.
5. `npx playwright test e2e/clinic-bulk-pdf.spec.ts` passes.
6. `! grep -rE "^import .* from ['\"]jspdf['\"]" src/ | grep -v test` returns zero matches.
7. Manual: select 3 patients, generate PDF, download; check audit_logs for 3 bulk_pdf_export rows.
</success_criteria>

<output>
After completion, create `.planning/phases/10-clinic-operator-surface/10-10-SUMMARY.md` with: bundle-size delta + dynamic-import verification + per-flow timing observations + audit row count from test fixtures.
</output>
