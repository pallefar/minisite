---
phase: 10-clinic-operator-surface
plan: 11
type: execute
wave: 5
depends_on: [02, 03, 04, 06, 07, 08, 09, 10]
files_modified:
  - e2e/fixtures/seed-org-50.ts
  - e2e/roster-perf.spec.ts
  - .github/workflows/ci.yml
  - .planning/phases/10-clinic-operator-surface/10-VALIDATION.md
autonomous: false  # Task 2 = blocking checkpoint to confirm CI roster-perf job triggers correctly + verify PostHog dashboard captures all 10 events post-deploy
requirements: [CLINIC-04, CLINIC-05, CLINIC-07]

must_haves:
  truths:
    - "`e2e/fixtures/seed-org-50.ts` script creates 1 test org + 50 synthetic patients (each with 30 days of injections/weights/symptoms via INSERT) using Supabase admin client; afterAll cleanup removes org + 50 users"
    - "`e2e/roster-perf.spec.ts` Playwright spec signs in as operator, navigates to /clinic/{slug}, asserts roster renders within 2000ms via `page.evaluate(() => performance.now() - navigationStart)`"
    - "`.github/workflows/ci.yml` has a NEW `roster-perf` job appended AFTER the existing `share-security-drill` job (HI-2 additive convention from memory `feedback_planner_iter1_anti_patterns.md`)"
    - "roster-perf CI job is path-scoped: triggers on PR when changed files match `leanshot/src/components/clinic/**` OR `supabase/migrations/*roster*.sql` OR `supabase/migrations/2026090100000[3-4]_*.sql` OR `supabase/functions/clinic-snapshot/**` OR `supabase/functions/bulk-csv-export/**`; runs the Playwright spec against the Vercel preview deploy URL (mirrors Phase 2.1 path-scoped Lighthouse pattern)"
    - "PHI-safety verification grep: `! grep -rE \"(patient_user_id|patient_name|score: [0-9]|raw_score|membership_id)\" src/components/clinic/ src/components/dashboard/settings/PatientActivityModal.tsx | grep posthog | grep -v test` returns zero matches"
    - "Bundle-size verification: `npm run build && node scripts/bundle-size-check.cjs` reports clinic ≤20 kB gz, clinic-settings ≤18 kB gz, read-only-patient-view ≤12 kB gz, share ≤6 kB gz, index ≤24.5 kB gz; jspdf appears ONLY in dynamically-loaded chunks"
    - "All Phase 10 Playwright e2e specs pass: clinic-roster-sort, clinic-drill-in, clinic-audit, clinic-bulk-pdf, roster-perf"
    - "VALIDATION.md frontmatter flipped to `status: complete` after all checks green"
    - "10-EVENTS.md PostHog event audit (manual checkpoint Task 2): all 10 events visible in PostHog Live Events tab within 30s of executing each user action against the live preview deploy"
  artifacts:
    - path: "e2e/fixtures/seed-org-50.ts"
      provides: "Reusable seed helper for roster-perf + future bulk tests"
    - path: "e2e/roster-perf.spec.ts"
      provides: "SC#5 < 2s render assertion under 50-patient load"
    - path: ".github/workflows/ci.yml"
      provides: "NEW path-scoped roster-perf CI job appended after share-security-drill"
  key_links:
    - from: "e2e/roster-perf.spec.ts"
      to: "e2e/fixtures/seed-org-50.ts"
      via: "beforeAll loads fixture; afterAll cleanup"
      pattern: "seed-org-50"
    - from: ".github/workflows/ci.yml"
      to: "roster-perf job after share-security-drill"
      via: "additive job append per HI-2 convention"
      pattern: "roster-perf:"
---

<objective>
Land the SC#5 verification: 50-patient roster render < 2s on Vercel preview, gated as a path-scoped PR check. Wire a 50-patient seed fixture. Append the CI job AFTER share-security-drill (additive convention). Verify all PHI-safety greps pass. Verify all bundle-size budgets respected. Flip VALIDATION.md to `status: complete`. Confirm via human checkpoint that PostHog Live Events captures all 10 events post-deploy.

Purpose: D-10 + D-11 + D-24 + D-25 + ROADMAP SC#5. This is the phase's verification harness. The CI job ensures regressions in any of Plans 10-02/03/04/06/07/10 (which together drive the roster's render path) are caught at PR time, not in production.

Output: 1 fixture + 1 spec + 1 CI job append + 1 VALIDATION.md flip + 1 manual PostHog checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-clinic-operator-surface/10-CONTEXT.md
@.planning/phases/10-clinic-operator-surface/10-RESEARCH.md
@.planning/phases/10-clinic-operator-surface/10-UI-SPEC.md
@.planning/phases/10-clinic-operator-surface/10-EVENTS.md
@.planning/phases/10-clinic-operator-surface/10-VALIDATION.md
@.github/workflows/ci.yml
@e2e/fixtures/shares.ts
@e2e/clinic-roster-sort.spec.ts
@e2e/clinic-drill-in.spec.ts
@e2e/clinic-audit.spec.ts
@e2e/clinic-bulk-pdf.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author seed-org-50 fixture + roster-perf spec + append roster-perf CI job + run PHI/bundle greps</name>
  <read_first>
    - e2e/fixtures/shares.ts (Phase 8 fixture pattern for admin-client seed)
    - .github/workflows/ci.yml (find existing share-security-drill job — append AFTER it)
    - .planning/phases/10-clinic-operator-surface/10-CONTEXT.md D-10 (perf assertion shape) + D-11 (path-scoped CI trigger)
    - scripts/bundle-size-check.cjs (or wherever; updated by Plan 10-05 with read-only-patient-view + share budgets)
  </read_first>
  <behavior>
    - Test 1 (seed fixture): script creates 1 org + 50 patients (each with 30 days of injections/weights/symptoms); returns object `{ orgId, slug, operatorJwt, patientUserIds }`; afterAll cleanup removes all 51 auth.users.
    - Test 2 (roster-perf spec): seed fixture → sign in as operator → navigate to /clinic/{slug} → assert all 50 rows visible AND `performance.now() - navigationStart < 2000` ms.
    - Test 3 (CI job presence): `.github/workflows/ci.yml` contains `roster-perf:` job AFTER `share-security-drill:` (text appears later in file). Path filter regex per spec.
    - Test 4 (PHI-safety grep): `grep -rE "(patient_user_id|patient_name|score: [0-9]|raw_score|membership_id)" src/components/clinic/ src/components/dashboard/settings/PatientActivityModal.tsx | grep posthog | grep -v test` returns zero matches.
    - Test 5 (bundle-size grep): `grep -rE "^import .* from ['\"]jspdf['\"]" src/ | grep -v test` returns zero matches.
    - Test 6 (all Phase 10 e2e green): `npx playwright test e2e/clinic-roster-sort.spec.ts e2e/clinic-drill-in.spec.ts e2e/clinic-audit.spec.ts e2e/clinic-bulk-pdf.spec.ts e2e/roster-perf.spec.ts` returns 0 failures.
  </behavior>
  <action>
    1. **`e2e/fixtures/seed-org-50.ts`:**

       Use Phase 8/9 admin-client seed pattern. Steps:
       - `const admin = createClient(SUPABASE_URL, SERVICE_ROLE_KEY);`
       - Create 1 operator user (`admin.auth.admin.createUser`).
       - Create 1 org: `admin.rpc('create_org', { p_name: 'Perf Test Clinic', p_slug: 'perf-test-' + Date.now(), ... })` (use Phase 9 RPC).
       - Loop 50 patients: createUser, then `admin.rpc('send_invite', ...)` then `admin.rpc('accept_invite_existing', ...)` (or directly INSERT membership via service-role bypass — choose simpler path; service-role can INSERT bypassing RLS).
       - For each of 50 patients: INSERT 30 injections + 30 weights + 30 symptoms with synthetic timestamps spread across last 30 days.
       - Generate operator JWT via `admin.auth.admin.generateLink({ type: 'magiclink', email: operator.email })` then resolve to a session — OR use `admin.auth.signInWithPassword`. Use whatever pattern Phase 9 e2e specs already use.
       - Return `{ orgId, slug, operatorJwt, patientUserIds: string[] }`.

       afterAll cleanup function: `for (const userId of [...patientUserIds, operatorUserId]) await admin.auth.admin.deleteUser(userId)` and `await admin.rpc('delete_org', { p_org_id: orgId })`.

    2. **`e2e/roster-perf.spec.ts`:**

       ```ts
       import { test, expect } from '@playwright/test';
       import { seedOrg50, cleanupOrg50 } from './fixtures/seed-org-50';

       test.describe('Roster perf SC#5', () => {
         let fixture: Awaited<ReturnType<typeof seedOrg50>>;

         test.beforeAll(async () => { fixture = await seedOrg50(); });
         test.afterAll(async () => { await cleanupOrg50(fixture); });

         test('50-patient roster renders within 2000ms', async ({ page }) => {
           // Seed JWT into localStorage via addInitScript (per memory `reference_playwright_state_seeding.md`)
           await page.addInitScript((jwt) => {
             window.localStorage.setItem('sb-' + 'XXX' + '-auth-token', JSON.stringify({ access_token: jwt, ... }));
           }, fixture.operatorJwt);

           const start = Date.now();
           await page.goto('/clinic/' + fixture.slug);
           await page.waitForSelector('[data-testid="roster-row"]:nth-child(50)', { timeout: 5000 });
           const renderMs = await page.evaluate(() => performance.now() - 0); // approximate
           const elapsed = Date.now() - start;
           expect(elapsed).toBeLessThan(2000);
         });
       });
       ```

       (Executor adapts the JWT seeding to the project's actual supabase storage key pattern. Add data-testid="roster-row" attribute to RosterRow component as part of this plan if missing.)

    3. **Append roster-perf job to `.github/workflows/ci.yml`:**

       Find the `share-security-drill:` job. AFTER it (in file order, NOT modifying existing content), append:

       ```yaml
         roster-perf:
           name: Roster perf (50-patient SC#5)
           needs: [share-security-drill]
           if: |
             contains(github.event.pull_request.changed_files, 'leanshot/src/components/clinic/') ||
             contains(github.event.pull_request.changed_files, 'supabase/migrations/2026090100000') ||
             contains(github.event.pull_request.changed_files, 'supabase/functions/clinic-snapshot/') ||
             contains(github.event.pull_request.changed_files, 'supabase/functions/bulk-csv-export/')
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version: '22' }
             - run: npm ci
             - run: npx playwright install --with-deps
             - name: Wait for Vercel preview deploy
               # reuse existing pattern from share-security-drill
               run: ./scripts/wait-for-vercel-preview.sh
             - name: Run roster-perf spec
               run: npx playwright test e2e/roster-perf.spec.ts
               env:
                 PLAYWRIGHT_BASE_URL: ${{ env.VERCEL_PREVIEW_URL }}
                 SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
                 SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
       ```

       (If the actual GitHub Actions `if:` syntax for path filtering uses `paths:` at job level instead of changed_files, adapt accordingly. Mirror the pattern Phase 2.1 used for path-scoped Lighthouse — search ci.yml for it.)

    4. **PHI-safety + bundle-size grep verification:**

       Run `! grep -rE "(patient_user_id|patient_name|score: [0-9]|raw_score|membership_id)" src/components/clinic/ src/components/dashboard/settings/PatientActivityModal.tsx | grep posthog | grep -v test` — must return zero matches.

       Run `! grep -rE "^import .* from ['\"]jspdf['\"]" src/ | grep -v test` — must return zero matches.

       Run `npm run build && node scripts/bundle-size-check.cjs` (or whatever the bundle guard script is named) — must report all chunks within budget.

    5. **Run all Phase 10 Playwright specs against local preview:**

       `npx playwright test e2e/clinic-roster-sort.spec.ts e2e/clinic-drill-in.spec.ts e2e/clinic-audit.spec.ts e2e/clinic-bulk-pdf.spec.ts e2e/roster-perf.spec.ts` — 0 failures.

    **Use `git commit -- <pathspec>` per logical batch (fixture + spec; ci.yml).**
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['e2e/fixtures/seed-org-50.ts','e2e/roster-perf.spec.ts']; for (const f of files) if (!fs.existsSync(f)) { console.error('MISSING '+f); process.exit(1); } const ci=fs.readFileSync('.github/workflows/ci.yml','utf8'); const sIdx=ci.indexOf('share-security-drill:'); const rIdx=ci.indexOf('roster-perf:'); if (sIdx === -1 || rIdx === -1 || rIdx < sIdx) { console.error('roster-perf job missing or wrong order'); process.exit(1); } console.log('OK files + CI job');" && ! grep -rE "(patient_user_id|patient_name|score: [0-9]|raw_score|membership_id)" src/components/clinic/ src/components/dashboard/settings/PatientActivityModal.tsx 2>/dev/null | grep posthog | grep -v test && ! grep -rE "^import .* from ['\"]jspdf['\"]" src/ | grep -v test && npm run build && node scripts/bundle-size-check.cjs && npx playwright test e2e/clinic-roster-sort.spec.ts e2e/clinic-drill-in.spec.ts e2e/clinic-audit.spec.ts e2e/clinic-bulk-pdf.spec.ts e2e/roster-perf.spec.ts</automated>
  </verify>
  <done>
    - 2 fixture files exist; CI job appended in correct position; all Playwright specs pass; PHI/bundle greps clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [BLOCKING]: Manual PostHog event audit + flip VALIDATION.md to status:complete</name>
  <what-built>11 plans implemented; all 10 PostHog events should be firing on the live preview.</what-built>
  <how-to-verify>
    1. Open the deployed Vercel preview URL.
    2. Sign in as a test operator with full role permissions.
    3. Execute each user action from `10-EVENTS.md`:
       - Navigate to /clinic/{slug} → expect `clinic_workspace_loaded` + `clinic_roster_loaded` in PostHog Live Events tab within 30s.
       - Click any column header to sort → expect `clinic_roster_sorted`.
       - Click a roster row → expect `clinic_patient_drilled_in`.
       - On the drill-in, expand any section → expect `clinic_drill_section_expanded`.
       - Navigate to /clinic/{slug}/settings/audit → expect `clinic_audit_tab_opened`.
       - Apply any filter → expect `clinic_audit_filter_applied`.
       - Back to roster, select 1 patient → expect `clinic_bulk_selected`.
       - Click bulk PDF → confirm → expect `clinic_bulk_action_executed`.
       - Click a score chip (as Coach/Owner) → expect `clinic_rank_breakdown_expanded`.
    4. Verify all 10 events appeared. Flag any that didn't.
    5. Also verify NO patient_user_id, patient_name, or raw_score property in any captured event.

    **After PostHog audit passes, flip VALIDATION.md frontmatter:**

    Edit `.planning/phases/10-clinic-operator-surface/10-VALIDATION.md` frontmatter:
    ```yaml
    status: complete   # was draft
    nyquist_compliant: true   # already true from Plan 10-01
    wave_0_complete: true     # already true from Plan 10-01
    ```

    Commit with: `git commit -- .planning/phases/10-clinic-operator-surface/10-VALIDATION.md` and message `docs(10-11): flip VALIDATION.md to status:complete after Phase 10 close`.
  </how-to-verify>
  <resume-signal>Type "verified" once all 10 events appear in PostHog Live Events tab AND no PHI properties are present AND VALIDATION.md is flipped + committed. Describe any missing events or PHI leaks otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed fixture → live DB | service-role bypass; cleanup removes test data |
| CI job → Vercel preview | runs against ephemeral preview deploy URL |
| PHI grep → posthog event capture sites | static analysis only; runtime confirmation in Task 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11-01 | Information disclosure | seed fixture leaks 50 test patients into prod DB | mitigate | afterAll cleanup deletes all 51 auth.users + org; CI runs against test project, not prod |
| T-10-11-02 | DoS | roster-perf job runs on every PR | mitigate | path-scoped triggers limit to PRs touching Phase 10 files only |
| T-10-11-03 | Tampering | bundle-size guard bypass via dynamic import that re-exports jspdf statically | accept | requires deliberate developer evasion; out of scope for v1 |
</threat_model>

<verification>
- 2 fixture files exist; CI job appended in correct position.
- 5 Playwright specs all green.
- PHI grep + bundle grep both return zero.
- Bundle-size budgets all met.
- VALIDATION.md flipped to complete after Task 2.
- All 10 PostHog events confirmed firing live.
</verification>

<success_criteria>
1. `e2e/fixtures/seed-org-50.ts` + `e2e/roster-perf.spec.ts` exist.
2. `.github/workflows/ci.yml` has roster-perf job AFTER share-security-drill.
3. `npx playwright test e2e/roster-perf.spec.ts` passes (50-patient render < 2000ms).
4. All 4 other Phase 10 Playwright specs pass.
5. PHI-safety grep returns zero.
6. Bundle-size grep returns zero static jspdf imports.
7. `node scripts/bundle-size-check.cjs` reports all chunks within budget.
8. VALIDATION.md flipped to `status: complete` after Task 2 PostHog audit.
9. All 10 PostHog events confirmed firing on live preview (Task 2 manual verification).
</success_criteria>

<output>
After completion, create `.planning/phases/10-clinic-operator-surface/10-11-SUMMARY.md` with: roster-perf timing observation + CI job activation log + PHI-safety + bundle-size grep results + PostHog event verification per-event status + Phase 10 close declaration.
</output>
