---
phase: 10-clinic-operator-surface
plan: 08
type: execute
wave: 4
depends_on: [01]
files_modified:
  - src/components/clinic/settings/AuditTab.tsx
  - src/components/clinic/settings/AuditFilterBar.tsx
  - src/components/clinic/settings/AuditRow.tsx
  - src/components/clinic/settings/AuditCustomRangeModal.tsx
  - src/components/clinic/settings/use-audit-events.ts
  - src/components/clinic/settings/ClinicSettingsPage.tsx
  - src/components/clinic/settings/AuditTab.test.tsx
  - e2e/clinic-audit.spec.ts
autonomous: true
requirements: [CLINIC-07]

must_haves:
  truths:
    - "AuditTab is added to the existing Phase 9 ClinicSettingsPage NAV array AFTER 'roles' (rightmost), gated by `has_permission(viewer, org_id, 'audit_log.read')` — for users without the permission, the tab is NOT in the nav (no 403 page)"
    - "AuditTab fetches audit_logs directly via supabase-js with RLS doing the work (no new Edge Function — RLS on audit_logs already gates by org_id IN <user's orgs with audit_log.read>)"
    - "Three filters: member dropdown (queries org members via Phase 9 list_org_members RPC), action-type dropdown (16 values per UI-SPEC action-enum table), time-range picker (24h / 7d / 30d / Custom)"
    - "Custom range modal validates from <= to AND to <= today AND range <= 13 months"
    - "Per-row collapsed format renders the canonical line; click expands to font-mono details block; multiple rows can be expanded simultaneously"
    - "Empty states: filtered-out (`No events match your filters` + Clear filters CTA); never-any (`No events yet`)"
    - "13-month retention notice renders dismissibly above filter row; dismissal persists per session via sessionStorage key `clinic_audit_retention_dismissed`"
    - "Pagination 50 rows/page; supabase-js .range(offset, offset + limit - 1)"
    - "PostHog events: `clinic_audit_tab_opened` + `clinic_audit_filter_applied` (debounced); both PHI-safe per 10-EVENTS.md (booleans only for filter state)"
    - "ClinicSettingsPage NAV array extended with `audit` entry (between Phase 9's `roles` and any future entries — actually rightmost in the v1 build); tab visible-when callback gates on permission"
    - "Vitest covers: filter changes refetch; row expand/collapse; custom range validation; 13-month notice dismissal persists"
    - "Playwright e2e covers: load tab → seed 5 audit rows of varying actor/action/time → filter by member → result count drops → clear filters → result count restores"
  artifacts:
    - path: "src/components/clinic/settings/AuditTab.tsx"
      provides: "Composition root for org-owner audit surface"
    - path: "src/components/clinic/settings/use-audit-events.ts"
      provides: "Hook with filter-state debouncing + supabase-js fetch + 13-month retention enforcement client-side"
    - path: "e2e/clinic-audit.spec.ts"
      provides: "End-to-end filter + drill + clear flow"
  key_links:
    - from: "src/components/clinic/settings/AuditTab.tsx"
      to: "supabase.from('audit_logs') (RLS-guarded)"
      via: "supabase.from('audit_logs').select(...).eq('org_id', orgId).range(offset, offset+limit-1)"
      pattern: "from\\('audit_logs'\\)"
    - from: "src/components/clinic/settings/ClinicSettingsPage.tsx"
      to: "Phase 9 NAV array structure"
      via: "additive nav-entry insert"
      pattern: "audit"
---

<objective>
Build the org-owner Audit tab in ClinicSettingsPage. Reads `audit_logs` directly via supabase-js (RLS guards). 3 filters (member / action / time-range). 13-month retention notice. Per-row expand/collapse. Pagination. PostHog events PHI-safe.

Purpose: D-07 + D-08 + D-09 — fulfills CLINIC-07's org-owner-side audit surface UI half. This is a read-only operator surface; no destructive actions. Patient-side mirror modal is Plan 10-09.

Output: 7 net-new files + 1 update to ClinicSettingsPage NAV.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-clinic-operator-surface/10-CONTEXT.md
@.planning/phases/10-clinic-operator-surface/10-RESEARCH.md
@.planning/phases/10-clinic-operator-surface/10-UI-SPEC.md
@.planning/phases/10-clinic-operator-surface/10-EVENTS.md
@src/types/clinic.ts
@src/components/clinic/settings/ClinicSettingsPage.tsx
@src/components/ui/Modal.tsx
@src/components/ui/Pill.tsx
@src/lib/clinic-events.ts
@src/lib/supabase.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author 7 audit files + ClinicSettingsPage NAV extension + Vitest + Playwright</name>
  <read_first>
    - src/components/clinic/settings/ClinicSettingsPage.tsx (Phase 9 — has tab nav array; add 'audit' entry rightmost)
    - src/components/ui/Modal.tsx + Pill.tsx + Card.tsx + EmptyState.tsx + Skeleton.tsx (primitives composed)
    - src/lib/supabase.ts
    - .planning/phases/10-clinic-operator-surface/10-UI-SPEC.md §"Operator-side: Audit tab" (full copywriting + action enum table)
    - .planning/phases/10-clinic-operator-surface/10-EVENTS.md §6-7 (PHI-safe filter event)
    - src/types/clinic.ts (PERMISSION_KEYS — audit_log.read gate)
  </read_first>
  <behavior>
    - Test 1 (Vitest, NAV gating): mock `useOrgPermissions` to return `audit_log.read=false` → ClinicSettingsPage NAV does NOT contain 'audit' entry.
    - Test 2 (Vitest, fetch): AuditTab mounts → useAuditEvents calls supabase.from('audit_logs').select(...).eq('org_id', orgId).order('created_at', desc).range(0,49); on result, renders rows.
    - Test 3 (Vitest, member filter): change member dropdown → debounced 200ms → re-fetches with .eq('actor_id', selectedMemberId); event `clinic_audit_filter_applied` fires with `{ filter_type: 'member', has_member_filter: true, has_action_filter: false, has_time_filter: false }` (NO actor_id property).
    - Test 4 (Vitest, action filter): same pattern with .eq('action', selectedAction).
    - Test 5 (Vitest, time-range): time-range '7d' adds .gte('created_at', now() - 7 days) to query.
    - Test 6 (Vitest, custom range modal): from=2026-04-01, to=2026-04-15 → applies .gte + .lte; from > to → inline error; range > 13 months → inline error.
    - Test 7 (Vitest, row expand): click row → details block renders below with font-mono labels; click again → collapses; multiple rows can be expanded simultaneously.
    - Test 8 (Vitest, retention notice): on first mount, sessionStorage key `clinic_audit_retention_dismissed` is unset → notice visible; click X → notice unmounts + sessionStorage set; on remount in same session → notice does NOT render.
    - Test 9 (Vitest, empty states): zero rows AND no filters → renders "No events yet"; zero rows AND filters → renders "No events match your filters" + Clear filters CTA.
    - Test 10 (Vitest, pagination): click Next → re-fetches with offset=50.
    - Test 11 (Playwright e2e): seed 5 audit rows (3 from operator A, 2 from operator B); navigate to /clinic/{slug}/settings/audit as Owner; assert 5 rows render; filter by member A → assert 3 rows; click Clear filters → 5 rows again.
  </behavior>
  <action>
    1. **`src/components/clinic/settings/use-audit-events.ts`:** Hook signature: `useAuditEvents({ orgId, memberFilter, actionFilter, timeRangeFilter, offset, limit }) → { rows: AuditEvent[], loading, error, totalCount, refresh }`. Internally builds the `supabase.from('audit_logs').select('*, actor:actor_id(raw_user_meta_data->display_name)')` query, applying filters conditionally. Uses .range for pagination. Total count via `.select('*, count: exact')` head request.

    2. **`src/components/clinic/settings/AuditFilterBar.tsx`:** Renders 3 filter triggers. Member dropdown queries org members via `supabase.rpc('list_org_members', { p_org_id: orgId })` (Phase 9 RPC). Action-type dropdown is the 16-value enum from UI-SPEC. Time-range picker has 4 options (24h / 7d / 30d / Custom). `Apply` button triggers debounced filter-state apply + fires PostHog `clinic_audit_filter_applied` with booleans.

    3. **`src/components/clinic/settings/AuditCustomRangeModal.tsx`:** Modal with two `<input type="date">`. Validation per behavior 6.

    4. **`src/components/clinic/settings/AuditRow.tsx`:** Renders one row collapsed format `{relative-time} · {actor display name} ({role badge}) {action verb} {target description}`. Action verb derived from canonical action-enum → human label table in UI-SPEC L484-499. Click toggles expand state; expanded block is font-mono key:value pairs.

    5. **`src/components/clinic/settings/AuditTab.tsx`:** Composition root. Mount → fires PostHog `clinic_audit_tab_opened` with `{ org_id }`. Renders dismissible 13-month notice → AuditFilterBar → result count line → AuditRow list → RosterPagination (reuse from Plan 10-06). Empty states per behavior 9.

    6. **Update `src/components/clinic/settings/ClinicSettingsPage.tsx`:** add `audit` entry to the NAV array AFTER 'roles' (rightmost). Use `visibleWhen: (perms) => perms['audit_log.read']` callback per UI-SPEC L584-589. Render branch maps `'audit'` to `<AuditTab orgId={...} />`.

    7. **`src/components/clinic/settings/AuditTab.test.tsx`:** 10 Vitest assertions per behaviors 1-10.

    8. **`e2e/clinic-audit.spec.ts`:** 1 Playwright assertion per behavior 11. Seed via Phase 9 admin client. afterAll cleanup.

    **Use `git commit -- <pathspec>`.**
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/components/clinic/settings/AuditTab.tsx','src/components/clinic/settings/AuditFilterBar.tsx','src/components/clinic/settings/AuditRow.tsx','src/components/clinic/settings/AuditCustomRangeModal.tsx','src/components/clinic/settings/use-audit-events.ts','src/components/clinic/settings/AuditTab.test.tsx','e2e/clinic-audit.spec.ts']; for (const f of files) if (!fs.existsSync(f)) { console.error('MISSING '+f); process.exit(1); } const csp=fs.readFileSync('src/components/clinic/settings/ClinicSettingsPage.tsx','utf8'); if (!csp.includes(\"'audit'\")) { console.error('NAV missing audit entry'); process.exit(1); } console.log('OK 7 files + NAV');" && npm run typecheck && npm run test -- --run settings/AuditTab && npx playwright test e2e/clinic-audit.spec.ts</automated>
  </verify>
  <done>
    - 7 files exist; NAV extended; typecheck + Vitest (10) + Playwright (1) all pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator client → audit_logs SELECT | Phase 7+9 RLS on audit_logs guards by org_id IN <user's orgs with audit_log.read> |
| filter state → PostHog | bool-only event payload makes PHI leakage impossible at type level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-01 | Information disclosure | AuditTab visible to user without audit_log.read | mitigate | NAV gate via visibleWhen callback; tab is absent (no 403) for users without permission |
| T-10-08-02 | Information disclosure | PostHog event leaks selected member uuid | mitigate | event payload schema is booleans only; NEVER includes selected ids; Vitest test 3 asserts no actor_id property |
| T-10-08-03 | Information disclosure | filter for member from another org | accept | RLS gates SELECT — even if filter is set, no rows return for non-org members |
</threat_model>

<verification>
- 7 files exist; ClinicSettingsPage NAV extended.
- 10 Vitest + 1 Playwright assertion pass.
- Bundle delta: clinic-settings chunk grows ~4 kB.
</verification>

<success_criteria>
1. `npm run typecheck` passes.
2. `npm run test -- --run settings/AuditTab` passes 10 assertions.
3. `npx playwright test e2e/clinic-audit.spec.ts` passes.
4. Manual: load /clinic/{slug}/settings → click `Audit` tab (only visible if Owner) → 5 seeded audit rows render → filter by member → 3 rows → clear → 5 rows.
</success_criteria>

<output>
After completion, create `.planning/phases/10-clinic-operator-surface/10-08-SUMMARY.md` with: 7-file inventory + bundle-size delta vs Plan 10-07 close + filter latency observation.
</output>
