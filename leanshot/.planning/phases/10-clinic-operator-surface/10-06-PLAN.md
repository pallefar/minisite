---
phase: 10-clinic-operator-surface
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/components/clinic/roster/RosterTable.tsx
  - src/components/clinic/roster/RosterRow.tsx
  - src/components/clinic/roster/RosterMobileCard.tsx
  - src/components/clinic/roster/ScoreChip.tsx
  - src/components/clinic/roster/ScoreBreakdownPopover.tsx
  - src/components/clinic/roster/RosterPagination.tsx
  - src/components/clinic/roster/use-rank-roster.ts
  - src/components/clinic/roster/use-roster-realtime.ts
  - src/components/clinic/ClinicWorkspace.tsx
  - src/lib/clinic-events.ts
  - src/components/clinic/roster/RosterTable.test.tsx
  - e2e/clinic-roster-sort.spec.ts
autonomous: true
requirements: [CLINIC-04]

must_haves:
  truths:
    - "RosterTable composes 8 net-new files: RosterTable, RosterRow, RosterMobileCard, ScoreChip, ScoreBreakdownPopover, RosterPagination + 2 hooks (use-rank-roster, use-roster-realtime)"
    - "RosterTable consumes `rank_org_patients` RPC (Plan 10-02) via supabase.rpc; default sort by score DESC; column-header click triggers re-RPC with new sort params"
    - "Realtime subscription via `org-{org_id}` channel (Plan 10-03 broadcasts) patches affected row's signal columns in place; does NOT re-fire rank RPC per event"
    - "Passive 30s refetch + manual `Refresh` button re-fire the RPC for score recomputation"
    - "ScoreChip renders score 0-100 with bucket-color (sage 0-29 / neutral 30-69 / warning 70-100); when permission `roster.read_breakdown` granted, ScoreBreakdownPopover opens on click and shows per-signal weights"
    - "Threshold-cross toast fires when a Realtime broadcast results in a score crossing 70 (either direction); copy per UI-SPEC"
    - "Row click drills to `/clinic/{slug}/patient/{user_id}` (Plan 10-07's drill-in route from Plan 10-05)"
    - "Mobile breakpoint (<768px): RosterTable renders RosterMobileCard stack with 2×2 signal grid; long-press to enter selection mode (selection actually wired in Plan 10-10)"
    - "PostHog events fire per `10-EVENTS.md`: `clinic_roster_loaded`, `clinic_roster_sorted`, `clinic_patient_drilled_in`, `clinic_rank_breakdown_expanded` — all PHI-safe (no patient ids, no raw scores, only score buckets)"
    - "ClinicWorkspace.tsx is updated to render RosterTable as its primary content (replacing Phase 9's empty roster shell stub)"
    - "Vitest covers: sort triggers re-RPC; threshold-cross toast logic; ScoreChip permission gating"
    - "Playwright e2e covers: load → sort by Last Dose ASC → drill-in to first row's patient page (asserts URL change)"
  artifacts:
    - path: "src/components/clinic/roster/*.tsx"
      provides: "Roster UI composition consuming Plan 10-02 RPC + Plan 10-03 Realtime broadcasts"
    - path: "src/lib/clinic-events.ts"
      provides: "scoreBucket helper from 10-EVENTS.md + PostHog event-name constants"
      exports: ["CLINIC_EVENTS", "scoreBucket"]
    - path: "e2e/clinic-roster-sort.spec.ts"
      provides: "End-to-end sort + drill-in click flow"
  key_links:
    - from: "src/components/clinic/roster/use-rank-roster.ts"
      to: "supabase/migrations/20260901000003_rank_org_patients_rpc.sql (Plan 10-02)"
      via: "supabase.rpc('rank_org_patients', { p_org_id, p_sort_column, p_sort_direction, p_offset, p_limit })"
      pattern: "rank_org_patients"
    - from: "src/components/clinic/roster/use-roster-realtime.ts"
      to: "supabase/migrations/20260901000004_clinic_realtime_broadcast_triggers.sql (Plan 10-03)"
      via: "supabase.channel('org-' + orgId).on('broadcast', { event: 'patient_signal_change' }, ...)"
      pattern: "patient_signal_change"
    - from: "src/components/clinic/ClinicWorkspace.tsx"
      to: "src/components/clinic/roster/RosterTable.tsx"
      via: "renders RosterTable as primary content"
      pattern: "RosterTable"
---

<objective>
Build the roster UI: a server-sorted table (with mobile card-stack fallback) consuming Plan 10-02's `rank_org_patients` RPC and Plan 10-03's Realtime broadcasts. Score chip + permission-gated breakdown popover. Threshold-cross toast on Realtime updates that push a patient across the 70 boundary. Row click drills to Plan 10-07's drill-in. PostHog events fire PHI-safely per `10-EVENTS.md`. Updates ClinicWorkspace.tsx to render the roster as its primary content (replacing Phase 9's stubbed empty shell).

Purpose: D-01 + D-05 + D-06 + D-13 + D-14 + D-16 + D-17 + D-22 + D-24 — this plan is the operator's primary surface. Without it the rank RPC + broadcast triggers from Plans 10-02/10-03 are unconsumed. Bulk-select and drill-in chrome live in Plans 10-10 and 10-07 respectively; this plan ships the read-only roster + sort + drill-in click + Realtime patching.

Output: 8 net-new files + 1 updated ClinicWorkspace + Vitest + 1 Playwright spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-clinic-operator-surface/10-CONTEXT.md
@.planning/phases/10-clinic-operator-surface/10-RESEARCH.md
@.planning/phases/10-clinic-operator-surface/10-UI-SPEC.md
@.planning/phases/10-clinic-operator-surface/10-EVENTS.md
@src/types/snapshot.ts
@src/types/clinic.ts
@src/components/clinic/ClinicWorkspace.tsx
@src/components/ui/Card.tsx
@src/components/ui/Button.tsx
@src/components/ui/Pill.tsx
@src/components/ui/Skeleton.tsx
@src/components/ui/EmptyState.tsx
@src/hooks/useToast.ts
@src/hooks/useReducedMotion.ts
@src/lib/supabase.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author 8 roster files + clinic-events lib + ClinicWorkspace integration</name>
  <read_first>
    - src/components/clinic/ClinicWorkspace.tsx (Phase 9 — currently renders empty roster shell; we now render RosterTable inside)
    - src/components/ui/Card.tsx + Button.tsx + Pill.tsx + Skeleton.tsx + EmptyState.tsx (primitives composed)
    - src/lib/supabase.ts (singleton client; rpc + channel methods)
    - src/hooks/useToast.ts + useReducedMotion.ts
    - .planning/phases/10-clinic-operator-surface/10-UI-SPEC.md §"Operator-side: Roster page" (full copywriting)
    - .planning/phases/10-clinic-operator-surface/10-EVENTS.md (PostHog event names + PHI-safety contract)
    - src/types/snapshot.ts (RankRosterRow, scoreBucket, ScoreBucket)
  </read_first>
  <behavior>
    - Test 1 (RosterTable.test.tsx — sort): clicking column header `Last dose` calls supabase.rpc with `p_sort_column='last_dose', p_sort_direction='desc'`; second click toggles to `asc`; third click reverts to default `score, desc`.
    - Test 2 (Realtime patch): broadcast `{event:'patient_signal_change', user_id:'A', section:'injections', changed_at}` patches row A's `last_injection_at` column to `changed_at` and triggers row flash (200ms class application; respects prefers-reduced-motion).
    - Test 3 (Threshold-cross toast): in-memory state with patient A at score 65; Realtime broadcast triggers passive refetch which returns score 75 → useToast called with copy `{firstName} now needs attention.`. Reverse direction → `{firstName}'s score improved.` (lower priority tone).
    - Test 4 (ScoreChip permission gating): when `permissionMap.canViewBreakdown=false` (Viewer role), ScoreChip is non-interactive (no click, no popover). When true (Coach/Owner), click opens ScoreBreakdownPopover with per-signal weights.
    - Test 5 (PostHog event PHI): `posthog.capture('clinic_patient_drilled_in', { org_id, score_bucket })` is called on row click — assert NO `patient_user_id`, NO `score` (raw value), NO `patient_name` properties in the captured event.
    - Test 6 (mobile card-stack): viewport width < 768px renders RosterMobileCard instead of RosterTable rows.
    - Test 7 (clinic-roster-sort.spec.ts e2e): seed 3 patients with distinct last-dose dates; sign in as operator; navigate to `/clinic/{slug}`; assert default sort by score DESC; click `Last dose` header; assert URL stays the same and DOM order changes; click first row → URL becomes `/clinic/{slug}/patient/{user_id}`.
  </behavior>
  <action>
    1. **`src/lib/clinic-events.ts`:** Export `scoreBucket()` (already in `src/types/snapshot.ts` — re-export from there) and `CLINIC_EVENTS` constant matching `10-EVENTS.md` enum (10 event-name strings). DO NOT inline patient_user_id or score values in any helper function — the type signature should make leakage impossible (e.g., helper accepts `score: number` and returns `score_bucket: ScoreBucket`).

    2. **`src/components/clinic/roster/use-rank-roster.ts`:** Custom hook. Signature: `useRankRoster({ orgId, sortColumn, sortDirection, offset, limit }) → { rows: RankRosterRow[], loading: boolean, error: Error|null, refresh: () => void, lastFetchedAt: Date|null }`. Internally: `useEffect` fires `supabase.rpc('rank_org_patients', { p_org_id: orgId, p_sort_column: sortColumn, p_sort_direction: sortDirection, p_offset: offset, p_limit: limit })` on dep changes. Tracks lastFetchedAt for "Refreshed Xs ago" hint. Error → 401/403 routes to error boundary; other errors set error state.

    3. **`src/components/clinic/roster/use-roster-realtime.ts`:** Hook. Signature: `useRosterRealtime({ orgId, onSignalChange })`. Internally: subscribe to `supabase.channel('org-' + orgId).on('broadcast', { event: 'patient_signal_change' }, payload => onSignalChange(payload))` on mount; remove channel on unmount. Returns nothing.

    4. **`src/components/clinic/roster/RosterTable.tsx`:** Composition root. Manages sort state + offset state + selection state (selection wired by Plan 10-10; this plan exposes the prop). Calls useRankRoster + useRosterRealtime. Maintains a `prevScoreByUserId` Map for threshold-crossing detection. On useRankRoster result update, diff against prev scores; for any patient crossing 70 boundary call `useToast({ message: thresholdCopy, tone: direction === 'up' ? 'warning' : 'neutral' })`. Renders desktop table (responsive container with 768px breakpoint via Tailwind `md:`). Each column header is `<button>` with `aria-sort` per UI-SPEC. PostHog event `clinic_roster_loaded` fires once per useRankRoster completion with `{ org_id, patient_count: rows.length, render_ms: performance.now() - fetchStart }`. Sort change fires `clinic_roster_sorted` with `{ column, direction }`.

    5. **`src/components/clinic/roster/RosterRow.tsx`:** Renders one `<tr>` with cells: ScoreChip, patient display name, last-dose relative-time, weight-trend arrow icon, recent_symptom_severity `N/5`, days-since-injection (with warning glyph when > 14d), missed-dose icon. `tabIndex={0}`, `role="row"`. Click handler routes to `/clinic/{slug}/patient/{user_id}` AND fires PostHog `clinic_patient_drilled_in` with `{ org_id, score_bucket: scoreBucket(row.score) }` BEFORE navigating. Realtime row flash class applied via 200ms transition; respects useReducedMotion.

    6. **`src/components/clinic/roster/RosterMobileCard.tsx`:** Same data as RosterRow but rendered as a Card with 2×2 signal grid per UI-SPEC L74. Same click + PostHog handlers.

    7. **`src/components/clinic/roster/ScoreChip.tsx`:** Renders score numeral inside a Pill colored per bucket. Permission gate: `permissionMap.canViewBreakdown` from prop → if true, wraps in `<button aria-haspopup="true" aria-expanded={open}>` and opens ScoreBreakdownPopover; if false, renders read-only span. PostHog event `clinic_rank_breakdown_expanded` with `{ score_bucket }` fires when popover opens.

    8. **`src/components/clinic/roster/ScoreBreakdownPopover.tsx`:** Modal-like popover anchored to the score chip. Renders `breakdown` jsonb as `{signal_name}: {weight}` rows. Close on Escape; focus trap; focus returns to ScoreChip on close.

    9. **`src/components/clinic/roster/RosterPagination.tsx`:** `Previous` / `Next` buttons + `Showing X–Y of N` label. Button disabled state per UI-SPEC.

    10. **Update `src/components/clinic/ClinicWorkspace.tsx`:** Read current Phase 9 file, replace the empty-roster placeholder section with `<RosterTable orgId={org.id} permissionMap={...} />`. Pass permissionMap derived from operator's role permissions (read from Phase 9 useOrgRole hook or equivalent). Workspace empty state copy (when 0 patients) is Phase 9 D-08 unchanged — RosterTable's EmptyState renders it.

    11. **Vitest `RosterTable.test.tsx`:** 6 assertions per behavior list (omit e2e test 7).

    12. **Playwright `e2e/clinic-roster-sort.spec.ts`:** seed 3 patients via Supabase admin client (extract reusable seed helper for Plan 10-11 to consume); sign in as operator; assert sort + drill-in flow per behavior 7.

    **Use `git commit -- <pathspec>` per logical batch (lib + hooks; UI components; ClinicWorkspace integration; tests).**
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/components/clinic/roster/RosterTable.tsx','src/components/clinic/roster/RosterRow.tsx','src/components/clinic/roster/RosterMobileCard.tsx','src/components/clinic/roster/ScoreChip.tsx','src/components/clinic/roster/ScoreBreakdownPopover.tsx','src/components/clinic/roster/RosterPagination.tsx','src/components/clinic/roster/use-rank-roster.ts','src/components/clinic/roster/use-roster-realtime.ts','src/lib/clinic-events.ts','src/components/clinic/roster/RosterTable.test.tsx','e2e/clinic-roster-sort.spec.ts']; for (const f of files) if (!fs.existsSync(f)) { console.error('MISSING '+f); process.exit(1); } console.log('OK 11 files');" && npm run typecheck && npm run test -- --run roster/RosterTable && npx playwright test e2e/clinic-roster-sort.spec.ts</automated>
  </verify>
  <done>
    - 11 files exist; typecheck passes; Vitest passes 6 assertions; Playwright spec passes.
    - ClinicWorkspace renders the roster as primary content.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator client → rank_org_patients RPC | Plan 10-02 RPC enforces RLS via has_permission; this plan trusts that gate |
| Realtime broadcast payload → roster UI | Server-emitted (Plan 10-03 trigger); payload is { user_id, section, changed_at } only — no PHI beyond what operator already sees |
| PostHog event capture | Bucket helper makes raw score leakage impossible by signature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Information disclosure | PostHog event captures raw score or patient_user_id | mitigate | scoreBucket() helper; type signature accepts score and returns bucket; Vitest test 5 asserts no PHI properties present |
| T-10-06-02 | Information disclosure | ScoreBreakdownPopover renders per-signal weights for Viewer role | mitigate | permissionMap.canViewBreakdown gate; popover unreachable when false |
| T-10-06-03 | DoS | Realtime broadcast spam triggers re-render storm | accept | broadcast filtered server-side by consent_scope (Plan 10-03); high-frequency events would still affect 1-3 rows max; React reconciliation handles |
| T-10-06-04 | Tampering | client manipulates sort param to bypass server validation | mitigate | Plan 10-02 RPC validates p_sort_column whitelist server-side; client sends arbitrary string but RPC raises |
</threat_model>

<verification>
- 11 files exist; typecheck + Vitest + Playwright spec all green.
- ClinicWorkspace updated; renders RosterTable.
- ScoreChip permission gating verified.
- PostHog events PHI-safe (Vitest assertion).
- Bundle delta: clinic chunk grows ~6 kB for the 8 new files.
</verification>

<success_criteria>
1. `npm run typecheck` passes.
2. `npm run test -- --run roster/RosterTable` passes 6 assertions.
3. `npx playwright test e2e/clinic-roster-sort.spec.ts` passes.
4. Manual: load /clinic/{slug} as operator → roster renders with score chips; sort by Last Dose; click first row → drill-in URL matches.
5. PostHog event grep: `! grep -rE "(patient_user_id|score: [0-9])" src/components/clinic/roster/` returns zero matches outside test files.
</success_criteria>

<output>
After completion, create `.planning/phases/10-clinic-operator-surface/10-06-SUMMARY.md` with: 11-file inventory + bundle-size delta vs Plan 10-05 close + Realtime broadcast latency observation + PostHog event PHI-safety grep result.
</output>
