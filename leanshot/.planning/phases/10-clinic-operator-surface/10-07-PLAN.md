---
phase: 10-clinic-operator-surface
plan: 07
type: execute
wave: 3
depends_on: [04, 05, 06]
files_modified:
  - src/components/clinic/drill-in/ClinicDrillInPage.tsx
  - src/components/clinic/drill-in/ClinicDrillInSubBar.tsx
  - src/components/clinic/drill-in/use-clinic-snapshot.ts
  - src/components/clinic/drill-in/ClinicDrillInPage.test.tsx
  - e2e/clinic-drill-in.spec.ts
autonomous: true
requirements: [CLINIC-05, CLINIC-07]

must_haves:
  truths:
    - "ClinicDrillInPage replaces the Plan 10-05 stub at `src/components/clinic/drill-in/ClinicDrillInPage.tsx`"
    - "Renders ClinicContextBar (Phase 9 component) + ClinicDrillInSubBar + ReadOnlyPatientView with viewerMode='clinic' + permissionMap from snapshot response"
    - "use-clinic-snapshot hook fetches via `clinic-snapshot` Edge Function (Plan 10-04); on 401 routes back to /clinic/{slug} after toast `{firstName} revoked access. Returning to roster.`; on 403 leaves user on page but unmounts the offending section + toast `{firstName} updated what they share. This data is no longer available.`"
    - "onSectionMount callback (passed to ReadOnlyPatientView) fires `supabase.rpc('log_clinic_view', { p_org_id, p_target_user_id, p_section_name })` AND `posthog.capture('clinic_drill_section_expanded', { section_name })` ONCE per section per drill-in instance (debounced via useRef Map)"
    - "Sub-bar renders: back-to-roster button, patient first-name + last-initial heading, Joined relative-time + sharing scope summary, Refreshed timestamp, Refresh IconButton, View activity IconButton (opens PatientActivityModal — Plan 10-09 component, this plan invokes via callback)"
    - "Back button preserves prior roster sort + offset state via session-storage round-trip (key `clinic_roster_state_{org_id}`)"
    - "Vitest covers: snapshot fetch + render with all sections; 401 toast + redirect; 403 toast + section unmount; per-section log_clinic_view fire on first mount only"
    - "Playwright e2e covers: drill in to a patient → assert all permitted sections render → assert one log_clinic_view audit row per visible section in DB"
  artifacts:
    - path: "src/components/clinic/drill-in/ClinicDrillInPage.tsx"
      provides: "Drill-in composition root replacing Plan 10-05 stub"
    - path: "src/components/clinic/drill-in/ClinicDrillInSubBar.tsx"
      provides: "Sub-bar with back/patient-name/refresh/view-activity affordances"
    - path: "src/components/clinic/drill-in/use-clinic-snapshot.ts"
      provides: "Fetch hook for clinic-snapshot Edge Function with 401/403 handling"
    - path: "e2e/clinic-drill-in.spec.ts"
      provides: "End-to-end drill-in + section audit row verification"
  key_links:
    - from: "src/components/clinic/drill-in/ClinicDrillInPage.tsx"
      to: "src/components/shared/ReadOnlyPatientView.tsx (Plan 10-05)"
      via: "renders ReadOnlyPatientView with viewerMode='clinic' + permissionMap + onSectionMount"
      pattern: "<ReadOnlyPatientView"
    - from: "src/components/clinic/drill-in/use-clinic-snapshot.ts"
      to: "supabase/functions/clinic-snapshot/index.ts (Plan 10-04)"
      via: "fetch('/functions/v1/clinic-snapshot?org_id=...&patient_id=...', { headers: { Authorization: 'Bearer ' + jwt } })"
      pattern: "clinic-snapshot"
    - from: "src/components/clinic/drill-in/ClinicDrillInPage.tsx"
      to: "src/components/clinic/ClinicContextBar.tsx (Phase 9)"
      via: "renders ClinicContextBar at top"
      pattern: "<ClinicContextBar"
---

<objective>
Build the operator's drill-in page that wraps Plan 10-05's `ReadOnlyPatientView` with clinic-mode chrome (ClinicContextBar from Phase 9 + sub-bar with back/patient/refresh/view-activity). Fetches snapshot via Plan 10-04's Edge Function. Per-section audit fires via Plan 10-04's `log_clinic_view` RPC. 401/403 handling per Phase 9 D-10/D-11 carry-forward.

Purpose: D-03 + D-04 + D-12 + D-15 + D-21 — this is the operator's view-into-one-patient surface. ReadOnlyPatientView (Plan 10-05) is the body; this plan supplies the clinic-mode chrome and the audit/permission glue.

Output: 5 net-new files (1 page, 1 sub-bar, 1 hook, 1 Vitest, 1 Playwright); Plan 10-05's stub is overwritten in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-clinic-operator-surface/10-CONTEXT.md
@.planning/phases/10-clinic-operator-surface/10-RESEARCH.md
@.planning/phases/10-clinic-operator-surface/10-UI-SPEC.md
@.planning/phases/10-clinic-operator-surface/10-EVENTS.md
@src/types/snapshot.ts
@src/components/clinic/ClinicContextBar.tsx
@src/components/shared/ReadOnlyPatientView.tsx
@src/components/ui/Button.tsx
@src/hooks/useToast.ts
@src/lib/supabase.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author drill-in page + sub-bar + snapshot hook + Vitest + Playwright</name>
  <read_first>
    - src/components/clinic/drill-in/ClinicDrillInPage.tsx (Plan 10-05 stub — overwrite in place)
    - src/components/clinic/ClinicContextBar.tsx (Phase 9 — render at top of drill-in unchanged)
    - src/components/shared/ReadOnlyPatientView.tsx (Plan 10-05 — onSectionMount integration)
    - src/types/snapshot.ts (SnapshotData + ReadOnlyPermissionMap)
    - .planning/phases/10-clinic-operator-surface/10-UI-SPEC.md §"Operator-side: Drill-in" (sub-bar copy + 401/403 handling)
    - .planning/phases/10-clinic-operator-surface/10-EVENTS.md §"5. clinic_drill_section_expanded"
    - src/lib/supabase.ts (singleton client + URL helper)
    - src/hooks/useToast.ts
  </read_first>
  <behavior>
    - Test 1 (Vitest, snapshot fetch): hook calls Edge Function on mount with current URL params; on 200, returns snapshot data + sets loading=false.
    - Test 2 (Vitest, render with permissions): given permissionMap with all true → ClinicDrillInPage renders ClinicContextBar + sub-bar + 6 visible sections (via ReadOnlyPatientView).
    - Test 3 (Vitest, permission absence): permissionMap.canViewPhotos=false → Photos section is absent (not a placeholder); 5 sections visible.
    - Test 4 (Vitest, log_clinic_view fire): on first mount of each visible section, ClinicDrillInPage's onSectionMount handler calls supabase.rpc('log_clinic_view', { p_org_id, p_target_user_id, p_section_name }) ONCE per section. PostHog event `clinic_drill_section_expanded` fires alongside.
    - Test 5 (Vitest, no duplicate fires): re-rendering ClinicDrillInPage (state change) does NOT re-fire log_clinic_view for already-mounted sections.
    - Test 6 (Vitest, 401 handling): Edge Function returns 401 → useToast called with copy `{firstName} revoked access. Returning to roster.`; navigate fires after 1s; URL becomes /clinic/{slug}.
    - Test 7 (Vitest, 403 handling): Edge Function returns 403 with metadata `{ blocked_section: 'photos' }` → useToast called with `{firstName} updated what they share. This data is no longer available.`; only the photos section unmounts; other sections remain.
    - Test 8 (Vitest, back button): click `Back to roster` → reads sessionStorage[`clinic_roster_state_{org_id}`] (sort + offset) → routes to /clinic/{slug} with that state restored.
    - Test 9 (Vitest, refresh): click Refresh IconButton → re-fires Edge Function fetch.
    - Test 10 (Playwright e2e): seed 1 patient with full consent_scope; sign in as operator with all permissions; navigate to /clinic/{slug}/patient/{user_id}; assert all 6 sections render; query DB for audit_logs rows where action='section_view' AND target_user_id={patient} → assert 6 rows (one per section name).
  </behavior>
  <action>
    1. **`src/components/clinic/drill-in/use-clinic-snapshot.ts`:**

       Hook signature: `useClinicSnapshot({ orgId, patientId }) → { snapshot: SnapshotData|null, loading: boolean, error: { status: 401|403|500, blockedSection?: string }|null, refresh: () => void, lastFetchedAt: Date|null }`. Internally: `fetch('${SUPABASE_URL}/functions/v1/clinic-snapshot?org_id=' + orgId + '&patient_id=' + patientId, { headers: { Authorization: 'Bearer ' + (await supabase.auth.getSession()).data.session.access_token } })`. On status 401/403/500 set error state with parsed JSON metadata.

    2. **`src/components/clinic/drill-in/ClinicDrillInSubBar.tsx`:**

       Props: `{ patientName, joinedAt, scopeSummary, lastFetchedAt, onBack, onRefresh, onViewActivity }`. Renders the sub-bar per UI-SPEC L424-440. Uses Button + IconButton primitives. Mobile responsive (back button visible on all sizes; label `← Roster` hidden on `<md` per UI-SPEC).

    3. **`src/components/clinic/drill-in/ClinicDrillInPage.tsx` (OVERWRITES Plan 10-05 stub):**

       Composition:
       - Parse `slug` and `user_id` from `window.location.pathname` (or via existing routing helper).
       - Look up `org_id` from slug via Phase 9 helper (e.g., useOrgBySlug hook from Phase 9; if missing, executor adds it as part of this plan).
       - Call useClinicSnapshot({ orgId, patientId: user_id }).
       - Render ClinicContextBar (Phase 9) at top.
       - Render ClinicDrillInSubBar with: patient first name + last initial from snapshot.display_name, joinedAt + scope summary derived from snapshot.consent_scope counts, lastFetchedAt from hook, callbacks for refresh + back + view-activity.
       - Render `<ReadOnlyPatientView snapshot={snapshot} viewerMode='clinic' permissionMap={snapshot.permission_map} onSectionMount={handleSectionMount} />` when snapshot is loaded.
       - `handleSectionMount(sectionName: string)`: useRef-guarded Map<sectionName, true>. First call per section: `await supabase.rpc('log_clinic_view', { p_org_id: orgId, p_target_user_id: user_id, p_section_name: sectionName })` AND `posthog.capture('clinic_drill_section_expanded', { section_name: sectionName })`. Subsequent calls: no-op.
       - On error.status === 401: useToast({ message: `${firstName} revoked access. Returning to roster.`, tone: 'destructive' }); setTimeout(() => navigate('/clinic/' + slug), 1000).
       - On error.status === 403 with blockedSection: useToast({ message: `${firstName} updated what they share. This data is no longer available.`, tone: 'warning' }); update local permissionMap to set `canView{Section}=false` so ReadOnlyPatientView re-renders without that section.
       - View-activity callback: open PatientActivityModal (Plan 10-09 component — this plan imports it). For now, accept that the modal component file may not yet exist when this plan runs; import the path that Plan 10-09 will create. If executor finds the import unresolved, leave the callback as a TODO comment with a `console.warn` and Plan 10-09 wires it.

    4. **`src/components/clinic/drill-in/ClinicDrillInPage.test.tsx`:** 9 Vitest assertions per behaviors 1-9.

    5. **`e2e/clinic-drill-in.spec.ts`:** 1 Playwright assertion per behavior 10. Use Phase 9 admin-client seed pattern. afterAll cleanup.

    **Use `git commit -- <pathspec>`.**
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/components/clinic/drill-in/ClinicDrillInPage.tsx','src/components/clinic/drill-in/ClinicDrillInSubBar.tsx','src/components/clinic/drill-in/use-clinic-snapshot.ts','src/components/clinic/drill-in/ClinicDrillInPage.test.tsx','e2e/clinic-drill-in.spec.ts']; for (const f of files) if (!fs.existsSync(f)) { console.error('MISSING '+f); process.exit(1); } const stub=fs.readFileSync('src/components/clinic/drill-in/ClinicDrillInPage.tsx','utf8'); if (stub.includes('Phase 10 stub')) { console.error('Stub not overwritten'); process.exit(1); } console.log('OK 5 files');" && npm run typecheck && npm run test -- --run drill-in/ClinicDrillInPage && npx playwright test e2e/clinic-drill-in.spec.ts</automated>
  </verify>
  <done>
    - 5 files exist; stub overwritten; typecheck + Vitest (9) + Playwright pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator client → clinic-snapshot Edge Function | JWT validates membership + permission |
| use-clinic-snapshot → 401/403 handler | Status code interpretation drives UX rollback |
| onSectionMount fire | Calls log_clinic_view RPC; RPC re-validates auth + membership server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Information disclosure | drill-in renders sections beyond consent_scope | mitigate | server (Plan 10-04) omits sections; client permissionMap is back-up; Test 3 verifies absence |
| T-10-07-02 | Repudiation | log_clinic_view audit row not written when section component crashes | accept | error boundary catches; missed audit row preferable to crashed UI per Phase 7 pattern |
| T-10-07-03 | Information disclosure | 403 toast text leaks patient first name to console | accept | first name is already visible to operator (they're viewing the patient); toast leak is no incremental disclosure |
| T-10-07-04 | DoS | rapid 401-redirect loop if Edge Function flaps | mitigate | one-shot setTimeout 1s before navigate; user can manually navigate before redirect; ignored on subsequent 401 within same drill-in session |
</threat_model>

<verification>
- 5 files exist; stub overwritten with real implementation.
- 9 Vitest assertions + 1 Playwright assertion pass.
- log_clinic_view audit rows present in DB after drill-in (Playwright assertion).
- 401/403 toast + redirect/section-unmount behavior verified.
</verification>

<success_criteria>
1. `npm run typecheck` passes.
2. `npm run test -- --run drill-in/ClinicDrillInPage` passes 9 assertions.
3. `npx playwright test e2e/clinic-drill-in.spec.ts` passes; DB query confirms one audit_logs row per visible section.
4. Manual: drill in to a patient → all sections render → check Supabase Logs for log_clinic_view RPC calls (one per section).
</success_criteria>

<output>
After completion, create `.planning/phases/10-clinic-operator-surface/10-07-SUMMARY.md` with: 5-file inventory + audit row count observed in test fixture + 401/403 handling verification + bundle-size delta vs Plan 10-06 close.
</output>
