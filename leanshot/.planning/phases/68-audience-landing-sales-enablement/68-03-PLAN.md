---
phase: 68-audience-landing-sales-enablement
plan: 3
wave: 2
depends_on: [68-01]
files_modified:
  - leanshot/src/components/landing/AudienceLandingPage.tsx
  - leanshot/src/components/landing/AudienceLandingPage.test.tsx
  - leanshot/src/App.tsx
  - leanshot/public/sitemap.xml
autonomous: true
requirements: [LAND-01, LAND-02, LAND-03, LAND-04]
---

<objective>
Render 3 audience landing pages at `/for-doctors`, `/for-clinics`, `/for-coaches`. Page-builder seed rows from 68-01 ARE the content; this plan ships the resolver component + route wiring + JSON-LD emission + sitemap.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: <AudienceLandingPage> component</name>
  <files>leanshot/src/components/landing/AudienceLandingPage.tsx, leanshot/src/components/landing/AudienceLandingPage.test.tsx</files>
  <action>
Single component that fetches a `landing_pages` row by slug from URL path + renders via existing page-builder block renderer (check `src/lib/page-builder/block-renderer.ts` or similar).

Props: none (reads from React Router `useParams` or `window.location.pathname`).

Logic:
1. Get current pathname (e.g. `/for-doctors`)
2. Fetch `landing_pages WHERE slug = ?` via Supabase anon client + RLS allows public-read
3. If found:
   - Render the `blocks` jsonb via page-builder's existing renderer
   - Emit `<script type="application/ld+json">` from `seo.json_ld` field using `src/lib/page-builder/json-ld.ts` helper
   - Set `<title>` + `<meta name="description">` from `seo` via existing helmet pattern (`react-helmet-async` per Phase 60-13)
4. If not found: 404 component

Test:
- Mock supabase fetch returning sample landing_pages row for /for-doctors → component renders hero text + JSON-LD script tag with correct serviceType
- Mock fetch returning null → 404 rendered
- 3 tests one per audience (parameterized) confirm JSON-LD has correct `audienceType` per audience

Tailwind v4 @theme tokens only. Typography 4-size / 2-weight ceiling.
  </action>
</task>

<task type="auto">
  <name>Task 2: Wire 3 routes in App.tsx</name>
  <files>leanshot/src/App.tsx</files>
  <action>
Read existing App.tsx routing pattern (likely switch-based per Phase 65/66 precedent). Add 3 cases:

```tsx
case path.startsWith('/for-doctors'): return <AudienceLandingPage />;
case path.startsWith('/for-clinics'): return <AudienceLandingPage />;
case path.startsWith('/for-coaches'): return <AudienceLandingPage />;
```

Add the cases BEFORE the catch-all default (per `[[feedback_admin_module_manifest_vs_router_branch_drift]]`). Each route maps to the SAME component; component infers slug from pathname.

Note: this is a Vite SPA — server-side `<title>` SEO needs a Vercel rewrite or static HTML; the JSON-LD via react-helmet-async is the launch baseline. Document in SUMMARY.
  </action>
</task>

<task type="auto">
  <name>Task 3: Sitemap inclusion</name>
  <files>leanshot/public/sitemap.xml</files>
  <action>
Inspect existing `leanshot/public/sitemap.xml` (likely from Phase 60-13). Add 3 new <url> entries for the audience pages with `<priority>0.9</priority>` + `<changefreq>monthly</changefreq>` + current `<lastmod>`.

Also check for a sitemap-builder script (e.g. `scripts/build-sitemap.ts`) — if present, update its static-route enumeration to include the 3 new paths. If not, the manual edit is fine.
  </action>
</task>

</tasks>
