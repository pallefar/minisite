---
phase: 68-audience-landing-sales-enablement
plan: 4
wave: 2
depends_on: [68-01]
files_modified:
  - supabase/functions/traffic-attribution-recorder/index.ts
  - supabase/functions/traffic-attribution-recorder/handler.ts
  - supabase/functions/traffic-attribution-recorder/__tests__/handler.test.ts
autonomous: true
requirements: [LAND-07, LAND-08]
---

<objective>
Extend existing Phase 51 `traffic-attribution-recorder` Edge Fn with: (1) `landing_page` dimension capture in PostHog `$pageview` and `$sign_up` events; (2) UTM-default-landing resolver — 307 redirect to mapped path when user lands on `/` with a UTM source that has a `utm_landing_defaults` row.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: landing_page dimension capture (LAND-07)</name>
  <files>supabase/functions/traffic-attribution-recorder/handler.ts, supabase/functions/traffic-attribution-recorder/__tests__/handler.test.ts</files>
  <action>
Inspect existing handler.ts. Add `landing_page` to the PostHog event properties:
- From the request URL's pathname (initial landing path) — first-touch dimension
- Captured once per session into `attribution_first_touch` table (or wherever Phase 51 stores first-touch)

Test: confirm new event property is in the PostHog payload when handler is called with sample landing URL.
  </action>
</task>

<task type="auto">
  <name>Task 2: UTM-default-landing resolver (LAND-08)</name>
  <files>supabase/functions/traffic-attribution-recorder/handler.ts</files>
  <action>
Add resolver step. When the incoming request:
- has pathname == `/` AND
- has a query param `utm_source` whose value matches a row in `utm_landing_defaults`
THEN respond with `307 Temporary Redirect` to the mapped `landing_path` + preserve all original query params (including the UTM source).

If no match → continue with normal attribution recording.

Note: this Fn may not actually be hit on `/` landing — landing-page routing is client-side in Vite SPA. The redirect mechanism may need to live in `leanshot/middleware.ts` instead. Audit existing middleware.ts for landing-redirect logic; if landing routing happens server-side via Vercel rewrites, the Fn is the right place. Otherwise, ADD the resolver to middleware.ts instead.

Document the decision in SUMMARY.

Test: 3 cases — `/?utm_source=clinic_outreach` → 307 to `/for-clinics`; `/?utm_source=unknown` → pass-through; `/some-other-path?utm_source=clinic_outreach` → pass-through (resolver only fires on root).
  </action>
</task>

</tasks>
