---
phase: 12-bootstrap-bundle-foundations
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified:
  - leanshot/.planning/PROJECT.md
  - leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json
  - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md
autonomous: false
nyquist_compliant: false
requirements: [SC-4, SC-5, CCC-6, CCC-7]
tags: [dns, resend, vendor-accounts, credentials, human-action]
user_setup:
  - service: resend
    why: "Verify app.leanshot.app domain for SPF/DKIM/DMARC and send a real lifecycle email from noreply@app.leanshot.app (Phase 12 SC-4 part 2)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Already in Supabase Function secrets per Phase 9 wiring (memory reference_resend_phase9_wiring.md); verify with `supabase secrets list`"
      - name: RESEND_FROM
        source: "Already in Supabase Function secrets; should be `LeanShot <noreply@app.leanshot.app>` per D-17"
    dashboard_config:
      - task: "Add app.leanshot.app domain to Resend"
        location: "Resend Dashboard → Domains → Add Domain"
      - task: "Publish SPF + DKIM + DMARC DNS records at registrar"
        location: "User's DNS registrar admin panel for leanshot.app (records per RESEARCH §6)"
      - task: "Trigger domain verification via Resend"
        location: "Resend Dashboard → Domains → app.leanshot.app → Verify, OR curl POST /domains/$ID/verify"
  - service: apple-developer-program
    why: "Required for Phase 16 (Capacitor iOS shell, TestFlight, native plugin testing) — D-05 makes this a Phase 12 gate"
    env_vars:
      - name: APPLE_TEAM_ID
        source: "Apple Developer Portal → Membership"
      - name: APPLE_BUNDLE_ID
        source: "Decide and reserve at App Store Connect; suggested: app.leanshot.ios"
    dashboard_config:
      - task: "Enroll in Apple Developer Program ($99/yr)"
        location: "https://developer.apple.com/programs/"
  - service: google-play-console
    why: "Required for Phase 16 (Capacitor Android shell, internal testing, signed AAB) — D-05 makes this a Phase 12 gate"
    env_vars:
      - name: PLAY_PACKAGE_NAME
        source: "Decide and reserve at Play Console; suggested: app.leanshot.android"
      - name: PLAY_SERVICE_ACCOUNT_JSON
        source: "Google Cloud Console → IAM → Service Accounts → Create Key (JSON) for the Play Developer API service account"
    dashboard_config:
      - task: "Register Google Play Console account ($25 one-time)"
        location: "https://play.google.com/console"
      - task: "Create OAuth service account + download JSON"
        location: "Google Cloud Console → IAM"
  - service: stripe-connect-express
    why: "Required for Phase 14 (Stripe Checkout + Customer Portal + clinic metered billing) and Phase 19 (affiliate Connect Express + W-9/W-8BEN/1099-NEC) — D-05 makes this a Phase 12 gate"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard → Developers → API keys (TEST key for Phase 12 → live key at Phase 14 billing go-live)"
      - name: STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard → Developers → API keys"
      - name: STRIPE_CONNECT_CLIENT_ID
        source: "Stripe Dashboard → Connect → Settings (ca_...)"
    dashboard_config:
      - task: "Activate Stripe account + request Connect Express platform approval"
        location: "https://dashboard.stripe.com/connect → Get started → Select Express"
must_haves:
  truths:
    - "User receives a real lifecycle email from `LeanShot <noreply@app.leanshot.app>` in their inbox (NOT spam, NOT from sandbox `onboarding@resend.dev`). Inbox screenshot or message-ID logged."
    - "`curl -H \"Authorization: Bearer $RESEND_API_KEY\" https://api.resend.com/domains` returns JSON containing an entry where `name == 'app.leanshot.app'` AND `status == 'verified'`."
    - "DNS records for SPF + DKIM (Resend-provided CNAME) + DMARC (p=quarantine initially per D-17) are live at the registrar for app.leanshot.app and propagated (verifiable via `dig`)."
    - "If Apple Developer Program is provisioned: APPLE_TEAM_ID + APPLE_BUNDLE_ID captured in Vercel env (verified by `vercel env ls | grep -E 'APPLE_(TEAM|BUNDLE)_ID'`)."
    - "If Google Play Console is provisioned: PLAY_PACKAGE_NAME reserved; PLAY_SERVICE_ACCOUNT_JSON captured in Supabase Function secrets (verified by `supabase secrets list | grep PLAY_SERVICE_ACCOUNT_JSON`)."
    - "If Stripe Connect Express is provisioned: STRIPE_SECRET_KEY (`sk_test_*` — TEST-mode at Phase 12 per T-PAY-01; live key swaps in at Phase 14) + STRIPE_PUBLISHABLE_KEY (`pk_test_*`) + STRIPE_CONNECT_CLIENT_ID (`ca_*`) captured in Vercel env (verified by `vercel env ls | grep -E 'STRIPE_(SECRET_KEY|PUBLISHABLE_KEY|CONNECT_CLIENT_ID)'` AND `vercel env pull --environment production /tmp/env && grep '^STRIPE_SECRET_KEY=sk_test_' /tmp/env`)."
    - "If any vendor (Apple/Play/Stripe) is in pending-review state: 12-VALIDATION.md rows 12-05-03/04/05 carry `⚠️ pending` with submission-date + ETA. Phase 12 stays in `closing` status (NOT closed) until every `⚠️ pending` row resolves to `✅ green`. Phase 13 (Design System) is unblocked and may start in parallel per CONTEXT D-05 + RESEARCH Open Question #3 resolution."
    - "PROJECT.md `## Vendor accounts` section reflects the current credential capture state per D-06 naming convention with a capture-date timestamp for each row."
    - "12-CONTEXT.md is updated with the resend-domain-proof.json link AND any deviations from the rough credential names noted in `_specifics_` for downstream phases to reference."
  artifacts:
    - path: "leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json"
      provides: "Persistent proof that app.leanshot.app verification succeeded — captured from `curl api.resend.com/domains/$ID`"
      contains: "verified"
    - path: "leanshot/.planning/PROJECT.md"
      provides: "Authoritative vendor-accounts table with capture dates"
      contains: "Vendor accounts"
    - path: "leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md"
      provides: "Phase 12 close addendum with Wave 2 outcomes"
      contains: "resend-domain-proof.json"
  key_links:
    - from: "Resend Dashboard `app.leanshot.app`"
      to: "DNS registrar SPF/DKIM/DMARC records"
      via: "Resend domain-add wizard → admin publishes records → `POST /domains/$ID/verify`"
      pattern: "v=spf1 include:amazonses.com"
    - from: "Resend → noreply@app.leanshot.app"
      to: "user's inbox (karsten.haldan@gmail.com per RESEARCH §6)"
      via: "Resend Emails API `POST /emails`"
      pattern: "from.*noreply@app.leanshot.app"
    - from: "PROJECT.md Vendor accounts"
      to: "Vercel env + Supabase Function secrets"
      via: "credential capture per D-06 naming convention"
      pattern: "STRIPE_|APPLE_|PLAY_|RESEND_"
---

<objective>
Wave 2 closes Phase 12 by completing the human-required vendor provisioning work and the DNS-propagation-dependent Resend domain verification. Per CONTEXT.md D-05 (ship code-only Phase 12; credentials trickle as later-phase entry conditions for AdMob/AdSense; required Phase 12 gates: Resend domain + real email + Apple Dev + Play Console + Stripe Connect). Per CONTEXT.md D-06 (naming convention: STRIPE_*, APPLE_*, PLAY_*, RESEND_*; capture in Vercel env AND Supabase Function secrets as appropriate). Per CONTEXT.md D-16/D-17 (subdomain `app.leanshot.app`, from `LeanShot <noreply@app.leanshot.app>`, reply-to `support@leanshot.app`, DMARC `p=quarantine` initially).

Purpose: Phase 12 SC-4 part 2 (real email sent + domain verified) + SC-5 (Apple Dev + Play Console + Stripe Connect Express provisioned + credentials in Vercel/Supabase) + cross-cutting concerns #6 (Resend Phase-12 prereq) and #7 (Capacitor process model — surfaces in Phase 16/18/20 CONTEXT.md, but vendor accounts are prereq).
Output: Resend domain verified (proof JSON committed), real email sent (manual proof), 3 vendor accounts provisioned, credentials in env stores, PROJECT.md vendor-accounts section updated, 12-CONTEXT.md addendum.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/PROJECT.md
@leanshot/.planning/ROADMAP.md
@leanshot/.planning/REQUIREMENTS.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md

<interfaces>
<!-- Resend API endpoints (RESEARCH §6 + memory reference_resend_phase9_wiring.md): -->
<!--   GET  /domains                    — list domains; find by name -->
<!--   POST /domains                    — body: {"name": "app.leanshot.app"} -->
<!--   GET  /domains/{id}               — get records + status -->
<!--   POST /domains/{id}/verify        — trigger DNS check -->
<!--   POST /emails                     — body includes from/to/reply_to/subject/html -->
<!-- All require: Authorization: Bearer $RESEND_API_KEY -->
<!-- RESEND_API_KEY + RESEND_FROM are already Supabase Function secrets per memory `reference_resend_phase9_wiring.md`. -->

<!-- DNS records (RESEARCH §6 — verify exact values against Resend dashboard after `POST /domains`): -->
<!-- SPF: TXT @ app.leanshot.app value="v=spf1 include:amazonses.com ~all" -->
<!-- DKIM: CNAME resend._domainkey.app.leanshot.app → Resend-provided target -->
<!-- DMARC: TXT _dmarc.app.leanshot.app value="v=DMARC1; p=quarantine; rua=mailto:dmarc-reports@leanshot.app; ruf=mailto:dmarc-reports@leanshot.app; fo=1" -->

<!-- Vendor credential names (D-06): -->
<!-- Apple: APPLE_TEAM_ID, APPLE_BUNDLE_ID -->
<!-- Play: PLAY_PACKAGE_NAME, PLAY_SERVICE_ACCOUNT_JSON -->
<!-- Stripe: STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_CONNECT_CLIENT_ID, STRIPE_WEBHOOK_SECRET (Phase 14) -->
<!-- Resend: RESEND_API_KEY (exists), RESEND_FROM (exists) -->
<!--  -->
<!-- Storage location per D-06: -->
<!--   - Build-time / Edge Function: Vercel env (`vercel env add NAME production`) -->
<!--   - Runtime in Supabase Deno Edge Functions: Supabase Function secrets (`supabase secrets set NAME=value`) -->
<!--  -->
<!-- AdMob + AdSense are explicitly NOT Phase 12 gates per D-05 — they're Phase 20 entry conditions. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Resend domain verification for app.leanshot.app + capture proof + send real email</name>
  <files>leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json</files>
  <read_first>
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §6 "Resend Domain Verification" — DNS records (lines 603–629), curl pattern (lines 633–658), proof capture (lines 662–670), real email test (lines 672–686)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md D-16 (subdomain `app.leanshot.app`), D-17 (sender + DMARC policy)
    - Memory `reference_resend_phase9_wiring.md` — RESEND_API_KEY + RESEND_FROM are already Supabase Function secrets from Phase 9 wiring; current FROM is `LeanShot <noreply@app.leanshot.app>` but the domain was NEVER verified (sandbox fallback was used in v1.1)
    - Memory `feedback_cli_over_paste_back.md` — fetch via CLI rather than asking user to paste; Resend API + supabase CLI + dig are CLI-accessible
  </read_first>
  <what-built>
    - DNS records published at the user's registrar for `app.leanshot.app`: SPF (TXT), DKIM (CNAME provided by Resend), DMARC (TXT with p=quarantine).
    - Resend domain `app.leanshot.app` registered + verified (status field returns `verified` from `GET /domains/{id}`).
    - Real lifecycle email sent from `LeanShot <noreply@app.leanshot.app>` to `karsten.haldan@gmail.com`, received in inbox (not spam).
    - Proof JSON at `leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json` captured from `GET /domains/{id}`.
  </what-built>
  <action>
    AGENT-PERFORMABLE STEPS:

    1. Fetch RESEND_API_KEY from Supabase Function secrets (CLI-over-paste-back per memory `feedback_cli_over_paste_back.md`):
       ```
       supabase secrets list --project-ref ytnsipxxmzgaebkqmokp 2>&1 | grep -E "^RESEND_API_KEY"
       ```
       If the value is not shell-accessible (Supabase CLI masks secrets), instruct the user to paste the value into `$RESEND_API_KEY` ONCE for the duration of this task. Do NOT log the key anywhere.

    2. Check whether `app.leanshot.app` is already added to Resend (it may have been added in v1.1 Phase 9 work but never verified):
       ```
       curl -s -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/domains
       ```
       Capture the response; if `app.leanshot.app` is in the `data[]` array, capture its `id`. If NOT, add it:
       ```
       curl -s -X POST https://api.resend.com/domains -H "Authorization: Bearer $RESEND_API_KEY" -H "Content-Type: application/json" -d '{"name": "app.leanshot.app", "region": "us-east-1"}'
       ```
       Resend's response includes the DNS records (DKIM CNAME selectors, SPF value, DMARC value) the user must publish at the registrar.

    3. Extract the exact DNS records from `GET /domains/{id}` and present them to the user as a shell-friendly checklist (Resend may use selectors like `resend._domainkey` OR `r._domainkey` — capture the EXACT names from the response):
       ```
       curl -s -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/domains/$DOMAIN_ID | python3 -m json.tool
       ```

    HUMAN-PERFORMABLE STEPS (paused checkpoint):

    4. **HUMAN ACTION:** Publish the three DNS records at the registrar for `leanshot.app`:
       - SPF (TXT, name `app.leanshot.app`, value as returned by Resend — typically `v=spf1 include:amazonses.com ~all`)
       - DKIM (CNAME records as returned by Resend — typically `resend._domainkey.app.leanshot.app` → Resend-provided target; there may be multiple DKIM selectors)
       - DMARC (TXT, name `_dmarc.app.leanshot.app`, value `v=DMARC1; p=quarantine; rua=mailto:dmarc-reports@leanshot.app; ruf=mailto:dmarc-reports@leanshot.app; fo=1` per D-17)
       Note: `dmarc-reports@leanshot.app` needs to be a deliverable address — either set up forwarding at the registrar OR change `rua`/`ruf` to a real existing address (e.g. user's gmail). Document the choice.

    5. **HUMAN ACTION + WAIT:** DNS propagation typically takes 5–60 minutes; can take up to 24h depending on registrar TTLs. Verify propagation via:
       ```
       dig TXT app.leanshot.app +short
       dig CNAME resend._domainkey.app.leanshot.app +short  # or whichever selector Resend uses
       dig TXT _dmarc.app.leanshot.app +short
       ```
       Each should return the expected value. If any returns empty, wait and re-query.

    AGENT-PERFORMABLE STEPS (resume):

    6. Trigger verification via Resend API:
       ```
       curl -s -X POST -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/domains/$DOMAIN_ID/verify
       ```

    7. Poll status until `verified`:
       ```
       curl -s -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/domains/$DOMAIN_ID | python3 -c "import json,sys; d=json.load(sys.stdin); print('status:', d.get('status')); [print(r.get('record'), '→', r.get('status')) for r in d.get('records', [])]"
       ```
       Repeat until top-level `status == "verified"` AND every record-level `status == "verified"`. If after 4 hours any record is still `pending`, surface the registrar-name + record value to the user for re-check (registrar may have rejected a wildcard / trailing-dot / TTL issue).

    8. Capture proof — write the verified-state JSON to `leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json`:
       ```
       curl -s -H "Authorization: Bearer $RESEND_API_KEY" https://api.resend.com/domains/$DOMAIN_ID | python3 -m json.tool > leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json
       ```

    9. Send the real lifecycle email from `LeanShot <noreply@app.leanshot.app>` to `karsten.haldan@gmail.com` (per RESEARCH §6 lines 672–686):
       ```
       curl -s https://api.resend.com/emails -H "Authorization: Bearer $RESEND_API_KEY" -H "Content-Type: application/json" -d '{"from": "LeanShot <noreply@app.leanshot.app>", "to": ["karsten.haldan@gmail.com"], "reply_to": "support@leanshot.app", "subject": "Phase 12 SC-4 — domain verification proof (v1.2 milestone)", "html": "<p>This email proves <strong>app.leanshot.app</strong> SPF/DKIM/DMARC is verified end-to-end via Resend. Phase 12 SC-4 part 2.</p><p>Subdomain pattern locked per Phase 12 CONTEXT.md D-16. DMARC policy is <code>p=quarantine</code> initially per D-17; tightened to <code>p=reject</code> at Phase 22 close.</p>"}'
       ```
       Capture the response — it should include a message `id` (UUID). If the API returns 4xx, surface to the user.

    10. **HUMAN VERIFICATION:** User confirms the email landed in inbox (not spam) from `noreply@app.leanshot.app`. Capture a screenshot OR the email message-ID and store under `leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-real-email-proof.txt` (just the message-ID + arrival timestamp; no screenshot binary needs to live in the planning tree).

    11. Update Supabase Function secrets to confirm `RESEND_FROM` is set to `LeanShot <noreply@app.leanshot.app>` (NOT sandbox):
       ```
       supabase secrets set --project-ref ytnsipxxmzgaebkqmokp RESEND_FROM="LeanShot <noreply@app.leanshot.app>"
       ```
       If it was already this value, the command is a no-op.

    DO NOT commit `resend-domain-proof.json` until top-level `status == verified` in the JSON content.
    DO NOT commit any file containing a Resend API key (the proof JSON does not contain it).
    DO NOT use the sandbox `onboarding@resend.dev` from address — per CONTEXT.md D-16 the sender must be `noreply@app.leanshot.app`.
  </action>
  <how-to-verify>
    1. `cat leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json | python3 -c "import json,sys; d=json.load(sys.stdin); assert d.get('status') == 'verified', f'expected status=verified, got {d.get(\"status\")}'; print('OK:', d.get('name'), 'is', d.get('status'))"` exits 0.
    2. User confirms inbox receipt of the real email (manual; checkpoint resume-signal).
    3. `dig TXT app.leanshot.app +short | grep -c 'v=spf1'` returns at least 1.
    4. `dig TXT _dmarc.app.leanshot.app +short | grep -c 'p=quarantine'` returns at least 1.
    5. `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep -E "RESEND_(API_KEY|FROM)" | wc -l` returns 2.
  </how-to-verify>
  <verify>
    <automated>test -f leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json && python3 -c "import json,sys; d=json.load(open('leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json')); assert d.get('status') == 'verified', f'expected verified, got {d.get(\"status\")}'; print('OK', d.get('name'))" && dig TXT app.leanshot.app +short | grep -c 'v=spf1' && dig TXT _dmarc.app.leanshot.app +short | grep -c 'p=quarantine'</automated>
  </verify>
  <resume-signal>Type "approved — email received from noreply@app.leanshot.app" once inbox arrival is confirmed; or describe the failure and pause.</resume-signal>
  <acceptance_criteria>
    - `leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json` exists and contains a top-level `"status": "verified"` (verified via the python one-liner above).
    - `dig TXT app.leanshot.app +short` returns a non-empty result containing `v=spf1`.
    - `dig TXT _dmarc.app.leanshot.app +short` returns a non-empty result containing `p=quarantine`.
    - User confirms the test email arrived in inbox from `noreply@app.leanshot.app` (NOT spam, NOT sandbox).
    - `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp 2>/dev/null | grep -c '^RESEND_FROM'` returns ≥ 1.
    - `leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-real-email-proof.txt` exists with a Resend message-ID + timestamp.
  </acceptance_criteria>
  <done>
    Resend domain verified end-to-end with cryptographic + on-the-wire proof; real email delivered; secrets confirmed.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Apple Developer Program + Google Play Console + Stripe Connect Express provisioning + credential capture</name>
  <files>none yet — credentials land in Vercel env + Supabase Function secrets; PROJECT.md updates in Task 3</files>
  <read_first>
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §7 "Vendor Account Provisioning Checklist" (lines 690–752) — per-vendor ETA, action, credential names, storage location
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md D-05 (required Phase 12 gates: Apple Dev + Play + Stripe Connect; AdMob + AdSense are Phase 20 entry conditions, NOT Phase 12 gates), D-06 (capture pattern: Vercel env AND Supabase Function secrets per credential type)
    - Memory `feedback_cli_over_paste_back.md` — Vercel CLI + supabase CLI + gcloud are CLI-accessible; only Apple Developer Portal lacks an automation-friendly API (per memory `feedback_mcp_auth_walls_block_full_automation.md`)
  </read_first>
  <what-built>
    - Apple Developer Program enrollment active (or pending Apple review, with capture date documented).
    - Google Play Console registered; service-account JSON for Play Developer API captured.
    - Stripe Connect Express platform active (or in approval queue with capture date).
    - Credentials captured per D-06 naming convention in:
      - Vercel env: `APPLE_TEAM_ID`, `APPLE_BUNDLE_ID`, `PLAY_PACKAGE_NAME`, `STRIPE_SECRET_KEY`, `STRIPE_PUBLISHABLE_KEY`, `STRIPE_CONNECT_CLIENT_ID`
      - Supabase Function secrets: `PLAY_SERVICE_ACCOUNT_JSON`, `STRIPE_SECRET_KEY` (also here for Edge Function consumption)
  </what-built>
  <action>
    APPLE DEVELOPER PROGRAM (per RESEARCH §7 lines 692–698):

    1. **HUMAN ACTION:** User checks whether enrollment is already active at https://developer.apple.com/account. If yes → capture `APPLE_TEAM_ID` from Membership tab. If no → enroll ($99/yr; identity verification may take 24–48h).

    2. **HUMAN ACTION + AGENT CONFIRMATION:** User reserves bundle ID `app.leanshot.ios` (or chosen alternative) at App Store Connect. User pastes `APPLE_TEAM_ID` value (10-char alphanumeric) and `APPLE_BUNDLE_ID` string to the agent.

    3. AGENT runs (per memory `feedback_cli_over_paste_back.md`, use Vercel CLI):
       ```
       cd leanshot
       echo -n "$APPLE_TEAM_ID_VALUE" | vercel env add APPLE_TEAM_ID production
       echo -n "$APPLE_BUNDLE_ID_VALUE" | vercel env add APPLE_BUNDLE_ID production
       ```
       (or use `vercel env add NAME production` interactively if echo-pipe is awkward). Verify: `vercel env ls | grep -E '^APPLE_(TEAM_ID|BUNDLE_ID)'` returns 2 lines.

    GOOGLE PLAY CONSOLE (per RESEARCH §7 lines 700–706):

    4. **HUMAN ACTION:** User registers Play Console at https://play.google.com/console ($25 one-time). Reserves `PLAY_PACKAGE_NAME` (suggested: `app.leanshot.android`).

    5. **HUMAN ACTION:** User creates an OAuth service account at Google Cloud Console → IAM → Service Accounts → Create Service Account → grant `Service Account User` role for Play Developer API → Create Key → JSON. Downloads the JSON to a secure local path.

    6. AGENT captures:
       ```
       echo -n "$PLAY_PACKAGE_NAME_VALUE" | vercel env add PLAY_PACKAGE_NAME production
       supabase secrets set --project-ref ytnsipxxmzgaebkqmokp PLAY_SERVICE_ACCOUNT_JSON="$(cat /path/to/service-account.json)"
       ```
       The JSON value is a multi-line string; using `$(cat ...)` preserves it as a single shell-arg. Confirm via `supabase secrets list | grep -c '^PLAY_SERVICE_ACCOUNT_JSON'` returning 1.

    7. **CRITICAL SECURITY:** The service-account JSON file MUST NOT enter the git repo. After capturing to Supabase secret, securely delete the local copy: `rm -P /path/to/service-account.json`. The agent must NOT cat the JSON to stdout (logs would capture it).

    STRIPE CONNECT EXPRESS (per RESEARCH §7 lines 708–718):

    8. **HUMAN ACTION:** User visits https://dashboard.stripe.com/connect → Get started → Select Express. Stripe approval typically 1–2 business days; account creation is instant. Captures:
       - `STRIPE_SECRET_KEY` — Developers → API keys → "Test mode" toggle ON for Phase 12 (live key swapped in at Phase 14 billing go-live). Value starts `sk_test_...`.
       - `STRIPE_PUBLISHABLE_KEY` — same page, value starts `pk_test_...`.
       - `STRIPE_CONNECT_CLIENT_ID` — Connect → Settings → Client ID (value starts `ca_...`).

    9. AGENT captures all three to Vercel env AND `STRIPE_SECRET_KEY` also to Supabase Function secrets (per D-06; Edge Functions calling Stripe API need it at runtime):
       ```
       echo -n "$STRIPE_SECRET_KEY_VALUE" | vercel env add STRIPE_SECRET_KEY production
       echo -n "$STRIPE_PUBLISHABLE_KEY_VALUE" | vercel env add STRIPE_PUBLISHABLE_KEY production
       echo -n "$STRIPE_CONNECT_CLIENT_ID_VALUE" | vercel env add STRIPE_CONNECT_CLIENT_ID production
       supabase secrets set --project-ref ytnsipxxmzgaebkqmokp STRIPE_SECRET_KEY="$STRIPE_SECRET_KEY_VALUE"
       ```
       Confirm via `vercel env ls | grep -cE '^STRIPE_(SECRET_KEY|PUBLISHABLE_KEY|CONNECT_CLIENT_ID)'` returning 3 and `supabase secrets list | grep -c '^STRIPE_SECRET_KEY'` returning 1.

    STRIPE LIVE-KEY HYGIENE (T-PAY-01 mitigation): Use TEST keys (`sk_test_...`, `pk_test_...`) for Phase 12. Phase 14 plan-checker contract verifies the live-key swap is in Plan 14-XX commit and not earlier. The prefix-naming convention is the structural signal.

    DO NOT capture AdMob or AdSense credentials in Phase 12 (per D-05 they are Phase 20 entry conditions). DO NOT set Stripe LIVE keys (`sk_live_...`) in Phase 12 — TEST keys only.
    DO NOT commit the Play service-account JSON file to git under any circumstance.
    DO NOT echo credential values into the Claude transcript — use `read -s` or pipe-from-file patterns where possible.

    If Apple Developer Program enrollment is in flight (Apple review pending) at the time of Wave 2 close: capture the enrollment-receipt screenshot path + estimated completion date in Task 3's PROJECT.md update; Phase 13 (Design System) can start in parallel (RESEARCH §"Open Questions" #3). D-05 says Phase 12 closes when provisioned; if approval is still pending, Phase 12 stays in "closing" status until APPLE_TEAM_ID lands. Phase 13 work is independent.
  </action>
  <how-to-verify>
    1. `vercel env ls 2>/dev/null | grep -cE "^(APPLE_TEAM_ID|APPLE_BUNDLE_ID|PLAY_PACKAGE_NAME|STRIPE_SECRET_KEY|STRIPE_PUBLISHABLE_KEY|STRIPE_CONNECT_CLIENT_ID) "` returns 6 (when all three vendors approved).
    2. `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp 2>/dev/null | grep -cE "^(PLAY_SERVICE_ACCOUNT_JSON|STRIPE_SECRET_KEY)"` returns 2.
    3. Stripe key prefix is `sk_test_` not `sk_live_` (re-fetch the value from Vercel env via `vercel env pull --environment production /tmp/env-check && grep '^STRIPE_SECRET_KEY=sk_test_' /tmp/env-check && rm /tmp/env-check`).
    4. No service-account JSON exists in the repo: `find . -name 'service-account*.json' -not -path './node_modules/*' -not -path './.planning/research/*'` returns nothing.
  </how-to-verify>
  <verify>
    <automated>vercel env ls 2>/dev/null | grep -cE "^(APPLE_TEAM_ID|APPLE_BUNDLE_ID|PLAY_PACKAGE_NAME|STRIPE_SECRET_KEY|STRIPE_PUBLISHABLE_KEY|STRIPE_CONNECT_CLIENT_ID) " | awk '{ if ($1 >= 6) exit 0; else { print "expected ≥6 vendor env vars, got " $1; exit 1 } }' && find /Users/karstenhaldan/minisite -name '*service-account*.json' -not -path '*/node_modules/*' 2>/dev/null | wc -l | awk '{ if ($1 == "0") exit 0; else { print "service-account JSON leaked: " $1 " file(s)"; exit 1 } }'</automated>
  </verify>
  <resume-signal>Per-vendor signal — type ONE of: `apple-done` / `play-done` / `stripe-done` as each vendor approves; or `apple-pending <ETA>` / `play-pending <ETA>` / `stripe-pending <ETA>` to record a still-in-flight state. Final `phase-12-credentials-done` after all three resolve to either `done` or `pending` with VALIDATION.md updated to match. Per warning 7 — this checkpoint may legitimately need 3 separate resume rounds; each vendor's signal flips its own row in VALIDATION.md and PROJECT.md.</resume-signal>
  <acceptance_criteria>
    - `vercel env ls | grep -c '^APPLE_TEAM_ID '` returns ≥ 1.
    - `vercel env ls | grep -c '^APPLE_BUNDLE_ID '` returns ≥ 1.
    - `vercel env ls | grep -c '^PLAY_PACKAGE_NAME '` returns ≥ 1.
    - `vercel env ls | grep -c '^STRIPE_SECRET_KEY '` returns ≥ 1; value starts `sk_test_` (TEST-mode in Phase 12 per T-PAY-01).
    - `vercel env ls | grep -c '^STRIPE_PUBLISHABLE_KEY '` returns ≥ 1; value starts `pk_test_`.
    - `vercel env ls | grep -c '^STRIPE_CONNECT_CLIENT_ID '` returns ≥ 1; value starts `ca_`.
    - `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep -c '^PLAY_SERVICE_ACCOUNT_JSON'` returns ≥ 1.
    - `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep -c '^STRIPE_SECRET_KEY'` returns ≥ 1.
    - `find /Users/karstenhaldan/minisite -name '*service-account*.json' -not -path '*/node_modules/*'` returns 0 results (no leak into repo).
    - If Apple enrollment is still in review at checkpoint time: the user explicitly confirms "Apple pending; capture deferred to <date>" and Phase 12 close note in Task 3 records this.
  </acceptance_criteria>
  <done>
    All three required vendor accounts provisioned (or explicitly marked as pending with timeline); credentials in correct stores per D-06; Stripe keys are TEST-mode (T-PAY-01); no JSON leaked into repo.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update PROJECT.md Vendor accounts section + 12-CONTEXT.md addendum, commit Plan 12-05 close</name>
  <files>leanshot/.planning/PROJECT.md, leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md, leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-05-PLAN.md, leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md</files>
  <read_first>
    - leanshot/.planning/PROJECT.md (locate or absent `## Vendor accounts` section)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §7 lines 742–752 "Credential table"
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md D-06 (capture pattern)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md rows 12-05-01..06
  </read_first>
  <behavior>
    - PROJECT.md contains a `## Vendor accounts` section listing 6 vendors (Apple, Google Play, Stripe Connect, Resend, AdMob, AdSense) with credential names, store location (Vercel env / Supabase Function secrets), capture status (provisioned / pending / Phase 20 gate), and capture date.
    - 12-CONTEXT.md gains a `_phase_12_close_` addendum block referencing resend-domain-proof.json + listing Wave 2 outcomes.
    - VALIDATION rows 12-05-01..06 marked `✅ green` (or explicit `⚠️ pending` for vendors still in review with documented reason).
    - This PLAN's `nyquist_compliant: true`.
  </behavior>
  <action>
    Step 1 — Edit `leanshot/.planning/PROJECT.md`. Locate or create a `## Vendor accounts` section near the top (after the project overview, before any milestone-specific sections). Add a table with these 6 rows (use exact column shape):

    | Vendor | Credential names | Stored in | Status | Captured (date) | Notes |
    |--------|------------------|-----------|--------|-----------------|-------|
    | Apple Developer Program | `APPLE_TEAM_ID`, `APPLE_BUNDLE_ID` | Vercel env (production) | provisioned / pending-review | YYYY-MM-DD | $99/yr; bundle `app.leanshot.ios` reserved. Required for Phase 16 Capacitor iOS shell + TestFlight. |
    | Google Play Console | `PLAY_PACKAGE_NAME`, `PLAY_SERVICE_ACCOUNT_JSON` | Vercel env + Supabase Function secrets (JSON) | provisioned | YYYY-MM-DD | $25 one-time; package `app.leanshot.android`. Service-account JSON capped via `supabase secrets set`. Required for Phase 16 Android. |
    | Stripe Connect Express | `STRIPE_SECRET_KEY` (sk_test_*), `STRIPE_PUBLISHABLE_KEY` (pk_test_*), `STRIPE_CONNECT_CLIENT_ID` (ca_*) | Vercel env + Supabase Function secrets (secret key) | provisioned / pending-approval | YYYY-MM-DD | TEST-mode keys at Phase 12; live keys swap at Phase 14 (T-PAY-01 gate). Connect platform approval 1–2 biz days. |
    | Resend | `RESEND_API_KEY`, `RESEND_FROM=LeanShot <noreply@app.leanshot.app>` | Supabase Function secrets | verified (domain SPF/DKIM/DMARC) | YYYY-MM-DD | Domain `app.leanshot.app` per D-16; DMARC `p=quarantine` per D-17; proof at `.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json`. DMARC tightening to `p=reject` is Phase 22 entry condition. |
    | AdMob | `ADMOB_APP_ID_IOS`, `ADMOB_APP_ID_ANDROID`, `ADMOB_PUBLISHER_ID` | Vercel env (when ready) | **Phase 20 gate** | — | Account created at https://admob.google.com but publisher approval typically 1–2 weeks after stores list the app. NOT a Phase 12 gate per D-05. |
    | AdSense | `ADSENSE_PUBLISHER_ID` (`ca-pub-...`) | Vercel env (when ready) | **Phase 20 gate** | — | Apply at https://adsense.google.com but approval often needs live deployed app with content (2–4 weeks). NOT a Phase 12 gate per D-05. |

    Replace `YYYY-MM-DD` with actual capture dates (today's date `2026-05-13` for vendors captured today). For vendors in pending state, set Status to `pending-review` and Captured to the enrollment-submit date.

    Step 2 — Append a `## Phase 12 close addendum (Wave 2 outcomes)` section at the bottom of `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md` BEFORE the existing `---` footer/`*Phase: 12-bootstrap-bundle-foundations*` line. Content:

    ```
    ## Phase 12 close addendum (Wave 2 outcomes)

    Closed: <YYYY-MM-DD>

    - Resend `app.leanshot.app` verified end-to-end. Proof: [`resend-domain-proof.json`](./resend-domain-proof.json). Real email message-ID: <from resend-real-email-proof.txt>. SPF + DKIM + DMARC (`p=quarantine` per D-17) all green.
    - Apple Developer Program: <provisioned | pending-review>. `APPLE_TEAM_ID` + `APPLE_BUNDLE_ID` captured to Vercel env <or pending>.
    - Google Play Console: provisioned. `PLAY_PACKAGE_NAME` + `PLAY_SERVICE_ACCOUNT_JSON` captured per D-06 split.
    - Stripe Connect Express: <provisioned | pending-approval>. TEST-mode keys captured (live-key swap is Phase 14 entry condition per T-PAY-01).
    - AdMob + AdSense: deliberately NOT captured (Phase 20 gates per D-05).

    Wave 1 outcomes (12-01..04): per plan SUMMARYs.

    Phase 12 fully closed when this addendum is committed AND all Wave 1 plans show `✅ green` in `12-VALIDATION.md`.
    ```

    Step 3 — Update VALIDATION.md rows 12-05-01..06:
    - 12-05-01 (Resend domain verified): `✅ green` if Task 1 produced status=verified
    - 12-05-02 (real email delivered): `✅ green` if Task 1 step 10 inbox confirmation captured
    - 12-05-03 (Apple Dev): `✅ green` if provisioned, OR `⚠️ pending` with a note if Apple's review is still in flight
    - 12-05-04 (Play Console): `✅ green`
    - 12-05-05 (Stripe Connect Express): `✅ green` if approved, OR `⚠️ pending` with note
    - 12-05-06 (PROJECT.md credentials checklist): `✅ green`

    Step 4 — Flip this PLAN's `nyquist_compliant: false` → `true`. Also update `wave_0_complete: false` → `wave_0_complete: true` in VALIDATION.md frontmatter (Wave 0 is Wave 1 in our naming — all four Wave-1 plans + Wave-2 plan now closed).

    Step 5 — Commit using pathspec form (note: this plan can sweep no sibling because Wave 1 is fully committed; still use pathspec for consistency):
       ```
       git add -- leanshot/.planning/PROJECT.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-real-email-proof.txt leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-05-PLAN.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md
       git commit -- leanshot/.planning/PROJECT.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-real-email-proof.txt leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-05-PLAN.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md -m "feat(12-05): Resend domain verified + Apple/Play/Stripe Connect provisioned (D-05/D-06/D-16/D-17)"
       ```
  </action>
  <verify>
    <automated>grep -c '^## Vendor accounts' leanshot/.planning/PROJECT.md && grep -c 'app\.leanshot\.app' leanshot/.planning/PROJECT.md && grep -c 'Phase 12 close addendum' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md && grep -c 'nyquist_compliant: true' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-05-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Vendor accounts' leanshot/.planning/PROJECT.md` returns `1`.
    - The vendor table has ≥ 6 rows (one each for Apple, Play, Stripe Connect, Resend, AdMob, AdSense): `grep -cE '^\| (Apple Developer|Google Play|Stripe Connect|Resend|AdMob|AdSense)' leanshot/.planning/PROJECT.md` returns `6`.
    - AdMob + AdSense rows explicitly say **Phase 20 gate**: `grep -c 'Phase 20 gate' leanshot/.planning/PROJECT.md` returns ≥ 2.
    - `grep -c 'Phase 12 close addendum' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md` returns `1`.
    - `grep -c 'resend-domain-proof.json' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md` returns ≥ 1.
    - `grep -c 'nyquist_compliant: true' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-05-PLAN.md` returns `1`.
    - VALIDATION rows 12-05-01..06 each show either `✅ green` or `⚠️ pending` (with an explicit note) — not `⬜ pending`.
    - The commit lists exactly the 6 files in this task's commit.
  </acceptance_criteria>
  <done>
    PROJECT.md vendor-accounts table updated with all 6 vendors. 12-CONTEXT.md addendum closes Phase 12. VALIDATION fully resolved (green or explicit pending-with-note). Plan 12-05 committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DNS registrar → Resend SMTP → user's inbox | SPF + DKIM + DMARC together establish cryptographic + envelope authority for `app.leanshot.app`. Drift in any record causes silent send failures or spam-folder routing — verified by the real-email test in Task 1. |
| Local dev machine → Vercel env / Supabase Function secrets | Credentials never enter git. Vercel CLI + supabase CLI write directly to the respective secret stores. Local copies of Play service-account JSON are securely deleted post-capture. |
| Test-mode Stripe keys (`sk_test_`, `pk_test_`) → Production environment | Phase 12 deliberately uses TEST keys. Phase 14 entry-condition contract verifies the live-key swap timing (T-PAY-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-EMAIL-01 | Spoofing | Inbound emails to user pretending to be from `noreply@app.leanshot.app` | mitigate | SPF (`v=spf1 include:amazonses.com ~all`) + DKIM (Resend-signed) + DMARC (`p=quarantine`) all live per Task 1. DMARC tightens to `p=reject` at Phase 22 (D-17). |
| T-EMAIL-02 | Spoofing / Deliverability | Outbound emails landing in spam due to sender-rep failure | mitigate | Subdomain pattern (`app.leanshot.app`) isolates sending reputation from the apex `leanshot.app` (D-16). Real-email inbox-arrival proof in Task 1 step 10. |
| T-PAY-01 | Information Disclosure / Privilege Escalation | Stripe LIVE-mode secret-key shipped in Phase 12 environment by accident | mitigate | Naming convention prefix (`sk_test_` vs `sk_live_`) is the structural signal. Phase 12 captures TEST keys only. Phase 14 plan-checker contract verifies the live-key-swap commit appears in Plan 14-XX, not earlier. Acceptance criteria in Task 2 enforce `sk_test_` prefix. |
| T-CRED-01 | Information Disclosure | Play service-account JSON leaked into git repo | mitigate | Task 2 step 7 mandates secure-delete of the local JSON file after capture. Acceptance criteria validate via `find` that no `*service-account*.json` exists outside `node_modules`. Process control: reviewer + plan-checker MUST flag any `.json` file in `git status` near this plan. |
| T-CRED-02 | Information Disclosure | Resend API key visible in agent transcript | accept (process control) | Memory `feedback_cli_over_paste_back.md` mitigates by fetching value via `supabase secrets list` rather than asking user to paste. Even so, if `RESEND_API_KEY` enters the shell environment in Task 1 step 1, the value is in process memory during that step. Acceptable since the key is already in production storage; transcript captures are bounded by session scope. |
| T-AVAIL-01 | Denial of Service / Phase blocker | Apple Developer Program enrollment review takes longer than expected | accept (workflow) | D-05 allows Phase 13 (Design System) to start in parallel while Apple review pends. Phase 12 stays in "closing" state with explicit `⚠️ pending` note in VALIDATION + PROJECT.md until APPLE_TEAM_ID lands. No Phase 13–15 code depends on Apple Dev approval. |
| T-AVAIL-02 | Denial of Service | DNS propagation delay blocks Phase 12 close (Pitfall 6 in RESEARCH §9) | mitigate | Task 1 explicitly waits via polling loop. Wave 2 can run in parallel with Wave 1 (DNS records published early) but the verification step waits for propagation. If propagation exceeds 4 hours: surface the registrar issue to user (TTL, wildcard, trailing-dot) per RESEARCH §9 Pitfall 6. |
</threat_model>

<verification>
- `cat leanshot/.planning/phases/12-bootstrap-bundle-foundations/resend-domain-proof.json | python3 -c "import json,sys; assert json.load(sys.stdin).get('status') == 'verified'"` exits 0.
- User-confirmed real email arrival from `noreply@app.leanshot.app`.
- `vercel env ls | grep -cE '^(APPLE_TEAM_ID|APPLE_BUNDLE_ID|PLAY_PACKAGE_NAME|STRIPE_SECRET_KEY|STRIPE_PUBLISHABLE_KEY|STRIPE_CONNECT_CLIENT_ID) '` returns 6 (or all-but-pending count documented in VALIDATION).
- `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep -cE '^(PLAY_SERVICE_ACCOUNT_JSON|STRIPE_SECRET_KEY|RESEND_API_KEY|RESEND_FROM)'` returns 4.
- `find /Users/karstenhaldan/minisite -name '*service-account*.json' -not -path '*/node_modules/*'` returns 0 results.
- PROJECT.md `## Vendor accounts` section present with 6 vendor rows.
- 12-CONTEXT.md addendum present.
</verification>

<success_criteria>
- All three task `<acceptance_criteria>` blocks pass (or have explicit documented pending-with-note status for vendors awaiting review).
- `nyquist_compliant: true`.
- VALIDATION.md rows 12-05-01..06 each show `✅ green` or `⚠️ pending` (with note); none remain `⬜ pending`.
- Phase 12 fully closes per D-05 (Resend domain verified + real email delivered + Apple Dev + Play + Stripe Connect each in provisioned-or-pending state).
- Cross-cutting concerns #6 (Resend Phase-0 prereq) + #7 (Capacitor process model surfaced via Apple+Play credential capture) discharged.
</success_criteria>

<output>
After Plan 12-05 closes, create `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-05-SUMMARY.md` per `templates/summary.md`. Then create the Phase 12 close summary `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-SUMMARY.md` aggregating Wave 1 (plans 12-01..04) + Wave 2 (this plan) outcomes; record any vendor in `⚠️ pending` state with the expected completion date and Phase 13 unblocking decision per D-05.
</output>
