---
phase: 12-bootstrap-bundle-foundations
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/e2e/clinic-ad-free.spec.ts
  - .github/workflows/ci.yml
autonomous: true
nyquist_compliant: false
requirements: [SC-3, CCC-2]
tags: [playwright, e2e, ad-free, ci-gate, security]
must_haves:
  truths:
    - "A Playwright spec at e2e/clinic-ad-free.spec.ts asserts on /clinic/*, /share/*, /admin/* routes for three layers: (a) zero <script src> tags whose URL matches a hardcoded ad-provider origins list, (b) zero <AdSlot> component mounts (selector `[data-ad-slot], [data-testid=\"ad-slot\"]`), (c) zero network requests captured to ad-provider origins during the page lifecycle."
    - "The spec passes against the current SPA (no ads exist anywhere) — verified by `npx playwright test e2e/clinic-ad-free.spec.ts` exiting 0 from leanshot/."
    - "The spec is wired into the existing parent-repo GitHub Actions workflow at .github/workflows/ci.yml as a PR-blocking gate (D-15) — either via an explicit named step in test-e2e or by confirming the existing `npm run test:e2e` step picks it up automatically (Playwright `testMatch: /.*\\.spec\\.ts$/`)."
    - "The hardcoded ad-provider origins list contains the 13 origins from RESEARCH §5 covering Google (gpt, AdSense, Ads, DoubleClick, AdMob), Meta (Audience Network, fbcdn), Amazon Ads, and major programmatic SSPs (Magnite, PubMatic, Index Exchange, Moat)."
  artifacts:
    - path: "leanshot/e2e/clinic-ad-free.spec.ts"
      provides: "Three-layer ad-free assertion on /clinic/*, /share/*, /admin/* routes"
      min_lines: 60
      exports: ["(test cases via test() calls)"]
    - path: ".github/workflows/ci.yml"
      provides: "PR-blocking gate that runs clinic-ad-free.spec.ts on every PR"
      contains: "clinic-ad-free"
  key_links:
    - from: "leanshot/e2e/clinic-ad-free.spec.ts"
      to: "13 ad-provider origins (googletagservices.com, googlesyndication.com, googleadservices.com, doubleclick.net, googletag.com, admob.googleapis.com, facebook.net, fbcdn.net, adsystem.amazon.com, moatads.com, casalemedia.com, pubmatic.com, rubiconproject.com)"
      via: "AD_PROVIDER_ORIGINS array + context.on('request') + page.evaluate(querySelectorAll('script[src]'))"
      pattern: "AD_PROVIDER_ORIGINS"
    - from: ".github/workflows/ci.yml test-e2e job"
      to: "leanshot/e2e/clinic-ad-free.spec.ts"
      via: "Playwright testMatch picks up *.spec.ts OR explicit `npx playwright test e2e/clinic-ad-free.spec.ts` step"
      pattern: "clinic-ad-free"
---

<objective>
Ship a Playwright spec at `leanshot/e2e/clinic-ad-free.spec.ts` that proves zero ads on B2B/share/admin routes via THREE assertion layers (DOM scripts, AdSlot component mounts, network requests), and wire it into the parent-repo `.github/workflows/ci.yml` as a PR-blocking gate per CONTEXT.md D-14/D-15. The spec runs against the current SPA (no ads anywhere) and trivially passes today — its real value activates when Phase 20 ships ad code, at which point the gate prevents accidental inclusion on B2B surfaces.

Purpose: Phase 12 SC-3 (Playwright e2e asserts zero ad-provider scripts/mounts/requests on `/clinic/*`, `/share/*`, `/admin/*`) and cross-cutting concern #2 ("No ads on B2B surfaces — TRIPLE-layered: AD-03 component check + AD-02 Edge Function + CSP report-only + Playwright e2e in Phase 12").
Output: New e2e spec file + CI wiring confirmed/updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/PROJECT.md
@leanshot/.planning/ROADMAP.md
@leanshot/.planning/REQUIREMENTS.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md
@leanshot/playwright.config.ts

<interfaces>
<!-- Existing Playwright shape: -->
<!-- - playwright.config.ts: testMatch: /.*\.spec\.ts$/ — picks up any *.spec.ts file in e2e/ -->
<!-- - package.json: `test:e2e` = `playwright test` (full e2e suite) -->
<!-- - 30+ existing specs in leanshot/e2e/ — same shape: `import { expect, test } from '@playwright/test';` then `test('name', async ({ page, context }) => { ... })` -->
<!-- - context.on('request', cb) — captures requests at the BrowserContext level (covers iframes, workers, service workers) -->
<!-- - page.evaluate(fn, arg) — returns whatever fn returns, runs in browser context -->
<!--  -->
<!-- Existing CI structure at /Users/karstenhaldan/minisite/.github/workflows/ci.yml: -->
<!-- - `test-e2e` job at line 79–195: builds production, runs `npm run test:e2e` (line 179) which is `playwright test` — picks up ALL specs INCLUDING this new one automatically. -->
<!-- - `share-security-drill` job at line 260+ runs ONE specific spec via `npx playwright test e2e/share-revocation-drill.spec.ts` — precedent for spec-targeted CI steps. -->
<!-- - The `needs: [lint, format-check, typecheck, test-unit, test-e2e, compliance-copy, deno-test]` line at 419 already makes the test-e2e job a PR-blocking gate. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create e2e/clinic-ad-free.spec.ts with three-layer ad-free assertion</name>
  <files>leanshot/e2e/clinic-ad-free.spec.ts</files>
  <read_first>
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §5 "Clinic-Ad-Free Playwright Spec" (lines 483–597) — concrete spec scaffold to adapt; 13-entry ad-provider origins list
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md D-14 (routes `/clinic/*`, `/share/*`, `/admin/*`; three assertion layers) and D-15 (PR-blocking gate timing)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §9 Pitfall 4 ("Playwright clinic-ad-free spec passes because routes don't exist yet") — the spec is trivially true today but must remain meaningful when Phase 14+ ships routes
    - leanshot/playwright.config.ts (testMatch pattern, baseURL, default reporter)
    - leanshot/e2e/clinic-roster-sort.spec.ts (representative existing spec for shape conventions — import style, test() vs test.describe(), waitUntil patterns)
  </read_first>
  <behavior>
    - `npx playwright test e2e/clinic-ad-free.spec.ts` from `leanshot/` exits 0 against the current SPA (no ads anywhere).
    - The spec contains exactly 3 test cases (one per protected route: `/clinic`, `/share`, `/admin`).
    - Each test case has THREE assertion layers (`expect(...).toHaveLength(0)` or `.toBe(0)`):
      Layer 1 — DOM scripts: `page.evaluate` queries `document.querySelectorAll('script[src]')`, filters by ad-provider origin, asserts empty array.
      Layer 2 — AdSlot mounts: `page.locator('[data-ad-slot], [data-testid="ad-slot"]').count()` returns 0.
      Layer 3 — Network requests: `context.on('request', ...)` accumulates URLs matching the origins list during the page lifecycle; final assertion is empty array.
    - Hardcoded `AD_PROVIDER_ORIGINS` is exactly 13 entries (per RESEARCH §5 list).
    - The spec uses `context.on('request', ...)` (not `page.route`) so it captures requests from iframes and workers.
    - Each test uses `page.goto(route, { waitUntil: 'networkidle' })` to ensure lazy-loaded scripts fire before assertions.
    - On `404` routes (Phase 12 state — none of `/clinic`, `/share`, `/admin` are real routes yet), the spec trivially passes because the SPA root has no ads.
  </behavior>
  <action>
    Create `leanshot/e2e/clinic-ad-free.spec.ts` following the RESEARCH §5 scaffold (lines 511–588). Adapt for the existing Playwright config (review playwright.config.ts for baseURL handling).

    Required structure:
    1. Standard imports: `import { expect, test } from '@playwright/test';`
    2. Module-level constants:
       - `AD_PROVIDER_ORIGINS: readonly string[]` — exactly 13 entries (per RESEARCH §5 table): `googletagservices.com`, `googlesyndication.com`, `googleadservices.com`, `doubleclick.net`, `googletag.com`, `admob.googleapis.com`, `facebook.net`, `fbcdn.net`, `adsystem.amazon.com`, `moatads.com`, `casalemedia.com`, `pubmatic.com`, `rubiconproject.com`.
       - `PROTECTED_ROUTES: readonly string[]` — exactly `['/clinic', '/share', '/admin']` (matches D-14).
    3. Helper: `function isAdProviderUrl(url: string): boolean { return AD_PROVIDER_ORIGINS.some((origin) => url.includes(origin)); }`
    4. Loop body — for each route in `PROTECTED_ROUTES`, register `test('no ad scripts, no AdSlot mounts, no ad network requests on ${route}', async ({ page, context }) => { ... })`.
    5. Inside each test:
       a. `const adRequests: string[] = [];`
       b. `context.on('request', (req) => { if (isAdProviderUrl(req.url())) adRequests.push(req.url()); });`
       c. `await page.goto(route, { waitUntil: 'networkidle' });`
       d. Layer 1 — `const adScripts = await page.evaluate((origins) => Array.from(document.querySelectorAll('script[src]')).map((s) => (s as HTMLScriptElement).src).filter((src) => origins.some((o) => src.includes(o))), AD_PROVIDER_ORIGINS as unknown as string[]);` then `expect(adScripts, \`Ad script tags found on \${route}: \${adScripts.join(', ')}\`).toHaveLength(0);`
       e. Layer 2 — `const adSlots = await page.locator('[data-ad-slot], [data-testid="ad-slot"]').count(); expect(adSlots, \`AdSlot mounts found on \${route}\`).toBe(0);`
       f. Layer 3 — `expect(adRequests, \`Ad network requests fired on \${route}: \${adRequests.join(', ')}\`).toHaveLength(0);`
    6. Top-of-file header comment block citing Phase 12 D-14/D-15, the cross-cutting concern #2 from REQUIREMENTS.md, AND a note: "Phase 12 baseline: SPA has no ads anywhere → spec trivially passes. Spec becomes meaningful when Phase 14+ ships clinic/share/admin routes AND when Phase 20 ships ad code. Maintenance contract: any new ad provider added in Phase 20+ MUST be appended to AD_PROVIDER_ORIGINS."

    Type strictness: Per CLAUDE.md project conventions, the spec is TypeScript strict. Use `readonly string[]` for the constant arrays. Cast `AD_PROVIDER_ORIGINS` to `string[]` only when passing into `page.evaluate` (it doesn't accept readonly arrays at the boundary).

    Pitfall 4 acknowledgment: Add a comment near the assertions: `// Phase 12 acknowledgment (Pitfall 4): routes don't exist yet → SPA root renders → trivially zero ads. The spec becomes a real safety net once Phase 14 ships /clinic/{slug}, Phase 15 ships /admin/*, etc. AND once Phase 20 ships ad code that would (incorrectly) try to load on these routes.`

    DO NOT use `test.fixme` or `test.skip` — the spec MUST pass today (memory `reference_vitest_skip_fixme.md` is about Vitest; for Playwright `test.fixme` works but is the wrong tool here — the gate must be live from Phase 12 onward per D-15).

    DO NOT add any `await page.waitForTimeout(...)` calls — they're flake bait. `waitUntil: 'networkidle'` is the right wait primitive.

    After creation: `cd leanshot && npx playwright test e2e/clinic-ad-free.spec.ts --reporter=line` and confirm 3 passing tests.
  </action>
  <verify>
    <automated>cd leanshot && npx playwright test e2e/clinic-ad-free.spec.ts --reporter=line 2>&1 | tee /tmp/clinic-ad-free-output.txt && grep -E '3 passed|3.*passed' /tmp/clinic-ad-free-output.txt</automated>
  </verify>
  <acceptance_criteria>
    - `test -f leanshot/e2e/clinic-ad-free.spec.ts` succeeds.
    - `grep -c "AD_PROVIDER_ORIGINS" leanshot/e2e/clinic-ad-free.spec.ts` returns at least `3` (declaration + 2 usages in evaluate + isAdProviderUrl).
    - The origins list contains exactly the 13 entries from RESEARCH §5: `for origin in googletagservices.com googlesyndication.com googleadservices.com doubleclick.net googletag.com admob.googleapis.com facebook.net fbcdn.net adsystem.amazon.com moatads.com casalemedia.com pubmatic.com rubiconproject.com; do grep -c "$origin" leanshot/e2e/clinic-ad-free.spec.ts || echo "MISSING $origin"; done` — all 13 must return ≥1.
    - `grep -c "PROTECTED_ROUTES" leanshot/e2e/clinic-ad-free.spec.ts` returns ≥ 2.
    - `grep -cE "'/clinic'|'/share'|'/admin'" leanshot/e2e/clinic-ad-free.spec.ts` returns at least 3.
    - `grep -c "context.on\(.request" leanshot/e2e/clinic-ad-free.spec.ts` returns 1 (network layer).
    - `grep -c 'data-ad-slot' leanshot/e2e/clinic-ad-free.spec.ts` returns 1 (AdSlot layer).
    - `grep -c "script\[src\]" leanshot/e2e/clinic-ad-free.spec.ts` returns 1 (DOM scripts layer).
    - `grep -c "waitUntil: 'networkidle'" leanshot/e2e/clinic-ad-free.spec.ts` returns 3 (one per protected route).
    - `cd leanshot && npx playwright test e2e/clinic-ad-free.spec.ts --reporter=line` exits 0 and reports `3 passed`.
    - Mutation proof: temporarily add `<script src="https://googletagservices.com/tag/js/gpt.js"></script>` to `leanshot/index.html`, re-run the spec; it must exit non-zero with `Ad script tags found on /clinic` in the failure output. Remove the test script before committing.
  </acceptance_criteria>
  <done>
    Spec exists, passes against current SPA, contains all 13 origins, all 3 routes, all 3 assertion layers. Mutation proof confirms a real ad insertion would trip the gate.
  </done>
</task>

<task type="auto">
  <name>Task 2: Investigate CI wiring; either confirm auto-pickup OR add an explicit named step</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - /Users/karstenhaldan/minisite/.github/workflows/ci.yml (entire file — locate the existing `test-e2e` job; the run-step at ~line 179 is `npm run test:e2e` which is `playwright test`)
    - leanshot/playwright.config.ts (testMatch pattern — `.*\.spec\.ts$` picks up any `*.spec.ts` in `e2e/`)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §10 Plan 12-03 — flagged "no workflows dir found" (the researcher checked under `leanshot/` only; the workflow lives at the parent repo `.github/workflows/`)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §"Open Questions" #2 ("CI job wiring for clinic-ad-free spec") — Plan 12-03 executor must investigate
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md D-15 (PR-blocking gate; independent of share-security-drill)
  </read_first>
  <behavior>
    - The clinic-ad-free spec is invoked by the parent-repo CI workflow on every PR.
    - PR cannot merge with a red clinic-ad-free gate (the existing `needs: [..., test-e2e, ...]` at line 419 already enforces test-e2e success for the final gate).
    - The CI step that runs the spec is identifiable by `grep clinic-ad-free .github/workflows/ci.yml` returning at least 1 match (either an explicit step name or an inline comment confirming auto-pickup).
  </behavior>
  <action>
    Investigation phase: confirm via reading `.github/workflows/ci.yml` whether the existing `test-e2e` job's `npm run test:e2e` step picks up the new spec automatically (Playwright `testMatch: /.*\.spec\.ts$/` says yes).

    Decision:

    Option A (preferred — explicit named step for grep-discoverability per VALIDATION row 12-03-02):
    Add a NEW named step inside the existing `test-e2e` job (after the existing `npm run test:e2e` step at ~line 179, before the upload-on-failure step at ~line 190). The step explicitly targets the spec to make it grep-discoverable and to emit a clearer pass/fail signal in the PR check listing:

    ```yaml
    - name: Phase 12 SC-3 — Clinic ad-free e2e gate
      working-directory: leanshot
      run: npx playwright test e2e/clinic-ad-free.spec.ts --reporter=line
    ```

    Place it AFTER the existing `Run Playwright smoke against production build` step. The spec runs against the same production build (`leanshot/dist/` is already built earlier in the job at line 124).

    Option B (lighter — comment-only confirmation): If adding a duplicate step would slow CI noticeably (running clinic-ad-free.spec.ts a second time inside `npm run test:e2e`), instead add a CI comment near the existing `npm run test:e2e` step documenting that this is the run that includes `e2e/clinic-ad-free.spec.ts` (Phase 12 SC-3 gate).

    Recommendation: Option A. Per VALIDATION row 12-03-02, the verification command is `grep -r clinic-ad-free .github/workflows/` — Option B does not satisfy that (a code comment is grep-discoverable but Option A also adds a distinct PR check entry which is more durable). The ~5s of extra CI wallclock is negligible.

    Implementation:
    1. Edit `/Users/karstenhaldan/minisite/.github/workflows/ci.yml`. Locate the `test-e2e` job (currently starts around line 79). Locate the existing step `- name: Run Playwright smoke against production build` followed by `run: npm run test:e2e`.
    2. Add the new step block IMMEDIATELY AFTER that step's body, before the `- name: Upload Playwright report on failure` step.
    3. Confirm YAML indentation matches sibling steps (typically 6 spaces for the `- name:` line if siblings use 6).
    4. Validate the YAML: `node -e "const yaml = require('js-yaml'); const fs = require('fs'); yaml.load(fs.readFileSync('/Users/karstenhaldan/minisite/.github/workflows/ci.yml', 'utf-8'));"` exits 0 (no parse error). If `js-yaml` is not available, use `python3 -c "import yaml; yaml.safe_load(open('/Users/karstenhaldan/minisite/.github/workflows/ci.yml'))"` (PyYAML is installed by default on macOS).

    DO NOT change the existing `npm run test:e2e` step. DO NOT modify the `needs:` array at line 419. DO NOT modify the share-security-drill job. DO NOT modify the roster-perf job.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c 'clinic-ad-free' .github/workflows/ci.yml | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && grep -c 'Phase 12 SC-3' .github/workflows/ci.yml | awk '{ if ($1 >= 1) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'clinic-ad-free' /Users/karstenhaldan/minisite/.github/workflows/ci.yml` returns ≥ `1`.
    - `grep -cF 'Phase 12 SC-3' /Users/karstenhaldan/minisite/.github/workflows/ci.yml` returns ≥ `1` (the new step name is grep-discoverable).
    - The YAML still parses: `python3 -c "import yaml; yaml.safe_load(open('/Users/karstenhaldan/minisite/.github/workflows/ci.yml'))"` exits 0.
    - The `needs:` array at the final-gate job is unchanged: `grep -E 'needs:.*test-e2e' /Users/karstenhaldan/minisite/.github/workflows/ci.yml | wc -l` is unchanged from main (still includes `test-e2e`).
    - The existing `Run Playwright smoke against production build` step is unmodified: `grep -c 'Run Playwright smoke against production build' /Users/karstenhaldan/minisite/.github/workflows/ci.yml` returns the same value as on main (typically 1).
  </acceptance_criteria>
  <done>
    The new step is grep-discoverable in ci.yml, YAML still parses, existing steps and gate-dependency lists are unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit Plan 12-03 with pathspec form, flip nyquist_compliant + VALIDATION rows</name>
  <files>leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-03-PLAN.md, leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md</files>
  <read_first>
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md (rows 12-03-01 and 12-03-02)
    - Memory `feedback_parallel_executor_git_isolation.md` — pathspec commit form
  </read_first>
  <behavior>
    - Commit contains exactly: `leanshot/e2e/clinic-ad-free.spec.ts`, `.github/workflows/ci.yml`, `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-03-PLAN.md`, `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md`. Four files. No sibling sweep.
    - VALIDATION rows 12-03-01 and 12-03-02 marked `✅ green`.
    - This PLAN's `nyquist_compliant: true`.
  </behavior>
  <action>
    After Tasks 1+2 pass locally:
    1. Update `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md` rows 12-03-01 and 12-03-02 from `⬜ pending` to `✅ green`.
    2. Flip this PLAN.md frontmatter `nyquist_compliant: false` → `nyquist_compliant: true`.
    3. Commit using pathspec form (per memory `feedback_parallel_executor_git_isolation.md`):
       ```
       git add -- leanshot/e2e/clinic-ad-free.spec.ts .github/workflows/ci.yml leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-03-PLAN.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md
       git commit -- leanshot/e2e/clinic-ad-free.spec.ts .github/workflows/ci.yml leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-03-PLAN.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md -m "feat(12-03): clinic-ad-free Playwright e2e gate (D-14/D-15)"
       ```
    4. Verify `git show --stat HEAD` lists exactly 4 files.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && git show --stat HEAD --pretty=format: 2>/dev/null | grep -cE "(leanshot/e2e/clinic-ad-free\.spec\.ts|\.github/workflows/ci\.yml|leanshot/\.planning/phases/12-bootstrap-bundle-foundations/12-03-PLAN\.md|leanshot/\.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION\.md)" | awk '{ if ($1 == "4") exit 0; else { print "expected 4 files, got " $1; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `git show --stat HEAD` lists exactly the 4 files listed in this plan's commit.
    - `grep -c 'nyquist_compliant: true' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-03-PLAN.md` returns `1`.
    - `grep -E '12-03-(01|02).*✅ green' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md | wc -l` returns `2`.
    - `cd leanshot && npx playwright test e2e/clinic-ad-free.spec.ts` exits 0.
  </acceptance_criteria>
  <done>
    Plan 12-03 committed atomically with pathspec; VALIDATION green; nyquist_compliant flipped.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-facing ad-eligible surfaces (free-tier patient routes) → B2B/share/admin surfaces | Phase 12 enforces the no-ads invariant on the B2B side as a Playwright e2e gate. AD-03 (Phase 20) is the component-level check; this spec is the runtime-DOM verification layer. Defense in depth: AD-02 Edge Function refuses, CSP report-only header, and this spec. |
| Future ad-network additions (Phase 20) → AD_PROVIDER_ORIGINS maintenance | Any new ad provider added in Phase 20 OR a future milestone MUST also be appended to `AD_PROVIDER_ORIGINS` in this spec, otherwise the gate has a blind spot. Process control documented in the spec's header comment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AD-02 | Information Disclosure / Compliance (B2B trust) | Ad-provider scripts loading on `/clinic/*`, `/share/*`, `/admin/*` routes | mitigate | Three-layer Playwright assertion in `e2e/clinic-ad-free.spec.ts` (Task 1): DOM scripts + AdSlot mounts + network requests. PR-blocking via CI wiring (Task 2). Defense in depth per cross-cutting concern #2: AD-03 component refuses + AD-02 Edge Function refuses + CSP report-only + this spec. |
| T-AD-06 | Tampering / Drift | A future PR adding a new ad provider but forgetting to update `AD_PROVIDER_ORIGINS` | accept (process control) | The spec header documents the maintenance contract. Plan-checker for Phase 20 MUST verify that any new ad-provider integration also appends the origin to this constant. No structural enforcement possible without a higher-order rule (e.g. an ESLint rule scanning for ad-provider FQDNs in source — out of Phase 12 scope). |
| T-AD-07 | Tampering | An ad provider served from a non-CSP-listed origin via a script proxy (CDN trampoline) | accept | The spec checks the EFFECTIVE URL of `<script src>` and network requests, not the FQDN listed in CSP. A determined attacker proxying via a non-listed origin would bypass the spec. CSP `default-src 'none'` + `script-src 'self'` (currently) blocks remote scripts at the CSP layer; this spec is belt-and-suspenders. |
</threat_model>

<verification>
- `cd leanshot && npx playwright test e2e/clinic-ad-free.spec.ts` exits 0 with 3 passing tests.
- `grep -c 'clinic-ad-free' .github/workflows/ci.yml` ≥ 1.
- YAML parses (`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0).
- All 13 origins from RESEARCH §5 present in the spec.
- All 3 routes (`/clinic`, `/share`, `/admin`) present in the spec.
</verification>

<success_criteria>
- All three task `<acceptance_criteria>` blocks pass.
- `nyquist_compliant: true` set.
- VALIDATION.md rows 12-03-01 and 12-03-02 show `✅ green`.
- Spec passes against current SPA AND fails on a synthetic ad-script insertion (mutation proof in Task 1).
</success_criteria>

<output>
After completion, create `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-03-SUMMARY.md` per `templates/summary.md`. Reference cross-cutting concern #2 (TRIPLE-layered B2B no-ads enforcement).
</output>
