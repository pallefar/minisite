---
phase: 12-bootstrap-bundle-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/scripts/assert-clinic-bundle-budget.sh
  - leanshot/scripts/test-hash-hyphen-regression.sh
autonomous: true
nyquist_compliant: false
requirements: [SC-1, CCC-3]
tags: [bundle-budget, ci-gate, foundations]
must_haves:
  truths:
    - "Five new per-chunk ceiling constants (stripe-elements, adsense-glue, page-builder-runtime, web-push, capacitor-bridge) are declared in scripts/assert-clinic-bundle-budget.sh with research-derived caps from D-07."
    - "Five new check_chunk_ceiling calls follow the existing wave-0 skip pattern — they emit a wave-0 notice rather than failing when the chunk has not yet been built."
    - "A regression test scripts/test-hash-hyphen-regression.sh exits 0 against a synthetic 'clinic-invite-BsW-HOUO' hash-with-hyphen input, proving the lines 147–186 sed loop still strips the hash to 'clinic-invite'."
    - "bash scripts/assert-clinic-bundle-budget.sh exits 0 against the current dist/ after npm run build, with five wave-0 skip lines for the new chunks."
  artifacts:
    - path: "leanshot/scripts/assert-clinic-bundle-budget.sh"
      provides: "Five new *_CEILING constants + five check_chunk_ceiling calls (D-07/D-08)"
      contains: "STRIPE_ELEMENTS_CEILING=30000"
    - path: "leanshot/scripts/test-hash-hyphen-regression.sh"
      provides: "Standalone shell regression test for the hash-hyphen sed loop"
      contains: "clinic-invite-BsW-HOUO"
  key_links:
    - from: "leanshot/scripts/assert-clinic-bundle-budget.sh"
      to: "leanshot/dist/assets/{stripe-elements,adsense-glue,page-builder-runtime,web-push,capacitor-bridge}-*.js"
      via: "check_chunk_ceiling glob match"
      pattern: "check_chunk_ceiling 'stripe-elements-\\*\\.js'"
    - from: "leanshot/scripts/test-hash-hyphen-regression.sh"
      to: "the sed loop logic at lines 147–186 of assert-clinic-bundle-budget.sh"
      via: "isolated replication of the sed strip loop"
      pattern: "sed 's/-\\[A-Za-z0-9\\]\\*\\[A-Z0-9\\]\\[A-Za-z0-9\\]\\*\\$//'"
---

<objective>
Extend the existing `leanshot/scripts/assert-clinic-bundle-budget.sh` with five new per-chunk ceiling constants and `check_chunk_ceiling` calls per CONTEXT.md D-07 (stripe-elements ≤30 kB gz, adsense-glue ≤8 kB gz, page-builder-runtime ≤25 kB gz, web-push ≤3 kB gz, capacitor-bridge ≤15 kB gz). Add a standalone regression test that exercises the hash-hyphen `sed` loop logic at lines 147–186 against a synthetic `clinic-invite-BsW-HOUO` filename to prevent reference_bundle_budget_hash_hyphen.md from regressing.

Purpose: Phase 12 SC-1 (per-chunk gz size measurement works for hyphen-hash filenames AND new ceilings are declared as PR blockers); cross-cutting concern #3 (bundle ceiling 24.5/50 kB gz, every new SDK via sync-defer.ts, per-chunk ceilings declared in Phase 12).
Output: Modified shell script with five new measured chunks (wave-0 skip until each owning phase ships the SDK) + new shell regression test wired into CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/PROJECT.md
@leanshot/.planning/ROADMAP.md
@leanshot/.planning/REQUIREMENTS.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md
@leanshot/scripts/assert-clinic-bundle-budget.sh

<interfaces>
<!-- Existing shape from scripts/assert-clinic-bundle-budget.sh -->
<!-- Function signature: -->
<!--   check_chunk_ceiling <chunk_glob> <ceiling_bytes> <label> -->
<!-- Existing ceiling constants pattern (top of script): -->
<!--   CLINIC_CEILING=22000 -->
<!--   CLINIC_SETTINGS_CEILING=18000 -->
<!--   CLINIC_INVITE_CEILING=6000 -->
<!--   IDX_PHASE9_CEILING=24500 -->
<!--   IDX_ABSOLUTE_CEILING=50000 -->
<!--   READ_ONLY_PATIENT_VIEW_CEILING=12000 -->
<!--   SHARE_CEILING=7000 -->
<!-- Existing check_chunk_ceiling calls (lines 218–227): -->
<!--   check_chunk_ceiling 'clinic-*.js' "$CLINIC_CEILING" 'clinic' -->
<!--   check_chunk_ceiling 'clinic-settings-*.js' "$CLINIC_SETTINGS_CEILING" 'clinic-settings' -->
<!--   check_chunk_ceiling 'clinic-invite-*.js' "$CLINIC_INVITE_CEILING" 'clinic-invite' -->
<!--   check_chunk_ceiling 'read-only-patient-view-*.js' "$READ_ONLY_PATIENT_VIEW_CEILING" 'read-only-patient-view' -->
<!--   check_chunk_ceiling 'share-*.js' "$SHARE_CEILING" 'share' -->
<!-- Hash-hyphen sed loop (lines 147–186 of the script) is the regression target. -->
<!-- The loop in pseudocode: for _ in 1..4 { recovered = sed 's/-[A-Za-z0-9]*[A-Z0-9][A-Za-z0-9]*$//'; if stable break }. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add five Phase 12 per-chunk ceiling constants + check_chunk_ceiling calls</name>
  <files>leanshot/scripts/assert-clinic-bundle-budget.sh</files>
  <read_first>
    - leanshot/scripts/assert-clinic-bundle-budget.sh (entire file — current ceiling constants live at lines 105–128, current check_chunk_ceiling calls at lines 218–227)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §1 "Updated ceiling table" (lines 147–182) — the exact constant names + values + comments to inject
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md D-07 (ceiling values), D-08 (per-phase tightening cadence), D-13 (hash-hyphen fix verification only)
  </read_first>
  <behavior>
    - bash scripts/assert-clinic-bundle-budget.sh exits 0 after npm run build with the current SPA dist/ (none of the five new chunks exist yet → five "wave-0" lines emitted).
    - grep -c '^STRIPE_ELEMENTS_CEILING=30000$' returns 1
    - grep -c '^ADSENSE_GLUE_CEILING=8000$' returns 1
    - grep -c '^PAGE_BUILDER_RUNTIME_CEILING=25000$' returns 1
    - grep -c '^WEB_PUSH_CEILING=3000$' returns 1
    - grep -c '^CAPACITOR_BRIDGE_CEILING=15000$' returns 1
    - grep -cE "check_chunk_ceiling '(stripe-elements|adsense-glue|page-builder-runtime|web-push|capacitor-bridge)-\*\.js'" (filter out commented lines first) returns 5
    - bash scripts/assert-clinic-bundle-budget.sh stdout contains five lines of the form "wave-0: no <label> chunk emitted" for the five new chunks.
  </behavior>
  <action>
    Edit `leanshot/scripts/assert-clinic-bundle-budget.sh` to inject the Phase 12 ceiling block immediately AFTER the existing `SHARE_CEILING=7000` constant (currently line 125) and a sibling `PHASE_12_REF` near the existing `PHASE_REF` / `PHASE_10_REF` lines (currently 127–128). Use these EXACT constant names and values per D-07 (researcher confirmed all five rough caps within 20% — no adjustment):

    Constant declarations (place after SHARE_CEILING):
      `STRIPE_ELEMENTS_CEILING=30000` — Phase 14 (Monetization) owns tightening per D-08; Stripe.js loader ~22 kB gz with Checkout helpers headroom.
      `ADSENSE_GLUE_CEILING=8000` — Phase 20 (Ad Network) owns tightening; GPT loaded as `<script>` so glue-only is `<AdSlot>` + placement config reader.
      `PAGE_BUILDER_RUNTIME_CEILING=25000` — Phase 15 (Page Builder) owns tightening; dnd-kit core + sortable ≈ 17.9 kB gz measured + ~5 kB recursive renderer.
      `WEB_PUSH_CEILING=3000` — Phase 17 (Push) owns tightening; browser-side service-worker registration glue only (the `web-push@3.6.7` npm package itself is server-side).
      `CAPACITOR_BRIDGE_CEILING=15000` — Phase 16 (Mobile Shells) owns tightening; `@capacitor/core` ≈ 12 kB gz + `src/lib/native/*.ts` wrappers ~3 kB.

    Add `PHASE_12_REF=".planning/phases/12-bootstrap-bundle-foundations/12-01-PLAN.md"` immediately after `PHASE_10_REF`.

    Place the five `check_chunk_ceiling` calls immediately after the existing `check_chunk_ceiling 'share-*.js' "$SHARE_CEILING" 'share'` line (currently line 227). Use this EXACT ordering and quoting (mirrors existing style — single-quoted glob, double-quoted variable, single-quoted label):
      `check_chunk_ceiling 'stripe-elements-*.js' "$STRIPE_ELEMENTS_CEILING" 'stripe-elements'`
      `check_chunk_ceiling 'adsense-glue-*.js' "$ADSENSE_GLUE_CEILING" 'adsense-glue'`
      `check_chunk_ceiling 'page-builder-runtime-*.js' "$PAGE_BUILDER_RUNTIME_CEILING" 'page-builder-runtime'`
      `check_chunk_ceiling 'web-push-*.js' "$WEB_PUSH_CEILING" 'web-push'`
      `check_chunk_ceiling 'capacitor-bridge-*.js' "$CAPACITOR_BRIDGE_CEILING" 'capacitor-bridge'`

    Add a leading comment block above the new constants explaining: "Phase 12 D-07/D-08 — five Phase v1.2 per-chunk ceilings declared in advance. wave-0 skip semantics protect until each owning phase actually emits the chunk. Owning phases tighten each ceiling to (measured + ~1 kB headroom) at phase close per D-08."

    Do NOT touch the hash-hyphen sed loop logic (lines 147–186) — D-13 stipulates verification only, not modification. Do NOT modify any other existing ceiling. Do NOT touch the index ceilings (D-09: 24,500 gz working + 50,000 gz absolute remain unchanged in Phase 12).

    Run `npm run build` and then `bash scripts/assert-clinic-bundle-budget.sh` from the `leanshot/` directory; verify exit 0 and five wave-0 lines.
  </action>
  <verify>
    <automated>cd leanshot && npm run build && bash scripts/assert-clinic-bundle-budget.sh && bash scripts/assert-clinic-bundle-budget.sh 2>&1 | grep -E "wave-0: no (stripe-elements|adsense-glue|page-builder-runtime|web-push|capacitor-bridge) chunk emitted" | wc -l | awk '{ if ($1 == "5") exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' leanshot/scripts/assert-clinic-bundle-budget.sh | grep -c '^STRIPE_ELEMENTS_CEILING=30000$'` returns `1`.
    - `grep -v '^#' leanshot/scripts/assert-clinic-bundle-budget.sh | grep -c '^ADSENSE_GLUE_CEILING=8000$'` returns `1`.
    - `grep -v '^#' leanshot/scripts/assert-clinic-bundle-budget.sh | grep -c '^PAGE_BUILDER_RUNTIME_CEILING=25000$'` returns `1`.
    - `grep -v '^#' leanshot/scripts/assert-clinic-bundle-budget.sh | grep -c '^WEB_PUSH_CEILING=3000$'` returns `1`.
    - `grep -v '^#' leanshot/scripts/assert-clinic-bundle-budget.sh | grep -c '^CAPACITOR_BRIDGE_CEILING=15000$'` returns `1`.
    - `grep -v '^#' leanshot/scripts/assert-clinic-bundle-budget.sh | grep -cE "check_chunk_ceiling '(stripe-elements|adsense-glue|page-builder-runtime|web-push|capacitor-bridge)-\\*\\.js'"` returns `5`.
    - After `npm run build`, `bash scripts/assert-clinic-bundle-budget.sh` exits 0 (no FAIL) and stdout contains exactly five `wave-0: no <label> chunk emitted` lines for the five new chunks.
    - The hash-hyphen sed loop region (lines that match `sed 's/-\[A-Za-z0-9\]\*\[A-Z0-9\]\[A-Za-z0-9\]\*\$//'`) is unchanged from the file's prior content (verify via git diff that only additions are present in that file region).
    - The two existing index ceiling constants are unchanged: `grep -v '^#' leanshot/scripts/assert-clinic-bundle-budget.sh | grep -c '^IDX_PHASE9_CEILING=24500$'` returns `1` and `grep -v '^#' leanshot/scripts/assert-clinic-bundle-budget.sh | grep -c '^IDX_ABSOLUTE_CEILING=50000$'` returns `1`.
  </acceptance_criteria>
  <done>
    Five new per-chunk ceilings declared, five `check_chunk_ceiling` calls emitted, index ceiling unchanged, script exits 0 against current dist/ with five wave-0 skip messages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/test-hash-hyphen-regression.sh and wire it into CI</name>
  <files>leanshot/scripts/test-hash-hyphen-regression.sh</files>
  <read_first>
    - leanshot/scripts/assert-clinic-bundle-budget.sh lines 147–186 (the sed loop being regression-tested)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §4 "Hash-Hyphen Regression Test" — the concrete shell-test scaffold to copy
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §9 "Pitfall 7" — BSD `sed` group-syntax warning (use `perl -i -pe` if grouping; this test uses NO grouping, just character classes, so BSD sed is fine)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md D-13 (hash-hyphen fix verification only — do NOT modify the sed loop)
  </read_first>
  <behavior>
    - `bash leanshot/scripts/test-hash-hyphen-regression.sh` exits 0 on the current main branch (the fix is already landed).
    - The test isolates the sed loop logic and runs it against the synthetic stem `clinic-invite-BsW-HOUO` (the exact regression case documented in `reference_bundle_budget_hash_hyphen.md`).
    - The recovered label must equal `clinic-invite` (NOT `clinic-invite-BsW` or any other partial strip).
    - The test exits non-zero (with a descriptive error message) if the sed loop is ever broken in the future.
    - The script `set -euo pipefail` for failure visibility; uses `mktemp -d` + `trap rm` for cleanup hygiene.
  </behavior>
  <action>
    Create `leanshot/scripts/test-hash-hyphen-regression.sh` containing a standalone shell test that replicates the sed loop from `assert-clinic-bundle-budget.sh` lines 184–188 in isolation against the synthetic stem `clinic-invite-BsW-HOUO`, asserts the result equals `clinic-invite`, and exits 0 on success / non-zero on failure. Make the file executable (`chmod +x`).

    The script structure (from 12-RESEARCH.md §4):
    1. `#!/usr/bin/env bash` shebang + `set -euo pipefail` + comment header citing Phase 12 D-13 and Plan 10-11.
    2. Define `STEM="clinic-invite-BsW-HOUO"` and `EXPECTED="clinic-invite"`.
    3. Run the sed loop in isolation: `RECOVERED="$STEM"; PREV=""; for _ in 1 2 3 4; do PREV="$RECOVERED"; RECOVERED=$(echo "$RECOVERED" | sed 's/-[A-Za-z0-9]*[A-Z0-9][A-Za-z0-9]*$//'); [ "$RECOVERED" = "$PREV" ] && break; done`.
    4. If `$RECOVERED` != `$EXPECTED` → `echo "FAIL: ..." >&2; exit 1`.
    5. Otherwise → `echo "PASS: hash-hyphen regression — 'clinic-invite-BsW-HOUO' correctly strips to 'clinic-invite'"; exit 0`.
    6. Also test a "no hash" case: `STEM2="clinic-invite"`, expect unchanged after the loop.
    7. Also test a single-segment hash: `STEM3="clinic-CBid3kQA"`, expect `clinic`.

    DO NOT use `sed -i` or any in-place file mutation. DO NOT use BSD-incompatible regex grouping like `\(group\)` — the existing sed expression uses only character classes which is BSD-compatible (verified in 12-RESEARCH.md §9 Pitfall 7).

    Add the script to CI by editing the parent-repo workflow at `/Users/karstenhaldan/minisite/.github/workflows/ci.yml`. Add a new step named `Hash-hyphen regression test (Phase 12 D-13)` inside the existing `test-e2e` job (which already has bundle-budget steps) after the existing `Assert bundle budget (jspdf chunk topology — Phase 7 COMPL-06 guard)` step (currently around line 161–162). The new step runs `bash scripts/test-hash-hyphen-regression.sh` from the `leanshot/` working directory (the job already sets `working-directory: leanshot` via the top-level `defaults.run.working-directory` block).

    NOTE on parallel executor git isolation (memory `feedback_parallel_executor_git_isolation.md`): when committing, use `git commit -- leanshot/scripts/test-hash-hyphen-regression.sh .github/workflows/ci.yml` (pathspec form) so a sibling Wave-1 plan modifying eslint.config.js or vite.config.ts cannot accidentally sweep into this commit.
  </action>
  <verify>
    <automated>cd leanshot && chmod +x scripts/test-hash-hyphen-regression.sh && bash scripts/test-hash-hyphen-regression.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x leanshot/scripts/test-hash-hyphen-regression.sh` succeeds (file exists and is executable).
    - `bash leanshot/scripts/test-hash-hyphen-regression.sh` exits 0 and stdout contains the string `PASS: hash-hyphen regression`.
    - `grep -c 'test-hash-hyphen-regression.sh' /Users/karstenhaldan/minisite/.github/workflows/ci.yml` returns at least `1` (the step is wired into CI).
    - `grep -c 'STEM="clinic-invite-BsW-HOUO"' leanshot/scripts/test-hash-hyphen-regression.sh` returns `1` (the exact regression case is exercised).
    - The script contains the same `sed 's/-[A-Za-z0-9]*[A-Z0-9][A-Za-z0-9]*$//'` expression that lives at line 186 of `assert-clinic-bundle-budget.sh` (verified by `grep -c "sed 's/-\[A-Za-z0-9\]\*\[A-Z0-9\]\[A-Za-z0-9\]\*\\\$//'" leanshot/scripts/test-hash-hyphen-regression.sh` returning `1`).
    - Mutation proof: temporarily replace the sed expression with `sed 's/-[a-z]*$//'` (a deliberately-broken non-uppercase strip), re-run; the script exits non-zero with a `FAIL:` message. Restore the original expression before committing.
  </acceptance_criteria>
  <done>
    Standalone regression test exists, executable, exits 0 against the current sed loop, and is wired as a CI step in `.github/workflows/ci.yml` before Phase 12 close.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit Plan 12-01 work with pathspec form, flip nyquist_compliant</name>
  <files>leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-01-PLAN.md, leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md</files>
  <read_first>
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md (rows 12-01-01 and 12-01-02; update Status from `⬜ pending` to `✅ green` ONLY after Tasks 1+2 verify automated commands all pass)
    - This PLAN.md frontmatter `nyquist_compliant: false` — flipped to `true` ONLY when both Task 1 and Task 2 verify commands pass
    - Memory `feedback_parallel_executor_git_isolation.md` — use pathspec commit form
  </read_first>
  <behavior>
    - The commit on this plan's branch includes ONLY the four files in this plan's `files_modified` plus the workflow and VALIDATION/PLAN frontmatter edits — verified by commit-message grep (`git log --oneline -10 | grep 'Per-chunk bundle ceilings'`) resolving the SHA and then `git show --stat <SHA>` listing exactly the expected paths. Wave 1 plans commit in parallel — HEAD ordering is non-deterministic.
    - VALIDATION.md rows for 12-01-01 and 12-01-02 show `✅ green` after Tasks 1+2 pass.
    - This PLAN's frontmatter `nyquist_compliant: true` after Tasks 1+2 pass.
  </behavior>
  <action>
    After Tasks 1 and 2 have verified-passed locally:

    1. Update `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md`: change Status cells from `⬜ pending` to `✅ green` for rows `12-01-01` and `12-01-02` (per VALIDATION map). Update the `wave_0_complete` frontmatter field's expected state only when the wave fully closes (do NOT change `wave_0_complete` in this plan — that flips when ALL of 12-01..04 land).

    2. Update this PLAN.md frontmatter: flip `nyquist_compliant: false` → `nyquist_compliant: true`. (Do NOT flip before automated verifies in Tasks 1+2 pass — memory `feedback_planner_iter1_anti_patterns.md` rule #3 says flip-timing must match test-pass timing, not aspiration.)

    3. Commit with the pathspec form per memory `feedback_parallel_executor_git_isolation.md`:
       ```
       git add -- leanshot/scripts/assert-clinic-bundle-budget.sh leanshot/scripts/test-hash-hyphen-regression.sh .github/workflows/ci.yml leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-01-PLAN.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md
       git commit -- leanshot/scripts/assert-clinic-bundle-budget.sh leanshot/scripts/test-hash-hyphen-regression.sh .github/workflows/ci.yml leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-01-PLAN.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md -m "feat(12-01): declare 5 v1.2 per-chunk bundle ceilings + hash-hyphen regression test"
       ```
       The pathspec form prevents sibling Wave-1 plans (12-02 eslint.config.js, 12-03 e2e spec, 12-04 vite.config.ts) from being swept into this commit if they happen to land first on the same shared checkout.

    4. Verify the commit touched only the expected files. Wave 1 plans commit in parallel — do NOT use `git show --stat HEAD` (HEAD may belong to a sibling plan). Use commit-message grep instead: `PLAN_SHA=$(git log --oneline -10 | grep 'Per-chunk bundle ceilings' | head -1 | awk '{print $1}') && git show --stat "$PLAN_SHA" --pretty=format:` lists exactly five paths.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && PLAN_SHA=$(git log --oneline -10 | grep 'Per-chunk bundle ceilings' | head -1 | awk '{print $1}') && test -n "$PLAN_SHA" && git show --stat "$PLAN_SHA" --pretty=format: 2>/dev/null | grep -cE "(leanshot/scripts/assert-clinic-bundle-budget\.sh|leanshot/scripts/test-hash-hyphen-regression\.sh|\.github/workflows/ci\.yml|leanshot/\.planning/phases/12-bootstrap-bundle-foundations/12-01-PLAN\.md|leanshot/\.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION\.md)" | awk '{ if ($1 == "5") exit 0; else { print "expected 5 files, got " $1; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -10 | grep -c 'Per-chunk bundle ceilings'` returns `1` (the 12-01 commit exists in recent history; Wave 1 plans commit in parallel, so HEAD is not used).
    - For the resolved SHA: `git show --stat <SHA> --pretty=format:` lists exactly the 5 files: `leanshot/scripts/assert-clinic-bundle-budget.sh`, `leanshot/scripts/test-hash-hyphen-regression.sh`, `.github/workflows/ci.yml`, `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-01-PLAN.md`, `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md`.
    - `grep -c 'nyquist_compliant: true' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-01-PLAN.md` returns `1`.
    - `grep -E '12-01-(01|02).*✅ green' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md | wc -l` returns `2`.
    - `bash leanshot/scripts/assert-clinic-bundle-budget.sh` after a fresh `npm run build` still exits 0 (no regression introduced by frontmatter edits).
    - `bash leanshot/scripts/test-hash-hyphen-regression.sh` exits 0.
  </acceptance_criteria>
  <done>
    Plan 12-01 work committed atomically with pathspec form, VALIDATION rows green, nyquist_compliant flipped to true.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / PR gate → main branch | The bundle-budget script is the only check standing between a regression introducing a hash-hyphen-style false-negative and silent breakage of per-chunk ceilings. |
| Phase 12 declared chunks → Phases 14/15/16/17/20 implementations | The five declared ceilings are forward contracts — owning phases must respect them and tighten on close per D-08. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-BUNDLE-01 | Tampering | `assert-clinic-bundle-budget.sh` hash-hyphen sed loop (lines 147–186) | mitigate | Task 2 creates `scripts/test-hash-hyphen-regression.sh` that exercises the loop in isolation against the documented `clinic-invite-BsW-HOUO` regression case; mutation-tested in Task 2 acceptance criteria; wired into the parent `.github/workflows/ci.yml` `test-e2e` job. |
| T-BUNDLE-02 | Denial of Service (silent) | Five new per-chunk ceilings | mitigate | All five constants emitted up-front so a future PR adding the SDK cannot also raise the ceiling silently — wave-0 skip until the chunk lands, then the ceiling enforces. Owning phases (14/15/16/17/20) tighten per D-08. |
| T-BUNDLE-03 | Tampering | Index ceiling drift | accept | Index ceilings (`IDX_PHASE9_CEILING=24500`, `IDX_ABSOLUTE_CEILING=50000`) are NOT changed in Phase 12 per D-09. The script continues to enforce both; any PR that breaches either fails. No new attack surface introduced. |
</threat_model>

<verification>
- `bash leanshot/scripts/assert-clinic-bundle-budget.sh` after `npm run build` exits 0 with five `wave-0: no <label> chunk emitted` lines.
- `bash leanshot/scripts/test-hash-hyphen-regression.sh` exits 0.
- `grep -c 'test-hash-hyphen-regression.sh' /Users/karstenhaldan/minisite/.github/workflows/ci.yml` ≥ 1.
- For SHA resolved by commit-message grep (`Per-chunk bundle ceilings`): `git show --stat <SHA>` lists exactly 5 files.
</verification>

<success_criteria>
- All three task `<acceptance_criteria>` blocks pass.
- `nyquist_compliant: true` set in this PLAN's frontmatter.
- VALIDATION.md rows 12-01-01 and 12-01-02 show `✅ green`.
- No regression in existing per-chunk ceilings (`clinic`, `clinic-settings`, `clinic-invite`, `read-only-patient-view`, `share`) — all still pass.
- Index ceiling unchanged (D-09).
</success_criteria>

<output>
After completion, create `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-01-SUMMARY.md` per `templates/summary.md`. Note: this plan covers cross-cutting concern #3 (bundle ceiling 24.5/50 kB gz target); the Phase 12 close-out summary (12-SUMMARY.md) is owned by Plan 12-05.
</output>
