---
phase: 12-bootstrap-bundle-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/eslint.config.js
  - leanshot/src/lib/native/health.ts
  - leanshot/src/lib/native/ads.ts
  - leanshot/src/lib/native/push.ts
  - leanshot/src/lib/native/iap.ts
  - leanshot/src/lib/native/deeplink.ts
  - leanshot/src/lib/native/platform.ts
autonomous: false
nyquist_compliant: false
requirements: [SC-2, CCC-4]
tags: [eslint, two-tunnel-firewall, native-bridge, security]
must_haves:
  truths:
    - "An `import-x/no-restricted-paths` zones block is configured in eslint.config.js with six zones blocking `./src/lib/native/health.ts` from being imported by native/ads, analytics, affiliate, ads-bag, marketing, and Stripe target directories per D-02."
    - "Block B (no-restricted-imports for `*.ad-eligible.ts` glob) and Block C (no-restricted-imports for `posthog*.ts` wrappers glob) cover D-02 buckets that `import-x/no-restricted-paths` cannot target (non-directory globs)."
    - "A second `no-restricted-imports` patterns block scoped to `src/**/*.ad-eligible.ts` files blocks the `*/native/health` import pattern (covers the glob case `import-x/no-restricted-paths` cannot)."
    - "Six stub files exist at `src/lib/native/{health,ads,push,iap,deeplink,platform}.ts` so the ESLint zone paths resolve to real files — without them the rule silently passes (Pitfall 1 from RESEARCH §9)."
    - "On a separate `firewall-test-violation` branch (which NEVER merges), a fixture file `src/lib/native/ads.fixture-violates-firewall.ts` imports from `./health` and `npm run lint` exits non-zero with an `import-x/no-restricted-paths` rule ID in the error output. Branch SHA documented in 12-CONTEXT.md D-03."
  artifacts:
    - path: "leanshot/eslint.config.js"
      provides: "Two-tunnel firewall ESLint static enforcement (D-01, D-02, D-03)"
      contains: "import-x/no-restricted-paths"
    - path: "leanshot/src/lib/native/health.ts"
      provides: "Firewall target — health data tunnel (must NOT be importable from ad-eligible zones)"
      contains: "export"
    - path: "leanshot/src/lib/native/ads.ts"
      provides: "Ad transport tunnel (Zone 1) — feature code routes ad SDK calls through this stub"
      contains: "export"
    - path: "leanshot/src/lib/native/push.ts"
      provides: "Push bridge stub (consumed by Phase 17)"
    - path: "leanshot/src/lib/native/iap.ts"
      provides: "In-app purchase bridge stub (consumed by Phase 16 MONEY-06)"
    - path: "leanshot/src/lib/native/deeplink.ts"
      provides: "Deep-link bridge stub (consumed by Phase 16 MOBILE-06)"
    - path: "leanshot/src/lib/native/platform.ts"
      provides: "Platform detection bridge stub"
  key_links:
    - from: "leanshot/eslint.config.js"
      to: "leanshot/src/lib/native/health.ts"
      via: "import-x/no-restricted-paths `from` field"
      pattern: "from: ['\"]\\./src/lib/native/health\\.ts['\"]"
    - from: "leanshot/eslint.config.js"
      to: "leanshot/src/lib/native/ads.ts, src/lib/analytics, src/lib/affiliate, src/lib/ads, src/lib/marketing"
      via: "import-x/no-restricted-paths `target` field (six zones)"
      pattern: "target: ['\"]\\./src/lib/native/ads['\"]"
---

<objective>
Land the Two-tunnel firewall as a STATIC ESLint build failure per CONTEXT.md D-01/D-02/D-03 and create the six `src/lib/native/*.ts` stub files that the rule zones resolve against. The firewall blocks `src/lib/native/health.ts` from flowing into the five ad-eligible buckets (ad transport, analytics, affiliate, ads bag, marketing) PLUS the `*.ad-eligible.ts` naming-convention glob. A separate `firewall-test-violation` branch contains a deliberate violation fixture proving the rule trips; the branch never merges, only its SHA is recorded.

Purpose: Phase 12 SC-2 (firewall ESLint rule forbids cross-imports between health.ts and ads-family files; produces a static build failure when violated) and cross-cutting concern #4 (Two-tunnel firewall cross-phase contract: Phase 12 ships ESLint, Phase 18 ships runtime, Phase 16 ships Privacy Manifest, Phase 20 audits).
Output: Modified `eslint.config.js`, six new stub files in `src/lib/native/`, fixture branch SHA recorded in CONTEXT.md D-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/PROJECT.md
@leanshot/.planning/ROADMAP.md
@leanshot/.planning/REQUIREMENTS.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md
@leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md
@leanshot/eslint.config.js

<interfaces>
<!-- Existing eslint.config.js shape: -->
<!-- - Uses `defineConfig([...])` from `eslint/config` (line 9, 11) -->
<!-- - Plugin `import-x` is ALREADY loaded at line 8 as `importXPlugin` -->
<!-- - Existing `files: ['src/**/*.{ts,tsx}', '../shared/**/*.ts']` block at line 24 already declares the `import-x` plugin in its `plugins` field -->
<!-- - `import-x/resolver` config at line 35–37 already points at `tsconfig.app.json` -->
<!-- - Existing `no-restricted-syntax` block at lines 82–94 is the precedent for adding new no-restricted-* rules -->
<!-- - Test-files override block at lines 111–123 must NOT be affected -->
<!-- New rule: `import-x/no-restricted-paths` with zones (D-02 six zones) -->
<!-- Plugin lookup: the `import-x` plugin object lives on the existing config block at line 31 — the new zones rule lives in the same `rules` block OR in an adjacent block sharing the same plugins -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create six native bridge stub files</name>
  <files>leanshot/src/lib/native/health.ts, leanshot/src/lib/native/ads.ts, leanshot/src/lib/native/push.ts, leanshot/src/lib/native/iap.ts, leanshot/src/lib/native/deeplink.ts, leanshot/src/lib/native/platform.ts</files>
  <read_first>
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md D-01 (canonical layout `src/lib/native/{health,ads,push,iap,deeplink,platform}.ts`) and D-04 (Phase 12 ships ESLint only — runtime guard lands in Phase 18/20)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §9 Pitfall 1 ("ESLint zone rule silently passes if target directories don't exist" — exactly why these stubs are mandatory in Wave 0)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §10 Plan 12-02 (file list)
  </read_first>
  <behavior>
    - Each file exports at least one named symbol so TypeScript treats it as a module (`tsc --noEmit` passes).
    - Each file's primary export is a stub typed as `(...args: unknown[]) => never` or equivalent that throws `new Error('not yet implemented — Phase 12 stub for Two-tunnel firewall path resolution')` so any feature code that imports it during Phase 13–17 (before the owning phase ships the SDK) gets a loud failure rather than silent no-op.
    - `health.ts` exports a `HealthSample` type alias used by the firewall-violation fixture on the never-merging branch.
    - `ads.ts` exports something the firewall ZONE 1 target resolves to — does NOT import from `./health`.
    - No file imports from another file in the `src/lib/native/` directory (Phase 12 stubs are isolated).
    - `npm run lint` passes on main with these files present.
    - `tsc --noEmit -p tsconfig.app.json` passes.
  </behavior>
  <action>
    Create the six files. Each file must be a real TypeScript module with at least one export so TypeScript and ESLint can resolve it. Use this template per file (adapt symbol names per file):

    `src/lib/native/health.ts`:
    - Comment header: `// Phase 12 D-01/D-02 stub — Two-tunnel firewall target. Real implementation lands in Phase 18 (HEALTH-01..08). DO NOT import from any *.ad-eligible.ts, src/lib/analytics/*, src/lib/affiliate/*, src/lib/ads/*, src/lib/marketing/*, or src/lib/native/ads*.ts file — enforced by import-x/no-restricted-paths in eslint.config.js.`
    - Export a type alias: `export type HealthSample = { metric: 'weight' | 'steps' | 'sleep' | 'hr'; value: number; recordedAt: string };`
    - Export a stub function: `export function readHealthSample(_metric: HealthSample['metric']): never { throw new Error('Phase 12 stub — implemented by Phase 18 via @capgo/capacitor-health'); }`

    `src/lib/native/ads.ts`:
    - Comment header: `// Phase 12 D-01/D-02 stub — ad transport tunnel. Real implementation lands in Phase 20 (AD-01..12). This file MUST NEVER import from ./health — the firewall enforces statically.`
    - Export a type alias: `export type AdPlacement = 'marketing-sidebar' | 'free-tier-banner' | 'interstitial';`
    - Export a stub function: `export function initAdNetwork(): never { throw new Error('Phase 12 stub — implemented by Phase 20 via @capacitor-community/admob + GPT'); }`

    `src/lib/native/push.ts`: stub for Phase 17. Export `export type PushChannel = 'apns' | 'fcm' | 'web-push';` and `export function registerForPush(): never { throw new Error('Phase 12 stub — implemented by Phase 17 (PUSH-01..05)'); }`.

    `src/lib/native/iap.ts`: stub for Phase 16 MONEY-06 (RevenueCat). Export `export type IapProvider = 'apple' | 'google';` and `export function purchaseSubscription(_productId: string): never { throw new Error('Phase 12 stub — implemented by Phase 16 via RevenueCat'); }`.

    `src/lib/native/deeplink.ts`: stub for Phase 16 MOBILE-06. Export `export type DeepLinkRoute = 'share' | 'affiliate' | 'app';` and `export function handleDeepLink(_url: string): never { throw new Error('Phase 12 stub — implemented by Phase 16 via Universal Links + App Links'); }`.

    `src/lib/native/platform.ts`: platform detection stub. Export `export type Platform = 'web' | 'ios' | 'android' | 'capacitor-web';` and `export function detectPlatform(): Platform { return 'web'; }` (this one is safe to return `'web'` since the web build is the only one that ships in Phase 12; the Capacitor-aware version lands in Phase 16).

    DO NOT add any imports between these files (Phase 12 stubs are leaf modules). DO NOT add any `@capacitor/*` import — those plugins land in Phase 16+.

    After creating: run `npx tsc --noEmit -p tsconfig.app.json` from `leanshot/` to confirm strict-mode TS still passes; run `npm run lint` to confirm ESLint still passes (the firewall rule itself lands in Task 2, but the stubs must be lint-clean against the existing rules).
  </action>
  <verify>
    <automated>cd leanshot && test -f src/lib/native/health.ts && test -f src/lib/native/ads.ts && test -f src/lib/native/push.ts && test -f src/lib/native/iap.ts && test -f src/lib/native/deeplink.ts && test -f src/lib/native/platform.ts && npx tsc --noEmit -p tsconfig.app.json && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist: `test -f leanshot/src/lib/native/health.ts && test -f leanshot/src/lib/native/ads.ts && test -f leanshot/src/lib/native/push.ts && test -f leanshot/src/lib/native/iap.ts && test -f leanshot/src/lib/native/deeplink.ts && test -f leanshot/src/lib/native/platform.ts` succeeds.
    - `grep -c 'export type HealthSample' leanshot/src/lib/native/health.ts` returns `1`.
    - `grep -c 'export type AdPlacement' leanshot/src/lib/native/ads.ts` returns `1`.
    - `grep -c "from ['\"]\\./health['\"]" leanshot/src/lib/native/ads.ts` returns `0` (ads.ts does NOT import health.ts on main).
    - `cd leanshot && npx tsc --noEmit -p tsconfig.app.json` exits 0.
    - `cd leanshot && npm run lint` exits 0.
    - No file in `src/lib/native/` contains an `import` statement (all six are leaf modules).
  </acceptance_criteria>
  <done>
    Six stub files exist, type-check, and lint clean against existing rules. Pre-condition for Task 2's zone resolution.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add import-x/no-restricted-paths zones block + supplemental no-restricted-imports for *.ad-eligible.ts to eslint.config.js</name>
  <files>leanshot/eslint.config.js</files>
  <read_first>
    - leanshot/eslint.config.js (entire file — current 124 lines; understand the existing `defineConfig([...])` shape, the existing `src/**/*.{ts,tsx}` block at lines 24–104, and the test-files override block at lines 111–123)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §2 "Concrete ESLint config block" (lines 211–280) — the scaffold to adapt
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md D-02 (full-spectrum firewall — five blocked buckets PLUS the *.ad-eligible.ts glob)
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §9 Pitfall 3 ("`no-restricted-imports` patterns trap — it blocks the IMPORTER, not the IMPORTED") — informs why we use `import-x/no-restricted-paths` for directories and `no-restricted-imports` only for the glob
  </read_first>
  <behavior>
    - `npm run lint` exits 0 on main (no actual violation exists on main).
    - Programmatic check: `npx eslint --print-config src/lib/native/ads.ts | grep -c '"import-x/no-restricted-paths"'` returns 1 (the rule is active on `src/lib/native/ads.ts`).
    - Programmatic check: the printed config's `rules['import-x/no-restricted-paths']` value contains exactly 6 zones, each with a `target` and `from` field; one of them has `target: './src/lib/native/ads'` and `from: './src/lib/native/health.ts'`; another has `target: './src/lib/stripe'` and `from: './src/lib/native/health.ts'`.
    - A deliberately-violating fixture (created on a SEPARATE never-merging branch in Task 3) trips `npm run lint` with a non-zero exit and the error output contains the rule ID `import-x/no-restricted-paths`.
  </behavior>
  <action>
    Edit `leanshot/eslint.config.js` to add the firewall rule. Per RESEARCH §2, the cleanest implementation is two complementary config objects appended to the `defineConfig([...])` array, AFTER the existing `src/**/*.{ts,tsx}` block (which ends around line 104) and BEFORE the test-files override block (around line 111). Do NOT modify the existing blocks.

    Block A — `import-x/no-restricted-paths` with six zones (D-02 buckets 1–5; the *.ad-eligible.ts glob is NOT a directory and is handled in Block B):

    Add this config object scoped to `files: ['src/**/*.{ts,tsx}']`, declaring `plugins: { 'import-x': importXPlugin }`, with `rules`:
    `'import-x/no-restricted-paths': ['error', { zones: [ ... ] }]`

    The SIX zones (use these EXACT `target` and `from` strings — directory paths relative to project root):
    - Zone 1 (ad transport): `target: './src/lib/native/ads'`, `from: './src/lib/native/health.ts'`, message: `'Two-tunnel firewall (Phase 12 D-02 Zone 1): health.ts must not flow into the ad transport. See 12-CONTEXT.md.'`
    - Zone 2 (analytics + posthog directory): `target: './src/lib/analytics'`, `from: './src/lib/native/health.ts'`, message: `'Two-tunnel firewall (Phase 12 D-02 Zone 2a): health.ts must not flow into analytics — PostHog distinctId leak path. See 12-CONTEXT.md.'`
    - Zone 3 (affiliate): `target: './src/lib/affiliate'`, `from: './src/lib/native/health.ts'`, message: `'Two-tunnel firewall (Phase 12 D-02 Zone 3): health.ts must not reach affiliate-attribute Edge Function payloads. See 12-CONTEXT.md.'`
    - Zone 4 (ads bag): `target: './src/lib/ads'`, `from: './src/lib/native/health.ts'`, message: `'Two-tunnel firewall (Phase 12 D-02 Zone 4): health.ts must not enter the ads module bag. See 12-CONTEXT.md.'`
    - Zone 5 (marketing): `target: './src/lib/marketing'`, `from: './src/lib/native/health.ts'`, message: `'Two-tunnel firewall (Phase 12 D-02 Zone 5): health.ts must not enter the marketing module bag. See 12-CONTEXT.md.'`
    - Zone 6 (Stripe metadata helpers — D-02 bucket 4 part 2): `target: './src/lib/stripe'`, `from: './src/lib/native/health.ts'`, message: `'Two-tunnel firewall (Phase 12 D-02 Zone 6): health.ts must not reach Stripe metadata helpers (visible to Connect partners + ad-reconciliation tools). See 12-CONTEXT.md.'`

    NOTE: `src/lib/analytics`, `src/lib/affiliate`, `src/lib/ads`, `src/lib/marketing`, `src/lib/stripe` directories do NOT exist yet — they're created in Phases 14/17/19/20/22. RESEARCH §9 Pitfall 1 warns that `import-x/no-restricted-paths` silently passes when the target directory doesn't exist. ACCEPT this limitation for Phase 12 — each zone activates the moment its directory lands; the unit-mode acceptance criterion in Task 3 is the deliberately-violating fixture on Zone 1 (`./src/lib/native/ads`) which DOES exist after Task 1. Plan-checker for Phases 14/17/19/20/22 MUST verify each directory creation also activates the corresponding firewall zone.

    Block B — supplemental `no-restricted-imports` for the `*.ad-eligible.ts` naming-convention glob (D-02 bucket 6):

    Add a second config object scoped to `files: ['src/**/*.ad-eligible.ts']` with `rules`:
    `'no-restricted-imports': ['error', { patterns: [{ group: ['*/native/health', '*/native/health.ts', '@/lib/native/health'], message: 'Two-tunnel firewall (Phase 12 D-02 supplement): *.ad-eligible.ts files must not import health.ts. See 12-CONTEXT.md.' }] }]`

    Block C — supplemental `no-restricted-imports` for the `posthog*.ts` wrappers glob (D-02 bucket 2 part 2):

    The Zone 2 rule above covers `src/lib/analytics/*`, but D-02 Zone 2 ALSO includes "any `src/lib/posthog*.ts` wrappers" — files like `src/lib/posthog-client.ts` or `src/lib/posthogWrapper.ts` (created in Phase 14/22) that may NOT live inside `src/lib/analytics/`. `import-x/no-restricted-paths` zones target directories, not globs, so a third config object is required.

    Add a third config object scoped to `files: ['src/lib/posthog*.ts', 'src/**/posthog*.ts']` with `rules`:
    `'no-restricted-imports': ['error', { patterns: [{ group: ['*/native/health', '*/native/health.ts', '@/lib/native/health'], message: 'Two-tunnel firewall (Phase 12 D-02 Zone 2b — PostHog wrappers): posthog*.ts files must not import health.ts. See 12-CONTEXT.md.' }] }]`

    Add a top-of-file (or above the new blocks) comment explaining the architecture: `// Phase 12 Two-tunnel firewall — health.ts is blocked from flowing into the ad-eligible bag (D-02). The directory-zone rule covers buckets 1, 2a, 3, 4, 5, and Stripe metadata (6 zones); the *.ad-eligible.ts glob and posthog*.ts wrappers glob are covered by separate no-restricted-imports rules below. See .planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md.`

    After editing: run `npm run lint` from `leanshot/` — must exit 0 (no violation exists on main). Then run `npx eslint --print-config src/lib/native/ads.ts > /tmp/eslint-resolved-config.json 2>&1` and confirm `import-x/no-restricted-paths` appears with six zones.

    DO NOT remove or modify any existing rule in the file. DO NOT change `react-hooks/rules-of-hooks`, `no-restricted-syntax`, or any other existing rule.
  </action>
  <verify>
    <automated>cd leanshot && npm run lint && npx eslint --print-config src/lib/native/ads.ts 2>/dev/null | node -e "const c = JSON.parse(require('fs').readFileSync(0, 'utf-8')); const r = c.rules && c.rules['import-x/no-restricted-paths']; if (!r) { console.error('rule missing'); process.exit(1); } const opts = Array.isArray(r) ? r[1] : null; const zones = opts && opts.zones; if (!zones || zones.length !== 6) { console.error('expected 6 zones, got', zones && zones.length); process.exit(1); } const hasZone1 = zones.some(z => z.target === './src/lib/native/ads' && z.from === './src/lib/native/health.ts'); if (!hasZone1) { console.error('Zone 1 (ads ← health) missing'); process.exit(1); } const hasZone6 = zones.some(z => z.target === './src/lib/stripe' && z.from === './src/lib/native/health.ts'); if (!hasZone6) { console.error('Zone 6 (stripe ← health) missing'); process.exit(1); } console.log('OK 6 zones, Zone 1 + Zone 6 present');"</automated>
  </verify>
  <acceptance_criteria>
    - `cd leanshot && npm run lint` exits 0 on main.
    - `cd leanshot && npx eslint --print-config src/lib/native/ads.ts` JSON output contains a `rules["import-x/no-restricted-paths"]` entry whose options-object's `zones` array has length exactly `6`.
    - One of the zones has `target: './src/lib/native/ads'` and `from: './src/lib/native/health.ts'` (Zone 1).
    - One of the zones has `target: './src/lib/stripe'` and `from: './src/lib/native/health.ts'` (Zone 6 — Stripe metadata helpers, D-02 bucket 4 part 2).
    - `grep -c "'import-x/no-restricted-paths'" leanshot/eslint.config.js` returns `1`.
    - `grep -c "files: \['src/\*\*/\*\.ad-eligible\.ts'\]" leanshot/eslint.config.js` returns `1` (Block B — *.ad-eligible.ts glob exists).
    - `grep -E "files:.*posthog\\*\\.ts" leanshot/eslint.config.js | wc -l` returns at least `1` (Block C — posthog*.ts wrappers glob exists).
    - The existing `no-restricted-syntax` rule block (lines 82–94 in the prior version) is unchanged — `grep -c "useStore(pickFocus|generateInsights)" leanshot/eslint.config.js` still returns the same count as on main (1).
    - The test-files override block is unchanged — `grep -c "files: \['src/\*\*/\*\.test\.{ts,tsx}'" leanshot/eslint.config.js` still returns 1.
  </acceptance_criteria>
  <done>
    Three firewall config blocks appended to eslint.config.js (Block A = 6 directory zones, Block B = *.ad-eligible.ts glob, Block C = posthog*.ts wrappers glob); main lints clean; resolved config confirms 6 zones AND both supplemental blocks are present.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Create firewall-test-violation branch with fixture, push, record SHA in CONTEXT.md, then commit Plan 12-02 to main with pathspec form</name>
  <files>leanshot/src/lib/native/ads.fixture-violates-firewall.ts (on branch firewall-test-violation only — never merges), leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md, leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-02-PLAN.md, leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md</files>
  <read_first>
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md D-03 ("A deliberately-failing fixture file ... lives on a `firewall-test-violation` branch that NEVER merges. CI on that branch must fail on the `no-restricted-imports` rule. Doc the branch SHA in CONTEXT so future audits can re-run.")
    - leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-RESEARCH.md §2 "Fixture file that proves the rule trips" (lines 282–303)
    - Memory `feedback_parallel_executor_git_isolation.md` — pathspec commits when committing main work
    - Memory `feedback_mcp_auth_walls_block_full_automation.md` — push step is what makes this a `human-action` checkpoint (the user owns the remote credentials; do not require the agent to push)
  </read_first>
  <what-built>
    1. Plan 12-02 Tasks 1+2 work staged on `main` (six stub files + eslint.config.js edits).
    2. A separate worktree/branch `firewall-test-violation` containing the fixture file at `leanshot/src/lib/native/ads.fixture-violates-firewall.ts` that imports `./health` and triggers the ESLint rule.
    3. `npm run lint` on the fixture branch exits NON-ZERO with output containing the rule ID `import-x/no-restricted-paths`.
    4. CONTEXT.md D-03 updated with the fixture branch SHA.
  </what-built>
  <action>
    Step-by-step (the agent performs steps 1–4 and 7–9; the human performs steps 5–6):

    1. Create the fixture file ONLY in a freshly-checked-out branch `firewall-test-violation` (do NOT add to main). From repo root: `git checkout -b firewall-test-violation`. Then create `leanshot/src/lib/native/ads.fixture-violates-firewall.ts` with content per RESEARCH §2 lines 286–300:
       ```
       // DO NOT MERGE. This file lives on branch firewall-test-violation only.
       // It proves the Two-tunnel firewall ESLint rule (Phase 12 D-03) trips
       // when health.ts is imported from the ad transport directory.
       //
       // To verify: run `npm run lint` on this branch — it must exit non-zero with
       // an import-x/no-restricted-paths error on the line below.

       // INTENTIONAL VIOLATION DO NOT COPY — must NOT be silenced with eslint-disable
       import type { HealthSample } from './health';

       export const _fixtureViolation: HealthSample | null = null;
       ```
       NOTE the original RESEARCH scaffold uses `// eslint-disable-next-line` — REMOVE that line; we WANT the rule to fire. Adjust accordingly.

    2. From `leanshot/`, run `npm run lint`. Verify exit is non-zero AND stdout/stderr contains `import-x/no-restricted-paths`. If it doesn't fire, the Zone 1 config from Task 2 is broken — fix Task 2 and restart this Task. Capture the lint output to `/tmp/firewall-fixture-lint-output.txt` for reference.

    3. Commit on the firewall-test-violation branch: `git add -- leanshot/src/lib/native/ads.fixture-violates-firewall.ts && git commit -- leanshot/src/lib/native/ads.fixture-violates-firewall.ts -m "test(12-02): deliberate firewall violation fixture (DO NOT MERGE — Phase 12 D-03)"`.

    4. Record the commit SHA: `git rev-parse HEAD` — capture the hash. Switch back to main: `git checkout main`.

    5. **HUMAN ACTION:** Push the firewall-test-violation branch to the remote so CI runs against it as proof: `git push origin firewall-test-violation`. This is a human-action checkpoint because pushing to the remote requires the user's git credentials (memory `feedback_mcp_auth_walls_block_full_automation.md` — the auth wall would block any automation attempt).

    6. **HUMAN VERIFICATION:** Open the GitHub Actions run on the pushed branch and confirm the `lint` job exits red with the `import-x/no-restricted-paths` rule ID in the failure log. Confirm the branch is NOT merged into main and add a branch-protection note in the user's mental model: "do not merge firewall-test-violation."

    7. On main, update `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md` D-03 entry: append a new line after "Doc the branch SHA in CONTEXT so future audits can re-run." with the literal text `Branch SHA: <SHA from step 4>` and a date stamp `recorded: 2026-05-13`.

    8. Update `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md` rows 12-02-01 and 12-02-02 Status cells from `⬜ pending` to `✅ green`. Flip this PLAN's `nyquist_compliant: false` → `true`.

    9. Commit Plan 12-02 work to main using the pathspec form (memory `feedback_parallel_executor_git_isolation.md`) so sibling Wave 1 plans (12-01 budget script, 12-03 e2e spec, 12-04 vite.config.ts/csp test) are NOT swept in if they happen to land first on the shared checkout:
       ```
       git add -- leanshot/eslint.config.js leanshot/src/lib/native/health.ts leanshot/src/lib/native/ads.ts leanshot/src/lib/native/push.ts leanshot/src/lib/native/iap.ts leanshot/src/lib/native/deeplink.ts leanshot/src/lib/native/platform.ts leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-02-PLAN.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md
       git commit -- leanshot/eslint.config.js leanshot/src/lib/native/health.ts leanshot/src/lib/native/ads.ts leanshot/src/lib/native/push.ts leanshot/src/lib/native/iap.ts leanshot/src/lib/native/deeplink.ts leanshot/src/lib/native/platform.ts leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-02-PLAN.md leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-VALIDATION.md -m "feat(12-02): Two-tunnel firewall ESLint rule (D-02 full-spectrum) + 6 native bridge stubs"
       ```
  </action>
  <how-to-verify>
    1. Confirm `git branch --list firewall-test-violation` shows the branch exists locally.
    2. On the fixture branch: `npm run lint` exits non-zero with `import-x/no-restricted-paths` in the output.
    3. On main: `grep -A 2 'D-03' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md | grep 'Branch SHA:'` returns one line with a 40-char SHA hex.
    4. On main: `git log --oneline -10 | grep -c 'Two-tunnel firewall ESLint rule'` returns `1` — verifies the 12-02 commit exists in recent history without depending on HEAD ordering (Wave 1 plans commit in parallel; HEAD may belong to a sibling plan).
    5. `npm run lint` on main exits 0.
    6. The fixture file `leanshot/src/lib/native/ads.fixture-violates-firewall.ts` does NOT exist on main: `test ! -f leanshot/src/lib/native/ads.fixture-violates-firewall.ts` succeeds.
  </how-to-verify>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && git branch --list firewall-test-violation | grep -c firewall-test-violation && test ! -f leanshot/src/lib/native/ads.fixture-violates-firewall.ts && grep -E 'Branch SHA: [0-9a-f]{40}' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md && cd leanshot && npm run lint</automated>
  </verify>
  <resume-signal>Type "approved" once GitHub Actions on `firewall-test-violation` branch is observed RED with `import-x/no-restricted-paths` rule ID; or describe failure mode and pause for fix.</resume-signal>
  <acceptance_criteria>
    - Branch `firewall-test-violation` exists locally and (after step 5) on `origin`.
    - `git rev-parse firewall-test-violation` returns a SHA that matches the value documented in CONTEXT.md D-03.
    - On the fixture branch, `npm run lint` exits non-zero AND `npm run lint 2>&1 | grep -c 'import-x/no-restricted-paths'` is `≥ 1`.
    - On main: `test ! -f leanshot/src/lib/native/ads.fixture-violates-firewall.ts` (the fixture does NOT exist on main).
    - On main: `grep -E 'Branch SHA: [0-9a-f]{40}' leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-CONTEXT.md` returns `1` match.
    - On main: `npm run lint` exits 0.
    - On main: `git log --oneline -10 | grep -c 'Two-tunnel firewall ESLint rule'` returns `1` (Wave 1 plans commit in parallel; the 12-02 commit must appear in recent history without depending on HEAD ordering).
    - On main: `git log -1 --pretty=format:'%s' $(git log --oneline -10 | grep 'Two-tunnel firewall ESLint rule' | head -1 | awk '{print $1}') --stat` lists exactly the 10 expected files (six stubs, eslint.config.js, CONTEXT.md, 12-02-PLAN.md, 12-VALIDATION.md) — verifies the pathspec commit form was used and no sibling-plan files were swept in.
    - This PLAN's `nyquist_compliant: true`.
    - VALIDATION rows 12-02-01 and 12-02-02 marked `✅ green`.
  </acceptance_criteria>
  <done>
    Firewall ESLint rule live on main with stub paths resolving. Fixture branch pushed, CI verified red on the fixture branch by human, SHA recorded in CONTEXT.md D-03, branch is NEVER merged. Plan 12-02 committed atomically to main.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/lib/native/health.ts` (Privacy Tunnel) ↔ `src/lib/native/ads.ts` and four other "second-tunnel" buckets | The Phase 12 D-02 full-spectrum firewall is the structural boundary. ESLint enforces it statically; runtime + Privacy Manifest enforce it again in Phase 18 + Phase 16 (defense in depth). |
| Feature code (Phase 13+) → native bridge layer (`src/lib/native/*.ts`) | Feature code may NEVER import `@capacitor/*` directly (PROJECT.md MOBILE-03). Stubs in this plan establish the indirection seam. |
| `firewall-test-violation` branch → main | The branch MUST NOT merge. CI on the branch is RED by design; branch protection on main means the rule fires for any merge attempt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AD-01 | Information Disclosure | HealthKit data leaking into AdMob via `src/lib/native/ads.ts` (Apple §5.1.3 violation) | mitigate | Static ESLint zone `target: './src/lib/native/ads', from: './src/lib/native/health.ts'` (Task 2 Zone 1) — build fails BEFORE the SDK is even installed in Phase 20. Tested by the deliberately-violating fixture on the firewall-test-violation branch (Task 3). Defense in depth: Phase 18 ships runtime guard, Phase 16 ships PrivacyInfo.xcprivacy. |
| T-AD-02 | Information Disclosure | HealthKit data leaking into PostHog `distinctId` payload via `src/lib/analytics/*` | mitigate | Static ESLint zone (Task 2 Zone 2) — silently passes today because `src/lib/analytics/` doesn't exist, BUT becomes active the moment Phase 14/22 creates the directory. Plan-checker for Phase 14/22 MUST verify the directory + the rule combo. |
| T-AD-03 | Information Disclosure | HealthKit data reaching affiliate-attribute Edge Function payloads | mitigate | Zone 3 (Task 2). Same passing-until-active dynamic as T-AD-02; activates when Phase 19 creates `src/lib/affiliate/`. |
| T-AD-04 | Information Disclosure | HealthKit data leaking via Stripe metadata helpers (D-02 item 4 part 2) | mitigate | Task 2 Zone 6: `target: './src/lib/stripe', from: './src/lib/native/health.ts'`. Zone is silently passing today because `src/lib/stripe/` does not exist yet (RESEARCH §9 Pitfall 1 dynamic — same wave-0 pattern as Zones 2/3/4/5). Zone activates when Phase 14 creates the directory; Phase 14 plan-checker MUST verify the directory + zone combo. |
| T-AD-08 | Information Disclosure | HealthKit data leaking via PostHog wrapper files outside `src/lib/analytics/` (e.g. `src/lib/posthog-client.ts`, `src/lib/posthogWrapper.ts`) | mitigate | Task 2 Block C: supplemental `no-restricted-imports` scoped to `files: ['src/lib/posthog*.ts', 'src/**/posthog*.ts']`. Covers D-02 Zone 2b that `import-x/no-restricted-paths` cannot target (non-directory glob). Activates when Phase 14/22 creates the wrapper file. |
| T-AD-05 | Information Disclosure | HealthKit data leaking via files named `*.ad-eligible.ts` | mitigate | Task 2 Block B supplemental `no-restricted-imports` patterns rule scoped to `src/**/*.ad-eligible.ts` glob — `import-x/no-restricted-paths` cannot target a glob, so this complementary rule covers the naming-convention case. |
| T-FIREWALL-01 | Tampering | A developer suppressing the rule with `// eslint-disable` | accept (process control) | Phase 12 cannot prevent local `eslint-disable` comments; reviewer + plan-checker process controls catch this. Defense in depth: Phase 18 runtime guard fires independently of ESLint. |
</threat_model>

<verification>
- `npm run lint` on main exits 0.
- `npm run lint` on `firewall-test-violation` branch exits non-zero with `import-x/no-restricted-paths` in the output.
- `npx eslint --print-config src/lib/native/ads.ts` resolved config has 6 zones AND Blocks B + C (glob rules) are present.
- All 6 stub files exist; tsc --noEmit passes.
- Fixture branch SHA recorded in CONTEXT.md D-03.
- Fixture file is NOT on main.
</verification>

<success_criteria>
- All three task `<acceptance_criteria>` blocks pass.
- `nyquist_compliant: true` set in this PLAN's frontmatter.
- VALIDATION.md rows 12-02-01 and 12-02-02 show `✅ green`.
- ESLint firewall rule live on main; firewall-test-violation branch on remote is RED.
- Branch SHA documented in CONTEXT.md D-03 for future audit.
</success_criteria>

<output>
After completion, create `leanshot/.planning/phases/12-bootstrap-bundle-foundations/12-02-SUMMARY.md` per `templates/summary.md`. Highlight cross-cutting concern #4 (Two-tunnel firewall cross-phase contract between Phase 18 implementer and Phase 20 auditor).
</output>
