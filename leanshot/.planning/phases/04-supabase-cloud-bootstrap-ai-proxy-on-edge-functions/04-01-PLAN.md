---
phase: 04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/karstenhaldan/minisite/.gitignore
  - /Users/karstenhaldan/minisite/supabase/config.toml
  - /Users/karstenhaldan/minisite/leanshot/package.json
  - /Users/karstenhaldan/minisite/leanshot/package-lock.json
  - /Users/karstenhaldan/minisite/leanshot/.planning/decisions/supabase.md
autonomous: false
requirements: [PROD-07]
threat_refs: []
tags: [supabase, bootstrap, infra, vercel, deno, moonshot]
addendum: 04-ADDENDUM-MOONSHOT.md  # 2026-05-11 pivot: Anthropic→Moonshot. Task 3 secrets are MOONSHOT_API_KEY (placeholder) + MOONSHOT_MODEL=kimi-k2-latest. Task 6 decision record cites the pivot.
user_setup:
  - service: supabase
    why: "Cloud project provisioning + Anthropic key + auth-provider toggles cannot be done by CLI on free tier"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API → Project URL"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API → anon public key"
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com → API Keys (existing platform key the founder owns)"
      - name: ANTHROPIC_MODEL
        source: "Hardcoded to claude-sonnet-4-6 per D-06 (env override for future model rotations)"
    dashboard_config:
      - task: "Create new Supabase project under user's org (us-east-1 region, strong DB password stored in 1Password)"
        location: "https://supabase.com/dashboard → New project"
      - task: "Enable Email magic-link provider"
        location: "Supabase Dashboard → Authentication → Providers → Email → toggle 'Magic Link' ON (SC#0 explicit; Phase 5 prereq)"
      - task: "Enable Anonymous Sign-Ins"
        location: "Supabase Dashboard → Authentication → Providers → 'Anonymous Sign-Ins' → ON (D-02)"
      - task: "Enable Manual Linking"
        location: "Supabase Dashboard → Authentication → Configuration → 'Enable Manual Linking' → ON (Phase 5 prereq per RESEARCH Pitfall 5)"
  - service: vercel
    why: "Push SUPABASE_URL + SUPABASE_ANON_KEY into both Vercel projects across prod/preview/development"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Same value as Supabase env above"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Same value as Supabase env above"
    dashboard_config:
      - task: "Confirm both Vercel projects exist: leanshot-app + leanshot-marketing"
        location: "https://vercel.com/dashboard → projects list"

must_haves:
  truths:
    - "Supabase cloud project exists in the user's org with a recorded project ref + region (SC#0)."
    - "`/Users/karstenhaldan/minisite/supabase/config.toml` is committed and `enable_anonymous_sign_ins = true` (D-02 mirror)."
    - "Supabase CLI is installed as a leanshot/ devDep and authenticated (`supabase login`) and linked (`supabase link --project-ref <ref>`)."
    - "`ANTHROPIC_API_KEY` + `ANTHROPIC_MODEL` are set as Supabase Function secrets (verified via `supabase secrets list`)."
    - "`VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` are present across production+preview+development for BOTH `leanshot-app` and `leanshot-marketing` Vercel projects (verified via `vercel env ls`)."
    - "Magic-link provider, Anonymous Sign-Ins, and Manual Linking are all toggled ON in the Supabase dashboard."
    - "`/Users/karstenhaldan/minisite/leanshot/.planning/decisions/supabase.md` records project_id, region, vercel_app_project_id, vercel_marketing_project_id, dashboard toggles, planned rate-limit thresholds (30/60/200), planned pg_cron schedule, and the Phase 5 hand-off contract."
    - "Curl smoke (`curl -i -X POST https://<ref>.supabase.co/functions/v1/ai-chat -d '{}'`) returns 401 (function endpoint reachable; JWT-verification gate working) — even though no function is deployed yet, the Supabase router answers."
  artifacts:
    - path: "/Users/karstenhaldan/minisite/supabase/config.toml"
      provides: "Supabase CLI project config (generated by `supabase init`, hand-edit only the [auth] block)"
      contains: "enable_anonymous_sign_ins = true"
    - path: "/Users/karstenhaldan/minisite/.gitignore"
      provides: "Excludes Supabase local artifacts + secrets from version control"
      contains: "supabase/.env.secrets"
    - path: "/Users/karstenhaldan/minisite/leanshot/package.json"
      provides: "Supabase CLI pinned as devDependency"
      contains: "\"supabase\":"
    - path: "/Users/karstenhaldan/minisite/leanshot/.planning/decisions/supabase.md"
      provides: "Decision record of project_id, region, Vercel project IDs, dashboard toggles, rate-limit thresholds, anon-row cleanup policy, Phase 5 hand-off contract"
      min_lines: 30
  key_links:
    - from: "Supabase CLI (leanshot/node_modules/.bin/supabase)"
      to: "Supabase cloud project <ref>"
      via: "`supabase link --project-ref <ref>` after `supabase login`"
      pattern: "project ref appears in supabase/.temp/project-ref"
    - from: "Vercel `leanshot-app` project envs"
      to: "Supabase project URL + anon key"
      via: "`vercel env add VITE_SUPABASE_URL production/preview/development` (×3 targets, then again for VITE_SUPABASE_ANON_KEY, then re-linked + repeated for `leanshot-marketing`)"
      pattern: "`vercel env ls production` lists VITE_SUPABASE_URL"
    - from: "Supabase Function secrets store"
      to: "ANTHROPIC_API_KEY + ANTHROPIC_MODEL"
      via: "`supabase secrets set --env-file supabase/.env.secrets`"
      pattern: "`supabase secrets list` includes both keys"
---

<objective>
Provision the Supabase cloud project for LeanShot v1, link the repo, set Function secrets, and wire env vars into both Vercel projects — without any code change to the React app.

This plan ships **SC#0** of Phase 4: the one-time infrastructure slice that every later cloud-using phase (4-02, 4-03, 5, 6, 8, 9, 10) implicitly depends on. The plan deliberately STOPS before any Deno function is authored — Plan 04-02 owns the `ai-chat` Edge Function source; Plan 04-03 owns the migrations.

Purpose: kill the BYO-Anthropic-key model by creating the cloud surface the proxy will run on. After this plan, `curl <project-url>/functions/v1/ai-chat` returns 401 (no function deployed yet, but the project + JWT gate are live).

Output: Supabase project provisioned + linked + secrets set; Vercel envs wired across two projects × three targets; `supabase/config.toml` committed; `.planning/decisions/supabase.md` records the decision tuple for posterity.

**This plan has manual checkpoints** (project creation, dashboard toggles, Vercel env-add prompts) — `autonomous: false`. Per the orchestrator brief, the project creation + Vercel `env add` flows require interactive prompts that cannot be suppressed without bypassing Vercel's confirmation UX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/PROJECT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/ROADMAP.md
@/Users/karstenhaldan/minisite/leanshot/.planning/STATE.md
@/Users/karstenhaldan/minisite/leanshot/.planning/REQUIREMENTS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-VALIDATION.md
@/Users/karstenhaldan/minisite/leanshot/CLAUDE.md

<repo_layout>
Critical: every command in this plan runs from `/Users/karstenhaldan/minisite/` (the git root), NOT from `/Users/karstenhaldan/minisite/leanshot/`. RESEARCH §1 + Pitfall 1 + PATTERNS line 231 all flag this. The Supabase `supabase/` directory MUST sit as a sibling of `leanshot/` and `.github/`, not inside `leanshot/`. Pre-flight every CLI call with `pwd` to confirm location.
</repo_layout>

<interfaces>
<!-- No new code interfaces in this plan — pure infra/config. The contract -->
<!-- consumed by 04-02 is the existence of: -->
<!--   1. supabase/config.toml at repo root -->
<!--   2. A linked project ref discoverable via `supabase status` -->
<!--   3. ANTHROPIC_API_KEY + ANTHROPIC_MODEL in Function secrets -->
<!--   4. VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY in Vercel envs -->
<!-- 04-02 will create supabase/functions/ai-chat/index.ts and consume those -->
<!-- secrets via Deno.env.get(). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Supabase CLI + run `supabase init` at repo root + commit supabase/config.toml with anonymous sign-ins enabled</name>
  <files>/Users/karstenhaldan/minisite/leanshot/package.json, /Users/karstenhaldan/minisite/leanshot/package-lock.json, /Users/karstenhaldan/minisite/supabase/config.toml, /Users/karstenhaldan/minisite/.gitignore</files>
  <read_first>
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §1 lines 975-1059 (full CLI bootstrap recipe, steps 1-4 + .gitignore additions)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §"Common Pitfalls" Pitfall 1 (lines 490-499) — `supabase init` MUST be at git root, NOT inside `leanshot/`
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-PATTERNS.md §"supabase/config.toml" lines 213-232 (analog convention: committed toolchain config, generated then hand-edited)
    - /Users/karstenhaldan/minisite/.gitignore (current state — top-level repo gitignore, currently has `.env`, `.env.local`, etc.)
    - /Users/karstenhaldan/minisite/leanshot/package.json (current devDependencies layout — confirm install location)
  </read_first>
  <action>
    Pre-flight: `cd /Users/karstenhaldan/minisite/` and `pwd` to confirm git-root location; `ls -la` to confirm `leanshot/`, `.github/`, `.git/` are siblings of the planned `supabase/` directory.

    1. Install Supabase CLI as a leanshot/ devDependency at version `^2.98.2` per RESEARCH §1 step 1. Command: `cd /Users/karstenhaldan/minisite/leanshot && npm install --save-dev supabase@^2.98.2`. Verify the binary lands at `/Users/karstenhaldan/minisite/leanshot/node_modules/.bin/supabase` and `cd ..` back to repo root immediately after.

    2. From repo root run `npx --prefix leanshot supabase init`. Accept all defaults; this generates `/Users/karstenhaldan/minisite/supabase/config.toml`. Do NOT generate IDE settings if prompted (no benefit; clutter).

    3. Hand-edit `supabase/config.toml`: find the `[auth]` section and set `enable_anonymous_sign_ins = true` (D-02 mirror). Leave every other key at its `supabase init` default. The dashboard toggle (Task 4) is the prod authority; this line makes local-dev parity correct.

    4. Edit `/Users/karstenhaldan/minisite/.gitignore` to add these lines (idempotent — only add if not already present):
       - `supabase/.env.secrets`
       - `supabase/.branches/`
       - `supabase/.temp/`
       Per RESEARCH §1 step 14 final block. `.env` and `.env.local` already exist in the gitignore from earlier phases — leave untouched.

    5. Verify directory layout post-init: `ls /Users/karstenhaldan/minisite/supabase/` should output `config.toml` (and possibly `.gitignore` autogenerated by the CLI — let it stay).

    6. Commit `supabase/config.toml` + `leanshot/package.json` + `leanshot/package-lock.json` + the updated repo-root `.gitignore` together. Commit message: `chore(04-01): install supabase CLI + init config.toml at repo root` per S-12 commit-protocol convention (Phase 3 established this).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test -f supabase/config.toml && grep -q "enable_anonymous_sign_ins = true" supabase/config.toml && grep -q '"supabase"' leanshot/package.json && grep -q "supabase/.env.secrets" .gitignore && npx --prefix leanshot supabase --version</automated>
  </verify>
  <acceptance_criteria>
    - `/Users/karstenhaldan/minisite/supabase/config.toml` exists and contains the literal string `enable_anonymous_sign_ins = true` within an `[auth]` table (not commented out).
    - `leanshot/package.json` `devDependencies` includes `"supabase"` with a semver pin starting `^2.98`.
    - `leanshot/node_modules/.bin/supabase --version` exits 0 and prints a version string `≥ 2.98`.
    - `/Users/karstenhaldan/minisite/.gitignore` contains all three new lines (`supabase/.env.secrets`, `supabase/.branches/`, `supabase/.temp/`) in addition to the existing `.env` and `.env.local`.
    - `git status` shows only the four expected modified/new files (`supabase/config.toml`, `leanshot/package.json`, `leanshot/package-lock.json`, `.gitignore`) — no stray `supabase/` subdirs created.
    - `/Users/karstenhaldan/minisite/supabase/` is a sibling of `/Users/karstenhaldan/minisite/leanshot/`, NOT inside it (verified via `test -d /Users/karstenhaldan/minisite/supabase && test ! -d /Users/karstenhaldan/minisite/leanshot/supabase`).
  </acceptance_criteria>
  <done>Supabase CLI installed and `supabase init` has produced a committed `supabase/config.toml` at the git root with anonymous sign-ins enabled in the `[auth]` block; repo-root `.gitignore` excludes Supabase local-artifact + secrets directories.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [HUMAN] Create Supabase cloud project + record project ref + region</name>
  <what-built>Task 1 installed the CLI and created `supabase/config.toml`. The CLI cannot create a brand-new Supabase project on the free tier without an interactive dashboard click (per CONTEXT.md "specifics" + RESEARCH §1 step 6 + 04-VALIDATION.md "Manual-Only Verifications" row 1).</what-built>
  <how-to-verify>
    1. Open https://supabase.com/dashboard. Confirm logged in under the user's existing org.
    2. Click "New project". Choose:
       - **Name:** `leanshot-prod` (recommended; final name is the user's call).
       - **Region:** `us-east-1` (RESEARCH §1 step 6 default; user can override — east-coast US is recommended because both Vercel projects deploy to `iad1` by default and inter-region latency dominates Edge Function cold-start cost).
       - **Database password:** generate a strong password (32+ chars); store immediately in 1Password under entry "LeanShot Supabase DB". Do NOT paste anywhere else.
    3. Wait for provisioning to complete (typically 60-120 seconds — dashboard shows a spinner).
    4. Once green, click into Project Settings → API. Copy the following three values into a scratch buffer (the values flow into Tasks 4-6 below):
       - **Project URL:** of the form `https://<ref>.supabase.co` (the `<ref>` is a 20-char string).
       - **Project Reference ID** (same `<ref>` string).
       - **`anon` `public` API key** (long JWT-shaped string).
    5. Reply to this checkpoint with all three values pasted in clearly-labelled order, or paste only the `<ref>` and confirm the URL + anon key are stored in 1Password. The downstream tasks need them.
  </how-to-verify>
  <resume-signal>Reply with project_ref + region + a confirmation that the DB password is in 1Password. Type `approved: ref=<ref> region=<region>`.</resume-signal>
  <files>(none — human action only)</files>
  <action>See &lt;what-built&gt; and &lt;how-to-verify&gt; above; this is a human-only checkpoint with no programmatic action.</action>
  <verify><automated>echo "Checkpoint task — human verification per &lt;how-to-verify&gt;; resumed by signal."</automated></verify>
  <done>Human replied with the &lt;resume-signal&gt; payload; SUMMARY records the response.</done>
</task>

<task type="auto">
  <name>Task 3: `supabase login` + `supabase link` + set Function secrets via `--env-file`</name>
  <files>/Users/karstenhaldan/minisite/supabase/.env.secrets (transient; gitignored; deleted after this task)</files>
  <read_first>
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §1 lines 1003-1027 (login + link + secrets recipe, steps 5-8)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §"Pitfall 6" lines 556-566 (Service Role Key MUST NOT leak into the browser — never put it in a Vite env var)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-CONTEXT.md "D-06" lines 66-68 (ANTHROPIC_MODEL env-var override with `claude-sonnet-4-6` default)
    - /Users/karstenhaldan/minisite/.gitignore (post-Task-1 state — must include `supabase/.env.secrets`)
  </read_first>
  <action>
    Receive the `<project-ref>` from the Task 2 resume signal. All commands run from `/Users/karstenhaldan/minisite/` (verify with `pwd` first).

    1. `npx --prefix leanshot supabase login` — interactive browser flow. After completion, `npx --prefix leanshot supabase projects list` should show the new project. If the CLI prompts for a `SUPABASE_ACCESS_TOKEN` non-interactively, abort and run interactively in a real terminal; do NOT embed the token in any file.

    2. `npx --prefix leanshot supabase link --project-ref <ref>` — prompts for the DB password from Task 2 (paste from 1Password). Verify post-link: `cat supabase/.temp/project-ref` should match `<ref>`.

    3. Get the Anthropic API key from the user (or from their existing platform key store — `console.anthropic.com`). Create `/Users/karstenhaldan/minisite/supabase/.env.secrets` (gitignored per Task 1; verify with `git check-ignore supabase/.env.secrets` returning the path) with EXACTLY these two lines and no trailing whitespace:
       ```
       ANTHROPIC_API_KEY=sk-ant-…
       ANTHROPIC_MODEL=claude-sonnet-4-6
       ```
       Per D-06: `ANTHROPIC_MODEL=claude-sonnet-4-6` is the current latest stable Sonnet (verified by researcher 2026-05-11 against the Anthropic docs); the value is set as a secret so a future rotation needs no code change — only a `supabase secrets set ANTHROPIC_MODEL=<new>` call.

    4. Push the secrets: `npx --prefix leanshot supabase secrets set --env-file supabase/.env.secrets`.

    5. Verify: `npx --prefix leanshot supabase secrets list` — output must list BOTH `ANTHROPIC_API_KEY` and `ANTHROPIC_MODEL` (the values themselves are hashed in the output — that's expected).

    6. DELETE `supabase/.env.secrets` immediately after push: `rm /Users/karstenhaldan/minisite/supabase/.env.secrets`. The file is gitignored so it never enters version control, but leaving it on disk is a key-exposure risk. Re-push requires re-creating the file from 1Password / Anthropic dashboard — that friction is by design.

    Do NOT store the Anthropic key in 1Password again if it already lives there — it's the user's own platform key. Do NOT echo the key to any terminal output, log, or file other than the transient `.env.secrets`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && npx --prefix leanshot supabase secrets list 2>&1 | grep -E "ANTHROPIC_API_KEY|ANTHROPIC_MODEL" | wc -l | grep -q "^[[:space:]]*2$" && test ! -f supabase/.env.secrets && test -f supabase/.temp/project-ref</automated>
  </verify>
  <acceptance_criteria>
    - `supabase secrets list` lists BOTH `ANTHROPIC_API_KEY` and `ANTHROPIC_MODEL` (exactly 2 matches in the grep above).
    - `supabase/.env.secrets` does NOT exist on disk post-task (verified via `test ! -f`).
    - `supabase/.temp/project-ref` exists and contains the project ref string (link succeeded).
    - `git check-ignore supabase/.env.secrets` returns the path (proves the gitignore rule from Task 1 is in effect — even though the file doesn't exist now, the rule must catch it if re-created).
    - No occurrence of `sk-ant-` in any committed file (`git grep -i "sk-ant-" || true` returns nothing).
  </acceptance_criteria>
  <done>Supabase CLI is authenticated and linked to the cloud project; both Function secrets (`ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL=claude-sonnet-4-6`) are stored in Supabase; the transient `.env.secrets` file has been deleted.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [HUMAN] Toggle Supabase dashboard providers — Magic Link, Anonymous Sign-Ins, Manual Linking</name>
  <what-built>Tasks 1-3 set up CLI + secrets. The auth-provider toggles below are dashboard-only (no CLI verb on the free tier as of RESEARCH §1 steps 11-13 + Pitfall 5).</what-built>
  <how-to-verify>
    Open https://supabase.com/dashboard → select the project from Task 2.

    1. **Magic-link provider (SC#0 explicit; Phase 5 prereq):**
       - Navigate to **Authentication → Providers → Email**.
       - Toggle **"Magic Link"** to ON.
       - Leave "Confirm email" at its default (ON — Phase 5 wants email verification).
       - Click Save.

    2. **Anonymous Sign-Ins (D-02 prereq; without this, the proxy in 04-02 cannot mint anonymous JWTs):**
       - Navigate to **Authentication → Providers → "Anonymous Sign-Ins"** (separate row, NOT inside the Email card).
       - Toggle to ON.
       - Click Save.

    3. **Manual Linking (Phase 5 prereq per RESEARCH Pitfall 5 — anonymous→permanent UID preservation):**
       - Navigate to **Authentication → Configuration → "Enable Manual Linking"**.
       - Toggle to ON.
       - Click Save.

    4. Verify via curl (no auth needed):
       ```
       curl -s https://<ref>.supabase.co/auth/v1/settings | jq '.external.email, .external.anonymous_users'
       ```
       Expected output: both fields exist and `email.enabled = true` and `anonymous_users.enabled = true` (exact shape may vary by Supabase Auth version; the keys must be present and truthy).

    5. Reply with the curl output pasted, or paste a screenshot of the three toggle screens.
  </how-to-verify>
  <resume-signal>Reply `approved` with curl output OR screenshots confirming all three toggles ON.</resume-signal>
  <files>(none — human action only)</files>
  <action>See &lt;what-built&gt; and &lt;how-to-verify&gt; above; this is a human-only checkpoint with no programmatic action.</action>
  <verify><automated>echo "Checkpoint task — human verification per &lt;how-to-verify&gt;; resumed by signal."</automated></verify>
  <done>Human replied with the &lt;resume-signal&gt; payload; SUMMARY records the response.</done>
</task>

<task type="auto">
  <name>Task 5: `vercel env add` SUPABASE_URL + SUPABASE_ANON_KEY across leanshot-app and leanshot-marketing — 3 targets × 2 vars × 2 projects = 12 invocations</name>
  <files>(no repo files modified — only Vercel-side state changes)</files>
  <read_first>
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §8 lines 1453-1490 (full Vercel CLI recipe, env-add table, marketing-project discretion call)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-CONTEXT.md "Claude's Discretion → Marketing site Supabase env vars" lines 78-80 — D-01 explicit "BOTH leanshot-app and leanshot-marketing"; RESEARCH §11 recommends provision both → planner picks PROVISION (matches ROADMAP literal text + future-proofs lead-capture forms)
    - /Users/karstenhaldan/minisite/leanshot/vercel.json (current Vercel config) — provides current project linking conventions
  </read_first>
  <action>
    Receive the Supabase project URL (`https://<ref>.supabase.co`) and anon public key from the Task 2 / Task 4 outputs. All commands run from `/Users/karstenhaldan/minisite/leanshot/`.

    Per D-01 + RESEARCH §11 recommendation: provision BOTH Vercel projects. Per Discretion area "Marketing site Supabase env vars" — `leanshot-marketing` doesn't currently call Supabase, but matching ROADMAP literal text + ~10 seconds of CLI time + future-proofing lead-capture forms is the cheaper option than documenting a deviation.

    Per RESEARCH §8 line 1486: `vercel env add` accepts a single target per invocation. Three targets (`production`, `preview`, `development`) × two env vars (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) × two projects (`leanshot-app`, `leanshot-marketing`) = 12 calls. Each `echo "<value>" | vercel env add <name> <target> --yes` pattern avoids exposing the value in shell history.

    1. `cd /Users/karstenhaldan/minisite/leanshot`
    2. `vercel link --yes --project leanshot-app` (interactive if not already linked; pick the user's Vercel org from the prompt).
    3. For target in `production`, `preview`, `development`:
       - `echo "https://<ref>.supabase.co" | vercel env add VITE_SUPABASE_URL <target> --yes`
       - `echo "<anon-key>" | vercel env add VITE_SUPABASE_ANON_KEY <target> --yes`
    4. `vercel link --yes --project leanshot-marketing` (re-link).
    5. Repeat the 6 env-add calls for `leanshot-marketing`.
    6. Verify per project + target:
       - `vercel env ls production | grep -E "VITE_SUPABASE_URL|VITE_SUPABASE_ANON_KEY" | wc -l` should output `2`.
       - Same for `preview` and `development`.
       - Re-link to `leanshot-app` and re-verify (CLI is single-project-scoped per command).

    Naming choice: `VITE_SUPABASE_URL` (not `SUPABASE_URL`) — the React app is Vite-built and only reads `import.meta.env.VITE_*` keys (per CLAUDE.md "Configuration" + RESEARCH §"Code Examples" sample 4 line 738). Marketing project uses the same prefix for consistency; if the marketing build pipeline differs, this is a no-op — unused env vars don't cost anything.

    If the CLI prompts for `--sensitive` choice, accept the default (sensitive for production+preview, encrypted for development — fine for these values per RESEARCH §8 line 1486 final paragraph; the `VITE_*` prefix means the value is public at runtime anyway).

    Do NOT paste the anon key into any committed file or any chat message after this task — keep it in the Task 2 scratch buffer / 1Password only.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && vercel link --yes --project leanshot-app 2>&1 >/dev/null && for t in production preview development; do count=$(vercel env ls $t 2>&1 | grep -cE "VITE_SUPABASE_URL|VITE_SUPABASE_ANON_KEY"); [ "$count" -ge 2 ] || { echo "FAIL leanshot-app/$t count=$count"; exit 1; }; done && vercel link --yes --project leanshot-marketing 2>&1 >/dev/null && for t in production preview development; do count=$(vercel env ls $t 2>&1 | grep -cE "VITE_SUPABASE_URL|VITE_SUPABASE_ANON_KEY"); [ "$count" -ge 2 ] || { echo "FAIL leanshot-marketing/$t count=$count"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `vercel env ls production` while linked to `leanshot-app` shows BOTH `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` (greppable on those exact substrings).
    - Same for `vercel env ls preview` and `vercel env ls development`.
    - Re-link to `leanshot-marketing` and repeat: same 6 entries across 3 targets.
    - Total = 12 env-var entries provisioned (2 vars × 3 targets × 2 projects).
    - No occurrence of the anon key value in any committed file (`git grep -E "eyJ[A-Za-z0-9_-]{40,}" || true` returns nothing — covers JWT-shaped strings).
  </acceptance_criteria>
  <done>Both Vercel projects (`leanshot-app` + `leanshot-marketing`) have `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` set across production+preview+development. 12 env entries total.</done>
</task>

<task type="auto">
  <name>Task 6: Write `.planning/decisions/supabase.md` decision record + commit; smoke-curl the project URL</name>
  <files>/Users/karstenhaldan/minisite/leanshot/.planning/decisions/supabase.md</files>
  <read_first>
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-PATTERNS.md §".planning/decisions/supabase.md" lines 876-922 (target shape: project metadata, dashboard toggles, rate-limit thresholds, anon-row cleanup, Phase 5 hand-off contract)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §11 "Open risks + Claude's Discretion recommendations" lines 1554-1568 (the recommended rate-limit thresholds + anon cleanup policy this decision record locks in)
    - /Users/karstenhaldan/minisite/leanshot/.planning/PROJECT.md "Constraints" + "Key Decisions" sections (analog style — bullet list, key:value)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/03-pharmacology-insights-hardening/03-CONTEXT.md (analog: `### D-NN` heading + rationale + trade-off shape)
  </read_first>
  <action>
    The `.planning/decisions/` directory does not exist yet. Create it first: `mkdir -p /Users/karstenhaldan/minisite/leanshot/.planning/decisions/` (per PATTERNS line 922 explicit gotcha).

    Write `/Users/karstenhaldan/minisite/leanshot/.planning/decisions/supabase.md` with the shape specified in PATTERNS lines 884-915. Concrete content:

    - **`# Supabase Cloud Project — Decision Record`** as the H1.
    - **Recorded:** today's date (2026-05-11 or current).
    - **Phase:** 4.

    Sections (use `## ` headings, bullet `- key: value` style — match PROJECT.md, not full sentences):

    1. **Project metadata** — `project_id` (the `<ref>`), `project_region` (from Task 2), `vercel_app_project_id` (from `vercel project ls` inside `leanshot-app` link), `vercel_marketing_project_id` (from `vercel project ls` inside `leanshot-marketing` link), Supabase dashboard URL of the form `https://supabase.com/dashboard/project/<ref>`.

    2. **Dashboard toggles** (records Task 4 results so future agents can verify drift):
       - Email magic-link provider: ON (SC#0)
       - Anonymous Sign-Ins: ON (D-02)
       - Enable Manual Linking: ON (Phase 5 prereq per RESEARCH Pitfall 5)

    3. **Rate-limit thresholds (Claude's discretion per CONTEXT line 74 + RESEARCH §11 row 1):**
       - minute: 30
       - hour: 60
       - day: 200
       - Rationale link: RESEARCH §5 + §11; SC#4 satisfied with 3.3× margin.
       - These are the values Plan 04-03 hard-wires into the migration's `increment_rate_limit` consumer.

    4. **Anon-row cleanup (Claude's discretion per CONTEXT line 75 + RESEARCH §13 + §11 row 2):**
       - Mechanism: pg_cron daily job (Plan 04-03 owns the migration).
       - Schedule: `'0 3 * * *'` (daily 03:00 UTC).
       - Retention: 30 days post-create (matches Supabase's docs example).
       - Source: `supabase/migrations/<ts>_anon_cleanup_pg_cron.sql` (created in 04-03).

    5. **Phase 5 hand-off contract (RESEARCH §3 lines 1142-1169 + Pitfall 5):**
       - Anonymous → permanent: `supabase.auth.updateUser({email})` then `supabase.auth.updateUser({password})` after verification. NOT `linkIdentity` (which is OAuth-only).
       - Invariant: `auth.uid()` does NOT change across the promotion; all `ai_messages` + `rate_limit_counters` rows survive.
       - Smoke-tested in Plan 04-03.

    6. **Vercel env wiring (records Task 5 outcome):**
       - `VITE_SUPABASE_URL` set on `leanshot-app` (production/preview/development) + `leanshot-marketing` (production/preview/development).
       - `VITE_SUPABASE_ANON_KEY` set on same 6 target/project combos.
       - Per RESEARCH §11 marketing-env discretion recommendation: provisioned both per ROADMAP literal text; marketing site doesn't read these today (no Supabase calls from `Landing.tsx`) but the env vars cost nothing and future-proof lead-capture forms.

    7. **What Plan 04-02 will deploy:**
       - `supabase/functions/ai-chat/index.ts` — Deno proxy entry point.
       - Function secrets already in place (Task 3 — `ANTHROPIC_API_KEY` + `ANTHROPIC_MODEL`).
       - Browser-side: `src/lib/supabase.ts` (new) + rewritten `src/lib/ai.ts` consuming `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY`.

    NO secrets pasted into this file. NO API keys, NO database password, NO JWT values. The `<ref>` and Vercel project IDs are non-sensitive (they appear in dashboard URLs).

    After writing, smoke-test the deployed Auth surface with curl (no JWT needed — this is the negative-case probe per RESEARCH §1 step 14):

    ```bash
    curl -i -X POST https://<ref>.supabase.co/functions/v1/ai-chat -d '{}'
    ```

    Expected: `HTTP/2 401` or `HTTP/2 404` (no function deployed yet, but the project router is alive). Record the response in the SUMMARY for downstream traceability.

    Commit `.planning/decisions/supabase.md` + the new directory mkdir result. Commit message: `docs(04-01): record supabase project provisioning decisions`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test -f leanshot/.planning/decisions/supabase.md && grep -E "project_id:|project_region:|Email magic-link provider:|Anonymous Sign-Ins:|Enable Manual Linking:|minute: 30|hour: 60|day: 200|updateUser" leanshot/.planning/decisions/supabase.md | wc -l | grep -qE "^[[:space:]]*[8-9]$|^[[:space:]]*1[0-9]$" && ! grep -qE "sk-ant-|eyJ[A-Za-z0-9_-]{40}" leanshot/.planning/decisions/supabase.md</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/karstenhaldan/minisite/leanshot/.planning/decisions/supabase.md` exists with at least 30 lines.
    - File contains all 7 required `## ` sections (Project metadata, Dashboard toggles, Rate-limit thresholds, Anon-row cleanup, Phase 5 hand-off contract, Vercel env wiring, What Plan 04-02 will deploy).
    - File contains the literal strings `minute: 30`, `hour: 60`, `day: 200` (rate-limit thresholds).
    - File contains the literal string `updateUser` (Phase 5 hand-off invariant).
    - File does NOT contain any string matching `sk-ant-` or a JWT-shape (`eyJ[A-Za-z0-9_-]{40,}`).
    - Curl probe of `https://<ref>.supabase.co/functions/v1/ai-chat` returns 401 or 404 (any 4xx is acceptable — proves the Supabase router is live; 5xx or connection error is a failure).
  </acceptance_criteria>
  <done>Decision record committed to `.planning/decisions/supabase.md` capturing all Phase 4 infra choices the executor of Plan 04-02 will need. Curl smoke confirms the Supabase project URL is reachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI → Supabase Cloud | `supabase login` token + `supabase link` interactive password authorize all subsequent CLI calls; the access token lives in `~/.supabase/config.json` (not in this repo). |
| Local CLI → Vercel Cloud | `vercel link` writes `.vercel/` (gitignored by Vercel's own .gitignore convention); env-add calls pipe values over HTTPS. |
| Supabase Function secrets store → (future) Edge Function runtime | Plan 04-02 introduces the consumer — this plan only writes secrets. |
| `.env.secrets` temporary file → disk | Mitigated by gitignore + `rm` immediately after secrets push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | `supabase/.env.secrets` (transient) | mitigate | (a) Gitignored in Task 1, (b) `git check-ignore` verification in Task 3 acceptance, (c) `rm` after `secrets set`, (d) acceptance `git grep -i "sk-ant-"` confirms no commit contains the key. (See `<security_threat_model>` T-04-06 in orchestrator brief — Plan 04-01 covers the bootstrap side; the runtime-log redaction concern lives in 04-02.) |
| INFRA-04-01-A | Information Disclosure | Anthropic key in shell history | mitigate | Use `--env-file` pattern (RESEARCH §1 step 8); avoid `supabase secrets set ANTHROPIC_API_KEY=sk-ant-…` direct CLI form which lands in `~/.zsh_history`. |
| INFRA-04-01-B | Spoofing | Wrong Supabase project linked | mitigate | Task 3 acceptance verifies `supabase/.temp/project-ref` matches the `<ref>` from Task 2 resume signal; manual cross-check before secrets push. |
| INFRA-04-01-C | Information Disclosure | Anon JWT (`VITE_SUPABASE_ANON_KEY`) treated as secret | accept | The anon key is INTENTIONALLY public — it ships in the browser bundle. Bundle-time inclusion is its design. RLS + JWT verification on the Edge Function (Plan 04-02) are the actual auth gates. Acceptance noting `eyJ` strings are checked is precautionary against accidentally committing the *service-role* key, which IS secret. |
| INFRA-04-01-D | Tampering | Wrong region selected (e.g. eu-west-1 with us-east-1 Vercel) | accept | Documented in `.planning/decisions/supabase.md` Task 6 + Task 2 step 2 explicit recommendation. Cross-region latency cost is operational, not security. |
| INFRA-04-01-E | Repudiation | No audit trail of who provisioned the project | accept | Supabase records project creator in their own audit log (visible in Dashboard → Settings → Audit). This plan does not duplicate that. |

Threats T-04-01 (prompt injection), T-04-02 (quota bypass), T-04-03 (prompt leak), T-04-04 (cross-tenant), T-04-05 (anon-row accumulation), T-04-07 (CORS) are deferred to 04-02 / 04-03 (proxy + hardening) — this plan deploys no code, only infrastructure.
</threat_model>

<verification>
**Smoke verification at plan end:**

1. `cd /Users/karstenhaldan/minisite && test -f supabase/config.toml && grep -q "enable_anonymous_sign_ins = true" supabase/config.toml`
2. `npx --prefix leanshot supabase secrets list 2>&1 | grep -c "ANTHROPIC_"` returns 2 (both `ANTHROPIC_API_KEY` and `ANTHROPIC_MODEL` present).
3. `npx --prefix leanshot supabase status` outputs the linked project ref (matches Task 2 record).
4. From `leanshot/`, after `vercel link --yes --project leanshot-app`, `vercel env ls production | grep -c VITE_SUPABASE_` returns 2; same after re-linking to `leanshot-marketing`.
5. `test -f /Users/karstenhaldan/minisite/leanshot/.planning/decisions/supabase.md && wc -l < /Users/karstenhaldan/minisite/leanshot/.planning/decisions/supabase.md` shows ≥ 30 lines.
6. `curl -i -X POST https://<ref>.supabase.co/functions/v1/ai-chat -d '{}' 2>&1 | head -1 | grep -E "HTTP/[12](\.[01])? 40[14]"` — 401 or 404 expected (no function yet; project is live).
7. `git grep -iE "sk-ant-|SUPABASE_DB_PASSWORD" -- ':!.planning/' || true` returns nothing (no secrets in committed files; `.planning/` is allowed to mention the variable NAME but not values).

**No code changes in `leanshot/src/`** — verify via `git diff --name-only main -- leanshot/src/` returns empty. This is an infra-only plan; any drift into source means a task has overreached and the plan should be re-run from the failing task.
</verification>

<success_criteria>
**Phase 4 SC#0 satisfied:**

- Supabase cloud project provisioned in user's org, region recorded, project ref captured.
- `supabase init` executed at `/Users/karstenhaldan/minisite/`; `config.toml` committed with `enable_anonymous_sign_ins = true`.
- `supabase login` + `supabase link --project-ref <ref>` authenticated.
- `ANTHROPIC_API_KEY` + `ANTHROPIC_MODEL=claude-sonnet-4-6` set as Function secrets.
- `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` wired into BOTH `leanshot-app` and `leanshot-marketing` Vercel projects across production+preview+development.
- Dashboard toggles confirmed: Email magic-link, Anonymous Sign-Ins, Manual Linking all ON.
- `.planning/decisions/supabase.md` committed with full decision record.
- `curl <project-url>/functions/v1/ai-chat` returns 401/404 (project router live; ready for 04-02 to deploy the function).
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-01-SUMMARY.md` recording:
- Project ref + region.
- The two Vercel project IDs.
- The exact curl-smoke response code from Task 6.
- Any deviations from RESEARCH §1 step ordering (e.g. CLI prompted for something unexpected — record verbatim for future Phase 5 reference).
</output>
