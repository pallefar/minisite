---
phase: 04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions
plan: 02
type: execute
wave: 2
depends_on: [04-01]
addendum: 04-ADDENDUM-MOONSHOT.md  # 2026-05-11 pivot: Anthropic→Moonshot. Edge Function calls Moonshot OpenAI-compatible Chat Completions API (not Anthropic Messages). Headers: Authorization: Bearer ${MOONSHOT_API_KEY}. Payload: messages: [{role: "system"|"user"|"assistant", content}], stream: true. Browser SSE parser consumes OpenAI delta shape: choices[0].delta.content. Read 04-ADDENDUM-MOONSHOT.md BEFORE Task 2.
files_modified:
  - /Users/karstenhaldan/minisite/supabase/functions/ai-chat/index.ts
  - /Users/karstenhaldan/minisite/supabase/functions/ai-chat/system-prompt.ts
  - /Users/karstenhaldan/minisite/supabase/functions/ai-chat/cors.ts
  - /Users/karstenhaldan/minisite/supabase/functions/ai-chat/deno.json
  - /Users/karstenhaldan/minisite/supabase/functions/import_map.json
  - /Users/karstenhaldan/minisite/leanshot/package.json
  - /Users/karstenhaldan/minisite/leanshot/package-lock.json
  - /Users/karstenhaldan/minisite/leanshot/src/lib/supabase.ts
  - /Users/karstenhaldan/minisite/leanshot/src/lib/ai.ts
  - /Users/karstenhaldan/minisite/leanshot/src/lib/storage.ts
  - /Users/karstenhaldan/minisite/leanshot/src/lib/store.ts
  - /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/SettingsPage.tsx
  - /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/ai/AIChatPanel.tsx
  - /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/tabs/NutritionTab.tsx
  - /Users/karstenhaldan/minisite/leanshot/src/components/marketing/Landing.tsx
  - /Users/karstenhaldan/minisite/leanshot/src/main.tsx
  - /Users/karstenhaldan/minisite/leanshot/src/lib/ai.test.ts
autonomous: false
requirements: [AI-01, AI-06]
threat_refs: [T-04-06, T-04-07]
tags: [supabase, edge-function, proxy, sse, anthropic, anonymous-auth, byo-key-removal]
user_setup:
  - service: supabase
    why: "After Plan deploys `ai-chat` via CLI, the function must respond to a real signed-in (anonymous) request from a browser. Manual smoke required against the deployed Preview URL because Edge Function deployment is part of this plan but live verification requires the Vercel Preview URL to exist."
    dashboard_config:
      - task: "Open Vercel Preview URL → AI Coach panel → ensure chat returns a streamed reply (manual UAT for SC#1 + SC#2)"
        location: "Vercel Preview (URL provided by Vercel comment on the PR this plan opens)"

must_haves:
  truths:
    - "Founder opens v2 app, types a message into AI Coach, gets a streamed reply with typing-effect UX — without pasting any key into Settings (SC#1)."
    - "Network tab shows the browser POSTing to `https://<ref>.supabase.co/functions/v1/ai-chat`, NOT to `api.anthropic.com` directly (SC#2)."
    - "Edge Function logs show the call hitting Anthropic with the model ID from the `ANTHROPIC_MODEL` Function secret (default `claude-sonnet-4-6`) — verifiable via `supabase functions logs ai-chat --since 1h` (SC#2)."
    - "Settings → AI card is GONE; the entire `section === 'ai'` block + the nav entry are removed (D-03)."
    - "`Landing.tsx` FAQ no longer advertises BYO key; the two specific lines (474, 486) carry the post-D-03 copy."
    - "`src/lib/ai.ts` no longer imports `apiKeyStorage`; `apiKeyStorage` + `API_KEY_STORAGE` constants are deleted from `src/lib/storage.ts`."
    - "A one-shot `localStorage.removeItem('leanshot_anthropic_key')` runs at app boot in `main.tsx` (S-3 try/catch) so existing users' stale pasted keys are wiped silently (D-03 migration cleanup)."
    - "Anonymous Supabase auth lifecycle works: first AI send mints an `auth.users` row with `is_anonymous = true` via `supabase.auth.signInAnonymously()`; subsequent sends reuse the cached JWT (D-02)."
  artifacts:
    - path: "/Users/karstenhaldan/minisite/supabase/functions/ai-chat/index.ts"
      provides: "Deno proxy: CORS, JWT verification, body validation, Anthropic streaming SSE pass-through with response.body.tee() + EdgeRuntime.waitUntil (refusal + rate-limit hooks added in 04-03 — leave well-marked stubs)"
      min_lines: 100
    - path: "/Users/karstenhaldan/minisite/supabase/functions/ai-chat/system-prompt.ts"
      provides: "`buildSystemPrompt(mode: 'coach' | 'macro-estimator')` — coach + macro templates; structural-separation language (`<user_data>` fence) baked in"
      exports: ["buildSystemPrompt"]
    - path: "/Users/karstenhaldan/minisite/supabase/functions/ai-chat/cors.ts"
      provides: "`corsHeaders` constant — canonical Supabase Allow-Origin: * + apikey allow-header pattern"
      exports: ["corsHeaders"]
    - path: "/Users/karstenhaldan/minisite/supabase/functions/ai-chat/deno.json"
      provides: "Deno project config (lint+fmt+test settings) — required for `deno test` discovery"
    - path: "/Users/karstenhaldan/minisite/supabase/functions/import_map.json"
      provides: "`shared/refusal` alias → `../../shared/refusal.ts` (consumed by 04-03 once the shared module exists; 04-02 references the alias even though `shared/refusal.ts` is still in `src/lib/insights-refusal.ts` — the function code in 04-02 does NOT import refusal yet; it's added in 04-03)"
    - path: "/Users/karstenhaldan/minisite/leanshot/src/lib/supabase.ts"
      provides: "Browser singleton — `createClient(VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY)` with `storageKey: 'sb-leanshot-auth'`"
      exports: ["supabase"]
    - path: "/Users/karstenhaldan/minisite/leanshot/src/lib/ai.ts"
      provides: "REPLACES current direct-Anthropic-fetch with `callAIChat({messages, mode, onText, signal, userContext?})` streaming proxy wrapper; ensures anonymous session before send"
      exports: ["callAIChat", "RateLimitedError", "AIUnavailableError", "ProxyMessage", "CallAIChatOpts"]
  key_links:
    - from: "src/components/dashboard/ai/AIChatPanel.tsx → send()"
      to: "src/lib/ai.ts → callAIChat({onText: (delta) => updateLastAssistant(delta)})"
      via: "import + event handler"
      pattern: "callAIChat\\("
    - from: "src/lib/ai.ts → callAIChat"
      to: "https://<ref>.supabase.co/functions/v1/ai-chat"
      via: "fetch() with Bearer JWT + apikey + Accept: text/event-stream"
      pattern: "PROXY_PATH.*functions/v1/ai-chat"
    - from: "supabase/functions/ai-chat/index.ts → Deno.serve handler"
      to: "https://api.anthropic.com/v1/messages with stream:true"
      via: "fetch + anthropicResp.body.tee() pass-through"
      pattern: "anthropicResp\\.body\\.tee"
    - from: "src/components/dashboard/tabs/NutritionTab.tsx → aiEstimate()"
      to: "src/lib/ai.ts → callAIChat({mode: 'macro-estimator', onText: (delta) => buffer += delta})"
      via: "import + event handler"
      pattern: "mode:\\s*['\"]macro-estimator['\"]"
    - from: "src/main.tsx"
      to: "localStorage.removeItem('leanshot_anthropic_key')"
      via: "one-shot cleanup wrapped in S-3 try/catch"
      pattern: "removeItem\\(['\"]leanshot_anthropic_key['\"]"
---

<objective>
Deliver the working AI proxy — the vertical slice that, when this plan is approved, lets the founder type into AI Coach and get a streamed reply without ever pasting a key. This ships **Phase 4 SC#1 + SC#2**.

Plan boundaries deliberately exclude:
- Refusal pre-check (moved to 04-03 — `shared/refusal.ts` doesn't exist yet; 04-02's `index.ts` carries a TODO + a leave-in stub at the refusal hook).
- Rate-limit table (moved to 04-03 — migrations live there).
- `ai_messages` persistence (moved to 04-03 — migration owns it; 04-02's `index.ts` carries a TODO at the persist hook).
- System-prompt structural separation FULL enforcement of AI-04 (the templates ARE structurally separated in this plan — `<user_data>` fence baked in — but the AI-04 acceptance test lives in 04-03 alongside the refusal corpus).

Why this slice ships SC#1 + SC#2 in isolation: without the proxy responding, no downstream hardening is verifiable. The proxy + browser-rewrite is the minimum vertical slice that puts a real user in front of a streaming reply.

Output: deployed `ai-chat` Edge Function answering 200s; rewired call sites; BYO-key UI removed; one-shot stale-key cleanup at boot.

**Manual checkpoint at end** (Task 5 — open Vercel Preview, hit AI chat) gates the SUMMARY. Per `<security_threat_model>`: T-04-07 CORS-overpermissive is mitigated by JWT-auth gate (Allow-Origin `*` is acceptable per RESEARCH §2 line 1067); T-04-06 key-exposure-via-logs is mitigated by the structured-error 4xx/5xx mapping (RESEARCH §2 lines 1069-1074) that wraps Anthropic non-2xx as `{error: 'anthropic-<status>'}` without echoing the upstream body.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-VALIDATION.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-01-PLAN.md
@/Users/karstenhaldan/minisite/leanshot/.planning/codebase/INTEGRATIONS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/codebase/CONVENTIONS.md
@/Users/karstenhaldan/minisite/leanshot/CLAUDE.md

<interfaces>
<!-- Key interfaces the executor will define (new) and consume (existing) -->

NEW exports from src/lib/ai.ts (replacing current MissingAPIKeyError + callAnthropic + DEFAULT_MODEL):
```typescript
export interface ProxyMessage { role: 'user' | 'assistant'; content: string }
export interface CallAIChatOpts {
  messages: ProxyMessage[];
  mode?: 'coach' | 'macro-estimator';
  userContext?: string;        // raw context string the browser builds today;
                                // Edge Function wraps it in <user_data> fence (AI-04 structural separation)
  onText: (delta: string) => void;
  signal?: AbortSignal;
}
export async function callAIChat(opts: CallAIChatOpts): Promise<void>;
export class RateLimitedError extends Error { constructor() }
export class AIUnavailableError extends Error {
  constructor(public kind: 'signin' | 'upstream' | 'network', message: string)
}
```

NEW export from src/lib/supabase.ts (singleton):
```typescript
export const supabase: SupabaseClient;
```

NEW Zustand store action (added to src/lib/store.ts to support streaming UX, per PATTERNS line 791-796 Option (a) recommendation):
```typescript
updateLastAssistant: (delta: string) => void;
// Appends `delta` to the .content of the LAST message in aiHistory if its role === 'assistant';
// no-ops if the last message isn't an assistant placeholder (defensive).
```

NEW Deno function exports (supabase/functions/ai-chat/system-prompt.ts):
```typescript
export function buildSystemPrompt(mode: 'coach' | 'macro-estimator'): string;
```

NEW Deno function exports (supabase/functions/ai-chat/cors.ts):
```typescript
export const corsHeaders: Record<string, string>;
```

DELETED:
- src/lib/ai.ts → MissingAPIKeyError class
- src/lib/ai.ts → DEFAULT_MODEL constant
- src/lib/ai.ts → callAnthropic function
- src/lib/storage.ts:32 → API_KEY_STORAGE constant
- src/lib/storage.ts:121-143 → apiKeyStorage helper

UNCHANGED (still consumed):
- @/lib/store useStore selectors (append, aiHistory, etc.)
- @/components/ui/* (Card, Section, Toast)
- @/lib/helpers cn utility
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `@supabase/supabase-js` + `eventsource-parser` browser deps; create `src/lib/supabase.ts` singleton + Vitest unit test</name>
  <files>/Users/karstenhaldan/minisite/leanshot/package.json, /Users/karstenhaldan/minisite/leanshot/package-lock.json, /Users/karstenhaldan/minisite/leanshot/src/lib/supabase.ts, /Users/karstenhaldan/minisite/leanshot/src/lib/supabase.test.ts</files>
  <read_first>
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-PATTERNS.md §"src/lib/supabase.ts" lines 544-579 (NEW client factory pattern; storageKey: 'sb-leanshot-auth'; defensive ?? '' fallback for missing env vars; S-1 + S-12 + S-13 patterns)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §"Code Examples" sample 4 lines 729-752 (verbatim factory source)
    - /Users/karstenhaldan/minisite/leanshot/src/lib/storage.ts (current state — confirm `apiKeyStorage` pattern as analog for singleton + try/catch)
    - /Users/karstenhaldan/minisite/leanshot/.planning/codebase/CONVENTIONS.md §"Module / Export Design" (named exports only)
    - /Users/karstenhaldan/minisite/leanshot/src/lib/insights-refusal.test.ts (existing vitest test pattern in `src/lib/`)
  </read_first>
  <behavior>
    - Test 1: `supabase` export is defined and is the result of `createClient(...)` — calling `supabase.auth` returns a non-null AuthClient.
    - Test 2: When `import.meta.env.VITE_SUPABASE_URL` is missing or empty at module-eval, `console.error` is called with a `[leanshot]` prefix; the module still exports a non-throwing `supabase` (fail-soft per CLAUDE.md "AI outage = degraded coach UX, not full-app outage").
    - Test 3: `supabase.auth.signInAnonymously` is callable (mockable surface — proves the SDK shape used by AIChatPanel is wired).
  </behavior>
  <action>
    1. `cd /Users/karstenhaldan/minisite/leanshot && npm install @supabase/supabase-js@^2.45.0 eventsource-parser@^3.0.0`. The first is the browser SDK; the second is the SSE-frame parser RESEARCH §"Don't Hand-Roll" line 465 mandates.

       **Bundle-size sanity:** RESEARCH Assumption A6 + PATTERNS line 578 flag `@supabase/supabase-js` adds ~25-30 kB gz. Phase 2.1 locked Performance ≥ 0.90. After install, do a quick `npm run build` and visually compare `dist/assets/index-*.js` gzipped size against the post-Phase-2.1 baseline (~270 kB total). If the SPA bundle breaks the 320 kB gz total ceiling, defer to checkpoint Task 5 for a discretion call on lazy-loading the supabase client behind the AIChatPanel React.lazy boundary (cost: extra round-trip on first chat open — acceptable).

    2. Create `/Users/karstenhaldan/minisite/leanshot/src/lib/supabase.ts` per RESEARCH §"Code Examples" sample 4 (lines 734-752 in RESEARCH; mirror exactly). Concrete identifiers:
       - Import `createClient` from `@supabase/supabase-js` (group 2: third-party — per S-5).
       - Read `import.meta.env.VITE_SUPABASE_URL` and `import.meta.env.VITE_SUPABASE_ANON_KEY` (S-13 env-read pattern).
       - If either is missing/empty, `console.error('[leanshot] Missing VITE_SUPABASE_URL or VITE_SUPABASE_ANON_KEY')` (S-1 prefix).
       - Export `const supabase = createClient(url ?? '', anonKey ?? '', { auth: { autoRefreshToken: true, persistSession: true, detectSessionInUrl: true, storageKey: 'sb-leanshot-auth' } })`.
       - File header (S-12) cites Phase 4 D-02 + the `storageKey` rationale.

    3. Create `/Users/karstenhaldan/minisite/leanshot/src/lib/supabase.test.ts` co-located per S-11. Test cases per `<behavior>` above.

       Mock `import.meta.env` for Test 2 via `vi.stubEnv('VITE_SUPABASE_URL', '')` + `vi.stubEnv('VITE_SUPABASE_ANON_KEY', '')` and `vi.spyOn(console, 'error')`. Vitest hot-reload of the module under stubbed env happens via dynamic `await import('./supabase')` inside the test (the top-level test file imports vitest only).

    4. Run `npm test -- --run src/lib/supabase.test.ts` to confirm GREEN.

    Do NOT bump `@supabase/supabase-js` to v3 if it exists at install time — the SDK has had breaking changes mid-2025; pin to v2 line per RESEARCH §"Standard Stack" and the `^2.45.0` floor.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && grep -q '"@supabase/supabase-js"' package.json && grep -q '"eventsource-parser"' package.json && test -f src/lib/supabase.ts && grep -q "storageKey:" src/lib/supabase.ts && grep -q "'sb-leanshot-auth'" src/lib/supabase.ts && npm test -- --run src/lib/supabase.test.ts 2>&1 | tail -5 | grep -E "passed|✓"</automated>
  </verify>
  <acceptance_criteria>
    - `leanshot/package.json` `dependencies` contains `@supabase/supabase-js` (^2.45 line) AND `eventsource-parser` (^3.0 line).
    - `leanshot/src/lib/supabase.ts` exports `supabase` and uses `storageKey: 'sb-leanshot-auth'`.
    - File header (S-12 JSDoc) cites Phase 4 D-02.
    - `npm test -- --run src/lib/supabase.test.ts` exits 0; all 3 behavior tests pass.
    - `npm run build 2>&1 | tail -10` succeeds and total bundle size (sum of `dist/assets/*.js` gzipped) is < 320 kB; if it breaches, Task 5 discretion call required.
    - Greppable invariant: `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' src/lib/supabase.ts | grep -c "console.error" | grep -q "^1$"` (exactly one `console.error` in non-comment code, for the missing-env path).
  </acceptance_criteria>
  <done>Browser-side Supabase client singleton wired with anonymous-friendly auth options + green vitest. Bundle measured and within ceiling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author Edge Function source (`index.ts` + `system-prompt.ts` + `cors.ts` + `deno.json` + `import_map.json`); add `updateLastAssistant` store action; rewrite `src/lib/ai.ts` as proxy wrapper; co-locate vitest unit tests</name>
  <files>/Users/karstenhaldan/minisite/supabase/functions/ai-chat/index.ts, /Users/karstenhaldan/minisite/supabase/functions/ai-chat/system-prompt.ts, /Users/karstenhaldan/minisite/supabase/functions/ai-chat/cors.ts, /Users/karstenhaldan/minisite/supabase/functions/ai-chat/deno.json, /Users/karstenhaldan/minisite/supabase/functions/import_map.json, /Users/karstenhaldan/minisite/leanshot/src/lib/ai.ts, /Users/karstenhaldan/minisite/leanshot/src/lib/store.ts, /Users/karstenhaldan/minisite/leanshot/src/lib/ai.test.ts</files>
  <read_first>
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §"Architecture Patterns" Pattern 1 (lines 228-317 — browser-side `callAIChat`) AND Pattern 2 (lines 319-450 — Deno proxy `index.ts` source skeleton)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §2 lines 1063-1075 (CORS + error mapping); §2 cont. lines 1077-1112 (refusalSSE — leave a stub call in 04-02; 04-03 wires the real refusal pre-check); §3 lines 1116-1170 (anonymous auth flow + JWT lifecycle)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §7 lines 1394-1450 (system-prompt + structural separation pattern — `<user_data>` fence; coach + macro-estimator templates)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §"Common Pitfalls" Pitfalls 2, 3, 7, 8 (import_map relative path; JWT verification default ON; cold-start tolerance; EdgeRuntime.waitUntil for SSE pass-through)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-PATTERNS.md §"supabase/functions/ai-chat/index.ts" lines 235-294 (target pattern + gotchas); §"supabase/functions/ai-chat/system-prompt.ts" lines 297-309; §"supabase/functions/ai-chat/cors.ts" lines 312-328; §"supabase/functions/import_map.json" lines 379-400
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-PATTERNS.md §"src/lib/ai.ts (REWRITTEN)" lines 582-639 (rewrite spec; contract change buffered→streaming)
    - /Users/karstenhaldan/minisite/leanshot/src/lib/ai.ts (current state — confirm `MissingAPIKeyError` + `DEFAULT_MODEL` + `callAnthropic` are the only exports; nothing else depends on this file beyond AIChatPanel + NutritionTab + SettingsPage)
    - /Users/karstenhaldan/minisite/leanshot/src/lib/store.ts (current state — find the `aiHistory` slice + `append` action shape so the new `updateLastAssistant` action mirrors it per CONVENTIONS.md verb-noun pattern)
    - /Users/karstenhaldan/minisite/leanshot/src/lib/insights-refusal.ts:42 (the post-Phase-3-fix STEM_PATTERN — confirm 04-02's index.ts has a CLEAR comment that `isDoseChangeAdvice(latestUser.content)` import is deferred to 04-03; do NOT import it in 04-02, the `shared/refusal.ts` module doesn't exist yet)
  </read_first>
  <behavior>
    - Test 1 (`src/lib/ai.test.ts`): `callAIChat({messages, mode: 'coach', onText})` calls `fetch` once with method POST, URL ending `/functions/v1/ai-chat`, headers including `Authorization: Bearer …` and `apikey: …` and `Accept: text/event-stream`, and body containing `messages` + `mode: 'coach'`.
    - Test 2: when `fetch` resolves with `status: 429`, `callAIChat` throws `RateLimitedError`.
    - Test 3: when `fetch` resolves with `status: 502`, `callAIChat` throws `AIUnavailableError` with `kind: 'upstream'`.
    - Test 4: when no session exists, `callAIChat` first calls `supabase.auth.signInAnonymously()` before fetching (mocked supabase client; assert call order).
    - Test 5: SSE frame stream consumed via `eventsource-parser` — feeding a canned Anthropic-shape stream `event: content_block_delta\ndata: {"delta":{"type":"text_delta","text":"hi"}}\n\n` calls `onText('hi')` exactly once.
    - Test 6 (`src/lib/store.test.ts` — add to existing test file or create): `updateLastAssistant('xx')` appends `'xx'` to the last aiHistory message if its role is `assistant`; no-ops if last is `user` or list is empty.
  </behavior>
  <action>
    **Sub-task 2A — Deno function source files (`supabase/functions/ai-chat/*`):**

    1. `mkdir -p /Users/karstenhaldan/minisite/supabase/functions/ai-chat`.

    2. Create `supabase/functions/ai-chat/cors.ts` per PATTERNS lines 312-328: export `corsHeaders` const with `Access-Control-Allow-Origin: '*'`, `Access-Control-Allow-Methods: 'POST, OPTIONS'`, `Access-Control-Allow-Headers: 'authorization, apikey, content-type, x-client-info'`. JSDoc header cites RESEARCH §2 line 1067 (JWT is the auth gate, not the Origin).

    3. Create `supabase/functions/ai-chat/system-prompt.ts` per RESEARCH §7 lines 1396-1430. Two template constants (`COACH_PROMPT_TEMPLATE` + `MACRO_PROMPT_TEMPLATE`) and a `buildSystemPrompt(mode)` function. The COACH template includes:
       - "Never recommend specific dose changes" instruction (defense-in-depth alongside the refusal pre-check 04-03 adds).
       - The `<user_data>…</user_data>` fence treatment instruction (AI-04 structural separation primitive).
       - Reference to Phase 3 PK disclaimer copy — **for 04-02, hardcode the disclaimer string inline** (Option B per RESEARCH lines 1402-1405); 04-03 will swap to `shared/disclaimers.ts` alongside refusal extraction. Mark the inline string with `// TODO(04-03): replace with import from shared/disclaimers.ts`.

    4. Create `supabase/functions/ai-chat/deno.json`:
       ```
       {
         "tasks": {
           "test": "deno test --allow-all --import-map=../import_map.json"
         },
         "lint": { "rules": { "tags": ["recommended"] } },
         "fmt": { "useTabs": false, "lineWidth": 100 }
       }
       ```
       Per 04-VALIDATION.md "Edge framework" row — this enables `cd supabase/functions/ai-chat && deno test` to work without further CLI args during local dev.

    5. Create `supabase/functions/import_map.json` per RESEARCH §"Code Examples" sample 3:
       ```
       {
         "imports": {
           "shared/refusal": "../../shared/refusal.ts"
         }
       }
       ```
       **Note for 04-02:** `shared/refusal.ts` does NOT exist yet (Plan 04-03 creates it). The import map points to its FUTURE location so Plan 04-03 doesn't need to come back and edit this file. The `index.ts` in 04-02 does NOT import `shared/refusal` yet — see Sub-task 2B step 3.

    6. Create `supabase/functions/ai-chat/index.ts` per RESEARCH §"Architecture Patterns" Pattern 2 (lines 326-450). Concrete identifiers (do NOT paste the full code verbatim into `<action>` — read RESEARCH §"Architecture Patterns" Pattern 2 + §2 + §3 + §7 in `<read_first>`; this `<action>` lists the structure):
       - Imports: `jsr:@std/dotenv/load`, `npm:@supabase/supabase-js@2` `createClient`, `./system-prompt.ts` `buildSystemPrompt`, `./cors.ts` `corsHeaders`.
       - Top-level constants from `Deno.env.get`: `ANTHROPIC_URL`, `ANTHROPIC_MODEL = Deno.env.get('ANTHROPIC_MODEL') ?? 'claude-sonnet-4-6'` (S-13 + D-06), `ANTHROPIC_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`.
       - Service-role admin client `admin = createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, { auth: { autoRefreshToken: false, persistSession: false } })`.
       - `ProxyRequest` interface (`messages`, `mode?`, `userContext?` — the `userContext` field is NEW for this plan, consumed by Step 7 below).
       - `Deno.serve(async (req) => { … })` handler implementing the 8 numbered steps from RESEARCH Pattern 2 (lines 358-440):
         1. CORS preflight.
         2. JWT → user via `admin.auth.getUser(jwt)`.
         3. Body parse + validate.
         4. **Refusal pre-check STUB:** add `// TODO(04-03): import { isDoseChangeAdvice } from 'shared/refusal'; if (isDoseChangeAdvice(latestUser.content)) return refusalSSE(corsHeaders);` — do not import anything; leave the comment as a deliberate hook for 04-03.
         5. **Rate-limit STUB:** add `// TODO(04-03): const allowed = await checkAndIncrement(user.id); if (!allowed) return jsonError(429, 'rate-limited');` — comment only.
         6. **`ai_messages` persistence STUB (user side):** comment only `// TODO(04-03): await admin.from('ai_messages').insert({user_id: user.id, role: 'user', content: latestUser.content, mode});`.
         7. **AI-04 structural separation NOW:** before calling Anthropic, transform `body.messages` so the FIRST user message's content is `<user_data>\n${body.userContext ?? ''}\n</user_data>\n\n${m.content}`. RESEARCH §7 lines 1437-1447 verbatim. The system prompt comes from `buildSystemPrompt(body.mode ?? 'coach')` — NEVER concatenate userContext into the system field.
         8. Open Anthropic stream with `stream: true`, model from env. `max_tokens: body.mode === 'macro-estimator' ? 250 : 1024`.
         9. `const [toClient, toCapture] = anthropicResp.body.tee()`; `EdgeRuntime.waitUntil(captureAndPersist(toCapture, user.id))` per RESEARCH §"Code Examples" + Pitfall 8 — for 04-02 the `captureAndPersist` body is `// TODO(04-03): persist assistant message to ai_messages; for 04-02, drain toCapture so the stream doesn't backpressure-stall:` followed by a minimal `const reader = stream.getReader(); while (!(await reader.read()).done) {}`. This avoids a stream-stall bug now while leaving the persist hook clean for 04-03.
         10. Return `new Response(toClient, {status: 200, headers: {...corsHeaders, 'Content-Type': 'text/event-stream', 'Cache-Control': 'no-cache, no-transform', 'Connection': 'keep-alive'}})`.
       - Helpers: `jsonError(status, code)` per RESEARCH §2 lines 442-447; `refusalSSE(corsHeaders)` per RESEARCH §2 cont. lines 1083-1111 — included now so 04-03 only needs to flip the call site, not author the helper.
       - Console-error prefix `[ai-chat]` (S-1 variant per PATTERNS line 50 cross-runtime grep-ability rule).
       - The `@ts-expect-error` on `EdgeRuntime.waitUntil` per RESEARCH line 428.
       - NO `any` types (S-8). NO `console.log` of headers / request bodies (T-04-06 mitigation — even Anthropic errors are wrapped as `{error: 'anthropic-<status>'}`, NEVER the upstream body).

    **Sub-task 2B — Browser-side `src/lib/supabase.ts` consumers + new ai.ts:**

    7. Rewrite `/Users/karstenhaldan/minisite/leanshot/src/lib/ai.ts` per PATTERNS lines 582-639 + RESEARCH §"Architecture Patterns" Pattern 1 (lines 228-317). Concrete identifiers:
       - DELETE the entire current file content (MissingAPIKeyError, DEFAULT_MODEL, ENDPOINT, callAnthropic, CallOptions).
       - Imports: `createParser` from `'eventsource-parser'`, `supabase` from `'@/lib/supabase'`. Import order per S-5.
       - Constants: `const PROXY_PATH = '/functions/v1/ai-chat'`.
       - Exported types: `ProxyMessage`, `CallAIChatOpts` (with `messages`, `mode?`, `userContext?`, `onText`, `signal?`).
       - Exported function: `callAIChat(opts: CallAIChatOpts): Promise<void>` implementing the 3-step pattern (1: ensureSession → signInAnonymously fallback; 2: fetch with Bearer + apikey + Accept; 3: parser.feed loop yielding onText for `content_block_delta` events). Error mapping: status 429 → RateLimitedError; non-2xx → AIUnavailableError('upstream', `${status}`); missing body → AIUnavailableError('upstream', 'empty body'); signin error → AIUnavailableError('signin', error.message).
       - Exported classes: `RateLimitedError`, `AIUnavailableError` (with `kind: 'signin' | 'upstream' | 'network'`).
       - File header (S-12): JSDoc cites Phase 4 D-02 + D-05 + the contract change from buffered to streaming. Note that the OLD `callAnthropic` returned `Promise<string>` and the new `callAIChat` returns `Promise<void>` + yields via `onText`.

    8. Add `updateLastAssistant: (delta: string) => void` action to `src/lib/store.ts` per PATTERNS line 791-796 (Option (a) — preserves typing-effect UX). Implementation: find the last entry in `aiHistory`; if `role === 'assistant'`, replace its `content` with `content + delta`. Else no-op. Mirror the existing action shape (verb-noun naming per CONVENTIONS.md). The existing `append` action is the closest analog — match its `set((s) => ({…}))` style and `partialize` shape preservation.

    9. Co-located vitest: `src/lib/ai.test.ts` per `<behavior>` block above. Mock `supabase.auth.getSession` + `supabase.auth.signInAnonymously`; mock global `fetch` to return a `Response` with a `ReadableStream` body for the SSE-parse test (canned Anthropic-shape frames). Use vitest's `vi.spyOn` + `vi.stubGlobal` patterns established in Phase 1 tests.

    10. Update `src/lib/store.test.ts` (or add `src/lib/store-updateLastAssistant.test.ts`) with Test 6 above.

    11. Run `npm run typecheck` (must pass — strict mode) and `npm test -- --run src/lib/` (all lib tests green).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test -f supabase/functions/ai-chat/index.ts && test -f supabase/functions/ai-chat/system-prompt.ts && test -f supabase/functions/ai-chat/cors.ts && test -f supabase/functions/ai-chat/deno.json && test -f supabase/functions/import_map.json && grep -q "anthropicResp.body.tee" supabase/functions/ai-chat/index.ts && grep -q "EdgeRuntime.waitUntil" supabase/functions/ai-chat/index.ts && grep -q "buildSystemPrompt" supabase/functions/ai-chat/index.ts && grep -q "ANTHROPIC_MODEL" supabase/functions/ai-chat/index.ts && grep -q "claude-sonnet-4-6" supabase/functions/ai-chat/index.ts && grep -q "<user_data>" supabase/functions/ai-chat/index.ts && grep -q "callAIChat" leanshot/src/lib/ai.ts && grep -q "RateLimitedError" leanshot/src/lib/ai.ts && grep -q "AIUnavailableError" leanshot/src/lib/ai.ts && ! grep -q "callAnthropic\|MissingAPIKeyError\|DEFAULT_MODEL" leanshot/src/lib/ai.ts && grep -q "updateLastAssistant" leanshot/src/lib/store.ts && cd leanshot && npm run typecheck 2>&1 | tail -5 && npm test -- --run src/lib/ai.test.ts src/lib/supabase.test.ts 2>&1 | tail -10 | grep -E "passed|✓"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/ai-chat/index.ts` exists; contains `anthropicResp.body.tee()` AND `EdgeRuntime.waitUntil(` AND `buildSystemPrompt(` AND env-read `Deno.env.get('ANTHROPIC_MODEL') ?? 'claude-sonnet-4-6'` AND the `<user_data>` fence transform (greppable: `grep -q '<user_data>' supabase/functions/ai-chat/index.ts`).
    - `supabase/functions/ai-chat/index.ts` contains exactly 3 TODO(04-03) markers (refusal-precheck, rate-limit, ai_messages-persist) — `grep -c "TODO(04-03)" supabase/functions/ai-chat/index.ts` returns ≥ 3.
    - `supabase/functions/import_map.json` has the `"shared/refusal": "../../shared/refusal.ts"` entry.
    - `leanshot/src/lib/ai.ts` exports `callAIChat`, `RateLimitedError`, `AIUnavailableError` and does NOT contain `callAnthropic`, `MissingAPIKeyError`, or `DEFAULT_MODEL`.
    - `leanshot/src/lib/store.ts` exports an `updateLastAssistant` action (greppable in the action interface + the `set((s) =>` block).
    - `npm run typecheck` exits 0.
    - `npm test -- --run src/lib/ai.test.ts src/lib/supabase.test.ts` shows all 6+ behavior tests passing.
    - No `any` types introduced — `grep -nE ': *any[^a-z_]|<any>' leanshot/src/lib/ai.ts leanshot/src/lib/supabase.ts supabase/functions/ai-chat/*.ts | grep -v "^[[:space:]]*\*\|// "` returns empty.
  </acceptance_criteria>
  <done>Edge Function source authored with all four production behaviors (CORS, JWT, body validation, structural-separation `<user_data>` fence, SSE pass-through via tee+waitUntil) and three commented hooks for 04-03 (refusal pre-check, rate-limit, ai_messages persist). Browser `callAIChat` rewrites complete with green vitest. Store `updateLastAssistant` action added.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite AIChatPanel.tsx + NutritionTab.tsx call sites; delete BYO key UI in SettingsPage.tsx; rewrite Landing.tsx FAQ copy; one-shot stale-key cleanup in main.tsx; delete apiKeyStorage from storage.ts</name>
  <files>/Users/karstenhaldan/minisite/leanshot/src/components/dashboard/ai/AIChatPanel.tsx, /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/tabs/NutritionTab.tsx, /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/SettingsPage.tsx, /Users/karstenhaldan/minisite/leanshot/src/components/marketing/Landing.tsx, /Users/karstenhaldan/minisite/leanshot/src/main.tsx, /Users/karstenhaldan/minisite/leanshot/src/lib/storage.ts</files>
  <read_first>
    - /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/ai/AIChatPanel.tsx (lines 1-130 — current send() shape, `ctx` build, current error branches, existing typing-loop hook)
    - /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/tabs/NutritionTab.tsx (lines 1-100 — current aiEstimate, MissingAPIKeyError branch, JSON.parse pattern)
    - /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/SettingsPage.tsx (lines 200-280 — AI Card to delete + sibling Section convention to preserve + nav array)
    - /Users/karstenhaldan/minisite/leanshot/src/components/marketing/Landing.tsx (lines 460-500 — FAQ array; lines 474 + 486 the two strings to rewrite)
    - /Users/karstenhaldan/minisite/leanshot/src/main.tsx (current state — hydrate() + createRoot — where to insert the one-shot localStorage cleanup)
    - /Users/karstenhaldan/minisite/leanshot/src/lib/storage.ts (lines 32 + 121-143 — API_KEY_STORAGE + apiKeyStorage to delete; confirm PersistedState interface lines 34-55 does NOT include `anthropicKey` so STORAGE_VERSION stays at 6 per PATTERNS line 651)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-PATTERNS.md §"src/components/dashboard/ai/AIChatPanel.tsx" lines 724-800 (target shape for send() with streaming + setError pattern); §"src/components/dashboard/tabs/NutritionTab.tsx" lines 804-845 (target shape for aiEstimate with buffer-and-parse-on-end + toast error branches); §"src/components/dashboard/settings/SettingsPage.tsx" lines 661-690 (deletion spec + nav-array cleanup gotcha); §"src/components/marketing/Landing.tsx" lines 694-720 (FAQ rewrite copy); §"src/lib/storage.ts" lines 642-657 (deletion spec + one-shot cleanup placement Option (a) in main.tsx)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-CONTEXT.md "D-03" lines 41-49 (BYO removal scope: 6 files)
  </read_first>
  <action>
    **Sub-task 3A — AIChatPanel.tsx (call-site rewrite):**

    1. Read AIChatPanel.tsx fully. Locate the import line 8-9 (`import { callAnthropic, MissingAPIKeyError } from '@/lib/ai';`) and replace with `import { callAIChat, RateLimitedError, AIUnavailableError } from '@/lib/ai';`. Plus pull in the new `updateLastAssistant` from useStore — `const updateLastAssistant = useStore((s) => s.updateLastAssistant)` selector per the store action's CONVENTIONS.md selector pattern.

    2. Rewrite `send()` per PATTERNS lines 759-789. Concrete shape:
       - First, `append({ role: 'user', content: text.trim() })`.
       - Then `append({ role: 'assistant', content: '' })` — placeholder the streaming will fill.
       - Build `ctx` (the existing local Zustand-state-derived context string at AIChatPanel.tsx:71 today) unchanged.
       - Call `await callAIChat({ messages: [...history, {role:'user', content: text.trim()}], mode: 'coach', userContext: ctx, onText: (delta) => updateLastAssistant(delta) })`.
       - Catch block: `if (e instanceof RateLimitedError)` → set a local error state and the placeholder assistant message becomes "Hit the AI rate limit — try again in a minute" via a final `updateLastAssistant(...)` or a `append` replacement (planner picks; simplest is `updateLastAssistant('Hit the AI rate limit — try again in a minute.')`); `else if (e instanceof AIUnavailableError)` → `updateLastAssistant('AI is unavailable right now. Please try again shortly.')`; `else` → generic catch-all same as AIUnavailableError branch.
       - The empty-state copy MUST be the user-friendly D-03 wording: "AI is unavailable right now" (NOT "Add your Anthropic key"). Per `<security_threat_model>` + AIChatPanel `role="status"` `aria-live="polite"` S-9 pattern.
       - Remove `ctx` from any `system:` field (the system prompt is server-side now); `userContext` is the new wire.

    3. Verify the typing-effect: `updateLastAssistant(delta)` should mutate the placeholder message in place; Phase 2 baseline typing-effect UX preserved per RESEARCH §"Recommended Project Structure" line 149.

    **Sub-task 3B — NutritionTab.tsx (call-site rewrite):**

    4. Read NutritionTab.tsx fully. Locate import at line 10 — replace `MissingAPIKeyError` import with `RateLimitedError, AIUnavailableError`. Keep `callAIChat` import.

    5. Rewrite `aiEstimate` per PATTERNS lines 810-838:
       - Set `setAIBusy(true)`.
       - Build `buffer = ''`.
       - `await callAIChat({ messages: [{role:'user', content: \`Estimate macros. Return ONLY a JSON object, no markdown.\nFormat: {"calories": number, "protein": number, "fiber": number}\n\nMeal: \${meal.name}\`}], mode: 'macro-estimator', onText: (delta) => { buffer += delta; } })`. NOTE: `mode: 'macro-estimator'` triggers the server-side MACRO_PROMPT_TEMPLATE — DIFFERENT system prompt + smaller max_tokens (250 vs 1024) per Sub-task 2A step 6.
       - After stream end: `const cleaned = buffer.replace(/```json|```/g, '').trim(); const parsed = JSON.parse(cleaned)`, set meal state per current shape, `toast('AI estimated')`.
       - Catch: `if (e instanceof RateLimitedError) toast('Hit the AI rate limit — try again in a minute', 'error');` `else if (e instanceof AIUnavailableError) toast('AI is unavailable right now — enter manually', 'error');` `else toast('AI failed — enter manually', 'error');`.
       - `finally { setAIBusy(false); }`.

    **Sub-task 3C — SettingsPage.tsx (BYO key deletion):**

    6. Read SettingsPage.tsx lines 200-280. Identify:
       - Lines ~224-262: the entire `{section === 'ai' && (<Section title="AI assistant" ...>...</Section>)}` block.
       - The `apiKeyStorage` import at the top of the file.
       - Any `const [apiKey, setApiKey] = useState(...)` local state.
       - The `'ai'` entry in the section-nav array (search for `section === 'ai'` references and the array literal — typically a `const SECTIONS = ['profile', 'ai', 'goals', ...]` or similar near top of file).

    7. Delete all of the above. Sibling `Section` blocks (`goals`, `notifications`, `data`, etc.) stay byte-identical. After deletion, the file should typecheck — there's no orphan reference to `apiKeyStorage` or `MissingAPIKeyError` in this file.

    8. Manual smoke verification recorded in Task 5: Settings drawer no longer shows an "AI" nav entry; no dead-route render.

    **Sub-task 3D — Landing.tsx FAQ rewrite:**

    9. Read Landing.tsx lines 460-500. Locate the FAQ items array. Line 474 (`a` field of "Is my data shared with anyone?") and Line 486 (`a` field of "Does AI cost extra?"). Rewrite EXACTLY per PATTERNS lines 715-716:
       - Line 474 new `a`: `"No. Everything lives in your browser's localStorage. We never send your weight, dose, or notes to any server. The only exception is the AI coach, which sends just your prompt + the relevant context to Anthropic through our secure server using your account (you never share an API key)."`
       - Line 486 new `a`: `"AI coaching is included — no separate API key, no extra setup. Pro adds priority support and unlimited progress card templates."`
       - All other FAQ entries stay byte-identical. The `items` array shape, the `<section>` wrapper, the `FAQ` component import all untouched.

    **Sub-task 3E — main.tsx one-shot cleanup (D-03 stale-key wipe):**

    10. Read main.tsx fully. After `await hydrate()` but before `createRoot`, insert a single block per S-3 (silent try/catch):
        ```ts
        // Phase 4 D-03 cleanup: remove any stale BYO Anthropic API key from
        // localStorage (the old pasted-key UX is gone in v4 cloud auth).
        try { localStorage.removeItem('leanshot_anthropic_key'); } catch { /* noop */ }
        ```
        File header / nearby JSDoc note S-12.

    **Sub-task 3F — storage.ts deletion:**

    11. Read storage.ts. Delete:
        - Line 32 `export const API_KEY_STORAGE = 'leanshot_anthropic_key';`.
        - Lines 121-143 entire `apiKeyStorage` const object literal.
        - Any unused imports.
        - Do NOT bump `STORAGE_VERSION` — PATTERNS line 651 + D-03 line 45 + a fresh inspection of `PersistedState` interface lines 34-55 confirms the field never lived in the partialized Zustand state. Stays at 6.

    **Sub-task 3G — Wiring + tests:**

    12. Run `npm run typecheck` (must pass).
    13. Run `npm test -- --run` (all existing tests green; Phase 3's 177/177 must NOT regress).
    14. Run `npm run lint` (S-1..S-13 invariants).
    15. Co-located component test for SettingsPage to assert the AI section is gone (R Testing Library — `render(<SettingsPage />)` and `expect(screen.queryByText(/AI assistant/i)).toBeNull()`). Place at `src/components/dashboard/settings/SettingsPage.test.tsx` (per S-11). Mock useStore to provide a minimal user fixture.

    16. Run a marketing-build smoke if there's a separate vite config: `cd /Users/karstenhaldan/minisite/leanshot && npm run build` and confirm both `dist/` and `dist-marketing/` (if marketing build exists) succeed with the new Landing.tsx copy.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && ! grep -rn "MissingAPIKeyError\|callAnthropic\|apiKeyStorage\|API_KEY_STORAGE" src/ --include="*.ts" --include="*.tsx" && grep -q "callAIChat" src/components/dashboard/ai/AIChatPanel.tsx && grep -q "RateLimitedError\|AIUnavailableError" src/components/dashboard/ai/AIChatPanel.tsx && grep -q "mode: 'macro-estimator'\|mode: \"macro-estimator\"" src/components/dashboard/tabs/NutritionTab.tsx && ! grep -q "section === 'ai'\|section === \"ai\"" src/components/dashboard/settings/SettingsPage.tsx && grep -q "through our secure server using your account" src/components/marketing/Landing.tsx && grep -q "AI coaching is included" src/components/marketing/Landing.tsx && grep -q "removeItem('leanshot_anthropic_key')" src/main.tsx && ! grep -q "apiKeyStorage\|API_KEY_STORAGE" src/lib/storage.ts && npm run typecheck 2>&1 | tail -3 && npm test -- --run 2>&1 | tail -10 | grep -E "Tests.*passed" && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Greppable: zero occurrences of `MissingAPIKeyError`, `callAnthropic`, `apiKeyStorage`, or `API_KEY_STORAGE` anywhere under `leanshot/src/`.
    - `AIChatPanel.tsx` imports `callAIChat, RateLimitedError, AIUnavailableError` from `'@/lib/ai'`.
    - `NutritionTab.tsx` aiEstimate call uses `mode: 'macro-estimator'`.
    - `SettingsPage.tsx` no longer contains `section === 'ai'` or the AI Card block.
    - `Landing.tsx` FAQ copy at the previous lines 474 + 486 contains the new strings (greppable on `"through our secure server using your account"` and `"AI coaching is included"`).
    - `main.tsx` contains the literal `removeItem('leanshot_anthropic_key')` wrapped in a try/catch (S-3).
    - `src/lib/storage.ts` no longer contains `apiKeyStorage` or `API_KEY_STORAGE`.
    - `npm run typecheck` exits 0.
    - `npm test -- --run` shows ALL tests green (Phase 3's 177/177 baseline preserved; new vitest from Tasks 1+2 added on top).
    - `npm run lint` exits 0.
    - `npm run build` succeeds for both `dist/` and `dist-marketing/` (if marketing build is separately scripted).
    - The new component test (`SettingsPage.test.tsx`) asserts the AI section is absent.
  </acceptance_criteria>
  <done>BYO key UI fully removed across all six surfaces (D-03 satisfied). AIChatPanel + NutritionTab call sites now use streaming proxy with typed-error branching. Stale-key wipe runs at boot. Full vitest + typecheck + lint + build green.</done>
</task>

<task type="auto">
  <name>Task 4: Deploy `ai-chat` Edge Function via `supabase functions deploy --import-map`; live curl smoke against deployed URL</name>
  <files>(no repo source files modified — deployment metadata only)</files>
  <read_first>
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §1 lines 1028-1031 (deploy command with import-map flag, JWT verification ON by default)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-RESEARCH.md §"Common Pitfalls" Pitfall 3 lines 516-535 (deploy default JWT verification ON; Pitfall 8 lines 575-583 EdgeRuntime.waitUntil must be present for SSE; Pitfall 7 lines 567-573 cold-start)
    - /Users/karstenhaldan/minisite/leanshot/.planning/decisions/supabase.md (post-04-01 — confirm project ref + the planned `<ref>` value)
    - /Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-VALIDATION.md "Per-Task Verification Map" row 04-02 (curl smoke target: `<fn-url> returns 200 + text/event-stream content-type`)
  </read_first>
  <action>
    Pre-flight: `cd /Users/karstenhaldan/minisite/` and confirm `supabase/functions/ai-chat/index.ts` + `supabase/functions/import_map.json` exist.

    1. Deploy the function: `npx --prefix leanshot supabase functions deploy ai-chat --import-map supabase/functions/import_map.json`. Per RESEARCH Pitfall 3, do NOT pass `--no-verify-jwt` — leave the default JWT-verification gate ON.

    2. Verify deployment: `npx --prefix leanshot supabase functions list` should now show `ai-chat` (previously the empty state per 04-VALIDATION.md Per-Task table row 04-01-01).

    3. Live curl smoke against the deployed URL. Read the project ref from `.planning/decisions/supabase.md`. Run:
       ```bash
       curl -i -X POST "https://<ref>.supabase.co/functions/v1/ai-chat" \
            -H "Content-Type: application/json" \
            -d '{"messages":[]}'
       ```
       Expected: `HTTP/2 401` (no JWT → JWT-gate rejects; mitigates T-04-07 partially by proving auth-gate is operative). Record the response in the SUMMARY.

    4. Second curl smoke WITH a real anonymous JWT — this is the SC#0 reachability proof and the SC#1 happy-path probe:
       ```bash
       # Obtain an anonymous JWT for the smoke (requires SUPABASE_ANON_KEY + project-ref):
       ANON_KEY=$(...)  # from Plan 04-01 Task 5 value — keep it in 1Password
       JWT=$(curl -s -X POST "https://<ref>.supabase.co/auth/v1/signup" \
              -H "apikey: $ANON_KEY" -H "Content-Type: application/json" \
              -d '{}' | jq -r '.access_token')

       curl -i -N -X POST "https://<ref>.supabase.co/functions/v1/ai-chat" \
            -H "Authorization: Bearer $JWT" \
            -H "apikey: $ANON_KEY" \
            -H "Content-Type: application/json" \
            -H "Accept: text/event-stream" \
            -d '{"messages":[{"role":"user","content":"hi"}],"mode":"coach"}' \
            --max-time 15 | head -40
       ```
       Expected:
       - `HTTP/2 200`
       - `content-type: text/event-stream`
       - One or more `event: content_block_delta` lines followed by `event: message_stop` (Anthropic SSE shape). The function spent < 5 seconds returning first byte (SC#0 latency floor: "< 5 seconds").

    5. `supabase functions logs ai-chat --since 1m` should show one POST that returned 200 and one upstream call to Anthropic with `model=claude-sonnet-4-6` in the log line (or whichever model env-secret resolves to). Record the log excerpt in the SUMMARY.

    6. If deploy fails because `shared/refusal.ts` is missing (it is — that's Plan 04-03's deliverable), the function should still deploy successfully — the import_map.json references the FUTURE path, but `index.ts` in 04-02 has the refusal import commented out (Sub-task 2A step 6 step 4). Deno only resolves imports at call time, so the missing target doesn't break deploy. If deploy does fail with a `cannot resolve "shared/refusal"` error, that means the executor accidentally uncommented the import; abort and re-check Sub-task 2A step 6 step 4.

    Do NOT paste the SUPABASE_ANON_KEY or the JWT value into the SUMMARY — only record the response status codes + content-type header + first event line of the SSE body.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && npx --prefix leanshot supabase functions list 2>&1 | grep -q "ai-chat" && REF=$(cat leanshot/.planning/decisions/supabase.md | grep -oE "project_id:[[:space:]]*[a-z0-9]+" | awk '{print $2}') && [ -n "$REF" ] && curl -sI -X POST "https://${REF}.supabase.co/functions/v1/ai-chat" -H "Content-Type: application/json" -d '{"messages":[]}' 2>&1 | head -1 | grep -qE "HTTP/[12](\.[01])? 401" && echo "Deploy + JWT-gate smoke OK"  </verify>
  <acceptance_criteria>
    - `supabase functions list` output contains `ai-chat` (function deployed).
    - Unauthenticated POST to `https://<ref>.supabase.co/functions/v1/ai-chat` returns HTTP 401 (JWT-gate active per RESEARCH Pitfall 3).
    - Authenticated POST (with anonymous JWT minted via `/auth/v1/signup`) returns HTTP 200 + `content-type: text/event-stream` AND at least one `content_block_delta` SSE frame within 15 seconds (SC#0 latency floor).
    - `supabase functions logs ai-chat --since 1m` shows the resolved model is `claude-sonnet-4-6` (per `ANTHROPIC_MODEL` Function secret from 04-01 Task 3).
    - SUMMARY.md records: function URL, two curl response codes, content-type header, first SSE event line, and the log excerpt.
    - No secrets (ANON_KEY, JWT, ANTHROPIC_API_KEY) appear in the SUMMARY or any committed file.
  </acceptance_criteria>
  <done>Edge Function deployed and live; JWT-gate verified; authenticated smoke streams a real Anthropic reply through the proxy with the correct model ID.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [HUMAN] Open Vercel Preview, hit AI Coach + Nutrition macro-estimator, confirm streamed reply WITHOUT pasting any key (SC#1 + SC#2)</name>
  <what-built>Tasks 1-4 deployed the proxy, rewired the call sites, and removed the BYO key UI. This checkpoint is the SC#1 + SC#2 verification — there is no automated substitute because "user opens app and types and sees a reply" is the SC's literal phrasing.</what-built>
  <how-to-verify>
    Open the PR that this plan produced (Tasks 1-3 committed code changes; the PR's Vercel comment links to a Preview URL).

    1. **AI Coach happy-path (SC#1 + SC#2):**
       - Open the Vercel Preview URL in a fresh incognito browser window (no prior leanshot localStorage).
       - Click through onboarding to reach the dashboard (use any minimal fixture values).
       - Click the AI Coach surface (sidebar or topbar; depends on layout).
       - Type "Hi" and submit.
       - Expected:
         - Within ~3 seconds, a streamed reply starts appearing character-by-character (typing-effect UX preserved per D-05 + RESEARCH §"Recommended Project Structure" line 149).
         - The reply is in the "warm, concise, practical coach" voice from the COACH_PROMPT_TEMPLATE (Sub-task 2A step 3).
         - NO settings dialog appears asking for an Anthropic key (SC#1 — no BYO).
       - Open browser DevTools → Network tab. Verify:
         - POST to `https://<ref>.supabase.co/functions/v1/ai-chat` (NOT to `api.anthropic.com`) — SC#2 explicit.
         - Response headers include `content-type: text/event-stream`.
         - The Anthropic call doesn't appear in the browser network log (it's server-side).

    2. **Nutrition macro-estimator path (mode: 'macro-estimator' exercised end-to-end):**
       - Navigate to Nutrition tab.
       - Add a meal "1 cup oatmeal with berries" → click "AI estimate".
       - Expected:
         - Macros are populated within ~5 seconds.
         - JSON parse succeeds (no toast error).
         - The Network tab again shows POST to `/functions/v1/ai-chat` with body containing `mode: "macro-estimator"`.

    3. **Settings AI section removal (D-03 visual UAT):**
       - Open Settings drawer.
       - Confirm there is NO "AI" or "AI assistant" entry in the section nav.
       - Confirm there is NO Anthropic key input anywhere in Settings.

    4. **Marketing FAQ copy (D-03 marketing-site UAT):**
       - Open the marketing Preview URL (separate Vercel project; URL likely `leanshot-marketing-git-<branch>-…vercel.app` per Phase 2 deploy split).
       - Scroll to FAQ section.
       - Confirm "Is my data shared with anyone?" answer mentions "through our secure server using your account".
       - Confirm "Does AI cost extra?" answer reads "AI coaching is included — no separate API key".

    5. **Edge Function Logs (SC#2 verification surface):**
       - In a separate terminal: `npx --prefix leanshot supabase functions logs ai-chat --since 5m`
       - Confirm a log line for the coach POST and the macro-estimator POST.
       - Confirm the model ID logged matches `claude-sonnet-4-6`.

    6. **Bundle-size regression check (RESEARCH Assumption A6 + PATTERNS line 578):**
       - Confirm Vercel build logs show SPA bundle size within the 320 kB gz ceiling. If breached, this checkpoint becomes a `checkpoint:decision` for lazy-loading the supabase client behind the AIChatPanel React.lazy boundary.

    Reply with screenshots OR a paste of: (a) the Network tab POST URL + status, (b) the streamed reply visible in the AI Coach panel, (c) Settings drawer screenshot showing no AI nav entry, (d) the marketing FAQ copy.
  </how-to-verify>
  <resume-signal>Reply `approved` after all 6 verification items pass, OR describe specifically which item failed for follow-up debug.</resume-signal>
  <files>(none — human action only)</files>
  <action>See &lt;what-built&gt; and &lt;how-to-verify&gt; above; this is a human-only checkpoint with no programmatic action.</action>
  <verify><automated>echo "Checkpoint task — human verification per &lt;how-to-verify&gt;; resumed by signal."</automated></verify>
  <done>Human replied with the &lt;resume-signal&gt; payload; SUMMARY records the response.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Edge Function | JWT (Bearer) verified by Supabase Edge Runtime BEFORE our code runs (per RESEARCH Pitfall 3 — JWT verification ON by default on deploy). The `apikey` header carries the anon key, also Supabase-verified. |
| Edge Function → Anthropic API | `x-api-key` header carries `ANTHROPIC_API_KEY` from Function secrets store (read via `Deno.env.get`); request body carries user-provided messages WITH the `<user_data>` fence applied (AI-04 structural separation in code as of Task 2 Sub-task 2A step 6 step 7). |
| Edge Function → service-role DB writes | Service-role client (`SUPABASE_SERVICE_ROLE_KEY` from Function secrets) bypasses RLS for writes; `user_id` field MUST come from the verified JWT (`user.id`), NEVER from the request body (T-04-04 mitigation primitive — fully enforced in 04-03 when `ai_messages` insert is enabled; commented stub in 04-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | Edge Function error-path logs | mitigate | Non-2xx upstream Anthropic responses are wrapped as `{error: 'anthropic-<status>'}` (RESEARCH §2 lines 1069-1074) — NEVER `r.text()` echoed. Headers + bodies are never logged. The `[ai-chat]` console-error prefix is used for structural logs only (which user_id was rate-limited, etc.) — no body content. Verified in Task 2 acceptance: `index.ts` does not call `console.log` of any header or request body. |
| T-04-07 | Tampering | CORS over-permissive | mitigate | `Access-Control-Allow-Origin: *` is intentional and acceptable because the JWT is the auth gate, not the Origin (RESEARCH §2 line 1067 + PATTERNS line 328). The `apikey` header in the allow-list is mandatory for `@supabase/supabase-js` callers. Allowing `*` here is NOT a vulnerability because (a) JWT-gate ON, (b) anon JWT is per-user identity, (c) state-changing writes use service-role client which is server-side. |
| T-04-04 | Information Disclosure | Cross-tenant via `user_id` spoof | mitigate (partial) | The persistence step is STUBBED in 04-02. When Plan 04-03 enables `ai_messages.insert`, the integrity invariant "`user_id` from verified JWT, never from body" must hold. Task 2 acceptance pre-bakes the comment hook that 04-03 follows. Anti-pattern explicit in RESEARCH §"Anti-Patterns" line 457. |
| INFRA-04-02-A | Spoofing | Plaintext key in browser bundle | mitigate | The bundle now contains only `VITE_SUPABASE_ANON_KEY` (intentionally public — JWT-issuer key, not service-role). `git grep -E "sk-ant-"` returns nothing. `apiKeyStorage` deleted from storage.ts; stale-key cleanup in main.tsx wipes any prior on-disk pasted keys (D-03 migration). |
| INFRA-04-02-B | Denial of Service | Cold-start latency | accept | RESEARCH Pitfall 7 — Edge Function cold-start is acceptable for streaming responses (Anthropic's own time-to-first-token dominates anyway). 04-03 adds rate-limit which both protects Anthropic spend AND short-circuits abuse before cold-start cost. |
| INFRA-04-02-C | Tampering | SSE stream interrupted by EdgeRuntime termination | mitigate | `EdgeRuntime.waitUntil(captureAndPersist(...))` ensures the function lives past the response close (RESEARCH Pitfall 8). In 04-02 the captureAndPersist is a no-op drain; 04-03 swaps in the real DB write. |

Threats T-04-01 (prompt-injection refusal), T-04-02 (rate-limit), T-04-03 (prompt leak via fence-token echo), T-04-05 (anon-row accumulation) are all DEFERRED to Plan 04-03 — this plan opens the proxy + structural-separation fence; 04-03 wires the refusal pre-check, rate-limit RPC, fence-token corpus assertion, and pg_cron cleanup.
</threat_model>

<verification>
**End-of-plan smoke (executor runs all of these in sequence — must all pass before checkpoint Task 5):**

1. `npm run typecheck` from `/Users/karstenhaldan/minisite/leanshot/` exits 0.
2. `npm test -- --run` shows all tests green; Phase 3's 177/177 baseline is preserved (the new vitest from Tasks 1-2 adds 6+ new tests on top of 177 = 183+ total).
3. `npm run lint` exits 0.
4. `npm run build` exits 0; produces `dist/` and `dist-marketing/` (if separately scripted).
5. `npx --prefix leanshot supabase functions list` from `/Users/karstenhaldan/minisite/` lists `ai-chat`.
6. Curl smoke (no JWT): `curl -i -X POST https://<ref>.supabase.co/functions/v1/ai-chat -d '{}'` returns 401.
7. Curl smoke (anonymous JWT): same URL with Bearer JWT returns 200 + `content-type: text/event-stream` + ≥ 1 `content_block_delta` SSE frame.
8. `supabase functions logs ai-chat --since 5m` shows the model resolved to `claude-sonnet-4-6` (from the secret set in 04-01 Task 3).
9. Greppable invariants:
   - `git grep -rE "callAnthropic|MissingAPIKeyError|apiKeyStorage|API_KEY_STORAGE" leanshot/src/` returns nothing.
   - `grep -c "TODO(04-03)" supabase/functions/ai-chat/index.ts` returns 3 (refusal, rate-limit, persist hooks deliberately deferred).
   - `grep -q "<user_data>" supabase/functions/ai-chat/index.ts` succeeds (AI-04 structural separation primitive present).
10. Checkpoint Task 5 manual UAT approves all 6 verification items.
</verification>

<success_criteria>
**Phase 4 SC#1 satisfied:** Founder opens v2 app, types a question into the AI coach, gets a streamed reply — without ever pasting a key into Settings; the Settings UI no longer offers BYO key.

**Phase 4 SC#2 satisfied:** Network tab on a real chat session shows the browser calling `/functions/v1/ai-chat` (not `api.anthropic.com` directly); Edge Function logs show the call hitting Anthropic with `claude-sonnet-4-6` (the current latest stable Sonnet per RESEARCH §12 verification).

**Partial AI-04 progress:** Structural separation of user content from system prompts is IN CODE (Sub-task 2A step 6 step 7 — `<user_data>` fence transform); 04-03 adds the adversarial-corpus assertion that proves the model never echoes fence tokens (T-04-03 mitigation).

**SC#3, SC#4, SC#5 explicitly NOT satisfied by this plan** — those move to Plan 04-03.
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/04-supabase-cloud-bootstrap-ai-proxy-on-edge-functions/04-02-SUMMARY.md` recording:
- Deployed Edge Function URL.
- The two curl smoke response codes (unauth → 401, anon-JWT → 200).
- Bundle size delta vs Phase 2.1 baseline (must be < 320 kB gz total).
- Vercel Preview URL the human used for Task 5 UAT.
- The model ID returned in Anthropic's response (proving `ANTHROPIC_MODEL` env is honored).
- The 3 TODO(04-03) hook locations (file + line numbers) so 04-03 doesn't grep blindly.
</output>
