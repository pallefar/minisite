---
phase: 50-admin-curated-rag-knowledge-base-peptide-topic-research-scra
plan: 06
type: execute
wave: 2
depends_on: [50-01, 50-02, 50-03, 50-05]
files_modified:
  - src/components/admin/rag/RagQueuePage.tsx
  - src/components/admin/rag/QueueDetailPane.tsx
  - src/components/admin/rag/RejectReasonSheet.tsx
  - src/components/admin/rag/EditChunkModal.tsx
  - src/components/admin/rag/RetractChunkModal.tsx
  - src/lib/admin/rag/chunk-api.ts
  - supabase/migrations/20260519000012_rag_chunk_transition_rpcs.sql
  - supabase/migrations/20260519000013_rag_sla_alert_cron.sql
  - src/lib/rag/__tests__/publish-flow.test.ts
  - src/lib/rag/__tests__/reject-reasons.test.ts
  - src/lib/rag/__tests__/sla-backlog.test.ts
  - src/lib/rag/__tests__/retract.test.ts
  - src/lib/rag/__tests__/erratum-flag.test.ts
  - e2e/admin-rag-queue.spec.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-15: Tier-A chunks auto-publish on scrape — rag_chunk_publish_tier_a() RPC sets published_at=now() for any chunk with source_tier='A' AND status='queued'; called automatically at end of Plan 50-05 summarize pipeline (post-hoc helper RPC; doesn't break P50-05 idempotency)"
    - "D-15: Tier-B/C chunks land in /admin/rag/queue with status='queued' until admin reviews"
    - "D-16: 6 reject reasons (off-topic | factually-wrong | off-label | low-quality | duplicate | safety-concern) selectable in queue UI; reject reason persisted to rag_chunks.reject_reason; rolling 30d count exposed to /admin/rag/sources for D-16 trust-tier downgrade signal (≥5 rejects/30d highlighted in danger color)"
    - "D-17: side-by-side source-text vs extracted-quote pane in queue detail (UI-SPEC §A3); quote_blocks rendered with kind badge (dose/indication/contraindication/adverse-event); source markdown sanitized via react-markdown + dompurify per UI-SPEC §Registry Safety"
    - "D-18: SLA monitor cron checks queue every hour; rag_chunks queued >7d at Tier-B OR >14d at Tier-C contributes to backlog count; backlog > 100 triggers Slack alert via Edge Fn (placeholder until Plan 50-09 finalizes alerting); UI shows 'Backlog: {N} items' warning when >100"
    - "D-20: retract action sets retracted_at + retracted_reason; chunk excluded from retrieval Edge Fn (Plan 50-07); audit log captures actor/timestamp/reason; already-sent newsletters unaffected (no retroactive correction at v1.3)"
    - "D-21: erratum_flag manual toggle in queue UI; combined with Plan 50-04 diff-detect re-queue covers erratum detection per CONTEXT"
    - "rag_chunks state machine transitions all owned by RPCs in supabase/migrations/20260519000012_rag_chunk_transition_rpcs.sql per [[feedback_status_machine_transition_owner.md]]"
    - "Admin queue UI uses optimistic updates: approve/reject removes row immediately, Toast 'Approved · undo' with 5s undo affordance per UI-SPEC §Interaction Contract"
    - "Keyboard shortcuts per UI-SPEC §A3: A approve, R reject, E edit, J/K next/prev, ?-help modal"
  artifacts:
    - "src/components/admin/rag/RagQueuePage.tsx"
    - "src/components/admin/rag/QueueDetailPane.tsx"
    - "src/components/admin/rag/RejectReasonSheet.tsx"
    - "src/components/admin/rag/EditChunkModal.tsx"
    - "src/components/admin/rag/RetractChunkModal.tsx"
    - "src/lib/admin/rag/chunk-api.ts"
    - "supabase/migrations/20260519000012_rag_chunk_transition_rpcs.sql"
    - "supabase/migrations/20260519000013_rag_sla_alert_cron.sql"
    - "src/lib/rag/__tests__/publish-flow.test.ts"
    - "src/lib/rag/__tests__/reject-reasons.test.ts"
    - "src/lib/rag/__tests__/sla-backlog.test.ts"
    - "src/lib/rag/__tests__/retract.test.ts"
    - "src/lib/rag/__tests__/erratum-flag.test.ts"
    - "e2e/admin-rag-queue.spec.ts"
  key_links:
    - "RagQueuePage → chunk-api.ts → SECURITY DEFINER state-transition RPCs → rag_chunks UPDATE + log_admin_action()"
    - "Plan 50-05 summarize Edge Fn → invokes rag_chunk_publish_tier_a() post-summarize for Tier-A auto-publish (D-15)"
    - "SLA cron → backlog count → Slack alert (placeholder until P50-09)"
    - "Plan 50-07 retrieval Edge Fn filters WHERE retracted_at IS NULL"
---

<objective>
Ship the admin review queue UI + chunk state-transition RPCs + SLA backlog monitor. The queue is UI-SPEC §A3 — highest-density admin surface with side-by-side source-text vs extracted-quote pane, optimistic approve/reject, keyboard shortcuts, edit modal, retract modal, and reject-reason sheet with 6-option taxonomy. Adds 5 SECURITY DEFINER RPCs that own the rag_chunks state machine (queued → approved | rejected | retracted | re-queued).

Purpose: this is where Karsten and the clinical advisor spend their reviewing time. UI-SPEC explicitly flags this as "HIGHEST UX SCRUTINY". Without it, the entire pipeline halts at "queued" status and Plan 50-07 has nothing to embed.
Output: 5 React components + 1 API wrapper + 2 migrations + 5 unit tests + 1 Playwright E2E.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-RESEARCH.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md
@src/components/admin/rag/RagLayout.tsx
@src/components/ui/Modal.tsx
@src/components/ui/Sheet.tsx
@src/components/ui/Toast.tsx

<interfaces>
- Plan 50-01 `rag_chunks` table + state-machine columns + 6-reason ENUM + 5-status ENUM.
- Plan 50-02 `RagLayout` sub-route `/admin/rag/queue` placeholder; this plan replaces it with the real component.
- Plan 50-02 admin RPC wrapper convention (`rag-api.ts`); this plan adds `chunk-api.ts` mirror.
- Plan 50-03 server-side `captureRagEvent('rag_chunk_reviewed' | 'rag_chunk_published' | 'rag_chunk_retracted')` — called by RPCs via PERFORM net.http_post pattern (simpler: client-side capture immediately after RPC returns; defer server-side capture optimization to v1.4).
- Plan 50-05 summarize pipeline writes summary + quote_blocks; this plan reads them.
- UI-SPEC §A3 + §Copywriting Contract + §Interaction Contract + §Accessibility Contract.
- `react-markdown` + `dompurify` already in stack per CLAUDE.md context.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write state-machine RPCs migration</name>
  <files>supabase/migrations/20260519000012_rag_chunk_transition_rpcs.sql</files>
  <read_first>supabase/migrations/20260519000003_rag_chunks_table.sql, supabase/migrations/20260519000010_rag_admin_rpcs.sql (RPC convention from Plan 50-02)</read_first>
  <action>
    Write `supabase/migrations/20260519000012_rag_chunk_transition_rpcs.sql` with 5 SECURITY DEFINER RPCs. Every RPC starts with `IF NOT public._rag_is_admin() THEN RAISE EXCEPTION 'not authorized'; END IF;` and includes `PERFORM public.log_admin_action(...)` in a defensive BEGIN/EXCEPTION block. All set `search_path = extensions, public, pg_temp`.

    1. `public.rag_chunk_approve(p_chunk_id uuid) RETURNS void` — UPDATE rag_chunks SET status='approved', published_at=now(), reviewed_by=auth.uid(), reviewed_at=now() WHERE id = p_chunk_id AND status IN ('queued','re-queued'); fail with RAISE if no rows.
    2. `public.rag_chunk_reject(p_chunk_id uuid, p_reason text) RETURNS void` — validate p_reason in 6-value enum; UPDATE SET status='rejected', reject_reason=p_reason, reviewed_by=auth.uid(), reviewed_at=now().
    3. `public.rag_chunk_edit_and_approve(p_chunk_id uuid, p_new_summary text, p_new_quote_blocks jsonb) RETURNS void` — UPDATE summary + quote_blocks + transition to approved with published_at = now().
    4. `public.rag_chunk_retract(p_chunk_id uuid, p_reason text) RETURNS void` — UPDATE SET status='retracted', retracted_at=now(), retracted_reason=p_reason, reviewed_by=auth.uid(); requires chunk to be currently published (status='approved').
    5. `public.rag_chunk_publish_tier_a() RETURNS int` — UPDATE rag_chunks SET status='approved', published_at=now(), reviewed_at=now() WHERE status='queued' AND source_tier='A'; RETURNS rowcount. This is the auto-publish helper invoked by Plan 50-05 summarize pipeline AFTER summarization completes per D-15. (Note: caller is `service_role` from the summarize Edge Fn, not `auth.uid()`; gate accordingly with a different RPC OR allow service_role bypass via a check `IF auth.uid() IS NULL OR public._rag_is_admin() THEN ... END IF;`.)
    6. `public.rag_chunk_set_erratum_flag(p_chunk_id uuid, p_flag boolean) RETURNS void` — D-21 manual toggle.

    Every RPC: grant execute to authenticated (RLS + the gate guard handle authz).
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && supabase db push --linked --dry-run | grep -v Skipping && supabase db query --linked 'SELECT proname FROM pg_proc WHERE proname LIKE \\'rag_chunk_%\\' ORDER BY proname;'"
    </automated>
  </verify>
  <acceptance_criteria>
    - 6 RPCs defined; every one has the gate guard, search_path set, log_admin_action call (defensive).
    - rag_chunk_publish_tier_a bypasses auth.uid() check when invoked by service_role per D-15.
    - State transitions enforce source state (e.g., approve only from queued/re-queued; retract only from approved).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write chunk-api.ts client wrapper</name>
  <files>src/lib/admin/rag/chunk-api.ts</files>
  <read_first>src/lib/admin/rag/rag-api.ts (Plan 50-02 wrapper pattern)</read_first>
  <action>
    Write `src/lib/admin/rag/chunk-api.ts`:
    - Typed wrappers for 6 RPCs: `ragChunkApprove(chunkId)`, `ragChunkReject(chunkId, reason)`, `ragChunkEditAndApprove(chunkId, summary, quoteBlocks)`, `ragChunkRetract(chunkId, reason)`, `ragChunkSetErratumFlag(chunkId, flag)`, plus `ragQueueList(opts: {tier?: 'A'|'B'|'C', tag?: string, sourceId?: string, limit?: number, offset?: number})` (SELECT from rag_chunks WHERE status IN ('queued','re-queued')).
    - All return `{ data, error }` shape per project convention.
    - After successful approve/reject/retract: client-side `posthog.capture('rag_chunk_reviewed', {chunk_id, source_tier, action, reject_reason?, queue_age_hours})` per D-35.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && npm run typecheck && npm run lint -- src/lib/admin/rag/chunk-api.ts"</automated>
  </verify>
  <acceptance_criteria>
    - 6 typed wrappers + 1 list function exported.
    - posthog.capture called with correctly-shaped properties per D-35.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build RagQueuePage + QueueDetailPane (side-by-side source/quote)</name>
  <files>src/components/admin/rag/RagQueuePage.tsx, src/components/admin/rag/QueueDetailPane.tsx</files>
  <read_first>.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (§A3 full spec, §Interaction Contract, §Accessibility Contract), src/components/ui/Sheet.tsx</read_first>
  <action>
    Write `RagQueuePage.tsx`:
    - Two-column desktop layout via `lg:grid-cols-[7fr_5fr]` (UI-SPEC §A3 single-component responsive rule — NO separate Desktop/Mobile components).
    - Left column: chunk list (sticky header `Backlog: {N} items` indicator — turns warning at >100). Filter pills `All | Tier B | Tier C | by tag | by source` per UI-SPEC §A3.
    - Each chunk row: Card variant="interactive" span={12} with source name + TierBadge + topic tag Pill + 1-line summary truncated + `queued {relative-time}` + per-row `Approve` Button (size="sm") + IconButton for reject (opens RejectReasonSheet).
    - Right column: QueueDetailPane (sticky) — shows selected chunk's full source markdown rendered via react-markdown + dompurify side-by-side with extracted quote_blocks (UI-SPEC §A3 side-by-side block — FIRST-CLASS). At <lg, sheets from bottom.
    - Keyboard handlers: A/R/E/J/K/Shift+? per UI-SPEC §A3.
    - Optimistic UI: after approve/reject, remove row from local state immediately, show Toast with 5s undo affordance.
    - Empty state: `EmptyState` with CheckCheck icon + UI-SPEC §A3 copy verbatim.

    Write `QueueDetailPane.tsx`:
    - Props: `{ chunk: RagChunk }`.
    - Header strip: source name (external link) · TierBadge · `Scraped {date}` · canonical URL link.
    - Side-by-side block:
      - Left half (label `SOURCE TEXT` uppercase eyebrow): `<ReactMarkdown rehypePlugins={[rehypeRaw]} components={{ /* sanitize */ }}>` then pass through DOMPurify before render. Background `bg-[var(--color-surface-soft)]` (UI-SPEC §A3 background diff).
      - Right half (label `EXTRACTED QUOTE` uppercase eyebrow accent): map over chunk.quote_blocks → each quote shows verbatim text + `Badge` with kind ('dose'|'indication'|'contraindication'|'adverse-event') + gloss if present (smaller text-secondary).
      - At <lg: stack vertically.
    - Summary block (full-width, below side-by-side): chunk.summary rendered as plain text.
    - Action row (sticky bottom): Reject (secondary, opens RejectReasonSheet) · Edit (ghost, opens EditChunkModal) · Approve (primary). Keyboard shortcuts shown as `<kbd>` chips: A, R, E, J/K.
    - DOMPurify allowlist per RESEARCH pitfall 20: `p, a, ul, ol, li, code, pre, strong, em, blockquote, mark, h2, h3, h4` only; no img, script, iframe, inline styles.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && npm run typecheck && npm run lint -- src/components/admin/rag/RagQueuePage.tsx src/components/admin/rag/QueueDetailPane.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; typecheck + lint pass.
    - Single component with `lg:` modifier classes (no separate Desktop/Mobile components per UI-SPEC).
    - DOMPurify allowlist enforced (grep: no `img`, `script`, `iframe` in sanitize config).
    - Keyboard shortcuts wired (A/R/E/J/K).
    - Optimistic UI + 5s undo Toast per UI-SPEC §Interaction Contract.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Build RejectReasonSheet + EditChunkModal + RetractChunkModal</name>
  <files>src/components/admin/rag/RejectReasonSheet.tsx, src/components/admin/rag/EditChunkModal.tsx, src/components/admin/rag/RetractChunkModal.tsx</files>
  <read_first>src/components/ui/Sheet.tsx, src/components/ui/Modal.tsx, .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (§A3, §Copywriting)</read_first>
  <action>
    `RejectReasonSheet.tsx`:
    - Bottom Sheet with 6 Pills (UI-SPEC §A3 taxonomy + UI-SPEC §Copywriting reject labels): Off-topic, Factually wrong, Off-label, Low quality, Duplicate, Safety concern.
    - Tap any Pill → single-click reject (calls ragChunkReject + closes sheet + Toast).
    - Danger-colored Pills: Factually wrong, Off-label, Safety concern.
    - `role="radiogroup"` semantics + arrow-key cycling per UI-SPEC §Accessibility.

    `EditChunkModal.tsx`:
    - Modal with title "Edit chunk".
    - Textarea for summary (default chunk.summary, 4 rows).
    - JSON editor for quote_blocks (simple textarea with monospace + JSON.parse on save with error display).
    - Save → calls ragChunkEditAndApprove + closes + Toast `Approved · undo`.

    `RetractChunkModal.tsx`:
    - Modal with UI-SPEC §Copywriting retract-confirmation copy verbatim: "Retract this chunk?" body "Chunk removes from RAG retrieval immediately. Already-sent newsletter inclusions stay sent. Action is logged." actions `Cancel` · `Retract chunk` (danger).
    - Textarea for `reason` (required).
    - Submit → calls ragChunkRetract.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && npm run typecheck && npm run lint -- src/components/admin/rag/RejectReasonSheet.tsx src/components/admin/rag/EditChunkModal.tsx src/components/admin/rag/RetractChunkModal.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist; typecheck + lint pass.
    - RejectReasonSheet has 6 Pills with correct danger-tone mapping.
    - EditChunkModal validates JSON before submit.
    - RetractChunkModal uses UI-SPEC verbatim copy.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Write SLA backlog monitor cron migration + Slack-alert placeholder</name>
  <files>supabase/migrations/20260519000013_rag_sla_alert_cron.sql</files>
  <read_first>supabase/migrations/20260519000011_rag_scrape_cron.sql (cron pattern), .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md (D-18)</read_first>
  <action>
    Write `supabase/migrations/20260519000013_rag_sla_alert_cron.sql`:
    - Function `public.rag_sla_backlog_count() RETURNS TABLE(tier text, count bigint, past_sla bigint) LANGUAGE sql STABLE SECURITY DEFINER SET search_path = extensions, public, pg_temp AS $$ SELECT 'B' AS tier, count(*) AS count, count(*) FILTER (WHERE scraped_at < now() - interval '7 days') AS past_sla FROM public.rag_chunks WHERE status IN ('queued','re-queued') AND source_tier = 'B' UNION ALL SELECT 'C', count(*), count(*) FILTER (WHERE scraped_at < now() - interval '14 days') FROM public.rag_chunks WHERE status IN ('queued','re-queued') AND source_tier = 'C' $$;`
    - GRANT EXECUTE TO authenticated.
    - Cron entry `rag-sla-alert-tick` schedule `0 * * * *` (hourly) that calls an Edge Fn `rag-sla-alert` (placeholder — Plan 50-09 finalizes Slack send; for now writes to a `rag_sla_alerts_log` table and console.logs warning). For Plan 50-06 scope: ship the migration + cron entry; Edge Fn body is a stub that just logs `await captureRagEvent('rag_backlog_above_threshold', {...})` (NOT in registry — defer event registration to Plan 50-09; for now use console.error).
    - Create supporting table: `CREATE TABLE IF NOT EXISTS public.rag_sla_alerts_log (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), checked_at timestamptz NOT NULL DEFAULT now(), tier_b_backlog int NOT NULL, tier_c_backlog int NOT NULL, past_sla_b int NOT NULL, past_sla_c int NOT NULL, alert_sent boolean NOT NULL DEFAULT false);`
  </action>
  <verify>
    <automated>bash -c "cd leanshot && supabase db push --linked --dry-run | grep -v Skipping && supabase db query --linked 'SELECT * FROM public.rag_sla_backlog_count();'"</automated>
  </verify>
  <acceptance_criteria>
    - Migration exists; rag_sla_backlog_count function works (returns 2 rows: tier B + tier C).
    - rag_sla_alerts_log table exists.
    - Cron entry `rag-sla-alert-tick` scheduled hourly.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Write 5 vitest suites + 1 Playwright E2E</name>
  <files>src/lib/rag/__tests__/publish-flow.test.ts, src/lib/rag/__tests__/reject-reasons.test.ts, src/lib/rag/__tests__/sla-backlog.test.ts, src/lib/rag/__tests__/retract.test.ts, src/lib/rag/__tests__/erratum-flag.test.ts, e2e/admin-rag-queue.spec.ts</files>
  <read_first>src/lib/rag/__tests__/rls-matrix.test.ts (impersonation fixture), e2e (existing playwright pattern)</read_first>
  <action>
    `publish-flow.test.ts`:
    - `it('Tier-A chunks auto-publish via rag_chunk_publish_tier_a RPC')` — D-15.
    - `it('Tier-B chunks remain queued after rag_chunk_publish_tier_a')`.
    - `it('Tier-C chunks remain queued after rag_chunk_publish_tier_a')`.
    - `it('rag_chunk_approve transitions queued → approved with published_at populated')`.
    - `it('rag_chunk_approve fails for already-rejected chunk')`.
    - RLS: `it('regular user cannot call rag_chunk_approve')`.

    `reject-reasons.test.ts`:
    - `it('all 6 reject reasons accepted')` — D-16; iterate over taxonomy.
    - `it('invalid reject reason throws')`.
    - `it('source with ≥5 rejects in 30d is flagged in /admin/rag/sources view query')` — D-16 trust-downgrade signal.

    `sla-backlog.test.ts`:
    - `it('rag_sla_backlog_count returns 0 for B and C when queue empty')`.
    - `it('past_sla_b counts chunks queued > 7 days')` — D-18.
    - `it('past_sla_c counts chunks queued > 14 days')`.

    `retract.test.ts`:
    - `it('rag_chunk_retract sets retracted_at + retracted_reason')` — D-20.
    - `it('retracted chunk excluded from approved-list query')`.
    - `it('rag_chunk_retract requires chunk currently approved')`.
    - `it('audit log row written on retract')`.

    `erratum-flag.test.ts`:
    - `it('rag_chunk_set_erratum_flag toggles erratum_flag column')` — D-21.

    `e2e/admin-rag-queue.spec.ts` (Playwright):
    - Use `[[reference_playwright_state_seeding]]` pattern (addInitScript to seed authenticated admin session).
    - Seed: super-admin user + 3 queued Tier-B chunks.
    - Test: visit `/admin/rag/queue`, see 3 rows in list. Click first row, see QueueDetailPane render with side-by-side source/quote. Press `A` keyboard shortcut, expect row to disappear and Toast 'Approved · undo' to appear.
    - Test: click second row, click Reject IconButton, sheet appears with 6 pills, click 'Off-topic', expect row disappears.
    - Use `PLAYWRIGHT_RUN_P50_RAG_QUEUE=1` env var to gate the test (per [[reference_playwright_conditional_project_argv]]).
  </action>
  <verify>
    <automated>bash -c "cd leanshot && vitest run src/lib/rag/__tests__/publish-flow.test.ts src/lib/rag/__tests__/reject-reasons.test.ts src/lib/rag/__tests__/sla-backlog.test.ts src/lib/rag/__tests__/retract.test.ts src/lib/rag/__tests__/erratum-flag.test.ts && PLAYWRIGHT_RUN_P50_RAG_QUEUE=1 npx playwright test e2e/admin-rag-queue.spec.ts"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 vitest suites green.
    - Playwright E2E green; gated by env var.
    - D-15, D-16, D-18, D-20, D-21 each covered.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `vitest run src/lib/rag/__tests__/` + `npx playwright test e2e/admin-rag-queue.spec.ts` — green.
- `supabase db push --linked --dry-run` — zero Skipping.
- Manual: load /admin/rag/queue with seeded Tier-B chunk, exercise side-by-side pane + keyboard shortcuts.
</verification>
