---
phase: 50-admin-curated-rag-knowledge-base-peptide-topic-research-scra
plan: 02
type: execute
wave: 1
depends_on: [50-01]
files_modified:
  - src/lib/admin/modules.ts
  - src/lib/admin/rag/rag-api.ts
  - src/components/admin/rag/RagLayout.tsx
  - src/components/admin/rag/RagTopicsPage.tsx
  - src/components/admin/rag/RagSourcesPage.tsx
  - src/components/admin/rag/TierBadge.tsx
  - src/components/admin/rag/HealthBadge.tsx
  - src/components/admin/rag/CostBar.tsx
  - src/components/admin/rag/AddTopicSheet.tsx
  - src/components/admin/rag/AddSourceSheet.tsx
  - supabase/migrations/20260519000010_rag_admin_rpcs.sql
  - src/lib/rag/__tests__/topic-crud.test.ts
  - src/lib/rag/__tests__/topic-crud-rls.test.ts
  - src/lib/rag/__tests__/topic-audit.test.ts
  - src/lib/rag/__tests__/soft-delete.test.ts
  - src/lib/rag/__tests__/telemetry-rollup.test.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-08: Topic shape (query, tag) is enforced by RPC `admin.rag_topic_create(p_query text, p_tag text, p_posture, p_cadence)`; UI form requires both fields non-empty"
    - "D-10: Admin UI exposes single-row CRUD only — no bulk import button, no CSV upload, no template clone surfaces"
    - "D-11: /admin/rag/telemetry shows per-topic: docs ingested, RAG hits 7d (placeholder until retrieval ships in Plan 50-07), tip impressions 7d, tip clicks 7d, newsletter inclusions 7d, source-tier mix bar"
    - "D-12: All admin RPCs gate on is_admin_at_least('superadmin'); non-super admins see read-only UI (Add buttons hidden)"
    - "D-13: Soft-delete: admin clicks Archive → RPC sets deleted_at; topic disappears from default list view; appears in 'Archived' filter; Restore button calls RPC to clear deleted_at"
    - "D-14: log_admin_action() called inside every CRUD RPC; rag_topic_audit row written via Plan 50-01 trigger"
    - "ADMIN_MODULES manifest in src/lib/admin/modules.ts has 'rag-knowledge-base' entry with key 'rag', label 'Knowledge Base', route '/admin/rag', icon: BookOpenCheck (lucide-react), lazy import, flagKey 'admin_rag_kb', minRole 'admin'"
    - "TierBadge wraps Badge primitive (no new primitive); HealthBadge wraps Badge with pulse prop on 'failing' only; CostBar is inline 4px div with rounded-pill background"
  artifacts:
    - "src/lib/admin/modules.ts"
    - "src/lib/admin/rag/rag-api.ts"
    - "src/components/admin/rag/RagLayout.tsx"
    - "src/components/admin/rag/RagTopicsPage.tsx"
    - "src/components/admin/rag/RagSourcesPage.tsx"
    - "src/components/admin/rag/TierBadge.tsx"
    - "src/components/admin/rag/HealthBadge.tsx"
    - "src/components/admin/rag/CostBar.tsx"
    - "src/components/admin/rag/AddTopicSheet.tsx"
    - "src/components/admin/rag/AddSourceSheet.tsx"
    - "supabase/migrations/20260519000010_rag_admin_rpcs.sql"
  key_links:
    - "ADMIN_MODULES (P24 manifest) → RagLayout (lazy-loaded) → 5 sub-pages"
    - "rag-api.ts wraps SECURITY DEFINER RPCs; client side fetches via supabase.rpc(...)"
---

<objective>
Ship the admin UI shell + CRUD surface for topics + sources + telemetry placeholders. Registers Phase 50 as a new Phase 24 admin module. Adds 8 SECURITY DEFINER RPCs (topic/source create/update/soft-delete/restore × tier-edit) all gated on `is_admin_at_least('superadmin')` and all calling `log_admin_action()` for the audit trail. Telemetry rollup function returns per-topic 7d aggregates (placeholders for events not yet emitting until Wave 3).

Purpose: foundation for Karsten authoring the 20 seed topics per D-36 ops requirement. Without this UI, no scraping pipeline can be configured. Without the RPCs, the manifest entry is decorative.
Output: 10 new files + 1 migration with 8 RPCs + 1 telemetry function + 5 vitest suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-RESEARCH.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md
@src/lib/admin/modules.ts
@src/components/admin/AdminLayout.tsx
@src/components/ui/Badge.tsx
@src/components/ui/Pill.tsx
@src/components/ui/Sheet.tsx
@src/components/ui/EmptyState.tsx

<interfaces>
- Phase 24 `ADMIN_MODULES as const … satisfies AdminModule[]` literal — Plan 50-02 inserts ONE entry literally at the end of the array (no full-file rewrite). Per [[project_phase24_shipped]] this array is the manifest source of truth.
- Phase 24 `log_admin_action(action_name, target_user_id, table_name, row_pk, before_jsonb, after_jsonb)` SECDEF — every Plan 50-02 RPC calls this.
- Phase 50 Plan 50-01 tables: `rag_topics`, `rag_sources`, `rag_topic_audit`.
- `src/components/ui/Sheet.tsx` (right-side panel for Add/Edit forms).
- `src/components/ui/Badge.tsx` (TierBadge + HealthBadge thin wrappers).
- UI-SPEC.md sections A1, A2 (topics page + sources page) — copy-driven CTA labels enforced.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 8 SECURITY DEFINER admin RPCs + 1 telemetry function</name>
  <files>supabase/migrations/20260519000010_rag_admin_rpcs.sql</files>
  <read_first>
    supabase/migrations/20260519000001_rag_topics_table.sql,
    supabase/migrations/20260519000002_rag_sources_table_and_seed.sql,
    supabase/migrations/20260519000009_rag_rls_policies.sql
  </read_first>
  <action>
    Write `20260519000010_rag_admin_rpcs.sql` with these 8 RPCs (all `SECURITY DEFINER SET search_path = extensions, public, pg_temp`, all start with `IF NOT public._rag_is_super() THEN RAISE EXCEPTION 'not authorized'; END IF;`, all end with a `PERFORM public.log_admin_action(...)` call when Phase 24 helper exists — wrap in BEGIN/EXCEPTION undefined_function block so missing helper does not break the RPC):

    1. `public.rag_topic_create(p_query text, p_tag text, p_posture text, p_cadence text) RETURNS uuid` — inserts topic, returns id, logs action.
    2. `public.rag_topic_update(p_topic_id uuid, p_query text, p_tag text, p_posture text, p_cadence text) RETURNS void` — captures before-jsonb, updates, logs.
    3. `public.rag_topic_soft_delete(p_topic_id uuid) RETURNS void` — sets `deleted_at = now()`, logs.
    4. `public.rag_topic_restore(p_topic_id uuid) RETURNS void` — sets `deleted_at = NULL`, logs.
    5. `public.rag_source_create(p_name text, p_domain text, p_tier text, p_freshness_window_days int) RETURNS uuid`.
    6. `public.rag_source_update_tier(p_source_id uuid, p_new_tier text) RETURNS void` — logs prior tier in audit before-jsonb.
    7. `public.rag_source_pause(p_source_id uuid, p_reason text) RETURNS void` — sets `paused_at = now()`, `health = 'paused'`.
    8. `public.rag_source_resume(p_source_id uuid) RETURNS void` — sets `paused_at = NULL`, `health = 'ok'`, `consecutive_failures = 0`.

    Telemetry function:
    `CREATE OR REPLACE FUNCTION public.rag_topic_telemetry_7d() RETURNS TABLE(topic_id uuid, tag text, docs_ingested bigint, rag_hits_7d bigint, tip_impressions_7d bigint, tip_clicks_7d bigint, newsletter_inclusions_7d bigint, tier_a_count bigint, tier_b_count bigint, tier_c_count bigint) LANGUAGE sql STABLE SECURITY DEFINER SET search_path = extensions, public, pg_temp AS $$ SELECT t.id, t.tag, count(c.id), 0::bigint, 0::bigint, 0::bigint, 0::bigint, count(c.id) FILTER (WHERE c.source_tier = 'A'), count(c.id) FILTER (WHERE c.source_tier = 'B'), count(c.id) FILTER (WHERE c.source_tier = 'C') FROM public.rag_topics t LEFT JOIN public.rag_chunks c ON c.topic_id = t.id AND c.published_at IS NOT NULL WHERE t.deleted_at IS NULL GROUP BY t.id, t.tag $$;`
    (rag_hits_7d / tip_impressions_7d / etc. return 0 until Plan 50-08 wires server-side capture; placeholder columns satisfy D-11 contract now.)
    `GRANT EXECUTE ON FUNCTION public.rag_topic_telemetry_7d() TO authenticated;`
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && supabase db push --linked --dry-run | grep -v Skipping && supabase db query --linked 'SELECT proname FROM pg_proc WHERE proname LIKE \\'rag_%\\' ORDER BY proname;' | grep -c rag_ | xargs -I{} test {} -ge 9"
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists with 8 RPCs + 1 telemetry function.
    - Every RPC starts with the super-admin gate AND calls `log_admin_action()` defensively (wrapped to no-op when P24 helper missing).
    - `search_path` set on every SECURITY DEFINER.
    - Migration applies dry-run cleanly with zero `Skipping`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register Phase 50 module in ADMIN_MODULES manifest + create RagLayout shell</name>
  <files>src/lib/admin/modules.ts, src/components/admin/rag/RagLayout.tsx</files>
  <read_first>
    src/lib/admin/modules.ts (current manifest shape and existing 12 entries),
    src/components/admin/AdminLayout.tsx (Pattern S1 module loader),
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (Admin shell composition)
  </read_first>
  <action>
    Edit `src/lib/admin/modules.ts`:
    - Append literal manifest entry to the `ADMIN_MODULES` array (no full-file rewrite per [[project_phase24_shipped]]):
      `{ key: 'rag', label: 'Knowledge Base', route: '/admin/rag', icon: BookOpenCheck, lazy: () => import('@/components/admin/rag/RagLayout'), flagKey: 'admin_rag_kb', minRole: 'admin' as const }`
    - Add `import { BookOpenCheck } from 'lucide-react'` at top if not present.
    - Manifest position: end of array (Phase 50 last; Phase 24's 12 modules first per insertion order rule).

    Create `src/components/admin/rag/RagLayout.tsx`:
    - Default export React component.
    - Top-level: nested router using existing admin route convention. 5 sub-routes: `/admin/rag/topics` (RagTopicsPage), `/admin/rag/sources` (RagSourcesPage), `/admin/rag/queue` (placeholder card 'Plan 50-06 ships review queue'), `/admin/rag/telemetry` (renders rag_topic_telemetry_7d() output in a table), `/admin/rag/cost` (placeholder card 'Plan 50-09 finalizes cost dashboard').
    - Left sidebar with the 5 sub-route labels: `Topics`, `Sources`, `Queue`, `Telemetry`, `Cost`. Active route uses accent color per UI-SPEC color rule.
    - Default route redirects to `/admin/rag/topics`.
    - Wrap content area in `<main class="max-w-screen-xl p-6 lg:p-8">` per UI-SPEC §A1 container rule.
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && grep -q \"key: 'rag'\" src/lib/admin/modules.ts && grep -q \"BookOpenCheck\" src/lib/admin/modules.ts && test -f src/components/admin/rag/RagLayout.tsx && npm run typecheck && npm run lint -- src/lib/admin/modules.ts src/components/admin/rag/RagLayout.tsx"
    </automated>
  </verify>
  <acceptance_criteria>
    - `ADMIN_MODULES` array has the new `rag` entry literally appended.
    - `RagLayout.tsx` exports a default component with 5 sub-routes wired.
    - Active-route accent indicator follows UI-SPEC §A1 color rule (`--color-primary` accent).
    - `npm run typecheck` and `npm run lint` both green on the changed files.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build TierBadge, HealthBadge, CostBar primitives</name>
  <files>src/components/admin/rag/TierBadge.tsx, src/components/admin/rag/HealthBadge.tsx, src/components/admin/rag/CostBar.tsx</files>
  <read_first>
    src/components/ui/Badge.tsx (existing primitive shape and tones),
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (Color section: tier color contract, health badge contract, cost-bar contract)
  </read_first>
  <action>
    Write `src/components/admin/rag/TierBadge.tsx`:
    - `type TierBadgeProps = { tier: 'A' | 'B' | 'C' }`.
    - Component returns `<Badge tone={tier === 'C' ? 'neutral' : 'info'}>Tier {tier}</Badge>` with `aria-label={`Tier ${tier} source${tier === 'A' ? ' (highest trust)' : tier === 'B' ? ' (established health source)' : ' (lay press or community)'}`}`.

    Write `src/components/admin/rag/HealthBadge.tsx`:
    - `type HealthBadgeProps = { state: 'ok' | 'paused' | 'failing'; reason?: string }`.
    - Maps: ok → `<Badge tone="success">Healthy</Badge>`, paused → `<Badge tone="warning">Paused{reason ? `: ${reason}` : ''}</Badge>`, failing → `<Badge tone="danger" pulse>Failing</Badge>` (pulse prop is existing Badge feature per UI-SPEC §Color "health badge contract").

    Write `src/components/admin/rag/CostBar.tsx`:
    - `type CostBarProps = { mtdUsd: number; capUsd: number }`.
    - `pct = Math.min(100, (mtdUsd / capUsd) * 100)`; color: `<80 success`, `80-99 warning`, `=100 danger` (per UI-SPEC §Color "cost-cap indicator").
    - Renders `<div role="progressbar" aria-valuenow={pct} aria-valuemin={0} aria-valuemax={100} aria-label={...} className="h-1 rounded-pill bg-[var(--color-surface-elevated)]"><div className={...} style={{width: `${pct}%`}} /></div>`.
    - Below the bar a caption `<p className="text-xs font-mono tabular-nums">${'$'}{mtdUsd.toFixed(2)} of ${'$'}{capUsd.toFixed(2)} MTD · {pct.toFixed(0)}%</p>`.
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && npm run typecheck && npm run lint -- src/components/admin/rag/TierBadge.tsx src/components/admin/rag/HealthBadge.tsx src/components/admin/rag/CostBar.tsx"
    </automated>
  </verify>
  <acceptance_criteria>
    - 3 new files exist, each composing existing primitives only (no new tokens introduced).
    - `TierBadge` renders the correct `aria-label` per tier (UI-SPEC Accessibility Contract).
    - `HealthBadge` only pulses for `failing` state (UI-SPEC: pulse-intent rule).
    - `CostBar` thresholds: <80% success, 80-99% warning, ≥100% danger.
    - Typecheck and lint pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Build RagTopicsPage + AddTopicSheet (table with filter pills, Sheet form)</name>
  <files>src/components/admin/rag/RagTopicsPage.tsx, src/components/admin/rag/AddTopicSheet.tsx, src/lib/admin/rag/rag-api.ts</files>
  <read_first>
    src/components/admin/rag/RagLayout.tsx,
    src/components/ui/Sheet.tsx,
    src/components/ui/Pill.tsx,
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (§A1, §Copywriting)
  </read_first>
  <action>
    Write `src/lib/admin/rag/rag-api.ts` with typed wrappers for all 8 RPCs from Task 1 + the telemetry function. Use the project's existing `supabase` client. Functions named `ragTopicCreate({query, tag, posture, cadence})` etc. Returns typed promises. On error, returns `{ error: PostgrestError | null, data: T | null }` shape consistent with other `admin-api.ts` modules in the codebase.

    Write `src/components/admin/rag/RagTopicsPage.tsx`:
    - Fetches `select * from public.rag_topics where deleted_at is null` via `supabase.from('rag_topics').select('*').is('deleted_at', null)`.
    - Renders header row: `Topics ({N})` display heading + `Add topic` primary CTA (right-aligned) per UI-SPEC §A1.
    - Filter bar of `Pill` filters: `All | Curated | Open-web | All cadences | All tags`. (Tag is a multi-select dropdown for v1; defer multi-select if time-boxed — single select acceptable.)
    - Table with columns from UI-SPEC §A1: `Topic query` (truncated, max-w-prose) · `Tag` (Pill) · `Mode` (Pill: curated/open-web) · `Cadence` · `Docs` (`font-mono`) · `RAG hits 7d` (`font-mono`) · `Last scraped` (relative time) · Actions (kebab menu).
    - Row click opens a `Sheet` on the right with topic detail (read-only view + Edit/Archive buttons).
    - Empty state: `<EmptyState />` with Database lucide icon, heading "No topics yet", body and CTA per UI-SPEC §A1 Copywriting row.
    - At `<md` breakpoint, table collapses to per-row stacked label+value via `md:` modifier classes (UI-SPEC responsive rule).

    Write `src/components/admin/rag/AddTopicSheet.tsx`:
    - Right-side `Sheet` with form: `Input` query (placeholder "e.g., tirzepatide muscle loss"), `Input` tag (placeholder "e.g., muscle-loss"), `Select` posture (`curated`/`open-web`), `Select` cadence (`daily`/`weekly`/`monthly`/`manual`).
    - Submit calls `ragTopicCreate(...)`; on success closes sheet and triggers list refresh + Toast "Topic added".
    - Validate query non-empty + tag matches `[a-z0-9-]+` regex (kebab-case enforcement per CONTEXT D-08).
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && npm run typecheck && npm run lint -- src/components/admin/rag/RagTopicsPage.tsx src/components/admin/rag/AddTopicSheet.tsx src/lib/admin/rag/rag-api.ts && vitest run src/lib/rag/__tests__/topic-crud.test.ts"
    </automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist; `rag-api.ts` exports all 9 wrapper functions.
    - `RagTopicsPage` renders table per UI-SPEC §A1 columns + filter pills + Add topic CTA.
    - Empty state uses `EmptyState` primitive with the exact copy from UI-SPEC §A1 Copywriting row.
    - `AddTopicSheet` validates tag regex and posts to `ragTopicCreate`.
    - `topic-crud.test.ts` covers create → list returns new topic → soft-delete → list excludes; restore → list includes again.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Build RagSourcesPage + AddSourceSheet</name>
  <files>src/components/admin/rag/RagSourcesPage.tsx, src/components/admin/rag/AddSourceSheet.tsx</files>
  <read_first>
    src/components/admin/rag/RagTopicsPage.tsx (mirror pattern),
    src/components/admin/rag/TierBadge.tsx,
    src/components/admin/rag/HealthBadge.tsx,
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (§A2)
  </read_first>
  <action>
    Write `src/components/admin/rag/RagSourcesPage.tsx`:
    - Fetches `select *, (select count(*) from public.rag_topics t join public.rag_chunks c on c.topic_id = t.id and c.source_id = rag_sources.id where t.deleted_at is null) as topics_using, (select count(*) from public.rag_chunks where source_id = rag_sources.id and status = 'rejected' and reviewed_at > now() - interval '30 days') as rejects_30d from public.rag_sources order by tier, name;` (use Postgres rich SELECT to compute rollups inline).
    - Header: `Sources ({N})` + CTA `Add source`.
    - Table columns per UI-SPEC §A2: `Source name` (linked external `<a href={domain}>` with new-tab) · `Domain` (`font-mono`) · `Trust tier` (`TierBadge`) · `Health` (`HealthBadge`, pulse on failing only) · `Topics using` (count) · `Rejects 30d` (count, `text-[var(--color-danger)]` if ≥5 per D-16) · `Last scrape` (relative time) · Actions (kebab: Edit tier, Pause/Resume, Remove).
    - Empty state: `EmptyState` with Globe lucide icon + UI-SPEC §A2 copy.

    Write `src/components/admin/rag/AddSourceSheet.tsx`:
    - Form: Input name, Input domain (regex `^[a-z0-9.-]+\.[a-z]{2,}$`), Select tier (A/B/C), Input freshness_window_days (default per tier).
    - Submit calls `ragSourceCreate`.
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && npm run typecheck && npm run lint -- src/components/admin/rag/RagSourcesPage.tsx src/components/admin/rag/AddSourceSheet.tsx"
    </automated>
  </verify>
  <acceptance_criteria>
    - 2 files exist; UI-SPEC §A2 columns rendered.
    - Rejects-30d count colored danger when ≥ 5 (D-16 trust-tier downgrade flag visualization).
    - Empty state copy matches UI-SPEC §A2 Copywriting row.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Write topic-crud + topic-crud-rls + topic-audit + soft-delete + telemetry-rollup vitest suites</name>
  <files>src/lib/rag/__tests__/topic-crud.test.ts, src/lib/rag/__tests__/topic-crud-rls.test.ts, src/lib/rag/__tests__/topic-audit.test.ts, src/lib/rag/__tests__/soft-delete.test.ts, src/lib/rag/__tests__/telemetry-rollup.test.ts</files>
  <read_first>
    src/lib/rag/__tests__/rls-matrix.test.ts (impersonation fixture pattern from Plan 50-01),
    src/lib/admin/rag/rag-api.ts
  </read_first>
  <action>
    Write 5 vitest files. Use the file-scoped TEST_SLUG_PREFIX pattern from [[feedback_rls_per_file_slug_prefix]] and the impersonation fixture from [[reference_rls_fixture_gotruechient_flake]].

    `topic-crud.test.ts`:
    - `it('create returns uuid')`, `it('update modifies query+tag')`, `it('list returns active topics ordered by created_at desc')`, `it('soft-delete sets deleted_at')`, `it('restore clears deleted_at')`, `it('no bulk import surface in rag-api')` (assert `rag-api.ts` exports do NOT include any function whose name matches /bulk|csv|import/i).

    `topic-crud-rls.test.ts`:
    - `it('regular authenticated user cannot create topic')`, `it('plain admin cannot create topic')`, `it('superadmin can create topic')`, `it('regular user cannot select topics')`, `it('plain admin can select topics')`.

    `topic-audit.test.ts`:
    - `it('rag_topic_audit row written on create')`, `it('rag_topic_audit captures before+after on update')`, `it('rag_topic_audit row written on soft-delete with action=delete')`, `it('rag_topic_audit row written on restore with action=restore')`.

    `soft-delete.test.ts`:
    - `it('soft-deleted topic excluded from default list')`, `it('soft-deleted topic chunks remain in rag_chunks')`, `it('restore makes topic visible again')`, `it('retrieval Edge Fn placeholder query ignores deleted_at flag')` (skip with `it.skip(... [DEFERRED until Plan 50-07 lands], ...)`).

    `telemetry-rollup.test.ts`:
    - `it('rag_topic_telemetry_7d returns one row per active topic')`, `it('docs_ingested counts published chunks per topic')`, `it('tier mix sums to docs_ingested')`, `it('placeholder columns rag_hits_7d, tip_impressions_7d, tip_clicks_7d, newsletter_inclusions_7d return 0 until Plan 50-08')`.
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && vitest run src/lib/rag/__tests__/topic-crud.test.ts src/lib/rag/__tests__/topic-crud-rls.test.ts src/lib/rag/__tests__/topic-audit.test.ts src/lib/rag/__tests__/soft-delete.test.ts src/lib/rag/__tests__/telemetry-rollup.test.ts"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files exist with file-scoped TEST_SLUG_PREFIX.
    - `topic-crud.test.ts` asserts the no-bulk-import invariant (D-10).
    - `topic-crud-rls.test.ts` covers the 3-role matrix (regular/admin/superadmin) per D-12.
    - `topic-audit.test.ts` covers all 4 audit actions (create/update/delete/restore) per D-14.
    - `soft-delete.test.ts` defers retrieval test to Plan 50-07 cleanly per [[reference_vitest_skip_fixme]].
    - Telemetry test asserts placeholder columns return 0 (D-11 honest contract — telemetry surface exists, real counts arrive in Plan 50-08).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run typecheck && npm run lint && vitest run src/lib/rag/__tests__/ && supabase db push --linked --dry-run` — all green.
- Manual: open `/admin/rag/topics` in dev, add a topic, see it in the table, soft-delete it, restore it, see it again.
- ADMIN_MODULES manifest has exactly one new entry; no manifest entries removed or reordered.
</verification>
