---
phase: 50-admin-curated-rag-knowledge-base-peptide-topic-research-scra
plan: 08
type: execute
wave: 3
depends_on: [50-03, 50-07]
files_modified:
  - src/components/dashboard/ai/CitationMarker.tsx
  - src/components/dashboard/ai/CitationPopover.tsx
  - src/components/dashboard/ai/AIChatPanel.tsx
  - src/components/dashboard/cards/TipOfTheDayCard.tsx
  - src/lib/rag/retrieve-client.ts
  - src/lib/rag/tip-rotation.ts
  - src/lib/i18n/rag-strings.ts
  - supabase/functions/_shared/server-rag-events.ts
  - src/components/dashboard/ai/__tests__/CitationPopover.test.tsx
  - src/components/dashboard/cards/__tests__/TipOfTheDayCard.test.tsx
  - src/lib/i18n/__tests__/rag-strings.test.ts
  - src/lib/rag/__tests__/tip-rotation.test.ts
  - e2e/coach-citations.spec.ts
  - e2e/tip-of-day.spec.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-22 MVP cut: AI coach citations + dashboard 'Tip of the day' card ship in this MUST-have wave; newsletter + Research Hub deferred to Plan 50-09 (wave 4 / stretch)"
    - "D-23: inline footnote markers [1] [2] rendered as Badge tone=info clickable; tap opens Popover (≥md) or bottom Sheet (<md); content per UI-SPEC §B1 (source title, TierBadge, 1-line summary, 'as of YYYY-MM-DD' mono, 'Open source ↗', disclaimer)"
    - "D-24: TipOfTheDayCard renders as Card span={6} on desktop, full-width on mobile; rotates daily at user's local midnight; manual reroll via RotateCcw IconButton; topic-tag filter aligns to user's drug + active themes"
    - "D-24: when no eligible chunks for user's drug+themes, fallback to generic Tier-A chunk for the drug; if none, hide card entirely (no placeholder)"
    - "D-32: stale chunks render 'as of YYYY-MM-DD' badge with subtle warning tone (UI-SPEC freshness UI), still surface-able but visibly de-emphasized"
    - "D-33: 'rag.attribution' and 'rag.disclaimer' i18n keys defined in src/lib/i18n/rag-strings.ts; t() shim returns English literal until Phase 32 ships full i18n; same key used by coach card, tip card, newsletter, Research Hub (Plan 50-09)"
    - "D-34: rag_tip_impression, rag_tip_clicked, rag_citation_clicked, rag_hub_pageview captured server-side via supabase.functions.invoke('server-rag-event-relay', {...}) → posthog-server.ts captureRagEvent; ad-block immune per P24"
    - "Disclaimer microcopy 'Not medical advice — consult your clinician' rendered on every surface (coach card, tip card)"
    - "AI Coach citation: when assistant message includes citation refs [^cid:UUID]^ pattern, CitationMarker component renders [N] superscript; popover fetches chunk metadata via rag-retrieve Edge Fn (or direct chunk lookup endpoint if available)"
  artifacts:
    - "src/components/dashboard/ai/CitationMarker.tsx"
    - "src/components/dashboard/ai/CitationPopover.tsx"
    - "src/components/dashboard/cards/TipOfTheDayCard.tsx"
    - "src/lib/rag/retrieve-client.ts"
    - "src/lib/rag/tip-rotation.ts"
    - "src/lib/i18n/rag-strings.ts"
    - "supabase/functions/_shared/server-rag-events.ts"
    - "src/components/dashboard/ai/__tests__/CitationPopover.test.tsx"
    - "src/components/dashboard/cards/__tests__/TipOfTheDayCard.test.tsx"
    - "src/lib/i18n/__tests__/rag-strings.test.ts"
    - "src/lib/rag/__tests__/tip-rotation.test.ts"
    - "e2e/coach-citations.spec.ts"
    - "e2e/tip-of-day.spec.ts"
  key_links:
    - "AIChatPanel.tsx (existing) → augmented citation rendering pass on assistant messages → CitationMarker → CitationPopover"
    - "Dashboard HomeTab.tsx (existing) → adds TipOfTheDayCard span={6} → calls rag-retrieve filtered by user's drug + active themes"
    - "All 4 server-side events route through supabase.functions.invoke('server-rag-event-relay') → captureRagEvent (Plan 50-03)"
---

<objective>
Ship the must-have user-facing surfaces per D-22 MVP cut: AI coach inline citations + expandable popover, and the dashboard "Tip of the day" Bento card. Wires server-side PostHog capture for impressions/clicks. Defines the shared i18n disclaimer strings (rag.attribution, rag.disclaimer) used across all Phase 50 surfaces. Adds a thin server-rag-event-relay Edge Fn so the browser can fire server-side captures without ad-block interference.

Purpose: this is the END-USER PAYOFF of Phase 50. After this plan, a patient on tirzepatide sees inline citations in coach answers AND a daily research tip on their dashboard. Wave 4 (Plan 50-09) is purely additive (newsletter + public hub); user-facing value lands here.
Output: 7 source files + 4 test files + 2 Playwright E2E.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-RESEARCH.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md
@src/components/dashboard/ai/AIChatPanel.tsx
@src/components/dashboard/cards
@src/components/ui/Badge.tsx
@src/components/admin/rag/TierBadge.tsx

<interfaces>
- Plan 50-07 `rag-retrieve` Edge Fn — POST {query, k, filters} → ranked chunks.
- Plan 50-02 `TierBadge` component (reused on citation card + tip card per UI-SPEC §B1, §B2).
- Plan 50-03 `captureRagEvent` server-side helper.
- Existing `AIChatPanel.tsx` — augmented (NOT replaced) with citation-rendering pass on assistant messages. Surgical edit adds a `renderWithCitations` helper.
- Existing dashboard `HomeTab.tsx` / bento grid — appends TipOfTheDayCard.
- Existing Sheet / Modal primitives for mobile fallback.
- UI-SPEC §B1 (coach citation), §B2 (tip card), §Copywriting (attribution + disclaimer).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n strings module + shim</name>
  <files>src/lib/i18n/rag-strings.ts, src/lib/i18n/__tests__/rag-strings.test.ts</files>
  <read_first>.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (§Localization), .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md (D-33)</read_first>
  <action>
    Write `src/lib/i18n/rag-strings.ts`:
    - Export `const RAG_STRINGS_EN = { 'rag.attribution': (vars: {source_name: string; date: string; tier: 'A'|'B'|'C'}) => `${vars.source_name} · As of ${vars.date} · Tier ${vars.tier} · Not medical advice — consult your clinician`, 'rag.disclaimer': () => 'Not medical advice — consult your clinician.', 'rag.tier.A.label': () => 'Tier A', 'rag.tier.B.label': () => 'Tier B', 'rag.tier.C.label': () => 'Tier C', 'rag.tier.A.aria': () => 'Tier A source (highest trust)', 'rag.tier.B.aria': () => 'Tier B source (established health source)', 'rag.tier.C.aria': () => 'Tier C source (lay press or community)', 'rag.empty.queue.heading': () => 'Queue clear', 'rag.empty.queue.body': () => "Nothing to review. Tier-A chunks auto-publish; we'll alert you when Tier B/C items arrive." } as const;`
    - Export `type RagStringKey = keyof typeof RAG_STRINGS_EN;`
    - Export `function t(key: RagStringKey, vars?: Record<string, unknown>): string` — returns `RAG_STRINGS_EN[key](vars as never)`. Until Phase 32 ships full i18n, this is a literal-English shim. Phase 32 will replace the shim with a locale-aware resolver reading `?lang=es` query param.

    Write `__tests__/rag-strings.test.ts`:
    - `it('rag.attribution composes source, date, tier, disclaimer')` — assert exact string per UI-SPEC §Copywriting.
    - `it('rag.disclaimer is the short standalone')`.
    - `it('single key rag.attribution and rag.disclaimer')` — ensures both keys present (D-33 invariant: one shared key).
    - `it('all keys defined')` — iterate enum, no undefined values.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && vitest run src/lib/i18n/__tests__/rag-strings.test.ts && npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - Both keys defined; verbatim composition matches UI-SPEC §Copywriting.
    - Type-safe `t()` helper.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: retrieve-client.ts + tip-rotation.ts + server-rag-events relay</name>
  <files>src/lib/rag/retrieve-client.ts, src/lib/rag/tip-rotation.ts, supabase/functions/_shared/server-rag-events.ts</files>
  <read_first>supabase/functions/rag-retrieve, src/lib/store.ts (user.drug, user.active_themes), supabase/functions/_shared/posthog-server.ts</read_first>
  <action>
    Write `src/lib/rag/retrieve-client.ts`:
    - `export async function ragRetrieve(args: { query: string; k?: number; filters?: { topic_tag?: string; source_tier?: ('A'|'B'|'C')[] } }): Promise<{ results: RagChunkResult[]; count: number }>` — calls supabase.functions.invoke('rag-retrieve', { body }).
    - `export type RagChunkResult = { chunk_id: string; summary: string; quote_blocks: Array<{quote: string; kind: string; gloss?: string}>; canonical_url: string; scraped_at: string; source_tier: 'A'|'B'|'C'; topic_tag: string; source_name: string; source_domain: string; final_score: number; stale: boolean };`
    - `export async function ragChunkById(chunkId: string): Promise<RagChunkResult | null>` — direct lookup wrapper used by CitationPopover when message text references `[^cid:UUID]^` markers.

    Write `src/lib/rag/tip-rotation.ts`:
    - `export function dayOfYear(date: Date = new Date()): number` — 1..366.
    - `export function pickDailyTip(chunks: RagChunkResult[], today: Date = new Date()): RagChunkResult | null` — deterministic: `chunks[dayOfYear(today) % chunks.length]` when chunks.length > 0; else null.
    - `export function buildTipQuery(userDrug: string | null, activeThemes: string[]): string` — composes query string from user state (e.g., `"tirzepatide muscle-loss nausea-management"`).
    - `export function filtersForUser(userDrug: string | null, activeThemes: string[]): { topic_tag?: string }` — single dominant theme topic_tag filter (first in activeThemes; null if none).

    Write `supabase/functions/_shared/server-rag-events.ts` (relay Edge Fn entry imported as shared module by a tiny `supabase/functions/server-rag-event-relay/index.ts`):
    - Actually create `supabase/functions/server-rag-event-relay/index.ts` as a thin HTTP endpoint that accepts POST `{event: 'rag_tip_impression'|'rag_tip_clicked'|'rag_citation_clicked'|'rag_hub_pageview', properties: Record<string, unknown>, distinct_id: string}` and forwards to `captureRagEvent(distinct_id, event, properties)`.
    - Authenticate caller via Supabase JWT (Edge Fn `--verify-jwt` default).
    - Add `supabase/functions/_shared/server-rag-events.ts` as a re-export module if needed (consolidates shared helper imports).
  </action>
  <verify>
    <automated>bash -c "cd leanshot && npm run typecheck && npm run lint -- src/lib/rag/retrieve-client.ts src/lib/rag/tip-rotation.ts && deno check supabase/functions/server-rag-event-relay/index.ts && supabase functions deploy server-rag-event-relay --linked"</automated>
  </verify>
  <acceptance_criteria>
    - retrieve-client.ts wraps rag-retrieve Edge Fn invocation.
    - tip-rotation.ts pickDailyTip deterministic (same day = same chunk index).
    - server-rag-event-relay deploys and uses captureRagEvent.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build CitationMarker + CitationPopover</name>
  <files>src/components/dashboard/ai/CitationMarker.tsx, src/components/dashboard/ai/CitationPopover.tsx</files>
  <read_first>.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (§B1 verbatim), src/components/ui/Badge.tsx, src/components/admin/rag/TierBadge.tsx, src/lib/i18n/rag-strings.ts</read_first>
  <action>
    Write `CitationMarker.tsx`:
    - Props: `{ refIndex: number; chunkId: string; onClick: () => void }`.
    - Renders `<Badge tone="info" as="button" className="text-[11px] rounded-full h-[14px] w-[14px] inline-flex items-center justify-center" onClick={onClick} aria-label={`Open citation ${refIndex}`}>[{refIndex}]</Badge>`.

    Write `CitationPopover.tsx`:
    - Props: `{ chunkId: string; anchorEl: HTMLElement | null; onClose: () => void }`.
    - On mount: `ragChunkById(chunkId)` → setState.
    - Render (≥md): floating Popover anchored to `anchorEl` (use a small `useFloating` helper or position via `getBoundingClientRect`).
    - Render (<md): bottom `Sheet`.
    - Card content per UI-SPEC §B1 verbatim:
      - Source title (linked to canonical_url, target=_blank, rel=noopener noreferrer; `text-base font-semibold`)
      - TierBadge inline-right
      - 1-line summary (`text-sm line-clamp-2`)
      - `As of {date}` (eyebrow + date in `font-mono tabular-nums`)
      - `Open source ↗` (Button variant=ghost, accent-colored, with external-link icon from lucide)
      - Disclaimer microcopy: `t('rag.disclaimer')` rendered `text-xs text-tertiary`.
    - On Open: fire `rag_citation_clicked` via `fetch('/functions/v1/server-rag-event-relay', {method:'POST', body: JSON.stringify({event:'rag_citation_clicked', properties: {chunk_id, source_tier, topic_tag, surface:'coach'}, distinct_id})})`.
    - Keyboard: ESC closes; `←/→` cycle (when parent provides prev/next refs).
    - Accessibility: `role="dialog"`, `aria-modal="true"`, focus trap, return-focus to marker on close.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && npm run typecheck && npm run lint -- src/components/dashboard/ai/CitationMarker.tsx src/components/dashboard/ai/CitationPopover.tsx && vitest run src/components/dashboard/ai/__tests__/CitationPopover.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - 2 files exist; typecheck + lint pass.
    - Popover renders source title + TierBadge + summary + as-of + Open-source + disclaimer per UI-SPEC §B1.
    - rag_citation_clicked fired server-side on open.
    - aria-modal + focus trap per UI-SPEC §Accessibility.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Augment AIChatPanel.tsx with citation rendering pass</name>
  <files>src/components/dashboard/ai/AIChatPanel.tsx</files>
  <read_first>src/components/dashboard/ai/AIChatPanel.tsx (existing message render path), src/components/dashboard/ai/CitationMarker.tsx, src/components/dashboard/ai/CitationPopover.tsx</read_first>
  <action>
    SURGICAL EDIT of `AIChatPanel.tsx` (do NOT replace the file):
    - Locate the assistant-message render branch.
    - Wrap text content in a new helper `renderWithCitations(text: string): React.ReactNode[]` that scans for `[^cid:<UUID>]^` markers, splits text at marker boundaries, increments a per-message refCounter, and emits `<CitationMarker key={...} refIndex={n} chunkId={uuid} onClick={...} />` in place of each marker.
    - Add a popoverState `useState<{chunkId: string; anchorEl: HTMLElement} | null>(null)`.
    - Mount `<CitationPopover {...popoverState} onClose={() => setPopoverState(null)} />` at the panel root.
    - Click handler in marker sets `{chunkId, anchorEl: e.currentTarget}` for popover positioning.

    Notes:
    - Existing message render must remain backward-compatible: messages without `[^cid:` markers render unchanged.
    - The assistant's prompt template (calls to Anthropic) needs to be updated SEPARATELY in a follow-up (out of scope for Plan 50-08); for v1.3 ship, manual injection by the assistant is acceptable. Document this as a known-incomplete in the SUMMARY.md once executed.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && npm run typecheck && npm run lint -- src/components/dashboard/ai/AIChatPanel.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - AIChatPanel.tsx still renders messages without `[^cid:` markers correctly (regression-safe).
    - When marker present, CitationMarker renders in place; click opens CitationPopover.
    - No new dependencies added.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Build TipOfTheDayCard + dashboard mount</name>
  <files>src/components/dashboard/cards/TipOfTheDayCard.tsx</files>
  <read_first>.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (§B2 verbatim), src/lib/rag/retrieve-client.ts, src/lib/rag/tip-rotation.ts, src/components/ui/Card.tsx</read_first>
  <action>
    Write `src/components/dashboard/cards/TipOfTheDayCard.tsx`:
    - On mount: `ragRetrieve({query: buildTipQuery(user.drug, user.activeThemes), k: 14, filters: filtersForUser(user.drug, user.activeThemes)})` → setState.
    - Pick chunk: `pickDailyTip(results)`.
    - If no chunk: fallback to `ragRetrieve({query: user.drug ?? 'GLP-1', k: 7, filters: {source_tier: ['A']}})` → pickDailyTip. If still none: render `null` (hide card per UI-SPEC §B2 empty-state rule).
    - If chunk found: render `<Card span={6}>` (UI-SPEC §B2 justification for span=6 over span=4):
      - Eyebrow `TIP OF THE DAY · {topic-tag Pill}` (accent + neutral pill).
      - Headline (`text-base font-semibold line-clamp-2`) — derived from chunk.summary first sentence.
      - Body (`text-base text-secondary line-clamp-3`) — derived from chunk.summary second sentence + first quote_block (if any).
      - Footer flex-between: left `{source_name} · As of {date} · TierBadge`; right `Open source ↗` (ghost Button to chunk.canonical_url).
      - Bottom disclaimer `text-xs text-tertiary`: `t('rag.disclaimer')`.
      - Top-right RotateCcw IconButton (`aria-label="Show another tip"`) — manually rerolls within today's chunk set.
    - On mount AND on each new card render: fire `rag_tip_impression` via server-rag-event-relay.
    - On `Open source ↗` click: fire `rag_tip_clicked` server-side.
    - Skeleton state while fetching.
    - Add card to dashboard `HomeTab.tsx` bento grid (surgical edit — append `<TipOfTheDayCard />` to the bento entries).
  </action>
  <verify>
    <automated>bash -c "cd leanshot && npm run typecheck && npm run lint -- src/components/dashboard/cards/TipOfTheDayCard.tsx src/components/dashboard/tabs/HomeTab.tsx && vitest run src/components/dashboard/cards/__tests__/TipOfTheDayCard.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - File exists; renders per UI-SPEC §B2 structure verbatim.
    - Hides card (returns null) when no eligible chunks per D-24 empty-state rule.
    - Daily rotation deterministic (same day = same chunk).
    - rag_tip_impression + rag_tip_clicked fired server-side.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Write 4 unit + 2 Playwright E2E tests</name>
  <files>src/components/dashboard/ai/__tests__/CitationPopover.test.tsx, src/components/dashboard/cards/__tests__/TipOfTheDayCard.test.tsx, src/lib/rag/__tests__/tip-rotation.test.ts, e2e/coach-citations.spec.ts, e2e/tip-of-day.spec.ts</files>
  <read_first>src/components/dashboard/ai/CitationPopover.tsx, src/components/dashboard/cards/TipOfTheDayCard.tsx, src/lib/rag/tip-rotation.ts</read_first>
  <action>
    `CitationPopover.test.tsx` (vitest + @testing-library/react):
    - `it('fetches chunk metadata on mount via ragChunkById')`.
    - `it('renders source title, TierBadge, summary, as-of date, Open-source link, disclaimer')` per UI-SPEC §B1.
    - `it('fires rag_citation_clicked on open with correct properties')`.
    - `it('Esc closes popover')`.
    - `it('focus traps inside popover and returns focus to marker on close')`.

    `TipOfTheDayCard.test.tsx`:
    - `it('renders eyebrow + headline + body + footer + disclaimer per UI-SPEC §B2')`.
    - `it('hides card when no eligible chunks (D-24 empty state)')`.
    - `it('fallbacks to generic Tier-A chunk when no themed match')`.
    - `it('reroll button calls pickDailyTip with manual offset')`.
    - `it('fires rag_tip_impression on mount')`.

    `tip-rotation.test.ts`:
    - `it('pickDailyTip deterministic same day same chunk')` — day N call twice → same chunk.
    - `it('pickDailyTip cycles across day boundary')` — day N vs N+1 → different chunks (when chunks.length > 1).
    - `it('pickDailyTip returns null when chunks empty')`.
    - `it('dayOfYear matches Date.UTC range 1..366')`.

    `e2e/coach-citations.spec.ts` (Playwright):
    - Use addInitScript to seed auth + paste an assistant message containing `[^cid:<seeded-chunk-uuid>]^` into the chat panel state.
    - Open AIChatPanel; click the `[1]` marker; expect Popover with source name and TierBadge.
    - Assert keyboard Esc closes Popover.
    - Gate via `PLAYWRIGHT_RUN_P50_COACH_CITATIONS=1`.

    `e2e/tip-of-day.spec.ts`:
    - Seed authenticated user with `drug='tirzepatide'`, `activeThemes=['muscle-loss']`, and seed 5 approved chunks with topic_tag='muscle-loss'.
    - Visit dashboard; expect TipOfTheDayCard rendered with one of the seeded chunk's source name + TierBadge.
    - Click `Open source ↗`; assert new tab open to canonical_url.
    - Gate via `PLAYWRIGHT_RUN_P50_TIP_OF_DAY=1`.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && vitest run src/components/dashboard/ai/__tests__/CitationPopover.test.tsx src/components/dashboard/cards/__tests__/TipOfTheDayCard.test.tsx src/lib/rag/__tests__/tip-rotation.test.ts && PLAYWRIGHT_RUN_P50_COACH_CITATIONS=1 PLAYWRIGHT_RUN_P50_TIP_OF_DAY=1 npx playwright test e2e/coach-citations.spec.ts e2e/tip-of-day.spec.ts"</automated>
  </verify>
  <acceptance_criteria>
    - All vitest + Playwright tests pass.
    - D-22 MVP cut validated (citations + tip card live).
    - D-24 hide-on-empty asserted.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `vitest run src/components/dashboard/ src/lib/rag src/lib/i18n` + Playwright gated runs — green.
- Manual UAT: load dashboard with seeded chunks → TipOfTheDayCard appears with correct source/tier; click → external tab opens.
- Bundle: HomeTab + AIChatPanel chunk delta ≤ 5 kB gz (per P24 bundle ceiling discipline — measure delta against pre-50-08 baseline; if exceeds, defer non-essential code to lazy import).
</verification>
