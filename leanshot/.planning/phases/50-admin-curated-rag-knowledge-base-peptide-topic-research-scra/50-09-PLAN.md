---
phase: 50-admin-curated-rag-knowledge-base-peptide-topic-research-scra
plan: 09
type: execute
wave: 4
depends_on: [50-03, 50-07, 50-08]
files_modified:
  - supabase/functions/rag-newsletter-sender/index.ts
  - supabase/functions/rag-newsletter-sender/templates/digest.html
  - supabase/functions/rag-newsletter-unsubscribe-1click/index.ts
  - supabase/migrations/20260519000015_rag_newsletter_cron.sql
  - src/pages/Research.tsx
  - src/pages/ResearchArticle.tsx
  - src/components/research/ResearchHubList.tsx
  - src/components/research/ResearchArticleDetail.tsx
  - src/components/dashboard/settings/NewsletterSettings.tsx
  - src/components/admin/rag/RagCostPage.tsx
  - src/lib/rag/__tests__/newsletter-subscribe.test.ts
  - src/components/research/__tests__/ResearchHub.test.tsx
  - e2e/research-hub.spec.ts
  - e2e/newsletter-unsubscribe.spec.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-22 STRETCH: Research newsletter Edge Fn + public /research Research Hub page + Cost dashboard polish ship in this wave; MUST-have wave (Plan 50-08) ships independently — Plan 50-09 can be deferred to Phase 51 if scope tightens (per CONTEXT D-22 wave-boundary rule)"
    - "D-25: rag-newsletter-sender SEPARATE from Phase 38 weekly Claude email — different sender, different template, different opt-in path; verifiable via grep -L 'weekly Claude' supabase/functions/rag-newsletter-sender/"
    - "D-26: weekly cadence (cron at Mon 09:00 user-timezone); opt-in default ON for paid users (rag_newsletter_subscriptions seed at upgrade-to-paid hook OR backfill cron); 1-click unsubscribe in every email; signup-time disclosure required"
    - "D-27: /research is public + indexable; reuses Phase 15 page-builder primitives WHERE AVAILABLE (Phase 15 may not yet have shipped; if missing, Plan 50-09 ships standalone components composing existing Card primitive — documented as follow-up to migrate to page-builder when Phase 15 lands); SEO meta + sitemap entry + JSON-LD Article schema (per-item dateModified from chunk.scraped_at)"
    - "D-33 reuse: /research and newsletter both use t('rag.attribution') i18n key from Plan 50-08"
    - "D-34: /research route added to DISABLE_RECORDING_URL_REGEX (Plan 50-03 already added); rag_hub_pageview captured server-side"
    - "Newsletter unsubscribe-1click: signed-token GET endpoint; sets rag_newsletter_subscriptions.unsubscribed_at; returns success page with 'Resubscribe' link; v1.3 standalone; consolidation into Phase 49 unsubscribe-1click documented as follow-up per RESEARCH §Open Questions"
    - "Cost dashboard (Plan 50-09 finalizes from Plan 50-02 placeholder): vendor card layout (Firecrawl/OpenAI/Anthropic cards span={4}) + CostBar + auto-pause banner + Acknowledge-and-resume flow per UI-SPEC §A5"
  artifacts:
    - "supabase/functions/rag-newsletter-sender/index.ts"
    - "supabase/functions/rag-newsletter-sender/templates/digest.html"
    - "supabase/functions/rag-newsletter-unsubscribe-1click/index.ts"
    - "supabase/migrations/20260519000015_rag_newsletter_cron.sql"
    - "src/pages/Research.tsx"
    - "src/pages/ResearchArticle.tsx"
    - "src/components/research/ResearchHubList.tsx"
    - "src/components/research/ResearchArticleDetail.tsx"
    - "src/components/dashboard/settings/NewsletterSettings.tsx"
    - "src/components/admin/rag/RagCostPage.tsx"
    - "src/lib/rag/__tests__/newsletter-subscribe.test.ts"
    - "src/components/research/__tests__/ResearchHub.test.tsx"
    - "e2e/research-hub.spec.ts"
    - "e2e/newsletter-unsubscribe.spec.ts"
  key_links:
    - "cron rag-newsletter-weekly → rag-newsletter-sender → P25 email-router (phi:false consumer Resend) → user inbox → 1-click unsubscribe → rag-newsletter-unsubscribe-1click → rag_newsletter_subscriptions UPDATE"
    - "/research route → ResearchHubList → ragRetrieve (Plan 50-07) → rag_chunks WHERE published_at IS NOT NULL"
    - "/admin/rag/cost → rag_mtd_spend_by_vendor (Plan 50-01) + Acknowledge-and-resume RPC"
---

<objective>
Ship the STRETCH-wave surfaces per D-22: weekly Research newsletter, public Research Hub at `/research`, NewsletterSettings opt-in UI, and the final Cost dashboard with auto-pause banner. Reuses all Plan 50-01..50-08 infrastructure; adds new Edge Fns for sender + unsubscribe + a few React pages. This wave can be deferred to Phase 51 if v1.3 closure timeline tightens.

Purpose: completes the four output surfaces from CONTEXT (coach + tip card + newsletter + Research Hub). Without this plan, Phase 50 still delivers the must-have value (Plan 50-08), but newsletter subscribers + public-content surface are absent — both are revenue/retention adjacent surfaces.
Output: 2 Edge Fns + 1 HTML template + 6 React components + 1 cron migration + 2 vitest + 2 Playwright E2E.
</objective>

<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-RESEARCH.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md
@supabase/functions/_shared/posthog-server.ts
@src/components/admin/rag/CostBar.tsx
@src/lib/rag/retrieve-client.ts

<interfaces>
- Phase 25 `_shared/email-router.ts` — `sendEmail({phi:false, to, subject, html, ...})` → Resend.
- Phase 49 `unsubscribe-1click` Edge Fn (NOT YET BUILT at v1.3 start) — Plan 50-09 ships standalone equivalent; consolidates later per RESEARCH §Open Questions.
- Plan 50-01 `rag_newsletter_subscriptions` table + RLS.
- Plan 50-07 `ragRetrieve` for /research content.
- Plan 50-08 `t('rag.attribution')` + `t('rag.disclaimer')` reused across newsletter HTML + Research Hub.
- Plan 50-01 `rag_mtd_spend_by_vendor()` + `rag_budget_caps` table for cost dashboard.
- React SPA routing: leanshot uses Zustand-derived "no router" pattern per CLAUDE.md. For `/research` we need to ADD route detection — either patch `App.tsx` to check `window.location.pathname.startsWith('/research')` and render Research instead of dashboard, OR add a minimal router (preferred: minimal pathname check at App.tsx root, no react-router dependency). Follow existing pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Research Hub list page + article detail + route wiring</name>
  <files>src/pages/Research.tsx, src/pages/ResearchArticle.tsx, src/components/research/ResearchHubList.tsx, src/components/research/ResearchArticleDetail.tsx</files>
  <read_first>.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (§C2 verbatim), src/App.tsx, src/lib/rag/retrieve-client.ts, src/lib/i18n/rag-strings.ts</read_first>
  <action>
    Write `src/pages/Research.tsx`:
    - Top-level route component for `/research` and `/research/:chunkId`.
    - If pathname matches `/research/:chunkId`: render `<ResearchArticleDetail chunkId={...} />`.
    - Else: render `<ResearchHubList />`.

    Patch `src/App.tsx` SURGICAL EDIT to detect `/research` paths at root:
    - In the existing view selector (marketing / onboarding / dashboard), add a top-priority branch: `if (window.location.pathname.startsWith('/research')) return <Suspense><Research /></Suspense>;`
    - Lazy-import `Research` via `const Research = lazy(() => import('@/pages/Research'))`.
    - Fire `rag_hub_pageview` server-side via server-rag-event-relay on path change (use a small effect).

    Write `src/components/research/ResearchHubList.tsx`:
    - Hero strip: H1 `Research` in Fraunces italic (the ONLY Fraunces moment per UI-SPEC §C2) + subtitle `Curated peptide and metabolic research, summarized with sources.`
    - Filter Pill row: `All` · per-tag (dynamic from approved chunks) · per-tier (A/B/C).
    - URL-state syncing: `?tag=X&tier=A` updates pills; shareable URLs.
    - Article list: query `SELECT c.id, c.summary, c.canonical_url, c.scraped_at, c.source_tier, c.topic_tag, s.name AS source_name FROM public.rag_chunks c JOIN public.rag_sources s ON s.id = c.source_id WHERE c.published_at IS NOT NULL AND c.retracted_at IS NULL ORDER BY c.published_at DESC LIMIT 50;` (later paginate).
    - Each card: `Card variant="interactive" span={6}` per UI-SPEC §C2 (NOT a new Article variant):
      - TierBadge top-right
      - Topic tag Pill top-left
      - Headline (`text-base font-semibold line-clamp-2`)
      - 2-line summary excerpt
      - Footer: `{source} · As of {YYYY-MM-DD}` + `Read at source ↗` button (links to canonical_url).
      - Click on card body (not the source link) → navigate to `/research/${chunk.id}`.
    - Empty filtered state: `EmptyState` per UI-SPEC §C2 copy.
    - SEO: `<title>Research · LeanShot</title>` + meta description + open-graph.

    Write `src/components/research/ResearchArticleDetail.tsx`:
    - Props: `{ chunkId: string }`.
    - Fetch via `ragChunkById(chunkId)`.
    - Render: react-markdown + dompurify rendered chunk (uses same allowlist as Plan 50-06 review queue).
    - Persistent disclaimer banner at top and bottom (uses `t('rag.disclaimer')`).
    - "Open at source ↗" CTA pinned in sticky right rail (desktop) or bottom (mobile).
    - Inject `<link rel="canonical" href={chunk.canonical_url}>` into document head via a useEffect.
    - Inject JSON-LD `Article` schema (`@context, @type:'Article', headline, datePublished:chunk.scraped_at, dateModified:chunk.scraped_at, author, publisher`) via a `<script type="application/ld+json">` tag rendered into a portal at `<head>` (or use a small helper).
    - SEO meta: `<meta name="og:type" content="article">`, `<meta property="article:published_time" content={chunk.scraped_at}>`, `<meta property="article:modified_time" content={chunk.scraped_at}>`, `<meta property="article:tag" content={chunk.topic_tag}>`.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && npm run typecheck && npm run lint -- src/pages/Research.tsx src/pages/ResearchArticle.tsx src/components/research/ && vitest run src/components/research/__tests__/ResearchHub.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - 4 files exist; typecheck + lint pass.
    - `/research` and `/research/:chunkId` both route correctly via patched App.tsx.
    - Filter URL-syncing works.
    - JSON-LD Article schema embedded with dateModified from chunk.scraped_at per D-27.
    - rel="canonical" link points to chunk.canonical_url.
    - Fraunces italic H1 is the ONLY Fraunces moment on the page (UI-SPEC §C2 constraint).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build rag-newsletter-sender Edge Function + digest.html template</name>
  <files>supabase/functions/rag-newsletter-sender/index.ts, supabase/functions/rag-newsletter-sender/templates/digest.html</files>
  <read_first>.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (§C1 verbatim), supabase/functions/_shared (email-router from P25 if exists; else stub), supabase/functions/rag-scrape-runner/cost-ledger.ts</read_first>
  <action>
    Write `supabase/functions/rag-newsletter-sender/templates/digest.html`:
    - HTML email template per UI-SPEC §C1: single-column, max-width 600px, light theme only.
    - `<head>` includes Geist via Google Fonts `<link>` with fallback `Arial, sans-serif`.
    - Template placeholders: `{{first_name}}`, `{{intro_line}}`, `{{per_tag_sections}}` (HTML string composed by Edge Fn), `{{manage_preferences_url}}`, `{{unsubscribe_1click_url}}`.
    - Per-item HTML row template (rendered in Edge Fn loop): source name (linked) · TierBadge (colored Pill rendered as `<span style="background-color:#dceae5;color:#1b4842;padding:2px 8px;border-radius:9999px;font-weight:600">Tier A</span>`) · 1-line summary · `Read at {source} ↗` button (32px tall, accent bg).
    - Standard attribution string from t('rag.attribution').

    Write `supabase/functions/rag-newsletter-sender/index.ts`:
    - Accept POST (no body) cron-tick mode. Selects `rag_newsletter_subscriptions WHERE frequency = 'weekly' AND unsubscribed_at IS NULL AND (last_sent_at IS NULL OR last_sent_at < now() - interval '6 days')`.
    - For each subscriber:
      1. Fetch user email + first_name from `auth.users` + `profiles`.
      2. For each followed tag, `ragRetrieve({query: tag, k: 3, filters: {topic_tag: tag, source_tier: ['A','B']}})` (Tier-C excluded from newsletter per editorial choice).
      3. If 0 new approved chunks since `subscription.last_sent_at` for any followed tag → SKIP send per UI-SPEC §C1 empty-state rule.
      4. Compose per-tag sections HTML.
      5. Generate signed unsubscribe token: `crypto.subtle.sign('HMAC', signingKey, payload={user_id, exp})` → base64url → URL `https://leanshot.app/functions/v1/rag-newsletter-unsubscribe-1click?t={token}`.
      6. Substitute template placeholders.
      7. Call `sendEmail({to:user.email, phi:false, subject:'Research digest: ...', html: substitutedTemplate})` via P25 email-router (or stub fallback if P25 not yet shipped).
      8. UPDATE rag_newsletter_subscriptions SET last_sent_at = now() WHERE user_id = $1.
      9. logVendorCost(client, {vendor:'resend', amountUsd: 0.0001, action:'newsletter_send'}).
    - Health check GET /healthz.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && deno check supabase/functions/rag-newsletter-sender/index.ts && supabase functions deploy rag-newsletter-sender --no-verify-jwt --linked"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; Edge Fn deploys.
    - SKIP-empty-send per UI-SPEC §C1.
    - Tier-C excluded from newsletter (Tier A+B only) per editorial choice (Plan-level decision, documented in summary).
    - Signed-token URL pattern; HMAC signing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build rag-newsletter-unsubscribe-1click Edge Function</name>
  <files>supabase/functions/rag-newsletter-unsubscribe-1click/index.ts</files>
  <read_first>supabase/functions/rag-newsletter-sender/index.ts (signing helper)</read_first>
  <action>
    Write `supabase/functions/rag-newsletter-unsubscribe-1click/index.ts`:
    - GET endpoint accepts `?t={signed_token}`.
    - Verify signature; extract `user_id` + `exp`. Reject if expired or signature invalid (400).
    - UPDATE `rag_newsletter_subscriptions` SET unsubscribed_at = now() WHERE user_id = $1 AND unsubscribed_at IS NULL.
    - Capture event server-side: `await captureRagEvent('rag-system', 'rag_newsletter_unsubscribed', {via: '1click'})` — note: distinct_id should be the user's id, not 'rag-system'; rewrite to use extracted user_id.
    - Response: HTML success page with "You're unsubscribed. [Resubscribe]" link going back to `/settings/notifications`.
    - No confirmation page (UI-SPEC §C1 says: "instant success page with Resubscribe link").
    - Document: when Phase 49 unsubscribe-1click Edge Fn ships, this should consolidate; for now standalone per RESEARCH §Open Questions.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && deno check supabase/functions/rag-newsletter-unsubscribe-1click/index.ts && supabase functions deploy rag-newsletter-unsubscribe-1click --no-verify-jwt --linked"</automated>
  </verify>
  <acceptance_criteria>
    - GET endpoint validates HMAC signature.
    - UNSUBSCRIBED state set; event captured server-side.
    - Success HTML page with Resubscribe link.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Build NewsletterSettings UI + RagCostPage final + newsletter cron migration</name>
  <files>src/components/dashboard/settings/NewsletterSettings.tsx, src/components/admin/rag/RagCostPage.tsx, supabase/migrations/20260519000015_rag_newsletter_cron.sql</files>
  <read_first>.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-UI-SPEC.md (§A5, §C1 settings affordance), src/components/admin/rag/CostBar.tsx</read_first>
  <action>
    Write `src/components/dashboard/settings/NewsletterSettings.tsx`:
    - Renders inside existing SettingsPage drawer.
    - Section heading: `Research newsletter`.
    - Toggle: `Send me the weekly research digest` (default ON if user.tier === 'paid', else OFF per D-26).
    - Multi-select tags: lists all distinct topic_tags from rag_topics; user selects which to follow.
    - Save → UPDATE rag_newsletter_subscriptions (UPSERT on user_id).
    - Fires `rag_newsletter_subscribed` event on subscribe; `rag_newsletter_unsubscribed` (via:'settings') on toggle off.

    Write `src/components/admin/rag/RagCostPage.tsx` (replaces placeholder from Plan 50-02):
    - Top row 3 vendor cards (`Card span={4}` each) per UI-SPEC §A5: Firecrawl · OpenAI embeddings · Anthropic summaries. Each card:
      - Eyebrow (vendor name)
      - Large mono spend `$XX.XX` (text-2xl)
      - Caption `of $YYY.YY budget · {N}% used`
      - CostBar (Plan 50-02 primitive)
      - Reset countdown `Resets {N} days`
    - Per-topic breakdown Card span={12} table.
    - Auto-paused banner: when any vendor at 100%, full-width banner above bento with `lucide-react AlertOctagon` + UI-SPEC §A5 copy `Scrapers auto-paused — {vendor} budget exhausted. Acknowledge to resume.` + danger CTA `Acknowledge and resume`.
    - Acknowledge handler: calls RPC `rag_acknowledge_budget_cap(p_vendor text)` which UPDATEs rag_budget_caps.last_acknowledged_at=now() and unpauses sources. Add RPC definition INLINE in this plan's migration file:
      - INSERT INTO migration `20260519000015_rag_newsletter_cron.sql` ALSO defines `rag_acknowledge_budget_cap(p_vendor text) RETURNS void` SECURITY DEFINER + log_admin_action, unpauses rag_sources for that vendor.

    Write `supabase/migrations/20260519000015_rag_newsletter_cron.sql`:
    - cron entry `rag-newsletter-weekly` at `0 9 * * 1` (Mon 09:00 UTC; per-user-timezone scheduling deferred to v1.4). Invokes rag-newsletter-sender Edge Fn via net.http_post with vault auth.
    - `rag_acknowledge_budget_cap(p_vendor text)` RPC per Task 4 RagCostPage handler.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && npm run typecheck && npm run lint -- src/components/dashboard/settings/NewsletterSettings.tsx src/components/admin/rag/RagCostPage.tsx && supabase db push --linked --dry-run | grep -v Skipping"</automated>
  </verify>
  <acceptance_criteria>
    - NewsletterSettings UI default ON for paid users.
    - RagCostPage renders 3 vendor cards + auto-pause banner per UI-SPEC §A5 verbatim.
    - rag_acknowledge_budget_cap RPC defined with super-admin gate.
    - Cron `rag-newsletter-weekly` scheduled.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Write newsletter-subscribe + ResearchHub vitest + 2 Playwright E2E</name>
  <files>src/lib/rag/__tests__/newsletter-subscribe.test.ts, src/components/research/__tests__/ResearchHub.test.tsx, e2e/research-hub.spec.ts, e2e/newsletter-unsubscribe.spec.ts</files>
  <read_first>src/components/dashboard/settings/NewsletterSettings.tsx, src/components/research/ResearchHubList.tsx</read_first>
  <action>
    `newsletter-subscribe.test.ts`:
    - `it('UPSERT rag_newsletter_subscriptions on save')` — D-26.
    - `it('default ON for paid users, OFF for free users')` — D-26.
    - `it('unsubscribe sets unsubscribed_at')` — D-26.
    - `it('1-click unsubscribe via signed token works')` — D-26 1-click.

    `ResearchHub.test.tsx`:
    - `it('lists approved chunks with TierBadge')` — D-27.
    - `it('filter pills sync URL query params')`.
    - `it('clicking card navigates to /research/:chunkId')`.
    - `it('article detail page embeds JSON-LD Article schema')` — D-27.
    - `it('article detail page sets rel=canonical to canonical_url')` — D-27.
    - `it('Fraunces italic only used for H1')` — UI-SPEC §C2 constraint.

    `e2e/research-hub.spec.ts`:
    - Seed: 5 approved chunks.
    - Visit `/research` unauthenticated, expect listing visible (D-27 public).
    - Filter by tag, expect URL updates and list narrows.
    - Click first card, expect article detail page; assert JSON-LD schema in DOM and canonical link in <head>.
    - Gate: `PLAYWRIGHT_RUN_P50_RESEARCH_HUB=1`.

    `e2e/newsletter-unsubscribe.spec.ts`:
    - Seed: subscribed user.
    - Generate signed token, GET `/functions/v1/rag-newsletter-unsubscribe-1click?t=...`, expect 200 with success HTML and `unsubscribed_at` populated in DB.
    - Gate: `PLAYWRIGHT_RUN_P50_NEWSLETTER_UNSUBSCRIBE=1`.
  </action>
  <verify>
    <automated>bash -c "cd leanshot && vitest run src/lib/rag/__tests__/newsletter-subscribe.test.ts src/components/research/__tests__/ResearchHub.test.tsx && PLAYWRIGHT_RUN_P50_RESEARCH_HUB=1 PLAYWRIGHT_RUN_P50_NEWSLETTER_UNSUBSCRIBE=1 npx playwright test e2e/research-hub.spec.ts e2e/newsletter-unsubscribe.spec.ts"</automated>
  </verify>
  <acceptance_criteria>
    - All vitest + Playwright tests pass.
    - D-25, D-26, D-27 covered.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All vitest + Playwright green; both Edge Fns deploy; cron entry created.
- Bundle: research route is lazy-imported (own chunk, no impact on dashboard index ceiling per P24 enforcement).
- Manual UAT: visit `/research` from incognito, see listing; click article, see detail + JSON-LD in view-source; click unsubscribe link from a real email, see success page.
- Note: this is STRETCH per D-22; if v1.3 closure timeline tightens, this plan can defer to Phase 51 without breaking Plans 50-01..50-08.
</verification>
