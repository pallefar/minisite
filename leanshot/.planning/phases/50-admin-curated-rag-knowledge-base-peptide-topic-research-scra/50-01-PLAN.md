---
phase: 50-admin-curated-rag-knowledge-base-peptide-topic-research-scra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260519000001_rag_topics_table.sql
  - supabase/migrations/20260519000002_rag_sources_table_and_seed.sql
  - supabase/migrations/20260519000003_rag_chunks_table.sql
  - supabase/migrations/20260519000004_external_kb_embeddings_table.sql
  - supabase/migrations/20260519000005_rag_topic_audit_table.sql
  - supabase/migrations/20260519000006_rag_scrape_runs_table.sql
  - supabase/migrations/20260519000007_rag_cost_ledger_table.sql
  - supabase/migrations/20260519000008_rag_newsletter_subscriptions_table.sql
  - supabase/migrations/20260519000009_rag_rls_policies.sql
  - src/lib/rag/__tests__/seed-sources.test.ts
  - src/lib/rag/__tests__/embeddings-schema.test.ts
  - src/lib/rag/__tests__/tier-check.test.ts
  - src/lib/rag/__tests__/rls-matrix.test.ts
autonomous: false
requirements: []
user_setup:
  - service: supabase
    why: "Confirm pgvector + pg_cron + pg_net extensions enabled (per Phase 24 user_setup; Phase 50 reuses)"
    env_vars: []
    dashboard_config:
      - task: "Verify `pgvector`, `pg_cron`, `pg_net` extensions are enabled (Supabase Dashboard → Database → Extensions). All three were provisioned during Phase 24 wave-0; this plan blocks if they're disabled."
        location: "Supabase Dashboard → Database → Extensions"

must_haves:
  truths:
    - "D-01: rag_topics.posture enum has values 'curated' and 'open-web'; topic row carries posture column"
    - "D-04: rag_sources seeded with 12 curated rows on first migration (PubMed E-utils, FDA Drug Labels, drugs.com, Examine.com, Lilly tirzepatide, Novo semaglutide, Novo liraglutide, Eli Lilly retatrutide, NIH MedlinePlus, Mayo Clinic peptide info, Cleveland Clinic GLP-1, UpToDate-public)"
    - "D-06: rag_sources.tier enum has values 'A','B','C'; per-source tier column populated on seed"
    - "D-08: rag_topics has (query text, tag text) columns; unique index on (query, deleted_at) prevents duplicate active topics"
    - "D-09: rag_topics.cadence enum has values 'daily','weekly','monthly','manual'; column defaults to 'weekly'"
    - "D-13: rag_topics.deleted_at timestamptz nullable; soft-delete preserves chunks; RLS filters deleted_at IS NULL for admin list, retrieval ignores flag"
    - "D-14: rag_topics has last_edited_by uuid REFERENCES auth.users(id); rag_topic_audit table captures before/after JSONB on every edit"
    - "D-15: rag_chunks.published_at timestamptz nullable; Tier-A inserts populate it on creation, Tier-B/C insert with NULL"
    - "D-16: rag_chunks.reject_reason enum has six values off-topic|factually-wrong|off-label|low-quality|duplicate|safety-concern"
    - "D-17: rag_chunks.quote_blocks JSONB nullable; populated by summarizer per D-17"
    - "D-20: rag_chunks.retracted_at timestamptz nullable; retraction does not delete the row"
    - "D-28: external_kb_embeddings table EXISTS as a SEPARATE table from content_embeddings; both vector(1536); HNSW index on external_kb_embeddings using (vector_cosine_ops) with (m=16, ef_construction=64)"
    - "D-30: rag_cost_ledger captures (vendor, ts, amount_usd, topic_id?, source_id?, action); aggregation function returns MTD spend per vendor"
    - "D-12: Direct SELECT/INSERT/UPDATE/DELETE on rag_topics revoked for authenticated/anon; super-admin RLS policy via is_admin_at_least('superadmin')"
    - "D-34: external_kb_embeddings has no PHI columns (no user_id, no patient_id, no chat_history reference); only chunk_id + source_id + topic_id + topic_tag + source_tier"
  artifacts:
    - "supabase/migrations/20260519000001_rag_topics_table.sql"
    - "supabase/migrations/20260519000002_rag_sources_table_and_seed.sql"
    - "supabase/migrations/20260519000003_rag_chunks_table.sql"
    - "supabase/migrations/20260519000004_external_kb_embeddings_table.sql"
    - "supabase/migrations/20260519000005_rag_topic_audit_table.sql"
    - "supabase/migrations/20260519000006_rag_scrape_runs_table.sql"
    - "supabase/migrations/20260519000007_rag_cost_ledger_table.sql"
    - "supabase/migrations/20260519000008_rag_newsletter_subscriptions_table.sql"
    - "supabase/migrations/20260519000009_rag_rls_policies.sql"
    - "src/lib/rag/__tests__/seed-sources.test.ts"
    - "src/lib/rag/__tests__/embeddings-schema.test.ts"
    - "src/lib/rag/__tests__/tier-check.test.ts"
    - "src/lib/rag/__tests__/rls-matrix.test.ts"
  key_links:
    - "Phase 24 log_admin_action() → rag_topic_audit + rag_chunks state transitions (admin-side actions)"
    - "Phase 24 is_admin_at_least('superadmin') → rag_topics + rag_sources RLS gate"
    - "external_kb_embeddings.chunk_id → rag_chunks.id ON DELETE CASCADE (retract chain)"
---

<objective>
Lay the SQL foundation for Phase 50. Apply 9 migrations creating 8 new tables (`rag_topics`, `rag_sources`, `rag_chunks`, `external_kb_embeddings`, `rag_topic_audit`, `rag_scrape_runs`, `rag_cost_ledger`, `rag_newsletter_subscriptions`) + RLS policies + 12 seed `rag_sources` rows + a `mtd_spend_by_vendor()` aggregation function. All other Phase 50 plans depend on these tables existing.

Purpose: without these tables, the scrape runner cannot persist results, the admin UI cannot CRUD, the embeddings pipeline cannot upsert, and the retrieval Edge Fn cannot query. Wave-1 foundation that Waves 2/3/4 all consume.
Output: 9 SQL migrations + 4 vitest RLS / schema invariant tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-RESEARCH.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-VALIDATION.md
@supabase/migrations
@src/lib/admin/__tests__

<interfaces>
- Phase 24 (not yet shipped at v1.3 start but locked at CONTEXT level) `is_admin_at_least(public.admin_role)` SECURITY DEFINER helper — Phase 50 RLS policies reuse it. If Phase 24 has not yet shipped at Phase 50 execute time, plan 50-01 vendors a defensive fallback `coalesce(public.is_admin_at_least('superadmin'::public.admin_role), false)` that returns FALSE rather than failing migration apply.
- Phase 24 `log_admin_action(action_name, target_user_id, table_name, row_pk, before_jsonb, after_jsonb)` — called from Phase 50 admin RPCs in plans 50-02 and 50-06.
- Phase 38 (not yet shipped) `content_embeddings vector(1536)` table + HNSW index — Phase 50 mirrors the index shape on `external_kb_embeddings` per D-28; if `content_embeddings` doesn't exist yet, this is fine — both tables are independent.
- `auth.users(id)` for `actor_user_id`, `last_edited_by`, newsletter `user_id`.

Memory references (rationale):
- `[[reference_supabase_migration_filename_regex]]` — 14-digit strict filenames; letter-suffix silently skipped. All 9 migrations use the `20260519000001..9` form.
- `[[reference_supabase_migration_gotchas]]` — SECURITY DEFINER fns set `search_path = extensions, public, pg_temp`; partial-index expressions must be IMMUTABLE.
- `[[reference_rls_fixture_gotruechient_flake]]` — RLS test pattern: admin.generateLink + /auth/v1/verify via PLAIN fetch (ES256-compat).
- `[[feedback_status_machine_transition_owner.md]]` — rag_chunks state machine: every status value (queued / approved / rejected / retracted / re-queued) must have an owning plan + transition function. Plan 50-06 owns the transitions; this plan owns the columns.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write rag_topics table + audit trigger glue</name>
  <files>supabase/migrations/20260519000001_rag_topics_table.sql, supabase/migrations/20260519000005_rag_topic_audit_table.sql</files>
  <read_first>
    supabase/migrations (most recent Phase 24 migrations for header conventions and idempotency patterns),
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md (D-08, D-09, D-12, D-13, D-14),
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-RESEARCH.md (pitfalls 1, 2, 17)
  </read_first>
  <action>
    Write `20260519000001_rag_topics_table.sql` with:
    - Top comment: `-- Phase 50: rag_topics — admin-curated research topic CRUD per D-08/D-09/D-12/D-13/D-14`
    - Create two ENUMs: `rag_topic_posture` with values `'curated','open-web'` (D-01) and `rag_topic_cadence` with values `'daily','weekly','monthly','manual'` (D-09). Wrap each `CREATE TYPE` in a `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN null; END $$;` block (Postgres < 16 lacks `CREATE TYPE IF NOT EXISTS`).
    - `CREATE TABLE IF NOT EXISTS public.rag_topics (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), query text NOT NULL, tag text NOT NULL, posture public.rag_topic_posture NOT NULL DEFAULT 'curated', cadence public.rag_topic_cadence NOT NULL DEFAULT 'weekly', next_scrape_at timestamptz, last_scraped_at timestamptz, created_by uuid REFERENCES auth.users(id), last_edited_by uuid REFERENCES auth.users(id), created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now(), deleted_at timestamptz);`
    - Unique partial index: `CREATE UNIQUE INDEX IF NOT EXISTS rag_topics_query_active_uq ON public.rag_topics (query) WHERE deleted_at IS NULL;`
    - Index on `(tag)` and `(next_scrape_at) WHERE deleted_at IS NULL`.
    - `updated_at` trigger: `CREATE OR REPLACE FUNCTION public.tg_rag_topics_updated_at() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.updated_at = now(); RETURN NEW; END $$;` then `DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_trigger WHERE tgname='trg_rag_topics_updated_at') THEN CREATE TRIGGER trg_rag_topics_updated_at BEFORE UPDATE ON public.rag_topics FOR EACH ROW EXECUTE FUNCTION public.tg_rag_topics_updated_at(); END IF; END $$;`
    - `ALTER TABLE public.rag_topics ENABLE ROW LEVEL SECURITY;` (policies in migration 20260519000009).

    Write `20260519000005_rag_topic_audit_table.sql` with:
    - `CREATE TABLE IF NOT EXISTS public.rag_topic_audit (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), topic_id uuid NOT NULL REFERENCES public.rag_topics(id) ON DELETE CASCADE, actor_user_id uuid REFERENCES auth.users(id), action text NOT NULL CHECK (action IN ('create','update','delete','restore')), before_data jsonb, after_data jsonb, created_at timestamptz NOT NULL DEFAULT now());`
    - Index `(topic_id, created_at DESC)`.
    - `ENABLE ROW LEVEL SECURITY;`
    - Audit trigger: `CREATE OR REPLACE FUNCTION public.fn_rag_topic_audit_trigger() RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path = extensions, public, pg_temp AS $$ BEGIN IF current_setting('app.suppress_audit', true) = 'on' THEN RETURN coalesce(NEW, OLD); END IF; INSERT INTO public.rag_topic_audit (topic_id, actor_user_id, action, before_data, after_data) VALUES (coalesce(NEW.id, OLD.id), auth.uid(), CASE TG_OP WHEN 'INSERT' THEN 'create' WHEN 'UPDATE' THEN (CASE WHEN NEW.deleted_at IS NOT NULL AND OLD.deleted_at IS NULL THEN 'delete' WHEN NEW.deleted_at IS NULL AND OLD.deleted_at IS NOT NULL THEN 'restore' ELSE 'update' END) WHEN 'DELETE' THEN 'delete' END, CASE WHEN TG_OP IN ('UPDATE','DELETE') THEN to_jsonb(OLD) ELSE NULL END, CASE WHEN TG_OP IN ('INSERT','UPDATE') THEN to_jsonb(NEW) ELSE NULL END); RETURN coalesce(NEW, OLD); END $$;`
    - Attach trigger on `rag_topics` AFTER INSERT/UPDATE/DELETE per [[reference_supabase_migration_gotchas]] pattern.
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && supabase db push --linked --dry-run 2>&1 | tee /tmp/50-01-t1.log && ! grep -i '^Skipping' /tmp/50-01-t1.log && grep -q 'rag_topics' /tmp/50-01-t1.log"
    </automated>
  </verify>
  <acceptance_criteria>
    - Files `20260519000001_rag_topics_table.sql` AND `20260519000005_rag_topic_audit_table.sql` exist.
    - Both files have `-- Phase 50:` top comment with D-ID refs.
    - `rag_topic_posture` and `rag_topic_cadence` enums created idempotently.
    - `rag_topics` has columns: id, query, tag, posture, cadence, next_scrape_at, last_scraped_at, created_by, last_edited_by, created_at, updated_at, deleted_at.
    - Unique partial index `rag_topics_query_active_uq` exists on `(query) WHERE deleted_at IS NULL`.
    - `fn_rag_topic_audit_trigger()` is SECURITY DEFINER with `set search_path = extensions, public, pg_temp` AND respects `app.suppress_audit` GUC.
    - `supabase db push --linked --dry-run` reports zero `Skipping` warnings.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write rag_sources table + 12 seed allowlist rows</name>
  <files>supabase/migrations/20260519000002_rag_sources_table_and_seed.sql, src/lib/rag/__tests__/seed-sources.test.ts</files>
  <read_first>
    supabase/migrations/20260519000001_rag_topics_table.sql,
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md (D-04, D-06, D-07, D-32),
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-RESEARCH.md
  </read_first>
  <action>
    Write `20260519000002_rag_sources_table_and_seed.sql`:
    - ENUM `rag_source_tier` with values `'A','B','C'`. Wrap in DO block.
    - ENUM `rag_source_health` with values `'ok','paused','failing'`. Wrap in DO block.
    - `CREATE TABLE IF NOT EXISTS public.rag_sources (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), name text NOT NULL, domain text NOT NULL UNIQUE, tier public.rag_source_tier NOT NULL, freshness_window_days int NOT NULL, health public.rag_source_health NOT NULL DEFAULT 'ok', consecutive_failures int NOT NULL DEFAULT 0, paused_at timestamptz, paused_reason text, last_scrape_at timestamptz, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now());`
    - Default `freshness_window_days` per tier (D-32): A=365, B=90, C=30. Enforce via CHECK constraint `CHECK (freshness_window_days BETWEEN 1 AND 3650)`.
    - Idempotent INSERT for 12 seed rows using `INSERT INTO public.rag_sources (name, domain, tier, freshness_window_days) VALUES (...) ON CONFLICT (domain) DO NOTHING;`. Seed list:
      | name | domain | tier | freshness |
      |---|---|---|---|
      | PubMed E-utilities | pubmed.ncbi.nlm.nih.gov | A | 365 |
      | FDA Drug Labels (DailyMed) | dailymed.nlm.nih.gov | A | 365 |
      | NIH MedlinePlus | medlineplus.gov | A | 365 |
      | Eli Lilly tirzepatide PI | lilly.com | A | 365 |
      | Novo Nordisk semaglutide PI | novonordisk.com | A | 365 |
      | drugs.com monographs | drugs.com | B | 90 |
      | Examine.com | examine.com | B | 90 |
      | Mayo Clinic peptide reference | mayoclinic.org | B | 90 |
      | Cleveland Clinic GLP-1 | my.clevelandclinic.org | B | 90 |
      | UpToDate (public summaries) | uptodate.com | B | 90 |
      | Healthline GLP-1 (lay press) | healthline.com | C | 30 |
      | Diabetes Daily community | diabetesdaily.com | C | 30 |
    - `updated_at` trigger via shared `tg_set_updated_at()` (define if not present).
    - `ALTER TABLE public.rag_sources ENABLE ROW LEVEL SECURITY;`

    Write `src/lib/rag/__tests__/seed-sources.test.ts` (vitest):
    - Imports the project's `supabase` test client.
    - `it('seeds 12 rag_sources rows with valid tier + freshness windows')`: query `SELECT count(*) AS n FROM public.rag_sources;` → expect `≥ 12`.
    - `it('Tier A sources have freshness_window_days = 365')`: query `SELECT count(*) FROM public.rag_sources WHERE tier = 'A' AND freshness_window_days = 365` → expect 5.
    - `it('Tier B sources have freshness_window_days = 90')`: expect 5.
    - `it('Tier C sources have freshness_window_days = 30')`: expect 2.
    - `it('CHECK constraint rejects freshness_window_days = 0')`: attempt insert with `freshness_window_days = 0`, expect error.
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && supabase db push --linked --dry-run 2>&1 | grep -v Skipping && vitest run src/lib/rag/__tests__/seed-sources.test.ts"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `20260519000002_rag_sources_table_and_seed.sql` exists.
    - `rag_sources` table has columns: id, name, domain (UNIQUE), tier, freshness_window_days, health, consecutive_failures, paused_at, paused_reason, last_scrape_at, created_at, updated_at.
    - 12 seed rows inserted via ON CONFLICT DO NOTHING (idempotent across re-applies).
    - Per-tier counts: 5 Tier-A, 5 Tier-B, 2 Tier-C with correct freshness defaults.
    - Test file `seed-sources.test.ts` exists and all five test cases pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Write rag_chunks table with state-machine columns</name>
  <files>supabase/migrations/20260519000003_rag_chunks_table.sql, src/lib/rag/__tests__/tier-check.test.ts</files>
  <read_first>
    supabase/migrations/20260519000002_rag_sources_table_and_seed.sql,
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md (D-15, D-16, D-17, D-19, D-20, D-21, D-32),
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-RESEARCH.md (pitfall 17 state-machine)
  </read_first>
  <action>
    Write `20260519000003_rag_chunks_table.sql`:
    - ENUM `rag_chunk_status` values `'queued','approved','rejected','retracted','re-queued'`. Wrap in DO block.
    - ENUM `rag_reject_reason` values `'off-topic','factually-wrong','off-label','low-quality','duplicate','safety-concern'`. Wrap in DO block.
    - `CREATE TABLE IF NOT EXISTS public.rag_chunks (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), topic_id uuid NOT NULL REFERENCES public.rag_topics(id) ON DELETE CASCADE, source_id uuid NOT NULL REFERENCES public.rag_sources(id), canonical_url text NOT NULL, source_text_excerpt text NOT NULL, summary text NOT NULL, quote_blocks jsonb DEFAULT '[]'::jsonb, content_hash text NOT NULL, topic_tag text NOT NULL, source_tier public.rag_source_tier NOT NULL, status public.rag_chunk_status NOT NULL DEFAULT 'queued', published_at timestamptz, retracted_at timestamptz, retracted_reason text, reject_reason public.rag_reject_reason, reviewed_by uuid REFERENCES auth.users(id), reviewed_at timestamptz, erratum_flag boolean NOT NULL DEFAULT false, scraped_at timestamptz NOT NULL DEFAULT now(), created_at timestamptz NOT NULL DEFAULT now());`
    - Compound unique index: `CREATE UNIQUE INDEX IF NOT EXISTS rag_chunks_dedup_uq ON public.rag_chunks (topic_id, source_id, content_hash);`
    - Indexes: `(status, scraped_at DESC) WHERE status = 'queued'` (queue scan), `(topic_id, status) WHERE published_at IS NOT NULL`, `(source_id, reviewed_at)` (for D-16 30d rejection rolling window).
    - CHECK constraint: `CHECK ((status = 'rejected') = (reject_reason IS NOT NULL))` — reject_reason mandatory when rejected.
    - CHECK constraint: `CHECK ((status = 'retracted') = (retracted_at IS NOT NULL))` — retracted_at mandatory when retracted.
    - `ALTER TABLE public.rag_chunks ENABLE ROW LEVEL SECURITY;`

    Write `src/lib/rag/__tests__/tier-check.test.ts`:
    - `it('rag_chunk_status enum has 5 values')`: query `SELECT unnest(enum_range(null::public.rag_chunk_status))::text` → expect array of `queued,approved,rejected,retracted,re-queued`.
    - `it('rag_reject_reason enum has 6 values')`: similar shape.
    - `it('CHECK: rejected chunk requires reject_reason')`: attempt insert `status='rejected', reject_reason=NULL` → expect error.
    - `it('CHECK: retracted chunk requires retracted_at')`: attempt insert `status='retracted', retracted_at=NULL` → expect error.
    - `it('dedup unique: cannot insert same (topic_id, source_id, content_hash) twice')`.
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && supabase db push --linked --dry-run | grep -v Skipping && vitest run src/lib/rag/__tests__/tier-check.test.ts"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `20260519000003_rag_chunks_table.sql` exists.
    - `rag_chunks` table has columns enumerated above; both CHECK constraints present.
    - 3 indexes created (queue scan, published lookup, source rejection roll-up).
    - All 5 vitest cases in `tier-check.test.ts` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Write external_kb_embeddings table + HNSW index</name>
  <files>supabase/migrations/20260519000004_external_kb_embeddings_table.sql, src/lib/rag/__tests__/embeddings-schema.test.ts</files>
  <read_first>
    supabase/migrations/20260519000003_rag_chunks_table.sql,
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md (D-28, D-29, D-34),
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-RESEARCH.md (HNSW snippet)
  </read_first>
  <action>
    Write `20260519000004_external_kb_embeddings_table.sql`:
    - `CREATE EXTENSION IF NOT EXISTS vector;` (idempotent; provisioned at P38/P24 wave-0 but defensive).
    - `CREATE TABLE IF NOT EXISTS public.external_kb_embeddings (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), chunk_id uuid NOT NULL REFERENCES public.rag_chunks(id) ON DELETE CASCADE, embedding vector(1536) NOT NULL, topic_id uuid NOT NULL REFERENCES public.rag_topics(id), source_id uuid NOT NULL REFERENCES public.rag_sources(id), source_tier public.rag_source_tier NOT NULL, topic_tag text NOT NULL, freshness_window_days int NOT NULL, embedded_at timestamptz NOT NULL DEFAULT now());`
    - HNSW index: `CREATE INDEX IF NOT EXISTS external_kb_embeddings_hnsw ON public.external_kb_embeddings USING hnsw (embedding vector_cosine_ops) WITH (m = 16, ef_construction = 64);`
    - Supporting index: `CREATE INDEX IF NOT EXISTS external_kb_embeddings_tag_tier ON public.external_kb_embeddings (topic_tag, source_tier);`
    - `ALTER TABLE public.external_kb_embeddings ENABLE ROW LEVEL SECURITY;`
    - Unique on chunk_id: `CREATE UNIQUE INDEX IF NOT EXISTS external_kb_embeddings_chunk_uq ON public.external_kb_embeddings (chunk_id);` — one embedding per chunk.

    Write `src/lib/rag/__tests__/embeddings-schema.test.ts`:
    - `it('external_kb_embeddings has vector(1536) embedding column')`: query `information_schema.columns` joined with `pg_attribute + format_type` → expect `vector(1536)`.
    - `it('HNSW index exists on embedding column')`: query `pg_indexes` for `external_kb_embeddings_hnsw` → expect rowcount 1.
    - `it('no PHI columns')`: assert column list excludes `user_id`, `patient_id`, `chat_history`, `conversation_id`.
    - `it('ON DELETE CASCADE chunk_id → external_kb_embeddings')`: insert chunk + embedding, delete chunk, expect embedding gone.
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && supabase db push --linked --dry-run | grep -v Skipping && vitest run src/lib/rag/__tests__/embeddings-schema.test.ts"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `20260519000004_external_kb_embeddings_table.sql` exists.
    - Table has `vector(1536)` column; HNSW index present.
    - No PHI columns; CASCADE delete behavior asserted by test.
    - 4 vitest cases in `embeddings-schema.test.ts` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Write rag_scrape_runs + rag_cost_ledger + rag_newsletter_subscriptions tables</name>
  <files>supabase/migrations/20260519000006_rag_scrape_runs_table.sql, supabase/migrations/20260519000007_rag_cost_ledger_table.sql, supabase/migrations/20260519000008_rag_newsletter_subscriptions_table.sql</files>
  <read_first>
    supabase/migrations/20260519000003_rag_chunks_table.sql,
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md (D-26, D-30, D-31, D-35)
  </read_first>
  <action>
    Write `20260519000006_rag_scrape_runs_table.sql`:
    - `CREATE TABLE IF NOT EXISTS public.rag_scrape_runs (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), topic_id uuid NOT NULL REFERENCES public.rag_topics(id) ON DELETE CASCADE, source_id uuid REFERENCES public.rag_sources(id), started_at timestamptz NOT NULL DEFAULT now(), completed_at timestamptz, status text NOT NULL CHECK (status IN ('running','ok','partial','failed')) DEFAULT 'running', chunks_found int NOT NULL DEFAULT 0, error_message text, attempt int NOT NULL DEFAULT 1);`
    - Index `(topic_id, started_at DESC)` and `(source_id, status, completed_at DESC)`.
    - `ALTER TABLE public.rag_scrape_runs ENABLE ROW LEVEL SECURITY;`

    Write `20260519000007_rag_cost_ledger_table.sql`:
    - ENUM `rag_vendor` values `'firecrawl','openai_embed','anthropic_summary','resend'`. Wrap in DO block.
    - `CREATE TABLE IF NOT EXISTS public.rag_cost_ledger (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), vendor public.rag_vendor NOT NULL, ts timestamptz NOT NULL DEFAULT now(), amount_usd numeric(10,4) NOT NULL CHECK (amount_usd >= 0), topic_id uuid REFERENCES public.rag_topics(id) ON DELETE SET NULL, source_id uuid REFERENCES public.rag_sources(id) ON DELETE SET NULL, action text NOT NULL, meta jsonb DEFAULT '{}'::jsonb);`
    - Indexes `(vendor, ts DESC)` and `(topic_id, ts DESC)`.
    - `CREATE TABLE IF NOT EXISTS public.rag_budget_caps (vendor public.rag_vendor PRIMARY KEY, monthly_cap_usd numeric(10,2) NOT NULL, last_paused_at timestamptz, last_acknowledged_at timestamptz);` — admin-editable.
    - Seed defaults (D-30): firecrawl=200.00, openai_embed=50.00, anthropic_summary=300.00, resend=20.00. INSERT ... ON CONFLICT DO NOTHING.
    - `CREATE OR REPLACE FUNCTION public.rag_mtd_spend_by_vendor() RETURNS TABLE(vendor public.rag_vendor, mtd_usd numeric, monthly_cap_usd numeric, pct_used numeric) LANGUAGE sql STABLE SECURITY DEFINER SET search_path = extensions, public, pg_temp AS $$ SELECT bc.vendor, coalesce(sum(l.amount_usd) FILTER (WHERE l.ts >= date_trunc('month', now())), 0) AS mtd_usd, bc.monthly_cap_usd, ROUND( (coalesce(sum(l.amount_usd) FILTER (WHERE l.ts >= date_trunc('month', now())), 0) / NULLIF(bc.monthly_cap_usd, 0)) * 100, 2) AS pct_used FROM public.rag_budget_caps bc LEFT JOIN public.rag_cost_ledger l ON l.vendor = bc.vendor GROUP BY bc.vendor, bc.monthly_cap_usd $$;`
    - `GRANT EXECUTE ON FUNCTION public.rag_mtd_spend_by_vendor() TO authenticated;` (admin-only access enforced via RLS on tables consumed).
    - `ALTER TABLE public.rag_cost_ledger ENABLE ROW LEVEL SECURITY;` and `ALTER TABLE public.rag_budget_caps ENABLE ROW LEVEL SECURITY;`

    Write `20260519000008_rag_newsletter_subscriptions_table.sql`:
    - `CREATE TABLE IF NOT EXISTS public.rag_newsletter_subscriptions (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, frequency text NOT NULL CHECK (frequency IN ('weekly','off')) DEFAULT 'weekly', tags_followed text[] NOT NULL DEFAULT '{}', unsubscribed_at timestamptz, last_sent_at timestamptz, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now());`
    - Unique on `user_id`: `CREATE UNIQUE INDEX IF NOT EXISTS rag_newsletter_subscriptions_user_uq ON public.rag_newsletter_subscriptions (user_id);`
    - `updated_at` trigger reusing `tg_set_updated_at()`.
    - `ALTER TABLE public.rag_newsletter_subscriptions ENABLE ROW LEVEL SECURITY;`
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && supabase db push --linked --dry-run | grep -v Skipping && supabase db query --linked 'SELECT * FROM public.rag_mtd_spend_by_vendor();'"
    </automated>
  </verify>
  <acceptance_criteria>
    - Three migration files exist with strict 14-digit timestamps.
    - `rag_mtd_spend_by_vendor()` is SECURITY DEFINER with search_path set.
    - `rag_budget_caps` seeded with 4 vendor rows defaulting to D-30 caps.
    - `rag_newsletter_subscriptions` has CHECK on frequency + unique on user_id.
    - `supabase db push --linked --dry-run` reports zero skips.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Write RLS policies migration + RLS impersonation matrix test</name>
  <files>supabase/migrations/20260519000009_rag_rls_policies.sql, src/lib/rag/__tests__/rls-matrix.test.ts</files>
  <read_first>
    supabase/migrations/20260519000001_rag_topics_table.sql,
    supabase/migrations/20260519000002_rag_sources_table_and_seed.sql,
    supabase/migrations/20260519000003_rag_chunks_table.sql,
    supabase/migrations/20260519000004_external_kb_embeddings_table.sql,
    supabase/migrations/20260519000006_rag_scrape_runs_table.sql,
    supabase/migrations/20260519000007_rag_cost_ledger_table.sql,
    supabase/migrations/20260519000008_rag_newsletter_subscriptions_table.sql,
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md (D-12, D-13, D-34),
    src/lib/admin/__tests__/audit-logs-rls.test.ts (cross-tenant impersonation fixture reference)
  </read_first>
  <action>
    Write `20260519000009_rag_rls_policies.sql`:
    - Helper note in top comment: "If `public.is_admin_at_least(public.admin_role)` does not yet exist (Phase 24 not shipped), the policies below silently DENY because the function call evaluates as NULL. This is acceptable behavior — admin features remain locked. Once Phase 24 ships its migration, the policies activate without further migration."
    - Defensive function shim: `CREATE OR REPLACE FUNCTION public._rag_is_super() RETURNS boolean LANGUAGE plpgsql STABLE SECURITY DEFINER SET search_path = extensions, public, pg_temp AS $$ BEGIN BEGIN RETURN public.is_admin_at_least('superadmin'::public.admin_role); EXCEPTION WHEN undefined_function THEN RETURN false; WHEN undefined_object THEN RETURN false; END; END $$;`
    - `CREATE OR REPLACE FUNCTION public._rag_is_admin() RETURNS boolean LANGUAGE plpgsql STABLE SECURITY DEFINER SET search_path = extensions, public, pg_temp AS $$ BEGIN BEGIN RETURN public.is_admin_at_least('admin'::public.admin_role); EXCEPTION WHEN undefined_function THEN RETURN false; WHEN undefined_object THEN RETURN false; END; END $$;`
    - `GRANT EXECUTE ON FUNCTION public._rag_is_super() TO authenticated, anon;` and same for `_rag_is_admin`.

    Per table policies:
    - `rag_topics`: SELECT/INSERT/UPDATE/DELETE policies all `USING (public._rag_is_super())` (D-12 super-admin only). `REVOKE INSERT, UPDATE, DELETE ON public.rag_topics FROM authenticated, anon;`
    - `rag_sources`: SELECT for `public._rag_is_admin()` (admin+ can read for retrieval-extension UI); INSERT/UPDATE/DELETE for `public._rag_is_super()` only.
    - `rag_chunks`: SELECT for `public._rag_is_admin()` OR (`published_at IS NOT NULL AND retracted_at IS NULL`) (anon read of published chunks for /research hub). INSERT/UPDATE/DELETE for `public._rag_is_admin()` (admin can review). Direct revoke `REVOKE INSERT, UPDATE, DELETE ON public.rag_chunks FROM authenticated, anon;`
    - `external_kb_embeddings`: SELECT allowed for `authenticated` (needed by retrieval Edge Fn); INSERT/UPDATE/DELETE only via SECURITY DEFINER (revoke all DML from authenticated and anon).
    - `rag_scrape_runs`: SELECT for `public._rag_is_admin()`; revoke DML from authenticated/anon.
    - `rag_cost_ledger`: SELECT for `public._rag_is_admin()`; revoke DML from authenticated/anon.
    - `rag_budget_caps`: SELECT for `public._rag_is_admin()`; UPDATE for `public._rag_is_super()` (cost-cap edits).
    - `rag_newsletter_subscriptions`: SELECT/UPDATE for `user_id = auth.uid()` (users manage own subscription); SELECT all for `public._rag_is_admin()` (admin view subscriber counts).
    - `rag_topic_audit`: SELECT for `public._rag_is_admin()`; revoke DML (only trigger-owner can write).

    Write `src/lib/rag/__tests__/rls-matrix.test.ts` using the `admin.generateLink + /auth/v1/verify via PLAIN fetch (ES256-compat)` pattern from [[reference_rls_fixture_gotruechient_flake]]:
    - File-scoped TEST_SLUG_PREFIX (e.g., `p50-rls-${randomUUID().slice(0,8)}`) per [[feedback_rls_per_file_slug_prefix]].
    - Fixtures: super-admin user, plain admin user, regular authenticated user, anon (no JWT).
    - Matrix tests per table:
      - super-admin can SELECT, INSERT, UPDATE, DELETE on rag_topics.
      - plain admin can SELECT but cannot INSERT on rag_topics (expect 403).
      - regular user CANNOT SELECT on rag_topics.
      - anon CAN SELECT published `rag_chunks` rows only; CANNOT SELECT queued.
      - regular user CAN SELECT own `rag_newsletter_subscriptions` row; CANNOT SELECT other users' rows.
      - regular user CAN UPDATE own newsletter subscription (set unsubscribed_at) but CANNOT UPDATE other users'.
    - afterAll: cleanup test rows by TEST_SLUG_PREFIX.
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && supabase db push --linked --dry-run | grep -v Skipping && vitest run src/lib/rag/__tests__/rls-matrix.test.ts"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `20260519000009_rag_rls_policies.sql` exists.
    - `_rag_is_super()` and `_rag_is_admin()` shim functions handle missing Phase 24 helper gracefully (return false).
    - Every Phase 50 table has explicit SELECT + DML policies (no implicit deny via missing policy on RLS-enabled table).
    - File-scoped TEST_SLUG_PREFIX used in `rls-matrix.test.ts` per memory rule.
    - Cross-tenant impersonation fixture pattern from `[[reference_rls_fixture_gotruechient_flake]]` followed (admin.generateLink + plain fetch).
    - All RLS matrix tests pass.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run `supabase db reset && supabase db push --linked --dry-run` — zero `Skipping` warnings; all 9 migrations apply cleanly.
- Run `vitest run src/lib/rag/__tests__/` — all 4 test files green.
- Grep: every SECURITY DEFINER function in the 9 migrations declares `set search_path = extensions, public, pg_temp`.
- Grep: every `rag_*` table has `ENABLE ROW LEVEL SECURITY`.
- `_rag_is_super()` and `_rag_is_admin()` defined as defensive shims that no-op when Phase 24 helper missing.
</verification>
