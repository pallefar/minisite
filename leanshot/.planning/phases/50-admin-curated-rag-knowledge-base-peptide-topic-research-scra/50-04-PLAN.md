---
phase: 50-admin-curated-rag-knowledge-base-peptide-topic-research-scra
plan: 04
type: execute
wave: 2
depends_on: [50-01, 50-02, 50-03]
files_modified:
  - supabase/functions/rag-scrape-runner/index.ts
  - supabase/functions/rag-scrape-runner/firecrawl.ts
  - supabase/functions/rag-scrape-runner/diff-detector.ts
  - supabase/functions/rag-scrape-runner/cost-ledger.ts
  - supabase/functions/_shared/sentry.ts
  - supabase/migrations/20260519000011_rag_scrape_cron.sql
  - src/lib/rag/__tests__/scrape-runner.test.ts
  - src/lib/rag/__tests__/cron-orchestrator.test.ts
  - src/lib/rag/__tests__/diff-detector.test.ts
  - src/lib/rag/__tests__/cost-ledger.test.ts
  - supabase/functions/rag-scrape-runner/__tests__/integration.test.ts
autonomous: false
requirements: []
user_setup:
  - service: firecrawl
    why: "Firecrawl API key required for the scraper to call /v1/scrape and /v1/crawl"
    env_vars: [FIRECRAWL_API_KEY]
    dashboard_config:
      - task: "Sign up at firecrawl.dev (Starter plan or higher), mint API key, then run `supabase secrets set FIRECRAWL_API_KEY=fc-xxx --linked` from the project root. Health check probe at startup will log a warning and no-op if missing (per [[reference_vendor_gated_send_health_check]]) so deploy never fails on missing key."
        location: "https://firecrawl.dev/app/api-keys"

must_haves:
  truths:
    - "D-01 + D-07: curated topic refuses scraping non-allowlist domains; open-web topic uses Firecrawl /v1/crawl with seed URL; on-label gating ENUM excludes peptide-bro forums"
    - "D-02: scraper calls @mendable/firecrawl-js v1.x via esm.sh (NOT npm:); imports work in Deno deploy"
    - "D-03: respects robots.txt — calls /v1/robots first; stores only excerpt + summary + canonical URL in rag_chunks (no full_text column populated)"
    - "D-09: pg_cron orchestrator fires every 5 minutes; selects topics where next_scrape_at <= now() AND deleted_at IS NULL; updates next_scrape_at per cadence after each run"
    - "D-19: diff-detect computes Levenshtein-ratio on existing approved chunk content_hash vs new excerpt; >=20% diff OR new section markers triggers status='re-queued'; <20% auto-applies (updates excerpt without re-queue)"
    - "D-30: every Firecrawl + OpenAI + Anthropic + Resend vendor call writes a rag_cost_ledger row with amount_usd; cost-ledger helper computes MTD against rag_budget_caps; 80% threshold triggers email; 100% sets rag_sources.paused_at and refuses further scrapes"
    - "D-31: per-source failure handling: 3 attempts exponential backoff (1m/5m/15m), Sentry capture with tag rag.scraper.failed, set rag_sources.health='failing' + paused_at after 3 consecutive failed runs"
    - "rag-scrape-runner Edge Fn writes one rag_scrape_runs row per topic-run; status field tracks running|ok|partial|failed; captures rag_scrape_run event server-side via captureRagEvent helper from Plan 50-03"
    - "rag_chunks INSERT uses ON CONFLICT (topic_id, source_id, content_hash) DO NOTHING for idempotent re-runs"
  artifacts:
    - "supabase/functions/rag-scrape-runner/index.ts"
    - "supabase/functions/rag-scrape-runner/firecrawl.ts"
    - "supabase/functions/rag-scrape-runner/diff-detector.ts"
    - "supabase/functions/rag-scrape-runner/cost-ledger.ts"
    - "supabase/functions/_shared/sentry.ts"
    - "supabase/migrations/20260519000011_rag_scrape_cron.sql"
    - "src/lib/rag/__tests__/scrape-runner.test.ts"
    - "src/lib/rag/__tests__/cron-orchestrator.test.ts"
    - "src/lib/rag/__tests__/diff-detector.test.ts"
    - "src/lib/rag/__tests__/cost-ledger.test.ts"
    - "supabase/functions/rag-scrape-runner/__tests__/integration.test.ts"
  key_links:
    - "pg_cron `rag-scrape-tick` → net.http_post → rag-scrape-runner Edge Fn → Firecrawl REST → rag_chunks INSERT + rag_cost_ledger INSERT + rag_scrape_runs INSERT"
    - "rag_budget_caps + rag_mtd_spend_by_vendor() (from Plan 50-01) → cost-ledger.ts hard-gate at 100%"
    - "Plan 50-03 captureRagEvent('rag_scrape_run') called once per topic-run"
---

<objective>
Ship the cron-driven scrape pipeline: pg_cron tick → Edge Fn → Firecrawl (curated allowlist OR open-web discovery) → robots.txt enforcement → excerpt-only storage → diff-detect re-validation → cost-ledger gating → Sentry alert + auto-pause on repeated failure. Stores chunks in `rag_chunks` with `status='queued'` (Tier-B/C) or pre-tagged for auto-publish (Tier-A; published_at populated by Plan 50-06 review pipeline after Tier-A path is finalized).

Purpose: this is the heart of Phase 50. Without this Edge Fn, no external content ever lands in `rag_chunks` and the entire RAG pipeline is decorative. Cron orchestration AND failure handling AND cost gating are all first-class — under-doing any one of them breaks D-30 / D-31.
Output: 1 Edge Fn (4 files) + 1 shared Sentry helper + 1 cron migration + 5 vitest + 1 Deno integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md
@.planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-RESEARCH.md
@supabase/functions/_shared
@supabase/migrations/20260519000007_rag_cost_ledger_table.sql

<interfaces>
- Firecrawl JS SDK `@mendable/firecrawl-js@1.x` imported from `https://esm.sh/` per [[reference_supabase_edge_function_deploy]] (NOT npm: scheme — Edge Fn bundler ignores import_map.json).
- Phase 50 Plan 50-01 tables: `rag_topics`, `rag_sources`, `rag_chunks`, `rag_scrape_runs`, `rag_cost_ledger`, `rag_budget_caps`.
- Phase 50 Plan 50-01 function: `public.rag_mtd_spend_by_vendor()`.
- Phase 50 Plan 50-03 server helper: `captureRagEvent` from `_shared/posthog-server.ts`.
- Phase 24 SECURITY DEFINER `_rag_is_super`/`_rag_is_admin` — Edge Fn runs as service_role, so RLS bypass is intentional (Plan 50-01 RLS revokes DML from authenticated/anon; service_role can write).
- Sentry SDK existing wiring at `supabase/functions/_shared/sentry.ts` (Phase 25 pattern; Plan 50-04 extends if missing).
- esm.sh import for Firecrawl: `import { FirecrawlApp } from "https://esm.sh/@mendable/firecrawl-js@1";`

Memory references:
- `[[reference_supabase_edge_function_deploy]]` — esm.sh imports, Function Secrets, UAT-probe pattern.
- `[[reference_supabase_migration_gotchas]]` — SECURITY DEFINER + GUC + IMMUTABLE-predicate rules.
- `[[reference_vendor_gated_send_health_check]]` — startup health probe pattern; missing FIRECRAWL_API_KEY logs warning and no-ops scrape (does NOT fail deploy).
- `[[reference_deno_test_discovery]]` — Edge Fn test filename `<name>.test.ts` (strict).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Firecrawl wrapper module with robots.txt + on-label gating</name>
  <files>supabase/functions/rag-scrape-runner/firecrawl.ts</files>
  <read_first>
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-RESEARCH.md (Firecrawl JS SDK snippet),
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md (D-01, D-02, D-03, D-07)
  </read_first>
  <action>
    Write `supabase/functions/rag-scrape-runner/firecrawl.ts` (Deno-compat):
    - Top: `import { FirecrawlApp } from "https://esm.sh/@mendable/firecrawl-js@1";`
    - Export `class FirecrawlClient { constructor(private apiKey: string) {} }`.
    - Method `async healthCheck(): Promise<{ ok: boolean; reason?: string }>` — startup probe per [[reference_vendor_gated_send_health_check]]. Calls Firecrawl `/v1/scrape` with `https://example.com` and 5s timeout; returns `{ ok: true }` on success; `{ ok: false, reason }` on failure or missing key (caller logs warning, does not throw).
    - Method `async respectsRobots(url: string): Promise<boolean>` — fetches `https://${domain}/robots.txt`, parses, returns true if scraping path allowed for user-agent `*`. Use a tiny inline parser (only need to check Allow/Disallow rules — no need for full robotstxt spec).
    - Method `async scrapeSingle(url: string, opts: { timeoutMs?: number } = {}): Promise<{ markdown: string; canonicalUrl: string; scrapedAt: string }>` — wraps `fc.scrapeUrl(url, { formats: ['markdown'], onlyMainContent: true, waitFor: 1500, timeout: opts.timeoutMs ?? 30_000 })`. On failure, throws `FirecrawlError`.
    - Method `async crawlDiscover(seedUrl: string, opts: { limit?: number } = {}): Promise<Array<{ url: string; markdown: string }>>` — wraps `fc.crawlUrl(seedUrl, { limit: opts.limit ?? 10, scrapeOptions: { formats: ['markdown'], onlyMainContent: true } })`.
    - Export `class FirecrawlError extends Error { constructor(msg: string, public retriable: boolean) { super(msg); } }`.
    - On-label gating: export `const EXCLUDED_DOMAINS = ['peptidesources.com','peptidesciences.com','reddit.com/r/peptides','reddit.com/r/Peptides','musclechemistry.com','anabolicminds.com'] as const;` Helper `function isExcludedDomain(url: string): boolean` — checks if URL hostname matches any excluded domain (case-insensitive).
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && deno check supabase/functions/rag-scrape-runner/firecrawl.ts && grep -q 'esm.sh/@mendable/firecrawl-js@1' supabase/functions/rag-scrape-runner/firecrawl.ts && ! grep 'npm:@mendable' supabase/functions/rag-scrape-runner/firecrawl.ts"
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists; Deno `check` passes.
    - Firecrawl imported from esm.sh, NOT npm: (bundler-safe per memory).
    - `healthCheck()` returns failure gracefully (does not throw on missing key).
    - `EXCLUDED_DOMAINS` constant covers peptide-bro forums per D-07.
    - `respectsRobots` parses User-agent: * Disallow rules correctly.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write diff-detector + cost-ledger modules</name>
  <files>supabase/functions/rag-scrape-runner/diff-detector.ts, supabase/functions/rag-scrape-runner/cost-ledger.ts</files>
  <read_first>
    supabase/migrations/20260519000007_rag_cost_ledger_table.sql,
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md (D-19, D-30)
  </read_first>
  <action>
    Write `supabase/functions/rag-scrape-runner/diff-detector.ts`:
    - Export `function computeDiffRatio(oldText: string, newText: string): number` — returns 0..1 ratio of changed characters using a simple Levenshtein-like distance / max-length. For perf, normalize whitespace then compare hash of token bags. (Full Levenshtein at scale is too slow; we use token-bag diff which is good-enough for D-19's 20% threshold.)
    - Export `function detectNewSections(oldMarkdown: string, newMarkdown: string): boolean` — extracts `^#{1,3} .+$` headings from both, returns true if newMarkdown has any heading not present in oldMarkdown.
    - Export `function shouldRequeue(oldText: string, newText: string): boolean` — `computeDiffRatio(...) >= 0.20 || detectNewSections(oldText, newText)`.
    - Export `function computeContentHash(text: string): string` — `crypto.subtle.digest('SHA-256', new TextEncoder().encode(text.trim().toLowerCase().replace(/\\s+/g, ' ')))` returned as hex string.

    Write `supabase/functions/rag-scrape-runner/cost-ledger.ts`:
    - Export `async function logVendorCost(client: SupabaseClient, args: { vendor: 'firecrawl'|'openai_embed'|'anthropic_summary'|'resend'; amountUsd: number; topicId?: string; sourceId?: string; action: string; meta?: Record<string, unknown> }): Promise<void>` — INSERT into rag_cost_ledger.
    - Export `async function getMtdStatus(client: SupabaseClient, vendor: string): Promise<{ pctUsed: number; mtdUsd: number; capUsd: number; status: 'ok'|'warn'|'capped' }>` — call `rag_mtd_spend_by_vendor()`, filter by vendor, classify (<80=ok, 80-99=warn, >=100=capped).
    - Export `async function gateOrThrow(client: SupabaseClient, vendor: string): Promise<void>` — if status === 'capped', throw `CostCapExceededError` with message; caller should set source.paused_at and return without calling vendor.
    - Export `async function sendEightyPctEmail(client: SupabaseClient, vendor: string, mtdUsd: number, capUsd: number): Promise<void>` — idempotent (writes a row to a `rag_budget_alerts_sent` table OR uses `rag_budget_caps.last_paused_at` to dedupe within the month). For MVP, send only if no row for this vendor + month exists. (If `rag_budget_alerts_sent` table doesn't exist yet, this is a no-op TODO and the function should log a warning rather than fail — Plan 50-09 finalizes the email send path.)
    - Estimated costs (used by callers when vendor doesn't return actual cost in response):
      - Firecrawl scrape: $0.001 / single-URL scrape; $0.002 / crawl-discovered URL (current Starter plan pricing as of 2026-05).
      - OpenAI text-embedding-3-small: $0.02 / 1M tokens, ~1500 tokens avg per chunk = ~$0.00003 per chunk.
      - Anthropic claude-sonnet-4 summary: ~$0.015 / 1K input tokens × 2K input + ~$0.075 / 1K output tokens × 1K output ≈ $0.105 per chunk.
      - Resend transactional: ~$0.0001 / email (Pro tier).
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && deno check supabase/functions/rag-scrape-runner/diff-detector.ts supabase/functions/rag-scrape-runner/cost-ledger.ts && vitest run src/lib/rag/__tests__/diff-detector.test.ts src/lib/rag/__tests__/cost-ledger.test.ts"
    </automated>
  </verify>
  <acceptance_criteria>
    - 2 files exist; Deno `check` passes.
    - `shouldRequeue` returns true at exactly 20% diff (boundary test).
    - `computeContentHash` is deterministic across whitespace normalization (test: same hash for `"  foo bar"` and `"foo bar"`).
    - `gateOrThrow` throws when status === 'capped'.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Write rag-scrape-runner Edge Function entrypoint</name>
  <files>supabase/functions/rag-scrape-runner/index.ts, supabase/functions/_shared/sentry.ts</files>
  <read_first>
    supabase/functions/rag-scrape-runner/firecrawl.ts,
    supabase/functions/rag-scrape-runner/diff-detector.ts,
    supabase/functions/rag-scrape-runner/cost-ledger.ts,
    supabase/functions/_shared/posthog-server.ts (captureRagEvent from Plan 50-03),
    .planning/phases/50-admin-curated-rag-knowledge-base-peptide-topic-research-scra/50-CONTEXT.md (D-22, D-31)
  </read_first>
  <action>
    Write `supabase/functions/_shared/sentry.ts` if not present (Phase 25 carry-forward shim):
    - `export function sentryCapture(err: unknown, tags: Record<string, string> = {}): void` — best-effort; if `SENTRY_DSN` env missing, console.error and return; else POST to Sentry minidump endpoint (or use `https://esm.sh/@sentry/deno`).

    Write `supabase/functions/rag-scrape-runner/index.ts`:
    - Standard Supabase Edge Fn entry: `import { serve } from "https://deno.land/std@0.218.0/http/server.ts";`
    - Inputs accepted: `{ topic_id?: string }` POST body. If `topic_id` provided → scrape that topic. If empty body → scrape all topics where `next_scrape_at <= now()` AND `deleted_at IS NULL` (cron-tick mode).
    - For each topic to scrape:
      1. Lock the row with `UPDATE rag_topics SET last_scraped_at = now() WHERE id = $1 RETURNING *` (idempotent SELECT-FOR-UPDATE pattern via UPDATE-with-RETURNING).
      2. Insert `rag_scrape_runs` row with `status='running'`.
      3. Resolve source list: if `posture='curated'` → all `rag_sources` rows that have an admin-configured association (for v1 simplicity, scrape ALL non-paused active Tier-A and Tier-B sources for curated topics — admin per-topic source pinning deferred to v1.4); if `posture='open-web'` → Firecrawl `/v1/crawl` on a Google-search-style seed URL composed from `topic.query` (research note: open-web mode v1 uses a `https://duckduckgo.com/html/?q=${encodeURIComponent(topic.query)}` seed).
      4. For each source: skip if `paused_at IS NOT NULL`; otherwise:
         - `await gateOrThrow(client, 'firecrawl')` — short-circuit if budget exhausted.
         - `if (!await fc.respectsRobots(url))` → skip with reason in scrape_runs.error_message.
         - 3-attempt exponential backoff (1m / 5m / 15m sleeps): wrap `fc.scrapeSingle(url)` in retry. On final failure, increment `rag_sources.consecutive_failures`; if reaches 3, set `health='failing', paused_at=now(), paused_reason='3 consecutive failed scrapes'`, call `sentryCapture(err, {'rag.scraper.failed': '1', source_id, topic_id})`.
         - On success: `await logVendorCost(client, {vendor: 'firecrawl', amountUsd: 0.001, topicId, sourceId, action: 'scrape_single'})`.
         - Compute `contentHash = await computeContentHash(markdown)`.
         - Check for existing chunk: `SELECT * FROM rag_chunks WHERE topic_id = $1 AND source_id = $2 AND content_hash = $3 LIMIT 1` — if exists, skip (idempotent re-run).
         - Otherwise check diff against prior approved chunk: `SELECT source_text_excerpt FROM rag_chunks WHERE topic_id = $1 AND source_id = $2 AND published_at IS NOT NULL ORDER BY created_at DESC LIMIT 1`. If `shouldRequeue(prior, new)` → INSERT with `status='re-queued'`. Otherwise INSERT new with `status='queued'`.
         - Note: rag-summarize-and-chunk Edge Fn (Plan 50-05) is invoked separately (next paragraph) — this scrape runner only persists raw `source_text_excerpt`. After scrape runner finishes a topic, it calls `await client.functions.invoke('rag-summarize-and-chunk', { body: { topic_id } })` to chain summarization (fire-and-forget).
      5. Update `rag_scrape_runs` row: completed_at, status (ok/partial/failed), chunks_found.
      6. Set `rag_topics.next_scrape_at` based on cadence: daily → `now() + 1 day`, weekly → `now() + 7 days`, monthly → `now() + 30 days`, manual → NULL.
      7. `await captureRagEvent(distinctId='rag-system', name='rag_scrape_run', properties={topic_id, source_count, chunks_found, duration_ms, status, cost_usd})`.
      8. Response: 200 with `{ ok: true, topics_scraped, chunks_found }`.

    On any unhandled error in the top-level: `sentryCapture(err, {'rag.scraper.failed': '1'})` + return 500.

    Health check exposure: GET `/healthz` returns `{ ok: boolean, vendors: { firecrawl, openai, anthropic } }` from `fc.healthCheck()`. Used by smoke probes per `[[reference_supabase_edge_function_deploy]]` UAT pattern.
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && deno check supabase/functions/rag-scrape-runner/index.ts supabase/functions/_shared/sentry.ts && supabase functions deploy rag-scrape-runner --no-verify-jwt --linked"
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists; Deno check passes.
    - Topic locked via UPDATE-with-RETURNING (idempotent against concurrent cron ticks).
    - 3-attempt backoff (1m/5m/15m); after 3 consecutive failed runs, source auto-paused + Sentry capture.
    - Cost gating short-circuits before any Firecrawl call when status === 'capped'.
    - captureRagEvent fired once per topic-run with duration_ms + cost_usd populated.
    - `supabase functions deploy` succeeds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Write pg_cron migration for scrape orchestrator</name>
  <files>supabase/migrations/20260519000011_rag_scrape_cron.sql</files>
  <read_first>
    supabase/migrations/20260518000007_audit_archive_cron.sql (Phase 24 cron pattern for invoking Edge Fn from net.http_post)
  </read_first>
  <action>
    Write `supabase/migrations/20260519000011_rag_scrape_cron.sql`. Follow the Phase 24 cron pattern (see `supabase/migrations/20260518000007_audit_archive_cron.sql`) exactly. Required identifiers:
    - Idempotency wrapper: `DO $$ BEGIN PERFORM cron.unschedule('rag-scrape-tick') WHERE EXISTS (SELECT 1 FROM cron.job WHERE jobname = 'rag-scrape-tick'); EXCEPTION WHEN OTHERS THEN null; END $$;`
    - Cron registration: `SELECT cron.schedule('rag-scrape-tick', '*/5 * * * *', '<body>')` where `<body>` is a `SELECT net.http_post(...)` call with three named args: `url := 'https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/rag-scrape-runner'`, `headers := jsonb_build_object('Authorization', 'Bearer ' || (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'SUPABASE_SERVICE_ROLE_KEY'), 'Content-Type', 'application/json')`, and `body := '{}'::jsonb`.
    - Use `$$` dollar-quoting for the inner SQL body string passed to `cron.schedule(...)` (same convention as `20260518000007_audit_archive_cron.sql`).
    - Cron schedule string: literal `*/5 * * * *` (every 5 minutes).
    - Job name: literal `rag-scrape-tick`.
    - Note for runtime: vault key MUST exist (see Phase 24 user_setup); if missing, cron call returns 401 and the Edge Fn writes a rag_scrape_runs row with `status='failed', error_message='missing vault SUPABASE_SERVICE_ROLE_KEY'` and emits a Sentry alert from the Edge Fn itself.
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && supabase db push --linked --dry-run | grep -v Skipping && supabase db query --linked 'SELECT jobname FROM cron.job WHERE jobname = \\'rag-scrape-tick\\';' | grep rag-scrape-tick"
    </automated>
  </verify>
  <acceptance_criteria>
    - cron entry `rag-scrape-tick` exists at schedule `*/5 * * * *`.
    - Idempotent — re-running the migration replaces the cron entry without error.
    - Migration applies with zero `Skipping`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Write all unit + integration vitest + Deno test suites for Wave 2 scrape</name>
  <files>src/lib/rag/__tests__/scrape-runner.test.ts, src/lib/rag/__tests__/cron-orchestrator.test.ts, src/lib/rag/__tests__/diff-detector.test.ts, src/lib/rag/__tests__/cost-ledger.test.ts, supabase/functions/rag-scrape-runner/__tests__/integration.test.ts</files>
  <read_first>
    supabase/functions/rag-scrape-runner/firecrawl.ts,
    supabase/functions/rag-scrape-runner/diff-detector.ts,
    supabase/functions/rag-scrape-runner/cost-ledger.ts,
    supabase/functions/rag-scrape-runner/index.ts
  </read_first>
  <action>
    `scrape-runner.test.ts` (vitest — mocks Firecrawl):
    - `it('curated topic refuses non-allowlist URL')` — D-01: when topic.posture='curated' and source not in rag_sources, scrape rejects.
    - `it('open-web topic calls Firecrawl /v1/crawl with seed URL')` — D-02.
    - `it('stores excerpt + canonical URL only; honors robots disallow')` — D-03.
    - `it('refuses scrape on excluded domains')` — D-07 (EXCLUDED_DOMAINS list).
    - `it('3-attempt exponential backoff (1m/5m/15m)')` — D-31; mock clock, assert sleeps.
    - `it('auto-pause source after 3 consecutive failed runs')` — D-31; assert paused_at set + Sentry capture called.

    `cron-orchestrator.test.ts`:
    - `it('next_scrape_at computed from cadence: daily → +1d, weekly → +7d, monthly → +30d, manual → NULL')` — D-09.
    - `it('cron tick only picks topics where next_scrape_at <= now() AND deleted_at IS NULL')`.

    `diff-detector.test.ts`:
    - `it('shouldRequeue returns true at >=20% diff')` — D-19.
    - `it('shouldRequeue returns true on new section markers regardless of diff')` — D-19.
    - `it('shouldRequeue returns false at <20% diff and no new sections')`.
    - `it('computeContentHash deterministic across whitespace normalization')`.

    `cost-ledger.test.ts`:
    - `it('logVendorCost writes a row with all required fields')`.
    - `it('getMtdStatus returns ok when MTD < 80% of cap')`.
    - `it('getMtdStatus returns warn when 80% <= MTD < 100%')`.
    - `it('getMtdStatus returns capped when MTD >= 100%')` — D-30.
    - `it('gateOrThrow throws CostCapExceededError when status === capped')`.
    - `it('sendEightyPctEmail is idempotent per vendor+month')`.

    `supabase/functions/rag-scrape-runner/__tests__/integration.test.ts` (Deno test, filename `<name>.test.ts` per memory):
    - `Deno.test('full pipeline: mock Firecrawl → INSERT rag_chunks → INSERT rag_cost_ledger → captureRagEvent')` — uses Deno test mocking + a local Supabase instance. Verify state after one mocked scrape run.
    - `Deno.test('health check returns vendor status')` — calls GET /healthz with FIRECRAWL_API_KEY unset, expects `{ ok: false, vendors: { firecrawl: { ok: false } } }` per [[reference_vendor_gated_send_health_check]].
  </action>
  <verify>
    <automated>
      bash -c "cd leanshot && vitest run src/lib/rag/__tests__/scrape-runner.test.ts src/lib/rag/__tests__/cron-orchestrator.test.ts src/lib/rag/__tests__/diff-detector.test.ts src/lib/rag/__tests__/cost-ledger.test.ts && deno test supabase/functions/rag-scrape-runner/__tests__/integration.test.ts --allow-env --allow-net"
    </automated>
  </verify>
  <acceptance_criteria>
    - All test files exist; all test cases pass.
    - D-01, D-02, D-03, D-07, D-09, D-19, D-30, D-31 each covered by at least one test assertion.
    - Deno integration test validates end-to-end scrape → DB writes.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `vitest run src/lib/rag/__tests__/` + `deno test supabase/functions/rag-scrape-runner/__tests__/` — all green.
- `supabase functions deploy rag-scrape-runner --linked --no-verify-jwt` — deploy succeeds.
- `supabase db query --linked "SELECT * FROM cron.job WHERE jobname = 'rag-scrape-tick';"` — returns 1 row.
- Manual UAT: `curl -X POST https://<project>.supabase.co/functions/v1/rag-scrape-runner` returns 200 + chunks inserted (admin gates user_setup with FIRECRAWL_API_KEY first).
</verification>
