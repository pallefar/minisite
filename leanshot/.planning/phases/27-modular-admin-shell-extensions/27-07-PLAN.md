---
phase: 27-modular-admin-shell-extensions
plan: 07
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/migrations/20260601000060_bulk_undo_token_purge_cron.sql
  - leanshot/e2e/admin-bulk-undo-integration.spec.ts
autonomous: true
requirements: [ADMIN-04]
tags: [admin, bulk-actions, undo, cron, purge]

must_haves:
  truths:
    - "pg_cron job 'bulk-undo-token-purge' runs every minute and (1) deletes bulk_action_undo_token rows where expires_at < now() AND (2) deletes events_mirror rows older than 30 days"
    - "Purge is pure SQL (NOT an Edge Function) per RESEARCH correction #3 + PATTERNS B3 recommendation — saves an Edge Fn deployment + complexity"
    - "events_mirror retention of 30 days bounds the table size — required so the funnel-anomaly-cron baseline-window queries (max 29-day lookback) stay under the 5-min cron budget as event volume grows"
    - "End-to-end undo flow proven via Playwright e2e: bulk action → undo banner appears → wait 65s → click Undo → expect token_expired error (cron has purged the token)"
    - "Cron schedule '* * * * *' (every minute) verified no collision with other Phase 27 crons (anomaly */5, cohort matview 7,22,37,52, bulk-worker * * * * *) — purge AND bulk-worker both run every minute but on different jobnames, pg_cron parallelizes"
    - "Purge cron also handles cleanup of bulk_action_undo_token rows orphaned by ban-then-immediately-quit admin session"
  artifacts:
    - path: "supabase/migrations/20260601000060_bulk_undo_token_purge_cron.sql"
      provides: "Pure SQL pg_cron schedule for undo-token purge"
      contains: "cron.schedule"
    - path: "leanshot/e2e/admin-bulk-undo-integration.spec.ts"
      provides: "Playwright end-to-end of complete undo lifecycle including expiry"
  key_links:
    - from: "pg_cron 'bulk-undo-token-purge'"
      to: "bulk_action_undo_token DELETE WHERE expires_at < now()"
      via: "inline SQL inside cron.schedule call body"
      pattern: "delete from .*bulk_action_undo_token"
---

<objective>
Ship the bulk_action_undo_token purge cron for ADMIN-04 cleanup hygiene + complete the end-to-end undo flow integration test. Per RESEARCH correction #3 and PATTERNS B3 alternative recommendation: this purge runs as PURE SQL inside the cron schedule body (NOT an Edge Function) — saves a deployment, zero HTTP overhead, idempotent by design.

Purpose: ADMIN-04 D-03 — undo tokens have a 60-second TTL; after expiry they must be purged so the bulk_action_undo_token table stays small + so the undo RPC consistently returns 'token_expired' (rather than 'invalid token shape' or other downstream errors caused by stale rows). Also provides the load-bearing test that proves the entire undo+expiry lifecycle works end-to-end across Plans 27-01 + this plan.

Output: 1 migration (pure SQL cron schedule, ~10 lines), 1 Playwright e2e that exercises the full lifecycle including the 60-second wait.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-CONTEXT.md
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-RESEARCH.md
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-PATTERNS.md
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-01-PLAN.md
@supabase/migrations/20270101000009_click_baseline_refresh_cron.sql

<interfaces>
<!-- Cross-plan / cross-phase contracts -->

Plan 27-01 carry (this plan DEPENDS on schema from 27-01):
- public.bulk_action_undo_token table with columns: token uuid pk, issued_to uuid, action_type text, reverse_payload jsonb, expires_at timestamptz default now()+60s, created_at
- Index idx_bulk_undo_expires on (expires_at)
- RLS enabled; ZERO write policies — only SECDEF RPCs + service_role
- admin_bulk_action_undo RPC raises 'token_expired' (22023) on expires_at < now() OR missing token

Pure SQL cron pattern analog:
- supabase/migrations/20270101000009_click_baseline_refresh_cron.sql — uses `select cron.schedule('jobname', 'schedule', $$ <pure SQL> $$);` with no HTTP-post. Same shape used here.

Cron collision matrix verification (already documented in 27-04 + 27-06):
- bulk-undo-token-purge: '* * * * *' (every minute)
- admin-bulk-job-worker: '* * * * *' (Plan 27-06)
- funnel-anomaly-cron: '*/5 * * * *' (Plan 27-04)
- cohort-membership-refresh: '7,22,37,52 * * * *' (Plan 27-02)
- existing: lifecycle-welcome '0 */4 * * *', lifecycle-behavior '*/15 * * * *', audit-archive 'day-1 03:00', etc.
- ACCEPTABLE: pg_cron parallelizes across distinct jobnames; per-jobname single-instance overlap protection applies

End-to-end undo lifecycle (the test this plan ships):
1. Admin invokes bulk ban on 5 users → admin_bulk_action_execute returns {mode:'sync', undo_token:uuid}
2. AdminUndoBanner renders with 60-second countdown
3. Test waits 65 seconds
4. bulk-undo-token-purge cron has fired at least once in the interval and purged the expired token
5. Test clicks Undo button → admin_bulk_action_undo(token) returns 'token_expired' (22023)
6. UI surfaces "Undo window expired" toast
7. Assert: 5 users remain banned (no rollback occurred); 1 audit_logs row per user with action_name='bulk_ban' but NO 'bulk_action_undone' rows
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pure SQL pg_cron schedule for bulk-undo-token-purge</name>
  <files>
    supabase/migrations/20260601000060_bulk_undo_token_purge_cron.sql
  </files>
  <behavior>
    - cron.schedule('bulk-undo-token-purge', '* * * * *', $$ delete from public.bulk_action_undo_token where expires_at < now(); delete from public.events_mirror where created_at < now() - interval '30 days'; $$) — TWO retention sweeps in the same cron body (bulk_action_undo_token TTL + events_mirror 30-day retention)
    - Header docblock cites RESEARCH correction #3 (pure SQL beats Edge Fn for this purpose)
    - Header docblock documents BOTH purge targets + the 30-day retention rationale (anomaly cron lookback maxes at 29 days; 30-day retention covers it)
    - Header docblock documents the collision matrix verification
    - Idempotent: re-running has no side effect (DELETE WHERE on already-purged rows is a no-op)
  </behavior>
  <action>
    Create supabase/migrations/<next-14-digit>_bulk_undo_token_purge_cron.sql. Header docblock structure follows supabase/migrations/20270101000009_click_baseline_refresh_cron.sql analog: "-- Phase 27 D-22 (bulk-undo-token-purge — pure SQL cron). // RESEARCH correction #3: skip Edge Fn; inline SQL is simpler + zero HTTP overhead. // PATTERNS B3 alternative recommendation aligned. // Collision check: '* * * * *' shared with admin-bulk-job-worker; pg_cron parallelizes distinct jobnames; no functional collision. // Cron purges TWO tables: (1) bulk_action_undo_token (60s TTL) and (2) events_mirror (30-day retention — anomaly cron's max lookback is 29 days per Plan 27-04 baseline window, so 30-day cap is safe).". Body: `select cron.schedule('bulk-undo-token-purge', '* * * * *', $$ delete from public.bulk_action_undo_token where expires_at < now(); delete from public.events_mirror where created_at < now() - interval '30 days'; $$);`. Per CONTEXT D-22 + RESEARCH correction #3 + PATTERNS B3.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/migrations/*_bulk_undo_token_purge_cron.sql | head -1 | xargs -I{} bash -c 'grep -q "bulk-undo-token-purge" "{}" && grep -q "\* \* \* \* \*" "{}" && grep -q "delete from public.bulk_action_undo_token" "{}" && grep -q "expires_at < now()" "{}" && grep -q "events_mirror" "{}" && grep -q "interval .30 days" "{}"'</automated>
  </verify>
  <done>Pure SQL cron migration exists; schedule '* * * * *'; idempotent DELETE WHERE pattern; purges BOTH bulk_action_undo_token (TTL) and events_mirror (30-day retention to keep anomaly cron baseline queries under 5-min budget).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Playwright e2e — full undo lifecycle including 60s expiry → token_expired</name>
  <files>
    leanshot/e2e/admin-bulk-undo-integration.spec.ts
  </files>
  <behavior>
    - Test "expired undo token returns token_expired and state remains banned" passes
    - Seeds 5 test users via supabase admin
    - Logs in as admin via addInitScript
    - Navigates to /admin/members, selects 5 users, clicks Ban, confirms
    - Captures the undo_token from the RPC response
    - Asserts AdminUndoBanner visible
    - Waits 65 seconds (sufficient for cron to fire AT LEAST once at the 60s+ mark)
    - Asserts via direct DB query that bulk_action_undo_token row for that token is GONE (purged by cron)
    - Clicks Undo in the banner
    - Awaits the admin_bulk_action_undo RPC response
    - Asserts response error code 22023 (token_expired)
    - Asserts UI shows 'Undo window expired' toast
    - Asserts 5 profiles still have account_state='banned'
    - Asserts ZERO audit_logs rows with action_name='bulk_action_undone' for the 5 target_user_ids
    - afterAll cleanup
  </behavior>
  <action>
    Create leanshot/e2e/admin-bulk-undo-integration.spec.ts using same scaffold as e2e/admin-bulk-actions.spec.ts (Plan 27-01). file-scoped slug `'undo-${Date.now()}-'`. beforeAll: 5 test users + 1 admin profile. addInitScript admin session. Navigate /#/admin/members; select 5 users; click Ban; capture undo_token from response of admin_bulk_action_execute (use page.on('response', ...) to capture). Assert banner visible. Use `await page.waitForTimeout(65000)` — yes this is slow but the test exercises real expiry semantics; if test runtime is unacceptable, an alternative is to use `await admin.from('bulk_action_undo_token').update({expires_at: new Date(Date.now() - 1000).toISOString()}).eq('token', undoToken)` to force-expire BEFORE the cron tick + assert UI behavior + then separately assert cron purges expired rows in a smaller unit test. RECOMMENDED for CI speed: use the force-expire approach + add a separate non-Playwright integration test for the cron purge itself. Both approaches documented. Force-expire approach test body: (1) seed + ban (admin_bulk_action_execute), (2) `await admin.from('bulk_action_undo_token').update({expires_at: new Date(Date.now()-1000).toISOString()}).eq('token', undoToken)`, (3) click Undo, (4) assert error 22023, (5) assert profiles still banned, (6) assert no bulk_action_undone audit row. Plus separately: `await admin.rpc('exec_sql', 'delete from bulk_action_undo_token where expires_at < now()')` then assert table empty for that token — OR just trust pg_cron is enabled by the migration. Per CONTEXT D-03 + 27-VALIDATION Wave 0.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx playwright test e2e/admin-bulk-undo-integration.spec.ts --reporter=line 2>&1 | tail -15 | grep -E 'passed|✓'</automated>
  </verify>
  <done>e2e proves expired-token undo correctly returns token_expired + state unchanged + zero undo audit rows; force-expire approach used for CI speed; force-purge cron behavior optionally exercised separately.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] supabase db push --linked + cron presence probe</name>
  <files>supabase/migrations/</files>
  <action>
    From /Users/karstenhaldan/minisite: `supabase db push --linked 2>&1 | tee /tmp/27-07-push.log`. Grep `^Skipping`. Verify cron: `supabase db query --linked "select jobname, schedule from cron.job where jobname='bulk-undo-token-purge'"` expect `* * * * *`. Verify cron is firing (after waiting ~70s): `supabase db query --linked "select count(*) from cron.job_run_details where jobid in (select jobid from cron.job where jobname='bulk-undo-token-purge') and start_time > now() - interval '5 minutes'"` expect ≥1. Full Phase 27 cron summary: `supabase db query --linked "select jobname, schedule from cron.job where jobname in ('bulk-undo-token-purge','admin-bulk-job-worker','funnel-anomaly-cron','cohort-membership-refresh') order by jobname"` expect 4 rows.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && supabase db query --linked "select count(*) from cron.job where jobname='bulk-undo-token-purge'" 2>&1 | grep -q '^\s*1\s*$' && supabase db query --linked "select count(*) from cron.job where jobname in ('bulk-undo-token-purge','admin-bulk-job-worker','funnel-anomaly-cron','cohort-membership-refresh')" 2>&1 | grep -q '^\s*4\s*$'</automated>
  </verify>
  <done>Pure SQL cron live; all 4 Phase 27 crons present in cron.job table (bulk-undo-token-purge + admin-bulk-job-worker + funnel-anomaly-cron + cohort-membership-refresh).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron → bulk_action_undo_token DELETE | postgres-superuser context (pg_cron); RLS bypassed by design; DELETE is bounded by `expires_at < now()` — cannot accidentally drop unexpired tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07-01 | Tampering | Admin alters bulk_action_undo_token to extend expiry | mitigate | RLS denies UPDATE for authenticated (no policy); only SECDEF RPCs from Plan 27-01 + service_role purge cron touch the table |
| T-27-07-02 | Denial of Service | Purge cron deletes unexpired tokens due to clock skew | mitigate | DELETE WHERE expires_at < now() — Postgres clock used consistently for both INSERT default + DELETE filter; no client-supplied timestamps |
| T-27-07-03 | Information Disclosure | bulk_action_undo_token contains PHI in reverse_payload | accept | reverse_payload from Plan 27-01 stores minimal data (e.g. {account_state:'active'}) — no PHI; documented contract in Plan 27-01 |
| T-27-07-04 | Denial of Service | Purge cron silently fails leaving stale rows forever | mitigate | pg_cron records job_run_details; absence of recent successful runs is visible via supabase db query (manual UAT in 27-VALIDATION) |
</threat_model>

<verification>
- Migration + cron present (Task 3)
- Cron actually firing after deploy (Task 3 secondary probe via cron.job_run_details)
- e2e proves token expiry + UI handling (Task 2)
- Combined with Plan 27-01's e2e: full undo lifecycle covered (Plan 27-01 covers successful undo; this plan covers expired undo)
</verification>

<success_criteria>
- ADMIN-04 D-03 cleanup hygiene satisfied: bulk_action_undo_token rows auto-purged within ~60s of expiry
- Pure SQL approach honored per RESEARCH correction #3 + PATTERNS B3 — no Edge Fn deployment
- Full undo lifecycle proven end-to-end (sync execute + 60s banner + expired undo returns token_expired)
- Status machine ownership: bulk_action_undo_token rows transition CREATE (Plan 27-01) → DELETE (this plan's cron)
- All 4 Phase 27 crons present in cron.job table after this plan ships
</success_criteria>

<output>
After completion, create `.planning/phases/27-modular-admin-shell-extensions/27-07-SUMMARY.md` documenting: pure SQL cron migration shipped (zero Edge Fn — RESEARCH correction #3 honored), the 4 Phase 27 crons summary table (bulk-undo-token-purge, admin-bulk-job-worker, funnel-anomaly-cron, cohort-membership-refresh) with collision analysis, e2e outcome (expired token path), and the choice of force-expire vs wait-65s in the e2e implementation.
</output>
