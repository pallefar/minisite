---
phase: 27-modular-admin-shell-extensions
plan: 05
type: execute
wave: 2
depends_on: [04]
files_modified:
  - supabase/migrations/20260601000040_anomaly_tracked_funnels_admin_rpcs.sql
  - leanshot/src/lib/admin/anomaly/config-api.ts
  - leanshot/src/lib/admin/anomaly/config-api.test.ts
  - leanshot/src/components/admin/anomaly/AdminAnomalyTrackedFunnelsConfig.tsx
  - leanshot/src/components/admin/anomaly/AnomalyConfigPage.tsx
autonomous: true
requirements: [TAXO-05]
tags: [admin, anomaly, config, ui]

must_haves:
  truths:
    - "Superadmin navigates to /admin/anomaly and sees AnomalyConfigPage with two sections: the AdminAnomalyAcknowledgeQueue (alerts list from Plan 27-04) and AdminAnomalyTrackedFunnelsConfig (this plan)"
    - "AdminAnomalyTrackedFunnelsConfig renders all rows from anomaly_tracked_funnels with: event_name, is_enabled toggle, baseline_lookback_days input, sigma_threshold input, created_by, created_at, Save + Delete buttons"
    - "Adding a new funnel: form with event_name (text) + lookback (number) + sigma (number) + Add button; on submit calls anomaly_funnel_define RPC"
    - "Toggling is_enabled or editing thresholds calls anomaly_funnel_update RPC; UI is optimistic with rollback on error"
    - "Deleting a funnel calls anomaly_funnel_delete RPC (superadmin-only); confirmation modal first"
    - "All RPCs gate on is_admin_at_least('superadmin'); non-superadmin gets 'forbidden' 42501"
  artifacts:
    - path: "supabase/migrations/20260601000040_anomaly_tracked_funnels_admin_rpcs.sql"
      provides: "3 SECDEF RPCs: anomaly_funnel_define, anomaly_funnel_update, anomaly_funnel_delete"
      contains: "security definer"
    - path: "leanshot/src/lib/admin/anomaly/config-api.ts"
      provides: "Client wrappers + discriminated error class"
      exports: ["defineFunnel", "updateFunnel", "deleteFunnel", "listTrackedFunnels", "AnomalyConfigApiError"]
    - path: "leanshot/src/components/admin/anomaly/AdminAnomalyTrackedFunnelsConfig.tsx"
      provides: "CRUD UI for anomaly_tracked_funnels"
    - path: "leanshot/src/components/admin/anomaly/AnomalyConfigPage.tsx"
      provides: "Page hosting both AdminAnomalyAcknowledgeQueue + AdminAnomalyTrackedFunnelsConfig; mounted at /admin/anomaly route"
  key_links:
    - from: "AdminAnomalyTrackedFunnelsConfig"
      to: "anomaly_funnel_define / update / delete RPCs"
      via: "config-api.ts wrappers"
      pattern: "anomaly_funnel_(define|update|delete)"
    - from: "Phase 24 ADMIN_MODULES manifest"
      to: "AnomalyConfigPage at /admin/anomaly"
      via: "ADMIN_MODULES already lists 'Analytics' module per 24-03-PLAN — this plan's route plugs into that module's child routes (or — if no Analytics-child routing — adds a new module entry at integration time via a separate ADDENDUM)"
      pattern: "AnomalyConfigPage"
---

<objective>
Ship the admin config UI for anomaly tracked funnels (TAXO-05 admin surface). Plan 27-04 ships the data layer + 5 seeded funnels; this plan lets a superadmin add/edit/disable/delete tracked funnels post-deploy. Plus a page wrapper that combines the acknowledgment queue (from Plan 27-04) and the config CRUD into a single /admin/anomaly route.

Purpose: TAXO-05 SC#5 operator self-serve — without this plan, adding a new funnel after the 5 seeds requires a migration; with this plan, founder can adjust thresholds + add funnels live.

Output: 1 migration (3 SECDEF RPCs), 1 client-lib module with tests, 2 UI components (config CRUD + page wrapper).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-CONTEXT.md
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-RESEARCH.md
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-PATTERNS.md
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-04-PLAN.md
@supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql
@leanshot/src/lib/admin/affiliate-review.ts
@leanshot/src/components/admin/AdminAffiliatesReviewQueue.tsx

<interfaces>
<!-- Cross-plan / cross-phase contracts -->

Plan 27-04 carry (this plan DEPENDS on schema from 27-04):
- public.anomaly_tracked_funnels table — created by Plan 27-04 Task 1
- public.funnel_anomaly_alerts table — created by Plan 27-04 Task 1
- AdminAnomalyAcknowledgeQueue component — created by Plan 27-04 Task 5
- AdminAnomalyBanner — already mounted in AdminGlobals via Plan 27-04 Task 5

Phase 24 carry:
- public.is_admin_at_least('superadmin') gate
- log_admin_action helper
- audit_logs.action_name FREE TEXT — new values: 'anomaly_funnel_defined', 'anomaly_funnel_updated', 'anomaly_funnel_deleted'

New SECDEF RPC contracts:

  anomaly_funnel_define(p_event_name text, p_lookback_days int default 7, p_sigma_threshold numeric default 2.0) returns uuid
    - raises 'forbidden' (42501) if not is_admin_at_least('superadmin')
    - raises 'duplicate_event_name' (23505) on UNIQUE violation
    - inserts row + audit-logs; returns new funnel_id

  anomaly_funnel_update(p_funnel_id uuid, p_is_enabled boolean default null, p_lookback_days int default null, p_sigma_threshold numeric default null) returns void
    - raises 'forbidden' (42501) if not is_admin_at_least('superadmin')
    - raises 'not_found' if funnel_id missing
    - updates only non-null params; audit-logs before/after diff

  anomaly_funnel_delete(p_funnel_id uuid) returns void
    - raises 'forbidden' (42501) if not is_admin_at_least('superadmin')
    - DELETE on anomaly_tracked_funnels (CASCADE on funnel_anomaly_alerts? — choose NO CASCADE; raise 'has_alerts' if alerts exist; document that admins must purge alerts before delete OR use anomaly_funnel_update(is_enabled=false) instead)

Existing analogs (verbatim shape):
- supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql lines 145–203 — SECDEF skeleton
- leanshot/src/lib/admin/affiliate-review.ts — discriminated error contract
- leanshot/src/components/admin/AdminAffiliatesReviewQueue.tsx — filter pills + row list + per-row action

Route: /admin/anomaly. Routing approach in leanshot: hash router per CLAUDE.md (`/#/admin/anomaly`). Add route entry by mounting AnomalyConfigPage inside Phase 24's AdminLayout module-switch. If Phase 24's ADMIN_MODULES has a placeholder entry for "Anomaly" or "Analytics" → use its lazy() target. If not → this plan creates AnomalyConfigPage as a stand-alone component reachable via direct hash link `/#/admin/anomaly`; the ADMIN_MODULES entry addition is documented as a follow-up ADDENDUM (NOT in scope here per anti-pattern guards "no shared-file choreography").

Coordination: this plan does NOT modify supabase/functions/_shared/posthog-server.ts (Plan 27-04 owns); does NOT modify AdminLayout (Plan 27-04 owns the AdminGlobals mount).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration — 3 SECDEF RPCs (anomaly_funnel_define/update/delete)</name>
  <files>
    supabase/migrations/20260601000040_anomaly_tracked_funnels_admin_rpcs.sql
  </files>
  <behavior>
    - 3 RPCs created; all SECURITY DEFINER + set search_path = public, extensions, pg_catalog
    - All gate on is_admin_at_least('superadmin')
    - All audit-log via log_admin_action + use set_config('app.suppress_audit','true',true) per Pattern S3
    - anomaly_funnel_define: insert row with defaults from D-14; returns id
    - anomaly_funnel_update: COALESCE pattern for partial updates; raises 'not_found' if 0 rows affected
    - anomaly_funnel_delete: raises 'has_alerts' (22023) if any funnel_anomaly_alerts row exists for funnel_id; else DELETE
  </behavior>
  <action>
    Copy SECDEF skeleton verbatim from supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql lines 145–203. For each RPC: auth check, is_admin_at_least('superadmin') check, perform set_config app.suppress_audit, business logic, explicit audit_logs INSERT with action_name + after_data jsonb_build_object'd. For anomaly_funnel_update: build before_data via `select row_to_json(t) from anomaly_tracked_funnels t where funnel_id = p_funnel_id into v_before` BEFORE update; update with COALESCE for each nullable param (e.g. `update ... set is_enabled = coalesce(p_is_enabled, is_enabled)`); GET DIAGNOSTICS v_rows = ROW_COUNT; raise 'not_found' if v_rows = 0; build after_data; log_admin_action(action_name='anomaly_funnel_updated', target_user_id=null, p_before=v_before, p_after=v_after). For anomaly_funnel_delete: `select count(*) from funnel_anomaly_alerts where funnel_id = p_funnel_id into v_alert_count`; if v_alert_count > 0 raise 'has_alerts'; delete; audit. End each with revoke-all + grant-execute-to-authenticated. Per CONTEXT D-14 + D-23.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c "security definer" supabase/migrations/*_anomaly_tracked_funnels_admin_rpcs.sql | tail -1 | awk -F: '{print $2}' | grep -qE '^[3-9]|^[1-9][0-9]' && grep -q "is_admin_at_least..superadmin" supabase/migrations/*_anomaly_tracked_funnels_admin_rpcs.sql && grep -q 'has_alerts' supabase/migrations/*_anomaly_tracked_funnels_admin_rpcs.sql && grep -q 'anomaly_funnel_define\|anomaly_funnel_update\|anomaly_funnel_delete' supabase/migrations/*_anomaly_tracked_funnels_admin_rpcs.sql</automated>
  </verify>
  <done>3 SECDEF functions defined with superadmin gate, audit suppression, and has_alerts guard on delete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client config-api wrapper + tests</name>
  <files>
    leanshot/src/lib/admin/anomaly/config-api.ts,
    leanshot/src/lib/admin/anomaly/config-api.test.ts
  </files>
  <behavior>
    - defineFunnel(eventName, lookbackDays?, sigmaThreshold?) returns {funnelId:string}
    - updateFunnel(funnelId, patch:{isEnabled?, lookbackDays?, sigmaThreshold?}) returns void
    - deleteFunnel(funnelId) returns void; on has_alerts → AnomalyConfigApiError('has_alerts')
    - listTrackedFunnels() reads from anomaly_tracked_funnels via supabase.from (RLS allows admin SELECT)
    - AnomalyConfigApiError discriminated: not_staff | not_authenticated | duplicate_event_name | not_found | has_alerts | network | unknown
  </behavior>
  <action>
    Tests FIRST. Mirror verbatim the shape of leanshot/src/lib/admin/affiliate-review.ts lines 19–82 (read first). Export `AnomalyConfigApiErrorCode` discriminated union + `AnomalyConfigApiError` class. Internal `callConfigRpc(rpcName, params)`. Public functions per behavior above. config-api.test.ts (≥5 tests using mock supabase): (a) defineFunnel success → returns funnelId; (b) 42501 → AnomalyConfigApiError('not_staff'); (c) 23505 → AnomalyConfigApiError('duplicate_event_name'); (d) deleteFunnel with has_alerts → AnomalyConfigApiError('has_alerts'); (e) listTrackedFunnels returns mapped rows. Per Pattern S7.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npm test -- --run src/lib/admin/anomaly/config-api.test.ts 2>&1 | tail -10 | grep -E 'Tests.*passed' | grep -v '0 passed'</automated>
  </verify>
  <done>config-api.test.ts green ≥5 tests; discriminated error contract covers all 5 server-side error states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AdminAnomalyTrackedFunnelsConfig + AnomalyConfigPage UI</name>
  <files>
    leanshot/src/components/admin/anomaly/AdminAnomalyTrackedFunnelsConfig.tsx,
    leanshot/src/components/admin/anomaly/AnomalyConfigPage.tsx
  </files>
  <behavior>
    - AdminAnomalyTrackedFunnelsConfig renders a table of all tracked funnels with inline edit (is_enabled toggle, lookback number input, sigma number input) + a "Save changes" per-row button (or auto-save on blur) + Delete button (with confirm modal)
    - "Add tracked funnel" form below the table: event_name text input + lookback (default 7) + sigma (default 2.0) + Add button
    - All actions show toast on success/error; UI is optimistic with rollback on error
    - AnomalyConfigPage wraps {AdminAnomalyAcknowledgeQueue from 27-04} above {AdminAnomalyTrackedFunnelsConfig} on a single page with section headers
    - Superadmin-only render: probe via is_admin_at_least('superadmin') (call a helper RPC OR check profiles.admin_role client-side); non-superadmin sees NotAuthorizedCard
  </behavior>
  <action>
    Read leanshot/src/components/admin/AdminAffiliatesReviewQueue.tsx FIRST for the table + per-row action handler verbatim shape. AdminAnomalyTrackedFunnelsConfig: useState<TrackedFunnel[]>; useState<{[id]:boolean}>(busy); useEffect on mount calls listTrackedFunnels + setRows. Per-row render: TR with TD per column. is_enabled column is a Toggle component (build from leanshot/src/components/ui/ primitives if existing Toggle absent; use plain `<input type="checkbox">` styled with token classes). lookback + sigma: `<input type="number">` with controlled value + onBlur dispatches updateFunnel({lookbackDays:newVal}). Delete button opens a small confirm modal: "Delete tracked funnel '<event_name>'? Any existing alerts must be purged first." On Confirm → deleteFunnel → on has_alerts → toast.error('Delete denied: funnel has alert history. Disable instead.') → keep row. Add form: useState for the 3 new fields; Add button calls defineFunnel + on success: prepend new row + clear form. AnomalyConfigPage: renders two sections with `<h2>` headers per Phase 13 design system tokens: "Firing alerts" (mounts AdminAnomalyAcknowledgeQueue) + "Tracked funnels" (mounts AdminAnomalyTrackedFunnelsConfig). At top, isStaff probe → if not superadmin show NotAuthorizedCard from Phase 24 admin shell. Wire the route: add a small switch case in Phase 24's AdminShell or a new lazy() route — but since ADMIN_MODULES is owned by Phase 24, this plan DOES NOT modify modules.ts per anti-pattern guards. Instead: this plan creates the page component; integration step (manual) documented in SUMMARY as: "Add ADMIN_MODULES entry `{key:'anomaly', label:'Anomaly', route:'anomaly', minRole:'superadmin', lazy:()=>import('@/components/admin/anomaly/AnomalyConfigPage'), flagKey:'admin.anomaly.enabled'}` — this is a 1-line addition to leanshot/src/lib/admin/modules.ts and may be applied either by Phase 24's own follow-up or via a Phase 27 ADDENDUM after Wave 2 ships". Per CONTEXT D-14 + D-17 + Claude's Discretion.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npm run typecheck 2>&1 | tail -10 | (grep -E 'error TS.*AdminAnomalyTrackedFunnelsConfig|error TS.*AnomalyConfigPage' && exit 1 || echo OK) && npm run lint -- src/components/admin/anomaly/ 2>&1 | tail -10 | (grep -E '✖|error TS' && exit 1 || echo OK)</automated>
  </verify>
  <done>UI components compile + lint; AnomalyConfigPage hosts both queue + config; superadmin gate at page top; ADMIN_MODULES entry deferred to addendum as documented.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] supabase db push --linked + RPC presence probe</name>
  <files>supabase/migrations/</files>
  <action>
    From /Users/karstenhaldan/minisite: `supabase db push --linked 2>&1 | tee /tmp/27-05-push.log`. Grep `^Skipping` and fail on any skip. Verify: `supabase db query --linked "select proname from pg_proc where proname in ('anomaly_funnel_define','anomaly_funnel_update','anomaly_funnel_delete')"` expect 3 rows.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && supabase db query --linked "select count(*) from pg_proc where proname in ('anomaly_funnel_define','anomaly_funnel_update','anomaly_funnel_delete')" 2>&1 | grep -q '^\s*3\s*$'</automated>
  </verify>
  <done>3 SECDEF RPCs live in linked DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → anomaly_funnel_* RPCs | JWT carries admin_role; SECDEF re-verifies via is_admin_at_least('superadmin') |
| Direct UPDATE on anomaly_tracked_funnels | Blocked by RLS — admin can SELECT but not directly mutate (only SECDEF RPCs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | Elevation of Privilege | non-superadmin modifies tracked funnels | mitigate | All 3 RPCs gate on is_admin_at_least('superadmin'); client UI is UX-only per Pattern S1 |
| T-27-05-02 | Tampering | Delete-then-recreate to erase alert history | mitigate | anomaly_funnel_delete refuses when funnel_anomaly_alerts has rows for that funnel; alerts retain even after disable |
| T-27-05-03 | Repudiation | Config change without trace | mitigate | All 3 RPCs audit-log via log_admin_action with before/after data + set_config audit suppression |
| T-27-05-04 | Denial of Service | Admin spams new funnels | accept | Superadmin role is one person (founder) at v1.3; rate-limit out of scope |
</threat_model>

<verification>
- Unit tests: config-api.test.ts green (Task 2)
- Migration lands + RPCs present in pg_proc (Task 4)
- UI compiles + lints clean (Task 3)
- Manual: navigate /#/admin/anomaly + add new funnel + edit + delete-with-alerts → expect denied (27-VALIDATION manual-only)
</verification>

<success_criteria>
- TAXO-05 admin self-serve satisfied: founder can adjust thresholds + add/disable/delete funnels live without migration
- 3 SECDEF RPCs gate on superadmin
- has_alerts guard prevents alert-history erasure via delete-then-recreate
- AnomalyConfigPage combines acknowledgment queue (27-04) + config CRUD (this plan) into one /admin/anomaly route
- ADMIN_MODULES entry deferral documented (anti-pattern guard: no shared-file choreography in this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/27-modular-admin-shell-extensions/27-05-SUMMARY.md` documenting: 1 migration with 3 SECDEF RPCs, config-api wrapper, AdminAnomalyTrackedFunnelsConfig + AnomalyConfigPage components, the ADMIN_MODULES entry suggested addendum (1-line addition to modules.ts), and the AnomalyConfigPage route mounting strategy.
</output>
