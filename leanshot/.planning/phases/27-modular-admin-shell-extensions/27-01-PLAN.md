---
phase: 27-modular-admin-shell-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260601000001_admin_bulk_jobs.sql
  - supabase/migrations/20260601000002_bulk_action_undo_token.sql
  - supabase/migrations/20260601000003_admin_bulk_actions_extend_audit.sql
  - supabase/migrations/20260601000004_admin_bulk_action_rpcs.sql
  - leanshot/src/lib/admin/bulk/action-handlers.ts
  - leanshot/src/lib/admin/bulk/action-handlers.test.ts
  - leanshot/src/lib/admin/bulk/undo.ts
  - leanshot/src/lib/admin/bulk/undo.test.ts
  - leanshot/src/lib/admin/bulk/types.ts
  - leanshot/src/components/admin/bulk/AdminBulkActionsBar.tsx
  - leanshot/src/components/admin/bulk/AdminBulkConfirmModal.tsx
  - leanshot/src/components/admin/bulk/AdminUndoBanner.tsx
  - leanshot/src/components/admin/members/MembersTable.tsx
  - leanshot/e2e/admin-bulk-actions.spec.ts
autonomous: true
requirements: [ADMIN-04]
tags: [admin, bulk-actions, members, audit]

must_haves:
  truths:
    - "Admin selects ≥1 user in Members table and the BulkActionsBar surfaces 5 actions (csv_export, tag, comp_plan, ban, force_password_reset)"
    - "Clicking Ban opens a confirmation modal showing 'Ban N users — Alice, Bob, Carol, +K more' and Cancel/Confirm buttons"
    - "Confirm with ≤100 selected users executes synchronously via admin_bulk_action_execute RPC; profiles update + one audit_logs row per affected user is written with source='rpc' and action_name='bulk_ban'"
    - "After sync execute returns, an undo banner appears for 60 seconds for ban/comp_plan/tag with an Undo button that reverses the action via admin_bulk_action_undo RPC"
    - "force_password_reset and csv_export show NO undo banner (D-03)"
    - "bulk_action_undo_token row carries an expires_at = now()+60s and is consumed atomically by the undo RPC"
  artifacts:
    - path: "supabase/migrations/20260601000001_admin_bulk_jobs.sql"
      provides: "admin_bulk_jobs table + append-only RLS"
      contains: "create table public.admin_bulk_jobs"
    - path: "supabase/migrations/20260601000002_bulk_action_undo_token.sql"
      provides: "bulk_action_undo_token transient table (60s TTL)"
      contains: "expires_at timestamptz"
    - path: "supabase/migrations/20260601000003_admin_bulk_actions_extend_audit.sql"
      provides: "audit_logs action_name extension does NOT enforce CHECK (free text per Phase 24 D-14) — file documents the new action names"
    - path: "supabase/migrations/20260601000004_admin_bulk_action_rpcs.sql"
      provides: "admin_bulk_action_execute + admin_bulk_action_undo SECDEF RPCs"
      contains: "security definer"
    - path: "leanshot/src/lib/admin/bulk/action-handlers.ts"
      provides: "5-action dispatch + sync/async branching"
      exports: ["executeBulkAction", "BulkActionResult", "BulkApiError"]
    - path: "leanshot/src/lib/admin/bulk/undo.ts"
      provides: "60s undo token redeem"
      exports: ["redeemUndoToken"]
    - path: "leanshot/src/components/admin/bulk/AdminBulkConfirmModal.tsx"
      provides: "Confirmation modal with sample preview (state machine confirm|running|done|error)"
    - path: "leanshot/src/components/admin/bulk/AdminUndoBanner.tsx"
      provides: "60s undo toast"
    - path: "leanshot/e2e/admin-bulk-actions.spec.ts"
      provides: "Playwright end-to-end of ban + undo flow"
  key_links:
    - from: "leanshot/src/components/admin/members/MembersTable.tsx"
      to: "AdminBulkActionsBar"
      via: "selection state pumped via prop; bar renders inline when selectedIds.length > 0"
      pattern: "AdminBulkActionsBar"
    - from: "leanshot/src/lib/admin/bulk/action-handlers.ts"
      to: "admin_bulk_action_execute SECDEF RPC"
      via: "supabase.rpc('admin_bulk_action_execute', ...)"
      pattern: "supabase\\.rpc\\(['\"]admin_bulk_action_execute"
    - from: "admin_bulk_action_execute"
      to: "log_admin_action (Phase 24)"
      via: "loop: per-affected-user audit_logs insert with action_name='bulk_<verb>', source='rpc'"
      pattern: "log_admin_action\\("
---

<objective>
Ship bulk actions on the Members table for ADMIN-04: 5 sync action handlers (csv_export, tag, comp_plan, ban, force_password_reset), confirmation modal with sample preview, 60-second undo banner for reversible actions, and per-row audit_logs entries via Phase 24's log_admin_action. This plan owns ONLY the sync path (≤100 rows); the async worker for >100 rows lives in Plan 27-06 and consumes the admin_bulk_jobs table that this plan ships.

Purpose: ADMIN-04 + SC#1. Operators must run member-level operations safely (with preview + undo) and traceably (per-row audit log) without leaving the Members surface.

Output: 4 new migrations, the SECDEF execute/undo RPC pair, 5-action client dispatcher, undo token lifecycle helper, BulkActionsBar/ConfirmModal/UndoBanner components wired into MembersTable, and a Playwright e2e for the ban + undo round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-CONTEXT.md
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-RESEARCH.md
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-PATTERNS.md
@leanshot/.planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-01-PLAN.md
@leanshot/.planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-03-PLAN.md
@supabase/migrations/20260601000001_audit_logs.sql
@supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql
@leanshot/src/components/clinic/roster/BulkExportCSVFlow.tsx
@leanshot/src/components/clinic/roster/RosterBulkSelectionBar.tsx
@leanshot/src/components/admin/AdminAffiliatesReviewQueue.tsx
@leanshot/src/lib/admin/affiliate-review.ts

<interfaces>
<!-- Cross-plan / cross-phase contracts the executor must NOT rediscover -->

Phase 24 audit_logs (carry, do NOT alter):
- action_name is FREE TEXT (no CHECK constraint per Phase 24 actual DDL)
- source enum is 'rpc' | 'trigger'
- ACTUAL columns (per Phase 24 24-01-PLAN.md line 128): `id, created_at, actor_user_id, target_user_id, action_name, table_name, row_pk, before_data, after_data, org_id, source`
- service_role has REVOKE on direct INSERT — only SECDEF RPCs may write
- log_admin_action(p_action_name text, p_target_user_id uuid, p_table_name text, p_row_pk text, p_before jsonb, p_after jsonb) — Phase 24 helper. **STRONGLY PREFERRED**: route every per-row audit through this helper, NOT raw INSERTs. If a raw INSERT is unavoidable, use the actual column names above (`actor_user_id`, `row_pk` — NOT `user_id`/`user_id_hash`/`row_id`).

Phase 24 admin gating (carry):
- public.is_admin_at_least(min_role text) — SECDEF ordinal comparator; pass 'admin' for bulk actions
- profiles.admin_role enum: 'staff' | 'admin' | 'superadmin'
- aal2 freshness on destructive actions: this plan's RPC accepts requests at the 'admin' tier; aal2-step-up gating for palette-invoked destructive actions is owned by Plan 27-03 (palette layer) — RPCs themselves do NOT block on aal2 freshness here (palette enforces before invoking)

New SECDEF RPC contracts (this plan):

  admin_bulk_action_execute(
    p_action_type text,           -- 'csv_export'|'tag'|'comp_plan'|'ban'|'force_password_reset'
    p_target_user_ids uuid[],     -- explicit IDs (planner avoids JSONB filter for v1)
    p_params jsonb default '{}'   -- e.g. {tag:'priority'}, {days:30} for comp_plan
  ) returns jsonb
    -- shape: {mode:'sync', affected:int, undo_token:uuid|null, job_id:null}
    --     or {mode:'async', affected:0, undo_token:null, job_id:uuid}
    -- raises 'forbidden' (42501) if not is_admin_at_least('admin')
    -- raises 'too_many_rows' (22023) if array length > 10000

  admin_bulk_action_undo(p_undo_token uuid) returns jsonb
    -- shape: {reversed:int}
    -- raises 'token_expired' (22023) if expires_at < now() OR token missing

TS client error contract (mirror affiliate-review.ts):
  type BulkApiErrorCode = 'not_staff' | 'not_authenticated' | 'token_expired'
    | 'too_many_rows' | 'invalid_action' | 'network' | 'unknown';
  class BulkApiError extends Error { code: BulkApiErrorCode }

Existing analogs to mirror (read these to extract verbatim shapes):
- supabase/migrations/20260601000001_audit_logs.sql lines 41–144: append-only RLS pattern (this plan applies to admin_bulk_jobs + bulk_action_undo_token)
- supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql lines 145–203: SECDEF RPC verbatim shape (per-RPC: auth check + is_staff check + set_config app.suppress_audit + business write + explicit audit_logs INSERT + revoke/grant)
- leanshot/src/components/clinic/roster/BulkExportCSVFlow.tsx lines 92–135: Modal + state machine 'confirm'|'running'|'done'|'error' verbatim
- leanshot/src/lib/admin/affiliate-review.ts lines 19–82: discriminated error + callRpc wrapper verbatim
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations for admin_bulk_jobs + bulk_action_undo_token (Wave 0 schema + RLS)</name>
  <files>
    supabase/migrations/20260601000001_admin_bulk_jobs.sql,
    supabase/migrations/20260601000002_bulk_action_undo_token.sql,
    supabase/migrations/20260601000003_admin_bulk_actions_extend_audit.sql
  </files>
  <behavior>
    - admin_bulk_jobs row insertable ONLY via SECDEF RPC (no authenticated INSERT policy); SELECT policy returns only requested_by=auth.uid()
    - bulk_action_undo_token row deletable ONLY via SECDEF RPC + service_role purge; SELECT policy returns only issued_to=auth.uid()
    - Status enum 'pending'|'running'|'completed'|'failed' encoded via text CHECK constraint (NOT pg_enum per [[feedback_planner_iter1_anti_patterns]] Pitfall 3)
    - Both tables enable RLS; absence of INSERT/UPDATE/DELETE policies IS the tamper mitigation (Pattern S2)
    - bulk_action_undo_token.expires_at defaults to now() + interval '60 seconds'; index on (expires_at) for purge cron lookup
    - admin_bulk_jobs has columns: id uuid pk, action_type text, target_filter jsonb, target_user_ids uuid[], requested_by uuid references auth.users, status text check, rows_total int, rows_completed int default 0, error_log jsonb default '[]', claimed_by uuid, claimed_at timestamptz, created_at, completed_at
    - File 03 documents the 7 new audit action_name values that downstream plans will also append to (bulk_action_executed, bulk_action_undone, cohort_defined, cohort_archived, palette_destructive_invoked, anomaly_baseline_computed, anomaly_acknowledged) — but since Phase 24 D-14 declares action_name FREE TEXT (no CHECK), file 03 is a docs-only migration with a comment block + a no-op `select 1`. Do NOT add a CHECK constraint — Phase 24 deliberately omits it.
  </behavior>
  <action>
    Create 3 migrations under /Users/karstenhaldan/minisite/supabase/migrations/ using strict 14-digit timestamps (verify each `^[0-9]{14}_` per [[reference_supabase_migration_filename_regex]] — letter suffixes are SILENTLY SKIPPED). Migration 01 creates admin_bulk_jobs per the analog at supabase/migrations/20260601000001_audit_logs.sql lines 41–144 (verbatim header docblock pattern; verbatim `enable row level security` + select-only policy + tamper-mitigation comment block). Status uses `text not null check (status in ('pending','running','completed','failed')) default 'pending'`. Add `create index idx_admin_bulk_jobs_pending on public.admin_bulk_jobs (status) where status = 'pending'` — note that partial index expressions must be IMMUTABLE per [[reference_supabase_migration_gotchas]], and `status = 'pending'` is IMMUTABLE so this is safe. Migration 02 creates bulk_action_undo_token per the PATTERNS A6 skeleton (columns: token uuid pk default gen_random_uuid(), issued_to uuid references auth.users on delete cascade, action_type text not null, reverse_payload jsonb not null, original_audit_log_ids uuid[] not null, expires_at timestamptz not null default (now() + interval '60 seconds'), created_at timestamptz default now()); create index idx_bulk_undo_expires on (expires_at); RLS enabled with NO write policies — only SECDEF RPCs touch it. Migration 03 is a docs-only no-op: header docblock listing the 7 net-new action_name strings Phase 27 will use; ends with `select 1` so `supabase db push` accepts it. Each migration file opens with the standard header docblock pattern from the audit_logs analog: "-- Phase 27 D-XX (...). Companion migrations: ...". Use sequential YYYYMMDDHHMMSS timestamps starting at the next available timestamp after the latest existing migration (check with `ls /Users/karstenhaldan/minisite/supabase/migrations/ | sort | tail -3` to find max, then increment by 1 second for each of the 3). Per D-01/D-03/D-23.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/migrations/ | grep -E '^[0-9]{14}_(admin_bulk_jobs|bulk_action_undo_token|admin_bulk_actions_extend_audit)\.sql$' | wc -l | grep -q '^\s*3\s*$' && grep -q 'check (status in' supabase/migrations/*_admin_bulk_jobs.sql && grep -q 'expires_at' supabase/migrations/*_bulk_action_undo_token.sql && grep -q 'enable row level security' supabase/migrations/*_admin_bulk_jobs.sql && grep -L 'create policy.*for insert' supabase/migrations/*_admin_bulk_jobs.sql supabase/migrations/*_bulk_action_undo_token.sql | wc -l | grep -q '^\s*2\s*$'</automated>
  </verify>
  <done>3 migrations exist with strict 14-digit timestamps; admin_bulk_jobs has status CHECK + RLS + select-only policy; bulk_action_undo_token has expires_at default 60s + RLS + zero write policies; migration 03 is a docs-only no-op.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admin_bulk_action_execute + admin_bulk_action_undo SECDEF RPCs</name>
  <files>
    supabase/migrations/20260601000004_admin_bulk_action_rpcs.sql,
    supabase/functions/_shared/admin-bulk-action-test.ts
  </files>
  <behavior>
    - admin_bulk_action_execute raises 'forbidden' (42501) when caller is_admin_at_least('admin') returns false
    - admin_bulk_action_execute raises 'too_many_rows' (22023) when array_length(p_target_user_ids,1) > 10000
    - For array_length ≤ 100: executes inline (sync mode); returns {mode:'sync', affected:N, undo_token:uuid|null, job_id:null}; writes ONE audit_logs row per affected user via log_admin_action; mints undo_token for action_type in ('ban','comp_plan','tag')
    - For 100 < array_length ≤ 10000: inserts admin_bulk_jobs row with status='pending'; returns {mode:'async', affected:0, undo_token:null, job_id:uuid}; worker (Plan 27-06) drains
    - admin_bulk_action_undo validates token existence + expires_at > now(); deletes token; reverses the action via reverse_payload jsonb (e.g. {restore_account_state:'active'}); writes a new audit_logs row per reversed user with action_name='bulk_action_undone'
    - csv_export is a degenerate sync action: no DB mutation, returns count + a server-generated download URL token (client fetches CSV via a follow-up streaming endpoint OWNED by this task — but for v1 csv_export simply returns the rows as jsonb in p_params response and the client builds the CSV); NO undo, NO audit_logs PHI dump (per D-03 + HIPAA defaults — log action_name='bulk_csv_export' with after_data={count:N} only)
    - All 5 action_type literals (csv_export, tag, comp_plan, ban, force_password_reset) dispatch to the correct mutation; unknown action_type raises 'invalid_action' (22023)
  </behavior>
  <action>
    Create supabase/migrations/<next-14-digit>_admin_bulk_action_rpcs.sql containing both RPCs. Copy the SECDEF skeleton verbatim from supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql lines 145–203 — per Pattern S3, `perform set_config('app.suppress_audit', 'on', true);` BEFORE the per-row mutation loop so the AFTER trigger does not duplicate the explicit log_admin_action call. Per-action mutation logic: tag → update profiles set tags = array_append(coalesce(tags,'{}'), p_params->>'tag'); comp_plan → insert into comp_plan_grants(user_id, days, granted_by) values (...); ban → update profiles set account_state='banned'; force_password_reset → insert into password_reset_requests + delete from auth.sessions for that user; csv_export → no mutation, just count. Use a plpgsql FOREACH loop over p_target_user_ids capturing before_data via `select row_to_json(p) from profiles p where id = uid` BEFORE the mutation, after_data via the same query AFTER, calling `perform public.log_admin_action(p_action_name => 'bulk_' || p_action_type, p_target_user_id => uid, p_table_name => 'profiles', p_row_pk => uid::text, p_before => v_before, p_after => v_after)` per row (uses Phase 24's helper — correct column shape baked in). For reversible actions (ban, comp_plan, tag), build `reverse_payload jsonb` once at function entry and insert one bulk_action_undo_token row with `original_audit_log_ids = array(select id from audit_logs where actor_user_id = auth.uid() and action_name = 'bulk_' || p_action_type and created_at >= v_start_at)` (NOTE: `actor_user_id` is the real column — NOT `user_id`) so the undo RPC knows which logs to reverse-reference. Per CONTEXT D-21. End each RPC with `revoke all on function public.admin_bulk_action_execute(text, uuid[], jsonb) from public; grant execute on function public.admin_bulk_action_execute(text, uuid[], jsonb) to authenticated;`. Header docblock follows analog. Include Deno test stub at supabase/functions/_shared/admin-bulk-action-test.ts that uses supabase-js admin client to (1) invoke as non-staff user → expect 42501; (2) invoke as admin with 5 user ids action_type='tag' → expect {mode:'sync', affected:5, undo_token:uuid}; (3) invoke undo with that token → expect {reversed:5}; (4) invoke undo with same token again → expect 'token_expired'. Per D-01 + D-03 + D-04.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/migrations/*_admin_bulk_action_rpcs.sql | head -1 | xargs -I{} bash -c 'grep -c "security definer" "{}" | grep -q "^[2-9]" && grep -q "set_config..app.suppress_audit" "{}" && grep -q "is_admin_at_least" "{}" && grep -q "log_admin_action" "{}" && grep -q "too_many_rows" "{}" && grep -q "10000" "{}"'</automated>
  </verify>
  <done>Both RPCs defined as security definer with is_admin_at_least gate, audit-suppression set_config, log_admin_action loop, undo token lifecycle, sync/async branch at 100 rows, hard cap at 10000.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Client lib — 5-action dispatcher + undo helper + types</name>
  <files>
    leanshot/src/lib/admin/bulk/types.ts,
    leanshot/src/lib/admin/bulk/action-handlers.ts,
    leanshot/src/lib/admin/bulk/action-handlers.test.ts,
    leanshot/src/lib/admin/bulk/undo.ts,
    leanshot/src/lib/admin/bulk/undo.test.ts
  </files>
  <behavior>
    - executeBulkAction(action, targetUserIds, params) returns BulkActionResult discriminated by mode='sync'|'async'
    - Sync result includes undoToken (string|null) for ban|comp_plan|tag; null for csv_export|force_password_reset
    - Async result includes jobId; caller polls admin_bulk_jobs via separate hook (Plan 27-06 ships the polling UI)
    - redeemUndoToken(token) returns {reversed:number}; rejects with BulkApiError(code:'token_expired') when server returns 22023
    - BulkApiError discriminated union: not_staff|not_authenticated|token_expired|too_many_rows|invalid_action|network|unknown
    - All 5 action types compile-time enforced via union type BulkActionType
    - csv_export branch streams rows into a Blob client-side using URL.createObjectURL + anchor click (existing pattern from BulkExportCSVFlow.tsx — read it before writing this branch)
  </behavior>
  <action>
    Write tests FIRST per Wave 0 mapping in 27-VALIDATION.md. types.ts exports `export type BulkActionType = 'csv_export' | 'tag' | 'comp_plan' | 'ban' | 'force_password_reset';` plus `BulkActionResult` discriminated union + `BulkApiErrorCode` + `BulkApiError` class. action-handlers.ts mirrors the verbatim shape of leanshot/src/lib/admin/affiliate-review.ts lines 19–82 (read it first) — discriminated error class + `mapRpcError(err)` SQLSTATE switch (42501→not_staff, 28000→not_authenticated, 22023→one_of_token_expired/too_many_rows/invalid_action based on message substring match) + `callBulkRpc` wrapper. Public function `executeBulkAction` calls supabase.rpc('admin_bulk_action_execute', {p_action_type, p_target_user_ids, p_params}); for csv_export, after RPC returns, fetch the row data via a SELECT (already authorized by RLS) and trigger client-side download via Blob + URL.createObjectURL + anchor click — model on the download branch of leanshot/src/components/clinic/roster/BulkExportCSVFlow.tsx. undo.ts exports `redeemUndoToken(token)` calling admin_bulk_action_undo RPC. action-handlers.test.ts uses vi.mock('@/lib/supabase') to mock supabase.rpc; covers (a) each of the 5 action types dispatches with correct RPC params, (b) 42501 → BulkApiError('not_staff'), (c) 22023 with message 'too_many_rows' → BulkApiError('too_many_rows'), (d) 22023 with message 'invalid_action' → BulkApiError('invalid_action'), (e) network error → BulkApiError('network'). undo.test.ts covers (a) successful redeem returns {reversed:N}, (b) expired token → BulkApiError('token_expired'). Per CONTEXT D-04 + D-03; per Pattern S7. Run `npm test --run src/lib/admin/bulk/` after authoring tests; expect RED before implementation, GREEN after.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npm test -- --run src/lib/admin/bulk/ 2>&1 | tail -20 | grep -E 'Test Files.*passed|Tests.*passed' | grep -v '0 passed'</automated>
  </verify>
  <done>vitest reports green for action-handlers.test.ts (≥5 tests) and undo.test.ts (≥2 tests); all 5 BulkActionType literals dispatched; BulkApiError code matrix covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: UI — AdminBulkActionsBar + AdminBulkConfirmModal + AdminUndoBanner wired into MembersTable</name>
  <files>
    leanshot/src/components/admin/bulk/AdminBulkActionsBar.tsx,
    leanshot/src/components/admin/bulk/AdminBulkConfirmModal.tsx,
    leanshot/src/components/admin/bulk/AdminUndoBanner.tsx,
    leanshot/src/components/admin/members/MembersTable.tsx
  </files>
  <behavior>
    - When MembersTable selectedIds.length > 0, AdminBulkActionsBar renders at top of table with 5 action buttons (Export CSV, Tag, Grant Pro, Ban, Reset Password)
    - Clicking any action opens AdminBulkConfirmModal with sampleNames (first 3) + count + Confirm/Cancel
    - Modal state machine: 'confirm'|'running'|'done'|'error' (verbatim shape from BulkExportCSVFlow.tsx lines 92–135)
    - On Confirm → call executeBulkAction → if sync + undoable → render AdminUndoBanner with 60s countdown
    - AdminUndoBanner has role="status" + aria-live="polite" (per CLAUDE.md accessibility conventions)
    - Undo click → redeemUndoToken → toast 'Action reversed' → banner dismisses
    - 60-second setTimeout auto-dismisses banner; server-side TTL is the authority (client is just UX)
    - force_password_reset + csv_export skip the banner entirely
    - For action_type === 'tag' or 'comp_plan', the modal includes a small input (tag name; days) before Confirm
  </behavior>
  <action>
    Read leanshot/src/components/clinic/roster/RosterBulkSelectionBar.tsx + leanshot/src/components/clinic/roster/BulkExportCSVFlow.tsx (lines 92–135) + leanshot/src/components/ui/Toast.tsx + leanshot/src/hooks/useToast.ts FIRST to extract the exact shape. AdminBulkActionsBar receives `selectedIds: string[]`, `sampleNames: string[]`, `onClear: () => void`; renders a horizontal toolbar with 5 Button variants (per Phase 13 design system). Each button opens AdminBulkConfirmModal with the chosen action_type. AdminBulkConfirmModal copies the BulkExportCSVFlow state machine verbatim — `type FlowState = 'confirm' | 'running' | 'done' | 'error';`. For tag/comp_plan, the 'confirm' state renders an Input above the Confirm button. On Confirm → setState('running') → call executeBulkAction → on success setState('done') + if undoable invoke `onUndoAvailable({undoToken, count})` callback so parent renders AdminUndoBanner. AdminUndoBanner renders a fixed-position toast with `role="status" aria-live="polite"`, a 60-second visual countdown ring (reuse leanshot/src/components/ui/ProgressRing.tsx), action text "Banned 47 users", Undo button. On Undo click → redeemUndoToken → on success show useToast.success('Reversed'); on failure (token_expired) → useToast.error('Undo window expired'). setTimeout(60_000) auto-dismisses. Wire into MembersTable: read the existing leanshot/src/components/admin/members/MembersTable.tsx, ADD (don't replace) selection-state plumbing — add `const [selectedIds, setSelectedIds] = useState<string[]>([])` + per-row checkbox column + header checkbox for select-all-on-page + conditional `{selectedIds.length > 0 && <AdminBulkActionsBar ... />}` above the table. Do NOT modify any other admin component (no cross-plan file pollution per [[feedback_parallel_executor_git_isolation]]). Per CONTEXT D-02 + D-03 + D-04.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npm run typecheck 2>&1 | tail -10 | grep -q 'error TS' && exit 1 || true; npm run lint -- src/components/admin/bulk/ src/components/admin/members/MembersTable.tsx 2>&1 | tail -10 | grep -E 'error|✖' && exit 1 || echo OK</automated>
  </verify>
  <done>Three new components compile + lint; MembersTable wires selection + bar; AdminUndoBanner has role=status + aria-live=polite; modal state machine matches BulkExportCSVFlow verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Playwright e2e — ban 5 users + 60s undo round-trip</name>
  <files>
    leanshot/e2e/admin-bulk-actions.spec.ts
  </files>
  <behavior>
    - Test "admin bans 5 members and undoes within 60s" passes
    - Seeds 5 test users via supabase admin client (per-file slug prefix per [[feedback_rls_per_file_slug_prefix]])
    - Logs in as a seeded admin via page.addInitScript (per [[reference_playwright_state_seeding]]) — NEVER via UI login (auth rate limits)
    - Navigates to /admin/members; selects 5 users via checkboxes; clicks Ban in BulkActionsBar
    - Modal shows "Ban 5 users — <name1>, <name2>, <name3>, +2 more"; click Confirm
    - After RPC returns, asserts 5 profiles updated to account_state='banned' via direct supabase admin query (not via UI)
    - AdminUndoBanner is visible with role="status"; click Undo
    - After undo RPC returns, asserts 5 profiles restored to account_state='active' AND a new audit_logs row exists per user with action_name='bulk_action_undone'
    - afterAll cleanup deletes the seeded users
  </behavior>
  <action>
    Create leanshot/e2e/admin-bulk-actions.spec.ts using the existing Playwright config (leanshot/playwright.config.ts). Read leanshot/e2e/patient-activity-modal.spec.ts FIRST as the analog for addInitScript-based session seeding + supabase admin fixture pattern. Use a file-scoped slug prefix `const SLUG = 'bulk-${Date.now()}-'` to avoid collision with parallel files. beforeAll: create 5 test profiles via supabase service-role client with name like `${SLUG}alice`, etc.; create a seeded admin profile with admin_role='admin' + a TOTP-already-verified session. addInitScript injects the admin's access_token into localStorage under the supabase-js session key. Navigate to /#/admin/members (the existing hash router per CLAUDE.md). Use page.locator + role-based selectors (button name, checkbox name) — DO NOT depend on CSS class names. After clicking Confirm, await a network response matching `**/rest/v1/rpc/admin_bulk_action_execute`. Then query supabase admin client directly to verify account_state='banned'. Await role=status banner visible (the AdminUndoBanner). Click Undo button. Await response for admin_bulk_action_undo. Re-query supabase admin client: account_state='active' AND audit_logs has 5 rows with action_name='bulk_action_undone' AND target_user_id in the seeded set. afterAll deletes via supabase admin .from('profiles').delete().in('id', [...]). Per CONTEXT D-02 + D-03 + 27-VALIDATION Wave 0.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx playwright test e2e/admin-bulk-actions.spec.ts --reporter=line 2>&1 | tail -15 | grep -E 'passed|✓' && ! grep -E 'failed|✘' /tmp/pw-out 2>/dev/null</automated>
  </verify>
  <done>Playwright spec passes; 5 users banned + 5 users restored + 5 undo audit rows confirmed via direct DB query (DB-level invariant per [[feedback_realtime_layer_e2e_pattern]] generalization).</done>
</task>

<task type="auto">
  <name>Task 6: [BLOCKING] supabase db push --linked + sanity probe</name>
  <files>supabase/migrations/</files>
  <action>
    From /Users/karstenhaldan/minisite (NOT leanshot/ — supabase CLI quirks per [[reference_supabase_worktree_temp_state]]), run `supabase db push --linked 2>&1 | tee /tmp/27-01-push.log`. Grep the output for `^Skipping` per [[reference_supabase_migration_filename_regex]] — if any migration is silently skipped, FAIL the task and rename the offending file to a strict 14-digit timestamp. After successful push, sanity-probe the live DB: `supabase db query --linked "select tablename from pg_tables where schemaname='public' and tablename in ('admin_bulk_jobs','bulk_action_undo_token')"` — expect 2 rows. `supabase db query --linked "select proname from pg_proc where proname in ('admin_bulk_action_execute','admin_bulk_action_undo')"` — expect 2 rows. `supabase db query --linked "select policyname from pg_policies where tablename='admin_bulk_jobs'"` — expect ONLY select policies, NO insert/update/delete. Per [[reference_supabase_db_query_linked]]. This task is BLOCKING for the phase: no Wave-2 plan may run before it succeeds (Plan 27-06 consumes admin_bulk_jobs; Plan 27-07 consumes bulk_action_undo_token).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && supabase db query --linked "select count(*) from pg_tables where schemaname='public' and tablename in ('admin_bulk_jobs','bulk_action_undo_token')" 2>&1 | grep -q '^\s*2\s*$' && supabase db query --linked "select count(*) from pg_proc where proname in ('admin_bulk_action_execute','admin_bulk_action_undo')" 2>&1 | grep -q '^\s*2\s*$'</automated>
  </verify>
  <done>2 new tables + 2 new RPCs live in linked DB; zero migrations skipped; only select-policies on append-only tables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → SECDEF RPC | Admin's JWT carries admin_role; RPC verifies via is_admin_at_least('admin') |
| SECDEF RPC → profiles bulk mutation | RPC is owner of mutation; auth bypassed; gate is the is_admin_at_least check |
| Client → undo token redemption | Token issued at execute time; redeemed within 60s via separate RPC; single-use |
| Cron purge → bulk_action_undo_token | service_role purges expired tokens (Plan 27-07 owns the cron) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Elevation of Privilege | admin_bulk_action_execute | mitigate | RPC raises 'forbidden' (42501) when is_admin_at_least('admin') returns false; client gate is UX-only per Pattern S1 |
| T-27-01-02 | Tampering | admin_bulk_jobs | mitigate | Append-only RLS per Pattern S2 (no INSERT/UPDATE/DELETE policies); only SECDEF RPCs + service_role write; client can never directly mutate jobs |
| T-27-01-03 | Repudiation | bulk action without trace | mitigate | Per-row log_admin_action call inside SECDEF loop writes one audit_logs row per affected user with before_data + after_data + source='rpc' |
| T-27-01-04 | Information Disclosure | csv_export PHI leak | mitigate | csv_export action_name logs only {count:N} in audit_logs (no row contents); HIPAA pre-CHPD posture per CLAUDE.md (no clinic PHI exists yet — this phase ships before Phase 28); ESLint lint-stripe-phi.ts already enforces no-PHI in adjacent surfaces |
| T-27-01-05 | Denial of Service | bulk action on 1M users | mitigate | Hard cap 10000 enforced server-side in execute RPC (raises 'too_many_rows' 22023); sync cap 100 protects RPC tx duration |
| T-27-01-06 | Tampering | undo token replay | mitigate | bulk_action_undo_token is single-use (DELETE on redeem); expires_at < now() raises 'token_expired'; 1-min purge cron (Plan 27-07) keeps table small |
| T-27-01-07 | Spoofing | non-admin invokes undo with stolen token | mitigate | undo RPC checks `issued_to = auth.uid()` AND is_admin_at_least('admin') — a stolen token bound to a different admin's session is rejected |
| T-27-01-08 | Elevation of Privilege | aal2 step-up bypass via direct RPC call | accept | Bulk-action RPCs intentionally do NOT gate on aal2 freshness — aal2 step-up is a palette-layer concern owned by Plan 27-03; bulk actions invoked from Members table assume admin already auth'd at aal2 per Phase 24 D-09 |
</threat_model>

<verification>
- `supabase db query --linked "..."` confirms 2 new tables + 2 new RPCs live with correct policies (Task 6 automated)
- `npm test -- --run src/lib/admin/bulk/` green (Task 3 automated)
- `npm run typecheck && npm run lint` clean for new files (Task 4 automated)
- `npx playwright test e2e/admin-bulk-actions.spec.ts` green (Task 5 automated)
- Audit log post-condition: 5 rows with action_name='bulk_ban' + 5 rows with action_name='bulk_action_undone' after the e2e (asserted via DB query inside the test)
</verification>

<success_criteria>
- Phase SC#1 satisfied: admin runs bulk action with confirmation modal + per-row audit_logs entry
- All 5 action types ship (csv_export, tag, comp_plan, ban, force_password_reset)
- 60-second undo banner functional for ban + comp_plan + tag (D-03 reversible set)
- force_password_reset + csv_export deliberately ship without undo per D-03
- admin_bulk_jobs schema in place for Plan 27-06 to consume (async worker)
- bulk_action_undo_token schema in place for Plan 27-07 to consume (purge cron)
- Zero direct-write capability on the two new tables (append-only RLS verified by absence of insert/update/delete policies)
</success_criteria>

<output>
After completion, create `.planning/phases/27-modular-admin-shell-extensions/27-01-SUMMARY.md` documenting: migrations created (3 ts + 1 docs no-op), RPC contracts shipped, 5-action client dispatcher, UI components wired into MembersTable, e2e green, audit_logs action_names introduced (bulk_<verb>, bulk_action_undone, bulk_csv_export), undo token TTL behavior, and the handoff contracts for Plans 27-06 (admin_bulk_jobs.status='pending' poll-claim) + 27-07 (bulk_action_undo_token.expires_at purge).
</output>
