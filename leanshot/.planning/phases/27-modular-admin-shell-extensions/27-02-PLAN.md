---
phase: 27-modular-admin-shell-extensions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260601000010_cohort_definitions.sql
  - supabase/migrations/20260601000011_cohort_membership_matview.sql
  - supabase/migrations/20260601000012_cohort_rpcs.sql
  - supabase/migrations/20260601000013_cohort_matview_refresh_cron.sql
  - leanshot/src/lib/cohort/field-allowlist.ts
  - leanshot/src/lib/cohort/rule-tree-schema.ts
  - leanshot/src/lib/cohort/rule-tree-schema.test.ts
  - leanshot/src/lib/cohort/rule-tree-to-sql.ts
  - leanshot/src/lib/cohort/rule-tree-to-sql.test.ts
  - leanshot/src/lib/cohort/api.ts
  - leanshot/src/lib/cohort/api.test.ts
  - leanshot/src/components/admin/cohort/AdminCohortBuilder.tsx
  - leanshot/src/components/admin/cohort/AdminCohortList.tsx
  - leanshot/src/components/admin/cohort/CohortFieldPicker.tsx
  - leanshot/src/components/admin/cohort/CohortRuleNode.tsx
  - leanshot/e2e/admin-cohort-builder.spec.ts
autonomous: true
requirements: [ADMIN-05, TAXO-03]
tags: [admin, cohort, matview, rule-tree, taxonomy]

must_haves:
  truths:
    - "Admin defines a cohort named 'free users >7d' in the visual builder UI; clicking Save creates a cohort_definitions row with status='draft'"
    - "Admin promotes a draft cohort to 'active'; the next matview refresh populates cohort_membership rows for matching profiles"
    - "Admin archives an active cohort; matview refresh excludes archived cohorts from population"
    - "Visual builder enforces 15-field allowlist (D-06) — picking any field outside the allowlist is impossible (dropdown options are the only source)"
    - "Visual builder rejects rule trees deeper than MAX_DEPTH=8 with an inline error before save"
    - "rule-tree-to-sql.ts translator emits parameterized SQL — no string interpolation of user-supplied values; SQL injection test in unit suite passes"
    - "cohort_membership matview refresh CONCURRENTLY succeeds because (user_id, cohort_id) UNIQUE index is present"
    - "First-ever refresh is NON-concurrent (matview created WITH NO DATA + initial REFRESH MATERIALIZED VIEW without CONCURRENTLY); subsequent cron refreshes are CONCURRENTLY"
    - "Consumer query `select 1 from cohort_membership where user_id=? and cohort_id=?` returns in <50ms p99 at v1 cohort count (<20)"
  artifacts:
    - path: "supabase/migrations/20260601000010_cohort_definitions.sql"
      provides: "cohort_definitions table + status text CHECK + RLS"
    - path: "supabase/migrations/20260601000011_cohort_membership_matview.sql"
      provides: "cohort_membership MATVIEW + UNIQUE index + 2-step initial refresh"
      contains: "create unique index"
    - path: "supabase/migrations/20260601000012_cohort_rpcs.sql"
      provides: "cohort_define + cohort_archive SECDEF RPCs + plpgsql defensive validator"
      contains: "security definer"
    - path: "supabase/migrations/20260601000013_cohort_matview_refresh_cron.sql"
      provides: "pg_cron schedule '7,22,37,52 * * * *' for non-blocking concurrent refresh"
      contains: "cron.schedule"
    - path: "leanshot/src/lib/cohort/rule-tree-schema.ts"
      provides: "zod schema + 15-field type literal + MAX_DEPTH constant"
      exports: ["ruleTreeSchema", "RuleNode", "RuleField", "MAX_DEPTH", "FIELD_ALLOWLIST"]
    - path: "leanshot/src/lib/cohort/rule-tree-to-sql.ts"
      provides: "Recursive translator with parameterized output"
      exports: ["ruleTreeToSql"]
    - path: "leanshot/src/components/admin/cohort/AdminCohortBuilder.tsx"
      provides: "Hand-rolled nested-tree builder UI"
    - path: "leanshot/e2e/admin-cohort-builder.spec.ts"
      provides: "Playwright define + archive flow"
  key_links:
    - from: "leanshot/src/components/admin/cohort/AdminCohortBuilder.tsx"
      to: "leanshot/src/lib/cohort/api.ts → cohort_define RPC"
      via: "Save click → defineCohort(name, ruleTree)"
      pattern: "defineCohort\\("
    - from: "cron 'cohort-membership-refresh'"
      to: "cohort_membership matview"
      via: "refresh materialized view concurrently"
      pattern: "refresh materialized view concurrently"
    - from: "rule-tree-schema.ts"
      to: "cohort_define plpgsql validator"
      via: "TS zod enum mirrored as plpgsql allowlist in RPC; defense-in-depth per [[feedback_planner_iter1_anti_patterns]]"
      pattern: "p_rule.*->>.*field"
---

<objective>
Ship the cohort builder for ADMIN-05 + TAXO-03: JSONB rule-tree schema (15-field allowlist, MAX_DEPTH=8), recursive TS-to-SQL translator with parameterized output, cohort_definitions + cohort_membership matview, 2 SECDEF RPCs (cohort_define + cohort_archive), pg_cron 15-min refresh on stagger schedule `7,22,37,52` (per RESEARCH correction #2 — avoids quarter-hour collision with anomaly cron `*/5`), and the hand-rolled visual builder UI. Single TS rule-tree schema is the source of truth; the plpgsql validator at the RPC layer mirrors the allowlist for defense-in-depth.

Purpose: ADMIN-05 + TAXO-03 + SC#2 + SC#4. Cohorts are load-bearing infrastructure for PAYWALL (P39), RECOMMEND (P38), SAVE (P40), GAME (P35), TAXO funnels — they must ship correct + indexed so downstream phases can JOIN against them without bespoke filters.

Output: 4 new migrations (cohort_definitions, cohort_membership matview, RPCs, cron), 3 client-lib modules (schema, translator, api wrapper) all with unit tests, 4 UI components (builder, list, field picker, rule node), and a Playwright e2e for define+archive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-CONTEXT.md
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-RESEARCH.md
@leanshot/.planning/phases/27-modular-admin-shell-extensions/27-PATTERNS.md
@leanshot/.planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-01-PLAN.md
@supabase/migrations/20270101000007_affiliate_click_baseline_mv.sql
@supabase/migrations/20270601000008_user_activity_daily_matview.sql
@supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql
@supabase/migrations/20270101000009_click_baseline_refresh_cron.sql
@leanshot/src/lib/page-builder/block-schema.ts
@leanshot/src/lib/admin/affiliate-review.ts
@leanshot/src/components/admin/AdminAffiliatesReviewQueue.tsx

<interfaces>
<!-- Cross-plan / cross-phase contracts -->

Phase 24 carry:
- audit_logs.action_name is FREE TEXT (no CHECK) — new action_name values added by this plan: 'cohort_defined', 'cohort_archived'
- log_admin_action(p_action_name, p_target_user_id, p_before, p_after) helper
- is_admin_at_least(min_role) — gate cohort RPCs with min_role='admin' (cohort_define) and min_role='superadmin' (cohort_archive per D-21 intent — superadmin gates destructive lifecycle)

profiles table fields available to the 15-field allowlist (must exist on profiles or be joinable):
- tier (text: 'free'|'pro'|'lifetime')
- role (text: 'patient'|'clinician'|'admin'|'superadmin')
- days_since_signup (computed: extract(day from now() - created_at))
- days_since_last_login (computed: extract(day from now() - last_sign_in_at))
- total_paid_amount_cents (from stripe_customer_summary or denormalized)
- active_streak_days (from gamification — may be 0 pre-Phase 35)
- has_active_subscription (boolean from tier_effective.has_active per v1.2 contract)
- signup_source (text: 'organic'|'paid_meta'|...)
- country, language (text)
- has_org (boolean: org_id IS NOT NULL — column may be nullable pre-Phase 28)
- has_completed_onboarding (boolean from activation_events row count > 0)
- is_affiliate (boolean from affiliate_program row exists)
- anomaly_flagged (boolean from anomaly review flag — Phase 26)
- account_state (text: 'active'|'banned'|'comp'|'past_due')

For columns that may not yet exist (active_streak_days pre-P35, has_org pre-P28), the matview SQL must use `coalesce(...)` + a `case when column-exists then ... else default end` PROBE — but plpgsql cannot probe column existence at view-creation time. Approach: write the matview to depend ONLY on columns that ARE in v1.2 profiles + Phase 24 additions; defer fields that depend on yet-unshipped tables to a Phase 27 ADDENDUM at consumer-phase time. For v1 ship the matview SELECT uses NULL placeholders for active_streak_days + has_org if those columns don't exist yet (probe with `select column_name from information_schema.columns where table_name='profiles' and column_name=...` in a DO block at migration runtime — generate the matview body dynamically using `format()` + `execute`).

New SECDEF RPC contracts:

  cohort_define(p_name text, p_rule jsonb) returns uuid
    - raises 'forbidden' (42501) if not is_admin_at_least('admin')
    - raises 'invalid_rule' (22023) if defensive plpgsql validator rejects
      (any leaf.field NOT IN the 15-field allowlist, OR depth > 8,
       OR malformed shape — missing children/field/op)
    - raises 'duplicate_name' (23505) on UNIQUE constraint
    - returns new cohort_definitions.id
    - inserts with status='draft' (admin promotes via separate update or via cohort_define returning then cohort_promote — for v1 the visual builder issues a follow-up update via cohort_define with same name + status='active' is NOT supported; instead expose cohort_promote later or use direct UPDATE via service role at promote-time — v1 ships ONLY define[draft] + archive)

  cohort_archive(p_cohort_id uuid) returns void
    - raises 'forbidden' (42501) if not is_admin_at_least('superadmin')
    - raises 'not_found' if cohort_id missing
    - updates status='archived'; archived cohorts excluded from matview population
    - audit-logged with action_name='cohort_archived'

Promotion (draft → active): NOT exposed as RPC in v1 per CONTEXT D-09 phrasing ("Only `active` cohorts populated"). Researcher and CONTEXT do not specify a promote RPC. Decision: this plan exposes `cohort_set_status(p_cohort_id uuid, p_status text)` as a third RPC (covers draft→active AND active→archived; cohort_archive becomes a thin wrapper for the superadmin-gated archive case). This is per Claude's Discretion area in CONTEXT — discretion is bounded but status-transition ownership per [[feedback_status_machine_transition_owner]] requires an explicit owner. Document explicitly in plan output.

Existing analogs to mirror verbatim:
- supabase/migrations/20270101000007_affiliate_click_baseline_mv.sql — matview + UNIQUE + grant
- supabase/migrations/20270601000008_user_activity_daily_matview.sql — full matview migration with 2-step initial refresh
- supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql lines 145–203 — SECDEF RPC verbatim shape
- supabase/migrations/20270101000009_click_baseline_refresh_cron.sql — pg_cron in-DB SQL schedule
- leanshot/src/lib/page-builder/block-schema.ts lines 1–60 — single-source-of-truth zod + literal-union enum

UI analogs:
- leanshot/src/components/admin/AdminAffiliatesReviewQueue.tsx — isStaff probe + filter pill + per-row action handler (AdminCohortList mirrors verbatim)
- leanshot/src/components/admin/pages/blocks/*.tsx — flat-tree edit pattern (nearest cousin for the nested CohortRuleNode UI)

UI primitive: hand-rolled nested editor per RESEARCH (no dnd-kit). Each branch node is a stack of children + an "Add condition" / "Add group" pair of buttons + an "AND/OR" toggle + a "Remove" button.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: zod schema + 15-field allowlist + recursive translator (TS-side, unit-tested)</name>
  <files>
    leanshot/src/lib/cohort/field-allowlist.ts,
    leanshot/src/lib/cohort/rule-tree-schema.ts,
    leanshot/src/lib/cohort/rule-tree-schema.test.ts,
    leanshot/src/lib/cohort/rule-tree-to-sql.ts,
    leanshot/src/lib/cohort/rule-tree-to-sql.test.ts
  </files>
  <behavior>
    - FIELD_ALLOWLIST is exactly the 15 strings from CONTEXT D-06; any addition requires a migration + ESLint review
    - ruleTreeSchema (zod) rejects leaves with field outside FIELD_ALLOWLIST
    - ruleTreeSchema rejects branches with >50 children (DoS bound)
    - ruleTreeSchema rejects trees deeper than MAX_DEPTH=8 (rejected at schema parse, not at translator)
    - ruleTreeToSql emits parameterized SQL with numbered placeholders ($1, $2, ...) and a separate params array — no value ever inlined into the SQL string
    - Each leaf op in the supported set (=, !=, >, <, >=, <=, in, is_null, is_not_null) emits the correct SQL fragment
    - is_null and is_not_null emit NO placeholder (and no params entry)
    - in op requires value to be a non-empty array
    - SQL injection probe: rule = `{field:'tier', op:'=', value:"'; DROP TABLE profiles; --"}` produces parameterized SQL with the malicious string in params[0], never in the SQL string itself
    - Branch with op='and' joins children with `AND`; op='or' joins with `OR`; wraps in parens
  </behavior>
  <action>
    Write tests FIRST. field-allowlist.ts exports the 15 string literals as a const tuple + a `FIELD_ALLOWLIST_SET = new Set(FIELD_ALLOWLIST)`. rule-tree-schema.ts mirrors the verbatim header docblock from leanshot/src/lib/page-builder/block-schema.ts lines 1–60 (read first); exports `RuleField` literal union, `RuleOp` literal union (9 ops listed above), `RuleLeaf` zod object, `RuleBranch` zod object (lazy/recursive), `RuleNode` discriminated union, `MAX_DEPTH = 8`, `ruleTreeSchema` (the top-level parser). The depth check is a custom `.superRefine` on ruleTreeSchema that walks the tree. rule-tree-to-sql.ts implements `export function ruleTreeToSql(node: RuleNode, paramOffset = 0): { sql: string; params: unknown[]; nextOffset: number }` — recursive; branch case concatenates children with op (uppercased) inside parens; leaf case switches on op. Maintain a `paramOffset` counter to emit `$${offset+1}`, `$${offset+2}`, etc. Tests (rule-tree-schema.test.ts ≥6 tests): (a) parse valid simple leaf, (b) parse valid nested AND/OR tree, (c) reject leaf with field='unknown_field' → ZodError, (d) reject tree with depth 9 → ZodError, (e) reject branch with 51 children, (f) parse all 9 op literals. Tests (rule-tree-to-sql.test.ts ≥6 tests): (a) leaf field='tier' op='=' value='pro' → {sql:'p.tier = $1', params:['pro'], nextOffset:1}, (b) leaf op='is_null' → {sql:'p.has_org IS NULL', params:[], nextOffset:0}, (c) leaf op='in' value=['free','pro'] → {sql:'p.tier = ANY($1::text[])', params:[['free','pro']], nextOffset:1}, (d) branch AND with 2 leaves → '(p.tier = $1 AND p.country = $2)', (e) nested OR-of-ANDs depth 3 → correct parens + correct param offsets, (f) SQL injection: value="'; DROP TABLE--" → ends up in params[0] verbatim, SQL string contains only the placeholder. Run `npm test --run src/lib/cohort/` → RED before impl, GREEN after. Per CONTEXT D-05 + D-06 + Pattern S8.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npm test -- --run src/lib/cohort/rule-tree-schema.test.ts src/lib/cohort/rule-tree-to-sql.test.ts 2>&1 | tail -15 | grep -E 'Test Files.*passed|Tests.*passed' | grep -v '0 passed'</automated>
  </verify>
  <done>Both unit suites green; ≥12 tests total; SQL injection probe asserts value is in params not SQL; FIELD_ALLOWLIST is the canonical 15-item tuple imported by both translator and (later) the UI field picker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrations — cohort_definitions table + cohort_membership matview (2-step initial refresh) + cron schedule</name>
  <files>
    supabase/migrations/20260601000010_cohort_definitions.sql,
    supabase/migrations/20260601000011_cohort_membership_matview.sql,
    supabase/migrations/20260601000013_cohort_matview_refresh_cron.sql
  </files>
  <behavior>
    - cohort_definitions table exists with: id uuid pk default gen_random_uuid(), name text not null unique, rule jsonb not null, status text not null check (status in ('draft','active','archived')) default 'draft', created_by uuid references auth.users, created_at, updated_at
    - RLS enabled; SELECT policy for is_admin_at_least('admin'); NO INSERT/UPDATE/DELETE policies (SECDEF RPCs are only writers per Pattern S2)
    - cohort_membership materialized view created WITH NO DATA initially
    - UNIQUE index on (user_id, cohort_id) using ONLY column names (no expression, no WHERE) — required for REFRESH CONCURRENTLY per PostgreSQL docs
    - Initial REFRESH MATERIALIZED VIEW (NO CONCURRENTLY) executed inside the migration BEFORE the cron is enabled (RESEARCH correction #1)
    - pg_cron schedule 'cohort-membership-refresh' on '7,22,37,52 * * * *' (RESEARCH correction #2; deliberately staggered off quarter-hour to avoid pile-up with anomaly cron `*/5`)
    - Matview SELECT uses dynamic plpgsql + information_schema column probe to handle pre-Phase 28/35 missing columns (defaults: has_org=false, active_streak_days=0)
    - Grant select on matview to authenticated for admin RPCs; consumer reads go through SECDEF helper cohort_is_member(user_id, cohort_id) — define this helper here too
  </behavior>
  <action>
    Migration 10 (cohort_definitions): copy header docblock pattern from analog audit_logs migration; standard `create table` + `alter table ... enable row level security` + SELECT policy `using (public.is_admin_at_least('admin'))`. Index on (status) for the matview build's `where status='active'` predicate (full-column predicate is IMMUTABLE, safe per [[reference_supabase_migration_gotchas]] Pitfall 1). Migration 11 (matview): use `create materialized view public.cohort_membership ... with no data` per RESEARCH Pattern 4. The view body is generated dynamically in a DO block — probe profiles columns via information_schema, then `execute format('create materialized view public.cohort_membership as %s with no data', v_sql)` where v_sql is constructed from the active cohort_definitions rules at refresh time. Since translator runs in TS not SQL, the matview body for v1 is the simpler form: `select c.id as cohort_id, p.id as user_id, now() as joined_at from public.cohort_definitions c cross join public.profiles p where c.status = 'active' and public.cohort_rule_matches(c.rule, p.id)` — where `public.cohort_rule_matches(rule jsonb, user_id uuid) returns boolean` is a SECDEF helper function created in Task 3 that mirrors the TS translator in plpgsql (recursive jsonb walk + the 15-field allowlist hardcoded as a CASE expression). For v1 simplicity: ship the matview body using cohort_rule_matches helper. Add the UNIQUE index per Pattern 4: `create unique index cohort_membership_pk on public.cohort_membership (user_id, cohort_id)`. Add header comment block per PATTERNS A3 callout (Pitfall 5 + Pitfall 1 landmines). At end of migration 11 (after CREATE + INDEX): `refresh materialized view public.cohort_membership;` (NO CONCURRENTLY — first refresh of a WITH NO DATA matview cannot use CONCURRENTLY per RESEARCH correction #1). Grant SELECT on matview to service_role + authenticated. Add `create or replace function public.cohort_is_member(p_user_id uuid, p_cohort_id uuid) returns boolean language sql security definer stable set search_path = public, pg_catalog as $$ select exists(select 1 from public.cohort_membership where user_id = p_user_id and cohort_id = p_cohort_id) $$;` with `revoke all ... grant execute to authenticated`. Migration 13 (cron): copy analog from supabase/migrations/20270101000009_click_baseline_refresh_cron.sql; `select cron.schedule('cohort-membership-refresh', '7,22,37,52 * * * *', $$ refresh materialized view concurrently public.cohort_membership $$);`. Use sequential 14-digit timestamps; verify no collision with other Phase 27 timestamps. Per CONTEXT D-07 + D-08 + RESEARCH corrections #1 + #2.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/migrations/ | grep -E '^[0-9]{14}_(cohort_definitions|cohort_membership_matview|cohort_matview_refresh_cron)\.sql$' | wc -l | grep -q '^\s*3\s*$' && grep -q 'create unique index cohort_membership_pk' supabase/migrations/*_cohort_membership_matview.sql && grep -q 'with no data' supabase/migrations/*_cohort_membership_matview.sql && grep -q 'refresh materialized view public.cohort_membership' supabase/migrations/*_cohort_membership_matview.sql && ! grep 'refresh materialized view concurrently public.cohort_membership' supabase/migrations/*_cohort_membership_matview.sql && grep -q "7,22,37,52 \* \* \* \*" supabase/migrations/*_cohort_matview_refresh_cron.sql</automated>
  </verify>
  <done>3 migrations exist; matview has UNIQUE index; initial refresh is NON-concurrent per RESEARCH correction #1; cron schedule is the staggered `7,22,37,52` per correction #2; cohort_is_member helper grants execute to authenticated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cohort SECDEF RPCs + plpgsql defensive validator (cohort_rule_matches)</name>
  <files>
    supabase/migrations/20260601000012_cohort_rpcs.sql
  </files>
  <behavior>
    - cohort_define(p_name, p_rule) gates on is_admin_at_least('admin'); raises 'forbidden' (42501) otherwise
    - cohort_define defensive validator: walks p_rule jsonb recursively (plpgsql function cohort_validate_rule) — rejects any leaf.field NOT IN the 15-string allowlist (hardcoded CASE statement); raises 'invalid_rule' (22023)
    - cohort_define inserts with status='draft' + created_by=auth.uid(); raises 'duplicate_name' (23505) on UNIQUE violation
    - cohort_set_status(p_cohort_id, p_status) — for draft→active (admin) and active→archived (superadmin); raises 'forbidden' for archived transitions if caller not superadmin
    - cohort_archive(p_cohort_id) — superadmin-only thin wrapper calling cohort_set_status(p_cohort_id, 'archived')
    - cohort_rule_matches(p_rule, p_user_id) — used by the matview body; recursively walks rule tree + 15-field allowlist hardcoded; returns boolean; SECURITY DEFINER + STABLE for matview-build performance
    - Every cohort_define / cohort_set_status / cohort_archive calls log_admin_action AND uses `perform set_config('app.suppress_audit', 'true', true)` per Pattern S3
  </behavior>
  <action>
    Create supabase/migrations/<ts>_cohort_rpcs.sql. Copy SECDEF skeleton verbatim from supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql lines 145–203 (verbatim shape per Pattern S3). Five new SQL functions in this file: (1) `cohort_validate_rule(p_rule jsonb, p_depth int default 0) returns void` — plpgsql; if `p_rule->>'op'` in ('and','or') then iterate children (`p_rule->'children'`) and recurse with depth+1; if depth > 8 raise 'invalid_rule'; if leaf, check `p_rule->>'field'` against the hardcoded 15-string CASE WHEN list and raise 'invalid_rule' if not in it; check `p_rule->>'op'` against the 9-op CASE list. (2) `cohort_define(p_name text, p_rule jsonb) returns uuid` — auth check, is_admin_at_least('admin') check, perform cohort_validate_rule(p_rule), perform set_config('app.suppress_audit','true',true), insert into cohort_definitions returning id, insert into audit_logs (action_name='cohort_defined', target_user_id=auth.uid(), after_data=jsonb_build_object('name',p_name,'rule',p_rule), source='rpc'), return id. (3) `cohort_set_status(p_cohort_id uuid, p_status text) returns void` — gate on `if p_status='archived' and not is_admin_at_least('superadmin') then raise forbidden`; update + audit. (4) `cohort_archive(p_cohort_id uuid) returns void` — thin wrapper: `perform cohort_set_status(p_cohort_id, 'archived')`. (5) `cohort_rule_matches(p_rule jsonb, p_user_id uuid) returns boolean` — recursive plpgsql; loads `select * from profiles where id=p_user_id into v_profile`; switches on `p_rule->>'op'`: for and/or, iterate children + short-circuit; for leaf, switch on field name (CASE WHEN), compute the comparison using v_profile.<field> against `p_rule->'value'`. The matview migration (Task 2) calls this function in its WHERE clause. Mark cohort_rule_matches as STABLE + SECURITY DEFINER + set search_path = public, pg_catalog. End each function with revoke-all + grant-execute-to-authenticated. Per CONTEXT D-05 + D-09 + D-21; per Pattern S1 + S3.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/migrations/*_cohort_rpcs.sql | head -1 | xargs -I{} bash -c 'grep -c "security definer" "{}" | grep -q "^[5-9]" && grep -q "cohort_validate_rule" "{}" && grep -q "cohort_rule_matches" "{}" && grep -q "is_admin_at_least..admin" "{}" && grep -q "is_admin_at_least..superadmin" "{}" && grep -q "set_config..app.suppress_audit" "{}" && grep -c "raise exception .invalid_rule" "{}" | grep -q "^[1-9]"'</automated>
  </verify>
  <done>5 SECDEF functions defined; recursive validator hardcodes 15-field allowlist; archive gated on superadmin; cohort_rule_matches consumed by matview body; audit suppression set_config present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Cohort API wrapper + tests</name>
  <files>
    leanshot/src/lib/cohort/api.ts,
    leanshot/src/lib/cohort/api.test.ts
  </files>
  <behavior>
    - defineCohort(name, rule) → returns {cohortId:string}; client-side schema parse BEFORE RPC call (fails fast with ZodError on invalid tree)
    - archiveCohort(cohortId) → returns void; maps 42501 → CohortApiError('not_staff')
    - setCohortStatus(cohortId, status) → returns void
    - isCohortMember(userId, cohortId) → calls cohort_is_member RPC → returns boolean (consumer-side helper for PAYWALL/RECOMMEND/SAVE/GAME plans in later phases)
    - mapRpcError SQLSTATE switch: 42501→not_staff, 28000→not_authenticated, 22023+message_contains('invalid_rule')→invalid_rule, 23505→duplicate_name, else→network/unknown
  </behavior>
  <action>
    Tests FIRST. Mirror the verbatim shape of leanshot/src/lib/admin/affiliate-review.ts lines 19–82 (read first). Export `CohortApiErrorCode` discriminated union per the contracts above + `CohortApiError extends Error`. Internal `callCohortRpc(rpcName, params)` mirror of analog; tries `supabase.rpc(...)` + catches + throws CohortApiError. Public `defineCohort(name, rule)` first runs `ruleTreeSchema.parse(rule)` to catch malformed input before hitting network; on ZodError re-throws as `CohortApiError('invalid_rule', {cause: zodError})`. Then `await callCohortRpc('cohort_define', {p_name: name, p_rule: rule})`. `archiveCohort` calls cohort_archive. `setCohortStatus` calls cohort_set_status. `isCohortMember` calls cohort_is_member. api.test.ts (≥5 tests): (a) defineCohort with malformed tree → CohortApiError('invalid_rule'); (b) defineCohort with valid tree + mocked RPC returning {data: 'uuid-x'} → returns 'uuid-x'; (c) RPC error 42501 → CohortApiError('not_staff'); (d) RPC error 23505 → CohortApiError('duplicate_name'); (e) isCohortMember returns boolean. Per Pattern S7. Run `npm test --run src/lib/cohort/api.test.ts` GREEN.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npm test -- --run src/lib/cohort/api.test.ts 2>&1 | tail -10 | grep -E 'Tests.*passed' | grep -v '0 passed'</automated>
  </verify>
  <done>api.test.ts green ≥5 tests; defineCohort fails fast on invalid tree (no RPC call) before hitting network; SQLSTATE→error-code matrix covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Hand-rolled visual builder UI (AdminCohortBuilder + CohortRuleNode + CohortFieldPicker + AdminCohortList)</name>
  <files>
    leanshot/src/components/admin/cohort/CohortFieldPicker.tsx,
    leanshot/src/components/admin/cohort/CohortRuleNode.tsx,
    leanshot/src/components/admin/cohort/AdminCohortBuilder.tsx,
    leanshot/src/components/admin/cohort/AdminCohortList.tsx
  </files>
  <behavior>
    - CohortFieldPicker renders a select dropdown with EXACTLY the 15 FIELD_ALLOWLIST entries — user cannot pick anything else (UI-enforced first layer of defense)
    - CohortRuleNode renders recursively: if branch → renders AND/OR toggle + child rows + "Add condition" + "Add group" + "Remove group" buttons; if leaf → renders field picker + op picker + value input (typed based on field — text/number/boolean/select-options)
    - AdminCohortBuilder hosts the recursive tree, runs `ruleTreeSchema.safeParse(tree)` on each change, displays inline errors (e.g. "Tree depth 9 exceeds MAX_DEPTH 8"), disables Save when invalid
    - Save click calls `defineCohort(name, tree)` → on success toast + clear builder + reload list; on `duplicate_name` show inline error on name input
    - AdminCohortList renders verbatim shape of AdminAffiliatesReviewQueue (filter pill by status + row list + per-row Archive button); Archive click confirms then calls `archiveCohort(id)`
    - All actions audit-logged server-side (per Task 3 RPCs); no client-side audit calls needed
  </behavior>
  <action>
    Read leanshot/src/components/admin/AdminAffiliatesReviewQueue.tsx FIRST (lines 83–101 filter pill, lines 121–199 list, lines 134–161 isStaff probe). CohortFieldPicker: simple select using `<select>` from leanshot/src/components/ui/` primitives; options come from `FIELD_ALLOWLIST.map(f => ({value: f, label: humanize(f)}))`; humanize is a local map (`'tier' → 'Tier'`, `'days_since_signup' → 'Days since signup'`). CohortRuleNode is the recursive piece: receives `node: RuleNode`, `onChange: (next: RuleNode) => void`, `depth: number`, `onRemove?: () => void`. Branch case renders a bordered card with AND/OR toggle + children mapped via `<CohortRuleNode key={i} node={c} onChange={(next) => updateChild(i, next)} depth={depth+1} onRemove={() => removeChild(i)} />` + "Add condition" button (adds a default leaf) + "Add group" button (adds `{op:'and', children:[default-leaf]}`) + "Remove group" if onRemove provided. Leaf case renders 3 inputs in a row: CohortFieldPicker, OpPicker (the 9 ops, but disable irrelevant ones based on field type — e.g. text fields hide `>` `<`), and a value input (text/number/boolean/multi-select-for-`in`). AdminCohortBuilder owns the tree state via `useState<RuleNode>(...)` + a name input + a Save button. On every state change call `ruleTreeSchema.safeParse(tree)` and set local error. Save disabled when error or name empty. Save click: `await defineCohort(name, tree); useToast.success('Cohort created (draft)'); onSaved()`. AdminCohortList: mirror AdminAffiliatesReviewQueue shape verbatim — isStaff probe (lines 134–161 verbatim), filter pill by status (draft/active/archived/all), row map with name + status badge + created_at + Archive button (Archive only enabled for active status + superadmin role probe). All UI inside `/Users/karstenhaldan/minisite/leanshot/src/components/admin/cohort/`. Mount AdminCohortBuilder + AdminCohortList behind a new admin module entry — but since Phase 24 owns ADMIN_MODULES, this plan must NOT modify modules.ts (per [[feedback_planner_iter1_anti_patterns]] no-shared-file-choreography). Instead: this plan ships the components only; the ADMIN_MODULES entry for "Cohorts" is owned by Phase 24's manifest (already declared per 24-03-PLAN.md as one of the 12 placeholder entries). Confirm by grep: `grep -i cohort leanshot/src/lib/admin/modules.ts` — if entry exists, point its lazy() at this plan's AdminCohortBuilder + AdminCohortList wrapped in a single page component (CohortsPage.tsx) and write that wrapper here. If entry does NOT exist, document the gap in the SUMMARY and recommend a Phase 27 ADDENDUM at integration time. Per CONTEXT D-05; per Claude's Discretion (UI primitives).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npm run typecheck 2>&1 | tail -15 | grep -E 'error TS.*cohort' && exit 1 || true; npm run lint -- src/components/admin/cohort/ 2>&1 | tail -10 | grep -E '✖|error' && exit 1 || echo OK</automated>
  </verify>
  <done>4 UI files compile + lint; CohortFieldPicker is exhaustive of FIELD_ALLOWLIST; CohortRuleNode is recursive; AdminCohortBuilder enforces MAX_DEPTH client-side via ruleTreeSchema.safeParse; AdminCohortList mirrors AdminAffiliatesReviewQueue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Playwright e2e — define + promote + archive cohort, verify matview population</name>
  <files>
    leanshot/e2e/admin-cohort-builder.spec.ts
  </files>
  <behavior>
    - Test "admin defines 'free users >7d' cohort, promotes to active, matview includes matching users" passes
    - Seeds 10 profiles via supabase admin (5 free + signup >7d ago, 5 pro)
    - Logs in as admin via addInitScript
    - Navigates to /#/admin/cohorts; opens builder
    - Builds tree: AND[leaf(tier,=,'free'), leaf(days_since_signup,>,7)] via clicks
    - Saves; asserts cohort_definitions row exists with status='draft'
    - Promotes draft→active via list UI Promote button (or calls cohort_set_status directly if no Promote UI in v1 per builder note above)
    - Calls `supabase.rpc('refresh_cohort_membership')` OR direct SQL `refresh materialized view public.cohort_membership` via admin client to force immediate refresh (cron tick would be too slow for tests)
    - Asserts cohort_membership has 5 rows matching the 5 free+aged users
    - Archive button click → cohort status='archived'; next refresh → cohort_membership rows for this cohort = 0
    - afterAll cleanup
  </behavior>
  <action>
    Create leanshot/e2e/admin-cohort-builder.spec.ts using same scaffold as e2e/admin-bulk-actions.spec.ts (Plan 27-01) — file-scoped slug, supabase admin fixture, addInitScript session, hash-route nav. Seed via service-role `from('profiles').insert([...])`; set `created_at` to 8 days ago for the 5 free users (or use a service-role direct UPDATE if insert doesn't allow created_at override). For the matview refresh in test, use supabase admin to execute `refresh materialized view public.cohort_membership` via a raw SQL call (`supabase.rpc('exec_sql', ...)` only if such helper exists; otherwise use `psql` via subprocess as a last resort — recommended approach: add a `cohort_refresh_now()` SECDEF RPC gated on superadmin in Task 3 specifically for tests). Document the test-RPC in SUMMARY. Build the tree via clicks: locator('button', {name:'Add condition'}).click(); locator('select', {name:'Field'}).first().selectOption('tier'); etc. Save → wait for response on `**/rest/v1/rpc/cohort_define`. Promote → call setCohortStatus directly via supabase admin client (avoids needing a UI promote button in v1). Refresh matview. Query cohort_membership directly. Assert row count = 5. Click Archive → confirm → wait for cohort_archive response. Refresh again. Assert row count = 0. Per CONTEXT D-05/D-07/D-09 + 27-VALIDATION Wave 0.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx playwright test e2e/admin-cohort-builder.spec.ts --reporter=line 2>&1 | tail -15 | grep -E 'passed|✓'</automated>
  </verify>
  <done>e2e spec passes; cohort_membership row count proven against DB query (not UI traversal); archive properly excludes from next refresh.</done>
</task>

<task type="auto">
  <name>Task 7: [BLOCKING] supabase db push --linked + matview refresh + cron sanity</name>
  <files>supabase/migrations/</files>
  <action>
    From /Users/karstenhaldan/minisite, run `supabase db push --linked 2>&1 | tee /tmp/27-02-push.log`. Grep `^Skipping` and fail on any skip. After push, verify the initial REFRESH ran by querying `supabase db query --linked "select count(*) from public.cohort_membership"` (expect 0 with no active cohorts yet — that's correct). Verify cron is scheduled: `supabase db query --linked "select jobname, schedule from cron.job where jobname='cohort-membership-refresh'"` (expect `7,22,37,52 * * * *`). Verify NO collision: `supabase db query --linked "select jobname, schedule from cron.job order by schedule"` — manually confirm no other cron is on `7,22,37,52` (anomaly cron from Plan 27-04 is `*/5` which lands on 0,5,10,15,...,55 — non-colliding with 7,22,37,52). Verify cohort_membership UNIQUE index exists: `supabase db query --linked "select indexname from pg_indexes where tablename='cohort_membership'"` (expect cohort_membership_pk). Per [[reference_supabase_db_query_linked]] + [[reference_supabase_migration_filename_regex]].
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && supabase db query --linked "select count(*) from pg_matviews where matviewname='cohort_membership'" 2>&1 | grep -q '^\s*1\s*$' && supabase db query --linked "select schedule from cron.job where jobname='cohort-membership-refresh'" 2>&1 | grep -q '7,22,37,52' && supabase db query --linked "select count(*) from pg_indexes where indexname='cohort_membership_pk'" 2>&1 | grep -q '^\s*1\s*$'</automated>
  </verify>
  <done>cohort_membership matview live with UNIQUE index; cron scheduled at staggered `7,22,37,52`; zero schedule collisions; 5 SECDEF cohort RPCs callable via supabase rpc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → cohort_define RPC | JWT carries admin_role; RPC re-verifies via is_admin_at_least; defensive plpgsql validator re-checks 15-field allowlist |
| cohort_rule_matches → profiles | SECURITY DEFINER; bypasses RLS; allowlist guarantees only profile fields in the safe-15 are referenced |
| cron → matview refresh | postgres-superuser context (pg_cron); refresh CONCURRENTLY non-blocking to readers |
| consumer (Phase 38/39/40) → cohort_is_member RPC | SECDEF helper; returns boolean; caller learns membership without learning rule contents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | SQL injection via rule.value | mitigate | rule-tree-to-sql.ts emits PARAMETERIZED SQL only (test (f) in rule-tree-to-sql.test.ts asserts value never inlined); plpgsql cohort_rule_matches uses jsonb operators not string concatenation |
| T-27-02-02 | Tampering | rule.field bypass to read non-allowlisted profile columns | mitigate | Defense-in-depth: (1) TS zod enum FIELD_ALLOWLIST, (2) plpgsql cohort_validate_rule hardcoded CASE allowlist, (3) plpgsql cohort_rule_matches hardcoded CASE for column access — three independent layers per [[feedback_planner_iter1_anti_patterns]] |
| T-27-02-03 | Denial of Service | pathological rule tree depth 10000 | mitigate | MAX_DEPTH=8 enforced at zod schema parse (client) + plpgsql cohort_validate_rule recursion depth check + branch.children max 50 |
| T-27-02-04 | Elevation of Privilege | non-superadmin archives a cohort | mitigate | cohort_set_status raises 'forbidden' when p_status='archived' and caller not is_admin_at_least('superadmin') |
| T-27-02-05 | Repudiation | cohort lifecycle action without trace | mitigate | every define/promote/archive writes audit_logs via log_admin_action with source='rpc'; audit-suppression set_config prevents duplicate trigger-fire |
| T-27-02-06 | Information Disclosure | matview leaks (user_id, cohort_id) to non-admin | accept | matview is granted only to service_role; cohort_is_member SECDEF helper exposes only a boolean; raw matview rows never returned to client per Pattern S2 |
| T-27-02-07 | Denial of Service | matview refresh blocks reads | mitigate | REFRESH CONCURRENTLY (subsequent ticks) is non-blocking; UNIQUE index satisfies CONCURRENTLY requirement; initial NON-concurrent refresh executes inside migration (one-time) |
| T-27-02-08 | Tampering | direct UPDATE to cohort_definitions bypassing audit | mitigate | RLS denies authenticated UPDATE (no policy); only SECDEF RPCs write; matview reads only `status='active'` so archived rows are functionally orphaned (correct) |
</threat_model>

<verification>
- Unit tests green: `npm test --run src/lib/cohort/` (Tasks 1 + 4)
- Migrations land cleanly: `supabase db push --linked` zero skips (Task 7)
- Matview live + indexed: `pg_matviews` + `pg_indexes` checks (Task 7)
- Cron scheduled: `cron.job` table query (Task 7)
- e2e: `npx playwright test e2e/admin-cohort-builder.spec.ts` green (Task 6)
- Sub-50ms p99 cohort_is_member read: spot-check via `supabase db query --linked "explain analyze select cohort_is_member(...)"` (manual)
</verification>

<success_criteria>
- Phase SC#2 satisfied: admin defines cohort in builder; cohort reusable across PAYWALL/RECOMMEND/SAVE/GAME/TAXO via cohort_is_member helper
- Phase SC#4 satisfied: matview refreshes via pg_cron + serves sub-50ms p99 reads (UNIQUE index + small row count <100k × <20 cohorts)
- TAXO-03 satisfied: cohort_definitions + cohort_membership matview shipped with 15-min staggered cron
- 15-field allowlist is single-source-of-truth in TS, mirrored in 3 plpgsql layers (validator, rule_matches, audit)
- Status machine ownership: cohort_define writes draft; cohort_set_status writes active|archived; cohort_archive is superadmin-only wrapper per [[feedback_status_machine_transition_owner]]
- RESEARCH correction #1 honored: initial refresh is NON-concurrent, subsequent are CONCURRENTLY
- RESEARCH correction #2 honored: cron schedule `7,22,37,52` not `*/15` — avoids quarter-hour collision with Plan 27-04's `*/5` anomaly cron
</success_criteria>

<output>
After completion, create `.planning/phases/27-modular-admin-shell-extensions/27-02-SUMMARY.md` documenting: migrations (4 ts files), 5 SECDEF functions shipped (validate_rule, define, set_status, archive, rule_matches), cohort_is_member consumer helper, 15-field allowlist single-source-of-truth in TS + 3 plpgsql mirrors, MAX_DEPTH=8, matview body + UNIQUE index + 2-step refresh pattern, pg_cron staggered schedule, UI components, e2e, and the cohort_refresh_now() test helper (if added). Document the ADMIN_MODULES "Cohorts" entry status (present-already vs deferred-to-addendum) so verifier knows whether to flag.
</output>
