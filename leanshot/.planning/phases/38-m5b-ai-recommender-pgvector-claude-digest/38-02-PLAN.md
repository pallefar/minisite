---
phase: 38-m5b-ai-recommender-pgvector-claude-digest
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/openai-embed.ts
  - supabase/functions/_shared/openai-embed.test.ts
  - supabase/functions/_shared/anthropic-summarize.ts
  - supabase/functions/_shared/anthropic-summarize.test.ts
  - supabase/functions/_shared/baa-scope.ts
  - supabase/functions/_shared/baa-scope.test.ts
  - supabase/functions/_shared/digest-schema.ts
  - supabase/functions/_shared/digest-schema.test.ts
  - supabase/functions/_shared/render-user-context.ts
  - supabase/functions/_shared/render-user-facts.ts
  - supabase/functions/_shared/recommender-rank.ts
  - supabase/functions/_shared/recommender-rank.test.ts
  - supabase/functions/_shared/anthropic-baa-allowlist.ts
  - supabase/functions/import_map.json
autonomous: true
requirements: [RECOMMEND-02, RECOMMEND-05, RECOMMEND-07]
must_haves:
  truths:
    - "openai-embed.ts wraps AI Gateway /embeddings; returns 1536-d vector; uses AbortController 25s timeout"
    - "anthropic-summarize.ts targets /v1/messages (NOT /chat/completions) with output_config.format.json_schema per AI-SPEC §3 Pitfall #2"
    - "BAA-scope guard runs BEFORE prompt build (Phase 25 HIPAA-01); breadcrumb baa.scope.resolved precedes anthropic.messages.create"
    - "anthropic-baa-allowlist.ts includes claude-sonnet-4-6 (extends current allowlist that only has claude-sonnet-4-5)"
    - "DigestOutputSchema enforces WHITELIST_ACTION_IDS enum + reason ≤120 chars + clinical-action keyword blocklist"
    - "recommender-rank.ts re-ranks by recency, dismissed, locale, surface_target tag, D-13 auto-approve-KB"
    - "render-user-facts caps inputs (top 5 symptoms, last 7 weights, last 14 injections) per AI-SPEC §4b"
  artifacts:
    - path: supabase/functions/_shared/openai-embed.ts
      provides: "embed(text) → number[1536]"
      exports: ["embed"]
    - path: supabase/functions/_shared/anthropic-summarize.ts
      provides: "summarizeDigest(facts, orgId) → DigestOutput via /v1/messages"
      exports: ["summarizeDigest", "generateDigestWithRetry"]
    - path: supabase/functions/_shared/baa-scope.ts
      provides: "resolveBaaScope(userId) → {credential, isClinical, orgId}; assertBaaScope wrapper"
      exports: ["resolveBaaScope", "assertBaaScope", "BaaScopeError"]
    - path: supabase/functions/_shared/digest-schema.ts
      provides: "Zod schemas + WHITELIST_ACTION_IDS + digestJsonSchema + CLINICAL_KEYWORD_BLOCKLIST"
      exports: ["WHITELIST_ACTION_IDS", "DigestActionSchema", "DigestOutputSchema", "digestJsonSchema", "CLINICAL_KEYWORD_BLOCKLIST"]
    - path: supabase/functions/_shared/anthropic-baa-allowlist.ts
      provides: "extended BAA_COVERED_MODELS"
      contains: "claude-sonnet-4-6"
  key_links:
    - from: supabase/functions/_shared/anthropic-summarize.ts
      to: supabase/functions/_shared/baa-scope.ts
      via: "resolveBaaScope called BEFORE summarizeDigest builds prompt"
      pattern: "resolveBaaScope.*before.*summarizeDigest|baa\\.scope\\.resolved"
    - from: supabase/functions/_shared/anthropic-summarize.ts
      to: supabase/functions/_shared/anthropic-baa-allowlist.ts
      via: "assertBaaCoveredModel called on clinical path"
      pattern: "assertBaaCoveredModel"
    - from: supabase/functions/_shared/digest-schema.ts
      to: supabase/functions/_shared/anthropic-summarize.ts
      via: "DigestOutputSchema.parse on model response"
      pattern: "DigestOutputSchema\\.parse"
---

<objective>
Build the `_shared/` helper layer that Phase 38 Edge Functions (Plans 38-03..38-08) depend on: OpenAI embed wrapper, Anthropic Messages API wrapper (`/v1/messages` NOT `/chat/completions`), BAA-scope guard (Phase 25 HIPAA-01), Zod digest schema + whitelist enum + clinical-keyword blocklist, deterministic facts/context templating, business-rule re-ranker, BAA allowlist extension for `claude-sonnet-4-6`.

Purpose: Wave 1 helper foundation. Plans 38-03..38-08 import from these files. Building them in Wave 1 alongside the schema (Plan 38-01) means Wave 2 plans run fully parallel.
Output: 7 helper modules + 5 unit test files + extended BAA allowlist + import_map.json alias for `shared/*`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-CONTEXT.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-RESEARCH.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-AI-SPEC.md
@supabase/functions/_shared/anthropic-baa-allowlist.ts
@supabase/functions/_shared/email-router.ts
@supabase/functions/_shared/posthog-server.ts
@supabase/functions/_shared/refusal.ts

<interfaces>
Deno deps (esm.sh URL specifiers, NO npm — memory `reference_supabase_edge_function_deploy`):
- `https://esm.sh/@supabase/supabase-js@2.105.0`
- `https://esm.sh/zod@3.23.8`

Function Secrets (set via `supabase secrets set` per env, listed in Plan 38-10 deploy):
- `AI_GATEWAY_API_KEY_CONSUMER` — Vercel AI Gateway consumer key
- `AI_GATEWAY_API_KEY_CLINICAL` — Vercel AI Gateway clinical (BAA) key
- `AI_GATEWAY_BASE_URL` = `https://ai-gateway.vercel.sh/v1`
- `ANTHROPIC_MODEL_DIGEST` = `anthropic/claude-sonnet-4-6` (hyphenated; see RESEARCH version log)
- `OPENAI_EMBED_MODEL` = `openai/text-embedding-3-small`

WHITELIST_ACTION_IDS per CONTEXT D-15 (verbatim):
`['read_kb', 'log_weight', 'log_injection', 'log_meal', 'view_curve', 'share_with_doctor', 'complete_onboarding_step', 'try_recipe', 'watch_tutorial']`

(D-15 lists `read_kb:<slug>` / `try_recipe:<slug>` / `watch_tutorial:<slug>` — the colon-suffix slug travels in a sibling `deeplink` field; the action_id enum stays simple per AI-SPEC §4b Zod pattern.)

CLINICAL_KEYWORD_BLOCKLIST regex (AI-SPEC §6 O2 verbatim):
`/\b(mg|mcg|dose|titrate|skip injection|increase|decrease|split dose|prescribe|diagnos)\b/i`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: digest-schema.ts + render-user-facts.ts + render-user-context.ts (pure data; full TDD)</name>
  <files>supabase/functions/_shared/digest-schema.ts, supabase/functions/_shared/digest-schema.test.ts, supabase/functions/_shared/render-user-facts.ts, supabase/functions/_shared/render-user-context.ts</files>
  <behavior>
    - Test 1: DigestOutputSchema.parse accepts `{narrative: "40-char-min...", actions: [{id: 'log_weight', reason: '≤120 char'}]}` → returns parsed
    - Test 2: DigestOutputSchema.parse REJECTS `actions: [{id: 'increase_dose', reason: '...'}]` (whitelist miss) → throws ZodError
    - Test 3: DigestOutputSchema.parse REJECTS narrative < 40 chars (boundary)
    - Test 4: DigestOutputSchema.parse REJECTS `actions: []` (minItems: 1; covers AI-SPEC §5 Dim 10 row 19)
    - Test 5: CLINICAL_KEYWORD_BLOCKLIST regex matches "Consider increasing to 1mg" → flagged; rejects via post-Zod `validateNoClinicalKeywords(reason)` helper
    - Test 6: digestJsonSchema (JSON-Schema object passed to AI Gateway) has `additionalProperties: false` + `actions.items.properties.id.enum === WHITELIST_ACTION_IDS`
    - Test 7: renderUserFacts caps inputs at top-5 symptoms / last-7 weights / last-14 injections (AI-SPEC §4b Context Window Management)
    - Test 8: renderUserContext returns a single string formatted per RESEARCH Pattern 1 Option B
  </behavior>
  <action>
Create `digest-schema.ts` per AI-SPEC §4b verbatim:
- `export const WHITELIST_ACTION_IDS = [...] as const;` (9 entries per CONTEXT D-15 — `read_kb`, `log_weight`, `log_injection`, `log_meal`, `view_curve`, `share_with_doctor`, `complete_onboarding_step`, `try_recipe`, `watch_tutorial`). Note: AI-SPEC §4b lists 7 entries (subset); follow CONTEXT D-15 (9) per `<context_fidelity>` rule — CONTEXT is the user's locked decision.
- `export const DigestActionSchema = z.object({ id: z.enum(WHITELIST_ACTION_IDS), reason: z.string().max(120) });`
- `export const DigestOutputSchema = z.object({ narrative: z.string().min(40).max(600), actions: z.array(DigestActionSchema).min(1).max(3) });`
- `export const digestJsonSchema = { ...JSON-Schema object... }` — must keep enum array in lockstep with WHITELIST_ACTION_IDS (import + spread).
- `export const CLINICAL_KEYWORD_BLOCKLIST = /\b(mg|mcg|dose|titrate|skip injection|increase|decrease|split dose|prescribe|diagnos)\b/i;`
- `export function validateNoClinicalKeywords(reason: string): void` — throws if regex matches; called by `generateDigestWithRetry` in `anthropic-summarize.ts`.

Create `render-user-facts.ts` (RESEARCH Pattern 3 + AI-SPEC §4 `renderUserFacts`):
- `export interface UserFactsForDigest { userId: string; orgId: string | null; weekStartIso: string; injections: Array<{at: string, site?: string}>; weights: Array<{date: string, value_kg: number}>; symptoms: Array<{name: string, count: number}>; mood?: {avg: number, n: number}; streak: {days: number, status: 'active'|'broken'|'milestone'}; missedCheckIns: string[]; redFlags: Array<{type: 'severe_abdominal_pain'|'intractable_vomiting'|'ruq_pain'|'bloody_stool'|'dehydration_ckd', loggedAt: string}>; }`
- `export function renderUserFacts(f: UserFactsForDigest): string` — caps top-5 symptoms by `count`, last-7 weights, last-14 injections; outputs deterministic block enclosed in `<user_facts>...</user_facts>` and `<whitelist>...</whitelist>` (whitelist injected from WHITELIST_ACTION_IDS).
- Sanitization per D-04: NEVER emit drug names, dose values, or specific weight values (numbers + units allowed in counts/deltas; raw weight values redacted to "weight delta -X.X lb/kg").

Create `render-user-context.ts` (RESEARCH Pattern 1 Option B):
- `export interface UserContextFacts { goalType: 'weight_loss'|'maintenance'|'metabolic'|'muscle_preservation'; glp1Phase: 'starting'|'escalation'|'maintenance'|'tapering'; injectionCount: number; weightDelta30d: number | null; units: 'kg'|'lb'; moodAvg: number | null; topSymptoms: string[]; streakDays: number; }`
- `export function renderUserContext(f: UserContextFacts): string` — returns single embedding-input string per RESEARCH §Pattern 1.

Test file uses Vitest (`*.test.ts` in `_shared/` runs under Vitest via `vitest.config.ts` workspace include; verify by `npm run test -- supabase/functions/_shared/digest-schema.test.ts`).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && npm run test -- supabase/functions/_shared/digest-schema.test.ts supabase/functions/_shared/render-user-facts.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>All Zod schema tests + render-cap tests pass. Coverage of WHITELIST_ACTION_IDS enum verified by grep `grep -c "'log_weight'" supabase/functions/_shared/digest-schema.ts` == 1 (single source of truth).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BAA-scope guard + extended allowlist + Anthropic Messages wrapper</name>
  <files>supabase/functions/_shared/anthropic-baa-allowlist.ts, supabase/functions/_shared/baa-scope.ts, supabase/functions/_shared/baa-scope.test.ts, supabase/functions/_shared/anthropic-summarize.ts, supabase/functions/_shared/anthropic-summarize.test.ts</files>
  <behavior>
    - Test 1: BAA_COVERED_MODELS array contains BOTH `claude-sonnet-4-5` (existing) AND `claude-sonnet-4-6` (Phase 38 extension) — critical for Phase 38 clinical digest calls (RESEARCH Pitfall #10).
    - Test 2: assertBaaCoveredModel('claude-sonnet-4-6') passes; assertBaaCoveredModel('claude-3-opus-20240229') throws; assertBaaCoveredModel('anthropic/claude-sonnet-4-6') passes after vendor-prefix strip.
    - Test 3: resolveBaaScope(consumer_user_id) returns `{credential: AI_GATEWAY_API_KEY_CONSUMER value, isClinical: false, orgId: null}`.
    - Test 4: resolveBaaScope(clinic_user_id) returns `{credential: AI_GATEWAY_API_KEY_CLINICAL value, isClinical: true, orgId: <uuid>}`.
    - Test 5: BREADCRUMB ORDER assertion — Sentry breadcrumb `baa.scope.resolved` MUST precede `anthropic.messages.create` (mock Sentry breadcrumb buffer; assert order). Covers AI-SPEC §5 Dim 5 — this is the load-bearing test for Phase 25 HIPAA-01 audit.
    - Test 6: summarizeDigest posts to `${AI_GATEWAY_BASE_URL.replace('/v1','')}/v1/messages` NOT `/chat/completions` (URL string assertion on mocked fetch).
    - Test 7: summarizeDigest body includes `output_config.format.json_schema` (per AI-SPEC §3 Pitfall #2; NOT `response_format`).
    - Test 8: summarizeDigest body sets `max_tokens: 1024` (REQUIRED by Anthropic — 400 if missing per RESEARCH Pitfall §4).
    - Test 9: generateDigestWithRetry retries 3x on ZodError; on 3rd failure throws (caller decides fallback per AI-SPEC §4b retry logic).
    - Test 10: On clinical path, model ID is stripped of `anthropic/` prefix before allowlist check.
  </behavior>
  <action>
Extend `supabase/functions/_shared/anthropic-baa-allowlist.ts` (existing file — see context @import): add `'claude-sonnet-4-6'` to `BAA_COVERED_MODELS`. Update `Last reviewed:` to today. Keep existing `claude-sonnet-4-5`, `claude-opus-4-6`, `claude-haiku-4-5-20251001`. Per memory `feedback_planner_iter1_anti_patterns`, the extension MUST happen in the SAME plan as the dependent consumers (this plan) — not a separate plan.

Create `baa-scope.ts`:
- `export class BaaScopeError extends Error {}` — used by Guardrail O3.
- `export interface BaaScope { credential: string; isClinical: boolean; orgId: string | null; }`
- `export async function resolveBaaScope(supabase: SupabaseClient, userId: string): Promise<BaaScope>` — loads `profiles.org_id` for userId via service-role client; selects `AI_GATEWAY_API_KEY_CLINICAL` when org_id non-null else `AI_GATEWAY_API_KEY_CONSUMER`. Emits Sentry breadcrumb `baa.scope.resolved` via `_shared/sentry.ts` BEFORE returning.
- `export function assertBaaScope(scope: BaaScope, modelId: string): void` — if `scope.isClinical`, calls `assertBaaCoveredModel(modelId.replace(/^anthropic\//, ''))` per RESEARCH Pitfall #10.
- Throws BaaScopeError if any step fails. Sentry breadcrumb order is the audit signal (AI-SPEC §5 Dim 5 + §6 O3).

Create `anthropic-summarize.ts` per RESEARCH §Code Examples + AI-SPEC §4 `summarizeDigest`:
- `summarizeDigest(userFactsText, orgId)`: resolves credential, asserts BAA scope, POSTs to `/v1/messages` with `output_config.format.json_schema: digestJsonSchema`, 25s AbortController, parses `j.content.find(b => b.type==='text').text` via DigestOutputSchema, returns DigestOutput.
- `generateDigestWithRetry(facts, orgId, captureFn)`: 3-attempt retry per AI-SPEC §4b verbatim; 500ms then 2s backoff; logs `digest.validation_failed` via posthog-server.
- NEVER log raw model output, raw user facts, or prompt (AI-SPEC §4b — PHI risk).
- `SYSTEM_PROMPT_DIGEST` constant matches AI-SPEC §4b verbatim (4 HARD RULES).

Vitest tests: mock fetch via vi.spyOn(globalThis, 'fetch'); mock Sentry breadcrumb buffer; assert URL, body shape, breadcrumb order.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && npm run test -- supabase/functions/_shared/baa-scope.test.ts supabase/functions/_shared/anthropic-summarize.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>All 10 behaviors pass; `grep -c "claude-sonnet-4-6" supabase/functions/_shared/anthropic-baa-allowlist.ts` ≥ 1; `grep -c "/v1/messages" supabase/functions/_shared/anthropic-summarize.ts` ≥ 1 and `grep -c "/chat/completions" supabase/functions/_shared/anthropic-summarize.ts` == 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OpenAI embed wrapper + recommender-rank business-rule re-ranker + import_map alias</name>
  <files>supabase/functions/_shared/openai-embed.ts, supabase/functions/_shared/openai-embed.test.ts, supabase/functions/_shared/recommender-rank.ts, supabase/functions/_shared/recommender-rank.test.ts, supabase/functions/import_map.json</files>
  <behavior>
    - Test 1: embed(text) POSTs to `${AI_GATEWAY_BASE_URL}/embeddings`, body `{model, input}`, returns `j.data[0].embedding` (1536 floats).
    - Test 2: embed honors 25s AbortController; embed honors 429 backoff (Retry-After header → exponential cap 3 retries per RESEARCH Pitfall #3).
    - Test 3: embed throws on non-2xx with message including status code + body slice (200 char cap, no PHI).
    - Test 4: recommenderRank takes 10 candidate rows + user context (recent dismissed IDs, locale, surface_target filter) → returns 3 ranked.
    - Test 5: recommenderRank EXCLUDES rows where user dismissed `< 24h` ago (AI-SPEC §4 State Management note).
    - Test 6: recommenderRank tags each result with `surface_target: string[]` derived from caller-passed `surface` parameter (D-01 multi-surface payload).
    - Test 7: recommenderRank attaches `auto_approve_kb: true` flag when `source_type === 'kb_article'` AND `action_id ∈ {null, 'read_kb'}` (D-13).
    - Test 8: recommenderRank attaches `expires_at` = `now() + 7d` (D-03).
  </behavior>
  <action>
Create `openai-embed.ts` per RESEARCH §Code Examples:
- `export async function embed(text: string, credential?: string): Promise<number[]>` — defaults credential to `AI_GATEWAY_API_KEY_CONSUMER` (embeddings are non-PHI for content; for user-context text, the same key path is fine — user-context embedding does NOT travel through clinical BAA path because the embed-only call is to OpenAI not Anthropic; document inline per RESEARCH Pitfall #9 / Pre-embed scrub).
- AbortController 25s; 429 → honor Retry-After (parse seconds), max 3 retries; emit `embed.rate_limit_429` via posthog-server.
- Pre-embed scrub: import refusal patterns from `_shared/refusal.ts` (existing) and strip matches BEFORE the fetch (RESEARCH Pitfall #9). Emit `recommendation.refusal_stripped` PostHog event when scrub fires (Guardrail O6).

Create `recommender-rank.ts`:
- `export interface RankCandidate { source_table: string; content_id: string; source_type: string; title: string; similarity: number; weighted_score: number; }`
- `export interface RankInput { candidates: RankCandidate[]; userId: string; dismissedIds: string[]; locale: string; surface: 'dashboard'|'kb_footer'|'community_feed'|'course_landing'; excludeContentId?: string; }`
- `export interface RankedRecommendation { recommendation_id: string; source_type: string; source_id: string; title: string; deeplink: string; score: number; surface_target: string[]; action_id?: string; expires_at: string; auto_approve_kb: boolean; }`
- `export function rankRecommendations(input: RankInput): RankedRecommendation[]` — implements D-01 (multi-surface tag), D-03 (expires_at 7d, content-only fallback when no action_id), D-13 (auto-approve KB), D-15 (whitelist action_id only).
- Deeplink derivation: kb_article → `/kb/<source_id>`, blog_post → `/blog/<source_id>`, community_post → `/community/p/<source_id>` (shim, D-01), course_lesson → `/courses/lesson/<source_id>` (shim, D-01).

Create/extend `supabase/functions/import_map.json` per memory `reference_supabase_functions_deploy_import_map_flag` (CLI deprecates but honors `--import-map` — STILL REQUIRED if Edge Fns alias `shared/`). Add aliases:
```
{ "imports": { "shared/": "./_shared/" } }
```
If file already exists, MERGE — do not overwrite. Verify by `cat supabase/functions/import_map.json | jq '.imports'`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && npm run test -- supabase/functions/_shared/openai-embed.test.ts supabase/functions/_shared/recommender-rank.test.ts 2>&1 | tail -15; cat supabase/functions/import_map.json | jq -e '.imports["shared/"]'</automated>
  </verify>
  <done>Embed wrapper handles 429 with Retry-After; recommender-rank tests all pass; import_map has shared/ alias.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge Fn → AI Gateway | Bearer token crosses; consumer vs clinical key selection enforced by `_shared/baa-scope.ts` |
| User text → embed call | Adversarial prompt-injection scrub (refusal regex bank) crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-07 | Information Disclosure | anthropic-summarize.ts | mitigate | resolveBaaScope MUST run BEFORE prompt build; Sentry breadcrumb order asserted in test (AI-SPEC §5 Dim 5 + §6 O3) |
| T-38-08 | Tampering | digest-schema.ts whitelist enforcement | mitigate | Zod enum (compile-time + runtime) + CLINICAL_KEYWORD_BLOCKLIST regex (post-Zod) — 2-layer defense per AI-SPEC §6 O1/O2 |
| T-38-09 | Tampering | embed input (prompt injection via user-context text) | mitigate | Pre-embed scrub via _shared/refusal.ts; emit `recommendation.refusal_stripped` event (Guardrail O6) |
| T-38-10 | Information Disclosure | anthropic-baa-allowlist.ts | mitigate | claude-sonnet-4-6 extension lands in SAME plan as dependent consumer (no temporal gap where clinical calls 403) |
| T-38-11 | Repudiation | generateDigestWithRetry | mitigate | Logs attempt + error class + error summary (NEVER raw model output, NEVER raw user facts, NEVER prompt) |
| T-38-12 | Denial of Service | embed 429 retry storm | mitigate | Cap 3 retries; honor Retry-After; emit `embed.rate_limit_429` for alerting |
</threat_model>

<verification>
`grep -E "'claude-sonnet-4-6'" supabase/functions/_shared/anthropic-baa-allowlist.ts` returns 1 line. `grep -E "/v1/messages" supabase/functions/_shared/anthropic-summarize.ts` ≥ 1; `grep "/chat/completions" supabase/functions/_shared/anthropic-summarize.ts` returns 0. All 5 test files pass Vitest run.
</verification>

<success_criteria>
All `_shared/` helpers ready to be imported by Wave 2 Edge Functions (38-03..38-06). BAA-scope breadcrumb order assertion is the load-bearing test — covers Phase 25 HIPAA-01 audit pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-02-SUMMARY.md`.
</output>
