---
phase: 38-m5b-ai-recommender-pgvector-claude-digest
plan: 05
type: execute
wave: 2
depends_on: [38-01, 38-02]
files_modified:
  - supabase/functions/weekly-digest/index.ts
  - supabase/functions/weekly-digest/index.test.ts
  - supabase/functions/weekly-digest/timezone.test.ts
  - supabase/functions/_shared/digest-email-template.ts
autonomous: true
requirements: [RECOMMEND-05, RECOMMEND-07]
must_haves:
  truths:
    - "weekly-digest Edge Fn resolves users.org_id BEFORE building prompt (Phase 25 HIPAA-01; AI-SPEC §5 Dim 5)"
    - "Uses /v1/messages with output_config.format.json_schema (NOT /chat/completions; AI-SPEC §3 Pitfall #2)"
    - "Routes via Resend consumer path (D-04 sanitized narrative = non-PHI for all tiers v1)"
    - "Red-flag detection in render-user-facts triggers escalation rewrite + open_symptom_check first action (Guardrail O5; AI-SPEC §5 Dim 3)"
    - "Opt-IN enforcement: only sends if user_preferences.weekly_digest_opt_in = true (D-04)"
    - "DST handling: 6h dedup window per user prevents double-send on fall-back (RESEARCH Pitfall #7)"
    - "Non-KB / novel digest narratives queue ai_suggestion_review (D-12); KB-sourced auto-approved (D-13)"
  artifacts:
    - path: supabase/functions/weekly-digest/index.ts
      provides: "per-user weekly digest Edge Fn"
      contains: "Deno.serve"
      min_lines: 100
    - path: supabase/functions/_shared/digest-email-template.ts
      provides: "plaintext + HTML email body composition"
      exports: ["renderDigestEmail"]
  key_links:
    - from: supabase/functions/weekly-digest/index.ts
      to: supabase/functions/_shared/baa-scope.ts
      via: "resolveBaaScope BEFORE summarizeDigest call (breadcrumb order)"
      pattern: "resolveBaaScope.*summarizeDigest|baa\\.scope\\.resolved"
    - from: supabase/functions/weekly-digest/index.ts
      to: supabase/functions/_shared/anthropic-summarize.ts
      via: "generateDigestWithRetry returns DigestOutput"
      pattern: "generateDigestWithRetry"
    - from: supabase/functions/weekly-digest/index.ts
      to: supabase/functions/_shared/email-router.ts
      via: "routes via Resend consumer path (D-04)"
      pattern: "email-router|emailRouter|sendEmail"
---

<objective>
Ship the `weekly-digest` Edge Function: pg_cron-invoked per-user weekly digest sender. Resolves BAA scope FIRST (Phase 25 HIPAA-01; AI-SPEC §5 Dim 5 — load-bearing breadcrumb-order assertion), loads last-7-day user facts deterministically (`render-user-facts`), calls Anthropic via AI Gateway `/v1/messages` (NOT `/chat/completions`), validates structured output via Zod + clinical-keyword blocklist, routes via Resend consumer path (D-04 = non-PHI sanitized narrative for ALL tiers v1), inserts `weekly_digest_sends` audit row, captures PostHog event, queues non-KB narratives in `ai_suggestion_review` HITL queue (D-12/D-13).

Purpose: Powers RECOMMEND-05 weekly digest + RECOMMEND-07 HITL queue (digest narratives auto-queue for super-admin review unless KB-sourced). Wave 2 — parallel with 38-03, 38-04, 38-06.
Output: 1 Edge Fn + 2 test files (happy path + DST timezone edge cases) + 1 shared email-template helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-CONTEXT.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-RESEARCH.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-AI-SPEC.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-01-PLAN.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-02-PLAN.md
@supabase/functions/_shared/email-router.ts
@supabase/functions/_shared/email-layout.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: weekly-digest Edge Fn + digest-email-template helper</name>
  <files>supabase/functions/weekly-digest/index.ts, supabase/functions/weekly-digest/index.test.ts, supabase/functions/_shared/digest-email-template.ts</files>
  <behavior>
    - Test 1: Request `{user_id: <consumer>}` from cron service_role → resolveBaaScope called FIRST; AI Gateway credential used = `AI_GATEWAY_API_KEY_CONSUMER`; breadcrumb order `baa.scope.resolved` precedes `anthropic.messages.create` (Sentry mock order assertion — AI-SPEC §5 Dim 5).
    - Test 2: Request for clinic user (org_id non-null) → uses `AI_GATEWAY_API_KEY_CLINICAL`; assertBaaCoveredModel called with `claude-sonnet-4-6` (stripped vendor prefix); call succeeds (allowlist extended in Plan 38-02).
    - Test 3: Opt-out user (user_preferences.weekly_digest_opt_in = false) → handler returns 200 with `{status: 'skipped_optout'}`, no AI Gateway call (D-04).
    - Test 4: 6h dedup — if `weekly_digest_sends` has row with `sent_at > now() - interval '6 hours' AND user_id = $1`, handler returns 200 with `{status: 'skipped_dedup'}`, no AI Gateway call (RESEARCH Pitfall #7).
    - Test 5: Red-flag user (symptom_log includes severe abdominal pain >24h) → narrative MUST contain "talk to your prescriber" OR "contact your clinician"; first action MUST be `open_symptom_check`; narrative MUST NOT contain {great|crushing|push through|keep going} regex (Guardrail O5; AI-SPEC §5 Dim 3 — load-bearing).
    - Test 6: Zod parse failure → 3 retries (500ms, 2s); on 3rd → emits `digest.validation_failed`, INSERTs row with `status='failed'`, does NOT send email.
    - Test 7: Clinical-keyword leak in `reason` → blocks send + emits `digest.clinical_action_leak` Sentry alert (Guardrail O2).
    - Test 8: Successful send → emails via Resend (mocked `_shared/email-router.ts` consumer path; D-04); INSERTs `weekly_digest_sends` row with `status='sent'`, narrative, actions_jsonb, model_id, prompt_cache_hit; emits `digest.sent` PostHog event.
    - Test 9: Auto-approve KB-sourced (D-13): if all actions[].id ∈ {null, 'read_kb'} AND digest is purely KB-pointed → `weekly_digest_sends.status='sent'`, NO `ai_suggestion_review` row. Else: status='pending_review' + ai_suggestion_review row inserted (D-12 single queue, type='digest').
    - Test 10: max_tokens: 1024 + temperature: 0.4 + anthropic-version header set per RESEARCH Pattern 4.
  </behavior>
  <action>
Create `supabase/functions/weekly-digest/index.ts`:

Algorithm (locked by AI-SPEC §4 + RESEARCH Pattern 4 + CONTEXT D-04..D-07):
1. Auth: service_role only (cron caller); 403 otherwise.
2. Parse body: `{user_id}`.
3. **Phase 25 HIPAA-01 STEP — FIRST**: `const scope = await resolveBaaScope(supabase, user_id)` from `_shared/baa-scope.ts`. Sentry breadcrumb `baa.scope.resolved` emitted by helper (Guardrail O3).
4. Opt-out check: `SELECT weekly_digest_opt_in FROM user_preferences WHERE user_id = $1`. If false → INSERT `weekly_digest_sends (status='skipped_optout')`; return 200 (D-04).
5. 6h dedup check: `SELECT 1 FROM weekly_digest_sends WHERE user_id=$1 AND sent_at > now() - interval '6 hours'`. If exists → return 200 with `{status: 'skipped_dedup'}`.
6. Load user facts (last 7d): injections, weights, mood, top symptoms, streak, missed check-ins, red-flag symptoms (matching the AI-SPEC §1b list). Construct `UserFactsForDigest`.
7. Render: `const userFactsText = renderUserFacts(facts)` from `_shared/render-user-facts.ts` (caps applied; whitelist injected).
8. **Detect red flags BEFORE LLM call**: if `facts.redFlags.length > 0`, set flag `forceEscalationRewrite = true` (used to validate post-LLM output per Guardrail O5).
9. Call `const output = await generateDigestWithRetry(facts, scope.orgId)` — internally uses `summarizeDigest` which POSTs to `/v1/messages` with `output_config.format.json_schema: digestJsonSchema`, max_tokens 1024, temp 0.4, system SYSTEM_PROMPT_DIGEST.
10. **Guardrail O5 post-validation**: if `forceEscalationRewrite`, verify narrative contains prescriber-contact phrase AND first action_id === 'open_symptom_check'. If not, retry once with stronger system prompt prefix; on retry fail, REPLACE narrative with hardcoded safety template + force `open_symptom_check` action; emit `digest.redflag_rewrite`. NOTE: the WHITELIST_ACTION_IDS per CONTEXT D-15 does NOT include `open_symptom_check`; resolve this: AI-SPEC §1b uses `open_symptom_check`, AI-SPEC §4b uses both `open_symptom_check` AND `share_with_doctor`. **Per CONTEXT D-15 (locked)**, only D-15 enum is permitted. RESOLUTION: when red-flag detected, force `action_id = 'share_with_doctor'` (closest D-15 match) AND replace narrative with hardcoded safety template (which references talking to prescriber). Document this resolution inline as a code comment + emit `digest.redflag_action_substitution` event. This is per `<context_fidelity>` rule — CONTEXT D-15 wins over AI-SPEC §4b list.
11. **Guardrail O2 clinical-keyword check**: for each `output.actions[].reason`, run `CLINICAL_KEYWORD_BLOCKLIST.test(reason)`; if any match → block send + Sentry P1 + INSERT `weekly_digest_sends.status='failed'` + emit `digest.clinical_action_leak`.
12. HITL routing (D-12, D-13): determine if ALL actions are KB-sourced (`action_id ∈ {null, 'read_kb'}` AND every action's underlying source_type is 'kb_article'). If yes → auto-approve. Else → INSERT `ai_suggestion_review (type='digest', user_id, payload={narrative, actions, facts_summary}, source_type='digest_narrative', status='pending')`; weekly_digest_sends.status='pending_review'; **DO NOT send email yet** (super-admin must approve in admin shell — Plan 38-07).
13. If auto-approved: render email via `renderDigestEmail(output, user)` from new `_shared/digest-email-template.ts` (plaintext per RESEARCH discretion + `_shared/email-layout.ts` wrap). Route via `_shared/email-router.ts` consumer path (Resend, non-PHI per D-04). Use Phase 49 1-click unsubscribe pattern (D-05).
14. INSERT `weekly_digest_sends (user_id, org_id=scope.orgId, narrative, actions_jsonb, status='sent', model_id=ANTHROPIC_MODEL_DIGEST, prompt_cache_hit=<from response header>)`. Emit `digest.sent`. `posthog.shutdown()`. Return 200.

Create `supabase/functions/_shared/digest-email-template.ts`:
- `renderDigestEmail(digest: DigestOutput, user: {email, displayName?, locale}) → {subject, html, text, unsubscribeUrl}` — uses `_shared/email-layout.ts` for HTML wrap; plaintext fallback per RESEARCH discretion.
- Subject: "Your LeanShot week — {weekStartDateLocale}" (no PHI per D-04).
- Footer: 1-click unsubscribe link per Phase 49 DIGEST-04 pattern.

Vitest unit tests for all 10 behaviors using mock-fetch for AI Gateway + mock for Resend + mock supabase-js with table fixtures.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && deno test supabase/functions/weekly-digest/index.test.ts --allow-env --allow-net 2>&1 | tail -20</automated>
  </verify>
  <done>All 10 behaviors pass. `grep -c "/v1/messages" supabase/functions/weekly-digest/index.ts` ≥ 1 OR the wrapper helper is invoked (anthropic-summarize). `grep -c "resolveBaaScope" supabase/functions/weekly-digest/index.ts` ≥ 1 AND grep shows it precedes any `summarizeDigest` or `fetch` call in line ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DST timezone edge-case test suite (RESEARCH Pitfall #7)</name>
  <files>supabase/functions/weekly-digest/timezone.test.ts</files>
  <behavior>
    - Test 1: DST spring-forward (2026-03-08 02:00 → 03:00 in America/New_York): user with timezone='America/New_York' — cron fires when `now()` is within Sunday 09:00 ET; verify the digest IS sent (not skipped because the 09:00 hour doesn't "skip" — it's just shifted into UTC differently).
    - Test 2: DST fall-back (2026-11-01): local 09:00 occurs TWICE; cron WHERE includes `NOT EXISTS (SELECT 1 FROM weekly_digest_sends WHERE user_id=$1 AND sent_at > now() - interval '6 hours')` — second cron tick at the duplicate 09:00 is suppressed by 6h dedup. Verify exactly ONE row inserted.
    - Test 3: IANA name validation: timezone='UTC+5' rejected by CHECK constraint (Plan 38-01); only IANA names accepted. Insert profile with invalid tz fails.
    - Test 4: Different timezones (America/New_York, Europe/London, Asia/Tokyo) — each user receives digest at LOCAL 09:00 Sunday. Mock current_setting('timezone') or use AT TIME ZONE in test SQL.
    - Test 5: Default timezone fallback: user with NULL timezone defaults to 'America/New_York' (D-06).
  </behavior>
  <action>
Use vitest with `pg-mem` OR live linked Supabase (preferred for DST math fidelity). Insert profiles with various timezones; use `now()` mocking via `BEGIN; SET LOCAL TIMEZONE = 'UTC'; SELECT now() AT TIME ZONE 'America/New_York'; ROLLBACK;` patterns. The 6h dedup window is tested by inserting a `weekly_digest_sends` row with sent_at = 5h-ago, then calling the handler — should return skipped_dedup.

DST validation specifically: simulate the SQL cron predicate `extract(dow FROM (now() AT TIME ZONE p.timezone)) = 0 AND extract(hour FROM (now() AT TIME ZONE p.timezone)) = 9` at specific UTC timestamps that bracket DST transitions. For 2026-03-08 (US DST spring), set test `now()` to UTC times that map to 08:55, 09:05, 10:05 local — verify the cron predicate fires exactly once (at 09:05 local).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && npm run test -- supabase/functions/weekly-digest/timezone.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>All DST edge-cases pass. 6h dedup window confirmed to suppress duplicate fall-back send.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron → weekly-digest | service_role from vault; handler auth check rejects non-service-role |
| Edge Fn → Anthropic (via AI Gateway /v1/messages) | BAA-scope guard chooses CONSUMER vs CLINICAL key BEFORE prompt build |
| Edge Fn → Resend SMTP | non-PHI consumer path only (D-04 sanitized narrative; clinic tier uses same consumer path in v1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-22 | Information Disclosure | BAA scope violation (Phase 25 HIPAA-01) | mitigate | resolveBaaScope FIRST; Sentry breadcrumb order asserted (load-bearing test); assertBaaCoveredModel on clinical path; P0 alert on `digest.baa_scope_violation` |
| T-38-23 | Tampering | LLM emits non-whitelist or clinical-keyword action | mitigate | Zod enum (3-attempt retry on parse fail) + CLINICAL_KEYWORD_BLOCKLIST regex (post-Zod) + HITL queue for non-KB narratives |
| T-38-24 | Repudiation | red-flag tone minimization (false reassurance) | mitigate | Guardrail O5 post-LLM check: prescriber-contact phrase + share_with_doctor action; replace with hardcoded safety template on retry-fail |
| T-38-25 | Repudiation | duplicate send on DST fall-back | mitigate | 6h dedup window in cron WHERE + handler-side check |
| T-38-26 | Information Disclosure | PHI leakage in digest narrative (D-04) | mitigate | render-user-facts redacts raw drug/dose/weight values; Zod narrative.maxLength = 600; HARD RULE in SYSTEM_PROMPT_DIGEST forbids un-templated numerics |
| T-38-27 | Spoofing | non-cron caller forces digest send | mitigate | Auth check requires service_role; user JWT rejected with 403 |
</threat_model>

<verification>
Deploy: `supabase functions deploy weekly-digest --import-map supabase/functions/import_map.json`. Cron schedule installed by Plan 38-09 (hourly fan-out with per-user timezone check).
</verification>

<success_criteria>
RECOMMEND-05 covered. RECOMMEND-07 (HITL routing for digest narratives) covered. BAA-scope breadcrumb order assertion is THE load-bearing test for Phase 25 audit pattern.
</success_criteria>

<output>
Create `.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-05-SUMMARY.md`.
</output>
