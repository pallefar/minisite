---
phase: 38-m5b-ai-recommender-pgvector-claude-digest
plan: 03
type: execute
wave: 2
depends_on: [38-01, 38-02]
files_modified:
  - supabase/functions/recommend-next-best-action/index.ts
  - supabase/functions/recommend-next-best-action/index.test.ts
  - tests/e2e/recommender.spec.ts
  - tests/rls/recommender-cross-tenant.spec.ts
  - tests/e2e/multi-surface-payload.spec.ts
autonomous: true
requirements: [RECOMMEND-04, RECOMMEND-06, RECOMMEND-08]
must_haves:
  truths:
    - "Edge Fn recommend-next-best-action returns top-3 array with multi-surface payload per CONTEXT D-01/D-03"
    - "Sparse-history cold-start fallback fires when user has <5 events in 14d (D-02) — returns top-3 popular KB"
    - "Cross-tenant impersonation test proves user A's call never returns user B's content (project rule)"
    - "Same Edge Fn serves dashboard, kb_footer, community_feed (shim), course_landing (shim) — D-01 + RECOMMEND-08"
    - "Multi-source RPC call passes sources=['content_embeddings']; Phase 50 will pass ['content_embeddings','external_kb_embeddings']"
    - "PostHog recommendation.shown event emits with recommendation_id + surface_target + score"
  artifacts:
    - path: supabase/functions/recommend-next-best-action/index.ts
      provides: "Edge Fn entry point"
      contains: "Deno.serve"
      min_lines: 80
    - path: tests/rls/recommender-cross-tenant.spec.ts
      provides: "live impersonation proof"
      contains: "cross-tenant"
  key_links:
    - from: supabase/functions/recommend-next-best-action/index.ts
      to: supabase/functions/_shared/openai-embed.ts
      via: "embed user-context vector"
      pattern: "import.*embed.*shared/openai-embed"
    - from: supabase/functions/recommend-next-best-action/index.ts
      to: supabase/migrations/20270705000004_phase38_match_content_embeddings_fn.sql
      via: "supabase.rpc('match_content_embeddings', { sources: ['content_embeddings'] })"
      pattern: "match_content_embeddings.*sources"
    - from: supabase/functions/recommend-next-best-action/index.ts
      to: supabase/functions/_shared/recommender-rank.ts
      via: "rankRecommendations on candidate rows"
      pattern: "rankRecommendations"
---

<objective>
Ship the `recommend-next-best-action` Edge Function: user-triggered top-3 next-best-action recommender. Builds deterministic user-context text (RESEARCH Pattern 1 Option B), embeds via AI Gateway, calls `match_content_embeddings` RPC with `sources=['content_embeddings']` (Phase 50 future-proof), re-ranks via `_shared/recommender-rank.ts`, returns multi-surface payload (D-01), falls back to popularity baseline for sparse-history users (D-02), captures `recommendation.shown` PostHog event with all 4 surfaces.

Purpose: Core RECOMMEND-04 + RECOMMEND-08 surface. Wave 2 — depends on Wave 1 schema (38-01) + helpers (38-02). Parallel with 38-04, 38-05, 38-06 (no file overlap).
Output: 1 Edge Fn + 1 Deno unit test + 3 integration/e2e tests (one is the load-bearing RLS cross-tenant impersonation proof).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-CONTEXT.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-RESEARCH.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-AI-SPEC.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-01-PLAN.md
@.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-02-PLAN.md

<interfaces>
Request shape:
```
POST /recommend-next-best-action
Authorization: Bearer <user JWT> OR <service_role for cron>
Body: {
  user_id: string,                      // required
  surface: 'dashboard'|'kb_footer'|'community_feed'|'course_landing',  // D-01
  exclude_content_id?: string,          // for kb_footer: don't recommend the article you're on
  sources?: string[],                   // default ['content_embeddings']; Phase 50 extension hook
  locale?: string                       // for ranking
}
```

Response shape per RESEARCH Pattern 5 (D-03 verbatim):
```
{
  recommendations: Array<{
    recommendation_id: string,
    source_type: 'kb_article'|'blog_post'|'community_post'|'course_lesson',
    source_id: string,
    title: string,
    deeplink: string,
    score: number,
    surface_target: Array<'dashboard'|'kb_footer'|'community_feed'|'course_landing'>,
    action_id?: string,
    expires_at: string
  }>,
  fallback: 'personalized' | 'popular'
}
```

Sparse-history threshold (D-02): `<5 events in last 14 days` based on `recommendation_events` + `injection_log` + `weight_log` (canonical event source TBD by codebase grep; if no unified event view, use `recommendation_events` AS PROXY for v1 and TODO note for Phase 24 TAXO unification).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: recommend-next-best-action Edge Fn + Deno unit test</name>
  <files>supabase/functions/recommend-next-best-action/index.ts, supabase/functions/recommend-next-best-action/index.test.ts</files>
  <behavior>
    - Test 1: Request with user_id + surface=dashboard → response.recommendations.length ≤ 3; each has all 8 required keys per RESEARCH Pattern 5
    - Test 2: Sparse-history user (<5 events 14d) → response.fallback === 'popular'; recommendations from globally-popular KB list (D-02)
    - Test 3: Dense-history user (≥5 events) → response.fallback === 'personalized'; cosine-ranked
    - Test 4: surface=kb_footer with exclude_content_id → returned recs do not include that ID
    - Test 5: sources defaults to ['content_embeddings'] when omitted; explicit sources=['content_embeddings'] yields identical results
    - Test 6: surface_target field includes the requested surface AND tag-fanout: kb_article result returned for dashboard surface also lists kb_footer in surface_target (D-01 multi-surface payload)
    - Test 7: PostHog mocked: each returned recommendation emits exactly one `recommendation.shown` event with recommendation_id, source_type, source_id, surface_target, score
    - Test 8: When match_content_embeddings throws (e.g., AI Gateway 500), Edge Fn returns popularity fallback (graceful degradation per AI-SPEC §4b retry)
    - Test 9: Missing user_id → 400 with `{error: 'user_id required'}`; never embeds without user_id (Guardrail O4)
    - Test 10: Service_role caller (cron) is allowed; user JWT caller is allowed; anon is rejected (401)
  </behavior>
  <action>
Create `supabase/functions/recommend-next-best-action/index.ts`:
- `Deno.serve(async (req) => ...)`.
- Auth: parse Authorization header; if anon (no JWT, no service_role), 401. Otherwise `createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, {global: {headers: {Authorization: authHeader}}})` (RLS re-imposed below).
- Parse body: `user_id`, `surface`, `exclude_content_id?`, `sources?`, `locale?`. Default `sources = ['content_embeddings']`. Validate surface enum.
- Sparse-history check (D-02): `SELECT count(*) FROM recommendation_events WHERE user_id = $1 AND shown_at > now() - interval '14 days'`. If <5, branch to popularity fallback path: `SELECT content_id, kind, title FROM content WHERE kind='kb_article' ORDER BY (SELECT count(*) FROM recommendation_events re WHERE re.source_id = content.id AND re.clicked_at IS NOT NULL AND re.shown_at > now()-interval '30 days') DESC LIMIT 3` (popularity = click count). Return with `fallback: 'popular'`.
- Personalized path: load UserContextFacts from DB joins (`profiles` for goalType/glp1Phase, `injection_log` last 30d count, `weight_log` for delta, `mood_log` avg, `symptom_log` top 3, `streak` from existing helper). Call `renderUserContext(facts)` from `_shared/render-user-context.ts`.
- `const vec = await embed(contextText)` from `_shared/openai-embed.ts`.
- `const { data, error } = await supabase.rpc('match_content_embeddings', { query_embedding: vec, match_count: 10, requesting_user_id: user_id, sources: sources })`.
- Filter: load `dismissedIds` from `recommendation_events` where `dismissed_at > now() - interval '24 hours' AND user_id = $1` (state mgmt per AI-SPEC §4).
- Call `rankRecommendations({ candidates: data, userId, dismissedIds, locale: locale ?? 'en', surface, excludeContentId })`. Take `slice(0, 3)`.
- INSERT 3 rows into `recommendation_events` (recommendation_id is server-generated UUID v4 per Open Question #1 recommendation; status=shown).
- Emit `recommendation.shown` × 3 via `captureServer` from `_shared/posthog-server.ts`. **Call `client.shutdown()` BEFORE the Response return** per memory `feedback_planner_iter1_anti_patterns` (PostHog Edge Fn pattern).
- Return JSON `{ recommendations: ranked, fallback: 'personalized'|'popular' }`.

Cold-start error handling: if `embed` throws OR `match_content_embeddings` returns error, fall through to popularity path (do not throw to caller); emit `recommendation.embedding_fallback` event with reason.

Deno unit test (`index.test.ts`) uses Deno.test + mock-fetch helper; verifies all 10 behaviors above; runs via `deno test supabase/functions/recommend-next-best-action/index.test.ts --allow-env --allow-net`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && deno test supabase/functions/recommend-next-best-action/index.test.ts --allow-env --allow-net 2>&1 | tail -15</automated>
  </verify>
  <done>All 10 unit behaviors pass. `grep -c "sources:.*content_embeddings" supabase/functions/recommend-next-best-action/index.ts` ≥ 1. PostHog `client.shutdown()` called before Response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RLS cross-tenant impersonation proof + e2e recommender + multi-surface snapshot</name>
  <files>tests/rls/recommender-cross-tenant.spec.ts, tests/e2e/recommender.spec.ts, tests/e2e/multi-surface-payload.spec.ts</files>
  <behavior>
    - Test 1 (RLS): Create user A (consumer) + user B (clinic). Insert clinic-org content with `visible_to_user_id = B.id`. Call recommender for user A. Assert ZERO returned recommendations point to user B's content_id (AI-SPEC §5 Dim 4 — load-bearing).
    - Test 2 (RLS): Same but via service_role with mis-set requesting_user_id (impersonation attempt): RPC raises EXCEPTION when requesting_user_id IS NULL (Guardrail O4). Pass mismatched user_id → only that user's visible content returned.
    - Test 3 (e2e): Dense user gets personalized; sparse user gets popular fallback; the same request with different `surface` values returns same shape.
    - Test 4 (e2e snapshot): Multi-surface payload — request with surface=dashboard, kb_footer, community_feed, course_landing each returns response matching saved snapshot (locks the contract for Phase 50 + future surfaces).
    - Test 5 (e2e perf): P95 latency < 800ms (RECOMMEND-06 gate; AI-SPEC §5 Dim 9). Run 20 sequential requests; assert p95.
  </behavior>
  <action>
Create `tests/rls/recommender-cross-tenant.spec.ts` using the Phase 25 RLS fixture pattern (memory `reference_rls_fixture_gotrueclient_flake` — admin.generateLink + /auth/v1/verify via plain fetch; NOT supabase-js signInWithPassword which cross-contaminates under vitest). Per-file TEST_SLUG_PREFIX (memory `feedback_rls_per_file_slug_prefix`). Spin up 2 users (consumer + clinic), seed 10 content rows across both visibility scopes, call Edge Fn via fetch (NOT supabase.functions.invoke — full HTTP path matches prod).

Create `tests/e2e/recommender.spec.ts`: vitest with Playwright NOT required (no UI); use fetch to hit deployed staging Edge Fn URL. Sparse-history seeding inserts 4 events; dense-history seeds 10.

Create `tests/e2e/multi-surface-payload.spec.ts`: snapshot test (`expect(response).toMatchSnapshot()`) for all 4 surface values. Snapshot lives at `tests/e2e/__snapshots__/multi-surface-payload.spec.ts.snap` and is committed — locks the contract for Phase 50.

Perf test 5 inline in `recommender.spec.ts` — uses real staging Supabase + AI Gateway. Run 20 requests serially; compute p95 from durations. If p95 > 800ms, FAIL with diagnostic (cold-start vs HNSW warm vs embed latency breakdown).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && npm run test -- tests/rls/recommender-cross-tenant.spec.ts tests/e2e/recommender.spec.ts tests/e2e/multi-surface-payload.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>RLS cross-tenant impersonation proof PASSES (zero leaks). E2E shape snapshot committed. Recommender P95 ≤ 800ms in 20-sample run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → recommend-next-best-action | user JWT crosses; RLS + explicit requesting_user_id re-imposition guards cross-tenant |
| Edge Fn → match_content_embeddings RPC | service-role bypass + explicit user_id param (defense-in-depth per Guardrail O4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-13 | Information Disclosure | cross-tenant content leak | mitigate | RPC raises EXCEPTION on null requesting_user_id; explicit `(visible_to_user_id IS NULL OR = requesting_user_id)` filter; live impersonation proof in tests/rls/ |
| T-38-14 | Tampering | recommendation_id forgery | mitigate | Server-generated UUID v4 (NOT client-supplied) per Open Question #1; written to recommendation_events at impression time |
| T-38-15 | Tampering | prompt injection via user-context text | mitigate | _shared/refusal.ts scrub pre-embed (Guardrail O6); `recommendation.refusal_stripped` PostHog event |
| T-38-16 | Denial of Service | AI Gateway / embed outage | mitigate | Cold-start popularity fallback always returns 3 recs; embed failure → graceful `fallback: 'popular'` (no thrown error to caller) |
| T-38-17 | Spoofing | service_role abuse | mitigate | Edge Fn requires either user JWT OR service_role; anon rejected with 401 |
</threat_model>

<verification>
Phase 38 Plan 38-03 verified COMPLETE when: (a) unit tests pass; (b) cross-tenant RLS test passes; (c) p95 < 800ms; (d) `supabase functions deploy recommend-next-best-action --import-map supabase/functions/import_map.json` succeeds (CLI no longer supports `--linked` per memory; link auto-read from `.temp/`).
</verification>

<success_criteria>
RECOMMEND-04 + RECOMMEND-06 (partial) + RECOMMEND-08 covered. Multi-surface snapshot is the Phase 50 + Phase 44 + Phase 46 contract.
</success_criteria>

<output>
Create `.planning/phases/38-m5b-ai-recommender-pgvector-claude-digest/38-03-SUMMARY.md`.
</output>
