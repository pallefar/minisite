---
phase: 49-m4-search-email-digests
plan: 07
type: execute
wave: 2
depends_on: [49-03, 49-04, 49-05, 49-08]
files_modified:
  - supabase/functions/community-weekly-digest/index.ts
  - supabase/functions/community-weekly-digest/deno.json
  - supabase/functions/community-weekly-digest/index.test.ts
  - supabase/functions/_shared/email-templates/community-weekly-digest.ts
  - supabase/functions/_shared/email-router.ts
autonomous: true
requirements: [DIGEST-03]
must_haves:
  truths:
    - "community-weekly-digest Edge Fn exists; same shape as daily; consumes digest_course_progress_delta_7d + digest_upcoming_events_7d_rsvpd + digest_community_top3_7d via Promise.all"
    - "isEmpty predicate: course_progress_delta_7d returns 0 rows AND upcoming_events_7d_rsvpd returns 0 rows AND community_top3_7d returns 0 rows"
    - "Opt-out check on (category='weekly_community_digest', channel='email')"
    - "sendEmail template='community_weekly_digest'; phi:false; List-Unsubscribe + List-Unsubscribe-Post headers"
    - "digest_send_log UPSERT kind='weekly' onConflict (user_id, kind, sent_date)"
    - "Deno.serve guard with COMMUNITY_WEEKLY_DIGEST_DISABLE_SERVE env var"
    - "email-router.ts EXTENDED with 2 new EmailTemplate union variants + switch arms for community_daily_digest + community_weekly_digest"
  artifacts:
    - path: "supabase/functions/community-weekly-digest/index.ts"
      provides: "Weekly digest Edge Fn — POST endpoint; service-role-bearer authed; 3-bucket weekly content + Resend send"
      contains: "handleRun"
    - path: "supabase/functions/community-weekly-digest/deno.json"
      provides: "Per-Fn import map"
      contains: "imports"
    - path: "supabase/functions/community-weekly-digest/index.test.ts"
      provides: "Deno test scaffold"
      contains: "Deno.test"
    - path: "supabase/functions/_shared/email-templates/community-weekly-digest.ts"
      provides: "subject() + render() for weekly-digest HTML"
      contains: "export function subject"
    - path: "supabase/functions/_shared/email-router.ts"
      provides: "EXTEND — add 2 EmailTemplate union variants + switch arms"
      contains: "community_weekly_digest"
  key_links:
    - from: "community-weekly-digest handleRun"
      to: "digest_course_progress_delta_7d + digest_upcoming_events_7d_rsvpd + digest_community_top3_7d"
      via: "Promise.all 3 supabase.rpc calls"
      pattern: "rpc\\('digest_(course_progress_delta_7d|upcoming_events_7d_rsvpd|community_top3_7d)'"
    - from: "email-router.ts switch"
      to: "email-templates/community-{daily,weekly}-digest.ts"
      via: "subjectFor + renderTemplate"
      pattern: "case 'community_(daily|weekly)_digest'"
---

<objective>
Ship the `community-weekly-digest` Edge Fn — near-identical to daily; deltas = bucket helper RPCs, isEmpty predicate, template name, opt-out category, disable-serve env var, log kind. Also EXTEND `_shared/email-router.ts` with 2 new EmailTemplate union variants (community_daily_digest + community_weekly_digest) so BOTH daily (Plan 49-06) and weekly route correctly.

Purpose: Per CONTEXT D-09 — Phase 38 weekly-digest (AI summary) is UNTOUCHED; Phase 49 weekly is a SEPARATE Sunday-09:00 email. Per CONTEXT D-11 — Sunday 09:00 user-local TZ; content = course progress recap + upcoming events RSVP'd + community top-3.

**File-overlap note vs Plan 49-06:** Plan 49-07 owns the `email-router.ts` EXTEND (single owner per file). Plan 49-06's email template is referenced by router but template file (`community-daily-digest.ts`) is owned by 49-06; router switch arm added by 49-07. Same-wave parallelism preserved because each plan modifies disjoint files; `email-router.ts` is exclusive to 49-07.
Output: 5 files (4 net-new + 1 EXTEND).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/49-m4-search-email-digests/49-CONTEXT.md
@leanshot/.planning/phases/49-m4-search-email-digests/49-RESEARCH.md
@leanshot/.planning/phases/49-m4-search-email-digests/49-PATTERNS.md
@supabase/functions/weekly-digest/index.ts
@supabase/functions/_shared/email-router.ts
@supabase/functions/_shared/digest-email-template.ts

**Memory references (load-bearing):**
- `reference_deno_test_top_level_serve_trap` — guard via `COMMUNITY_WEEKLY_DIGEST_DISABLE_SERVE`.
- `reference_supabase_functions_deploy_import_map_flag` — per-Fn deno.json required.
- `reference_supabase_service_role_key_format_divergence` — checkServiceRoleBearer handles sb_secret_*.
- `feedback_rpc_auth_uid_vs_service_role_mismatch` — pass p_user_id explicitly.
- `feedback_state_counter_table_needs_upsert_on_event` — onConflict on (user_id, kind, sent_date).
- `reference_profiles_email_vs_auth_users_email` — admin.auth.admin.getUserById for email.
- `reference_minisite_monorepo_layout` — supabase/functions/... from git root.

**Reuse target:** FORK structure verbatim from `supabase/functions/community-daily-digest/index.ts` (sibling plan 49-06). Deltas only: 3 different RPCs; isEmpty predicate; template 'community_weekly_digest'; category 'weekly_community_digest'; disable-serve env var name; digest_send_log kind 'weekly'.

email-router.ts EXTEND target: existing union type `EmailTemplate`; add 2 variants. Existing switch in `subjectFor` + `renderTemplate` (or equivalent) gets 2 new case arms importing the new template modules.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: community-weekly-digest Edge Fn + deno.json + email template</name>
  <files>supabase/functions/community-weekly-digest/index.ts, supabase/functions/community-weekly-digest/deno.json, supabase/functions/_shared/email-templates/community-weekly-digest.ts</files>
  <behavior>
    - index.ts exports `__internal` with handleRun + computeWeeklyDigest + isEmpty.
    - handleRun(userId) calls 3 SECDEF RPCs: digest_course_progress_delta_7d, digest_upcoming_events_7d_rsvpd, digest_community_top3_7d.
    - isEmpty: all 3 result arrays empty.
    - Opt-out category 'weekly_community_digest'; unsub URL category same.
    - sendEmail template 'community_weekly_digest'; phi:false; same RFC 8058 headers as daily.
    - Deno.serve guard env var COMMUNITY_WEEKLY_DIGEST_DISABLE_SERVE.
    - email-templates/community-weekly-digest.ts exports `subject(vars)` + `render(vars)`; 3 sections (Course Progress / Upcoming Events / Community Top 3).
  </behavior>
  <action>
    Create `supabase/functions/community-weekly-digest/index.ts` (FORK 49-06 daily verbatim; apply deltas below).

    Imports identical to daily:
    ```typescript
    import {
      checkServiceRoleBearer,
      corsHeaders,
      jsonError,
      jsonResponse,
      makeLazyAdmin,
    } from '../_shared/lifecycle-utils.ts';
    import { sendEmail } from '../_shared/email-router.ts';
    import { mintUnsubscribeToken } from '../_shared/unsubscribe-token.ts';

    const { admin } = makeLazyAdmin();
    ```

    `computeWeeklyDigest(supabase, userId)` — Promise.all of:
    - `supabase.rpc('digest_course_progress_delta_7d', { p_user_id: userId })`
    - `supabase.rpc('digest_upcoming_events_7d_rsvpd', { p_user_id: userId })`
    - `supabase.rpc('digest_community_top3_7d', { p_user_id: userId })`

    `isEmpty(content)`:
    ```typescript
    function isEmpty(c: WeeklyDigestContent): boolean {
      return c.courseProgress.length === 0
        && c.upcomingEvents.length === 0
        && c.communityTop3.length === 0;
    }
    ```

    `upsertDigestLog` — same as daily but `kind: 'weekly'`, onConflict 'user_id,kind,sent_date'.

    `handleRun` — same shape as daily; deltas:
    - opt-out check: `category='weekly_community_digest', channel='email'`.
    - unsub URL category: 'weekly_community_digest'; exp = `now()/1000 + 90*86400`.
    - sendEmail: `template: 'community_weekly_digest', phi: false, headers: { 'List-Unsubscribe': '<' + unsubUrl + '>', 'List-Unsubscribe-Post': 'List-Unsubscribe=One-Click' }`.

    Deno.serve guard (env var name only differs):
    ```typescript
    if (Deno.env.get('COMMUNITY_WEEKLY_DIGEST_DISABLE_SERVE') !== '1') {
      Deno.serve(async (req: Request) => { /* identical to daily */ });
    }
    export const __internal = { handleRun, computeWeeklyDigest, isEmpty };
    ```

    Create `supabase/functions/community-weekly-digest/deno.json` (FORK from mux-webhook/deno.json verbatim).

    Create `supabase/functions/_shared/email-templates/community-weekly-digest.ts` (FORK Phase 38 digest-email-template.ts HTML shape). 3 sections:
    1. **Your Course Progress** — table of course rows: title, completed_this_week, percent_complete (with simple progress bar via inline-styled <div>).
    2. **Upcoming Events** — table of event rows: title, start_at relative time, "View event" link.
    3. **Community Top 3** — top 3 post cards: author, snippet, reactions+comments count.

    HTML-escape all user content. Footer includes visible unsub link from `vars.unsubUrl`.

    Per memory `feedback_negation_grep_defeated_by_comment_string` — do NOT include `phi: true` or `Phase 38 merged digest` as rejected-alt comments.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/functions/community-weekly-digest/index.ts supabase/functions/community-weekly-digest/deno.json supabase/functions/_shared/email-templates/community-weekly-digest.ts 2>&1 | grep -v 'cannot access' | wc -l | xargs test 3 -le && grep -c 'COMMUNITY_WEEKLY_DIGEST_DISABLE_SERVE' supabase/functions/community-weekly-digest/index.ts | xargs test 1 -le && grep -c "digest_course_progress_delta_7d\|digest_upcoming_events_7d_rsvpd\|digest_community_top3_7d" supabase/functions/community-weekly-digest/index.ts | xargs test 3 -le && grep -c "weekly_community_digest" supabase/functions/community-weekly-digest/index.ts | xargs test 2 -le && grep -c 'List-Unsubscribe-Post' supabase/functions/community-weekly-digest/index.ts | xargs test 1 -le && grep -c 'phi: false' supabase/functions/community-weekly-digest/index.ts | xargs test 1 -le && test $(grep -c 'phi: true' supabase/functions/community-weekly-digest/index.ts) -eq 0</automated>
  </verify>
  <done>
    3 files exist; weekly Fn handleRun calls 3 weekly SECDEF RPCs; opt-out category 'weekly_community_digest'; List-Unsubscribe-Post + phi:false; Deno.serve guarded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: email-router.ts EXTEND + community-weekly-digest test scaffold</name>
  <files>supabase/functions/_shared/email-router.ts, supabase/functions/community-weekly-digest/index.test.ts</files>
  <behavior>
    - email-router.ts EmailTemplate union widened with 'community_daily_digest' + 'community_weekly_digest'.
    - switch / case arms for both new templates import from `_shared/email-templates/community-{daily,weekly}-digest.ts` and call subject() + render().
    - Existing arms (Phase 38, Phase 47, etc.) UNTOUCHED.
    - community-weekly-digest/index.test.ts: 4 Deno.test blocks (auth 401, empty-skip, opted-out, sent); sets COMMUNITY_WEEKLY_DIGEST_DISABLE_SERVE=1 BEFORE import.
  </behavior>
  <action>
    Read existing `supabase/functions/_shared/email-router.ts` (around lines 9-15 per RESEARCH cite). Identify the `EmailTemplate` union type + `subjectFor` + `renderTemplate` (or single switch fn).

    EXTEND surgically:
    1. Widen union: add `| 'community_daily_digest' | 'community_weekly_digest'`.
    2. Add `import * as communityDailyDigest from './email-templates/community-daily-digest.ts';`
    3. Add `import * as communityWeeklyDigest from './email-templates/community-weekly-digest.ts';`
    4. Add 2 case arms each in subject + render switch (or equivalent dispatch):
       ```typescript
       case 'community_daily_digest':  return communityDailyDigest.subject(vars);
       case 'community_weekly_digest': return communityWeeklyDigest.subject(vars);
       ```
       (mirror for render).
    5. **DO NOT modify any other case arms or signatures.**

    Create `supabase/functions/community-weekly-digest/index.test.ts` (FORK from `notify-community/index.test.ts`):
    ```typescript
    import { assertEquals } from 'https://deno.land/std@0.224.0/assert/mod.ts';
    Deno.env.set('COMMUNITY_WEEKLY_DIGEST_DISABLE_SERVE', '1');
    const { __internal } = await import('./index.ts');

    Deno.test('handleRun: empty content → skipped:no-content', async () => { /* TODO */ });
    Deno.test('handleRun: opt-out → skipped:opted_out', async () => { /* TODO */ });
    Deno.test('handleRun: non-empty + opted-in → sent', async () => { /* TODO */ });
    Deno.test('Deno.serve guard respects COMMUNITY_WEEKLY_DIGEST_DISABLE_SERVE=1', () => { /* TODO */ });
    ```
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c "community_weekly_digest" supabase/functions/_shared/email-router.ts | xargs test 1 -le && grep -c "community_daily_digest" supabase/functions/_shared/email-router.ts | xargs test 1 -le && ls supabase/functions/community-weekly-digest/index.test.ts && grep -c 'Deno.test' supabase/functions/community-weekly-digest/index.test.ts | xargs test 4 -le && grep -c 'COMMUNITY_WEEKLY_DIGEST_DISABLE_SERVE' supabase/functions/community-weekly-digest/index.test.ts | xargs test 1 -le</automated>
  </verify>
  <done>
    email-router.ts widened with both new template names; case arms added; weekly test scaffold has 4 Deno.test blocks.
  </done>
</task>

</tasks>

<acceptance_criteria>
- `ls supabase/functions/community-weekly-digest/index.ts` — exists.
- `ls supabase/functions/community-weekly-digest/deno.json` — exists.
- `ls supabase/functions/community-weekly-digest/index.test.ts` — exists.
- `ls supabase/functions/_shared/email-templates/community-weekly-digest.ts` — exists.
- `grep -c 'COMMUNITY_WEEKLY_DIGEST_DISABLE_SERVE' supabase/functions/community-weekly-digest/index.ts` — at least 1.
- `grep -c 'phi: false' supabase/functions/community-weekly-digest/index.ts` — at least 1.
- `grep -c 'phi: true' supabase/functions/community-weekly-digest/index.ts` — exactly 0.
- `grep -c 'digest_course_progress_delta_7d' supabase/functions/community-weekly-digest/index.ts` — at least 1.
- `grep -c 'digest_upcoming_events_7d_rsvpd' supabase/functions/community-weekly-digest/index.ts` — at least 1.
- `grep -c 'digest_community_top3_7d' supabase/functions/community-weekly-digest/index.ts` — at least 1.
- `grep -c 'List-Unsubscribe-Post' supabase/functions/community-weekly-digest/index.ts` — at least 1.
- `grep -c "weekly_community_digest" supabase/functions/community-weekly-digest/index.ts` — at least 2 (category + token).
- `grep -c "community_weekly_digest" supabase/functions/_shared/email-router.ts` — at least 1.
- `grep -c "community_daily_digest" supabase/functions/_shared/email-router.ts` — at least 1.
- `grep -c 'Deno.test' supabase/functions/community-weekly-digest/index.test.ts` — at least 4.
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron net.http_post → Edge Fn | service_role bearer; checkServiceRoleBearer |
| Edge Fn → Resend | phi:false; community + courses + events content non-PHI |
| Email template render → output HTML | HTML-escape user content; ts_headline `<b>` allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-18 | I (Info disclosure) | course progress leaks across enrolled-but-not-this-user accounts | mitigate | digest_course_progress_delta_7d SECDEF takes p_user_id; per-user filter |
| T-49-19 | T (Tampering) | XSS via unescaped post body in weekly digest HTML | mitigate | HTML-escape helper applied to all user-content in render(); ts_headline `<b>` already safe |
| T-49-20 | R (Repudiation) | Phase 38 weekly conflicts with Phase 49 weekly | accept | Different jobnames + different templates + different opt-out categories per D-09 |
</threat_model>

<verification>
- 5 files exist; grep gates pass.
- Cross-Fn Deno sweep at Wave 3 close-out exercises both daily + weekly together.
- Wave 3 deploys via `supabase functions deploy community-weekly-digest`.
- email-router.ts widened so BOTH daily AND weekly route correctly (single source of truth).
</verification>

<success_criteria>
- 5 files committed (4 new + 1 EXTEND).
- All `<acceptance_criteria>` greps pass.
- email-router.ts dispatch covers both new templates without touching other arms.
- Phase 38 weekly-digest untouched (per D-09 coexist).
</success_criteria>

<output>
After completion, create `.planning/phases/49-m4-search-email-digests/49-07-SUMMARY.md` per template.
</output>
