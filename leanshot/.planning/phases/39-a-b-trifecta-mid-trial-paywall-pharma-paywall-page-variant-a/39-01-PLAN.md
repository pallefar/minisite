---
phase: 39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-01-SUMMARY.md
  - supabase/migrations/20270714000001_p39_user_experiments.sql
  - supabase/migrations/20270714000002_p39_variant_config.sql
  - supabase/migrations/20270714000003_p39_utm_variant_map.sql
  - supabase/migrations/20270714000004_p39_pharma_content.sql
  - supabase/migrations/20270714000005_p39_pharma_content_versions.sql
  - supabase/migrations/20270714000006_p39_page_variants.sql
  - supabase/migrations/20270714000007_p39_experiment_results_matview.sql
  - supabase/migrations/20270714000008_p39_cohort_seed_5_default.sql
  - supabase/migrations/20270714000009_p39_utm_variant_map_seed.sql
  - supabase/migrations/20270714000013_p39_subscriptions_refunded_at.sql
  - supabase/migrations/20270714000014_p39_resolve_cohort_for_user_rpc.sql
  - supabase/tests/p39_rls_user_experiments.sql
  - supabase/tests/p39_rls_pharma_content_versions.sql
  - supabase/tests/p39_seed_counts.sql
autonomous: true
requirements:
  - PAYWALL-03
  - PAYWALL-04
  - PAYWALL-05
  - PAYWALL-07
  - PAGEAB-01
  - PAGEAB-03
  - PAGEAB-07
  - PHARMA-04
  - PHARMA-07
must_haves:
  truths:
    - "user_experiments table exists with (user_id, surface, variant_id, cohort_id, utm_variant_id) and admin SELECT RLS"
    - "variant_config table exists with composite_score column + cohort_id FK + archived_at + warned_at"
    - "utm_variant_map table exists with exactly 4 seed rows (lean, transformation, clinical, default)"
    - "cohort_definitions has exactly 5 Phase 39 seeded rows (free-user, past-due, trial-day-3, trial-day-7, post-activation)"
    - "page_variants table exists with canonical_page_id FK, created_at, warned_at, archived_at, traffic_to_control"
    - "experiment_results matview exists with composite_score = paid_rate × retention_30d_rate column derived"
    - "pharma_content table exists with safety_category text column (non-null = always free)"
    - "pharma_content_versions table is append-only via RLS (no UPDATE / DELETE policy)"
    - "subscriptions.refunded_at column added (was missing on main; required for refund-rate kill cron)"
    - "resolve_cohort_for_user(uid uuid) SECDEF RPC exists and is callable from service-role with explicit uid param"
    - "Migration timestamps strictly > 20270709000008 (P40 tail); all match \\d{14}_<name>.sql"
  artifacts:
    - path: "supabase/migrations/20270714000001_p39_user_experiments.sql"
      provides: "user_experiments table + RLS"
      contains: "create table if not exists public.user_experiments"
    - path: "supabase/migrations/20270714000002_p39_variant_config.sql"
      provides: "variant_config table + RLS"
      contains: "composite_score"
    - path: "supabase/migrations/20270714000003_p39_utm_variant_map.sql"
      provides: "utm_variant_map table + RLS"
      contains: "create table if not exists public.utm_variant_map"
    - path: "supabase/migrations/20270714000004_p39_pharma_content.sql"
      provides: "pharma_content table with safety_category column"
      contains: "safety_category text"
    - path: "supabase/migrations/20270714000005_p39_pharma_content_versions.sql"
      provides: "pharma_content_versions append-only table"
      contains: "clinical_signoff_by"
    - path: "supabase/migrations/20270714000006_p39_page_variants.sql"
      provides: "page_variants table with 42-day lifecycle columns"
      contains: "canonical_page_id"
    - path: "supabase/migrations/20270714000007_p39_experiment_results_matview.sql"
      provides: "experiment_results matview with composite_score"
      contains: "create materialized view"
    - path: "supabase/migrations/20270714000008_p39_cohort_seed_5_default.sql"
      provides: "5 default cohort seed rows"
      contains: "free-user"
    - path: "supabase/migrations/20270714000009_p39_utm_variant_map_seed.sql"
      provides: "4 UTM map seed rows"
      contains: "transformation"
    - path: "supabase/migrations/20270714000013_p39_subscriptions_refunded_at.sql"
      provides: "subscriptions.refunded_at column add"
      contains: "alter table public.subscriptions add column if not exists refunded_at"
    - path: "supabase/migrations/20270714000014_p39_resolve_cohort_for_user_rpc.sql"
      provides: "resolve_cohort_for_user(uuid) SECDEF helper"
      contains: "create or replace function public.resolve_cohort_for_user"
  key_links:
    - from: "variant_config.cohort_id"
      to: "cohort_definitions.id"
      via: "FK"
      pattern: "references public.cohort_definitions"
    - from: "page_variants.canonical_page_id"
      to: "landing_pages.id"
      via: "FK"
      pattern: "references public.landing_pages"
    - from: "pharma_content_versions.content_id"
      to: "pharma_content.id"
      via: "FK + append-only RLS"
      pattern: "references public.pharma_content"
---

<objective>
Wave 1 foundation migrations — ship ALL Phase 39 schema (10 migrations) + RLS + SECDEF cohort helper + the subscriptions.refunded_at prerequisite (RESEARCH OQ-1 RESOLVED: column does NOT exist on main, this plan owns the add).

Purpose: All downstream plans (Edge Fns, consumer UI, admin UI) depend on this schema. Wave 1 is gated by these migrations existing in the worktree; phase close-out (Plan 39-10) executes the supabase db push.

Output:
- 8 net-new tables/matview (user_experiments, variant_config, utm_variant_map, page_variants, pharma_content, pharma_content_versions, experiment_results matview) plus column-add on subscriptions
- 5 seed cohort rows (D-08) + 4 UTM map rows (D-09)
- 1 SECDEF RPC (resolve_cohort_for_user) supporting the variant-resolver Edge Fn in Wave 2
- pgTAP RLS proofs for cross-tenant impersonation + append-only pharma_content_versions
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CONTEXT.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-RESEARCH.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-PATTERNS.md

<read_first>
- supabase/migrations/20270602000010_cohort_definitions.sql — EXACT table+RLS template (Pattern: read once, mirror shape verbatim)
- supabase/migrations/20270602000011_cohort_membership_matview.sql — matview shape template
- supabase/migrations/20270602000012_cohort_rpcs.sql — SECDEF RPC shape; check for any existing resolve_cohort_for_user signature (per RESEARCH OQ-3: introspect with `supabase db query --linked "\\df+ resolve_cohort_for_user"`; if absent, this plan creates it; if present, this plan is a no-op for that file)
- supabase/migrations/20270706000004_p34_activation_events_alter.sql — activation source consumed by PAYWALL-01 (no edits, anchor only)
- supabase/migrations/20260601000019_stripe_subscriptions.sql — subscriptions table shape; verify refunded_at absence before writing the alter migration
</read_first>

<interfaces>
Existing table contracts this plan references via FK:
- public.cohort_definitions(id uuid PK, key text unique, label text, filter_rule jsonb, leaderboard_enabled boolean, ...) — extended via seed insert; do NOT alter columns
- public.landing_pages(id uuid PK, slug text unique, ...) — referenced via page_variants.canonical_page_id FK
- public.subscriptions(id, user_id, status, ...) — extended with refunded_at timestamptz
- auth.users(id) — referenced via created_by columns

New table contracts this plan ships (downstream consumers must match these column names exactly):

user_experiments:
  id uuid PK, user_id uuid -> auth.users, surface text check IN ('paywall','page','pharma'),
  variant_id uuid -> variant_config.id (nullable for control), cohort_id uuid -> cohort_definitions.id,
  utm_variant_id uuid -> utm_variant_map.id, created_at, updated_at
  UNIQUE (user_id, surface)

variant_config:
  id uuid PK, surface text check IN ('paywall','page','pharma'),
  cohort_id uuid -> cohort_definitions.id (nullable),
  config jsonb not null default '{}'::jsonb,
  composite_score numeric, posterior numeric,
  warned_at timestamptz, archived_at timestamptz,
  created_by uuid -> auth.users, created_at, updated_at

utm_variant_map:
  id uuid PK, utm_source text not null unique,
  variant_id uuid -> variant_config.id,
  created_by, created_at, updated_at

pharma_content:
  id uuid PK, slug text unique, summary text, full_content text,
  safety_category text, -- D-05: non-null = always free
  created_by, created_at, updated_at

pharma_content_versions:
  id uuid PK, content_id uuid -> pharma_content.id,
  diff jsonb, author_id uuid -> auth.users,
  clinical_signoff_by uuid -> auth.users (nullable), clinical_signoff_at timestamptz,
  created_at timestamptz default now()
  -- append-only via RLS: SELECT for admins; NO INSERT/UPDATE/DELETE policies
  -- (writes flow only through SECDEF RPC, future plan)

page_variants:
  id uuid PK, canonical_page_id uuid -> landing_pages.id,
  variant_blocks jsonb, traffic_split numeric default 0.5,
  warned_at timestamptz, archived_at timestamptz,
  traffic_to_control boolean default false,
  created_by, created_at, updated_at

experiment_results (matview):
  variant_id, surface, paid_rate, retention_30d_rate,
  composite_score = paid_rate * retention_30d_rate,
  refund_rate_7d, posterior, sample_size

resolve_cohort_for_user signature:
  create or replace function public.resolve_cohort_for_user(uid uuid) returns uuid
  language sql security definer set search_path = public, extensions
  -- Body: SELECT id FROM cohort_definitions WHERE filter_rule matches user state.
  -- Per [[feedback_rpc_auth_uid_vs_service_role_mismatch]]: takes EXPLICIT uid param,
  -- does NOT reference auth.uid() — safe to call from service-role Edge Fn.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 8 core schema migrations + 2 seed migrations</name>
  <files>supabase/migrations/20270714000001_p39_user_experiments.sql, supabase/migrations/20270714000002_p39_variant_config.sql, supabase/migrations/20270714000003_p39_utm_variant_map.sql, supabase/migrations/20270714000004_p39_pharma_content.sql, supabase/migrations/20270714000005_p39_pharma_content_versions.sql, supabase/migrations/20270714000006_p39_page_variants.sql, supabase/migrations/20270714000007_p39_experiment_results_matview.sql, supabase/migrations/20270714000008_p39_cohort_seed_5_default.sql, supabase/migrations/20270714000009_p39_utm_variant_map_seed.sql</files>
  <action>
Pre-flight: Run `ls supabase/migrations/20270714*.sql` to confirm no collisions; bump suffix per `[[reference_migration_timestamp_collision_precheck]]` if any sibling has claimed slots. Then Read the cohort_definitions migration once to internalize the table+RLS shape (Pattern S2 from PATTERNS).

Author the 7 core schema migrations + matview migration matching the column contracts declared in `<interfaces>` above. For EACH migration:
- Strict 14-digit underscore filename per `[[reference_supabase_migration_filename_regex]]`
- Header comment block citing CONTEXT D-NN that justifies the table
- `create table if not exists public.<name> (...)` followed by `alter table ... enable row level security`
- SELECT policy for admins only via `public.is_admin_at_least('admin')`; NO INSERT/UPDATE/DELETE policies (writes flow through SECDEF RPCs in Wave 2)
- Indexes per `<interfaces>` declared lookup columns (user_id+surface, cohort_id, variant_id, canonical_page_id, slug, etc.)
- `comment on table` describing purpose

For migration 7 (experiment_results matview): mirror `20270602000011_cohort_membership_matview.sql` shape; the composite_score column is computed (paid_rate * retention_30d_rate) inside the matview SELECT. Sample-size, refund_rate_7d, posterior columns nullable (populated by Wave 2 RPC + cron).

For migration 8 (cohort seed): INSERT exactly 5 rows into `cohort_definitions` with keys `free-user`, `past-due-3d`, `trial-day-3`, `trial-day-7`, `post-activation`; each carries a `filter_rule jsonb` describing the user-state predicate per D-08. Use `ON CONFLICT (key) DO NOTHING` for idempotency.

For migration 9 (utm seed): INSERT exactly 4 rows into `utm_variant_map` with utm_source values `lean`, `transformation`, `clinical`, `default` per D-09. Each variant_id points to a placeholder variant_config row that this same migration creates (use a CTE chain: first INSERT 4 variant_config rows for surface='paywall', then INSERT utm_variant_map rows referencing them). `ON CONFLICT (utm_source) DO NOTHING`.

Decision references: D-01, D-02, D-03, D-05, D-07, D-08, D-09, D-11, D-13.

Anti-patterns to avoid: no BEGIN/COMMIT around DDL (Postgres auto-tx); CHECK constraints on enum-like text columns MUST list all valid values per `[[feedback_planner_missed_status_enum_widening]]`; SECDEF function `search_path` must include `extensions`.
  </action>
  <verify>
    <automated>ls supabase/migrations/20270714000001_p39_*.sql supabase/migrations/20270714000002_p39_*.sql supabase/migrations/20270714000003_p39_*.sql supabase/migrations/20270714000004_p39_*.sql supabase/migrations/20270714000005_p39_*.sql supabase/migrations/20270714000006_p39_*.sql supabase/migrations/20270714000007_p39_*.sql supabase/migrations/20270714000008_p39_*.sql supabase/migrations/20270714000009_p39_*.sql | wc -l | grep -q '^[ ]*9$' && grep -l 'safety_category' supabase/migrations/20270714000004_p39_pharma_content.sql && grep -l 'composite_score' supabase/migrations/20270714000007_p39_experiment_results_matview.sql</automated>
  </verify>
  <done>9 files exist with correct filename pattern; pharma_content has safety_category column; matview has composite_score column.</done>
</task>

<task type="auto">
  <name>Task 2: Author subscriptions.refunded_at + resolve_cohort_for_user RPC + pgTAP proofs</name>
  <files>supabase/migrations/20270714000013_p39_subscriptions_refunded_at.sql, supabase/migrations/20270714000014_p39_resolve_cohort_for_user_rpc.sql, supabase/tests/p39_rls_user_experiments.sql, supabase/tests/p39_rls_pharma_content_versions.sql, supabase/tests/p39_seed_counts.sql</files>
  <action>
Pre-flight: `grep -rE "refunded_at" supabase/migrations/` to confirm column absence (RESEARCH OQ-1 RESOLVED: missing on main). Then `supabase db query --linked "SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='subscriptions';"` to introspect actual column list before writing the alter migration.

Migration 13 (subscriptions.refunded_at): `alter table public.subscriptions add column if not exists refunded_at timestamptz`. Index on `(refunded_at)` partial WHERE `refunded_at IS NOT NULL` (immutable expression per `[[reference_supabase_migration_gotchas]]`). Header comment cites D-02 + RESEARCH OQ-1 resolution. The refund-rate kill cron (Plan 39-03) reads this column.

Migration 14 (resolve_cohort_for_user RPC): Pre-flight introspect `\\df+ resolve_cohort*` to detect prior versions; if present, the migration is a `create or replace` overwrite preserving signature (uid uuid) returns uuid. The function body SELECTs the first matching cohort_definitions.id whose filter_rule jsonb matches the user's state (joins subscriptions, activation_events, trial_started_at, etc.). `security definer`, `set search_path = public, extensions`. Per `[[feedback_rpc_auth_uid_vs_service_role_mismatch]]`: takes EXPLICIT `uid` param; does NOT reference `auth.uid()`. Grant EXECUTE to `service_role` and `authenticated`.

pgTAP RLS proofs:
- p39_rls_user_experiments.sql: PROVE that a non-admin user CANNOT SELECT another user's user_experiments row (cross-tenant impersonation). Use the project RLS-fixture pattern from `[[reference_rls_fixture_gotrueclient_flake]]`. Mark admin SELECT works for admin role.
- p39_rls_pharma_content_versions.sql: PROVE the table is append-only — attempt UPDATE and DELETE as admin and non-admin; both must fail. Only SELECT permitted.
- p39_seed_counts.sql: assert exactly 5 cohort_definitions rows with phase39 keys; exactly 4 utm_variant_map seed rows.

Decision references: D-02 (refund kill), D-07 (cohort + UTM resolution via RPC), D-08 + D-09 (seed counts).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20270714000013_p39_subscriptions_refunded_at.sql && test -f supabase/migrations/20270714000014_p39_resolve_cohort_for_user_rpc.sql && test -f supabase/tests/p39_rls_user_experiments.sql && test -f supabase/tests/p39_rls_pharma_content_versions.sql && grep -q 'refunded_at' supabase/migrations/20270714000013_p39_subscriptions_refunded_at.sql && grep -q 'resolve_cohort_for_user' supabase/migrations/20270714000014_p39_resolve_cohort_for_user_rpc.sql && ! grep -q 'auth.uid()' supabase/migrations/20270714000014_p39_resolve_cohort_for_user_rpc.sql</automated>
  </verify>
  <done>All 5 files exist. refunded_at column add migration present. RPC migration uses explicit uid param (NO auth.uid() reference). pgTAP proofs assert RLS append-only + cross-tenant denial + seed-row counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service-role Edge Fn -> Postgres | variant-resolver (Wave 2) calls resolve_cohort_for_user RPC with explicit uid; no auth.uid() trust |
| Admin browser -> Postgres SELECT RLS | Admin reads variant_config, experiment_results matview, pharma_content_versions via is_admin_at_least('admin') gate |
| Cross-tenant attacker -> user_experiments | Non-admin user MUST NOT SELECT another user's variant assignment row |
| Append-only RLS surface | pharma_content_versions UPDATE/DELETE forbidden for all roles; writes flow through SECDEF RPC (Wave 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | Information disclosure | user_experiments cross-tenant SELECT | mitigate | SELECT policy WHERE user_id = auth.uid() OR is_admin_at_least('admin'); pgTAP proof p39_rls_user_experiments.sql asserts non-admin cross-tenant denial |
| T-39-01-02 | Tampering | pharma_content_versions audit-log retroactive edit | mitigate | RLS has SELECT-only policies; NO INSERT/UPDATE/DELETE policies; writes flow only through future SECDEF RPC; pgTAP proof p39_rls_pharma_content_versions.sql asserts UPDATE+DELETE fail |
| T-39-01-03 | Elevation of privilege | resolve_cohort_for_user RPC trusts caller identity | mitigate | RPC takes EXPLICIT uid param; does NOT reference auth.uid(); safe from `[[feedback_rpc_auth_uid_vs_service_role_mismatch]]`; grep gate in <verify> confirms no auth.uid() in migration body |
| T-39-01-04 | Repudiation | variant assignment cannot be traced to source decision | mitigate | user_experiments.created_at + cohort_id + utm_variant_id columns audit the deciding inputs at assignment time |
| T-39-01-05 | Denial of service | back-dated migration blocks db push | accept | Migration timestamps 20270714000001..14 strictly > P40 tail 20270709000008; collision pre-check in Task 1 |
| T-39-01-06 | Tampering | safety_category column ignored by future Paywall component | mitigate | safety_category text column on pharma_content; phaCheck() (Plan 39-02) + ESLint rule + CI grep gate (Plan 39-02) form 3-layer enforcement |
</threat_model>

<verification>
- 11 migration files exist, all matching strict `\d{14}_<name>.sql` regex
- pharma_content has `safety_category text` column
- pharma_content_versions has NO INSERT/UPDATE/DELETE policies (only SELECT)
- resolve_cohort_for_user RPC body does NOT contain string `auth.uid()` (use grep -v `^#` to strip comments)
- subscriptions.refunded_at column add migration exists
- cohort_definitions seed: exactly 5 Phase 39 rows
- utm_variant_map seed: exactly 4 rows
- pgTAP proof files exist and reference the project RLS-fixture pattern
</verification>

<success_criteria>
- All 11 migrations in `supabase/migrations/` matching 20270714000001..14 naming
- All 3 pgTAP proof files in `supabase/tests/`
- Decision IDs D-01, D-02, D-05, D-07, D-08, D-09, D-11, D-13 each referenced in at least one migration header comment
- No `auth.uid()` reference in resolve_cohort_for_user RPC body
- No BEGIN/COMMIT wrapping DDL
- File contract matches `<interfaces>` block exactly so downstream Wave 2/3/4 plans can scaffold against the same column names
</success_criteria>

<output>
Create `.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-01-SUMMARY.md` per template, listing all 11 migrations + 3 pgTAP proofs + the introspection findings (refunded_at absence confirmed; resolve_cohort_for_user prior signature if any).
</output>
