---
phase: 39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a
plan: 07
type: execute
wave: 5
depends_on: [01, 02, 03, 06]
files_modified:
  - leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-07-SUMMARY.md
  - leanshot/src/components/admin/growth/PaywallExperimentTab.tsx
  - leanshot/src/components/admin/growth/PaywallExperimentTab.test.tsx
  - leanshot/src/components/admin/growth/PageExperimentTab.tsx
  - leanshot/src/components/admin/growth/PageExperimentTab.test.tsx
  - leanshot/src/components/admin/growth/ShipWinnerConfirmModal.tsx
  - leanshot/src/components/admin/growth/ShipWinnerConfirmModal.test.tsx
  - leanshot/src/components/admin/growth/BayesianBadge.tsx
  - leanshot/src/components/admin/growth/BayesianBadge.test.tsx
  - leanshot/src/components/admin/growth/TrafficSplitSlider.tsx
  - leanshot/src/components/admin/growth/TrafficSplitSlider.test.tsx
  - leanshot/src/components/admin/growth/ExperimentDashboardPage.tsx
  - supabase/migrations/20270714000016_p39_get_experiment_results_rpc.sql
  - supabase/migrations/20270714000017_p39_admin_ship_below_95_rpc.sql
  - supabase/tests/p39_ship_below_95_audit.sql
autonomous: true
requirements:
  - PAYWALL-03
  - PAYWALL-04
  - PAGEAB-03
  - PAGEAB-05
  - PAGEAB-07
must_haves:
  truths:
    - "PaywallExperimentTab renders a table of paywall variant rows (variant_id, cohort_label, sample_size, composite_score, posterior, refund_rate_7d, archived_at) with Ship-Winner action button per row"
    - "PageExperimentTab renders a table of page variant rows with the 35-day warn banner + 42-day archived banner per D-11"
    - "Ship-Winner button verbatim reuses the contract from OnboardingABPanel.tsx: same data-action='ship-winner', same supabase.functions.invoke('ship-winner-flag', { body: { flag_id, variant } }) shape"
    - "BayesianBadge renders tri-state: <80% gray neutral, 80-95% warning, ≥95% success per D-12"
    - "ShipWinnerConfirmModal opens when Ship-Winner clicked with posterior < 0.95; enables confirm ONLY when input string equals 'ship-below-95' AND reason field is non-empty (D-12)"
    - "ShipWinnerConfirmModal confirm action invokes admin_ship_below_95 SECDEF RPC that writes admin_audit_log row + calls ship-winner-flag"
    - "TrafficSplitSlider renders Input type=range styled with DSv2 tokens + preset Pills at 10/20/50/80/90%"
    - "ExperimentDashboardPage updated to render PaywallExperimentTab when activeTab='paywall' and PageExperimentTab when activeTab='page'"
    - "get_experiment_results SECDEF RPC returns ExperimentRow[] filtered by surface param; respects is_admin_at_least('admin'); computes composite_score from experiment_results matview"
    - "admin_ship_below_95 SECDEF RPC enforces is_admin_at_least('superadmin') gate + writes admin_audit_log + invokes ship-winner-flag via service-role path"
    - "pgTAP proof asserts admin_audit_log row written when below-95 confirmed AND admin_ship_below_95 rejected for non-superadmin role"
  artifacts:
    - path: "leanshot/src/components/admin/growth/PaywallExperimentTab.tsx"
      provides: "Paywall sub-tab content for ExperimentDashboardPage"
      contains: "ship-winner-flag"
    - path: "leanshot/src/components/admin/growth/PageExperimentTab.tsx"
      provides: "Page-Builder sub-tab content with 35/42-day banners"
      contains: "warned_at"
    - path: "leanshot/src/components/admin/growth/ShipWinnerConfirmModal.tsx"
      provides: "Typed-confirmation modal for below-95% ship (D-12)"
      contains: "ship-below-95"
    - path: "leanshot/src/components/admin/growth/BayesianBadge.tsx"
      provides: "Tri-state Bayesian posterior badge (PAGEAB-07)"
      contains: "posterior"
    - path: "leanshot/src/components/admin/growth/TrafficSplitSlider.tsx"
      provides: "Traffic split slider + preset Pills"
      contains: "traffic_split"
    - path: "supabase/migrations/20270714000016_p39_get_experiment_results_rpc.sql"
      provides: "Admin RPC returning ExperimentRow[]"
      contains: "get_experiment_results"
    - path: "supabase/migrations/20270714000017_p39_admin_ship_below_95_rpc.sql"
      provides: "Audit-logged ship-below-95 SECDEF RPC"
      contains: "admin_ship_below_95"
  key_links:
    - from: "leanshot/src/components/admin/growth/PaywallExperimentTab.tsx"
      to: "supabase/functions/ship-winner-flag/index.ts"
      via: "supabase.functions.invoke('ship-winner-flag') — VERBATIM reuse"
      pattern: "ship-winner-flag"
    - from: "leanshot/src/components/admin/growth/ShipWinnerConfirmModal.tsx"
      to: "public.admin_ship_below_95 RPC"
      via: "supabase.rpc on confirm"
      pattern: "admin_ship_below_95"
    - from: "leanshot/src/components/admin/growth/BayesianBadge.tsx"
      to: "ExperimentRow.posterior"
      via: "prop posterior 0..1 -> Badge tone selection"
      pattern: "posterior"
---

<objective>
Wave 5 admin slice A — fill in the Paywall + Page-Builder sub-tabs of the ExperimentDashboardPage shell (Plan 39-06), plus the Ship-Winner typed-confirmation modal (D-12), the Bayesian badge (PAGEAB-07), and the supporting SECDEF RPCs for admin read + audit-logged ship-below-95.

Purpose: This plan turns the Plan 39-06 shell into a functional dashboard for 2 of the 3 surfaces. Verbatim Ship-Winner reuse via the Phase 34/35 Edge Fn (UI-SPEC Hard Constraint 8). Plan 39-08 fills in the third (Pharma) tab.

Output:
- 2 sub-tab content components + 3 supporting widgets (modal, badge, slider)
- 1 edit to ExperimentDashboardPage wiring sub-tabs
- 2 SECDEF RPCs (admin read + audit-logged ship-below-95)
- 1 pgTAP proof for the audit-trail invariant
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-CONTEXT.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-UI-SPEC.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-PATTERNS.md
@leanshot/.planning/phases/39-a-b-trifecta-mid-trial-paywall-pharma-paywall-page-variant-a/39-06-SUMMARY.md

<read_first>
- leanshot/src/components/admin/onboarding-builder/OnboardingABPanel.tsx — verbatim source for Ship-Winner button contract (lines 98-122 area per PATTERNS): data-action='ship-winner', supabase.functions.invoke('ship-winner-flag', { body: { flag_id, variant } }), error code handling for forbidden_not_superadmin / vendor_unconfigured. DO NOT fork — copy invoke shape.
- supabase/functions/ship-winner-flag/index.ts — verify the body shape variant + flag_id is expected; this plan's button MUST match.
- leanshot/src/components/admin/growth/CACDashboardPage.tsx — table + Sheet drill-in pattern source.
- leanshot/src/components/admin/growth/experiment-types.ts (from Plan 39-06) — types consumed by this plan.
- leanshot/src/components/ui/Modal.tsx + Input.tsx + Badge.tsx + Pill.tsx — primitive contracts.
- supabase/migrations/20270706000006_p34_record_activation_rpc.sql — SECDEF RPC template for admin RPCs.
- supabase/migrations/20270602000012_cohort_rpcs.sql — alternative SECDEF RPC template.
- leanshot/src/components/admin/growth/ExperimentDashboardPage.tsx (from Plan 39-06) — read to find the data-testid='experiment-tab-content' placeholder slot this plan replaces.
</read_first>

<interfaces>
PaywallExperimentTab props:
  interface PaywallExperimentTabProps {
    rows: ExperimentRow[];
    onShip: (variantId: string) => void;
    busyKey: string | null;
  }

PageExperimentTab props:
  interface PageExperimentTabProps {
    rows: ExperimentRow[];
    onShip: (variantId: string) => void;
    busyKey: string | null;
  }
  // Renders 35-day warn banner per row when row.warned_at != null AND row.archived_at == null
  // Renders 42-day archived banner per row when row.archived_at != null

BayesianBadge props:
  interface BayesianBadgeProps { posterior: number /* 0..1 */ }
  // < 0.80 -> Badge tone='neutral' text 'Insufficient data (N%)'
  // 0.80..0.95 -> Badge tone='warning' text 'Trending (N%)'
  // ≥ 0.95 -> Badge tone='success' text 'Significant (N%)'

ShipWinnerConfirmModal props:
  interface ShipWinnerConfirmModalProps {
    open: boolean;
    variantId: string;
    variantName: string;
    posterior: number;
    onConfirm: (reason: string) => Promise<void>;
    onClose: () => void;
  }
  // Confirm enabled ONLY when typedValue === 'ship-below-95' AND reason.trim().length > 0
  // On confirm: invokes supabase.rpc('admin_ship_below_95', { variant_id, reason, posterior })

TrafficSplitSlider props:
  interface TrafficSplitSliderProps {
    value: number /* 0..100 */;
    onChange: (next: number) => void;
  }
  // Preset Pills: 10, 20, 50, 80, 90
  // Custom value via Input type=range

Ship-Winner button verbatim contract (from OnboardingABPanel.tsx):
  - data-action='ship-winner' attribute on the button
  - On click async: supabase.functions.invoke('ship-winner-flag', { body: { flag_id: String(flagId), variant } })
  - Error handling for codes: 'forbidden_not_superadmin', 'vendor_unconfigured', other
  - Toast on success: `Shipped variant ${variant} to 100%`
  - busyKey state per (flag_id, variant) prevents double-click

get_experiment_results RPC signature:
  create or replace function public.get_experiment_results(p_surface text) returns setof public.experiment_row_t
  language plpgsql security definer set search_path = public, extensions
  -- Body: gate on is_admin_at_least('admin'); SELECT from experiment_results matview WHERE surface = p_surface
  -- Returns: variant_id, variant_name, cohort_id, cohort_label, sample_size, paid_rate, retention_30d_rate,
  --          composite_score, posterior, refund_rate_7d, refund_rate_baseline_30d, warned_at, archived_at, created_at

admin_ship_below_95 RPC signature:
  create or replace function public.admin_ship_below_95(
    p_variant_id uuid,
    p_reason text,
    p_posterior numeric
  ) returns void
  language plpgsql security definer set search_path = public, extensions
  -- Gate: is_admin_at_least('superadmin') — REJECT otherwise
  -- Validate p_reason length > 0
  -- INSERT admin_audit_log row with action='ship_below_95', actor_id=auth.uid(), context=jsonb_build_object('variant_id', p_variant_id, 'posterior', p_posterior, 'reason', p_reason)
  -- The Edge Fn ship-winner-flag invocation happens from the CALLING client (browser invokes RPC for audit, then invokes Edge Fn for the actual flag flip) — splits responsibilities so service-role context stays on the cron path, not the user-action path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BayesianBadge + ShipWinnerConfirmModal + TrafficSplitSlider + SECDEF RPCs</name>
  <files>leanshot/src/components/admin/growth/BayesianBadge.tsx, leanshot/src/components/admin/growth/BayesianBadge.test.tsx, leanshot/src/components/admin/growth/ShipWinnerConfirmModal.tsx, leanshot/src/components/admin/growth/ShipWinnerConfirmModal.test.tsx, leanshot/src/components/admin/growth/TrafficSplitSlider.tsx, leanshot/src/components/admin/growth/TrafficSplitSlider.test.tsx, supabase/migrations/20270714000016_p39_get_experiment_results_rpc.sql, supabase/migrations/20270714000017_p39_admin_ship_below_95_rpc.sql, supabase/tests/p39_ship_below_95_audit.sql</files>
  <behavior>
    - RED BayesianBadge: tests across boundary posteriors (0.0, 0.79, 0.80, 0.94, 0.95, 1.0) asserting tone + text per `<interfaces>`.
    - GREEN BayesianBadge: thin Badge wrapper; rounds posterior to integer percent.
    - RED ShipWinnerConfirmModal: tests (a) opens when open=true; (b) confirm button disabled initially; (c) typing 'ship-below-95' in input does NOT enable confirm if reason empty; (d) filling reason does NOT enable confirm if input wrong; (e) BOTH valid -> confirm enabled; (f) clicking confirm calls onConfirm with reason string; (g) wrong typed value 'ship below 95' (spaces) keeps confirm disabled — exact-match contract.
    - GREEN ShipWinnerConfirmModal: wrap Modal primitive; input has aria-describedby pointing to explanatory body; copy from UI-SPEC table.
    - RED TrafficSplitSlider: tests (a) presets render 5 Pills with values 10/20/50/80/90; (b) clicking preset calls onChange; (c) input type=range changes onChange; (d) value prop is the displayed value.
    - GREEN TrafficSplitSlider: thin wrapper.
    - SECDEF RPCs: write get_experiment_results + admin_ship_below_95 per `<interfaces>` signatures. The RPCs MUST be standalone migrations (no inline DDL inside other plan files).
    - pgTAP proof p39_ship_below_95_audit.sql: SETUP admin user (NOT superadmin) and superadmin user. Assert (a) admin_ship_below_95 raises insufficient_privilege for non-superadmin; (b) superadmin call succeeds and writes exactly one admin_audit_log row with action='ship_below_95' + p_reason + p_posterior fields populated.
  </behavior>
  <action>
Pre-read Modal.tsx + Badge.tsx + Pill.tsx for primitive contracts.

For RPC migrations: header comments cite D-12 (typed-confirm + audit) + PAGEAB-07 (posterior). The admin_ship_below_95 RPC body MUST NOT invoke the Edge Fn server-side (split responsibilities — browser calls RPC for audit, then invokes Edge Fn separately for the flag flip). Document this split inline.

Per `[[reference_supabase_migration_filename_regex]]`: strict 14-digit underscore filenames. Pre-flight: `ls supabase/migrations/20270714*.sql` for collisions; Plan 39-03 ended at 20270714000015, so 16+17 are the next free slots.

UI-SPEC Hard Constraints honored: BayesianBadge uses semantic tokens neutral/warning/success — NEVER teal accent for the badge bg (per accent allowlist). Slider preset Pills use DSv2 Pill primitive — NOT new component. Modal uses primitive (inherits role='dialog' + aria-modal='true').

Decision references: D-12 (typed-confirm + audit), PAGEAB-07 (Bayesian posterior), PAGEAB-05 (Ship-Winner promotes), V13-3 typed-confirmation pattern mirror.

Per Pattern M: post-Edit, git status --short to confirm all 9 files dirty.
  </action>
  <verify>
    <automated>cd leanshot && npx vitest run src/components/admin/growth/BayesianBadge.test.tsx src/components/admin/growth/ShipWinnerConfirmModal.test.tsx src/components/admin/growth/TrafficSplitSlider.test.tsx 2>&1 | tail -15 && test -f ../supabase/migrations/20270714000016_p39_get_experiment_results_rpc.sql && test -f ../supabase/migrations/20270714000017_p39_admin_ship_below_95_rpc.sql && test -f ../supabase/tests/p39_ship_below_95_audit.sql && grep -q "is_admin_at_least('superadmin')" ../supabase/migrations/20270714000017_p39_admin_ship_below_95_rpc.sql</automated>
  </verify>
  <done>3 vitest suites GREEN. 2 RPC migrations exist with proper SECDEF + superadmin gate (for ship_below_95). pgTAP audit-trail proof file exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PaywallExperimentTab + PageExperimentTab + ExperimentDashboardPage wiring</name>
  <files>leanshot/src/components/admin/growth/PaywallExperimentTab.tsx, leanshot/src/components/admin/growth/PaywallExperimentTab.test.tsx, leanshot/src/components/admin/growth/PageExperimentTab.tsx, leanshot/src/components/admin/growth/PageExperimentTab.test.tsx, leanshot/src/components/admin/growth/ExperimentDashboardPage.tsx</files>
  <behavior>
    - RED PaywallExperimentTab: tests (a) renders one row per ExperimentRow with variant_name + cohort_label + composite_score formatted + BayesianBadge + refund_rate_7d when present; (b) Ship-Winner button per row with data-action='ship-winner'; (c) clicking Ship-Winner with posterior >= 0.95 invokes onShip directly; (d) clicking with posterior < 0.95 opens ShipWinnerConfirmModal; (e) archived_at non-null row shows refund-rate kill-switch banner with 'Re-enable variant' danger CTA; (f) composite score callout 'Composite: paid_rate × 30d_retention = {score}' per UI-SPEC.
    - RED PageExperimentTab: tests (a) per-row 35-day warn banner when warned_at != null AND archived_at == null; (b) 42-day archived banner when archived_at != null with copy from UI-SPEC table 'This variant was auto-archived on {date}. Traffic is back on control.'; (c) Ship-Winner per row reuses the same confirm-modal cascade as PaywallExperimentTab.
    - GREEN: implement both tabs. Reuse OnboardingABPanel.tsx Ship-Winner invoke shape verbatim (per UI-SPEC Hard Constraint 8 + PATTERNS Pattern K).
    - Update ExperimentDashboardPage.tsx to replace the placeholder div with conditional rendering: activeTab='paywall' -> PaywallExperimentTab; activeTab='page' -> PageExperimentTab; activeTab='pharma' -> existing placeholder (Plan 39-08 fills this).
  </behavior>
  <action>
Pre-read OnboardingABPanel.tsx ONCE for the verbatim Ship-Winner button shape + busyKey state. Pre-read ExperimentDashboardPage.tsx (from Plan 39-06) to find the data-testid='experiment-tab-content' placeholder.

The Ship-Winner click handler is a 2-step cascade:
  1. If posterior >= 0.95 -> directly invoke ship-winner-flag (no modal)
  2. If posterior < 0.95 -> open ShipWinnerConfirmModal; on confirm, the modal calls admin_ship_below_95 RPC (for audit) AND THEN invokes ship-winner-flag

The data-action='ship-winner' attribute MUST appear on the button per UI-SPEC Hard Constraint 8. Verifiable via testing-library getByTestId or attribute query.

Banner styling: 35-day warn = warning tone (--color-warning); 42-day archived = neutral row tint + tertiary text; refund-rate kill = danger tone (--color-danger) with 'Re-enable variant' button variant=danger.

Copy verbatim from UI-SPEC copy table for ALL banners + buttons. NO hardcoded English in component source for any UI-SPEC-tabled element.

ExperimentDashboardPage wiring: import PaywallExperimentTab + PageExperimentTab; replace the placeholder block with a switch on activeTab. The pharma branch keeps the placeholder for Plan 39-08 (its tests guard against regression by asserting placeholder still present when activeTab='pharma').

Decision references: D-02 (refund kill banner), D-11 (42-day banners), D-12 (Ship-Winner cascade), PAGEAB-03/05/07, PAYWALL-04.
  </action>
  <verify>
    <automated>cd leanshot && npx vitest run src/components/admin/growth/PaywallExperimentTab.test.tsx src/components/admin/growth/PageExperimentTab.test.tsx src/components/admin/growth/ExperimentDashboardPage.test.tsx 2>&1 | tail -15 && grep -q "data-action=\"ship-winner\"" src/components/admin/growth/PaywallExperimentTab.tsx && grep -q "ship-winner-flag" src/components/admin/growth/PaywallExperimentTab.tsx</automated>
  </verify>
  <done>3 vitest suites GREEN (including the existing ExperimentDashboardPage test extended for tab-switching). data-action='ship-winner' attribute present. ship-winner-flag Edge Fn invoke verbatim. ShipWinnerConfirmModal opens for posterior < 0.95.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> admin_ship_below_95 RPC | superadmin gate (NOT just admin); rejects non-superadmin with insufficient_privilege |
| Admin browser -> ship-winner-flag Edge Fn | Edge Fn re-checks superadmin via its own JWT validation (P34/35 verbatim) |
| Admin RPC -> admin_audit_log | INSERT-only via RPC; admin_audit_log RLS proven append-only by Phase 24 |
| ShipWinnerConfirmModal -> typed-string match | Exact-match 'ship-below-95' enforcement; client-side AND verified at audit-log row inspection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07-01 | Repudiation | Operator ships variant below 95% without audit trail | mitigate | D-12 typed-confirm + admin_ship_below_95 SECDEF RPC writes admin_audit_log row before flag flip; pgTAP proof asserts audit row creation |
| T-39-07-02 | Elevation | Non-superadmin admin invokes admin_ship_below_95 | mitigate | is_admin_at_least('superadmin') gate in RPC body; pgTAP proof asserts insufficient_privilege raised |
| T-39-07-03 | Tampering | Operator copies the 'ship-below-95' string from devtools console bypass | accept (low value) | Typed-confirmation is a UX speed-bump for operational discipline, not a security control; audit-log row + reason field provide post-hoc accountability |
| T-39-07-04 | Spoofing | Ship-Winner button forked to call a different Edge Fn | mitigate | UI-SPEC Hard Constraint 8 + verbatim reuse from OnboardingABPanel + grep gate asserts data-action='ship-winner' + 'ship-winner-flag' literal both present in PaywallExperimentTab source |
| T-39-07-05 | Information disclosure | get_experiment_results leaks cross-tenant variant data to non-admin | mitigate | SECDEF RPC gates on is_admin_at_least('admin'); RLS on underlying matview also admin-only |
| T-39-07-06 | DoS | Operator double-clicks Ship-Winner | mitigate | busyKey state per (flag_id, variant) prevents re-invoke; verbatim pattern from OnboardingABPanel |
</threat_model>

<verification>
- 6 vitest test files green
- 2 RPC migrations exist with proper search_path + SECDEF + role gate
- pgTAP audit proof exists
- data-action='ship-winner' attribute present in PaywallExperimentTab (UI-SPEC Hard Constraint 8)
- 'ship-winner-flag' literal Edge Fn invocation in both tab components
- All copy strings traceable to UI-SPEC copy table (no ad-hoc English)
- ExperimentDashboardPage switches activeTab to render correct sub-tab; pharma branch still placeholder
</verification>

<success_criteria>
- PAYWALL-03/04 + PAGEAB-03/05/07 fully wired
- Ship-Winner contract verbatim-reused (no fork)
- Typed-confirmation + audit-log + Bayesian badge all operational
- Plans 39-08 can mount the Pharma tab into the same shell without disrupting Paywall/Page tabs
</success_criteria>

<output>
Create `.planning/phases/.../39-07-SUMMARY.md` — list 14 files, decision IDs covered (D-02, D-11, D-12, PAGEAB-03/05/07, PAYWALL-04), the 2 RPC names, audit-log action string ('ship_below_95'), and the Ship-Winner cascade rule (≥95% direct vs <95% modal).
</output>
