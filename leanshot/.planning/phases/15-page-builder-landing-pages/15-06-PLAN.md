---
phase: 15-page-builder-landing-pages
plan: 06
type: execute
wave: 3
depends_on: [15-03, 15-04]
files_modified:
  - src/lib/page-builder/embed-src.ts
  - src/lib/page-builder/embed-src.test.ts
  - src/components/admin/pages/blocks/CalendlyBlock.tsx
  - src/components/admin/pages/blocks/YouTubeBlock.tsx
  - src/components/admin/pages/blocks/TallyBlock.tsx
  - src/components/admin/pages/blocks/blocks.test.tsx
  - src/components/admin/pages/editor/PropertyPanel.tsx
  - supabase/functions/page-render/render.ts
autonomous: true
requirements: [PAGE-03]
user_setup: []

must_haves:
  truths:
    - "A staff user can drag a Calendly, a YouTube, and a Tally block — three distinct palette entries — into a page"
    - "Each embed block has its OWN property editor: YouTube exposes videoId + startSeconds + autoplay; Calendly exposes calendlyUrl + prefillEmail; Tally exposes tallyFormUrl + hideTitle"
    - "A published page containing an embed block renders a sandboxed <iframe> with a non-empty title attribute"
    - "The rendered iframe src is built only from a validated provider ID/URL — never from raw user HTML — and resolves to exactly one of calendly.com / www.youtube-nocookie.com / tally.so"
    - "Each rendered embed shows a loading skeleton until the iframe load event fires"
  artifacts:
    - path: "src/lib/page-builder/embed-src.ts"
      provides: "Pure validate-and-build helpers for the 3 embed iframe src strings (no DOM, no Deno globals)"
      exports: ["buildCalendlySrc", "buildYouTubeSrc", "buildTallySrc", "EMBED_IFRAME_TITLES"]
    - path: "src/components/admin/pages/blocks/CalendlyBlock.tsx"
      provides: "CalendlyBlock editor preview component"
      contains: "CalendlyBlock"
    - path: "src/components/admin/pages/blocks/YouTubeBlock.tsx"
      provides: "YouTubeBlock editor preview component"
      contains: "YouTubeBlock"
    - path: "src/components/admin/pages/blocks/TallyBlock.tsx"
      provides: "TallyBlock editor preview component"
      contains: "TallyBlock"
    - path: "supabase/functions/page-render/render.ts"
      provides: "renderBlock() switch — 3 new embed-* case branches producing sandboxed iframe HTML"
      contains: "embed-youtube"
  key_links:
    - from: "supabase/functions/page-render/render.ts"
      to: "src/lib/page-builder/embed-src.ts"
      via: "import of buildCalendlySrc / buildYouTubeSrc / buildTallySrc + EMBED_IFRAME_TITLES"
      pattern: "embed-src"
    - from: "src/components/admin/pages/editor/PropertyPanel.tsx"
      to: "block.type calendly | youtube | tally"
      via: "per-type tailored property-editor field config"
      pattern: "youtube|calendly|tally"
    - from: "src/components/admin/pages/blocks/*Block.tsx"
      to: "src/lib/page-builder/block-schema.ts"
      via: "import type { BlockNode } — never redefined"
      pattern: "block-schema"
---

<objective>
Ship the 3 embed-provider blocks — Calendly, YouTube, Tally — as three SEPARATE draggable
blocks (D-01, D-02), each with its own tailored property editor, plus their three
`renderBlock()` iframe branches in the public `page-render` renderer.

Purpose: PAGE-03's block set is 8 core + 3 embed + 1 lead-form = 12. This plan delivers the
3 embed blocks. It thickens the MVP slice produced by Wave 2 (15-03 backend + 15-04 editor
shell): after this plan a staff user can drop a YouTube/Calendly/Tally block onto a page,
tune it in the right rail, publish, and a visitor sees a real sandboxed iframe with a
loading skeleton.

Output:
- `src/lib/page-builder/embed-src.ts` — pure, fully unit-tested validate+build helpers for
  the 3 iframe `src` strings (the XSS-critical seam — isolated so it is testable with
  vitest without Deno test infra).
- 3 editor block components in `src/components/admin/pages/blocks/`.
- 3 tailored property-editor field configs added to the existing `PropertyPanel`.
- 3 new `embed-*` case branches in the shared `page-render/render.ts` switch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-page-builder-landing-pages/15-CONTEXT.md
@.planning/phases/15-page-builder-landing-pages/15-UI-SPEC.md
@.planning/phases/15-page-builder-landing-pages/15-PATTERNS.md
@.planning/phases/15-page-builder-landing-pages/15-PLAN-OUTLINE.md

<!-- These files are CREATED by 15-03 (block-schema + render.ts + page-render) and 15-04
     (editor shell + PropertyPanel). They MUST exist before this plan runs (wave 3,
     depends_on 15-03 + 15-04). Read them first — do not redefine their exports. -->
@src/lib/page-builder/block-schema.ts
@supabase/functions/page-render/render.ts
@src/components/admin/pages/editor/PropertyPanel.tsx
@src/components/admin/pages/blocks/HeroBlock.tsx

<interfaces>
<!-- Contracts this plan CONSUMES — defined by 15-03's block-schema.ts. Import, never
     redefine. (If a name below differs from 15-03's actual export, defer to 15-03's
     block-schema.ts and adjust — do not introduce a parallel type.) -->

From src/lib/page-builder/block-schema.ts (owned by 15-03):
```typescript
export type BlockType =
  | 'hero' | 'cta' | 'faq' | 'pricing' | 'testimonial'
  | 'feature-grid' | 'image-text' | 'footer'
  | 'calendly' | 'youtube' | 'tally' | 'lead-form';

export interface BlockStyle {
  backgroundTone?: 'default' | 'subtle' | 'brand' | 'dark';
  alignment?: 'left' | 'center' | 'right';
  spacingDensity?: 'compact' | 'default' | 'spacious';
  hideOnMobile?: boolean;
}

export interface BlockNode {
  id: string;
  type: BlockType;
  parent_id: string | null;
  order: number;
  content: Record<string, unknown>;   // type-narrowed per BlockType at the consumer
  style: BlockStyle;
}
```

<!-- Note on render.ts block.type literals: 15-03's BlockType union uses `calendly`,
     `youtube`, `tally` for the block discriminant. The 15-PLAN-OUTLINE Cross-Plan Contract
     Notes name the render.ts case literals `embed-calendly` / `embed-youtube` /
     `embed-tally`. These are NOT in conflict — `block.type` is the schema literal
     (`youtube`), and the renderer switch may key on it directly. Use the EXACT literals
     present in 15-03's shipped `block-schema.ts` + `render.ts` switch when you read them.
     This plan's verify commands assert the string `embed-youtube` appears in render.ts as
     the renderer-side case marker per the outline contract; if 15-03 keyed the switch on
     bare `youtube`, add a comment `// embed-youtube` on the branch so the marker holds and
     downstream greps stay stable. -->
</interfaces>

<!-- Embed content-field shapes THIS plan defines (stored in BlockNode.content). These are
     the property-editor field keys and the renderBlock() branch inputs. -->
<embed_content_shapes>
calendly  → content: { calendlyUrl: string; prefillEmail: boolean }
youtube   → content: { videoId: string; startSeconds: number; autoplay: boolean }
tally     → content: { tallyFormUrl: string; hideTitle: boolean }
</embed_content_shapes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure embed iframe-src validate+build helpers + unit tests</name>
  <files>src/lib/page-builder/embed-src.ts, src/lib/page-builder/embed-src.test.ts</files>
  <read_first>
    - src/lib/page-builder/block-schema.ts (15-03 — to confirm the BlockType literals; this file imports nothing from it but must stay schema-consistent)
    - src/lib/insights.ts (the project's pure-function module style — no DOM, no store, data in → data out)
    - 15-UI-SPEC.md "Published Landing-Page Blocks — Visual Contracts" rows for Calendly / YouTube / Tally embed (the `title`, sandbox, aspect-ratio, min-height contract)
    - 15-RESEARCH.md "Don't Hand-Roll" row "HTML sanitization for block content" (the injection-surface posture: only known fields, no innerHTML)
  </read_first>
  <behavior>
    - Test: `buildYouTubeSrc({ videoId: 'dQw4w9WgXcQ', startSeconds: 0, autoplay: false })` returns a string starting with `https://www.youtube-nocookie.com/embed/dQw4w9WgXcQ` and contains NO `autoplay=1` and NO `start=`.
    - Test: `buildYouTubeSrc({ videoId: 'dQw4w9WgXcQ', startSeconds: 90, autoplay: true })` includes `start=90` and `autoplay=1`.
    - Test: `buildYouTubeSrc` with a videoId containing anything outside `[A-Za-z0-9_-]` (e.g. `"abc\"><script>"`, `"../evil"`, `"a b"`) returns `null` (rejected — not escaped-and-passed).
    - Test: `buildYouTubeSrc` with `videoId` longer than 20 chars returns `null` (YouTube IDs are 11 chars; cap defensively).
    - Test: `buildYouTubeSrc` with negative or non-integer `startSeconds` clamps to `0` (no `start=` param emitted).
    - Test: `buildCalendlySrc({ calendlyUrl: 'https://calendly.com/leanshot/intro', prefillEmail: false })` returns a string whose URL origin is exactly `https://calendly.com` and path is preserved.
    - Test: `buildCalendlySrc` with a URL whose host is NOT `calendly.com` (e.g. `https://evil.com/calendly.com`, `https://calendly.com.evil.com`, `http://calendly.com/x` non-https, `javascript:alert(1)`) returns `null`.
    - Test: `buildCalendlySrc` with `prefillEmail: true` appends `&hide_gdpr_banner=1` is NOT asserted — instead assert it appends nothing that is not a Calendly-recognized query param; specifically with `prefillEmail: true` the returned src contains `embed_domain` is NOT required — assert only that the origin stays `https://calendly.com` and no user string is interpolated unescaped. (prefillEmail is a client-side widget toggle; for the static iframe it only governs whether the editor passes the visitor's email — keep the src origin-locked regardless.)
    - Test: `buildTallySrc({ tallyFormUrl: 'https://tally.so/r/wAbC12', hideTitle: true })` returns a string with origin exactly `https://tally.so`, path preserved, and contains `hideTitle=1`.
    - Test: `buildTallySrc({ ..., hideTitle: false })` contains no `hideTitle=1`.
    - Test: `buildTallySrc` with a non-`tally.so` host, non-https scheme, or a `tally.so` look-alike (`tally.so.evil.com`) returns `null`.
    - Test: `EMBED_IFRAME_TITLES` exports a non-empty `title` string for each of `calendly`, `youtube`, `tally` (e.g. `EMBED_IFRAME_TITLES.youtube` is a non-empty string).
  </behavior>
  <action>
    Create `src/lib/page-builder/embed-src.ts` as a PURE module — no DOM, no `Deno.*`, no
    store, no React, no side effects — so it is importable by both the Deno renderer
    (`page-render/render.ts`) and vitest. This module is the single XSS/clickjacking-critical
    seam: all three iframe `src` strings are constructed here from validated inputs ONLY.

    Export three builder functions — `buildCalendlySrc`, `buildYouTubeSrc`, `buildTallySrc` —
    each taking the typed `content` shape from `<embed_content_shapes>` and returning
    `string | null` (null = invalid input, caller renders a fallback / nothing, never an
    iframe). Export `EMBED_IFRAME_TITLES`, a `Record<'calendly'|'youtube'|'tally', string>`
    of the required non-empty a11y `title` attribute text per UI-SPEC.

    Validation rules (allow-list, NEVER escape-and-pass):
    - `buildYouTubeSrc`: validate `videoId` against `/^[A-Za-z0-9_-]{1,20}$/` — reject (return
      null) on any other character or length. Build `https://www.youtube-nocookie.com/embed/{videoId}`.
      Use `youtube-nocookie.com` (D-01 / UI-SPEC: "prefer nocookie"). Coerce `startSeconds` to a
      non-negative integer via `Math.max(0, Math.floor(n))`; if 0, omit the `start` param. Append
      `autoplay=1` only when `autoplay === true`, else omit. Always append `rel=0`. Build query
      string with `URLSearchParams`.
    - `buildCalendlySrc`: parse `calendlyUrl` with the `URL` constructor inside try/catch; if
      parse throws, return null. Reject unless `url.protocol === 'https:'` AND `url.hostname === 'calendly.com'`
      (exact equality — NOT `endsWith`, NOT `includes`; this defeats `calendly.com.evil.com` and
      `evil.com/calendly.com`). Preserve `url.pathname` + existing `url.search`. Do NOT interpolate
      any raw user string into the src beyond the origin-validated URL. `prefillEmail` does not
      change the static src origin — it only governs whether the editor-side widget is configured
      to prefill; for the rendered iframe, treat it as a no-op on `src` construction (keep the
      origin locked). Return the validated, re-serialized URL string.
    - `buildTallySrc`: same `URL`-parse + `https:` + exact `hostname === 'tally.so'` allow-list as
      Calendly. Preserve pathname. Append `hideTitle=1` to the query when `hideTitle === true`,
      else omit. Re-serialize via the `URL` object so no unescaped user input survives.

    The three allow-listed origins MUST be exactly the three origins 15-02 added to the
    rendered-page CSP `frame-src` (`calendly.com`, `www.youtube-nocookie.com`, `tally.so`). Do
    NOT widen them. Do NOT re-touch CSP — 15-02 owns it.

    Then create `src/lib/page-builder/embed-src.test.ts` (vitest, co-located per project
    convention) covering every case in `<behavior>`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/page-builder/embed-src.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/page-builder/embed-src.test.ts` exits 0 with all `<behavior>` cases passing.
    - `npx tsc -b --noEmit` passes (strict mode — no `any`, no unused).
    - `grep -nE "youtube-nocookie\.com" src/lib/page-builder/embed-src.ts` returns ≥1 match; `grep -c "youtube\.com/embed" src/lib/page-builder/embed-src.ts` — the bare `youtube.com/embed` (non-nocookie) MUST NOT appear: `grep -v '^//' src/lib/page-builder/embed-src.ts | grep -c 'www\.youtube\.com'` returns `0`.
    - `grep -nE "hostname ===|hostname!==|=== 'calendly\.com'|=== 'tally\.so'" src/lib/page-builder/embed-src.ts` confirms exact-equality host checks (no `includes`/`endsWith` on hostname): `grep -nE "hostname.*(includes|endsWith)" src/lib/page-builder/embed-src.ts` returns NO matches.
    - `grep -nE "innerHTML|dangerouslySetInnerHTML|eval\(" src/lib/page-builder/embed-src.ts` returns NO matches.
  </acceptance_criteria>
  <done>
    All embed-src unit tests pass; the module is pure (no DOM/Deno/React imports), exports the
    3 builders + `EMBED_IFRAME_TITLES`, and every invalid-host / invalid-id / wrong-scheme
    input returns `null` rather than an escaped-but-rendered src.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3 embed editor block components + 3 tailored PropertyPanel field configs</name>
  <files>src/components/admin/pages/blocks/CalendlyBlock.tsx, src/components/admin/pages/blocks/YouTubeBlock.tsx, src/components/admin/pages/blocks/TallyBlock.tsx, src/components/admin/pages/blocks/blocks.test.tsx, src/components/admin/pages/editor/PropertyPanel.tsx</files>
  <read_first>
    - src/lib/page-builder/block-schema.ts (15-03 — `BlockNode`, `BlockType`; import, never redefine)
    - src/lib/page-builder/embed-src.ts (Task 1 — the block components use the SAME builders to render their in-editor preview iframe)
    - src/components/admin/pages/editor/PropertyPanel.tsx (15-04 — the EXISTING property-editor pattern; you ADD 3 per-type field configs into it, you do not rewrite it)
    - src/components/admin/pages/blocks/HeroBlock.tsx (15-04 — the established block-component shell: `{ block }: { block: BlockNode }`, token-bounded style classes, `cn` from `@/lib/helpers`)
    - src/components/ui/Input.tsx (DS `Input` / `Select`; the `Textarea` export is also here per 15-PATTERNS)
    - src/components/ui/Skeleton.tsx (DS loading skeleton primitive)
    - 15-UI-SPEC.md "Component Inventory" + the 3 embed rows in "Visual Contracts" + "Interaction Contracts → iframe load (Calendly/YouTube/Tally)" (skeleton → opacity transition)
  </read_first>
  <behavior>
    - Test: `<YouTubeBlock>` rendered with `content: { videoId: 'dQw4w9WgXcQ', startSeconds: 0, autoplay: false }` renders an `<iframe>` whose `title` attribute is non-empty (equals `EMBED_IFRAME_TITLES.youtube`) and whose `src` starts with `https://www.youtube-nocookie.com/embed/dQw4w9WgXcQ`.
    - Test: `<YouTubeBlock>` with an invalid `videoId` (`'../evil'`) renders NO `<iframe>` (renders a fallback placeholder node instead) — asserts the block never emits an iframe with an unvalidated src.
    - Test: `<CalendlyBlock>` with a valid `calendlyUrl` renders an `<iframe>` with a non-empty `title` and `sandbox` attribute present.
    - Test: `<TallyBlock>` with a valid `tallyFormUrl` renders an `<iframe>` with a non-empty `title`; with an invalid (non-`tally.so`) url renders no iframe.
    - Test: each of the 3 block components renders a skeleton element (DS `Skeleton`) that is present before the iframe `load` event (assert the skeleton node exists in the initial render).
    - Test: `getEmbedPropertyFields('youtube')` (or the equivalent config lookup added to PropertyPanel) returns field descriptors for exactly `videoId`, `startSeconds`, `autoplay` — and NOT `calendlyUrl` or `tallyFormUrl`.
    - Test: `getEmbedPropertyFields('calendly')` returns exactly `calendlyUrl`, `prefillEmail`.
    - Test: `getEmbedPropertyFields('tally')` returns exactly `tallyFormUrl`, `hideTitle`.
  </behavior>
  <action>
    Create three editor block components — `CalendlyBlock.tsx`, `YouTubeBlock.tsx`,
    `TallyBlock.tsx` — in `src/components/admin/pages/blocks/`, each following the HeroBlock
    shell: a named export taking `{ block }: { block: BlockNode }`, importing `BlockNode` from
    `@/lib/page-builder/block-schema` (never redefine). These render the IN-EDITOR preview of
    the block. Each component:
    - Narrows `block.content` to its `<embed_content_shapes>` shape.
    - Calls the matching builder from `@/lib/page-builder/embed-src` (`buildYouTubeSrc` /
      `buildCalendlySrc` / `buildTallySrc`). If the builder returns `null`, render a fallback
      placeholder (e.g. a `Card`-wrapped "Add a valid {provider} link" hint) — NEVER an iframe
      with an unvalidated src.
    - When the builder returns a src, render an `<iframe>` with: `src` = builder output,
      `title` = `EMBED_IFRAME_TITLES[provider]` (REQUIRED, non-empty — UI-SPEC a11y / Lighthouse
      ≥95), `sandbox` attribute (minimal-necessary — see render.ts task for the exact attribute
      set; the editor preview uses the SAME set), `referrerpolicy="no-referrer"`, `loading="lazy"`.
      YouTube: 16:9 aspect (`aspect-video` or padding-bottom 56.25%). Calendly: min-height 600px.
      Tally: min-height 400px.
    - Render a DS `<Skeleton>` sized to the iframe, shown until the iframe's `onLoad` fires
      (track with a `useState` `loaded` flag; `opacity-0 → opacity-100` transition on the
      iframe per UI-SPEC Performance Contract). Respect `useReducedMotion()` for the transition.
    - Apply token-bounded style classes from `block.style` exactly as HeroBlock does
      (`backgroundTone`, `spacingDensity`, `hideOnMobile` → `hidden md:block`). No raw hex.

    Then extend `PropertyPanel.tsx` (15-04's file — ADD to it, do not rewrite): add a
    per-type field-config lookup so the right rail shows a TAILORED editor for each embed
    block (D-02 — three separate editors, NOT one generic provider dropdown). Add a function
    (e.g. `getEmbedPropertyFields(type)`) or extend whatever per-type config structure 15-04
    already established, returning the field descriptors:
    - `youtube`: `videoId` (text Input, label "YouTube video ID"), `startSeconds` (number
      Input, label "Start time (seconds)"), `autoplay` (boolean toggle, label "Autoplay",
      default off).
    - `calendly`: `calendlyUrl` (text Input, label "Calendly link"), `prefillEmail` (boolean
      toggle, label "Prefill visitor email").
    - `tally`: `tallyFormUrl` (text Input, label "Tally form link"), `hideTitle` (boolean
      toggle, label "Hide form title").
    Wire these into the existing PropertyPanel field-rendering path (the same `onContentChange`
    pattern HeroBlock's fields use). Do NOT touch the style-field section — that is shared and
    already token-bounded by 15-04.

    Create `src/components/admin/pages/blocks/blocks.test.tsx` (vitest + the project's React
    testing setup) covering every `<behavior>` case. If 15-04 already created a
    `blocks.test.tsx`, ADD your cases to it instead of creating a second file (check in
    `read_first`).
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/pages/blocks/blocks.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/admin/pages/blocks/blocks.test.tsx` exits 0 with all `<behavior>` cases passing.
    - `npx tsc -b --noEmit` passes.
    - `grep -lE "redefine|^export (type )?BlockNode|^interface BlockNode" src/components/admin/pages/blocks/CalendlyBlock.tsx src/components/admin/pages/blocks/YouTubeBlock.tsx src/components/admin/pages/blocks/TallyBlock.tsx` returns NO files (BlockNode is imported, never redefined).
    - `grep -cE "title=" src/components/admin/pages/blocks/CalendlyBlock.tsx src/components/admin/pages/blocks/YouTubeBlock.tsx src/components/admin/pages/blocks/TallyBlock.tsx` — each file has ≥1 `title=` on its iframe.
    - `grep -nE "Skeleton" src/components/admin/pages/blocks/YouTubeBlock.tsx` returns ≥1 match (loading skeleton present); same for Calendly + Tally.
    - `grep -nE "calendlyUrl|tallyFormUrl" src/components/admin/pages/blocks/YouTubeBlock.tsx` returns NO matches (the YouTube editor is tailored — no cross-provider fields).
    - `npm run lint` reports no NEW errors in the 5 modified/created files (baseline lint debt is pre-existing — see project memory).
  </acceptance_criteria>
  <done>
    Three tailored embed block components exist, each rendering a `title`'d sandboxed iframe
    (or a safe fallback when input is invalid) with a loading skeleton; PropertyPanel shows a
    distinct, per-provider field set for each of the 3 blocks; all component tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 3 embed renderBlock() branches in the shared page-render/render.ts</name>
  <files>supabase/functions/page-render/render.ts, src/lib/page-builder/embed-src.test.ts</files>
  <read_first>
    - supabase/functions/page-render/render.ts (15-03 — the EXISTING `renderBlock()` switch; you ADD exactly 3 case branches, you edit ONLY your 3 branches — orchestrator pathspec-commits this shared file)
    - src/lib/page-builder/block-schema.ts (15-03 — the exact `block.type` literals + `BlockNode.content` shape)
    - src/lib/page-builder/embed-src.ts (Task 1 — render.ts imports the SAME 3 builders + `EMBED_IFRAME_TITLES`; the renderer must NOT re-implement src construction)
    - 15-UI-SPEC.md the 3 embed rows in "Published Landing-Page Blocks — Visual Contracts" (sandbox attrs, `allow=`, `referrerpolicy`, aspect ratio, min-heights, `title` required)
    - 15-RESEARCH.md Pitfall 5 (CSP frame-src — 15-02 owns it; this branch RELIES on the 3 origins being allow-listed, must not re-touch CSP)
    - tests/csp/csp-snapshot.txt (READ-ONLY here — confirm the 3 frame-src origins 15-02 added match the 3 builder origins; do NOT edit this file)
  </read_first>
  <behavior>
    - Test (added to embed-src.test.ts as a render-contract block): a `renderEmbedBlock`-style
      pure helper — OR the assertion is performed by importing the builders and asserting the
      HTML-string contract — produces, for a valid YouTube block, an HTML string that contains
      `<iframe`, contains `title="`, contains the `youtube-nocookie.com/embed/` src, and
      contains a `sandbox=` attribute.
    - Test: for an INVALID embed block (builder returns null), the rendered HTML for that
      branch contains NO `<iframe` substring (renders an empty string or a static fallback
      `<div>`, never an iframe with a bad src).
    - Test: the rendered iframe HTML for each provider contains the exact `title` text from
      `EMBED_IFRAME_TITLES`.
    - Test: the rendered iframe HTML does NOT contain any raw unescaped `<` or `"` originating
      from a hostile `content` value (feed `videoId: '"><script>'` → branch emits no iframe).
  </behavior>
  <action>
    Open `supabase/functions/page-render/render.ts` (created by 15-03). Locate the
    `renderBlock()` switch. ADD exactly THREE new case branches — for the embed block types
    (`youtube`, `calendly`, `tally` per 15-03's `BlockType`; tag each branch with a
    `// embed-youtube` / `// embed-calendly` / `// embed-tally` comment so the outline's
    renderer-side case markers hold and downstream greps stay stable). Edit ONLY these 3
    branches — do not modify 15-03's core-block branches, the switch scaffold, the SEO head
    injection, or any helper. The orchestrator pathspec-commits this file across parallel
    Wave-3 plans.

    Each branch:
    - Imports the matching builder from `../../../src/lib/page-builder/embed-src.ts` (the pure
      module — adjust the relative path to whatever resolves from `render.ts`; if the Deno
      function's import resolution cannot reach `src/`, instead have 15-03's render.ts already
      structured to accept a shared module — but the contract here is: render.ts and the
      browser blocks use the SAME `embed-src.ts` builders; do NOT re-implement validation in
      Deno). Also imports `EMBED_IFRAME_TITLES`.
    - Narrows `block.content` to the `<embed_content_shapes>` shape, calls the builder.
    - If the builder returns `null`: the branch returns an empty string (or a minimal static
      `<div class="...">` placeholder) — NEVER an `<iframe>` with an unvalidated/raw src.
    - If the builder returns a src string: return an `<iframe>` HTML string with —
      `src="{builderOutput}"`, `title="{EMBED_IFRAME_TITLES[provider]}"` (REQUIRED — UI-SPEC
      a11y / Lighthouse ≥95), a minimal `sandbox` attribute set: `allow-scripts allow-same-origin`
      plus `allow-popups` only where the provider needs it (Calendly booking opens popups —
      `allow-popups`; YouTube `youtube-nocookie` needs `allow-scripts allow-same-origin
      allow-presentation`; Tally `allow-scripts allow-same-origin`), `referrerpolicy="no-referrer"`,
      `loading="lazy"`, and an `allow=` attribute per UI-SPEC (Calendly: `clipboard-write; payment`;
      YouTube: `encrypted-media; picture-in-picture`; Tally: none needed). Wrap in the block's
      token-bounded section wrapper exactly as the core-block branches do (background tone,
      spacing density, `hide-on-mobile` → the renderer's existing mobile-hide mechanism).
      Apply the loading-skeleton markup the renderer uses for media (a sized placeholder div
      that the iframe overlays once loaded — match whatever skeleton convention 15-03's
      core image/embed-adjacent branches established; if none, emit a sized wrapper `div` with
      a neutral background so CLS stays 0 per UI-SPEC Performance Contract).
    - The iframe `src` MUST come ONLY from the `embed-src.ts` builder output — never
      string-concatenate `block.content` values directly into the HTML. The builder already
      origin-locked + allow-listed the URL; the branch must not widen it.

    Add the render-contract test cases from `<behavior>` to `src/lib/page-builder/embed-src.test.ts`
    (extending Task 1's file — keep the HTML-string contract testable in vitest; if the actual
    HTML assembly lives in Deno-only `render.ts`, factor the per-provider iframe-HTML string
    builder into a pure exported helper in `embed-src.ts` — e.g. `buildYouTubeIframeHtml(content)` —
    that `render.ts` calls, so the HTML contract is unit-testable without Deno test infra).
  </action>
  <verify>
    <automated>npx vitest run src/lib/page-builder/embed-src.test.ts && grep -cE "embed-youtube|embed-calendly|embed-tally" supabase/functions/page-render/render.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/page-builder/embed-src.test.ts` exits 0 — all Task 1 + Task 3 cases pass.
    - `grep -cE "embed-youtube|embed-calendly|embed-tally" supabase/functions/page-render/render.ts` returns `3` (exactly the 3 new branch markers).
    - `grep -nE "title=" supabase/functions/page-render/render.ts` — every embed branch's iframe HTML includes a `title=` attribute (manual confirm the 3 embed branches each have one).
    - `grep -nE "youtube-nocookie" supabase/functions/page-render/render.ts` returns ≥1 OR the nocookie origin is confirmed sourced via the imported builder (render.ts must NOT contain a bare `www.youtube.com/embed` literal: `grep -v '^[[:space:]]*//' supabase/functions/page-render/render.ts | grep -c "www\.youtube\.com"` returns `0`).
    - `git diff --unified=0 supabase/functions/page-render/render.ts` shows ONLY added lines inside the 3 new case branches + the new import line — no edits to 15-03's existing branches or scaffold.
    - The 3 builder origins (`calendly.com`, `www.youtube-nocookie.com`, `tally.so`) each appear in `tests/csp/csp-snapshot.txt`'s `frame-src` line (read-only confirmation that 15-02's CSP widening covers this plan): `grep -E "frame-src" tests/csp/csp-snapshot.txt | grep -cE "calendly\.com.*youtube-nocookie.*tally\.so|tally\.so.*youtube-nocookie.*calendly\.com"` is allowed to be order-independent — at minimum `grep frame-src tests/csp/csp-snapshot.txt` contains all three substrings. If it does NOT, STOP — 15-02 has not landed; this is a dependency violation, not something this plan fixes.
  </acceptance_criteria>
  <done>
    `render.ts` has exactly 3 new embed case branches, each producing a `title`'d, sandboxed,
    origin-locked iframe (or a safe non-iframe fallback), each sourcing its `src` from the
    shared `embed-src.ts` builder; no edits leaked into 15-03's branches; the HTML-string
    contract is covered by vitest; the 3 origins are confirmed present in the CSP snapshot
    that 15-02 owns.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff editor → BlockNode.content | A staff user types `videoId` / `calendlyUrl` / `tallyFormUrl` / `startSeconds` / boolean toggles into the PropertyPanel. Staff are trusted-ish but the values are persisted JSONB and later rendered into public HTML — treat as untrusted at the render boundary. |
| BlockNode.content (JSONB) → page-render iframe `src` | The renderer reads persisted `content` and must build iframe `src` strings. This is the primary XSS / clickjacking surface of this plan. |
| published page → embedded third-party iframe | calendly.com / youtube-nocookie.com / tally.so run untrusted third-party code inside the iframe; the `sandbox` + `referrerpolicy` attributes are the containment boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Tampering / Elevation | `embed-src.ts` builders → `render.ts` iframe src | mitigate | iframe `src` is built ONLY from allow-listed inputs: YouTube `videoId` matched against `/^[A-Za-z0-9_-]{1,20}$/`; Calendly/Tally URLs parsed via `URL` ctor + exact `hostname ===` equality + `protocol === 'https:'`. Invalid input returns `null` → no iframe rendered. No escape-and-pass. (Task 1 + Task 3; unit-tested with hostile inputs incl. `calendly.com.evil.com`, `"><script>`, `javascript:`.) |
| T-15-06-02 | Information disclosure | rendered embed iframe leaking referrer / parent context to third party | mitigate | Every embed iframe carries `referrerpolicy="no-referrer"` and a minimal `sandbox` attribute set (no `allow-top-navigation`, no `allow-modals` unless required). YouTube uses `youtube-nocookie.com` (no tracking cookie until play). (Task 2 + Task 3.) |
| T-15-06-03 | Spoofing | look-alike provider domains (`tally.so.evil.com`, `evil.com/calendly.com`) passing validation | mitigate | Host check is exact string equality on `url.hostname` — never `includes`/`endsWith`. Acceptance criteria greps assert no `includes`/`endsWith` on hostname. (Task 1.) |
| T-15-06-04 | Tampering | iframe `src` origin drifting outside the CSP `frame-src` allow-list | mitigate | The 3 builder origins are pinned to exactly the 3 origins 15-02 added to `frame-src`. Task 3 acceptance criteria reads `tests/csp/csp-snapshot.txt` to confirm alignment; mismatch HALTS the plan (dependency violation, not a fix-here item). This plan does NOT touch CSP. |
| T-15-06-05 | Elevation (clickjacking via embedded frame) | third-party iframe attempting to escape its frame | accept-with-mitigation | `sandbox` attribute restricts the iframe to minimal-necessary capabilities (`allow-scripts allow-same-origin` + provider-specific minimum). Full elimination is impossible while embedding third-party widgets — this is the inherent cost of D-01. Residual risk accepted: the providers (Calendly/YouTube/Tally) are reputable, the blast radius is one sandboxed frame on a public marketing page with no app session, and the rendered page ships zero app JS (UI-SPEC: published pages are static HTML, no React bundle). |
| T-15-06-06 | Denial of service | hostile `startSeconds` (huge / negative / NaN) breaking the renderer | mitigate | `startSeconds` coerced via `Math.max(0, Math.floor(n))`; non-finite → 0. Unit-tested. (Task 1.) |
</threat_model>

<verification>
- `npx vitest run src/lib/page-builder/embed-src.test.ts` — pure builder + HTML-contract tests green.
- `npx vitest run src/components/admin/pages/blocks/blocks.test.tsx` — 3 embed block components green.
- `npx tsc -b --noEmit` — strict TypeScript passes across all created/modified files.
- `grep -cE "embed-youtube|embed-calendly|embed-tally" supabase/functions/page-render/render.ts` returns `3`.
- `git diff --unified=0 supabase/functions/page-render/render.ts` shows only additive changes inside the 3 new branches + the import line — no edits to 15-03's existing code, no CSP file touched.
- `grep -E "frame-src" tests/csp/csp-snapshot.txt` contains `calendly.com`, `www.youtube-nocookie.com`, and `tally.so` (read-only dependency confirmation — 15-02 owns this file).
- `npm run lint` — no NEW lint errors in this plan's files (pre-existing baseline debt excepted).
</verification>

<success_criteria>
- Three SEPARATE draggable embed blocks (Calendly, YouTube, Tally) exist as distinct block
  components with distinct, tailored PropertyPanel field sets — NOT one generic Embed block
  with a provider dropdown (D-01 / D-02 satisfied).
- The shared `page-render/render.ts` has exactly 3 new embed `renderBlock()` branches, each
  producing a sandboxed `<iframe>` with a REQUIRED non-empty `title` attribute, an origin-locked
  `src` built only from validated input, and a CLS-safe loading wrapper.
- Every iframe `src` resolves to exactly one of `calendly.com` / `www.youtube-nocookie.com` /
  `tally.so` — the 3 origins 15-02 allow-listed in the rendered-page CSP `frame-src` — and
  hostile input (look-alike hosts, script-injection strings, wrong scheme) is rejected to a
  no-iframe fallback rather than escaped-and-rendered.
- PAGE-03's embed-block portion (3 of the 12 blocks) is delivered.
- CSP and `tests/csp/csp-snapshot.txt` are NOT modified by this plan (15-02 owns them).
- All automated verifies green; `tsc` strict passes.
</success_criteria>

<output>
After completion, create `.planning/phases/15-page-builder-landing-pages/15-06-SUMMARY.md`
using the summary template. Note explicitly: which `block.type` literals 15-03's shipped
`block-schema.ts` actually used (so downstream plans + verification stay aligned), whether
the iframe-HTML assembly was factored into a pure `embed-src.ts` helper or kept inline in
`render.ts`, and confirm the 3 CSP `frame-src` origins were present (15-02 dependency
satisfied) or flag if they were not.
</output>
