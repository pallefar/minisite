---
phase: 15-page-builder-landing-pages
plan: 09
type: execute
wave: 3
depends_on: [15-04]
files_modified:
  - src/lib/page-builder/templates.ts
  - src/lib/page-builder/templates.test.ts
  - src/lib/page-builder/build-image-url.ts
  - src/lib/page-builder/build-image-url.test.ts
  - src/lib/page-builder/page-assets.ts
  - src/components/admin/pages/TemplatePicker.tsx
  - src/components/admin/pages/AssetLibraryPicker.tsx
  - src/components/admin/pages/editor/VersionHistoryPanel.tsx
  - src/components/admin/pages/editor/VersionHistoryPanel.test.tsx
autonomous: true
requirements: [PAGE-04, PAGE-07, PAGE-01]

user_setup:
  - service: supabase
    why: "buildImageUrl() tier-conditional helper needs to know if the project's Supabase plan supports Storage image transforms (Pro-tier only — RESEARCH Pitfall 3)."
    env_vars:
      - name: VITE_SUPABASE_STORAGE_TRANSFORMS_ENABLED
        source: "Set to 'true' ONLY if project ytnsipxxmzgaebkqmokp is on the Supabase Pro plan (check `supabase projects list` or dashboard billing). Leave unset/false on the free tier — buildImageUrl() then serves raw bucket URLs. Add to Vercel env (all environments) + leanshot/.env.local for dev."

must_haves:
  truths:
    - "Staff opens version history and sees an append-only list of every saved revision (timestamp + author) for the current page"
    - "Staff restores a prior revision and the live page reverts to it within the revalidation window — the revision row itself is never mutated"
    - "Staff opens the asset library, uploads an image, and the upload is rejected unless a non-empty alt-text is supplied"
    - "Staff picks one of 5 code-defined templates and gets a new, fully independent page whose block tree is a deep copy — editing it never touches the template"
    - "Rendered images resolve to a transform URL when VITE_SUPABASE_STORAGE_TRANSFORMS_ENABLED is true, and to the raw bucket URL otherwise"
  artifacts:
    - path: "src/lib/page-builder/templates.ts"
      provides: "5 code-defined templates (long-form-sales, lead-magnet, comparison, faq, testimonial) as BlockNode[] trees + scaffoldFromTemplate() deep-copy helper"
      exports: ["TEMPLATES", "scaffoldFromTemplate", "TemplateId"]
    - path: "src/lib/page-builder/build-image-url.ts"
      provides: "Tier-conditional Supabase Storage URL builder"
      exports: ["buildImageUrl"]
    - path: "src/lib/page-builder/page-assets.ts"
      provides: "page-assets bucket upload + list wrappers with required alt-text + MIME/size validation"
      exports: ["uploadPageAsset", "listPageAssets", "PAGE_ASSETS_BUCKET"]
    - path: "src/components/admin/pages/editor/VersionHistoryPanel.tsx"
      provides: "Sheet listing landing_page_revisions + restore-with-confirm that re-points published_revision_id via page-publish"
      exports: ["VersionHistoryPanel"]
    - path: "src/components/admin/pages/AssetLibraryPicker.tsx"
      provides: "Modal + grid asset picker with upload + required alt-text capture"
      exports: ["AssetLibraryPicker"]
    - path: "src/components/admin/pages/TemplatePicker.tsx"
      provides: "Modal grid of 5 template cards; selection scaffolds an independent page"
      exports: ["TemplatePicker"]
  key_links:
    - from: "src/components/admin/pages/editor/VersionHistoryPanel.tsx"
      to: "supabase/functions/page-publish"
      via: "POST { pageId, revisionId } to re-point published_revision_id"
      pattern: "page-publish"
    - from: "src/components/admin/pages/AssetLibraryPicker.tsx"
      to: "src/lib/page-builder/page-assets.ts"
      via: "uploadPageAsset() + listPageAssets() on the page-assets bucket"
      pattern: "uploadPageAsset|listPageAssets"
    - from: "src/components/admin/pages/TemplatePicker.tsx"
      to: "src/lib/page-builder/templates.ts"
      via: "scaffoldFromTemplate() deep-copies the template block tree"
      pattern: "scaffoldFromTemplate"
    - from: "src/lib/page-builder/page-assets.ts"
      to: "src/lib/page-builder/build-image-url.ts"
      via: "listPageAssets() returns display URLs built by buildImageUrl()"
      pattern: "buildImageUrl"
---

<objective>
Thicken the page-builder slice with the three "page management" surfaces that turn the
minimal editor (15-04) into a production tool: **version history + restore** (PAGE-07),
the **asset library** on the `page-assets` bucket (PAGE-01 scope expansion per D-13), and
the **5-template scaffold** picker (PAGE-04, D-14).

Purpose: After 15-04 a staff user can scaffold a blank page, save, and publish. This plan
gives them (a) a safety net — restore any prior revision; (b) reusable media — upload once,
pick anywhere, with mandatory alt-text for the Lighthouse a11y ≥95 gate; (c) a fast start —
pick a high-converting template instead of a blank canvas.

Output: `templates.ts` (+ test), `build-image-url.ts` (+ test), `page-assets.ts`,
`VersionHistoryPanel.tsx` (+ test), `AssetLibraryPicker.tsx`, `TemplatePicker.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-page-builder-landing-pages/15-CONTEXT.md
@.planning/phases/15-page-builder-landing-pages/15-PATTERNS.md
@.planning/phases/15-page-builder-landing-pages/15-UI-SPEC.md
@.planning/phases/15-page-builder-landing-pages/15-PLAN-OUTLINE.md

<interfaces>
<!-- Contracts this plan consumes. Import these — do NOT redefine. -->

<!-- From src/lib/page-builder/block-schema.ts (created by 15-03, the contract file): -->
```typescript
export type BlockType =
  | 'hero' | 'cta' | 'faq' | 'pricing' | 'testimonial'
  | 'feature-grid' | 'image-text' | 'footer'
  | 'calendly' | 'youtube' | 'tally' | 'lead-form';

export interface BlockStyle {
  backgroundTone?: 'default' | 'subtle' | 'brand' | 'dark';
  alignment?: 'left' | 'center' | 'right';
  spacingDensity?: 'compact' | 'default' | 'spacious';
  hideOnMobile?: boolean;
}

export interface BlockNode {
  id: string;                        // nanoid(8) — stable within a revision
  type: BlockType;
  parent_id: string | null;          // null = root level
  order: number;                     // integer sibling sort key
  content: Record<string, unknown>;  // type-specific fields
  style: BlockStyle;
}
```
NOTE: if 15-03 named the id generator or exported a `newBlockId()` helper, reuse it.
If not present, generate ids with `nanoid(8)` (nanoid is already a transitive dep via
existing libs; if absent, fall back to `crypto.randomUUID().slice(0, 8)`).

<!-- From src/lib/supabase.ts (existing): -->
```typescript
export const supabase: SupabaseClient;  // browser-side singleton, storageKey 'sb-leanshot-auth'
```

<!-- From src/components/ui/Sheet.tsx (existing DS primitive): -->
```typescript
export interface SheetProps {
  open: boolean;
  onClose: () => void;
  title?: ReactNode;
  height?: number;       // mobile only, default 0.75
  children?: ReactNode;
}
export function Sheet(props: SheetProps): JSX.Element;
// Applies role="dialog" + aria-modal + Escape-to-close automatically.
```

<!-- From src/components/ui/Modal.tsx (existing DS primitive): -->
```typescript
export interface ModalProps {
  open: boolean;
  onClose: () => void;
  title?: ReactNode;
  size?: 'sm' | 'md' | 'lg' | 'xl';
  mobileFullscreen?: boolean;
  hideClose?: boolean;
  closeOnBackdrop?: boolean;   // when false, ESC + backdrop are no-ops
  headerAction?: ReactNode;
  children?: ReactNode;
}
export function Modal(props: ModalProps): JSX.Element;
// Applies role="dialog" + aria-modal + focus trap + focus-return automatically.
```

<!-- page-publish Edge Function contract (created by 15-04). RESTORE IS A PUBLISH of an
     older revision — do NOT add a new function. page-publish already re-points
     landing_pages.published_revision_id and fires the x-prerender-revalidate bypass. -->
POST {VITE_SUPABASE_FUNCTIONS_URL}/page-publish
  Authorization: Bearer <user JWT>
  body: { "pageId": "<uuid>", "revisionId": "<uuid>" }
  → 200 { ok: true } on success; 401 unauthenticated; 403 forbidden (not is_staff)
  page-publish enforces that revisionId.page_id === pageId server-side (15-04 owns
  that check). This plan's VersionHistoryPanel only supplies a revisionId drawn from
  THIS page's own revision list — never a cross-page id.

<!-- uploadOrgLogo analog — src/lib/clinic.ts (the storage-upload pattern to mirror): -->
```typescript
// supabase.storage.from('org-logos').upload(path, file, { contentType, upsert: true })
// returns { ok: true, path } | { ok: false, error: 'file_too_large' | 'invalid_mime' | 'network' }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: templates.ts — 5 code-defined templates + scaffoldFromTemplate deep-copy</name>
  <files>src/lib/page-builder/templates.ts, src/lib/page-builder/templates.test.ts</files>
  <read_first>
    - .planning/phases/15-page-builder-landing-pages/15-RESEARCH.md (Pattern 1 — block-tree schema; lines ~253-285)
    - .planning/phases/15-page-builder-landing-pages/15-UI-SPEC.md ("Block Vertical Rhythm", "Component Inventory" block visual contracts, Copywriting Contract — published-page copy)
    - src/lib/constants.ts (the static-lookup-data analog — SCREAMING_SNAKE_CASE module constant pattern)
    - src/lib/insights.ts (pure-function module style — no side effects, no DOM, no store access)
    - src/lib/page-builder/block-schema.ts (the BlockType / BlockNode / BlockStyle contract — IMPORT, never redefine)
  </read_first>
  <behavior>
    - Test: TEMPLATES has exactly 5 entries with ids 'long-form-sales', 'lead-magnet', 'comparison', 'faq', 'testimonial'.
    - Test: every template's `blocks` array is non-empty and every BlockNode has a valid BlockType from the imported union.
    - Test: every BlockNode in every template has a unique `id` within that template; `order` values are sequential integers per sibling group; root blocks have `parent_id: null`.
    - Test: each template's block trees use ONLY token-bounded BlockStyle values (backgroundTone in the 4-value set, alignment in the 3-value set, spacingDensity in the 3-value set) — no arbitrary values (D-05).
    - Test: scaffoldFromTemplate('faq') returns a BlockNode[] that is a DEEP copy — mutating a nested `content` object or `style` object on the result does NOT mutate TEMPLATES['faq'] (D-14 independence invariant).
    - Test: scaffoldFromTemplate regenerates every block `id` — no id in the scaffolded tree equals any id in the source template (so two scaffolds from the same template never collide).
    - Test: scaffoldFromTemplate preserves the parent/child topology — if block B had parent_id pointing at block A in the template, the scaffolded B's parent_id points at the scaffolded A's NEW id (re-mapped, not stale).
  </behavior>
  <action>
    Create `src/lib/page-builder/templates.ts` as a pure, side-effect-free data + transform
    module (mirror `src/lib/insights.ts` discipline — no imports from the store, no DOM).

    Export `type TemplateId = 'long-form-sales' | 'lead-magnet' | 'comparison' | 'faq' | 'testimonial'`.
    Export `interface Template { id: TemplateId; name: string; description: string; thumbnail: string;
    seoSchemaType: SchemaType; blocks: BlockNode[] }` where `name`/`description` come from the
    UI-SPEC Template picker copy (Long-form sales, Lead-magnet opt-in, Comparison, FAQ,
    Testimonial-driven) and `seoSchemaType` defaults the JSON-LD type per D-16 (faq template →
    'FAQPage', others → 'WebPage'; 'comparison' may use 'WebPage'). `thumbnail` is a path string
    under `/assets/page-templates/<id>.webp` — a placeholder path is acceptable; do NOT generate
    image binaries. Import `SchemaType` from `src/lib/page-builder/json-ld.ts` if 15-08 has
    landed it; if that import is not yet resolvable at execution time, inline a local
    `type SchemaType = 'WebPage' | 'FAQPage' | 'Product' | 'Article' | 'Event'` with a
    `// TODO: re-import from json-ld.ts once 15-08 lands` comment (parallel-wave tolerance).

    Export `const TEMPLATES: Record<TemplateId, Template>`. Build each template's `blocks` as a
    `BlockNode[]` using the 8 core block types only (hero/cta/faq/pricing/testimonial/
    feature-grid/image-text/footer — embed + lead-form blocks belong to other plans and need not
    appear in templates, though `lead-magnet` SHOULD include a `lead-form` block since D-12 makes
    that template's opt-in a native form). Each block's `content` carries sensible default copy
    drawn from the UI-SPEC published-page Copywriting Contract (e.g. hero ctaLabel "Get started").
    Keep trees realistic but compact (4-8 blocks each) — these are starting points, not finished pages.

    Export `function scaffoldFromTemplate(id: TemplateId): BlockNode[]`. It MUST:
    1. Deep-clone the template's `blocks` (structuredClone is available in the target ES2022
       runtime — use it; do NOT use JSON.parse(JSON.stringify()) which loses nothing here but
       structuredClone is the project-correct choice).
    2. Build an id-remap table: for every source block id, generate a fresh id (nanoid(8) — reuse
       15-03's id helper if it exported one; otherwise `crypto.randomUUID().slice(0, 8)`).
    3. Rewrite each cloned block's `id` to its new id AND rewrite each non-null `parent_id` through
       the remap table so the topology is preserved against the NEW ids.
    Return the remapped deep copy. The caller (TemplatePicker) owns persisting it as the new
    page's first revision.

    Per D-14: this is a ONE-TIME scaffold. There is no live link, no propagation — the function
    name and the deep-copy guarantee ARE the contract. Add a file-header comment stating this.
  </action>
  <verify>
    <automated>cd leanshot && npm run test -- src/lib/page-builder/templates.test.ts --run</automated>
  </verify>
  <done>
    `npm run test -- src/lib/page-builder/templates.test.ts --run` passes all behavior cases;
    `npx tsc -b` clean; TEMPLATES has 5 entries; scaffoldFromTemplate proven to deep-copy +
    regenerate ids + remap parent_id.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildImageUrl tier-conditional helper + page-assets bucket wrappers + TemplatePicker + AssetLibraryPicker</name>
  <files>src/lib/page-builder/build-image-url.ts, src/lib/page-builder/build-image-url.test.ts, src/lib/page-builder/page-assets.ts, src/components/admin/pages/TemplatePicker.tsx, src/components/admin/pages/AssetLibraryPicker.tsx</files>
  <read_first>
    - .planning/phases/15-page-builder-landing-pages/15-RESEARCH.md (Pitfall 3 — Supabase Storage transforms Pro-only, lines ~548-551; Environment Availability A6, lines ~814+)
    - .planning/phases/15-page-builder-landing-pages/15-PATTERNS.md ("TemplatePicker" Modal+Card-grid pattern, "AssetLibraryPicker" Modal+image-grid pattern, "DS Primitive Imports", "Accessibility Conventions")
    - .planning/phases/15-page-builder-landing-pages/15-UI-SPEC.md ("Asset library (D-13)" interaction contract, "Template picker (D-14)" interaction contract, Copywriting Contract — empty states + errors)
    - src/lib/clinic.ts (the `uploadOrgLogo` storage-upload analog — MIME set, byte cap, upsert, typed Result return — lines ~392-425)
    - src/lib/supabase.ts (the browser supabase singleton — import `supabase`)
    - src/components/ui/Modal.tsx (Modal primitive — props + auto a11y)
    - src/components/ui/Card.tsx (Card variants — `clickable` for template cards, `default` for asset cards)
    - src/components/ui/Input.tsx (Input/Select/Textarea — alt-text field uses Input with aria-required)
    - src/components/ui/Button.tsx (Button variants — `secondary` for upload, `primary` for confirm)
  </read_first>
  <behavior>
    - Test (buildImageUrl): when VITE_SUPABASE_STORAGE_TRANSFORMS_ENABLED is 'true', buildImageUrl(path, { width: 1200 }) returns a URL containing the `render/image` transform segment and `width=1200`.
    - Test (buildImageUrl): when the flag is unset/'false', buildImageUrl(path, { width: 1200 }) returns the raw `object/public` (or signed) bucket URL with NO `width=` query param and NO `render/image` segment.
    - Test (buildImageUrl): buildImageUrl(path) with no opts returns the raw bucket URL on both tiers (no transform requested → no transform applied).
    - Test (buildImageUrl): the flag is read through a single injectable seam (a `transformsEnabled` arg defaulting to `import.meta.env.VITE_SUPABASE_STORAGE_TRANSFORMS_ENABLED === 'true'`) so the test can exercise both branches without mutating import.meta.env.
  </behavior>
  <action>
    **build-image-url.ts** — Export `function buildImageUrl(path: string, opts?: { width?: number;
    height?: number; resize?: 'cover' | 'contain' | 'fill'; transformsEnabled?: boolean }): string`.
    The `transformsEnabled` param defaults to `import.meta.env.VITE_SUPABASE_STORAGE_TRANSFORMS_ENABLED
    === 'true'` (the injectable seam — the test passes it explicitly). When transforms are enabled AND
    at least one of width/height is supplied, return the Supabase Storage **image transform** URL
    (`.../storage/v1/render/image/public/page-assets/<path>?width=...&resize=...`) — derive the base
    from `import.meta.env.VITE_SUPABASE_URL`. Otherwise return the raw public object URL
    (`.../storage/v1/object/public/page-assets/<path>`). Per RESEARCH Pitfall 3: this conditional
    is the entire point — free-tier projects 400 on transform URLs, so the flag gates it. Add a
    file-header comment citing Pitfall 3 + the `VITE_SUPABASE_STORAGE_TRANSFORMS_ENABLED` env var.

    **page-assets.ts** — Export `const PAGE_ASSETS_BUCKET = 'page-assets'`. Mirror `uploadOrgLogo`
    from `src/lib/clinic.ts`:
    - `const PAGE_ASSET_MAX_BYTES = 10 * 1024 * 1024` (10 MB — matches the bucket's `file_size_limit`
      set by 15-01) and `const PAGE_ASSET_MIMES = new Set(['image/jpeg','image/png','image/webp','image/gif'])`
      (matches the bucket's `allowed_mime_types` from 15-01).
    - `async function uploadPageAsset(file: File, altText: string): Promise<{ ok: true; path: string;
      url: string } | { ok: false; error: 'file_too_large' | 'invalid_mime' | 'missing_alt' | 'network' }>`.
      It MUST reject with `'missing_alt'` when `altText.trim()` is empty — the **required alt-text**
      enforcement (D-13, Lighthouse a11y ≥95) lives here as the floor, not just in the UI. Validate
      size + MIME first, then `supabase.storage.from(PAGE_ASSETS_BUCKET).upload(path, file, {
      contentType: file.type, upsert: false })`. Path shape: `${crypto.randomUUID()}.${ext}`. Persist
      the alt-text alongside the object — store it via the upload's `metadata` option if supported,
      otherwise write it as a Storage object `name`-adjacent record; simplest project-correct route:
      pass it in the upload options' `metadata` field AND return it so the picker can surface it.
      Return `url` built via `buildImageUrl(path)`.
    - `async function listPageAssets(): Promise<Array<{ path: string; url: string; altText: string;
      name: string }>>` — `supabase.storage.from(PAGE_ASSETS_BUCKET).list()`, map each object to its
      display `url` via `buildImageUrl()`, and read back the stored alt-text from object metadata
      (empty string if absent). The bucket listing RLS (created by 15-01) already scopes this to
      `is_staff` users — do NOT re-implement an auth check here; rely on RLS (a non-staff caller
      gets an empty/denied list from Supabase).

    **TemplatePicker.tsx** — Export `function TemplatePicker({ open, onClose, onScaffold }: {
    open: boolean; onClose: () => void; onScaffold: (blocks: BlockNode[]) => void })`. Use the
    `Modal` primitive (size="lg", title "Choose a template"). Render a 2-3 col grid of `Card`
    (variant="clickable") — one per `TEMPLATES` entry — each with thumbnail `<img alt="">`,
    name (16px semibold), description (13px). On card click/Enter: call
    `onScaffold(scaffoldFromTemplate(tpl.id))` then `onClose()`. Include a "Start blank" `Button`
    (variant="ghost") that calls `onScaffold([])` + `onClose()`. Per UI-SPEC: shown only on new-page
    creation; cards are keyboard-activatable (`role="button"`, `tabIndex={0}`, `onKeyDown` Enter).
    The Modal already supplies role="dialog"/aria-modal/focus-trap.

    **AssetLibraryPicker.tsx** — Export `function AssetLibraryPicker({ open, onClose, onSelect }: {
    open: boolean; onClose: () => void; onSelect: (asset: { path: string; url: string;
    altText: string }) => void })`. Use the `Modal` primitive (aria-label="Asset library").
    On open, call `listPageAssets()` into state (show a `Skeleton` grid while loading; the
    "No images uploaded" empty state copy from the UI-SPEC when the list is empty). Render a
    3-col image grid; each image is a selectable card with a checkmark when selected. An "Upload
    image" `Button` (variant="secondary") opens a file input accepting
    `image/jpeg,image/png,image/webp,image/gif`. **Required alt-text:** when the user selects an
    image (newly uploaded OR existing-from-grid), show an `Input` (`aria-required`,
    label "Alt text") below the grid; if the asset already has a saved alt-text it pre-fills; the
    confirm `Button` (variant="primary", calls `onSelect`) stays `disabled` until the alt-text input
    is non-empty. For a new upload, call `uploadPageAsset(file, altText)` — surface its typed errors
    inline with the UI-SPEC error copy ("Upload failed. Images must be under 10 MB. Try a different
    file." for file_too_large/invalid_mime/network; the alt-text gate prevents `missing_alt` from
    the UI but handle it defensively). All admin-bundle components import DS primitives from
    `@/components/ui/*` and use token-bounded styling (CSS custom properties, never raw hex).
  </action>
  <verify>
    <automated>cd leanshot && npm run test -- src/lib/page-builder/build-image-url.test.ts --run && npx tsc -b</automated>
  </verify>
  <done>
    `build-image-url.test.ts` passes both tier branches + the no-opts case; `npx tsc -b` clean;
    `page-assets.ts` rejects empty alt-text with `'missing_alt'`; TemplatePicker + AssetLibraryPicker
    compile and import only existing DS primitives + the new lib helpers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VersionHistoryPanel — append-only revision list + restore-with-confirm re-pointing published_revision_id</name>
  <files>src/components/admin/pages/editor/VersionHistoryPanel.tsx, src/components/admin/pages/editor/VersionHistoryPanel.test.tsx</files>
  <read_first>
    - .planning/phases/15-page-builder-landing-pages/15-PATTERNS.md ("VersionHistory.tsx" Sheet pattern — Sheet + Modal confirm; "Accessibility Conventions"; "DS Primitive Imports")
    - .planning/phases/15-page-builder-landing-pages/15-UI-SPEC.md ("Version history (D-07)" interaction contract; Restore confirmation copy; "No saved versions yet..." empty state)
    - .planning/phases/15-page-builder-landing-pages/15-CONTEXT.md (D-07 — published-pointer + draft revisions; restore re-points, NEVER mutates a revision row)
    - .planning/phases/15-page-builder-landing-pages/15-PLAN-OUTLINE.md ("Cross-Plan Contract Notes" — append-only invariant; 15-04 owns page-publish)
    - src/components/ui/Sheet.tsx (Sheet primitive)
    - src/components/ui/Modal.tsx (Modal primitive — restore confirmation dialog)
    - src/components/ui/Button.tsx (Button — `secondary` Restore row button, `primary` confirm)
    - src/lib/supabase.ts (browser supabase singleton — used to query landing_page_revisions)
    - tests/rls/subscriptions-impersonation.test.ts (the project test conventions — describe/it/expect from vitest, env-gated live-DB pattern; reference only)
  </read_first>
  <behavior>
    - Test: VersionHistoryPanel renders one row per revision passed in, each showing a formatted timestamp and author email; rows are ordered newest-first.
    - Test: with an empty revisions array, the panel renders the empty-state copy "No saved versions yet. Changes are saved automatically." and renders no Restore buttons.
    - Test: clicking a row's "Restore" button does NOT immediately call the restore handler — it opens the confirmation Modal first (title "Restore this version?").
    - Test: confirming in the Modal calls the injected restore callback exactly once with that row's revision id; cancelling calls it zero times and closes the Modal.
    - Test (invariant guard): the restore callback is only ever invoked with a revisionId that exists in the panel's own `revisions` prop — the component never fabricates or accepts an out-of-list id (proves no cross-page re-pointing from this surface).
  </behavior>
  <action>
    Export `function VersionHistoryPanel({ open, onClose, pageId, revisions, onRestored }: {
    open: boolean; onClose: () => void; pageId: string;
    revisions: Array<{ id: string; created_at: string; created_by_email: string }>;
    onRestored?: () => void })`.

    Use the `Sheet` primitive (title "Version history") — it slides in from the right and supplies
    role="dialog"/aria-modal. The parent editor (15-04's PageEditorView) owns fetching the
    `revisions` list from `landing_page_revisions` for `pageId` and passing it in; this component
    does not fetch — but it MAY accept the list pre-sorted or sort it newest-first by `created_at`
    itself (do the sort here so the contract is self-contained). Render each revision as a row:
    timestamp in Geist Mono 13px (use the `font-mono` token class), author email 13px, and a
    `Button` (variant="secondary", size="sm") labelled "Restore". Empty state: the UI-SPEC copy.

    **Restore flow (D-07 — re-point, never mutate):** clicking "Restore" sets a
    `confirmRevisionId` state and opens a `Modal` (size="sm", title "Restore this version?", body
    "The page will revert to this revision. Any unpublished changes will be preserved in history.",
    confirm `Button` variant="primary" "Restore", cancel `Button` variant="secondary" "Cancel" —
    exact UI-SPEC copy). On confirm: POST to `page-publish` (the Edge Function created by 15-04) with
    `{ pageId, revisionId: confirmRevisionId }` and the user's `Authorization: Bearer` JWT (read the
    session token via `supabase.auth.getSession()`). page-publish re-points
    `landing_pages.published_revision_id` to that revision id and fires the on-demand revalidation —
    **restore is literally publishing an older revision; the `landing_page_revisions` row is never
    UPDATEd.** Do NOT add a new Edge Function and do NOT write SQL that mutates a revision row. On
    success: close the Modal, call `onRestored?.()`, show a `role="status"` live-region confirmation.
    On failure: inline error using the UI-SPEC copy "Couldn't restore this version. Try again."

    Make the restore network call injectable for the test — accept an optional
    `restoreFn?: (args: { pageId: string; revisionId: string }) => Promise<void>` prop that defaults
    to the real `page-publish` POST. The test passes a spy `restoreFn` to assert the
    call-exactly-once + correct-revisionId behavior without hitting the network. Add `aria-busy` on
    the confirm button during the in-flight call.
  </action>
  <verify>
    <automated>cd leanshot && npm run test -- src/components/admin/pages/editor/VersionHistoryPanel.test.tsx --run && npx tsc -b</automated>
  </verify>
  <done>
    `VersionHistoryPanel.test.tsx` passes all behavior cases including the invariant guard; restore
    goes through the confirmation Modal then calls `page-publish` (re-point, not mutate); empty
    state renders UI-SPEC copy; `npx tsc -b` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor (browser, is_staff) → `page-assets` Storage bucket | Staff uploads image files; untrusted file bytes + filenames cross here |
| editor (browser, is_staff) → `page-publish` Edge Function | Restore re-points `published_revision_id`; the revisionId is attacker-influenceable client input |
| `page-assets` bucket listing → AssetLibraryPicker | Bucket object list crosses back to the client; must not leak non-page-builder objects |
| asset `url` (built by `buildImageUrl`) → block `content` → `page-render` HTML | An image `src` eventually lands in published HTML; origin must be the trusted bucket only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09-01 | Tampering | `uploadPageAsset` in `page-assets.ts` | mitigate | Validate `file.size <= 10 MB` and `file.type` ∈ {jpeg,png,webp,gif} client-side BEFORE upload; the `page-assets` bucket (15-01) enforces the same `file_size_limit` + `allowed_mime_types` server-side as the real floor. SVG is excluded from the MIME set (mirrors org-logos T-09-09 — SVG can carry inline-script XSS). |
| T-15-09-02 | Elevation of Privilege | `uploadPageAsset` / `listPageAssets` | mitigate | No client-side auth check is the *intent*: the `page-assets` bucket's INSERT/SELECT RLS policies (created by 15-01) gate every operation on `profiles.is_staff = true`. A non-staff caller's upload/list is denied by Postgres RLS, not by hideable client code. 15-01 owns the live cross-tenant impersonation proof test for this bucket. |
| T-15-09-03 | Information Disclosure | `listPageAssets` → AssetLibraryPicker | mitigate | `list()` is scoped to the single `page-assets` bucket id (`PAGE_ASSETS_BUCKET` constant) — it cannot enumerate `org-logos`, `photos`, or any other bucket. The bucket's listing RLS additionally restricts the object set to staff. |
| T-15-09-04 | Tampering | `VersionHistoryPanel` restore → `page-publish` | mitigate | The panel only ever submits a `revisionId` drawn from THIS page's own `revisions` prop (Task 3 invariant test proves it). `page-publish` (15-04) independently verifies `revision.page_id === pageId` server-side before re-pointing — defence in depth against a forged revisionId. Restore re-points the pointer; it never UPDATEs a `landing_page_revisions` row, preserving the append-only invariant (15-01's invariant test). |
| T-15-09-05 | Tampering | asset `url` from `buildImageUrl` inserted into block `content` | accept | `buildImageUrl` only ever emits URLs under `${VITE_SUPABASE_URL}/storage/v1/...` for the `page-assets` bucket — it cannot produce an arbitrary-origin URL because it takes a bucket-relative `path`, not a full URL. Block `content` image fields therefore carry trusted-origin URLs only. The `page-render` HTML-escaping of content fields (15-03's renderer) is the secondary control. Low residual risk; no extra mitigation in this plan. |
| T-15-09-06 | Spoofing | `buildImageUrl` transform branch | accept | A forced `VITE_SUPABASE_STORAGE_TRANSFORMS_ENABLED=true` on a free-tier project causes Supabase to 400 the transform URL — a visible broken image, not a security event. Disposition: accept; the env var is operator-controlled build config, not attacker input. |
</threat_model>

<verification>
- `cd leanshot && npm run test -- src/lib/page-builder/ src/components/admin/pages/ --run` — all three new test files green (templates, build-image-url, VersionHistoryPanel).
- `cd leanshot && npx tsc -b` — strict typecheck clean across the 9 new/modified files.
- `cd leanshot && npm run lint -- src/lib/page-builder/ src/components/admin/pages/` — no NEW lint errors introduced (pre-existing import-x/order debt on unrelated files is the known baseline, not this plan's regression).
- Manual contract check: `grep -rn "scaffoldFromTemplate\|buildImageUrl\|uploadPageAsset\|page-publish" src/components/admin/pages/` confirms the three key_links are wired (TemplatePicker→templates, AssetLibraryPicker→page-assets→build-image-url, VersionHistoryPanel→page-publish).
- Append-only invariant respected: `grep -rn "landing_page_revisions" src/components/admin/pages/editor/VersionHistoryPanel.tsx` shows NO `.update(` against that table — restore only POSTs to `page-publish`.
</verification>

<success_criteria>
- Version history (PAGE-07): VersionHistoryPanel lists `landing_page_revisions` newest-first and restore re-points `published_revision_id` via `page-publish` — the revision row is never mutated; Task 3 invariant test proves no out-of-list revisionId.
- Asset library (PAGE-01 / D-13): `page-assets.ts` uploads to the `page-assets` bucket with 10 MB + MIME validation and **rejects any upload missing alt-text**; AssetLibraryPicker's confirm button stays disabled until alt-text is non-empty; `buildImageUrl` is tier-conditional (RESEARCH Pitfall 3) and proven on both branches.
- Templates (PAGE-04 / D-14): `templates.ts` ships 5 code-defined templates; `scaffoldFromTemplate` produces a deep copy with regenerated ids + remapped `parent_id` topology — editing a scaffolded page provably never touches the template.
- All three new test files green; `npx tsc -b` clean; `<threat_model>` dispositions all assigned.
</success_criteria>

<output>
After completion, create `.planning/phases/15-page-builder-landing-pages/15-09-SUMMARY.md`
(write to the leanshot-subdir `.planning/`, not the repo-root `.planning/`).
</output>
