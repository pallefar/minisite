---
phase: 31-white-label-path-based-org-roles-clinic-onboarding-builder
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql
  - src/lib/org.ts
  - src/types/org.ts
  - src/components/clinic/settings/ClinicSettingsPage.tsx
  - src/lib/__tests__/_fixtures/p28-rls-fixture.ts
  - src/lib/__tests__/org.test.ts
  - src/lib/__tests__/store-org-slice.test.ts
  - src/lib/__tests__/rls-org-members.test.ts
  - src/lib/__tests__/rls-org-invites.test.ts
  - src/lib/__tests__/with-org-scope.test.ts
autonomous: true
requirements: [ORG-12]

must_haves:
  truths:
    - "Postgres enum public.org_member_role has values: 'owner', 'clinician', 'staff' (no 'admin', no 'viewer' present)"
    - "Every SECDEF function in /Users/karstenhaldan/minisite/supabase/migrations/ that previously referenced 'admin' / 'staff' / 'viewer' role string-literals is re-created in the same migration so role checks still return the correct boolean after the rename"
    - "The Phase 28 helper SECDEF _is_org_admin is renamed _is_org_owner and every call-site updated"
    - "src/lib/org.ts ROLE_PERMISSIONS uses keys owner / clinician / staff (P31 plan 31-01 expands the value sets to 12 keys — this plan only flips the keys)"
    - "src/types/org.ts OrgRole = 'owner' | 'clinician' | 'staff'"
    - "All P28 RLS test fixtures + tests using 'admin' / 'staff' / 'viewer' role string literals are updated and the suites still type-check and run"
    - "After the migration is pushed, supabase db query --linked confirms the enum values are exactly {owner, clinician, staff} and _is_org_owner exists"
  artifacts:
    - path: "supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql"
      provides: "Single atomic transaction: ALTER TYPE RENAME VALUE x3 + CREATE OR REPLACE of every SECDEF whose body references role literals + rename of _is_org_admin → _is_org_owner"
      contains: "alter type public.org_member_role rename value 'admin' to 'owner'"
    - path: "src/lib/org.ts"
      provides: "ROLE_PERMISSIONS with keys owner / clinician / staff (value sets unchanged in this plan — 31-01 expands them)"
      contains: "ROLE_PERMISSIONS"
    - path: "src/types/org.ts"
      provides: "OrgRole = 'owner' | 'clinician' | 'staff'"
      contains: "type OrgRole = 'owner' | 'clinician' | 'staff'"
  key_links:
    - from: "supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql"
      to: "public.org_member_role enum + every dependent SECDEF body"
      via: "single transaction (ALTER TYPE RENAME VALUE precedes CREATE OR REPLACE FUNCTION bodies — safe because RENAME VALUE is catalog-level and does not trigger the 55P04 unsafe-new-enum restriction)"
      pattern: "alter type public\\.org_member_role rename value"
    - from: "src/lib/org.ts ROLE_PERMISSIONS"
      to: "src/types/org.ts OrgRole"
      via: "TypeScript Record<OrgRole, ...> compile-time check (renaming OrgRole forces all consumer files to update)"
      pattern: "Record<OrgRole"
    - from: "Phase 28 / 30 callers of _is_org_admin"
      to: "renamed _is_org_owner"
      via: "search-and-replace inside the same migration body so call-sites resolve post-rename"
      pattern: "_is_org_owner"
---

<objective>
RECONCILE Plan 31-00 — atomically rename the org_member_role enum (admin→owner, staff→clinician, viewer→staff) AND ripple the rename through every consumer (SECDEF function bodies, helper function name _is_org_admin → _is_org_owner, TypeScript ROLE_PERMISSIONS keys, OrgRole type, RLS-test fixtures, single client-side role-literal usage in ClinicSettingsPage) in a single migration + matching TS commit, so that downstream P31 plans (31-01..31-06) can start from a fully-renamed catalog.

Purpose: Without this Wave-0 ripple, every other P31 plan would be writing against the wrong vocabulary (or silently emitting code that compares an enum-typed column to a string literal that no longer matches any enum value — Postgres returns false rather than raising, so the bug is invisible until a permission check silently denies everything). RESEARCH Finding 1 confirms the blast radius: 11+ migration files contain role-string literals inside SECDEF bodies that MUST be re-created with the new spellings; the rename is catalog-level and silently breaks string-literal comparisons.

Output: One Postgres migration (atomic transaction), one renamed helper SECDEF, ROLE_PERMISSIONS keyed by the new role names, OrgRole type aligned, all P28 RLS tests + fixtures updated, single client-side role-literal usage in ClinicSettingsPage updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-CONTEXT.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-RESEARCH.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-PLAN-OUTLINE.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-EXTENSION-CONTRACT.md
@supabase/migrations/20270601100003_org_member_role_enum.sql
@src/lib/org.ts
@src/types/org.ts
@src/lib/__tests__/_fixtures/p28-rls-fixture.ts
@CLAUDE.md

<interfaces>
<!-- Verified from current codebase (2026-05-18). The executor uses these directly. -->

Current Postgres enum (will be renamed by this plan):
  CREATE TYPE public.org_member_role AS ENUM ('admin', 'staff', 'viewer');   -- supabase/migrations/20270601100003_*.sql

Current TypeScript shape (will be replaced by this plan):
  // src/types/org.ts:12
  export type OrgRole = 'admin' | 'staff' | 'viewer';

  // src/lib/org.ts:36
  const ROLE_PERMISSIONS: Record<OrgRole, ReadonlySet<string>> = {
    admin: new Set(['members.invite','members.revoke','members.list','settings.edit','branding.edit','patients.link']),
    staff: new Set(['members.list','patients.link']),
    viewer: new Set(['members.list']),
  };

Phase 28 helper SECDEF (signature; body is the body to recreate):
  CREATE OR REPLACE FUNCTION public._is_org_admin(p_org_id uuid, p_user_id uuid) RETURNS boolean
    -- bodies check `role = 'admin'`; after rename this resolves to false silently.
  -- Live callers grepped 2026-05-18:
  --   supabase/migrations/20270601200003_org_patient_invites.sql
  --   supabase/migrations/20270601300008_p30_fix_clinician_alerts_rls.sql
  -- These two migration files are the ONLY current callers — both must read `_is_org_owner` after this plan.

Final TypeScript shape this plan ships:
  export type OrgRole = 'owner' | 'clinician' | 'staff';
  const ROLE_PERMISSIONS: Record<OrgRole, ReadonlySet<string>> = {
    owner:     new Set(['members.invite','members.revoke','members.list','settings.edit','branding.edit','patients.link']),
    clinician: new Set(['members.list','patients.link']),
    staff:     new Set(['members.list']),
  };
  // Per D-03 the value sets EXPAND to 12 keys in Plan 31-01. This plan ONLY flips
  // the keys + their existing value sets. Do NOT pre-emptively add the new 6 keys.

P28 RLS-fixture admin-seeding (will be flipped to 'owner'):
  // src/lib/__tests__/_fixtures/p28-rls-fixture.ts:189,196
  await admin.from('org_members').insert({ org_id: orgX, user_id: userA.id, role: 'admin' });

Test files that contain role string literals and MUST be updated (verbatim grep results 2026-05-18):
  src/lib/__tests__/org.test.ts        — 8 occurrences (mostly 'admin', also 'staff' + 'viewer')
  src/lib/__tests__/store-org-slice.test.ts — 'admin'
  src/lib/__tests__/rls-org-members.test.ts — 'admin' (seed) + 'viewer' (update attempt) + 'admin' (post-assertion)
  src/lib/__tests__/rls-org-invites.test.ts — 'staff' x2 + 'viewer' x2
  src/lib/__tests__/with-org-scope.test.ts  — 'staff' x1 (p_role param payload)

Single client code-path with a role literal:
  src/components/clinic/settings/ClinicSettingsPage.tsx:99
    const isAdmin = currentOrgRole === 'admin';
  After this plan: `const isOwner = currentOrgRole === 'owner';` (and update the dependent
  `isAdmin` references in the permMap below it: `'org_role.admin': isOwner`).

NOT a role literal — do NOT change:
  src/lib/clinic.ts:434 — RESERVED_SLUGS Set contains literal 'admin' as a reserved URL slug
  (banned slug name, NOT a role). Leave untouched.

Migration files whose SECDEF bodies / RLS-policy bodies contain role string literals
that MUST be re-created in this plan (verbatim grep 2026-05-18):
  20260801000002_orgs.sql
  20270601000026_admin_role_enum.sql                       — check whether 'admin'/'staff'/'viewer' refer to the org_member_role enum or to the SEPARATE app-admin role enum (admin_role); only org_member_role values change here.
  20270601000027_profiles_admin_role_column.sql            — same caveat
  20270601000028_audit_logs_admin_columns_rls.sql          — same caveat
  20270601000029_log_admin_action_function.sql             — same caveat
  20270601100005_org_settings_table.sql
  20270601100006_org_branding_table.sql
  20270601100007_org_invites_table.sql
  20270601100008_org_subscriptions_table.sql
  20270601100010_org_consent_grants_table.sql
  20270601100012_send_revoke_org_invite_rpcs.sql
  20270601100013_link_org_patient_rpc.sql                   — `role in ('admin','staff')` (gate that allows admins+staff to link patients; after this plan: `role in ('owner','clinician')`)
  20270601200002_count_active_patients_v2.sql
  20270601200003_org_patient_invites.sql                    — calls _is_org_admin
  20270601200004_org_patient_invite_rpcs.sql                — `v_role <> 'admin'`
  20270601300001_p30_org_settings_extensions.sql
  20270601300002_p30_clinician_alerts_schema.sql
  20270601300003_p30_secdef_and_triggers.sql
  20270601300006_p30_dose_thresholds_rpc.sql                — `v_role <> 'admin'`
  20270601300007_p30_reset_patient_thresholds_rpc.sql
  20270601300008_p30_fix_clinician_alerts_rls.sql           — calls _is_org_admin
  20270601300010_p30_fix_update_org_ranking_weights_upsert.sql — `v_role <> 'admin'`
  20270601300011_p30_fix_get_clinic_alert_metrics_ambiguous.sql

CRITICAL CAVEAT: There are TWO unrelated enums in this codebase:
  (a) public.org_member_role with values 'admin','staff','viewer' — THIS is what changes.
  (b) public.admin_role (Phase 24) — app-staff platform admin enum — DO NOT TOUCH.
  Bodies referencing column names like `admin_role` or `is_admin_at_least(...)` belong to (b)
  and must be left intact. Only string literals compared against `org_members.role` columns
  (or via `org_member_role` casts) are in scope here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the atomic enum-rename + SECDEF-ripple migration</name>
  <files>supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql</files>
  <read_first>
    - supabase/migrations/20270601100003_org_member_role_enum.sql (current enum DDL)
    - Every migration listed in the &lt;interfaces&gt; "Migration files whose SECDEF bodies / RLS-policy bodies contain role string literals" list. Open each, identify EVERY SECDEF function body and EVERY CREATE POLICY body that contains a role string literal (`'admin'`, `'staff'`, `'viewer'`) compared against an `org_members.role` column (or an `org_member_role`-typed variable). Skip any literal that refers to the unrelated `public.admin_role` enum (Phase 24 app-admin).
    - .planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-RESEARCH.md §"Finding 1" (verbatim migration pattern + 55P04 caveat).
  </read_first>
  <action>
    Create a single migration file with the strict 14-digit timestamp filename `20260518070000_p31_00_enum_rename_and_secdef_ripple.sql` (per reference_supabase_migration_filename_regex — letter-suffix names are silently skipped by `supabase db push`).

    Inside the file, wrap everything in a single `begin; ... commit;` transaction (per D-01 + RESEARCH Finding 1: RENAME VALUE is catalog-level and does NOT trigger the 55P04 "unsafe use of new value of enum type" restriction that ADD VALUE has, so SECDEFs that reference the renamed values may be CREATE OR REPLACEd in the same transaction).

    Step 1 — three rename statements (idempotent guard so re-running is safe):
      do $$
      begin
        if exists (select 1 from pg_enum where enumtypid = 'public.org_member_role'::regtype and enumlabel = 'admin') then
          alter type public.org_member_role rename value 'admin' to 'owner';
        end if;
        if exists (select 1 from pg_enum where enumtypid = 'public.org_member_role'::regtype and enumlabel = 'staff') then
          alter type public.org_member_role rename value 'staff' to 'clinician';
        end if;
        if exists (select 1 from pg_enum where enumtypid = 'public.org_member_role'::regtype and enumlabel = 'viewer') then
          alter type public.org_member_role rename value 'viewer' to 'staff';
        end if;
      end $$;

      Order is critical and matches D-01 exactly: admin→owner FIRST, staff→clinician SECOND, viewer→staff THIRD. The 'staff' label appears twice in the rename sequence (once as a source label, once as a target label) — Postgres applies the renames sequentially within the do-block, so after step 2 there is no 'staff' label anymore, and step 3 reuses the freed-up label safely. This is the ONLY correct ordering — reordering will collide.

    Step 2 — `CREATE OR REPLACE FUNCTION` for EVERY SECDEF whose body contains a role string literal compared against `org_members.role`. The executor uses the &lt;interfaces&gt; migration list as the starting set; for each one, re-emit the function body verbatim from the source file, substituting:
      'admin'  → 'owner'
      'staff'  → 'clinician'
      'viewer' → 'staff'
      role in ('admin','staff') → role in ('owner','clinician')
      v_role &lt;&gt; 'admin' → v_role &lt;&gt; 'owner'
    Preserve `security definer`, the existing `set search_path = pg_catalog, public, extensions` line, and the volatility/language declarations exactly. Do NOT change function signatures (return type, parameter list, parameter names).

    Step 3 — Rename `_is_org_admin → _is_org_owner` (per D-13). Strategy: CREATE OR REPLACE the function with the new name `_is_org_owner` containing the same body (with 'admin'→'owner' literal swap), then DROP the old `_is_org_admin(uuid, uuid)` function. In the SAME migration, re-create the two known callers (`20270601200003_org_patient_invites.sql` and `20270601300008_p30_fix_clinician_alerts_rls.sql` — verify via the `_is_org_admin` grep results in &lt;interfaces&gt;) with the new function name.

    Step 4 — DROP + recreate every RLS policy whose `using` / `with check` clause references a role string literal. For each such policy: `alter table ... disable row level security; drop policy if exists "..." on ...; create policy "..." ...; alter table ... enable row level security;` (single statement per policy, wrapped in the same transaction).

    Step 5 — At the END of the migration, add post-flight assertions that will RAISE if the catalog is in a bad state:
      do $$
      begin
        if exists (select 1 from pg_enum where enumtypid = 'public.org_member_role'::regtype and enumlabel in ('admin','viewer')) then
          raise exception 'P31-00 post-flight FAIL: enum still contains admin/viewer';
        end if;
        if not exists (select 1 from pg_proc where proname = '_is_org_owner' and pronamespace = 'public'::regnamespace) then
          raise exception 'P31-00 post-flight FAIL: _is_org_owner not created';
        end if;
        if exists (select 1 from pg_proc where proname = '_is_org_admin' and pronamespace = 'public'::regnamespace) then
          raise exception 'P31-00 post-flight FAIL: _is_org_admin not dropped';
        end if;
      end $$;

    Idempotency: the rename guard (`if exists`) covers re-running. The CREATE OR REPLACE statements are idempotent by construction. The DROP/CREATE of policies uses `drop policy if exists` so re-runs are safe. The `drop function _is_org_admin` should use `drop function if exists public._is_org_admin(uuid, uuid);` form.

    DO NOT split this into multiple migration files. DO NOT use `drop type ... cascade`. DO NOT skip any SECDEF body identified in &lt;interfaces&gt; — silently-broken role checks are the entire reason this plan exists (RESEARCH Pitfall 1).
  </action>
  <verify>
    <automated>grep -E "^alter type public\.org_member_role rename value 'admin' to 'owner'" supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql &amp;&amp; grep -E "rename value 'staff' to 'clinician'" supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql &amp;&amp; grep -E "rename value 'viewer' to 'staff'" supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql &amp;&amp; grep -E "create or replace function public\._is_org_owner" supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql &amp;&amp; grep -E "drop function if exists public\._is_org_admin" supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql &amp;&amp; grep -E "P31-00 post-flight FAIL" supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql</automated>
  </verify>
  <done>Migration file exists at the exact path; opens with `begin;` and ends with `commit;`; contains all three rename statements in the D-01 order; CREATE OR REPLACE of every SECDEF identified in the &lt;interfaces&gt; migration list (no body still containing a comparison against `'admin'` / `'viewer'` for an org_member_role column); `_is_org_admin` is dropped and `_is_org_owner` exists; post-flight do-block raises on any catalog drift; running `npx supabase db diff --linked` (offline-safe equivalent: `cat` the file and dry-eyeball-confirm balanced `begin/commit`) shows no syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Flip TypeScript role vocabulary + RLS fixtures + client role-literal usage</name>
  <files>src/types/org.ts, src/lib/org.ts, src/components/clinic/settings/ClinicSettingsPage.tsx, src/lib/__tests__/_fixtures/p28-rls-fixture.ts, src/lib/__tests__/org.test.ts, src/lib/__tests__/store-org-slice.test.ts, src/lib/__tests__/rls-org-members.test.ts, src/lib/__tests__/rls-org-invites.test.ts, src/lib/__tests__/with-org-scope.test.ts</files>
  <read_first>
    - src/types/org.ts (full file — 12 lines)
    - src/lib/org.ts (current ROLE_PERMISSIONS — lines 30–50; surfaceCheck — lines 86–90)
    - src/components/clinic/settings/ClinicSettingsPage.tsx (lines 90–110 — `isAdmin` usage)
    - src/lib/__tests__/_fixtures/p28-rls-fixture.ts (lines 185–200 — org_members insert role)
    - Each test file listed in &lt;files&gt; — read in full to locate every role-literal occurrence.
  </read_first>
  <action>
    Edit-level changes ONLY (no behavior changes beyond vocabulary):

    src/types/org.ts:
      Change line 12 from `export type OrgRole = 'admin' | 'staff' | 'viewer';`
      to                     `export type OrgRole = 'owner' | 'clinician' | 'staff';`
      Update the JSDoc on the same export to read `/** The three roles available to org members (per CONTEXT D-01 — renamed in Phase 31). */`.

    src/lib/org.ts:
      In the `ROLE_PERMISSIONS` const (line 36), rename the keys in place: `admin` → `owner`, `staff` → `clinician`, `viewer` → `staff`. KEEP the existing value sets verbatim — do NOT add or remove permissions in this plan. (Plan 31-01 expands the matrix to 12 keys; doing it here would conflate two independent changes and pre-empt 31-01's sync-test contract.)
      Update the comment block above the const (lines 30–34) to reference D-01 + D-03 and note "value sets remain 6-permission shape until Plan 31-01 expands them per D-03".

    src/components/clinic/settings/ClinicSettingsPage.tsx:
      Line 99: change `const isAdmin = currentOrgRole === 'admin';` to `const isOwner = currentOrgRole === 'owner';`.
      Line 102: change `'org_role.admin': isAdmin,` to `'org_role.admin': isOwner,` (KEEP the permission key `'org_role.admin'` — it is the NAV-permission map key consumed by the tab strip and is plan-31-05 owned; renaming it here would break the NAV; only the local boolean identifier flips).
      If any other reference to `isAdmin` exists in the same file (use Grep within the file), update it consistently.

    src/lib/__tests__/_fixtures/p28-rls-fixture.ts:
      Lines 189, 196 (the two `role: 'admin'` literals seeding org_members): change both to `role: 'owner'`. The two users seeded by `createTwoOrgsTwoUsers` are now created as OWNERS of their respective orgs (which is what the existing tests intended — they were "admins" pre-rename).

    src/lib/__tests__/org.test.ts:
      Every `setCurrentOrg(orgFixture, 'admin')` → `setCurrentOrg(orgFixture, 'owner')`.
      Every `setCurrentOrg(orgFixture, 'staff')` → `setCurrentOrg(orgFixture, 'clinician')`.
      Every `setCurrentOrg(orgFixture, 'viewer')` → `setCurrentOrg(orgFixture, 'staff')`.
      Every `expect(...).toBe('admin')` (where the value is a role) → `'owner'`. Apply the same mapping to `'staff'`/`'viewer'` role assertions.

    src/lib/__tests__/store-org-slice.test.ts:
      Apply the same role mapping (`'admin'` → `'owner'`, `'staff'` → `'clinician'`, `'viewer'` → `'staff'`) to every role literal. The `expect(state.currentOrgRole).toBe&lt;OrgRole&gt;('admin')` becomes `toBe&lt;OrgRole&gt;('owner')`.

    src/lib/__tests__/rls-org-members.test.ts:
      Line 60 `role: 'admin'` (seed) → `role: 'owner'`.
      Line 70 `.update({ role: 'viewer' })` → `.update({ role: 'staff' })` (the test is asserting that a non-owner cannot demote a member; the new value is the renamed equivalent).
      Line 80 `expect(rowData?.role).toBe('admin')` → `toBe('owner')`.

    src/lib/__tests__/rls-org-invites.test.ts:
      Lines 35, 61 `invited_role: 'staff'` → `invited_role: 'clinician'`.
      Lines 85, 97 `p_role: 'viewer'` → `p_role: 'staff'`.

    src/lib/__tests__/with-org-scope.test.ts:
      Line 232 `p_role: 'staff'` → `p_role: 'clinician'`.

    Do NOT touch src/lib/clinic.ts:434 (`'admin'` there is a reserved-URL-slug, not a role).
    Do NOT touch any reference to the unrelated `admin_role` (Phase 24 app-admin) enum or `is_admin_at_least(...)` calls.
    Do NOT pre-emptively add the 6 new permission keys from D-03 (those land in Plan 31-01).
    Do NOT add new tests in this plan — the per-action SECDEF tests + role-matrix-sync test belong to Plan 31-01.

    The grep gate in Task 3 enforces the negative invariant: no `OrgRole`-typed comparison anywhere in src/ may still equal `'admin'` / `'viewer'` after this plan.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | tee /tmp/p31-00-tsc.log; ! grep -E "error TS" /tmp/p31-00-tsc.log</automated>
  </verify>
  <done>`OrgRole = 'owner' | 'clinician' | 'staff'` in src/types/org.ts; ROLE_PERMISSIONS keys in src/lib/org.ts are exactly `owner`, `clinician`, `staff` with original value sets intact; ClinicSettingsPage.tsx compiles with the renamed local boolean; all P28 RLS test fixtures + tests reference the new role names; `tsc -p tsconfig.app.json --noEmit` reports zero TypeScript errors; `npm run vitest -- run src/lib/__tests__/org.test.ts src/lib/__tests__/store-org-slice.test.ts` passes (these are unit tests that do not hit the DB).</done>
</task>

<task type="auto">
  <name>Task 3: Grep-gate the negative invariant + push the migration + verify against live catalog</name>
  <files>(verification only — no file writes)</files>
  <read_first>
    - reference_supabase_db_query_linked memory note
    - reference_supabase_migration_filename_regex memory note (Skipping-line grep pattern)
    - reference_grep_gate_comment_strip memory note (strip SQL/TS comments before grepping role literals)
  </read_first>
  <action>
    Step A — local grep gate (comment-stripped per reference_grep_gate_comment_strip so doc comments do not trigger false BLOCKERs):

      # SQL: strip leading `--` line comments
      grep -rn --include='*.ts' --include='*.tsx' -E "OrgRole.*['\"](admin|viewer)['\"]|['\"](admin|viewer)['\"].*OrgRole|currentOrgRole === ['\"](admin|viewer)['\"]" src/ | grep -v '^\s*//' | grep -v '^\s*\*' || echo "PASS: no OrgRole-typed comparison vs admin/viewer in src/"
      # Same scan on the new migration file MUST find ONLY the rename statements:
      grep -E "'admin'|'viewer'" supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql | grep -v "^\s*--" | grep -v "rename value 'admin'" | grep -v "rename value 'viewer'" | grep -v "enumlabel = 'admin'" | grep -v "enumlabel = 'viewer'" | grep -v "enumlabel in ('admin','viewer')" || echo "PASS: no stray admin/viewer literals outside the rename statements"

    Step B — confirm the migration filename is NOT silently skipped (per reference_supabase_migration_filename_regex):
      ls -1 supabase/migrations/20260518070000_*.sql  # exactly one match
      # filename must match ^[0-9]{14}_[a-z0-9_]+\.sql$ — verify:
      basename=$(basename supabase/migrations/20260518070000_p31_00_enum_rename_and_secdef_ripple.sql)
      [[ "$basename" =~ ^[0-9]{14}_[a-z0-9_]+\.sql$ ]] || { echo "FAIL: filename pattern"; exit 1; }

    Step C — push to live Supabase (this is a HUMAN/CHECKPOINT-grade action via CLI; we own it inline because no interactive auth is needed — the worktree must have `.temp/` per project_worktree_supabase_cli memory):
      # Pre-flight: copy CLI state if running from a worktree
      test -d supabase/.temp || { echo "WARN: supabase CLI state missing; copying from main checkout"; cp -R ../leanshot/supabase/.temp supabase/.temp 2>/dev/null || true; }
      # Push
      npx supabase db push --linked 2>&amp;1 | tee /tmp/p31-00-push.log
      # Per reference_supabase_migration_filename_regex — fail loud on silent-skip
      ! grep -E "^Skipping" /tmp/p31-00-push.log

    Step D — live-catalog verification (per reference_supabase_db_query_linked):
      npx supabase db query --linked --csv "select enumlabel from pg_enum where enumtypid = 'public.org_member_role'::regtype order by enumsortorder;" | tee /tmp/p31-00-enum.csv
      # MUST contain exactly: owner / clinician / staff (no admin, no viewer)
      grep -q '^owner$' /tmp/p31-00-enum.csv &amp;&amp; grep -q '^clinician$' /tmp/p31-00-enum.csv &amp;&amp; grep -q '^staff$' /tmp/p31-00-enum.csv &amp;&amp; ! grep -qE '^(admin|viewer)$' /tmp/p31-00-enum.csv

      npx supabase db query --linked --csv "select proname from pg_proc where pronamespace = 'public'::regnamespace and proname in ('_is_org_owner','_is_org_admin') order by proname;" | tee /tmp/p31-00-fns.csv
      # MUST contain _is_org_owner and NOT _is_org_admin
      grep -q '^_is_org_owner$' /tmp/p31-00-fns.csv &amp;&amp; ! grep -q '^_is_org_admin$' /tmp/p31-00-fns.csv

    Step E — re-run the P28 RLS cross-tenant proof tests to confirm the fixture role flip + the renamed SECDEFs work end-to-end against the live DB:
      npm run vitest -- run src/lib/__tests__/rls-org-members.test.ts src/lib/__tests__/rls-org-invites.test.ts

    If any step fails: do NOT attempt to "fix forward" by writing a follow-up migration. Roll back by editing the original migration file, adding compensating `drop function if exists` / `alter type ... rename value` statements at the head of the file (as guarded `if exists` blocks), and re-running. The plan is idempotent by construction.
  </action>
  <verify>
    <automated>bash -c "grep -rn --include='*.ts' --include='*.tsx' -E \"currentOrgRole === ['\\\"](admin|viewer)['\\\"]\" src/ | grep -v '^[[:space:]]*//' | grep -v '^[[:space:]]*\\*' &gt; /tmp/p31-00-stale-role.txt; test ! -s /tmp/p31-00-stale-role.txt"</automated>
  </verify>
  <done>Grep gate confirms zero stale `OrgRole === 'admin'|'viewer'` comparisons in src/ (comments excluded); migration filename matches `^[0-9]{14}_[a-z0-9_]+\.sql$`; `supabase db push --linked` succeeds with no `^Skipping` lines in the log; live enum values are exactly {owner, clinician, staff}; `_is_org_owner` exists and `_is_org_admin` does not; the two P28 RLS proof tests run green against the live DB with the renamed fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → live Postgres catalog | DDL crosses from versioned source into the live multi-tenant database; mid-state during the rename + ripple window is a security-critical surface. |
| TypeScript source → bundle → browser | Renamed `OrgRole` type is a compile-time-only artifact; consumer files that miss the rename will fail typecheck (no runtime untrusted-input boundary crossed by this plan). |
| Test fixture (admin client, service-role key) → live DB | Fixtures seed users + orgs with the renamed role; failure mode is test-only (no production data crossed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-00-01 | Tampering | Postgres catalog (`public.org_member_role` + every dependent SECDEF body) | mitigate | Single atomic `begin; ... commit;` transaction wraps RENAME VALUE x3 + CREATE OR REPLACE of every dependent SECDEF + the policy DROP/CREATE pairs. Post-flight `do $$ ... raise exception ...` block aborts the transaction (rolling back the rename) if the catalog ends up in a half-renamed state. RESEARCH Finding 1 confirms `RENAME VALUE` is catalog-level and does NOT trigger the 55P04 "unsafe new value" restriction, so SECDEFs CAN be re-created in the same transaction. |
| T-31-00-02 | Elevation of Privilege (functional break, not security break) | Un-rippled SECDEF body comparing `role = 'admin'` after the enum has been renamed | mitigate | The grep gate in Task 1 + the explicit list of migration files in `<interfaces>` + the post-flight DDL assertions guarantee no SECDEF is missed. Effect of the bug (had it shipped): owner-check SECDEFs would silently return false → all owner-gated actions denied → org admins locked out of their own settings (denial-of-service to a legitimate owner; NOT a privilege-escalation vector because the false case is restrictive, not permissive). The DENIAL semantics are why this is functional-break-not-security-break — but mitigation is still mandatory because the loss-of-administrability blocks downstream P31 plans. |
| T-31-00-03 | Repudiation | `audit_logs` writes during the migration window | accept | `log_admin_action` (Phase 24) writes during the migration transaction would block on the AccessExclusiveLock the ALTER TYPE takes — they queue rather than fail-and-lose-data. Migration window is &lt;1s on the current catalog size (small org_member_role footprint). No special handling needed beyond running the push during low-traffic window. Documented as accepted because (a) the lock duration is bounded, (b) writes block-then-succeed rather than dropping, and (c) admin actions during the same second as a P31-00 schema change are vanishingly rare. |
| T-31-00-04 | Information Disclosure | RLS-test fixture role flip (`'admin'` → `'owner'`) | mitigate | The fixture seeds test users with the renamed role. If the fixture were NOT flipped, `createTwoOrgsTwoUsers` would fail at insert-time because `'admin'` is no longer a valid `org_member_role` enum value (Postgres would raise `invalid input value for enum org_member_role: "admin"`). This fail-loud behavior is the right side of the leak vs availability trade — the test crashes rather than silently seeding a row with a wrong-role value that would then mask a real RLS bug. |
</threat_model>

<verification>
After all three tasks complete:

1. Grep gate (TS): no `currentOrgRole === 'admin'` or `=== 'viewer'` in src/ (comments stripped per reference_grep_gate_comment_strip).
2. Type check: `npx tsc -p tsconfig.app.json --noEmit` is clean.
3. Migration filename: matches `^[0-9]{14}_[a-z0-9_]+\.sql$` and is the SOLE 20260518070000_* file in supabase/migrations/.
4. Push log: `supabase db push --linked` exit 0, no `^Skipping` lines.
5. Live enum: `select enumlabel from pg_enum where enumtypid = 'public.org_member_role'::regtype` returns exactly {owner, clinician, staff}.
6. Live functions: `_is_org_owner` exists, `_is_org_admin` does not.
7. RLS tests: `npm run vitest -- run src/lib/__tests__/rls-org-members.test.ts src/lib/__tests__/rls-org-invites.test.ts` passes against the renamed catalog.
8. Per project_worktree_supabase_cli: if this plan runs in a worktree, the migration file is also visible from the main checkout (orchestrator cleanup before merge).
</verification>

<success_criteria>
- [ ] `public.org_member_role` enum on the LIVE Supabase project (ref ytnsipxxmzgaebkqmokp) contains exactly the values `owner`, `clinician`, `staff` — no `admin`, no `viewer`.
- [ ] `public._is_org_owner(p_org_id uuid, p_user_id uuid)` SECDEF exists; `public._is_org_admin` no longer exists.
- [ ] Every SECDEF function in /Users/karstenhaldan/minisite/supabase/migrations/ whose body previously compared `role = 'admin'` / `'staff'` / `'viewer'` (against an `org_members.role` column or an `org_member_role`-typed variable) has been re-created in the new migration with the renamed literals; live `pg_proc.prosrc` for each function reflects the new spellings.
- [ ] `src/types/org.ts` exports `OrgRole = 'owner' | 'clinician' | 'staff'`.
- [ ] `src/lib/org.ts` `ROLE_PERMISSIONS` is keyed by `owner` / `clinician` / `staff` (value sets unchanged from pre-plan shape — 31-01 expands).
- [ ] `src/components/clinic/settings/ClinicSettingsPage.tsx` no longer compares `currentOrgRole === 'admin'`; the NAV permission map key `'org_role.admin'` is preserved verbatim (changed in 31-05 not here).
- [ ] All P28 RLS fixtures + tests use the renamed role names and pass under `npm run vitest`.
- [ ] `npx tsc -p tsconfig.app.json --noEmit` is clean.
- [ ] CI grep gate passes: no `OrgRole`-typed equality against `'admin'` or `'viewer'` in src/ (comments excluded).
- [ ] Plan-checker BLOCKER is now armed for downstream P31 plans: any new file in `.planning/phases/31-*/` containing literal `'admin'` (single-quoted, role-context) inside a code block flags as FAIL.
</success_criteria>

<output>
After completion, create `.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-00-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md with:
  - migration filename + SHA shipped
  - exact list of SECDEF functions re-created (function name + signature)
  - list of policies dropped/recreated
  - list of TS files changed + line-count delta
  - the live-catalog verification CSV outputs (enum values + function names)
  - any RLS test failures encountered + how they were resolved
  - confirmation that the grep gate is green
  - explicit handoff note to Plan 31-01: "ROLE_PERMISSIONS keys are now {owner, clinician, staff} with the original 6-permission value sets; 31-01 owns the expansion to 12 permission keys per D-03."
</output>
