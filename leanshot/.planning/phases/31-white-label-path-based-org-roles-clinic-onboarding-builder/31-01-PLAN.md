---
phase: 31-white-label-path-based-org-roles-clinic-onboarding-builder
plan: 01
type: execute
wave: 1
depends_on: [31-00]
files_modified:
  - scripts/assert-clinic-bundle-budget.sh
  - supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql
  - src/lib/org.ts
  - src/lib/__tests__/role-matrix-sync.test.ts
autonomous: true
requirements: [ORG-12]
tags: [auth, rbac, secdef, postgres, bundle-ceiling]

must_haves:
  truths:
    - "DB function public.has_permission(p_role org_member_role, p_perm text) returns boolean exists and resolves the 12-key permission matrix per D-03 (owner=ALL 12; clinician=members.list+roster.view+alerts.ack+alerts.snooze; staff=members.list+roster.view)."
    - "DB function public.get_caller_role(p_org_id uuid) returns org_member_role exists, returns auth.uid()'s role for the given org, returns null when caller is not a member."
    - "TS ROLE_PERMISSIONS const in src/lib/org.ts has exactly 3 role keys (owner, clinician, staff — post-31-00 rename) and exactly 12 distinct permission keys distributed per D-03; surfaceCheck() consumes the expanded matrix without behavior regression for previously-supported keys."
    - "role-matrix-sync.test.ts queries DB has_permission for all 36 (role, perm) pairs and asserts equality vs TS ROLE_PERMISSIONS[role].has(perm); test fails the suite if any pair drifts."
    - "scripts/assert-clinic-bundle-budget.sh CLINIC_CEILING is raised from 36000 to 50000 bytes before any Wave 2 plan can build — atomic standalone commit. **Rationale for 50000 over RESEARCH's recommended 48000:** the +12 kB Wave-2 estimate has ±2 kB variance (RESEARCH Finding 10 MEDIUM-confidence); 50000 gives ~3.5 kB headroom vs 48000's ~1.5 kB. Phase 32 (Spanish i18n) lands soon and will add lazy-loaded translation tables; setting 50000 once is cheaper than chasing raises every phase. Reverting to 48000 is a one-line bash change."
  artifacts:
    - path: "scripts/assert-clinic-bundle-budget.sh"
      provides: "Raised CLINIC_CEILING=50000; existing dnd-kit-not-in-index guard at line ~417 unchanged."
      contains: "CLINIC_CEILING=50000"
    - path: "supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql"
      provides: "has_permission() + get_caller_role() SECURITY DEFINER functions with explicit search_path; GRANT EXECUTE to authenticated."
      contains: "create or replace function public.has_permission"
    - path: "src/lib/org.ts"
      provides: "Expanded ROLE_PERMISSIONS const (3 roles × 12 perms total surface; per-role allocation per D-03) consumed by surfaceCheck()."
      contains: "ROLE_PERMISSIONS"
    - path: "src/lib/__tests__/role-matrix-sync.test.ts"
      provides: "Vitest sync test asserting DB↔TS matrix equality across 36 (role, perm) pairs; uses SHOULD_RUN env-gate from p28-rls-fixture; SKIPs gracefully without SUPABASE env."
      contains: "has_permission"
  key_links:
    - from: "src/lib/org.ts surfaceCheck()"
      to: "ROLE_PERMISSIONS const (12 keys)"
      via: "Set.has lookup"
      pattern: "ROLE_PERMISSIONS\\[role\\]\\.has"
    - from: "role-matrix-sync.test.ts"
      to: "supabase RPC has_permission"
      via: "admin client .rpc('has_permission', { p_role, p_perm })"
      pattern: "\\.rpc\\(['\"]has_permission['\"]"
    - from: "Downstream P31 admin SECDEFs (save_org_branding, save_org_onboarding_flow, change_member_role)"
      to: "has_permission(get_caller_role(p_org_id), '<PERM>')"
      via: "first-line guard in each SECDEF body (CALLERS in plans 31-02 / 31-04 / 31-05, not modified here)"
      pattern: "has_permission\\(get_caller_role"
---

<objective>
Land the security floor for the entire 12-key role matrix: a SECURITY DEFINER `has_permission(role, perm)` function in Postgres (per D-02 source-of-truth rule), a `get_caller_role(p_org_id)` helper that downstream P31 SECDEFs consume, the expanded 12-key TS `ROLE_PERMISSIONS` const in `src/lib/org.ts` (replacing the Phase 28 6-key skeleton, per D-03), and a vitest sync test that asserts DB↔TS equality across all 36 (role, perm) pairs. Also raise `CLINIC_CEILING=36000→50000` in `scripts/assert-clinic-bundle-budget.sh` as the first task — must merge before any Wave 2 build runs CI, per RESEARCH Finding 10 and the outline's Wave 1 dispatch ordering.

Purpose: ORG-12 hinges on a single, drift-proof source of truth for role capability. The DB function is the floor; the TS const is the UX-hint mirror; the sync test prevents one side from drifting without the other (per D-02). All later P31 admin-mutation SECDEFs (`save_org_branding` in 31-02, `save_org_onboarding_flow` + `activate_onboarding_flow_version` + `mark_onboarding_complete` in 31-04, `change_member_role` in 31-05) call `has_permission(get_caller_role(p_org_id), '<perm>')` as their first-line guard — both functions ship here so those plans can land without forward-referencing missing dependencies.

Output: 1 migration (2 new SECDEFs + GRANTs), 1 TS const expansion (3 roles × Set-of-permissions with 12 distinct keys), 1 vitest sync test (36 assertions), 1 bash script edit (CLINIC_CEILING raise).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-CONTEXT.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-RESEARCH.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-PLAN-OUTLINE.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-EXTENSION-CONTRACT.md
@src/lib/org.ts
@src/lib/__tests__/_fixtures/p28-rls-fixture.ts
@src/lib/__tests__/clinician-alert-debounce.test.ts
@scripts/assert-clinic-bundle-budget.sh

<interfaces>
<!-- Extracted from src/lib/org.ts: current Phase 28 6-key matrix to be REPLACED by 12-key per D-03 -->
<!-- Executor: do NOT explore further; this is the full surface that mutates. -->

Current `ROLE_PERMISSIONS` shape (Phase 28 skeleton — REPLACE per D-03):
```
const ROLE_PERMISSIONS: Record<OrgRole, ReadonlySet<string>> = {
  admin:  Set(['members.invite','members.revoke','members.list','settings.edit','branding.edit','patients.link']),
  staff:  Set(['members.list','patients.link']),
  viewer: Set(['members.list']),
};
```

After 31-00 enum rename, `OrgRole = 'owner' | 'clinician' | 'staff'` (was `'admin' | 'staff' | 'viewer'`). 31-00 already updates the keys to the new role names (mechanical rename). THIS plan replaces the value-sets with the full 12-key allocation.

`surfaceCheck(permission: string): boolean` is unchanged — consumes the new matrix via `ROLE_PERMISSIONS[role].has(permission)`. The function body does not need editing; only the const it reads from grows.

Test-only export `_ROLE_PERMISSIONS_FOR_TEST = ROLE_PERMISSIONS` exists at the bottom of org.ts — the new test imports this symbol (preferred over re-exporting the const at the public surface).

From src/lib/__tests__/_fixtures/p28-rls-fixture.ts (LIVE — DO NOT modify):
- `SHOULD_RUN: boolean` — env-gate (`SUPABASE_URL && SUPABASE_SERVICE_ROLE_KEY` present)
- `getAdmin(): SupabaseClient` — service-role client for SECDEF .rpc() calls
- `makeSlugPrefix(filename: string): string` — file-scoped prefix (NOT NEEDED for matrix-sync test; no org rows created)
- `cleanupByPrefix(prefix: string): Promise<void>` — NOT NEEDED for matrix-sync test

Test pattern reference (clinician-alert-debounce.test.ts):
```
const describeIfLive = SHOULD_RUN ? describe : describe.skip;
describeIfLive('feature', () => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise CLINIC_CEILING 36000 → 50000 in bundle-budget guard (standalone atomic commit)</name>
  <files>scripts/assert-clinic-bundle-budget.sh</files>
  <action>
  Edit `scripts/assert-clinic-bundle-budget.sh` line 132 (current value `CLINIC_CEILING=36000`) to `CLINIC_CEILING=50000`. Per D-03 + RESEARCH Finding 10 the clinic chunk grows ~12.5 kB gz in Phase 31 (BrandingTab + OnboardingTab + RoleEditorModal expansion lands in Wave 2 plan 31-05); projected ~48.5 kB gz with ~1.5 kB headroom. Append to the existing history-comment block at lines 120-131 a new comment block: `# CLINIC_CEILING=50000 — Phase 31 Plan 31-01 baseline raise (D-03 + RESEARCH Finding 10). Phase 31 adds BrandingTab (~5 kB) + OnboardingTab (~6 kB) + RoleEditorModal 12-key matrix expansion (~1.5 kB) lazy-loaded into the clinic chunk via existing manualChunks routing; SortableTreePanel extraction in 31-00b is net-zero (page-builder chunk loses 1.5 kB while clinic gains 1.5 kB). Raised here (Wave 1) — not in Wave 2 plan 31-05 that actually adds the weight — so the ceiling lands BEFORE any Wave 2 CI run hits the bundle gate. History: 12→16→17→17→25→22→28→30→35→36→50 kB.` Update the project memory tagline at the comment to point to `PHASE_31_REF=".planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-01-PLAN.md"` IF that constant exists in the file; otherwise just add a new constant line `PHASE_31_REF="..."` next to `PHASE_REF` / `PHASE_10_REF` / `PHASE_12_REF` near line 188-190. Do NOT modify the per-glob check_chunk_ceiling call list — `'clinic'` ceiling consumption is wired automatically by the existing `check_chunk_ceiling 'clinic-*.js' "$CLINIC_CEILING" 'clinic'` call at line 280. Do NOT modify the dnd-kit-not-in-index guard at lines 400-425 (per 31-00b owns that invariant verification). Commit this task SOLO with message `chore(31-01): raise CLINIC_CEILING 36000→50000 for Phase 31 (D-03 / Finding 10)` so the bash edit is trivially revertable if the rest of the plan misbehaves.
  </action>
  <verify>
    <automated>grep -c '^CLINIC_CEILING=50000$' scripts/assert-clinic-bundle-budget.sh | grep -q '^1$' && bash -n scripts/assert-clinic-bundle-budget.sh && echo OK</automated>
  </verify>
  <done>CLINIC_CEILING is 50000 (single exact-match line); shellcheck/`bash -n` parses the script without syntax error; history comment block updated to include the 31-01 entry; committed as a standalone commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Postgres migration — has_permission() + get_caller_role() SECDEFs with 12-key matrix</name>
  <files>supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql</files>
  <behavior>
    - `has_permission('owner', '<any of 12 keys>')` returns true; returns false for unknown key.
    - `has_permission('clinician', 'members.list')`, `('clinician','roster.view')`, `('clinician','alerts.ack')`, `('clinician','alerts.snooze')` all return true; any other key returns false.
    - `has_permission('staff', 'members.list')` and `('staff','roster.view')` return true; any other key returns false.
    - `has_permission(NULL, 'anything')` returns false (defensive — NULL caller role from get_caller_role when not a member must NOT grant).
    - `get_caller_role(<org_id where caller is owner>)` returns `'owner'::org_member_role`.
    - `get_caller_role(<org_id where caller is NOT a member>)` returns NULL.
    - `get_caller_role` body uses `auth.uid()` (does NOT accept a caller param) — security floor: a SECDEF that takes a user_id arg could be tricked.
    - Both functions execute as SECURITY DEFINER with `set search_path = pg_catalog, public, extensions` (per `[[reference_supabase_migration_gotchas]]`).
    - Both functions: `GRANT EXECUTE ... TO authenticated, service_role`. `has_permission` may also be granted to `anon` for completeness (returns false for any input when caller has no role — does not leak; researcher confirmed); err on the side of NOT granting to anon — keep authenticated+service_role only since no client code path needs anon-role access.
    - Migration body uses `create or replace function` so re-runs are idempotent on local fixtures (downgrade path: `drop function` in a reverse migration if ever needed; not required here).
  </behavior>
  <action>
  Create migration file `supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql` (14-digit timestamp `20270601310101` — phase-31 / plan-01 / step-01 — fits the strict regex per `[[reference_supabase_migration_filename_regex]]`; ensure NO letter suffix). Migration contents:

  **(a) `public.has_permission(p_role public.org_member_role, p_perm text) returns boolean`** — SECURITY DEFINER, language sql, IMMUTABLE (pure function over inputs; no relation reads), `set search_path = pg_catalog, public, extensions`. Body returns `case p_role when 'owner' then p_perm = ANY(ARRAY['members.invite','members.revoke','members.list','members.role.edit','settings.edit','branding.edit','onboarding.edit','roster.view','roster.thresholds.edit','alerts.ack','alerts.snooze','billing.view']) when 'clinician' then p_perm = ANY(ARRAY['members.list','roster.view','alerts.ack','alerts.snooze']) when 'staff' then p_perm = ANY(ARRAY['members.list','roster.view']) else false end`. The 12 keys are verbatim per D-03; the 'else false' branch handles NULL role and any future enum value not yet allocated.

  **(b) `public.get_caller_role(p_org_id uuid) returns public.org_member_role`** — SECURITY DEFINER, language sql, STABLE (reads `org_members` table; not IMMUTABLE), `set search_path = pg_catalog, public, extensions`. Body: `select role from public.org_members where org_id = p_org_id and user_id = auth.uid() limit 1` (returns null if no row).

  **(c) GRANTs:** `revoke all on function public.has_permission(public.org_member_role, text) from public;` then `grant execute on function public.has_permission(public.org_member_role, text) to authenticated, service_role;`. Same revoke/grant pair for `get_caller_role(uuid)`. Do NOT grant to anon (no caller path needs it; reduces blast radius).

  **(d) Comment block at file top** stating purpose, the D-02/D-03 source-of-truth contract, the 12-key list, and a NOTE that any future permission addition MUST land in BOTH this CASE expression AND `src/lib/org.ts` ROLE_PERMISSIONS — drift is caught by `src/lib/__tests__/role-matrix-sync.test.ts` (created in Task 4). Reference the `[[feedback_planner_iter1_anti_patterns]]` rule about defensive contracts.

  **Postgres DDL transaction safety per `[[feedback_planner_iter1_anti_patterns]]`:** both `create or replace function` statements implicitly run inside the migration's surrounding transaction — no explicit BEGIN/COMMIT needed. Migration is single-file, atomic.

  **Do NOT push to live DB in this task.** The BLOCKING `supabase db push --linked` is owned by Plan 31-04 per outline §"Schema-push BLOCKING task ownership". This migration sits as a pending file on the agent branch until 31-04's final task pushes the batch.

  **Anti-pattern guard:** do NOT add the `'admin'`/`'staff'`/`'viewer'` legacy string literals anywhere in this file (per 31-00 BLOCKER rule). Only the new vocabulary `'owner'`/`'clinician'`/`'staff'` may appear.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql &&
      grep -c 'create or replace function public.has_permission' supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql | grep -q '^1$' &&
      grep -c 'create or replace function public.get_caller_role' supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql | grep -q '^1$' &&
      grep -c 'security definer' supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql | awk '$1>=2{exit 0} {exit 1}' &&
      grep -cE "set search_path *= *pg_catalog, public, extensions" supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql | awk '$1>=2{exit 0} {exit 1}' &&
      ! grep -vE "^[[:space:]]*(--|/\*|\*)" supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql | grep -qE "'(admin|viewer)'" &&
      grep -c 'billing.view' supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql | grep -q '^1$' &&
      echo OK</automated>
  </verify>
  <done>Migration file exists at the exact 14-digit-timestamp path; both SECDEFs declared with SECURITY DEFINER + explicit search_path (≥2 occurrences each); no legacy `'admin'`/`'viewer'` string literals appear outside comments (grep skips comment lines starting with `--` / `/*` / `*`); all 12 permission keys present including the canary `billing.view`; revoke + grant statements present; commit ready (held on branch — push owned by 31-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand src/lib/org.ts ROLE_PERMISSIONS const to 12 keys per D-03</name>
  <files>src/lib/org.ts</files>
  <behavior>
    - `ROLE_PERMISSIONS.owner.size === 12`, with exactly these keys: `members.invite, members.revoke, members.list, members.role.edit, settings.edit, branding.edit, onboarding.edit, roster.view, roster.thresholds.edit, alerts.ack, alerts.snooze, billing.view`.
    - `ROLE_PERMISSIONS.clinician.size === 4`, with keys: `members.list, roster.view, alerts.ack, alerts.snooze`.
    - `ROLE_PERMISSIONS.staff.size === 2`, with keys: `members.list, roster.view`.
    - `surfaceCheck('billing.view')` returns true iff caller is owner; returns false for clinician + staff + null roles.
    - `surfaceCheck('roster.view')` returns true for all three roles (owner ∪ clinician ∪ staff).
    - `surfaceCheck('alerts.snooze')` returns true for owner + clinician, false for staff + null.
    - `surfaceCheck('unknown.perm')` returns false for every role (Set.has lookup; typo-safe).
    - Type alias `OrgRole = 'owner' | 'clinician' | 'staff'` is preserved (31-00 already updated it; do not redefine here).
    - Test-only export `_ROLE_PERMISSIONS_FOR_TEST = ROLE_PERMISSIONS` remains exported (consumed by Task 4's sync test).
  </behavior>
  <action>
  Edit `src/lib/org.ts` lines 30-48 (the `ROLE_PERMISSIONS` declaration block). Replace the entire `const ROLE_PERMISSIONS: Record<OrgRole, ReadonlySet<string>> = { ... };` block (currently 6 keys distributed across `admin`/`staff`/`viewer`) with the 12-key allocation per D-03:

  ```
  const ROLE_PERMISSIONS: Record<OrgRole, ReadonlySet<string>> = {
    owner: new Set([
      'members.invite',
      'members.revoke',
      'members.list',
      'members.role.edit',
      'settings.edit',
      'branding.edit',
      'onboarding.edit',
      'roster.view',
      'roster.thresholds.edit',
      'alerts.ack',
      'alerts.snooze',
      'billing.view',
    ]),
    clinician: new Set([
      'members.list',
      'roster.view',
      'alerts.ack',
      'alerts.snooze',
    ]),
    staff: new Set([
      'members.list',
      'roster.view',
    ]),
  };
  ```

  Update the JSDoc-style comment block at lines 30-34 from "Only 6 permissions are needed at the client layer (UX hints)" to "Full 12-key matrix per Phase 31 D-03; mirrors DB SECDEF `public.has_permission(role, perm)` (Phase 31 Plan 31-01). Sync test at `src/lib/__tests__/role-matrix-sync.test.ts` asserts equality across all 36 (role, perm) pairs — any drift fails CI." Update line 32 comment reference from "(Phase 9)" to "(Phase 31 Plan 31-01)" to point at the correct SECDEF source.

  Do NOT modify: `surfaceCheck()` body (it is a generic `ROLE_PERMISSIONS[role].has(permission)` lookup — gains the new keys automatically), `OrgRole` type import (already renamed by 31-00 to `'owner' | 'clinician' | 'staff'`), `getCurrentOrgId` / `withOrgPath` / `overlayBrandingTokens` / `useCurrentOrg`, the `_ROLE_PERMISSIONS_FOR_TEST` export. Keep these stable so dependents in 31-02/04/05 see the same public surface.

  Anti-pattern guard per outline §31-00: any reference to legacy `'admin'` / `'viewer'` string literals in this file is a BLOCKER. Verify after edit that only `'owner'` / `'clinician'` / `'staff'` appear as role keys in the const, and that ROLE_PERMISSIONS values contain only the 12 new permission keys (no stray legacy `'patients.link'` from Phase 28).

  Per `[[reference_eslint_import_x_path_gotcha]]` and the lint baseline — run `npm run lint -- src/lib/org.ts --fix` after the edit to absorb any import-x/order or formatting drift; the file is small enough that any remaining lint warnings must be hand-fixed.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json &&
      ! grep -nE "'(admin|viewer|patients\.link)'" src/lib/org.ts &&
      grep -cE "'(owner|clinician|staff)':" src/lib/org.ts | awk '$1>=3{exit 0} {exit 1}' &&
      grep -c "'billing.view'" src/lib/org.ts | grep -q '^1$' &&
      grep -c "'onboarding.edit'" src/lib/org.ts | grep -q '^1$' &&
      grep -c "'roster.thresholds.edit'" src/lib/org.ts | grep -q '^1$' &&
      echo OK</automated>
  </verify>
  <done>tsc passes with zero errors; no legacy `'admin'` / `'viewer'` / `'patients.link'` literals remain in org.ts; all three new role keys present; the three canary permissions unique to Phase 31 (`billing.view`, `onboarding.edit`, `roster.thresholds.edit`) each appear exactly once; lint clean on this file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create role-matrix-sync vitest asserting 36-pair DB↔TS equality</name>
  <files>src/lib/__tests__/role-matrix-sync.test.ts</files>
  <behavior>
    - Test SKIPs gracefully when `SHOULD_RUN === false` (no SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY env vars) — uses `describeIfLive = SHOULD_RUN ? describe : describe.skip` pattern.
    - When live: for each role in `['owner','clinician','staff']` (3) × each perm in the union of all values in `_ROLE_PERMISSIONS_FOR_TEST` (12) = 36 (role, perm) pairs, calls `admin.rpc('has_permission', { p_role: role, p_perm: perm })` and asserts the DB-returned boolean equals `_ROLE_PERMISSIONS_FOR_TEST[role].has(perm)`.
    - Drives one `it()` per pair (`it(\`(\${role}, \${perm}) -> \${expected}\`, ...)`) so a single failing pair surfaces in CI output with the exact mismatch.
    - Also includes a single it() asserting that `Object.keys(_ROLE_PERMISSIONS_FOR_TEST).length === 3` and `union of all values = 12 distinct keys` — guards against accidentally dropping a role or a permission from the TS side.
    - Also includes a single it() asserting `admin.rpc('has_permission', { p_role: 'owner', p_perm: 'made.up.perm' })` returns false (defensive — confirms DB CASE expression's `else false` branch).
    - Does NOT create org rows / fixtures (no per-file slug prefix needed; no cleanupByPrefix needed).
    - Does NOT use authenticated client (matrix is pure CASE over inputs; service-role admin client suffices and avoids ES256 GoTrueClient flake per `[[reference_rls_fixture_gotrueclient_flake]]`).
  </behavior>
  <action>
  Create `src/lib/__tests__/role-matrix-sync.test.ts`. Imports:
  - `import { describe, expect, it } from 'vitest';`
  - `import { SHOULD_RUN, getAdmin } from './_fixtures/p28-rls-fixture';`
  - `import { _ROLE_PERMISSIONS_FOR_TEST } from '../org';`

  File header JSDoc: "Phase 31 Plan 31-01 ORG-12 — DB↔TS role permission matrix sync test (D-02 source-of-truth contract). Asserts public.has_permission(role, perm) SECDEF result equals ROLE_PERMISSIONS[role].has(perm) for every (role, perm) pair (36 pairs). Drift in either direction fails the suite — the security floor is the DB function; the TS const is a UX hint mirror that MUST stay in lockstep. See also: 31-CONTEXT.md D-02 / D-03."

  Compute the perm universe once at module scope: `const ALL_PERMS = Array.from(new Set(Object.values(_ROLE_PERMISSIONS_FOR_TEST).flatMap(s => Array.from(s))));` — yields the 12-key union. `const ALL_ROLES = Object.keys(_ROLE_PERMISSIONS_FOR_TEST) as Array<keyof typeof _ROLE_PERMISSIONS_FOR_TEST>;` — yields `['owner','clinician','staff']`.

  Use `const describeIfLive = SHOULD_RUN ? describe : describe.skip;` per the existing pattern (see `clinician-alert-debounce.test.ts` line 27 reference). At top-level: `if (!SHOULD_RUN) console.warn('[role-matrix-sync.test] SKIPPED — SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY env not set.');`.

  Structure:
  ```
  describeIfLive('role matrix DB↔TS sync (Phase 31 D-02)', () => {
    const admin = getAdmin();

    it('TS matrix shape sanity', () => {
      expect(ALL_ROLES.length).toBe(3);
      expect(ALL_PERMS.length).toBe(12);
    });

    it('unknown permission returns false from DB', async () => {
      const { data, error } = await admin.rpc('has_permission', { p_role: 'owner', p_perm: 'made.up.perm' });
      expect(error).toBeNull();
      expect(data).toBe(false);
    });

    for (const role of ALL_ROLES) {
      for (const perm of ALL_PERMS) {
        const expected = _ROLE_PERMISSIONS_FOR_TEST[role].has(perm);
        it(`(${role}, ${perm}) -> ${expected}`, async () => {
          const { data, error } = await admin.rpc('has_permission', { p_role: role, p_perm: perm });
          expect(error).toBeNull();
          expect(data).toBe(expected);
        });
      }
    }
  });
  ```

  Per `[[reference_vitest_skip_fixme]]`: use `describe.skip` (not `describe.fixme` which is not a function in vitest 4.x). Per `[[feedback_defer_then_batch_fix_pattern]]`: do NOT use `test.fixme` / `it.skip` to silence a failing pair — a failing pair is a BLOCKER for the security floor, not a deferrable test.

  Do NOT introduce any per-file slug prefix or `cleanupByPrefix` — the test creates zero rows; the existing `makeSlugPrefix` / `cleanupByPrefix` imports are unused and should NOT appear (avoids lint warnings about unused imports per the strict tsconfig settings `noUnusedLocals` / `noUnusedParameters`).

  After file is written, run the test locally if SUPABASE env is set (best-effort — CI is the gate). If running locally fails because the migration from Task 2 hasn't been pushed yet, this is EXPECTED — the test will pass in CI after Plan 31-04's BLOCKING `supabase db push --linked` lands all P31 migrations. Document in the final task summary that the test is `wave-2-gated` (runs green only after 31-04 push); CI before push will SKIP if env unset or FAIL if env is set with stale DB. The vitest is committed regardless so 31-04 verifies the matrix as part of its push verification.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json &&
      test -f src/lib/__tests__/role-matrix-sync.test.ts &&
      grep -c "describeIfLive = SHOULD_RUN ? describe : describe.skip" src/lib/__tests__/role-matrix-sync.test.ts | grep -q '^1$' &&
      grep -c "admin.rpc('has_permission'" src/lib/__tests__/role-matrix-sync.test.ts | awk '$1>=2{exit 0} {exit 1}' &&
      grep -c "_ROLE_PERMISSIONS_FOR_TEST" src/lib/__tests__/role-matrix-sync.test.ts | awk '$1>=2{exit 0} {exit 1}' &&
      ! grep -qE "'(admin|viewer|patients\.link)'" src/lib/__tests__/role-matrix-sync.test.ts &&
      SUPABASE_URL= SUPABASE_SERVICE_ROLE_KEY= npx vitest run src/lib/__tests__/role-matrix-sync.test.ts 2>&1 | grep -qE 'skipped|0 passed' &&
      echo OK</automated>
  </verify>
  <done>Test file exists; tsc strict passes (no unused imports, no implicit any); SKIP-when-env-unset path works (`vitest run` with env cleared exits 0 with skipped/0-passed status); file uses only the new role/perm vocabulary; ready for CI to exercise the 36 pairs after 31-04 pushes the migration to live DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → SECDEF RPC | Untrusted caller invokes `has_permission` / `get_caller_role` via PostgREST; both functions are pure / read-own-row only — no caller-controlled write path. |
| Server SECDEF body → CASE expression | The 12-key matrix is hard-coded into the function body. A bad CASE branch grants the wrong role wrong permissions. |
| TS `ROLE_PERMISSIONS` ↔ DB `has_permission` | Two independent CASE-vs-Set encodings of the same matrix. Drift = UI hides menu items while server denies (degraded UX), OR worse, UI shows menu items while server denies (still safe — server is floor). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | E (Elevation of Privilege) | `has_permission(role, perm)` CASE expression | mitigate | The CASE returns `false` for any unknown role (NULL caller, future enum value not yet allocated) via the `else false` branch — verified by Task 4 it() asserting `(NULL is implicitly tested via clinician/staff cases) + unknown 'made.up.perm' returns false`. Migration code-review against D-03's literal 12 keys is mandatory pre-merge; the migration file uses ARRAY[] literals (not nested CASE) so deviations are visually obvious. |
| T-31-01-02 | T (Tampering) | TS `ROLE_PERMISSIONS` const vs DB `has_permission` body | mitigate | Task 4's `role-matrix-sync.test.ts` runs every CI build and asserts equality across all 36 pairs. A drift in either side fails the suite. Plan-checker BLOCKER per D-02 if a future plan adds a perm to one side without the other. CI runs the test post-migration-push (gated by SHOULD_RUN env). |
| T-31-01-03 | I (Information Disclosure) | `get_caller_role(p_org_id)` reads `org_members` | mitigate | Function is SECURITY DEFINER but only returns the CALLER's own role (`auth.uid()` filter inside the SELECT). Caller cannot pass `p_user_id` — function has no such parameter. Returns NULL when not a member; downstream SECDEFs interpret NULL as "deny" (the `else false` branch in `has_permission` short-circuits any subsequent call). |
| T-31-01-04 | E (Elevation of Privilege) | `has_permission` exposed to unintended role | mitigate | `revoke all from public` followed by explicit `grant execute to authenticated, service_role` ONLY — `anon` does not receive EXECUTE (no anon code path needs it; reduces blast radius). Verified by reading the migration file — grep `grant execute.*anon` in CI guard would catch a re-introduction (not added in this plan since no such pattern exists yet; future hardening). |
| T-31-01-05 | T (Tampering) | Bundle ceiling raise 36→50 kB enabling silent regressions | accept | Raising the ceiling lifts the floor against which CI compares. Documented in the script's history-comment block + tied to a specific D-03/Finding-10 justification. Wave 2 plan 31-05 must hit ≤50 kB; if it doesn't, the failure surfaces in 31-05's CI run (not silently). Standalone commit makes revert trivial. |
| T-31-01-06 | E (Elevation of Privilege) | Migration filename letter-suffix silently skipped | mitigate | Filename `20270601310101_p31_01_has_permission_secdef.sql` is strict 14-digit prefix + lowercase snake-case suffix per `[[reference_supabase_migration_filename_regex]]`. CI script `grep '^Skipping'` (per project memory) would catch a malformed filename if introduced. Task 2 verify command explicitly checks `test -f` against the exact path. |

</threat_model>

<verification>
After all 4 tasks complete:

1. **Bash script syntax + ceiling value** — `bash -n scripts/assert-clinic-bundle-budget.sh && grep -c '^CLINIC_CEILING=50000$' scripts/assert-clinic-bundle-budget.sh` returns 1.

2. **Migration filename strictness** — `ls supabase/migrations/20270601310101_*.sql` returns exactly one file; filename matches `^[0-9]{14}_[a-z0-9_]+\.sql$`; `supabase db lint` or equivalent local dry-run shows no syntax errors.

3. **TS strict compile** — `npx tsc --noEmit -p tsconfig.app.json` exits 0.

4. **Lint clean on touched files** — `npm run lint -- src/lib/org.ts src/lib/__tests__/role-matrix-sync.test.ts` exits 0.

5. **Legacy literal absence** — `grep -nE "'(admin|viewer|patients\.link)'" src/lib/org.ts supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql src/lib/__tests__/role-matrix-sync.test.ts` returns NO matches (excluding comments).

6. **Test SKIPs without env** — `SUPABASE_URL= SUPABASE_SERVICE_ROLE_KEY= npx vitest run src/lib/__tests__/role-matrix-sync.test.ts` exits 0 with all 38 tests skipped (36 pairs + 2 sanity).

7. **Unit-test smoke** — full `npm run test:unit` exits 0 (sync test SKIPs without env; other tests unaffected since this plan does not touch their files).

8. **Bundle build still passes the old ceiling** — `npm run build && bash scripts/assert-clinic-bundle-budget.sh` exits 0. Current clinic chunk (pre-31-05) measures ~35-36 kB gz; well under the new 50 kB ceiling. Builds verify the script edit didn't break the script.

NOTE: The matrix sync test does NOT run in CI green until Plan 31-04 pushes the migration to live DB (per outline §"Schema-push BLOCKING task ownership"). Pre-push CI runs in SKIP mode (env-gated); post-31-04-push CI runs the live 36-pair assertion. This is intentional — the test is committed in 31-01 so it cannot be "forgotten" later; it gates green at 31-04 push time.

</verification>

<success_criteria>
- [ ] `scripts/assert-clinic-bundle-budget.sh` has `CLINIC_CEILING=50000` (exact-line grep returns 1); history comment block updated with the 31-01 entry.
- [ ] Migration file `supabase/migrations/20270601310101_p31_01_has_permission_secdef.sql` exists with both SECDEFs, explicit `search_path`, RBAC GRANTs to `authenticated, service_role` only.
- [ ] No legacy `'admin'` / `'viewer'` / `'patients.link'` string literals appear outside comments in any of the 4 touched files.
- [ ] All 12 D-03 permission keys present in BOTH the migration CASE expression AND `src/lib/org.ts` `ROLE_PERMISSIONS`; per-role allocation matches D-03 (owner=12, clinician=4, staff=2).
- [ ] `src/lib/__tests__/role-matrix-sync.test.ts` exists; uses `describeIfLive` pattern; SKIPs without SUPABASE env; emits 36 pair-assertions + 2 sanity tests when live.
- [ ] `npx tsc --noEmit -p tsconfig.app.json` exits 0.
- [ ] `npm run lint -- src/lib/org.ts src/lib/__tests__/role-matrix-sync.test.ts` exits 0.
- [ ] `npm run build && bash scripts/assert-clinic-bundle-budget.sh` exits 0 (sanity: ceiling raise didn't break the bash script).
- [ ] All 4 tasks committed (Task 1 as standalone commit per its action directive; Tasks 2-4 may share a commit or be split).
- [ ] Migration NOT pushed to live DB (per outline — Plan 31-04 owns the BLOCKING push).
</success_criteria>

<output>
After completion, create `.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-01-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`. Specifically document:

1. **Artifacts shipped:** 1 migration (held on branch, awaiting 31-04 push), 1 TS const expansion, 1 vitest sync test, 1 bash ceiling raise.
2. **DB↔TS contract:** D-02 source-of-truth wiring is now live; downstream P31 admin SECDEFs in 31-02/04/05 can call `has_permission(get_caller_role(p_org_id), '<perm>')` without forward-reference.
3. **Sync test gating:** Test will SKIP in CI until 31-04's BLOCKING push lands all P31 migrations; record this as a known wave-gated test so it is not falsely reported as "broken" between waves.
4. **Bundle ceiling history:** Add the new entry `12→16→17→17→25→22→28→30→35→36→50 kB`. Note that Wave 2 plan 31-05 will tighten back toward `measured + ~1 kB headroom` at phase close per D-08 discipline.
5. **No live DB changes from this plan.**
6. **Open items handed to 31-04:** Push migration `20270601310101_p31_01_has_permission_secdef.sql` as part of its BLOCKING `supabase db push --linked` step.
</output>
