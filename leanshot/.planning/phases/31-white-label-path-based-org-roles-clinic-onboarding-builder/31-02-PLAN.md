---
phase: 31
plan: 02
type: execute
wave: 1
depends_on: [31-00, 31-01]
files_modified:
  - supabase/migrations/20270601400003_p31_02_branding_expand_and_wcag.sql
  - supabase/migrations/20270601400004_p31_02_branding_storage.sql
  - supabase/functions/branding-asset-upload-url/index.ts
  - supabase/functions/branding-asset-upload-url/index.test.ts
  - supabase/functions/branding-asset-upload-url/deno.json
  - src/lib/__tests__/rls-org-branding.test.ts
autonomous: true
requirements: [ORG-11]
tags: [supabase, postgres, secdef, storage, edge-function, wcag, rls, white-label]

must_haves:
  truths:
    - "Owner can persist a valid `org_branding` row with 4 oklch colors, 2 font choices, radius scale, and favicon URL."
    - "Server rejects rgb/hex color values with structured error `INVALID_OKLCH_FORMAT`."
    - "Server rejects (text, bg) pairs below 4.5:1 with structured error `CONTRAST_TEXT_BG_FAIL`."
    - "Server rejects (primary, bg) pairs below 3.0:1 with structured error `CONTRAST_PRIMARY_BG_FAIL`."
    - "Caller without `branding.edit` permission gets `insufficient_privilege` raise when calling `save_org_branding`."
    - "Owner of Org X cannot upload an asset to Org Y's bucket path (Storage RLS denial)."
    - "Authenticated owner can request a presigned upload URL from the Edge Function for their own org and upload directly."
    - "Public-read works: an anonymous GET against `org-branding/{org_id}/logo.png` returns the object."
  artifacts:
    - path: "supabase/migrations/20270601400003_p31_02_branding_expand_and_wcag.sql"
      provides: "ALTER TABLE org_branding (5 new cols + rename); `_compute_wcag_contrast(text,text) returns numeric`; `save_org_branding(uuid, jsonb) returns void` SECDEF"
      contains: "alter table public.org_branding"
    - path: "supabase/migrations/20270601400004_p31_02_branding_storage.sql"
      provides: "Public storage buckets `org-branding` + `org-onboarding-assets` plus path-prefix-RLS policies on storage.objects"
      contains: "insert into storage.buckets"
    - path: "supabase/functions/branding-asset-upload-url/index.ts"
      provides: "Service-role Edge Fn returning presigned upload URL after `has_permission(role, 'branding.edit')` gate"
      exports: ["default Deno.serve handler"]
    - path: "src/lib/__tests__/rls-org-branding.test.ts"
      provides: "Cross-tenant + WCAG + oklch + Storage path-prefix denial test cases"
      contains: "save_org_branding"
  key_links:
    - from: "save_org_branding SECDEF"
      to: "public.has_permission(role, 'branding.edit')"
      via: "SQL call inside SECDEF body"
      pattern: "has_permission\\([^,]+,\\s*'branding\\.edit'"
    - from: "save_org_branding SECDEF"
      to: "_compute_wcag_contrast()"
      via: "SQL function call"
      pattern: "_compute_wcag_contrast\\("
    - from: "save_org_branding SECDEF"
      to: "public.log_admin_action()"
      via: "audit write per D-13"
      pattern: "log_admin_action\\("
    - from: "branding-asset-upload-url Edge Fn"
      to: "supabase.storage.from('org-branding').createSignedUploadUrl(...)"
      via: "service-role storage SDK"
      pattern: "createSignedUploadUrl"
    - from: "storage.objects RLS (INSERT/UPDATE/DELETE policies, both buckets)"
      to: "public.org_members.role = 'owner' AND (storage.foldername(name))[1]::uuid = org_id"
      via: "path-prefix predicate per Phase 9 org-logos precedent"
      pattern: "storage\\.foldername"
---

<objective>
Expand the Phase 28 `org_branding` skeleton into the bounded 10-token brand-essentials shape from D-05, ship the WCAG AA enforcement helper from D-06, create the two public Storage buckets (logos+favicons; onboarding intro_card images) per D-08, and ship the SECDEF + Edge Function pair that lets clinic owners persist branding and obtain presigned upload URLs without ever giving the patient surface direct table writes.

Purpose: This plan is the server-side foundation for the entire white-label story (REQ ORG-11). Plan 31-03 (`resolve_clinic_branding` public RPC + pre-mount fetch) depends on the new columns existing; Plan 31-05 (BrandingTab UI) depends on `save_org_branding` + the Edge Function being callable. Without this plan, no clinic can persist a theme.

Output:
- Two new migration files (schema/SECDEF separate from Storage bucket for crash safety per [[reference_supabase_migration_gotchas]] — Storage policy errors won't roll back the schema half).
- One new Supabase Edge Function (`branding-asset-upload-url`) with Deno tests.
- Extended `rls-org-branding.test.ts` covering 4 new attack surfaces (cross-tenant save, WCAG reject, oklch reject, Storage path-prefix denial).
- All migrations committed but NOT pushed by this plan (Plan 31-04 owns the BLOCKING `supabase db push --linked` for the entire phase per outline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-CONTEXT.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-RESEARCH.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-UI-SPEC.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-PLAN-OUTLINE.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-EXTENSION-CONTRACT.md
@supabase/migrations/20270601100006_org_branding_table.sql
@supabase/migrations/20260801000003_org_logos_storage.sql
@src/lib/__tests__/rls-org-branding.test.ts
@src/lib/__tests__/_fixtures/p28-rls-fixture.ts
@supabase/functions/clinic-invite/index.ts
@supabase/functions/_shared/with-org-scope.ts

<interfaces>
<!--
Existing SECDEFs/types this plan calls or follows. Executors use these directly.
-->

DEPENDENCY from Plan 31-00 (must merge first):
- `public.org_member_role` enum has been renamed: VALUES are now `'owner'`, `'clinician'`, `'staff'` (was `'admin'`, `'staff'`, `'viewer'`).
- `_is_org_owner(p_org_id uuid, p_user_id uuid) returns boolean` exists (renamed from `_is_org_admin`).
- All P28/P30 SECDEF bodies referencing role literals have been ripple-renamed.
- `src/lib/org.ts` `ROLE_PERMISSIONS` keys are `owner` / `clinician` / `staff`.

DEPENDENCY from Plan 31-01 (must merge first):
- `public.has_permission(p_role org_member_role, p_perm text) returns boolean` SECDEF exists.
- `public.get_caller_role(p_org_id uuid) returns org_member_role` SECDEF exists (returns null if not a member; raises nothing — callers raise their own errors).
- `'branding.edit'` is a recognized permission key in both the DB matrix and the TS `ROLE_PERMISSIONS` const.

Existing helper from Phase 24:
- `public.log_admin_action(p_action_type text, p_resource_id uuid, p_metadata jsonb) returns void` — call signature follows Phase 24 audit pattern; consult `supabase/migrations/202606*_audit_logs*.sql` for exact param order if uncertain.

Existing pattern (Phase 9):
- `org-logos` public bucket precedent at `supabase/migrations/20260801000003_org_logos_storage.sql` uses `(storage.foldername(name))[1]::uuid` to extract the org_id from path `{org_id}/logo.png`. THIS PLAN COPIES THE PATTERN VERBATIM but gates on `org_members.role = 'owner'` (not `has_permission(...)`) for direct uploads, because client uploads via Storage SDK do NOT pass through the Edge Fn.

Edge Function pattern (Phase 9 `clinic-invite/index.ts`):
- Dispatcher `Deno.serve` reads `Authorization: Bearer <jwt>` header.
- Creates a JWT-bearing supabase client to resolve `auth.uid()` server-side.
- Creates a separate service-role client for privileged operations (e.g. presigned URL generation, here).
- Always returns JSON. CORS allows `*` origin, no credentials.
- See `cors.ts` and `import_map.json` siblings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branding schema migration — ALTER org_branding + WCAG helper + save_org_branding SECDEF</name>
  <files>supabase/migrations/20270601400003_p31_02_branding_expand_and_wcag.sql</files>
  <behavior>
    Tests to be added to `src/lib/__tests__/rls-org-branding.test.ts` in Task 3 will verify:
    - `save_org_branding(orgY, {...})` from user A (member of org X only) raises `insufficient_privilege`.
    - `save_org_branding(orgX, { primary_color: 'rgb(255, 0, 0)' })` from user A (owner of org X) raises `INVALID_OKLCH_FORMAT`.
    - `save_org_branding(orgX, { text_color: 'oklch(0.95 0.01 90)', bg_color: 'oklch(0.97 0.01 90)' })` raises `CONTRAST_TEXT_BG_FAIL` (ratio ~1.08).
    - `save_org_branding(orgX, { primary_color: 'oklch(0.92 0.05 240)', bg_color: 'oklch(0.95 0.02 240)' })` raises `CONTRAST_PRIMARY_BG_FAIL`.
    - `save_org_branding(orgX, { primary_color: 'oklch(0.20 0.18 240)', accent_color: 'oklch(0.55 0.10 150)', bg_color: 'oklch(0.97 0.02 95)', text_color: 'oklch(0.15 0.02 50)', heading_font: 'Fraunces', body_font: 'Inter', radius_scale: 'md', logo_url: 'https://x/y', favicon_url: 'https://x/z', support_email: 'a@b.c' })` from owner persists all 10 fields (upsert).
    - After save, `select * from public.audit_logs where action_type='org_branding.update'` returns a row with `org_id=orgX` for the caller.
  </behavior>
  <action>
    Create migration `supabase/migrations/20270601400003_p31_02_branding_expand_and_wcag.sql` (per [[reference_supabase_migration_filename_regex]] 14-digit `<ts>_<snake>.sql`).

    Wrap in a single `begin; ... commit;`. Inside:

    (a) ALTER TABLE expansion per D-05:
    - `alter table public.org_branding rename column font_family to heading_font;`
    - `alter table public.org_branding add column favicon_url text null;`
    - `alter table public.org_branding add column bg_color text null;`
    - `alter table public.org_branding add column text_color text null;`
    - `alter table public.org_branding add column body_font text null;`
    - `alter table public.org_branding add column radius_scale text null check (radius_scale in ('sm','md','lg','xl'));`
    - All new columns are nullable (defaults supplied by 31-03's public RPC at read time, NOT at the DDL layer — per `<task_anti_patterns>` "no sparse nullable defaults").

    (b) Create `public._compute_wcag_contrast(c1 text, c2 text) returns numeric` per RESEARCH Finding 2 (verbatim, including the regex extraction of the oklch L channel and the `L^2.2` luminance approximation). Tag the inline comment block as `-- [ASSUMED/MEDIUM confidence] L^2.2 approximation; sufficient for WCAG AA gate; revisit if false negatives surface in v1.4 telemetry.`. Function MUST:
    - Be `language plpgsql security definer`.
    - Carry `set search_path = pg_catalog, public, extensions` (non-negotiable per [[reference_supabase_migration_gotchas]]).
    - Return 0.0 (treated as a fail) when either input fails the oklch regex extraction (defensive — never raise from inside the helper; let `save_org_branding` decide the error code based on context).

    (c) Define a single shared regex constant in the migration prelude (as a SQL DO-block local or as a CHECK-helper IMMUTABLE function) using the RESEARCH-recommended pattern: `^oklch\s*\(\s*(\d+\.?\d*%?)\s+(\d*\.?\d+)\s+(\d+\.?\d*)\s*(?:\/\s*[\d.]+%?)?\s*\)$`. Either inline into `save_org_branding` (preferred for atomicity) or create `public._is_valid_oklch(text) returns boolean immutable language sql` and reference it.

    (d) Create `public.save_org_branding(p_org_id uuid, p_tokens jsonb) returns void` per D-13:
    - `language plpgsql security definer`, `set search_path = pg_catalog, public, extensions`.
    - Body sequence:
      1. Resolve caller role: `v_role := public.get_caller_role(p_org_id);`
      2. Permission gate: `if not public.has_permission(v_role, 'branding.edit') then raise exception 'insufficient_privilege' using errcode='42501'; end if;`
      3. Extract typed locals from jsonb: `v_logo_url := p_tokens->>'logo_url'`, `v_favicon_url`, `v_primary_color`, `v_accent_color`, `v_bg_color`, `v_text_color`, `v_heading_font`, `v_body_font`, `v_radius_scale`, `v_support_email`. All defaulted to NULL if absent.
      4. oklch validation: for each of the 4 color fields when non-null, check the regex; raise `using errcode='22023', message='INVALID_OKLCH_FORMAT', detail=v_field_name` on first failure.
      5. radius_scale check: when non-null, assert `v_radius_scale in ('sm','md','lg','xl')`; raise `INVALID_RADIUS_SCALE` on mismatch.
      6. body_font / heading_font: when non-null, assert membership in `('Inter','Fraunces','JetBrains Mono','Lora','IBM Plex Sans')` (D-05 curated list); raise `INVALID_FONT_FAMILY` on mismatch.
      7. WCAG pairs — ONLY when ALL four colors of the relevant pair are non-null:
         - `if v_text_color is not null and v_bg_color is not null and public._compute_wcag_contrast(v_text_color, v_bg_color) < 4.5 then raise exception 'CONTRAST_TEXT_BG_FAIL' using errcode='22023'; end if;`
         - `if v_primary_color is not null and v_bg_color is not null and public._compute_wcag_contrast(v_primary_color, v_bg_color) < 3.0 then raise exception 'CONTRAST_PRIMARY_BG_FAIL' using errcode='22023'; end if;`
      8. Upsert via `insert into public.org_branding (org_id, ...10 cols..., updated_at) values (...) on conflict (org_id) do update set ... where org_branding.org_id = excluded.org_id;` — set `updated_at = now()`.
      9. `perform public.log_admin_action('org_branding.update', p_org_id, jsonb_build_object('changed_fields', (select array_agg(k) from jsonb_object_keys(p_tokens) as k), 'org_id', p_org_id));` — call signature from Phase 24 audit pattern (consult `supabase/migrations/202606*_audit_logs*.sql` if param order is uncertain at execute time; do NOT silently drop the call — error out if signature unknown).
    - GRANT: `grant execute on function public.save_org_branding(uuid, jsonb) to authenticated;` (anon never calls this — D-13).

    Per `<threat_model>` in the planning_context: error messages MUST NOT carry PHI. The `detail` of `INVALID_OKLCH_FORMAT` carries the field name (`primary_color`, `text_color`, etc.) — not the value. Verify no `raise ... message %` formatter interpolates `p_tokens` content.

    Per [[feedback_planner_iter1_anti_patterns]] item #4 (defensive jsonb contracts): the validator above is the SINGLE source of jsonb shape truth. There is no parallel TS validator. The client (BrandingTab) WILL parse oklch client-side for the live meter, but the DB hard-block is the contract per D-06.

    Verify migration filename matches `^[0-9]{14}_[a-z0-9_]+\.sql$` (14 digits + `_` + snake_case). Confirm with `ls supabase/migrations/ | grep p31_02_branding_expand_and_wcag` — letter-suffix timestamps are silently skipped per [[reference_supabase_migration_filename_regex]].
  </action>
  <verify>
    <automated>
    test -f supabase/migrations/20270601400003_p31_02_branding_expand_and_wcag.sql &amp;&amp; ls supabase/migrations/ | grep -E '^[0-9]{14}_p31_02_branding_expand_and_wcag\.sql$' &amp;&amp; grep -c 'set search_path = pg_catalog, public, extensions' supabase/migrations/20270601400003_p31_02_branding_expand_and_wcag.sql | awk '$1 >= 2 { exit 0 } { exit 1 }' &amp;&amp; grep -c 'CONTRAST_TEXT_BG_FAIL\|CONTRAST_PRIMARY_BG_FAIL\|INVALID_OKLCH_FORMAT\|insufficient_privilege' supabase/migrations/20270601400003_p31_02_branding_expand_and_wcag.sql | awk '$1 >= 4 { exit 0 } { exit 1 }' &amp;&amp; grep -q 'log_admin_action' supabase/migrations/20270601400003_p31_02_branding_expand_and_wcag.sql &amp;&amp; grep -q 'rename column font_family to heading_font' supabase/migrations/20270601400003_p31_02_branding_expand_and_wcag.sql
    </automated>
  </verify>
  <done>
    Migration file exists with the correct filename pattern. The file contains the 5 ALTER TABLE statements (rename + 4 new col additions + the radius_scale col with CHECK), the `_compute_wcag_contrast` helper, and the `save_org_branding` SECDEF with all four structured error codes and the `log_admin_action` call. `search_path` is set on every SECDEF. NO `'admin'` string literal anywhere (would fail Plan 31-00's plan-checker BLOCKER).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Storage buckets + path-prefix RLS migration + branding-asset-upload-url Edge Function</name>
  <files>
    supabase/migrations/20270601400004_p31_02_branding_storage.sql,
    supabase/functions/branding-asset-upload-url/index.ts,
    supabase/functions/branding-asset-upload-url/index.test.ts,
    supabase/functions/branding-asset-upload-url/deno.json
  </files>
  <behavior>
    Edge Function `branding-asset-upload-url` MUST:
    - Test E1: POST without `Authorization` header → returns 401 JSON `{error: 'missing_jwt'}`.
    - Test E2: POST with valid JWT for user who is owner of org X, body `{ org_id: '<orgX>', kind: 'logo', ext: 'png' }` → returns 200 with JSON `{ upload_url: string, path: 'orgX/logo.png', token: string }`.
    - Test E3: POST with valid JWT for user who is owner of org X, body `{ org_id: '<orgY>', kind: 'logo' }` → returns 403 JSON `{error: 'insufficient_privilege'}` (calls `has_permission` via JWT-scoped client).
    - Test E4: POST body with `kind: 'banner'` (not in `['logo','favicon','intro_card']`) → returns 400 JSON `{error: 'invalid_kind'}`.
    - Test E5: POST body with `ext: 'gif'` (not in PNG/JPG/SVG/ICO for branding bucket; or PNG/JPG for onboarding bucket) → returns 400 JSON `{error: 'invalid_ext'}`.

    Storage RLS via vitest in Task 3:
    - Test S1: Owner of org X uploading to bucket `org-branding` at path `orgX/logo.png` succeeds.
    - Test S2: Owner of org X uploading to bucket `org-branding` at path `orgY/logo.png` is denied by RLS (returns error, not 200).
    - Test S3: Anonymous GET against a previously-uploaded public-read object in `org-branding` succeeds.
  </behavior>
  <action>
    Create migration `supabase/migrations/20270601400004_p31_02_branding_storage.sql` separate from Task 1's schema migration (per `<key_decisions_to_lock>` item 5 — separate file gives crash safety: Storage policy errors won't roll back the org_branding ALTER TABLE).

    (a) Two `insert into storage.buckets ... on conflict (id) do nothing` calls following the Phase 9 `org-logos` precedent verbatim:
    - Bucket `org-branding`: `public=true`, `file_size_limit=524288` (500 KB per D-08), `allowed_mime_types=array['image/png','image/jpeg','image/svg+xml','image/x-icon','image/vnd.microsoft.icon']`.
    - Bucket `org-onboarding-assets`: `public=true`, `file_size_limit=2097152` (2 MB — `intro_card.custom.image_url` per D-09 Claude's-Discretion item; same generosity as `org-logos`), `allowed_mime_types=array['image/png','image/jpeg']` (SVG excluded per Phase 9 T-09-09 XSS concern; SVG is allowed for `org-branding` logos only because operators expect SVG support for vector logos — accept the smaller attack surface, document the tradeoff inline).

    (b) FOUR policies per bucket, mirroring `org-logos` exactly, wrapped in `do $$ begin if not exists (...) then create policy ... end if; end $$;` blocks to make the migration safely re-runnable:
    - `objects_select_org_branding`: `for select to authenticated, anon using (bucket_id = 'org-branding')`. (Public-read; CDN serves directly.)
    - `objects_insert_org_branding`: `for insert to authenticated with check (bucket_id = 'org-branding' and exists (select 1 from public.org_members where org_id = ((storage.foldername(name))[1])::uuid and user_id = auth.uid() and role = 'owner'))`. (D-08: owner gate.)
    - `objects_update_org_branding`: same `using` + `with check` as insert. (Lets owner overwrite logo on re-upload.)
    - `objects_delete_org_branding`: same `using` as insert. (Lets owner "Remove logo" from BrandingTab.)
    - Repeat the four-policy set for `org-onboarding-assets` with the same predicate shape but `bucket_id = 'org-onboarding-assets'`.

    Note: The predicate uses `role = 'owner'` (renamed per Plan 31-00). Do NOT use `has_permission(role, 'branding.edit')` here even though `clinician` could in principle hold `branding.edit` — D-03 explicitly scopes `branding.edit` to owner-only, and Storage RLS evaluates per-request so SECDEF-style indirection adds query cost without security benefit. If D-03 expands in v1.4 to grant `branding.edit` to clinicians, this policy gets revisited in that phase. Inline comment must explain.

    (c) Create Edge Function `supabase/functions/branding-asset-upload-url/`:

    `deno.json`:
    - Minimal config matching `supabase/functions/clinic-invite/deno.json` (extend that file's `imports` map — use esm.sh for `@supabase/supabase-js` per [[reference_supabase_edge_function_deploy_gotchas]] which warns the bundler ignores import_map.json).

    `index.ts`:
    - Single `Deno.serve` handler.
    - CORS preflight per `clinic-invite/cors.ts` pattern: allow `*` origin, no credentials, methods `POST, OPTIONS`, headers `Content-Type, Authorization`.
    - Parse `Authorization: Bearer <jwt>` header; if missing → `return Response.json({error:'missing_jwt'}, {status:401})`.
    - Build JWT-bearing supabase client: `createClient(SUPABASE_URL, SUPABASE_ANON_KEY, { global: { headers: { Authorization: req.headers.get('Authorization') ?? '' } } })`.
    - Build service-role supabase client SEPARATELY via the project's brand-typed `_createServiceRoleClientUnsafe()` helper from `supabase/functions/_shared/supabase-server.ts` (per Phase 28 D-29c — never `createClient(SUPABASE_URL, SERVICE_ROLE_KEY)` directly; the brand-type lint rule blocks raw construction).
    - Parse body: `{ org_id: string, kind: 'logo'|'favicon'|'intro_card', ext: 'png'|'jpg'|'jpeg'|'svg'|'ico' }`. Validate kind ∈ {logo,favicon,intro_card}; validate ext per bucket (org-branding allows png/jpg/jpeg/svg/ico; org-onboarding-assets allows png/jpg/jpeg only). Reject with 400 on mismatch.
    - Resolve caller role + permission via JWT-scoped client: `const { data: roleData } = await jwtClient.rpc('get_caller_role', { p_org_id: org_id });` then `const { data: hasPerm } = await jwtClient.rpc('has_permission', { p_role: roleData, p_perm: 'branding.edit' });`. If `!hasPerm` → return 403 JSON `{error:'insufficient_privilege'}`.
    - Pick bucket: `kind === 'intro_card' ? 'org-onboarding-assets' : 'org-branding'`.
    - Build path: `${org_id}/${kind}.${ext}` (path-prefix matches Storage RLS predicate `(storage.foldername(name))[1] = org_id`).
    - Call `serviceClient.storage.from(bucket).createSignedUploadUrl(path)` per Supabase Storage SDK. Return `Response.json({ upload_url: data.signedUrl, path, token: data.token, bucket }, { status: 200 })`.
    - Any unexpected error → log via `console.error` (Sentry-ingested per `_shared` logger if present); return 500 JSON `{error:'internal'}` with no detail leakage.

    `index.test.ts` (Deno test):
    - Tests E1–E5 above per `<behavior>`.
    - Use `.test.ts` filename (NOT `-test.ts`) per [[reference_deno_test_discovery]].
    - Use `Deno.test({ name, sanitizeOps: false, sanitizeResources: false }, async () => {...})` blocks invoking the handler directly (extract handler into named export `export async function handler(req: Request): Promise<Response>` and have `Deno.serve(handler)` at bottom — makes it unit-testable per the `clinic-invite` pattern).
    - Mock JWT via known service-role-signed test JWT for owner-of-orgX; mock service-role storage client via dependency injection (handler accepts optional `{ jwtClientFactory, serviceClientFactory }` param defaulting to real impls).

    Filename verification: `ls supabase/migrations/ | grep p31_02_branding_storage` MUST match the 14-digit prefix pattern. Confirm filename does NOT carry letter suffix.
  </action>
  <verify>
    <automated>
    test -f supabase/migrations/20270601400004_p31_02_branding_storage.sql &amp;&amp; test -f supabase/functions/branding-asset-upload-url/index.ts &amp;&amp; test -f supabase/functions/branding-asset-upload-url/index.test.ts &amp;&amp; test -f supabase/functions/branding-asset-upload-url/deno.json &amp;&amp; ls supabase/migrations/ | grep -E '^[0-9]{14}_p31_02_branding_storage\.sql$' &amp;&amp; grep -c "insert into storage.buckets" supabase/migrations/20270601400004_p31_02_branding_storage.sql | awk '$1 >= 2 { exit 0 } { exit 1 }' &amp;&amp; grep -c "storage.foldername" supabase/migrations/20270601400004_p31_02_branding_storage.sql | awk '$1 >= 6 { exit 0 } { exit 1 }' &amp;&amp; grep -q "createSignedUploadUrl" supabase/functions/branding-asset-upload-url/index.ts &amp;&amp; grep -q "has_permission" supabase/functions/branding-asset-upload-url/index.ts &amp;&amp; cd supabase/functions/branding-asset-upload-url &amp;&amp; deno test --allow-env --allow-net --no-check index.test.ts
    </automated>
  </verify>
  <done>
    Migration `20270601400004_p31_02_branding_storage.sql` exists, creates two public buckets, and installs 4 RLS policies per bucket (8 total) all referencing `(storage.foldername(name))[1]` + `role = 'owner'`. Edge Function `branding-asset-upload-url` exists with `index.ts` (named-export `handler`) + Deno test file passing all 5 cases. `deno.json` resolves `@supabase/supabase-js` via esm.sh per the gotcha rule. No raw `createClient(SUPABASE_URL, SERVICE_ROLE_KEY)` — service-role client is built via `_createServiceRoleClientUnsafe()`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend rls-org-branding.test.ts with WCAG + cross-tenant + oklch + Storage path-prefix cases</name>
  <files>src/lib/__tests__/rls-org-branding.test.ts</files>
  <behavior>
    Extend the existing 4-test suite with these new cases under a new `describeIfLive('P31 RLS — save_org_branding SECDEF + Storage path-prefix', ...)` block:

    - T6 (cross-tenant save denial): user A (owner of org X only) calls `sessA.client.rpc('save_org_branding', { p_org_id: fixture.orgY, p_tokens: {...} })`. Expect `error` non-null with `error.code === '42501'` OR error message containing `insufficient_privilege`.

    - T7 (oklch reject): user B (owner of org Y) calls `save_org_branding(orgY, { primary_color: 'rgb(255, 0, 0)' })`. Expect error with message containing `INVALID_OKLCH_FORMAT`.

    - T8 (radius_scale reject): user B calls `save_org_branding(orgY, { radius_scale: 'huge' })`. Expect error containing `INVALID_RADIUS_SCALE`.

    - T9 (font reject): user B calls `save_org_branding(orgY, { heading_font: 'Comic Sans' })`. Expect error containing `INVALID_FONT_FAMILY`.

    - T10 (WCAG text/bg fail): user B calls `save_org_branding(orgY, { text_color: 'oklch(0.95 0.01 90)', bg_color: 'oklch(0.97 0.01 90)' })`. Expect error containing `CONTRAST_TEXT_BG_FAIL`.

    - T11 (WCAG primary/bg fail): user B calls `save_org_branding(orgY, { primary_color: 'oklch(0.92 0.05 240)', bg_color: 'oklch(0.95 0.02 240)' })`. Expect error containing `CONTRAST_PRIMARY_BG_FAIL`.

    - T12 (happy path): user B calls `save_org_branding(orgY, { primary_color: 'oklch(0.20 0.18 240)', accent_color: 'oklch(0.55 0.10 150)', bg_color: 'oklch(0.97 0.02 95)', text_color: 'oklch(0.15 0.02 50)', heading_font: 'Fraunces', body_font: 'Inter', radius_scale: 'md', logo_url: 'https://x/y', favicon_url: 'https://x/z', support_email: 'a@b.c' })`. Expect no error. Admin-read confirms all 10 fields persisted. Admin-read of `public.audit_logs` filtered by `action_type='org_branding.update'` AND `metadata->>'org_id' = orgY` returns ≥ 1 row.

    - T13 (Storage path-prefix DENIAL): user A (owner of orgX only, NOT orgY) attempts `sessA.client.storage.from('org-branding').upload(`${fixture.orgY}/logo.png`, new Blob([Uint8Array.of(0x89,0x50,0x4e,0x47)], { type: 'image/png' }))`. Expect error non-null (Storage RLS denial). Confirm via admin-read that no object exists at that path.

    - T14 (Storage path-prefix ALLOW): user B (owner of orgY) uploads to `${fixture.orgY}/logo.png` with same blob. Expect no error. Admin-read confirms object exists. Anonymous GET against `${SUPABASE_URL}/storage/v1/object/public/org-branding/${fixture.orgY}/logo.png` returns 200.

    - T15 (Edge Fn 403 cross-tenant): plain `fetch` to `${SUPABASE_URL}/functions/v1/branding-asset-upload-url` with `Authorization: Bearer ${sessA.jwt}`, body `{ org_id: fixture.orgY, kind: 'logo', ext: 'png' }`. Expect 403 + JSON `{error: 'insufficient_privilege'}`.

    - T16 (Edge Fn 200 own org): same call but `org_id: fixture.orgY` with `sessB.jwt`. Expect 200 + JSON containing `upload_url` + `token`.

    All new tests preserve the file-scoped `TEST_SLUG_PREFIX` + `cleanupByPrefix` pattern (per [[feedback_rls_per_file_slug_prefix]]) and use the ES256-compatible fixture (`admin.generateLink` + plain fetch `/auth/v1/verify` via the existing `_fixtures/p28-rls-fixture.ts` helpers) per [[reference_rls_fixture_gotrueclient_flake]].
  </behavior>
  <action>
    Open `src/lib/__tests__/rls-org-branding.test.ts`. Preserve the existing 4 tests (T3, T4, T5, member-can-read). After the existing `describeIfLive` block closes, add a NEW `describeIfLive('P31 RLS — save_org_branding SECDEF + Storage path-prefix isolation', () => {...})` block that:

    1. Reuses `fixture: TwoOrgsTwoUsers` from the same `createTwoOrgsTwoUsers(TEST_SLUG_PREFIX)` call (extend the existing `beforeAll` rather than spawning a second fixture — slug-prefix overlap is the same file, cleanup remains single-source).
    2. In `beforeAll` AFTER fixture creation: ensure user B is set as `role = 'owner'` of orgY (the fixture currently makes them a regular member — admin-update via `getAdmin().from('org_members').update({ role: 'owner' }).eq('user_id', fixture.userB).eq('org_id', fixture.orgY)`).
    3. Implements T6–T16 above. Each test gets its own `it(...)` block with a per-test 30_000ms timeout. Use `error.message?.includes(...)` for error-code checks because Supabase wraps errors in the JS client (the raw Postgres errcode is in `error.code` but the structured token like `CONTRAST_TEXT_BG_FAIL` is in `error.message`).
    4. For T15/T16, derive `sessA.jwt` and `sessB.jwt` from the existing fixture (the `_fixtures/p28-rls-fixture.ts` `Session` shape exposes the JWT used to instantiate the client — if not currently exposed, ADD `jwt: string` to the Session interface and populate from the `admin.generateLink` flow). Single small extension to the shared fixture is acceptable.
    5. Do NOT touch the existing `'P28 RLS org_branding — gating check'` describe block at the bottom — leave it intact.

    Do not introduce a separate test file — extending the existing one keeps the slug-prefix cleanup single-source.
  </action>
  <verify>
    <automated>
    grep -c 'P31 RLS' src/lib/__tests__/rls-org-branding.test.ts | awk '$1 >= 1 { exit 0 } { exit 1 }' &amp;&amp; grep -c 'CONTRAST_TEXT_BG_FAIL\|CONTRAST_PRIMARY_BG_FAIL\|INVALID_OKLCH_FORMAT\|INVALID_RADIUS_SCALE\|INVALID_FONT_FAMILY' src/lib/__tests__/rls-org-branding.test.ts | awk '$1 >= 5 { exit 0 } { exit 1 }' &amp;&amp; grep -q "storage.from('org-branding')" src/lib/__tests__/rls-org-branding.test.ts &amp;&amp; grep -q 'branding-asset-upload-url' src/lib/__tests__/rls-org-branding.test.ts &amp;&amp; npx vitest run src/lib/__tests__/rls-org-branding.test.ts --reporter=basic
    </automated>
  </verify>
  <done>
    `rls-org-branding.test.ts` contains the original 4 tests + 11 new ones (T6–T16) under a P31-specific `describeIfLive`. All 15 tests pass when `SUPABASE_SERVICE_ROLE_KEY` is set; cleanly skip when not (existing gating-check describe still works). No `signInWithPassword` calls (would trip [[reference_rls_fixture_gotrueclient_flake]]). File-scoped `TEST_SLUG_PREFIX` preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (BrandingTab UI) → `save_org_branding` SECDEF | Untrusted oklch + font + radius inputs cross here; server is the security floor per D-06 |
| Client (BrandingTab UI) → `branding-asset-upload-url` Edge Fn | Untrusted JWT + `{org_id, kind, ext}` body crosses here |
| Edge Fn → Postgres (via JWT-scoped client) | Server-internal but the JWT carries the user identity — boundary is JWT validation |
| Edge Fn → Supabase Storage (via service-role client) | Privileged boundary; only crossed AFTER `has_permission` succeeds |
| Client → Supabase Storage direct upload (via presigned URL OR direct SDK) | Storage RLS evaluates per-request; `(storage.foldername(name))[1]` predicate gates per-path |
| Anonymous visitor → Storage public-read GET | Public-read by design (logos rendered before auth — Phase 9 precedent); not a confidentiality boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | T (Tampering) | `save_org_branding` SECDEF | mitigate | WCAG hard-block via `_compute_wcag_contrast` — clinic CANNOT persist (text,bg) < 4.5 or (primary,bg) < 3.0; structured error codes `CONTRAST_TEXT_BG_FAIL` / `CONTRAST_PRIMARY_BG_FAIL` raised before UPDATE. T10/T11 in test suite proves it. |
| T-31-02-02 | T (Tampering) | `save_org_branding` color inputs | mitigate | oklch-only regex (`^oklch\s*\(...\)$`); rgb/hex/hsl/named-color/css-var rejected with `INVALID_OKLCH_FORMAT`. Prevents both CSS injection (no `url()`, no `;`) and round-trip-unreadable theme bypass via crafted color spaces. T7 proves it. |
| T-31-02-03 | T (Tampering) | Storage cross-tenant upload | mitigate | Storage RLS predicate `(storage.foldername(name))[1]::uuid = org_id` + `org_members.role = 'owner'`. User A cannot write to orgY's bucket path. T13 proves it via direct supabase-js storage call. |
| T-31-02-04 | E (Elevation) | `branding-asset-upload-url` Edge Fn | mitigate | JWT verified at Fn entry via JWT-scoped supabase client; then `get_caller_role(org_id)` + `has_permission(role, 'branding.edit')` BOTH called via the JWT-scoped client (not service-role) so RLS still applies to the role lookup. Service-role only used AFTER the auth gate to mint the presigned URL. T15 proves cross-tenant denial. |
| T-31-02-05 | E (Elevation) | Edge Fn missing JWT | mitigate | First action of handler is `Authorization: Bearer` extraction; missing → 401 `{error:'missing_jwt'}`. Test E1 covers. |
| T-31-02-06 | I (Information disclosure) | Error message leakage | mitigate | All `raise exception` statements use fixed structured tokens (`CONTRAST_TEXT_BG_FAIL` etc.); `detail` field carries only the OFFENDING FIELD NAME, never the patient/user data or the offending value. No `format()` or `%` interpolation against `p_tokens` content. Edge Fn 500 path returns `{error:'internal'}` with no detail. |
| T-31-02-07 | I (Information disclosure) | Public-read bucket | accept | Logos + favicons + intro_card images are non-PHI by D-08 (operators are warned in BrandingTab helper text). Public-read is required for first-paint (D-07); separate `org-onboarding-assets` bucket gives ops the ability to delete operational images independently of brand identity in v1.4. |
| T-31-02-08 | T (Tampering) | Storage SVG XSS in `org-branding` bucket | accept-with-mitigation | SVG allowed in `org-branding` only (logos); `org-onboarding-assets` excludes SVG. The clinic logo renders via `<img src=...>` (not inline `<svg>`), so script tags inside the SVG file do not execute in the browser. Future hardening: Edge Fn `branding-asset-validate` (D-08) can sanitize SVG with DOMPurify-on-server in v1.4 if needed. Documented in migration inline comment. |
| T-31-02-09 | E (Elevation) | `has_permission(role, 'branding.edit')` drift between DB and TS | mitigate | Plan 31-01 ships the `role-matrix-sync.test.ts` 36-pair equality test; THIS plan inherits that guarantee. The Edge Fn calls the DB function directly (not the TS const) so even if TS drifts, the security floor holds. |
| T-31-02-10 | R (Repudiation) | Untracked branding changes | mitigate | `save_org_branding` calls `public.log_admin_action('org_branding.update', p_org_id, {...})` before returning. T12 verifies the audit_logs row materializes. |
| T-31-02-11 | D (Denial of service) | Large file uploads | mitigate | `file_size_limit` set to 500 KB on `org-branding` bucket and 2 MB on `org-onboarding-assets` — Supabase Storage rejects oversize uploads at the gateway, no Postgres or Edge Fn work consumed. |
| T-31-02-12 | S (Spoofing) | JWT impersonation | accept | Standard Supabase JWT validation; ES256 keys per project memory. No additional surface introduced by this plan. |
</threat_model>

<verification>
After all three tasks complete:

1. `git diff --stat` shows changes confined to: 2 migrations, 4 Edge Fn files, 1 test file. No accidental edits to other files.
2. `npx tsc --noEmit -p tsconfig.app.json` is green (no TS regression from the small fixture extension in Task 3).
3. `npx vitest run src/lib/__tests__/rls-org-branding.test.ts` is green (or cleanly skips when env not set).
4. `cd supabase/functions/branding-asset-upload-url && deno test --allow-env --allow-net --no-check index.test.ts` is green.
5. NO new `'admin'` string literal added anywhere (would break Plan 31-00's plan-checker BLOCKER): `grep -rn "'admin'" supabase/migrations/20270601400003* supabase/migrations/20270601400004* supabase/functions/branding-asset-upload-url/` returns 0 hits.
6. NO direct `createClient(SUPABASE_URL, ...SERVICE_ROLE...)` in the Edge Fn (would break Phase 28 D-29c brand-typed service-role lint): `grep -E "createClient\\(.*SERVICE_ROLE" supabase/functions/branding-asset-upload-url/index.ts` returns 0.
7. Migrations are NOT pushed by this plan. The BLOCKING `supabase db push --linked` is owned by Plan 31-04 (last migration to land, full-wave verification cycle per outline §Schema-push BLOCKING task ownership).
8. Wave-2 verification (after Plan 31-04's push): `supabase db query --linked --sql "select column_name from information_schema.columns where table_schema='public' and table_name='org_branding' order by ordinal_position"` returns all 10 columns including `heading_font` (renamed from font_family), `body_font`, `favicon_url`, `bg_color`, `text_color`, `radius_scale`. `supabase db query --linked --sql "select proname from pg_proc where proname in ('save_org_branding','_compute_wcag_contrast')"` returns both names.
</verification>

<success_criteria>
- Plan 31-03 (`resolve_clinic_branding` public RPC) can SELECT all 10 columns from `org_branding` because they exist.
- Plan 31-05 (BrandingTab UI) can call `save_org_branding(org_id, tokens jsonb)` and get either success, structured WCAG error, or oklch error — and call the Edge Fn `branding-asset-upload-url` for logo+favicon+intro_card uploads.
- Multi-tenant isolation holds: cross-tenant save AND cross-tenant Storage upload BOTH fail in vitest (T6, T13, T15).
- WCAG hard-block holds: a deliberately low-contrast theme cannot be persisted (T10, T11).
- oklch input contract holds: rgb/hex/css-var/named-color all rejected (T7).
- Audit trail holds: every successful save writes one `audit_logs` row with `action_type='org_branding.update'` (T12).
- No `'admin'` string literal introduced (Plan 31-00's BLOCKER preserved).
- No raw service-role client construction (Phase 28 D-29c brand-typed lint preserved).
- Edge Fn has Deno tests covering missing-JWT, valid-own-org, cross-tenant denial, invalid kind, invalid ext (E1–E5).
- Both migrations match the strict 14-digit filename regex; no letter suffix (would be silently skipped per [[reference_supabase_migration_filename_regex]]).
</success_criteria>

<output>
After completion, create `.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-02-SUMMARY.md` documenting:
- Final filenames of both migrations (with the 14-digit timestamp).
- Exact `save_org_branding` body signature + the 5 structured error codes it can raise.
- Edge Fn entry point + the 5 error responses (401, 400×2, 403, 500).
- Bucket configurations (file_size_limit, allowed_mime_types) for both buckets.
- Any deviation from the planned key_decisions (e.g., if `log_admin_action` had a different param order than assumed) — call out for Plan 31-03/31-05 consumers.
- Note that migrations are committed but NOT pushed; Plan 31-04 owns the BLOCKING push.
</output>
