---
phase: 31
plan: 06
type: execute
wave: 3
depends_on: [31-04, 31-05]
files_modified:
  - src/lib/onboarding-builder/use-org-onboarding-flow.ts
  - src/lib/onboarding-builder/__tests__/use-org-onboarding-flow.test.ts
  - src/components/onboarding/OnboardingFlow.tsx
  - src/App.tsx
  - e2e/patient-org-onboarding.spec.ts
  - playwright.config.ts
autonomous: true
requirements: [ORG-13]

must_haves:
  truths:
    - "Invited patient signed in for the first time, with `profiles.primary_org_id` resolving to an org that has an active `org_onboarding_flows` row, sees that org's customized welcome text (e.g., 'Welcome to {orgName}') instead of the canonical LeanShot welcome — proving the org steps array was rendered, not DEFAULT_STEPS."
    - "When that invited patient reaches the final step and confirms completion, `profiles.completed_onboarding_at` is written (now()) by `mark_onboarding_complete()` SECDEF, observable via service-role query."
    - "On the next sign-in (with `completed_onboarding_at` non-null), the patient renders the dashboard immediately — the onboarding surface is not shown."
    - "If the same patient is later invited by a second org and accepts (creating a second `org_patient_links` row), navigating the patient-facing onboarding route still shows the dashboard — no re-onboarding triggers (first-clinic-wins per D-15)."
    - "Anonymous / pre-signin visitors and signed-in patients with no `primary_org_id` continue to render the existing consumer `DEFAULT_STEPS` path unchanged — local-first invariant from CLAUDE.md is preserved (the app works offline + without backend)."
  artifacts:
    - path: "src/lib/onboarding-builder/use-org-onboarding-flow.ts"
      provides: "useOrgOnboardingFlow() React hook returning {status, orgId, orgName, steps}"
      exports: ["useOrgOnboardingFlow", "OrgOnboardingFlowState"]
      min_lines: 80
    - path: "src/lib/onboarding-builder/__tests__/use-org-onboarding-flow.test.ts"
      provides: "Unit tests for the 4 hook return states (loading/consumer/org/completed) with mocked supabase client"
      min_lines: 90
    - path: "src/components/onboarding/OnboardingFlow.tsx"
      provides: "Patient onboarding surface with render-branch + final-step mark_onboarding_complete call"
      contains: "useOrgOnboardingFlow"
    - path: "src/App.tsx"
      provides: "Dashboard-entry render gate that mounts OnboardingFlow when invited-patient hook returns 'org'"
      contains: "useOrgOnboardingFlow"
    - path: "e2e/patient-org-onboarding.spec.ts"
      provides: "4-scenario Playwright e2e: org-flow renders, mark_complete writes timestamp, second visit skips, multi-clinic invite does not re-trigger"
      min_lines: 250
    - path: "playwright.config.ts"
      provides: "PLAYWRIGHT_RUN_P31=1 env-gated project for the new spec (per [[reference_playwright_conditional_project_argv]])"
      contains: "PLAYWRIGHT_RUN_P31"
  key_links:
    - from: "src/components/onboarding/OnboardingFlow.tsx"
      to: "src/lib/onboarding-builder/use-org-onboarding-flow.ts"
      via: "import + hook call at top of component"
      pattern: "useOrgOnboardingFlow"
    - from: "src/lib/onboarding-builder/use-org-onboarding-flow.ts"
      to: "supabase profiles + organizations + org_onboarding_flows tables"
      via: "single joined SELECT via supabase-js"
      pattern: "from\\('profiles'\\)"
    - from: "src/components/onboarding/OnboardingFlow.tsx"
      to: "mark_onboarding_complete() SECDEF (from 31-04)"
      via: "supabase.rpc('mark_onboarding_complete') in final-step complete handler"
      pattern: "mark_onboarding_complete"
    - from: "src/App.tsx"
      to: "src/lib/onboarding-builder/use-org-onboarding-flow.ts"
      via: "render-gate before dashboard mount when status === 'org'"
      pattern: "useOrgOnboardingFlow"
    - from: "e2e/patient-org-onboarding.spec.ts"
      to: "OnboardingFlow + mark_onboarding_complete + profiles.completed_onboarding_at"
      via: "Playwright addInitScript session seeding (per [[reference_playwright_state_seeding]]) + service-role admin verification queries"
      pattern: "addInitScript"
---

<objective>
Wire the patient-side render branch that closes Phase 31's onboarding-builder story end-to-end: invited patients see their org's saved flow on first sign-in (D-10 + D-14); completion writes `profiles.completed_onboarding_at = now()` via the `mark_onboarding_complete()` SECDEF (D-13); subsequent visits render the dashboard directly; multi-clinic invites do NOT re-trigger onboarding (D-15 first-clinic-wins). Consumer DEFAULT_STEPS path remains untouched — local-first invariant from CLAUDE.md is preserved.

Purpose: Without this plan, `org_onboarding_flows` rows shipped in 31-04 and editable via 31-05's OnboardingTab are dead weight — clinics can save flows, but no patient ever sees them. This is the highest-risk integration seam in Phase 31 (per [[feedback_chunked_planning_integration_seam_blindspot]]) — the wire between the clinic-side builder and the patient-facing onboarding surface.

Output: NEW `useOrgOnboardingFlow` hook + NEW vitest unit suite + MODIFIED `OnboardingFlow.tsx` (render branch + final-step SECDEF call) + MODIFIED `App.tsx` (dashboard-entry gate for invited patients) + NEW Playwright e2e (4 scenarios) + `playwright.config.ts` env-gated project entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-CONTEXT.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-RESEARCH.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-UI-SPEC.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-PLAN-OUTLINE.md

# Wave-2 producers consumed here
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-04-SUMMARY.md
@.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-05-SUMMARY.md

# Source files modified or consumed
@src/components/onboarding/OnboardingFlow.tsx
@src/components/onboarding/ProgressIndicator.tsx
@src/lib/supabase.ts
@src/lib/store.ts
@src/lib/clinic.ts
@src/App.tsx
@playwright.config.ts

# Test-fixture patterns to follow
@src/lib/__tests__/_fixtures/p28-rls-fixture.ts
@e2e/workspace-switcher-jwt-propagation.spec.ts

<interfaces>
<!-- Contracts the executor needs without exploring further. Extracted from 31-04 + 31-05 outputs and existing code. -->

From `src/types/onboarding-step.ts` (created in 31-04):
```typescript
export type StepType =
  | 'welcome' | 'intro_card' | 'medication' | 'goals'
  | 'body_stats' | 'consent' | 'doctor_invite' | 'tour';

export interface OnboardingStepNode {
  id: string;
  type: StepType;
  skip?: boolean;
  custom?: { title?: string; body?: string; image_url?: string };
}
```

From `supabase/migrations/*p31_04_*` (31-04 SECDEFs):
- `public.mark_onboarding_complete()` returns `void`; writes `UPDATE public.profiles SET completed_onboarding_at = now() WHERE id = auth.uid() AND completed_onboarding_at IS NULL;` (idempotent).
- `public.profiles.completed_onboarding_at timestamptz null` (NEW column).
- `public.org_onboarding_flows (id, org_id, steps jsonb, version, is_active, created_by, created_at)` with RLS SELECT permitted for any `org_members` of the same `org_id`.

From Phase 29 (already live):
- `public.profiles.primary_org_id uuid null references organizations(id)`.

From `src/lib/supabase.ts`:
```typescript
export const supabase: SupabaseClient;
```

From `src/components/onboarding/OnboardingFlow.tsx`:
- Component signature: `OnboardingFlow({ onCancel, onComplete }: OnboardingFlowProps)`.
- `TOTAL_STEPS = 8`; renders steps 0..7 (DEFAULT_STEPS path).
- `complete()` handler at step 7 builds the `User` object and calls `setUser` + `upsertWeight` + `onComplete()`.

From `src/App.tsx`:
- `selectView()` returns `'dashboard'` when `opts.user` (zustand `user`) is truthy.
- For invited patients in the existing flow, the dashboard renders before any onboarding gate — there is currently no signed-in onboarding mount point. This plan adds one.
- `signedIn` slice on the store exposes `signedIn.user` (Supabase auth user) + `signedIn.session`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useOrgOnboardingFlow hook + unit tests</name>
  <files>src/lib/onboarding-builder/use-org-onboarding-flow.ts, src/lib/onboarding-builder/__tests__/use-org-onboarding-flow.test.ts</files>
  <behavior>
    - Test 1 (anonymous → 'consumer'): when supabase.auth.getUser() returns null, hook returns status 'consumer' (orgId null, orgName null, steps null). Local-first invariant: NO blocking on backend.
    - Test 2 (signed-in but no primary_org_id → 'consumer'): when profiles row has primary_org_id=null, returns 'consumer'.
    - Test 3 (signed-in with active org flow → 'org'): when join returns flow_steps array, returns status 'org' with orgId + orgName + steps populated.
    - Test 4 (signed-in with completed_onboarding_at set → 'completed'): when profiles.completed_onboarding_at IS NOT NULL, returns 'completed' regardless of whether org or flow exist (D-14 + D-15 first-clinic-wins).
    - Test 5 (signed-in, org resolved but no active flow → 'consumer'): when join returns primary_org_id but flow_steps is null (org has not configured a flow), falls back to 'consumer'.
    - Test 6 (loading state): initial render returns status 'loading' before the Supabase query resolves.
    - Test 7 (network error → 'consumer'): when supabase query throws, fail open to 'consumer' (per T-31-06-04 availability mitigation — patient is never blocked).
  </behavior>
  <action>
    Create directory `src/lib/onboarding-builder/` (if not already created by 31-04/31-05). Create the hook file with this exact public API per locked decision #1:

    ```
    export interface OrgOnboardingFlowState {
      status: 'loading' | 'consumer' | 'org' | 'completed';
      orgId: string | null;
      orgName: string | null;
      steps: OnboardingStepNode[] | null;
    }
    export function useOrgOnboardingFlow(): OrgOnboardingFlowState;
    ```

    Implementation: useEffect on mount calls `supabase.auth.getUser()`; if no user → set state to 'consumer' immediately (no DB round-trip). If user present, FIRST do a thin SELECT of just `profiles.primary_org_id` + `profiles.completed_onboarding_at` (1 small row). If `completed_onboarding_at IS NOT NULL` → return 'completed' immediately (no further query). If `primary_org_id IS NULL` → return 'consumer' immediately (no further query — closes plan-checker WARNING 2: avoids unnecessary org_onboarding_flows JOIN for signed-in consumers who never joined a clinic). Only if `primary_org_id IS NOT NULL` AND `completed_onboarding_at IS NULL` do we fire the second query for org + active flow:

    Use supabase-js builder form (preferred over raw SQL for type safety): `supabase.from('profiles').select('primary_org_id, completed_onboarding_at, primary_org:organizations!profiles_primary_org_id_fkey(id, name, active_flow:org_onboarding_flows!inner(steps))').eq('id', user.id).eq('primary_org.active_flow.is_active', true).maybeSingle()`. If the nested-filter form is rejected by Postgrest at runtime (it can be — nested `.eq` on a related table inside select is supported but sometimes returns the row WITH unfiltered nested array), fall back to TWO sequential queries: (1) SELECT profiles.primary_org_id, completed_onboarding_at, organizations.name; (2) if primary_org_id present, SELECT steps FROM org_onboarding_flows WHERE org_id = $1 AND is_active = true LIMIT 1. Pick whichever resolves on the live schema during execution; unit tests mock the supabase client so either path is testable.

    Branch logic (per locked decision #1):
    - completed_onboarding_at IS NOT NULL → 'completed'
    - else primary_org_id resolved AND active flow row exists → 'org' (return steps, orgName)
    - else → 'consumer'
    - Any thrown error → log via console.warn + return 'consumer' (fail-open per T-31-06-04)

    Hook MUST NOT re-fetch on every render; use useEffect with empty dep array. State updates use a single setState to avoid intermediate inconsistent reads.

    Anti-pattern: do NOT introduce a new VITE_ env key (per [[reference_vite_static_env_inlining]]) — use the existing `supabase` export from `src/lib/supabase.ts`.

    Tests: create `__tests__/use-org-onboarding-flow.test.ts` using vitest + @testing-library/react renderHook (already a dep). Mock the supabase client with vi.mock('@/lib/supabase', ...) returning a chainable builder stub. Use `it.skip('description [DEFERRED — reason]', ...)` not `it.fixme` per [[reference_vitest_skip_fixme]]. Assert each of the 7 behaviors above. Use `waitFor` to bridge the async useEffect.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npx vitest run src/lib/onboarding-builder/__tests__/use-org-onboarding-flow.test.ts</automated>
  </verify>
  <done>
    File `src/lib/onboarding-builder/use-org-onboarding-flow.ts` exports `useOrgOnboardingFlow` and `OrgOnboardingFlowState`. All 7 unit tests pass. `npx tsc --noEmit -p tsconfig.app.json` is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire render branch in OnboardingFlow.tsx + dashboard-entry gate in App.tsx + mark_onboarding_complete call</name>
  <files>src/components/onboarding/OnboardingFlow.tsx, src/App.tsx</files>
  <behavior>
    - Test 1: OnboardingFlow with hook returning 'consumer' renders unchanged DEFAULT_STEPS path (TOTAL_STEPS=8, existing copy "Welcome in.", existing illustrations). Use @testing-library/react render + screen.getByText. Mock the hook to return {status:'consumer', orgId:null, orgName:null, steps:null}.
    - Test 2: OnboardingFlow with hook returning 'org' (with a 3-step flow whose welcome step has custom.title='Welcome to Acme Health') renders "Welcome to Acme Health" copy and a 3-step progress indicator (NOT TOTAL_STEPS=8). Mock the hook accordingly.
    - Test 3: OnboardingFlow with hook returning 'completed' renders nothing (returns null) — the App.tsx gate is what makes this branch unreachable in production, but the component must self-defend.
    - Test 4: OnboardingFlow with hook returning 'loading' renders a small skeleton (reuse existing Skeleton primitive) for ≤ 1 frame.
    - Test 5 (App.tsx integration is exercised via the e2e in Task 3 — no separate unit test).
  </behavior>
  <action>
    **In `src/components/onboarding/OnboardingFlow.tsx`:**

    Add `import { useOrgOnboardingFlow } from '@/lib/onboarding-builder/use-org-onboarding-flow';` and `import { supabase } from '@/lib/supabase';`. At the top of the component (before existing `useState(0)`), insert: `const flowState = useOrgOnboardingFlow();`.

    Render branch placement: AFTER the existing `useEffect(() => { track('onboarding_started'); }, [])` and BEFORE the return statement. Switch on `flowState.status`:
    - 'loading' → render a Skeleton placeholder using the existing `<Skeleton>` primitive from `src/components/ui/`.
    - 'completed' → return null (App.tsx gate already short-circuits in production; this is a defense-in-depth no-op).
    - 'org' → render the org-flow render path described below.
    - 'consumer' or default → render the existing DEFAULT_STEPS path UNCHANGED.

    **Org-flow render path:** Reuse the existing card chrome (max-w-[560px], rounded-[28px], banner gradient) and `<ProgressIndicator>` primitive — only the inner step contents and TOTAL count change. Iterate over `flowState.steps` (the org's OnboardingStepNode[] array). For each step, render based on `step.type`:
    - `welcome` → use `step.custom?.title ?? 'Welcome'` as H1, `step.custom?.body ?? default_body` as subtitle. Show OnboardWelcome illustration. Bold welcome text with orgName fallback: title prop = step.custom?.title ?? `Welcome to ${flowState.orgName}`.
    - `intro_card` → render `step.custom?.title`, `step.custom?.body`, and `step.custom?.image_url` if present (use `<img>` with `alt={step.custom?.title}`).
    - `medication`, `body_stats`, `goals`, `consent`, `doctor_invite`, `tour` → for v1, reuse the existing DEFAULT_STEPS form contents for the matching step (the canonical LeanShot input collection — medication form, body form, etc.). These are skippable iff `step.skip === true` AND the type is in the skippable set per D-09 (welcome, intro_card, goals, body_stats, doctor_invite, tour); medication + consent are never skippable regardless of `step.skip`. Skipped steps advance to next on render without showing UI.

    ProgressIndicator total prop is the rendered (non-skipped) step count, not the array length.

    **Final-step completion handler:** The existing `complete()` builds the `User` object and calls `setUser` + `upsertWeight` + `onComplete()`. WRAP this so that — REGARDLESS of which path was rendered — after the local state mutations succeed, if a signed-in supabase user exists, call `await supabase.rpc('mark_onboarding_complete')` and swallow errors (log via console.warn). Local store update happens first; SECDEF call is best-effort (the column is set on next sign-in attempt anyway since `mark_onboarding_complete` is idempotent). For the consumer DEFAULT_STEPS path: the SECDEF call still fires if the user has been promoted via Phase 5 signup. Anonymous users skip the SECDEF call.

    For the org-flow path: the medication/body/etc. forms feed the same DraftState the consumer path uses; this is intentional — onboarding produces a single User regardless of which step ordering the clinic chose. Goals + intro_card-only flows (rare; clinic skipped medication step — illegal because medication is mandatory per D-09, so `_validate_onboarding_steps` rejects such configs at save time) cannot occur.

    DEFAULT_STEPS extraction: extract the existing inline step content into a module-level const if it makes the org-flow branch cleaner; otherwise leave inline and ensure the existing consumer-path render is byte-identical (run the visual regression Phase 13 baselines in CI to catch drift).

    **In `src/App.tsx`:**

    Locate the dashboard render branch (around the `view === 'dashboard'` clause that returns `<AppShell>...`). BEFORE rendering AppShell, call `const orgFlow = useOrgOnboardingFlow();`. If `orgFlow.status === 'org'`, render `<Onboarding onCancel={...} onComplete={...} />` (the existing lazy-loaded OnboardingFlow Suspense boundary) INSTEAD of the AppShell. onComplete handler simply does NOT change view (the hook will return 'completed' on the next render after mark_onboarding_complete fires).

    Caveat: the new hook fires for EVERY dashboard mount; this is one Supabase round-trip on every signed-in render. Mitigate by using a top-level React Context cache OR by reading from a Zustand slice keyed on `signedIn.user.id` so subsequent mounts within the same session don't re-fetch. For v1 of this plan, the one-time fetch on dashboard mount is acceptable — measure in field telemetry; optimize only if it shows up. Document this choice in the executor SUMMARY.

    For the `view === 'onboarding'` consumer-signup path (existing `setView('onboarding')` from Marketing), the hook still runs but returns 'consumer' for anonymous users → DEFAULT_STEPS path renders identically. No change needed there.

    Anti-patterns (per planning_context):
    - Do NOT modify Phase 9 `OrgInviteAcceptance` or `link_org_patient` SECDEF — first-clinic-wins is emergent from `completed_onboarding_at IS NOT NULL` check, NOT a special case in invite-accept.
    - Do NOT track per-(user, org) completion — D-14/D-15 explicitly chose single `profiles.completed_onboarding_at`.
    - Do NOT break the consumer DEFAULT_STEPS path — local-first must continue to work without backend (CLAUDE.md hard constraint).

    Tests (Task 2): add `src/components/onboarding/__tests__/OnboardingFlow.test.tsx` (or extend existing if present). Mock `useOrgOnboardingFlow` via vi.mock; assert the 4 unit-test behaviors above.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npx vitest run src/components/onboarding &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; bash scripts/assert-clinic-bundle-budget.sh</automated>
  </verify>
  <done>
    OnboardingFlow render-branch unit tests pass. tsc green. Bundle budget script passes (clinic chunk under 50 kB gz raised by 31-01; index unaffected because OnboardingFlow remains lazy-loaded). Visual regression Phase 13 baselines still pass (consumer path byte-identical).
  </done>
</task>

<task type="auto">
  <name>Task 3: Playwright e2e spec (4 scenarios) + playwright.config.ts P31 project entry</name>
  <files>e2e/patient-org-onboarding.spec.ts, playwright.config.ts</files>
  <action>
    **In `playwright.config.ts`:**

    Add P31 conditional-project pattern per [[reference_playwright_conditional_project_argv]]. After the existing P30_OPT_IN constant, add:

    `const P31_OPT_IN = process.env.PLAYWRIGHT_RUN_P31 === '1';`

    Add `/e2e\/patient-org-onboarding\.spec\.ts$/` to the chromium project's `testIgnore` array (so default CI runs do not pick it up without the env var). Then append a conditional project entry mirroring the P30 shape:

    ```
    ...(P31_OPT_IN ? [{ name: 'p31', testMatch: [/e2e\/patient-org-onboarding\.spec\.ts$/], use: { ...devices['Desktop Chrome'] } }] : [])
    ```

    Do NOT use `process.argv.some(...)` for detection — that fails in worker subprocesses per the reference. Env-var only.

    **In `e2e/patient-org-onboarding.spec.ts`:**

    Follow the established pattern from `e2e/workspace-switcher-jwt-propagation.spec.ts` for addInitScript session seeding (per [[reference_playwright_state_seeding]]) and from `src/lib/__tests__/_fixtures/p28-rls-fixture.ts` for service-role admin fixture creation.

    File-scoped slug prefix per [[feedback_rls_per_file_slug_prefix]]:
    `const TEST_SLUG_PREFIX = 'p31_e2e_' + path.basename(__filename).replace(/[^a-z0-9]/gi, '_') + '_' + Date.now();`

    **beforeAll (admin client setup):**
    - Create admin client with SUPABASE_SERVICE_ROLE_KEY (per existing fixture pattern).
    - Create org X: insert into organizations (slug = TEST_SLUG_PREFIX + '_orgX', name = 'Acme Health Test').
    - Create org X owner user via `admin.auth.admin.generateLink({ type: 'magiclink', email: TEST_SLUG_PREFIX + '_owner@example.test' })` then PLAIN fetch to `${SUPABASE_URL}/auth/v1/verify?token_hash=...&type=magiclink` per [[reference_rls_fixture_gotrueclient_flake]] (ES256-compat).
    - Insert org_members row linking owner to orgX as 'owner'.
    - As owner (via supabase-js with owner's session token): call `supabase.rpc('save_org_onboarding_flow', { p_org_id: orgX.id, p_steps: [{ id: 'w1', type: 'welcome', custom: { title: 'Welcome to Acme Health', body: 'Your treatment journey starts here.' }}, { id: 'm1', type: 'medication' }, { id: 'c1', type: 'consent' }]})`. Verify the row was inserted via admin-client SELECT.
    - Create patient user via same admin.generateLink + plain-fetch /auth/v1/verify pattern.
    - Insert org_patient_links row linking patient to orgX.
    - Admin UPDATE on profiles: SET primary_org_id = orgX.id, completed_onboarding_at = null WHERE id = patient.id.

    **Test 1 — invited-patient sees org flow:**
    - Use `page.addInitScript()` to inject the patient's Supabase session into localStorage under key `sb-leanshot-auth` (per [[reference_playwright_state_seeding]]). Diagnostic seam hooks localStorage.setItem directly BEFORE goto.
    - `await page.goto('/')` — App.tsx will hydrate, see signedIn.user + primary_org_id + no completed_onboarding_at, render OnboardingFlow with status='org'.
    - Assert: `await expect(page.getByText('Welcome to Acme Health')).toBeVisible({ timeout: 10000 })` — proves the org's custom welcome text rendered, NOT the canonical "Welcome in." consumer copy.

    **Test 2 — completion writes profiles.completed_onboarding_at:**
    - Continuing from Test 1's session, click through the org flow's steps (welcome → medication → consent), filling required fields. Click final "Open dashboard" / "Complete" button.
    - Wait for navigation to dashboard (or for the OnboardingFlow to unmount).
    - Use admin client to query `SELECT completed_onboarding_at FROM profiles WHERE id = patient.id` and assert the timestamp is non-null AND within the last 60 seconds.

    **Test 3 — second visit skips onboarding:**
    - With `completed_onboarding_at` now set (from Test 2), reload the page: `await page.reload()`.
    - Assert: OnboardingFlow welcome text is NOT visible: `await expect(page.getByText('Welcome to Acme Health')).not.toBeVisible({ timeout: 5000 })`.
    - Assert: dashboard surface IS visible (use a stable dashboard testid like `data-testid="app-shell"` or `getByRole('navigation', { name: 'Primary' })` — whichever the existing AppShell exposes).

    **Test 4 — multi-clinic invite does not re-trigger onboarding (D-15 first-clinic-wins):**
    - Use admin client: create org Y (slug = TEST_SLUG_PREFIX + '_orgY', name = 'Beta Clinic Test'). Create orgY owner. Save a different active flow for orgY (e.g., welcome.custom.title = 'Welcome to Beta Clinic'). Insert org_patient_links linking patient to orgY (SECOND org for same patient).
    - Reload patient session: `await page.reload()`.
    - Assert: dashboard still visible (no re-onboarding triggered). Assert neither "Welcome to Acme Health" nor "Welcome to Beta Clinic" copy is visible. This proves the `completed_onboarding_at IS NOT NULL` check is the gate — NOT special-case logic in link_org_patient.

    **afterAll:**
    - Delete org_patient_links rows for both orgs.
    - Delete org_onboarding_flows rows for both orgs.
    - Delete org_members rows for both orgs.
    - Delete organizations rows for both orgs.
    - Delete patient + both owner auth users via admin.auth.admin.deleteUser().
    - Wrap each cleanup in try/catch so a partial-failure case doesn't poison the next run.

    **Spec-level constraints:**
    - All admin queries use service-role; all patient queries use the patient session.
    - Use `it.skip` not `it.fixme` for any deferred subtests (per [[reference_vitest_skip_fixme]] — applies to Playwright `test.skip` as well to keep grep-pattern uniform).
    - File MUST be runnable in isolation: `PLAYWRIGHT_RUN_P31=1 npx playwright test --project=p31`.
    - Document the SUPABASE_URL + SUPABASE_ANON_KEY + SUPABASE_SERVICE_ROLE_KEY env requirements in a comment at the top of the file.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; PLAYWRIGHT_RUN_P31=1 npx playwright test --project=p31 e2e/patient-org-onboarding.spec.ts --reporter=list</automated>
  </verify>
  <done>
    All 4 Playwright scenarios pass against linked Supabase project. Default `npx playwright test` (chromium project) does NOT pick up the spec (verified by `npx playwright test --list | grep -c patient-org-onboarding` returning 0). Cleanup leaves no test rows in organizations / org_members / org_patient_links / org_onboarding_flows tables (verified via `supabase db query --linked "SELECT count(*) FROM organizations WHERE slug LIKE 'p31_e2e_%'"` returning 0).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → SECDEF | patient-facing OnboardingFlow.tsx calls `mark_onboarding_complete()` SECDEF; client cannot UPDATE profiles.completed_onboarding_at directly (RLS deny) |
| client → DB SELECT | patient-facing useOrgOnboardingFlow hook SELECTs from profiles + organizations + org_onboarding_flows; RLS enforces own-profile + org-member-of policies |
| client (localStorage) → trust | Zustand-cached completed_onboarding_at IS UX cache only; render-gate trusts only the DB read from the hook |
| Edge / Postgres | DB validators (`_validate_onboarding_steps`) + RLS are the security floor; client cannot bypass by crafting steps blob |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06-01 | Tampering | OnboardingFlow.tsx + Zustand store | mitigate | `mark_onboarding_complete()` SECDEF operates on `auth.uid()` only; client-side state mutations (Zustand setUser) are UX-only; the next mount's hook re-reads from DB. RLS denies direct UPDATE on profiles.completed_onboarding_at. Verified by Test 2 (admin-client SELECT after completion). |
| T-31-06-02 | Information Disclosure | useOrgOnboardingFlow query | mitigate | RLS on org_onboarding_flows (SELECT permitted only for org_members of same org_id, shipped in 31-04). Patient only sees flow for their OWN primary_org_id (single JOIN constrained to auth.uid()'s profile row). Cross-tenant proof shipped in 31-04's `rls-org-onboarding-flows.test.ts`. |
| T-31-06-03 | Denial of Service | _validate_onboarding_steps trigger | mitigate | DB validator (31-04) rejects malformed/oversized steps blob at save time; org_onboarding_flows.steps jsonb has implicit Postgres size limits. Patient render path is bounded by the validated shape — clinic owner cannot craft a megabyte payload that would crash the patient client. |
| T-31-06-04 | Availability | useOrgOnboardingFlow hook (network failure) | mitigate | Hook catches all errors and returns 'consumer' (fail-open). Patient sees DEFAULT_STEPS path and can still complete local-first onboarding even with Supabase down. CLAUDE.md hard constraint preserved. |
| T-31-06-05 | Elevation of Privilege | mark_onboarding_complete SECDEF | mitigate | SECDEF body restricts UPDATE to `WHERE id = auth.uid() AND completed_onboarding_at IS NULL` (idempotent). Cannot mark another user's profile. Verified by 31-04 RLS test suite. |
| T-31-06-06 | Tampering | D-15 first-clinic-wins enforcement | mitigate | First-clinic-wins is emergent from `completed_onboarding_at IS NOT NULL` gate at mount — NOT a brittle special-case in link_org_patient. Cannot be bypassed by accepting more invites. Verified by Test 4 (multi-clinic invite still shows dashboard). |
</threat_model>

<verification>
- Source audit: ORG-13 covered (single REQ for this plan; covered end-to-end by hook + render branch + e2e).
- D-10 (REPLACE pattern): the org's saved flow REPLACES DEFAULT_STEPS for invited patients — verified by Test 1 asserting "Welcome to Acme Health" (NOT "Welcome in.").
- D-14 (first-sign-in only): `completed_onboarding_at IS NULL` gate verified by Test 3 (second visit skips).
- D-15 (first-clinic-wins): subsequent invites do NOT re-trigger — verified by Test 4.
- D-09 (step library): org flow rendering uses the same canonical step components (medication/consent/etc.) — clinic only customizes ordering + welcome/intro_card text.
- Local-first invariant from CLAUDE.md: T-31-06-04 mitigation (network failure → 'consumer') AND Task 1 unit Test 1 (anonymous → 'consumer') AND Task 2 unit Test 1 (consumer path byte-identical) all defend this.
- Bundle ceiling: OnboardingFlow.tsx grows by ~hook import + render-branch logic (~1-2 kB gz); App.tsx gains the hook import (small); both are lazy-loaded in App.tsx's Suspense boundaries so index chunk is unaffected. Clinic chunk budget raised to 50 kB gz by 31-01 — verified by `bash scripts/assert-clinic-bundle-budget.sh`.
- Schema-push: NONE. This plan ships no migrations. All schema is owned by 31-04 (already pushed in Wave 2 BLOCKING task).
- Wave gate: 31-04 + 31-05 must be merged before this plan starts (executor verifies via `git log --oneline -- supabase/migrations/*p31_04* src/components/clinic/settings/OnboardingTab.tsx | head -2` returning 2 commits).
</verification>

<success_criteria>
- `npx vitest run src/lib/onboarding-builder/__tests__/use-org-onboarding-flow.test.ts` — all 7 tests green.
- `npx vitest run src/components/onboarding` — all OnboardingFlow render-branch tests green (including unchanged consumer-path tests if present).
- `npx tsc --noEmit -p tsconfig.app.json` — green; no type drift from new hook + render branch + App.tsx gate.
- `bash scripts/assert-clinic-bundle-budget.sh` — passes under the 50 kB gz ceiling raised by 31-01.
- `PLAYWRIGHT_RUN_P31=1 npx playwright test --project=p31 e2e/patient-org-onboarding.spec.ts` — all 4 scenarios pass.
- `npx playwright test --list 2>/dev/null | grep -c patient-org-onboarding` returns 0 (default suite does NOT include the env-gated spec).
- `supabase db query --linked "SELECT count(*) FROM organizations WHERE slug LIKE 'p31_e2e_%'"` returns 0 after afterAll cleanup (no test pollution).
- Existing Phase 13 visual regression baselines for the consumer OnboardingFlow path still pass — `npx playwright test --grep onboarding-visual` green (proves byte-identical consumer render).
- Existing Phase 5/6 auth-flow e2e specs (auth-signup-verify-signin.spec.ts, etc.) still green — proves the App.tsx dashboard-entry gate did not regress signed-in dashboard mount for consumer users.
</success_criteria>

<output>
After completion, create `.planning/phases/31-white-label-path-based-org-roles-clinic-onboarding-builder/31-06-SUMMARY.md` documenting:
- The hook fetch strategy chosen (single nested join vs two sequential queries) and any Postgrest quirks encountered.
- Whether the dashboard-mount hook was left as one-fetch-per-render or wrapped in a Zustand cache (decision rationale).
- DEFAULT_STEPS extraction outcome (left inline vs extracted to module-level const).
- Any e2e flake observed + how addressed (per [[feedback_defer_then_batch_fix_pattern]]: defer via `test.skip` with descriptive message + entry in `.planning/deferred-tests.md` if applicable).
- Bundle delta: clinic + index chunks before/after in kB gz.
- Confirmation that 31-04 + 31-05 producers were used unchanged (no patches to OrgInviteAcceptance or link_org_patient).
</output>
