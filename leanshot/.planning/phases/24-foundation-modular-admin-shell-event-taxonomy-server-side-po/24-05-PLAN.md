---
phase: 24-foundation-modular-admin-shell-event-taxonomy-server-side-po
plan: 05
type: execute
wave: 2
depends_on:
  - 24-01
  - 24-03
files_modified:
  - src/components/admin/SetupTotpPage.tsx
  - src/components/admin/StepUpTotpPage.tsx
  - src/components/admin/AdminLayout.tsx
  - src/components/admin/SettingsModule.tsx
  - src/lib/admin/totp.ts
  - src/lib/admin/__tests__/totp.test.ts
  - src/lib/supabase.ts
  - supabase/migrations/20260518000008_admin_totp_rpcs.sql
  - supabase/functions/admin-reset-totp/index.ts
  - supabase/functions/admin-reset-totp/admin-reset-totp.test.ts
  - e2e/admin-mfa-enroll.spec.ts
  - e2e/admin-mfa-middleware.spec.ts
autonomous: true
requirements: [ADMIN-03]

must_haves:
  truths:
    - "On first /admin/* request after Phase 24 deploy, AdminLayout checks profiles.has_totp; if false, renders SetupTotpPage redirect per D-06 hard-cut (no grace window)"
    - "SetupTotpPage uses supabase.auth.mfa.enroll({factorType:'totp'}) per D-07; renders QR code from data.totp.qr_code"
    - "After successful enroll + challenge + verify, server sets profiles.has_totp=true AND issues 10 HMAC-hashed backup codes into admin_backup_codes (one-time display)"
    - "On subsequent admin sessions, AdminLayout checks auth.jwt() ->> 'aal' === 'aal2'; if not, renders StepUpTotpPage (per-session step-up per D-09)"
    - "admin.reset_totp(target_user_id) SECURITY DEFINER RPC clears MFA factor + sends new enrollment email + writes audit_log; gated to superadmin role per D-08"
    - "admin-reset-totp Edge Function wraps the RPC + emails via Resend"
    - "Backup code consumption RPC marks used_at + denies re-use"
    - "e2e tests: TOTP enrollment flow shows QR + backup codes; middleware redirects unenrolled admin"
  artifacts:
    - "src/components/admin/SetupTotpPage.tsx"
    - "src/components/admin/StepUpTotpPage.tsx"
    - "src/components/admin/SettingsModule.tsx (augmented — shows enrolled-MFA status + 'Generate new backup codes')"
    - "src/components/admin/AdminLayout.tsx (edited — adds has_totp + aal2 gate)"
    - "src/lib/admin/totp.ts (client helper for enroll/challenge/verify + backup-code gen)"
    - "src/lib/supabase.ts (edited — assertAal2 helper)"
    - "supabase/migrations/20260518000008_admin_totp_rpcs.sql (issue_backup_codes, consume_backup_code, reset_totp)"
    - "supabase/functions/admin-reset-totp/index.ts + .test.ts"
    - "e2e/admin-mfa-enroll.spec.ts, e2e/admin-mfa-middleware.spec.ts"
  key_links:
    - "admin_backup_codes table (Plan 24-01) -> issue_backup_codes RPC writes -> SetupTotpPage displays once"
    - "admin_backup_codes -> consume_backup_code RPC marks used_at"
    - "supabase.auth.mfa -> SetupTotpPage QR -> profiles.has_totp=true"
    - "AdminLayout has_totp + aal2 gate -> SetupTotpPage / StepUpTotpPage"
    - "log_admin_action (Plan 24-01) -> called from reset_totp RPC -> audit_logs row"
---

<objective>
Ship the 2FA TOTP path end-to-end per D-06..09. Add an MFA-enrollment landing page that uses Supabase Auth MFA APIs to enroll a TOTP factor + issue 10 HMAC-hashed backup codes. Add a step-up page for subsequent sessions. Augment AdminLayout (refactored in Plan 24-03) to gate on `has_totp` + JWT `aal` claim per Pattern S1 dual-layer (client gate + RPC check). Add a superadmin `admin.reset_totp(target)` RPC + Edge Function that clears the user's MFA factor + emails a new enrollment URL + writes audit log per D-08. e2e tests prove enrollment flow + middleware redirect.

Purpose: HIPAA-grade admin posture. Without this, the modular admin shell from Plan 24-03 is not safe to expose. ADMIN-03 closes here.
Output: 4 new components + 1 lib helper + 1 supabase.ts edit + 1 migration + 1 Edge Function + 2 e2e specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-CONTEXT.md
@.planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-RESEARCH.md
@src/lib/supabase.ts
@src/components/admin/AdminLayout.tsx
@src/components/admin/SettingsModule.tsx
@supabase/functions

<interfaces>
From Plan 24-01:
- profiles.has_totp boolean
- profiles.admin_role enum
- admin_backup_codes table (id, user_id, code_hash, used_at)
- public.is_admin_at_least(min_role)
- public.log_admin_action(p_action_name, p_target_user_id, p_table_name, p_row_pk, p_before, p_after) -> uuid

From Plan 24-03:
- AdminLayout already fetches admin_role
- AdminShell + ADMIN_MODULES manifest

From Supabase Auth (verified Context7 2026-05-17):
- supabase.auth.mfa.enroll({ factorType: 'totp', issuer, friendlyName }) -> { data: { id, totp: { qr_code } }, error }
- supabase.auth.mfa.challenge({ factorId }) -> { data: { id }, error }
- supabase.auth.mfa.verify({ factorId, challengeId, code }) -> { data, error } (session auto-upgrades to aal2 on success)
- supabase.auth.mfa.listFactors() -> { data: { totp: Factor[] }, error }
- supabase.auth.mfa.unenroll({ factorId }) -> { data, error }
- JWT claim `aal`: 'aal1' (one factor) or 'aal2' (post-MFA)

From Resend integration (v1.2):
- supabase/functions/_shared/email-router.ts OR existing Resend send helper — use whatever v1.2 ships; do not invent.

Memory references:
- `[[reference_resend_phase9_wiring]]` — RESEND_API_KEY + RESEND_FROM; sandbox `onboarding@resend.dev` for self-test only; prod uses `LeanShot <noreply@app.leanshot.app>` (verified domain).
- `[[reference_phase4_linkidentity_correction]]` — `linkIdentity({email,password})` is OAuth-only; use `updateUser` for email/password promotion.
- `[[reference_supabase_auth_traps]]` — hash-route SPA + implicit-grant trap.
- `[[reference_playwright_state_seeding]]` — use `page.addInitScript` for state seeding; never `page.goto + page.evaluate(seed) + reload`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TOTP client helper + backup-code generation (Web Crypto HMAC)</name>
  <files>
    src/lib/admin/totp.ts,
    src/lib/admin/__tests__/totp.test.ts,
    src/lib/supabase.ts
  </files>
  <read_first>
    src/lib/supabase.ts (existing supabase client init),
    .planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-RESEARCH.md (Pattern 4 + Pitfall 6 + Code Examples Browser MFA)
  </read_first>
  <behavior>
    - Test 1: `generateBackupCodes(10)` returns array of 10 unique strings, each `^[A-Z0-9]{4}-[A-Z0-9]{4}-[A-Z0-9]{4}$`-shaped (12 chars + 2 dashes = 14).
    - Test 2: `hashBackupCode(code, secret)` produces deterministic SHA-256 HMAC hex of `secret`+`code` (test against known input/output).
    - Test 3: `assertAal2(session)` returns true when `auth.jwt() ->> 'aal'` equals 'aal2'; throws otherwise.
    - Test 4: `enrollTotp({ issuer, friendlyName })` calls `supabase.auth.mfa.enroll` with factorType:'totp'.
    - Test 5: `verifyTotp({ factorId, code })` runs the challenge→verify flow.
  </behavior>
  <action>
    Create `src/lib/admin/totp.ts`:
    ```typescript
    import { supabase } from '@/lib/supabase';

    const ALPHA = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ23456789'; // omit 0,1,O,I,L to avoid OCR/typo confusion

    export function generateBackupCodes(count: number): string[] {
      const buf = new Uint8Array(count * 12);
      crypto.getRandomValues(buf);
      const codes = new Set<string>();
      let i = 0;
      while (codes.size < count) {
        const c = Array.from({ length: 12 }, () => ALPHA[buf[i++ % buf.length] % ALPHA.length]).join('');
        codes.add(`${c.slice(0,4)}-${c.slice(4,8)}-${c.slice(8,12)}`);
      }
      return Array.from(codes);
    }

    export async function hashBackupCode(code: string, secret: string): Promise<string> {
      const enc = new TextEncoder();
      const key = await crypto.subtle.importKey('raw', enc.encode(secret), { name: 'HMAC', hash: 'SHA-256' }, false, ['sign']);
      const sig = await crypto.subtle.sign('HMAC', key, enc.encode(code));
      return Array.from(new Uint8Array(sig)).map((b) => b.toString(16).padStart(2, '0')).join('');
    }

    export async function enrollTotp(opts: { issuer: string; friendlyName: string }) {
      return supabase.auth.mfa.enroll({ factorType: 'totp', issuer: opts.issuer, friendlyName: opts.friendlyName });
    }

    export async function verifyTotp(opts: { factorId: string; code: string }) {
      const ch = await supabase.auth.mfa.challenge({ factorId: opts.factorId });
      if (ch.error) return { data: null, error: ch.error };
      return supabase.auth.mfa.verify({ factorId: opts.factorId, challengeId: ch.data.id, code: opts.code });
    }
    ```

    Edit `src/lib/supabase.ts` — add helper:
    ```typescript
    export async function assertAal2(): Promise<boolean> {
      const { data } = await supabase.auth.getSession();
      const aal = (data.session?.access_token ? parseJwtAal(data.session.access_token) : null);
      return aal === 'aal2';
    }
    function parseJwtAal(token: string): string | null {
      try {
        const [, payload] = token.split('.');
        const json = JSON.parse(atob(payload.replace(/-/g, '+').replace(/_/g, '/')));
        return typeof json.aal === 'string' ? json.aal : null;
      } catch { return null; }
    }
    ```

    Test backup-code uniqueness, format, and HMAC determinism. Mock supabase.auth.mfa for enroll/verify tests.

    Backup-code SECRET source: a single SUPABASE_VAULT key. Plan 24-05 uses `import.meta.env.VITE_BACKUP_CODE_PEPPER` is WRONG (would ship secret to browser). Instead — the HMAC must happen SERVER-SIDE in the issue RPC, not browser-side. Revise: `hashBackupCode` is server-side only; in browser we just display the plaintext code after issue. The RPC `admin.issue_backup_codes` (Task 3) computes the HMAC using `vault.secrets` key.

    Therefore: `hashBackupCode` and SECRET handling live INSIDE the Postgres RPC (Task 3), not in `totp.ts`. Update Task 1 to drop server-side helpers — keep only `generateBackupCodes`, `enrollTotp`, `verifyTotp`, `assertAal2`. The PG-side RPC accepts the plaintext codes and HMACs them on the server before insert.

    Revised behavior for Test 2: skip (no client-side hashing). Replace with: Test 2 = `generateBackupCodes(10)` returns distinct codes across 100 calls (statistical sanity).
  </action>
  <verify>
    <automated>cd leanshot && npm test -- src/lib/admin/__tests__/totp.test.ts --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `generateBackupCodes(10)` returns 10 unique codes in `XXXX-XXXX-XXXX` form.
    - `assertAal2()` reads JWT correctly.
    - enroll + verify mock-test green.
  </acceptance_criteria>
  <done>
    Client helper + supabase.ts gate live.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build SetupTotpPage + StepUpTotpPage + augment AdminLayout + augment SettingsModule</name>
  <files>
    src/components/admin/SetupTotpPage.tsx,
    src/components/admin/StepUpTotpPage.tsx,
    src/components/admin/AdminLayout.tsx,
    src/components/admin/SettingsModule.tsx
  </files>
  <read_first>
    src/lib/admin/totp.ts (from Task 1),
    src/components/admin/AdminLayout.tsx (from Plan 24-03 — already has admin_role fetch),
    src/components/admin/SettingsModule.tsx (from Plan 24-03 — stub),
    .planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-RESEARCH.md (Code Examples Browser MFA + Vercel middleware)
  </read_first>
  <action>
    Create `SetupTotpPage.tsx`:
    - Calls `enrollTotp({ issuer: 'LeanShot Admin', friendlyName: 'Authenticator' })`.
    - Renders `<img src={data.totp.qr_code} alt="Scan with your authenticator app" />` (per Supabase MFA — the URI IS the QR SVG data-URI).
    - Input for OTP code; on submit, `verifyTotp({ factorId, code })`.
    - On success, call the RPC `admin.issue_backup_codes` (Task 3), display the 10 backup codes ONCE with a copy-to-clipboard button + a checkbox "I have saved these codes" gate.
    - On checkbox + confirm, set `profiles.has_totp = true` via RPC `admin.set_has_totp_true()` (Task 3) and redirect to `/admin`.

    Create `StepUpTotpPage.tsx`:
    - Lists factors via `supabase.auth.mfa.listFactors()`, picks the first totp factor.
    - Challenges + accepts OTP code input.
    - On success (session upgraded to aal2), redirect back to `/admin` (or stored `next` query param).

    Edit `AdminLayout.tsx` — add ABOVE the AdminShell render:
    ```typescript
    // Pattern S1 layer 1: client gate (per CONTEXT.md D-03)
    if (!profile?.admin_role) return <NotAuthorizedCard />;
    if (!profile.has_totp) return <SetupTotpPage />;
    const isAal2 = await assertAal2();
    if (!isAal2) return <StepUpTotpPage next={location.pathname} />;
    ```
    NOTE: this is async — AdminLayout already has a useEffect-driven profile fetch; piggyback on it. Don't introduce a Suspense throwable; render a small `<LoadingShell />` while resolving `assertAal2`.

    Per [[reference_supabase_auth_traps]]: LeanShot is a hash-route SPA. SetupTotpPage and StepUpTotpPage are NEW hash routes (`/admin/setup-2fa`, `/admin/step-up`). Wire them into the existing hash router pattern AdminLayout uses for sub-routes.

    Edit `SettingsModule.tsx` — augment the Plan 24-03 stub:
    - Show enrolled MFA status (`supabase.auth.mfa.listFactors()` — count of TOTP factors).
    - Button "Regenerate backup codes" → calls `admin.issue_backup_codes` (overwrites existing unconsumed; consumed-marker history preserved).
    - Button "Reset MFA" (DANGEROUS): only shown to superadmin; calls `admin.reset_totp` for their own user (self-reset is fine).

    PATTERN S1 — DB-side enforcement: every admin RPC (Plans 24-01, 24-05, 24-06) MUST check `is_admin_at_least` + the JWT `aal` claim where appropriate. The middleware-style gate at the SPA layer is UX-only; reality is enforced server-side.

    There is NO Vercel routing middleware in this project (per RESEARCH Open Question 1; LeanShot is Vite SPA with hash routing). The "middleware" enforcement happens via (1) AdminLayout React gate (Pattern S1 client layer) and (2) RPC `is_admin_at_least` + `aal2` JWT claim checks in every admin SECURITY DEFINER function (Pattern S1 server layer). DOCUMENT this in the AdminLayout file header comment so future contributors don't search for a non-existent `middleware.ts`.
  </action>
  <verify>
    <automated>
      cd leanshot && \
      npm test -- src/components/admin/__tests__/SetupTotpPage.test.tsx src/components/admin/__tests__/StepUpTotpPage.test.tsx --run 2>&1 | tail -20 && \
      npm run build 2>&1 | tail -10 && \
      grep -q "has_totp" src/components/admin/AdminLayout.tsx && \
      grep -q "assertAal2" src/components/admin/AdminLayout.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - SetupTotpPage renders QR + accepts code + displays backup codes ONCE with save-gate.
    - StepUpTotpPage challenges + verifies, redirects on success.
    - AdminLayout has_totp + aal2 gate (Pattern S1 layer 1).
    - SettingsModule augmented with enrolled-status + regenerate + reset buttons.
    - No `middleware.ts` invented; SPA-internal gating only (documented in header).
  </acceptance_criteria>
  <done>
    Full TOTP enroll + step-up UX shipped at the SPA layer.
  </done>
</task>

<task type="auto">
  <name>Task 3: SQL migration for TOTP RPCs (issue_backup_codes, consume_backup_code, reset_totp, set_has_totp_true)</name>
  <files>
    supabase/migrations/20260518000008_admin_totp_rpcs.sql
  </files>
  <read_first>
    supabase/migrations/20260518000006_admin_backup_codes_table.sql (from Plan 24-01),
    supabase/migrations/20260518000004_log_admin_action_function.sql (from Plan 24-01),
    .planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-CONTEXT.md (D-08)
  </read_first>
  <action>
    Write `supabase/migrations/20260518000008_admin_totp_rpcs.sql` adding 4 SECURITY DEFINER functions. ALL with `set search_path = extensions, public, pg_temp;`.

    1. `admin.issue_backup_codes()` — generates 10 codes server-side (Postgres `gen_random_bytes` + base32-ish encoding), HMACs each with a vault secret (`select decrypted_secret from vault.decrypted_secrets where name = 'BACKUP_CODE_PEPPER'`), upserts (user_id, code_hash) rows, returns array of PLAINTEXT codes to the caller (one-time display).
       - Caller MUST be the row owner (auth.uid()) AND have aal=aal2 (caller is in the middle of TOTP enrollment — challenge/verify just upgraded the session).
       - Delete any prior unconsumed codes for that user before insert (regenerate = reset).
       - `grant execute on function admin.issue_backup_codes() to authenticated;` (RLS-level gate inside).

    2. `admin.consume_backup_code(p_code text)` — looks up `code_hash = hmac(p_code, pepper)`; if found + not used + belongs to auth.uid(), sets used_at and returns true; else returns false. Caller must be auth.uid()=owner.

    3. `admin.set_has_totp_true()` — sets `profiles.has_totp = true` for auth.uid(). Caller must have aal=aal2.

    4. `admin.reset_totp(p_target_user_id uuid)` — superadmin-only. (a) Look up the target user's MFA factors via `auth.mfa_factors` table (or call into `auth.mfa.unenroll` — note: Supabase Auth's PG-side admin path is `select auth.mfa_remove_factor(...)` or similar — VERIFY exact API at execution time; if unavailable, fall back to direct delete from `auth.mfa_factors where user_id = p_target_user_id`). (b) Set target's `profiles.has_totp = false`. (c) `select log_admin_action('reset_totp', p_target_user_id, 'profiles', p_target_user_id::text, jsonb_build_object('has_totp', true), jsonb_build_object('has_totp', false))`. (d) Insert email-send request into `email_outbox` (existing v1.2 table) OR set a `signaled_reset_totp_at` timestamp that the Edge Function (Task 4) reads.
       - Caller MUST be `is_admin_at_least('superadmin')`.

    Schema namespace: prefer the `public` schema for all 4 functions (Supabase clients call via `supabase.rpc('issue_backup_codes')` — they look up `public.<name>` by default). If you prefer `admin.<name>` schema isolation, declare it explicitly in the migration AND in client code.

    Vault key setup: `BACKUP_CODE_PEPPER` must exist in `vault.secrets`. If absent at migration time, function call FAILS LOUDLY (raises). Plan 24-05 user_setup task asks operator to provision it.

    Per [[reference_phase7_research_findings]] — pgsodium deprecated; use Vault for secrets.
  </action>
  <verify>
    <automated>
      cd leanshot && \
      supabase db reset && supabase db push --linked --dry-run 2>&1 | tee /tmp/24-05-mig.log && \
      ! grep -i "^Skipping" /tmp/24-05-mig.log && \
      supabase db query --linked --sql "select proname from pg_proc where pronamespace = 'public'::regnamespace and proname in ('issue_backup_codes','consume_backup_code','set_has_totp_true','reset_totp');" 2>&1 | grep -c "issue_backup_codes" | awk '{ if ($1 < 1) exit 1 }'
    </automated>
  </verify>
  <acceptance_criteria>
    - 4 functions created with `set search_path` per [[reference_supabase_migration_gotchas]].
    - `issue_backup_codes` returns array of 10 plaintext codes; deletes prior unconsumed; inserts hashed.
    - `consume_backup_code` single-use enforced.
    - `reset_totp` superadmin-only; writes audit_log; emits a reset signal for the Edge Function.
    - Migration applies cleanly with `supabase db push --linked --dry-run`.
  </acceptance_criteria>
  <done>
    SQL RPCs ready; SetupTotpPage (Task 2) and admin-reset-totp Edge Fn (Task 4) wire to them.
  </done>
</task>

<task type="auto">
  <name>Task 4: admin-reset-totp Edge Function (calls RPC + emails new enrollment URL)</name>
  <files>
    supabase/functions/admin-reset-totp/index.ts,
    supabase/functions/admin-reset-totp/admin-reset-totp.test.ts
  </files>
  <read_first>
    supabase/functions/admin-reset-totp (create if absent),
    supabase/functions (find existing email-send helper from v1.2 — most likely `_shared/email-router.ts` or direct Resend call),
    supabase/functions/_shared/posthog-server.ts (from Plan 24-04),
    .planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-RESEARCH.md (D-08)
  </read_first>
  <action>
    Create `supabase/functions/admin-reset-totp/index.ts`:
    - Verify caller via Supabase Auth (header `Authorization: Bearer <jwt>`).
    - Verify caller `is_admin_at_least('superadmin')` (call into Postgres via supabase-js).
    - Parse JSON body `{ target_user_id: string }`.
    - Call `supabase.rpc('reset_totp', { p_target_user_id })` — this also writes the audit log + sets has_totp=false.
    - Build a Supabase password-reset / email-otp link for the target user using `supabase.auth.admin.generateLink({ type: 'magiclink', email: <target_email> })`.
    - Send via existing Resend helper (look up the v1.2 path; if `_shared/email-router.ts` exists, use it; else direct `fetch('https://api.resend.com/emails', {...})` with `RESEND_API_KEY` env var per [[reference_resend_phase9_wiring]]).
    - Capture server-side event via Plan 24-04 helper: `captureServer({ userId: target_user_id, event: 'admin_action', properties: { action_name: 'reset_totp' } })`.
    - Wrap entire handler in try/finally with `await shutdownPostHog()` per PITFALL 1.

    Create `admin-reset-totp.test.ts` (Deno test — note `.test.ts` naming per [[reference_deno_test_discovery]]):
    - Mock Supabase rpc + admin.generateLink + Resend fetch.
    - Test: non-superadmin caller rejected with 403.
    - Test: superadmin caller invokes RPC + sends email + returns 200.
    - Test: handler shuts down posthog in finally even on error path.
  </action>
  <verify>
    <automated>
      cd leanshot && \
      supabase functions test admin-reset-totp 2>&1 | tail -25 && \
      test -f supabase/functions/admin-reset-totp/admin-reset-totp.test.ts && \
      grep -q "shutdownPostHog" supabase/functions/admin-reset-totp/index.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - Edge Function exists + 3 Deno tests pass.
    - Non-superadmin caller gets 403.
    - Email send wired via existing Resend helper (no duplicate Resend client).
    - posthog-server shutdown in finally.
  </acceptance_criteria>
  <done>
    superadmin can reset another admin's TOTP via the admin-reset-totp Edge Function; user receives new enrollment link by email; action audit-logged.
  </done>
</task>

<task type="auto">
  <name>Task 5: e2e tests for TOTP enrollment + middleware redirect</name>
  <files>
    e2e/admin-mfa-enroll.spec.ts,
    e2e/admin-mfa-middleware.spec.ts
  </files>
  <read_first>
    e2e (existing Playwright config + at least one v1.2 admin spec for fixture pattern),
    playwright.config.ts,
    .planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-RESEARCH.md (Playwright + state-seeding pitfall references)
  </read_first>
  <action>
    Use [[reference_playwright_state_seeding]] — `page.addInitScript` NOT `page.goto + page.evaluate(seed) + reload`. Use [[reference_playwright_conditional_project_argv]] — detect by env var, not `process.argv`.

    `e2e/admin-mfa-enroll.spec.ts`:
    - Bootstrap a synthetic admin user (no TOTP enrolled) via the project's RLS fixture pattern.
    - Navigate to `/admin` (hash route).
    - Assert SetupTotpPage rendered (QR `<img>` present, OTP input present).
    - Programmatically generate a TOTP code from the secret embedded in the QR URI (Playwright can read the data-URI; parse the `otpauth://totp/...?secret=BASE32` query; compute current OTP via a small `totp.ts` helper imported into the spec — or use `otplib`).
    - Submit code; assert backup codes displayed (10 chips with `XXXX-XXXX-XXXX` format).
    - Check the "I have saved" checkbox; submit.
    - Assert redirect to `/admin` and AdminShell renders.

    `e2e/admin-mfa-middleware.spec.ts`:
    - Bootstrap an admin user WITHOUT TOTP.
    - Navigate to `/admin/users`.
    - Assert URL hash redirects to `#/admin/setup-2fa` (or whichever route SetupTotpPage uses).
    - Bootstrap admin WITH TOTP but session aal=aal1.
    - Navigate to `/admin/users`.
    - Assert redirect to `#/admin/step-up`.

    Defer-then-batch per [[feedback_defer_then_batch_fix_pattern]]: if Playwright + TOTP-code-generation in the test is too flaky on first attempt, mark with `it.skip('... [DEFERRED — see .planning/deferred-tests.md]', ...)` and add an entry in `.planning/deferred-tests.md`. NEVER use `it.fixme` per [[reference_vitest_skip_fixme]] — Playwright `test.fixme` also accepted only on `test`, not `it`.
  </action>
  <verify>
    <automated>cd leanshot && PLAYWRIGHT_RUN_ADMIN_MFA=1 npm run test:e2e -- admin-mfa-enroll.spec.ts admin-mfa-middleware.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Both specs exist + pass (or DEFERRED with `.planning/deferred-tests.md` entry).
    - `page.addInitScript` used for state seeding (not `page.goto + reload`).
    - TOTP code generated from QR secret in-spec (or via a small helper module).
  </acceptance_criteria>
  <done>
    User-flow level proof of 2FA enrollment + middleware-equivalent redirect.
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 6: HUMAN — set BACKUP_CODE_PEPPER in Supabase Vault + RESEND_API_KEY check</name>
  <files>(vendor UI + Supabase Vault)</files>
  <read_first>
    .planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-CONTEXT.md (D-08)
  </read_first>
  <action>
    Two secrets:
    1. `BACKUP_CODE_PEPPER` (vault — Postgres-side): generate random 32-byte hex locally `openssl rand -hex 32`; insert via `supabase db query --linked --sql "select vault.create_secret('<hex-output>', 'BACKUP_CODE_PEPPER')"`. NEVER commit. NEVER reuse the SUPABASE_SERVICE_ROLE_KEY hex.
    2. `RESEND_API_KEY` (Edge Function secret — must already be set from v1.2 per [[reference_resend_phase9_wiring]]). Verify: `supabase secrets list | grep RESEND_API_KEY`.
    Reply: "BACKUP_CODE_PEPPER inserted; RESEND_API_KEY present".
  </action>
  <verify>
    User confirms both. Orchestrator may auto-verify via `supabase db query --linked --sql "select name from vault.secrets where name='BACKUP_CODE_PEPPER';"` and `supabase secrets list`.
  </verify>
  <acceptance_criteria>
    - Vault row `BACKUP_CODE_PEPPER` present.
    - Function secret `RESEND_API_KEY` present.
  </acceptance_criteria>
  <done>
    Backup-code HMAC + reset-email path operational.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser POSTs TOTP code → Supabase Auth verify | Untrusted; Supabase MFA verifies HOTP/TOTP server-side |
| superadmin calls reset_totp(target) → MFA factor cleared | Privilege boundary; cannot be available to admin or staff |
| Backup code displayed once → user copies → user enters later | One-time display; lost = superadmin reset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02 | Elevation of privilege | Admin bypasses MFA (no TOTP enrolled but session active) | mitigate | AdminLayout has_totp gate redirects to SetupTotpPage; RPCs additionally check aal=aal2 for sensitive actions |
| T-24-02b | Elevation of privilege | Adversary resets another admin's TOTP via reset_totp | mitigate | RPC gated to is_admin_at_least('superadmin'); audit-logged; emails target so they know |
| T-24-14 | Tampering | Backup code stolen + replayed | mitigate | HMAC server-side hash + single-use (used_at unique-by-row) |
| T-24-15 | Tampering | TOTP secret leaks via QR URI persisted in browser history | accept | QR is data-URI; not stored unless user manually copies; cleared from DOM on Successful verify; document in 24-PLAN security note |
| T-24-16 | Repudiation | superadmin denies resetting an admin's TOTP | mitigate | Every reset_totp call writes log_admin_action with actor + target + timestamp; audit_logs append-only RLS |
</threat_model>

<verification>
- `npm test -- src/lib/admin/__tests__/totp.test.ts --run` passes.
- `supabase db push --linked --dry-run` after 24-01 + 24-05 migrations is clean.
- `supabase functions test admin-reset-totp` passes 3 Deno tests.
- `npm run build` exits 0 (TS + Vite).
- `npm run test:e2e -- admin-mfa-enroll.spec.ts admin-mfa-middleware.spec.ts` pass (or DEFERRED with entry).
- Manual: vault BACKUP_CODE_PEPPER + RESEND_API_KEY both present.
</verification>

<success_criteria>
- TOTP enrollment + step-up shipped end-to-end at SPA layer.
- Pattern S1 dual-layer enforced: client gate (AdminLayout) + RPC layer (`is_admin_at_least` + aal2 checks in every SECURITY DEFINER func).
- 10 backup codes issued once, HMAC-hashed at rest, single-use.
- superadmin reset_totp RPC + Edge Function operational; audit-logged.
- e2e proves the enrollment + redirect flow.
- ADMIN-03 closes.
</success_criteria>

<output>
Create `.planning/phases/24-foundation-modular-admin-shell-event-taxonomy-server-side-po/24-05-SUMMARY.md` when done.
</output>
