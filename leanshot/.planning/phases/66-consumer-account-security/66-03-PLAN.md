---
phase: 66-consumer-account-security
plan: 3
type: execute
wave: 2
depends_on: [66-02]
files_modified:
  - leanshot/src/components/settings/SecuritySettingsPage.tsx
  - leanshot/src/components/settings/SecuritySettingsPage.test.tsx
  - leanshot/src/components/auth/TotpEnrollFlow.tsx
  - leanshot/src/components/auth/TotpEnrollFlow.test.tsx
  - leanshot/src/App.tsx
autonomous: true
requirements: [AUTH-12]
---

<objective>
Consumer MFA enrollment surface. `/settings/security` route mounts `<SecuritySettingsPage>` which uses `<TotpEnrollFlow mode="consumer">`. Extract `TotpEnrollFlow` from Phase 25's `SetupTotpPage` so admin + consumer share the component; admin SetupTotpPage becomes a thin wrapper.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extract <TotpEnrollFlow> from Phase 25 SetupTotpPage</name>
  <files>leanshot/src/components/auth/TotpEnrollFlow.tsx, leanshot/src/components/auth/TotpEnrollFlow.test.tsx, leanshot/src/components/admin/SetupTotpPage.tsx</files>
  <action>
Read `leanshot/src/components/admin/SetupTotpPage.tsx` and extract the QR-code + verify input + backup-codes UI into a reusable `<TotpEnrollFlow>` at `leanshot/src/components/auth/TotpEnrollFlow.tsx`.

Props:
```ts
interface TotpEnrollFlowProps {
  mode: 'admin' | 'consumer';
  onSuccess: () => void;
  onCancel?: () => void;
}
```

Copy divergence (per UI-SPEC):
- `mode === 'admin'`: heading "Set up MFA for admin access"
- `mode === 'consumer'`: heading "Add 2-factor authentication"

Stages: idle → enrolling (QR shown) → verifying (6-digit input) → backup-codes (10 codes shown + download TXT) → done. Uses `enrollTotp` / `verifyTotpChallenge` / `generateBackupCodes` from `src/lib/auth/totp-shared.ts` (Plan 66-02).

After backup codes shown, user MUST click "I've saved these" before `onSuccess` fires. Backup codes are NOT persisted in the client; they're persisted hashed in Supabase (mfa_factors metadata or a backup_codes table — check existing Phase 25 pattern).

Update `src/components/admin/SetupTotpPage.tsx` to render `<TotpEnrollFlow mode="admin" onSuccess={navigate('/admin')} />` — keep the existing route working.

Tailwind v4: use only tokens from `src/index.css` @theme block (no `bg-surface-card` / `text-text-primary` / `border-border-subtle` — those are undefined). Use `bg-surface`, `text-text`, `border-border`. Typography ceiling 11/13/18/text-heading.

Test:
- Renders heading per `mode` prop
- Clicking "Enroll" calls `enrollTotp`, shows QR
- Typing 6-digit code + "Verify" calls `verifyTotpChallenge`
- On success shows backup codes
- "Download codes" triggers TXT download
- Clicking "I've saved these" calls `onSuccess`
  </action>
</task>

<task type="auto">
  <name>Task 2: Create <SecuritySettingsPage> + wire /settings/security route</name>
  <files>leanshot/src/components/settings/SecuritySettingsPage.tsx, leanshot/src/components/settings/SecuritySettingsPage.test.tsx, leanshot/src/App.tsx</files>
  <action>
`<SecuritySettingsPage>`:
- Header: "Security"
- Section 1: "Two-factor authentication"
  - If enrolled: green "On since <date>" badge + button "Re-enroll" (calls unenrollFactor then re-opens flow) + button "View backup codes" (warns about regeneration)
  - If not enrolled: `<TotpEnrollFlow mode="consumer" onSuccess={() => refresh()}>`
- Section 2 (Phase 70 visible only if MFA enrolled): list of "Trusted devices" — stub for future, just shows "No trusted devices" empty state
- Section 3: "Active sessions" — calls `supabase.auth.admin.listSessions` (or whatever exists); stub OK if API not exposed

Wire route in `App.tsx`. Use Phase 64-04 routing pattern (whichever the project uses — switch-based or React Router). Add route guard: must be authenticated.

Test: render with MFA-not-enrolled state → see Enroll flow. Render with MFA-enrolled state → see "On since" badge + Re-enroll button.
  </action>
</task>

</tasks>
