---
phase: 66-consumer-account-security
plan: 6
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - leanshot/src/components/admin/users/RoleMfaRequirementTable.tsx
  - leanshot/src/components/admin/users/RoleMfaRequirementTable.test.tsx
  - leanshot/src/components/admin/users/MfaStatusBadge.tsx
  - leanshot/src/components/admin/users/MfaStatusBadge.test.tsx
  - leanshot/src/components/admin/users/UserDetailPage.tsx
  - leanshot/src/lib/admin/role-mfa-config.ts
  - leanshot/src/lib/admin/role-mfa-config.test.ts
  - leanshot/src/lib/admin/modules.ts
autonomous: true
requirements: [AUTH-17]
---

<objective>
Admin surfaces for MFA: per-role requirement toggle table (`/admin/users/security`) + MFA-status badge in user-detail view. Reads/writes `mfa_role_requirements` table from Plan 66-01 via SECDEF RPC `set_mfa_role_requirement`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: `<RoleMfaRequirementTable>` + admin-module registration</name>
  <files>leanshot/src/components/admin/users/RoleMfaRequirementTable.tsx, leanshot/src/components/admin/users/RoleMfaRequirementTable.test.tsx, leanshot/src/lib/admin/role-mfa-config.ts, leanshot/src/lib/admin/role-mfa-config.test.ts, leanshot/src/lib/admin/modules.ts</files>
  <action>
Lib `role-mfa-config.ts`:
- `listRoleMfaRequirements(supabase): Promise<Array<{ role: string; required: boolean; since: string }>>`
- `setRoleMfaRequirement(supabase, role: string, required: boolean): Promise<void>` — calls `set_mfa_role_requirement` RPC

`<RoleMfaRequirementTable>`:
- Table with columns: Role | Required (checkbox) | Since
- Toggling checkbox calls setRoleMfaRequirement; optimistic update + rollback on error
- Only superadmin can mutate (RPC enforces; UI greys out checkbox for non-superadmin)
- 5 seeded rows from migration

Register in `ADMIN_MODULES` array (or whatever the admin module manifest is called) with key `users-security`, route `/admin/users/security`. NOTE per Phase 65 lesson: also add router branch in App.tsx — don't just add to manifest.

Tailwind v4 tokens only (verify against `src/index.css`). Typography ceiling 11/13/18/text-heading.
  </action>
</task>

<task type="auto">
  <name>Task 2: `<MfaStatusBadge>` + wire into user-detail page</name>
  <files>leanshot/src/components/admin/users/MfaStatusBadge.tsx, leanshot/src/components/admin/users/MfaStatusBadge.test.tsx, leanshot/src/components/admin/users/UserDetailPage.tsx</files>
  <action>
`<MfaStatusBadge>` props: `userMfaFactors: number`, `userRole: string`, `roleRequirements: Map<string, boolean>`.

3 states:
- `on` — `userMfaFactors > 0` → green badge "MFA on" (`bg-success-soft text-success`)
- `required-not-enrolled` — `roleRequirements.get(userRole) === true && userMfaFactors === 0` → amber badge "MFA required" (`bg-warning-soft text-warning`)
- `off` — neither → grey badge "MFA off" (`bg-surface-soft text-text-muted`)

In `UserDetailPage.tsx`, fetch user's MFA factors via `supabase.auth.admin.getUser(userId)` (or the existing admin user-detail data source) AND fetch role requirements via `listRoleMfaRequirements()` once + cache. Render badge next to user's name.

Test: 3 badge states each render with expected token classes.
  </action>
</task>

</tasks>
