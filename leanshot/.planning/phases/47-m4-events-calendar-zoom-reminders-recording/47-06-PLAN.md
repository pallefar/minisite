---
phase: 47-m4-events-calendar-zoom-reminders-recording
plan: 06
type: execute
wave: 1
depends_on: [47-01, 47-02, 47-03, 47-05]
files_modified:
  - supabase/functions/zoom-create-meeting/index.ts
  - supabase/functions/zoom-create-meeting/deno.json
autonomous: true
requirements: [EVENT-03]
user_setup:
  - service: zoom
    why: "S2S OAuth — create meetings on the platform-shared Zoom account"
    env_vars:
      - name: ZOOM_S2S_ACCOUNT_ID
        source: "Zoom Marketplace → Manage → Server-to-Server OAuth app → App Credentials"
      - name: ZOOM_S2S_CLIENT_ID
        source: "same screen"
      - name: ZOOM_S2S_CLIENT_SECRET
        source: "same screen"
    dashboard_config:
      - task: "Create Server-to-Server OAuth app + add scope meeting:write:admin"
        location: "Zoom Marketplace → Develop → Build App → Server-to-Server OAuth"
must_haves:
  truths:
    - "POST /users/me/meetings via Zoom Meeting API returns id + join_url"
    - "S2S OAuth token cached in-memory (per-instance), refreshed 60s before expiry, refetched on 401 with single retry"
    - "Service-role write-back to events.zoom_meeting_id + events.join_url + events.zoom_managed=true"
    - "Admin gate via supabase.rpc('is_staff') — non-admin returns 403"
    - "Deno.serve guarded by import.meta.main per reference_deno_test_top_level_serve_trap"
  artifacts:
    - path: "supabase/functions/zoom-create-meeting/index.ts"
      provides: "Edge Fn: S2S OAuth + Zoom meeting creation + write-back"
      contains: "export async function handler"
    - path: "supabase/functions/zoom-create-meeting/deno.json"
      provides: "Per-Fn deno.json (per memory reference_supabase_functions_deploy_import_map_flag)"
      contains: "imports"
  key_links:
    - from: "zoom-create-meeting"
      to: "https://api.zoom.us/v2/users/me/meetings"
      via: "fetch with Bearer <s2s-token>"
      pattern: "https://api.zoom.us/v2/users/me/meetings"
    - from: "zoom-create-meeting"
      to: "events row"
      via: "service-role admin update"
      pattern: "admin.from\\('events'\\)\\.update"
---

<objective>
Ship the `zoom-create-meeting` Edge Fn. Implements D-06 + D-07.

Purpose: Admin event-create form calls this Fn (after event INSERT) when picking "Generate Zoom meeting" radio; Fn obtains S2S OAuth token, calls Zoom Meeting API, writes back the join_url + zoom_meeting_id.
Output: 1 Edge Fn (index.ts + deno.json). RESEARCH §Code Examples Example 5 lands verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-CONTEXT.md
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-RESEARCH.md
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-PATTERNS.md
@supabase/functions/mux-create-upload/index.ts
@supabase/functions/mux-webhook/deno.json

**Memory references (load-bearing):**
- `feedback_vendor_secret_preflight_surface` — `ZOOM_S2S_ACCOUNT_ID`, `ZOOM_S2S_CLIENT_ID`, `ZOOM_S2S_CLIENT_SECRET` MUST be set before this Fn deploys. Surface in Wave 0 dispatch confirmation.
- `reference_supabase_is_staff_helper` — `public.is_staff()` is the admin check.
- `reference_supabase_functions_deploy_import_map_flag` — per-Fn `deno.json` mandatory; CLI v2.101.0 ignores `--import-map`.
- `reference_deno_test_top_level_serve_trap` — guard `Deno.serve()` with `import.meta.main`.
- `reference_supabase_service_role_key_format_divergence` — service-role admin client uses `SUPABASE_SERVICE_ROLE_KEY` env (set to `sb_secret_*` token in production).

**RESEARCH §Pitfall 2** — Zoom S2S token TTL 3600s; cache in-memory per-instance; on 401 from Zoom, null cache + retry once.

**RESEARCH §Code Examples Example 5** (lines 724-820) — full Fn skeleton lands verbatim.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: zoom-create-meeting Edge Fn (index.ts + deno.json)</name>
  <files>supabase/functions/zoom-create-meeting/index.ts, supabase/functions/zoom-create-meeting/deno.json</files>
  <behavior>
    Per VALIDATION.md mapping:
    - Happy path: caller passes `{ event_id }`; Fn fetches event from DB, calls Zoom OAuth token endpoint, POSTs to Zoom Meeting API, receives `{ id, join_url }`, writes back to events row via service-role admin client. Returns `{ ok: true, meeting_id, join_url }`.
    - 401 from Zoom API → null in-memory token cache, refetch token, retry POST once. Subsequent failure → 502 `zoom_create_<status>`.
    - Non-admin caller → 403 forbidden via `supabase.rpc('is_staff')`.
    - Unauthenticated → 401 unauthorized.
    - Missing event row → 404 event_not_found.
  </behavior>
  <action>
    Land RESEARCH §Code Examples Example 5 (lines 724-820) verbatim as `supabase/functions/zoom-create-meeting/index.ts`.

    Key invariants:
    1. Module-level cache: `let _cachedToken: { value: string; expiresAt: number } | null = null;` — per-instance; cold start refreshes; expires at `now + (expires_in - 60) * 1000` ms (60s safety margin).
    2. Zoom token endpoint: `POST https://zoom.us/oauth/token?grant_type=account_credentials&account_id=${accountId}` with `Authorization: Basic ${btoa(client_id:client_secret)}`.
    3. Zoom meeting create: `POST https://api.zoom.us/v2/users/me/meetings` with `Authorization: Bearer ${token}` and JSON body `{ topic, type: 2, start_time, duration, timezone: 'UTC', settings: { join_before_host: false, waiting_room: true } }`. `duration` derived from `Math.ceil((Date.parse(end_at) - Date.parse(start_at)) / 60000)`.
    4. Admin gate via `supabase.rpc('is_staff')` — Phase 44 / 28 helper. Pre-flight call before any Zoom API hit.
    5. Service-role write-back: separate admin client `createClient(URL, SERVICE_ROLE_KEY, { auth: { autoRefreshToken: false, persistSession: false } })` updates `events.zoom_meeting_id`, `events.join_url`, `events.zoom_managed = true` for the event_id.
    6. NEVER log Authorization header or token value (per Threat Register T-47-25).
    7. CORS headers per existing Edge Fn pattern.
    8. `Deno.serve` guarded: `const denoGlobal: any = (globalThis as any).Deno; if (import.meta.main && denoGlobal?.serve) denoGlobal.serve(handler);` per memory `reference_deno_test_top_level_serve_trap`.

    Create sibling `deno.json` — FORK `supabase/functions/mux-webhook/deno.json` verbatim style:
    ```
    {
      "imports": {
        "npm:@supabase/supabase-js@2": "npm:@supabase/supabase-js@2"
      }
    }
    ```
    Per memory `reference_supabase_functions_deploy_import_map_flag` — CLI v2.101.0 silently ignores `--import-map` flag; per-Fn `deno.json` is the only reliable resolution.

    Errors: use snake_case strings — `unauthorized` / `forbidden` / `event_not_found` / `zoom_oauth_<status>` / `zoom_create_<status>` / `zoom_not_configured` (when any of the 3 env vars are missing).

    Test injection seam: export setter `export function setAdminForTest(client: SupabaseClient) { … }` mirroring `mux-create-upload`'s `setVerifyForTest` / `setAdminForTest` pattern so the Wave 0 test scaffold (`supabase/functions/zoom-create-meeting/index.test.ts`) can inject a mock.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test -f supabase/functions/zoom-create-meeting/index.ts && test -f supabase/functions/zoom-create-meeting/deno.json && grep -c 'is_staff' supabase/functions/zoom-create-meeting/index.ts | xargs test 1 -le && grep -c 'api.zoom.us/v2/users/me/meetings' supabase/functions/zoom-create-meeting/index.ts | xargs test 1 -le && grep -c 'import.meta.main' supabase/functions/zoom-create-meeting/index.ts | xargs test 1 -le</automated>
  </verify>
  <done>
    Both files present; admin gate via is_staff; Zoom create endpoint hit; Deno.serve guarded by import.meta.main; per-Fn deno.json with @supabase/supabase-js@2 import.
  </done>
</task>

</tasks>

<acceptance_criteria>
- `test -f supabase/functions/zoom-create-meeting/index.ts && test -f supabase/functions/zoom-create-meeting/deno.json` — both exist.
- `grep -c 'api.zoom.us/v2/users/me/meetings' supabase/functions/zoom-create-meeting/index.ts` ≥ 1.
- `grep -c 'zoom.us/oauth/token' supabase/functions/zoom-create-meeting/index.ts` ≥ 1.
- `grep -c 'is_staff' supabase/functions/zoom-create-meeting/index.ts` ≥ 1.
- `grep -c 'ZOOM_S2S_ACCOUNT_ID' supabase/functions/zoom-create-meeting/index.ts` ≥ 1.
- `grep -c 'ZOOM_S2S_CLIENT_ID' supabase/functions/zoom-create-meeting/index.ts` ≥ 1.
- `grep -c 'ZOOM_S2S_CLIENT_SECRET' supabase/functions/zoom-create-meeting/index.ts` ≥ 1.
- `grep -c 'SUPABASE_SERVICE_ROLE_KEY' supabase/functions/zoom-create-meeting/index.ts` ≥ 1.
- `grep -c 'import.meta.main' supabase/functions/zoom-create-meeting/index.ts` ≥ 1.
- `grep -c 'zoom_managed' supabase/functions/zoom-create-meeting/index.ts` ≥ 1.
- `grep -c '401' supabase/functions/zoom-create-meeting/index.ts` ≥ 1 (retry-on-401 path).
- `grep -c 'console.log.*token\|console.log.*Authorization' supabase/functions/zoom-create-meeting/index.ts` — exactly 0 (no token logging per T-47-25).
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → zoom-create-meeting | admin gate via is_staff RPC |
| zoom-create-meeting → Zoom API | S2S OAuth bearer; secrets from Function Secrets |
| zoom-create-meeting → Supabase events table | service-role admin client write-back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-21 | S (Spoofing) | non-admin calls Fn | mitigate | `supabase.rpc('is_staff')` returns false → 403. |
| T-47-22 | T (Tampering) | Fn writes to a stale event_id | accept | service-role update by event_id is authoritative; bad event_id → no-op update. |
| T-47-23 | I (Info Disclosure) | Zoom credentials in logs | mitigate | NEVER log Authorization header or Basic auth string; grep guard prohibits. |
| T-47-24 | D (DoS) | Zoom API rate-limit on bulk meeting creation | accept | v1 expects 1 meeting/event/admin click; rate limit (200/min per account) is far above. |
| T-47-25 | I | Zoom token cached cross-tenant | accept | Per-instance cache; Edge Fn cold-start isolation. |
| T-47-26 | E (Elevation) | service-role bearer leaks into client response | mitigate | Service-role client used only for `events.update`; never serialized into response body. |
</threat_model>

<verification>
- File pair committed.
- All grep gates pass.
- Wave 0 scaffold `supabase/functions/zoom-create-meeting/index.test.ts` will GREEN after this plan ships + Wave 3 close-out plan applies the test bodies + `supabase functions deploy zoom-create-meeting`.
</verification>

<success_criteria>
- All grep gates pass.
- No token logging.
- Retry-on-401 path present.
- deno.json sibling present.
</success_criteria>

<output>
After completion, create `.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-06-SUMMARY.md`.
</output>
