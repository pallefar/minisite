---
phase: 47-m4-events-calendar-zoom-reminders-recording
plan: 03
type: execute
wave: 0
depends_on: [47-01, 47-02]
files_modified:
  - supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql
  - supabase/migrations/20270801000004_p47_waitlist_promotion_trigger.sql
  - supabase/migrations/20270801000008_p47_event_reminder_sent_and_queue.sql
autonomous: true
requirements: [EVENT-02]
must_haves:
  truths:
    - "event_rsvp_create SECDEF RPC uses SELECT FOR UPDATE on events row for atomic capacity check"
    - "RPC branches INSERT vs UPDATE on UNIQUE(event_id, user_id); never uses ON CONFLICT DO DELETE"
    - "RPC uses auth.uid() — user-initiated, NOT callable from service-role"
    - "AFTER UPDATE/DELETE trigger promotes head of waitlist via FOR UPDATE SKIP LOCKED"
    - "event_reminder_sent UNIQUE(event_id, user_id, kind) — idempotent reminder dedup"
    - "event_promotion_queue UNIQUE(event_id, user_id) — fan-out drains once per promotion"
    - "event_get_join_url(p_event_id) SECDEF returns join_url only if rsvp_status='going' AND now BETWEEN start_at-15min AND end_at"
  artifacts:
    - path: "supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql"
      provides: "event_rsvp_create + event_get_join_url SECDEF RPCs"
      contains: "create or replace function public.event_rsvp_create"
    - path: "supabase/migrations/20270801000004_p47_waitlist_promotion_trigger.sql"
      provides: "promote_waitlist_on_rsvp_change + AFTER trigger"
      contains: "create trigger trg_promote_waitlist_on_rsvp_change"
    - path: "supabase/migrations/20270801000008_p47_event_reminder_sent_and_queue.sql"
      provides: "event_reminder_sent + event_promotion_queue tables"
      contains: "create table if not exists public.event_reminder_sent"
  key_links:
    - from: "event_rsvp_create"
      to: "events row lock"
      via: "SELECT FOR UPDATE"
      pattern: "FROM public.events WHERE id = p_event_id FOR UPDATE"
    - from: "promote_waitlist_on_rsvp_change"
      to: "event_promotion_queue"
      via: "INSERT after promotion"
      pattern: "INSERT INTO public.event_promotion_queue"
    - from: "event_get_join_url"
      to: "events.join_url"
      via: "SECDEF read"
      pattern: "select join_url from public.events"
---

<objective>
Ship the three SECDEF migration files that own the capacity/waitlist invariant + the join-URL gate + the reminder dedup tables. Implements D-02, D-03, D-09, D-10, D-18.

Purpose: This plan is the atomic-invariant kernel. Capacity check (D-02), waitlist FIFO promotion (D-03), reminder dedup (D-10), and join-URL gate (D-09 + D-18) all live or are referenced here.
Output: 3 migration files. Migration 03 ships 2 SECDEF RPCs; migration 04 ships trigger fn + trigger; migration 08 ships 2 tracking tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-CONTEXT.md
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-RESEARCH.md
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-PATTERNS.md
@supabase/migrations/20270720000005_p44_community_secdef_rpcs.sql

**RESEARCH §Code Examples Example 1** (event_rsvp_create) — body lands verbatim per RESEARCH.md lines 472-547.
**RESEARCH §Code Examples Example 2** (promote_waitlist_on_rsvp_change) — body lands verbatim per RESEARCH.md lines 552-624.

**Memory references (load-bearing):**
- `reference_postgres_no_insert_on_conflict_do_delete` — capacity branch uses SELECT FOR UPDATE + INSERT … ON CONFLICT DO UPDATE; never `DO DELETE`.
- `feedback_rpc_auth_uid_vs_service_role_mismatch` — event_rsvp_create body references `auth.uid()`; the RPC is user-initiated. `event-reminders-fanout` (service-role) MUST NOT call this RPC; it writes directly to `event_reminder_sent`.
- `reference_supabase_migration_gotchas` — SECDEF needs `set search_path = public, extensions`.
- `feedback_negation_grep_defeated_by_comment_string` — keep rejected names out of committed SQL.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: event_rsvp_create + event_get_join_url SECDEF RPCs</name>
  <files>supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql</files>
  <behavior>
    - `event_rsvp_create(p_event_id uuid, p_status text)` returns jsonb `{ status, waitlist_position }`.
    - Raises `42501` if `auth.uid()` is NULL (unauthenticated).
    - Raises `22023` if `p_status NOT IN ('going','maybe','not_going')` (client cannot request 'waitlist').
    - Raises `P0002` if event not found.
    - Locks events row via `SELECT … FOR UPDATE`.
    - If capacity=0 OR count(going) < capacity → assigns 'going'; else assigns 'waitlist' with next waitlist_position.
    - Idempotent via `INSERT … ON CONFLICT (event_id, user_id) DO UPDATE SET status=excluded.status, …`.
    - `event_get_join_url(p_event_id uuid)` returns jsonb `{ url }` if caller has rsvp_status='going' AND `now() BETWEEN start_at - interval '15 minutes' AND end_at` (D-18). Otherwise returns `{ error: 'rsvp_required' }` OR `{ error: 'too_early', opens_at: ... }`.
    - Both functions: `language plpgsql security definer set search_path = public, extensions`; `revoke all from public; grant execute to authenticated;` — NEVER service_role per memory `feedback_rpc_auth_uid_vs_service_role_mismatch`.
  </behavior>
  <action>
    Land RESEARCH §Code Examples Example 1 verbatim as `public.event_rsvp_create`. Wrap in `begin; … commit;`.

    Add `public.event_get_join_url(p_event_id uuid) returns jsonb` SECDEF (D-09 + D-18):
    ```
    create or replace function public.event_get_join_url(p_event_id uuid) returns jsonb
    language plpgsql security definer set search_path = public, extensions
    as $fn$
    declare
      v_user_id uuid := auth.uid();
      v_event   public.events%rowtype;
      v_rsvp    public.event_rsvps%rowtype;
    begin
      if v_user_id is null then raise exception 'auth required' using errcode = '42501'; end if;
      select * into v_event from public.events where id = p_event_id;
      if not found then raise exception 'event not found' using errcode = 'P0002'; end if;

      -- Admin bypass (event creator OR is_staff() can preview anytime)
      if v_event.created_by = v_user_id or public.is_staff() then
        return jsonb_build_object('url', v_event.join_url);
      end if;

      -- 15-min pre-window (D-18)
      if now() < v_event.start_at - interval '15 minutes' then
        return jsonb_build_object('error', 'too_early', 'opens_at', to_char(v_event.start_at - interval '15 minutes', 'YYYY-MM-DD"T"HH24:MI:SS"Z"'));
      end if;
      if now() > v_event.end_at then
        return jsonb_build_object('error', 'event_ended');
      end if;

      select * into v_rsvp from public.event_rsvps where event_id = p_event_id and user_id = v_user_id;
      if not found or v_rsvp.status <> 'going' then
        return jsonb_build_object('error', 'rsvp_required');
      end if;

      return jsonb_build_object('url', v_event.join_url);
    end;
    $fn$;
    revoke all on function public.event_get_join_url(uuid) from public;
    grant execute on function public.event_get_join_url(uuid) to authenticated;
    ```

    Both RPCs MUST use `set search_path = public, extensions` (per memory `reference_supabase_migration_gotchas`).

    Do NOT use `service_role` GRANT (per memory `feedback_rpc_auth_uid_vs_service_role_mismatch` — auth.uid() body requires user JWT caller).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -v '^--' supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql | grep -c 'create or replace function public.event_rsvp_create' | xargs test 1 -le && grep -v '^--' supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql | grep -c 'create or replace function public.event_get_join_url' | xargs test 1 -le && grep -v '^--' supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql | grep -c 'on conflict do delete' | xargs test 0 -eq</automated>
  </verify>
  <done>
    Both SECDEF RPCs created; FOR UPDATE present in body; auth.uid() check present; no `on conflict do delete`; revoke/grant lines present for `authenticated` only.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: promote_waitlist_on_rsvp_change trigger fn + AFTER trigger</name>
  <files>supabase/migrations/20270801000004_p47_waitlist_promotion_trigger.sql</files>
  <behavior>
    - Trigger fn `public.promote_waitlist_on_rsvp_change()` returns trigger.
    - On UPDATE: if OLD.status='going' AND NEW.status<>'going' → promote head of waitlist.
    - On DELETE: if OLD.status='going' → promote head of waitlist.
    - Head-of-waitlist SELECT uses `ORDER BY waitlist_position ASC NULLS LAST LIMIT 1 FOR UPDATE SKIP LOCKED` per D-03 + Pitfall 5.
    - If promotion happens: UPDATE rsvp row to status='going', waitlist_position=NULL; INSERT event_promotion_queue row (idempotent on UNIQUE(event_id, user_id)).
    - AFTER UPDATE OR DELETE trigger registered on event_rsvps.
  </behavior>
  <action>
    Land RESEARCH §Code Examples Example 2 verbatim as `public.promote_waitlist_on_rsvp_change()`. Wrap in `begin; … commit;`.

    Use single dollar-quote tag `$fn$` (file is standalone — no cron nesting, so no need for unique tag per memory `reference_postgres_dollar_quote_nesting_in_cron_body`).

    `CREATE TRIGGER trg_promote_waitlist_on_rsvp_change AFTER UPDATE OR DELETE ON public.event_rsvps FOR EACH ROW EXECUTE FUNCTION public.promote_waitlist_on_rsvp_change();`

    SECURITY DEFINER + `set search_path = public, extensions` per memory `reference_supabase_migration_gotchas`.

    **Add this inline SQL comment in the function body** so future edits cannot regress: `-- IMPORTANT: this SECDEF trigger fn must NOT reference auth.uid() — fires under multiple roles (user INSERT/UPDATE/DELETE on event_rsvps, plus service-role drain on event_promotion_queue). Per memory feedback_rpc_auth_uid_vs_service_role_mismatch.`

    Capacity guard: if `v_capacity <> 0 AND v_going_count >= v_capacity` → return without promoting (defensive in case multiple cascading triggers fire before the row count settles).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -v '^--' supabase/migrations/20270801000004_p47_waitlist_promotion_trigger.sql | grep -c 'for update skip locked' | xargs test 1 -le && grep -v '^--' supabase/migrations/20270801000004_p47_waitlist_promotion_trigger.sql | grep -c 'create trigger trg_promote_waitlist_on_rsvp_change' | xargs test 1 -le</automated>
  </verify>
  <done>
    Trigger fn + trigger committed; FOR UPDATE SKIP LOCKED present (case-insensitive grep matches); AFTER UPDATE OR DELETE registered.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: event_reminder_sent + event_promotion_queue tables</name>
  <files>supabase/migrations/20270801000008_p47_event_reminder_sent_and_queue.sql</files>
  <behavior>
    - `event_reminder_sent` table: (id uuid pk, event_id uuid not null FK events ON DELETE CASCADE, user_id uuid not null FK auth.users ON DELETE CASCADE, kind text not null CHECK in ('1d','1h','promotion'), sent_at timestamptz not null default now()). UNIQUE(event_id, user_id, kind).
    - `event_promotion_queue` table: (id uuid pk, event_id uuid not null FK events ON DELETE CASCADE, user_id uuid not null FK auth.users ON DELETE CASCADE, promoted_at timestamptz not null default now(), drained_at timestamptz). UNIQUE(event_id, user_id).
    - RLS: both tables service-role-only (no authenticated SELECT/INSERT/UPDATE/DELETE). Fan-out Fn writes via service role.
  </behavior>
  <action>
    Create both tables in one migration. Wrap in `begin; … commit;`.

    Schema:
    ```
    create table if not exists public.event_reminder_sent (
      id         uuid primary key default gen_random_uuid(),
      event_id   uuid not null references public.events(id) on delete cascade,
      user_id    uuid not null references auth.users(id) on delete cascade,
      kind       text not null check (kind in ('1d','1h','promotion')),
      sent_at    timestamptz not null default now(),
      constraint event_reminder_sent_unique unique (event_id, user_id, kind)
    );
    create index if not exists event_reminder_sent_event_idx on public.event_reminder_sent (event_id);

    create table if not exists public.event_promotion_queue (
      id          uuid primary key default gen_random_uuid(),
      event_id    uuid not null references public.events(id) on delete cascade,
      user_id     uuid not null references auth.users(id) on delete cascade,
      promoted_at timestamptz not null default now(),
      drained_at  timestamptz,
      constraint event_promotion_queue_unique unique (event_id, user_id)
    );
    create index if not exists event_promotion_queue_undrained_idx on public.event_promotion_queue (event_id, user_id) where drained_at is null;

    alter table public.event_reminder_sent enable row level security;
    alter table public.event_promotion_queue enable row level security;
    -- No policies = no authenticated access. service_role bypasses RLS by definition.
    ```

    Comment per D-10: "event_reminder_sent enforces idempotent dedup for hourly fan-out per (event, user, kind)."
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -v '^--' supabase/migrations/20270801000008_p47_event_reminder_sent_and_queue.sql | grep -c "unique (event_id, user_id, kind)" | xargs test 1 -le && grep -v '^--' supabase/migrations/20270801000008_p47_event_reminder_sent_and_queue.sql | grep -c "check (kind in ('1d','1h','promotion'))" | xargs test 1 -le</automated>
  </verify>
  <done>
    Both tables created with FKs + UNIQUE constraints; CHECK kind enum present; RLS enabled with NO public policies (service-role only).
  </done>
</task>

</tasks>

<acceptance_criteria>
- `ls supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql supabase/migrations/20270801000004_p47_waitlist_promotion_trigger.sql supabase/migrations/20270801000008_p47_event_reminder_sent_and_queue.sql` — all 3 exist.
- `grep -v '^--' supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql | grep -ic 'for update' | xargs test 1 -le` — FOR UPDATE present (case-insensitive).
- `grep -v '^--' supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql | grep -c 'auth.uid()'` ≥ 2 (event_rsvp_create + event_get_join_url).
- `grep -v '^--' supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql | grep -ic 'on conflict do delete'` — exactly 0.
- `grep -v '^--' supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql | grep -c 'grant execute on function public.event_rsvp_create(uuid, text) to authenticated'` — exactly 1.
- `grep -v '^--' supabase/migrations/20270801000003_p47_event_rsvp_secdef.sql | grep -ic 'grant execute on function public.event_rsvp_create.* to service_role'` — exactly 0.
- `grep -v '^--' supabase/migrations/20270801000004_p47_waitlist_promotion_trigger.sql | grep -ic 'for update skip locked'` ≥ 1.
- `grep -v '^--' supabase/migrations/20270801000008_p47_event_reminder_sent_and_queue.sql | grep -c 'unique (event_id, user_id, kind)'` — exactly 1.
- All three files start with `begin;` and end with `commit;`.
- No `staff_users` references in any of the 3 files.
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → event_rsvp_create RPC | auth.uid() guard; capacity invariant via SELECT FOR UPDATE |
| client → event_get_join_url RPC | auth.uid() guard; RSVP status check; 15-min pre-window |
| trigger context → event_promotion_queue | SECDEF; FOR UPDATE SKIP LOCKED prevents double-promotion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-09 | T (Tampering) | capacity race | mitigate | `SELECT … FROM events WHERE id=p_event_id FOR UPDATE` serializes capacity check. |
| T-47-10 | T | client requests status='waitlist' | mitigate | RPC raises errcode='22023' if p_status NOT IN ('going','maybe','not_going'). |
| T-47-11 | T | concurrent cancel double-promotion | mitigate | Trigger uses `FOR UPDATE SKIP LOCKED` on waitlist head select. |
| T-47-12 | I (Info Disclosure) | non-RSVP'd user reads join_url | mitigate | event_get_join_url SECDEF checks rsvp_status='going' before returning URL. |
| T-47-13 | I | pre-event link-sharing | mitigate | event_get_join_url rejects with `too_early` until `now() >= start_at - 15min`. |
| T-47-14 | E (Elevation) | service_role calls event_rsvp_create | accept | Granting only to `authenticated` (not service_role); a malicious service-role caller has bigger problems. Per memory `feedback_rpc_auth_uid_vs_service_role_mismatch`. |
</threat_model>

<verification>
- All 3 migration files committed at strict 14-digit timestamps.
- All `<acceptance_criteria>` greps pass.
- Wave 0 test scaffolds (`event_rsvp_create.test.ts`, `waitlist-fifo-promotion.test.ts`, `waitlist-concurrent-cancel.test.ts`, `reminder-dedup.test.ts`) will RED locally; GREEN in Wave 3 close-out after `supabase db push --linked`.
</verification>

<success_criteria>
- All grep gates pass.
- RPCs use auth.uid() — never granted to service_role.
- Trigger uses FOR UPDATE SKIP LOCKED.
- No `staff_users` references.
- No `on conflict do delete` syntax.
</success_criteria>

<output>
After completion, create `.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-03-SUMMARY.md`.
</output>
