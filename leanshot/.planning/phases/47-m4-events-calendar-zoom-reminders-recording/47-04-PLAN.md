---
phase: 47-m4-events-calendar-zoom-reminders-recording
plan: 04
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270801000005_p47_event_covers_bucket.sql
  - supabase/migrations/20270801000006_p47_notification_event.sql
autonomous: true
requirements: [EVENT-01, EVENT-04]
must_haves:
  truths:
    - "event-covers Storage bucket exists (public read; 2MB cap; jpeg/png/webp only)"
    - "Bucket RLS: admin (is_staff()) writes; public read; path-prefix event_id"
    - "notification_settings VALID_CATEGORIES widened with 3 new categories: event_reminders_1d, event_reminders_1h, event_promotion"
    - "Same widening applied to notification_category_config, user_notifications, notification_dismissal_state (4 tables in one txn per Phase 44 pattern)"
    - "Defaults: all 3 new categories ON for existing users (backfill insert ON CONFLICT DO NOTHING)"
  artifacts:
    - path: "supabase/migrations/20270801000005_p47_event_covers_bucket.sql"
      provides: "event-covers Supabase Storage bucket + RLS"
      contains: "insert into storage.buckets"
    - path: "supabase/migrations/20270801000006_p47_notification_event.sql"
      provides: "VALID_CATEGORIES widening on 4 tables + backfill"
      contains: "alter table public.notification_settings"
  key_links:
    - from: "event-covers bucket"
      to: "Phase 44 community-media bucket"
      via: "FORK pattern (public+2MB cap)"
      pattern: "values \\('event-covers'"
    - from: "notification_settings CHECK"
      to: "Phase 44 community-mention/replies addition"
      via: "FORK pattern — widen check on 4 tables in one txn"
      pattern: "drop constraint if exists notification_settings_category_chk"
---

<objective>
Ship two infrastructure migrations: (1) `event-covers` Storage bucket per D-16, (2) `notification_settings` VALID_CATEGORIES widening with 3 new event categories per D-19.

Purpose: D-16 lets admin upload an event cover image. D-19 lets users opt out of event reminders per-kind (notification_settings.email|in_app flags read by event-reminders-fanout).
Output: 2 migration files. Bucket usable for admin uploads; categories live for fan-out Fn read.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-CONTEXT.md
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-RESEARCH.md
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-PATTERNS.md
@supabase/migrations/20270720000003_p44_community_media_bucket.sql
@supabase/migrations/20270720000004_p44_notification_community.sql

**Memory references:**
- `reference_supabase_is_staff_helper` — `public.is_staff()` is the canonical admin check; do NOT reference `staff_users`.
- `feedback_state_counter_table_needs_upsert_on_event` — backfill new notification rows with INSERT … ON CONFLICT DO NOTHING.
- `feedback_negation_grep_defeated_by_comment_string` — rejected alternative names stay out of committed SQL.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: event-covers Storage bucket + RLS</name>
  <files>supabase/migrations/20270801000005_p47_event_covers_bucket.sql</files>
  <behavior>
    - Bucket id='event-covers', public=true, file_size_limit=2097152 (2MB), allowed_mime_types includes only image/jpeg, image/png, image/webp.
    - storage.objects INSERT policy: `public.is_staff() AND (storage.foldername(name))[1] = bucket_id_segment_of_caller` (admin-only upload; path-prefix per event_id).
    - storage.objects SELECT: public read.
    - storage.objects UPDATE / DELETE: admin only via `public.is_staff()`.
  </behavior>
  <action>
    FORK `supabase/migrations/20270720000003_p44_community_media_bucket.sql` line 25-40. Wrap in `begin; … commit;`.

    Insert bucket (per RESEARCH §Pattern F + RESEARCH skeleton):
    ```
    insert into storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
    values (
      'event-covers',
      'event-covers',
      true,                                              -- D-16: public read
      2097152,                                           -- 2 MB cap
      array['image/jpeg','image/png','image/webp']
    )
    on conflict (id) do nothing;
    ```

    RLS policies on storage.objects scoped to this bucket:
    ```
    create policy event_covers_admin_insert on storage.objects
      for insert to authenticated
      with check (bucket_id = 'event-covers' and public.is_staff());

    create policy event_covers_public_read on storage.objects
      for select to authenticated, anon
      using (bucket_id = 'event-covers');

    create policy event_covers_admin_update on storage.objects
      for update to authenticated
      using (bucket_id = 'event-covers' and public.is_staff());

    create policy event_covers_admin_delete on storage.objects
      for delete to authenticated
      using (bucket_id = 'event-covers' and public.is_staff());
    ```

    Per memory `reference_supabase_migration_gotchas` — storage.objects DELETE may need `allow_delete_query` GUC on the migration; include `set local request.jwt.claims.role = 'service_role'` or use `do $$ … $$` with GUC if RLS test fails (defer to executor adjustment if needed).

    Do NOT introduce `staff_users` references (per memory `feedback_negation_grep_defeated_by_comment_string`).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -v '^--' supabase/migrations/20270801000005_p47_event_covers_bucket.sql | grep -c "'event-covers'" | xargs test 1 -le && grep -v '^--' supabase/migrations/20270801000005_p47_event_covers_bucket.sql | grep -c 'public.is_staff()' | xargs test 1 -le && grep -v '^--' supabase/migrations/20270801000005_p47_event_covers_bucket.sql | grep -c 'staff_users' | xargs test 0 -eq</automated>
  </verify>
  <done>
    Bucket migration committed; bucket public + 2MB + 3 MIME types; 4 RLS policies present; admin gate uses public.is_staff() only.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: notification_settings VALID_CATEGORIES widening (4 tables, 1 txn)</name>
  <files>supabase/migrations/20270801000006_p47_notification_event.sql</files>
  <behavior>
    - 4 tables (`notification_settings`, `notification_category_config`, `user_notifications`, `notification_dismissal_state`) drop+add CHECK constraint in ONE txn per D-19 + memory `feedback_state_counter_table_needs_upsert_on_event` + Phase 44 precedent.
    - 3 new category strings added: 'event_reminders_1d', 'event_reminders_1h', 'event_promotion' (underscored per D-19; mirrors Phase 44 community_* naming).
    - Backfill: `INSERT INTO notification_settings (user_id, category, in_app, email) SELECT id, c.category, true, true FROM profiles, VALUES(…) AS c(category) ON CONFLICT (user_id, category) DO NOTHING` — defaults ON for existing users.
  </behavior>
  <action>
    FORK `supabase/migrations/20270720000004_p44_notification_community.sql` lines 17-49. Wrap in `begin; … commit;`.

    For each of 4 tables, drop existing CHECK constraint by name, then re-add with the existing categories + 3 new ones. Use a single transaction so the constraint is consistent across all 4 tables at every visible state.

    Constraint names (from Phase 44):
    - `notification_settings_category_chk`
    - `notification_category_config_category_chk`
    - `user_notifications_category_chk`
    - `notification_dismissal_state_category_chk`

    New category list (existing + 3 additions):
    ```
    'dose-reminders','ai-insights','clinic-alerts','billing','marketing',
    'community-mentions','community-replies',
    'event_reminders_1d','event_reminders_1h','event_promotion'
    ```
    (Note Phase 44 uses hyphens for community-*; D-19 uses underscores for event_*. Keep both styles — the CHECK doesn't care.)

    Backfill block:
    ```
    insert into public.notification_settings (user_id, category, in_app, email)
    select p.id, c.category, true, true
      from public.profiles p,
           (values ('event_reminders_1d'),('event_reminders_1h'),('event_promotion')) as c(category)
    on conflict (user_id, category) do nothing;
    ```

    If `notification_category_config` is reference-data (not per-user), seed 3 rows there too with defaults.

    Comment per D-19: "P47 D-19: widen VALID_CATEGORIES with event_reminders_1d / event_reminders_1h / event_promotion."
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -v '^--' supabase/migrations/20270801000006_p47_notification_event.sql | grep -c "'event_reminders_1d'" | xargs test 3 -le && grep -v '^--' supabase/migrations/20270801000006_p47_notification_event.sql | grep -c 'drop constraint if exists' | xargs test 4 -le</automated>
  </verify>
  <done>
    Migration commits drop+add on 4 tables in 1 txn; 3 new categories present; backfill INSERT ON CONFLICT DO NOTHING shipped.
  </done>
</task>

</tasks>

<acceptance_criteria>
- `ls supabase/migrations/20270801000005_p47_event_covers_bucket.sql supabase/migrations/20270801000006_p47_notification_event.sql` — both exist.
- `grep -v '^--' supabase/migrations/20270801000005_p47_event_covers_bucket.sql | grep -c "'event-covers'"` ≥ 1.
- `grep -v '^--' supabase/migrations/20270801000005_p47_event_covers_bucket.sql | grep -c '2097152'` — exactly 1.
- `grep -v '^--' supabase/migrations/20270801000005_p47_event_covers_bucket.sql | grep -c 'public.is_staff()'` ≥ 3.
- `grep -v '^--' supabase/migrations/20270801000005_p47_event_covers_bucket.sql | grep -c 'staff_users'` — exactly 0.
- `grep -v '^--' supabase/migrations/20270801000006_p47_notification_event.sql | grep -c "'event_reminders_1d'"` ≥ 3.
- `grep -v '^--' supabase/migrations/20270801000006_p47_notification_event.sql | grep -c "'event_reminders_1h'"` ≥ 3.
- `grep -v '^--' supabase/migrations/20270801000006_p47_notification_event.sql | grep -c "'event_promotion'"` ≥ 3.
- `grep -v '^--' supabase/migrations/20270801000006_p47_notification_event.sql | grep -c 'drop constraint if exists'` ≥ 4.
- Both files start with `begin;` and end with `commit;`.
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → event-covers upload | bucket RLS gates on `public.is_staff()`; MIME whitelist + size cap |
| user → notification_settings | per-user opt-out toggles read by fan-out Fn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-15 | T (Tampering) | non-admin uploads cover | mitigate | bucket INSERT policy `with check (public.is_staff())`. |
| T-47-16 | T | malicious file type upload (e.g., svg with embedded JS) | mitigate | allowed_mime_types whitelist = jpeg/png/webp only; svg explicitly excluded. |
| T-47-17 | I | category enum drift between tables | mitigate | 4-table CHECK widening in ONE transaction (atomic). |
</threat_model>

<verification>
- Migration files committed.
- All grep gates pass.
- Bucket exists (verifiable post-push via `supabase storage list event-covers` or SQL `select * from storage.buckets where id='event-covers'`).
- VALID_CATEGORIES widening verifiable post-push via `select pg_get_constraintdef(oid) from pg_constraint where conname like '%_category_chk'`.
</verification>

<success_criteria>
- All grep gates pass.
- No `staff_users` references.
- Backfill uses `ON CONFLICT DO NOTHING` (idempotent).
</success_criteria>

<output>
After completion, create `.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-04-SUMMARY.md`.
</output>
