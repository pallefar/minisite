---
phase: 47-m4-events-calendar-zoom-reminders-recording
plan: 05
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270801000010_p47_pg_cron_schedules.sql
  - leanshot/tests/rls/event-visibility.test.ts
  - leanshot/tests/rls/event-visibility-orgscope.test.ts
  - leanshot/tests/rls/event-visibility-tiergate.test.ts
  - leanshot/tests/rls/fixtures-events.ts
  - leanshot/tests/integration/event-rsvp-capacity-race.test.ts
  - leanshot/tests/integration/waitlist-fifo-promotion.test.ts
  - leanshot/tests/integration/waitlist-concurrent-cancel.test.ts
  - leanshot/tests/integration/reminder-dedup.test.ts
  - supabase/functions/__tests__/event_rsvp_create.test.ts
  - supabase/functions/event-join-url/index.test.ts
  - supabase/functions/event-reminders-fanout/index.test.ts
  - supabase/functions/event-reminders-fanout/phi-routing.test.ts
  - supabase/functions/zoom-create-meeting/index.test.ts
  - supabase/functions/mux-webhook/event-recording.test.ts
  - leanshot/scripts/assert-bundle-budget.sh
autonomous: true
requirements: [EVENT-02, EVENT-03, EVENT-04, EVENT-05]
must_haves:
  truths:
    - "phase47-event-reminders-hourly pg_cron job scheduled at 0 * * * *"
    - "Cron body uses outer $cron$ + inner $reminders$ tags (NEVER bare $$)"
    - "Cron reads service_role_key from vault.decrypted_secrets WHERE name='service_role_key'"
    - "All 14 Wave 0 test files exist as RED scaffolds (it.todo / Deno.test('TODO'))"
    - "assert-bundle-budget.sh has an events chunk 25 kB gz entry (Wave 2 sets the actual size; scaffold adds the line)"
  artifacts:
    - path: "supabase/migrations/20270801000010_p47_pg_cron_schedules.sql"
      provides: "phase47-event-reminders-hourly cron schedule"
      contains: "select cron.schedule"
    - path: "leanshot/tests/rls/fixtures-events.ts"
      provides: "Event/RSVP seed helpers for RLS + integration tests"
      contains: "export async function createEventFixture"
    - path: "leanshot/scripts/assert-bundle-budget.sh"
      provides: "Bundle budget gate extended with events chunk"
      contains: "events"
  key_links:
    - from: "cron.schedule body"
      to: "vault.decrypted_secrets"
      via: "service_role_key lookup"
      pattern: "from vault.decrypted_secrets[^;]*name = 'service_role_key'"
    - from: "cron HTTP body"
      to: "https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/event-reminders-fanout"
      via: "pg_net.http_post"
      pattern: "functions/v1/event-reminders-fanout"
---

<objective>
Ship the hourly pg_cron + ALL 14 Wave 0 test scaffolds + bundle-budget extension. Implements D-10 + D-12 + VALIDATION.md Wave 0 gap list.

Purpose: Wave 0 test scaffolds enable Wave 1 executors to drive RED → GREEN against real RPC + Edge Fn behavior. Cron migration sits in Wave 0 (not Wave 1) so by the time the fan-out Fn deploys in Wave 1, the cron infrastructure is already pushed.
Output: 1 cron migration + 14 test scaffold files + 1 bundle-budget script edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-CONTEXT.md
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-RESEARCH.md
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-PATTERNS.md
@leanshot/.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-VALIDATION.md
@supabase/migrations/20270705000030_phase38_pg_cron_schedules.sql
@leanshot/tests/rls/fixtures-community.ts
@leanshot/tests/rls/community-spaces-rls.test.ts
@leanshot/tests/integration/community-mention-notification.test.ts

**Memory references (load-bearing):**
- `reference_postgres_dollar_quote_nesting_in_cron_body` — outer `$cron$` + inner `$reminders$` (UNIQUE per project — collision with Phase 38's `$digest$` is avoided).
- `reference_supabase_pg_cron_vault_service_role_pattern` — `vault.decrypted_secrets` row name='service_role_key'; hardcoded URL.
- `reference_supabase_service_role_key_format_divergence` — bearer = `sb_secret_*` token; cron migration reads vault → bearer.
- `reference_minisite_monorepo_layout` — paths relative to git root `/Users/karstenhaldan/minisite/`.
- `reference_deno_test_top_level_serve_trap` — Deno test scaffolds use `Deno.test('TODO …', () => {})` stubs; no top-level `Deno.serve()`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase47-event-reminders-hourly cron migration</name>
  <files>supabase/migrations/20270801000010_p47_pg_cron_schedules.sql</files>
  <action>
    FORK `supabase/migrations/20270705000030_phase38_pg_cron_schedules.sql` job 1 (`phase38-weekly-digest-hourly-fanout`). Wrap in `begin; … commit;`.

    Critical: outer dollar-quote tag `$cron$`, inner `$reminders$` (UNIQUE — must NOT collide with Phase 38's `$digest$`). Per memory `reference_postgres_dollar_quote_nesting_in_cron_body` — bare `$$ … $$` inside `cron.schedule(…, $$ … $$)` silently closes the outer quote causing "syntax error at or near DECLARE" at apply time.

    Body (copy RESEARCH §Code Examples Example 4 verbatim, lines 673-722):
    ```
    create extension if not exists pg_cron;
    create extension if not exists pg_net;

    do $unschedule$
    begin
      perform cron.unschedule('phase47-event-reminders-hourly');
    exception when others then null;
    end $unschedule$;

    select cron.schedule(
      'phase47-event-reminders-hourly',
      '0 * * * *',
      $cron$
      do $reminders$
      declare
        fn_url constant text := 'https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/event-reminders-fanout';
        service_key text;
      begin
        select decrypted_secret
          into service_key
          from vault.decrypted_secrets
         where name = 'service_role_key'
         limit 1;

        if service_key is null then
          raise notice 'phase47-event-reminders-hourly: service_role_key vault entry missing — skipping';
          return;
        end if;

        perform net.http_post(
          url := fn_url,
          body := '{}'::jsonb,
          headers := jsonb_build_object(
            'Authorization', 'Bearer ' || service_key,
            'Content-Type', 'application/json'
          ),
          timeout_milliseconds := 60000
        );
      end;
      $reminders$;
      $cron$
    );
    ```

    Hardcoded project ref `ytnsipxxmzgaebkqmokp` per memory `reference_supabase_pg_cron_vault_service_role_pattern`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -v '^--' supabase/migrations/20270801000010_p47_pg_cron_schedules.sql | grep -c '\$reminders\$' | xargs test 2 -le && grep -v '^--' supabase/migrations/20270801000010_p47_pg_cron_schedules.sql | grep -c 'phase47-event-reminders-hourly' | xargs test 2 -le && grep -v '^--' supabase/migrations/20270801000010_p47_pg_cron_schedules.sql | grep -c 'vault.decrypted_secrets' | xargs test 1 -le</automated>
  </verify>
  <done>
    Cron migration committed; outer `$cron$` + inner `$reminders$` tags present; project ref hardcoded; vault.decrypted_secrets read present; no bare `$$ … $$` nesting.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test scaffolds (14 files) + bundle-budget extension</name>
  <files>leanshot/tests/rls/event-visibility.test.ts, leanshot/tests/rls/event-visibility-orgscope.test.ts, leanshot/tests/rls/event-visibility-tiergate.test.ts, leanshot/tests/rls/fixtures-events.ts, leanshot/tests/integration/event-rsvp-capacity-race.test.ts, leanshot/tests/integration/waitlist-fifo-promotion.test.ts, leanshot/tests/integration/waitlist-concurrent-cancel.test.ts, leanshot/tests/integration/reminder-dedup.test.ts, supabase/functions/__tests__/event_rsvp_create.test.ts, supabase/functions/event-join-url/index.test.ts, supabase/functions/event-reminders-fanout/index.test.ts, supabase/functions/event-reminders-fanout/phi-routing.test.ts, supabase/functions/zoom-create-meeting/index.test.ts, supabase/functions/mux-webhook/event-recording.test.ts, leanshot/scripts/assert-bundle-budget.sh</files>
  <action>
    Create RED scaffolds for every Wave 0 test listed in VALIDATION.md §Wave 0 Requirements. Each file MUST be a minimal skeleton with TODO stubs that Wave 1+ executors fill in.

    **For Vitest TS files** (`leanshot/tests/...`):
    ```ts
    // REQUIRES: 47-{NN} supabase db push --linked + supabase functions deploy + ZOOM_S2S_* set
    import { describe, it } from 'vitest';
    describe('Phase 47 — <test name>', () => {
      it.todo('TODO: <behavior #1 from VALIDATION.md>');
      it.todo('TODO: <behavior #2>');
    });
    ```

    **For Deno test files** (`supabase/functions/...`):
    ```ts
    // REQUIRES: 47-{NN} migrations applied + Fn deployed + secrets set
    Deno.test('TODO Phase 47 — <behavior #1 from VALIDATION.md>', () => {
      throw new Error('TODO: scaffold; implement in Wave 1');
    });
    Deno.test('TODO Phase 47 — <behavior #2>', () => {
      throw new Error('TODO');
    });
    ```

    Per memory `reference_deno_test_top_level_serve_trap` — Deno test files MUST NOT call `Deno.serve(...)` at module top-level; only inside guarded `if (import.meta.main && Deno.serve)` blocks if any handler imports are added. Stubs above contain no serve() calls.

    **Test files to create** (mirror VALIDATION.md §Wave 0 Requirements):

    1. `leanshot/tests/rls/event-visibility.test.ts` — FORK `tests/rls/community-spaces-rls.test.ts` skeleton; add it.todo() stubs for EVENT-01 (cross-space RLS visibility, tier-gate, org-isolation across-space mix).
    2. `leanshot/tests/rls/event-visibility-orgscope.test.ts` — FORK `tests/rls/community-spaces-rls.test.ts`; it.todo() cross-tenant impersonation proof for org-scoped event.
    3. `leanshot/tests/rls/event-visibility-tiergate.test.ts` — FORK `tests/rls/community-tier-gating-rls.test.ts`; it.todo() free-tier user cannot see Pro-only event.
    4. `leanshot/tests/rls/fixtures-events.ts` — FORK `leanshot/tests/rls/fixtures-community.ts`; export `createEventFixture(admin, { space_id, capacity, … })`, `createRsvpFixture(admin, { event_id, user_id, status })`, plus the file-scoped `TEST_SLUG_PREFIX` per memory `feedback_rls_per_file_slug_prefix` precedent (used as part of seeded titles to enable teardown).
    5. `leanshot/tests/integration/event-rsvp-capacity-race.test.ts` — FORK `tests/integration/community-mention-notification.test.ts`; it.todo() 10 concurrent RSVPs at capacity=5 → exactly 5 going + 5 waitlist.
    6. `leanshot/tests/integration/waitlist-fifo-promotion.test.ts` — FORK same; it.todo() cancel #1 promotes head; cancel #2 promotes next.
    7. `leanshot/tests/integration/waitlist-concurrent-cancel.test.ts` — FORK same; it.todo() SKIP LOCKED proof (no double-promotion).
    8. `leanshot/tests/integration/reminder-dedup.test.ts` — FORK `tests/integration/notification-frequency-cap.test.ts`; it.todo() UNIQUE event_reminder_sent (event_id, user_id, kind) prevents duplicate send.
    9. `supabase/functions/__tests__/event_rsvp_create.test.ts` — FORK `supabase/functions/__tests__/community-mention-notification.test.ts` style; Deno.test stubs for: 'going under capacity', 'waitlist at capacity', 'auth.uid() null raises 42501', 'invalid status raises 22023', 'idempotent on (event_id, user_id) — second call UPDATEs not throws'.
    10. `supabase/functions/event-join-url/index.test.ts` — FORK `supabase/functions/mux-create-upload/index.test.ts`; Deno.test stubs for: 'returns url when going + in window', '403 too_early', '403 rsvp_required for maybe/not_going/no-row', '403 event_ended', 'admin (is_staff) preview anytime'.
    11. `supabase/functions/event-reminders-fanout/index.test.ts` — FORK `supabase/functions/notify-community/index.test.ts`; Deno.test stubs for: '1d window [23h,25h)', '1h window [0h,2h)', 'multi-TZ correctness (Los_Angeles vs New_York)', 'dedup UNIQUE skips already-sent', 'promotion-queue drain'.
    12. `supabase/functions/event-reminders-fanout/phi-routing.test.ts` — FORK `supabase/functions/_shared/email-router.test.ts`; Deno.test stubs for: 'phi=true → SES path for org-scoped event', 'phi=false → Resend path for global event', 'notification_settings off skips send'.
    13. `supabase/functions/zoom-create-meeting/index.test.ts` — FORK `supabase/functions/mux-create-upload/index.test.ts`; Deno.test stubs for: 'happy path → join_url + zoom_meeting_id written back', '401-retry-with-refresh', 'non-admin returns 403', 'event_not_found returns 404'.
    14. `supabase/functions/mux-webhook/event-recording.test.ts` — FORK `supabase/functions/mux-webhook/index.test.ts`; Deno.test stubs for: 'event-recording + attach_to_module_id NOT NULL → INSERTs course_lessons', 'attach_to_module_id NULL → writes events.recording_* only', 'missing event_id passthrough → log + 200'.

    **Bundle budget extension** — open `leanshot/scripts/assert-bundle-budget.sh` and add an `events` chunk entry. Read the file first; add a line for events chunk ≤25 kB gz. Exact format matches existing entries (community-feed, course-player precedent). If the script uses a JS array or a bash assoc array, append `events:25` (25 kB ceiling). Plan-checker grep guard: `grep 'events' leanshot/scripts/assert-bundle-budget.sh` returns ≥1.

    Each file MUST start with a header comment line: `// REQUIRES: 47-NN supabase db push --linked + supabase functions deploy …` per memory `feedback_handoff_doc_with_embedded_discoveries` and Phase 44 precedent.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls leanshot/tests/rls/event-visibility.test.ts leanshot/tests/rls/event-visibility-orgscope.test.ts leanshot/tests/rls/event-visibility-tiergate.test.ts leanshot/tests/rls/fixtures-events.ts leanshot/tests/integration/event-rsvp-capacity-race.test.ts leanshot/tests/integration/waitlist-fifo-promotion.test.ts leanshot/tests/integration/waitlist-concurrent-cancel.test.ts leanshot/tests/integration/reminder-dedup.test.ts supabase/functions/__tests__/event_rsvp_create.test.ts supabase/functions/event-join-url/index.test.ts supabase/functions/event-reminders-fanout/index.test.ts supabase/functions/event-reminders-fanout/phi-routing.test.ts supabase/functions/zoom-create-meeting/index.test.ts supabase/functions/mux-webhook/event-recording.test.ts && grep -l events leanshot/scripts/assert-bundle-budget.sh</automated>
  </verify>
  <done>
    All 14 test scaffold files exist with `it.todo` or `Deno.test('TODO …')` stubs; bundle budget script mentions `events`; no top-level `Deno.serve(handler)` calls in scaffolds.
  </done>
</task>

</tasks>

<acceptance_criteria>
- `ls supabase/migrations/20270801000010_p47_pg_cron_schedules.sql` exists.
- `grep -c '\$reminders\$' supabase/migrations/20270801000010_p47_pg_cron_schedules.sql` ≥ 2 (open + close tag).
- `grep -c '\$\$' supabase/migrations/20270801000010_p47_pg_cron_schedules.sql` — bare `$$` count limited to the `$unschedule$` block; no bare `$$` inside `cron.schedule(…)` body. Acceptable: outer `$cron$` + inner `$reminders$` + `$unschedule$` opener.
- `grep -c 'ytnsipxxmzgaebkqmokp' supabase/migrations/20270801000010_p47_pg_cron_schedules.sql` ≥ 1.
- All 14 test scaffold files exist at the canonical paths listed in `files_modified`.
- Each test scaffold contains `it.todo` (Vitest) OR `Deno.test('TODO` (Deno test).
- `grep -c 'Deno.serve' supabase/functions/__tests__/event_rsvp_create.test.ts supabase/functions/event-join-url/index.test.ts supabase/functions/event-reminders-fanout/index.test.ts supabase/functions/event-reminders-fanout/phi-routing.test.ts supabase/functions/zoom-create-meeting/index.test.ts supabase/functions/mux-webhook/event-recording.test.ts | awk -F: '{s+=$2} END {print s}'` — exactly 0 (no top-level serve in scaffolds, per memory `reference_deno_test_top_level_serve_trap`).
- `grep -l events leanshot/scripts/assert-bundle-budget.sh` non-empty.
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron → Edge Fn HTTP invoke | service_role_key from vault.decrypted_secrets; bearer auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-18 | S (Spoofing) | arbitrary caller invokes event-reminders-fanout | mitigate | checkServiceRoleBearer validates `sb_secret_*` token; cron is the only legitimate caller. |
| T-47-19 | T | dollar-quote nesting collision causes silent migration corruption | mitigate | outer `$cron$` + inner `$reminders$` (UNIQUE tags); plan-checker greps for `$reminders$` ≥ 2 and bare `$$` ≤ 1 inside cron body. |
| T-47-20 | I | service_role_key leaked in logs | accept | pg_net does not log Authorization header; project posture. |
</threat_model>

<verification>
- Cron migration committed; tag-nesting verified.
- All 14 test scaffolds present; can be invoked via `npx vitest run` (RED, as expected for `.todo`) and `deno test --no-check` (will throw on stubs, also RED).
- Bundle budget script updated.
</verification>

<success_criteria>
- All acceptance_criteria greps pass.
- No bare `$$` nesting inside `cron.schedule(…)` body (manual review by executor confirms).
- All test files start with REQUIRES header.
</success_criteria>

<output>
After completion, create `.planning/phases/47-m4-events-calendar-zoom-reminders-recording/47-05-SUMMARY.md`.
</output>
