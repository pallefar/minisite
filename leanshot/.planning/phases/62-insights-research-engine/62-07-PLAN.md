---
phase: 62-insights-research-engine
plan: 07
type: execute
wave: 1
depends_on:
  - 62-01
files_modified:
  - leanshot/src/components/dashboard/settings/ResearchConsentSection.tsx
  - leanshot/src/components/dashboard/settings/SettingsPage.tsx
  - leanshot/src/components/dashboard/settings/__tests__/ResearchConsentSection.test.tsx
autonomous: true
requirements:
  - INSIGHTS-05
user_setup: []

must_haves:
  truths:
    - "Settings page renders 'Research Participation' section with toggle"
    - "Toggle default is OFF; opt-in only (per HIPAA §164.508)"
    - "Toggle OFF → ON: silent enable + success toast"
    - "Toggle ON → OFF: opens revoke confirmation modal (never fire-and-forget)"
    - "Modal confirm calls UPDATE profiles SET research_consent=false, consent_revoked_at=now()"
    - "Confirm copy verbatim: 'Revoking will remove your data from future research within 24 hours. Already-published papers cite aggregate cohorts, not individuals — past publications are not retracted.'"
  artifacts:
    - path: "leanshot/src/components/dashboard/settings/ResearchConsentSection.tsx"
      provides: "Settings section with toggle + revoke confirmation modal"
      min_lines: 120
    - path: "leanshot/src/components/dashboard/settings/__tests__/ResearchConsentSection.test.tsx"
      provides: "Tests for toggle behavior + revoke modal"
  key_links:
    - from: "ResearchConsentSection toggle"
      to: "profiles.research_consent column"
      via: "supabase.from('profiles').update({ research_consent, consent_revoked_at })"
      pattern: "research_consent"
    - from: "SettingsPage NAV array"
      to: "ResearchConsentSection lazy import"
      via: "section entry { id: 'research-consent', Icon: FlaskConical }"
      pattern: "research-consent"
---

<objective>
Ship Surface 6 from UI-SPEC: the Research Participation consent toggle inside SettingsPage. Default OFF (opt-in only per HIPAA Privacy Rule §164.508). Toggle ON → silent enable + success toast. Toggle OFF (revoke) → confirmation modal with the verbatim copy from UI-SPEC. On confirm: writes UPDATE profiles SET research_consent=false, consent_revoked_at=now() — the nightly cron registered in 62-08 then handles the actual purge.

Purpose: Closes INSIGHTS-05 (opt-in consent + revoke triggers data drop within 30 days; actual SLA ≈24h via nightly cron).

Output: 1 new component + 1 modified SettingsPage + 1 test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-insights-research-engine/62-CONTEXT.md
@.planning/phases/62-insights-research-engine/62-UI-SPEC.md
@.planning/phases/62-insights-research-engine/62-PATTERNS.md

# Existing settings page (extension point)
@leanshot/src/components/dashboard/settings/SettingsPage.tsx

# DS primitives
@leanshot/src/components/ui/Modal.tsx
@leanshot/src/components/ui/Button.tsx

<interfaces>
<!-- SettingsPage NAV pattern (per PATTERNS.md lines 144-170 of analog):
-- Each section is a lazy-loaded component. Pattern from NewsletterSettings:
const NewsletterSettings = lazy(() => import('./NewsletterSettings'));
// In NAV array: { id: 'newsletter', label: 'Newsletter', Icon: Mail, Component: NewsletterSettings }

-- profiles row shape needed:
{ id: uuid, research_consent: boolean, consent_revoked_at: timestamptz | null, last_purged_at: timestamptz | null }

-- Toggle DS primitive: src/components/ui/Toggle.tsx if exists, else use checkbox + Tailwind toggle style or build small inline toggle.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResearchConsentSection component + revoke modal + tests</name>
  <files>leanshot/src/components/dashboard/settings/ResearchConsentSection.tsx, leanshot/src/components/dashboard/settings/__tests__/ResearchConsentSection.test.tsx</files>
  <behavior>
    - On mount: fetch profiles row for current user; set initial consent state.
    - Toggle ON (was OFF): calls supabase.from('profiles').update({ research_consent: true, consent_revoked_at: null }).eq('id', userId); on success show toast 'Research consent enabled.'; on error revert + show toast.
    - Toggle OFF (was ON): opens confirmation modal. Confirm → supabase.from('profiles').update({ research_consent: false, consent_revoked_at: new Date().toISOString() }).eq('id', userId); toast 'Research consent revoked. Your data will be removed from future rollups within 24 hours.'.
    - Modal cancel → toggle remains ON, no DB write.
    - During pending request: aria-busy on toggle.
    - All copy verbatim from UI-SPEC §Copywriting Contract.
  </behavior>
  <action>
**ResearchConsentSection.tsx** — per UI-SPEC §Surface 6 + PATTERNS.md ResearchConsentSection pattern:
- Imports: useState, useEffect, useCallback, supabase, useToast, Button, Modal (or use existing ConfirmModal at @/components/ui/Confirm if available), FlaskConical from lucide-react.
- State: consent (boolean), loading (boolean for initial fetch), updating (boolean), showRevokeModal (boolean).
- On mount: `supabase.auth.getUser()` → userId; `supabase.from('profiles').select('research_consent, consent_revoked_at').eq('id', userId).single()` → setConsent(data.research_consent ?? false); setLoading(false).
- Layout per UI-SPEC §Surface 6:
  - Section heading: `<h3 className="text-[18px] font-semibold text-[var(--color-text)]">Research Participation</h3>`
  - Description: `<p className="text-[13px] text-[var(--color-text-secondary)]">Your anonymized, aggregate data may be included in LeanShot research publications. No personal information is ever shared.</p>` (UI-SPEC copy verbatim).
  - Toggle row: `<FlaskConical className="size-5" aria-hidden="true" />` + `<label className="text-[13px] font-semibold">Research consent</label>` + Toggle switch (use existing Toggle primitive if available; else build inline `<button role="switch" aria-checked={consent} aria-busy={updating} ...>`).
  - Status text: `<span className="text-[11px] font-normal text-[var(--color-${consent ? 'success' : 'text-secondary'})]">{consent ? 'Enabled' : 'Disabled'}</span>`.
  - Toggle min-height 44px per UI-SPEC accessibility baseline.
  - Active toggle fill: `var(--color-primary)` when on.
- handleToggle(next: boolean):
  - If next === true (enabling): setUpdating(true); supabase.from('profiles').update({ research_consent: true, consent_revoked_at: null }).eq('id', userId); on success setConsent(true) + showToast('Research consent enabled.', 'success'); finally setUpdating(false).
  - If next === false (disabling): setShowRevokeModal(true); do NOT write yet.
- Revoke modal (use ConfirmModal/Modal DS):
  - Heading: 'Revoke research consent?' (18px/600).
  - Body: 'Revoking will remove your data from future research within 24 hours. Already-published papers cite aggregate cohorts, not individuals — past publications are not retracted.' (13px text-secondary).
  - Confirm button: 'Revoke consent' (variant=danger, min-height 44px).
  - Cancel button: 'Keep consent enabled' (variant=secondary).
  - On confirm: setUpdating(true); supabase.from('profiles').update({ research_consent: false, consent_revoked_at: new Date().toISOString() }).eq('id', userId); on success setConsent(false) + setShowRevokeModal(false) + showToast('Research consent revoked. Your data will be removed from future rollups within 24 hours.', 'info'); finally setUpdating(false).
- Error handling: revert state on error + toast 'Failed to update consent. Please try again.'.

Both named + default exports.

Typography ceiling: text-[11px]/text-[13px]/text-[18px] only. font-normal or font-semibold only.

**Tests at __tests__/ResearchConsentSection.test.tsx** (Vitest + Testing Library if available; otherwise logic-only tests):
- "Toggle defaults to false when profile.research_consent is false"
- "Toggle ON does not open revoke modal" — mock supabase update.
- "Toggle OFF opens revoke modal" — assert modal visible after user click.
- "Modal confirm writes UPDATE with research_consent=false + consent_revoked_at" — mock supabase.from('profiles').update; assert called with correct args.
- "Modal cancel keeps consent=true and does NOT write" — assert update not called.
- "Confirm modal copy includes verbatim 'within 24 hours' substring"

If @testing-library/react is unavailable, write tests against an extracted pure helper `useResearchConsentState` (hook) OR pure logic functions (e.g., a `getRevokeUpdate(): { research_consent: false; consent_revoked_at: string }` helper).
  </action>
  <verify>
    <automated>test -f leanshot/src/components/dashboard/settings/ResearchConsentSection.tsx &amp;&amp; grep -q "Revoke research consent" leanshot/src/components/dashboard/settings/ResearchConsentSection.tsx &amp;&amp; grep -q "within 24 hours" leanshot/src/components/dashboard/settings/ResearchConsentSection.tsx &amp;&amp; cd leanshot &amp;&amp; npx vitest run src/components/dashboard/settings/__tests__/ResearchConsentSection.test.tsx 2>&amp;1 | tee /tmp/p62-07-1.log &amp;&amp; grep -qE "Tests +[1-9][0-9]* passed" /tmp/p62-07-1.log &amp;&amp; ! grep -qE "[1-9][0-9]* failed" /tmp/p62-07-1.log &amp;&amp; npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | tee /tmp/p62-07-1b.log; if grep -qE "error TS" /tmp/p62-07-1b.log; then echo "tsc FAILED"; exit 1; fi; echo OK</automated>
  </verify>
  <done>Section component + tests pass; modal copy verbatim from UI-SPEC; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ResearchConsentSection into SettingsPage NAV</name>
  <files>leanshot/src/components/dashboard/settings/SettingsPage.tsx</files>
  <action>
- Read SettingsPage.tsx and locate the NAV array (per PATTERNS.md lines 144-170 + Settings page §section pattern).
- Add lazy import: `const ResearchConsentSection = lazy(() => import('./ResearchConsentSection'));` (mirror the NewsletterSettings lazy import).
- Add NAV entry. The exact position: AFTER any existing privacy/data-export sections, BEFORE the "danger zone" / account-deletion section if one exists. If unclear, append at the end of the privacy-related cluster.
  ```typescript
  { id: 'research-consent', label: 'Research Participation', Icon: FlaskConical, Component: ResearchConsentSection }
  ```
- Import FlaskConical from 'lucide-react' if not already imported.
- If the SettingsPage uses a `Section` type from `@/lib/i18n/settings-labels` (per PATTERNS.md convention), add `'research-consent'` to the Section union there.
- Verify tsc-clean.

Do NOT modify existing sections.
  </action>
  <verify>
    <automated>grep -q "ResearchConsentSection" leanshot/src/components/dashboard/settings/SettingsPage.tsx &amp;&amp; grep -q "research-consent" leanshot/src/components/dashboard/settings/SettingsPage.tsx &amp;&amp; cd leanshot &amp;&amp; npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | tee /tmp/p62-07-2.log; if grep -qE "error TS" /tmp/p62-07-2.log; then echo "tsc FAILED"; exit 1; fi; echo OK</automated>
  </verify>
  <done>SettingsPage NAV has research-consent entry; lazy import present; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user client → profiles row | RLS limits to `auth.uid() = id`; user can only update own row |
| revoke confirmation | confirmation modal prevents accidental disable; never fire-and-forget |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-07-01 | Tampering | accidental opt-in via auto-checked checkbox | mitigate | default OFF; toggle requires explicit user action |
| T-62-07-02 | Repudiation | user claims they never revoked | mitigate | consent_revoked_at timestamp persisted + nightly cron logs purge action |
| T-62-07-03 | Information Disclosure | toggle network error leaves UI inconsistent with DB | mitigate | optimistic state revert on error; toast surfaces failure |
| T-62-07-04 | Tampering | server-side accept invalid consent value | accept | RLS limits updates to authenticated user's own row; only boolean column updated |
</threat_model>

<verification>
- Modal copy contains literal 'within 24 hours' substring (UI-SPEC verbatim)
- Default research_consent is false in initial render before fetch
- Toggle OFF without confirmation impossible (modal is the only path)
- SettingsPage NAV array includes 'research-consent' entry
- tsc -p tsconfig.app.json --noEmit clean
</verification>

<success_criteria>
- Settings page renders Research Participation section
- Toggle ON enables consent without modal
- Toggle OFF opens revoke confirmation; only confirm writes
- Profile row updated with research_consent + consent_revoked_at
</success_criteria>

<output>
Create `.planning/phases/62-insights-research-engine/62-07-SUMMARY.md` when done. Include: SettingsPage NAV diff, modal copy verification, default state confirmation.
</output>
