---
phase: 62-insights-research-engine
plan: 05
type: execute
wave: 1
depends_on:
  - 62-01
  - 62-02
files_modified:
  - leanshot/src/types/research.ts
  - leanshot/src/components/admin/research/PublicationsListPage.tsx
  - leanshot/src/components/admin/research/PublicationEditorPage.tsx
  - leanshot/src/components/admin/research/PublicationStatusBadge.tsx
  - leanshot/src/components/admin/research/ResearchReviewBanner.tsx
  - leanshot/src/components/admin/research/ResearchKeyboardHelpModal.tsx
autonomous: true
requirements:
  - INSIGHTS-07
user_setup: []

must_haves:
  truths:
    - "PublicationsListPage lists research_publications with filter pills (All|Draft|In Review|Published|Archived)"
    - "PublicationEditorPage two-column layout with markdown textarea + metadata panel"
    - "Submit for Review transitions draft → in_review via submit_research_for_review RPC"
    - "Publish Research button absent from DOM when isAuthor=true (2-person rule, not disabled)"
    - "Publish Research click → publish_research RPC; SELF_REVIEW_REJECTED → 'Another admin must review' toast"
    - "ResearchReviewBanner mirrors ProtocolReviewBanner verbatim"
    - "PublicationStatusBadge tones: draft=neutral, in_review=warning, published=success, archived=neutral"
  artifacts:
    - path: "leanshot/src/types/research.ts"
      provides: "Canonical ResearchPublication type export — consumed by both Plan 62-05 admin UI and Plan 62-06 public hub"
      exports: ["ResearchPublication", "ResearchPublicationStatus"]
    - path: "leanshot/src/components/admin/research/PublicationsListPage.tsx"
      provides: "List + filter pills + keyboard nav (j/k/n/?)"
      min_lines: 200
    - path: "leanshot/src/components/admin/research/PublicationEditorPage.tsx"
      provides: "Two-column editor + 2-person rule DOM gate"
      min_lines: 300
    - path: "leanshot/src/components/admin/research/PublicationStatusBadge.tsx"
      provides: "Badge wrapper for status tones"
    - path: "leanshot/src/components/admin/research/ResearchReviewBanner.tsx"
      provides: "Verbatim mirror of ProtocolReviewBanner"
      min_lines: 50
    - path: "leanshot/src/components/admin/research/ResearchKeyboardHelpModal.tsx"
      provides: "Modal listing j/k/n/?/Esc shortcuts"
  key_links:
    - from: "Publish Research click"
      to: "publish_research RPC"
      via: "supabase.rpc('publish_research', {p_publication_id})"
      pattern: "publish_research"
    - from: "PublicationEditorPage isSelfCreated"
      to: "ResearchReviewBanner publish button visibility"
      via: "isAuthor prop controls DOM render (not disabled)"
      pattern: "isAuthor"
    - from: "Submit for Review click"
      to: "submit_research_for_review RPC"
      via: "supabase.rpc('submit_research_for_review', {p_publication_id})"
      pattern: "submit_research_for_review"
---

<objective>
Ship the white-paper authoring + 2-person review surface: PublicationsListPage (list + filter pills + keyboard shortcuts), PublicationEditorPage (two-column markdown editor + metadata panel + state machine buttons), PublicationStatusBadge, ResearchReviewBanner (verbatim mirror of ProtocolReviewBanner), ResearchKeyboardHelpModal.

Purpose: Surface 2 + Surface 3 from UI-SPEC. Closes INSIGHTS-07 (white-paper publishing pipeline with 2-person review per PROTOCOL-04). The 3-layer invariant for 2-person rule: (1) DB SECDEF SELF_REVIEW_REJECTED, (2) UI conditional render, (3) CI test from Plan 62-02.

Output: 5 new component files. tsc-clean. SELF_REVIEW_REJECTED toast wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-insights-research-engine/62-CONTEXT.md
@.planning/phases/62-insights-research-engine/62-UI-SPEC.md
@.planning/phases/62-insights-research-engine/62-PATTERNS.md

# Verbatim analogs (per PATTERNS.md and feedback_planner_prompt_explicit_reuse_targets)
@leanshot/src/components/admin/protocols/ProtocolsListPage.tsx
@leanshot/src/components/admin/protocols/ProtocolEditorPage.tsx
@leanshot/src/components/admin/protocols/ProtocolReviewBanner.tsx
@leanshot/src/components/admin/protocols/ProtocolStatusBadge.tsx
@leanshot/src/components/admin/protocols/ProtocolKeyboardHelpModal.tsx

<interfaces>
<!-- Types this plan introduces (also used by 62-06 public hub):
export interface ResearchPublication {
  id: string;
  slug: string;
  markdown_path: string;
  title: string;
  abstract: string | null;
  status: 'draft' | 'in_review' | 'published' | 'archived';
  created_by: string | null;
  reviewer_id: string | null;
  published_at: string | null;
  cohort_size: number | null;
  epsilon: number | null;
  suppressed_buckets: number | null;
  created_at: string;
  updated_at: string;
}

-- publish_research RPC error semantics:
--   message contains 'SELF_REVIEW_REJECTED' → 403; show toast 'Another admin must review this publication before publish.'
--   message contains 'cannot publish research in status' → 422; show 'Publication is not ready for publish.'
--   other → generic error toast

-- ProtocolReviewBanner verbatim shape (per PATTERNS.md):
interface ResearchReviewBannerProps {
  isAuthor: boolean;
  reviewerName?: string;
  onPublish?: () => Promise<void>;
  publishing?: boolean;
}
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Canonical ResearchPublication type export (interface contract for 62-06)</name>
  <files>leanshot/src/types/research.ts</files>
  <action>
Create `leanshot/src/types/research.ts` with the canonical ResearchPublication type the admin UI (this plan) AND the public hub (Plan 62-06) both consume. This avoids the dual-declaration smell flagged at plan-check WARNING 4.

Exports (exact shape — mirrors Plan 62-01 schema + the ALTER from Plan 62-02 Task 2):
- `export type ResearchPublicationStatus = 'draft' | 'in_review' | 'published' | 'archived';`
- `export interface ResearchPublication { id: string; slug: string; markdown_path: string; markdown_body: string | null; title: string; abstract: string | null; status: ResearchPublicationStatus; created_by: string | null; reviewer_id: string | null; published_at: string | null; cohort_size: number | null; epsilon: number | null; suppressed_buckets: number | null; created_at: string; updated_at: string; }`

PublicationsListPage / PublicationEditorPage (Task 1 + Task 2) MUST `import type { ResearchPublication, ResearchPublicationStatus } from '@/types/research';` — no inline re-declaration.

Plan 62-06 lib/research/api.ts MUST also import from this same module via depends_on edge (added in 62-06 frontmatter).
  </action>
  <verify>
    <automated>test -f leanshot/src/types/research.ts &amp;&amp; grep -q "export interface ResearchPublication" leanshot/src/types/research.ts &amp;&amp; grep -q "markdown_body" leanshot/src/types/research.ts &amp;&amp; cd leanshot &amp;&amp; npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <done>types/research.ts exists; exports ResearchPublication + ResearchPublicationStatus; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: ResearchReviewBanner + PublicationStatusBadge + KeyboardHelpModal (small verbatim mirrors)</name>
  <files>leanshot/src/components/admin/research/ResearchReviewBanner.tsx, leanshot/src/components/admin/research/PublicationStatusBadge.tsx, leanshot/src/components/admin/research/ResearchKeyboardHelpModal.tsx</files>
  <behavior>
    - ResearchReviewBanner renders ONLY when status === 'in_review'. When isAuthor=true: banner shows 'Pending review by another admin' + Clock icon, NO publish button. When isAuthor=false: shows 'Review as: {reviewerName}' + Publish Research button.
    - PublicationStatusBadge maps status to Badge tone: draft→neutral, in_review→warning, published→success, archived→neutral. Label text matches status capitalized.
    - ResearchKeyboardHelpModal lists shortcuts j/k (navigate rows), n (new), Enter (open), ? (this help), Esc (close).
  </behavior>
  <action>
**ResearchReviewBanner.tsx** — VERBATIM mirror of ProtocolReviewBanner.tsx (per PATTERNS.md lines 18-63). Changes:
- Component name: ProtocolReviewBanner → ResearchReviewBanner
- Props interface: ResearchReviewBannerProps
- Publish button label: 'Publish Protocol' → 'Publish Research'
- File is small (~50 lines). Both export named + default.

**PublicationStatusBadge.tsx** — mirror ProtocolStatusBadge.tsx structure. Props: `{ status: 'draft' | 'in_review' | 'published' | 'archived' }`. Returns Badge with tone mapping per behavior above. Label capitalized first letter + space replacing underscore (e.g., 'In Review').

**ResearchKeyboardHelpModal.tsx** — mirror ProtocolKeyboardHelpModal.tsx. Same shortcuts list. Props: `{ open: boolean; onClose: () => void }`. Uses Modal DS primitive.

All three files: typography STRICTLY text-[11px]/text-[13px]/text-[18px]/text-heading. font-normal or font-semibold only. @theme tokens only.

No tests required for these three small mirror files (covered by analog tests already passing).
  </action>
  <verify>
    <automated>test -f leanshot/src/components/admin/research/ResearchReviewBanner.tsx &amp;&amp; test -f leanshot/src/components/admin/research/PublicationStatusBadge.tsx &amp;&amp; test -f leanshot/src/components/admin/research/ResearchKeyboardHelpModal.tsx &amp;&amp; grep -q "Publish Research" leanshot/src/components/admin/research/ResearchReviewBanner.tsx &amp;&amp; grep -q "isAuthor" leanshot/src/components/admin/research/ResearchReviewBanner.tsx &amp;&amp; cd leanshot &amp;&amp; npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | tee /tmp/p62-05-1.log; if grep -qE "error TS" /tmp/p62-05-1.log; then echo "tsc FAILED"; exit 1; fi; echo OK</automated>
  </verify>
  <done>3 small mirror files exist; ResearchReviewBanner contains 'Publish Research' literal; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PublicationsListPage + PublicationEditorPage (2-person rule + state machine)</name>
  <files>leanshot/src/components/admin/research/PublicationsListPage.tsx, leanshot/src/components/admin/research/PublicationEditorPage.tsx</files>
  <behavior>
    - PublicationsListPage fetches `research_publications` ordered by updated_at desc; renders table with columns Title | Slug | Status | Created by | Published date | Actions; filter pills All|Draft|In Review|Published|Archived; keyboard shortcuts j/k/n/?; new publication button.
    - PublicationEditorPage loads single publication by id from URL; two-column grid (markdown textarea + metadata panel sticky right 320px).
    - Editor "Save Draft" updates publication row.
    - Editor "Submit for Review" calls submit_research_for_review RPC; works only when current user = created_by and status = draft.
    - Editor renders <ResearchReviewBanner isAuthor={isSelfCreated} ...> when status === 'in_review'.
    - Editor handlePublish() calls publish_research RPC; SELF_REVIEW_REJECTED substring or code=42501 → toast 'Another admin must review this publication before publish.' (UI-SPEC copy).
    - Publish button NEVER renders in DOM when isSelfCreated (conditional render in ResearchReviewBanner via isAuthor prop) — UI-SPEC interaction contract.
  </behavior>
  <action>
**PublicationsListPage.tsx** — mirror ProtocolsListPage.tsx (per PATTERNS.md exact match):
- Imports: useCallback, useEffect, useMemo, useState, Badge, Button, EmptyState, Pill, supabase, useStore.
- Fetch in useEffect with cancellation token: `supabase.from('research_publications').select('id, slug, title, abstract, status, created_by, reviewer_id, published_at, created_at, updated_at').order('updated_at', { ascending: false })`.
- Filter pills (Pill DS, size="sm", aria-pressed): All | Draft | In Review | Published | Archived. useMemo for filtered rows.
- Keyboard shortcuts (per analog lines 143-174): `j`/`k` to highlight next/prev row, `n` to navigate to /admin/research/publications/new (handleNew), `Enter` to open highlighted row, `Shift+?` opens ResearchKeyboardHelpModal, `Escape` closes modal/help.
- handleNew(): inserts a draft row `supabase.from('research_publications').insert({ slug: tempSlug, title: 'Untitled Research', markdown_path: '', status: 'draft', created_by: currentUserId }).select().single()` then navigates to `/admin/research/publications/{newId}`.
- Render columns per UI-SPEC §Surface 2 Publication list. PublicationStatusBadge for status column.
- Sticky header: `z-10 pb-3 border-b border-[var(--color-border)] mb-4`.
- Row hover: `var(--color-admin-table-row-hover)`.
- Typography ceiling enforced (11/13/18/text-heading).
- New Publication CTA Button variant="primary" with text "New Publication" (UI-SPEC copywriting contract).
- Filter mode prop optional: `<PublicationsListPage initialStatusFilter="in_review">` lets the ResearchLayout "Review Queue" sub-nav reuse this page with status=in_review default.
- Both named + default exports.

**PublicationEditorPage.tsx** — mirror ProtocolEditorPage.tsx (per PATTERNS.md):
- Imports: useCallback, useEffect, useMemo, useState, supabase, useToast, useStore, Button, Skeleton, Modal, ResearchReviewBanner, PublicationStatusBadge.
- URL parse: extract publication id from window.location.pathname matching `/admin/research/publications/([^/]+)`.
- Fetch publication into state on mount (loadPublication callback).
- State: publication (Publication | undefined | null), saving, publishing, submitting, archiving, currentUserId.
- isSelfCreated = currentUserId === publication?.created_by.
- Layout: `grid gap-6 lg:grid-cols-[1fr_320px]`.
- Left column: markdown_path display (font-mono 13px text-secondary), textarea for markdown_content (h-96, font-mono, text-[13px], var(--color-border) border, focus ring var(--color-primary), resize-y). onBlur saves draft via handleSaveDraft.
- Right column (sticky): inline title input (18px/600 editable), <PublicationStatusBadge status={publication.status} />, created-by display, "Save Draft" button (variant=secondary), "Submit for Review" (primary, accent, author only, only when status=draft), <ResearchReviewBanner ...> (when status=in_review), "Archive" link (text-danger, 13px) opens confirm modal.
- handleSaveDraft: supabase.from('research_publications').update({ title, markdown_path, abstract, updated_at: new Date().toISOString() }).eq('id', publication.id). Toast on success.
- handleSubmit: supabase.rpc('submit_research_for_review', { p_publication_id: publication.id }). Reload publication. Toast.
- handlePublish (per PATTERNS.md lines 313-336): supabase.rpc('publish_research', { p_publication_id: publication.id }). If error.message?.includes('SELF_REVIEW_REJECTED') || error.code === '42501' → showToast('Another admin must review this publication before publish.', 'error'). Otherwise show error.message. On success: showToast('Publication published.', 'success'); reload publication.
- handleArchive: open confirm modal; on confirm call supabase.rpc('archive_research', { p_publication_id }).
- Loading state: <Skeleton className="h-8 w-1/3 rounded-card" /> stack.
- Error state: `<div className="text-[13px] text-[var(--color-danger)] p-6">Failed to load publication.</div>`.
- 2-person rule in UI: <ResearchReviewBanner isAuthor={isSelfCreated} reviewerName={...} onPublish={handlePublish} publishing={publishing} /> — Publish button fully absent from DOM when isAuthor=true (per ResearchReviewBanner internal conditional from Task 1).
- New publication route: when URL is `/admin/research/publications/new`, render a "create new" form (slug + title) → on submit inserts row + navigates to `/admin/research/publications/{newId}`.
- Both named + default exports.

Both files: text-* tokens STRICTLY 11/13/18/text-heading. @theme tokens only.

If `react-router-dom` is wired for admin per [[reference_react_router_consumer_admin_split]], use useNavigate; else use window.history.pushState + popstate (matches ProtocolEditorPage).

Tests (optional smoke at __tests__/PublicationEditorPage.test.tsx):
- "Publish button NOT in DOM when isSelfCreated" — render with currentUserId === publication.created_by; assert no element with text 'Publish Research'.
- "handlePublish shows SELF_REVIEW_REJECTED toast when RPC errors with that substring" — mock rpc.
- Defer full smoke to 62-08 close-out against staging.
  </action>
  <verify>
    <automated>test -f leanshot/src/components/admin/research/PublicationsListPage.tsx &amp;&amp; test -f leanshot/src/components/admin/research/PublicationEditorPage.tsx &amp;&amp; grep -q "publish_research" leanshot/src/components/admin/research/PublicationEditorPage.tsx &amp;&amp; grep -q "submit_research_for_review" leanshot/src/components/admin/research/PublicationEditorPage.tsx &amp;&amp; grep -q "SELF_REVIEW_REJECTED" leanshot/src/components/admin/research/PublicationEditorPage.tsx &amp;&amp; grep -q "isAuthor" leanshot/src/components/admin/research/PublicationEditorPage.tsx &amp;&amp; cd leanshot &amp;&amp; npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | tee /tmp/p62-05-2.log; if grep -qE "error TS" /tmp/p62-05-2.log; then echo "tsc FAILED"; exit 1; fi; grep -vh '^[[:space:]]*//' src/components/admin/research/PublicationsListPage.tsx src/components/admin/research/PublicationEditorPage.tsx | grep -E "text-base|text-lg|text-md|text-sm|text-xl|text-2xl" | wc -l | awk '{ if ($1 == 0) print "OK"; else { print "typography-FAIL count=" $1; exit 1 } }' &amp;&amp; \
# WARNING 6 scope-limited @theme token audit — admin/research/* List + Editor pages only
USED_TOKENS=$(grep -ohE "var\(--color-[a-zA-Z0-9_-]+\)" src/components/admin/research/PublicationsListPage.tsx src/components/admin/research/PublicationEditorPage.tsx 2>/dev/null | sort -u); for tok in $USED_TOKENS; do name=$(echo "$tok" | sed -E "s/var\((--color-[a-zA-Z0-9_-]+)\)/\1/"); grep -q "$name" src/index.css || { echo "UNDEFINED @theme token: $name"; exit 1; }; done; echo @theme-OK</automated>
  </verify>
  <done>List + Editor pages ship; both RPC wires (publish_research + submit_research_for_review) present; SELF_REVIEW_REJECTED toast literal present; tsc clean; typography ceiling enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author client → publish_research | DOM hides publish button; SECDEF rejects with SELF_REVIEW_REJECTED if forced; 3-layer invariant |
| draft → in_review | submit_research_for_review requires actor === created_by + status === draft |
| in_review → published | publish_research requires actor !== created_by + status === in_review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-05-01 | Tampering | author bypasses 2-person via DevTools | mitigate | DB SECDEF SELF_REVIEW_REJECTED guard (Plan 62-02) — UI hide is one of 3 layers |
| T-62-05-02 | Tampering | concurrent publish race | mitigate | publish_research FOR UPDATE row lock (Plan 62-02) |
| T-62-05-03 | Information Disclosure | unpublished draft visible to non-staff | mitigate | RLS on research_publications (Plan 62-01) — public SELECT only when status='published' |
| T-62-05-04 | Tampering | typography ceiling breach renders ugly | mitigate | grep gate in <verify> blocks banned utilities |
| T-62-05-05 | Tampering | SELF_REVIEW_REJECTED message drift between RPC and UI | mitigate | UI matches literal substring 'SELF_REVIEW_REJECTED' (per Phase 61 pattern + Plan 62-02 RAISE EXCEPTION literal) |
</threat_model>

<verification>
- Both pages reference the three RPCs by exact name
- SELF_REVIEW_REJECTED literal substring present in PublicationEditorPage.tsx
- ResearchReviewBanner Publish button conditionally rendered (NOT disabled) per isAuthor prop
- Typography ceiling clean (no text-base/lg/md/sm/xl/2xl)
- tsc -p tsconfig.app.json --noEmit clean
</verification>

<success_criteria>
- /admin/research/publications renders list with filter pills + keyboard nav
- /admin/research/publications/{id} renders editor with 2-column layout
- Submit for Review wires to RPC; Publish Research wires to RPC
- Self-publish attempt produces UI-SPEC error toast literal copy
- ResearchReviewBanner mirrors ProtocolReviewBanner verbatim
</success_criteria>

<output>
Create `.planning/phases/62-insights-research-engine/62-05-SUMMARY.md` when done. Include: 5 component file paths, RPC wire-up confirmations, 2-person rule layer summary, @theme tokens used.
</output>
