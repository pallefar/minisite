---
phase: 62-insights-research-engine
plan: 04
type: execute
wave: 1
depends_on:
  - 62-01
  - 62-02
files_modified:
  - leanshot/src/components/admin/research/ResearchLayout.tsx
  - leanshot/src/components/admin/research/CohortBuilderPage.tsx
  - leanshot/src/components/admin/research/CohortBuilderForm.tsx
  - leanshot/src/components/admin/research/RetentionChart.tsx
  - leanshot/src/components/admin/research/CrossTabMatrix.tsx
  - leanshot/src/lib/admin/modules.ts
autonomous: true
requirements:
  - INSIGHTS-01
  - INSIGHTS-02
  - INSIGHTS-06
user_setup: []

must_haves:
  truths:
    - "/admin/research route renders ResearchLayout with sub-nav (Cohort Builder | Publications | Review Queue)"
    - "CohortBuilderForm posts to estimate_research_cohort on filter change (debounced 400ms) and to compile_research_cohort on Run Cohort click"
    - "k_floor sentinel UX active: <5 displays 'Cohort too small (k<5) — broaden filters', disables Run Cohort button"
    - "RetentionChart renders Chart.js stacked-area via BaseChart wrapper with key={theme} on parent"
    - "CrossTabMatrix renders Table with epsilon caption + suppressed cells as '—' in text-tertiary"
    - "admin/modules.ts registers research module with FlaskConical icon"
  artifacts:
    - path: "leanshot/src/components/admin/research/ResearchLayout.tsx"
      provides: "Admin research shell; mirrors ProtocolsLayout"
      min_lines: 80
    - path: "leanshot/src/components/admin/research/CohortBuilderPage.tsx"
      provides: "Page composing CohortBuilderForm + RetentionChart + CrossTabMatrix"
      min_lines: 80
    - path: "leanshot/src/components/admin/research/CohortBuilderForm.tsx"
      provides: "Filter form + live size estimate + k_floor warning + Run Cohort"
      min_lines: 100
    - path: "leanshot/src/components/admin/research/RetentionChart.tsx"
      provides: "BaseChart wrapper for stacked-area retention curve"
      min_lines: 30
    - path: "leanshot/src/components/admin/research/CrossTabMatrix.tsx"
      provides: "Pure display Table with suppressed cells"
      min_lines: 40
    - path: "leanshot/src/lib/admin/modules.ts"
      provides: "Adds research module entry to ADMIN_MODULES"
      contains: "key: 'research'"
  key_links:
    - from: "CohortBuilderForm"
      to: "compile_research_cohort SECDEF RPC"
      via: "supabase.rpc('compile_research_cohort', {p_filters})"
      pattern: "compile_research_cohort"
    - from: "CohortBuilderForm filter change"
      to: "estimate_research_cohort SECDEF RPC"
      via: "debounced 400ms supabase.rpc call"
      pattern: "estimate_research_cohort"
    - from: "ADMIN_MODULES research entry"
      to: "ResearchLayout lazy import"
      via: "lazy: () => import('@/components/admin/research/ResearchLayout')"
      pattern: "components/admin/research/ResearchLayout"
---

<objective>
Ship the admin cohort builder surface: ResearchLayout (mirror ProtocolsLayout verbatim), CohortBuilderPage + CohortBuilderForm (filter UI + live size estimate + Run Cohort button), RetentionChart (BaseChart wrapper for stacked-area), CrossTabMatrix (Table primitive with suppressed cells), and the admin module manifest entry.

Purpose: Surface 1 from UI-SPEC. Closes INSIGHTS-06 (admin dashboard) and gives a UI on top of Plan 62-02's compile_research_cohort / estimate_research_cohort RPCs. Closes INSIGHTS-01/02 visibly (k_floor sentinel + epsilon display).

Output: 5 new components + 1 modified manifest file. Verified via tsc + vitest smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-insights-research-engine/62-CONTEXT.md
@.planning/phases/62-insights-research-engine/62-UI-SPEC.md
@.planning/phases/62-insights-research-engine/62-PATTERNS.md

# Verbatim analogs (per PATTERNS.md and feedback_planner_prompt_explicit_reuse_targets)
@leanshot/src/components/admin/protocols/ProtocolsLayout.tsx
@leanshot/src/components/admin/protocols/ProtocolsListPage.tsx
@leanshot/src/components/dashboard/charts/BaseChart.tsx
@leanshot/src/lib/admin/modules.ts

# DS primitives used
@leanshot/src/components/ui/Button.tsx
@leanshot/src/components/ui/Pill.tsx
@leanshot/src/components/ui/Badge.tsx
@leanshot/src/components/ui/EmptyState.tsx

<interfaces>
<!-- ProtocolsLayout pattern (mirror verbatim per PATTERNS.md):
function resolveActive(pathname: string): SubRoute {
  const m = pathname.match(/^\/admin\/research\/?(?:([^/]+).*)?$/);
  const seg = (m?.[1] ?? '').toLowerCase();
  const found = SUB_ROUTES.find((r) => r.path === seg);
  return found ?? SUB_ROUTES.find((r) => r.key === DEFAULT_KEY)!;
}

const SUB_ROUTES: SubRoute[] = [
  { key: 'cohort', path: 'cohort', label: 'Cohort Builder', Component: lazy(() => import('./CohortBuilderPage')) },
  { key: 'publications', path: 'publications', label: 'Publications', Component: lazy(() => import('./PublicationsListPage')) },
  { key: 'review', path: 'review', label: 'Review Queue', Component: lazy(() => import('./PublicationsListPage')) },  -- reuses PublicationsListPage with status=in_review default filter (Plan 62-05)
];
const DEFAULT_KEY: SubRouteKey = 'cohort';

-- compile_research_cohort RPC response shape (from Plan 62-02):
type CohortResponse =
  | { suppressed: true; reason: 'k_floor'; k: number }
  | { suppressed: false; cohort_size: number; epsilon: number; suppressed_buckets: number; metric: string; data: Array<Record<string, any>> };

-- estimate_research_cohort response shape:
type EstimateResponse = { cohort_size: number; below_floor: boolean };

-- BaseChart props (from BaseChart.tsx):
interface BaseChartProps { config: ChartConfiguration; ariaLabel: string; height?: number }

-- ADMIN_MODULES entry shape (lines 323-337 of modules.ts):
{ key: string; label: string; route: string; icon: LucideIcon; lazy: () => Promise<{default: ComponentType}>; flagKey?: string; minRole?: 'staff' }
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResearchLayout + CohortBuilderForm (k_floor + epsilon UX) + module manifest</name>
  <files>leanshot/src/components/admin/research/ResearchLayout.tsx, leanshot/src/components/admin/research/CohortBuilderForm.tsx, leanshot/src/lib/admin/modules.ts</files>
  <behavior>
    - ResearchLayout renders sub-nav (Cohort Builder | Publications | Review Queue) + content area. Mirrors ProtocolsLayout structure verbatim (200px left + 1fr right grid).
    - CohortBuilderForm exposes filter controls: compound multiselect, tenure_bucket select, audience multiselect, outcome_metric select.
    - On filter change: debounced 400ms call to estimate_research_cohort RPC; updates `estimatedCohortSize` state.
    - When estimatedCohortSize < 5: replaces estimate display with 'Cohort too small (k<5) — broaden filters' (13px/600, color-warning, AlertTriangle icon); disables Run Cohort button (aria-disabled='true' + pointer-events-none + opacity-50).
    - When estimatedCohortSize >= 5: shows estimate (font-mono tabular-nums) + epsilon display 'epsilon = 1.0 (admin output)'.
    - Run Cohort click: calls compile_research_cohort RPC; lifts result up to parent CohortBuilderPage (in Task 2) via onResult prop.
    - ADMIN_MODULES gains a 'research' entry at end of array.
  </behavior>
  <action>
**ResearchLayout.tsx** — per PATTERNS.md mirror ProtocolsLayout.tsx verbatim:
- Copy lines 1-120 of leanshot/src/components/admin/protocols/ProtocolsLayout.tsx.
- Change: aria-label="Protocols sections" → "Research sections"; the regex `/^\/admin\/protocols\/?(?:([^/]+).*)?$/` → `/^\/admin\/research\/?(?:([^/]+).*)?$/`; href base `/admin/protocols/` → `/admin/research/`; SUB_ROUTES per the <interfaces> block above (cohort | publications | review); DEFAULT_KEY = 'cohort'.
- Imports: React, Suspense, lazy, useEffect, useState, ComponentType.
- Active route accent: `bg-[var(--color-primary)] text-[var(--color-primary-foreground)]` per UI-SPEC §accent reserved-for #2.
- Typography: text-[13px] font-semibold per UI-SPEC.
- Suspense fallback: `<div className="p-6 text-[13px] text-[var(--color-text-secondary)]">Loading…</div>`.
- Export both named (ResearchLayout) and default per CLAUDE.md convention.

**CohortBuilderForm.tsx** — new component (no direct analog; closest is ProtocolsListPage filter pills + form patterns):
- Props: `{ onResult: (result: CohortResponse) => void; onLoading?: (loading: boolean) => void }`.
- State: compounds (string[]), tenureBucket ('<3m'|'3-6m'|'6-12m'|'12m+'|null), audience (string[]), outcomeMetric ('weight_change'|'dose_adherence'|'retention_30d'|'symptom_severity'), estimatedCohortSize (number|null), estimating (boolean), running (boolean).
- Debounced effect (400ms via setTimeout in useEffect with cleanup) on filter change: calls `supabase.rpc('estimate_research_cohort', { p_filters: { metric: outcomeMetric, compound: compounds, tenure_bucket: tenureBucket, audience_segment: audience } })`. Cancellation token pattern: `let cancelled = false; ...; return () => { cancelled = true; }` (per PATTERNS.md convention).
- k_floor sentinel UX (UI-SPEC §k_floor sentinel):
  - When estimatedCohortSize !== null && estimatedCohortSize < 5: render warning row with `<AlertTriangle className="size-4 text-[var(--color-warning)]" aria-hidden="true" />` + `<span className="text-[13px] font-semibold text-[var(--color-warning)]">Cohort too small (k&lt;5) — broaden filters</span>`. Min height 40px per UI-SPEC spacing exceptions.
  - When estimatedCohortSize !== null && estimatedCohortSize >= 5: render `<span className="text-[13px] font-mono tabular-nums text-[var(--color-text-secondary)]">Estimated cohort: {estimatedCohortSize} users</span>` + epsilon row `<span className="text-[11px] font-mono text-[var(--color-text-secondary)]">epsilon = 1.0 (admin output)</span>`.
- Run Cohort Button: variant="primary", min-height-44, disabled (via aria-disabled + pointer-events-none + opacity-50) when estimatedCohortSize < 5. On click: setRunning(true); calls compile_research_cohort with same filters; calls onResult(result); setRunning(false). On error: onResult({ suppressed: true, reason: 'k_floor', k: 0 }) defensive fallback; show toast 'Cohort could not be compiled. Check filters and try again.' (UI-SPEC copywriting contract).
- Use Pill DS primitive for compound multiselect (size="sm", aria-pressed). Use native `<select>` styled with var(--color-border) for tenure/outcome selects (matches admin DS).
- All copy strings VERBATIM from UI-SPEC copywriting contract table.
- Typography STRICTLY 11/13/18 with font-normal or font-semibold only (UI-SPEC HARD CEILING).
- @theme tokens ONLY — verify each var(--color-*) is defined in leanshot/src/index.css before committing (per UI-SPEC + [[feedback_ui_auditor_catches_undefined_theme_tokens]]). If any token referenced doesn't exist, add it to src/index.css @theme block.

**modules.ts** — per PATTERNS.md Admin Module Registration snippet:
- Import: `FlaskConical` from 'lucide-react' (already used elsewhere).
- Append entry to ADMIN_MODULES (after protocols entry per Phase 61 ordering):
  ```typescript
  {
    key: 'research',
    label: 'Research',
    route: 'research',
    icon: FlaskConical,
    lazy: () => import('@/components/admin/research/ResearchLayout'),
    flagKey: 'admin_research',
    minRole: 'staff',
  }
  ```
- Verify the admin shell catch-all branch (per [[feedback_admin_module_manifest_vs_router_branch_drift]]) handles `/admin/research/*` automatically via URL-prefix; if it doesn't, surface this in 62-04 SUMMARY for 62-08 close-out.

Tests at leanshot/src/components/admin/research/__tests__/CohortBuilderForm.test.tsx (Vitest + @testing-library/react if available; if not, fall back to a pure logic test on a helper extracted from the component):
- "renders k_floor warning when estimatedCohortSize is 3"
- "renders cohort size + epsilon when estimatedCohortSize is 47"
- "Run Cohort button is aria-disabled when below k_floor"
- "calls compile_research_cohort on Run Cohort click" (mock supabase.rpc)

If @testing-library/react is NOT in package.json, add it as devDependency (already standard in many React projects — verify). If install is friction, write logic-only tests against an extracted pure helper `shouldDisableRunButton(estimate: number | null): boolean`.
  </action>
  <verify>
    <automated>test -f leanshot/src/components/admin/research/ResearchLayout.tsx &amp;&amp; test -f leanshot/src/components/admin/research/CohortBuilderForm.tsx &amp;&amp; grep -q "key: 'research'" leanshot/src/lib/admin/modules.ts &amp;&amp; cd leanshot &amp;&amp; npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | tee /tmp/p62-04-1.log; grep -qE "error TS" /tmp/p62-04-1.log &amp;&amp; { echo "tsc FAILED"; exit 1; } || echo "tsc OK"</automated>
  </verify>
  <done>ResearchLayout mounts sub-nav; CohortBuilderForm wires both RPCs + k_floor sentinel; modules.ts has research entry; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CohortBuilderPage + RetentionChart + CrossTabMatrix</name>
  <files>leanshot/src/components/admin/research/CohortBuilderPage.tsx, leanshot/src/components/admin/research/RetentionChart.tsx, leanshot/src/components/admin/research/CrossTabMatrix.tsx</files>
  <behavior>
    - CohortBuilderPage hosts <CohortBuilderForm onResult={setResult} /> + suppressed banner + <RetentionChart data={...} epsilon={result.epsilon} /> + <CrossTabMatrix rows={...} columns={...} epsilon={result.epsilon} />.
    - When result.suppressed: render banner ABOVE chart area: 'Cohort suppressed — k<5 at the selected filters.' (text-warning, font-semibold); chart + matrix remain empty/EmptyState.
    - RetentionChart wraps BaseChart with a stacked-area config (Chart.js type='line', fill: true, tension: 0.3). Receives `data: Array<{ week_label: string; retained_pct: number }>`.
    - CrossTabMatrix renders Table with column headers (11px tertiary), cell values (13px), suppressed cells as '—' tertiary. Caption: 'ε = {epsilon} (differential privacy noise applied)' (11px text-tertiary).
  </behavior>
  <action>
**CohortBuilderPage.tsx**:
- Imports: useState, CohortBuilderForm, RetentionChart, CrossTabMatrix, EmptyState.
- State: `result: CohortResponse | null`.
- Layout: vertical stack of (1) heading H1 "Research Dashboard" (28px/600, text-heading) per UI-SPEC, (2) sub-heading "Cohort Builder" (18px/600), (3) <CohortBuilderForm onResult={setResult} />, (4) conditional banner / chart / matrix.
- Suppressed banner copy verbatim: "Cohort suppressed — k<5 at the selected filters." (per UI-SPEC interaction contracts).
- Empty-state copy verbatim: "Run a cohort to see retention curves." (when result === null).
- Animations: `animate-fade-in` gated on `useReducedMotion()` per UI-SPEC.
- Layout spacing: gap-6 between major sections (UI-SPEC spacing scale 2xl=48px between cohort builder and chart panel — use `mt-12 lg:mt-12` or `space-y-12 lg:space-y-12`).

**RetentionChart.tsx** — per PATTERNS.md:
- Props: `{ data: Array<{ week_label: string; retained_pct: number }>; epsilon: number }`.
- Imports: ChartConfiguration from 'chart.js', useTheme, BaseChart.
- Build config inline (or memoized via useMemo): `type: 'line'`, dataset with `fill: true`, `tension: 0.3`, `borderColor: 'var(--color-primary)'`, `backgroundColor: 'rgba(27, 72, 66, 0.15)'`.
- Wrap in keyed BaseChart: `<BaseChart key={theme} config={config} ariaLabel="Retention curve: percentage retained by week" height={280} />` per PATTERNS.md convention.
- If `data.length === 0`: render EmptyState with copy "No retention data for selected filters." per UI-SPEC.

**CrossTabMatrix.tsx** — per PATTERNS.md Table pattern (lines 222-249 of analog ProtocolsListPage):
- Props: `{ rows: Array<{ label: string; values: Array<number | null> }>; columns: string[]; epsilon: number; suppressedBuckets: number }`.
- Render `<div className="overflow-x-auto">` wrapping `<table className="w-full text-[13px]">`.
- Caption: `<caption className="text-[11px] text-[var(--color-text-tertiary)] text-left pb-2">ε = {epsilon} (differential privacy noise applied){suppressedBuckets > 0 && ` — ${suppressedBuckets} sub-group${suppressedBuckets !== 1 ? 's' : ''} suppressed`}</caption>`.
- thead row uppercase tracking-[0.06em] columns.
- tbody row: `border-t border-[var(--color-border)]`, first cell font-semibold label, subsequent cells `v !== null ? v.toFixed(1) : <span className="text-[var(--color-text-tertiary)]">—</span>`.

Typography compliance: all font-size classes MUST be `text-[11px]`, `text-[13px]`, `text-[18px]`, or `text-heading`. No `text-base`, `text-lg`, etc.

Verify tsc-clean before committing.

Test scaffold (optional but recommended): `__tests__/CohortBuilderPage.test.tsx`:
- "renders empty state when result is null"
- "renders suppressed banner when result.suppressed"
- Quick smoke: import-graph compiles.

Defer full integration test (live RPC + render) to 62-08 close-out smoke against staging.
  </action>
  <verify>
    <automated>test -f leanshot/src/components/admin/research/CohortBuilderPage.tsx &amp;&amp; test -f leanshot/src/components/admin/research/RetentionChart.tsx &amp;&amp; test -f leanshot/src/components/admin/research/CrossTabMatrix.tsx &amp;&amp; cd leanshot &amp;&amp; npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | tee /tmp/p62-04-2.log; if grep -qE "error TS" /tmp/p62-04-2.log; then echo "tsc FAILED"; exit 1; fi; grep -v '^[[:space:]]*//' src/components/admin/research/*.tsx | grep -E "text-base|text-lg|text-md|text-sm|text-xl|text-2xl" | wc -l | awk '{ if ($1 == 0) print "typography-OK"; else { print "typography-FAIL count=" $1; exit 1 } }' &amp;&amp; \
# WARNING 6 scope-limited @theme token audit — admin/research/* surfaces only: every var(--color-*) referenced MUST be defined in src/index.css @theme block (per [[feedback_ui_auditor_catches_undefined_theme_tokens]] Tailwind v4 silent no-op trap)
USED_TOKENS=$(grep -rohE "var\(--color-[a-zA-Z0-9_-]+\)" src/components/admin/research/ 2>/dev/null | sort -u); for tok in $USED_TOKENS; do name=$(echo "$tok" | sed -E "s/var\((--color-[a-zA-Z0-9_-]+)\)/\1/"); grep -q "$name" src/index.css || { echo "UNDEFINED @theme token referenced by admin/research/* surfaces: $name"; exit 1; }; done; echo @theme-OK</automated>
  </verify>
  <done>Page + chart + matrix components ship; tsc passes; no banned font-size utilities in the new files; @theme tokens validated against src/index.css.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form input → RPC | p_filters jsonb is parameterized; SQL injection N/A (PostgREST RPC) |
| RPC response → chart render | client trusts SECDEF RPC output; matview HAVING already enforces k≥5 |
| admin route gate | ADMIN_MODULES minRole='staff' + RPC is_staff() guard (defense-in-depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-04-01 | Spoofing | non-staff hits /admin/research | mitigate | ADMIN_MODULES minRole='staff' enforces UI gate; SECDEF RPCs reject with 42501 server-side (Plan 62-02) |
| T-62-04-02 | Information Disclosure | k_floor bypass via client trust | mitigate | client UI is defense-in-depth ONLY; estimate_research_cohort + compile_research_cohort both enforce server-side (per CONTEXT D-PRIVACY 1) |
| T-62-04-03 | Information Disclosure | undefined @theme token renders INVISIBLE | mitigate | UI-SPEC dimension audit + manual @theme src/index.css verification per [[feedback_ui_auditor_catches_undefined_theme_tokens]] |
| T-62-04-04 | Tampering | typography ceiling violation | mitigate | grep gate in <verify> blocks banned Tailwind size utilities |
</threat_model>

<verification>
- /admin/research route resolvable via ADMIN_MODULES catch-all (per [[feedback_admin_module_manifest_vs_router_branch_drift]])
- ResearchLayout sub-nav active state uses `bg-[var(--color-primary)]` per UI-SPEC accent reserved-for #2
- k_floor warning displays exact UI-SPEC copy "Cohort too small (k<5) — broaden filters"
- Epsilon display uses font-mono tabular-nums (UI-SPEC §typography)
- No banned text-* utilities in any new file
- tsc -p tsconfig.app.json --noEmit clean
</verification>

<success_criteria>
- /admin/research/cohort renders cohort builder (verified visually in 62-08 smoke)
- estimate_research_cohort fires within 400ms of filter change (debounce assertion)
- Run Cohort button disabled below k_floor; enabled above
- Chart + Matrix populate with RPC response data
- ADMIN_MODULES research entry committed
- @theme tokens all defined in src/index.css (no INVISIBLE rendering risk)
</success_criteria>

<output>
Create `.planning/phases/62-insights-research-engine/62-04-SUMMARY.md` when done. Include file inventory, RPC wire-up confirmation, @theme tokens verified (list them), and admin module manifest diff.
</output>
