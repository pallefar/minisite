---
phase: 45
plan: 08
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - leanshot/src/admin/modules/community/CommunityAdminLayout.tsx
  - leanshot/src/admin/modules/community/AdminCliniciansPage.tsx
  - leanshot/src/admin/modules/community/AdminReportsDigestPage.tsx
  - leanshot/src/admin/modules/community/SpaceEditor.tsx
  - leanshot/src/admin/modules.ts
autonomous: true
requirements:
  - COMMUNITY-07
  - COMMUNITY-09
must_haves:
  truths:
    - "Admin module catch-all branch matches URL prefix /admin/community/* (per memory feedback_admin_module_manifest_vs_router_branch_drift — pathname-based not switch-based)"
    - "Pathname /admin/community/profiles renders AdminCliniciansPage with table of profiles + verified-clinician toggle wired to admin_set_clinician_verified RPC"
    - "Pathname /admin/community/reports renders AdminReportsDigestPage with the caller's admin_digest_opt_in toggle (admin_toggle_report_digest_opt_in RPC) + summary count of open reports"
    - "SpaceEditor (existing) extended with leaderboard_enabled toggle wired to admin_toggle_space_leaderboard RPC"
    - "All admin writes go through SECDEF RPCs that gate on public.is_staff() (RPCs ship in 45-01); UI does NOT bypass via direct table update"
    - "All 3 admin RPCs are referenced by exactly one UI call site each (no orphan RPC; no duplicate caller)"
    - "Admin module registered in src/admin/modules.ts with URL-prefix-keyed catch-all (memory feedback_admin_module_manifest_vs_router_branch_drift)"
  artifacts:
    - path: "leanshot/src/admin/modules/community/AdminCliniciansPage.tsx"
      provides: "Clinician verified-toggle table view"
      min_lines: 80
    - path: "leanshot/src/admin/modules/community/AdminReportsDigestPage.tsx"
      provides: "Admin digest opt-in + open-report summary"
      min_lines: 60
    - path: "leanshot/src/admin/modules/community/CommunityAdminLayout.tsx"
      provides: "Extended pathname router with new sub-routes"
  key_links:
    - from: "leanshot/src/admin/modules/community/AdminCliniciansPage.tsx"
      to: "admin_set_clinician_verified RPC"
      via: "supabase.rpc()"
      pattern: "admin_set_clinician_verified"
    - from: "leanshot/src/admin/modules/community/AdminReportsDigestPage.tsx"
      to: "admin_toggle_report_digest_opt_in RPC"
      via: "supabase.rpc()"
      pattern: "admin_toggle_report_digest_opt_in"
    - from: "leanshot/src/admin/modules/community/SpaceEditor.tsx"
      to: "admin_toggle_space_leaderboard RPC"
      via: "supabase.rpc()"
      pattern: "admin_toggle_space_leaderboard"
---

<objective>
Extend the existing `CommunityAdminLayout` (Phase 44) with three new admin surfaces: (a) clinician verified-toggle table at `/admin/community/profiles`, (b) admin daily-digest opt-in + summary at `/admin/community/reports`, (c) per-space leaderboard_enabled toggle integrated into `SpaceEditor`. All three writes route through SECDEF RPCs shipped by plan 45-01 (which gate on `public.is_staff()` — memory `reference_supabase_is_staff_helper`).

Critical: the admin module catch-all branch in `src/admin/modules.ts` MUST key on URL prefix (`/admin/community/*`), NOT a switch on tab id, per memory `feedback_admin_module_manifest_vs_router_branch_drift` (Phase 42-10 caught 6 pre-existing broken admin routes from manifest-vs-switch-router drift). Pathname-based routing inside CommunityAdminLayout matches `ReviewsLayout` per memory `reference_react_router_consumer_admin_split`.

Purpose: Wave 2 — depends only on 45-01 (admin SECDEF RPCs). NO moderation queue UI here — Phase 48 owns that (CONTEXT D-11).
Output: 2 new admin sub-pages + 2 extended files + 1 modules manifest edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-CONTEXT.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-01-PLAN.md

# Primary analog — extend, do not replace
@leanshot/src/admin/modules/community/CommunityAdminLayout.tsx
@leanshot/src/admin/modules/community/SpaceEditor.tsx
@leanshot/src/admin/modules.ts
</context>

<interfaces>
<!-- SECDEF RPCs (shipped by 45-01) consumed here -->

- `supabase.rpc('admin_set_clinician_verified', { p_user_id, p_verified })` → boolean
- `supabase.rpc('admin_toggle_report_digest_opt_in', { p_enabled })` → boolean
- `supabase.rpc('admin_toggle_space_leaderboard', { p_space_id, p_enabled })` → boolean

All three are SECDEF + `if not public.is_staff() then raise 'forbidden' end if;` guarded inside the SQL function body.

<!-- New admin routes -->

- `/admin/community/profiles` → AdminCliniciansPage
- `/admin/community/reports` → AdminReportsDigestPage
- (Existing) `/admin/community/spaces/<id>` → SpaceEditor (now with leaderboard_enabled toggle)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CommunityAdminLayout routing extension + modules.ts catch-all branch</name>
  <files>leanshot/src/admin/modules/community/CommunityAdminLayout.tsx, leanshot/src/admin/modules.ts</files>
  <read_first>
    - leanshot/src/admin/modules/community/CommunityAdminLayout.tsx (full file — pathname resolveView at lines 125-132 + navigate helper at 147-151 per 45-PATTERNS.md)
    - leanshot/src/admin/modules.ts (current manifest — verify catch-all branch shape)
    - memory references: feedback_admin_module_manifest_vs_router_branch_drift (Phase 42-10 fix pattern) + reference_react_router_consumer_admin_split
  </read_first>
  <behavior>
    - CommunityAdminLayout.tsx: extend the existing `resolveView()` pathname-match regex to recognize 3 new patterns: /admin/community/profiles (returns view type 'clinicians'), /admin/community/reports (returns 'reports-digest'), /admin/community/spaces/:id (returns 'edit'; already exists — verify, don't duplicate)
    - View dispatch switch: add 2 new arms for the new types rendering AdminCliniciansPage and AdminReportsDigestPage
    - Add nav links/tabs at the top of the layout for "Profiles", "Reports" alongside existing "Spaces" — same Tab component used elsewhere in this file
    - modules.ts: ensure the community admin module's URL-prefix catch-all matches `/admin/community/*` (memory feedback_admin_module_manifest_vs_router_branch_drift — switch-based fall-through is what Phase 42-10 fixed). If currently keyed on exact `/admin/community` only, change to prefix match
    - lazy import the 2 new sub-pages
  </behavior>
  <action>
    Open `leanshot/src/admin/modules/community/CommunityAdminLayout.tsx`. Locate `resolveView()` (~lines 125-132 per 45-PATTERNS.md). Extend the regex match or add additional branches:
    ```typescript
    if (pathname.startsWith('/admin/community/profiles')) return { type: 'clinicians' };
    if (pathname.startsWith('/admin/community/reports')) return { type: 'reports-digest' };
    // existing: spaces / new / edit
    ```

    Add lazy imports near top:
    ```typescript
    const AdminCliniciansPage = lazy(() => import('./AdminCliniciansPage').then(m => ({ default: m.AdminCliniciansPage })));
    const AdminReportsDigestPage = lazy(() => import('./AdminReportsDigestPage').then(m => ({ default: m.AdminReportsDigestPage })));
    ```

    In the view dispatch switch, add 2 new arms:
    ```tsx
    {view.type === 'clinicians' && <AdminCliniciansPage onBack={() => navigate('/admin/community')} />}
    {view.type === 'reports-digest' && <AdminReportsDigestPage onBack={() => navigate('/admin/community')} />}
    ```

    Add 2 tab buttons in the header nav row: "Profiles" → navigate('/admin/community/profiles'); "Reports" → navigate('/admin/community/reports').

    Open `leanshot/src/admin/modules.ts`. Find the community module registration. Ensure the `match` (or whatever the manifest field is called) is a URL-PREFIX function `(p) => p.startsWith('/admin/community')` and NOT an exact-equality check or a tab-id switch. If the current manifest uses a different shape, change to URL-prefix match per memory feedback_admin_module_manifest_vs_router_branch_drift. Add a code comment citing the memory key to prevent future re-drift.
  </action>
  <acceptance_criteria>
    - `grep -c "/admin/community/profiles" leanshot/src/admin/modules/community/CommunityAdminLayout.tsx` returns ≥2 (resolveView + navigate call)
    - `grep -c "/admin/community/reports" leanshot/src/admin/modules/community/CommunityAdminLayout.tsx` returns ≥2
    - `grep -c "AdminCliniciansPage\\|AdminReportsDigestPage" leanshot/src/admin/modules/community/CommunityAdminLayout.tsx` returns ≥3 (2 lazy imports + 2 JSX uses, ≥ 3 total)
    - `grep -c "startsWith.*'/admin/community'" leanshot/src/admin/modules.ts` returns ≥1 (URL-prefix catch-all)
    - `cd leanshot && npx tsc -p tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && grep -c "/admin/community/profiles" src/admin/modules/community/CommunityAdminLayout.tsx | awk '$1 >= 2 {print "OK"; exit 0} {print "FAIL"; exit 1}' && grep -c "/admin/community/reports" src/admin/modules/community/CommunityAdminLayout.tsx | awk '$1 >= 2 {print "OK"; exit 0} {print "FAIL"; exit 1}' && grep -c "startsWith.*'/admin/community'" src/admin/modules.ts | awk '$1 >= 1 {print "OK"; exit 0} {print "FAIL"; exit 1}' && npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <done>Routing extended; modules manifest uses URL-prefix catch-all; TypeScript compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdminCliniciansPage + AdminReportsDigestPage + SpaceEditor leaderboard toggle</name>
  <files>leanshot/src/admin/modules/community/AdminCliniciansPage.tsx, leanshot/src/admin/modules/community/AdminReportsDigestPage.tsx, leanshot/src/admin/modules/community/SpaceEditor.tsx</files>
  <read_first>
    - leanshot/src/admin/modules/community/CommunityAdminLayout.tsx (existing SpacesListPage at lines 41-116 per 45-PATTERNS.md — analog for sub-page shape with table + action button)
    - leanshot/src/admin/modules/community/SpaceEditor.tsx (full file — extend with one new toggle)
    - leanshot/src/lib/store.ts (useStore selector pattern)
  </read_first>
  <behavior>
    - AdminCliniciansPage: table with columns (handle, display_name, is_clinician_verified, action). On mount, fetch `supabase.from('profiles').select('id, handle, display_name, is_clinician_verified').order('handle').limit(100)` (admin context — RLS bypassed by is_staff check on the admin client OR via service-role; if neither available, query is mediated through is_staff-gated RPC `admin_search_clinicians` — if that RPC isn't shipped, do a direct query that requires is_staff-bypassing RLS on profiles which doesn't exist; SIMPLEST: provide a search input + ad-hoc query and let admin handle scale in v1). Each row has a toggle button: on click calls `supabase.rpc('admin_set_clinician_verified', { p_user_id: row.id, p_verified: !row.is_clinician_verified })`; optimistic update local state; on error toast.
    - AdminReportsDigestPage: top section: a single toggle "Receive daily report digest email" wired to `admin_toggle_report_digest_opt_in`; initial state from `supabase.from('profiles').select('admin_digest_opt_in').eq('id', currentUserId).single()`. Below the toggle: simple summary "Open reports today: N" fetched via `supabase.from('community_reports').select('*', { count: 'exact', head: true }).eq('status', 'open')`. NO list view — Phase 48 owns the queue UI.
    - SpaceEditor extension: locate existing form fields (name, description, min_tier, etc.). Add ONE new Switch/Toggle for `leaderboard_enabled` (read from existing space query — if not selected, add it). On change, call `supabase.rpc('admin_toggle_space_leaderboard', { p_space_id: space.id, p_enabled: newValue })`; optimistic local state.
    - All 3 pages: one useStore selector per primitive; toasts use existing `useToast`; aria-labels on icon buttons; `prefers-reduced-motion` honored
    - NO direct UPDATE on profiles, community_spaces, or other admin-write tables — ALL writes go through the SECDEF RPCs (which enforce is_staff() guard server-side)
  </behavior>
  <action>
    1. Create `leanshot/src/admin/modules/community/AdminCliniciansPage.tsx`. Copy the table-with-action-button structure from CommunityAdminLayout's `SpacesListPage` (lines 41-116 per 45-PATTERNS.md). Replace data fetch with profiles.select + handle/display_name/is_clinician_verified. Replace action button with verified-clinician toggle. Use Tailwind utility classes.

    2. Create `leanshot/src/admin/modules/community/AdminReportsDigestPage.tsx`. Top: Switch component (existing UI primitive) labeled "Receive daily report digest email" — initial state from profiles query, onChange → RPC. Bottom: small card showing "Open reports: N" with count fetched from community_reports head query. Footer paragraph: "Full moderation queue lands in Phase 48 — for now, daily email digest summarizes counts by target type."

    3. Open `leanshot/src/admin/modules/community/SpaceEditor.tsx`. Locate the existing form fields (the file is 8512 bytes ≈ ~200 lines — find the section that renders form fields for the space). Add a new Switch labeled "Enable leaderboard for this space" near the bottom of the form. State source: `space.leaderboard_enabled` from the existing space object (if the existing select() doesn't pull this column, add it — `select('id, name, description, min_tier, leaderboard_enabled, ...')`). On change, call `supabase.rpc('admin_toggle_space_leaderboard', { p_space_id: space.id, p_enabled: !space.leaderboard_enabled })` and update local state.

    ALL writes: use RPC calls, NOT direct `supabase.from('profiles').update(...)`. The RPCs gate on `public.is_staff()` per 45-01.

    Each component uses one `useStore` selector per primitive (per CLAUDE.md State Management). Toasts use existing `useToast` hook. Add `aria-label` on every icon button. Honor `useReducedMotion` for any transition animations.
  </action>
  <acceptance_criteria>
    - `leanshot/src/admin/modules/community/AdminCliniciansPage.tsx` exists; ≥80 lines
    - `leanshot/src/admin/modules/community/AdminReportsDigestPage.tsx` exists; ≥60 lines
    - `grep -c "admin_set_clinician_verified" leanshot/src/admin/modules/community/AdminCliniciansPage.tsx` returns ≥1
    - `grep -c "admin_toggle_report_digest_opt_in" leanshot/src/admin/modules/community/AdminReportsDigestPage.tsx` returns ≥1
    - `grep -c "admin_toggle_space_leaderboard" leanshot/src/admin/modules/community/SpaceEditor.tsx` returns ≥1
    - NO direct admin-table write outside RPCs: `grep -cE "from\\('profiles'\\)\\.update|from\\('community_spaces'\\)\\.update" leanshot/src/admin/modules/community/AdminCliniciansPage.tsx leanshot/src/admin/modules/community/AdminReportsDigestPage.tsx leanshot/src/admin/modules/community/SpaceEditor.tsx | awk -F: '{s+=$2}END{print s}'` returns 0 OR only counts pre-existing non-related update calls (verify manually that the 3 admin writes are RPC-mediated)
    - `cd leanshot && npx tsc -p tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && test -f src/admin/modules/community/AdminCliniciansPage.tsx && test -f src/admin/modules/community/AdminReportsDigestPage.tsx && test $(grep -c "admin_set_clinician_verified" src/admin/modules/community/AdminCliniciansPage.tsx) -ge 1 && test $(grep -c "admin_toggle_report_digest_opt_in" src/admin/modules/community/AdminReportsDigestPage.tsx) -ge 1 && test $(grep -c "admin_toggle_space_leaderboard" src/admin/modules/community/SpaceEditor.tsx) -ge 1 && npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <done>3 admin surfaces ship; all writes RPC-mediated; TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser→Edge/RPC | Admin staff invokes SECDEF RPCs; is_staff() gate is the authoritative check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-08 | Spoofing | verified-clinician flag tampering | mitigate | admin_set_clinician_verified RPC SECDEF + is_staff() guard; UI cannot bypass (no direct profiles.update); flag never self-claimable |
| T-45-14 | Tampering | space leaderboard_enabled toggle | mitigate | admin_toggle_space_leaderboard RPC SECDEF + is_staff() guard |
| T-45-09 | Information Disclosure | admin reports listing | mitigate | Per CONTEXT D-11, no full moderation queue in v1; AdminReportsDigestPage only shows aggregate count, no row-level data exposure |
</threat_model>

<verification>
- TypeScript compiles
- All 3 admin writes use RPCs (no direct table update)
- modules.ts catch-all is URL-prefix-keyed
</verification>

<success_criteria>
- 2 new admin sub-pages + 1 SpaceEditor extension + module manifest catch-all
- All writes RPC-mediated; all RPCs use is_staff() per 45-01
</success_criteria>

<output>
After completion, create `.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-08-SUMMARY.md` documenting: routes added, RPCs consumed, and confirmation that NO direct admin-table updates exist in the new files.
</output>
