---
phase: 45
plan: 07
type: execute
wave: 2
depends_on: [45-01, 45-02, 45-03, 45-04, 45-06]
files_modified:
  - leanshot/src/lib/store.ts
  - leanshot/src/components/community/CommunityTabShell.tsx
  - leanshot/src/components/community/CommunityDirectoryView.tsx
  - leanshot/src/components/community/ProfileCard.tsx
  - leanshot/src/components/community/DMInboxView.tsx
  - leanshot/src/components/community/DMThreadView.tsx
  - leanshot/src/components/community/DMMessageComposer.tsx
  - leanshot/src/components/community/LeaderboardChip.tsx
  - leanshot/src/components/community/ReportButton.tsx
  - leanshot/src/components/community/use-dm-inbox-realtime.ts
  - leanshot/src/components/community/CommunitySpaceView.tsx
  - leanshot/src/components/community/CommunityPost.tsx
  - leanshot/src/components/community/CommunityCommentThread.tsx
  - leanshot/src/components/dashboard/settings/SettingsPage.tsx
  - leanshot/vite.config.ts
  - leanshot/scripts/assert-bundle-budget.sh
autonomous: true
requirements:
  - COMMUNITY-07
  - COMMUNITY-08
  - COMMUNITY-09
must_haves:
  truths:
    - "Zustand store extended with activeCommunityView: 'feed'|'directory'|'dm'|'space:<id>'|null (NOT a new TabId per memory reference_react_router_consumer_admin_split)"
    - "CommunityTabShell dispatches on activeCommunityView; directory, dm-inbox, dm-thread, leaderboard chip all reachable from within currentTab='community'"
    - "CommunityDirectoryView renders ProfileCard list via supabase.rpc('search_directory') with handle-prefix + tier filter pagination cursor (handle, user_id)"
    - "ProfileCard renders 4 badges (tier/level/clinician/streak) honoring per-user toggles; bio rendered via Phase 44 dompurify-config (NO new policy); Message CTA flips activeCommunityView to dm + opens DMThreadView for the recipient"
    - "DMInboxView lists threads via direct query (RLS-gated participant-only) + unread count badge; subscribes via use-dm-inbox-realtime hook with channel dm:${userId}"
    - "DMThreadView virtualizes message list via react-virtuoso (already in deps); body rendered via Phase 44 react-markdown + dompurify pipeline; attachments rendered via getDmAttachmentSignedUrl helper"
    - "DMMessageComposer POSTs to dm-create-thread Edge Fn for new threads; for replies uses direct insert into direct_messages (no rate-limit on replies per D-07); attachment upload reuses community-storage helpers"
    - "LeaderboardChip fetches via supabase.rpc('get_community_space_leaderboard', { p_space_id }); mounted on CommunitySpaceView; shows top-3 + 'View full' expand; honors tier-gating (D-15 locked-card UX → /pricing CTA)"
    - "ReportButton mounted on CommunityPost + CommunityCommentThread + DMThreadView messages + ProfileCard; calls community_report_create RPC; toast 'Report submitted' on success"
    - "SettingsPage gains Community section toggles: directory_opt_in, dm_open, show_tier_badge, show_streak_badge, leaderboard_opt_in (one Zustand setter per toggle calling supabase.from('profiles').update)"
    - "vite.config.ts manualChunks: community-directory (CommunityDirectoryView + ProfileCard) AND community-dm (DM* + use-dm-inbox-realtime) BOTH placed BEFORE line 206 community-feed catch-all (per RESEARCH Pitfall 6)"
    - "scripts/assert-bundle-budget.sh has new ceilings: community-directory 10 kB gz, community-dm 35 kB gz"
    - "Toast + unread-count badge wired into MobileNav + Sidebar 'community' (D-19); prefers-reduced-motion respected"
    - "Web push subscription request honored on signed-in user opt-in for DM category (D-21) via Phase 42 usePushSubscription"
  artifacts:
    - path: "leanshot/src/components/community/CommunityDirectoryView.tsx"
      provides: "Directory list + search + ProfileCard rendering"
      min_lines: 100
    - path: "leanshot/src/components/community/DMInboxView.tsx"
      provides: "Thread list + unread count + realtime subscription"
      min_lines: 80
    - path: "leanshot/src/components/community/DMThreadView.tsx"
      provides: "Virtualized message list + composer + attachment render"
      min_lines: 120
    - path: "leanshot/src/components/community/LeaderboardChip.tsx"
      provides: "Top-3 + expand + tier-locked card UX"
      min_lines: 50
    - path: "leanshot/src/components/community/use-dm-inbox-realtime.ts"
      provides: "Per-user channel hook (dm:${userId}) mirroring use-space-realtime"
    - path: "leanshot/vite.config.ts"
      provides: "Two new manualChunk rules ordered BEFORE community-feed catch-all"
    - path: "leanshot/scripts/assert-bundle-budget.sh"
      provides: "community-directory and community-dm ceilings"
  key_links:
    - from: "leanshot/src/components/community/DMMessageComposer.tsx"
      to: "supabase/functions/dm-create-thread/index.ts"
      via: "supabase.functions.invoke('dm-create-thread')"
      pattern: "dm-create-thread"
    - from: "leanshot/src/components/community/LeaderboardChip.tsx"
      to: "get_community_space_leaderboard RPC"
      via: "supabase.rpc()"
      pattern: "get_community_space_leaderboard"
    - from: "leanshot/src/components/community/use-dm-inbox-realtime.ts"
      to: "direct_messages table"
      via: "supabase.channel(dm:${userId}).on('postgres_changes', { filter: 'recipient_user_id=eq.${userId}' })"
      pattern: "recipient_user_id=eq"
---

<objective>
Land the entire consumer UI for Phase 45: directory + DM inbox/thread/composer + leaderboard chip + report button + settings toggles + realtime hook + vite chunk ordering + bundle budget ceilings. All sub-views live INSIDE the existing 'community' TabId (NO new TabId per memory reference_react_router_consumer_admin_split + CONTEXT code_context); navigation is via a new `activeCommunityView` Zustand sub-state. Bundle ceilings: `community-directory` 10 kB gz, `community-dm` 35 kB gz — manualChunk rules MUST be ordered BEFORE the existing community-feed catch-all at vite.config.ts:206 (RESEARCH Pitfall 6 + memory reference_bundle_budget_hash_hyphen).

Purpose: Wave 2 — depends on Wave 0 schema/migrations (45-01, 45-02, 45-03) AND Wave 1 Edge Fn (45-04) AND Wave 1 leaderboard (45-06). Plan 45-09 close-out exercises this UI via HUMAN-UAT signals.
Output: 16 file changes (4 extensions + 9 new components + 1 hook + 2 build config edits). Sized as 3 tasks to fit ~50% context per task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-CONTEXT.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-RESEARCH.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md

# Primary analogs — copy verbatim
@leanshot/src/components/community/CommunityTabShell.tsx
@leanshot/src/components/community/CommunitySpaceView.tsx
@leanshot/src/components/community/CommunityFeed.tsx
@leanshot/src/components/community/CommunityPost.tsx
@leanshot/src/components/community/CommunityPostComposer.tsx
@leanshot/src/components/community/ReactionBar.tsx
@leanshot/src/components/community/use-space-realtime.ts
@leanshot/src/lib/community/community-storage.ts
@leanshot/src/lib/community/dompurify-config.ts
@leanshot/src/lib/store.ts
@leanshot/vite.config.ts
@leanshot/scripts/assert-bundle-budget.sh
</context>

<interfaces>
<!-- Store extension shape -->
```typescript
// Added to AppState interface in src/lib/store.ts after activeCommunitySpaceId:
activeCommunityView: 'feed' | 'directory' | 'dm' | null;       // null = legacy space-list view
activeDmThreadId: string | null;                                // selected thread within 'dm' view
setActiveCommunityView: (v: AppState['activeCommunityView']) => void;
setActiveDmThread: (id: string | null) => void;
```

<!-- RPC contracts (shipped by 45-01) used by this plan -->
- `supabase.rpc('search_directory', { p_handle_prefix, p_tier_filter, p_cursor_handle, p_cursor_user_id, p_limit })` — note: 45-01 does NOT explicitly ship this RPC; if missing, this plan adds a 4th SECDEF function to 45-01's migration OR uses direct supabase.from('profiles').select() with RLS doing the work (RLS already gates via directory_members_select policy from 45-01). Prefer direct .select() — simpler, no extra RPC.
- `supabase.rpc('get_community_space_leaderboard', { p_space_id })` — ships in 45-01 SECDEF migration; returns `{ handle, score, rank_in_space }[]`
- `supabase.rpc('toggle_community_block', { p_target_user_id })` — ships in 45-01; returns boolean
- `supabase.rpc('community_report_create', { p_target_type, p_target_id, p_reason })` — ships in 45-01; returns uuid
- `supabase.rpc('update_community_last_active')` — ships in 45-01; SETs profiles.community_last_active_at = now()

<!-- Edge Fn contract (45-04) -->
- `supabase.functions.invoke('dm-create-thread', { body: { recipient_user_id, body, attachment_path?, creator_user_id } })`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zustand store extension + CommunityTabShell view dispatch + use-dm-inbox-realtime hook</name>
  <files>leanshot/src/lib/store.ts, leanshot/src/components/community/CommunityTabShell.tsx, leanshot/src/components/community/use-dm-inbox-realtime.ts</files>
  <read_first>
    - leanshot/src/lib/store.ts (lines 76-100 AppState interface; lines 580-660 store creation + partialize; lines 645-655 setter patterns)
    - leanshot/src/components/community/CommunityTabShell.tsx (current dispatch logic — extend, don't replace)
    - leanshot/src/components/community/use-space-realtime.ts (primary analog — copy verbatim shape, change channel name + filter)
    - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md §"use-dm-inbox-realtime.ts" (full hook code) + §"CommunityTabShell.tsx (EXTEND)"
  </read_first>
  <behavior>
    - store.ts: add `activeCommunityView`, `activeDmThreadId` to AppState; add `setActiveCommunityView`, `setActiveDmThread` actions; ephemeral (NOT in partialize whitelist — must not persist per RESEARCH-implied "ephemeral UI" convention) — verify by checking partialize at lines 231-250 of store.ts does NOT include these two new fields
    - CommunityTabShell.tsx: read both `activeCommunitySpaceId` (existing) AND `activeCommunityView` (new); dispatch table:
      - activeCommunityView === 'directory' → render CommunityDirectoryView
      - activeCommunityView === 'dm' && activeDmThreadId === null → render DMInboxView
      - activeCommunityView === 'dm' && activeDmThreadId !== null → render DMThreadView (with threadId)
      - activeCommunityView === null (legacy): existing CommunitySpaceList / CommunitySpaceView behavior unchanged
    - use-dm-inbox-realtime.ts: copy use-space-realtime.ts verbatim; only changes are channel name `dm:${userId}` (per D-18), table 'direct_messages', filter `recipient_user_id=eq.${userId}`, payload type `{ thread_id, sender_user_id }`
    - All new lazy imports placed AFTER existing CommunitySpaceList / CommunitySpaceView lazy imports — community-feed catch-all in vite.config.ts handles them until Task 3 splits them out
  </behavior>
  <action>
    1. Open `leanshot/src/lib/store.ts`. Locate line 82 (`activeCommunitySpaceId: string | null;`). Insert below it:
       ```
       /** Phase 45 — community sub-view (directory / DM). Ephemeral; not persisted. */
       activeCommunityView: 'feed' | 'directory' | 'dm' | null;
       /** Phase 45 — selected DM thread within the 'dm' sub-view. Ephemeral. */
       activeDmThreadId: string | null;
       ```
       Then add action signatures alongside the existing `setActiveCommunitySpace` (around line 653):
       ```
       setActiveCommunityView: (v: AppState['activeCommunityView']) => void;
       setActiveDmThread: (id: string | null) => void;
       ```
       And implementations:
       ```
       setActiveCommunityView: (v) => set({ activeCommunityView: v }),
       setActiveDmThread: (id) => set({ activeDmThreadId: id }),
       ```
       Initial state additions (around line 585):
       ```
       activeCommunityView: null,
       activeDmThreadId: null,
       ```
       Per memory `reference_zustand_persisted_user_blocks_marketing_uat` + CLAUDE.md State Management convention: do NOT add these to `partialize` — they're ephemeral UI flags like `currentTab`.

    2. Open `leanshot/src/components/community/CommunityTabShell.tsx`. Add 4 new lazy imports after existing ones:
       ```
       const CommunityDirectoryView = lazy(() => import('./CommunityDirectoryView').then(m => ({ default: m.CommunityDirectoryView })));
       const DMInboxView = lazy(() => import('./DMInboxView').then(m => ({ default: m.DMInboxView })));
       const DMThreadView = lazy(() => import('./DMThreadView').then(m => ({ default: m.DMThreadView })));
       ```
       Read `activeCommunityView` + `activeDmThreadId` via per-primitive selectors (per CLAUDE.md State Management). Replace the existing ternary inside `<Suspense>` with a switch on `activeCommunityView`:
       ```tsx
       {activeCommunityView === 'directory' && <CommunityDirectoryView currentUserId={currentUserId} currentTier={currentTier} />}
       {activeCommunityView === 'dm' && activeDmThreadId === null && <DMInboxView currentUserId={currentUserId} />}
       {activeCommunityView === 'dm' && activeDmThreadId !== null && <DMThreadView threadId={activeDmThreadId} currentUserId={currentUserId} />}
       {activeCommunityView === null && (activeSpaceId ? <CommunitySpaceView .../> : <CommunitySpaceList .../>)}
       ```

    3. Create `leanshot/src/components/community/use-dm-inbox-realtime.ts` — copy `use-space-realtime.ts` verbatim per 45-PATTERNS.md §"use-dm-inbox-realtime.ts" (FULL 27-line code block provided there). Only changes: channel name template `\`dm:\${userId}\`` (per D-18), table `'direct_messages'`, filter `\`recipient_user_id=eq.\${userId}\``, payload typed `{ thread_id: string; sender_user_id: string }`.

    Per CLAUDE.md State Management rule: each useStore selector returns ONE primitive. Do NOT introduce `useStore(s => s)`.
  </action>
  <acceptance_criteria>
    - `grep -c "activeCommunityView" leanshot/src/lib/store.ts` returns ≥4 (interface field + initial state + setter signature + setter impl)
    - `grep -c "activeDmThreadId" leanshot/src/lib/store.ts` returns ≥4
    - `grep -c "activeCommunityView\\|activeDmThreadId" leanshot/src/lib/store.ts` does NOT increase the `partialize` block at lines 231-250 (must remain ephemeral — `grep -A 30 'partialize:' leanshot/src/lib/store.ts | grep -c activeCommunityView` returns 0)
    - File `leanshot/src/components/community/use-dm-inbox-realtime.ts` exists
    - `grep -c "dm:\\${userId}" leanshot/src/components/community/use-dm-inbox-realtime.ts` returns ≥1
    - `grep -c "recipient_user_id=eq" leanshot/src/components/community/use-dm-inbox-realtime.ts` returns ≥1
    - `grep -c "CommunityDirectoryView\\|DMInboxView\\|DMThreadView" leanshot/src/components/community/CommunityTabShell.tsx` returns ≥3 (one each as lazy import)
    - `cd leanshot && npx tsc -p tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && grep -c "activeCommunityView" src/lib/store.ts | awk '$1 >= 4 {print "OK"; exit 0} {print "FAIL"; exit 1}' && test $(awk '/partialize:/,/^[[:space:]]*\\}/' src/lib/store.ts | grep -c "activeCommunityView") -eq 0 && test -f src/components/community/use-dm-inbox-realtime.ts && npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <done>Store + shell + hook ready for the new components in Tasks 2 + 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Directory + ProfileCard + LeaderboardChip + ReportButton + Settings toggles</name>
  <files>leanshot/src/components/community/CommunityDirectoryView.tsx, leanshot/src/components/community/ProfileCard.tsx, leanshot/src/components/community/LeaderboardChip.tsx, leanshot/src/components/community/ReportButton.tsx, leanshot/src/components/community/CommunitySpaceView.tsx, leanshot/src/components/community/CommunityPost.tsx, leanshot/src/components/community/CommunityCommentThread.tsx, leanshot/src/components/dashboard/settings/SettingsPage.tsx</files>
  <read_first>
    - leanshot/src/components/community/CommunitySpaceView.tsx (role-match analog for CommunityDirectoryView)
    - leanshot/src/components/community/CommunityPost.tsx (analog for ProfileCard markdown rendering — uses renderPostBodyHtml from dompurify-config.ts)
    - leanshot/src/components/community/ReactionBar.tsx (analog for LeaderboardChip + ReportButton)
    - leanshot/src/lib/community/dompurify-config.ts (REUSE — sanitizeCommunityMarkdown + renderPostBodyHtml exports)
    - leanshot/src/components/dashboard/settings/SettingsPage.tsx (extend with Community section)
    - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md §"CommunityDirectoryView.tsx" + §"LeaderboardChip.tsx" + §"DOMPurify Reuse"
    - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-CONTEXT.md (D-01..D-05 badges + directory shape; D-12..D-17 leaderboard; D-11 report)
  </read_first>
  <behavior>
    - CommunityDirectoryView: search bar (handle prefix) + tier filter dropdown + ProfileCard list; pagination via cursor `(handle, user_id)` per D-04; loads via `supabase.from('profiles').select('id, handle, display_name, bio, links, is_clinician_verified, show_tier_badge, show_streak_badge, leaderboard_handle').eq('directory_opt_in', true).ilike('handle', \`\${prefix}%\`).order('handle').order('id').limit(20)` — RLS does the cross-tenant filtering automatically
    - ProfileCard: avatar (use existing user avatar pattern from Phase 44 CommunityPost), handle, display_name, bio (rendered via `renderPostBodyHtml` from dompurify-config.ts — NO new policy), up to 5 links (rendered as anchor tags with HTTPS-only check), 4 badges (tier from tier_effective.tier_label — fetch via existing readTierLabel helper from CommunityTabShell; level from gamification — defer if not exposed; clinician from is_clinician_verified; streak from streak_state.current_streak_days), Message CTA button that sets activeCommunityView='dm' + activeDmThreadId based on existing thread lookup (or null to trigger composer for new thread)
    - LeaderboardChip: small inline component mounted inside CommunitySpaceView; fetches `supabase.rpc('get_community_space_leaderboard', { p_space_id })`; shows top-3 + "View full leaderboard" expansion; receives `leaderboardEnabled: boolean` prop — when false, renders nothing; when true but user tier is below space.min_tier, renders Phase 44 locked-card UX with `/pricing` CTA per D-15
    - ReportButton: small icon button (lucide-react Flag); on click opens a tiny Sheet with reason input (text); on submit calls `supabase.rpc('community_report_create', { p_target_type, p_target_id, p_reason })`; toast "Report submitted — admin will review" on success; mounted in: CommunityPost (target_type='post'), CommunityCommentThread (target_type='comment'), ProfileCard (target_type='profile'), DMThreadView messages (target_type='dm_message')
    - SettingsPage Community section: 5 toggles (directory_opt_in, dm_open, show_tier_badge, show_streak_badge, leaderboard_opt_in); each writes via `supabase.from('profiles').update({ <col>: <bool> }).eq('id', currentUserId)`; toggles read initial state from a per-mount fetch of profiles row
    - All bio + markdown rendering uses `renderPostBodyHtml` from `@/lib/community/dompurify-config` — NO new DOMPurify instantiation (memory feedback_silent_scope_reduction_patterns)
    - All toasts use `useToast` from existing hooks; honor `useReducedMotion` per CLAUDE.md a11y
    - CommunitySpaceView extension: add `<LeaderboardChip spaceId={spaceId} leaderboardEnabled={space.leaderboard_enabled} currentTier={currentTier} />` near the top of the space view header
    - CommunityPost / CommunityCommentThread extension: add `<ReportButton targetType="post" targetId={post.id} />` (and "comment" / comment.id) inside the existing actions row
  </behavior>
  <action>
    Create the 4 new components in `leanshot/src/components/community/`. For each:
    1. Use `Suspense + lazy` only inside parent (CommunityTabShell already does this for CommunityDirectoryView)
    2. One useStore selector per primitive (per CLAUDE.md)
    3. Import dompurify via `import { renderPostBodyHtml } from '@/lib/community/dompurify-config'` — NO new DOMPurify
    4. Tailwind v4 utility classes for styling (existing community/* style language)
    5. `aria-label` on every icon-only button; `role="status" aria-live="polite"` on async loading regions; `prefers-reduced-motion` via existing `useReducedMotion` hook

    CommunityDirectoryView.tsx: copy CommunitySpaceView.tsx shape; replace its `useEffect` data fetch with the profiles directory query above; render `<ProfileCard profile={...} onMessage={() => setActiveCommunityView('dm')...} />` mapped over results. Pagination: cursor (handle, user_id) stored in component-local useState; "Load more" button.

    ProfileCard.tsx: stateless component; props `{ profile, currentUserId, onMessage }`. Render avatar + handle + display_name + bio (via renderPostBodyHtml) + links (filtered HTTPS-only) + 4 badge components (use simple Badge primitive from `@/components/ui/Badge`). Message CTA dispatches via callback prop.

    LeaderboardChip.tsx: mount-time fetch via `supabase.rpc('get_community_space_leaderboard', { p_space_id })`; if leaderboardEnabled=false return null; show top-3 + expand. Tier check: if `currentTier` is below the space's `min_tier`, render the Phase 44 locked-card UX (find pattern in CommunitySpaceView's tier-locked code path).

    ReportButton.tsx: icon button + Sheet (existing `@/components/ui/Sheet`) with textarea for reason; submit calls RPC; toast on success.

    SettingsPage extension: locate the existing settings sections (likely an array or sequential JSX). Add a new "Community" section near AI/Profile sections containing 5 labeled Switch components (use existing Switch / Toggle UI primitive). Initial state: `useEffect(() => supabase.from('profiles').select(...))` then setLocalState. Each onChange writes via update + optimistic local state.

    CommunitySpaceView edit: locate the existing header/title block (around the SpaceCard render). Insert `<LeaderboardChip ... />` immediately below. Pass `leaderboardEnabled` from the existing space object (must read `community_spaces.leaderboard_enabled` in the existing fetch — if not already selected, add it).

    CommunityPost + CommunityCommentThread edits: locate the existing reaction/action bar. Insert `<ReportButton targetType="post" targetId={post.id} />` (or "comment") in the trailing slot.
  </action>
  <acceptance_criteria>
    - All 4 new component files exist with `export function` or `export const` named exports matching expected names (CommunityDirectoryView, ProfileCard, LeaderboardChip, ReportButton)
    - `grep -rE "new DOMPurify\\(|createDOMPurify\\(" leanshot/src/components/community/CommunityDirectoryView.tsx leanshot/src/components/community/ProfileCard.tsx leanshot/src/components/community/LeaderboardChip.tsx leanshot/src/components/community/ReportButton.tsx` returns no matches (CRITICAL — memory feedback_silent_scope_reduction_patterns)
    - `grep -c "renderPostBodyHtml\\|sanitizeCommunityMarkdown" leanshot/src/components/community/ProfileCard.tsx` returns ≥1
    - `grep -c "get_community_space_leaderboard" leanshot/src/components/community/LeaderboardChip.tsx` returns ≥1
    - `grep -c "community_report_create" leanshot/src/components/community/ReportButton.tsx` returns ≥1
    - `grep -c "<ReportButton" leanshot/src/components/community/CommunityPost.tsx` returns ≥1
    - `grep -c "<ReportButton" leanshot/src/components/community/CommunityCommentThread.tsx` returns ≥1
    - `grep -c "<LeaderboardChip" leanshot/src/components/community/CommunitySpaceView.tsx` returns ≥1
    - `grep -cE "directory_opt_in|dm_open|show_tier_badge|show_streak_badge|leaderboard_opt_in" leanshot/src/components/dashboard/settings/SettingsPage.tsx` returns ≥5 (all 5 toggles)
    - `cd leanshot && npx tsc -p tsconfig.app.json --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && test -f src/components/community/CommunityDirectoryView.tsx && test -f src/components/community/ProfileCard.tsx && test -f src/components/community/LeaderboardChip.tsx && test -f src/components/community/ReportButton.tsx && test $(grep -rcE "new DOMPurify\\(|createDOMPurify\\(" src/components/community/CommunityDirectoryView.tsx src/components/community/ProfileCard.tsx src/components/community/LeaderboardChip.tsx src/components/community/ReportButton.tsx | awk -F: '{s+=$2}END{print s}') -eq 0 && grep -cE "directory_opt_in|dm_open|show_tier_badge|show_streak_badge|leaderboard_opt_in" src/components/dashboard/settings/SettingsPage.tsx | awk '$1 >= 5 {print "OK"; exit 0} {print "FAIL"; exit 1}' && npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <done>Directory + ProfileCard + LeaderboardChip + ReportButton ship; settings toggles wired; TypeScript compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DM inbox + thread + composer + vite chunks + bundle budget</name>
  <files>leanshot/src/components/community/DMInboxView.tsx, leanshot/src/components/community/DMThreadView.tsx, leanshot/src/components/community/DMMessageComposer.tsx, leanshot/vite.config.ts, leanshot/scripts/assert-bundle-budget.sh</files>
  <read_first>
    - leanshot/src/components/community/CommunityFeed.tsx (role-match analog for DMInboxView + DMThreadView)
    - leanshot/src/components/community/CommunityPostComposer.tsx (primary analog for DMMessageComposer — copy attachment upload flow)
    - leanshot/src/lib/community/community-storage.ts (REUSE getDmAttachmentSignedUrl from 45-03; COMMUNITY_MEDIA_MIMES; DM_ATTACHMENT_MAX_BYTES)
    - leanshot/vite.config.ts (lines 195-206 — manualChunks ordering)
    - leanshot/scripts/assert-bundle-budget.sh (line 46-48 — chunk ceiling entries)
    - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md §"DMMessageComposer.tsx" + §"vite.config.ts" + §"assert-bundle-budget.sh"
    - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-RESEARCH.md §Pitfall 6 (chunk ordering)
    - Phase 42 `usePushSubscription` hook location (grep `usePushSubscription` to find file path)
  </read_first>
  <behavior>
    - DMInboxView: list of threads via direct supabase query (RLS gates to participant-only); per-thread: counterparty handle + last_message_at + unread count (count direct_messages WHERE thread_id = $1 AND created_at > last_read_at — simplest: just show last message preview without unread count; carry "unread badge" feature to v1.3-uat-deferred if needed); subscribes via use-dm-inbox-realtime; on new message arrival, refetches the inbox query
    - DMThreadView: virtualized message list via `import { Virtuoso } from 'react-virtuoso'`; fetches direct_messages WHERE thread_id=$1 ORDER BY created_at; rendered body via renderPostBodyHtml (Phase 44 dompurify); attachment_path rendered via `<img src={signedUrl} />` fetched via getDmAttachmentSignedUrl; composer at bottom; on focus, calls `supabase.rpc('update_community_last_active')` for the 5-min debounce signal (D-20)
    - DMMessageComposer: textarea + attachment input (≤5 MB, MIME from COMMUNITY_MEDIA_MIMES); for NEW thread (composer rendered standalone, before thread exists): calls `supabase.functions.invoke('dm-create-thread', { body: { recipient_user_id, body, attachment_path, creator_user_id } })` and on 201 redirects to the new thread via setActiveDmThread; for REPLY (composer inside existing thread): direct insert into direct_messages (no rate limit per D-07); on 429 from Fn, toast "You can only start 3 new conversations per day. Try again in N hours."; on 403 'blocked' toast "You can't message this person right now."
    - Web push: on first DM compose (or settings toggle for DM push), call Phase 42 `requestPushPermission({ fromUserGesture: true })` per CLAUDE.md memory reference_vapid_keypair_supabase_setup if not yet subscribed; subscription category 'community-dm' per D-21
    - vite.config.ts manualChunks: INSERT 2 new rules BEFORE existing line 206 (`if (id.includes('/src/components/community/')) return 'community-feed';`) — community-directory (CommunityDirectoryView.tsx, ProfileCard.tsx) and community-dm (DMInboxView.tsx, DMThreadView.tsx, DMMessageComposer.tsx, use-dm-inbox-realtime.ts) per RESEARCH Pitfall 6 + 45-PATTERNS.md §"vite.config.ts"
    - assert-bundle-budget.sh: APPEND two new ceiling entries — "community-directory  10 ..." and "community-dm  35 ..." per 45-PATTERNS.md §"assert-bundle-budget.sh"
    - Production build must succeed and the 2 new chunks must appear in the manifest under 10 kB gz / 35 kB gz respectively
  </behavior>
  <action>
    1. DMInboxView.tsx: Copy `CommunityFeed.tsx` skeleton (list + skeleton fallback). Replace data fetch with `supabase.from('dm_threads').select('id, creator_user_id, recipient_user_id, last_message_at, direct_messages!inner(body, created_at)').order('last_message_at', { ascending: false }).limit(50)`. Render each thread row with: counterparty handle (whichever side is not currentUserId), last_message_at, preview from last direct_messages row. Click → `setActiveDmThread(thread.id)`. Mount `useDmInboxRealtime(currentUserId, () => refetch())` at top.

    2. DMThreadView.tsx: Use react-virtuoso `<Virtuoso data={messages} itemContent={(idx, m) => <MessageRow message={m} />} />`. MessageRow renders sender handle + body via renderPostBodyHtml + attachment (if any) via signed URL. On mount, call `supabase.rpc('update_community_last_active')`. Composer at bottom — render `<DMMessageComposer threadId={threadId} recipientUserId={recipient} mode="reply" />`.

    3. DMMessageComposer.tsx: Copy `CommunityPostComposer.tsx` for attachment upload pattern. Two modes via prop: `mode="new"` (no threadId yet — calls supabase.functions.invoke('dm-create-thread')) and `mode="reply"` (has threadId — direct insert into direct_messages). Use COMMUNITY_MEDIA_MIMES whitelist + DM_ATTACHMENT_MAX_BYTES cap. On 429, toast with Retry-After hint. On 403 blocked, toast "You can't message this person right now."

    4. vite.config.ts edit: Locate line 206 (`if (id.includes('/src/components/community/')) return 'community-feed';`). INSERT IMMEDIATELY ABOVE it:
       ```typescript
       // Phase 45 Plan 45-07 — community-directory + community-dm sub-chunks.
       // ORDER MATTERS: must appear BEFORE the community-feed catch-all below.
       if (id.includes('/src/components/community/CommunityDirectoryView') ||
           id.includes('/src/components/community/ProfileCard')) return 'community-directory';
       if (id.includes('/src/components/community/DM') ||
           id.includes('/src/components/community/use-dm-inbox-realtime')) return 'community-dm';
       ```

    5. scripts/assert-bundle-budget.sh edit: locate the existing community-feed and community-media ceiling rows (around lines 46-48). APPEND two new rows after them with the exact format the script uses (read the file first to confirm the row syntax — likely `"chunk-name  N  reason"` per 45-PATTERNS.md sample). Ceilings: community-directory 10 (kB gz), community-dm 35 (kB gz).

    6. Verify production build: `cd leanshot && npm run build` succeeds. Then `scripts/assert-bundle-budget.sh` exits 0.
  </action>
  <acceptance_criteria>
    - All 3 new component files exist with named exports DMInboxView, DMThreadView, DMMessageComposer
    - `grep -c "import { Virtuoso }" leanshot/src/components/community/DMThreadView.tsx` returns ≥1 (or some equivalent react-virtuoso import — must reference the package per RESEARCH)
    - `grep -c "dm-create-thread" leanshot/src/components/community/DMMessageComposer.tsx` returns ≥1 (Fn invoke)
    - `grep -c "update_community_last_active" leanshot/src/components/community/DMThreadView.tsx` returns ≥1 (5-min debounce signal)
    - `grep -c "useDmInboxRealtime" leanshot/src/components/community/DMInboxView.tsx` returns ≥1
    - `grep -rE "new DOMPurify\\(|createDOMPurify\\(" leanshot/src/components/community/DMInboxView.tsx leanshot/src/components/community/DMThreadView.tsx leanshot/src/components/community/DMMessageComposer.tsx` returns no matches (memory feedback_silent_scope_reduction_patterns)
    - vite.config.ts: the new community-directory + community-dm rules appear BEFORE line 206 (verify via `awk` extracting their line numbers — both must be < 206). Specifically: `awk '/community-directory.|community-dm./ {print NR; found=1} /return .community-feed./ && found {print NR}' leanshot/vite.config.ts` returns 3 numbers in ascending order
    - scripts/assert-bundle-budget.sh: `grep -cE "community-directory|community-dm" leanshot/scripts/assert-bundle-budget.sh` returns ≥2
    - `cd leanshot && npm run build` exits 0 (production bundle compiles)
    - `cd leanshot && bash scripts/assert-bundle-budget.sh` exits 0 (new chunks under their ceilings; 35 kB ceiling is generous and react-virtuoso is already in deps — should pass)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && test -f src/components/community/DMInboxView.tsx && test -f src/components/community/DMThreadView.tsx && test -f src/components/community/DMMessageComposer.tsx && test $(grep -rE "new DOMPurify\\(|createDOMPurify\\(" src/components/community/DMInboxView.tsx src/components/community/DMThreadView.tsx src/components/community/DMMessageComposer.tsx | wc -l) -eq 0 && awk 'BEGIN{a=0;b=0;c=0} /community-directory/ && !a {a=NR} /community-dm/ && !b {b=NR} /return .community-feed./ && !c {c=NR} END{exit (a>0 && b>0 && c>0 && a<c && b<c) ? 0 : 1}' vite.config.ts && grep -cE "community-directory|community-dm" scripts/assert-bundle-budget.sh | awk '$1 >= 2 {print "OK"; exit 0} {print "FAIL"; exit 1}' && npm run build</automated>
  </verify>
  <done>DM inbox + thread + composer ship; vite chunks ordered correctly; bundle budget passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Postgres | Direct queries gated by RLS (directory_members_select, dm_threads SELECT participant-only) |
| browser→Edge Fn | DMMessageComposer invokes dm-create-thread with user JWT |
| browser→Storage | dm-attachments signed URL fetch via getDmAttachmentSignedUrl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05 | Tampering (XSS) | bio + DM body render | mitigate | All markdown rendered via Phase 44 renderPostBodyHtml (dompurify-config.ts); NO new DOMPurify instantiation; grep gate in acceptance criteria |
| T-45-01 | Information Disclosure | directory query | mitigate | RLS directory_members_select policy enforces two-mode visibility (45-01); client query unable to bypass |
| T-45-04 | DoS | new thread spam | mitigate | DMMessageComposer surfaces 429 from Fn (Retry-After hint in toast); client cannot bypass server-side rate limit |
| T-45-15 | Tampering | leaderboard tier-bypass | mitigate | LeaderboardChip honors locked-card UX when currentTier < space.min_tier per D-15 (Phase 44 pattern reuse) |
</threat_model>

<verification>
- TypeScript compiles after all 3 tasks
- vite production build succeeds
- bundle-budget script passes
- No new DOMPurify policy anywhere in 45-* files
- Realtime channel uses dm:${userId} (D-18 per-user inbox, not per-thread)
</verification>

<success_criteria>
- 4 new component files + 1 hook file + 2 config edits ship
- Zustand sub-view dispatch in place (NO new TabId)
- 2 new manualChunk rules ordered BEFORE community-feed catch-all
- All bio + DM body rendering routes through Phase 44 dompurify config
</success_criteria>

<output>
After completion, create `.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-07-SUMMARY.md` documenting: 16 file changes, final chunk sizes from npm run build (cite the manifest), 5 settings toggles wired, and any deferred features (e.g., unread-count badge if deferred). Cite the unread-count carry-over if deferred to v1.3-uat-deferred per memory feedback_milestone_uat_deferral_consolidation.
</output>
