---
phase: 45
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270727000001_p45_schema.sql
  - supabase/migrations/20270727000002_p45_rls.sql
  - supabase/migrations/20270727000003_p45_secdef_rpcs.sql
  - leanshot/src/lib/community/community-types.ts
  - tests/rls/community-directory-rls.test.ts
  - tests/rls/community-dm-rls.test.ts
  - tests/rls/community-reports-rls.test.ts
autonomous: true
requirements:
  - COMMUNITY-07
  - COMMUNITY-08
  - COMMUNITY-09
must_haves:
  truths:
    - "profiles table has 11 new columns (directory_opt_in, dm_open, is_clinician_verified, show_tier_badge, show_streak_badge, bio, links, admin_digest_opt_in, community_last_active_at, leaderboard_handle, leaderboard_opt_in)"
    - "community_spaces.leaderboard_enabled boolean column added"
    - "5 new tables exist: dm_threads, direct_messages, dm_thread_audit, user_block_list, community_reports"
    - "RLS policy on profiles enforces two-mode directory visibility (consumer community-wide, clinic-org same-org only)"
    - "RLS INSERT policy on dm_threads enforces symmetric block + dm_open + creator=auth.uid()"
    - "SECDEF RPCs ship: toggle_community_block, community_report_create, admin_toggle_space_leaderboard, admin_set_clinician_verified, admin_toggle_report_digest_opt_in, update_community_last_active, get_community_space_leaderboard"
    - "Cross-tenant impersonation proof tests fail when one user tries to read another's clinic-org-private directory entry"
    - "RLS test suite proves symmetric block: user B cannot create thread to user A if A has blocked B"
  artifacts:
    - path: "supabase/migrations/20270727000001_p45_schema.sql"
      provides: "Profile columns + 5 new tables + community_spaces.leaderboard_enabled"
      contains: "ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS leaderboard_handle"
    - path: "supabase/migrations/20270727000002_p45_rls.sql"
      provides: "RLS policies on all new tables + directory two-mode + dm_threads symmetric block"
      contains: "directory_members_select"
    - path: "supabase/migrations/20270727000003_p45_secdef_rpcs.sql"
      provides: "7 SECDEF RPCs for toggle/admin/leaderboard reads"
      contains: "create or replace function public.toggle_community_block"
    - path: "tests/rls/community-directory-rls.test.ts"
      provides: "Cross-tenant clinic-org isolation proof"
    - path: "tests/rls/community-dm-rls.test.ts"
      provides: "Symmetric block + thread-participant access proof"
    - path: "tests/rls/community-reports-rls.test.ts"
      provides: "Write-only consumer + staff-read-via-digest proof"
  key_links:
    - from: "supabase/migrations/20270727000001_p45_schema.sql"
      to: "profiles.leaderboard_handle"
      via: "ALTER TABLE ADD COLUMN IF NOT EXISTS"
      pattern: "leaderboard_handle text"
    - from: "supabase/migrations/20270727000002_p45_rls.sql"
      to: "user_block_list"
      via: "dm_threads INSERT WITH CHECK predicate"
      pattern: "NOT EXISTS.*user_block_list"
    - from: "supabase/migrations/20270727000003_p45_secdef_rpcs.sql"
      to: "public.is_staff()"
      via: "admin RPC guards"
      pattern: "public\\.is_staff\\(\\)"
---

<objective>
Land the Phase 45 schema foundation: profile column additions (including the new `leaderboard_handle` + `leaderboard_opt_in` columns per RESEARCH critical correction — Phase 35 stored handles per-cohort in `leaderboard_optin`, NOT globally on `profiles`); 5 new tables (`dm_threads`, `direct_messages`, `dm_thread_audit`, `user_block_list`, `community_reports`); `community_spaces.leaderboard_enabled`; all RLS policies (two-mode directory visibility per D-02 + Phase 28; symmetric block on `dm_threads` INSERT; tier-gated leaderboard SELECT mirror of Phase 44 D-08); 7 SECDEF RPCs (per memory `reference_postgres_no_insert_on_conflict_do_delete` — no `ON CONFLICT DO DELETE`; use SELECT-then-INSERT-or-DELETE); and 3 cross-tenant impersonation proof RLS test files (project Rule 2).

Purpose: Foundational schema all Wave 1 plans depend on. RLS-first design enforces D-02 cross-tenant isolation + D-10 symmetric block at the database (defense-in-depth; Edge Fn is belt).
Output: 3 migration files + 3 RLS test files + community-types.ts extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-CONTEXT.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-RESEARCH.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-PATTERNS.md

# Primary analogs — copy structure verbatim, adapt names + columns
@supabase/migrations/20270720000001_p44_community_schema.sql
@supabase/migrations/20270720000002_p44_community_rls.sql
@supabase/migrations/20270720000005_p44_community_secdef_rpcs.sql
@supabase/migrations/20270708000011_p35_leaderboard_optin.sql
@supabase/migrations/20271101000006_is_staff_helper.sql

# Cross-tenant test analog
@tests/rls/community-feed-rls.test.ts
</context>

<interfaces>
<!-- Key contracts that downstream Wave 1 plans need. -->

From `supabase/migrations/20270727000001_p45_schema.sql`:
- New `profiles` columns: `directory_opt_in boolean default false`, `dm_open boolean default true`, `is_clinician_verified boolean default false`, `show_tier_badge boolean default true`, `show_streak_badge boolean default true`, `bio text check (length(bio) <= 500)`, `links jsonb default '[]'::jsonb`, `admin_digest_opt_in boolean default false`, `community_last_active_at timestamptz`, `leaderboard_handle text check (leaderboard_handle ~ '^[a-zA-Z0-9_-]{6,24}$')`, `leaderboard_opt_in boolean default false`
- New `community_spaces.leaderboard_enabled boolean default false`
- `dm_threads (id uuid pk, creator_user_id uuid, recipient_user_id uuid, last_message_at timestamptz, created_at timestamptz)`
- `direct_messages (id uuid pk, thread_id uuid fk, sender_user_id uuid, body text check length<=2000, attachment_path text nullable, created_at timestamptz)`
- `dm_thread_audit (id uuid pk, thread_id uuid, actor_user_id uuid, reason text check in ('clinician_bypass'), created_at timestamptz)`
- `user_block_list (blocker_user_id uuid, blocked_user_id uuid, created_at timestamptz, primary key (blocker_user_id, blocked_user_id))`
- `community_reports (id uuid pk, reporter_user_id uuid, target_type text check in ('post','comment','dm_message','profile'), target_id uuid, reason text, status text default 'open', created_at timestamptz)`

From `supabase/migrations/20270727000003_p45_secdef_rpcs.sql` — exported SQL function signatures:
- `public.toggle_community_block(p_target_user_id uuid) returns boolean` — true if blocked, false if unblocked
- `public.community_report_create(p_target_type text, p_target_id uuid, p_reason text) returns uuid` — auth.uid()-based; user-context only (no service-role variant needed per RESEARCH Pattern 3 — digest Fn only reads)
- `public.admin_toggle_space_leaderboard(p_space_id uuid, p_enabled boolean) returns boolean` — is_staff()-guarded
- `public.admin_set_clinician_verified(p_user_id uuid, p_verified boolean) returns boolean` — is_staff()-guarded
- `public.admin_toggle_report_digest_opt_in(p_enabled boolean) returns boolean` — auth.uid() + is_staff() guard
- `public.update_community_last_active() returns timestamptz` — sets profiles.community_last_active_at = now()
- `public.get_community_space_leaderboard(p_space_id uuid) returns table (handle text, score bigint, rank_in_space int)` — top-10 + ±5 neighborhood; tier-gated via RLS reuse

From `leanshot/src/lib/community/community-types.ts` (extended):
- `export interface DmThread { id: string; creator_user_id: string; recipient_user_id: string; last_message_at: string | null; created_at: string }`
- `export interface DirectMessage { id: string; thread_id: string; sender_user_id: string; body: string; attachment_path: string | null; created_at: string }`
- `export interface BlockEntry { blocker_user_id: string; blocked_user_id: string; created_at: string }`
- `export interface CommunityReport { id: string; reporter_user_id: string; target_type: 'post'|'comment'|'dm_message'|'profile'; target_id: string; reason: string; status: 'open'|'reviewing'|'closed'; created_at: string }`
- `export interface SpaceLeaderboardEntry { handle: string; score: number; rank_in_space: number }`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration — profile columns + 5 new tables + community_spaces extension</name>
  <files>supabase/migrations/20270727000001_p45_schema.sql, leanshot/src/lib/community/community-types.ts</files>
  <read_first>
    - supabase/migrations/20270720000001_p44_community_schema.sql (primary analog — verbatim begin;/commit; wrapper, create table if not exists, primary key default gen_random_uuid(), CHECK constraints, comments)
    - supabase/migrations/20270708000011_p35_leaderboard_optin.sql (handle CHECK regex source — line 26)
    - leanshot/src/lib/community/community-types.ts (extend existing barrel)
  </read_first>
  <behavior>
    - profiles ALTER ADD COLUMN IF NOT EXISTS for 11 new columns with correct defaults + CHECKs (per 45-PATTERNS.md "Plan 45-01" section)
    - community_spaces ADD COLUMN IF NOT EXISTS leaderboard_enabled boolean default false
    - dm_threads created with FK to profiles(id) ON DELETE CASCADE on both creator_user_id + recipient_user_id; UNIQUE (creator_user_id, recipient_user_id) to prevent dup threads; index on (recipient_user_id, last_message_at desc) for inbox query
    - direct_messages created with FK to dm_threads ON DELETE CASCADE; CHECK body length <= 2000; index on (thread_id, created_at)
    - dm_thread_audit created; reason CHECK in ('clinician_bypass')
    - user_block_list with composite PK (blocker_user_id, blocked_user_id); FKs to profiles ON DELETE CASCADE
    - community_reports created; target_type CHECK; NO polymorphic FK on target_id (document inline per CONTEXT integration-point — Phase 48 application-level enforcement)
    - All ALTERs and CREATEs wrapped in single `begin;/commit;` transaction
    - community-types.ts barrel adds 5 new exported interfaces matching the SQL shapes exactly
  </behavior>
  <action>
    Migration filename: `supabase/migrations/20270727000001_p45_schema.sql` (14-digit timestamp + underscore + name per memory `reference_supabase_migration_filename_regex`). Open analog `20270720000001_p44_community_schema.sql`; copy the `begin;/commit;` wrapper and `comment on table/column` conventions. Add the 11 ALTER ADD COLUMN IF NOT EXISTS statements on profiles in declaration order matching the `<interfaces>` block above. The `leaderboard_handle` CHECK regex MUST be `'^[a-zA-Z0-9_-]{6,24}$'` (exact mirror of Phase 35 `leaderboard_optin.handle` constraint per RESEARCH critical correction #1 — adds the column that D-16 incorrectly claimed already existed).

    Create the 5 new tables in this order: `dm_threads` (PK uuid, FK creator + recipient → profiles, UNIQUE (creator_user_id, recipient_user_id) — prevents dup threads since this is 1:1 only), `direct_messages` (FK thread_id → dm_threads, sender_user_id → profiles, body CHECK length<=2000, attachment_path nullable), `dm_thread_audit` (thread_id, actor_user_id, reason CHECK in ('clinician_bypass')), `user_block_list` (composite PK, both FKs to profiles), `community_reports` (target_type CHECK; document polymorphic target_id absence inline with `comment on column community_reports.target_id is 'Polymorphic FK — no DB constraint; enforced application-side. Phase 48 moderation queue consumes.'`).

    Add `ALTER TABLE community_spaces ADD COLUMN IF NOT EXISTS leaderboard_enabled boolean NOT NULL DEFAULT false;` per D-14.

    Index list: `idx_dm_threads_recipient_last_msg` on (recipient_user_id, last_message_at DESC) for inbox; `idx_dm_threads_creator_created` on (creator_user_id, created_at) for rate-limit count; `idx_direct_messages_thread_created` on (thread_id, created_at); `idx_user_block_list_blocked` on (blocked_user_id, blocker_user_id) for inverse lookup; `idx_community_reports_status_target_type` on (status, target_type) for digest query.

    Extend `leanshot/src/lib/community/community-types.ts` with 5 new exported interfaces verbatim per the `<interfaces>` block.

    Do NOT add RLS in this task — that's Task 2. Do NOT add SECDEF RPCs — that's Task 3.
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/20270727000001_p45_schema.sql` exists; filename matches regex `^[0-9]{14}_[a-z_0-9]+\.sql$`
    - `grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/20270727000001_p45_schema.sql` returns ≥12 (11 profiles + 1 community_spaces)
    - `grep -c "leaderboard_handle text" supabase/migrations/20270727000001_p45_schema.sql` returns 1 (the critical RESEARCH-correction column)
    - `grep -c "create table if not exists public\\.\\(dm_threads\\|direct_messages\\|dm_thread_audit\\|user_block_list\\|community_reports\\)" supabase/migrations/20270727000001_p45_schema.sql` returns 5
    - `grep -v '^[[:space:]]*--' supabase/migrations/20270727000001_p45_schema.sql | grep -c "primary key default gen_random_uuid"` returns ≥4 (per-table uuid PKs)
    - `grep -c "leanshot/src/lib/community/community-types.ts" -lE "export interface (DmThread|DirectMessage|BlockEntry|CommunityReport|SpaceLeaderboardEntry)"` returns 1 path (all 5 present)
    - `cd leanshot && npm run build` succeeds (TypeScript barrel compiles)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/20270727000001_p45_schema.sql | awk '$1 >= 12 {print "OK"; exit 0} {print "FAIL"; exit 1}' && grep -c "leaderboard_handle text" supabase/migrations/20270727000001_p45_schema.sql | awk '$1 == 1 {print "OK"; exit 0} {print "FAIL"; exit 1}' && cd leanshot && npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <done>Schema migration file exists with all 12 ALTERs + 5 CREATEs; community-types.ts compiles with 5 new exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RLS migration + 3 cross-tenant proof test suites</name>
  <files>supabase/migrations/20270727000002_p45_rls.sql, tests/rls/community-directory-rls.test.ts, tests/rls/community-dm-rls.test.ts, tests/rls/community-reports-rls.test.ts</files>
  <read_first>
    - supabase/migrations/20270720000002_p44_community_rls.sql (primary RLS analog — DO $$ begin if not exists idempotency, policy naming convention)
    - tests/rls/community-feed-rls.test.ts (analog test structure — admin.generateLink + /auth/v1/verify pattern per memory `reference_rls_fixture_gotrueclient_flake`)
    - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-RESEARCH.md §Pattern 1 + §Pattern 8 (full RLS predicates)
  </read_first>
  <behavior>
    - enable row level security on all 6 new/extended objects (profiles for directory_members_select; dm_threads; direct_messages; dm_thread_audit; user_block_list; community_reports)
    - Directory two-mode policy `directory_members_select` on profiles per RESEARCH Pattern 8 (consumer NOT EXISTS org_members OR clinic same-org via EXISTS)
    - dm_threads INSERT policy `dm_threads_insert_not_blocked` per RESEARCH Pattern 1 (creator=auth.uid AND recipient.dm_open=true AND NOT EXISTS block)
    - dm_threads SELECT policy: participant-only (creator OR recipient = auth.uid)
    - direct_messages SELECT + INSERT policies: participant-only via JOIN to dm_threads
    - dm_thread_audit SELECT: staff-only via public.is_staff(); INSERT: service-role only (no policy for authenticated → write blocked)
    - user_block_list SELECT + INSERT + DELETE: blocker_user_id = auth.uid()
    - community_reports INSERT: auth.uid() = reporter_user_id; SELECT: public.is_staff() (write-only for consumers; staff read-via-digest pattern per CONTEXT D-11)
    - Test 1 (community-directory-rls.test.ts): two clinic-org users in different orgs both opted-in → user A cannot SELECT user B's row; consumer user C (no org) can SELECT both
    - Test 2 (community-dm-rls.test.ts): user A blocks user B → user B INSERT on dm_threads (creator=B, recipient=A) returns 42501 (RLS violation); user B SELECT on existing thread before block still works
    - Test 3 (community-reports-rls.test.ts): consumer user A SELECT community_reports returns 0 rows (RLS hides); A INSERT works; staff user B SELECT returns A's row
  </behavior>
  <action>
    Migration filename: `supabase/migrations/20270727000002_p45_rls.sql`. Copy `20270720000002_p44_community_rls.sql` policy creation pattern (DO $$ begin if not exists ... policyname check). Implement all 8 policies named above. The directory_members_select policy text comes verbatim from 45-PATTERNS.md §"Plan 45-01 — directory_members_select policy" (RESEARCH Pattern 8). The dm_threads_insert_not_blocked policy text comes from RESEARCH Pattern 1.

    For staff-gated policies (`dm_thread_audit_select`, `community_reports_select`), use `using (public.is_staff())` — NOT a `staff_users` join per memory `feedback_negation_grep_defeated_by_comment_string` (do not add a comment referencing the rejected alternative). Helper `public.is_staff()` lives at `supabase/migrations/20271101000006_is_staff_helper.sql`.

    Create the 3 RLS test files at `tests/rls/community-directory-rls.test.ts`, `tests/rls/community-dm-rls.test.ts`, `tests/rls/community-reports-rls.test.ts`. Use the file-scoped slug prefix pattern (per memory `feedback_rls_per_file_slug_prefix` — TEST_SLUG_PREFIX defined inside each file, never shared). Use admin.generateLink + /auth/v1/verify via plain fetch for fixture user provisioning (per memory `reference_rls_fixture_gotrueclient_flake` — the 2026-05-16 ES256 fix). Test bodies follow vitest `describe/it` shape used in analog `tests/rls/community-feed-rls.test.ts`.

    Each test file must include AT LEAST one cross-tenant impersonation negative-proof test (project Rule 2 — every RLS surface gets a live cross-tenant proof). Use `it.skip` only with the `[DEFERRED — see deferred-tests.md]` suffix if needed (per memory `reference_vitest_skip_fixme` — `it.fixme` throws).
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/20270727000002_p45_rls.sql` exists
    - `grep -vE '^[[:space:]]*--' supabase/migrations/20270727000002_p45_rls.sql | grep -c "create policy"` returns ≥8
    - `grep -c "public\\.is_staff()" supabase/migrations/20270727000002_p45_rls.sql` returns ≥2 (dm_thread_audit_select, community_reports_select)
    - `grep -c "staff_users" supabase/migrations/20270727000002_p45_rls.sql` returns 0 (rejected alternative — per memory feedback_negation_grep_defeated_by_comment_string)
    - 3 test files exist with .test.ts suffix (not -test.ts)
    - Each test file declares its own `TEST_SLUG_PREFIX` constant
    - `cd leanshot && npm test -- --run tests/rls/community-directory-rls.test.ts tests/rls/community-dm-rls.test.ts tests/rls/community-reports-rls.test.ts` exits 0 OR explicitly skips with deferred-tests.md reference (test infrastructure may not be reachable in worktree; document in SUMMARY if so)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -vE '^[[:space:]]*--' supabase/migrations/20270727000002_p45_rls.sql | grep -c "create policy" | awk '$1 >= 8 {print "OK"; exit 0} {print "FAIL"; exit 1}' && test $(grep -c "staff_users" supabase/migrations/20270727000002_p45_rls.sql) -eq 0 && test -f tests/rls/community-directory-rls.test.ts && test -f tests/rls/community-dm-rls.test.ts && test -f tests/rls/community-reports-rls.test.ts && echo "OK"</automated>
  </verify>
  <done>RLS migration creates ≥8 policies; staff gating uses is_staff() not staff_users; 3 test files exist with file-scoped slugs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SECDEF RPC migration — 7 RPCs (toggle/admin/leaderboard/activity)</name>
  <files>supabase/migrations/20270727000003_p45_secdef_rpcs.sql</files>
  <read_first>
    - supabase/migrations/20270720000005_p44_community_secdef_rpcs.sql (primary analog — verbatim function shell at lines 25–86; idempotent toggle pattern at lines 57–76)
    - supabase/migrations/20270708000014_p35_leaderboard_rpcs.sql (get_leaderboard_for_user analog for get_community_space_leaderboard)
    - 45-PATTERNS.md §"supabase/migrations/<ts>_p45_secdef_rpcs.sql"
  </read_first>
  <behavior>
    - 7 SECDEF functions per 45-PATTERNS.md analog list, all with `security definer set search_path = public, extensions`, `if v_user_id is null then raise 'unauthenticated'` guard, `revoke execute from public, anon; grant execute to authenticated`
    - toggle_community_block: SELECT FOR UPDATE then INSERT or DELETE (NO `ON CONFLICT DO DELETE` per memory reference_postgres_no_insert_on_conflict_do_delete); returns boolean (true = now blocked, false = unblocked)
    - community_report_create: auth.uid()-based insert; returns inserted id uuid (per RESEARCH Pattern 3 — single user-context RPC; digest Fn only reads, no service-role write RPC needed)
    - admin_toggle_space_leaderboard: is_staff() guard then UPDATE community_spaces.leaderboard_enabled
    - admin_set_clinician_verified: is_staff() guard then UPDATE profiles.is_clinician_verified
    - admin_toggle_report_digest_opt_in: is_staff() guard then UPDATE profiles.admin_digest_opt_in WHERE id = auth.uid()
    - update_community_last_active: UPDATE profiles.community_last_active_at = now() WHERE id = auth.uid(); returns the timestamptz
    - get_community_space_leaderboard: SELECT top-10 + ±5 neighborhood of caller from community_space_leaderboard_matview WHERE space_id = p_space_id; returns table (handle text, score bigint, rank_in_space int) — note: matview itself ships in 45-06, but the function definition can reference it (function body is not validated until run-time)
  </behavior>
  <action>
    Migration filename: `supabase/migrations/20270727000003_p45_secdef_rpcs.sql`. Copy the 20270720000005 file's `create or replace function ... security definer set search_path = public, extensions ... as $fn$ ... $fn$;` shell + the `if v_user_id is null` guard + the `revoke/grant` trailer for every one of the 7 functions.

    For `toggle_community_block`: use SELECT FOR UPDATE on user_block_list inside the function body — `if exists (...) then delete ... else insert ... end if;` pattern (verbatim shape from analog lines 57–76). Return `not exists(...)` after the toggle = boolean reflecting the new state.

    For admin guards: add `if not public.is_staff() then raise exception 'forbidden' using errcode = '42501'; end if;` BEFORE any write.

    For `get_community_space_leaderboard`: implement the top-10 + ±5 neighborhood with the same pattern as Phase 35 `get_leaderboard_for_user` in `20270708000014_p35_leaderboard_rpcs.sql` (UNION of `rank <= 10` with `rank between (caller_rank - 5) and (caller_rank + 5)`). The matview body lands in plan 45-06 — this function compiles even if the matview is not yet created (function body validation is lazy).

    Do NOT use `INSERT … ON CONFLICT DO DELETE` anywhere (memory reference_postgres_no_insert_on_conflict_do_delete). Do NOT reference `staff_users` (memory feedback_negation_grep_defeated_by_comment_string).
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/20270727000003_p45_secdef_rpcs.sql` exists
    - `grep -vE '^[[:space:]]*--' supabase/migrations/20270727000003_p45_secdef_rpcs.sql | grep -cE "create or replace function public\\.(toggle_community_block|community_report_create|admin_toggle_space_leaderboard|admin_set_clinician_verified|admin_toggle_report_digest_opt_in|update_community_last_active|get_community_space_leaderboard)"` returns 7
    - `grep -c "ON CONFLICT DO DELETE" supabase/migrations/20270727000003_p45_secdef_rpcs.sql` returns 0 (prohibited per memory)
    - `grep -c "staff_users" supabase/migrations/20270727000003_p45_secdef_rpcs.sql` returns 0
    - `grep -c "security definer" supabase/migrations/20270727000003_p45_secdef_rpcs.sql` returns ≥7
    - `grep -c "set search_path = public, extensions" supabase/migrations/20270727000003_p45_secdef_rpcs.sql` returns ≥7
    - `grep -c "public.is_staff()" supabase/migrations/20270727000003_p45_secdef_rpcs.sql` returns ≥3 (the 3 admin_* RPCs)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -vE '^[[:space:]]*--' supabase/migrations/20270727000003_p45_secdef_rpcs.sql | grep -cE "create or replace function public\\.(toggle_community_block|community_report_create|admin_toggle_space_leaderboard|admin_set_clinician_verified|admin_toggle_report_digest_opt_in|update_community_last_active|get_community_space_leaderboard)" | awk '$1 == 7 {print "OK"; exit 0} {print "FAIL"; exit 1}' && test $(grep -c "ON CONFLICT DO DELETE" supabase/migrations/20270727000003_p45_secdef_rpcs.sql) -eq 0 && test $(grep -c "staff_users" supabase/migrations/20270727000003_p45_secdef_rpcs.sql) -eq 0 && echo "OK"</automated>
  </verify>
  <done>7 SECDEF RPCs ship with auth.uid() + is_staff() guards as required; no prohibited syntax; ready for Wave 1 Edge Fn callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Postgres RLS | Authenticated user reads/writes profiles, dm_threads, direct_messages, user_block_list, community_reports |
| client→SECDEF RPC | User invokes toggle/admin/activity RPCs; auth.uid() + is_staff() enforced inside function |
| Edge Fn→Postgres | Service-role bypasses RLS; only `community-admin-report-digest` reads community_reports server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information Disclosure | profiles RLS (directory) | mitigate | Two-mode `directory_members_select` policy enforces consumer community-wide OR clinic same-org via Phase 28 `org_members` join; cross-tenant proof test in `community-directory-rls.test.ts` |
| T-45-02 | Spoofing | dm_threads INSERT | mitigate | RLS WITH CHECK creator_user_id = auth.uid() (sender cannot forge identity); Edge Fn 45-04 adds JWT-sub vs body claim check (defense-in-depth) |
| T-45-06 | Tampering | dm_threads INSERT — symmetric block | mitigate | RLS predicate NOT EXISTS user_block_list where recipient blocked creator; proof test in `community-dm-rls.test.ts` |
| T-45-07 | DoS | community_reports — report spam | mitigate | community_report_create RPC requires auth.uid(); per-user daily-cap NOT in v1 (deferred to Phase 48); accept low-volume in v1 |
| T-45-08 | Spoofing | profiles.is_clinician_verified | mitigate | admin_set_clinician_verified RPC is_staff()-guarded; no self-claim path; consumer UI exposes read-only badge |
</threat_model>

<verification>
- All 3 migration files compile via `supabase db push --linked` (deferred to plan 45-09 final push)
- TypeScript barrel `leanshot/src/lib/community/community-types.ts` compiles with `tsc -p tsconfig.app.json --noEmit`
- 3 RLS test files exist with file-scoped slug prefixes
- No prohibited tokens in any migration (`ON CONFLICT DO DELETE`, `staff_users`)
</verification>

<success_criteria>
- 11 new profile columns + 5 new tables + community_spaces.leaderboard_enabled all exist in migration 20270727000001
- 8+ RLS policies in migration 20270727000002 with is_staff() (not staff_users)
- 7 SECDEF RPCs in migration 20270727000003 with auth.uid() + is_staff() + no ON CONFLICT DO DELETE
- 3 cross-tenant impersonation proof test files exist (one per attack surface: directory, dm, reports)
</success_criteria>

<output>
After completion, create `.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-01-SUMMARY.md` documenting: migrations created (filenames + table/column count), RLS policy list, RPC list, test file paths, and any deferred test cases (with deferred-tests.md cross-ref).
</output>
