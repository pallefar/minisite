---
phase: 45
plan: 09
type: execute
wave: 3
depends_on: [45-01, 45-02, 45-03, 45-04, 45-05, 45-06, 45-07, 45-08]
files_modified:
  - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-CARRY-OVER.md
  - .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-SUMMARY.md
autonomous: false
requirements:
  - COMMUNITY-07
  - COMMUNITY-08
  - COMMUNITY-09
must_haves:
  truths:
    - "All 6 P45 migrations pushed to linked Supabase project (supabase db push --linked) — schema, RLS, SECDEF RPCs, notification widening, dm-attachments bucket, leaderboard matview + cron, admin digest cron"
    - "Both new Edge Fns deployed (dm-create-thread, community-admin-report-digest) — supabase functions deploy NO --linked NO --import-map flag per memory references"
    - "Production bundle build succeeds + bundle-budget script passes (community-directory ≤10 kB gz, community-dm ≤35 kB gz)"
    - "Phase 35 cron now refreshes BOTH matviews — verified via `supabase db query --linked` SELECT FROM cron.job at schedule 12,27,42,57"
    - "Daily community-admin-report-digest cron registered at 0 9 * * *"
    - "Notification CHECK widening verified on live DB: 'community-dm' and 'community-admin-report' both accepted; non-listed category rejected (CHECK violation 23514)"
    - "6-signal HUMAN-UAT checkpoint discrete carry-over per memory feedback_multi_signal_human_verify_checkpoint_pattern"
    - "45-SUMMARY.md documents phase outcome + per-plan push-status matrix per memory feedback_phase_close_out_db_push_verification"
    - "ROADMAP.md Phase 45 entry flipped from [ ] to [x] (or status set to 'Complete + approved automated-verify-only' if HUMAN-UAT defers any signals)"
  artifacts:
    - path: ".planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-CARRY-OVER.md"
      provides: "Per-plan push-status matrix + deferred HUMAN-UAT signal list"
    - path: ".planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-SUMMARY.md"
      provides: "Phase outcome summary + plan SUMMARY index + key learnings"
  key_links:
    - from: "supabase db push --linked"
      to: "6 P45 migrations + 1 Phase 35 cron consolidation"
      via: "CLI"
      pattern: "20270727"
    - from: "supabase functions deploy dm-create-thread"
      to: "Linked project"
      via: "Per-fn deno.json"
      pattern: "dm-create-thread"
---

<objective>
Phase 45 close-out: push all 6 P45 migrations + deploy 2 Edge Fns + verify cron consolidation + verify notification CHECK widening + production bundle build + 6-signal HUMAN-UAT checkpoint. This plan is the [BLOCKING] gate before Phase 45 completes. Per memory `feedback_multi_signal_human_verify_checkpoint_pattern`, the HUMAN gate is structured as 6 discrete signals so operator can approve CLI-runnable inline + carry browser/device signals to milestone close (per memory `feedback_hitl_walkthrough_deferred_when_fixtures_missing`).

Per memory `feedback_phase_close_out_db_push_verification`: the close-out MUST include a per-plan push-status matrix so subsequent phases don't leak un-pushed migrations.

Purpose: Wave 3 — depends on Wave 2 completion (45-07, 45-08) and indirectly on all prior plans.
Output: Deployment evidence + verification queries + 6-signal HUMAN-UAT + 45-CARRY-OVER.md + 45-SUMMARY.md + ROADMAP flip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-CONTEXT.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-RESEARCH.md

# Prior plan SUMMARYs (referenced for plan-status matrix)
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-01-PLAN.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-02-PLAN.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-03-PLAN.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-04-PLAN.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-05-PLAN.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-06-PLAN.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-07-PLAN.md
@.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-08-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-push validation sweep (migration filename + Deno cross-Fn sweep + bundle build)</name>
  <files>(reads only — no writes; sets pre-push gate)</files>
  <read_first>
    - memory reference_supabase_migration_filename_regex
    - memory reference_migration_timestamp_collision_precheck
    - memory feedback_post_merge_deno_sweep_pattern
    - memory feedback_batched_edits_verify_file_count
  </read_first>
  <behavior>
    - Verify all 6 P45 migration filenames match `^[0-9]{14}_p45[a-z_]+\.sql$`
    - Verify no timestamp collisions: `ls supabase/migrations/20270727*.sql | wc -l` returns 6 (no surprises from sibling-plan additions)
    - Run a single cross-Fn Deno sweep per memory feedback_post_merge_deno_sweep_pattern: `$HOME/.deno/bin/deno test --no-check --allow-env supabase/functions/dm-create-thread/index.test.ts supabase/functions/community-admin-report-digest/index.test.ts supabase/functions/notify-community/index.test.ts` — all 3 suites must pass (notify-community regression check from the 45-02 extension)
    - Run a production bundle build: `cd leanshot && npm run build` succeeds
    - Run bundle-budget script: `cd leanshot && bash scripts/assert-bundle-budget.sh` exits 0
    - Run TypeScript noEmit check across whole app: `cd leanshot && npx tsc -p tsconfig.app.json --noEmit` exits 0
    - Verify per memory feedback_batched_edits_verify_file_count: `git show --stat HEAD~10..HEAD` includes all expected files from each plan SUMMARY's commit (visual review)
  </behavior>
  <action>
    Run the following commands sequentially. If any fails, HALT and report to operator — do NOT proceed to db push:

    ```
    # 1. Filename regex check (no letter suffixes — memory reference_supabase_migration_filename_regex)
    ls supabase/migrations/20270727*.sql | grep -vE '^supabase/migrations/[0-9]{14}_p45[a-z_]+\.sql$' && echo "FAIL: filename regex" || echo "OK: filenames"

    # 2. Collision pre-check (memory reference_migration_timestamp_collision_precheck)
    test $(ls supabase/migrations/20270727*.sql | wc -l) -eq 6 || (echo "FAIL: expected 6 P45 migrations, found different count" && ls supabase/migrations/20270727*.sql)

    # 3. Cross-Fn Deno sweep (memory feedback_post_merge_deno_sweep_pattern)
    $HOME/.deno/bin/deno test --no-check --allow-env \
      supabase/functions/dm-create-thread/index.test.ts \
      supabase/functions/community-admin-report-digest/index.test.ts \
      supabase/functions/notify-community/index.test.ts

    # 4. Bundle build
    cd leanshot && npm run build

    # 5. Bundle-budget check
    cd leanshot && bash scripts/assert-bundle-budget.sh

    # 6. TypeScript noEmit
    cd leanshot && npx tsc -p tsconfig.app.json --noEmit
    ```

    Document the output of each step in the task SUMMARY (cite which checks ran green / red).
  </action>
  <acceptance_criteria>
    - All 6 commands exit 0
    - 6 P45 migration files all present with correct 14-digit timestamps
    - No filename in the P45 migration set has a letter suffix
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/migrations/20270727*.sql | grep -vE '^supabase/migrations/[0-9]{14}_p45[a-z_]+\\.sql$' | wc -l | awk '$1 == 0 {print "OK"; exit 0} {print "FAIL"; exit 1}' && test $(ls supabase/migrations/20270727*.sql | wc -l) -eq 6 && $HOME/.deno/bin/deno test --no-check --allow-env supabase/functions/dm-create-thread/index.test.ts supabase/functions/community-admin-report-digest/index.test.ts supabase/functions/notify-community/index.test.ts && cd leanshot && npm run build && bash scripts/assert-bundle-budget.sh && npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <done>Pre-push validation green; ready for db push + Fn deploy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: supabase db push + Edge Fn deploys + live verification queries</name>
  <what-built>
    All 6 P45 migrations + 2 Edge Fns ready to deploy. Pre-push validation passed in Task 1.

    Deployment surface:
    - `supabase db push --linked` from `/Users/karstenhaldan/minisite` (memory reference_minisite_monorepo_layout — git root, NOT inside leanshot/) — applies the 6 P45 migrations in timestamp order: 000001 schema, 000002 RLS, 000003 SECDEF RPCs, 000004 notification widening, 000005 dm-attachments bucket, 000006 admin digest cron, 000007 leaderboard matview + Phase 35 cron consolidation.
    - `supabase functions deploy dm-create-thread` (NO --linked, NO --import-map per memory references reference_supabase_functions_deploy_no_linked_flag + reference_supabase_functions_deploy_import_map_flag)
    - `supabase functions deploy community-admin-report-digest` (same flags)
    - `supabase functions deploy notify-community` (re-deploy because 45-02 extended it with kind='dm_new')
    - `supabase functions deploy notification-send` (re-deploy because 45-02 extended VALID_CATEGORIES)

    Post-deploy verification commands (assistant runs these via Bash; operator confirms):
    1. `supabase db query --linked "SELECT jobname, schedule FROM cron.job WHERE jobname IN ('phase35-leaderboard-refresh', 'community-admin-report-digest') ORDER BY jobname"` — expect 2 rows; phase35 at `12,27,42,57 * * * *`, community-admin-report-digest at `0 9 * * *`
    2. `supabase db query --linked "SELECT count(*) FROM pg_matviews WHERE matviewname = 'community_space_leaderboard_matview'"` — expect 1
    3. `supabase db query --linked "SELECT count(*) FROM pg_indexes WHERE indexname = 'idx_community_space_lb_space_user' AND tablename = 'community_space_leaderboard_matview'"` — expect 1 (UNIQUE index for REFRESH CONCURRENTLY)
    4. `supabase db query --linked "SELECT category FROM notification_category_config WHERE category IN ('community-dm','community-admin-report') ORDER BY category"` — expect 2 rows
    5. `supabase db query --linked "SELECT id, file_size_limit FROM storage.buckets WHERE id = 'dm-attachments'"` — expect 1 row with file_size_limit=5242880
    6. `supabase db query --linked "SELECT COUNT(*) FROM pg_proc WHERE proname IN ('toggle_community_block','community_report_create','admin_toggle_space_leaderboard','admin_set_clinician_verified','admin_toggle_report_digest_opt_in','update_community_last_active','get_community_space_leaderboard')"` — expect 7

    Also probe both deployed Fns return 401 on missing bearer (no auth attempted):
    - `curl -s -o /dev/null -w '%{http_code}\n' https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/dm-create-thread -X POST -d '{}'` — expect 401
    - `curl -s -o /dev/null -w '%{http_code}\n' https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/community-admin-report-digest -X POST -d '{}'` — expect 401
  </what-built>
  <how-to-verify>
    Operator confirms by approving the inline output of the 6 db queries + 2 curl probes. Each query result should match the expected row count/value above.

    If any query returns wrong count: HALT and surface the discrepancy. Likely causes per memory `reference_supabase_back_dated_migration_blocks_push`: a back-dated migration in another branch blocks the push (recovery: mv to /tmp, push, restore).

    Per memory `feedback_worktree_executor_pwd_drift_leaks_to_main` + `reference_gsd_sdk_state_complete_phase_cwd_sensitivity`: ensure all CLI invocations run from `/Users/karstenhaldan/minisite/` (git root), NOT from `/Users/karstenhaldan/minisite/leanshot/`.

    Per memory `reference_supabase_functions_deploy_no_linked_flag`: `supabase functions deploy <name>` from project root with NO `--linked` flag (link auto-read from `supabase/.temp/`). Per memory `reference_supabase_functions_deploy_import_map_flag`: NO `--import-map` flag (CLI v2.101.0+ ignores it; per-fn deno.json is the contract).
  </how-to-verify>
  <resume-signal>Operator types "db push + deploys verified" OR documents specific failures with remediation steps.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 6-signal HUMAN-UAT (discrete per memory feedback_multi_signal_human_verify_checkpoint_pattern)</name>
  <what-built>
    Phase 45 is deployed (Task 2). The 6 signals below correspond to the success criteria in CONTEXT.md and the HUMAN-UAT checkpoint pattern. Operator approves each signal individually OR carries to milestone-uat-deferred per memory `feedback_milestone_uat_deferral_consolidation`.
  </what-built>
  <how-to-verify>
    **Signal A — Directory opt-in toggle round-trip (COMMUNITY-07)**
    1. Operator signs in as a consumer user (clear `localStorage.leanshot_v4` first per memory `reference_zustand_persisted_user_blocks_marketing_uat`)
    2. Navigate Settings → Community → toggle "Show me in the directory" ON
    3. Open Community → Directory sub-view
    4. Confirm own profile card renders
    5. Toggle OFF → card disappears from directory

    **Signal B — DM thread creation + rate-limit 4th attempt → 429 (COMMUNITY-08)**
    1. Operator signs in as user A; user B exists with dm_open=true
    2. Send DM to user B (thread 1)
    3. Send DMs to users C, D (threads 2, 3)
    4. Attempt to send to user E (thread 4)
    5. Confirm toast "You can only start 3 new conversations per day. Try again in N hours." (429 from Edge Fn)

    **Signal C — Block-then-create → 403 (COMMUNITY-08)**
    1. As user A, navigate to user B's profile card
    2. Click block (toggle_community_block RPC) — confirm UI updates to "Blocked"
    3. As user B (separate session or impersonation via dev tools), attempt to create a new thread to user A
    4. Confirm "You can't message this person right now." toast (403 blocked)

    **Signal D — Leaderboard chip renders with seeded post data (COMMUNITY-09)**
    1. Operator seeds 3 community_posts for user A in a leaderboard-enabled space (via SQL or fixture script)
    2. Wait for cron refresh (max 15 min) OR run `supabase db query --linked "REFRESH MATERIALIZED VIEW CONCURRENTLY public.community_space_leaderboard_matview;"` to force refresh
    3. Open the space in consumer UI
    4. Confirm LeaderboardChip renders top-3 with user A's leaderboard_handle + score 9 (3 posts × 3)

    **Signal E — Admin verified-clinician toggle (COMMUNITY-07)**
    1. Operator signs in as admin (is_staff=true)
    2. Navigate /admin/community/profiles
    3. Toggle verified-clinician on user A
    4. As another user, view A's profile card in directory → confirm verified-clinician badge appears

    **Signal F — Admin report-digest email receipt (COMMUNITY-09)**
    1. Operator toggles admin_digest_opt_in ON via /admin/community/reports
    2. Operator submits a test report (consumer flow: any post → Report button → submit reason)
    3. Operator manually triggers the cron: `curl -X POST https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/community-admin-report-digest -H "Authorization: Bearer $SERVICE_ROLE_KEY" -d '{}'`
    4. Confirm digest email arrives in admin inbox within 30s with open_count >= 1 and target_type row

    **Carry-over policy per memory feedback_hitl_walkthrough_deferred_when_fixtures_missing:** If any signal cannot be tested locally (missing fixtures, missing OAuth wiring, missing Resend domain verification, etc.), defer it to `.planning/milestones/v1.3-uat-deferred.md` (memory `feedback_milestone_uat_deferral_consolidation`) and flip phase status to "complete + approved automated-verify-only".
  </how-to-verify>
  <resume-signal>Operator types one of:
    - "all 6 signals approved" → flip to complete
    - "A,C,D approved; B,E,F deferred to v1.3-uat-deferred" (or similar comma list) → flip to complete + approved automated-verify-only; carry-over the listed signals
    - "halt: <reason>" → close-out blocked; orchestrator routes to /gsd-plan-phase --gaps
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write 45-CARRY-OVER.md + 45-SUMMARY.md + flip ROADMAP entry</name>
  <files>.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-CARRY-OVER.md, .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-SUMMARY.md, .planning/ROADMAP.md</files>
  <read_first>
    - memory feedback_phase_close_out_db_push_verification (per-plan push-status matrix shape)
    - memory feedback_milestone_uat_deferral_consolidation (v1.3-uat-deferred.md format)
    - .planning/ROADMAP.md (line 64 — Phase 45 entry to flip)
  </read_first>
  <behavior>
    - 45-CARRY-OVER.md: per-plan push-status matrix (table with columns: Plan | Migration filename | Push status | Fn deploy status | Notes); HUMAN-UAT signal status table (signal A..F | approved/deferred | carry-over destination)
    - 45-SUMMARY.md: phase outcome (Complete / Complete + automated-verify-only / Blocked); plan index (link to each 45-NN-SUMMARY.md); key learnings to feed into MEMORY.md (e.g., "Phase 35 leaderboard_handle was per-cohort not per-profile — RESEARCH inverted the planner-outline concern"); links to CARRY-OVER.md
    - ROADMAP.md edit: line 64 — flip `- [ ] **Phase 45:` → `- [x] **Phase 45:` (or use sed/Edit to toggle the checkbox). If status is "complete + automated-verify-only", append " ✓ approved automated-verify-only" to the entry per memory feedback_hitl_walkthrough_deferred_when_fixtures_missing
    - Per memory reference_gsd_sdk_state_complete_phase_cwd_sensitivity: do NOT run `gsd-sdk query state.complete-phase` from git root — must be inside `leanshot/` directory. Alternative: use direct file Edit on ROADMAP.md (per the memory: `roadmap.complete-phase` verb does NOT exist, use direct sed/Edit to toggle `- [ ]` → `- [x]`)
  </behavior>
  <action>
    1. Create `.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-CARRY-OVER.md`:

       ```markdown
       # Phase 45 — Carry-Over

       ## Per-Plan Push Status

       | Plan | Migration | DB pushed | Fn deployed | Notes |
       |------|-----------|-----------|-------------|-------|
       | 45-01 | 20270727000001_p45_schema.sql + 000002_p45_rls.sql + 000003_p45_secdef_rpcs.sql | ✓ | — | Schema + RLS + 7 SECDEF RPCs |
       | 45-02 | 20270727000004_p45_notification_widening.sql | ✓ | notify-community ✓ / notification-send ✓ | Atomic CHECK widening |
       | 45-03 | 20270727000005_p45_dm_attachments_bucket.sql | ✓ | — | Storage bucket + RLS |
       | 45-04 | — | — | dm-create-thread ✓ | New Edge Fn |
       | 45-05 | 20270727000006_p45_admin_report_digest_cron.sql | ✓ | community-admin-report-digest ✓ | Daily 9am cron |
       | 45-06 | 20270727000007_p45_leaderboard_matview.sql | ✓ | — | Matview + Phase 35 cron consolidation |
       | 45-07 | — | — | — | Consumer UI; bundle build passed |
       | 45-08 | — | — | — | Admin UI; pathname-routed; SECDEF RPCs from 45-01 |

       ## HUMAN-UAT Signal Status

       | Signal | Description | Status | Carry-over |
       |--------|-------------|--------|-----------|
       | A | Directory opt-in round-trip | approved / deferred | (link to v1.3-uat-deferred if deferred) |
       | B | Rate-limit 4th attempt → 429 | ... | ... |
       | C | Block-then-create → 403 | ... | ... |
       | D | Leaderboard chip renders | ... | ... |
       | E | Admin verified-clinician toggle | ... | ... |
       | F | Admin report-digest email | ... | ... |
       ```

       Fill in actual status from Task 3 operator response.

    2. Create `.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-SUMMARY.md`:

       ```markdown
       # Phase 45 — M4 Community Spaces + Member Directory + Opt-in DMs + Leaderboard — SUMMARY

       **Status:** Complete (or "Complete + approved automated-verify-only" if any HUMAN-UAT signal deferred)
       **Closed:** 2026-05-23
       **Requirements:** COMMUNITY-07 ✓, COMMUNITY-08 ✓, COMMUNITY-09 ✓

       ## Plan Index

       - [45-01](./45-01-SUMMARY.md) — Schema + RLS + SECDEF RPCs
       - [45-02](./45-02-SUMMARY.md) — Notification CHECK widening (atomic)
       - [45-03](./45-03-SUMMARY.md) — dm-attachments Storage bucket
       - [45-04](./45-04-SUMMARY.md) — dm-create-thread Edge Fn
       - [45-05](./45-05-SUMMARY.md) — community-admin-report-digest Edge Fn + daily cron
       - [45-06](./45-06-SUMMARY.md) — Leaderboard matview + Phase 35 cron consolidation
       - [45-07](./45-07-SUMMARY.md) — Consumer UI (directory + DM + leaderboard chip)
       - [45-08](./45-08-SUMMARY.md) — Admin UI (clinician toggle + report digest opt-in + space leaderboard toggle)

       ## Key Learnings (for MEMORY.md candidates)

       - Phase 35 stored leaderboard handles per-cohort in `leaderboard_optin`, not per-profile — RESEARCH inverted the planner-outline concern at STATE.md line 788. Memory candidate: `reference_phase35_leaderboard_handle_per_cohort_storage`.
       - `dm-attachments` Storage RLS required EXISTS dm_threads JOIN (not the simple `(foldername)[1] = auth.uid()` pattern from community-media) because path[0] is thread_id, not user_id. Memory candidate: `reference_storage_bucket_path_segment_is_resource_id`.
       - Cron consolidation pattern: UNSCHEDULE + RE-REGISTER with same job name, multiple REFRESH calls in body — beats creating a parallel cron at same cadence.

       ## See Also
       - [45-CARRY-OVER.md](./45-CARRY-OVER.md)
       ```

    3. Open `.planning/ROADMAP.md` and Edit line 64. If all 6 HUMAN-UAT signals approved: change `- [ ] **Phase 45:` → `- [x] **Phase 45:`. If any signal deferred: change to `- [x] **Phase 45:` AND append ` ✓ approved automated-verify-only` after the existing description (or per current ROADMAP convention — re-read the line first to pattern-match). Update the Progress table (around line 597-599) marking COMMUNITY-07/08/09 status from "Pending" → "Complete".
  </action>
  <acceptance_criteria>
    - `.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-CARRY-OVER.md` exists with per-plan matrix + HUMAN-UAT table
    - `.planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-SUMMARY.md` exists with plan index + learnings
    - `grep -c "Phase 45:" .planning/ROADMAP.md | head -1` shows line 64 prefix is `- [x]` (not `- [ ]`)
    - Progress table at .planning/ROADMAP.md around lines 597-599 marks COMMUNITY-07/08/09 status as "Complete" (or "Approved AVO" if any signal deferred)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test -f .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-CARRY-OVER.md && test -f .planning/phases/45-m4-community-spaces-member-directory-opt-in-dms-leaderboard/45-SUMMARY.md && grep -q "^- \\[x\\] \\*\\*Phase 45:" .planning/ROADMAP.md && echo "OK"</automated>
  </verify>
  <done>Carry-over + SUMMARY shipped; ROADMAP flipped; phase officially closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→Supabase CLI | db push + functions deploy invoke admin-level operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-CO1 | Repudiation | partial deploy (some migrations + some Fns) | mitigate | Per-plan push-status matrix in CARRY-OVER.md provides audit trail; Task 1 pre-push validation catches drift before Task 2 |
| T-45-CO2 | Tampering | wrong project ref | mitigate | All CLI commands cite the linked project (auto-read from supabase/.temp/); operator confirms in Task 2 |
</threat_model>

<verification>
- All 6 P45 migrations live on linked project (verification queries return expected counts)
- Both new Edge Fns deployed; auth probe returns 401 on missing bearer
- Phase 35 cron now refreshes both matviews
- ROADMAP Phase 45 entry flipped
</verification>

<success_criteria>
- All 6 P45 migrations pushed
- 4 Edge Fns deployed (2 new + 2 re-deploys for the extensions in 45-02)
- 6-signal HUMAN-UAT completed (all approved OR explicit deferral list)
- 45-CARRY-OVER.md + 45-SUMMARY.md + ROADMAP flip
</success_criteria>

<output>
After completion, ensure all four artifacts are in place: 45-09-SUMMARY.md (this plan's outcome), 45-CARRY-OVER.md (per-plan matrix), 45-SUMMARY.md (phase outcome), ROADMAP.md (Phase 45 checkbox flipped). Per memory `feedback_orchestrator_inline_completes_returned_executor`: if executor returned status:completed without writing SUMMARY, orchestrator writes inline rather than respawning.
</output>
