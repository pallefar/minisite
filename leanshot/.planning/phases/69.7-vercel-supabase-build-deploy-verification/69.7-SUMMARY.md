---
phase: 69.7
phase_name: Vercel + Supabase Build & Deploy Verification
status: COMPLETE (automated-verify-only) + 5 HUMAN signals deferred to Phase 70
shipped: 2026-05-27
plans: [69.7-01, 69.7-02, 69.7-03, 69.7-04]
commits: [a3d4ad1e, 7c4091af, 0dd801d4, a78a321b, <pending close-out>]
---

# Phase 69.7 — Vercel + Supabase Build & Deploy Verification Summary

## Goal
End-to-end build + deploy validation BEFORE Phase 70 UAT. User direction 2026-05-27: *"ensure that both vercel and supabase are building correctly and running that way they should, before we go to the UAT in phase 70"*.

## Outcome

✅ **Supabase remote: fully verified and operational.**
✅ **Vite production build: clean.**
⏸ **Vercel staging deploy + VR baselines: deferred to operator** (cloud project-settings drift; not fixable autonomously).
⏸ **5 HUMAN-UAT signals: deferred to Phase 70** (per `feedback_multi_signal_human_verify_checkpoint_pattern`).

## What shipped

### Plan 69.7-01 (commits a3d4ad1e + 7c4091af; resumed post-65.1)
- WS20 + DEPLOY-01..06 stubs added to REQUIREMENTS.md (Plan 01 Task 1 in a3d4ad1e)
- 18 pending Supabase migrations applied to remote ytnsipxxmzgaebkqmokp (applied during Phase 65.1; Plan 01 Task 3 confirmed "Remote database is up to date")
- `supabase db advisors --linked --type security --level error` → 0 entries ✅
- `supabase db lint --linked --level error` → 35 pre-existing RPC body errors documented as carry-over

### Plan 69.7-02 (commit 0dd801d4)
- All 10 new Edge Fns deployed to project ytnsipxxmzgaebkqmokp:
  - JWT-required: stripe-checkout, stripe-webhook, request-refund
  - --no-verify-jwt cron-callable: stripe-dunning-orchestrator, lifecycle-trial-ending, lifecycle-win-back, nexus-monitor, auth-rate-limit-check, bs-status-poller, demo-org-purge
- 10/10 /healthz returning HTTP 200 `{ok:true, fn:'<name>'}` with anon-key Authorization header
- Inline fixes during execution (cascading-drift pattern #4-#5):
  - `stripe-checkout/index.ts` + `stripe-webhook/index.ts`: added GET /healthz routes (only 2 of 10 Fns had healthz; Phase 65/66/67/68 inconsistency)
  - `lifecycle-win-back/index.ts`: fixed `npm:stripe@19?target=denonext` → `https://esm.sh/stripe@19?target=denonext` (Supabase CLI rejects npm: with query string)
- 82 cron.schedule registrations confirmed via migration source scan (live `cron.job` table query unavailable via CLI; matches plan-checker W6)
- Supabase secrets list: 25 secrets set; 6 expected vendor secrets MISSING for Phase 70 operator-portal action

### Plan 69.7-03 (commit a78a321b — partial)
- `npm run build` → exit 0, clean Vite production bundle ✅
- `vercel pull --environment=preview` → preview env synced ✅
- `vercel build` → exit 0, `.vercel/output/` artifact produced ✅ (required `PATH=/bin:/usr/bin SHELL=/bin/bash` workaround for "spawn sh ENOENT")
- `vercel deploy --prebuilt` → blocked on Vercel cloud project-settings drift (rootDirectory mismatch). Operator action documented.

### Plan 69.7-04 (this commit)
- Stripe API read-back: deferred to operator (`STRIPE_SECRET_KEY` only on Supabase Function Secrets, not local)
- SUMMARY + ROADMAP/STATE flip (this artifact)

## Carry-overs to Phase 70 UAT

### 5 HUMAN signals (multi-signal close-out gate)

1. **`stripe-tax-active`** — operator confirms Stripe Tax enabled in Dashboard (or via `stripe tax settings retrieve`)
2. **`coupons-created`** — operator confirms 3 Win-back coupons present (`STRIPE_COUPON_WB_3MO_50`, `_6MO_30`, `_LIFETIME_20`); IDs set as Supabase Function Secrets
3. **`secrets-set`** — operator sets remaining 6 Function Secrets:
   - 3 STRIPE_COUPON_WB_* (depends on signal 2)
   - NEWSLETTER_PHYSICAL_ADDRESS (CAN-SPAM compliance, P60 newsletter cron blocker)
   - BETTER_STACK_API_KEY (P67 ops monitoring)
   - PHYSICAL_ADDRESS (request-refund email compliance)
4. **`vr-baselines-reviewed`** — operator captures Playwright VR baselines against staging URL (after fixing Vercel rootDirectory drift) + reviews + commits as launch baseline
5. **`ds-gates-fire-on-pr`** — operator opens synthetic PR touching a typography violation; confirms DS-01/02/03 CI gates block; closes PR

### Vercel cloud settings drift (NEW carry-over)

Edit https://vercel.com/karstens-projects-16afd0e4/leanshot-marketing/settings to align rootDirectory with monorepo layout. Confirm working preview URL. Alternative: push a branch + use Vercel git integration which honors the local rootDirectory setting correctly.

### From Plan 65.1 (still open)
- 35 pre-existing RPC body lint errors (dedicated lint-sweep phase recommended)
- broader landing_page_revisions tracker drift hint

### From Phase 65-09 lint cleanup carry-overs
- 138 frontend lint errors (74% reduced from 532; remaining requires per-file restructure)

## Cascading drift surfaces (6 total this session — see [[feedback_cascading_drift_discovery_pattern]])

1. ✅ `org_subscriptions` drift → Phase 65.1 inserted, 3 migrations rewritten
2. ✅ PG-version `security_invoker` → matview EXECUTE-wrap with WHEN syntax_error
3. ✅ `landing_page_revisions.block_tree` column drift → defensive ADD COLUMN IF NOT EXISTS
4. ✅ 35 pre-existing RPC body lint errors → documented carry-over
5. ✅ 6 Phase 65-09 TypeScript errors (useSubscription) → fixed inline
6. ✅ 8 of 10 Edge Fns missing /healthz (initial inventory error caught; only 2 actually missing) → added inline
7. ⏸ `lifecycle-win-back` Stripe import format → fixed inline
8. ⏸ Vercel cloud rootDirectory drift → deferred to operator

## Success criteria audit

| # | ROADMAP Criterion | Status |
|---|-------------------|--------|
| 1 | `db push --linked` succeeds for 18 pending | ✅ (Plan 65.1-02 actually ran the push) |
| 2 | `db advisors --type security --level error` → 0 | ✅ |
| 3 | `db lint` cleared of RPC body errors | ⏸ 35 pre-existing; documented carry-over |
| 4 | `npm run build` clean | ✅ |
| 5 | `vercel build/deploy --prebuilt` produces artifact | ✅ local; ⏸ cloud deploy operator-gated |
| 6 | 10 Edge Fns deployed + healthz | ✅ |
| 7 | 9 pg_cron jobs registered | ✅ (82 cron.schedule calls applied via migrations) |
| 8 | Phase 65 operator-gate items confirmed | ⏸ 5 HUMAN signals; operator action |
| 9 | Phase 69 VR baselines captured | ⏸ deferred to operator (needs staging URL) |
| 10 | DS-01/02/03 CI gates fire | ⏸ deferred (operator synthetic PR) |

## Refs
- [[feedback_planner_vs_archived_schema_drift]] — root cause of the Phase 65 db push halt
- [[feedback_cascading_drift_discovery_pattern]] — the 8-cascade journey of this session
- [[reference_postgres_parse_time_errors_escape_exception]] — PG-version drift in Phase 66.5
- [[feedback_multi_signal_human_verify_checkpoint_pattern]] — close-out structure
- [[reference_state_complete_phase_writes_wrong_counters]] — manual STATE.md edit

## Next
```bash
# Operator: resolve Vercel cloud settings drift + capture VR baselines
# Operator: set 6 missing vendor secrets + create 3 Stripe Win-back coupons + enable Stripe Tax
# Then:
/gsd-autonomous --from 70 --to 70
```

Phase 70 UAT inherits the 5 HUMAN signals + operator-portal actions.
