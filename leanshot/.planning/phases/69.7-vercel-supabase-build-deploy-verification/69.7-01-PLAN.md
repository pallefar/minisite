---
phase: 69.7-vercel-supabase-build-deploy-verification
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-push.log
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-advisors.log
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-lint.log
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/migration-list.log
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02]
must_haves:
  truths:
    - "Phase 65 org_subscriptions schema-tracking drift is resolved by operator (verified via clean migration list)"
    - "All 18 pending Supabase migrations applied to remote ytnsipxxmzgaebkqmokp project"
    - "supabase db advisors returns 0 ERROR-level security findings"
    - "supabase db lint returns 0 RPC body errors"
    - "REQUIREMENTS.md contains DEPLOY-01..06 stubs with acceptance criteria"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DEPLOY-01..06 requirement stubs"
      contains: "DEPLOY-01"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-push.log"
      provides: "stdout/stderr capture of supabase db push"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-advisors.log"
      provides: "supabase db advisors output (must contain 0 errors)"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-lint.log"
      provides: "supabase db lint output (must contain 0 errors)"
  key_links:
    - from: "operator psql resolution"
      to: "supabase db push --linked"
      via: "migration list returns 18 pending in chronological order"
      pattern: "supabase migration list --linked"
---

<objective>
Resolve Phase 65 `org_subscriptions` schema-tracking drift (operator-owned), apply all 18 pending Supabase migrations to remote project `ytnsipxxmzgaebkqmokp`, and clear ERROR-level advisor + RPC-body lint findings.

Purpose: Unblock Edge Fn deploy (Plan 69.7-02) and Vercel staging deploy (Plan 69.7-03) by ensuring the remote DB schema matches what Phase 65-68 code expects. Add new DEPLOY-01..06 requirement stubs to REQUIREMENTS.md so traceability is complete before close-out.
Output: DEPLOY-01..06 stubs in REQUIREMENTS.md; clean db-push.log / db-advisors.log / db-lint.log artifacts; STATE.md progressed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-CONTEXT.md
</context>

<critical_invariants>
- **PWD INVARIANT** — Every commit MUST run from `/Users/karstenhaldan/minisite/leanshot` (the worktree root). Before each `git commit`, run `git rev-parse --show-toplevel` and assert it ends with `/leanshot`. If not, `cd /Users/karstenhaldan/minisite/leanshot` first.
- **No worktree isolation** — This phase runs INLINE in main checkout (per `feedback_phase_close_out_supabase_gotchas` (3); ambient `supabase` CLI auth doesn't transfer to spawned Agents).
- **No destructive drift recovery** — If `supabase migration list --linked` shows tracking drift, HALT and surface to operator via the HUMAN gate. Do NOT run `migration repair` autonomously.
- **No back-dated migrations** — If a local migration timestamp predates remote's last-applied, HALT and surface operator-only `git mv` fix per `reference_supabase_back_dated_migration_blocks_push`.
- **No `CREATE POLICY IF NOT EXISTS`** — if Task 5 generates a retroactive RPC migration, use bare `CREATE POLICY` (or `DROP POLICY IF EXISTS` + `CREATE POLICY`) per `feedback_phase_close_out_supabase_gotchas` (1).
</critical_invariants>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Operator drift-resolution gate</name>
  <what-built>
    Before this plan can execute, the operator must have resolved the Phase 65 `org_subscriptions` schema-tracking drift via psql (per STATE.md "Operator next" line). This is a HUMAN gate — Plan 69.7 cannot autonomously recover from migration tracking drift.
  </what-built>
  <how-to-verify>
    Operator runs the following and confirms output:

    ```bash
    cd /Users/karstenhaldan/minisite/leanshot
    npx supabase migration list --linked --project-ref ytnsipxxmzgaebkqmokp
    ```

    Expected: 18 local migrations appear in `Local` column with `Remote` column EMPTY (i.e. pending). The previous failing migration `20290104000001` should NOT show `applied with errors` or similar drift markers.

    If the operator cannot get the list clean, they must intervene via Supabase Dashboard → Database → Migration history, OR open a `psql` session and manually reconcile `supabase_migrations.schema_migrations` rows. Plan cannot proceed otherwise.
  </how-to-verify>
  <resume-signal>Type `drift-resolved` if migration list is clean. Type `blocked` + paste the drift output to halt and re-route to operator.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add DEPLOY-01..06 stubs to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — find the appropriate WS section near the end OR add a new "WS20 — Deploy Verification (DEPLOY, 6 REQ-IDs)" block)
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-CONTEXT.md (acceptance criteria for each DEPLOY ID)
    - .planning/ROADMAP.md lines 549-569 (Success Criteria 1-10 map to DEPLOY-01..06)
  </read_first>
  <action>
    Append a new workstream block to REQUIREMENTS.md (placement: after the last existing WS, before any closing footer). Use this exact text structure (insert into REQUIREMENTS.md verbatim — no fenced code blocks, no "v1" qualifiers):

    Block header: `### WS20 — Deploy Verification (DEPLOY, 6 REQ-IDs)` with a one-line description: `> Phase 69.7 — end-to-end build + deploy validation before Phase 70 UAT.`

    Then 6 checkbox lines:
    - DEPLOY-01: All 18 pending Supabase migrations (10 Phase 65 + 2 Phase 66 + 3 Phase 66.5 + 3 Phase 68) applied to remote project `ytnsipxxmzgaebkqmokp`; `supabase migration list --linked` shows all entries with non-empty Remote column.
    - DEPLOY-02: `supabase db advisors --linked --type security --level error` returns 0 entries; `supabase db lint --linked --fail-on error` clears RPC body errors.
    - DEPLOY-03: All 10 new Edge Fns deployed via `supabase functions deploy`; `GET /functions/v1/<name>/healthz` returns `{"ok":true,...}` for each.
    - DEPLOY-04: 9 pg_cron jobs registered and visible in `cron.job` table: 5 Phase 65 jobs + 1 Phase 66 (auth-attempts retention) + 1 Phase 67 (bs-status-poller) + 1 Phase 68 (demo-org-purge) + 1 additional Phase 66 cron.
    - DEPLOY-05: `npm run build` in `leanshot/` produces clean dist/; `vercel deploy --prebuilt` against staging returns a preview URL; Phase 69 VR baselines captured against the staging URL and committed.
    - DEPLOY-06: Phase 65 operator-gate items confirmed via Stripe API read-back (Stripe Tax active, 3 Win-back coupons present) + `supabase secrets list` (4-5 vendor secrets); DS-01/02/03 CI gates fire on a synthetic PR with intentional violation.
  </action>
  <verify>
    <automated>grep -c "^- \[ \] \*\*DEPLOY-0" /Users/karstenhaldan/minisite/leanshot/.planning/REQUIREMENTS.md | grep -E "^[6-9]|^[1-9][0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains exactly 6 lines matching `- [ ] **DEPLOY-0` (one per requirement).
    - WS20 header `### WS20 — Deploy Verification` present.
    - Each DEPLOY-0X line includes the acceptance criteria from the action body (no placeholder text like `TBD` or `TODO`).
  </acceptance_criteria>
  <done>REQUIREMENTS.md has WS20 block with 6 DEPLOY-0X requirement stubs; file committed.</done>
</task>

<task type="auto">
  <name>Task 2: Audit pending migrations + check for back-dated timestamps</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/migration-list.log</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-CONTEXT.md (CLI authentication + back-dated migration constraints)
    - leanshot/supabase/migrations/ (list directory; identify the 18 pending files by recency)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/leanshot`, run `npx supabase migration list --linked --project-ref ytnsipxxmzgaebkqmokp` and capture both stdout and stderr to `.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/migration-list.log` (create artifacts directory if needed).

    Parse the output:
    1. Find the most recent timestamp in the `Remote` column (last applied migration on remote).
    2. List all 18 entries with empty `Remote` column (the pending set).
    3. Verify EVERY pending local timestamp is strictly GREATER than the last-applied remote timestamp.

    If ANY pending local timestamp is less than the last-applied remote timestamp, HALT immediately. Append a clear error block to the log file and surface to user with the exact migration filename + remediation: operator should `git mv` the offending file to a forward timestamp (per `feedback_migration_filename_dependency_order` + `reference_supabase_back_dated_migration_blocks_push`). Do NOT auto-mv.

    If all timestamps forward-of-remote: proceed to Task 3.
  </action>
  <verify>
    <automated>test -f /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/migration-list.log && grep -E "Local.*Remote" /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/migration-list.log</automated>
  </verify>
  <acceptance_criteria>
    - `migration-list.log` exists and contains the table header `Local | Remote`.
    - The artifact captures at minimum 18 rows with empty Remote column.
    - No back-dated timestamps detected (or if detected, plan HALTED with clear remediation surfaced).
  </acceptance_criteria>
  <done>Migration list captured; back-dated check passed OR plan halted with remediation surfaced.</done>
</task>

<task type="auto">
  <name>Task 3: Apply all 18 pending migrations via supabase db push</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-push.log</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/migration-list.log (from Task 2)
    - leanshot/supabase/migrations/ (the 18 pending files)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/leanshot`, run `npx supabase db push --linked --project-ref ytnsipxxmzgaebkqmokp` (idempotent — applies only pending migrations) and tee BOTH stdout and stderr to `.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-push.log`.

    Expected outcome: all 18 migrations applied successfully. Exit code 0.

    If exit code non-zero:
    - Capture failing migration filename + error message from log.
    - Surface to user. Do NOT attempt `supabase migration repair`.
    - Halt plan.

    After success: re-run `npx supabase migration list --linked --project-ref ytnsipxxmzgaebkqmokp` and append output to `db-push.log` for verification — every previously-pending row should now show a Remote timestamp.
  </action>
  <verify>
    <automated>grep -E "^Finished|^Applying migration|18 migrations" /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-push.log | head -5</automated>
  </verify>
  <acceptance_criteria>
    - db-push.log shows "Finished" or equivalent success indicator from supabase CLI.
    - No rows with empty Remote column when migration list is re-run (or operator-surfaced halt).
    - Exit code of the db push command was 0.
  </acceptance_criteria>
  <done>18 migrations applied to remote; success confirmed via re-running migration list.</done>
</task>

<task type="auto">
  <name>Task 4: Run db advisors (security errors must be 0)</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-advisors.log</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-push.log (confirm push succeeded)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/leanshot`, run `npx supabase db advisors --linked --project-ref ytnsipxxmzgaebkqmokp --type security --level error` and tee to `.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-advisors.log`.

    Note: the `--fail-on error` flag may or may not be supported depending on CLI version — if rejected, parse the output manually. Acceptance criterion: 0 ERROR-level entries.

    If any ERROR rows surface despite Phase 66.5 having shipped the code fixes:
    - Capture each finding (table, function, or RLS policy + remediation hint).
    - Surface to user with options: (a) draft retroactive migration in a new file `leanshot/supabase/migrations/<new-forward-ts>_p69-7-advisor-remediation.sql` (use bare `CREATE POLICY` or `DROP POLICY IF EXISTS` + `CREATE POLICY`, NOT `CREATE POLICY IF NOT EXISTS`); (b) defer to Phase 70.
    - Do NOT auto-draft the migration without user direction.
  </action>
  <verify>
    <automated>test -f /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-advisors.log</automated>
  </verify>
  <acceptance_criteria>
    - db-advisors.log exists and contains the advisors output.
    - ERROR-level count is 0 OR all surfaced findings have an operator-confirmed disposition (remediate now / defer to Phase 70).
  </acceptance_criteria>
  <done>Security advisor output captured; ERROR count = 0 OR user-confirmed disposition recorded.</done>
</task>

<task type="auto">
  <name>Task 5: Run db lint (RPC body errors must be 0)</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-lint.log</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-advisors.log (from Task 4)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/leanshot`, run `npx supabase db lint --linked --project-ref ytnsipxxmzgaebkqmokp --fail-on error` and tee to `.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-lint.log`.

    Acceptance criterion: 0 ERROR-level findings (warnings acceptable).

    If RPC body errors surface:
    - Identify the offending function name and lint message.
    - Surface to user with retroactive migration option: drop + recreate the RPC in a new forward-timestamped migration. Bare `CREATE FUNCTION` (no `IF NOT EXISTS`) preceded by `DROP FUNCTION IF EXISTS <name>(<args>) CASCADE;`. Do NOT auto-draft.

    Defer non-error severities to Phase 70.
  </action>
  <verify>
    <automated>test -f /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-lint.log</automated>
  </verify>
  <acceptance_criteria>
    - db-lint.log exists and contains the lint output.
    - ERROR-level count is 0 OR all surfaced findings have an operator-confirmed disposition.
  </acceptance_criteria>
  <done>DB lint output captured; ERROR count = 0 OR user-confirmed disposition recorded.</done>
</task>

<task type="auto">
  <name>Task 6: Commit artifacts + progress STATE.md</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/migration-list.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-push.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-advisors.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/db-lint.log
  </files>
  <read_first>
    - .planning/STATE.md (current counters; do NOT use `state.complete-phase` SDK verb — manual edit only)
  </read_first>
  <action>
    Assert PWD: `git rev-parse --show-toplevel` MUST end in `/leanshot`. If not, `cd /Users/karstenhaldan/minisite/leanshot`.

    Stage artifacts + REQUIREMENTS.md. Commit message: `chore(69.7-01): db push + advisor/lint sweep + DEPLOY-01..06 stubs`.

    Do NOT progress STATE.md `completed_phases` here — Plan 69.7-04 (close-out) owns the final counter flip. Optionally update STATE.md "Current Position" to `Phase: 69.7 (Vercel + Supabase Build & Deploy Verification) — EXECUTING, Plan 1 complete`.

    Use `gsd-sdk query commit` or plain `git add` + `git commit` depending on available tooling.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && git log -1 --pretty=%s | grep -E "69.7-01"</automated>
  </verify>
  <acceptance_criteria>
    - Single commit landed on main with subject matching `chore(69.7-01)`.
    - All 4 artifact log files present in the commit.
    - REQUIREMENTS.md change included in commit.
    - PWD invariant honored (no commit landed from outside leanshot/).
  </acceptance_criteria>
  <done>Plan 69.7-01 artifacts committed; downstream plans 69.7-02 + 69.7-03 unblocked.</done>
</task>

</tasks>

<verification>
- All 18 migrations applied to remote (db-push.log shows success + re-run migration list shows no pending).
- db advisors security-level ERROR count = 0.
- db lint ERROR count = 0 (or operator-disposed).
- REQUIREMENTS.md WS20 block with 6 DEPLOY-0X stubs.
- Single clean commit on main with PWD invariant honored.
</verification>

<success_criteria>
DEPLOY-01 and DEPLOY-02 acceptance criteria satisfied; Plans 69.7-02 + 69.7-03 can dispatch in parallel as Wave 1.
</success_criteria>

<output>
Create `.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-01-SUMMARY.md` when done.
</output>
