---
phase: 69.7-vercel-supabase-build-deploy-verification
plan: 04
type: execute
wave: 2
depends_on: ["69.7-02", "69.7-03"]
files_modified:
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/stripe-tax-readback.json
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/stripe-coupons-readback.json
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/ds-gate-verification.log
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [DEPLOY-06]
must_haves:
  truths:
    - "Stripe Tax confirmed active (via Stripe API read-back OR operator confirmation)"
    - "3 Win-back coupons confirmed present in Stripe account (via API OR operator confirmation)"
    - "Required operator-only secrets confirmed set (NEWSLETTER_PHYSICAL_ADDRESS, BETTER_STACK_API_KEY, VITE_CALENDLY_BOOK_DEMO_URL)"
    - "VR baselines from Plan 69.7-03 reviewed and accepted by operator as launch baseline"
    - "DS-01/02/03 CI gates confirmed firing on a synthetic PR with intentional violation"
    - "69.7-SUMMARY.md exists with full phase outcome roll-up"
    - "ROADMAP.md Phase 69.7 entry flipped to checked"
    - "STATE.md completed_phases counter incremented (manual edit, NOT state.complete-phase verb)"
  artifacts:
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/stripe-tax-readback.json"
      provides: "Stripe API GET /v1/tax/settings response (must show status active)"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/stripe-coupons-readback.json"
      provides: "Stripe API GET /v1/coupons response (must contain 3 Win-back coupon IDs)"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/ds-gate-verification.log"
      provides: "Synthetic PR + CI gate blocking confirmation"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-SUMMARY.md"
      provides: "Phase 69.7 outcome SUMMARY"
  key_links:
    - from: "Stripe API read-back"
      to: "Operator HUMAN gate"
      via: "auto-confirms what's already done; only flags missing items"
      pattern: "tax/settings | coupons"
    - from: "Synthetic PR with intentional violation"
      to: "GitHub Actions CI run"
      via: "DS-01/02/03 gates from Phase 69"
      pattern: "DS-01"
---

<objective>
Auto-confirm what Stripe API can read back (Tax settings + Win-back coupons), then run a multi-signal HUMAN close-out gate for the rest (operator portal items + VR baseline review + synthetic-PR CI-gate test). Write phase SUMMARY, flip ROADMAP, increment STATE counter.

Purpose: Resolve the final operator-gate items deferred from Phase 65, validate that Phase 69's CI gates (DS-01/02/03) actually block bad PRs, and formally close Phase 69.7 so the autonomous runner can advance to Phase 70 UAT.
Output: 2 Stripe read-back JSON artifacts + DS-gate verification log + SUMMARY + ROADMAP/STATE updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-CONTEXT.md
@.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-01-PLAN.md
@.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-02-PLAN.md
@.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-03-PLAN.md
</context>

<critical_invariants>
- **PWD INVARIANT** — All `git commit` calls MUST run from `/Users/karstenhaldan/minisite/leanshot`.
- **Inline dispatch only** — no isolated worktree (ambient CLI auth + STATE.md cwd-sensitivity).
- **STATE.md manual edit only** — DO NOT use `gsd-sdk query state.complete-phase` verb per `reference_state_complete_phase_writes_wrong_counters`; it rewrites counters incorrectly. Edit STATE.md by hand, then `git diff` to verify counter monotonically increases (12 → 13 completed_phases for v1.4).
- **Multi-signal HUMAN gate** — per `feedback_multi_signal_human_verify_checkpoint_pattern`, Task 3 ships N discrete resume signals (operator approves each individually). Auto-confirm reduces operator load — only items the API can't verify go in front of the operator.
- **No operator-portal automation** — Stripe Dashboard, Calendly, Better Stack portals are NOT scripted. Phase 70 owns operator-portal registrations; this plan only verifies what's already done.
- **No production cutover** — staging URL only. `vercel --prod` is Phase 70's call.
</critical_invariants>

<tasks>

<task type="auto">
  <name>Task 1: Stripe API read-back — Tax settings active?</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/stripe-tax-readback.json</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/secrets-list.log (from 69.7-02; confirm STRIPE_SECRET_KEY is set on Supabase, which mirrors a usable test/live key)
  </read_first>
  <action>
    Resolve `STRIPE_SECRET_KEY` from local environment. Order of preference:
    1. `$STRIPE_SECRET_KEY` if exported in current shell
    2. `.env.local` in `leanshot/` (grep `STRIPE_SECRET_KEY=`)
    3. `npx supabase secrets list --project-ref ytnsipxxmzgaebkqmokp` (NOTE: this returns names only, not values — if neither (1) nor (2) yields the key, surface to user as a HUMAN gate sub-signal: operator pastes the key OR exports it before re-running)

    Once resolved, run:
    `curl -sS https://api.stripe.com/v1/tax/settings -u "${STRIPE_SECRET_KEY}:" -H "Stripe-Version: 2024-06-20"` and tee response JSON to `artifacts/stripe-tax-readback.json`.

    Parse `.status` field from JSON. Acceptance: `"status": "active"`.

    Outcomes:
    - `status:active` → mark Tax-gate AUTO-CONFIRMED. No need for operator signal `stripe-tax-active` in Task 3.
    - `status:pending` or non-200 response → mark Tax-gate NEEDS-OPERATOR. Include `stripe-tax-active` in Task 3 HUMAN gate signal list.

    Append disposition line to JSON file (as a trailing JSON line comment OR a sibling `.disposition` text file): `disposition: AUTO-CONFIRMED | NEEDS-OPERATOR`.
  </action>
  <verify>
    <automated>test -f /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/stripe-tax-readback.json</automated>
  </verify>
  <acceptance_criteria>
    - stripe-tax-readback.json exists with the Stripe API response body.
    - Disposition (AUTO-CONFIRMED or NEEDS-OPERATOR) captured.
  </acceptance_criteria>
  <done>Tax setting state known; routed to auto-confirm OR HUMAN gate.</done>
</task>

<task type="auto">
  <name>Task 2: Stripe API read-back — 3 Win-back coupons present?</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/stripe-coupons-readback.json</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/secrets-list.log (confirm STRIPE_COUPON_WB_* secrets present; if missing, the read-back attempt is still useful — operator may have created them in Stripe with different IDs)
  </read_first>
  <action>
    Using the same `STRIPE_SECRET_KEY` resolved in Task 1:

    `curl -sS "https://api.stripe.com/v1/coupons?limit=100" -u "${STRIPE_SECRET_KEY}:" -H "Stripe-Version: 2024-06-20"` and tee response JSON to `artifacts/stripe-coupons-readback.json`.

    Look for coupons matching:
    - 3-month 50% off (Win-back 3mo / 50%) — naming convention: contains "WB" and "3" and "50" in id/name OR matches the value of secret `STRIPE_COUPON_WB_3MO_50`
    - 6-month 30% off (Win-back 6mo / 30%) — match `STRIPE_COUPON_WB_6MO_30` value if accessible
    - Lifetime 20% off (Win-back lifetime / 20%) — match `STRIPE_COUPON_WB_LIFETIME_20` value

    If you cannot resolve the secret VALUES (Supabase masks them), fall back to fuzzy match: look for coupons where the coupon's `id` or `name` field contains "wb" + the percentage + the duration.

    Acceptance: 3 matching coupons found.

    Outcomes:
    - 3 matches → AUTO-CONFIRMED, no operator signal needed.
    - <3 matches → NEEDS-OPERATOR, include `coupons-created` in Task 3.

    Append disposition.
  </action>
  <verify>
    <automated>test -f /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/stripe-coupons-readback.json</automated>
  </verify>
  <acceptance_criteria>
    - stripe-coupons-readback.json exists.
    - Disposition recorded (3 matches AUTO-CONFIRMED, OR <3 NEEDS-OPERATOR with the missing set listed).
  </acceptance_criteria>
  <done>Coupon state known; routed appropriately.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Multi-signal HUMAN close-out gate</name>
  <what-built>
    Plan 69.7-01 applied 18 migrations, Plan 69.7-02 deployed 10 Fns + 9 cron jobs + inventoried secrets, Plan 69.7-03 built Vercel staging + captured VR baselines, and Tasks 1-2 above attempted Stripe read-back for Tax + Coupons.

    What remains: 5 discrete signals the operator must confirm. Each can be approved individually — partial approval is acceptable and rolls deferred items into Phase 70.
  </what-built>
  <how-to-verify>
    Review the artifact files from 69.7-01..03 + Tasks 1-2, then individually approve each signal below by typing its keyword. Skip signals already AUTO-CONFIRMED in Task 1/2 — orchestrator will only present the un-confirmed subset.

    **Signal A — `stripe-tax-active`** (only if Task 1 returned NEEDS-OPERATOR)
    Confirm Stripe Tax is enabled in your Stripe Dashboard → Settings → Tax → Tax settings (status = Active for the live key). If not enabled, you can defer to Phase 70.

    **Signal B — `coupons-created`** (only if Task 2 returned NEEDS-OPERATOR)
    Confirm 3 Win-back coupons exist in Stripe Dashboard → Products → Coupons:
    - 3-month 50% off
    - 6-month 30% off
    - Lifetime 20% off
    Defer to Phase 70 if missing.

    **Signal C — `secrets-set`**
    Review `.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/secrets-list.log` (from 69.7-02). For any MISSING line in the inventory:
    - `NEWSLETTER_PHYSICAL_ADDRESS` — required before newsletter cron fires (Sun 13:00 UTC). Set via `supabase secrets set NEWSLETTER_PHYSICAL_ADDRESS="..." --project-ref ytnsipxxmzgaebkqmokp` if you have an address, else defer to Phase 70.
    - `BETTER_STACK_API_KEY` — required by Phase 67 status page. Set if you have a Better Stack account, else defer.
    - `VITE_CALENDLY_BOOK_DEMO_URL` — Vercel build-time env. Set via `vercel env add VITE_CALENDLY_BOOK_DEMO_URL preview` (and `production` when promoting). Defer to Phase 70 if no Calendly link yet.
    Approve `secrets-set` when each MISSING item is either set OR explicitly deferred.

    **Signal D — `vr-baselines-reviewed`**
    Visually inspect baselines committed by Plan 69.7-03 (under `leanshot/test-results/vr-snapshots/` or wherever Phase 69.4 placed them). For a fast review:
    ```bash
    cd /Users/karstenhaldan/minisite/leanshot
    open leanshot/test-results/vr-snapshots/  # macOS
    ```
    Confirm: no rendering bugs visible (no invisible text per [[feedback_ui_auditor_catches_undefined_theme_tokens]] regression), no obvious layout breaks, no placeholder strings showing in production paths. If any baseline shows a bug, halt — fix needs a follow-up plan before launch.

    **Signal E — `ds-gates-fire-on-pr`**
    Open a synthetic PR with one intentional violation, e.g.:
    1. `cd /Users/karstenhaldan/minisite/leanshot && git checkout -b test/p69.7-ds-gate-fire`
    2. Edit any file under `leanshot/src/` to add e.g. `<div className="text-[15px]">test</div>` (violates DS-02 typography ceiling)
    3. `git add . && git commit -m "test(p69.7): synthetic DS-02 violation" && git push origin test/p69.7-ds-gate-fire`
    4. `gh pr create --title "[p69.7] test DS gate fire" --body "Synthetic — close after gate fires" --base main`
    5. Wait for GitHub Actions; confirm at least one of DS-01/02/03 gates BLOCKS the PR with a failing check.
    6. `gh pr close <pr-number> --delete-branch`

    Tee the CI run URL + pass/fail per gate to `.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/ds-gate-verification.log` before approving.
  </how-to-verify>
  <resume-signal>
    Type any subset of these keywords on separate lines (one per signal):
    - `stripe-tax-active` — Tax enabled (or defer-to-p70)
    - `coupons-created` — 3 coupons present (or defer-to-p70)
    - `secrets-set` — missing secrets set or deferred
    - `vr-baselines-reviewed` — baselines acceptable
    - `ds-gates-fire-on-pr` — gates fired on synthetic PR, log written, PR closed
    - `defer-to-p70 <signal>` — explicitly defer one signal to Phase 70
    - `halt <reason>` — block close-out (e.g. VR baseline shows a regression)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write 69.7-SUMMARY.md</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-SUMMARY.md</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/ (all artifact files from 69.7-01..03 + Tasks 1-3 above)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template)
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-CONTEXT.md (phase boundary + deferred items)
  </read_first>
  <action>
    Write `69.7-SUMMARY.md` capturing the phase outcome. Required sections:

    1. **Header** — phase id, dates, autonomous: false, outcome (CODE-COMPLETE or PARTIAL-DEFERRED)
    2. **What shipped:**
       - 18 migrations applied to remote `ytnsipxxmzgaebkqmokp` (reference db-push.log)
       - 0 ERROR-level advisor findings (reference db-advisors.log)
       - 0 RPC-body lint errors (reference db-lint.log)
       - 10/10 Edge Fns deployed (reference fn-deploy.log, list each Fn)
       - 10/10 healthz responses ok (reference fn-healthz.log)
       - N pg_cron jobs registered (reference cron-jobs.log)
       - Vite production build clean (reference npm-build.log)
       - Vercel staging URL: <url from staging-url.txt>
       - VR baselines: N PNG files committed
       - DS-01/02/03 CI gates fired on synthetic PR (or deferred — reference ds-gate-verification.log)
    3. **Operator-gate dispositions:**
       - Stripe Tax: AUTO-CONFIRMED / OPERATOR-CONFIRMED / DEFERRED-TO-P70
       - Win-back coupons: same
       - Each MISSING vendor secret: SET / DEFERRED-TO-P70
       - VR baselines: REVIEWED-AND-ACCEPTED / FLAGGED-FOR-FIX
    4. **DEPLOY-0X requirement closure:**
       - DEPLOY-01: COMPLETE / PARTIAL (reason)
       - DEPLOY-02: same
       - DEPLOY-03: same
       - DEPLOY-04: same
       - DEPLOY-05: same
       - DEPLOY-06: same
    5. **Deferred to Phase 70:**
       - Any unapproved Task 3 signals
       - Newsletter cron go-live (already deferred per CONTEXT)
       - Production cutover
       - Operator-portal items
    6. **Carry-overs** (any unexpected items surfaced during execution that aren't in CONTEXT's deferred list)
    7. **Next step:** `/gsd-plan-phase 70` (or whatever the user's preferred entry to Phase 70 UAT is).
  </action>
  <verify>
    <automated>test -f /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-SUMMARY.md && grep -E "DEPLOY-01" /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY.md exists with all 7 sections populated.
    - Each DEPLOY-0X has a disposition (COMPLETE / PARTIAL).
    - Each Task 3 signal has a disposition.
  </acceptance_criteria>
  <done>Phase outcome captured in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 5: Flip ROADMAP + manually increment STATE.md counter</name>
  <files>
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (find Phase 69.7 entry around line 32 — `- [ ] **Phase 69.7:`)
    - .planning/STATE.md (current counters; expect `completed_phases: 12` based on STATE snapshot)
  </read_first>
  <action>
    **ROADMAP.md:**
    Find the line `- [ ] **Phase 69.7: Vercel + Supabase Build & Deploy Verification**` (around line 32). Replace `- [ ]` with `- [x]`. If the SUMMARY in Task 4 marked this phase PARTIAL-DEFERRED, leave the checkbox UNCHECKED and add a marginal note `(PARTIAL — see SUMMARY for carry-overs)` after the bold title.

    **STATE.md:** (MANUAL EDIT — DO NOT USE `state.complete-phase` SDK VERB per `reference_state_complete_phase_writes_wrong_counters`)
    1. Read current `completed_phases` value (expected 12).
    2. Increment by 1 → 13 (only if Task 4 SUMMARY marked phase COMPLETE; if PARTIAL, keep at 12 and add to "Blockers").
    3. Recompute `percent`: floor(completed_phases / total_phases * 100) = floor(13/20 * 100) = 65. NOTE: STATE shows `percent: 94` which doesn't match 12/20 = 60% — there's a pre-existing drift. Do NOT chase that; just monotonically increment from whatever current value is.

    Actually: simpler rule — do `git diff .planning/STATE.md` after edit and confirm `completed_phases` increased by 1 and `percent` increased monotonically (no exact value enforced).

    Update STATE.md "Current Position":
    - Phase: `70 (Consolidated UAT — v1.4 Launch Gate) — PENDING` (or next phase in queue)
    - Plan: `1 of N` (TBD)

    Update STATE.md `last_updated` to current ISO timestamp.

    Add a `### Phase 69.7 deliverables (this session, <date>)` section to STATE.md "Accumulated Context" with bullet roll-up: 18 migrations applied / 10 Fns deployed / N cron jobs / staging URL / VR baselines / DS gates verified / operator dispositions.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && (grep -E "Phase 69.7:.* — End-to-end" .planning/ROADMAP.md | grep -E "^- \[x\]") && grep -E "^  completed_phases:" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 69.7 line shows `- [x]` (or `- [ ]` + PARTIAL note).
    - STATE.md `completed_phases` incremented (or unchanged with reason logged in Blockers).
    - STATE.md `last_updated` reflects today's date.
    - STATE.md "Current Position" updated.
    - `git diff` shows clean, intended changes only.
  </acceptance_criteria>
  <done>ROADMAP + STATE reflect Phase 69.7 outcome.</done>
</task>

<task type="auto">
  <name>Task 6: Final commit closing Phase 69.7</name>
  <files>
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/stripe-tax-readback.json,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/stripe-coupons-readback.json,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/ds-gate-verification.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-SUMMARY.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <read_first>None — files already on disk.</read_first>
  <action>
    Assert PWD: `git rev-parse --show-toplevel` MUST end in `/leanshot`. Else `cd /Users/karstenhaldan/minisite/leanshot`.

    Stage all 6 listed files. Commit message:
    - If Task 4 SUMMARY marked COMPLETE: `chore(69.7): close phase 69.7 — deploy + verify complete; advance to Phase 70`
    - If PARTIAL: `chore(69.7): close phase 69.7 PARTIAL — N signals deferred to Phase 70`

    Run `git diff HEAD~1 --stat` after commit to verify all 6 files landed.

    Run `git log -1 --pretty=%H` and append the SHA to `69.7-SUMMARY.md` "Closing commit:" line (amend if needed; safer to just add as a final line — single trailing amend acceptable).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && git log -1 --pretty=%s | grep -E "chore\(69.7\):"</automated>
  </verify>
  <acceptance_criteria>
    - Single (or two amended) commit on main closing the phase.
    - Subject matches `chore(69.7):`.
    - All 6 files included in the diff.
    - PWD invariant honored throughout.
  </acceptance_criteria>
  <done>Phase 69.7 closed; orchestrator can advance to Phase 70 or report milestone status.</done>
</task>

</tasks>

<verification>
- 5 close-out signals each have a disposition (approved / deferred / halted).
- 2 Stripe read-back JSONs captured.
- DS-gate verification log captured (or signal deferred).
- 69.7-SUMMARY.md exists with all DEPLOY-0X dispositions.
- ROADMAP Phase 69.7 entry flipped (or marked PARTIAL).
- STATE.md counters updated manually (NOT via state.complete-phase SDK verb).
- Final close-out commit landed on main with PWD invariant.
</verification>

<success_criteria>
DEPLOY-06 closed (auto-confirmed + operator-confirmed + deferred subsets all dispositioned). Phase 69.7 in canonical close state. Phase 70 UAT can begin.
</success_criteria>

<output>
This plan's output IS `.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-SUMMARY.md` (created in Task 4, committed in Task 6).
</output>
