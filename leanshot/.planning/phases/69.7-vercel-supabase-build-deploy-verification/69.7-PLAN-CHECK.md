---
plan_check_phase: 69.7
plan_count: 4
verdict: FLAG
critical_findings: 0
warnings: 6
date: 2026-05-27
---

# Plan Check Report — Phase 69.7: Vercel + Supabase Build & Deploy Verification

## 1. Verdict

**FLAG** — No blockers found. 6 warnings require attention before execution, none of which prevent the phase from delivering its goal. The plans are structurally sound and correctly cover all 10 success criteria.

---

## 2. Coverage Matrix — 10 ROADMAP Success Criteria × Plan.Task

| # | Success Criterion | Plan | Task | Acceptance Line |
|---|-------------------|------|------|-----------------|
| 1 | `npx supabase db push --linked` succeeds for all 18 pending migrations | 69.7-01 | Task 3 | "db-push.log shows 'Finished' or equivalent; re-run migration list shows no pending" |
| 2 | `npx supabase db advisors` returns 0 entries | 69.7-01 | Task 4 | "ERROR-level count is 0 OR all surfaced findings have an operator-confirmed disposition" |
| 3 | `npx supabase db lint` cleared of RPC body errors | 69.7-01 | Task 5 | "ERROR-level count is 0 OR all surfaced findings have an operator-confirmed disposition" |
| 4 | `npm run build` in leanshot/ produces clean Vite production bundle | 69.7-03 | Task 1 | "leanshot/dist/ exists; exit 0 OR HALT surfaced with type error trace" |
| 5 | `vercel build --prod` or `vercel deploy --prebuilt` produces deployable artifact | 69.7-03 | Task 2 | "staging-url.txt contains https:// URL; vercel-deploy.log shows 'Deployed to'" |
| 6 | All 10 new Edge Fns deployed + `/healthz` returns `ok:true` for each | 69.7-02 | Tasks 2+3 | "fn-deploy.log: 10/10 Deployed Function; fn-healthz.log: 10 lines with ok:true" |
| 7 | 9 pg_cron jobs registered | 69.7-02 | Task 4 | "cron-jobs.log contains at least 9 data rows" |
| 8 | Phase 65 operator-gate actions complete (Stripe Tax + 3 coupons + 4 secrets) | 69.7-04 | Tasks 1+2+3 | "stripe-tax-readback.json + stripe-coupons-readback.json + Task 3 gate signals: stripe-tax-active / coupons-created / secrets-set" |
| 9 | Phase 69 VR baselines captured against staging | 69.7-03 | Task 4 | "vr-baseline-run.log shows Playwright exit 0 + ≥1 PNG baseline written" |
| 10 | DS-01/02/03 CI gates fire on the next PR | 69.7-04 | Task 3 (Signal E) + implicit Task 4 | "synthetic PR with intentional violation; ds-gate-verification.log written; at least one DS gate blocks" |

**All 10 success criteria are covered.**

---

## 3. Findings by Dimension

### Dim 1 — Goal Coverage: PASS

All 10 ROADMAP success criteria map to at least one task acceptance criterion. No orphans.

---

### Dim 2 — Requirement Coverage: PASS

| Requirement | Plan(s) | Status |
|-------------|---------|--------|
| DEPLOY-01 | 69.7-01 | Covered |
| DEPLOY-02 | 69.7-01 | Covered |
| DEPLOY-03 | 69.7-02 | Covered |
| DEPLOY-04 | 69.7-02 | Covered |
| DEPLOY-05 | 69.7-03 | Covered |
| DEPLOY-06 | 69.7-02 (partial) + 69.7-04 | Covered |

All 6 requirement IDs appear across the four plans' `requirements:` frontmatter fields.

---

### Dim 3 — Wave + depends_on Integrity: PASS with 1 WARNING

**Wave graph:**

- 69.7-01: wave 0, depends_on: [] ✅
- 69.7-02: wave 1, depends_on: ["69.7-01"] ✅
- 69.7-03: wave 1, depends_on: ["69.7-01"] ✅
- 69.7-04: wave 2, depends_on: ["69.7-02", "69.7-03"] ✅

No cycles. No forward references. No intra-wave dependency between 69.7-02 and 69.7-03.

**WARNING — W1: 69.7-02 and 69.7-03 share the artifacts directory**

Both Wave 1 plans write to `.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/`. If the executor dispatches them in parallel (per `feedback_intra_wave_dependency_split` pattern), there is a potential conflict on directory creation. In practice this is low-risk since each plan writes *distinct* artifact files with no overlap, and neither creates the same file. However, if the artifacts directory doesn't exist before dispatch, both plans may race on `mkdir`. Recommend each plan's first task include `mkdir -p` as an explicit guard.

```yaml
issue:
  plan: ["69.7-02", "69.7-03"]
  dimension: dependency_correctness
  severity: warning
  description: "Both Wave 1 plans create distinct files in the same artifacts/ directory. If run in parallel and the directory does not yet exist (created in Wave 0), both plans may race on mkdir -p."
  fix_hint: "Either ensure 69.7-01 Task 6 creates the artifacts directory as part of its commit, or add explicit 'mkdir -p artifacts/' as the first shell command in each Wave 1 plan's Task 1 action."
```

---

### Dim 4 — `<read_first>` + `<acceptance_criteria>` Presence: PASS with 1 WARNING

All `auto` tasks in all 4 plans have both `<read_first>` and `<acceptance_criteria>` elements.

**WARNING — W2: 69.7-04 Task 3 (`checkpoint:human-verify`) has no `<acceptance_criteria>` element**

The task type is `checkpoint:human-verify`, so formal `<acceptance_criteria>` is not required by spec. However the gate has a `<how-to-verify>` and `<resume-signal>` — the resume signal structure is well-formed. This is informational only; checkpoint tasks are exempt.

**WARNING — W3: 69.7-02 Task 6 `<read_first>` says "None"**

Task 6 stages 5 artifact files and commits them. The `<read_first>: None` is acceptable for a commit-only task, but the task reads `git log -1 --pretty=%s` in the `<verify>` block, which verifies the wrong commit if an unexpected commit lands between Task 5 and Task 6. Consider reading the commit SHA from Task 5 artifacts or just accepting this as an expected pattern.

```yaml
issue:
  plan: "69.7-02"
  dimension: task_completeness
  severity: warning
  description: "Task 6 <read_first> says 'None' but relies on git log -1 to verify commit subject. If any extraneous commit lands between Task 5 completion and Task 6 commit, the verify grep will pass against the wrong commit."
  fix_hint: "Low risk in inline dispatch. Acceptable as-is, or verify against git log --grep='69.7-02' instead of -1."
```

---

### Dim 5 — HUMAN Gate Structure: PASS with 1 WARNING

**69.7-01 — autonomous: false ✅**
Task 0 is `checkpoint:human-verify` with resume-signal `drift-resolved`. Explicit halt branch on `blocked` signal. ✅

**69.7-04 — autonomous: false ✅**
Task 3 is `checkpoint:human-verify` with 5 discrete signals:
- `stripe-tax-active` ✅
- `coupons-created` ✅
- `secrets-set` ✅
- `vr-baselines-reviewed` ✅
- `ds-gates-fire-on-pr` ✅

Task 1 (Stripe Tax API read-back) precedes Task 2 (coupons API read-back), both auto-confirm before Task 3 fires. ✅ Per `feedback_multi_signal_human_verify_checkpoint_pattern` this is correctly structured.

**69.7-02 and 69.7-03 — autonomous: true ✅**

**WARNING — W4: Task 3 resume-signal includes `defer-to-p70 <signal>` as a compound keyword**

Per `feedback_multi_signal_human_verify_checkpoint_pattern`, each signal should be independently approvable. The `defer-to-p70 <signal>` compound form is fine, but the orchestrator needs to correctly handle "defer" as a disposition rather than an approval. The plan correctly notes "Skip signals already AUTO-CONFIRMED" which is good. However if the orchestrator parses for exact keyword matches, `defer-to-p70 stripe-tax-active` (with the argument) may not match the expected pattern unless the orchestrator handles partial matches.

```yaml
issue:
  plan: "69.7-04"
  dimension: human_gate_structure
  severity: warning
  description: "resume-signal 'defer-to-p70 <signal>' is a compound keyword that accepts an argument. Standard GSD orchestrators parse for exact keyword match. If the operator types 'defer-to-p70 stripe-tax-active', the orchestrator may not recognize it."
  fix_hint: "Add 'defer-to-p70' as a standalone keyword in the resume-signal list (defers all remaining un-confirmed signals), OR ensure the orchestrator prompt handles argument-form keywords. Current structure is unambiguous to a human reader but may confuse automated parsing."
```

---

### Dim 6 — Anti-Patterns from Memory: PASS with 1 WARNING

**(a) `CREATE POLICY IF NOT EXISTS` — PASS ✅**
69.7-01 Task 4 explicitly states: "use bare `CREATE POLICY` (or `DROP POLICY IF EXISTS` + `CREATE POLICY`), NOT `CREATE POLICY IF NOT EXISTS`". This is locked in `<critical_invariants>` of 69.7-01.

**(b) Back-dated migration halt branch — PASS ✅**
69.7-01 Task 2 includes explicit HALT branch: "HALT immediately ... Do NOT auto-mv."

**(c) Inline dispatch only (no worktree) — PASS ✅**
All 4 plans' `<critical_invariants>` explicitly state "Inline dispatch only — no isolated worktree."

**(d) STATE.md manual edit only — PASS ✅**
69.7-04 `<critical_invariants>` explicitly: "DO NOT use `gsd-sdk query state.complete-phase` verb per `reference_state_complete_phase_writes_wrong_counters`; Edit STATE.md by hand."

**(e) Fn deploy BEFORE cron verification — PASS ✅**
69.7-02 Task 2 (deploy) precedes Task 4 (cron verify). `<critical_invariants>` states this ordering explicitly. ✅

**(f) Per-commit PWD assertion — PASS ✅**
All 4 plans include `<critical_invariants>` with: "Every commit MUST run from `/Users/karstenhaldan/minisite/leanshot`. Before each `git commit`, run `git rev-parse --show-toplevel` and assert it ends with `/leanshot`."

**WARNING — W5: 69.7-03 Task 1 action block runs from wrong path**

The action block says: `From /Users/karstenhaldan/minisite/leanshot/leanshot, run: 1. npm install ... 2. npm run build`.

The `<critical_invariants>` say commits must be from `/Users/karstenhaldan/minisite/leanshot` (the git root in the monorepo). The action correctly uses the `leanshot/leanshot` subdirectory for the npm commands (since `package.json` lives there), but per `reference_minisite_monorepo_layout`, the git root is `/Users/karstenhaldan/minisite`, and `leanshot/` is a subdirectory. The PWD invariant refers to `leanshot/` as the monorepo root for git operations. There is a risk the executor runs `git commit` from `leanshot/leanshot/` instead of `leanshot/`.

The `<verify>` block in Task 5 (commit task) correctly asserts `git rev-parse --show-toplevel` and has the right path, so this risk is mitigated at the commit step.

```yaml
issue:
  plan: "69.7-03"
  dimension: anti_pattern_check
  severity: warning
  description: "Task 1 action says 'From /Users/karstenhaldan/minisite/leanshot/leanshot' (the npm project dir), but git commits in Task 5 need to run from the monorepo checkout root /Users/karstenhaldan/minisite/leanshot. An executor reading Task 1's cwd for all subsequent tasks may commit from the wrong directory."
  fix_hint: "Add an explicit note in Task 1: 'npm commands run from leanshot/leanshot/; git operations in Task 5 MUST cd back to /Users/karstenhaldan/minisite/leanshot first.' Task 5 already has the PWD invariant check, so the actual commit is protected."
```

---

### Dim 7 — Concreteness of `<action>` Blocks: PASS

**Edge Fn deploy commands — PASS ✅**
69.7-02 Task 2 explicitly lists all 10 Fn names with the JWT mapping (--no-verify-jwt vs default) and the exact command template: `npx supabase functions deploy <name> --project-ref ytnsipxxmzgaebkqmokp [--no-verify-jwt]`.

**Secrets verification — PASS ✅**
69.7-02 Task 5 lists the exact 8 expected secret names and uses `npx supabase secrets list --project-ref ytnsipxxmzgaebkqmokp`.

**Playwright VR invocation — PASS ✅**
69.7-03 Task 4 specifies: `PLAYWRIGHT_BASE_URL="<staging-url>" npx playwright test --config playwright.config.vr.ts --update-snapshots`.

**Stripe read-back — PASS ✅**
69.7-04 Tasks 1+2 provide exact curl commands with `-u "${STRIPE_SECRET_KEY}:"` and `-H "Stripe-Version: 2024-06-20"`.

**Cron verification — MINOR WARNING embedded in W6 below**

**WARNING — W6: 69.7-02 Task 4 cron query has a fragile fallback path**

The action says to use `npx supabase db remote query ...` but notes "CLI may not have `remote query`; fall back to psql... OR use `supabase functions invoke` against a dedicated query Fn if available." The fallback chain is reasonable but the final fallback ("fall back to Supabase Dashboard → SQL Editor and ask the operator") would convert an `auto` task to `checkpoint:human-verify` mid-flight. The plan correctly notes this is conditional on CLI method failure, and the preceding artifact log requirement is a tolerated partial-verify.

```yaml
issue:
  plan: "69.7-02"
  dimension: action_concreteness
  severity: warning
  description: "Task 4 cron verification fallback: if supabase db remote query AND psql both fail, the task falls back to asking the operator (converting auto→human mid-flight). This is a risk if the CLI version on this machine doesn't support the remote query verb."
  fix_hint: "Pre-verify that 'npx supabase db remote query' works in this CLI version (run a simple SELECT 1 as a preflight). Alternatively, the CONTEXT.md already established that psql ambient auth works; prefer the psql fallback and document the exact connection string: 'psql $(npx supabase db remote query --project-ref ytnsipxxmzgaebkqmokp --shell)' or similar."
```

---

### Dim 8 — Verification Structure: PASS

All `auto` tasks in all 4 plans have `<verify>` blocks with `<automated>` CLI-runnable commands. No MISSING markers.

**69.7-04 `<human_verification>`** — The task type is `checkpoint:human-verify` rather than `auto`, so the `<verify>` block is replaced by `<how-to-verify>` and `<resume-signal>`. The 5 signals are all enumerated as discrete items. This is correctly structured.

---

## 4. Recommendations

### R1 (W1) — Artifacts directory creation race
**In 69.7-01 Task 6 action block**, add:
```bash
mkdir -p /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts
```
This ensures the directory exists before Wave 1 plans begin. The risk is low (both plans will write distinct files) but eliminates any race on first mkdir.

### R2 (W4) — defer-to-p70 compound keyword
In 69.7-04 Task 3 `<resume-signal>`, add a standalone form:
```
- `defer-all-to-p70` — defer all remaining un-confirmed signals to Phase 70 (bulk skip)
```
This gives the orchestrator a clean keyword without argument-parsing ambiguity.

### R3 (W5) — Task 1 cwd ambiguity in 69.7-03
Add a note to 69.7-03 Task 1 action: "NOTE: npm commands run from `leanshot/` project dir; git commits in Task 5 must `cd /Users/karstenhaldan/minisite/leanshot` first per PWD invariant." Task 5 already has this check, so no change needed there.

### R4 (W6) — Cron query preflight
Add a preflight at the top of 69.7-02 Task 4 action:
```bash
npx supabase --version | grep -E "^[1-9]" && npx supabase db remote query "SELECT 1" --project-ref ytnsipxxmzgaebkqmokp || echo "remote query unavailable — use psql fallback"
```
This surfacies the CLI capability issue before the actual query, giving the executor clean signal on which path to take.

---

## Summary

All 6 warnings are advisory. The phase plans are structurally complete, correctly wired, and address all 10 ROADMAP success criteria and all 6 requirement IDs. The critical anti-patterns from project memory (CREATE POLICY IF NOT EXISTS, back-dated migration auto-mv, STATE.md SDK verb, inline dispatch only, Fn-deploy-before-cron, PWD assertion) are all explicitly handled in plan invariants. The multi-signal HUMAN gate in 69.7-04 is correctly structured with discrete approvable signals. No blockers found.

The 4 plans can be executed in sequence: 69.7-01 (Wave 0, HUMAN gated) → 69.7-02 + 69.7-03 (Wave 1, parallel, autonomous) → 69.7-04 (Wave 2, HUMAN gated close-out).
