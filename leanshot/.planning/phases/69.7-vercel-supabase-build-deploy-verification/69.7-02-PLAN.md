---
phase: 69.7-vercel-supabase-build-deploy-verification
plan: 02
type: execute
wave: 1
depends_on: ["69.7-01"]
files_modified:
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-deploy.log
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-healthz.log
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/cron-jobs.log
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/secrets-list.log
autonomous: true
requirements: [DEPLOY-03, DEPLOY-04, DEPLOY-06]
must_haves:
  truths:
    - "All 10 new Edge Fns deployed to project ytnsipxxmzgaebkqmokp"
    - "GET /functions/v1/<name>/healthz returns ok:true for each of the 10 Fns"
    - "9 pg_cron jobs registered (visible in cron.job table)"
    - "Required vendor secrets present on Supabase Function Secrets (any missing are surfaced to close-out HUMAN gate)"
  artifacts:
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-deploy.log"
      provides: "stdout of all 10 supabase functions deploy commands"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-healthz.log"
      provides: "curl healthz output for all 10 Fns"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/cron-jobs.log"
      provides: "select jobname, schedule, command from cron.job output"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/secrets-list.log"
      provides: "supabase secrets list output (names only, masked values)"
  key_links:
    - from: "supabase functions deploy"
      to: "/functions/v1/<name>/healthz"
      via: "curl smoke test against deployed Fn"
      pattern: "\"ok\":true"
    - from: "Phase 65/66/67/68 cron-registration migrations"
      to: "cron.job table rows"
      via: "supabase db push (applied in Plan 69.7-01) + select"
      pattern: "cron\\.schedule"
---

<objective>
Deploy all 10 new Edge Fns from Phases 65-68 to remote Supabase project, smoke-test each `/healthz` endpoint, verify 9 pg_cron jobs are registered, and list current vendor Function Secrets (surface missing ones to close-out).

Purpose: Make the remote DB schema (applied in Plan 69.7-01) actually executable end-to-end. Per `feedback_fn_deploy_before_cron_db_push`, Fns deploy BEFORE cron jobs can fire. Cron migrations were included in the 18 pending applied in Plan 69.7-01, but their target Fns weren't deployed until now.
Output: 4 artifact logs documenting deploys + healthz + cron registry + secrets inventory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-CONTEXT.md
@.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-01-PLAN.md
</context>

<critical_invariants>
- **PWD INVARIANT** — All `git commit` calls MUST run from `/Users/karstenhaldan/minisite/leanshot`. Pre-commit guard: `git rev-parse --show-toplevel`.
- **Inline dispatch only** — no isolated worktree (ambient `supabase` CLI auth required).
- **Fn-deploy BEFORE cron verification** — even though cron registration migrations were applied in 69.7-01, do not verify cron jobs targeting non-existent Fns. Deploy Fns FIRST (Task 2), THEN query cron.job (Task 4).
- **No new feature code** — if an expected Fn directory is missing, HALT and surface to user. Do NOT scaffold.
- **JWT verification flags** — cron-callable Fns deploy with `--no-verify-jwt`; user-callable Fns deploy with default (JWT required). Mapping in Task 2.
</critical_invariants>

<tasks>

<task type="auto">
  <name>Task 1: Verify all 10 Edge Fn directories exist locally</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-inventory.log</files>
  <read_first>
    - leanshot/supabase/functions/ (list directory; expected 10 new Fn dirs from Phases 65-68)
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-CONTEXT.md (10 Fn names enumerated under "Reusable Assets")
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/leanshot`, verify each of these directories exists with both `index.ts` and a `_test.ts` (or equivalent test file):

    Expected 10 Fns:
    1. stripe-checkout
    2. stripe-webhook
    3. stripe-dunning-orchestrator
    4. request-refund
    5. lifecycle-trial-ending
    6. lifecycle-win-back
    7. nexus-monitor
    8. auth-rate-limit-check
    9. bs-status-poller
    10. demo-org-purge

    For each, check `leanshot/supabase/functions/<name>/index.ts` exists. Log each PASS/FAIL line to `fn-inventory.log`.

    If ANY directory is missing, HALT and surface to user. Phase 69.7 is a verification phase only — do NOT auto-scaffold missing Fns.

    Also verify each Fn's `index.ts` exposes a `/healthz` route (grep for the literal string `healthz`). If a Fn is missing healthz, log as FAIL-NO-HEALTHZ and surface.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && for fn in stripe-checkout stripe-webhook stripe-dunning-orchestrator request-refund lifecycle-trial-ending lifecycle-win-back nexus-monitor auth-rate-limit-check bs-status-poller demo-org-purge; do test -f supabase/functions/$fn/index.ts || (echo "MISSING: $fn" && exit 1); done && echo "all-10-present"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 Fn directories present with `index.ts`.
    - Each `index.ts` contains the substring `healthz`.
    - `fn-inventory.log` written with one line per Fn (status PASS).
  </acceptance_criteria>
  <done>10 Fns inventoried; ready for deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy all 10 Edge Fns via supabase functions deploy</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-deploy.log</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-inventory.log (Task 1)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/leanshot`, deploy each Fn sequentially. Tee each command's stdout + stderr to `artifacts/fn-deploy.log` (append mode).

    JWT verification mapping (per CONTEXT.md):
    - `--no-verify-jwt` (cron-callable, service-role auth): stripe-dunning-orchestrator, lifecycle-trial-ending, lifecycle-win-back, nexus-monitor, bs-status-poller, demo-org-purge, auth-rate-limit-check
    - Default JWT required (user-callable): stripe-checkout, request-refund
    - Special: stripe-webhook — Stripe signature verification is in-body; deploy with `--no-verify-jwt` (Stripe doesn't send a Supabase JWT).

    Command template:
    `npx supabase functions deploy <name> --project-ref ytnsipxxmzgaebkqmokp [--no-verify-jwt]`

    Loop through all 10. If any single deploy fails (non-zero exit), capture the error, continue with the remaining Fns (do not short-circuit), then HALT at end and surface the failure list. Partial-deploy state is recoverable; aborting mid-loop leaves more uncertainty.

    After the loop, count successful deploys. Append summary line: `Deployed N/10 Edge Fns to ytnsipxxmzgaebkqmokp`.
  </action>
  <verify>
    <automated>grep -c "Deployed Function" /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-deploy.log | grep -E "^([1-9]|10)$"</automated>
  </verify>
  <acceptance_criteria>
    - fn-deploy.log shows at least 10 "Deployed Function" success messages.
    - Summary line confirms 10/10.
    - If <10/10, plan halts and surfaces failure list with the affected Fn names.
  </acceptance_criteria>
  <done>10 Edge Fns deployed to remote; ready for healthz smoke tests.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-test /healthz for all 10 deployed Fns</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-healthz.log</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-deploy.log (confirm all deploys landed)
  </read_first>
  <action>
    For each of the 10 deployed Fns, curl the healthz endpoint:

    `curl -sS --max-time 10 https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/<name>/healthz`

    Note: some Fns may require an `Authorization: Bearer <SUPABASE_ANON_KEY>` header even for healthz depending on the JWT verification setting. Default to attempting WITHOUT auth header first; if the response is 401, retry with `Authorization: Bearer $(supabase projects api-keys --project-ref ytnsipxxmzgaebkqmokp --output json | jq -r '.[] | select(.name=="anon") | .api_key')` or equivalent.

    Tee each response + status line to `artifacts/fn-healthz.log`. Acceptance per Fn: response JSON contains `"ok":true`.

    If any Fn returns non-ok or non-200, capture and continue (don't short-circuit). After the loop, count `ok:true` responses. Append summary: `Healthz OK: N/10`.

    If N < 10, HALT and surface — operator needs to investigate (Fn may have boot-time error in env vars; refer to vendor secret task in Task 5).
  </action>
  <verify>
    <automated>grep -c '"ok":true' /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-healthz.log | grep -E "^([1-9]|10)$"</automated>
  </verify>
  <acceptance_criteria>
    - fn-healthz.log contains at least 10 lines with `"ok":true`.
    - Summary line `Healthz OK: 10/10` (or HALT surfaced with failure list).
  </acceptance_criteria>
  <done>All 10 Fns confirmed live on Supabase Edge runtime.</done>
</task>

<task type="auto">
  <name>Task 4: Verify 9 pg_cron jobs registered</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/cron-jobs.log</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-healthz.log (Fns confirmed live before checking cron targets them)
    - leanshot/supabase/migrations/ (find cron-registration migration files; expected at least 4 — Phase 65 batch + P66 + P67 + P68)
  </read_first>
  <action>
    The cron migrations were applied to remote in Plan 69.7-01. Now query the live `cron.job` table to confirm 9 jobs registered.

    From `/Users/karstenhaldan/minisite/leanshot`, run:
    `npx supabase db remote query "select jobid, jobname, schedule, command from cron.job order by jobid" --project-ref ytnsipxxmzgaebkqmokp` (or equivalent — CLI may not have `remote query`; fall back to `psql` against the pooler connection string, OR use `supabase functions invoke` against a dedicated query Fn if available).

    If neither CLI path works, fall back to the Supabase Dashboard → SQL Editor and ask the operator (this would convert the task to checkpoint:human-verify mid-flight — only if both CLI methods fail).

    Tee output to `artifacts/cron-jobs.log`. Expected 9 jobs (per CONTEXT.md):
    - 5 Phase 65 jobs (dunning batches, lifecycle scans, nexus monitor, etc.)
    - 1 Phase 66 (auth-attempts retention)
    - 1 Phase 66 (other — TBD from migration files)
    - 1 Phase 67 (bs-status-poller)
    - 1 Phase 68 (demo-org-purge)

    Acceptance: at least 9 rows in cron.job. If <9, list which expected job names are missing (grep cron-registration migrations for `cron.schedule(` calls to enumerate expected) and surface to user.
  </action>
  <verify>
    <automated>test -f /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/cron-jobs.log && grep -cE "^[[:space:]]*[0-9]+[[:space:]]*\|" /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/cron-jobs.log | grep -E "^([9-9]|[1-9][0-9])$"</automated>
  </verify>
  <acceptance_criteria>
    - cron-jobs.log exists and contains at least 9 data rows (header rows excluded).
    - Each expected cron job name (per migration grep) appears in the output OR is surfaced as missing.
  </acceptance_criteria>
  <done>9 pg_cron jobs confirmed registered on remote.</done>
</task>

<task type="auto">
  <name>Task 5: List current Supabase Function Secrets + flag missing</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/secrets-list.log</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-CONTEXT.md (vendor secret handling — do NOT block on operator-portal items)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/leanshot`, run:
    `npx supabase secrets list --project-ref ytnsipxxmzgaebkqmokp` and tee to `artifacts/secrets-list.log` (names only — Supabase masks values).

    Expected secrets to verify present:
    - STRIPE_COUPON_WB_3MO_50
    - STRIPE_COUPON_WB_6MO_30
    - STRIPE_COUPON_WB_LIFETIME_20
    - SLACK_GUARDRAIL_WEBHOOK_URL
    - NEWSLETTER_PHYSICAL_ADDRESS
    - BETTER_STACK_API_KEY
    - STRIPE_SECRET_KEY (already in place from earlier phases — confirm)
    - SUPABASE_SERVICE_ROLE_KEY (auto-set by Supabase — should always be present)

    For each expected secret, grep the log. Append a summary block:
    ```
    Secret inventory (vs Phase 69.7 expected set):
    - STRIPE_COUPON_WB_3MO_50: PRESENT | MISSING
    - STRIPE_COUPON_WB_6MO_30: PRESENT | MISSING
    ... etc
    ```

    Per CONTEXT.md "Vendor Secret Handling": do NOT block this plan if any are missing. Carry the missing list to Plan 69.7-04 close-out HUMAN gate (operator confirms via dashboard or sets via CLI).

    Note `VITE_CALENDLY_BOOK_DEMO_URL` is a Vercel/build-time env, NOT a Supabase secret — verify via `vercel env ls preview` in Plan 69.7-03 (or surface in close-out).
  </action>
  <verify>
    <automated>test -f /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/secrets-list.log && grep -c "Secret inventory" /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/secrets-list.log</automated>
  </verify>
  <acceptance_criteria>
    - secrets-list.log exists with both the raw `supabase secrets list` output and the appended "Secret inventory" summary block.
    - Each of the 8 expected secrets has a PRESENT or MISSING disposition.
  </acceptance_criteria>
  <done>Secret inventory captured; missing items deferred to Plan 69.7-04 close-out (NOT blocking here).</done>
</task>

<task type="auto">
  <name>Task 6: Commit Plan 69.7-02 artifacts</name>
  <files>
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-deploy.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-healthz.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/cron-jobs.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/secrets-list.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/fn-inventory.log
  </files>
  <read_first>None — artifact files already on disk.</read_first>
  <action>
    Assert PWD: `git rev-parse --show-toplevel` MUST end in `/leanshot`. Else `cd /Users/karstenhaldan/minisite/leanshot`.

    Stage all 5 artifact files. Commit message: `chore(69.7-02): deploy 10 Edge Fns + verify cron + inventory secrets`.

    Note: if 69.7-03 ships in parallel (Wave 1 sibling), avoid touching the SUMMARY or STATE.md here — sibling will conflict. Plan 69.7-04 owns STATE.md flip.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && git log -1 --pretty=%s | grep -E "69.7-02"</automated>
  </verify>
  <acceptance_criteria>
    - Single commit on main with subject matching `chore(69.7-02)`.
    - All 5 artifact files included.
    - PWD invariant honored.
  </acceptance_criteria>
  <done>Plan 69.7-02 artifacts committed; 69.7-04 close-out can proceed once 69.7-03 also merges.</done>
</task>

</tasks>

<verification>
- 10/10 Edge Fns deployed (fn-deploy.log).
- 10/10 healthz responses with `ok:true` (fn-healthz.log).
- ≥9 cron jobs in cron.job table (cron-jobs.log).
- Secret inventory complete with PRESENT/MISSING dispositions (secrets-list.log).
- Single clean commit on main.
</verification>

<success_criteria>
DEPLOY-03 + DEPLOY-04 satisfied; DEPLOY-06 partially satisfied (secrets portion; Stripe-side and CI-gate portions land in Plan 69.7-04).
</success_criteria>

<output>
Create `.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-02-SUMMARY.md` when done.
</output>
