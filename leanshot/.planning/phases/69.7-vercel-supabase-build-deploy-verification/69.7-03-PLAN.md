---
phase: 69.7-vercel-supabase-build-deploy-verification
plan: 03
type: execute
wave: 1
depends_on: ["69.7-01"]
files_modified:
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/npm-build.log
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/vercel-deploy.log
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-smoke.log
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/vr-baseline-run.log
  - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-url.txt
  - leanshot/test-results/vr-snapshots/
autonomous: true
requirements: [DEPLOY-05]
must_haves:
  truths:
    - "npm run build in leanshot/ produces clean dist/ (no Vite errors, no missing-env warnings)"
    - "vercel deploy --prebuilt against staging returns a working preview URL"
    - "Staging smoke curls succeed (200) on /, /knowledge, /sitemap.xml, /robots.txt"
    - "Phase 69 VR baselines captured against the staging URL and committed"
  artifacts:
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/npm-build.log"
      provides: "stdout/stderr of npm run build"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/vercel-deploy.log"
      provides: "stdout of vercel build + deploy"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-url.txt"
      provides: "the staging preview URL (single line)"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-smoke.log"
      provides: "curl status codes for 4 critical routes"
    - path: ".planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/vr-baseline-run.log"
      provides: "Playwright VR --update-snapshots stdout"
    - path: "leanshot/test-results/vr-snapshots/"
      provides: "captured VR baseline PNGs (or wherever P69.4 placed them)"
  key_links:
    - from: "npm run build"
      to: "vercel deploy --prebuilt"
      via: "Vite dist/ output consumed by Vercel CLI"
      pattern: "vercel build"
    - from: "staging URL"
      to: "Playwright VR baselines"
      via: "PLAYWRIGHT_BASE_URL env var"
      pattern: "PLAYWRIGHT_BASE_URL="
---

<objective>
Run `npm install` + `npm run build` in `leanshot/`, deploy to Vercel staging via `vercel deploy --prebuilt`, smoke-curl critical routes, and capture Phase 69 visual-regression baselines against the staging URL.

Purpose: Validate the Vite production bundle builds clean after Phase 65-69 merges + confirm the staging environment is live for Phase 70 UAT walkthroughs + establish the VR baseline set so Phase 69 CI gates (DS-04..) have a reference to diff against.
Output: 4 artifact logs + staging URL persisted to file + VR baselines committed to repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-CONTEXT.md
</context>

<critical_invariants>
- **PWD INVARIANT** — All `git commit` calls MUST run from `/Users/karstenhaldan/minisite/leanshot`. Pre-commit guard: `git rev-parse --show-toplevel`.
- **Inline dispatch only** — no isolated worktree (ambient `vercel` CLI auth required).
- **Staging only** — `vercel deploy --prebuilt` (NO `--prod` flag). Production cutover is Phase 70.
- **No new feature code** — if build fails on missing component/util, HALT and surface. Phase 69.7 is verification only.
- **VR baselines are commit-tracked** — Playwright `--update-snapshots` writes PNGs that MUST be committed alongside the artifact logs.
- **npm install drift** — per `reference_npm_install_worktree_main_drift`, after Phase 65-69 merges to main, `npm install` may be required even though no package.json change shipped (lockfile or worktree node_modules drift).
</critical_invariants>

<tasks>

<task type="auto">
  <name>Task 1: npm install + npm run build in leanshot/</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/npm-build.log</files>
  <read_first>
    - leanshot/package.json (find `build` script — expected `tsc -b && vite build`)
    - leanshot/vite.config.ts (any new plugins from Phases 65-69)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/leanshot/leanshot`, run:
    1. `npm install` — capture stdout/stderr (tee to npm-build.log, append mode).
    2. `npm run build` — capture stdout/stderr (tee to npm-build.log, append).

    Expected outcome: exit 0 on both. Vite output `dist/` created. No `[vite]: missing` warnings.

    If `tsc -b` surfaces type errors, capture them, HALT and surface to user. Do NOT auto-fix — Phase 69.7 is verification only; type errors mean Phase 65-69 shipped broken code that needs targeted fix in a follow-up plan.

    If `vite build` surfaces missing-env warnings (e.g. `import.meta.env.VITE_CALENDLY_BOOK_DEMO_URL is undefined`), note them but do NOT halt — Vite missing-env is build-time WARN not ERROR. Surface the list to user in Plan 69.7-04 close-out (operator may need to set Vercel env vars before promotion to prod).

    Append summary line: `Build OK: dist/ created at <path>` or `Build FAIL: <error>`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot/leanshot && test -d dist && grep -E "Build OK|built in" /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/npm-build.log</automated>
  </verify>
  <acceptance_criteria>
    - npm-build.log exists with both `npm install` and `npm run build` output.
    - `leanshot/dist/` directory exists post-build.
    - Build exited 0 (or HALT surfaced with type error trace).
  </acceptance_criteria>
  <done>Vite production bundle produced; ready for Vercel.</done>
</task>

<task type="auto">
  <name>Task 2: Vercel build + deploy to staging preview</name>
  <files>
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/vercel-deploy.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-url.txt
  </files>
  <read_first>
    - leanshot/.vercel/project.json (confirm Vercel project linkage exists; if missing, HALT and surface — operator must `vercel link`)
    - leanshot/vercel.json (deployment config; check `framework` field)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/leanshot/leanshot`:

    1. `vercel pull --yes --environment=preview` — syncs project config + env vars for preview env. Tee to vercel-deploy.log.
    2. `vercel build` (no `--prod` flag — preview build). Tee to vercel-deploy.log (append).
    3. `vercel deploy --prebuilt --yes` (NOT `--prod`). Capture the preview URL printed at the end of the command's output.

    Persist the staging URL to `artifacts/staging-url.txt` (single line, no trailing newline issues). Example URL format: `https://leanshot-abc123.vercel.app`.

    If any step fails with auth error, HALT — operator needs to `vercel login` (do NOT prompt the auth flow inside an autonomous task).

    If any step surfaces missing-env warnings for required runtime env vars (e.g. `VITE_CALENDLY_BOOK_DEMO_URL`), capture and continue — these are Phase 70 operator-portal items per `project_v1_4_phase_restructure`. Surface to close-out HUMAN gate.
  </action>
  <verify>
    <automated>test -f /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-url.txt && grep -E "^https://" /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-url.txt</automated>
  </verify>
  <acceptance_criteria>
    - staging-url.txt contains a URL starting with `https://` (Vercel preview pattern).
    - vercel-deploy.log shows `Deployed to` or equivalent success indicator.
    - Exit code of `vercel deploy --prebuilt` was 0.
  </acceptance_criteria>
  <done>Staging preview live at captured URL; ready for smoke + VR.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-curl 4 critical routes on staging URL</name>
  <files>.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-smoke.log</files>
  <read_first>
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-url.txt (Task 2)
  </read_first>
  <action>
    Read the staging URL from `artifacts/staging-url.txt` (strip trailing whitespace).

    For each of the following paths, run `curl -sS -o /dev/null -w "%{http_code} %{url_effective}\n" --max-time 15 "<URL><path>"` and tee to `artifacts/staging-smoke.log`:

    1. `/` (home / landing)
    2. `/knowledge` (Phase 60 public hub — should return 200)
    3. `/sitemap.xml` (Phase 60-13 SEO artifact)
    4. `/robots.txt` (static)

    Expected: all 4 return HTTP 200.

    If any return 4xx/5xx, capture and surface — but do NOT halt this task. Smoke failures may indicate a missing-env at runtime (e.g. SPA without proper rewrites for /knowledge route) which is recoverable via vercel.json rewrites in a follow-up. Carry to Plan 69.7-04 close-out.

    Append summary: `Smoke: 200/200/200/200` or list non-200 routes.
  </action>
  <verify>
    <automated>grep -c "^200 " /Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-smoke.log | grep -E "^[4-9]|^[1-9][0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - staging-smoke.log contains at least 4 lines starting with `200 `.
    - Summary line records pass/fail per route.
  </acceptance_criteria>
  <done>Critical routes confirmed live on staging.</done>
</task>

<task type="auto">
  <name>Task 4: Capture Phase 69 VR baselines against staging URL</name>
  <files>
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/vr-baseline-run.log,
    leanshot/test-results/vr-snapshots/
  </files>
  <read_first>
    - leanshot/playwright.config.vr.ts (Phase 69.4 VR config — confirm baseURL is overridable via env)
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-url.txt (Task 2)
    - .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-smoke.log (confirm routes returning 200 before snapshotting)
  </read_first>
  <action>
    Read staging URL from `artifacts/staging-url.txt`.

    Pre-check (per `feedback_vr_snapshot_plan_route_existence_check`): grep `leanshot/playwright.config.vr.ts` or any companion test spec under `leanshot/tests/vr/` to enumerate the routes the VR suite navigates. Confirm each route exists on staging (returned 200 in Task 3) OR is one of the 4 critical routes already smoked. If the VR suite covers a route that returned non-200 in Task 3, HALT and surface — VR baseline capture against a broken route would commit a broken baseline.

    From `/Users/karstenhaldan/minisite/leanshot/leanshot`, run:
    `PLAYWRIGHT_BASE_URL="<staging-url>" npx playwright test --config playwright.config.vr.ts --update-snapshots`

    Tee stdout/stderr to `artifacts/vr-baseline-run.log`.

    Acceptance: exit 0 (all snapshots captured fresh). The first `--update-snapshots` run typically passes 100% since it writes baselines rather than comparing.

    If Playwright fails to install browsers (cached install missing), prepend `npx playwright install chromium` then re-run.

    After completion, list the snapshot files written: `find leanshot/test-results/vr-snapshots/ -name '*.png' | wc -l` and append to log.
  </action>
  <verify>
    <automated>test -d /Users/karstenhaldan/minisite/leanshot/leanshot/test-results/vr-snapshots && find /Users/karstenhaldan/minisite/leanshot/leanshot/test-results/vr-snapshots -name '*.png' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `leanshot/test-results/vr-snapshots/` directory exists with at least one `.png` baseline.
    - vr-baseline-run.log shows Playwright exit 0.
    - Snapshot count logged in the artifact.
  </acceptance_criteria>
  <done>VR baselines captured against staging URL; ready to commit.</done>
</task>

<task type="auto">
  <name>Task 5: Commit Plan 69.7-03 artifacts + VR baselines</name>
  <files>
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/npm-build.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/vercel-deploy.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-url.txt,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/staging-smoke.log,
    .planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/vr-baseline-run.log,
    leanshot/test-results/vr-snapshots/
  </files>
  <read_first>
    - leanshot/.gitignore (confirm `test-results/vr-snapshots/` is NOT git-ignored; if it is, surface — baselines must be commit-tracked)
  </read_first>
  <action>
    Assert PWD: `git rev-parse --show-toplevel` MUST end in `/leanshot`. Else `cd /Users/karstenhaldan/minisite/leanshot`.

    Verify `leanshot/test-results/vr-snapshots/` is git-trackable. If `.gitignore` excludes `test-results/`, check whether there's an exception like `!test-results/vr-snapshots/`. If not, surface to user — baselines need to land in repo OR be relocated to a tracked path (Phase 69.4 plan should have set this; this plan does not move them).

    Stage:
    - All 5 artifact files in `.planning/phases/69.7-vercel-supabase-build-deploy-verification/artifacts/`
    - All PNG files under `leanshot/test-results/vr-snapshots/` (or wherever Phase 69.4 placed them)

    Commit message: `chore(69.7-03): vercel build + staging deploy + VR baselines`.

    Note: this commit may include many PNG files — confirm `git status` looks clean (only intended files) before committing. Per `feedback_batched_edits_verify_file_count`, post-commit `git show --stat` to confirm all files landed.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && git log -1 --pretty=%s | grep -E "69.7-03"</automated>
  </verify>
  <acceptance_criteria>
    - Single commit on main with subject matching `chore(69.7-03)`.
    - Commit includes 5 artifact logs + ≥1 VR baseline PNG.
    - PWD invariant honored.
  </acceptance_criteria>
  <done>Plan 69.7-03 work committed; staging URL persisted; baselines tracked.</done>
</task>

</tasks>

<verification>
- npm-build.log shows clean build, dist/ created.
- staging-url.txt contains a valid Vercel preview URL.
- staging-smoke.log shows 4/4 routes returning 200.
- vr-baseline-run.log shows Playwright exit 0 + ≥1 PNG baseline written.
- Single clean commit on main with PWD invariant honored.
</verification>

<success_criteria>
DEPLOY-05 acceptance criteria satisfied; staging URL available for Plan 69.7-04 operator-gate verification + Phase 70 UAT.
</success_criteria>

<output>
Create `.planning/phases/69.7-vercel-supabase-build-deploy-verification/69.7-03-SUMMARY.md` when done.
</output>
