# Phase 69.7: Vercel + Supabase Build & Deploy Verification - Context

**Gathered:** 2026-05-27
**Status:** Ready for planning
**Mode:** Infrastructure phase — discuss skipped (verification/deploy phase, all success criteria are technical CLI commands)

<domain>
## Phase Boundary

End-to-end build + deploy validation BEFORE Phase 70 UAT.

Inserted mid-run per user direction 2026-05-27: *"ensure that both vercel and supabase are building correctly and running that way they should, before we go to the UAT in phase 70"*.

This phase resolves all remote-deploy items deferred from Phases 65–69:
- 18 pending Supabase migrations (10 P65 + 2 P66 + 3 P66.5 + 3 P68)
- 10 new Edge Fns to deploy (stripe-checkout, stripe-webhook, stripe-dunning-orchestrator, request-refund, lifecycle-trial-ending, lifecycle-win-back, nexus-monitor, auth-rate-limit-check, bs-status-poller, demo-org-purge)
- 9 pg_cron jobs to register (P65 ×5, P66 ×1, P67 ×1 bs-status-poller, P68 ×1 demo-org-purge, P66 ×1 auth-attempts retention)
- 11 ERROR-level Supabase advisor findings to clear (code already shipped in P66.5)
- Phase 65 operator-gate actions: Stripe Tax enabled, 3 Win-back Coupons created, 4 vendor secrets verified
- Phase 69 VR baselines captured against staging
- DS-01/02/03 CI gates fired on first PR

</domain>

<decisions>
## Implementation Decisions

### Phase Type
- **Infrastructure / verification phase** — no new product code, just deploy + verify what Phases 65-69 already shipped code-complete.
- All implementation choices are at Claude's discretion (per `feedback_compress_planner_skip_research_patterns` for prescriptive late-batch phases).

### Pre-execution Operator Gate
- **Phase 65 `org_subscriptions` schema-tracking drift MUST be resolved by operator via psql BEFORE this phase can execute** (per STATE.md "Operator next" line + ROADMAP "Depends on").
- Plan MUST include a Task 0 that verifies drift is resolved (e.g. `supabase db push --linked --dry-run` returns clean, OR `supabase migration list --linked` shows all 18 pending migrations queued in order).
- If drift not resolved at execute time → halt + surface to user (do NOT attempt destructive drift recovery autonomously).

### Plan Decomposition
Per ROADMAP `Plans` hint: ~3-4 plans. Recommended split:
1. **69.7-01 — DB push + advisor/lint sweep:** resolve drift (verify only — operator owns the psql side), `db push`, `db advisors`, `db lint`. If advisor/lint still flag, draft retroactive migration or document carry-over.
2. **69.7-02 — Edge Fn deploys + cron + secrets:** deploy all 10 Fns via `supabase functions deploy`, register 9 pg_cron jobs (idempotent SQL), set/verify 4-5 vendor secrets via `supabase secrets set`.
3. **69.7-03 — Vercel build + VR baselines:** `npm run build` in leanshot/, `vercel build --prod`, `vercel deploy --prebuilt` to staging, run Playwright VR suite `--update-snapshots` against staging URL, commit baselines.
4. **69.7-04 — Close-out:** confirm `/healthz` ok for all 10 Fns, confirm cron schedule, confirm DS-01/02/03 CI gates fire on a synthetic PR, write SUMMARY + flip ROADMAP, defer any remaining items to Phase 70 UAT.

### Vendor Secret Handling
- Follow `feedback_consolidated_vendor_setup_phase_pattern` — set what we can via CLI from local env, surface anything operator-only (Stripe Dashboard, Calendly, Better Stack portal) in the close-out plan as a HUMAN gate. Do NOT block on portal-required items mid-execution.
- Stripe Tax + 3 Win-back Coupons are **operator-only** (Stripe Dashboard) — confirm via Stripe API read-back, not portal automation.

### Deployment Target
- **Staging only** in this phase. Production promotion is Phase 70 UAT's call.
- `vercel deploy --prebuilt` (not `--prod`) — produces preview URL for VR baseline capture without touching production domain.

### Failure Handling
- Per `feedback_partial_phase_classification_before_execute` — distinguish vendor-deferred vs genuinely-failed at close-out.
- Per `feedback_multi_signal_human_verify_checkpoint_pattern` — close-out HUMAN gate ships as N discrete resume signals (operator action items roll up to Phase 70).
- Per `feedback_fn_deploy_before_cron_db_push` — Fns deploy BEFORE pg_cron jobs that target them (covered by plan ordering 69.7-02 → 69.7-04).
- Per `reference_supabase_back_dated_migration_blocks_push` — if any migration timestamp is older than remote's last applied, surface to operator + stage temp-move fix (do NOT auto-mv migrations).

### CLI Authentication
- Use ambient `supabase` + `vercel` + `gh` CLI auth (per `feedback_cli_over_paste_back`). Inline-dispatch (not background Agent) per `reference_leanshot_supabase_project_ref` — ambient auth doesn't transfer to spawned Agents.
- Per `feedback_phase_close_out_supabase_gotchas` (3) — run close-out plans INLINE in main worktree, not isolated worktree.

### What this phase explicitly DOES NOT do
- ❌ Full Phase 70 UAT walkthroughs (browser-driven, multi-device, multi-region).
- ❌ Production traffic cutover.
- ❌ Newsletter cron go-live (still gated on NEWSLETTER_PHYSICAL_ADDRESS secret).
- ❌ Operator-portal-only actions beyond confirming what's been done (Stripe Tax, Coupons, App Store accounts, etc. — those are Phase 70).
- ❌ Any new feature code (this is a verification phase).

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- **`leanshot/scripts/audit-*.ts`** — Phase 69 CI gate scripts (DS-01..09). Plan 69.7-04 invokes these to verify CI gate firing.
- **`leanshot/playwright.config.vr.ts`** — VR baseline config from Phase 69.4. Plan 69.7-03 invokes with `--update-snapshots`.
- **`leanshot/supabase/migrations/*`** — 18 pending migration files (Phase 65 ×10, P66 ×2, P66.5 ×3, P68 ×3). All already on disk, just unpushed.
- **`leanshot/supabase/functions/{stripe-checkout,…}/`** — 10 new Edge Fn directories. Each has a `_test.ts` + `index.ts` and `/healthz` route (per Phase 65/66/67/68 conventions).
- **`leanshot/supabase/migrations/*pg_cron*.sql`** — pg_cron registration SQL (9 jobs). Phase 65/66/67/68 each have at least one cron migration.

### Established Patterns
- **Edge Fn deploy:** `supabase functions deploy <name> --project-ref ytnsipxxmzgaebkqmokp --no-verify-jwt` (for the cron-callable ones) or with JWT for the user-callable ones.
- **Healthz pattern:** all Phase 65+ Fns expose `GET /functions/v1/<name>/healthz` returning `{ok:true, version, deps_ok}`. Used by Better Stack monitor in Phase 67.
- **Cron registration:** idempotent SQL using `cron.schedule()` with `CASE WHEN EXISTS …` guard. All shipped in Phase 65/66/67/68 close-out plans but unpushed.
- **VR baseline:** Playwright `--update-snapshots` writes to `leanshot/test-results/vr-snapshots/`. Commit baselines to repo for CI diff.
- **Vercel deploy:** ambient `vercel` CLI auth, `vercel deploy --prebuilt --prod=false` for staging preview.

### Integration Points
- Supabase project ref: `ytnsipxxmzgaebkqmokp` (per `reference_leanshot_supabase_project_ref`).
- Vercel project: linked via `.vercel/project.json` (leanshot/).
- Slack guardrail webhook: env-var fast-path (per `feedback_vault_to_env_var_fast_path_pattern`).
- Better Stack monitors: created in Phase 67 — `/healthz` endpoints feed status page.

</code_context>

<specifics>
## Specific Ideas

- User wants this to be **the final verification gate before Phase 70 UAT** — focus on "does it build, does it deploy, do the smoke tests pass" rather than feature work.
- Per `project_v1_4_phase_restructure` — Phase 70 will own ALL operator-portal registrations (Apple Dev, Play, AdMob, AdSense, DMCA, Calendly, Better Stack). Phase 69.7 only sets what CLI can set + confirms what Phase 65/66/68 needed.
- Per memory hint, prefer **direct planner spawn** (skip research + patterns) since this phase is highly prescriptive (10 success criteria, all CLI commands).

</specifics>

<deferred>
## Deferred Ideas

- **Production cutover / DNS / traffic ramp** → Phase 70 UAT.
- **App Store / Play Store / AdMob / AdSense account registrations** → Phase 70 (operator portal).
- **Newsletter cron go-live** → Phase 70 (requires `NEWSLETTER_PHYSICAL_ADDRESS` secret operator must set).
- **Stripe production webhook signing secret rotation** → Phase 70 (production endpoint).
- **Better Stack status page public URL announce** → Phase 70.

</deferred>
