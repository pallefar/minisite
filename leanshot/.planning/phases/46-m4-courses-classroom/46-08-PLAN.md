---
phase: 46
plan: 08
type: execute
wave: 2
depends_on: [46-04, 46-05, 46-06, 46-07]
files_modified:
  - src/types/index.ts
  - src/lib/store.ts
  - src/lib/course/course-storage.ts
  - src/lib/course/course-progress.ts
  - src/lib/course/course-progress.test.ts
  - src/components/course/ClassroomTabShell.tsx
  - src/components/course/CourseListView.tsx
  - src/components/course/CourseDetailView.tsx
  - src/components/course/CourseSidebar.tsx
  - src/components/course/LessonPlayerView.tsx
  - src/components/course/LessonResourceList.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/MobileNav.tsx
  - src/App.tsx
autonomous: true
requirements: [COURSE-01, COURSE-02, COURSE-03, COURSE-05, COURSE-06]
tags: [courses, consumer-ui, classroom, mux-player, lazy, bundle-budget]
threat_refs: [T-46-01, T-46-02, T-46-05, T-46-06, T-46-08]

must_haves:
  truths:
    - "TabId union in src/types/index.ts includes 'classroom' (NEW TabId — EXCEPTION to no-new-TabId rule per CONTEXT)"
    - "Sidebar.tsx + MobileNav.tsx render a Classroom nav entry that sets currentTab='classroom'"
    - "ClassroomTabShell renders CourseListView when no activeLessonId; LessonPlayerView when activeLessonId set"
    - "LessonPlayerView fetches signed playback+thumbnail tokens via mux-sign-playback Edge Fn"
    - "Mux Player imported via `@mux/mux-player-react/lazy` (NOT bare entry) to defer heavy player bytes"
    - "onTimeUpdate accumulates max_position_reached_seconds client-side and writes via debounced 15s RPC (NO video.view webhook expected)"
    - "Tab-close + visibility-hidden triggers navigator.sendBeacon to lesson-progress-beacon Edge Fn with text/plain JSON"
    - "Mark Complete button enabled when max_position_reached_seconds/duration_seconds >= 0.95 (client-side gate matches server-side complete_lesson re-check)"
    - "On course 100% completion, UI invokes generate-course-certificate and surfaces download_url"
    - "Resource downloads call isResourceAllowed(tier) before showing download button; gated path 403 surfaces upgrade CTA"
    - "Lesson content_md rendered through src/lib/course/dompurify-config.ts (reused from Phase 44; NO new policy)"
    - "course-player chunk stays ≤30 kB gz (verified by scripts/assert-bundle-budget.sh)"
  artifacts:
    - path: "src/types/index.ts"
      provides: "TabId union widened to include 'classroom'"
      contains: "'classroom'"
    - path: "src/lib/store.ts"
      provides: "activeCourseId + activeLessonId + setActiveCourse action"
      contains: "activeCourseId"
    - path: "src/lib/course/course-progress.ts"
      provides: "debounced sync helper + sendBeacon helper + isLessonComplete()"
      exports: ["createProgressSyncer", "sendProgressBeacon", "isLessonComplete"]
    - path: "src/lib/course/course-storage.ts"
      provides: "COURSE_RESOURCES_BUCKET constants + signed-URL helper"
      exports: ["COURSE_RESOURCES_BUCKET", "downloadResourceSignedUrl"]
    - path: "src/components/course/ClassroomTabShell.tsx"
      provides: "Top-level shell with Suspense + sub-view dispatch"
      min_lines: 40
    - path: "src/components/course/LessonPlayerView.tsx"
      provides: "Mux Player + signed tokens + progress sync + Mark Complete CTA"
      min_lines: 120
  key_links:
    - from: "ClassroomTabShell"
      to: "CourseListView | LessonPlayerView"
      via: "Zustand activeLessonId branch"
      pattern: "activeLessonId"
    - from: "LessonPlayerView"
      to: "@mux/mux-player-react/lazy"
      via: "lazy import — defers player bytes"
      pattern: "@mux/mux-player-react/lazy"
    - from: "LessonPlayerView"
      to: "mux-sign-playback Edge Fn"
      via: "supabase.functions.invoke('mux-sign-playback', { body: { lesson_id }})"
      pattern: "mux-sign-playback"
    - from: "LessonPlayerView onTimeUpdate"
      to: "navigator.sendBeacon → lesson-progress-beacon Edge Fn"
      via: "tab-close handler with text/plain JSON body"
      pattern: "navigator\\.sendBeacon"
---

<objective>
Ship the consumer-side Classroom surface: a new TabId `'classroom'` (EXCEPTION to the no-new-TabId rule, justified by CONTEXT — courses are a distinct content surface, not a feed sub-view), Sidebar + MobileNav entries, a `ClassroomTabShell` mirroring `CommunityTabShell`, and the three primary views (CourseListView, CourseDetailView, LessonPlayerView) plus sidebar + resource list. The lesson player wires Mux Player's `/lazy` entry to the `mux-sign-playback` Edge Fn for signed tokens, implements client-side `onTimeUpdate` anti-skip accumulation (max_position_reached_seconds), 15-second debounced UPSERTs via the `update_lesson_position` RPC, and a tab-close `navigator.sendBeacon` to `lesson-progress-beacon`. The Mark Complete CTA enables at ≥95% and invokes `complete_lesson` RPC; course 100% triggers `generate-course-certificate`.

Purpose: This plan is the user-visible delivery for COURSE-01, COURSE-02, COURSE-03, COURSE-05 (consumer landing read), COURSE-06. The course-player chunk MUST stay ≤30 kB gz (ROADMAP success criterion #2) — achievable via `@mux/mux-player-react/lazy` + path-isolated chunk routing already in `vite.config.ts`.

Output: 11 new files + 3 modified files. Bundle budget verified at end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-m4-courses-classroom/46-CONTEXT.md
@.planning/phases/46-m4-courses-classroom/46-RESEARCH.md
@.planning/phases/46-m4-courses-classroom/46-PATTERNS.md
@.planning/phases/46-m4-courses-classroom/46-01-PLAN.md
@.planning/phases/46-m4-courses-classroom/46-03-PLAN.md
@.planning/phases/46-m4-courses-classroom/46-04-PLAN.md
@.planning/phases/46-m4-courses-classroom/46-06-PLAN.md
@.planning/phases/46-m4-courses-classroom/46-07-PLAN.md

# Analog files (READ — mirror exactly where indicated):
@src/components/community/CommunityTabShell.tsx
@src/components/community/media/CommunityVideoPlayer.tsx
@src/components/community/media/CommunityMediaUploader.tsx
@src/lib/community/community-storage.ts
@src/lib/community/tier-gate.ts
@src/components/layout/Sidebar.tsx
@src/components/layout/MobileNav.tsx
@src/types/index.ts
@src/lib/store.ts
@src/App.tsx
@vite.config.ts
@scripts/assert-bundle-budget.sh

# Plan 46-03 contracts (read for type imports):
@src/lib/course/course-types.ts
@src/lib/course/dompurify-config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Foundation — TabId, store fields, course-progress + course-storage utilities, nav entries</name>
  <read_first>
    - src/types/index.ts (find TabId union; widen by adding 'classroom')
    - src/lib/store.ts (find activeCommunitySpaceId pattern in AppState/Actions/initial state/actions; mirror for activeCourseId + activeLessonId + setActiveCourse)
    - src/components/layout/Sidebar.tsx (find nav entries; add Classroom entry mirroring Community)
    - src/components/layout/MobileNav.tsx (same)
    - src/lib/community/community-storage.ts (analog for course-storage.ts: bucket constant + signed-URL helper)
    - .planning/phases/46-m4-courses-classroom/46-PATTERNS.md §ClassroomTabShell store additions + TabId
  </read_first>
  <files>
    src/types/index.ts
    src/lib/store.ts
    src/lib/course/course-storage.ts
    src/lib/course/course-progress.ts
    src/lib/course/course-progress.test.ts
    src/components/layout/Sidebar.tsx
    src/components/layout/MobileNav.tsx
  </files>
  <behavior>
    - TabId union accepts 'classroom'.
    - useStore exposes activeCourseId, activeLessonId, setActiveCourse(courseId, lessonId?).
    - createProgressSyncer({ lessonId, supabaseClient }) returns { onTimeUpdate(currentTime), flush(), getMaxReached() } with 15s debounce.
    - createProgressSyncer.onTimeUpdate: tracks max via Math.max; stores last known position; only schedules a write when last write was >15000ms ago.
    - sendProgressBeacon({ lessonId, lastPosition, maxReached, accessToken }) calls navigator.sendBeacon with text/plain JSON body — verified by spying on navigator.sendBeacon.
    - isLessonComplete(progress, lesson): true iff lesson.duration_seconds > 0 AND progress.max_position_reached_seconds / lesson.duration_seconds >= 0.95.
    - Sidebar + MobileNav render a "Classroom" entry that calls setTab('classroom').
  </behavior>
  <action>
    Step A — `src/types/index.ts`:
    - Find the `export type TabId = ...` union (around line 234).
    - Add `| 'classroom'` to the union. Document with a brief inline comment: "Phase 46 NEW TabId — courses are a distinct content surface; per CONTEXT D-? + RESEARCH; EXCEPTION to the no-new-TabId convention from reference_react_router_consumer_admin_split."

    Step B — `src/lib/store.ts`:
    - Find `activeCommunitySpaceId` (around lines 82/118/585/653 per PATTERNS).
    - Add to AppState interface: `activeCourseId: string | null; activeLessonId: string | null;` after activeCommunitySpaceId.
    - Add to Actions interface: `setActiveCourse: (courseId: string | null, lessonId?: string | null) => void;`.
    - Add to initial state: `activeCourseId: null, activeLessonId: null,`.
    - Add to actions implementation: `setActiveCourse: (courseId, lessonId = null) => set({ activeCourseId: courseId, activeLessonId: lessonId }),`.
    - Update `partialize` (around line 231-250) so the NEW fields are EXCLUDED from persistence (they are ephemeral nav state, mirror activeCommunitySpaceId treatment — if Community fields aren't persisted, neither should courses).

    Step C — `src/lib/course/course-storage.ts`:
    - Mirror community-storage.ts: bucket constants + MIME whitelist + max-bytes + Result type + signed-URL helper.
    - Exports:
      ```ts
      export const COURSE_RESOURCES_BUCKET = 'course-resources' as const;
      export const COURSE_RESOURCES_MIMES: ReadonlySet<string> = new Set(['application/pdf','video/mp4','application/zip']);
      export const COURSE_RESOURCES_MAX_BYTES = 200 * 1024 * 1024; // 200 MB

      export type DownloadResult =
        | { ok: true; url: string }
        | { ok: false; error: 'forbidden' | 'not_found' | 'network' };

      export async function downloadResourceSignedUrl(path: string): Promise<DownloadResult> {
        const { data, error } = await supabase.storage
          .from(COURSE_RESOURCES_BUCKET)
          .createSignedUrl(path, 3600);
        if (error || !data) {
          if ((error?.message ?? '').toLowerCase().includes('forbidden')) {
            return { ok: false, error: 'forbidden' };
          }
          return { ok: false, error: 'network' };
        }
        return { ok: true, url: data.signedUrl };
      }
      ```

    Step D — `src/lib/course/course-progress.ts`:
    - Exports `createProgressSyncer({ lessonId, supabaseClient })` returning `{ onTimeUpdate(t: number): void; flush(): Promise<void>; getMaxReached(): number; getLastPosition(): number; }`.
    - Internal state: `maxReached` (number), `lastPosition`, `lastSyncMs`, `pendingTimer` (NodeJS.Timeout | null).
    - `SYNC_DEBOUNCE_MS = 15_000`.
    - onTimeUpdate(t): maxReached = Math.max(maxReached, t); lastPosition = t; if pendingTimer, clearTimeout; pendingTimer = setTimeout(flush, SYNC_DEBOUNCE_MS).
    - flush(): pendingTimer && clearTimeout; await supabaseClient.rpc('update_lesson_position', { p_lesson_id: lessonId, p_last_position_seconds: Math.round(lastPosition), p_max_position_reached_seconds: Math.round(maxReached) }); lastSyncMs = Date.now().
    - Also exports `sendProgressBeacon(opts: { lessonId; lastPosition; maxReached; accessToken })`:
      - Calls `navigator.sendBeacon(${SUPABASE_URL}/functions/v1/lesson-progress-beacon, JSON.stringify({...}))`.
      - Returns the boolean from sendBeacon (success/failure flag).
    - Also exports `isLessonComplete(progress: LessonProgress | null, lesson: CourseLesson): boolean`.

    Step E — `src/lib/course/course-progress.test.ts` (vitest):
    - Test "createProgressSyncer.onTimeUpdate tracks max correctly across scrub-back" (call with 100, 50, 110 → maxReached=110).
    - Test "flush calls rpc('update_lesson_position') with rounded position + max" (stub supabase.rpc; assert call args).
    - Test "debounce: rapid onTimeUpdate within 15s doesn't trigger multiple flushes" (vi.useFakeTimers; spy on rpc; advance time partially).
    - Test "sendProgressBeacon calls navigator.sendBeacon with JSON body" (spy `navigator.sendBeacon`; assert body string is valid JSON containing lesson_id + maxReached + lastPosition + access_token).
    - Test "isLessonComplete returns false when duration_seconds=0 or null".
    - Test "isLessonComplete returns true when ratio >= 0.95, false at 0.94".

    Step F — `src/components/layout/Sidebar.tsx`:
    - Read the file to find the existing nav entries (e.g. Community entry).
    - Add a new Classroom entry mirroring the Community entry exactly: icon (use `BookOpen` from lucide-react), label "Classroom", onClick → setTab('classroom'), aria-label, active-state styling.
    - Position: directly below the Community entry.

    Step G — `src/components/layout/MobileNav.tsx`:
    - Same: add Classroom entry directly below Community entry. If MobileNav has a 5-tab cap (iOS bottom-bar convention), check the existing tab order and either: replace Insights (if not visible by default for free-tier) OR add as a 6th item if the layout supports horizontal scrolling. Decision lives with the executor based on the actual MobileNav layout; document the choice in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; grep -qE "'classroom'" src/types/index.ts &amp;&amp; grep -qE "activeCourseId" src/lib/store.ts &amp;&amp; grep -qE "setActiveCourse" src/lib/store.ts &amp;&amp; grep -qE "COURSE_RESOURCES_BUCKET" src/lib/course/course-storage.ts &amp;&amp; grep -qE "createProgressSyncer" src/lib/course/course-progress.ts &amp;&amp; grep -qE "isLessonComplete" src/lib/course/course-progress.ts &amp;&amp; grep -qE "setTab\(.*classroom" src/components/layout/Sidebar.tsx &amp;&amp; grep -qE "setTab\(.*classroom" src/components/layout/MobileNav.tsx &amp;&amp; npm test -- --run src/lib/course/course-progress.test.ts &amp;&amp; npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - TabId widened with 'classroom'.
    - Store has activeCourseId + activeLessonId + setActiveCourse.
    - course-storage.ts + course-progress.ts exports complete with vitest passing.
    - Sidebar + MobileNav have Classroom entries.
    - tsc clean.
  </acceptance_criteria>
  <done>Foundation primitives + nav entries shipped; Tasks 2-3 build views on top.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClassroomTabShell + CourseListView + CourseDetailView + CourseSidebar (no-video views)</name>
  <read_first>
    - src/components/community/CommunityTabShell.tsx (analog — mirror Suspense + lazy sub-view dispatch)
    - .planning/phases/46-m4-courses-classroom/46-PATTERNS.md §ClassroomTabShell + CourseSidebar + CourseDetailView
    - src/admin/modules/community/CommunityAdminLayout.tsx (analog for list sub-page; reuse list-of-cards pattern)
    - src/lib/community/tier-gate.ts (readTierLabel for tier-aware UI)
    - src/components/ui/Card.tsx + Skeleton.tsx (UI primitives)
  </read_first>
  <files>
    src/components/course/ClassroomTabShell.tsx
    src/components/course/CourseListView.tsx
    src/components/course/CourseDetailView.tsx
    src/components/course/CourseSidebar.tsx
    src/App.tsx
  </files>
  <behavior>
    - ClassroomTabShell: Suspense fallback skeleton; renders LessonPlayerView when activeLessonId && activeCourseId; otherwise renders CourseListView (when no activeCourseId) or CourseDetailView (when activeCourseId but no activeLessonId).
    - CourseListView: lists all courses (supabase.from('courses').select('*').order('created_at')); clicking a course calls setActiveCourse(courseId).
    - CourseDetailView: loads course + its modules + lessons; renders course title + description + cover + CourseSidebar (module/lesson tree); "Start course" / "Resume" CTA based on lesson_progress; clicking a lesson calls setActiveCourse(courseId, lessonId).
    - CourseSidebar: nested module/lesson list with completion checkmarks (read lesson_progress for current user); active lesson highlighted; free-preview lessons accessible to free tier (lock icon on non-preview lessons for free users).
    - App.tsx: add `const ClassroomTabShell = lazy(() => import('@/components/course/ClassroomTabShell'));` near the other lazy tab imports; add `{currentTab === 'classroom' && <ClassroomTabShell />}` to the tab-switcher area mirroring how Community tab is rendered.
  </behavior>
  <action>
    Files in `src/components/course/` MUST use the singular path per `vite.config.ts:207` (`if (id.includes('/src/components/course/')) return 'course-player';`). Plural `courses/` would land in default chunk.

    `src/components/course/ClassroomTabShell.tsx`:
    - Mirror `CommunityTabShell.tsx` lines 1-72 (per PATTERNS).
    - Suspense fallback: small skeleton stack with role="status" aria-live="polite".
    - Branching:
      ```ts
      if (activeLessonId && activeCourseId) → <LessonPlayerView .../>
      else if (activeCourseId) → <CourseDetailView courseId={activeCourseId} .../>
      else → <CourseListView currentTier={currentTier} />
      ```
    - Lazy-import CourseListView + CourseDetailView + LessonPlayerView (all in src/components/course/ → same chunk).

    `src/components/course/CourseListView.tsx`:
    - Loads courses via `supabase.from('courses').select('id, title, slug, description, cover_url, created_at').order('created_at', { ascending: false })` on mount.
    - Renders grid of cards (Card component): title, description, cover_url, "View course" button.
    - Empty state: EmptyState component with "No courses yet" message.
    - Click → setActiveCourse(course.id).
    - Use var(--color-*) tokens for styling per project conventions.

    `src/components/course/CourseDetailView.tsx`:
    - Props: `{ courseId: string; currentTier: TierLabel; currentUserId: string }`.
    - On mount, loads course + modules + lessons via two queries (or one with PostgREST embed `select('*, course_modules(*, course_lessons(*))').eq('id', courseId).single()`).
    - Also loads lesson_progress for current user: `supabase.from('lesson_progress').select('lesson_id, completed_at').eq('user_id', currentUserId).eq('course_id', courseId)`.
    - Renders: course title, description, cover image, total lesson count, completion percentage, "Start course" / "Resume lesson X" CTA, then CourseSidebar with the module/lesson tree.
    - Back button → setActiveCourse(null).
    - dompurify-sanitize the description if it's markdown — import from `@/lib/course/dompurify-config`.

    `src/components/course/CourseSidebar.tsx`:
    - Props: `{ modules: CourseModule[]; lessonsByModule: Record<string, CourseLesson[]>; progressByLesson: Record<string, LessonProgress>; activeLessonId: string | null; currentTier: TierLabel; onLessonClick: (lessonId: string) => void; }`.
    - Renders nested list: each module → expandable group → list of lessons.
    - Each lesson row: title, duration, completion checkmark if completed_at, lock icon if !lesson.is_free_preview AND currentTier === 'free'.
    - aria-current="page" on the active lesson row.

    Modify `src/App.tsx`:
    - Add lazy import near other lazy tab imports: `const ClassroomTabShell = lazy(() => import('@/components/course/ClassroomTabShell'));`.
    - Add render branch in the tab-switcher: `{currentTab === 'classroom' && <ClassroomTabShell />}` mirroring the Community entry.

    Plan-checker grep gate (run during verify): no `new DOMPurify` or `createDOMPurify` in any of these new files.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; test -f src/components/course/ClassroomTabShell.tsx &amp;&amp; test -f src/components/course/CourseListView.tsx &amp;&amp; test -f src/components/course/CourseDetailView.tsx &amp;&amp; test -f src/components/course/CourseSidebar.tsx &amp;&amp; grep -qE "ClassroomTabShell" src/App.tsx &amp;&amp; grep -qE "currentTab === 'classroom'" src/App.tsx &amp;&amp; ! grep -rE 'new DOMPurify|createDOMPurify' src/components/course/ &amp;&amp; npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 4 view files in `src/components/course/` (singular).
    - App.tsx renders ClassroomTabShell when currentTab='classroom'.
    - No new DOMPurify policies — re-uses src/lib/course/dompurify-config (which re-exports Phase 44's).
    - tsc clean.
  </acceptance_criteria>
  <done>Course discovery + detail views shipped; LessonPlayerView (Task 3) completes the surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LessonPlayerView + LessonResourceList + bundle-budget verification</name>
  <read_first>
    - src/components/community/media/CommunityVideoPlayer.tsx (analog — mirror MuxPlayer lazy import + token fetch pattern)
    - .planning/phases/46-m4-courses-classroom/46-PATTERNS.md §LessonPlayerView (Mux Player + signed tokens + onTimeUpdate + sendBeacon)
    - .planning/phases/46-m4-courses-classroom/46-04-PLAN.md (mux-sign-playback contract)
    - .planning/phases/46-m4-courses-classroom/46-06-PLAN.md (lesson-progress-beacon contract)
    - .planning/phases/46-m4-courses-classroom/46-07-PLAN.md (generate-course-certificate contract)
    - src/lib/course/course-progress.ts (the helpers — created in Task 1)
    - src/lib/community/tier-gate.ts (isResourceAllowed — added in Plan 46-03)
    - vite.config.ts (line 207 course-player chunk rule)
    - scripts/assert-bundle-budget.sh (line 49 — 30 kB gz ceiling)
  </read_first>
  <files>
    src/components/course/LessonPlayerView.tsx
    src/components/course/LessonResourceList.tsx
  </files>
  <behavior>
    - LessonPlayerView mount: invoke `mux-sign-playback` with lesson_id → store {playback, thumbnail} tokens. Show skeleton until tokens received.
    - Player: `<MuxPlayer playbackId={...} tokens={...} streamType="on-demand" startTime={lastPosition} onTimeUpdate={handleTimeUpdate} className="w-full" />` — playerInitTime + metadata for Mux Data.
    - handleTimeUpdate: call `progressSyncer.onTimeUpdate(currentTime)`.
    - On mount: load lesson_progress for this lesson → set startTime + initial maxReached.
    - On unmount: `progressSyncer.flush()` (await).
    - beforeunload + visibilitychange→hidden: `sendProgressBeacon({...})`.
    - Mark Complete button: enabled when isLessonComplete(progress, lesson) is true; click → invoke `complete_lesson` RPC; on success → reload progress; if course now 100% complete, invoke `generate-course-certificate` and surface { download_url, verification_url } in a CertReady toast/modal.
    - Below the player: lesson.content_md sanitized via DOMPurify from `@/lib/course/dompurify-config` and rendered.
    - Below the markdown: LessonResourceList component.
    - Prev/Next-lesson navigation: query sibling lessons via order_index; setActiveCourse(courseId, prevId|nextId) on click.
    - tier_required (403 from mux-sign-playback) → show upgrade CTA in place of player.

    LessonResourceList:
    - Loads `lesson_resources` (NEW table? NO — per CONTEXT D-15 + CONTEXT.md original scope, lesson resources are listed as part of the lesson record. Decision: store resources as a JSONB column `resources jsonb` on course_lessons containing array of `{name, path, mime, size, requires_pro: boolean}` — BUT Plan 46-01 did NOT add a resources column. **Resolution:** add `course_lessons.resources jsonb default '[]'::jsonb` in Plan 46-01 schema migration. The executor of Plan 46-01 should already include this column based on the requirement; if not, this task adds it as a separate migration file `20270725000006_p46_course_lessons_resources_column.sql` AND filename collision-checks before commit.)
    - Renders each resource: name, size, mime icon, "Download" button.
    - Click: if requires_pro && !isResourceAllowed(currentTier, mimeToType(mime)) → show upgrade CTA; else call downloadResourceSignedUrl(path) and `window.open(result.url, '_blank')`.
  </behavior>
  <action>
    **Pre-flight scope correction:** Confirm `course_lessons.resources jsonb` is part of Plan 46-01 schema. If executor of 46-01 omitted it (review 46-01-SUMMARY.md before starting this task), add a SUPPLEMENTAL migration `20270725000006_p46_course_lessons_resources_column.sql` here:
    ```sql
    alter table public.course_lessons add column if not exists resources jsonb not null default '[]'::jsonb;
    comment on column public.course_lessons.resources is 'P46 COURSE-06: array of {name, path, mime, size, requires_pro} per D-? — downloadable Pro-gated resources.';
    ```
    (Filename has no collision with the 5 timestamps already used.)

    `src/components/course/LessonPlayerView.tsx`:
    - Top of file imports:
      ```ts
      import MuxPlayer from '@mux/mux-player-react/lazy'; // CRITICAL: /lazy entry — defers ~170 kB
      import DOMPurify from '@/lib/course/dompurify-config';
      import { createProgressSyncer, sendProgressBeacon, isLessonComplete } from '@/lib/course/course-progress';
      import { supabase } from '@/lib/supabase'; // existing project import
      import { useStore } from '@/lib/store';
      import type { CourseLesson, LessonProgress } from '@/lib/course/course-types';
      ```
    - Props: `{ lessonId: string; courseId: string; currentTier: TierLabel }`.
    - State: `tokens` ({playback, thumbnail} | null), `lesson` (CourseLesson|null), `progress` (LessonProgress|null), `siblings` ({prev, next}|null), `gatedError` (string|null), `certReady` ({url, verification_url}|null).
    - useEffect on lessonId change:
      1. Load lesson row (`supabase.from('course_lessons').select('*').eq('id', lessonId).single()`).
      2. Load progress (`...lesson_progress.select('*').eq('lesson_id', lessonId).single()`).
      3. Fetch siblings (modules order + lessons order to determine prev/next).
      4. Invoke `mux-sign-playback`:
         ```ts
         const { data, error } = await supabase.functions.invoke('mux-sign-playback', { body: { lesson_id: lessonId } });
         if (error) {
           const msg = (error as any)?.context?.error ?? (error as any)?.message ?? '';
           if (msg.includes('tier_required')) { setGatedError('tier_required'); return; }
           setGatedError('playback_unavailable'); return;
         }
         setTokens(data as { playback: string; thumbnail: string; playback_id: string });
         ```
    - progressSyncer ref:
      ```ts
      const syncerRef = useRef<ReturnType<typeof createProgressSyncer> | null>(null);
      useEffect(() => {
        syncerRef.current = createProgressSyncer({ lessonId, supabaseClient: supabase });
        return () => { void syncerRef.current?.flush(); };
      }, [lessonId]);
      ```
    - handleTimeUpdate (passed to MuxPlayer):
      ```ts
      function handleTimeUpdate(e: Event) {
        const v = e.currentTarget as HTMLVideoElement;
        syncerRef.current?.onTimeUpdate(v.currentTime);
      }
      ```
    - beforeunload + visibilitychange listeners installed in useEffect; both call `sendProgressBeacon({ lessonId, lastPosition: syncerRef.current?.getLastPosition() ?? 0, maxReached: syncerRef.current?.getMaxReached() ?? 0, accessToken: session?.access_token })`. Cleanup removes listeners.
    - Mark Complete button:
      ```ts
      const canComplete = isLessonComplete(progress, lesson);
      <button disabled={!canComplete} onClick={handleMarkComplete}>Mark Complete</button>
      ```
    - handleMarkComplete:
      ```ts
      const { data: ok, error } = await supabase.rpc('complete_lesson', { p_lesson_id: lessonId });
      if (error || !ok) { /* toast */ return; }
      // Reload progress
      // Check if course 100% complete by re-loading course progress
      // If 100% → invoke generate-course-certificate
      const { data: cert } = await supabase.functions.invoke('generate-course-certificate', { body: { course_id: courseId } });
      if (cert && (cert as any).download_url) {
        setCertReady({ url: (cert as any).download_url, verification_url: (cert as any).verification_url });
      }
      ```
      (404/403 from generate-course-certificate is OK — just means course isn't fully done yet.)
    - Render branches:
      - gatedError === 'tier_required' → upgrade CTA card.
      - !tokens && !gatedError → skeleton.
      - tokens → `<MuxPlayer playbackId={tokens.playback_id} tokens={{ playback: tokens.playback, thumbnail: tokens.thumbnail }} streamType="on-demand" startTime={progress?.last_position_seconds ?? 0} onTimeUpdate={handleTimeUpdate} className="w-full" />`.
      - Below player: `<div dangerouslySetInnerHTML={{ __html: DOMPurify.sanitize(lesson?.content_md ?? '') }} />` (using the imported DOMPurify which is the Phase 44 reuse — verify dompurify-config exports a usable sanitize function; if it exports a different API surface, adapt to that).
      - LessonResourceList component.
      - Mark Complete + Prev/Next nav.
      - certReady → `<CertReadyToast download_url={certReady.url} verification_url={certReady.verification_url} />`.

    `src/components/course/LessonResourceList.tsx`:
    - Props: `{ lesson: CourseLesson; currentTier: TierLabel }`.
    - Iterate over `lesson.resources` (jsonb array).
    - For each: render row with name, size, mime icon, button.
    - Button click → if `requires_pro && !isResourceAllowed(currentTier, mimeToType(mime))` → setUpgradeOpen(true); else call downloadResourceSignedUrl(resource.path) → window.open.
    - mimeToType helper: 'application/pdf' → 'pdf', 'video/mp4' → 'video', 'application/zip' → 'zip'.

    **Bundle budget verification:**
    - After all files in this plan are written, run:
      ```bash
      cd /Users/karstenhaldan/minisite/leanshot && npm run build && bash scripts/assert-bundle-budget.sh
      ```
    - The script asserts `course-player` chunk ≤ 30 kB gz. If it exceeds, the executor must:
      a. Verify all course files are in `src/components/course/` (singular).
      b. Verify MuxPlayer imports use `@mux/mux-player-react/lazy` (NOT bare entry).
      c. Verify no heavy SDKs imported eagerly (e.g. chart.js, framer-motion entry).
      d. If still over, file a follow-up STATE entry — DO NOT ship over-budget.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; grep -qE "@mux/mux-player-react/lazy" src/components/course/LessonPlayerView.tsx &amp;&amp; grep -qE "mux-sign-playback" src/components/course/LessonPlayerView.tsx &amp;&amp; grep -qE "navigator\.sendBeacon" src/lib/course/course-progress.ts &amp;&amp; ! grep -rE 'new DOMPurify|createDOMPurify' src/components/course/ &amp;&amp; ! grep -rE "from '@mux/mux-player-react'[^/]" src/components/course/ &amp;&amp; npm run build &amp;&amp; bash scripts/assert-bundle-budget.sh</automated>
  </verify>
  <acceptance_criteria>
    - LessonPlayerView + LessonResourceList at exact paths.
    - MuxPlayer imported via `/lazy` entry-point (NOT bare `@mux/mux-player-react`).
    - mux-sign-playback invoked via supabase.functions.invoke.
    - progressSyncer wired to onTimeUpdate + flush on unmount.
    - sendProgressBeacon installed on beforeunload + visibilitychange.
    - complete_lesson RPC + conditional generate-course-certificate invocation.
    - No new DOMPurify policy in any course file.
    - Bundle budget script passes (`course-player ≤ 30 kB gz`).
  </acceptance_criteria>
  <done>Consumer Classroom surface complete; user can browse → enroll-effective via tier → play → progress → complete → cert. Plan 46-09 admin lets actual courses get created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → mux-sign-playback | bearer JWT; tier_effective.has_active OR is_free_preview gate |
| Browser onTimeUpdate → update_lesson_position RPC | per-user supabase client; RLS+SECDEF enforces user_id=auth.uid() |
| Browser beforeunload → lesson-progress-beacon | text/plain JSON body; access_token in body (sendBeacon can't set headers) |
| Browser → complete_lesson RPC | SECDEF re-checks ≥95% server-side |
| Browser → generate-course-certificate | SECDEF complete_course re-checks 100% threshold |
| Browser → resource download | RLS on course-resources bucket gates on tier_effective.has_active |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Information Disclosure | Free user accesses Pro lesson playback | mitigate | mux-sign-playback returns 403 → LessonPlayerView shows upgrade CTA instead of player |
| T-46-02 | Tampering | Spoofed onTimeUpdate / sendBeacon position | accept (soft) | Server complete_lesson RPC re-checks max_position_reached_seconds; client gate is UX only; CONTEXT D-12 documents the trade |
| T-46-05 | Tampering | Markdown XSS in lesson content_md | mitigate | DOMPurify.sanitize via src/lib/course/dompurify-config (Phase 44 reuse); enforced by grep gate (no new policy) |
| T-46-06 | Information Disclosure | Resource path traversal | mitigate | Storage bucket RLS path-prefix check (Plan 46-02); supabase.storage.createSignedUrl uses path passed by SECDEF-validated source (course_lessons.resources jsonb) |
| T-46-08 | Information Disclosure | Resource URL leakage beyond TTL | mitigate | 60-min signed URL; user re-fetches by clicking button |
</threat_model>

<verification>
- vitest passes for src/lib/course/course-progress.test.ts.
- tsc clean.
- Build + bundle budget green (`course-player ≤ 30 kB gz`).
- Grep gates: classroom TabId present, MuxPlayer `/lazy` entry, no new DOMPurify, no bare `@mux/mux-player-react` import.
- Plan 46-11 runs the Playwright @phase46 suite that walks the full consumer flow (course list → detail → lesson → mark complete → cert ready).
</verification>

<success_criteria>
- [ ] 11 new files + 3 modified files at exact paths.
- [ ] Bundle ceiling: course-player ≤ 30 kB gz.
- [ ] TabId 'classroom' added with documented rationale.
- [ ] Mux Player imported via /lazy entry only.
- [ ] sendBeacon uses text/plain (handled by lesson-progress-beacon's req.text() path).
- [ ] dompurify reuse (no new policy).
- [ ] vitest + tsc green.
</success_criteria>

<output>
After completion, create `.planning/phases/46-m4-courses-classroom/46-08-SUMMARY.md` with: file paths + SHAs, vitest count, course-player chunk size (must include the actual gz byte count from build output), Plan 46-11 reminder to run Playwright @phase46 against deployed Fns + bundle gate.
</output>
