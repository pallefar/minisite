---
phase: 53-capacitor-mobile-shells-ios-android
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/fastlane/Gemfile
  - leanshot/fastlane/Appfile
  - leanshot/fastlane/Matchfile
  - leanshot/fastlane/Fastfile
  - leanshot/fastlane/metadata/ios/en-US/description.txt
  - leanshot/fastlane/metadata/ios/en-US/keywords.txt
  - leanshot/fastlane/metadata/ios/en-US/release_notes.txt
  - leanshot/fastlane/metadata/ios/en-US/privacy_url.txt
  - leanshot/fastlane/metadata/ios/en-US/support_url.txt
  - leanshot/fastlane/metadata/android/en-US/title.txt
  - leanshot/fastlane/metadata/android/en-US/short_description.txt
  - leanshot/fastlane/metadata/android/en-US/full_description.txt
  - leanshot/fastlane/README.md
  - leanshot/apps/ios/marketing/privacy-nutrition-labels.md
autonomous: true
requirements: [MOBILE-02, MOBILE-03, MOBILE-07, MOBILE-09, MOBILE-10]
user_setup: []

must_haves:
  truths:
    - "fastlane Fastfile defines build_ios_unsigned + build_android_unsigned lanes (always-run) and upload_testflight + upload_play lanes (gated on secrets)"
    - "fastlane lane names referenced by CI workflows exist as a stable contract"
    - "Store-metadata directories exist for ios + android with placeholder copy sourced from existing store-listing files"
    - "Privacy nutrition labels documentation cross-references PrivacyInfo.xcprivacy (Apple) + data-safety.md (Google)"
    - "Matchfile is scaffolded with a TODO note because fastlane match repo provisioning defers to Phase 70"
  artifacts:
    - path: "leanshot/fastlane/Fastfile"
      provides: "iOS + Android build/upload lanes (unsigned always, signed/upload gated)"
      contains: "build_ios_unsigned"
    - path: "leanshot/fastlane/Gemfile"
      provides: "Pinned fastlane gem version for reproducible CI installs"
      contains: "fastlane"
    - path: "leanshot/fastlane/Appfile"
      provides: "Bundle/package id config (app.leanshot.ios / app.leanshot.android)"
      contains: "app.leanshot"
    - path: "leanshot/apps/ios/marketing/privacy-nutrition-labels.md"
      provides: "Apple App Privacy + Google Data Safety nutrition-label cross-reference"
      contains: "Data Safety"
  key_links:
    - from: "leanshot/fastlane/Fastfile"
      to: "apps/ios/App/App.xcodeproj/project.xcworkspace"
      via: "gym workspace param"
      pattern: "App.xcworkspace|App.xcodeproj"
    - from: "leanshot/fastlane/Fastfile"
      to: "apps/android/"
      via: "gradle project_dir param"
      pattern: "apps/android"
---

<objective>
Scaffold the fastlane build/signing/upload toolchain plus the App Store + Play Store metadata package (including per-store privacy nutrition labels documentation). This is the build-automation contract that the Phase 53 CI workflows (plan 53-03) invoke by lane name.

Purpose: MOBILE-02/03 require iOS + Android bundles to build via GitHub Actions with conditional signing; MOBILE-07/10 require a store-metadata + privacy-labels submission package; MOBILE-09 requires TestFlight + Play upload lanes (gated, actual upload deferred to Phase 70). All signing/upload steps are GATED on GitHub secrets that are pending-provisioning — when absent, lanes either skip or build unsigned-green. No native build runs here; the fastlane files are scaffold + config, verified by `ruby -c` syntax check + file-existence.

Output: `leanshot/fastlane/` (Gemfile, Appfile, Matchfile, Fastfile, README) + `leanshot/fastlane/metadata/{ios,android}/en-US/` copy files + `leanshot/apps/ios/marketing/privacy-nutrition-labels.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@leanshot/.planning/phases/53-capacitor-mobile-shells-ios-android/53-RESEARCH.md

# Existing store-listing source files to derive metadata copy from (do NOT invent product copy — reuse these):
@leanshot/apps/android/store-listing-en.md
@leanshot/apps/android/data-safety.md

<facts>
<!-- Verified from filesystem 2026-05-25 — obey these over any conflicting source text. -->
- Native projects ALREADY EXIST (Phase 16): `leanshot/apps/ios` is SPM-based (CapApp-SPM/Package.swift; NO CocoaPods — NEVER add a `pod install` lane or step). `leanshot/apps/android` is Gradle. capacitor.config.ts already sets ios.path='apps/ios', android.path='apps/android'.
- iOS bundle id: `app.leanshot.ios` (project.pbxproj PRODUCT_BUNDLE_IDENTIFIER). Android applicationId: `app.leanshot.android` (apps/android/app/build.gradle:7).
- iOS workspace path (relative to leanshot/): `apps/ios/App/App.xcodeproj/project.xcworkspace`; scheme `App`; configuration `Release`.
- Android gradle project dir (relative to leanshot/): `apps/android/`.
- Existing iOS screenshot placeholders: `leanshot/apps/ios/marketing/screenshots/en-US/` (9 PNGs across 3 device sizes) — fastlane `deliver` screenshots_path can point here; do NOT recreate them.
- Existing Android store listing: `leanshot/apps/android/store-listing-en.md`; existing data-safety doc: `leanshot/apps/android/data-safety.md`; existing Apple privacy manifest: `leanshot/apps/ios/App/App/PrivacyInfo.xcprivacy`.
- fastlane match repo is `pallefar/leanshot-fastlane-match` (MOBILE-02) but its provisioning status is unknown / pending Phase 52-70 → Matchfile must be scaffolded with a TODO and the upload lanes must tolerate match running readonly only when secrets are present.
- CI npm install MUST use `npm ci --legacy-peer-deps` (@sentry/capacitor sibling-check). Not relevant to fastlane files directly, but the README runbook should note it.
- system Ruby is 2.6.10 locally; CI uses ruby/setup-ruby. `ruby -c <Fastfile>` is a valid syntax-only check that does not require the fastlane gem.
- Gated GitHub secret names (all pending-provisioning, deferred to Phase 70): APPLE_CERTIFICATE_BASE64, MATCH_GIT_BASIC_AUTHORIZATION, MATCH_PASSWORD, FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD, KEYSTORE_BASE64, KEYSTORE_PASSWORD, KEY_ALIAS, KEY_PASSWORD, PLAY_SERVICE_ACCOUNT_JSON.
</facts>

<lane_contract>
<!-- These lane names are the contract consumed by plan 53-03 CI workflows. Do NOT rename. -->
- ios lane `build_ios_unsigned`  — gym with skip_codesigning:true, workspace apps/ios/App/App.xcodeproj/project.xcworkspace, scheme App, configuration Release. Always-runnable.
- ios lane `upload_testflight`    — match(type:"appstore", readonly:true) + gym(export_method:"app-store") + pilot(skip_waiting_for_build_processing:true). GATED — only invoked by CI when Apple secrets present.
- android lane `build_android_unsigned` — gradle(task:"bundle", build_type:"Release", project_dir:"apps/android/") with empty injected signing props. Always-runnable.
- android lane `upload_play`      — supply(track:"internal", json_key_data: ENV["PLAY_SERVICE_ACCOUNT_JSON"]). GATED — only invoked by CI when Play secrets present.
</lane_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fastlane core scaffold — Gemfile, Appfile, Matchfile, Fastfile</name>
  <files>leanshot/fastlane/Gemfile, leanshot/fastlane/Appfile, leanshot/fastlane/Matchfile, leanshot/fastlane/Fastfile</files>
  <action>Create the fastlane toolchain (MOBILE-02, MOBILE-03, MOBILE-09). Gemfile: source rubygems.org, pin `gem "fastlane", "~> 2.222"`. Appfile: set app_identifier "app.leanshot.ios", package_name "app.leanshot.android" (per-platform bundle ids — verified from project.pbxproj + build.gradle); leave apple_id / team_id reading from ENV with a comment noting real values defer to Phase 70 (do NOT hardcode an Apple Team ID). Matchfile: set git_url "https://github.com/pallefar/leanshot-fastlane-match", type "appstore", storage_mode "git", readonly true; add a `# TODO(Phase 70): match repo may not be provisioned yet — upload_testflight is gated on MATCH_* secrets so this file is inert until then.` comment. Fastfile: implement the four lanes EXACTLY as named in the <lane_contract> block above. Use `platform :ios do ... end` and `platform :android do ... end`. The unsigned lanes must run without any secret. The upload lanes assume the caller (CI) has already gated on secret presence — inside upload_testflight call match readonly + gym app-store export + pilot; inside upload_play call supply internal track reading json_key_data from ENV["PLAY_SERVICE_ACCOUNT_JSON"]. CRITICAL: NO cocoapods / `cocoapods` / `pod install` lane or step anywhere — this is an SPM project; adding pod logic breaks the build. Paths in gym/gradle are relative to the `leanshot/` directory (fastlane runs with working-directory leanshot in CI).</action>
  <verify>
    <automated>cd leanshot && ruby -c fastlane/Fastfile && ruby -c fastlane/Appfile && ruby -c fastlane/Matchfile && test -f fastlane/Gemfile && grep -q 'fastlane' fastlane/Gemfile && grep -q 'build_ios_unsigned' fastlane/Fastfile && grep -q 'build_android_unsigned' fastlane/Fastfile && grep -q 'upload_testflight' fastlane/Fastfile && grep -q 'upload_play' fastlane/Fastfile && ! grep -riq 'pod install\|cocoapods' fastlane/</automated>
  </verify>
  <done>Four fastlane config files exist; Fastfile/Appfile/Matchfile pass `ruby -c`; all four contract lane names present; zero cocoapods references.</done>
</task>

<task type="auto">
  <name>Task 2: Store metadata package + privacy nutrition labels doc + runbook</name>
  <files>leanshot/fastlane/metadata/ios/en-US/description.txt, leanshot/fastlane/metadata/ios/en-US/keywords.txt, leanshot/fastlane/metadata/ios/en-US/release_notes.txt, leanshot/fastlane/metadata/ios/en-US/privacy_url.txt, leanshot/fastlane/metadata/ios/en-US/support_url.txt, leanshot/fastlane/metadata/android/en-US/title.txt, leanshot/fastlane/metadata/android/en-US/short_description.txt, leanshot/fastlane/metadata/android/en-US/full_description.txt, leanshot/fastlane/README.md, leanshot/apps/ios/marketing/privacy-nutrition-labels.md</name>
  <action>Build the fastlane store-metadata package (MOBILE-07) + per-store privacy nutrition labels documentation (MOBILE-10). Populate the ios `metadata/ios/en-US/` and android `metadata/android/en-US/` text files by REUSING copy already present in `leanshot/apps/android/store-listing-en.md` (do NOT invent new product marketing copy — derive title, short/full description, keywords from that source; fall back to PROJECT.md core-value line for the description if the store-listing lacks a field). privacy_url.txt → `https://app.leanshot.app/privacy`; support_url.txt → `https://app.leanshot.app/support`. Create `privacy-nutrition-labels.md` that cross-references the EXISTING `apps/ios/App/App/PrivacyInfo.xcprivacy` (Apple App Privacy) and `apps/android/data-safety.md` (Google Data Safety) — it is a documentation artifact mapping each NSPrivacyCollectedDataType to its Apple label and Google Data Safety category; it must NOT duplicate or replace those files. Create `fastlane/README.md` runbook documenting: (a) `npm ci --legacy-peer-deps` requirement; (b) that screenshots live at `apps/ios/marketing/screenshots/en-US/` (placeholders, real shots deferred to Phase 70); (c) the Phase 70 deferral matrix — signed artifacts, TestFlight/Play upload, real Apple Team ID / Play signing SHA256 substitution into association files all gated on secrets and deferred. NO "v1"/"placeholder-for-now" scope-reduction language for the deferred items — describe them as explicitly Phase-70-gated, not simplified.</action>
  <verify>
    <automated>cd leanshot && test -s fastlane/metadata/ios/en-US/description.txt && test -s fastlane/metadata/android/en-US/full_description.txt && test -s fastlane/metadata/android/en-US/short_description.txt && grep -q 'app.leanshot.app/privacy' fastlane/metadata/ios/en-US/privacy_url.txt && test -f apps/ios/marketing/privacy-nutrition-labels.md && grep -qi 'Data Safety' apps/ios/marketing/privacy-nutrition-labels.md && grep -qi 'PrivacyInfo' apps/ios/marketing/privacy-nutrition-labels.md && grep -qi 'Phase 70' fastlane/README.md</automated>
  </verify>
  <done>All ios + android metadata text files exist and are non-empty; privacy_url points at app.leanshot.app; privacy-nutrition-labels.md cross-references both PrivacyInfo + Data Safety; README runbook documents the Phase 70 deferral matrix and the --legacy-peer-deps requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → signing material | Apple cert + Play keystore arrive as GitHub Secrets, decoded only inside gated jobs |
| Committed repo → public | Appfile/Matchfile/metadata are committed; must contain zero real secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Information Disclosure | Appfile / Matchfile committed to repo | mitigate | No real Apple Team ID, no service-account JSON, no cert in committed files; Appfile reads apple_id/team_id from ENV; Matchfile uses readonly + TODO; real values set at Phase 70 only |
| T-53-02 | Elevation of Privilege | fastlane upload lanes | mitigate | Upload lanes assume CI has gated on secret presence; lanes are inert (never invoked) until APPLE/PLAY secrets exist |
| T-53-03 | Tampering | fastlane gem install | accept | fastlane is the official Fastlane/Google CI tool (RubyGems, widely used); version pinned `~> 2.222` in Gemfile; no new npm packages introduced this plan |
| T-53-SC | Tampering | npm/gem installs | accept | No npm installs in this plan; fastlane gem is `[Approved]` per RESEARCH Package Legitimacy Audit (official, pre-vetted); no [ASSUMED]/[SUS]/[SLOP] packages |
</threat_model>

<verification>
- `ruby -c` passes on all Ruby fastlane config files.
- All four contract lane names present in Fastfile.
- Zero cocoapods/pod-install references anywhere under `leanshot/fastlane/`.
- Metadata text files non-empty; privacy-nutrition-labels.md cross-references both store manifests.
- README runbook documents Phase 70 deferral matrix.
</verification>

<success_criteria>
fastlane toolchain scaffolded with the lane-name contract that 53-03 CI workflows invoke; store-metadata + privacy-nutrition-labels package complete; all deferred (secret-gated) items documented as Phase-70-gated (not simplified away).
</success_criteria>

<output>
Create `.planning/phases/53-capacitor-mobile-shells-ios-android/53-01-SUMMARY.md` when done. Record: lane names shipped, metadata files created, and the exact Phase 70 deferral list for the next planner/operator.
</output>
