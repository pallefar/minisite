---
phase: 53-capacitor-mobile-shells-ios-android
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/.env.example
  - leanshot/src/lib/native/deeplink-association.test.ts
  - leanshot/src/lib/native/settings-delete-reachability.test.tsx
  - leanshot/apps/ios/marketing/deeplink-id-substitution-runbook.md
autonomous: true
requirements: [MOBILE-01, MOBILE-04, MOBILE-05, MOBILE-06, MOBILE-08]
user_setup: []

must_haves:
  truths:
    - "VITE_RC_API_KEY_IOS + VITE_RC_API_KEY_ANDROID are declared as env stubs in .env.example with a comment that values defer to Phase 70"
    - "AASA + assetlinks.json are valid JSON and contain the expected appID/package_name shapes"
    - "DeleteAccountModal is reachable from SettingsPage at a 375px mobile viewport (rendered, not display:none, tap target present)"
    - "capacitor.config.ts ios.path/android.path point at the existing apps/ios + apps/android dirs (no cap add — config validity only)"
    - "A runbook documents the TEAMID (AASA) + SHA256 (assetlinks) substitution as Phase-70-gated"
  artifacts:
    - path: "leanshot/.env.example"
      provides: "RevenueCat native SDK key stubs (MOBILE-06)"
      contains: "VITE_RC_API_KEY_IOS"
    - path: "leanshot/src/lib/native/deeplink-association.test.ts"
      provides: "AASA + assetlinks JSON validity + shape assertions (MOBILE-05)"
      contains: "apple-app-site-association"
    - path: "leanshot/src/lib/native/settings-delete-reachability.test.tsx"
      provides: "Mobile-viewport account-deletion reachability test (MOBILE-08)"
      contains: "DeleteAccountModal"
    - path: "leanshot/apps/ios/marketing/deeplink-id-substitution-runbook.md"
      provides: "TEAMID + SHA256 Phase 70 swap procedure"
      contains: "TEAMID"
  key_links:
    - from: "leanshot/src/App.tsx"
      to: "configureRC"
      via: "dynamic import of src/lib/native/iap.ts"
      pattern: "configureRC"
    - from: "leanshot/src/lib/native/iap.ts"
      to: "VITE_RC_API_KEY_IOS / VITE_RC_API_KEY_ANDROID"
      via: "import.meta.env read"
      pattern: "VITE_RC_API_KEY_(IOS|ANDROID)"
---

<objective>
Wire RevenueCat env-var declarations, prove the deep-link association files are valid and correctly shaped, prove in-app account deletion is reachable on a mobile viewport, and confirm capacitor.config validity against the EXISTING native dirs — all without a native build.

Purpose: MOBILE-06 needs the RevenueCat SDK keys declared (the `configureRC()` bridge already reads them — only env stubs are net-new). MOBILE-05 needs the AASA + assetlinks files present + valid (they already exist with placeholders). MOBILE-08 needs account deletion reachable from mobile Settings (the DeleteAccountModal is already wired — this proves mobile-viewport reachability). MOBILE-01/04 need cap config validity (native dirs + plugins already exist). The phase contract is "config valid + files present + reachable", verified by Vitest + JSON.parse + grep — NOT by an Xcode/Gradle build.

Output: RC env stubs in `.env.example`; two new test files under `src/lib/native/`; a TEAMID/SHA256 substitution runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@leanshot/.planning/phases/53-capacitor-mobile-shells-ios-android/53-RESEARCH.md

# Existing files this plan reads/extends — executor should use these directly, no exploration needed:
@leanshot/src/lib/native/iap.ts
@leanshot/src/components/dashboard/settings/SettingsPage.tsx
@leanshot/capacitor.config.ts
@leanshot/public/.well-known/apple-app-site-association
@leanshot/public/.well-known/assetlinks.json
@leanshot/vitest-mobile.config.ts

<interfaces>
<!-- Verified from filesystem 2026-05-25. Use directly — do NOT re-implement iap.ts or DeleteAccountModal. -->

From src/lib/native/iap.ts (ALREADY IMPLEMENTED — do not modify):
  export async function configureRC(appUserID: string): Promise<void>   // no-op on web; reads VITE_RC_API_KEY_IOS/ANDROID
  export class RcConfigError extends Error
  // configureRC is invoked in src/App.tsx (~line 1319) via dynamic import after sign-in.

From src/components/dashboard/settings/SettingsPage.tsx (ALREADY WIRED — do not modify):
  - imports { DeleteAccountModal } from './DeleteAccountModal' (line 49)
  - renders a "Delete account" section heading (line 643) + button text "Delete account" (line 675)
  - <DeleteAccountModal open={deleteOpen} onClose={...} /> mounted at line 885
  - delete button opens the modal via setDeleteOpen(true)

From vitest-mobile.config.ts (test discovery):
  - include globs already cover: 'src/lib/native/**/*.test.ts' AND 'src/lib/native/**/*.test.tsx'
  - environment: 'jsdom', globals: true; aliases mock @capacitor/* + @revenuecat/* + @capgo/*
  - run command: `npm run test:mobile-unit` (= vitest run --config vitest-mobile.config.ts)
  - PLACE NEW TEST FILES UNDER src/lib/native/ so they are auto-discovered by the existing globs.

From capacitor.config.ts (ALREADY CORRECT — verify only, do NOT run cap add):
  - ios.path: 'apps/ios', android.path: 'apps/android', webDir: 'dist'
  - server.allowNavigation: ['leanshot.app', 'app.leanshot.app']
</interfaces>

<facts>
- `.env.example` exists (NOT `.env.local.example`). It already has VITE_SENTRY_* + VITE_POSTHOG_* + STRIPE_* sections. Append RC keys; do NOT rewrite existing sections.
- RC keys are RevenueCat PUBLIC SDK keys (designed to be public, VITE_-prefixed is correct per iap.ts threat-model comment). Values are pending-provisioning → stub empty, defer to Phase 70.
- AASA appID shape: "TEAMID.app.leanshot.ios" (TEAMID placeholder intentional). assetlinks package_name: "app.leanshot.android"; sha256 placeholder "REPLACE_WITH_PLAY_APP_SIGNING_SHA256_AT_PLAN_16_09".
- DO NOT run `npx cap add` (would overwrite Phase 16 native projects). cap config validity is verified by asserting capacitor.config.ts points at existing dirs that exist on disk.
- iOS App.entitlements ALREADY has com.apple.developer.associated-domains with applinks:leanshot.app + applinks:app.leanshot.app — no edit needed (verify-only, optional grep in test).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RevenueCat env stubs + TEAMID/SHA256 substitution runbook</name>
  <files>leanshot/.env.example, leanshot/apps/ios/marketing/deeplink-id-substitution-runbook.md</files>
  <action>Append a "Phase 53 — RevenueCat native IAP (MOBILE-06)" section to the END of `leanshot/.env.example` (do NOT modify existing Sentry/PostHog/Stripe sections). Declare `VITE_RC_API_KEY_IOS=` and `VITE_RC_API_KEY_ANDROID=` (empty), with a comment block noting: these are RevenueCat PUBLIC SDK keys read by `src/lib/native/iap.ts::configureRC()` (already implemented), they are browser-safe (VITE_-prefixed is intentional), and real values are pending-provisioning → set at Phase 70 (smoke-tracked in the Phase 52 vendor dashboard). Create `deeplink-id-substitution-runbook.md` documenting the Phase 70 swap procedure for the two deep-link placeholders: (1) AASA `TEAMID` → real Apple Team ID from Phase 52 VENDOR-01 enrollment (file: public/.well-known/apple-app-site-association, two occurrences: appID + webcredentials); (2) assetlinks `REPLACE_WITH_PLAY_APP_SIGNING_SHA256_AT_PLAN_16_09` → real Play App Signing SHA256 fingerprint from Play Console (file: public/.well-known/assetlinks.json). State plainly these are Phase-70-gated (NOT "v1"/"temporary"); App Links/Universal Links only need to resolve in production. Note Vercel already serves both files with Content-Type: application/json (vercel.json) — do not touch vercel.json.</action>
  <verify>
    <automated>cd leanshot && grep -q 'VITE_RC_API_KEY_IOS' .env.example && grep -q 'VITE_RC_API_KEY_ANDROID' .env.example && grep -q 'VITE_SENTRY_DSN_WEB' .env.example && test -f apps/ios/marketing/deeplink-id-substitution-runbook.md && grep -q 'TEAMID' apps/ios/marketing/deeplink-id-substitution-runbook.md && grep -qi 'Phase 70' apps/ios/marketing/deeplink-id-substitution-runbook.md</automated>
  </verify>
  <done>.env.example contains both RC key stubs AND still contains pre-existing sections (VITE_SENTRY_DSN_WEB present); runbook documents both placeholder swaps as Phase-70-gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deep-link association validity test + cap config validity test</name>
  <files>leanshot/src/lib/native/deeplink-association.test.ts</files>
  <behavior>
    - AASA file parses as valid JSON (MOBILE-05).
    - AASA applinks.details[0].appID matches /\.app\.leanshot\.ios$/ (TEAMID prefix tolerated).
    - AASA paths array includes the expected deep-link routes (e.g. "/share/*", "/r/*", "/signin").
    - assetlinks.json parses as valid JSON; [0].target.package_name === "app.leanshot.android"; relation includes "delegate_permission/common.handle_all_urls".
    - capacitor.config validity: assert apps/ios and apps/android dirs exist on disk and capacitor.config.ts references ios.path 'apps/ios' + android.path 'apps/android' (read config source as text; assert substrings). This proves cap-config validity WITHOUT running cap add/sync.
    - MOBILE-04 plugin presence: assert package.json dependencies include @capacitor/app, @capacitor/preferences, @capacitor/network, @capacitor/status-bar, @capacitor/splash-screen, @capacitor/keyboard.
  </behavior>
  <action>Create `src/lib/native/deeplink-association.test.ts` (auto-discovered by vitest-mobile globs). Use node:fs to read the well-known files + capacitor.config.ts + package.json relative to process.cwd() (tests run with cwd=leanshot). Implement the assertions in <behavior>. Write the assertions FIRST and confirm they pass against the existing committed files (these files already exist with placeholders, so the test is GREEN immediately — it is a regression guard, not RED-first). Keep file reads resilient: resolve paths from the repo root via path.resolve. Do NOT mock the files — read the real committed artifacts. This covers MOBILE-01 (dirs exist), MOBILE-04 (plugins), MOBILE-05 (association files valid).</action>
  <verify>
    <automated>cd leanshot && npx vitest run --config vitest-mobile.config.ts src/lib/native/deeplink-association.test.ts</automated>
  </verify>
  <done>Test file runs green under vitest-mobile: AASA + assetlinks valid JSON with correct shapes, native dirs exist, capacitor.config points at apps/ios + apps/android, all six MOBILE-04 plugins present in package.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Account-deletion mobile-viewport reachability test</name>
  <files>leanshot/src/lib/native/settings-delete-reachability.test.tsx</files>
  <behavior>
    - At a 375px-wide viewport (jsdom: set window.innerWidth=375 + matchMedia stub), SettingsPage renders the "Delete account" control (MOBILE-08, Apple §5.1.1(v) + Play §13.7).
    - The delete control is present in the DOM and NOT display:none / aria-hidden.
    - Activating it surfaces DeleteAccountModal (modal mounts / open state flips) — i.e. the deletion path is reachable, not orphaned.
    - Web-platform default: configureRC is a no-op (sanity guard that the shell does not require native bridge to render Settings).
  </behavior>
  <action>Create `src/lib/native/settings-delete-reachability.test.tsx` (auto-discovered by vitest-mobile globs — jsdom env + React plugin already configured). Render SettingsPage (import from '@/components/dashboard/settings/SettingsPage') inside whatever store/provider scaffolding the existing `src/test/account-delete.test.tsx` uses — READ that file first and mirror its setup (store seeding for a signed-in user, any required mocks) to avoid re-deriving the harness. Set window.innerWidth=375 and stub window.matchMedia before render. Query for the "Delete account" button/heading (text from SettingsPage line 643/675). Assert it is in the document and visible (not display:none). Click it and assert the DeleteAccountModal becomes reachable (query by its confirm copy "DELETE MY ACCOUNT" or role=dialog). If SettingsPage requires capacitor mocks, rely on the aliases already in vitest-mobile.config.ts. Write the assertions FIRST; if any assertion fails because the button is genuinely unreachable at 375px, that is a real MOBILE-08 gap — fix the reachability in SettingsPage (minimal, e.g. ensure the section is not hidden behind a desktop-only media query) and document the fix in the SUMMARY; do NOT weaken the test to pass.</action>
  <verify>
    <automated>cd leanshot && npx vitest run --config vitest-mobile.config.ts src/lib/native/settings-delete-reachability.test.tsx</automated>
  </verify>
  <done>Test green: at 375px viewport SettingsPage renders a visible "Delete account" control that opens DeleteAccountModal. Any reachability fix is in SettingsPage (not a weakened assertion) and noted in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite env → browser bundle | VITE_-prefixed RC keys are embedded in the client bundle (by design — public SDK keys) |
| Public web → AASA/assetlinks | Association files are publicly served static JSON consumed by iOS/Android at install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | Information Disclosure | VITE_RC_API_KEY_* in client bundle | accept | RevenueCat public SDK keys are designed to be public (documented in iap.ts); not a secret. Only VITE_RC_* are exposed; no server secret receives a VITE_ prefix. |
| T-53-05 | Information Disclosure | Apple Team ID in committed AASA | mitigate | Keep `TEAMID` placeholder in committed file; real ID substituted only at Phase 70 in production deploy (runbook documents the swap) |
| T-53-06 | Spoofing | assetlinks SHA256 fingerprint | accept | The SHA256 fingerprint is a required public Android artifact (not secret); placeholder until Phase 70 prod wiring |
| T-53-SC | Tampering | npm/gem installs | accept | No new packages installed; all Capacitor + RevenueCat packages pre-existing + `[Approved]` per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `.env.example` gains RC key stubs without losing existing sections.
- deeplink-association test green: association files valid + correctly shaped, native dirs + plugins present, cap config valid.
- settings-delete-reachability test green at 375px viewport.
- Substitution runbook documents both placeholder swaps as Phase-70-gated.
</verification>

<success_criteria>
RevenueCat keys declared; deep-link association files proven valid + shaped; account deletion proven reachable on mobile viewport; cap config validity confirmed against existing native dirs — all automatable without a native build. Device-level deep-link resolution + real-ID substitution explicitly deferred to Phase 70.
</success_criteria>

<output>
Create `.planning/phases/53-capacitor-mobile-shells-ios-android/53-02-SUMMARY.md` when done. Record any SettingsPage reachability fix made for MOBILE-08, and the Phase 70 deferral items (RC key values, TEAMID, SHA256).
</output>
