---
phase: 29
plan: 05
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - supabase/functions/clinic-patient-invite/index.ts
  - supabase/functions/clinic-patient-invite/cors.ts
  - supabase/functions/clinic-patient-invite/deno.json
  - supabase/functions/clinic-patient-invite/clinic-patient-invite.test.ts
  - src/lib/clinic-patient-invite.ts
  - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-05-SUMMARY.md
autonomous: true
requirements: [ORG-10]
tags: [edge-function, magic-link, resend, invites, secdef]
must_haves:
  truths:
    - "Edge Function clinic-patient-invite exists with 3 endpoints: POST /send, POST /preview, POST /accept"
    - "POST /send: JWT-authed clinic admin; calls send_org_patient_invite RPC; W-1 invariant identical-200 response shape regardless of email existence (mirrors clinic-org-invite/send precedent)"
    - "POST /send: dispatches non-PHI invite email via _shared/email-router.ts (phi:false template — magic-link not embedded here; sent only on accept)"
    - "POST /preview: anonymous-callable; calls accept_org_patient_invite_preview RPC; returns {org_name, org_logo_url, scope_summary} OR 404 (identical for invalid/expired per W-1)"
    - "POST /accept: anonymous + token-authed; (a) calls admin.generateLink({type: 'magiclink', email}) to mint magic link (D-08 step f via admin.generateLink per RESEARCH Resolved-Recommendation §1); (b) calls accept_org_patient_invite RPC to commit consent grants; (c) returns {ok: true, redirect_url: action_link}"
    - "Two-phase pattern per RESEARCH Open Q3: RPC commits atomically THEN admin.generateLink fires post-commit; if generateLink fails, invite is marked accepted but no magic-link; Sentry captures the edge case"
    - "Startup health check warns when RESEND_API_KEY absent (per [[reference_vendor_gated_send_health_check]]); invites still persist; emails skipped"
    - "RESEND_API_KEY + RESEND_FROM env secrets exist in Function Secrets (or warning logged)"
    - "src/lib/clinic-patient-invite.ts (browser-side) exports sendPatientInvite(orgId, email, scope) for the clinic admin billing UI (Plan 29-06 consumer)"
    - "Deno test proves: (a) W-1 identical-200 shape for new vs existing email, (b) 404 from /preview for invalid token, (c) /accept two-phase pattern — RPC mock fires before generateLink, (d) PHI-keyword grep on the email body template returns 0 hits"
  artifacts:
    - path: "supabase/functions/clinic-patient-invite/index.ts"
      provides: "Edge Function with 3 routes: /send /preview /accept"
      contains: "Deno.serve"
    - path: "supabase/functions/clinic-patient-invite/clinic-patient-invite.test.ts"
      provides: "Deno tests for W-1 + two-phase + 404"
      contains: "Deno.test"
    - path: "src/lib/clinic-patient-invite.ts"
      provides: "Browser-side helper: sendPatientInvite, previewInvite, acceptInvite"
      contains: "export async function sendPatientInvite"
  key_links:
    - from: "clinic-patient-invite/index.ts /send"
      to: "send_org_patient_invite RPC (Plan 29-02)"
      via: "admin.rpc('send_org_patient_invite', {...})"
      pattern: "rpc.*send_org_patient_invite"
    - from: "clinic-patient-invite/index.ts /send"
      to: "_shared/email-router.ts (Phase 25)"
      via: "sendEmail({phi: false, template: 'clinic-patient-invite', ...})"
      pattern: "email-router|sendEmail"
    - from: "clinic-patient-invite/index.ts /accept"
      to: "Supabase Auth admin.generateLink"
      via: "adminClient.auth.admin.generateLink({type: 'magiclink', email, options: {redirectTo}})"
      pattern: "admin.generateLink"
    - from: "src/lib/clinic-patient-invite.ts"
      to: "Edge Function (browser side)"
      via: "fetch + Authorization Bearer JWT"
      pattern: "fetch.*clinic-patient-invite"
---

<objective>
Ship the ORG-10 Edge Function and browser-side helper. Direct blueprint = Phase 28 `clinic-org-invite/index.ts` (D-07). Routes differ: org-admin invite (clinic-org-invite) vs patient consent invite (clinic-patient-invite).

Purpose: this is the runtime that ties the Plan 29-02 RPC layer to a real user-facing flow. Without it, clinic admins have a write API but no UI plumbing; patients have nothing to click.

Output: Edge Function (3 routes) + deno tests + browser-side helper + Function Secrets verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-RESEARCH.md
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-02-SUMMARY.md
@.planning/phases/25-hipaa-audit-hardening-vendor-baa-chain/25-CONTEXT.md

<interfaces>
From supabase/functions/clinic-org-invite/index.ts (DIRECT BLUEPRINT — verified via file read):
  - W-1 anti-enumeration: ALWAYS returns `{ok: true, invite_id}`
  - Startup health check: `if (!Deno.env.get('RESEND_API_KEY')) console.warn(...)`
  - CORS: `*` Origin, no credentials
  - JWT auth via Authorization Bearer
  - Lazy admin client via `_createServiceRoleClientUnsafe()`

From supabase/functions/_shared/email-router.ts (Phase 25 D-03):
  - export async function sendEmail(opts: {phi: boolean, template: string, to: string, vars: Record<string, unknown>}): Promise<void>
  - phi:false → Resend route; phi:true → AWS SES route (when SES BAA is signed)

From src/lib/clinic.ts:
  - hashInviteToken(rawToken: string): Promise<string> — SHA-256 hex (line 505)
  - generateInviteToken(): string — 32 bytes hex random (search for symbol if needed)

From src/lib/auth.ts (Phase 5):
  - signInWithMagicLink(email: string): Promise<{error?: AuthError}> — NOT used in this plan (per RESEARCH Resolved-Recommendation §1; admin.generateLink chosen instead)

From @supabase/supabase-js v2 admin API:
  - adminClient.auth.admin.generateLink({type: 'magiclink', email: string, options: {redirectTo: string}}): Promise<{data: {properties: {action_link: string, hashed_token: string}}, error}>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author Edge Function (3 routes) + deno tests + deploy</name>
  <files>
    - supabase/functions/clinic-patient-invite/index.ts
    - supabase/functions/clinic-patient-invite/cors.ts
    - supabase/functions/clinic-patient-invite/deno.json
    - supabase/functions/clinic-patient-invite/clinic-patient-invite.test.ts
  </files>
  <read_first>
    - supabase/functions/clinic-org-invite/index.ts (FULL file — direct blueprint per D-07)
    - supabase/functions/clinic-org-invite/cors.ts (verbatim copy target)
    - supabase/functions/clinic-org-invite/deno.json (verbatim copy target)
    - supabase/functions/clinic-org-invite/index.test.ts (test-shape precedent — read closely for service-role client mocking, JWT mocking, response assertion patterns)
    - supabase/functions/_shared/email-router.ts (sendEmail signature + phi:false branch behavior)
    - supabase/functions/_shared/supabase-server.ts (_createServiceRoleClientUnsafe)
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md §D-07, D-08
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-RESEARCH.md §Pattern 2 (admin.generateLink verbatim) + §Resolved Recommendations §1 (two-phase rationale) + §Open Q3 (transactional boundary)
    - src/lib/clinic.ts §hashInviteToken (line 505) + generateInviteToken
  </read_first>
  <behavior>
    - Test 1 (W-1 send): POST /send with new email returns `{ok: true, invite_id: '<uuid>'}` shape.
    - Test 2 (W-1 send): POST /send with existing-in-auth.users email returns IDENTICAL shape `{ok: true, invite_id: '<uuid>'}` (no extra field, same status code).
    - Test 3 (auth gate): POST /send without Authorization header returns 401.
    - Test 4 (PHI lint): the email body template literal in index.ts contains NO PHI keywords (patient_name, diagnosis, dose, mg, mcg, units, condition) — grep negative test.
    - Test 5 (preview 404): POST /preview with non-existent token_hash returns 404 with body `{ok: false, error: 'invite_not_found'}`.
    - Test 6 (preview 404 expired): POST /preview with valid-but-expired token returns IDENTICAL 404 body (anti-enumeration).
    - Test 7 (preview happy): POST /preview with valid pending token returns `{ok: true, org_name, org_logo_url, scope_summary}`.
    - Test 8 (accept two-phase): /accept handler calls accept_org_patient_invite RPC BEFORE generateLink (mocked order verified); if RPC throws, generateLink is NOT called; if generateLink throws AFTER RPC commits, Sentry captures and response is `{ok: false, error: 'magic_link_failed', invite_accepted: true}`.
    - Test 9 (startup health check): function instantiates without RESEND_API_KEY env and emits warn log (test via console intercept).
  </behavior>
  <action>
    Create `supabase/functions/clinic-patient-invite/`:

    **`cors.ts`**: copy verbatim from `supabase/functions/clinic-org-invite/cors.ts`.

    **`deno.json`**: copy from `clinic-org-invite/deno.json`, add no new deps (uses `_shared/`).

    **`index.ts`**:
    Top-level imports:
    - `import { createClient } from 'npm:@supabase/supabase-js@2';` (for admin.generateLink which requires the full client — `_createServiceRoleClientUnsafe` returns a typed wrapper; the auth.admin API needs the underlying SupabaseClient. Use _createServiceRoleClientUnsafe + cast OR use it directly if it exposes `.auth.admin`).
    - `import * as Sentry from '../_shared/sentry.ts';`
    - `import { sendEmail } from '../_shared/email-router.ts';` (Phase 25)
    - `import { corsHeaders } from './cors.ts';`

    Startup health check: `if (!Deno.env.get('RESEND_API_KEY')) console.warn('[clinic-patient-invite] RESEND_API_KEY missing; invites will persist but emails will NOT be sent');`. Also: `if (!Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')) throw new Error('SUPABASE_SERVICE_ROLE_KEY required')`.

    Constants:
    - `const APP_ORIGIN = Deno.env.get('PUBLIC_APP_ORIGIN') ?? 'https://leanshot.app';`
    - `const RESEND_FROM = Deno.env.get('RESEND_FROM') ?? 'LeanShot <noreply@app.leanshot.app>';`

    Helper `generateRawToken()`: `return crypto.getRandomValues(new Uint8Array(32)).reduce((s,b)=>s+b.toString(16).padStart(2,'0'),'')` — 64-hex-char.
    Helper `hashToken(raw)`: SHA-256 over UTF-8 raw → hex (same as Phase 9 `makeInviteTokenHash`).

    Route dispatcher in `Deno.serve(async (req) => { ... })`:
    - CORS preflight: `if (req.method === 'OPTIONS') return new Response('ok', { headers: corsHeaders });`
    - Parse URL path: `/send`, `/preview`, `/accept`. Anything else → 404.

    **`/send` route** (JWT-required):
    1. Extract `Authorization: Bearer <jwt>` → return 401 if missing.
    2. Parse body: `{org_id: uuid, patient_email: string, consent_scope: jsonb}`.
    3. Create user-scoped client: `const userClient = createClient(SUPABASE_URL, SUPABASE_ANON_KEY, { global: { headers: { Authorization: 'Bearer ' + jwt }}})`.
    4. Generate raw token + hash.
    5. Call RPC via userClient: `await userClient.rpc('send_org_patient_invite', {p_org_id, p_patient_email, p_consent_scope, p_invite_token_hash: hash})`. RPC does the Pattern S1 admin check; if not admin, RPC raises 42501 → return 403.
    6. On success: dispatch email via `sendEmail({phi: false, template: 'clinic-patient-invite', to: patient_email, vars: {invite_url: \`${APP_ORIGIN}/accept-clinic-invite?token=${rawToken}\`, expires_days: 14}})`. NO patient name, NO org-clinical context in vars (just URL + expiry).
    7. Return `Response.json({ok: true, invite_id: data.invite_id}, {status: 200, headers: corsHeaders})` — W-1 identical shape.

    **`/preview` route** (anonymous):
    1. Parse body: `{token: string}`.
    2. Hash token: `const hash = await hashToken(token)`.
    3. Admin client: `const admin = _createServiceRoleClientUnsafe()`.
    4. Call RPC: `await admin.rpc('accept_org_patient_invite_preview', {p_invite_token_hash: hash})`.
    5. On error (P0002 or any): return `Response.json({ok: false, error: 'invite_not_found'}, {status: 404, headers: corsHeaders})` — anti-enumeration identical for invalid/expired/used.
    6. On success: return `Response.json({ok: true, ...data}, {status: 200, headers: corsHeaders})`.

    **`/accept` route** (anonymous; client provides patient email + decision):
    1. Parse body: `{token: string, patient_email: citext}`.
    2. Hash token.
    3. Admin client.
    4. **Phase 1 (commit):** lookup-or-create `auth.users` row for `patient_email`:
       - `const { data: existing } = await admin.auth.admin.getUserByEmail(patient_email);`
       - If absent, `await admin.auth.admin.createUser({email: patient_email, email_confirm: false});` then re-fetch id.
       - Capture `patient_user_id`.
    5. Call RPC: `const { data: rpcRes, error: rpcErr } = await admin.rpc('accept_org_patient_invite', {p_invite_token_hash: hash, p_patient_user_id: patient_user_id});`. If error: return 4xx with anti-enumeration message.
    6. **Phase 2 (post-commit best-effort):** `const { data: linkData, error: linkErr } = await admin.auth.admin.generateLink({type: 'magiclink', email: patient_email, options: {redirectTo: \`${APP_ORIGIN}/dashboard?welcome=clinic\`}});`
    7. If linkErr: `Sentry.captureMessage('accept_org_patient_invite generateLink failed — invite accepted but no magic-link', {level: 'warning', extra: {patient_email_hash: hash}});` return `Response.json({ok: false, error: 'magic_link_failed', invite_accepted: true}, {status: 200, headers: corsHeaders})` — patient can request password reset.
    8. On success: return `Response.json({ok: true, redirect_url: linkData.properties.action_link}, {status: 200, headers: corsHeaders})`.

    **`clinic-patient-invite.test.ts`**:
    Behavior tests 1-9 above, using fetch-mock + Deno.test pattern (mirror `clinic-org-invite/index.test.ts` test shape):
    - Tests 1-3: mock admin.rpc, mock sendEmail; instantiate `Deno.serve` handler via `import { default as handler }` — note `Deno.serve` returns a server but the registered handler is callable directly if structured as `export async function handler(req: Request): Promise<Response> { ... }; Deno.serve(handler);`. Refactor index.ts to export `handler` for testability.
    - Test 4 (PHI lint): `const src = await Deno.readTextFile('supabase/functions/clinic-patient-invite/index.ts'); for (const kw of ['patient_name','diagnosis','dose','mg','mcg']) { assertEquals(src.toLowerCase().includes(kw.toLowerCase()), false, \`PHI keyword "\${kw}" found in template\`); }` — but include allowlist for code paths that LEGITIMATELY mention these (e.g., none expected — strict check).
    - Tests 5-9: mock RPC responses + assert response shape.

    Run: `deno test supabase/functions/clinic-patient-invite/clinic-patient-invite.test.ts --allow-env --allow-net=esm.sh,jsr.io --allow-read`. All pass.

    Deploy: `supabase functions deploy clinic-patient-invite`. Verify: `supabase functions list --linked | grep clinic-patient-invite`.

    Set Function Secrets (re-use existing — both already in scope per RESEARCH §Environment Availability):
    - `supabase secrets list --linked | grep RESEND_API_KEY` — verify present (set up in clinic-org-invite). If absent: `supabase secrets set RESEND_API_KEY=re_xxx --linked` (HUMAN-CHECKPOINT in Plan 29-07 if value unknown — Plan 29-05 ships with the warning logged, vendor-gated per [[reference_vendor_gated_send_health_check]]).
  </action>
  <verify>
    <automated>test -f supabase/functions/clinic-patient-invite/index.ts && test -f supabase/functions/clinic-patient-invite/clinic-patient-invite.test.ts && grep -q "admin.generateLink" supabase/functions/clinic-patient-invite/index.ts && grep -q "send_org_patient_invite\|accept_org_patient_invite" supabase/functions/clinic-patient-invite/index.ts && grep -q "email-router\|sendEmail" supabase/functions/clinic-patient-invite/index.ts && grep -q "RESEND_API_KEY" supabase/functions/clinic-patient-invite/index.ts && deno test supabase/functions/clinic-patient-invite/clinic-patient-invite.test.ts --allow-env --allow-net=esm.sh,jsr.io --allow-read 2>&1 | tee /tmp/p29_05_deno.txt | grep -E 'ok \| [0-9]+ passed \| 0 failed' && supabase functions list --linked 2>&1 | grep -q clinic-patient-invite</automated>
  </verify>
  <acceptance_criteria>
    - Function dir exists with index.ts + cors.ts + deno.json + clinic-patient-invite.test.ts.
    - index.ts uses `_createServiceRoleClientUnsafe` OR `createClient` (admin.generateLink requires SupabaseClient).
    - index.ts imports `sendEmail` from `../_shared/email-router.ts` (NOT a direct Resend HTTP call — Phase 25 D-03 router enforced).
    - index.ts calls `admin.generateLink({type: 'magiclink', ...})` per RESEARCH Resolved-Recommendation §1.
    - Email body template contains NO PHI keywords (D-11 / D-07 / Phase 25 D-12); deno test #4 enforces.
    - W-1 invariant: identical 200 shape for new vs existing email (test #1+2).
    - Two-phase pattern: RPC commits FIRST, generateLink AFTER (test #8 mock-order assertion).
    - 9 deno tests pass.
    - Function deployed live.
  </acceptance_criteria>
  <done>Edge Function shipped + deployed; ORG-10 runtime layer complete.</done>
</task>

<task type="auto">
  <name>Task 2: Browser-side helper src/lib/clinic-patient-invite.ts + Plan 05 SUMMARY + commit</name>
  <files>
    - src/lib/clinic-patient-invite.ts
    - .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-05-SUMMARY.md
  </files>
  <read_first>
    - src/lib/clinic.ts (full file — adopt the existing RPC wrapper + Result discriminated-union pattern)
    - src/lib/auth.ts (magic-link helper signature — for reference; do NOT call directly per Resolved Recommendation §1)
    - $HOME/.claude/get-shit-done/templates/summary.md
    - /tmp/p29_05_deno.txt
  </read_first>
  <action>
    Create `src/lib/clinic-patient-invite.ts` exporting three async functions:

    ```typescript
    // Browser-side helpers for the clinic-patient-invite Edge Function.
    // Consumer: Plan 29-06 ClinicBillingCard / ConsentAcceptScreen.
    // All requests use fetch + Authorization Bearer JWT (admin) OR anonymous (preview/accept).
    // Result type follows src/lib/clinic.ts Result<T, E> discriminated-union pattern.

    import { supabase } from './supabaseClient'; // (or whatever the existing import is — match src/lib/clinic.ts)

    const EDGE_URL = `${import.meta.env.VITE_SUPABASE_URL}/functions/v1/clinic-patient-invite`;

    export type InviteResult<T> = { ok: true; data: T } | { ok: false; error: string };

    export async function sendPatientInvite(orgId: string, patientEmail: string, consentScope: Record<string, unknown>): Promise<InviteResult<{ invite_id: string }>> { ... }

    export async function previewInvite(token: string): Promise<InviteResult<{ org_name: string; org_logo_url: string | null; scope_summary: Record<string, unknown> }>> { ... }

    export async function acceptInvite(token: string, patientEmail: string): Promise<InviteResult<{ redirect_url: string }>> { ... }
    ```

    Each function:
    - For `sendPatientInvite`: fetch with `Authorization: Bearer <session.access_token>` from `(await supabase.auth.getSession()).data.session?.access_token`. POST body = `{org_id, patient_email, consent_scope}`.
    - For `previewInvite` + `acceptInvite`: NO Authorization header (anonymous routes).
    - Parse JSON, map to `InviteResult<T>` discriminated union.
    - Re-throw on network error → caller catches and renders error UI.

    Write SUMMARY per template + commit pathspec: `git add supabase/functions/clinic-patient-invite/ src/lib/clinic-patient-invite.ts .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-05-SUMMARY.md && git commit -m "feat(29-05): clinic-patient-invite Edge Fn (send+preview+accept) + browser helper"`.
  </action>
  <verify>
    <automated>test -f src/lib/clinic-patient-invite.ts && grep -q "export async function sendPatientInvite" src/lib/clinic-patient-invite.ts && grep -q "export async function previewInvite" src/lib/clinic-patient-invite.ts && grep -q "export async function acceptInvite" src/lib/clinic-patient-invite.ts && test -f .planning/phases/29-org-subscriptions-per-patient-metered-billing/29-05-SUMMARY.md && npx tsc --noEmit 2>&1 | grep -c "src/lib/clinic-patient-invite.ts.*error" | grep -v -E '^[1-9]' && git log --oneline -1 | grep -q "29-05"</automated>
  </verify>
  <acceptance_criteria>
    - Helper file exists at `src/lib/clinic-patient-invite.ts` exporting `sendPatientInvite`, `previewInvite`, `acceptInvite`.
    - All three return a discriminated-union `InviteResult<T>` (`ok: true | false`).
    - TypeScript compiles cleanly (no errors in this file).
    - SUMMARY exists with Verification Evidence quoting deno test pass-line.
    - Commit references both Edge Function + helper + SUMMARY.
  </acceptance_criteria>
  <done>Plan 29-05 deliverable complete; UI plan (29-06) has typed browser helpers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clinic admin browser → /send | JWT-authed write — Pattern S1 dual-layer |
| Anonymous browser → /preview | Anti-enumeration read |
| Anonymous browser → /accept | Two-phase commit + magic-link mint |
| Edge Fn → Resend API | Email dispatch (non-PHI) |
| Edge Fn → Supabase Auth admin API | User-create + magic-link generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Information Disclosure | Email enumeration via /send | mitigate | W-1 identical-200 shape; Plan 29-02 send RPC inserts unconditionally; deno tests 1+2 enforce. |
| T-29-05-02 | Information Disclosure | PHI leakage in invite email | mitigate | Phase 25 D-12 alert-email PHI-free design enforced via email-router phi:false + deno test 4 PHI-keyword grep. |
| T-29-05-03 | Tampering | Token brute-force on /accept | mitigate | invite_token_hash 256-bit; 14-day expiry; preview rate-limited by Stripe-of-the-realtime (not enforced in Plan 29-05 — accept naturally limited by Supabase Auth magic-link rate limit ~2/hour). |
| T-29-05-04 | Spoofing | Forged /accept POST with stolen token | accept | Magic link follow-through requires email-deliverable mailbox; token alone does NOT grant session (magic-link click + Supabase verify is the auth boundary). |
| T-29-05-05 | Elevation of Privilege | /send called by non-admin org member | mitigate | RPC `send_org_patient_invite` Pattern S1 re-check raises 42501; Edge Fn forwards 403 to client. |
| T-29-05-06 | Repudiation | Patient disputes acceptance | mitigate | accept RPC writes `org_consent_grants` + `log_admin_action` audit row; Phase 24 append-only RLS prevents tampering. |
| T-29-05-07 | Denial of Service | Two-phase pattern leaves invite accepted but no magic-link | accept | Per RESEARCH Open Q3: patient can request password reset; Sentry captures the edge case for ops review; rare condition. |
</threat_model>

<verification>
- Edge Fn deployed live + 9 deno tests pass (Task 1 automated).
- Helper file exists + TS compiles (Task 2 automated).
- SUMMARY committed (Task 2 automated).
- PHI-keyword grep on email body returns 0 hits (Task 1 test #4).
</verification>

<success_criteria>
- ORG-10 SC#3: clinic admin sends invite → patient receives email → patient accepts → primary_org_id set + consent grant recorded.
- W-1: identical 200 response for new email vs existing email (test enforced).
- Two-phase atomicity: RPC commits before generateLink; if generateLink fails, invite is marked accepted (audit row written) and Sentry alert fires.
</success_criteria>

<output>
After completion, create `.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-05-SUMMARY.md`.
</output>
