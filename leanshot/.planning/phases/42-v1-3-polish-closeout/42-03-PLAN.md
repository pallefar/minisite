---
phase: 42-v1-3-polish-closeout
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/package.json
  - leanshot/package-lock.json
  - leanshot/src/index.css
  - leanshot/e2e/visual/dark-mode.spec.ts
  - leanshot/e2e/visual/__snapshots__/dark-mode
  - leanshot/playwright.config.ts
autonomous: false
requirements:
  - POLISH-08

must_haves:
  truths:
    - "Tailwind v4 beta version is PINNED (no caret) to a known-good release that supports @variant dark attribute selector (Pitfall 7)"
    - "data-theme='dark' attribute selector and Tailwind v4 dark: utility classes coexist without specificity collisions"
    - "Every v1.3 new surface (admin shell, helpdesk, onboarding builder, clinic dashboard, community, courses) has dark variants for its tokens defined in src/index.css under [data-theme='dark']"
    - "Playwright VR snapshots exist for the 6 v1.3 surfaces in BOTH light + dark modes (12 snapshots total)"
    - "No NEW contrast regressions introduced (verified via axe contrast rule from 42-02)"
  artifacts:
    - path: "leanshot/src/index.css"
      provides: "Extended @theme + [data-theme='dark'] blocks with all v1.3 surface tokens"
      contains: "color-admin-shell-bg"
    - path: "leanshot/e2e/visual/dark-mode.spec.ts"
      provides: "Playwright VR test that visits each v1.3 surface in light + dark and snapshots a stable frame"
      min_lines: 80
    - path: "leanshot/e2e/visual/__snapshots__/dark-mode"
      provides: "12 PNG baseline snapshots (6 surfaces × 2 themes)"
  key_links:
    - from: "leanshot/src/index.css"
      to: "useTheme.applyThemeToDOM"
      via: "data-theme attribute toggles the [data-theme='dark'] selector block"
      pattern: "\\[data-theme=['\"]dark['\"]\\]"
    - from: "leanshot/e2e/visual/dark-mode.spec.ts"
      to: "leanshot/src/index.css tokens"
      via: "Playwright sets localStorage[leanshot_theme_v4]=dark then reloads + snapshots"
      pattern: "localStorage\\.setItem.*leanshot_theme_v4"
---

<objective>
Achieve POLISH-08 dark mode parity across ALL six v1.3 new surfaces: admin shell, helpdesk, onboarding builder, clinic dashboard, community feed, course player. Pin Tailwind v4 beta to a known-good version (Pitfall 7), extend src/index.css [data-theme='dark'] block with the missing tokens, then lock the result with 12 Playwright VR snapshots (6 surfaces × {light, dark}) per RESEARCH §Pattern 5 audit-approach recommendation.

Purpose: Existing useTheme + applyThemeToDOM pattern (v1.1) is sound; the missing piece is per-v1.3-surface token coverage and a regression gate. Manual review alone is brittle; full-page VR diff is over-investment; 12 targeted snapshots is the Goldilocks middle.

Output: Pinned Tailwind v4 + updated index.css + 12 baseline VR snapshots + a Playwright test that fails on dark-mode regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/src/index.css
@/Users/karstenhaldan/minisite/leanshot/src/hooks/useTheme.ts
@/Users/karstenhaldan/minisite/leanshot/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Tailwind v4 beta version and configure @variant dark attribute selector</name>
  <files>leanshot/package.json, leanshot/package-lock.json, leanshot/src/index.css</files>
  <action>From leanshot/, run `npm view tailwindcss versions --json | jq -r '.[]' | grep -E '^4\.0\.0-beta' | tail -10` to see the latest beta. Pick the most recent beta (current is beta.7 per CLAUDE.md; check if beta.8+ has shipped). PIN BOTH `tailwindcss` AND `@tailwindcss/vite` to the EXACT same beta version (no caret prefix) per Pitfall 7 and Assumption A2. Edit leanshot/package.json: change `"tailwindcss": "^4.0.0-beta.7"` → `"tailwindcss": "4.0.0-beta.X"` and same for @tailwindcss/vite. Run `npm install` to refresh the lockfile. Then add to the top of leanshot/src/index.css (immediately after `@import 'tailwindcss';`): `@variant dark (&:where([data-theme='dark'], [data-theme='dark'] *));` per RESEARCH Pitfall 7 fix. This makes the Tailwind `dark:` utility prefix resolve to our existing data-theme attribute (not prefers-color-scheme). Verify by adding a test element with `class="bg-white dark:bg-black"` to a temporary fixture page and toggling [data-theme='dark'] — both should work.</action>
  <verify>
    <automated>cd leanshot && grep -E '"tailwindcss":\s*"4\.0\.0-beta\.[0-9]+"' package.json && grep -E '"@tailwindcss/vite":\s*"4\.0\.0-beta\.[0-9]+"' package.json && grep -F "@variant dark (&:where([data-theme=" src/index.css</automated>
  </verify>
  <done>tailwindcss + @tailwindcss/vite pinned to identical exact beta version (no caret); @variant dark rule appears in src/index.css; npm run build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Extend [data-theme='dark'] block with v1.3 surface tokens</name>
  <files>leanshot/src/index.css</files>
  <action>Read leanshot/src/index.css fully. Identify the existing @theme {} block and [data-theme='dark'] selector block. ADD inside the [data-theme='dark'] block the following v1.3 surface tokens (using values from RESEARCH §Pattern 5 example as starting points, adjust by inspecting the v1.3 surface palettes in src/components/admin/*, src/components/helpdesk/*, src/components/clinic/*, src/components/community/*, src/components/courses/*, src/components/onboarding/builder/*): --color-admin-shell-bg, --color-admin-sidebar, --color-admin-table-row, --color-admin-table-row-hover, --color-helpdesk-msg-bg, --color-helpdesk-msg-bg-mine, --color-helpdesk-sidebar, --color-clinic-dashboard-card, --color-clinic-patient-row, --color-community-post-bg, --color-community-reaction-bar, --color-course-player-bg, --color-course-lesson-row, --color-course-progress-fill, --color-onboarding-builder-canvas, --color-onboarding-builder-block-bg, --color-onboarding-builder-handle. Also ADD the corresponding light-mode values to the existing @theme block where missing. Per Pitfall 7: after writing, run `npm run build` and inspect the generated CSS (in leanshot/dist/) to ensure both the [data-theme='dark'] custom prop overrides AND Tailwind `dark:` utility classes resolve correctly. Document each token with an inline `/* POLISH-08 v1.3 SURFACE: <surface-name> */` comment for future audit.</action>
  <verify>
    <automated>cd leanshot && grep -c "POLISH-08 v1.3 SURFACE" src/index.css | awk '{ if ($1 >= 6) print "OK: " $1 " surfaces tagged"; else { print "FAIL: only " $1 " surfaces tagged"; exit 1 } }' && cd leanshot && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>At least 6 unique POLISH-08 v1.3 SURFACE comments in src/index.css; all token names cited above appear under both @theme and [data-theme='dark'] blocks; production build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright VR snapshots for 6 surfaces × {light, dark}</name>
  <files>leanshot/e2e/visual/dark-mode.spec.ts, leanshot/e2e/visual/__snapshots__/dark-mode, leanshot/playwright.config.ts</files>
  <action>Create leanshot/e2e/visual/dark-mode.spec.ts using the project's existing Playwright pattern. Per memory reference_playwright_state_seeding, use `page.addInitScript()` to seed localStorage BEFORE first navigation: `localStorage.setItem('leanshot_theme_v4', theme)` and a minimal authenticated-user seed for routes that require auth. Per memory reference_playwright_conditional_project_argv, gate the visual project via env var (PLAYWRIGHT_VISUAL_RUN=1) NOT argv-sniffing. For each of the 6 surfaces × 2 themes (admin → /admin, helpdesk → /helpdesk, onboarding builder → /admin/onboarding/builder, clinic dashboard → /clinic/test-clinic/dashboard, community → /community/feed, courses → /courses/getting-started), navigate the page, wait for a deterministic ready signal (e.g. wait for a specific element to be visible — pick a stable selector per surface; do NOT use arbitrary timeouts), then `await expect(page).toHaveScreenshot('<surface>-<theme>.png', { maxDiffPixelRatio: 0.01 })`. Add to leanshot/playwright.config.ts a new project entry `visual` with viewport 1280x800 and `expect.toHaveScreenshot.threshold: 0.2`. Initial snapshots are created via `cd leanshot && PLAYWRIGHT_VISUAL_RUN=1 npx playwright test --project=visual --update-snapshots`. COMMIT the 12 generated PNGs under e2e/visual/__snapshots__/dark-mode/.</action>
  <verify>
    <automated>cd leanshot && ls -1 e2e/visual/__snapshots__/dark-mode/*.png 2>/dev/null | wc -l | awk '{ if ($1 >= 12) print "OK: " $1 " snapshots"; else { print "FAIL: only " $1 "/12 snapshots"; exit 1 } }'</automated>
  </verify>
  <done>12 PNG snapshots committed; dark-mode.spec.ts runs PLAYWRIGHT_VISUAL_RUN=1 successfully against the baselines.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN — Visual review of 12 dark-mode snapshots</name>
  <what-built>12 Playwright VR baseline snapshots covering the 6 v1.3 new surfaces in both light and dark themes.</what-built>
  <how-to-verify>
    1. Open `leanshot/e2e/visual/__snapshots__/dark-mode/` in Finder (or the IDE's image preview).
    2. For each surface (admin, helpdesk, onboarding-builder, clinic-dashboard, community, courses) inspect the dark.png variant:
       - Background tokens render as a dark surface (not white)
       - Text contrast is readable (aim for WCAG AA contrast — 4.5:1 for body text)
       - Card / row / panel borders are visible (not lost in the dark background)
       - No "light flash" zones where a v1.3 surface token was missed
    3. Cross-check against the corresponding light.png variant — they should be visually parallel (same layout, same focal point, just different tones).
    4. If any surface looks broken, identify the missing token and add it to src/index.css; re-run `PLAYWRIGHT_VISUAL_RUN=1 npx playwright test --project=visual --update-snapshots`.
  </how-to-verify>
  <resume-signal>Type "dark-mode-approved" or list which surfaces need token fixes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visual regression test → main branch | Prevents accidental dark-mode regressions during follow-up work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03-01 | Tampering | VR snapshots silently regenerated to mask regression | mitigate | PR review surface; snapshots are PNGs in git so reviewers see the diff |
| T-42-03-02 | Denial of Service | Flaky snapshots on font/animation timing | mitigate | Deterministic wait for stable selector; threshold 0.2; maxDiffPixelRatio 0.01 (loose enough for anti-alias jitter) |
| T-42-03-SC | Tampering | tailwindcss + @tailwindcss/vite pin to beta | accept | Already in stack; pinning REDUCES supply-chain risk vs floating caret |
</threat_model>

<verification>
- `cd leanshot && npm run build` succeeds with the pinned Tailwind v4 beta.
- All 12 VR snapshots committed and the visual test passes against them.
- The 42-02 axe-core CI gate from the parallel Wave 1 plan shows no NEW contrast violations on any v1.3 surface (cross-check after both plans land).
</verification>

<success_criteria>
- Toggling [data-theme='dark'] on `<html>` changes every v1.3 surface from light to dark without leaving any element in the wrong palette.
- A follow-up branch that breaks a token (e.g. removes --color-admin-shell-bg from dark block) causes the Playwright visual project to fail loudly.
</success_criteria>

<output>
After completion, create `.planning/phases/42-v1-3-polish-closeout/42-03-SUMMARY.md`.
</output>
