---
phase: 42-v1-3-polish-closeout
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/functions/_spike_web_push/index.ts
  - supabase/functions/_spike_web_push/README.md
  - .planning/phases/42-v1-3-polish-closeout/42-01-SPIKE-RESULT.md
autonomous: false
requirements:
  - POLISH-05

must_haves:
  truths:
    - "VAPID keypair generated and stored as Supabase Function Secrets VAPID_PRIVATE_KEY + VAPID_PUBLIC_KEY"
    - "A Deno Edge Function can sign a VAPID ECDSA P-256 JWT and send a single web-push notification end-to-end to FCM or APNS"
    - "The signing path is decided: npm:web-push@3.6.7 (preferred) OR hand-rolled crypto.subtle (documented fallback)"
    - "Wave 2 notification Edge Fn (42-08) has an unblocked import recipe written in 42-01-SPIKE-RESULT.md"
  artifacts:
    - path: "supabase/functions/_spike_web_push/index.ts"
      provides: "Disposable Deno Edge Fn that signs VAPID JWT + dispatches one push; logs success/failure"
    - path: ".planning/phases/42-v1-3-polish-closeout/42-01-SPIKE-RESULT.md"
      provides: "Documented decision: npm:web-push specifier works (or fallback recipe with concrete crypto.subtle implementation hints)"
  key_links:
    - from: "supabase/functions/_spike_web_push/index.ts"
      to: "VAPID_PRIVATE_KEY (Supabase Function Secret)"
      via: "Deno.env.get('VAPID_PRIVATE_KEY')"
      pattern: "Deno\\.env\\.get\\(['\"]VAPID_PRIVATE_KEY['\"]\\)"

user_setup:
  - service: vapid
    why: "Web Push requires a VAPID ECDSA P-256 keypair; the private key signs every push request; the public key (applicationServerKey) is shipped to the browser"
    env_vars:
      - name: VAPID_PRIVATE_KEY
        source: "Generated via npx web-push generate-vapid-keys; stored as Supabase Function Secret (supabase secrets set VAPID_PRIVATE_KEY=...)"
      - name: VAPID_PUBLIC_KEY
        source: "Generated alongside VAPID_PRIVATE_KEY; stored as Supabase Function Secret AND exposed to the browser via Vite env VITE_VAPID_PUBLIC_KEY (or static fetch endpoint)"
      - name: VAPID_SUBJECT
        source: "mailto:karsten.haldan@gmail.com (literal); required by RFC 8292"
    dashboard_config:
      - task: "Generate VAPID keypair locally and set as Supabase Function Secrets"
        location: "Local CLI: npx web-push generate-vapid-keys; then supabase secrets set VAPID_PRIVATE_KEY=<key> VAPID_PUBLIC_KEY=<key> VAPID_SUBJECT=mailto:karsten.haldan@gmail.com"
---

<objective>
Resolve open question RESEARCH §Open-Questions #2: prove that VAPID ECDSA P-256 signing for Web Push works inside a Supabase Edge Function (Deno) before Wave 2 commits to a signing path. Either npm:web-push@3.6.7 works via the npm: specifier (preferred — uses the canonical library) or it does not, in which case 42-01 documents the hand-rolled crypto.subtle fallback recipe for Wave 2 to consume.

Purpose: Wave 2 (notification backend) is BLOCKED until the signing path is known. Without this spike, plan 42-08 must hedge between two implementations, doubling its scope.

Output: a disposable Edge Fn that successfully sends one push to a real subscription, plus 42-01-SPIKE-RESULT.md recording the decision and concrete code recipe Wave 2 imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: HUMAN — Generate VAPID keypair + set Function Secrets</name>
  <what-built>Nothing yet — this task is purely human-gated because VAPID generation is a one-shot local CLI invocation and the secrets must be set against the live Supabase project ytnsipxxmzgaebkqmokp.</what-built>
  <how-to-verify>
    1. Run locally: `npx web-push generate-vapid-keys`
    2. Capture the two base64url strings printed (public + private)
    3. Set as Supabase Function Secrets via:
       `supabase secrets set VAPID_PRIVATE_KEY='<private>' VAPID_PUBLIC_KEY='<public>' VAPID_SUBJECT='mailto:karsten.haldan@gmail.com' --project-ref ytnsipxxmzgaebkqmokp`
    4. Verify: `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep -E 'VAPID_(PRIVATE|PUBLIC)_KEY|VAPID_SUBJECT'` — three lines expected.
    5. Add `VITE_VAPID_PUBLIC_KEY=<public>` to `leanshot/.env.local` (the public key is safe to expose to the browser; do NOT add the private key).
  </how-to-verify>
  <resume-signal>Type "secrets-set" once `supabase secrets list` shows all three VAPID_* entries</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build disposable web-push spike Edge Function</name>
  <files>supabase/functions/_spike_web_push/index.ts, supabase/functions/_spike_web_push/README.md</files>
  <action>Create a Deno Edge Function at supabase/functions/_spike_web_push/index.ts that accepts POST { endpoint, p256dh, auth, payload } (a single browser push subscription object). PRIMARY path: `import webpush from 'npm:web-push@3.6.7'`, call `webpush.setVapidDetails(Deno.env.get('VAPID_SUBJECT')!, Deno.env.get('VAPID_PUBLIC_KEY')!, Deno.env.get('VAPID_PRIVATE_KEY')!)`, then `await webpush.sendNotification({endpoint, keys:{p256dh, auth}}, payload, { TTL: 60, urgency: 'normal' })`. Return 200 with the FCM/APNS response status code. If the npm: import THROWS at module load OR webpush.sendNotification throws a "crypto" / "subtle" / "ECDSA" / "P-256" related error, FALLBACK: use Deno's built-in crypto.subtle.importKey(JWK) + sign(ECDSA P-256 SHA-256) to forge the VAPID JWT manually (header `{alg:'ES256',typ:'JWT'}`, claims `{aud:<origin from endpoint URL>, exp:<now+12h>, sub:<VAPID_SUBJECT>}`) and POST to the endpoint with headers `Authorization: vapid t=<jwt>, k=<base64url-encoded-public-key>` + `TTL: 60` + `Content-Encoding: aes128gcm`. Add a README.md alongside index.ts explaining this is a Wave 0 spike that will be DELETED after Wave 2 imports the recipe. Deploy via `supabase functions deploy _spike_web_push --project-ref ytnsipxxmzgaebkqmokp` (do NOT pass --linked per memory reference_supabase_functions_deploy_no_linked_flag). Per D-03 the spike does NOT need to exercise urgency:'high' — just prove the signing path works.</action>
  <verify>
    <automated>supabase functions list --project-ref ytnsipxxmzgaebkqmokp | grep -q _spike_web_push</automated>
  </verify>
  <done>Edge Fn deployed and listed in the project; index.ts contains either the npm:web-push primary path or the documented crypto.subtle fallback (or both with a runtime try/catch fan-out)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN — Fire one push end-to-end and record decision in 42-01-SPIKE-RESULT.md</name>
  <what-built>The disposable Edge Fn is deployed; we now need a real browser subscription to invoke it against.</what-built>
  <how-to-verify>
    1. Open Chrome on desktop, visit any HTTPS page on the project domain (app.leanshot.app), open DevTools Console.
    2. Run this snippet (replaces VITE_VAPID_PUBLIC_KEY with the public key from Task 1):
       `const reg = await navigator.serviceWorker.register('data:application/javascript,self.addEventListener("push", e => console.log("PUSH", e.data?.text()))'); await Notification.requestPermission(); const sub = await reg.pushManager.subscribe({ userVisibleOnly: true, applicationServerKey: '<VAPID_PUBLIC_KEY>' }); console.log(JSON.stringify(sub));`
    3. Copy the printed subscription JSON (`endpoint`, `keys.p256dh`, `keys.auth`).
    4. Invoke the spike Fn:
       `curl -X POST https://ytnsipxxmzgaebkqmokp.functions.supabase.co/_spike_web_push -H 'Content-Type: application/json' -d '{"endpoint":"<endpoint>","p256dh":"<p256dh>","auth":"<auth>","payload":"hello from 42-01"}'`
    5. Confirm: (a) curl returns 200 or 201; (b) Chrome DevTools Console logs "PUSH hello from 42-01" from the service worker.
    6. Write `.planning/phases/42-v1-3-polish-closeout/42-01-SPIKE-RESULT.md` with sections: ## Decision (npm:web-push works | fallback required), ## Working import recipe (the exact import line(s) Wave 2 should use), ## Sample successful send (curl + response + console-log evidence), ## Caveats for Wave 2 (any quirks discovered — e.g. specific TTL behavior, urgency header acceptance, p256dh format).
    7. Commit 42-01-SPIKE-RESULT.md.
  </how-to-verify>
  <resume-signal>Type "spike-verified" once 42-01-SPIKE-RESULT.md is committed AND the curl invocation produced a real browser push notification</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge Fn → browser push endpoint (FCM/APNS) | Signed JWT crosses; private key never leaves Function Secret store |
| Local CLI → Supabase Function Secrets | VAPID_PRIVATE_KEY transits HTTPS; never committed to repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-01 | Information Disclosure | VAPID_PRIVATE_KEY | mitigate | Never log; stored only as Supabase Function Secret; not in .env or VITE_* vars |
| T-42-01-02 | Spoofing | Forged push from third party | mitigate | VAPID JWT signed with ECDSA P-256 per RFC 8292; private key access controls keep forge surface server-only |
| T-42-01-03 | Tampering | npm:web-push supply-chain | accept | Pinned version 3.6.7; canonical Deque-style library; widely deployed; supply-chain risk is low and shared with the rest of the npm ecosystem |
| T-42-01-SC | Tampering | npm install of web-push during execute | mitigate | web-push@3.6.7 verified in RESEARCH §Standard-Stack; LEGITIMATE per npm registry; no human checkpoint needed for spike (disposable Fn) |
</threat_model>

<verification>
- Edge Fn deployed and present in `supabase functions list` output.
- Real browser push notification received during Task 3 human verification.
- 42-01-SPIKE-RESULT.md committed with a definitive "Wave 2 should import X" recipe.
- VAPID_PRIVATE_KEY, VAPID_PUBLIC_KEY, VAPID_SUBJECT visible in `supabase secrets list`.
</verification>

<success_criteria>
- Wave 2 (plan 42-08) can copy the import recipe from 42-01-SPIKE-RESULT.md without further investigation.
- VAPID secrets are persistent in the Supabase project Function Secret store.
- The spike Edge Fn exists but is flagged for deletion in Wave 4 (42-11) phase cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/42-v1-3-polish-closeout/42-01-SUMMARY.md` per the standard SUMMARY template, plus 42-01-SPIKE-RESULT.md as a load-bearing artifact for Wave 2.
</output>
