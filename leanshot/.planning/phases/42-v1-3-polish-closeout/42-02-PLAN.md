---
phase: 42-v1-3-polish-closeout
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/package.json
  - leanshot/package-lock.json
  - leanshot/vitest.config.ts
  - leanshot/tests/a11y/axe-baseline.test.ts
  - leanshot/tests/a11y/accessibility-baseline.json
  - leanshot/tests/a11y/routes-manifest.ts
  - leanshot/tests/a11y/render-route.tsx
  - leanshot/src/test-setup.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - POLISH-09

must_haves:
  truths:
    - "axe-core scans EVERY v1.1 + v1.2 + v1.3 route in CI under the WCAG 2.2 AA tag set"
    - "Critical + Serious violations BLOCK PR merge only when count exceeds per-route baseline (D-09)"
    - "Moderate violations are reported in PR comment but do not block (D-10)"
    - "Minor violations are ignored (D-10)"
    - "accessibility-baseline.json is committed and contains the current per-route count for every route in routes-manifest.ts"
    - "CI runs the a11y test in under 60 seconds for the full route set (D-10 fast-CI requirement)"
  artifacts:
    - path: "leanshot/tests/a11y/axe-baseline.test.ts"
      provides: "Vitest test that mounts each route into jsdom and runs axe-core with WCAG 2.2 AA tags; compares blocking count vs baseline"
      min_lines: 60
    - path: "leanshot/tests/a11y/accessibility-baseline.json"
      provides: "Per-route blocking + moderate violation counts captured at phase entry"
    - path: "leanshot/tests/a11y/routes-manifest.ts"
      provides: "Exported `routesManifest` array of {path, mountComponent} entries enumerating every scanable route"
      min_lines: 30
    - path: "leanshot/tests/a11y/render-route.tsx"
      provides: "renderRoute(path) helper that mounts the App component into jsdom and returns the JSDOM instance"
    - path: ".github/workflows/ci.yml"
      provides: "CI step `test:a11y` runs vitest a11y suite; PR comment posted if Moderate count rises"
  key_links:
    - from: "leanshot/tests/a11y/axe-baseline.test.ts"
      to: "axe-core@4.11.4"
      via: "import axe from 'axe-core'; axe.run(dom.window.document, {runOnly:{type:'tag',values:WCAG_TAGS}})"
      pattern: "runOnly.*wcag22aa"
    - from: "leanshot/tests/a11y/axe-baseline.test.ts"
      to: "leanshot/tests/a11y/accessibility-baseline.json"
      via: "import baseline from './accessibility-baseline.json'; expect(blocking.length).toBeLessThanOrEqual(baseline[route].blocking)"
      pattern: "toBeLessThanOrEqual.*baseline"
---

<objective>
Stand up the WCAG 2.2 AA CI gate (POLISH-09) using axe-core 4.11.4 + @axe-core/react 4.11.3 running in vitest with jsdom (NO Playwright by default per D-10). Capture an initial accessibility-baseline.json for every existing route (v1.1 + v1.2 + v1.3); wire CI to fail ONLY when blocking-severity (Critical/Serious) violation count rises above baseline on any route. Pre-existing a11y debt is grandfathered.

Purpose: Per D-09, all v1.3 NEW surfaces must add zero new a11y violations. Per D-10, severity gate is Critical+Serious blocking. Per Pitfall 10, the baseline must include a documented quarterly review cadence to avoid calcification.

Output: vitest a11y suite + baseline JSON + CI integration; runs under 60 seconds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/CLAUDE.md
@/Users/karstenhaldan/minisite/leanshot/src/App.tsx
@/Users/karstenhaldan/minisite/leanshot/src/test-setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install axe-core dev deps and enumerate route manifest</name>
  <files>leanshot/package.json, leanshot/package-lock.json, leanshot/tests/a11y/routes-manifest.ts, leanshot/tests/a11y/render-route.tsx</files>
  <action>From `leanshot/` directory: install dev deps via `npm install -D axe-core@^4.11.4 @axe-core/react@^4.11.3` (pinned to RESEARCH-verified versions). Per memory feedback_npm_sentry_capacitor_peer_conflict, add `--legacy-peer-deps` if peer-dep resolver complains. Then create leanshot/tests/a11y/routes-manifest.ts exporting `routesManifest: RouteEntry[]` where each entry is `{path: string, label: string, mountComponent: () => Promise<ReactElement>}`. Enumerate ALL routes from v1.1 + v1.2 + v1.3: '/', '/dashboard', '/dashboard/medication', '/dashboard/body', '/dashboard/nutrition', '/dashboard/mood', '/dashboard/photos', '/dashboard/ai', '/dashboard/settings', '/dashboard/settings/notifications' (NEW v1.3 — placeholder Card with title only for now; populated in 42-08), '/admin', '/admin/users', '/admin/audit', '/admin/cancellation/rules', '/admin/cancellation/roi', '/admin/nps/quarterly' (NEW v1.3 — placeholder for 42-11), '/helpdesk', '/helpdesk/tickets/:id', '/clinic/:slug/dashboard', '/community/feed', '/community/spaces/:slug', '/courses/:slug', '/courses/:slug/lessons/:lessonId', '/nps/respond', '/embed/:provider', '/status', '/whats-new'. Each mountComponent dynamically imports the relevant component (use the same React.lazy targets that src/App.tsx uses) and returns it wrapped in MemoryRouter at the appropriate path. Create leanshot/tests/a11y/render-route.tsx exporting `async function renderRoute(path: string)` that uses ReactDOM.renderToString or @testing-library/react render against jsdom (vitest's built-in jsdom env) and returns `{ window, document, unmount }`. Use the Zustand store with a minimal hydrated test fixture (no real user data) so authenticated routes do not redirect to marketing.</action>
  <verify>
    <automated>cd leanshot && npx vitest run tests/a11y/routes-manifest.test.ts --reporter=verbose 2>&1 | head -20 || echo "Manifest module loads"; cd leanshot && node -e "import('./tests/a11y/routes-manifest.ts').then(m => console.log('routes:', m.routesManifest.length)).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <done>routes-manifest.ts exports an array with at least 20 entries; render-route.tsx mounts at least one route into jsdom without throwing; axe-core + @axe-core/react in leanshot/package.json devDependencies at the pinned versions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write axe-baseline.test.ts and capture initial baseline</name>
  <files>leanshot/tests/a11y/axe-baseline.test.ts, leanshot/tests/a11y/accessibility-baseline.json, leanshot/vitest.config.ts, leanshot/src/test-setup.ts</files>
  <behavior>
    - Test 1: For every route in routesManifest, axe.run() returns a violations array with .impact tagged (critical | serious | moderate | minor)
    - Test 2: Blocking severities (critical + serious) are filtered into a separate array
    - Test 3: For each route, blocking count is asserted ≤ baseline[route].blocking (initially 0 for new routes, captured-from-snapshot for existing)
    - Test 4: When BASELINE_UPDATE=1 env var is set, the test instead WRITES current counts to accessibility-baseline.json (snapshot capture mode)
    - Test 5: The full suite for ALL routes completes in under 60 seconds (D-10 fast-CI claim)
  </behavior>
  <action>Create leanshot/tests/a11y/axe-baseline.test.ts following RESEARCH Pattern 4. Use vitest's `describe.concurrent` to parallelize per-route scans. Import baseline JSON; const WCAG_TAGS = ['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa']; const BLOCK_SEVERITIES = new Set(['critical', 'serious']). For each route: mount via renderRoute, call `await axe.run(dom.window.document, { runOnly: { type: 'tag', values: WCAG_TAGS } })`. Filter violations by impact. If `process.env.BASELINE_UPDATE === '1'`: collect counts into newBaseline object, then in afterAll write JSON.stringify(newBaseline, null, 2) to accessibility-baseline.json. Otherwise: expect(blocking.length, formatViolations(blocking)).toBeLessThanOrEqual(baseline[route]?.blocking ?? 0). The formatViolations helper prints each violation's id + nodes[].html for actionable failure output. Update leanshot/vitest.config.ts to add a separate `a11y` test config block or include this new path under the default test pattern with `environment: 'jsdom'` and testTimeout 5000ms. Extend leanshot/src/test-setup.ts to register @axe-core/react in dev mode (per RESEARCH §Project Structure). Then RUN the test once with `BASELINE_UPDATE=1 npx vitest run tests/a11y/axe-baseline.test.ts` to populate leanshot/tests/a11y/accessibility-baseline.json with the as-of-today counts. COMMIT the baseline JSON. Add an `npm run test:a11y` script in leanshot/package.json: `"test:a11y": "vitest run tests/a11y"` and `"test:a11y:update": "BASELINE_UPDATE=1 vitest run tests/a11y"`. Top of accessibility-baseline.json must include a comment-style key `"__meta": { "captured_at": "<ISO>", "quarterly_review_due": "<ISO+90d>", "reviewer_note": "Per Pitfall 10: re-baseline every quarter; aim to reduce blocking counts by >=1 per route" }`.</action>
  <verify>
    <automated>cd leanshot && npm run test:a11y 2>&1 | tail -20 && test -s tests/a11y/accessibility-baseline.json && jq -r '.["__meta"].captured_at' tests/a11y/accessibility-baseline.json</automated>
  </verify>
  <done>npm run test:a11y exits 0; accessibility-baseline.json exists with per-route entries + __meta block; test completes in &lt;60s; npm run test:a11y prints route-level pass status for every route in the manifest.</done>
</task>

<task type="auto">
  <name>Task 3: Wire test:a11y into CI workflow with baseline-diff PR comment</name>
  <files>.github/workflows/ci.yml</files>
  <action>Read existing .github/workflows/ci.yml. Add a new job step (or extend the existing test job) AFTER the lint + typecheck steps and BEFORE the build step: `- name: a11y baseline gate` running `cd leanshot && npm run test:a11y`. Add a second informational step `- name: a11y moderate-violations comment` that runs even on failure, computes the Moderate-severity diff vs baseline (re-run vitest with `BASELINE_UPDATE=0` and JSON reporter; pipe through a tiny inline node snippet that compares baseline.moderate vs current.moderate per route), and posts the diff as a PR comment using `actions/github-script` IF (a) the PR exists AND (b) any route's moderate count rose. Do NOT make Moderate violations block. Per RESEARCH Validation §Sampling Rate, the a11y suite runs on every PR and every merge to main. Ensure node 22 is the runner version (matches CLAUDE.md `Node.js v22.18.0`).</action>
  <verify>
    <automated>grep -E "test:a11y|a11y.*baseline|axe" .github/workflows/ci.yml | head -10 && yamllint .github/workflows/ci.yml 2>/dev/null || python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <done>CI workflow has a `test:a11y` step that runs the a11y baseline gate; the workflow YAML parses cleanly; the step appears AFTER lint+typecheck and BEFORE build/deploy steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → main branch | a11y gate prevents regression-bearing merges |
| CI runner → repo write (PR comment) | github-script uses GITHUB_TOKEN scoped to PR-comment only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02-01 | Tampering | accessibility-baseline.json gamed by deleting/inflating counts | mitigate | PR review surface; per RESEARCH §Security Domain bullet "axe-core baseline gamed by deleting test"; documented quarterly review cadence in __meta block |
| T-42-02-02 | Repudiation | Stale baseline calcifies; pre-existing violations never fixed | mitigate | Pitfall 10: quarterly_review_due field in __meta; reviewer must lower at least 1 count per route per quarter |
| T-42-02-SC | Tampering | npm install of axe-core@4.11.4 + @axe-core/react@4.11.3 | mitigate | LEGITIMATE per RESEARCH §Package Legitimacy (Deque Labs maintainer; canonical a11y tooling) |
</threat_model>

<verification>
- `cd leanshot && npm run test:a11y` exits 0 on a clean checkout.
- accessibility-baseline.json contains an entry for every route in routesManifest.
- CI workflow YAML parses; the test:a11y step is present.
- Suite runs under 60 seconds (verify by `time npm run test:a11y`).
</verification>

<success_criteria>
- Adding a deliberate WCAG violation to any v1.3 surface in a follow-up branch causes the CI a11y step to fail with an actionable message identifying the route + rule id.
- Pre-existing v1.1/v1.2 violations are captured in baseline and not blocking (D-09 grandfather).
</success_criteria>

<output>
After completion, create `.planning/phases/42-v1-3-polish-closeout/42-02-SUMMARY.md`.
</output>
