---
phase: 42-v1-3-polish-closeout
plan: "10"
type: execute
wave: 3
depends_on: ["42-07"]
files_modified:
  - leanshot/src/lib/nps/quarterly-modal.ts
  - leanshot/src/lib/nps/quarterly-eligibility.ts
  - leanshot/src/components/nps/QuarterlyNPSModal.tsx
  - leanshot/src/components/nps/QuarterlyNPSModal.test.tsx
  - leanshot/src/components/admin/QuarterlyNPSDashboard.tsx
  - leanshot/src/components/admin/QuarterlyNPSDashboard.test.tsx
  - leanshot/src/lib/admin/modules.ts
  - leanshot/src/App.tsx
  - leanshot/e2e/nps-modal-fallback.spec.ts
  - leanshot/src/lib/analytics/events.ts
autonomous: false
requirements:
  - POLISH-12

must_haves:
  truths:
    - "After 30 days of email-sent without response AND user logs in, the in-app QuarterlyNPSModal renders ONCE per quarter per D-21"
    - "QuarterlyNPSModal uses the 5-star modal pattern from P36 D-09 (NOT the 0-10 scale used in email per D-21)"
    - "The unconditional native fire is invoked UNCONDITIONALLY when eligibility computes true — the eslint rule from 42-07 task 3 prevents anyone wrapping it in a feature-flag condition"
    - "Admin dashboard at /admin/nps/quarterly renders: current-quarter score + delta-vs-prior + 4-quarter trend chart + verbatim list per D-24"
    - "Admin dashboard supports filter dimensions: tenure-bucket, plan-tier, cohort per D-24 + ROADMAP"
    - "Admin dashboard reuses the Phase 33 admin-dashboard chart pattern (BaseChart wrapper)"
    - "Two new TAXO events fire: nps_quarterly_sent (server-side), nps_quarterly_responded (server-side and client-side)"
    - "axe-core gate from 42-02 passes for both /nps/respond and /admin/nps/quarterly routes"
  artifacts:
    - path: "leanshot/src/components/nps/QuarterlyNPSModal.tsx"
      provides: "5-star modal using DS Modal primitive; submits response via fetch to nps-quarterly-respond Edge Fn (in-app path)"
      min_lines: 80
    - path: "leanshot/src/components/admin/QuarterlyNPSDashboard.tsx"
      provides: "Admin module rendering NPS score + trend + verbatim list"
      min_lines: 120
    - path: "leanshot/src/lib/nps/quarterly-eligibility.ts"
      provides: "isUserEligibleForQuarterlyNPS(user) — checks last_sign_in_at >= 90d AND no response this quarter AND no in-app shown this quarter"
    - path: "leanshot/src/lib/nps/quarterly-modal.ts"
      provides: "showQuarterlyNpsModal() — UNCONDITIONAL call site; checked by eslint rule from 42-07"
  key_links:
    - from: "leanshot/src/components/admin/QuarterlyNPSDashboard.tsx"
      to: "leanshot/src/lib/admin/modules.ts"
      via: "registerAdminModule({id:'nps-quarterly', route:'nps/quarterly', minRole:'admin', component: QuarterlyNPSDashboard})"
      pattern: "registerAdminModule.*nps"
    - from: "leanshot/src/App.tsx"
      to: "leanshot/src/lib/nps/quarterly-modal.ts"
      via: "useEffect after auth → eligibility check → showQuarterlyNpsModal()"
      pattern: "showQuarterlyNpsModal"
---

<objective>
Build the quarterly NPS user-facing and admin-facing surfaces (POLISH-12 UI tier): in-app fallback modal that fires after 30 days of email non-response per D-21 (using P36's 5-star modal pattern per D-21), and the admin dashboard at /admin/nps/quarterly with score + trend + verbatim per D-24 (reusing Phase 33's admin chart pattern). Consumes plan 42-07 backend.

Purpose: Per D-19, this is a SEPARATE instrument from P36 — same modal primitive, distinct schema and dashboard. Per D-20 + plan 42-07 task 3, the in-app trigger is UNCONDITIONAL (the ESLint rule prevents conditionalization). Per D-21, in-app fallback fires only if email didn't get a response after 30d.

Output: Working modal + admin dashboard, both axe-baseline-clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/src/components/dashboard/charts/BaseChart.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Modal.tsx
@/Users/karstenhaldan/minisite/leanshot/src/lib/admin/modules.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Eligibility + UNCONDITIONAL modal trigger + 5-star Modal component</name>
  <files>leanshot/src/lib/nps/quarterly-eligibility.ts, leanshot/src/lib/nps/quarterly-modal.ts, leanshot/src/components/nps/QuarterlyNPSModal.tsx, leanshot/src/components/nps/QuarterlyNPSModal.test.tsx, leanshot/src/App.tsx, leanshot/src/lib/analytics/events.ts</files>
  <behavior>
    - Eligibility test 1: User signed in 95d ago + responded to current quarter → eligible=false (already responded).
    - Eligibility test 2: User signed in 30d ago + NO response + nonce created >30d ago → eligible=true (in-app fallback).
    - Eligibility test 3: User signed in 30d ago + NO response + nonce created <30d ago → eligible=false (email window still open).
    - Eligibility test 4: User signed in 100d ago → eligible=false (inactive per D-23 90d rule).
    - Modal test 1: Renders 5 star buttons (1..5) per D-21 + open-text "Anything to add?" textarea + Submit button + Skip button.
    - Modal test 2: Click star → highlights stars 1..N → Submit POSTs {score: N*2, comment, responded_via: 'in-app', nonce: <from-eligibility>} (score is *2 to map 5-star → 0-10 scale internally; document this in the action).
    - Modal test 3: Skip stores "skipped" record (a row with score=NULL, responded_via='in-app') so we don't re-prompt this quarter.
    - Trigger test: showQuarterlyNpsModal() is callable from App.tsx without conditionals; ESLint rule from 42-07 passes; wrapping the call in `if (...)` triggers the lint rule.
  </behavior>
  <action>Create leanshot/src/lib/nps/quarterly-eligibility.ts. The eligibility check needs to query backend: GET to a NEW Edge Fn (or augment nps-quarterly-followup) `nps-quarterly-eligibility`? Simpler: expose a SECURITY DEFINER RPC `public.is_user_eligible_for_quarterly_nps(p_user_id uuid)` defined in a NEW small migration at 20270704000024 — performs the eligibility logic in SQL (checks auth.users.last_sign_in_at, quarterly_nps_responses presence, quarterly_nps_nonces age). Call this via supabase.rpc('is_user_eligible_for_quarterly_nps', {p_user_id}). The function returns {eligible: boolean, nonce: text|null} so the frontend can pass the nonce to the Modal submit. Create leanshot/src/lib/nps/quarterly-modal.ts exposing `showQuarterlyNpsModal()` — a thin imperative trigger that the App.tsx useEffect calls when eligibility resolves true. This is THE function name the ESLint rule from 42-07 watches for. Per D-20: this call MUST be unconditional (no `if (...)` wrapper) — the eligibility check is the gate, NOT a conditional on the call itself. Document this with an inline comment: `/* D-20 UNCONDITIONAL: the eligibility resolver controls whether the modal shows. Do NOT wrap this call in a feature-flag conditional — eslint-rules/no-conditional-native-review.cjs will block. */`. Create leanshot/src/components/nps/QuarterlyNPSModal.tsx using the project DS Modal primitive (leanshot/src/components/ui/Modal.tsx). Layout: title "Quick quarterly check-in", subtitle "How likely are you to recommend LeanShot?", 5-star row (buttons with aria-label 'Rate N stars'), textarea for the open-text follow-up, Submit + Skip buttons. On Submit: fetch POST /functions/v1/nps-quarterly-respond with {score: stars*2, comment, nonce, responded_via:'in-app'}. On Skip: POST same endpoint with {score: null, responded_via:'in-app', nonce, skipped:true}. Modify leanshot/src/App.tsx: inside the authenticated wrapper, useEffect → call eligibility RPC → if eligible, call showQuarterlyNpsModal() once (track via a session-scoped flag to prevent re-fire). Add 2 new TAXO events to leanshot/src/lib/analytics/events.ts: `nps_quarterly_sent` (server-side via posthog-server.ts; type-define here for visibility), `nps_quarterly_responded` (client-side fired on Submit success). Write QuarterlyNPSModal.test.tsx covering the 3 modal tests + 4 eligibility tests above. Add small RPC migration 20270704000024 alongside (this is a TINY migration — single CREATE FUNCTION). Per memory feedback_planner_missed_status_enum_widening: since 42-07 already created responded_via with the ENUM constraint, no widening needed; verify the existing constraint allows 'in-app' value.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx vitest run src/components/nps/QuarterlyNPSModal.test.tsx src/lib/nps/ 2>&1 | tail -10 && grep -F "D-20 UNCONDITIONAL" src/lib/nps/quarterly-modal.ts && npm run lint 2>&1 | tail -3 && test -f /Users/karstenhaldan/minisite/supabase/migrations/20270704000024_*.sql</automated>
  </verify>
  <done>All 7 tests pass; ESLint rule passes (call is unconditional); the trigger-modal function is callable from a deliberate test conditional wrapper → ESLint FAILS on that fixture; eligibility RPC migration exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin dashboard at /admin/nps/quarterly</name>
  <files>leanshot/src/components/admin/QuarterlyNPSDashboard.tsx, leanshot/src/components/admin/QuarterlyNPSDashboard.test.tsx, leanshot/src/lib/admin/modules.ts</files>
  <behavior>
    - Dashboard test 1: Loads quarterly_nps_responses via a SECDEF admin RPC; renders current-quarter NPS score (computed: %promoters - %detractors, mapping 0-10 scale: 9-10 promoters, 0-6 detractors).
    - Dashboard test 2: Renders delta-vs-prior-quarter (numeric change + arrow indicator).
    - Dashboard test 3: Renders trend chart for last 4 quarters using BaseChart (Chart.js wrapper — same pattern as Phase 33 admin dashboards).
    - Dashboard test 4: Filter dimensions: dropdown for {tenure-bucket: 'new'|'<6mo'|'>6mo'|'>1yr', plan-tier: 'free'|'pro'|'lifetime', cohort: 'all'|<cohort-list>}. Changing filter re-fetches with the dimension query parameter.
    - Dashboard test 5: Verbatim list (paginated, 20 per page) shows comment + score + responded_at + tenure-bucket + plan-tier. Filterable by score range.
    - a11y: axe-baseline.test.ts passes for /admin/nps/quarterly.
    - Admin access: Module is registered with minRole='admin' (or higher per Phase 27 pattern); non-admin user routing here returns 403.
  </behavior>
  <action>Create leanshot/src/components/admin/QuarterlyNPSDashboard.tsx following Phase 33's admin-CAC dashboard pattern (which Phase 33 SUMMARY recorded as the canonical chart pattern — read its SUMMARY and code first). Use BaseChart wrapper for the 4-quarter trend chart. The data source is a NEW SECDEF admin RPC `public.get_quarterly_nps_dashboard(p_quarter text, p_tenure_bucket text default null, p_plan_tier text default null, p_cohort text default null)` defined in another small migration 20270704000025. The RPC checks is_admin() at top, computes: current quarter's promoters/passives/detractors counts; previous quarter's same; 4-quarter trend buckets; verbatim list with pagination. Returns JSON. Register the new admin module in leanshot/src/lib/admin/modules.ts: `registerAdminModule({id:'nps-quarterly', route:'nps/quarterly', minRole:'admin', component: lazy(() => import('@/components/admin/QuarterlyNPSDashboard')), label:'Quarterly NPS', icon:<icon>})` — read the existing module registration shape first (modules.ts has the pattern from Phase 27). Use the existing surfaceCheck pattern from leanshot/src/lib/org.ts for `surfaceCheck('admin.nps.quarterly')`. Write the comprehensive RTL test covering the 7 behaviors above. Per memory feedback_planner_iter1_anti_patterns: DO NOT inline hedge "if data unavailable, show empty state" — the empty state is part of D-24 spec (small text "No responses yet this quarter" — appears when quarterly_nps_responses count = 0).</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx vitest run src/components/admin/QuarterlyNPSDashboard.test.tsx 2>&1 | tail -10 && grep -E "id:\s*['\"]nps-quarterly['\"]" src/lib/admin/modules.ts && test -f /Users/karstenhaldan/minisite/supabase/migrations/20270704000025_*.sql && npm run build 2>&1 | tail -5 && npm run check-bundle-budget 2>&1 | tail -5</automated>
  </verify>
  <done>All 7 dashboard tests pass; admin module registered; bundle-budget passes (admin-shell ceiling 30 kB gz respected — verify the new admin chunk delta is reasonable); SECDEF RPC migration exists.</done>
</task>

<task type="auto">
  <name>Task 3: e2e Playwright test for full quarterly NPS flow (email fallback + in-app)</name>
  <files>leanshot/e2e/nps-modal-fallback.spec.ts</files>
  <action>Create leanshot/e2e/nps-modal-fallback.spec.ts. Test scenarios: (1) seed a test user with an unused quarterly_nps_nonces row created 31 days ago AND no quarterly_nps_responses row for the current quarter → user logs in → modal appears; (2) submit 4 stars + "Great app" comment → modal closes → supabase query confirms quarterly_nps_responses row with score=8, comment='Great app', responded_via='in-app'; (3) reload page → modal does NOT re-appear (already responded this quarter); (4) test the signed-token email path: GET /nps-quarterly-respond?score=9&token=<valid-signed-token> via Playwright API request → confirm 200 + quarterly_nps_responses row with score=9, responded_via='email'; (5) replay same URL → 409 (single-use enforcement from 42-07). For seeding/teardown use service-role direct DB writes. Per memory reference_playwright_state_seeding: use addInitScript for auth seeding.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx playwright test e2e/nps-modal-fallback.spec.ts 2>&1 | tail -10</automated>
  </verify>
  <done>5 e2e scenarios pass; in-app modal works; email signed-token path works; replay returns 409.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN — Verify admin dashboard end-to-end + push new RPC migrations</name>
  <what-built>QuarterlyNPSModal + QuarterlyNPSDashboard + 2 new SECDEF RPC migrations (20270704000024 eligibility, 20270704000025 dashboard).</what-built>
  <how-to-verify>
    1. From `/Users/karstenhaldan/minisite/`: `git rev-parse --show-toplevel` must print monorepo root.
    2. `supabase db push --linked` — applies the 2 new SECDEF RPC migrations. Verify cleanly.
    3. Seed 5-10 fake quarterly_nps_responses rows across recent quarters for visual variety:
       `supabase db query --linked "INSERT INTO quarterly_nps_responses (user_id, quarter, score, comment, responded_via) SELECT id, '2026-Q2', (random()*10)::int, 'fake response', 'email' FROM auth.users LIMIT 10"`
    4. Sign in as an admin user (or temporarily grant admin role to your test user via supabase db query: `UPDATE profiles SET role = 'admin' WHERE id = '<your-uuid>'`).
    5. Navigate to /admin/nps/quarterly. Verify:
       - Current-quarter score renders as a number
       - Delta-vs-prior shows with arrow indicator
       - 4-quarter trend chart renders without console errors
       - Verbatim list renders with the 10 seeded rows
       - Filter dropdowns (tenure / plan / cohort) trigger re-fetch on change
    6. Test the eligibility-based in-app modal by inserting a fake nonce row aged >30d for your test user and reloading the app:
       `supabase db query --linked "INSERT INTO quarterly_nps_nonces (nonce, user_id, quarter, created_at) VALUES (gen_random_uuid()::text, '<your-uuid>', '2026-Q3', now() - interval '31 days')"`
       Reload the dashboard root URL → QuarterlyNPSModal should appear.
    7. Submit 4 stars + a comment → modal closes → query confirms quarterly_nps_responses row.
  </how-to-verify>
  <resume-signal>Type "nps-ui-verified" once: (a) /admin/nps/quarterly renders correctly with seeded data, (b) in-app modal appears for eligible user, (c) submit writes the response row</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → quarterly-eligibility RPC | Auth-scoped; returns own user's nonce only |
| Admin → quarterly-nps-dashboard RPC | is_admin() check inside SECDEF |
| Eslint rule → developer code | Static-analysis enforcement of UNCONDITIONAL principle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-10-01 | Elevation of Privilege | Non-admin reaches /admin/nps/quarterly | mitigate | minRole='admin' in module registration + surfaceCheck + RLS via is_admin() in dashboard RPC |
| T-42-10-02 | Spoofing | Developer wraps showQuarterlyNpsModal in conditional | mitigate | ESLint rule from 42-07 task 3; CI lint gate |
| T-42-10-03 | Information Disclosure | Verbatim comments contain PII | accept | Free-text comments may contain user-volunteered info; admin-only access; documented in privacy policy |
| T-42-10-04 | Repudiation | User claims they never submitted | mitigate | quarterly_nps_responses + nonce single-use trail |
| T-42-10-SC | — | No new package installs in this plan | accept | Reuses BaseChart (Chart.js already in stack) + react-markdown (in stack from 42-09) |
</threat_model>

<verification>
- /admin/nps/quarterly renders score + trend + verbatim with filter dimensions.
- In-app modal appears for eligibility-satisfied user.
- ESLint rule blocks conditional wrapping.
- e2e flow covers both email signed-token and in-app paths.
</verification>

<success_criteria>
- POLISH-12 success criterion fully satisfied: quarterly NPS survey sends to active users via Resend, segmented by tenure + plan + cohort, results visible in admin.
- In-app fallback fires after 30d email window.
</success_criteria>

<output>
After completion, create `.planning/phases/42-v1-3-polish-closeout/42-10-SUMMARY.md`.
</output>
