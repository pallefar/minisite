---
phase: 42-v1-3-polish-closeout
plan: "07"
type: execute
wave: 2
depends_on: []
files_modified:
  - supabase/migrations/20270704000020_quarterly_nps_responses.sql
  - supabase/migrations/20270704000021_quarterly_nps_nonces.sql
  - supabase/migrations/20270704000022_quarterly_nps_rls.sql
  - supabase/migrations/20270704000023_quarterly_nps_cron.sql
  - supabase/functions/nps-quarterly-enqueue/index.ts
  - supabase/functions/nps-quarterly-respond/index.ts
  - supabase/functions/nps-quarterly-followup/index.ts
  - supabase/functions/_shared/nps-token.ts
  - leanshot/eslint-rules/no-conditional-native-review.cjs
  - leanshot/eslint-rules/no-conditional-native-review.test.cjs
  - leanshot/eslint.config.js
  - leanshot/tests/rls/quarterly-nps-rls.test.ts
  - leanshot/tests/integration/quarterly-nps-cron.test.ts
  - leanshot/tests/integration/quarterly-nps-respond.test.ts
  - leanshot/tests/integration/quarterly-nps-fallback.test.ts
autonomous: false
requirements:
  - POLISH-12

must_haves:
  truths:
    - "quarterly_nps_responses table is per-user UNIQUE on (user_id, quarter) — single-use enforcement per Pitfall 11"
    - "quarterly_nps_nonces table tracks single-use signed tokens; nonce invalidated on first use"
    - "Cross-tenant test proves user A cannot read user B's quarterly_nps_responses"
    - "pg_cron schedule fires on Jan 1 / Apr 1 / Jul 1 / Oct 1 UTC midnight per D-22"
    - "Cron body uses named $cron$ + $inner$ dollar-quote tags per memory reference_postgres_dollar_quote_nesting_in_cron_body"
    - "Cron uses vault.decrypted_secrets pattern + hardcoded URL per memory reference_supabase_pg_cron_vault_service_role_pattern"
    - "Eligibility filter: active in last 90d per D-23 (uses auth.users.last_sign_in_at OR a fallback activity signal)"
    - "Email-first delivery via _shared/email-router.ts → Resend per D-21 (non-PHI)"
    - "Signed-token response path verifies HMAC + invalidates nonce (Pitfall 11)"
    - "no-conditional-native-review ESLint rule CREATED in P42 (folds P36 review-prompt baseline + P42 quarterly-NPS surfacing) and REGISTERED in leanshot/eslint.config.js per D-20"
    - "Quarterly NPS is SEPARATE from P36 review-prompt — does NOT share P36 cooldown / 5-lifetime cap per D-19"
  artifacts:
    - path: "supabase/migrations/20270704000020_quarterly_nps_responses.sql"
      provides: "Table {id, user_id, quarter text e.g. '2026-Q3', score int 0-10 CHECK, comment text, responded_via text ENUM('email','in-app'), responded_at} UNIQUE(user_id, quarter)"
    - path: "supabase/migrations/20270704000023_quarterly_nps_cron.sql"
      provides: "pg_cron schedule using vault pattern + named dollar-quote tags"
    - path: "supabase/functions/nps-quarterly-enqueue/index.ts"
      provides: "Cron target — identifies eligible users + enqueues Resend campaign batch"
    - path: "supabase/functions/nps-quarterly-respond/index.ts"
      provides: "GET /nps/respond?score=N&token=... → verify HMAC + invalidate nonce + INSERT response + serve follow-up landing"
    - path: "supabase/functions/_shared/nps-token.ts"
      provides: "signQuarterlyNpsToken(user_id, quarter, score, nonce) + verifyQuarterlyNpsToken(token) — HMAC SHA-256 using Function Secret QUARTERLY_NPS_SIGNING_KEY"
    - path: "leanshot/eslint-rules/no-conditional-native-review.cjs"
      provides: "ESLint AST rule that flags conditional surfacing of BOTH the P36 review-prompt instruments (requestReview/showReviewPrompt/triggerReviewPrompt) AND the P42 quarterly-NPS instruments (showQuarterlyNpsModal/triggerQuarterlyNps). Created in P42 (Phase 36 not yet executed); P36 must reuse not recreate when it ships."
    - path: "leanshot/eslint.config.js"
      provides: "Registers the no-conditional-native-review custom rule + scopes it to leanshot/src/**/*.{ts,tsx}."

user_setup:
  - service: supabase
    why: "4 migrations + 3 Edge Functions + 1 cron job + 1 Function Secret"
    env_vars:
      - name: QUARTERLY_NPS_SIGNING_KEY
        source: "Generate via openssl rand -base64 32; set as Supabase Function Secret"
    dashboard_config:
      - task: "Generate signing key + push migrations + deploy functions + verify cron"
        location: "Combined human checkpoint at end of plan"
---

<objective>
Build the quarterly NPS backend (POLISH-12): SEPARATE instrument from P36 review-prompt per D-19. New schema (quarterly_nps_responses + quarterly_nps_nonces), pg_cron first-of-quarter trigger using the project's vault service-role pattern, three Edge Fns (enqueue, respond via signed-token, followup), the HMAC token signer/verifier, and an EXTENSION of the existing eslint-rules/no-conditional-native-review.cjs to cover quarterly NPS surfacing per D-20.

Purpose: Per D-19, the quarterly instrument has different cadence (every 90d), different question framing ("quarterly check-in"), and different downstream consumer (admin /admin/nps/quarterly dashboard) than P36's event-triggered review-prompt. Per D-20, the same UNCONDITIONAL native-fire principle is universal across NPS instruments — the ESLint rule must enforce it for both code paths.

Output: Server tier + lint extension; in-app fallback UI + admin dashboard ship in plan 42-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/eslint-rules/no-conditional-native-review.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations — quarterly_nps_responses + quarterly_nps_nonces + RLS + pg_cron</name>
  <files>supabase/migrations/20270704000020_quarterly_nps_responses.sql, supabase/migrations/20270704000021_quarterly_nps_nonces.sql, supabase/migrations/20270704000022_quarterly_nps_rls.sql, supabase/migrations/20270704000023_quarterly_nps_cron.sql, leanshot/tests/rls/quarterly-nps-rls.test.ts</files>
  <behavior>
    - Schema test 1: quarterly_nps_responses {id uuid pk, user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE, quarter text NOT NULL (format '\d{4}-Q[1-4]'), score int CHECK (score BETWEEN 0 AND 10), comment text NULL, responded_via text CHECK IN ('email','in-app'), responded_at timestamptz NOT NULL DEFAULT now()} UNIQUE(user_id, quarter).
    - Schema test 2: quarterly_nps_nonces {nonce text pk, user_id uuid, quarter text, created_at timestamptz default now(), used_at timestamptz NULL}. Index on (user_id, quarter, used_at).
    - RLS test 1: quarterly_nps_responses — user can INSERT own row; SELECT own row; admin (is_admin()) can SELECT all rows.
    - RLS test 2: quarterly_nps_nonces — service-role only (no authenticated direct access). Authenticated SELECT denied.
    - Cross-tenant test: user A's JWT cannot read user B's quarterly_nps_responses (project rule).
    - Cron test (live verification at human checkpoint): `SELECT jobname, schedule FROM cron.job WHERE jobname = 'quarterly-nps-enqueue'` shows schedule '0 0 1 1,4,7,10 *'.
  </behavior>
  <action>Write 20270704000020..20270704000023. Schema is straightforward. The cron migration (20270704000023) is the gotcha-magnet — per memory reference_postgres_dollar_quote_nesting_in_cron_body, the inner DO block MUST use a named dollar-quote tag distinct from the outer cron.schedule body tag. Use this pattern: `SELECT cron.schedule('quarterly-nps-enqueue', '0 0 1 1,4,7,10 *', $cron$ SELECT net.http_post(url := 'https://ytnsipxxmzgaebkqmokp.functions.supabase.co/nps-quarterly-enqueue', headers := jsonb_build_object('Authorization', 'Bearer ' || (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'service_role_key'), 'Content-Type', 'application/json'), body := '{}'::jsonb); $cron$);`. Per memory reference_supabase_pg_cron_vault_service_role_pattern: vault.decrypted_secrets is the ONLY supported path on this project (current_setting GUC does NOT work). Hardcode the URL (do NOT use app.supabase_url GUC). Per memory reference_supabase_migration_gotchas: SECURITY DEFINER on any helper functions must set `search_path = extensions, public`. The RLS migration uses is_admin() (confirmed exists per Phase 33 work). Write leanshot/tests/rls/quarterly-nps-rls.test.ts using the standard admin.generateLink + /auth/v1/verify fixture pattern. Write a SEPARATE migration assertion test that does `SELECT pg_get_function_arguments(...)` to verify cron body parses without dollar-quote collision.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && for f in 20270704000020 20270704000021 20270704000022 20270704000023; do ls supabase/migrations/${f}_*.sql >/dev/null || { echo "MISSING $f"; exit 1; }; done; grep -q '\$cron\$' supabase/migrations/20270704000023_*.sql && grep -q "vault.decrypted_secrets" supabase/migrations/20270704000023_*.sql; cd leanshot && npx vitest run --config vitest-e2e.config.ts tests/rls/quarterly-nps-rls.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>4 migrations exist with strict naming; cron migration uses named $cron$ tag + vault pattern + hardcoded URL; RLS cross-tenant proof passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Edge Fns — enqueue + respond + followup + HMAC signer/verifier</name>
  <files>supabase/functions/nps-quarterly-enqueue/index.ts, supabase/functions/nps-quarterly-respond/index.ts, supabase/functions/nps-quarterly-followup/index.ts, supabase/functions/_shared/nps-token.ts, leanshot/tests/integration/quarterly-nps-cron.test.ts, leanshot/tests/integration/quarterly-nps-respond.test.ts, leanshot/tests/integration/quarterly-nps-fallback.test.ts</files>
  <behavior>
    - Token test 1 (nps-token): signQuarterlyNpsToken(user_id, quarter, score, nonce) + verifyQuarterlyNpsToken(token) round-trip with valid signing key returns {user_id, quarter, score, nonce}.
    - Token test 2: A token signed with key K cannot verify under key K'.
    - Token test 3: A token whose nonce has been used (used_at IS NOT NULL) is rejected as 'replay'.
    - Enqueue test: For a quarter '2026-Q3', enqueue identifies users active in last 90d AND without an existing 2026-Q3 row in quarterly_nps_responses; calls sendEmail() via _shared/email-router.ts for each batch; INSERTs quarterly_nps_nonces rows.
    - Respond test 1 (single-use): GET /nps-quarterly-respond?score=8&token=<valid> → 200 → INSERTs quarterly_nps_responses; second call with same token → 409 'already used'.
    - Respond test 2 (HMAC fail): Tampered score in URL (mismatching token) → 401.
    - Respond test 3 (UNIQUE clamp): If responded_via='in-app' row already exists for this quarter, email response returns 409 'already responded'.
    - Fallback test (in-app eligibility): After 30 days of NPS-sent without response, the in-app trigger flag goes true for that user on next login (verified via SELECT EXISTS query the front-end will use in 42-10).
  </behavior>
  <action>Create supabase/functions/_shared/nps-token.ts. Signing key from Deno.env.get('QUARTERLY_NPS_SIGNING_KEY')!. signQuarterlyNpsToken: payload = JSON.stringify({user_id, quarter, score, nonce}); use Node's crypto.createHmac('sha256', key) inside Deno via `import { createHmac } from 'node:crypto'` (works in Deno per RESEARCH §Pattern 7). Token format: `base64url(payload).hmac_base64url`. verifyQuarterlyNpsToken: split + timingSafeEqual the HMAC. Implement supabase/functions/nps-quarterly-enqueue/index.ts: triggered by pg_cron via service-role bearer. Read current quarter from now() (e.g. compute '2026-Q3'). Query: `SELECT id FROM auth.users WHERE last_sign_in_at >= now() - interval '90 days' AND NOT EXISTS (SELECT 1 FROM quarterly_nps_responses WHERE user_id = auth.users.id AND quarter = $1)` — paginated in batches of 500. For each batch: INSERT INTO quarterly_nps_nonces (nonce, user_id, quarter) VALUES (gen_random_uuid()::text, ...) RETURNING nonce. Build 11 score-link URLs per user (0..10) using signQuarterlyNpsToken. Compose email with signed-link rows. Call sendEmail({to, subject: 'Quick check-in: how likely are you to recommend LeanShot?', body, phi: false}) via _shared/email-router.ts (D-21 routes via Resend for non-PHI). Implement supabase/functions/nps-quarterly-respond/index.ts: GET handler reading ?score=N&token=... query params. Verify token; verify quarterly_nps_nonces.used_at IS NULL; transaction: UPDATE quarterly_nps_nonces SET used_at = now() WHERE nonce = $1 AND used_at IS NULL RETURNING 1 (the rowcount=1 guards against race) → INSERT INTO quarterly_nps_responses (...) ON CONFLICT (user_id, quarter) DO NOTHING. Return HTML follow-up page (or 302 redirect to https://app.leanshot.app/nps/followup?nonce=<nonce>). Implement supabase/functions/nps-quarterly-followup/index.ts: POST {nonce, comment} → verifies nonce belongs to a committed response → UPDATE quarterly_nps_responses SET comment = $2 WHERE user_id = (SELECT user_id FROM quarterly_nps_nonces WHERE nonce = $1) AND quarter = (...). Per memory reference_supabase_functions_deploy_no_linked_flag: deploy without --linked. Write the 3 integration tests in leanshot/tests/integration/. The cron test uses pg_dump-style assertions via supabase/postgres-meta API OR a direct `cron.job` query through supabase-js with service role.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && for fn in nps-quarterly-enqueue nps-quarterly-respond nps-quarterly-followup; do test -f "supabase/functions/$fn/index.ts" || { echo "MISSING $fn"; exit 1; }; done; test -f supabase/functions/_shared/nps-token.ts; cd leanshot && npx vitest run tests/integration/quarterly-nps-respond.test.ts tests/integration/quarterly-nps-cron.test.ts tests/integration/quarterly-nps-fallback.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Three Edge Fns + shared token helper; all 3 integration tests pass; single-use enforcement proven (replay returns 409).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CREATE eslint-rules/no-conditional-native-review.cjs (folds P36 + P42 NPS instruments) + register in eslint.config.js</name>
  <files>leanshot/eslint-rules/no-conditional-native-review.cjs, leanshot/eslint-rules/no-conditional-native-review.test.cjs, leanshot/eslint.config.js</files>
  <behavior>
    - Rule test 1: A conditional P36-style review-prompt invocation (`if (cohort === 'X') { requestReview(); }` or `showReviewPrompt()` wrapped in any IfStatement / ConditionalExpression / LogicalExpression) FAILS the rule.
    - Rule test 2: A conditional P42 quarterly-NPS invocation (`if (...) { showQuarterlyNpsModal(); }` or `triggerQuarterlyNps()` under a conditional ancestor) FAILS the rule.
    - Rule test 3: An UNCONDITIONAL quarterly-NPS invocation at module/function top-level (no IfStatement / ConditionalExpression / LogicalExpression ancestor inside the same function body) PASSES.
    - Rule test 4: An UNCONDITIONAL native `review.requestReview()` invocation PASSES.
  </behavior>
  <action>
**IMPORTANT — context shift from CONTEXT D-20 wording.** Phase 36 (which CONTEXT D-20 referenced as the rule's origin) is NOT yet executed (ROADMAP shows Phase 36 unchecked; the file `leanshot/eslint-rules/no-conditional-native-review.cjs` does not exist on disk; sibling rule files `additive-only-events.cjs` and `no-raw-service-role-client.cjs` ARE present and provide the project's rule-pattern template). P42 must therefore CREATE the rule from scratch, folding BOTH instruments under one rule. When Phase 36 ships its own execution, it should treat the rule as already-existing and only contribute its review-prompt call-site code (not re-create the rule).

Step 1: Read `leanshot/eslint-rules/additive-only-events.cjs` (or the other sibling rule file) to see the project's exact ESLint rule-export convention + corresponding `.test.cjs` RuleTester harness.

Step 2: Create `leanshot/eslint-rules/no-conditional-native-review.cjs` exporting a rule with:
  - top-of-file comment: `// Created in P42 D-20 to enforce the UNCONDITIONAL principle on BOTH NPS instruments: P36 review-prompt (requestReview / showReviewPrompt / triggerReviewPrompt) AND P42 quarterly check-in (showQuarterlyNpsModal / triggerQuarterlyNps). Phase 36's V13-3 BLOCKER decision applies here.`
  - meta: `{ type: 'problem', docs: { description: 'Native review prompts and quarterly NPS modal must fire unconditionally — no cohort gating, no conditional ancestors.' }, schema: [] }`
  - target call identifiers (Identifier name match in CallExpression callee position OR MemberExpression property name): `requestReview`, `showReviewPrompt`, `triggerReviewPrompt`, `showQuarterlyNpsModal`, `triggerQuarterlyNps`.
  - logic: walk parents from each matched CallExpression; if any ancestor up to the enclosing FunctionDeclaration / ArrowFunctionExpression / Program is an `IfStatement`, `ConditionalExpression`, `LogicalExpression` (`&&` / `||` short-circuit guard), or `SwitchCase` — report `context.report({ node, messageId: 'conditionalSurface' })` with message `'Native review / NPS surfacing must be unconditional — found conditional ancestor of type {{type}}. Per V13-3 BLOCKER + D-20.'`.

Step 3: Create `leanshot/eslint-rules/no-conditional-native-review.test.cjs` using the same RuleTester pattern as the sibling rule's test. Include the 4 fixtures above. Run with `node --test`.

Step 4: Register the rule in `leanshot/eslint.config.js` — add a config object that loads the rule via `local: require('./eslint-rules/no-conditional-native-review.cjs')` (or whatever pattern the sibling rules use; mirror exactly) and turn it to `'error'` for the `leanshot/src/**/*.{ts,tsx}` glob. Mirror the existing import-x / sibling-rule registration block exactly — do NOT invent new config shape.

Step 5: Verify by running `cd leanshot && npm run lint`. Expected: 0 NEW errors. The repo's existing 84-error baseline (per memory `project_lint_debt_import_x_order`) MUST NOT change in count.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && test -f eslint-rules/no-conditional-native-review.cjs && test -f eslint-rules/no-conditional-native-review.test.cjs && node --test eslint-rules/no-conditional-native-review.test.cjs 2>&1 | tail -10 && grep -F "Created in P42 D-20" eslint-rules/no-conditional-native-review.cjs && grep -F "no-conditional-native-review" eslint.config.js && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>4 rule tests pass; rule registered in eslint.config.js; npm run lint exits with no NEW errors beyond the documented 84-baseline; rule recognizes BOTH P36 + P42 call patterns under any conditional ancestor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN — Set signing key, push migrations, deploy functions, verify cron</name>
  <what-built>4 migrations + 3 Edge Functions + 1 cron schedule + ESLint rule extension.</what-built>
  <how-to-verify>
    1. From `/Users/karstenhaldan/minisite/`: `git rev-parse --show-toplevel` must print monorepo root.
    2. Generate signing key + set Function Secret:
       `openssl rand -base64 32` → copy output → `supabase secrets set QUARTERLY_NPS_SIGNING_KEY='<output>' --project-ref ytnsipxxmzgaebkqmokp`
    3. Push migrations: `supabase db push --linked`. Expect 20270704000020..00023 applied; NO "Skipping" lines.
    4. Verify cron schedule: `supabase db query --linked "SELECT jobname, schedule FROM cron.job WHERE jobname = 'quarterly-nps-enqueue'"` → expects 1 row with schedule `'0 0 1 1,4,7,10 *'`.
    5. Deploy Edge Fns: `supabase functions deploy nps-quarterly-enqueue nps-quarterly-respond nps-quarterly-followup --project-ref ytnsipxxmzgaebkqmokp`.
    6. Smoke-test the enqueue path (do NOT wait for Jan 1):
       `curl -X POST https://ytnsipxxmzgaebkqmokp.functions.supabase.co/nps-quarterly-enqueue -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" -d '{}'` → expects 200 + a JSON body reporting eligible_user_count.
    7. (Optional) end-to-end live test: pick a real test user, create a quarterly_nps_nonces row, sign a token via the deployed Fn, hit /nps-quarterly-respond with score=8 — expect 200 + new quarterly_nps_responses row.
  </how-to-verify>
  <resume-signal>Type "nps-backend-deployed" once signing key set + migrations applied + Fns deployed + cron schedule verified</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron → nps-quarterly-enqueue Edge Fn | Service-role bearer from vault.decrypted_secrets |
| Email link → nps-quarterly-respond Edge Fn | Unauthenticated; HMAC-signed token is the only auth |
| User → quarterly_nps_responses | Per-user RLS for SELECT/INSERT own |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07-01 | Spoofing | Forwarded NPS email submits another user's response (Pitfall 11) | mitigate | HMAC-signed token + UNIQUE(user_id, quarter) + quarterly_nps_nonces single-use enforcement |
| T-42-07-02 | Tampering | Score parameter mutated in URL | mitigate | HMAC covers score; verifier rejects mismatched tokens |
| T-42-07-03 | Information Disclosure | Cross-tenant read of NPS responses | mitigate | RLS per-user policy + cross-tenant proof |
| T-42-07-04 | Repudiation | User claims they never received NPS email | accept | quarterly_nps_nonces row is sufficient audit trail; email send logs in email-router |
| T-42-07-05 | Tampering | Cron body dollar-quote collision (Pitfall 8) | mitigate | Named $cron$ + $inner$ tags per memory reference_postgres_dollar_quote_nesting_in_cron_body |
| T-42-07-06 | Spoofing | Eslint rule bypassed by renaming call site | mitigate | Rule covers known call identifiers + AST patterns; PR review surface |
</threat_model>

<verification>
- 4 migrations apply cleanly.
- Cross-tenant RLS proof passes.
- 3 integration tests pass.
- pg_cron schedule appears in cron.job table.
- Three Edge Fns deploy and respond.
- ESLint rule extension covers quarterly NPS without breaking P36.
</verification>

<success_criteria>
- The cron will fire at the next first-of-quarter UTC boundary (Jul 1 or Oct 1 2026).
- A user replaying a signed-token URL gets 409 'already used' on second invocation.
- The eslint rule blocks any future PR that conditionalizes quarterly NPS surfacing.
</success_criteria>

<output>
After completion, create `.planning/phases/42-v1-3-polish-closeout/42-07-SUMMARY.md`.
</output>
