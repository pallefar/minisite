---
phase: 42-v1-3-polish-closeout
plan: "11"
type: execute
wave: 4
depends_on: ["42-02", "42-03", "42-04", "42-08", "42-09", "42-10"]
files_modified:
  - leanshot/tests/a11y/accessibility-baseline.json
  - leanshot/.planning/phases/42-v1-3-polish-closeout/42-VOICEOVER-UAT.md
  - leanshot/scripts/assert-bundle-budgets.sh
autonomous: false
requirements:
  - POLISH-05
  - POLISH-06
  - POLISH-07
  - POLISH-08
  - POLISH-09
  - POLISH-11
  - POLISH-12

must_haves:
  truths:
    - "Re-run of axe-baseline.test.ts shows zero NEW blocking violations across all routes including newly added v1.3 routes"
    - "accessibility-baseline.json is updated to include the new v1.3 routes (e.g. /settings/notifications, /admin/nps/quarterly, /whats-new)"
    - "npm run check-bundle-budget passes — index chunk, admin-shell chunk, course-player chunk, community-feed chunk all under their per-chunk ceilings"
    - "VoiceOver HUMAN-UAT documented on top-5 v1.3 user flows per D-11"
    - "The 42-01 disposable _spike_web_push Edge Function is REMOVED from the project (decommissioned)"
    - "All 7 REQ-IDs (POLISH-05/06/07/08/09/11/12) have observable evidence in this phase"
  artifacts:
    - path: "leanshot/.planning/phases/42-v1-3-polish-closeout/42-VOICEOVER-UAT.md"
      provides: "Founder's VoiceOver UAT checklist + results for the top-5 v1.3 flows per D-11"
    - path: "leanshot/tests/a11y/accessibility-baseline.json"
      provides: "Updated baseline including new v1.3 routes with __meta.quarterly_review_due 90d from now"
  key_links:
    - from: "leanshot/scripts/assert-bundle-budgets.sh"
      to: "Phase 12 chunk ceilings + Phase 24 D-18..20 ceilings"
      via: "Re-run after all 42-* plans land; assert no ceiling violations"
      pattern: "check-bundle-budget"
---

<objective>
Close out Phase 42: re-run the axe-core baseline against all routes (existing + new v1.3 surfaces); update accessibility-baseline.json to register the new routes at zero-blocking; verify per-chunk bundle budget; perform the VoiceOver HUMAN-UAT per D-11 on the top-5 v1.3 user flows; decommission the 42-01 spike Edge Function.

Purpose: This is the integration gate. Every preceding plan asserts its own pass criteria; this plan asserts the WHOLE phase is internally consistent — a11y didn't regress across the new surfaces, bundle ceilings aren't exceeded, voice-over reveals no focus-trap bugs (Pitfall 6), and the disposable spike Fn is cleaned up so production has no orphan endpoints.

Output: Updated baseline; UAT checklist; bundle assertion; clean Supabase project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/42-v1-3-polish-closeout/42-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/tests/a11y/accessibility-baseline.json
@/Users/karstenhaldan/minisite/leanshot/scripts/assert-bundle-budgets.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-baseline axe-core + bundle budget assertion</name>
  <files>leanshot/tests/a11y/accessibility-baseline.json, leanshot/scripts/assert-bundle-budgets.sh</files>
  <action>From leanshot/: (1) Run `BASELINE_UPDATE=1 npm run test:a11y` to regenerate accessibility-baseline.json including the newly added v1.3 routes (/settings/notifications, /admin/nps/quarterly, /whats-new). The new routes MUST have blocking=0 (they were built fresh with the gate active in 42-02). If any new route has blocking>0, FAIL — the issue must be fixed in the owning plan (revisit 42-08/09/10) rather than baselined. Existing routes' baselines should be IDENTICAL to the pre-phase capture (no regression). Update the __meta.captured_at to today's date and __meta.quarterly_review_due to +90 days. (2) Verify bundle budget: `npm run check-bundle-budget`. Per memory reference_bundle_budget_hash_hyphen: ensure the script handles hash-hyphenated chunk names; if a chunk reports `skip` due to hash containing `-`, fix the script as Phase 10 plan 11 did. Per Phase 12/24 ceilings: index ≤ 50 kB gz; admin-shell ≤ 30 kB; helpdesk-widget ≤ 25 kB; gamification-burst ≤ 8 kB; community-feed ≤ 20 kB; course-player ≤ 30 kB. New chunks introduced by 42-* (whats-new drawer, nps modal, nps dashboard, notification settings, pwa register) MUST be checked for inclusion in the ceilings file or added with new explicit ceilings. Update scripts/assert-bundle-budgets.sh if needed to add ceilings for the new chunks. (3) Run `npm run typecheck && npm run lint && npm run test` (full suite) — all green.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && BASELINE_UPDATE=1 npm run test:a11y 2>&1 | tail -10 && for newroute in /settings/notifications /admin/nps/quarterly /whats-new; do jq -e --arg r "$newroute" '.[$r].blocking == 0' tests/a11y/accessibility-baseline.json || { echo "NEW route $newroute has blocking violations — fix in owning plan"; exit 1; }; done; npm run check-bundle-budget 2>&1 | tail -10 && npm run typecheck 2>&1 | tail -5 && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <done>accessibility-baseline.json updated; all new v1.3 routes have blocking=0; bundle-budget passes; typecheck + lint green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HUMAN — VoiceOver UAT on top-5 v1.3 user flows + decommission spike Fn</name>
  <what-built>Phase 42 is feature-complete; this is the closing accessibility + cleanup pass.</what-built>
  <how-to-verify>
    PART A — VoiceOver UAT per D-11:
    1. macOS: System Settings → Accessibility → VoiceOver → ON (Cmd+F5 toggle).
    2. Open Chrome/Safari on http://localhost:5173 (or the deployed app).
    3. Create `leanshot/.planning/phases/42-v1-3-polish-closeout/42-VOICEOVER-UAT.md` with a section for each of the 5 flows. For each flow, walk through with VoiceOver enabled and record observations:
       (a) **Signup flow (P34 onboarding hybrid 3-card):** Can you tab through each onboarding step with screen reader announcing field labels + validation messages? Note any focus-trap escapes or unlabeled controls.
       (b) **Dose log entry:** Open the log-dose modal. Can VoiceOver navigate the date picker + dose amount + injection site fields without losing context? Is the modal's `aria-modal="true"` honored (background NOT spoken)?
       (c) **Share-link generation (Phase 8 doctor read-share):** Generate a share link. Is the resulting URL announced? Is the Copy button labeled?
       (d) **Clinic invite acceptance (P28):** Open a clinic invite link in a fresh session. Can VoiceOver navigate the accept/decline flow?
       (e) **Quarterly NPS modal (this phase D-21 in-app fallback):** Trigger the modal (see plan 42-10 task 4 step 6 for the trigger seed). Are the 5 star buttons individually labeled? Is the comment textarea reachable + announced? Are Submit/Skip distinguishable?
    4. For each flow, mark PASS / FAIL / WARN with notes. PASS = no issues. WARN = minor (acceptable for v1.3). FAIL = blocker requiring a follow-up gap-closure plan.
    5. Per D-11: this UAT is NOT blocking ship but findings are recorded in commit notes.

    PART B — Decommission 42-01 spike:
    6. `supabase functions delete _spike_web_push --project-ref ytnsipxxmzgaebkqmokp` to remove the disposable Edge Fn.
    7. Optionally `rm -rf /Users/karstenhaldan/minisite/supabase/functions/_spike_web_push/` to remove source — keep 42-01-SPIKE-RESULT.md as historical record.
    8. Verify: `supabase functions list --project-ref ytnsipxxmzgaebkqmokp | grep _spike_web_push` returns empty.

    PART C — Final gate:
    9. Run the full test suite: `cd leanshot && npm run test && npm run test:a11y` — all green.
    10. Confirm in the PR/commit message that all 7 REQ-IDs map to evidence: POLISH-05 → 42-05/42-08; POLISH-06 → 42-08; POLISH-07 → 42-04; POLISH-08 → 42-03; POLISH-09 → 42-02; POLISH-11 → 42-06/42-09; POLISH-12 → 42-07/42-10.
  </how-to-verify>
  <resume-signal>Type "phase-42-closed" once: (a) 42-VOICEOVER-UAT.md committed with PASS/WARN/FAIL annotations for all 5 flows, (b) _spike_web_push deleted from project, (c) full test suite green, (d) all 7 REQ-IDs mapped</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase exit gate → main branch | Final integration verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-11-01 | Information Disclosure | Disposable spike Fn left in production | mitigate | Task 2 PART B deletes _spike_web_push from project |
| T-42-11-02 | Repudiation | Baseline silently allows new regressions | mitigate | Task 1 explicitly fails if new routes baseline > 0 blocking — forces fix in owning plan |
</threat_model>

<verification>
- All 7 POLISH REQ-IDs have observable evidence.
- accessibility-baseline.json has new routes at blocking=0.
- Bundle budget passes.
- VoiceOver UAT completed and documented.
- Spike Fn decommissioned.
</verification>

<success_criteria>
- Phase 42 ships; v1.3 milestone is structurally complete pending milestone close-out.
- A re-run of `npm run test:a11y` 3 months from now will fail if any v1.3 surface regresses (per __meta.quarterly_review_due).
</success_criteria>

<output>
After completion, create `.planning/phases/42-v1-3-polish-closeout/42-11-SUMMARY.md` AND a phase-level rollup `.planning/phases/42-v1-3-polish-closeout/42-PHASE-SUMMARY.md` cross-referencing all 11 plan SUMMARYs and mapping REQ-IDs to ship evidence.
</output>
