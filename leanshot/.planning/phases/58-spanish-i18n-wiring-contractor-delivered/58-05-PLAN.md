---
phase: 58-spanish-i18n-wiring-contractor-delivered
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/src/components/dashboard/cards/EffectivenessCard.tsx
  - leanshot/src/components/dashboard/cards/FocusCard.tsx
  - leanshot/src/components/dashboard/cards/ForYouCard.tsx
  - leanshot/src/components/dashboard/cards/GamificationCard.tsx
  - leanshot/src/components/dashboard/cards/GLPCurveCard.tsx
  - leanshot/src/components/dashboard/cards/HeroCard.tsx
  - leanshot/src/components/dashboard/cards/LeaderboardCard.tsx
  - leanshot/src/components/dashboard/cards/LevelProgressCard.tsx
  - leanshot/src/components/dashboard/cards/QuickLogCard.tsx
  - leanshot/src/components/dashboard/cards/SiteRotationCard.tsx
  - leanshot/src/components/dashboard/cards/StreakCard.tsx
  - leanshot/src/components/dashboard/cards/StreaksCard.tsx
  - leanshot/src/components/dashboard/cards/SymptomCard.tsx
  - leanshot/src/components/dashboard/cards/WeeklyChallengeCard.tsx
  - leanshot/public/locales/en/patient.json
  - leanshot/public/locales/es/patient.json
autonomous: true
requirements: [I18N-11, I18N-13]
must_haves:
  truths:
    - "A user on /?lang=es sees all dashboard cards (hero, site rotation, symptoms, streaks, GLP curve) rendered in Spanish"
    - "Clinical card strings (phase labels, injection sites, symptom names, dose/level copy) are keyed and ES-translated"
    - "en/patient.json and es/patient.json have identical leaf-key sets after this plan"
  artifacts:
    - path: "leanshot/public/locales/en/patient.json"
      provides: "EN source keys for dashboard cards under card.* prefix (this plan ESTABLISHES the file)"
      contains: "card"
    - path: "leanshot/public/locales/es/patient.json"
      provides: "ES translations at parity"
      contains: "card"
  key_links:
    - from: "leanshot/src/components/dashboard/cards/SiteRotationCard.tsx"
      to: "patient namespace"
      via: "useTranslation('patient')"
      pattern: "useTranslation\\(\\['?patient"
---

<objective>
Externalize inline English in the dashboard CARDS into the `patient` namespace under the `card.*` key prefix and ship ES translations at parity. This is the FIRST of three patient-namespace plans; it ESTABLISHES `public/locales/{en,es}/patient.json`. Plans 58-06 (tabs) and 58-07 (overlays) are serialized AFTER this via depends_on to avoid concurrent patient.json writes (per the coordination-in-depends_on rule — same-namespace JSON writers must NOT run in the same wave).

Purpose: Cards are the dashboard centerpiece (site rotation, GLP curve, hero = the product's headline per CLAUDE.md). They carry the densest clinical strings (phase labels, injection sites, symptom names).
Output: 14 keyed card components + patient.json (en+es) populated with `card.*` keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-PATTERNS.md

<read_first>
- leanshot/src/components/dashboard/cards/SymptomCard.tsx  (canonical inline->t() analog, full file)
- leanshot/src/components/dashboard/cards/SiteRotationCard.tsx  (inline CardHeader title + legend labels)
- leanshot/src/components/dashboard/cards/HeroCard.tsx  (clinical phase/phaseTip/direction strings ~lines 48-59)
- leanshot/src/components/layout/GreetingStrip.tsx  (single-ns useTranslation analog)
- leanshot/src/lib/i18n/nav-labels.ts  (static switch helper for any dynamic card label arrays)
- leanshot/public/locales/en/common.json + es/common.json  (ICU + plural reference; reuse common:injection_one/_other if applicable)
- leanshot/.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-PATTERNS.md  (section 3: card.* key structure + HeroCard clinical keying)
</read_first>

<interfaces>
<!-- Verified facts. -->
- KEY-PREFIX OWNERSHIP (coordination): this plan owns `patient:card.*` and `patient:<plural>` count keys used by cards. Plan 58-06 (tabs) owns `patient:tab.*`; plan 58-07 (overlays) owns `patient:ai.*`/`patient:modal.*`/`patient:chart.*`. Disjoint key prefixes keep the serialized patient.json appends conflict-free.
- This plan ESTABLISHES patient.json (currently `{}`). 58-06 and 58-07 APPEND via i18next-parser extraction over the union of all keyed callsites — they depend_on this plan so the file is non-empty and conflict-ordered.
- i18next-parser: static t('patient:...') literals only. Dynamic label arrays (e.g. symptom lists, phase labels) MUST use exhaustive switch helpers (nav-labels.ts pattern), NOT template literals.
- Plural counts: use `patient:dose_count_one`/`_other` with {{count}}; ES translates value only, keeps English suffix.
- Clinical strings (HeroCard phase labels, injection sites, symptom names) keyed normally; ES governed by clinical-glossary.md (58-01), flagged for P70 signoff — do not block.
- eslint i18next/no-literal-string mode jsx-text-only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Key clinical-heavy cards (Hero, SiteRotation, Symptom, GLPCurve, Effectiveness)</name>
  <files>leanshot/src/components/dashboard/cards/HeroCard.tsx, leanshot/src/components/dashboard/cards/SiteRotationCard.tsx, leanshot/src/components/dashboard/cards/SymptomCard.tsx, leanshot/src/components/dashboard/cards/GLPCurveCard.tsx, leanshot/src/components/dashboard/cards/EffectivenessCard.tsx</files>
  <read_first>
    - leanshot/src/components/dashboard/cards/HeroCard.tsx
    - leanshot/src/components/dashboard/cards/SiteRotationCard.tsx
    - leanshot/src/components/dashboard/cards/SymptomCard.tsx
    - leanshot/src/components/dashboard/cards/GLPCurveCard.tsx
    - leanshot/src/components/dashboard/cards/EffectivenessCard.tsx
  </read_first>
  <action>
    Add useTranslation('patient') (or ['patient','common']) to each card. Replace inline JSX text + user-visible string props with t('patient:card.<card>.<element>') literals per 58-PATTERNS section 3. HeroCard: key the phase labels (Tolerance/Titration/Maintenance), phase tips, and direction (Lost/Gained) via a static switch on the computed phase (NOT inline ternary strings) so the parser sees each key — these are clinical, align ES with clinical-glossary.md. SiteRotationCard: key the CardHeader title + the three legend labels (recent/older/next). SymptomCard: key title, log action, empty title/body, pattern warning. GLPCurveCard + EffectivenessCard: key medication-level / effectiveness copy (clinical). Reuse common:action.* and common:injection_one/_other where exact matches exist.
  </action>
  <verify>
    <automated>cd leanshot && for f in HeroCard SiteRotationCard SymptomCard GLPCurveCard EffectivenessCard; do grep -q "useTranslation('patient'\|useTranslation(\['patient'" src/components/dashboard/cards/$f.tsx || { echo "FAIL $f"; exit 1; }; done; ! grep -rE "t\(\`patient:" src/components/dashboard/cards/ && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c 'error TS' | grep -qx 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 5 cards call useTranslation for the patient namespace
    - HeroCard clinical phase/direction labels resolve via static keys (no inline ternary string literals for phase/phaseTip/direction)
    - No template-literal patient keys in src/components/dashboard/cards/
    - npx tsc -p tsconfig.app.json --noEmit 0 errors; npx eslint clean (no-literal-string) for these 5 files
  </acceptance_criteria>
  <done>Clinical-heavy cards keyed under patient:card.*; clinical labels are static keys; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Key remaining cards; establish patient.json; translate ES</name>
  <files>leanshot/src/components/dashboard/cards/FocusCard.tsx, leanshot/src/components/dashboard/cards/ForYouCard.tsx, leanshot/src/components/dashboard/cards/GamificationCard.tsx, leanshot/src/components/dashboard/cards/LeaderboardCard.tsx, leanshot/src/components/dashboard/cards/LevelProgressCard.tsx, leanshot/src/components/dashboard/cards/QuickLogCard.tsx, leanshot/src/components/dashboard/cards/StreakCard.tsx, leanshot/src/components/dashboard/cards/StreaksCard.tsx, leanshot/src/components/dashboard/cards/WeeklyChallengeCard.tsx, leanshot/public/locales/en/patient.json, leanshot/public/locales/es/patient.json</files>
  <read_first>
    - leanshot/src/components/dashboard/cards/FocusCard.tsx (+ the other 8 listed cards)
    - leanshot/public/locales/es/common.json (ES style reference)
    - leanshot/.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md (EN->ES Machine Translation Protocol)
  </read_first>
  <action>
    Key the remaining 9 cards with useTranslation('patient'); externalize user-visible strings under patient:card.<card>.*. For gamification/streak count strings use plural keys (patient:streak_day_one/_other with {{count}}).
    Run cd leanshot && npm run i18n:extract — this ESTABLISHES en/patient.json (no longer {}) from the union of all 14 keyed cards. Inspect git diff. Machine/Claude-translate every leaf key into es/patient.json: identical key structure, {{vars}} verbatim, _one/_other English suffixes, "tú" patient address, clinical terms via clinical-glossary.md. Run npm run i18n:check; confirm patient namespace passes.
  </action>
  <verify>
    <automated>cd leanshot && npm run i18n:extract >/dev/null 2>&1; test "$(cat public/locales/en/patient.json)" != "{}"; EN=$(jq -r 'paths(scalars)|join(".")' public/locales/en/patient.json|sort -u); ES=$(jq -r 'paths(scalars)|join(".")' public/locales/es/patient.json|sort -u); [ "$EN" = "$ES" ] && echo PARITY-PASS || echo PARITY-FAIL; ! grep -E '\{\{[a-záéíóúñ]' public/locales/es/patient.json && echo ICU-PASS; bash scripts/check-locale-coverage.sh 2>&1 | (grep -qiE 'patient.*(fail|mismatch|missing)' && echo COV-FAIL || echo COV-PASS)</automated>
  </verify>
  <acceptance_criteria>
    - All 9 remaining cards call useTranslation('patient'); npx eslint clean (no-literal-string) across src/components/dashboard/cards/
    - public/locales/en/patient.json is non-empty and contains card.* keys
    - es/patient.json leaf-key set IDENTICAL to en/patient.json
    - bash scripts/check-locale-coverage.sh patient namespace at parity
    - grep -E '\{\{[a-záéíóúñ]' public/locales/es/patient.json finds nothing
    - npm run i18n:extract && git diff --quiet -- public/locales/en/patient.json (no drift)
  </acceptance_criteria>
  <done>All 14 cards keyed; patient.json established (en) and at full ES parity; ready for 58-06 to append tab keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patient ES catalog (card.*) -> rendered UI | dashboard card clinical copy shown to patients |
| serialized patient.json appends | three plans write patient.json; concurrent writes would conflict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | es/patient.json interpolation integrity | mitigate | Task 2 asserts no translated `{{vars}}`; Gate 3 (58-01) enforces in CI |
| T-58-02 | Tampering | clinical card strings (phase labels, injection sites, symptoms, dose/level copy) | mitigate + transfer | ES routed through clinical-glossary.md; human signoff TRANSFERRED to Phase 70 |
| T-58-05 | Information Disclosure | missing ES key -> silent EN fallthrough | mitigate | check-locale-coverage.sh parity gate (Task 2) |
| T-58-09 | Tampering | concurrent patient.json writes across plans | mitigate | depends_on serialization: 58-05 (cards) -> 58-06 (tabs) -> 58-07 (overlays), disjoint key prefixes (card./tab./ai.) |
| T-58-SC | Tampering | npm installs | accept | zero new packages this plan |
</threat_model>

<verification>
- cd leanshot && bash scripts/check-locale-coverage.sh passes for patient namespace
- cd leanshot && npx tsc -p tsconfig.app.json --noEmit 0 errors
- es/patient.json has no translated interpolation variable names
</verification>

<success_criteria>
- All 14 dashboard cards keyed to patient:card.*
- patient.json established (en) and at full ES parity
- Clinical labels are static keys aligned with clinical-glossary.md
</success_criteria>

<output>
Create `.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-05-SUMMARY.md` when done
</output>
