---
phase: 58-spanish-i18n-wiring-contractor-delivered
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/src/components/onboarding/OnboardingFlow.tsx
  - leanshot/src/components/onboarding/ConsumerOnboardingRenderer.tsx
  - leanshot/src/components/onboarding/ProgressIndicator.tsx
  - leanshot/src/components/onboarding/UnitToggle.tsx
  - leanshot/src/components/onboarding/AnonymousPreviewView.tsx
  - leanshot/src/components/onboarding/AnonymousPreviewLayer.tsx
  - leanshot/src/components/onboarding/FirstActionSurface.tsx
  - leanshot/src/components/onboarding/social-proof/LiveSignupCounter.tsx
  - leanshot/src/components/onboarding/social-proof/TestimonialRotator.tsx
  - leanshot/src/lib/i18n/onboarding-labels.ts
  - leanshot/public/locales/en/onboarding.json
  - leanshot/public/locales/es/onboarding.json
autonomous: true
requirements: [I18N-11, I18N-13]
must_haves:
  truths:
    - "A user on /?lang=es sees the onboarding flow rendered in Spanish (name label, medication step, units, CTAs)"
    - "OnboardingFlow medication/goal option labels resolve through static-key helpers (no template-literal keys)"
    - "en/onboarding.json and es/onboarding.json have identical leaf-key sets"
  artifacts:
    - path: "leanshot/public/locales/en/onboarding.json"
      provides: "EN source keys for the onboarding surface"
      contains: "step"
    - path: "leanshot/public/locales/es/onboarding.json"
      provides: "ES translations at leaf-key parity with EN"
      contains: "step"
    - path: "leanshot/src/lib/i18n/onboarding-labels.ts"
      provides: "exhaustive switch helpers for medication + goal option labels"
      exports: ["medicationLabel"]
  key_links:
    - from: "leanshot/src/components/onboarding/OnboardingFlow.tsx"
      to: "onboarding namespace"
      via: "useTranslation('onboarding')"
      pattern: "useTranslation\\(\\['?onboarding"
    - from: "leanshot/src/lib/i18n/onboarding-labels.ts"
      to: "onboarding.json"
      via: "static t('onboarding:...') calls in exhaustive switch"
      pattern: "case '[a-z]+': return t\\('onboarding:"
---

<objective>
Externalize all inline English in the onboarding surface into the `onboarding` namespace and ship ES translations at parity. This plan EXCLUSIVELY owns `public/locales/{en,es}/onboarding.json` so it runs in Wave 1 with zero JSON-write conflicts against the other namespace plans.

Purpose: Onboarding is step 2 of the I18N-15 critical flow (signup -> onboarding -> dose log -> AI chat -> cancellation -> KB search). It is the densest single inline-English file in scope (OnboardingFlow.tsx ~1206 lines, ~32 string patterns).
Output: Keyed onboarding components + onboarding-labels.ts helper + populated en/es onboarding.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-PATTERNS.md

<read_first>
- leanshot/src/components/onboarding/OnboardingFlow.tsx  (the target — inline strings at lines ~344-461; medication/goal option arrays)
- leanshot/src/components/layout/GreetingStrip.tsx  (single-namespace useTranslation analog: import line 2, hook line 22)
- leanshot/src/components/layout/Sidebar.tsx  (multi-namespace analog: `useTranslation(['nav','common'])` line 78)
- leanshot/src/lib/i18n/nav-labels.ts  (exhaustive switch static-key helper — lines 19-52 — COPY this pattern for onboarding-labels.ts)
- leanshot/public/locales/en/common.json + es/common.json  (ICU + plural reference; key structure)
- leanshot/public/locales/en/nav.json  (flat-key reference)
</read_first>

<interfaces>
<!-- Verified facts. -->
- i18next-parser config: `namespaceSeparator: ':'`, `keySeparator: '.'`, `pluralSeparator: '_'`, `functions: ['t']`, lexer reads STATIC `t('...')` calls only. Template literals `t(`onboarding:${id}`)` extract ZERO keys (Pitfall 1). Use exhaustive switch helpers for dynamic label arrays.
- `npm run i18n:extract` (= `i18next-parser`) populates public/locales/en/onboarding.json from the t() calls. It does NOT write es/. ES is hand/machine-translated.
- eslint rule `i18next/no-literal-string` mode is `jsx-text-only` — only JSX text nodes must be keyed; aria-labels and string props passed to attributes are NOT flagged (do not over-key aria-labels unless user-visible).
- onboarding namespace is lazy-loaded by http-backend on first `useTranslation('onboarding')` — no change to src/lib/i18n/init.ts `ns` list needed.
- Plural keys use ENGLISH suffixes `_one`/`_other` regardless of target language (i18next pluralSeparator). ES translates the VALUE only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Key OnboardingFlow + create onboarding-labels.ts helper</name>
  <files>leanshot/src/components/onboarding/OnboardingFlow.tsx, leanshot/src/components/onboarding/ConsumerOnboardingRenderer.tsx, leanshot/src/lib/i18n/onboarding-labels.ts</files>
  <read_first>
    - leanshot/src/components/onboarding/OnboardingFlow.tsx
    - leanshot/src/components/onboarding/ConsumerOnboardingRenderer.tsx
    - leanshot/src/lib/i18n/nav-labels.ts
  </read_first>
  <action>
    Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation(['onboarding', 'common']);` to OnboardingFlow and ConsumerOnboardingRenderer. Replace every inline JSX English text node and user-visible string prop (labels, placeholders, headings, subheads, CTA button text, step titles, `<option>` text) with `t('onboarding:<dotted.key>')` using literals. Follow the key structure in 58-PATTERNS.md section 2 (`step.body.*`, `step.medication.*`). For shared primitives (Continue/Back/Save/Cancel) use `t('common:action.*')` (those keys already exist in common.json — do NOT duplicate into onboarding.json).
    Create src/lib/i18n/onboarding-labels.ts: export exhaustive-switch helper functions for the dynamic option arrays — `medicationLabel(t, id: MedicationId)` and `goalLabel(t, id: GoalType)` (and any other iterated option list e.g. units, activity level) — each a `switch` returning a STATIC `t('onboarding:step.medication.<id>')` literal per case with a `default: { const _exhaustive: never = id; return _exhaustive; }` exhaustiveness guard. Wire OnboardingFlow/ConsumerOnboardingRenderer to call these helpers instead of inline `<option>` labels. This guarantees i18next-parser sees every key statically. Use the correct domain types from src/types/index.ts (MedicationId, GoalType, etc.).
    Clinical strings (medication names, dose units mg/mL/units) are keyed normally here; their ES translations are governed by clinical-glossary.md (plan 58-01) and flagged for P70 signoff — do NOT block on signoff.
  </action>
  <verify>
    <automated>cd leanshot && grep -q "useTranslation(\['onboarding'" src/components/onboarding/OnboardingFlow.tsx && test -f src/lib/i18n/onboarding-labels.ts && grep -cE "case '[a-z_]+': return t\('onboarding:" src/lib/i18n/onboarding-labels.ts | awk '$1>=3{print "PASS"; exit} {print "FAIL need>=3 static cases"}' && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c 'error TS' | grep -qx 0 && echo TSC-PASS</automated>
  </verify>
  <acceptance_criteria>
    - OnboardingFlow.tsx + ConsumerOnboardingRenderer.tsx import and call `useTranslation(['onboarding','common'])`
    - src/lib/i18n/onboarding-labels.ts exists and exports `medicationLabel` (and goalLabel) as exhaustive switches with >=3 static `t('onboarding:...')` cases each and a `never` exhaustiveness default
    - No template-literal `t()` calls in any onboarding file: `grep -rE "t\(\`onboarding:" src/components/onboarding/` returns nothing
    - `npx tsc -p tsconfig.app.json --noEmit` reports 0 errors
    - `npx eslint src/components/onboarding/OnboardingFlow.tsx` reports no `i18next/no-literal-string` violations
  </acceptance_criteria>
  <done>OnboardingFlow + ConsumerOnboardingRenderer render via t(); dynamic option labels resolve through static-key switch helpers; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Key remaining onboarding components + extract + translate ES</name>
  <files>leanshot/src/components/onboarding/ProgressIndicator.tsx, leanshot/src/components/onboarding/UnitToggle.tsx, leanshot/src/components/onboarding/AnonymousPreviewView.tsx, leanshot/src/components/onboarding/AnonymousPreviewLayer.tsx, leanshot/src/components/onboarding/FirstActionSurface.tsx, leanshot/src/components/onboarding/social-proof/LiveSignupCounter.tsx, leanshot/src/components/onboarding/social-proof/TestimonialRotator.tsx, leanshot/public/locales/en/onboarding.json, leanshot/public/locales/es/onboarding.json</files>
  <read_first>
    - leanshot/src/components/onboarding/ProgressIndicator.tsx (+ the other 6 files listed)
    - leanshot/public/locales/es/common.json (ES translation style: tú address, {{count}} verbatim, _one/_other suffix in English)
    - leanshot/.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md (EN->ES Machine Translation Protocol)
  </read_first>
  <action>
    Key the remaining onboarding components (ProgressIndicator, UnitToggle, AnonymousPreviewView, AnonymousPreviewLayer, FirstActionSurface, LiveSignupCounter, TestimonialRotator) with `useTranslation('onboarding')` (or `['onboarding','common']` if they use shared actions). LiveSignupCounter has a count interpolation — use a plural key `onboarding:social.signups_today_one`/`_other` with `{{count}}`.
    Run `cd leanshot && npm run i18n:extract` to populate public/locales/en/onboarding.json from all keyed callsites. Inspect `git diff public/locales/en/onboarding.json`.
    Machine/Claude-translate EVERY leaf key into public/locales/es/onboarding.json following the protocol: byte-for-byte identical key structure to en/; preserve `{{vars}}` verbatim (NEVER {{recuento}}); keep `_one`/`_other` English suffixes; Latin-American neutral Spanish, "tú" address; route clinical terms (medication names, mg/mL/units) through docs/clinical-glossary.md phrasing.
    Run `npm run i18n:check` and confirm onboarding namespace passes (en<->es parity).
  </action>
  <verify>
    <automated>cd leanshot && npm run i18n:extract >/dev/null 2>&1; bash scripts/check-locale-coverage.sh 2>&1 | (grep -qiE 'onboarding.*(fail|mismatch|missing)' && echo FAIL || echo PASS); EN=$(jq -r 'paths(scalars)|join(".")' public/locales/en/onboarding.json | sort -u); ES=$(jq -r 'paths(scalars)|join(".")' public/locales/es/onboarding.json | sort -u); [ "$EN" = "$ES" ] && echo PARITY-PASS || echo PARITY-FAIL; ! grep -E '\{\{[a-záéíóúñ]' public/locales/es/onboarding.json && echo ICU-PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 7 remaining onboarding files call useTranslation and contain no flagged inline JSX text (`npx eslint src/components/onboarding/` clean for no-literal-string)
    - `public/locales/en/onboarding.json` is non-empty (no longer `{}`) and contains the keyed strings
    - `public/locales/es/onboarding.json` leaf-key set is IDENTICAL to en/onboarding.json (jq paths diff empty)
    - `bash scripts/check-locale-coverage.sh` reports onboarding namespace at parity (no FAIL)
    - `grep -E '\{\{[a-záéíóúñ]' public/locales/es/onboarding.json` finds NOTHING (Gate 3 ICU integrity — no translated var names)
    - `npm run i18n:extract && git diff --quiet -- public/locales/en/onboarding.json` (no drift after extraction)
  </acceptance_criteria>
  <done>All onboarding components keyed; en/onboarding.json populated by parser; es/onboarding.json at full leaf-key parity with preserved interpolations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onboarding ES catalog -> rendered UI | medication/dose-unit strings shown to patients during signup |
| dynamic option arrays -> i18next-parser | template-literal keys would be invisible to the static parser, producing missing-key EN fallthrough |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | es/onboarding.json interpolation integrity | mitigate | Task 2 acceptance asserts no translated `{{vars}}`; Gate 3 (58-01) enforces in CI |
| T-58-02 | Tampering | onboarding clinical strings (medication names, mg/mL/units) | mitigate + transfer | ES phrasing routed through docs/clinical-glossary.md; human signoff TRANSFERRED to Phase 70 |
| T-58-05 | Information Disclosure | missing ES key -> silent EN fallthrough masking a gap | mitigate | check-locale-coverage.sh parity gate (Task 2) fails on any missing onboarding ES key; the es-smoke spec (58-08) asserts real ES onboarding strings render |
| T-58-SC | Tampering | npm installs | accept | zero new packages this plan |
</threat_model>

<verification>
- `cd leanshot && bash scripts/check-locale-coverage.sh` passes for onboarding namespace
- `cd leanshot && npx tsc -p tsconfig.app.json --noEmit` 0 errors
- `cd leanshot && npx eslint src/components/onboarding/` no no-literal-string violations
- es/onboarding.json has no translated interpolation variable names
</verification>

<success_criteria>
- All 9 onboarding component files keyed to the `onboarding` namespace
- onboarding-labels.ts static-key helpers replace dynamic option labels
- en/onboarding.json populated by i18next-parser; es/onboarding.json at full parity
- ES interpolations preserved verbatim; clinical terms flagged via glossary
</success_criteria>

<output>
Create `.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-02-SUMMARY.md` when done
</output>
