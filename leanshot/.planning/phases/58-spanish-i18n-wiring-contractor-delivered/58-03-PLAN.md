---
phase: 58-spanish-i18n-wiring-contractor-delivered
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/src/components/clinic-invite/ClinicInvitePage.tsx
  - leanshot/src/components/clinic-invite/ConsentDialog.tsx
  - leanshot/src/components/clinic-invite/InviteSignupForm.tsx
  - leanshot/public/locales/en/clinic.json
  - leanshot/public/locales/es/clinic.json
autonomous: true
requirements: [I18N-11, I18N-13]
must_haves:
  truths:
    - "A patient opening a clinic invite on /?lang=es sees the invite/consent/signup flow in Spanish"
    - "Only patient-facing clinic-invite strings are keyed; clinician-admin surfaces are untouched"
    - "en/clinic.json and es/clinic.json have identical leaf-key sets"
  artifacts:
    - path: "leanshot/public/locales/en/clinic.json"
      provides: "EN source keys for the patient-side clinic invite surface"
      contains: "invite"
    - path: "leanshot/public/locales/es/clinic.json"
      provides: "ES translations at parity with EN"
      contains: "invite"
  key_links:
    - from: "leanshot/src/components/clinic-invite/ClinicInvitePage.tsx"
      to: "clinic namespace"
      via: "useTranslation('clinic')"
      pattern: "useTranslation\\(\\['?clinic"
---

<objective>
Externalize inline English in the PATIENT-SIDE clinic invite flow into the `clinic` namespace with ES translations at parity. This plan EXCLUSIVELY owns `public/locales/{en,es}/clinic.json` (Wave 1, zero JSON conflicts). It is intentionally narrow: only `src/components/clinic-invite/*` (patient consents to clinic access) — NOT `src/components/clinic/*` (clinician-admin, OUT of scope).

Purpose: I18N-11 patient-facing clinic strings. The clinic-invite flow is patient-visible (a patient accepting a clinic's data-access request); clinician roster/alerts/billing surfaces stay English (admin scope, OUT).
Output: Keyed clinic-invite components + populated en/es clinic.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-PATTERNS.md

<read_first>
- leanshot/src/components/clinic-invite/ClinicInvitePage.tsx  (target — state-machine render switch with inline error/consent/signup strings, ~lines 105-160)
- leanshot/src/components/clinic-invite/ConsentDialog.tsx
- leanshot/src/components/clinic-invite/InviteSignupForm.tsx
- leanshot/src/components/dashboard/cards/SymptomCard.tsx  (inline-string -> t() analog)
- leanshot/public/locales/en/common.json + es/common.json  (ICU reference; reuse common:action.* for Accept/Decline/Cancel if present)
- leanshot/.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-PATTERNS.md  (section 6: en/clinic.json key structure `invite.*`)
</read_first>

<interfaces>
<!-- Verified facts. -->
- SCOPE BOUNDARY (RESEARCH Pitfall 4): `clinic` namespace covers ONLY patient-facing clinic-invite strings (ClinicInvitePage, ConsentDialog, InviteSignupForm). DO NOT key clinician-admin strings (roster, alerts, billing, ClinicWorkspace, ClinicDashboardOverview) — those are admin surface, OUT of scope. If a string is shown to a clinician-operator, leave it English.
- clinic.json is currently `{}` in both en/ and es/.
- i18next-parser: static `t('clinic:...')` literals only; no template literals.
- Plural/interpolation: `{{orgName}}`, `{{email}}` preserved verbatim in ES.
- Consent text is consent-flow UI copy (not clinical dosing) — translate normally; no glossary flag needed unless a medical term appears.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Key clinic-invite patient-side components</name>
  <files>leanshot/src/components/clinic-invite/ClinicInvitePage.tsx, leanshot/src/components/clinic-invite/ConsentDialog.tsx, leanshot/src/components/clinic-invite/InviteSignupForm.tsx</files>
  <read_first>
    - leanshot/src/components/clinic-invite/ClinicInvitePage.tsx
    - leanshot/src/components/clinic-invite/ConsentDialog.tsx
    - leanshot/src/components/clinic-invite/InviteSignupForm.tsx
    - leanshot/src/components/layout/GreetingStrip.tsx (single-ns useTranslation analog)
  </read_first>
  <action>
    Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation(['clinic', 'common']);` to each of the three clinic-invite components. Replace inline JSX English text and user-visible string props with `t('clinic:<dotted.key>')` literals, following the key structure in 58-PATTERNS.md section 6 (`invite.loading`, `invite.error.load`, `invite.error.expired_title`, `invite.error.already_used`, `invite.error.canceled`, `invite.consent.title/body/accept/decline`, `invite.signup.title/body`, `invite.magic_link.title/body`). Preserve `{{orgName}}` and `{{email}}` interpolations. Reuse `common:action.*` for generic Accept/Decline/Cancel/Continue ONLY if exact-match keys already exist in common.json; otherwise key under `clinic:invite.consent.*`. Do NOT touch any file under src/components/clinic/ (clinician-admin — OUT of scope).
  </action>
  <verify>
    <automated>cd leanshot && grep -q "useTranslation(\['clinic'" src/components/clinic-invite/ClinicInvitePage.tsx && ! grep -rE "t\(\`clinic:" src/components/clinic-invite/ && [ -z "$(git diff --name-only -- src/components/clinic/ 2>/dev/null)" ] && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c 'error TS' | grep -qx 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 3 clinic-invite files call `useTranslation(['clinic','common'])`
    - No template-literal clinic keys: `grep -rE "t\(\`clinic:" src/components/clinic-invite/` returns nothing
    - ZERO files under src/components/clinic/ modified (scope guard — `git diff --name-only -- src/components/clinic/` empty)
    - `npx tsc -p tsconfig.app.json --noEmit` 0 errors
    - `npx eslint src/components/clinic-invite/` no no-literal-string violations
  </acceptance_criteria>
  <done>All patient-side clinic-invite strings keyed to the clinic namespace; clinician-admin surfaces untouched; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Extract EN + translate ES clinic.json</name>
  <files>leanshot/public/locales/en/clinic.json, leanshot/public/locales/es/clinic.json</files>
  <read_first>
    - leanshot/public/locales/es/common.json (ES style reference)
    - leanshot/.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md (EN->ES Machine Translation Protocol)
  </read_first>
  <action>
    Run `cd leanshot && npm run i18n:extract` to populate public/locales/en/clinic.json from the keyed callsites. Inspect `git diff public/locales/en/clinic.json`. Machine/Claude-translate every leaf key into public/locales/es/clinic.json: byte-for-byte identical key structure; preserve `{{orgName}}`/`{{email}}` verbatim; Latin-American neutral Spanish, "tú" address for patient-facing copy. Run `npm run i18n:check` and confirm clinic namespace passes.
  </action>
  <verify>
    <automated>cd leanshot && npm run i18n:extract >/dev/null 2>&1; EN=$(jq -r 'paths(scalars)|join(".")' public/locales/en/clinic.json | sort -u); ES=$(jq -r 'paths(scalars)|join(".")' public/locales/es/clinic.json | sort -u); [ "$EN" = "$ES" ] && echo PARITY-PASS || echo PARITY-FAIL; ! grep -E '\{\{[a-záéíóúñ]' public/locales/es/clinic.json && echo ICU-PASS; bash scripts/check-locale-coverage.sh 2>&1 | (grep -qiE 'clinic.*(fail|mismatch|missing)' && echo COV-FAIL || echo COV-PASS)</automated>
  </verify>
  <acceptance_criteria>
    - public/locales/en/clinic.json is non-empty (no longer `{}`)
    - es/clinic.json leaf-key set IDENTICAL to en/clinic.json (jq paths diff empty)
    - `bash scripts/check-locale-coverage.sh` clinic namespace at parity
    - `grep -E '\{\{[a-záéíóúñ]' public/locales/es/clinic.json` finds nothing
    - `npm run i18n:extract && git diff --quiet -- public/locales/en/clinic.json` (no drift)
  </acceptance_criteria>
  <done>en/clinic.json populated; es/clinic.json at full parity with preserved interpolations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clinic ES catalog -> rendered UI | invite/consent copy shown to patients granting clinic data access |
| scope boundary clinic-invite vs clinic | keying clinician-admin strings would leak OUT-of-scope work into the patient namespace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | es/clinic.json interpolation integrity | mitigate | Task 2 asserts no translated `{{orgName}}`/`{{email}}`; Gate 3 (58-01) enforces in CI |
| T-58-05 | Information Disclosure | missing ES key -> silent EN fallthrough | mitigate | check-locale-coverage.sh parity gate (Task 2) fails on any missing clinic ES key |
| T-58-06 | Tampering | scope creep into clinician-admin surfaces | mitigate | Task 1 acceptance asserts ZERO files under src/components/clinic/ modified |
| T-58-SC | Tampering | npm installs | accept | zero new packages this plan |
</threat_model>

<verification>
- `cd leanshot && bash scripts/check-locale-coverage.sh` passes for clinic namespace
- `cd leanshot && npx tsc -p tsconfig.app.json --noEmit` 0 errors
- `git diff --name-only -- leanshot/src/components/clinic/` is empty (scope guard)
</verification>

<success_criteria>
- Patient-side clinic-invite components keyed to `clinic` namespace
- en/clinic.json populated; es/clinic.json at full parity; interpolations preserved
- Clinician-admin surfaces untouched
</success_criteria>

<output>
Create `.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-03-SUMMARY.md` when done
</output>
