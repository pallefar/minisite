---
phase: 58-spanish-i18n-wiring-contractor-delivered
plan: 07
type: execute
wave: 3
depends_on: ["58-06"]
files_modified:
  - leanshot/src/components/dashboard/ai/AIChatPanel.tsx
  - leanshot/src/components/dashboard/modals/DoctorReport.tsx
  - leanshot/src/components/dashboard/modals/PhotoCompareModal.tsx
  - leanshot/src/components/dashboard/charts/MedLevelChart.tsx
  - leanshot/src/components/dashboard/charts/SimpleCharts.tsx
  - leanshot/src/components/layout/GreetingStrip.tsx
  - leanshot/public/locales/en/patient.json
  - leanshot/public/locales/es/patient.json
autonomous: true
requirements: [I18N-11, I18N-13]
must_haves:
  truths:
    - "A user on /?lang=es sees the AI chat panel, doctor report / photo-compare modals, and chart labels rendered in Spanish"
    - "GreetingStrip has no remaining inline patient English strings (audited)"
    - "en/patient.json and es/patient.json remain at identical leaf-key sets after overlay keys are appended"
  artifacts:
    - path: "leanshot/public/locales/en/patient.json"
      provides: "ai.* / modal.* / chart.* keys appended to card.* + tab.* (final patient namespace state)"
      contains: "ai"
  key_links:
    - from: "leanshot/src/components/dashboard/ai/AIChatPanel.tsx"
      to: "patient namespace"
      via: "useTranslation(['patient','common'])"
      pattern: "useTranslation\\(\\['?patient"
---

<objective>
Externalize inline English in the dashboard OVERLAYS — AI chat panel (I18N-15 step 4), doctor report + photo-compare modals, chart labels — into the `patient` namespace under `ai.*`/`modal.*`/`chart.*`, and audit GreetingStrip for any remaining inline patient strings. This is the THIRD/final patient-namespace plan, APPENDING to patient.json (card.* from 58-05, tab.* from 58-06). Serialized last via depends_on 58-06 (same-namespace JSON writer).

Purpose: AI chat is step 4 of the I18N-15 smoke; modals/charts complete the dashboard surface. This plan finalizes the patient namespace so the es-smoke (58-08) can assert ES across the whole dashboard.
Output: Keyed AI/modals/charts + GreetingStrip audit + final patient.json (en+es) with ai./modal./chart. keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-PATTERNS.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-06-SUMMARY.md

<read_first>
- leanshot/src/components/dashboard/ai/AIChatPanel.tsx  (AI coach panel — prompts, empty state, error copy)
- leanshot/src/components/dashboard/modals/DoctorReport.tsx
- leanshot/src/components/dashboard/modals/PhotoCompareModal.tsx
- leanshot/src/components/dashboard/charts/MedLevelChart.tsx + SimpleCharts.tsx  (user-visible axis/legend labels)
- leanshot/src/components/layout/GreetingStrip.tsx  (already uses useTranslation('common') + helpers — audit for inline patient strings)
- leanshot/src/components/layout/Topbar.tsx  (multi-namespace analog: useTranslation(['nav','common']))
- leanshot/public/locales/en/patient.json  (card.* + tab.* from 58-05/58-06 — append ai./modal./chart., preserve existing)
</read_first>

<interfaces>
<!-- Verified facts. -->
- KEY-PREFIX OWNERSHIP: this plan owns `patient:ai.*`, `patient:modal.*`, `patient:chart.*`. 58-05 owns card.*, 58-06 owns tab.*. Append only.
- DEPENDENCY: depends_on 58-06 so card.* + tab.* are already present; extraction MERGES (keepRemoved:true) and adds ai./modal./chart.
- GreetingStrip already imports useTranslation('common') and uses i18nMoodLabel/i18nEnergyLabel helpers — it is mostly keyed. AUDIT it: only key any REMAINING inline patient strings; if none remain, the file may need no change (acceptable — record in SUMMARY).
- BaseChart.tsx is a theme-aware wrapper with no user-visible text — NOT in files_modified; only MedLevelChart/SimpleCharts may carry visible labels.
- Chart.js axis/legend labels rendered via config are user-visible — key them through t() passed into the chart config.
- i18next-parser static literals only; clinical AI/chart strings via clinical-glossary.md (flagged P70).
- eslint i18next/no-literal-string mode jsx-text-only (chart config string options are NOT JSX text — eslint won't flag them, but they should still be keyed for ES rendering).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Key AIChatPanel + modals + charts; audit GreetingStrip</name>
  <files>leanshot/src/components/dashboard/ai/AIChatPanel.tsx, leanshot/src/components/dashboard/modals/DoctorReport.tsx, leanshot/src/components/dashboard/modals/PhotoCompareModal.tsx, leanshot/src/components/dashboard/charts/MedLevelChart.tsx, leanshot/src/components/dashboard/charts/SimpleCharts.tsx, leanshot/src/components/layout/GreetingStrip.tsx</files>
  <read_first>
    - leanshot/src/components/dashboard/ai/AIChatPanel.tsx
    - leanshot/src/components/dashboard/modals/DoctorReport.tsx
    - leanshot/src/components/dashboard/modals/PhotoCompareModal.tsx
    - leanshot/src/components/dashboard/charts/MedLevelChart.tsx
    - leanshot/src/components/dashboard/charts/SimpleCharts.tsx
    - leanshot/src/components/layout/GreetingStrip.tsx
  </read_first>
  <action>
    AIChatPanel: add useTranslation(['patient','common']); key prompts, placeholder, empty-state, error copy (including the MissingAPIKeyError user-facing message), suggested-question chips under patient:ai.*. Modals: key DoctorReport + PhotoCompareModal titles, section headings, button labels, body copy under patient:modal.doctor_report.* / patient:modal.photo_compare.*. Charts: key user-visible axis titles / legend labels in MedLevelChart + SimpleCharts under patient:chart.* — pass t() into the Chart.js config (these are clinical: medication-level axis, etc. — align ES with clinical-glossary.md). GreetingStrip: audit for any remaining inline patient strings; key them under patient:* if found. If GreetingStrip already has zero inline strings, leave it unchanged and note in the SUMMARY (acceptable). Use static literals only.
  </action>
  <verify>
    <automated>cd leanshot && grep -q "useTranslation(\['patient'\|useTranslation('patient'" src/components/dashboard/ai/AIChatPanel.tsx && grep -q "patient:modal" src/components/dashboard/modals/DoctorReport.tsx && ! grep -rE "t\(\`patient:" src/components/dashboard/ai/ src/components/dashboard/modals/ src/components/dashboard/charts/ && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c 'error TS' | grep -qx 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - AIChatPanel calls useTranslation for patient namespace; AI copy keyed under patient:ai.*
    - DoctorReport + PhotoCompareModal keyed under patient:modal.*
    - MedLevelChart + SimpleCharts visible labels keyed under patient:chart.*
    - GreetingStrip audited: no remaining inline patient JSX text (either keyed or confirmed none)
    - No template-literal patient keys in ai/, modals/, charts/
    - npx tsc -p tsconfig.app.json --noEmit 0 errors; npx eslint clean (no-literal-string) for these files
  </acceptance_criteria>
  <done>AI panel, modals, charts keyed under patient:ai./modal./chart.; GreetingStrip audited; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize patient.json — extract + ES parity for ai./modal./chart.</name>
  <files>leanshot/public/locales/en/patient.json, leanshot/public/locales/es/patient.json</files>
  <read_first>
    - leanshot/public/locales/en/patient.json (card.* + tab.* present — preserve)
    - leanshot/public/locales/es/common.json (ES style reference)
    - leanshot/.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md (EN->ES Machine Translation Protocol)
  </read_first>
  <action>
    Run cd leanshot && npm run i18n:extract — MERGES ai./modal./chart. into en/patient.json (card.* + tab.* preserved via keepRemoved). Inspect git diff to confirm prior keys untouched and new keys added. Machine/Claude-translate the NEW ai./modal./chart. leaf keys into es/patient.json (card.* + tab.* ES already present — do not retranslate). Re-verify full parity. Run npm run i18n:check. This is the FINAL state of the patient namespace.
  </action>
  <verify>
    <automated>cd leanshot && npm run i18n:extract >/dev/null 2>&1; jq -e '.card and .tab and .ai' public/locales/en/patient.json >/dev/null && echo "ALL-PREFIXES-PRESENT" || echo "FAIL missing prefix"; EN=$(jq -r 'paths(scalars)|join(".")' public/locales/en/patient.json|sort -u); ES=$(jq -r 'paths(scalars)|join(".")' public/locales/es/patient.json|sort -u); [ "$EN" = "$ES" ] && echo PARITY-PASS || echo PARITY-FAIL; ! grep -E '\{\{[a-záéíóúñ]' public/locales/es/patient.json && echo ICU-PASS; bash scripts/check-locale-coverage.sh 2>&1 | (grep -qiE 'patient.*(fail|mismatch|missing)' && echo COV-FAIL || echo COV-PASS)</automated>
  </verify>
  <acceptance_criteria>
    - en/patient.json contains card.* AND tab.* AND ai.* prefixes (jq -e '.card and .tab and .ai')
    - es/patient.json leaf-key set IDENTICAL to en/patient.json (full parity)
    - bash scripts/check-locale-coverage.sh patient namespace at parity
    - grep -E '\{\{[a-záéíóúñ]' public/locales/es/patient.json finds nothing
    - npm run i18n:extract && git diff --quiet -- public/locales/en/patient.json (no drift)
  </acceptance_criteria>
  <done>patient namespace finalized: card.* + tab.* + ai./modal./chart. all present in en, full ES parity, interpolations preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patient ES catalog (ai./modal./chart.) -> rendered UI | AI coach + report + chart copy shown to patients |
| serialized patient.json final append | must preserve card.* + tab.* while adding ai./modal./chart. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | es/patient.json interpolation integrity | mitigate | Task 2 asserts no translated `{{vars}}`; Gate 3 (58-01) enforces in CI |
| T-58-02 | Tampering | AI/chart clinical copy (medication-level axis, dose guidance) | mitigate + transfer | ES routed through clinical-glossary.md; human signoff TRANSFERRED to Phase 70 |
| T-58-09 | Tampering | final append dropping card.*/tab.* keys | mitigate | Task 2 acceptance asserts all three prefixes present (jq -e .card and .tab and .ai) |
| T-58-10 | Information Disclosure | AI coach error string leaking key/internal detail | accept | MissingAPIKeyError message is user-facing by design; keyed translation does not expose secrets (key stays in localStorage) |
| T-58-SC | Tampering | npm installs | accept | zero new packages this plan |
</threat_model>

<verification>
- cd leanshot && bash scripts/check-locale-coverage.sh passes for patient namespace
- cd leanshot && npx tsc -p tsconfig.app.json --noEmit 0 errors
- jq -e '.card and .tab and .ai' confirms full prefix coverage; es parity; no translated vars
</verification>

<success_criteria>
- AI panel + modals + charts keyed to patient:ai./modal./chart.; GreetingStrip audited
- patient namespace finalized with full ES parity across card./tab./ai./modal./chart.
</success_criteria>

<output>
Create `.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-07-SUMMARY.md` when done
</output>
