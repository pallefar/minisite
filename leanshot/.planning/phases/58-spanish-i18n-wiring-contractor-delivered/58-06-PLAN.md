---
phase: 58-spanish-i18n-wiring-contractor-delivered
plan: 06
type: execute
wave: 2
depends_on: ["58-05"]
files_modified:
  - leanshot/src/components/dashboard/tabs/HomeTab.tsx
  - leanshot/src/components/dashboard/tabs/MedicationTab.tsx
  - leanshot/src/components/dashboard/tabs/BodyTab.tsx
  - leanshot/src/components/dashboard/tabs/ActivityTab.tsx
  - leanshot/src/components/dashboard/tabs/NutritionTab.tsx
  - leanshot/src/components/dashboard/tabs/MoodTab.tsx
  - leanshot/src/components/dashboard/tabs/SymptomsTab.tsx
  - leanshot/src/components/dashboard/tabs/SupplementsTab.tsx
  - leanshot/src/components/dashboard/tabs/InsightsTab.tsx
  - leanshot/src/lib/i18n/tab-labels.ts
  - leanshot/public/locales/en/patient.json
  - leanshot/public/locales/es/patient.json
autonomous: true
requirements: [I18N-11, I18N-13]
must_haves:
  truths:
    - "A user on /?lang=es sees all dashboard tabs (Home, Medication, Body, Activity, Nutrition, Mood, Symptoms, Supplements, Insights) rendered in Spanish"
    - "MedicationTab clinical copy (current dose, estimated levels, doses remaining) is keyed and ES-translated"
    - "en/patient.json and es/patient.json remain at identical leaf-key sets after tab keys are appended"
  artifacts:
    - path: "leanshot/public/locales/en/patient.json"
      provides: "tab.* keys appended to the card.* keys established by 58-05"
      contains: "tab"
    - path: "leanshot/src/lib/i18n/tab-labels.ts"
      provides: "static-key helpers for any dynamic tab list/option labels"
      exports: ["tabHeading"]
  key_links:
    - from: "leanshot/src/components/dashboard/tabs/MedicationTab.tsx"
      to: "patient namespace"
      via: "useTranslation('patient')"
      pattern: "useTranslation\\(\\['?patient"
---

<objective>
Externalize inline English in the dashboard TABS into the `patient` namespace under the `tab.*` key prefix, APPENDING to the patient.json established by 58-05. Serialized AFTER 58-05 via depends_on (same-namespace JSON writer — must NOT share a wave with another patient.json writer per the coordination rule).

Purpose: Tabs are the dashboard surface frames; MedicationTab is dense clinical copy (~30 strings: current dose, estimated medication levels, doses remaining) and is step 3 (first dose log) context of the I18N-15 smoke.
Output: 9 keyed tab components + tab-labels.ts helper + patient.json (en+es) extended with `tab.*` keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-PATTERNS.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-05-SUMMARY.md

<read_first>
- leanshot/src/components/dashboard/tabs/MedicationTab.tsx  (densest clinical tab — current dose, est. levels, doses remaining)
- leanshot/src/components/dashboard/tabs/HomeTab.tsx
- leanshot/src/components/layout/GreetingStrip.tsx  (single-ns useTranslation analog)
- leanshot/src/lib/i18n/nav-labels.ts  (static switch helper — COPY for tab-labels.ts)
- leanshot/public/locales/en/patient.json  (AS ESTABLISHED BY 58-05 — append tab.* keys; do NOT remove card.* keys)
- leanshot/.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-PATTERNS.md
</read_first>

<interfaces>
<!-- Verified facts. -->
- KEY-PREFIX OWNERSHIP: this plan owns `patient:tab.*`. 58-05 owns `patient:card.*`; 58-07 owns `patient:ai.*`/`patient:modal.*`/`patient:chart.*`. Append only — do NOT touch card.* keys 58-05 created.
- DEPENDENCY: depends_on 58-05 so patient.json already exists with card.* keys before this plan's extraction runs (i18next-parser MERGES new keys into the existing file; keepRemoved:true preserves card.* keys).
- i18next-parser merges (keepRemoved:true) — running extract here KEEPS 58-05's card.* keys and ADDS tab.* keys. After extract, es/patient.json must be re-synced to full parity (add ES for new tab.* keys; card.* ES already present from 58-05).
- Dynamic tab option lists (symptom picklists, supplement lists, activity types) MUST use exhaustive switch helpers (tab-labels.ts), NOT template literals.
- Clinical MedicationTab strings keyed normally; ES via clinical-glossary.md, flagged P70.
- eslint i18next/no-literal-string mode jsx-text-only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Key MedicationTab + HomeTab + tab-labels.ts</name>
  <files>leanshot/src/components/dashboard/tabs/MedicationTab.tsx, leanshot/src/components/dashboard/tabs/HomeTab.tsx, leanshot/src/lib/i18n/tab-labels.ts</files>
  <read_first>
    - leanshot/src/components/dashboard/tabs/MedicationTab.tsx
    - leanshot/src/components/dashboard/tabs/HomeTab.tsx
    - leanshot/src/lib/i18n/nav-labels.ts
  </read_first>
  <action>
    Add useTranslation('patient') (or ['patient','common']) to MedicationTab and HomeTab. Replace inline JSX text + user-visible string props with t('patient:tab.medication.*') / t('patient:tab.home.*') literals. MedicationTab clinical copy (current dose, estimated medication levels, doses remaining, next-dose timing) MUST be keyed — align ES with clinical-glossary.md (these are safety-relevant). Create src/lib/i18n/tab-labels.ts with exhaustive switch helpers for any dynamic option lists rendered in tabs (e.g. tabHeading(t, tabId) and any picklist label helpers) returning static t('patient:tab.*') literals + never default. Wire dynamic lists to call these helpers.
  </action>
  <verify>
    <automated>cd leanshot && grep -q "useTranslation('patient'\|useTranslation(\['patient'" src/components/dashboard/tabs/MedicationTab.tsx && test -f src/lib/i18n/tab-labels.ts && grep -qE "case '[a-z_-]+': return t\('patient:tab" src/lib/i18n/tab-labels.ts && ! grep -rE "t\(\`patient:" src/components/dashboard/tabs/ && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c 'error TS' | grep -qx 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - MedicationTab + HomeTab call useTranslation for the patient namespace
    - MedicationTab clinical copy (current dose / estimated levels / doses remaining) keyed under patient:tab.medication.*
    - tab-labels.ts exports static-key switch helper(s) with t('patient:tab.*') cases + never default
    - No template-literal patient keys in src/components/dashboard/tabs/
    - npx tsc -p tsconfig.app.json --noEmit 0 errors; npx eslint clean (no-literal-string) for these files
  </acceptance_criteria>
  <done>MedicationTab + HomeTab keyed under patient:tab.*; dynamic tab labels resolve via static helpers; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Key remaining tabs; append patient.json; sync ES parity</name>
  <files>leanshot/src/components/dashboard/tabs/BodyTab.tsx, leanshot/src/components/dashboard/tabs/ActivityTab.tsx, leanshot/src/components/dashboard/tabs/NutritionTab.tsx, leanshot/src/components/dashboard/tabs/MoodTab.tsx, leanshot/src/components/dashboard/tabs/SymptomsTab.tsx, leanshot/src/components/dashboard/tabs/SupplementsTab.tsx, leanshot/src/components/dashboard/tabs/InsightsTab.tsx, leanshot/public/locales/en/patient.json, leanshot/public/locales/es/patient.json</files>
  <read_first>
    - leanshot/src/components/dashboard/tabs/BodyTab.tsx (+ the other 6 listed tabs)
    - leanshot/public/locales/en/patient.json (card.* keys from 58-05 — preserve)
    - leanshot/public/locales/es/common.json (ES style reference)
    - leanshot/.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md (EN->ES Machine Translation Protocol; Pitfall 3 keepRemoved behavior)
  </read_first>
  <action>
    Key the remaining 7 tabs with useTranslation('patient'); externalize user-visible strings under patient:tab.<tab>.*. Symptom/supplement/mood option lists use exhaustive switch helpers (extend tab-labels.ts), NOT template literals.
    Run cd leanshot && npm run i18n:extract — MERGES tab.* into the existing en/patient.json (card.* preserved via keepRemoved). Inspect git diff to confirm card.* keys are untouched and tab.* keys added. Machine/Claude-translate the NEW tab.* leaf keys into es/patient.json (card.* ES already present from 58-05 — do NOT retranslate them). Re-verify full parity. Run npm run i18n:check.
  </action>
  <verify>
    <automated>cd leanshot && npm run i18n:extract >/dev/null 2>&1; jq -e '.card' public/locales/en/patient.json >/dev/null && echo "CARD-KEYS-PRESERVED" || echo "FAIL card.* dropped"; EN=$(jq -r 'paths(scalars)|join(".")' public/locales/en/patient.json|sort -u); ES=$(jq -r 'paths(scalars)|join(".")' public/locales/es/patient.json|sort -u); [ "$EN" = "$ES" ] && echo PARITY-PASS || echo PARITY-FAIL; ! grep -E '\{\{[a-záéíóúñ]' public/locales/es/patient.json && echo ICU-PASS; bash scripts/check-locale-coverage.sh 2>&1 | (grep -qiE 'patient.*(fail|mismatch|missing)' && echo COV-FAIL || echo COV-PASS)</automated>
  </verify>
  <acceptance_criteria>
    - All 7 remaining tabs call useTranslation('patient'); npx eslint clean (no-literal-string) across src/components/dashboard/tabs/
    - en/patient.json STILL contains the card.* keys from 58-05 (not dropped by extraction) AND now contains tab.* keys
    - es/patient.json leaf-key set IDENTICAL to en/patient.json (full parity, card.* + tab.*)
    - bash scripts/check-locale-coverage.sh patient namespace at parity
    - grep -E '\{\{[a-záéíóúñ]' public/locales/es/patient.json finds nothing
    - npm run i18n:extract && git diff --quiet -- public/locales/en/patient.json (no drift)
  </acceptance_criteria>
  <done>All 9 tabs keyed under patient:tab.*; patient.json extended with tab keys (card.* preserved); full ES parity; ready for 58-07 overlay keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patient ES catalog (tab.*) -> rendered UI | dashboard tab clinical copy shown to patients |
| serialized patient.json append after 58-05 | must preserve 58-05's card.* keys while adding tab.* |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | es/patient.json interpolation integrity | mitigate | Task 2 asserts no translated `{{vars}}`; Gate 3 (58-01) enforces in CI |
| T-58-02 | Tampering | MedicationTab clinical copy (dose/levels/remaining) | mitigate + transfer | ES routed through clinical-glossary.md; human signoff TRANSFERRED to Phase 70 |
| T-58-09 | Tampering | append dropping 58-05's card.* keys | mitigate | Task 2 acceptance asserts card.* keys preserved (jq -e .card) after extraction |
| T-58-05 | Information Disclosure | missing ES key -> EN fallthrough | mitigate | check-locale-coverage.sh parity gate (Task 2) |
| T-58-SC | Tampering | npm installs | accept | zero new packages this plan |
</threat_model>

<verification>
- cd leanshot && bash scripts/check-locale-coverage.sh passes for patient namespace
- cd leanshot && npx tsc -p tsconfig.app.json --noEmit 0 errors
- jq -e '.card' confirms 58-05 keys preserved; es/patient.json full parity, no translated vars
</verification>

<success_criteria>
- All 9 tabs keyed to patient:tab.*
- patient.json extended (card.* preserved + tab.* added); full ES parity
- MedicationTab clinical copy keyed and glossary-aligned
</success_criteria>

<output>
Create `.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-06-SUMMARY.md` when done
</output>
