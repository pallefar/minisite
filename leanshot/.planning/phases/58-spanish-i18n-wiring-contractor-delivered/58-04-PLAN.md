---
phase: 58-spanish-i18n-wiring-contractor-delivered
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/src/components/dashboard/settings/SettingsPage.tsx
  - leanshot/src/components/dashboard/settings/cancellation/CancellationModal.tsx
  - leanshot/src/components/dashboard/settings/cancellation/OfferCard.tsx
  - leanshot/src/components/dashboard/settings/cancellation/PauseControls.tsx
  - leanshot/src/components/dashboard/settings/cancellation/steps/LossSummaryStep.tsx
  - leanshot/src/components/dashboard/settings/cancellation/steps/OfferStep.tsx
  - leanshot/src/components/dashboard/settings/cancellation/steps/ReasonPicklistStep.tsx
  - leanshot/src/components/dashboard/settings/NotificationsSubtab.tsx
  - leanshot/src/components/dashboard/settings/LeaderboardsSubtab.tsx
  - leanshot/src/components/dashboard/settings/EmailPreferencesPage.tsx
  - leanshot/src/components/dashboard/settings/ActiveSharesSection.tsx
  - leanshot/src/components/dashboard/settings/CreateShareModal.tsx
  - leanshot/src/components/dashboard/settings/DeleteAccountModal.tsx
  - leanshot/src/components/dashboard/settings/EditConsentScopeModal.tsx
  - leanshot/src/components/dashboard/settings/PatientMfaCard.tsx
  - leanshot/src/components/dashboard/settings/PhiAccessLogTab.tsx
  - leanshot/src/components/dashboard/settings/CommunitySettingsTab.tsx
  - leanshot/src/components/dashboard/settings/PatientActivityModal.tsx
  - leanshot/src/components/dashboard/settings/PatientActivityRow.tsx
  - leanshot/src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx
  - leanshot/src/lib/i18n/settings-labels.ts
  - leanshot/src/components/kb/RelatedArticlesFooter.tsx
  - leanshot/public/locales/en/settings.json
  - leanshot/public/locales/es/settings.json
  - leanshot/public/locales/en/kb.json
  - leanshot/public/locales/es/kb.json
  - supabase/migrations/20270708000001_p58_kb_articles_es_seed.sql
autonomous: true
requirements: [I18N-11, I18N-13, I18N-14]
must_haves:
  truths:
    - "A user on /?lang=es sees the Settings page (nav + sections) and the cancellation flow in Spanish"
    - "The KB 'Related articles' string renders in Spanish"
    - "kb_articles rows have title_es/body_es populated and locale_set = array['en','es'] so search_kb_articles(p_locale='es') returns ES titles"
    - "en/settings.json + es/settings.json and en/kb.json + es/kb.json each have identical leaf-key sets"
  artifacts:
    - path: "leanshot/public/locales/en/settings.json"
      provides: "EN source keys for settings surface"
      contains: "nav"
    - path: "leanshot/public/locales/es/settings.json"
      provides: "ES translations at parity"
      contains: "nav"
    - path: "leanshot/public/locales/en/kb.json"
      provides: "EN keys for KB UI"
      contains: "related_articles"
    - path: "supabase/migrations/20270708000001_p58_kb_articles_es_seed.sql"
      provides: "ES content population for kb_articles (UPDATE/INSERT, no schema change) + locale_set"
      contains: "title_es"
  key_links:
    - from: "leanshot/src/components/dashboard/settings/SettingsPage.tsx"
      to: "settings namespace"
      via: "useTranslation(['settings','common'])"
      pattern: "useTranslation\\(\\['?settings"
    - from: "supabase/migrations/20270708000001_p58_kb_articles_es_seed.sql"
      to: "kb_articles.title_es/body_es/locale_set"
      via: "UPDATE setting ES columns + locale_set array['en','es']"
      pattern: "locale_set"
---

<objective>
Externalize inline English in the settings surface (including the cancellation flow — step 5 of the I18N-15 smoke) into the `settings` namespace, key the one KB UI string into `kb`, and populate ES KB article content so `search_kb_articles(p_locale='es')` returns Spanish titles. This plan EXCLUSIVELY owns settings.json + kb.json (Wave 1, zero JSON conflicts with the patient/onboarding/clinic plans).

Purpose: Settings + cancellation are smoke-critical (cancellation is the I18N-15 fifth step); KB is I18N-14. The Postgres ES search stack already exists (Phase 37: title_es/body_es/search_vector_es/locale_set/search_kb_articles) — NO schema migration; only a CONTENT seed/UPDATE.
Output: Keyed settings + KB components, settings-labels.ts helper, populated settings/kb locale JSON, KB ES content seed migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-PATTERNS.md

<read_first>
- leanshot/src/components/dashboard/settings/SettingsPage.tsx  (NAV array with hard-coded label strings, ~lines 117-162; language section ~line 557)
- leanshot/src/components/dashboard/settings/cancellation/CancellationModal.tsx + steps/*  (smoke-critical cancellation copy)
- leanshot/src/lib/i18n/nav-labels.ts  (exhaustive switch helper — COPY for settings-labels.ts NAV labels)
- leanshot/src/components/kb/RelatedArticlesFooter.tsx  (inline "Related articles" at ~line 122)
- leanshot/public/locales/en/common.json + es/common.json  (reuse common:action.* for shared buttons)
- leanshot/.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-PATTERNS.md  (section 5: settings.json + kb.json key structures)
- supabase/migrations/20270707000006_helpdesk_search_kb_fn.sql  (search_kb_articles: p_locale allowlist en/es, spanish::regconfig, coalesce(title_es,title))
- supabase/migrations/20270707000001_helpdesk_schema.sql  (kb_articles columns: title, body, title_es, body_es, locale_set default array['en'])
</read_first>

<interfaces>
<!-- Verified facts (monorepo paths are GIT-ROOT-relative). -->
- KB ES Postgres stack ALREADY SHIPPED in Phase 37 — NO new schema migration. kb_articles has: title, body, title_es, body_es, search_vector_es (GENERATED STORED tsvector, GIN-indexed), locale_set text[] default array['en']. search_kb_articles(p_locale text default 'en') validates p_locale in ('en','es') and uses spanish::regconfig for es, returning coalesce(title_es, title). The ONLY DB work is a CONTENT seed/UPDATE migration setting title_es/body_es + locale_set = array['en','es'].
- supabase/migrations/ is at the MONOREPO ROOT (sibling of leanshot/), NOT under leanshot/. New migration filename MUST be timestamp-sequenced AFTER the last existing helpdesk migration (20270707000009) — use 20270708000001_p58_kb_articles_es_seed.sql.
- LanguageSwitcher already wired in SettingsPage.tsx section 'language' — no switcher work, just key the surrounding copy.
- SettingsPage NAV array initializes label strings at module scope; move labels into a render-time t() lookup or a static switch helper (settings-labels.ts) so i18next-parser sees static keys (58-PATTERNS section 5).
- eslint i18next/no-literal-string mode jsx-text-only. Static t() literals only; no template literals.
- This is the heaviest namespace (~20 files). Scope to USER-VISIBLE strings only; do not key dev-only/debug strings or aria-labels that aren't user-visible.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Key SettingsPage + cancellation flow + settings-labels.ts</name>
  <files>leanshot/src/components/dashboard/settings/SettingsPage.tsx, leanshot/src/components/dashboard/settings/cancellation/CancellationModal.tsx, leanshot/src/components/dashboard/settings/cancellation/OfferCard.tsx, leanshot/src/components/dashboard/settings/cancellation/PauseControls.tsx, leanshot/src/components/dashboard/settings/cancellation/steps/LossSummaryStep.tsx, leanshot/src/components/dashboard/settings/cancellation/steps/OfferStep.tsx, leanshot/src/components/dashboard/settings/cancellation/steps/ReasonPicklistStep.tsx, leanshot/src/lib/i18n/settings-labels.ts</files>
  <read_first>
    - leanshot/src/components/dashboard/settings/SettingsPage.tsx
    - leanshot/src/components/dashboard/settings/cancellation/CancellationModal.tsx (+ steps/*)
    - leanshot/src/lib/i18n/nav-labels.ts
  </read_first>
  <action>
    Add useTranslation(['settings','common']) to SettingsPage and the cancellation components. Replace the SettingsPage NAV array's hard-coded label strings: create src/lib/i18n/settings-labels.ts exporting a static-key helper sectionLabel(t, id: Section) — an exhaustive switch returning a STATIC t('settings:nav.<id>') per case with a never exhaustiveness default (copy nav-labels.ts). Wire the NAV array to call sectionLabel(t, id) at render. Key the language section title/body (settings:section.language.title / .body), and all cancellation flow copy (settings:cancellation.*) — reason picklist options, offer copy, pause controls, loss-summary text. The cancellation flow is the I18N-15 fifth smoke step, so it MUST be fully keyed. Use the Section type from SettingsPage. Reuse common:action.* for Cancel/Confirm/Save/Continue.
  </action>
  <verify>
    <automated>cd leanshot && grep -q "useTranslation(\['settings'" src/components/dashboard/settings/SettingsPage.tsx && test -f src/lib/i18n/settings-labels.ts && grep -cE "case '[a-z-]+': return t\('settings:nav" src/lib/i18n/settings-labels.ts | awk '$1>=5{print "PASS"; exit}{print "FAIL"}' && grep -q "settings:cancellation" src/components/dashboard/settings/cancellation/CancellationModal.tsx && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -c 'error TS' | grep -qx 0 && echo TSC-PASS</automated>
  </verify>
  <acceptance_criteria>
    - SettingsPage + all cancellation files call useTranslation(['settings','common'])
    - settings-labels.ts exports sectionLabel as an exhaustive switch with >=5 static t('settings:nav.*') cases + never default
    - SettingsPage NAV array labels resolve via sectionLabel(t, id) (no module-scope literal labels)
    - Cancellation flow copy keyed under settings:cancellation.*
    - No template-literal settings keys: grep -rE "t\(\`settings:" src/components/dashboard/settings/ returns nothing
    - npx tsc -p tsconfig.app.json --noEmit 0 errors; npx eslint clean for these files (no-literal-string)
  </acceptance_criteria>
  <done>SettingsPage nav + language section + full cancellation flow keyed; settings-labels.ts replaces dynamic NAV labels; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Key remaining settings + KB component; extract EN; translate ES</name>
  <files>leanshot/src/components/dashboard/settings/NotificationsSubtab.tsx, leanshot/src/components/dashboard/settings/LeaderboardsSubtab.tsx, leanshot/src/components/dashboard/settings/EmailPreferencesPage.tsx, leanshot/src/components/dashboard/settings/ActiveSharesSection.tsx, leanshot/src/components/dashboard/settings/CreateShareModal.tsx, leanshot/src/components/dashboard/settings/DeleteAccountModal.tsx, leanshot/src/components/dashboard/settings/EditConsentScopeModal.tsx, leanshot/src/components/dashboard/settings/PatientMfaCard.tsx, leanshot/src/components/dashboard/settings/PhiAccessLogTab.tsx, leanshot/src/components/dashboard/settings/CommunitySettingsTab.tsx, leanshot/src/components/dashboard/settings/PatientActivityModal.tsx, leanshot/src/components/dashboard/settings/PatientActivityRow.tsx, leanshot/src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx, leanshot/src/components/kb/RelatedArticlesFooter.tsx, leanshot/public/locales/en/settings.json, leanshot/public/locales/es/settings.json, leanshot/public/locales/en/kb.json, leanshot/public/locales/es/kb.json</files>
  <read_first>
    - leanshot/src/components/dashboard/settings/NotificationsSubtab.tsx (+ the other listed settings files)
    - leanshot/src/components/kb/RelatedArticlesFooter.tsx
    - leanshot/public/locales/es/common.json (ES style reference)
    - leanshot/.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md (EN->ES Machine Translation Protocol)
  </read_first>
  <action>
    Key the remaining settings components with useTranslation(['settings','common']) — externalize user-visible JSX text, section titles, body copy, button labels under settings:<section>.*. Use formal "usted" address for settings sections per RESEARCH protocol (settings is the one surface where formal address is appropriate; patient-facing onboarding/dashboard use "tú").
    Key RelatedArticlesFooter: useTranslation('kb'), replace inline "Related articles" CardHeader title with t('kb:related_articles.title').
    Run cd leanshot && npm run i18n:extract — populates en/settings.json + en/kb.json. Inspect git diff. Machine/Claude-translate every leaf key into es/settings.json + es/kb.json: identical key structure, {{vars}} verbatim, _one/_other English suffixes. Run npm run i18n:check; confirm settings + kb namespaces pass.
  </action>
  <verify>
    <automated>cd leanshot && npm run i18n:extract >/dev/null 2>&1; grep -q "kb:related_articles" src/components/kb/RelatedArticlesFooter.tsx; for ns in settings kb; do EN=$(jq -r 'paths(scalars)|join(".")' public/locales/en/$ns.json|sort -u); ES=$(jq -r 'paths(scalars)|join(".")' public/locales/es/$ns.json|sort -u); [ "$EN" = "$ES" ] && echo "$ns PARITY-PASS" || echo "$ns PARITY-FAIL"; ! grep -E '\{\{[a-záéíóúñ]' public/locales/es/$ns.json && echo "$ns ICU-PASS"; done; bash scripts/check-locale-coverage.sh 2>&1 | (grep -qiE '(settings|kb).*(fail|mismatch|missing)' && echo COV-FAIL || echo COV-PASS)</automated>
  </verify>
  <acceptance_criteria>
    - All listed settings files + RelatedArticlesFooter call useTranslation and contain no flagged inline JSX text (npx eslint clean for no-literal-string)
    - public/locales/en/settings.json and en/kb.json are non-empty (no longer {})
    - es/settings.json and es/kb.json leaf-key sets IDENTICAL to their en/ counterparts (jq paths diff empty)
    - bash scripts/check-locale-coverage.sh settings + kb namespaces at parity
    - grep -E '\{\{[a-záéíóúñ]' over es/settings.json and es/kb.json finds NOTHING
    - npm run i18n:extract && git diff --quiet -- public/locales/en/settings.json public/locales/en/kb.json (no drift)
  </acceptance_criteria>
  <done>All settings components + KB footer keyed; en settings/kb JSON populated; es settings/kb JSON at full parity with preserved interpolations.</done>
</task>

<task type="auto">
  <name>Task 3: Populate ES KB article content (content seed migration, NO schema change)</name>
  <files>supabase/migrations/20270708000001_p58_kb_articles_es_seed.sql</files>
  <read_first>
    - supabase/migrations/20270707000001_helpdesk_schema.sql (kb_articles columns + locale_set)
    - supabase/migrations/20270707000006_helpdesk_search_kb_fn.sql (search_kb_articles p_locale es branch)
    - supabase/migrations/20270707000004_helpdesk_seed_macros.sql (existing seed style/idempotency pattern)
  </read_first>
  <action>
    Create a CONTENT-ONLY migration (NO ALTER TABLE, NO new columns — the ES schema already exists from Phase 37). The migration must be idempotent and handle BOTH cases:
    (1) If global kb_articles rows already exist with English title/body: UPDATE them to set title_es, body_es (machine/Claude ES translations of the EN title/body), and locale_set = array['en','es']. Use a stable WHERE on a deterministic identifier (slug or id) so re-running is a no-op overwrite, not a duplicate.
    (2) If NO global kb_articles exist (RESEARCH Open-Q2 / Assumption A2): INSERT 2-3 seed articles WITH both EN (title/body) and ES (title_es/body_es) and locale_set = array['en','es'], using ON CONFLICT (slug) DO UPDATE for idempotency. Choose patient-relevant KB topics (e.g. "How injection-site rotation works", "Reading your medication-level curve", "What to do about nausea") — route any clinical terms through docs/clinical-glossary.md and flag for P70 signoff.
    Do NOT recreate search_vector_es or any index/function — they exist. Preserve numbers/units verbatim in ES body. Filename is timestamp-sequenced after 20270707000009.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test -f supabase/migrations/20270708000001_p58_kb_articles_es_seed.sql && grep -q 'title_es' supabase/migrations/20270708000001_p58_kb_articles_es_seed.sql && grep -q "locale_set" supabase/migrations/20270708000001_p58_kb_articles_es_seed.sql && ! grep -qiE 'alter table .*add column|create (index|.*generated)|create or replace function search_kb' supabase/migrations/20270708000001_p58_kb_articles_es_seed.sql && echo "NO-SCHEMA-CHANGE-PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at the monorepo-root supabase/migrations/ with the 20270708000001 prefix (sequenced after 20270707000009)
    - Migration sets title_es, body_es AND locale_set = array['en','es']
    - Migration contains NO ALTER TABLE ADD COLUMN, NO CREATE INDEX, NO CREATE/REPLACE of search_kb_articles or search_vector_es (schema already shipped Phase 37)
    - Migration is idempotent (UPDATE on stable key, or INSERT ... ON CONFLICT DO UPDATE)
    - ES content preserves numbers/units verbatim; clinical terms aligned with docs/clinical-glossary.md
    - NOTE: live `supabase db push` is a phase close-out step (operator-run); this task only authors the migration file
  </acceptance_criteria>
  <done>A content-only, idempotent migration populates ES KB titles/bodies + locale_set so search_kb_articles(p_locale='es') returns Spanish results; zero schema changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings/kb ES catalog -> rendered UI | settings + cancellation + KB copy shown to patients |
| KB ES content -> search_kb_articles RPC | ES article titles/bodies returned to patients via spanish::regconfig search |
| migration -> production kb_articles | content UPDATE/INSERT against a live table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | es/settings.json + es/kb.json interpolation integrity | mitigate | Task 2 asserts no translated `{{vars}}`; Gate 3 (58-01) enforces in CI |
| T-58-02 | Tampering | KB ES clinical body content (nausea/dose/site copy) | mitigate + transfer | clinical terms routed through clinical-glossary.md flagged signoff-pending; human signoff TRANSFERRED to Phase 70 |
| T-58-05 | Information Disclosure | missing ES key -> silent EN fallthrough | mitigate | check-locale-coverage.sh parity gate (Task 2) for settings + kb |
| T-58-07 | Tampering | KB seed migration accidentally altering schema/duplicating rows | mitigate | Task 3 acceptance forbids ALTER/CREATE statements + requires idempotent UPSERT on stable key |
| T-58-08 | Spoofing | search_kb_articles p_locale injection | accept | RPC already validates p_locale in ('en','es') and raises 22023 on others (Phase 37) — no change |
| T-58-SC | Tampering | npm installs | accept | zero new packages this plan |
</threat_model>

<verification>
- cd leanshot && bash scripts/check-locale-coverage.sh passes for settings + kb namespaces
- cd leanshot && npx tsc -p tsconfig.app.json --noEmit 0 errors
- KB migration contains no schema-altering statements
- es/settings.json + es/kb.json have no translated interpolation variable names
</verification>

<success_criteria>
- Settings surface (incl. cancellation flow) keyed to settings namespace; KB footer keyed to kb namespace
- en settings/kb populated by parser; es settings/kb at full parity
- Idempotent content-only KB ES seed migration sets title_es/body_es + locale_set without schema changes
</success_criteria>

<output>
Create `.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-04-SUMMARY.md` when done
</output>
