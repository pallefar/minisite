---
phase: 58-spanish-i18n-wiring-contractor-delivered
plan: 08
type: execute
wave: 4
depends_on: ["58-01", "58-02", "58-03", "58-04", "58-07"]
files_modified:
  - leanshot/e2e/i18n/es-smoke.spec.ts
autonomous: true
requirements: [I18N-14, I18N-15]
must_haves:
  truths:
    - "Running PLAYWRIGHT_RUN_ES_SMOKE=1 with the p58-es-smoke project, the spec asserts real Spanish strings render across signup -> onboarding -> first dose log -> AI chat -> cancellation -> KB search"
    - "The smoke fails if any step shows English fallthrough (asserts specific ES words, not just page-loads)"
    - "All in-scope namespaces pass en<->es parity and Gate 3 ICU integrity"
  artifacts:
    - path: "leanshot/e2e/i18n/es-smoke.spec.ts"
      provides: "GREEN es-smoke covering the full I18N-15 critical flow with real ES assertions"
      contains: "toHaveAttribute"
  key_links:
    - from: "leanshot/e2e/i18n/es-smoke.spec.ts"
      to: "es/* locale catalogs"
      via: "assert specific ES strings render (no EN fallthrough)"
      pattern: "getByText|toHaveText"
---

<objective>
Complete the RED es-smoke scaffold (created by 58-01) into a GREEN Playwright spec that traverses the I18N-15 critical flow (signup -> onboarding -> first dose log -> AI chat -> cancellation -> KB search) and asserts REAL Spanish strings render at each step — guarding against silent English fallthrough. Serialized last (depends_on all keying plans + scaffold) because it asserts ES strings that only exist after every namespace is populated.

Purpose: I18N-15 (full ES smoke) + I18N-14 KB-search ES branch. This is the phase's goal-backward proof: if a patient sets locale=es, the whole critical flow is Spanish, not English.
Output: GREEN es-smoke.spec.ts with per-step ES assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-RESEARCH.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-PATTERNS.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-02-SUMMARY.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-04-SUMMARY.md
@.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-07-SUMMARY.md

<read_first>
- leanshot/e2e/i18n/es-smoke.spec.ts  (RED scaffold from 58-01 — fill the fixme tests)
- leanshot/e2e/i18n-profile-locale-persistence.spec.ts  (addInitScript seed + STORAGE_KEY='leanshot_v4' + SEEDED_PERSISTED_STATE shape — full analog)
- leanshot/e2e/i18n-language-switch.spec.ts  (html lang assertion)
- leanshot/playwright.config.ts  (p58-es-smoke project + ES_SMOKE_OPT_IN — from 58-01)
- leanshot/public/locales/es/onboarding.json  (pick concrete ES strings to assert — e.g. "Tu nombre")
- leanshot/public/locales/es/patient.json  (dashboard/dose-log/AI ES strings to assert)
- leanshot/public/locales/es/settings.json  (cancellation ES strings to assert)
- leanshot/public/locales/es/kb.json  (Related-articles ES string to assert)
</read_first>

<interfaces>
<!-- Verified facts. -->
- Consumer SPA has NO router (CLAUDE.md + RESEARCH Pitfall 5). Locale set via /?lang=es querystring; navigation via Zustand setTab (click tab buttons), NOT URL routes. NEVER goto('/dashboard') etc.
- Seed onboarded state via page.addInitScript -> localStorage.setItem('leanshot_v4', JSON.stringify(state)); also seed leanshot_tour_seen_v4='1' to skip the tour (analog uses TOUR_KEY). Set user.locale='es' so the profilesLocale detector applies.
- p58-es-smoke is opt-in: run with PLAYWRIGHT_RUN_ES_SMOKE=1 npx playwright test e2e/i18n/es-smoke.spec.ts --project=p58-es-smoke. Excluded from default chromium via testIgnore.
- ANTI-FALLTHROUGH RULE (RESEARCH Pitfall 2 + silent-scope-reduction memory): each step MUST assert a SPECIFIC ES string from the es/*.json catalogs (e.g. getByText(/Tu nombre/i)), NOT merely that the page loaded or html lang=es. A missing ES key silently renders the EN string; asserting the ES word catches it.
- KB search step (I18N-14): drive the KB UI search with a query, assert an ES result title renders (search_kb_articles p_locale='es' branch returns coalesce(title_es,title)). If the live Supabase RPC is unavailable in the test env, assert the keyed KB UI ES string (kb:related_articles) renders AND document the live-RPC verification as a Phase-70 deferral in the SUMMARY (do not fake a pass).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire es-smoke assertions for signup -> onboarding -> first dose log</name>
  <files>leanshot/e2e/i18n/es-smoke.spec.ts</files>
  <read_first>
    - leanshot/e2e/i18n/es-smoke.spec.ts (RED scaffold)
    - leanshot/e2e/i18n-profile-locale-persistence.spec.ts (seed shape)
    - leanshot/public/locales/es/onboarding.json + es/patient.json (concrete ES strings)
  </read_first>
  <action>
    Replace the onboarding + first-dose-log test.fixme placeholders with real tests. Seed an onboarding-entry state (or no user, to hit signup->onboarding) via addInitScript. goto('/?lang=es'); assert html lang=es; then assert a SPECIFIC ES onboarding string from es/onboarding.json renders (e.g. the name label "Tu nombre" or whatever the executor translated it to — read the actual value from the JSON, do not guess). For the first-dose-log step: seed an onboarded user (user.locale='es', minimal injections), navigate to the medication tab via the nav button (getByRole('button', { name: /<ES nav label>/i }) using the es/nav.json label), click the log-dose action, and assert a SPECIFIC ES string from es/patient.json (tab.medication.* or card.dose_log.*) renders. Every assertion targets a string read from the es catalogs (anti-fallthrough). No URL-route navigation.
  </action>
  <verify>
    <automated>cd leanshot && grep -q "toHaveAttribute('lang', 'es'\|toHaveAttribute('lang','es')" e2e/i18n/es-smoke.spec.ts && grep -cE "getByText|getByRole|toHaveText" e2e/i18n/es-smoke.spec.ts | awk '$1>=3{print "PASS"; exit}{print "FAIL need real assertions"}' && ! grep -q "test.fixme" e2e/i18n/es-smoke.spec.ts && ! grep -qE "goto\('/dashboard|goto\('/medication|goto\('/settings|goto\('/onboarding" e2e/i18n/es-smoke.spec.ts && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -v es-smoke | grep -c 'error TS' | grep -qx 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - onboarding + first-dose-log tests assert specific ES strings (getByText/getByRole on ES catalog values), not just page-load
    - All assertions for these two steps reference strings that EXIST in es/onboarding.json / es/patient.json
    - No test.fixme remains for the onboarding + dose-log steps
    - Navigation uses /?lang=es + Zustand tab clicks only (no goto to /dashboard, /medication, /settings, /onboarding)
    - Spec compiles (tsc clean)
  </acceptance_criteria>
  <done>Onboarding + first-dose-log steps assert real ES strings via tab-click navigation; no English fallthrough passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AI chat + cancellation + KB search assertions; run full smoke GREEN</name>
  <files>leanshot/e2e/i18n/es-smoke.spec.ts</files>
  <read_first>
    - leanshot/e2e/i18n/es-smoke.spec.ts (after Task 1)
    - leanshot/public/locales/es/patient.json (ai.* strings)
    - leanshot/public/locales/es/settings.json (cancellation.* strings)
    - leanshot/public/locales/es/kb.json (related_articles string)
  </read_first>
  <action>
    Replace the AI-chat, cancellation, and KB-search fixme placeholders. AI chat (step 4): open the AI panel (click its nav/topbar entry by ES label), assert a SPECIFIC ES string from es/patient.json ai.* (e.g. the empty-state or placeholder). Cancellation (step 5): navigate to settings -> subscription/cancellation, trigger the cancellation flow, assert a SPECIFIC ES string from es/settings.json cancellation.*. KB search (step 6, I18N-14): open the KB surface, assert the ES kb:related_articles string renders; if a live search RPC is reachable in the env, perform an ES query and assert an ES result title — otherwise assert the keyed KB UI ES string and record the live-RPC verification as a Phase-70 deferral in the SUMMARY (no faked pass).
    Run the full opt-in smoke: PLAYWRIGHT_RUN_ES_SMOKE=1 npx playwright test e2e/i18n/es-smoke.spec.ts --project=p58-es-smoke. All steps must be GREEN (or KB-live-RPC explicitly deferred with the keyed-string assertion still green). Also run the locale parity + Gate 3 sweep across all namespaces.
  </action>
  <verify>
    <automated>cd leanshot && PLAYWRIGHT_RUN_ES_SMOKE=1 npx playwright test e2e/i18n/es-smoke.spec.ts --project=p58-es-smoke 2>&1 | tail -20 | grep -qiE 'passed|[0-9]+ passed' && echo SMOKE-GREEN || echo "SMOKE check output"; bash scripts/check-locale-coverage.sh && echo COV-ALL-PASS; ! grep -rE '\{\{[a-záéíóúñ]' public/locales/es/ && echo GATE3-ALL-PASS</automated>
  </verify>
  <acceptance_criteria>
    - AI chat + cancellation + KB steps assert specific ES strings from the es catalogs (anti-fallthrough)
    - PLAYWRIGHT_RUN_ES_SMOKE=1 ... --project=p58-es-smoke runs GREEN across all six flow steps (KB live-RPC may defer to P70 with the keyed-string assertion still green + documented in SUMMARY)
    - bash scripts/check-locale-coverage.sh passes for ALL in-scope namespaces (onboarding, patient, settings, kb, clinic)
    - grep -rE '\{\{[a-záéíóúñ]' public/locales/es/ finds NOTHING (Gate 3 ICU integrity across all ES catalogs)
    - No test.fixme remains anywhere in the spec
  </acceptance_criteria>
  <done>Full I18N-15 ES smoke is GREEN with real ES assertions at every step; all-namespace parity + ICU gates pass; English fallthrough cannot silently pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| es-smoke assertions -> ES catalogs | the smoke is the last line of defense against silent EN fallthrough masking missing ES clinical strings |
| `?lang=es` -> i18next locale selection | locale chosen by URL/seeded profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-05 | Information Disclosure / correctness | EN fallthrough masking a missing ES clinical key | mitigate | Every smoke step asserts a SPECIFIC ES string (not page-load); a missing key renders EN and fails the assertion |
| T-58-11 | Tampering | a green smoke that doesn't actually exercise the localized path (silent-scope-reduction lesson) | mitigate | acceptance forbids URL-route navigation (must use the real Zustand tab path) and forbids page-load-only assertions |
| T-58-08 | Spoofing | `?lang=es` / KB p_locale | accept | init.ts supportedLngs clamps; search_kb_articles validates p_locale (Phase 37) |
| T-58-SC | Tampering | npm installs | accept | zero new packages this plan |
</threat_model>

<verification>
- cd leanshot && PLAYWRIGHT_RUN_ES_SMOKE=1 npx playwright test e2e/i18n/es-smoke.spec.ts --project=p58-es-smoke is GREEN
- cd leanshot && bash scripts/check-locale-coverage.sh passes for all in-scope namespaces
- cd leanshot && grep -rE '\{\{[a-záéíóúñ]' public/locales/es/ finds nothing
- cd leanshot && npx tsc -p tsconfig.app.json --noEmit 0 errors
</verification>

<success_criteria>
- es-smoke GREEN across signup -> onboarding -> first dose log -> AI chat -> cancellation -> KB search with real ES assertions
- All in-scope namespaces at en<->es parity; Gate 3 ICU integrity across all ES catalogs
- KB ES search verified (or live-RPC deferred to P70 with keyed-string assertion green + documented)
</success_criteria>

<output>
Create `.planning/phases/58-spanish-i18n-wiring-contractor-delivered/58-08-SUMMARY.md` when done
</output>
