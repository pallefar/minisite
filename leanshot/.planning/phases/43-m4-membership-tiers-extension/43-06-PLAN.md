---
phase: 43-m4-membership-tiers-extension
plan: 06
type: execute
wave: 4
depends_on: [43-01, 43-02, 43-03, 43-04, 43-05]
files_modified:
  - .planning/phases/43-m4-membership-tiers-extension/43-DEPLOY-NOTES.md
  - .planning/phases/43-m4-membership-tiers-extension/43-CARRY-OVER.md
autonomous: false
requirements: [MEMBER-01, MEMBER-02, MEMBER-03, MEMBER-04]
must_haves:
  truths:
    - "All 7 Phase 43 migrations (20270715000001..07) are applied on the linked Supabase project after `supabase db push --linked` succeeds."
    - "stripe-webhook + stripe-checkout + cancellation-accept-offer Edge Fns are redeployed with the lifetime + grandfathered + 70%-cap changes."
    - "STRIPE_PRICE_LIFETIME Function Secret is set; `stripe_price_lookup` rows for lifetime + plus_monthly + plus_yearly + clinic_base + clinic_overage are populated post-deploy."
    - "Operator-confirmed Stripe Dashboard pre-creation: Lifetime Product + Price exist; any required grandfathered Price objects exist."
    - "Live smoke (HUMAN-UAT, 4 discrete signals): (a) lifetime purchase end-to-end, (b) grandfathered pricing displayed silently, (c) coupon-stack 70% cap hit, (d) RLS cross-tenant deny proof for grandfathered_prices."
    - "Bundle budget assertion passes (admin chunk + new billing sub-page within ceiling); full vitest + Playwright suite green."
  artifacts:
    - path: ".planning/phases/43-m4-membership-tiers-extension/43-DEPLOY-NOTES.md"
      provides: "Step-by-step operator deploy sequence + outcome record"
      contains: "supabase db push --linked"
    - path: ".planning/phases/43-m4-membership-tiers-extension/43-CARRY-OVER.md"
      provides: "Any items deferred from Phase 43 to a later phase / milestone close"
      contains: "(written even if empty)"
  key_links:
    - from: "Supabase linked project"
      to: "7 Phase 43 migrations"
      via: "supabase db push --linked"
      pattern: "20270715000001..07"
    - from: "Edge Fn runtime"
      to: "3 redeployed Fns"
      via: "supabase functions deploy stripe-webhook stripe-checkout cancellation-accept-offer --import-map supabase/functions/import_map.json"
      pattern: "functions deploy"
---

<objective>
Close out Phase 43: deploy all 7 migrations + 3 Edge Fns; populate `stripe_price_lookup` with operator-supplied Stripe Price IDs; run automated full-suite verification; conduct 4-signal HUMAN-UAT smoke.

Per [[feedback_multi_signal_human_verify_checkpoint_pattern]], Task 2 is structured as a HUMAN gate with 4 DISCRETE resume signals — operator can approve automated/CLI items inline and carry browser/Stripe-test-mode items to milestone close if any are blocked.

Purpose: gate that downstream consumers of Phase 43 (Phases 44/46/47 + roll-forward 43-PRO-GATING-CONTRACT consumers) can rely on the schema + RPCs + helpers being live.

Output:
- Deploy sequence run + recorded in 43-DEPLOY-NOTES.md
- HUMAN-UAT outcomes recorded in 43-DEPLOY-NOTES.md (one section per signal)
- 43-CARRY-OVER.md written (even if empty — explicit "no carry-over" is the canonical close state)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-m4-membership-tiers-extension/43-CONTEXT.md
@.planning/phases/43-m4-membership-tiers-extension/43-RESEARCH.md
@.planning/phases/43-m4-membership-tiers-extension/43-PATTERNS.md
@.planning/phases/43-m4-membership-tiers-extension/43-01-SUMMARY.md
@.planning/phases/43-m4-membership-tiers-extension/43-02-SUMMARY.md
@.planning/phases/43-m4-membership-tiers-extension/43-03-SUMMARY.md
@.planning/phases/43-m4-membership-tiers-extension/43-04-SUMMARY.md
@.planning/phases/43-m4-membership-tiers-extension/43-05-SUMMARY.md

<interfaces>
Migrations to push (in this order — alphabetical/timestamp ordering enforces dependency):
  20270715000001_p43_lifetime_purchases.sql                  (43-01)
  20270715000002_p43_tier_effective_view_v2.sql              (43-01)
  20270715000003_p43_grandfathered_prices.sql                (43-02)
  20270715000004_p43_grandfathered_prices_rpcs.sql           (43-02)
  20270715000005_p43_entitlement_helpers.sql                 (43-03)
  20270715000006_p43_resolve_user_effective_price.sql        (43-03)
  20270715000007_p43_promo_trial_extensions_log.sql          (43-04)

Edge Fns to redeploy:
  stripe-webhook (handler extended in 43-01)
  stripe-checkout (extended in 43-04)
  cancellation-accept-offer (extended in 43-04)

Function Secret to set (NEW):
  STRIPE_PRICE_LIFETIME — operator pre-creates one-time Price in Stripe Dashboard ($499 default per CONTEXT specifics), pastes ID here

stripe_price_lookup rows to populate (NEW table from 43-03):
  ('plus_monthly', <existing STRIPE_PRICE_PLUS_MONTHLY value>)
  ('plus_yearly', <existing STRIPE_PRICE_PLUS_YEARLY value>)
  ('clinic_base', <existing STRIPE_PRICE_CLINIC_BASE value>)
  ('clinic_overage', <existing STRIPE_PRICE_CLINIC_OVERAGE value>)
  ('lifetime', <new STRIPE_PRICE_LIFETIME value>)

Pre-merge timestamp collision precheck:
  ls supabase/migrations/20270715*.sql | wc -l   (must equal 7; no collisions)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy validation + automated full-suite verification</name>
  <files>.planning/phases/43-m4-membership-tiers-extension/43-DEPLOY-NOTES.md</files>
  <action>
Run these checks IN ORDER and record outcomes in `43-DEPLOY-NOTES.md`:

(1) Migration filename collision precheck:
  ```bash
  cd /Users/karstenhaldan/minisite && ls supabase/migrations/20270715*.sql
  ```
  Expected: exactly 7 files (001..007 prefixed `20270715`). Record the list.

(2) Migration filename strict regex check:
  ```bash
  cd /Users/karstenhaldan/minisite && for f in supabase/migrations/20270715*.sql; do echo "$f" | grep -E '^supabase/migrations/[0-9]{14}_[a-z0-9_]+\.sql$' || echo "FAIL: $f"; done
  ```
  Expected: all 7 match; no "FAIL:" lines.

(3) Remote tail check (must be < 20270715000001):
  ```bash
  cd /Users/karstenhaldan/minisite && npx supabase db query --linked "SELECT MAX(version) FROM supabase_migrations.schema_migrations;"
  ```
  Expected: result < `20270715000001`. Record actual value.

(4) Sibling-test sweep (per [[feedback_post_merge_deno_sweep_pattern]]):
  ```bash
  cd /Users/karstenhaldan/minisite && for d in supabase/functions/stripe-webhook supabase/functions/stripe-checkout supabase/functions/cancellation-accept-offer supabase/functions/_shared; do $HOME/.deno/bin/deno test --allow-all --no-check $d/; done
  ```
  Expected: all green.

(5) Full leanshot suite:
  ```bash
  cd /Users/karstenhaldan/minisite/leanshot && npm install && npx tsc -p tsconfig.app.json --noEmit && npx vitest run && npx eslint . --max-warnings 0
  ```
  Expected: all green. (Note: `npm install` per [[reference_npm_install_worktree_main_drift]] — needed after Wave 2/3 merges if any new deps.)

(6) Bundle budget assertion (admin chunk delta from new GrandfatheredPricesPage):
  ```bash
  cd /Users/karstenhaldan/minisite/leanshot && npm run build && bash scripts/assert-bundle-budgets.sh 2>&1
  ```
  Expected: pass; if admin chunk exceeded ceiling, surface as BLOCKER and DO NOT proceed to Task 2.

Write all 6 outcomes to `43-DEPLOY-NOTES.md` under heading `## Pre-Deploy Validation`. Use ✅ / ❌ marks.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && [ -f .planning/phases/43-m4-membership-tiers-extension/43-DEPLOY-NOTES.md ] && grep -n "Pre-Deploy Validation" .planning/phases/43-m4-membership-tiers-extension/43-DEPLOY-NOTES.md && [ "$(ls supabase/migrations/20270715*.sql 2>/dev/null | wc -l | tr -d ' ')" = "7" ]</automated>
  </verify>
  <done>All 6 pre-deploy checks pass and are recorded in 43-DEPLOY-NOTES.md; no failures requiring rework.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push + functions deploy + 4-signal HUMAN-UAT</name>
  <what-built>
    The deploy phase. Claude executes the deploy commands and records outcomes; operator validates 4 discrete behaviors via Stripe Dashboard + browser + CLI proofs.
  </what-built>
  <how-to-verify>

**Claude executes these inline first (record outcomes in 43-DEPLOY-NOTES.md):**

1) **DB push:**
   ```bash
   cd /Users/karstenhaldan/minisite && supabase db push --linked
   ```
   If push fails with "back-dated migration" → halt + investigate (likely collision; check [[reference_supabase_back_dated_migration_blocks_push]] for the mv-then-restore recipe).

2) **Edge Fn deploy (3 fns):**
   ```bash
   cd /Users/karstenhaldan/minisite && supabase functions deploy stripe-webhook stripe-checkout cancellation-accept-offer --import-map supabase/functions/import_map.json
   ```
   Per [[reference_supabase_functions_deploy_no_linked_flag]] — NO `--linked` flag; per [[reference_supabase_functions_deploy_import_map_flag]] — `--import-map` is required if _shared/* aliases are imported.

3) **Set STRIPE_PRICE_LIFETIME Function Secret:**
   ```bash
   # Operator-provided Lifetime Stripe Price ID (paste from Stripe Dashboard → Products → Lifetime → Pricing)
   read -p "STRIPE_PRICE_LIFETIME: " PRICE_ID
   echo "$PRICE_ID" | supabase secrets set --project-ref ytnsipxxmzgaebkqmokp STRIPE_PRICE_LIFETIME
   supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep STRIPE_PRICE_LIFETIME
   ```

4) **Populate stripe_price_lookup table (admin via supabase db query --linked):**
   ```bash
   cd /Users/karstenhaldan/minisite && supabase db query --linked "
     update public.stripe_price_lookup set stripe_price_id = '<paste-each-id>' where plan_name = 'lifetime';
     -- repeat for plus_monthly, plus_yearly, clinic_base, clinic_overage using existing Stripe Function Secrets' values
     select plan_name, stripe_price_id from public.stripe_price_lookup order by plan_name;
   "
   ```
   Confirm 5 rows, no empty `stripe_price_id` values.

**Then operator validates these 4 signals (each can be approved independently):**

**SIGNAL A — Lifetime purchase smoke (MEMBER-01):**
  - Open https://app.leanshot.app/pricing (or local dev) signed in as a free-tier test user.
  - Click "Buy Lifetime $499" CTA.
  - Stripe Checkout (test mode) → use card `4242 4242 4242 4242`.
  - After redirect: dashboard shows LIFETIME badge next to user name.
  - Verify in DB: `supabase db query --linked "select * from public.lifetime_purchases where stripe_customer_id = '<cus_...>';"` returns 1 row.
  - Verify in DB: `supabase db query --linked "select user_id, has_active, tier_label from public.tier_effective where user_id = '<uuid>';"` returns `has_active=true, tier_label='lifetime'`.
  - **Resume signal A:** "approved-A" / "issue-A: <details>"

**SIGNAL B — Grandfathered pricing silent at NEW stripe-checkout (MEMBER-02):**
  - Verify a NEW user matching a grandfathered cohort sees the cohort price at stripe-checkout. Existing-subscriber renewal-time update is DEFERRED — see 43-CARRY-OVER.md.
  - As admin: visit `/admin/billing/grandfathered-prices`. Create a cohort override (cohort: any existing P27 cohort the test user belongs to; stripe_price_id: a pre-created Stripe Price; effective_from: now; effective_until: null).
  - Switch to a NEW (non-subscribed) test user matching that cohort. Open `/pricing`. Confirm the displayed price is the GRANDFATHERED amount (not the public default). NO banner, NO badge, NO upgrade prompt (D-05 silent stability).
  - Existing-subscriber renewal-time price update is OUT OF SCOPE for v1.3 (deferred per 43-CARRY-OVER.md item 2).
  - **Resume signal B:** "approved-B" / "issue-B: <details>"

**SIGNAL C — 70% cap hit (MEMBER-03):**
  - Stripe Dashboard → create a 50% promo coupon `TEST50`.
  - In a test session: have an existing 50% SAVE-offer accepted on the subscription (or mock by directly inserting into cancellation_offers_log).
  - Call `POST /functions/v1/stripe-checkout` with `{ plan: 'plus_monthly', promo_code: 'TEST50' }`.
  - Expected response: HTTP 400 `{ error: 'discount_combination_exceeds_max' }`.
  - **Resume signal C:** "approved-C" / "issue-C: <details>"

**SIGNAL D — RLS cross-tenant deny proof (MEMBER-02, MEMBER-04):**
  - As non-admin authenticated user: `supabase.from('grandfathered_prices').select('*')` → expect empty array (RLS admin-only-read).
  - As non-admin authenticated user: `supabase.from('lifetime_purchases').select('*').neq('user_id', currentUserId)` → expect empty array (self-only-read).
  - As authenticated user calling `supabase.rpc('current_user_has_pro')` → returns true/false matching tier_effective.
  - Per [[reference_supabase_project]] rule: every RLS surface gets a live cross-tenant impersonation proof. Use an existing RLS test fixture pattern from prior phases.
  - **Resume signal D:** "approved-D" / "issue-D: <details>"

If any signal returns "issue-X", operator describes the failure; Claude diagnoses + creates fix-up plan (potentially a new gap-closure plan 43-07 if non-trivial).

If 1-3 signals are blocked by environmental/operator unavailability, per [[feedback_multi_signal_human_verify_checkpoint_pattern]]: ship the approved signals + defer the blocked ones to milestone close in 43-CARRY-OVER.md.
  </how-to-verify>
  <resume-signal>Type one of: "approved-A approved-B approved-C approved-D" (all 4 green) | "approved-A approved-B approved-C defer-D: <reason>" | "issue-X: <details>" for any signal needing rework.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 43-CARRY-OVER.md + finalize 43-DEPLOY-NOTES.md</name>
  <files>.planning/phases/43-m4-membership-tiers-extension/43-CARRY-OVER.md, .planning/phases/43-m4-membership-tiers-extension/43-DEPLOY-NOTES.md</files>
  <action>
Based on Task 2 outcomes:

(a) Append to `43-DEPLOY-NOTES.md`:
  - `## Deploy Sequence` — record each command's outcome (db push, fn deploy, secrets set, lookup populate).
  - `## HUMAN-UAT Signals` — one subsection per signal (A/B/C/D) recording approved or deferred + any operator notes.
  - `## Linked Dashboards` (helpful for future audit): Stripe Dashboard product link, Supabase project URL, deployed Fn URLs.

(b) Write `43-CARRY-OVER.md`:
  - If ALL 4 signals approved + no operational gaps: write a minimal file stating "Phase 43 closed clean — no carry-over." with a one-line confirmation list of all 5 plan SUMMARYs landed.
  - Else, for each deferred / issue signal:
    - Heading `## Deferred Item: <topic>`
    - `Source`: which plan + which decision
    - `Reason for deferral`: environmental / operator-blocked / non-trivial fix
    - `Destination`: which future phase OR milestone close OR new gap-closure plan ID
    - `Acceptance criteria for closure`: bulleted list
  - Mirror the pattern from prior `<phase>-CARRY-OVER.md` files (e.g., Phase 35 + 40 ones referenced in MEMORY.md).
  - Also update ROADMAP.md inline annotation `[DEFERRED to <dest>]` next to MEMBER-NN entries if any are partially deferred (per [[feedback_partial_phase_carry_over_md_pattern]]).
</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && [ -f .planning/phases/43-m4-membership-tiers-extension/43-CARRY-OVER.md ] && [ -f .planning/phases/43-m4-membership-tiers-extension/43-DEPLOY-NOTES.md ] && grep -n "Deploy Sequence" .planning/phases/43-m4-membership-tiers-extension/43-DEPLOY-NOTES.md && grep -n "HUMAN-UAT Signals" .planning/phases/43-m4-membership-tiers-extension/43-DEPLOY-NOTES.md</automated>
  </verify>
  <done>Both documents exist; deploy sequence + HUMAN-UAT signal outcomes recorded; carry-over file accurately reflects "no carry-over" OR specific deferred items with destination + acceptance criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → linked Supabase project | `supabase db push --linked` uses CLI-stored credentials; service-role-scoped CLI action |
| Operator → Stripe Dashboard | Operator authentication via Stripe login; secret rotation outside this plan's scope |
| HUMAN-UAT Signal D → live RLS | Cross-tenant proof done with live JWT impersonation (rls fixture pattern); destructive only for the test user account |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-06-01 | Tampering | db push applies a stale local migration file that has been edited post-merge | mitigate | Pre-deploy Task 1 verifies file count (7) + filename regex + remote tail timestamp. The grep-strict check catches accidental backups (`*.bak`) or letter-suffix renames per [[reference_supabase_migration_filename_regex]] |
| T-43-06-02 | Repudiation | operator cannot confirm which Stripe Price ID was loaded into stripe_price_lookup | mitigate | The supabase db query SELECT statement in Task 2 step (4) prints all 5 rows to stdout; output captured in 43-DEPLOY-NOTES.md |
| T-43-06-03 | EoP | a HUMAN-UAT test user retains LIFETIME entitlement after phase close (test data leak) | accept | Per CONTEXT deferred-list: refund handling is operator-manual; test lifetime_purchases rows from UAT remain in prod DB with no impact (no incidents triggered, no real money). Document test user IDs in DEPLOY-NOTES for audit. |
| T-43-06-04 | DoS | Edge Fn deploy fails mid-sequence leaving 1/3 Fns redeployed | mitigate | If functions deploy fails, halt + investigate; do NOT proceed to HUMAN-UAT signals (would produce false negatives). Re-issue deploy with full Fn list (Supabase deploy is idempotent on Fn version). |
| T-43-06-05 | Information Disclosure | UAT recordings (4 signals) commit Stripe customer IDs / payment intent IDs to git | mitigate | 43-DEPLOY-NOTES.md records IDs only after they are confirmed test-mode (cus_ + pi_ prefixes are public Stripe identifiers, not secrets). Do NOT commit raw card numbers or webhook payloads. |
| T-43-06-06 | Tampering | partial-phase ship with deferred signals — downstream phases assume all of Phase 43 is live | mitigate | 43-CARRY-OVER.md + inline ROADMAP annotation `[DEFERRED to <dest>]` per [[feedback_partial_phase_carry_over_md_pattern]] ensures downstream consumers (44/46/47) check the carry-over file before relying on entitlement gates. Plan-checker for downstream phases must grep `43-CARRY-OVER.md` for unresolved items affecting `current_user_has_pro` / `grandfathered_prices`. |
</threat_model>

<verification>
- Migration count check: exactly 7 files matching `supabase/migrations/20270715*.sql`.
- Filename regex `^[0-9]{14}_[a-z0-9_]+\.sql$` passes for all 7.
- `supabase db push --linked` exit code 0 + remote tail advances to `20270715000007`.
- 3 Edge Fns redeployed without error (`Function deployed: stripe-webhook` etc.).
- `STRIPE_PRICE_LIFETIME` appears in `supabase secrets list`.
- `stripe_price_lookup` 5 rows all have non-empty `stripe_price_id`.
- 43-DEPLOY-NOTES.md + 43-CARRY-OVER.md exist with correct headings.
</verification>

<success_criteria>
- All 7 Phase 43 migrations applied to linked project
- All 3 Edge Fns redeployed
- STRIPE_PRICE_LIFETIME secret set + stripe_price_lookup table fully populated
- ≥1 of 4 HUMAN-UAT signals approved (more required for a "clean close"; partial close acceptable per multi-signal pattern)
- 43-DEPLOY-NOTES.md records deploy sequence + UAT outcomes
- 43-CARRY-OVER.md accurately reflects close state (either "no carry-over" or itemized deferrals)
- Phase 43 ROADMAP.md entry updated to reflect closeout state
</success_criteria>

<output>
After completion, create `.planning/phases/43-m4-membership-tiers-extension/43-06-SUMMARY.md` covering:
- Deploy sequence completion + outcomes
- HUMAN-UAT signal pass/defer breakdown
- Files touched + line counts
- Phase 43 close state (clean / partial-with-carry-over)
- Cross-phase note for Phases 44/46/47 planners: 43-PRO-GATING-CONTRACT.md is now stable + canonical; consume verbatim
- Next milestone-close action: review 43-CARRY-OVER.md before milestone v1.3 close
</output>
