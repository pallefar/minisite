---
phase: 43-m4-membership-tiers-extension
plan: 05
type: execute
wave: 3
depends_on: [43-01, 43-02, 43-03]
files_modified:
  - leanshot/src/components/billing/PaywallUpsell.tsx
  - leanshot/src/components/billing/PaywallUpsell.test.tsx
  - leanshot/src/lib/entitlement/current-user-has-pro.ts
  - leanshot/src/lib/entitlement/current-user-has-pro.test.ts
  - leanshot/src/components/billing/LifetimeBadge.tsx
  - leanshot/src/components/billing/LifetimeBadge.test.tsx
  - leanshot/src/admin/modules/billing/GrandfatheredPricesPage.tsx
  - leanshot/src/admin/modules/billing/GrandfatheredPricesPage.test.tsx
  - leanshot/src/lib/admin/modules.ts
autonomous: true
requirements: [MEMBER-01, MEMBER-02, MEMBER-04]
must_haves:
  truths:
    - "PaywallUpsell renders with `gating_reason='pro_only_resource'` displays pro-only-resource upsell copy + CTA; renders with default (or `'activation_paywall'`) preserves Phase 39 behavior — existing call sites compile without changes (OPTIONAL prop)."
    - "`useCurrentUserHasPro()` returns `{ has_pro: boolean, loading: boolean }`; reads from `tier_effective.has_active` via supabase.from() once, caches in module-level `Map<userId,{value,expiresAt}>` with 60s TTL + LRU eviction at 10k entries (per D-13)."
    - "LifetimeBadge component renders the LIFETIME pill ONLY when `tier_effective.tier_label === 'lifetime'`; renders nothing otherwise."
    - "GrandfatheredPricesPage admin page lists rows from `public.grandfathered_prices` joined with `public.cohort_definitions.name`; supports Create/Update/Delete via `supabase.rpc('grandfathered_price_*')`."
    - "ADMIN_MODULES manifest has a `billing/grandfathered-prices` sub-route that resolves to GrandfatheredPricesPage; URL `/admin/billing/grandfathered-prices` mounts the page (per [[feedback_admin_module_manifest_vs_router_branch_drift]])."
  artifacts:
    - path: "leanshot/src/components/billing/PaywallUpsell.tsx"
      provides: "Optional `gating_reason` + `resource_type` + `resource_name` props (backward-compatible additive extension)"
      contains: "gating_reason"
    - path: "leanshot/src/lib/entitlement/current-user-has-pro.ts"
      provides: "`useCurrentUserHasPro` hook + module-level 60s LRU cache"
      exports: ["useCurrentUserHasPro", "invalidateProCache"]
    - path: "leanshot/src/components/billing/LifetimeBadge.tsx"
      provides: "Conditional LIFETIME pill (reads tier_effective.tier_label)"
      exports: ["LifetimeBadge"]
    - path: "leanshot/src/admin/modules/billing/GrandfatheredPricesPage.tsx"
      provides: "Admin CRUD for grandfathered_prices via SECDEF RPCs"
      exports: ["GrandfatheredPricesPage"]
    - path: "leanshot/src/lib/admin/modules.ts"
      provides: "Extended ADMIN_MODULES with grandfathered-prices sub-route"
      contains: "grandfathered-prices"
  key_links:
    - from: "PaywallUpsell (gating_reason=pro_only_resource)"
      to: "useCurrentUserHasPro"
      via: "hook check; if has_pro=true, render children (or null); if has_pro=false, render upsell"
      pattern: "useCurrentUserHasPro"
    - from: "useCurrentUserHasPro"
      to: "public.tier_effective"
      via: "supabase.from('tier_effective').select('has_active,tier_label').eq('user_id', userId).single()"
      pattern: "from\\(['\"]tier_effective"
    - from: "GrandfatheredPricesPage"
      to: "public.grandfathered_price_{create,update,delete} RPCs"
      via: "supabase.rpc('grandfathered_price_create' | '_update' | '_delete', {...})"
      pattern: "grandfathered_price_(create|update|delete)"
    - from: "ADMIN_MODULES manifest"
      to: "GrandfatheredPricesPage lazy import"
      via: "lazy: () => import('@/admin/modules/billing/GrandfatheredPricesPage').then(m => ({ default: m.GrandfatheredPricesPage }))"
      pattern: "GrandfatheredPricesPage"
---

<objective>
Ship the consumer-facing surfaces: (a) the PaywallUpsell extension that the cross-phase contract (43-03 PRO-GATING-CONTRACT) references, (b) the client-side entitlement hook with 60s in-memory cache per D-13, (c) the LIFETIME badge component, (d) the admin Grandfathered Prices CRUD page, and (e) the admin module manifest entry that routes `/admin/billing/grandfathered-prices` to it.

Purpose: MEMBER-01 D-01 (LIFETIME badge surface) + MEMBER-02 D-03 (admin CRUD) + MEMBER-04 D-11 (PaywallUpsell variant) + MEMBER-04 D-13 (60s cache).

Output: 5 source files + 4 test files + 1 manifest extension. All TypeScript / React.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-m4-membership-tiers-extension/43-CONTEXT.md
@.planning/phases/43-m4-membership-tiers-extension/43-RESEARCH.md
@.planning/phases/43-m4-membership-tiers-extension/43-PATTERNS.md
@.planning/phases/43-m4-membership-tiers-extension/43-PRO-GATING-CONTRACT.md
@.planning/phases/43-m4-membership-tiers-extension/43-01-SUMMARY.md
@.planning/phases/43-m4-membership-tiers-extension/43-02-SUMMARY.md
@.planning/phases/43-m4-membership-tiers-extension/43-03-SUMMARY.md

# Reuse-target files (CONFIRMED present)
@leanshot/src/components/billing/PaywallUpsell.tsx
@leanshot/src/admin/modules/hitl-queue/HitlQueuePage.tsx
@leanshot/src/lib/admin/modules.ts

<interfaces>
<!-- Wave 3: depends on 43-01 (tier_effective view), 43-02 (grandfathered_prices + RPCs), 43-03 (helper functions).
     Does NOT depend on 43-04 — UI surfaces consume tables/views/RPCs directly, not the Edge Fn extensions. -->

From leanshot/src/components/billing/PaywallUpsell.tsx (existing, lines 32-67):
  - export interface PaywallUpsellProps { variant: 'overlay' | 'cta'; feature: FeatureKey; headline?: string; plan?: Plan; }
  - Existing call sites (DO NOT BREAK): MedLevelChart, AIChatPanel — these pass only `variant` + `feature` (per PATTERNS Pitfall 8).

From leanshot/src/admin/modules/hitl-queue/HitlQueuePage.tsx (analog for GrandfatheredPricesPage):
  - Uses `import { supabase } from '@/lib/supabase';`
  - useState for rows / loading / error
  - useCallback-wrapped reload using `supabase.from(...).select(...)` with try/catch/finally
  - Write actions via `supabase.rpc(...)` with `if (error) setError(error.message)`
  - Lazy-default export pattern

From leanshot/src/lib/admin/modules.ts (existing, lines 198-221 — billing entry):
  ```ts
  {
    key: 'billing',
    label: 'Billing',
    route: 'billing',
    icon: CreditCardIcon,
    lazy: () => import('@/components/admin/pages/AdminAffiliatesPage').then((m) => ({ default: m.AdminAffiliatesPage })),
    flagKey: 'admin.billing.enabled',
    minRole: 'admin' as AdminRole,
  },
  ```
  P43 extension choice: ADD a SIBLING manifest entry `key: 'billing-grandfathered'`, `route: 'billing/grandfathered-prices'`, sub-route URL `/admin/billing/grandfathered-prices`. Per [[feedback_admin_module_manifest_vs_router_branch_drift]] — keep the sibling entry separate so the URL-prefix-keyed router resolves deterministically.

From 43-03 (Plan 03) outputs:
  - `public.current_user_has_pro()` (authenticated, reads auth.uid) — for direct .rpc() calls from authenticated client
  - `public.user_has_pro(p_user_id)` — NOT used by browser client (admin uses authenticated RLS predicate directly via tier_effective view)
  - `public.tier_effective` view with `tier_label` text column

From 43-02 outputs:
  - `public.grandfathered_prices` table (admin-read RLS) + `cohort_definitions` join via `cohort:cohort_definitions(id, name)` PostgREST embed
  - SECDEF RPCs: `grandfathered_price_create(p_cohort_id, p_stripe_price_id, p_effective_from, p_effective_until)`, `_update(p_id, p_stripe_price_id, p_effective_from, p_effective_until)`, `_delete(p_id)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PaywallUpsell extension + useCurrentUserHasPro hook + LifetimeBadge component (with tests)</name>
  <files>leanshot/src/components/billing/PaywallUpsell.tsx, leanshot/src/components/billing/PaywallUpsell.test.tsx, leanshot/src/lib/entitlement/current-user-has-pro.ts, leanshot/src/lib/entitlement/current-user-has-pro.test.ts, leanshot/src/components/billing/LifetimeBadge.tsx, leanshot/src/components/billing/LifetimeBadge.test.tsx</files>
  <behavior>
    PaywallUpsell:
    - Existing call site `<PaywallUpsell variant="overlay" feature="ai_chat" />` still compiles + renders activation-paywall copy (default gating_reason='activation_paywall').
    - `<PaywallUpsell variant="overlay" feature="community" gating_reason="pro_only_resource" resource_type="community" resource_name="GLP-1 Starters" />` renders pro-only-resource copy mentioning "GLP-1 Starters".
    - Required-prop addition does NOT break TS compile: `gating_reason`, `resource_type`, `resource_name` are ALL OPTIONAL with defaults.

    useCurrentUserHasPro:
    - First call with userId=A → fetches tier_effective.has_active via supabase.from(), caches result for 60s.
    - Second call within 60s with userId=A → returns cached value, NO supabase call.
    - Call after 60s TTL → re-fetches.
    - 10001st distinct userId → evicts oldest entry (LRU).
    - Call with userId=null → returns `{ has_pro: false, loading: false }` immediately, no fetch.
    - `invalidateProCache(userId)` clears the entry for that user; next call re-fetches.

    LifetimeBadge:
    - When supabase.from('tier_effective').select('tier_label').eq('user_id', userId).single() returns `{ tier_label: 'lifetime' }` → renders a pill with text "LIFETIME" + premium styling.
    - When tier_label is 'pro' / 'trial' / 'free' / null → renders nothing (returns null).
  </behavior>
  <action>
A) Extend `leanshot/src/components/billing/PaywallUpsell.tsx`:
  - Extend `PaywallUpsellProps` ADDITIVELY with OPTIONAL fields: `gating_reason?: 'activation_paywall' | 'pro_only_resource';`, `resource_type?: 'community' | 'course' | 'event';`, `resource_name?: string;`. Default `gating_reason = 'activation_paywall'` preserves existing behavior.
  - In the component body, branch headline/body copy on `gating_reason`. For `'pro_only_resource'`: headline = `\`${resource_name ?? 'This content'} is Pro-only\``; body = `\`Unlock the full ${resource_type ?? 'resource'} library with Pro.\``; primary CTA: existing "Upgrade to Pro" button (existing checkout invoke).
  - For `'activation_paywall'` (default): preserve existing copy + behavior verbatim.
  - Do NOT alter the existing checkout-invoke handler structure (per Pattern from PATTERNS lines 71-87).
  - Test file: add 3 test cases (default-preserves-old, pro_only_resource renders new copy, optional resource_name omitted falls back to "This content").

B) Create `leanshot/src/lib/entitlement/current-user-has-pro.ts`:
  - Module-scope cache: `const cache = new Map<string, { value: boolean; expiresAt: number }>();` with `const TTL_MS = 60_000;` and `const MAX_ENTRIES = 10_000;`.
  - `export function invalidateProCache(userId: string | null): void` — clears the entry.
  - `export function useCurrentUserHasPro(): { has_pro: boolean; loading: boolean }` — React hook that:
    1. Reads `userId` from existing supabase auth context (via `supabase.auth.getSession()` OR an existing app-level user hook — pick whatever pattern existing PaywallUpsell uses to identify the user).
    2. On userId change: check `cache.get(userId)`; if exists AND `expiresAt > Date.now()` → set state with cached value, loading=false.
    3. Otherwise: fetch `supabase.from('tier_effective').select('has_active').eq('user_id', userId).maybeSingle()`; on resolve, set cache + state.
    4. LRU eviction: when inserting and `cache.size >= MAX_ENTRIES`, delete the oldest entry (Map iteration order is insertion order in JS).
    5. Cache-key isolation: ALWAYS keyed on userId; never global (per RESEARCH Pitfall 9 — defends against cross-user state leak in tests / SSR).
  - Test file (vitest, RTL): renderHook covering all 6 behaviors above. Stub supabase via vi.mock.

C) Create `leanshot/src/components/billing/LifetimeBadge.tsx`:
  - Props: `{ userId: string | null }`.
  - Effect: query `supabase.from('tier_effective').select('tier_label').eq('user_id', userId).maybeSingle()` (uses RLS-protected view; user can only read their own row).
  - Render: when `data?.tier_label === 'lifetime'` → return `<span className="...pill-styling..." aria-label="Lifetime member">LIFETIME</span>`. Otherwise return `null`.
  - Pill styling per Tailwind v4 conventions: small rounded pill, accent color, uppercase text, no margin (consumers position).
  - Test file: 4 cases — lifetime → renders; pro → null; null userId → null; loading state initially → null (no flash of LIFETIME).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && grep -n "gating_reason" src/components/billing/PaywallUpsell.tsx && grep -n "resource_type" src/components/billing/PaywallUpsell.tsx && grep -n "export function useCurrentUserHasPro" src/lib/entitlement/current-user-has-pro.ts && grep -n "export function invalidateProCache" src/lib/entitlement/current-user-has-pro.ts && grep -n "const TTL_MS = 60_000" src/lib/entitlement/current-user-has-pro.ts && grep -n "MAX_ENTRIES = 10_000" src/lib/entitlement/current-user-has-pro.ts && grep -n "export function LifetimeBadge" src/components/billing/LifetimeBadge.tsx && grep -n "tier_label === 'lifetime'" src/components/billing/LifetimeBadge.tsx && npx vitest run src/components/billing/PaywallUpsell.test.tsx src/lib/entitlement/current-user-has-pro.test.ts src/components/billing/LifetimeBadge.test.tsx</automated>
  </verify>
  <done>All 3 source files + 3 test files exist; 3+6+4 = 13 vitest test cases pass; PaywallUpsell additive props are OPTIONAL (TS compile passes for existing call sites without modification); cache is keyed on userId; LifetimeBadge renders pill only on 'lifetime' label.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GrandfatheredPricesPage admin CRUD + ADMIN_MODULES manifest extension</name>
  <files>leanshot/src/admin/modules/billing/GrandfatheredPricesPage.tsx, leanshot/src/admin/modules/billing/GrandfatheredPricesPage.test.tsx, leanshot/src/lib/admin/modules.ts</files>
  <behavior>
    GrandfatheredPricesPage:
    - On mount: loads rows via `supabase.from('grandfathered_prices').select('*, cohort:cohort_definitions(id, name)').order('effective_from', { ascending: false })`. Renders a table.
    - Create: form with cohort picker (loaded separately from `cohort_definitions`), stripe_price_id text input (regex hint `^price_[A-Za-z0-9]+$`), effective_from datetime, effective_until datetime (optional). On submit: `supabase.rpc('grandfathered_price_create', { p_cohort_id, p_stripe_price_id, p_effective_from, p_effective_until })`; on success: reload table + reset form.
    - Update: per-row edit button toggles inline edit mode; on save: `supabase.rpc('grandfathered_price_update', { p_id, p_stripe_price_id, p_effective_from, p_effective_until })`.
    - Delete: per-row delete button (with confirm prompt); on confirm: `supabase.rpc('grandfathered_price_delete', { p_id })`.
    - Error path: any RPC error → setError(error.message); show banner above table; do NOT clear loaded rows.
    - Loading state: skeleton placeholder while initial load is in flight.

    ADMIN_MODULES manifest:
    - New sibling entry `key: 'billing-grandfathered'`, label `'Grandfathered Pricing'`, route `'billing/grandfathered-prices'`, icon (reuse `CreditCardIcon` or pick another lucide icon), `lazy: () => import('@/admin/modules/billing/GrandfatheredPricesPage').then((m) => ({ default: m.GrandfatheredPricesPage }))`, `flagKey: 'admin.billing.grandfathered.enabled'` (default true), `minRole: 'admin' as AdminRole`.

    Tests (vitest + RTL):
    - Test 1: mounts + renders table with 2 stub rows from mocked supabase.from → both visible.
    - Test 2: create form submission → supabase.rpc('grandfathered_price_create') called with form values; on success, reload triggered.
    - Test 3: update row inline → supabase.rpc('grandfathered_price_update') called with p_id + new values.
    - Test 4: delete row with confirm → supabase.rpc('grandfathered_price_delete') called with p_id.
    - Test 5: RPC error → error banner rendered with error.message text.
  </behavior>
  <action>
A) Create `leanshot/src/admin/modules/billing/GrandfatheredPricesPage.tsx`:
  - Structure cloned from `HitlQueuePage.tsx` per PATTERNS analog mandate. Single named export `GrandfatheredPricesPage` (no default — manifest lazy-import re-exports as default).
  - Header comment: 'Phase 43 Plan 05 — MEMBER-02 D-03 admin CRUD for grandfathered_prices.'
  - State: `rows`, `cohorts`, `loading`, `error`, `editingRowId`, `createForm` (cohort_id, stripe_price_id, effective_from, effective_until).
  - `reload` useCallback loads both rows + cohorts in parallel.
  - Render: error banner (if any) + create form + table with cohort name / stripe_price_id / effective_from / effective_until / actions.
  - Imports: `import { supabase } from '@/lib/supabase';` (or whatever existing convention HitlQueuePage uses).

B) Test file: 5 vitest+RTL cases per behavior block above. Mock supabase via `vi.mock('@/lib/supabase', () => ({ supabase: { from: vi.fn(...), rpc: vi.fn(...) } }))` pattern (whatever style other admin tests use).

C) Extend `leanshot/src/lib/admin/modules.ts`:
  - Add the new sibling entry immediately AFTER the existing `billing` entry (around lines 198-221). Preserve the existing entry verbatim — only ADD a new entry.
  - If existing import block does not have `CreditCardIcon` aliased for the sibling, reuse it OR import another lucide icon (`TagIcon`, `DollarSignIcon`) as appropriate.
  - Do NOT modify any other manifest entries or sort order outside the immediate vicinity.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && grep -n "export function GrandfatheredPricesPage\|export const GrandfatheredPricesPage" src/admin/modules/billing/GrandfatheredPricesPage.tsx && grep -n "grandfathered_price_create" src/admin/modules/billing/GrandfatheredPricesPage.tsx && grep -n "grandfathered_price_update" src/admin/modules/billing/GrandfatheredPricesPage.tsx && grep -n "grandfathered_price_delete" src/admin/modules/billing/GrandfatheredPricesPage.tsx && grep -n "billing-grandfathered\|billing/grandfathered-prices" src/lib/admin/modules.ts && grep -n "GrandfatheredPricesPage" src/lib/admin/modules.ts && npx vitest run src/admin/modules/billing/GrandfatheredPricesPage.test.tsx && npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <done>Admin page exists with full CRUD via 3 SECDEF RPCs; manifest has new sibling entry routed to `/admin/billing/grandfathered-prices`; 5 vitest cases pass; full TS typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser SPA → supabase JS client (authenticated) | JWT bearer; queries hit RLS-protected tables/views |
| useCurrentUserHasPro → tier_effective view | RLS via security_invoker=true on the view; user can only read their own row via subscriptions/lifetime_purchases self-read policies |
| GrandfatheredPricesPage → grandfathered_price_* RPCs | SECDEF RPCs re-check `is_admin_at_least('admin')` inside; client-side admin-gate is UX only |
| In-memory cache (useCurrentUserHasPro) | Per-tab module memory; keyed on userId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05-01 | Tampering | non-admin user navigates to `/admin/billing/grandfathered-prices` directly | mitigate | `minRole: 'admin'` in manifest filters route visibility; SECDEF RPCs server-side re-check (defense in depth — admin gate enforced even if client manifest is bypassed) |
| T-43-05-02 | Information Disclosure | useCurrentUserHasPro cache persists across user switches (sign-out → sign-in different account) | mitigate | Cache keyed on userId; new userId → cache miss → fresh fetch. On sign-out: existing app-level cleanup should call `invalidateProCache(prevUserId)` if available (document hook in SUMMARY for app-level wiring). |
| T-43-05-03 | EoP | user inspects PaywallUpsell prop in devtools, manually sets `gating_reason` to bypass | accept | gating_reason is presentational only — the actual access gate is server-side RLS (Phase 44/46/47 tables) + Edge Fn 403 (per PRO-GATING-CONTRACT). Client-side bypass shows different copy but does NOT grant access. |
| T-43-05-04 | DoS | useCurrentUserHasPro cache grows unbounded | mitigate | LRU eviction at MAX_ENTRIES=10_000; on insert when full, delete oldest Map entry (insertion-order iteration). |
| T-43-05-05 | Information Disclosure | non-admin user queries grandfathered_prices directly via supabase.from() | mitigate | RLS SELECT policy on `grandfathered_prices` requires `is_admin_at_least('admin')`; non-admin gets empty result set (verified in Plan 43-02 threat model). |
| T-43-05-06 | Tampering | XSS in cohort_name renders unescaped in admin table | mitigate | React's default JSX escaping handles text content. Admin-only surface (input controlled by other admins) reduces attack surface; no dangerouslySetInnerHTML usage. |
| T-43-05-07 | Stale state | tier downgrade not reflected in cache for up to 60s | accept | Per CONTEXT D-13 bounded-staleness acceptable. Admin can call `invalidateProCache(userId)` from settings tabs if needed (export hook for future use). |
</threat_model>

<verification>
- All 9 files exist at declared paths.
- PaywallUpsell additive props are OPTIONAL (TS strict compile passes for existing consumers without modification — verified by `tsc --noEmit` in Task 2 verify).
- useCurrentUserHasPro cache TTL = 60_000ms, MAX_ENTRIES = 10_000, keyed on userId.
- LifetimeBadge renders pill only when `tier_label === 'lifetime'` (exact string match).
- Manifest extension is ADDITIVE — existing `billing` entry untouched.
- 13 + 5 = 18 vitest cases pass.
- Full `tsc -p tsconfig.app.json --noEmit` green.
</verification>

<success_criteria>
- 5 source + 4 test files + 1 manifest extension shipped
- Zero breakage of existing PaywallUpsell call sites
- 60s LRU cache keyed on userId (no global leak)
- Admin CRUD page wired through SECDEF RPCs (no direct table writes)
- ADMIN_MODULES manifest extension is a SIBLING entry (no edit-in-place to existing `billing` entry)
- TS strict compile passes
</success_criteria>

<output>
After completion, create `.planning/phases/43-m4-membership-tiers-extension/43-05-SUMMARY.md` covering:
- 9 files modified + line counts
- 18 vitest test cases passing
- Manifest extension: 1 new sibling entry — does NOT replace `billing`
- Carry-over: Vite production build + bundle-budget assertion deferred to closeout 43-06
- Recommendation for downstream phases: when free-tier user hits pro_only resource → API renders `<PaywallUpsell variant="overlay" feature="<feature_key>" gating_reason="pro_only_resource" resource_type="community|course|event" resource_name="<resource name>" />`
</output>
