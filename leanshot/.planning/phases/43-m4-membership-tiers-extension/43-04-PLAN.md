---
phase: 43-m4-membership-tiers-extension
plan: 04
type: execute
wave: 2
depends_on: [43-01, 43-02, 43-03]
files_modified:
  - supabase/migrations/20270715000007_p43_promo_trial_extensions_log.sql
  - supabase/functions/_shared/clamp-combined-discount.ts
  - supabase/functions/_shared/clamp-combined-discount.test.ts
  - supabase/functions/stripe-checkout/index.ts
  - supabase/functions/stripe-checkout/index.test.ts
  - supabase/functions/cancellation-accept-offer/index.ts
autonomous: true
requirements: [MEMBER-01, MEMBER-02, MEMBER-03]
must_haves:
  truths:
    - "POST stripe-checkout with `plan: 'lifetime'` returns a Stripe Checkout Session URL where `mode='payment'` and `line_items[0].price === resolve_user_effective_price(user.id, 'lifetime')` (or default STRIPE_PRICE_LIFETIME)."
    - "POST stripe-checkout with `plan: 'lifetime'` AND `promo_code` set returns HTTP 400 `{error: 'lifetime_no_promo_code'}` (per OQ-6 RESOLVED)."
    - "resolve_user_effective_price returns the cohort-grandfathered Stripe price.id at NEW stripe-checkout time. Existing subscribers retain their current Stripe price until they create a new subscription (deferred to 43-CARRY-OVER.md)."
    - "`clampCombinedDiscount(promoPct, saveOfferPct)` returns `combinedPct: 1 - (1-promo)*(1-save)` when ≤0.70, otherwise clips PROMO (NOT SAVE-offer) so combinedPct=0.70 exactly per D-07."
    - "70%-cap validator runs BEFORE `stripe.checkout.sessions.create` in stripe-checkout AND BEFORE `applyDiscount` in cancellation-accept-offer; both fail with HTTP 400 `{error: 'discount_combination_exceeds_max'}` when promo would push past 70%."
    - "Trial extension via `applyExtendedTrial(subId, 7, stripe)` is invoked from `cancellation-accept-offer` (where `subscription_id` is known) and gated by INSERT into `promo_trial_extensions_log` PK (subscription_id, promo_code_id); a second call with the same pair is a no-op (idempotent per D-08). Stripe-checkout-side promo-driven trial extension at new-subscription creation is DEFERRED to 43-CARRY-OVER.md."
    - "If `stripe_price_lookup.stripe_price_id` is empty for the requested plan, stripe-checkout returns HTTP 503 `{error: 'vendor_unconfigured'}` (vendor-gated-send fallback) WITHOUT calling Stripe."
  artifacts:
    - path: "supabase/migrations/20270715000007_p43_promo_trial_extensions_log.sql"
      provides: "Service-role-only idempotency log for promo-code-driven trial extensions"
      contains: "create table if not exists public.promo_trial_extensions_log"
    - path: "supabase/functions/_shared/clamp-combined-discount.ts"
      provides: "Pure-function clamp algorithm consumed by stripe-checkout + cancellation-accept-offer"
      exports: ["clampCombinedDiscount"]
    - path: "supabase/functions/stripe-checkout/index.ts"
      provides: "Lifetime branch + grandfathered resolver wiring + 70%-cap pre-validator"
      contains: "plan === 'lifetime'"
    - path: "supabase/functions/cancellation-accept-offer/index.ts"
      provides: "70%-cap clamp injected BEFORE applyDiscount + idempotent trial-extension via promo_trial_extensions_log"
      contains: "clampCombinedDiscount"
  key_links:
    - from: "stripe-checkout/index.ts"
      to: "public.resolve_user_effective_price"
      via: "admin.rpc('resolve_user_effective_price', { p_user_id, p_plan })"
      pattern: "resolve_user_effective_price"
    - from: "stripe-checkout/index.ts"
      to: "_shared/clamp-combined-discount.ts"
      via: "import { clampCombinedDiscount } from '../_shared/clamp-combined-discount.ts'"
      pattern: "clampCombinedDiscount"
    - from: "cancellation-accept-offer/index.ts (promo+trial branch)"
      to: "public.promo_trial_extensions_log"
      via: "admin.from('promo_trial_extensions_log').insert(...).onConflict('subscription_id,promo_code_id')"
      pattern: "promo_trial_extensions_log"
    - from: "cancellation-accept-offer/index.ts"
      to: "_shared/clamp-combined-discount.ts"
      via: "import + call before applyDiscount"
      pattern: "clampCombinedDiscount"
---

<objective>
Wire the server-side checkout layer to honor (a) the Lifetime payment mode, (b) the grandfathered-price resolver at NEW stripe-checkout, (c) the 70%-cap multiplicative-stack validator, and (d) the trial-extension idempotency log at the cancellation-flow surface. Also inject the same cap clamp into the cancellation-accept-offer path (so SAVE-offer stacking on a subscription with an existing promo coupon respects the cap).

Per 43-CARRY-OVER.md: stripe-checkout-side promo-driven 7-day trial extension at new-subscription creation is DEFERRED; v1.3 ships the cancellation-flow-only trial extension where subscription_id IS known and the promo_trial_extensions_log PK (subscription_id, promo_code_id) is honored at write time. Existing-subscriber grandfathered renewal-time price update is also DEFERRED — v1.3 ships grandfathered routing at NEW stripe-checkout only.

Purpose: MEMBER-01 D-02 (lifetime checkout init), MEMBER-02 D-03/D-04 (grandfathered resolver wired at NEW stripe-checkout per v1.3 scope), MEMBER-03 D-06/D-07/D-08/D-09 (multiplicative stack + cap + cancellation-flow trial extension idempotency + Stripe Coupons).

Output:
- 1 migration (`20270715000007` — last of the Phase 43 window)
- 1 new shared utility module + test
- Extensions to 2 existing Edge Fns (stripe-checkout + cancellation-accept-offer) + 1 new test file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-m4-membership-tiers-extension/43-CONTEXT.md
@.planning/phases/43-m4-membership-tiers-extension/43-RESEARCH.md
@.planning/phases/43-m4-membership-tiers-extension/43-PATTERNS.md
@.planning/phases/43-m4-membership-tiers-extension/43-CARRY-OVER.md
@.planning/phases/43-m4-membership-tiers-extension/43-01-SUMMARY.md
@.planning/phases/43-m4-membership-tiers-extension/43-02-SUMMARY.md
@.planning/phases/43-m4-membership-tiers-extension/43-03-SUMMARY.md

# Edge Fns being extended
@supabase/functions/stripe-checkout/index.ts
@supabase/functions/cancellation-accept-offer/index.ts
@supabase/functions/cancellation-accept-offer/apply-discount.ts
@supabase/functions/cancellation-accept-offer/apply-extended-trial.ts

<interfaces>
<!-- This plan consumes outputs from 43-01/02/03 (Wave 1).
     File overlap: stripe-checkout/index.ts and cancellation-accept-offer/index.ts are wholly owned by this plan — no Wave 1 plan touches them. -->

From 43-03 (Plan 03) — public.resolve_user_effective_price(p_user_id uuid, p_plan text) returns text:
  - SECDEF + STABLE; grant execute to authenticated
  - Returns: grandfathered stripe_price_id (if user matches an active cohort window) || default from stripe_price_lookup || NULL
  - Callable from Edge Fn via `admin.rpc('resolve_user_effective_price', { p_user_id, p_plan })`

From existing supabase/functions/stripe-checkout/index.ts (lines 60-99 env helpers, 380-450 plan branch):
  - getStripeSecretKey, getPricePlusMonthly, getPricePlusYearly, getPriceClinicBase, getPriceClinicOverage — all `env('STRIPE_PRICE_...')` style
  - Stripe SDK import: `import Stripe from 'https://esm.sh/stripe@19?target=denonext';`
  - apiVersion: `'2026-04-22.dahlia'` (DO NOT bump)
  - createClient pattern for admin client (service-role): `createClient(getSupabaseUrl(), getSupabaseServiceRoleKey(), { auth: { autoRefreshToken: false, persistSession: false } })`
  - Existing plan branches: 'plus_monthly' / 'plus_yearly' / 'clinic' → mode:'subscription'
  - jsonError(status, code) helper used for 400/401 responses (existing)

From existing supabase/functions/cancellation-accept-offer/apply-discount.ts:
  - export async function applyDiscount(subscriptionId: string, couponId: string, stripe: Stripe): Promise<{appliedCouponId: string}>
  - Internally uses stripe.subscriptions.retrieve + discounts[] append (no cap math)

From existing supabase/functions/cancellation-accept-offer/apply-extended-trial.ts:
  - export async function applyExtendedTrial(subscriptionId: string, extensionDays: 7|14|30, stripe: Stripe): Promise<{newTrialEnd, nextInvoiceDate}>
  - Uses subscriptions.update({trial_end, proration_behavior:'none'}); only-forward (Pitfall handled internally)

Math contract (per RESEARCH Pattern 5 + D-07):
  - combinedPct = 1 - (1 - promoPct) * (1 - saveOfferPct)
  - If combinedPct > 0.70: clip PROMO (NOT save-offer). Final equation: 0.70 = 1 - (1 - clippedPromo)(1 - saveOfferPct)
  - → clippedPromo = 1 - 0.30 / (1 - saveOfferPct)
  - SAVE-offer always preserved at original value (D-07).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 07 + shared clamp utility (with tests)</name>
  <files>supabase/migrations/20270715000007_p43_promo_trial_extensions_log.sql, supabase/functions/_shared/clamp-combined-discount.ts, supabase/functions/_shared/clamp-combined-discount.test.ts</files>
  <action>Two files in one task — both are pure infrastructure for Tasks 2/3.

A) Migration `20270715000007_p43_promo_trial_extensions_log.sql`:
  Header comment names "Phase 43 Plan 04 — MEMBER-03 D-08 idempotency log for promo-code-driven trial extensions (cancellation-flow surface; stripe-checkout-side extension deferred per 43-CARRY-OVER.md)".
  Create table:
    `create table if not exists public.promo_trial_extensions_log (subscription_id text not null, promo_code_id text not null, extension_days integer not null check (extension_days > 0), applied_at timestamptz not null default now(), primary key (subscription_id, promo_code_id));`
  Enable RLS. NO read or write policies — service-role only. Add comment 'P43 MEMBER-03 D-08: idempotency log. PK (subscription_id, promo_code_id) prevents double-extension on retry. Written by cancellation-accept-offer where subscription_id is known. Service-role only.' Add `comment on column ...` for each field.

B) `supabase/functions/_shared/clamp-combined-discount.ts`:
  Module exports a single pure function:
  ```ts
  export interface ClampResult { combinedPct: number; clipped: boolean; finalPromoPct: number; finalSavePct: number; }
  export function clampCombinedDiscount(promoPct: number, saveOfferPct: number): ClampResult { ... }
  ```
  Implementation rules:
  - Validate inputs: both args must be finite, ≥0, ≤1 (else throw `Error('clamp:invalid_input')`).
  - Compute `naive = 1 - (1 - promoPct) * (1 - saveOfferPct)`.
  - If `naive <= 0.70 + 1e-9`: return `{ combinedPct: naive, clipped: false, finalPromoPct: promoPct, finalSavePct: saveOfferPct }`.
  - Else: SAVE-offer preserved. Compute `clippedPromo = 1 - 0.30 / (1 - saveOfferPct)` (when saveOfferPct < 1).
    - If saveOfferPct >= 1 (already 100% off via SAVE): return promo=0, save=saveOfferPct, combined=saveOfferPct, clipped=true (saveOfferPct already ≥ 0.70).
    - Else: return `{ combinedPct: 0.70, clipped: true, finalPromoPct: clippedPromo, finalSavePct: saveOfferPct }`.
  - Add docstring naming P43 D-06 multiplicative + D-07 70% cap + clip-direction note.

C) `supabase/functions/_shared/clamp-combined-discount.test.ts` (Deno test):
  - `Deno.test('passthrough when below cap')` — promo=0.30, save=0.25 → combinedPct ≈ 0.475, clipped=false.
  - `Deno.test('exactly at cap')` — promo=0.50, save=0.40 → naive=0.70, clipped=false (boundary inclusive).
  - `Deno.test('clips promo when above cap, preserves save')` — promo=0.50, save=0.50 → naive=0.75, clipped=true, finalSavePct=0.50, finalPromoPct ≈ 0.40 (since 0.30/0.50=0.60 → clipped=1-0.60=0.40), combinedPct=0.70.
  - `Deno.test('100% save → promo clipped to 0')` — promo=0.30, save=1.0 → save preserved at 1.0, promo=0, combined=1.0, clipped=true.
  - `Deno.test('invalid inputs throw')` — promo=-0.1 throws; promo=1.1 throws; promo=NaN throws.
  - `Deno.test('high-priority example from RESEARCH Pitfall 7')` — promo=0.50, save=0.30 → naive=0.65, clipped=false, combinedPct=0.65.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -n "create table if not exists public.promo_trial_extensions_log" supabase/migrations/20270715000007_p43_promo_trial_extensions_log.sql && grep -n "primary key (subscription_id, promo_code_id)" supabase/migrations/20270715000007_p43_promo_trial_extensions_log.sql && grep -n "export function clampCombinedDiscount" supabase/functions/_shared/clamp-combined-discount.ts && grep -n "1 - 0.30 / (1 - saveOfferPct)" supabase/functions/_shared/clamp-combined-discount.ts && $HOME/.deno/bin/deno test --allow-all --no-check supabase/functions/_shared/clamp-combined-discount.test.ts</automated>
  </verify>
  <done>Migration ships with composite PK; clamp utility exports pure function; 6 Deno test cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stripe-checkout lifetime branch + grandfathered resolver + 70%-cap</name>
  <files>supabase/functions/stripe-checkout/index.ts, supabase/functions/stripe-checkout/index.test.ts</files>
  <behavior>
    - Test 1: `plan='lifetime'` + no promo_code → returns 200 with sessionUrl; mocked Stripe receives `mode='payment'`, `payment_intent_data.metadata.tier_kind='lifetime'`, `payment_intent_data.metadata.user_id`, line_items[0].price = resolveResult ('price_LIFETIME_DEFAULT' from stub rpc).
    - Test 2: `plan='lifetime'` + promo_code='WELCOMEBACK' → HTTP 400 `{error: 'lifetime_no_promo_code'}` (OQ-6 RESOLVED).
    - Test 3: `plan='plus_monthly'` → admin.rpc('resolve_user_effective_price', { p_user_id, p_plan: 'plus_monthly' }) is called; line_items[0].price = resolveResult ('price_GRANDFATHERED_X' from stub).
    - Test 4: `plan='plus_monthly'` + promo_code + simulated save-offer percent (passed via request body OR resolved from save_offer_rules — for this test, stub the lookup to return 0.50 save + the promo coupon resolves to 0.50 promo) → `clampCombinedDiscount(0.50, 0.50)` clips promo; Edge Fn returns 400 `discount_combination_exceeds_max` (D-07 strict reading; fail-fast).
    - Test 5: stripe_price_lookup returns empty string → HTTP 503 `{error: 'vendor_unconfigured'}` (vendor-gated-send).
    - Test 6: existing 'clinic' branch unchanged (regression guard).
  </behavior>
  <action>Extend `supabase/functions/stripe-checkout/index.ts`:

(a) Imports: add `import { clampCombinedDiscount } from '../_shared/clamp-combined-discount.ts';`. Add helper `const getPriceLifetime = () => env('STRIPE_PRICE_LIFETIME');` for the env-var fallback (read AFTER resolve_user_effective_price returns null).

(b) Before the existing plan-branch (around line 380), add a new branch `if (plan === 'lifetime') { ... } else if (plan === 'plus_monthly') ...`. The lifetime branch:
  - Reject promo: `if (body.promo_code) return jsonError(400, 'lifetime_no_promo_code');`
  - Resolve price via `const { data: resolved } = await admin.rpc('resolve_user_effective_price', { p_user_id: user.id, p_plan: 'lifetime' }); const priceId = (resolved as string | null) ?? getPriceLifetime();`
  - If `!priceId` → `return jsonError(503, 'vendor_unconfigured');`
  - Set `lineItems = [{ price: priceId, quantity: 1 }];`
  - Build session with `mode: 'payment'`, `payment_intent_data: { metadata: { user_id: user.id, provider: 'stripe', tier_kind: 'lifetime' } }`, `metadata: { user_id: user.id, tier_kind: 'lifetime' }`, no `subscription_data`, `client_reference_id: user.id`.

(c) For the existing 'plus_monthly' / 'plus_yearly' branches: replace the static `getPricePlusMonthly()` lookup with `const resolved = await admin.rpc('resolve_user_effective_price', { p_user_id: user.id, p_plan }); const priceId = (resolved.data as string | null) ?? getPricePlus<Plan>();`. Same vendor-gated-send 503 fallback if both null. Clinic branch unchanged for now (clinic grandfathering deferred per CONTEXT specifics). v1.3 scope is NEW stripe-checkout only — existing-subscriber renewal-time price update is DEFERRED per 43-CARRY-OVER.md.

(d) Promo + save-offer cap clamp (for subscription plans only — NOT lifetime since lifetime rejects promo at step b):
  - When `body.promo_code` is set: look up the promo coupon's percent_off via `stripeInstance.coupons.retrieve(body.promo_code)` → `promoPct = (coupon.percent_off ?? 0) / 100`.
  - Look up any active SAVE-offer percent for this user (via `admin.from('cancellation_offers_log').select('offer_payload').eq('user_id', user.id).eq('status','accepted').order('decided_at', { ascending: false }).limit(1)` — if a recent accepted SAVE-offer exists with offer_type='discount', read its percent_off; else 0).
  - Call `const clamp = clampCombinedDiscount(promoPct, savePct);`. If `clamp.clipped` → `return jsonError(400, 'discount_combination_exceeds_max');` (D-07 strict reading: fail-fast user-visible error per Pitfall 7).
  - Else pass-through: include `discounts: [{ coupon: body.promo_code }]` in sessions.create.

(e) Trial extension via promo codes at new-subscription creation is OUT OF SCOPE for v1.3 — DEFERRED per 43-CARRY-OVER.md. The `promo_trial_extensions_log` PK (subscription_id, promo_code_id) cannot be satisfied here because subscription_id does not exist at sessions.create time. The cancellation-accept-offer path (Task 3) is the v1.3 idempotency-protected surface where subscription_id IS known. Do NOT add any `subscription_data.trial_period_days` promo-driven extension or `promo_trial_extensions_log` insert in this Edge Fn. Stripe coupon `metadata.extension_days` may exist on coupons (admin-set) but stripe-checkout in v1.3 IGNORES it.

(f) Add new test file `index.test.ts` (Deno) covering the 6 behaviors above. Use mocked `stripeInstance` (captured calls) + `__setAdminForTest(fakeAdmin)` + Deno.env stubs.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -n "plan === 'lifetime'" supabase/functions/stripe-checkout/index.ts && grep -n "lifetime_no_promo_code" supabase/functions/stripe-checkout/index.ts && grep -n "resolve_user_effective_price" supabase/functions/stripe-checkout/index.ts && grep -n "clampCombinedDiscount" supabase/functions/stripe-checkout/index.ts && grep -n "vendor_unconfigured" supabase/functions/stripe-checkout/index.ts && grep -n "discount_combination_exceeds_max" supabase/functions/stripe-checkout/index.ts && grep -n "mode: 'payment'" supabase/functions/stripe-checkout/index.ts && grep -n "tier_kind: 'lifetime'" supabase/functions/stripe-checkout/index.ts && [ "$(grep -c 'promo_trial_extensions_log' supabase/functions/stripe-checkout/index.ts)" = "0" ] && $HOME/.deno/bin/deno test --allow-all --no-check supabase/functions/stripe-checkout/index.test.ts</automated>
  </verify>
  <done>All 6 Deno tests pass; lifetime branch exists; grandfathered resolver wired for plus_monthly/plus_yearly; 70%-cap fails fast on overflow; vendor-gated 503 when stripe_price_lookup empty; clinic branch unchanged; NO promo_trial_extensions_log reference in stripe-checkout (deferred to cancellation-accept-offer per 43-CARRY-OVER.md).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cancellation-accept-offer 70%-cap injection BEFORE applyDiscount + trial-extension idempotency</name>
  <files>supabase/functions/cancellation-accept-offer/index.ts</files>
  <behavior>
    - Test 1: subscription with existing 30% promo coupon + accepting SAVE-offer of 25% → naive 47.5% ≤ 70% → applyDiscount called with the SAVE coupon (no clip).
    - Test 2: subscription with existing 50% promo coupon + accepting SAVE-offer of 50% → naive 75% > 70% → handler returns 400 `{error: 'discount_combination_exceeds_max'}` and applyDiscount is NOT called.
    - Test 3: subscription with no existing promo + accepting SAVE-offer of 35% → applyDiscount called normally (no clamp invocation needed).
    - Test 4: clamp invocation runs BEFORE applyDiscount (assertion on mock-function call order).
    - Test 5 (D-08 idempotency): accepting a SAVE-offer with `extension_days=7` for the FIRST time → applyExtendedTrial called + promo_trial_extensions_log INSERT succeeds; SECOND call with the same (subscription_id, promo_code_id) → INSERT no-ops via PK conflict + applyExtendedTrial is NOT called again.
  </behavior>
  <action>Edit `supabase/functions/cancellation-accept-offer/index.ts`:
  - Add import: `import { clampCombinedDiscount } from '../_shared/clamp-combined-discount.ts';`
  - At the call site of `applyDiscount`, prepend a clamp computation block:
    1. Retrieve current subscription via `stripe.subscriptions.retrieve(subscriptionId, { expand: ['discounts'] })`.
    2. From `sub.discounts ?? []`, resolve each coupon's percent_off (call `stripe.coupons.retrieve` for each unique id, OR pull from the existing coupon-cache if one exists). Sum-by-multiplicative: `existingPromoPct = 1 - product((1 - c.percent_off/100))`.
    3. Compute `newSaveOfferPct = offerCoupon.percent_off / 100` (the SAVE-offer being accepted; coupon id is in the decide-offer payload).
    4. Call `clampCombinedDiscount(existingPromoPct, newSaveOfferPct)`. Per D-07 the first arg is the clippable side; existing promo (lower priority) gets clipped. Add a comment block in the source file documenting this convention (e.g., `// D-07: clampCombinedDiscount(clippable, preserved). Existing promo is lower priority → clippable. New SAVE-offer is preserved.`).
    5. If `clamp.clipped` AND existing promo cannot be modified safely (Stripe doesn't allow per-coupon percent override mid-sub) → return jsonError(400, 'discount_combination_exceeds_max').
    6. Otherwise proceed to existing `applyDiscount` call.
  - Trial extension idempotency (D-08 — cancellation-flow surface, subscription_id IS known):
    - When the accepted offer payload carries `extension_days` (mirror of the Stripe coupon `metadata.extension_days`): BEFORE calling `applyExtendedTrial`, attempt `admin.from('promo_trial_extensions_log').insert({ subscription_id, promo_code_id: offerCoupon.id, extension_days }, { onConflict: 'subscription_id,promo_code_id', ignoreDuplicates: true })`. Read `count` / `data` to detect duplicate-suppression. If the INSERT was a fresh row → call `applyExtendedTrial(subscription_id, extension_days, stripe)`. If the INSERT collided on PK → SKIP the Stripe call (idempotent no-op).
    - Surface idempotency outcome in the response body as `{ trial_extended: boolean }` for the caller.
  - Inline `<behavior>` test assertions: extend the existing test file (or create `index.test.ts` if absent) using stubbed `stripe` (returns canned `subscriptions.retrieve` + `coupons.retrieve` responses) + stubbed admin client.
  - Do NOT modify `applyDiscount.ts` or `applyExtendedTrial.ts` — those stay verbatim per RESEARCH Pattern 4/6 reuse mandate.</action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -n "clampCombinedDiscount" supabase/functions/cancellation-accept-offer/index.ts && grep -n "discount_combination_exceeds_max" supabase/functions/cancellation-accept-offer/index.ts && grep -n "applyDiscount" supabase/functions/cancellation-accept-offer/index.ts && grep -n "promo_trial_extensions_log" supabase/functions/cancellation-accept-offer/index.ts && [ "$(awk '/clampCombinedDiscount/{c=NR} /applyDiscount[(]/{d=NR} END{print (c < d) ? "OK" : "FAIL"}' supabase/functions/cancellation-accept-offer/index.ts)" = "OK" ] && $HOME/.deno/bin/deno test --allow-all --no-check supabase/functions/cancellation-accept-offer/index.test.ts</automated>
  </verify>
  <done>cancellation-accept-offer/index.ts imports clampCombinedDiscount; clamp call appears textually BEFORE applyDiscount call in the source; promo_trial_extensions_log INSERT gates applyExtendedTrial; 5 Deno tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → stripe-checkout Edge Fn | JWT bearer; user.id from `supabase.auth.getUser()`; user-supplied `plan` + `promo_code` validated server-side |
| stripe-checkout → resolve_user_effective_price (Postgres) | Service-role admin client; SECDEF function honors search_path; result is just text price_id |
| stripe-checkout → Stripe API | HTTPS + secret key; mode field is server-controlled, NOT user-controlled |
| cancellation-accept-offer → Stripe Coupons API | Read-only retrieval to compute cap; idempotent; failures degrade to safe-deny (400) |
| cancellation-accept-offer → promo_trial_extensions_log | Service-role INSERT; PK (subscription_id, promo_code_id) prevents replay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04-01 | Elevation of Privilege | user passes `plan: 'lifetime'` + `promo_code` to chain a 100%-off lifetime purchase | mitigate | Lifetime branch rejects ANY promo_code with `lifetime_no_promo_code` 400 BEFORE Stripe call (per OQ-6 RESOLVED). Tested in Test 2. |
| T-43-04-02 | Tampering | user passes a forged stripe_price_id directly in request body | mitigate | Request body has NO price_id field — Edge Fn always resolves price via server-side `resolve_user_effective_price` + env helper. User input limited to `plan` enum + optional `promo_code` text. |
| T-43-04-03 | Tampering | user stacks promo + SAVE-offer for >70% off | mitigate | `clampCombinedDiscount` runs BEFORE `stripe.checkout.sessions.create` and BEFORE `applyDiscount`; 400 fail-fast on overflow. Multiplicative math verified by 6 unit tests (Task 1). |
| T-43-04-04 | EoP / Tampering | user replays trial-extension promo code at cancellation-accept-offer to chain multiple 7-day extensions | mitigate | `promo_trial_extensions_log` PK (subscription_id, promo_code_id) gates the Stripe call; `INSERT ... ON CONFLICT DO NOTHING` returns no-op for retries. v1.3 scope: cancellation-flow only. Stripe-checkout-side extension deferred per 43-CARRY-OVER.md. |
| T-43-04-05 | Denial of Service | Stripe coupon retrieve calls inflate per-request latency | accept | Per-request latency increase bounded (~1 round-trip per coupon × ≤2 coupons typically); fail-open on Stripe error returns 503 (vendor_unconfigured). |
| T-43-04-06 | Information Disclosure | Stripe API error message echoed to client | mitigate | jsonError helper returns only short error codes (e.g., 'lifetime_no_promo_code'); Stripe error details logged via `console.error('[stripe-checkout]', ...)` only, never in response body. |
| T-43-04-07 | EoP | clamp's `clippedPromo` math fails for save=100% edge case | mitigate | Unit test 4 in Task 1 explicitly covers save=1.0 (division by zero guard); function returns promo=0 + save=1.0 + combined=1.0 + clipped=true without throwing. |
| T-43-04-08 | Repudiation | user disputes "I never accepted the SAVE-offer with stacking" | mitigate | cancellation_offers_log (P40) records every accepted offer; combined with audit_log of payment intents, the full chain is reconstructable. |
</threat_model>

<verification>
- Migration 07 filename regex compliant; PK on (subscription_id, promo_code_id).
- _shared utility tests green (6 cases).
- stripe-checkout tests green (6 cases).
- cancellation-accept-offer tests green (5 cases including D-08 idempotency).
- Clamp call appears textually BEFORE applyDiscount call in cancellation-accept-offer/index.ts (awk-regex check in verify).
- stripe-checkout has zero references to promo_trial_extensions_log (deferred per 43-CARRY-OVER.md).
- Existing 'clinic' branch in stripe-checkout untouched (verified by git diff review at commit time).
</verification>

<success_criteria>
- 1 migration + 1 utility module + 2 Edge Fn extensions + 3 test files shipped
- Lifetime checkout uses mode='payment' (NEVER subscription) per Pitfall 10
- 70%-cap math is multiplicative, clips promo (not save), fails fast at 400 boundary
- Cancellation-accept-offer trial extension is idempotent via promo_trial_extensions_log PK
- Vendor-gated-send 503 when stripe_price_lookup unconfigured
- Stripe-checkout-side trial extension at new-sub creation deferred per 43-CARRY-OVER.md (NOT shipped)
- Existing-subscriber grandfathered renewal-time update deferred per 43-CARRY-OVER.md (v1.3 = NEW stripe-checkout only)
- No file outside `files_modified` touched
</success_criteria>

<output>
After completion, create `.planning/phases/43-m4-membership-tiers-extension/43-04-SUMMARY.md` covering:
- All files modified + line counts
- 6 + 5 + 6 = 17 unit tests passing (Deno)
- Argument-order convention for clampCombinedDiscount: `(promoPct, saveOfferPct)` where promoPct is the clippable side; per D-07 the SAVE-offer is preserved
- v1.3 scope clarification: stripe-checkout-side promo-driven trial extension at new-subscription creation is DEFERRED per 43-CARRY-OVER.md; cancellation-accept-offer is the only idempotency-protected surface in v1.3
- v1.3 grandfathering scope: NEW stripe-checkout only; existing-subscriber renewal-time update DEFERRED per 43-CARRY-OVER.md
- Carry-over: `supabase db push --linked` + `supabase functions deploy stripe-checkout cancellation-accept-offer` deferred to closeout 43-06
</output>
