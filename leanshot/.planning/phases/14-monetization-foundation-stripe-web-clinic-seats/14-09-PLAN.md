---
phase: 14-monetization-foundation-stripe-web-clinic-seats
plan: 09
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - leanshot/src/lib/billing-sync.ts
  - leanshot/src/lib/billing-sync.test.ts
  - leanshot/src/App.tsx
  - leanshot/src/lib/billing.ts
  - leanshot/src/components/billing/PaywallUpsell.tsx
  - leanshot/src/components/billing/PaywallUpsell.test.tsx
  - leanshot/src/components/billing/UpgradeCTA.tsx
autonomous: true
requirements: [MONEY-02, MONEY-03, MONEY-04, MONEY-09]
tags: [billing, tier-sync, paywall, zustand, stripe, gap-closure]

must_haves:
  truths:
    - "On SIGNED_IN / INITIAL_SESSION for a verified non-anon user, the app queries the user's `subscriptions` row, runs `getActiveTier(status, current_period_end, new Date())`, and calls `useStore.getState().setTier(...)` — so `useStore((s) => s.tier)` reflects the real Stripe state, not the persisted-stale `'free'` default. (SC#1, SC#4)"
    - "When the browser tab regains focus, the billing tier is refetched and re-applied via `setTier(...)` so a Customer-Portal round-trip is reflected within the SC#2 10-second budget. (SC#2)"
    - "A user with no `subscriptions` row resolves to `tier='free'` (the query returns no row → `getActiveTier(null, ...)` → `'free'`) and never throws."
    - "`PaywallUpsell` opens a real Stripe Checkout session: clicking Upgrade calls `supabase.functions.invoke('stripe-checkout/session', { body: { plan } })` and assigns `window.location.href = data.url` — no bare `/functions/v1/stripe-checkout` fetch, no `credentials:'include'`. (SC#1, SC#5)"
    - "`PaywallUpsell` accepts a `plan` prop (`'plus_monthly' | 'plus_yearly' | 'clinic'`, default `'plus_monthly'`) that is forwarded as the `plan` body field to the checkout endpoint."
    - "The `Plan` string-literal union lives in ONE place — `leanshot/src/lib/billing.ts` exports `type Plan = 'plus_monthly' | 'plus_yearly' | 'clinic'` — and both `PaywallUpsell.tsx` and `UpgradeCTA.tsx` import it; no duplicated local `Plan` union remains in either component. (CR-02 DRY)"
    - "The DB→store sync path is a single named module (`leanshot/src/lib/billing-sync.ts`) — one file owns the runtime connector between the webhook-written `subscriptions` table and the Zustand `tier` slice."
    - "No `@stripe/stripe-js` static import introduced in any plan-owned file; no hex color literals introduced (Phase 13 tokens only)."
    - "Bundle index chunk stays under 50 kB gz."
  artifacts:
    - path: "leanshot/src/lib/billing-sync.ts"
      provides: "`syncBillingTier(userId)` — queries `subscriptions`, collapses via `getActiveTier`, calls `setTier`; the named DB→store connector"
      exports: ["syncBillingTier"]
    - path: "leanshot/src/lib/billing-sync.test.ts"
      provides: "Vitest coverage: paid row → tier='paid', past_due row → tier='past_due', no row → tier='free', query error → tier unchanged + logged"
    - path: "leanshot/src/App.tsx"
      provides: "Surgical: SIGNED_IN + INITIAL_SESSION auth handlers call syncBillingTier; window 'focus' listener refetches"
    - path: "leanshot/src/lib/billing.ts"
      provides: "New exported `type Plan = 'plus_monthly' | 'plus_yearly' | 'clinic'` — single source of truth for the checkout plan union"
      contains: "type Plan"
    - path: "leanshot/src/components/billing/PaywallUpsell.tsx"
      provides: "Fixed checkout call via supabase.functions.invoke('stripe-checkout/session', { body: { plan } }) + new plan prop typed via the shared `Plan` union"
    - path: "leanshot/src/components/billing/PaywallUpsell.test.tsx"
      provides: "Vitest: Upgrade click invokes stripe-checkout/session with the plan body; redirects to data.url"
    - path: "leanshot/src/components/billing/UpgradeCTA.tsx"
      provides: "Local `Plan` union deleted; imports the shared `Plan` type from `@/lib/billing` (no behavior change)"
  key_links:
    - from: "leanshot/src/App.tsx (SIGNED_IN / INITIAL_SESSION handlers)"
      to: "leanshot/src/lib/billing-sync.ts"
      via: "dynamic import + syncBillingTier(session.user.id)"
      pattern: "syncBillingTier"
    - from: "leanshot/src/lib/billing-sync.ts"
      to: "leanshot/src/lib/store.ts (setTier action)"
      via: "useStore.getState().setTier(...)"
      pattern: "setTier"
    - from: "leanshot/src/lib/billing-sync.ts"
      to: "leanshot/src/lib/billing.ts (getActiveTier)"
      via: "import { getActiveTier }"
      pattern: "getActiveTier"
    - from: "leanshot/src/components/billing/PaywallUpsell.tsx"
      to: "stripe-checkout/session Edge Function"
      via: "supabase.functions.invoke('stripe-checkout/session', { body: { plan } })"
      pattern: "stripe-checkout/session"
    - from: "leanshot/src/components/billing/PaywallUpsell.tsx + UpgradeCTA.tsx"
      to: "leanshot/src/lib/billing.ts (Plan union)"
      via: "import type { Plan } from '@/lib/billing'"
      pattern: "import.*Plan.*billing"
---

<objective>
Close **CR-01** and **CR-02** — the two gap-closure BLOCKERs that make Phase 14's monetization flow non-functional for every real user.

**CR-01 (the keystone gap):** Phase 14 built the webhook to-DB write half correctly but never built the DB to-frontend read half. `setTier()` exists in `store.ts` with zero non-test call sites. No code ever queries the `subscriptions` table and pushes tier into the Zustand store, so `useStore((s) => s.tier)` is permanently `'free'` for 100% of real users — `<TierGate>`, `<PastDueBanner>`, `<UpgradeCTA>`, `<ManageSubscriptionLink>` are all driven off a dead value. This plan creates **one named module that owns the DB to-store sync path** (`billing-sync.ts`) and wires it into the `App.tsx` auth handler plus a window-focus refetch.

**CR-02:** `PaywallUpsell.fetchCheckoutUrl()` posts to a bare `/functions/v1/stripe-checkout` path (404 `unknown_action`), with `credentials:'include'` and no JWT (401 `unauthenticated`), and no `plan` body (400 `invalid_plan`). Every free user who clicks Upgrade on the chart blur-upsell or the AI panel CTA hits a silent `console.error`. The correct calling pattern already exists in `UpgradeCTA.tsx:49-52`. This plan rewrites `PaywallUpsell` to mirror it exactly and adds a `plan` prop. It also extracts the `Plan` string-literal union (today duplicated as a local type in `UpgradeCTA.tsx`) into `leanshot/src/lib/billing.ts` as the single source of truth, widened to include `'clinic'`.

Purpose: Re-satisfy MONEY-02 (web subscribe end-to-end), MONEY-03 (Portal change reflected within 10s), MONEY-04 (TierGate functional for real users), MONEY-09 (past_due banner can actually render). Unblocks SC#1, SC#2, SC#4, SC#5.

Output: 1 net-new module plus test (`billing-sync.ts` / `.test.ts`), 2 surgical edits to `App.tsx`, 1 type export added to `billing.ts`, 1 rewrite of `PaywallUpsell.tsx` plus its test, 1 surgical import change in `UpgradeCTA.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@leanshot/CLAUDE.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-CONTEXT.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-VERIFICATION.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-REVIEW.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-03-SUMMARY.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-05-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs verbatim. NO codebase exploration required beyond <read_first>. -->

### Existing `subscriptions` table columns this plan READS (from migration 20260601000019)
```
id                   text PRIMARY KEY      -- stripe sub_xxx
user_id              uuid NULL            -- the auth.users.id (NULL for clinic rows)
clinic_id            uuid NULL
status               text NOT NULL        -- stripe status verbatim (the 8 statuses)
ux_tier              text NOT NULL        -- 'free' | 'paid' | 'past_due' (webhook-collapsed)
plan_id              text
current_period_end   timestamptz
provider             text NOT NULL        -- 'stripe' | 'revenuecat'
```
RLS (from 14-01): a user can SELECT only their own `subscriptions` row (`user_id = auth.uid()`). So `supabase.from('subscriptions').select(...).eq('user_id', userId)` returns at most 1 row for a web user.

### Existing `getActiveTier` signature (`leanshot/src/lib/billing.ts` — already shipped, do NOT modify the function)
```
getActiveTier(stripeStatus, currentPeriodEnd, now) returns Tier ('free' | 'paid' | 'past_due')
```
`stripeStatus` is the 8-value Stripe status union or `null`. `getActiveTier(null, null, now)` returns `'free'` — use this for the no-row case. NOTE: this plan ADDS a new `export type Plan = ...` to `billing.ts` (a pure type addition) but does NOT change `getActiveTier` or any other existing export.

### Existing `setTier` store action (`leanshot/src/lib/store.ts` — already shipped, do NOT modify)
```
setTier(next: Partial<BillingState> & { tier: Tier }) => void
// call as: useStore.getState().setTier({ tier, current_period_end, plan_id, provider });
```

### NEW module contract — `leanshot/src/lib/billing-sync.ts`
```
syncBillingTier(userId: string): Promise<void>
// Queries the signed-in user's subscriptions row, collapses Stripe status via
// getActiveTier, and pushes the result into the Zustand tier slice. Safe to call
// repeatedly (SIGNED_IN, INITIAL_SESSION, window focus). Never throws — a query
// error logs and leaves the existing tier untouched.
```

### NEW shared type — `leanshot/src/lib/billing.ts`
```
export type Plan = 'plus_monthly' | 'plus_yearly' | 'clinic';
// Single source of truth for the checkout `plan` body field. UpgradeCTA.tsx
// currently has a LOCAL `type Plan = 'plus_monthly' | 'plus_yearly'` — this plan
// moves that union into billing.ts, widens it with 'clinic' (PaywallUpsell needs it),
// and both components import it.
```

### Correct checkout-invoke pattern (verbatim from `UpgradeCTA.tsx:49-56` — the source of truth)
```
const { data, error: invokeErr } = await supabase.functions.invoke(
  'stripe-checkout/session',
  { body: { plan: PLAN_MAP[planTag] } },
);
if (invokeErr || !data?.url) { throw new Error('no-url'); }
window.location.href = data.url;
```

### `supabase` client import
```
import { supabase } from '@/lib/supabase';   // singleton — already used across src/
```
</interfaces>

<invariants>
- **CR-01 ownership:** `billing-sync.ts` is the ONE file that owns the runtime connector between the `subscriptions` table and the Zustand store. Do NOT inline the query into `App.tsx` — keep the seam in a named, testable module. `App.tsx` only *calls* `syncBillingTier`.
- **Selector convention (CLAUDE.md):** never read the store via `useStore(s => s)`. `billing-sync.ts` writes via `useStore.getState().setTier(...)` (one-shot write outside render — same pattern as `useToast`). It does NOT subscribe.
- **Dynamic import in App.tsx:** mirror the existing Phase 6 D-12 `deferOnSignedIn` pattern — `billing-sync.ts` is imported via `await import('@/lib/billing-sync')` (or `import('@/lib/billing-sync').then(...)`) inside the auth handler so it stays off `App.tsx`'s static graph (bundle discipline). The `focus` listener also reaches `billing-sync.ts` via dynamic import. `App.tsx`'s top-level static import block MUST NOT gain a `billing-sync` line.
- **No-row is not an error:** `supabase.from('subscriptions').select().eq('user_id', userId).maybeSingle()` returns `{ data: null, error: null }` when there's no row. That path MUST call `setTier({ tier: 'free', current_period_end: null, plan_id: null, provider: null })` (equivalently run `getActiveTier(null, null, new Date())`), NOT skip the write — a previously-paid user who fully cancelled must drop to `'free'`.
- **Query error path:** if `error` is non-null, `console.error('[billing-sync]', error.message)` and RETURN without calling `setTier` — leave the persisted value as the best available guess (CONTEXT §6: stale tier offline is acceptable). Never throw out of `syncBillingTier`.
- **status cast:** `subscriptions.status` is `text` in Postgres. Narrow it to the `getActiveTier` status union before the call; an unrecognized string falls through `getActiveTier`'s `default` to `'free'`, which is the safe paywall default.
- **`Plan` union DRY (CR-02):** `UpgradeCTA.tsx` today defines `type Plan = 'plus_monthly' | 'plus_yearly'` locally. This plan extracts that union into `leanshot/src/lib/billing.ts` as `export type Plan = 'plus_monthly' | 'plus_yearly' | 'clinic'` (widened with `'clinic'` because `PaywallUpsell` needs it), then changes BOTH `UpgradeCTA.tsx` and `PaywallUpsell.tsx` to `import type { Plan } from '@/lib/billing'`. The local `type Plan = ...` line in `UpgradeCTA.tsx` MUST be deleted. `UpgradeCTA`'s runtime behavior is otherwise UNCHANGED — `PLAN_MAP` and every other line stay as-is; widening the union does not break `UpgradeCTA` because it never produces a `'clinic'` value.
- **Pattern G (Stripe.js bundle isolation):** ZERO `@stripe/stripe-js` imports in any plan-owned file. `PaywallUpsell` redirect stays a plain `window.location.href = data.url`.
- **Pattern D (design tokens):** no hex literals introduced. `PaywallUpsell` already uses `var(--color-*)` tokens — preserve them; the rewrite only touches the fetch helper plus props, not the JSX styling.
- **Pathspec discipline (Pattern F):** Wave 1 runs parallel with plan 14-10 (file-disjoint: 14-10 only touches `supabase/functions/**`). Commit with an explicit pathspec — `git commit -- leanshot/src/lib/billing-sync.ts leanshot/src/lib/billing-sync.test.ts leanshot/src/App.tsx leanshot/src/lib/billing.ts leanshot/src/components/billing/PaywallUpsell.tsx leanshot/src/components/billing/PaywallUpsell.test.tsx leanshot/src/components/billing/UpgradeCTA.tsx`. Never `git add -A`.
- **PaywallUpsell consumers unchanged:** `MedLevelChart` (blur-upsell `variant="overlay"`) and `AIChatPanel` (`variant="cta"`) render `<PaywallUpsell>` WITHOUT a `plan` prop today. The new `plan` prop MUST default to `'plus_monthly'` so those existing call sites compile and behave correctly with zero edits to those files.
</invariants>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: billing-sync.ts — the named DB-to-store connector + Vitest coverage</name>
  <files>
    leanshot/src/lib/billing-sync.ts,
    leanshot/src/lib/billing-sync.test.ts
  </files>
  <read_first>
    - leanshot/src/lib/billing.ts (the `getActiveTier` signature + collapse rules this module consumes)
    - leanshot/src/lib/store.ts (the `setTier` action signature + `BillingState` shape)
    - leanshot/src/lib/clinic-permissions.ts (the analog session-scoped fetch module — JSDoc-heavy header, `supabase` import, error-coerces-to-safe-default posture)
    - leanshot/src/lib/supabase.ts (the `supabase` singleton client export)
    - leanshot/src/lib/billing.test.ts (Vitest conventions for billing-area tests; mock style)
    - supabase/migrations/20260601000019_stripe_subscriptions.sql (the `subscriptions` columns + RLS this query relies on)
  </read_first>
  <behavior>
    - `syncBillingTier(userId)` with a `subscriptions` row `{ status: 'active', current_period_end: <future ISO>, plan_id: 'price_x', provider: 'stripe' }` calls `useStore.getState().setTier({ tier: 'paid', current_period_end: <future ISO>, plan_id: 'price_x', provider: 'stripe' })`.
    - `syncBillingTier(userId)` with a row `{ status: 'past_due', ... }` calls `setTier` with `tier: 'past_due'`.
    - `syncBillingTier(userId)` with a row `{ status: 'canceled', current_period_end: <past ISO> }` calls `setTier` with `tier: 'free'`.
    - `syncBillingTier(userId)` when the query returns `{ data: null, error: null }` (no row) calls `setTier` with `tier: 'free', current_period_end: null, plan_id: null, provider: null`.
    - `syncBillingTier(userId)` when the query returns `{ data: null, error: { message: 'boom' } }` does NOT call `setTier`; calls `console.error` once; resolves without throwing.
    - `syncBillingTier` never rejects — every path resolves.
  </behavior>
  <action>
    Create `leanshot/src/lib/billing-sync.ts` modeled on the `clinic-permissions.ts` header/JSDoc style.
    - Module JSDoc: cite CR-01 (the verification gap this closes) and state plainly "this is the single named DB-to-store connector for Phase 14 billing tier".
    - Import `supabase` from `@/lib/supabase`, `getActiveTier` from `@/lib/billing`, `useStore` from `@/lib/store`. Import the `SubscriptionProvider` / status types from `@/types` and `@/lib/billing` as needed.
    - Implement `export async function syncBillingTier(userId: string): Promise<void>`:
      1. `const { data, error } = await supabase.from('subscriptions').select('status, current_period_end, plan_id, provider').eq('user_id', userId).maybeSingle();`
      2. If `error` then `console.error('[billing-sync] subscriptions query failed', error.message)` and `return` (leave tier untouched).
      3. Narrow `data?.status` to the `getActiveTier` status union (a small cast/helper is fine — unrecognized values fall through to `'free'` in `getActiveTier`).
      4. `const tier = getActiveTier(status ?? null, data?.current_period_end ?? null, new Date());`
      5. `useStore.getState().setTier({ tier, current_period_end: data?.current_period_end ?? null, plan_id: data?.plan_id ?? null, provider: (data?.provider as SubscriptionProvider) ?? null });`
    - Keep it dependency-light — no React, no hooks; this is a plain async function callable from event handlers (mirrors `useToast`'s `getState()` write pattern).

    Create `leanshot/src/lib/billing-sync.test.ts`:
    - Vitest. `vi.mock('@/lib/supabase', ...)` to stub `supabase.from(...).select(...).eq(...).maybeSingle()` returning the desired `{ data, error }` per test.
    - `vi.mock('@/lib/store', ...)` to expose `useStore.getState` returning an object with a `setTier: vi.fn()` so each test can assert the exact `setTier` argument.
    - Cover the 6 `<behavior>` cases above. Use deterministic ISO timestamps (future = `2099-01-01T00:00:00Z`, past = `2000-01-01T00:00:00Z`) so `getActiveTier`'s `canceled` branch is deterministic.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx vitest run src/lib/billing-sync.test.ts --reporter=verbose && npx tsc -b --noEmit && npm run lint -- src/lib/billing-sync.ts src/lib/billing-sync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `leanshot/src/lib/billing-sync.ts` exists and exports `syncBillingTier`.
    - `grep -c "getActiveTier" leanshot/src/lib/billing-sync.ts` is at least 1 AND `grep -c "setTier" leanshot/src/lib/billing-sync.ts` is at least 1 — the module wires both ends of the connector.
    - `billing-sync.test.ts` runs at least 6 cases, all green; the no-row case asserts `setTier` called with `tier: 'free'`; the query-error case asserts `setTier` NOT called and `console.error` called once.
    - `npx tsc -b --noEmit` clean; `npm run lint` clean on both files.
    - `! grep -nE "from ['\"]@stripe/stripe-js['\"]" leanshot/src/lib/billing-sync.ts` (zero matches).
  </acceptance_criteria>
  <done>
    `billing-sync.ts` is the named, tested DB-to-store connector: queries `subscriptions`, collapses via `getActiveTier`, writes via `setTier`; no-row maps to `'free'`, query-error maps to no write plus log, never throws. At least 6 green Vitest cases. tsc + eslint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire syncBillingTier into App.tsx auth handlers + window-focus refetch</name>
  <files>
    leanshot/src/App.tsx
  </files>
  <read_first>
    - leanshot/src/App.tsx (lines 320-440 — the `onAuthStateChange` switch: INITIAL_SESSION ~356-377, SIGNED_IN ~378-391, SIGNED_OUT ~392-420; note the existing `deferOnSignedIn` dynamic-import-style pattern and the `email_confirmed_at && !is_anonymous` guard)
    - leanshot/src/lib/billing-sync.ts (the `syncBillingTier` contract created in Task 1)
    - leanshot/src/lib/store.ts (confirm `clearUserDataSlices` already resets the 4 billing fields — it does, per 14-05; SIGNED_OUT needs NO new billing code)
  </read_first>
  <action>
    Surgical edits to `leanshot/src/App.tsx` — the `onAuthStateChange` handler only. Do NOT touch the SIGNED_OUT branch (CR-01 is a *setup* gap; teardown via `clearUserDataSlices` already works per 14-05).

    Step 1 — INITIAL_SESSION branch (~line 356-377): inside the existing `if (session?.user && !session.user.is_anonymous && session.user.email_confirmed_at)` block, AFTER the existing `deferOnSignedIn(session.user.id, session)` call, add a fire-and-forget billing sync:
    `void import('@/lib/billing-sync').then(({ syncBillingTier }) => syncBillingTier(session.user.id));`
    (Dynamic import keeps `billing-sync.ts` off `App.tsx`'s static graph, matching the Phase 6 D-12 `deferOnSignedIn` discipline. `void` plus `.then` because the auth handler should not block on the billing query.)

    Step 2 — SIGNED_IN branch (~line 378-391): identical addition inside its `email_confirmed_at && !is_anonymous` block, after `deferOnSignedIn(...)`.

    Step 3 — window-focus refetch (for the SC#2 10-second Portal-return budget): add a `useEffect` in the `App` component body that registers `window.addEventListener('focus', handler)` where `handler` reads the current signed-in user id from `useStore.getState().signedIn?.user?.id` and, if present and the user is non-anonymous, dynamic-imports `@/lib/billing-sync` and calls `syncBillingTier(userId)`. The effect MUST clean up the listener on unmount via `removeEventListener('focus', handler)`. Guard against firing for anon / signed-out users (no `userId` → no-op). Add a brief comment citing CONTEXT D-09 (Portal return URL → webhook+refresh cycle picks up the change within 10s — the focus event IS that refresh trigger).

    Step 4 — verify no new STATIC import was added to `App.tsx`'s import block (the `billing-sync` reference must be dynamic-import only). Run `npm run lint` and `npx tsc -b --noEmit`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx tsc -b --noEmit && npm run lint -- src/App.tsx && grep -c "syncBillingTier" src/App.tsx && [ "$(grep -cE "^import .* from .@/lib/billing-sync." src/App.tsx)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'syncBillingTier' leanshot/src --include='*.tsx' --include='*.ts' | grep -v '\.test\.'` returns at least 3 non-test references: the export in `billing-sync.ts`, the INITIAL_SESSION + SIGNED_IN handlers in `App.tsx`, and the focus handler in `App.tsx`.
    - `[ "$(grep -cE "^import .* from .@/lib/billing-sync." leanshot/src/App.tsx)" = "0" ]` — `billing-sync` is referenced ONLY via dynamic `import('@/lib/billing-sync')`, never a top-level static import. (Bracketed-test form so a count of `0` — the DESIRED state — exits 0, not 1.)
    - `grep -c "addEventListener('focus'" leanshot/src/App.tsx` is at least 1 AND `grep -c "removeEventListener('focus'" leanshot/src/App.tsx` is at least 1 — the focus listener is registered and cleaned up.
    - `npx tsc -b --noEmit` clean; `npm run lint -- src/App.tsx` clean.
  </acceptance_criteria>
  <done>
    `App.tsx` calls `syncBillingTier` from both INITIAL_SESSION and SIGNED_IN handlers (after `deferOnSignedIn`) and from a cleaned-up window-`focus` listener — all via dynamic import. `useStore((s) => s.tier)` now reflects real Stripe state for signed-in users; Portal round-trips are picked up on tab refocus. SIGNED_OUT untouched (teardown already correct).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract shared Plan union to billing.ts + fix PaywallUpsell checkout call to mirror UpgradeCTA + add plan prop</name>
  <files>
    leanshot/src/lib/billing.ts,
    leanshot/src/components/billing/UpgradeCTA.tsx,
    leanshot/src/components/billing/PaywallUpsell.tsx,
    leanshot/src/components/billing/PaywallUpsell.test.tsx
  </files>
  <read_first>
    - leanshot/src/lib/billing.ts (the existing module — `getActiveTier` etc.; this task ADDS one exported `type Plan` and changes nothing else)
    - leanshot/src/components/billing/UpgradeCTA.tsx (THE SOURCE OF TRUTH for the invoke pattern — lines 44-65 show the correct `supabase.functions.invoke('stripe-checkout/session', { body: { plan } })` call + same-tab `window.location.href` redirect + the LOCAL `type Plan = 'plus_monthly' | 'plus_yearly'` + `PLAN_MAP` typing that this task extracts)
    - leanshot/src/components/billing/PaywallUpsell.tsx (the broken `fetchCheckoutUrl()` at lines 45-63 + the `PaywallUpsellProps` interface + both `variant` render branches)
    - leanshot/src/lib/supabase.ts (the `supabase` singleton)
    - leanshot/src/components/billing/UpgradeCTA.test.tsx (Vitest mock style for `supabase.functions.invoke` in billing-component tests — mirror it; also confirms `UpgradeCTA` still type-checks after the `Plan` import swap)
  </read_first>
  <behavior>
    - `leanshot/src/lib/billing.ts` exports `type Plan = 'plus_monthly' | 'plus_yearly' | 'clinic'`.
    - `UpgradeCTA.tsx` no longer declares a local `type Plan = ...`; it imports `Plan` from `@/lib/billing`. `UpgradeCTA`'s runtime output (which plan it sends, the redirect) is byte-for-byte unchanged.
    - `<PaywallUpsell variant="overlay" feature="pharma-forecast" />` (no `plan` prop) — clicking Upgrade calls `supabase.functions.invoke('stripe-checkout/session', { body: { plan: 'plus_monthly' } })` (the default).
    - `<PaywallUpsell variant="cta" feature="advanced-ai" plan="plus_yearly" />` — clicking Upgrade calls `supabase.functions.invoke('stripe-checkout/session', { body: { plan: 'plus_yearly' } })`.
    - On a successful invoke (`{ data: { url: 'https://checkout.stripe.test/...' }, error: null }`) the handler assigns `window.location.href` to `data.url`.
    - On `{ data: null, error: <truthy> }` OR `{ data: { url: undefined }, ... }` the handler does NOT redirect; it logs via `console.error` (no thrown error escapes the click handler).
    - `PaywallUpsell.tsx` contains NO `credentials:` key and NO bare `fetch` to `/functions/v1/stripe-checkout`.
    - `PaywallUpsell.tsx` contains NO `@stripe/stripe-js` import.
    - Neither `PaywallUpsell.tsx` nor `UpgradeCTA.tsx` declares a local `type Plan` — both import it from `@/lib/billing`.
  </behavior>
  <action>
    **Step 0 — extract the `Plan` union into `billing.ts` (the DRY fix).**
    - In `leanshot/src/lib/billing.ts`, add `export type Plan = 'plus_monthly' | 'plus_yearly' | 'clinic';` near the other type exports. One-line JSDoc: "Checkout `plan` body field — single source of truth (was duplicated as a local union in UpgradeCTA.tsx). `'clinic'` added for PaywallUpsell's clinic-tier upsell." Change NOTHING else in `billing.ts` — `getActiveTier` and every existing export stay byte-identical.
    - In `leanshot/src/components/billing/UpgradeCTA.tsx`: DELETE the local `type Plan = 'plus_monthly' | 'plus_yearly';` line and add `import type { Plan } from '@/lib/billing';` to the import block (alphabetized per the `import-x` rule). Do NOT touch `PLAN_MAP`, the invoke call, the JSX, or any other line — widening the union to include `'clinic'` is non-breaking because `UpgradeCTA` never constructs a `'clinic'` value. Confirm `npx tsc -b --noEmit` stays clean.

    Then rewrite the checkout plumbing of `leanshot/src/components/billing/PaywallUpsell.tsx`. The JSX (both `variant="overlay"` and `variant="cta"` render branches, all `var(--color-*)` token styling, the `Card`/`Button` usage, the `useReducedMotion` call) stays AS-IS — only the data-fetch helper and the props change.

    Step 1 — props: extend `PaywallUpsellProps` with `plan?: Plan` (import `Plan` from `@/lib/billing` — do NOT redeclare a local union) defaulting to `'plus_monthly'`. Destructure `plan = 'plus_monthly'` in the component signature.

    Step 2 — DELETE the `fetchCheckoutUrl()` helper entirely (lines ~45-63: the bare `fetch` to `/functions/v1/stripe-checkout` with `credentials:'include'`). Remove the now-unused `base` / `import.meta.env.VITE_SUPABASE_URL` reference.

    Step 3 — rewrite `handleUpgrade` to mirror `UpgradeCTA.tsx:44-65` exactly:
    - `import { supabase } from '@/lib/supabase';` (add to the import block).
    - `const { data, error } = await supabase.functions.invoke('stripe-checkout/session', { body: { plan } });`
    - `if (error || !data?.url) { console.error('[PaywallUpsell] checkout invoke failed', error?.message ?? 'no-url'); return; }`
    - `window.location.href = data.url;`
    - Keep the existing `try/catch` envelope so a thrown invoke error still logs and does not crash the click handler. Do NOT echo the upstream error message into any UI (Pattern G / Pitfall 8 — match `UpgradeCTA`'s "do NOT echo upstream error" posture).

    Step 4 — update the file's top JSDoc: replace the now-stale "thin local async function `fetchCheckoutUrl`" paragraph with a one-line note that the checkout call mirrors `UpgradeCTA.tsx` via `supabase.functions.invoke('stripe-checkout/session', ...)` and takes a `plan` prop (default `plus_monthly`) typed via the shared `@/lib/billing` `Plan` union. Reference CR-02 as the gap this closes.

    Step 5 — create `leanshot/src/components/billing/PaywallUpsell.test.tsx` (the file does not exist today):
    - Vitest + React Testing Library, mirroring `UpgradeCTA.test.tsx`'s mock of `supabase.functions.invoke`.
    - `vi.mock('@/lib/supabase', ...)` exposing `supabase.functions.invoke: vi.fn()`.
    - Handle `window.location.href` assignment per the existing redirect-test convention in `UpgradeCTA.test.tsx` (if `UpgradeCTA.test.tsx` does not assert the redirect, assert on the `invoke` mock call args instead — that is the load-bearing assertion).
    - Cover the 4 behavior cases: default-plan invoke args, explicit `plan` prop forwarded, success leads to redirect, error leads to no redirect + `console.error`.

    Step 6 — consumer-compat check: `MedLevelChart.tsx` and `AIChatPanel.tsx` render `<PaywallUpsell>` without a `plan` prop today — confirm they still type-check (the `plan?` default makes this a non-breaking change; do NOT edit those files).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx vitest run src/components/billing/PaywallUpsell.test.tsx --reporter=verbose && npx tsc -b --noEmit && npm run lint -- src/lib/billing.ts src/components/billing/UpgradeCTA.tsx src/components/billing/PaywallUpsell.tsx src/components/billing/PaywallUpsell.test.tsx && grep -c "stripe-checkout/session" src/components/billing/PaywallUpsell.tsx && ! grep -nE "credentials:|/functions/v1/stripe-checkout" src/components/billing/PaywallUpsell.tsx && ! grep -nE "from ['\"]@stripe/stripe-js['\"]" src/components/billing/PaywallUpsell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type Plan" leanshot/src/lib/billing.ts` is at least 1 — the shared union is exported from `billing.ts`.
    - `! grep -nE "^\s*type Plan\b" leanshot/src/components/billing/UpgradeCTA.tsx` (zero matches — the local `type Plan` declaration is deleted) AND `grep -c "import type { Plan }" leanshot/src/components/billing/UpgradeCTA.tsx` is at least 1.
    - `! grep -nE "^\s*type Plan\b" leanshot/src/components/billing/PaywallUpsell.tsx` (zero matches — `PaywallUpsell` does NOT redeclare a local `Plan` union) AND `grep -cE "import type \{ Plan \}|import type \{ .*Plan.* \}" leanshot/src/components/billing/PaywallUpsell.tsx` is at least 1.
    - `grep -c "stripe-checkout/session" leanshot/src/components/billing/PaywallUpsell.tsx` is at least 1.
    - `! grep -nE "credentials:" leanshot/src/components/billing/PaywallUpsell.tsx` (zero matches — the `credentials:'include'` is gone).
    - `! grep -nE "/functions/v1/stripe-checkout" leanshot/src/components/billing/PaywallUpsell.tsx` (zero matches — the bare fetch path is gone).
    - `! grep -nE "from ['\"]@stripe/stripe-js['\"]" leanshot/src/components/billing/PaywallUpsell.tsx` (zero matches).
    - `PaywallUpsellProps` has a `plan?` field defaulting to `'plus_monthly'`; `PaywallUpsell.test.tsx` runs at least 4 green cases including the default-plan and explicit-plan invoke-args assertions.
    - `npx tsc -b --noEmit` clean (proves `UpgradeCTA`, `MedLevelChart`, `AIChatPanel` all still compile after the `Plan` extraction + the no-`plan`-prop call sites); `npm run lint` clean on all 4 files.
  </acceptance_criteria>
  <done>
    `leanshot/src/lib/billing.ts` exports the single `Plan` union; `UpgradeCTA.tsx` and `PaywallUpsell.tsx` both import it (no duplicated local union). `PaywallUpsell` opens a real Stripe Checkout session via `supabase.functions.invoke('stripe-checkout/session', { body: { plan } })` with a `plan` prop (default `plus_monthly`); the broken bare-fetch + `credentials:'include'` helper is deleted; existing `MedLevelChart` / `AIChatPanel` / `UpgradeCTA` consumers still compile untouched. At least 4 green Vitest cases.
  </done>
</task>

</tasks>

<commit_pathspec>
Commit with explicit pathspec discipline (Pattern F — Wave 1 runs parallel with file-disjoint plan 14-10):

```bash
git commit -- \
  leanshot/src/lib/billing-sync.ts \
  leanshot/src/lib/billing-sync.test.ts \
  leanshot/src/App.tsx \
  leanshot/src/lib/billing.ts \
  leanshot/src/components/billing/PaywallUpsell.tsx \
  leanshot/src/components/billing/PaywallUpsell.test.tsx \
  leanshot/src/components/billing/UpgradeCTA.tsx
```

Never `git add -A` or `git add .` — the git index is shared with the parallel 14-10 executor.
</commit_pathspec>

<verification>
End-to-end checks after all 3 tasks:

```bash
cd /Users/karstenhaldan/minisite/leanshot

# 1. Plan-owned Vitest
npx vitest run src/lib/billing-sync.test.ts src/components/billing/PaywallUpsell.test.tsx --reporter=verbose

# 2. TypeScript compiles (proves consumers untouched still type-check)
npx tsc -b --noEmit

# 3. ESLint clean on plan-owned files
npm run lint -- \
  src/lib/billing-sync.ts src/lib/billing-sync.test.ts \
  src/App.tsx src/lib/billing.ts \
  src/components/billing/PaywallUpsell.tsx src/components/billing/PaywallUpsell.test.tsx \
  src/components/billing/UpgradeCTA.tsx

# 4. CR-01 closed — setTier now has a non-test caller chain
grep -rn 'syncBillingTier' src --include='*.ts' --include='*.tsx' | grep -v '\.test\.'
# expect: export in billing-sync.ts + >=3 call sites in App.tsx (INITIAL_SESSION, SIGNED_IN, focus)

# 5. CR-02 closed — correct invoke pattern, no broken fetch, shared Plan union
grep -c "stripe-checkout/session" src/components/billing/PaywallUpsell.tsx   # >=1
! grep -nE "credentials:|/functions/v1/stripe-checkout" src/components/billing/PaywallUpsell.tsx
grep -c "export type Plan" src/lib/billing.ts                               # >=1
! grep -nE "^\s*type Plan\b" src/components/billing/UpgradeCTA.tsx
! grep -nE "^\s*type Plan\b" src/components/billing/PaywallUpsell.tsx

# 6. Pattern G — no @stripe/stripe-js static imports introduced
! grep -rnE "from ['\"]@stripe/stripe-js['\"]" \
  src/lib/billing-sync.ts src/components/billing/PaywallUpsell.tsx src/App.tsx

# 7. billing-sync stays off App.tsx static graph
[ "$(grep -cE "^import .* from ['\"]@/lib/billing-sync['\"]" src/App.tsx)" = "0" ]

# 8. Bundle ceiling — index chunk stays under 50 kB gz
npm run build && bash scripts/assert-bundle-budget.sh

# 9. Full app test suite (smoke — no regressions)
npx vitest run --reporter=dot
```
</verification>

<success_criteria>
- `billing-sync.ts` is the single named DB-to-store connector: queries `subscriptions`, collapses via `getActiveTier`, writes via `setTier`; no-row maps to `'free'`, query-error maps to no-write + log, never throws.
- `App.tsx` calls `syncBillingTier` from INITIAL_SESSION, SIGNED_IN, and a cleaned-up window-`focus` listener — all via dynamic import; no new static import in `App.tsx`.
- `useStore((s) => s.tier)` reflects real Stripe state for signed-in users (CR-01 closed) — SC#1, SC#2, SC#4 unblocked.
- `PaywallUpsell` opens a real Checkout session via `supabase.functions.invoke('stripe-checkout/session', { body: { plan } })` with a `plan` prop default `'plus_monthly'`; broken bare-fetch deleted (CR-02 closed) — SC#1, SC#5 unblocked.
- The `Plan` union lives only in `billing.ts`; `UpgradeCTA.tsx` and `PaywallUpsell.tsx` both import it — no duplicated local union (CR-02 DRY).
- `MedLevelChart` / `AIChatPanel` / `UpgradeCTA` consumers compile untouched.
- Zero `@stripe/stripe-js` static imports, zero hex literals introduced in plan-owned files.
- `npm run build` succeeds; `scripts/assert-bundle-budget.sh` passes; index chunk gz under 50 kB.
- Full Vitest suite stays green (no regressions).
</success_criteria>

<output>
After completion, create `.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-09-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Summary MUST capture:
- Confirmation CR-01 is closed: `grep` output showing `setTier` now has the `billing-sync.ts` -> `App.tsx` call chain (3+ non-test call sites).
- Confirmation CR-02 is closed: `PaywallUpsell` uses `supabase.functions.invoke('stripe-checkout/session', ...)`, `credentials:` and the bare fetch path are gone, and the `Plan` union now lives in `billing.ts` with both components importing it.
- Vitest counts (billing-sync.test.ts + PaywallUpsell.test.tsx + full-suite total).
- Final bundle index gz size + delta.
- Any deviation from the plan (e.g. if `UpgradeCTA.test.tsx`'s redirect-test convention required a different `window.location` mock approach).
- Hand-off note for plan 14-11: the `clinic-metered-billing.spec.ts` e2e (WR-09) and `past-due-banner.spec.ts` now have a real DB-to-store path to assert against — 14-11's e2e fixes depend on this plan landing.

## PLAN COMPLETE

**Plan ID:** 14-09
</output>
