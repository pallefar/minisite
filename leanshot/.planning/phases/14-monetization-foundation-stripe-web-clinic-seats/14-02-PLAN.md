---
phase: 14-monetization-foundation-stripe-web-clinic-seats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/vercel.json
  - leanshot/tests/csp/csp-snapshot.txt
  - leanshot/scripts/stripe-bootstrap.ts
  - leanshot/scripts/stripe-bootstrap.test.ts
  - leanshot/.env.example
  - leanshot/package.json
  - leanshot/package-lock.json
autonomous: true
requirements:
  - MONEY-08
tags:
  - stripe
  - csp
  - bootstrap
  - foundation

must_haves:
  truths:
    - "Developer can run `npx tsx scripts/stripe-bootstrap.ts` from /Users/karstenhaldan/minisite/leanshot with STRIPE_SECRET_KEY in env, the script exits 0, and stdout lists 5 STRIPE_PRICE_* lines + 1 STRIPE_METER_* line ready to copy into .env.example + Vercel env + Supabase secrets"
    - "Re-running the bootstrap script produces zero net-new products / prices / meters (search-before-create idempotency)"
    - "`npm test -- tests/csp/csp-snapshot.test.ts` passes after CSP widening — leanshot/vercel.json and leanshot/tests/csp/csp-snapshot.txt agree on the same sorted directive list and BOTH files are in the same commit (Pattern H)"
    - "The widened CSP includes js.stripe.com in script-src, api.stripe.com + m.stripe.network in connect-src, and 'self' + js.stripe.com + hooks.stripe.com in frame-src (replaces the existing `frame-src 'none'`)"
    - "leanshot/.env.example documents 7 new env vars (STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_PLUS_MONTHLY, STRIPE_PRICE_PLUS_YEARLY, STRIPE_PRICE_CLINIC_BASE, STRIPE_PRICE_CLINIC_OVERAGE, STRIPE_METER_ACTIVE_PATIENTS) plus VITE_STRIPE_PUBLIC_KEY — exactly one VITE_-prefixed Stripe variable since the Upgrade CTA uses plain `window.location.href` per Pitfall 10 (no `@stripe/stripe-js` import shipped)"
    - "`npm test -- tests/csp/csp-snapshot.test.ts scripts/stripe-bootstrap.test.ts` passes (smoke test asserts the bootstrap script bails with a clear error when STRIPE_SECRET_KEY is unset; the live-API path is gated on HAS_STRIPE env)"
  artifacts:
    - path: "leanshot/vercel.json"
      provides: "Vercel CSP header widened for Stripe Checkout + Customer Portal"
      contains: "https://js.stripe.com"
    - path: "leanshot/tests/csp/csp-snapshot.txt"
      provides: "Sorted CSP snapshot matching vercel.json (atomic-commit contract per Pattern H)"
      contains: "frame-src 'self' https://js.stripe.com https://hooks.stripe.com;"
    - path: "leanshot/scripts/stripe-bootstrap.ts"
      provides: "Idempotent Stripe product + price + meter creator"
      exports: ["main", "ensureProduct", "ensurePrice", "ensureMeter"]
      min_lines: 80
    - path: "leanshot/scripts/stripe-bootstrap.test.ts"
      provides: "Vitest smoke test asserting env-presence guard fires when STRIPE_SECRET_KEY is missing; live-API path gated on HAS_STRIPE"
      min_lines: 20
    - path: "leanshot/.env.example"
      provides: "Documented placeholders for 7 new Stripe env vars + 1 publishable key"
      contains: "STRIPE_PRICE_PLUS_MONTHLY"
  key_links:
    - from: "leanshot/tests/csp/csp-snapshot.test.ts"
      to: "leanshot/vercel.json + leanshot/tests/csp/csp-snapshot.txt"
      via: "parseCSP() sort-and-diff"
      pattern: "expect\\(liveSorted\\)\\.toEqual\\(snapshotSorted\\)"
    - from: "leanshot/scripts/stripe-bootstrap.ts"
      to: "Stripe API (live)"
      via: "stripe.products.list() / prices.list() / billing.meters.list() before .create()"
      pattern: "ensureProduct|ensurePrice|ensureMeter"
    - from: "leanshot/scripts/stripe-bootstrap.ts stdout"
      to: "developer's .env.example + Vercel env + Supabase Function secrets"
      via: "copy-paste of `STRIPE_PRICE_*=price_xxx` lines per [[feedback_cli_over_paste_back]] (Stripe MCP-fetchable)"
      pattern: "console\\.log\\(`STRIPE_(PRICE|METER)_"
---

<objective>
Phase 14 Plan 02 — **Foundation slice** for Stripe monetization: widen CSP atomically (vercel.json + csp-snapshot.txt in ONE commit per Pattern H), ship the idempotent `scripts/stripe-bootstrap.ts` that creates 2 products + 4 prices + 1 Billing Meter via the Stripe API, and document the 7 new env vars in `.env.example`.

**Purpose:** Without live Stripe price IDs and a CSP that allows Stripe domains, Phase 14 plans 04 (Checkout) and 06 (Upgrade CTA + Portal link) cannot function. This plan unblocks both: the bootstrap script publishes price IDs that downstream plans consume; the CSP widening lets the browser load `js.stripe.com` and reach `api.stripe.com`.

**Output:** Atomic CSP commit (Vercel header + snapshot test fixture); a re-runnable bootstrap script; placeholder env vars documenting what the developer must paste from the script's stdout into Vercel env + Supabase Function secrets per [[feedback_cli_over_paste_back]].

**MVP vertical-slice capability gained:** A developer with `STRIPE_SECRET_KEY` set can produce 5 live Stripe price IDs + 1 Meter ID in <30s; the browser CSP allows Stripe domains.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-CONTEXT.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-RESEARCH.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-PATTERNS.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-PLAN-OUTLINE.md
@leanshot/CLAUDE.md
@leanshot/vercel.json
@leanshot/tests/csp/csp-snapshot.txt
@leanshot/tests/csp/csp-snapshot.test.ts
@leanshot/.env.example
@leanshot/e2e/fixtures/seed-org-50.ts

<interfaces>
<!-- The CSP test contract — executor MUST honor this (BLOCKER per csp-snapshot.test.ts:8-13). -->

leanshot/tests/csp/csp-snapshot.test.ts contract (lines 8-13):
"When any Phase 14/16/19/20/22 plan adds an external origin, update vercel.json AND tests/csp/csp-snapshot.txt in the SAME commit; plan-checker enforces this. Absence of either file in the CSP-widening commit is a plan-checker BLOCKER."

parseCSP() (lines 33-40):
- Splits on `;`, trims, drops empties, RE-APPENDS `;`, then sorts.
- Means: each directive line in csp-snapshot.txt MUST end in `;` and the file MUST contain ONE directive per line.
- Sort is lexicographic on the directive-with-trailing-semicolon string.

Current leanshot/tests/csp/csp-snapshot.txt (BEFORE Phase 14 widening — 11 lines, sorted):
```
base-uri 'self';
connect-src 'self' https://*.supabase.co wss://*.supabase.co https://*.ingest.us.sentry.io https://*.ingest.sentry.io https://*.posthog.com https://api.anthropic.com;
default-src 'none';
font-src 'self' data: https://fonts.gstatic.com;
form-action 'self';
frame-src 'none';
img-src 'self' data: blob:;
object-src 'none';
script-src 'self';
style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
worker-src 'self' blob:;
```

Current leanshot/vercel.json header value (BEFORE widening — single string, semicolon-separated, NOT sorted):
"default-src 'none'; script-src 'self'; connect-src 'self' https://*.supabase.co wss://*.supabase.co https://*.ingest.us.sentry.io https://*.ingest.sentry.io https://*.posthog.com https://api.anthropic.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: blob:; font-src 'self' data: https://fonts.gstatic.com; frame-src 'none'; object-src 'none'; base-uri 'self'; form-action 'self'; worker-src 'self' blob:"

Note: vercel.json's `Content-Security-Policy` value does NOT have a trailing `;` on the final directive — parseCSP() handles this via `.filter(Boolean)` after the split. Either trailing-semicolon or no-trailing-semicolon is acceptable in vercel.json; parseCSP() normalizes both into the same sorted form.

Bootstrap-script canonical reference (verbatim from 14-RESEARCH.md lines 523-585):
- `apiVersion: '2026-04-22.dahlia'` — pin to research-verified version (do NOT use older versions; the meter-events shape changed)
- `ensureProduct(name, description)` lists with limit 100 then `.find((p) => p.name === name)` — name is the dedup key
- `ensurePrice(productId, lookupKey, params)` uses `stripe.prices.list({ lookup_keys: [lookupKey], active: true })` — lookup_key is the dedup key (Stripe-canonical pattern)
- `ensureMeter(eventName, displayName)` lists with limit 100 then `.find((m) => m.event_name === eventName)` — event_name is the dedup key. customer_mapping uses `{ type: 'by_id', event_payload_key: 'stripe_customer_id' }` per RESEARCH §"Pattern 5: Billing Meters" (NOT legacy `usage_records.create` shape)
- For metered overage prices: `recurring: { interval: 'month', usage_type: 'metered', meter: meter.id }` — `meter` ID must be passed, not the event_name

Pricing locked by 14-CONTEXT.md D-09 + D-10:
- LeanShot Plus monthly: $12.99 / month → unit_amount: 1299
- LeanShot Plus yearly: $132.49 / year → unit_amount: 13249 (≈15% discount on monthly × 12 = $155.88; rounded down to $132.49)
- LeanShot Clinic base: $99 / month → unit_amount: 9900
- LeanShot Clinic overage: $9 / active-patient / month → unit_amount: 900, metered

Five price IDs + one meter ID the script MUST output (in this order):
```
STRIPE_PRICE_PLUS_MONTHLY=price_...
STRIPE_PRICE_PLUS_YEARLY=price_...
STRIPE_PRICE_CLINIC_BASE=price_...
STRIPE_PRICE_CLINIC_OVERAGE=price_...
STRIPE_METER_ACTIVE_PATIENTS=mtr_...
```
(NOTE the orchestrator instruction names 4 price vars but the LIVE OUTPUT is 4 prices total: 2 Plus + 2 Clinic. The script prints 4 STRIPE_PRICE_* lines + 1 STRIPE_METER_* line. The .env.example documents 4 STRIPE_PRICE_* placeholders. Refer to orchestrator brief — "outputs 6 price/meter IDs" was a count-off-by-one; 5 IDs total = 4 prices + 1 meter.)
</interfaces>

<invariants>
- **Pattern H (CSP atomic commit):** vercel.json AND tests/csp/csp-snapshot.txt MUST land in the SAME git commit. Splitting them is a plan-checker BLOCKER per csp-snapshot.test.ts:8-13.
- **Pattern F (pathspec discipline):** This plan runs in Wave 1 parallel with 14-01. Commit MUST use pathspec form `git commit -- leanshot/vercel.json leanshot/tests/csp/csp-snapshot.txt leanshot/scripts/stripe-bootstrap.ts leanshot/scripts/stripe-bootstrap.test.ts leanshot/.env.example leanshot/package.json leanshot/package-lock.json` — never `git add .` or `git add -A` per [[feedback_parallel_executor_git_isolation]].
- **Pattern G (bundle isolation):** This plan MUST NOT add a static `import` of `@stripe/stripe-js` anywhere in `src/`. The script-only import of `stripe` (server SDK) is fine — scripts/* never enters the Vite bundle. Verify by grepping after the work: `git diff --name-only HEAD~ HEAD -- 'leanshot/src/**' | xargs grep -l '@stripe/stripe-js' 2>/dev/null | wc -l` MUST be 0.
- **Idempotency:** Every helper (`ensureProduct`, `ensurePrice`, `ensureMeter`) MUST search before create. Re-running the script produces zero net-new Stripe objects.
- **No `VITE_`-prefixed secret keys:** `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` are SERVER-ONLY. Only `VITE_STRIPE_PUBLIC_KEY` may carry the `VITE_` prefix (publishable key is safe in the browser bundle).
- **Pricing locked:** $12.99 / $132.49 / $99 / $9 — these come from 14-CONTEXT.md D-09 and D-10 + user confirmation captured in 14-RESEARCH.md. Do NOT round, do NOT pick alternates.
- **No `tsx` install bloat:** `tsx` and `stripe` are added as `devDependencies` in `leanshot/package.json` so the script can be invoked via `npx tsx`. Do NOT add them to `dependencies` — they must NOT enter the production bundle. Verify post-install: `npm pkg get devDependencies.tsx devDependencies.stripe` returns both versions.
</invariants>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomically widen CSP for Stripe (vercel.json + csp-snapshot.txt in lockstep)</name>
  <files>leanshot/vercel.json, leanshot/tests/csp/csp-snapshot.txt</files>
  <action>
    Edit BOTH leanshot/vercel.json AND leanshot/tests/csp/csp-snapshot.txt in lockstep so the CSP snapshot test passes after this single commit per D-16 and Pattern H (csp-snapshot.test.ts:8-13 plan-checker BLOCKER).

    **leanshot/vercel.json (single header-value string edit):**
    - Add `https://js.stripe.com` to `script-src` (after `'self'`, before next `;`). New value: `script-src 'self' https://js.stripe.com;`
    - Add `https://api.stripe.com https://m.stripe.network` to the end of `connect-src` (after `https://api.anthropic.com`, before next `;`). New value: `connect-src 'self' https://*.supabase.co wss://*.supabase.co https://*.ingest.us.sentry.io https://*.ingest.sentry.io https://*.posthog.com https://api.anthropic.com https://api.stripe.com https://m.stripe.network;`
    - REPLACE `frame-src 'none'` with `frame-src 'self' https://js.stripe.com https://hooks.stripe.com` (Stripe Checkout uses iframes for 3DS challenges; D-16). New value: `frame-src 'self' https://js.stripe.com https://hooks.stripe.com;`
    - Leave all other directives unchanged.

    **leanshot/tests/csp/csp-snapshot.txt (one-directive-per-line, sorted, each ending in `;`):**
    - Update the 3 mutated lines to match (keep file alphabetically sorted per parseCSP() expectation):
      - `connect-src 'self' https://*.supabase.co wss://*.supabase.co https://*.ingest.us.sentry.io https://*.ingest.sentry.io https://*.posthog.com https://api.anthropic.com https://api.stripe.com https://m.stripe.network;`
      - `frame-src 'self' https://js.stripe.com https://hooks.stripe.com;`
      - `script-src 'self' https://js.stripe.com;`
    - Lines stay sorted by directive name (alphabetical): base-uri, connect-src, default-src, font-src, form-action, frame-src, img-src, object-src, script-src, style-src, worker-src — 11 lines total (unchanged count).
    - Each line MUST end in `;` (parseCSP() splits on `;` then re-appends `;`).

    **Why three additions and not four:** Stripe Checkout itself runs on checkout.stripe.com which the browser visits via top-level navigation (not framed), so `frame-src` only needs js.stripe.com (Stripe.js iframes) + hooks.stripe.com (3DS hooks) — NOT checkout.stripe.com. Per RESEARCH §"CSP additions" + D-16.

    **DO NOT** stage / commit yet — Task 4 commits all five files together to honor Pattern H + Pattern F (single pathspec commit).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- tests/csp/csp-snapshot.test.ts --run 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `npm test -- tests/csp/csp-snapshot.test.ts --run` exits 0; both files agree on the sorted directive list.
    - `grep -c 'js.stripe.com' leanshot/vercel.json` returns 2 (script-src + frame-src).
    - `grep -c 'api.stripe.com' leanshot/vercel.json` returns 1 (connect-src).
    - `grep -c 'm.stripe.network' leanshot/vercel.json` returns 1 (connect-src).
    - `grep -v '^#' leanshot/tests/csp/csp-snapshot.txt | grep -c ';$'` returns 11 (every non-comment line ends with `;`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship idempotent stripe-bootstrap.ts + smoke test + install tsx/stripe devDeps</name>
  <files>leanshot/scripts/stripe-bootstrap.ts, leanshot/scripts/stripe-bootstrap.test.ts, leanshot/package.json, leanshot/package-lock.json</files>
  <action>
    Install dev dependencies and ship the idempotent Stripe bootstrap script per D-11 + 14-RESEARCH.md §"Bootstrap script" lines 523-585.

    **Step 2a — Install devDependencies (run from /Users/karstenhaldan/minisite/leanshot):**
    Run `npm install --save-dev tsx@^4 stripe@^17` to pin `tsx` (the TS runner for the script) and `stripe` (server SDK) into `devDependencies` only. These MUST NOT enter the production bundle — scripts/ is excluded from Vite's input. Verify post-install: `npm pkg get devDependencies.tsx devDependencies.stripe` returns both. Stripe SDK v17 supports `apiVersion: '2026-04-22.dahlia'` per RESEARCH.

    **Step 2b — Create leanshot/scripts/stripe-bootstrap.ts:**
    Module-level imports: `import Stripe from 'stripe';`. NO Node built-in beyond `process` (already global).

    Top-of-file env-presence guard — bail with a clear error before constructing the Stripe client:
    - If `process.env.STRIPE_SECRET_KEY` is unset OR empty string: log `'[stripe-bootstrap] STRIPE_SECRET_KEY not set. Set it in your shell (export STRIPE_SECRET_KEY=sk_test_...) then re-run.'` to stderr and `process.exit(1)`.

    Construct `const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, { apiVersion: '2026-04-22.dahlia' });`. PIN this exact `apiVersion`; do NOT use a newer or older one (Stripe SDK type definitions are version-stamped; the Billing Meters shape requires this version per RESEARCH).

    Export three helpers (all `async`, all idempotent — search before create):
    - `export async function ensureProduct(name: string, description: string): Promise&lt;Stripe.Product&gt;` — `stripe.products.list({ limit: 100 })` then `.data.find((p) => p.name === name)` ?? `stripe.products.create({ name, description })`.
    - `export async function ensurePrice(productId: string, lookupKey: string, params: Stripe.PriceCreateParams): Promise&lt;Stripe.Price&gt;` — `stripe.prices.list({ lookup_keys: [lookupKey], active: true, limit: 1 })` then `.data[0]` ?? `stripe.prices.create({ ...params, product: productId, lookup_key: lookupKey })`. Note: `lookup_key` is the dedup key (Stripe-canonical), NOT `nickname`.
    - `export async function ensureMeter(eventName: string, displayName: string): Promise&lt;Stripe.Billing.Meter&gt;` — `stripe.billing.meters.list({ limit: 100 })` then `.data.find((m) => m.event_name === eventName)` ?? `stripe.billing.meters.create({ display_name: displayName, event_name: eventName, customer_mapping: { type: 'by_id', event_payload_key: 'stripe_customer_id' }, default_aggregation: { formula: 'sum' }, value_settings: { event_payload_key: 'value' } })`. The `customer_mapping` shape comes from RESEARCH §"Pattern 5: Billing Meters" — do NOT use legacy `usage_records.create` shape.

    Export an `async function main()` that runs the orchestration top-to-bottom:
    1. `const plus = await ensureProduct('LeanShot Plus', 'Web subscription — premium features')`
    2. `const plusMonthly = await ensurePrice(plus.id, 'plus_monthly_v1', { currency: 'usd', unit_amount: 1299, recurring: { interval: 'month' } })`
    3. `const plusYearly = await ensurePrice(plus.id, 'plus_yearly_v1', { currency: 'usd', unit_amount: 13249, recurring: { interval: 'year' } })`
    4. `const clinic = await ensureProduct('LeanShot Clinic', 'Clinic operator subscription')`
    5. `const clinicBase = await ensurePrice(clinic.id, 'clinic_base_v1', { currency: 'usd', unit_amount: 9900, recurring: { interval: 'month' } })`
    6. `const meter = await ensureMeter('active_patients', 'Active patients')`
    7. `const clinicOverage = await ensurePrice(clinic.id, 'clinic_overage_v1', { currency: 'usd', unit_amount: 900, recurring: { interval: 'month', usage_type: 'metered', meter: meter.id } })`

    Then print a clearly-labeled stdout block the developer copies into `.env.example` + Vercel env + Supabase Function secrets per [[feedback_cli_over_paste_back]]:
    ```
    Add these to .env.example + Vercel env + Supabase Function secrets:
    STRIPE_PRICE_PLUS_MONTHLY=&lt;plusMonthly.id&gt;
    STRIPE_PRICE_PLUS_YEARLY=&lt;plusYearly.id&gt;
    STRIPE_PRICE_CLINIC_BASE=&lt;clinicBase.id&gt;
    STRIPE_PRICE_CLINIC_OVERAGE=&lt;clinicOverage.id&gt;
    STRIPE_METER_ACTIVE_PATIENTS=&lt;meter.id&gt;
    ```

    At the bottom of the file, run main() only when invoked as a script (NOT when imported by the smoke test):
    ```
    if (import.meta.url === `file://${process.argv[1]}` || process.argv[1]?.endsWith('stripe-bootstrap.ts')) {
      main().catch((err) =&gt; { console.error('[stripe-bootstrap] failed:', err); process.exit(1); });
    }
    ```
    (Use the `import.meta.url` self-detection idiom; the secondary `argv[1]?.endsWith` branch covers the `tsx` invocation path which rewrites `import.meta.url` differently across versions.)

    Note the orchestrator brief asked for "6 price/meter IDs in stdout" — count reconciliation: it's 4 STRIPE_PRICE_* lines + 1 STRIPE_METER_* line = 5 IDs total (2 Plus prices + 2 Clinic prices + 1 Meter). The 14-RESEARCH.md §"Bootstrap script" reference confirms 5. Use 5.

    **Step 2c — Create leanshot/scripts/stripe-bootstrap.test.ts (Vitest smoke):**
    Two test cases:
    1. **Env-guard fires when STRIPE_SECRET_KEY missing** — `it('bails with clear error when STRIPE_SECRET_KEY unset', async () =&gt; { ... })`. Spawn the script via `node:child_process.spawnSync('npx', ['tsx', 'scripts/stripe-bootstrap.ts'], { env: { ...process.env, STRIPE_SECRET_KEY: '' }, cwd: process.cwd() })` and assert exit code !== 0 + stderr contains `STRIPE_SECRET_KEY not set`.
    2. **Live API path gated on HAS_STRIPE** — `describeIfStripe = process.env.HAS_STRIPE === 'true' ? describe : describe.skip;`. Inside, an `it('runs end-to-end against Stripe test mode and prints 5 IDs', async () =&gt; { ... })` that spawns the script with `STRIPE_SECRET_KEY` from env and asserts stdout matches `/STRIPE_PRICE_PLUS_MONTHLY=price_/` etc for all 5 lines. CI runs without HAS_STRIPE → the live test skips; local dev sets HAS_STRIPE=true to verify end-to-end.

    Imports for the smoke test: `import { describe, it, expect } from 'vitest';` + `import { spawnSync } from 'node:child_process';`. Keep timeout default-ish — env-guard test runs in &lt;500ms; live test needs ~15s for Stripe round-trips so add `it('...', { timeout: 30000 }, async () =&gt; { ... })` on the live case.

    **Step 2d — Verify install + types resolve:**
    Run `cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npx tsc --noEmit -p tsconfig.app.json 2&gt;&amp;1 | grep -i 'scripts/stripe-bootstrap' || echo OK`. If tsconfig.app.json excludes `scripts/**`, add a one-line entry to `tsconfig.app.json` `"include"` or create a minimal `scripts/tsconfig.json` extending the root — but ONLY if tsc errors. If tsc is clean from current config (likely, since `scripts/` is currently outside `src/`), do nothing.

    **DO NOT** commit yet — Task 4 commits all files together.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- scripts/stripe-bootstrap.test.ts --run 2>&amp;1 | tail -15 &amp;&amp; STRIPE_SECRET_KEY= npx tsx scripts/stripe-bootstrap.ts 2>&amp;1 | grep -c 'STRIPE_SECRET_KEY not set'</automated>
  </verify>
  <done>
    - `npm test -- scripts/stripe-bootstrap.test.ts --run` exits 0; env-guard test passes; live test SKIPPED (HAS_STRIPE unset in CI).
    - `STRIPE_SECRET_KEY= npx tsx scripts/stripe-bootstrap.ts` exits with code 1 and stderr contains `STRIPE_SECRET_KEY not set`.
    - `grep -E '"(tsx|stripe)"' leanshot/package.json` returns both with version pins in `devDependencies` section.
    - `grep -c "ensureProduct\|ensurePrice\|ensureMeter" leanshot/scripts/stripe-bootstrap.ts` returns ≥6 (3 function defs + ≥3 call sites).
    - `grep -c "apiVersion: '2026-04-22.dahlia'" leanshot/scripts/stripe-bootstrap.ts` returns 1.
    - `grep -c "unit_amount: 1299\|unit_amount: 13249\|unit_amount: 9900\|unit_amount: 900" leanshot/scripts/stripe-bootstrap.ts` returns 4.
    - `grep -c "STRIPE_PRICE_PLUS_MONTHLY=\|STRIPE_PRICE_PLUS_YEARLY=\|STRIPE_PRICE_CLINIC_BASE=\|STRIPE_PRICE_CLINIC_OVERAGE=\|STRIPE_METER_ACTIVE_PATIENTS=" leanshot/scripts/stripe-bootstrap.ts` returns 5 (all 5 stdout label lines present).
    - `grep -l '@stripe/stripe-js' leanshot/src/ -r 2>/dev/null | wc -l` returns 0 (Pattern G: no static import of the browser SDK yet).
  </done>
</task>

<task type="auto">
  <name>Task 3: Document 7 new Stripe env vars in leanshot/.env.example</name>
  <files>leanshot/.env.example</files>
  <action>
    Append a Phase 14 section to leanshot/.env.example below the existing Phase 8 section. Use the section-header style already established in the file (=== bordered comments + per-var inline comment + source-of-decision reference).

    Add EXACTLY these 8 lines of placeholder env (7 server-side + 1 VITE_-prefixed publishable):

    ```
    # =============================================================================
    # Phase 14 — Stripe monetization (MONEY-01..05, 08, 09)
    # Source: 14-CONTEXT.md D-09 (web pricing), D-10 (clinic pricing), D-11
    #         (bootstrap script), D-16 (CSP additions); 14-RESEARCH.md §"Bootstrap
    #         script" lines 523-585.
    # Fill in STRIPE_PRICE_* + STRIPE_METER_* after running:
    #   cd leanshot &amp;&amp; STRIPE_SECRET_KEY=sk_test_... npx tsx scripts/stripe-bootstrap.ts
    # Then copy the stdout block into .env.local + Vercel env + Supabase Function
    # secrets (per [[feedback_cli_over_paste_back]] — Stripe MCP-fetchable).
    # NEVER prefix STRIPE_SECRET_KEY or STRIPE_WEBHOOK_SECRET with VITE_ —
    # server-only secrets must not enter the browser bundle.
    # =============================================================================

    # Stripe API secret key (server-only — Edge Functions + bootstrap script)
    STRIPE_SECRET_KEY=sk_test_...

    # Stripe webhook signing secret (server-only — supabase/functions/stripe-webhook)
    STRIPE_WEBHOOK_SECRET=whsec_...

    # Stripe publishable key (BROWSER-safe — only Phase 14 var with VITE_ prefix).
    # Today: unused (Upgrade CTA uses plain `window.location.href` per Pitfall 10
    # to keep @stripe/stripe-js out of the bundle). Reserved for a future plan
    # that opts into Stripe.js for embedded Payment Element flows.
    VITE_STRIPE_PUBLIC_KEY=pk_test_...

    # Price IDs — filled after running scripts/stripe-bootstrap.ts (D-09 + D-10).
    # Web tier ($12.99/mo, $132.49/yr — 15% off):
    STRIPE_PRICE_PLUS_MONTHLY=price_...
    STRIPE_PRICE_PLUS_YEARLY=price_...
    # Clinic tier ($99/mo base + $9/active-patient overage, metered):
    STRIPE_PRICE_CLINIC_BASE=price_...
    STRIPE_PRICE_CLINIC_OVERAGE=price_...

    # Billing Meter ID for clinic active-patient overage (mtr_xxx) — D-10 / RESEARCH §"Billing Meters".
    STRIPE_METER_ACTIVE_PATIENTS=mtr_...
    ```

    Pinning notes:
    - Section header style mirrors Phase 1 / Phase 2 / Phase 8 sections already in the file (=== borders, multi-line comments, source attribution).
    - `VITE_STRIPE_PUBLIC_KEY` is the ONLY VITE_-prefixed Stripe var per the orchestrator instruction (Pitfall 10 — Hosted Checkout doesn't need `@stripe/stripe-js`; the var is reserved for future use, not consumed in Phase 14).
    - All 4 STRIPE_PRICE_* placeholders + 1 STRIPE_METER_* placeholder map 1:1 to the bootstrap script's stdout output (Task 2 / Step 2b labels).

    **DO NOT** commit yet — Task 4 commits the full set.
  </action>
  <verify>
    <automated>grep -c "^STRIPE_\|^VITE_STRIPE_" /Users/karstenhaldan/minisite/leanshot/.env.example</automated>
  </verify>
  <done>
    - The grep above returns 8 (STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_PLUS_MONTHLY, STRIPE_PRICE_PLUS_YEARLY, STRIPE_PRICE_CLINIC_BASE, STRIPE_PRICE_CLINIC_OVERAGE, STRIPE_METER_ACTIVE_PATIENTS, VITE_STRIPE_PUBLIC_KEY).
    - `grep -c "^VITE_STRIPE" leanshot/.env.example` returns exactly 1 (only the publishable key carries the VITE_ prefix).
    - `grep -c "Phase 14 — Stripe monetization" leanshot/.env.example` returns 1 (section-header sentinel).
    - `grep -c "stripe-bootstrap.ts" leanshot/.env.example` returns ≥1 (runbook reference in the comment).
  </done>
</task>

<task type="auto">
  <name>Task 4: Atomic pathspec commit of all five Phase 14-02 files (Pattern H + Pattern F)</name>
  <files>leanshot/vercel.json, leanshot/tests/csp/csp-snapshot.txt, leanshot/scripts/stripe-bootstrap.ts, leanshot/scripts/stripe-bootstrap.test.ts, leanshot/.env.example, leanshot/package.json, leanshot/package-lock.json</files>
  <action>
    Stage and commit all Phase 14-02 files in ONE atomic commit using pathspec form per [[feedback_parallel_executor_git_isolation]] (this plan runs in Wave 1 parallel with 14-01, whose executor is editing supabase/migrations/* + leanshot/tests/rls/* + leanshot/tests/sql/* in the same shared git index — pathspec discipline prevents sibling-plan file sweep).

    From `/Users/karstenhaldan/minisite` (git toplevel, per `git rev-parse --show-toplevel`):

    Pre-flight sanity-check that the files differ from HEAD:
    ```
    git status --short -- leanshot/vercel.json leanshot/tests/csp/csp-snapshot.txt leanshot/scripts/stripe-bootstrap.ts leanshot/scripts/stripe-bootstrap.test.ts leanshot/.env.example leanshot/package.json leanshot/package-lock.json
    ```
    Expect 7 lines (M or A or ??). If any file is missing/clean, the prior task didn't land — abort and report back.

    Single atomic commit (Pattern H + Pattern F):
    ```
    git commit -- \
      leanshot/vercel.json \
      leanshot/tests/csp/csp-snapshot.txt \
      leanshot/scripts/stripe-bootstrap.ts \
      leanshot/scripts/stripe-bootstrap.test.ts \
      leanshot/.env.example \
      leanshot/package.json \
      leanshot/package-lock.json \
      -m "feat(14-02): CSP widen + Stripe bootstrap script + env wiring (D-09/D-10/D-11/D-16)

    - Widen vercel.json CSP for Stripe: +script-src js.stripe.com,
      +connect-src api.stripe.com m.stripe.network, frame-src 'self'
      + js.stripe.com + hooks.stripe.com (was 'none'). Mirror in
      tests/csp/csp-snapshot.txt — atomic-commit per Pattern H.
    - Add idempotent scripts/stripe-bootstrap.ts: ensureProduct /
      ensurePrice / ensureMeter helpers; creates LeanShot Plus
      (\$12.99/mo, \$132.49/yr) + LeanShot Clinic (\$99 base +
      \$9/active-patient overage, metered via active_patients meter).
      Pinned apiVersion 2026-04-22.dahlia. Outputs 5 IDs (4 prices +
      1 meter) for paste into .env.example + Vercel + Supabase secrets
      per feedback_cli_over_paste_back.
    - Vitest smoke (scripts/stripe-bootstrap.test.ts) asserts env-guard;
      live-API path gated on HAS_STRIPE.
    - Document 7 new Stripe env vars + VITE_STRIPE_PUBLIC_KEY in
      .env.example. NO @stripe/stripe-js static import yet (Pitfall 10
      — Hosted Checkout uses plain window.location.href).
    - Add tsx + stripe v17 as devDependencies (script-only — never
      enters production bundle)."
    ```

    Note: The commit message uses `\$` escapes for shell so the dollar signs in pricing don't get clobbered by bash variable expansion when this heredoc-equivalent runs. If your shell handles literal `$` fine in `-m` strings, plain `$12.99` is also fine — the goal is the literal `$12.99` ends up in the commit message body.

    Post-commit verification (do NOT push — Wave 1 sibling 14-01 needs to land its own commit on the same branch / worktree first):
    ```
    git log -1 --stat -- leanshot/vercel.json leanshot/tests/csp/csp-snapshot.txt leanshot/scripts/stripe-bootstrap.ts leanshot/scripts/stripe-bootstrap.test.ts leanshot/.env.example leanshot/package.json leanshot/package-lock.json
    ```
    Expect exactly 7 files in the latest commit's stat. If a stray file (e.g. from 14-01) appears, the pathspec contract failed — investigate and reset.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && git log -1 --name-only --pretty=format: HEAD | grep -v '^$' | sort | diff - <(printf "leanshot/.env.example\nleanshot/package-lock.json\nleanshot/package.json\nleanshot/scripts/stripe-bootstrap.test.ts\nleanshot/scripts/stripe-bootstrap.ts\nleanshot/tests/csp/csp-snapshot.txt\nleanshot/vercel.json\n" | sort)</automated>
  </verify>
  <done>
    - HEAD commit touches EXACTLY the 7 expected pathspec files (the diff above is empty).
    - `git log -1 --pretty=%B HEAD | grep -c "14-02"` returns ≥1 (commit subject is properly scoped).
    - `git log -1 --pretty=%B HEAD | grep -cE "D-(09|10|11|16)"` returns ≥1 (decision IDs cited).
    - `npm test -- tests/csp/csp-snapshot.test.ts scripts/stripe-bootstrap.test.ts --run` exits 0 post-commit (full regression check on the committed state).
  </done>
</task>

</tasks>

<verification>
End-to-end checks that prove the plan landed:

1. **CSP atomic-commit contract (Pattern H — plan-checker BLOCKER if missing):**
   - `cd /Users/karstenhaldan/minisite && git log -1 --name-only HEAD | grep -cE "leanshot/(vercel\.json|tests/csp/csp-snapshot\.txt)"` returns 2 (BOTH files in the HEAD commit).
   - `cd /Users/karstenhaldan/minisite/leanshot && npm test -- tests/csp/csp-snapshot.test.ts --run` exits 0.

2. **CSP widening sanity (per D-16):**
   - `grep -c "js.stripe.com" leanshot/vercel.json` returns 2 (script-src + frame-src).
   - `grep -c "api.stripe.com\|m.stripe.network" leanshot/vercel.json` returns 2.
   - `grep -c "frame-src 'none'" leanshot/vercel.json` returns 0 (replaced).

3. **Bootstrap script idempotency surface:**
   - `grep -cE "ensureProduct|ensurePrice|ensureMeter" leanshot/scripts/stripe-bootstrap.ts` returns ≥6 (3 defs + ≥3 call sites).
   - `grep -cE "\.list\(|\.find\(|\?\?" leanshot/scripts/stripe-bootstrap.ts` returns ≥6 (search-before-create idiom on all three helpers).

4. **Pricing locked (per D-09 + D-10):**
   - `grep -c "unit_amount: 1299" leanshot/scripts/stripe-bootstrap.ts` returns 1 (Plus monthly $12.99).
   - `grep -c "unit_amount: 13249" leanshot/scripts/stripe-bootstrap.ts` returns 1 (Plus yearly $132.49).
   - `grep -c "unit_amount: 9900" leanshot/scripts/stripe-bootstrap.ts` returns 1 (Clinic base $99).
   - `grep -c "unit_amount: 900" leanshot/scripts/stripe-bootstrap.ts` returns 1 (Clinic overage $9).

5. **Billing Meter shape (per RESEARCH §Pattern 5):**
   - `grep -c "stripe.billing.meters" leanshot/scripts/stripe-bootstrap.ts` returns ≥2 (list + create).
   - `grep -c "customer_mapping" leanshot/scripts/stripe-bootstrap.ts` returns ≥1.
   - `grep -c "usage_type: 'metered'" leanshot/scripts/stripe-bootstrap.ts` returns 1 (clinic overage price links to meter).
   - `grep -c "usage_records" leanshot/scripts/stripe-bootstrap.ts` returns 0 (legacy API NOT used — Billing Meters API only, per Pitfall 1).

6. **Env-guard fires (smoke test):**
   - `cd /Users/karstenhaldan/minisite/leanshot && STRIPE_SECRET_KEY= npx tsx scripts/stripe-bootstrap.ts 2>&1 | grep -c "STRIPE_SECRET_KEY not set"` returns ≥1.
   - Exit code is non-zero (`echo $?` after the above returns 1).

7. **.env.example documents 8 Stripe vars (7 server + 1 publishable):**
   - `grep -cE "^STRIPE_|^VITE_STRIPE_" leanshot/.env.example` returns 8.
   - `grep -c "^VITE_STRIPE" leanshot/.env.example` returns 1 (only publishable key is browser-exposed).

8. **Pattern G (bundle isolation — no `@stripe/stripe-js` static import yet):**
   - `grep -rl '@stripe/stripe-js' leanshot/src/ 2>/dev/null | wc -l` returns 0.

9. **devDependencies scope:**
   - `node -e "const p=require('./leanshot/package.json'); process.exit(p.devDependencies?.tsx && p.devDependencies?.stripe ? 0 : 1)"` exits 0.
   - `node -e "const p=require('./leanshot/package.json'); process.exit(p.dependencies?.stripe || p.dependencies?.tsx ? 1 : 0)"` exits 0 (NEITHER in production deps).
</verification>

<success_criteria>
Plan 14-02 is complete when:
- [x] Both `leanshot/vercel.json` (CSP header) and `leanshot/tests/csp/csp-snapshot.txt` are updated in ONE commit (Pattern H — plan-checker BLOCKER else).
- [x] `cd leanshot && npm test -- tests/csp/csp-snapshot.test.ts --run` passes.
- [x] `leanshot/scripts/stripe-bootstrap.ts` exists, exports `main` + `ensureProduct` + `ensurePrice` + `ensureMeter`, and is idempotent (search-before-create on every helper).
- [x] Running `STRIPE_SECRET_KEY=sk_test_... npx tsx leanshot/scripts/stripe-bootstrap.ts` exits 0 and prints exactly 5 ID lines (4 STRIPE_PRICE_* + 1 STRIPE_METER_*) — observable end-to-end with `HAS_STRIPE=true npm test -- scripts/stripe-bootstrap.test.ts`.
- [x] Running the same script with `STRIPE_SECRET_KEY=` (unset) exits non-zero with a clear error referencing `STRIPE_SECRET_KEY not set`.
- [x] `leanshot/.env.example` documents 8 Stripe env vars (7 server-side STRIPE_* + 1 VITE_STRIPE_PUBLIC_KEY) under a clearly labeled Phase 14 section.
- [x] `tsx` and `stripe` are pinned in `leanshot/package.json` `devDependencies` (NOT `dependencies`).
- [x] No `@stripe/stripe-js` static import exists anywhere under `leanshot/src/` (Pattern G — Pitfall 10).
- [x] HEAD commit's name-only list equals EXACTLY the 7 pathspec files (Pattern F — no sibling sweep from Wave 1 parallel 14-01).
- [x] Commit subject scopes the change to `14-02` and the body cites D-09 / D-10 / D-11 / D-16.

**MVP-mode observable capability after this plan:** A developer can run the bootstrap script with `STRIPE_SECRET_KEY` set and produce 5 live Stripe IDs in <30s; the production CSP allows browsers to load `js.stripe.com` and reach `api.stripe.com` — both prerequisites for Wave 2 plans 14-03 (webhook) and 14-04 (checkout endpoint).
</success_criteria>

<output>
After completion, create `.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-02-SUMMARY.md` documenting:
- The exact CSP delta committed (lines added/removed in both files).
- Whether the live `npx tsx scripts/stripe-bootstrap.ts` run was performed against a Stripe test account; if yes, the 5 IDs produced (price_xxx / mtr_xxx) — to land in Vercel env + Supabase Function secrets via [[feedback_cli_over_paste_back]] (Stripe MCP-fetchable).
- Any deviation from the planned pathspec (should be none).
- Open follow-ups handed off to plan 14-04 (Checkout endpoint consumes these price IDs).
</output>
