---
phase: 14-monetization-foundation-stripe-web-clinic-seats
plan: 06
type: execute
wave: 4
depends_on: ["14-04", "14-05"]
files_modified:
  - leanshot/src/components/billing/PastDueBanner.tsx
  - leanshot/src/components/billing/PastDueBanner.test.tsx
  - leanshot/src/components/billing/ManageSubscriptionLink.tsx
  - leanshot/src/components/billing/ManageSubscriptionLink.test.tsx
  - leanshot/src/components/billing/UpgradeCTA.tsx
  - leanshot/src/components/billing/UpgradeCTA.test.tsx
  - leanshot/src/components/layout/AppShell.tsx
  - leanshot/src/components/dashboard/settings/SettingsPage.tsx
autonomous: true
requirements: [MONEY-03, MONEY-09]

must_haves:
  truths:
    - "Free user clicks Upgrade in Settings → Stripe Checkout opens (window.location.href = session.url)."
    - "Paid user clicks Manage subscription in Settings → Stripe Customer Portal opens."
    - "Past-due user sees warm-orange banner above every dashboard view; clicking Update card opens Stripe Customer Portal."
    - "Banner does NOT render for tier='free' or tier='paid' — zero layout shift in non-past_due states."
    - "Zero static @stripe/stripe-js imports anywhere under leanshot/src/."
    - "Zero hex literals in net-new billing components (Pattern D)."
    - "Bundle ceiling (index ≤50 kB gz) preserved; net delta from this plan ≤0.5 kB gz."
    - "Reduced-motion users see banner with no slide-in animation."
  artifacts:
    - path: "leanshot/src/components/billing/PastDueBanner.tsx"
      provides: "Global chrome banner; renders only when tier='past_due'"
      contains: 'role="alert"'
    - path: "leanshot/src/components/billing/ManageSubscriptionLink.tsx"
      provides: "Settings row that fetches portal URL from stripe-checkout/portal and opens it"
      contains: "ManageSubscriptionLink"
    - path: "leanshot/src/components/billing/UpgradeCTA.tsx"
      provides: "Settings header CTA that POSTs to stripe-checkout/session and redirects"
      contains: "UpgradeCTA"
    - path: "leanshot/src/components/layout/AppShell.tsx"
      provides: "Renders <PastDueBanner /> above main content"
      contains: "PastDueBanner"
    - path: "leanshot/src/components/dashboard/settings/SettingsPage.tsx"
      provides: "Subscription section renders UpgradeCTA (free) or ManageSubscriptionLink (paid/past_due) + status pill"
      contains: "ManageSubscriptionLink"
  key_links:
    - from: "PastDueBanner.tsx"
      to: "useStore selector s.tier"
      via: "useStore((s) => s.tier) === 'past_due'"
      pattern: "useStore\\(\\(s\\) => s\\.tier\\)"
    - from: "ManageSubscriptionLink.tsx"
      to: "stripe-checkout Edge Function portal endpoint (14-04)"
      via: "fetch('/functions/v1/stripe-checkout/portal') → window.open(url)"
      pattern: "stripe-checkout.*portal"
    - from: "UpgradeCTA.tsx"
      to: "stripe-checkout Edge Function session endpoint (14-04)"
      via: "fetch session → window.location.href = url"
      pattern: "window\\.location\\.href"
    - from: "AppShell.tsx"
      to: "PastDueBanner"
      via: "import + render above topbar"
      pattern: "<PastDueBanner"
    - from: "SettingsPage.tsx (subscription section)"
      to: "UpgradeCTA / ManageSubscriptionLink"
      via: "conditional render by tier"
      pattern: "(UpgradeCTA|ManageSubscriptionLink)"
---

<objective>
Ship the dashboard chrome + Settings management slice that closes the user-observable monetization loop. Three net-new components (`PastDueBanner`, `ManageSubscriptionLink`, `UpgradeCTA`) + two surgical edits (`AppShell.tsx`, `SettingsPage.tsx`).

Purpose: After 14-04 (Edge Function) and 14-05 (tier slice + TierGate primitive) land, the user has tier state in the store but no way to (a) start a subscription, (b) manage one, or (c) recover from a past_due lapse. This plan wires those three flows into the existing dashboard chrome so a real human can subscribe, manage, and recover from card failure.

Output: PastDueBanner (warm-orange `role="alert"` strip), ManageSubscriptionLink (Settings Subscription-section row), UpgradeCTA (Settings Subscription-section header CTA), AppShell wiring (banner global chrome), SettingsPage Subscription section rewrite. Vitest tests + bundle/grep gates.

MVP discipline: user-observable at plan close = free user clicks Upgrade → Stripe Checkout opens; paid user clicks Manage subscription → Stripe Portal opens; past-due user sees orange banner globally + can recover via Portal. No upstream gating left after this plan to start/manage/recover a subscription.

Honors D-08 (past_due always-on chrome), Pattern G (bundle isolation — NO `@stripe/stripe-js` static import), Pattern D (token consumption — no hex literals), Pattern E (reduced-motion gating), Pattern F (pathspec discipline for Wave 4 parallel with 14-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@leanshot/CLAUDE.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-CONTEXT.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-RESEARCH.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-PATTERNS.md
@leanshot/.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-PLAN-OUTLINE.md
@leanshot/src/components/ui/Toast.tsx
@leanshot/src/components/ui/Card.tsx
@leanshot/src/components/layout/AppShell.tsx
@leanshot/src/components/dashboard/settings/SettingsPage.tsx
@leanshot/src/hooks/useReducedMotion.ts

<interfaces>
<!-- These are contracts executor MUST use directly. NO codebase exploration required. -->

From Phase 14-05 (precedes this plan in Wave 3) — `leanshot/src/lib/billing.ts`:
```typescript
export function getActiveTier(state: { tier?: Tier | null; paid_until?: string | null }): Tier;
```

From Phase 14-05 — `leanshot/src/lib/store.ts` partialize additions:
```typescript
tier: 'free' | 'paid' | 'past_due';      // default 'free'
paid_until: string | null;                 // ISO timestamp
plan_id: string | null;                    // Stripe price ID
provider: 'stripe' | 'revenuecat' | null;  // default null
```

Read it via: `useStore((s) => s.tier)`. Never `useStore(s => s)` (per CLAUDE.md anti-pattern).

From Phase 14-04 (Wave 2) — Edge Function endpoints (Supabase Functions base path):
- POST `/functions/v1/stripe-checkout/session` → JSON body `{ plan: 'plus_monthly' | 'plus_yearly' }` → returns `{ url: string, session_id: string }`. JWT-authed via supabase client.
- POST `/functions/v1/stripe-checkout/portal` → JSON body `{}` → returns `{ url: string }` (Stripe Customer Portal session). JWT-authed.

Both endpoints accept the user's Supabase access token via `Authorization: Bearer <jwt>` header. Use the existing supabase-js client: `import { supabase } from '@/lib/supabase';` then read session via `await supabase.auth.getSession()` and POST manually OR use `supabase.functions.invoke('stripe-checkout/session', { body: {...} })` (preferred — handles auth header automatically). Check `clinic-invite` consumers (e.g. `leanshot/src/lib/auth.ts` or grep for `supabase.functions.invoke`) for the canonical invoke pattern in this codebase.

From Phase 13 design tokens (CSS vars, NOT hex literals — Pattern D):
- `var(--color-warning)` — warm-orange (past-due banner background/accent)
- `var(--color-warning-foreground)` — foreground on warning (text/icon contrast)
- `var(--color-primary)` / `var(--color-primary-foreground)` — Upgrade CTA button
- `var(--color-surface)` / `var(--color-surface-elevated)` — Card surfaces
- `var(--color-text)` / `var(--color-text-secondary)` / `var(--color-text-tertiary)`
- `var(--color-border)` — separators
- `var(--color-success)` — "Active" pill
- `var(--color-bg)` — page background

If any of `--color-warning` / `--color-warning-foreground` is not defined in `leanshot/src/index.css` `@theme {}`, that is a Phase 13 token-system bug — surface in SUMMARY rather than silently introducing a hex literal. Phase 13 token presence verified by 14-PATTERNS.md analog (Card.tsx StatTile uses `var(--color-warning)`).

From CLAUDE.md project conventions:
- Path alias: `@/*` → `./src/*` — import as `@/components/...`, `@/lib/...`, `@/hooks/...`.
- Component naming: `PascalCase.tsx` matching primary export.
- Hooks: `camelCase` `use`-prefix.
- Modal/sheet ARIA: `role="dialog"` + `aria-modal="true"`. Toasts: `role="status"` + `aria-live="polite"`. **Banner (this plan) uses `role="alert"`** — payment failure is a warning, not a passive status. WCAG SC 1.3.1 + 4.1.3 + AGoT pattern: `alert` fires assertive announcement; `status` fires polite. Past-due IS assertive.
- ESLint flat config blocks `useStore(generateInsights|pickFocus)` and `useStore((s) => generateInsights|pickFocus(s)…)`. Not relevant here but cite as proof the selector contract is enforced by tooling.
- Reduced motion: every animated component checks `useReducedMotion()` first. CSS-level reduced motion enforced via `index.css`.

From `leanshot/src/hooks/useReducedMotion.ts:8`:
```typescript
export function useReducedMotion(): boolean;
```

From `leanshot/src/components/dashboard/settings/SettingsPage.tsx:544-553` (the slot to replace):
```tsx
{section === 'subscription' && (
  <Section title="Subscription" body="Free forever. Pro adds polish.">
    <Card variant="flat">
      <p className="text-[14px] font-semibold mb-1">You&apos;re on the Free plan.</p>
      <p className="text-[13px] text-[var(--color-text-secondary)]">
        All 9 dashboard tabs, unlimited tracking, and one progress card template.
      </p>
    </Card>
  </Section>
)}
```
This stub is what we replace with the tier-conditional Subscription section.

From `leanshot/src/components/layout/AppShell.tsx:42-64` — `<main>` wraps `<WorkspaceSwitcher>` + `<Topbar>` + `<EmailVerificationBanner>` + `{children}`. PastDueBanner slots ABOVE `<WorkspaceSwitcher>` (banner is global chrome — first thing the user sees inside `<main>`, before WorkspaceSwitcher / Topbar / view content). Keep the existing `EmailVerificationBanner` pattern as a precedent for an inline self-hiding chrome strip — they coexist (different conditions: email-verification vs past_due).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build PastDueBanner with role="alert" + reduced-motion gate + Vitest tests</name>
  <files>leanshot/src/components/billing/PastDueBanner.tsx, leanshot/src/components/billing/PastDueBanner.test.tsx</files>
  <behavior>
    - Renders nothing when tier !== 'past_due' (free or paid).
    - Renders a warm-orange strip with role="alert" + aria-live="assertive" when tier === 'past_due'.
    - Message: "Your payment failed. Update your card to keep LeanShot Plus active."
    - "Update card" button opens portal URL in a NEW TAB via window.open(url, '_blank', 'noopener,noreferrer'). URL fetched on click from the 14-04 portal endpoint via supabase.functions.invoke('stripe-checkout/portal', { body: {} }).
    - While the portal fetch is in-flight, the button shows aria-busy="true" and a "Opening Stripe…" label; multiple rapid clicks are deduped (button disabled during flight).
    - On fetch error: show inline error span (role="alert" already on outer, so use a child <span role="status">) "Couldn't open Stripe. Try again." — do NOT echo upstream error message (per share/index.ts:379-383 pattern).
    - useReducedMotion() === true → render banner as a static block (no slide-in transition). useReducedMotion() === false → AnimatePresence slide-in (initial y:-8, animate y:0, exit y:-4, 0.25s).
    - Vitest tests:
      * Renders null when tier='free'.
      * Renders null when tier='paid'.
      * Renders banner with role="alert" when tier='past_due'.
      * Clicking Update card calls supabase.functions.invoke with 'stripe-checkout/portal' AND opens window.open with returned url. (Mock window.open + functions.invoke.)
      * Reduced-motion fallback: when useReducedMotion mock returns true, banner is in the DOM but no motion.div / no animation props. Verify by mocking the hook + asserting className OR data-testid presence + NO framer-motion AnimatePresence wrapper (assertion technique: render with reduced-motion mock + render without; assert serialized output differs in the motion wrapper layer OR use a structural assertion like the outer node having a specific data-attribute).
  </behavior>
  <action>
    Create `leanshot/src/components/billing/PastDueBanner.tsx`:
    - Default export the `PastDueBanner` function component (no props).
    - Imports: `useState` from 'react'; `motion, AnimatePresence` from 'framer-motion'; `AlertTriangle` icon from 'lucide-react'; `useStore` from '@/lib/store'; `useReducedMotion` from '@/hooks/useReducedMotion'; `supabase` from '@/lib/supabase'; `cn` from '@/lib/helpers'.
    - Read `tier = useStore((s) => s.tier)`. If tier !== 'past_due' return null (per D-08; banner is independent of TierGate).
    - State: `loading` (boolean), `error` (string | null).
    - Handler `handleUpdateCard`: set loading=true, try { const { data, error: invokeErr } = await supabase.functions.invoke('stripe-checkout/portal', { body: {} }); if (invokeErr || !data?.url) throw new Error('no-url'); window.open(data.url, '_blank', 'noopener,noreferrer'); } catch { setError("Couldn't open Stripe. Try again."); } finally { setLoading(false); }.
    - Render structure:
      * If `useReducedMotion()` returns true → plain `<div role="alert" aria-live="assertive">` block.
      * Else → wrap in `<AnimatePresence><motion.div role="alert" aria-live="assertive" initial={{ opacity: 0, y: -8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -4 }} transition={{ duration: 0.25, ease: [0.25, 1, 0.5, 1] }}>`.
    - Outer styling MUST consume Phase 13 tokens ONLY (Pattern D — NO hex literals). Tailwind arbitrary-value syntax with CSS vars: `bg-[var(--color-warning)] text-[var(--color-warning-foreground)] border-b border-[var(--color-border)] px-4 py-2.5 text-[13px] font-semibold flex items-center justify-between gap-3`. The Toast.tsx success/error/info bg-tokens are precedent (Toast.tsx:34-36) — same shape, just `--color-warning` instead.
    - Left side: `<AlertTriangle className="size-4 shrink-0" aria-hidden="true" />` + `<span>Your payment failed. Update your card to keep LeanShot Plus active.</span>`.
    - Right side: Button (use existing `@/components/ui/Button` `variant="ghost"` or build inline — match existing chrome bar density; if Button is too big, use a plain `<button>` with the same focus-ring tokens as the FAB at AppShell.tsx:78). Label: loading ? "Opening Stripe…" : "Update card". aria-busy={loading}. disabled={loading}. onClick={handleUpdateCard}.
    - If `error` is set, render below the message in a smaller span with `role="status"` aria-live="polite".
    - Implement per D-08 (always-visible at top of every dashboard view, regardless of TierGate state) + Pattern E (reduced-motion gating from 14-PATTERNS.md cross-cutting C).
    - NO `@stripe/stripe-js` import (Pattern G — Pitfall 10). NO hex literals (Pattern D). Verify visually.
    - Create `leanshot/src/components/billing/PastDueBanner.test.tsx`:
      * Use `@testing-library/react` (project standard — check existing `*.test.tsx` for the import line if uncertain; grep `from '@testing-library/react'` under `leanshot/src/` to confirm).
      * Mock `@/lib/store` with `vi.mock('@/lib/store', () => ({ useStore: vi.fn() }))` and reset between tests. Each test sets `useStore.mockImplementation((selector) => selector({ tier: 'past_due' }))`.
      * Mock `@/hooks/useReducedMotion` similarly.
      * Mock `@/lib/supabase` with `{ functions: { invoke: vi.fn() } }`.
      * Mock `window.open` with `vi.spyOn(window, 'open').mockImplementation(() => null)`.
      * 5 test cases per behavior above.
    - Do NOT pre-wire AppShell here; Task 4 owns that wiring (keeps tasks file-disjoint internally for review). Task 1's `<verify>` test ensures component-level correctness before wiring.
  </action>
  <verify>
    <automated>cd leanshot &amp;&amp; npx vitest run src/components/billing/PastDueBanner.test.tsx --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    File `leanshot/src/components/billing/PastDueBanner.tsx` exists with role="alert" + tier='past_due' guard + supabase.functions.invoke('stripe-checkout/portal') wiring + useReducedMotion fallback. 5 Vitest tests pass. Zero hex literals. Zero @stripe/stripe-js imports.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build ManageSubscriptionLink + UpgradeCTA + Vitest tests</name>
  <files>leanshot/src/components/billing/ManageSubscriptionLink.tsx, leanshot/src/components/billing/ManageSubscriptionLink.test.tsx, leanshot/src/components/billing/UpgradeCTA.tsx, leanshot/src/components/billing/UpgradeCTA.test.tsx</files>
  <behavior>
    ManageSubscriptionLink:
    - Settings section row that renders a Card with a CreditCard icon, "Manage subscription" title, brief description ("Update card, change plan, or cancel."), and a Button ("Open Stripe").
    - On click: supabase.functions.invoke('stripe-checkout/portal', { body: {} }) → on success window.location.href = data.url (per D-08 banner uses target=_blank; THIS link is in Settings drawer, opens in SAME tab so user returns naturally via Stripe's "Return to LeanShot" → success_url=/settings?from=portal per 14-CONTEXT D-08).
    - Loading state with aria-busy; error inline below the row using role="status".

    UpgradeCTA:
    - Renders nothing when tier !== 'free' (paid + past_due users do NOT see "Upgrade" — past_due sees Manage; paid sees Manage).
    - Renders a primary-token Button "Upgrade to Plus" with optional plan-select dropdown (D-09: monthly + yearly 15% off). Minimal v1: 2 buttons side-by-side ("$12.99/mo" + "$132.49/yr — save 15%") OR a single button + a small sub-link to switch between plans. PLANNER DISCRETION: pick whichever is fewer DOM nodes; both plans hit the same endpoint with different body.plan.
    - On click: supabase.functions.invoke('stripe-checkout/session', { body: { plan: 'plus_monthly' | 'plus_yearly' } }) → on success window.location.href = data.url. (Same-tab redirect — Hosted Checkout returns to success_url after card entry.)
    - NO `@stripe/stripe-js` import. Plain `window.location.href = url` (Pattern G — Pitfall 10).

    Vitest tests (both files):
    - ManageSubscriptionLink: button click invokes 'stripe-checkout/portal' AND sets window.location.href; loading state asserts aria-busy.
    - UpgradeCTA: renders null when tier='paid'; renders null when tier='past_due'; renders 2 plan buttons when tier='free'; clicking monthly button invokes session endpoint with { plan: 'plus_monthly' } AND sets window.location.href; same for yearly with { plan: 'plus_yearly' }.
    - Reduced-motion fallback: both components have no animation by default (they are buttons + Card, not animated chrome), but assert via render-no-error that useReducedMotion mock can be toggled without crashing. Light test; the real reduced-motion-fallback coverage is Task 1's banner.
  </behavior>
  <action>
    Create `leanshot/src/components/billing/ManageSubscriptionLink.tsx`:
    - Default export `ManageSubscriptionLink` function component, no props.
    - Imports: `useState` from 'react'; `CreditCard` from 'lucide-react' (already imported by SettingsPage analog at line 6 — same project icon set); `Card` from '@/components/ui/Card'; `Button` from '@/components/ui/Button'; `useStore` from '@/lib/store'; `supabase` from '@/lib/supabase'.
    - `loading` + `error` state.
    - Handler invokes `supabase.functions.invoke('stripe-checkout/portal', { body: {} })`; on success `window.location.href = data.url` (SAME tab — Settings UX); on error setError("Couldn't open Stripe. Try again.") (no upstream echo).
    - Render: `<Card>` wrapping a flex row: `<CreditCard className="size-5 text-[var(--color-text-secondary)]" aria-hidden="true" />` + 2-column text block ("Manage subscription" + helper text) + `<Button variant="primary" onClick={handleClick} aria-busy={loading} disabled={loading}>{loading ? 'Opening Stripe…' : 'Open Stripe'}</Button>`. Tokens-only per Pattern D.
    - Error below: small text-[var(--color-danger)] span with role="status" if error.

    Create `leanshot/src/components/billing/UpgradeCTA.tsx`:
    - Default export `UpgradeCTA` function component, no props.
    - Read tier = useStore((s) => s.tier). If tier !== 'free' return null.
    - `loading` (per-plan: 'monthly' | 'yearly' | null) + `error` state.
    - Handler `startCheckout(plan: 'plus_monthly' | 'plus_yearly')`: setLoading(planTag); invoke session endpoint with body { plan }; on success window.location.href = data.url; on error setError("Couldn't open Stripe. Try again.").
    - Render a Card with title "Upgrade to Plus", subtitle "Unlock 7-day forecast + advanced AI coach.", and 2 Buttons side-by-side (use a flex/grid container — Tailwind `flex gap-3`). Monthly: "$12.99/mo" (note: pricing decided in 14-02 bootstrap per D-09 — if executor finds a different price in `.env.example` or 14-02's bootstrap output, use that; this plan does NOT hardcode the dollar amount — the label is "$X.XX/mo" derived from a constant exported by 14-02 OR a `MONTHLY_PRICE_DISPLAY` constant defined inline in this file with a comment "Per D-09; keep in sync with leanshot/.env.example STRIPE_PRICE_PLUS_MONTHLY price object"). Yearly: "$132.49/yr — save 15%". Both per D-09 ($12.99 × 12 × 0.85 = $132.49).
    - aria-busy on the loading button only; disabled on both during flight.
    - NO `@stripe/stripe-js`. Plain redirect via `window.location.href`. (Pattern G.)

    Create both `.test.tsx` files with the test matrix in <behavior>. Use the same vi.mock setup as Task 1 for store + supabase. Mock `window.location` via `Object.defineProperty(window, 'location', { value: { href: '' }, writable: true })` BEFORE each test (jsdom default doesn't allow direct writes — see existing tests under `leanshot/src/` for the canonical pattern; if uncertain, grep `'window.location'` under `leanshot/src/**/*.test.*` for an analog).

    Per D-09 (web pricing): monthly + yearly 15% off. Per Pitfall 10 (Pattern G bundle isolation): NO `@stripe/stripe-js` import.
  </action>
  <verify>
    <automated>cd leanshot &amp;&amp; npx vitest run src/components/billing/ManageSubscriptionLink.test.tsx src/components/billing/UpgradeCTA.test.tsx --reporter=verbose 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    Both component files exist with required behavior + Vitest tests pass. Zero `@stripe/stripe-js` imports in either file. Zero hex literals. Both components use Phase 13 tokens.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire PastDueBanner into AppShell + Subscription section in SettingsPage</name>
  <files>leanshot/src/components/layout/AppShell.tsx, leanshot/src/components/dashboard/settings/SettingsPage.tsx</files>
  <action>
    Surgical edit `leanshot/src/components/layout/AppShell.tsx`:
    - Add import: `import { PastDueBanner } from '@/components/billing/PastDueBanner';` in the existing import block (alphabetized — import-x enforces; place per existing ordering between `EmailVerificationBanner` and `QuickLogSheet` OR wherever alphabetical placement lands — eslint-plugin-import-x will tell you).
    - Inside `<main>`, render `<PastDueBanner />` as the FIRST child of `<main>` (line 49 in current file, immediately after the `data-testid="dashboard"` opening tag — BEFORE the `<WorkspaceSwitcher>` wrapping div at lines 54-56). Banner is global chrome per D-08; it must be the topmost element inside the dashboard pane so it renders ABOVE workspace switcher + topbar.
    - Do NOT pass any props (component reads tier from store, self-hides for non-past_due).
    - Do NOT add any wrapper div, margin, or padding around the banner — the banner owns its own padding (px-4 py-2.5). Adding a wrapper would create layout shift for non-past_due users. The banner returns null for tier='free'|'paid' so its DOM footprint is literally zero — Pattern: see EmailVerificationBanner at line 63 for precedent.
    - Verify zero layout shift: open the file post-edit and confirm there is no `<div className=...><PastDueBanner /></div>` wrapper. Bare component call only.

    Surgical edit `leanshot/src/components/dashboard/settings/SettingsPage.tsx`:
    - Add imports: `import { UpgradeCTA } from '@/components/billing/UpgradeCTA';` and `import { ManageSubscriptionLink } from '@/components/billing/ManageSubscriptionLink';` in the existing import block. Place alphabetically per import-x.
    - Read tier via `const tier = useStore((s) => s.tier);` near the existing `const u = useStore((s) => s.user);` at line 95 (one selector per primitive per CLAUDE.md anti-pattern guard).
    - Replace lines 544-553 (the `{section === 'subscription' && (…)}` block) with a tier-conditional Subscription section:
      ```tsx
      {section === 'subscription' && (
        <Section
          title="Subscription"
          body={
            tier === 'free'
              ? 'Upgrade to Plus to unlock forecast + advanced AI coach.'
              : tier === 'past_due'
                ? 'Your payment failed. Update your card to keep Plus active.'
                : 'You’re on LeanShot Plus.'
          }
        >
          {/* Status pill */}
          <div className="flex items-center gap-2">
            <span
              className={cn(
                'inline-flex items-center px-2.5 py-1 rounded-pill text-[11px] font-semibold',
                tier === 'free' && 'bg-[var(--color-surface-elevated)] text-[var(--color-text-secondary)]',
                tier === 'paid' && 'bg-[var(--color-success)] text-white',
                tier === 'past_due' && 'bg-[var(--color-warning)] text-[var(--color-warning-foreground)]',
              )}
              role="status"
              aria-live="polite"
            >
              {tier === 'free' && 'Free'}
              {tier === 'paid' && 'Active'}
              {tier === 'past_due' && 'Past due — update card'}
            </span>
          </div>

          {/* Tier-conditional body */}
          {tier === 'free' && <UpgradeCTA />}
          {(tier === 'paid' || tier === 'past_due') && <ManageSubscriptionLink />}
        </Section>
      )}
      ```
    - The `cn` helper is already imported at line 36 (`import { todayStr, cn } from '@/lib/helpers';`) — no new import needed.
    - Keep the trial-day-count copy ("Trial — 5 days left") from the planning brief as a DEFERRED enhancement, NOT in v1: the `subscriptions.trial_end` field is on the row in 14-01 schema but the Settings drawer would need to fetch+derive it. Document in SUMMARY as Phase 14 follow-up; the "Active" pill covers both trial-active and post-conversion paid until we wire the trial countdown. (This is NOT scope reduction of any D-XX — D-08 only mandates the past_due banner; trial countdown is not in any locked decision.)
    - NO `@stripe/stripe-js` import.
  </action>
  <verify>
    <automated>cd leanshot &amp;&amp; npx tsc -b 2>&amp;1 | tail -20 &amp;&amp; npx eslint src/components/layout/AppShell.tsx src/components/dashboard/settings/SettingsPage.tsx 2>&amp;1 | tail -20 &amp;&amp; grep -nE "from '@stripe/stripe-js'" src/ -r &amp;&amp; echo "FAIL — found @stripe/stripe-js import" &amp;&amp; exit 1 || echo "OK — no @stripe/stripe-js static imports"</automated>
  </verify>
  <done>
    AppShell.tsx renders `<PastDueBanner />` as the first child of `<main>` with no wrapper. SettingsPage.tsx Subscription section renders the tier-conditional status pill + UpgradeCTA (free) OR ManageSubscriptionLink (paid|past_due). TypeScript build passes. ESLint passes. Zero `@stripe/stripe-js` static imports under `leanshot/src/`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Bundle ceiling + hex-literal + Pattern-G grep gates + pathspec commit</name>
  <files>leanshot/src/components/billing/PastDueBanner.tsx, leanshot/src/components/billing/ManageSubscriptionLink.tsx, leanshot/src/components/billing/UpgradeCTA.tsx, leanshot/src/components/layout/AppShell.tsx, leanshot/src/components/dashboard/settings/SettingsPage.tsx</files>
  <action>
    Run automated gates from the leanshot/ project root:
    1. Bundle ceiling: `bash scripts/assert-bundle-budget.sh` — must pass (index ≤50 kB gz; this plan's expected delta ≤0.5 kB gz). If FAIL with a regression > 0.5 kB, investigate which import is leaking; likely culprit is an accidental static `@stripe/stripe-js` or a heavy lucide-react re-import. Per Pattern G + Pitfall 10.
    2. Hex-literal gate (Pattern D): `! grep -nE "#[0-9a-fA-F]{3,8}\b" src/components/billing/PastDueBanner.tsx src/components/billing/ManageSubscriptionLink.tsx src/components/billing/UpgradeCTA.tsx` — must be empty. If non-empty, replace hex with the corresponding `var(--color-*)` token from `index.css` @theme.
    3. Pattern G grep (verify clause is durable): `grep -rnE "from '@stripe/stripe-js'" src/ | grep -v -E "^src/.*\\.test\\.tsx?:.*//.*allowed"` — must be empty (zero matches). The exclusion regex allows a future test fixture to comment-mark an intentional reference; the production tree must have zero.
    4. TierGate registry orphan check (Pattern I — inherited from 14-05): not directly owned by this plan, but if `leanshot/src/lib/billing.ts` exports a `TIER_GATE_REGISTRY` constant, grep `grep -rn "feature=" src/components/billing/` to confirm THIS plan's components don't accidentally use a `feature=` prop that 14-05 didn't register. THIS plan's components do NOT use `<TierGate>` directly (they are chrome, not feature gates per D-08), so the expected output is empty — no orphan.
    5. Vitest full pass: `npx vitest run src/components/billing/ --reporter=verbose 2>&1 | tail -40` — all 3 component test files green.

    Commit using PATHSPEC discipline (Wave 4 parallel with 14-07 per Pattern F + `feedback_parallel_executor_git_isolation`):
    ```
    git commit -- \
      leanshot/src/components/billing/PastDueBanner.tsx \
      leanshot/src/components/billing/PastDueBanner.test.tsx \
      leanshot/src/components/billing/ManageSubscriptionLink.tsx \
      leanshot/src/components/billing/ManageSubscriptionLink.test.tsx \
      leanshot/src/components/billing/UpgradeCTA.tsx \
      leanshot/src/components/billing/UpgradeCTA.test.tsx \
      leanshot/src/components/layout/AppShell.tsx \
      leanshot/src/components/dashboard/settings/SettingsPage.tsx \
      -m "feat(14-06): dashboard chrome + manage slice — PastDueBanner global chrome + ManageSubscriptionLink + UpgradeCTA + AppShell wiring (D-08, MONEY-03, MONEY-09)"
    ```

    NEVER `git add -A` or `git add .` — Wave 4 runs in parallel with 14-07 (stripe-meter-true-up Edge Function under `supabase/functions/stripe-meter-true-up/**`). Sweeping the index would steal 14-07's files. Pattern F enforced.

    NOTE on `supabase/migrations/*` worktree side-effect: this plan does NOT create migrations — N/A. But per `project_worktree_supabase_cli` memory, if executing in a worktree and adding any migration files, the file MUST also live in the main repo tree. Confirmed N/A for 14-06.
  </action>
  <verify>
    <automated>cd leanshot &amp;&amp; bash scripts/assert-bundle-budget.sh &amp;&amp; ! grep -nE "#[0-9a-fA-F]{3,8}\b" src/components/billing/PastDueBanner.tsx src/components/billing/ManageSubscriptionLink.tsx src/components/billing/UpgradeCTA.tsx &amp;&amp; ! grep -rnE "from '@stripe/stripe-js'" src/ &amp;&amp; npx vitest run src/components/billing/ --reporter=verbose 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    All 4 gates green: bundle ceiling under 50 kB gz with delta ≤0.5 kB; zero hex literals in net-new components; zero `@stripe/stripe-js` static imports under `leanshot/src/`; all Vitest billing component tests pass. Commit lands on the worktree branch via pathspec — no Wave 4 sibling files (14-07's `supabase/functions/stripe-meter-true-up/**`) swept.
  </done>
</task>

</tasks>

<invariants>

## Cross-Cutting Patterns (this plan)

- **Pattern D (token consumption) — 14-PATTERNS.md §"Cross-Cutting Patterns" line 137-138.** ZERO hex literals anywhere under `src/components/billing/` or the surgical edits to AppShell.tsx + SettingsPage.tsx (no NEW hex; existing hex elsewhere in SettingsPage is out of scope). Required tokens: `var(--color-warning)` + `var(--color-warning-foreground)` (banner); `var(--color-primary)` + `var(--color-primary-foreground)` (Upgrade CTA Button); `var(--color-surface)` + `var(--color-surface-elevated)` + `var(--color-border)` + `var(--color-text*)` (Card + status pill); `var(--color-success)` (Active pill); `var(--color-danger)` (inline error span). Verify gate in Task 4.

- **Pattern E (reduced-motion gating) — 14-PATTERNS.md cross-cutting E.** PastDueBanner gates the slide-in via `useReducedMotion()` — true → static block (no AnimatePresence/motion); false → animated. Tasked + tested in Task 1. ManageSubscriptionLink + UpgradeCTA have no animation, so the gate is N/A there (only loading-state focus + button click → no transition). Per Phase 14 RESEARCH §Pattern 4 line 389 + Phase 13 hook precedent.

- **Pattern F (pathspec discipline) — `feedback_parallel_executor_git_isolation`.** Wave 4 parallel batch: 14-06 (this plan) + 14-07 (Edge Function under `supabase/functions/stripe-meter-true-up/**`). Files DISJOINT. Commit MUST use `git commit -- <pathspec>` enumerated in Task 4. `git add .` / `git add -A` FORBIDDEN.

- **Pattern G (bundle isolation for Stripe.js) — 14-PATTERNS.md §G + RESEARCH Pitfall 10 lines 508-519 + memory `project_phase5_bundle_regression`.** ZERO `@stripe/stripe-js` STATIC imports anywhere under `leanshot/src/`. Hosted Checkout works via plain `window.location.href = session.url` — the JS SDK is not required for the redirect-checkout pattern. If the executor finds themselves writing `import Stripe from '@stripe/stripe-js'`, STOP — they are mis-implementing. The Edge Function (14-04) creates the session server-side; client just consumes the returned URL. Verify gate in Task 4.

- **Pattern I (TierGate registry orphan check) — 14-PATTERNS.md §I + `feedback_planner_iter1_anti_patterns` #6.** This plan does NOT add to `TIER_GATE_REGISTRY` (banner + Settings entries are CHROME per D-08, not feature gates). Task 4 verifies no orphan via grep `grep -rn "feature=" src/components/billing/` returning empty for THIS plan's files.

## Decision Traceability

| Decision | Implementing artifact in this plan |
|---|---|
| **D-08** (past_due banner always-on chrome with warm-orange + Customer Portal CTA) | `PastDueBanner.tsx` + AppShell.tsx wiring (Tasks 1, 3) |
| **D-09** (web tier monthly + yearly 15% off) | `UpgradeCTA.tsx` 2-plan selector (Task 2). Pricing comes from 14-02 bootstrap script per D-11 — this plan consumes price IDs via the Edge Function body `{ plan: 'plus_monthly' \| 'plus_yearly' }` and lets the Edge Function map plan tags → live price IDs. |
| **D-12** (Hosted Checkout — informational restate of goal text) | `UpgradeCTA.tsx` uses `window.location.href = url` redirect (Hosted Checkout pattern); NO Elements (Task 2). |
| **D-13** (7-day trial — informational; trial logic is 14-04 server-side) | UpgradeCTA does not display trial copy in v1 (chrome shows post-redirect "Active" pill regardless). Trial countdown surfaced in Settings is a Phase 14 follow-up (documented in SUMMARY). |
| **D-16** (CSP additions for `js.stripe.com`, etc.) | NOT in this plan. Already shipped by 14-02. This plan's redirects use the same domains; no further CSP changes. |

## REQ Coverage

- **MONEY-03 (Customer Portal endpoint + Settings entry):** ManageSubscriptionLink.tsx (Task 2) consumes the 14-04 portal endpoint and renders the Settings entry. PastDueBanner (Task 1) also opens the portal but in a new tab. AppShell wiring (Task 3) makes the banner global.
- **MONEY-09 (past_due banner):** PastDueBanner.tsx + AppShell wiring (Tasks 1, 3). End-to-end verified in 14-08's `past-due-banner.spec.ts`.

## NOT In Scope (carried by other plans or deferred)

- Trial-day countdown copy in Settings status pill → Phase 14 follow-up (NOT a locked decision; documented in SUMMARY).
- `stripe-checkout/portal` Edge Function implementation → 14-04.
- `stripe-checkout/session` Edge Function implementation → 14-04.
- `tier` Zustand slice definition → 14-05.
- `<TierGate>` primitive → 14-05.
- E2E spec for past_due flow → 14-08 (`past-due-banner.spec.ts`).
- E2E spec for portal flow → 14-08 (`portal-plan-change.spec.ts`).
- E2E spec for checkout trial flow → 14-08 (`checkout-trial-flow.spec.ts`).
- iOS/Android in-app upgrade UI → Phase 16 (RevenueCat).

</invariants>

<verification>

## Phase-level checks for plan 14-06

1. `cd leanshot && npx vitest run src/components/billing/ --reporter=verbose` — all 3 net-new test files green (≥5 + ≥3 + ≥4 test cases respectively).
2. `cd leanshot && npx tsc -b` — typecheck passes.
3. `cd leanshot && npx eslint src/components/billing/ src/components/layout/AppShell.tsx src/components/dashboard/settings/SettingsPage.tsx` — lint passes.
4. `cd leanshot && bash scripts/assert-bundle-budget.sh` — bundle ceilings respected; net delta ≤0.5 kB gz.
5. `cd leanshot && grep -rnE "from '@stripe/stripe-js'" src/` — empty (zero matches).
6. `cd leanshot && grep -nE "#[0-9a-fA-F]{3,8}\b" src/components/billing/PastDueBanner.tsx src/components/billing/ManageSubscriptionLink.tsx src/components/billing/UpgradeCTA.tsx` — empty.
7. Manual visual check (Task 3 done-check confirms tsc + eslint pass): in dev (`npm run dev`), set tier='past_due' via Zustand devtools → orange banner appears above WorkspaceSwitcher on every dashboard tab; banner-Update-card opens Stripe Portal in a new tab. Set tier='free' → Settings Subscription section shows Free pill + UpgradeCTA buttons. Set tier='paid' → Active pill + ManageSubscriptionLink. (This manual check is informational — it is NOT a blocking gate; 14-08 Playwright owns the e2e gate.)

</verification>

<success_criteria>

This plan is COMPLETE when:

- [ ] `leanshot/src/components/billing/PastDueBanner.tsx` exists with role="alert" + tier='past_due' guard + supabase.functions.invoke('stripe-checkout/portal') + useReducedMotion fallback.
- [ ] `leanshot/src/components/billing/ManageSubscriptionLink.tsx` exists with portal invoke + same-tab redirect.
- [ ] `leanshot/src/components/billing/UpgradeCTA.tsx` exists with tier='free' guard + 2-plan selector + session invoke + same-tab redirect.
- [ ] `leanshot/src/components/layout/AppShell.tsx` renders `<PastDueBanner />` as the first child of `<main>` (no wrapper, no margin, no padding around it).
- [ ] `leanshot/src/components/dashboard/settings/SettingsPage.tsx` Subscription section renders tier-conditional status pill + UpgradeCTA (free) OR ManageSubscriptionLink (paid|past_due).
- [ ] All 6 Vitest tests green (≥5 banner + ≥3 manage + ≥4 upgrade test cases).
- [ ] Zero `@stripe/stripe-js` static imports under `leanshot/src/`.
- [ ] Zero hex literals in net-new billing components.
- [ ] Bundle ceiling preserved (assert-bundle-budget.sh exits 0).
- [ ] TypeScript build + ESLint pass.
- [ ] Single commit via `git commit -- <pathspec>` containing exactly the 8 enumerated files (no sweep of 14-07's `supabase/functions/stripe-meter-true-up/**`).
- [ ] User-observable at plan close: free user → Upgrade → Checkout; paid user → Manage → Portal; past_due user → orange banner + Update card → Portal.

</success_criteria>

<output>
After completion, create `.planning/phases/14-monetization-foundation-stripe-web-clinic-seats/14-06-SUMMARY.md` documenting:

- What was built (3 components + 2 surgical edits).
- Decision traceability table (D-08 + D-09 + D-12).
- Patterns enforced (D, E, F, G, I).
- Bundle delta vs ceiling (numbers from `assert-bundle-budget.sh` output).
- Known follow-ups (trial-day countdown copy in Settings — NOT in v1, Phase 14 close candidate).
- Verification evidence (Vitest output snippet, grep gate outputs).
- Commit SHA(s).

Per `.planning/STATE.md` writeback rule, also append a brief plan completion line to STATE.md (single line, follows existing Phase 12/13 close conventions).
</output>
