---
phase: 13-design-system-v2-rollout
plan: 05
type: execute
wave: 2
depends_on: ["13-01", "13-03"]
files_modified:
  - src/components/marketing/Landing.tsx
autonomous: true
requirements: ["DS-03"]
tags: ["design-system", "marketing", "tokens", "illustrations"]

must_haves:
  truths:
    - "Marketing Landing page renders with v2 tokens (cream #F2EDE0 + paper-white #FEFCF7 + warm-tinted shadows) automatically via 13-01 token cascade"
    - "Marketing Landing imports HeroOrbital v2 and AIAvatar v2 from 13-03 — refreshed illustrations show in Hero + Features sections"
    - "Marketing Landing renders correctly in light theme (token + illustration v2)"
    - "Marketing Landing renders correctly in dark theme (token + illustration v2)"
    - "No structural redesign — same sections (Nav, Hero, Features, Testimonials, Pricing, FAQ, Footer) in the same order with the same copy hierarchy"
    - "No hard-coded hex literals in Landing.tsx — all colors flow through Tailwind utilities or var(--color-*)"
    - "Landing.tsx remains in the marketing bundle chunk — no bundle-ceiling regression (index <50 kB gz, marketing chunk holds Landing as before)"
    - "Landing.tsx ready for 13-06 visual regression snapshot capture (light + dark) once Batch A merges to main"
  artifacts:
    - path: "src/components/marketing/Landing.tsx"
      provides: "Refreshed marketing landing on v2 design system (token cascade + illustration v2)"
      contains: "import { HeroOrbital } from '@/illustrations/HeroOrbital'; import { AIAvatar } from '@/illustrations/AIAvatar';"
  key_links:
    - from: "src/components/marketing/Landing.tsx"
      to: "src/index.css (@theme block from 13-01)"
      via: "Tailwind utilities + bg-[var(--color-*)] references already in source"
      pattern: "var\\(--color-"
    - from: "src/components/marketing/Landing.tsx"
      to: "src/illustrations/HeroOrbital.tsx (v2 from 13-03)"
      via: "Named import"
      pattern: "from '@/illustrations/HeroOrbital'"
    - from: "src/components/marketing/Landing.tsx"
      to: "src/illustrations/AIAvatar.tsx (v2 from 13-03)"
      via: "Named import"
      pattern: "from '@/illustrations/AIAvatar'"
    - from: "src/components/marketing/Landing.tsx"
      to: "src/illustrations/ConnectData.tsx (v2 from 13-03)"
      via: "Named import (existing — illustration mutated in place by 13-03; import line stays unchanged)"
      pattern: "from '@/illustrations/ConnectData'"
---

<objective>
Refresh the marketing landing page (`src/components/marketing/Landing.tsx`) onto the v2 design system via **token cascade only** (from 13-01) plus **illustration import refresh** (consumed from 13-03). NO structural redesign, NO new sections, NO new copy, NO new routes.

**Purpose:** Owner of DS-03 ("Marketing Landing refresh"). End-to-end, a visitor lands on `/` and sees the same section composition (Nav → Hero → Features → Testimonials → Pricing → FAQ → Footer) but rendered with v2 cream/paper-white/warm-shadow tokens (automatic via CSS variable cascade) and refreshed `HeroOrbital` + `AIAvatar` + `ConnectData` illustrations (mutated in place by 13-03; import lines unchanged).

**Output:** A mutated `src/components/marketing/Landing.tsx` that:
- Continues to compose the same 7 sections in the same order.
- Drops any latent hard-coded hex literals (audit pass — none expected in current source, but verify).
- Passes lint, typecheck, prod build with zero new warnings.
- Is ready for 13-06 to capture light + dark VR baselines.

**Wave 2 / Batch A.** Parallel-safe with 13-02 + 13-03 by file pathspec (this plan owns `src/components/marketing/Landing.tsx` only). Uses `git commit -- src/components/marketing/Landing.tsx` per memory `feedback_parallel_executor_git_isolation.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-design-system-v2-rollout/13-CONTEXT.md
@.planning/phases/13-design-system-v2-rollout/13-PATTERNS.md
@.planning/phases/13-design-system-v2-rollout/13-PLAN-OUTLINE.md
@.planning/design-system/project/ui_kits/leanshot-marketing/marketing.jsx
@.planning/design-system/project/ui_kits/leanshot-marketing/marketing.css
@.planning/design-system/project/ui_kits/leanshot-marketing/index.html
@src/components/marketing/Landing.tsx
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/components/marketing/Landing.tsx (current top — 23 lines):
```typescript
import { motion } from 'framer-motion';
import {
  ArrowRight, Sun, Moon, Check, Zap, Brain, ChartLine,
  Sparkles, ChevronDown, Shield, Lock,
} from 'lucide-react';
import { useEffect, useRef, useState } from 'react';
import { LegalFooter } from '@/components/layout/LegalFooter';
import { Button, IconButton } from '@/components/ui/Button';
import { useReducedMotion } from '@/hooks/useReducedMotion';
import { useTheme } from '@/hooks/useTheme';
import { AIAvatar } from '@/illustrations/AIAvatar';
import { ConnectData } from '@/illustrations/ConnectData';
import { HeroOrbital } from '@/illustrations/HeroOrbital';

interface LandingProps {
  onStart: () => void;
}

export function Landing({ onStart }: LandingProps) { /* … */ }
```

Section composition (Landing.tsx:30-41) — DO NOT change order, names, or count:
```typescript
<div className="min-h-screen bg-[var(--color-bg)] overflow-x-hidden">
  <Nav theme={theme} toggle={toggle} onStart={onStart} />
  <Hero onStart={onStart} />
  <Features />
  <Testimonials />
  <Pricing onStart={onStart} />
  <FAQ />
  <Footer />
</div>
```

Illustration import contract (consumed from 13-03 in same wave):
- `HeroOrbital`: takes `className?: string` + `staticOnly?: boolean` props (see 13-PATTERNS.md §9). Used at Landing.tsx:182 (Hero hero-card, `staticOnly` NOT set — animation on) and Landing.tsx:294-295 (Features tile, `staticOnly` set).
- `AIAvatar`: takes `size?: number` + `className?: string` + `thinking?: boolean` props. Used at Landing.tsx:296-297 (Features tile, `size={96}`, no `thinking`).
- `ConnectData`: takes `className?: string`. Used at Landing.tsx:298-299 (Features tile, `className="w-[160px]"`).

**Critical:** 13-03 mutates these three illustration files IN PLACE. The named exports stay the same. **The import lines in Landing.tsx require zero edits for the illustration swap to take effect.** The visual change cascades automatically once 13-03 lands on main.

Token cascade contract (consumed from 13-01 in same wave):
- All Tailwind v4 `var(--color-*)` references continue to resolve through `src/index.css` `@theme` block, but the VALUES behind those tokens migrate (cream `#efebe0` → `#F2EDE0`; paper-white surface `#fdfbf6` → `#FEFCF7`; warm-tinted shadows). **Landing.tsx requires zero edits for the token swap to take effect.**

Project conventions (CLAUDE.md):
- `tsconfig.app.json`: strict, `noUnusedLocals`, `noUnusedParameters` (any import-removal must be clean — no dead imports).
- ESLint `import-x` group order: third-party (alphabetized) → `@/` aliased (alphabetized) → relative (alphabetized). `npm run lint:fix` auto-fixes.
- Anti-pattern: hard-coding colors in components — all colors via `var(--color-*)` or Tailwind utilities mapped to those tokens.
- Accessibility: `aria-hidden` on decorative SVG (already correct on the inline `hero-fill` linearGradient SVG inside `HeroVisual`).
- No new SDKs, no new vendor dependencies (Phase 13 scope guardrail).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Landing.tsx for v2 readiness (hex literals, illustration imports, structural drift)</name>
  <files>src/components/marketing/Landing.tsx</files>
  <action>
Read the current `src/components/marketing/Landing.tsx` (loaded in context above; ~586 lines) and perform a token + illustration + structure audit. Per D-03 + plan-outline 13-05 scope: the file MUST pick up new tokens automatically via 13-01's token cascade (no code change for color refs that go through Tailwind utilities or `var(--color-*)`), and MUST consume v2 illustration components from 13-03 via the existing named imports. This task is a **no-mutation audit pass**; Task 2 applies any fixes the audit surfaces.

Audit checks (perform sequentially, record findings in commit message of Task 2):

1. **Hex literal audit (FORBIDDEN per CLAUDE.md Anti-Pattern "Hard-coding colors in components"):**
   Run `grep -nE '#[0-9a-fA-F]{3,8}\b' src/components/marketing/Landing.tsx`. Inspect every hit. Acceptable: inline SVG `linearGradient` `stopColor` that uses CSS-var passthrough (e.g. `stopColor="white"` is fine — `white` is not a hex literal). Unacceptable: any `#RRGGBB`, `#RGB`, or `#RRGGBBAA` literal in JSX className, style, attribute, or string concatenation. If found, list line numbers for Task 2 to fix.

2. **Illustration import audit:**
   Verify the three illustration imports at lines 20–22 (`AIAvatar`, `ConnectData`, `HeroOrbital`) are intact and use the `@/illustrations/*` named-import pattern. They will be mutated in place by 13-03; THIS plan must NOT touch the import lines (refresh is automatic via the file mutation in 13-03). If any of the three imports is missing or aliased, flag for Task 2.

3. **Section composition audit:**
   Verify `export function Landing(...)` renders exactly these 7 sections, in this order, with these names: `<Nav>`, `<Hero>`, `<Features>`, `<Testimonials>`, `<Pricing>`, `<FAQ>`, `<Footer>`. Per plan-outline 13-05: "NO new sections, NO new copy, section layout + copy hierarchy stays unchanged." If anything has drifted, halt and flag for replan — do NOT silently restructure.

4. **Reference the marketing kit (`.planning/design-system/project/ui_kits/leanshot-marketing/marketing.{jsx,css}` + `index.html`) for visual-choice questions NOT covered by the token cascade:**
   The marketing kit may show new visual elements (e.g. glass cards, testimonial styling). Per plan-outline 13-05: "adopt only if they're already present in the current Landing.tsx in a similar form." If the kit suggests adding new sections, IGNORE — defer to scope guardrail. Surface any DELTA between the kit and current Landing that goes BEYOND token/illustration swap as a comment in Task 2's commit body (deferred for future polish) — do NOT implement structural changes in this plan.

5. **Confirm no `transition: width` on sidebar-like elements:** Landing.tsx has no sidebar, so this check is N/A but documented for executor's awareness of chat1.md landmine 1 (Wave-2 cross-plan invariant).

**No file mutations in this task.** Output: an audit report (in plan SUMMARY at end of phase) capturing the 5 audit findings.
  </action>
  <verify>
    <automated>grep -nE '#[0-9a-fA-F]{3,8}\b' src/components/marketing/Landing.tsx | grep -v '^[[:space:]]*//' || echo "No hex literals found — audit passes"</automated>
  </verify>
  <done>Audit complete. Hex-literal list (likely empty), import-line confirmation, section-composition confirmation, and marketing-kit-delta notes are all recorded in working notes ready for Task 2's commit message. No file mutation in this task.</done>
</task>

<task type="auto">
  <name>Task 2: Apply audit fixes (if any) + verify cascade, lint, typecheck, build</name>
  <files>src/components/marketing/Landing.tsx</files>
  <action>
Apply any fixes surfaced by Task 1's audit. The expected default outcome — given Landing.tsx already routes every color through `var(--color-*)` or Tailwind utilities (per `code_context` in 13-PATTERNS.md §8) — is **ZERO source changes to Landing.tsx**; the entire DS-03 deliverable cascades automatically through 13-01 (token swap) and 13-03 (in-place illustration mutation).

Mutation rules (only if audit flagged something):

1. **If Task 1 found hex literals:** Replace each with the equivalent Tailwind utility or `var(--color-*)` token. Map hex → token via the v2 token list in `src/index.css` (post-13-01) or `.planning/design-system/colors_and_type.css`. Forbidden: introducing a NEW token name from this plan — if a needed token doesn't exist post-13-01, halt and flag back to 13-01 (token plan).

2. **If Task 1 found a missing or aliased illustration import:** Restore the named imports at lines 20–22 exactly as:
   ```
   import { AIAvatar } from '@/illustrations/AIAvatar';
   import { ConnectData } from '@/illustrations/ConnectData';
   import { HeroOrbital } from '@/illustrations/HeroOrbital';
   ```
   (alphabetized within the `@/` group per `eslint-plugin-import-x`).

3. **If Task 1 found section composition drift:** STOP and surface to operator. Do NOT restructure; this plan is scoped to token + illustration swap only.

4. **If Task 1 found a marketing-kit-only visual delta (new section, glass card variant, new testimonial layout):** Capture as a "DEFERRED FOR FUTURE POLISH" note in commit body. Do NOT implement.

**After mutations (or after confirming zero mutations needed) — verification sweep:**

a. `npm run lint -- src/components/marketing/Landing.tsx` (or full `npm run lint` if file-scoped run not supported by config) — MUST pass clean with no new warnings.

b. `npm run typecheck` (or `npx tsc -b --noEmit` per repo conventions) — MUST pass; strict mode + `noUnusedLocals` + `noUnusedParameters` are enforced.

c. `npm run build` — MUST succeed. Confirm Landing.tsx still ends up in the marketing chunk (per `code_context` in 13-CONTEXT.md: "Landing.tsx already in marketing chunk; illustration changes are tree-shaken"). Bundle-size CI guards (`scripts/assert-bundle-budget.sh`, `scripts/assert-vendor-react-size.sh`) MUST stay green. Expect bundle delta ≈ 0 (token cascade is CSS-only; illustration refresh is JSX-only and the marketing chunk already holds the illustration imports).

d. **Visual sanity check (NOT a hard CI gate — that's 13-06):** Run `npm run dev` locally (optional, executor's discretion) and confirm Landing.tsx renders with new cream `#F2EDE0` (via `var(--color-bg)`) and new paper-white `#FEFCF7` (via `var(--color-surface)`) backgrounds in both `data-theme="light"` and `data-theme="dark"`. The hero card should show the refreshed `HeroOrbital` design from 13-03. **Do NOT block on this — visual regression is owned by 13-06.**

**Commit discipline (memory `feedback_parallel_executor_git_isolation.md`):**
- Use file-pathspec commit: `git commit -- src/components/marketing/Landing.tsx`
- Commit message format: `feat(13-05): DS-03 marketing landing refresh (token + illustration cascade)`
- Body of commit message: include the Task 1 audit findings (hex-literal count, import confirmation, section-composition confirmation, marketing-kit-delta notes).
- **NEVER** use `git add -A` or `git add .` — parallel executors on 13-02 and 13-03 are running concurrently.

If audit + verification all show "zero changes needed, cascade works as designed," still commit a `chore(13-05): DS-03 audit confirms automatic cascade — no source changes` empty-tree commit OR (cleaner) skip the commit and record the no-op outcome in the plan SUMMARY at phase close. Executor's call based on whether teammates need a paper trail.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
- `npm run lint` passes (no new warnings from Landing.tsx)
- `npm run typecheck` passes
- `npm run build` succeeds; marketing chunk still holds Landing.tsx; bundle-budget assertions stay green
- Landing.tsx contains zero hex literals (verified by grep in Task 1 verify)
- Landing.tsx imports `HeroOrbital`, `AIAvatar`, `ConnectData` from `@/illustrations/*` (named imports, alphabetized)
- Section composition unchanged: `<Nav><Hero><Features><Testimonials><Pricing><FAQ><Footer>`
- Either (a) zero mutations + recorded in SUMMARY, or (b) targeted mutation committed via `git commit -- src/components/marketing/Landing.tsx`
- 13-06 (VR suite) has a clean baseline to snapshot light + dark `<Landing>` against once Batch A merges to main
  </done>
</task>

</tasks>

<verification>
Phase-level rolls up into 13-06 (visual regression suite). Plan-level verification:

1. `grep -nE '#[0-9a-fA-F]{3,8}\b' src/components/marketing/Landing.tsx` returns empty (or only acceptable CSS-var-passthrough patterns).
2. `grep -n "from '@/illustrations/" src/components/marketing/Landing.tsx` shows the three expected imports (`AIAvatar`, `ConnectData`, `HeroOrbital`).
3. `grep -c "function \(Nav\|Hero\|Features\|Testimonials\|Pricing\|FAQ\|Footer\)" src/components/marketing/Landing.tsx | grep -v '^#'` returns 7 (one for each section sub-component). If the grep includes the `HeroVisual` helper, the count may be 8 — confirm by inspection that the section composition in `Landing()` itself is still 7 sections.
4. `npm run lint && npm run typecheck && npm run build` all pass.
5. Bundle-budget scripts (`scripts/assert-bundle-budget.sh`) pass — no marketing chunk regression beyond noise.
</verification>

<success_criteria>
- **DS-03 owned and delivered:** Marketing Landing renders on v2 tokens + v2 illustrations without structural redesign.
- **No hard-coded hex** in Landing.tsx (audit-verified).
- **No new sections, no new copy, no new routes** — section composition unchanged.
- **Bundle-neutral:** Landing.tsx stays in the marketing chunk; index ceiling stays well under 50 kB gz; per-chunk budgets stay green.
- **Cascade-ready for 13-06:** Light + dark VR snapshots in 13-06 will capture the new normal baseline once Batch A (this plan + 13-02 + 13-03) lands on main.
- **Parallel-execution-safe:** File pathspec commit (`git commit -- src/components/marketing/Landing.tsx`) prevents sweeping sibling-plan files from 13-02 / 13-03 / 13-04 / 13-06.
</success_criteria>

<output>
After completion, the executor will write `.planning/phases/13-design-system-v2-rollout/13-05-SUMMARY.md` capturing:

1. **Task 1 audit findings** verbatim (hex-literal count, import confirmation, section-composition confirmation, marketing-kit-delta notes deferred for future polish).
2. **Task 2 outcome:** "Zero source changes needed — cascade works as designed" OR a diff summary of the surgical fixes applied.
3. **Verification output:** `npm run lint && npm run typecheck && npm run build` tail showing success + bundle-budget assertion lines confirming no regression.
4. **Handoff to 13-06:** `Landing.tsx` is on main and ready for light + dark VR baseline capture.
5. **Carried-over items (if any):** marketing-kit-only visual deltas filed as deferred ideas for a future polish phase (Phase 22 or v1.3).
</output>

## PLAN COMPLETE

**Plan:** 13-05 — Marketing Landing refresh (token + illustration swap only; no structural redesign)
**File:** `/Users/karstenhaldan/minisite/leanshot/.planning/phases/13-design-system-v2-rollout/13-05-PLAN.md`
**Wave:** 2 (Batch A — parallel with 13-02 + 13-03)
**Depends on:** 13-01 (token cascade), 13-03 (illustration v2 mutations)
**Files modified:** `src/components/marketing/Landing.tsx` (at most — likely zero changes; audit-driven surgical fixes if needed)
**Requirements covered:** DS-03
