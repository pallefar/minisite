---
phase: 13-design-system-v2-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - leanshot/src/index.css
  - leanshot/index.html
  - leanshot/lighthouserc.json
  - .github/workflows/ci.yml
  - leanshot/scripts/assert-fcp-lcp-delta.sh
  - leanshot/.planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json
autonomous: true
requirements: [DS-01, DS-02]
tags: [design-system, tokens, fonts, lighthouse, fcp, lcp, perf]
must_haves:
  truths:
    - "Body text in Geist (sans), numerals/code in Geist Mono, display/italic accents in Fraunces — verified by inspecting computed font-family on any rendered page after npm run dev / npm run build."
    - "Page background is v2 cream (#F2EDE0), card/surface is v2 paper-white (#FEFCF7), borders are v2 stronger neutral (#DAD3C0), secondary text is v2 darker (#4D5E58) — verified by reading the computed values of --color-bg / --color-surface / --color-border / --color-text-secondary on :root."
    - "Light-mode multi-layer shadows are warm-tinted using rgba(40, 32, 20, …) (replacing the cool rgba(22, 34, 31, …) recipe) — verified by reading --shadow / --shadow-md / --shadow-lg / --shadow-2xl / --shadow-hero values."
    - "Body type floor is 16 px (--text-base resolves to 1rem / 16 px on a 16 px root, not 15 px) and heading tracking is -0.01em on the heading-tracking utility/token — verified by computed style on a representative <h2>."
    - "A paper-grain SVG fractal-noise overlay tile (~5 % opacity, 200×200 px) is registered as a CSS custom property and applied to <body> in light mode only — verified by toggling [data-theme=dark] in DevTools (overlay disappears) and confirming the SVG renders against the body in light mode."
    - "Primary CTA buttons (Button variant primary) render the 12 % white linear-gradient sheen top + 1 px inset highlight top + 1 px inset shadow bottom — verified by visual inspection of a Button on /#/auth/signin or any dashboard CTA AFTER the variantClasses primary entry inherits the new --button-primary-sheen token (no Button.tsx edit required — sheen ships as a CSS variable consumed by the existing primary classname)."
    - "Fonts load via <link rel=preload as=style> + <link rel=stylesheet media=print onload=this.media='all'> + <noscript><link rel=stylesheet> in index.html — all three URLs IDENTICAL, NO @import chain anywhere — verified by grep on index.html + src/index.css confirming zero occurrences of `@import url(...fonts.googleapis...)`."
    - "FCP baseline + LCP baseline from current main Vercel deploy are committed to .planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json with the captured commit SHA, capture timestamp, and Lighthouse run URL — verified by `cat 13-FCP-BASELINE.json | jq` returning the documented schema."
    - "CI lighthouse job fails the PR when FCP delta > 5 % OR LCP delta > 5 % vs the baseline — verified by `bash scripts/assert-fcp-lcp-delta.sh --dry-run` (or equivalent local smoke) returning exit 0 against synthetic equal values and exit 1 against synthetic +6 % values."
    - "CSP snapshot test (tests/csp/csp-snapshot.test.ts) continues to pass post-merge — confirms fonts.googleapis.com (style-src) and fonts.gstatic.com (font-src) remain allowed; no CSP regression from font swap."
  artifacts:
    - path: "leanshot/src/index.css"
      provides: "v2 @theme block (light token values + dark override values + new paper-grain noise CSS variable + new primary-CTA sheen CSS variable + 16 px --text-base + heading tracking) and v2 font-family tokens (Geist / Geist Mono / Fraunces unchanged)"
      contains: "--color-cream-100: #F2EDE0; --color-cream-card: #FEFCF7; --color-border: #DAD3C0; --color-text-secondary: #4D5E58; --font-sans: 'Geist'; --font-mono: 'Geist Mono';"
    - path: "leanshot/index.html"
      provides: "v2 Google Fonts <link> block (Geist 400-900 + Geist Mono 400-700 + Fraunces ital,opsz,wght@0,9..144,300..700;1,9..144,300..700) loaded via preload + onload swap + <noscript> fallback — three IDENTICAL URLs"
      contains: "family=Geist:wght@400;500;600;700;800;900&family=Geist+Mono:wght@400;500;600;700&family=Fraunces:ital,opsz,wght@0,9..144,300..700;1,9..144,300..700&display=swap"
    - path: "leanshot/.planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json"
      provides: "FCP + LCP baseline values captured from current main Vercel deploy BEFORE PR-1 merges — input to the CI delta gate"
      contains: '"fcp_ms"'
    - path: "leanshot/scripts/assert-fcp-lcp-delta.sh"
      provides: "Bash script that reads 13-FCP-BASELINE.json + Lighthouse audit JSON output and exits non-zero if FCP delta > 5 % OR LCP delta > 5 %"
      contains: "fcp_ms"
    - path: "leanshot/lighthouserc.json"
      provides: "Existing LHCI config extended (or referenced) so the autorun produces a parseable audit JSON the delta script can consume"
      contains: '"first-contentful-paint"'
    - path: ".github/workflows/ci.yml"
      provides: "lighthouse job extended with one additional step that invokes scripts/assert-fcp-lcp-delta.sh against the LHCI output — same job, same needs:, no new top-level job"
      contains: "assert-fcp-lcp-delta"
  key_links:
    - from: "leanshot/index.html"
      to: "https://fonts.googleapis.com/css2?family=Geist:…&family=Geist+Mono:…&family=Fraunces:…"
      via: "<link rel=preload as=style> + <link rel=stylesheet media=print onload> + <noscript><link rel=stylesheet> — three identical URLs"
      pattern: "rel=\"preload\".*Geist.*Geist\\+Mono.*Fraunces"
    - from: "leanshot/src/index.css"
      to: "leanshot/index.html"
      via: "--font-sans: 'Geist', … picks up the Geist webfont served by the <link> stylesheet"
      pattern: "--font-sans:\\s*'Geist'"
    - from: ".github/workflows/ci.yml lighthouse: job"
      to: "leanshot/scripts/assert-fcp-lcp-delta.sh"
      via: "Appended step runs the assert script with LHCI audit JSON + the committed 13-FCP-BASELINE.json"
      pattern: "assert-fcp-lcp-delta\\.sh"
    - from: "leanshot/scripts/assert-fcp-lcp-delta.sh"
      to: "leanshot/.planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json"
      via: "Reads baseline fcp_ms + lcp_ms and compares to current LHCI audit values"
      pattern: "13-FCP-BASELINE\\.json"
---

<objective>
Land the Phase 13 token + font swap on `main` ALONE, gated by an FCP + LCP delta CI guard, so that every Wave-2 plan inherits the v2 visual baseline and any perf regression is caught before merge.

Purpose:
- PR-1 of the two-PR ladder (D-02 + D-11): tokens + fonts ship first, revertable in one commit. Wave 2 (components / illustrations / login / marketing / VR suite) cascades visual changes through `var(--color-*)` automatically once these tokens flip.
- The FCP + LCP gate (D-07 + D-13) is the only hard CI guarantee that the new fonts (Geist replacing Inter) and the new paper-grain noise overlay don't regress cold-load perf. SC #1 verbatim: "FCP on cold load is within 5 % of pre-Phase-13 baseline." LCP added defensively because the paper-grain SVG fractal-noise overlay is a known LCP risk vector.

Output:
- Mutated `leanshot/src/index.css` (`@theme` block + `[data-theme=dark]` override, value-only diff — no plumbing change).
- Mutated `leanshot/index.html` (three font `<link>` URLs swapped from Inter/JetBrains-Mono to Geist/Geist-Mono, Fraunces preserved, preload+onload swap pattern preserved).
- New `leanshot/.planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json` with FCP + LCP captured from current `main` Vercel deploy (pre-PR-1 SHA recorded).
- New `leanshot/scripts/assert-fcp-lcp-delta.sh` enforcing ≤ 5 % delta on both metrics.
- Mutated `.github/workflows/ci.yml` — one extra step appended inside the existing `lighthouse:` job invoking the assert script.
- (Possibly) mutated `leanshot/lighthouserc.json` if the existing config needs an audit-JSON output flag to feed the delta script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/13-design-system-v2-rollout/13-CONTEXT.md
@leanshot/.planning/phases/13-design-system-v2-rollout/13-PATTERNS.md
@leanshot/.planning/PROJECT.md
@leanshot/.planning/ROADMAP.md
@leanshot/.planning/STATE.md
@leanshot/CLAUDE.md
@leanshot/src/index.css
@leanshot/index.html
@leanshot/.planning/design-system/project/leanshot/src/index.css
@leanshot/.planning/design-system/colors_and_type.css
@leanshot/.planning/design-system/chats/chat1.md
@leanshot/lighthouserc.json
@.github/workflows/ci.yml
@leanshot/tests/csp/csp-snapshot.txt

<interfaces>
<!-- Key facts the executor needs without further codebase exploration. -->

### Current v1 token values in leanshot/src/index.css (lines to MUTATE in place)

Light `@theme {}` block, lines 11–153 — values to swap (structure stays identical):

  --color-cream-100: #efebe0;        → #F2EDE0    (page bg — D-02 + design-system/colors_and_type.css)
  --color-cream-card: #fdfbf6;       → #FEFCF7    (paper-white surface)
  --color-border: #e2ddd0;           → #DAD3C0    (stronger v2 neutral)
  --color-text-secondary: #556660;   → #4D5E58    (6 % darker for contrast)

  /* shadows — swap the rgba tint from cool (22,34,31) to warm (40,32,20) — keep alpha + offset recipe */
  --shadow-xs:  0 1px 2px   rgba(22, 34, 31, 0.04);  → rgba(40, 32, 20, 0.04)
  --shadow-sm:  0 2px 4px   rgba(22, 34, 31, 0.05);  → rgba(40, 32, 20, 0.05)
  --shadow:     0 6px 16px  rgba(22, 34, 31, 0.06);  → rgba(40, 32, 20, 0.06)
  --shadow-md:  0 10px 24px rgba(22, 34, 31, 0.08);  → rgba(40, 32, 20, 0.08)
  --shadow-lg:  0 16px 40px rgba(22, 34, 31, 0.10);  → rgba(40, 32, 20, 0.10)
  --shadow-2xl: 0 32px 80px rgba(22, 34, 31, 0.16);  → rgba(40, 32, 20, 0.16)
  --shadow-hero: keep teal-tinted (it sits on hero panels — design-system/project/leanshot/src/index.css confirms hero shadow stays cool)

  /* body type floor */
  --text-base: 0.9375rem;  /* 15px */  → 1rem;  /* 16px floor */
  --text-base--line-height: 1.55;       /* keep */

  /* font-family swap */
  --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    → 'Geist', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  --font-mono: 'JetBrains Mono', ui-monospace, 'SF Mono', Menlo, monospace;
    → 'Geist Mono', ui-monospace, 'SF Mono', Menlo, monospace;
  --font-display: 'Fraunces', …  /* keep verbatim — Fraunces stays */

### NEW tokens to APPEND inside the same @theme {} block (do NOT split into a new block)

  /* Paper-grain noise overlay — light only */
  --grain-noise: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='200' height='200' viewBox='0 0 200 200'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='2' stitchTiles='stitch'/><feColorMatrix type='matrix' values='0 0 0 0 0.16  0 0 0 0 0.13  0 0 0 0 0.08  0 0 0 0.5 0'/></filter><rect width='100%' height='100%' filter='url(%23n)' opacity='0.05'/></svg>");

  /* Heading tracking — opt-in utility, not global */
  --heading-tracking-tight: -0.01em;

  /* Primary CTA sheen — consumed by .btn-primary or Button.tsx primary variant via inline gradient overlay */
  --button-primary-sheen-top: linear-gradient(to bottom, rgba(255,255,255,0.12), rgba(255,255,255,0));
  --button-primary-inset-highlight-top: inset 0 1px 0 rgba(255,255,255,0.18);
  --button-primary-inset-shadow-bottom: inset 0 -1px 0 rgba(0,0,0,0.12);

### NEW dark-mode override entries to APPEND inside the same [data-theme=dark] {} block

  /* No grain in dark — overlay handled via background-image: none on body[data-theme=dark] */
  --grain-noise: none;

  /* Dark shadows already correct (rgba(0,0,0,...)) — do NOT migrate to warm tint in dark */

### Body `background-image` overlay wiring

Current `body { background: var(--color-bg); }` (line ~230). Change to:

  body {
    background-color: var(--color-bg);
    background-image: var(--grain-noise);
    background-repeat: repeat;
    background-size: 200px 200px;
  }

  [data-theme='dark'] body {
    background-image: none;
  }

### Current v1 font <link> block in leanshot/index.html (lines 13–37)

Three <link> tags with IDENTICAL URLs (preload, stylesheet media=print onload swap, <noscript> fallback). Existing URL:

  https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Fraunces:ital,opsz,wght@0,9..144,300..700;1,9..144,300..700&family=JetBrains+Mono:wght@500;600&display=swap

v2 URL (swap into ALL THREE link tags — they MUST remain identical):

  https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700;800;900&family=Geist+Mono:wght@400;500;600;700&family=Fraunces:ital,opsz,wght@0,9..144,300..700;1,9..144,300..700&display=swap

- Geist weights 400-900 (DS-01 + design bundle calls for full range — Black 900 used for hero numerals)
- Geist Mono weights 400-700 (DS-01)
- Fraunces ital,opsz,wght@0,9..144,300..700;1,9..144,300..700 (unchanged from v1)
- &display=swap MUST be preserved (Phase 2.1 perf-fix invariant)

### CSP snapshot (leanshot/tests/csp/csp-snapshot.txt)

Already includes:
  font-src 'self' data: https://fonts.gstatic.com;
  style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;

→ NO CSP edits required. Phase 12 12-04 snapshot test must continue to pass post-merge.

### CI lighthouse job (.github/workflows/ci.yml lines 443–473) — pattern to extend

  lighthouse:
    name: Lighthouse (Vercel preview)
    runs-on: ubuntu-latest
    needs: [lint, format-check, typecheck, test-unit, test-e2e, compliance-copy, deno-test]
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
      - name: Wait for Vercel preview deployment
        id: wait
        uses: patrickedqvist/wait-for-vercel-preview@v1.3.2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          max_timeout: 300
          check_name: 'leanshot-app – Vercel'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: leanshot/package-lock.json
      - run: npm ci
      - name: Run Lighthouse against Vercel preview
        run: npx --yes @lhci/cli@0.15.1 autorun --collect.url=${{ steps.wait.outputs.url }}
        env:
          LHCI_BUILD_CONTEXT__CURRENT_HASH: ${{ github.sha }}

→ Append ONE new step after the existing autorun step, NOT a new top-level job (Phase 12 D-12 pattern + memory `feedback_planner_iter1_anti_patterns.md`).

### Current lighthouserc.json (leanshot/lighthouserc.json)

  { "ci": { "collect": { "numberOfRuns": 3, "settings": { "preset": "desktop" } },
            "assert": { "assertions": {
              "categories:performance": ["error", { "minScore": 0.9 }],
              "categories:accessibility": ["error", { "minScore": 0.9 }],
              "categories:best-practices": ["error", { "minScore": 0.9 }]
            }},
            "upload": { "target": "temporary-public-storage" } } }

→ `lhci autorun` already writes `.lighthouseci/lhr-*.json` to disk by default; the delta script reads from there. Update upload target OR add `"outputDir"` if needed, but prefer leaving the config alone and reading the default `.lighthouseci/` dir.

### 13-FCP-BASELINE.json schema (NEW file)

  {
    "fcp_ms": <integer — median of 3 Lighthouse runs on current main Vercel deploy>,
    "lcp_ms": <integer — median of 3 Lighthouse runs on current main Vercel deploy>,
    "captured_sha": "<git rev-parse main at capture time>",
    "captured_at": "<ISO-8601 timestamp>",
    "captured_url": "<Vercel deployment URL>",
    "lhci_version": "0.15.1",
    "preset": "desktop",
    "tolerance_pct": 5
  }

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture FCP + LCP baseline from current main + write 13-FCP-BASELINE.json</name>
  <files>leanshot/.planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json</files>
  <action>
    Capture the FCP + LCP baseline from the CURRENT `main` Vercel deploy BEFORE any token/font change touches the working tree.

    1. Get the current `main` commit SHA: `git rev-parse origin/main`.
    2. Resolve the current `leanshot-app` Vercel deployment URL for `main`. Two acceptable paths:
       - Preferred: `npx --yes vercel@latest ls --prod --json 2>/dev/null | jq '.[] | select(.name == "leanshot-app") | .url' | head -1` (no auth needed if `VERCEL_TOKEN` is set; if not, fall back to the next option).
       - Fallback: read the latest GitHub Actions `lighthouse` job log via `gh run list --workflow ci.yml --branch main --limit 1` then `gh run view <id> --log` and extract the `Wait for Vercel preview deployment` step's `url` output.
       - Last resort: the canonical production hostname `https://leanshot.app` (record this choice in the baseline JSON so the gate-time URL matches).
    3. Run Lighthouse 3 times against that URL with the same preset the CI uses (desktop):
         `npx --yes @lhci/cli@0.15.1 collect --url=<url> --numberOfRuns=3 --settings.preset=desktop`
       This writes `.lighthouseci/lhr-*.json` audit files.
    4. Read each `lhr-*.json` and extract `audits["first-contentful-paint"].numericValue` and `audits["largest-contentful-paint"].numericValue`. Take the MEDIAN across the 3 runs for each metric (round to integer ms).
    5. Write `leanshot/.planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json` with the schema from `<interfaces>` (fcp_ms, lcp_ms, captured_sha, captured_at, captured_url, lhci_version, preset, tolerance_pct). `captured_at` is ISO-8601 UTC. `tolerance_pct` is `5`.
    6. Delete the local `.lighthouseci/` directory after capture (it's not committed — `.gitignore` should already exclude it, but verify).

    Constraint: DO NOT yet touch `src/index.css` or `index.html`. The baseline must reflect the pre-Phase-13 state of `main`. If the working tree has any uncommitted Phase 13 changes when this task runs, abort and have the user stash them first.

    Note (per D-07): we deliberately persist the baseline in the phase folder (not in `.lighthouseci/` ephemera) so future phases can audit the rollback boundary. This file is checked in.
  </action>
  <verify>
    <automated>node -e "const b=require('./leanshot/.planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json'); const required=['fcp_ms','lcp_ms','captured_sha','captured_at','captured_url','lhci_version','preset','tolerance_pct']; const missing=required.filter(k=>!(k in b)); if(missing.length){console.error('MISSING',missing);process.exit(1)} if(typeof b.fcp_ms!=='number'||b.fcp_ms<=0){console.error('bad fcp_ms');process.exit(1)} if(typeof b.lcp_ms!=='number'||b.lcp_ms<=0){console.error('bad lcp_ms');process.exit(1)} if(b.tolerance_pct!==5){console.error('tolerance_pct must be 5');process.exit(1)} console.log('OK',b)"</automated>
  </verify>
  <done>
    `leanshot/.planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json` exists, validates against the schema in `<interfaces>`, contains plausible ms values (typically 800–3000 ms desktop), and `captured_sha` matches the pre-Phase-13 `origin/main` HEAD.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mutate src/index.css `@theme` + `[data-theme=dark]` + body bg with v2 token values</name>
  <files>leanshot/src/index.css</files>
  <action>
    In-place value mutation of `leanshot/src/index.css`. The Tailwind v4 plumbing is unchanged — only token VALUES migrate, plus a handful of APPENDED tokens for v2-only features.

    Edit operations (DO NOT use `Write` to rewrite the file; use `Edit` so the existing structure / comments stay intact):

    A. Inside `@theme { … }` (lines 11–153), change these values:
       - `--color-cream-100: #efebe0;` → `#F2EDE0`
       - `--color-cream-card: #fdfbf6;` → `#FEFCF7`
       - `--color-border: #e2ddd0;` → `#DAD3C0`
       - `--color-text-secondary: #556660;` → `#4D5E58`
       - In all six shadow tokens (`--shadow-xs`, `--shadow-sm`, `--shadow`, `--shadow-md`, `--shadow-lg`, `--shadow-2xl`), replace the literal `rgba(22, 34, 31, …)` with `rgba(40, 32, 20, …)` keeping the alpha and the leading `0 Npx Mpx` offset block IDENTICAL. Do NOT touch `--shadow-hero` (the teal-tinted hero shadow stays cool — see `<interfaces>`).
       - `--text-base: 0.9375rem;` → `1rem;` (16 px floor). Keep the `/* 15px */` comment updated to `/* 16px floor (v2) */`. `--text-base--line-height: 1.55;` stays.
       - `--font-sans: 'Inter', -apple-system, …;` → `'Geist', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;`
       - `--font-mono: 'JetBrains Mono', ui-monospace, …;` → `'Geist Mono', ui-monospace, 'SF Mono', Menlo, monospace;`
       - `--font-display: 'Fraunces', …` — LEAVE UNCHANGED.

    B. APPEND the following new tokens inside the same `@theme { … }` block (right after the existing `--animate-*` block, before the closing brace). Add a section comment `/* ---- v2 additions ---- */`:
       - `--grain-noise:` — set to the inline SVG data URL from `<interfaces>` (paper-grain fractal noise, ~5 % opacity, 200×200, warm-brown matrix).
       - `--heading-tracking-tight: -0.01em;` — exposed as a token; consumed by an opt-in utility (see step D).
       - `--button-primary-sheen-top: linear-gradient(to bottom, rgba(255,255,255,0.12), rgba(255,255,255,0));`
       - `--button-primary-inset-highlight-top: inset 0 1px 0 rgba(255,255,255,0.18);`
       - `--button-primary-inset-shadow-bottom: inset 0 -1px 0 rgba(0,0,0,0.12);`

    C. APPEND inside the existing `[data-theme='dark'] { … }` block (lines 158–203), before the closing brace:
       - `--grain-noise: none;` (overlay is light-mode only per `<interfaces>`)
       - Leave dark shadows AS-IS — they already use `rgba(0,0,0,…)` and stay correct in dark.
       - Leave the dark `--button-primary-*` sheen tokens INHERITED from `@theme` (sheen renders fine on the dark teal-300 primary; if QA later wants a separate dark sheen, follow-up phase can override — out of scope here).

    D. MUTATE the existing `body { … }` rule (line ~225):
       - Change `background: var(--color-bg);` to:
         ```
         background-color: var(--color-bg);
         background-image: var(--grain-noise);
         background-repeat: repeat;
         background-size: 200px 200px;
         ```
       - Keep every OTHER line of `body { … }` (font-family, font-size, color, smoothing, feature-settings, transition) IDENTICAL.

       Append a new rule directly after the `body { … }` block:
       ```
       [data-theme='dark'] body {
         background-image: none;
       }
       ```

    E. MUTATE the existing `.heading-display` rule OR add a new opt-in `.heading-tight` utility (planner's call — pick whichever requires no consumer file edits in this plan). Recommended: APPEND a new utility, leaving `.heading-display` untouched so Phase 13 Wave-2 component refresh can opt in selectively:
       ```
       .heading-tight {
         letter-spacing: var(--heading-tracking-tight);
       }
       ```
       (D-02 spec calls for `-0.01em` tracking on headings; we expose it as opt-in to keep Wave-2 component refresh free to apply it surface-by-surface without re-doing the global cascade.)

    Constraint reminders:
    - DO NOT add any `@import url(...fonts.googleapis...)` anywhere in `src/index.css` — chat1.md landmine 2 + D-10. The `@import 'tailwindcss';` on line 1 stays.
    - DO NOT introduce a SECOND `@theme {}` block — append inside the existing one (Tailwind v4 silently ignores duplicate `@theme` blocks in dev but the existing tooling assumes one).
    - All hex literals stay confined to `@theme {}` + `[data-theme='dark'] {}` — never inline elsewhere in `src/index.css` (CLAUDE.md anti-pattern: "Hard-coding colors in components"). The new `--grain-noise` data URL contains hex matrix coefficients in a `feColorMatrix` — that's inside the SVG payload, not a CSS color, and is allowed.
    - The `--text-base: 1rem` change is intentional and ripples to every consumer using `font-size: var(--text-base)` (most body copy). Wave-2 visual regression suite will catch any unwanted vertical-rhythm drift.

    After saving, run `npx prettier --write leanshot/src/index.css` so the diff is the smallest possible (eslint-plugin-import-x doesn't lint CSS but prettier does — keep the diff reviewable).
  </action>
  <verify>
    <automated>cd leanshot && node -e "const fs=require('fs');const c=fs.readFileSync('src/index.css','utf8');const must=[/--color-cream-100:\s*#F2EDE0/i,/--color-cream-card:\s*#FEFCF7/i,/--color-border:\s*#DAD3C0/i,/--color-text-secondary:\s*#4D5E58/i,/--font-sans:\s*'Geist'/,/--font-mono:\s*'Geist Mono'/,/--font-display:\s*'Fraunces'/,/--text-base:\s*1rem/,/--grain-noise:/,/--heading-tracking-tight:\s*-0\.01em/,/--button-primary-sheen-top:/,/--button-primary-inset-highlight-top:/,/--button-primary-inset-shadow-bottom:/,/rgba\(40,\s*32,\s*20,\s*0\.04\)/,/rgba\(40,\s*32,\s*20,\s*0\.06\)/,/rgba\(40,\s*32,\s*20,\s*0\.16\)/,/background-image:\s*var\(--grain-noise\)/,/\[data-theme='dark'\]\s+body/];const missing=must.filter(r=>!r.test(c));if(missing.length){console.error('MISSING:',missing.map(r=>r.source));process.exit(1)}const forbidden=[/@import\s+url\([^)]*fonts\.googleapis/,/--font-sans:[^;]*'Inter'/,/--font-mono:[^;]*'JetBrains Mono'/,/--color-cream-100:\s*#efebe0/i,/--color-cream-card:\s*#fdfbf6/i,/rgba\(22,\s*34,\s*31,\s*0\.04\)/,/rgba\(22,\s*34,\s*31,\s*0\.06\)/];const present=forbidden.filter(r=>r.test(c));if(present.length){console.error('FORBIDDEN STILL PRESENT:',present.map(r=>r.source));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <done>
    `leanshot/src/index.css` contains all v2 token values listed in `<interfaces>`, all v1 hex literals (`#efebe0`, `#fdfbf6`, `#e2ddd0`, `#556660`) are gone from the `@theme` block, the cool-shadow `rgba(22,34,31,…)` recipe is replaced by warm `rgba(40,32,20,…)` in the six body shadows, `--text-base` is `1rem`, the paper-grain noise overlay is wired on `body` in light mode and disabled in dark, font tokens reference Geist + Geist Mono + Fraunces, NO `@import url(fonts.googleapis...)` exists anywhere in the file, and `npm run build` (from inside `leanshot/`) succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Swap font `<link>` URLs in leanshot/index.html (preserve preload+onload swap)</name>
  <files>leanshot/index.html</files>
  <action>
    In-place mutation of `leanshot/index.html` lines 13–37. The Phase 2.1 perf-fix `<link rel="preload" as="style">` + `<link rel="stylesheet" media="print" onload="this.media='all'">` + `<noscript><link rel="stylesheet">` pattern MUST be preserved verbatim — ONLY the three `href` URLs change, and all three MUST remain IDENTICAL.

    Replace every occurrence of:
      `https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Fraunces:ital,opsz,wght@0,9..144,300..700;1,9..144,300..700&family=JetBrains+Mono:wght@500;600&display=swap`

    With:
      `https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700;800;900&family=Geist+Mono:wght@400;500;600;700&family=Fraunces:ital,opsz,wght@0,9..144,300..700;1,9..144,300..700&display=swap`

    Three occurrences — preload link (line ~26), stylesheet link (line ~30), `<noscript>` fallback link (line ~37). After the swap, ALL THREE href values must be byte-identical (sed -n 's/.*href="\([^"]*\)".*/\1/p' index.html | sort -u | wc -l → 1).

    KEEP:
    - The Phase 2.1 perf-fix comment block at lines 15–22 verbatim (it documents WHY the pattern exists — invaluable for any future refactor).
    - `<link rel="preconnect" href="https://fonts.googleapis.com" />` and `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />` exactly as-is — these still apply to the Geist URLs (same origin: Google Fonts API + Google Fonts CDN).
    - `media="print"` + `onload="this.media='all'"` on the stylesheet link — Filament Group canonical pattern.
    - `<noscript>` wrapper around the third `<link rel="stylesheet">`.
    - `&display=swap` query param — Phase 2.1 invariant; removing it would re-introduce render-blocking on font load.

    Update the meta `theme-color` ONLY if needed (currently `#EFEBE0` for light — that's the OLD cream value). Change `<meta name="theme-color" content="#EFEBE0" media="(prefers-color-scheme: light)" />` to `content="#F2EDE0"` to match the v2 `--color-cream-100`. Leave the dark theme-color `#0B1413` IDENTICAL (matches the v2 `--color-bg` in dark).

    DO NOT add any `<style>` block. DO NOT add a font preload for individual font files (Google Fonts CSS handles that). DO NOT add any new `<link>` tags.

    Constraint: chat1.md landmine 2 — `@import` font chain forbidden. If at any point during this task an `@import` of Google Fonts appears in any file under `leanshot/`, abort and fix.
  </action>
  <verify>
    <automated>cd leanshot && node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const v2='https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700;800;900&family=Geist+Mono:wght@400;500;600;700&family=Fraunces:ital,opsz,wght@0,9..144,300..700;1,9..144,300..700&display=swap';const urls=[...h.matchAll(/href=\"(https:\/\/fonts\.googleapis\.com[^\"]+)\"/g)].map(m=>m[1]);if(urls.length!==3){console.error('Expected 3 font hrefs, got',urls.length);process.exit(1)}if(!urls.every(u=>u===v2)){console.error('Font URLs not identical or wrong:',urls);process.exit(1)}if(!h.includes(\"onload=\\\"this.media='all'\\\"\")){console.error('Missing onload swap');process.exit(1)}if(!h.includes('<noscript>')){console.error('Missing noscript');process.exit(1)}if(/family=Inter:|family=JetBrains\\+Mono:/.test(h)){console.error('v1 font family still present');process.exit(1)}if(!h.includes('content=\"#F2EDE0\"')){console.error('light theme-color not v2');process.exit(1)}console.log('OK')"</automated>
  </verify>
  <done>
    `leanshot/index.html` has exactly 3 `<link>` hrefs pointing to Google Fonts, all 3 byte-identical, all 3 targeting the v2 URL (Geist + Geist Mono + Fraunces), `media="print"` + `onload="this.media='all'"` preserved on the stylesheet link, `<noscript>` fallback preserved, `<meta name="theme-color"...light>` updated to `#F2EDE0`, dark theme-color unchanged, no `@import` of Google Fonts anywhere in `leanshot/`, `npm run build` succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 4: Author scripts/assert-fcp-lcp-delta.sh + wire CI lighthouse job step</name>
  <files>leanshot/scripts/assert-fcp-lcp-delta.sh, .github/workflows/ci.yml</files>
  <action>
    Author `leanshot/scripts/assert-fcp-lcp-delta.sh` (executable bash) implementing the FCP + LCP delta gate (D-07 + D-13).

    Script contract:
    - Inputs (env vars or argv): path to LHCI output dir (default `.lighthouseci`), path to baseline JSON (default `.planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json`), optional `--dry-run` flag for synthetic-value smoke tests.
    - Behavior:
      1. Read `fcp_ms` + `lcp_ms` + `tolerance_pct` from the baseline JSON via `jq`.
      2. Find all `lhr-*.json` files in the LHCI output dir.
      3. Extract `audits["first-contentful-paint"].numericValue` and `audits["largest-contentful-paint"].numericValue` from each, take the MEDIAN per metric (rounded to integer ms).
      4. Compute deltas:
           fcp_delta_pct = (current_fcp - baseline_fcp) / baseline_fcp * 100
           lcp_delta_pct = (current_lcp - baseline_lcp) / baseline_lcp * 100
         (Use `awk` or `bc` for floating-point math — bash arithmetic is integer-only.)
      5. If `fcp_delta_pct > tolerance_pct` OR `lcp_delta_pct > tolerance_pct`, print a clear failure message including the baseline, current, and delta values, and `exit 1`.
      6. If both are within tolerance (including negative deltas — speedups are fine), print a passing line with both deltas and `exit 0`.
    - Header comment block:
      - Reference D-07 + D-13.
      - Document the tolerance is `5%` per `13-FCP-BASELINE.json.tolerance_pct` (NOT hardcoded in the script — read from the JSON so future phases can re-baseline without editing the script).
      - Document the `--dry-run` mode for unit-test purposes: accepts `--current-fcp=<ms> --current-lcp=<ms>` and skips LHCI output parsing, useful for verifying the script's math in CI smoke tests.
    - chmod +x the script after writing.

    Then edit `.github/workflows/ci.yml` `lighthouse:` job (lines 443–473):
    - Append ONE step after the existing `Run Lighthouse against Vercel preview` step (the one running `npx --yes @lhci/cli@0.15.1 autorun`). Name the new step: `Assert FCP/LCP delta vs Phase 13 baseline (D-07)`.
    - Step runs `bash leanshot/scripts/assert-fcp-lcp-delta.sh` from the repo root, passing the LHCI output dir (`.lighthouseci` is the default lhci writes to in the working directory — the autorun step runs from repo root via the `actions/checkout@v4` checkout, so it writes to `./.lighthouseci`).
    - DO NOT change `needs:` on the lighthouse job. DO NOT introduce a new top-level job. DO NOT change `if: github.event_name == 'pull_request'` — the gate runs on PRs only (baseline is captured manually before PR-1 opens, so PR-1's own lighthouse run is the first gated run).
    - Preserve every other step in the lighthouse job verbatim.

    Optional (only if `lhci autorun` doesn't write `.lighthouseci/lhr-*.json` by default in this CI environment): add `--upload.target=filesystem` and `--upload.outputDir=.lighthouseci` to the autorun command. Verify locally by running `npx --yes @lhci/cli@0.15.1 collect --url=https://example.com --numberOfRuns=1` and confirming `.lighthouseci/` is populated — `lhci collect` writes there by default, so no rc change should be needed.

    Smoke-test the script locally (this is part of the task, not the verify step):
    - Synthetic-pass: `bash scripts/assert-fcp-lcp-delta.sh --dry-run --current-fcp=$(jq -r .fcp_ms .planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json) --current-lcp=$(jq -r .lcp_ms .planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json)` → exit 0.
    - Synthetic-fail: `bash scripts/assert-fcp-lcp-delta.sh --dry-run --current-fcp=$(($(jq -r .fcp_ms .planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json) * 110 / 100)) --current-lcp=$(jq -r .lcp_ms .planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json)` → exit 1 with FCP-too-high message.
  </action>
  <verify>
    <automated>cd leanshot && test -x scripts/assert-fcp-lcp-delta.sh && bash scripts/assert-fcp-lcp-delta.sh --dry-run --current-fcp=$(jq -r .fcp_ms .planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json) --current-lcp=$(jq -r .lcp_ms .planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json) && ! bash scripts/assert-fcp-lcp-delta.sh --dry-run --current-fcp=$(($(jq -r .fcp_ms .planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json) * 110 / 100)) --current-lcp=$(jq -r .lcp_ms .planning/phases/13-design-system-v2-rollout/13-FCP-BASELINE.json) 2>/dev/null && cd .. && grep -q 'assert-fcp-lcp-delta' .github/workflows/ci.yml && grep -v '^#' .github/workflows/ci.yml | grep -c 'assert-fcp-lcp-delta' | (read n; test "$n" -ge 1)</automated>
  </verify>
  <done>
    `leanshot/scripts/assert-fcp-lcp-delta.sh` exists, is executable, exits 0 on synthetic equal values, exits non-zero on synthetic +10 % values, reads `tolerance_pct` from the baseline JSON (not hardcoded), and `.github/workflows/ci.yml` has exactly ONE new step inside the existing `lighthouse:` job invoking the script (verified by grep). No new top-level CI job. CSP snapshot test still passes (no script-src additions).
  </done>
</task>

<task type="auto">
  <name>Task 5: Build verification, CSP snapshot, lint, typecheck — end-of-plan green sweep</name>
  <files>leanshot/src/index.css, leanshot/index.html</files>
  <action>
    Run the existing CI invariants locally to confirm no Phase 12 guard regresses, no test fails, and the bundle ceiling holds.

    1. `cd leanshot && npm run lint` — must pass clean (eslint-plugin-import-x ignores CSS/HTML; this catches any unintended TS/TSX touch).
    2. `cd leanshot && npm run typecheck` (or `tsc -b`) — must pass clean.
    3. `cd leanshot && npm run build` — must succeed. Capture the index chunk gz size from Vite's `dist/` output; record in the SUMMARY.md (next phase's plan-checker compares).
    4. `cd leanshot && npm run test -- --run tests/csp/csp-snapshot.test.ts` — CSP snapshot test must still pass. The font-src / style-src lines for fonts.googleapis.com + fonts.gstatic.com already exist (verified pre-plan in `<interfaces>`); the font URL swap does NOT touch the CSP allowlist. If this test fails, the CSP middleware/header drifted somewhere else — investigate before merging.
    5. `cd leanshot && npm run dev` (5-second smoke, then Ctrl-C) — verify the dev server starts without console errors. NOT a hard gate; smoke only.
    6. Visual smoke (developer eyeball, no automated check possible at this layer):
       - Open `http://localhost:5173/` in a browser.
       - Open DevTools → Network → Fonts. Confirm 3 Google Fonts requests fire: one for Geist, one for Geist Mono, one for Fraunces (or merged variants thereof). No `Inter-*.woff2` or `JetBrainsMono-*.woff2` requests fire.
       - Open DevTools → Elements → `<html>` → `:root` computed styles. Verify `--color-cream-100: #F2EDE0`, `--color-cream-card: #FEFCF7`, `--font-sans` starts with `'Geist'`, `--font-mono` starts with `'Geist Mono'`, `--grain-noise` resolves to a `url("data:image/svg+xml...")` value.
       - Toggle to `[data-theme='dark']` (e.g., via the theme toggle in `Sidebar.tsx`). Verify `--grain-noise` resolves to `none` (no overlay in dark).
    7. Commit all files in this plan using a single `git commit -- <pathspec>` invocation listing every file in `files_modified` (per memory `feedback_parallel_executor_git_isolation.md` — even though this plan runs solo, the discipline keeps Wave-2 executors consistent).

       Commit message format (matches Phase 12 convention from the recent log):
       ```
       feat(13-01): token + font swap + FCP/LCP CI gate (DS-01, DS-02, D-02, D-07, D-10, D-11)
       ```

    Constraint: this plan MUST land alone on `main` before any Wave-2 executor starts (D-02 + D-11). The SUMMARY.md you write at end-of-plan MUST state this explicitly so the orchestrator does not parallel-dispatch Wave 2 prematurely.
  </action>
  <verify>
    <automated>cd leanshot && npm run lint && npm run typecheck && npm run build && npm run test -- --run tests/csp/csp-snapshot.test.ts</automated>
  </verify>
  <done>
    `npm run lint`, `npm run typecheck`, `npm run build`, and the CSP snapshot test all pass clean. Dev server starts without console errors. Visual smoke (font network requests + computed CSS variable values + dark-mode overlay disable) all behave as documented. All Phase-13-Plan-01 files committed in a single `git commit -- <pathspec>` invocation. SUMMARY.md states "Wave 2 must wait for this PR to land on main per D-02" verbatim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Google Fonts CDN (`fonts.googleapis.com` + `fonts.gstatic.com`) | Third-party font CSS + woff2 binaries crossed via `<link rel=preload>` + `<link rel=stylesheet>`. Already allowed in current CSP `style-src` + `font-src` directives. |
| browser → inline SVG data URL (`--grain-noise`) | Browser parses an inline `<svg>` from a CSS `url(data:image/svg+xml;...)` value applied as `background-image`. No third-party origin — fully sandboxed inside the document's own data scheme. |
| repo → committed baseline JSON (`13-FCP-BASELINE.json`) | A planning artifact, but the CI gate reads it as authoritative truth. If a malicious contributor inflates `fcp_ms` they bypass the gate. Trust enforced by code review on the PR that lands this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.01-01 | Spoofing | Google Fonts CDN | accept | Existing CSP `style-src https://fonts.googleapis.com` + `font-src https://fonts.gstatic.com` is the project's already-accepted trust boundary (Phase 2.1 + Phase 12 12-04 snapshot). No change in this plan. |
| T-13.01-02 | Tampering | `13-FCP-BASELINE.json` | mitigate | Baseline JSON is checked in and reviewed on the PR that captures it. Captured-SHA field provides an audit trail. CI script reads `tolerance_pct` from the file itself, so inflating tolerance is also visible in the diff. |
| T-13.01-03 | Repudiation | CI gate result | mitigate | The Lighthouse job log preserves both the baseline JSON (read at step start) and the current run's audit JSON, so any pass/fail is reproducible from CI artifacts. |
| T-13.01-04 | Info disclosure | Inline SVG noise overlay | accept | The fractal-noise SVG contains no user data — it's a static decorative pattern. Same trust level as any other CSS background. |
| T-13.01-05 | Denial of service | Font swap regression | mitigate | The FCP/LCP delta gate is itself the mitigation. If Geist's CDN serves slower than Inter's (hypothetical), the PR fails CI and is reverted. |
| T-13.01-06 | Elevation of privilege | `<link>` font load | accept | `<link rel=stylesheet>` from a CSP-allowed origin cannot execute scripts; `script-src` in the existing CSP is `'self'` only (no fonts.googleapis.com listed for script-src). No elevation surface. |

</threat_model>

<verification>
## Plan-Level Verification

1. `git diff --stat` on the PR-1 branch shows ONLY the 6 expected files in `files_modified` — no stray edits.
2. `grep -RIn '@import url(' leanshot/ | grep -i fonts.googleapis` returns ZERO matches (chat1.md landmine 2 enforcement).
3. `node -e` schema check on `13-FCP-BASELINE.json` passes (Task 1 verify).
4. All v2 token literals present + all v1 token literals absent in `src/index.css` (Task 2 verify).
5. All 3 font `<link>` URLs in `index.html` byte-identical + v2-shaped (Task 3 verify).
6. `assert-fcp-lcp-delta.sh` exits 0 on synthetic equal + exits 1 on synthetic +10 % (Task 4 verify).
7. `npm run lint`, `npm run typecheck`, `npm run build`, CSP snapshot test all green (Task 5 verify).
8. Bundle index chunk gz remains ≤ 50 kB (Phase 12 12-01 ceiling) — record actual size in SUMMARY.md.
9. After PR-1 lands on `main` and the GitHub Actions `lighthouse:` job completes, the new "Assert FCP/LCP delta vs Phase 13 baseline (D-07)" step reports a PASS line with both deltas ≤ 5 %.
</verification>

<success_criteria>
- DS-01: All three Google Fonts (Geist, Geist Mono, Fraunces) loaded via `<link>` + preconnect in `<head>` — verified by Task 3 + Task 5 dev-server network smoke.
- DS-02: v2 token values present in `:root` for color / shadow / border / text-secondary / type-floor — verified by Task 2 verify regex + Task 5 dev-server computed-style smoke.
- D-02: Token + font swap lands ALONE on `main` before Wave 2 starts — enforced by SUMMARY.md callout + this being the only plan in Wave 1.
- D-07 + D-13: FCP delta ≤ 5 % AND LCP delta ≤ 5 % vs `13-FCP-BASELINE.json` — enforced by `scripts/assert-fcp-lcp-delta.sh` invoked from the CI `lighthouse:` job.
- D-10: NO `@import url(...fonts.googleapis...)` chain anywhere in `leanshot/` — enforced by grep gate in `<verification>`.
- D-11: Tokens-only swap precedes components/illustrations/login/marketing — Wave 1 = this plan only; Wave 2 plans depend on `13-01` per outline.
- Phase 12 invariants preserved: CSP snapshot test green, bundle index chunk ≤ 50 kB gz, no native-firewall lint violation (Phase 13 touches no `src/lib/native/*`).
</success_criteria>

<output>
After completion, create `leanshot/.planning/phases/13-design-system-v2-rollout/13-01-SUMMARY.md` documenting:
- The captured baseline values (FCP ms, LCP ms, captured SHA, captured URL).
- The exact v1 → v2 token diff (a small table of the ~10 changed values).
- The new tokens appended (`--grain-noise`, `--heading-tracking-tight`, three `--button-primary-*` sheen tokens).
- The Geist + Geist Mono + Fraunces Google Fonts URL (verbatim, all 3 link tags) for posterity.
- The bundle index chunk gz size after this plan (compare to Phase 12 close: 21.49 kB).
- A bold callout: **"WAVE 2 EXECUTORS MUST WAIT FOR THIS PR TO LAND ON `main` (D-02 + D-11). Do not parallel-dispatch Wave 2 plans until the PR-1 merge commit is on `main` AND the `lighthouse:` CI job has passed on that merge commit."**
- The post-merge "Assert FCP/LCP delta" CI step's pass/fail outcome (only known after merge — leave a placeholder if writing the SUMMARY pre-merge).
- The CSP snapshot test result (must still pass).
- Any follow-ups for Wave 2: e.g., Button.tsx primary variant needs to consume `--button-primary-sheen-top` + `--button-primary-inset-*` (13-02), heading components opt into `.heading-tight` utility (13-02 + 13-04 + 13-05).
</output>
