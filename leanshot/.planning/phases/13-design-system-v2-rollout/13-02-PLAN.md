---
phase: 13-design-system-v2-rollout
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/components/ui/Card.tsx
  - src/components/ui/Button.tsx
  - src/components/ui/Pill.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/AppShell.tsx
autonomous: true
requirements: [DS-05, DS-06, DS-07, DS-08]
must_haves:
  truths:
    - "Card consumers (38 files) continue to render with the existing default/elevated/interactive/hero/flat variants AND four new additive variants are available: selected, clickable, tonal, footer."
    - "Button consumers (57 files) continue to render with existing variants AND a new 'tonal' variant + optional 'count' chip slot are available; aria-busy / aria-label / disabled+loading wiring is preserved verbatim."
    - "Pill consumers (3 files) continue to render with active/size/leadingIcon AND new additive props are available: count chip, iconOnly (with required aria-label), disabled styling; PillGroup gains an opt-in 'segmented' boolean for joined-pill segmented-control visual."
    - "Sidebar visually snaps instantly between 72 px (collapsed) and 232 px (expanded) on a data-attribute toggle, with ZERO CSS transition on the width property; inner content (brand wordmark, tab labels, account row) fades over 200 ms; reduced-motion users see no fade."
    - "AppShell's <main> left margin tracks the sidebar width (72 / 232 / 0 on mobile) — no content-shift bug; sidebar remains position: fixed; .app-shell stays a plain block container (NOT display:flex)."
    - "Focus rings on every refreshed primitive (Card clickable, Button all variants, Pill all states, Sidebar tab buttons) use the same focus-visible:ring-2 ring-[var(--color-primary)] ring-offset-2 ring-offset-[var(--color-bg)] pattern verbatim — zero regression."
    - "No consumer call-site is modified by this plan (refresh-in-place per D-01)."
    - "tsc -b passes; npm run lint passes; existing unit + e2e tests still pass (visual snapshots are captured in 13-06, not here)."
  artifacts:
    - path: "src/components/ui/Card.tsx"
      provides: "Card primitive — CardVariant union widened with 'selected' | 'clickable' | 'tonal' | 'footer'"
      contains: "selected"
    - path: "src/components/ui/Button.tsx"
      provides: "Button primitive — ButtonVariant union adds 'tonal'; new optional 'count' prop renders right-aligned chip"
      contains: "tonal"
    - path: "src/components/ui/Pill.tsx"
      provides: "Pill primitive — adds count + iconOnly + disabled; PillGroup adds 'segmented' opt-in"
      contains: "segmented"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Sidebar with data-sidebar=collapsed|expanded attribute toggle, w-[72px] / w-[232px], no transition on width, 200ms opacity fade on inner content"
      contains: "data-sidebar"
    - path: "src/components/layout/AppShell.tsx"
      provides: "Wiring of sidebar collapsed state + matching md:ml-[72px]/md:ml-[232px] on <main>"
      contains: "ml-[232px]"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/components/layout/AppShell.tsx"
      via: "shared `collapsed: boolean` state — Sidebar receives it as a prop; AppShell owns it via useState and toggles via a passed setter prop"
      pattern: "collapsed\\?:\\s*boolean|onToggleCollapsed"
    - from: "src/components/layout/Sidebar.tsx"
      to: "(self) inner content opacity"
      via: "data-sidebar attribute on <aside> + opacity-0/opacity-100 transition-opacity duration-200 on labels/wordmark/account row"
      pattern: "data-sidebar"
    - from: "src/components/ui/Card.tsx"
      to: "CardVariant union"
      via: "'selected' | 'clickable' | 'tonal' | 'footer' appended to existing union; variantClasses table extended"
      pattern: "'selected'\\s*\\|\\s*'clickable'\\s*\\|\\s*'tonal'\\s*\\|\\s*'footer'"
    - from: "src/components/ui/Button.tsx"
      to: "ButtonVariant union + count prop"
      via: "'tonal' appended to union; count?: number | string prop renders <span> chip after children"
      pattern: "'tonal'"
    - from: "src/components/ui/Pill.tsx"
      to: "PillGroup segmented prop"
      via: "segmented?: boolean on PillGroup applies joined-pill border collapse via :first-child / :last-child child selectors"
      pattern: "segmented\\?:\\s*boolean"
---

<objective>
Refresh four core UI primitives in place to the v2 design system, adding new variants additively so all ~100 existing consumers pick up new defaults silently and downstream Phase 14/15 work has the variants it needs. Sidebar gets a 72↔232 px collapse with the chat1.md `var()`-transition landmine explicitly avoided.

Purpose: Deliver DS-05 (Card variants), DS-06 (Button tonal + counter chips + states), DS-07 (Pill segmented + count badges + icon-only), DS-08 (Sidebar instant 72↔232 px snap + 200 ms inner fade) per CONTEXT.md D-01 (refresh-in-place + additive variants), D-12 (sidebar instant snap), and D-15 (no layout-shift or focus-ring regression).

Output: 5 files modified; no new files; no consumer call-site changes. Tasks committed via `git commit -- <pathspec>` per memory `feedback_parallel_executor_git_isolation.md` so this plan can land in parallel with 13-03 and 13-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@leanshot/.planning/phases/13-design-system-v2-rollout/13-CONTEXT.md
@leanshot/.planning/phases/13-design-system-v2-rollout/13-PATTERNS.md
@leanshot/CLAUDE.md

<interfaces>
<!-- Extracted from current src/ — executor uses these directly; no codebase exploration needed. -->

From src/components/ui/Card.tsx (CURRENT — to widen, NOT replace):
- export type CardVariant = 'default' | 'elevated' | 'interactive' | 'hero' | 'flat';
- export interface CardProps extends HTMLAttributes<HTMLDivElement> { variant?: CardVariant; padding?: 'none'|'sm'|'md'|'lg'; span?: 3|4|5|6|7|8|12; enter?: boolean; }
- const variantClasses: Record<CardVariant, string> table at lines 15-23 — append rows, do NOT touch existing rows' class strings.
- Header convention: CardHeader({ title, icon, action }) at lines 71-85 — DO NOT TOUCH.
- StatTile at lines 95-123 — DO NOT TOUCH (consumer of Card; passes variant='default').

From src/components/ui/Button.tsx (CURRENT — to widen, NOT replace):
- export type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'destructive' | 'success' | 'inverse';
- export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> { variant?; size?; loading?; leadingIcon?; trailingIcon?; block?; }
- baseClasses string at lines 23-26 includes the canonical focus-visible:ring-2 ring-[var(--color-primary)] ring-offset-2 ring-offset-[var(--color-bg)] — DO NOT MODIFY.
- aria-busy={loading || undefined} at line 67 — preserve verbatim.
- disabled={disabled || loading} at line 66 — preserve verbatim.
- Loader2 swap at lines 77-78 — preserve verbatim.
- IconButton sibling at lines 92-131 (separate export, separate variant union 'ghost'|'solid'|'inverse') — DO NOT TOUCH; aria-label remains REQUIRED.

From src/components/ui/Pill.tsx (CURRENT — to widen, NOT replace):
- export interface PillProps extends ButtonHTMLAttributes<HTMLButtonElement> { active?: boolean; size?: 'sm'|'md'; leadingIcon?: ReactNode; }
- Pill function at lines 10-36 emits aria-pressed={active} on <button type="button">.
- PillGroup at lines 38-44 wraps with role="group".

From src/components/layout/Sidebar.tsx (CURRENT — refresh in place):
- Single fixed <aside> at lines 46-49: `hidden md:flex fixed top-0 left-0 bottom-0 z-30 w-[80px] flex-col items-center py-5 bg-[var(--color-surface)] border-r border-[var(--color-border)] safe-top`
- data-tour="nav" at line 47 — MUST be preserved (GuidedTour reads it).
- TABS const at lines 22-32 — 9 tabs; label string is the source of the visible expanded-mode text.
- Active tab uses motion.span layoutId="sb-active" at lines 89-95 — preserve verbatim.
- Footer cluster at lines 105-139: AI button, theme toggle, settings button, separator, account-initial avatar.
- Sidebar exports: SidebarProps { onOpenAI, onOpenSettings } — extend with optional `collapsed?: boolean; onToggleCollapsed?: () => void;`. Default `collapsed = false` so existing consumers (only AppShell) and any downstream tests still type-check.

From src/components/layout/AppShell.tsx (CURRENT — wire collapsed state):
- AppShellProps { children, onLogDose, onOpenReport, onOpenAI, onOpenSettings } — DO NOT add to public API.
- Internal `const [sheetOpen, setSheetOpen] = useState(false);` at line 27 — add a SECOND useState beside it: `const [sidebarCollapsed, setSidebarCollapsed] = useState(false);`.
- <Sidebar> mount at line 30 — pass collapsed + onToggleCollapsed.
- <main> at line 31-34 currently has `md:ml-[80px]` — change to `md:ml-[72px]` when collapsed else `md:ml-[232px]` (use cn() + conditional class strings; do NOT use `style={{ marginLeft: ... }}` with var()).
- Mobile bottom-FAB / MobileNav / LegalFooter wiring — DO NOT TOUCH.
</interfaces>

<chat1_landmines>
<!-- VERBATIM from .planning/design-system/chats/chat1.md — internalize before writing code. -->

Landmine 1 (CSS-variable interpolation): "Browsers can't interpolate `var()` without `@property` registration. Applying `transition: width` to a `.sidebar` whose width is set via `width: var(--sidebar-w)` does NOT animate — the browser keeps the initially-resolved value and never re-resolves on attribute changes." → FIX: discrete attribute-selector classes (Tailwind `w-[72px]` / `w-[232px]`) toggled by `data-sidebar="collapsed"` / `data-sidebar="expanded"`. ZERO transition on the width property. Inner content gets `transition-opacity duration-200` instead.

Landmine 3 (flex + fixed sidebar): "`display: flex` on `.app-shell` + `flex: 1` on `.app-main` breaks `position: fixed` sidebar's `margin-left: var(--sidebar-w)` resolution. Sidebar is `position: fixed` anyway — flex provides no benefit. Make `.app-shell` a plain block container." → AppShell's outer `<div>` is already a plain block (`min-h-screen bg-[...] text-[...]`); DO NOT add flex utilities to it.
</chat1_landmines>

<no_layout_shift_audit>
<!-- D-15 (SC #3): "refreshed Card / Button / Pill / Sidebar without layout shift or focus-ring regressions" -->

Per-primitive rule:
- Card: keep `rounded-card relative` base class + same padding scale (`p-0/p-4/p-5 md:p-6/p-6 md:p-7`). Changes to radius/shadow are TOKEN-DRIVEN (var(--shadow-*) re-resolves from 13-01); class strings stay byte-stable for existing variants. New variants (`selected/clickable/tonal/footer`) ship their own class strings and only render when a consumer opts in.
- Button: keep `h-8/h-11/h-13` size scale and `px-3/px-5/px-7` horizontal padding verbatim. New `tonal` variant uses bg-[var(--color-primary-soft)] text-[var(--color-primary)] hover:bg-[var(--color-primary-soft)] hover:brightness-95 — same h/padding so no consumer reflows.
- Pill: keep `h-8 px-3` / `h-10 px-4` size scale verbatim. count chip is `ml-1.5 px-1.5 inline-flex items-center` inside the existing flex row — adds intrinsic width only when consumer passes the prop. iconOnly tightens to `aspect-square` (h-8 w-8 / h-10 w-10) — additive variant, never auto-applied.
- Sidebar: collapsed-width 72 px is 8 px narrower than today's 80 px. Update AppShell <main> `md:ml-[80px]` → `md:ml-[72px]` in the collapsed branch so content doesn't shift. Expanded-width 232 px requires `md:ml-[232px]`. The widening pushes content RIGHT — acceptable (it's the user opting into expand). NO mid-render width transitions.
</no_layout_shift_audit>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Widen Card variants (D-01 + DS-05)</name>
  <files>src/components/ui/Card.tsx</files>
  <read_first>
    - src/components/ui/Card.tsx (entire file — to widen, NOT replace)
    - leanshot/.planning/phases/13-design-system-v2-rollout/13-PATTERNS.md (section 3, lines 121-144 — Card analog)
    - leanshot/.planning/design-system/project/leanshot/src/components/ui/Card.tsx (bundle prototype — for any shape hints; do NOT copy verbatim, just align)
  </read_first>
  <action>
Widen the `CardVariant` type union and the `variantClasses` table additively. Add exactly four new union members: `'selected' | 'clickable' | 'tonal' | 'footer'`. Final union (in this order): `'default' | 'elevated' | 'interactive' | 'hero' | 'flat' | 'selected' | 'clickable' | 'tonal' | 'footer'`.

Append four matching entries to the `variantClasses: Record<CardVariant, string>` table BELOW the existing `flat` entry. Token-driven class strings only (`var(--color-*)`, `var(--shadow-*)`); no hex literals per CLAUDE.md anti-pattern.

Variant semantics (per design bundle + CONTEXT.md D-01):
- `selected` — strong selection state, used by multi-select roster rows / bulk-action targets. Class: surface-elevated bg + 2 px primary border + primary-soft inner ring shadow. Pattern: `'bg-[var(--color-surface-elevated)] border-2 border-[var(--color-primary)] shadow-[0_0_0_4px_var(--color-primary-soft)]'`.
- `clickable` — like `interactive` but with the canonical focus-ring tokenization applied directly (so a `<div>` rendered as `role="button" tabIndex={0}` gets correct focus visualization). Reuse the existing `interactive` hover/translate/shadow rules and ADD the focus-visible:ring-2 ring-[var(--color-primary)] ring-offset-2 ring-offset-[var(--color-bg)] block verbatim from Button.tsx baseClasses (lines 24-25). Class string is the existing interactive entry + that focus block appended.
- `tonal` — subtle accent surface for grouped/secondary content (e.g. AI insights card). Class: `'bg-[var(--color-primary-soft)] border border-[var(--color-primary-soft)] text-[var(--color-text)]'`.
- `footer` — flat sub-surface used inside compound cards for action rows; no shadow, no top-border, picks up parent radius via `rounded-b-card`. Class: `'bg-[var(--color-surface-elevated)] border-t border-[var(--color-border)] rounded-t-none'`.

DO NOT touch:
- `CardHeader` and `StatTile` exports (lines 71-123) — leave verbatim.
- The `paddingClasses` / `spanClasses` tables — leave verbatim.
- The `forwardRef` signature, prop defaults, or the `cn()` composition order.
- Existing entries in `variantClasses` for `default/elevated/interactive/hero/flat` — they must stay byte-stable so the 38 consumers see no visual diff at this level (any token-driven drift comes from 13-01's `@theme` swap, which is the intended behavior).

Reference D-01 in the file as a one-line comment above the four new entries:
`// Phase 13 D-01: additive variants for v2 design system. Defaults unchanged.`

After the edit: run `npx tsc -b` and `npm run lint -- src/components/ui/Card.tsx`. Commit only this file: `git add src/components/ui/Card.tsx && git commit -- src/components/ui/Card.tsx -m "feat(13-02): widen CardVariant with selected/clickable/tonal/footer (DS-05, D-01)"`.
  </action>
  <verify>
    <automated>cd leanshot && npx tsc -b && npm run lint -- src/components/ui/Card.tsx && grep -E "'selected'\s*\|\s*'clickable'\s*\|\s*'tonal'\s*\|\s*'footer'" src/components/ui/Card.tsx && grep -c "variantClasses" src/components/ui/Card.tsx</automated>
  </verify>
  <done>
    - `CardVariant` union contains 9 members in the documented order.
    - `variantClasses` table has 9 entries (4 new appended below `flat`).
    - No existing entry's class string changed.
    - `tsc -b` exits 0.
    - `eslint src/components/ui/Card.tsx` exits 0.
    - Committed via pathspec on this plan's executor branch.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Button 'tonal' variant + 'count' chip + preserved a11y (D-01 + DS-06)</name>
  <files>src/components/ui/Button.tsx</files>
  <read_first>
    - src/components/ui/Button.tsx (entire file — to widen, NOT replace)
    - leanshot/.planning/phases/13-design-system-v2-rollout/13-PATTERNS.md (section 4, lines 148-173 — Button analog)
    - leanshot/.planning/design-system/project/leanshot/src/components/ui/Button.tsx (bundle prototype for tonal class hints)
  </read_first>
  <action>
Two additive changes to `Button.tsx`:

(A) Add `'tonal'` to the `ButtonVariant` union (append after `'inverse'`). Final order: `'primary' | 'secondary' | 'ghost' | 'destructive' | 'success' | 'inverse' | 'tonal'`. Append one entry to `variantClasses: Record<ButtonVariant, string>`:
`tonal: 'bg-[var(--color-primary-soft)] text-[var(--color-primary)] hover:bg-[var(--color-primary-soft)] hover:brightness-95 hover:-translate-y-[1px] shadow-none border border-transparent'`.

(B) Add an optional `count?: number | string` prop to `ButtonProps`. Render as a right-aligned chip AFTER `children`, BEFORE the existing `!loading && trailingIcon` branch. Chip JSX:
`{count !== undefined && (<span aria-hidden className="ml-1 inline-flex items-center justify-center min-w-[20px] h-5 px-1.5 rounded-full text-[11px] font-semibold bg-[var(--color-primary-soft)] text-[var(--color-primary)] tabular-nums">{count}</span>)}`
When the variant is `primary` (and the surrounding text is light-on-dark), the chip inverts via a class-name selector — implement by reading `variant` in the function body and swapping the chip's `bg`/`text` to `bg-white/20 text-[var(--color-primary-foreground)]` when `variant === 'primary' || variant === 'destructive' || variant === 'success'`. The chip is decorative (count is reflected in children content / parent label) so `aria-hidden` is correct.

PRESERVE VERBATIM:
- `baseClasses` string at lines 23-26 — DO NOT MODIFY (DS-08 / SC #3 focus-ring stability).
- `aria-busy={loading || undefined}` at line 67.
- `disabled={disabled || loading}` at line 66.
- `<Loader2 className="size-4 animate-spin" aria-hidden />` at line 78.
- The `IconButton` sibling export and its `aria-label: string` REQUIRED prop typing.
- `sizeClasses` table (h-8/h-11/h-13) — NO height change to existing sizes.

Reference D-01 + DS-06 in a one-line comment above the new union entry:
`// Phase 13 D-01 / DS-06: tonal variant for grouped/secondary CTAs.`

After the edit: run `npx tsc -b` and `npm run lint -- src/components/ui/Button.tsx`. Commit only this file: `git add src/components/ui/Button.tsx && git commit -- src/components/ui/Button.tsx -m "feat(13-02): Button tonal variant + count chip slot (DS-06, D-01)"`.
  </action>
  <verify>
    <automated>cd leanshot && npx tsc -b && npm run lint -- src/components/ui/Button.tsx && grep -E "\|\s*'tonal'" src/components/ui/Button.tsx && grep -E "count\?:\s*number\s*\|\s*string" src/components/ui/Button.tsx && grep -c "aria-busy" src/components/ui/Button.tsx</automated>
  </verify>
  <done>
    - `ButtonVariant` union contains `'tonal'` as the 7th member.
    - `variantClasses` has 7 entries.
    - `count?: number | string` prop added to `ButtonProps`.
    - Chip JSX rendered between `<span>{children}</span>` and the trailingIcon branch.
    - `aria-busy`, `disabled || loading`, `Loader2 aria-hidden`, `baseClasses` focus-ring substring all present unchanged.
    - `tsc -b` exits 0, `eslint` exits 0.
    - Committed via pathspec.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Pill — count chip + iconOnly + disabled + PillGroup segmented (D-01 + DS-07)</name>
  <files>src/components/ui/Pill.tsx</files>
  <read_first>
    - src/components/ui/Pill.tsx (entire file — to widen)
    - leanshot/.planning/phases/13-design-system-v2-rollout/13-PATTERNS.md (section 5, lines 177-220 — Pill analog)
    - leanshot/.planning/design-system/project/leanshot/src/components/ui/Pill.tsx (bundle prototype)
  </read_first>
  <action>
Widen `PillProps` and `PillGroup` props. ADD these optional props to `PillProps` (extend existing interface, do NOT remove `active`, `size`, `leadingIcon`):
- `count?: number | string` — renders as a right-aligned chip after children.
- `iconOnly?: boolean` — when true, switches container to `aspect-square` and tightens padding to zero; consumer is REQUIRED to pass `aria-label` (declare a TS conditional helper interface OR add a runtime invariant comment — pick the conditional type if straightforward; otherwise add a JSDoc note `@requires aria-label when iconOnly is true`). Pragmatic choice for executor: add a JSDoc and rely on jsx-a11y/aria-label-has-name to flag misuse downstream; do not block on conditional-type ceremony.

Inside the `Pill` function:
- When `iconOnly === true`: replace the size-based `h-8 px-3` / `h-10 px-4` block with `h-8 w-8 p-0` (sm) / `h-10 w-10 p-0` (md). Keep `inline-flex items-center justify-center` (drop `gap-1.5` for iconOnly to center the single child).
- Add `disabled` visual styling — extend the `cn()` arg list with `'disabled:pointer-events-none disabled:opacity-50'` (mirrors Button.tsx baseClasses).
- Render count chip after `{children}`: `{count !== undefined && (<span aria-hidden className={cn('ml-1.5 inline-flex items-center justify-center min-w-[18px] h-[18px] px-1.5 rounded-full text-[10px] font-semibold tabular-nums', active ? 'bg-white/20 text-[var(--color-primary-foreground)]' : 'bg-[var(--color-primary-soft)] text-[var(--color-primary)]')}>{count}</span>)}`.

Widen `PillGroup` to take an optional `segmented?: boolean`. When `segmented === true`, the wrapper gets:
- `inline-flex` (not `flex flex-wrap`) so it doesn't break to a second row.
- A child-selector class that collapses inner borders for the joined-pill visual: append `' [&>button]:rounded-none [&>button:first-child]:rounded-l-pill [&>button:last-child]:rounded-r-pill [&>button:not(:first-child)]:-ml-px'`.
- `role="tablist"` instead of the default `role="group"` to communicate segmented-control semantics (still keep `role="group"` as the default branch).

PRESERVE VERBATIM:
- `aria-pressed={active}` at line 21.
- `<button type="button">` element.
- Existing focus-visible ring substring at line 23.
- `active`-state class branches at lines 25-27.

Reference DS-07 in a one-line comment above the count chip JSX:
`{/* Phase 13 DS-07: count chip — decorative; count semantic is in surrounding label */}`.

After the edit: run `npx tsc -b` and `npm run lint -- src/components/ui/Pill.tsx`. Commit only this file: `git add src/components/ui/Pill.tsx && git commit -- src/components/ui/Pill.tsx -m "feat(13-02): Pill count + iconOnly + disabled + PillGroup segmented (DS-07, D-01)"`.
  </action>
  <verify>
    <automated>cd leanshot && npx tsc -b && npm run lint -- src/components/ui/Pill.tsx && grep -E "count\?:\s*number\s*\|\s*string" src/components/ui/Pill.tsx && grep -E "iconOnly\?:\s*boolean" src/components/ui/Pill.tsx && grep -E "segmented\?:\s*boolean" src/components/ui/Pill.tsx && grep -c "aria-pressed" src/components/ui/Pill.tsx</automated>
  </verify>
  <done>
    - `PillProps` gains `count`, `iconOnly`.
    - `PillGroup` props gain `segmented` and emit `role="tablist"` when true (else `role="group"`).
    - `aria-pressed={active}` preserved on `<button>`.
    - Count chip renders only when `count !== undefined`, with `aria-hidden`.
    - `tsc -b` and `eslint` exit 0.
    - Committed via pathspec.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Sidebar 72↔232 px instant snap + 200 ms inner fade (D-12 + chat1.md landmines 1 + 3 + DS-08)</name>
  <files>src/components/layout/Sidebar.tsx, src/components/layout/AppShell.tsx</files>
  <read_first>
    - src/components/layout/Sidebar.tsx (entire file — refresh in place)
    - src/components/layout/AppShell.tsx (entire file — wire collapsed state)
    - leanshot/.planning/phases/13-design-system-v2-rollout/13-PATTERNS.md (section 6, lines 224-251 — Sidebar analog)
    - leanshot/.planning/design-system/chats/chat1.md (lines 410-425, 815-855 — sidebar landmines verbatim)
  </read_first>
  <action>
(A) Sidebar.tsx surgery — two-phase shape change preserving `data-tour="nav"`, `motion.span layoutId="sb-active"`, the TABS const, and the brand button + footer cluster.

1. Extend `SidebarProps` with two new optional fields:
```
interface SidebarProps {
  onOpenAI: () => void;
  onOpenSettings: () => void;
  collapsed?: boolean;
  onToggleCollapsed?: () => void;
}
```
Default `collapsed = false` in the destructure so existing tests (if any) still type-check.

2. Replace the `<aside>`'s width class. CURRENT: `w-[80px]`. NEW: drop `w-[80px]` and add an attribute selector via Tailwind arbitrary variants OR a `cn()` ternary picking `w-[72px]` (collapsed) vs `w-[232px]` (expanded). Use a `cn()` ternary — it's the project's idiom and avoids `data-[...]` Tailwind arbitrary-variant compile-time risk in v4 beta. Also emit `data-sidebar={collapsed ? 'collapsed' : 'expanded'}` on the `<aside>` so downstream tests / Playwright can drive state from a stable attribute.

CRITICAL — bake this comment ABOVE the width class, verbatim:
```
{/* DO NOT use `transition: width` here — see .planning/design-system/chats/chat1.md
    landmine 1: var() widths cannot be interpolated without @property registration,
    and even discrete Tailwind classes should NOT carry a width transition (DS-08
    mandates an INSTANT snap; inner content gets the 200ms fade instead). */}
```

3. Drop `flex-col items-center` from the `<aside>`'s base class string in the EXPANDED state and replace with a column layout that lets labels sit beside icons. In COLLAPSED state, keep the existing centered icon-only layout. Use a `cn()` ternary on the `<aside>`'s `className` to switch between:
- Collapsed: `'hidden md:flex fixed top-0 left-0 bottom-0 z-30 w-[72px] flex-col items-center py-5 bg-[var(--color-surface)] border-r border-[var(--color-border)] safe-top'`
- Expanded: `'hidden md:flex fixed top-0 left-0 bottom-0 z-30 w-[232px] flex-col items-stretch py-5 px-3 bg-[var(--color-surface)] border-r border-[var(--color-border)] safe-top'`

4. Brand button (lines 50-68): when expanded, render a wordmark beside the bolt SVG. Pattern: wrap the SVG and a `<span>` in a row container; the `<span>` gets `transition-opacity duration-200 ease-out` + opacity-0 on collapsed, opacity-100 on expanded. Wordmark text: `LeanShot` styled `text-[15px] font-semibold tracking-tight text-[var(--color-text)]`. Make the wordmark `aria-hidden` so the existing `aria-label="LeanShot home"` on the button isn't doubled by screen readers.

5. Tab buttons (the `.map` at lines 74-102): when expanded, each tab should show its label beside the icon. The icon stays in its own `relative z-10` slot; the label is a sibling `<span>` after the icon with `transition-opacity duration-200 ease-out` and class `cn('ml-3 text-[13px] font-medium relative z-10', collapsed && 'opacity-0 pointer-events-none')`. The existing hover-tooltip span (line 97-99 — the floating right-side tooltip) should render ONLY when `collapsed === true` (in expanded state, the inline label provides the affordance). Wrap that tooltip span in `{collapsed && (...)}`.

6. Active-state shape: when expanded, the tab buttons widen to `h-12 w-full justify-start px-3` (vs. collapsed `size-12`); the `motion.span layoutId="sb-active"` still works because it uses layoutId (framer animates between the two shapes — that's its job). Preserve the motion.span and its transition props verbatim.

7. Footer cluster (lines 105-139): same row pattern — labels appear next to AI / theme / settings buttons when expanded; account-initial avatar grows from `size-10` to a row with `size-10` avatar + name `<span>` (use `userInitial`'s parent name if available via store, else show literal `'Profile'`). All inner content text gets `transition-opacity duration-200 ease-out`.

8. Add an `onToggleCollapsed` trigger. Render a small chevron button at the top-right of the expanded sidebar (and a chevron-right at the bottom of the collapsed sidebar) that calls `onToggleCollapsed?.()` when clicked. Class: same focus-ring tokenization as the other sidebar buttons. Icon: import `ChevronsLeft` (collapse) / `ChevronsRight` (expand) from `lucide-react`. aria-label: `'Collapse navigation'` / `'Expand navigation'`.

9. Reduced-motion: respect `useReducedMotion()` — if `reduced === true`, drop the `transition-opacity duration-200 ease-out` classes from inner content (use `cn()` ternary). Import is `import { useReducedMotion } from '@/hooks/useReducedMotion';`. NO `transition: width` is ever applied regardless of motion preference (instant snap is THE deliberate behavior, not a motion concession).

(B) AppShell.tsx wiring:

1. Add a sibling `useState`:
```
const [sidebarCollapsed, setSidebarCollapsed] = useState(false);
```

2. Pass `collapsed` and `onToggleCollapsed` to `<Sidebar>`:
```
<Sidebar
  onOpenAI={onOpenAI}
  onOpenSettings={onOpenSettings}
  collapsed={sidebarCollapsed}
  onToggleCollapsed={() => setSidebarCollapsed((v) => !v)}
/>
```

3. Update the `<main>`'s left-margin class. CURRENT: `md:ml-[80px]`. NEW: cn() ternary `md:ml-[72px]` (collapsed) vs `md:ml-[232px]` (expanded). Full new className:
`cn('pt-5 md:pt-7 pb-[140px] md:pb-12 px-4 md:px-7 max-w-[1280px] mx-auto', sidebarCollapsed ? 'md:ml-[72px]' : 'md:ml-[232px]')`.

DO NOT touch:
- `MobileNav`, `WorkspaceSwitcher`, `EmailVerificationBanner`, `LegalFooter`, `Toast`, `QuickLogSheet`, the mobile FAB.
- `AppShellProps` public API — `collapsed` is internal-only.
- The outer `<div>`'s `min-h-screen bg-[var(--color-bg)] text-[var(--color-text)]` — DO NOT add `display: flex` here (chat1.md landmine 3).

(C) Reference D-12 + chat1.md landmines 1 and 3 in a header comment block at the top of Sidebar.tsx (above the `import` block):
```
/*
 * Phase 13 DS-08 / D-12: 72 ↔ 232 px instant snap.
 *
 * INVARIANTS — DO NOT REGRESS:
 *   1. NO `transition` on the width property. var() interpolation requires
 *      @property registration (chat1.md landmine 1); even discrete Tailwind
 *      widths get a hard snap by design.
 *   2. Sidebar stays `position: fixed`. AppShell stays a plain block container
 *      — NO `display:flex` on .app-shell (chat1.md landmine 3).
 *   3. Inner content (wordmark, tab labels, account row) gets a 200ms
 *      opacity fade — gated by useReducedMotion().
 *   4. `data-tour="nav"` MUST remain on the <aside> (GuidedTour reads it).
 */
```

After the edits: run `npx tsc -b`, `npm run lint -- src/components/layout/Sidebar.tsx src/components/layout/AppShell.tsx`, and confirm `grep -n "transition: width" src/components/layout/Sidebar.tsx` returns nothing AND `grep -n "transition-\[width" src/components/layout/Sidebar.tsx` returns nothing.

Commit the two files together via pathspec (they form one logical change):
`git add src/components/layout/Sidebar.tsx src/components/layout/AppShell.tsx && git commit -- src/components/layout/Sidebar.tsx src/components/layout/AppShell.tsx -m "feat(13-02): Sidebar instant 72↔232px snap + 200ms inner fade (DS-08, D-12, chat1 landmines 1+3)"`.
  </action>
  <verify>
    <automated>cd leanshot && npx tsc -b && npm run lint -- src/components/layout/Sidebar.tsx src/components/layout/AppShell.tsx && ! grep -E "transition:\s*width|transition-\[width" src/components/layout/Sidebar.tsx && grep -E "data-sidebar" src/components/layout/Sidebar.tsx && grep -E "w-\[72px\]" src/components/layout/Sidebar.tsx && grep -E "w-\[232px\]" src/components/layout/Sidebar.tsx && grep -E "md:ml-\[232px\]" src/components/layout/AppShell.tsx && grep -E "data-tour=\"nav\"" src/components/layout/Sidebar.tsx && grep -E "transition-opacity duration-200" src/components/layout/Sidebar.tsx && ! grep -E "className=\"[^\"]*flex[^\"]*\"" src/components/layout/AppShell.tsx | grep -v "Sidebar\|FAB\|hidden md:flex\|inline-flex\|flex flex-col"</automated>
  </verify>
  <done>
    - Sidebar renders 72 px wide when `collapsed=true`, 232 px when `collapsed=false`.
    - NO `transition: width` or `transition-[width` anywhere in Sidebar.tsx.
    - `data-sidebar` attribute is emitted on the `<aside>`.
    - `data-tour="nav"` still on the `<aside>` (GuidedTour-safe).
    - Inner content (wordmark `<span>`, tab labels, account name, footer chevron) carries `transition-opacity duration-200 ease-out`, gated by `useReducedMotion()`.
    - AppShell's `<main>` margin class switches between `md:ml-[72px]` and `md:ml-[232px]`.
    - AppShell's outer `<div>` is still a plain block (no `display:flex` added).
    - `motion.span layoutId="sb-active"` retained.
    - `tsc -b` exits 0, `eslint` exits 0.
    - Committed via pathspec.
  </done>
</task>

</tasks>

<verification>
After all 4 tasks land:

1. `cd leanshot && npx tsc -b` — exits 0.
2. `cd leanshot && npm run lint` — exits 0.
3. `cd leanshot && npm test -- --run` — every existing unit test still passes (no consumer call-site changes; we're only widening unions and adding additive props).
4. `cd leanshot && npm run build` — production build succeeds; bundle-size CI gate in 12-01 stays green (index ceiling 50 kB gz; this plan adds class strings only, no new dependencies).
5. Manual smoke (executor or 13-06 will catch via VR suite — no checkpoint here): start dev server, click the new sidebar chevron — width snaps instantly, labels fade in/out over ~200 ms, main content's left margin tracks the new width.
6. `grep -rE "transition:\s*width|transition-\[width" src/components/layout/Sidebar.tsx` returns zero matches.
7. `grep -c "aria-busy\|aria-pressed\|aria-label" src/components/ui/Button.tsx src/components/ui/Pill.tsx` returns ≥ 1 per file (a11y patterns preserved).
8. No file outside `files_modified` was touched (git status confirms).
</verification>

<success_criteria>
- DS-05 satisfied: Card has 4 additive variants on top of the existing 5. Total 9.
- DS-06 satisfied: Button has `tonal` variant + `count` chip slot; aria-busy / loading / disabled wiring intact; focus ring tokenization unchanged.
- DS-07 satisfied: Pill has count chip + iconOnly + disabled styling; PillGroup has segmented opt-in (role="tablist").
- DS-08 satisfied: Sidebar collapses 72↔232 px instantly (NO width transition, comment baked in source); inner content fades 200 ms; reduced-motion drops the fade; data-tour="nav" preserved; AppShell stays a plain block (chat1.md landmine 3 not regressed).
- D-15 (SC #3) satisfied: no consumer call-site changes; existing variants' class strings byte-stable; focus-ring tokenization identical across all primitives.
- Plan committed as 4 commits via `git commit -- <pathspec>` (per memory `feedback_parallel_executor_git_isolation.md`) so 13-03 and 13-05 can land in the same Wave 2 batch without index contamination.
</success_criteria>

<output>
After completion, create `leanshot/.planning/phases/13-design-system-v2-rollout/13-02-SUMMARY.md` covering:
- Files modified and the variant unions before/after.
- Confirmation that `data-tour="nav"` and `motion.span layoutId="sb-active"` are preserved.
- Confirmation that `transition: width` is NOT present anywhere in `Sidebar.tsx` (chat1.md landmine 1 audit).
- Bundle delta (gz) for the index chunk and any consumer-route chunks vs. pre-13-02 baseline.
- Note any AppShell layout reasoning (e.g. why `display:flex` was NOT added — chat1.md landmine 3).
- Hand-off note for 13-04 (login restyle) and 13-06 (VR suite): Sidebar attribute is `data-sidebar="collapsed"|"expanded"` — Playwright VR specs can drive both states deterministically; new Pill `segmented` boolean is what 13-04 will use for the Sign in / Sign up tabs.
</output>
