---
phase: 67-operational-runbooks-observability
plan: 5
wave: 2
depends_on: [67-01, 67-02, 67-03, 67-04]
files_modified:
  - leanshot/.planning/phases/67-operational-runbooks-observability/67-SUMMARY.md
  - leanshot/.planning/phases/67-operational-runbooks-observability/67-VERIFICATION.md
  - leanshot/.planning/phases/67-operational-runbooks-observability/67-CARRY-OVER.md
  - leanshot/.planning/ROADMAP.md
  - leanshot/.planning/STATE.md
  - leanshot/.planning/REQUIREMENTS.md
autonomous: false
requirements: [OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06, OPS-07, OPS-08, OPS-09, OPS-10]
---

<objective>
Phase 67 close-out — planning artifacts inline; remote deploys + operator-run scripts (k6, cold-start audit, posthog seed, bs-status-poller deploy + cron) deferred to Phase 70 UAT.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: SUMMARY + VERIFICATION + CARRY-OVER + ROADMAP/STATE/REQUIREMENTS flips</name>
  <action>
SUMMARY: 4 runbooks + 3 k6 scripts + vercel.json + middleware.ts + sentry CI workflow + 2 CI scripts + 2 operator scripts + 1 Edge Fn = 10 OPS REQs covered.

VERIFICATION (status: human_needed):
- Automated: file existence + tsc/deno test green for the new Fn + CI workflow valid YAML
- HUMAN signals (Phase 70):
  - S1: Run k6 baseline against staging; results captured in load-test-baseline.md
  - S2: Run cold-start audit against deployed Fns; results captured in edge-fn-cold-starts.md
  - S3: Run posthog/seed-funnel-alerts.sh against prod PostHog; 3 alerts visible in PostHog UI
  - S4: Deploy bs-status-poller Fn + register 5min pg_cron job
  - S5: Verify SENTRY_DSN CI guard passes on next PR
  - S6: Operator reviews + accepts each runbook
  - S7: Execute backup-restore drill (HIPAA §164.308(a)(7) contingency testing)

CARRY-OVER:
- Vercel rate-limit Edge Middleware works only on /api/ Serverless Fns; Vite static routes have NO rate-limit. For full coverage, upgrade to Vercel Firewall (paid) OR add per-Fn check in each public Edge Fn.
- Upstash Redis token-bucket replacement for in-memory middleware (cross-instance correctness) — defer to v1.5.
- Edge-Fn cold-start refactor (if outliers found) — defer to v1.5.
- PostHog feature-flag-based gradual rollout of rate-limits — defer to v1.5.

ROADMAP: flip Phase 67 → [x]. Append CODE-COMPLETE.
STATE: Status → Phase 67 CODE-COMPLETE. Advancing to Phase 68.
REQUIREMENTS: OPS-01..10 → Complete (code).
  </action>
</task>

</tasks>
