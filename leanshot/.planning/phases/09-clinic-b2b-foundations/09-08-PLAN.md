---
phase: 09-clinic-b2b-foundations
plan: 08
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/components/layout/WorkspaceSwitcher.tsx
  - src/components/layout/WorkspaceSwitcher.test.tsx
  - src/components/clinic/ClinicContextBar.tsx
  - src/components/layout/AppShell.tsx
autonomous: true
requirements: [CLINIC-01, CLINIC-02]
mvp_slice: "Single-identity affordance slice — WorkspaceSwitcher lives in the index chunk (visible on every authenticated route, NOT only on clinic routes) and renders 3 groups (Personal account always top + Memberships + Workspaces I run). Pitfall #8 invariant: even users with zero memberships and zero workspaces see the Personal account row + the two empty-hint groups. User-visible outcome: a patient with 1 membership in clinic 'Acme' AND 1 personal workspace 'My Studio' opens the switcher and sees 3 groups + can switch contexts; route changes when a row is clicked. Index chunk delta ≤ 3 kB gz (Phase 6 baseline 21.49 kB → Phase 9 ceiling 24.5 kB / 79 kB raw)."

must_haves:
  truths:
    - "WorkspaceSwitcher mounts in `src/components/layout/AppShell.tsx` between sidebar and topbar on desktop; renders inside a `<Sheet>` on mobile"
    - "Switcher is visible on EVERY authenticated route (patient dashboard /, clinic /clinic/{slug}, settings /settings, Active orgs tab, etc.) — but NOT on anonymous routes (/clinic-invite/, marketing, auth pages). Hidden during loading/SIGNED_OUT states"
    - "Switcher trigger: `<button aria-label='Switch workspace. Currently in {context name}.' aria-haspopup='listbox' aria-expanded={open}>` per UI-SPEC line 683"
    - "Dropdown renders 3 groups in fixed order: Personal account → Memberships → Workspaces I run"
    - "Group 1: 'Personal account' always renders with the single 'Your LeanShot' row (subtitle 'Your tracking data') — Pitfall #8 single-identity affordance MUST render even when other groups are empty"
    - "Group 2: 'Memberships' (orgs this user is a patient in — joins memberships → orgs); empty hint 'When clinics or coaches invite you, they'll appear here.'"
    - "Group 3: 'Workspaces I run' (orgs this user owns OR is an operator in — joins memberships → orgs where role has org.update OR org.delete); empty hint 'Create a workspace to start managing patients.'"
    - "Each row: avatar (org logo or monogram) + label + subtitle (role badge for groups 2/3) + 2px teal-700 left-edge stripe when currently-active + aria-current='true' on active row"
    - "Click a row → routes via `window.history.pushState` to: `/` (personal) OR `/clinic/{slug}` (workspaces I run) OR for memberships → currently routes to `/?org={slug}` placeholder OR opens an Active organizations tab deep-link (Phase 10 finalizes membership-context UX; Phase 9 routes to `/settings/organizations` so the patient can see the membership row)"
    - "Footer action 'Create a new workspace' → routes to OrgCreateFlow (use the existing path — likely a query-string trigger like `/?create-workspace=1` that ClinicWorkspace or App.tsx detects)"
    - "Keyboard: arrow keys navigate rows within a group; tab moves between groups; enter activates; escape closes. Focus returns to trigger button on close"
    - "Pitfall #9 mitigation: defer-mount the switcher until `supabase.auth.getSession()` resolves with a non-null user. Show a Skeleton in the trigger position during the auth-resolving window. Reduces empty-list flash on hard reload"
    - "W-7 explicit bundle ceiling (plan-checker iter 1): post-build assertion enforces `index-<hash>.js.gz ≤ 25088 bytes` (24.5 kB) OR `index-<hash>.js ≤ 80896 bytes` (79 kB raw) — whichever file the bundle produces. Phase 6 close was 21.49 kB gz; Phase 9 +3 kB delta budget for WorkspaceSwitcher = 24.5 kB ceiling. Absolute cap 50 kB gz preserved per Plan 09-01 guard config."
    - "ClinicContextBar (Plan 09-02) replaces its WorkspaceSwitcher placeholder with the real component"
    - "RTL tests cover all State Coverage Checklist rows from UI-SPEC §'Workspace switcher' lines 705-716"
  artifacts:
    - path: "src/components/layout/WorkspaceSwitcher.tsx"
      provides: "Switcher trigger + 3-group dropdown (or Sheet on mobile) + keyboard nav"
      min_lines: 250
    - path: "src/components/layout/AppShell.tsx"
      provides: "Mounts WorkspaceSwitcher in topbar/sidebar position"
      contains: "WorkspaceSwitcher"
    - path: "src/components/clinic/ClinicContextBar.tsx"
      provides: "Plan 09-02 ClinicContextBar — replace switcher-placeholder with real WorkspaceSwitcher import"
      contains: "import.*WorkspaceSwitcher"
  key_links:
    - from: "src/components/layout/WorkspaceSwitcher.tsx"
      to: "supabase.from('memberships') + supabase.from('orgs')"
      via: "joined query for 3-group population"
      pattern: "from\\('memberships'\\)"
    - from: "src/components/layout/AppShell.tsx"
      to: "WorkspaceSwitcher"
      via: "topbar mount on authenticated routes"
      pattern: "<WorkspaceSwitcher"
    - from: "src/components/clinic/ClinicContextBar.tsx"
      to: "WorkspaceSwitcher"
      via: "context-bar mounts the shared switcher"
      pattern: "<WorkspaceSwitcher"
---

<objective>
Ship the WorkspaceSwitcher — the single-identity affordance making Pitfall #8 visible to the user. The switcher lives in the index chunk because it appears on every authenticated route (patient dashboard, clinic workspace, settings, active orgs). Total index chunk delta ≤ 3 kB; Phase 6 close was 21.49 kB; Phase 9 ceiling 24.5 kB gz / 79 kB raw (W-7 explicit assertion in verify).

Purpose: D-09 (clinic-context bar on /clinic/{slug}/* shows org name + switcher dropdown) + D-14 (single switcher grouped by relationship). Pitfall #8's "one auth.users per email + relationship tables for context" invariant is MEANINGFUL TO THE USER only because the switcher renders 3 grouped contexts. A patient with a personal account AND a clinic membership AND an operator workspace sees ALL THREE — proving the single-identity model.

Output: WorkspaceSwitcher renders on every authenticated route; ≤3 kB delta to index chunk; covers all 10 State Coverage Checklist rows from UI-SPEC; ClinicContextBar uses the real component (replacing Plan 09-02 stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-clinic-b2b-foundations/09-CONTEXT.md D-09 + D-14
@.planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md
@.planning/phases/09-clinic-b2b-foundations/09-02-PLAN.md
@src/components/layout/AppShell.tsx
@src/components/layout/Topbar.tsx
@src/components/layout/Sidebar.tsx
@src/components/ui/Sheet.tsx
@src/components/clinic/ClinicContextBar.tsx
@src/lib/supabase.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WorkspaceSwitcher (index chunk) + AppShell mount + ClinicContextBar real-import</name>
  <read_first>
    - .planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md §"Operator-side: Clinic-context bar + Workspace switcher" lines 157-178 + §"Workspace switcher interaction" lines 620-630 + §"State Coverage Checklist" lines 705-716
    - .planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md §"Common Pitfalls" Pitfall #9 (defer-mount until auth resolves)
    - src/components/layout/AppShell.tsx (existing layout structure — desktop sidebar + mobile bottom-nav)
    - src/components/layout/Topbar.tsx (existing top-bar; switcher mounts here on desktop)
    - src/components/ui/Sheet.tsx (mobile bottom-sheet pattern — switcher uses Sheet on mobile)
    - src/components/clinic/ClinicContextBar.tsx (Plan 09-02 placeholder)
    - src/types/clinic.ts (Membership + Org types)
    - src/lib/clinic-permissions.ts (useHasPermission — to determine "Workspaces I run" group membership: orgs where the user has org.update OR org.delete)
  </read_first>
  <behavior>
    - Test 1 (defer-mount Pitfall #9): on hard refresh, supabase.auth.getSession() returns a Promise → switcher renders a Skeleton in the trigger position until resolved; after session resolves with a user, switcher mounts; after session resolves with null, switcher hides entirely.
    - Test 2 (Personal-only state): user with 0 memberships → switcher trigger renders 'Your LeanShot' label; dropdown shows 3 groups; Memberships + Workspaces I run show empty hints.
    - Test 3 (Personal + 1 workspace): user owns 1 org → trigger shows current context label; dropdown Group 3 shows 1 row.
    - Test 4 (Personal + 1 membership + 1 workspace): 3 rows + 2 empty hints not shown anymore.
    - Test 5 (currently in personal): personal row has 2px teal-700 left-edge stripe + aria-current='true'.
    - Test 6 (currently in workspace): workspace row has the stripe + aria-current; personal row has no stripe.
    - Test 7 (membership row click): routes to `/settings/organizations` (Phase 10 will refine).
    - Test 8 (workspace row click): routes to `/clinic/{slug}`.
    - Test 9 (personal row click): routes to `/`.
    - Test 10 (Create-new-workspace footer click): routes to OrgCreateFlow (via `/?create-workspace=1` query — implementer picks the trigger).
    - Test 11 (keyboard arrow + tab + enter + escape): full keyboard nav with focus returning to trigger.
    - Test 12 (mobile Sheet variant): viewport ≤768px → trigger opens `<Sheet>` instead of dropdown.
    - Test 13 (dark mode + reduced-motion snapshots).
    - Test 14 (ClinicContextBar): renders the real `<WorkspaceSwitcher>` (no more placeholder no-op button).
    - Test 15 (anonymous routes): /clinic-invite/{token} and /auth/* do NOT mount WorkspaceSwitcher.
    - Test 16 (Realtime cross-context update): when a new membership is INSERTed or revoked, the switcher list updates via subscribeToUserChannel from Plan 09-02.
    - Test 17 (single-identity invariant copy): even with zero memberships AND zero workspaces, Personal account group renders + both empty hints render.
    - Test 18 (W-7 bundle ceiling — post-build CI gate): the inline guard script (see <verify>) asserts the built `index-<hash>.js.gz` is ≤ 25088 bytes (24.5 kB) OR raw `index-<hash>.js` ≤ 80896 bytes (79 kB). If exceeded, CI fails with an explicit message naming Phase 6 baseline + Phase 9 delta budget.
  </behavior>
  <action>
    Author `src/components/layout/WorkspaceSwitcher.tsx`:

    **Component structure:**
    - State: `{open, contexts, activeContextId, isLoading}`.
    - On mount: defer until `supabase.auth.getSession()` resolves. If session is null, render nothing (anonymous route). If user exists, fetch contexts:
      ```typescript
      const { data: memberships } = await supabase
        .from('memberships')
        .select('id, role_id, org_id, orgs(id, slug, name, logo_storage_path, owner_user_id), roles(name)')
        .eq('user_id', user.id)
        .is('revoked_at', null);
      ```
    - Partition memberships into Group 2 (Memberships — patient role) vs Group 3 (Workspaces I run — operator role). Heuristic: Group 3 if `roles.name in ('Owner','Coach') OR orgs.owner_user_id = user.id`; otherwise Group 2.
      - This duplicates server logic. To avoid drift, future work can surface a `is_operator boolean` on memberships OR a SECURITY DEFINER `list_user_contexts()` RPC; Phase 9 ships with the client-side heuristic + document the gap in SUMMARY.md.
    - Active context detected from `window.location.pathname`:
      - `/clinic/{slug}/*` → activeContextId = `org:{slug}`
      - anything else → activeContextId = `personal`
    - On click trigger: `setOpen(true)` (desktop dropdown) or open Sheet (mobile via `useReducedMotion()` + `matchMedia('(max-width: 768px)')`).

    **Rendering:**
    Use `<button>` for trigger with the full aria-label per UI-SPEC line 683. Dropdown uses `role="listbox"` with `role="group"` children for the 3 groups (UI-SPEC line 684); rows are `role="option"` with `aria-selected={isActive}`.

    Active context has `aria-current="true"` + 2px teal-700 left-edge stripe via `border-l-2 border-teal-700` Tailwind classes.

    Empty hints render when groups have zero rows; use UI-SPEC copy verbatim lines 167-171.

    Footer action: `<button>` with text `Create a new workspace` → on click: `setOpen(false)` + `window.location.assign('/?create-workspace=1')` OR pushState event the OrgCreateFlow listens for.

    Keyboard nav:
    - ArrowDown/ArrowUp: cycle rows within the open group; cross-group navigation via Tab.
    - Enter on focused row: activate (route).
    - Escape: close + focus returns to trigger.

    **Realtime:** subscribe to `user:{user_id}` channel via Plan 09-02's `subscribeToUserChannel`. On any membership INSERT/UPDATE/DELETE payload: refetch contexts. This keeps the switcher in sync with operator-side revokes + new invite accepts.

    **Pitfall #9 invariant defense:** during the auth-resolving window (Promise pending), render a Skeleton box where the trigger goes — DO NOT render an empty trigger that flashes then re-renders. Use the existing `<Skeleton>` primitive.

    **Anonymous-route guard:** mount uses `useEffect` checking `window.location.pathname.startsWith('/clinic-invite/')` OR pathname matches an auth route → early return null. Belt-and-suspenders: AppShell only mounts WorkspaceSwitcher on authenticated routes (App.tsx selectView returns 'dashboard'/'clinic'/'clinic-settings'/'legal'/'share').

    **Bundle hygiene (W-7 explicit per plan-checker iter 1):** the switcher MUST live in the index chunk (NOT lazy-loaded). Verify by inspecting `npm run build` output: `dist/assets/index-*.js` includes WorkspaceSwitcher source. Total index delta ≤ 3 kB gz — Phase 6 baseline was 21.49 kB → Phase 9 ceiling 24.5 kB. The inline guard in `<verify>` enforces this at the byte level by reading the build output and comparing against 25088 bytes (gz) or 80896 bytes (raw, ≈79 kB). If bundle exceeds, fall back to the `sync-defer.ts` idle-deferred-init wrapper from Phase 6 Plan 06-01 (per UI-SPEC line 592).

    **Edit `src/components/layout/AppShell.tsx`:** mount `<WorkspaceSwitcher />` in the topbar position (between left navigation and right actions). Read current AppShell structure first; the natural mount point is inside `<Topbar>` or as a sibling on desktop, and inside a top-of-screen affordance on mobile.

    **Edit `src/components/clinic/ClinicContextBar.tsx`:** import `<WorkspaceSwitcher>` and replace the Plan 09-02 placeholder button. The context-bar still owns the org logo + org name display; the switcher trigger becomes the dropdown-opening element.

    Co-locate RTL test `src/components/layout/WorkspaceSwitcher.test.tsx` per Test 1-17.

    **No `s.user!` non-null assertions.**

    Commit with pathspec `git commit -- src/components/layout/WorkspaceSwitcher.tsx src/components/layout/WorkspaceSwitcher.test.tsx src/components/layout/AppShell.tsx src/components/clinic/ClinicContextBar.tsx`.
  </action>
  <verify>
    <automated>npm run test -- --run src/components/layout/WorkspaceSwitcher.test.tsx</automated>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npm run build && node -e "const fs=require('fs'); const idx=fs.readdirSync('dist/assets').find(f=>f.startsWith('index-')&&f.endsWith('.js.gz')) || fs.readdirSync('dist/assets').find(f=>f.startsWith('index-')&&f.endsWith('.js')); const sz=fs.statSync('dist/assets/'+idx).size; const limit = idx.endsWith('.gz')?25088:80896; if(sz>limit){console.error('W-7 ceiling exceeded: index '+sz+' > '+limit+' ('+idx+'); Phase 6 baseline 21.49 kB gz; Phase 9 delta budget +3 kB = 24.5 kB ceiling'); process.exit(1)} else { console.log('W-7 OK: index '+sz+' ≤ '+limit+' ('+idx+')'); }"</automated>
    <automated>node scripts/bundle-size-check.cjs</automated>
  </verify>
  <done>
    - WorkspaceSwitcher renders + 18 RTL tests pass (including W-7 explicit byte-level bundle gate).
    - Index chunk delta ≤ 3 kB gz (verified by inline guard script: 24.5 kB gz / 79 kB raw ceiling).
    - AppShell mounts it on authenticated routes.
    - ClinicContextBar uses the real component.
    - Pitfall #9 defer-mount works (no empty flash on reload).
    - Pitfall #8 single-identity invariant visible: 3 groups always render + empty hints when appropriate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user browser → supabase.from('memberships') | RLS `memberships_select_own` (Plan 09-01) gates to user's own rows |
| switcher → window.history.pushState | Client-side routing; selectView in App.tsx is the security boundary for which view renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-45 | Information disclosure | switcher renders rows even on anonymous routes (logged-out leak) | mitigate | Anonymous-route guard + defer-mount until session resolves; AppShell only mounts on authenticated routes |
| T-09-46 | Tampering | client-side group-partition heuristic mismatches server permission state | accept | Heuristic is UX hint only; clicking any row routes through path-based App.tsx selectView which gates against actual signed-in state; security is unaffected if heuristic is briefly stale |
| T-09-47 | Spoofing | URL active-context detection mismatches actual current org membership | mitigate | If user navigates to `/clinic/{slug}` for an org they're not a member of, ClinicWorkspace (Plan 09-02) fetches the org row → RLS denies → route back to `/`. Switcher's "active" indicator is informational only. |
</threat_model>

<verification>
- 18 RTL tests pass.
- `npm run build` succeeds; index chunk ≤ 24.5 kB gz / 79 kB raw (W-7 inline guard).
- Manual smoke: user with 1 membership + 1 workspace sees both groups + active stripe + can switch.
- Anonymous route `/clinic-invite/abc123` does NOT render the switcher (verified via DOM check).
- No s.user! introductions.
</verification>

<success_criteria>
1. WorkspaceSwitcher renders on every authenticated route.
2. 3 groups (Personal account, Memberships, Workspaces I run) with empty hints.
3. Pitfall #8 single-identity affordance always visible (Test 17).
4. Pitfall #9 defer-mount works.
5. Index chunk delta ≤ 3 kB / 24.5 kB gz total ceiling (W-7 byte-level assertion).
6. ClinicContextBar uses real component.
</success_criteria>

<output>
After completion, create `.planning/phases/09-clinic-b2b-foundations/09-08-SUMMARY.md` documenting bundle-size delta + actual measurement + any client-side partition heuristic limitations.
</output>
