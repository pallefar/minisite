---
phase: 09-clinic-b2b-foundations
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/clinic/ClinicWorkspace.tsx
  - src/components/clinic/ClinicWorkspace.test.tsx
  - src/components/clinic/ClinicContextBar.tsx
  - src/components/clinic/OrgCreateFlow.tsx
  - src/components/clinic/OrgCreateFlow.test.tsx
  - src/components/clinic/InvitePatientModal.tsx
  - src/components/clinic/InvitePatientModal.test.tsx
  - src/lib/clinic.ts
  - src/lib/clinic-realtime.ts
autonomous: true
requirements: [CLINIC-01, CLINIC-02]
mvp_slice: "Operator-facing slice — operator completes OrgCreateFlow, lands at /clinic/{slug}, sees the empty roster shell, opens InvitePatientModal, fires send_invite. User-visible outcome: a real operator can go from signup → org-create → workspace home → invite-modal post-send confirmation entirely in the UI (Edge Function dispatch hooks up in Wave 3 Plan 09-06; Plan 02 stubs the call to a typed client wrapper that returns `{ ok: true, invite_id: '<uuid>' }` until 09-06 wires it for real). **B-2 fix per plan-checker iter 1:** Plan 09-01 already created the `ClinicWorkspace.tsx` stub + App.tsx routing — this plan OVERWRITES the stub file and does NOT touch App.tsx."

must_haves:
  truths:
    - "Plan 09-01 already created `src/components/clinic/ClinicWorkspace.tsx` as a Phase 9 stub + App.tsx lazy import + selectView branches; this plan OVERWRITES the stub with the real implementation. NO App.tsx edits in this plan."
    - "Operator visiting `/clinic/{slug}` while signed in mounts the lazy `clinic` chunk and renders ClinicWorkspace"
    - "ClinicWorkspace renders empty-roster shell with copy from UI-SPEC lines 181-189 verbatim + 'Invite patient' primary CTA"
    - "ClinicContextBar renders the operator's current org name + logo + WorkspaceSwitcher trigger placeholder (the switcher itself ships in Plan 09-08; until then, the trigger renders a no-op button with the correct aria-label)"
    - "OrgCreateFlow renders Step 1 with workspace-name Input auto-focused; slug auto-derives from name on keystroke (lowercased, kebab-cased, special chars stripped); slug becomes independently editable on first slug-input change (200ms debounced detection)"
    - "Slug uniqueness check fires on blur via `clinic.checkSlugAvailable(slug)` typed wrapper (Plan 09-06 wires to Edge Function; Plan 02 stubs to a Postgres RPC `check_slug_available` OR uses `create_org` exception-as-validation pattern — planner picks the lower-risk path)"
    - "Slug invalid/reserved/taken inline errors render with destructive red copy verbatim from UI-SPEC lines 140-142"
    - "Logo upload accepts PNG/JPEG ≤2 MB; preview shows 64×64 square crop; rejection toasts use copy from UI-SPEC lines 148-149"
    - "Successful org-create transitions to success state (Fraunces italic heading 'Workspace created') + dual CTAs ('Invite first patient' / 'Go to workspace')"
    - "'Invite first patient' transition routes to `/clinic/{slug}` AND opens InvitePatientModal in the same transition (no empty-state flash)"
    - "InvitePatientModal renders email Input + 10 scope-preselect checkboxes (using DATA_TYPE_LABELS from src/types/clinic.ts) all defaulted-on"
    - "Submit transitions modal to anti-enumeration post-send state with universal 'Invitation sent' heading + copy from UI-SPEC lines 207-211 — D-02 invariant"
    - "`sendInvite` wrapper return shape (W-1 fix per plan-checker iter 1): `{ ok: true, invite_id: string }` on success regardless of whether email exists in auth.users — invite_id is freshly generated server-side, no enumeration leak. Operator UI uses invite_id for pending-invites list."
    - "`src/lib/clinic.ts` exports typed wrappers: `createOrg`, `updateOrg`, `sendInvite`, `cancelInvite`, `acceptInviteExisting`, `acceptInviteNew`, `rejectInvite`, `revokeMembership`, `updateConsentScope`, `updateMemberRole`, `createRole`, `updateRole`, `deleteRole` — each returns a discriminated-union `{ok:true, data:T} | {ok:false, error:string}` shape"
    - "`src/lib/clinic-realtime.ts` exports `subscribeToOrgChannel(orgId, onChange)` and `subscribeToUserChannel(userId, onChange)` — both call `supabase.realtime.setAuth()` before `.subscribe()` (Pitfall #2 mitigation)"
    - "Clinic chunk bundle gz ≤ 12 kB (CI guard from Plan 09-01)"
    - "RTL tests for ClinicWorkspace + OrgCreateFlow + InvitePatientModal cover the State Coverage Checklist rows from UI-SPEC lines 705-754 (empty-state + loading + error + success + reduced-motion + dark mode all snapshot-tested)"
  artifacts:
    - path: "src/components/clinic/ClinicWorkspace.tsx"
      provides: "Operator workspace home component — OVERWRITES Plan 09-01 stub"
      min_lines: 80
    - path: "src/components/clinic/ClinicContextBar.tsx"
      provides: "Top bar on every /clinic/{slug}/* route — org name + logo + WorkspaceSwitcher trigger placeholder + settings link"
      min_lines: 50
    - path: "src/components/clinic/OrgCreateFlow.tsx"
      provides: "Org-create wizard (Step 1 + success state)"
      min_lines: 200
    - path: "src/components/clinic/InvitePatientModal.tsx"
      provides: "Invite modal with email + 10 scope-preselect checkboxes + D-02 anti-enumeration post-send state"
      min_lines: 120
    - path: "src/lib/clinic.ts"
      provides: "Typed RPC wrappers — discriminated-union return shape"
      exports: ["createOrg", "updateOrg", "sendInvite", "cancelInvite", "acceptInviteExisting", "acceptInviteNew", "rejectInvite", "revokeMembership", "updateConsentScope", "updateMemberRole", "createRole", "updateRole", "deleteRole", "checkSlugAvailable", "uploadOrgLogo"]
    - path: "src/lib/clinic-realtime.ts"
      provides: "Realtime subscribe helpers — setAuth-before-subscribe pattern (Pitfall #2 mitigation)"
      exports: ["subscribeToOrgChannel", "subscribeToUserChannel"]
  key_links:
    - from: "src/components/clinic/ClinicWorkspace.tsx"
      to: "src/lib/clinic.ts"
      via: "typed RPC wrapper imports"
      pattern: "from '@/lib/clinic'"
    - from: "src/components/clinic/OrgCreateFlow.tsx"
      to: "supabase.rpc('create_org', ...)"
      via: "createOrg wrapper"
      pattern: "createOrg\\("
    - from: "src/components/clinic/InvitePatientModal.tsx"
      to: "src/types/clinic.ts DATA_TYPE_LABELS"
      via: "10 scope-preselect checkboxes iterate DATA_TYPE_KEYS"
      pattern: "DATA_TYPE_KEYS|DATA_TYPE_LABELS"
    - from: "src/components/clinic/ClinicWorkspace.tsx"
      to: "Plan 09-01 App.tsx routing"
      via: "Plan 09-01 lazy import binds this file to `/clinic/{slug}` route; this plan OVERWRITES stub"
      pattern: "lazy.*ClinicWorkspace"
---

<objective>
Ship the operator-facing slice: OrgCreateFlow → workspace home (`/clinic/{slug}`) → ClinicContextBar → InvitePatientModal. **App.tsx routing already wired in Plan 09-01 (B-2 fix per plan-checker iter 1).** This plan only OVERWRITES `src/components/clinic/ClinicWorkspace.tsx` (Plan 09-01 stub) and creates the supporting components/wrappers.

Purpose: CLINIC-01 SC#1 (operator signs up + creates org + lands at workspace URL + sees clinic-context bar) needs OrgCreateFlow + ClinicWorkspace + ClinicContextBar. CLINIC-02 first leg (operator types `karsten@example.com` into the invite-patient flow) needs InvitePatientModal — and the D-02 anti-enumeration invariant ("Invitation sent" universal copy) is enforced at the UI layer here, not just the server.

Output: Operator can complete an end-to-end org-create → workspace-home → invite-modal flow in the local dev environment. The Edge Function dispatch is stubbed (returns `{ ok: true, invite_id: '<uuid>' }` immediately) until Plan 09-06 wires Resend. The `clinic` lazy chunk weighs ≤12 kB gz.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-clinic-b2b-foundations/09-CONTEXT.md
@.planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md
@.planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md
@.planning/phases/09-clinic-b2b-foundations/09-01-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-01-SUMMARY.md
@src/types/clinic.ts
@src/App.tsx
@src/components/ui/Button.tsx
@src/components/ui/Card.tsx
@src/components/ui/Modal.tsx
@src/components/ui/Input.tsx
@src/components/ui/EmptyState.tsx
@src/components/ui/Skeleton.tsx
@src/lib/supabase.ts
@src/lib/store.ts
</context>

<interfaces>
<!-- Plan 09-01 ships these types. Plan 09-02 consumes them. -->

From src/types/clinic.ts (Plan 09-01):
```typescript
export const DATA_TYPE_KEYS: readonly DataTypeKey[];
export type DataTypeKey = 'injections' | 'weights' | 'photos' | 'symptoms' | 'meals' | 'workouts' | 'supplements' | 'mood' | 'sleep' | 'doctor_report';
export type ConsentScope = Record<DataTypeKey, boolean>;
export const DATA_TYPE_LABELS: Record<DataTypeKey, { label: string; description: string }>;
export interface Org { /* see 09-01 */ }
```

Plan 09-02 exports (consumed by 09-03, 09-04, 09-05, 09-06, 09-07, 09-08):
```typescript
// src/lib/clinic.ts
export async function createOrg(p: {name: string, slug: string, description?: string, website_url?: string}): Promise<{ok: true, data: {org_id: string, slug: string}} | {ok: false, error: 'slug_taken'|'unauthenticated'|'network'}>;
// W-1 fix: sendInvite returns invite_id server-side regardless of email existence (no enumeration leak)
export async function sendInvite(p: {org_id: string, email: string, requested_scope: ConsentScope}): Promise<{ok: true, data: {invite_id: string}} | {ok: false, error: string}>;
// ... plus other wrappers per files_modified
export async function uploadOrgLogo(orgId: string, file: File): Promise<{ok: true, path: string} | {ok: false, error: string}>;
export async function checkSlugAvailable(slug: string): Promise<{available: boolean, reason?: 'taken'|'invalid'|'reserved'}>;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/lib/clinic.ts + clinic-realtime.ts (typed RPC wrappers + Realtime helpers)</name>
  <read_first>
    - .planning/phases/09-clinic-b2b-foundations/09-01-SUMMARY.md (B-2 fix: confirms App.tsx + stubs already in place)
    - src/lib/sync.ts:560-600 (existing Realtime postgres_changes pattern for comparison + extraction of the supabase client init)
    - src/lib/supabase.ts (existing supabase-js client singleton)
    - .planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md §"Pattern 4" lines 614-628 (subscribeToOrgChannel example) and §"Common Pitfalls" Pitfall #2 (setAuth before subscribe)
    - src/types/clinic.ts (from Plan 09-01)
  </read_first>
  <behavior>
    - Test 1 (createOrg happy): mocks supabase.rpc('create_org',...) returning {data: {org_id: 'uuid', slug: 'acme'}}; wrapper returns {ok: true, data: {...}}.
    - Test 2 (createOrg slug taken): mocks .rpc returning error code 23505; wrapper returns {ok: false, error: 'slug_taken'}.
    - Test 3 (sendInvite shape — W-1): asserts the RPC call passes p_org_id + p_email (lowercased + trimmed) + p_invite_token_hash + p_requested_scope; success response shape is `{ok: true, data: {invite_id: '<uuid>'}}` — invite_id is freshly generated, no email enumeration possible.
    - Test 4 (uploadOrgLogo): file passes 2 MB + MIME check; storage.upload called with `{org_id}/logo.{ext}` (bucket-relative path per Pitfall #6).
    - Test 5 (subscribeToOrgChannel): mocks supabase.realtime; asserts `setAuth()` is called BEFORE `.subscribe()` (Pitfall #2 invariant — order verified via call-order assertion).
    - Test 6 (subscribeToUserChannel): same ordering invariant.
    - Test 7 (isConsentScope drift): wrapper validates incoming ConsentScope objects via `isConsentScope` (Plan 09-01 export) before forwarding to RPCs; rejects extras/missing keys with `{ok:false, error:'invalid_scope'}` (Pitfall #8 jsonb drift).
  </behavior>
  <action>
    Create `src/lib/clinic.ts` with all 14 typed wrappers per the `<interfaces>` block above. Each wrapper:
    - Calls `supabase.rpc('<rpc_name>', { p_param1: ..., p_param2: ... })`.
    - Returns `{ok: true, data: T}` on `data` present without `error`.
    - Maps Postgres error codes to discriminated-union error variants: `23505 → 'slug_taken'`, `42501 → 'forbidden'`, `28000 → 'unauthenticated'`, `'invite_email_mismatch' → 'email_mismatch'`, `'invite_not_found_or_used' → 'invalid_invite'`, network failures → `'network'`.
    - NEVER throws — discriminated-union pattern (downstream UIs `switch`-on `.error`).

    `sendInvite` (W-1 fix per plan-checker iter 1): calls `supabase.rpc('send_invite', {p_email, p_org_id, p_invite_token_hash, p_requested_scope})` — server returns `[{invite_id}]`; wrapper returns `{ok: true, data: {invite_id}}`. The invite_id is generated server-side regardless of whether p_email matches an auth.users row, so no enumeration leak.

    `uploadOrgLogo(orgId, file)` uses `supabase.storage.from('org-logos').upload(`${orgId}/logo.${ext}`, file, { contentType: file.type, upsert: true })`. Validates file size ≤ 2 MB and MIME ∈ {`image/png`, `image/jpeg`} client-side BEFORE upload. Returns `{ok: true, path: '{orgId}/logo.{ext}'}` so caller can write to `orgs.logo_storage_path`.

    `checkSlugAvailable(slug)`: client-side regex + reserved-word check first (cheap); on pass, call lightweight RPC `select 1 from public.orgs where lower(slug) = lower($1) limit 1` via `supabase.from('orgs').select('id').eq('slug', slug).maybeSingle()` (RLS allows owner read on own; if not owner, returns null which is fine — UI treats null as available). For unauthenticated slug-check (during OrgCreateFlow pre-create), use anon client — slug visibility is intentionally public for collision detection.

    Create `src/lib/clinic-realtime.ts` with `subscribeToOrgChannel(orgId, onChange)` and `subscribeToUserChannel(userId, onChange)` per RESEARCH Pattern 4 verbatim:
    ```typescript
    export async function subscribeToOrgChannel(orgId: string, onChange: (payload: any) => void) {
      await supabase.realtime.setAuth();  // Pitfall #2 — load-bearing for private channels
      const channel = supabase
        .channel(`org:${orgId}`, { config: { private: true } })
        .on('broadcast', { event: 'INSERT' }, onChange)
        .on('broadcast', { event: 'UPDATE' }, onChange)
        .on('broadcast', { event: 'DELETE' }, onChange);
      await channel.subscribe();
      return channel;
    }
    ```

    Both helpers MUST be defer-mount-safe: if `supabase.auth.getSession()` resolves to `null`, return a no-op unsubscribe (the channel never subscribes). Pitfall #9 — WorkspaceSwitcher mounts before auth completes.

    Co-located vitest test file `src/lib/clinic.test.ts` covering behaviors 1-7 above.

    **No new `s.user!` non-null assertions anywhere** (anti-pattern from RESEARCH).

    Commit with `git commit -- src/lib/clinic.ts src/lib/clinic-realtime.ts src/lib/clinic.test.ts` (pathspec hygiene per memory).
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/clinic.test.ts</automated>
  </verify>
  <done>
    14 typed wrappers exist + clinic-realtime helpers exist; co-located tests pass; sendInvite W-1 invite_id shape locked; no s.user! introductions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClinicWorkspace (overwrites stub) + ClinicContextBar + OrgCreateFlow + InvitePatientModal — NO App.tsx edits</name>
  <read_first>
    - .planning/phases/09-clinic-b2b-foundations/09-01-SUMMARY.md (confirms App.tsx + ClinicWorkspace stub already exist)
    - src/App.tsx (Plan 09-01 already wired the `clinic` lazy import + selectView branch; verify, do NOT edit)
    - src/components/clinic/ClinicWorkspace.tsx (Plan 09-01 stub — OVERWRITE this file)
    - src/components/dashboard/settings/SettingsPage.tsx (NAV pattern + lazy-section pattern — reference only)
    - src/components/ui/{Modal, Card, Button, Input, EmptyState, Skeleton, Toast}.tsx (primitives)
    - src/illustrations/ (HeroOrbital pattern for empty-state art if helpful)
    - .planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md §"Operator-side: Org-create flow" + §"Workspace home" + §"Clinic-context bar + Workspace switcher" + §"Invite patient modal" + §"Data-type label table" — copy verbatim
    - .planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md §"State Coverage Checklist" lines 705-754 (test obligations)
    - src/hooks/useToast.ts (toast pattern)
    - src/hooks/useReducedMotion.ts (reduced-motion pattern)
  </read_first>
  <behavior>
    - Test 1 (OrgCreateFlow Step 1 mount): workspace-name Input auto-focuses; slug Input is empty.
    - Test 2 (slug auto-derive): typing "Northside Endo" → slug Input shows "northside-endo" within 200ms debounce.
    - Test 3 (slug independent edit): once user types in slug Input, subsequent name edits do NOT overwrite slug.
    - Test 4 (slug invalid inline error): typing "Foo!" in slug → inline error "Use lowercase letters, numbers, and hyphens only."
    - Test 5 (slug reserved inline error): typing "admin" → inline error "That URL is reserved. Try another."
    - Test 6 (slug taken inline error): mock checkSlugAvailable returning {available: false, reason: 'taken'} → inline error "That URL is already taken. Try another."
    - Test 7 (logo too-large): drop a 3 MB file → "That file is over 2 MB. Try a smaller image." inline error; logo state cleared.
    - Test 8 (logo wrong-type): drop a .gif file → "Use a PNG or JPEG image." error.
    - Test 9 (Create workspace disabled): button disabled until name + slug pass validation.
    - Test 10 (success state): submit succeeds → "Workspace created" heading visible (Fraunces italic class); workspace URL displayed; "Invite first patient" + "Go to workspace" CTAs both visible.
    - Test 11 (server error): submit fails with slug_taken → inline error preserving form state (no state reset).
    - Test 12 (ClinicWorkspace empty-roster): heading "{org name}"; empty-state body "Invite your first patient by email. They'll see your workspace name and choose what data to share with you."; "Invite patient" + "Customize workspace" CTAs.
    - Test 13 (ClinicWorkspace loading): renders Skeleton in context-bar + Skeleton empty-state until org data hydrates.
    - Test 14 (ClinicWorkspace network error): inline retry button + error copy.
    - Test 15 (ClinicContextBar): renders org logo (or monogram fallback if logo_storage_path is null) + org name (truncated to 32 chars) + WorkspaceSwitcher trigger placeholder (no-op button with aria-label "Switch workspace. Currently in {org name}.") + settings link.
    - Test 16 (InvitePatientModal initial): email Input focused; 10 scope-preselect checkboxes all checked; "Send invitation" disabled (no email yet).
    - Test 17 (InvitePatientModal email valid → button enabled).
    - Test 18 (InvitePatientModal anti-enumeration D-02 + W-1 invite_id shape): submit triggers sendInvite stub returning `{ok: true, data: {invite_id: 'uuid'}}` → modal transitions to post-send state with "Invitation sent" heading + universal copy regardless of whether email exists. NO branching copy. The invite_id is captured by parent for pending-invites list (Plan 09-03 MembersTab reads it).
    - Test 19 (InvitePatientModal failure): submit fails with network error → toast "Couldn't send invitation. Check your connection and try again." + modal stays open with form preserved.
    - Test 20 (No App.tsx edit in this plan — B-2): assert `git diff HEAD~1 -- src/App.tsx` returns empty (Wave 0 from Plan 09-01 is the only writer).
    - Test 21 (dark mode + reduced-motion snapshot): each component renders consistently under `prefers-color-scheme: dark` + `prefers-reduced-motion: reduce`.
  </behavior>
  <action>
    **B-2 fix per plan-checker iter 1:** Plan 09-01 already created the stub `src/components/clinic/ClinicWorkspace.tsx`, added the lazy import to App.tsx, and wired the `selectView` branch. This plan **OVERWRITES** the stub file with the real implementation. **DO NOT touch `src/App.tsx`.**

    Author 4 React components per UI-SPEC verbatim:

    **`src/components/clinic/ClinicWorkspace.tsx` (OVERWRITES Plan 09-01 stub)** — lazy-chunk root. Props: none (reads slug from `window.location.pathname.match(/^\/clinic\/([^/]+)/)`). Pulls `org` via `supabase.from('orgs').select('*').eq('slug', slug).maybeSingle()` (RLS gates to owner/member). On 401 → route back to `/`. On 404 (no row) → route back to `/`. On hydrated: ClinicContextBar + page heading + empty-roster shell with "Invite patient" CTA (opens InvitePatientModal via local useState). Roster table NOT rendered in Phase 9 (Phase 10 task).

    **`src/components/clinic/ClinicContextBar.tsx`** — sticky top bar (matches AppShell topbar height). Renders org logo `<img>` with `alt="{org_name} logo"` (using `supabase.storage.from('org-logos').getPublicUrl(logo_storage_path)`) OR monogram fallback. Switcher trigger placeholder: `<button aria-label="Switch workspace. Currently in {org name}." aria-haspopup="listbox" aria-expanded="false" onClick={() => { /* WorkspaceSwitcher in Plan 09-08 */ }}>{org_name}</button>`. Settings link `<a href={`/clinic/${slug}/settings`}>` (Plan 09-01 already wired the route; Plan 09-03 overwrites the ClinicSettingsPage stub).

    **`src/components/clinic/OrgCreateFlow.tsx`** — wizard. State: `{name, slug, slugManuallyEdited, logoFile, logoPreviewUrl, slugError, logoError, submitting, success, createdSlug}`. Slug auto-derive: `slug = name.toLowerCase().replace(/[^a-z0-9-]+/g, '-').replace(/^-+|-+$/g, '').slice(0, 60)`. Slug check on blur (debounced 400ms): calls `checkSlugAvailable(slug)` → sets `slugError`. Logo file picker: hidden `<input type="file" accept="image/png,image/jpeg">` triggered by visible `<label class="...">` styled as Button. Drag-drop overlay handles drop event. On submit: validate → `createOrg({name, slug, ...})` → on success: `uploadOrgLogo(org_id, logoFile)` if logoFile set → call `updateOrg({org_id, logo_storage_path})` → set success state. Success state has Fraunces italic class (from Phase 5/2.1 design tokens) on heading "Workspace created". Dual CTAs route via `window.history.pushState` + dispatch a custom event the App listens for.

    **`src/components/clinic/InvitePatientModal.tsx`** — controlled modal (props: `{open: boolean, orgId: string, onClose: () => void, onInviteSent?: (inviteId: string) => void}`). Email Input + 10 scope-preselect checkboxes mapped from `DATA_TYPE_KEYS.map(k => <Checkbox label={DATA_TYPE_LABELS[k].label} description={DATA_TYPE_LABELS[k].description} checked={scope[k]} onChange={...} />)`. Submit: `sendInvite({org_id: orgId, email: email.trim().toLowerCase(), requested_scope: scope})` → on `{ok: true, data: {invite_id}}` (W-1 shape): call optional `onInviteSent(invite_id)` callback + transition to anti-enumeration post-send state showing "Invitation sent" universal copy. On network error: toast + modal preserved.

    **NO App.tsx edits in this plan (B-2).** Plan 09-01's Task 1b already added the lazy imports, selectView branches in correct order (settings before base `/clinic/`), and the 4 stub files. Verify the existing wiring by reading App.tsx (do NOT modify).

    Co-locate RTL tests in `*.test.tsx` files. Use the Phase 5/6/7/8 testing-library pattern.

    **Bundle hygiene:** these components MUST be inside the `clinic` lazy chunk. Vite + Phase 6 `manualChunks` config auto-groups `src/components/clinic/**` if it's not already configured; check `vite.config.ts` and add a manualChunks function-form entry per Phase 2.1 pattern:
    ```typescript
    manualChunks(id) {
      if (id.includes('/components/clinic-invite/')) return 'clinic-invite';
      if (id.includes('/components/clinic/settings/')) return 'clinic-settings';
      if (id.includes('/components/clinic/')) return 'clinic';
      // ... existing rules
    }
    ```
    Confirm: `npm run build && ls dist/assets/clinic-*.js` shows ≥1 file; `gzip -c dist/assets/clinic-*.js | wc -c` returns ≤12288.

    Commit with `git commit -- <pathspec for each component file>` per memory `feedback_parallel_executor_git_isolation.md` (Wave 2 runs in parallel with 09-03/04/05).
  </action>
  <verify>
    <automated>npm run test -- --run src/components/clinic/ && npm run build && node scripts/bundle-size-check.cjs && git diff HEAD~1 -- src/App.tsx | wc -l | awk '{ if ($1 != 0) { print "ERROR: this plan modified App.tsx; B-2 says Plan 09-01 owns App.tsx writes"; exit 1 } else { print "OK: App.tsx untouched" } }'</automated>
  </verify>
  <done>
    - All 4 components exist + render the State Coverage Checklist rows.
    - 21 RTL test cases pass (including B-2 App.tsx-untouched assertion).
    - `npm run build` succeeds; `clinic` chunk ≤ 12 kB gz.
    - App.tsx selectView (wired by Plan 09-01) routes `/clinic/{slug}` → ClinicWorkspace lazy chunk; signed-out users redirected to auth.
    - Operator can complete an end-to-end manual smoke: signup → "/clinic/{slug}" route forced via dev tools → ClinicWorkspace mounts with empty-roster shell → "Invite patient" button opens InvitePatientModal → submit shows "Invitation sent" confirmation (with stubbed sendInvite returning `{ok:true, data:{invite_id:'uuid'}}`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → supabase.rpc('create_org', ...) | Operator's JWT validated by SECURITY DEFINER RPC; slug constraints CHECK'd server-side |
| operator browser → supabase.storage.from('org-logos').upload(...) | Storage RLS enforces `(storage.foldername(name))[1]::uuid` matches an org the operator has 'org.update' on |
| operator browser → InvitePatientModal submit | D-02 anti-enumeration enforced at UI layer (universal post-send copy) AND server (Edge Function in 09-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Tampering | client-side slug regex bypass via dev tools | mitigate | Server-side CHECK constraint on orgs.slug + UNIQUE(lower(slug)) reject; client validation is UX only |
| T-09-14 | Information disclosure | slug-availability check leaks org existence | accept | Slug list is intentionally semi-public (it's in URLs); reserved-word and uniqueness checks reveal nothing more than visiting the URL would |
| T-09-15 | Tampering | logo upload of non-image content | mitigate | Client-side MIME + size check + Storage bucket `allowed_mime_types = png+jpeg` server-side enforcement |
| T-09-16 | Information disclosure | D-02 anti-enumeration UI variance | mitigate | InvitePatientModal post-send copy is universal (Test 18); invite_id returned regardless of email existence (W-1 — generated server-side, leaks nothing); also enforced by Edge Function in 09-06 |
| T-09-17 | Spoofing | operator submits sendInvite for org they don't own | mitigate | send_invite RPC (Plan 09-01) gates on has_permission(uid, p_org_id, 'members.invite') — server-side; UI gating is UX hint only |
</threat_model>

<verification>
- `npm run test -- --run src/components/clinic/` passes all 21 cases.
- `npm run build` succeeds.
- `node scripts/bundle-size-check.cjs` reports clinic chunk ≤ 12 kB gz.
- `npm run typecheck` passes.
- Manual smoke (developer): visit `http://localhost:5173/clinic/acme` after creating an org for the signed-in user via the OrgCreateFlow; the workspace home renders.
- `grep -rn "s\.user!" src/components/clinic/ src/lib/clinic.ts src/lib/clinic-realtime.ts` returns 0 matches.
- B-2 invariant: `git diff HEAD~1 -- src/App.tsx` is empty for commits in this plan.
</verification>

<success_criteria>
1. All 4 components render + 21 RTL tests pass.
2. `clinic` lazy chunk gz ≤ 12 kB per CI guard.
3. App.tsx routing wired by Plan 09-01 — no edits in this plan (B-2).
4. OrgCreateFlow end-to-end: name → slug auto-derive → logo upload → submit → success state with workspace URL displayed.
5. InvitePatientModal anti-enumeration invariant: post-send copy is universal + invite_id returned (D-02 + W-1).
6. No s.user! non-null assertions introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/09-clinic-b2b-foundations/09-02-SUMMARY.md` with: component inventory, bundle-size measurements, any deviations from UI-SPEC copy.
</output>
