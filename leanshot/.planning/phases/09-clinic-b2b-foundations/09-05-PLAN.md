---
phase: 09-clinic-b2b-foundations
plan: 05
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/dashboard/settings/ActiveOrganizationsSection.tsx
  - src/components/dashboard/settings/ActiveOrganizationsSection.test.tsx
  - src/components/dashboard/settings/EditConsentScopeModal.tsx
  - src/components/dashboard/settings/EditConsentScopeModal.test.tsx
  - src/components/dashboard/settings/SettingsPage.tsx
autonomous: true
requirements: [CLINIC-02, CLINIC-03]
mvp_slice: "Patient-side org-management slice — patient opens Settings → 'Active organizations' tab → sees per-membership row with current scope → edits scope OR revokes. User-visible outcome: patient with 1 active membership can edit consent_scope.photos from true→false in EditConsentScopeModal → Save → row reflects updated scope; or click Revoke → typed-confirm → row animates out → audit_logs row written server-side (verified by Plan 09-10's revoke-latency drill). D-15 invariant satisfied."

must_haves:
  truths:
    - "SettingsPage NAV array contains new entry `{id:'organizations',label:'Active organizations',Icon:Building2}` between `'shares'` (Phase 8) and `'recovery'` per UI-SPEC line 575"
    - "ActiveOrganizationsSection renders per-membership row: org logo + name + role badge + joined relative-time + scope summary text (e.g., 'Sharing 7 of 10 data types') + Edit + Revoke IconButtons"
    - "Empty state copy 'You haven't joined any clinics yet.' + body 'When a clinic invites you, you'll see them here.'"
    - "Section pulls memberships via `supabase.from('memberships').select('*, orgs(*), roles(name)').eq('user_id', auth.uid()).is('revoked_at', null)` — RLS allows patient to SELECT own"
    - "EditConsentScopeModal renders 10 checkboxes pre-filled from current `memberships.consent_scope` using DATA_TYPE_LABELS; Save calls `updateConsentScope({membership_id, consent_scope})` → on success: toast `Sharing updated.` + row updates"
    - "Revoke flow: typed-confirm modal (Type {org name} to revoke) → submit calls `revokeMembership({membership_id})` → row animates out + toast `You've left {org name}.`"
    - "Realtime: subscribe to `user:{user_id}` channel via `subscribeToUserChannel` from Plan 09-02 — when operator revokes from their side, patient's row receives broadcast + animates out + toast `{org name} ended your membership.`"
    - "Both Edit and Revoke actions persist via Plan 09-01 RPCs (update_consent_scope + revoke_membership) — every write produces an audit_logs row (CLINIC-07 capture half)"
    - "RTL tests cover State Coverage Checklist rows 833-852 from UI-SPEC"
  artifacts:
    - path: "src/components/dashboard/settings/ActiveOrganizationsSection.tsx"
      provides: "Patient-side Active organizations section + per-membership rows + scope summary + Edit/Revoke + Realtime subscription"
      min_lines: 180
    - path: "src/components/dashboard/settings/EditConsentScopeModal.tsx"
      provides: "10-checkbox scope edit modal + Save action"
      min_lines: 100
    - path: "src/components/dashboard/settings/SettingsPage.tsx"
      provides: "NAV array extension — new 'organizations' entry"
      contains: "id: 'organizations'"
  key_links:
    - from: "src/components/dashboard/settings/ActiveOrganizationsSection.tsx"
      to: "src/lib/clinic-realtime.ts subscribeToUserChannel"
      via: "realtime push for cross-context revoke broadcasts"
      pattern: "subscribeToUserChannel"
    - from: "src/components/dashboard/settings/EditConsentScopeModal.tsx"
      to: "src/lib/clinic.ts updateConsentScope"
      via: "typed RPC wrapper"
      pattern: "updateConsentScope"
---

<objective>
Ship the patient-side org-management slice — Active organizations tab in SettingsPage with per-membership rows, scope-edit modal, and revoke flow. The Realtime cross-context broadcast (operator revokes patient → patient's tab updates without manual refresh) proves D-10 Layer 1 end-to-end from the patient direction; Plan 09-10 will exercise the operator direction.

Purpose: D-15 (patient-side Active organizations tab) is the canonical revocation affordance — SC#5's "patient revokes membership from Settings" path. D-06 (consent_scope editable post-acceptance) is the EditConsentScopeModal. CLINIC-03 second half (revocation path visible at consent time + actually usable post-acceptance) closes here. Every revoke + scope-change writes an audit_logs row server-side (Plan 09-01 RPCs) — CLINIC-07 capture half verified by Plan 09-09's RLS impersonation test.

Output: Patient can view all their active memberships, edit per-membership scope, revoke any. Realtime broadcasts deliver operator-side revokes within ~100-300ms. Section rides the existing settings chunk (≤4 kB delta).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-clinic-b2b-foundations/09-CONTEXT.md
@.planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md
@.planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md
@.planning/phases/09-clinic-b2b-foundations/09-01-PLAN.md
@src/types/clinic.ts
@src/lib/clinic.ts
@src/lib/clinic-realtime.ts
@src/components/dashboard/settings/SettingsPage.tsx
@src/components/dashboard/settings/ActiveSharesSection.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActiveOrganizationsSection + EditConsentScopeModal + SettingsPage NAV extension</name>
  <read_first>
    - .planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md §"Patient-side: Settings → Active organizations tab" lines 488-517 (all copy verbatim)
    - .planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md §"State Coverage Checklist" lines 833-852
    - src/components/dashboard/settings/SettingsPage.tsx (current NAV array)
    - src/components/dashboard/settings/ActiveSharesSection.tsx (Phase 8 — section pattern + revoke pattern; reuse structure)
    - src/types/clinic.ts (DATA_TYPE_LABELS + ConsentScope)
    - src/lib/clinic.ts updateConsentScope + revokeMembership wrappers
    - src/lib/clinic-realtime.ts subscribeToUserChannel
  </read_first>
  <behavior>
    - Test 1 (SettingsPage NAV extension): NAV array contains `'organizations'` entry between `'shares'` and `'recovery'`; clicking renders ActiveOrganizationsSection.
    - Test 2 (ActiveOrganizationsSection empty state): no memberships → 'You haven't joined any clinics yet.' heading + body.
    - Test 3 (ActiveOrganizationsSection row render): membership with 7 trues / 3 falses in consent_scope → scope summary 'Sharing 7 of 10 data types'.
    - Test 4 (Edit IconButton): opens EditConsentScopeModal pre-filled with current consent_scope.
    - Test 5 (EditConsentScopeModal 10 checkboxes): renders all 10 from DATA_TYPE_LABELS in canonical order.
    - Test 6 (EditConsentScopeModal save): toggles checkbox + Save → calls updateConsentScope({membership_id, consent_scope}) → on success: modal closes + toast 'Sharing updated.' + parent row reflects new summary.
    - Test 7 (Revoke IconButton): opens typed-confirm modal 'Type {org name} to revoke'.
    - Test 8 (Revoke submit): calls revokeMembership({membership_id}) → row animates out + toast `You've left {org name}.`
    - Test 9 (Realtime cross-context broadcast): mock subscribeToUserChannel; simulate payload `{eventType:'UPDATE', new:{id:M, revoked_at:'2026-...'}}` → matching row animates out + toast `{org name} ended your membership.` (operator-side revoke).
    - Test 10 (Realtime scope change broadcast): simulate payload with new consent_scope → row scope summary updates without manual refresh.
    - Test 11 (loading skeleton): initial mount before memberships fetch resolves shows Skeleton rows.
    - Test 12 (network error): fetch fails → inline retry button.
    - Test 13 (Pitfall #8 jsonb drift defense): mock returns membership with consent_scope missing the 'sleep' key → ActiveOrganizationsSection renders sleep as "not yet granted" hint; EditConsentScopeModal renders all 10 checkboxes with sleep unchecked.
    - Test 14 (dark mode + reduced-motion snapshots).
  </behavior>
  <action>
    Author 2 new component files + edit 1 existing:

    **`src/components/dashboard/settings/ActiveOrganizationsSection.tsx`** — section component (mirrors Phase 8 `ActiveSharesSection`). Pulls memberships via:
    ```typescript
    const { data, error } = await supabase
      .from('memberships')
      .select('id, consent_scope, joined_at, accepted_at, role_id, org_id, orgs(id, name, logo_storage_path), roles(name)')
      .is('revoked_at', null)
      .order('joined_at', { ascending: false });
    ```

    Renders empty state OR list of rows. Each row (use `<li role="listitem">` inside `<ul role="list">` per UI-SPEC accessibility line 691):
    - Org logo `<img>` (public URL via `supabase.storage.from('org-logos').getPublicUrl(logo_storage_path)`) OR monogram fallback.
    - Org name (bold) + role badge (`<Pill>` with role name).
    - Joined relative-time (use existing `formatRelativeTime` helper from Phase 6/7 if it exists; otherwise inline).
    - Scope summary: `Sharing {N} of 10 data types` where N = `Object.values(consent_scope).filter(v => v === true).length`. Defensively use `DATA_TYPE_KEYS.filter(k => consent_scope[k] === true).length` (Pitfall #8 — count only canonical keys).
    - Edit IconButton (`aria-label="Edit what you share with {org name}"`).
    - Revoke IconButton (`aria-label="Revoke membership in {org name}"`).

    Subscribe to user channel on mount via:
    ```typescript
    useEffect(() => {
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) return;
      const channel = await subscribeToUserChannel(user.id, (payload) => {
        // Re-fetch memberships OR optimistically apply the payload to local state
        refetchMemberships();
        // Toast for cross-context revokes
        if (payload.eventType === 'UPDATE' && payload.new.revoked_at && !payload.old?.revoked_at) {
          const orgName = /* lookup from local state */;
          toast({ message: `${orgName} ended your membership.` });
        }
      });
      return () => { channel.unsubscribe(); };
    }, []);
    ```

    Edit click → opens `<EditConsentScopeModal membership={row} onSaved={() => refetchMemberships()} onClose={() => ...} />`.

    Revoke click → opens typed-confirm modal. On confirm: `revokeMembership({membership_id})` → on success: animate row out (use existing framer-motion AnimatePresence pattern from MembersTab in Plan 09-03 if helpful) + toast. The Realtime channel will also broadcast the UPDATE; UI must guard against double-render (either ignore-broadcast-when-source OR rely on idempotent refetch).

    **`src/components/dashboard/settings/EditConsentScopeModal.tsx`** — controlled modal. Props: `{membership: Membership & {orgs: Org}, onSaved: () => void, onClose: () => void}`. State: `scope: ConsentScope` initialized from `membership.consent_scope` with defensive default for missing keys: `DATA_TYPE_KEYS.reduce((acc, k) => ({...acc, [k]: membership.consent_scope[k] === true}), {} as ConsentScope)`. 10 checkboxes mapped from `DATA_TYPE_KEYS`. Save calls `updateConsentScope({membership_id: membership.id, consent_scope: scope})`. On success: `onSaved()` + close. Toast handled by parent.

    **`src/components/dashboard/settings/SettingsPage.tsx`** — edit ONLY the NAV array to add the new entry between `'shares'` and `'recovery'`:
    ```typescript
    const NAV = [
      // ... existing entries through 'shares'
      { id: 'shares', label: 'Active shares', Icon: Link2 },
      { id: 'organizations', label: 'Active organizations', Icon: Building2 },  // NEW Phase 9
      { id: 'recovery', label: 'Recovery', Icon: RotateCcw },
      // ... rest unchanged
    ] as const;
    ```
    Add the section-component mapping:
    ```typescript
    case 'organizations': return <ActiveOrganizationsSection />;
    ```
    Lazy-load if SettingsPage uses lazy-section pattern (check existing pattern; ActiveSharesSection is lazy → mirror that).

    Co-locate RTL tests. Mock `supabase.from`, `supabase.rpc`, `subscribeToUserChannel`.

    **Bundle:** rides the existing settings chunk (≤4 kB delta budget per UI-SPEC line 590). Confirm via `npm run build`.

    **Parallel-executor hygiene:** SettingsPage.tsx is touched by THIS plan only in Wave 2 (no overlap with 09-02/03/04). Commit with pathspec `git commit -- src/components/dashboard/settings/`.

    **No `s.user!` introductions.**
  </action>
  <verify>
    <automated>npm run test -- --run src/components/dashboard/settings/ActiveOrganizationsSection src/components/dashboard/settings/EditConsentScopeModal</automated>
  </verify>
  <done>
    - 2 components exist + 14 RTL tests pass.
    - SettingsPage NAV array contains the 'organizations' entry in correct position.
    - Settings chunk delta ≤ 4 kB.
    - Manual smoke: patient with active membership opens Settings → Active organizations → sees row → opens EditConsentScopeModal → toggles → Save → toast + row updates.
    - Mocked Realtime broadcast simulates operator revoke → patient's row animates out + toast.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patient browser → supabase.from('memberships') | RLS `memberships_select_own` (Plan 09-01) restricts to patient's own rows |
| patient browser → updateConsentScope RPC | Server-side validates auth.uid() = memberships.user_id; 10-key jsonb shape check |
| patient browser → revokeMembership RPC | Server allows self-revoke (auth.uid() = memberships.user_id) |
| patient browser → user:{user_id} Realtime channel | realtime.messages RLS dispatches to auth.uid() = parsed user_id (Plan 09-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-29 | Tampering | client-side consent_scope drift attack | mitigate | EditConsentScopeModal iterates DATA_TYPE_KEYS (not Object.keys); update_consent_scope RPC validates jsonb shape against 10 canonical keys (Plan 09-01); Pitfall #8 mitigation |
| T-09-30 | Information disclosure | Realtime user-channel leak across users | mitigate | realtime.messages RLS gates `user:` topic on auth.uid() = parsed user_id; cross-user negative-space test in Plan 09-10 |
| T-09-31 | Repudiation | patient denies revoking | mitigate | revoke_membership RPC writes audit_logs row with actor_type='org_member' + org_id + user_id_hash; visible in Phase 10 audit surface |
</threat_model>

<verification>
- 14 RTL tests pass.
- SettingsPage NAV array order matches UI-SPEC.
- Manual smoke: full edit-scope + revoke flow works end-to-end against the live DB.
- Realtime cross-context broadcast simulation: operator-side revoke → patient row updates within UI test.
- `grep -rn "s\.user!" src/components/dashboard/settings/ActiveOrganizationsSection.tsx src/components/dashboard/settings/EditConsentScopeModal.tsx` returns 0 matches.
</verification>

<success_criteria>
1. Patient-side Active organizations section renders rows + scope summary + Edit + Revoke.
2. EditConsentScopeModal updates consent_scope via updateConsentScope RPC.
3. Revoke flow writes revoked_at + animates row out + toast.
4. Realtime user-channel cross-context updates work.
5. Settings chunk delta ≤ 4 kB.
6. SettingsPage NAV array properly extended.
</success_criteria>

<output>
After completion, create `.planning/phases/09-clinic-b2b-foundations/09-05-SUMMARY.md`.
</output>
