---
phase: 09-clinic-b2b-foundations
plan: 05
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx
  - src/components/dashboard/settings/sections/ActiveOrganizationsSection.test.tsx
  - src/components/dashboard/settings/EditConsentScopeModal.tsx
  - src/components/dashboard/settings/EditConsentScopeModal.test.tsx
autonomous: true
requirements: [CLINIC-02, CLINIC-03, CLINIC-07]
mvp_slice: "Patient-side org-management slice — patient opens Settings → 'Active organizations' tab → sees per-membership row with current scope → edits scope OR revokes. User-visible outcome: patient with 1 active membership can edit consent_scope.photos from true→false in EditConsentScopeModal → Save → row reflects updated scope; or click Revoke → typed-confirm → row animates out → audit_logs row written server-side (verified by Plan 09-10's revoke-latency drill). D-15 invariant satisfied. **B-2 fix per plan-checker iter 1:** Plan 09-01 already added the SettingsPage NAV `'organizations'` entry + the stub `ActiveOrganizationsSection.tsx` file — this plan only OVERWRITES the stub. NO SettingsPage.tsx edits in this plan."

must_haves:
  truths:
    - "Plan 09-01 already added the `'organizations'` entry to SettingsPage NAV array between `'shares'` and `'recovery'` + created stub `src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx`; this plan OVERWRITES the stub. NO SettingsPage.tsx edits in this plan."
    - "ActiveOrganizationsSection renders per-membership row: org logo + name + role badge + joined relative-time + scope summary text (e.g., 'Sharing 7 of 10 data types') + Edit + Revoke IconButtons"
    - "Empty state copy 'You haven't joined any clinics yet.' + body 'When a clinic invites you, you'll see them here.'"
    - "Section pulls memberships via `supabase.from('memberships').select('*, orgs(*), roles(name)').eq('user_id', auth.uid()).is('revoked_at', null)` — RLS allows patient to SELECT own"
    - "EditConsentScopeModal renders 10 checkboxes pre-filled from current `memberships.consent_scope` using DATA_TYPE_LABELS; Save calls `updateConsentScope({membership_id, consent_scope})` → on success: toast `Sharing updated.` + row updates"
    - "EditConsentScopeModal defensively builds initial scope from membership.consent_scope: `DATA_TYPE_KEYS.reduce((acc, k) => ({...acc, [k]: membership.consent_scope[k] === true}), {} as ConsentScope)` — Pitfall #8 jsonb drift defense (mirrors W-5 fix in Plan 09-04 ConsentDialog)"
    - "Revoke flow: typed-confirm modal (Type {org name} to revoke) → submit calls `revokeMembership({membership_id})` → row animates out + toast `You've left {org name}.`"
    - "Realtime: subscribe to `user:{user_id}` channel via `subscribeToUserChannel` from Plan 09-02 — when operator revokes from their side, patient's row receives broadcast + animates out + toast `{org name} ended your membership.`"
    - "Both Edit and Revoke actions persist via Plan 09-01 RPCs (update_consent_scope + revoke_membership) — every write produces an audit_logs row (CLINIC-07 capture half)"
    - "RTL tests cover State Coverage Checklist rows 833-852 from UI-SPEC"
  artifacts:
    - path: "src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx"
      provides: "Patient-side Active organizations section + per-membership rows + scope summary + Edit/Revoke + Realtime subscription — OVERWRITES Plan 09-01 stub"
      min_lines: 180
    - path: "src/components/dashboard/settings/EditConsentScopeModal.tsx"
      provides: "10-checkbox scope edit modal + Save action + defensive scope-init"
      min_lines: 100
  key_links:
    - from: "src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx"
      to: "src/lib/clinic-realtime.ts subscribeToUserChannel"
      via: "realtime push for cross-context revoke broadcasts"
      pattern: "subscribeToUserChannel"
    - from: "src/components/dashboard/settings/EditConsentScopeModal.tsx"
      to: "src/lib/clinic.ts updateConsentScope"
      via: "typed RPC wrapper — writes audit row (CLINIC-07)"
      pattern: "updateConsentScope"
    - from: "src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx"
      to: "src/lib/clinic.ts revokeMembership"
      via: "patient self-revoke writes audit row (CLINIC-07 capture half)"
      pattern: "revokeMembership"
    - from: "Plan 09-01 SettingsPage NAV"
      to: "ActiveOrganizationsSection"
      via: "NAV entry binds this section file (overwritten in place)"
      pattern: "id:\\s*'organizations'"
---

<objective>
Ship the patient-side org-management slice — Active organizations tab in SettingsPage with per-membership rows, scope-edit modal, and revoke flow. The Realtime cross-context broadcast (operator revokes patient → patient's tab updates without manual refresh) proves D-10 Layer 1 end-to-end from the patient direction; Plan 09-10 will exercise the operator direction. **SettingsPage NAV + stub file already added by Plan 09-01 (B-2 fix per plan-checker iter 1).** This plan only OVERWRITES the stub `ActiveOrganizationsSection.tsx`.

Purpose: D-15 (patient-side Active organizations tab) is the canonical revocation affordance — SC#5's "patient revokes membership from Settings" path. D-06 (consent_scope editable post-acceptance) is the EditConsentScopeModal. CLINIC-03 second half (revocation path visible at consent time + actually usable post-acceptance) closes here. Every revoke + scope-change writes an audit_logs row server-side (Plan 09-01 RPCs) — CLINIC-07 capture half verified by Plan 09-09's RLS impersonation test.

Output: Patient can view all their active memberships, edit per-membership scope, revoke any. Realtime broadcasts deliver operator-side revokes within ~100-300ms. Section rides the existing settings chunk (≤4 kB delta).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-clinic-b2b-foundations/09-CONTEXT.md
@.planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md
@.planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md
@.planning/phases/09-clinic-b2b-foundations/09-01-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-01-SUMMARY.md
@src/types/clinic.ts
@src/lib/clinic.ts
@src/lib/clinic-realtime.ts
@src/components/dashboard/settings/SettingsPage.tsx
@src/components/dashboard/settings/ActiveSharesSection.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActiveOrganizationsSection (overwrites stub) + EditConsentScopeModal — NO SettingsPage edits</name>
  <read_first>
    - .planning/phases/09-clinic-b2b-foundations/09-01-SUMMARY.md (B-2 fix: confirms SettingsPage NAV + ActiveOrganizationsSection stub already in place)
    - .planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md §"Patient-side: Settings → Active organizations tab" lines 488-517 (all copy verbatim)
    - .planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md §"State Coverage Checklist" lines 833-852
    - src/components/dashboard/settings/SettingsPage.tsx (Plan 09-01 already added NAV entry + lazy mount; verify, do NOT edit)
    - src/components/dashboard/settings/ActiveSharesSection.tsx (Phase 8 — section pattern + revoke pattern; reuse structure)
    - src/types/clinic.ts (DATA_TYPE_KEYS + DATA_TYPE_LABELS + ConsentScope for defensive scope-init mirroring W-5)
    - src/lib/clinic.ts updateConsentScope + revokeMembership wrappers
    - src/lib/clinic-realtime.ts subscribeToUserChannel
  </read_first>
  <behavior>
    - Test 1 (No SettingsPage edits — B-2): assert `git diff HEAD~1 -- src/components/dashboard/settings/SettingsPage.tsx` returns empty. Plan 09-01 already added the NAV entry.
    - Test 2 (ActiveOrganizationsSection empty state): no memberships → 'You haven't joined any clinics yet.' heading + body.
    - Test 3 (ActiveOrganizationsSection row render): membership with 7 trues / 3 falses in consent_scope → scope summary 'Sharing 7 of 10 data types'.
    - Test 4 (Edit IconButton): opens EditConsentScopeModal pre-filled with current consent_scope.
    - Test 5 (EditConsentScopeModal 10 checkboxes): renders all 10 from DATA_TYPE_LABELS in canonical order.
    - Test 6 (EditConsentScopeModal save): toggles checkbox + Save → calls updateConsentScope({membership_id, consent_scope}) → on success: modal closes + toast 'Sharing updated.' + parent row reflects new summary. Server writes audit_logs row (CLINIC-07).
    - Test 7 (Revoke IconButton): opens typed-confirm modal 'Type {org name} to revoke'.
    - Test 8 (Revoke submit): calls revokeMembership({membership_id}) → row animates out + toast `You've left {org name}.` + audit_logs row written server-side (CLINIC-07 capture half).
    - Test 9 (Realtime cross-context broadcast): mock subscribeToUserChannel; simulate payload `{eventType:'UPDATE', new:{id:M, revoked_at:'2026-...'}}` → matching row animates out + toast `{org name} ended your membership.` (operator-side revoke).
    - Test 10 (Realtime scope change broadcast): simulate payload with new consent_scope → row scope summary updates without manual refresh.
    - Test 11 (loading skeleton): initial mount before memberships fetch resolves shows Skeleton rows.
    - Test 12 (network error): fetch fails → inline retry button.
    - Test 13 (Pitfall #8 jsonb drift defense): mock returns membership with consent_scope missing the 'sleep' key AND extra 'foo' key → ActiveOrganizationsSection renders sleep as "not yet granted" hint; EditConsentScopeModal initializes via `DATA_TYPE_KEYS.reduce(...)` → renders all 10 checkboxes with sleep unchecked AND no 'foo' key sent on Save.
    - Test 14 (dark mode + reduced-motion snapshots).
  </behavior>
  <action>
    **B-2 fix per plan-checker iter 1:** Plan 09-01 already added the SettingsPage NAV entry `{id:'organizations', label:'Active organizations', Icon:Building2}` between `'shares'` and `'recovery'`, plus created the stub file `src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx`. This plan **OVERWRITES** the stub. **DO NOT touch `src/components/dashboard/settings/SettingsPage.tsx`.**

    Author 2 component files:

    **`src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx` (OVERWRITES Plan 09-01 stub)** — section component (mirrors Phase 8 `ActiveSharesSection`). Pulls memberships via:
    ```typescript
    const { data, error } = await supabase
      .from('memberships')
      .select('id, consent_scope, joined_at, accepted_at, role_id, org_id, orgs(id, name, logo_storage_path), roles(name)')
      .is('revoked_at', null)
      .order('joined_at', { ascending: false });
    ```

    Renders empty state OR list of rows. Each row (use `<li role="listitem">` inside `<ul role="list">` per UI-SPEC accessibility line 691):
    - Org logo `<img>` (public URL via `supabase.storage.from('org-logos').getPublicUrl(logo_storage_path)`) OR monogram fallback.
    - Org name (bold) + role badge (`<Pill>` with role name).
    - Joined relative-time.
    - Scope summary: `Sharing {N} of 10 data types` where N = `DATA_TYPE_KEYS.filter(k => consent_scope[k] === true).length` (Pitfall #8 — count only canonical keys via canonical const).
    - Edit IconButton (`aria-label="Edit what you share with {org name}"`).
    - Revoke IconButton (`aria-label="Revoke membership in {org name}"`).

    Subscribe to user channel on mount via:
    ```typescript
    useEffect(() => {
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) return;
      const channel = await subscribeToUserChannel(user.id, (payload) => {
        refetchMemberships();
        if (payload.eventType === 'UPDATE' && payload.new.revoked_at && !payload.old?.revoked_at) {
          const orgName = /* lookup from local state */;
          toast({ message: `${orgName} ended your membership.` });
        }
      });
      return () => { channel.unsubscribe(); };
    }, []);
    ```

    Edit click → opens `<EditConsentScopeModal membership={row} onSaved={() => refetchMemberships()} onClose={() => ...} />`.

    Revoke click → opens typed-confirm modal. On confirm: `revokeMembership({membership_id})` → on success: animate row out + toast. Server writes `membership_revoked` audit row (CLINIC-07).

    **`src/components/dashboard/settings/EditConsentScopeModal.tsx`** — controlled modal. Props: `{membership: Membership & {orgs: Org}, onSaved: () => void, onClose: () => void}`.

    **Defensive scope-init (mirrors Plan 09-04 W-5 fix):**
    ```typescript
    import { DATA_TYPE_KEYS, type ConsentScope } from '@/types/clinic';

    const [scope, setScope] = useState<ConsentScope>(() =>
      DATA_TYPE_KEYS.reduce(
        (acc, k) => ({ ...acc, [k]: membership.consent_scope[k] === true }),
        {} as ConsentScope,
      )
    );
    ```
    10 checkboxes mapped from `DATA_TYPE_KEYS`. Save calls `updateConsentScope({membership_id: membership.id, consent_scope: scope})`. On success: `onSaved()` + close. Toast handled by parent.

    **NO SettingsPage.tsx edits in this plan (B-2).** Plan 09-01 already added the NAV entry + lazy section mount. Verify by reading SettingsPage.tsx (do NOT modify).

    Co-locate RTL tests. Mock `supabase.from`, `supabase.rpc`, `subscribeToUserChannel`.

    **Bundle:** rides the existing settings chunk (≤4 kB delta budget per UI-SPEC line 590). Confirm via `npm run build`.

    **Parallel-executor hygiene:** SettingsPage.tsx is OWNED by Plan 09-01 — no overlap with 09-02/03/04 in Wave 2. Commit with pathspec `git commit -- src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx src/components/dashboard/settings/sections/ActiveOrganizationsSection.test.tsx src/components/dashboard/settings/EditConsentScopeModal.tsx src/components/dashboard/settings/EditConsentScopeModal.test.tsx`.

    **No `s.user!` introductions.**
  </action>
  <verify>
    <automated>npm run test -- --run src/components/dashboard/settings/sections/ActiveOrganizationsSection src/components/dashboard/settings/EditConsentScopeModal && git diff HEAD~1 -- src/components/dashboard/settings/SettingsPage.tsx | wc -l | awk '{ if ($1 != 0) { print "ERROR: B-2 violated — SettingsPage.tsx touched"; exit 1 } else { print "OK: SettingsPage.tsx untouched" } }'</automated>
  </verify>
  <done>
    - 2 components exist + 14 RTL tests pass (including B-2 invariant + W-5-style defensive scope-init).
    - SettingsPage NAV array contains the 'organizations' entry in correct position (verified, not edited).
    - Settings chunk delta ≤ 4 kB.
    - Manual smoke: patient with active membership opens Settings → Active organizations → sees row → opens EditConsentScopeModal → toggles → Save → toast + row updates.
    - Mocked Realtime broadcast simulates operator revoke → patient's row animates out + toast.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patient browser → supabase.from('memberships') | RLS `memberships_select_own` (Plan 09-01) restricts to patient's own rows |
| patient browser → updateConsentScope RPC | Server-side validates auth.uid() = memberships.user_id; 10-key jsonb shape check |
| patient browser → revokeMembership RPC | Server allows self-revoke (auth.uid() = memberships.user_id); writes audit_logs row (CLINIC-07) |
| patient browser → user:{user_id} Realtime channel | realtime.messages RLS dispatches to auth.uid() = parsed user_id (Plan 09-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-29 | Tampering | client-side consent_scope drift attack | mitigate | EditConsentScopeModal iterates DATA_TYPE_KEYS (not Object.keys); update_consent_scope RPC validates jsonb shape against 10 canonical keys (Plan 09-01); Pitfall #8 mitigation |
| T-09-30 | Information disclosure | Realtime user-channel leak across users | mitigate | realtime.messages RLS gates `user:` topic on auth.uid() = parsed user_id; cross-user negative-space test in Plan 09-10 |
| T-09-31 | Repudiation | patient denies revoking | mitigate | revoke_membership RPC writes audit_logs row with actor_type='org_member' + org_id + user_id_hash (CLINIC-07 capture half); visible in Phase 10 audit surface |
</threat_model>

<verification>
- 14 RTL tests pass.
- SettingsPage NAV array order matches UI-SPEC (verified, untouched by this plan — B-2).
- Manual smoke: full edit-scope + revoke flow works end-to-end against the live DB.
- Realtime cross-context broadcast simulation: operator-side revoke → patient row updates within UI test.
- `grep -rn "s\.user!" src/components/dashboard/settings/sections/ActiveOrganizationsSection.tsx src/components/dashboard/settings/EditConsentScopeModal.tsx` returns 0 matches.
- B-2 invariant: this plan's commits do not modify src/components/dashboard/settings/SettingsPage.tsx.
</verification>

<success_criteria>
1. Patient-side Active organizations section renders rows + scope summary + Edit + Revoke (overwrites Plan 09-01 stub).
2. EditConsentScopeModal updates consent_scope via updateConsentScope RPC with defensive scope-init.
3. Revoke flow writes revoked_at + animates row out + toast + audit_logs row (CLINIC-07).
4. Realtime user-channel cross-context updates work.
5. Settings chunk delta ≤ 4 kB.
6. SettingsPage NAV array properly extended (by Plan 09-01, verified here).
7. No SettingsPage edits (B-2).
</success_criteria>

<output>
After completion, create `.planning/phases/09-clinic-b2b-foundations/09-05-SUMMARY.md`.
</output>
