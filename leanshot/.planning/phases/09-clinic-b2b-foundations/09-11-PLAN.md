---
phase: 09-clinic-b2b-foundations
plan: 11
type: execute
wave: 4
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05", "09-06", "09-07", "09-08", "09-09", "09-10"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/09-clinic-b2b-foundations/09-VALIDATION.md
  - .planning/phases/09-clinic-b2b-foundations/09-SUMMARY.md
autonomous: true
requirements: [CLINIC-01, CLINIC-02, CLINIC-03, CLINIC-06]
mvp_slice: "Phase close slice — verify all 6 SCs satisfied; update ROADMAP entries for Phase 9 (mark plans complete) and Phase 10 (CLINIC-06 moved here, CLINIC-07 split); update REQUIREMENTS.md traceability table; fill the per-task verification map in 09-VALIDATION.md and flip `status: complete`; draft 09-SUMMARY.md aggregating per-plan SUMMARYs. No code changes — pure docs sync + verification. **B-5 fix per plan-checker iter 1:** Plan 09-01 Task 2 already flipped `wave_0_complete: true` AND `nyquist_compliant: true` in 09-VALIDATION.md frontmatter immediately after the supabase db push succeeded. Plan 09-11 only flips the phase-close marker `status: complete` and fills the per-task verification map."

must_haves:
  truths:
    - "ROADMAP.md Phase 9 entry Plans list updated: 11 plan rows marked complete (or in-progress for any deferred plan) with one-line descriptions"
    - "ROADMAP.md Phase 9 Plans count line updated to '**Plans:** 11 plans'"
    - "ROADMAP.md Phase 10 entry Scope note already reflects CLINIC-06 absorbed by Phase 9; verify and update if any drift detected"
    - "REQUIREMENTS.md per-phase summary table updated: Phase 9 row count = 4 (CLINIC-01, CLINIC-02, CLINIC-03, CLINIC-06) + audit-capture portion of CLINIC-07; Phase 10 row count = 3 (CLINIC-04, CLINIC-05, CLINIC-07 surface UI half)"
    - "REQUIREMENTS.md traceability table: CLINIC-01..03 marked Complete; CLINIC-06 marked Complete with note 'shipped Phase 9 per scope expansion 2026-05-12'; CLINIC-07 row split or footnoted to indicate capture half is shipped Phase 9 + surface UI half shipped Phase 10"
    - "STATE.md Current Position updated to reflect Phase 9 complete (if all SCs verified) OR document blockers/deferred items if any specs are RC5-blocked"
    - "STATE.md Accumulated Context: append decision rows for any new architectural rules established in Phase 9 (e.g., `has_permission()` as the single RLS dispatch primitive; Realtime broadcast pattern + realtime.messages RLS; org-deletion cascade GUC hooks)"
    - "09-VALIDATION.md frontmatter flip in THIS plan: `status: draft` → `status: complete` ONLY. `wave_0_complete` and `nyquist_compliant` were already flipped by Plan 09-01 Task 2 (B-5 ownership rule per plan-checker iter 1). This plan does NOT touch those two flags — verifies they are already true and FAILS gate if they aren't."
    - "09-VALIDATION.md per-task verification map (lines 42-54): replace 11 placeholder rows with the real plan IDs from 09-01 through 09-11; update Status column to ✅ for green specs / ⚠️ for deferred / ❌ if any are still failing"
    - "09-SUMMARY.md (NEW phase-level summary): aggregate per-plan SUMMARYs into a single phase close document with: deliverables list, key architectural decisions, deviations from RESEARCH.md/UI-SPEC.md, deferred items list with remediation owners, all 6 SCs marked PASS/FAIL with evidence, follow-on items for Phase 10"
    - "Source audit: confirm 18 LOCKED decisions (D-01..D-18) from CONTEXT.md each have a plan implementation reference"
    - "Final commit: single commit per memory `feedback_parallel_executor_git_isolation.md` (Wave 4 sequential — no parallelism risk)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 9 plans list + Phase 9 status + Phase 10 scope note verification"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability + per-phase summary tables reflecting CLINIC-06 + CLINIC-07 split"
    - path: ".planning/STATE.md"
      provides: "Phase 9 close marker + new decision rows + accumulated context update"
    - path: ".planning/phases/09-clinic-b2b-foundations/09-VALIDATION.md"
      provides: "status=complete (Plan 09-01 already set wave_0_complete+nyquist_compliant per B-5) + filled per-task verification map"
    - path: ".planning/phases/09-clinic-b2b-foundations/09-SUMMARY.md"
      provides: "Phase 9 close summary + 6 SC PASS/FAIL evidence + deferred items"
      min_lines: 150
  key_links:
    - from: ".planning/ROADMAP.md Phase 9"
      to: ".planning/phases/09-clinic-b2b-foundations/09-SUMMARY.md"
      via: "phase close reference"
      pattern: "09-SUMMARY"
    - from: ".planning/REQUIREMENTS.md"
      to: "REQUIREMENTS Phase 9 row"
      via: "traceability table update"
      pattern: "CLINIC-01|CLINIC-02|CLINIC-03|CLINIC-06|CLINIC-07"
---

<objective>
Close Phase 9 with a clean docs sweep: ROADMAP / REQUIREMENTS / STATE updates + 09-VALIDATION.md status flip (status only — wave_0_complete and nyquist_compliant were flipped by Plan 09-01 Task 2 per B-5) + 09-SUMMARY.md phase close document. Verify all 6 SCs satisfied (or document deferred items). No code changes — pure docs sync.

Purpose: 09-CONTEXT.md Deferred → Follow-ups required BEFORE plan-phase (lines 226-229) flagged that ROADMAP/REQUIREMENTS need updating for the CLINIC-06 scope expansion. That update was deferred to this close plan instead of doing it mid-phase (per memory `feedback_addendum_pattern_for_mid_execution_pivots.md`). B-5 ownership rule (plan-checker iter 1): VALIDATION.md frontmatter `wave_0_complete` + `nyquist_compliant` flags are owned by Plan 09-01 Task 2 (flipped right after `supabase db push` succeeds — the moment Wave-0 scaffolds are actually present in the repo + migration tree). Plan 09-11 only flips `status: complete`.

Output: ROADMAP/REQUIREMENTS/STATE reflect Phase 9 complete + Phase 10 scope shrink; 09-VALIDATION.md per-task verification map is filled with real status + status=complete; 09-SUMMARY.md aggregates per-plan results; STATE.md decisions log carries the new architectural primitives forward.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-clinic-b2b-foundations/09-CONTEXT.md
@.planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md
@.planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md
@.planning/phases/09-clinic-b2b-foundations/09-VALIDATION.md
@.planning/phases/09-clinic-b2b-foundations/09-01-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-02-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-03-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-04-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-05-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-06-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-07-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-08-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-09-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-10-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ROADMAP + REQUIREMENTS + STATE updates + 09-VALIDATION.md status flip + 09-SUMMARY.md draft</name>
  <read_first>
    - .planning/ROADMAP.md (current Phase 9 + Phase 10 entries)
    - .planning/REQUIREMENTS.md (current traceability table + per-phase summary)
    - .planning/STATE.md (current accumulated context decisions list)
    - .planning/phases/09-clinic-b2b-foundations/09-VALIDATION.md (current frontmatter + per-task map + sign-off list) — verify that wave_0_complete + nyquist_compliant are already true (Plan 09-01 flipped them per B-5)
    - All 10 per-plan SUMMARY.md files (09-01-SUMMARY.md through 09-10-SUMMARY.md)
    - .planning/phases/08-doctor-read-share/08-SUMMARY.md (if it exists — reference for phase-summary structure)
  </read_first>
  <action>
    **Step 0 — B-5 pre-flight check (verify Plan 09-01 already flipped the flags):**

    Read 09-VALIDATION.md frontmatter and confirm:
    - `wave_0_complete: true` (flipped by Plan 09-01 Task 2 after db push succeeded)
    - `nyquist_compliant: true` (flipped by Plan 09-01 Task 2)

    If EITHER is still `false`, abort and surface the gap to the orchestrator — Plan 09-01 Task 2 did not complete the post-push flip step. Do not attempt to flip them here; that would mask a Wave-1 regression.

    **Step 1 — ROADMAP.md updates:**

    For Phase 9 entry:
    - `**Plans:** 11 plans` (currently TBD).
    - Plans list (after the **Plans** line):
      ```markdown
      - [x] 09-01-PLAN.md — Schema foundation: 13 migrations + 16 RPCs + has_permission + broadcast trigger + Pitfall #8 RLS proofs + Wave 0 scaffolds + App.tsx routing + 4 stub files + VALIDATION flag flip (Wave 1)
      - [x] 09-02-PLAN.md — Clinic chunk UI: ClinicWorkspace + ClinicContextBar + OrgCreateFlow + InvitePatientModal + clinic.ts wrappers + clinic-realtime.ts (Wave 2)
      - [x] 09-03-PLAN.md — Clinic-settings UI: Workspace + Members + Roles tabs + RoleEditorModal + clinic-permissions hook (Wave 2)
      - [x] 09-04-PLAN.md — Clinic-invite UI: ClinicInvitePage states A-H + ConsentDialog (W-5 defensive scope-init) + InviteSignupForm (Wave 2)
      - [x] 09-05-PLAN.md — Patient-side Active organizations tab + EditConsentScopeModal (Wave 2)
      - [x] 09-06-PLAN.md — clinic-invite Edge Function (4 endpoints, W-1 invite_id response) + Resend HTTPS dispatch + Vercel rewrites (Wave 3) [HUMAN CHECKPOINT]
      - [x] 09-07-PLAN.md — clinic-photo Edge Function + D-12 3-check gate + 5-min signed URL TTL (Wave 3)
      - [x] 09-08-PLAN.md — WorkspaceSwitcher (index chunk, +3 kB W-7 byte-level gate) + AppShell mount + ClinicContextBar real-import (Wave 3)
      - [x] 09-09-PLAN.md — Pitfall #8 5-scenario matrix + clinic-photo-access + clinic-role-permission-grid e2e (W-4 actual-RLS-enforcement) (Wave 4)
      - [x] 09-10-PLAN.md — SC#5 revoke-latency drill + Pitfall #2 realtime.messages RLS negative-space test (Wave 4)
      - [x] 09-11-PLAN.md — Traceability sweep + ROADMAP/REQUIREMENTS/STATE sync + 09-SUMMARY.md (Wave 4)
      ```
    - Update Progress table at the bottom: Phase 9 row → `11/11 | Complete | <today's date>`.

    For Phase 10 entry: verify the Scope note correctly states CLINIC-06 absorbed by Phase 9 + CLINIC-07 split.

    Mark uncompleted plans as `- [ ]` instead of `- [x]` IF the execute orchestrator reports failures or deferred plans.

    **Step 2 — REQUIREMENTS.md updates:**

    Traceability table:
    - CLINIC-01: Phase 9 → Status Complete
    - CLINIC-02: Phase 9 → Status Complete
    - CLINIC-03: Phase 9 → Status Complete
    - CLINIC-06: Phase 9 → Status Complete (preserve `moved 2026-05-12 per 09-CONTEXT.md D-07` note)
    - CLINIC-07: Phase 9 (capture half) + Phase 10 (surface UI half)

    Per-phase summary:
    - Phase 9 row count: 4 (CLINIC-01, CLINIC-02, CLINIC-03, CLINIC-06; + CLINIC-07 capture footnoted).
    - Phase 10 row: 3 (CLINIC-04, CLINIC-05, CLINIC-07 surface UI half).
    - Verify count math.

    Footer last-updated line: append note `*Last updated: <today's date> — Phase 9 close: CLINIC-01..03 + CLINIC-06 marked complete; CLINIC-07 split into capture (Phase 9) + surface UI (Phase 10) halves.*`.

    **Step 3 — STATE.md updates:**

    Current Position:
    - Phase: 09 (clinic-b2b-foundations) — COMPLETE OR IN PROGRESS based on actual state
    - Plan: 11 of 11
    - Status: Complete
    - Last activity: <today's date>

    Accumulated Context → Decisions: append new rows:
    - `[Phase 9]: has_permission(user_id, org_id, permission_key) SECURITY DEFINER STABLE helper is the SINGLE RLS dispatch primitive for clinic-scoped tables; all RLS policies on memberships/roles/role_permissions/realtime.messages/storage delegate to it.`
    - `[Phase 9]: Realtime cross-tenant subscribe uses private channels + realtime.broadcast_changes trigger + realtime.messages RLS — supersedes postgres_changes for any cross-tenant subscription.`
    - `[Phase 9]: org-deletion cascades require BOTH app.suppress_audit GUC + storage.allow_delete_query GUC. Applied preventively in delete_org RPC.`
    - `[Phase 9]: D-13 accepted tradeoff: clinic-photo signed URLs have 5-min TTL; revoke kills NEW mints immediately but existing URLs stay valid until natural TTL expiry.`
    - `[Phase 9]: Pitfall #11 invariant: clinic-* Edge Functions use JWT-or-token-in-URL auth; NO cookies; CORS allow * Origin acceptable.`
    - `[Phase 9]: Wave-0 ownership rule (B-5): the plan that creates Wave-0 scaffolds (App.tsx routing + stub files + RLS proofs + migration push) ALSO flips wave_0_complete + nyquist_compliant in VALIDATION.md immediately after the scaffolds are verified present. Phase-close plan only flips status: complete.`

    Accumulated Context → Blockers/Concerns: append any deferred items from Plans 09-09/09-10 (RC5 cluster fixme'd specs, etc.).

    Progress block: update completed_phases counter + percent.

    Session Continuity: update with Phase 9 close marker.

    **Step 4 — 09-VALIDATION.md updates (B-5 narrowed scope):**

    Frontmatter flip in THIS plan:
    - `status: draft` → `status: complete` (the phase-close marker — Plan 09-11 owns this).

    DO NOT touch `wave_0_complete` or `nyquist_compliant` — these were flipped by Plan 09-01 Task 2 immediately after `supabase db push` succeeded (B-5 ownership rule per plan-checker iter 1). Step 0 above verifies they are already true; if they aren't, abort.

    Per-Task Verification Map (lines 42-54): replace placeholder rows with concrete plan IDs:
    ```markdown
    | 09-01-01 | 01 schema | 1 | CLINIC-01..03+06+07 (schema gate) | T-09-01..07 | RLS surfaces enforce cross-tenant isolation; has_permission STABLE | RLS unit + Postgres unit | `npm run test:e2e:rls -- 'rls-(orgs|memberships|invites|roles|role-permissions|org-logos-storage)\\.test\\.ts'` | ✅ exists | ✅ green |
    | 09-02-01 | 02 clinic chunk UI | 2 | CLINIC-01, CLINIC-02 | T-09-13..17 | Org-create + workspace home + invite modal anti-enumeration + W-1 invite_id | RTL + bundle-size | `npm run test -- --run src/components/clinic/` | ✅ exists | ✅ green |
    | ... (one row per plan, with W-4/W-5/W-7 fixes called out where applicable) ... |
    ```

    Sign-Off checklist: mark each box ✅ or ❌ with one-line evidence.

    **Step 5 — 09-SUMMARY.md (NEW) — phase close document:**

    Use this structure:
    ```markdown
    # Phase 9 Close Summary

    **Closed:** <today's date>
    **Status:** Complete | Complete with deferred items | Blocked

    ## Deliverables

    | Plan | Wave | Deliverable |
    |------|------|-------------|
    | 09-01 | 1 | 13 migrations + 16 RPCs + has_permission helper + broadcast trigger + 6 RLS proofs + Wave 0 scaffolds + App.tsx routing + 4 stubs + VALIDATION flag flip |
    | 09-02 | 2 | ClinicWorkspace + ClinicContextBar + OrgCreateFlow + InvitePatientModal + clinic.ts/clinic-realtime.ts wrappers (W-1 invite_id) |
    | ... | | |

    ## Success Criteria Verification

    | SC | Description | Evidence | Status |
    |----|-------------|----------|--------|
    | SC#1 | Operator signs up → creates org → workspace URL + clinic-context bar | Plan 09-02 + Plan 09-09 | ✅ |
    | SC#2 | Operator invites patient → email → consent → accept → roster | Plan 09-06 + Plan 09-04 + Plan 09-09 (a)+(b) | ✅ |
    | SC#3 | Pitfall #8 5-scenario matrix passes | Plan 09-09 e2e specs | ✅ or ⚠️ if deferred |
    | SC#4 | Existing-user invited → personal data private + accept → roster but no aiHistory | Plan 09-04 explicit-exclusion + Plan 09-01 RLS | ✅ |
    | SC#5 | Patient revokes → operator roster within 1s + drill-in 401 | Plan 09-10 Layer 1 + Layer 2 | ✅ or ⚠️ |
    | SC#6 | Owner creates custom role → assigns → RLS enforces | Plan 09-03 + Plan 09-09 Test 3a (W-4 actual RLS) | ✅ |

    ## Plan-Checker Iteration 1 Fixes Applied

    | Fix | Plans Affected | Resolution |
    |-----|----------------|-----------|
    | B-1 | 09-10 | depends_on adds 09-09 (fixture dependency) |
    | B-2 | 09-01 (owner), 09-02/03/04/05 (no App.tsx) | App.tsx + 4 stubs consolidated into Plan 09-01 Wave-0 scaffolds |
    | B-3 | 09-01/05/07/09/10 | CLINIC-07 added to requirements of all audit-row-writing plans |
    | B-4 | 09-01 (owner), 09-03 (consumer) | list_org_members RPC included as 16th RPC in Plan 09-01 migration 11 |
    | B-5 | 09-01 (owner), 09-11 (verifier) | wave_0_complete + nyquist_compliant flipped by Plan 09-01 Task 2; status: complete flipped by Plan 09-11 |
    | W-1 | 09-02, 09-06 | sendInvite response shape locked to `{ok: true, invite_id}` (no enumeration leak) |
    | W-2 | 09-01 | Task 1 split into 1a (migrations 1-9) + 1b (migrations 10-13 + scaffolds + routing) |
    | W-3 | 09-06, 09-07 | implementation-detail truths demoted to <implementation_notes> / <non_regression> blocks |
    | W-4 | 09-09 | Test 3a added — actual RLS rejection (42501) verifies the security gate, not just has_permission helper |
    | W-5 | 09-04, 09-05 | ConsentDialog + EditConsentScopeModal build scope state defensively via DATA_TYPE_KEYS.reduce |
    | W-6 | 09-01 | Task 2 verify prose includes worktree gotcha note |
    | W-7 | 09-08 | Inline byte-level bundle-size gate in <verify> (24.5 kB gz / 79 kB raw ceiling) |

    ## Key Architectural Decisions

    1. has_permission(user_id, org_id, permission_key) SECURITY DEFINER STABLE — single RLS dispatch primitive.
    2. realtime.broadcast_changes + realtime.messages RLS — cross-tenant Realtime pattern.
    3. Custom invites table + hashed token + 7-day expiry — D-01 (mirrors Phase 8 share).
    4. ConsentScope: 10 canonical keys with DATA_TYPE_KEYS.reduce(...) defensive init at every consumer (W-5).
    5. 5-min signed URL TTL for clinic-photo — D-13 accepted tradeoff.
    6. D-02 anti-enumeration: universal `{ok: true, invite_id}` response shape at server + universal Invitation sent copy at UI (W-1).
    7. Path-based routing for /clinic/* and /clinic-invite/* via Vercel rewrites + App.tsx selectView extensions consolidated in Plan 09-01 (B-2).
    8. 3 lazy chunks + 1 index delta: clinic ≤12 kB, clinic-settings ≤14 kB, clinic-invite ≤6 kB, index ≤24.5 kB (Phase 9 ceiling) / 50 kB absolute (W-7 byte-level gate).
    9. Wave-0 ownership rule: the plan that creates scaffolds owns the VALIDATION.md flag flip (B-5).
    10. Helper-function unit tests are not load-bearing for RLS security — actual RPC denial paths are (W-4).

    ## Deviations from RESEARCH.md / UI-SPEC.md

    (List each deviation with rationale.)

    ## Deferred Items

    | Item | Owner | Re-enable condition |
    |------|-------|---------------------|
    | (e.g., RC5-blocked revoke-latency Layer 1) | Phase 10 entry | Supabase Realtime broadcast jitter fix |
    | BAA/HIPAA disclosure copy review | Counsel | Phase 10 close or v1 launch gate |
    | Operator-offboarding semantics | Phase 10 | gray area; default-routed per CONTEXT.md |

    ## Follow-on Items for Phase 10

    - Roster ranking via rankPatients(orgState) — CLINIC-04 main deliverable
    - Drill-in via SHARE-02 component reuse — CLINIC-05 main deliverable
    - Operator audit-log surface UI (org-owner-facing) — CLINIC-07 second half
    - Any RC5 deferred specs re-enable + remediation

    ## Bundle Size Final

    | Chunk | Budget | Actual | Delta vs Phase 6 close |
    |-------|--------|--------|-------------------------|
    | index | ≤24.5 kB gz / 50 kB absolute | <actual> | +<x> kB (was 21.49 kB) |
    | clinic | ≤12 kB | <actual> | net-new |
    | clinic-settings | ≤14 kB | <actual> | net-new |
    | clinic-invite | ≤6 kB | <actual> | net-new |
    ```

    Use the actual measurements + status from running `npm run build && node scripts/bundle-size-check.cjs`.

    **Step 6 — source audit (CONTEXT D-01..D-18 traceability):**

    For each locked decision D-XX, identify which plan implements it. Format as a table appendix in 09-SUMMARY.md:
    | Decision | Implementing Plan | Evidence |
    |----------|-------------------|----------|
    | D-01 (custom invites + hashed token) | 09-01 (RPCs + schema) + 09-06 (Edge Function /send) | invites table + send_invite RPC + token hashing in handleSend |
    | D-02 (anti-enumeration) | 09-02 InvitePatientModal universal copy + 09-06 /send universal `{ok: true, invite_id}` (W-1) + Plan 09-09 Test 5 | grep tests + Pitfall #8 (a) anti-enumeration assertion |
    | ... |

    Confirm all 18 decisions are accounted for.

    **Step 7 — commit:**

    Single commit covering all docs touches. Use pathspec:
    ```
    git commit -- .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/phases/09-clinic-b2b-foundations/09-VALIDATION.md .planning/phases/09-clinic-b2b-foundations/09-SUMMARY.md
    ```
    Commit message:
    ```
    docs(09): Phase 9 close — ROADMAP/REQUIREMENTS/STATE sync + 09-SUMMARY.md + VALIDATION.md status flip

    - ROADMAP Phase 9: 11/11 plans complete; Phase 10 scope verified
    - REQUIREMENTS traceability: CLINIC-01..03+06 marked Complete
    - STATE accumulated context: 6 new Phase 9 architectural decisions (incl. B-5 ownership rule)
    - VALIDATION.md: status=complete (wave_0_complete + nyquist_compliant flipped by Plan 09-01 per B-5); per-task map filled
    - 09-SUMMARY.md: 6 SCs verified; plan-checker iter 1 fixes documented; deferred items + Phase 10 follow-ons recorded
    ```
  </action>
  <verify>
    <automated>node -e "const {readFileSync} = require('fs'); const roadmap = readFileSync('.planning/ROADMAP.md', 'utf8'); if (!roadmap.match(/09-11-PLAN\.md/)) { console.error('Phase 9 plans list not updated'); process.exit(1); } const reqs = readFileSync('.planning/REQUIREMENTS.md', 'utf8'); if (!reqs.match(/CLINIC-06.*Phase 9.*Complete/i)) { console.error('REQUIREMENTS CLINIC-06 not marked Complete'); process.exit(1); } const validation = readFileSync('.planning/phases/09-clinic-b2b-foundations/09-VALIDATION.md', 'utf8'); if (!validation.match(/wave_0_complete:\s*true/)) { console.error('B-5 pre-flight: wave_0_complete not true — Plan 09-01 Task 2 did not complete its flip step'); process.exit(1); } if (!validation.match(/nyquist_compliant:\s*true/)) { console.error('B-5 pre-flight: nyquist_compliant not true — Plan 09-01 Task 2 did not complete its flip step'); process.exit(1); } if (!validation.match(/status:\s*complete/)) { console.error('Plan 09-11 did not flip status: complete'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <done>
    - ROADMAP Phase 9 has 11 plan rows; status Complete.
    - REQUIREMENTS traceability marks all 4 CLINIC reqs Complete.
    - STATE has 6 new Phase 9 decisions in accumulated context (incl. B-5 ownership rule).
    - 09-VALIDATION.md status flipped to `complete` by THIS plan; `wave_0_complete` + `nyquist_compliant` verified-already-true from Plan 09-01 Task 2 (B-5).
    - 09-SUMMARY.md exists with 6 SC verifications + plan-checker iter 1 fixes table + 18 D-XX traceability + bundle-size final + Phase 10 follow-ons.
    - Single docs commit pushed.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

(Docs-only plan — no new trust boundaries or threats.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-52 | Repudiation | docs drift between SUMMARY claims and actual deliverables | mitigate | 09-SUMMARY.md sources data from per-plan SUMMARYs + actual bundle-size + actual e2e green-state; source audit table covers all 18 D-XX decisions |
| T-09-53 | Tampering | this plan masks a Wave-1 regression by flipping wave_0_complete/nyquist_compliant late | mitigate | B-5 ownership rule: Plan 09-01 Task 2 owns those two flips (right after scaffolds verified present). Plan 09-11 Step 0 verifies the flags are already true and ABORTS if not — never re-flips. |
</threat_model>

<verification>
- Traceability table covers all 4 CLINIC requirement IDs + CLINIC-07 capture half.
- All 18 D-XX decisions mapped to implementing plans.
- 6 SCs explicitly marked PASS or DEFERRED with evidence.
- Bundle-size final measurements match `npm run build` output.
- 09-VALIDATION.md `wave_0_complete: true` AND `nyquist_compliant: true` were already set by Plan 09-01 Task 2; THIS plan only flips `status: complete`.
</verification>

<success_criteria>
1. ROADMAP Phase 9 entry reflects 11/11 plans complete.
2. REQUIREMENTS traceability table updated for CLINIC-06 + CLINIC-07 split.
3. STATE.md decisions log carries 6 new Phase 9 primitives (incl. B-5 ownership rule).
4. 09-VALIDATION.md `status: complete` flipped here; pre-existing `wave_0_complete: true` + `nyquist_compliant: true` (from Plan 09-01) verified.
5. 09-SUMMARY.md exists with full phase close package + plan-checker iter 1 fixes table.
6. Single commit covering all 5 docs touches.
</success_criteria>

<output>
After completion, the Phase 9 close package (09-SUMMARY.md) is the canonical reference for `/gsd-verify-work 9` and any downstream Phase 10 plan-phase invocations.
</output>
