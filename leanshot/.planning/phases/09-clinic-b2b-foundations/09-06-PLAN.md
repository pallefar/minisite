---
phase: 09-clinic-b2b-foundations
plan: 06
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-04"]
files_modified:
  - supabase/functions/clinic-invite/index.ts
  - supabase/functions/clinic-invite/index.test.ts
  - supabase/functions/clinic-invite/resend.ts
  - supabase/functions/clinic-invite/template-clinic-invite.ts
  - supabase/functions/clinic-invite/rate-limit.ts
  - vercel.json
autonomous: false  # HUMAN CHECKPOINTS: Resend account + DNS + RESEND_API_KEY + Vercel rewrites
requirements: [CLINIC-02, CLINIC-03]
mvp_slice: "Backend slice for invite flow — clinic-invite Edge Function ships 4 endpoints (/send, /lookup, /accept, /reject) on a single Deno function. Resend wired for branded invite emails. Vercel rewrites configured for path-based /clinic/* and /clinic-invite/* routes. After Plan 09-06, the operator can fire a REAL invite from Plan 09-02's modal; the patient receives a REAL Resend email; clicking lands on Plan 09-04's ClinicInvitePage; the entire D-16 state machine resolves against real data. SC#2 end-to-end demoable."

must_haves:
  truths:
    - "Edge Function `supabase/functions/clinic-invite/index.ts` serves 4 endpoints distinguished by URL path: /send (POST, JWT-authed), /lookup (GET, anon-OR-JWT), /accept (POST, JWT-authed post-signup), /reject (POST, JWT-authed) — single Deno.serve dispatching by `new URL(req.url).pathname`"
    - "/send: JWT-validates operator → rate-limit `clinic_invite_send:{operator_uid}` 20/hour → validates has_permission(uid, p_org_id, 'members.invite') via supabase.rpc → generates 16-byte CSPRNG token via `crypto.getRandomValues` → sha256-hashes via `crypto.subtle.digest` → calls `send_invite` RPC (Plan 09-01) → dispatches Resend email with raw token in URL → returns universal 200 `{ ok: true, invite_id: '<uuid>' }` regardless of email existence (D-02 anti-enumeration + W-1 fix per plan-checker iter 1 — invite_id is generated server-side regardless of whether email exists in auth.users; operator UI uses invite_id for the pending-invites list)"
    - "/lookup: hashes incoming token → SELECTs invites (joined with orgs for org name + logo) → branches state per Plan 09-04 LookupResponse contract → returns LookupResponse JSON. Rate-limit 10/min/IP (Pitfall #12 brute-force defense)"
    - "/accept: JWT-validates patient → hashes token → calls accept_invite_existing OR accept_invite_new RPC (Plan 09-01). Returns 200 with {membership_id, org_id} OR 4xx with error code"
    - "/reject: JWT-validates patient → hashes token → calls reject_invite RPC → returns 200"
    - "Email template (`template-clinic-invite.ts`) inlines HTML using copy from UI-SPEC §'Patient-side: Invitation email' lines 364-381 verbatim. Org logo URL via public bucket. WMHMDA footer carry-forward from Phase 7 transactional template"
    - "Rate-limit helper wraps Phase 4's `increment_rate_limit` RPC with clinic-specific keys: `clinic_invite_send:{operator_uid}` (20/hour), `clinic_invite_lookup:{ip_or_token}` (10/min), `clinic_invite_accept:{invite_id}` (5/min)"
    - "Deno tests at `supabase/functions/clinic-invite/index.test.ts` cover all 4 endpoint handlers: happy paths + error branches + D-02 anti-enumeration assertion + W-1 invite_id response shape"
    - "Vercel rewrites: `vercel.json` adds rules `{ source: '/clinic/(.*)', destination: '/index.html' }` and `{ source: '/clinic-invite/(.*)', destination: '/index.html' }`"
    - "Plan 09-02's InvitePatientModal sendInvite stub is replaced with a real Edge Function call (`fetch('${VITE_SUPABASE_URL}/functions/v1/clinic-invite/send', ...)`)"
    - "Plan 09-04's ClinicInvitePage lookup call hits the real /clinic-invite/lookup endpoint and renders against real LookupResponse"
  artifacts:
    - path: "supabase/functions/clinic-invite/index.ts"
      provides: "Deno.serve dispatching /send + /lookup + /accept + /reject"
      min_lines: 250
    - path: "supabase/functions/clinic-invite/resend.ts"
      provides: "HTTPS fetch wrapper to api.resend.com/emails with test-stub gate"
      min_lines: 60
    - path: "supabase/functions/clinic-invite/template-clinic-invite.ts"
      provides: "HTML email template — inlined CSS, branded, WMHMDA footer"
      min_lines: 80
    - path: "supabase/functions/clinic-invite/rate-limit.ts"
      provides: "Wraps Phase 4 increment_rate_limit RPC with clinic-specific keys"
      min_lines: 40
    - path: "supabase/functions/clinic-invite/index.test.ts"
      provides: "Deno tests for all 4 endpoint handlers"
      min_lines: 200
    - path: "vercel.json"
      provides: "Path rewrites for /clinic/* and /clinic-invite/* SPA routing"
      contains: "rewrites"
  key_links:
    - from: "supabase/functions/clinic-invite/index.ts"
      to: "send_invite + accept_invite_existing + reject_invite RPCs (Plan 09-01)"
      via: "supabase.rpc calls via service-role admin client"
      pattern: "admin\\.rpc\\('(send_invite|accept_invite_existing|reject_invite)'"
    - from: "supabase/functions/clinic-invite/resend.ts"
      to: "Resend API (api.resend.com/emails)"
      via: "HTTPS POST + Authorization Bearer"
      pattern: "api\\.resend\\.com/emails"
    - from: "src/lib/clinic.ts (sendInvite)"
      to: "/functions/v1/clinic-invite/send"
      via: "fetch wrapper replacing the supabase.rpc('send_invite') stub from Plan 09-02; response shape `{ok: true, invite_id}` (W-1)"
      pattern: "/functions/v1/clinic-invite/send"
---

<objective>
Wire the clinic-invite backend: Edge Function with 4 endpoints, Resend dispatch, rate-limiting, Vercel path-based routing. Replace the Plan 09-02 sendInvite stub + Plan 09-04 lookup stub with real Edge Function calls. After Plan 09-06, an operator can fire a REAL invite, the patient receives a REAL email, and the full accept flow completes against real data.

Purpose: D-16 (Resend handles branded invite email; Supabase Auth handles signup verification leg) is the dual-email design implemented here. D-17 (7-day expiry) is enforced by send_invite RPC (Plan 09-01); /lookup branches to State E if `now() > invites.expires_at`. D-02 (anti-enumeration on /send) is the load-bearing UI/server invariant — W-1 fix per plan-checker iter 1 locks the response shape to `{ok: true, invite_id: '<uuid>'}` (invite_id is generated server-side regardless of email existence, so no enumeration leak). Pitfall #7 (Resend free-tier) is mitigated by CI stub. Pitfall #11 (cookie-attach quirk does NOT recur in Phase 9) is enforced by CORS allow `*` + no Set-Cookie ever issued.

Output: Plan 09-02 InvitePatientModal fires real invites; Plan 09-04 ClinicInvitePage resolves against real LookupResponse; Resend dispatches via HTTPS only (no SDK); Vercel routes `/clinic/*` + `/clinic-invite/*` to SPA. HUMAN CHECKPOINT for Resend account + DNS + secret.
</objective>

<implementation_notes>
<!-- W-3 demoted from must_haves.truths: implementation details, not verification-derived behaviors -->
- Resend dispatch uses HTTPS only (NO SDK in repo — keep bundle clean; helper at `resend.ts` POSTs to `https://api.resend.com/emails`).
- RESEND_API_KEY lives in Deno.env (server-only); never reaches the browser.
- CI stub: if `RESEND_API_KEY === 'test-stub'`, skip actual HTTPS dispatch and return ok (Pitfall #7 free-tier mitigation).
- CORS: echo-Origin, NO credentials (matches `supabase/functions/ai-chat/cors.ts` verbatim per Pitfall #11) — Phase 9 functions are JWT-authed via Authorization header; no cookies.
- Pitfall #11 invariant: CORS allow `*` Origin OK — no cookies issued or read by clinic-invite Edge Function. See threat T-09-38.
- Pitfall #1 collapse: per Plan 09-01 RPC notes, accept_invite_existing and accept_invite_new have identical bodies (UNIQUE(user_id, org_id) is the single-identity floor). Plan 09-06 always calls accept_invite_existing for simplicity.
</implementation_notes>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-clinic-b2b-foundations/09-CONTEXT.md
@.planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md
@.planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md
@.planning/phases/09-clinic-b2b-foundations/09-01-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-02-PLAN.md
@.planning/phases/09-clinic-b2b-foundations/09-04-PLAN.md
@supabase/functions/ai-chat/index.ts
@supabase/functions/ai-chat/cors.ts
@supabase/functions/ai-chat/rate-limit.ts
@src/lib/clinic.ts
@src/components/clinic-invite/ClinicInvitePage.tsx
</context>

<user_setup>
  - service: resend
    why: "Branded transactional invite emails per D-16 (Supabase Auth signup leg is separate)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API Keys (create a 'Phase 9 clinic-invite' key); set as Supabase Edge Function secret via `supabase secrets set RESEND_API_KEY=...`"
    dashboard_config:
      - task: "Create Resend account at https://resend.com/signup"
        location: "Resend signup flow"
      - task: "Verify domain app.leanshot.app — add SPF + DKIM TXT records to DNS"
        location: "Resend → Domains → app.leanshot.app → DNS Records"
      - task: "Confirm sender `noreply@app.leanshot.app` works"
        location: "Resend Dashboard"
  - service: vercel
    why: "Path-based /clinic/* and /clinic-invite/* SPA rewrites (RESEARCH §Open Question #1)"
    dashboard_config:
      - task: "Verify vercel.json rewrites land on the PR preview"
        location: "Vercel preview deployment"
</user_setup>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Edge Function index.ts (4 endpoints) + resend.ts + template + rate-limit + Deno tests</name>
  <read_first>
    - supabase/functions/ai-chat/index.ts (Edge Function template — Deno.serve, auth header parsing, CORS, structured response)
    - supabase/functions/ai-chat/cors.ts (verbatim copy target — Pitfall #11 invariant)
    - supabase/functions/ai-chat/rate-limit.ts (increment_rate_limit RPC wrapper — extend with clinic keys)
    - .planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md §"Code Examples" `clinic-invite handleSend` lines 419-466 + `handleLookup` lines 477-510
    - .planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md §"Resend HTTPS dispatch" lines 1251-1288
    - .planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md §"Patient-side: Invitation email" lines 364-381 (template copy)
    - memory `reference_deno_test_discovery.md` (.test.ts suffix convention)
  </read_first>
  <behavior>
    - Test 1 (/send happy): operator JWT + valid email + valid scope → 200 + side-effect: invites row inserted + Resend mock called.
    - Test 2 (/send rate-limited): 21st request in an hour → 429 + body `{error:'rate_limited'}`.
    - Test 3 (/send permission denied): operator without `members.invite` permission → 403 + body `{error:'forbidden'}`.
    - Test 4 (/send unauthenticated): no Authorization header → 401.
    - Test 5 (W-1 + D-02 anti-enumeration response shape): /send with `email='nonexistent@example.com'` and /send with `email='existing@example.com'` BOTH return 200 with body `{ ok: true, invite_id: '<uuid>' }`. The invite_id values differ (each call inserts a fresh invites row) but neither response reveals whether the email matched an auth.users row. Operator UI consumes invite_id to add the row to its pending-invites list.
    - Test 6 (/lookup not-found): token doesn't hash to any invites row → `{state: 'not_found'}` 200.
    - Test 7 (/lookup expired): expires_at in past → `{state: 'expired'}` 200.
    - Test 8 (/lookup already-used): accepted_at IS NOT NULL → `{state: 'already_used'}`.
    - Test 9 (/lookup valid_logged_in): valid invite + Authorization header w/ matching email → `{state: 'valid_logged_in', invite: {...}}`.
    - Test 10 (/lookup valid_logged_out_existing): valid + no Authorization + admin.listUsers shows email exists → `{state: 'valid_logged_out_existing', invite: {...}}`.
    - Test 11 (/lookup valid_new_user): valid + no Authorization + admin.listUsers returns empty → `{state: 'valid_new_user', invite: {...}}`.
    - Test 12 (/lookup rate-limit): 11th lookup in 60s from same IP → 429.
    - Test 13 (/accept happy): patient JWT + valid token + consent_scope → 200 + accept_invite_existing called + membership created.
    - Test 14 (/accept email mismatch): patient JWT email ≠ invites.email → 403 + body `{error:'email_mismatch'}`.
    - Test 15 (/accept invalid scope): consent_scope missing required keys OR extra keys → 400 + body `{error:'invalid_scope'}` (RPC validation; Pitfall #8).
    - Test 16 (/reject happy): patient JWT + valid token → 200; reject_invite RPC called.
    - Test 17 (CORS allow `*` Origin no credentials): OPTIONS preflight returns `Access-Control-Allow-Origin: *` and NO `Access-Control-Allow-Credentials` header (Pitfall #11 invariant).
    - Test 18 (Resend CI stub): RESEND_API_KEY='test-stub' → no HTTPS call to api.resend.com; returns ok.
    - Test 19 (Resend network failure): Resend returns 502 → /send still returns 200 (don't block the operator on email-provider hiccups). Audit_logs row is still written.
  </behavior>
  <action>
    Author Edge Function files:

    **`supabase/functions/clinic-invite/cors.ts`** — verbatim copy of `supabase/functions/ai-chat/cors.ts`. Comment header: `// CORS: allow * Origin; NO credentials. Phase 9 Edge Functions are JWT-authed via Authorization header; no cookies issued or read. Pitfall #11 invariant.`

    **`supabase/functions/clinic-invite/rate-limit.ts`** — wraps Phase 4's `increment_rate_limit` RPC.

    **`supabase/functions/clinic-invite/template-clinic-invite.ts`** — exports `renderInviteHtml(params: {orgName, orgLogoUrl, operatorFirstName, inviteUrl, expiresAt}): string`. Inlined HTML with inline CSS. Copy from UI-SPEC §"Patient-side: Invitation email" lines 364-381 verbatim.

    **`supabase/functions/clinic-invite/resend.ts`** — example at 09-RESEARCH.md lines 1251-1288 verbatim. Test-stub gate: `if (RESEND_API_KEY === 'test-stub') return { ok: true };`.

    **`supabase/functions/clinic-invite/index.ts`** — main dispatcher per RESEARCH §"Code Examples" lines 419-510 verbatim.

    `handleSend` (W-1 response shape lock): after `admin.rpc('send_invite', {...})` returns `[{invite_id}]`, ALWAYS respond with `{ok: true, invite_id}`. Do NOT branch on whether `admin.auth.admin.listUsers({email})` finds a row — that check is deferred to /accept-time. D-02 anti-enumeration invariant: both real and fake emails get the same response shape with a fresh invite_id.

    `handleLookup`: parse `token` from query string → hash → `admin.from('invites').select('*, orgs(*)').eq('invite_token_hash', tokenHash).maybeSingle()` → branch state per `<behavior>` Test 6-11.

    `handleAccept`: extract JWT → admin.auth.getUser → hash incoming token → `admin.rpc('accept_invite_existing', {...})`. Per Pitfall #1 collapse note: always call accept_invite_existing (the _new variant has identical body).

    `handleReject`: calls `reject_invite` RPC.

    **`supabase/functions/clinic-invite/index.test.ts`** — Deno tests per Test 1-19 above. Tests use `.test.ts` suffix per memory.

    **`vercel.json`** at repo root: extend (do NOT overwrite) existing rewrites:
    ```json
    {
      "rewrites": [
        { "source": "/clinic/(.*)", "destination": "/index.html" },
        { "source": "/clinic-invite/(.*)", "destination": "/index.html" }
      ]
    }
    ```

    **Patch `src/lib/clinic.ts`:** Replace the `sendInvite` wrapper body to `fetch('${import.meta.env.VITE_SUPABASE_URL}/functions/v1/clinic-invite/send', {method:'POST', headers:{...auth}, body:JSON.stringify({org_id, email, requested_scope})})`. Parse the W-1 response shape `{ok: true, invite_id}` into the discriminated-union `{ok: true, data: {invite_id}}`. Same fetch refactor for `acceptInviteExisting`, `acceptInviteNew`, `rejectInvite`.

    Deploy Edge Function: `supabase functions deploy clinic-invite --project-ref ytnsipxxmzgaebkqmokp`.

    Commit with pathspec `git commit -- supabase/functions/clinic-invite/ vercel.json src/lib/clinic.ts`.
  </action>
  <verify>
    <automated>cd supabase/functions/clinic-invite && deno test --allow-env --allow-net</automated>
  </verify>
  <done>
    - 4 endpoints serve correctly; 19 Deno tests pass.
    - W-1 invariant: /send returns `{ok: true, invite_id}` for both existing and nonexistent emails (Test 5).
    - Resend dispatch via HTTPS only; CI stub mode works.
    - Vercel rewrites land in vercel.json.
    - src/lib/clinic.ts wrappers call real Edge Function and parse W-1 response shape.
    - Edge Function deployed to live project.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [HUMAN CHECKPOINT]: Resend account + DNS records + RESEND_API_KEY secret + Vercel preview verify</name>
  <what-built>4-endpoint clinic-invite Edge Function + Resend HTTPS dispatch helper + Vercel rewrites for path-based routing.</what-built>
  <how-to-verify>
    Resend setup (cannot be automated — requires browser sign-up + DNS provider access):
    1. Sign up at https://resend.com/signup (if not already).
    2. Add domain `app.leanshot.app` in Resend → Domains.
    3. Copy the SPF + DKIM TXT records from Resend.
    4. Add them to the DNS provider authoritative for app.leanshot.app.
    5. Wait for Resend domain verification (~5-30 min).
    6. Create an API key: Resend → API Keys → "Phase 9 clinic-invite".
    7. Set the secret: `supabase secrets set RESEND_API_KEY=re_... --project-ref ytnsipxxmzgaebkqmokp`.
    8. Set CI secret in GitHub Actions: `RESEND_API_KEY=test-stub` (so Pitfall #8 e2e specs don't exhaust the 100/day free tier).

    Vercel rewrites preview verify:
    1. Open the PR preview URL for this plan's commit.
    2. Visit `https://leanshot-pr-NN.vercel.app/clinic/test-org` — should load the SPA.
    3. Visit `https://leanshot-pr-NN.vercel.app/clinic-invite/abc123` — should load the SPA → ClinicInvitePage → State H.

    Smoke test the real flow end-to-end on the preview:
    1. Sign up as `operator@yourtestdomain.com` on the preview.
    2. Create an org "Test Clinic" with slug "test-clinic".
    3. Open InvitePatientModal → enter `patient@yourtestdomain.com` → Send.
    4. Check the inbox at `patient@yourtestdomain.com` — Resend email should arrive within 30s.
    5. Click "Review invitation" → land on /clinic-invite/{token} → State D/B/C.
    6. Complete signup if needed → ConsentDialog → Accept.
    7. Confirm in Supabase dashboard: `select count(*) from memberships where org_id = (select id from orgs where slug='test-clinic')` → 1.
    8. Confirm: `select count(*) from audit_logs where action='membership_invite_accepted' and org_id = ...` → 1.
  </how-to-verify>
  <resume-signal>Type "verified" once Resend dispatches real emails AND vercel.json rewrites route correctly AND the smoke flow lands a row in memberships. Describe failures otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous → /clinic-invite/lookup | Untrusted token; hash compared; rate-limited 10/min/IP (brute-force defense T-09-32) |
| operator JWT → /clinic-invite/send | JWT validated by admin.auth.getUser; has_permission(members.invite) gate; rate-limited 20/hour/operator |
| patient JWT → /clinic-invite/accept | JWT-authenticated; RPC re-validates email match; UNIQUE(user_id, org_id) partial index single-identity floor |
| Edge Function → Resend HTTPS | Server-only RESEND_API_KEY in Deno.env; key NEVER reaches browser |
| CORS surface | Allow `*` Origin; NO credentials (Pitfall #11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-32 | Spoofing | brute-force invite token guess | mitigate | 128-bit entropy + 10/min/IP rate-limit on lookup; even at 1B guesses/sec for 100 years = 10^17 guesses; infeasible. Resend dispatch happens over HTTPS only (no SDK exposed); response body never includes the raw token after /send |
| T-09-33 | Information disclosure | email enumeration via /send | mitigate | D-02 + W-1: /send always returns `{ok: true, invite_id}` with a freshly-generated invite_id regardless of email existence; auth.users lookup deferred to /accept |
| T-09-34 | Information disclosure | Resend API key leak in logs | mitigate | Key in Deno.env, never echoed; non-2xx Resend response wraps as `{error:'resend_<status>'}` |
| T-09-35 | Tampering | open redirect via invite-token URL | mitigate | Token in path segment (not query); no redirect chain; SPA renders ClinicInvitePage which is a local component |
| T-09-36 | Replay | token reuse after acceptance | mitigate | accept_invite_existing UPDATE accepted_at + consumed_at; subsequent /lookup returns state 'already_used' |
| T-09-37 | Information disclosure | non-2xx Resend response leaks dispatch metadata | mitigate | Phase 4 pattern: never echo `res.text()`; wrap as `{ok:false, error:'resend_<status>'}` |
| T-09-38 | Tampering | CORS misconfiguration enables cross-origin invite sends from attacker page | accept | All clinic-invite endpoints require Authorization JWT (or anonymous token-in-URL) — CSRF via cross-origin form post requires the attacker to already hold a victim's JWT; same risk class as Supabase RPCs. Allow `*` matches ai-chat pattern. |
</threat_model>

<verification>
- `cd supabase/functions/clinic-invite && deno test` passes 19 cases.
- `supabase functions deploy clinic-invite --project-ref ytnsipxxmzgaebkqmokp` succeeds.
- HUMAN CHECKPOINT Task 2: smoke flow lands an end-to-end membership row.
- Vercel preview routes `/clinic/*` + `/clinic-invite/*` to SPA correctly.
- `grep -rn "anthropic-dangerous-direct-browser-access\|RESEND_API_KEY" src/` returns 0 matches (key never reaches browser).
</verification>

<success_criteria>
1. Edge Function with 4 endpoints deployed live.
2. Resend dispatches branded emails to a real inbox.
3. Vercel rewrites configured.
4. End-to-end SC#2 demoable: operator invites → patient receives email → click → accept.
5. CI stub mode (`RESEND_API_KEY=test-stub`) works for Pitfall #8 e2e suite.
6. D-02 + W-1 anti-enumeration test passes (Test 5): both real and fake emails get `{ok: true, invite_id}` response.
</success_criteria>

<output>
After completion, create `.planning/phases/09-clinic-b2b-foundations/09-06-SUMMARY.md` documenting the Resend domain config + any DNS quirks encountered + the smoke flow result.
</output>
