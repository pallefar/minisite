---
phase: 09-clinic-b2b-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260801000001_audit_logs_org_columns.sql
  - supabase/migrations/20260801000002_orgs.sql
  - supabase/migrations/20260801000003_org_logos_storage.sql
  - supabase/migrations/20260801000004_audit_logs_org_fk.sql
  - supabase/migrations/20260801000005_permissions.sql
  - supabase/migrations/20260801000006_roles.sql
  - supabase/migrations/20260801000007_memberships.sql
  - supabase/migrations/20260801000008_invites.sql
  - supabase/migrations/20260801000009_has_permission_fn.sql
  - supabase/migrations/20260801000010_seed_system_roles_trigger.sql
  - supabase/migrations/20260801000011_clinic_rpcs.sql
  - supabase/migrations/20260801000012_realtime_messages_rls.sql
  - supabase/migrations/20260801000013_broadcast_membership_changes_trigger.sql
  - supabase/functions/clinic-invite/deno.json
  - supabase/functions/clinic-invite/index.test.ts
  - supabase/functions/clinic-invite/cors.ts
  - supabase/functions/clinic-photo/deno.json
  - supabase/functions/clinic-photo/index.test.ts
  - supabase/functions/clinic-photo/cors.ts
  - e2e/rls-orgs.test.ts
  - e2e/rls-memberships.test.ts
  - e2e/rls-invites.test.ts
  - e2e/rls-roles.test.ts
  - e2e/rls-role-permissions.test.ts
  - e2e/rls-org-logos-storage.test.ts
  - e2e/clinic-pitfall-8-existing-user-invited.spec.ts
  - e2e/clinic-pitfall-8-no-user-invited.spec.ts
  - e2e/clinic-pitfall-8-existing-user-two-invites.spec.ts
  - e2e/clinic-pitfall-8-invited-never-accepts.spec.ts
  - e2e/clinic-pitfall-8-accepts-then-rejects.spec.ts
  - e2e/clinic-revoke-latency.spec.ts
  - e2e/clinic-photo-access.spec.ts
  - e2e/clinic-role-permission-grid.spec.ts
  - src/types/clinic.ts
autonomous: false  # supabase db push interactive prompt + bundle-size guard config update
requirements: [CLINIC-01, CLINIC-02, CLINIC-03, CLINIC-06]
mvp_slice: "Foundation slice — 13 migrations + has_permission helper + broadcast trigger + Pitfall #8 RLS impersonation proofs + Wave 0 test scaffolds. Without this, no clinic UI can bind, no invite can route, no revoke can broadcast. User-visible outcome on completion: a developer can manually `select public.create_org('Acme', 'acme', null, null)` via SQL and a Playwright RLS spec confirms cross-tenant isolation."

must_haves:
  truths:
    - "13 migrations applied to `ytnsipxxmzgaebkqmokp` in the exact order listed in files_modified"
    - "`audit_logs.org_id` column exists (nullable uuid FK to orgs); `audit_logs.actor_type` enum values `'org_operator'` and `'org_member'` exist; action check constraint extended with `'org_create'`, `'org_delete'`, `'membership_invite_sent'`, `'membership_invite_accepted'`, `'membership_invite_rejected'`, `'membership_invite_canceled'`, `'membership_revoked'`, `'membership_scope_updated'`, `'membership_role_changed'`, `'clinic_photo_view'`, `'permission_denied'`, `'role_create'`, `'role_update'`, `'role_delete'`"
    - "`orgs` table exists with columns id, slug (unique lowercased, kebab+reserved-word CHECK), name, description, website_url, logo_storage_path, owner_user_id FK auth.users, created_at, deleted_at; RLS enabled with `orgs_select_by_member` policy via `has_permission`; NO INSERT/UPDATE/DELETE policy"
    - "`org-logos` Storage bucket exists (public=true, mime png+jpeg, 2 MB limit); Storage RLS allows operator with `org.update` to write under `{org_id}/...` and authenticated read of `{org_id}/...` (D-07 admin UI dependency)"
    - "`permissions` table contains exactly 10 seed rows: org.read, org.update, org.delete, members.invite, members.revoke, members.list, roles.manage, patient_data.read, patient_photos.read, audit_log.read"
    - "`roles` table + `role_permissions` junction exist with FKs"
    - "`memberships` table exists with UNIQUE(user_id, org_id) WHERE revoked_at IS NULL (Pitfall #8 single-identity invariant); RLS enables owner SELECT + members.list SELECT via has_permission; NO INSERT/UPDATE/DELETE policy"
    - "`invites` table exists with hashed token column + 7-day expiry + frozen consent_scope_at_acceptance column + IMMUTABLE partial index (no now() in predicate)"
    - "`public.has_permission(user_id uuid, org_id uuid, permission_key text)` SECURITY DEFINER STABLE function exists with `set search_path = public, extensions, pg_catalog`"
    - "`orgs_seed_system_roles_trigger` AFTER INSERT trigger seeds 3 system roles + correct role_permissions rows (Owner=all 10 keys; Coach=org.read+members.list+patient_data.read+patient_photos.read; View-only=org.read+members.list+patient_data.read)"
    - "12 SECURITY DEFINER RPCs in `20260801000011_clinic_rpcs.sql`: `create_org`, `update_org`, `delete_org`, `send_invite`, `cancel_invite`, `accept_invite_existing`, `accept_invite_new`, `reject_invite`, `revoke_membership`, `update_consent_scope`, `update_member_role`, `create_role`, `update_role`, `delete_role`, `log_clinic_event` (planner final count: 15) — each has `set search_path = public, extensions, pg_catalog`; `delete_org` uses `app.suppress_audit` GUC + `storage.allow_delete_query` GUC bypass (cascade-safe)"
    - "RLS on `realtime.messages`: SELECT policy parses topic prefix `org:` and `user:` then dispatches to `has_permission(uid, org_id, 'org.read')` or `auth.uid() = user_id`"
    - "`broadcast_membership_changes()` trigger fires AFTER INSERT/UPDATE/DELETE on memberships, broadcasts to topics `org:{org_id}` and `user:{user_id}` via `realtime.broadcast_changes()`"
    - "Cross-tenant RLS impersonation proofs PASS for all 6 new RLS surfaces (orgs, memberships, invites, roles, role_permissions, org-logos bucket) — REQUIRED by project rule from memory `reference_supabase_project.md`"
    - "8 Wave-0 Playwright spec scaffolds + 6 RLS impersonation specs exist (failing or skipped — turn green in Waves 2-4)"
    - "Shared TypeScript types exported from `src/types/clinic.ts`: Org, Membership, Invite, Role, Permission, ConsentScope (strict 10-key shape per Pitfall #8 jsonb drift)"
    - "`supabase db push` succeeds against linked project"
  artifacts:
    - path: "supabase/migrations/20260801000001_audit_logs_org_columns.sql"
      provides: "audit_logs extension: org_id nullable column + actor_type enum values + action check constraint extension"
      contains: "alter type public.audit_actor_type add value"
    - path: "supabase/migrations/20260801000002_orgs.sql"
      provides: "orgs table + slug CHECK + UNIQUE(lower(slug)) + RLS"
      contains: "create table public.orgs"
    - path: "supabase/migrations/20260801000003_org_logos_storage.sql"
      provides: "org-logos bucket + Storage RLS"
      contains: "insert into storage.buckets"
    - path: "supabase/migrations/20260801000004_audit_logs_org_fk.sql"
      provides: "audit_logs.org_id FK to orgs(id) — deferred to step 4 so orgs exists"
      contains: "alter table public.audit_logs add constraint audit_logs_org_id_fkey"
    - path: "supabase/migrations/20260801000005_permissions.sql"
      provides: "permissions table + 10 seed rows + RLS read-all"
      contains: "insert into public.permissions"
    - path: "supabase/migrations/20260801000006_roles.sql"
      provides: "roles + role_permissions tables + indexes"
      contains: "create table public.roles"
    - path: "supabase/migrations/20260801000007_memberships.sql"
      provides: "memberships table + UNIQUE(user_id, org_id) WHERE revoked_at IS NULL + RLS"
      contains: "create table public.memberships"
    - path: "supabase/migrations/20260801000008_invites.sql"
      provides: "invites table + hashed token + 7-day expiry + IMMUTABLE partial index"
      contains: "create table public.invites"
    - path: "supabase/migrations/20260801000009_has_permission_fn.sql"
      provides: "has_permission() SECURITY DEFINER STABLE helper"
      contains: "create or replace function public.has_permission"
    - path: "supabase/migrations/20260801000010_seed_system_roles_trigger.sql"
      provides: "AFTER INSERT trigger seeds 3 system roles per org"
      contains: "create trigger orgs_seed_system_roles_trigger"
    - path: "supabase/migrations/20260801000011_clinic_rpcs.sql"
      provides: "All 14-15 SECURITY DEFINER RPCs (create_org, send_invite, accept_invite_existing, accept_invite_new, reject_invite, cancel_invite, revoke_membership, update_consent_scope, update_member_role, create_role, update_role, delete_role, update_org, delete_org, log_clinic_event)"
      contains: "create or replace function public.create_org"
    - path: "supabase/migrations/20260801000012_realtime_messages_rls.sql"
      provides: "realtime.messages RLS dispatches to has_permission via topic prefix parse"
      contains: "create policy clinic_org_topic_select on realtime.messages"
    - path: "supabase/migrations/20260801000013_broadcast_membership_changes_trigger.sql"
      provides: "AFTER INSERT/UPDATE/DELETE trigger on memberships → realtime.broadcast_changes()"
      contains: "create trigger memberships_broadcast_trigger"
    - path: "src/types/clinic.ts"
      provides: "Strict-shape TS types: Org, Membership, Invite, Role, Permission, ConsentScope (exactly 10 keys, all boolean)"
      exports: ["Org", "Membership", "Invite", "Role", "Permission", "ConsentScope", "DATA_TYPE_LABELS", "PERMISSION_KEYS"]
    - path: "supabase/functions/clinic-invite/deno.json"
      provides: "Deno test config for clinic-invite Edge Function (Wave 0 scaffold)"
    - path: "supabase/functions/clinic-invite/index.test.ts"
      provides: "Failing Deno test scaffold — `.test.ts` suffix per memory `reference_deno_test_discovery.md`"
    - path: "supabase/functions/clinic-invite/cors.ts"
      provides: "Echo-Origin CORS (no credentials — matches ai-chat/cors.ts)"
    - path: "supabase/functions/clinic-photo/deno.json"
      provides: "Deno test config for clinic-photo Edge Function"
    - path: "supabase/functions/clinic-photo/index.test.ts"
      provides: "Failing Deno test scaffold"
    - path: "supabase/functions/clinic-photo/cors.ts"
      provides: "Echo-Origin CORS (no credentials)"
    - path: "e2e/rls-orgs.test.ts"
      provides: "Cross-tenant RLS impersonation proof (orgs)"
    - path: "e2e/rls-memberships.test.ts"
      provides: "Cross-tenant RLS impersonation proof + Pitfall #8 UNIQUE constraint test"
    - path: "e2e/rls-invites.test.ts"
      provides: "Cross-tenant RLS proof + consent_scope_at_acceptance freeze test"
    - path: "e2e/rls-roles.test.ts"
      provides: "Cross-tenant RLS proof + system roles seeded test"
    - path: "e2e/rls-role-permissions.test.ts"
      provides: "Cross-tenant RLS proof + system-role-delete-forbidden test"
    - path: "e2e/rls-org-logos-storage.test.ts"
      provides: "Cross-tenant Storage RLS proof on org-logos bucket"
  key_links:
    - from: "supabase/migrations/20260801000001_audit_logs_org_columns.sql"
      to: "supabase/migrations/20260601000001_audit_logs.sql (Phase 7)"
      via: "ALTER TABLE additive column + ALTER TYPE add value"
      pattern: "alter (table|type) public\\.(audit_logs|audit_actor_type)"
    - from: "supabase/migrations/20260801000004_audit_logs_org_fk.sql"
      to: "orgs table"
      via: "FK constraint added AFTER orgs exists (step 4 ordering)"
      pattern: "foreign key \\(org_id\\) references public\\.orgs"
    - from: "supabase/migrations/20260801000009_has_permission_fn.sql"
      to: "memberships + role_permissions"
      via: "SECURITY DEFINER STABLE join"
      pattern: "set search_path = public, extensions, pg_catalog"
    - from: "supabase/migrations/20260801000012_realtime_messages_rls.sql"
      to: "has_permission() helper"
      via: "topic prefix parse → has_permission dispatch"
      pattern: "has_permission\\(auth\\.uid\\(\\), substring"
    - from: "supabase/migrations/20260801000013_broadcast_membership_changes_trigger.sql"
      to: "realtime.broadcast_changes()"
      via: "AFTER trigger on memberships"
      pattern: "perform realtime\\.broadcast_changes"
    - from: "e2e/rls-orgs.test.ts"
      to: "orgs RLS"
      via: "supabase-js with two test users"
      pattern: "createClient.*anon"
---

<objective>
Land all Postgres schema, RLS, RPCs, broadcast triggers, Edge Function scaffolds, and Wave-0 test scaffolds for Phase 9 in a single foundation slice. Push 13 migrations to `ytnsipxxmzgaebkqmokp` in the exact order specified. Without this gate, no clinic UI, Edge Function, or test in Waves 2/3/4 can bind to anything real.

Purpose: Pitfall #8 single-identity invariant (one auth.users per email; UNIQUE(user_id, org_id) on memberships) is enforced at the DB layer via partial unique index. Every revoke/scope-change/permission-denied writes audit_logs (CLINIC-07 capture half). `has_permission()` is the single RLS dispatch primitive (D-07). `realtime.broadcast_changes` + `realtime.messages` RLS is the NEW cross-tenant subscribe pattern (D-10 Layer 1). 4 reusable migration gotchas from memory `reference_supabase_migration_gotchas.md` applied preventively: (1) IMMUTABLE predicate on invites partial index — no now() in predicate, defer "active" check to query WHERE; (2) SECURITY DEFINER functions include `extensions` in search_path — pgcrypto digest() requires it; (3) `delete_org` uses `storage.allow_delete_query` GUC to delete org-logo Storage objects; (4) `delete_org` sets `app.suppress_audit` GUC before cascade so the audit_trigger stops firing mid-cascade. Cross-tenant RLS impersonation proof on all 6 new surfaces — project rule from memory `reference_supabase_project.md`.

Output: 13 migrations pushed live, 14-15 SECURITY DEFINER RPCs callable, has_permission() in use by RLS, broadcast trigger wired, 6 RLS proofs green, 8 e2e spec files exist as Wave-0 scaffolds, Edge Function deno scaffolds exist, strict-shape TypeScript types exported.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-clinic-b2b-foundations/09-CONTEXT.md
@.planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md
@.planning/phases/09-clinic-b2b-foundations/09-UI-SPEC.md
@.planning/phases/09-clinic-b2b-foundations/09-VALIDATION.md
@.planning/research/PITFALLS.md
@supabase/migrations/20260601000001_audit_logs.sql
@supabase/migrations/20260601000002_audit_triggers.sql
@supabase/migrations/20260601000011_initiate_account_deletion_rpc.sql
@supabase/migrations/20260601000016_finalize_storage_bypass.sql
@supabase/migrations/20260601000017_audit_trigger_suppress_guc.sql
@supabase/functions/ai-chat/cors.ts
@supabase/functions/ai-chat/rate-limit.ts
@e2e/rls-injections.test.ts
@e2e/rls-photos-storage.test.ts
</context>

<interfaces>
<!-- Strict TS contract that downstream plans (02/03/04/05/06/07/08) bind against. -->
<!-- Source: 09-CONTEXT.md D-04 + D-05 + D-07; 09-UI-SPEC.md data-type label table + permission grid. -->

From src/types/clinic.ts (to be created):
```typescript
export const DATA_TYPE_KEYS = [
  'injections', 'weights', 'photos', 'symptoms', 'meals',
  'workouts', 'supplements', 'mood', 'sleep', 'doctor_report'
] as const;
export type DataTypeKey = typeof DATA_TYPE_KEYS[number];
export type ConsentScope = Record<DataTypeKey, boolean>;

export const PERMISSION_KEYS = [
  'org.read', 'org.update', 'org.delete',
  'members.invite', 'members.revoke', 'members.list',
  'roles.manage',
  'patient_data.read', 'patient_photos.read', 'audit_log.read'
] as const;
export type PermissionKey = typeof PERMISSION_KEYS[number];

export interface Org {
  id: string; slug: string; name: string;
  description: string | null; website_url: string | null;
  logo_storage_path: string | null;
  owner_user_id: string; created_at: string;
}

export interface Membership {
  id: string; user_id: string; org_id: string; role_id: string;
  consent_scope: ConsentScope;
  invited_from_invite_id: string | null;
  joined_at: string; accepted_at: string | null;
  revoked_at: string | null; last_scope_changed_at: string | null;
}

export interface Invite {
  id: string; email: string; org_id: string; invited_by: string;
  requested_scope: ConsentScope;
  consent_scope_at_acceptance: ConsentScope | null;  // frozen on accept (D-18)
  expires_at: string;
  accepted_at: string | null; rejected_at: string | null; consumed_at: string | null;
  created_at: string;
}

export interface Role {
  id: string; org_id: string; name: string;
  description: string | null; is_system: boolean; created_at: string;
}

export interface Permission { key: PermissionKey; description: string }

export const DATA_TYPE_LABELS: Record<DataTypeKey, { label: string; description: string }> = {
  injections: { label: 'Injections', description: 'Doses, dates, and injection sites' },
  weights: { label: 'Weight log', description: 'Weight measurements over time' },
  photos: { label: 'Body photos', description: "Progress photos you've taken" },
  symptoms: { label: 'Symptoms', description: "Symptoms you've logged" },
  meals: { label: 'Meals', description: "Food and macros you've tracked" },
  workouts: { label: 'Workouts', description: 'Exercise and activity sessions' },
  supplements: { label: 'Supplements', description: 'Daily supplement intake' },
  mood: { label: 'Mood', description: 'Mood and energy logs' },
  sleep: { label: 'Sleep', description: 'Sleep duration and quality' },
  doctor_report: { label: 'Doctor report', description: 'Your generated doctor-report summary' },
};
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 13 migrations + has_permission helper + broadcast trigger + clinic_rpcs (D-01..D-18)</name>
  <read_first>
    - supabase/migrations/20260601000001_audit_logs.sql (audit base + actor_type enum target for extension)
    - supabase/migrations/20260601000017_audit_trigger_suppress_guc.sql (GUC suppression pattern — apply to delete_org per memory gotcha #4)
    - supabase/migrations/20260601000016_finalize_storage_bypass.sql (storage.allow_delete_query GUC bypass — apply to delete_org per memory gotcha #3)
    - supabase/migrations/20260601000011_initiate_account_deletion_rpc.sql (SECURITY DEFINER RPC template with `set search_path = public, extensions, pg_catalog` per memory gotcha #2)
    - supabase/migrations/20260514000009_photos.sql + 20260514000010_storage_bucket.sql (Storage bucket creation pattern + `storage.foldername(name)[1]` RLS extraction — see Pitfall #6: object name MUST be `{org_id}/logo.png` bucket-relative, not `org-logos/{org_id}/...`)
    - .planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md §"Code Examples" lines 779-1205 — copy verbatim with planner-named columns
    - .planning/phases/09-clinic-b2b-foundations/09-RESEARCH.md §"Common Pitfalls" Pitfalls 1-11 (load-bearing for every migration)
    - .planning/research/PITFALLS.md §"Pitfall 8" — 5-scenario matrix invariants
  </read_first>
  <behavior>
    - Test 1 (RLS proof — orgs): user A creates org via create_org RPC; user B's anon client SELECT on orgs returns 0 rows (cross-tenant isolation) — REQUIRED by project rule.
    - Test 2 (RLS proof — memberships UNIQUE): user A creates org X; admin client INSERT memberships for user A in org X succeeds first time; second INSERT (same user_id, org_id, revoked_at IS NULL) FAILS with unique_violation — Pitfall #8 single-identity invariant.
    - Test 3 (RLS proof — invites cross-tenant): user A creates invite for `test@example.com` in org X; user B's client (not member of X) SELECT invites returns 0 rows.
    - Test 4 (RLS proof — roles cross-tenant): user A creates org X (3 system roles seeded); user B SELECT roles WHERE org_id = X returns 0 rows.
    - Test 5 (RLS proof — role_permissions cross-tenant): same as above for role_permissions junction.
    - Test 6 (RLS proof — org-logos Storage bucket): user A uploads `{X}/logo.png` to org-logos bucket (org.update permission verified); user B's client UPLOAD to `{X}/logo.png` FAILS with 403; public read of `{X}/logo.png` SUCCEEDS (public bucket).
    - Test 7 (system roles seeded): after `select public.create_org('Acme','acme',null,null)`, exactly 3 rows exist in `roles WHERE org_id = $1`: Owner / Coach / View-only; `role_permissions` rows: Owner has 10, Coach has 4, View-only has 3.
    - Test 8 (Pitfall #4 trigger ordering): create_org RPC inside ONE transaction inserts org, trigger seeds roles AFTER the orgs INSERT, RPC then SELECTs Owner role_id and INSERTs operator's membership — succeeds without FK violation.
    - Test 9 (Pitfall #5 IMMUTABLE): `create index invites_org_active_idx on invites (...) WHERE accepted_at is null and rejected_at is null` — push succeeds (no now() in predicate).
    - Test 10 (Pitfall #3 STABLE): `select prosrc, provolatile from pg_proc where proname='has_permission'` returns `provolatile='s'` (STABLE).
    - Test 11 (delete_org cascade-safe): after orgs created with 2 members + 3 roles + 2 invites + 1 logo; `select public.delete_org($1)` succeeds; audit_logs has exactly 1 row for the org_delete action (cascade rows suppressed via `app.suppress_audit` GUC); Storage object deleted.
    - Test 12 (consent_scope freeze D-18): accept_invite_existing writes `invites.consent_scope_at_acceptance = $consent_scope`; later UPDATE memberships.consent_scope does NOT mutate invites row.
    - Test 13 (anti-enumeration D-02): send_invite returns 200 regardless of whether `email` exists in auth.users (auth.users existence lookup deferred to lookup/accept path).
    - Test 14 (Realtime broadcast topic format): after `update memberships set revoked_at = now() where id = $1`, the broadcast trigger fires; SELECT from `realtime.messages` WHERE topic = `'org:'||$org_id` shows the UPDATE event (verified with service-role client; production gating is the `realtime.messages` RLS which we test separately in Plan 09 via the negative-space subscribe test).
  </behavior>
  <action>
    Author 13 migrations in EXACT order with the EXACT filenames in `files_modified`. Use the SQL examples from `09-RESEARCH.md §Code Examples` lines 779-1205 verbatim with the following load-bearing adjustments:

    **Migration 1 — `20260801000001_audit_logs_org_columns.sql`:**
    - `ALTER TYPE public.audit_actor_type ADD VALUE IF NOT EXISTS 'org_operator';`
    - `ALTER TYPE public.audit_actor_type ADD VALUE IF NOT EXISTS 'org_member';`
    - `ALTER TABLE public.audit_logs ADD COLUMN IF NOT EXISTS org_id uuid;` (FK added in migration 4)
    - `ALTER TABLE public.audit_logs DROP CONSTRAINT IF EXISTS audit_logs_action_check;`
    - `ALTER TABLE public.audit_logs ADD CONSTRAINT audit_logs_action_check CHECK (action IN (...));` — list ALL pre-existing actions (read from `20260601000001_audit_logs.sql` and downstream extensions) PLUS the 13 new clinic actions: `org_create`, `org_delete`, `org_update`, `membership_invite_sent`, `membership_invite_accepted`, `membership_invite_rejected`, `membership_invite_canceled`, `membership_revoked`, `membership_scope_updated`, `membership_role_changed`, `clinic_photo_view`, `permission_denied`, `role_create`, `role_update`, `role_delete`. (PostgreSQL needs the union list — DO NOT lose any existing values; read them from prior migrations FIRST.)
    - Partial index `audit_logs_clinic_actor_idx on audit_logs (org_id, created_at desc) WHERE actor_type IN ('org_operator','org_member')`.

    **Migration 2 — `20260801000002_orgs.sql`:** Use the example at `09-RESEARCH.md` line 781-820 verbatim. Reserved-word blocklist MUST include: `api`, `auth`, `settings`, `admin`, `app`, `clinic`, `clinic-invite`, `legal`, `login`, `logout`, `signup`, `help`, `about`, `home`, `static`, `public`, `assets`, `functions`, `share`. RLS policy `orgs_select_by_member` dispatches via `has_permission(auth.uid(), id, 'org.read')` OR `auth.uid() = owner_user_id`. **NO** insert/update/delete policy (writes only via RPCs). Add the orgs table to `supabase_realtime` publication for parity (per RESEARCH.md note).

    **Migration 3 — `20260801000003_org_logos_storage.sql`:** Insert bucket via `insert into storage.buckets (id, name, public, file_size_limit, allowed_mime_types) values ('org-logos', 'org-logos', true, 2097152, ARRAY['image/png','image/jpeg']) on conflict do nothing;`. Storage RLS: `objects_select_org_logos for select to authenticated, anon using (bucket_id = 'org-logos')` (public read); `objects_insert_org_logos for insert to authenticated using (bucket_id = 'org-logos' and public.has_permission(auth.uid(), (storage.foldername(name))[1]::uuid, 'org.update'))`; same shape for update/delete. **Critical (Pitfall #6):** object `name` is `{org_id}/logo.{ext}` — BUCKET-RELATIVE — so `(storage.foldername(name))[1]` returns `{org_id}`.

    **Migration 4 — `20260801000004_audit_logs_org_fk.sql`:** `ALTER TABLE public.audit_logs ADD CONSTRAINT audit_logs_org_id_fkey FOREIGN KEY (org_id) REFERENCES public.orgs(id) ON DELETE SET NULL;` — ordering: orgs MUST exist first (step 2).

    **Migration 5 — `20260801000005_permissions.sql`:** Use the example at lines 891-911 verbatim. 10 seed rows with the exact descriptions from `09-UI-SPEC.md` Permission grid rows lines 329-348. `permissions_read_all` RLS policy.

    **Migration 6 — `20260801000006_roles.sql`:** Use the example at lines 913-928 verbatim. Add RLS: `roles_select_by_member for select using (public.has_permission(auth.uid(), org_id, 'org.read'))`; NO direct INSERT/UPDATE/DELETE policy. Same shape for `role_permissions` (USING `has_permission(auth.uid(), (select org_id from roles where id = role_permissions.role_id), 'org.read')`).

    **Migration 7 — `20260801000007_memberships.sql`:** Use the example at lines 823-854 verbatim. Pitfall #8 invariant: `create unique index memberships_user_org_idx on public.memberships (user_id, org_id) where revoked_at is null;`. Add `memberships_select_own` AND `memberships_select_by_org_member` policies. Add the memberships table to `supabase_realtime` publication so the broadcast trigger's `realtime.broadcast_changes` works (note: broadcast pattern doesn't strictly require this, but parity with Phase 5/6 prevents surprises).

    **Migration 8 — `20260801000008_invites.sql`:** Use the example at lines 858-887 verbatim. Pitfall #5 invariant: partial index predicate uses ONLY `accepted_at is null and rejected_at is null` (NO `now() < expires_at` — defer to query WHERE). `invites_select_by_org_manager` RLS via `has_permission(uid, org_id, 'members.list')`. NO direct INSERT/UPDATE/DELETE.

    **Migration 9 — `20260801000009_has_permission_fn.sql`:** Use the example at lines 521-546 verbatim. **CRITICAL: `STABLE` (Pitfall #3) + `set search_path = public, extensions, pg_catalog` (memory gotcha #2)**. Grant execute to authenticated, service_role; revoke from public.

    **Migration 10 — `20260801000010_seed_system_roles_trigger.sql`:** Use the example at lines 933-977 verbatim. Owner has all 10 permissions (SELECT key from public.permissions); Coach has `org.read`, `members.list`, `patient_data.read`, `patient_photos.read`; View-only has `org.read`, `members.list`, `patient_data.read`. **Pitfall #4 ordering:** trigger is `AFTER INSERT ON orgs FOR EACH ROW` so roles seed BEFORE the create_org RPC SELECTs Owner.id.

    **Migration 11 — `20260801000011_clinic_rpcs.sql`:** Author **15 SECURITY DEFINER RPCs** (final list — superset of CONTEXT.md D-07's "12 clinic-scoped RPCs"). Each uses `set search_path = public, extensions, pg_catalog` (memory gotcha #2) and writes an audit_logs row via inline INSERT (avoid `log_clinic_event` indirection within the RPCs that already hold the transaction):
      1. `create_org(p_name, p_slug, p_description, p_website_url) returns table(org_id, slug)` — example at lines 983-1023 verbatim. Audit: action='org_create', actor_type='org_operator'.
      2. `update_org(p_org_id, p_name, p_description, p_website_url, p_logo_storage_path)` — gate on has_permission(uid, p_org_id, 'org.update'). Audit: action='org_update'.
      3. `delete_org(p_org_id)` — example at lines 646-680 verbatim. Gate on `org.delete`. `set_config('app.suppress_audit','true',true)` + `set_config('storage.allow_delete_query','true',true)`. Inline skeleton audit row BEFORE cascade. Delete org-logo storage object via `delete from storage.objects where bucket_id='org-logos' and (storage.foldername(name))[1] = p_org_id::text`. Delete orgs (cascades memberships, invites, roles, role_permissions via FKs).
      4. `send_invite(p_email, p_org_id, p_invite_token_hash, p_requested_scope) returns table(invite_id)` — gate on has_permission(uid, p_org_id, 'members.invite'). Insert invites row with expires_at = now() + interval '7 days'. Audit: action='membership_invite_sent', actor_type='org_operator', org_id=p_org_id. **D-02 anti-enumeration:** caller (Edge Function) reads back invite_id but returns identical 200 regardless of whether p_email matches an auth.users row — that check happens at lookup/accept time only.
      5. `cancel_invite(p_invite_id)` — gate on has_permission(uid, invites.org_id, 'members.invite'). UPDATE rejected_at=now(), consumed_at=now(). Audit: action='membership_invite_canceled'.
      6. `accept_invite_existing(p_invite_token_hash, p_consent_scope) returns table(membership_id, org_id)` — example at lines 1030-1086 verbatim. Validates p_consent_scope keys are exactly the 10 DATA_TYPE_KEYS (use plpgsql jsonb_object_keys check; reject extras and missing keys). Default role = View-only system role. **D-18:** writes `invites.consent_scope_at_acceptance = p_consent_scope` in the SAME transaction. Audit: action='membership_invite_accepted', actor_type='org_member'.
      7. `accept_invite_new(p_invite_token_hash, p_consent_scope) returns table(membership_id, org_id)` — identical to (6) except this is called AFTER the new user has completed Supabase Auth signup (their auth.uid() exists by the time this RPC fires). Single-identity invariant enforced by the UNIQUE(user_id, org_id) partial index. Note: Pitfall #1 — signup path goes through `supabase.auth.signUp` on the client; this RPC only writes memberships after auth.uid() is set.
      8. `reject_invite(p_invite_token_hash)` — UPDATE rejected_at=now() + consumed_at=now(). Audit: action='membership_invite_rejected'.
      9. `revoke_membership(p_membership_id)` — example at lines 1091-1128 verbatim. Idempotent (early return if already revoked_at IS NOT NULL). Either user revoking self OR operator with members.revoke. Audit actor_type varies.
      10. `update_consent_scope(p_membership_id, p_consent_scope)` — patient only (auth.uid() = memberships.user_id). Validates 10-key shape. UPDATE memberships set consent_scope, last_scope_changed_at=now(). Audit: action='membership_scope_updated'.
      11. `update_member_role(p_membership_id, p_role_id)` — gate on has_permission(uid, memberships.org_id, 'roles.manage'). Verify p_role_id belongs to the same org_id. UPDATE memberships set role_id. Audit: action='membership_role_changed'.
      12. `create_role(p_org_id, p_name, p_description, p_permission_keys text[]) returns table(role_id)` — gate on has_permission(uid, p_org_id, 'roles.manage'). Insert roles (is_system=false) + role_permissions rows. Audit: action='role_create'.
      13. `update_role(p_role_id, p_name, p_description, p_permission_keys text[])` — gate on has_permission(uid, roles.org_id, 'roles.manage'). Forbid editing is_system roles. Atomically DELETE then INSERT role_permissions for p_role_id. Audit: action='role_update'.
      14. `delete_role(p_role_id)` — gate on roles.manage. Forbid is_system=true. Before delete, UPDATE memberships set role_id = (View-only role of same org) WHERE role_id = p_role_id. Return count of reassigned members for the toast copy. Audit: action='role_delete'.
      15. `log_clinic_event(p_actor_type, p_action, p_org_id, p_target_user_id, p_row_id)` — generic audit-writing RPC for Edge Function use (clinic-photo permission_denied path). Hashes auth.uid() into user_id_hash. Validates p_action is in the allowed clinic action set.

    All RPCs `revoke all from public; grant execute to authenticated;` (service_role inherits via Edge Function client which uses service-role key).

    **Migration 12 — `20260801000012_realtime_messages_rls.sql`:** Use the example at lines 600-612 verbatim. Note: `realtime.messages` already has RLS enabled in current Supabase; we ADD the policy (don't reset). Two prefix branches: `'org:%'` → has_permission for 'org.read'; `'user:%'` → auth.uid() = parsed user_id; else false.

    **Migration 13 — `20260801000013_broadcast_membership_changes_trigger.sql`:** Use the example at lines 568-595 verbatim. `set search_path = public, extensions, pg_catalog`. Broadcasts to BOTH topics (`org:{org_id}` and `user:{user_id}`) on every INSERT/UPDATE/DELETE.

    **Wave-0 test scaffolds** (failing or skipped — turn green in later waves):
    - `supabase/functions/clinic-invite/deno.json` — copy from `supabase/functions/ai-chat/deno.json` (Wave 0).
    - `supabase/functions/clinic-invite/index.test.ts` — Deno test scaffold with one failing assertion `// Plan 09-06 turns this green`. Use `.test.ts` suffix per memory `reference_deno_test_discovery.md`.
    - `supabase/functions/clinic-invite/cors.ts` — echo-Origin CORS, NO credentials (mirrors `ai-chat/cors.ts` verbatim; comment `// No credentials — JWT/no-auth only per D-16; * origin OK per Pitfall #11`).
    - Identical scaffolds for `supabase/functions/clinic-photo/`.
    - 6 RLS impersonation specs at `e2e/rls-{orgs,memberships,invites,roles,role-permissions,org-logos-storage}.test.ts` — use the structure from `e2e/rls-injections.test.ts` + `e2e/rls-photos-storage.test.ts`; implement the 6 RLS proofs listed in Test 1-6 of `<behavior>`. These tests MUST PASS at the end of Task 1 — they prove the RLS layer is correct before any UI ships.
    - 8 Playwright spec scaffolds at `e2e/clinic-pitfall-8-*.spec.ts` + `e2e/clinic-revoke-latency.spec.ts` + `e2e/clinic-photo-access.spec.ts` + `e2e/clinic-role-permission-grid.spec.ts` — header + describe block + `test.fixme('Plan 09-NN turns this green', async () => {});` body. Wave 4 plans (09-09, 09-10) flesh these out. Add a top-of-file comment `// Wave-0 scaffold — Plan 09-NN owner` referencing the implementing plan number.

    **`src/types/clinic.ts`:** Author per the `<interfaces>` block above. Include exhaustive type guards: `export function isConsentScope(v: unknown): v is ConsentScope` (validates exactly 10 boolean keys — Pitfall #8 drift mitigation). Export `DATA_TYPE_LABELS` (the table from UI-SPEC) and `PERMISSION_KEYS` const tuple. NO `s.user!` non-null assertions anywhere (per RESEARCH anti-pattern).

    **Bundle-size guard extension:** Edit `scripts/bundle-size-check.cjs` (or wherever the Phase 6 bundle guard lives — search the repo) to add budgets: clinic ≤12 kB gz, clinic-settings ≤14 kB gz, clinic-invite ≤6 kB gz, index ≤50 kB gz (Phase 6 close = 21.49 kB; allow +3 kB for WorkspaceSwitcher = 24.5 kB cap, with absolute 50 kB ceiling preserved).

    **Use `git commit -- <pathspec>` for every commit** per memory `feedback_parallel_executor_git_isolation.md`; this plan is Wave 1 (single executor) so no parallel contamination, but future waves run parallel and the rule is plan-wide discipline.
  </action>
  <verify>
    <automated>npm run test:e2e:rls -- 'rls-(orgs|memberships|invites|roles|role-permissions|org-logos-storage)\.test\.ts'</automated>
  </verify>
  <done>
    - 13 migrations exist in `supabase/migrations/` with the exact filenames + ordering listed.
    - 15 SECURITY DEFINER RPCs callable; each has `set search_path = public, extensions, pg_catalog`.
    - 10 permission keys seeded; 3 system roles seeded per org via trigger.
    - All 6 RLS impersonation specs PASS (turn green at the end of Task 1).
    - 8 Playwright spec files exist as Wave-0 scaffolds.
    - Edge Function deno scaffolds + cors.ts exist for clinic-invite + clinic-photo.
    - `src/types/clinic.ts` exports Org, Membership, Invite, Role, Permission, ConsentScope, DATA_TYPE_LABELS, PERMISSION_KEYS, isConsentScope.
    - Bundle-size guard updated with clinic/clinic-settings/clinic-invite budgets.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [BLOCKING]: supabase db push + verify migrations applied</name>
  <what-built>13 migrations + 15 RPCs + 6 RLS proofs from Task 1 — pushed to ytnsipxxmzgaebkqmokp.</what-built>
  <how-to-verify>
    Run: `cd /Users/karstenhaldan/minisite && supabase db push --linked`

    Confirm the migration runner reports all 13 migrations applied (`20260801000001` through `20260801000013`).

    Then verify with: `supabase db remote commit --linked` OR via psql:
    `select migration from supabase_migrations.schema_migrations where migration like '20260801%' order by migration;`
    Should return 13 rows.

    Also confirm via SQL editor in Supabase dashboard:
    1. `select count(*) from public.permissions;` → 10
    2. `select proname, provolatile from pg_proc where proname='has_permission';` → returns `'has_permission', 's'`
    3. `select tgname from pg_trigger where tgname in ('orgs_seed_system_roles_trigger','memberships_broadcast_trigger');` → 2 rows
    4. `select policyname from pg_policies where tablename='messages' and schemaname='realtime' and policyname='clinic_org_topic_select';` → 1 row

    **If push fails:** capture the exact error, attempt fixes per memory `reference_supabase_migration_gotchas.md` (search_path / IMMUTABLE / Storage delete / GUC suppression), retry. Do NOT proceed to Wave 2 plans until push is green.

    **Non-TTY workaround:** if Claude can't run `supabase db push` interactively, set `SUPABASE_ACCESS_TOKEN` env var (already set in project) and use `--linked --include-all`.
  </how-to-verify>
  <resume-signal>Type "applied" once all 13 migrations are in `supabase_migrations.schema_migrations`, or describe the failure mode + the fix you applied.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator client → Supabase RPC | Operator's JWT crosses RLS gate via has_permission helper |
| anonymous client → Edge Function `/clinic-invite/lookup` | Untrusted token in URL; hash compared server-side |
| patient client (post-signup) → accept_invite_* RPC | Patient's JWT + invite-token-hash; UNIQUE(user_id, org_id) partial index is the single-identity floor |
| operator client → Realtime `org:{org_id}` channel | `realtime.messages` RLS dispatches to has_permission |
| direct DB write (RLS bypass attempt) | RLS denies all authenticated INSERT/UPDATE/DELETE on orgs/memberships/invites/roles/role_permissions — only SECURITY DEFINER RPCs write |
| Storage object `org-logos/{org_id}/...` | Per-tenant Storage RLS via `(storage.foldername(name))[1]::uuid` extraction (Pitfall #6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information disclosure | invites table | mitigate | RLS `invites_select_by_org_manager` via `has_permission(uid, org_id, 'members.list')`; cross-tenant impersonation proof in `e2e/rls-invites.test.ts` |
| T-09-02 | Elevation of privilege | role_permissions table | mitigate | NO authenticated INSERT/UPDATE/DELETE policy; only `create_role`/`update_role` RPCs gate on `roles.manage` |
| T-09-03 | Tampering | direct memberships INSERT | mitigate | RLS denies authenticated INSERT; only `accept_invite_*` RPCs write; UNIQUE(user_id, org_id) WHERE revoked_at IS NULL enforces single-identity invariant at DB layer |
| T-09-04 | Elevation of privilege | self-promote role via memberships UPDATE | mitigate | RLS denies authenticated UPDATE; only `update_member_role` RPC (gated on `roles.manage`) writes role_id |
| T-09-05 | Information disclosure | cross-tenant memberships SELECT | mitigate | `memberships_select_by_org_member` requires `has_permission(uid, org_id, 'members.list')`; negative-space test in rls-memberships.test.ts |
| T-09-06 | Tampering | direct INSERT to audit_logs to hide a revoke | mitigate | audit_logs has NO authenticated INSERT policy; SECURITY DEFINER RPCs are the only write path (Phase 7 pattern carried forward) |
| T-09-07 | Tampering / spoofing | duplicate auth.users via race (Pitfall #1) | mitigate | All signup-from-invite goes through `supabase.auth.signUp` (client) — Supabase Auth's UNIQUE(email) is the serializer; collision routes to State C (sign-in) |
| T-09-08 | Tampering | phantom org via slug squatting | mitigate | UNIQUE(lower(slug)) on orgs + reserved-word blocklist CHECK constraint + slug regex |
| T-09-09 | Tampering / XSS | org-logo SVG upload | mitigate | Storage bucket `allowed_mime_types = ARRAY['image/png','image/jpeg']` — SVG excluded |
| T-09-10 | Information disclosure | Realtime topic subscribe by non-member | mitigate | RLS on `realtime.messages` calls `has_permission(uid, parsed_org_id, 'org.read')`; negative-space test in Plan 09-10 |
| T-09-11 | Repudiation | audit-log row write race on permission-denied | mitigate | `log_clinic_event` RPC runs inside Edge Function transaction; failure to audit returns 500 to caller |
| T-09-12 | Spoofing | brute-force invite token guess | mitigate | 128-bit token entropy via `gen_random_bytes(16)`; rate-limit on `/clinic-invite/lookup` (10/min/IP) in Plan 09-06 |
</threat_model>

<verification>
- All 13 migrations applied to `ytnsipxxmzgaebkqmokp` (Task 2 verifies).
- 6 RLS impersonation specs pass: `npm run test:e2e:rls -- 'rls-(orgs|memberships|invites|roles|role-permissions|org-logos-storage)\.test\.ts'`.
- Migration filename ordering matches `files_modified` exactly (Pitfall #4 audit_logs FK + Pitfall #4 trigger ordering both hinge on this).
- `supabase/functions/clinic-{invite,photo}/deno.json + index.test.ts + cors.ts` exist (Wave 0 scaffolds).
- `src/types/clinic.ts` exports the strict-shape types; `isConsentScope` rejects objects with extra/missing keys.
- 8 Playwright spec scaffolds exist; each is `test.fixme()`'d with a reference comment to its implementing plan.
- `scripts/bundle-size-check.cjs` (or equivalent) includes budgets for clinic / clinic-settings / clinic-invite chunks + index ≤50 kB ceiling.
- No new `s.user!` non-null assertions introduced anywhere in `src/types/clinic.ts`.
</verification>

<success_criteria>
1. `supabase db push --linked` completes with all 13 migrations applied (Task 2).
2. `npm run test:e2e:rls -- 'rls-(orgs|memberships|invites|roles|role-permissions|org-logos-storage)\.test\.ts'` returns 0 failures.
3. SQL probe `select count(*) from public.permissions` returns 10.
4. SQL probe `select count(*) from public.roles where is_system = true` returns 0 (no orgs created yet — no system roles seeded yet; gating proof that the trigger only fires on orgs INSERT).
5. After test creates 1 org: `select count(*) from public.roles where is_system = true` returns 3 (Owner / Coach / View-only).
6. `select pg_get_functiondef('public.has_permission(uuid,uuid,text)'::regprocedure)` shows STABLE + `search_path = public, extensions, pg_catalog`.
7. TypeScript typecheck (`npm run typecheck`) passes with no new errors after `src/types/clinic.ts` added.
</success_criteria>

<output>
After completion, create `.planning/phases/09-clinic-b2b-foundations/09-01-SUMMARY.md` with: migration list + RLS proof results + RPC inventory + any deviations from the planned migration content.
</output>
