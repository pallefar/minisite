---
phase: 41-public-status-page-embed-provider-blocks
plan: 06
type: execute
wave: 3
depends_on:
  - 41-02
  - 41-03
  - 41-04
  - 41-05
files_modified:
  - leanshot/src/lib/admin/modules.ts
  - leanshot/src/components/admin/embeds/AllowlistPage.tsx
  - leanshot/src/components/admin/embeds/AddHostnameForm.tsx
  - leanshot/src/components/admin/embeds/AllowlistTable.tsx
  - leanshot/src/components/admin/embeds/RemoveHostnameConfirm.tsx
  - leanshot/src/components/admin/embeds/ReferencesSheet.tsx
  - leanshot/src/components/admin/embeds/__tests__/AllowlistPage.test.tsx
  - leanshot/src/components/admin/embeds/__tests__/AddHostnameForm.test.tsx
  - leanshot/src/components/admin/embeds/__tests__/RemoveHostnameConfirm.test.tsx
  - leanshot/tests/smoke/status-page.smoke.test.ts
autonomous: false
requirements:
  - POLISH-10
  - EMBED-04
  - EMBED-07
tags:
  - admin
  - superadmin
  - allowlist
  - closeout
  - schema-push
  - human-uat

user_setup:
  - service: better-stack
    why: "POLISH-10 — Better Stack hosts the public status page (D-01..D-06). $12/mo paid tier required for Sentry + Vercel + Supabase integrations. Vendor-locked per CONTEXT canonical_refs."
    env_vars:
      - name: BETTERSTACK_API_KEY
        source: "Better Stack dashboard -> User profile -> API tokens -> Create token (read-only sufficient for smoke). Required for B6 smoke test: assert 7 components (D-01) + 3 integrations (D-02) via GET /api/v3/status-pages."
    dashboard_config:
      - task: "Better Stack account paid-tier upgrade ($12/mo)"
        location: "https://uptime.betterstack.com/team/billing"
      - task: "Configure 7-component hybrid hierarchy per D-01: top-level Patient App / Clinic App / Admin / Public Site; sub-grouped Auth / Database / Webhooks"
        location: "Better Stack → Status Pages → Components"
      - task: "Wire Sentry OAuth integration (org optimizenet, de.sentry.io); error-rate threshold >5% over 5min per D-02"
        location: "Better Stack → Integrations → Sentry"
      - task: "Wire Vercel deploy-hook (project leanshot-marketing); prod-deploy failures trigger incident per D-02"
        location: "Better Stack → Integrations → Vercel"
      - task: "Configure Supabase heartbeat URLs (4 user-facing surfaces) every 30s per D-02"
        location: "Better Stack → Monitors → Heartbeat"
      - task: "Add CNAME status.leanshot.app → Better Stack endpoint per D-05"
        location: "DNS registrar for leanshot.app zone"
      - task: "Upload LeanShot brand tokens per UI-SPEC §Color: primary #1b4842 (teal-700), hero bg #0f302c (teal-900), page bg #f2ede0 (cream-100), wordmark SVG, footer link https://leanshot.app"
        location: "Better Stack → Status Pages → Branding"

must_haves:
  truths:
    - "AdminShell renders a new 'Embeds' module entry at /admin/embeds, gated by minRole: 'superadmin' (D-17)."
    - "Superadmin opens /admin/embeds/allowlist and sees: header, add-hostname form, allowlist table with hostname/added-by/added/last-used/references/actions columns, empty state if no rows."
    - "Add-hostname form validates client-side (empty / protocol / path / wildcard / duplicate) per UI-SPEC §Surface E + calls add_iframe_allowlist_hostname RPC from Plan 41-02."
    - "Remove confirmation modal shows 2 copy variants (0-refs vs >=1-refs) per UI-SPEC §Surface E + calls remove_iframe_allowlist_hostname RPC."
    - "Non-superadmin staff member who reaches /admin/embeds/allowlist URL receives a 403-shaped denial (no module entry visible in sidebar + page-level role check)."
    - "Atomic CSP snapshot extension: any final tweaks to vercel.json from Plan 41-03 are reconciled in this plan with a matching tests/csp/csp-snapshot.txt regeneration committed in the same commit."
    - "Migrations from Plan 41-02 + Edge Fns from Plan 41-04 + Vercel middleware from Plan 41-03 deploy via the BLOCKING closeout task: supabase db push --linked AND supabase functions deploy."
    - "POLISH-10 smoke test asserts status.leanshot.app resolves (DNS smoke) + returns HTTP 200; gated by HUMAN-UAT operator approval after Better Stack setup."
  artifacts:
    - path: "leanshot/src/lib/admin/modules.ts"
      provides: "ADMIN_MODULES entry for 'embeds' module"
      contains: "embeds"
    - path: "leanshot/src/components/admin/embeds/AllowlistPage.tsx"
      provides: "Superadmin page wiring per UI-SPEC §Surface E"
      exports: ["AllowlistPage"]
    - path: "leanshot/src/components/admin/embeds/AddHostnameForm.tsx"
      provides: "Form with 5 client-side validation rules"
      exports: ["AddHostnameForm"]
    - path: "leanshot/src/components/admin/embeds/AllowlistTable.tsx"
      provides: "Table with 6 columns + sort + remove action"
      exports: ["AllowlistTable"]
    - path: "leanshot/src/components/admin/embeds/RemoveHostnameConfirm.tsx"
      provides: "Confirm modal with 2 copy variants"
      exports: ["RemoveHostnameConfirm"]
    - path: "leanshot/src/components/admin/embeds/ReferencesSheet.tsx"
      provides: "Sheet listing referencing pages"
      exports: ["ReferencesSheet"]
    - path: "leanshot/tests/smoke/status-page.smoke.test.ts"
      provides: "POLISH-10 smoke (dig + curl) — gated on Better Stack CNAME live"
  key_links:
    - from: "AddHostnameForm.tsx"
      to: "Plan 41-02 addHostname"
      via: "supabase.rpc('add_iframe_allowlist_hostname', {p_hostname})"
      pattern: "addHostname"
    - from: "modules.ts"
      to: "AllowlistPage lazy import"
      via: "lazy: () => import('@/components/admin/embeds/AllowlistPage')"
      pattern: "embeds/AllowlistPage"
---

<objective>
Close out Phase 41: ship the superadmin allowlist admin module (UI-SPEC §Surface E), reconcile the Phase 12 CSP snapshot if Plan 41-03 left any deltas, atomically run all phase migrations + Edge Fn deploys, and gate POLISH-10 Better Stack signoff via the HUMAN-UAT checkpoint.

Purpose: This is the phase closeout plan. It depends on Plans 41-02 (RPCs + admin client wrappers), 41-03 (CSP snapshot baseline + middleware), 41-04 (Calendly OAuth Edge Fns), and 41-05 (block UI). The closeout plan owns: (a) the new admin module entries that consume Plan 41-02's wrappers, (b) the BLOCKING `supabase db push --linked` step that pushes all Phase 41 migrations together (per `feedback_phase_close_out_db_push_verification`), (c) `supabase functions deploy` for the 2 Calendly OAuth Fns + the page-render redeploy, (d) HUMAN-UAT for POLISH-10 Better Stack vendor setup + DNS CNAME + smoke verification.

Output: 5 new admin embeds UI files + 3 test files, 1 modified admin manifest, 1 smoke test, BLOCKING deploy task with multi-signal HUMAN gate (per `feedback_multi_signal_human_verify_checkpoint_pattern`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-CONTEXT.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-RESEARCH.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-PATTERNS.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-UI-SPEC.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-02-SUMMARY.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-03-SUMMARY.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-04-SUMMARY.md
@.planning/phases/41-public-status-page-embed-provider-blocks/41-05-SUMMARY.md
@leanshot/src/lib/admin/modules.ts
@leanshot/src/components/admin/AdminShell.tsx

<interfaces>
From Plan 41-02:
- `addHostname(client, hostname)`, `removeHostname(client, id)`, `listHostnames(client)`, `AllowlistRow` from `@/lib/admin/iframe-allowlist`

ADMIN_MODULES entry shape (verified `modules.ts` lines 170-181 — anomaly entry is the canonical superadmin analog):
```typescript
{
  key: 'embeds',
  label: 'Embeds',
  route: 'embeds',                // W8 pin: matches sibling helpdesk/anomaly prefix-branch pattern (NOT 'embeds/allowlist')
  icon: ShieldCheckIcon,  // from lucide-react
  lazy: () => import('@/components/admin/embeds/AllowlistPage'),
  flagKey: 'admin.embeds.enabled',
  minRole: 'superadmin' as AdminRole,
}
```

W8 note: UI-SPEC §Surface E line 241 says `route:'embeds/allowlist'` — that is INCORRECT and must be updated to `route:'embeds'` to match the AdminShell prefix-branch convention (`pathname.startsWith('/admin/' + route + '/')`). The sub-route `/admin/embeds/allowlist` resolves automatically once `route:'embeds'` is in place because AllowlistPage owns the entire `/admin/embeds/*` subtree. Plan 41-06 Task 1 updates UI-SPEC line 241 to match.

AdminShell prefix-branch matching (verified `modules.ts:124`): `pathname.startsWith('/admin/${m.route}/')` — so `/admin/embeds/allowlist` resolves automatically once the module entry exists. Per memory `feedback_admin_module_manifest_vs_router_branch_drift`: existing prefix-branch handles sub-routes without additional switch wiring.

UI-SPEC §Surface E copy strings (verbatim, sentence case, no exclamation):
- Empty state heading: "No hostnames on the allowlist yet"
- Empty state body: "Add the first hostname above. Any page using the Custom-iframe block requires its hostname here first — exact match only."
- Add button: "Add hostname"
- 5 validation errors (verbatim from UI-SPEC §Copywriting Contract)
- Remove confirm: 2 body variants by reference count
- Success toasts: "Added [hostname] to allowlist" / "Removed [hostname] from allowlist"

Migration filenames (from Plan 41-02 — verbatim):
- `supabase/migrations/20270711000001_p41_iframe_allowlist.sql`
- `supabase/migrations/20270711000002_p41_iframe_allowlist_rpcs.sql`

Edge Functions to deploy (from Plan 41-04):
- `supabase functions deploy calendly-oauth-start --import-map supabase/functions/import_map.json` (per memory `reference_supabase_functions_deploy_import_map_flag`)
- `supabase functions deploy calendly-oauth-callback --import-map supabase/functions/import_map.json`
- `supabase functions deploy page-render --import-map supabase/functions/import_map.json` (redeploy with the Plan 41-03 changes)

Memory references applicable here:
- `reference_supabase_back_dated_migration_blocks_push` — pre-push check for back-dated files
- `reference_migration_timestamp_collision_precheck` — collision pre-check before push
- `feedback_phase_close_out_db_push_verification` — close-out plan owns the push
- `reference_supabase_worktree_temp_state` — copy `.temp/` before `--linked` if running in worktree
- `feedback_multi_signal_human_verify_checkpoint_pattern` — break HUMAN gate into discrete resume signals
- `feedback_spike_accept_deploy_evidence_defer_runtime_verify` — accept deploy evidence for items operator cannot test
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin module manifest + AllowlistPage + Add/Remove/Table/References UI</name>
  <files>leanshot/src/lib/admin/modules.ts, leanshot/src/components/admin/embeds/AllowlistPage.tsx, leanshot/src/components/admin/embeds/AddHostnameForm.tsx, leanshot/src/components/admin/embeds/AllowlistTable.tsx, leanshot/src/components/admin/embeds/RemoveHostnameConfirm.tsx, leanshot/src/components/admin/embeds/ReferencesSheet.tsx, leanshot/src/components/admin/embeds/__tests__/AllowlistPage.test.tsx, leanshot/src/components/admin/embeds/__tests__/AddHostnameForm.test.tsx, leanshot/src/components/admin/embeds/__tests__/RemoveHostnameConfirm.test.tsx</files>
  <behavior>
    - AllowlistPage Test 1 (superadmin gate + W8 route render): When current user role is 'staff' (NOT superadmin), AllowlistPage renders a 403-shaped denial via existing `surfaceCheck('admin.embeds.allowlist')` pattern; module entry is hidden from AdminShell sidebar. Additionally (W8): navigating to `/admin/embeds` (no sub-path) renders the AllowlistPage — verifies the AdminShell prefix-branch route resolution under `route:'embeds'`.
    - AllowlistPage Test 2 (empty state): With listHostnames mocked to return [], renders the empty-state EmptyState primitive with EXACT UI-SPEC §Surface E heading + body copy.
    - AllowlistPage Test 3 (loaded): With 3 rows, renders the AllowlistTable with 3 rows + the AddHostnameForm above.
    - AllowlistPage Test 4 (error state): listHostnames rejects → renders Card with --color-danger-soft bg + WifiOff icon + "We couldn't load the allowlist. Check your connection and try again." + Retry button.
    - AddHostnameForm Test 1: Empty input + click "Add hostname" → renders "Hostname is required" inline (aria-invalid='true' + --color-danger).
    - AddHostnameForm Test 2: Input 'https://meet.example.com' → renders "Enter hostname only — no protocol".
    - AddHostnameForm Test 3: Input 'meet.example.com/path' → renders "Enter hostname only — no path or query".
    - AddHostnameForm Test 4: Input '*.example.com' → renders "Wildcards and leading dots are not supported".
    - AddHostnameForm Test 5: Input '.example.com' (leading dot) → renders same wildcard error.
    - AddHostnameForm Test 6: Input 'meet.example.com' that already exists in the parent's allowlist prop → renders "[meet.example.com] is already on the allowlist".
    - AddHostnameForm Test 7 (success): Valid input → calls addHostname RPC + on success calls onAdded callback + clears input + dispatches Toast 'success' "Added meet.example.com to allowlist".
    - AddHostnameForm Test 8 (server error): addHostname rejects with 42501 (forbidden) → renders inline error "Only superadmins can add hostnames" (UX for the dual-layer denial).
    - RemoveHostnameConfirm Test 1 (0 refs): Modal title "Remove meet.example.com from allowlist?"; body variant "This hostname is not in use on any page. Removing it stops future embeds — existing pages are unaffected because no page references it."; confirm button label "Remove hostname".
    - RemoveHostnameConfirm Test 2 (>=1 refs): Body variant includes "[N] page" or "[N] pages" + the in-use warning copy; --color-danger-soft banner.
    - RemoveHostnameConfirm Test 3 (cancel): Cancel button label "Keep on allowlist" + Escape key closes (Modal primitive handles).
    - RemoveHostnameConfirm Test 4 (confirm): Calls removeHostname RPC; on success closes modal + dispatches Toast 'success' "Removed meet.example.com from allowlist".
  </behavior>
  <action>Step A — Open `leanshot/src/lib/admin/modules.ts`. Per 41-PATTERNS lines 467-487: locate the existing `anomaly` entry (superadmin-only sibling) around line 170-181. Add new entry BELOW it (anywhere in the array — `feedback_admin_module_manifest_vs_router_branch_drift` confirms order does not affect routing; AdminShell uses prefix-branch match). Entry shape per interfaces block above. Import `ShieldCheckIcon` from `lucide-react` (per UI-SPEC §Surface E icon list — already in lucide stdlib at v0.460.0).
Step B — Create `AllowlistPage.tsx` per UI-SPEC §Surface E. Top-level layout: padding-xl (32px) outer. H1 "Custom iframe allowlist" + subtitle "Hostnames on this list can be embedded via the Custom-iframe block on landing pages and KB articles. Exact-match only — no wildcards or subdomains." (VERBATIM). Use `surfaceCheck('admin.embeds.allowlist')` from existing P27 helper (the existing pattern — see `src/lib/org.ts`); on denial render the existing 403 component shape used by sibling admin pages. Fetches list via `listHostnames` from Plan 41-02 (useEffect on mount). Wires AddHostnameForm (above table) + AllowlistTable (below). Empty/loading/error states per UI-SPEC §Surface E (5 Skeleton rows for loading; EmptyState primitive for empty; Card with WifiOff for error).
Step C — Create `AddHostnameForm.tsx` per UI-SPEC §Surface E "Add-hostname form" section. Card variant='elevated' padding='lg' margin-top-lg. Inline form: Input (label "Hostname", placeholder "meet.example.com", hint "Exact hostname only — do not include https://, paths, or wildcards.") + Button variant='primary' label "Add hostname". Validation rules per UI-SPEC §Copywriting Contract: empty / protocol-present / path-present / wildcard / duplicate (passed in props.allowlist). Client-side checks BEFORE the RPC call. On success: call addHostname → on resolve, call onAdded callback + useToast tone='success' "Added [hostname] to allowlist".
Step D — Create `AllowlistTable.tsx`. Use proper `<table>`/`<thead>`/`<tbody>` semantics per UI-SPEC §Accessibility (NOT divs). Column-headers `scope='col'`; hostname cells `scope='row'`. 6 columns: Hostname (font-mono --text-sm + copy-to-clipboard icon button), Added by (InitialsAvatar + email), Added (relative timestamp + tooltip ISO), Last used (relative or "Never" italic), References (Badge --color-success-soft if 0 / --color-warning-soft if ≥1; clicking opens ReferencesSheet), Actions (Trash2 icon button → opens RemoveHostnameConfirm). Sort default = Added desc; column-click toggles sort with ChevronUp/Down. Touch targets 44×44 minimum per UI-SPEC §Spacing Exceptions.
Step E — Create `RemoveHostnameConfirm.tsx` using DSv2 `Confirm` primitive. Title interpolates hostname. Two body variants based on `referenceCount` prop (0 vs >=1). Confirm CTA variant='danger' "Remove hostname"; Cancel "Keep on allowlist". Calls removeHostname on confirm + useToast on success.
Step F — Create `ReferencesSheet.tsx` (DSv2 Sheet primitive). Lists referencing pages — for v1.3, the reference-count source is a query over `landing_page_revisions.blocks` JSONB (planner picks — simplest: a server-side helper or RPC that scans current published revisions and returns rows {page_id, page_title, slug, last_edited_at}). If a real query is too heavy for v1.3, accept a SIMPLE "[N] page[s]" badge with the Sheet returning a TODO placeholder + a v1.4 enhancement note IN THE SUMMARY (NOT in the plan output — per `feedback_partial_phase_carry_over_md_pattern`, surface as a CARRY-OVER if needed). For v1.3 close-out, accept empty list with caption "Reference scanning ships in v1.4; remove with caution if last-used timestamp is recent" — this is acceptable scope per RESEARCH Open Question 4 + UI-SPEC §Surface E badge design (badge still uses the simple reference_count from the iframe_allowlist table once last_used_at is wired by middleware Plan 41-03; we already have one signal — "last_used_at IS NULL" → safe-to-remove badge "Unused").
Step G — Test files cover 4 + 8 + 4 = 16 behaviors above. Mock listHostnames/addHostname/removeHostname via vitest. Use @testing-library/react.
Per `feedback_executor_tdd_scaffolds_sibling_files`: each test must mock the supabase client + RPC wrappers, NOT call live RPCs (live integration is verified by HUMAN-UAT in Task 3).
Per memory `reference_vitest_skip_fixme`: do NOT use `it.fixme` — use `it.skip(...)` with explicit `[DEFERRED — ...]` tag if any behavior is too costly to mock.</action>
  <verify>
    <automated>cd leanshot && npx vitest run src/components/admin/embeds/__tests__/ && npx tsc -p tsconfig.app.json --noEmit && grep -q "key: 'embeds'" src/lib/admin/modules.ts && grep -q "minRole: 'superadmin'" src/lib/admin/modules.ts</automated>
  </verify>
  <done>16 unit tests green; tsc strict clean; ADMIN_MODULES has 'embeds' entry with superadmin gate + lazy import; UI copy matches UI-SPEC §Surface E verbatim.</done>
</task>

<task type="auto">
  <name>Task 2a [W7 split — auto]: Create POLISH-10 smoke test file with B6 Better Stack API assertion</name>
  <files>leanshot/tests/smoke/status-page.smoke.test.ts</files>
  <action>Create `leanshot/tests/smoke/status-page.smoke.test.ts` — vitest smoke. Per Plan 41-PATTERNS lines 17-18 + RESEARCH Validation Architecture. Three assertion groups:

**Group 1 — DNS smoke (D-05):** `dig +short CNAME status.leanshot.app` (run via `child_process.execSync`) returns a non-empty Better Stack endpoint. If empty: `it.skip('CNAME not yet configured — Better Stack DNS pending HUMAN-UAT')` per `reference_vitest_skip_fixme` (NOT `it.fixme` — that throws TypeError on vitest 4.x).

**Group 2 — HTTP smoke:** `curl -sIL https://status.leanshot.app | head -1` returns `HTTP/2 200`. Skip if Group 1 skipped.

**Group 3 — B6 Better Stack API assertion (NEW per B6):** read `process.env.BETTERSTACK_API_KEY` (from user_setup; injected via `vercel env` or local `.env.test` for CI). When key is present, fetch `GET https://uptime.betterstack.com/api/v3/status-pages` with `Authorization: Bearer ${BETTERSTACK_API_KEY}` header. Parse JSON response and assert: (a) `data.length >= 1` (at least one status page configured); (b) for the first status page, fetch `GET /api/v3/status-pages/${id}/sections` and verify section count >= 4 + total component count = 7 (D-01 hybrid 7-component shape); (c) fetch `GET /api/v3/status-pages/${id}/resources` and verify integration count >= 3 (Sentry + Vercel + Supabase per D-02). When `BETTERSTACK_API_KEY` is unset (pre-deploy / pre-vendor-setup), skip Group 3 with `it.skip('BETTERSTACK_API_KEY unset — POLISH-10 success criterion 1 deferred to milestone close per feedback_milestone_uat_deferral_consolidation')`.

Test MUST tolerate the "Better Stack not yet wired" state and not fail CI — gracefully skip until vendor setup + DNS is live. Per `feedback_hitl_walkthrough_deferred_when_fixtures_missing` + `feedback_spike_accept_deploy_evidence_defer_runtime_verify`: deferred-runtime-verify is acceptable; when skipped, the smoke test file's documented `it.skip` reason text is the audit trail that gets carried into `v1.3-uat-deferred.md` per `feedback_milestone_uat_deferral_consolidation`.

POLISH-10 success criterion 1 (auto-incident detection per D-02 thresholds) is covered by Group 3's assertion that >=3 integrations exist. When BETTERSTACK_API_KEY is unset, this criterion is automatically classified as "approved automated-verify-only" via the skip + carry-over annotation. Plan 41-06 closeout SUMMARY MUST log the classification.</action>
  <verify>
    <automated>cd leanshot &amp;&amp; npx vitest run tests/smoke/status-page.smoke.test.ts &amp;&amp; grep -q "BETTERSTACK_API_KEY" tests/smoke/status-page.smoke.test.ts &amp;&amp; grep -q "/api/v3/status-pages" tests/smoke/status-page.smoke.test.ts &amp;&amp; grep -q "component" tests/smoke/status-page.smoke.test.ts</automated>
  </verify>
  <done>Smoke test file exists at the exact path; references BETTERSTACK_API_KEY env + /api/v3/status-pages endpoint + 7-component + 3-integration assertions per B6; test passes inline OR gracefully-skips with documented carry-over reason text matching `feedback_milestone_uat_deferral_consolidation` template.</done>
</task>

<task type="checkpoint:autonomous-with-gate" gate="blocking">
  <name>Task 2b [W7 split — BLOCKING]: Atomic phase deploy chain — db push + Edge Fn deploys + Vercel push + secrets</name>
  <what-built>
This is the BLOCKING phase-closeout deploy chain per `feedback_phase_close_out_db_push_verification`. The deploy chain runs autonomously (no human input mid-chain), but the chain itself BLOCKS subsequent steps — each step's success gates the next. The gate is "deploy chain success" — on any step failure, executor halts and reports.
  </what-built>
  <how-to-verify>
Execute the 8 steps below in strict order. NO single command runs in background.

**Step A — Pre-push migration audit.** Per memory `reference_supabase_back_dated_migration_blocks_push`: run `ls supabase/migrations/ | sort | tail -10` and verify no migration filename precedes the remote tail (was `20270710000006_p36_review_funnel_aggregate_rpc.sql` per brief; in repo currently `20270709000008_p40_roi_view.sql` — Phase 41 migrations use `20270711000001+` per Plan 41-02 + the brief mandate). Per `reference_migration_timestamp_collision_precheck`: `ls supabase/migrations/20270711*.sql | wc -l` MUST equal 2 (the two Phase 41 files). If ≠ 2, ABORT and renumber.

**Step B — `supabase db push --linked`** from the primary checkout (per `feedback_worktree_executor_pwd_drift_leaks_to_main`: verify `git rev-parse --show-toplevel` matches `/Users/karstenhaldan/minisite` BEFORE the push command). Capture output; grep for "Skipping" (per `reference_supabase_migration_filename_regex`) — any skip = ABORT and rename. Expected: 2 new migrations applied (20270711000001 + 20270711000002).

**Step C — Deploy 3 Edge Fns:** `supabase functions deploy calendly-oauth-start --import-map supabase/functions/import_map.json` then `supabase functions deploy calendly-oauth-callback --import-map supabase/functions/import_map.json` then `supabase functions deploy page-render --import-map supabase/functions/import_map.json` (redeploy with the Plan 41-03 changes — page-render now requires `iframe_allowlist` orchestrator wiring per B3). Per memory `reference_supabase_functions_deploy_no_linked_flag`: do NOT pass `--linked` to functions deploy. Per `reference_supabase_functions_deploy_import_map_flag`: the deprecated `--import-map` flag is still required and honored.

**Step D — Set Supabase Function Secrets:** `supabase secrets set CALENDLY_OAUTH_CLIENT_ID=<from user_setup> CALENDLY_OAUTH_CLIENT_SECRET=<from user_setup> CALENDLY_OAUTH_REDIRECT_URI=<from user_setup> LEANSHOT_APP_ORIGIN=https://app.leanshot.app OAUTH_STATE_SECRET=<random 32-byte hex>`. Per `reference_supabase_service_role_key_format_divergence`: the new `sb_secret_*` token format is what the CLI accepts (do NOT use the legacy HS256 JWT). Verify with `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp`.

**Step E — Vercel deploy.** Push the commit (containing middleware.ts + vercel.json CSP+rewrites changes + csp-snapshot.txt + smoke test) to origin/main (verify per `feedback_push_origin_before_parallel_worktree_dispatch` that local main is in sync; if N commits ahead, `git push origin main` first). Vercel auto-deploys; capture the preview URL via `vercel --prod=false` or read from the Vercel dashboard. Per memory `reference_vercel_project`: project ref `prj_vUAbx6chhVpKWnAT9IBFWOLhnYbc`.

**Step F — Set Vercel env vars** (CSP report URI from user_setup): `echo "<sentry-csp-report-uri>" | vercel env add VITE_SENTRY_CSP_REPORT_URI production`. The Plan 41-03 Task 2 middleware reads this env at request time to assemble the CSP `report-uri` directive (B4).

**Step G — Set BETTERSTACK_API_KEY** (B6 smoke dependency): `echo "<betterstack-api-key>" | vercel env add BETTERSTACK_API_KEY production` AND copy to local `.env.test` for local smoke runs. Re-run smoke (`cd leanshot && npx vitest run tests/smoke/status-page.smoke.test.ts`) — Group 3 B6 assertions should now run inline (not skipped).

**Step H — Post-deploy verification:** `curl -sIL https://app.leanshot.app/embed-test-page 2>/dev/null | grep -i "content-security-policy" | grep -q "calendly\\|tally\\|youtube"` to verify the served CSP includes the D-12 additions AND the middleware-assembled `report-uri` directive (B4). Per `feedback_post_merge_deno_sweep_pattern`: also run `$HOME/.deno/bin/deno test --no-check --allow-read --allow-env supabase/functions/page-render/ supabase/functions/calendly-oauth-start/ supabase/functions/calendly-oauth-callback/` if Deno tests exist.

On all 8 steps succeeding, proceed to Task 4 (HUMAN-UAT multi-signal checkpoint). On any step failing, executor STOPS and reports the failing step + captured output.
  </how-to-verify>
  <resume-signal>Auto-resume on full deploy-chain success. On step failure, executor halts and reports — operator unblocks before continuing.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 [CHECKPOINT]: Multi-signal HUMAN-UAT for POLISH-10 Better Stack + Phase 41 close-out</name>
  <what-built>
Phase 41 is now code-complete: 4 retrofit + 1 new embed block types ship with consent-gating, the Custom-iframe block + superadmin allowlist UI lives at /admin/embeds/allowlist, the Vercel Edge Middleware augments CSP frame-src dynamically, Calendly inline preview uses popup-OAuth, all Phase 12 CSP snapshot tests pass, and all Phase 41 migrations + Edge Fns are deployed.

What remains is the founder-side HUMAN-UAT for POLISH-10 (Better Stack vendor setup + DNS CNAME) and the EMBED-08 Calendly OAuth credentials setup. These are vendor-side actions only the operator can perform.
  </what-built>
  <how-to-verify>
Per `feedback_multi_signal_human_verify_checkpoint_pattern`: this checkpoint is broken into discrete resume signals. Approve each separately; un-approved signals carry to milestone close.

**Signal 1 — Better Stack vendor setup (POLISH-10 D-06):**
1. Visit https://uptime.betterstack.com/team/billing — verify paid-tier subscription active ($12/mo).
2. Visit Status Pages → Components — verify the 7-component hybrid hierarchy per D-01: top-level Patient App / Clinic App / Admin / Public Site; sub-grouped Auth / Database / Webhooks.
3. Visit Integrations → confirm Sentry (org optimizenet, error rate threshold >5%/5min), Vercel (project leanshot-marketing deploy-hook), Supabase heartbeats (30s on 4 user-facing surfaces).
4. Visit Branding — confirm LeanShot tokens applied: primary #1b4842, hero bg #0f302c, page bg #f2ede0, wordmark uploaded, footer "Back to LeanShot" link to https://leanshot.app.
Resume signal: type `bstack-approved` or list issues.

**Signal 2 — DNS CNAME live (POLISH-10 D-05):**
1. Run `dig +short CNAME status.leanshot.app` — expect Better Stack endpoint.
2. Open https://status.leanshot.app — expect Better Stack status page rendering with LeanShot branding (no Vercel default).
3. Run smoke test: `cd leanshot && npx vitest run tests/smoke/status-page.smoke.test.ts` — expect green (no skips).
Resume signal: type `cname-live` or list issues.

**Signal 3 — Calendly OAuth credentials (EMBED-08):**
1. Open https://developer.calendly.com/ → My Apps — verify LeanShot OAuth app created with redirect URI exactly `https://app.leanshot.app/api/calendly/oauth-callback` (per user_setup).
2. Verify Function Secrets are set in Supabase: `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp | grep CALENDLY` — expect CALENDLY_OAUTH_CLIENT_ID + CALENDLY_OAUTH_CLIENT_SECRET + CALENDLY_OAUTH_REDIRECT_URI.
3. Open PageEditor → drop a Calendly block → click "Connect Calendly" in the inline preview (Surface D) → verify popup opens to Calendly OAuth → complete auth → verify popup closes + Surface D shows State D3 (connected-account caption).
Resume signal: type `calendly-oauth-approved` or list issues.

**Signal 4 — Allowlist superadmin UI (EMBED-07 + D-17):**
1. Log in as superadmin → AdminShell sidebar shows new "Embeds" entry.
2. Navigate to /admin/embeds/allowlist → empty state visible.
3. Add a test hostname `embed-test.leanshot.app` via the form → verify Toast success + table row appears.
4. Verify audit log row: `supabase db query --linked "select action_name, metadata from public.audit_logs where action_name = 'iframe_allowlist.add' order by created_at desc limit 1"` — expect 1 row with `metadata->>'hostname' = 'embed-test.leanshot.app'`.
5. Click Remove → confirm modal shows "0-references" body variant → confirm → row gone + Toast + new audit row `iframe_allowlist.remove`.
6. Log in as non-superadmin → /admin/embeds/allowlist → 403-shaped denial + module entry hidden from sidebar.
Resume signal: type `allowlist-approved` or list issues.

**Signal 5 — Consent-gating live verification (EMBED-01/02/03/04/05):**
1. Open any LeanShot landing page with a published Calendly/YouTube/Tally block in a fresh incognito window → expect the branded placeholder card (UI-SPEC §Surface B State 1) — NOT the iframe.
2. Click "Manage cookie preferences" → P22 banner opens → grant required category → expect Skeleton → iframe with opacity transition.
3. Open DevTools Network tab → grant marketing → reload — confirm no Calendly/YouTube/Tally requests fire BEFORE consent grant.
4. Open DevTools Application → Cookies — verify no cross-origin cookies set BEFORE consent grant for any provider.
Resume signal: type `consent-gating-approved` or list issues.

**Signal 6 — Custom-iframe end-to-end (EMBED-07):**
1. Add a Custom-iframe hostname `youtube-nocookie.com` via the allowlist UI.
2. In PageEditor, drop a Custom-iframe block with embedUrl `https://youtube-nocookie.com/embed/dQw4w9WgXcQ` + iframeTitle "Test custom embed".
3. Publish the page → visit it in incognito → expect placeholder until marketing consent → grant → iframe loads sandboxed.
4. View page source — verify CSP `frame-src` header includes `https://youtube-nocookie.com` (D-14 dynamic injection via Vercel middleware).
5. Remove the hostname from allowlist → reload published page → expect Custom-iframe block to gracefully degrade (placeholder only, iframe never hydrates because validator returns null at server render).
Resume signal: type `custom-iframe-approved` or list issues.

Per `feedback_spike_accept_deploy_evidence_defer_runtime_verify`: any signal that requires unavailable test fixtures (no published embed pages yet, no Better Stack paid tier) may be deferred to milestone close with documented `[DEFERRED — see v1.3-uat-deferred.md]` annotation. Use `feedback_milestone_uat_deferral_consolidation` pattern at v1.3 milestone close.
  </how-to-verify>
  <resume-signal>Type any of: `bstack-approved`, `cname-live`, `calendly-oauth-approved`, `allowlist-approved`, `consent-gating-approved`, `custom-iframe-approved`, or `all-approved`. Un-approved signals will be carried to v1.3 milestone close per multi-signal pattern.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Superadmin browser → SECDEF RPC | User JWT carries role claim; RPC re-verifies server-side. |
| Better Stack vendor → DNS | CNAME makes Better Stack authoritative for status.leanshot.app — vendor lock per D-04. |
| supabase CLI → remote Postgres | Migration push has elevated privilege; collision/back-date checks gate the push. |
| Vercel CDN → middleware → response | Middleware injection trusts the Supabase REST allowlist endpoint (public-SELECT). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06-01 | Elevation of Privilege | Non-superadmin reaches /admin/embeds/allowlist by typing URL directly | mitigate | Dual-layer: ADMIN_MODULES entry has `minRole: 'superadmin'` (hides from sidebar); AllowlistPage.tsx calls `surfaceCheck('admin.embeds.allowlist')` at page-level + renders 403 on denial. RPCs additionally re-verify via `is_admin_at_least('superadmin')`. Task 1 Test 1 covers. |
| T-41-06-02 | Tampering | Migration timestamp collision causes silent skip | mitigate | Task 2 Step A pre-push audit per `reference_migration_timestamp_collision_precheck` — `ls 20270711*.sql | wc -l` MUST equal 2. ABORT path if not. |
| T-41-06-03 | Tampering | Back-dated migration blocks push | mitigate | Task 2 Step A per `reference_supabase_back_dated_migration_blocks_push` — `supabase migration list --linked` audit before push; if remote has files ahead of local, follow the mv-to-tmp recovery pattern. |
| T-41-06-04 | Information Disclosure | Calendly client_secret leaked via Vercel env var into client bundle | mitigate | Task 2 Step D sets CALENDLY_OAUTH_CLIENT_SECRET as a SUPABASE Function Secret (Deno.env), NOT a Vercel env var. Per `reference_vite_static_env_inlining`: only `VITE_*` prefixed env vars get inlined client-side; non-prefixed never reach the browser. |
| T-41-06-05 | Repudiation | Better Stack subscriber emails lost on vendor switch | accept | Documented in CONTEXT D-04 (deferred) + RESEARCH §Pitfall 4. Accepted vendor lock. |
| T-41-06-06 | Denial of Service | Vercel middleware fetches allowlist on every request → Supabase REST throttling | mitigate | 60s in-memory cache per Plan 41-03 Pattern 3. v1.3 traffic estimate <10 req/s/region; cache amortizes to ≤1 fetch/min/region. Per RESEARCH Assumption A5: allowlist size <1000 rows accepted. |
| T-41-06-07 | Tampering | Edge Fn deploy succeeds but with stale module cache | mitigate | Task 2 Step C deploys via `supabase functions deploy <name>` (CLI v2.100+ per memory `reference_supabase_functions_deploy_no_linked_flag`) — bundles fresh from local source; no caching layer. Verify deploy timestamp post-Step C. |
| T-41-06-08 | Information Disclosure | Audit log retention exceeded for sensitive admin actions | accept | D-17 specifies 90d; managed by existing `audit_retention_cron` (no Phase 41 change per 41-PATTERNS line 589). |
</threat_model>

<verification>
- `cd leanshot && npx vitest run src/components/admin/embeds/__tests__/` — 16 unit tests green.
- `cd leanshot && npx tsc -p tsconfig.app.json --noEmit` — strict TS clean.
- `cd /Users/karstenhaldan/minisite && supabase migration list --linked 2>&1 | grep -E "20270711000001|20270711000002" | wc -l` returns 2 (Task 2b Step B).
- `grep -q "BETTERSTACK_API_KEY" leanshot/tests/smoke/status-page.smoke.test.ts` (B6: smoke asserts Better Stack API integration count).
- `supabase secrets list --project-ref ytnsipxxmzgaebkqmokp 2>&1 | grep -E "CALENDLY_OAUTH_(CLIENT_ID|CLIENT_SECRET|REDIRECT_URI)" | wc -l` returns 3.
- `supabase functions list --project-ref ytnsipxxmzgaebkqmokp 2>&1 | grep -E "calendly-oauth-(start|callback)" | wc -l` returns 2.
- `cd leanshot && npx vitest run tests/smoke/status-page.smoke.test.ts` returns green OR gracefully-skipped with documented HUMAN-UAT carry-over.
- `cd leanshot && npx vitest run tests/csp/csp-snapshot.test.ts` green — final CSP matches snapshot.
- `cd leanshot && npm run build` succeeds.
- All 6 HUMAN-UAT signals either approved or annotated in v1.3-uat-deferred.md per `feedback_milestone_uat_deferral_consolidation`.
</verification>

<success_criteria>
- 5 new admin embeds UI files + 3 test files + 1 modified manifest + 1 smoke test land.
- ADMIN_MODULES has 'embeds' entry; AllowlistPage renders 4 states (loading/empty/loaded/error).
- 16 unit tests green covering allowlist CRUD UX.
- `supabase db push --linked` applied both Phase 41 migrations (collision pre-check clean; back-date audit clean).
- 3 Edge Functions deployed (page-render redeploy + 2 Calendly OAuth Fns).
- Vercel main deploy includes middleware.ts + extended CSP + snapshot.
- HUMAN-UAT 6 signals — operator approves inline + carries deferred signals to v1.3-uat-deferred.md.
- Phase 41 status flips to "complete" once Signals 4+5+6 (code-side) approved; Signals 1+2+3 (vendor-side) may carry per milestone-UAT-deferral pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/41-public-status-page-embed-provider-blocks/41-06-SUMMARY.md` describing: (1) the admin module entry + lazy-load wiring, (2) the BLOCKING deploy chain executed (migrations applied + Edge Fns deployed + Vercel push), (3) all 6 HUMAN-UAT signal statuses (approved/deferred — link to v1.3-uat-deferred.md for any deferrals), (4) confirmation that POLISH-10 Better Stack carry-over (if any) lands per `feedback_milestone_uat_deferral_consolidation`, (5) phase status — `complete + approved automated-verify-only` if any vendor-side signal deferred; `complete` if all six signals approved inline.
</output>
