---
phase: 16-capacitor-mobile-shells-ios-android
plan: 07
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-04]
files_modified:
  - apps/ios/App/App/PrivacyInfo.xcprivacy
  - apps/ios/App/App/Info.plist
  - apps/ios/App/App/App.entitlements
  - apps/android/data-safety.md
  - scripts/audit-privacy-manifest.mjs
  - .github/workflows/mobile-privacy-audit.yml
  - package.json
autonomous: true
requirements: [MOBILE-05]

must_haves:
  truths:
    - "iOS build embeds PrivacyInfo.xcprivacy declaring every required-reason API used by the 14-plugin inventory (D-07/D-18)"
    - "Info.plist contains NSFaceIDUsageDescription, NSHealthShareUsageDescription stub, NSPhotoLibraryUsageDescription, plus biometric+associated-domains entitlements"
    - "App.entitlements declares com.apple.developer.associated-domains for applinks:leanshot.app + applinks:app.leanshot.app (D-09)"
    - "apps/android/data-safety.md captures every Play Data Safety form answer with rationale, ready to copy-paste into Play Console"
    - "scripts/audit-privacy-manifest.mjs exits 0 when manifest matches plugin inventory + Apple canonical reason-code list, exits 1 with ::error:: annotation on any miss"
    - "A dedicated GitHub Actions workflow runs the audit on every PR touching apps/ios/App/App/PrivacyInfo.xcprivacy or the 14 plugin packages"
    - "HealthKit privacy entry declares NSPrivacyCollectedDataTypeLinked=false per CONTEXT D-18 (HealthKit data NOT linked to user identity)"
  artifacts:
    - path: "apps/ios/App/App/PrivacyInfo.xcprivacy"
      provides: "Apple Privacy Manifest — declares required-reason APIs + tracking + collected data types"
      contains: "NSPrivacyAccessedAPITypes, NSPrivacyTracking, NSPrivacyCollectedDataTypes"
    - path: "apps/ios/App/App/Info.plist"
      provides: "iOS usage-string declarations + Associated Domains reference"
      contains: "NSFaceIDUsageDescription, NSHealthShareUsageDescription, NSPhotoLibraryUsageDescription"
    - path: "apps/ios/App/App/App.entitlements"
      provides: "Universal Links entitlement (applinks:) + keychain access group for biometric credential storage"
      contains: "com.apple.developer.associated-domains"
    - path: "apps/android/data-safety.md"
      provides: "Operator-readable Play Data Safety form answers + change log for re-submission"
      min_lines: 80
    - path: "scripts/audit-privacy-manifest.mjs"
      provides: "CI-gate validator — diffs declared APIs vs Apple canonical reason-code list + plugin inventory"
      min_lines: 120
    - path: ".github/workflows/mobile-privacy-audit.yml"
      provides: "GitHub Actions job that runs audit-privacy-manifest.mjs on relevant PRs"
      contains: "audit-privacy-manifest"
  key_links:
    - from: "scripts/audit-privacy-manifest.mjs"
      to: "apps/ios/App/App/PrivacyInfo.xcprivacy"
      via: "fs.readFileSync + plist/XML parse"
      pattern: "PrivacyInfo\\.xcprivacy"
    - from: "scripts/audit-privacy-manifest.mjs"
      to: "package.json dependencies"
      via: "fs.readFileSync of package.json → cross-check 14-plugin inventory"
      pattern: "package\\.json"
    - from: ".github/workflows/mobile-privacy-audit.yml"
      to: "scripts/audit-privacy-manifest.mjs"
      via: "node scripts/audit-privacy-manifest.mjs"
      pattern: "audit-privacy-manifest\\.mjs"
    - from: "apps/ios/App/App/Info.plist"
      to: "App.entitlements"
      via: "CodeSignEntitlements build setting (Xcode-generated by 16-01 cap add ios)"
      pattern: "associated-domains"
---

<objective>
Ship a complete, auditable iOS Privacy Manifest + Google Play Data Safety package so the first App Store + Play Store submission clears MOBILE-05.

Purpose: Apple now rejects builds without a complete `PrivacyInfo.xcprivacy` declaring every required-reason API used by the binary (including those reached transitively through Capacitor plugins). Google Play requires a complete Data Safety form before any public-track release. Without both, MOBILE-05 fails and submission is blocked.

Output: Hand-crafted `PrivacyInfo.xcprivacy` from the 14-plugin inventory (D-07/D-18), populated `Info.plist` usage strings + Associated Domains entitlement, populated `data-safety.md` for Play Console copy-paste, working `audit-privacy-manifest.mjs` CI gate, and a dedicated `mobile-privacy-audit.yml` workflow that runs the gate on every PR touching the manifest or plugin set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-capacitor-mobile-shells-ios-android/16-CONTEXT.md
@.planning/phases/16-capacitor-mobile-shells-ios-android/16-RESEARCH.md
@.planning/phases/16-capacitor-mobile-shells-ios-android/16-PATTERNS.md
@scripts/assert-bundle-budget.sh

<file_ownership_coordination>
**SHARED-FILE RISK with sibling plan 16-09 (Wave 3, parallel).**

- Plan 16-09 CREATES `.github/workflows/mobile.yml`. If this plan also wrote to that file, parallel-wave execution would hit an add/add merge conflict (per `feedback_parallel_executor_git_isolation.md`).
- **Resolution:** This plan creates a SEPARATE workflow file `.github/workflows/mobile-privacy-audit.yml` with only the privacy-audit job. `mobile.yml` (16-09) is untouched.
- 16-09's plan still references invoking `audit-privacy-manifest.mjs` from its build lanes if desired — that's a 16-09 concern; this plan only ensures the script is callable and a PR-scope workflow exists.
- All `git add` calls in this plan MUST use pathspec (`git add -- <path>`) per the project rule.

**Wave-0 stub coordination with 16-00:** 16-00 creates `scripts/audit-privacy-manifest.mjs` as a STUB (per outline). This plan FILLS that stub with the real diff logic. If 16-00's stub file is present, treat it as a placeholder and overwrite. If absent (16-00 deferred the stub), create the file fresh.
</file_ownership_coordination>

<interfaces>
<!-- Existing CI-gate script convention (analog: scripts/assert-bundle-budget.sh). Reuse pattern. -->

From scripts/assert-bundle-budget.sh:
```bash
#!/usr/bin/env bash
set -euo pipefail
# Emit GitHub Actions annotations on failure:
#   echo "::error::<message>"
#   exit 1
# Success path emits a single summary line + exit 0.
```

<!-- The 14-plugin inventory this plan must enumerate, from 16-RESEARCH.md "Standard Stack" table. -->
<!-- Each plugin → 0..N PrivacyInfo required-reason API entries. -->

14-plugin set + RC + biometric + Sentry + virtuoso (from 16-CONTEXT.md D-07 + RESEARCH §"Standard Stack"):
- @capacitor/core               (8.3.4)   → required: SystemBootTime (35F9.1), DiskSpace (E174.1), UserDefaults (CA92.1) when Preferences used transitively
- @capacitor/cli                (devOnly) → no runtime privacy entry
- @capacitor/ios                (build)   → no runtime privacy entry
- @capacitor/android            (build)   → N/A (Android)
- @capacitor/app                (8.1.0)   → no required-reason API
- @capacitor/preferences        (8.0.1)   → UserDefaults (CA92.1)
- @capacitor/share              (8.0.1)   → no required-reason API
- @capacitor/splash-screen      (8.0.1)   → no required-reason API
- @capacitor/status-bar         (8.0.2)   → no required-reason API
- @capacitor/haptics            (8.0.2)   → no required-reason API
- @capacitor/browser            (8.0.3)   → no required-reason API
- @capacitor/keyboard           (8.0.3)   → no required-reason API
- @capacitor/network            (8.0.1)   → no required-reason API
- @capacitor/filesystem         (8.1.2)   → FileTimestamp (C617.1), DiskSpace (E174.1)
- @capacitor/clipboard          (8.0.1)   → no required-reason API
- @revenuecat/purchases-capacitor (13.1.1)→ UserDefaults (CA92.1) per RC SDK README; tracking domain = api.revenuecat.com
- @capgo/capacitor-native-biometric (8.4.5) → no required-reason API (Keychain + LAContext only); requires NSFaceIDUsageDescription
- @sentry/capacitor             (4.0.0)   → SystemBootTime (35F9.1) for crash context; UserDefaults (CA92.1) for breadcrumb storage; tracking domain = *.ingest.sentry.io
- react-virtuoso                (4.18.7)  → JS-only, no native API
- @capgo/capacitor-updater      (8.46.0)  → NOT initialized in P16 (D-12 bundled-only); install-only; FileTimestamp + DiskSpace if/when enabled — declare as STUB comment for v1.2.1 fill

Apple canonical required-reason API list (verified RESEARCH §"References"):
- NSPrivacyAccessedAPICategoryUserDefaults       — reason codes: CA92.1, C56D.1, 1C8F.1, AC6B.1
- NSPrivacyAccessedAPICategoryFileTimestamp      — reason codes: C617.1, 3B52.1, 0A2A.1, 3D61.1
- NSPrivacyAccessedAPICategorySystemBootTime     — reason codes: 35F9.1, 8FFB.1, 3D61.1
- NSPrivacyAccessedAPICategoryDiskSpace          — reason codes: E174.1, 85F4.1, 7D9E.1, B728.1
- NSPrivacyAccessedAPICategoryActiveKeyboards    — reason codes: 54BD.1, 8C5D.1, 3EC4.1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-craft PrivacyInfo.xcprivacy + Info.plist usage strings + App.entitlements (D-18)</name>
  <files>apps/ios/App/App/PrivacyInfo.xcprivacy, apps/ios/App/App/Info.plist, apps/ios/App/App/App.entitlements</files>
  <read_first>
    - apps/ios/App/App/Info.plist — file is generated by `npx cap add ios` in 16-01; read current contents to know which usage strings already exist vs need adding (per D-07 plugin inventory the Capacitor template only adds `NSCameraUsageDescription`/`NSPhotoLibraryUsageDescription` placeholders)
    - apps/ios/App/App/App.entitlements — generated by 16-01; if absent, must be created and wired into the Xcode target's `CODE_SIGN_ENTITLEMENTS` build setting
    - .planning/phases/16-capacitor-mobile-shells-ios-android/16-RESEARCH.md §"Standard Stack" (lines 115-140) for plugin-to-reason-code mapping
    - .planning/phases/16-capacitor-mobile-shells-ios-android/16-CONTEXT.md D-06 (biometric plugin), D-09 (Universal Links domains), D-18 (hand-crafted manifest), D-17 (Sentry coverage)
  </read_first>
  <action>
PrivacyInfo.xcprivacy (plist XML format — root `<plist version="1.0"><dict>...</dict></plist>`) declares these top-level keys per Apple's schema:

1. `NSPrivacyAccessedAPITypes` (array of dicts) — declare each required-reason API category + reason codes. Per the plugin-inventory mapping in the `<interfaces>` block, declare exactly these four categories:
   - `NSPrivacyAccessedAPICategoryUserDefaults` with `NSPrivacyAccessedAPITypeReasons = [CA92.1]` (covers @capacitor/preferences + @revenuecat/purchases-capacitor + @sentry/capacitor breadcrumb persistence — single CA92.1 entry suffices)
   - `NSPrivacyAccessedAPICategoryFileTimestamp` with `NSPrivacyAccessedAPITypeReasons = [C617.1]` (covers @capacitor/filesystem — installed per D-07 even though feature deferred to v1.3)
   - `NSPrivacyAccessedAPICategorySystemBootTime` with `NSPrivacyAccessedAPITypeReasons = [35F9.1]` (covers @capacitor/core base + @sentry/capacitor crash-context startup-time)
   - `NSPrivacyAccessedAPICategoryDiskSpace` with `NSPrivacyAccessedAPITypeReasons = [E174.1]` (covers @capacitor/filesystem + @sentry/capacitor disk-buffer when offline)

2. `NSPrivacyTracking` = `<false/>` (we do not link with third-party SDKs for ad tracking; Sentry + RevenueCat are infrastructure, not tracking).

3. `NSPrivacyTrackingDomains` = `<array/>` (empty array — required when NSPrivacyTracking=false).

4. `NSPrivacyCollectedDataTypes` (array of dicts) — declare data we collect, paired with `NSPrivacyCollectedDataTypeLinked` + `NSPrivacyCollectedDataTypeTracking` + `NSPrivacyCollectedDataTypePurposes`. Required entries per CONTEXT D-18:
   - `NSPrivacyCollectedDataTypeEmailAddress` → Linked=true, Tracking=false, Purposes=[`NSPrivacyCollectedDataTypePurposeAppFunctionality`, `NSPrivacyCollectedDataTypePurposeAccountManagement`]
   - `NSPrivacyCollectedDataTypeUserID` → Linked=true, Tracking=false, Purposes=[`AppFunctionality`, `AccountManagement`]
   - `NSPrivacyCollectedDataTypeHealth` → Linked=**false** (PER D-18 EXPLICIT), Tracking=false, Purposes=[`AppFunctionality`, `Analytics`]
   - `NSPrivacyCollectedDataTypePhotosOrVideos` → Linked=true, Tracking=false, Purposes=[`AppFunctionality`] — covers progress photos
   - `NSPrivacyCollectedDataTypeCrashData` → Linked=false, Tracking=false, Purposes=[`AppFunctionality`, `Analytics`] — Sentry crash reports with PII-scrubbed payloads (per `beforeSend` hook from Phase 7)
   - `NSPrivacyCollectedDataTypePurchaseHistory` → Linked=true, Tracking=false, Purposes=[`AppFunctionality`] — RevenueCat IAP records

Generate Info.plist additions (use PlistBuddy or hand-edit; do NOT overwrite Capacitor template — append/merge):
   - `NSFaceIDUsageDescription` = `"LeanShot uses Face ID to unlock your medication and body data so a stranger can't see them if your phone is unattended."` (D-06; required by @capgo/capacitor-native-biometric)
   - `NSHealthShareUsageDescription` = `"STUB — Phase 18 fills with the actual HealthKit read scope. Required string is present so Xcode validates."` (per outline note — STUB only)
   - `NSPhotoLibraryUsageDescription` = `"LeanShot saves your progress photos so you can compare them over time. Photos stay on your device unless you choose to share them."`
   - `NSPhotoLibraryAddUsageDescription` = `"LeanShot saves progress photos you capture in the app to your photo library if you choose to export."`
   - `NSCameraUsageDescription` = `"LeanShot uses the camera to capture progress photos for your body-recomposition timeline."`
   - `NSAppTransportSecurity` = leave default (App Transport Security ON; no exceptions per D-09 max-coverage on HTTPS hosts).
   - `LSApplicationQueriesSchemes` array — add `"itms-apps"` so the in-app `/clinic/billing` external link can open App Store / Settings if RevenueCat surfaces a manage-subscription redirect.

App.entitlements (plist XML) — declare:
   - `com.apple.developer.associated-domains` = array with TWO entries per D-09 max-coverage:
     - `applinks:leanshot.app`
     - `applinks:app.leanshot.app`
   - `keychain-access-groups` = array `["$(AppIdentifierPrefix)app.leanshot.ios"]` (required by @capgo/capacitor-native-biometric for secure-credential persistence per Plugin README)

CRITICAL — when writing PrivacyInfo.xcprivacy, do NOT add comments inside the plist (`<!--...-->`) for the required-reason entries themselves; some Apple validation tools choke on inline comments under `NSPrivacyAccessedAPITypes`. Header comment is fine. Use 4-space indentation (matches Xcode default).

Verify the Xcode build setting `CODE_SIGN_ENTITLEMENTS = App/App.entitlements` is wired in `apps/ios/App/App.xcodeproj/project.pbxproj` — if 16-01's `cap add ios` did not set it (Capacitor 8 sometimes omits when no entitlements file exists at scaffold time), edit the pbxproj to add it. Verify via `grep -n CODE_SIGN_ENTITLEMENTS apps/ios/App/App.xcodeproj/project.pbxproj`.

Implements D-18 (hand-crafted manifest), D-09 (associated domains), D-06 (biometric usage string), D-17 (Sentry crash coverage declared as CollectedData NOT Tracking).
  </action>
  <verify>
    <automated>plutil -lint apps/ios/App/App/PrivacyInfo.xcprivacy apps/ios/App/App/Info.plist apps/ios/App/App/App.entitlements &amp;&amp; grep -q "NSPrivacyAccessedAPICategoryUserDefaults" apps/ios/App/App/PrivacyInfo.xcprivacy &amp;&amp; grep -q "NSPrivacyAccessedAPICategoryFileTimestamp" apps/ios/App/App/PrivacyInfo.xcprivacy &amp;&amp; grep -q "NSPrivacyAccessedAPICategorySystemBootTime" apps/ios/App/App/PrivacyInfo.xcprivacy &amp;&amp; grep -q "NSPrivacyAccessedAPICategoryDiskSpace" apps/ios/App/App/PrivacyInfo.xcprivacy &amp;&amp; grep -q "NSFaceIDUsageDescription" apps/ios/App/App/Info.plist &amp;&amp; grep -q "NSHealthShareUsageDescription" apps/ios/App/App/Info.plist &amp;&amp; grep -q "applinks:leanshot.app" apps/ios/App/App/App.entitlements &amp;&amp; grep -q "applinks:app.leanshot.app" apps/ios/App/App/App.entitlements &amp;&amp; grep -q "CODE_SIGN_ENTITLEMENTS" apps/ios/App/App.xcodeproj/project.pbxproj</automated>
  </verify>
  <done>
    PrivacyInfo.xcprivacy + Info.plist + App.entitlements pass `plutil -lint`; PrivacyInfo declares 4 required-reason API categories + 6 collected-data types (HealthKit explicitly Linked=false); Info.plist contains all 5 usage strings; App.entitlements has applinks for both hosts + keychain-access-group; project.pbxproj wires CODE_SIGN_ENTITLEMENTS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill scripts/audit-privacy-manifest.mjs with declared-vs-canonical diff logic</name>
  <files>scripts/audit-privacy-manifest.mjs, scripts/audit-privacy-manifest.test.mjs, package.json</files>
  <read_first>
    - scripts/assert-bundle-budget.sh — analog CI-gate script; copy the `::error::` annotation pattern + non-zero-exit convention
    - .planning/phases/16-capacitor-mobile-shells-ios-android/16-PATTERNS.md §"`scripts/audit-privacy-manifest.mjs`" (lines 635-668) — declared analog + script skeleton
    - apps/ios/App/App/PrivacyInfo.xcprivacy — written in Task 1; the diff target
    - package.json `dependencies` — source of truth for which plugins are installed (must cross-check)
    - The `<interfaces>` block above for the plugin-to-reason-code mapping table
  </read_first>
  <behavior>
    - Test 1: Given a valid PrivacyInfo.xcprivacy that declares all 4 required reason-code categories matching the installed plugin inventory in a fixture package.json, script exits 0 and prints `audit-privacy-manifest: PASS`.
    - Test 2: Given a PrivacyInfo.xcprivacy missing `NSPrivacyAccessedAPICategoryUserDefaults` while `@capacitor/preferences` is in fixture package.json, script exits 1 and emits a `::error::` annotation naming the missing category + the plugin that requires it.
    - Test 3: Given a PrivacyInfo.xcprivacy declaring `NSPrivacyAccessedAPICategoryActiveKeyboards` (which no installed plugin uses), script exits 1 and emits a `::warning::` annotation flagging an over-declaration (informational; warning not error per Apple's "declare only what you use").
    - Test 4: Given a PrivacyInfo.xcprivacy with an INVALID reason code (e.g. `XX99.1` not in Apple's canonical list under its declared category), script exits 1 with a `::error::` naming the bogus code.
    - Test 5: Given the 14-plugin inventory + 4 declared categories AND `NSPrivacyTracking=false` AND `NSPrivacyCollectedDataTypeHealth` with `NSPrivacyCollectedDataTypeLinked=false`, script exits 0 (verifies D-18 HealthKit-NOT-linked assertion is honored).
  </behavior>
  <action>
Implement `scripts/audit-privacy-manifest.mjs` as a Node 22+ ESM script (matches `.mjs` extension; no transpilation). Use only built-in modules — no npm dependencies — to keep the script auditable and immune to supply-chain risk on a security-critical asset.

Structure:
1. Imports: `node:fs`, `node:path`. Parse the plist XML by hand (Apple plists are well-formed; a tiny regex-based extractor is sufficient AND safer than pulling in a `plist` npm dep). Specifically: walk `<dict>` blocks; extract `<key>NSPrivacyAccessedAPITypes</key>` followed by its `<array>` siblings.

2. Constants at the top of the file:
   - `PLUGIN_TO_REQUIRED_CATEGORIES` — Map of npm package name → array of `NSPrivacyAccessedAPICategory*` strings the plugin needs. Exactly as enumerated in the `<interfaces>` block above. Comments cite each plugin's privacy README URL.
   - `CANONICAL_REASON_CODES_BY_CATEGORY` — Map of `NSPrivacyAccessedAPICategory*` → array of allowed reason codes (from Apple's documentation). Use the lists in `<interfaces>` block. Comment: "Sourced from Apple's PrivacyInfo schema docs; refresh when Apple updates their canonical list."

3. Main flow:
   a. Read `apps/ios/App/App/PrivacyInfo.xcprivacy` (or path passed via `--manifest=<path>` flag for tests). Extract declared categories + reason codes.
   b. Read `package.json` (or `--package-json=<path>` for tests). Extract `dependencies` keys; intersect with `PLUGIN_TO_REQUIRED_CATEGORIES` to compute the REQUIRED category set.
   c. Diff: every required category MUST appear in declared. Missing → `::error::`. Extra declared category not in required → `::warning::`.
   d. Validate each declared reason code is in `CANONICAL_REASON_CODES_BY_CATEGORY[category]`. Invalid → `::error::`.
   e. Assert top-level keys: `NSPrivacyTracking` must equal `<false/>` (D-18 we are not a tracking app). Assert `NSPrivacyCollectedDataTypeHealth` block (if present) has `NSPrivacyCollectedDataTypeLinked` = `<false/>` per D-18.
   f. Collect all errors+warnings; on errors exit 1; on warnings-only exit 0 (but emit annotations); on clean exit 0 with `audit-privacy-manifest: PASS`.

4. CLI flags via simple `process.argv` parsing — no npm CLI parser:
   - `--manifest=<path>` (default: `apps/ios/App/App/PrivacyInfo.xcprivacy` resolved from script dir + `..`)
   - `--package-json=<path>` (default: `package.json` resolved from script dir + `..`)
   - `--strict` (treat warnings as errors — for use in fastlane lanes; default off)

5. Make executable: shebang `#!/usr/bin/env node` on line 1.

`scripts/audit-privacy-manifest.test.mjs` (Node built-in `node:test` + `node:assert` — no vitest dependency for this script's tests, keeping it self-contained):
   - Use `node:test`'s `describe`/`it`/`before`/`after` from `node:test`.
   - Write fixture files in `os.tmpdir()` per test (one manifest + one package.json each).
   - Spawn the script via `node:child_process.execFileSync` with `--manifest` + `--package-json` pointing to the tmp fixtures.
   - Assert exit code + stderr/stdout contains expected `::error::` / `::warning::` / `PASS` text.
   - Test cases mirror the 5 behaviors enumerated in `<behavior>`.

Add to `package.json` scripts (per pathspec rule, edit ONLY the `scripts` block; commit with `git add -- package.json`):
   - `"audit:privacy"`: `"node scripts/audit-privacy-manifest.mjs"`
   - `"audit:privacy:test"`: `"node --test scripts/audit-privacy-manifest.test.mjs"`
   - `"audit:privacy:strict"`: `"node scripts/audit-privacy-manifest.mjs --strict"`

Why no plist npm dependency: this script is a security-critical CI gate. Pulling `plist` (or `@xmldom/xmldom`) adds supply-chain surface for a parser we only need to extract ~10 known keys from. A hand-rolled regex-based extractor of `<key>NAME</key>\s*<(true|false|string|array)/?>` patterns is auditable in <50 lines and has zero dependencies.

Implements MOBILE-05 (CI-gate half), D-18 (canonical reason-code cross-check).
  </action>
  <verify>
    <automated>node --test scripts/audit-privacy-manifest.test.mjs &amp;&amp; node scripts/audit-privacy-manifest.mjs &amp;&amp; npm run audit:privacy &amp;&amp; grep -c "PLUGIN_TO_REQUIRED_CATEGORIES" scripts/audit-privacy-manifest.mjs | grep -qv "^0$" &amp;&amp; grep -c "CANONICAL_REASON_CODES_BY_CATEGORY" scripts/audit-privacy-manifest.mjs | grep -qv "^0$" &amp;&amp; head -1 scripts/audit-privacy-manifest.mjs | grep -q "^#!/usr/bin/env node"</automated>
  </verify>
  <done>
    All 5 behavior tests pass under `node --test`; running the script against the Task 1 PrivacyInfo emits `audit-privacy-manifest: PASS` and exits 0; `npm run audit:privacy` works from repo root; script has shebang + zero npm dependencies; package.json scripts block contains the three new entries.
  </done>
</task>

<task type="auto">
  <name>Task 3: Play Data Safety form answers + dedicated CI workflow wiring</name>
  <files>apps/android/data-safety.md, .github/workflows/mobile-privacy-audit.yml</files>
  <read_first>
    - .planning/phases/16-capacitor-mobile-shells-ios-android/16-CONTEXT.md D-18 (form completeness)
    - .planning/phases/16-capacitor-mobile-shells-ios-android/16-RESEARCH.md §"References" (line 957) — Google Play Data Safety category list
    - .planning/phases/16-capacitor-mobile-shells-ios-android/16-PATTERNS.md §"`.github/workflows/mobile.yml`" (lines 487-535) — workflow structural pattern (concurrency, working-directory, secret injection)
    - apps/ios/App/App/PrivacyInfo.xcprivacy — Task 1 output; data-safety.md MUST mirror the iOS NSPrivacyCollectedDataTypes entries to keep both stores in sync
    - scripts/audit-privacy-manifest.mjs — Task 2 output; the workflow invokes this
    - .github/workflows/ci.yml — the existing CI workflow; mirror its concurrency + working-directory structure
  </read_first>
  <action>
**apps/android/data-safety.md** — Operator-readable + Play-Console-copy-paste-ready document. Required sections in this exact order:

1. **Header** with last-updated date + form-version tag + cross-reference to the iOS PrivacyInfo file (must mirror it).

2. **Data collection: yes/no answer.** YES — LeanShot collects user data. (Required because account-based features exist.)

3. **Data types collected — table with these columns:** `Category`, `Type`, `Collected?`, `Shared with third parties?`, `Required or optional?`, `Purpose(s)`, `Encrypted in transit?`, `User can request deletion?`. Populate ALL of these rows per Play's category list and matching the iOS PrivacyInfo from Task 1:
   - Personal info → Email address → YES → No → Required → Account management, App functionality → Yes → Yes
   - Personal info → User ID → YES → No → Required → Account management, App functionality → Yes → Yes
   - Personal info → Name → NO (LeanShot doesn't collect name at signup)
   - Health & fitness → Health info → YES (subset of HealthKit/Health-Connect — Phase 18 expands) → No → **Optional** → App functionality, Analytics → Yes → Yes
     - Note: "Health info NOT linked to user ID per our internal privacy posture (matches iOS NSPrivacyCollectedDataTypeLinked=false on Health). On Play, this maps to: `Data is not linked to your identity` = YES."
   - Photos and videos → Photos → YES (progress photos) → No → Optional → App functionality → Yes (HTTPS to Supabase Storage) → Yes
   - Financial info → Purchase history → YES (via Google Play Billing through RevenueCat) → Yes (with RevenueCat as data processor) → Required for purchase → App functionality → Yes → Yes
   - App activity → App interactions → NO (no analytics SDK; Sentry breadcrumbs are crash-context only, scrubbed of PII per `beforeSend` hook)
   - App info & performance → Crash logs → YES → Yes (with Sentry as data processor) → Required → App functionality, Analytics → Yes → Yes
     - Note: "PII scrubbed before send per Sentry `beforeSend` hook from Phase 7."
   - Device or other IDs → Device or other IDs → NO (Capacitor's installationId is generated locally; not sent to any server)

4. **Data sharing — Yes/no per third party:**
   - RevenueCat (data processor for purchase history + entitlement state): YES — required by IAP architecture; users informed via Privacy Policy
   - Sentry (data processor for crash logs): YES — required; PII scrubbed
   - Supabase (data processor for all other categories): YES; LeanShot has a Supabase DPA in place (note Phase 7 Supabase BAA carry-over for HIPAA — separate from Play Data Safety)

5. **Security practices section** (each is a yes/no in Play Console):
   - Data is encrypted in transit: YES (HTTPS everywhere, app-transport-security ON per Info.plist Task 1)
   - You can request that data be deleted: YES (Settings → Delete account, per Phase 7 deletion flow)
   - Committed to following Play Families Policy: NO (not a kids app; gated 18+ in v1)
   - Independent security review: NO (deferred to post-launch)

6. **Source-of-truth cross-reference table** — list each declared data type alongside the matching iOS PrivacyInfo `NSPrivacyCollectedDataType*` key. Format: `| Play category/type | iOS NSPrivacyCollectedDataType | Linked? | Tracking? |`. This table is the regression check: if iOS PrivacyInfo changes, this row must change.

7. **Change log section** — date-stamped entries. Initial entry: `YYYY-MM-DD — Initial completion for v1.2 first submission (Phase 16). Source: 16-07-PLAN.`

8. **Submission instructions** — terse 5-step copy-paste guide for the operator: where in Play Console, what to upload, what to double-check.

**.github/workflows/mobile-privacy-audit.yml** — dedicated workflow (NOT mobile.yml — that file belongs to 16-09; see file_ownership_coordination block above):

```
name: Mobile Privacy Manifest Audit
on:
  push:
    branches: [main]
    paths:
      - 'leanshot/apps/ios/App/App/PrivacyInfo.xcprivacy'
      - 'leanshot/scripts/audit-privacy-manifest.mjs'
      - 'leanshot/scripts/audit-privacy-manifest.test.mjs'
      - 'leanshot/package.json'
      - 'leanshot/package-lock.json'
      - 'leanshot/apps/android/data-safety.md'
  pull_request:
    branches: [main]
    paths:
      - 'leanshot/apps/ios/App/App/PrivacyInfo.xcprivacy'
      - 'leanshot/scripts/audit-privacy-manifest.mjs'
      - 'leanshot/scripts/audit-privacy-manifest.test.mjs'
      - 'leanshot/package.json'
      - 'leanshot/package-lock.json'
      - 'leanshot/apps/android/data-safety.md'

concurrency:
  group: mobile-privacy-audit-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: leanshot

jobs:
  audit-privacy-manifest:
    name: Audit PrivacyInfo + Data Safety parity
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: leanshot/package-lock.json
      - run: npm ci
      - name: Run audit-privacy-manifest tests
        run: npm run audit:privacy:test
      - name: Run audit-privacy-manifest (strict)
        run: npm run audit:privacy:strict
      - name: Verify data-safety.md exists and references iOS PrivacyInfo
        run: |
          test -f apps/android/data-safety.md
          grep -q "NSPrivacyCollectedDataType" apps/android/data-safety.md
          grep -q "Source-of-truth cross-reference" apps/android/data-safety.md
```

Use ubuntu-latest (not macos) — this is a Node-only check, no Xcode needed; ubuntu runners are ~10× cheaper. Mirrors the existing `ci.yml` working-directory + concurrency conventions per the PATTERNS analog.

Implements MOBILE-05 (Play Data Safety half + CI-gate wiring half), D-18 (full coverage of plugin inventory).
  </action>
  <verify>
    <automated>test -f apps/android/data-safety.md &amp;&amp; test -f .github/workflows/mobile-privacy-audit.yml &amp;&amp; wc -l apps/android/data-safety.md | awk '{exit ($1 &gt;= 80) ? 0 : 1}' &amp;&amp; grep -q "Source-of-truth cross-reference" apps/android/data-safety.md &amp;&amp; grep -q "NSPrivacyCollectedDataType" apps/android/data-safety.md &amp;&amp; grep -q "audit-privacy-manifest" .github/workflows/mobile-privacy-audit.yml &amp;&amp; grep -q "working-directory: leanshot" .github/workflows/mobile-privacy-audit.yml &amp;&amp; grep -q "node-version: '22'" .github/workflows/mobile-privacy-audit.yml &amp;&amp; (command -v yamllint &gt;/dev/null &amp;&amp; yamllint .github/workflows/mobile-privacy-audit.yml || true) &amp;&amp; (command -v actionlint &gt;/dev/null &amp;&amp; actionlint .github/workflows/mobile-privacy-audit.yml || true)</automated>
  </verify>
  <done>
    apps/android/data-safety.md exists, ≥80 lines, contains all 8 sections including the source-of-truth cross-reference table tying every Play Data Safety entry back to an iOS NSPrivacyCollectedDataType; `.github/workflows/mobile-privacy-audit.yml` exists, mirrors ci.yml structure (concurrency + working-directory: leanshot + Node 22 + npm cache), and invokes `audit:privacy:test` + `audit:privacy:strict`; if `actionlint`/`yamllint` are installed locally they pass; mobile.yml is NOT touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App binary → Apple App Review | PrivacyInfo.xcprivacy is read by Apple's automated review pipeline; mismatches between declared and observed API usage are rejected. Adversary surface is "self-inflicted submission rejection," not an external attacker. |
| App binary → Google Play Review | Data Safety form answers (entered in Play Console from the .md file) are the audit trail. Mismatches with actual collection → store removal. |
| CI gate → repo main branch | `audit-privacy-manifest.mjs` is the regression-prevention gate. Bypassing it (e.g., merging without the workflow running) re-opens the rejection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07-01 | Tampering | `apps/ios/App/App/PrivacyInfo.xcprivacy` is hand-edited and could drift from actual API usage as plugins are added/removed | mitigate | `audit-privacy-manifest.mjs` (Task 2) cross-checks declared categories against `package.json` plugin inventory on every PR via `mobile-privacy-audit.yml` (Task 3). Required-status check on the workflow blocks merge. |
| T-16-07-02 | Information Disclosure | Sentry crash logs could leak PII (email, RC `appUserID`) into the Crash Logs data category we declare on both stores | accept | PII scrubbing already enforced in Sentry `beforeSend` hook from Phase 7 (`reference_phase7_research_findings.md`). Phase 16-04 carries this over verbatim. data-safety.md notes the scrubbing explicitly so a reviewer can verify our declaration is honest. |
| T-16-07-03 | Repudiation | Apple/Play reviewer asks "what data do you collect?" → answer drifts from binary behavior over time | mitigate | Source-of-truth cross-reference table in `data-safety.md` ties every Play row to an iOS NSPrivacyCollectedDataType key. Change-log section records every edit. Both files are reviewed together. |
| T-16-07-04 | Spoofing | `mobile-privacy-audit.yml` could be disabled by a malicious PR removing the path filters | mitigate | Workflow is on a SEPARATE file from `mobile.yml` (16-09 owns that), keeping the file footprint small and easy to enforce as a required check in branch protection (operator action, flagged in 16-10 launch checklist). |
| T-16-07-05 | Information Disclosure | Universal Links entitlement (applinks:leanshot.app + app.leanshot.app) exposes our domain ownership claim; a misconfigured AASA could let a malicious host claim our deep-link namespace | mitigate | AASA file is published in 16-03 with strict path allowlist; iOS validates AASA at install (Apple platform-level check). Threat is mitigated by the platform, not by us. |
| T-16-07-06 | Tampering | A plugin upgrade (e.g., `@capacitor/preferences` 8.0.1 → 8.1.0) could silently introduce new required-reason API usage | mitigate | `audit-privacy-manifest.mjs` runs on every `package.json` / `package-lock.json` change (path filter in workflow). The `PLUGIN_TO_REQUIRED_CATEGORIES` map is the version-agnostic spec — if a plugin's privacy footprint grows in a minor version, the audit catches the mismatch (declared < observed) and the team updates the manifest before merge. |
| T-16-07-07 | Elevation of Privilege | If `audit-privacy-manifest.mjs` had npm dependencies, a supply-chain attack on those deps could silently neutralize the gate | mitigate | Task 2 mandates zero npm dependencies in the script (Node built-ins only). Auditable in <200 lines. |
</threat_model>

<verification>
## Phase-level checks for this plan

```bash
# 1. iOS privacy artifacts validate as plists
plutil -lint apps/ios/App/App/PrivacyInfo.xcprivacy
plutil -lint apps/ios/App/App/Info.plist
plutil -lint apps/ios/App/App/App.entitlements

# 2. Audit script self-tests pass + script passes against shipped manifest
npm run audit:privacy:test
npm run audit:privacy

# 3. Strict mode (warnings → errors) passes — confirms zero over-declaration drift
npm run audit:privacy:strict

# 4. Data safety doc mirrors iOS PrivacyInfo (cross-ref table present)
grep -q "Source-of-truth cross-reference" apps/android/data-safety.md
grep -q "NSPrivacyCollectedDataType" apps/android/data-safety.md

# 5. Dedicated workflow exists and references the audit script
test -f .github/workflows/mobile-privacy-audit.yml
grep -q "audit:privacy:strict" .github/workflows/mobile-privacy-audit.yml

# 6. mobile.yml (owned by 16-09) is NOT modified by this plan
git diff --name-only HEAD~1..HEAD -- .github/workflows/mobile.yml | wc -l  # should be 0 when run as commit verification

# 7. HealthKit Linked=false per D-18 (single most critical regression check)
grep -A2 "NSPrivacyCollectedDataTypeHealth" apps/ios/App/App/PrivacyInfo.xcprivacy | grep -A1 "NSPrivacyCollectedDataTypeLinked" | grep -q "<false/>"
```
</verification>

<success_criteria>
- `apps/ios/App/App/PrivacyInfo.xcprivacy` exists, validates as a plist, declares all 4 required-reason API categories for the 14-plugin inventory (UserDefaults / FileTimestamp / SystemBootTime / DiskSpace) AND 6 collected-data types with HealthKit explicitly Linked=false per D-18.
- `Info.plist` contains NSFaceIDUsageDescription (D-06), NSHealthShareUsageDescription (STUB for Phase 18), photo-library + camera usage strings.
- `App.entitlements` declares Associated Domains for BOTH `leanshot.app` and `app.leanshot.app` (D-09 max-coverage) + keychain-access-group for biometric credential storage.
- `scripts/audit-privacy-manifest.mjs` (filled, not stubbed) cross-checks declared categories against the installed plugin set in `package.json` AND validates reason codes against Apple's canonical list. Zero npm dependencies. Self-tests pass via `node --test`.
- `apps/android/data-safety.md` contains all 8 sections including a source-of-truth cross-reference table tying every Play Data Safety entry to an iOS NSPrivacyCollectedDataType — the regression contract.
- `.github/workflows/mobile-privacy-audit.yml` runs on every PR touching the PrivacyInfo file, plugin set, audit script, or data-safety doc — and runs the script in strict mode.
- Plan 16-09's `mobile.yml` is untouched (file-ownership coordination honored; no merge conflict on parallel Wave 3 execution).
- MOBILE-05 acceptance criterion satisfied: PrivacyInfo + Data Safety form complete, CI-enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/16-capacitor-mobile-shells-ios-android/16-07-SUMMARY.md` covering:
- What shipped (paths to 6 new/modified files)
- The 4 required-reason API categories declared + which of the 14 plugins drive each
- The 6 collected-data types declared and which Linked/Tracking flag each got (HealthKit Linked=false call-out)
- Confirmation that mobile.yml was NOT touched (file-ownership coordination with 16-09)
- Open items for 16-10 launch checklist: (a) operator copies data-safety.md into Play Console; (b) operator enables `mobile-privacy-audit` workflow as a required-status check in GitHub branch protection; (c) Phase 18 fills the NSHealthShareUsageDescription STUB string.
</output>
