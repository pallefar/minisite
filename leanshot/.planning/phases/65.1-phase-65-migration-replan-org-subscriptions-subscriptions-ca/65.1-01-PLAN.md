---
phase: 65.1-phase-65-migration-replan-org-subscriptions-subscriptions-ca
plan: 01
type: execute
wave: 0
depends_on: []
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, PAY-05, PAY-06, PAY-07, PAY-08, PAY-09, PAY-10, PAY-11]
files_modified:
  - supabase/migrations/20290104000001_subscriptions_tax_id.sql
  - supabase/migrations/20290104000006_tax_collection_log.sql
  - supabase/migrations/20290106000001_enable_rls_6_public_tables.sql
  - supabase/functions/stripe-checkout/index.ts
  - supabase/functions/stripe-webhook/events/tax-collection-log.ts
  - supabase/functions/stripe-webhook/events/checkout-session-completed.ts
  - supabase/functions/stripe-webhook/events/checkout-session-completed.test.ts
  - supabase/functions/_shared/with-org-scope.ts
  - supabase/functions/demo-org-purge/handler.ts
  - leanshot/src/lib/__tests__/with-org-scope.test.ts
  - leanshot/src/lib/__tests__/rls-org-subscriptions.test.ts

must_haves:
  truths:
    - "Migration 20290104000001 targets public.subscriptions (not public.org_subscriptions)"
    - "Migration 20290104000006 FK column is subscription_id text → public.subscriptions(id) (not org_subscription_id uuid → org_subscriptions(org_id))"
    - "Migration 20290106000001 no longer references public.org_subscriptions; header says 6 public tables"
    - "No file under supabase/functions or leanshot/src reads or writes public.org_subscriptions"
    - "stripe-webhook tax-collection-log helper resolves only subscription_id (text); no org_subscription_id branch"
    - "checkout-session-completed mirrors tax_id to public.subscriptions filtered by id=subscriptionId (no org_subscriptions update)"
    - "ORG_SCOPED_TABLES no longer lists 'org_subscriptions'"
    - "demo-org-purge ORG_CHILD_TABLES no longer lists 'org_subscriptions'"
    - "rls-org-subscriptions.test.ts is deleted from the repo"
    - "npm run build + npm run lint + vitest pass in leanshot/"
    - "deno test --no-check passes on stripe-webhook, _shared, demo-org-purge"
  artifacts:
    - path: supabase/migrations/20290104000001_subscriptions_tax_id.sql
      provides: "tax_id column on public.subscriptions + clinic+tax_id partial index"
      contains: "alter table public.subscriptions"
    - path: supabase/migrations/20290104000006_tax_collection_log.sql
      provides: "tax_collection_log table referencing only public.subscriptions"
      contains: "subscription_id text"
    - path: supabase/migrations/20290106000001_enable_rls_6_public_tables.sql
      provides: "RLS enable for 6 public tables (org_subscriptions block removed)"
      contains: "6 public tables"
  key_links:
    - from: supabase/migrations/20290104000001_subscriptions_tax_id.sql
      to: supabase/migrations/20260601000019_stripe_subscriptions.sql
      via: "ALTER TABLE public.subscriptions ADD COLUMN"
      pattern: "alter table public\\.subscriptions"
    - from: supabase/functions/stripe-webhook/events/checkout-session-completed.ts
      to: public.subscriptions
      via: "supabase.from('subscriptions').update({tax_id}).eq('id', subscriptionId)"
      pattern: "from\\(['\"]subscriptions['\"]\\).*update.*tax_id"
    - from: supabase/functions/stripe-webhook/events/tax-collection-log.ts
      to: public.tax_collection_log
      via: "insert with subscription_id (text)"
      pattern: "subscription_id"
---

<objective>
Rewrite Phase 65 artifacts that target the dropped `public.org_subscriptions` table so they instead target the canonical `public.subscriptions` (Phase 14, `WHERE clinic_id IS NOT NULL` for clinic data). This is a pure surgical rewrite — no new functionality, no new REQ IDs. Unblocks Phase 69.7 Plan 01 Task 3 (`supabase db push --linked`) which halted at SQLSTATE 42P01.

Purpose: Phase 29 D-14 dropped `public.org_subscriptions` as BREAKING CHANGE four phases before Phase 65 was planned. Phase 65 migrations 01 + 06 hard-break on `db push`; migration 06_RLS is drift-safe but misleading. Consumer Edge Fns + tests carry the same drift.

Output: 3 migration files rewritten (2 renamed via `git mv`), 5 Edge Fn files swept, 2 test files patched (1 deleted, 1 fixture-updated). One atomic commit at the monorepo git root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/PROJECT.md
@leanshot/.planning/ROADMAP.md
@leanshot/.planning/STATE.md
@leanshot/.planning/phases/65.1-phase-65-migration-replan-org-subscriptions-subscriptions-ca/65.1-CONTEXT.md
@supabase/migrations/20260601000019_stripe_subscriptions.sql
@supabase/migrations/20290104000001_org_subscriptions_tax_id.sql
@supabase/migrations/20290104000006_tax_collection_log.sql
@supabase/migrations/20290106000001_enable_rls_7_public_tables.sql

<interfaces>
<!-- Canonical schema reference: public.subscriptions (Phase 14) -->

From supabase/migrations/20260601000019_stripe_subscriptions.sql:
```sql
create table public.subscriptions (
  id text primary key,                                                -- Stripe sub_xxx natural PK
  provider text not null default 'stripe' check (provider in ('stripe','revenuecat')),
  user_id uuid references auth.users(id) on delete cascade,
  clinic_id uuid references public.orgs(id) on delete cascade,
  stripe_customer_id text,
  status text not null,
  ux_tier text not null check (ux_tier in ('free','paid','past_due')),
  plan_id text,
  current_period_end timestamptz,
  trial_end timestamptz,
  cancel_at_period_end boolean not null default false,
  metadata jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  check ((user_id is null) <> (clinic_id is null))    -- exactly one of user_id / clinic_id
);

-- Existing IMMUTABLE-safe partial indexes
create index idx_subscriptions_user   on public.subscriptions(user_id)   where user_id   is not null;
create index idx_subscriptions_clinic on public.subscriptions(clinic_id) where clinic_id is not null;
```

Phase 29 BREAKING CHANGE (drops org_subscriptions):
```sql
-- supabase/migrations/20270601200001_p29_reconcile.sql
drop table if exists public.org_subscriptions cascade;
-- "public.org_subscriptions DROPPED — downstream callers MUST use
--  public.subscriptions WHERE clinic_id IS NOT NULL for clinic subscriptions."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv migration files for filename changes (2 renames)</name>
  <files>
    supabase/migrations/20290104000001_org_subscriptions_tax_id.sql → supabase/migrations/20290104000001_subscriptions_tax_id.sql
    supabase/migrations/20290106000001_enable_rls_7_public_tables.sql → supabase/migrations/20290106000001_enable_rls_6_public_tables.sql
  </files>
  <read_first>
    - /Users/karstenhaldan/minisite/supabase/migrations/20290104000001_org_subscriptions_tax_id.sql
    - /Users/karstenhaldan/minisite/supabase/migrations/20290106000001_enable_rls_7_public_tables.sql
  </read_first>
  <action>
    From /Users/karstenhaldan/minisite (monorepo git root), assert PWD invariant with `git rev-parse --show-toplevel` (must print `/Users/karstenhaldan/minisite`). Then run two `git mv` commands — DO NOT use `mv` + `git add` (loses blame history):
      git mv supabase/migrations/20290104000001_org_subscriptions_tax_id.sql supabase/migrations/20290104000001_subscriptions_tax_id.sql
      git mv supabase/migrations/20290106000001_enable_rls_7_public_tables.sql supabase/migrations/20290106000001_enable_rls_6_public_tables.sql
    Do NOT rename 20290104000006_tax_collection_log.sql — only its body changes. Timestamp prefixes stay identical so migration ordering against the remote (last-applied 20290103000005) remains forward.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && git status --short supabase/migrations/ | grep -E '^R' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short supabase/migrations/` shows exactly 2 `R` (rename) entries: one for 20290104000001 and one for 20290106000001
    - File `supabase/migrations/20290104000001_subscriptions_tax_id.sql` exists; old path does not
    - File `supabase/migrations/20290106000001_enable_rls_6_public_tables.sql` exists; old path does not
    - `git rev-parse --show-toplevel` printed `/Users/karstenhaldan/minisite` before commands ran
  </acceptance_criteria>
  <done>Two `git mv` renames recorded in the index; no body edits yet.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite migration 20290104000001 body — target public.subscriptions.tax_id</name>
  <files>supabase/migrations/20290104000001_subscriptions_tax_id.sql</files>
  <read_first>
    - /Users/karstenhaldan/minisite/supabase/migrations/20260601000019_stripe_subscriptions.sql (canonical schema)
    - /Users/karstenhaldan/minisite/supabase/migrations/20290104000001_org_subscriptions_tax_id.sql (the OLD body — read once for comment patterns to preserve)
  </read_first>
  <action>
    Overwrite the (renamed) file `supabase/migrations/20290104000001_subscriptions_tax_id.sql` with this body:

    - Header comment block preserving the Phase 65-01 / PAY-03 context, updated to state:
      * Target is `public.subscriptions` (Phase 14 canonical, NOT the dropped `public.org_subscriptions`)
      * Phase 29 D-14 dropped `org_subscriptions` as BREAKING CHANGE; clinic subs live in `subscriptions WHERE clinic_id IS NOT NULL`
      * Reference [[reference_supabase_back_dated_migration_blocks_push]] for timestamp choice (20290104 > 20290103000005)
      * Reference [[reference_rag_sources_source_type_drift]] for `ADD COLUMN IF NOT EXISTS` guard
    - Single transaction: `begin; ... commit;`
    - Step 1: `alter table public.subscriptions add column if not exists tax_id text;`
    - Step 2: `create index if not exists idx_subscriptions_clinic_tax_id on public.subscriptions(clinic_id, tax_id) where clinic_id is not null and tax_id is not null;` — IMMUTABLE-safe predicate (column-only `IS NOT NULL`s)
    - No CHECK constraints needed (text column, nullable; B2B-only — consumer subs leave NULL)
    - RLS already enabled on public.subscriptions in 20260601000019 — no RLS changes here. Add a comment noting that.
    - NO `CREATE POLICY IF NOT EXISTS` (per `feedback_phase_close_out_supabase_gotchas`)
    - End comment: cross-link to consumer rewrite in `supabase/functions/stripe-webhook/events/checkout-session-completed.ts` (now writes `subscriptions.tax_id` via `.eq('id', subscriptionId)`).

    Do NOT include any reference to `public.org_subscriptions` anywhere in the file body.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c "org_subscriptions" supabase/migrations/20290104000001_subscriptions_tax_id.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "org_subscriptions" supabase/migrations/20290104000001_subscriptions_tax_id.sql` returns `0` (zero references to the dropped table, including in comments)
    - `grep -c "alter table public.subscriptions" supabase/migrations/20290104000001_subscriptions_tax_id.sql` returns `>= 1`
    - `grep -c "idx_subscriptions_clinic_tax_id" supabase/migrations/20290104000001_subscriptions_tax_id.sql` returns `>= 1`
    - File contains `begin;` and `commit;` lines (single transaction)
    - File contains `where clinic_id is not null and tax_id is not null` (partial index predicate)
  </acceptance_criteria>
  <done>Migration body re-targets `public.subscriptions`; partial index uses both `clinic_id` and `tax_id` columns; no residual `org_subscriptions` text.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite migration 20290104000006 body — FK column rename (org_subscription_id uuid → subscription_id text)</name>
  <files>supabase/migrations/20290104000006_tax_collection_log.sql</files>
  <read_first>
    - /Users/karstenhaldan/minisite/supabase/migrations/20290104000006_tax_collection_log.sql (current body — read once for header comments + RLS policy names to preserve)
    - /Users/karstenhaldan/minisite/supabase/migrations/20260601000019_stripe_subscriptions.sql (confirms `subscriptions.id` is `text`)
  </read_first>
  <action>
    Overwrite `supabase/migrations/20290104000006_tax_collection_log.sql` body. The file KEEPS its filename. Required edits relative to the current body:

    1. Header comment: remove the "Both subscription_id (text) AND org_subscription_id (uuid) FKs exist — a checkout produces exactly one of them. CHECK: at least one of subscription_id / org_subscription_id is non-null. org_subscription_id references org_subscriptions(org_id) ..." paragraph. Replace with: "Single FK column `subscription_id text references public.subscriptions(id) on delete set null`. Clinic vs consumer distinction is derived downstream via `subscriptions.clinic_id IS NOT NULL`. Phase 29 D-14 dropped `public.org_subscriptions`; clinic subs live in `public.subscriptions` with `clinic_id IS NOT NULL`."

    2. CREATE TABLE block — remove the `org_subscription_id uuid references public.org_subscriptions(org_id) on delete set null,` column line entirely. Keep `subscription_id text references public.subscriptions(id) on delete set null,` exactly as it is.

    3. Remove the CHECK constraint `tax_collection_log_sub_or_org_check` (the "at least one is non-null" guard). Replace with NO replacement CHECK — `subscription_id` is now simply nullable; rows without a sub (rare edge case) are allowed and downstream queries filter `WHERE subscription_id IS NOT NULL` if needed. Add inline comment explaining this.

    4. Indexes — remove `idx_tax_collection_log_org_subscription_id` entirely. Keep `idx_tax_collection_log_subscription_id` (already references `subscription_id`), `idx_tax_collection_log_state_created_at`, and `idx_tax_collection_log_session_id` unchanged.

    5. RLS section — unchanged (policies do not reference the removed column).

    6. The two `create policy` blocks at the bottom — unchanged.

    Result: the file references `public.subscriptions(id)` exactly once (the FK definition) and zero references to `public.org_subscriptions` anywhere.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -cE "org_subscription_id|org_subscriptions" supabase/migrations/20290104000006_tax_collection_log.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "org_subscription_id|org_subscriptions" supabase/migrations/20290104000006_tax_collection_log.sql` returns `0`
    - `grep -c "subscription_id text" supabase/migrations/20290104000006_tax_collection_log.sql` returns `>= 1` (the FK column line)
    - `grep -c "references public.subscriptions(id)" supabase/migrations/20290104000006_tax_collection_log.sql` returns `1`
    - `grep -c "tax_collection_log_sub_or_org_check" supabase/migrations/20290104000006_tax_collection_log.sql` returns `0` (CHECK removed)
    - `grep -c "idx_tax_collection_log_subscription_id" supabase/migrations/20290104000006_tax_collection_log.sql` returns `>= 1`
    - `grep -c "idx_tax_collection_log_org_subscription_id" supabase/migrations/20290104000006_tax_collection_log.sql` returns `0`
    - Both `create policy "service_role_all_tax_collection_log"` and `create policy "staff_select_tax_collection_log"` blocks remain present
  </acceptance_criteria>
  <done>Migration body has a single `subscription_id text` FK to `public.subscriptions(id)`; no `org_subscription_id` column, no `org_subscriptions` references, no sub-or-org CHECK.</done>
</task>

<task type="auto">
  <name>Task 4: Edit migration 20290106000001 (renamed _6_public_tables) — delete org_subscriptions DO block + update header count</name>
  <files>supabase/migrations/20290106000001_enable_rls_6_public_tables.sql</files>
  <read_first>
    - /Users/karstenhaldan/minisite/supabase/migrations/20290106000001_enable_rls_7_public_tables.sql (the OLD body content — the renamed file currently still contains it)
  </read_first>
  <action>
    Edit the (already-renamed via Task 1) file `supabase/migrations/20290106000001_enable_rls_6_public_tables.sql`. NOTE: this file does NOT currently contain an `org_subscriptions` block — verify by grep first. The task body description in CONTEXT.md was based on an earlier draft. If grep returns 0 for `org_subscriptions`, the only edits needed are header-comment updates.

    Step 1: Grep the file for `org_subscriptions`. If 0 matches:
      - Update header comment line 1 from `Phase 66.5 Plan 01 Task 1 — SEC-01: Enable RLS on 7 RLS-disabled public tables.` to `Phase 66.5 Plan 01 Task 1 — SEC-01: Enable RLS on 6 RLS-disabled public tables. (Phase 65.1: dropped org_subscriptions reference — Phase 29 D-14 dropped that table.)`
      - Update the Tables comment block listing the 7 tables — IF an `org_subscriptions` entry exists, remove it and renumber. Otherwise leave the list alone.
      - Update `Findings count:  7 ERROR-level rls_disabled_in_public entries.` → `Findings count: 6 ERROR-level rls_disabled_in_public entries (org_subscriptions excluded per Phase 65.1 — table dropped Phase 29 D-14).`

    Step 2: If grep DOES return matches for `org_subscriptions`:
      - Delete the entire DO/EXCEPTION block (the `do $$ begin alter table public.org_subscriptions enable row level security; ...end $$;` block) AND the matching `drop policy if exists "org_subscriptions_..." / create policy ...` block AND the matching `comment on table public.org_subscriptions ...` block. Keep the 6 other tables' blocks intact (email_send_counters, ad_spend_facts_y2026m05/06/07/08, paywall_events, plan_history).
      - Apply the header edits described above.

    Step 3: Verify the file ends cleanly (no dangling `end $$;` or comment fragments).

    DO NOT touch the 6 retained tables' blocks under any circumstance.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c "org_subscriptions" supabase/migrations/20290106000001_enable_rls_6_public_tables.sql; grep -cE "^-- *([1-7])\. " supabase/migrations/20290106000001_enable_rls_6_public_tables.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "org_subscriptions" supabase/migrations/20290106000001_enable_rls_6_public_tables.sql` returns `0`
    - Header comment line 1 mentions `6 RLS-disabled public tables` (not 7)
    - The 6 retained-table block headers (`-- N. <table_name>`) still exist exactly: email_send_counters, ad_spend_facts_y2026m05, ad_spend_facts_y2026m06, ad_spend_facts_y2026m07, ad_spend_facts_y2026m08, paywall_events, plan_history → 6 entries (note: original had 7; if all 7 already lack org_subscriptions, only the header count changes and the 7 numbered headers remain — both states are acceptable as long as the actual `org_subscriptions` references are zero)
    - File has matching `do $$ begin` and `end $$;` counts (no dangling block)
  </acceptance_criteria>
  <done>File has zero `org_subscriptions` references; header comment count reflects the post-rewrite reality.</done>
</task>

<task type="auto">
  <name>Task 5: Rewrite stripe-webhook tax-collection-log helper — drop org_subscription_id branch</name>
  <files>supabase/functions/stripe-webhook/events/tax-collection-log.ts</files>
  <read_first>
    - /Users/karstenhaldan/minisite/supabase/functions/stripe-webhook/events/tax-collection-log.ts
    - /Users/karstenhaldan/minisite/supabase/migrations/20290104000006_tax_collection_log.sql (post-Task-3 body — confirms schema)
  </read_first>
  <action>
    Rewrite `writeTaxCollectionLog()` to insert ONLY `subscription_id` (text). Specific edits:

    1. JSDoc comment block (top of file): remove the line `Schema reference: ... org_subscription_id (uuid, FK org_subscriptions.org_id) — clinic path`. Replace the schema reference list with the post-Task-3 schema (single `subscription_id text` FK to `subscriptions.id`). Add a one-line note: "Phase 65.1 rewrite: dual subscription_id/org_subscription_id resolution removed — clinic vs consumer is now derived from `subscriptions.clinic_id IS NOT NULL` downstream."

    2. Function body — remove this entire block (lines ~63-89 of the current file):
       ```
       const subDataMeta = ((session.subscription_data?.metadata ?? session.metadata) as Record<string, string>) ?? {};
       const tierKind = subDataMeta.tier_kind;
       let subscriptionId: string | null = null;
       let orgSubscriptionId: string | null = null;
       if (tierKind === 'clinic') {
         orgSubscriptionId = subDataMeta.clinic_id ?? null;
       } else {
         subscriptionId = (session.subscription as string | null) ?? null;
       }
       if (!subscriptionId && !orgSubscriptionId) { ... skip warn ... return; }
       ```
       Replace with:
       ```
       // Phase 65.1: subscriptions.id is the canonical Stripe sub_xxx text PK. Both
       // clinic and consumer Checkout sessions populate session.subscription. Skip if
       // absent (e.g. setup-mode sessions); the FK is nullable.
       const subscriptionId: string | null = (session.subscription as string | null) ?? null;
       ```
       Do NOT skip the insert when subscriptionId is null — the FK is nullable post-Task-3 (CHECK removed).

    3. Row object (the `row: Record<string, unknown>` literal) — remove the `org_subscription_id: orgSubscriptionId,` line. Keep `subscription_id: subscriptionId,` exactly.

    4. Console log fields — remove any reference to `org_subscription_id` from console.warn / console.error JSON.stringify payloads. Keep `session_id`, `status`, `state_set`, `error_message`, `error_code`.

    5. No new imports needed; no signature change to `writeTaxCollectionLog(admin, session)`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -cE "org_subscription_id|orgSubscriptionId|org_subscriptions" supabase/functions/stripe-webhook/events/tax-collection-log.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "org_subscription_id|orgSubscriptionId|org_subscriptions" supabase/functions/stripe-webhook/events/tax-collection-log.ts` returns `0`
    - `grep -c "subscription_id: subscriptionId" supabase/functions/stripe-webhook/events/tax-collection-log.ts` returns `1`
    - `grep -c "session.subscription as string" supabase/functions/stripe-webhook/events/tax-collection-log.ts` returns `>= 1`
    - The exported function signature `export async function writeTaxCollectionLog(admin: SupabaseClient, session: Stripe.Checkout.Session): Promise<void>` is unchanged
  </acceptance_criteria>
  <done>Helper resolves only `subscription_id` (text); no `tier_kind`-gated branching; no references to the dropped table.</done>
</task>

<task type="auto">
  <name>Task 6: Rewrite checkout-session-completed.ts — mirror tax_id to public.subscriptions (not org_subscriptions)</name>
  <files>supabase/functions/stripe-webhook/events/checkout-session-completed.ts</files>
  <read_first>
    - /Users/karstenhaldan/minisite/supabase/functions/stripe-webhook/events/checkout-session-completed.ts
  </read_first>
  <action>
    Edit the PAY-03 tax_id mirror block (currently lines ~157-213 of the file — the section between `// ─── Phase 65-04 — PAY-03: clinic tax_id mirror ...` and `// ─── Phase 65-04 — PAY-01: tax_collection_log audit ...`). Required changes:

    1. Replace the entire `.from('org_subscriptions').update({ tax_id: firstTaxId.value }).eq('org_id', clinicId)` chain with:
       ```
       const { error: taxIdErr } = await admin
         .from('subscriptions')
         .update({ tax_id: firstTaxId.value })
         .eq('id', subId);                              // subId = session.subscription (text)
       ```
       `subId` is already defined at the top of `handle()` (`const subId = session.subscription as string;`).

    2. Update the comment block above the mutation (currently mentions "mirror ... into org_subscriptions.tax_id keyed by org_id"). Rewrite to:
       ```
       // Stripe Tax `tax_id_collection` populates session.customer_tax_ids on B2B
       // checkouts. We mirror the FIRST collected value into public.subscriptions.tax_id
       // keyed by the Stripe subscription id (session.subscription). The row's
       // clinic_id IS NOT NULL distinguishes it from consumer subs (Phase 14 +
       // Phase 29 D-14 — org_subscriptions was dropped; subscriptions is canonical).
       //
       // PII guard (T-65-04-02): only log `tax_id_set: true/false`, NEVER the value.
       ```

    3. Update the error log JSON payload: replace `clinic_id: clinicId, tax_id_set: false` with `subscription_id: subId, clinic_id: clinicId, tax_id_set: false`. Update the success log payload similarly. Never log `firstTaxId.value`.

    4. The outer guards remain: still gate on `meta.tier_kind === 'clinic'` (only B2B sessions carry `customer_tax_ids`), still require `customerTaxIds && customerTaxIds.length > 0` and `firstTaxId?.value`.

    5. Replace the no-clinic-id warning's wording to mention subscription_id instead.

    6. Outer try/catch with `console.error('[stripe-webhook/checkout-completed] tax_id mirror failed', ...)` is preserved (T-65-04-04 accept disposition — audit/mirror failures MUST NOT fail the webhook).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -cE "org_subscriptions|\\.eq\\('org_id'" supabase/functions/stripe-webhook/events/checkout-session-completed.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "org_subscriptions|\\.eq\\('org_id'" supabase/functions/stripe-webhook/events/checkout-session-completed.ts` returns `0`
    - `grep -c "from('subscriptions')" supabase/functions/stripe-webhook/events/checkout-session-completed.ts` returns `>= 3` (the existing `web` upsert, the existing `clinic` upsert, and the new tax_id update)
    - `grep -c ".update({ tax_id:" supabase/functions/stripe-webhook/events/checkout-session-completed.ts` returns `1`
    - `grep -c ".eq('id', subId)" supabase/functions/stripe-webhook/events/checkout-session-completed.ts` returns `>= 1` (the new tax_id mirror)
    - The PAY-01 call `await writeTaxCollectionLog(admin, session)` remains unchanged
    - The outer try/catch around the PAY-03 mirror block remains
  </acceptance_criteria>
  <done>tax_id mirror writes to `public.subscriptions` filtered by `id = subId`; no `org_subscriptions` references; PII-safe logging preserved.</done>
</task>

<task type="auto">
  <name>Task 7: Update checkout-session-completed.test.ts fixtures (T10/T11/T12) — expect subscriptions table writes</name>
  <files>supabase/functions/stripe-webhook/events/checkout-session-completed.test.ts</files>
  <read_first>
    - /Users/karstenhaldan/minisite/supabase/functions/stripe-webhook/events/checkout-session-completed.test.ts (lines 290-500)
    - /Users/karstenhaldan/minisite/supabase/functions/stripe-webhook/events/checkout-session-completed.ts (post-Task-6, for the new mutation shape)
  </read_first>
  <action>
    Update the 3 PAY-03 tests (`65-T10`, `65-T11`, `65-T12`). For each:

    1. `65-T10` — currently asserts `c.table === 'org_subscriptions' && c.op === 'update' && c.data.tax_id === '12-3456789'` filtered by `org_id`. Change to:
       - Test name: `'65-T10: clinic session with customer_tax_ids writes subscriptions.tax_id'`
       - Comment header: `// ─── 65-T10: clinic session with customer_tax_ids → subscriptions.tax_id ────`
       - Assert finder: `c.table === 'subscriptions' && c.op === 'update' && c.data.tax_id === '12-3456789'`
       - Assert filter: `taxIdUpdate!.filter?.col === 'id'` and `taxIdUpdate!.filter?.val === 'sub_clinic_tax_001'` (the default `subId` from `buildClinicTaxEvent`)
       - Error messages mentioning `org_subscriptions.tax_id` → `subscriptions.tax_id`

    2. `65-T11` — currently asserts no `org_subscriptions` write. Change to: assert no `subscriptions` write with `op === 'update'` AND `data.tax_id` field present. Note: the existing `subscriptions` upsert (the `web` upsert at the top of `handle()`) IS expected to fire — exclude it by filtering on `op === 'update'` + `'tax_id' in c.data`. Update test name + comment to mention `subscriptions.tax_id` instead of `org_subscriptions`.

    3. `65-T12` — currently asserts no `org_subscriptions` update on empty `customer_tax_ids`. Change to: assert no `subscriptions` update where `data.tax_id` field is present. Same exclusion logic as T11 (the clinic upsert at top of handler still fires; we only care about the tax_id mirror call). Update test name + comment.

    4. Comments at the top of the section (lines 297-313) describing T10/T11/T12 behaviors — replace `org_subscriptions` with `subscriptions` throughout. Update T10 description to mention `subscriptions.tax_id` keyed by `id` (Stripe sub_xxx), not `org_id`.

    Do NOT change T13/T14/T15/T16 (tax_collection_log tests).

    Do NOT change `buildClinicTaxEvent` / `buildWebTaxEvent` helpers — the event shape is unchanged.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c "org_subscriptions" supabase/functions/stripe-webhook/events/checkout-session-completed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "org_subscriptions" supabase/functions/stripe-webhook/events/checkout-session-completed.test.ts` returns `0`
    - 65-T10 finder uses `c.table === 'subscriptions'` and `taxIdUpdate!.filter?.col === 'id'`
    - 65-T10 asserts filter value equals the Stripe subscription id (`'sub_clinic_tax_001'` per default), not the clinic id
    - 65-T11 and 65-T12 assert no `subscriptions` row update with a `tax_id` field set (the non-tax_id upserts at top of handler still fire — that's fine)
    - T13/T14/T15/T16 test bodies are unchanged
  </acceptance_criteria>
  <done>Three PAY-03 tests assert writes against `public.subscriptions` table keyed by Stripe subscription id; no remaining `org_subscriptions` strings in the file.</done>
</task>

<task type="auto">
  <name>Task 8: Edit stripe-checkout/index.ts — update comment (org_subscriptions → subscriptions)</name>
  <files>supabase/functions/stripe-checkout/index.ts</files>
  <read_first>
    - /Users/karstenhaldan/minisite/supabase/functions/stripe-checkout/index.ts (lines 530-555)
  </read_first>
  <action>
    Single-line comment update around line 544. Replace:
      `//   tax calc. Mirroring the collected tax_id to org_subscriptions.tax_id is`
    with:
      `//   tax calc. Mirroring the collected tax_id to subscriptions.tax_id (clinic`
      `//   rows, where clinic_id IS NOT NULL) is`

    No code changes — comment only. The Edge Fn does not write to either table; it only creates Checkout sessions.

    Verify no other occurrences exist in this file via grep — if there are, replace them with `subscriptions.tax_id WHERE clinic_id IS NOT NULL` similarly.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -cE "org_subscriptions" supabase/functions/stripe-checkout/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "org_subscriptions" supabase/functions/stripe-checkout/index.ts` returns `0`
    - `grep -c "subscriptions.tax_id" supabase/functions/stripe-checkout/index.ts` returns `>= 1`
    - No code-line changes — only comments edited (verify via `git diff supabase/functions/stripe-checkout/index.ts | grep -E "^[+-][^/]"` showing only comment-line `//` edits)
  </acceptance_criteria>
  <done>Comment block accurately describes the post-rewrite mirror target; zero `org_subscriptions` references in file.</done>
</task>

<task type="auto">
  <name>Task 9: Remove 'org_subscriptions' from ORG_SCOPED_TABLES in _shared/with-org-scope.ts</name>
  <files>supabase/functions/_shared/with-org-scope.ts</files>
  <read_first>
    - /Users/karstenhaldan/minisite/supabase/functions/_shared/with-org-scope.ts (lines 30-58)
  </read_first>
  <action>
    Edit the `ORG_SCOPED_TABLES` `Set` literal:
    1. Remove the line `  'org_subscriptions',` (currently 4th entry).
    2. Above the set, add a one-line comment: `// Phase 65.1: 'org_subscriptions' removed — Phase 29 D-14 dropped that table.`

    Do NOT remove any other entry. Do NOT change exported symbols or function signatures. The `Set` should still contain all other listed tables (`organizations`, `org_members`, `org_invites`, `org_settings`, `org_branding`, `org_patient_links`, `org_consent_grants`, `org_patient_invites`, plus the P30/P31 entries).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c "'org_subscriptions'" supabase/functions/_shared/with-org-scope.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'org_subscriptions'" supabase/functions/_shared/with-org-scope.ts` returns `0`
    - The `ORG_SCOPED_TABLES` set still exports and is non-empty; `grep -c "ORG_SCOPED_TABLES" supabase/functions/_shared/with-org-scope.ts` returns `>= 3` (declaration + Proxy gate + export)
    - Other entries (`'organizations'`, `'org_members'`, `'org_invites'`, `'org_settings'`, `'org_branding'`, `'org_patient_links'`, `'org_consent_grants'`, `'org_patient_invites'`, `'clinician_alerts'`, `'clinician_alert_deliveries'`, `'org_patient_thresholds'`, `'org_onboarding_flows'`) remain in the set
  </acceptance_criteria>
  <done>`'org_subscriptions'` removed from ORG_SCOPED_TABLES; no other behavioral change to the helper.</done>
</task>

<task type="auto">
  <name>Task 10: Remove 'org_subscriptions' from ORG_CHILD_TABLES in demo-org-purge/handler.ts</name>
  <files>supabase/functions/demo-org-purge/handler.ts</files>
  <read_first>
    - /Users/karstenhaldan/minisite/supabase/functions/demo-org-purge/handler.ts (lines 25-95)
  </read_first>
  <action>
    Two edits:

    1. Header comment block (around lines 27-33): remove `org_subscriptions, ` from the parenthetical list of Phase-28 child-table FK examples. Result: `(org_members, org_invites, org_settings, org_branding, org_patient_links, org_consent_grants)`.

    2. `ORG_CHILD_TABLES` array (around lines 83-93): remove the line `  'org_subscriptions',`. Result: an 8-entry array (`org_patient_links`, `org_consent_grants`, `org_invites`, `org_branding`, `org_settings`, `org_members`, `memberships`, `roles`). Add a one-line comment above the array: `// Phase 65.1: 'org_subscriptions' removed — Phase 29 D-14 dropped that table.`

    Do NOT change any other logic. The `42P01` (relation does not exist) tolerance branch inside the for-loop is preserved and would have masked this drift at runtime, but the array should still be accurate.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c "org_subscriptions" supabase/functions/demo-org-purge/handler.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "org_subscriptions" supabase/functions/demo-org-purge/handler.ts` returns `0`
    - The `ORG_CHILD_TABLES` array has exactly 8 entries
    - The 8 retained entries are: `'org_patient_links'`, `'org_consent_grants'`, `'org_invites'`, `'org_branding'`, `'org_settings'`, `'org_members'`, `'memberships'`, `'roles'` (verify via `grep -E "^  '[a-z_]+'," supabase/functions/demo-org-purge/handler.ts | grep -v "^  '"` order-preserving listing if helpful)
    - The header-comment FK example list does not mention `org_subscriptions`
  </acceptance_criteria>
  <done>Demo purge no longer attempts to clear the dropped `org_subscriptions` table.</done>
</task>

<task type="auto">
  <name>Task 11: Update with-org-scope.test.ts fixture — drop org_subscriptions from expected tables list</name>
  <files>leanshot/src/lib/__tests__/with-org-scope.test.ts</files>
  <read_first>
    - /Users/karstenhaldan/minisite/leanshot/src/lib/__tests__/with-org-scope.test.ts (lines 240-273)
  </read_first>
  <action>
    Edit the `expectedTables` array inside the test `'ORG_SCOPED_TABLES contains all 8 Phase 28 org-scoped tables'`:

    1. Remove the line `      'org_subscriptions',` from the array.
    2. Update the test name string from `'ORG_SCOPED_TABLES contains all 8 Phase 28 org-scoped tables'` to `'ORG_SCOPED_TABLES contains the 7 retained Phase 28 org-scoped tables (org_subscriptions dropped per Phase 65.1)'`. The expected array now has 7 entries: `'organizations'`, `'org_members'`, `'org_invites'`, `'org_settings'`, `'org_branding'`, `'org_patient_links'`, `'org_consent_grants'`.
    3. Add an inline `// Phase 65.1: org_subscriptions removed — Phase 29 D-14 dropped that table; see rls-org-subscriptions.test.ts deletion in same commit.` comment above the `expectedTables` declaration.

    Do NOT touch any other test (Tests 1-7, the additional bypass loop, etc.). The "OrgScopeBypassError raised for each ORG_SCOPED_TABLE" loop iterates `ORG_SCOPED_TABLES` directly — no change needed there.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c "'org_subscriptions'" leanshot/src/lib/__tests__/with-org-scope.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'org_subscriptions'" leanshot/src/lib/__tests__/with-org-scope.test.ts` returns `0`
    - The `expectedTables` array in the renamed test has exactly 7 entries
    - All other tests (1-7 + additional bypass loop) remain structurally unchanged (no removed `it(...)` blocks)
  </acceptance_criteria>
  <done>Fixture matches post-rewrite ORG_SCOPED_TABLES; vitest expectation count adjusted.</done>
</task>

<task type="auto">
  <name>Task 12: Delete leanshot/src/lib/__tests__/rls-org-subscriptions.test.ts</name>
  <files>leanshot/src/lib/__tests__/rls-org-subscriptions.test.ts (deletion)</files>
  <read_first>
    - /Users/karstenhaldan/minisite/leanshot/src/lib/__tests__/rls-org-subscriptions.test.ts (entire file — confirm it only tests the dropped table)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite` (PWD invariant: assert via `git rev-parse --show-toplevel`):
      git rm leanshot/src/lib/__tests__/rls-org-subscriptions.test.ts

    Use `git rm` (NOT `rm`) so the deletion is staged. The file's entire purpose is to prove RLS isolation on the now-dropped `public.org_subscriptions` table — nothing to test.

    Do NOT delete any other test file. Do NOT delete the `_fixtures/p28-rls-fixture` module that this test imports — other RLS tests (org-members, org-invites, etc.) use it.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && git status --short leanshot/src/lib/__tests__/rls-org-subscriptions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short leanshot/src/lib/__tests__/rls-org-subscriptions.test.ts` shows `D ` (deletion staged)
    - File no longer exists on disk: `test -f leanshot/src/lib/__tests__/rls-org-subscriptions.test.ts` returns non-zero exit
    - `_fixtures/p28-rls-fixture.ts` (or `.ts`) still exists: `ls leanshot/src/lib/__tests__/_fixtures/ | grep p28-rls-fixture` returns >= 1
  </acceptance_criteria>
  <done>Test file deletion staged via git rm; fixture module preserved for sibling tests.</done>
</task>

<task type="auto">
  <name>Task 13: Run local test sweep — npm build + lint + vitest + Deno tests</name>
  <files>(no file edits — verification only)</files>
  <read_first>
    - /Users/karstenhaldan/minisite/leanshot/CLAUDE.md (test command conventions; vitest config gotcha)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite`, run the four verification commands. PWD invariant: assert via `git rev-parse --show-toplevel` returning `/Users/karstenhaldan/minisite` BEFORE each `cd leanshot/` block.

    1. cd leanshot && npm run build
       (must exit 0; `tsc -b && vite build` — catches type errors from any missed references)

    2. cd leanshot && npm run lint
       (must exit 0; ESLint flat config; no `no-raw-service-role-client` violations introduced)

    3. cd leanshot && npx vitest run --config vite.config.ts
       (must exit 0; explicit `--config vite.config.ts` per [[reference_vitest_4_projects_config_masks_default]] — the leanshot/vitest.config.ts `projects:` block masks the default test config)

    4. From `/Users/karstenhaldan/minisite` (NOT inside leanshot/):
       $HOME/.deno/bin/deno test --no-check \
         supabase/functions/stripe-webhook/ \
         supabase/functions/_shared/ \
         supabase/functions/demo-org-purge/
       (must exit 0; `--no-check` per [[reference_deno_binary_path]]; cross-Fn sweep optional but recommended)

    If any command fails:
    - npm build TypeError → re-read the failing file, find any leftover `org_subscriptions` / `org_subscription_id` reference, patch and re-run
    - lint failure → run `npm run lint:fix`; if still failing, the rule is genuinely violated — fix the code
    - vitest failure → identify the failing test; cross-check against Tasks 7 + 11 fixtures
    - Deno test failure → the helper rewrite (Task 5) likely has a typo; check signature unchanged

    Per [[reference_deno_test_top_level_serve_trap]]: if Deno test hangs or aborts on top-level `Deno.serve()`, this is the existing project-wide trap — narrow the test path to the specific changed file (e.g. `supabase/functions/stripe-webhook/events/checkout-session-completed.test.ts`). Document the workaround in Plan 02 SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npm run build 2>&1 | tail -3 && npm run lint 2>&1 | tail -3 && npx vitest run --config vite.config.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (vite build success — no TypeScript errors)
    - `npm run lint` exits 0 (no ESLint violations)
    - `npx vitest run --config vite.config.ts` exits 0 with summary line showing 0 failures
    - `$HOME/.deno/bin/deno test --no-check supabase/functions/stripe-webhook/` exits 0 OR the failures are exclusively from the known top-level `Deno.serve()` trap (narrow-path retry must pass for the directly-edited files)
    - No new test failures introduced (compare against pre-edit baseline if questionable)
  </acceptance_criteria>
  <done>All four verification commands pass; any Deno-serve-trap workarounds documented for Plan 02 SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 14: Atomic commit — single fix() commit for all 65.1-01 changes</name>
  <files>(commit; no file edits)</files>
  <read_first>
    - /Users/karstenhaldan/minisite/.gitignore (sanity-check no surprise paths excluded)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite`:

    1. Assert PWD: `git rev-parse --show-toplevel` MUST print `/Users/karstenhaldan/minisite` exactly. If it prints anything else (e.g. `/Users/karstenhaldan/minisite/leanshot`), `cd /Users/karstenhaldan/minisite` first and re-assert.

    2. Stage everything (Tasks 1-12 changes — renames, body rewrites, fn edits, test patches, deletion):
       git add supabase/migrations/20290104000001_subscriptions_tax_id.sql
       git add supabase/migrations/20290104000006_tax_collection_log.sql
       git add supabase/migrations/20290106000001_enable_rls_6_public_tables.sql
       git add supabase/functions/stripe-checkout/index.ts
       git add supabase/functions/stripe-webhook/events/tax-collection-log.ts
       git add supabase/functions/stripe-webhook/events/checkout-session-completed.ts
       git add supabase/functions/stripe-webhook/events/checkout-session-completed.test.ts
       git add supabase/functions/_shared/with-org-scope.ts
       git add supabase/functions/demo-org-purge/handler.ts
       git add leanshot/src/lib/__tests__/with-org-scope.test.ts
       git add -u leanshot/src/lib/__tests__/rls-org-subscriptions.test.ts    # deletion

       (the two `git mv` from Task 1 are already staged)

    3. Run `git status --short` and verify exactly these paths appear (R=rename, M=modified, D=deleted, A=added):
       R  supabase/migrations/20290104000001_org_subscriptions_tax_id.sql -> supabase/migrations/20290104000001_subscriptions_tax_id.sql
       M  supabase/migrations/20290104000001_subscriptions_tax_id.sql     (post-rename body rewrite)
       M  supabase/migrations/20290104000006_tax_collection_log.sql
       R  supabase/migrations/20290106000001_enable_rls_7_public_tables.sql -> supabase/migrations/20290106000001_enable_rls_6_public_tables.sql
       M  supabase/migrations/20290106000001_enable_rls_6_public_tables.sql (post-rename header edit)
       M  supabase/functions/stripe-checkout/index.ts
       M  supabase/functions/stripe-webhook/events/tax-collection-log.ts
       M  supabase/functions/stripe-webhook/events/checkout-session-completed.ts
       M  supabase/functions/stripe-webhook/events/checkout-session-completed.test.ts
       M  supabase/functions/_shared/with-org-scope.ts
       M  supabase/functions/demo-org-purge/handler.ts
       M  leanshot/src/lib/__tests__/with-org-scope.test.ts
       D  leanshot/src/lib/__tests__/rls-org-subscriptions.test.ts

       Note: rename + body edit may appear as one combined R-with-percent-similarity line per file; both forms are acceptable as long as the new path is correct.

    4. Final PWD re-assert: `[[ "$(git rev-parse --show-toplevel)" == "/Users/karstenhaldan/minisite" ]] || exit 1`

    5. Commit:
       git commit -m "fix(65.1-01): replan org_subscriptions → subscriptions canonical (planner-vs-archived-schema drift)

       Phase 29 D-14 dropped public.org_subscriptions 4 phases before Phase 65 was
       planned. Phase 69.7 Plan 01 Task 3 (supabase db push --linked) halted
       2026-05-27 at 20290104000001 with SQLSTATE 42P01.

       This commit rewrites the 3 Phase 65 migrations to target the canonical
       public.subscriptions table (with clinic_id IS NOT NULL filtering for clinic
       data per Phase 14 + Phase 29 D-14), and sweeps 5 Edge Fn files + 2 test
       files for residual org_subscriptions / org_subscription_id references.

       Migrations:
         - 20290104000001 renamed + body retargets public.subscriptions.tax_id
           (clinic+tax_id partial index)
         - 20290104000006 FK column rename: org_subscription_id uuid → subscription_id text
           references public.subscriptions(id); sub-or-org CHECK dropped
         - 20290106000001 renamed (_7 → _6) + org_subscriptions block removed

       Consumers swept:
         - stripe-checkout/index.ts: comment-only
         - stripe-webhook/events/tax-collection-log.ts: drop dual-path; resolve
           only subscription_id (text)
         - stripe-webhook/events/checkout-session-completed.ts: mirror tax_id to
           public.subscriptions filtered by .eq('id', subId)
         - stripe-webhook/events/checkout-session-completed.test.ts: T10/T11/T12
           fixtures updated
         - _shared/with-org-scope.ts: drop 'org_subscriptions' from ORG_SCOPED_TABLES
         - demo-org-purge/handler.ts: drop 'org_subscriptions' from ORG_CHILD_TABLES
         - leanshot/src/lib/__tests__/with-org-scope.test.ts: fixture updated
         - leanshot/src/lib/__tests__/rls-org-subscriptions.test.ts: DELETED

       Verified locally: npm run build, npm run lint, vitest, Deno test sweep all
       pass. Remote 'supabase db push --linked' deferred to 65.1-02.

       Closes Phase 65.1 success criteria 1-4 + 6. Criteria 5 + 7 (remote push +
       Phase 69.7 unblock) ship in 65.1-02.

       Refs: feedback_planner_vs_archived_schema_drift, reference_supabase_back_dated_migration_blocks_push, feedback_phase_close_out_supabase_gotchas, reference_state_complete_phase_writes_wrong_counters, reference_vitest_4_projects_config_masks_default, reference_deno_binary_path"

    6. Verify commit landed: `git log -1 --oneline` should show the commit message head. `git log -1 --stat` should list ~12 files changed (the 2 renames may compact to one line each).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && [[ "$(git rev-parse --show-toplevel)" == "/Users/karstenhaldan/minisite" ]] && git log -1 --pretty=%s | grep -c "fix(65.1-01)" && git log -1 --stat | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --show-toplevel` printed `/Users/karstenhaldan/minisite` before AND after commit
    - Commit subject matches `fix(65.1-01): replan org_subscriptions → subscriptions canonical`
    - `git log -1 --stat` shows ~12 files changed (10 modifications + 1 deletion + the renames may appear as either renames or modifications depending on similarity threshold)
    - `git diff HEAD~1 HEAD --name-only | grep -c "org_subscriptions"` returns `0` (no path still contains the dropped-table name after the rename)
    - Working tree clean: `git status --short` returns empty
  </acceptance_criteria>
  <done>Single atomic commit lands all 11 file mutations + 1 deletion at the monorepo git root; working tree clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe webhook → Edge Fn | Untrusted POST from Stripe; signature verified upstream in stripe-webhook/index.ts (unchanged by this plan). |
| Edge Fn → supabase service-role | Service-role client mutates public.subscriptions / public.tax_collection_log via PostgREST; RLS bypass + with-org-scope Proxy where applicable. |
| Local dev → git index | git mv preserves blame history; renames vs add-delete must not lose the Phase 29 BREAKING CHANGE lineage on touched files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65.1-01 | Tampering | Migration 20290104000001 body | mitigate | `add column if not exists` guard; partial index IMMUTABLE-safe; no `org_subscriptions` reference anywhere (grep-gated in acceptance criteria). |
| T-65.1-02 | Tampering | Migration 20290104000006 FK | mitigate | FK references `public.subscriptions(id)` exclusively; `on delete set null` preserved; CHECK constraint dropped (nullable column is downstream-filterable). |
| T-65.1-03 | Information Disclosure | tax_id mirror logging | mitigate | console logs include `tax_id_set: true/false` only; never `firstTaxId.value` (T-65-04-02 carry-over from Phase 65). |
| T-65.1-04 | Denial of Service | Webhook tax_id mirror failure | accept | Per T-65-04-04: tax_id mirror wrapped in try/catch; never re-throws. Webhook returns 200 even if subscriptions.update fails. |
| T-65.1-05 | Repudiation | git mv preserving blame | mitigate | Task 1 uses `git mv` (not rm+add); `git log --follow` on renamed files preserves Phase 65 author lineage. |
| T-65.1-SC | Tampering | npm/pip/cargo installs | mitigate | NO new package installs in this plan — `npm run build/lint/test` uses existing deps only. No npm install needed. |
</threat_model>

<verification>
End-to-end gate for Plan 65.1-01:
- All 14 task-level `<verify><automated>` commands pass
- `grep -rE "org_subscriptions|org_subscription_id" supabase/migrations/2029* supabase/functions/ leanshot/src/` returns 0 hits (full sweep)
- `git log -1 --pretty=%s` matches `fix(65.1-01):` prefix
- Working tree clean post-commit (`git status --short` empty)

Plan 65.1-02 owns the remote `supabase db push --linked` verification; this plan does NOT push.
</verification>

<success_criteria>
- All migration body files contain zero references to `public.org_subscriptions` or `org_subscription_id`
- All 5 Edge Fn files swept; ORG_SCOPED_TABLES + ORG_CHILD_TABLES no longer list the dropped table
- Test fixtures (with-org-scope.test.ts, checkout-session-completed.test.ts) reflect post-rewrite reality
- `rls-org-subscriptions.test.ts` deleted via `git rm`
- `npm run build`, `npm run lint`, `vitest`, Deno test sweep — all pass locally
- Single atomic commit at `/Users/karstenhaldan/minisite` with message `fix(65.1-01): replan org_subscriptions → subscriptions canonical (planner-vs-archived-schema drift)`
</success_criteria>

<output>
Create `.planning/phases/65.1-phase-65-migration-replan-org-subscriptions-subscriptions-ca/65.1-01-SUMMARY.md` when done. Reference the canonical schema (Phase 14 `public.subscriptions`), the Phase 29 D-14 BREAKING CHANGE that motivated the rewrite, and the `feedback_planner_vs_archived_schema_drift` memory.
</output>
