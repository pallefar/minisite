# Phase 65.1: Migration replan — `org_subscriptions` → `subscriptions` canonical - Context

**Gathered:** 2026-05-27
**Status:** Ready for planning
**Mode:** Infrastructure / code-rewrite phase — discuss skipped (all 7 success criteria are file-level/CLI-level; per `feedback_compress_planner_skip_research_patterns`)

<domain>
## Phase Boundary

Rewrite Phase 65 artifacts that target `public.org_subscriptions` (dropped by Phase 29 D-14 as BREAKING CHANGE) to instead target the canonical `public.subscriptions` table with `WHERE clinic_id IS NOT NULL` filtering for clinic-specific data.

**Discovered:** Phase 69.7 Plan 01 Task 3 (`supabase db push --linked`) halted 2026-05-27 at `20290104000001_org_subscriptions_tax_id.sql` with `relation "public.org_subscriptions" does not exist (SQLSTATE 42P01)`. Phase 29's `20270601200001_p29_reconcile.sql` explicitly dropped `org_subscriptions` 4 phases before Phase 65 was planned. Diagnosis: **planner-vs-archived-schema drift** (see [[feedback_planner_vs_archived_schema_drift]] memory).

**Out of scope:** Any new Phase 65 functionality. This phase is a pure surgical rewrite — no new REQ IDs, no UX changes.

</domain>

<decisions>
## Implementation Decisions

### Canonical schema target (per Phase 14 + Phase 29 D-14)
- **Table:** `public.subscriptions` (Phase 14, migration `20260601000019_stripe_subscriptions.sql`)
- **PK:** `id text` (Stripe `sub_xxx` natural key)
- **Clinic filter:** `clinic_id uuid references public.orgs(id) on delete cascade` (NOT NULL for clinic subs; mutually exclusive with `user_id` per CHECK constraint)
- **Existing partial index:** `idx_subscriptions_clinic on public.subscriptions(clinic_id) where clinic_id is not null`
- **Columns to add for Phase 65 needs:** `tax_id text` (B2B-only — partial index `WHERE clinic_id IS NOT NULL AND tax_id IS NOT NULL`)

### Migration rewrite scope (3 files)

**1. `supabase/migrations/20290104000001_org_subscriptions_tax_id.sql` — REWRITE**
- Current: `alter table public.org_subscriptions add column if not exists tax_id text` + partial index on `org_subscriptions(tax_id) where tax_id is not null`
- Target: `alter table public.subscriptions add column if not exists tax_id text` + partial index `idx_subscriptions_clinic_tax_id on public.subscriptions(clinic_id, tax_id) where clinic_id is not null and tax_id is not null`
- Filename should also be renamed via `git mv` to `20290104000001_subscriptions_tax_id.sql` to reflect new target

**2. `supabase/migrations/20290104000006_tax_collection_log.sql` — REWRITE FK column**
- Current: `org_subscription_id uuid references public.org_subscriptions(org_id) on delete set null` + index on `org_subscription_id`
- Target: `subscription_id text references public.subscriptions(id) on delete set null` + index on `subscription_id`
- **Type change:** uuid → text (subscriptions.id is text)
- Update CHECK constraints, RLS policies, and any other in-file references to the renamed column
- Filename stays the same (no migration body filename change needed — just internal references)

**3. `supabase/migrations/20290106000001_enable_rls_7_public_tables.sql` — DROP the org_subscriptions block**
- Current: contains a DO/EXCEPTION-wrapped `alter table public.org_subscriptions enable row level security` block (already drift-safe — no-ops on missing table)
- Target: DELETE the entire org_subscriptions DO block (it's misleading — the table doesn't exist and never will). Keep all 6 other table blocks.
- Update header comment count `7 public tables` → `6 public tables`. Rename file via `git mv` to `20290106000001_enable_rls_6_public_tables.sql`

### Consumer code rewrite scope (per repo grep)

**Edge Fns:**
- `supabase/functions/stripe-checkout/index.ts:544` — comment only (mentions `org_subscriptions.tax_id` mirror). Update comment to `subscriptions.tax_id WHERE clinic_id IS NOT NULL`.
- `supabase/functions/stripe-webhook/events/tax-collection-log.ts` — multiple refs. The function resolves `subscription_id` (text) OR `org_subscription_id` (uuid) from the Stripe session. **Rewrite:** drop the dual-path; resolve only `subscription_id` (text, from Stripe session subscription field). The clinic distinction is now `subscriptions.clinic_id IS NOT NULL` — derive from a downstream query, not from a separate ID column.
- `supabase/functions/stripe-webhook/events/checkout-session-completed.ts:157-181` — mirrors collected B2B tax_id to `org_subscriptions.tax_id`. **Rewrite:** mirror to `subscriptions.tax_id` filtered by the stored `subscription_id` (which uniquely identifies a clinic sub via the `clinic_id` link); update the supabase.from('org_subscriptions') call to `.from('subscriptions')` with `.update({tax_id})` and `.eq('id', subscriptionId)`.
- `supabase/functions/stripe-webhook/events/checkout-session-completed.test.ts` — test fixtures expect `org_subscriptions` table. **Rewrite:** update fixtures to `subscriptions` table with `clinic_id` set.
- `supabase/functions/_shared/with-org-scope.ts:45` — `'org_subscriptions'` is in the list of org-scoped tables. **Remove** from the list (table doesn't exist).
- `supabase/functions/demo-org-purge/handler.ts:29,89` — `'org_subscriptions'` listed as a table to purge for demo orgs. **Remove** from the list and update the header comment.

**Test files:**
- `src/lib/__tests__/with-org-scope.test.ts` — adjust fixtures to drop `org_subscriptions` from expected tables.
- `src/lib/__tests__/rls-org-subscriptions.test.ts` — **DELETE entire file**. The test exists for a table that doesn't exist; nothing to test. Add a brief inline comment in the with-org-scope test noting why this fixture was removed.

### Plan Decomposition (2 plans)

**Plan 65.1-01 — Migration rewrite + consumer sweep + local tests**
- Wave 0
- autonomous: true (no operator gate)
- Requirements: PAY-01..11 (re-validate; no new REQ)
- Tasks:
  1. `git mv` migrations 01 + 06 + 06 if filename changes warranted
  2. Rewrite migration 01 body (target subscriptions.tax_id)
  3. Rewrite migration 06 body (FK column rename + type change)
  4. Edit migration 06's RLS file: delete org_subscriptions DO block
  5. Update 5 Edge Fn files (stripe-checkout comment, tax-collection-log, checkout-session-completed + test, with-org-scope, demo-org-purge)
  6. Delete `src/lib/__tests__/rls-org-subscriptions.test.ts` entirely
  7. Update `src/lib/__tests__/with-org-scope.test.ts` fixtures
  8. Run `npm run build` + `npm run lint` + vitest in leanshot/ → must pass
  9. Run Deno test sweep on the 5 modified Edge Fns → must pass (cross-Fn sweep optional)
  10. Atomic commit: `fix(65.1): replan org_subscriptions → subscriptions canonical (planner-vs-archived-schema drift)`

**Plan 65.1-02 — Push verification + SUMMARY + ROADMAP/STATE flip**
- Wave 1
- depends_on: 65.1-01
- autonomous: true (operator drift was the prior blocker; resolved via THIS phase's code rewrite, not psql intervention)
- Requirements: DEPLOY-01 (verify subset — this phase's success criteria 5+6)
- Tasks:
  1. `npx supabase db push --linked` → must succeed for all 18 pending migrations (including the 3 rewritten Phase 65 ones)
  2. `npx supabase db advisors --linked --type security --level error` → 0 entries
  3. `npx supabase db lint --linked --fail-on error` → 0 entries (or operator-disposed)
  4. Re-run vitest + Deno tests against now-real-remote-schema (no fixture mocks) — must still pass
  5. Write `65.1-SUMMARY.md`
  6. Flip ROADMAP Phase 65.1 entry to `- [x]`; update STATE.md manually (per `reference_state_complete_phase_writes_wrong_counters` — never use `state.complete-phase`); set "Operator next" → `/gsd-autonomous --from 69.7 --to 69.7`
  7. Final commit: `chore(65.1): close phase 65.1 — migration replan complete; 69.7 unblocked`

### Critical invariants
- **PWD invariant** (per `feedback_worktree_executor_pwd_drift_leaks_to_main`): every commit must run from `/Users/karstenhaldan/minisite` (the monorepo git root, NOT `leanshot/`). Assert via `git rev-parse --show-toplevel`.
- **No worktree isolation** (per `feedback_phase_close_out_supabase_gotchas` (3)): ambient supabase CLI auth + cwd-sensitive SDK state verbs require INLINE execution in main checkout.
- **No `CREATE POLICY IF NOT EXISTS`** (per same memory): if any retroactive RPC migration is drafted, use bare `CREATE POLICY` or `DROP POLICY IF EXISTS` + `CREATE POLICY`.
- **Do NOT use `state.complete-phase` SDK verb** (per `reference_state_complete_phase_writes_wrong_counters`) — manually edit STATE.md.
- **Migration filename order** (per `feedback_migration_filename_dependency_order`): if any rewrite forces filename change, ensure all timestamps remain forward-ordered against remote's last-applied (20290103000005).
- **No new feature code** — this is a pure rewrite. If a consumer file has unrelated changes pending, surface separately.

</decisions>

<code_context>
## Existing Code Insights

### Canonical schema (Phase 14)
- `public.subscriptions` (`supabase/migrations/20260601000019_stripe_subscriptions.sql`): one row per user OR clinic; `id text PK = Stripe sub_xxx`; CHECK enforces exactly one of `user_id` / `clinic_id` is non-null; partial indexes exist on both filter columns.
- `public.subscription_events` exists alongside, FK `subscription_id text references subscriptions(id) on delete set null`.

### Phase 29 BREAKING CHANGE (4 phases before Phase 65)
- `supabase/migrations/20270601200001_p29_reconcile.sql`: `drop table if exists public.org_subscriptions cascade;`
- Header explicitly states: *"BREAKING CHANGE: public.org_subscriptions DROPPED — downstream callers MUST use public.subscriptions WHERE clinic_id IS NOT NULL for clinic subscriptions. Phase 14 subscriptions table is canonical going forward."*

### Affected Phase 65 artifacts (already enumerated above)
- 3 migration files (2 hard-broken, 1 drift-safe but misleading)
- 5 Edge Fn files (1 comment-only, 4 real refactor)
- 2 test files (1 to delete, 1 fixture update)

### Existing patterns to reuse
- `supabase.from('subscriptions').update({tax_id}).eq('id', subscriptionId)` — standard Phase 65 subscriptions mutation pattern (e.g. dunning-state update in `20290104000002_subscriptions_dunning_state.sql` + corresponding Edge Fn).
- Partial-index `WHERE clinic_id IS NOT NULL` matches existing `idx_subscriptions_clinic` style (`20260601000019_stripe_subscriptions.sql:73`).
- DO/EXCEPTION drift-guard pattern from Phase 66.5 `20290106000001` — already in repo; can be applied to the rewritten migration 01 + 06 as belt-and-suspenders if desired.

</code_context>

<specifics>
## Specific Ideas

- Diagnostic artifact preserved: `/Users/karstenhaldan/minisite/leanshot/.planning/phases/69.7-…/artifacts/db-push.log` captures the failing SQLSTATE 42P01 — Plan 65.1-01's RED test should reference this exact error string as the "before" state.
- Forward-compat: once Phase 65.1 lands, the in-progress Phase 69.7 Plan 01 Tasks 3-6 + Plans 02/03/04 can run unchanged. No new dependencies created by 65.1.
- File rename via `git mv` (not write+delete) so blame history transfers.

</specifics>

<deferred>
## Deferred Ideas

- **Retroactive audit of OTHER late-phase migrations targeting potentially-archived schema** — possible Phase 69.7 close-out tech-debt item; outside 65.1 scope. Run `for tbl in $(grep -rhE 'references public\.[a-z_]+' supabase/migrations/2029*.sql | sed 's/.*public\.\([a-z_]*\).*/\1/' | sort -u); do echo "$tbl:"; grep -l "drop table.*$tbl" supabase/migrations/2026*.sql 2027*.sql 2028*.sql; done` to find others.
- **Plan-checker enhancement** to grep historical migrations for `drop table` lineage of every newly-referenced table — see `feedback_planner_vs_archived_schema_drift` "How to apply" #1. Track as GSD SDK issue.
- **STATE.md "Phase 65 close-out" line correction** — STATE.md currently says "5 Rule-1 schema drift fixes" including "org_subscriptions PK=org_id". That's not actually a Phase 65 fix that landed; it was misclassified. Update the STATE.md historical entry as part of 65.1-02 SUMMARY.

</deferred>
