---
phase: 02.1-spa-lighthouse-perf
plan: 05
type: execute
wave: 5
depends_on: ["02.1-01", "02.1-02", "02.1-03", "02.1-04"]
files_modified:
  - leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-BUNDLE-MEASUREMENT.md
  - leanshot/.planning/phases/02-visible-compliance-public-deploy/02-02-BUNDLE-MEASUREMENT.md
  - leanshot/.planning/ROADMAP.md
autonomous: false
requirements: [PROD-01]
tags: [lighthouse, verification, documentation, sign-off]

must_haves:
  truths:
    - "On the final post-fix Preview deployment, `npx --yes @lhci/cli@0.15.1 collect --url=<SPA-PREVIEW-URL> --numberOfRuns=3 --settings.preset=desktop` reports median Performance ≥ 0.90 (SC#1)"
    - "Same lhci run reports Accessibility ≥ 0.95 AND Best Practices ≥ 0.93 (SC#4 — no regression)"
    - "`scripts/assert-vendor-react-size.sh` exits 0 against the final dist/ (SC#2)"
    - "EITHER FCP ≤ 1.8s + LCP ≤ 2.5s (SC#3 path A) OR `dist/index.html` contains `<link rel=\"modulepreload\" href=\"/assets/vendor-react-*.js\">` (SC#3 path B)"
    - "`02.1-BUNDLE-MEASUREMENT.md` exists and documents the root cause + applied fixes + post-fix chunk shape + post-fix Lighthouse scores (SC#5)"
    - "Phase 2.1 entry in ROADMAP.md is updated to Status=Complete with Plans Complete=N/N and Completed date"
    - "`02-02-BUNDLE-MEASUREMENT.md` carries an addendum block linking forward to 02.1-BUNDLE-MEASUREMENT.md so the next reader of the Phase 2 measurement doc understands why the projected vendor-react number didn't materialise and what fixed it"
    - "A human checkpoint confirms the deployed Preview URL Lighthouse score in a browser run (not just CLI lhci)"
  artifacts:
    - path: "leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-BUNDLE-MEASUREMENT.md"
      provides: "Phase 2.1 bundle-topology change log + post-fix Lighthouse scores + carry-forward notes"
      contains: "vendor-react"
    - path: "leanshot/.planning/phases/02-visible-compliance-public-deploy/02-02-BUNDLE-MEASUREMENT.md"
      provides: "Backreference / addendum pointing forward to 02.1-BUNDLE-MEASUREMENT.md"
      contains: "Phase 2.1"
    - path: "leanshot/.planning/ROADMAP.md"
      provides: "Updated Phase 2.1 status + plan count + completion date"
      contains: "Phase 2.1"
  key_links:
    - from: "lhci 3-run on deployed Preview"
      to: "Phase 2.1 SC#1 Performance ≥ 0.90"
      via: "median of 3 runs"
      pattern: "performance"
    - from: "02.1-BUNDLE-MEASUREMENT.md"
      to: "02-02-BUNDLE-MEASUREMENT.md addendum"
      via: "explicit pointer"
      pattern: "Phase 2.1"
---

<objective>
Final verification and documentation for Phase 2.1. This wave runs AFTER all applied performance levers (Wave 1 always; Waves 2, 3 conditionally) AND after the CI improvements (Wave 4). Its purpose is to:

1. **Verify the final deployed Preview hits all five SCs** with a 3-run lhci measurement.
2. **Document the bundle-topology change** in `02.1-BUNDLE-MEASUREMENT.md` (SC#5) and add a backreference to `02-02-BUNDLE-MEASUREMENT.md`.
3. **Update ROADMAP.md** to mark Phase 2.1 complete with the actual plans-completed count and completion date.
4. **Human-eyeball checkpoint** — the developer opens the Preview URL in a real browser, runs Lighthouse from DevTools (not just CLI lhci), confirms the score visually, and signs off. RESEARCH §"Wave 5" recommends this as the final gate before declaring victory; lhci CLI scores can occasionally drift from in-browser measurements due to extension load, throttling differences, or first-vs-warm cache state.

Purpose: Close out Phase 2.1 with verifiable measurements (CLI + human-confirmed) and a paper trail that the next reader (whether opening 02-02-BUNDLE-MEASUREMENT.md or the Phase 2.1 directory) can follow without spelunking commit history.

Output:
- `02.1-BUNDLE-MEASUREMENT.md` documenting root cause, fix path, post-fix chunk sizes, post-fix Lighthouse scores, carry-forward notes
- Addendum block at the bottom of `02-02-BUNDLE-MEASUREMENT.md` pointing forward to 02.1
- Updated `ROADMAP.md` Phase 2.1 entry: Status, Plans Complete, Completed date
- Human checkpoint confirmed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/PROJECT.md
@leanshot/.planning/ROADMAP.md
@leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-RESEARCH.md
@leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-01-SUMMARY.md
@leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-02-SUMMARY.md
@leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-03-SUMMARY.md
@leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-04-SUMMARY.md
@leanshot/.planning/phases/02-visible-compliance-public-deploy/02-02-BUNDLE-MEASUREMENT.md
@leanshot/.planning/phases/02-visible-compliance-public-deploy/02-HUMAN-UAT.md
@leanshot/lighthouserc.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run lhci 3-run on the final post-fix Preview and capture all five SCs</name>
  <files>leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-MEASUREMENT-LOG.md</files>
  <behavior>
    - The deployed Preview being measured is the one produced by the PR carrying Phase 2.1's commits (Waves 1, 4, and conditionally 2 and 3). Use the same Vercel preview URL pattern as 02-HUMAN-UAT Section A1 (`leanshot-app-*.vercel.app`).
    - Three independent lhci runs against that URL with the desktop preset. Median Performance is the gate (SC#1).
    - Results captured in `02.1-MEASUREMENT-LOG.md` with verbatim CLI output (raw scores per run, median calculation, FCP/LCP/TBT/SI/CLS Web Vitals).
    - All five SCs have explicit pass/fail next to them with the supporting measurement.

    Test cases:
    - Case 1: Run 1 / Run 2 / Run 3 Performance scores recorded; median ≥ 0.90 → SC#1 PASS.
    - Case 2: A11y ≥ 0.95 AND BP ≥ 0.93 in the same run → SC#4 PASS.
    - Case 3: vendor-react chunk size from `gzip -c dist/assets/vendor-react-*.js | wc -c` ≥ 30000 → SC#2 PASS.
    - Case 4: EITHER (FCP median ≤ 1800ms AND LCP median ≤ 2500ms) OR `grep -q 'modulepreload.*vendor-react' dist/index.html` returns true → SC#3 PASS.
    - If any of SC#1, SC#2, SC#3, or SC#4 FAIL, the executor escalates to the next playbook lever (back to Wave 2 if Wave 1 was the last fix; back to Wave 3 if Wave 2 was the last fix; or escalate to user if all three rungs are exhausted).
  </behavior>
  <action>
    1. Identify the SPA Preview URL for the current PR. If running locally before opening a PR, push the branch first and let Vercel deploy; capture the URL from `gh pr view --json statusCheckRollup` or by inspecting the Vercel project's recent deployments via `gh api`.

    2. Run lhci 3 times against that URL with the desktop preset:

       ```bash
       npx --yes @lhci/cli@0.15.1 collect --url=<SPA-PREVIEW-URL> --numberOfRuns=3 --settings.preset=desktop
       npx --yes @lhci/cli@0.15.1 assert
       ```

       The `assert` step uses the assertions in `leanshot/lighthouserc.json` (Performance ≥ 0.90, Accessibility ≥ 0.95, Best Practices ≥ 0.93 — verify these thresholds match the SCs; if not, update lighthouserc.json IN-LINE with this task and capture the diff).

    3. Capture the raw CLI output. lhci writes a JSON report to `.lighthouseci/`; extract:
       - Per-run Performance score (3 numbers)
       - Median Performance score
       - Per-run FCP, LCP, TBT, SI, CLS
       - Median FCP and LCP (for SC#3 thresholds)
       - Per-run Accessibility + Best Practices

    4. Run the chunk-size assertion locally against the same dist/:

       ```bash
       cd leanshot && npm run build && bash scripts/assert-vendor-react-size.sh
       ```

       Capture the size and exit status.

    5a. **Record the actual vendor-react and index chunk sizes for the W1 tighten-the-floor follow-up** (Plan 01 / RESEARCH §W1):

       ```bash
       VR_FILE=$(find leanshot/dist/assets -name 'vendor-react-*.js' -type f -not -name '*.map' | head -1)
       IDX_FILE=$(find leanshot/dist/assets -name 'index-*.js' -type f -not -name '*.map' | head -1)
       echo "vendor-react actual size: $(gzip -c "$VR_FILE" | wc -c) bytes gz"
       echo "index actual size: $(gzip -c "$IDX_FILE" | wc -c) bytes gz"
       echo "Proposed tightened vendor-react floor (actual - 10kB): $(($(gzip -c "$VR_FILE" | wc -c) - 10000)) bytes gz"
       ```

       Capture both numbers and the proposed tightened floor for Section 6 of `02.1-BUNDLE-MEASUREMENT.md` (Task 2 below).

    5. Inspect the deployed `index.html` for the modulepreload link:

       ```bash
       curl -s <SPA-PREVIEW-URL>/index.html | grep -E 'modulepreload[^>]*vendor-react'
       ```

    6. Write `leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-MEASUREMENT-LOG.md` with the structure:

       ```markdown
       # Phase 2.1 Final Measurement Log

       **Measured:** <date>
       **Preview URL:** <url>
       **Commit SHA:** <sha>
       **lhci version:** 0.15.1
       **Preset:** desktop

       ## Per-run scores

       | Run | Performance | Accessibility | Best Practices | FCP (ms) | LCP (ms) | TBT (ms) | SI (ms) | CLS |
       |-----|-------------|---------------|----------------|----------|----------|----------|---------|-----|
       | 1 | ... | ... | ... | ... | ... | ... | ... | ... |
       | 2 | ... | ... | ... | ... | ... | ... | ... | ... |
       | 3 | ... | ... | ... | ... | ... | ... | ... | ... |
       | **Median** | ... | ... | ... | ... | ... | ... | ... | ... |

       ## Success Criteria

       | SC | Threshold | Measured | Status |
       |----|-----------|----------|--------|
       | SC#1 | Performance ≥ 0.90 | <median> | PASS / FAIL |
       | SC#2 | vendor-react ≥ 30 kB gz | <bytes> | PASS / FAIL |
       | SC#3 | FCP ≤ 1800ms AND LCP ≤ 2500ms — OR — modulepreload present in index.html | <fcp/lcp> / <preload-line-or-N/A> | PASS / FAIL |
       | SC#4 | A11y ≥ 0.95 AND BP ≥ 0.93 | <a11y> / <bp> | PASS / FAIL |
       | SC#5 | 02.1-BUNDLE-MEASUREMENT.md exists | (Task 2 delivers) | (PENDING) |

       ## Final chunk shape (gzipped sizes)

       <output of: for f in leanshot/dist/assets/*.js; do raw=$(wc -c < "$f"); gz=$(gzip -c "$f" | wc -c); printf "%s  raw=%s gz=%s\n" "$(basename "$f")" "$raw" "$gz"; done | sort -t= -k3 -n -r | head -10>

       ## Lever applied

       - [x] Wave 1 (function-form manualChunks) — applied / SUMMARY 02.1-01
       - [ / x] Wave 2 (modulepreload injection) — skipped per measurement gate / applied per gate
       - [ / x] Wave 3 (LazyMotion migration) — skipped per measurement gate / applied per gate
       - [x] Wave 4 (CI improvements) — applied / SUMMARY 02.1-04

       ## Comparison with Phase 2 baseline

       | Metric | Phase 2 ship (E1 in 02-HUMAN-UAT) | Phase 2.1 (this run) | Delta |
       |--------|------------------------------------|----------------------|-------|
       | Performance | 0.74 | <new> | <delta> |
       | FCP (ms) | 3900 | <new> | <delta> |
       | LCP (ms) | 4200 | <new> | <delta> |
       | TBT (ms) | 22 | <new> | <delta> |
       | vendor-react gz | 3009 | <new> | <delta> |
       | index gz | 69543 | <new> | <delta> |
       ```

    7. If any SC failed, escalate per the playbook ladder (do NOT silently mark complete):
       - SC#1 FAIL after Wave 1 only → return to Wave 2 plan; re-run that wave's gate check.
       - SC#1 FAIL after Wave 1 + Wave 2 → return to Wave 2.5 diagnose, then Wave 3 if appropriate.
       - SC#1 FAIL after Wave 1 + Wave 2 + Wave 3 → escalate to user; the playbook is exhausted.
       - SC#4 FAIL → CRITICAL regression. Identify which lever broke A11y or BP and revert.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test -f leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-MEASUREMENT-LOG.md && grep -q 'SC#1' leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-MEASUREMENT-LOG.md && grep -q 'PASS' leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-MEASUREMENT-LOG.md</automated>
    Manual: SC#1, SC#2, SC#3, SC#4 each show PASS in the measurement log. If any FAIL, the wave does not proceed to Task 2 — escalate per the playbook ladder.
  </verify>
  <done>
    - `02.1-MEASUREMENT-LOG.md` exists with per-run scores, median, Web Vitals, chunk-size, modulepreload status
    - All four CLI-verifiable SCs (SC#1, SC#2, SC#3, SC#4) explicitly marked PASS
    - Lever-application table records which waves were applied vs skipped per measurement gates
    - Comparison table shows Phase 2 → Phase 2.1 deltas
    - If any SC failed, documented escalation path (NOT proceeding to Task 2)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write 02.1-BUNDLE-MEASUREMENT.md and add backreference to 02-02-BUNDLE-MEASUREMENT.md (SC#5)</name>
  <files>leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-BUNDLE-MEASUREMENT.md, leanshot/.planning/phases/02-visible-compliance-public-deploy/02-02-BUNDLE-MEASUREMENT.md</files>
  <behavior>
    - `02.1-BUNDLE-MEASUREMENT.md` covers six sections required for SC#5 + RESEARCH §Wave 4 step 19:
      1. Root cause (RESEARCH §"Why object-form `manualChunks` didn't pull react-dom into `vendor-react`")
      2. Fix applied (Wave 1 always; Waves 2, 3 if applied)
      3. Post-fix chunk sizes (verbatim from Task 1's measurement log)
      4. Lighthouse scores before/after with Web Vitals
      5. CI changes (Wave 4 — what was wrong, what was fixed)
      6. Forward-looking notes: Vite 8 / Rolldown migration TODO, CSP nonce caveat (RESEARCH §"Pitfall P8"), framer-motion swap deferred-or-applied note
    - `02-02-BUNDLE-MEASUREMENT.md` gets an "Addendum: Phase 2.1" block at the bottom (NOT in the middle, NOT replacing existing sections) that points forward: "The 5-chunk topology approved here was correct in shape but wrong in encoding — see `02.1-BUNDLE-MEASUREMENT.md` for the function-form fix that delivered the projected savings."
    - Both files render as valid markdown (no broken links, no stray frontmatter).

    Test cases:
    - Case 1: `02.1-BUNDLE-MEASUREMENT.md` exists and contains all six required sections (verify by `grep` for each section header).
    - Case 2: `02-02-BUNDLE-MEASUREMENT.md` ends with an "Addendum: Phase 2.1" section that references `02.1-BUNDLE-MEASUREMENT.md` by relative path.
    - Case 3: All file paths in cross-references are valid (test by `test -f` for each referenced markdown file).
  </behavior>
  <action>
    1. Create `leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-BUNDLE-MEASUREMENT.md`. Use this skeleton (fill from Task 1's measurement log + the Wave SUMMARYs):

       ```markdown
       # Phase 2.1 Bundle Topology Change Log

       **Closed:** <date>
       **Predecessor:** `.planning/phases/02-visible-compliance-public-deploy/02-02-BUNDLE-MEASUREMENT.md` (5-chunk topology decision)
       **Trigger:** 02-HUMAN-UAT Section E1 — SPA shipped at Performance ≈ 0.74 instead of the projected ≥ 0.90

       ## 1. Root cause

       Object-form `manualChunks` in `vite.config.ts` did NOT actually move `react-dom` into `vendor-react`. The named chunk shipped with only `scheduler` (~3 kB gz) and `react-dom` (~98 kB gz) collapsed back into `index`.

       Why: Rollup's object-form semantics combined with Vite's static-graph optimisation. Every consumer of React in `src/` is loaded via `React.lazy(...)` except `main.tsx`'s static `createRoot` import. Rollup decided the entry was the only static consumer of react-dom and folded it into `index`. The named `vendor-react` chunk was reachable only via dynamic-import paths (per `__vite__mapDeps` array inside `index-*.js`), which is why the projected ~107 kB gz savings never materialised.

       Full root-cause analysis: `02.1-RESEARCH.md` §"Why object-form `manualChunks` didn't pull react-dom into `vendor-react`".

       ## 2. Fix applied

       <fill from Task 1's lever-application table>

       - Wave 1 (function-form manualChunks) — APPLIED. Anchored regex (`/node_modules\/(react|react-dom|scheduler)(\/|$)/`) forces ANY id matching the pattern into `vendor-react` regardless of static-vs-lazy graph reachability. SUMMARY: `02.1-01-SUMMARY.md`.
       - Wave 2 (modulepreload injection) — APPLIED / SKIPPED <reason from gate>. SUMMARY: `02.1-02-SUMMARY.md`.
       - Wave 3 (LazyMotion migration) — APPLIED / SKIPPED <reason from gate>. SUMMARY: `02.1-03-SUMMARY.md`.

       ## 3. Post-fix chunk sizes

       <copy the chunk-size table from 02.1-MEASUREMENT-LOG.md>

       Compared with Phase 2 baseline (from `02-02-BUNDLE-MEASUREMENT.md` "Top 10 emitted chunks by gzipped size"):

       | Chunk | Phase 2 ship | Phase 2.1 | Delta |
       |-------|--------------|-----------|-------|
       | vendor-react | 3,009 gz | <new> | <delta> |
       | index | 69,543 gz | <new> | <delta> |

       ## 4. Lighthouse scores before / after

       | Metric | Phase 2 ship (E1) | Phase 2.1 | Threshold | Status |
       |--------|--------------------|-----------|-----------|--------|
       | Performance (median of 3) | 0.74 | <new> | ≥ 0.90 | PASS |
       | Accessibility | 0.95 | <new> | ≥ 0.95 | PASS |
       | Best Practices | 0.93 | <new> | ≥ 0.93 | PASS |
       | FCP (ms) | 3900 | <new> | ≤ 1800 (or modulepreload mask) | PASS |
       | LCP (ms) | 4200 | <new> | ≤ 2500 (or modulepreload mask) | PASS |
       | TBT (ms) | 22 | <new> | already green | PASS |

       ## 5. CI changes (Wave 4)

       Two long-standing CI gaps that allowed the Phase 2 regression to ship undetected:

       - The lhci job was scoring marketing (~1.00), not the SPA, because `wait-for-vercel-preview` returned the alphabetically-first project name. **Fixed:** `check_name: '<exact string>'` filter in the lighthouse job (or split-job fallback per RESEARCH P6 Option 3 — see SUMMARY 02.1-04).
       - There was no chunk-shape guard. **Fixed:** new step in test-e2e job invokes `bash scripts/assert-vendor-react-size.sh` against every PR's build; threshold 30 kB gz on vendor-react.

       Detail: `02.1-04-SUMMARY.md`.

       ## 6. Carry-forward notes

       - **Tighten the chunk-size floor (Phase 2.1 W1 follow-up):** The script's vendor-react floor is 30 kB gz (catches the catastrophic 3 kB Phase 2 regression class) but the predicted post-fix size is 50-60 kB gz. Once the actual measured size is in (Task 1 above records it), propose a follow-up commit raising the floor to `(actual_size - 10000)` for tighter regression guarding. The 80 kB gz ceiling and the 50 kB gz index ceiling should also be reviewed against actuals. Record the actual numbers + the proposed follow-up threshold in this section.
       - **Vite 8 / Rolldown migration:** Vite 8 replaces `manualChunks` with `advancedChunks` (per RESEARCH §"State of the Art"). The function-form pattern landed here is forward-compatible in spirit (Rolldown supports function form) but the migration will require restructuring. TODO in a Phase 2.x tracker once Vite 8 lands.
       - **CSP nonce caveat (RESEARCH P8):** If a future phase replaces `script-src 'self'` with `script-src 'nonce-...'`, the modulepreload `<link>` injected by `emitVendorReactPreload` (if present) will need a `nonce` attribute. The plugin currently does not inject one because no nonce is in scope.
       - **Framer-motion swap (Wave 3):** APPLIED / DEFERRED. <If applied:> The 11-file LazyMotion migration is in place; subsequent phases that add new framer-motion consumers MUST use `<m.X>` imports from `motion/react-m`, not `<motion.X>` from `framer-motion`. The `LazyMotion strict` prop in `main.tsx` will throw at render time on regressions. <If deferred:> Framer-motion stays on the existing `vendor-motion` chunk; Wave 5 measurement showed it was not on the cold-load critical path post-(a) [or post-(a)+(b)], so the swap was unnecessary.
       - **Marketing build untouched:** `vite.marketing.config.ts` continues to use default Rollup chunking; marketing scores ~1.00 and does not need the SPA's lever ladder.
       - **Sentry static import:** `main.tsx:1` still does `import * as Sentry from '@sentry/react'`. This puts vendor-telemetry on the entry's static graph (RESEARCH §Wave 2.5). If a future regression points back to telemetry-on-cold-path, Phase 4 (AI Proxy) will revisit; deferred per RESEARCH §"Wave 2.5".
       ```

    2. Open `leanshot/.planning/phases/02-visible-compliance-public-deploy/02-02-BUNDLE-MEASUREMENT.md` (last line is the bash reproduction snippet inside a code fence). Append at the very end:

       ```markdown

       ---

       ## Addendum: Phase 2.1

       The 5-chunk topology approved above was correct in **shape** but wrong in **encoding**. Object-form `manualChunks` did not actually move `react-dom` into `vendor-react` (chunk shipped with only `scheduler`, ~3 kB gz; react-dom collapsed back into `index`). Phase 2 shipped at Performance ≈ 0.74 instead of the projected ≥ 0.90.

       Phase 2.1 converted to function-form `manualChunks` (anchored regex per `node_modules/<pkg>(\\/|$)`) so the chunking algorithm forces matching ids into the named chunk regardless of static-vs-lazy graph reachability.

       Read the full root-cause analysis, fix details, post-fix chunk sizes, post-fix Lighthouse scores, CI improvements, and carry-forward notes (Vite 8 migration, CSP nonce caveat, framer-motion status) in:

       → `../02.1-spa-lighthouse-perf/02.1-BUNDLE-MEASUREMENT.md`
       ```

    3. Verify both files render as valid markdown:
       - `02.1-BUNDLE-MEASUREMENT.md` has section headers `## 1.` through `## 6.`
       - `02-02-BUNDLE-MEASUREMENT.md` ends with `→ \`../02.1-spa-lighthouse-perf/02.1-BUNDLE-MEASUREMENT.md\``
       - Cross-reference path resolves: `test -f leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-BUNDLE-MEASUREMENT.md` exits 0
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && test -f leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-BUNDLE-MEASUREMENT.md && grep -q '## 1. Root cause' leanshot/.planning/phases/02.1-spa-lighthouse-perf/02.1-BUNDLE-MEASUREMENT.md && grep -q 'Addendum: Phase 2.1' leanshot/.planning/phases/02-visible-compliance-public-deploy/02-02-BUNDLE-MEASUREMENT.md</automated>
  </verify>
  <done>
    - `02.1-BUNDLE-MEASUREMENT.md` exists with all six sections filled from Task 1's measurement log + the Wave SUMMARYs (SC#5)
    - `02-02-BUNDLE-MEASUREMENT.md` has an Addendum: Phase 2.1 block at the bottom with a relative-path link forward to the new document
    - Cross-references resolve
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update ROADMAP.md Phase 2.1 entry to Complete</name>
  <files>leanshot/.planning/ROADMAP.md</files>
  <behavior>
    - The Phase 2.1 entry in the "## Phases" header list (line 17) gets `- [x]` instead of `- [ ]`.
    - The "## Phase Details" Phase 2.1 detail block's `**Plans**: TBD` line is replaced with the actual count and the per-plan checklist (matching the format used by Phase 1 and Phase 2 detail blocks).
    - The "## Progress" table row for Phase 2.1 is updated: `Plans Complete: N/N`, `Status: Complete`, `Completed: <date>`.
    - All other ROADMAP.md content is UNCHANGED — no other phases, no overview, no header.

    Test cases:
    - Case 1: `grep -E '^\s*- \[x\] \*\*Phase 2\.1' ROADMAP.md` returns 1 line.
    - Case 2: `grep -A1 'Phase 2.1: SPA Lighthouse Performance Fix' ROADMAP.md` (in the progress table) shows `Complete` and a date.
    - Case 3: All other Phase entries in the progress table are unchanged (compare diff before/after — only the Phase 2.1 row changes).
  </behavior>
  <action>
    1. Read `leanshot/.planning/ROADMAP.md` and locate three update sites:

       a. The "## Phases" list (line 17): change `- [ ] **Phase 2.1: SPA Lighthouse Performance Fix** (INSERTED) - ...` to `- [x] **Phase 2.1: SPA Lighthouse Performance Fix** (INSERTED) - ...`.

       b. The "## Phase Details" Phase 2.1 detail block (around line 70-82): the line `**Plans**: TBD` becomes:

          ```markdown
          **Plans:** N plans
          - [x] 02.1-01-PLAN.md — Function-form manualChunks + chunk-size assertion script (Wave 1)
          - [<x or skipped>] 02.1-02-PLAN.md — Modulepreload injection (Wave 2 — applied / skipped per gate)
          - [<x or skipped>] 02.1-03-PLAN.md — LazyMotion migration (Wave 3 — applied / skipped per gate)
          - [x] 02.1-04-PLAN.md — CI improvements: lhci SPA targeting + chunk-size assertion in CI (Wave 4)
          - [x] 02.1-05-PLAN.md — Final lhci verification + 02.1-BUNDLE-MEASUREMENT.md + ROADMAP update (Wave 5)
          ```

          Use `[x]` for plans that ran; use `[ ]` annotated `(skipped per measurement gate)` for plans that didn't ship per Waves 2/3 gates. Match the exact format used by Phase 2's plan list (lines 60-67).

       c. The "## Progress" table row for Phase 2.1 (around line 202): update from `| 2.1. SPA Lighthouse Performance Fix (INSERTED) | 0/TBD | Not started | - |` to `| 2.1. SPA Lighthouse Performance Fix (INSERTED) | N/N | Complete | <date> |`.

    2. Update the footer date stamp at the bottom of ROADMAP.md (last line) from "after Phase 1" to "after Phase 2.1 (SPA Lighthouse Performance Fix)" with the current date — match the existing footer format.

    3. Verify no other Phase rows changed.

    Pitfall guards:
    - Do NOT renumber any phases; the decimal `2.1` is intentional.
    - Do NOT change Phase 3 onwards' `0/TBD` rows.
    - Do NOT touch the "Phase Numbering" explanation block (lines 9-13).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c '^\- \[x\] \*\*Phase 2\.1' leanshot/.planning/ROADMAP.md | grep -q '^1$' && grep -q 'Complete' leanshot/.planning/ROADMAP.md</automated>
  </verify>
  <done>
    - Phase 2.1 entry checkbox flipped to `[x]` in the "## Phases" list
    - "## Phase Details" Phase 2.1 detail block's `**Plans**` line replaced with the actual plan count + per-plan checklist (matching Phase 1/Phase 2 format)
    - "## Progress" table row for Phase 2.1 updated to `N/N`, `Complete`, `<date>`
    - Footer date stamp updated
    - No other phase rows changed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the deployed Preview Lighthouse score in a real browser</name>
  <what-built>
    Phase 2.1 has applied its lever ladder, deployed to Preview, run lhci 3-run from the CLI, written 02.1-BUNDLE-MEASUREMENT.md, and updated ROADMAP.md. The CLI lhci median Performance is reported as ≥ 0.90 (Task 1).

    Per RESEARCH §Wave 5 sign-off recommendation, before declaring Phase 2.1 complete, the developer eyeballs the actual deployed Preview in a real browser and runs Lighthouse from DevTools — to catch any drift between CLI lhci scoring and in-browser scoring (different throttling, extension load, first-vs-warm cache).
  </what-built>
  <how-to-verify>
    1. Open the SPA Preview URL recorded in `02.1-MEASUREMENT-LOG.md` in Chrome (incognito window — disables extensions).
    2. Open DevTools → Lighthouse panel.
    3. Select: Mode = "Navigation (Default)", Categories = Performance + Accessibility + Best Practices, Device = Desktop. Match the same preset lhci used.
    4. Click "Analyze page load".
    5. Wait for the report to render (~30 seconds).
    6. Confirm:
       - Performance score ≥ 90 (the in-browser equivalent of the CLI's 0.90)
       - Accessibility ≥ 95
       - Best Practices ≥ 93
       - No new diagnostic surfaced as a critical issue (the "Diagnostics" section in the LH report)
    7. (Optional but recommended) Run the in-browser Lighthouse a second time. Note the variance between runs in the SUMMARY — if Run 1 = 0.91 and Run 2 = 0.87, the floor is borderline and a follow-up regression is more likely.
    8. Open the deployed `index.html` in the Network tab; confirm:
       - The actual cold-load fetches `/assets/vendor-react-<hash>.js` and `/assets/index-<hash>.js` in parallel (or serially with vendor-react in the static `<link rel="modulepreload">` tags).
       - No 404s, no CSP violations in the Console.
  </how-to-verify>
  <resume-signal>Reply "approved" with the in-browser Performance / Accessibility / Best Practices scores, OR describe any discrepancy with the CLI lhci result.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → future readers | The bundle-measurement docs are the paper trail for why the regression happened and how it was fixed; readers (including future Claude executors) rely on these for context |
| Human eyeball → CLI lhci | The human checkpoint catches CLI-vs-browser drift that automated lhci cannot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-20 | Repudiation | Measurement record | mitigate | Verbatim CLI output captured in `02.1-MEASUREMENT-LOG.md` with run dates, commit SHA, and Preview URL — provides audit trail |
| T-02.1-21 | Tampering | Phase status | mitigate | ROADMAP.md update goes through git; status flip is reviewable in PR diff |
| T-02.1-22 | Spoofing | CLI vs browser scoring | mitigate | Human checkpoint runs Lighthouse in a real browser to confirm CLI lhci wasn't gaming the metric (e.g., due to lhci's headless Chrome flags differing from the user's actual browser) |
| T-02.1-23 | Information Disclosure | Measurement docs | accept | The docs are public in the repo; no secrets, no PII; disclosure is intentional (forward readers, audit trail) |
</threat_model>

<verification>
1. `02.1-MEASUREMENT-LOG.md` exists with median Performance ≥ 0.90, A11y ≥ 0.95, BP ≥ 0.93, vendor-react ≥ 30 kB gz, and either FCP ≤ 1.8s + LCP ≤ 2.5s OR modulepreload present
2. `02.1-BUNDLE-MEASUREMENT.md` exists with all six required sections
3. `02-02-BUNDLE-MEASUREMENT.md` has Addendum: Phase 2.1 block at the bottom
4. `ROADMAP.md` Phase 2.1 entry: list checkbox `[x]`, detail block plan list filled in, progress table `N/N` + `Complete` + date
5. Human checkpoint approved with in-browser scores recorded
6. `git diff` shows only the four files in `files_modified` were changed (plus `02.1-MEASUREMENT-LOG.md` from Task 1)
</verification>

<success_criteria>
- ✅ All five SCs from ROADMAP §Phase 2.1 detail block measurably PASS (with verbatim evidence in the measurement log)
- ✅ `02.1-BUNDLE-MEASUREMENT.md` documents root cause + fix path + post-fix chunk shape + Lighthouse scores + CI changes + carry-forward notes (SC#5)
- ✅ `02-02-BUNDLE-MEASUREMENT.md` has a forward-pointer addendum so the next reader of the Phase 2 measurement doc lands on 02.1
- ✅ ROADMAP.md reflects Phase 2.1 = Complete with the actual plan count
- ✅ Human checkpoint approved; in-browser Lighthouse confirms CLI lhci was not gaming the metric
- ✅ No regressions to A11y or BP; no other phase entries in ROADMAP.md changed
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-spa-lighthouse-perf/02.1-05-SUMMARY.md` with:
- Summary of which waves applied vs skipped (and the gate evidence for each skip)
- Final Lighthouse scores (CLI median + in-browser human-verified)
- Final chunk-shape table (Phase 2 vs Phase 2.1, gzipped)
- Confirmation `02.1-BUNDLE-MEASUREMENT.md` written + addendum to 02-02 added
- Confirmation ROADMAP.md updated
- Human checkpoint sign-off message
- Pointer for downstream phases: any carry-forward TODOs (Vite 8 migration, CSP nonce, dynamic Sentry import) that future phases should pick up
</output>

---

## Revision log

**2026-05-11 — Revision 1 (W1 forward-link from 02.1-PLAN-CHECK.md):**
- Added Task 1 action step 5a: record actual vendor-react and index chunk sizes plus proposed tightened floor (`actual - 10kB`) for the W1 follow-up.
- Added a tighten-the-floor bullet to Section 6 (carry-forward notes) of the `02.1-BUNDLE-MEASUREMENT.md` skeleton in Task 2 — explicitly asks Wave 5 to record actuals and propose a follow-up commit raising the floor for tighter regression guarding. References the 30 kB floor / 80 kB ceiling / 50 kB index ceiling from the W1-revised assertion script.
- No change to wave count, `depends_on`, autonomy, or task count. Plan 05 stays `autonomous: false` because of its existing checkpoint:human-verify task.
