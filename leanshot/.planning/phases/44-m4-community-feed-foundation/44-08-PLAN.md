---
phase: 44
plan: 08
type: execute
wave: 1
depends_on: [44-03]
files_modified:
  - leanshot/src/components/community/media/CommunityImageUploader.tsx
  - leanshot/src/components/community/media/CommunityMediaUploader.tsx
  - leanshot/src/components/community/media/CommunityVideoPlayer.tsx
  - leanshot/src/components/community/CommunityPostMediaStrip.tsx
  - leanshot/package.json
autonomous: true
requirements:
  - COMMUNITY-04

must_haves:
  truths:
    - "CommunityImageUploader enforces D-04 10-image cap CLIENT-SIDE before upload (assertImageCap from Wave 0)"
    - "CommunityImageUploader rejects MIME types other than image/jpeg, image/png, image/webp (NO image/svg+xml)"
    - "CommunityMediaUploader (Mux) is hidden when isVideoAllowed(currentTier) is false (D-06)"
    - "CommunityMediaUploader passes maxFileSize=500*1024 (KB units per Mux SDK = 500 MB) and dynamicChunkSize (D-05)"
    - "CommunityVideoPlayer imports @mux/mux-player-react/lazy entry point (NOT the default export) to enable viewport-deferred load (RESEARCH Pitfall 1)"
    - "All three components are under src/components/community/media/ so 44-09's vite.config.ts sub-chunk rule keeps @mux/* and ~170 kB out of community-feed"
    - "@mux/mux-uploader-react and @mux/mux-player-react are added to leanshot/package.json dependencies"
    - "CommunityPostMediaStrip.tsx is REPLACED (not created) — 44-06 ships the file as a null-rendering STUB so both plans tsc-clean in parallel; this plan replaces the stub body with the real implementation at merge time. The orchestrator resolves the file collision via `git checkout --ours` on the 44-08 worktree per memory feedback_executor_tdd_scaffolds_sibling_files."
  artifacts:
    - path: "leanshot/src/components/community/media/CommunityImageUploader.tsx"
      provides: "Multi-file image upload + 10-image cap UI"
    - path: "leanshot/src/components/community/media/CommunityMediaUploader.tsx"
      provides: "Mux direct-upload wrapper (Pro+Lifetime+Trial gated)"
    - path: "leanshot/src/components/community/media/CommunityVideoPlayer.tsx"
      provides: "Mux Player adaptive HLS playback (lazy entry)"
    - path: "leanshot/src/components/community/CommunityPostMediaStrip.tsx"
      provides: "REPLACES the 44-06 STUB — renders post's image carousel + Mux thumbnail-with-click-to-play (NOT in media/ — strip is in community-feed chunk; player is lazy). Prop signature MUST match the stub from 44-06 exactly."
    - path: "leanshot/package.json"
      provides: "Adds @mux/mux-uploader-react ^1.5.0, @mux/mux-player-react ^3.13.0"
  key_links:
    - from: "CommunityMediaUploader endpoint prop"
      to: "mux-create-upload Edge Fn"
      via: "async endpoint() resolves Mux upload URL via fetch with user session JWT"
      pattern: "mux-create-upload"
    - from: "CommunityVideoPlayer import"
      to: "@mux/mux-player-react/lazy"
      via: "import MuxPlayer from '@mux/mux-player-react/lazy'"
      pattern: "@mux/mux-player-react/lazy"
    - from: "media/ directory path"
      to: "community-media vite chunk"
      via: "44-09 vite.config.ts rule keys on /src/components/community/media/"
      pattern: "community-media"
    - from: "CommunityPostMediaStrip (this plan's REPLACEMENT)"
      to: "44-06 stub"
      via: "Same prop signature {post, mediaSignedUrls}; 44-08 worktree wins the merge via --ours"
      pattern: "CommunityPostMediaStripProps"
---

<objective>
Ship the media subsystem: an image uploader enforcing the 10-image cap + MIME whitelist (T-44-05 SVG-XSS defense), a Mux direct-upload wrapper gated to Pro+Lifetime+Trial (D-06), a Mux Player using the `/lazy` entry point (RESEARCH Pitfall 1), and a REPLACEMENT for the 44-06-shipped media-strip stub. Add Mux npm deps to package.json.

Directory layout is CRITICAL: all three uploader/player components live under `src/components/community/media/`. Plan 44-09's vite.config.ts rule keys on this path to route Mux's ~186 kB gz into a separate `community-media` chunk. The media strip (CommunityPostMediaStrip) stays in the community-feed chunk because it's small and only the Mux player itself is lazy-imported.

**Stub-replacement contract:** Plan 44-06 ships `CommunityPostMediaStrip.tsx` as a thin stub (renders null, exports the prop interface). This plan REPLACES that stub at the same path with the real implementation. Both plans run in parallel under Wave 1; at merge time the orchestrator uses `git checkout --ours` on the 44-08 worktree to keep the real implementation. The prop signature (`CommunityPostMediaStripProps { post, mediaSignedUrls }`) is the cross-plan contract — both worktrees agree on this shape so the merge result is consistent regardless of which side wins the file content.

Purpose: Implements COMMUNITY-04 image + video media path with bundle-budget compliance.

Output: 3 React components in media/ + 1 strip REPLACEMENT in community/ + npm package.json updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-m4-community-feed-foundation/44-CONTEXT.md
@.planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md
@.planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md

@leanshot/src/lib/community/community-storage.ts
@leanshot/src/lib/community/tier-gate.ts

<!-- Stub precondition: 44-06 ships this file as a null-rendering stub. 44-08 replaces it. -->
@.planning/phases/44-m4-community-feed-foundation/44-06-PLAN.md

<interfaces>
CommunityImageUploaderProps {
  postId: string;
  currentUserId: string;
  existingMediaCount: number;       // caller maintains a tally; uploader rejects when existingMediaCount + selected files > 10
  onUploaded: (uploaded: { path: string; display_order: number }[]) => void;
}

CommunityMediaUploaderProps {
  postId: string;
  currentTier: TierLabel;
  onSuccess: (uploadId: string) => void;
  onError: (msg: string) => void;
}
// Component returns null when isVideoAllowed(currentTier) === false.

CommunityVideoPlayerProps {
  playbackId: string;
  posterTime?: number;     // default 1
}

CommunityPostMediaStripProps {
  post: CommunityPost;
  mediaSignedUrls: Record<string, string>;   // resolved by parent (CommunityFeed in 44-06)
}
// Prop signature MUST match the 44-06 stub exactly. This plan replaces the stub body; the interface is unchanged.
// If post.community_post_media.length > 0 → image carousel.
// If post.video_status === 'ready' && mux_playback_id → thumbnail + click-to-load lazy MuxPlayer.
// Otherwise → null.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Mux dependencies to package.json + ship CommunityImageUploader + REPLACE CommunityPostMediaStrip stub</name>
  <files>leanshot/package.json, leanshot/src/components/community/media/CommunityImageUploader.tsx, leanshot/src/components/community/CommunityPostMediaStrip.tsx</files>
  <read_first>
    - leanshot/package.json (current dependencies block; do NOT modify any other entries)
    - leanshot/src/lib/community/community-storage.ts (just shipped 44-03)
    - .planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md §Standard Stack (lines 116–143)
    - **PRECONDITION**: leanshot/src/components/community/CommunityPostMediaStrip.tsx — if 44-06 has already merged, this file exists as a STUB with comment `PHASE-44-06 STUB`. This task REPLACES the file body. If 44-06 has not yet merged (parallel execution), this task CREATES the file with the same prop interface. Either order is safe because both plans pin the prop signature in their `<interfaces>` block.
  </read_first>
  <action>
    Modify `leanshot/package.json`:
    - Add to `dependencies`: `"@mux/mux-uploader-react": "^1.5.0"` and `"@mux/mux-player-react": "^3.13.0"` (versions verified by RESEARCH §Standard Stack — npm registry confirmed). Maintain alphabetical sort within dependencies block per repo convention.
    - Do NOT add @mux/mux-node to leanshot/ — it's a Deno-side dep (already imported via `npm:@mux/mux-node@14` in Edge Fns; not needed in the frontend bundle).
    - Run `cd /Users/karstenhaldan/minisite/leanshot && npm install` to update package-lock.json. (npm install is allowed in this task; it's necessary for the type imports below.)

    Author `src/components/community/media/CommunityImageUploader.tsx`:
    - Path under `media/` is critical for bundle hygiene (44-09 vite rule).
    - State: `selected: File[]`, `uploading: boolean`, `errors: string[]`.
    - On file input change:
      1. Filter selected files through `COMMUNITY_MEDIA_MIMES` (re-import from community-storage.ts).
      2. Reject anything > 10 MB.
      3. Call `assertImageCap(existingMediaCount + valid.length)` from community-storage.ts; reject if false with `'Maximum 10 images per post'`.
      4. setSelected(valid).
    - On submit:
      1. setUploading(true).
      2. For each file: call `uploadCommunityMedia(file, currentUserId, postId)` from community-storage.ts. Collect successes; accumulate display_order starting from `existingMediaCount + 1`.
      3. For each success: INSERT into community_post_media via `supabase.from('community_post_media').insert({ post_id, path, display_order })`. Trigger from 44-01 enforces 10-cap at DB level (T-44-04 defense in depth).
      4. Call onUploaded(uploaded).
    - Render: a hidden `<input type="file" accept="image/jpeg,image/png,image/webp" multiple>` + a styled `<button>Add images</button>` that triggers it. Selected previews via URL.createObjectURL. Error toast row.
    - Accessibility: file input has aria-label; previews have alt text "Image preview {i+1}".

    **REPLACE the 44-06 stub at `src/components/community/CommunityPostMediaStrip.tsx`** with the real implementation:
    - Use `Write` (full overwrite) on this file. The 44-06 stub had ZERO runtime deps and rendered null; this replacement adds the image carousel + lazy Mux player.
    - Stays in `community/` (community-feed chunk) — this component is small, only the lazy MuxPlayer import goes to community-media.
    - Imports `lazy` + `Suspense`. `const CommunityVideoPlayer = lazy(() => import('./media/CommunityVideoPlayer'));`
    - Add a top-of-file comment: `// Owner: Plan 44-08. Replaces 44-06 STUB. CommunityPost.tsx (Plan 44-06) renders this as the media surface.`
    - Re-export the SAME prop signature as the stub (the contract is fixed):
      ```ts
      export interface CommunityPostMediaStripProps {
        post: CommunityPost;
        mediaSignedUrls: Record<string, string>;
      }
      ```
    - If `post.community_post_media.length > 0`: render a horizontal `<ul role="list">` of `<img>` tags with explicit width/height (e.g., 256×256), `loading="lazy"`, `alt=""` plus `aria-label` on the parent ul. Source from `mediaSignedUrls[media.path]`.
    - If `post.video_status === 'ready'` && `post.mux_playback_id`:
      - Initial render: thumbnail `<img src={`https://image.mux.com/${encodeURIComponent(post.mux_playback_id)}/thumbnail.jpg?time=1&width=640&height=360`} width="640" height="360" loading="lazy" alt="Video thumbnail">` with a `<button aria-label="Play video">▶</button>` overlay.
      - On click: setShowPlayer(true) → `<Suspense fallback={<div role="status">Loading player…</div>}><CommunityVideoPlayer playbackId={post.mux_playback_id} /></Suspense>` renders.
    - If `post.video_status === 'uploading' || 'processing'`: render a skeleton block + "Video is processing".
    - If `post.video_status === 'rejected'`: render error pill "Video could not be processed".
    - Else: return null.
    - Export both `CommunityPostMediaStrip` named export AND a default export (matching the stub's export shape) so 44-06's `import { CommunityPostMediaStrip } from './CommunityPostMediaStrip'` continues to resolve.

    **Cross-plan merge note** (echoed from 44-06 + memory feedback_executor_tdd_scaffolds_sibling_files):
    - Both 44-06 and 44-08 worktrees touch `src/components/community/CommunityPostMediaStrip.tsx`. This is EXPECTED. The orchestrator merges 44-06 first (smaller, stub-only), then 44-08 with `git checkout --ours leanshot/src/components/community/CommunityPostMediaStrip.tsx` to overwrite the stub with the real implementation. The 44-08 SUMMARY MUST call this out explicitly.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && grep -E '@mux/mux-uploader-react|@mux/mux-player-react' package.json | wc -l | grep -E '^[2-9]|^[1-9][0-9]+' && ls src/components/community/media/CommunityImageUploader.tsx src/components/community/CommunityPostMediaStrip.tsx && grep -c "COMMUNITY_MEDIA_MIMES\\|uploadCommunityMedia" src/components/community/media/CommunityImageUploader.tsx | grep -v '^0$' && grep -c "assertImageCap" src/components/community/media/CommunityImageUploader.tsx | grep -v '^0$' && grep -c "community_post_media" src/components/community/media/CommunityImageUploader.tsx | grep -v '^0$' && grep -c "image.mux.com" src/components/community/CommunityPostMediaStrip.tsx | grep -v '^0$' && grep -c "lazy(.*media/CommunityVideoPlayer" src/components/community/CommunityPostMediaStrip.tsx | grep -v '^0$' && grep -q "Replaces 44-06 STUB\\|Owner: Plan 44-08" src/components/community/CommunityPostMediaStrip.tsx && (! grep -q "PHASE-44-06 STUB\\|return null;.*$" src/components/community/CommunityPostMediaStrip.tsx || grep -q "image.mux.com" src/components/community/CommunityPostMediaStrip.tsx) && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/components/community/media/CommunityImageUploader|src/components/community/CommunityPostMediaStrip" | (! grep error)</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `@mux/mux-uploader-react` ^1.5.0 and `@mux/mux-player-react` ^3.13.0 in dependencies (grep returns ≥2).
    - package-lock.json updated (file modified after `npm install`).
    - CommunityImageUploader at exact path `src/components/community/media/CommunityImageUploader.tsx`.
    - CommunityImageUploader imports `assertImageCap`, `uploadCommunityMedia`, `COMMUNITY_MEDIA_MIMES` from community-storage.
    - CommunityImageUploader contains literal `accept="image/jpeg,image/png,image/webp"` (NO `image/svg`).
    - CommunityImageUploader inserts into community_post_media (literal grep).
    - CommunityPostMediaStrip at exact path `src/components/community/CommunityPostMediaStrip.tsx`.
    - CommunityPostMediaStrip contains literal `Replaces 44-06 STUB` OR `Owner: Plan 44-08` in the top-of-file comment (proves stub-replacement contract honored).
    - CommunityPostMediaStrip contains literal `lazy(` import for `./media/CommunityVideoPlayer`.
    - CommunityPostMediaStrip contains `image.mux.com` literal (Mux thumbnail endpoint).
    - CommunityPostMediaStrip does NOT contain the literal `PHASE-44-06 STUB` (the stub has been replaced) — the comment in the new file references the replacement, not the stub.
    - CommunityPostMediaStrip exports both `CommunityPostMediaStrip` named export AND a default export (same shape as stub).
    - tsc --noEmit clean for both new component files (the package-lock change does not affect tsc).
  </acceptance_criteria>
  <done>Image uploader + media strip REPLACEMENT shipped; Mux deps in package.json; image cap + MIME guard proven via Wave 0 test integration. 44-06 stub no longer exists post-merge — replaced by the real implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Ship CommunityMediaUploader (Mux) + CommunityVideoPlayer (lazy entry)</name>
  <files>leanshot/src/components/community/media/CommunityMediaUploader.tsx, leanshot/src/components/community/media/CommunityVideoPlayer.tsx</files>
  <read_first>
    - leanshot/src/lib/community/tier-gate.ts (just shipped 44-03)
    - .planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md §Pattern 3 (lines 346–365) and §Pitfall 1 (lines 511–516)
    - .planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md (line 887 — Mux uploader/player chunk routing)
  </read_first>
  <action>
    Author `src/components/community/media/CommunityMediaUploader.tsx`:
    - Imports: `import MuxUploader from '@mux/mux-uploader-react';` (16 kB gz — kept in community-media chunk by the path-based vite rule in 44-09). `import { isVideoAllowed } from '@/lib/community/tier-gate';`.
    - Early return: `if (!isVideoAllowed(currentTier)) return null;` per D-06.
    - Render `<MuxUploader … />` per 44-PATTERNS.md / RESEARCH Pattern 3:
      ```
      <MuxUploader
        endpoint={async () => {
          const { data: { session } } = await supabase.auth.getSession();
          const res = await fetch(`${import.meta.env.VITE_SUPABASE_URL}/functions/v1/mux-create-upload`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${session?.access_token ?? ''}` },
            body: JSON.stringify({ post_id: postId }),
          });
          if (!res.ok) {
            const err = await res.json().catch(() => ({}));
            onError(err.error === 'VIDEO_TIER_REQUIRED' ? 'Video upload requires a Pro or Lifetime membership.' : 'Upload setup failed.');
            throw new Error('endpoint_fail');
          }
          const { url, upload_id } = await res.json();
          // Stash upload_id for onSuccess via closure or component state.
          uploadIdRef.current = upload_id;
          return url;
        }}
        maxFileSize={500 * 1024}    // D-05: 500 MB (Mux SDK takes KB)
        dynamicChunkSize
        onSuccess={() => uploadIdRef.current && onSuccess(uploadIdRef.current)}
        onUploadError={(e) => onError('Upload failed: ' + (e?.detail?.message ?? 'unknown'))}
      />
      ```
    - Wrap MuxUploader render in a `<div>` with an explanatory label "Upload a video (max 5 min, 500 MB)" referencing D-05.

    Author `src/components/community/media/CommunityVideoPlayer.tsx`:
    - CRITICAL import per RESEARCH §State of the Art (lines 680–683): `import MuxPlayer from '@mux/mux-player-react/lazy';`. The `/lazy` entry point defers the ~170 kB player bytes until the component scrolls into the viewport via Mux's blurhash placeholder.
    - Render `<MuxPlayer playbackId={playbackId} poster={posterTime ? \`https://image.mux.com/${encodeURIComponent(playbackId)}/thumbnail.jpg?time=${posterTime}\` : undefined} streamType="on-demand" />`.
    - Accessibility: MuxPlayer handles its own a11y; ensure the surrounding container has aria-label="Community video player".
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && ls src/components/community/media/CommunityMediaUploader.tsx src/components/community/media/CommunityVideoPlayer.tsx && grep -c "import MuxUploader from '@mux/mux-uploader-react'" src/components/community/media/CommunityMediaUploader.tsx | grep -v '^0$' && grep -c "isVideoAllowed" src/components/community/media/CommunityMediaUploader.tsx | grep -v '^0$' && grep -c "mux-create-upload" src/components/community/media/CommunityMediaUploader.tsx | grep -v '^0$' && grep -c "maxFileSize={500" src/components/community/media/CommunityMediaUploader.tsx | grep -v '^0$' && grep -c "@mux/mux-player-react/lazy" src/components/community/media/CommunityVideoPlayer.tsx | grep -v '^0$' && (! grep -r "from '@mux/mux-player-react'\"\\s*\$" src/components/community/) && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/components/community/media/Community(Media|Video)" | (! grep error)</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at exact paths under `src/components/community/media/`.
    - CommunityMediaUploader imports MuxUploader from `@mux/mux-uploader-react` (literal).
    - CommunityMediaUploader contains `isVideoAllowed` early-return check (D-06 tier gate).
    - CommunityMediaUploader contains literal `mux-create-upload` (Edge Fn URL).
    - CommunityMediaUploader contains literal `maxFileSize={500` (D-05 500 MB cap).
    - CommunityVideoPlayer imports MuxPlayer from `@mux/mux-player-react/lazy` LITERAL (NOT `@mux/mux-player-react` without the `/lazy` suffix — this is the bundle-budget contract).
    - NO file under `src/components/community/` (recursive) imports from bare `@mux/mux-player-react` (lazy entry must be the only import path).
    - tsc --noEmit clean for both files.
  </acceptance_criteria>
  <done>Mux uploader (tier-gated) + Mux player (lazy entry) shipped under media/ path; 44-09 vite rule will route ~186 kB into community-media chunk separate from 20 kB community-feed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → mux-create-upload | User JWT auth; server-side tier check; Mux secrets never reach browser |
| Browser → Storage upload | community-storage.ts client-side validation as UX gate; storage RLS + MIME whitelist + 10-image trigger are authoritative |
| Mux Cloud → MuxPlayer playback | Public playback policy on Mux side; playback_id flows through community_posts (no signed playback in v1) |
| 44-06 stub → 44-08 replacement | Same file path, same prop signature, different body. Orchestrator merges with `--ours` on 44-08 worktree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02 | Elevation of Privilege | CommunityMediaUploader UI gate | mitigate | isVideoAllowed(currentTier) hides the uploader for Free users; mux-create-upload Edge Fn (Plan 44-04) is the authoritative 403 gate. Defense in depth. |
| T-44-04 | Tampering (storage path traversal) | CommunityImageUploader | mitigate | uploadCommunityMedia (Plan 44-03) constructs path with leading userId segment; storage RLS (Plan 44-01) enforces `(storage.foldername(name))[1] = auth.uid()::text`. |
| T-44-05 | Tampering (SVG XSS) | CommunityImageUploader MIME filter | mitigate | accept="image/jpeg,image/png,image/webp" attribute + COMMUNITY_MEDIA_MIMES set + storage bucket allowed_mime_types whitelist (all three layers exclude image/svg+xml). |
</threat_model>

<verification>
- `cd /Users/karstenhaldan/minisite/leanshot && npx tsc --noEmit -p tsconfig.json` returns 0 errors
- `grep -r "from '@mux/mux-player-react'" src/components/community/ | grep -v '/lazy'` returns 0 (no bare imports)
- After 44-09 builds: `bash scripts/assert-bundle-budget.sh dist/assets` reports community-feed ≤ 20 kB gz with Mux bytes routed into community-media chunk
- Post-merge: `grep -q "PHASE-44-06 STUB" src/components/community/CommunityPostMediaStrip.tsx` returns 1 (stub marker absent) AND `grep -q "Owner: Plan 44-08\\|Replaces 44-06 STUB" src/components/community/CommunityPostMediaStrip.tsx` returns 0 (replacement marker present)
</verification>

<success_criteria>
- 4 component files + package.json updated; Mux deps installed.
- CommunityImageUploader enforces 10-image cap + MIME whitelist (T-44-05).
- CommunityMediaUploader hidden for Free tier; mints upload URL via mux-create-upload Edge Fn (T-44-02).
- CommunityVideoPlayer uses `/lazy` entry (RESEARCH Pitfall 1 mitigation).
- All media-heavy code lives under media/ path so 44-09's vite rule isolates the bundle.
- CommunityPostMediaStrip stub from 44-06 REPLACED with the real implementation; merge-time conflict resolved via `--ours` on the 44-08 worktree.
</success_criteria>

<output>
After completion, create `.planning/phases/44-m4-community-feed-foundation/44-08-SUMMARY.md` summarizing the media subsystem + bundle-isolation strategy. **Explicitly note the CommunityPostMediaStrip stub-to-real replacement and the `git checkout --ours` resolution at merge time** (per memory feedback_executor_tdd_scaffolds_sibling_files). CommunityPostMediaStrip is the integration seam between 44-06's CommunityPost and 44-08's lazy player.
</output>
</content>
</invoke>