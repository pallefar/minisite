---
phase: 44
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270720000004_p44_notification_community.sql
  - supabase/functions/_shared/email-router.ts
  - supabase/functions/_shared/templates/community-mention.ts
  - supabase/functions/_shared/templates/community-reply.ts
  - supabase/functions/notification-send/index.ts
autonomous: true
requirements:
  - COMMUNITY-03

must_haves:
  truths:
    - "All 4 notification CHECK constraints accept 'community-mentions' and 'community-replies' atomically (one migration, one transaction)"
    - "notification_category_config has rows for both new categories with daily_cap=20 (mention spam defense T-44-06)"
    - "email-router EmailTemplate union includes 'community_mention' and 'community_reply'; subjectFor + renderTemplate handle both"
    - "notification-send VALID_CATEGORIES set includes both new community categories"
    - "Community email templates render with mentioned_by + post_url variable substitution and unsubscribe link via existing email-router footer"
  artifacts:
    - path: "supabase/migrations/20270720000004_p44_notification_community.sql"
      provides: "Widens 4 CHECK constraints atomically + seeds 2 category_config rows"
      contains: "drop constraint if exists notification_settings_category_chk"
    - path: "supabase/functions/_shared/email-router.ts"
      provides: "EmailTemplate union widened; subjectFor + renderTemplate switch arms added"
      contains: "'community_mention'"
    - path: "supabase/functions/_shared/templates/community-mention.ts"
      provides: "HTML render for @mention notification email"
    - path: "supabase/functions/_shared/templates/community-reply.ts"
      provides: "HTML render for new-reply notification email"
    - path: "supabase/functions/notification-send/index.ts"
      provides: "VALID_CATEGORIES set updated to include 2 new categories"
      contains: "'community-mentions'"
  key_links:
    - from: "notification-send VALID_CATEGORIES"
      to: "notification_settings.category CHECK constraint"
      via: "category value passes both gates"
      pattern: "'community-mentions'"
    - from: "email-router EmailTemplate"
      to: "templates/community-mention.ts"
      via: "renderTemplate switch arm calls communityMentionTemplate.render(vars)"
      pattern: "case 'community_mention'"
---

<objective>
Eliminate the Phase 44 production HARD BLOCKER documented in RESEARCH §3.1: 4 CHECK constraints in `notification_settings`, `notification_category_config`, `user_notifications`, `notification_dismissal_state` enumerate only the 5 existing categories. The community notify-community Edge Fn (plan 44-05) would 500 in production without atomic widening. This plan ships widening + email-router widening + notification-send VALID_CATEGORIES update + 2 new HTML templates IN THE SAME COMMIT per feedback_planner_missed_status_enum_widening.

Purpose: Prevents Phase 44 from shipping with a runtime CHECK violation. Locked-step contract: migration + email-router + VALID_CATEGORIES + templates land together. Plan 44-05 (notify-community) and plan 44-10 (deploy) depend on this.

Output: 1 migration + 4 modified/new TypeScript files, ready for `supabase db push --linked` in plan 44-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-m4-community-feed-foundation/44-CONTEXT.md
@.planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md
@.planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md

@supabase/migrations/20270704000001_notification_settings.sql
@supabase/functions/_shared/email-router.ts
@supabase/functions/notification-send/index.ts

<interfaces>
<!-- Contracts this plan establishes that plan 44-05 (notify-community) and 44-10 (deploy) consume -->

EmailTemplate union extension:
  | 'community_mention'   // non-PHI → Resend
  | 'community_reply'     // non-PHI → Resend

community_mention template variables:
  { mentioned_by: string, space_name: string, post_excerpt: string, post_url: string }

community_reply template variables:
  { commenter_name: string, post_excerpt: string, comment_excerpt: string, post_url: string }

notification_category_config seed row contract:
  ('community-mentions', daily_cap=20, weekly_cap=null, urgent_escalation=false, push=false, email=true,  in_app=true)
  ('community-replies',  daily_cap=20, weekly_cap=null, urgent_escalation=false, push=false, email=false, in_app=true)
  -- Mentions email-on by default (Skool parity); replies email-off by default (conservative D-14 fan-out)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen 4 notification CHECK constraints + seed category_config + extend email-router + update VALID_CATEGORIES</name>
  <files>supabase/migrations/20270720000004_p44_notification_community.sql, supabase/functions/_shared/email-router.ts, supabase/functions/_shared/templates/community-mention.ts, supabase/functions/_shared/templates/community-reply.ts, supabase/functions/notification-send/index.ts</files>
  <read_first>
    - supabase/migrations/20270704000001_notification_settings.sql (lines 1–80 — current CHECK constraint shape)
    - supabase/functions/_shared/email-router.ts (full file; lines 56–86 for EmailTemplate union; lines 150–186 subjectFor; lines 188+ renderTemplate)
    - supabase/functions/notification-send/index.ts (lines 169–175 VALID_CATEGORIES Set)
    - .planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md (lines 219–277 widening pattern; lines 451–480 email-router widening)
    - .planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md Pattern 6 (lines 416–467)
  </read_first>
  <action>
    Migration file `20270720000004_p44_notification_community.sql` (note: timestamp `...000004` is between 44-01's `...000003` and `...000005` to preserve apply order with the schema). Wrap in `begin; ... commit;`. Per 44-PATTERNS.md lines 226–276:
    - ALTER all 4 tables (notification_settings, notification_category_config, user_notifications, notification_dismissal_state) DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT with the widened CHECK enumerating 7 categories: the original 5 (dose-reminders, ai-insights, clinic-alerts, billing, marketing) + community-mentions + community-replies.
    - INSERT INTO notification_category_config the 2 new rows per the interfaces block contract. Use `on conflict (category) do update set …` (UPSERT per reference_state_counter_table_needs_upsert_on_event); update daily_cap, push_enabled_default, email_enabled_default, in_app_enabled_default, updated_at fields.

    Modify `supabase/functions/_shared/email-router.ts`:
    - After line 76 (`| 'pause_resumed_t0'`), add `| 'community_mention'` and `| 'community_reply'` to the EmailTemplate union.
    - In `subjectFor` switch (line 150+), add `case 'community_mention': return \`${(vars.mentioned_by as string | undefined) ?? 'Someone'} mentioned you in ${(vars.space_name as string | undefined) ?? 'a space'}\`;` and `case 'community_reply': return 'New reply on your post';`.
    - In `renderTemplate` switch (line 188+), add `case 'community_mention': return communityMentionTemplate(vars);` and `case 'community_reply': return communityReplyTemplate(vars);`.
    - Add imports at top: `import { communityMentionTemplate } from './templates/community-mention.ts';` and `import { communityReplyTemplate } from './templates/community-reply.ts';` (use existing template directory if it exists; otherwise create `_shared/templates/`).

    Create `supabase/functions/_shared/templates/community-mention.ts`: export function `communityMentionTemplate(vars: Record<string, unknown>): string` returning HTML string. Use the same prose-table layout the helpdesk_agent_reply template uses (read renderTemplate for that case to mirror styling). Variables interpolated: mentioned_by, space_name, post_excerpt (truncate to 200 chars), post_url. Include a `<a href="${post_url}">View the post</a>` CTA. Footer unsubscribe link is added by the email-router footer wrapper (do NOT hand-roll one — the router appends it).

    Create `supabase/functions/_shared/templates/community-reply.ts`: same structure; variables commenter_name, post_excerpt, comment_excerpt, post_url. Subject is handled by subjectFor.

    Modify `supabase/functions/notification-send/index.ts` lines 169–175 VALID_CATEGORIES Set: add `'community-mentions'` and `'community-replies'` as new Set entries. This is the IN-MEMORY gate that pairs with the DB CHECK gate.

    Critical: ALL 5 file changes must land in this task / commit. Splitting any of them across plans = production 500 per RESEARCH Pitfall 2. Do not push the migration here (44-10 owns the push).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -c "drop constraint if exists notification_settings_category_chk\\|drop constraint if exists notification_category_config_category_chk\\|drop constraint if exists user_notifications_category_chk\\|drop constraint if exists notification_dismissal_state_category_chk" supabase/migrations/20270720000004_p44_notification_community.sql | grep -E '^[4-9]' && grep -c "'community-mentions'" supabase/migrations/20270720000004_p44_notification_community.sql | grep -E '^[2-9]|^[1-9][0-9]+' && grep -c "'community_mention'" supabase/functions/_shared/email-router.ts | grep -E '^[3-9]|^[1-9][0-9]+' && ls supabase/functions/_shared/templates/community-mention.ts supabase/functions/_shared/templates/community-reply.ts && grep -c "'community-mentions'" supabase/functions/notification-send/index.ts | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at exact 14-digit timestamp 20270720000004 and contains `drop constraint if exists` for all 4 named CHECK constraints (notification_settings_category_chk, notification_category_config_category_chk, user_notifications_category_chk, notification_dismissal_state_category_chk).
    - Migration file contains exactly 7 categories in each ADD CONSTRAINT CHECK list: the 5 originals + community-mentions + community-replies (grep `community-mentions` appears ≥4 times for the 4 ADD CHECKs + ≥1 in the seed INSERT).
    - Migration file contains `on conflict (category) do update set` literal (UPSERT seed pattern).
    - Migration file's category_config seed for `community-mentions` has email_enabled_default=true and for `community-replies` has email_enabled_default=false (D-14 fan-out asymmetry).
    - email-router.ts has `'community_mention'` literal appearing ≥3 times (union + subjectFor + renderTemplate).
    - email-router.ts has `'community_reply'` literal appearing ≥3 times.
    - email-router.ts has `import { communityMentionTemplate }` and `import { communityReplyTemplate }` lines.
    - templates/community-mention.ts exports `communityMentionTemplate` function; references `mentioned_by`, `space_name`, `post_url`, `post_excerpt` variable names.
    - templates/community-reply.ts exports `communityReplyTemplate` function; references `commenter_name`, `post_url`, `comment_excerpt`.
    - notification-send/index.ts has `'community-mentions'` literal AND `'community-replies'` literal added to the VALID_CATEGORIES Set definition (grep for both).
    - No `signInWithPassword` literal in templates (PII-clean).
  </acceptance_criteria>
  <done>Migration + email-router + 2 templates + notification-send VALID_CATEGORIES all updated in one commit; ready for push in 44-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge Fn → Postgres (CHECK) | notification-send writes user_notifications rows with category; CHECK constraint is the last-line defense |
| Edge Fn → Resend SMTP | community_mention / community_reply templates rendered server-side; HTML must be safe (no user-controlled scripts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Denial of Service | notification fan-out via community_post_mentions | mitigate | notification_category_config.daily_cap=20 for community-mentions (enforced by notification-send rate-limit at call site). Server-side mention dedupe (one row per user_id in community_post_mentions PRIMARY KEY) prevents N-mention spam from a single post. |
| T-44-05 | Tampering (XSS in template) | community-mention.ts / community-reply.ts | mitigate | Template variables (post_excerpt, comment_excerpt) MUST be HTML-escaped before interpolation. Use the existing `escapeHtml` helper from email-router if present, OR inline a small `replace(/[&<>"']/g, …)` helper. NEVER allow unescaped user input in template bodies. |
</threat_model>

<verification>
- `grep -c "category in.*community-mentions.*community-replies" supabase/migrations/20270720000004_*.sql` → at least 4 (one per table CHECK)
- `npx tsc --noEmit -p supabase/functions/_shared/tsconfig.json` (if it exists) returns 0 errors
- Functional verification (after 44-10 push): Edge Fn deploy + a test call `curl … -d '{"category":"community-mentions"}'` succeeds (no CHECK violation)
</verification>

<success_criteria>
- The Phase 44 HARD BLOCKER from RESEARCH §3.1 is closed: 4 CHECK constraints widened atomically in one migration; email-router union + subjectFor + renderTemplate updated in same commit; VALID_CATEGORIES set updated in same commit; 2 HTML templates rendered.
- notify-community Edge Fn (plan 44-05) can safely call notification-send with category='community-mentions' without CHECK violation.
- Mention spam DoS (T-44-06) defended via daily_cap=20.
- Template XSS (T-44-05) defended via HTML-escaped variable interpolation.
</success_criteria>

<output>
After completion, create `.planning/phases/44-m4-community-feed-foundation/44-02-SUMMARY.md` summarizing the 5 file changes and confirming the atomic widening contract is closed.
</output>
