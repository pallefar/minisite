---
phase: 44
plan: 09
type: execute
wave: 2
depends_on: [44-06, 44-07, 44-08]
files_modified:
  - leanshot/src/components/community/use-space-realtime.ts
  - leanshot/src/components/community/CommunitySpaceList.tsx
  - leanshot/src/components/community/CommunitySpaceView.tsx
  - leanshot/src/components/community/CommunityTabShell.tsx
  - leanshot/src/admin/modules/community/SpaceEditor.tsx
  - leanshot/src/admin/modules/community/CommunityAdminLayout.tsx
  - leanshot/vite.config.ts
  - supabase/migrations/20270720000006_p44_community_spaces_admin_policies.sql
  - leanshot/scripts/assert-bundle-budget.sh
  - leanshot/src/App.tsx
  - leanshot/src/types/index.ts
  - leanshot/src/lib/store.ts
  - leanshot/src/components/layout/Sidebar.tsx
  - leanshot/src/components/layout/MobileNav.tsx
autonomous: true
requirements:
  - COMMUNITY-05
  - COMMUNITY-06

must_haves:
  truths:
    - "use-space-realtime subscribes to postgres_changes on community_posts (filter space_id=eq.$id), community_comments (filter space_id=eq.$id via the denormalized column), and community_reactions (*); unsubscribes on unmount"
    - "CommunitySpaceList renders a tier-locked card (D-08) when canAccessSpace(space.min_tier, currentTier) is false; locked cards show name + member count + post count + lock icon + /pricing CTA; post bodies NOT rendered"
    - "Org-private spaces (org_id NOT NULL) where caller is not org_member are NOT returned by the SELECT query at all (RLS-hidden per D-09)"
    - "SpaceEditor (admin) supports create / edit / archive spaces with name + description + min_tier (free/pro/lifetime) + optional org_id picker"
    - "vite.config.ts has manualChunks rules `/src/components/community/media/` → 'community-media' AND `/src/components/community/mentions/` → 'community-mentions' AND `/node_modules/@mux/(mux-player-react|mux-uploader-react)(/|$)` → 'community-media', ALL three rules placed BEFORE the existing `/src/components/community/` → 'community-feed' catch-all at line 194"
    - "src/types/index.ts TabId union contains literal 'community'"
    - "src/App.tsx contains a branch `currentTab === 'community'` that mounts <CommunityTabShell /> via lazy + Suspense; consumer surface uses Zustand setTab + store.activeCommunitySpaceId for list-vs-detail, NOT react-router-dom"
    - "Admin SpaceEditor lives under src/admin/modules/community/ and registers under the existing admin react-router subtree (admin surface, not consumer)"
    - "Admin write policy migration uses `public.is_staff()` (the existing SECDEF helper at supabase/migrations/20261101000006_is_staff_helper.sql) and does NOT reference a `staff_users` table"
  artifacts:
    - path: "leanshot/src/components/community/use-space-realtime.ts"
      provides: "Per-space Realtime channel hook"
      exports: ["useSpaceRealtime"]
    - path: "leanshot/src/components/community/CommunitySpaceList.tsx"
      provides: "Tier-gated space directory + locked-card rendering"
    - path: "leanshot/src/components/community/CommunitySpaceView.tsx"
      provides: "Space page: feed + composer + comment thread + realtime wiring"
    - path: "leanshot/src/components/community/CommunityTabShell.tsx"
      provides: "Consumer-surface tab shell: switches list-vs-detail based on store.activeCommunitySpaceId (NO react-router)"
    - path: "leanshot/src/admin/modules/community/SpaceEditor.tsx"
      provides: "Admin CRUD for community_spaces (admin react-router surface — react-router IS allowed for admin per CLAUDE.md)"
    - path: "leanshot/src/admin/modules/community/CommunityAdminLayout.tsx"
      provides: "Admin module entry — registers <Route path='spaces' element={<SpaceEditor/>} /> under admin shell"
    - path: "leanshot/vite.config.ts"
      provides: "manualChunks sub-rules for community-media + community-mentions (order: more-specific first)"
    - path: "leanshot/src/App.tsx"
      provides: "Lazy mount of CommunityTabShell when currentTab === 'community'"
    - path: "leanshot/src/types/index.ts"
      provides: "TabId union widened with 'community'"
    - path: "leanshot/src/lib/store.ts"
      provides: "activeCommunitySpaceId state + setActiveCommunitySpace action"
    - path: "supabase/migrations/20270720000006_p44_community_spaces_admin_policies.sql"
      provides: "RLS INSERT/UPDATE policies on community_spaces gated by public.is_staff()"
  key_links:
    - from: "CommunitySpaceView"
      to: "useSpaceRealtime + CommunityFeed.refreshNonce"
      via: "useSpaceRealtime increments a state counter on each update; passed to CommunityFeed as refreshNonce prop"
      pattern: "useSpaceRealtime"
    - from: "vite.config.ts new rules"
      to: "community-media chunk + community-mentions chunk"
      via: "manualChunks function path-prefix check BEFORE community/ catch-all"
      pattern: "community-media|community-mentions"
    - from: "CommunitySpaceList.tsx"
      to: "canAccessSpace from tier-gate.ts"
      via: "import for locked-card rendering decision"
      pattern: "canAccessSpace"
    - from: "src/App.tsx"
      to: "CommunityTabShell"
      via: "currentTab === 'community' branch (Zustand-driven, NOT react-router)"
      pattern: "currentTab === 'community'"
    - from: "admin-policy migration"
      to: "public.is_staff() helper"
      via: "using (public.is_staff()) with check (public.is_staff()) RLS predicates"
      pattern: "public\\.is_staff\\(\\)"
---

<objective>
Wire the community subsystem into the application via TWO ROUTING SURFACES:

1. **Consumer surface** (Zustand-driven per CLAUDE.md "No router" rule): widen `TabId` with `'community'`, add an `activeCommunitySpaceId` store field, mount a new `<CommunityTabShell>` from `src/App.tsx` when `currentTab === 'community'`, expose the tab in Sidebar + MobileNav.
2. **Admin surface** (react-router-dom, ALLOWED per project convention for admin paths only): register `SpaceEditor` under `src/admin/modules/community/CommunityAdminLayout.tsx` using the existing `<Route>` pattern from `src/components/admin/AdminShell.tsx` and `src/admin/modules/reviews/ReviewsLayout.tsx`.

Also: ship the Realtime hook (D-13), tier-gated SpaceList with locked-card discovery (D-08, D-09), the space view that composes Feed + Composer + CommentThread, the vite.config.ts manualChunks sub-rules that keep Mux + Fuse.js OUT of the 20 kB community-feed chunk, and the staff-write RLS migration pinned to `public.is_staff()`.

Purpose: Implements COMMUNITY-05 (Realtime) + COMMUNITY-06 (Spaces) and CLOSES the bundle-budget contract before the deploy in 44-10. Resolves iter-1 BLOCKER 3 (is_staff predicate pinning) and BLOCKER 4 (consumer surface MUST NOT use react-router per CLAUDE.md).

Output: 13 files across consumer + admin surfaces (no router collision: consumer uses Zustand tabs, admin uses existing react-router subtree).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-m4-community-feed-foundation/44-CONTEXT.md
@.planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md
@.planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md

@leanshot/src/components/clinic/roster/use-roster-realtime.ts
@leanshot/src/components/clinic/alerts/use-clinician-alerts-realtime.ts
@leanshot/src/components/admin/cancellation/RuleEditor.tsx
@leanshot/vite.config.ts
@leanshot/src/App.tsx
@leanshot/src/lib/community/tier-gate.ts
@leanshot/src/lib/community/community-types.ts
@leanshot/src/types/index.ts
@leanshot/src/lib/store.ts
@leanshot/src/components/layout/Sidebar.tsx
@leanshot/src/components/layout/MobileNav.tsx
@leanshot/src/components/admin/AdminShell.tsx
@leanshot/src/admin/modules/reviews/ReviewsLayout.tsx

<!-- The existing is_staff helper — Fix-B PIN ANCHOR for the admin RLS migration -->
@supabase/migrations/20261101000006_is_staff_helper.sql

<interfaces>
useSpaceRealtime(spaceId: string, onUpdate: () => void): void;
  // Subscribes to postgres_changes for community_posts (INSERT, filter space_id=eq.{spaceId}),
  // community_comments (INSERT, filter space_id=eq.{spaceId} via denormalized column from 44-01),
  // community_reactions (event: '*').
  // Single channel per mounted space view per D-13.
  // onUpdate is intentionally excluded from useEffect deps (per use-roster-realtime.ts).

CommunitySpaceListProps {
  currentUserId: string;
  currentTier: TierLabel;
  onSelectSpace: (spaceId: string) => void;
}

CommunitySpaceViewProps {
  spaceId: string;
  currentUserId: string;
  currentTier: TierLabel;
}

CommunityTabShellProps {} // reads currentUserId + tier + activeCommunitySpaceId from store

SpaceEditorProps {
  spaceId?: string;     // when set → edit; else → create
  onSaved: (spaceId: string) => void;
}

// Store widening (additive, in src/lib/store.ts):
//   activeCommunitySpaceId: string | null;
//   setActiveCommunitySpace: (id: string | null) => void;
// TabId widening (additive, in src/types/index.ts):
//   ... | 'community'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship use-space-realtime + CommunitySpaceList + CommunitySpaceView</name>
  <files>leanshot/src/components/community/use-space-realtime.ts, leanshot/src/components/community/CommunitySpaceList.tsx, leanshot/src/components/community/CommunitySpaceView.tsx</files>
  <read_first>
    - leanshot/src/components/clinic/roster/use-roster-realtime.ts (entire 51 lines — direct analog)
    - leanshot/src/components/clinic/alerts/use-clinician-alerts-realtime.ts (97 lines — richer CHANNEL_ERROR variant)
    - .planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md (lines 505–562 — use-space-realtime pattern verbatim)
    - .planning/phases/44-m4-community-feed-foundation/44-CONTEXT.md D-08, D-09, D-13
  </read_first>
  <action>
    Author `src/components/community/use-space-realtime.ts` by VERBATIM forking `use-roster-realtime.ts` and swapping table names + filter. Per 44-PATTERNS.md lines 519–549 (the entire hook body is given). Critical:
    - `supabase.channel(\`community:${spaceId}\`)` — channel name uniqueness across spaces.
    - Three `.on('postgres_changes', { event, schema, table, filter }, () => onUpdate())` calls:
      - community_posts INSERT, filter `space_id=eq.${spaceId}`
      - community_comments INSERT, filter `space_id=eq.${spaceId}` (denormalized space_id from 44-01)
      - community_reactions event: '*' (no filter — broadcast is small enough)
    - `.subscribe((status) => { if (status === 'CHANNEL_ERROR') console.warn('[community-realtime] channel error — live updates paused'); })`.
    - Cleanup: `return () => { void supabase.removeChannel(channel); };`
    - `onUpdate` intentionally excluded from deps; ESLint exception comment.

    Author `src/components/community/CommunitySpaceList.tsx`:
    - Functional component with `CommunitySpaceListProps`.
    - On mount: `await supabase.from('community_spaces').select('id, name, description, org_id, min_tier').order('created_at', { ascending: false })`. RLS (44-01) filters org-private spaces the caller is not a member of — no client-side org check needed.
    - For each space: compute `canAccess = canAccessSpace(space.min_tier, currentTier)` from tier-gate.ts.
    - If canAccess: render an interactive card with name, description, member-count placeholder (Phase 45 adds), recent-post-count placeholder, onClick → `onSelectSpace(space.id)`.
    - If !canAccess (D-08 locked card): render a lock-icon card with `name`, "Upgrade to {space.min_tier === 'pro' ? 'Pro' : 'Lifetime'} to join", and a `<a href="/pricing" class="...">Upgrade</a>` CTA. Do NOT render description body. Do NOT call onSelectSpace.
    - Accessibility: lock card has `aria-label="Locked space: {name}, upgrade required"`.

    Author `src/components/community/CommunitySpaceView.tsx`:
    - Functional component with `CommunitySpaceViewProps`.
    - State: `refreshNonce: number` (starts 0).
    - Use `useSpaceRealtime(spaceId, () => setRefreshNonce(n => n + 1))` — D-13 channel lifecycle.
    - On mount: fetch space metadata `await supabase.from('community_spaces').select('*').eq('id', spaceId).single()`. If RLS rejects (returns 0 rows) → render an "Access denied / Not found" empty state. This is the runtime defense for cross-tenant or tier probing.
    - Layout: `<header>` with space name + description, `<CommunityPostComposer spaceId={spaceId} currentUserId={currentUserId} onSubmitted={() => setRefreshNonce(n+1)} />`, `<CommunityFeed spaceId={spaceId} currentUserId={currentUserId} refreshNonce={refreshNonce} />`. Comment thread is opened per-post via a modal/sheet UI; if scope-tight: defer thread expansion to a `CommentThreadDrawer` modal triggered by clicking a post (use the existing `Sheet` UI primitive at src/components/ui/Sheet.tsx).
    - Include a back-button: `<button onClick={() => useStore.getState().setActiveCommunitySpace(null)} aria-label="Back to space list">← Back</button>` — this is how the consumer surface exits the detail view (Zustand-driven, no router navigation).
    - Accessibility: `role="main"`, `aria-labelledby` referencing the space-name heading.
    - File path is at `src/components/community/CommunitySpaceView.tsx` (community-feed chunk by default).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && ls src/components/community/use-space-realtime.ts src/components/community/CommunitySpaceList.tsx src/components/community/CommunitySpaceView.tsx && grep -c "supabase.channel" src/components/community/use-space-realtime.ts | grep -v '^0$' && grep -c "community_posts\\|community_comments\\|community_reactions" src/components/community/use-space-realtime.ts | grep -E '^[3-9]|^[1-9][0-9]+' && grep -c "space_id=eq" src/components/community/use-space-realtime.ts | grep -E '^[2-9]|^[1-9][0-9]+' && grep -c "canAccessSpace" src/components/community/CommunitySpaceList.tsx | grep -v '^0$' && grep -c "/pricing" src/components/community/CommunitySpaceList.tsx | grep -v '^0$' && grep -c "useSpaceRealtime" src/components/community/CommunitySpaceView.tsx | grep -v '^0$' && grep -c "refreshNonce" src/components/community/CommunitySpaceView.tsx | grep -v '^0$' && grep -c "setActiveCommunitySpace" src/components/community/CommunitySpaceView.tsx | grep -v '^0$' && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/components/community/(use-space-realtime|CommunitySpaceList|CommunitySpaceView)" | (! grep error)</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist.
    - use-space-realtime.ts contains literal `supabase.channel(` and ≥3 occurrences of community_posts/community_comments/community_reactions across the `.on()` registrations.
    - use-space-realtime.ts contains ≥2 occurrences of `space_id=eq.` (post + comment filters).
    - use-space-realtime.ts cleanup uses `removeChannel`.
    - CommunitySpaceList.tsx imports `canAccessSpace` and contains literal `/pricing` (D-08 locked CTA).
    - CommunitySpaceList.tsx does NOT call `onSelectSpace` inside the locked-card branch (grep visual review).
    - CommunitySpaceView.tsx calls `useSpaceRealtime` and uses `refreshNonce` state.
    - CommunitySpaceView.tsx contains literal `setActiveCommunitySpace` (back-button wires Zustand exit, not router).
    - tsc --noEmit clean for these 3 files.
  </acceptance_criteria>
  <done>Realtime hook + space list + space view shipped; D-13 single-channel-per-space + D-08 locked-card discovery enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Update vite.config.ts manualChunks + assert-bundle-budget.sh ceilings</name>
  <files>leanshot/vite.config.ts, leanshot/scripts/assert-bundle-budget.sh</files>
  <read_first>
    - leanshot/vite.config.ts (lines 140–200 — manualChunks current rules; line 194 is the community-feed catch-all)
    - leanshot/scripts/assert-bundle-budget.sh (current ceiling table)
    - .planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md (lines 868–895 — vite.config.ts sub-chunk rules)
  </read_first>
  <action>
    Modify `leanshot/vite.config.ts`:
    - In the manualChunks function (around line 140), find line 194 `if (id.includes('/src/components/community/')) return 'community-feed';`.
    - INSERT the following rules IMMEDIATELY BEFORE line 194 (order matters — more-specific rules first per the comment at line 170):
      ```
      // community-media sub-chunk — keeps @mux/mux-player-react (~170 kB gz) + @mux/mux-uploader-react (~16 kB gz) out of community-feed
      if (id.includes('/src/components/community/media/')) return 'community-media';

      // community-mentions sub-chunk — keeps fuse.js (8 kB gz) out of community-feed (mirrors helpdesk-macros)
      if (id.includes('/src/components/community/mentions/')) return 'community-mentions';

      // Mux player + uploader npm packages → community-media (sub-chunk catches transitive imports)
      if (/node_modules\/@mux\/(mux-player-react|mux-uploader-react)(\/|$)/.test(id)) {
        return 'community-media';
      }
      ```

    Modify `leanshot/scripts/assert-bundle-budget.sh`:
    - Add entries to the ceiling table for community-media (assume ~190 kB ceiling = Mux player ~170 kB + uploader ~16 kB + glue ~4 kB) and community-mentions (assume ~12 kB ceiling = fuse.js ~8 kB + glue).
    - Per reference_bundle_budget_hash_hyphen: confirm the existing per-chunk regex tolerates hash-with-hyphen content hashes; if not, fix in the same task.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && grep -c "community-media" vite.config.ts | grep -E '^[2-9]|^[1-9][0-9]+' && grep -c "community-mentions" vite.config.ts | grep -E '^[2-9]|^[1-9][0-9]+' && grep -c "@mux/(mux-player-react|mux-uploader-react)" vite.config.ts | grep -v '^0$' && awk '/src\/components\/community\/media\//{print NR; exit}' vite.config.ts > /tmp/p44_media_line && awk '/src\/components\/community\/.{0,20}return .community-feed/{print NR; exit}' vite.config.ts > /tmp/p44_feed_line && test "$(cat /tmp/p44_media_line)" -lt "$(cat /tmp/p44_feed_line)" && grep -c "community-media\\|community-mentions" scripts/assert-bundle-budget.sh | grep -E '^[2-9]|^[1-9][0-9]+'</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts contains both `community-media` and `community-mentions` literals (≥2 occurrences each across rules + comments).
    - vite.config.ts has the `/src/components/community/media/` literal rule appearing BEFORE the `community-feed` catch-all (line-number check verifies ordering — verify command above asserts this).
    - vite.config.ts has the `@mux/(mux-player-react|mux-uploader-react)` regex rule for node_modules catch-all.
    - scripts/assert-bundle-budget.sh contains entries for community-media and community-mentions (≥2 grep matches).
  </acceptance_criteria>
  <done>Bundle isolation rules in place — community-feed catch-all now sees only the strip + small components; ~186 kB Mux + 8 kB fuse routed to sibling chunks.</done>
</task>

<task type="auto">
  <name>Task 3: Wire consumer surface (Zustand tabs + Sidebar + MobileNav) AND admin surface (react-router) + admin RLS migration</name>
  <files>leanshot/src/types/index.ts, leanshot/src/lib/store.ts, leanshot/src/App.tsx, leanshot/src/components/community/CommunityTabShell.tsx, leanshot/src/components/layout/Sidebar.tsx, leanshot/src/components/layout/MobileNav.tsx, leanshot/src/admin/modules/community/SpaceEditor.tsx, leanshot/src/admin/modules/community/CommunityAdminLayout.tsx, supabase/migrations/20270720000006_p44_community_spaces_admin_policies.sql</files>
  <read_first>
    - leanshot/src/types/index.ts (TabId definition at line 234)
    - leanshot/src/lib/store.ts (currentTab + setTab patterns; find the store creator and the existing actions)
    - leanshot/src/App.tsx (the tab-branch chain around lines 1888–1893; the lazy import pattern; locate the first `{currentTab === 'home' && <HomeTab` line and verify the exact line numbers before editing)
    - leanshot/src/components/layout/Sidebar.tsx (existing tab list — find the navItems / tabs array and replicate the entry shape)
    - leanshot/src/components/layout/MobileNav.tsx (same — find the tab array structure)
    - leanshot/src/components/admin/AdminShell.tsx (existing admin react-router subtree)
    - leanshot/src/admin/modules/reviews/ReviewsLayout.tsx (admin module Route entry pattern — direct analog for CommunityAdminLayout)
    - leanshot/src/components/admin/cancellation/RuleEditor.tsx (admin CRUD form analog for SpaceEditor)
    - supabase/migrations/20261101000006_is_staff_helper.sql (the is_staff() SECDEF function definition — read the function body to confirm signature)
  </read_first>
  <action>

    **STEP 1: Widen TabId in src/types/index.ts.**
    - Find the `export type TabId =` block at line 234.
    - Add `| 'community'` as a new union member (preserve existing members). Final shape:
      ```
      export type TabId =
        | 'home'
        | 'medication'
        | 'symptoms'
        | 'body'
        | 'nutrition'
        | 'activity'
        | 'supplements'
        | 'mood'
        | 'insights'
        | 'community';
      ```

    **STEP 2: Add activeCommunitySpaceId to the store in src/lib/store.ts.**
    - Locate the AppState interface OR the store creator (search for `currentTab:` and `setTab:`); add:
      - State field: `activeCommunitySpaceId: string | null;` (initial value `null`).
      - Action: `setActiveCommunitySpace: (id: string | null) => void;` implemented as `set({ activeCommunitySpaceId: id })`.
    - Exclude `activeCommunitySpaceId` from the `partialize` block per the project convention that transient UI state is NOT persisted (search for the existing `partialize` function and add the new field to the excluded set — it should follow the same pattern as `currentTab` and `toast`).

    **STEP 3: Author CommunityTabShell at src/components/community/CommunityTabShell.tsx.**
    - Functional component (NO props). Reads from store:
      ```
      const currentUserId = useStore((s) => s.user?.id ?? '');
      const currentTier = useStore((s) => s.user?.tier ?? 'free') as TierLabel;
      const activeSpaceId = useStore((s) => s.activeCommunitySpaceId);
      const setActiveSpace = useStore((s) => s.setActiveCommunitySpace);
      ```
      (Adapt `s.user?.tier` to whatever the actual tier-source path is — read tier-gate.ts's readTierLabel helper from Wave 0 to find the canonical accessor.)
    - Render:
      ```
      if (activeSpaceId) return <CommunitySpaceView spaceId={activeSpaceId} currentUserId={currentUserId} currentTier={currentTier} />;
      return <CommunitySpaceList currentUserId={currentUserId} currentTier={currentTier} onSelectSpace={(id) => setActiveSpace(id)} />;
      ```
    - Both child components are lazy-imported inside CommunityTabShell.tsx (Suspense wraps each branch). This concentrates the community-feed chunk entry behind a single Suspense boundary inside the shell.

    **STEP 4: Mount CommunityTabShell in src/App.tsx.**
    - Add a lazy import alongside the existing tab lazy imports:
      ```
      const CommunityTabShell = lazy(() => import('./components/community/CommunityTabShell'));
      ```
    - In the tab-branch chain (around line 1893; verify the exact line by reading the file first), add a new branch immediately after the last existing branch:
      ```
      {currentTab === 'community' && <Suspense fallback={<Loading />}><CommunityTabShell /></Suspense>}
      ```
      Match the surrounding Suspense fallback shape used by sibling tabs (read the `home` branch a few lines above and replicate exactly).
    - DO NOT add `<Route path="/community/..." ...>` or import `useNavigate` from `react-router-dom` anywhere in App.tsx for the consumer surface. CLAUDE.md is explicit: consumer App.tsx uses Zustand-driven tab switching.

    **STEP 5: Expose the Community tab in Sidebar.tsx + MobileNav.tsx.**
    - Sidebar.tsx: locate the existing nav-items array (search for the `home` entry) and append/insert a `community` entry with: label `'Community'`, icon (use a lucide-react icon like `Users` or `MessageSquare`), tabId `'community'`. Wire onClick → `setTab('community')`.
    - MobileNav.tsx: same insertion in its tab list. If MobileNav has space constraints (mobile bottom nav typically shows 4–5 tabs), evaluate replacing a less-frequently-used tab with community OR adding an overflow indicator — DEFAULT: insert at position 3 (after home/dashboard, before insights) and let the existing horizontal-scroll-overflow handle layout. Verify by reading the file first.

    **STEP 6: Author admin SpaceEditor + CommunityAdminLayout under src/admin/modules/community/.**
    - This is the ADMIN surface — react-router IS allowed here per CLAUDE.md (admin paths use react-router-dom; consumer paths use Zustand tabs).
    - `src/admin/modules/community/SpaceEditor.tsx`:
      - Functional component with `SpaceEditorProps { spaceId?: string; onSaved: (id: string) => void; }`.
      - Form fields: name (Input, required), description (textarea, optional), min_tier (select with options 'free','pro','lifetime'), org_id (optional free-text UUID input with helper label "Leave blank for global space; paste org UUID to make this clinic-private").
      - On submit (create): `await supabase.from('community_spaces').insert({ name, description, min_tier, org_id: org_id || null });`.
      - On submit (edit): UPDATE … WHERE id = spaceId.
      - On success: call onSaved(spaceId) and show a toast.
    - `src/admin/modules/community/CommunityAdminLayout.tsx`:
      - Mirrors ReviewsLayout.tsx structure (read that file first).
      - Registers `<Route path="spaces" element={<SpaceEditor />}>` (and optionally a list view) under the admin shell's subroute tree. Adapt the exact route shape from ReviewsLayout.

    **STEP 7: Ship the admin RLS migration `supabase/migrations/20270720000006_p44_community_spaces_admin_policies.sql`.**
    - **CRITICAL Fix-B pin:** The predicate MUST be exactly `public.is_staff()` (no arguments, no fallback). DO NOT reference a `staff_users` table; the helper at `supabase/migrations/20261101000006_is_staff_helper.sql` is the canonical staff check (reads profiles.is_staff column, returns boolean, SECURITY DEFINER).
    - Migration body (literal SQL):
      ```sql
      -- Phase 44 follow-up: admin write policies on community_spaces.
      -- Pinned to public.is_staff() per iter-2 BLOCKER-3 fix.

      alter table public.community_spaces enable row level security;

      drop policy if exists cspace_insert_staff on public.community_spaces;
      create policy cspace_insert_staff on public.community_spaces
        for insert
        to authenticated
        with check (public.is_staff());

      drop policy if exists cspace_update_staff on public.community_spaces;
      create policy cspace_update_staff on public.community_spaces
        for update
        to authenticated
        using (public.is_staff())
        with check (public.is_staff());

      -- DELETE intentionally omitted: spaces use soft-archive (Phase 45). Hard delete is admin-console-only and not exposed via PostgREST.
      ```
    - Filename respects reference_supabase_migration_filename_regex: `20270720000006_p44_community_spaces_admin_policies.sql` (14-digit prefix).
    - DO NOT include any reference to `staff_users` in this file — Fix-B explicitly requires the negation grep to pass.

    **STEP 8: Add an admin-policy RLS test to 44-01's test suite.**
    - This is a cross-plan addition: the test file is `leanshot/tests/rls/community-spaces-rls.test.ts` (already in 44-01's files_modified). 44-01 owns the file scaffold; 44-09 contributes the additional it() block(s) AT EXECUTE-TIME via the depends_on graph (44-09 runs after 44-01 has shipped the file in Wave 0).
    - Add two new `it()` blocks to community-spaces-rls.test.ts:
      ```
      it('non-staff authed user gets permission denied when INSERTing into community_spaces', async () => {
        // Seed: user with profiles.is_staff = false. Use the project's RLS fixture helper (admin.generateLink + /auth/v1/verify per reference_rls_fixture_gotrueclient_flake).
        const { client } = await createOrgScopedUser('pro');
        const res = await client.from('community_spaces').insert({ name: TEST_SLUG_PREFIX + '-blocked', min_tier: 'free' });
        // Expect RLS rejection — Postgres reports "new row violates row-level security policy".
        expect(res.error).not.toBeNull();
        expect(res.error?.code).toBe('42501'); // insufficient_privilege OR check the actual code returned by RLS rejection
      });

      it('staff user (profiles.is_staff=true) CAN INSERT community_spaces', async () => {
        const { client, userId } = await createOrgScopedUser('pro');
        await admin.from('profiles').update({ is_staff: true }).eq('id', userId);
        const res = await client.from('community_spaces').insert({ name: TEST_SLUG_PREFIX + '-staff-ok', min_tier: 'free' }).select('id').single();
        expect(res.error).toBeNull();
        expect(res.data?.id).toBeTruthy();
      });
      ```
    - The executor MUST add these blocks to the existing file authored by 44-01 (do not create a duplicate file). Update 44-01-PLAN.md's verify command is NOT needed; the rows are exercised by 44-10's RLS test sweep after push.

    **STEP 9: Cross-plan note for executor.**
    - This task touches files owned by 44-01 (community-spaces-rls.test.ts append) AND by 44-09 (everything else). When this plan runs in Wave 2, 44-01 has already merged its initial test file, so the append is safe. Use Edit (not Write) on the RLS test file.

    **STEP 10: Bundle ownership note.**
    - SpaceEditor + CommunityAdminLayout live under `src/admin/modules/community/`, NOT under `src/components/community/`. The vite.config.ts admin-shell chunk rule (existing) will catch them. The path-based community-media + community-mentions + community-feed rules do not apply. Verify by reading vite.config.ts and confirming the admin-shell catch-all comes after the community-feed rule (or that admin/* has its own chunk; either is fine — the goal is that SpaceEditor is not in community-feed).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && grep -q "'community'" src/types/index.ts && grep -q "activeCommunitySpaceId" src/lib/store.ts && grep -q "setActiveCommunitySpace" src/lib/store.ts && grep -q "currentTab === 'community'" src/App.tsx && grep -q "CommunityTabShell" src/App.tsx && (! grep -q "<Route path=\"/community" src/App.tsx) && (! grep -q "useNavigate(" src/App.tsx) && ls src/components/community/CommunityTabShell.tsx && ls src/admin/modules/community/SpaceEditor.tsx src/admin/modules/community/CommunityAdminLayout.tsx && grep -q "'community'" src/components/layout/Sidebar.tsx && grep -q "'community'" src/components/layout/MobileNav.tsx && cd /Users/karstenhaldan/minisite && ls supabase/migrations/20270720000006_p44_community_spaces_admin_policies.sql && grep -q "public.is_staff()" supabase/migrations/20270720000006_p44_community_spaces_admin_policies.sql && grep -q "using (public.is_staff())" supabase/migrations/20270720000006_p44_community_spaces_admin_policies.sql && (! grep -q "staff_users" supabase/migrations/20270720000006_p44_community_spaces_admin_policies.sql) && cd /Users/karstenhaldan/minisite/leanshot && grep -c "is_staff" tests/rls/community-spaces-rls.test.ts | grep -v '^0$' && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/(App\\.tsx|types/index\\.ts|lib/store\\.ts|components/community/CommunityTabShell|admin/modules/community/)" | (! grep error)</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/index.ts` TabId union contains literal `'community'` (grep `grep -q "'community'" src/types/index.ts` passes).
    - `src/lib/store.ts` contains literal `activeCommunitySpaceId` AND literal `setActiveCommunitySpace`.
    - `src/App.tsx` contains literal `currentTab === 'community'` AND literal `CommunityTabShell` (lazy import).
    - `src/App.tsx` does NOT contain `<Route path="/community` and does NOT contain `useNavigate(` (negation greps — these would indicate router contamination of the consumer surface).
    - `src/components/community/CommunityTabShell.tsx` exists.
    - `src/admin/modules/community/SpaceEditor.tsx` AND `src/admin/modules/community/CommunityAdminLayout.tsx` exist (admin surface — react-router IS used here).
    - `src/components/layout/Sidebar.tsx` AND `src/components/layout/MobileNav.tsx` each contain literal `'community'` (the new TabId in their nav arrays).
    - Migration `20270720000006_p44_community_spaces_admin_policies.sql` exists at the supabase/migrations/ path (git root).
    - Migration contains literal `public.is_staff()` AND literal `using (public.is_staff())` (grep both).
    - Migration does NOT contain the string `staff_users` (negation grep — Fix-B).
    - `tests/rls/community-spaces-rls.test.ts` contains literal `is_staff` (the new admin-policy test rows).
    - tsc --noEmit clean for all touched files in this task.
  </acceptance_criteria>
  <done>Consumer surface uses Zustand-tab pattern (CLAUDE.md compliant). Admin surface uses react-router (existing project convention for admin). Admin RLS pinned to public.is_staff(). New RLS test rows added to 44-01's community-spaces-rls test. Route registered. Bundle-budget contract closed for the 44-10 deploy gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → community_spaces SELECT | RLS (44-01) gates org-private + tier; client-side `canAccessSpace` is the UX gate only |
| Browser → community_spaces INSERT/UPDATE (admin) | New `public.is_staff()`-gated RLS policy (this plan's follow-up migration) — only profiles.is_staff=true users can write |
| Realtime channel → browser | Per-space channel with filter; cross-tenant noise minimized by space_id filter (Pitfall 5 denorm) |
| Consumer App.tsx | Zustand-only navigation (per CLAUDE.md); NO react-router-dom usage means no deep-linking, no browser back/forward; activeCommunitySpaceId in non-persisted store slice |
| Admin App | react-router-dom IS used (existing project pattern); SpaceEditor mounts under admin subtree, isolated from consumer surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Elevation of Privilege | CommunitySpaceList + SpaceView | mitigate | RLS-driven invisibility (D-09) — non-org users get zero rows for org-private spaces; tier-locked rows surface as upgrade-CTA cards (D-08) without leaking post bodies. SpaceView re-checks via `from('community_spaces').eq('id', spaceId).single()` → empty result = "Access denied / Not found" empty state. |
| T-44-01b | Elevation of Privilege | admin SpaceEditor | mitigate | New RLS policy `cspace_insert_staff` + `cspace_update_staff` require `public.is_staff()` (SECDEF helper reading profiles.is_staff). Shipped in `20270720000006_p44_community_spaces_admin_policies.sql`. RLS test in community-spaces-rls.test.ts proves non-staff INSERT rejection + staff INSERT success. **Fix-B pinned predicate prevents iter-1 ambiguity over staff_users-vs-is_staff().** |
| T-44-06 | DoS (Realtime broadcast spam) | use-space-realtime | mitigate | Per-space channel (D-13) limits broadcast scope; community_comments space_id filter (Pitfall 5 denorm) prevents cross-space noise; community_reactions broadcasts are small and rate-limited by user reaction rate. |
| T-44-01c (new) | Spoofing (router contamination) | App.tsx consumer surface | mitigate | Fix-C pin: consumer surface MUST NOT import `react-router-dom`. Verified via negation grep at acceptance time. Prevents future-maintainer drift away from CLAUDE.md "No router" rule. |
</threat_model>

<verification>
- After Wave 1 + this plan: `cd /Users/karstenhaldan/minisite/leanshot && npm run build && bash scripts/assert-bundle-budget.sh dist/assets` reports community-feed ≤ 20 kB gz, community-media ≤ 190 kB, community-mentions ≤ 12 kB. (Plan 44-10 owns the actual CI gate run.)
- Functional (post-44-10): A Free user opens the space list — sees Pro-only spaces as locked cards with /pricing CTA; opens a Pro-only space → sees "Access denied / Not found". An org_A user does NOT see org_B's private space in the list.
- Admin RLS proof: a non-staff user attempting `community_spaces.insert(...)` is rejected by RLS; a profiles.is_staff=true user succeeds. Verified by the new community-spaces-rls.test.ts blocks (added in Task 3 Step 8).
</verification>

<success_criteria>
- 13 files shipped (consumer surface: 5; admin surface: 2; cross-cutting: 3 types/store/App; layout: 2; migration: 1).
- D-08 locked card surface + D-09 org-private RLS-hidden surface both rendered.
- D-13 single-channel-per-space Realtime enforced.
- COMMUNITY-05 + COMMUNITY-06 satisfied at the UI layer.
- Bundle isolation: community-media chunk + community-mentions chunk + community-feed ≤ 20 kB gz contract ready for 44-10 verification.
- Fix-B: admin migration pinned to `public.is_staff()`; no `staff_users` reference; RLS test proves the policy.
- Fix-C: consumer surface uses Zustand TabId pattern; react-router-dom NOT imported in App.tsx for the community surface; admin surface keeps existing react-router usage.
- Fix-D: vite + bundle-budget split out of the App+admin task to control scope.
</success_criteria>

<output>
After completion, create `.planning/phases/44-m4-community-feed-foundation/44-09-SUMMARY.md` summarizing:
- The Zustand-tab vs react-router split (consumer vs admin) and why.
- The is_staff() predicate pin in the admin migration.
- The chunk-rule ordering decision.
- The 2 new RLS test rows added to community-spaces-rls.test.ts (cross-plan append; not a new file).
- The activeCommunitySpaceId store field as non-persisted UI state.
</output>
</content>
</invoke>