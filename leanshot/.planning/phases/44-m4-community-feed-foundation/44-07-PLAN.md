---
phase: 44
plan: 07
type: execute
wave: 1
depends_on: [44-03]
files_modified:
  - leanshot/src/components/community/CommunityPostComposer.tsx
  - leanshot/src/components/community/CommunityCommentComposer.tsx
  - leanshot/src/components/community/mentions/MentionTypeahead.tsx
autonomous: true
requirements:
  - COMMUNITY-01
  - COMMUNITY-03

must_haves:
  truths:
    - "CommunityPostComposer enforces a 5000-char client-side cap and disables submit when over limit (D-11)"
    - "CommunityPostComposer autosaves to localStorage key `community_draft_${spaceId}_${userId}` on 500ms debounce; restores on remount; clears on successful insert"
    - "On submit success, composer triggers parseMentions(body), upserts community_post_mentions rows for resolved handles, and POSTs to notify-community Fn with kind='mention'"
    - "MentionTypeahead lives under src/components/community/mentions/ so vite.config.ts (44-09) routes it to the community-mentions sub-chunk (fuse.js stays out of community-feed)"
    - "CommunityCommentComposer reuses the same submit + mention-resolve path; reply notification fires via notify-community kind='reply'"
  artifacts:
    - path: "leanshot/src/components/community/CommunityPostComposer.tsx"
      provides: "Post create + edit composer with markdown textarea + mention typeahead trigger"
    - path: "leanshot/src/components/community/CommunityCommentComposer.tsx"
      provides: "Comment create composer with mention typeahead"
    - path: "leanshot/src/components/community/mentions/MentionTypeahead.tsx"
      provides: "Fuse.js fuzzy @-handle picker (lazy-imported)"
  key_links:
    - from: "CommunityPostComposer submit"
      to: "community_post_mentions table"
      via: "supabase.from('community_post_mentions').upsert(rows, { onConflict: 'post_id,user_id' })"
      pattern: "community_post_mentions"
    - from: "CommunityPostComposer submit"
      to: "notify-community Edge Fn"
      via: "fetch /functions/v1/notify-community with user session JWT (Fn re-checks via service-role? — see action note)"
      pattern: "notify-community"
    - from: "MentionTypeahead"
      to: "Fuse.js"
      via: "lazy-imported under mentions/ → community-mentions sub-chunk"
      pattern: "import Fuse from 'fuse.js'"
---

<objective>
Ship the write-side community UI: post composer, comment composer, and the lazy-loaded mention typeahead. Drafts autosave to localStorage; submit resolves @mentions via Wave 0's parseMentions; mention rows upsert to community_post_mentions / community_comment_mentions; notify-community is fired for fan-out. MentionTypeahead lives under `src/components/community/mentions/` so 44-09's vite.config.ts sub-chunk rule keeps fuse.js out of the 20 kB community-feed chunk.

Purpose: Implements COMMUNITY-01 write path + COMMUNITY-03 mention authoring + notification trigger.

Output: 3 React components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-m4-community-feed-foundation/44-CONTEXT.md
@.planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md
@.planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md

@leanshot/src/helpdesk/MacroTypeahead.tsx
@leanshot/src/components/admin/cancellation/RuleEditor.tsx
@leanshot/src/lib/community/mention-parse.ts
@leanshot/src/lib/community/dompurify-config.ts

<interfaces>
<!-- Props -->

CommunityPostComposerProps {
  spaceId: string;
  currentUserId: string;
  editingPost?: CommunityPost;  // when set, composer is in edit mode (D-15 edit forever)
  onSubmitted?: (postId: string) => void;
}

CommunityCommentComposerProps {
  postId: string;
  spaceId: string;
  currentUserId: string;
  editingComment?: CommunityComment;
  onSubmitted?: (commentId: string) => void;
}

MentionTypeaheadProps {
  query: string;                          // debounced 300ms by parent or internally
  excludeUserIds?: string[];              // dedupe self
  onSelect: (handle: string, userId: string) => void;
}

// Submit path (shared between post + comment composer):
//   1. Validate body length <= 5000 (D-11).
//   2. INSERT community_posts (or community_comments) with body, author_id, space_id, post_id (for comment); receive new row id.
//   3. parseMentions(body) → handles[].
//   4. SELECT profiles WHERE handle IN handles → resolve user_ids.
//   5. UPSERT community_post_mentions (or community_comment_mentions) rows.
//   6. POST /functions/v1/notify-community with kind='mention' AND if comment: ALSO POST with kind='reply'.
//   7. Clear localStorage draft.
//   8. Call onSubmitted(newId).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship MentionTypeahead under mentions/ sub-chunk path</name>
  <files>leanshot/src/components/community/mentions/MentionTypeahead.tsx</files>
  <read_first>
    - leanshot/src/helpdesk/MacroTypeahead.tsx (entire file — analog for Fuse.js + listbox a11y)
    - .planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md lines 566–633 (MentionTypeahead pattern with verbatim chunk-isolation comment)
  </read_first>
  <action>
    Author `src/components/community/mentions/MentionTypeahead.tsx`:
    - File path is exact: `src/components/community/mentions/MentionTypeahead.tsx`. The `mentions/` directory matters — 44-09's vite.config.ts rule keys on it for the community-mentions sub-chunk.
    - Top-of-file comment (verbatim per 44-PATTERNS.md line 572): `// This file is lazy-imported → lands in 'community-mentions' chunk, NOT 'community-feed'`. This comment is the cue the reviewer/checker uses to confirm bundle hygiene.
    - Imports: `import Fuse from 'fuse.js'; import { useEffect, useMemo, useState, type JSX } from 'react'; import { supabase } from '@/lib/supabase';`. Fuse import is static here BUT this file is only imported via React.lazy() from CommunityPostComposer + CommunityCommentComposer — that places Fuse in this chunk.
    - 300ms debounce on `query` prop using a `useDebounce` hook (inline-define one if not present: `function useDebounce<T>(v: T, ms: number) { const [d, setD] = useState(v); useEffect(() => { const t = setTimeout(() => setD(v), ms); return () => clearTimeout(t); }, [v, ms]); return d; }`).
    - On debounced query change: `supabase.from('profiles').select('id, handle, display_name').ilike('handle', `${debouncedQuery}%`).limit(8)`. Use cancelled-fetch guard.
    - Filter out `excludeUserIds`.
    - Fuse.js instance: `useMemo(() => items?.length ? new Fuse(items, { keys: ['handle','display_name'], threshold: 0.4, includeScore: true }) : null, [items]);`.
    - Render listbox per 44-PATTERNS.md lines 591–612 (role="listbox", role="option", aria-selected, aria-label).
    - On select: call `onSelect(handle, userId)`.

    Important: Do NOT add a top-level `Fuse.search()` call before mount; the parent component decides when to show the typeahead (after `@` keystroke).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && ls src/components/community/mentions/MentionTypeahead.tsx && grep -c "import Fuse from 'fuse.js'" src/components/community/mentions/MentionTypeahead.tsx | grep -v '^0$' && grep -c "community-mentions" src/components/community/mentions/MentionTypeahead.tsx | grep -v '^0$' && grep -c 'role="listbox"' src/components/community/mentions/MentionTypeahead.tsx | grep -v '^0$' && grep -c "aria-selected" src/components/community/mentions/MentionTypeahead.tsx | grep -v '^0$' && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/components/community/mentions/" | (! grep error)</automated>
  </verify>
  <acceptance_criteria>
    - File at exact path `src/components/community/mentions/MentionTypeahead.tsx`.
    - Top-of-file comment contains literal `community-mentions` (chunk-routing cue).
    - Source contains `import Fuse from 'fuse.js'` literal (static import within this file — Fuse stays in the mentions chunk via parent's React.lazy).
    - Source contains `role="listbox"` AND `aria-selected` literals (a11y).
    - Source contains `threshold: 0.4` literal (matches Macro analog).
    - Source uses cancelled-fetch guard pattern (`let cancelled = false`).
    - tsc --noEmit clean for this file.
  </acceptance_criteria>
  <done>MentionTypeahead in the right path; Fuse.js confined to community-mentions chunk via path-based vite rule (44-09).</done>
</task>

<task type="auto">
  <name>Task 2: Ship CommunityPostComposer + CommunityCommentComposer with draft autosave + mention resolve + notify-community fire</name>
  <files>leanshot/src/components/community/CommunityPostComposer.tsx, leanshot/src/components/community/CommunityCommentComposer.tsx</files>
  <read_first>
    - leanshot/src/components/admin/cancellation/RuleEditor.tsx (form CRUD + toast pattern)
    - leanshot/src/lib/community/mention-parse.ts (just shipped 44-03)
    - .planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md lines 712–766 (Composer pattern)
    - .planning/phases/44-m4-community-feed-foundation/44-CONTEXT.md D-11, D-14, D-15
  </read_first>
  <action>
    Author `CommunityPostComposer.tsx`:
    - Imports include: `useEffect`, `useRef`, `useState`, `lazy`, `Suspense`, `Button`, `Input` or textarea, `useToast`, `supabase`, `parseMentions`, `sanitizeCommunityMarkdown` (for preview), `CommunityPost` type. `const MentionTypeahead = lazy(() => import('./mentions/MentionTypeahead'));` — this lazy import is what routes Fuse.js into community-mentions chunk.
    - State: `body` (string), `submitting` (boolean), `mentionQuery` (string | null — non-null when user is typing `@…` actively), `mentionAnchor` ({start: number} | null).
    - Draft autosave per 44-PATTERNS.md lines 727–745:
      ```
      const DRAFT_KEY = (spaceId, userId) => `community_draft_${spaceId}_${userId}`;
      ```
      On mount: `localStorage.getItem(DRAFT_KEY(spaceId, currentUserId))` → setBody. On change: `setTimeout` 500ms → `localStorage.setItem(...)`. On unmount with empty body: no clear. On successful insert: `localStorage.removeItem(...)`.
      If editingPost prop set, hydrate body from editingPost.body and bypass draft load.
    - 5000-char cap (D-11): `const isOverLimit = body.length > 5000;` — render counter `${body.length} / 5000` and disable submit button when over.
    - Mention trigger detection: on textarea keyDown of `@`, set `mentionQuery = ''` and `mentionAnchor = textarea.selectionStart`. On subsequent character keys with mentionAnchor set, append to mentionQuery; on space/newline, close (set mentionQuery = null). Render `<Suspense fallback={null}><MentionTypeahead query={mentionQuery} excludeUserIds={[currentUserId]} onSelect={(handle, userId) => insertMention(handle)} /></Suspense>` floating below the textarea (positioned absolute relative to a wrapping div).
    - Submit handler:
      1. If isOverLimit → showError('Post is too long (5000 chars max)'). Return.
      2. setSubmitting(true).
      3. If editingPost: `await supabase.from('community_posts').update({ body, edited_at: new Date().toISOString() }).eq('id', editingPost.id).eq('author_id', currentUserId);` (RLS will reject if not author).
         Else: `const { data, error } = await supabase.from('community_posts').insert({ space_id: spaceId, author_id: currentUserId, body: body.trim() }).select('id').single();` → newPostId = data.id.
      4. On error → showError; return.
      5. const handles = parseMentions(body); — server-side dedup via PK guarantees safety.
      6. If handles.length > 0: `const { data: mentioned } = await supabase.from('profiles').select('id, handle').in('handle', handles);`. For each row, build `{ post_id: newPostId, user_id: row.id }`. `await supabase.from('community_post_mentions').upsert(rows, { onConflict: 'post_id,user_id' });` per D-14.
         Then fire notify-community:
         ```
         await fetch(`${import.meta.env.VITE_SUPABASE_URL}/functions/v1/notify-community`, {
           method: 'POST',
           headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${session.access_token}` },
           body: JSON.stringify({ kind: 'mention', target_type: 'post', target_id: newPostId, space_id: spaceId, mentioned_by_user_id: currentUserId, mentioned_by_name: profile.display_name }),
         });
         ```
      7. Clear draft from localStorage.
      8. setSubmitting(false); call onSubmitted(newPostId).
    - Markdown preview tab: optional `<button>Preview</button>` toggle; when on, render `<div dangerouslySetInnerHTML={{ __html: sanitizeCommunityMarkdown(body) }} />`. (Safe because the sanitize chokepoint.)
    - Accessibility: textarea has aria-label="Post body"; counter has aria-live="polite"; submit has aria-busy={submitting}.

    Author `CommunityCommentComposer.tsx`:
    - Same structure as PostComposer but:
      - Draft key: `community_comment_draft_${postId}_${userId}` (separate from post draft).
      - INSERT to community_comments instead of community_posts (with post_id + space_id columns; space_id resolved via the postId argument — pre-fetch from props).
      - Mention fan-out same path (target_type='comment', target_id=newCommentId).
      - ADDITIONALLY fire reply notification: `await fetch('/functions/v1/notify-community', { ..., body: JSON.stringify({ kind: 'reply', post_id: postId, comment_id: newCommentId, space_id, commenter_user_id: currentUserId, commenter_name: profile.display_name }) });`. notify-community's logic (per 44-05) skips self-reply.
      - 5000-char cap also applies (CHECK constraint on community_comments per 44-01).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && ls src/components/community/CommunityPostComposer.tsx src/components/community/CommunityCommentComposer.tsx && grep -c "community_draft_" src/components/community/CommunityPostComposer.tsx | grep -v '^0$' && grep -c "parseMentions" src/components/community/CommunityPostComposer.tsx | grep -v '^0$' && grep -c "community_post_mentions" src/components/community/CommunityPostComposer.tsx | grep -v '^0$' && grep -c "notify-community" src/components/community/CommunityPostComposer.tsx | grep -v '^0$' && grep -c "5000" src/components/community/CommunityPostComposer.tsx | grep -v '^0$' && grep -c "lazy(.*mentions/MentionTypeahead" src/components/community/CommunityPostComposer.tsx | grep -v '^0$' && grep -c "kind: 'reply'" src/components/community/CommunityCommentComposer.tsx | grep -v '^0$' && grep -c "community_comment_mentions" src/components/community/CommunityCommentComposer.tsx | grep -v '^0$' && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/components/community/Community(Post|Comment)Composer" | (! grep error)</automated>
  </verify>
  <acceptance_criteria>
    - 2 composer files exist.
    - CommunityPostComposer contains literal `community_draft_` (draft key prefix per D-11).
    - CommunityPostComposer contains literal `parseMentions` (import + call).
    - CommunityPostComposer contains literal `community_post_mentions` (upsert target).
    - CommunityPostComposer contains literal `notify-community` (Edge Fn URL).
    - CommunityPostComposer contains literal `5000` (char cap).
    - CommunityPostComposer contains literal `lazy(` with `./mentions/MentionTypeahead` (lazy chunk routing).
    - CommunityCommentComposer contains literal `kind: 'reply'` (reply notification fan-out per D-14).
    - CommunityCommentComposer contains literal `community_comment_mentions` (upsert target).
    - Both composers contain `onConflict: 'post_id,user_id'` or `onConflict: 'comment_id,user_id'` literal (PK upsert per silent-scope-reduction memory).
    - tsc --noEmit clean for both files.
    - Note: notify-community Edge Fn (Plan 44-05) supports BOTH service-role bearer and user JWT (with sub === mentioned_by_user_id self-check), so this browser call with session JWT succeeds directly.
  </acceptance_criteria>
  <done>Both composers ship draft autosave + mention resolve + upsert + notify-community fire (D-14 fan-out). Lazy MentionTypeahead keeps Fuse in community-mentions chunk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → community_posts INSERT | RLS policy `cpost_insert` (Plan 44-01) enforces author_id = auth.uid() AND caller can read the target space |
| Browser → community_post_mentions UPSERT | PK (post_id, user_id) prevents duplicate-mention spam; RLS scoped to the mentioning post's author |
| Browser → notify-community | User JWT with mentioned_by_user_id self-check (must equal jwt.sub) — native dual-auth path in Plan 44-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Denial of Service | mention spam via composer | mitigate | community_post_mentions PRIMARY KEY (post_id, user_id) prevents duplicate inserts; daily_cap=20 on notification-send (Plan 44-02); typeahead suggestion cap of 8 results limits surface. |
| T-44-05 | Tampering (XSS in preview tab) | Composer preview | mitigate | Preview renders sanitizeCommunityMarkdown output via dangerouslySetInnerHTML, which is safe because sanitize is the chokepoint (Plan 44-03). |
| T-44-07 | Repudiation (silent edit-after-viral) | Edit-forever mode | accept | D-15 explicitly accepts this risk; edited_at marker surfaces it to readers. Phase 48 adds an admin audit log. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` clean for the 3 new files
- Post-44-09 + 44-10: open a space, type `@b` → typeahead suggests handles starting with `b`; select a handle, submit; verify `community_post_mentions` row + notification arrives in test mailbox (HUMAN-UAT signal in 44-10).
</verification>

<success_criteria>
- 3 components ship with draft autosave (D-11), mention typeahead (D-14), reply notification fan-out (D-14), 5000-char cap (D-11), edit-forever (D-15).
- MentionTypeahead is path-isolated under `mentions/` for the community-mentions sub-chunk (44-09 wires the vite rule).
- Composer calls notify-community directly with the session JWT — Plan 44-05 ships native dual-auth (service-role OR user JWT with sub self-check).
</success_criteria>

<output>
After completion, create `.planning/phases/44-m4-community-feed-foundation/44-07-SUMMARY.md` summarizing the 3 components ; notify-community dual-auth is owned by Plan 44-05.
</output>
