---
phase: 44
plan: 06
type: execute
wave: 1
depends_on: [44-03]
files_modified:
  - leanshot/src/components/community/CommunityPost.tsx
  - leanshot/src/components/community/CommunityFeed.tsx
  - leanshot/src/components/community/CommunityCommentThread.tsx
  - leanshot/src/components/community/ReactionBar.tsx
  - leanshot/src/components/community/use-feed.ts
autonomous: true
requirements:
  - COMMUNITY-01
  - COMMUNITY-02

must_haves:
  truths:
    - "CommunityPost renders sanitized markdown via sanitizeCommunityMarkdown + ReactMarkdown + rehype-raw"
    - "CommunityPost displays `[deleted]` tombstone when deleted_at is set (D-15)"
    - "CommunityPost displays (edited) marker when edited_at is set (D-15)"
    - "CommunityFeed performs cursor-paginated fetch via use-feed hook with PAGE_SIZE=20"
    - "ReactionBar renders 5 fixed emojis (like/heart/target/fire/clap) and calls toggle_community_reaction RPC on click"
    - "CommunityCommentThread renders a flat list (D-01) with depth=1; supports edit-author-only and soft-delete buttons"
  artifacts:
    - path: "leanshot/src/components/community/CommunityPost.tsx"
      provides: "Single post card with sanitized body, edited/deleted markers, ReactionBar"
    - path: "leanshot/src/components/community/CommunityFeed.tsx"
      provides: "Cursor-paginated feed list for one space"
    - path: "leanshot/src/components/community/CommunityCommentThread.tsx"
      provides: "Flat 1-level comment list under a post"
    - path: "leanshot/src/components/community/ReactionBar.tsx"
      provides: "5-emoji reaction pill row + toggle action"
    - path: "leanshot/src/components/community/use-feed.ts"
      provides: "Cursor-paginated feed query hook"
      exports: ["useFeed"]
  key_links:
    - from: "CommunityPost.tsx"
      to: "src/lib/community/dompurify-config.ts"
      via: "import { renderPostBodyHtml } from '@/lib/community/dompurify-config'"
      pattern: "renderPostBodyHtml"
    - from: "ReactionBar.tsx"
      to: "toggle_community_reaction RPC"
      via: "supabase.rpc('toggle_community_reaction', { p_target_type, p_target_id, p_emoji })"
      pattern: "toggle_community_reaction"
    - from: "use-feed.ts"
      to: "community_posts table"
      via: "cursor pagination on (created_at, id) DESC per D-12"
      pattern: "order\\(.created_at..*ascending: false"
---

<objective>
Ship the read-side community UI: post card, feed list, comment thread, reaction bar, and the cursor-paginated feed hook. All components consume Wave 0 utilities (`@/lib/community/*`). Files land under `src/components/community/` (the community-feed catch-all chunk in vite.config.ts at line 194; sub-chunk rules are added in 44-09).

Purpose: Implements COMMUNITY-01 (render markdown posts + comments) and COMMUNITY-02 (reaction toggle) read paths. Composer (write path) is in 44-07.

Output: 5 React/TS files under src/components/community/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-m4-community-feed-foundation/44-CONTEXT.md
@.planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md
@.planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md

@leanshot/src/helpdesk/KBArticleView.tsx
@leanshot/src/lib/community/dompurify-config.ts
@leanshot/src/lib/community/community-types.ts

<interfaces>
<!-- Props contracts -->

CommunityPostProps {
  post: CommunityPost;
  reactions: CommunityReaction[];           // pre-fetched aggregate; flat list filtered to (target_type='post', target_id=post.id)
  mediaSignedUrls: Record<string, string>;  // path → signed URL; pre-resolved by feed
  currentUserId: string;
  onEdit?: (postId: string) => void;        // wired by 44-07
  onDelete?: (postId: string) => void;      // soft-delete; wired by 44-07
}

CommunityFeedProps {
  spaceId: string;
  currentUserId: string;
}
// Internally calls useFeed(spaceId) and renders <CommunityPost> per row.

CommunityCommentThreadProps {
  postId: string;
  spaceId: string;
  currentUserId: string;
}

ReactionBarProps {
  targetType: 'post' | 'comment';
  targetId: string;
  reactions: CommunityReaction[];           // pre-aggregated for this target
  currentUserId: string;
}

useFeed(spaceId: string): {
  posts: CommunityPost[];
  loading: boolean;
  error: string | null;
  loadMore: () => void;
  hasMore: boolean;
}
// Internals: cursor = { created_at, id }; PAGE_SIZE = 20; query per D-12 cursor pagination.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship CommunityPost + CommunityCommentThread + ReactionBar</name>
  <files>leanshot/src/components/community/CommunityPost.tsx, leanshot/src/components/community/CommunityCommentThread.tsx, leanshot/src/components/community/ReactionBar.tsx</files>
  <read_first>
    - leanshot/src/helpdesk/KBArticleView.tsx (lines 12–16 imports; lines 64–101 render pattern)
    - leanshot/src/lib/community/dompurify-config.ts (just shipped by 44-03)
    - leanshot/src/lib/community/community-types.ts (just shipped by 44-03)
    - .planning/phases/44-m4-community-feed-foundation/44-PATTERNS.md lines 678–708 (CommunityPost analog)
    - .planning/phases/44-m4-community-feed-foundation/44-CONTEXT.md D-01, D-02, D-03, D-15
  </read_first>
  <action>
    Author `CommunityPost.tsx`:
    - Functional component with `CommunityPostProps`.
    - Imports: `ReactMarkdown`, `remarkGfm`, `rehypeRaw`, `renderPostBodyHtml` (or `sanitizeCommunityMarkdown` direct), types, ReactionBar, `Card` UI primitive.
    - Use a `Card` wrapper (existing UI primitive at src/components/ui/Card.tsx).
    - Header: avatar + display_name + relative timestamp (use existing helpers if present, else `new Date(post.created_at).toLocaleString()`). Include `(edited)` span when `post.edited_at` is non-null (per D-15).
    - Body: if `post.deleted_at` is non-null → render `<em>[deleted]</em>` literal (or call `renderPostBodyHtml(post)`). Else → `<article className="prose prose-sm max-w-none"><ReactMarkdown remarkPlugins={[remarkGfm]} rehypePlugins={[rehypeRaw]}>{sanitizeCommunityMarkdown(post.body)}</ReactMarkdown></article>` (44-PATTERNS.md lines 687–691).
    - Media rendering (images + Mux video): delegate to `<CommunityPostMediaStrip post={post} mediaSignedUrls={mediaSignedUrls} />`. CommunityPostMediaStrip lives in `src/components/community/CommunityPostMediaStrip.tsx` and is OWNED by Plan 44-08 (which also owns the lazy `./media/CommunityVideoPlayer` import). 44-06 does NOT import @mux/* directly and does NOT inline `image.mux.com` URLs — the strip component owns all media render branches (image carousel, thumbnail, lazy player, uploading/rejected states).

    - Footer: `<ReactionBar targetType='post' targetId={post.id} reactions={reactions} currentUserId={currentUserId} />` and an Edit/Delete pair (visible only when `post.author_id === currentUserId` && `post.deleted_at === null`).
    - Accessibility: `role="article"`, `aria-label={` `Post by ${displayName}`}`; reaction pills carry `aria-pressed`.

    Author `ReactionBar.tsx`:
    - Fixed 5-emoji list (D-03): `const EMOJIS = ['like','heart','target','fire','clap'] as const;`. Render OS-native emoji per Claude's Discretion (e.g., `like → 👍, heart → ❤️, target → 🎯, fire → 🔥, clap → 👏`). Pill button per emoji: count badge + active state when current user has the reaction.
    - Click handler: optimistic UI per RESEARCH Pitfall 6 — flip local state immediately, then call `await supabase.rpc('toggle_community_reaction', { p_target_type: targetType, p_target_id: targetId, p_emoji: emoji })`; on RPC failure, revert.
    - Compute aggregate per-emoji counts from the `reactions` prop using `Array.reduce`. Compute `myReaction = reactions.find(r => r.user_id === currentUserId && r.emoji === e)` for each pill.
    - Accessibility: `<button aria-pressed={!!myReaction} aria-label={`React with ${emoji}`}>`.

    Author `CommunityCommentThread.tsx`:
    - Functional component with `CommunityCommentThreadProps`.
    - Use `useEffect` + cancelled-fetch guard (44-PATTERNS.md lines 967–975) to fetch `community_comments WHERE post_id = $postId AND deleted_at IS NULL ORDER BY created_at ASC` (flat list per D-01).
    - For each comment: render avatar + display_name + body via `<ReactMarkdown>{sanitizeCommunityMarkdown(c.body)}</ReactMarkdown>` (same sanitization as posts; no inline images per D-10) + ReactionBar (`targetType='comment'`).
    - Edit/Delete buttons visible only when comment.author_id === currentUserId.
    - Composer input for new comment is OWNED by 44-07 (CommunityPostComposer also handles comment composition). Here just render `{children}` placeholder for the composer to slot in, OR render a `<CommentComposerPlaceholder />` stub component.
    - Soft-delete render: comments with `deleted_at !== null` render `[deleted]` (consistent with posts; D-15).
    - Accessibility: `role="list"` with `role="listitem"` children.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && ls src/components/community/CommunityPost.tsx src/components/community/CommunityCommentThread.tsx src/components/community/ReactionBar.tsx && grep -c "sanitizeCommunityMarkdown\\|renderPostBodyHtml" src/components/community/CommunityPost.tsx | grep -v '^0$' && grep -c "toggle_community_reaction" src/components/community/ReactionBar.tsx | grep -v '^0$' && grep -c "deleted_at" src/components/community/CommunityPost.tsx | grep -v '^0$' && grep -c "edited_at" src/components/community/CommunityPost.tsx | grep -v "^0$" && grep -c "CommunityPostMediaStrip" src/components/community/CommunityPost.tsx | grep -v "^0$" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/components/community/(CommunityPost|CommunityCommentThread|ReactionBar)" | (! grep error)</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist at the exact paths.
    - CommunityPost.tsx imports from `@/lib/community/dompurify-config` (literal in source).
    - CommunityPost.tsx contains `deleted_at` and `edited_at` literal references in render branches.
    - CommunityPost.tsx contains literal `CommunityPostMediaStrip` (delegation to 44-08-owned media renderer).
    - CommunityPost.tsx does NOT directly import @mux/* and does NOT contain literal `image.mux.com` (media render delegated to CommunityPostMediaStrip).
    - ReactionBar.tsx contains literal `'like','heart','target','fire','clap'` 5-emoji array.
    - ReactionBar.tsx contains literal `toggle_community_reaction` rpc call.
    - ReactionBar.tsx contains literal `aria-pressed`.
    - CommunityCommentThread.tsx contains literal `sanitizeCommunityMarkdown`.
    - CommunityCommentThread.tsx contains literal `deleted_at` filter (soft-delete render).
    - tsc --noEmit reports zero errors for the 3 new files.
  </acceptance_criteria>
  <done>3 read-side components shipped; tsc clean; reaction toggle wired to RPC.</done>
</task>

<task type="auto">
  <name>Task 2: Ship CommunityFeed + use-feed cursor-pagination hook</name>
  <files>leanshot/src/components/community/CommunityFeed.tsx, leanshot/src/components/community/use-feed.ts</files>
  <read_first>
    - leanshot/src/lib/community/community-types.ts (just shipped)
    - .planning/phases/44-m4-community-feed-foundation/44-RESEARCH.md §Code Examples lines 609–636 (cursor pagination)
    - .planning/phases/44-m4-community-feed-foundation/44-CONTEXT.md D-12 (Recent sort + cursor pagination)
  </read_first>
  <action>
    Author `use-feed.ts`:
    - Export `useFeed(spaceId: string)`.
    - State: `posts: CommunityPost[]`, `loading: boolean`, `error: string | null`, `cursor: { created_at: string; id: string } | null`, `hasMore: boolean`.
    - `PAGE_SIZE = 20`.
    - Inner async `fetchPage(cursorVal)`:
      ```
      let q = supabase.from('community_posts')
        .select('id, body, created_at, edited_at, deleted_at, author_id, space_id, mux_upload_id, mux_playback_id, video_status, community_post_media(path, display_order)')
        .eq('space_id', spaceId)
        .is('deleted_at', null)
        .order('created_at', { ascending: false })
        .order('id', { ascending: false })
        .limit(PAGE_SIZE);
      if (cursorVal) {
        q = q.or(`created_at.lt.${cursorVal.created_at},and(created_at.eq.${cursorVal.created_at},id.lt.${cursorVal.id})`);
      }
      const { data, error } = await q;
      ```
    - On mount + on `spaceId` change: fetch first page (cursor null); set posts. On `loadMore()`: fetch next page using `posts[posts.length-1]` as cursor; append. Set `hasMore = data.length === PAGE_SIZE`.
    - Use the cancelled-fetch guard pattern (44-PATTERNS.md lines 967–975).
    - Returns the contract per the interfaces block.

    Author `CommunityFeed.tsx`:
    - Functional component with `CommunityFeedProps`.
    - Calls `useFeed(spaceId)`.
    - For each post, resolve media signed URLs via `Promise.all(post.community_post_media.map(m => getCommunityMediaSignedUrl(m.path)))` and pass into `<CommunityPost mediaSignedUrls={…}>`. Cache signed URLs in a ref keyed by path; refresh when TTL > 50 min has passed. Simple approach: resolve once on first render of each post.
    - Render a `loadMore` button when `hasMore`.
    - Empty state: when `!loading && posts.length === 0` render an EmptyState card with `"No posts yet"` + reference to the composer (composed by 44-09 Space view).
    - Realtime hook integration is owned by 44-09 (which adds `useSpaceRealtime` hook); here, expose an `onRefresh()` callback prop so 44-09 can trigger a refetch when the channel fires.
    - For Plan 44-06 alone, expose useFeed's `refresh()` method (call again with cursor=null) and call it on a `key` prop change OR on a `refreshNonce` prop bump — 44-09 will pass realtime-bumped nonce.

    Update CommunityFeedProps to include optional `refreshNonce?: number;` for 44-09 wiring.

    Reactions: each `CommunityPost` needs its `reactions` prop. Options: (A) feed query joins community_reactions for each post (over-fetch); (B) issue a single `from('community_reactions').select().in('target_id', postIds)` after the feed loads. Use option B for efficiency. Store reactions in feed-local state keyed by post_id; pass the filtered subset to each CommunityPost.

    Add aria-labels to feed list + loadMore button.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && ls src/components/community/CommunityFeed.tsx src/components/community/use-feed.ts && grep -c "ascending: false" src/components/community/use-feed.ts | grep -v '^0$' && grep -c "PAGE_SIZE\\s*=\\s*20" src/components/community/use-feed.ts | grep -v '^0$' && grep -c "is\\(.*deleted_at.*null" src/components/community/use-feed.ts | grep -v '^0$' && grep -c "community_post_media\\|getCommunityMediaSignedUrl" src/components/community/CommunityFeed.tsx | grep -v '^0$' && grep -c "useFeed" src/components/community/CommunityFeed.tsx | grep -v '^0$' && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "src/components/community/(CommunityFeed|use-feed)" | (! grep error)</automated>
  </verify>
  <acceptance_criteria>
    - 2 files exist.
    - use-feed.ts contains `PAGE_SIZE = 20` literal.
    - use-feed.ts contains `ascending: false` for both order() calls (cursor pagination by (created_at, id) DESC).
    - use-feed.ts contains `.is('deleted_at', null)` literal (soft-delete filter).
    - use-feed.ts uses the cancelled-fetch guard (`let cancelled = false; … return () => { cancelled = true; };`).
    - CommunityFeed.tsx imports useFeed.
    - CommunityFeed.tsx contains literal `getCommunityMediaSignedUrl` (signed-URL resolution).
    - CommunityFeed.tsx contains literal `refreshNonce` prop wiring (44-09 entry seam).
    - tsc --noEmit clean for these 2 files.
  </acceptance_criteria>
  <done>Feed + use-feed shipped; cursor pagination on (created_at, id) DESC; signed URL resolution wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| community_posts row → CommunityPost render | All body content passes through sanitizeCommunityMarkdown (no raw `dangerouslySetInnerHTML` outside the sanitized branch) |
| Storage path → signed URL fetch | getCommunityMediaSignedUrl is called client-side; bucket RLS enforces SELECT (Plan 44-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05 | Tampering (XSS) | CommunityPost.tsx + CommunityCommentThread.tsx | mitigate | Both call sanitizeCommunityMarkdown (Plan 44-03 chokepoint). No direct `dangerouslySetInnerHTML` with raw body. |
| T-44-01 | Elevation of Privilege (cross-tenant read) | use-feed.ts query | mitigate | Query uses only spaceId filter; RLS on community_posts (Plan 44-01 cpost_select_tier) is the actual gate — Free users get 0 rows for Pro-only spaces regardless of client query shape. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` reports zero errors for the 5 new files
- After 44-09 wires the route + 44-10 pushes: visit a space URL, see posts render with sanitized markdown, edited/deleted markers visible, reactions clickable
</verification>

<success_criteria>
- 5 read-side files shipped under src/components/community/.
- ReactionBar calls toggle_community_reaction RPC (idempotent toggle per D-02/D-03).
- use-feed implements cursor pagination on (created_at, id) DESC with PAGE_SIZE=20 per D-12.
- Soft-delete tombstone + (edited) marker rendered per D-15.
- Mux player kept in community-media chunk via React.lazy (proves bundle hygiene).
</success_criteria>

<output>
After completion, create `.planning/phases/44-m4-community-feed-foundation/44-06-SUMMARY.md` summarizing the 5 read-side files and the bundle-isolation strategy (lazy MuxPlayer import).
</output>
