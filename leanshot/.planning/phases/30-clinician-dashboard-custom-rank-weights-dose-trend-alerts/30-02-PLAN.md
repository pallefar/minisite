---
phase: 30
plan: 02
type: execute
wave: 1
depends_on: ["30-00"]
files_modified:
  - supabase/migrations/20270601300006_p30_dose_thresholds_rpc.sql
  - leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.tsx
  - leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.test.tsx
  - leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.tsx
  - leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.test.tsx
  - leanshot/src/components/clinic/settings/ClinicSettingsPage.tsx
  - leanshot/src/components/clinic/roster/use-org-settings-realtime.ts
  - leanshot/src/components/clinic/roster/use-org-settings-realtime.test.ts
  - leanshot/src/components/clinic/roster/RosterTable.tsx
  - leanshot/src/components/clinic/roster/RosterRow.tsx
autonomous: true
requirements: [CLIN-01, CLIN-02]
user_setup: []

must_haves:
  truths:
    - "(D-05) `ClinicRankingWeightsForm` renders 4 number inputs (0–100 integer %) for dose_adherence / weight_loss / activity / symptoms with auto-normalize-on-blur, live `Total: N%` counter (green at 100, amber otherwise), Save button aria-disabled when sum ≠ 100; clicking Save calls SECDEF `update_org_ranking_weights(p_org_id, p_weights)` with normalized 0–1 numerics"
    - "(D-07) `ClinicDoseTrendThresholdsForm` renders 3 number inputs (Missed doses N min 1 max 30, Observation window M min 7 max 90, Variance % X min 5 max 100) with placeholder text showing v1.3 defaults (2/14/25); Save calls SECDEF that upserts `org_settings.dose_trend_thresholds`"
    - "(D-06) `use-org-settings-realtime` browser hook subscribes via `channelNameFor(orgId, 'settings')` (Phase 28 helper) to Realtime `postgres_changes` UPDATE events on `org_settings` and invokes a passed `onWeightsChanged` callback; RosterTable wires this hook so weight saves in tab A trigger `useRankRoster.refresh()` in tab B within 1s (SC#1)"
    - "(SC#1 e2e support) `RosterRow.tsx` renders `data-testid='roster-row' data-patient-id={patient.id}` attributes on the row container, enabling Plan 30-05 Playwright e2e to capture row order via `pageB.locator('[data-testid=\"roster-row\"]')`"
    - "(B3 unconditional) `update_org_dose_trend_thresholds(p_org_id uuid, p_thresholds jsonb)` SECDEF shipped in this plan's migration `20270601300006_p30_dose_thresholds_rpc.sql` (NOT in Plan 30-00 — verified at 4 SECDEFs there)"
    - "Both forms reuse existing primitives only (Card / CardHeader / Input / Button / Badge) — NO new design system primitives added; bundle delta for two settings forms < 4 kB gz combined"
    - "Form copy matches 30-UI-SPEC Copywriting Contract exactly: weights heading 'Ranking weights', save CTA 'Save ranking weights', reset link 'Reset to equal weights', sum-valid 'Total: 100% — ready to save' in --color-success, sum-invalid 'Total: N% — must equal 100%' in --color-danger; thresholds heading 'Alert thresholds', save CTA 'Save thresholds'"
    - "Accessibility: weight inputs have `aria-label='{signal} weight'` + `aria-describedby` → sum row id; sum row has `role='status' aria-live='polite'`; loading state uses `aria-busy='true'` on Save button; framer-motion entry animations gated by `useReducedMotion()`"
    - "Forms appear as new tab inside `ClinicSettingsPage` (existing Workspace / Members / Roles / Audit tabs gain a 'Clinical' tab housing both forms); admin-role-only render gate via existing `surfaceCheck` pattern"
  artifacts:
    - path: "leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.tsx"
      provides: "4-input weights form; auto-normalize; SECDEF save; live sum counter"
      contains: "update_org_ranking_weights"
      min_lines: 100
    - path: "leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.tsx"
      provides: "3-input thresholds form; placeholder defaults; SECDEF save"
      contains: "dose_trend_thresholds"
      min_lines: 60
    - path: "leanshot/src/components/clinic/roster/use-org-settings-realtime.ts"
      provides: "browser HMAC channel subscription hook for D-06 settings change broadcast"
      contains: "channelNameFor"
      min_lines: 40
    - path: "supabase/migrations/20270601300006_p30_dose_thresholds_rpc.sql"
      provides: "update_org_dose_trend_thresholds SECDEF (admin-only; mirrors update_org_ranking_weights pattern from Plan 30-00)"
      contains: "update_org_dose_trend_thresholds"
      min_lines: 20
    - path: "leanshot/src/components/clinic/roster/RosterRow.tsx"
      provides: "data-testid='roster-row' data-patient-id={patient.id} on row container for Plan 30-05 SC#1 e2e"
      contains: "data-testid=\"roster-row\""
    - path: "leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.test.tsx"
      provides: "RTL tests: sum-valid/invalid render, auto-normalize, save dispatch, error toast"
      contains: "describe"
    - path: "leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.test.tsx"
      provides: "RTL tests: min/max input clamping, save dispatch, placeholder defaults"
      contains: "describe"
    - path: "leanshot/src/components/clinic/roster/use-org-settings-realtime.test.ts"
      provides: "hook test: subscription registered + onWeightsChanged invoked on broadcast"
      contains: "channelNameFor"
  key_links:
    - from: "leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.tsx"
      to: "supabase/migrations/20270601300003_p30_secdef_and_triggers.sql"
      via: "supabase.rpc('update_org_ranking_weights', { p_org_id, p_weights })"
      pattern: "update_org_ranking_weights"
    - from: "leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.tsx"
      to: "supabase/migrations/20270601300003_p30_secdef_and_triggers.sql"
      via: "supabase.rpc('update_org_dose_trend_thresholds') — admin-only SECDEF; shipped by THIS plan in migration 20270601300006 (Plan 30-00 covers 4 SECDEFs but NOT this one; no hedge)"
      pattern: "dose_trend_thresholds"
    - from: "leanshot/src/components/clinic/roster/RosterTable.tsx"
      to: "leanshot/src/components/clinic/roster/use-org-settings-realtime.ts"
      via: "useOrgSettingsRealtime({orgId, onWeightsChanged: () => useRankRoster.refresh()})"
      pattern: "useOrgSettingsRealtime"
    - from: "leanshot/src/components/clinic/roster/use-org-settings-realtime.ts"
      to: "leanshot/src/lib/org-realtime.ts"
      via: "channelNameFor(orgId, 'settings') Phase 28 browser HMAC helper"
      pattern: "channelNameFor"
---

<objective>
Settings UI: two forms (weights + thresholds) in a new 'Clinical' tab inside ClinicSettingsPage, plus the realtime hook that wires the AFTER UPDATE trigger broadcast back into RosterTable for the 1-second reorder requirement (SC#1).

Purpose: CLIN-01 (per-clinic ranking weights configurable) + CLIN-02 (per-clinic dose-trend thresholds configurable) + SC#1 (roster reorders within 1s of save) closure.

Output: 3 new components + 1 hook + 3 test files + RosterTable wired with hook + ClinicSettingsPage gains 'Clinical' tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-CONTEXT.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-RESEARCH.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-00-PLAN.md
@leanshot/src/components/clinic/settings/ClinicSettingsPage.tsx
@leanshot/src/components/clinic/settings/WorkspaceTab.tsx
@leanshot/src/components/clinic/settings/RolesTab.tsx
@leanshot/src/components/clinic/roster/RosterTable.tsx
@leanshot/src/components/clinic/roster/use-roster-realtime.ts
@leanshot/src/lib/org-realtime.ts
@leanshot/src/components/ui/Card.tsx
@leanshot/src/components/ui/Input.tsx
@leanshot/src/components/ui/Button.tsx
@leanshot/src/components/ui/Badge.tsx
@leanshot/src/hooks/useReducedMotion.ts
@leanshot/src/hooks/useToast.ts

<interfaces>
From leanshot/src/lib/org-realtime.ts (Phase 28):
  export async function channelNameFor(orgId: string, suffix: string): Promise<string>  // SHA-256 HMAC, first 8 hex

From leanshot/src/components/clinic/roster/use-roster-realtime.ts:
  export function useRosterRealtime({ orgId, onSignalChange }: UseRosterRealtimeOptions): void
  (existing pattern — mirror for use-org-settings-realtime)

Phase 30 SECDEFs (shipped Plan 30-00 Task 2):
  rpc('update_org_ranking_weights', { p_org_id: string, p_weights: { dose_adherence: number, weight_loss: number, activity: number, symptoms: number } }) → returns void; raises 42501 / P0001
  rpc('update_org_dose_trend_thresholds', { p_org_id: string, p_thresholds: { missed_doses_n: number, window_days_m: number, variance_pct_x: number } }) → returns void; raises 42501

  NOTE: `update_org_dose_trend_thresholds` SECDEF is NOT in Plan 30-00 (which ships 4 SECDEFs: update_org_ranking_weights, set_patient_dose_thresholds, acknowledge_clinician_alert, snooze_clinician_alert). THIS plan unconditionally ships migration `20270601300006_p30_dose_thresholds_rpc.sql` in Task 1. Pattern mirrors `update_org_ranking_weights` exactly: admin-role gate on `org_members.role='admin'`, UPDATE `org_settings` SET dose_trend_thresholds = p_thresholds WHERE org_id = p_org_id, audit log via direct INSERT + `set_config('app.suppress_audit','on',true)`.

UI primitives:
  <Card variant="elevated"> / <CardHeader title icon action />
  <Input type="number" min max aria-label aria-describedby aria-invalid />
  <Button variant="primary" loading aria-busy disabled />
  <Badge tone="success|amber|danger|neutral" />

Toast: const toast = useToast(); toast.success("..."); toast.error("...");
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClinicRankingWeightsForm + ClinicDoseTrendThresholdsForm + ClinicSettingsPage 'Clinical' tab</name>
  <files>
    leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.tsx,
    leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.test.tsx,
    leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.tsx,
    leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.test.tsx,
    leanshot/src/components/clinic/settings/ClinicSettingsPage.tsx,
    supabase/migrations/20270601300006_p30_dose_thresholds_rpc.sql
  </files>
  <read_first>
    - leanshot/src/components/clinic/settings/ClinicSettingsPage.tsx (existing tab structure to extend)
    - leanshot/src/components/clinic/settings/WorkspaceTab.tsx (form layout precedent)
    - leanshot/src/components/clinic/settings/RolesTab.tsx (admin-role surface-check precedent)
    - leanshot/src/components/ui/Card.tsx + Input.tsx + Button.tsx + Badge.tsx (primitive API surface)
    - leanshot/src/hooks/useToast.ts (toast helper signature)
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md §"ClinicRankingWeightsForm" + §"ClinicDoseTrendThresholdsForm" + §"Copywriting Contract"
  </read_first>
  <behavior>
    ClinicRankingWeightsForm:
    - Test 1: renders with 4 inputs each defaulting to 25 (equal weights) when org_settings.ranking_weights is null
    - Test 2: typing values that sum to 100 → Save button enabled, sum row shows "Total: 100% — ready to save" in --color-success
    - Test 3: typing values that sum to 90 → Save button aria-disabled, sum row shows "Total: 90% — must equal 100%" in --color-danger
    - Test 4: clicking Save calls supabase.rpc('update_org_ranking_weights', { p_org_id: orgId, p_weights: { dose_adherence: 0.25, weight_loss: 0.25, activity: 0.25, symptoms: 0.25 } }); on success shows toast 'Ranking weights saved. Roster will reorder shortly.'
    - Test 5: on RPC error shows toast 'Save failed — please try again.' and re-enables button
    - Test 6: clicking 'Reset to equal weights' resets all 4 inputs to 25 (no confirmation modal)
    - Test 7: blur on an input triggers auto-normalize — e.g., values (40, 30, 30, 30) → blur → (31, 23, 23, 23) approximately (proportional scaling to sum=100, rounded to integer)

    ClinicDoseTrendThresholdsForm:
    - Test 1: 3 inputs with placeholder "2" / "14" / "25" respectively when org_settings.dose_trend_thresholds is the default
    - Test 2: input value clamping respects min/max (N min 1 max 30, M min 7 max 90, X min 5 max 100)
    - Test 3: Save calls supabase.rpc('update_org_dose_trend_thresholds', { p_org_id, p_thresholds }) with integer values
    - Test 4: success toast 'Alert thresholds saved. New alerts will use these thresholds.'
  </behavior>
  <action>
    **Step 0 (unconditional):** Create `supabase/migrations/20270601300006_p30_dose_thresholds_rpc.sql` shipping the `update_org_dose_trend_thresholds(p_org_id uuid, p_thresholds jsonb) returns void` SECDEF. Pattern verbatim mirrors `update_org_ranking_weights` from Plan 30-00 Task 2: `security definer`, `set search_path = pg_catalog, public, extensions`, admin-role gate `if not exists (select 1 from public.org_members where org_id = p_org_id and user_id = auth.uid() and role = 'admin') then raise insufficient_privilege using errcode = '42501'; end if;`, `UPDATE public.org_settings SET dose_trend_thresholds = p_thresholds WHERE org_id = p_org_id`, audit log via direct `INSERT INTO public.audit_logs(...)` preceded by `perform set_config('app.suppress_audit', 'on', true)`. Then `supabase db push --linked` and verify with `supabase db query --linked "select to_regprocedure('public.update_org_dose_trend_thresholds(uuid,jsonb)') is not null as exists" --output csv | tail -1 | grep -q true`.

    Write `ClinicRankingWeightsForm.tsx` per 30-UI-SPEC:
    - Layout: `<Card variant="elevated"><CardHeader title="Ranking weights" /><div className="space-y-4">...</div></Card>`
    - Description text per UI-SPEC: "Adjust how patient signals are weighted when ordering the roster. Weights must sum to 100%."
    - 4 `<Input type="number" min={0} max={100}>` for dose_adherence / weight_loss / activity / symptoms with labels "Dose adherence" / "Weight loss" / "Activity" / "Symptoms" (sentence case per UI-SPEC).
    - Internal state holds integer-percent values [0-100]. On blur of any input: auto-normalize all 4 to sum to 100 proportionally (scale = 100 / current_sum, round to integer, distribute rounding error to the largest weight).
    - Sum row below inputs: `<div role="status" aria-live="polite" id="weights-sum-row">Total: {N}% — {N === 100 ? 'ready to save' : 'must equal 100%'}</div>` with class `text-[var(--color-success)]` when N=100 else `text-[var(--color-danger)]`.
    - `aria-describedby="weights-sum-row"` on each input; `aria-label="{signal} weight"` per UI-SPEC accessibility table.
    - Save button: `<Button variant="primary" disabled={sum !== 100} aria-busy={isSaving}>Save ranking weights</Button>`. On click: convert to 0-1 numerics (divide each by 100), call `supabase.rpc('update_org_ranking_weights', { p_org_id, p_weights })`. On success: `toast.success('Ranking weights saved. Roster will reorder shortly.')`. On error: `toast.error('Save failed — please try again.')`.
    - Below Save: text link "Reset to equal weights" (NOT a button per UI-SPEC); onClick sets all 4 inputs to 25.
    - Lazy-load: ensure the form file does NOT import anything from outside the `/clinic/*` chunk to maintain bundle ceiling per UI-SPEC §"Bundle Constraint Reminder".

    Write `ClinicDoseTrendThresholdsForm.tsx`:
    - Same Card scaffolding; heading "Alert thresholds"; description per UI-SPEC.
    - 3 number inputs: "Missed doses (N)" min=1 max=30, "Observation window (days)" min=7 max=90, "Interval variance (%)" min=5 max=100. Placeholder shows the v1.3 default value (2 / 14 / 25). Input value starts empty; only fills when the org has saved custom thresholds.
    - Save button: `<Button variant="primary">Save thresholds</Button>`. On click: `supabase.rpc('update_org_dose_trend_thresholds', { p_org_id, p_thresholds: { missed_doses_n: n, window_days_m: m, variance_pct_x: x } })`. Toast success/error per UI-SPEC.

    Both `.test.tsx` files use vitest + @testing-library/react + @testing-library/user-event. Mock `supabase` client (`vi.mock('@/lib/supabase', ...)`) and `useToast`. Implement all behavior tests above.

    Update `ClinicSettingsPage.tsx`: add new tab "Clinical" alongside existing Workspace / Members / Roles / Audit tabs. The new tab renders `<><ClinicRankingWeightsForm orgId={orgId} /><ClinicDoseTrendThresholdsForm orgId={orgId} /></>` stacked vertically with `gap-6` per spacing scale. Visibility gated by existing admin-role surface-check pattern from RolesTab.tsx (only org-admins see the 'Clinical' tab).
  </action>
  <verify>
    <automated>
      test -f supabase/migrations/20270601300006_p30_dose_thresholds_rpc.sql && \
      grep -q 'update_org_dose_trend_thresholds' supabase/migrations/20270601300006_p30_dose_thresholds_rpc.sql && \
      grep -q "set search_path = pg_catalog, public, extensions" supabase/migrations/20270601300006_p30_dose_thresholds_rpc.sql && \
      test -f leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.tsx && \
      test -f leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.tsx && \
      grep -q 'update_org_ranking_weights' leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.tsx && \
      grep -q 'Save ranking weights' leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.tsx && \
      grep -q 'aria-live' leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.tsx && \
      grep -q 'role="status"' leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.tsx && \
      grep -q 'Reset to equal weights' leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.tsx && \
      grep -q 'Save thresholds' leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.tsx && \
      grep -q 'dose_trend_thresholds\|update_org_dose_trend_thresholds' leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.tsx && \
      grep -q 'Clinical' leanshot/src/components/clinic/settings/ClinicSettingsPage.tsx && \
      cd leanshot && npm run test:unit -- --run src/components/clinic/settings/ClinicRankingWeightsForm.test.tsx src/components/clinic/settings/ClinicDoseTrendThresholdsForm.test.tsx 2>&1 | tail -10 | grep -qE "Test Files\\s+2 passed"
    </automated>
  </verify>
  <done>
    Both forms ship per UI-SPEC copy/a11y contract; SECDEFs called with correct payload shape; auto-normalize math works; tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: use-org-settings-realtime hook + RosterTable wiring (SC#1 1-second reorder)</name>
  <files>
    leanshot/src/components/clinic/roster/use-org-settings-realtime.ts,
    leanshot/src/components/clinic/roster/use-org-settings-realtime.test.ts,
    leanshot/src/components/clinic/roster/RosterTable.tsx
  </files>
  <read_first>
    - leanshot/src/components/clinic/roster/use-roster-realtime.ts (existing realtime hook pattern to mirror)
    - leanshot/src/components/clinic/roster/RosterTable.tsx (insertion point — likely already uses useRankRoster + useRosterRealtime)
    - leanshot/src/lib/org-realtime.ts (channelNameFor signature + caching behavior)
    - leanshot/src/lib/__tests__/org-realtime.test.ts (existing mock pattern for channel subscription)
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-RESEARCH.md §"Browser hook: subscribe to settings channel for D-06"
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md §"SC#1 visual support map"
  </read_first>
  <behavior>
    - Test 1: useOrgSettingsRealtime mounts → channelNameFor(orgId, 'settings') is called → supabase.channel(topic) is invoked
    - Test 2: incoming postgres_changes UPDATE event on org_settings table where new.org_id matches → onWeightsChanged callback invoked exactly once
    - Test 3: postgres_changes UPDATE event for a DIFFERENT org_id is ignored (callback NOT invoked)
    - Test 4: hook unmount calls supabase.removeChannel
    - Test 5: subscription failure (channel.subscribe returns CHANNEL_ERROR) does NOT throw; logs warning; downstream code unaffected (failsafe is Phase 10 30s polling — preserved)
  </behavior>
  <action>
    Write `use-org-settings-realtime.ts`:
    ```
    Signature: export function useOrgSettingsRealtime({ orgId, onWeightsChanged }: { orgId: string; onWeightsChanged: () => void }): void
    Body: useEffect dependent on [orgId, onWeightsChanged] — call channelNameFor(orgId, 'settings') (async — handle inside the effect), then supabase.channel(topic).on('postgres_changes', { event: 'UPDATE', schema: 'public', table: 'org_settings' }, (payload) => { if ((payload.new as { org_id?: string }).org_id === orgId) onWeightsChanged(); }).subscribe((status) => { if (status === 'CHANNEL_ERROR') console.warn('[use-org-settings-realtime] HMAC channel subscribe failed for org', orgId); }). Cleanup: supabase.removeChannel(channel).
    ```
    Mirror existing `use-roster-realtime.ts` structure exactly (it uses the same channelNameFor + channel.subscribe pattern).

    Write `use-org-settings-realtime.test.ts` with 5 tests per behavior block. Use vitest + mock `@/lib/supabase` (channel/on/subscribe/removeChannel mocks returning chainable proxies) + mock `@/lib/org-realtime` (channelNameFor returns deterministic 'org-aaaa1111-settings').

    Update `RosterTable.tsx`:
    - Locate the existing `useRosterRealtime({ orgId, onSignalChange })` call.
    - Add `useOrgSettingsRealtime({ orgId, onWeightsChanged: useCallback(() => rankRosterQuery.refetch(), [rankRosterQuery]) });` immediately after.
    - The existing useRankRoster hook MUST expose `.refetch()` or equivalent — verify and use the exact API. If only a setter-style refresh exists, use that pattern.
    - Do NOT modify the 30-second polling — it stays as failsafe per D-06.

    Update `RosterRow.tsx` (B5 plan-checker fix — Plan 30-05 SC#1 e2e dependency):
    - Locate the root row element (likely a `<tr>` or `<div>` with role='row').
    - Add `data-testid="roster-row"` and `data-patient-id={patient.id}` attributes (use the actual prop name for the patient ID — verify by reading the existing RosterRow.tsx props interface). These attributes are inert at runtime (no behavior change); they exist purely to let Plan 30-05's Playwright spec capture row order without selector brittleness.
    - DO NOT change any other rendering, styles, or behavior. Single-attribute add.
  </action>
  <verify>
    <automated>
      test -f leanshot/src/components/clinic/roster/use-org-settings-realtime.ts && \
      test -f leanshot/src/components/clinic/roster/use-org-settings-realtime.test.ts && \
      grep -q 'channelNameFor' leanshot/src/components/clinic/roster/use-org-settings-realtime.ts && \
      grep -q 'postgres_changes' leanshot/src/components/clinic/roster/use-org-settings-realtime.ts && \
      grep -q 'org_settings' leanshot/src/components/clinic/roster/use-org-settings-realtime.ts && \
      grep -q 'useOrgSettingsRealtime' leanshot/src/components/clinic/roster/RosterTable.tsx && \
      grep -q 'data-testid="roster-row"' leanshot/src/components/clinic/roster/RosterRow.tsx && \
      grep -q 'data-patient-id' leanshot/src/components/clinic/roster/RosterRow.tsx && \
      cd leanshot && npm run test:unit -- --run src/components/clinic/roster/use-org-settings-realtime.test.ts 2>&1 | tail -10 | grep -qE "Test Files\\s+1 passed"
    </automated>
  </verify>
  <done>
    Hook subscribes to HMAC settings channel + invokes callback only for matching org_id + survives subscription failures; RosterTable wires the hook; tests green; 30s polling failsafe preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→SECDEF | Form Save invokes supabase.rpc — SECDEF re-validates admin role + validates jsonb shape (trigger raises P0001 for sum violations) |
| Realtime→client | postgres_changes broadcast on HMAC channel — Phase 28 realtime_topic_authorized validates HMAC + JWT org_ids on subscribe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | Tampering | Client bypasses sum-100 validation in form | mitigate | Server-side `_validate_ranking_weights` trigger raises P0001 even if client sends sum ≠ 1.0 — form UI is convenience only |
| T-30-02-02 | Information disclosure | Realtime channel leaks another org's settings change | mitigate | HMAC channel name derived from `(orgId, 'settings')` + Vault secret; subscriber JWT org_ids claim re-checked by realtime_topic_authorized; Plan 30-05 e2e proves cross-org subscribe returns CHANNEL_ERROR |
| T-30-02-03 | Denial of service | Realtime subscription failure leaves RosterTable stuck on stale weights | accept | Phase 10 30s polling failsafe is preserved by design; degrades from 1s to 30s — acceptable per CLIN-01 SC#1 wording |
| T-30-02-04 | Elevation of privilege | Non-admin user sees 'Clinical' tab and tries to call SECDEF | mitigate | Tab render gated by existing admin surface-check; SECDEF re-checks `org_members.role = 'admin'` and raises 42501 |
</threat_model>

<verification>
- `cd leanshot && npm run test:unit -- --run src/components/clinic/settings/ src/components/clinic/roster/use-org-settings-realtime.test.ts` all passes
- `cd leanshot && npx tsc --noEmit -p tsconfig.app.json` exits 0 (strict mode compliance)
- `cd leanshot && npm run lint -- src/components/clinic/settings/ src/components/clinic/roster/use-org-settings-realtime.ts` exits 0
- Visual sanity: `cd leanshot && npm run dev` → open `/clinic/{slug}/settings` → 'Clinical' tab visible to admin role, hidden for staff role
</verification>

<success_criteria>
- ClinicRankingWeightsForm + ClinicDoseTrendThresholdsForm ship per UI-SPEC copy/a11y contracts
- use-org-settings-realtime hook mirrors use-roster-realtime pattern + survives subscription failures
- RosterTable refetches rank query within ~1s of weight save in another tab (verified by Plan 30-05 Playwright e2e)
- No new design system primitives introduced; bundle delta <4 kB gz for the 3 new client files
- Plan 30-00 SECDEFs called with correct payload shape (4 numerics summing 1.0; 3 integer thresholds)
</success_criteria>

<output>
After completion, create `.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-02-SUMMARY.md` documenting: form file paths + Clinical tab integration point + use-org-settings-realtime hook signature + whether `update_org_dose_trend_thresholds` SECDEF was added in this plan vs Plan 30-00.
</output>
