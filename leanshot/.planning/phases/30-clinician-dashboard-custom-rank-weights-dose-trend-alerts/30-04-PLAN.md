---
phase: 30
plan: 04
type: execute
wave: 1
depends_on: ["30-00"]
files_modified:
  - leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.tsx
  - leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.test.tsx
  - leanshot/src/components/clinic/dashboard/use-clinic-metrics.ts
  - leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx
  - leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.test.tsx
  - leanshot/src/components/clinic/drill-in/ClinicDrillInPage.tsx
autonomous: true
requirements: [CLIN-05, CLIN-07, CLIN-08]
user_setup: []

must_haves:
  truths:
    - "(D-15/D-16/CLIN-05/CLIN-08) `ClinicDashboardOverview` renders 4 stat cards in responsive grid (≥768px 3-col + wraps; mobile single column); 'Pending alerts' is primary focal point (shadow-md vs shadow-sm + 20px figure size); reads from `mv_clinic_alert_metrics` + `mv_clinic_dose_trend_population` + `clinic_matview_refresh_log`"
    - "Stat cards: (1) Pending alerts (this week) — amber when >0 / success when 0; (2) Ack rate (7 days) from mv_clinic_alert_metrics.ack_rate_pct color-coded per UI-SPEC (>=80 success / 50-79 amber / <50 danger); (3) Patients below dosing range from mv_clinic_dose_trend_population — amber fill when >0; (4) Alert types breakdown (dose_adherence vs dose_variance counts)"
    - "Matview staleness caption: `<p className='text-[13px] text-[var(--color-text-tertiary)]'>Updated every 15 minutes · Last: {relative time}</p>` reads from `clinic_matview_refresh_log` (shipped Plan 30-00)"
    - "Empty state per UI-SPEC: heading 'No data yet' / body 'Metrics appear after the first nightly alert scan. Check back tomorrow.'"
    - "(D-08/CLIN-07) `PatientThresholdOverrideForm` lives as new tab/section inside `ClinicDrillInPage`, label 'Dose thresholds'; 3 number inputs (N/M/X) with placeholder showing effective clinic default; Save calls SECDEF `set_patient_dose_thresholds(p_org_id, p_patient_user_id, p_thresholds)` (Plan 30-00); Reset opens `<Modal>` with title 'Reset patient thresholds' + buttons 'Reset to defaults' (danger) + 'Keep current thresholds' (secondary)"
    - "Reset modal copy (canonical per UI-SPEC §Copywriting Contract): body = 'This will remove the override for {patient display name} and use the clinic's default thresholds. This cannot be undone.'; on confirm: DELETE row via SECDEF (or upsert with NULL) — researcher decides; toast success 'Thresholds reset to clinic defaults.'"
    - "All new components React.lazy-loaded; no new heavy dependencies (no chart.js per UI-SPEC §Bundle Constraint)"
    - "Visibility gating: ClinicDashboardOverview visible to org_members with `role in ('admin','staff')` (any clinic-side user); PatientThresholdOverrideForm visible to `role in ('admin','staff')` only"
  artifacts:
    - path: "leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.tsx"
      provides: "4-stat-card responsive grid + staleness caption + empty/loading states"
      contains: "mv_clinic_alert_metrics"
      min_lines: 120
    - path: "leanshot/src/components/clinic/dashboard/use-clinic-metrics.ts"
      provides: "Query hook fetching both matviews + clinic_matview_refresh_log timestamps"
      contains: "mv_clinic_dose_trend_population"
    - path: "leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx"
      provides: "3-input override form + Reset modal with binary-choice buttons"
      contains: "set_patient_dose_thresholds"
      min_lines: 100
    - path: "leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.test.tsx"
      provides: "Render tests: 4 cards, color coding, staleness caption, empty state"
      contains: "describe"
    - path: "leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.test.tsx"
      provides: "Tests: save dispatch, reset modal confirmation, placeholder defaults"
      contains: "describe"
  key_links:
    - from: "leanshot/src/components/clinic/dashboard/use-clinic-metrics.ts"
      to: "supabase/migrations/20270601300004_p30_matviews_and_cron.sql"
      via: "supabase.from('mv_clinic_alert_metrics').select(...) + supabase.from('mv_clinic_dose_trend_population').select(...) + supabase.from('clinic_matview_refresh_log').select(...)"
      pattern: "mv_clinic_alert_metrics"
    - from: "leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx"
      to: "supabase/migrations/20270601300003_p30_secdef_and_triggers.sql"
      via: "supabase.rpc('set_patient_dose_thresholds', { p_org_id, p_patient_user_id, p_thresholds })"
      pattern: "set_patient_dose_thresholds"
    - from: "leanshot/src/components/clinic/drill-in/ClinicDrillInPage.tsx"
      to: "leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx"
      via: "new 'Dose thresholds' tab/section in drill-in subnav"
      pattern: "PatientThresholdOverrideForm"
---

<objective>
Aggregate clinic dashboard (CLIN-05/08) reading the 2 matviews + refresh-log timestamp, AND the patient drill-in extension (CLIN-07) for per-patient threshold overrides with reset modal.

Purpose: SC#5 (population-level metrics visible in 15-min-refresh dashboard) + CLIN-07 (per-patient thresholds via drill-in surface).

Output: ClinicDashboardOverview + use-clinic-metrics + PatientThresholdOverrideForm + ClinicDrillInPage tab integration + 2 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-CONTEXT.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-RESEARCH.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-00-PLAN.md
@leanshot/src/components/clinic/drill-in/ClinicDrillInPage.tsx
@leanshot/src/components/clinic/drill-in/ClinicDrillInSubBar.tsx
@leanshot/src/components/clinic/drill-in/PatientActivityModal.tsx
@leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.tsx
@leanshot/src/components/ui/Card.tsx
@leanshot/src/components/ui/Modal.tsx
@leanshot/src/components/ui/EmptyState.tsx
@leanshot/src/components/ui/Skeleton.tsx

<interfaces>
Phase 30 schema (Plan 30-00):
  Matview: public.mv_clinic_alert_metrics
    Columns: (org_id, alert_type, pending_count, acknowledged_count, total_count, ack_rate_pct, avg_time_to_ack_minutes)
  Matview: public.mv_clinic_dose_trend_population
    Columns: (org_id, medication_name, dosing_range_status, patient_count)
  Table: public.clinic_matview_refresh_log
    Columns: (view_name text pk, refreshed_at timestamptz)

SECDEF (Plan 30-00 Task 2):
  rpc('set_patient_dose_thresholds', { p_org_id: string, p_patient_user_id: string, p_thresholds: { missed_doses_n: number, window_days_m: number, variance_pct_x: number } | null }) → returns void
  NULL p_thresholds → DELETE the override row (researcher decision — verify behavior or add a sibling SECDEF `reset_patient_dose_thresholds(p_org_id, p_patient_user_id)` if NULL-as-delete is rejected). If Plan 30-00 SECDEF body uses INSERT...ON CONFLICT DO UPDATE only, add `reset_patient_dose_thresholds` to a small migration in this plan's Task 2.

UI primitives:
  <Card variant="elevated"><CardHeader title /></Card>
  <Modal isOpen onClose title role="dialog" aria-modal>
  <EmptyState heading body icon? />
  <Skeleton width height />

CSS tokens per UI-SPEC §Color:
  --color-amber (#e0af4e) — pending count > 0; ack rate 50-79
  --color-success — ack rate >= 80; 0 pending
  --color-danger — ack rate < 50
  --color-text-tertiary — staleness caption
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClinicDashboardOverview + use-clinic-metrics query hook</name>
  <files>
    leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.tsx,
    leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.test.tsx,
    leanshot/src/components/clinic/dashboard/use-clinic-metrics.ts
  </files>
  <read_first>
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md §"ClinicDashboardOverview (CLIN-05/08)" (layout, visual focal point, 4-card spec)
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md §"Copywriting Contract" (exact stat-card copy)
    - leanshot/src/components/clinic/billing/ClinicBillingCard.tsx (existing clinic stat card pattern to mirror)
    - leanshot/src/components/ui/Card.tsx (Card + CardHeader API + shadow variants)
    - leanshot/src/components/ui/Skeleton.tsx + EmptyState.tsx
  </read_first>
  <behavior>
    use-clinic-metrics:
    - Test 1: returns `{ pendingThisWeek: number; ackRatePct: number | null; belowDosingRange: number; alertTypeBreakdown: { dose_adherence: number; dose_variance: number }; lastRefreshedAt: Date | null; isLoading; error }`
    - Test 2: aggregates pending across alert_types correctly (sum(pending_count))
    - Test 3: lastRefreshedAt = max(refreshed_at) across the 2 matview rows in clinic_matview_refresh_log
    - Test 4: handles empty matview state (returns zeros + lastRefreshedAt=null)

    ClinicDashboardOverview:
    - Test 1: renders heading 'Clinic overview' and 4 stat cards in DOM order: Pending alerts, Ack rate (7 days), Below dosing range, Alert types
    - Test 2: 'Pending alerts' card has `shadow-md` (other cards `shadow-sm`); figure uses 20px size (text-[20px]); when count > 0 figure is `text-[var(--color-amber)]`, when 0 is `text-[var(--color-success)]`
    - Test 3: Ack rate >=80 → `text-[var(--color-success)]`; 50-79 → `text-[var(--color-amber)]`; <50 → `text-[var(--color-danger)]`; null → renders '—' in `text-[var(--color-text-tertiary)]`
    - Test 4: 'Patients below dosing range' renders count from mv_clinic_dose_trend_population where dosing_range_status='below'; amber fill when >0
    - Test 5: 'Alert types' card renders two sub-figures labeled 'Missed doses' and 'Dose variance'
    - Test 6: Staleness caption renders 'Updated every 15 minutes · Last: {relative time}' using `text-[13px] text-[var(--color-text-tertiary)]`
    - Test 7: Empty state when both matviews are empty AND lastRefreshedAt is null → renders 'No data yet / Metrics appear after the first nightly alert scan. Check back tomorrow.'
    - Test 8: Loading state renders 4 Skeleton placeholders (one per stat card)
    - Test 9: Each stat card figure has `aria-label="{heading}: {value}"` for screen-reader comprehension
  </behavior>
  <action>
    Write `use-clinic-metrics.ts`. Returns the type shape from behavior Test 1. Implementation: 3 parallel queries via `Promise.all`:
    (1) `supabase.from('mv_clinic_alert_metrics').select('alert_type, pending_count, acknowledged_count, ack_rate_pct').eq('org_id', orgId)` → reduce to aggregate stats.
    (2) `supabase.from('mv_clinic_dose_trend_population').select('medication_name, dosing_range_status, patient_count').eq('org_id', orgId).eq('dosing_range_status', 'below')` → sum patient_count.
    (3) `supabase.from('clinic_matview_refresh_log').select('view_name, refreshed_at').in('view_name', ['mv_clinic_alert_metrics', 'mv_clinic_dose_trend_population'])` → take max(refreshed_at).
    Mirror existing query hook patterns from `use-clinic-snapshot.ts` (drill-in package) — useEffect + useState + refetch helper.

    Write `ClinicDashboardOverview.tsx`:
    - `<section aria-labelledby="clinic-overview-heading"><h1 id="clinic-overview-heading" className="text-[20px] font-semibold">Clinic overview</h1>` per UI-SPEC §Typography page-heading.
    - Grid: `<div className="grid grid-cols-1 md:grid-cols-3 gap-4">`. 'Pending alerts' wraps to occupy the prominent first slot per UI-SPEC visual hierarchy (e.g., `md:col-span-3` for a wide hero card OR `md:row-span-2` — choose one approach that gives it visual primacy without breaking the 3-col grid).
    - Stat card pattern: `<Card variant="elevated" className={isPendingCard ? 'shadow-md' : 'shadow-sm'}><CardHeader title={heading} /><p className={figureClasses} aria-label={`${heading}: ${value}`}>{value}</p>{subLabel && <p className="text-[13px] text-[var(--color-text-tertiary)]">{subLabel}</p>}</Card>`.
    - Pending alerts card: heading 'Pending alerts'; figure text-[20px]; class amber-when->0 or success-when-0.
    - Ack rate (7 days) card: heading 'Ack rate (7 days)'; figure shows `{ackRatePct}%` or `'—'`; color logic per UI-SPEC.
    - Below dosing range card: heading 'Below dosing range' / sub 'This week'; figure amber when >0 else text-tertiary.
    - Alert types card: heading 'Alert types'; two sub-figures `<div>Missed doses: {n}</div><div>Dose variance: {n}</div>`.
    - Below grid: staleness caption per UI-SPEC. Use date-fns or similar (already in project — verify) to format "Last: 12 minutes ago". If date-fns absent, hand-roll a small `relativeTime(date)` helper.
    - Empty state: `<EmptyState heading="No data yet" body="Metrics appear after the first nightly alert scan. Check back tomorrow." />` when isLoading=false AND lastRefreshedAt=null AND all counts are 0.
    - Loading state: render 4 `<Skeleton className="h-32 rounded-lg" />` placeholders in the same grid positions.

    Write `ClinicDashboardOverview.test.tsx` with 9 tests per behavior block. Mock `@/lib/supabase` returning stubbed query results; mock `use-clinic-metrics` for isolated card-rendering tests.

    Lazy-load: this component is mounted from the clinic dashboard route (TBD — likely `ClinicWorkspaceTab` or a new route under `/clinic/{slug}/overview`). Ensure parent imports via `React.lazy`. Verify post-build bundle ceiling in Plan 30-05.
  </action>
  <verify>
    <automated>
      test -f leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.tsx && \
      test -f leanshot/src/components/clinic/dashboard/use-clinic-metrics.ts && \
      grep -q 'mv_clinic_alert_metrics' leanshot/src/components/clinic/dashboard/use-clinic-metrics.ts && \
      grep -q 'mv_clinic_dose_trend_population' leanshot/src/components/clinic/dashboard/use-clinic-metrics.ts && \
      grep -q 'clinic_matview_refresh_log' leanshot/src/components/clinic/dashboard/use-clinic-metrics.ts && \
      grep -q 'Pending alerts' leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.tsx && \
      grep -q 'Ack rate (7 days)' leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.tsx && \
      grep -q 'Below dosing range' leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.tsx && \
      grep -q 'Alert types' leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.tsx && \
      grep -q 'Updated every 15 minutes' leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.tsx && \
      grep -q 'No data yet' leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.tsx && \
      grep -q "var(--color-amber)" leanshot/src/components/clinic/dashboard/ClinicDashboardOverview.tsx && \
      cd leanshot && npm run test:unit -- --run src/components/clinic/dashboard/ClinicDashboardOverview.test.tsx 2>&1 | tail -10 | grep -qE "Test Files\\s+1 passed"
    </automated>
  </verify>
  <done>
    Dashboard renders 4 stat cards per UI-SPEC layout + copy + color coding; staleness caption reads refresh-log; empty/loading states green; 9 tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PatientThresholdOverrideForm + ClinicDrillInPage 'Dose thresholds' tab + reset_patient_dose_thresholds SECDEF (if needed)</name>
  <files>
    leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx,
    leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.test.tsx,
    leanshot/src/components/clinic/drill-in/ClinicDrillInPage.tsx,
    supabase/migrations/20270601300007_p30_reset_patient_thresholds_rpc.sql
  </files>
  <read_first>
    - leanshot/src/components/clinic/drill-in/ClinicDrillInPage.tsx (existing tab/section structure to extend)
    - leanshot/src/components/clinic/drill-in/ClinicDrillInSubBar.tsx (subnav primitive)
    - leanshot/src/components/clinic/settings/ClinicDoseTrendThresholdsForm.tsx (Plan 30-02 — 3-input layout pattern to mirror)
    - leanshot/src/components/ui/Modal.tsx (role=dialog + aria-modal contract)
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md §"PatientThresholdOverrideForm (CLIN-07)" (placement + 3 inputs + reset modal copy)
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md §"Copywriting Contract" (canonical reset modal body)
    - supabase/migrations/20270601200004_org_patient_invite_rpcs.sql (SECDEF audit pattern)
  </read_first>
  <behavior>
    - Test 1: form renders 3 inputs (N/M/X) with placeholders showing the EFFECTIVE clinic default (read from org_settings.dose_trend_thresholds via prop)
    - Test 2: when no override exists, inputs are empty; Save dispatches `set_patient_dose_thresholds({p_org_id, p_patient_user_id, p_thresholds: {n,m,x}})`; success toast 'Patient thresholds saved.'
    - Test 3: when override exists, inputs prefill with override values
    - Test 4: 'Reset to clinic defaults' button opens Modal with title 'Reset patient thresholds' + body containing patient display name + canonical UI-SPEC text
    - Test 5: Modal has 'Reset to defaults' button (variant='danger') + 'Keep current thresholds' button (variant='secondary')
    - Test 6: Clicking 'Reset to defaults' dispatches reset SECDEF; on success closes modal + toast 'Thresholds reset to clinic defaults.'
    - Test 7: Clicking 'Keep current thresholds' closes modal WITHOUT calling SECDEF
    - Test 8: input min/max clamping respected (same constraints as ClinicDoseTrendThresholdsForm: N 1-30, M 7-90, X 5-100)
  </behavior>
  <action>
    Verify Plan 30-00 SECDEF `set_patient_dose_thresholds` behavior: does NULL p_thresholds delete the row? If yes, reset = `set_patient_dose_thresholds(p_org_id, p_patient_user_id, null)`. If no, create `supabase/migrations/20270601300007_p30_reset_patient_thresholds_rpc.sql`:
    `create or replace function public.reset_patient_dose_thresholds(p_org_id uuid, p_patient_user_id uuid) returns void language plpgsql security definer set search_path = pg_catalog, public, extensions as $$ declare v_caller_role public.org_member_role; begin select role into v_caller_role from public.org_members where org_id = p_org_id and user_id = auth.uid(); if v_caller_role not in ('admin','staff') then raise exception 'admin or staff role required' using errcode = '42501'; end if; perform set_config('app.suppress_audit', 'on', true); delete from public.org_patient_thresholds where org_id = p_org_id and patient_user_id = p_patient_user_id; insert into public.audit_logs(action, table_name, record_id, actor_user_id) values ('reset_patient_dose_thresholds', 'org_patient_thresholds', p_patient_user_id, auth.uid()); end; $$;` — wrap in `begin; ... commit;`.

    Write `PatientThresholdOverrideForm.tsx`:
    - Props: `{ orgId: string; patientUserId: string; patientDisplayName: string; clinicDefaults: { missed_doses_n: number; window_days_m: number; variance_pct_x: number }; existingOverride: { missed_doses_n: number; window_days_m: number; variance_pct_x: number } | null }`.
    - Heading: 'Dose thresholds for this patient' (per UI-SPEC Copywriting).
    - Description text per UI-SPEC: "Overrides the clinic's default thresholds for this patient only. Leave blank to use the clinic default."
    - 3 number inputs with same constraints as `ClinicDoseTrendThresholdsForm`. Placeholders show clinicDefaults values; initial value = existingOverride value if present else empty.
    - Below inputs: small `text-[13px] text-[var(--color-text-tertiary)]` note: "Overrides clinic default for this patient only."
    - `<Button variant="primary">Save patient thresholds</Button>` → `await supabase.rpc('set_patient_dose_thresholds', { p_org_id, p_patient_user_id, p_thresholds: { missed_doses_n: n, window_days_m: m, variance_pct_x: x } })`; toast.success('Patient thresholds saved.') on success.
    - `<Button variant="secondary" onClick={openResetModal}>Reset to clinic defaults</Button>` below Save.
    - Reset Modal: `<Modal isOpen={resetOpen} onClose={closeReset} title="Reset patient thresholds">`. Body: exact UI-SPEC canonical copy `"This will remove the override for ${patientDisplayName} and use the clinic's default thresholds. This cannot be undone."`. Buttons row: `<Button variant="danger" onClick={confirmReset}>Reset to defaults</Button>` + `<Button variant="secondary" onClick={closeReset}>Keep current thresholds</Button>`. On confirm: `await supabase.rpc('reset_patient_dose_thresholds', { p_org_id, p_patient_user_id })` (or `set_patient_dose_thresholds(..., null)` if that's the Plan 30-00 contract); toast.success('Thresholds reset to clinic defaults.'); close modal; reset form to empty.

    Update `ClinicDrillInPage.tsx`: add a new tab/section in the drill-in subnav labeled 'Dose thresholds' (after existing tabs). Renders `<PatientThresholdOverrideForm orgId={orgId} patientUserId={patient.id} patientDisplayName={patient.display_name} clinicDefaults={resolvedClinicDefaults} existingOverride={overrideRow} />`. Fetch `clinicDefaults` from `org_settings.dose_trend_thresholds` (cached in clinic context or fetched here) and `existingOverride` from `org_patient_thresholds.thresholds` for `(orgId, patientUserId)`.

    Write `PatientThresholdOverrideForm.test.tsx` with 8 tests per behavior block.
  </action>
  <verify>
    <automated>
      test -f leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx && \
      grep -q 'set_patient_dose_thresholds\|reset_patient_dose_thresholds' leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx && \
      grep -q 'Dose thresholds for this patient' leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx && \
      grep -q 'Reset patient thresholds' leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx && \
      grep -q 'Reset to defaults' leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx && \
      grep -q 'Keep current thresholds' leanshot/src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx && \
      grep -q 'Dose thresholds' leanshot/src/components/clinic/drill-in/ClinicDrillInPage.tsx && \
      grep -q 'PatientThresholdOverrideForm' leanshot/src/components/clinic/drill-in/ClinicDrillInPage.tsx && \
      cd leanshot && npm run test:unit -- --run src/components/clinic/drill-in/PatientThresholdOverrideForm.test.tsx 2>&1 | tail -10 | grep -qE "Test Files\\s+1 passed"
    </automated>
  </verify>
  <done>
    Override form ships in drill-in 'Dose thresholds' tab; reset modal uses canonical UI-SPEC copy + binary-choice buttons; SECDEF dispatched correctly; 8 tests green; reset_patient_dose_thresholds migration shipped (if not already in Plan 30-00).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→matview SELECT | RLS on matviews — verify matview inherits source-table RLS via SECURITY INVOKER (default for matviews; verify org members can SELECT their own org rows only) |
| client→SECDEF | set_patient_dose_thresholds + reset_patient_dose_thresholds re-check `org_member.role in ('admin','staff')` for p_org_id and patient link exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | Information disclosure | Matview leaks another org's metrics via direct SELECT | mitigate | RLS on source table `clinician_alerts` filters; matview SELECT runs with caller's role; verify in Plan 30-05 e2e cross-tenant matview test |
| T-30-04-02 | Tampering | Patient threshold override applied to wrong patient | mitigate | SECDEF re-checks `org_patient_links` row exists for `(p_org_id, p_patient_user_id)` (Plan 30-00 Task 2 contract); raises 42704 if no link |
| T-30-04-03 | Repudiation | Reset action removes override without audit | mitigate | reset_patient_dose_thresholds SECDEF writes audit_logs row with `set_config('app.suppress_audit','on',true)` per Phase 29 pattern |
| T-30-04-04 | Information disclosure | Stat cards show patient counts that imply individual identity (e.g., '1 patient below dosing range') | accept | Population-level metrics; single-patient cohorts could be probed for identity but RLS already gates SELECT to org members who have direct access anyway. Residual risk negligible. |
</threat_model>

<verification>
- `cd leanshot && npm run test:unit -- --run src/components/clinic/dashboard/ src/components/clinic/drill-in/PatientThresholdOverrideForm.test.tsx` all passes
- `cd leanshot && npx tsc --noEmit -p tsconfig.app.json` exits 0
- `cd leanshot && npm run lint -- src/components/clinic/dashboard/ src/components/clinic/drill-in/PatientThresholdOverrideForm.tsx src/components/clinic/drill-in/ClinicDrillInPage.tsx` exits 0
</verification>

<success_criteria>
- ClinicDashboardOverview ships 4 stat cards per UI-SPEC layout + color coding + staleness caption
- Reads from mv_clinic_alert_metrics + mv_clinic_dose_trend_population + clinic_matview_refresh_log
- PatientThresholdOverrideForm ships in drill-in 'Dose thresholds' tab with canonical reset modal copy
- reset_patient_dose_thresholds SECDEF exists (in Plan 30-00 OR shipped here)
- All new components React.lazy-loaded; no chart.js; bundle delta < 8 kB gz combined
</success_criteria>

<output>
After completion, create `.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-04-SUMMARY.md` documenting: dashboard/drill-in file paths + which migrations were used (Plan 30-00 only vs `reset_patient_dose_thresholds` added here) + ClinicDrillInPage tab integration point + bundle impact estimate.
</output>
