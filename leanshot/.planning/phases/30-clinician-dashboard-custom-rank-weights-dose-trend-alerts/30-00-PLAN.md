---
phase: 30
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270601300001_p30_org_settings_extensions.sql
  - supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql
  - supabase/migrations/20270601300003_p30_secdef_and_triggers.sql
  - supabase/migrations/20270601300004_p30_matviews_and_cron.sql
  - supabase/migrations/20270601300005_p30_weighted_rank_org_patients.sql
  - supabase/functions/_shared/with-org-scope.ts
  - leanshot/src/lib/__tests__/rls-org-clinician-alerts.test.ts
  - leanshot/src/lib/__tests__/rls-org-patient-thresholds.test.ts
  - leanshot/src/lib/__tests__/rank-org-patients-weights.test.ts
  - leanshot/src/lib/__tests__/clinician-alert-debounce.test.ts
  - leanshot/src/lib/__tests__/clinician-alert-auto-resolve.test.ts
  - leanshot/src/lib/__tests__/mv-clinic-alert-metrics.test.ts
autonomous: false
requirements: [CLIN-01, CLIN-02, CLIN-04, CLIN-05, CLIN-06, CLIN-07, CLIN-08]
user_setup: []

must_haves:
  truths:
    - "(D-03) org_settings has `ranking_weights jsonb null` + `dose_trend_thresholds jsonb default` columns with CHECK-like validator trigger enforcing 4 keys / [0,1] / sum=1.0±0.001"
    - "(D-04) `rank_org_patients` SECDEF reads `org_settings.ranking_weights` and falls back to existing hardcoded defaults when NULL (Phase 10 contract preserved)"
    - "(D-06) AFTER UPDATE OF ranking_weights trigger on org_settings emits pg_notify so Realtime postgres_changes broadcasts on `org-{hmac8}-settings` channel"
    - "(D-07) org_settings.dose_trend_thresholds defaults to `{missed_doses_n:2, window_days_m:14, variance_pct_x:25}` jsonb"
    - "(D-08) `org_patient_thresholds(org_id, patient_user_id, thresholds jsonb, set_by, set_at)` table exists with composite PK + FK to org_patient_links, RLS forced, append-only invariant maintained by SECDEF-only writes"
    - "(D-09) `clinician_alerts` table exists with id/org_id/patient_user_id/alert_type/severity/status/threshold_snapshot/ack_by/ack_at/snooze_until/retry_count/last_attempt_at/debounce_key/created_at/auto_resolved_at columns; status CHECK enum 5 values; severity smallint persisted (UI omits in v1.3)"
    - "(D-11) UNIQUE index on (org_id, debounce_key) enforces same-day same-type same-patient deduplication at INSERT"
    - "(D-14) `clinician_alert_deliveries(id, alert_id, channel, attempted_at, success, error)` append-only table exists; INSERT only via service_role; no UPDATE policy"
    - "(D-10) status-machine SECDEFs `acknowledge_clinician_alert(p_alert_id)`, `snooze_clinician_alert(p_alert_id, p_duration)`, `update_org_ranking_weights(p_org_id, p_weights)`, `set_patient_dose_thresholds(p_org_id, p_patient_user_id, p_thresholds)` exist with `search_path=pg_catalog,public,extensions`, direct INSERT INTO audit_logs + `set local app.suppress_audit='on'`, role-gate on `org_member_role` enum (`'admin'` and `'staff'`)"
    - "(D-15) `mv_clinic_alert_metrics` matview exists with UNIQUE index permitting REFRESH MATERIALIZED VIEW CONCURRENTLY"
    - "(D-16) `mv_clinic_dose_trend_population` matview exists with UNIQUE index permitting REFRESH MATERIALIZED VIEW CONCURRENTLY"
    - "(D-17) 4 pg_cron jobs registered: `p30_clinician_alert_detect` `30 3 * * *`, `p30_clinician_alert_deliver` `*/20 * * * *`, `p30_clinician_alert_auto_resolve` `15 4 * * *`, `p30_clinic_matview_refresh` `2,17,32,47 * * * *`"
    - "Phase 28 EXTENSION-CONTRACT R1-R5 honored: `org_id uuid not null references organizations(id) on delete restrict` on all 3 new org-scoped tables; paired `rls-org-*` test file present for clinician_alerts + org_patient_thresholds; ORG_SCOPED_TABLES appended with `clinician_alerts`, `clinician_alert_deliveries`, `org_patient_thresholds`; SECDEF `search_path` locked"
    - "Cross-tenant impersonation proof: User A (Org X) cannot SELECT/INSERT/UPDATE/DELETE `clinician_alerts` rows of Org Y OR `org_patient_thresholds` rows of Org Y; tests use ES256-compat fixture + file-scoped TEST_SLUG_PREFIX"
    - "Schema pushed to live Supabase project ref `ytnsipxxmzgaebkqmokp` via `supabase db push --linked`; no rows skipped (filename regex strict 14-digit)"
  artifacts:
    - path: "supabase/migrations/20270601300001_p30_org_settings_extensions.sql"
      provides: "ranking_weights + dose_trend_thresholds columns + _validate_ranking_weights validator function + BEFORE INSERT/UPDATE validator trigger"
      contains: "alter table public.org_settings"
    - path: "supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql"
      provides: "3 new org-scoped tables (clinician_alerts, clinician_alert_deliveries, org_patient_thresholds) + RLS forced + paired indexes + clinic_matview_refresh_log table"
      contains: "create table if not exists public.clinician_alerts"
    - path: "supabase/migrations/20270601300003_p30_secdef_and_triggers.sql"
      provides: "4 status-machine SECDEFs + AFTER UPDATE OF ranking_weights trigger emitting pg_notify"
      contains: "create or replace function public.acknowledge_clinician_alert"
    - path: "supabase/migrations/20270601300004_p30_matviews_and_cron.sql"
      provides: "2 matviews + UNIQUE indexes + 4 pg_cron jobs + detect-cron pure-SQL body + auto-resolve pure-SQL body"
      contains: "create materialized view if not exists public.mv_clinic_alert_metrics"
    - path: "supabase/migrations/20270601300005_p30_weighted_rank_org_patients.sql"
      provides: "rank_org_patients SECDEF extension reading org_settings.ranking_weights with NULL-fallback"
      contains: "create or replace function public.rank_org_patients"
    - path: "supabase/functions/_shared/with-org-scope.ts"
      provides: "ORG_SCOPED_TABLES Set now includes clinician_alerts + clinician_alert_deliveries + org_patient_thresholds"
      contains: "clinician_alerts"
    - path: "leanshot/src/lib/__tests__/rls-org-clinician-alerts.test.ts"
      provides: "cross-tenant SELECT/INSERT/UPDATE/DELETE deny tests + same-org SELECT permit + service_role bypass test"
      contains: "P30 RLS"
    - path: "leanshot/src/lib/__tests__/rls-org-patient-thresholds.test.ts"
      provides: "cross-tenant SELECT/INSERT/UPDATE/DELETE deny tests + same-org SELECT permit"
      contains: "P30 RLS"
    - path: "leanshot/src/lib/__tests__/rank-org-patients-weights.test.ts"
      provides: "weighted-ranking invariant + NULL-fallback parity test against Phase 10 baseline"
      contains: "rank_org_patients"
    - path: "leanshot/src/lib/__tests__/clinician-alert-debounce.test.ts"
      provides: "UNIQUE (org_id, debounce_key) dedup test — second INSERT same day/type/patient is silently ignored"
      contains: "ON CONFLICT"
    - path: "leanshot/src/lib/__tests__/clinician-alert-auto-resolve.test.ts"
      provides: "pending alerts > 7d with snooze_until IS NULL transition to auto_resolved via cron SQL"
      contains: "auto_resolved"
    - path: "leanshot/src/lib/__tests__/mv-clinic-alert-metrics.test.ts"
      provides: "ack_rate_pct + avg_time_to_ack_minutes computed correctly; REFRESH CONCURRENTLY succeeds"
      contains: "mv_clinic_alert_metrics"
  key_links:
    - from: "supabase/migrations/20270601300003_p30_secdef_and_triggers.sql"
      to: "supabase/migrations/20270601100015_get_realtime_secret_secdef_fn.sql"
      via: "AFTER UPDATE trigger uses pg_notify 'org_settings_weights_changed' channel; Realtime SECDEF auth gates HMAC subscribe"
      pattern: "perform pg_notify"
    - from: "supabase/migrations/20270601300005_p30_weighted_rank_org_patients.sql"
      to: "supabase/migrations/20260901000003_rank_org_patients_rpc.sql"
      via: "EXTENDS Phase 10 SECDEF; preserves RankRosterRow shape + non-clinic path via NULL fallback"
      pattern: "select ranking_weights into v_weights from public.org_settings"
    - from: "supabase/migrations/20270601300004_p30_matviews_and_cron.sql"
      to: "supabase/functions/clinician-alert-deliver-cron"
      via: "pg_cron `*/20 * * * *` HTTP POST to Edge Fn (shipped Plan 30-01)"
      pattern: "net.http_post"
    - from: "leanshot/src/lib/__tests__/rls-org-clinician-alerts.test.ts"
      to: "leanshot/src/lib/__tests__/_fixtures/p28-rls-fixture.ts"
      via: "createTwoOrgsTwoUsers fixture + makeSlugPrefix + ES256-compat session mint"
      pattern: "createTwoOrgsTwoUsers"
---

<objective>
Ship the Phase 30 schema foundation in a single atomic Wave 0 push: ranking_weights + dose_trend_thresholds columns on org_settings, 3 new org-scoped tables (clinician_alerts, clinician_alert_deliveries, org_patient_thresholds), 4 status-machine SECDEFs (ack/snooze/update_weights/set_patient_thresholds), AFTER UPDATE pg_notify trigger for ranking_weights, weighted rank_org_patients extension with NULL-fallback, 2 matviews + clinic_matview_refresh_log, 4 pg_cron jobs (detect 03:30 / deliver */20 / auto-resolve 04:15 / matview refresh staggered), ORG_SCOPED_TABLES update for the 3 new tables, paired cross-tenant RLS proof tests + matview/debounce/auto-resolve/weighted-rank unit tests.

Purpose: Every downstream Phase 30 plan (Edge Fn deliver-cron, UI forms, alerts panel, dashboard) depends on this schema being live. Schema must push successfully before any Wave 1 plan starts.

Output: 5 migrations on disk + pushed via `supabase db push --linked`; `ORG_SCOPED_TABLES` Set updated; 6 vitest files covering RLS isolation + matview correctness + debounce + auto-resolve + weighted-rank parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-CONTEXT.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-RESEARCH.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-EXTENSION-CONTRACT.md
@.planning/phases/29-org-subscriptions-per-patient-metered-billing/29-CONTEXT.md
@supabase/migrations/20270601100005_org_settings_table.sql
@supabase/migrations/20270601100003_org_member_role_enum.sql
@supabase/migrations/20270601200004_org_patient_invite_rpcs.sql
@supabase/migrations/20260901000003_rank_org_patients_rpc.sql
@supabase/migrations/20270601100015_get_realtime_secret_secdef_fn.sql
@supabase/functions/_shared/with-org-scope.ts
@leanshot/src/lib/__tests__/_fixtures/p28-rls-fixture.ts
@leanshot/src/lib/__tests__/rls-org-patient-invites.test.ts

<interfaces>
<!-- Canonical helpers and constants downstream tasks must reuse -->

From supabase/migrations/20270601100003_org_member_role_enum.sql:
  Enum: public.org_member_role = ('admin', 'staff', 'viewer')
  NOTE: 'clinician' is NOT a valid value. Use 'staff' as the clinician-role analog throughout all RLS policies + SECDEF role-gates.

From supabase/migrations/20270601100005_org_settings_table.sql:
  Table: public.org_settings (org_id PK references organizations(id))
  Phase 30 ADDS columns: ranking_weights jsonb null, dose_trend_thresholds jsonb default

From supabase/functions/_shared/with-org-scope.ts:
  Set: ORG_SCOPED_TABLES — append 'clinician_alerts', 'clinician_alert_deliveries', 'org_patient_thresholds'

From leanshot/src/lib/__tests__/_fixtures/p28-rls-fixture.ts:
  Export: SHOULD_RUN, createTwoOrgsTwoUsers(prefix), cleanupByPrefix(prefix), makeSlugPrefix(filename), TwoOrgsTwoUsers type

From supabase/migrations/20270601200004_org_patient_invite_rpcs.sql:
  SECDEF audit pattern (P29 verified): direct INSERT INTO audit_logs with `perform set_config('app.suppress_audit', 'on', true);` — NOT log_admin_action (platform-admin-only).

From supabase/migrations/20260901000003_rank_org_patients_rpc.sql:
  SECDEF: public.rank_org_patients(p_org_id uuid, …) returns setof RankRosterRow
  EXTENSION POINT: scored CTE uses hardcoded weight integers; extract into v_w_dose_adherence/v_w_weight_loss/v_w_activity/v_w_symptoms scalars before EXECUTE, fall back to existing hardcoded defaults when org_settings.ranking_weights IS NULL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migrations — org_settings extensions + 3 new tables + RLS</name>
  <files>
    supabase/migrations/20270601300001_p30_org_settings_extensions.sql,
    supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql
  </files>
  <read_first>
    - supabase/migrations/20270601100005_org_settings_table.sql (org_settings baseline)
    - supabase/migrations/20270601100003_org_member_role_enum.sql (enum is `('admin','staff','viewer')`)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-EXTENSION-CONTRACT.md §2 (RLS template) + §4 (naming + `on delete restrict`)
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-RESEARCH.md §"Code Examples > Migration: ADD ranking_weights column" + §"Migration: clinician_alerts table"
    - supabase/migrations/20270601200003_org_patient_invites.sql (Phase 29 RLS pattern for org-scoped table)
  </read_first>
  <action>
    Filename slot `20270601300001` (per D-03 strict 14-digit; next available after Phase 29 `20270601200007`). Write `20270601300001_p30_org_settings_extensions.sql` containing: (a) `alter table public.org_settings add column if not exists ranking_weights jsonb null default null;`, (b) `alter table public.org_settings add column if not exists dose_trend_thresholds jsonb null default '{"missed_doses_n":2,"window_days_m":14,"variance_pct_x":25}'::jsonb;`, (c) `create or replace function public._validate_ranking_weights(p_weights jsonb) returns void language plpgsql security definer set search_path = pg_catalog, public, extensions as $$ ... $$` enforcing all 4 keys present (`dose_adherence`, `weight_loss`, `activity`, `symptoms`), each in [0,1], sum=1.0±0.001 (raise exception with errcode `P0001` on violation; return silently when input is NULL). Function body MUST be wrapped in `BEGIN ... END` PL/pgSQL block per [[reference_supabase_migration_gotchas]]. Skip CHECK constraint — JSONB shape checks cannot be expressed in CHECK; use a BEFORE INSERT OR UPDATE OF ranking_weights trigger that invokes `_validate_ranking_weights(new.ranking_weights)` (per D-03 CHECK-or-trigger choice).

    Write `20270601300002_p30_clinician_alerts_schema.sql` containing: (a) `create table if not exists public.clinician_alerts (id uuid pk default gen_random_uuid(), org_id uuid not null references public.organizations(id) on delete restrict, patient_user_id uuid not null references auth.users(id) on delete restrict, alert_type text not null check (alert_type in ('dose_adherence','dose_variance')), severity smallint not null default 1 check (severity in (1,2,3)), status text not null default 'pending' check (status in ('pending','acknowledged','snoozed','auto_resolved','delivery_failed')), threshold_snapshot jsonb not null, ack_by uuid null references auth.users(id) on delete set null, ack_at timestamptz null, snooze_until timestamptz null, retry_count smallint not null default 0, last_attempt_at timestamptz null, debounce_key text not null, created_at timestamptz not null default now(), auto_resolved_at timestamptz null);` per D-09. Add 3 indexes: `clinician_alerts_org_id_idx (org_id)`, partial `clinician_alerts_pending_idx (status) where status='pending'`, UNIQUE `clinician_alerts_debounce_uq (org_id, debounce_key)` per D-11. `alter table ... enable row level security; ... force row level security;`. Add SELECT policy permitting org members where `role in ('admin','staff')` (NOT 'clinician' — RESEARCH §Open Questions #2 enum is `('admin','staff','viewer')`). NO INSERT/UPDATE/DELETE policies for authenticated.

    Add (b) `create table if not exists public.clinician_alert_deliveries (id uuid pk default gen_random_uuid(), alert_id uuid not null references public.clinician_alerts(id) on delete cascade, channel text not null check (channel in ('realtime','email')), attempted_at timestamptz not null default now(), success boolean not null, error text null);` per D-14. Append-only: enable RLS + force RLS + SELECT policy for org_members of the parent alert's org (subquery join to clinician_alerts); explicitly NO INSERT/UPDATE/DELETE policies (service_role only via SECDEF or detect/deliver-cron). Add index on `(alert_id, attempted_at)`.

    Add (c) `create table if not exists public.org_patient_thresholds (org_id uuid not null references public.organizations(id) on delete restrict, patient_user_id uuid not null, thresholds jsonb not null, set_by uuid null references auth.users(id) on delete set null, set_at timestamptz not null default now(), primary key (org_id, patient_user_id));` per D-08. Add FK `foreign key (org_id, patient_user_id) references public.org_patient_links(org_id, patient_user_id) on delete cascade` to enforce link-before-override invariant. Enable + force RLS. SELECT policy for org_members where `role in ('admin','staff')`. NO INSERT/UPDATE/DELETE policies (only via SECDEF Plan 30-00 Task 2).

    Add (d) `create table if not exists public.clinic_matview_refresh_log (view_name text primary key, refreshed_at timestamptz not null default now());` per RESEARCH Pitfall 1 (avoid `now()` inside matview SELECT). Forced RLS; SELECT permitted for all authenticated; no INSERT/UPDATE/DELETE for authenticated.

    Wrap each migration in `begin; ... commit;`. Verify filename regex `^[0-9]{14}_[a-z0-9_]+\.sql$` per [[reference_supabase_migration_filename_regex]] (no letter suffix). Do NOT push yet — Task 4 owns push.
  </action>
  <verify>
    <automated>
      test -f supabase/migrations/20270601300001_p30_org_settings_extensions.sql && \
      test -f supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql && \
      grep -q 'ranking_weights jsonb' supabase/migrations/20270601300001_p30_org_settings_extensions.sql && \
      grep -q 'dose_trend_thresholds' supabase/migrations/20270601300001_p30_org_settings_extensions.sql && \
      grep -q '_validate_ranking_weights' supabase/migrations/20270601300001_p30_org_settings_extensions.sql && \
      grep -q 'create table if not exists public.clinician_alerts' supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql && \
      grep -q 'clinician_alerts_debounce_uq' supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql && \
      grep -q 'create table if not exists public.clinician_alert_deliveries' supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql && \
      grep -q 'create table if not exists public.org_patient_thresholds' supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql && \
      grep -q 'create table if not exists public.clinic_matview_refresh_log' supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql && \
      grep -qE "role in \('admin','staff'\)|role = 'admin' or .*role = 'staff'" supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql && \
      ! grep -q "'clinician'" supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql && \
      ls supabase/migrations/20270601300001_*.sql supabase/migrations/20270601300002_*.sql | grep -vE '^supabase/migrations/[0-9]{14}_[a-z0-9_]+\.sql$' | wc -l | grep -q '^0$'
    </automated>
  </verify>
  <done>
    Both migration files exist, contain all required tables/columns/validators/indexes, use 'staff' not 'clinician' for org_member_role, pass filename regex, and reference `on delete restrict` on org_id FKs per EXTENSION-CONTRACT R3.
  </done>
</task>

<task type="auto">
  <name>Task 2: SECDEFs + AFTER UPDATE trigger + weighted rank_org_patients extension + matviews + 4 crons</name>
  <files>
    supabase/migrations/20270601300003_p30_secdef_and_triggers.sql,
    supabase/migrations/20270601300004_p30_matviews_and_cron.sql,
    supabase/migrations/20270601300005_p30_weighted_rank_org_patients.sql,
    supabase/functions/_shared/with-org-scope.ts
  </files>
  <read_first>
    - supabase/migrations/20270601200004_org_patient_invite_rpcs.sql (SECDEF audit pattern with `app.suppress_audit`)
    - supabase/migrations/20260901000003_rank_org_patients_rpc.sql (extension point — DECLARE block + EXECUTE format)
    - supabase/migrations/20270601200006_org_metered_billing_cron.sql (pg_cron net.http_post pattern + vault.decrypted_secrets pattern)
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-RESEARCH.md §"D-17: Cron Collision Audit" + §"Pattern 1: BEFORE UPDATE Trigger" (note: AFTER UPDATE corrected) + §"Pitfall 1" (no now() in matview SELECT) + §"Pitfall 3" (JSONB weight scalar extraction)
    - supabase/functions/_shared/with-org-scope.ts (ORG_SCOPED_TABLES Set)
    - supabase/migrations/20270601100015_get_realtime_secret_secdef_fn.sql (get_realtime_secret SECDEF)
  </read_first>
  <action>
    Write `20270601300003_p30_secdef_and_triggers.sql` containing 4 SECDEF RPCs and the AFTER UPDATE trigger. Each SECDEF uses `language plpgsql security definer set search_path = pg_catalog, public, extensions` and writes audit via direct `insert into public.audit_logs(...)` preceded by `perform set_config('app.suppress_audit', 'on', true);` (NOT `log_admin_action` per Pitfall 5 + Phase 29 verified pattern).

    SECDEFs to create:
    (a) `update_org_ranking_weights(p_org_id uuid, p_weights jsonb) returns void` — re-checks caller has `org_member_role = 'admin'` for p_org_id via `org_members` (raise `42501` if not); UPDATE org_settings SET ranking_weights = p_weights WHERE org_id = p_org_id (trigger validates shape); writes audit row `{action: 'update_org_ranking_weights', table_name: 'org_settings', record_id: p_org_id, new_data: jsonb_build_object('ranking_weights', p_weights)}`.
    (b) `set_patient_dose_thresholds(p_org_id uuid, p_patient_user_id uuid, p_thresholds jsonb) returns void` — re-checks caller role in `('admin','staff')` via `org_members` AND verifies org_patient_links row exists for `(p_org_id, p_patient_user_id) where unlinked_at is null` (raise `42704` if no link); upsert into `org_patient_thresholds` via INSERT ... ON CONFLICT (org_id, patient_user_id) DO UPDATE SET thresholds = excluded.thresholds, set_by = auth.uid(), set_at = now(); audit log.
    (c) `acknowledge_clinician_alert(p_alert_id uuid) returns void` — fetch alert.org_id; re-check caller role in `('admin','staff')` for org_id; UPDATE clinician_alerts SET status='acknowledged', ack_by=auth.uid(), ack_at=now() WHERE id=p_alert_id AND status='pending' (raise `22023` if 0 rows affected — guards re-ack); audit log.
    (d) `snooze_clinician_alert(p_alert_id uuid, p_duration text) returns void` — `p_duration` is one of `'1h'|'4h'|'24h'|'7d'` (raise `22023` for others — preset-only per D-13); fetch alert.org_id; re-check role in `('admin','staff')`; map duration to interval (`'1h'`→`interval '1 hour'`, etc.); UPDATE clinician_alerts SET status='snoozed', snooze_until=now()+v_interval WHERE id=p_alert_id AND status='pending'; audit log.

    Add trigger DDL per D-06 (RESEARCH correction — AFTER UPDATE not BEFORE):
    `create or replace function public._notify_org_settings_weights_changed() returns trigger language plpgsql security definer set search_path = pg_catalog, public, extensions as $$ begin if (new.ranking_weights IS DISTINCT FROM old.ranking_weights) then perform pg_notify('org_settings_weights_changed', json_build_object('org_id', new.org_id)::text); end if; return new; end; $$;`
    `create trigger org_settings_weights_changed_trigger after update of ranking_weights on public.org_settings for each row execute function public._notify_org_settings_weights_changed();`
    Browser subscribes to `org-{hmac8}-settings` channel via `postgres_changes` event on `org_settings` table per RESEARCH §Pattern 1 (consumed by Plan 30-02).

    Write `20270601300005_p30_weighted_rank_org_patients.sql` — extend Phase 10 SECDEF per D-04. Add DECLARE block variables `v_weights jsonb`, `v_w_dose_adherence numeric`, `v_w_weight_loss numeric`, `v_w_activity numeric`, `v_w_symptoms numeric`. After permission gate, `select ranking_weights into v_weights from public.org_settings where org_id = p_org_id;`. Compute `v_w_dose_adherence := coalesce((v_weights->>'dose_adherence')::numeric * 100, 28)` (28 = existing hardcoded Phase 10 value; verify the four baseline integers in `20260901000003_rank_org_patients_rpc.sql` and substitute per-signal). Pass the 4 scalars as `%L` format arguments in the EXECUTE format() call (per Pitfall 3 — never inject jsonb directly). Preserve RankRosterRow shape exactly. Behavior MUST be identical to Phase 10 when `v_weights IS NULL` (all 4 scalars take their existing hardcoded defaults).

    Write `20270601300004_p30_matviews_and_cron.sql`:
    - `create materialized view if not exists public.mv_clinic_alert_metrics as select org_id, alert_type, count(*) filter (where status='pending') as pending_count, count(*) filter (where status='acknowledged') as acknowledged_count, count(*) as total_count, round(100.0 * count(*) filter (where status='acknowledged') / nullif(count(*), 0), 2) as ack_rate_pct, round(avg(extract(epoch from (ack_at - created_at))/60.0) filter (where ack_at is not null), 2) as avg_time_to_ack_minutes from public.clinician_alerts where created_at > now() - interval '7 days' group by org_id, alert_type;` — NO period_start/period_end columns (Pitfall 1 — track via clinic_matview_refresh_log instead).
    - `create unique index if not exists mv_clinic_alert_metrics_uq on public.mv_clinic_alert_metrics (org_id, alert_type);` (REQUIRED before first CONCURRENTLY refresh per Pitfall 2).
    - `create materialized view if not exists public.mv_clinic_dose_trend_population as select opl.org_id, coalesce((select v.medication_name from public.vials v where v.user_id = opl.patient_user_id order by v.created_at desc limit 1), 'Unknown') as medication_name, case when ca.alert_type = 'dose_adherence' then 'below' else 'within' end as dosing_range_status, count(distinct opl.patient_user_id) as patient_count from public.org_patient_links opl left join public.clinician_alerts ca on ca.patient_user_id = opl.patient_user_id and ca.org_id = opl.org_id and ca.created_at > now() - interval '7 days' and ca.status != 'auto_resolved' where opl.unlinked_at is null group by opl.org_id, medication_name, dosing_range_status;` — Per RESEARCH A2 (vials.medication_name) verify column exists; if column name differs, substitute the verified one. Add UNIQUE index `mv_clinic_dose_trend_population_uq (org_id, medication_name, dosing_range_status)`.
    - Initial population: `refresh materialized view public.mv_clinic_alert_metrics; refresh materialized view public.mv_clinic_dose_trend_population;` (non-CONCURRENTLY for first run; CONCURRENTLY for cron).
    - 4 pg_cron jobs (idempotent — unschedule any existing job by name first, per Plan 29-04 pattern):
      • `p30_clinician_alert_detect` schedule `'30 3 * * *'` — pure SQL body executing detection: WITH adherence (count distinct date_trunc('day', created_at) per patient over `effective_thresholds.window_days_m` < `effective_thresholds.missed_doses_n` from `public.injections` joined on `org_patient_links`) AND variance (STDDEV_POP over `LAG(created_at)` injection intervals > `effective_thresholds.variance_pct_x`%); effective thresholds = `COALESCE(org_patient_thresholds.thresholds, org_settings.dose_trend_thresholds)` per D-08. INSERT INTO clinician_alerts(org_id, patient_user_id, alert_type, threshold_snapshot, debounce_key) SELECT ... ON CONFLICT (org_id, debounce_key) DO NOTHING. `debounce_key = format('%s:%s:%s', alert_type, patient_user_id, to_char(current_date, 'YYYY-MM-DD'))` per D-11. Adherence rule simplifies to "patient has < N injection-days in past M days" per RESEARCH Open Questions #1 (no prescribed_cadence_days column).
      • `p30_clinician_alert_deliver` schedule `'*/20 * * * *'` — net.http_post to `https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/clinician-alert-deliver-cron` (Edge Fn shipped Plan 30-01) with `Authorization: Bearer ` from `vault.decrypted_secrets where name = 'service_role_key'`, timeout 300000ms.
      • `p30_clinician_alert_auto_resolve` schedule `'15 4 * * *'` — pure SQL body: UPDATE clinician_alerts SET status='auto_resolved', auto_resolved_at=now() WHERE status='pending' AND snooze_until IS NULL AND created_at < now() - interval '7 days'; ALSO snoozed → pending: UPDATE clinician_alerts SET status='pending', snooze_until=null WHERE status='snoozed' AND snooze_until < now() (snooze-resume folded into auto-resolve per D-12).
      • `p30_clinic_matview_refresh` schedule `'2,17,32,47 * * * *'` — pure SQL body: `refresh materialized view concurrently public.mv_clinic_alert_metrics; refresh materialized view concurrently public.mv_clinic_dose_trend_population; insert into public.clinic_matview_refresh_log(view_name, refreshed_at) values ('mv_clinic_alert_metrics', now()), ('mv_clinic_dose_trend_population', now()) on conflict (view_name) do update set refreshed_at = excluded.refreshed_at;` (staggered from lifecycle-behavior-triggered :00/:15/:30/:45 per RESEARCH §D-17).

    Update `supabase/functions/_shared/with-org-scope.ts`: append 3 string literals to `ORG_SCOPED_TABLES` Set: `'clinician_alerts'`, `'clinician_alert_deliveries'`, `'org_patient_thresholds'`. Add comments `// P30: clinician_alerts`, etc. for audit traceability per EXTENSION-CONTRACT §5.
  </action>
  <verify>
    <automated>
      test -f supabase/migrations/20270601300003_p30_secdef_and_triggers.sql && \
      test -f supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      test -f supabase/migrations/20270601300005_p30_weighted_rank_org_patients.sql && \
      grep -q 'acknowledge_clinician_alert' supabase/migrations/20270601300003_p30_secdef_and_triggers.sql && \
      grep -q 'snooze_clinician_alert' supabase/migrations/20270601300003_p30_secdef_and_triggers.sql && \
      grep -q 'update_org_ranking_weights' supabase/migrations/20270601300003_p30_secdef_and_triggers.sql && \
      grep -q 'set_patient_dose_thresholds' supabase/migrations/20270601300003_p30_secdef_and_triggers.sql && \
      grep -q 'after update of ranking_weights' supabase/migrations/20270601300003_p30_secdef_and_triggers.sql && \
      grep -q 'pg_notify' supabase/migrations/20270601300003_p30_secdef_and_triggers.sql && \
      grep -q "set_config('app.suppress_audit'" supabase/migrations/20270601300003_p30_secdef_and_triggers.sql && \
      ! grep -q 'log_admin_action' supabase/migrations/20270601300003_p30_secdef_and_triggers.sql && \
      grep -q "search_path = pg_catalog, public, extensions" supabase/migrations/20270601300003_p30_secdef_and_triggers.sql && \
      grep -q 'mv_clinic_alert_metrics' supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      grep -q 'mv_clinic_dose_trend_population' supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      grep -q "create unique index" supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      grep -q "p30_clinician_alert_detect" supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      grep -q "p30_clinician_alert_deliver" supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      grep -q "p30_clinician_alert_auto_resolve" supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      grep -q "p30_clinic_matview_refresh" supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      grep -q "'30 3 \* \* \*'" supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      grep -q "'\*/20 \* \* \* \*'" supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      grep -q "'15 4 \* \* \*'" supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      grep -q "'2,17,32,47 \* \* \* \*'" supabase/migrations/20270601300004_p30_matviews_and_cron.sql && \
      grep -q 'v_weights jsonb' supabase/migrations/20270601300005_p30_weighted_rank_org_patients.sql && \
      grep -q 'select ranking_weights into v_weights' supabase/migrations/20270601300005_p30_weighted_rank_org_patients.sql && \
      grep -c "clinician_alerts\|clinician_alert_deliveries\|org_patient_thresholds" supabase/functions/_shared/with-org-scope.ts | awk '$1>=3 {exit 0} {exit 1}'
    </automated>
  </verify>
  <done>
    All 3 SQL migrations exist with required SECDEFs/trigger/matviews/crons; ORG_SCOPED_TABLES Set extended; uses `'staff'` not `'clinician'`; uses direct INSERT audit_logs with suppress_audit GUC (NOT log_admin_action); AFTER UPDATE (not BEFORE) trigger pattern; UNIQUE indexes precede matviews; pg_cron schedules match RESEARCH §D-17 resolved map.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 RLS proof tests + matview/debounce/auto-resolve/weighted-rank unit tests</name>
  <files>
    leanshot/src/lib/__tests__/rls-org-clinician-alerts.test.ts,
    leanshot/src/lib/__tests__/rls-org-patient-thresholds.test.ts,
    leanshot/src/lib/__tests__/rank-org-patients-weights.test.ts,
    leanshot/src/lib/__tests__/clinician-alert-debounce.test.ts,
    leanshot/src/lib/__tests__/clinician-alert-auto-resolve.test.ts,
    leanshot/src/lib/__tests__/mv-clinic-alert-metrics.test.ts
  </files>
  <read_first>
    - leanshot/src/lib/__tests__/_fixtures/p28-rls-fixture.ts (createTwoOrgsTwoUsers + ES256-compat session)
    - leanshot/src/lib/__tests__/rls-org-patient-invites.test.ts (Phase 29 reference for RLS test layout)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-EXTENSION-CONTRACT.md §3 (T3/T3b/T4/T5a/T5b test recipe)
    - [[reference_rls_fixture_gotrueclient_flake]] + [[feedback_rls_per_file_slug_prefix]]
  </read_first>
  <action>
    Use vitest 4.x with `vitest-e2e.config.ts` (RLS suite gated by `VITE_SUPABASE_URL` + service-role env via `SHOULD_RUN` from fixture; `describeIfLive` pattern).

    Each test file MUST start with `const TEST_SLUG_PREFIX = makeSlugPrefix(path.basename(__filename));` (file-scoped per [[feedback_rls_per_file_slug_prefix]]) and `afterAll(() => cleanupByPrefix(TEST_SLUG_PREFIX))`.

    `rls-org-clinician-alerts.test.ts` — 5 assertions per EXTENSION-CONTRACT §3b template: T3 (User A cannot SELECT Org Y alerts), T4 (User A cannot INSERT into Org Y alerts), T5a (User A cannot UPDATE Org Y alerts), T5b (User A cannot DELETE Org Y alerts), T3b (User A CAN SELECT Org X own alerts when seeded). Seed Org Y alert via service-role admin client `getAdmin().from('clinician_alerts').insert({ org_id: fixture.orgY, patient_user_id: fixture.patientY, alert_type: 'dose_adherence', threshold_snapshot: {missed_doses_n:2,window_days_m:14}, debounce_key: 'dose_adherence:'+fixture.patientY+':'+new Date().toISOString().slice(0,10) })`. Use existing fixture User B (Org Y staff member). Add a 6th assertion: User A (Org X admin) calls `rpc('acknowledge_clinician_alert', { p_alert_id: <seeded Org Y alert id> })` and expects error code `42501` (cross-tenant SECDEF re-check).

    `rls-org-patient-thresholds.test.ts` — Same 5 assertions for org_patient_thresholds table. Seed Org Y threshold via admin client `INSERT INTO org_patient_links(org_id, patient_user_id, ...)` first (FK requirement), then `INSERT INTO org_patient_thresholds(org_id, patient_user_id, thresholds, set_by)`. Add 6th: User A calls `rpc('set_patient_dose_thresholds', { p_org_id: orgY, p_patient_user_id: patientY, p_thresholds: {...} })` and expects `42501`.

    `rank-org-patients-weights.test.ts` — Behavior parity: (1) when `org_settings.ranking_weights IS NULL`, `rank_org_patients(orgX)` returns rows whose composite_score matches Phase 10 baseline (exact equality acceptable since we use COALESCE to existing hardcoded defaults). (2) when admin calls `update_org_ranking_weights(orgX, {dose_adherence:0.5, weight_loss:0.2, activity:0.2, symptoms:0.1})`, subsequent `rank_org_patients(orgX)` returns rows whose composite_score reflects new weights. (3) `update_org_ranking_weights(orgX, {dose_adherence:0.5,...sum=0.9})` raises `P0001` (validator trigger rejects sum ≠ 1.0±0.001).

    `clinician-alert-debounce.test.ts` — Insert two alerts via service-role admin client with identical `(org_id, debounce_key)`; assert second INSERT returns 0 rows affected (ON CONFLICT DO NOTHING) AND `select count(*) from clinician_alerts where debounce_key = $1` returns 1.

    `clinician-alert-auto-resolve.test.ts` — (1) Insert alert with `created_at = now() - interval '8 days'`, `status='pending'`, `snooze_until=null`; run `update clinician_alerts set status='auto_resolved', auto_resolved_at=now() where status='pending' and snooze_until is null and created_at < now() - interval '7 days'` (same SQL as cron body); assert status flipped. (2) Insert snoozed alert with `snooze_until = now() - interval '1 minute'`; run snooze-resume SQL; assert status flipped back to 'pending'.

    `mv-clinic-alert-metrics.test.ts` — Insert 4 alerts (2 pending, 2 acknowledged with varying ack_at offsets); execute `refresh materialized view public.mv_clinic_alert_metrics`; query `mv_clinic_alert_metrics where org_id = $1`; assert ack_rate_pct = 50, avg_time_to_ack_minutes within expected range. Second assertion: `refresh materialized view concurrently public.mv_clinic_alert_metrics` succeeds (UNIQUE index present per Pitfall 2).

    All tests gated by `describeIfLive = SHOULD_RUN ? describe : describe.skip` so they no-op in offline CI.
  </action>
  <verify>
    <automated>
      test -f leanshot/src/lib/__tests__/rls-org-clinician-alerts.test.ts && \
      test -f leanshot/src/lib/__tests__/rls-org-patient-thresholds.test.ts && \
      test -f leanshot/src/lib/__tests__/rank-org-patients-weights.test.ts && \
      test -f leanshot/src/lib/__tests__/clinician-alert-debounce.test.ts && \
      test -f leanshot/src/lib/__tests__/clinician-alert-auto-resolve.test.ts && \
      test -f leanshot/src/lib/__tests__/mv-clinic-alert-metrics.test.ts && \
      grep -l 'makeSlugPrefix(path.basename(__filename))' leanshot/src/lib/__tests__/rls-org-clinician-alerts.test.ts && \
      grep -l 'cleanupByPrefix' leanshot/src/lib/__tests__/rls-org-clinician-alerts.test.ts && \
      grep -l 'describeIfLive' leanshot/src/lib/__tests__/rls-org-clinician-alerts.test.ts && \
      cd leanshot && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -E 'src/lib/__tests__/(rls-org-clinician-alerts|rls-org-patient-thresholds|rank-org-patients-weights|clinician-alert-debounce|clinician-alert-auto-resolve|mv-clinic-alert-metrics)\.test\.ts' | wc -l | grep -q '^0$'
    </automated>
  </verify>
  <done>
    All 6 test files exist with file-scoped slug prefix + describeIfLive gating; tsc passes; tests are runnable via `npm run test:e2e:rls` (will execute when env present, skip cleanly otherwise).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: BLOCKING — copy migrations to main checkout, push schema, verify cron jobs registered</name>
  <files>
    supabase/migrations/20270601300001_p30_org_settings_extensions.sql,
    supabase/migrations/20270601300002_p30_clinician_alerts_schema.sql,
    supabase/migrations/20270601300003_p30_secdef_and_triggers.sql,
    supabase/migrations/20270601300004_p30_matviews_and_cron.sql,
    supabase/migrations/20270601300005_p30_weighted_rank_org_patients.sql
  </files>
  <read_first>
    - [[reference_worktree_supabase_push_leak]] (copy .sql to main checkout before push)
    - [[reference_supabase_keychain_token_extraction]] (SUPABASE_ACCESS_TOKEN extraction for non-TTY)
    - [[reference_supabase_worktree_temp_state]] (.temp state for worktree CLI)
    - supabase/.temp/ existence check
  </read_first>
  <what-built>
    Executor:
    (1) If this plan is running in a worktree, copy all 5 `20270601300*_*.sql` files from worktree to MAIN checkout (`/Users/karstenhaldan/minisite/supabase/migrations/`) per [[reference_worktree_supabase_push_leak]] — the Supabase CLI requires migrations to exist in the main project tree.
    (2) Extract `SUPABASE_ACCESS_TOKEN` from macOS keychain per [[reference_supabase_keychain_token_extraction]] OR use ambient environment if already present.
    (3) `cd /Users/karstenhaldan/minisite && SUPABASE_ACCESS_TOKEN=$TOKEN npx supabase db push --linked --include-all 2>&1 | tee /tmp/p30-push.log`. Grep `/tmp/p30-push.log` for `^Skipping` lines per [[reference_supabase_migration_filename_regex]] — if ANY skip, halt and report (regex violation).
    (4) Verify schema live: `supabase db query --linked "select column_name from information_schema.columns where table_schema='public' and table_name='org_settings' and column_name in ('ranking_weights','dose_trend_thresholds');" --output json` returns both. `supabase db query --linked "select tablename from pg_tables where schemaname='public' and tablename in ('clinician_alerts','clinician_alert_deliveries','org_patient_thresholds','clinic_matview_refresh_log');" --output json` returns 4. `supabase db query --linked "select jobname from cron.job where jobname like 'p30_%';" --output json` returns 4 rows.
    (5) Commit migrations + ORG_SCOPED_TABLES update + test files on the agent branch (do NOT push the worktree branch yet — Wave 1 will rebase/merge). Use explicit pathspec `git add` per [[feedback_parallel_executor_git_isolation]] to avoid sibling-plan file capture.
  </what-built>
  <how-to-verify>
    Human verifies the push succeeded and live schema matches expectations:
    1. Confirm `/tmp/p30-push.log` shows `Pushed 5 migrations` (or equivalent success line) with no `Skipping` warnings.
    2. Confirm output of step (4) shows the 2 new columns + 4 new tables + 4 cron jobnames (`p30_clinician_alert_detect`, `p30_clinician_alert_deliver`, `p30_clinician_alert_auto_resolve`, `p30_clinic_matview_refresh`).
    3. Confirm a hot-spot SELECT works: `supabase db query --linked "select count(*) from public.clinician_alerts;"` returns `0` (table exists, queryable, no data yet).
    4. Confirm the validator trigger fires: `supabase db query --linked "begin; update public.org_settings set ranking_weights = '{\"dose_adherence\":0.5,\"weight_loss\":0.3,\"activity\":0.1,\"symptoms\":0.05}'::jsonb where org_id = (select id from public.organizations limit 1); rollback;"` — must raise exception with errcode P0001 ("must sum to 1.0").
  </how-to-verify>
  <resume-signal>
    Type "schema-live" or describe issues (e.g., "validator trigger did not fire" / "cron jobs missing" / "skip warning in push log").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→SECDEF | Authenticated browser session calls `update_org_ranking_weights` / `acknowledge_clinician_alert` / `snooze_clinician_alert` / `set_patient_dose_thresholds` — untrusted org_id parameter must be re-validated against caller's JWT org_ids claim and `org_members.role` |
| client→RLS | Authenticated session direct-reads `clinician_alerts` / `org_patient_thresholds` — RLS predicate filters by `org_member` JOIN; cross-tenant must return 0 rows |
| service_role→tables | Detect-cron + deliver-cron + matview-refresh-cron write/read via service_role — `withOrgScope` Proxy is the 4th layer of defense; runtime assertion + Sentry fatal alert on bypass |
| trigger→Realtime | AFTER UPDATE trigger fires pg_notify; Realtime forwards as `postgres_changes`; HMAC channel auth (Phase 28 `realtime_topic_authorized`) gates subscribe — sender side is implicitly trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-00-01 | Tampering | clinician_alerts.status writes from non-SECDEF path | mitigate | No INSERT/UPDATE/DELETE policy on `authenticated` role — all transitions go through the 4 named SECDEFs OR detect/deliver crons (service_role) |
| T-30-00-02 | Information disclosure | Cross-tenant read of clinician_alerts via client | mitigate | Forced RLS + SELECT policy gates on `org_member.role in ('admin','staff')` for same org_id; paired RLS test (Task 3) proves Org A→Org B read returns 0 rows |
| T-30-00-03 | Information disclosure | Cross-tenant SECDEF call (User A targets Org Y alert ID) | mitigate | SECDEFs re-check `org_members.role` for alert.org_id (NOT just the alert id) — Task 3 6th assertion proves 42501 raised |
| T-30-00-04 | Tampering | ranking_weights shape corruption (sum ≠ 1.0; missing key) | mitigate | `_validate_ranking_weights` validator + BEFORE INSERT/UPDATE trigger raises P0001 — Task 3 weighted-rank parity test asserts rejection |
| T-30-00-05 | Repudiation | Clinician acks/snoozes without audit trail | mitigate | All 4 SECDEFs write `audit_logs` row via direct INSERT + `app.suppress_audit='on'` GUC (Phase 29 verified pattern) |
| T-30-00-06 | Elevation of privilege | SECDEF caller manipulates search_path to redirect functions | mitigate | All SECDEFs explicit `set search_path = pg_catalog, public, extensions` per [[reference_supabase_migration_gotchas]] |
| T-30-00-07 | Denial of service | matview REFRESH CONCURRENTLY fails first run | mitigate | UNIQUE index in same migration as CREATE MATERIALIZED VIEW (Pitfall 2); initial REFRESH non-concurrently in migration body |
| T-30-00-08 | Tampering | Trigger emits notify on uncommitted transaction | mitigate | AFTER UPDATE (not BEFORE) per RESEARCH §Pattern 1 correction — notify fires only on commit |
| T-30-00-09 | Repudiation | Audit trigger infinite recursion on audit_logs INSERT inside SECDEF | mitigate | `perform set_config('app.suppress_audit', 'on', true);` before every direct audit insert (Pitfall 7) |
| T-30-00-10 | Information disclosure | clinician_alert_deliveries shows error text containing PHI | accept | error field captures Resend API response (status codes, network errors); upstream alert template is PHI-stripped per D-02. Residual risk low; v1.4 may scrub error text. |
</threat_model>

<verification>
After all 4 tasks complete and schema is pushed:
- `supabase db query --linked "select tablename from pg_tables where schemaname='public' and tablename in ('clinician_alerts','clinician_alert_deliveries','org_patient_thresholds','clinic_matview_refresh_log');"` returns 4 rows
- `supabase db query --linked "select jobname, schedule from cron.job where jobname like 'p30_%' order by jobname;"` returns 4 jobs at scheduled times (`'30 3 * * *'`, `'*/20 * * * *'`, `'15 4 * * *'`, `'2,17,32,47 * * * *'`)
- `cd leanshot && npm run test:e2e:rls -- src/lib/__tests__/rls-org-clinician-alerts.test.ts` passes (or skips cleanly with `SHOULD_RUN=false`)
- `cd leanshot && npm run test:e2e:rls -- src/lib/__tests__/rank-org-patients-weights.test.ts` passes parity assertion (NULL fallback returns Phase 10 baseline scores)
- `grep -c "clinician_alerts\\|clinician_alert_deliveries\\|org_patient_thresholds" supabase/functions/_shared/with-org-scope.ts` returns ≥3
</verification>

<success_criteria>
- 5 migrations created with strict 14-digit filename + `begin;...commit;` wrappers; no letter suffix
- All 3 new org-scoped tables follow EXTENSION-CONTRACT R1-R5 (paired RLS test + ORG_SCOPED_TABLES entry + `on delete restrict` + `org_*` naming + SECDEF search_path locked)
- Status-machine ownership (D-10) honored: 4 SECDEFs + 2 cron transitions (auto-resolve, snooze-resume) named in migrations
- AFTER UPDATE trigger (not BEFORE) on `ranking_weights` emits pg_notify per RESEARCH §Pattern 1 correction
- `'staff'` enum value used throughout RLS + SECDEF role-gates (NOT `'clinician'`)
- Phase 10 `rank_org_patients` NULL-fallback behavior preserved (parity test green)
- pg_cron schedules per RESEARCH §D-17 resolved map (no collisions)
- Schema pushed to `ytnsipxxmzgaebkqmokp` via `supabase db push --linked` with zero skipped migrations
- ORG_SCOPED_TABLES Set in `_shared/with-org-scope.ts` includes the 3 new tables
- 6 vitest files exist + typecheck clean; runnable via `npm run test:e2e:rls`
</success_criteria>

<output>
After completion, create `.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-00-SUMMARY.md` documenting: migration filenames + push timestamp + cron job names + ORG_SCOPED_TABLES diff + Phase 28 BLOCKER R1-R5 compliance check + any deviation from RESEARCH-recommended SQL (e.g., if `vials.medication_name` column did not exist and a substitution was made).
</output>
