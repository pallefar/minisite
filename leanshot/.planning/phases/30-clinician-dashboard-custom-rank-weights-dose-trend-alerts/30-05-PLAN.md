---
phase: 30
plan: 05
type: execute
wave: 2
depends_on: ["30-00", "30-01", "30-02", "30-03", "30-04"]
files_modified:
  - leanshot/e2e/clinic-ranking-weights-roster-reorder.spec.ts
  - leanshot/e2e/clinician-alerts-realtime.spec.ts
  - leanshot/playwright.config.ts
  - leanshot/scripts/assert-clinic-bundle-budget.sh
autonomous: false
requirements: [CLIN-01, CLIN-03, CLIN-06]
user_setup: []

must_haves:
  truths:
    - "(SC#1) Playwright e2e proves: admin saves ranking weights in tab A → org-{hmac8}-settings HMAC broadcast → RosterTable in tab B refetches rank_org_patients with new weights → row order changes within 1 second"
    - "(SC#3) Playwright e2e proves: detect-cron INSERT into clinician_alerts triggers deliver-cron → realtime broadcast on org-{hmac8}-alerts → ClinicianAlertsPanel in clinician's browser session appends new alert within 5 seconds; per [[feedback_realtime_layer_e2e_pattern]] the receiving operator's `supabase.channel(...).subscribe()` is instantiated DIRECTLY in the test file (DB-level invariant verification beats UI traversal)"
    - "Both Playwright specs use `page.addInitScript` for state seeding per [[reference_playwright_state_seeding]] — NEVER page.goto + page.evaluate (races INITIAL_SESSION)"
    - "Conditional project gate per [[reference_playwright_conditional_project_argv]]: env var `PLAYWRIGHT_RUN_P30=1` enables P30 specs (CLI argv check fails in worker subprocesses)"
    - "Deliver-cron Edge Function deployed: `supabase functions deploy clinician-alert-deliver-cron --no-verify-jwt --project-ref ytnsipxxmzgaebkqmokp` — Bearer service-role token gates cron-only access (no public JWT verify needed since pg_cron supplies the token from vault)"
    - "Bundle ceiling verified: `bash leanshot/scripts/assert-clinic-bundle-budget.sh` exits 0 with the 6 new Phase 30 components inside the clinic chunk; admin-shell stays under 45 kB raised ceiling (Plan 29-06 baseline)"
    - "All Phase 30 RLS test files (Plan 30-00 Task 3) execute green against live Supabase: `cd leanshot && PLAYWRIGHT_RUN_P30=1 npm run test:e2e:rls -- src/lib/__tests__/rls-org-clinician-alerts.test.ts src/lib/__tests__/rls-org-patient-thresholds.test.ts src/lib/__tests__/rank-org-patients-weights.test.ts` passes"
  artifacts:
    - path: "leanshot/e2e/clinic-ranking-weights-roster-reorder.spec.ts"
      provides: "SC#1 e2e — weight save in tab A triggers reorder in tab B within 1s"
      contains: "channelNameFor"
    - path: "leanshot/e2e/clinician-alerts-realtime.spec.ts"
      provides: "SC#3 e2e — alert INSERT → broadcast → panel renders within 5s; receiving channel.subscribe() instantiated in test file"
      contains: "channelNameFor"
    - path: "leanshot/playwright.config.ts"
      provides: "PLAYWRIGHT_RUN_P30 env-var conditional project per [[reference_playwright_conditional_project_argv]]"
      contains: "PLAYWRIGHT_RUN_P30"
  key_links:
    - from: "leanshot/e2e/clinic-ranking-weights-roster-reorder.spec.ts"
      to: "leanshot/src/components/clinic/settings/ClinicRankingWeightsForm.tsx"
      via: "page.getByText('Save ranking weights').click()"
      pattern: "Save ranking weights"
    - from: "leanshot/e2e/clinician-alerts-realtime.spec.ts"
      to: "leanshot/src/lib/org-realtime.ts"
      via: "test instantiates channelNameFor(orgId, 'alerts') + supabase.channel(name).subscribe() directly"
      pattern: "channelNameFor"
    - from: "leanshot/scripts/assert-clinic-bundle-budget.sh"
      to: "leanshot/dist/assets/"
      via: "post-build chunk size enforcement; Phase 30 components inside clinic-shell chunk"
      pattern: "clinic-shell"
---

<objective>
End-of-phase validation: 2 Playwright e2e specs proving the two realtime success criteria (SC#1 roster 1s reorder + SC#3 alerts 5s notification), Edge Function deploy, bundle ceiling re-assertion with the 6 new Phase 30 components present, RLS suite execution against live Supabase, and phase close.

Purpose: Convert the locked Phase 30 schema + UI + Edge Fn into proven, end-to-end-validated behavior. Phase 30 cannot be marked complete without these e2e proofs.

Output: 2 Playwright specs + playwright.config.ts conditional project + Edge Fn deployment + bundle assertion + phase SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-CONTEXT.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-RESEARCH.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-UI-SPEC.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-00-PLAN.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-01-PLAN.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-02-PLAN.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-03-PLAN.md
@.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-04-PLAN.md
@leanshot/playwright.config.ts
@leanshot/scripts/assert-clinic-bundle-budget.sh
@leanshot/e2e/admin-mfa-enroll.spec.ts

<interfaces>
From leanshot/playwright.config.ts:
  Conditional project pattern (existing for `aso`):
    process.env.PLAYWRIGHT_RUN_ASO === '1' → conditional project enabled
  Mirror for P30: `PLAYWRIGHT_RUN_P30 === '1'` → conditional project named 'p30' that owns the 2 new specs.

From leanshot/src/lib/org-realtime.ts:
  export async function channelNameFor(orgId: string, suffix: string): Promise<string>

Playwright state seeding pattern (per [[reference_playwright_state_seeding]]):
  page.addInitScript(() => { window.localStorage.setItem('sb-...-auth-token', JSON.stringify(seedToken)); })

Realtime e2e pattern (per [[feedback_realtime_layer_e2e_pattern]]):
  In the test file: import { createClient } from '@supabase/supabase-js'; const sb = createClient(...); const ch = sb.channel(await channelNameFor(orgId, 'alerts')); const received = new Promise(r => ch.on('broadcast', {event: 'new_alert'}, p => r(p))).then(...); ch.subscribe();
  Then trigger via service-role admin client: insert clinician_alert row directly OR call detect-cron Edge Fn manually OR call deliver-cron HTTP endpoint.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright e2e — SC#1 roster reorder + SC#3 alert realtime panel render</name>
  <files>
    leanshot/e2e/clinic-ranking-weights-roster-reorder.spec.ts,
    leanshot/e2e/clinician-alerts-realtime.spec.ts,
    leanshot/playwright.config.ts
  </files>
  <read_first>
    - leanshot/playwright.config.ts (existing aso conditional project to mirror)
    - leanshot/e2e/admin-mfa-enroll.spec.ts (existing addInitScript seeding pattern)
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-CONTEXT.md §"Realtime e2e pattern" [[feedback_realtime_layer_e2e_pattern]]
    - .planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-RESEARCH.md §"Realtime e2e Pattern"
    - [[reference_playwright_state_seeding]] + [[reference_playwright_conditional_project_argv]]
    - [[reference_rls_fixture_gotrueclient_flake]] (ES256-compat session for any auth setup)
  </read_first>
  <action>
    Update `playwright.config.ts`: mirror the existing `aso` conditional project pattern. Add `process.env.PLAYWRIGHT_RUN_P30 === '1' || /* CLI argv check OPTIONAL — env var is the source of truth */ false`. Define a new conditional `projects` entry named `'p30'` with `testMatch: ['e2e/clinic-ranking-weights-roster-reorder.spec.ts', 'e2e/clinician-alerts-realtime.spec.ts']`. The default project (chrome) should EXCLUDE these specs via `testIgnore: ['e2e/clinic-ranking-weights-roster-reorder.spec.ts', 'e2e/clinician-alerts-realtime.spec.ts']` so they only run under the conditional project.

    Write `clinic-ranking-weights-roster-reorder.spec.ts` per RESEARCH §"Realtime e2e Pattern":
    - Use 2 browser contexts (`browser.newContext()` × 2) — context A for the admin saving weights, context B for the admin viewing roster.
    - In each context: `page.addInitScript` to seed Supabase auth localStorage token for an org-admin in TEST_ORG. Use the Plan 30-00 fixture pattern (admin.generateLink + /auth/v1/verify) to mint the seed token before launching contexts.
    - Context B: `await pageB.goto('/clinic/{slug}/roster')`. Wait for RosterTable rows to render. Capture initial row order via `await pageB.locator('[data-testid="roster-row"]').evaluateAll(els => els.map(el => el.getAttribute('data-patient-id')))` (verify the actual testid attribute used in RosterRow.tsx — if missing, add `data-testid="roster-row" data-patient-id={patient.id}` in Plan 30-02 wiring; coordinate via SUMMARY).
    - Context A: `await pageA.goto('/clinic/{slug}/settings')` → click 'Clinical' tab → fill weight inputs with values that flip the dominant signal (e.g., dose_adherence=80, weight_loss=10, activity=5, symptoms=5) → click `'Save ranking weights'`.
    - Capture `tStart = Date.now()` at click time.
    - In context B: `await pageB.waitForFunction((initialOrder) => { const newOrder = Array.from(document.querySelectorAll('[data-testid="roster-row"]')).map(el => el.getAttribute('data-patient-id')); return JSON.stringify(newOrder) !== JSON.stringify(initialOrder); }, initialOrder, { timeout: 5000 })` — assert reorder happens. Then assert `Date.now() - tStart < 1000` for SC#1 (within 1 second). Allow ±200ms tolerance for test infra noise.
    - Cleanup: revert weights to equal (avoid polluting next run).

    Write `clinician-alerts-realtime.spec.ts` per [[feedback_realtime_layer_e2e_pattern]] — DB-level invariant verification:
    - Setup: ES256 fixture mints a clinician (org_member.role='staff') in TEST_ORG. Service-role admin client available.
    - In the TEST file itself (NOT in the browser): instantiate `import { createClient } from '@supabase/supabase-js'; const subClient = createClient(SUPABASE_URL, SUPABASE_ANON_KEY); const channelName = await channelNameFor(TEST_ORG_ID, 'alerts'); const channel = subClient.channel(channelName); const receivedAlert = new Promise<{alert_id: string}>(resolve => channel.on('broadcast', { event: 'new_alert' }, msg => resolve(msg.payload))); await channel.subscribe();` — set client's auth via `subClient.auth.setSession({access_token: clinicianJwt, refresh_token: ''})` before subscribing.
    - Trigger: service-role admin client INSERT into clinician_alerts (mimics detect-cron output). Then HTTP POST to deliver-cron Edge Fn URL with service-role Bearer (mimics pg_cron's `*/20` invocation).
    - Assert: `await expect(receivedAlert).resolves.toMatchObject({ alert_id: expect.any(String), alert_type: 'dose_adherence' })` within `{ timeout: 5000 }` for SC#3.
    - ALSO open a browser context with the clinician's seeded session, navigate to a clinic route → click bell icon → assert the alert row renders in ClinicianAlertsPanel within the same 5s window (UI proof complementing DB-level proof).
    - Cleanup: service-role DELETE the test alert + delivery rows by `debounce_key LIKE 'test:%'`.

    Both specs use file-scoped slug prefix (per project pattern) — e.g., `const TEST_SLUG = 'p30-e2e-' + Date.now().toString(36)` — to avoid cross-test data contamination.
  </action>
  <verify>
    <automated>
      test -f leanshot/e2e/clinic-ranking-weights-roster-reorder.spec.ts && \
      test -f leanshot/e2e/clinician-alerts-realtime.spec.ts && \
      grep -q 'PLAYWRIGHT_RUN_P30' leanshot/playwright.config.ts && \
      grep -q 'addInitScript' leanshot/e2e/clinic-ranking-weights-roster-reorder.spec.ts && \
      grep -q 'channelNameFor' leanshot/e2e/clinician-alerts-realtime.spec.ts && \
      grep -q 'channel.subscribe\|.channel(.*).subscribe' leanshot/e2e/clinician-alerts-realtime.spec.ts && \
      cd leanshot && PLAYWRIGHT_RUN_P30=1 npx playwright test --list e2e/clinic-ranking-weights-roster-reorder.spec.ts e2e/clinician-alerts-realtime.spec.ts 2>&1 | tail -5 | grep -qE "[0-9]+ tests in 2 files"
    </automated>
  </verify>
  <done>
    Both specs exist + listed by Playwright under PLAYWRIGHT_RUN_P30=1; specs use addInitScript + DB-level channel.subscribe pattern; default project excludes these specs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy deliver-cron Edge Function + run full e2e + bundle assertion + RLS suite</name>
  <files>
    leanshot/scripts/assert-clinic-bundle-budget.sh
  </files>
  <read_first>
    - leanshot/scripts/assert-clinic-bundle-budget.sh (existing ceiling enforcement; verify Phase 30 components fit clinic-shell budget)
    - [[reference_supabase_edge_function_deploy]] (deploy command + esm.sh imports)
    - [[reference_bundle_budget_hash_hyphen]] (script hash-hyphen bug — already fixed Plan 10-11; verify still working)
  </read_first>
  <what-built>
    Executor:
    (1) Verify Plan 30-01 Edge Fn deno tests pass: `cd /Users/karstenhaldan/minisite/supabase/functions/clinician-alert-deliver-cron && deno test --no-check` → must exit 0.
    (2) Deploy: `SUPABASE_ACCESS_TOKEN=$TOKEN npx supabase functions deploy clinician-alert-deliver-cron --no-verify-jwt --project-ref ytnsipxxmzgaebkqmokp` (project ref from [[reference_supabase_project]]). `--no-verify-jwt` because pg_cron supplies the service-role Bearer token from vault (not a user JWT). Capture deploy URL from output.
    (3) UAT-probe per [[reference_supabase_edge_function_deploy]]: `curl -X POST <deploy-url> -H "Authorization: Bearer $SERVICE_ROLE_KEY"` → expect 200 with body `{"ok":true,"processed":0}` (no pending alerts in fresh state).
    (4) Bundle: `cd /Users/karstenhaldan/minisite/leanshot && npm run build && bash scripts/assert-clinic-bundle-budget.sh` → exit 0; chunk report shows clinic-shell under 45 kB raised ceiling.
    (5) RLS suite live: `cd /Users/karstenhaldan/minisite/leanshot && npm run test:e2e:rls -- src/lib/__tests__/rls-org-clinician-alerts.test.ts src/lib/__tests__/rls-org-patient-thresholds.test.ts src/lib/__tests__/rank-org-patients-weights.test.ts src/lib/__tests__/clinician-alert-debounce.test.ts src/lib/__tests__/clinician-alert-auto-resolve.test.ts src/lib/__tests__/mv-clinic-alert-metrics.test.ts` → all pass against live Supabase (SUPABASE_URL + SERVICE_ROLE_KEY env present).
    (6) Playwright e2e: `cd /Users/karstenhaldan/minisite/leanshot && PLAYWRIGHT_RUN_P30=1 npx playwright test --project=p30` → both specs pass.
    (7) Verify cron jobs are active: `supabase db query --linked "select jobname, schedule, active from cron.job where jobname like 'p30_%' order by jobname;"` → 4 rows all `active=true`.
    (8) Commit + write `30-05-SUMMARY.md` documenting deploy URL, all test outputs, bundle delta, and link to phase-close SUMMARY.

    Also write the phase-close `30-PHASE-SUMMARY.md` (across all 6 plans) listing:
    - All 8 REQ-IDs (CLIN-01..08) marked complete with → which plan(s) addressed them
    - D-01..D-18 + A1 decision coverage map (which plan implements each)
    - Cron schedule final map (4 P30 jobs + no collisions with pre-existing 18 jobs)
    - Status machine ownership table (D-10) — 5 transitions named
    - Bundle ceiling final numbers
    - Outstanding deferred items (none expected — D-01 swap-in is Phase 25 close, deliberately deferred)
  </what-built>
  <how-to-verify>
    Human verifies:
    1. `/tmp/supabase-functions-deploy.log` (or stdout) shows `Deployed Function clinician-alert-deliver-cron`.
    2. UAT-probe curl returns 200 + `{"ok":true,"processed":0}`.
    3. `scripts/assert-clinic-bundle-budget.sh` final stdout: `OK: clinic-shell {N} kB < 45 kB` (no `BLOCK` or `wave-0 skip` per [[reference_bundle_budget_hash_hyphen]]).
    4. RLS test suite output: `Test Files 6 passed (6)`.
    5. Playwright output: `2 passed (X.Xs)` under p30 project.
    6. cron.job query returns 4 rows with `active=true`.
    7. `30-PHASE-SUMMARY.md` exists with all 8 REQs marked complete + decision coverage map intact.
  </how-to-verify>
  <resume-signal>
    Type "phase-30-shipped" or describe issues (e.g., "deploy failed: secret missing" / "bundle over ceiling: X kB" / "Playwright SC#1 timed out at 1.4s").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI→live Supabase | RLS suite runs with service-role key (env var); writes test data scoped by file-prefix slug; cleanup in afterAll |
| Playwright→live deliver-cron | Test directly HTTP POSTs the deployed Edge Fn URL with service-role Bearer; mimics pg_cron behavior |
| Edge Fn deploy→production | Anyone with SUPABASE_ACCESS_TOKEN can deploy; mitigation = token rotation per [[reference_supabase_keychain_token_extraction]] (developer-machine only; not in CI by default) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05-01 | Information disclosure | Test data leaks into production (real org data co-located) | mitigate | File-scoped slug prefix per [[feedback_rls_per_file_slug_prefix]]; cleanupByPrefix in afterAll; only test users + test orgs touched |
| T-30-05-02 | Tampering | Bundle assertion script false-passes due to hash-hyphen bug | mitigate | Plan 10-11 already fixed the bug; Task 2 verify step asserts no `wave-0 skip` lines in script output |
| T-30-05-03 | Denial of service | Playwright test pollutes cron-job state (deliver-cron triggered manually during test) | accept | Manual HTTP POST during test is read-only against pending alerts (test creates its own); side-effect = retry_count bump on test alert which is cleaned up; production cron continues normally |
| T-30-05-04 | Spoofing | UAT-probe with service-role token executed in wrong project | mitigate | `--project-ref ytnsipxxmzgaebkqmokp` explicit; SUPABASE_URL hardcoded in test config matches the project |
</threat_model>

<verification>
- All 8 CLIN REQ-IDs marked complete in 30-PHASE-SUMMARY.md
- All D-01..D-18 + A1 decisions implementation-mapped
- 6 RLS/unit/matview test files green against live
- 2 Playwright specs green under PLAYWRIGHT_RUN_P30=1
- Edge Fn live + UAT-probe returns 200
- 4 pg_cron jobs active=true
- Bundle ceiling re-asserted (clinic-shell <45 kB)
- ROADMAP.md Phase 30 entry updated to complete
</verification>

<success_criteria>
- SC#1 e2e proves roster reorder within 1s (with tolerance)
- SC#3 e2e proves alert appears in panel within 5s (DB-level channel.subscribe + UI proof)
- SC#4 (debounce + retry) covered by Plan 30-00 unit tests + Plan 30-01 deno tests (no e2e needed — DB-level invariants)
- SC#5 (matview refresh + dashboard surfacing) covered by Plan 30-04 + Plan 30-00 mv-clinic-alert-metrics test
- All Phase 30 schema live in production; all Edge Fn deployed; all cron jobs registered + active
- Phase SUMMARY committed; ROADMAP entry updated
</success_criteria>

<output>
After completion, create:
- `.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-05-SUMMARY.md` — this plan's outputs (deploy URL, test commands + results, bundle delta)
- `.planning/phases/30-clinician-dashboard-custom-rank-weights-dose-trend-alerts/30-PHASE-SUMMARY.md` — full phase close: 8 REQs ✓, 18 decisions mapped to plans, cron table, status-machine owners, vendor pass list (RESEND_API_KEY verified), deferred items (D-01 swap-in to Phase 25 close)
- Update `.planning/STATE.md`: mark Phase 30 complete + bump v1.3 progress counter
</output>
