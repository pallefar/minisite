---
phase: 60
slug: rag-knowledge-base-completion-waves-2-4
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-26
---

# Phase 60 — UI Design Contract

> Visual and interaction contract for Phase 60: RAG Knowledge Base Completion (Waves 2-4).
> Generated by gsd-ui-researcher. Consumed by gsd-ui-checker, gsd-planner, gsd-executor, gsd-ui-auditor.
>
> **Source pre-population:** Most fields derived from upstream artifacts. Per-field source noted.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none — LeanShot DS v2 (Tailwind v4 `@theme` only) | CLAUDE.md |
| Preset | not applicable | codebase scan |
| Component library | radix-ui (via existing Modal/Sheet/Popover primitives in `src/components/ui/`) | codebase scan |
| Icon library | lucide-react ^0.460.0 | CLAUDE.md + package.json |
| Font (sans) | Geist (-apple-system fallback chain) | `src/index.css` `--font-sans` |
| Font (display) | Fraunces (display/serif headings only — ONE usage per surface per Phase 50-09 §C2 constraint) | `src/index.css` `--font-display` |
| Font (mono) | Geist Mono | `src/index.css` `--font-mono` |

**Note: No shadcn gate executed.** This project does not use shadcn. All components are LeanShot DS primitives in `src/components/ui/`. No `components.json` exists. Registry safety gate is not applicable.

---

## Spacing Scale

Declared values — all multiples of 4. Source: `src/index.css` `--spacing: 0.25rem` (4px base unit).

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline chip padding, `[N]` citation marker hitbox padding |
| sm | 8px | Compact element spacing, pill row gaps, citation popover internal padding |
| md | 16px | Default element spacing, card body padding, form field spacing |
| lg | 24px | Section padding, card header height |
| xl | 32px | Layout gaps, between bento card rows |
| 2xl | 48px | Major section breaks, page-level hero strips |
| 3xl | 64px | Page-level vertical spacing (public `/knowledge/*` pages) |

Exceptions:
- Citation `[N]` superscript tap target: invisible padded hitbox yields ≥24px tap area even though the visible badge is 14px — use `p-[5px]` (padding beyond the 4-scale; acceptable for touch-target compliance only). Alternative `p-1.5` (6px) achieves the same tap target and is closer to the 4-scale — executor may substitute if preferred.
- Admin queue two-column split: `lg:grid-cols-[7fr_5fr]` — column ratio is not a spacing token; this is a layout fraction.

---

## Typography

**Hard ceiling: ONLY 4 sizes permitted (11 / 13 / 18 / 28 px). ONLY 2 weights permitted (400 regular + 600 semibold).** Phase 69 will CI-gate this. Phase 60 MUST pre-comply.

Mapping to LeanShot DS tokens (`src/index.css`):
- 11px → `text-micro` (`--text-micro: 0.6875rem`)
- 13px → `text-sm` (`--text-sm: 0.8125rem`)
- 18px → `text-lg` (`--text-lg: 1.125rem`)
- 28px → `text-heading` (`--text-heading: 1.75rem`) — **NEW token required in `src/index.css` `@theme` block**

> 28px token: No existing `@theme` token maps to exactly 28px (`text-2xl` = 26px, `text-3xl` = 32px). Executor MUST add `--text-heading: 1.75rem` and `--text-heading--line-height: 1.2` to the `@theme` block in `src/index.css`. This token is used exclusively for the public `/knowledge` H1 display heading (Fraunces italic). Do NOT reuse it for other elements; that would break the 4-size ceiling.

| Role | Size (px) | Tailwind Token | Weight | Line Height | Usage |
|------|-----------|---------------|--------|-------------|-------|
| Display heading | 28px | `text-heading` | 600 | 1.2 | Public `/knowledge` root H1, `/knowledge/<topic>` index H1 — Fraunces italic, font-display ONLY |
| Heading | 18px | `text-lg` | 600 | 1.5 | Card headings, section titles, chunk titles in popover, featured chunk title, newsletter section headings, primary CTAs, `/knowledge/<topic>/<slug>` detail page H1 (font-sans) |
| Body / label | 13px | `text-sm` | 400 | 1.5 | Card body text, chunk text in queue and popover, coach reply text, eyebrow labels, source meta strips, freshness timestamps, form field labels, queue list item summaries, breadcrumb, related chunk titles, article grid copy, newsletter blurb, inline body anywhere 16px was previously used |
| Caption / micro | 11px | `text-micro` | 400 | 1.4 | Disclaimer copy ("Not medical advice"), tier badge labels in tiny contexts, `<kbd>` shortcut chips, eyebrow labels when uppercase-small, cost dashboard reset countdown, freshness strip dates |

Semibold (600) reserved for: headings, primary CTA button labels, tier badge text in popover, chunk title in citation popover, `[N]` citation marker number, display heading (28px).

Monospace (`font-mono`): "As of YYYY-MM-DD" freshness dates in popovers and cost dashboard numbers. Weight 400 only. Size 11px (`text-micro`) for freshness strips; 18px (`text-lg`) for large spend figures.

Fraunces exception: public `/knowledge` root page and `/knowledge/<topic>` index page H1 renders in `font-display` (Fraunces italic) at `text-heading` (28px) weight 600. The `/knowledge/<topic>/<slug>` detail page H1 uses `text-lg` (18px) font-sans (NOT Fraunces — Fraunces is index/root-level only). This is the ONLY Fraunces usage on these pages — no other elements may use it. Source: 50-09 §C2 constraint.

---

## Color

All colors are Tailwind v4 `@theme` tokens from `src/index.css`. No ad-hoc hex values. No new `@theme` color blocks.

| Role | Token | Resolved Value (light) | Usage |
|------|-------|----------------------|-------|
| Dominant (60%) | `bg-bg` | `#f2ede0` (cream-100) | Page backgrounds for public hub, admin pages, settings |
| Secondary (30%) | `bg-surface` | `#fefcf7` (cream-card) | Cards, queue rows, popover backgrounds, newsletter card shells |
| Accent (10%) | `text-primary` / `bg-primary` | `#1b4842` (teal-700) | Reserved-list below |
| Elevated surface | `bg-surface-elevated` | `#f6f2e8` | Tip-of-day Bento card (`variant="elevated"`) |
| Soft surface | `bg-surface-soft` | `#ebe7dc` | QueueDetailPane source-text background, side-by-side diff panel |
| Destructive | `text-danger` / `bg-danger` | `#cf5454` (clay) | Reject button in queue, RetractChunkModal confirm, danger Pills (Factually wrong / Off-label / Safety concern) |
| Warning | `text-warning` | `#e37748` (orange) | Freshness "Last reviewed" warning pill (>2yr stale), auto-pause banner |
| Info / link | `text-info` | `#1b4842` (teal-700) | Citation `[N]` badge tone, "Open source" external link |

**Accent reserved for (explicit list — never general interactive):**
1. Citation `[N]` marker badge background (`tone="info"`)
2. "Open source ↗" ghost button text on citation popover and tip card
3. "Approve chunk" primary button in admin queue
4. Active filter pill selected state in admin queue and `/knowledge/*` hub
5. Primary CTA buttons ("Approve chunk", "Save preferences", "Subscribe to digest", "Acknowledge and resume")
6. Focus ring on interactive elements (native ring color)

**Source tier badge coloring — neutral palette only (NO traffic-light coloring):**
- Tier A (FDA label / NLM / authoritative external): `bg-surface-elevated` + `text-text` + `font-semibold` — neutral high-contrast, NOT green.
- Tier B (peer-reviewed / clinical guideline): `bg-surface-soft` + `text-text-secondary` — neutral medium.
- Tier C (community / lay press): `bg-cream-200` + `text-text-tertiary` — neutral low-emphasis.
- `leanshot_research` source type: `bg-amber-soft` + `text-amber` text label "LeanShot Research (k≥5)" — visually distinct from external sources; amber communicates "internal evidence, distinct from peer review" without implying endorsement or concern.

**Medical safety rationale:** Green = "safe/approved" is a health-UI anti-pattern that implies clinical endorsement. Neutral hierarchy (contrast only) is required by health-literacy standards.

**Dark mode:** Every surface must render correctly under `[data-theme="dark"]`. Use only semantic tokens (`bg-surface`, `text-text`, etc.) — never hardcoded hex. Dark-mode values live in `src/index.css` `[data-theme='dark']` block; Phase 60 adds no new dark-mode overrides.

---

## Copywriting Contract

All copy is in English only at MVP. Spanish gated on Phase 58 contractor expansion. Source: CONTEXT.md §i18n.

### Global disclaimer (appended to every non-refusal answer and every RAG surface)

```
Not medical advice — consult your clinician.
```

Key: `rag.disclaimer` in `src/lib/i18n/rag-strings.ts`. Font: 11px (`text-micro`) weight 400 `text-text-tertiary`. Do NOT omit from any RAG surface.

### Citation Popover

| Element | Copy |
|---------|------|
| "Open source" link label | `Open source ↗` |
| Freshness label (<6 months) | (no badge) |
| Freshness label (6mo–2yr) | `Last reviewed YYYY-MM` |
| Freshness label (>2yr stale) | `Last reviewed YYYY-MM` + warning pill: `May be outdated` |
| Verbatim quote truncation | `[quote text up to 280 chars]…` + link `Read full chunk` |
| Disclaimer | `Not medical advice — consult your clinician.` |
| Close label (screen reader) | `Close citation` |
| `leanshot_research` disclosure | `LeanShot Research (k≥5 cohort, DP-ε noise applied)` |

### Admin Curation Queue (`/admin/rag/queue`)

| Element | Copy |
|---------|------|
| Page title | `Curation Queue` |
| Empty state heading | `Queue clear` |
| Empty state body | `Nothing to review. Tier-A chunks auto-publish; new Tier-B/C items will appear here for review.` |
| Backlog warning badge | `Backlog: {N} items` (appears warning-colored when N > 100) |
| Approve CTA | `Approve chunk` |
| Reject CTA | `Reject chunk` |
| Edit CTA | `Edit & approve` |
| Retract CTA | `Retract` |
| Approved toast | `Approved · Undo` (5s undo affordance) |
| Rejected toast | `Rejected` |
| 2-person rule badge | `You created this — needs a different reviewer` |
| Reject reason options | `Off-topic` · `Factually wrong` · `Off-label` · `Low quality` · `Duplicate` · `Safety concern` |
| Retract confirmation title | `Retract this chunk?` |
| Retract confirmation body | `Chunk removes from RAG retrieval immediately. Already-sent newsletter inclusions stay sent. Action is logged.` |
| Retract confirmation actions | `Keep chunk` (secondary) · `Retract chunk` (danger) |
| Error state | `Failed to load queue. Refresh to try again.` |
| Keyboard shortcut help label | `Keyboard shortcuts` |

### Admin Federated Source Toggle (`/admin/rag/federated`)

| Element | Copy |
|---------|------|
| Page title | `Federated Sources` |
| Source names | `PubMed (NLM E-utilities)` · `OpenFDA` · `DailyMed` |
| Enabled toggle label | `Sync enabled` |
| Last sync label | `Last synced {relative-time}` (e.g., "Last synced 3 hours ago") |
| Last error label | `Last error: {message}` (error pill, low-emphasis warning tone) |
| Sync cadence label | `Daily 3:00 AM UTC` |
| Full-historical-pull CTA | `Pull full history` |
| Pull confirmation | `This will fetch all available records and may increase costs significantly. Continue?` |
| Empty error state | (no error pill shown when `last_error IS NULL`) |

### Cost Dashboard Extension (`/admin/research/cost`)

| Element | Copy |
|---------|------|
| Section heading (new rows) | `Phase 60 Synthesis` |
| Row labels | `Coach synthesis / day` · `Tip-of-day cron / day` · `Newsletter cron / week` |
| Auto-pause banner | `Scrapers auto-paused — {vendor} budget exhausted. Acknowledge to resume.` |
| Acknowledge CTA | `Acknowledge and resume` |
| Budget caption | `of ${budget} budget · {N}% used` |
| Reset countdown | `Resets in {N} days` |

### AI-Coach Citation UI

| Element | Copy |
|---------|------|
| Sources footer (collapsed) | `Sources ({N})` + chevron-down icon |
| Sources footer (expanded) | `Sources ({N})` + chevron-up icon |
| Screen-reader auto-expand label | `Sources section — {N} citations` |
| Citation marker aria-label | `Open citation {N}` |

### AI-Coach Refusal UX

**Out-of-corpus refusal (G3/G4):**
```
I don't have evidence for that in our knowledge base. Please consult your clinician or ask about a related topic I do have information on.
```

**PHARMA-02 carveout refusal (G1) — exact copy from Phase 39 39-02 D-06. DO NOT invent new safety copy:**
```
That topic requires clinician guidance — please ask your doctor.
```

Note: The PHARMA-02 refusal text is owned by the existing 3-layer invariant (Phase 39 39-02 D-06: ESLint AST + runtime helper + CI grep). Phase 60 MUST reuse the exact string above. Changing this copy requires updating all three invariant layers.

**Citation-validation-failed refusal (G5):**
```
I'm not confident in the supporting evidence — please rephrase your question.
```

### Tip-of-Day Bento Card

| Element | Copy |
|---------|------|
| Eyebrow | `TIP OF THE DAY` (uppercase) |
| Disclaimer | `Not medical advice — consult your clinician.` |
| Reroll button aria-label | `Show another tip` |
| "Open source" link | `Open source ↗` |
| Attribution format | `{source_name} · As of {YYYY-MM-DD} · Tier {A/B/C}` |
| Empty state | (card hidden — returns null per D-24; no placeholder shown) |

### Tip-of-Day Push Notification

iOS/Android character limits: title ≤ 65 chars; body ≤ 240 chars.

| Element | Copy (template) |
|---------|----------------|
| Title | `Today's tip: {chunk_title_truncated_to_50_chars}` |
| Body | `{chunk_first_sentence_truncated_to_180_chars} — LeanShot Research` |

Note: push body must NOT include dosing numbers, prescriptive verbs ("take", "increase", "start"), or equivalence claims. PHARMA-02 CI grep gate applies to push payload generation.

### Public `/knowledge/<topic>/<slug>` Hub Page

| Element | Copy |
|---------|------|
| Page title format | `{chunk_title} — LeanShot Knowledge` |
| Breadcrumb | `Home › Knowledge › {topic} › {slug}` |
| Source meta strip format | `{source_name} · {tier_badge} · Last reviewed {YYYY-MM-DD}` |
| "Read full source" CTA | `Read at source ↗` |
| Related chunks heading | `Related` |
| Footer disclaimer | `Not medical advice — consult your clinician.` |
| FDA off-label disclaimer (in footer, per Phase 64 coupling) | `This information is for educational purposes only and does not constitute medical advice, diagnosis, or treatment. Consult a licensed healthcare provider before making any changes to your health regimen. Some content may reference off-label uses; such uses may not be FDA-approved.` |
| Rate-limit explanation (Phase 67 couples to this) | (deferred to Phase 67 — do not author parallel disclaimer) |

### Public `/knowledge/<topic>` Index Page

| Element | Copy |
|---------|------|
| H1 | `{topic_display_name}` (Fraunces italic, 28px `text-heading` — sole Fraunces usage on page) |
| Subtitle | `Curated peptide and metabolic research, summarized with sources.` |
| Search placeholder | `Search {topic_display_name}…` |
| Empty filter state heading | `No results for "{query}"` |
| Empty filter state body | `Try a broader search term or browse all topics.` |

### Public `/knowledge` Root

| Element | Copy |
|---------|------|
| H1 | `Knowledge Base` (Fraunces italic, 28px `text-heading` — sole Fraunces usage on page) |
| Subtitle | `Curated peptide and metabolic research, summarized with sources.` |
| Newsletter signup heading | `Get the weekly research digest` |
| Newsletter signup body | `Curated summaries from peer-reviewed sources, delivered Sundays. Unsubscribe anytime.` |
| Subscribe CTA | `Subscribe to digest` |
| Subscribe confirmation | `You're subscribed. Check your inbox for a confirmation.` |

### Settings Page Newsletter Toggle

| Element | Copy |
|---------|------|
| Section heading | `Research newsletter` |
| Toggle label | `Send me the weekly research digest` |
| Toggle sublabel | `Curated summaries from approved research sources, sent Sundays. Unsubscribe anytime.` |
| Save CTA | `Save preferences` |

### Onboarding Newsletter Checkbox

| Element | Copy |
|---------|------|
| Checkbox label | `Send me the weekly research digest (optional — unsubscribe anytime)` |
| Default state | Unchecked (CAN-SPAM affirmative opt-in required) |

### Newsletter Email Template

| Element | Copy |
|---------|------|
| Subject format | `LeanShot Research: Week of {YYYY-MM-DD}` |
| Header | `LeanShot Research Digest` |
| Intro placeholder label | `{admin-editable intro paragraph}` |
| Chunk section heading | `This week in {topic_tag}` |
| "Read full" CTA per chunk | `Read at {source_name} ↗` |
| Footer unsubscribe | `Unsubscribe · Manage preferences` |
| Footer disclaimer | `Not medical advice — consult your clinician. LeanShot, [address per CAN-SPAM].` |
| Unsubscribe success page | `You're unsubscribed. [Resubscribe]` |

### Error States (all surfaces)

| Surface | Error copy |
|---------|-----------|
| Citation popover load failure | `Couldn't load source details. Try again.` |
| Queue load failure | `Failed to load queue. Refresh to try again.` |
| Federated source sync error | `Last error: {message}` (in error pill, low-emphasis) |
| Hub page load failure | `Couldn't load this article. Refresh or go back.` |
| Newsletter subscribe failure | `Something went wrong. Try again in a moment.` |
| Cost dashboard load failure | `Couldn't load cost data. Refresh to try again.` |

---

## Surface Contracts

### Surface 1: Admin Curation Queue (`/admin/rag/queue`)

**Layout:** Two-column desktop (`lg:grid-cols-[7fr_5fr]`). Single responsive component — NO separate desktop/mobile components. At `<lg`, QueueDetailPane slides up as bottom Sheet.

**Left column — queue list:**
- Sticky header: `Curation Queue` (18px/600) + `Backlog: {N} items` badge (13px, warning-colored when N > 100).
- Filter pills: `All` · `Tier B` · `Tier C` · `by tag` · `by source` — active pill uses accent background, inactive uses `bg-surface-soft` + `text-text-secondary`.
- Each row: `Card variant="interactive" span={12}`.
  - Left: source name (13px/600) + `TierBadge` + topic tag `Pill` + 1-line summary (13px/400, `line-clamp-1`).
  - Right: `queued {relative-time}` (11px/400 `text-text-tertiary`) + `Approve chunk` Button (`size="sm"`) + reject `IconButton` (`aria-label="Reject chunk"`).
- **2-person rule badge**: when `current_actor_id === chunk.created_by`, render `<Badge tone="warning">You created this — needs a different reviewer</Badge>` inline below the row; disable the `Approve chunk` button for this row.

**Right column — QueueDetailPane:**
- Header strip: source name (external link, 13px/600) + `TierBadge` + `Scraped {date}` (11px/400) + canonical URL link.
- Side-by-side block (FIRST-CLASS per 50-06 §D-17):
  - Left half, label `SOURCE TEXT` (11px/600 uppercase, `text-text-tertiary`): react-markdown + DOMPurify rendered. Background `bg-surface-soft`. Allowlist: `p a ul ol li code pre strong em blockquote mark h2 h3 h4` only — no `img script iframe` inline styles.
  - Right half, label `EXTRACTED QUOTE` (11px/600 uppercase, `text-text-tertiary`): each quote_block renders verbatim text (13px/400) + kind `Badge` (`dose | indication | contraindication | adverse-event`) + gloss (11px/400 `text-text-secondary` if present).
  - At `<lg`: stack vertically (source text above, quotes below).
- Summary block (full-width below): chunk.summary as plain text (13px/400).
- Sticky action row: `Reject chunk` (secondary) · `Edit & approve` (ghost) · `Approve chunk` (primary). Keyboard shortcut `<kbd>` chips: `A`, `R`, `E`, `J/K`.

**Keyboard shortcuts:** `A` = approve selected, `R` = reject (opens RejectReasonSheet), `E` = edit (opens EditChunkModal), `J`/`K` = next/prev row, `Shift+?` = help modal.

**Interaction contract:**
- Optimistic: after approve/reject, remove row from local list immediately.
- Toast `Approved · Undo` (5s undo affordance) per 50-06 §Interaction Contract.
- ESC dismisses RejectReasonSheet / EditChunkModal / RetractChunkModal.

**RejectReasonSheet:** Bottom Sheet with 6 `Pill` components in `role="radiogroup"`. Arrow-key cycling. Danger-toned pills: `Factually wrong`, `Off-label`, `Safety concern`. Neutral-toned: `Off-topic`, `Low quality`, `Duplicate`. Single tap = immediate reject + close + Toast.

**RetractChunkModal:** Modal, title `Retract this chunk?`, body verbatim per copywriting contract above, `reason` textarea (required), `Keep chunk` (secondary) + `Retract chunk` (danger).

**Empty state:** `EmptyState` component with CheckCheck icon + heading `Queue clear` + body copy above.

**a11y:**
- `role="dialog"` + `aria-modal="true"` on Sheet and Modals.
- `aria-label` on all icon-only buttons.
- `aria-pressed` on filter pills.
- `role="radiogroup"` + `aria-checked` on RejectReasonSheet pills.
- Focus trap in all overlays; return focus to trigger on close.

---

### Surface 2: Admin Federated Source Toggle (`/admin/rag/federated`)

**Layout:** Single-column list of 3 source rows within existing `RagLayout` admin shell.

Each row renders as `Card variant="default" span={12}`:
- Left: source name (18px/600) + sync cadence (13px/400 `text-text-secondary`).
- Center: last-sync timestamp (13px/400) + last-error Pill (warning tone, only shown when `last_error IS NOT NULL`).
- Right: enabled `Toggle` (`aria-pressed` on the toggle button itself).

`Full-history pull` button renders as ghost `Button` below each row's footer. Destructive confirmation via `Dialog` (not a full Modal — inline confirm pattern): `Pull full history — this may increase costs significantly. Continue? [Cancel pull] [Pull history]`.

**a11y:** `aria-label="Enable {source_name} sync"` on each toggle. `aria-live="polite"` region for last-sync timestamp updates.

---

### Surface 3: Admin Cost Dashboard Extension (`/admin/research/cost`)

**Extends (does NOT replace) existing `RagCostPage` at `/admin/research/cost`.** Adds three new rows in the existing vendor-card grid.

New cards (`Card span={4}` each, matching existing pattern from 50-09):
- `Coach synthesis / day`
- `Tip-of-day cron / day`
- `Newsletter cron / week`

Each card:
- Eyebrow (11px/400 uppercase `text-text-tertiary`): row label above.
- Large mono spend `$XX.XX` (18px/600 `font-mono`).
- Caption (13px/400): `of ${budget} budget · {N}% used`.
- `CostBar` (existing primitive from Phase 50-02).
- Reset countdown (11px/400): `Resets in {N} days`.

Auto-pause banner: full-width above bento grid when any vendor hits 100%. `AlertOctagon` lucide icon + copy per copywriting contract. `Acknowledge and resume` CTA (primary Button).

---

### Surface 4: AI-Coach Citation Marker + Popover

**CitationMarker:**
- Renders `[N]` as `<Badge tone="info" as="button">` inline within assistant message text.
- Visual: 14×14px visible badge, rounded-full, 11px/600 teal text.
- Tap target: invisible `padding: 5px` surrounding the badge yields ≥24px hitbox — acceptable exception to 4-scale spacing (touch-target compliance only).
- `aria-label="Open citation {N}"`.

**Citation Popover (desktop ≥md):**
- Floats anchored to `[N]` button via `getBoundingClientRect` offset.
- Width: 320px max.
- Header: source title (18px/600) linked to `canonical_url` (`target="_blank" rel="noopener noreferrer"`) + `TierBadge` inline-right.
- Verbatim quote: 13px/400, `bg-surface-soft` background, `border-l-2 border-primary` left accent. Truncated at 280 chars with `…` + `Read full chunk` link (13px/400 accent-colored) if longer.
- Freshness strip: `As of YYYY-MM-DD` (11px/400 `font-mono`) + stale warning Pill (13px/400 warning tone) if >2yr.
- `leanshot_research` disclosure line: (11px/400 `text-text-tertiary`) if `source_type === 'leanshot_research'`.
- "Open source ↗" Button (`variant="ghost"` accent text, 13px/400).
- Disclaimer: 11px/400 `text-text-tertiary` `Not medical advice — consult your clinician.`
- Keyboard: ESC closes; `←`/`→` cycle previous/next citation.
- `role="dialog"` + `aria-modal="true"` + focus trap + return focus to marker on close.

**Citation Popover (mobile <md):**
- Bottom Sheet (existing `Sheet` primitive).
- Same content as popover, full-width.

**Sources footer (below assistant message):**
- Default: collapsed. `Sources ({N})` (13px/400 `text-text-secondary`) + `ChevronDown` icon. `aria-expanded="false"`.
- Expanded: `ChevronUp`. `aria-expanded="true"`.
- Auto-expands when screen reader navigates to message region — use `aria-live="polite"` on the expanding region.
- Each source entry: numbered [N] + source name (13px/400) + TierBadge + truncated URL.

---

### Surface 5: AI-Coach Refusal UX

**MUST NOT look like a normal coach reply with empty citations.**

Renders as `<Card variant="flat">` with:
- Left accent: `AlertTriangle` lucide icon (18px, warning/danger tone depending on refusal type).
- Body text: refusal copy per copywriting contract (13px/400 `text-text`).
- NO `[N]` citation markers anywhere in refusal text.
- NO "Sources (N)" footer.
- Disclaimer (11px/400 `text-text-tertiary`) below body — only for out-of-corpus refusal. PHARMA-02 refusal does NOT append disclaimer (the refusal IS the safety intervention).

PHARMA-02 vs out-of-corpus visual distinction:
- PHARMA-02: `AlertTriangle` icon, neutral card background — same as refusal card but no escalating visual alarm (to avoid patient anxiety from visual design alone).
- Out-of-corpus: `Info` icon, `bg-surface` — softer, informational.
- Citation-validation-failed: `AlertCircle` icon, `bg-surface` — informational.

---

### Surface 6: Tip-of-Day Bento Card

**Location:** Home tab, `<Card span={4} variant="elevated">` in top-right bento slot.

Content when chunk available:
- Eyebrow (11px/400 uppercase `text-text-tertiary`): `TIP OF THE DAY` · topic-tag `Pill`.
- Headline (18px/600 `line-clamp-2`): derived from chunk.summary first sentence.
- Body (13px/400 `text-text-secondary` `line-clamp-3`): chunk.summary remainder + first quote_block if present.
- Footer flex-between: left `{source_name} · As of {YYYY-MM-DD} · TierBadge` (11px/400); right `Open source ↗` ghost Button (13px/400 accent).
- Bottom disclaimer: 11px/400 `text-text-tertiary` `Not medical advice — consult your clinician.`
- Top-right `RotateCcw` icon button (`aria-label="Show another tip"`).
- Skeleton state during fetch.

Content when no eligible chunks: `return null` (card slot collapses; no placeholder).

**Skeleton:** `Skeleton` primitive, matching card height. Animates unless `useReducedMotion()` returns true.

**Dark mode:** `variant="elevated"` maps to `--color-surface-elevated` which has dark counterpart in `[data-theme='dark']`.

---

### Surface 7: Tip-of-Day Push Notification

Shape (extends Phase 54 push pipeline, `push_subscription_categories` row: `"Research tips"`):

```
title:  "Today's tip: {chunk_title_truncated_50}"   (max 65 chars)
body:   "{chunk_first_sentence_180}"                  (max 240 chars)
```

Notification does NOT include dosing numbers, prescriptive verbs, or compounded-equivalence claims. PHARMA-02 CI grep gate applies to the push body string before dispatch.

Deep-link: tapping notification opens `/knowledge/<topic>/<slug>` (or in-app equivalent if native).

---

### Surface 8: Public `/knowledge/<topic>/<slug>` Hub Page

**Route:** consumer-surface path, added to App.tsx `window.location.pathname.startsWith('/knowledge')` branch (per [[reference_react_router_consumer_admin_split]] pattern from Phase 50-09).

**Layout:** single-column content (max-width 720px centered) + optional right sidebar on desktop ≥1024px for "Related chunks".

Header section:
- Breadcrumb (13px/400 `text-text-secondary`): `Home › Knowledge › {topic} › {slug}`. Each segment is a link.
- H1 (18px/600 font-sans — NOT Fraunces on detail pages; Fraunces is index/root-page-only): chunk title.
- Source/tier/freshness meta strip (13px/400 `text-text-secondary`): `{source_name} · TierBadge · Last reviewed {YYYY-MM-DD}`.

Body section:
- Full chunk text rendered via react-markdown + DOMPurify. DOMPurify allowlist: `p a ul ol li code pre strong em blockquote mark h2 h3 h4` — NO `img script iframe`.
- "Read at source ↗" CTA: Button (`variant="ghost"` accent, 13px/400) sticky in right rail on desktop, below content on mobile.

Related chunks sidebar (right rail desktop / collapsed accordion mobile):
- Heading `Related` (13px/600 `text-text-secondary`).
- 3–5 chunk cards: `Card variant="interactive"` with title (13px/600 `line-clamp-2`) + source + TierBadge.

Footer:
- Disclaimer line 1 (13px/400): `Not medical advice — consult your clinician.`
- FDA off-label/DSHEA disclaimer (13px/400 `text-text-tertiary`): full text per copywriting contract.
- `noindex` meta added when `kb_chunks.public_visibility = false`.

SEO meta (via react-helmet-async):
- `<title>{chunk_title} — LeanShot Knowledge</title>`
- `<meta name="description" content="{verbatim_quote_first_160_chars}">`
- `<link rel="canonical" href="https://app.leanshot.app/knowledge/{topic}/{slug}">`
- `<meta property="og:type" content="article">`
- JSON-LD `MedicalWebPage` schema: `@context, @type:'MedicalWebPage', headline, datePublished, dateModified, author, publisher`.

---

### Surface 9: Public `/knowledge/<topic>` Index Page

**Layout:** full-width hero strip + filter/search bar + article grid.

- H1 (28px/600 font-display Fraunces italic — SOLE Fraunces usage on this page): `{topic_display_name}`.
- Subtitle (13px/400): `Curated peptide and metabolic research, summarized with sources.`
- Search input (`Input` primitive, placeholder per copywriting contract). Client-side fuse.js search (already in package.json).
- Filter pills row: `All Tiers` · `Tier A` · `Tier B` · `Tier C`. URL-synced (`?tier=A`).
- Article grid: `Card variant="interactive" span={6}` per chunk (12-col bento). Per card: TierBadge top-right + topic Pill top-left + title (18px/600 `line-clamp-2`) + 2-line summary (13px/400) + footer `{source_name} · As of {YYYY-MM-DD}` + `Read at source ↗` ghost link (13px/400 accent).
- Empty filtered state: `EmptyState` component.
- JSON-LD `CollectionPage` schema in `<head>`.

---

### Surface 10: Public `/knowledge` Root

**Layout:** topic grid (top) + featured chunk card + newsletter signup.

Topic grid: `Card variant="interactive" span={4}` per topic. Topic count label (13px/400) below topic name.

Featured chunk: `Card variant="elevated" span={12}`. Title (18px/600) + source + TierBadge + 2-line summary (13px/400) + `Read more →` link (13px/400).

Newsletter signup section:
- Heading (18px/600): `Get the weekly research digest`.
- Body (13px/400): per copywriting contract.
- `Input` for email + `Subscribe to digest` Button (primary). Inline success message on submit.
- Disclaimer (11px/400 `text-text-tertiary`): `Unsubscribe anytime. Not medical advice.`

JSON-LD `WebSite` schema in `<head>`.

---

### Surface 11: Settings Page Newsletter Toggle

Additive row in existing `<SettingsPage>` notifications section:

- Section heading `Research newsletter` (18px/600).
- Toggle row: label (13px/400) + sublabel (13px/400 `text-text-secondary`) + Toggle (`aria-pressed`).
- Multi-select tags: `Pill` components for each topic_tag; `aria-pressed` per pill.
- `Save preferences` Button (primary).

Default: OFF for free users, ON for paid users (D-26).

---

### Surface 12: Onboarding Newsletter Checkbox

Optional checkbox at an appropriate onboarding step (planner picks slot):
- `<input type="checkbox">` (unchecked by default — CAN-SPAM).
- Label (13px/400): per copywriting contract.
- `aria-describedby` pointing to sublabel explaining what the newsletter is.

---

### Surface 13: Newsletter Email Template

**Platform:** Resend HTML template (single-column, max-width 600px, light theme only — email clients do not reliably honor dark mode).

**Font:** Geist via Google Fonts `<link>` with Arial fallback.

Structure:
- Header (teal-700 background, white text): LeanShot logo + `LeanShot Research Digest` + `Week of {YYYY-MM-DD}`.
- Admin-editable intro paragraph (13px/400 dark text).
- Per-topic sections: heading `This week in {topic}` (18px/600) + 3 tier-A chunk cards.
- Each chunk card (bordered, 12px radius, surface-card background):
  - TierBadge (inline `<span>` styled neutral — no CSS classes, inline styles only for email compatibility).
  - Chunk title (18px/600, dark).
  - 1-line summary (13px/400, secondary text).
  - `Read at {source_name} ↗` CTA button (32px height, teal-700 background, white text, `border-radius: 8px`).
- 1 evergreen popular chunk (same card layout, label `Popular this month`).
- Footer: `Not medical advice — consult your clinician.` + `Unsubscribe · Manage preferences` links (13px/400) + CAN-SPAM address.

**TierBadge in email (inline style only):**
- Tier A: `background:#ebe7dc; color:#1b4842; padding:2px 8px; border-radius:9999px; font-weight:600; font-size:11px`
- Tier B: `background:#f6f2e8; color:#4d5e58; padding:2px 8px; border-radius:9999px; font-weight:400; font-size:11px`
- Tier C: `background:#f8f5ec; color:#8d958f; padding:2px 8px; border-radius:9999px; font-weight:400; font-size:11px`

---

## Critical UI Invariants

The following invariants are load-bearing and MUST NOT be violated:

1. **Citation popover MUST display verbatim quote.** Source: AI-SPEC §4 `CitationSchema.verbatim_quote`. Truncate to 280 chars with ellipsis + `Read full chunk` link — never omit. This prevents citation fraud (ungrounded claims hidden by empty popover).

2. **Refusal UX MUST NOT look like a normal coach reply.** Use `<Card variant="flat">` with AlertTriangle/Info/AlertCircle icon. Zero `[N]` markers in refusal text. Zero "Sources (N)" footer.

3. **PHARMA-02 refusal copy is LOCKED** to `"That topic requires clinician guidance — please ask your doctor."` — exact string from Phase 39 39-02 D-06. Do not modify without updating all 3 invariant layers.

4. **All admin queue actions MUST show 2-person rule badge** when `current_actor_id === chunk.created_by`. Approve chunk button MUST be disabled in this state. This is a SECDEF RPC-level enforcement (CONTEXT.md §Admin Curation Queue) mirrored in UI.

5. **`/knowledge/*` pages MUST include FDA off-label / DSHEA disclaimer in footer.** Full verbatim text from copywriting contract above. This is required pre-Phase 64 (Legal Refresh); Phase 64 will audit and potentially revise — do NOT abbreviate.

6. **Source tier badge coloring is neutral-only.** No green = endorsed, no red = dangerous. Medical safety norm. Only contrast hierarchy permitted.

7. **Freshness indicator rules:** `<6mo` = no badge; `6mo–2yr` = `Last reviewed YYYY-MM` (text only); `>2yr` = `Last reviewed YYYY-MM` + `May be outdated` warning Pill.

8. **Tier badge in citation popover distinguishes `leanshot_research` visually** from external peer-reviewed sources. Use amber-soft background + disclosure line `LeanShot Research (k≥5 cohort, DP-ε noise applied)`. This distinction is required by AI-SPEC §5 Dim #8 and FTC health-claim substantiation guidance.

9. **Newsletter opt-in checkbox is UNCHECKED by default** everywhere it appears (onboarding, settings). CAN-SPAM affirmative opt-in requirement.

10. **No horizontal scroll at ≥375px.** All surfaces must reflow. Tap targets ≥44px (EXCEPT citation `[N]` marker hitbox which is ≥24px via padding — explicitly documented exception).

11. **Typography 4-size ceiling enforced.** Only 11 / 13 / 18 / 28 px. No 16px. Phase 69 CI-gate will FAIL any `text-base` or `text-md` usage in Phase 60 components. The `text-heading` (28px) token is restricted to Fraunces H1 on `/knowledge` root and `/knowledge/<topic>` index pages only — executor MUST NOT use it elsewhere.

---

## Accessibility Baseline

Per LeanShot DS conventions (CLAUDE.md):

- `role="dialog"` + `aria-modal="true"` on: CitationPopover (both floating and Sheet forms), EditChunkModal, RetractChunkModal, keyboard-shortcuts help modal.
- `aria-label` on icon-only buttons: reject icon, reroll icon, citation marker (per `Open citation {N}`), Sources chevron toggle, federated source sync button.
- `aria-pressed` on: filter pills (queue + hub), newsletter toggle, federated source enabled toggle.
- `role="status"` + `aria-live="polite"` on: approve/reject Toast, newsletter subscribe confirmation, federated sync status updates.
- `aria-busy` on: Approve chunk button while RPC in flight.
- `role="radiogroup"` + arrow-key cycling on: RejectReasonSheet pills.
- `useReducedMotion()` gates: Skeleton animations, Card hover transitions, Popover entry animations.
- Focus trap in all overlays. Return focus to trigger element on close.
- `aria-expanded` on Sources footer chevron toggle.
- `aria-describedby` on newsletter checkbox pointing to sublabel.

---

## Animation Contract

| Motion | Duration | Easing | Reduced-motion fallback |
|--------|----------|--------|------------------------|
| Citation popover entry (scale + fade) | `--duration-quick` (200ms) | `--ease-out-quart` | Instant (no animation) |
| Bottom Sheet entry (translate-y) | `--duration-standard` (300ms) | `--ease-out-quart` | Instant |
| Toast entry (fade + rise) | `--duration-standard` (300ms) | `--ease-out-quart` | Instant |
| Skeleton shimmer | 1.6s linear infinite | `--animate-shimmer` | Static `bg-skeleton` (no shimmer) |
| Queue row remove (height collapse) | `--duration-quick` (200ms) | `--ease-in-out-quart` | Instant removal |
| Bento card hover (elevation) | `--duration-instant` (100ms) | `--ease-out-quart` | No hover effect |

All `useReducedMotion()` checks use the existing `src/hooks/useReducedMotion.ts` hook.

---

## Dark Mode Parity

Every Phase 60 surface MUST render correctly under `[data-theme="dark"]`. The dark theme token overrides are defined in `src/index.css`. Phase 60 adds NO new dark-mode overrides.

Mapping verification (existing token pairs):
- `bg-surface` (light `#fefcf7`) → dark `#16201e`
- `bg-surface-elevated` (light `#f6f2e8`) → dark `#1d2a28`
- `bg-surface-soft` (light `#ebe7dc`) → dark `#16201e`
- `text-text` (light `#16221f`) → dark (see `[data-theme='dark']` in index.css)
- `text-text-secondary` → dark counterpart in index.css
- `text-text-tertiary` → dark counterpart in index.css

Newsletter HTML email: light-only (email clients do not reliably support dark mode).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — project uses LeanShot DS primitives |
| Third-party | none declared | not applicable |

No third-party component registries are used in Phase 60. All components are from `src/components/ui/` (LeanShot DS v2). Registry vetting gate is not required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
