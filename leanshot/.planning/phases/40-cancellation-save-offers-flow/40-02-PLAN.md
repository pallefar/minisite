---
phase: 40
plan: 02
type: execute
wave: 1
title: "Stripe webhook extension + pause-reminder cron + email-router union widening"
requirements: [POLISH-03]
depends_on: []
files_modified:
  - supabase/migrations/20270709000004_p40_subscriptions_pause_cols.sql
  - supabase/migrations/20270709000005_p40_pause_reminder_cron.sql
  - supabase/migrations/20270709000006_p40_pause_autoresume_reconcile_cron.sql
  - supabase/functions/stripe-webhook/events/subscription-updated.ts
  - supabase/functions/stripe-webhook/events/subscription-updated.test.ts
  - supabase/functions/pause-reminder-fire/index.ts
  - supabase/functions/pause-reminder-fire/index.test.ts
  - supabase/functions/_shared/email-router.ts
  - supabase/functions/_shared/email-templates/pause-reminder-t7.ts
  - supabase/functions/_shared/email-templates/pause-resumed-t0.ts
autonomous: true
must_haves:
  truths:
    - "subscriptions table has columns paused_until timestamptz nullable, is_paused boolean not null default false, reminded_t7 boolean not null default false"
    - "Stripe webhook customer.subscription.updated detects pause_collection state deltas and mirrors them to subscriptions.paused_until + is_paused (NO new case arms in index.ts per RESEARCH §Pitfall 1)"
    - "Auto-resume transition (was_paused && !is_paused_now) fires T-0 confirmation email via _shared/email-router.ts"
    - "pg_cron 'p40-pause-t7-reminder' runs hourly; calls /functions/v1/pause-reminder-fire with service_role_key from vault.decrypted_secrets (NOT current_setting GUC per reference_supabase_pg_cron_vault_service_role_pattern)"
    - "pause-reminder-fire Fn queries subscriptions where paused_until BETWEEN now() AND now()+7d AND reminded_t7=false; sends T-7d email via email-router; sets reminded_t7=true"
    - "Auto-resume reconcile fail-safe cron 'p40-pause-autoresume-reconcile' (per Assumption A3): runs every 4 hours; finds subscriptions where paused_until < now() AND is_paused=true; calls stripe.subscriptions.retrieve and updates local mirror if Stripe shows pause_collection=null"
    - "email-router.ts EmailTemplate union widened with 'pause_reminder_t7' | 'pause_resumed_t0'; subjectFor + renderTemplate switch arms ship in SAME commit (per feedback_planner_missed_status_enum_widening)"
    - "PHI flag is server-derived: subscription.clinic_id IS NOT NULL → phi=true (SES); else phi=false (Resend) — D-09"
    - "All Postgres cron bodies use named dollar-quote tags ($cron$, $invoke$, $unschedule$); zero bare $$ — per reference_postgres_dollar_quote_nesting_in_cron_body"
    - "subscriptions.id FK type is TEXT (per reference_minisite_monorepo_layout); migration ALTERs do not break existing P14 schema"
  artifacts:
    - path: "supabase/migrations/20270709000004_p40_subscriptions_pause_cols.sql"
      provides: "ALTER subscriptions add paused_until + is_paused + reminded_t7 + partial index"
    - path: "supabase/migrations/20270709000005_p40_pause_reminder_cron.sql"
      provides: "Hourly pg_cron T-7d reminder fire"
    - path: "supabase/migrations/20270709000006_p40_pause_autoresume_reconcile_cron.sql"
      provides: "Every-4h auto-resume reconcile sweep (A3 mitigation)"
    - path: "supabase/functions/stripe-webhook/events/subscription-updated.ts"
      provides: "Extended handler: pause_collection mirror + T-0 transition detection"
      exports: ["handle"]
    - path: "supabase/functions/pause-reminder-fire/index.ts"
      provides: "Cron worker — T-7d email batch send"
    - path: "supabase/functions/_shared/email-router.ts"
      provides: "Widened union + subjectFor + renderTemplate switch arms"
  key_links:
    - from: "stripe-webhook dispatcher index.ts case 'customer.subscription.updated'"
      to: "events/subscription-updated.ts handle()"
      via: "lazy-import dispatch (existing routing at index.ts:159-162)"
      pattern: "case 'customer.subscription.updated'"
    - from: "events/subscription-updated.ts extension"
      to: "subscriptions.paused_until + is_paused"
      via: "admin.from('subscriptions').update({...})"
      pattern: "paused_until|is_paused"
    - from: "pg_cron p40-pause-t7-reminder"
      to: "pause-reminder-fire Edge Fn"
      via: "net.http_post with vault.decrypted_secrets bearer"
      pattern: "net\\.http_post.*pause-reminder-fire"
    - from: "pause-reminder-fire Fn"
      to: "_shared/email-router.ts sendEmail()"
      via: "{ template: 'pause_reminder_t7', to, vars, phi }"
      pattern: "sendEmail\\(.*pause_reminder_t7"
---

<objective>
Ship the pause-lifecycle infrastructure: Stripe webhook detects `pause_collection` deltas (extends the EXISTING `subscription-updated` handler — no new case arms per RESEARCH §Pitfall 1), pg_cron fires T-7d reminder emails 7 days before auto-resume, the webhook itself fires T-0 confirmation on auto-resume, and a fail-safe reconcile cron handles the unlikely case where Stripe's auto-resume webhook drops (per Assumption A3).

Purpose: D-06..D-11 (pause mechanics) require local mirror of Stripe's `pause_collection` state and time-based reminder notifications. The state-mirror invariant is the source of truth for D-07 read-only-during-pause UI gating that 40-04 implements.

Output: 3 migrations + 1 extended Stripe event handler + 1 cron-driven Edge Fn + 1 widened email-router + 2 new email templates. All within Wave 1 (parallel-safe with 40-01 — disjoint file sets).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-CONTEXT.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-RESEARCH.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-PATTERNS.md
@supabase/functions/stripe-webhook/events/subscription-updated.ts
@supabase/functions/stripe-webhook/index.ts
@supabase/functions/_shared/email-router.ts
@supabase/migrations/20270708000021_p35_challenge_evaluate_cron.sql
@supabase/functions/challenge-evaluate-cron/index.ts
@supabase/migrations/20260601000019_stripe_subscriptions.sql

<interfaces>
<!-- The verified email-router signature lives at _shared/email-router.ts:51-96 (per PATTERNS §8). -->
<!-- Executors MUST use this exact shape — NOT the legacy { template_name, recipient, props, phi_flag } hinted in CONTEXT. -->

export type EmailTemplate =
  | 'welcome' | 'receipt' | 'password_reset' | 'marketing_announcement'
  | 'nps_quarterly'
  | 'clinic_notification' | 'dose_alert' | 'doctor_share' | 'patient_access_log_notification'
  | 'csat_followup' | 'helpdesk_agent_reply' | 'sla_breach_alert' | 'helpdesk_unknown_sender'
  | 'pause_reminder_t7' | 'pause_resumed_t0';  // ← 40-02 widening

export type SendEmailArgs = {
  template: EmailTemplate;
  to: string;
  vars: Record<string, unknown>;
  phi: boolean;
};

export async function sendEmail(args: SendEmailArgs): Promise<SendEmailResult>;

<!-- Stripe webhook handler signature (verified at subscription-updated.ts:1-100) -->
export async function handle(
  event: Stripe.Event,
  admin: SupabaseClient,
  _d05Spy?: D05Spy,
): Promise<void>;

<!-- subscriptions table extensions added in this plan: -->
ALTER TABLE public.subscriptions
  ADD COLUMN paused_until timestamptz,
  ADD COLUMN is_paused boolean NOT NULL DEFAULT false,
  ADD COLUMN reminded_t7 boolean NOT NULL DEFAULT false;

CREATE INDEX idx_subscriptions_pause_t7
  ON public.subscriptions(paused_until)
  WHERE is_paused = true AND reminded_t7 = false;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: subscriptions ALTER + pre-merge collision check (per PATTERNS §12, A8 mitigation)</name>
  <files>supabase/migrations/20270709000004_p40_subscriptions_pause_cols.sql</files>
  <action>
    Before writing the migration, run `grep -rn 'alter table public.subscriptions' supabase/migrations/ | grep -v 20260601000019` to confirm no concurrent phase touches `subscriptions` (per A8 + feedback_wave_n_push_correction_invalidates_wave_n_plus_1_plans). If any unexpected ALTER appears, surface to the operator before proceeding. If clean, write `supabase/migrations/20270709000004_p40_subscriptions_pause_cols.sql` with the ALTER body from &lt;interfaces&gt; verbatim — three columns + one partial index (column-only predicate `where is_paused = true and reminded_t7 = false` is IMMUTABLE per reference_supabase_migration_gotchas). Use `add column if not exists` for replay-safety. NO trigger logic needed — auto-resume detection happens in Task 2's webhook handler extension.

    Implements D-06 / D-09 (storage shape for pause state mirror + dedup flag).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; supabase db query --linked --command "select column_name, data_type, is_nullable from information_schema.columns where table_name='subscriptions' and column_name in ('paused_until','is_paused','reminded_t7') order by column_name"</automated>
  </verify>
  <done>Migration applies cleanly; SELECT returns 3 rows; partial index `idx_subscriptions_pause_t7` exists (`supabase db query --linked --command "select indexname from pg_indexes where tablename='subscriptions' and indexname='idx_subscriptions_pause_t7'"`).</done>
</task>

<task type="auto">
  <name>Task 2: EXTEND stripe-webhook events/subscription-updated.ts for pause_collection mirror + T-0 auto-resume detection (per PATTERNS §3, RESEARCH §Pitfall 1)</name>
  <files>supabase/functions/stripe-webhook/events/subscription-updated.ts, supabase/functions/stripe-webhook/events/subscription-updated.test.ts, supabase/functions/_shared/email-router.ts, supabase/functions/_shared/email-templates/pause-reminder-t7.ts, supabase/functions/_shared/email-templates/pause-resumed-t0.ts</files>
  <action>
    First, EXTEND `_shared/email-router.ts` (per PATTERNS §8). Widen the `EmailTemplate` union to add `'pause_reminder_t7' | 'pause_resumed_t0'`. Add the corresponding cases in BOTH the `subjectFor()` switch (~line 138) AND the `renderTemplate()` switch (~line 171) — IN THE SAME COMMIT (per feedback_planner_missed_status_enum_widening — splitting causes silent default-case misroute). Subjects: pause_reminder_t7 → `"Your LeanShot subscription resumes {date}"`; pause_resumed_t0 → `"Welcome back — your LeanShot subscription resumed today"`. Create the two template files under `_shared/email-templates/` rendering the consumer-friendly copy from D-09 (T-7d: "Your subscription resumes {date}. Card on file: •••• {last4}. Extend pause / Resume now?" links; T-0: "Welcome back — billing resumed today."). Mirror the existing `nps-quarterly` template file shape — verify nearest analog via `ls supabase/functions/_shared/email-templates/` and copy its export shape verbatim.

    Then EXTEND `supabase/functions/stripe-webhook/events/subscription-updated.ts` IN-PLACE (do NOT add a new file; do NOT add a new case arm to `stripe-webhook/index.ts` — `customer.subscription.updated` already routes here per index.ts:159-162). After the existing `subscriptions.upsert` block, append the new logic from PATTERNS §3 (the 40-02 extension block, lines 134-160 of PATTERNS): (1) extract `pauseCollection` typed `{ behavior: 'void'|'mark_uncollectible'|'keep_as_draft'; resumes_at: number|null } | null`, (2) compute `newPausedUntil` (ISO from resumes_at unix-sec) + `newIsPaused` (pauseCollection !== null), (3) read-back previous state via `admin.from('subscriptions').select('is_paused, clinic_id').eq('id', subscription.id).single()` — capture both `wasPaused` AND the `clinic_id` for the PHI flag, (4) `admin.from('subscriptions').update({ paused_until: newPausedUntil, is_paused: newIsPaused, reminded_t7: newIsPaused ? undefined : false })` (reset reminded_t7 on auto-resume so future pauses re-arm), (5) if `wasPaused && !newIsPaused` → call `await sendEmail({ template: 'pause_resumed_t0', to: customerEmail, vars: { user_first_name, resumed_at: new Date().toISOString() }, phi: clinic_id !== null })` — fetch customerEmail from `subscriptions` joined to `profiles` OR from the Stripe `customer` field already in `event.data.object` (verify shape). Wrap any error in Sentry breadcrumb via `_shared/sentry.ts` — log `err.message + err.code` only, NEVER the full event payload (per T-14-03-I1). Do NOT call `await shutdownPostHog()` here — that's the dispatcher's responsibility in `index.ts:317-322`.

    EXTEND the existing test file `subscription-updated.test.ts` with new Deno.test blocks: (a) pause-start (pause_collection present, prev is_paused=false) → asserts UPDATE called with paused_until + is_paused=true; (b) pause-extend (pause_collection.resumes_at moves forward, prev is_paused=true) → asserts UPDATE called with new paused_until, no T-0 email; (c) auto-resume (pause_collection=null, prev is_paused=true) → asserts UPDATE called with is_paused=false + sendEmail called with template='pause_resumed_t0'; (d) clinic-org auto-resume → asserts sendEmail called with phi=true; (e) no-op on unrelated update (status change without pause_collection delta) → asserts NO update to paused_until/is_paused.

    Implements D-08 (auto-resume detection) + D-09 (T-0 email) + D-11 (correct event-name handling per RESEARCH override).
  </action>
  <verify>
    <automated>$HOME/.deno/bin/deno test --no-check supabase/functions/stripe-webhook/events/subscription-updated.test.ts &amp;&amp; grep -c "case 'customer.subscription.paused'\|case 'customer.subscription.resumed'" supabase/functions/stripe-webhook/index.ts | grep -q "^0$"</automated>
    <manual>After Edge Fn deploy in 40-06 close-out: `stripe trigger customer.subscription.updated` against deployed dispatcher; assert `select paused_until, is_paused from subscriptions where id='<test-sub>'` reflects the test payload.</manual>
  </verify>
  <done>Deno test green with ≥5 new test blocks; email-router union widening + subjectFor + renderTemplate arms in same commit (verified by `git diff --stat supabase/functions/_shared/email-router.ts` shows the additions in one diff); zero new case arms in `stripe-webhook/index.ts`; template files exist.</done>
</task>

<task type="auto">
  <name>Task 3: pg_cron T-7d reminder + auto-resume reconcile sweep + pause-reminder-fire Edge Fn (per PATTERNS §7, A3 mitigation)</name>
  <files>supabase/migrations/20270709000005_p40_pause_reminder_cron.sql, supabase/migrations/20270709000006_p40_pause_autoresume_reconcile_cron.sql, supabase/functions/pause-reminder-fire/index.ts, supabase/functions/pause-reminder-fire/index.test.ts</files>
  <action>
    Write `supabase/functions/pause-reminder-fire/index.ts` — copy structure verbatim from `supabase/functions/challenge-evaluate-cron/index.ts` lines 1-80 (per PATTERNS §7). Skeleton: (1) Imports from `_shared/lifecycle-utils.ts` (`checkServiceRoleBearer`, `corsHeaders`, `jsonError`, `jsonResponse`, `makeLazyAdmin`). (2) `const { admin } = makeLazyAdmin()`. (3) Inside `Deno.serve`: gate via `checkServiceRoleBearer(req)` → 401 on fail. (4) `const { data: candidates } = await admin.from('subscriptions').select('id, user_id, clinic_id, paused_until').lt('paused_until', new Date(Date.now() + 7*24*3600*1000).toISOString()).gt('paused_until', new Date().toISOString()).eq('is_paused', true).eq('reminded_t7', false).limit(200)` — uses the partial index `idx_subscriptions_pause_t7`. (5) For each candidate: lookup `profiles.email + first_name + card_last4` (verify columns via `grep -n 'card_last4\|last4' supabase/migrations/` — if column not present, omit from vars; D-09 copy says "Card on file: •••• {last4}" — surface as `{last4}` placeholder filled with profile column OR 'on file' fallback string). (6) Call `await sendEmail({ template: 'pause_reminder_t7', to: profile.email, vars: { user_first_name, resumes_at: candidate.paused_until, last4 }, phi: candidate.clinic_id !== null })`. (7) After successful send: `await admin.from('subscriptions').update({ reminded_t7: true }).eq('id', candidate.id)`. (8) Per-candidate errors do NOT abort batch (continue loop; capture via `_shared/sentry.ts`). (9) `try { /* main */ } finally { await shutdownPostHog() }`. (10) Return `jsonResponse(200, { processed: candidates.length, sent: n_sent, errors: n_errors })`.

    Write the corresponding `index.test.ts` — mock admin client to return 3 candidates (one consumer, one clinic-org, one with missing email); mock sendEmail to (a) succeed for consumer (phi=false), (b) succeed for clinic (phi=true), (c) skip for missing-email. Assert: sendEmail called 2× with correct phi flags; reminded_t7 update called 2× (only for successful sends); 1 error logged. Mirror existing `*-cron/index.test.ts` patterns — find via `ls supabase/functions/*cron*/index.test.ts`.

    Write `supabase/migrations/20270709000005_p40_pause_reminder_cron.sql` — copy P35 challenge-evaluate-cron structure (`supabase/migrations/20270708000021_p35_challenge_evaluate_cron.sql`) verbatim: (1) idempotent unschedule via `do $unschedule$ ... begin perform cron.unschedule('p40-pause-t7-reminder'); exception when others then null; end $unschedule$;`. (2) `cron.schedule('p40-pause-t7-reminder', '0 * * * *', $cron$ do $invoke$ ... declare v_secret text; begin select decrypted_secret into v_secret from vault.decrypted_secrets where name='service_role_key' limit 1; if v_secret is null then raise notice 'p40-pause-t7-reminder: service_role_key missing — skipping'; return; end if; perform net.http_post( url := 'https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/pause-reminder-fire', headers := jsonb_build_object('Authorization', 'Bearer ' || v_secret, 'Content-Type', 'application/json'), body := '{}'::jsonb, timeout_milliseconds := 30000 ); exception when others then raise notice 'p40-pause-t7-reminder: error % — continuing', sqlerrm; end $invoke$; $cron$);`. Hardcoded URL per reference_supabase_pg_cron_vault_service_role_pattern (no `current_setting` GUC). Named dollar tags ONLY — `$cron$` outer, `$invoke$` inner, `$unschedule$` for the pre-flight; ZERO bare `$$`. Minute slot: `0 * * * *` (P35 uses 22; this slot is unclaimed per PATTERNS §7 gotcha).

    Write `supabase/migrations/20270709000006_p40_pause_autoresume_reconcile_cron.sql` — A3 mitigation. Same structure as above but: cron name `'p40-pause-autoresume-reconcile'`, schedule `'15 */4 * * *'` (every 4 hours at minute 15), URL `/functions/v1/pause-reminder-fire?mode=reconcile` (same Fn handles both modes). EXTEND the `pause-reminder-fire/index.ts` to check `new URL(req.url).searchParams.get('mode')` and branch: `mode='reconcile'` → query `subscriptions where paused_until < now() AND is_paused = true LIMIT 100`; for each, call `stripe.subscriptions.retrieve(id)` and if Stripe shows `pause_collection === null`, run the same auto-resume update + T-0 email logic as the webhook would (mirror Task 2's auto-resume branch). This is the fail-safe for Assumption A3 (Stripe drops the auto-resume webhook). Per RESEARCH Open Q3 + Assumption A3.

    Implements D-09 (T-7d reminder) + A3 mitigation (auto-resume reconcile).
  </action>
  <verify>
    <automated>$HOME/.deno/bin/deno test --no-check supabase/functions/pause-reminder-fire/index.test.ts &amp;&amp; supabase db query --linked --command "select jobname, schedule, active from cron.job where jobname like 'p40-pause-%'" | grep -c "p40-pause-t7-reminder\|p40-pause-autoresume-reconcile" | grep -q "^2$"</automated>
    <manual>After deploy: `supabase db query --linked --command "select cron.schedule('manual-test', '* * * * *', \$\$select 1\$\$); select cron.unschedule('manual-test');"` confirms cron.schedule API works on linked project. Then verify the two p40 crons appear in `cron.job` table.</manual>
  </verify>
  <done>Both migrations apply cleanly; Deno test green with 3 candidate scenarios; cron.job table shows both p40 entries active=true; zero bare `$$` in either migration (`grep -E '^\s*\$\$\s*$' 20270709000005_p40_*.sql 20270709000006_p40_*.sql | wc -l` → 0); pause-reminder-fire Fn supports both `mode=fire` (default) and `mode=reconcile` query params.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe→stripe-webhook dispatcher | HMAC signature verified via `Stripe.createSubtleCryptoProvider()` (existing); only signed payloads pass |
| pg_cron→pause-reminder-fire | service-role bearer from `vault.decrypted_secrets`; net.http_post over HTTPS |
| pause-reminder-fire→Postgres | service-role admin client; reads `subscriptions` + `profiles`; updates `reminded_t7` |
| pause-reminder-fire→email-router | template + PHI flag passed; router resolves Resend vs SES |
| pause-reminder-fire→Stripe API (reconcile mode) | secret API key; `subscriptions.retrieve` only — no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02-01 | Spoofing | Forged `customer.subscription.updated` payload bypassing pause logic | mitigate | Existing dispatcher verifies Stripe-Signature via SubtleCryptoProvider — extension inherits this; no new attack surface |
| T-40-02-02 | Tampering | pg_cron triggers with stale/wrong service_role_key | mitigate | Vault.decrypted_secrets is the only source; `select decrypted_secret into v_secret` + null-check → silent skip if missing (no fallback path) |
| T-40-02-03 | Information Disclosure | T-7d email leaks card last4 / clinic-org PHI to consumer Resend channel | mitigate | PHI flag server-derived from `clinic_id IS NOT NULL`; email-router routes PHI to SES (P25 D-03); pgTAP / unit test asserts phi flag plumbing |
| T-40-02-04 | Repudiation | Auto-resume happens but T-0 email never sent (A3) | mitigate | A3 reconcile cron runs every 4h; if Stripe shows pause_collection=null AND local is_paused=true → forces UPDATE + T-0 email (idempotent via reminded_t7 reset on auto-resume) |
| T-40-02-05 | Information Disclosure | Sentry error log contains Stripe event payload (customer email, etc.) | mitigate | Per T-14-03-I1 — log `err.message + err.code` only; NEVER `event.data.object`; Sentry breadcrumbs scrub via existing P25 mask |
| T-40-02-06 | Denial of Service | Bug in reconcile cron loops over thousands of paused subs and burns Stripe API quota | mitigate | LIMIT 100 per invocation; runs every 4h not every minute; per-candidate try/catch prevents one bad row from aborting batch; A4 documents Stripe's silent pause_collection limits |
| T-40-02-07 | Tampering | Race condition — webhook + reconcile cron both fire for same auto-resume → double T-0 email | mitigate | Idempotency: reconcile cron checks `is_paused=true AND paused_until < now()` BEFORE Stripe retrieve; webhook resets is_paused=false on the same predicate; second invocation finds no match and no-ops. T-0 email is best-effort idempotent (user gets at-most-2 emails on race — acceptable per UX) |
| T-40-02-08 | Elevation of Privilege | Non-service-role caller invokes pause-reminder-fire to spam emails | mitigate | `checkServiceRoleBearer` gates entry; 401 on any non-service-role bearer (per reference_supabase_service_role_key_format_divergence — sb_secret_* token required) |
</threat_model>

<verification>
- `grep -c "case 'customer.subscription.paused'" supabase/functions/stripe-webhook/index.ts` returns 0
- `grep -c "case 'customer.subscription.resumed'" supabase/functions/stripe-webhook/index.ts` returns 0
- `grep -E "^\s*\\\$\\\$\s*$" supabase/migrations/20270709000005_p40_*.sql supabase/migrations/20270709000006_p40_*.sql | wc -l` returns 0
- Deno test suite green: subscription-updated.test.ts (≥5 new blocks) + pause-reminder-fire/index.test.ts (≥3 scenarios)
- `cron.job` shows `p40-pause-t7-reminder` (hourly) + `p40-pause-autoresume-reconcile` (every 4h) both active
- email-router.ts union widened: `grep -c "pause_reminder_t7\|pause_resumed_t0" supabase/functions/_shared/email-router.ts` ≥ 4 (union + subjectFor + renderTemplate × 2 templates)
</verification>

<success_criteria>
- POLISH-03 webhook mirror works: pause-start + pause-extend + auto-resume all reflected in `subscriptions.paused_until + is_paused`
- T-7d reminder fires for every paused subscription within 24-hour window of its resume date (hourly granularity)
- T-0 confirmation fires via webhook OR (fail-safe) via reconcile cron — Assumption A3 risk neutralized
- No new event-name case arms in stripe-webhook/index.ts (per RESEARCH §Pitfall 1)
- email-router widening is single-commit atomic (no silent default-case misroute window)
</success_criteria>

<output>
After completion, create `.planning/phases/40-cancellation-save-offers-flow/40-02-SUMMARY.md` capturing: (a) any concurrent ALTER on `subscriptions` discovered at execute-time (A8); (b) profile column used for `last4` (or null fallback); (c) cron.job entries verified active; (d) deferred-item for `customer.subscription.paused`/`.resumed` Stripe Dashboard subscription — research recommendation is to SKIP (no-op event for pause_collection), but planner can flag for belt-and-suspenders in 40-06 HUMAN-UAT.
</output>
