---
phase: 40
plan: 04
type: execute
wave: 2
title: "CancellationModal single-chunk + analytics events + cancellation-feedback-to-ticket Fn"
requirements: [POLISH-01]
depends_on: ["40-01"]
files_modified:
  - leanshot/src/types/cancellation.ts
  - leanshot/src/components/dashboard/settings/cancellation/CancellationModal.tsx
  - leanshot/src/components/dashboard/settings/cancellation/steps/ReasonPicklistStep.tsx
  - leanshot/src/components/dashboard/settings/cancellation/steps/OfferStep.tsx
  - leanshot/src/components/dashboard/settings/cancellation/steps/LossSummaryStep.tsx
  - leanshot/src/components/dashboard/settings/cancellation/OfferCard.tsx
  - leanshot/src/components/dashboard/settings/cancellation/PauseControls.tsx
  - leanshot/src/lib/cancellation/decide-offer-client.ts
  - leanshot/src/lib/cancellation/accept-offer-client.ts
  - leanshot/src/lib/analytics/events.ts
  - leanshot/src/App.tsx
  - leanshot/src/components/dashboard/settings/SettingsPage.tsx
  - leanshot/scripts/assert-bundle-budget.sh
  - supabase/functions/cancellation-feedback-to-ticket/index.ts
  - supabase/functions/cancellation-feedback-to-ticket/index.test.ts
autonomous: true
must_haves:
  truths:
    - "CancellationModal.tsx + steps/ + OfferCard.tsx + PauseControls.tsx live under ONE directory and import non-lazy from CancellationModal.tsx so Vite produces a SINGLE 'cancellation' chunk (per CONTEXT specifics + RESEARCH §Pitfall 8)"
    - "App.tsx registers CancellationModal via React.lazy with default-export shimming (per PATTERNS §9); 40-04 is the SINGLE writer to App.tsx for Phase 40 modal additions"
    - "SettingsPage renders a 'Cancel subscription' Button variant=ghost tone=danger that sets the modal-open flag (existing settings-action pattern)"
    - "leanshot/src/types/cancellation.ts is the SINGLE-WRITER source-of-truth for client-side OfferType / OfferConfig / DecideOfferResponse / AcceptOfferRequest types (mirrors supabase/functions/_shared/cancellation-types.ts shape per PATTERNS §'TS types one-writer rule')"
    - "analytics/events.ts extended with cancellation_started, cancellation_reason_picked, save_offer_shown, save_offer_accepted, save_offer_declined, cancellation_dismissed, cancellation_aborted, cancellation_completed events; subscription_paused, subscription_resumed events also registered"
    - "Modal is keyboard-accessible: role='dialog' + aria-modal='true' + aria-labelledby + aria-describedby; ESC closes Step 1 + Step 3 only (Step 2 prompts close-confirmation per UI-SPEC); reduced-motion respected"
    - "decide-offer + accept-offer Edge Fns called via lib/cancellation/*-client.ts with user JWT from supabase.auth.getSession() (NOT service-role)"
    - "cancellation-feedback-to-ticket Edge Fn forwards user JWT (NOT service-role) to call create_ticket_with_first_message RPC (per PATTERNS §14 + RESEARCH §Pitfall 4)"
    - "Bundle budget script adds entry 'cancellation 13' (kB gz ceiling per UI-SPEC)"
    - "Loss-summary tile uses MedLevelChart thumbnail (existing P35 component) — NO new chart code"
    - "Cancellation-scheduled toast: 6s undo window; on undo → cancel the Stripe cancel_at_period_end; on commit → fire D-21 ticket-create Fn IF reason==='service_quality_issue'"
  artifacts:
    - path: "leanshot/src/components/dashboard/settings/cancellation/CancellationModal.tsx"
      provides: "Three-step state machine + Modal primitive wrapper"
      min_lines: 80
    - path: "leanshot/src/lib/analytics/events.ts"
      provides: "9 new event names registered"
    - path: "supabase/functions/cancellation-feedback-to-ticket/index.ts"
      provides: "User-JWT-forwarding helpdesk ticket-create caller (D-21)"
    - path: "leanshot/scripts/assert-bundle-budget.sh"
      provides: "cancellation chunk ceiling 13 kB gz"
  key_links:
    - from: "leanshot/src/App.tsx"
      to: "CancellationModal lazy chunk"
      via: "const CancellationModalLazy = lazy(() => import('@/components/dashboard/settings/cancellation/CancellationModal').then(m => ({ default: m.CancellationModal })))"
      pattern: "CancellationModal"
    - from: "leanshot/src/components/dashboard/settings/SettingsPage.tsx"
      to: "App.tsx modal-open state (via existing settings → app prop hook OR Zustand action)"
      via: "setCancellationOpen(true)"
      pattern: "setCancellationOpen"
    - from: "lib/cancellation/decide-offer-client.ts"
      to: "POST /functions/v1/cancellation-decide-offer"
      via: "fetch + supabase.auth.getSession().access_token"
      pattern: "/functions/v1/cancellation-decide-offer"
    - from: "lib/cancellation/accept-offer-client.ts"
      to: "POST /functions/v1/cancellation-accept-offer"
      via: "fetch + JWT bearer"
      pattern: "/functions/v1/cancellation-accept-offer"
    - from: "CancellationModal Step 3 'Cancel anyway' → undo expires"
      to: "cancellation-feedback-to-ticket Fn (only if reason==='service_quality_issue')"
      via: "fetch with user JWT"
      pattern: "/functions/v1/cancellation-feedback-to-ticket"
---

<objective>
Ship the user-facing cancellation flow: a single lazy-chunked Modal with the three-step UX (reason → server-picked offer → loss-summary), plus the D-21 service-quality-issue → P37 helpdesk ticket Edge Fn. The Modal calls the 40-03 Edge Fns; this plan owns NO business logic — only the UX wiring and the ticket-create pathway.

Purpose: D-17..D-21 (modal UX + D-21 helpdesk feedback loop) become real. Owner of the single shared edit to `leanshot/src/App.tsx` (lazy registration); owner of `src/types/cancellation.ts` (client-side type contract); owner of the `cancellation` bundle-budget entry.

Output: 8 React/TS files + 1 Edge Fn + 1 test + bundle-budget update. Wave 2; depends on 40-01 (schema must exist for the Fns 40-03 calls); parallel-safe with 40-03 (disjoint file sets except for both consuming the type contract — but each owns a separate file with the same shape).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-CONTEXT.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-RESEARCH.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-PATTERNS.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-UI-SPEC.md
@leanshot/src/components/changelog/WhatsNewDrawer.tsx
@leanshot/src/components/nps/QuarterlyNPSModal.tsx
@leanshot/src/components/dashboard/settings/DeleteAccountModal.tsx
@leanshot/src/components/ui/Modal.tsx
@leanshot/src/lib/analytics/events.ts
@supabase/migrations/20270707000009_helpdesk_create_ticket_rpc.sql

<interfaces>
<!-- leanshot/src/types/cancellation.ts — client-side single writer. Mirror of Deno-side _shared/cancellation-types.ts. -->
export type OfferType = 'pause' | 'discount' | 'extended_trial' | 'downgrade' | 'contact_csm';
export type TenureBucket = '<30d' | '30-180d' | '>180d';
export type CancellationReason =
  | 'too_expensive' | 'not_using' | 'found_alternative'
  | 'health_goals_changed' | 'temporary_break'
  | 'service_quality_issue' | 'other';
export type OfferConfig =
  | { type: 'pause'; pause_months: 1 | 2 | 3 }
  | { type: 'discount'; coupon_id: string; percent_off: number; duration_in_months: 2 | 3 }
  | { type: 'extended_trial'; extension_days: 7 | 14 | 30 }
  | { type: 'downgrade'; target: 'price_monthly' }
  | { type: 'contact_csm' };
export type IneligibleCode =
  | 'OFFER_INELIGIBLE_LIFETIME_CAP'
  | 'OFFER_INELIGIBLE_COOLDOWN'
  | 'OFFER_INELIGIBLE_NO_RULE'
  | 'NO_SUBSCRIPTION';
export interface DecideOfferResponse {
  offer_id: string | null;
  offer_type: OfferType | null;
  offer_config: OfferConfig | null;
  stacking: { existing_pct: number; capped_pct: number } | null;
  ineligible_code: IneligibleCode | null;
}
export interface AcceptOfferRequest {
  offer_id: string;
  pause_months_override?: 1 | 2 | 3;
}

<!-- create_ticket_with_first_message RPC signature (per PATTERNS §14, verified at 20270707000009_helpdesk_create_ticket_rpc.sql) -->
public.create_ticket_with_first_message(
  p_subject text,    -- max 200 chars
  p_body text,
  p_priority text    -- 'p1'|'p2'|'p3'|'p4'
) returns uuid;
-- Requires authenticated user context (auth.uid() referenced inside RPC body)
-- Forwards user JWT, NOT service-role (per Pitfall 4)

<!-- App.tsx lazy-modal registration pattern (per PATTERNS §9 line 357-368) -->
const CancellationModalLazy = lazy(() =>
  import('@/components/dashboard/settings/cancellation/CancellationModal').then((m) => ({
    default: m.CancellationModal,
  })),
);
// Conditional render in App.tsx near existing modal slots (~lines 1870-1890):
{cancellationOpen && (
  <Suspense fallback={null}>
    <CancellationModalLazy onClose={() => setCancellationOpen(false)} />
  </Suspense>
)}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + Modal + 3 step components + OfferCard + PauseControls (single chunk per PATTERNS §9 + UI-SPEC Surface 1)</name>
  <files>leanshot/src/types/cancellation.ts, leanshot/src/components/dashboard/settings/cancellation/CancellationModal.tsx, leanshot/src/components/dashboard/settings/cancellation/steps/ReasonPicklistStep.tsx, leanshot/src/components/dashboard/settings/cancellation/steps/OfferStep.tsx, leanshot/src/components/dashboard/settings/cancellation/steps/LossSummaryStep.tsx, leanshot/src/components/dashboard/settings/cancellation/OfferCard.tsx, leanshot/src/components/dashboard/settings/cancellation/PauseControls.tsx, leanshot/src/lib/cancellation/decide-offer-client.ts, leanshot/src/lib/cancellation/accept-offer-client.ts</files>
  <action>
    Write `leanshot/src/types/cancellation.ts` with the types block from &lt;interfaces&gt; verbatim. Re-export from `leanshot/src/types/index.ts` barrel via `export * from './cancellation'`.

    Write `lib/cancellation/decide-offer-client.ts` — `export async function callDecideOffer({ reason, reason_other_text }): Promise&lt;DecideOfferResponse&gt;` — uses `supabase.auth.getSession()` to extract `access_token` then `fetch(\`${import.meta.env.VITE_SUPABASE_URL}/functions/v1/cancellation-decide-offer\`, { method: 'POST', headers: { Authorization: \`Bearer ${access_token}\`, 'Content-Type': 'application/json', 'Cache-Control': 'no-store' }, body: JSON.stringify({ reason, reason_other_text }) })`. Parse response; on non-200 throw. Mirror the existing Edge-Fn-client pattern from `lib/ai.ts` or `lib/helpdesk/*` — verify via `ls leanshot/src/lib/` and grep for similar fetch wrappers. NOTE: per CLAUDE.md Configuration section, VITE_SUPABASE_URL is NOT currently set in `.env.local`. Per `reference_vite_static_env_inlining` use literal `import.meta.env.VITE_SUPABASE_URL` (not dynamic key access). If the constant is missing at runtime, throw 'CONFIG_MISSING' — this is acceptable because the cancellation modal is only reachable for authenticated users with a Stripe subscription (must have completed P14 onboarding which would have set the env var).

    Write `lib/cancellation/accept-offer-client.ts` — `callAcceptOffer({ offer_id, pause_months_override? }): Promise&lt;AcceptOfferResponse&gt;` — same pattern.

    Write `OfferCard.tsx` per UI-SPEC Surface 1 Step 2 (lines 146-167). Props: `{ offerType: OfferType; offerConfig: OfferConfig; stacking: DecideOfferResponse['stacking']; onAccept(): void; onDecline(): void; isPending: boolean }`. Internal structure per UI-SPEC: 32×32 lucide icon in 48×48 primary-soft circle → headline → body copy → details strip → conditional stacking notice strip → footer CTAs. Icon mapping per UI-SPEC Surface 1 line 159 (PauseCircle/BadgePercent/CalendarPlus/ArrowDownToLine/Headset). All copy verbatim from UI-SPEC Copywriting Contract Surface 1 (lines 348-415). `aria-busy={isPending}` on the Accept button. Reduced-motion handled via existing `useReducedMotion()` hook.

    Write `PauseControls.tsx` per UI-SPEC Surface 1 line 167: `<PillGroup>` for 1/2/3 month preset selection, default 2 months. Props: `{ value: 1|2|3; onChange(v: 1|2|3): void; resumesAt: string }`. Renders the "Resumes {date}" details strip live-updated.

    Write `ReasonPicklistStep.tsx` per UI-SPEC Surface 1 Step 1 (lines 119-145). Vertical stack of 7 `<Pill role="radio">` rows; roving tabindex pattern from existing `Pill.tsx`. When reason=7 (Other), expand Textarea below; require 4-280 chars trimmed; show char counter at 240+. Props: `{ onSubmit(reason: CancellationReason, reason_other_text?: string): void; onKeep(): void }`. Wire `cancellation_reason_picked` analytics on submit (per Task 2).

    Write `OfferStep.tsx` per UI-SPEC Surface 1 Step 2. Calls `callDecideOffer({ reason, reason_other_text })` on mount; shows skeleton (per UI-SPEC line 547) for ≤800ms. On response: if `ineligible_code` present → render the anti-gaming EmptyState (UI-SPEC lines 173-179); else render `<OfferCard>`. On Accept: calls `callAcceptOffer({ offer_id, pause_months_override })`, fires `save_offer_accepted` analytics, advances to Step 3 with success toast (per UI-SPEC Toast copy lines 409-414). On Decline: fires `save_offer_declined`, advances to Step 3. Props: `{ reason: CancellationReason; reason_other_text?: string; onAdvance(): void }`.

    Write `LossSummaryStep.tsx` per UI-SPEC Surface 1 Step 3 (lines 180-200). 2×2 grid: Streak (from `useStreaks` P35 hook) | Med-level chart preview (280×120 `MedLevelChart` thumbnail — import from `@/components/dashboard/charts/MedLevelChart`) | AI coach count (from `useStore((s) => s.aiMessages.length)`) | Data export reminder static tile. NO countdown timers. Buttons: "Keep my account" (primary, fires `cancellation_aborted`) | "Cancel anyway" (ghost danger, fires the cancellation-scheduled toast w/ 6s undo). On undo expiration: call Stripe `cancel_at_period_end` via existing `lib/stripe-billing.ts` (verify path via `grep -rn 'cancel_at_period_end' leanshot/src/lib/`); if reason==='service_quality_issue', also POST to `/functions/v1/cancellation-feedback-to-ticket` with user JWT.

    Write `CancellationModal.tsx` — outer wrapper:
      - Props: `{ onClose(): void }`.
      - State: `step: 1 | 2 | 3`, `reason: CancellationReason | null`, `reason_other_text: string | undefined`.
      - Uses `<Modal>` primitive `size="lg"`, `aria-labelledby="cancel-step-title"` `aria-describedby="cancel-step-body"`.
      - ESC handler: enabled on Step 1 + Step 3; on Step 2 shows inline close-confirmation per UI-SPEC line 207.
      - Renders one of 3 step components based on `step`. Non-lazy imports (single chunk).
      - Step indicator (8×8 dot trio) at top. `aria-current="step"` on active dot.
      - Fires `cancellation_started` on mount.
      - Reduced-motion: step transitions instant via `useReducedMotion()`.

    Critical bundle discipline: ALL step files + OfferCard + PauseControls use NON-LAZY imports from CancellationModal.tsx. Vite chunks the directory tree as ONE because the outer Modal is the lazy entry. Per PATTERNS §9 + RESEARCH §Pitfall 8 + CONTEXT specifics.

    Implements D-17, D-18, D-19, D-20, partial D-22 (variant_id can be threaded via decide-offer response in future — v1.3 leaves null).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npx tsc -p tsconfig.app.json --noEmit &amp;&amp; npx eslint src/components/dashboard/settings/cancellation/ src/lib/cancellation/ src/types/cancellation.ts</automated>
    <manual>Playwright MCP smoke (deferred to 40-06 HUMAN-UAT per project posture — no vitest in leanshot/): open SettingsPage → click Cancel subscription → walk through 3 steps with reason='Too expensive' → verify offer card renders + Accept dispatches to Stripe + accepted toast appears.</manual>
  </verify>
  <done>tsc passes with zero errors; eslint clean; `grep -rn 'react-router\|useNavigate' leanshot/src/components/dashboard/settings/cancellation/` returns zero matches (single-chunk modal, not routes); `grep -c "export function CancellationModal\|export const CancellationModal" leanshot/src/components/dashboard/settings/cancellation/CancellationModal.tsx` ≥ 1.</done>
</task>

<task type="auto">
  <name>Task 2: App.tsx lazy registration + SettingsPage trigger + analytics events + bundle budget entry</name>
  <files>leanshot/src/App.tsx, leanshot/src/components/dashboard/settings/SettingsPage.tsx, leanshot/src/lib/analytics/events.ts, leanshot/scripts/assert-bundle-budget.sh</files>
  <action>
    EXTEND `leanshot/src/App.tsx` ONE-WRITER (per PATTERNS §"Cross-Phase Coordination" + feedback_planner_iter1_anti_patterns):
      - Add `const CancellationModalLazy = lazy(() => import('@/components/dashboard/settings/cancellation/CancellationModal').then((m) => ({ default: m.CancellationModal })))` near the existing lazy modal block (~lines 135-149 where WhatsNewDrawerHost and QuarterlyNPSModalLazy live).
      - Add `const [cancellationOpen, setCancellationOpen] = useState(false)` near the existing modal-open state.
      - Render `{cancellationOpen && (<Suspense fallback={null}><CancellationModalLazy onClose={() => setCancellationOpen(false)} /></Suspense>)}` near the existing modal slot block (~lines 1870-1890; mirror NPS modal's conditional-render shape).
      - Pass `setCancellationOpen` to SettingsPage via existing settings-action prop pattern (verify via grep `setSettingsOpen\|onCancelClick` in App.tsx; mirror the analogous prop wiring).

    EXTEND `SettingsPage.tsx`:
      - Add `<Button variant="ghost" tone="danger" onClick={() => onCancelClick()}>Cancel subscription</Button>` in the Account section. Visibility gate: only render when user has an active Stripe subscription — verify the existing pattern via `grep -n 'isSubscribed\|hasSubscription' leanshot/src/components/dashboard/settings/SettingsPage.tsx`; if no such gate exists, conditionally render based on `useStore((s) => s.user?.subscription_id)` or similar; if the project does not yet expose subscription state at SettingsPage, the button is always rendered and the Edge Fn returns NO_SUBSCRIPTION which the Modal handles gracefully.

    EXTEND `leanshot/src/lib/analytics/events.ts`:
      - Register 9 new event names (per CONTEXT canonical_refs line 115):
        cancellation_started, cancellation_reason_picked, save_offer_shown, save_offer_accepted, save_offer_declined, cancellation_dismissed, cancellation_aborted, cancellation_completed, subscription_paused, subscription_resumed.
      - Mirror the existing event-registration pattern (likely `export const ANALYTICS_EVENTS = { ... } as const` + `export type AnalyticsEventName = keyof typeof ANALYTICS_EVENTS`); inspect file first via `head -60 leanshot/src/lib/analytics/events.ts`.

    EXTEND `leanshot/scripts/assert-bundle-budget.sh`:
      - Add a new line into the `CHUNK_CONFIG` array (alphabetical position — between `admin-shell` and `course-player` based on the existing entries observed at lines 44-50):
        `"cancellation       13 Plan 40-04 baseline — three-step modal single chunk. If regressed, lazy-split OfferCard animation via sync-defer.ts or remove framer-motion from loss-summary tile fade-in."`
      - Verify the array syntax via reading the existing entries first (`Read` the script).

    Implements D-22 partial (analytics groundwork; PostHog Ship-Winner wiring lives in 40-06).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npx tsc -p tsconfig.app.json --noEmit &amp;&amp; npm run build 2>&amp;1 | tail -40 &amp;&amp; bash scripts/assert-bundle-budget.sh cancellation</automated>
    <manual>Open SettingsPage in dev; verify "Cancel subscription" button visible for active subscribers; verify clicking opens the modal (lazy chunk loads &lt; 300ms on local dev server).</manual>
  </verify>
  <done>`npm run build` succeeds; `bash scripts/assert-bundle-budget.sh cancellation` reports OK (chunk ≤ 13 kB gz; may also report `MISSING` if the build did not produce the chunk yet — acceptable for first build but the dev build with the modal-opened code path must produce a non-MISSING chunk before 40-06 close-out); `grep -c "CancellationModalLazy" leanshot/src/App.tsx` ≥ 2 (declaration + usage); `grep -c "cancellation_started" leanshot/src/lib/analytics/events.ts` ≥ 1.</done>
</task>

<task type="auto">
  <name>Task 3: cancellation-feedback-to-ticket Edge Fn (user JWT forwarding per PATTERNS §14 + Pitfall 4)</name>
  <files>supabase/functions/cancellation-feedback-to-ticket/index.ts, supabase/functions/cancellation-feedback-to-ticket/index.test.ts</files>
  <action>
    Write `supabase/functions/cancellation-feedback-to-ticket/index.ts` per PATTERNS §14 verbatim code block (lines 567-599). Key invariants:
      - Require authenticated user (NOT service-role). Per Pitfall 4 — `create_ticket_with_first_message` references `auth.uid()` and other authenticated-session columns; service-role call would NULL out and the RPC raises 'unauthenticated' exception.
      - Validate `reason === 'service_quality_issue'`; reject 400 'wrong_reason' otherwise (defense — the client only calls this Fn for that reason path).
      - Build `userClient = createClient(SUPABASE_URL, SUPABASE_ANON_KEY, { global: { headers: { Authorization: \`Bearer ${userJwt}\` } } })`.
      - Call `userClient.rpc('create_ticket_with_first_message', { p_subject, p_body, p_priority: 'p3' })`.
      - Subject: `'Feedback from cancellation: service quality issue'` (≤200 chars per RPC line 30).
      - Body: `reason_other_text ?? 'User cited service quality issue at cancellation. No additional detail.'`. Truncate to 4000 chars defensively.
      - Tag the resulting ticket: `userClient.from('tickets').update({ tags: ['cancellation-feedback', 'sentiment:negative'] }).eq('id', ticketId)`. Hardcode sentiment='negative' (per PATTERNS §14 gotcha — simpler than running helpdesk-ai-assist sentiment scoring).
      - PHI flag: NOT passed; RPC derives it from clinic_id lookup internally (per PATTERNS §14 gotcha).
      - Return `jsonResponse(200, { ticket_id: ticketId })`.
      - try/catch with Sentry breadcrumb (err.message only, NEVER body content — PII risk per ASVS V8).

    Write `index.test.ts` covering: (a) service-role bearer → 401 (RPC returns null auth.uid() — assert 500 or specific 'unauthenticated' propagation); (b) valid user JWT + reason='service_quality_issue' → RPC called with correct subject+body, tickets UPDATE called with both tags; (c) reason='too_expensive' → 400 'wrong_reason' (defensive validation); (d) reason_other_text > 4000 chars → truncated.

    Implements D-21.
  </action>
  <verify>
    <automated>$HOME/.deno/bin/deno test --no-check supabase/functions/cancellation-feedback-to-ticket/</automated>
  </verify>
  <done>Deno test green; `grep -c "create_ticket_with_first_message" supabase/functions/cancellation-feedback-to-ticket/index.ts` == 1; `grep -c "SUPABASE_SERVICE_ROLE_KEY" supabase/functions/cancellation-feedback-to-ticket/index.ts` == 0 (no service-role import — user JWT only per Pitfall 4); `grep -c "cancellation-feedback" supabase/functions/cancellation-feedback-to-ticket/index.ts` ≥ 1 (tag literal).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→cancellation-decide-offer / accept-offer | user JWT from supabase.auth.getSession (40-04 client) → 40-03 Fn |
| browser→cancellation-feedback-to-ticket | user JWT (RPC requires auth.uid context — service-role would fail) |
| Modal→SettingsPage→App.tsx | local Zustand or prop state; no external trust |
| App.tsx lazy chunk load | Vite-emitted JS; same-origin; CSP self |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04-01 | Information Disclosure | reason_other_text contains PHI/sensitive info displayed in admin support_admin context | mitigate | reason_other_text rendered only inside admin role-gated context (40-05 + 40-06); never in PostHog event vars (only event names); admin role gate enforced at DB layer via RLS |
| T-40-04-02 | Tampering | Client manipulates DOM to call accept-offer with arbitrary offer_id | mitigate | 40-03 Fn re-verifies offer_id ownership via JWT auth.uid (T-40-03-01); client-side defense is best-effort only |
| T-40-04-03 | Spoofing | service-role token used by accident to call cancellation-feedback-to-ticket | mitigate | Per PATTERNS §14 + Pitfall 4 — RPC requires auth.uid; service-role gets null auth.uid → exception raised; test asserts this fails |
| T-40-04-04 | Denial of Service | User repeatedly opens/closes modal triggering decide-offer Fn (creates a log row each time per UI-SPEC line 211 `cancellation_dismissed`) | accept | Decide-offer log rows are 'offered'-status (not 'accepted') so don't hit cap; rate-limit naturally bounded by Supabase Edge Fn quota; v1.3 accepts; if abuse seen, add per-IP throttle in dispatcher |
| T-40-04-05 | Repudiation | User clicks Cancel anyway but undo within 6s — was the cancel scheduled or not? | mitigate | Toast undo behavior is local-only until 6s commit; commit fires a SINGLE Stripe call (idempotent via cancel_at_period_end which is itself idempotent); user-visible state matches Stripe state |
| T-40-04-06 | Information Disclosure | Sentry/console.log accidentally logs reason_other_text | mitigate | Strict ESLint custom rule: do not console.log raw form values inside cancellation/ tree; sentry breadcrumb scrubs via P25 mask layer |
| T-40-04-07 | Tampering | Bundle regression — CancellationModal imported eagerly somewhere blows the cancellation chunk into the dashboard chunk | mitigate | Bundle-budget script entry 'cancellation 13' kB ceiling; CI fails on regression (per project_phase5_bundle_regression); plan owns the budget addition |
</threat_model>

<verification>
- `npm run build` succeeds with no TypeScript errors
- `bash scripts/assert-bundle-budget.sh` reports cancellation chunk ≤ 13 kB gz (after dev build; MISSING acceptable initially)
- `grep -rn 'react-router\|useNavigate\|navigate(' leanshot/src/components/dashboard/settings/cancellation/` returns zero (single-chunk, NOT routes per Pitfall 8)
- `grep -c "CancellationModalLazy" leanshot/src/App.tsx` ≥ 2
- `grep -c "SUPABASE_SERVICE_ROLE_KEY" supabase/functions/cancellation-feedback-to-ticket/index.ts` == 0
- All 9 analytics event names registered in `leanshot/src/lib/analytics/events.ts`
- Modal a11y attributes: `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, `aria-describedby` all present
</verification>

<success_criteria>
- POLISH-01 user-facing modal flows end-to-end (Step 1 → 2 → 3 → cancel-or-keep)
- D-21 service-quality-issue path creates a P37 helpdesk ticket on cancellation commit
- Bundle ceiling enforced; cancellation chunk lazy-loaded
- Analytics events instrumented for 40-06 ROI dashboard consumption
- Single writer to App.tsx (no merge conflict surface for parallel Wave 2 plans)
</success_criteria>

<output>
After completion, create `.planning/phases/40-cancellation-save-offers-flow/40-04-SUMMARY.md` capturing: (a) actual cancellation chunk size in kB gz at commit-time (from build output); (b) whether SettingsPage subscription-state gate was added or modal handles NO_SUBSCRIPTION; (c) any Playwright MCP findings during local smoke; (d) confirmation that `grep` for legacy event names returns clean.
</output>
