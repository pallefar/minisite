---
phase: 40
plan: 06
type: execute
wave: 3
title: "Admin ROI Dashboard + CSV export + PostHog Ship-Winner + phase close-out"
requirements: [POLISH-02]
depends_on: ["40-01", "40-02", "40-03", "40-04", "40-05"]
files_modified:
  - supabase/migrations/20270709000008_p40_roi_view.sql
  - supabase/tests/p40_roi_view_test.sql
  - supabase/functions/download-cancellation-roi-csv/index.ts
  - supabase/functions/download-cancellation-roi-csv/index.test.ts
  - leanshot/src/components/admin/cancellation/CancellationRoiTab.tsx
  - leanshot/src/components/admin/cancellation/CancellationRoiKpiTiles.tsx
  - leanshot/src/components/admin/cancellation/CancellationRoiChart.tsx
  - leanshot/src/components/admin/cancellation/CancellationCohortTable.tsx
  - leanshot/src/lib/cancellation/posthog-variants.ts
autonomous: false
must_haves:
  truths:
    - "v_cancellation_offers_roi VIEW (plain SQL, NOT matview) exists with offered_day + offer_type + cohort_id + tenure_bucket grouping; deferred_mrr_cents_est computed per RESEARCH §ROI"
    - "View inherits cancellation_offers_log RLS (admin-only SELECT via base table policy); SECDEF view wrapper NOT required (per PATTERNS §11)"
    - "Threshold for matview migration: p95 > 500ms OR row-count > 10000 — documented inline in view comment; not implemented in v1.3"
    - "download-cancellation-roi-csv Edge Fn: admin-role-gated; streams CSV via Response(ReadableStream); Content-Type: text/csv; charset=utf-8; Content-Disposition attachment"
    - "ROI dashboard chart uses BaseChart wrapper (P33 pattern); chunked stacked-bar of shown/accepted/declined per offer_type"
    - "PostHog variant attribution: posthog_variant_id stored on cancellation_offers_log; CancellationRoiKpiTiles can group by variant when present (cold-start: all null)"
    - "Phase close-out: supabase db push --linked succeeds; ALL Phase 40 Edge Fns deployed (supabase functions deploy <name> --import-map supabase/functions/import_map.json); bundle-budget green; HUMAN-UAT checkpoint passes"
    - "Stripe coupon seed Fn invoked once at close-out: POST /functions/v1/cancellation-seed-coupons with service-role bearer; verify Stripe Dashboard shows 6 coupons"
  artifacts:
    - path: "supabase/migrations/20270709000008_p40_roi_view.sql"
      provides: "v_cancellation_offers_roi VIEW + indexes"
    - path: "supabase/functions/download-cancellation-roi-csv/index.ts"
      provides: "admin CSV export endpoint"
    - path: "leanshot/src/components/admin/cancellation/CancellationRoiTab.tsx"
      provides: "ROI tab content — KPI tiles + chart + cohort table + export button"
  key_links:
    - from: "CancellationRoiTab.tsx"
      to: "v_cancellation_offers_roi VIEW"
      via: "supabase.from('v_cancellation_offers_roi').select('*').filter(...)"
      pattern: "v_cancellation_offers_roi"
    - from: "CancellationRoiTab.tsx Export button"
      to: "download-cancellation-roi-csv Edge Fn"
      via: "fetch + admin JWT"
      pattern: "/functions/v1/download-cancellation-roi-csv"
    - from: "CancellationRoiKpiTiles.tsx"
      to: "posthog-variants.ts variant grouping"
      via: "groupBy posthog_variant_id"
      pattern: "posthog_variant_id"
---

<objective>
Close out Phase 40 with the admin ROI dashboard (POLISH-02), CSV export, PostHog Ship-Winner wiring, and the phase-close-out tasks: `supabase db push --linked` + Edge Fn deploys + bundle audit + HUMAN-UAT walkthrough.

Purpose: POLISH-02 completes when admin sees per-cohort offer-take ROI in the dashboard. Phase 35 Plan 35-10's close-out template (plus feedback_phase_close_out_db_push_verification — Phase 37 lesson: DO NOT skip the db push) is the gold standard — replicated here as a BLOCKING task.

Output: 1 migration (view + indexes) + 1 pgTAP test + 1 Edge Fn + 4 React components + 1 client lib + the phase close-out checkpoint. Wave 3 — depends on EVERYTHING in Phase 40 (40-01..05 all merged).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-CONTEXT.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-RESEARCH.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-PATTERNS.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-UI-SPEC.md
@leanshot/src/components/admin/AdminMetricsMrrChart.tsx
@leanshot/src/components/dashboard/charts/BaseChart.tsx
@supabase/functions/bulk-csv-export/index.ts

<interfaces>
<!-- v_cancellation_offers_roi VIEW shape per RESEARCH §Code Examples lines 929-950 + PATTERNS §11 -->

create or replace view public.v_cancellation_offers_roi as
select
  date_trunc('day', offered_at) as offered_day,
  offer_type,
  cohort_snapshot->>'cohort_id' as cohort_id,
  tenure_bucket,
  posthog_variant_id,
  count(*) filter (where status = 'offered')  as shown_count,
  count(*) filter (where status = 'accepted') as accepted_count,
  count(*) filter (where status = 'declined') as declined_count,
  sum(case when status='accepted' and offer_type in ('discount','pause','extended_trial')
           then coalesce((offer_config->>'percent_off')::numeric, 0) * 12.99
           else 0 end) as deferred_mrr_cents_est
from public.cancellation_offers_log
where org_id is null  -- v1.3 excludes clinic-org per RESEARCH Open Q3 (clinic-CSM counted separately)
group by 1, 2, 3, 4, 5;
-- COMMENT ON VIEW: 'Phase 40 ROI. Matview migration threshold: p95 > 500ms OR row count > 10000.'

<!-- CSV export Fn Response shape -->
return new Response(csvStream, {
  status: 200,
  headers: {
    'Content-Type': 'text/csv; charset=utf-8',
    'Content-Disposition': `attachment; filename="cancellation-roi-${yyyymmdd}.csv"`,
    'Cache-Control': 'private, no-store',
    ...corsHeaders,
  },
});
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ROI view migration + pgTAP aggregation test (per PATTERNS §11, RESEARCH §ROI)</name>
  <files>supabase/migrations/20270709000008_p40_roi_view.sql, supabase/tests/p40_roi_view_test.sql</files>
  <action>
    Write `supabase/migrations/20270709000008_p40_roi_view.sql` per &lt;interfaces&gt; verbatim. Add inline comment `'Phase 40 ROI. Matview migration threshold: p95 > 500ms OR row count > 10000 — see 40-RESEARCH §ROI Analytics Architecture.'`. The view inherits the base-table RLS policy from `cancellation_offers_log` (admin SELECT only) per PATTERNS §11 gotcha — no separate SECDEF wrapper.

    Note: the WHERE clause excludes `org_id IS NOT NULL` rows (clinic-org cancellations) per RESEARCH Open Q3 — clinic-CSM counted separately in v1.3 due to variable per-patient billing math. Document this in the view COMMENT.

    Write `supabase/tests/p40_roi_view_test.sql` pgTAP — seed 10 cancellation_offers_log rows across 2 cohorts × 4 offer_types × mixed statuses; assert view returns correct counts:
      - 2 rows with status='offered', offer_type='discount', cohort_A → expect shown_count=2 in group (cohort_A, discount, *)
      - 1 of those rows status='accepted' → accepted_count=1
      - 1 declined → declined_count=1
      - deferred_mrr_cents_est computed correctly for accepted discount row at 25%: 25 * 12.99 = 324.75
      - clinic-org rows EXCLUDED (org_id IS NOT NULL): seed 1 such row + assert it does NOT appear in view output
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; supabase db query --linked --file supabase/tests/p40_roi_view_test.sql</automated>
  </verify>
  <done>Migration applies; pgTAP test green with ≥4 assertions covering shown/accepted/declined/deferred_mrr math + clinic-org exclusion.</done>
</task>

<task type="auto">
  <name>Task 2: ROI dashboard tab components + CSV export Fn + PostHog variant attribution (per PATTERNS §11, UI-SPEC Surface 3)</name>
  <files>leanshot/src/components/admin/cancellation/CancellationRoiTab.tsx, leanshot/src/components/admin/cancellation/CancellationRoiKpiTiles.tsx, leanshot/src/components/admin/cancellation/CancellationRoiChart.tsx, leanshot/src/components/admin/cancellation/CancellationCohortTable.tsx, leanshot/src/lib/cancellation/posthog-variants.ts, supabase/functions/download-cancellation-roi-csv/index.ts, supabase/functions/download-cancellation-roi-csv/index.test.ts</files>
  <action>
    Write `CancellationRoiTab.tsx` per UI-SPEC Surface 3 layout (lines 281-340):
      - Heading + sub-label.
      - Filter bar: Date range PillGroup ("Last 7 d" / "Last 30 d" default / "Last 90 d" / "All time"), Cohort MultiSelect, Offer type PillGroup multi-select. Filters drive state passed to all child components.
      - Hero KPI row: `<CancellationRoiKpiTiles />` — 4 tiles (Offers shown / Acceptance rate / Revenue recovered / Avg retention extension). Each tile uses `--text-xl` Fraunces display per UI-SPEC line 64 ROI hero exception.
      - Chart: `<CancellationRoiChart />`.
      - Cohort table: `<CancellationCohortTable />`.
      - Export button (top-right header row) → fetch `/functions/v1/download-cancellation-roi-csv?range=...&cohorts=...&offers=...` with admin JWT → triggers browser download via toast confirm.
      - Empty state per UI-SPEC line 333-335.

    Write `CancellationRoiKpiTiles.tsx` — query `supabase.from('v_cancellation_offers_roi').select('*')` with date-range filter; aggregate client-side to compute the 4 KPIs:
      - Offers shown: SUM(shown_count) over range
      - Acceptance rate: SUM(accepted_count) / SUM(shown_count) * 100 (in pp; show as percentage)
      - Revenue recovered: SUM(deferred_mrr_cents_est) (presented as USD)
      - Avg retention extension: AVG over accepted rows of (offer_config.duration_in_months || pause_months || extension_days/30) — query separately as it needs offer_config jsonb shape
      - Δ vs prior period: query the immediately-prior date window with same filters; compute pp/% delta; render as Badge tone='success' or 'danger' per UI-SPEC line 302.

    Write `CancellationRoiChart.tsx` per UI-SPEC Surface 3 "Chart" section (lines 306-318):
      - Uses `BaseChart` wrapper (existing — `leanshot/src/components/dashboard/charts/BaseChart.tsx`).
      - Type: `bar` with `indexAxis: 'x'`.
      - 3 stacked series per bar: accepted (--color-primary) + declined (--color-text-tertiary) + shown_no_response (--color-border, computed as shown - accepted - declined).
      - X-axis: 4 columns (Pause / Discount / Extended trial / Downgrade).
      - 320 px on md:, 280 px on mobile.
      - Custom HTML legend below chart per UI-SPEC line 318 (chart.js inline legend has color-token integration issues per P33 lesson).
      - Theme-aware via `var(--color-*)`.

    Write `CancellationCohortTable.tsx` per UI-SPEC Surface 3 "Cohort breakdown table" (lines 319-330):
      - Rows = cohort. Columns: Cohort | Pause % | Discount % | Extended trial % | Downgrade % | Best offer.
      - Acceptance rate cells: bold when ≥ 30%.
      - "Best offer" column: Badge tone=info with offer icon + name.
      - Sticky first column on mobile horizontal scroll.

    Write `lib/cancellation/posthog-variants.ts` — wiring for D-22 PostHog Experiments + Ship-Winner. Skeleton:
      - `export async function getActiveCancellationVariant(): Promise<{ variant_id: string; variant_name: string } | null>` — calls `posthog.getFeatureFlag('cancellation_save_flow_variant')` or similar; returns variant assignment.
      - For v1.3 cold-start: returns null (no variants active yet). Wire the threading from decide-offer client (40-04) → variant_id in DecideOfferRequest body → decide Fn (40-03) stores in cancellation_offers_log.posthog_variant_id column → ROI dashboard groups by it.
      - NOTE: The body-passing requires extending 40-03's decide Fn to accept `posthog_variant_id` in the request body. This is a minor schema extension to DecideOfferRequest; if 40-03 has shipped without it, file a small follow-up in the 40-06 SUMMARY and ship v1.3 with variant_id always null. Per RESEARCH Open Q5 + D-22 (Ship-Winner is future-work-ready, not v1.3-ship-blocking).

    Write `supabase/functions/download-cancellation-roi-csv/index.ts` per PATTERNS §11:
      - JWT bearer auth + admin role check (`hasMinRole` equivalent server-side: SELECT FROM admin_roles_view).
      - Query the view with the filters from URL search params (?range, ?cohorts, ?offers).
      - Stream rows via `Response(new ReadableStream({ ... }))` — push header row + each result row as CSV-encoded string. Mirror existing `supabase/functions/bulk-csv-export/` pattern.
      - Response headers per &lt;interfaces&gt; — Content-Type, Content-Disposition with filename including yyyymmdd.

    Write `index.test.ts` — mock view query results; assert response is `text/csv`; assert first line is the header row; assert row count matches mock data; assert non-admin JWT → 403.

    Implements POLISH-02 (ROI dashboard) + D-22 (PostHog wiring scaffolding).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npx tsc -p tsconfig.app.json --noEmit &amp;&amp; npx eslint src/components/admin/cancellation/ src/lib/cancellation/ &amp;&amp; $HOME/.deno/bin/deno test --no-check supabase/functions/download-cancellation-roi-csv/</automated>
  </verify>
  <done>tsc + eslint + Deno test all green; `grep -c "v_cancellation_offers_roi" leanshot/src/components/admin/cancellation/CancellationRoiTab.tsx` ≥ 1; chart and KPI tiles render in dev with empty-state when no data; CSV download triggers browser save dialog in manual smoke.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Phase 40 close-out — db push + Edge Fn deploys + bundle audit + HUMAN-UAT walkthrough (per feedback_phase_close_out_db_push_verification + feedback_multi_signal_human_verify_checkpoint_pattern)</name>
  <what-built>
    Phase 40 complete end-to-end build:
    - 8 migrations (40-01 schema + 40-02 webhook/cron + 40-05 RPCs + 40-06 view)
    - 5 new Edge Fns: cancellation-seed-coupons, pause-reminder-fire, cancellation-decide-offer, cancellation-accept-offer, cancellation-feedback-to-ticket, download-cancellation-roi-csv (6 total)
    - 1 existing Edge Fn extended: stripe-webhook/events/subscription-updated.ts
    - 1 new lazy chunk: cancellation (modal)
    - 1 admin module: /admin/cancellation with Rules + ROI tabs
    - All Stripe-side state: 6 coupons SAVE-{20,25,30}-{2,3}MO seeded
  </what-built>
  <how-to-verify>
    The CLI-runnable items are AUTO-VERIFY (the executor MUST run these before flipping the checkpoint to "approved"); the browser items are HUMAN-VERIFY (operator walks through):

    **Auto-verify (executor runs these inline; report results in the resume-signal):**

    1. `cd /Users/karstenhaldan/minisite && supabase db push --linked` — must succeed with NO "Skipping" warnings. Per reference_supabase_migration_filename_regex.
       - If a back-dated migration file blocks per reference_supabase_back_dated_migration_blocks_push, surface the offending filename in the checkpoint output.
    2. Deploy each Edge Fn (omitting `--linked` per reference_supabase_functions_deploy_no_linked_flag; passing `--import-map` per reference_supabase_functions_deploy_import_map_flag):
       ```
       for fn in cancellation-seed-coupons pause-reminder-fire cancellation-decide-offer cancellation-accept-offer cancellation-feedback-to-ticket download-cancellation-roi-csv stripe-webhook; do
         supabase functions deploy "$fn" --import-map supabase/functions/import_map.json
       done
       ```
    3. Seed the Stripe coupons (one-time): `curl -X POST -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" "https://ytnsipxxmzgaebkqmokp.supabase.co/functions/v1/cancellation-seed-coupons"` — expect first run `{ created: 6, skipped: 0 }`, idempotent re-run `{ created: 0, skipped: 6 }`.
    4. `cd /Users/karstenhaldan/minisite/leanshot && npm run build && bash scripts/assert-bundle-budget.sh` — overall script must report OK; `cancellation` chunk ≤ 13 kB gz; admin chunk delta within ceiling.
    5. Full Deno sweep: `cd /Users/karstenhaldan/minisite && $HOME/.deno/bin/deno test --no-check supabase/functions/` — green per feedback_post_merge_deno_sweep_pattern.
    6. Cron registration check: `supabase db query --linked --command "select jobname, schedule, active from cron.job where jobname like 'p40-%'"` — returns 2 rows both active.
    7. Stripe coupons live: `curl -H "Authorization: Bearer $STRIPE_SECRET_KEY" "https://api.stripe.com/v1/coupons?limit=10" | jq -r '.data[] | select(.id | startswith("SAVE-")) | .id'` — returns all 6 SAVE-* IDs.

    **Human-verify (operator walks through in browser; signals "approved (browser walked)" once complete OR "deferred — automated-verify-only" per feedback_hitl_walkthrough_deferred_when_fixtures_missing if no test subscription is available):**

    A. **Cancellation modal end-to-end (consumer)**: Log in as a test user with an active Stripe subscription. Settings → Account → "Cancel subscription". Walk Step 1 (pick "Too expensive"), Step 2 (verify offer card renders with the appropriate icon per offer_type — pause should show PauseCircle + 1/2/3 month pills; discount should show BadgePercent + the coupon details strip), Step 3 (verify 4 loss-summary tiles + Cancel anyway flow with 6s undo). Cancel anyway → wait 6s → verify Stripe subscription shows cancel_at_period_end=true.
    B. **Pause apply + webhook mirror**: From Step 2 with offer_type='pause', accept. Verify (a) toast shows correct Resume date, (b) Stripe Dashboard shows pause_collection set, (c) `select paused_until, is_paused from subscriptions where id='<sub>'` reflects the pause.
    C. **D-15 stacking notice**: Set up a test user with an active affiliate 10% coupon. Trigger cancellation with reason='too_expensive' AND a rule assigning SAVE-30-3MO. Verify the stacking notice strip appears in Step 2 reading "Your existing 10% referral discount caps this save-offer at 35% effective." (or similar UI-SPEC line 382 copy with computed numbers).
    D. **Admin ROI dashboard**: Log in as admin. Visit /admin/cancellation → ROI tab. Verify 4 KPI tiles render (will show 0s + empty state if no real cancellations yet — that's the empty state per UI-SPEC line 333). Export CSV → file downloads.
    E. **Admin rule editor**: /admin/cancellation → Rules tab. Create a new rule with offer_type='discount' + coupon='SAVE-25-3MO' + tenure='30-180d' + reason='too_expensive' + priority=10 + active=true. Verify it appears in rule list. Toggle active off → on. Drag-reorder. Delete (with 6s undo → let it commit).
    F. **D-21 helpdesk ticket**: Trigger a cancellation with reason='service_quality_issue'. After 6s undo window expires, verify (a) a new ticket appears in /admin/helpdesk with subject "Feedback from cancellation: service quality issue" and tags including 'cancellation-feedback' + 'sentiment:negative', (b) the user receives the standard P37 ticket-confirmation email.
    G. **HUMAN-UAT belt-and-suspenders (optional per RESEARCH §"Runtime State Inventory")**: Stripe Dashboard → Developers → Webhooks → existing endpoint → confirm subscribed events include `customer.subscription.updated` (which they already are per P14). RESEARCH recommendation: do NOT add `customer.subscription.paused` / `.resumed` subscriptions because they will NEVER fire for pause_collection (the deferred-item from CONTEXT is dropped per RESEARCH override).
  </how-to-verify>
  <resume-signal>
    Reply with ONE of these discrete signals (per feedback_multi_signal_human_verify_checkpoint_pattern — multi-signal lets operator approve CLI-runnable items inline and carry browser-only items to milestone close):

    - "approved — full walkthrough" → all auto-verify + all human-verify items confirmed green; phase ships.
    - "approved — automated-verify-only" → all CLI auto-verify items green; browser walk-throughs deferred to a staging/production test-subscription pass per feedback_hitl_walkthrough_deferred_when_fixtures_missing. Phase 40 CARRY-OVER.md captures the deferred browser items.
    - "blocked — auto-verify failed: <which item>" → executor halts; describe the failure for triage.
    - "blocked — browser walkthrough failed: <which item + what was wrong>" → executor halts; addresses the defect in a follow-up plan or inline fix per feedback_inline_fix_over_replan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser→v_cancellation_offers_roi | RLS on base table; view inherits admin-only SELECT |
| admin browser→download-cancellation-roi-csv | JWT bearer + server-side admin role check |
| operator→supabase db push --linked | CLI requires operator credentials; ensures migration timestamp ordering |
| operator→supabase functions deploy | requires linked project; deploys to production Edge Fn slot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06-01 | Information Disclosure | Non-admin user queries v_cancellation_offers_roi directly via supabase-js | mitigate | View inherits cancellation_offers_log RLS (admin-only SELECT); non-admin gets empty result set |
| T-40-06-02 | Information Disclosure | CSV export streams data to non-admin caller | mitigate | Edge Fn JWT + server-side `hasMinRole(role, 'admin')` check; 403 on sub-admin |
| T-40-06-03 | Information Disclosure | CSV contains reason_other_text PII (free text from cancelling users) | mitigate | reason_other_text rendered ONLY in admin context; admin role gate enforced; PII is acceptable disclosure to admins (consent implied by ToS) |
| T-40-06-04 | Tampering | Operator skips `supabase db push --linked` at close-out → migrations sit unapplied → Phase 40 silently dead in production | mitigate | This BLOCKING checkpoint task asserts the push; per feedback_phase_close_out_db_push_verification — Phase 37 lesson |
| T-40-06-05 | Denial of Service | View aggregation on cancellation_offers_log becomes slow as table grows beyond 10k rows | accept | RESEARCH §ROI documents the matview migration threshold; v1.3 acceptable at p95 < 500ms; revisit at 30-day post-launch metrics |
| T-40-06-06 | Tampering | PostHog variant_id from client could be spoofed | mitigate | v1.3 ships variant_id=null cold-start; future-work variant attribution must be server-resolved via PostHog server-side flag fetch (per P34 D-20 Ship-Winner pattern) — flag for v1.4 |
| T-40-06-07 | Information Disclosure | Stripe coupon seed leaks coupon metadata to non-admin Stripe Dashboard viewer | accept | coupon IDs (SAVE-{20,25,30}-{2,3}MO) are non-secret; admin context only for Stripe Dashboard access |
</threat_model>

<verification>
- ROI view applies + pgTAP aggregation test green
- CSV export Fn admin-gated + Deno test green
- All 8 Phase 40 migrations apply via `supabase db push --linked` with no "Skipping" warnings
- All 6 Phase 40 Edge Fns deploy via `supabase functions deploy <name> --import-map ...`
- Bundle audit: cancellation chunk ≤ 13 kB gz; overall budget script reports OK
- Stripe coupon seed: 6 coupons live in Stripe (auto-verifiable via `/v1/coupons` API)
- Cron registration: `p40-pause-t7-reminder` + `p40-pause-autoresume-reconcile` both active in cron.job table
- HUMAN-UAT signaled "approved" (one of the discrete signals)
</verification>

<success_criteria>
- POLISH-02 admin ROI dashboard live; admin sees offer-take rate per cohort × offer_type
- All 4 Phase 40 requirements (POLISH-01..04) closed
- Phase 40 deployed end-to-end (migrations + Fns + frontend chunks)
- No carry-over blockers beyond explicit HUMAN-UAT deferrals (which are captured in 40-CARRY-OVER.md if any)
- Bundle budget enforced; bundle-CI green
</success_criteria>

<output>
After completion, create `.planning/phases/40-cancellation-save-offers-flow/40-06-SUMMARY.md` capturing: (a) ROI view p95 query latency (manual EXPLAIN ANALYZE if measurable); (b) all 6 Edge Fns deployed (with deploy timestamps); (c) Stripe Dashboard coupon presence verified; (d) HUMAN-UAT signal received; (e) any browser-walk-through items deferred to 40-CARRY-OVER.md (per feedback_partial_phase_carry_over_md_pattern); (f) PostHog Ship-Winner wiring status (v1.3 cold-start vs ready-for-v1.4 variants).

Also update `.planning/ROADMAP.md` Phase 40 "Status: complete" + checkboxes for POLISH-01..04. Also update `.planning/REQUIREMENTS.md` POLISH-01..04 rows to "Complete".
</output>
