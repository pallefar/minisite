---
phase: 40
plan: 03
type: execute
wave: 2
title: "Edge Fns — cancellation-decide-offer (lookup) + cancellation-accept-offer (Stripe write)"
requirements: [POLISH-01, POLISH-03, POLISH-04]
depends_on: ["40-01"]
files_modified:
  - supabase/functions/_shared/cancellation-types.ts
  - supabase/functions/cancellation-decide-offer/index.ts
  - supabase/functions/cancellation-decide-offer/index.test.ts
  - supabase/functions/cancellation-decide-offer/resolve-rule.ts
  - supabase/functions/cancellation-decide-offer/resolve-rule.test.ts
  - supabase/functions/cancellation-decide-offer/anti-gaming.ts
  - supabase/functions/cancellation-decide-offer/anti-gaming.test.ts
  - supabase/functions/cancellation-decide-offer/clinic-fork.test.ts
  - supabase/functions/cancellation-decide-offer/log-insert.test.ts
  - supabase/functions/cancellation-accept-offer/index.ts
  - supabase/functions/cancellation-accept-offer/index.test.ts
  - supabase/functions/cancellation-accept-offer/apply-pause.ts
  - supabase/functions/cancellation-accept-offer/apply-pause.test.ts
  - supabase/functions/cancellation-accept-offer/apply-discount.ts
  - supabase/functions/cancellation-accept-offer/apply-discount.test.ts
  - supabase/functions/cancellation-accept-offer/extend-pause.ts
  - supabase/functions/cancellation-accept-offer/extend-pause-counter.test.ts
  - supabase/functions/cancellation-accept-offer/apply-extended-trial.ts
  - supabase/functions/cancellation-accept-offer/apply-downgrade.ts
autonomous: true
must_haves:
  truths:
    - "cancellation-decide-offer is a JWT-authenticated Edge Fn (user-callable, NOT service-role); inserts log row directly via service-role admin client (NO SECDEF RPC — per Pitfall 4)"
    - "Decide-offer is lookup-only (p99 ≤ 100ms target — no Stripe API round-trip) — returns OFFER_INELIGIBLE_* codes or offer payload + offer_id"
    - "cancellation-accept-offer is JWT-authenticated; reads the offer_id, validates ownership via user_id, calls Stripe API based on offer_type, INSERTs a SECOND log row (status='accepted' linked via offered_log_id) per PATTERNS §6 option (a) 2-row append"
    - "D-01 tenure gating: <30d → pause+discount only; 30-180d → all 4; >180d → no extended_trial"
    - "D-02 2-lifetime-take cap; pause EXEMPT from cap; discount + extended_trial + downgrade each count"
    - "D-03 12-month cooldown between non-pause takes; pause initial does NOT trigger cooldown but extended pause via D-10 DOES"
    - "D-04 clinic-org fork: org_type='clinic' → only contact_csm + discount offered; never pause/extended/downgrade (enforced server-side, validated by clinic-fork.test.ts)"
    - "D-15 stacking clamp: clampSavePct(existingPct, savePct) caps combined effective at 35% (STACKING_CAP_EFFECTIVE=0.35); applies ONLY to offer_type='discount' (extended_trial/pause/downgrade exempt per Open Q5 resolution)"
    - "Coupon stacking uses Stripe discounts[] array APPEND (NOT singular discount field overwrite — per Pitfall 2); existing affiliate coupons preserved"
    - "All Stripe SDK calls pin apiVersion: '2026-04-22.dahlia' and import from https://esm.sh/stripe@19?target=denonext"
    - "PostHog flush in finally for any captureServer() use (per PATTERNS §'PostHog flush in finally')"
    - "Sentry breadcrumbs log err.message + err.code only; NEVER Stripe payload"
    - "OfferConfig + OfferType + DecideOfferRequest + DecideOfferResponse + AcceptOfferRequest TS types live in supabase/functions/_shared/cancellation-types.ts (Deno-side single-writer per PATTERNS §'TS types one-writer rule'); 40-04 owns the parallel leanshot/src/types/cancellation.ts file"
  artifacts:
    - path: "supabase/functions/_shared/cancellation-types.ts"
      provides: "Deno-side type contracts: OfferType, OfferConfig, DecideOfferRequest/Response, AcceptOfferRequest/Response, IneligibleCode"
      exports: ["OfferType", "OfferConfig", "DecideOfferRequest", "DecideOfferResponse", "AcceptOfferRequest", "IneligibleCode"]
    - path: "supabase/functions/cancellation-decide-offer/index.ts"
      provides: "POST /functions/v1/cancellation-decide-offer — returns offer_id + offer_type + offer_config OR ineligible code"
    - path: "supabase/functions/cancellation-decide-offer/resolve-rule.ts"
      provides: "resolveRule(admin, {user_id, tenureBucket, reason, isClinicOrg}) → save_offer_rules row OR null (priority ASC LIMIT 1)"
    - path: "supabase/functions/cancellation-decide-offer/anti-gaming.ts"
      provides: "clampSavePct + cap/cooldown checks; D-15 cap math per RESEARCH lines 591-611 verbatim"
      exports: ["clampSavePct", "checkLifetimeCap", "checkCooldown", "STACKING_CAP_EFFECTIVE"]
    - path: "supabase/functions/cancellation-accept-offer/index.ts"
      provides: "POST /functions/v1/cancellation-accept-offer — dispatches to apply-{pause|discount|extended_trial|downgrade}; writes accepted log row"
    - path: "supabase/functions/cancellation-accept-offer/apply-pause.ts"
      provides: "Stripe pause_collection apply for 1/2/3-month preset"
      exports: ["applyPause"]
    - path: "supabase/functions/cancellation-accept-offer/apply-discount.ts"
      provides: "Stripe discounts[] array APPEND preserving affiliates"
      exports: ["applyDiscount"]
    - path: "supabase/functions/cancellation-accept-offer/extend-pause.ts"
      provides: "D-10 pause-extension — Stripe pause_collection.resumes_at update; increments take counter"
      exports: ["extendPause"]
  key_links:
    - from: "cancellation-decide-offer index.ts"
      to: "anti-gaming.ts clampSavePct + checkLifetimeCap + checkCooldown"
      via: "imported helpers"
      pattern: "import.*clampSavePct"
    - from: "cancellation-decide-offer index.ts"
      to: "cancellation_offers_log INSERT (service-role admin client)"
      via: "admin.from('cancellation_offers_log').insert({...status: 'offered'})"
      pattern: "cancellation_offers_log.*offered"
    - from: "cancellation-accept-offer index.ts"
      to: "Stripe SDK pause/discounts/items mutation"
      via: "Stripe@19 pinned at 2026-04-22.dahlia"
      pattern: "stripe\\.subscriptions\\.update"
    - from: "cancellation-accept-offer index.ts"
      to: "cancellation_offers_log INSERT (status='accepted', offered_log_id FK)"
      via: "2-row append pattern per PATTERNS §6 option (a)"
      pattern: "status:\\s*'accepted'"
---

<objective>
Ship the two Edge Functions that drive the cancellation save-flow server-side: a fast lookup-only `cancellation-decide-offer` (≤100ms; reads rules, applies anti-gaming, returns a single offer) and a Stripe-write `cancellation-accept-offer` (≤2s; pause/discount/extended-trial/downgrade dispatch). Split per RESEARCH Open Q1: the modal shows a skeleton during accept while keeping decide fast so the offer card renders instantly.

Purpose: every business rule (D-01..D-04 eligibility, D-15 stacking clamp, D-10 pause-extend counter) is enforced server-side here. The modal in 40-04 is a thin client — it never sees rules, cooldowns, or coupon math.

Output: Two Edge Fns + shared type contract + 9 test files. Implements 11 of 19 VALIDATION rows (40-XX-01..05, 11, 16, 17, 18). Wave 2 — depends on 40-01 schema; parallel-safe with 40-04 modal (no overlapping files since 40-04 owns the leanshot/-side type file separately).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-CONTEXT.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-RESEARCH.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-PATTERNS.md
@leanshot/.planning/phases/40-cancellation-save-offers-flow/40-01-PLAN.md
@supabase/functions/recommend-next-best-action/index.ts
@supabase/functions/helpdesk-ai-assist/index.ts
@supabase/functions/stripe-webhook/index.ts
@supabase/functions/_shared/lifecycle-utils.ts

<interfaces>
<!-- _shared/cancellation-types.ts — Deno-side single-writer; mirrored in 40-04's leanshot/src/types/cancellation.ts -->
export type OfferType = 'pause' | 'discount' | 'extended_trial' | 'downgrade' | 'contact_csm';

export type TenureBucket = '<30d' | '30-180d' | '>180d';

export type CancellationReason =
  | 'too_expensive' | 'not_using' | 'found_alternative'
  | 'health_goals_changed' | 'temporary_break'
  | 'service_quality_issue' | 'other';

export type OfferConfig =
  | { type: 'pause'; pause_months: 1 | 2 | 3 }
  | { type: 'discount'; coupon_id: string; percent_off: number; duration_in_months: 2 | 3 }
  | { type: 'extended_trial'; extension_days: 7 | 14 | 30 }
  | { type: 'downgrade'; target: 'price_monthly' }
  | { type: 'contact_csm' };

export type IneligibleCode =
  | 'OFFER_INELIGIBLE_LIFETIME_CAP'
  | 'OFFER_INELIGIBLE_COOLDOWN'
  | 'OFFER_INELIGIBLE_NO_RULE'
  | 'NO_SUBSCRIPTION';

export interface DecideOfferRequest {
  reason: CancellationReason;
  reason_other_text?: string;     // required if reason === 'other'
}

export interface DecideOfferResponse {
  offer_id: string | null;        // log-row uuid; null when ineligible
  offer_type: OfferType | null;
  offer_config: OfferConfig | null;
  stacking: { existing_pct: number; capped_pct: number } | null;
  ineligible_code: IneligibleCode | null;
}

export interface AcceptOfferRequest {
  offer_id: string;
  // Pause: client passes the selected preset (overrides offer_config.pause_months if user picked a different tile in UI Surface 1 Step 2 pill group)
  pause_months_override?: 1 | 2 | 3;
}

export interface AcceptOfferResponse {
  accepted: boolean;
  resumes_at?: string;            // ISO when offer_type='pause'
  next_invoice_date?: string;     // ISO when discount/extended_trial
  applied_coupon_id?: string;     // when offer_type='discount'
}

// Constants
export const STACKING_CAP_EFFECTIVE = 0.35;
export const COOLDOWN_DAYS = 365;
export const LIFETIME_CAP_NON_PAUSE = 2;

<!-- Stripe API import pattern (per PATTERNS §"Stripe SDK init") -->
import Stripe from 'https://esm.sh/stripe@19?target=denonext';
const stripe = new Stripe(Deno.env.get('STRIPE_SECRET_KEY')!, {
  apiVersion: '2026-04-22.dahlia' as Stripe.LatestApiVersion,
});

<!-- User-bearer auth pattern (per PATTERNS §6 — copy from helpdesk-ai-assist/index.ts ~line 236) -->
const authHeader = req.headers.get('Authorization');
if (!authHeader?.startsWith('Bearer ')) return jsonError(401, 'unauthenticated');
const userJwt = authHeader.slice(7);
// Verify JWT via supabase-js userClient — extract auth.uid()

<!-- clampSavePct math (per RESEARCH lines 591-611 verbatim) -->
function clampSavePct(existingPct: number, savePct: number): {
  finalSavePct: number; clamped: boolean;
  preClampEffective: number; postClampEffective: number;
} {
  const preEffective = 1 - (1 - existingPct) * (1 - savePct);
  if (preEffective <= STACKING_CAP_EFFECTIVE) {
    return { finalSavePct: savePct, clamped: false,
             preClampEffective: preEffective, postClampEffective: preEffective };
  }
  const finalSavePct = Math.max(0, 1 - (1 - STACKING_CAP_EFFECTIVE) / (1 - existingPct));
  return { finalSavePct, clamped: true,
           preClampEffective: preEffective, postClampEffective: STACKING_CAP_EFFECTIVE };
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared types + cancellation-decide-offer Fn skeleton + resolve-rule + anti-gaming (per PATTERNS §6, RESEARCH §Code Examples lines 850-925)</name>
  <files>supabase/functions/_shared/cancellation-types.ts, supabase/functions/cancellation-decide-offer/index.ts, supabase/functions/cancellation-decide-offer/resolve-rule.ts, supabase/functions/cancellation-decide-offer/resolve-rule.test.ts, supabase/functions/cancellation-decide-offer/anti-gaming.ts, supabase/functions/cancellation-decide-offer/anti-gaming.test.ts, supabase/functions/cancellation-decide-offer/index.test.ts, supabase/functions/cancellation-decide-offer/clinic-fork.test.ts, supabase/functions/cancellation-decide-offer/log-insert.test.ts</files>
  <action>
    First write `supabase/functions/_shared/cancellation-types.ts` with the types + constants block from &lt;interfaces&gt; verbatim. This file is the Deno-side single source of truth (per PATTERNS §"TS types one-writer rule"); the leanshot/src/types/cancellation.ts file (owned by 40-04) mirrors these types for the client. NO cross-import — Deno fns and leanshot/ Vite app use separate type files because they have different path-resolution semantics.

    Write `supabase/functions/cancellation-decide-offer/anti-gaming.ts` with:
      - export `STACKING_CAP_EFFECTIVE = 0.35` + `COOLDOWN_DAYS = 365` + `LIFETIME_CAP_NON_PAUSE = 2`.
      - export `clampSavePct(existingPct: number, savePct: number)` — implementation EXACTLY as the RESEARCH lines 591-611 block in &lt;interfaces&gt;. Returns `{ finalSavePct, clamped, preClampEffective, postClampEffective }`.
      - export `checkLifetimeCap(priorTakes: Array&lt;{ offer_type: OfferType }&gt;): { exceeded: boolean; nonPauseCount: number }` — counts where `offer_type !== 'pause'`; exceeded when count ≥ 2.
      - export `checkCooldown(priorTakes: Array&lt;{ taken_at: string; offer_type: OfferType }&gt;): { onCooldown: boolean; lastTakenAt: string | null }` — looks at the most recent `taken_at` across non-pause takes; on cooldown if within 365 days.
      - export `applyTenureGate(tenureBucket: TenureBucket, offerType: OfferType): boolean` — D-01 gating: returns true if offer is eligible for this bucket. &lt;30d: only pause/discount. 30-180d: all four. &gt;180d: no extended_trial.

    Write `anti-gaming.test.ts` with explicit Deno.test blocks per PATTERNS §"Pitfall 5":
      - clampSavePct: existing=0.1, save=0.25 → preEffective=0.325, clamped=false (under cap), finalSavePct=0.25
      - clampSavePct: existing=0.1, save=0.30 → preEffective=0.37, clamped=true, postClampEffective=0.35, finalSavePct=1 - (1-0.35)/(1-0.1) = 0.2778 (assert with epsilon 0.001)
      - clampSavePct: existing=0.40 (above cap) → finalSavePct=0 (offer cannot beat cap; caller routes to pause)
      - clampSavePct: existing=0, save=0.25 → preEffective=0.25, clamped=false, finalSavePct=0.25 (no affiliate)
      - checkLifetimeCap: 2 non-pause takes → exceeded=true; 1 non-pause + 5 pauses → exceeded=false (D-02 pause exemption)
      - checkCooldown: most-recent non-pause take 30 days ago → onCooldown=true; 400 days ago → onCooldown=false
      - applyTenureGate('&lt;30d', 'extended_trial') → false; applyTenureGate('30-180d', 'extended_trial') → true; applyTenureGate('&gt;180d', 'extended_trial') → false

    Write `resolve-rule.ts` with:
      - `export async function resolveRule(admin, { user_id, tenureBucket, reason, isClinicOrg })` returning matching `save_offer_rules` row or null.
      - SQL: `SELECT * FROM save_offer_rules WHERE active = true AND (tenure_buckets = '{}' OR :tenureBucket = ANY(tenure_buckets)) AND (reasons = '{}' OR :reason = ANY(reasons)) AND (org_type = 'any' OR org_type = :userOrgType) ORDER BY priority ASC LIMIT 1` — empty array means "match any" per &lt;interfaces&gt; from 40-01.
      - Also do cohort filtering: if cohort_id is set on a rule, verify the user is in that cohort via P27 `cohort_membership` matview (per A6 assumption) — `EXISTS (SELECT 1 FROM cohort_membership WHERE user_id = :user_id AND cohort_id = rule.cohort_id)`. Combine into the WHERE clause using `(cohort_id IS NULL OR EXISTS(...))`.

    Write `resolve-rule.test.ts` — mock admin client to return 4 rule rows with mixed priorities + tenure/reason filters; assert (a) highest-priority matching rule wins, (b) inactive rules excluded, (c) tenure-mismatch excludes, (d) clinic-only rule excludes consumer caller, (e) cohort-scoped rule excludes non-member.

    Write `supabase/functions/cancellation-decide-offer/index.ts` — entry. Skeleton (~150 lines) based on RESEARCH §Code Examples lines 850-925 + PATTERNS §6 user-bearer pattern. Steps:
      1. CORS preflight via `_shared/lifecycle-utils.ts` corsHeaders.
      2. Verify Bearer JWT via `createClient(SUPABASE_URL, ANON_KEY, { global: { headers: { Authorization: authHeader } } })` then `await userClient.auth.getUser()` → extract `user.id`. On null user → `jsonError(401, 'unauthenticated')`. Service-role admin client (`makeLazyAdmin()`) for the rest of the work.
      3. Parse body: `{ reason, reason_other_text }`. Validate reason ∈ 7 enum values; if reason='other', require reason_other_text ≥ 4 trimmed chars. Else 400.
      4. Read `profiles` (org_id) + `subscriptions` (id, created_at, clinic_id) for user.id. No sub → `jsonError(404, { code: 'NO_SUBSCRIPTION' })`.
      5. Compute tenureBucket from `created_at`.
      6. Read priorTakes: `admin.from('cancellation_offers_log').select('id, taken_at, offer_type').eq('user_id', user.id).eq('status', 'accepted')`. Apply `checkLifetimeCap` → if exceeded, log ineligible row + return `{ ineligible_code: 'OFFER_INELIGIBLE_LIFETIME_CAP', ... }`. Apply `checkCooldown` → if on cooldown, log ineligible row + return `{ ineligible_code: 'OFFER_INELIGIBLE_COOLDOWN', ... }`.
      7. Determine `isClinicOrg`: `subscription.clinic_id IS NOT NULL` (per D-04). NOTE: do NOT query `orgs.type` separately — the subscription.clinic_id column is the canonical clinic-org flag set by P14 + P29.
      8. Call `resolveRule(admin, { user_id, tenureBucket, reason, isClinicOrg })`. If null → log row with offer_type='none' status='offered' + return `{ ineligible_code: 'OFFER_INELIGIBLE_NO_RULE' }`.
      9. Apply `applyTenureGate(tenureBucket, rule.offer_type)` — if false (rule misconfigured for this bucket — shouldn't happen but defense-in-depth), log + return NO_RULE.
      10. If `rule.offer_type === 'discount'`: read existing Stripe discount % via `stripe.subscriptions.retrieve(subscription.id, { expand: ['discounts'] })` — sum existing `percent_off / 100` across discount objects. Call `clampSavePct(existingPct, rule.coupon_id_percent / 100)`. If `finalSavePct === 0` → fallback to pause (recursively re-resolve with offer_type filter OR per RESEARCH line 900 simply return INELIGIBLE_NO_RULE + log; pick fallback-to-pause per Open Q1 spirit: call `admin.from('save_offer_rules').select('*').eq('active', true).eq('offer_type', 'pause').order('priority').limit(1).single()`; if no pause rule exists, return NO_RULE). Update `effectiveOfferConfig.percent_off` to clamped value. NOTE: Per RESEARCH Open Q5 — clamp applies ONLY to discount, not extended_trial.
      11. Build `offer_config: OfferConfig` from rule (pause_months / coupon_id+percent_off / extension_days / target).
      12. INSERT cancellation_offers_log with status='offered', cohort_snapshot, tenure_bucket, prior_takes_count, stacking_pre_clamp_pct/post_clamp_pct/clamped, rule_id, posthog_variant_id (NULL for v1.3 cold-start — 40-06 wires Ship-Winner). Use `.select().single()` to get the inserted offer_id.
      13. Return `DecideOfferResponse` JSON. Cache-Control: `private, no-store`.
      14. Wrap entire body in `try { ... } catch (err) { Sentry.captureException(err); return jsonError(500, 'decide_failed'); } finally { await shutdownPostHog(); }`.

    Write `index.test.ts` (happy path: returns offer for a 60-day-tenure consumer with reason='too_expensive'); `clinic-fork.test.ts` (clinic-org caller asks for offer; assert only contact_csm or discount rule returned, never pause/extended/downgrade); `log-insert.test.ts` (D-18 — assert reason captured even when no rule matches; assert reason_other_text persisted when reason='other').

    Implements D-01, D-02, D-03, D-04, D-15 (decide-side), D-18, D-19. VALIDATION rows 40-XX-01, 02, 03, 04, 05, 18.
  </action>
  <verify>
    <automated>$HOME/.deno/bin/deno test --no-check supabase/functions/cancellation-decide-offer/</automated>
  </verify>
  <done>All 5 test files green with ≥3 assertions each; `grep -c 'STACKING_CAP_EFFECTIVE\s*=\s*0\.35' supabase/functions/cancellation-decide-offer/anti-gaming.ts` == 1; `grep -c "case 'pause'\|case 'discount'\|case 'extended_trial'\|case 'downgrade'\|case 'contact_csm'" supabase/functions/cancellation-decide-offer/index.ts` ≥ 0 (dispatcher logic uses offer_type field rather than switch — that's OK; verify by checking the index returns the rule offer_type unchanged); `grep -rn "subscriptions.update\|subscriptions.retrieve" supabase/functions/cancellation-decide-offer/index.ts` == 1 (only the read for stacking check; NO write to Stripe from decide Fn).</done>
</task>

<task type="auto">
  <name>Task 2: cancellation-accept-offer Fn + apply-pause + apply-discount + extend-pause + apply-extended-trial + apply-downgrade (per PATTERNS §4, §5, RESEARCH §Code Examples lines 733-784)</name>
  <files>supabase/functions/cancellation-accept-offer/index.ts, supabase/functions/cancellation-accept-offer/index.test.ts, supabase/functions/cancellation-accept-offer/apply-pause.ts, supabase/functions/cancellation-accept-offer/apply-pause.test.ts, supabase/functions/cancellation-accept-offer/apply-discount.ts, supabase/functions/cancellation-accept-offer/apply-discount.test.ts, supabase/functions/cancellation-accept-offer/extend-pause.ts, supabase/functions/cancellation-accept-offer/extend-pause-counter.test.ts, supabase/functions/cancellation-accept-offer/apply-extended-trial.ts, supabase/functions/cancellation-accept-offer/apply-downgrade.ts</files>
  <action>
    Write `apply-pause.ts` per RESEARCH lines 736-756 verbatim: `applyPause(subscriptionId: string, pauseMonths: 1|2|3, stripe: Stripe): Promise&lt;{ resumesAt: number }&gt;`. Compute `now + pauseMonths * 30 * 24 * 60 * 60` unix seconds; call `stripe.subscriptions.update(subscriptionId, { pause_collection: { behavior: 'void', resumes_at: resumesAt } })`. Wrap in try/catch — on Stripe error (A5: trialing sub may reject pause), `throw new Error('PAUSE_FAILED:' + err.code)`. Caller handles fallback.

    Write `apply-pause.test.ts` — assert correct unix timestamps for 1/2/3 month presets; assert Stripe call shape; assert error wrapping on trial-state rejection.

    Write `apply-discount.ts` per PATTERNS §5 + RESEARCH lines 411-422 verbatim: `applyDiscount(subscriptionId, couponId, stripe)`. (1) `stripe.subscriptions.retrieve(subscriptionId, { expand: ['discounts'] })`. (2) `const existing = sub.discounts.map(d => ({ coupon: typeof d.coupon === 'string' ? d.coupon : d.coupon.id }))` — handle A1 ambiguity (coupon may be string OR Stripe.Coupon object). (3) `stripe.subscriptions.update(subscriptionId, { discounts: [...existing, { coupon: couponId }] })`. NEVER use the legacy singular `discount:` field (per Pitfall 2). The 35% clamp already happened in decide-offer; couponId here is the FINAL clamped coupon (may differ from rule.coupon_id if clamp rounded — but for v1.3 the 6-coupon catalog is fixed so clamp returns a percent that may not match any catalog entry → in that case decide-offer falls back to the closest catalog entry under the clamp; document this in the SUMMARY).

    Actually re-reading: the catalog is FIXED 6 combos. The clamp can produce a non-catalog percent. Resolution: when `clamped=true`, decide-offer chooses the CLOSEST catalog coupon ≤ finalSavePct (so the clamp can only REDUCE the offer, never raise it above cap). E.g., 27.78% clamp → pick SAVE-25-{2,3}MO. Document this in apply-discount.ts header comment + add assert in decide-offer.

    Write `apply-discount.test.ts` — assert existing discounts preserved (start with [{coupon:'REF-10'}] → after apply, discounts contains BOTH); assert NEVER calls singular `discount:` field (grep test); assert A1 dual-shape handling (string coupon AND Coupon object both produce correct `{ coupon: id }` shape).

    Write `extend-pause.ts` per RESEARCH lines 763-784 verbatim: `extendPause(subscriptionId, extraMonths, stripe)`. (1) `retrieve` current `pause_collection.resumes_at`. (2) Throw `Error('SUB_NOT_PAUSED')` if currentResumesAt is null. (3) `newResumesAt = currentResumesAt + extraMonths * 30 * 24 * 60 * 60`. (4) `update({ pause_collection: { behavior: 'void', resumes_at: newResumesAt } })`. Important per D-10: extending a pause counts as a NEW pause-take and DOES increment the lifetime cap counter (despite D-02's "pause exempt" — extensions are the exception to the exception). See `extend-pause-counter.test.ts`.

    Write `extend-pause-counter.test.ts` per PATTERNS §"Pitfall 5":
      - Scenario A: User has 0 prior takes, makes initial 3-month pause. Counter should remain 0 (D-02 pause exemption applies to INITIAL pauses).
      - Scenario B: User has 0 prior takes, makes initial pause (counter=0), then 7 days before resume extends by 1 month. Counter increments to 1 (D-10).
      - Scenario C: User at counter=1 from a prior extension, extends again. Counter increments to 2. Next extension attempt → BLOCKED with OFFER_INELIGIBLE_LIFETIME_CAP from decide-offer.
      - This test exercises the integration between accept-offer's `incrementCounterIfExtension` logic and decide-offer's `checkLifetimeCap`.

    Write `apply-extended-trial.ts` — Stripe `stripe.subscriptions.update(subscriptionId, { trial_end: <new-unix>, proration_behavior: 'none' })` extending the trial. Verify against Stripe types — extension_days is from offer_config; compute newTrialEnd = current trial_end (or now if not in trial) + extension_days * 86400. NO test file required if pattern is trivial.

    Write `apply-downgrade.ts` — Stripe API sequence: `stripe.subscriptions.update(subscriptionId, { items: [{ id: subItem.id, price: 'price_monthly' }], proration_behavior: 'none', billing_cycle_anchor: 'now' })` swapping annual → monthly at next renewal. Per CONTEXT Discretion + RESEARCH discussion. Implements D-01 "Annual → Monthly is the primary downgrade path".

    Write `cancellation-accept-offer/index.ts` — entry. Skeleton:
      1. CORS preflight + JWT bearer auth (same pattern as decide-offer).
      2. Parse body: `{ offer_id, pause_months_override }`. Validate uuid.
      3. Read the OFFER row from cancellation_offers_log via service-role admin: `select * where id = offer_id and user_id = caller.user.id and status = 'offered'`. If null → `jsonError(404, 'offer_not_found_or_not_owner')`. This is the security gate — accept Fn cannot apply an offer not owned by the JWT caller. Use single() to assert exactly 1 row.
      4. Read subscription: `select id, clinic_id from subscriptions where user_id = caller.user.id`. Compute Stripe context.
      5. Dispatch on `offer.offer_type`:
         - 'pause': `applyPause(sub.id, pause_months_override ?? offer.offer_config.pause_months, stripe)` → returns { resumesAt }. If error 'PAUSE_FAILED:*' (A5 trial reject) → return 500 + log to Sentry + DO NOT write accepted row; client can retry with discount fallback.
         - 'discount': `applyDiscount(sub.id, offer.offer_config.coupon_id, stripe)`.
         - 'extended_trial': `applyExtendedTrial(...)`.
         - 'downgrade': `applyDowngrade(...)`.
         - 'contact_csm': NO Stripe call (D-04 clinic CSM contact flow); 40-04's `cancellation-feedback-to-ticket` handles the helpdesk ticket creation. Just write the accepted log row + return.
      6. Detect "extension semantics" per D-10: if offer_type='pause' AND there's a prior `accepted` pause row for this user with `taken_at > now() - paused_until_window` (i.e., user is currently paused), this is an EXTENSION not an initial pause → bump prior_takes_count for cap math. Pass extension flag into the accepted-row insert.
      7. INSERT a SECOND log row (per PATTERNS §6 option (a) 2-row append): `cancellation_offers_log` with `user_id`, `org_id`, `subscription_id`, `reason` (carry from offered row), `offer_type` (same), `offer_config` (final applied — may differ from offered if pause_months_override), `cohort_snapshot` (copy), `tenure_bucket` (copy), `rule_id` (copy), `prior_takes_count` (current count BEFORE increment), `status='accepted'`, `taken_at=now()`, `posthog_variant_id` (copy). Note: there is NO `offered_log_id` FK column declared in 40-01's schema; the 2-row link is by `(user_id, rule_id, offered_at)` tuple (same rule + same session). Document this in SUMMARY.
      8. Update Stripe-side latency budget Sentry breadcrumb (informational).
      9. Return `AcceptOfferResponse`.
      10. `try {...} catch {...sentry...} finally { await shutdownPostHog() }`.

    Write `index.test.ts` covering: (a) pause offer → applyPause called with right months + accepted row written; (b) discount offer → applyDiscount called + accepted row; (c) cross-user offer_id attempt → 404; (d) re-accept of already-accepted offer_id → 404 (status='accepted' filter excludes); (e) contact_csm offer → no Stripe call + accepted row.

    Implements D-06 (pause apply), D-10 (extend counter math), D-14 (discounts[] append), D-15 (clamp delivery), D-16 (next-invoice via repeating coupon). VALIDATION rows 40-XX-11, 16, 17.
  </action>
  <verify>
    <automated>$HOME/.deno/bin/deno test --no-check supabase/functions/cancellation-accept-offer/ &amp;&amp; grep -rE "\\bdiscount:\\s*" supabase/functions/cancellation-accept-offer/ | grep -v 'discounts:' | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>All test files green; `grep -c "discounts:\\s*\\[" supabase/functions/cancellation-accept-offer/apply-discount.ts` ≥ 1; `grep -c "discount:\\s*" supabase/functions/cancellation-accept-offer/` (excluding `discounts:`) is 0; `grep -c "esm.sh/stripe@19" supabase/functions/cancellation-accept-offer/` ≥ 1; `grep -c "2026-04-22.dahlia" supabase/functions/cancellation-accept-offer/` ≥ 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→cancellation-decide-offer | user JWT verified via supabase-js userClient; admin client for log writes |
| browser→cancellation-accept-offer | user JWT verified; offer_id ownership re-verified server-side (defense against cross-user replay) |
| Edge Fn→Stripe API | secret API key; pause/coupon/trial/items writes; per-call try/catch with code-level error wrapping |
| Edge Fn→cancellation_offers_log | service-role insert (RLS bypass intentional); read service-role for cap/cooldown |
| Edge Fn→save_offer_rules | service-role read; never write (40-05 owns writes via SECDEF) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03-01 | Tampering | User passes guessed offer_id from another account to accept-offer | mitigate | accept-offer SELECT filters `user_id = caller.uid AND status='offered'`; cross-user offer returns 404; test covers this |
| T-40-03-02 | Tampering | User passes arbitrary coupon_id via DecideOfferRequest body to bypass catalog | mitigate | DecideOfferRequest schema has NO coupon_id field; coupon is server-resolved from save_offer_rules; user cannot inject |
| T-40-03-03 | Business Logic | User exhausts 2-take lifetime cap via concurrent decide-offer races (2 parallel requests, both pass cap check, both insert) | mitigate | Edge Fn re-reads counter just before insert — narrow race window. If true concurrency proves an issue, add pg_advisory_lock(hashtext(user_id)) per RESEARCH §security Anti-gaming bypass race; document as v1.4 hardening. Accept v1.3 risk: race window ~50ms, attack cost ≥ 2 simultaneous taps in cancellation UI — very low |
| T-40-03-04 | Business Logic | Stripe stacking abuse: 10% affiliate + 30% save → 37% effective without clamp | mitigate | D-15 server clamp at 35% effective; `STACKING_CAP_EFFECTIVE = 0.35` in anti-gaming.ts; clamp logged to stacking_clamped column; UI shows informational notice (D-15 option b) |
| T-40-03-05 | Information Disclosure | Stripe API error contains card last4 / customer email and leaks via Sentry | mitigate | Catch err; log `err.message + err.code + err.type` only; NEVER err.raw or err.headers (per T-14-03-I1) |
| T-40-03-06 | Elevation of Privilege | Edge Fn called with service-role bearer instead of user JWT (operator misuse) | mitigate | Use `supabaseClient.auth.getUser()` flow which validates JWT signature server-side AND extracts auth.uid(); service-role token has no auth.uid → 401 with 'unauthenticated' |
| T-40-03-07 | Repudiation | Save-offer accepted but no log trail | mitigate | Both decide and accept INSERT to cancellation_offers_log; append-only triggers in 40-01 prevent post-hoc tampering |
| T-40-03-08 | Tampering | apply-discount accidentally overwrites affiliate via legacy `discount:` field | mitigate | Verified test asserts `discounts:` array is used; grep gate in `<verify><automated>` filters `discount:` (singular without 's'); zero matches required |
| T-40-03-09 | Denial of Service | applyPause on trial subscription throws Stripe error; user sees generic 500 | mitigate | A5: catch 'invalid_request' from Stripe, surface error code to client; client (40-04) shows toast "Pause unavailable during trial. Try a discount instead." + log to Sentry |
</threat_model>

<verification>
- Deno test suite green: all 9 test files exercise documented behaviors
- `grep -c "import Stripe from 'https://esm.sh/stripe@19" supabase/functions/cancellation-accept-offer/*.ts` ≥ 4 (apply-pause + apply-discount + extend-pause + index)
- Singular legacy `discount:` field absent: `grep -rE "\\bdiscount:\\s*[^[]" supabase/functions/cancellation-*/` returns zero non-array matches
- decide-offer Fn does NOT call `stripe.subscriptions.update` (lookup-only invariant): `grep -c "subscriptions.update" supabase/functions/cancellation-decide-offer/` == 0
- All Edge Fns include `await shutdownPostHog()` in finally: `grep -c "shutdownPostHog" supabase/functions/cancellation-*/index.ts` ≥ 2
- Path resolution sanity: `cancellation-types.ts` imports nowhere from leanshot/ (Deno-side isolation)
</verification>

<success_criteria>
- POLISH-01 eligibility logic (D-01..D-04) fully enforced server-side; client UI never sees raw rules
- POLISH-03 pause apply works for 1/2/3 month presets; D-10 extension counter math correct
- POLISH-04 coupon stacking preserves affiliates (Stripe discounts[] append); D-15 clamp gates discount catalog selection
- VALIDATION rows 40-XX-01..05, 11, 16, 17, 18 covered by automated tests
- A5 trial-pause failure path graceful (catch + Sentry + client fallback path)
</success_criteria>

<output>
After completion, create `.planning/phases/40-cancellation-save-offers-flow/40-03-SUMMARY.md` capturing: (a) catalog-coupon selection rule when clamp produces non-catalog percent (largest-≤-clamp); (b) 2-row append linking key — `(user_id, rule_id, offered_at)` tuple or whatever was actually used; (c) cohort_membership matview shape verified per A6; (d) any A5 trial-pause failures observed in test fixtures.
</output>
