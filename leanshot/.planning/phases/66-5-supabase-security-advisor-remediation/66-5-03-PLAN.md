---
phase: 66-5-supabase-security-advisor-remediation
plan: 3
type: execute
wave: 2
depends_on: [66-5-01, 66-5-02]
files_modified:
  - leanshot/.planning/phases/66-5-supabase-security-advisor-remediation/66-5-SUMMARY.md
  - leanshot/.planning/phases/66-5-supabase-security-advisor-remediation/66-5-VERIFICATION.md
  - leanshot/.planning/phases/66-5-supabase-security-advisor-remediation/66-5-CARRY-OVER.md
  - leanshot/.planning/ROADMAP.md
  - leanshot/.planning/STATE.md
  - leanshot/.planning/REQUIREMENTS.md
autonomous: false
requirements: [SEC-01, SEC-02, SEC-03]
---

<objective>
Phase 66.5 close-out — inline planning artifacts (per autonomous run policy). 3 migrations ship to main; remote `db push` + advisor re-run deferred to Phase 70 milestone UAT.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Generate 66-5-SUMMARY.md + 66-5-VERIFICATION.md + 66-5-CARRY-OVER.md</name>
  <action>
SUMMARY (digest):
- 3 migrations: enable RLS on 7 tables + security_invoker views + 16 search_path fixes
- All 11 ERROR-level advisor findings + 16 mutable search_path covered
- 714 WARN-level findings explicitly deferred to Phase 69.5 with rationale

VERIFICATION (status: human_needed):
- Automated: 3 migration files exist + DO-block drift safety + grep counts match
- HUMAN signals deferred to Phase 70:
  - S1: `npx supabase db push --linked` applies all 3 migrations (depends on Phase 65 `org_subscriptions` drift resolution)
  - S2: `npx supabase db advisors --linked --type security --level error` returns 0 entries
  - S3: `share_snapshot_view` does NOT return auth.users.email to anon role (curl test)
  - S4: `v_cancellation_offers_roi` returns 0 rows for non-admin user (RLS-enforced)

CARRY-OVER:
- 714 WARN-level Supabase advisor findings deferred to Phase 69.5 (full report at `66-SUPABASE-ADVISORS.json`):
  - 238 anon-SECDEF-executable: audit which are intentional public RPCs; revoke EXECUTE from public on rest
  - 256 authenticated-SECDEF-executable: similar audit for `to authenticated`
  - 179 anonymous-sign-in policies: most are by-design (anon-INSERT on legal opt-out tables, etc.); document each
  - 14 matview-in-api: move sensitive matviews to a non-PostgREST schema OR add RLS
  - 4 public-bucket-allows-listing: `event-covers` / `org-branding` / `org-logos` / `org-onboarding-assets` — verify by-design
  - 3 extension-in-public: move `pg_net`, `vector`, `pgtap` to dedicated schema (high effort; defer)
  - 2 rls_policy_always_true: `privacy_optout_requests` (Phase 64 anon-INSERT — by design; document)
  - 1 auth_leaked_password_protection: enable HaveIBeenPwned check via Supabase Studio (operator)
  - 1 auth_insufficient_mfa_options: TOTP-only; WebAuthn deferred per Phase 66 carry-over
- Per `[[reference_supabase_migration_list_applied_vs_table_missing]]`: if any of the 7 RLS tables don't exist on remote (drift), the DO-block guard prevents migration failure but the RLS is effectively a no-op for that table. Operator must verify table existence post-`org_subscriptions` fix.
  </action>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP/STATE/REQUIREMENTS</name>
  <action>
ROADMAP: flip Phase 66.5 - [ ] to - [x] in top phase-list. Append "CODE-COMPLETE 2026-05-27" suffix.

STATE: Status → "Phase 66.5 CODE-COMPLETE — 3 migrations addressing 11 ERROR + 16 search_path advisor findings. Advancing to Phase 67".

REQUIREMENTS: add SEC-01, SEC-02, SEC-03 to v1.4 traceability:
- SEC-01: All public tables have RLS enabled (Phase 66.5)
- SEC-02: No SECURITY DEFINER views OR auth.users-exposing views (Phase 66.5)
- SEC-03: All public functions have explicit search_path (Phase 66.5)

Manual edits only.
  </action>
</task>

</tasks>
