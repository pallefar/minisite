---
phase: 66-5-supabase-security-advisor-remediation
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20290106000003_function_search_path_fix.sql
autonomous: true
requirements: [SEC-03]
---

<objective>
Phase 66.5 Wave 1 (parallel with 66-5-01) — Fix 16 functions with mutable search_path via single migration `20290106000003_function_search_path_fix.sql`. Adds `SET search_path = public, pg_temp` to each.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: ALTER FUNCTION ... SET search_path for 16 functions</name>
  <files>supabase/migrations/20290106000003_function_search_path_fix.sql</files>
  <action>
Source: `leanshot/.planning/phases/66-consumer-account-security/66-SUPABASE-ADVISORS.json` → filter where `name === 'function_search_path_mutable'` → extract 16 function names from `metadata.name`.

The 16 functions (from advisor report 2026-05-27):
- `increment_rate_limit`
- `landing_pages_set_updated_at`
- `locale_overrides_set_updated_at`
- `_user_changelog_dismissed_touch_updated_at`
- `set_feature_flags_updated_at`
- `set_affiliates_updated_at`
- `cohort_definitions_updated_at`
- `block_landing_page_revisions_delete`
- `save_offer_rules_updated_at`
- `vendor_baa_chain_set_updated_at`
- `tg_set_updated_at`
- `set_payouts_updated_at`
- `_changelog_entries_touch_updated_at`
- `site_settings_set_updated_at`
- `tg_rag_topics_updated_at`
- `block_landing_page_revisions_update`

For each, write:

```sql
do $$
begin
  alter function public.<fn_name>() set search_path = public, pg_temp;
exception when undefined_function then
  raise notice 'public.<fn_name>() does not exist on remote; skipping search_path fix (drift)';
end $$;
```

CRITICAL — function signatures:
- Most of the above are trigger functions (`returns trigger`) which always have `()` signature. Verify each via `grep` in `supabase/migrations/`:
  ```bash
  grep -E "create.*function.*<fn_name>" supabase/migrations/
  ```
- If any function has a non-empty signature (e.g. `increment_rate_limit(p_key text, p_window_seconds int, p_limit int)`), use the FULL signature in the `ALTER FUNCTION` statement. PostgreSQL identifies functions by name + argument types; wrong signature = no-op.
- The `DO $$ EXCEPTION WHEN undefined_function` block makes the migration drift-safe (mirrors 66-5-01 pattern).

Header comment: cite Phase 66.5 SEC-03, advisor link:
- https://supabase.com/docs/guides/database/database-linter?lint=0011_function_search_path_mutable

Migration is mechanical — 16 DO-blocks, each ~6 lines. Total file ~110 lines.
  </action>
  <verify>
    <automated>grep -cE "alter function public\." supabase/migrations/20290106000003_function_search_path_fix.sql | tr -d ' ' | grep -q "^16$"</automated>
  </verify>
</task>

</tasks>

<verification>
After migration applies, `supabase db advisors --linked --type security --level warn | jq '[.[] | select(.name=="function_search_path_mutable")] | length'` should drop from 16 to 0.
</verification>
