---
phase: 22
plan: 01
type: execute
wave: 0
depends_on: []
autonomous: false
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06, ADMIN-08, DEL-01, GDPR-01, GDPR-02, GDPR-03, ON-02, ON-03]
requirements_addressed: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06, ADMIN-08, DEL-01, GDPR-01, GDPR-02, GDPR-03, ON-02, ON-03]
files_modified:
  - supabase/migrations/20270601000001_finalize_cron_seven_days.sql
  - supabase/migrations/20270601000002_audit_action_enum_phase22.sql
  - supabase/migrations/20270601000003_audit_logs_impersonator_cols.sql
  - supabase/migrations/20270601000004_feature_flag_overrides_table.sql
  - supabase/migrations/20270601000005_consent_records_table.sql
  - supabase/migrations/20270601000006_dsar_requests_table.sql
  - supabase/migrations/20270601000007_dsar_exports_storage_bucket.sql
  - supabase/migrations/20270601000008_user_activity_daily_matview.sql
  - supabase/migrations/20270601000009_user_activity_refresh_cron.sql
  - supabase/migrations/20270601000010_cohort_retention_view.sql
  - supabase/migrations/20270601000011_admin_list_members_rpc.sql
  - supabase/migrations/20270601000012_impersonation_write_deny_policies.sql
  - supabase/migrations/20270601000013_resend_unverified_skips_counter.sql
  - supabase/migrations/20270601000014_cancel_account_deletion_rpc.sql
  - supabase/migrations/20270601000015_admin_stripe_action_audit_rpc.sql
  - supabase/migrations/20270601000016_feature_flag_overrides_cleanup_cron.sql
  - leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-A1-PROBE.md
  - leanshot/src/components/admin/__tests__/AdminMembersPage.test.tsx
  - leanshot/src/components/admin/members/__tests__/MembersTable.test.tsx
  - leanshot/src/components/admin/members/__tests__/RefundModal.test.tsx
  - leanshot/src/components/admin/cohorts/__tests__/CohortHeatmap.test.tsx
  - leanshot/src/components/impersonation/__tests__/ImpersonationBanner.test.tsx
  - leanshot/src/components/impersonation/__tests__/useImpersonationReadOnly.test.ts
  - leanshot/src/components/consent/__tests__/CookieConsentBootstrap.test.tsx
  - leanshot/src/components/dsar/__tests__/DsarPortalPage.test.tsx
  - leanshot/src/components/soft-delete/__tests__/SoftDeleteCountdownBanner.test.tsx
  - leanshot/src/lib/admin/__tests__/admin-api.test.ts
  - leanshot/src/lib/consent/__tests__/feature-flag-overrides.test.ts
  - leanshot/src/lib/consent/__tests__/consent-records.test.ts
  - leanshot/src/lib/dsar/__tests__/dsar-pdf-render.test.ts
  - supabase/functions/admin-impersonate/index.test.ts
  - supabase/functions/admin-stripe-action/index.test.ts
  - supabase/functions/dsar-export/index.test.ts
  - supabase/functions/lifecycle-welcome-series/index.test.ts
  - supabase/functions/lifecycle-behavior-triggered/index.test.ts
  - supabase/functions/lifecycle-transactional/index.test.ts
  - supabase/functions/lifecycle-retention/index.test.ts
  - supabase/functions/lifecycle-preference-update/index.test.ts
  - supabase/functions/_shared/__tests__/resend-domain-health-check.test.ts
  - leanshot/e2e/rls-audit-logs-impersonation.test.ts
  - leanshot/e2e/rls-feature-flag-overrides.test.ts
  - leanshot/e2e/rls-consent-records.test.ts
  - leanshot/e2e/rls-dsar-requests.test.ts
  - leanshot/e2e/rls-dsar-exports-storage.test.ts
  - leanshot/e2e/admin-impersonation-write-deny.test.ts
  - leanshot/e2e/cron-finalize-7day.test.ts
  - leanshot/e2e/cookie-consent.spec.ts
  - leanshot/e2e/cookie-consent-geo.spec.ts
  - leanshot/e2e/posthog-defer.spec.ts
  - leanshot/e2e/account-delete-cancel.spec.ts
  - leanshot/e2e/lifecycle-welcome-series.spec.ts
  - leanshot/e2e/dsar-export.spec.ts

must_haves:
  truths:
    - "All 16 P22 migrations apply via `supabase db push --linked` (Wave 0 BLOCKING task) with zero `Skipping migration` lines in dry-run"
    - "Postgres ships `dsar_requests`, `consent_records`, `feature_flag_overrides`, `user_activity_daily` matview, `cohort_retention` view, plus 11 new `audit_action_type` enum values (impersonate_start/end/blocked_write, refund_issued, subscription_canceled_admin, subscription_comped, feature_flag_override_set, dsar_requested, dsar_completed, consent_recorded, account_deletion_cancelled)"
    - "`audit_logs` table gains `impersonator_id uuid` + `target_user_id uuid` columns (both ON DELETE SET NULL)"
    - "`finalize_account_deletions_batch()` function body uses `interval '7 days'` (down from 30) per D-01 + Conflict #2"
    - "`admin_list_members(p_search, p_tier, p_page, p_size)` RPC exists, gated on `is_staff`, joining `tier_effective`"
    - "`cancel_account_deletion(token text)` RPC exists, HMAC-verified against Vault `CANCEL_DELETION_HMAC_KEY` per Open Q1"
    - "`admin_set_feature_flag_override(user_id, flag_key, value, expires_at)` RPC exists, is_staff-gated"
    - "`impersonation_write_deny_policies` migration adds `(insert|update|delete)` deny policies reading `request.jwt.claims->app_metadata->impersonator_id IS NULL` on all 17 user-writable tables (injections, weights, meals, workouts, supplements, mood, sleep, symptoms, vials, settings, photos, ai_messages, shares, consent_records, pending_account_deletions, feature_flag_overrides, dsar_requests)"
    - "`dsar-exports` private Storage bucket exists with RLS scoped to `(storage.foldername(name))[1] = auth.uid()::text`"
    - "`user_activity_daily` matview refresh cron scheduled at `0 2 * * *` UTC"
    - "Postman probe doc `22-A1-PROBE.md` captures whether `admin.updateUserById({app_metadata:{impersonator_id}})` propagates to the next minted JWT within one request cycle (RESEARCH Assumption A1)"
    - "All 28 Wave-0 test scaffolds exist as failing/skipped placeholders that import the not-yet-created modules — `npm test -- --run` reports them as `expected to fail` rather than `cannot find module`"
  artifacts:
    - path: supabase/migrations/20270601000001_finalize_cron_seven_days.sql
      provides: "30d→7d soft-delete cron + ALTER FUNCTION finalize_account_deletions_batch (D-01, Conflict #2)"
    - path: supabase/migrations/20270601000002_audit_action_enum_phase22.sql
      provides: "11 enum value extensions via ALTER TYPE ADD VALUE IF NOT EXISTS (Pitfall 3)"
    - path: supabase/migrations/20270601000012_impersonation_write_deny_policies.sql
      provides: "Cross-table RLS write-deny on 17 tables × 3 ops (D-05, Pattern 1)"
    - path: supabase/migrations/20270601000007_dsar_exports_storage_bucket.sql
      provides: "Private `dsar-exports` bucket + RLS"
    - path: leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-A1-PROBE.md
      provides: "ASSUMED A1 verification log: whether app_metadata propagates per JWT mint"
    - path: leanshot/e2e/rls-audit-logs-impersonation.test.ts
      provides: "Per-file slug prefix IMP_; RLS proof scaffold (failing)"
  key_links:
    - from: supabase/migrations/20270601000002_audit_action_enum_phase22.sql
      to: supabase/migrations/20270601000015_admin_stripe_action_audit_rpc.sql
      via: "audit_action_type enum values are READ in admin RPC bodies"
      pattern: "audit_action_type"
    - from: supabase/migrations/20270601000012_impersonation_write_deny_policies.sql
      to: "request.jwt.claims setting set by PostgREST per request"
      via: "current_setting('request.jwt.claims', true)::json #>> '{app_metadata,impersonator_id}'"
      pattern: "request.jwt.claims"
    - from: supabase/migrations/20270601000001_finalize_cron_seven_days.sql
      to: leanshot/src/components/dashboard/settings/DeleteAccountModal.tsx
      via: "DB enforces 7-day grace; UI copy must match (sweep in 22-05)"
      pattern: "interval '7 days'"
---

<objective>
Land the foundational migrations + test scaffolds for Phase 22.

Purpose: Eliminate the Phase 7 30-day vs P22 7-day soft-delete conflict (Conflict #2), provide all schema surfaces required by Wave 1+2 plans, and seed every failing test that subsequent waves will turn green. Includes BLOCKING `supabase db push --linked` so downstream waves can rely on live tables. Also resolves RESEARCH Assumption A1 via Postman probe so Wave 1 plan 22-03 can pick between `admin.updateUserById` JWT-mint vs Custom Access Token Hook.

Output: 16 migrations applied to remote project `ytnsipxxmzgaebkqmokp`; 28 failing test files; A1 probe verdict captured.
</objective>

<execution_context>
@/Users/karstenhaldan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karstenhaldan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/STATE.md
@/Users/karstenhaldan/minisite/leanshot/.planning/ROADMAP.md
@/Users/karstenhaldan/minisite/leanshot/.planning/REQUIREMENTS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-VALIDATION.md
@/Users/karstenhaldan/minisite/supabase/migrations/20260601000010_pending_account_deletions.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20260601000011_initiate_account_deletion_rpc.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20260601000013_finalize_account_deletions_cron.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20260601000017_audit_trigger_suppress_guc.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20260601000001_audit_logs.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20260701000001_audit_logs_share_columns.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20260801000004_audit_action_enum.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20260901000001_extend_audit_action_enum_phase10.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20260901000003_rank_org_patients_rpc.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20261101000006_is_staff_helper.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20270101000004_tier_effective_view.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20270101000007_affiliate_click_baseline_mv.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20270101000009_click_baseline_refresh_cron.sql
@/Users/karstenhaldan/minisite/supabase/migrations/20270101000014_service_role_key_vault_load.sql
@/Users/karstenhaldan/minisite/leanshot/e2e/rls-audit-logs.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 16 P22 migrations under /Users/karstenhaldan/minisite/supabase/migrations/</name>
  <files>
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000001_finalize_cron_seven_days.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000002_audit_action_enum_phase22.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000003_audit_logs_impersonator_cols.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000004_feature_flag_overrides_table.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000005_consent_records_table.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000006_dsar_requests_table.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000007_dsar_exports_storage_bucket.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000008_user_activity_daily_matview.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000009_user_activity_refresh_cron.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000010_cohort_retention_view.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000011_admin_list_members_rpc.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000012_impersonation_write_deny_policies.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000013_resend_unverified_skips_counter.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000014_cancel_account_deletion_rpc.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000015_admin_stripe_action_audit_rpc.sql,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000016_feature_flag_overrides_cleanup_cron.sql
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave A section, lines 27-365 — every migration's analog + gotcha),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Pattern 5 lines 546-598 for cron rewrite; Pattern 1 lines 365-410 for write-deny policy; §"Common Operation 1" lines 796-844 for admin_list_members; §"Common Operation 4" lines 856-895 for matview + cohort_retention; Pitfall 1, 2, 3, 4, 5, 10),
    /Users/karstenhaldan/minisite/supabase/migrations/20260601000013_finalize_account_deletions_cron.sql (full — cron + run_finalize_account_deletions_cron_now test hook to keep parity),
    /Users/karstenhaldan/minisite/supabase/migrations/20260901000001_extend_audit_action_enum_phase10.sql (enum-extend idempotent pattern),
    /Users/karstenhaldan/minisite/supabase/migrations/20260601000010_pending_account_deletions.sql (RLS-locked-table pattern lines 41-58),
    /Users/karstenhaldan/minisite/supabase/migrations/20260701000001_audit_logs_share_columns.sql (additive-column ALTER TABLE pattern),
    /Users/karstenhaldan/minisite/supabase/migrations/20270101000007_affiliate_click_baseline_mv.sql (matview + UNIQUE-index pattern; Pitfall 1 IMMUTABLE comment lines 17-21),
    /Users/karstenhaldan/minisite/supabase/migrations/20270101000009_click_baseline_refresh_cron.sql (cron.schedule + refresh concurrently pattern),
    /Users/karstenhaldan/minisite/supabase/migrations/20260601000017_audit_trigger_suppress_guc.sql (app.suppress_audit GUC pattern),
    /Users/karstenhaldan/minisite/supabase/migrations/20261101000006_is_staff_helper.sql (is_staff function used by admin RPC gates),
    /Users/karstenhaldan/minisite/supabase/migrations/20270101000004_tier_effective_view.sql (view shape consumed by admin_list_members),
    /Users/karstenhaldan/minisite/supabase/migrations/20270101000014_service_role_key_vault_load.sql (Vault secret load pattern reused for CANCEL_DELETION_HMAC_KEY)
  </read_first>
  <action>
    Create the 16 migrations listed in <files> with strict 14-digit timestamp + underscore filename (Pitfall 2). Each migration begins with a header comment naming Phase 22, the D-NN decision it implements, and the analog migration. Specifics:

    File 01 `finalize_cron_seven_days.sql`: Per RESEARCH Pattern 5 — `select cron.unschedule('finalize-account-deletions');` then `select cron.schedule('finalize-account-deletions', '0 4 * * *', $$ ... $$);` with `WHERE initiated_at + interval '7 days' <= now()`. Then `create or replace function public.finalize_account_deletions_batch() returns void security definer set search_path = public, pg_catalog language plpgsql as $$ ... $$;` with the same `interval '7 days'` in the inner loop. Also `create or replace function public.run_finalize_account_deletions_cron_now()` test-hook updated to use 7 days. Use `WHERE initiated_at + interval '7 days' <= now()` form to preserve IMMUTABLE-compat from Phase 7 analog (NOT `initiated_at <= now() - interval '7 days'`).

    File 02 `audit_action_enum_phase22.sql`: `ALTER TYPE public.audit_action_type ADD VALUE IF NOT EXISTS '<value>';` one per line for the 11 values listed in must_haves. ENUM-ONLY migration; NO downstream usage (Pitfall 3 / sqlstate 55P04). Verify enum identifier via prior `extend_audit_action_enum_phase10` analog — same `public.audit_action_type`.

    File 03 `audit_logs_impersonator_cols.sql`: `ALTER TABLE public.audit_logs ADD COLUMN IF NOT EXISTS impersonator_id uuid REFERENCES auth.users(id) ON DELETE SET NULL, ADD COLUMN IF NOT EXISTS target_user_id uuid REFERENCES auth.users(id) ON DELETE SET NULL;` + partial index `audit_logs_impersonator_idx ON public.audit_logs (impersonator_id, timestamp DESC) WHERE impersonator_id IS NOT NULL`. Use `ON DELETE SET NULL` to mirror `user_id` column behavior from analog `20260601000001_audit_logs.sql` lines 55-56.

    File 04 `feature_flag_overrides_table.sql`: `create table public.feature_flag_overrides (id uuid primary key default gen_random_uuid(), user_id uuid not null references auth.users(id) on delete cascade, flag_key text not null, value boolean not null, set_by uuid not null references auth.users(id) on delete set null, set_at timestamptz not null default now(), expires_at timestamptz not null, unique(user_id, flag_key));`. Add `enable row level security` + `create policy "feature_flag_overrides_select_own" ... using (auth.uid() = user_id);`. NO authenticated write policy (admin RPC `admin_set_feature_flag_override` is the only writer per D-08). Add plain `index feature_flag_overrides_expires_idx on public.feature_flag_overrides (expires_at)` — NOT partial with `now()` predicate (Pitfall 1 IMMUTABLE rejection). Also create `admin_set_feature_flag_override(p_user_id uuid, p_flag_key text, p_value boolean, p_expires_at timestamptz) returns void security definer set search_path = public, pg_catalog` with is_staff gate (mirror `rank_org_patients_rpc.sql` lines guarding `is_staff`); inside body call `perform set_config('app.suppress_audit', 'true', true);` then `insert into public.audit_logs (action_type, actor_id, target_user_id, payload) values ('feature_flag_override_set', auth.uid(), p_user_id, jsonb_build_object('flag_key', p_flag_key, 'value', p_value, 'expires_at', p_expires_at));` then UPSERT into feature_flag_overrides.

    File 05 `consent_records_table.sql`: `create table public.consent_records (id uuid primary key default gen_random_uuid(), user_id uuid references auth.users(id) on delete cascade, anonymous_id text, cookie_categories jsonb not null default '{}'::jsonb, email_preferences jsonb not null default '{}'::jsonb, recorded_at timestamptz not null default now(), revoked_at timestamptz, consent_revision int not null default 1, user_agent text, ip_inet inet, country_code text);`. Note `user_id` is nullable (anonymous visitors record consent before signup using `anonymous_id`). RLS: enable + `create policy "consent_records_select_own" using (auth.uid() = user_id);` + `create policy "consent_records_upsert_own" for insert/update with check (auth.uid() = user_id OR user_id is null);`. Per PATTERNS S6 status-machine audit: the implicit active→revoked transition is owned by (a) cookie banner onChange in plan 22-10, (b) `/settings/email-preferences` Save in plan 22-11. Document both writers in header comment.

    File 06 `dsar_requests_table.sql`: `create type public.dsar_request_status as enum ('pending','in_progress','completed','rejected');` then `create table public.dsar_requests (id uuid primary key default gen_random_uuid(), user_id uuid not null references auth.users(id) on delete cascade, requested_at timestamptz not null default now(), completed_at timestamptz, status public.dsar_request_status not null default 'pending', rejection_reason text, export_path text, export_signed_url_expires_at timestamptz);`. RLS: select-own + insert-own only (UPDATE/DELETE service_role only). Writers per S6: `pending` = `DsarPortalPage` insert (22-11), `in_progress` = `dsar-export` Edge Fn start (22-04), `completed` = `dsar-export` Edge Fn finish (22-04), `rejected` = admin RPC `admin_reject_dsar(p_request_id uuid, p_reason text)` security definer is_staff-gated — include this RPC in same migration to close the writer gap up-front.

    File 07 `dsar_exports_storage_bucket.sql`: `insert into storage.buckets (id, name, public) values ('dsar-exports','dsar-exports', false) on conflict (id) do nothing;`. RLS: `create policy "dsar_exports_select_own_object" on storage.objects for select to authenticated using (bucket_id = 'dsar-exports' AND auth.uid()::text = (storage.foldername(name))[1]);`. NO insert/update/delete for authenticated (service_role only). Mirror analog `20260601000014_photos_pending_shred_storage.sql` for storage.objects RLS shape.

    File 08 `user_activity_daily_matview.sql`: Per RESEARCH §Common Operation 4 lines 862-878 — `create materialized view public.user_activity_daily as select u.id as user_id, date_trunc('week', u.created_at)::date as cohort_week, date_trunc('day', u.last_sign_in_at)::date as activity_day, (date_trunc('day', u.last_sign_in_at) - date_trunc('week', u.created_at))::int as day_offset from auth.users u where u.last_sign_in_at is not null;`. `create unique index idx_uad_user on public.user_activity_daily(user_id, activity_day);` (supports REFRESH CONCURRENTLY per Pitfall 5 of analog `affiliate_click_baseline_mv.sql`). `grant select on public.user_activity_daily to service_role;` (NOT authenticated — admin-only data per D-04).

    File 09 `user_activity_refresh_cron.sql`: `select cron.schedule('user-activity-daily-refresh', '0 2 * * *', $$ refresh materialized view concurrently public.user_activity_daily; $$);` per D-04 02:00 UTC. Mirror analog `click_baseline_refresh_cron.sql`.

    File 10 `cohort_retention_view.sql`: Per RESEARCH §Common Operation 4 lines 881-893 — `create or replace view public.cohort_retention as select cohort_week, day_offset, count(distinct user_id)::int as active_users, (count(distinct user_id)::float / nullif((select count(*) from auth.users where date_trunc('week', created_at)::date = uad.cohort_week), 0)) * 100 as retention_pct from public.user_activity_daily uad where day_offset between 0 and 90 group by cohort_week, day_offset order by cohort_week desc, day_offset asc;`. `grant select on public.cohort_retention to service_role;`. Add k-anonymity guard per security threat: in the cohort RPC (separate plan, but record contract here in header comment) cells where `count(distinct user_id) < 10` render as "&lt;10" client-side.

    File 11 `admin_list_members_rpc.sql`: Per RESEARCH §Common Operation 1 lines 796-844 — verbatim, with strict is_staff gate via `if not exists (select 1 from public.profiles where id = auth.uid() and is_staff = true) then raise exception 'forbidden' using errcode = '42501'; end if;`. JOIN to `public.tier_effective` (NOT raw `subscriptions.status`) per CONTEXT cross-phase rule.

    File 12 `impersonation_write_deny_policies.sql`: Per RESEARCH §Pattern 1 lines 376-388. For each of these 17 tables — injections, weights, meals, workouts, supplements, mood, sleep, symptoms, vials, settings, photos, ai_messages, shares, consent_records, pending_account_deletions, feature_flag_overrides, dsar_requests — emit 3 policies (INSERT, UPDATE, DELETE) named `deny_writes_during_impersonation_<op>` with check/using `(current_setting('request.jwt.claims', true)::json #>> '{app_metadata,impersonator_id}') is null`. Pre-flight each table with `do $$ begin if exists (select 1 from information_schema.tables where table_schema='public' and table_name='<t>') then ... end if; end $$;`. NEVER use `app.impersonator_id` GUC (Pitfall 1 — does not persist across PostgREST requests). Header comment: this migration ships 51 policies (17 tables × 3 ops); add via single migration to avoid shared-file choreography across waves (Pre-emptive warning 4). Existing owner-write policies are ALSO required to pass — RLS is AND-combined.

    File 13 `resend_unverified_skips_counter.sql`: `create table if not exists public.email_send_counters (key text primary key, value bigint not null default 0, updated_at timestamptz not null default now());` + `insert into public.email_send_counters (key, value) values ('resend_domain_unverified_skips', 0) on conflict (key) do nothing;`. Then `create or replace function public.increment_resend_domain_unverified_skips() returns void security definer set search_path = public, pg_catalog language sql as $$ update public.email_send_counters set value = value + 1, updated_at = now() where key = 'resend_domain_unverified_skips'; $$;`. `grant execute on function public.increment_resend_domain_unverified_skips() to service_role;`.

    File 14 `cancel_account_deletion_rpc.sql`: `create or replace function public.cancel_account_deletion(p_token text) returns void security definer set search_path = public, pg_catalog, extensions language plpgsql as $$ declare v_user_id uuid; v_initiated_at timestamptz; v_payload jsonb; v_hmac_key text; begin select decrypted_secret into v_hmac_key from vault.decrypted_secrets where name = 'CANCEL_DELETION_HMAC_KEY' limit 1; if v_hmac_key is null then raise exception 'hmac_key_missing'; end if; -- decode token (base64url JSON {uid, initiated_at, sig}); verify sig = hmac_sha256(uid||initiated_at, v_hmac_key); if invalid raise 'invalid_token'; if extract(epoch from (now() - to_timestamp(v_initiated_at)))::int > 7*86400 then raise exception 'token_expired'; end if; perform set_config('app.suppress_audit', 'true', true); delete from public.pending_account_deletions where user_id = v_user_id; insert into public.audit_logs (action_type, actor_id, target_user_id, payload) values ('account_deletion_cancelled', v_user_id, v_user_id, jsonb_build_object('initiated_at', v_initiated_at)); end; $$;`. Token verification uses `extensions.hmac()` from pgcrypto. Per Open Q1 → HS256-style HMAC token. Vault secret load mirror `20270101000014_service_role_key_vault_load.sql`.

    File 15 `admin_stripe_action_audit_rpc.sql`: Three SECURITY DEFINER RPCs — `admin_log_refund(p_target_user_id uuid, p_charge_id text, p_amount_cents int, p_reason text)`, `admin_log_subscription_canceled(p_target_user_id uuid, p_subscription_id text)`, `admin_log_subscription_comped(p_target_user_id uuid, p_subscription_id text, p_trial_end timestamptz)`. Each starts with is_staff gate + `perform set_config('app.suppress_audit', 'true', true);` + insert into audit_logs with the matching enum value from File 02 (`refund_issued`, `subscription_canceled_admin`, `subscription_comped`). These are called by the Edge Function in 22-03 to ensure audit rows are written even if Stripe API succeeds but webhook lag happens.

    File 16 `feature_flag_overrides_cleanup_cron.sql`: `select cron.schedule('feature-flag-overrides-cleanup', '0 5 * * *', $$ delete from public.feature_flag_overrides where expires_at < now() - interval '7 days'; $$);` per RESEARCH Pitfall 10 + A3.

    Header pattern for every file: `-- Phase 22 plan 01 — <D-NN>: <one-line purpose>` + `-- Analog: <path>:<lines>` + `-- Pitfalls applied: <numbered list from RESEARCH>`. NO letter-suffix timestamps (Pitfall 2). All `if not exists`/`if exists` for idempotency.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && ls supabase/migrations/2027060100000{1..9}*.sql supabase/migrations/2027060100001{0..6}*.sql | wc -l | grep -q "^16$" &amp;&amp; grep -L "^-- Phase 22" supabase/migrations/20270601*.sql | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>All 16 migration files exist with strict timestamps, every file's header references "Phase 22", no letter suffixes anywhere in filenames, every file has analog citation in header.</done>
</task>

<task type="auto">
  <name>Task 2: Author 28 test scaffolds across vitest, Deno, and Playwright (all failing/skipped)</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/__tests__/AdminMembersPage.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/__tests__/MembersTable.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/__tests__/RefundModal.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/cohorts/__tests__/CohortHeatmap.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/impersonation/__tests__/ImpersonationBanner.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/impersonation/__tests__/useImpersonationReadOnly.test.ts,
    /Users/karstenhaldan/minisite/leanshot/src/components/consent/__tests__/CookieConsentBootstrap.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/dsar/__tests__/DsarPortalPage.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/soft-delete/__tests__/SoftDeleteCountdownBanner.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/lib/admin/__tests__/admin-api.test.ts,
    /Users/karstenhaldan/minisite/leanshot/src/lib/consent/__tests__/feature-flag-overrides.test.ts,
    /Users/karstenhaldan/minisite/leanshot/src/lib/consent/__tests__/consent-records.test.ts,
    /Users/karstenhaldan/minisite/leanshot/src/lib/dsar/__tests__/dsar-pdf-render.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/admin-impersonate/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/admin-stripe-action/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/dsar-export/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-welcome-series/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-behavior-triggered/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-transactional/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-retention/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-preference-update/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/_shared/__tests__/resend-domain-health-check.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-audit-logs-impersonation.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-feature-flag-overrides.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-consent-records.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-dsar-requests.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-dsar-exports-storage.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/admin-impersonation-write-deny.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/cron-finalize-7day.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/cookie-consent.spec.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/cookie-consent-geo.spec.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/posthog-defer.spec.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/account-delete-cancel.spec.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/lifecycle-welcome-series.spec.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/dsar-export.spec.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Validation Architecture lines 1010-1093 — every test pinned to a REQ-ID + automated command + file path),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave G section lines 805-855 — RLS scaffold pattern, env-gating, per-file slug prefix),
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-audit-logs.test.ts (lines 30-100 — env-gating + two-user setup + unique storageKey),
    /Users/karstenhaldan/minisite/supabase/functions/affiliate-payout/index.test.ts (Deno test scaffold + lazy admin singleton mock pattern),
    /Users/karstenhaldan/minisite/supabase/functions/clinic-invite/index.test.ts (Resend test-stub short-circuit pattern)
  </read_first>
  <action>
    Author each test file as a placeholder per the Nyquist map in RESEARCH §Validation Architecture lines 1027-1054. Each file MUST:

    (a) Import the not-yet-existing module from its planned path (so plan-checker can see the test exists; module import failure is acceptable while subsequent plans wire it up).
    (b) Declare at least one `it.skip('... [DEFERRED — Wave 0 scaffold; impl in plan 22-XX]', () => {})` describing the behavior under test. Use `it.skip` NOT `it.fixme` (Pitfall: vitest 4.x `it.fixme` throws TypeError — see project memory `reference_vitest_skip_fixme.md`).
    (c) For RLS files: declare per-file unique slug prefix constant (PATTERNS S5). Example file scoping:
        - rls-audit-logs-impersonation: `const IMPERSONATION_PREFIX = 'phase22-impersonation-rls';`
        - rls-feature-flag-overrides: `const FLAG_OVERRIDES_PREFIX = 'phase22-flag-overrides-rls';`
        - rls-consent-records: `const CONSENT_RECORDS_PREFIX = 'phase22-consent-records-rls';`
        - rls-dsar-requests: `const DSAR_REQUESTS_PREFIX = 'phase22-dsar-requests-rls';`
        - rls-dsar-exports-storage: `const DSAR_STORAGE_PREFIX = 'phase22-dsar-storage-rls';`
        - admin-impersonation-write-deny: `const WRITE_DENY_PREFIX = 'phase22-write-deny-rls';`
       Each prefix produces unique email + storageKey constants. NEVER share with other files.
    (d) For RLS files: include `describeIfLive` env-gate (URL/ANON/SERVICE) per analog lines 30-36 so tests skip gracefully without live DB credentials.
    (e) For Deno tests: include the lazy-admin-singleton + Proxy mock pattern from `affiliate-payout/index.test.ts` so the subject Edge Fn can be imported without env vars set.
    (f) For Playwright specs: declare `test.skip('... [DEFERRED — Wave 0 scaffold]', async ({page}) => {})` with at least one specific behavior assertion in a comment so executor (later wave) knows what to implement (e.g., for cookie-consent.spec.ts: "asserts banner is `fixed bottom-0` after first paint + `lucide:Cookie` icon visible + 3 buttons Reject/Customize/Accept present").

    Each test file's behavior list maps to REQ-IDs per the Nyquist map:
    - AdminMembersPage.test.tsx — ADMIN-01 client gate + RPC dispatch + Pill filter
    - MembersTable.test.tsx — ADMIN-01 sort 3-click revert + pagination URL
    - RefundModal.test.tsx — ADMIN-04 3-step gating + typed-confirm `REFUND ${amount}`
    - CohortHeatmap.test.tsx — ADMIN-08 13×91 grid render + k-anonymity "&lt;10" rendering
    - ImpersonationBanner.test.tsx — ADMIN-03 banner mount + countdown decrement + auto-end at 0
    - useImpersonationReadOnly.test.ts — ADMIN-03 disabled-props provider when JWT claims include `impersonator_id`
    - CookieConsentBootstrap.test.tsx — GDPR-01 dynamic-import gate fires after `requestIdleCallback`
    - DsarPortalPage.test.tsx — GDPR-03 form submit creates `dsar_requests` row via RPC
    - SoftDeleteCountdownBanner.test.tsx — DEL-01 countdown N days + Cancel CTA invokes cancel_account_deletion
    - admin-api.test.ts — admin_list_members RPC mock + result shape
    - feature-flag-overrides.test.ts — ADMIN-05 overrides-first check + expires_at fallthrough
    - consent-records.test.ts — GDPR-02 UPSERT shape contract
    - dsar-pdf-render.test.ts — GDPR-03 7-section render + hashed referred-user emails
    - admin-impersonate/index.test.ts — ADMIN-03 JWT mint + audit_log impersonate_start row
    - admin-stripe-action/index.test.ts — ADMIN-04 refund + cancel + comp + audit + suppress-audit GUC
    - dsar-export/index.test.ts — GDPR-03 7-step orchestrator + status transitions pending → in_progress → completed
    - lifecycle-{welcome,behavior,transactional,retention,preference-update}/index.test.ts — ON-02 each: health-check unverified → 200 + counter++, verified → send
    - _shared/resend-domain-health-check.test.ts — D-03 shared helper unit
    - rls-audit-logs-impersonation.test.ts — impersonator_id column write + read scope
    - rls-feature-flag-overrides.test.ts — select-own + admin RPC required for writes
    - rls-consent-records.test.ts — anonymous insert allowed + cross-tenant select denied
    - rls-dsar-requests.test.ts — select-own + status transitions writer per Pattern S6
    - rls-dsar-exports-storage.test.ts — only own folder readable via storage.foldername(name)[1]
    - admin-impersonation-write-deny.test.ts — JWT with app_metadata.impersonator_id → INSERT on 17 tables returns 42501
    - cron-finalize-7day.test.ts — back-date initiated_at to now()-interval '8 days' + invoke run_finalize_account_deletions_cron_now() → user gone
    - cookie-consent.spec.ts — bottom slide-up + 3 buttons + cc_cookie cookie after Accept
    - cookie-consent-geo.spec.ts — `?force_geo=eu` → analytics default off; `?force_geo=us` → analytics default on
    - posthog-defer.spec.ts — window.posthog === undefined before consent; defined after Accept Analytics
    - account-delete-cancel.spec.ts — request deletion → receive email link (test mode via admin.generateLink stub) → click → pending row gone
    - lifecycle-welcome-series.spec.ts — RESEND_API_KEY=test-stub → fn returns success without HTTP call
    - dsar-export.spec.ts — submit DSAR → poll status → download bundle from signed URL

    NO `it.fixme`. NO heredoc; use Write tool. Every file ends with a citation comment `// Wave 0 scaffold per .planning/phases/22-…/22-RESEARCH.md §Validation Architecture — DEFERRED implementation owner: <plan-id>`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; ls src/components/admin/__tests__/AdminMembersPage.test.tsx src/components/admin/members/__tests__/MembersTable.test.tsx src/components/admin/members/__tests__/RefundModal.test.tsx src/components/admin/cohorts/__tests__/CohortHeatmap.test.tsx src/components/impersonation/__tests__/ImpersonationBanner.test.tsx src/components/impersonation/__tests__/useImpersonationReadOnly.test.ts src/components/consent/__tests__/CookieConsentBootstrap.test.tsx src/components/dsar/__tests__/DsarPortalPage.test.tsx src/components/soft-delete/__tests__/SoftDeleteCountdownBanner.test.tsx src/lib/admin/__tests__/admin-api.test.ts src/lib/consent/__tests__/feature-flag-overrides.test.ts src/lib/consent/__tests__/consent-records.test.ts src/lib/dsar/__tests__/dsar-pdf-render.test.ts e2e/rls-audit-logs-impersonation.test.ts e2e/rls-feature-flag-overrides.test.ts e2e/rls-consent-records.test.ts e2e/rls-dsar-requests.test.ts e2e/rls-dsar-exports-storage.test.ts e2e/admin-impersonation-write-deny.test.ts e2e/cron-finalize-7day.test.ts e2e/cookie-consent.spec.ts e2e/cookie-consent-geo.spec.ts e2e/posthog-defer.spec.ts e2e/account-delete-cancel.spec.ts e2e/lifecycle-welcome-series.spec.ts e2e/dsar-export.spec.ts 2&gt;/dev/null | wc -l | grep -q "^26$" &amp;&amp; cd /Users/karstenhaldan/minisite &amp;&amp; ls supabase/functions/admin-impersonate/index.test.ts supabase/functions/admin-stripe-action/index.test.ts supabase/functions/dsar-export/index.test.ts supabase/functions/lifecycle-welcome-series/index.test.ts supabase/functions/lifecycle-behavior-triggered/index.test.ts supabase/functions/lifecycle-transactional/index.test.ts supabase/functions/lifecycle-retention/index.test.ts supabase/functions/lifecycle-preference-update/index.test.ts supabase/functions/_shared/__tests__/resend-domain-health-check.test.ts | wc -l | grep -q "^9$" &amp;&amp; cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; grep -lR "it\\.fixme\\|test\\.fixme" src/components/admin/__tests__ src/components/admin/members/__tests__ src/components/admin/cohorts/__tests__ src/components/impersonation/__tests__ src/components/consent/__tests__ src/components/dsar/__tests__ src/components/soft-delete/__tests__ src/lib/admin/__tests__ src/lib/consent/__tests__ src/lib/dsar/__tests__ e2e 2&gt;/dev/null | grep -v "^$" | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>All 35 test files exist (26 leanshot/src + e2e, 9 supabase/functions). Zero use of `it.fixme`/`test.fixme`. Every RLS file declares its own slug prefix constant.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: BLOCKING — push migrations to remote + run Postman probe for A1</name>
  <files>/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-A1-PROBE.md</files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Pattern 1 lines 365-410 — ASSUMED A1 statement; Pitfall 1 lines 691-698; §Assumptions Log lines 952-957),
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/reference_supabase_db_query_linked.md (read-only verification pattern; query against linked project),
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/reference_supabase_worktree_temp_state.md (copy supabase/.temp/ before --linked CLI use in worktree)
  </read_first>
  <what-built>16 migrations + 35 test scaffolds committed. Migrations have NOT been applied to remote `ytnsipxxmzgaebkqmokp` yet because that requires `SUPABASE_ACCESS_TOKEN` in the executor's env.</what-built>
  <how-to-verify>
    Step 1: From `/Users/karstenhaldan/minisite/`, run a dry-run + verify zero "Skipping migration" lines:
    `cd /Users/karstenhaldan/minisite &amp;&amp; supabase db push --linked --dry-run 2&gt;&amp;1 | tee /tmp/p22-dryrun.log &amp;&amp; ! grep -E "^Skipping" /tmp/p22-dryrun.log`
    Expected: exit 0 AND grep returns nothing AND dry-run output lists all 16 new migrations.

    Step 2: Apply live: `cd /Users/karstenhaldan/minisite &amp;&amp; supabase db push --linked` — confirm at prompt; verify exit 0 + "Finished" message.

    Step 3: Live verify 5 key objects exist via `supabase db query --linked`:
    `supabase db query --linked "select count(*) from pg_class where relname in ('user_activity_daily','feature_flag_overrides','consent_records','dsar_requests','email_send_counters');"` → expect 5.
    `supabase db query --linked "select count(*) from cron.job where jobname in ('finalize-account-deletions','user-activity-daily-refresh','feature-flag-overrides-cleanup');"` → expect 3 (finalize was unscheduled+rescheduled, so should still show once).
    `supabase db query --linked "select count(*) from pg_proc where proname in ('admin_list_members','admin_set_feature_flag_override','cancel_account_deletion','admin_log_refund','increment_resend_domain_unverified_skips');"` → expect 5.
    `supabase db query --linked "select count(*) from pg_policy where polname like 'deny_writes_during_impersonation_%';"` → expect 51 (17 tables × 3 ops).
    `supabase db query --linked "select count(*) from storage.buckets where id = 'dsar-exports';"` → expect 1.

    Step 4: A1 PROBE (Pitfall 1 fallback). Write `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-A1-PROBE.md` with the curl + node script results that answer: "Does `supabase.auth.admin.updateUserById(targetId, {app_metadata:{impersonator_id: adminUuid}})` propagate to the NEXT minted JWT (via `admin.generateLink({type:'magiclink', email: targetEmail})`) so that `current_setting('request.jwt.claims',true)::json #>> '{app_metadata,impersonator_id}'` returns the adminUuid on the next REST call?"

    Probe script outline (record in 22-A1-PROBE.md):
      1. Create test user via service-role admin.createUser({email, password, email_confirm:true}).
      2. admin.updateUserById(uid, {app_metadata: {impersonator_id: '00000000-0000-0000-0000-000000000001'}}).
      3. admin.generateLink({type:'magiclink', email}) → extract access_token from hash.
      4. fetch supabase REST endpoint /rest/v1/rpc/echo_jwt_claims (a probe RPC that returns current_setting('request.jwt.claims',true)::jsonb — author inline as part of the probe).
      5. Assert response includes `"impersonator_id":"00000000-0000-0000-0000-000000000001"` within app_metadata.

    Record PASS/FAIL + propagation latency. If FAIL → 22-A1-PROBE.md must include the fallback decision: switch plan 22-03 implementation to Custom Access Token Hook + document Vault `IMPERSONATION_JWT_SIGNING_KEY` mint as new vendor pass.

    Step 5: Validate worktree state — copy `supabase/.temp/` to worktree if applicable (`reference_supabase_worktree_temp_state.md`).

    Report to user: dry-run log path, push exit code, 5 verification counts, A1 PROBE verdict (PASS/FAIL), and any decision implications for plan 22-03.
  </how-to-verify>
  <resume-signal>Reply "approved" to mark applied + A1 PROBE recorded; OR describe any verification mismatch (e.g. "1 migration was Skipped — Skipping line: X") so executor can fix the offending filename before re-running.</resume-signal>
  <done>Migrations live on remote + 5 verification queries pass + 22-A1-PROBE.md committed with A1 PASS/FAIL verdict + fallback decision (if FAIL).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Authenticated users hitting PostgREST; admin RPC bodies must self-verify is_staff |
| PostgREST request → RLS | request.jwt.claims is set by PostgREST at request-start; impersonation read by RLS write-deny |
| Vault → cancel_account_deletion RPC | HMAC key never leaves DB; pulled via vault.decrypted_secrets |
| Storage cleanup → dsar-exports objects | Direct storage.objects DELETE requires storage.allow_delete_query GUC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | S | admin RPC callers (admin_list_members, admin_set_feature_flag_override, admin_log_*, cancel_account_deletion) | mitigate | Every SECURITY DEFINER RPC starts with `if not exists (select 1 from public.profiles where id = auth.uid() and is_staff = true) then raise exception 'forbidden' using errcode = '42501'; end if;` per analog 20261101000006_is_staff_helper.sql |
| T-22-02 | T | impersonation_write_deny_policies migration uses request.jwt.claims | mitigate | Pitfall 1 — never use app.X GUC (does not persist across PostgREST requests); 51 policies × `(current_setting('request.jwt.claims', true)::json #>> '{app_metadata,impersonator_id}') is null` enforce read at request-start |
| T-22-03 | R | audit cascade during cancel_account_deletion + admin_set_feature_flag_override + admin_log_* | mitigate | `set_config('app.suppress_audit', 'true', true)` at top of each function body — analog 20260601000017_audit_trigger_suppress_guc.sql |
| T-22-04 | I | feature_flag_overrides public read | mitigate | RLS select-own (auth.uid() = user_id); NO authenticated write policy; admin RPC is the only writer |
| T-22-05 | I | dsar_requests visibility cross-user | mitigate | RLS select-own + insert-own + status updates limited to service_role |
| T-22-06 | I | dsar-exports storage objects | mitigate | Storage RLS scoped to (storage.foldername(name))[1] = auth.uid()::text; service-role uploads with that folder convention |
| T-22-07 | T | cancel_account_deletion token forgery | mitigate | HMAC-SHA256 token using Vault-stored CANCEL_DELETION_HMAC_KEY; 7-day TTL enforced via initiated_at delta check inside RPC |
| T-22-08 | E | new audit_action_type values used in same migration as ADD VALUE | mitigate | Pitfall 3 — File 02 is enum-add-only; File 15 (admin_stripe_action_audit_rpc) is a SEPARATE migration that consumes the new values |
| T-22-09 | E | filename regex skip on letter-suffix timestamps | mitigate | Pitfall 2 — every filename uses strict <14-digit>_name.sql; dry-run grep "^Skipping" in Task 3 |
| T-22-10 | D | cohort_retention re-identification via low-bucket cells | mitigate | k-anonymity contract: client-side renders "<10" when count(distinct user_id) < 10; comment in File 10 header binds View consumers |
</threat_model>

<verification>
- All 16 migrations have strict 14-digit `_name.sql` filenames (Pitfall 2 sweep).
- `supabase db push --linked --dry-run` emits zero `Skipping migration` lines.
- `supabase db push --linked` applies all 16; remote project `ytnsipxxmzgaebkqmokp` reflects new schema (5 verification queries in Task 3).
- 35 test scaffolds exist and import their target modules (failing import is OK at Wave 0).
- Zero `it.fixme`/`test.fixme` across new test files (Pre-emptive warning 10).
- 22-A1-PROBE.md committed with PASS/FAIL verdict for app_metadata JWT propagation.
- finalize_account_deletions_batch() body uses `interval '7 days'` — verifiable via `supabase db query --linked "select pg_get_functiondef('public.finalize_account_deletions_batch'::regproc)::text;" | grep -c "7 days"` returns ≥1.
- All 51 impersonation deny policies present (3 ops × 17 tables).
</verification>

<success_criteria>
- 16 migrations land on `ytnsipxxmzgaebkqmokp` with no Skipping lines.
- 35 test scaffolds exist and are picked up by `npm test --run` (expected-fail or expected-skip, NOT cannot-find-module — that's OK for the scaffolds that import not-yet-existing modules; module-resolution will green up as Wave 1+2 lands).
- 22-A1-PROBE.md records the A1 verdict so plan 22-03 picks the right impersonation implementation.
- VALIDATION.md per-task rows updated with the 35 test files mapped to Plan 22-01 in the Wave column.
- Phase 22 schema is live; Wave 1 plans can rely on every table/policy/RPC referenced in their PLAN.md.
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-01-SUMMARY.md` per template. Include: migration count + push exit code, list of 5 verification query results, A1 PROBE verdict + fallback decision (if any), list of 35 test scaffolds with their owner-plan IDs.
</output>
