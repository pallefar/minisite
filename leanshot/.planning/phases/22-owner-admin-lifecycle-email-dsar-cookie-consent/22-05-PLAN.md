---
phase: 22
plan: 05
type: execute
wave: 2
depends_on: [01, 02]
autonomous: true
requirements: [DEL-01, DEL-02]
requirements_addressed: [DEL-01, DEL-02]
files_modified:
  - leanshot/src/components/dashboard/settings/DeleteAccountModal.tsx
  - leanshot/src/lib/account-delete.ts
  - leanshot/src/components/soft-delete/SoftDeleteCountdownBanner.tsx
  - leanshot/src/components/soft-delete/__tests__/SoftDeleteCountdownBanner.test.tsx
  - leanshot/src/components/dashboard/settings/DeleteAccountModal.test.tsx
  - leanshot/src/pages/cancel-deletion.tsx
  - leanshot/e2e/account-delete-cancel.spec.ts

must_haves:
  truths:
    - "DeleteAccountModal.tsx copy updated 30 days → 7 days at lines 99, 108, 137 (Conflict #2 + Pitfall 9 sweep)"
    - "account-delete.ts:65-66 toast copy updated 30 days → 7 days"
    - "Every match of `30 days` across leanshot/src/ + leanshot/e2e/ relating to soft-delete is updated (grep sweep)"
    - "Post-delete confirmation invokes lifecycle-transactional with template='deletion_scheduled' carrying cancel_url with HMAC token (per RESEARCH Open Q1 + Pattern 5)"
    - "SoftDeleteCountdownBanner mounts at AppShell root when pending_account_deletions row exists for current user; displays N-days-remaining + 'Cancel deletion' CTA"
    - "Cancel CTA hits cancel_account_deletion RPC (migration 20270601000014) with the token; on success, banner unmounts + toast 'Deletion canceled. Welcome back!'"
    - "/cancel-deletion route reads token from URL query, invokes RPC, handles 4 outcomes (success, invalid token, expired, already deleted) per UI-SPEC §Soft-delete copy"
    - "e2e/account-delete-cancel.spec.ts: full round-trip — request deletion (admin.generateLink stub for email) → click cancel-link → assert pending row gone + banner gone"
    - "Tap count from settings to delete confirm = 3 (Apple §5.1.1(v)) — verified by RTL assertion in DeleteAccountModal.test.tsx"
  artifacts:
    - path: leanshot/src/components/soft-delete/SoftDeleteCountdownBanner.tsx
      provides: "DEL-01 sticky banner at AppShell root reading pending_account_deletions"
    - path: leanshot/src/pages/cancel-deletion.tsx
      provides: "Landing for HMAC-signed cancel link from deletion_scheduled email"
  key_links:
    - from: leanshot/src/components/dashboard/settings/DeleteAccountModal.tsx
      to: supabase/functions/lifecycle-transactional/index.ts
      via: "After successful initiate_account_deletion, invoke {template:'deletion_scheduled', user_id, data:{cancel_url, days_remaining:7}}"
      pattern: "lifecycle-transactional"
    - from: leanshot/src/pages/cancel-deletion.tsx
      to: public.cancel_account_deletion(token)
      via: "rpc invoke with token from URL hash"
      pattern: "cancel_account_deletion"
    - from: leanshot/src/components/soft-delete/SoftDeleteCountdownBanner.tsx
      to: public.pending_account_deletions
      via: "select where user_id = auth.uid() to compute days_remaining"
      pattern: "pending_account_deletions"
---

<objective>
Wire the 30-day → 7-day soft-delete UX (Conflict #2 resolution): modal copy sweep + post-delete confirmation email + countdown banner + cancel-link landing page.

Purpose: Land DEL-01 + DEL-02 user-facing surface. The backend cron is already 7 days (plan 22-01 File 01). All that's left is making the UI consistent with the new window + introducing the cancel-by-email affordance Phase 7 didn't have.

Output: Modal copy updated, banner shipped, cancel-link route shipped, e2e round-trip green.
</objective>

<execution_context>
@/Users/karstenhaldan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karstenhaldan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md
@/Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/DeleteAccountModal.tsx
@/Users/karstenhaldan/minisite/leanshot/src/lib/account-delete.ts
@/Users/karstenhaldan/minisite/leanshot/src/components/clinic/ClinicContextBar.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/soft-delete/__tests__/SoftDeleteCountdownBanner.test.tsx
@/Users/karstenhaldan/minisite/leanshot/e2e/account-delete-cancel.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: HEAD-reset + grep sweep for "30 days" across src/ + e2e/</name>
  <files>(read-only sweep documenting hits)</files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Pitfall 9 lines 770-779 — known hits in DeleteAccountModal.tsx + account-delete.ts),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (lines 637-647 — account-delete.ts:65-66 + DeleteAccountModal.tsx lines 99, 108, 137)
  </read_first>
  <action>
    HEAD-reset verify: `test -f /Users/karstenhaldan/minisite/supabase/migrations/20270601000001_finalize_cron_seven_days.sql || exit 1` (Wave 0 cron change must be live before we update UI copy that asserts the window).

    Grep sweep — record all hits in 22-05-SUMMARY.md draft notes:
    - `cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; grep -rn "30 days" src/ e2e/ 2>/dev/null | grep -iE "(delet|soft|grace|pending)"` — list every hit; the surfaces that must change include DeleteAccountModal.tsx (lines 99, 108, 137), account-delete.ts (lines 65-66 toast), DeleteAccountModal.test.tsx (assertion strings), e2e/account-delete*.spec.ts.
    - Also grep `"30-day"` (hyphenated form) and `"thirty days"` (spelled-out form).
    - Exclude unrelated 30-day usages (e.g., Stripe billing periods, last-90-days windows). Document the exclusion list.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; grep -rn "30 days" src/ e2e/ 2&gt;/dev/null | grep -iE "(soft|delet|grace|pending)" | wc -l</automated>
  </verify>
  <done>All in-scope hits enumerated and recorded for Task 2-3 to address.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update DeleteAccountModal.tsx + account-delete.ts copy + wire lifecycle-transactional invoke</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/DeleteAccountModal.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/lib/account-delete.ts,
    /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/DeleteAccountModal.test.tsx
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/DeleteAccountModal.tsx (full file — copy strings + flow + typed-confirm),
    /Users/karstenhaldan/minisite/leanshot/src/lib/account-delete.ts (full — RPC invoke + toast),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (Settings → Delete Account flow lines 298-313; Copywriting Contract lines 562-579),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (lines 744-757 — DeleteAccountModal modify spec)
  </read_first>
  <behavior>
    - Test: modal heading text === "Delete account" (per UI-SPEC Copywriting line 567).
    - Test: modal body intro === "This will permanently delete your LeanShot account, including:" (line 569).
    - Test: bullet list includes "injections", "photos", "weight logs", "AI history", "doctor shares", "affiliate referrals".
    - Test: typed-text label === "Type DELETE MY ACCOUNT to confirm" (line 572).
    - Test: typed-text placeholder === "DELETE MY ACCOUNT" (line 573).
    - Test: confirm button label === "Delete account" (line 575); when busy === "Deleting...".
    - Test: success toast === "Account scheduled for deletion. We've sent a confirmation email." (line 577).
    - Test: NO occurrence of "30 days" or "30-day" remains in the file.
    - Test: After successful initiate_account_deletion RPC, the modal invokes supabase.functions.invoke('lifecycle-transactional', {body: {template:'deletion_scheduled', user_id, data:{cancel_url, days_remaining:7}}}).
    - Test: Tap count assertion — programmatic render of SettingsPage → click "Account" → click "Delete account" → modal appears within 3 user events (Apple §5.1.1(v) compliance).
  </behavior>
  <action>
    Modify `DeleteAccountModal.tsx`:
    - Line 99: change "This starts a 30-day soft-delete. For 30 days..." → use UI-SPEC body copy ("This will permanently delete your LeanShot account, including:" + bullet list).
    - Line 107: change "Same-email re-signup during the 30-day window is blocked. After the window ends..." → "Same-email re-signup during the 7-day window is blocked. After the window ends, you can re-register with the same email."
    - Line 137: change "Schedule deletion in 30 days" → "Delete account" (matches UI-SPEC Copywriting line 575).
    - Add retention disclosure text per UI-SPEC line 571: "Stripe payment records and affiliate ledger entries are retained for 7 years per IRS requirements (anonymized)."
    - After success path in handleSubmit: add `await supabase.functions.invoke('lifecycle-transactional', {body: {template: 'deletion_scheduled', user_id: user.id, data: {cancel_url: undefined /* lifecycle-transactional mints the HMAC token + builds URL */, days_remaining: 7}}});` — wrap in try/catch; failure to send the confirmation email is NOT a hard error (deletion already initiated).
    - Update success toast (currently delegated to account-delete.ts) — see Task 2 update to account-delete.ts.
    - Preserve typed-confirmation gate (existing lines 42-93), already-pending toast (lines 79-83), recent_auth_required inline-error handling (lines 71-77).

    Modify `account-delete.ts`:
    - Line 65-66: `toast('Account scheduled for deletion in 30 days. You've been signed out.', 'success');` → `toast("Account scheduled for deletion. We've sent a confirmation email.", 'success');` (matches UI-SPEC Copywriting line 577).

    Update/create `DeleteAccountModal.test.tsx`:
    - Cover all 10 behavior bullets above. Use @testing-library/react.
    - Tap-count assertion: render `<SettingsPage>` (or wrap modal opener) → simulate user.click on "Account" section → user.click on "Delete account" → assert modal is open. Count user.click events ≤ 3.

    Use ONLY string literals from UI-SPEC §Copywriting — do not paraphrase.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; ! grep -E "30 days|30-day|thirty days" src/components/dashboard/settings/DeleteAccountModal.tsx src/lib/account-delete.ts &amp;&amp; npm test -- --run DeleteAccountModal 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓"</automated>
  </verify>
  <done>Modal copy + toast updated to 7-day; lifecycle-transactional invoke wired; tap-count test asserts ≤3; zero "30 days" string remaining in the two files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement SoftDeleteCountdownBanner + test + AppShell mount integration note</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/soft-delete/SoftDeleteCountdownBanner.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/soft-delete/__tests__/SoftDeleteCountdownBanner.test.tsx
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/components/clinic/ClinicContextBar.tsx (analog — sticky banner mounted at AppShell root pattern),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (Soft-delete countdown banner lines 315-323; Copywriting lines 580-586),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave F SoftDeleteCountdownBanner section — analog ClinicContextBar)
  </read_first>
  <behavior>
    - Test: returns null when no pending_account_deletions row exists for current user.
    - Test: renders banner with `role="alert" aria-live="polite"` (per UI-SPEC line 322) when row exists.
    - Test: text matches "Account scheduled for deletion in {N} days." where N = ceil((initiated_at + 7d - now()) / 1day).
    - Test: Cancel CTA labeled "Cancel deletion" (per UI-SPEC Copywriting line 585).
    - Test: clicking Cancel invokes `supabase.rpc('cancel_account_deletion', {p_token: ...})` — token comes from query param OR null (if banner shown after page reload, token may be unavailable; in that case Cancel CTA opens /cancel-deletion route with helper text).
    - Test: success path → banner unmounts + toast "Deletion canceled. Welcome back!" (line 586).
    - Test: banner has `bg-[var(--color-danger-soft)] text-[var(--color-danger)] border-b border-[var(--color-danger)]` + sticky top-0 z-[60] + h-12 per UI-SPEC line 319.
    - Test: when impersonation banner is mounted (App state), SoftDelete banner is NOT mounted (priority per UI-SPEC line 317).
  </behavior>
  <action>
    Create `src/components/soft-delete/SoftDeleteCountdownBanner.tsx`:
    - Imports: `import { AlertTriangle } from 'lucide-react';` + `useEffect, useState, useCallback` + supabase client + toast hook + useImpersonation hook (from plan 22-09; for now import as `useImpersonation` from `@/components/impersonation/useImpersonation` and rely on it returning `{active: false}` if module is stub).
    - Component reads current user via `supabase.auth.getUser()`; SELECTs `pending_account_deletions` for `user_id = auth.uid()` returning `initiated_at`.
    - Computes `daysRemaining = Math.ceil((initiatedAt + 7*86400000 - Date.now()) / 86400000)`.
    - Conditional render: `if (!row || daysRemaining <= 0) return null; if (useImpersonation().active) return null;` (impersonation wins per UI-SPEC priority).
    - Render banner per UI-SPEC §Soft-delete countdown banner — sticky div with `bg-[var(--color-danger-soft)] text-[var(--color-danger)] border-b border-[var(--color-danger)] sticky top-0 z-[60] h-12 flex items-center justify-between gap-3 px-4`, `role="alert" aria-live="polite"`.
    - Layout: `<AlertTriangle aria-hidden />` icon + body span `Account scheduled for deletion in {daysRemaining} days.` + inverse button "Cancel deletion" → onClick handler.
    - Cancel handler: prefer token from `window.location.search` `?cancel_token=...`; if absent navigate to `/cancel-deletion` (which has help text + email-resend CTA — handled in Task 4). If token present invoke `supabase.rpc('cancel_account_deletion', {p_token: token})`, on success show toast "Deletion canceled. Welcome back!" + setRow(null).
    - Note for plan 22-12 wiring: AppShell must mount `<SoftDeleteCountdownBanner />` above the topbar in App.tsx.

    Update test file: cover all 8 behavior bullets above.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run SoftDeleteCountdownBanner 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓"</automated>
  </verify>
  <done>Banner component exists with priority logic; test covers all 8 behaviors green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement /cancel-deletion route + green e2e round-trip</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/pages/cancel-deletion.tsx,
    /Users/karstenhaldan/minisite/leanshot/e2e/account-delete-cancel.spec.ts
  </files>
  <read_first>
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/reference_supabase_auth_traps.md (hash-route + email-link traps; admin.generateLink for tests),
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/reference_playwright_state_seeding.md (addInitScript over goto+evaluate for any localStorage state),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (Soft-delete copy lines 580-586)
  </read_first>
  <behavior>
    - Test (e2e): create test user via admin.auth.admin.createUser → sign in → POST initiate_account_deletion RPC → assert pending_account_deletions row exists → mint token via admin server-side helper (mirrors the HMAC the lifecycle-transactional fn would emit) → visit /cancel-deletion?token=... → assert page shows "Cancel scheduled deletion?" + confirm button → click confirm → assert pending_account_deletions row is gone + assert success message visible.
    - Test (e2e): invalid token → page shows "This link is no longer valid" + CTA back to settings.
    - Test (e2e): expired token (initiated_at > 7 days ago) → page shows "Your cancel window has expired" + helpful text.
  </behavior>
  <action>
    Create `src/pages/cancel-deletion.tsx`:
    - Read token from URL `?token=...` (use window.location.search; project intentionally has no router per CLAUDE.md, so this is a standalone HTML mount target — extend index.html OR add as a Suspense-lazy view in App.tsx that activates when window.location.pathname === '/cancel-deletion').
    - Single-column layout, max-w-md, centered (`max-w-md mx-auto px-4 py-12`).
    - Heading: "Cancel scheduled deletion?".
    - Body: "Click Confirm to cancel the deletion request and restore your account." (planner discretion; UI-SPEC doesn't lock this string — pick consistent with brand voice).
    - Confirm CTA: `<Button variant="primary">Confirm</Button>` → calls `supabase.rpc('cancel_account_deletion', {p_token: token})`.
    - State machine: idle → confirming → success ("Deletion canceled. Welcome back!" + link to /dashboard) | error (4 outcomes per behavior list: invalid_token, token_expired, hmac_key_missing, already_cancelled — derived from RPC error code).
    - If no `?token=` in URL: show "Cancel deletion" heading + "If you didn't request this email, no action is needed." + link to settings.

    Create `e2e/account-delete-cancel.spec.ts`:
    - test.describe.skip if SUPABASE_URL/SERVICE_ROLE_KEY missing.
    - Setup: admin.createUser → page.addInitScript to seed session (per `reference_playwright_state_seeding.md` — do NOT use goto+evaluate, races supabase-js INITIAL_SESSION).
    - Trigger initiate_account_deletion via supabase-js with seeded session.
    - Server-side helper: mint HMAC token matching the format produced by lifecycle-transactional. Either (a) import the same HMAC helper from supabase Edge Fn shared code if extractable, OR (b) call a test-only RPC `mint_cancel_token_for_test(p_user_id, p_initiated_at)` (NEW — add to migration only if needed; cleanly skip if too invasive — fall back to invoking lifecycle-transactional in test mode and intercept the email).
    - Easiest path: invoke lifecycle-transactional with RESEND_API_KEY=test-stub → fn would still produce the cancel_url but skip the email send. Capture the cancel_url from the fn response or test seam.
    - Visit /cancel-deletion?token=... → click Confirm → assert success.
    - Cleanup: admin.deleteUser.

    Per-file slug prefix per Pattern S5: `const CANCEL_PREFIX = 'phase22-cancel-deletion-e2e';`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; test -f src/pages/cancel-deletion.tsx &amp;&amp; test -f e2e/account-delete-cancel.spec.ts &amp;&amp; npx playwright test --reporter=line e2e/account-delete-cancel.spec.ts 2&gt;&amp;1 | tail -3 | grep -qE "passed|skipped"</automated>
  </verify>
  <done>cancel-deletion route exists; e2e round-trip runs green (or skipped if env-gated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| email link → cancel-deletion route | HMAC token verifies user identity without re-auth (allows cancellation even from logged-out state) |
| client → cancel_account_deletion RPC | RPC re-verifies HMAC against Vault key + 7-day TTL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-33 | T | Cancel-link token forgery | mitigate | HMAC-SHA256 with Vault CANCEL_DELETION_HMAC_KEY; RPC verifies in plan 22-01 File 14 |
| T-22-34 | T | Cancel-link replay after deletion already finalized | mitigate | RPC checks `if not exists (select 1 from pending_account_deletions where user_id = v_user_id) then raise 'already_finalized';` (added to migration 14) |
| T-22-35 | I | UI copy mismatch (30 days vs 7 days) creates user-facing drift | mitigate | Pitfall 9 grep sweep in Task 1 catches all hits; Tasks 2-3 update each |
| T-22-36 | E | Apple §5.1.1(v) audit fails if tap count > 3 | mitigate | RTL test asserts ≤3 user.click events from settings to modal-open |
</threat_model>

<verification>
- `grep -rE "30 days|30-day" leanshot/src/ leanshot/e2e/ | grep -iE "(soft|delet|grace)"` returns zero hits.
- DeleteAccountModal test green (10 behaviors).
- SoftDeleteCountdownBanner test green (8 behaviors).
- /cancel-deletion route exists + e2e green or skipped.
- AppShell mount note recorded for plan 22-12 integration task.
</verification>

<success_criteria>
- DEL-01 user-facing surface complete: modal updated, banner shipped, cancel-link route shipped.
- DEL-02 cross-phase contract honored: backend cascade (Phase 19 account-delete Edge Fn) still invoked unchanged; only the soft-delete grace window changed.
- Tap-count compliance asserted programmatically (Apple §5.1.1(v) primary check; iOS native check defers to P16).
- VALIDATION.md per-task rows updated for DEL-01 + DEL-02 with Plan 22-05.
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-05-SUMMARY.md` per template. Include: grep sweep hits resolved (line-by-line), tap-count test result, e2e round-trip status, AppShell mount note for plan 22-12.
</output>
