---
phase: 22
plan: 07
type: execute
wave: 2
depends_on: [01, 03, 06]
autonomous: true
requirements: [ADMIN-04, ADMIN-06]
requirements_addressed: [ADMIN-04, ADMIN-06]
files_modified:
  - leanshot/src/components/admin/members/RefundModal.tsx
  - leanshot/src/components/admin/members/CancelSubModal.tsx
  - leanshot/src/components/admin/members/CompSubModal.tsx
  - leanshot/src/components/admin/AdminAffiliatesReviewQueue.tsx
  - leanshot/src/components/admin/pages/AdminAffiliatesPage.tsx
  - leanshot/src/lib/admin/admin-stripe-actions.ts
  - leanshot/src/lib/admin/affiliate-review.ts
  - leanshot/src/components/admin/members/__tests__/RefundModal.test.tsx
  - supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql

must_haves:
  truths:
    - "RefundModal implements 3-step flow per UI-SPEC §Refund modal: Pick charge → Enter amount → Confirm with typed-text 'REFUND ${amount}'"
    - "CancelSubModal uses existing <Confirm> primitive with body 'Cancel {email}'s subscription? They'll keep access until {period_end}.' + destructive CTA"
    - "CompSubModal: Pill quick-fill for comp duration (1mo / 3mo / 6mo / custom) + Confirm"
    - "All three modals invoke admin-stripe-action Edge Fn (plan 22-03) and refresh the underlying Stripe/billing data on success"
    - "AdminAffiliatesReviewQueue extends Phase 19 AdminAffiliatesScaffold in-place by adding 4 per-row actions (Approve, Hold, Pay out, Reject) + fraud-signal badges column + row-expansion panel"
    - "ADMIN-06 Approve action writes affiliate_conversions.status = 'confirmed' via new RPC admin_approve_affiliate_conversion (closes Phase 19 BL-11 status-graph gap: pending → confirmed transition now has an owner)"
    - "Hold action writes status = 'on_hold'; Reject writes status = 'rejected'; Pay out is a no-op trigger (cron 19-09 picks up confirmed rows on next run)"
    - "Row-expansion panel shows fraud signal detail (Z-score values, IP cluster size, click-velocity, public-email flag) + payout history mini-table + audit log mini-list per UI-SPEC §/admin/affiliates"
    - "Receipt email fires on successful refund (already wired in admin-stripe-action Edge Fn from plan 22-03)"
  artifacts:
    - path: leanshot/src/components/admin/members/RefundModal.tsx
      provides: "ADMIN-04 3-step refund modal"
    - path: leanshot/src/components/admin/AdminAffiliatesReviewQueue.tsx
      provides: "ADMIN-06 extension of Phase 19 scaffold with status-machine writer for confirmed"
    - path: supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql
      provides: "admin_approve/hold/reject_affiliate_conversion RPCs — status writers (PATTERNS S6)"
  key_links:
    - from: leanshot/src/components/admin/members/RefundModal.tsx
      to: supabase/functions/admin-stripe-action/index.ts
      via: "supabase.functions.invoke('admin-stripe-action', {body: {operation:'refund', target_user_id, charge_id, amount_cents, reason}})"
      pattern: "admin-stripe-action"
    - from: leanshot/src/components/admin/AdminAffiliatesReviewQueue.tsx
      to: public.admin_approve_affiliate_conversion
      via: "supabase.rpc — writes status='confirmed' (closes Phase 19 BL-11 status-graph gap)"
      pattern: "admin_approve_affiliate_conversion"
---

<objective>
Ship the admin action modals (ADMIN-04: refund/cancel/comp) and the affiliate review queue extension (ADMIN-06) that closes the Phase 19 status-graph gap.

Purpose: This plan bridges plan 22-06 (members table stubs) and plan 22-03 (admin Edge Fns) — the modals are the connective tissue. Also closes Critical Conflict #1 from RESEARCH: nobody currently writes `affiliate_conversions.status='confirmed'`, so monthly payouts ship dead. ADMIN-06 Approve writes it.

Output: 3 admin modals + extended affiliate scaffold + 3 status-writer RPCs.
</objective>

<execution_context>
@/Users/karstenhaldan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karstenhaldan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md
@/Users/karstenhaldan/minisite/leanshot/src/components/admin/AdminAffiliatesScaffold.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/DeleteAccountModal.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Modal.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Confirm.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/admin/members/__tests__/RefundModal.test.tsx

<interfaces>
<!-- From plan 22-03 Edge Fn -->
supabase.functions.invoke('admin-stripe-action', {body: {operation: 'refund'|'cancel'|'comp', target_user_id: uuid, ...op-specific}})
returns 200 {refund_id?, amount_refunded?} | 502 {error: 'stripe_<code>'} | 403 forbidden

<!-- From Phase 19 affiliate_conversions enum -->
status: 'pending' | 'confirmed' | 'flagged' | 'rejected' | 'on_hold' | 'paid_out'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HEAD-reset verify + RefundModal (3-step) + CancelSubModal + CompSubModal + admin-stripe-actions.ts wrapper</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/RefundModal.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/CancelSubModal.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/CompSubModal.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/lib/admin/admin-stripe-actions.ts,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/__tests__/RefundModal.test.tsx
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/DeleteAccountModal.tsx (typed-confirm gate lines 42-93; typedConfirmMatches helper; busy state),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (Refund modal lines 370-388 — 3-step structure; Copywriting lines 656-680; Destructive actions table line 688 for Cancel/Comp confirmation copy),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave E RefundModal section lines 716-738 — typed-confirm gate; clone DeleteAccountModal),
    /Users/karstenhaldan/minisite/leanshot/src/components/ui/Modal.tsx (existing Modal primitive),
    /Users/karstenhaldan/minisite/leanshot/src/components/ui/Confirm.tsx (existing Confirm primitive — used by Cancel/Comp modals)
  </read_first>
  <behavior>
    - Test (RefundModal): step 1 — renders "Pick a charge" heading + charge select (from last 90d charges) OR "Use most recent" toggle; Continue disabled until selection.
    - Test (RefundModal): step 2 — renders "Refund amount" + amount Input with `max={charge.amount}` + Pill quick-fill (Full / Half / Custom) + reason Input (optional, 200 chars max); Continue disabled if amount empty or > charge.amount.
    - Test (RefundModal): step 3 — renders "Confirm refund" + summary "You are about to refund ${amount} to {email}." + typed-text Input "Type REFUND ${amount} to confirm" exact-match case-sensitive; Submit button enabled only when typed matches.
    - Test (RefundModal): submit invokes `supabase.functions.invoke('admin-stripe-action', {body: {operation:'refund', target_user_id, charge_id, amount_cents, reason}})` → on success modal closes + toast success.
    - Test (RefundModal): Stripe error → inline error above action row "Stripe rejected the refund: {message}. Try again or contact support." (per UI-SPEC line 680).
    - Test (RefundModal): Back button on step 2 returns to step 1 with selection preserved.
    - Test (RefundModal): Esc on step ≥ 2 prompts confirmation before closing (per UI-SPEC line 388).
    - Test (CancelSubModal): renders Confirm primitive with body "Cancel {email}'s subscription? They'll keep access until {period_end}." + destructive CTA "Cancel subscription"; on confirm invokes admin-stripe-action operation:'cancel'.
    - Test (CompSubModal): renders Confirm with Pill quick-fill (1 month / 3 months / 6 months / Custom); on confirm invokes admin-stripe-action operation:'comp' with comp_days.
  </behavior>
  <action>
    HEAD-reset verify: `test -f /Users/karstenhaldan/minisite/supabase/functions/admin-stripe-action/index.ts || exit 1` (plan 22-03 Wave 1 must be live).

    Implement `admin-stripe-actions.ts`:
    - `refundCharge({targetUserId, chargeId, amountCents, reason?: string}): Promise<{refundId: string; amountRefunded: number}>` — wraps supabase.functions.invoke('admin-stripe-action', {body: {operation:'refund', ...}}). Discriminated-error pattern.
    - `cancelSubscription({targetUserId, subscriptionId}): Promise<void>`.
    - `compSubscription({targetUserId, subscriptionId, compDays}): Promise<void>`.
    - Map Edge Fn error codes (`stripe_<code>`, `forbidden`, etc.) to typed AdminStripeError.

    Implement `RefundModal.tsx` per UI-SPEC §Refund modal:
    - Props: `{charge: StripeCharge; targetUserEmail: string; isOpen: boolean; onClose: () => void; onSuccess: (refundId: string) => void}`.
    - State machine: step 1 → step 2 → step 3 → submitting → success/error.
    - Use existing Modal primitive (`<Modal size="md">`); step heading with "Step {N} of 3" + step title (per UI-SPEC line 374).
    - Step 3 typed-confirm: `typedConfirmMatches(typed, "REFUND $" + (amount/100).toFixed(2))` — clone helper from DeleteAccountModal pattern, exact-match + case-sensitive.
    - On submit: `await refundCharge({targetUserId, chargeId: charge.id, amountCents, reason})`. Toast success per UI-SPEC line 679 "Refund of ${amount} issued to {email}". On error → inline error per UI-SPEC line 680.
    - Accessibility: step heading aria-live="polite" so step transitions announce.

    Implement `CancelSubModal.tsx` — wraps `<Confirm>` primitive with body copy per UI-SPEC Destructive table line 688; on confirm invokes `cancelSubscription`.

    Implement `CompSubModal.tsx` — adds Pill quick-fill for comp_days; otherwise like CancelSubModal; copy per UI-SPEC line 689 pattern.

    Wire MemberRowActions (from plan 22-06) to open these modals — replace the placeholder stubs from plan 22-06 Task 1. Touches `MemberRowActions.tsx` + `MemberStripeTab.tsx`; these files are also touched by plan 22-06. Per Pre-emptive warning 4: use `git commit -- <pathspec>` and coordinate with plan 22-06's diff — at execute time, the shared-file conflict can be resolved by sequencing 22-06 before 22-07 (already enforced by depends_on: [06]).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run RefundModal 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓"</automated>
  </verify>
  <done>3 modals exist; RefundModal 9 behavior tests green; CancelSubModal + CompSubModal smoke tests green; MemberRowActions stubs replaced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admin_affiliate_review_rpcs migration + AdminAffiliatesReviewQueue extension</name>
  <files>
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000019_admin_affiliate_review_rpcs.sql,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/AdminAffiliatesReviewQueue.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/pages/AdminAffiliatesPage.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/lib/admin/affiliate-review.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/AdminAffiliatesScaffold.tsx (full file 379 lines — extend in-place per UI-SPEC §/admin/affiliates lines 268-274),
    /Users/karstenhaldan/minisite/supabase/migrations/20270101000002_affiliate_clicks_conversions_payouts.sql (post-Wave-0 — affiliate_conversions schema with status enum),
    /Users/karstenhaldan/minisite/supabase/migrations/20270101000008_fraud_trigger_conversion.sql (Phase 19 fraud trigger writes status='flagged'; we add the 'confirmed' transition writer here per Critical Conflict #1),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Critical Conflict #1 lines 119-128),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (lines 268-274 — review queue layout; Copywriting lines 519-536)
  </read_first>
  <behavior>
    - Test (RPC migration): admin_approve_affiliate_conversion(p_conversion_id uuid) — is_staff-gated; UPDATE affiliate_conversions SET status='confirmed', reviewed_by=auth.uid(), reviewed_at=now() WHERE id=p_conversion_id; INSERT audit_logs.
    - Test (RPC): admin_hold_affiliate_conversion writes status='on_hold'.
    - Test (RPC): admin_reject_affiliate_conversion writes status='rejected'.
    - Test (UI): AdminAffiliatesReviewQueue extends scaffold with 4 per-row buttons (Approve, Hold, Pay out, Reject) per UI-SPEC copy line 524-527.
    - Test (UI): clicking Approve invokes admin_approve_affiliate_conversion → row updates to status='confirmed' badge + audit row.
    - Test (UI): row click expands inline panel with fraud signal detail (Z-score, IP cluster, velocity, public-email) + payout history + audit log per UI-SPEC line 272.
    - Test (UI): filter Pill segmented per UI-SPEC line 270: All / Pending / Flagged / Approved / Rejected / Suspended.
    - Test (UI): bulk actions NOT present (per UI-SPEC line 273 — bulk deferred to v1.3).
  </behavior>
  <action>
    Author migration `20270601000019_admin_affiliate_review_rpcs.sql`:
    - Three RPCs: `admin_approve_affiliate_conversion(p_conversion_id uuid)`, `admin_hold_affiliate_conversion(p_conversion_id uuid)`, `admin_reject_affiliate_conversion(p_conversion_id uuid, p_reason text)`.
    - Each: SECURITY DEFINER + is_staff gate + `set_config('app.suppress_audit','true',true)` + UPDATE affiliate_conversions + INSERT audit_logs with action_type = 'affiliate_conversion_approved'/'_held'/'_rejected' (NOTE: these enum values are NOT in plan 22-01 File 02 — add via this migration's preamble: `ALTER TYPE public.audit_action_type ADD VALUE IF NOT EXISTS 'affiliate_conversion_approved';` etc. Per Pitfall 3 — ADD VALUE must be in a separate migration from USE; since this migration has BOTH the ADD and the USE, split: this migration's preamble ADDs the values via implicit commit by ordering them as separate statements at the top, then the function bodies USE them in subsequent statements. Postgres permits this because the COMMIT between ALTER TYPE and CREATE FUNCTION happens at statement boundaries in plpgsql. If this fails at supabase db push, split into 20270601000019 (ADD only) + 20270601000020 (RPCs USE)).
    - Approve also enables payout eligibility — no extra column needed; cron 19-09 already filters `status='confirmed'` per Critical Conflict #1.
    - grant execute to authenticated.

    Implement `affiliate-review.ts` client wrapper: `approveAffiliateConversion`, `holdAffiliateConversion`, `rejectAffiliateConversion(id, reason)`. Discriminated AdminApiError.

    Implement `AdminAffiliatesReviewQueue.tsx` — copy AdminAffiliatesScaffold.tsx as starting point + modify in-place:
    - Add fraud-signal badges column (multi-Badge cell reading from conversion.fraud_signals JSONB).
    - Add 4 per-row action buttons (ghost variant): Approve (calls approveAffiliateConversion), Hold (calls hold), Pay out (no-op + tooltip "Cron picks up confirmed rows on next monthly run"), Reject (opens prompt for reason).
    - Add row-expansion: state `expandedRowId`; click row → expand inline panel with 3 sections per UI-SPEC line 272 (Fraud signals / Payout history / Audit log).
    - Filter Pill segmented per UI-SPEC line 270 with per-state count Badge.
    - Empty state per UI-SPEC line 536.

    Implement `AdminAffiliatesPage.tsx` — wraps AdminAffiliatesReviewQueue + AdminLayout.

    Per `feedback_status_machine_transition_owner.md`: this is the closeout of Phase 19 BL-11 — `confirmed` transition now has an owner. Verify in 22-07-SUMMARY.md that the next monthly payout cron (cron 19-09) would include rows approved here.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; supabase db push --linked --dry-run 2&gt;&amp;1 | grep -v "^Skipping" &amp;&amp; cd leanshot &amp;&amp; npm test -- --run AdminAffiliatesReviewQueue 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓|tests"</automated>
  </verify>
  <done>Migration 19 lands + 3 RPCs live; AdminAffiliatesReviewQueue extends scaffold with 4 actions + row-expansion + 6-state filter; status-machine writer for 'confirmed' now in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client RefundModal → admin-stripe-action | Caller JWT validated by Edge Fn gateway + is_staff re-checked in Fn body |
| client AdminAffiliatesReviewQueue → admin_approve_* RPC | is_staff gate in each RPC body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-42 | T | Admin issues refund > original charge amount | mitigate | RefundModal input `max={charge.amount}`; admin-stripe-action Edge Fn (plan 22-03) also enforces; Stripe API rejects natively |
| T-22-43 | T | Admin issues two refunds for same charge with different amounts (replay) | mitigate | admin-stripe-action uses `idempotencyKey = 'refund-' + chargeId + '-' + amountCents`; same-arg repeat returns existing refund record |
| T-22-44 | E | Affiliate review queue lets non-staff approve | mitigate | admin_approve_affiliate_conversion RPC is_staff-gated server-side; UI gate is UX-only per Pattern S1 |
| T-22-45 | T | Status-machine deadlock: confirmed but no payout cron pickup | mitigate | Critical Conflict #1 closed — cron 19-09 already filters status='confirmed'; this plan documents the now-active path in 22-07-SUMMARY.md |
| T-22-46 | E | Pitfall 3 — ALTER TYPE ADD VALUE + USE in same migration | mitigate | If split needed, file 19 ADD only + file 20 USE; dry-run before live push |
</threat_model>

<verification>
- RefundModal 3-step flow tested (9 behaviors).
- AdminAffiliatesReviewQueue: row actions + row-expansion + filter Pill present.
- New status-writer RPCs in migration 19 land successfully.
- Phase 19 BL-11 status-graph closed: `confirmed` has an owner.
</verification>

<success_criteria>
- ADMIN-04 + ADMIN-06 functional from /admin/members/{id} + /admin/affiliates.
- VALIDATION.md rows updated for ADMIN-04, ADMIN-06.
- Phase 19 status-graph gap (BL-11 — pending → confirmed) explicitly closed.
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-07-SUMMARY.md` per template. Include: modal test results, migration 19 push result, affiliate confirmed-writer end-to-end smoke (manual: approve a row → query cron.job + verify next run includes it).
</output>
