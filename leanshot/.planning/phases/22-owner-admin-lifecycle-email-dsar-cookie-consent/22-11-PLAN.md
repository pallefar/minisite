---
phase: 22
plan: 11
type: execute
wave: 2
depends_on: [01, 02, 04]
autonomous: true
requirements: [GDPR-03, ON-03]
requirements_addressed: [GDPR-03, ON-03]
files_modified:
  - leanshot/src/components/dsar/DsarPortalPage.tsx
  - leanshot/src/components/dsar/DsarStatusCard.tsx
  - leanshot/src/lib/dsar/dsar-export-client.ts
  - leanshot/src/lib/dsar/dsar-pdf-render.ts
  - leanshot/src/components/dashboard/settings/EmailPreferencesPage.tsx
  - leanshot/src/components/dashboard/settings/SettingsPage.tsx
  - leanshot/src/components/dsar/__tests__/DsarPortalPage.test.tsx
  - leanshot/src/lib/dsar/__tests__/dsar-pdf-render.test.ts

must_haves:
  truths:
    - "/settings/privacy/dsar route renders hero card + Export CTA → confirmation modal → POST to dsar-export Edge Fn (plan 22-04) via create_dsar_request RPC + dsar-export invoke"
    - "DsarStatusCard renders status badge per state (pending/in_progress/completed/rejected) per UI-SPEC §/settings/privacy/dsar lines 348-355"
    - "Completed status → 'Download bundle' CTA opens signed Storage URL in new tab + caption 'Available until {expiry_date}'"
    - "Rejected status → rejection_reason inline + 'Contact support' link"
    - "Status polling: subscribe to dsar_requests changes via Supabase Realtime (or 30s polling fallback) — aria-live='polite' on status badge announces transitions"
    - "/settings/email-preferences route renders 6 categories (Transactional locked + Welcome + Behavior-triggered + Retention + Weekly digest + Affiliate updates if user is affiliate)"
    - "Save preferences invokes lifecycle-preference-update Edge Fn (plan 22-02) → UPSERTs consent_records.email_preferences JSONB"
    - "SettingsPage.tsx gains 2 new links: 'Email preferences' + 'Privacy & DSAR' per UI-SPEC §SettingsPage sub-page pattern (Phase 15 inherited)"
    - "DSAR PDF render client-side path REMOVED (server-side only per plan 22-04 + Pitfall 10) — dsar-pdf-render.ts on client side is a thin wrapper for future use cases; for v1.2 only fires via the Edge Fn"
    - "History list shows past DSAR requests (date + status) per UI-SPEC line 354"
  artifacts:
    - path: leanshot/src/components/dsar/DsarPortalPage.tsx
      provides: "GDPR-03 user-facing entry point"
    - path: leanshot/src/components/dashboard/settings/EmailPreferencesPage.tsx
      provides: "ON-03 self-serve preference center"
    - path: leanshot/src/lib/dsar/dsar-export-client.ts
      provides: "Client invoker — calls create_dsar_request RPC + invokes dsar-export Edge Fn + polls status"
  key_links:
    - from: leanshot/src/components/dsar/DsarPortalPage.tsx
      to: public.create_dsar_request RPC (migration 18 plan 22-04)
      via: "supabase.rpc('create_dsar_request') returns request_id; then supabase.functions.invoke('dsar-export', {body: {request_id}})"
      pattern: "create_dsar_request"
    - from: leanshot/src/components/dashboard/settings/EmailPreferencesPage.tsx
      to: supabase/functions/lifecycle-preference-update/index.ts
      via: "supabase.functions.invoke('lifecycle-preference-update', {body: {categories: {welcome, behavior_triggered, retention, weekly_digest, affiliate}}})"
      pattern: "lifecycle-preference-update"
    - from: leanshot/src/components/dashboard/settings/SettingsPage.tsx
      to: leanshot/src/components/dsar/DsarPortalPage.tsx + EmailPreferencesPage.tsx
      via: "2 new sub-page links"
      pattern: "/settings/(privacy/dsar|email-preferences)"
---

<objective>
Ship the user-facing GDPR-03 portal (DSAR) and ON-03 (email preferences center).

Purpose: This is the user-facing half of GDPR-03 (server-side cascade ships in plan 22-04). The preference center (ON-03) consumes the lifecycle-preference-update Edge Fn from plan 22-02 to keep consent_records.email_preferences JSONB authoritative.

Output: 2 new settings sub-pages + SettingsPage navigation extension + client invokers + tests green.
</objective>

<execution_context>
@/Users/karstenhaldan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karstenhaldan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md
@/Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/SettingsPage.tsx
@/Users/karstenhaldan/minisite/leanshot/src/lib/account-delete.ts
@/Users/karstenhaldan/minisite/leanshot/src/lib/export-data.ts
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Card.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Badge.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/dsar/__tests__/DsarPortalPage.test.tsx
@/Users/karstenhaldan/minisite/leanshot/src/lib/dsar/__tests__/dsar-pdf-render.test.ts

<interfaces>
<!-- From plan 22-04 -->
public.create_dsar_request() → returns uuid (request_id); raises 'already_pending' if active request exists
supabase.functions.invoke('dsar-export', {body: {request_id}}) — service-role bearer required (called by admin or cron, NOT by user JWT)

<!-- BUT for the user-flow: we need a user-JWT path. Plan 22-04 dsar-export uses service-role bearer per security model. -->
<!-- Resolution: user POSTs to create_dsar_request → row inserted with status='pending' → a TRIGGER or background job invokes dsar-export with service-role. -->
<!-- Simpler: introduce a thin user-callable Edge Fn `dsar-trigger` OR have create_dsar_request RPC itself queue the job via pg_notify + a dsar-export cron tick. -->
<!-- For v1.2 simplest: user clicks Export → DsarPortalPage calls create_dsar_request RPC → poll dsar_requests.status; dsar-export is triggered by a 5min cron that picks up pending rows. This decouples user UI from service-role bearer. -->

<!-- From plan 22-02 -->
supabase.functions.invoke('lifecycle-preference-update', {body: {categories: {...}}}) — user JWT path (verify_jwt=true)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HEAD-reset + add dsar-export cron tick migration (5min poll for pending DSAR requests)</name>
  <files>/Users/karstenhaldan/minisite/supabase/migrations/20270601000021_dsar_export_cron_tick.sql</files>
  <read_first>
    /Users/karstenhaldan/minisite/supabase/migrations/20270101000012_payouts_materialization_and_cron.sql (existing cron + net.http_post pattern + Vault service_role_key load),
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000018_dsar_request_rpcs.sql (post-plan-22-04 — create_dsar_request RPC writes status='pending')
  </read_first>
  <action>
    HEAD-reset: `test -f /Users/karstenhaldan/minisite/supabase/migrations/20270601000018_dsar_request_rpcs.sql || exit 1` (plan 22-04 Wave 1).

    Author migration `20270601000021_dsar_export_cron_tick.sql`:
    - `select cron.schedule('dsar-export-tick', '*/5 * * * *', $$ do $body$ declare v_request record; v_service_key text; begin select decrypted_secret into v_service_key from vault.decrypted_secrets where name='service_role_key' limit 1; if v_service_key is null then return; end if; for v_request in select id from public.dsar_requests where status = 'pending' order by requested_at limit 10 loop perform net.http_post(url := current_setting('app.settings.supabase_url', true) || '/functions/v1/dsar-export', headers := jsonb_build_object('Content-Type','application/json','Authorization','Bearer '||v_service_key), body := jsonb_build_object('request_id', v_request.id)); end loop; end $body$; $$);`.
    - Header comment notes: cron pulls Vault service_role_key (Phase 19 deferred vendor pass; if Vault missing key, cron no-ops gracefully — DSAR requests stay pending until vendor pass completes).
    - Push: dry-run + live.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; test -f supabase/migrations/20270601000021_dsar_export_cron_tick.sql &amp;&amp; supabase db push --linked --dry-run 2&gt;&amp;1 | grep -v "^Skipping" &gt; /dev/null</automated>
  </verify>
  <done>Cron tick scheduled; service-role-key gated so vendor pass missing won't crash; cron runs every 5 minutes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DsarPortalPage + DsarStatusCard + dsar-export-client.ts + tests</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/dsar/DsarPortalPage.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/dsar/DsarStatusCard.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/lib/dsar/dsar-export-client.ts,
    /Users/karstenhaldan/minisite/leanshot/src/lib/dsar/dsar-pdf-render.ts,
    /Users/karstenhaldan/minisite/leanshot/src/components/dsar/__tests__/DsarPortalPage.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/lib/dsar/__tests__/dsar-pdf-render.test.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/SettingsPage.tsx (lines 240-294 — analog export flow; dynamic-import jsPDF pattern lines 259-294),
    /Users/karstenhaldan/minisite/leanshot/src/lib/export-data.ts (jsPDF dynamic-import analog),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (lines 340-356 — DSAR portal layout; Copywriting lines 608-630),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave F DsarPortalPage section lines 779-801)
  </read_first>
  <behavior>
    - Test (DsarPortalPage): renders hero card with heading 'Your data' + body copy per UI-SPEC line 612 + SLA badge '30-day SLA' + Export CTA.
    - Test (DsarPortalPage): Export CTA opens confirmation Modal with heading 'Start your data export?' (UI-SPEC line 615) + body (line 616) + Cancel/Confirm CTAs.
    - Test (DsarPortalPage): Confirm in modal invokes `supabase.rpc('create_dsar_request')` → on success closes modal + shows DsarStatusCard with status='pending' + toast 'Export started. We'll email you when it's ready.' (UI-SPEC line 629).
    - Test (DsarPortalPage): 'already_pending' error → toast "Couldn't start your export. Try again or contact support." (UI-SPEC line 630).
    - Test (DsarStatusCard): renders correct Badge tone per state per UI-SPEC §Badge mappings lines 216-219 (pending→neutral, in_progress→info, completed→success, rejected→danger).
    - Test (DsarStatusCard completed): shows 'Download bundle' CTA opening signed URL + caption 'Available until {date}'.
    - Test (DsarStatusCard rejected): shows rejection_reason + 'Contact support' link.
    - Test (DsarStatusCard): aria-live='polite' on status badge announces transitions.
    - Test (dsar-pdf-render): client-side wrapper exists but is NOT called by v1.2 user flow (server-side only); the file is a placeholder for v1.3 client-side export option.
    - Test (Realtime/polling): status changes from pending → in_progress → completed observed within 30s via polling OR Realtime subscription.
  </behavior>
  <action>
    Implement `dsar-export-client.ts`:
    - `requestDsarExport(): Promise<string>` — `const {data, error} = await supabase.rpc('create_dsar_request'); if (error) throw mapDsarError(error); return data as string;` (returns request_id).
    - `getDsarStatus(requestId): Promise<DsarRequestRow>` — `await supabase.from('dsar_requests').select('*').eq('id', requestId).single();`.
    - `subscribeDsarStatus(userId, callback): RealtimeChannel` — subscribes to dsar_requests changes for current user; returns unsubscribe function.
    - Discriminated DsarError type: 'already_pending' | 'forbidden' | 'unknown'.

    Implement `dsar-pdf-render.ts` (CLIENT-SIDE placeholder; v1.2 path is server-side via plan 22-04):
    - `export async function renderDsarPdfClient(bundle: DsarBundle): Promise<Blob>` — dynamic-import jsPDF + autotable per existing src/lib/export-data.ts pattern. Header comment: "v1.2 uses server-side rendering in supabase/functions/dsar-export/pdf-render.ts; this client wrapper is reserved for v1.3 client-side option but currently unused."
    - Test asserts dynamic-import (no static jspdf import in this file at module top).

    Implement `DsarPortalPage.tsx` per UI-SPEC §/settings/privacy/dsar layout:
    - Single-column max-w-3xl mx-auto px-4 py-12.
    - Hero `<Card padding="lg">`: heading + body + SLA badge + Export CTA.
    - Confirmation `<Modal>`: copy verbatim from UI-SPEC §Copywriting lines 615-618.
    - useState for activeRequestId + status; useEffect subscribes to status changes.
    - Render DsarStatusCard when activeRequestId present.
    - History list at bottom showing past DSAR requests (date + status).

    Implement `DsarStatusCard.tsx` per UI-SPEC §Status card lines 348-353:
    - Badge with tone per state.
    - Conditional content: completed → Download CTA; rejected → reason + support link; pending/in_progress → just badge.
    - `aria-live="polite"` on the badge span.

    Tests cover 10 behavior bullets. Mock supabase rpc/from/functions.invoke + Realtime channel.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run DsarPortalPage dsar-pdf-render 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓|tests"</automated>
  </verify>
  <done>DSAR portal + status card + client invoker + 10 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: EmailPreferencesPage + SettingsPage nav extension</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/EmailPreferencesPage.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/SettingsPage.tsx
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/SettingsPage.tsx (existing sub-page pattern from Phase 15),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (lines 357-368 — preference center layout; Copywriting lines 632-653 — 6 category labels + bodies),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md (D-07 + D-03 + ON-03 spec)
  </read_first>
  <behavior>
    - Test: renders 6 categories per UI-SPEC line 364-365 (Transactional locked + 5 toggleable).
    - Test: Transactional toggle is disabled with title "Required — these relate to your account and purchases" (UI-SPEC line 650).
    - Test: Affiliate row only renders if user.app_metadata.is_affiliate === true (or analogous: query affiliates table for current user).
    - Test: Save preferences invokes `supabase.functions.invoke('lifecycle-preference-update', {body: {categories: {welcome, behavior_triggered, retention, weekly_digest, affiliate}}})` + toast 'Preferences saved' (UI-SPEC line 652).
    - Test: SettingsPage gains 2 links: 'Email preferences' → /settings/email-preferences + 'Privacy & DSAR' → /settings/privacy/dsar.
    - Test: ESLint compliance — uses `(s) => s.someSlice` selector (no broken patterns from CLAUDE.md restricted-syntax).
  </behavior>
  <action>
    Implement `EmailPreferencesPage.tsx` per UI-SPEC §/settings/email-preferences:
    - Single-column max-w-3xl + heading + intro per UI-SPEC line 637.
    - useState preferences (default fetched from `consent_records.email_preferences` for current user, or {welcome: true, behavior_triggered: true, retention: true, weekly_digest: false, affiliate: true} fallback).
    - 6 Card sections per category — Pill segmented On/Off per category (matches cookie banner pattern per UI-SPEC line 365).
    - Transactional row: Pill disabled with tooltip per UI-SPEC line 650.
    - Affiliate row: render only if user is an affiliate (query `affiliates` table where user_id = auth.uid()).
    - Save button: `<Button variant="primary">Save preferences</Button>` → invokes lifecycle-preference-update → toast 'Preferences saved' on success.
    - Save success/error toasts per UI-SPEC line 652-653.

    Modify `SettingsPage.tsx`:
    - Add 2 new sub-page links to the existing nav structure (whatever the Phase 15 pattern uses — typically a list of Card links). One: 'Email preferences' (with envelope/Mail icon) → navigate to /settings/email-preferences. Two: 'Privacy & DSAR' (with shield icon) → /settings/privacy/dsar.
    - Preserve all existing settings links (don't break Phase 7 + Phase 15 wiring).

    Per CLAUDE.md ESLint: `useStore((s) => s.someSlice)` patterns only.

    Wiring note for plan 22-12: both new routes need to be lazy-mounted in App.tsx (per UI-SPEC bundle budget — every /settings/* sub-page is React.lazy).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run EmailPreferencesPage 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓|tests"</automated>
  </verify>
  <done>EmailPreferencesPage renders 6 categories with Transactional locked; SettingsPage has 2 new links; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → create_dsar_request RPC | RPC validates auth.uid() present + no active pending request; SECURITY DEFINER |
| user → dsar_requests SELECT | RLS select-own (user_id = auth.uid()) |
| user → lifecycle-preference-update Edge Fn | Caller JWT validates user identity; UPSERT scoped to auth.uid() |
| cron → dsar-export Edge Fn | Vault service_role_key bearer; user JWT not needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-62 | D | User spams DSAR requests | mitigate | create_dsar_request raises 'already_pending' if active request exists; UI surfaces friendly error |
| T-22-63 | I | DSAR signed URL leak via console.log | mitigate | URL only stored in dsar_requests.export_path (not logged); UI fetches via supabase select |
| T-22-64 | T | User pays attention to preference center but lifecycle Fns don't check email_preferences | mitigate | Plan 22-02 each lifecycle Fn SELECTs consent_records.email_preferences before send; test green |
| T-22-65 | I | Email preferences expose user's affiliate status to other users | mitigate | Affiliate row conditionally rendered based on `affiliates` table where user_id = auth.uid(); RLS prevents cross-user reads |
| T-22-66 | D | DSAR cron tick misses Vault service_role_key → DSAR requests stuck pending forever | accept | Migration 21 cron silently no-ops if key missing; documented in 22-11-SUMMARY as P22 closeout vendor pass; admin can manually trigger via dashboard or re-create_dsar_request after key loaded |
</threat_model>

<verification>
- /settings/privacy/dsar route renders + Export creates dsar_requests row + status updates observable.
- /settings/email-preferences renders 6 categories + Save updates consent_records.email_preferences.
- SettingsPage has 2 new sub-page links.
- DSAR cron tick scheduled (gated on Vault service_role_key).
- 16 tests green across DsarPortalPage + DsarStatusCard + EmailPreferencesPage + dsar-pdf-render.
</verification>

<success_criteria>
- GDPR-03 user-facing surface complete; cron picks up pending requests; dsar-export Edge Fn (plan 22-04) does the heavy lifting.
- ON-03 self-serve preference center; lifecycle Fns (plan 22-02) honor email_preferences JSONB on every send.
- VALIDATION.md rows for GDPR-03 + ON-03 updated with Plan 22-11.
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-11-SUMMARY.md` per template. Include: route mount paths, cron tick schedule, vendor passes still open (Vault service_role_key, Resend domain verify, PostHog PERSONAL_API_KEY).
</output>
