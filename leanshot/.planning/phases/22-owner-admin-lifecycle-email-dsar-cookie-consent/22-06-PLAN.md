---
phase: 22
plan: 06
type: execute
wave: 2
depends_on: [01]
autonomous: true
requirements: [ADMIN-01, ADMIN-05]
requirements_addressed: [ADMIN-01, ADMIN-05]
files_modified:
  - leanshot/src/components/admin/AdminLayout.tsx
  - leanshot/src/components/admin/pages/AdminMembersPage.tsx
  - leanshot/src/components/admin/members/MembersTable.tsx
  - leanshot/src/components/admin/members/MembersFilterBar.tsx
  - leanshot/src/components/admin/members/MemberRowActions.tsx
  - leanshot/src/components/admin/pages/AdminMemberDetailPage.tsx
  - leanshot/src/components/admin/members/MemberProfileTab.tsx
  - leanshot/src/components/admin/members/MemberBillingTab.tsx
  - leanshot/src/components/admin/members/MemberActivityTab.tsx
  - leanshot/src/components/admin/members/MemberStripeTab.tsx
  - leanshot/src/components/admin/members/MemberFlagsTab.tsx
  - leanshot/src/components/admin/members/MemberAuditTab.tsx
  - leanshot/src/lib/admin/admin-api.ts
  - leanshot/src/lib/consent/feature-flag-overrides.ts
  - leanshot/src/components/admin/__tests__/AdminMembersPage.test.tsx
  - leanshot/src/components/admin/members/__tests__/MembersTable.test.tsx
  - leanshot/src/lib/admin/__tests__/admin-api.test.ts
  - leanshot/src/lib/consent/__tests__/feature-flag-overrides.test.ts

must_haves:
  truths:
    - "/admin/members route renders Members table with 7 columns (avatar+email, tier, signup, last_active, clinic, country, stripe_status) + row actions (per UI-SPEC §/admin/members)"
    - "is_staff client-gate present on AdminLayout (mirrors AdminAffiliatesScaffold Phase 19 pattern) — UX layer only; admin_list_members RPC is the security boundary"
    - "MembersFilterBar Pill segmented group: All / Free / Paid / Clinic / Past due / Canceled — drives admin_list_members p_tier parameter"
    - "Server pagination via URL `?page=N&size=M` (25/50/100); 30s polling reflects real-world admin workflows"
    - "AdminMemberDetailPage has 6 tabs (Profile · Billing · Activity · Stripe · Flags · Audit) per UI-SPEC §/admin/members/{id}; ?tab= URL query persists active tab"
    - "Profile tab shows read-only field list (user_id, signup_date, last_active, clinic, country, IP, browser); Billing tab shows subscription + payment-method; Activity tab shows last 50 events; Stripe tab shows charges + per-row Refund action (modal in plan 22-07); Flags tab is the per-user feature-flag override CRUD (ADMIN-05); Audit tab shows audit_logs scoped to target_user_id"
    - "feature-flag-overrides.ts wrapper: isFeatureEnabled(key) checks `feature_flag_overrides` cache where `expires_at > now()` first, falls back to posthog.isFeatureEnabled() (D-08; Pitfall 10 SQL-side filter)"
    - "loadOverrides(userId) prefetches all active overrides into Map cache; subsequent isFeatureEnabled lookups are sync"
    - "MemberFlagsTab UI: list of flags (read from PostHog mirror via posthog.getFeatureFlag or admin RPC) + per-flag toggle + expires-at date-picker + 'Set override' button → calls admin_set_feature_flag_override RPC"
    - "Routes mounted via React.lazy in App.tsx (per UI-SPEC bundle budget rule — lazy-load required for /admin/*)"
  artifacts:
    - path: leanshot/src/components/admin/AdminLayout.tsx
      provides: "is_staff gate + sub-nav for /admin/members /admin/metrics /admin/cohorts /admin/affiliates"
    - path: leanshot/src/components/admin/pages/AdminMembersPage.tsx
      provides: "ADMIN-01 /admin/members"
    - path: leanshot/src/components/admin/pages/AdminMemberDetailPage.tsx
      provides: "ADMIN-01 + ADMIN-05 /admin/members/{id}"
    - path: leanshot/src/lib/consent/feature-flag-overrides.ts
      provides: "D-08 PostHog wrapper with overrides-first check"
  key_links:
    - from: leanshot/src/lib/admin/admin-api.ts
      to: public.admin_list_members
      via: "supabase.rpc('admin_list_members', {p_search, p_tier, p_page, p_size})"
      pattern: "admin_list_members"
    - from: leanshot/src/components/admin/members/MemberFlagsTab.tsx
      to: public.admin_set_feature_flag_override
      via: "supabase.rpc('admin_set_feature_flag_override', {p_user_id, p_flag_key, p_value, p_expires_at})"
      pattern: "admin_set_feature_flag_override"
    - from: leanshot/src/lib/consent/feature-flag-overrides.ts
      to: public.feature_flag_overrides
      via: "select where user_id = $1 and expires_at > now() — SQL-side filter"
      pattern: "feature_flag_overrides"
---

<objective>
Ship the Members table (ADMIN-01) + Member drill-in with 6 tabs + Feature-flag overrides UI (ADMIN-05).

Purpose: This is the operator's everyday entry point. The Phase 19 AdminAffiliatesScaffold pattern gives us the shell + is_staff gate; here we adapt it to the members domain + introduce the drill-in tab structure that ADMIN-04 + ADMIN-05 + ADMIN-06 + ADMIN-07 (deferred) all live inside.

Output: /admin/members + /admin/members/{id} live; PostHog override wrapper functional; tests green.
</objective>

<execution_context>
@/Users/karstenhaldan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karstenhaldan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md
@/Users/karstenhaldan/minisite/leanshot/src/components/admin/AdminAffiliatesScaffold.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/clinic/roster/RosterTable.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/clinic/drill-in/ClinicDrillInPage.tsx
@/Users/karstenhaldan/minisite/leanshot/src/lib/feature-flags.ts
@/Users/karstenhaldan/minisite/leanshot/src/lib/supabase.ts
@/Users/karstenhaldan/minisite/leanshot/src/App.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Card.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Pill.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Badge.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/InitialsAvatar.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminLayout + admin-api.ts wrapper + /admin/members page + MembersTable + MembersFilterBar + MemberRowActions</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/AdminLayout.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/lib/admin/admin-api.ts,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/pages/AdminMembersPage.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/MembersTable.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/MembersFilterBar.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/MemberRowActions.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/__tests__/AdminMembersPage.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/__tests__/MembersTable.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/lib/admin/__tests__/admin-api.test.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/AdminAffiliatesScaffold.tsx (full file — is_staff gate lines 82-108; Pill segmented filter; render guard lines 155-176; copy verbatim for AdminLayout shell),
    /Users/karstenhaldan/minisite/leanshot/src/components/clinic/roster/RosterTable.tsx (lines 1-80 — sort + pagination + 3-click-revert behavior; PAGE_SIZE; SortState shape),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (lines 234-246 — /admin/members layout contract; lines 458-483 — copywriting),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave E section + Pattern S1 dual-layer gate),
    /Users/karstenhaldan/minisite/leanshot/src/components/ui/Pill.tsx (PillGroup + segmented usage),
    /Users/karstenhaldan/minisite/leanshot/src/components/ui/Badge.tsx (Badge tone semantics)
  </read_first>
  <behavior>
    - Test (admin-api): admin_list_members returns shape `{user_id, email, tier, signup_date, last_active, clinic_name, country, stripe_status}` — wrapper unwraps + typed.
    - Test (admin-api): RPC call passes p_search, p_tier, p_page, p_size when set; defaults p_search=null, p_tier=null, p_page=1, p_size=50.
    - Test (admin-api): RPC error code 42501 surfaces as `AdminApiError(code:'not_staff')`.
    - Test (AdminMembersPage): renders nothing while is_staff resolves (initial null state per analog line 155).
    - Test (AdminMembersPage): when is_staff = false, renders NotAuthorizedCard.
    - Test (AdminMembersPage): when is_staff = true, fetches and renders MembersTable with rows.
    - Test (AdminMembersPage): filter Pill click on "Paid" → re-invokes admin_list_members with p_tier='paid'.
    - Test (AdminMembersPage): search input change debounced 300ms → re-invokes admin_list_members with p_search=string.
    - Test (MembersTable): renders 7 columns + correct headers per UI-SPEC copy (Email, Tier, Signed up, Last active, Clinic, Country, Stripe).
    - Test (MembersTable): sort click cycles asc → desc → reset (3-click-revert per analog RosterTable).
    - Test (MembersTable): row click navigates to /admin/members/{id} (Enter key also works).
    - Test (MembersTable): row actions menu (kebab) opens with 6 items: Impersonate / Refund last charge / Cancel subscription / Deactivate / Override feature flag / View detail (per UI-SPEC line 241).
    - Test (MembersTable): pagination footer shows "Showing 1-50 of {total}" + prev/next buttons.
  </behavior>
  <action>
    Implement `AdminLayout.tsx`:
    - Clone AdminAffiliatesScaffold's is_staff gate logic verbatim (lines 82-108): resolves `profiles.is_staff` for current user; renders null while resolving; renders `<NotAuthorizedCard />` if not staff.
    - When staff: renders `<AdminSubNav />` with links to /admin/members, /admin/metrics, /admin/cohorts, /admin/affiliates + `<Outlet />` analog (since project has no router; use a switch on `window.location.pathname` or a `currentAdminPage` Zustand selector — pick lightest approach; v1.1 onboarding uses similar minimal-router pattern).
    - Mount via React.lazy in App.tsx (note: don't actually wire App.tsx here; defer that to plan 22-12 Wave 3 integration to avoid shared-file choreography per Pre-emptive warning 4).

    Implement `admin-api.ts` exports:
    - `listMembers({search?, tier?, page?, size?}: ListMembersFilters): Promise<MembersResult>` — wraps admin_list_members RPC.
    - `setFeatureFlagOverride({userId, flagKey, value, expiresAt}: ...): Promise<void>` — wraps admin_set_feature_flag_override RPC.
    - `AdminApiError extends Error { code: 'not_staff'|'invalid'|'network'|'unknown' }` — discriminated-error pattern (analog `src/lib/account-delete.ts` lines 23-39).
    - Typed shapes: `Member`, `MembersResult`.

    Implement `AdminMembersPage.tsx`:
    - useState for search, tier, page, size, members, isStaff, isLoading.
    - useEffect resolves is_staff (analog AdminAffiliatesScaffold lines 82-108).
    - useEffect fetches members via admin-api when filters/page change; 30s polling.
    - Render: KPI strip (4 cards — Total / Paid / Clinic seats / 30d churn — from members aggregate counts; for v1.2 compute client-side from current page or add admin_members_kpi_strip RPC if needed — planner picks: client-side first-pass acceptable since v1.2 user counts are <10k).
    - Render: `<MembersFilterBar />` + `<MembersTable />` + pagination footer.

    Implement `MembersTable.tsx`:
    - Reuse RosterTable.tsx sort + pagination pattern verbatim (DEFAULT_SORT, SortState, click cycle).
    - 7 columns per UI-SPEC + per-row `<MemberRowActions />` (kebab menu).
    - Empty state via `<EmptyState>` "No members match your filters" (copy from UI-SPEC line 481).
    - Loading state via `<Skeleton>` 10 rows.
    - Mobile: collapse to vertical card list (one card per row) at <md breakpoint per UI-SPEC line 245.

    Implement `MembersFilterBar.tsx`:
    - PillGroup segmented: All / Free / Paid / Clinic / Past due / Canceled (per UI-SPEC line 239).
    - Search Input with debounce 300ms.
    - Country select + date range native `<input type="date">` per UI-SPEC line 239.

    Implement `MemberRowActions.tsx`:
    - IconButton kebab → popover with 6 items per UI-SPEC line 241 + copy from line 474-479.
    - "Impersonate" → invokes admin-impersonate Edge Fn (plan 22-03); handler stubbed if plan 22-09 ImpersonationBanner not yet mounted (just call invoke + redirect via setSession).
    - "Refund last charge" → opens RefundModal (plan 22-07; until that plan ships, render disabled button with tooltip "Awaiting plan 22-07").
    - "Cancel subscription" / "Deactivate" → same — disabled placeholders if dependent plan not yet shipped.
    - "Override feature flag" → navigates to /admin/members/{id}?tab=flags.
    - "View detail" → navigates to /admin/members/{id}.

    Wire ESLint compliance — no `useStore(selector(s))` pattern that hits the restricted-syntax rule from CLAUDE.md. Use `(s) => s.someSlice` selectors only.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run AdminMembersPage MembersTable admin-api 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓"</automated>
  </verify>
  <done>/admin/members renders for staff with table + filters + pagination; 13 behavior tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdminMemberDetailPage + 6 tabs (Profile / Billing / Activity / Stripe / Flags / Audit)</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/pages/AdminMemberDetailPage.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/MemberProfileTab.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/MemberBillingTab.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/MemberActivityTab.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/MemberStripeTab.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/MemberFlagsTab.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/members/MemberAuditTab.tsx
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/components/clinic/drill-in/ClinicDrillInPage.tsx (drill-in pattern — header + tab bar + tab content),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (lines 247-258 — /admin/members/{id} layout contract; lines 485-498 — copywriting; lines 256-257 Flags tab + Audit tab specifics)
  </read_first>
  <behavior>
    - Test: header renders InitialsAvatar + member name + email + tier badge + Stripe-status badge + action cluster (Impersonate / Cancel / Refund icon buttons).
    - Test: tab bar renders 6 Pills per UI-SPEC copy line 488 ("Profile · Billing · Activity · Stripe · Flags · Audit"); ?tab= URL query persists.
    - Test: arrow-key navigation between tabs (Pill primitive existing keyboard contract).
    - Test: Profile tab — renders read-only field list with values from auth.users + profiles (mocked).
    - Test: Billing tab — renders subscription card + payment-method card.
    - Test: Activity tab — renders last 50 events from a (mocked) activity feed query.
    - Test: Stripe tab — renders charges table with "Refund this charge" per-row link (action stubbed if plan 22-07 not yet shipped).
    - Test: Flags tab — renders list of feature flags + per-row toggle + expires-at date input + "Set override" button → calls admin-api setFeatureFlagOverride; on success → row shows `<Badge tone="info">Overridden</Badge>` (per UI-SPEC line 256).
    - Test: Audit tab — renders audit_logs rows scoped to `target_user_id = {id}` in reverse-chronological order; each row shows timestamp + actor + action_type + before/after JSON expand.
    - Test: Empty states per UI-SPEC line 489-495 ("No activity yet", "No charges in the last 90 days", "No audit entries").
  </behavior>
  <action>
    Implement `AdminMemberDetailPage.tsx`:
    - useParams equivalent: read id from window.location.pathname (`/admin/members/{id}`).
    - Header per UI-SPEC line 249.
    - PillGroup tab bar: tabs in order Profile · Billing · Activity · Stripe · Flags · Audit. ?tab= URL state.
    - Conditionally render the active tab component.

    Each tab component is a self-contained `<Card>`-based view per UI-SPEC line 251-257:
    - **MemberProfileTab**: SELECT from auth.users + public.profiles via supabase.rpc OR direct query if RLS allows admin via is_staff bypass (use service-role-callable RPC if needed; for v1.2 direct query is acceptable for admins).
    - **MemberBillingTab**: query subscriptions + payment_methods (existing P14 schema).
    - **MemberActivityTab**: query audit_logs WHERE actor_id = {id} OR target_user_id = {id} (last 50 events).
    - **MemberStripeTab**: query stripe_charges (last 90d) — column 4 is "Refund this charge" link that opens RefundModal from plan 22-07 (until that plan lands, render stub `<Button disabled title="Refund modal lands in plan 22-07">Refund</Button>`).
    - **MemberFlagsTab**: list of flags. For v1.2, list is hardcoded set of known flag keys (project current set: `aff_manual_entry`, `share_link_v2`, ...). For each flag: existing override row from feature_flag_overrides + toggle + expires-at date input + "Set override" button → calls `setFeatureFlagOverride({userId, flagKey, value, expiresAt})` → on success toast "Override set" + refetch. Show `<Badge tone="info">Overridden</Badge>` when override row exists.
    - **MemberAuditTab**: SELECT audit_logs WHERE target_user_id = {id} ORDER BY timestamp DESC LIMIT 100.

    Per CLAUDE.md ESLint no-restricted-syntax: do NOT use `useStore(s => admin_aggregate(s))` patterns; pull primitive selectors only.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run AdminMemberDetailPage MemberFlagsTab 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓|tests"</automated>
  </verify>
  <done>Drill-in page renders 6 tabs; Flags tab functional with admin_set_feature_flag_override; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: feature-flag-overrides.ts wrapper (D-08 — PostHog override-first check)</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/lib/consent/feature-flag-overrides.ts,
    /Users/karstenhaldan/minisite/leanshot/src/lib/consent/__tests__/feature-flag-overrides.test.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/lib/feature-flags.ts (analog — existing project feature flag wrapper to extend),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (§Common Operation 5 lines 901-924 — wrap-and-override pattern),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave D feature-flag-overrides section + Pitfall 10 SQL-side filter)
  </read_first>
  <behavior>
    - Test: loadOverrides(userId) queries feature_flag_overrides WHERE user_id = userId AND expires_at > now() (SQL-side filter per Pitfall 10).
    - Test: isFeatureEnabled(key) returns cached override value if cache hit AND override.expires_at > now().
    - Test: isFeatureEnabled(key) returns false-fallback to posthog.isFeatureEnabled(key) if cache miss or expired.
    - Test: cache invalidation — calling loadOverrides again after override expires re-fetches.
    - Test: TypeScript signature: isFeatureEnabled(key: string): boolean (sync, not Promise).
  </behavior>
  <action>
    Implement `src/lib/consent/feature-flag-overrides.ts` per RESEARCH §Common Operation 5:
    - Module-level `const overrideCache = new Map<string, {value: boolean; exp: number}>();`
    - `export async function loadOverrides(userId: string): Promise<void>` — `const {data} = await supabase.from('feature_flag_overrides').select('flag_key, value, expires_at').eq('user_id', userId).gt('expires_at', new Date().toISOString());` then for each row `overrideCache.set(row.flag_key, {value: row.value, exp: new Date(row.expires_at).getTime()});`. The `.gt('expires_at', now)` filter happens in the SQL — never client-side (Pitfall 10).
    - `export function isFeatureEnabled(key: string): boolean` — sync: check overrideCache first (with expiry check), else fall through to `posthog.isFeatureEnabled(key)`.
    - `export function clearOverrideCache(): void` — for tests + on logout.

    Wire `loadOverrides(user.id)` into App.tsx mount path post-auth (defer actual wiring to plan 22-12 to avoid shared-file choreography; document here in header comment).

    Test file: cover 5 behavior bullets with mocked supabase + posthog clients.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run feature-flag-overrides 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓"</automated>
  </verify>
  <done>Wrapper exists, SQL-side expiry filter enforced, test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → admin_list_members RPC | is_staff gate in RPC body (server-side); client is_staff check is UX-only |
| client → admin_set_feature_flag_override RPC | is_staff gate in RPC body |
| client → feature_flag_overrides table SELECT | RLS select-own; client wrapper only reads current user's rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-37 | E | Non-staff user bypasses client is_staff gate via direct URL navigation | mitigate | admin_list_members RPC raises forbidden 42501 if !is_staff; client gate is UX-only per Pattern S1 |
| T-22-38 | T | Admin sets feature_flag_overrides row directly via supabase-js (bypassing RPC) | mitigate | RLS on feature_flag_overrides has NO insert/update/delete policy for authenticated; only admin_set_feature_flag_override (SECURITY DEFINER, is_staff-gated) writes |
| T-22-39 | I | feature_flag_overrides leak across users via cache pollution | mitigate | overrideCache is module-level but populated only via loadOverrides(currentUserId); cleared on logout (in plan 22-12 wiring) |
| T-22-40 | T | Expired override applied because of stale cache | mitigate | Map values store `exp` timestamp; isFeatureEnabled checks `exp > Date.now()` before returning cached value; SQL filter is the source-of-truth |
| T-22-41 | I | Audit tab leaks other-user actions (if RLS misconfigured) | accept | Audit row scoping is server-side; if RLS lets admin see all rows that's the intended behavior; tab filter `target_user_id = {id}` is a UX filter only |
</threat_model>

<verification>
- /admin/members renders with 7-column table + 6-item filter Pill + 30s polling.
- /admin/members/{id} renders with 6 tabs; Flags tab calls admin_set_feature_flag_override RPC.
- feature-flag-overrides wrapper: SQL-side `gt('expires_at', now)` filter present + 5 behavior tests green.
- All admin components mount via `AdminLayout` is_staff gate (Pattern S1 dual-layer).
- React.lazy mount in App.tsx deferred to plan 22-12 (shared-file).
</verification>

<success_criteria>
- ADMIN-01 + ADMIN-05 UI surfaces ready; refund/cancel actions stubbed for plan 22-07 to fill in.
- VALIDATION.md rows updated for ADMIN-01 + ADMIN-05 with Plan 22-06.
- Tab navigation respects ?tab= URL state per UI-SPEC.
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-06-SUMMARY.md` per template. Include: tab rendering smoke results, feature-flag-overrides wrapper test count, stub list for plan 22-07/22-09 wiring.
</output>
