---
phase: 22
plan: 10
type: execute
wave: 2
depends_on: [01]
autonomous: true
requirements: [GDPR-01, GDPR-02]
requirements_addressed: [GDPR-01, GDPR-02]
files_modified:
  - package.json
  - package-lock.json
  - leanshot/src/components/consent/CookieConsentBootstrap.tsx
  - leanshot/src/components/consent/consent-config.ts
  - leanshot/src/lib/consent/consent-defer.ts
  - leanshot/src/lib/consent/consent-records.ts
  - leanshot/src/components/consent/__tests__/CookieConsentBootstrap.test.tsx
  - leanshot/src/lib/consent/__tests__/consent-records.test.ts

must_haves:
  truths:
    - "vanilla-cookieconsent@^3.1.0 added to package.json (verified npm 2025-02-04 stable per RESEARCH)"
    - "consent-defer.ts buffers init call via requestIdleCallback + dynamic-imports vanilla-cookieconsent — keeps bundle index gz ≤ 50 kB ceiling (currently ~21 kB; vanilla-cookieconsent ~12 kB gz adds via separate lazy chunk per UI-SPEC bundle budget)"
    - "consent-config.ts implements Pattern 3 — bottom slide-up + Customize inline-expand + 4 categories (Essential/Analytics/Marketing/Personalization) + Consent Mode v2 gtag('consent','update') bridge"
    - "EU geo detection via window.__VERCEL_GEO__.country (from Phase 12 firewall) — EU → all non-Essential default OFF; US → Analytics default ON (CCPA opt-out)"
    - "onConsent + onChange + onFirstConsent callbacks UPSERT consent_records (server-side audit per D-07 + GDPR Art. 7)"
    - "consent_records UPSERT writes: cookie_categories (jsonb of acceptedCategory map), user_agent, ip_inet (server-side via Edge Fn OR null client-side), country_code, consent_revision, recorded_at"
    - "CookieConsentBootstrap renders null + kicks off scheduleConsentInit() once on mount (5-line invoker)"
    - "Banner uses `acceptedService()` per-service API (Pitfall 7) — required for Consent Mode v2 distinguishing analytics_storage vs ad_storage"
    - "Type-only import of vanilla-cookieconsent in consent-defer.ts (`import type * as CookieConsentModule from 'vanilla-cookieconsent';`) — value import ONLY inside async loadConsent() (Pattern 4)"
    - "PostHog SDK loads via dynamic-import ONLY after `acceptedCategory('analytics')` returns true (GDPR-02 gate; existing src/lib/sync-defer.ts pattern reused)"
  artifacts:
    - path: leanshot/src/lib/consent/consent-defer.ts
      provides: "Pattern 4 — bundle budget gate for vanilla-cookieconsent"
    - path: leanshot/src/components/consent/consent-config.ts
      provides: "Pattern 3 — banner config + Consent Mode v2 bridge"
    - path: leanshot/src/components/consent/CookieConsentBootstrap.tsx
      provides: "Mount point at App.tsx root that kicks off scheduleConsentInit()"
    - path: leanshot/src/lib/consent/consent-records.ts
      provides: "GDPR-02 audit writer; consumed by onConsent/onChange/onFirstConsent callbacks"
  key_links:
    - from: leanshot/src/components/consent/consent-config.ts
      to: leanshot/src/lib/consent/consent-records.ts
      via: "Three callbacks (onFirstConsent, onConsent, onChange) all invoke upsertConsentRecord(cookie)"
      pattern: "upsertConsentRecord"
    - from: leanshot/src/lib/consent/consent-records.ts
      to: public.consent_records table
      via: "supabase.from('consent_records').upsert({user_id, anonymous_id, cookie_categories, ...}, {onConflict: 'user_id,anonymous_id'})"
      pattern: "consent_records"
    - from: leanshot/src/lib/consent/consent-defer.ts
      to: posthog-js SDK
      via: "Existing src/lib/sync-defer.ts already gates PostHog loading; this plan adds gate-check via acceptedCategory('analytics') before unblocking"
      pattern: "acceptedCategory"
---

<objective>
Ship the cookie consent banner (GDPR-01) + consent records audit (GDPR-02) + dynamic-import gate for analytics SDKs.

Purpose: This is the regulator-facing surface — must be locked down at the design contract level (UI-SPEC §Cookie consent banner) and the audit level (D-07 server-side consent_records). Bundle budget is the load-bearing constraint: vanilla-cookieconsent ships ~12 kB gz, which must NOT land in the index entry chunk (project ceiling 50 kB; current ~21 kB).

Output: 4 new files + vanilla-cookieconsent installed + 2 test files green.
</objective>

<execution_context>
@/Users/karstenhaldan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karstenhaldan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md
@/Users/karstenhaldan/minisite/leanshot/src/lib/sync-defer.ts
@/Users/karstenhaldan/minisite/leanshot/src/lib/account-delete.ts
@/Users/karstenhaldan/minisite/leanshot/src/lib/supabase.ts
@/Users/karstenhaldan/minisite/leanshot/src/components/consent/__tests__/CookieConsentBootstrap.test.tsx
@/Users/karstenhaldan/minisite/leanshot/src/lib/consent/__tests__/consent-records.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: npm install vanilla-cookieconsent + verify bundle budget unchanged</name>
  <files>/Users/karstenhaldan/minisite/leanshot/package.json, /Users/karstenhaldan/minisite/leanshot/package-lock.json</files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/package.json (current deps),
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/project_phase5_bundle_regression.md (bundle budget rule + sync-defer pattern; any heavy SDK in App.tsx/main.tsx/store.ts is blocked by CI)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/leanshot/`:
    1. `npm install vanilla-cookieconsent@^3.1.0`.
    2. Verify package.json now has `vanilla-cookieconsent` as a dependency at ^3.1.0.
    3. Run `npm run build` to validate it still builds.
    4. Verify index chunk gz stays under 50 kB (project current ~21 kB; only OK if vanilla-cookieconsent is NOT statically imported by App.tsx/main.tsx/store.ts — which is the entire point of Task 2's consent-defer.ts gate).
    5. Document baseline index gz in 22-10-SUMMARY.md draft notes for later regression detection.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; grep -q "\"vanilla-cookieconsent\":" package.json &amp;&amp; npm run build 2&gt;&amp;1 | tail -5 | grep -qE "(built in|✓)"</automated>
  </verify>
  <done>vanilla-cookieconsent installed at ^3.1.0; project still builds; index ceiling not yet validated (validated in Task 4).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: consent-defer.ts (Pattern 4 bundle gate) + consent-config.ts (Pattern 3 banner)</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/lib/consent/consent-defer.ts,
    /Users/karstenhaldan/minisite/leanshot/src/components/consent/consent-config.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/lib/sync-defer.ts (full 270 lines — pre-init buffer + idle-load + drain pattern; type-only namespace import to prevent value leak),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Pattern 3 lines 445-511 — full consent-config example with Consent Mode v2; Pattern 4 lines 514-544 — defer-load),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave D lines 591-625 — consent-defer + Pitfall 7 acceptedService),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (Cookie consent banner lines 324-338; Copywriting lines 588-606)
  </read_first>
  <behavior>
    - Test (consent-defer): scheduleConsentInit() with idle support calls requestIdleCallback once.
    - Test (consent-defer): scheduleConsentInit() without idle support falls back to setTimeout(100).
    - Test (consent-defer): subsequent scheduleConsentInit() calls are no-ops (idempotent).
    - Test (consent-defer): consent-config module is dynamically imported (NOT statically) — verify build artifacts contain a separate chunk for vanilla-cookieconsent.
    - Test (consent-config): config object has guiOptions.consentModal.layout='box inline' + position='bottom right' (per UI-SPEC §Layout collapsed line 328).
    - Test (consent-config): EU geo (window.__VERCEL_GEO__.country='DE') → analytics category enabled=false in initial config.
    - Test (consent-config): US geo (country='US') → analytics enabled=true (CCPA opt-out per D-07).
    - Test (consent-config): onConsent callback calls upsertConsentRecord + gtag('consent','update', ...) with mapping from acceptedService.
  </behavior>
  <action>
    Implement `consent-defer.ts` per RESEARCH Pattern 4 lines 521-544 + analog `sync-defer.ts` lines 48-58, 211-226:
    - Type-only namespace import: `import type * as CookieConsentModule from 'vanilla-cookieconsent';`
    - Module-level `let initialized = false;`, `let loadingPromise: Promise<void> | null = null;`.
    - `export function scheduleConsentInit(): void` — if initialized OR loadingPromise return early. startLoad function uses `if ('requestIdleCallback' in window) window.requestIdleCallback(fire, {timeout: 2000}); else setTimeout(fire, 100);`. fire = `void loadConsent();`.
    - `async function loadConsent(): Promise<void>` — `if (initialized) return; initialized = true; const { initCookieConsent } = await import('@/components/consent/consent-config'); initCookieConsent();`. This dynamic-import is the bundle-budget gate.

    Implement `consent-config.ts` per RESEARCH Pattern 3 lines 452-511:
    - `import * as CookieConsent from 'vanilla-cookieconsent';` (value import — fine here because this module is only reached via dynamic-import gate).
    - Read `window.__VERCEL_GEO__?.country` (from Phase 12 firewall); `const EU_COUNTRIES = ['AT','BE','BG','CY','CZ','DE','DK','EE','ES','FI','FR','GR','HR','HU','IE','IT','LT','LU','LV','MT','NL','PL','PT','RO','SE','SI','SK','GB']` (28 EU + UK per RESEARCH §Specifics line 142).
    - `const isEU = country ? EU_COUNTRIES.includes(country) : false;`.
    - `window.dataLayer = window.dataLayer ?? [];` `function gtag(...args) { dataLayer.push(arguments); }`.
    - `gtag('consent', 'default', { ad_storage: 'denied', ad_user_data: 'denied', ad_personalization: 'denied', analytics_storage: isEU ? 'denied' : 'granted', functionality_storage: 'granted', personalization_storage: 'denied', security_storage: 'granted' });` (Consent Mode v2 default state).
    - `function updateGtagConsent()` — uses `CookieConsent.acceptedCategory('analytics')` etc. to set analytics_storage / ad_storage / etc.
    - `export function initCookieConsent()` — calls `CookieConsent.run({...})` with config:
      - guiOptions.consentModal: `{layout: 'box inline', position: 'bottom right', equalWeightButtons: true}` (per UI-SPEC line 328).
      - guiOptions.preferencesModal: `{layout: 'box'}` (inline-expand per D-07).
      - cookie: `{name: 'cc_cookie', expiresAfterDays: 182}`.
      - categories: necessary (readOnly), analytics (enabled: !isEU, autoClear cookies), marketing (enabled: false), personalization (enabled: false).
      - onFirstConsent + onConsent + onChange callbacks → updateGtagConsent() + `void upsertConsentRecord(cookie);`.
      - language: per UI-SPEC Copywriting lines 588-606 — different lead copy for US vs EU (`isEU` switch).
    - Per Pitfall 7: each category should declare `services` array (e.g. analytics has `services: {posthog: {...}}`) so `acceptedService('posthog', 'analytics')` works for finer-grained gating.

    Banner styling tokens inherited from UI-SPEC §Color + Typography. The vanilla-cookieconsent library has its own CSS; per UI-SPEC line 327 banner uses `bg-[var(--color-surface)] border-t border-[var(--color-border)] shadow-[var(--shadow-lg)]` — apply via custom CSS override in `cookie-consent-overrides.css` (NEW file imported by consent-config.ts) OR rely on library's default + brand voice tweak (UI-SPEC notes legal counsel review at go-live; planner picks library default for v1.2 launch).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run consent-defer consent-config 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓|tests"</automated>
  </verify>
  <done>Defer + config implemented; 8 behavior tests green; vanilla-cookieconsent never imported statically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: consent-records.ts (GDPR-02 audit writer) + CookieConsentBootstrap + tests</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/lib/consent/consent-records.ts,
    /Users/karstenhaldan/minisite/leanshot/src/components/consent/CookieConsentBootstrap.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/lib/consent/__tests__/consent-records.test.ts,
    /Users/karstenhaldan/minisite/leanshot/src/components/consent/__tests__/CookieConsentBootstrap.test.tsx
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/lib/account-delete.ts (RPC wrapper analog),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave A consent_records writer audit lines 245-251),
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000005_consent_records_table.sql (post-Wave-0 — table schema)
  </read_first>
  <behavior>
    - Test (consent-records): upsertConsentRecord(cookie) UPSERTs row with cookie_categories JSONB, user_agent, country_code, consent_revision, recorded_at.
    - Test (consent-records): when user is authenticated → upserts with user_id = auth.uid(), anonymous_id = null.
    - Test (consent-records): when user is anonymous → upserts with user_id = null, anonymous_id = cookie.uid (vanilla-cookieconsent generates a uid per visitor).
    - Test (consent-records): subsequent calls UPDATE the existing row (not duplicate) — onConflict via user_id OR anonymous_id.
    - Test (consent-records): if Supabase write fails, function logs warning but DOES NOT throw (banner UX must not break on network failure).
    - Test (CookieConsentBootstrap): mounts as null + calls scheduleConsentInit() once on mount.
    - Test (CookieConsentBootstrap): subsequent re-renders do NOT call scheduleConsentInit() again (idempotent).
  </behavior>
  <action>
    Implement `consent-records.ts`:
    - `import { supabase } from '@/lib/supabase';`
    - Type `CookieConsentSnapshot` matching vanilla-cookieconsent's `cookie` argument shape (uid, categories, services, level, ...).
    - `export async function upsertConsentRecord(cookie: CookieConsentSnapshot): Promise<void>`:
      - Resolve `const {data: {user}} = await supabase.auth.getUser();`.
      - Build payload: `{user_id: user?.id ?? null, anonymous_id: user ? null : cookie.uid, cookie_categories: cookie.categories, recorded_at: new Date().toISOString(), consent_revision: cookie.revision ?? 1, user_agent: navigator.userAgent.slice(0, 500), country_code: (window as any).__VERCEL_GEO__?.country ?? null}`.
      - `await supabase.from('consent_records').upsert(payload, {onConflict: user ? 'user_id' : 'anonymous_id'});` — wrap in try/catch; log warning on failure, do NOT throw.
    - Note: ip_inet is server-side only (would require an Edge Fn to capture); v1.2 captures user_agent + country_code client-side; ip_inet stays NULL (acceptable per UI-SPEC; full-IP capture deferred).

    Implement `CookieConsentBootstrap.tsx`:
    - 5-line invoker per PATTERNS line 772-776:
      ```
      import { useEffect } from 'react';
      import { scheduleConsentInit } from '@/lib/consent/consent-defer';
      export function CookieConsentBootstrap() {
        useEffect(() => { scheduleConsentInit(); }, []);
        return null;
      }
      ```

    Mount note for plan 22-12: `<CookieConsentBootstrap />` mounts at App.tsx root (sibling of `<AppShell />` outlet) — covers marketing + dashboard + onboarding routes uniformly.

    Tests: 7 behaviors covered. Mock supabase via vi.mock; spy on supabase.from('consent_records').upsert + scheduleConsentInit.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run consent-records CookieConsentBootstrap 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓"</automated>
  </verify>
  <done>consent_records writer + bootstrap component + 7 tests green.</done>
</task>

<task type="auto">
  <name>Task 4: Bundle budget verification — confirm vanilla-cookieconsent stays out of index chunk</name>
  <files>(read-only)</files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/scripts/assert-clinic-bundle-budget.sh (existing budget guard if present),
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/project_phase5_bundle_regression.md
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/leanshot/`:
    1. `npm run build` → success.
    2. Verify the build produces a separate lazy chunk for vanilla-cookieconsent: `ls dist/assets/ | grep -i "cookieconsent\\|consent-config"`. Expected: at least one chunk containing the library, separate from `index-*.js`.
    3. Measure index chunk: `ls -la dist/assets/index-*.js | awk '{print $5}' | head -1` (raw bytes) then gzip-equivalent estimate `gzip -c dist/assets/index-*.js | wc -c`. Expect index gz < 50000 (50 kB ceiling).
    4. If index gz > 50 kB OR vanilla-cookieconsent is bundled into index: FAIL — verify consent-defer.ts is doing dynamic-import correctly (Pattern 4) and that no module on App.tsx/main.tsx/store.ts static-import graph references @/components/consent/consent-config (only @/components/consent/CookieConsentBootstrap and @/lib/consent/consent-defer should appear — and consent-defer uses type-only import).
    5. Record actual index gz size in 22-10-SUMMARY.md.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm run build 2&gt;&amp;1 | tail -10 | grep -qE "built|✓"</automated>
  </verify>
  <done>Build succeeds; vanilla-cookieconsent in separate lazy chunk; index gz under 50 kB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor → consent_records | RLS allows insert with user_id=null AND anonymous_id NOT null |
| authenticated user → consent_records | RLS allows insert/update where auth.uid() = user_id |
| analytics SDK (PostHog) | Only loaded after acceptedCategory('analytics') = true (GDPR-02 gate via existing sync-defer.ts pattern) |
| cc_cookie localStorage | Library-managed; consent state mirrored to consent_records for audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-56 | T | User directly writes cc_cookie to bypass banner | mitigate | Server-side consent_records is source-of-truth for audit; client-side cookie is UX only (matches GDPR Art. 7(1) requirement) |
| T-22-57 | I | Banner loaded with all data including PII before consent | mitigate | gtag('consent','default', ...) with denied defaults runs BEFORE banner mounts; PostHog SDK never loads until acceptedCategory('analytics') flips |
| T-22-58 | T | EU visitor served US default (analytics on) due to bad geo data | accept | Vercel geo can return null on edge case → defaults to !isEU = false → EU treatment (analytics off) — fail-safe direction |
| T-22-59 | I | Consent banner UI shows different defaults than what we write to consent_records | mitigate | Same `acceptedCategory()` source for both banner state and consent_records upsert |
| T-22-60 | D | vanilla-cookieconsent bundled into index → bundle budget regression | mitigate | Type-only import in consent-defer.ts; value import only inside async loadConsent(); Task 4 verifies separate chunk |
| T-22-61 | E | Pitfall 7 — acceptedCategory coarseness misses ad_storage vs analytics_storage distinction | mitigate | Pattern 3 uses acceptedService() with per-service declarations; Google Tag Assistant verification deferred to P20 |
</threat_model>

<verification>
- vanilla-cookieconsent installed; never statically imported.
- Bundle: index gz < 50 kB; separate lazy chunk for consent library.
- consent_records row created for each consent decision (authenticated + anonymous paths).
- Default state denied-everywhere (Consent Mode v2 compliance); EU + US lead-copy variants per UI-SPEC.
- 15 tests green across consent-defer + consent-config + consent-records + CookieConsentBootstrap.
</verification>

<success_criteria>
- GDPR-01 banner ready to mount (plan 22-12 does the App.tsx wiring).
- GDPR-02 audit infrastructure ready; PostHog gate enforced via existing sync-defer.
- VALIDATION.md rows for GDPR-01 + GDPR-02 updated with Plan 22-10.
- Bundle baseline recorded in 22-10-SUMMARY.md for regression detection.
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-10-SUMMARY.md` per template. Include: vanilla-cookieconsent gz cost (measured), index gz before/after, lazy chunk name, consent_records row shape, Pitfall 7 acceptedService verification.
</output>
