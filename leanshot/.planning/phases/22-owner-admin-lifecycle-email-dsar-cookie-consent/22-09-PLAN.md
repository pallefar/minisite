---
phase: 22
plan: 09
type: execute
wave: 2
depends_on: [01, 03]
autonomous: true
requirements: [ADMIN-03]
requirements_addressed: [ADMIN-03]
files_modified:
  - leanshot/src/components/impersonation/ImpersonationBanner.tsx
  - leanshot/src/components/impersonation/useImpersonation.ts
  - leanshot/src/components/impersonation/useImpersonationReadOnly.ts
  - leanshot/src/lib/admin/admin-impersonate.ts
  - leanshot/src/components/impersonation/__tests__/ImpersonationBanner.test.tsx
  - leanshot/src/components/impersonation/__tests__/useImpersonationReadOnly.test.ts

must_haves:
  truths:
    - "useImpersonation() hook reads `app_metadata.impersonator_id` + `app_metadata.impersonation_exp` from current session JWT (NOT from app.X GUC per Pitfall 1)"
    - "useImpersonation() returns {active: boolean, impersonatorId: string|null, targetEmail: string|null, secondsRemaining: number, endImpersonation: () => Promise<void>}"
    - "ImpersonationBanner mounts only when useImpersonation().active === true; sticky top-0 z-[60] bg-[var(--color-danger)] h-12 per UI-SPEC §Impersonation banner lines 284-297"
    - "Banner body text per UI-SPEC line 556: `Impersonating {email} · Read-only · {N}m {S}s remaining` with countdown decrementing every 1s via useInterval"
    - "End-impersonation CTA invokes admin-impersonate Edge Fn with action='end'; on success swaps back to admin session via stored refresh_token"
    - "Auto-expire: at secondsRemaining === 0 fires endImpersonation() automatically + toast 'Impersonation session expired.'"
    - "useImpersonationReadOnly() returns {disabled: bool, props: {disabled: bool, 'aria-disabled': bool, title: string}} — spread on every interactive element in the app to enforce client-side read-only (per UI-SPEC line 295 + RESEARCH Pattern 1 lines 392-407)"
    - "Inline form notice 'Read-only — changes cannot be saved during impersonation.' appears at top of any form during impersonation"
    - "admin-impersonate.ts client wrapper: startImpersonation({targetUserId}) stores admin's refresh_token in sessionStorage BEFORE setSession to target → enables endImpersonation restoration"
    - "Accessibility: role='alert' aria-live='assertive' on mount; countdown digits in span aria-live='off'; reduced-motion fallback drops the last-60s pulse animation"
    - "Implementation branch (A1 PROBE): if PASS — reads claims from supabase session.user.app_metadata; if FAIL — decodes custom HS256 JWT and reads claims from payload"
  artifacts:
    - path: leanshot/src/components/impersonation/ImpersonationBanner.tsx
      provides: "ADMIN-03 visual surface — red sticky banner with countdown + End CTA"
    - path: leanshot/src/components/impersonation/useImpersonation.ts
      provides: "Session-reading hook + endImpersonation flow"
    - path: leanshot/src/components/impersonation/useImpersonationReadOnly.ts
      provides: "Client-side disabled-props provider for all writable UI elements"
    - path: leanshot/src/lib/admin/admin-impersonate.ts
      provides: "Edge Fn invoker; refresh_token sessionStorage restoration"
  key_links:
    - from: leanshot/src/components/impersonation/useImpersonation.ts
      to: supabase.auth.getSession().data.session.user.app_metadata
      via: "useEffect + onAuthStateChange listener"
      pattern: "app_metadata.impersonator_id"
    - from: leanshot/src/lib/admin/admin-impersonate.ts
      to: supabase/functions/admin-impersonate/index.ts
      via: "supabase.functions.invoke('admin-impersonate', {body: {action: 'start'|'end', target_user_id}})"
      pattern: "admin-impersonate"
    - from: leanshot/src/components/impersonation/useImpersonationReadOnly.ts
      to: "every form/button/input across the app"
      via: "spread `props` from hook onto disabled-eligible element (used by plan 22-12 sweep)"
      pattern: "useImpersonationReadOnly"
---

<objective>
Ship ADMIN-03's UI half: ImpersonationBanner (sticky red 48px banner with countdown) + useImpersonation hook (session-reading + endImpersonation) + useImpersonationReadOnly hook (client-side read-only disabled-props provider).

Purpose: This is the visible side of read-only impersonation. Plan 22-03 ships the server-side mint + RLS deny; this plan ships the user-facing affordance + client-side UX gate. Together with the AppShell mount (plan 22-12) the feature is end-to-end.

Output: 3 files + 1 client lib + 2 tests green; A1 PROBE branch implemented.
</objective>

<execution_context>
@/Users/karstenhaldan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karstenhaldan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-A1-PROBE.md
@/Users/karstenhaldan/minisite/leanshot/src/components/clinic/ClinicContextBar.tsx
@/Users/karstenhaldan/minisite/leanshot/src/lib/account-delete.ts
@/Users/karstenhaldan/minisite/leanshot/src/hooks/useReducedMotion.ts
@/Users/karstenhaldan/minisite/leanshot/src/lib/supabase.ts
@/Users/karstenhaldan/minisite/leanshot/src/components/impersonation/__tests__/ImpersonationBanner.test.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/impersonation/__tests__/useImpersonationReadOnly.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HEAD-reset + useImpersonation hook + admin-impersonate.ts client wrapper</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/impersonation/useImpersonation.ts,
    /Users/karstenhaldan/minisite/leanshot/src/lib/admin/admin-impersonate.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-A1-PROBE.md (verdict drives implementation branch),
    /Users/karstenhaldan/minisite/leanshot/src/lib/account-delete.ts (analog — RPC wrapper + discriminated-error pattern lines 23-71),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Pattern 1 lines 365-410; Pitfall 1; A1 ASSUMED + fallback),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave D admin-impersonate.ts section lines 565-587),
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/reference_supabase_auth_traps.md (hash-route + setSession traps for magiclink flow)
  </read_first>
  <action>
    HEAD-reset: `test -f /Users/karstenhaldan/minisite/supabase/functions/admin-impersonate/index.ts || exit 1` + `test -f /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-A1-PROBE.md || exit 1`.

    Read A1 verdict. Document branch at top of admin-impersonate.ts header comment: `// A1 PROBE verdict: <PASS|FAIL> — claims read from <session.user.app_metadata | decoded HS256 payload>`.

    Implement `useImpersonation.ts`:
    - Hook signature: `export function useImpersonation(): { active: boolean; impersonatorId: string | null; targetEmail: string | null; secondsRemaining: number; endImpersonation: () => Promise<void> }`.
    - useState for session; useEffect on mount + supabase.auth.onAuthStateChange to keep session current.
    - PRIMARY branch (A1 PASS): read `session?.user?.app_metadata?.impersonator_id` + `session?.user?.app_metadata?.impersonation_exp`.
    - FALLBACK branch (A1 FAIL): decode the access_token JWT payload (`session?.access_token` split by '.', base64-decode middle segment) and read `payload.app_metadata.impersonator_id` + `impersonation_exp`.
    - secondsRemaining: `Math.max(0, Math.floor((impersonation_exp - Date.now()) / 1000))`.
    - active: `Boolean(impersonator_id && secondsRemaining > 0)`.
    - useInterval(1000) recomputes secondsRemaining; auto-fires endImpersonation when reaches 0.
    - endImpersonation(): invokes admin-impersonate Edge Fn with action='end' + target_user_id (read from session.user.id) → on success restore admin session via stored refresh_token from sessionStorage → reload page.

    Implement `src/lib/admin/admin-impersonate.ts`:
    - Discriminated error type: `ImpersonationErrorCode = 'not_staff' | 'invalid_target' | 'session_expired' | 'cannot_impersonate_self' | 'cannot_impersonate_admin' | 'unknown'` per PATTERNS line 570-578.
    - `startImpersonation({targetUserId}): Promise<{actionLink: string; expiresAt: number}>` — BEFORE invoking Edge Fn, store admin's refresh_token: `const {data: {session}} = await supabase.auth.getSession(); sessionStorage.setItem('impersonation_admin_refresh', session.refresh_token);`. Then `const {data, error} = await supabase.functions.invoke('admin-impersonate', {body: {action: 'start', target_user_id: targetUserId}});` → on success, extract access_token + refresh_token from data.action_link hash → `await supabase.auth.setSession({access_token, refresh_token});` → return data.
    - `endImpersonation({targetUserId}): Promise<void>` — invoke action='end' → on success restore admin refresh_token: `const adminRefresh = sessionStorage.getItem('impersonation_admin_refresh'); if (adminRefresh) await supabase.auth.refreshSession({refresh_token: adminRefresh}); sessionStorage.removeItem('impersonation_admin_refresh');`.
    - Per PATTERNS line 588 — flag the sessionStorage admin refresh_token storage as a known security footgun in header comment: "sessionStorage stores admin refresh_token transiently during impersonation; cleared on end. XSS exposure is the inherent risk; mitigated by SPA CSP from Phase 12 + impersonation TTL of 30 min limiting blast radius."
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; test -f src/components/impersonation/useImpersonation.ts &amp;&amp; test -f src/lib/admin/admin-impersonate.ts &amp;&amp; grep -q "useImpersonation" src/components/impersonation/useImpersonation.ts &amp;&amp; grep -q "startImpersonation\\|endImpersonation" src/lib/admin/admin-impersonate.ts</automated>
  </verify>
  <done>Hook + client wrapper exist with A1 branch implemented; sessionStorage admin refresh persistence documented + cleared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ImpersonationBanner + useImpersonationReadOnly + both tests</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/impersonation/ImpersonationBanner.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/impersonation/useImpersonationReadOnly.ts,
    /Users/karstenhaldan/minisite/leanshot/src/components/impersonation/__tests__/ImpersonationBanner.test.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/impersonation/__tests__/useImpersonationReadOnly.test.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/components/clinic/ClinicContextBar.tsx (analog sticky banner pattern + UI-SPEC contracts),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (Impersonation banner lines 284-297; Copywriting lines 552-560),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Pattern 1 lines 392-407 — useImpersonationReadOnly excerpt),
    /Users/karstenhaldan/minisite/leanshot/src/hooks/useReducedMotion.ts (existing hook to honor for countdown pulse)
  </read_first>
  <behavior>
    - Test (Banner): returns null when useImpersonation().active === false.
    - Test (Banner): renders sticky top-0 z-[60] bg-[var(--color-danger)] text-white h-12 (per UI-SPEC line 287-288).
    - Test (Banner): body text matches `Impersonating {email} · Read-only · {N}m {S}s remaining` (per UI-SPEC line 290 + Copywriting line 556).
    - Test (Banner): End-impersonation button labeled "End impersonation" + onClick invokes useImpersonation().endImpersonation.
    - Test (Banner): role="alert" + aria-live="assertive" on mount; countdown span aria-live="off" (per UI-SPEC line 292).
    - Test (Banner): countdown decrements every 1s — fake-timer test asserts text changes after advancing 1000ms.
    - Test (Banner): when secondsRemaining < 60, body text pulses (CSS class added); when useReducedMotion=true, NO pulse class added (per UI-SPEC line 293).
    - Test (Banner): auto-end at 0 — fake-timer advances past expiration → endImpersonation invoked once + toast "Impersonation session expired." (per UI-SPEC line 559).
    - Test (Banner): icon `lucide:UserCog` with aria-hidden (per UI-SPEC line 290).
    - Test (useImpersonationReadOnly): when useImpersonation().active === false → returns `{disabled: false, props: {}}`.
    - Test (useImpersonationReadOnly): when active === true → returns `{disabled: true, props: {disabled: true, 'aria-disabled': true, title: 'Read-only during impersonation'}}` per RESEARCH lines 399-405.
  </behavior>
  <action>
    Implement `ImpersonationBanner.tsx`:
    - Read useImpersonation() + useReducedMotion().
    - Return null if !active.
    - Container: `<div className="sticky top-0 z-[60] bg-[var(--color-danger)] text-white h-12 flex items-center justify-between gap-3 px-4" role="alert" aria-live="assertive">`.
    - Left: `<UserCog aria-hidden className="size-4" /> Impersonating {targetEmail} · Read-only · <span aria-live="off">{Math.floor(secondsRemaining/60)}m {secondsRemaining%60}s</span> remaining`.
    - Right: `<button onClick={endImpersonation} className="bg-white text-[var(--color-danger)] h-8 px-3 rounded-pill text-sm font-semibold">End impersonation</button>`.
    - Pulse: if `secondsRemaining < 60 && !reducedMotion` add `className="animate-pulse"` or custom CSS animation per UI-SPEC line 293.
    - Auto-expire effect: useEffect dependent on secondsRemaining; when === 0 + previously > 0 → call endImpersonation() + toast.

    Implement `useImpersonationReadOnly.ts` per RESEARCH lines 392-407 verbatim:
    - Hook returns `{disabled: boolean, props: object}`.
    - Read `useImpersonation().active` → if false return `{disabled: false, props: {}}` → if true return `{disabled: true, props: {disabled: true, 'aria-disabled': true, title: 'Read-only during impersonation'}}`.

    Tests: 9 banner behaviors + 2 hook behaviors. Use `@testing-library/react` + vitest fake timers. Mock useImpersonation via vi.mock.

    Wiring note for plan 22-12: AppShell must spread `<ImpersonationBanner />` above topbar. Also: every <Button>/<Input>/<form> in the writable surfaces needs `{...useImpersonationReadOnly().props}` spread — this is a sweep in plan 22-12 Wave 3.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run ImpersonationBanner useImpersonationReadOnly 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓"</automated>
  </verify>
  <done>Banner + read-only hook + 11 tests green; reduced-motion fallback honored; auto-expire works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → admin-impersonate Edge Fn | Caller JWT validated by Edge Fn gateway + is_staff re-check in body |
| impersonated session → RLS write-deny | request.jwt.claims->app_metadata.impersonator_id read by 51 policies from plan 22-01 File 12 |
| sessionStorage admin_refresh | Stored client-side during impersonation window; cleared on end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-51 | T | Client UI bypassed → impersonated session attempts UPDATE | mitigate | Defense-in-depth: RLS write-deny (plan 22-01 File 12) is the security boundary; useImpersonationReadOnly is UX-only |
| T-22-52 | I | sessionStorage admin refresh_token XSS exposure | mitigate | SPA CSP from Phase 12 limits XSS surface; 30-min impersonation TTL limits blast radius; cleared on endImpersonation; documented in header comment |
| T-22-53 | T | Impersonation TTL bypass via banner stale countdown | mitigate | Auto-expire useEffect fires endImpersonation() when secondsRemaining === 0; even if banner misses the tick, JWT exp claim on server stops further mutations |
| T-22-54 | T | Admin refresh_token used after impersonation ends (replay) | accept | Refresh tokens are bearer; rotation handled by Supabase Auth; if admin's session needs to be revoked use admin.deleteUser or admin.signOut all-sessions |
| T-22-55 | E | A1 PROBE FAIL branch uses custom HS256 → claim verification mismatch with PostgREST expectations | mitigate | Fallback branch documented in 22-A1-PROBE.md; supabase.auth.setSession({access_token, refresh_token: null}) works for custom JWTs if signed with same SUPABASE_JWT_SECRET; if not, mint Vault IMPERSONATION_JWT_SIGNING_KEY separately and configure Custom Access Token Hook in Supabase Auth |
</threat_model>

<verification>
- 11 tests green across ImpersonationBanner + useImpersonationReadOnly.
- A1 PROBE branch correctly implemented in useImpersonation.
- sessionStorage refresh_token cleared on endImpersonation.
- Mount wiring for AppShell + read-only sweep tracked in plan 22-12.
</verification>

<success_criteria>
- ADMIN-03 visual + interaction complete.
- VALIDATION.md row for ADMIN-03 UI updated with Plan 22-09.
- Defense-in-depth dual-layer (RLS server + useImpersonationReadOnly client) documented.
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-09-SUMMARY.md` per template. Include: A1 branch chosen, sessionStorage security note, list of files requiring useImpersonationReadOnly spread (for plan 22-12 sweep).
</output>
