---
phase: 22
plan: 04
type: execute
wave: 1
depends_on: [01]
autonomous: true
requirements: [GDPR-03]
requirements_addressed: [GDPR-03]
files_modified:
  - supabase/functions/dsar-export/index.ts
  - supabase/functions/dsar-export/pdf-render.ts
  - supabase/functions/dsar-export/deno.json
  - supabase/migrations/20270601000018_dsar_request_rpcs.sql

must_haves:
  truths:
    - "dsar-export Edge Fn implements the 9-step orchestrator pattern: status pending → in_progress → aggregate Postgres → SHA-256 hash referred emails (D-06) → render JSON+PDF → ZIP → upload to dsar-exports bucket → sign 7-day URL → invoke lifecycle-transactional dsar_ready → status completed"
    - "PDF rendered server-side via jsPDF + jspdf-autotable (esm.sh imports for Deno) with the 7-section structure from D-06: Profile, Subscriptions, Health Log, Photos, Sharing History, Affiliate Activity, Communications"
    - "Affiliate referred-user emails hashed via Postgres `extensions.digest(email::bytea, 'sha256')` then `encode(..., 'hex')` before inclusion (D-06 cross-tenant pseudonymization)"
    - "DSAR PDF delivered ONLY via signed Storage URL (per Pitfall 10 — gateway overrides Content-Type to text/plain; inline PDF response broken)"
    - "ZIP packaging uses Deno stdlib archive OR https://deno.land/x/zipjs for streaming-friendly concatenation"
    - "PostHog events optionally pulled at export time via REST API if PERSONAL_API_KEY Function secret present; gracefully omitted with log warning if unset (Open Q4 + fallback per RESEARCH §Environment Availability)"
    - "Two new RPCs: create_dsar_request (user-callable, INSERTs pending row) + admin_reject_dsar (is_staff-gated, writes rejected status)"
    - "Status transitions covered (PATTERNS S6): pending (create_dsar_request, plan 22-11) → in_progress (dsar-export start) → completed (dsar-export finish) → rejected (admin_reject_dsar)"
  artifacts:
    - path: supabase/functions/dsar-export/index.ts
      provides: "GDPR-03 orchestrator; aggregates user data, builds bundle, delivers via signed URL"
    - path: supabase/functions/dsar-export/pdf-render.ts
      provides: "Deno-side jsPDF + jspdf-autotable rendering for 7-section PDF"
    - path: supabase/migrations/20270601000018_dsar_request_rpcs.sql
      provides: "create_dsar_request (user) + admin_reject_dsar (staff) status-machine writers"
  key_links:
    - from: supabase/functions/dsar-export/index.ts
      to: dsar-exports Storage bucket
      via: "service-role upload + 7-day signed URL (admin.storage.from('dsar-exports').createSignedUrl(path, 7*86400))"
      pattern: "dsar-exports"
    - from: supabase/functions/dsar-export/index.ts
      to: supabase/functions/lifecycle-transactional/index.ts
      via: "POST {template:'dsar_ready', user_id, data:{signed_url}}"
      pattern: "lifecycle-transactional"
    - from: supabase/functions/dsar-export/index.ts
      to: public.dsar_requests
      via: "UPDATE status='in_progress' at start, 'completed' at finish, completed_at = now()"
      pattern: "dsar_requests"
---

<objective>
Ship the DSAR export Edge Function (GDPR-03): aggregates the user's data across Postgres + Storage + Stripe + PostHog + affiliate ledger (referred emails hashed per D-06), generates JSON + PDF, ZIPs, uploads to private Storage, signs 7-day URL, emails delivery.

Purpose: Close the GDPR Article 15 (right of access) primitive. The complexity is in the redaction contract (D-06) and the multi-system aggregation — implemented as a single Edge Function so the 30-day SLA tracker logic stays in one place.

Output: dsar-export deployed; status machine has all 4 writers; Deno test green covers the cascade + hash-pseudonymization invariant.
</objective>

<execution_context>
@/Users/karstenhaldan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karstenhaldan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md
@/Users/karstenhaldan/minisite/supabase/functions/account-delete/index.ts
@/Users/karstenhaldan/minisite/supabase/functions/affiliate-payout/index.ts
@/Users/karstenhaldan/minisite/leanshot/src/lib/export-data.ts
@/Users/karstenhaldan/minisite/supabase/functions/dsar-export/index.test.ts

<interfaces>
<!-- From plan 22-01 migrations -->

From supabase/migrations/20270601000006_dsar_requests_table.sql:
public.dsar_requests (id uuid, user_id uuid, requested_at timestamptz, completed_at timestamptz, status public.dsar_request_status, rejection_reason text, export_path text, export_signed_url_expires_at timestamptz)
public.dsar_request_status enum: 'pending' | 'in_progress' | 'completed' | 'rejected'

From supabase/migrations/20270601000007_dsar_exports_storage_bucket.sql:
storage.buckets row: id='dsar-exports', public=false
storage.objects RLS: select-own scoped to (storage.foldername(name))[1] = auth.uid()::text

From plan 22-02:
supabase/functions/lifecycle-transactional/index.ts — accepts {template:'dsar_ready', user_id, data:{signed_url}}

From Phase 19 supabase/functions/account-delete/index.ts (multi-step orchestrator analog):
- 10-step cascade with try/catch + per-step audit
- Lazy admin singleton + Proxy
- UUID_RE + CORS headers + service-role bearer compare

From src/lib/export-data.ts (jsPDF dynamic-import analog):
- 7-section structure
- autotable usage pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HEAD-reset verify + author 2 DSAR RPCs migration</name>
  <files>/Users/karstenhaldan/minisite/supabase/migrations/20270601000018_dsar_request_rpcs.sql</files>
  <read_first>
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000006_dsar_requests_table.sql (post-Wave-0 — schema reference),
    /Users/karstenhaldan/minisite/supabase/migrations/20260601000011_initiate_account_deletion_rpc.sql (analog for user-callable SECURITY DEFINER RPC pattern),
    /Users/karstenhaldan/minisite/supabase/migrations/20261101000006_is_staff_helper.sql (is_staff gate pattern for admin_reject_dsar),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave A dsar_requests writer audit lines 245-259 — S6 status-machine ownership)
  </read_first>
  <action>
    HEAD-reset verification: `test -f /Users/karstenhaldan/minisite/supabase/migrations/20270601000006_dsar_requests_table.sql || exit 1`.

    Author migration `20270601000018_dsar_request_rpcs.sql` with two SECURITY DEFINER RPCs:

    (1) `create or replace function public.create_dsar_request() returns uuid security definer set search_path = public, pg_catalog language plpgsql as $$ declare v_request_id uuid; begin if auth.uid() is null then raise exception 'unauthenticated'; end if; -- one active request at a time per user (prevent abuse) if exists (select 1 from public.dsar_requests where user_id = auth.uid() and status in ('pending','in_progress')) then raise exception 'already_pending'; end if; perform set_config('app.suppress_audit','true',true); insert into public.dsar_requests (user_id, status) values (auth.uid(), 'pending') returning id into v_request_id; insert into public.audit_logs (action_type, actor_id, target_user_id, payload) values ('dsar_requested', auth.uid(), auth.uid(), jsonb_build_object('request_id', v_request_id)); return v_request_id; end; $$;` + grant execute to authenticated.

    (2) `create or replace function public.admin_reject_dsar(p_request_id uuid, p_reason text) returns void security definer set search_path = public, pg_catalog language plpgsql as $$ declare v_user_id uuid; begin if not exists (select 1 from public.profiles where id = auth.uid() and is_staff = true) then raise exception 'forbidden' using errcode = '42501'; end if; select user_id into v_user_id from public.dsar_requests where id = p_request_id; if v_user_id is null then raise exception 'not_found'; end if; perform set_config('app.suppress_audit','true',true); update public.dsar_requests set status = 'rejected', rejection_reason = p_reason, completed_at = now() where id = p_request_id; insert into public.audit_logs (action_type, actor_id, target_user_id, payload) values ('dsar_completed', auth.uid(), v_user_id, jsonb_build_object('request_id', p_request_id, 'outcome','rejected','reason', p_reason)); end; $$;` + grant execute to authenticated.

    Header comment cites Pattern S6 (writers for `pending` = this RPC, `rejected` = this RPC, `in_progress`+`completed` = dsar-export Edge Fn). Use `dsar_requested` enum value from plan 22-01 File 02; `dsar_completed` is also from that file.

    Push: `cd /Users/karstenhaldan/minisite &amp;&amp; supabase db push --linked --dry-run | grep -v "^Skipping" &amp;&amp; supabase db push --linked` then verify `supabase db query --linked "select count(*) from pg_proc where proname in ('create_dsar_request','admin_reject_dsar');"` → expect 2.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; test -f supabase/migrations/20270601000018_dsar_request_rpcs.sql &amp;&amp; grep -q "create_dsar_request" supabase/migrations/20270601000018_dsar_request_rpcs.sql &amp;&amp; grep -q "admin_reject_dsar" supabase/migrations/20270601000018_dsar_request_rpcs.sql</automated>
  </verify>
  <done>Migration 18 exists with 2 RPCs covering pending + rejected status writers; pushed to remote.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dsar-export Edge Function + pdf-render.ts + Deno test</name>
  <files>
    /Users/karstenhaldan/minisite/supabase/functions/dsar-export/index.ts,
    /Users/karstenhaldan/minisite/supabase/functions/dsar-export/pdf-render.ts,
    /Users/karstenhaldan/minisite/supabase/functions/dsar-export/deno.json,
    /Users/karstenhaldan/minisite/supabase/functions/dsar-export/index.test.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/supabase/functions/account-delete/index.ts (lines 1-200 — multi-step orchestrator + per-step try/catch + audit pattern; 10-step cascade structure to mirror in 9-step DSAR),
    /Users/karstenhaldan/minisite/supabase/functions/affiliate-payout/index.ts (lazy admin singleton + Proxy + CORS + bearer compare),
    /Users/karstenhaldan/minisite/leanshot/src/lib/export-data.ts (jsPDF 7-section pattern + autotable usage; lines 23-32 dynamic-import pattern; lines 37-60 EXPORT_WHITELIST_KEYS),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Pattern 6 lines 600-635 — DSAR cascade flow; Pitfall 10 lines 506-510 — gateway Content-Type override forces signed URL delivery),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave C dsar-export section lines 474-525 — 9-step orchestrator; Deno-side jsPDF; ZIP via zipjs),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md (D-06 — DSAR scope including SHA-256 of referred emails; 7-section structure)
  </read_first>
  <behavior>
    - Test: POST without service_role bearer → 401.
    - Test: POST {request_id} for a request belonging to user X → admin reads dsar_requests row, status='pending' → updates to 'in_progress'.
    - Test: aggregate phase — selects from injections, weights, meals, workouts, supplements, mood, sleep, symptoms, vials, settings, photos, ai_messages, shares, affiliate_clicks, affiliate_conversions, affiliate_payouts, consent_records WHERE user_id = X.
    - Test: hash phase — affiliate_conversions rows include `converter_user_id` (UUID) but DSAR bundle includes `converter_email_sha256` (hex string of SHA-256). Plaintext email is NEVER in bundle.
    - Test: PDF render — calls renderDsarPdf(bundle) which uses jsPDF + jspdf-autotable to produce a Blob with 7 sections.
    - Test: ZIP packaging — combines `data.json` + `data.pdf` + (optional) `photos/<name>` files into a single zip Blob.
    - Test: upload phase — service-role uploads to `dsar-exports/{user_id}/{request_id}.zip` via admin.storage.from('dsar-exports').upload.
    - Test: sign URL — admin.storage.from('dsar-exports').createSignedUrl(path, 7*86400) → returns signed URL.
    - Test: lifecycle invoke — admin.functions.invoke('lifecycle-transactional', {body: {template:'dsar_ready', user_id, data:{signed_url, expires_at}}}).
    - Test: final UPDATE — dsar_requests.status = 'completed', completed_at = now(), export_path, export_signed_url_expires_at = now() + 7 days.
    - Test: PostHog branch — if Deno.env.get('POSTHOG_PERSONAL_API_KEY') is set, fn fetches events from PostHog REST API filtered by distinct_id; if unset, bundle.posthog_events = null + console.warn.
    - Test: Storage cleanup of old DSAR exports does NOT happen in this Fn (deferred to a separate v1.3 cron — Pitfall 5 documented).
  </behavior>
  <action>
    Implement `supabase/functions/dsar-export/index.ts` as a 9-step orchestrator. Structure mirrors `account-delete/index.ts` with per-step try/catch + status update.

    Imports:
    - `import { createClient } from 'npm:@supabase/supabase-js@2';`
    - `import { renderDsarPdf } from './pdf-render.ts';`
    - `import { zip } from 'https://deno.land/x/zipjs@v2.7.45/index.js';` (or Deno stdlib archive if simpler — pick what works)

    Lazy admin singleton + Proxy + CORS headers + bearer compare against SUPABASE_SERVICE_ROLE_KEY (cron + admin-invocable; no user JWT path).

    Handler body — orchestrate:

    1. Parse `{request_id: uuid}` from body. Validate UUID_RE.
    2. SELECT dsar_requests row → if status !== 'pending' return 200 (already processing or done; idempotent). Otherwise UPDATE status='in_progress'.
    3. user_id = row.user_id; assemble `bundle` object:
       - profile: SELECT from auth.users + public.profiles WHERE id = user_id (whitelist keys).
       - subscriptions: SELECT from public.subscriptions WHERE user_id = user_id.
       - health_log: arrays for injections / weights / meals / workouts / supplements / mood / sleep / symptoms / vials.
       - photos: SELECT id, path, created_at FROM photos WHERE user_id = user_id (path used for signed URLs to include in ZIP).
       - sharing_history: SELECT token, doctor_email, created_at, last_accessed_at FROM shares WHERE user_id = user_id (NOT doctor's notes per D-06).
       - affiliate_activity: SELECT from affiliate_clicks + affiliate_conversions + affiliate_payouts WHERE affiliate_user_id = user_id; for each conversion, replace `converter_email` (joined from auth.users.email) with `converter_email_sha256 = encode(digest(email::bytea, 'sha256'), 'hex')` (D-06 hash via pgcrypto — invoke via `select encode(extensions.digest($1::bytea, 'sha256'), 'hex')` RPC OR direct SELECT with extensions schema reachable).
       - communications: SELECT ai_messages WHERE user_id = user_id AND role = 'user' (only user side per D-06).
       - posthog_events: if `Deno.env.get('POSTHOG_PERSONAL_API_KEY')` is set, fetch `https://app.posthog.com/api/projects/{project_id}/events/?distinct_id={user_id}` with bearer; else null + console.warn.
       - generated_at, schema_version: 'phase22-v1'.
    4. Render JSON: `const jsonBlob = new Blob([JSON.stringify(bundle, null, 2)], {type: 'application/json'});`.
    5. Render PDF: `const pdfBlob = await renderDsarPdf(bundle);` (server-side jsPDF; see pdf-render.ts below).
    6. ZIP: download each photo via admin.storage.from('photos').download(photo.path) → include as `photos/<id>.<ext>` in zip; combine `data.json` + `data.pdf` + photos → produce `bundleZipBlob`.
    7. Upload: `await admin.storage.from('dsar-exports').upload(\`${user_id}/${request_id}.zip\`, bundleZipBlob, {contentType: 'application/zip', upsert: true});`.
    8. Sign: `const {data: signed} = await admin.storage.from('dsar-exports').createSignedUrl(\`${user_id}/${request_id}.zip\`, 7*86400);`.
    9. Invoke lifecycle: `await admin.functions.invoke('lifecycle-transactional', {body: {template: 'dsar_ready', user_id, data: {signed_url: signed.signedUrl, expires_at: new Date(Date.now()+7*86400*1000).toISOString()}}});`.
    10. UPDATE dsar_requests SET status='completed', completed_at=now(), export_path=path, export_signed_url_expires_at=... + insert audit_logs row with action_type='dsar_completed'.

    Per-step try/catch: on error, UPDATE dsar_requests.status='rejected', rejection_reason=err.message (sanitized — no Stripe/Resend PII). Audit log row with payload {step, error_code}.

    Implement `pdf-render.ts`:
    - `import { jsPDF } from 'https://esm.sh/jspdf@4?target=denonext';`
    - `import autoTable from 'https://esm.sh/jspdf-autotable@5?target=denonext';`
    - export `async function renderDsarPdf(bundle: DsarBundle): Promise<Blob>` — render 7 sections in order: Profile, Subscriptions, Health Log, Photos (file list only — actual photos are in ZIP), Sharing History, Affiliate Activity, Communications. Use `autoTable(doc, {head, body, startY})` per section. Final `return doc.output('blob');`.
    - Bundle-budget note in header comment: this is Deno-side; static esm.sh import is fine (no client bundle impact).

    `deno.json`: copy from `supabase/functions/account-delete/deno.json` or `affiliate-payout/deno.json`.

    `index.test.ts`: per <behavior> list. Mock supabase queries, storage upload, functions.invoke, jsPDF. Spy on the SHA-256 hashing step explicitly (assert that no plaintext email ends up in the JSON bundle).

    Critical: per Pitfall 10 (gateway Content-Type override + CSP sandbox injection), the Fn MUST NOT return the PDF or ZIP bytes inline. ONLY return status JSON + the signed URL is delivered via email.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/supabase/functions &amp;&amp; deno test --allow-all dsar-export/index.test.ts 2&gt;&amp;1 | tail -3 | grep -E "ok \\| [1-9]"</automated>
  </verify>
  <done>dsar-export Fn implements 9-step cascade with hash-pseudonymization invariant tested; pdf-render produces 7-section PDF; Deno test green.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy dsar-export + smoke verify</name>
  <files>(deployment-only)</files>
  <read_first>
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/reference_supabase_edge_function_deploy.md
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/`:
    1. `supabase functions deploy dsar-export` (cron-only callable; verify_jwt=false because service-role bearer is the gate).
    2. Verify list: `supabase functions list | grep -E "^dsar-export"`.
    3. Smoke: `curl -X POST -H "Authorization: Bearer wrong-key" "$SUPABASE_URL/functions/v1/dsar-export"` → expect 401.
    4. Verify `dsar-exports` bucket exists and is private: `supabase db query --linked "select id, public from storage.buckets where id = 'dsar-exports';"` → expect 1 row with public=false.
    5. Document follow-up vendor pass: PostHog `POSTHOG_PERSONAL_API_KEY` Function secret + project_id env — record in 22-04-SUMMARY.md as P22 closeout item (per RESEARCH §Environment Availability line 1002).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; supabase functions list 2&gt;/dev/null | grep -q "^dsar-export"</automated>
  </verify>
  <done>dsar-export deployed; bucket private; vendor-pass for PostHog REST documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → dsar-export | Service-role bearer only (cron + admin invoke; not user-facing) |
| Edge Fn → photo Storage | service-role download for inclusion in ZIP |
| Edge Fn → dsar-exports Storage | service-role upload; signed URL with 7-day TTL is the only download path |
| dsar-export → PostHog REST | optional Bearer with PERSONAL_API_KEY; omitted gracefully if unset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-26 | I | DSAR export bundle leaks plaintext converter emails (D-06 violation) | mitigate | SHA-256 hash via Postgres `extensions.digest(email::bytea, 'sha256')` then `encode(..., 'hex')`; explicit test asserts NO plaintext email in JSON bundle |
| T-22-27 | I | DSAR signed URL forwarded to unauthorized recipient | accept | 7-day TTL is the bound; one-time download flag deferred to v1.3 (RESEARCH §Security line 1124) |
| T-22-28 | I | Cross-tenant data leak (other clinic members' records) | mitigate | D-06 redaction contract — bundle only patient's OWN data; clinic_membership row is patient's own, not other members |
| T-22-29 | T | Edge Fn returns PDF bytes inline → gateway override breaks Content-Type | mitigate | Pitfall 10 — Fn ONLY returns status JSON; PDF delivered via Storage signed URL |
| T-22-30 | R | DSAR generation failure leaves request stuck in 'in_progress' | mitigate | Per-step try/catch → status='rejected' + rejection_reason on any failure; admin can re-create via admin_reject_dsar or user re-submits |
| T-22-31 | D | DSAR for user with thousands of photos exhausts Fn memory | accept | Stream ZIP via zipjs (chunked); 30-day SLA absorbs retry windows; v1.3 cursor-pagination if user load proves it |
| T-22-32 | T | PostHog PERSONAL_API_KEY exfiltration via export bundle | mitigate | Key in Function secret only; never logged to Sentry; bundle includes events not the key |
</threat_model>

<verification>
- dsar-export Fn deployed; non-service-role bearer → 401.
- dsar_requests status machine has all 4 writers (PATTERNS S6 covered): pending (create_dsar_request from 22-11), in_progress (dsar-export start), completed (dsar-export finish), rejected (admin_reject_dsar OR dsar-export error path).
- SHA-256 hash invariant tested: JSON bundle includes `converter_email_sha256` (64-char hex) and NO field named `converter_email`.
- PDF delivery only via signed URL (Pitfall 10 enforced).
- PostHog branch tested with key set and unset.
</verification>

<success_criteria>
- GDPR-03 server-side complete; UI (plan 22-11) just needs to invoke `create_dsar_request` RPC and poll dsar_requests.status.
- VALIDATION.md per-task rows updated for GDPR-03 with Plan 22-04.
- Vendor pass logged: PostHog PERSONAL_API_KEY Function secret.
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-04-SUMMARY.md` per template. Include: 9-step orchestrator trace, hash-pseudonymization test result, ZIP library choice (deno.land/x/zipjs vs stdlib), PostHog vendor-pass status.
</output>
