---
phase: 22
plan: 02
type: execute
wave: 1
depends_on: [01]
autonomous: true
requirements: [ON-02]
requirements_addressed: [ON-02]
files_modified:
  - supabase/functions/_shared/resend-domain-health-check.ts
  - supabase/functions/lifecycle-welcome-series/index.ts
  - supabase/functions/lifecycle-welcome-series/templates.ts
  - supabase/functions/lifecycle-welcome-series/deno.json
  - supabase/functions/lifecycle-behavior-triggered/index.ts
  - supabase/functions/lifecycle-behavior-triggered/templates.ts
  - supabase/functions/lifecycle-behavior-triggered/deno.json
  - supabase/functions/lifecycle-transactional/index.ts
  - supabase/functions/lifecycle-transactional/templates.ts
  - supabase/functions/lifecycle-transactional/deno.json
  - supabase/functions/lifecycle-retention/index.ts
  - supabase/functions/lifecycle-retention/templates.ts
  - supabase/functions/lifecycle-retention/deno.json
  - supabase/functions/lifecycle-preference-update/index.ts
  - supabase/functions/lifecycle-preference-update/deno.json
  - supabase/migrations/20270601000017_lifecycle_cron_schedules.sql

must_haves:
  truths:
    - "_shared/resend-domain-health-check.ts exports `resendDomainHealthCheck(supabase)` returning {ok, status} and increments `email_send_counters.resend_domain_unverified_skips` when status !== 'verified' (D-03)"
    - "All 5 lifecycle Edge Functions short-circuit when RESEND_API_KEY === 'test-stub' (Pitfall 6 — Resend free-tier 2/hour rate limit kills lifecycle tests)"
    - "All 5 lifecycle Edge Functions perform health-check on every invocation; if !ok return 200 + log warning + Sentry breadcrumb (no send)"
    - "12 templates implemented per UI-SPEC table: welcome_immediately, getting_started_day1, first_injection_reminder, week_1_check_in, first_injection_celebration, 7_day_streak, missed_dose_day3, receipt, password_reset, reengagement_7d_inactive, cancellation_winback_30d, weekly_digest — every template is inline-style HTML only (Pitfall 8 — Gmail strips <style>)"
    - "Email send respects `consent_records.email_preferences` JSONB — lifecycle Edge Fns SELECT preferences before send, skip if user disabled the matching category"
    - "Cron schedules registered: welcome-series every 4h, behavior-triggered every 15min, retention daily 06:00 UTC, preference-update HTTP-only (no cron), transactional HTTP-only (no cron)"
    - "Sender pinned to `LeanShot <noreply@app.leanshot.app>` per D-03 — no sandbox onboarding@resend.dev address anywhere"
  artifacts:
    - path: supabase/functions/_shared/resend-domain-health-check.ts
      provides: "D-03 shared startup gate; reused by 5 lifecycle fns + dsar-export delivery"
      exports: [resendDomainHealthCheck]
    - path: supabase/functions/lifecycle-welcome-series/index.ts
      provides: "Cron-invoked; queries auth.users created in last 4h; renders welcome_immediately, getting_started_day1, first_injection_reminder, week_1_check_in based on age delta"
    - path: supabase/functions/lifecycle-behavior-triggered/index.ts
      provides: "Cron drains a behavior_event_queue table OR computes eligible recipients directly (first_injection_celebration, 7_day_streak, missed_dose_day3)"
    - path: supabase/functions/lifecycle-transactional/index.ts
      provides: "HTTP-invoked; templates receipt, password_reset, dsar_ready, deletion_scheduled; consumed by DSAR Edge Fn + DEL-01 modal + Stripe webhook"
    - path: supabase/functions/lifecycle-retention/index.ts
      provides: "Cron daily; reengagement_7d_inactive, cancellation_winback_30d, weekly_digest (opt-in)"
    - path: supabase/functions/lifecycle-preference-update/index.ts
      provides: "HTTP-invoked from /settings/email-preferences; sync Resend audience contact + UPSERT consent_records.email_preferences"
    - path: supabase/migrations/20270601000017_lifecycle_cron_schedules.sql
      provides: "pg_cron schedules for welcome-series + behavior-triggered + retention (HTTP-invoked Fns have no cron)"
  key_links:
    - from: supabase/functions/lifecycle-*/index.ts
      to: supabase/functions/_shared/resend-domain-health-check.ts
      via: "Every lifecycle fn calls `await resendDomainHealthCheck(admin); if (!health.ok) return jsonResponse(200, {skipped:true});` at startup"
      pattern: "resendDomainHealthCheck"
    - from: supabase/functions/lifecycle-*/index.ts
      to: supabase/functions/clinic-invite/resend.ts
      via: "All 5 reuse direct-HTTPS POST to api.resend.com/emails (no SDK)"
      pattern: "api.resend.com/emails"
    - from: supabase/functions/lifecycle-*/index.ts
      to: public.consent_records.email_preferences
      via: "SELECT email_preferences->>{category} = 'true' before send; skip if disabled"
      pattern: "email_preferences"
---

<objective>
Ship the lifecycle email layer (ON-02): one shared D-03 health-check helper + 5 Edge Functions covering 12 templates + cron schedules. Every Function is gated on Resend domain verification — production-ready before user verifies DNS (zero code change at cutover).

Purpose: Close the lifecycle gap for the entire v1.2 surface (welcome onboarding → behavior reinforcement → transactional ack → retention reactivation). Reuse the Phase 9 `clinic-invite/resend.ts` direct-HTTPS pattern + lazy admin singleton + Proxy from `affiliate-payout`. Templates ship inline-style HTML only (Gmail/Outlook compat per Pitfall 8 + UI-SPEC §Email Templates).

Output: 5 Edge Functions live, 12 templates renderable, cron jobs scheduled, all Deno tests green.
</objective>

<execution_context>
@/Users/karstenhaldan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karstenhaldan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md
@/Users/karstenhaldan/minisite/supabase/functions/clinic-invite/resend.ts
@/Users/karstenhaldan/minisite/supabase/functions/clinic-invite/index.ts
@/Users/karstenhaldan/minisite/supabase/functions/clinic-invite/template-clinic-invite.ts
@/Users/karstenhaldan/minisite/supabase/functions/clinic-invite/deno.json
@/Users/karstenhaldan/minisite/supabase/functions/affiliate-payout/index.ts
@/Users/karstenhaldan/minisite/supabase/functions/_shared/__tests__/resend-domain-health-check.test.ts
@/Users/karstenhaldan/minisite/supabase/functions/lifecycle-welcome-series/index.test.ts
@/Users/karstenhaldan/minisite/supabase/functions/lifecycle-behavior-triggered/index.test.ts
@/Users/karstenhaldan/minisite/supabase/functions/lifecycle-transactional/index.test.ts
@/Users/karstenhaldan/minisite/supabase/functions/lifecycle-retention/index.test.ts
@/Users/karstenhaldan/minisite/supabase/functions/lifecycle-preference-update/index.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author _shared/resend-domain-health-check.ts + 5 lifecycle Edge Functions + cron migration</name>
  <files>
    /Users/karstenhaldan/minisite/supabase/functions/_shared/resend-domain-health-check.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-welcome-series/index.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-welcome-series/templates.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-welcome-series/deno.json,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-behavior-triggered/index.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-behavior-triggered/templates.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-behavior-triggered/deno.json,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-transactional/index.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-transactional/templates.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-transactional/deno.json,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-retention/index.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-retention/templates.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-retention/deno.json,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-preference-update/index.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-preference-update/deno.json,
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000017_lifecycle_cron_schedules.sql
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Pattern 2 lines 412-443 — health check; Pitfall 6, 8 lines 740-769; Common Operation flow lines 529-553),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (Email Templates section lines 392-450 — 12 template table; token-to-inline-CSS mapping table lines 402-417; Layout pattern lines 419-425; Copywriting Contract lines 622-693 for transactional copy),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave B + Wave C sections lines 49-72 + 357-553 — analog file paths + line anchors),
    /Users/karstenhaldan/minisite/supabase/functions/clinic-invite/resend.ts (full 90 lines — direct-HTTPS pattern; env-read; test-stub short-circuit; PII-safe error handling),
    /Users/karstenhaldan/minisite/supabase/functions/clinic-invite/index.ts (Deno serve + bearer-compare against SUPABASE_SERVICE_ROLE_KEY; CORS headers; deno serve handler signature),
    /Users/karstenhaldan/minisite/supabase/functions/clinic-invite/template-clinic-invite.ts (HTML+inline-style template structure to clone),
    /Users/karstenhaldan/minisite/supabase/functions/clinic-invite/deno.json (deno config to copy verbatim),
    /Users/karstenhaldan/minisite/supabase/functions/affiliate-payout/index.ts (lines 30-90 — lazy admin singleton + Proxy pattern; constant-time bearer compare line 12; cron-only fn pattern)
  </read_first>
  <action>
    Implement 5 Edge Functions + shared helper + cron migration.

    Shared helper `_shared/resend-domain-health-check.ts`: export `async function resendDomainHealthCheck(supabase: SupabaseClient): Promise<{ok: boolean; status: string}>` per RESEARCH lines 424-442. Behavior:
    - read `Deno.env.get('RESEND_API_KEY')` → if missing return `{ok:false, status:'no_api_key'}`
    - if `apiKey === 'test-stub'` return `{ok:true, status:'verified'}` (Pitfall 6 — keeps lifecycle tests deterministic)
    - fetch `https://api.resend.com/domains` with `Authorization: Bearer ${apiKey}`
    - find domain where `name === 'app.leanshot.app'`
    - if `domain?.status === 'verified'` return `{ok:true, status:'verified'}`
    - else `await supabase.rpc('increment_resend_domain_unverified_skips');` + `console.warn(...)` + return `{ok:false, status: domain?.status ?? 'not_found'}`
    NEVER echo `res.text()` or exception details (PII safety per `clinic-invite/resend.ts` lines 70-89; T-09-34/37 pattern).

    Each lifecycle Edge Fn skeleton (clone `affiliate-payout/index.ts` lines 30-90 + `clinic-invite/index.ts` for Deno.serve):
    - Imports: `import { createClient } from 'npm:@supabase/supabase-js@2';` + `import type { SupabaseClient } from 'npm:@supabase/supabase-js@2';` + `import { resendDomainHealthCheck } from '../_shared/resend-domain-health-check.ts';` + `import { renderTemplate } from './templates.ts';`
    - Lazy admin singleton with Proxy per analog (do NOT redesign — eager instantiation captures empty env strings at import-time during Deno tests).
    - CORS headers + UUID_RE constant (copy from analog).
    - Constant-time bearer compare against `SUPABASE_SERVICE_ROLE_KEY` for cron-invoked fns (welcome-series, behavior-triggered, retention).
    - HTTP-invoked fns (transactional, preference-update) validate user JWT instead.
    - Per-fn body:
      1. `const health = await resendDomainHealthCheck(admin); if (!health.ok) return jsonResponse(200, {skipped:true, status:health.status});`
      2. Query DB for eligible recipients (per fn-specific logic below).
      3. For each recipient: SELECT `consent_records.email_preferences` to check category opt-in; if disabled → skip.
      4. Render template via `renderTemplate(name, data)` from templates.ts.
      5. POST to `https://api.resend.com/emails` with `{from: Deno.env.get('RESEND_FROM') ?? 'LeanShot <noreply@app.leanshot.app>', to, subject, html, text}`.
      6. Audit-log per send via `admin.from('audit_logs').insert({action_type, actor_id: null, target_user_id: recipient.id, payload: {template, message_id}})`.

    Per-fn eligibility queries (cron schedule + behavior):
    - `lifecycle-welcome-series` (every 4h cron): `select id, email, raw_user_meta_data->>'first_name' as first_name, created_at from auth.users where created_at > now() - interval '8 days' and email_confirmed_at is not null` — partition by age (0-4h → welcome_immediately, 22-26h → getting_started_day1, 70-74h → first_injection_reminder, 166-170h → week_1_check_in); only send if user hasn't received that template before (check audit_logs by template name in payload).
    - `lifecycle-behavior-triggered` (every 15min cron): three queries — (a) users whose first injection was logged in last 15min → first_injection_celebration, (b) users at exactly day-7 streak → 7_day_streak, (c) users with last injection 3 days ago + no logs since → missed_dose_day3. Reuse a small `behavior_event_queue` table OR compute directly via SQL (planner chooses; computation is simpler at v1.2 scale).
    - `lifecycle-transactional` (HTTP-invoked): POST body `{template: 'receipt'|'password_reset'|'dsar_ready'|'deletion_scheduled', user_id, data: {...}}`. Validates the caller has service_role bearer OR an admin user JWT. NO cron. This is consumed by: dsar-export (plan 22-04) for `dsar_ready`, DEL-01 modal (plan 22-05) for `deletion_scheduled`, Stripe webhook (existing P14) for `receipt`, auth flow for `password_reset` (re-skin of existing).
    - `lifecycle-retention` (daily 06:00 UTC cron): three queries — (a) users where last_sign_in_at = now() - 7 days → reengagement_7d_inactive, (b) users with `subscriptions.canceled_at = now() - interval '30 days'` → cancellation_winback_30d, (c) Monday cron only → weekly_digest for users opted-in (email_preferences->>'weekly_digest' = 'true').
    - `lifecycle-preference-update` (HTTP-invoked from preference center in 22-11): POST body `{categories: {welcome: bool, behavior_triggered: bool, retention: bool, weekly_digest: bool, affiliate: bool}}`. Validates user JWT, UPSERTs `consent_records.email_preferences` for `auth.uid()`, optionally syncs Resend audience contact membership (if Resend account ID known via `RESEND_AUDIENCE_ID` env var; skip if unset).

    `templates.ts` per fn: export `renderTemplate(name: string, data: Record<string, unknown>): {subject: string, html: string, text: string}`. HTML structure per UI-SPEC layout pattern lines 419-425 — single 600px-wide table outer wrapper, inline-style on every element using the token-to-inline-CSS mapping (e.g. `style="background-color: #FEFCF7; padding: 32px; ..."`). NO Tailwind classes. NO `<style>` blocks (Pitfall 8). Subject + hero copy + CTA URL per UI-SPEC template table lines 429-442.

    Cron migration `20270601000017_lifecycle_cron_schedules.sql`: `select cron.schedule('lifecycle-welcome-series', '0 */4 * * *', $$ select net.http_post(url:='<SUPABASE_URL>/functions/v1/lifecycle-welcome-series', headers:=jsonb_build_object('Authorization', 'Bearer ' || (select decrypted_secret from vault.decrypted_secrets where name='service_role_key' limit 1))); $$);` analogous to existing `affiliate-payout` cron pattern in `20270101000012_payouts_materialization_and_cron.sql`. Three schedules: welcome-series every 4h, behavior-triggered every 15 min (`*/15 * * * *`), retention daily 06:00 UTC (`0 6 * * *`). NO cron for transactional or preference-update. Header comment cites D-03 + Vault `service_role_key` dependency (from Phase 19 deferred vendor pass — if Vault key missing, cron POST returns 401 but the gated-send pattern still keeps prod safe).

    `deno.json` per fn: identical to `supabase/functions/affiliate-payout/deno.json` (verbatim copy). Importer bundler ignores `import_map.json` per `reference_supabase_edge_function_deploy.md`.

    Per `feedback_parallel_executor_git_isolation.md`: use `git commit -- <pathspec>` when committing — do NOT `git add -A` since this plan touches 16 files in parallel-eligible Wave 1.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; ls supabase/functions/_shared/resend-domain-health-check.ts supabase/functions/lifecycle-welcome-series/index.ts supabase/functions/lifecycle-welcome-series/templates.ts supabase/functions/lifecycle-welcome-series/deno.json supabase/functions/lifecycle-behavior-triggered/index.ts supabase/functions/lifecycle-behavior-triggered/templates.ts supabase/functions/lifecycle-behavior-triggered/deno.json supabase/functions/lifecycle-transactional/index.ts supabase/functions/lifecycle-transactional/templates.ts supabase/functions/lifecycle-transactional/deno.json supabase/functions/lifecycle-retention/index.ts supabase/functions/lifecycle-retention/templates.ts supabase/functions/lifecycle-retention/deno.json supabase/functions/lifecycle-preference-update/index.ts supabase/functions/lifecycle-preference-update/deno.json supabase/migrations/20270601000017_lifecycle_cron_schedules.sql | wc -l | grep -q "^16$" &amp;&amp; grep -v '^[[:space:]]*//' supabase/functions/lifecycle-welcome-series/templates.ts supabase/functions/lifecycle-behavior-triggered/templates.ts supabase/functions/lifecycle-transactional/templates.ts supabase/functions/lifecycle-retention/templates.ts | grep -c '<style' | grep -q "^0$"</automated>
  </verify>
  <done>All 16 files exist; zero `<style` tags in templates (inline-only per Pitfall 8); cron migration registers 3 schedules.</done>
</task>

<task type="auto">
  <name>Task 2: Turn Wave-0 Deno test scaffolds green (6 fns × 1 test file each)</name>
  <files>
    /Users/karstenhaldan/minisite/supabase/functions/_shared/__tests__/resend-domain-health-check.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-welcome-series/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-behavior-triggered/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-transactional/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-retention/index.test.ts,
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-preference-update/index.test.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/supabase/functions/affiliate-payout/index.test.ts (full — env-stub + lazy admin import pattern; mock supabase queries via stub),
    /Users/karstenhaldan/minisite/supabase/functions/clinic-invite/index.test.ts (Resend test-stub assertion pattern),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Validation Architecture lines 1052-1054 — ON-02 test contract: each fn: health-check unverified → 200 + counter++, verified → send)
  </read_first>
  <action>
    Replace Wave-0 `it.skip` placeholders with green tests per the Nyquist contract. Each test file should cover at minimum:

    For `_shared/resend-domain-health-check.test.ts`:
    - "returns {ok:true} when RESEND_API_KEY === 'test-stub'" — set env stub, assert
    - "returns {ok:false, status:'no_api_key'} when env unset"
    - "increments counter + warns + returns {ok:false} when domain status !== 'verified'" — mock `fetch` to return `{data: [{name:'app.leanshot.app', status:'pending'}]}` + mock supabase.rpc spy
    - "returns {ok:true} when domain status === 'verified'"

    For each `lifecycle-{name}/index.test.ts`:
    - "skips send when health check fails (Resend domain unverified) — returns 200 + skipped:true + no Resend HTTP call"
    - "sends and logs audit row when health check passes" (use `RESEND_API_KEY=test-stub` so no actual fetch happens; assert internal flow via spy on the supabase.from('audit_logs').insert call)
    - "respects consent_records.email_preferences — skips when category disabled"
    - Plus fn-specific: for welcome-series, "selects correct template based on user age bucket"; for transactional, "validates template name allowlist"; for preference-update, "UPSERTs consent_records.email_preferences for auth.uid()"

    Use the lazy admin singleton + Proxy pattern from `affiliate-payout/index.test.ts` so importing the subject fn doesn't crash when env is empty. Mock `fetch` globally to avoid real HTTP. Mock `Deno.env.get` via test-scoped stubs.

    Run command for verification: `cd /Users/karstenhaldan/minisite/supabase/functions &amp;&amp; deno test --allow-all _shared/__tests__/resend-domain-health-check.test.ts lifecycle-welcome-series/index.test.ts lifecycle-behavior-triggered/index.test.ts lifecycle-transactional/index.test.ts lifecycle-retention/index.test.ts lifecycle-preference-update/index.test.ts`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/supabase/functions &amp;&amp; deno test --allow-all _shared/__tests__/resend-domain-health-check.test.ts lifecycle-welcome-series/index.test.ts lifecycle-behavior-triggered/index.test.ts lifecycle-transactional/index.test.ts lifecycle-retention/index.test.ts lifecycle-preference-update/index.test.ts 2&gt;&amp;1 | tail -3 | grep -E "ok \\| [1-9]"</automated>
  </verify>
  <done>All 6 Deno test files run green: zero `it.skip` remaining for ON-02 surfaces; counter increment + audit log + consent gating all asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy 5 lifecycle Edge Functions + apply migration 17 to remote</name>
  <files>(deployment-only; no source files changed)</files>
  <read_first>
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/reference_supabase_edge_function_deploy.md (bundler ignores import_map.json — use esm.sh; gateway Content-Type override; verify_jwt default; UAT-probe pattern for Function Secrets),
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/reference_resend_phase9_wiring.md (RESEND_FROM expected value + domain-verify check),
    /Users/karstenhaldan/minisite/supabase/functions/lifecycle-welcome-series/deno.json (post-Task 1)
  </read_first>
  <action>
    From `/Users/karstenhaldan/minisite/`:
    1. Apply migration: `supabase db push --linked --dry-run 2>&1 | grep -E "^Skipping" &&  echo FAIL-DRY-RUN-HAS-SKIPS && exit 1; supabase db push --linked` (expect 1 new migration applied).
    2. Deploy each of the 5 Edge Functions: `supabase functions deploy lifecycle-welcome-series --no-verify-jwt false`, `supabase functions deploy lifecycle-behavior-triggered --no-verify-jwt false`, `supabase functions deploy lifecycle-transactional`, `supabase functions deploy lifecycle-retention --no-verify-jwt false`, `supabase functions deploy lifecycle-preference-update`. (transactional + preference-update keep `verify_jwt = true` for user-token validation; the cron-invoked three use service-role bearer compare so `verify_jwt = false` to allow the cron's net.http_post to invoke them.) Note: `--no-verify-jwt` syntax may not exist — the correct mechanism is `deno.json` config `verify_jwt` field. If CLI flag is wrong, set in each deno.json instead.
    3. Verify each deploy via curl smoke that exercises the health-check skip path (RESEND_API_KEY=test-stub if available, else real key + expect skip on unverified domain): `curl -X POST -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" "$SUPABASE_URL/functions/v1/lifecycle-welcome-series"` → expect 200 + `{"skipped": true|false}`.
    4. Verify cron schedules registered: `supabase db query --linked "select jobname, schedule from cron.job where jobname in ('lifecycle-welcome-series','lifecycle-behavior-triggered','lifecycle-retention');"` → expect 3 rows.

    DO NOT verify any actual email delivery (Resend domain still pending per D-03 + deferred-from-P19 vendor pass; the skip path IS the intended behavior at this point).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; supabase functions list 2&gt;/dev/null | grep -c "^lifecycle-" | grep -q "^5$"</automated>
  </verify>
  <done>5 lifecycle Fns deployed + migration 17 live + 3 cron rows present in cron.job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → Edge Fn | pg_cron net.http_post uses Vault `service_role_key` bearer; lifecycle fn validates bearer with constant-time compare |
| Edge Fn → Resend | direct HTTPS to api.resend.com with Bearer RESEND_API_KEY; never logs response body |
| user → /settings/email-preferences → lifecycle-preference-update | user JWT validated; UPSERT scoped to auth.uid() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-11 | S | cron-invoked lifecycle fns called by arbitrary attacker | mitigate | Constant-time bearer compare against SUPABASE_SERVICE_ROLE_KEY (analog affiliate-payout line 12); reject with 401 otherwise |
| T-22-12 | T | Resend hijack via leaked API key | accept | API key in Supabase Function secret (not source); rotate via Resend dashboard if leaked; from: pinned to noreply@app.leanshot.app |
| T-22-13 | I | Resend API error messages leak user emails to logs/Sentry | mitigate | NEVER echo `res.text()` or exception messages; wrap as `{error: 'resend_<status>'}` per clinic-invite/resend.ts lines 70-89 pattern |
| T-22-14 | I | Lifecycle email sent to user who unsubscribed | mitigate | SELECT consent_records.email_preferences before each send; skip if category disabled |
| T-22-15 | D | Resend 429 rate limit kills test suite | mitigate | Pitfall 6 — RESEND_API_KEY=test-stub short-circuit in helper + all 5 lifecycle fns; deterministic test path |
| T-22-16 | T | Gmail strips <style> blocks → unstyled emails reach user | mitigate | Pitfall 8 — templates.ts uses inline `style="..."` on every element only; no `<style>` tags; lint check in Task 1 verify |
| T-22-17 | I | Resend domain unverified → emails sent from sandbox could leak app brand | mitigate | D-03 gated-send pattern (Pattern 2); health-check returns ok:false → fn 200s without send + counter++ + Sentry breadcrumb until DNS verified |
</threat_model>

<verification>
- 5 lifecycle Edge Fns deployed on `ytnsipxxmzgaebkqmokp` (`supabase functions list` shows them).
- `_shared/resend-domain-health-check.ts` exists + 6 Deno tests green.
- All 12 templates renderable via `renderTemplate(name, data)`; zero `<style>` blocks in any template (Pitfall 8 sweep).
- 3 cron schedules live: welcome-series every 4h, behavior-triggered every 15min, retention daily 06:00 UTC.
- transactional + preference-update have NO cron entry (HTTP-only).
- Curl smoke per fn returns 200 (skipped:true if Resend domain still unverified — that's the gated-send path proving correct behavior per D-03).
</verification>

<success_criteria>
- ON-02 lifecycle infrastructure complete — all 5 Fns deployed, 12 templates renderable, sender pinned to `noreply@app.leanshot.app`.
- D-03 health-check gates every send: zero accidental sends from unverified domain.
- Deno test suite for lifecycle fns green (run command in task verify).
- VALIDATION.md per-task rows updated for ON-02 entries with Plan 22-02 in the Plan column.
- Resend domain verify remains a P22-closeout vendor pass — documented in 22-02-SUMMARY.md.
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-02-SUMMARY.md` per template. Include: deployed Fns + version IDs, cron job names + schedules, Resend domain status (still pending vs verified), Deno test result counts.
</output>
