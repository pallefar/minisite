---
phase: 22
plan: 12
type: execute
wave: 3
depends_on: [02, 03, 04, 05, 06, 07, 08, 09, 10, 11]
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06, ADMIN-08, DEL-01, DEL-02, GDPR-01, GDPR-02, GDPR-03, ON-02, ON-03]
requirements_addressed: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06, ADMIN-08, DEL-01, DEL-02, GDPR-01, GDPR-02, GDPR-03, ON-02, ON-03]
files_modified:
  - leanshot/src/App.tsx
  - leanshot/src/components/layout/AppShell.tsx
  - leanshot/e2e/rls-audit-logs-impersonation.test.ts
  - leanshot/e2e/rls-feature-flag-overrides.test.ts
  - leanshot/e2e/rls-consent-records.test.ts
  - leanshot/e2e/rls-dsar-requests.test.ts
  - leanshot/e2e/rls-dsar-exports-storage.test.ts
  - leanshot/e2e/admin-impersonation-write-deny.test.ts
  - leanshot/e2e/cron-finalize-7day.test.ts
  - leanshot/e2e/cookie-consent.spec.ts
  - leanshot/e2e/cookie-consent-geo.spec.ts
  - leanshot/e2e/posthog-defer.spec.ts
  - leanshot/e2e/lifecycle-welcome-series.spec.ts
  - leanshot/e2e/dsar-export.spec.ts
  - leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-VALIDATION.md

must_haves:
  truths:
    - "App.tsx mounts CookieConsentBootstrap + ImpersonationBanner + SoftDeleteCountdownBanner at root (priority: impersonation wins per UI-SPEC line 317)"
    - "App.tsx adds React.lazy routes for /admin/* + /settings/privacy/dsar + /settings/email-preferences + /cancel-deletion"
    - "AppShell main content top-padding shifts pt-12 when banner mounted (per UI-SPEC line 55)"
    - "useImpersonationReadOnly() props spread on writable surfaces: at minimum all major Form CTAs (Save preferences, Save settings, Submit logs, etc.) — best-effort sweep, not exhaustive (RLS write-deny is the security boundary anyway)"
    - "10 e2e/RLS tests turned green (turn ON the Wave-0 it.skip placeholders): rls-audit-logs-impersonation, rls-feature-flag-overrides, rls-consent-records, rls-dsar-requests, rls-dsar-exports-storage, admin-impersonation-write-deny, cron-finalize-7day, cookie-consent.spec, cookie-consent-geo.spec, posthog-defer.spec, lifecycle-welcome-series.spec, dsar-export.spec"
    - "Each RLS test uses its file-scoped slug prefix constant (per PATTERNS S5 + Pre-emptive warning 8)"
    - "admin-impersonation-write-deny test: loops through 17 trigger-tables × INSERT/UPDATE/DELETE = 51 attempts; all return 42501 when JWT app_metadata.impersonator_id set; same JWT without impersonator_id succeeds (control)"
    - "VALIDATION.md per-task rows fully populated (Task ID, Plan, Wave columns); status flipped to ✅ green for each REQ-ID with passing automated command; nyquist_compliant frontmatter STAYS false (verifier owns the final flip per VALIDATION sign-off rule)"
    - "feature_flag_overrides loadOverrides(user.id) wired into App.tsx post-auth hook"
    - "Final bundle check: index gz < 50 kB ceiling (regression detection)"
  artifacts:
    - path: leanshot/src/App.tsx
      provides: "Final wiring of 3 banners + 4 new routes + loadOverrides + lazy route registration"
    - path: leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-VALIDATION.md
      provides: "Per-task rows populated with Plan + Wave columns; status ✅ per passing REQ-ID"
  key_links:
    - from: leanshot/src/App.tsx
      to: leanshot/src/components/impersonation/ImpersonationBanner.tsx + SoftDeleteCountdownBanner + CookieConsentBootstrap
      via: "Conditional mount at AppShell root with priority logic"
      pattern: "<ImpersonationBanner|SoftDeleteCountdownBanner|CookieConsentBootstrap"
    - from: leanshot/e2e/admin-impersonation-write-deny.test.ts
      to: supabase/migrations/20270601000012_impersonation_write_deny_policies.sql
      via: "Test asserts the 51 deny policies effectively block writes; pass = RLS shipped correctly"
      pattern: "deny_writes_during_impersonation"
---

<objective>
Land the Wave 3 integration: AppShell mount of all 3 overlays + App.tsx route registration + 12 e2e/RLS tests turned green + VALIDATION.md per-task rows populated.

Purpose: This is the closeout — wires everything together + proves the security invariants via cross-tenant RLS tests + closes the validation contract (minus the nyquist_compliant flag, which verifier owns).

Output: App.tsx integrated; 12 e2e/RLS tests green; VALIDATION.md populated.
</objective>

<execution_context>
@/Users/karstenhaldan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karstenhaldan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-VALIDATION.md
@/Users/karstenhaldan/minisite/leanshot/src/App.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/layout/AppShell.tsx
@/Users/karstenhaldan/minisite/leanshot/e2e/rls-audit-logs.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: HEAD-reset + App.tsx integration (mount 3 banners + lazy routes + loadOverrides)</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/App.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/layout/AppShell.tsx
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/App.tsx (current root component + lazy route pattern),
    /Users/karstenhaldan/minisite/leanshot/src/components/layout/AppShell.tsx (current sticky header + main wrapper),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (banner mount specs lines 286, 316-318, 327; bundle budget lines 754-764)
  </read_first>
  <action>
    HEAD-reset verify all Wave 2 outputs exist:
    `test -f /Users/karstenhaldan/minisite/leanshot/src/components/impersonation/ImpersonationBanner.tsx && test -f /Users/karstenhaldan/minisite/leanshot/src/components/soft-delete/SoftDeleteCountdownBanner.tsx && test -f /Users/karstenhaldan/minisite/leanshot/src/components/consent/CookieConsentBootstrap.tsx && test -f /Users/karstenhaldan/minisite/leanshot/src/components/admin/AdminLayout.tsx && test -f /Users/karstenhaldan/minisite/leanshot/src/components/dsar/DsarPortalPage.tsx && test -f /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/settings/EmailPreferencesPage.tsx && test -f /Users/karstenhaldan/minisite/leanshot/src/pages/cancel-deletion.tsx || exit 1`

    Modify `App.tsx`:
    - Add React.lazy imports: `const AdminLayout = lazy(() => import('@/components/admin/AdminLayout')); const DsarPortalPage = lazy(() => import('@/components/dsar/DsarPortalPage')); const EmailPreferencesPage = lazy(() => import('@/components/dashboard/settings/EmailPreferencesPage')); const CancelDeletionPage = lazy(() => import('@/pages/cancel-deletion'));`
    - Project intentionally has NO router (per CLAUDE.md "Routing: Intentionally none. All navigation is `setTab(...)` against the store."). For /admin/* + /settings/privacy/dsar etc., extend the existing pathname-switch pattern (in src/App.tsx the view selector reads from store; add a `currentRoute` Zustand selector OR direct read of window.location.pathname with re-render on hashchange). Use `useEffect` + popstate listener for SPA navigation.
    - Top-level conditional: if pathname starts with `/admin` and user.is_staff → render `<Suspense fallback={<Skeleton />}><AdminLayout /></Suspense>`; if `/settings/privacy/dsar` → `<DsarPortalPage />`; if `/settings/email-preferences` → `<EmailPreferencesPage />`; if `/cancel-deletion` → `<CancelDeletionPage />`; else existing AppShell + tab dashboard.
    - Mount 3 overlays at root (outside route switch so they show on every page): `<ImpersonationBanner /> <SoftDeleteCountdownBanner /> <CookieConsentBootstrap />`. Per UI-SPEC priority line 317: SoftDeleteCountdownBanner's own logic already checks `useImpersonation().active` and returns null when impersonating, so no extra parent-level coordination needed.
    - Post-auth hook: after `useStore((s) => s.user)` becomes truthy, call `void loadOverrides(user.id)` (from `@/lib/consent/feature-flag-overrides`) — wire as a useEffect on user.id change.
    - On signOut: call `clearOverrideCache()` to prevent leakage.

    Modify `AppShell.tsx`:
    - When a banner is mounted at root (ImpersonationBanner OR SoftDeleteCountdownBanner), AppShell's main content area shifts `pt-12` to clear the 48px tall banner (per UI-SPEC line 55). Use a context OR Zustand `bannerHeight` slice OR direct DOM read; simplest: pass a prop down from App.tsx based on `useImpersonation().active || useSoftDeleteStatus().active`.

    Per CLAUDE.md ESLint: no `useStore(generateInsights|pickFocus)` patterns and no useStore selector calling factories.

    Per Pre-emptive warning 4 (shared-file choreography): App.tsx is touched ONLY in this plan during Wave 3 (Wave 2 plans 22-06/22-09/22-10/22-11 deferred all App.tsx mount-wiring to here). Confirm via `git log src/App.tsx --since="<wave-1-start>"` shows no prior Wave 2 commits to App.tsx.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm run build 2&gt;&amp;1 | tail -5 | grep -qE "built|✓"</automated>
  </verify>
  <done>App.tsx mounts 3 overlays + 4 new lazy routes + loadOverrides wired; build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn green 5 RLS test files (rls-audit-logs-impersonation, rls-feature-flag-overrides, rls-consent-records, rls-dsar-requests, rls-dsar-exports-storage)</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-audit-logs-impersonation.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-feature-flag-overrides.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-consent-records.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-dsar-requests.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-dsar-exports-storage.test.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-audit-logs.test.ts (full file — env-gating + two-user setup + storageKey uniqueness pattern; lines 30-100),
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/feedback_rls_per_file_slug_prefix.md (per-file slug prefix rule),
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/reference_rls_fixture_gotrueclient_flake.md (supabase-js v2.105 flake; service-role-minted JWT pattern as fallback if signInWithPassword cross-contaminates)
  </read_first>
  <behavior>
    - rls-audit-logs-impersonation: user A INSERTs audit_logs.impersonator_id = userA → user B cannot read → user A can read; SELECT scope correct.
    - rls-feature-flag-overrides: user A SELECTs own overrides → succeeds; user A attempts INSERT/UPDATE directly → fails (RLS has no authenticated write policy); admin_set_feature_flag_override RPC as is_staff → succeeds.
    - rls-consent-records: anonymous insert with anonymous_id succeeds (RLS allows user_id=null); user A SELECTs own consent records → succeeds; user B cannot read user A's records.
    - rls-dsar-requests: create_dsar_request RPC as user A → row inserted with user_id=auth.uid(); user A SELECTs own → succeeds; user B cannot SELECT user A's.
    - rls-dsar-exports-storage: service-role uploads file to dsar-exports/{userA-id}/test.zip; user A SELECTs storage.objects WHERE bucket_id='dsar-exports' → can list own folder; user B SELECTs → empty.
  </behavior>
  <action>
    Replace Wave 0 `it.skip` scaffolds with full assertions. For each file:
    - Reuse `rls-audit-logs.test.ts` two-user setup pattern (lines 62-99): admin.auth.admin.createUser for emailA + emailB with file-scoped slug prefix (PATTERNS S5).
    - Each file declares its own prefix constant from plan 22-01 scaffold (already in place; just expand the test bodies).
    - Storage key uniqueness per file (reference_rls_fixture_gotrueclient_flake.md): `storageKey: '${PREFIX}-a'` etc.
    - If signInWithPassword cross-contaminates (a known flake), fall back to service-role-minted JWT via `headers.Authorization` per memory reference.
    - Cleanup: afterAll deletes both created users via `admin.auth.admin.deleteUser(uid)`.
    - env-gate via `describeIfLive = SHOULD_RUN ? describe : describe.skip`.

    For rls-dsar-exports-storage: service-role uploads test bytes to `dsar-exports/{user_a_id}/test-{timestamp}.zip` before testing; cleanup deletes the test object too.

    Run via `npm run test:e2e:rls -- rls-audit-logs-impersonation rls-feature-flag-overrides rls-consent-records rls-dsar-requests rls-dsar-exports-storage`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm run test:e2e:rls -- rls-audit-logs-impersonation rls-feature-flag-overrides rls-consent-records rls-dsar-requests rls-dsar-exports-storage 2&gt;&amp;1 | tail -5 | grep -qE "passed|skipped"</automated>
  </verify>
  <done>5 RLS tests run green or skipped (env-gated); per-file slug prefix constants honored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Turn green remaining 7 e2e tests (impersonation-write-deny, cron-finalize-7day, cookie-consent, cookie-consent-geo, posthog-defer, lifecycle-welcome-series, dsar-export)</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/e2e/admin-impersonation-write-deny.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/cron-finalize-7day.test.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/cookie-consent.spec.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/cookie-consent-geo.spec.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/posthog-defer.spec.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/lifecycle-welcome-series.spec.ts,
    /Users/karstenhaldan/minisite/leanshot/e2e/dsar-export.spec.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/e2e/rls-multi-table.test.ts (multi-table loop pattern),
    /Users/karstenhaldan/minisite/leanshot/e2e/account-delete.spec.ts (Playwright e2e pattern for backend cascade tests),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md (Wave G analog notes per file),
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/reference_playwright_state_seeding.md (addInitScript over goto+evaluate),
    /Users/karstenhaldan/.claude/projects/-Users-karstenhaldan-minisite/memory/feedback_realtime_layer_e2e_pattern.md (Pre-emptive warning 18 — realtime broadcast tests use channel.subscribe() not UI traversal)
  </read_first>
  <behavior>
    - admin-impersonation-write-deny: loops 17 tables × 3 operations = 51 attempts; JWT with app_metadata.impersonator_id → all 51 return 42501; same JWT without claim → all 51 succeed.
    - cron-finalize-7day: create test user → initiate_account_deletion → back-date `initiated_at = now() - interval '8 days'` → invoke `run_finalize_account_deletions_cron_now()` → assert auth.users row gone.
    - cookie-consent.spec: page.goto('/') → assert banner `fixed bottom-0` visible after first paint + `lucide:Cookie` icon visible + 3 buttons Reject/Customize/Accept present; click Accept all → assert `cc_cookie` cookie exists in browser context.
    - cookie-consent-geo.spec: `?force_geo=eu` query param (helper added in plan 22-10 if needed for testing) → assert Analytics toggle in Customize defaults Off; `?force_geo=us` → Analytics defaults On.
    - posthog-defer.spec: page.goto('/') → assert window.posthog === undefined; click Accept all → wait for analytics dynamic-import → assert window.posthog is defined.
    - lifecycle-welcome-series.spec: RESEND_API_KEY=test-stub set → POST to lifecycle-welcome-series Edge Fn → assert response has `skipped: false` and no real Resend HTTP call made (mocked).
    - dsar-export.spec: create test user → submit DSAR via DsarPortalPage → poll dsar_requests.status (or wait for cron tick) → status becomes 'completed' → click Download bundle → asserts signed URL returns 200 + Content-Type is application/zip.
  </behavior>
  <action>
    For each test file: replace `test.skip`/`it.skip` placeholders with full assertions.

    admin-impersonation-write-deny: loop over array of 17 tables (injections, weights, meals, workouts, supplements, mood, sleep, symptoms, vials, settings, photos, ai_messages, shares, consent_records, pending_account_deletions, feature_flag_overrides, dsar_requests); for each table × {INSERT, UPDATE, DELETE} attempt the op with a JWT that has `app_metadata.impersonator_id` set (mint via admin.updateUserById + admin.generateLink); assert response code 42501. Per-file slug prefix per Pattern S5.

    cron-finalize-7day: Phase 7 test-hook RPC `run_finalize_account_deletions_cron_now()` is now updated to use 7 days per plan 22-01 Task 1. Test creates user → initiates deletion → back-dates initiated_at via UPDATE → invokes the cron-now RPC → asserts user is gone.

    cookie-consent.spec / cookie-consent-geo.spec / posthog-defer.spec: Playwright tests with addInitScript (per `reference_playwright_state_seeding.md` — never goto+evaluate). For geo tests, add a `?force_geo=eu|us` query helper in `consent-config.ts` (modify in this task) that overrides `window.__VERCEL_GEO__.country` for testing.

    lifecycle-welcome-series.spec: invoke Edge Fn directly with curl (NOT via UI) — Playwright test runs `fetch` against the function endpoint with test-stub key.

    dsar-export.spec: full UI flow — page.addInitScript for session seed, navigate to /settings/privacy/dsar, click Export, click Confirm, poll status. Wait for cron tick (5 min) is too slow for CI — alternative: invoke dsar-export Edge Fn directly with service-role bearer after creating the request, mimicking what the cron does.

    Run via `npx playwright test e2e/{cookie-consent,cookie-consent-geo,posthog-defer,lifecycle-welcome-series,dsar-export}.spec.ts` + `npm run test:e2e:rls -- admin-impersonation-write-deny cron-finalize-7day`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npx playwright test --reporter=line e2e/cookie-consent.spec.ts e2e/cookie-consent-geo.spec.ts e2e/posthog-defer.spec.ts e2e/lifecycle-welcome-series.spec.ts e2e/dsar-export.spec.ts 2&gt;&amp;1 | tail -5 | grep -qE "passed|skipped"</automated>
  </verify>
  <done>7 e2e tests run green or env-gated skipped.</done>
</task>

<task type="auto">
  <name>Task 4: Populate VALIDATION.md per-task rows + final bundle check</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-VALIDATION.md
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-VALIDATION.md (current state — Task ID / Plan / Wave columns are 'TBD'),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-01-SUMMARY.md through 22-11-SUMMARY.md (each plan's summary; pull test names + Plan IDs from SUMMARYs)
  </read_first>
  <action>
    Populate the "Per-Task Verification Map" table in `22-VALIDATION.md`:
    - For each REQ-ID row: fill Task ID (concrete task name from the originating plan, e.g., `22-06-T2`), Plan (e.g., `22-06`), Wave (e.g., `2`).
    - Change `File Exists` column from `❌ W0` to `✓ landed` for each test file created across plans 22-01..22-11.
    - Change `Status` column from `⬜ pending` to `✅ green` for REQ-IDs where the automated command (from RESEARCH §Validation Architecture) passes.
    - LEAVE `nyquist_compliant: false` in frontmatter — that flag is verifier-owned per VALIDATION.md sign-off rule line 102 + Pre-emptive warning 6 (planner must NOT flip).

    Add a closing summary section "Plan-by-Plan Coverage":
    | Plan | REQ-IDs | Wave | Tests added | Tests green |
    |------|---------|------|-------------|-------------|
    | 22-01 | (Wave 0 setup — covers all 13) | 0 | 35 scaffolds | (turned on in later plans) |
    | 22-02 | ON-02 | 1 | 6 Deno | 6 |
    | ... etc.

    Final bundle check: `cd leanshot && npm run build && gzip -c dist/assets/index-*.js | wc -c` → assert < 50000 (50 kB ceiling). Record actual value in 22-12-SUMMARY.md.

    Verify all 14 REQ-IDs (ADMIN-01..06, 08, DEL-01, 02, GDPR-01, 02, 03, ON-02, 03) appear in at least one plan's `requirements_addressed` frontmatter field via:
    `grep -h "requirements_addressed:" /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-*-PLAN.md | tr ',' '\n' | sort -u`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; grep -c "✅ green\\|✓ landed" .planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-VALIDATION.md | awk '$1 &gt; 10 {print "OK"; exit 0} {exit 1}'</automated>
  </verify>
  <done>VALIDATION.md per-task rows populated; nyquist_compliant still false (verifier owns); bundle < 50 kB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppShell mount order → banner priority | impersonation > soft-delete (UI-SPEC line 317); cookie banner is non-blocking, mounts separately |
| App.tsx route switch → admin gate | AdminLayout's is_staff gate is the second line of defense after server-side RPC gate |
| RLS test fixtures → live DB | Each test creates + deletes its own users via admin API; concurrent CI runs accumulate orphans only if cleanup hooks crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-67 | T | App.tsx mount priority bug: impersonation + soft-delete banner both show | mitigate | SoftDeleteCountdownBanner's own logic returns null if useImpersonation().active; tested in plan 22-05 |
| T-22-68 | I | RLS test fixtures leak users across parallel test files | mitigate | Per-file slug prefix (Pattern S5) prevents cleanup hook collisions; verified across all 5 new RLS files |
| T-22-69 | E | admin-impersonation-write-deny test passes but RLS policy missing on a new table added post-P22 | accept | Audit at start of every future schema-extending phase; v1.2 closeout assumes 17 tables is the complete set |
| T-22-70 | D | RLS test fixtures slow CI | accept | env-gated via `describeIfLive`; CI runs against test project, not main |
| T-22-71 | E | nyquist_compliant frontmatter accidentally flipped to true by planner | mitigate | Pre-emptive warning 6 explicit; verifier owns the flip per VALIDATION sign-off rule |
| T-22-72 | T | Bundle regression from vanilla-cookieconsent or jspdf static import | mitigate | Task 4 measures gz of index chunk; CI bundle-budget script enforces (existing scripts/assert-clinic-bundle-budget.sh — though Bundle-budget script hash-hyphen bug per project memory may still need Phase 23 fix) |
</threat_model>

<verification>
- 12 e2e/RLS tests turned green or skipped.
- App.tsx mounts 3 overlays + 4 lazy routes.
- VALIDATION.md per-task rows fully populated; nyquist_compliant unchanged at false.
- Bundle index gz < 50 kB.
- All 14 REQ-IDs appear in at least one plan's `requirements_addressed` (verified via grep).
- Audit trail: every D-NN decision from CONTEXT.md cited in at least one plan's must_haves (D-01 plan 22-01 + 22-05; D-02 carved out, NO plan touches onboarding; D-03 plan 22-02; D-04 plan 22-01 + 22-08; D-05 plan 22-01 + 22-03 + 22-09; D-06 plan 22-04; D-07 plan 22-10; D-08 plan 22-01 + 22-06).
</verification>

<success_criteria>
- Phase 22 v1.2 surface end-to-end live for owner + patient.
- 14 REQ-IDs covered; 13 D-NN decisions implemented (D-02 explicitly excluded per CONTEXT — onboarding deferred to P22b).
- VALIDATION.md ready for `/gsd:verify-work` to flip nyquist_compliant flag.
- Vendor pass list documented in 22-12-SUMMARY (Resend domain DNS, PostHog PERSONAL_API_KEY, Vault service_role_key if Phase 19 deferred is still pending, IMPERSONATION_JWT_SIGNING_KEY if A1 FAIL branch).
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-12-SUMMARY.md` per template. Include:
- App.tsx integration trace (banners + routes + loadOverrides wiring),
- 12 e2e/RLS test results,
- VALIDATION.md update summary (% of rows populated, % green),
- Final bundle gz (index + new lazy chunks),
- D-NN decision coverage table (all 8 D-NN ↔ Plan owner mapping),
- 14 REQ-IDs coverage table (REQ-ID ↔ Plan owner mapping),
- Vendor pass status checklist (DNS, PostHog key, Vault keys),
- Open follow-ups for Phase 23 (clinic-invite retrofit to D-03 health check; v1.3 deferrals).
</output>
