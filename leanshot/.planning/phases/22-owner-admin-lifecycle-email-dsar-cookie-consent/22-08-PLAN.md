---
phase: 22
plan: 08
type: execute
wave: 2
depends_on: [01]
autonomous: true
requirements: [ADMIN-02, ADMIN-08]
requirements_addressed: [ADMIN-02, ADMIN-08]
files_modified:
  - leanshot/src/components/admin/pages/AdminMetricsPage.tsx
  - leanshot/src/components/admin/AdminMetricsKpiStrip.tsx
  - leanshot/src/components/admin/AdminMetricsMrrChart.tsx
  - leanshot/src/components/admin/AdminMetricsClinicSeatList.tsx
  - leanshot/src/components/admin/pages/AdminCohortsPage.tsx
  - leanshot/src/components/admin/cohorts/CohortHeatmap.tsx
  - leanshot/src/lib/admin/admin-metrics.ts
  - leanshot/src/components/admin/cohorts/__tests__/CohortHeatmap.test.tsx
  - supabase/migrations/20270601000020_admin_metrics_rpcs.sql

must_haves:
  truths:
    - "AdminMetricsPage renders 4 KPI tiles (MRR, ARR, 30d churn, clinic-seat utilization Sparkline) per UI-SPEC §/admin/metrics"
    - "MRR/ARR computed via admin_compute_mrr_arr() RPC reading public.tier_effective view (P19) — NOT raw subscriptions.status (cross-phase contract)"
    - "Main chart uses existing <BaseChart> primitive (chart.js, already bundled) — free-vs-paid stacked bars + churn-rate line overlay"
    - "AdminCohortsPage renders /admin/cohorts with weekly cohort × 91-day heatmap from public.cohort_retention view"
    - "CohortHeatmap uses CSS-grid (NO new chart primitive) per UI-SPEC line 279 — `grid grid-cols-[120px_repeat(91,_minmax(8px,_1fr))]`"
    - "K-anonymity guard: cells with active_users < 10 render as '<10' instead of exact count (T-22-10 mitigation; per ASSUMED security threat)"
    - "Each cell uses inline style `background-color: color-mix(in srgb, var(--color-primary) {N}%, var(--color-surface))` where N = retention_pct (D-04)"
    - "13 weeks visible by default; 'Show all weeks' toggle expands to full history (UI-SPEC line 278)"
    - "Both pages mount under AdminLayout's is_staff gate (Pattern S1 dual-layer)"
    - "Period filter Pill (7d / 30d / 90d / 12m) drives admin_compute_mrr_arr p_period parameter"
  artifacts:
    - path: leanshot/src/components/admin/pages/AdminMetricsPage.tsx
      provides: "ADMIN-02 /admin/metrics"
    - path: leanshot/src/components/admin/cohorts/CohortHeatmap.tsx
      provides: "ADMIN-08 CSS-grid heatmap with k-anonymity"
    - path: supabase/migrations/20270601000020_admin_metrics_rpcs.sql
      provides: "admin_compute_mrr_arr + admin_get_cohort_retention RPCs (server-paginated)"
  key_links:
    - from: leanshot/src/components/admin/AdminMetricsMrrChart.tsx
      to: public.tier_effective view (Phase 19)
      via: "admin_compute_mrr_arr() RPC body joins on tier_effective NOT raw subscriptions.tier"
      pattern: "tier_effective"
    - from: leanshot/src/components/admin/cohorts/CohortHeatmap.tsx
      to: public.cohort_retention view (Wave 0 migration 10)
      via: "admin_get_cohort_retention(p_weeks) RPC returns rows from cohort_retention"
      pattern: "cohort_retention"
---

<objective>
Ship the business-metrics page (ADMIN-02) and the cohort retention heatmap (ADMIN-08).

Purpose: Owner gets the financial overview + retention picture in one wave. Both surfaces are READ-only — no novel writers — but the cohort heatmap is the first CSS-grid heatmap in the repo (no analog).

Output: /admin/metrics + /admin/cohorts live; CSS-grid heatmap with k-anonymity guard; tests green.
</objective>

<execution_context>
@/Users/karstenhaldan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karstenhaldan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-CONTEXT.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-PATTERNS.md
@/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md
@/Users/karstenhaldan/minisite/leanshot/src/components/dashboard/charts/BaseChart.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Sparkline.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/ui/Card.tsx
@/Users/karstenhaldan/minisite/leanshot/src/components/admin/AdminAffiliatesScaffold.tsx
@/Users/karstenhaldan/minisite/leanshot/src/hooks/useCountUp.ts
@/Users/karstenhaldan/minisite/leanshot/src/components/admin/cohorts/__tests__/CohortHeatmap.test.tsx

<interfaces>
<!-- From Wave 0 migrations -->
public.tier_effective (view, Phase 19) — joins subscriptions w/ MAX(current_period_end) per user; returns user_id + tier
public.cohort_retention (view, plan 22-01 File 10) — returns cohort_week + day_offset + active_users + retention_pct
public.user_activity_daily (matview, plan 22-01 File 08) — refreshed daily 02:00 UTC
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HEAD-reset verify + author admin_metrics_rpcs migration</name>
  <files>/Users/karstenhaldan/minisite/supabase/migrations/20270601000020_admin_metrics_rpcs.sql</files>
  <read_first>
    /Users/karstenhaldan/minisite/supabase/migrations/20270101000004_tier_effective_view.sql (Phase 19 — tier_effective shape),
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000008_user_activity_daily_matview.sql (post-Wave-0),
    /Users/karstenhaldan/minisite/supabase/migrations/20270601000010_cohort_retention_view.sql (post-Wave-0),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (§Common Operation 4 lines 856-895 — cohort_retention shape)
  </read_first>
  <action>
    HEAD-reset: `test -f /Users/karstenhaldan/minisite/supabase/migrations/20270601000010_cohort_retention_view.sql || exit 1`.

    Author migration 20 with two RPCs:

    (1) `admin_compute_mrr_arr(p_period text default '30d') returns jsonb security definer set search_path = public, pg_catalog language plpgsql as $$ begin if not exists (select 1 from public.profiles where id = auth.uid() and is_staff = true) then raise exception 'forbidden' using errcode = '42501'; end if; return jsonb_build_object('mrr', (select coalesce(sum(price_cents),0)::int from public.subscriptions s join public.tier_effective te on te.user_id = s.user_id where te.tier in ('paid','clinic_seat') and s.status = 'active'), 'arr', (select coalesce(sum(price_cents),0)::int * 12 from public.subscriptions ...), 'churn_30d', (select ...), 'free_count', ..., 'paid_count', ..., 'clinic_seat_count', ..., 'churn_series', (select jsonb_agg(...) from generate_series + monthly bins), 'clinic_seat_utilization', (select jsonb_agg(jsonb_build_object('clinic_name', name, 'used', used_seats, 'total', total_seats, 'series', series) from ...)); end; $$;` Behavior: aggregate everything the AdminMetricsPage needs in one round trip.

    (2) `admin_get_cohort_retention(p_weeks int default 13) returns table (cohort_week date, day_offset int, active_users int, retention_pct numeric) security definer set search_path = public, pg_catalog language plpgsql as $$ begin if not exists (select 1 from public.profiles where id = auth.uid() and is_staff = true) then raise exception 'forbidden' using errcode = '42501'; end if; return query select cr.cohort_week, cr.day_offset, cr.active_users, cr.retention_pct from public.cohort_retention cr where cr.cohort_week >= (current_date - (p_weeks * 7))::date order by cr.cohort_week desc, cr.day_offset asc; end; $$;`. Grant execute to authenticated.

    Push: `supabase db push --linked --dry-run 2>&1 | grep -v "^Skipping" && supabase db push --linked`.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite &amp;&amp; test -f supabase/migrations/20270601000020_admin_metrics_rpcs.sql &amp;&amp; grep -q "admin_compute_mrr_arr" supabase/migrations/20270601000020_admin_metrics_rpcs.sql &amp;&amp; grep -q "admin_get_cohort_retention" supabase/migrations/20270601000020_admin_metrics_rpcs.sql</automated>
  </verify>
  <done>Migration 20 lands with 2 RPCs (both is_staff-gated).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdminMetricsPage + KPI strip + MRR chart + clinic-seat list + admin-metrics.ts wrapper</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/pages/AdminMetricsPage.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/AdminMetricsKpiStrip.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/AdminMetricsMrrChart.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/AdminMetricsClinicSeatList.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/lib/admin/admin-metrics.ts
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/src/components/dashboard/charts/BaseChart.tsx (chart.js wrapper; ChartConfiguration usage),
    /Users/karstenhaldan/minisite/leanshot/src/components/ui/Sparkline.tsx (clinic-seat per-row sparkline),
    /Users/karstenhaldan/minisite/leanshot/src/hooks/useCountUp.ts (KPI count-up animation; honor useReducedMotion per UI-SPEC line 700),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (lines 259-267 — /admin/metrics layout; Copywriting lines 499-516)
  </read_first>
  <behavior>
    - Test (admin-metrics): fetchMetrics({period}) calls admin_compute_mrr_arr RPC + returns typed shape.
    - Test (AdminMetricsPage): renders 4-tile KPI strip (MRR / ARR / 30d churn / clinic-seat utilization Sparkline).
    - Test: period Pill (7d/30d/90d/12m) re-fetches with new p_period.
    - Test: chart uses BaseChart with line+bar combo config.
    - Test: clinic-seat list renders 1 row per clinic with sparkline.
    - Test: KPI values use useCountUp animation; respects useReducedMotion fallback.
    - Test: empty state per UI-SPEC line 515 ("No revenue data yet. Connect Stripe to see MRR and ARR here.").
  </behavior>
  <action>
    Implement `admin-metrics.ts`: `fetchMetrics({period?: '7d'|'30d'|'90d'|'12m'}): Promise<AdminMetrics>` — wraps admin_compute_mrr_arr RPC.

    Implement `AdminMetricsKpiStrip.tsx`: 4 Card span={3} with KPI value text-3xl 600 (per UI-SPEC type budget line 85-90); useCountUp on mount; aria-live="polite".

    Implement `AdminMetricsMrrChart.tsx`: wraps BaseChart with line+bar combo configuration. X-axis monthly bins. Y-left subscribers. Y-right churn %. Legend "Free · Paid · Churn rate" per UI-SPEC line 514.

    Implement `AdminMetricsClinicSeatList.tsx`: Card with one row per clinic — `{clinic_name}  {used}/{total} seats  <Sparkline>`.

    Implement `AdminMetricsPage.tsx`: useState for period (default '30d'), fetches metrics on mount + period change. Renders header per UI-SPEC line 261 + KPI strip + main chart + clinic-seat list. Empty state if all KPIs are zero.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run AdminMetrics 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓|tests"</automated>
  </verify>
  <done>/admin/metrics renders KPIs + chart + clinic-seat list; 7 behavior tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AdminCohortsPage + CohortHeatmap (CSS-grid + k-anonymity)</name>
  <files>
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/pages/AdminCohortsPage.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/cohorts/CohortHeatmap.tsx,
    /Users/karstenhaldan/minisite/leanshot/src/components/admin/cohorts/__tests__/CohortHeatmap.test.tsx
  </files>
  <read_first>
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-UI-SPEC.md (lines 276-282 — /admin/cohorts layout contract; Copywriting lines 538-550; lines 279 + 194 — color-mix CSS for cell density),
    /Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-RESEARCH.md (Pattern: Cohort heatmap density mapping + K-anonymity threat T-22-10 + Common Operation 4 query shape)
  </read_first>
  <behavior>
    - Test: CohortHeatmap renders CSS-grid with `grid-cols-[120px_repeat(91,_minmax(8px,_1fr))]` per UI-SPEC line 279.
    - Test: renders 13 weeks × 91 days = 1183 cells when data has 13 rows × 91 cols (default visible range).
    - Test: cell with retention_pct = 80 has inline `background-color: color-mix(in srgb, var(--color-primary) 80%, var(--color-surface))`.
    - Test: cell with active_users < 10 renders text "<10" instead of exact count + tooltip "Hidden for privacy" (k-anonymity guard per T-22-10).
    - Test: tooltip text format per UI-SPEC line 546: "{pct}% retention · {active_users}/{cohort_size} users active on day {day} of cohort starting {date}".
    - Test: "Show all weeks" toggle per UI-SPEC line 278 — re-fetches with p_weeks = 52.
    - Test: empty state per UI-SPEC line 549-550 ("No cohort data yet — The cohort matview refreshes daily at 02:00 UTC. Check back tomorrow.").
    - Test: legend renders horizontal gradient from --color-surface (0%) → --color-primary (100%) with tick marks 0/25/50/75/100.
    - Test: each cell has descriptive aria-label per UI-SPEC line 279.
  </behavior>
  <action>
    Implement `AdminCohortsPage.tsx`:
    - useState weeks default 13; toggle for showAll → weeks = 52.
    - fetchCohortRetention({weeks}) wraps admin_get_cohort_retention RPC.
    - Renders header + CohortHeatmap + legend.

    Implement `CohortHeatmap.tsx`:
    - Group fetched rows by cohort_week; each cohort row = 91 cells (day_offset 0..90).
    - Render container: `<div className="grid grid-cols-[120px_repeat(91,_minmax(8px,_1fr))] gap-px">`.
    - For each cohort row: 1 label cell ("Week of {date}") + 91 data cells.
    - For each cell: compute color via inline `style={{ backgroundColor: \`color-mix(in srgb, var(--color-primary) ${Math.round(cell.retention_pct)}%, var(--color-surface))\` }}` + `aria-label`.
    - K-anonymity: if `cell.active_users < 10` render `<span className="text-[10px]">&lt;10</span>` instead of bare cell + tooltip "Hidden for privacy".
    - Hover/focus: native `title` attribute with full tooltip text per UI-SPEC line 546 OR controlled state if styling needed; v1.2 native title is fine.
    - Empty state if zero rows.
    - Legend below grid: horizontal gradient div with tick mark spans.

    Performance: per UI-SPEC line 281 — `will-change: transform` on grid; v1.3 virtualization deferred.

    Refresh cohort_retention live by calling RPC; no realtime subscription (matview refreshes daily 02:00 UTC).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot &amp;&amp; npm test -- --run CohortHeatmap 2&gt;&amp;1 | tail -3 | grep -qE "PASS|✓|tests"</automated>
  </verify>
  <done>/admin/cohorts renders 13×91 grid with color-mix density + k-anonymity guard; 9 behavior tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → admin_compute_mrr_arr RPC | is_staff gate in body |
| client → admin_get_cohort_retention RPC | is_staff gate in body |
| cohort_retention view → admin client | Aggregated bucket data; per-user activity_day NOT returned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-47 | I | Cohort heatmap exposes per-user activity_day via tooltip | mitigate | Tooltip shows only aggregate (active_users + retention_pct); no user_id in result set |
| T-22-48 | I | Low-density cohort cells enable re-identification | mitigate | k-anonymity client-side guard: cells with active_users < 10 render as "<10" (T-22-10 from plan 22-01 carries into UI here) |
| T-22-49 | E | Non-staff fetches metrics via admin_compute_mrr_arr | mitigate | is_staff gate in RPC body; non-staff gets 42501 forbidden |
| T-22-50 | T | Matview refresh fails silently → admin sees stale retention | accept | cron 02:00 UTC daily; if refresh fails admin sees yesterday's data; not load-bearing for billing decisions; document in 22-08-SUMMARY follow-ups |
</threat_model>

<verification>
- /admin/metrics renders 4 KPIs + main chart + clinic-seat list.
- /admin/cohorts renders 13×91 CSS-grid with color-mix density per cell.
- K-anonymity guard verified: cells with active_users < 10 render "<10".
- Both RPCs is_staff-gated; non-staff smoke returns 42501.
</verification>

<success_criteria>
- ADMIN-02 + ADMIN-08 surfaces complete and live behind AdminLayout's is_staff gate.
- VALIDATION.md rows for ADMIN-02, ADMIN-08 updated with Plan 22-08.
- Critical security: k-anonymity threat T-22-10 is operationally mitigated in UI.
</success_criteria>

<output>
After completion, create `/Users/karstenhaldan/minisite/leanshot/.planning/phases/22-owner-admin-lifecycle-email-dsar-cookie-consent/22-08-SUMMARY.md` per template. Include: KPI test results, heatmap k-anonymity verification, color-mix support note (Safari ≥ 16.4 / Chrome ≥ 111 — all in-scope per CLAUDE.md ES2022 target).
</output>
