---
phase: 28
plan: 01
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - supabase/migrations/20270601100003_org_member_role_enum.sql
  - supabase/migrations/20270601100004_org_members_table.sql
  - supabase/migrations/20270601100005_org_settings_table.sql
  - supabase/migrations/20270601100006_org_branding_table.sql
  - supabase/migrations/20270601100007_org_invites_table.sql
  - supabase/migrations/20270601100008_org_subscriptions_table.sql
  - supabase/migrations/20270601100009_org_patient_links_table.sql
  - supabase/migrations/20270601100010_org_consent_grants_table.sql
  - supabase/migrations/20270601100012_send_revoke_org_invite_rpcs.sql
  - supabase/migrations/20270601100013_link_org_patient_rpc.sql
  - supabase/migrations/20270601100018_org_invites_expiry_purge_cron.sql
  - supabase/functions/clinic-org-invite/index.ts
  - supabase/functions/clinic-org-invite/deno.json
  - src/lib/__tests__/_fixtures/p28-rls-fixture.ts
  - src/lib/__tests__/rls-org-members.test.ts
  - src/lib/__tests__/rls-org-invites.test.ts
  - src/lib/__tests__/rls-org-subscriptions.test.ts
  - src/lib/__tests__/rls-org-settings.test.ts
  - src/lib/__tests__/rls-org-branding.test.ts
  - src/lib/__tests__/rls-org-patient-links.test.ts
  - src/lib/__tests__/rls-org-consent-grants.test.ts
  - src/lib/__tests__/rls-org-organizations.test.ts
  - tests/sql/p28-schema-shape.test.sql
autonomous: true
requirements:
  - ORG-01
  - ORG-05

must_haves:
  truths:
    - "8 org-scoped tables exist with the columns from CONTEXT D-11..D-18 (organizations already exists post Plan-0; 7 NET-NEW + organizations adds-only): org_members, org_invites, org_subscriptions, org_settings, org_branding, org_patient_links, org_consent_grants."
    - "Every new table has RLS ENABLED and at least one SELECT policy joining on org_id."
    - "Every new table has a paired *-rls.test.ts file using the ES256-compatible admin.generateLink + plain fetch /auth/v1/verify pattern (per [[reference_rls_fixture_gotrueclient_flake]])."
    - "Every test file uses a file-scoped TEST_SLUG_PREFIX derived from __filename + Date.now() (per [[feedback_rls_per_file_slug_prefix]]) — no shared prefix clobbering."
    - "User A in Org X CANNOT SELECT/INSERT/UPDATE/DELETE rows in Org Y on any of the 8 tables (cross-tenant impersonation proof per ORG-05)."
    - "org_invites.invite_token_hash is generated via the Phase 9 makeInviteTokenHash helper (per addendum A2 — do NOT re-implement)."
    - "send_org_invite RPC and clinic-org-invite Edge Function return identical 200-status shape regardless of whether the invitee email exists in auth.users (Phase 9 W-1 anti-enumeration preserved per addendum A2)."
    - "org_consent_grants.scope validation reuses Phase 9 _validate_consent_scope (per CONTEXT D-18 + [[feedback_planner_iter1_anti_patterns]] #4 — no duplicate jsonb shape contract)."
    - "org_subscriptions has RLS deny-all for writes in v1.3 (P29 owns writes); SELECT for org admins only."
    - "org_invites.status state machine has explicit owners: pending (default INSERT) → accepted (RPC accept_org_invite, deferred to P29/P31 consumer) | expired (cron at 04:00 UTC) | revoked (revoke_org_invite RPC). Per [[feedback_status_machine_transition_owner]] every transition has a named owning function."
  artifacts:
    - path: "supabase/migrations/20270601100003_org_member_role_enum.sql"
      provides: "org_member_role enum (admin, staff, viewer) per D-12"
      contains: "create type org_member_role"
    - path: "supabase/migrations/20270601100004_org_members_table.sql"
      provides: "org_members table + RLS policies + index on (user_id) for hook latency (per Pitfall 4)"
      contains: "create table public.org_members"
    - path: "supabase/migrations/20270601100007_org_invites_table.sql"
      provides: "org_invites table + RLS + status enum (pending,accepted,expired,revoked)"
      contains: "invite_token_hash text not null"
    - path: "supabase/migrations/20270601100012_send_revoke_org_invite_rpcs.sql"
      provides: "send_org_invite + revoke_org_invite + accept_org_invite SECDEF RPCs reusing makeInviteTokenHash from clinic.ts; W-1 anti-enumeration preserved"
      contains: "security definer"
    - path: "supabase/functions/clinic-org-invite/index.ts"
      provides: "Edge Function sibling to Phase 9's clinic-invite/send; imports makeInviteTokenHash; identical 200-status shape per addendum A2"
    - path: "supabase/migrations/20270601100018_org_invites_expiry_purge_cron.sql"
      provides: "pg_cron schedule 04:00 daily UTC to flip pending → expired"
      contains: "select cron.schedule"
    - path: "src/lib/__tests__/_fixtures/p28-rls-fixture.ts"
      provides: "sessionFor(email), createTwoOrgsTwoUsers(slugPrefix), file-scoped TEST_SLUG_PREFIX factory, afterAll cleanup helper"
    - path: "tests/sql/p28-schema-shape.test.sql"
      provides: "ORG-01 column presence + RLS-enabled flag assertions for all 8 tables"
  key_links:
    - from: "org_members.org_id, org_invites.org_id, org_subscriptions.org_id, org_settings.org_id, org_branding.org_id, org_patient_links.org_id, org_consent_grants.org_id"
      to: "organizations(id) — renamed from public.orgs by Plan-0"
      via: "FK with on delete restrict"
      pattern: "references organizations\\(id\\)"
    - from: "org_consent_grants.scope"
      to: "Phase 9 _validate_consent_scope() DB function"
      via: "CHECK constraint or BEFORE INSERT trigger calling the existing validator"
      pattern: "_validate_consent_scope"
    - from: "clinic-org-invite Edge Function"
      to: "Phase 9 makeInviteTokenHash export"
      via: "import from shared module accessible to Deno (e.g., supabase/functions/_shared/invite-token.ts mirroring src/lib/clinic.ts:makeInviteTokenHash)"
      pattern: "makeInviteTokenHash"
---

<objective>
Wave 1 schema slab — 7 NET-NEW org-scoped tables (organizations already renamed by Plan-0) with full RLS + cross-tenant impersonation proof tests + W-1-preserving org-admin invite flow. Lands the ORG-01 schema deliverable and the ORG-05 cross-tenant proof in a single coherent unit.

Purpose: Establish the multi-tenant schema bedrock on which Plans 02-07 layer the 4-vector defense. Per addendum A2, org_invites ships as a parallel table to public.invites with a new Edge Function — does NOT extend Phase 9's clinic-invite/send via discriminator.

Output: 11 migration files (7 tables + 1 enum + 2 RPCs + 1 cron) + 1 Edge Function + 8 RLS test files + 1 fixture helper + 1 SQL schema-shape test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-CONTEXT.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-ADDENDUM-orgs-reconciliation.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-RESEARCH.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-VALIDATION.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-00-SUMMARY.md
@.planning/research/PITFALLS.md
@src/lib/clinic.ts

<interfaces>
<!-- Phase 9 reusable exports — used by tests AND by clinic-org-invite Edge Function -->

From src/lib/clinic.ts:
```typescript
export async function makeInviteTokenHash(rawToken: string): Promise<string>;
// SHA-256 over rawToken via crypto.subtle.digest; returns hex string.
// W-1 invariant: send-side stores the hash; accept-side recomputes from rawToken.
// Edge Function MUST import a Deno-compatible mirror at supabase/functions/_shared/invite-token.ts
```

From src/types/clinic.ts:
```typescript
export type ConsentScope = { ... };  // exact shape — re-used by org_consent_grants
export function isConsentScope(x: unknown): x is ConsentScope;
```

<!-- DB-side shape contract for org_consent_grants.scope: -->
-- Phase 9 already shipped `_validate_consent_scope(scope jsonb) returns boolean`.
-- org_consent_grants MUST use it via CHECK constraint:
--   constraint org_consent_grants_scope_valid CHECK (_validate_consent_scope(scope))
-- DO NOT duplicate the jsonb shape contract (Pitfall #8 + [[feedback_planner_iter1_anti_patterns]] #4).

<!-- RLS fixture pattern (from research §G) — file-scoped prefix is MANDATORY -->
```typescript
// src/lib/__tests__/_fixtures/p28-rls-fixture.ts
export function makeSlugPrefix(testFilename: string): string {
  const base = testFilename.split('/').pop()!.replace('.test.ts', '');
  return `p28-${base}-${Date.now()}`;
}
export async function sessionFor(email: string): Promise<{ access_token: string; user_id: string }>;
export async function createTwoOrgsTwoUsers(prefix: string): Promise<{ orgX: string; orgY: string; userA: string; userB: string; sessA: Session; sessB: Session }>;
```

<!-- org_invites status state machine (per [[feedback_status_machine_transition_owner]]) -->
| From    | To       | Owner                                                       |
|---------|----------|-------------------------------------------------------------|
| (none)  | pending  | send_org_invite RPC                                         |
| pending | accepted | accept_org_invite RPC (consumed by P31 onboarding UI)       |
| pending | expired  | pg_cron job org_invites_expiry_purge (04:00 UTC daily)      |
| pending | revoked  | revoke_org_invite RPC                                       |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 7 net-new migrations + enum + RLS policies + indexes + RPCs + cron (single commit per table)</name>
  <read_first>
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-CONTEXT.md (D-12 through D-18 — exact column specs)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-RESEARCH.md §Recommended Project Structure + §Pitfall 4 (hook latency index) + §Pitfall 5 (RLS queries org_members directly, not JWT claim) + §Pitfall 7 (filename regex)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-ADDENDUM-orgs-reconciliation.md (A2 — org_invites parallel new table; import makeInviteTokenHash)
    - reference: memory [[reference_supabase_migration_gotchas]] (SECDEF search_path; partial-index IMMUTABLE; audit cascade GUC)
    - reference: memory [[reference_supabase_migration_filename_regex]]
    - src/lib/clinic.ts (16 SECDEF RPC wrappers — Pattern S1 dual-layer; p_org_id convention)
  </read_first>
  <behavior>
    - Test 1 (tests/sql/p28-schema-shape.test.sql): For each of the 8 tables (organizations + 7 new), `select relrowsecurity from pg_class where relname = '<table>'` returns true (RLS enabled).
    - Test 2: For each new table, `select column_name from information_schema.columns where table_name = '<table>'` returns the exact column list from CONTEXT D-12..D-18.
    - Test 3: `select tgname from pg_trigger where tgrelid = 'public.org_consent_grants'::regclass` includes a CHECK or BEFORE INSERT trigger invoking `_validate_consent_scope`.
    - Test 4: `select cron.job_name from cron.job where command like '%org_invites%'` returns at least one row with schedule '0 4 * * *' (04:00 daily UTC).
    - Test 5: `select proname, prosecdef, proconfig from pg_proc where proname in ('send_org_invite','revoke_org_invite','accept_org_invite','link_org_patient')` returns 4 rows, all with prosecdef=true, all with proconfig containing 'search_path=pg_catalog, public, extensions'.
    - Test 6: `select indexname from pg_indexes where tablename = 'org_members' and indexdef like '%user_id%'` returns at least one row (per Pitfall 4 — hook latency).
    - Test 7: `supabase db push --linked` shows zero `^Skipping` lines (Pitfall 7 — strict filename regex).
  </behavior>
  <action>
    Create 11 migration files in `supabase/migrations/` with strict 14-digit timestamps (NEVER `supabase/functions/migrations/` — Edge Functions live in `supabase/functions/`, schema migrations in `supabase/migrations/`). Each migration ships in this order:

    1. `20270601100003_org_member_role_enum.sql` — `create type org_member_role as enum ('admin','staff','viewer');` per D-12.

    2. `20270601100004_org_members_table.sql` — columns per D-12 (id, org_id references organizations, user_id references auth.users on delete cascade, role org_member_role not null, joined_at timestamptz default now(), last_active_at timestamptz, unique(org_id, user_id)). Enable RLS. Policies: SELECT for `auth.uid()` matches user_id OR `exists(select 1 from org_members om2 where om2.org_id = org_members.org_id and om2.user_id = auth.uid())` (members see other members of same org); INSERT/UPDATE/DELETE via SECDEF RPCs only (no direct write policy for `authenticated`). Add `create index org_members_user_id_idx on org_members(user_id);` (per Pitfall 4 — Custom Access Token Hook latency budget; index keeps lookup sub-ms).

    3. `20270601100005_org_settings_table.sql` — columns per D-15 (org_id pk references organizations, default_timezone text default 'UTC', enforce_clinician_mfa boolean default true, auto_revoke_inactive_after_days int default 90, mask_patient_emails_in_roster boolean default false, notification_email citext null). Enable RLS. SELECT for org_members (`exists(select 1 from org_members where org_id = org_settings.org_id and user_id = auth.uid())`); UPDATE for admin role only.

    4. `20270601100006_org_branding_table.sql` — columns per D-16 (org_id pk, logo_url text null, primary_color text null, accent_color text null, font_family text null, support_email citext null, updated_at timestamptz default now()). Same RLS shape as org_settings.

    5. `20270601100007_org_invites_table.sql` — columns per D-13 + addendum A2. Create enum `org_invite_status as enum ('pending','accepted','expired','revoked')` first. Table: id uuid pk default gen_random_uuid(), org_id references organizations, email citext not null, invited_role org_member_role not null, invite_token_hash text not null, status org_invite_status not null default 'pending', expires_at timestamptz not null default now() + interval '7 days', accepted_at timestamptz null, revoked_at timestamptz null, created_at timestamptz default now(), created_by uuid references auth.users(id). RLS SELECT for org admins only; INSERT/UPDATE/DELETE via SECDEF only. Add index `org_invites_status_expires_idx on org_invites(status, expires_at) where status = 'pending'` (partial index — verify expression IMMUTABLE per [[reference_supabase_migration_gotchas]]).

    6. `20270601100008_org_subscriptions_table.sql` — columns per D-14 (org_id pk references organizations, stripe_customer_id text, stripe_subscription_id text null, status text not null default 'pending', current_period_end timestamptz null, seats_paid int default 0, seats_used int default 0, updated_at timestamptz default now()). Enable RLS. SELECT for org admins only; INSERT/UPDATE/DELETE explicit DENY-ALL (no policy — Postgres default-deny when RLS is on; explicitly comment `-- v1.3: P28 ships skeleton; P29 owns writes via org-subscription-webhook Edge Function with service_role bypass`).

    7. `20270601100009_org_patient_links_table.sql` — columns per D-17 (id uuid pk, org_id references organizations, patient_user_id uuid references auth.users on delete cascade, linked_by uuid references auth.users, linked_at timestamptz default now(), unlinked_at timestamptz null, consent_grant_id uuid references org_consent_grants(id)). NOTE: forward reference to org_consent_grants — order migration AFTER consent_grants OR use deferrable FK. Use deferrable FK to avoid migration ordering brittleness. RLS SELECT for org_members OR `auth.uid() = patient_user_id`; INSERT via SECDEF `link_org_patient` only.

    8. `20270601100010_org_consent_grants_table.sql` — columns per D-18 (id uuid pk, org_id references organizations, patient_user_id references auth.users, scope jsonb not null, granted_at timestamptz default now(), revoked_at timestamptz null, granted_via text not null check (granted_via in ('invite','manual','migration'))). CHECK constraint: `constraint org_consent_grants_scope_valid CHECK (_validate_consent_scope(scope))` — RE-USES Phase 9 validator per CONTEXT D-18 + [[feedback_planner_iter1_anti_patterns]] #4. RLS SELECT for `auth.uid() = patient_user_id` OR org admins; INSERT/UPDATE via SECDEF only. Append-only update pattern: only `revoked_at` may transition from null to a timestamp (per Phase 24 D-17 append-only RLS pattern).

    9. `20270601100012_send_revoke_org_invite_rpcs.sql` — three SECDEF functions:
       - `send_org_invite(p_org_id uuid, p_email citext, p_role org_member_role) returns jsonb` — generates UUID for invite_id, generates raw token, computes hash (mirror Phase 9 makeInviteTokenHash in PL/pgSQL using pgcrypto `digest(token, 'sha256')`), INSERTs row, returns `{invite_id, token}`. CRITICAL W-1 invariant: returns IDENTICAL shape regardless of whether p_email exists in auth.users. Calls Phase 24 `log_admin_action('org_invite_sent', p_org_id, jsonb_build_object('invite_id', invite_id, 'role', p_role))`.
       - `revoke_org_invite(p_invite_id uuid) returns void` — caller must be admin of invite's org; sets status='revoked', revoked_at=now(). Calls log_admin_action.
       - `accept_org_invite(p_token text) returns jsonb` — recomputes hash from p_token, finds matching pending invite with expires_at > now(), atomically: INSERTs org_members row with the invited_role, sets invite.status='accepted', accepted_at=now(). Returns `{org_id, role}`. Pattern S1: dual-layer (no role gate — token IS the auth; W-1 invariant — returns generic error code 42501 if token doesn't match).

       ALL three: `security definer set search_path = pg_catalog, public, extensions` per [[reference_supabase_migration_gotchas]]. ALL three call Phase 24 `log_admin_action`. Revoke from authenticated/public; grant execute to authenticated only.

    10. `20270601100013_link_org_patient_rpc.sql` — SECDEF `link_org_patient(p_org_id uuid, p_patient_user_id uuid, p_consent_grant_id uuid) returns jsonb` — caller must be admin/staff of p_org_id; verifies `org_consent_grants` row exists for (p_org_id, p_patient_user_id) with revoked_at IS NULL; INSERTs into org_patient_links. Pattern S1 + log_admin_action.

    11. `20270601100018_org_invites_expiry_purge_cron.sql` — `select cron.schedule('p28_org_invites_expiry_purge', '0 4 * * *', $$ update public.org_invites set status='expired' where status='pending' and expires_at < now() $$);` Schedule per CONTEXT specifics (04:00 daily UTC, no collision with P27 / P22 crons).

    **[BLOCKING] Push gate — discrete step, MUST succeed before Task 3 runs.** Push migrations: `SUPABASE_ACCESS_TOKEN=<token> supabase db push --linked 2>&1 | tee /tmp/p28-01-push.log`. Scan output for `^Skipping` — if ANY skip line appears, ABORT (do not proceed to Task 3); fix the filename to match the strict `<14-digits>_name.sql` regex (per `[[reference_supabase_migration_filename_regex]]`) and re-push. Plan-checker BLOCKER: the verify command MUST exit non-zero if `^Skipping` count > 0.

    Run `supabase db query --linked --file tests/sql/p28-schema-shape.test.sql` (the SQL fixture from Task 3) to verify all 8 tables present with RLS enabled. (Read-only verification — does not double-push.)

    Pattern S1 (dual-layer) for ALL 4 SECDEF RPCs: function body starts with `if not exists (select 1 from org_members where user_id = auth.uid() and org_id = p_org_id and role = 'admin') then raise exception 'forbidden' using errcode = '42501'; end if;` (except `accept_org_invite` which is token-gated, not role-gated).
  </action>
  <verify>
    <automated>SUPABASE_ACCESS_TOKEN="${SUPABASE_ACCESS_TOKEN}" supabase db push --linked 2>&amp;1 | tee /tmp/p28-01-push.log | grep -c '^Skipping' | grep -q '^0$' &amp;&amp; supabase db query --linked --file tests/sql/p28-schema-shape.test.sql</automated>
  </verify>
  <done>11 migrations pushed; 8 tables show RLS enabled in pg_class; 4 SECDEF RPCs exist with explicit search_path; cron schedule registered; partial index on org_invites is IMMUTABLE-valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship clinic-org-invite Edge Function (sibling to Phase 9) + _shared/invite-token.ts Deno helper preserving W-1</name>
  <read_first>
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-ADDENDUM-orgs-reconciliation.md (A2 — parallel Edge Function; identical 200 shape)
    - src/lib/clinic.ts (makeInviteTokenHash signature — must mirror SHA-256 over raw token)
    - reference: memory [[reference_supabase_edge_function_deploy]] (bundler import_map.json ignored; use esm.sh; gateway forces text/plain + CSP sandbox)
    - reference: memory [[reference_resend_phase9_wiring]] (RESEND_API_KEY + RESEND_FROM as Function secrets; sandbox onboarding@resend.dev)
    - reference: memory [[reference_vendor_gated_send_health_check]] (build prod path + startup health check that no-ops with logged warning if vendor secret absent)
  </read_first>
  <behavior>
    - Test 1: `supabase/functions/_shared/invite-token.ts` exports `makeInviteTokenHash(raw: string): Promise<string>` whose SHA-256 hex output exactly matches `src/lib/clinic.ts:makeInviteTokenHash(raw)` for a fixture set of 10 inputs.
    - Test 2: `supabase/functions/clinic-org-invite/index.ts` POST `{org_id, email: existing_email, role}` returns 200 + `{invite_id}` shape.
    - Test 3: `supabase/functions/clinic-org-invite/index.ts` POST `{org_id, email: nonexistent_email, role}` returns 200 + `{invite_id}` shape — IDENTICAL response (W-1 anti-enumeration). Status code, headers, body keys all identical.
    - Test 4: `supabase functions deploy clinic-org-invite --linked` succeeds; subsequent `curl <function-url> -d '{...}'` against the deployed function returns 200.
    - Test 5: Startup health-check at function load: if RESEND_API_KEY absent, function logs `[warn] clinic-org-invite: RESEND_API_KEY missing; invites will be persisted but emails will NOT be sent` and proceeds (no-op send path).
  </behavior>
  <action>
    Create `supabase/functions/_shared/invite-token.ts` — Deno-compatible mirror of `src/lib/clinic.ts:makeInviteTokenHash`. Use the Web Crypto API (`crypto.subtle.digest('SHA-256', new TextEncoder().encode(raw))` then hex-encode). Exported function name MUST match Phase 9 verbatim. Add 10-input fixture test in `supabase/functions/_shared/invite-token.test.ts` asserting hex-for-hex equality against pre-computed expected values (copy expected values from running Phase 9 `makeInviteTokenHash` against the same 10 inputs).

    Create `supabase/functions/clinic-org-invite/index.ts`:
    - Imports `makeInviteTokenHash` from `../_shared/invite-token.ts`.
    - Validates request body via zod: `{ org_id: string().uuid(), email: string().email(), role: enum(['admin','staff','viewer']) }`.
    - Authenticates caller via JWT (Supabase Edge Function `req.headers.get('Authorization')` → `supabase.auth.getUser(jwt)` — admin role of p_org_id check via `supabase.from('org_members').select('role').eq('org_id', body.org_id).eq('user_id', user.id).maybeSingle()`).
    - Generates raw token (`crypto.getRandomValues(new Uint8Array(32))` then hex), computes hash, calls `supabase.rpc('send_org_invite', { p_org_id, p_email, p_role })` which performs the INSERT.
    - Dispatches email via Resend (or no-op + warn-log if `RESEND_API_KEY` absent — per `[[reference_vendor_gated_send_health_check]]`).
    - Returns 200 + `{ invite_id }` REGARDLESS of email-exists check (W-1 invariant per addendum A2). Identical response shape, identical status, identical headers.

    Create `supabase/functions/clinic-org-invite/deno.json` with import-map pointing at `esm.sh` (per `[[reference_supabase_edge_function_deploy]]` — bundler ignores import_map.json).

    Add startup health check (top of `index.ts`):
    ```typescript
    if (!Deno.env.get('RESEND_API_KEY')) {
      console.warn('[clinic-org-invite] RESEND_API_KEY missing; invites will be persisted but emails will NOT be sent');
    }
    ```

    Deploy via `supabase functions deploy clinic-org-invite --linked`. Verify with `curl -X POST $FUNCTION_URL -H 'Authorization: Bearer $JWT' -d '{"org_id":"...","email":"test@example.com","role":"staff"}'`.

    Commit message: `feat(28-01): clinic-org-invite Edge Function — parallel to Phase 9, imports makeInviteTokenHash, preserves W-1 (per addendum A2)`.
  </action>
  <verify>
    <automated>deno test supabase/functions/_shared/invite-token.test.ts &amp;&amp; supabase functions deploy clinic-org-invite --linked 2>&amp;1 | grep -E 'Deployed|deployed successfully'</automated>
  </verify>
  <done>Edge Function deployed; Deno test proves hash parity with Phase 9; W-1 response shape verified identical for existing + nonexistent emails; health-check warn-logs on missing RESEND_API_KEY without crashing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship 8 cross-tenant RLS test files + shared fixture helper + SQL schema-shape test</name>
  <read_first>
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-RESEARCH.md §G (cross-tenant RLS test fixture skeleton)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-VALIDATION.md (Wave 0 file list)
    - reference: memory [[reference_rls_fixture_gotrueclient_flake]] (admin.generateLink + plain fetch /auth/v1/verify ES256-compat)
    - reference: memory [[feedback_rls_per_file_slug_prefix]] (file-scoped TEST_SLUG_PREFIX)
    - reference: memory [[reference_vitest_skip_fixme]] (use it.skip, NOT it.fixme — vitest 4.1.5 TypeError at load)
    - src/lib/clinic.test.ts (existing v1.1 cross-tenant fixture convention — exact layout)
  </read_first>
  <behavior>
    - Test 1: `src/lib/__tests__/_fixtures/p28-rls-fixture.ts` exports `sessionFor`, `createTwoOrgsTwoUsers`, `makeSlugPrefix` and is importable from all 8 RLS test files without circular-dep errors.
    - Test 2: For each of 8 test files, file-scoped TEST_SLUG_PREFIX is derived from `__filename + Date.now()` — vitest parallel runs of two RLS files do NOT clobber each other's fixtures.
    - Test 3 (per RLS file): User A in Org X attempting SELECT * FROM <table> WHERE org_id = Org Y returns 0 rows (cross-tenant SELECT proof).
    - Test 4 (per RLS file): User A INSERT into Org Y fails with error code 42501 OR PGRST204 (RLS denial OR no-rows-returned-from-INSERT-with-RLS).
    - Test 5 (per RLS file): User A UPDATE/DELETE on Org Y rows returns 0 affected rows (RLS predicate filter).
    - Test 6 (rls-org-invites.test.ts ONLY): send_org_invite returns identical shape for existing vs nonexistent email (W-1 invariant verified via direct RPC call).
    - Test 7 (rls-org-patient-links.test.ts ONLY): patient (auth.uid() = patient_user_id) CAN read their own link rows (patient-self-read path per CONTEXT D-17).
    - Test 8 (rls-org-consent-grants.test.ts ONLY): _validate_consent_scope re-use proven — INSERT with invalid scope shape fails with check_violation; INSERT with valid Phase 9 ConsentScope succeeds.
    - Test 9 (rls-org-subscriptions.test.ts ONLY): authenticated user CANNOT INSERT/UPDATE/DELETE — only SELECT for org admins (v1.3 deny-all writes per D-14).
    - Test 10 (rls-org-organizations.test.ts): User A SELECT * FROM organizations sees only orgs they're a member of; cannot UPDATE Org Y; cannot DELETE any org (status='archived' is the soft-delete path).
    - Test 11 (tests/sql/p28-schema-shape.test.sql): 8 assertions — each table has relrowsecurity=true and the expected column set.
  </behavior>
  <action>
    Create `src/lib/__tests__/_fixtures/p28-rls-fixture.ts` per research §G — exports `makeSlugPrefix(testFilename)`, `sessionFor(email)` (admin.generateLink + plain fetch /auth/v1/verify pattern, per [[reference_rls_fixture_gotrueclient_flake]]), `createTwoOrgsTwoUsers(prefix)` (creates 2 orgs + 2 users + maps A→X, B→Y; returns `{orgX, orgY, userA, userB, sessA, sessB}`), and an `afterAll` cleanup helper that deletes by `slug like '${prefix}-%'`.

    Create 8 test files in `src/lib/__tests__/`:
    - `rls-org-organizations.test.ts` — covers Test 10
    - `rls-org-members.test.ts` — covers Tests 3-5 against org_members
    - `rls-org-invites.test.ts` — covers Tests 3-5 + Test 6 (W-1 RPC invariant)
    - `rls-org-subscriptions.test.ts` — covers Tests 3-5 + Test 9
    - `rls-org-settings.test.ts` — covers Tests 3-5
    - `rls-org-branding.test.ts` — covers Tests 3-5
    - `rls-org-patient-links.test.ts` — covers Tests 3-5 + Test 7 (patient-self-read)
    - `rls-org-consent-grants.test.ts` — covers Tests 3-5 + Test 8 (_validate_consent_scope re-use)

    Each file:
    - Top of file: `const TEST_SLUG_PREFIX = makeSlugPrefix(__filename);` — file-scoped per [[feedback_rls_per_file_slug_prefix]].
    - `beforeAll` calls `createTwoOrgsTwoUsers(TEST_SLUG_PREFIX)`.
    - `afterAll` cleanup keyed by TEST_SLUG_PREFIX.
    - Use `it.skip(... [DEFERRED — note])` for any skip cases — NEVER `it.fixme` (per [[reference_vitest_skip_fixme]]).

    Create `tests/sql/p28-schema-shape.test.sql` — 8 select-assertion blocks, one per table:
    ```sql
    -- organizations
    select case when (select relrowsecurity from pg_class where relname='organizations') then 'OK' else 'FAIL: RLS not enabled on organizations' end;
    select case when (select count(*) from information_schema.columns where table_name='organizations' and column_name in ('id','slug','name','status','created_by','is_public_listing','current_rank_weights_version')) = 7 then 'OK' else 'FAIL: organizations missing columns' end;
    -- ... repeat for org_members, org_invites, org_subscriptions, org_settings, org_branding, org_patient_links, org_consent_grants
    ```

    Run all 8 RLS test files: `npx vitest run src/lib/__tests__/rls-org-*.test.ts`. Expected: all green; <45s wall time (per VALIDATION.md sampling rate).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/rls-org-*.test.ts --reporter=verbose 2>&amp;1 | tail -3 | grep -E 'Test Files\s+8 passed' &amp;&amp; supabase db query --linked --file tests/sql/p28-schema-shape.test.sql | grep -v 'FAIL'</automated>
  </verify>
  <done>8 RLS test files green; fixture helper exports 3 functions used by all 8; SQL shape test returns zero FAIL lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → org-scoped RLS-gated tables | Cross-tenant leak attack surface (V13-2(b)) |
| authenticated user → SECDEF RPC entry | Role gate inside RPC body (Pattern S1 dual-layer) |
| Edge Function → org_invites RPC | W-1 anti-enumeration boundary (addendum A2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Information Disclosure | org_members, org_invites, etc. — SELECT cross-tenant | mitigate (V13-2b) | RLS USING clause `exists(select 1 from org_members where org_id = T.org_id and user_id = auth.uid())` queries org_members directly (NOT JWT claim) per Pitfall 5; cross-tenant proof via 8 rls-org-*.test.ts files. |
| T-28-01-02 | Information Disclosure | org_invites email enumeration via send_org_invite | mitigate (V13-2 email collision vector) | send_org_invite + clinic-org-invite Edge Function return identical 200-status shape regardless of email-in-auth.users (addendum A2; W-1 invariant); rls-org-invites.test.ts Test 6 verifies. |
| T-28-01-03 | Tampering | org_consent_grants.scope jsonb shape drift | mitigate | CHECK constraint reuses Phase 9 `_validate_consent_scope` (CONTEXT D-18; one DB validator; no duplicate per [[feedback_planner_iter1_anti_patterns]] #4). |
| T-28-01-04 | Elevation of Privilege | send_org_invite called by non-admin | mitigate | Pattern S1 — RPC body: `if not exists (select 1 from org_members where user_id = auth.uid() and org_id = p_org_id and role = 'admin') then raise exception ... using errcode = '42501'; end if;`. |
| T-28-01-05 | Repudiation | org_invite issuance / revocation not logged | mitigate | Every SECDEF RPC calls Phase 24 `log_admin_action(action, target_org_id, details_jsonb)`. |
| T-28-01-06 | Information Disclosure | org_subscriptions stripe_customer_id leak to non-admins | mitigate | RLS SELECT for admins only; deny-all writes for authenticated; comment notes P29 owns writes via service_role. |
| T-28-01-07 | Denial of Service | org_members(user_id) full-scan in Custom Access Token Hook | mitigate (Pitfall 4) | Migration 04 creates `org_members_user_id_idx`; hook latency stays <50ms p95. |
| T-28-01-08 | Tampering | Migration filename letter-suffix silent skip | mitigate (Pitfall 7) | All 11 filenames are strict 14-digit timestamp + snake_case; `grep -c '^Skipping' supabase db push` output must be 0. |
| T-28-01-09 | Tampering | Append-only violation on org_consent_grants | mitigate | RLS UPDATE policy allows only `revoked_at` transition from null to a timestamp (per Phase 24 append-only RLS pattern). |
</threat_model>

<verification>
- 11 migrations pushed without `^Skipping` warnings.
- 8 RLS test files green (`npx vitest run src/lib/__tests__/rls-org-*.test.ts`).
- SQL schema-shape test returns zero FAIL lines.
- clinic-org-invite Edge Function deployed and W-1 invariant verified.
- All 4 SECDEF RPCs use `set search_path = pg_catalog, public, extensions` (per pg_proc query).
- org_invites status state machine has named owners for every transition (verified by grep of cron + RPC names).
</verification>

<success_criteria>
- Wave 1 schema slab complete: 8 org-scoped tables live with RLS + paired cross-tenant tests.
- W-1 anti-enumeration preserved on the new org-admin invite path (parallel to Phase 9 patient invites).
- org_consent_grants reuses Phase 9 _validate_consent_scope (single DB validator).
- pg_cron expiry purge active at 04:00 UTC.
- Wave 2 plans (03, 04, 05, 06) can now: query org_members from JWT hook; HMAC-derive channel names on top of the table allowlist; resolve org context from path slug.
</success_criteria>

<output>
After completion, create `.planning/phases/28-clinic-organizations-schema-rls-hardening/28-01-SUMMARY.md` capturing:
1. List of 11 migration filenames pushed (timestamp + name).
2. RLS test summary: 8 files, all green; total test count + runtime.
3. Edge Function deploy URL + W-1 parity-check result.
4. SQL schema-shape test output.
5. Open follow-ups: which downstream plans extend the `org_scoped_tables` allowlist (Plan 02 owns), which add to `allowed_table_regex` in realtime RLS policy (Plan 04 owns).
</output>
