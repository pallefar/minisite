---
phase: 28
plan: 02
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - supabase/functions/_shared/supabase-server.ts
  - supabase/functions/_shared/with-org-scope.ts
  - eslint-rules/no-raw-service-role-client.cjs
  - eslint-rules/__tests__/no-raw-service-role-client.test.cjs
  - eslint.config.js
  - src/lib/__tests__/with-org-scope.test.ts
autonomous: true
requirements:
  - ORG-03

must_haves:
  truths:
    - "`createClient(url, SUPABASE_SERVICE_ROLE_KEY)` outside `supabase/functions/_shared/supabase-server.ts` fails ESLint with rule `leanshot/no-raw-service-role-client` (compile-time fence)."
    - "`_createServiceRoleClientUnsafe()` returns a `ServiceRoleClient<'Unscoped'>` brand-typed value whose `.from(table)` method is typed `never` — TS callers cannot reach it without going through `withOrgScope`."
    - "`withOrgScope(orgId, fn)` returns `ServiceRoleClient<'OrgScoped'>` to the user fn; runtime Proxy interposes `.from(table)` for tables in `ORG_SCOPED_TABLES` allowlist; throws `OrgScopeBypassError` + emits `Sentry.captureException(level: 'fatal', tags: {org_scope_bypass: 'true'})` when `.then()` resolves without an `.eq('org_id', orgId)` filter."
    - "`ORG_SCOPED_TABLES` const exported from `supabase/functions/_shared/with-org-scope.ts` includes all 8 P28 tables (organizations, org_members, org_invites, org_subscriptions, org_settings, org_branding, org_patient_links, org_consent_grants); downstream phases extend per 28-EXTENSION-CONTRACT.md."
    - "Compile-time `// @ts-expect-error` proof that `_createServiceRoleClientUnsafe().from('any')` is typed `never` (Test 1)."
    - "Runtime proof that well-formed `withOrgScope(orgId, c => c.from('org_members').select().eq('org_id', orgId))` succeeds (Test 2)."
    - "Runtime proof that `withOrgScope(orgId, c => c.from('org_members').select())` (missing .eq) throws `OrgScopeBypassError` + Sentry fatal alert (Test 3)."
    - "Runtime proof that `withOrgScope(orgId, c => c.from('unscoped_table').select())` (non-org-scoped table) does NOT throw (Test 4 — allowlist gating)."
  artifacts:
    - path: "supabase/functions/_shared/supabase-server.ts"
      provides: "_createServiceRoleClientUnsafe() returning ServiceRoleClient<'Unscoped'> (branded type)"
      contains: "_createServiceRoleClientUnsafe"
    - path: "supabase/functions/_shared/with-org-scope.ts"
      provides: "withOrgScope(orgId, fn) + ORG_SCOPED_TABLES const + OrgScopeBypassError class + ServiceRoleClient<S> brand type"
      contains: "ORG_SCOPED_TABLES"
    - path: "eslint-rules/no-raw-service-role-client.cjs"
      provides: "ESLint rule blocking raw createClient(..., SERVICE_ROLE_KEY) outside supabase/functions/_shared/supabase-server.ts"
      contains: "raw-service-role-client"
    - path: "src/lib/__tests__/with-org-scope.test.ts"
      provides: "D-28 compile-time + runtime bypass test (4 cases)"
  key_links:
    - from: "withOrgScope Proxy interposition"
      to: "supabase-js PostgrestQueryBuilder thenable surface"
      via: "Recursive Proxy wrap on .from() return value; intercepts .eq/.in/.contains tracking + .then() resolution gate"
      pattern: "new Proxy.*get.*prop === 'from'"
    - from: "eslint-rules/no-raw-service-role-client.cjs"
      to: "eslint.config.js leanshot plugin block"
      via: "plugins.leanshot.rules['no-raw-service-role-client'] + rules['leanshot/no-raw-service-role-client'] = 'error'"
      pattern: "leanshot/no-raw-service-role-client"
    - from: "OrgScopeBypassError throw"
      to: "Sentry fatal alert"
      via: "Sentry.captureException(err, {level: 'fatal', tags: {org_scope_bypass: 'true'}})"
      pattern: "level:\\s*'fatal'"
---

<objective>
Wave 1 — ORG-03 4-layer defense: brand types (compile-time) + ESLint rule (build-time) + runtime Proxy (request-time) + Sentry alert (post-incident). Closes V13-2 service_role-bypass vector.

Purpose: Make it physically impossible to ship a service-role query that doesn't filter by org_id on an org-scoped table. Layered defense per CONTEXT D-04 — HIPAA stakes justify defense-in-depth.

Output: 4 source files + 1 ESLint rule + 1 ESLint rule test + 1 vitest suite covering all 4 layers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-CONTEXT.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-RESEARCH.md
@.planning/research/PITFALLS.md
@src/lib/supabase.ts
@eslint-rules/additive-only-events.cjs
@eslint.config.js

<interfaces>
<!-- Brand types — D-05 -->
```typescript
// supabase/functions/_shared/supabase-server.ts
import { createClient, type SupabaseClient } from '@supabase/supabase-js';

declare const __OrgScope: unique symbol;
export type ServiceRoleClient<S extends 'OrgScoped' | 'Unscoped'> =
  Omit<SupabaseClient, 'from' | 'rpc'> & {
    readonly [__OrgScope]: S;
  } & (S extends 'OrgScoped' ? { from: SupabaseClient['from']; rpc: SupabaseClient['rpc'] } : { from: never; rpc: SupabaseClient['rpc'] });

export function _createServiceRoleClientUnsafe(): ServiceRoleClient<'Unscoped'>;
// .rpc() still callable (SECDEF RPCs are the escape hatch per Pitfall 6); .from() typed never.
```

<!-- withOrgScope — D-07 + Research §Pattern 1 (Proxy-based) -->
```typescript
// supabase/functions/_shared/with-org-scope.ts
export const ORG_SCOPED_TABLES = new Set<string>([
  'organizations','org_members','org_invites','org_subscriptions',
  'org_settings','org_branding','org_patient_links','org_consent_grants',
  // Downstream phases extend per 28-EXTENSION-CONTRACT.md D-29
]);

export class OrgScopeBypassError extends Error {
  constructor(public table: string, public orgId: string);
}

export async function withOrgScope<T>(
  orgId: string,
  fn: (client: ServiceRoleClient<'OrgScoped'>) => Promise<T>,
): Promise<T>;
```

<!-- ESLint rule pattern — Research §F -->
```javascript
// eslint-rules/no-raw-service-role-client.cjs
// Forbids createClient(*, SERVICE_ROLE_KEY) outside supabase/functions/_shared/supabase-server.ts
// Pattern matches identifier name regex /SERVICE_ROLE_KEY$/ for both:
//   - Direct identifier: createClient(url, SUPABASE_SERVICE_ROLE_KEY)
//   - Env-getter: createClient(url, Deno.env.get('SUPABASE_SERVICE_ROLE_KEY'))
//   - Env-getter: createClient(url, process.env.SUPABASE_SERVICE_ROLE_KEY)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship supabase-server.ts + with-org-scope.ts (brand types + Proxy interposition + Sentry wiring)</name>
  <read_first>
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-RESEARCH.md §Pattern 1 (Proxy-based withOrgScope — 110 LoC skeleton with recursive Proxy + filter tracking + .then() gate)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-CONTEXT.md (D-04..D-08)
    - .planning/research/PITFALLS.md §V13-2(a) (service_role bypass attack vector)
    - src/lib/supabase.ts (browser client singleton — pattern target for `_createServiceRoleClientUnsafe`)
    - reference: memory [[reference_edit_tool_html_entity_encoding]] (Edit-tool quirk on `<` / `>` — prefer `.js` file via Write or single-quoted outer + double-quoted inner shell args)
  </read_first>
  <behavior>
    - Test 1 (compile-time, expect-error): `// @ts-expect-error` precedes `_createServiceRoleClientUnsafe().from('org_members')`; if the `@ts-expect-error` is unused (i.e., the call compiles), TypeScript errors out — proving `.from()` is typed `never`.
    - Test 2 (runtime, well-formed): `await withOrgScope('orgX', async c => { const r = await c.from('org_members').select().eq('org_id', 'orgX'); return r; })` resolves without throwing; no Sentry capture.
    - Test 3 (runtime, missing eq): `await withOrgScope('orgX', async c => c.from('org_members').select())` rejects with `OrgScopeBypassError`; `Sentry.captureException` mock called once with `level: 'fatal'` and tag `org_scope_bypass: 'true'`.
    - Test 4 (runtime, non-org-scoped table): `await withOrgScope('orgX', async c => c.from('audit_logs').select())` resolves without throwing (audit_logs is NOT in ORG_SCOPED_TABLES).
    - Test 5 (runtime, .in('org_id', [orgX])): array-based org filter ALSO counts as scoped — does NOT throw.
    - Test 6 (runtime, .eq('org_id', 'WRONG_ORG')): filter is present but value mismatches — throws OrgScopeBypassError (catches the "filtered but to wrong org" leak).
    - Test 7 (.rpc() unguarded): `withOrgScope('orgX', async c => c.rpc('some_rpc'))` does NOT throw (per CONTEXT D-07 + Pitfall 6 — .rpc() is policed by SECDEF function body, not the Proxy).
  </behavior>
  <action>
    Create `supabase/functions/_shared/supabase-server.ts`:
    - Imports `createClient`, `SupabaseClient` from `@supabase/supabase-js`.
    - Declares `__OrgScope: unique symbol`.
    - Exports `ServiceRoleClient<S>` brand type per interfaces block above. The `Unscoped` variant types `.from` as `never`; the `OrgScoped` variant exposes the full SupabaseClient surface.
    - Exports `_createServiceRoleClientUnsafe(): ServiceRoleClient<'Unscoped'>` — reads `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` from `Deno.env.get(...)` (Edge Function / Deno runtime, per **ADDENDUM A7**). Constructs `createClient(url, key, { auth: { persistSession: false } })` and casts via `as unknown as ServiceRoleClient<'Unscoped'>`. Throws at module-load if either env var is missing (Edge Function will fail to deploy — fail-loud is correct for service-role).
    - Add file header comment: `// THIS FILE IS THE ONLY PLACE allowed to construct a service-role client. Enforced by eslint-rules/no-raw-service-role-client.cjs.`

    Create `supabase/functions/_shared/with-org-scope.ts` per research §Pattern 1 (~120 LoC):
    - Imports `_createServiceRoleClientUnsafe`, brand types from `./supabase-server`.
    - Imports Sentry from `@sentry/deno` (per **ADDENDUM A7**) — Edge Functions are Deno. If a project-wide Edge Function Sentry init already exists in `supabase/functions/_shared/sentry.ts` (from Phase 25 D-15), import the existing client; otherwise ship a minimal `_shared/sentry.ts` here. Do NOT import `@sentry/node` — Node-only, fails at Deno deploy time.
    - Exports `ORG_SCOPED_TABLES = new Set<string>([...8 tables])` with comment pointing at 28-EXTENSION-CONTRACT.md for downstream phase additions.
    - Exports `OrgScopeBypassError` class.
    - Exports `withOrgScope<T>(orgId, fn)`:
      1. Validates `orgId` truthy.
      2. Calls `_createServiceRoleClientUnsafe()`.
      3. Wraps in outer Proxy intercepting `get(target, prop)`: if `prop === 'from'`, returns `(table: string) => { const builder = target.from(table); return ORG_SCOPED_TABLES.has(table) ? wrapBuilder(builder, table, orgId) : builder; }`. Otherwise `Reflect.get`.
      4. Returns `fn(proxiedClient as ServiceRoleClient<'OrgScoped'>)`.
    - Internal `wrapBuilder(builder, table, orgId)` — recursive Proxy per research §Pattern 1 lines 387-437:
      - Tracks `filterCalls: Array<{method, col, val}>` in a closure.
      - For `prop === 'eq' | 'in' | 'contains'`: pushes to filterCalls + re-wraps return.
      - For `prop === 'then'`: returns `(resolve, reject) => { const hasOrgScope = filterCalls.some(c => (c.method === 'eq' && c.col === 'org_id' && c.val === orgId) || (c.method === 'in' && c.col === 'org_id' && Array.isArray(c.val) && c.val.includes(orgId)) || (c.method === 'contains' && c.col === 'org_ids' && Array.isArray(c.val) && c.val.includes(orgId))); if (!hasOrgScope) { const err = new OrgScopeBypassError(table, orgId); Sentry.captureException(err, { level: 'fatal', tags: { org_scope_bypass: 'true' } }); return Promise.reject(err).then(resolve, reject); } return original.call(target, resolve, reject); }`.
      - For all other methods: re-wraps return value.

    CRITICAL — per CONTEXT D-07 + Pitfall 6: the Proxy does NOT police `.rpc()` calls. RPCs are gated by SECDEF function bodies (Pattern S1). The `withOrgScope` proxy only intercepts `.from(table)` for tables in `ORG_SCOPED_TABLES`.

    NO `any` types — use `unknown` + narrowing per `@typescript-eslint/no-explicit-any: error`. Cast at Proxy boundary via `as unknown as ServiceRoleClient<'OrgScoped'>`.

    NO `s.user!` non-null assertions (project anti-pattern per P23 D-05 carry-forward).

    File MUST NOT be importable from `src/` (browser) — lives in `supabase/functions/_shared/` (Deno runtime). Add explicit JSDoc warning at top: `* @internal Edge Function / Deno runtime only. Do not import from src/ (browser bundle).` Per **ADDENDUM A7**: use `Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')` instead of `process.env.SUPABASE_SERVICE_ROLE_KEY`; Sentry capture uses `@sentry/deno` (or the project's existing Edge Function Sentry init from Phase 25 D-15 if available — discover by reading `supabase/functions/_shared/` for an existing `sentry.ts`; create a minimal one if absent).
  </action>
  <verify>
    <automated>test -f supabase/functions/_shared/supabase-server.ts &amp;&amp; test -f supabase/functions/_shared/with-org-scope.ts &amp;&amp; grep -q 'ORG_SCOPED_TABLES' supabase/functions/_shared/with-org-scope.ts &amp;&amp; grep -q "Deno.env.get" supabase/functions/_shared/supabase-server.ts &amp;&amp; ! grep -q "process.env" supabase/functions/_shared/supabase-server.ts &amp;&amp; ! grep -q "@sentry/deno" supabase/functions/_shared/with-org-scope.ts &amp;&amp; npx tsc -p tsconfig.app.json --noEmit 2>&amp;1 | grep -cE 'error TS' | grep -q '^0$'</automated>
  </verify>
  <done>Both files exist; TypeScript compiles clean; ORG_SCOPED_TABLES exports all 8 table names; Sentry import wired with fatal level + org_scope_bypass tag.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship eslint-rules/no-raw-service-role-client.cjs + wire into eslint.config.js + rule unit test</name>
  <read_first>
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-RESEARCH.md §F (ESLint rule skeleton — ~50 LoC, matches both `Identifier` and `Deno.env.get('...SERVICE_ROLE_KEY')` patterns)
    - eslint-rules/additive-only-events.cjs (existing stylistic template — module.exports shape, meta/messages/schema fields, CallExpression visitor)
    - eslint.config.js (existing flat config — where to wire the new rule under `leanshot` plugin)
    - reference: memory [[reference_eslint_import_x_path_gotcha]] (file-target gotcha — `containsPath()` requires glob, not bare path)
  </read_first>
  <behavior>
    - Test 1: Linting a file containing `createClient(url, SUPABASE_SERVICE_ROLE_KEY)` outside `supabase/functions/_shared/supabase-server.ts` reports 1 error with messageId `raw-service-role-client`.
    - Test 2: Linting `supabase/functions/_shared/supabase-server.ts` containing the same code reports 0 errors.
    - Test 3: Linting `createClient(url, Deno.env.get('SUPABASE_SERVICE_ROLE_KEY'))` outside the allowed file reports 1 error.
    - Test 4: Linting `createClient(url, Deno.env.get('SUPABASE_SERVICE_ROLE_KEY'))` outside the allowed file reports 1 error (rule must match both `process.env.SUPABASE_SERVICE_ROLE_KEY` Node form AND `Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')` Deno form — see Task 2 rule logic).
    - Test 5: Linting `createClient(url, SUPABASE_ANON_KEY)` reports 0 errors (regex `/SERVICE_ROLE_KEY$/` does not match anon key).
    - Test 6: `npm run lint` over the full src/ tree exits 0 (no existing usages exist; the new rule does not regress).
  </behavior>
  <action>
    Create `eslint-rules/no-raw-service-role-client.cjs` per research §F:
    - `meta.type = 'problem'`.
    - `meta.docs.description` cites Phase 28 D-06.
    - `meta.messages['raw-service-role-client']` = clear instruction to use `withOrgScope` instead.
    - `create(context)` returns `CallExpression(node)` visitor:
      - If `context.filename` (or `context.getFilename?.()` fallback for older ESLint versions) ends with `supabase/functions/_shared/supabase-server.ts`, return empty handler set.
      - Match `node.callee.type === 'Identifier' && node.callee.name === 'createClient'`.
      - Require `node.arguments.length >= 2`.
      - Inspect `node.arguments[1]`:
        - If `Identifier` with `name` matching `/SERVICE_ROLE_KEY$/` → report.
        - If `MemberExpression` accessing `Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')` pattern (CallExpression with callee.property.name === 'get' and first arg Literal matching `/SERVICE_ROLE_KEY$/`) → report.
        - If `MemberExpression` accessing `process.env.SUPABASE_SERVICE_ROLE_KEY` (property.name matches `/SERVICE_ROLE_KEY$/`) → report.
        - If `CallExpression` matching `Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')` (callee.object.name === 'Deno' AND callee.object.property?.name === 'env' AND callee.property.name === 'get' AND arg[0].value matches `/SERVICE_ROLE_KEY$/`) → report. Per **ADDENDUM A7** the Edge Function form is the primary risk surface.

    Create `eslint-rules/__tests__/no-raw-service-role-client.test.cjs` using ESLint's `RuleTester`:
    - 6 valid + invalid case pairs covering Tests 1-5.
    - For `valid` cases, use `filename: 'supabase/functions/_shared/supabase-server.ts'` to test the allowlist exception.

    Wire into `eslint.config.js`:
    - In the existing `leanshot` plugin block (where `additive-only-events` is registered), add `'no-raw-service-role-client': require('./eslint-rules/no-raw-service-role-client.cjs')` to the plugin's rules map.
    - In the same flat-config block's `rules` field, add `'leanshot/no-raw-service-role-client': 'error'`.

    Run `npm run lint` to confirm no regressions: the rule reports 0 errors against existing src/ (no current `createClient(..., SERVICE_ROLE_KEY)` usages — confirmed via `grep -rn "createClient.*SERVICE_ROLE" src/`).

    Run the rule test directly: `node --test eslint-rules/__tests__/no-raw-service-role-client.test.cjs` (Node built-in test runner) OR `npx mocha eslint-rules/__tests__/no-raw-service-role-client.test.cjs` (whichever matches the existing `additive-only-events` test convention — discover via `find eslint-rules/__tests__ -name '*.cjs' | head`).
  </action>
  <verify>
    <automated>node --test eslint-rules/__tests__/no-raw-service-role-client.test.cjs 2>&amp;1 | tail -3 | grep -E '(pass|ok)' &amp;&amp; npm run lint 2>&amp;1 | grep -E 'no-raw-service-role-client' | grep -v 'supabase/functions/_shared/supabase-server.ts' | wc -l | grep -q '^0$'</automated>
  </verify>
  <done>Rule file + test + eslint.config.js wiring; rule tests green; `npm run lint` reports 0 violations of the new rule against existing src/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship src/lib/__tests__/with-org-scope.test.ts (D-28 — compile-time + runtime bypass tests)</name>
  <read_first>
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-CONTEXT.md (D-28 — exact test cases)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-VALIDATION.md (Wave 0 test file list)
    - supabase/functions/_shared/with-org-scope.ts (created in Task 1 — exports under test)
    - reference: memory [[reference_rls_fixture_gotrueclient_flake]] (admin auth client pattern for hitting the live DB)
  </read_first>
  <behavior>
    - Test 1 (compile-time, `// @ts-expect-error`): `_createServiceRoleClientUnsafe().from('org_members')` precedes a `// @ts-expect-error` comment. `npx tsc --noEmit` succeeds; if the brand type ever allowed `.from()` directly, TS would error on the unused `@ts-expect-error`.
    - Test 2 (runtime, well-formed): `withOrgScope('orgX', async c => await c.from('org_members').select('*').eq('org_id', 'orgX'))` resolves successfully; Sentry mock NOT called.
    - Test 3 (runtime, missing .eq): `withOrgScope('orgX', async c => await c.from('org_members').select('*'))` rejects with `OrgScopeBypassError`; Sentry mock called exactly once with first arg an `OrgScopeBypassError`, second arg matching `{ level: 'fatal', tags: { org_scope_bypass: 'true' } }`.
    - Test 4 (runtime, non-org-scoped table): `withOrgScope('orgX', async c => await c.from('audit_logs').select('*'))` resolves; Sentry NOT called.
    - Test 5 (runtime, .in alternate scope): `withOrgScope('orgX', async c => await c.from('org_members').select('*').in('org_id', ['orgX']))` resolves; Sentry NOT called.
    - Test 6 (runtime, wrong-value .eq): `withOrgScope('orgX', async c => await c.from('org_members').select('*').eq('org_id', 'orgY'))` rejects with OrgScopeBypassError (filter present but to wrong org).
    - Test 7 (runtime, .rpc() not policed): `withOrgScope('orgX', async c => await c.rpc('send_org_invite', {p_org_id: 'orgX', p_email: 'x@y.z', p_role: 'staff'}))` does NOT throw OrgScopeBypassError (per CONTEXT D-07 — Pitfall 6).
  </behavior>
  <action>
    Create `src/lib/__tests__/with-org-scope.test.ts`:
    - Use vi.mock for `@sentry/deno`: `vi.mock('@sentry/deno', () => ({ captureException: vi.fn() }))`. Per-test `vi.clearAllMocks()` in `beforeEach`.
    - Top-of-file pattern: import `{ withOrgScope, OrgScopeBypassError, _createServiceRoleClientUnsafe }` from `@/server/with-org-scope` and `@/server/supabase-server`.
    - Test 1 (type-level): use a separate `describe('compile-time fence', () => { it('typechecks _createServiceRoleClientUnsafe().from to be never', () => { const c = _createServiceRoleClientUnsafe(); // @ts-expect-error — .from should be typed never on Unscoped client const _ = c.from; expect(true).toBe(true); }); });`
    - Tests 2-7: runtime — use either (a) a live test DB pointed at via `SUPABASE_URL_TEST` env var + service role key + fixture org_members rows OR (b) a mocked PostgrestQueryBuilder. Choose (a) — matches Phase 9 clinic.test.ts convention and `[[reference_rls_fixture_gotrueclient_flake]]`. Use the fixture from `_fixtures/p28-rls-fixture.ts` (Plan 01 Task 3) — `createTwoOrgsTwoUsers` provides orgX + orgY.
    - Skip Test 6 if `.then()` is called only AFTER chaining `.eq` — verify the Proxy correctly captures the value comparison (researcher's pattern in §Pattern 1: `c.method === 'eq' && c.col === 'org_id' && c.val === orgId`).

    Per CONTEXT D-28: this test file is THE proof for ORG-03 (runtime). The ESLint rule test (Task 2) is THE proof for ORG-03 (lint). The `@ts-expect-error` test (Test 1) is THE proof for ORG-03 (compile-time). All three layers covered.

    Document in test file header: `// D-28 bypass tests — compile-time (@ts-expect-error) + runtime (Proxy interposition + Sentry alert). Covers ORG-03 success criterion #3.`
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/with-org-scope.test.ts --reporter=verbose 2>&amp;1 | tail -5 | grep -E 'Tests\s+[0-9]+ passed'</automated>
  </verify>
  <done>7 test cases green; Sentry mock asserts fatal-level + tag; @ts-expect-error proves compile-time `.from` is never.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server code → service_role client | Service-role bypasses RLS; any `.from(table)` without `.eq('org_id', ...)` on an org-scoped table is a cross-tenant leak (V13-2a) |
| build pipeline → developer commit | New code introducing raw `createClient(..., SERVICE_ROLE_KEY)` outside the allowed file (V13-2a via dev mistake) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Elevation of Privilege | service_role client bypasses RLS | mitigate (V13-2a) | Layered: (1) ESLint rule blocks raw construction outside supabase/functions/_shared/supabase-server.ts; (2) brand type makes `.from()` typed `never` on Unscoped client; (3) Proxy interposes `.from(table)` for ORG_SCOPED_TABLES and asserts `.eq('org_id', orgId)` at `.then()` time; (4) Sentry fatal alert if all 3 prior layers fail. |
| T-28-02-02 | Information Disclosure | Wrong-org_id filter passes Proxy gate | mitigate | Proxy compares `c.val === orgId` (not just presence of `.eq('org_id', _)`); Test 6 verifies. |
| T-28-02-03 | Tampering | .rpc() called inside withOrgScope without internal role gate | accept (documented) | Per CONTEXT D-07 + Pitfall 6 — Proxy explicitly does NOT police .rpc(); SECDEF function body MUST enforce caller role check (Pattern S1). Documented in withOrgScope JSDoc + 28-EXTENSION-CONTRACT.md. |
| T-28-02-04 | Repudiation | Bypass attempt not logged | mitigate | `Sentry.captureException` with `level: 'fatal'` + tag `org_scope_bypass: 'true'` — surfaces to ops dashboard. Phase 24 `log_admin_action` not used here because the bypass is server-side (no admin caller context). |
| T-28-02-05 | Tampering | Downstream plan adds new org-scoped table to DB but forgets to add to ORG_SCOPED_TABLES const | mitigate | 28-EXTENSION-CONTRACT.md (Plan 07) declares the extension checklist; plan-checker BLOCKER for downstream phases per CONTEXT D-08; Plan 07 ships the BLOCKER rule. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean — brand types compile.
- `node --test eslint-rules/__tests__/no-raw-service-role-client.test.cjs` green.
- `npx vitest run src/lib/__tests__/with-org-scope.test.ts` green (7 tests).
- `npm run lint` reports 0 violations of the new rule against current src/.
- ORG_SCOPED_TABLES contains all 8 P28 tables (verified by grep).
</verification>

<success_criteria>
- 4-layer defense live: brand types + ESLint rule + runtime Proxy + Sentry alert.
- Plans 03/04/05/06 can now wrap any service-role usage in `withOrgScope(orgId, async c => ...)` and inherit the defense.
- Downstream phases (P29, P30, P31, P40) will be plan-checker-BLOCKED if they add an org-scoped table without updating ORG_SCOPED_TABLES (rule shipped in Plan 07).
</success_criteria>

<output>
After completion, create `.planning/phases/28-clinic-organizations-schema-rls-hardening/28-02-SUMMARY.md` capturing:
1. ORG_SCOPED_TABLES contents (verify 8 table names).
2. ESLint rule test results.
3. with-org-scope.test.ts results.
4. Sample lint output showing the rule blocks raw createClient outside supabase/functions/_shared/supabase-server.ts.
5. Note for Plan 07: downstream phases extend ORG_SCOPED_TABLES + add plan-checker BLOCKER (referenced in 28-EXTENSION-CONTRACT.md).
</output>
