---
phase: 28
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20270601100001_p28_reconcile_orgs_to_organizations.sql
  - scripts/p28-rename-diff.sh
  - tests/sql/p28-rename-verification.test.sql
  - src/lib/clinic.ts
  - src/components/clinic/settings/ClinicSettingsPage.tsx
  - src/components/clinic/drill-in/ClinicDrillInPage.tsx
  - src/components/clinic/ClinicWorkspace.tsx
  - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-00-SUMMARY.md
autonomous: false
requirements:
  - ORG-01
user_setup:
  - service: supabase
    why: "Live ALTER TABLE on production data — operator confirms blast-radius audit before push"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "macOS keychain (per reference_stripe_legacy_key_and_supabase_token) or Supabase Dashboard -> Account -> Access Tokens"
    dashboard_config:
      - task: "Verify Phase 9 public.orgs row count BEFORE migration push"
        location: "Plan-0 Task 1 runs `supabase db query --linked` audit; operator approves checkpoint before Task 2 push"

must_haves:
  truths:
    - "public.orgs is renamed to public.organizations atomically (single transaction); zero data migration; zero downtime gap."
    - "Phase 9 RLS policies (SELECT/INSERT/UPDATE on memberships, invites, roles, role_permissions) are preserved bit-for-bit after rename (verified by re-running v1.1 cross-tenant fixtures)."
    - "All 4 src/ callsites of `.from('orgs')` (src/lib/clinic.ts:469, ClinicSettingsPage.tsx:108, ClinicDrillInPage.tsx:144, ClinicWorkspace.tsx:100) are patched to `.from('organizations')` in the SAME commit as the migration — no two-phase deploy gap."
    - "org_status enum exists with values 'active','suspended','archived'."
    - "organizations table has 3 new columns: status org_status default 'active', is_public_listing boolean default false, current_rank_weights_version uuid null."
    - "owner_user_id column is renamed to created_by; FK to auth.users(id) on delete restrict preserved."
    - "pg_dump --schema-only diff between pre and post migration shows ONLY the renamed table, renamed column, 3 added columns, and 1 added enum — no other schema drift."
  artifacts:
    - path: "supabase/migrations/20270601100001_p28_reconcile_orgs_to_organizations.sql"
      provides: "ALTER TABLE rename + column rename + 3 new columns + org_status enum in one transaction"
      contains: "ALTER TABLE public.orgs RENAME TO organizations"
    - path: "scripts/p28-rename-diff.sh"
      provides: "Pre/post pg_dump --schema-only diff shell script"
      contains: "pg_dump --schema-only"
    - path: "tests/sql/p28-rename-verification.test.sql"
      provides: "Post-rename FK + RLS preservation check"
      contains: "select count(*) from public.organizations"
    - path: ".planning/phases/28-clinic-organizations-schema-rls-hardening/28-00-SUMMARY.md"
      provides: "Pre-rename audit row counts, post-rename diff output, Phase 9 RLS regression sweep result"
  key_links:
    - from: "ALTER TABLE rename"
      to: "Phase 9 FK references in memberships, invites, roles, role_permissions"
      via: "Postgres pg_depend automatic rewrite on RENAME TABLE"
      pattern: "references organizations\\(id\\)"
    - from: "src/lib/clinic.ts:469"
      to: "renamed organizations table"
      via: "direct `.from('organizations')` callsite patch"
      pattern: "\\.from\\(['\\\"]organizations['\\\"]\\)"
---

<objective>
Plan-0 RECONCILE — close the live-schema gap between Phase 9's existing `public.orgs` table and CONTEXT D-11..D-29 which were written as if greenfield (per addendum A1/A5). Single non-parallel Wave 0 prerequisite for every other P28 plan.

Purpose: Eliminate the Q-A1 LANDMINE before any other migration ships. Without this plan, P28 plans 01-07 would attempt to create FKs against an `organizations` table that doesn't exist, while live data sits in `public.orgs`.

Output: Renamed `public.organizations` table with the 3 D-11 columns added, `org_status` enum, all 4 `.from('orgs')` callsites patched in the same commit as the migration, Phase 9 RLS regression sweep verified green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-CONTEXT.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-ADDENDUM-orgs-reconciliation.md
@.planning/phases/28-clinic-organizations-schema-rls-hardening/28-RESEARCH.md
@.planning/research/PITFALLS.md
@src/lib/clinic.ts

<interfaces>
<!-- The 4 src/ callsites that must be patched IN THE SAME COMMIT as the rename migration. -->
<!-- Confirmed via: grep -rn "from\s*['\"]orgs['\"]\|\.from(['\"]orgs['\"])" src/ -->

src/lib/clinic.ts:469      .from('orgs')    -> .from('organizations')
src/components/clinic/settings/ClinicSettingsPage.tsx:108   .from('orgs') -> .from('organizations')
src/components/clinic/drill-in/ClinicDrillInPage.tsx:144    .from('orgs') -> .from('organizations')
src/components/clinic/ClinicWorkspace.tsx:100               .from('orgs') -> .from('organizations')

<!-- Addendum A1 spec — table shape POST-RENAME: -->
public.organizations (
  id uuid pk,
  slug text unique not null,
  name text not null,
  created_at timestamptz default now(),
  created_by uuid references auth.users(id) on delete restrict,   -- renamed from owner_user_id
  status org_status default 'active',                              -- NEW (D-11)
  is_public_listing boolean default false,                         -- NEW (D-11; reserved v1.4)
  current_rank_weights_version uuid null,                          -- NEW (D-11; reserved P30)
  -- plus any Phase 9 columns preserved as-is (logo_storage_path, description, website_url, etc.)
);

create type org_status as enum ('active','suspended','archived');
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: HUMAN-CHECKPOINT — Pre-rename audit + operator approval</name>
  <read_first>
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-ADDENDUM-orgs-reconciliation.md (A1 Plan-0 task #1 — audit query)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-RESEARCH.md §Q-A1 (LANDMINE context)
  </read_first>
  <what-built>
    Pre-rename row-count audit captured. Operator confirms the migration is safe to push given the live row counts.

    Run the audit query (do NOT run this as part of an autonomous batch — operator gates):

    ```bash
    supabase db query --linked --query "
      select n.nspname as schema, c.relname as table, c.reltuples::bigint as approx_rows
      from pg_class c join pg_namespace n on n.oid = c.relnamespace
      where n.nspname = 'public' and c.relkind = 'r'
        and c.relname in ('orgs','organizations','memberships','invites','roles','role_permissions','consents','org_audit_logs')
      order by c.relname;
    "
    ```

    Capture the full output in `.planning/phases/28-clinic-organizations-schema-rls-hardening/28-00-SUMMARY.md` under heading `## Pre-Rename Audit (captured 2026-MM-DD)`.

    Also run the callsite grep:

    ```bash
    grep -rn "from\s*['\"]orgs['\"]\|\.from(['\"]orgs['\"])\|table.*['\"]orgs['\"]" src/ | head
    ```

    Expected: 4 hits per the interfaces block above. If MORE than 4 or DIFFERENT files, STOP and escalate — the codebase has drifted since plan was authored.
  </what-built>
  <how-to-verify>
    1. Confirm `public.orgs` exists and shows approx row count.
    2. Confirm `public.organizations` does NOT exist (audit row for `organizations` is absent or count is 0). If `public.organizations` is unexpectedly present → STOP + escalate (partial roll-forward from prior attempt; manual recovery required, not autopilot — per addendum A1).
    3. Confirm Phase 9 sibling tables (`memberships`, `invites`, `roles`, `role_permissions`) are present.
    4. Confirm grep returns exactly 4 callsites (or a number that matches the actual current state — record in SUMMARY).
    5. Confirm zero in-flight migrations on the supabase project (no pending `supabase db push` from another agent).
  </how-to-verify>
  <acceptance_criteria>
    - SUMMARY.md updated with the audit table.
    - Operator types "approved — proceed with rename".
    - If audit shows unexpected `organizations` row OR fewer/different `.from('orgs')` callsites than the interfaces block above → operator types "halt — investigate".
  </acceptance_criteria>
  <resume-signal>Type "approved — proceed with rename" or describe issues</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Write rename migration + pg_dump diff script + post-rename verification SQL fixture</name>
  <read_first>
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-ADDENDUM-orgs-reconciliation.md (A1 Plan-0 spec)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-CONTEXT.md (D-11 column spec)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-RESEARCH.md §Pitfall 1 + §Pitfall 7 (filename regex)
    - reference: memory [[reference_supabase_migration_gotchas]] (RENAME TABLE + string-literal SECDEF breakage)
    - reference: memory [[reference_supabase_migration_filename_regex]] (strict 14-digit)
  </read_first>
  <behavior>
    - Test 1 (tests/sql/p28-rename-verification.test.sql): `select count(*) from public.organizations` succeeds (returns row count >= 0).
    - Test 2: `select to_regclass('public.orgs')` returns NULL (renamed table is no longer accessible under the old name).
    - Test 3: `select unnest(enum_range(NULL::org_status))` returns the three values: active, suspended, archived.
    - Test 4: `select column_name from information_schema.columns where table_name = 'organizations' and column_name in ('status','is_public_listing','current_rank_weights_version','created_by')` returns 4 rows.
    - Test 5: `select column_name from information_schema.columns where table_name = 'organizations' and column_name = 'owner_user_id'` returns 0 rows (column renamed).
    - Test 6 (pg_dump diff): `bash scripts/p28-rename-diff.sh` produces a unified diff whose only changes are (a) `CREATE TYPE org_status`, (b) `CREATE TABLE organizations` (was `orgs`), (c) the 3 new columns, (d) the renamed column. No other schema drift.
  </behavior>
  <action>
    Create migration `supabase/migrations/20270601100001_p28_reconcile_orgs_to_organizations.sql` — single transaction with the following statements in order (per addendum A1):

    1. `create type if not exists org_status as enum ('active','suspended','archived');` — wrap the `create type` in a DO-block guard if `if not exists` is not supported on the deployed Postgres version (researcher's pattern: `do $$ begin if not exists (select 1 from pg_type where typname='org_status') then create type org_status as enum ('active','suspended','archived'); end if; end $$;`).
    2. `alter table public.orgs rename to organizations;`
    3. `alter table public.organizations rename column owner_user_id to created_by;` (if `owner_user_id` exists; verify schema by reading the Phase 9 migration name — researcher noted `supabase/migrations/20260801000002_orgs.sql`. If the column is named differently, plan-0 reconciles to `created_by uuid references auth.users(id) on delete restrict`.)
    4. `alter table public.organizations add column if not exists status org_status default 'active' not null;`
    5. `alter table public.organizations add column if not exists is_public_listing boolean default false not null;`
    6. `alter table public.organizations add column if not exists current_rank_weights_version uuid null;`
    7. Comment block at top of migration citing addendum A1 + CONTEXT D-11 for traceability.

    Filename MUST be strict 14-digit timestamp per `[[reference_supabase_migration_filename_regex]]` — `20270601100001_p28_reconcile_orgs_to_organizations.sql`. NO letter suffix (silently skipped per pitfall 7). Verify with `ls supabase/migrations/ | grep -E '^[0-9]{14}_.*\.sql$'` after creation.

    Create `scripts/p28-rename-diff.sh`:

    ```bash
    #!/usr/bin/env bash
    # Pre/post pg_dump --schema-only diff for P28 Plan-0 rename (addendum A1).
    # Usage: bash scripts/p28-rename-diff.sh <before.sql> <after.sql>
    # Exits 0 on success (diff written to stdout); 1 on error.
    set -euo pipefail
    BEFORE="${1:?pre-rename schema dump path required}"
    AFTER="${2:?post-rename schema dump path required}"
    diff -u "$BEFORE" "$AFTER" || true   # diff returns 1 when files differ; we want the output, not the exit
    ```

    chmod +x the script.

    Create `tests/sql/p28-rename-verification.test.sql` with 6 select statements (Tests 1-5 above + a 6th for FK targets: `select conname, confrelid::regclass from pg_constraint where contype='f' and confrelid='public.organizations'::regclass`). Each select runs as a sanity assertion via `supabase db query --linked --file tests/sql/p28-rename-verification.test.sql`.

    Do NOT push the migration in this task — Task 2 owns the push (separates author/run for verification).
  </action>
  <verify>
    <automated>ls supabase/migrations/20270601100001_p28_reconcile_orgs_to_organizations.sql && ls scripts/p28-rename-diff.sh && ls tests/sql/p28-rename-verification.test.sql && grep -E '^[0-9]{14}_.*\.sql$' &lt;(ls supabase/migrations/20270601100001_p28_reconcile_orgs_to_organizations.sql | xargs -n1 basename) && grep -c 'alter table public.orgs rename to organizations' supabase/migrations/20270601100001_p28_reconcile_orgs_to_organizations.sql | grep -q '^1$'</automated>
  </verify>
  <done>Migration file + diff script + verification SQL exist; filename matches strict regex; rename + 3 new columns + enum present in migration body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Patch 4 .from('orgs') callsites + push migration via supabase db push (BLOCKING — Step 5.7)</name>
  <read_first>
    - src/lib/clinic.ts (line 469 — single `.from('orgs')` callsite)
    - src/components/clinic/settings/ClinicSettingsPage.tsx (line 108)
    - src/components/clinic/drill-in/ClinicDrillInPage.tsx (line 144)
    - src/components/clinic/ClinicWorkspace.tsx (line 100)
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-ADDENDUM-orgs-reconciliation.md (A1 Plan-0 task #2 — caller patch in SAME commit as migration)
    - reference: memory [[reference_supabase_worktree_temp_state]] (CLI .temp state requirement)
  </read_first>
  <behavior>
    - Test 1: `grep -rn "\.from(['\"]orgs['\"])" src/` returns 0 matches after patch.
    - Test 2: `grep -rn "\.from(['\"]organizations['\"])" src/` returns 4 matches (one per file in read_first).
    - Test 3: `supabase db push --linked` succeeds without "Skipping" warnings in output.
    - Test 4: `supabase db query --linked --file tests/sql/p28-rename-verification.test.sql` returns expected rows for all 6 select statements (Tests 1-6 from previous task).
  </behavior>
  <action>
    Patch the 4 callsites in a single atomic edit batch — replace `.from('orgs')` with `.from('organizations')` in:
    - src/lib/clinic.ts:469
    - src/components/clinic/settings/ClinicSettingsPage.tsx:108
    - src/components/clinic/drill-in/ClinicDrillInPage.tsx:144
    - src/components/clinic/ClinicWorkspace.tsx:100

    Do NOT modify any other content in those files (clinic.ts D-01 lock — Phase 9 wrappers stay unchanged except for this one literal-string update; ClinicSettingsPage / ClinicDrillInPage / ClinicWorkspace are Phase 9 UI components — only the literal changes).

    Capture pre-push schema dump: `supabase db dump --linked --schema-only > /tmp/p28-before.sql` (operator-supplied or pulled via `supabase db dump`).

    Push the migration: `SUPABASE_ACCESS_TOKEN=<token> supabase db push --linked`. Non-TTY workaround per `[[reference_supabase_worktree_temp_state]]` if running inside a worktree: copy `supabase/.temp/` from main checkout first.

    BLOCKING: scan push output for `^Skipping` lines (per pitfall 7). If ANY skipping line appears → abort, fix filename, retry.

    Capture post-push schema dump: `supabase db dump --linked --schema-only > /tmp/p28-after.sql`.

    Run `bash scripts/p28-rename-diff.sh /tmp/p28-before.sql /tmp/p28-after.sql > .planning/phases/28-clinic-organizations-schema-rls-hardening/p28-rename-diff.txt` and append the diff to 28-00-SUMMARY.md.

    Run `supabase db query --linked --file tests/sql/p28-rename-verification.test.sql` and capture output in SUMMARY.

    Commit (single commit per addendum A1 — no two-phase deploy gap): `feat(28-00): RECONCILE orgs -> organizations (rename + 3 cols + 4 callsites, per addendum A1)`.

    Per [[reference_supabase_migration_gotchas]]: post-migration, grep for any literal `'orgs'` token inside SECDEF function bodies in the schema dump: `grep -n "'orgs'" /tmp/p28-after.sql | grep -v '^#'`. Expected: 0 hits (Postgres rewrites pg_depend on RENAME; only `EXECUTE format(...)` string-literal references would remain). If hits found, patch those function bodies in this same commit.
  </action>
  <verify>
    <automated>grep -v '^#' src/lib/clinic.ts src/components/clinic/settings/ClinicSettingsPage.tsx src/components/clinic/drill-in/ClinicDrillInPage.tsx src/components/clinic/ClinicWorkspace.tsx | grep -c "\.from(['\"]orgs['\"])" | grep -q '^0$' &amp;&amp; grep -v '^#' src/lib/clinic.ts src/components/clinic/settings/ClinicSettingsPage.tsx src/components/clinic/drill-in/ClinicDrillInPage.tsx src/components/clinic/ClinicWorkspace.tsx | grep -c "\.from(['\"]organizations['\"])" | grep -q '^4$'</automated>
  </verify>
  <done>All 4 callsites patched; migration pushed without skipping; rename verification SQL passes; diff captured in SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Phase 9 RLS regression sweep — verify membership/invite cross-tenant isolation preserved post-rename</name>
  <read_first>
    - .planning/phases/28-clinic-organizations-schema-rls-hardening/28-ADDENDUM-orgs-reconciliation.md (A1 Plan-0 task #3 — RLS regression check)
    - src/lib/clinic.test.ts (existing v1.1 cross-tenant fixture — exact path for clinic-level tests)
    - reference: memory [[reference_rls_fixture_gotrueclient_flake]] (admin.generateLink + plain fetch /auth/v1/verify)
  </read_first>
  <behavior>
    - Test 1: `npx vitest run src/lib/clinic.test.ts src/lib/clinic-permissions.test.ts` exits 0 (all v1.1 RLS tests still pass against the renamed table).
    - Test 2: No test references the old `'orgs'` literal in `.from(...)` — `grep -rn "\.from(['\"]orgs['\"])" src/lib/__tests__/ src/lib/*.test.ts` returns 0.
    - Test 3: If any existing test file referenced `from('orgs')`, it is updated to `from('organizations')` in this same commit.
  </behavior>
  <action>
    Run existing Phase 9 test suite against the renamed table:

    ```bash
    npx vitest run src/lib/clinic.test.ts src/lib/clinic-permissions.test.ts
    ```

    If any test references `.from('orgs')` (grep returns matches), patch those test files to `.from('organizations')` in this same commit.

    If any test FAILS (red after rename), investigate per addendum A1 task #3: revert migration via `supabase migration repair --status reverted <timestamp>` and escalate to operator (do NOT auto-revert in this task — operator must approve the revert path).

    Append the test-run output to 28-00-SUMMARY.md under heading `## Phase 9 RLS Regression Sweep`.

    Commit message: `test(28-00): verify Phase 9 RLS preserved after orgs -> organizations rename`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/clinic.test.ts src/lib/clinic-permissions.test.ts --reporter=verbose 2>&amp;1 | tail -5 | grep -E 'Test Files\s+[0-9]+ passed'</automated>
  </verify>
  <done>Existing Phase 9 vitest suite green against renamed table; SUMMARY.md captures the test output; no `.from('orgs')` literals remain anywhere in src/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → supabase production DB | ALTER TABLE on live data; non-reversible without rollback migration |
| commit → production deploy | Vercel auto-deploys on main merge; the 4 callsite patches + migration MUST land in the same commit to avoid runtime 42P01 (relation does not exist) errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-00-01 | Tampering | ALTER TABLE rename | mitigate (V13-2a) | Wrap in single transaction; pg_dump pre/post diff via `scripts/p28-rename-diff.sh` proves only the intended changes landed. |
| T-28-00-02 | Information Disclosure | Phase 9 RLS preservation | mitigate (V13-2b) | Phase 9 cross-tenant vitest fixtures re-run in Task 3; any regression aborts the deploy. RLS policies reference table by oid, not name — RENAME preserves them. |
| T-28-00-03 | Denial of Service | Deploy gap between rename and callsite patch | mitigate | Single commit, single deploy — no two-phase window where production code calls `.from('orgs')` against a renamed table. |
| T-28-00-04 | Tampering | SECDEF function string-literal `'orgs'` references | mitigate | Post-migration grep on schema dump for `'orgs'` literal in function bodies; patch any hits in same commit. Postgres auto-rewrites pg_depend for direct table refs but NOT for string-literal `EXECUTE format(...)` references. |
| T-28-00-05 | Repudiation | Pre-rename audit | mitigate | Audit row counts captured in 28-00-SUMMARY.md before migration; any unexpected `organizations` row triggers escalation per addendum A1 task #1. |
</threat_model>

<verification>
- Migration filename matches strict 14-digit regex (Pitfall 7).
- `supabase db push --linked` output contains zero `^Skipping` lines.
- pg_dump pre/post diff shows ONLY the rename + 3 new columns + enum + renamed column.
- All 4 `.from('orgs')` callsites are patched (grep returns 0 matches for old, 4 for new).
- Phase 9 vitest cross-tenant suite (clinic.test.ts, clinic-permissions.test.ts) passes against the renamed table.
- 28-00-SUMMARY.md contains: pre-rename audit, post-rename diff, RLS regression test output.
</verification>

<success_criteria>
- `public.organizations` exists with 3 new columns + renamed `created_by` column + `org_status` enum.
- Zero downtime between rename and callsite patch (single commit, single deploy).
- Phase 9 cross-tenant isolation preserved (existing tests green).
- Wave 1 plans (28-01, 28-02) and Wave 2 plans (28-03, 28-04, 28-05, 28-06) can now reference `organizations(id)` in their migrations without FK errors.
</success_criteria>

<output>
After completion, create `.planning/phases/28-clinic-organizations-schema-rls-hardening/28-00-SUMMARY.md` capturing:
1. Pre-rename audit row counts (Task 0)
2. Migration file path + diff output (Task 2)
3. Phase 9 RLS regression sweep results (Task 3)
4. List of 4 patched callsites with line numbers
5. Open follow-ups for downstream plans (none expected; if any, list them explicitly)
</output>
