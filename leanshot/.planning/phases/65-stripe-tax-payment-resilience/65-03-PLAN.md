---
phase: 65-stripe-tax-payment-resilience
plan: 03
type: execute
wave: 2
depends_on: [65-01]
files_modified:
  - supabase/functions/stripe-checkout/index.ts
  - supabase/functions/stripe-checkout/index.test.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03]
must_haves:
  truths:
    - "Every Stripe Checkout session created in stripe-checkout/index.ts includes automatic_tax: { enabled: true }"
    - "Every Checkout session sets customer_update: { address: 'auto', name: 'auto' } so address-on-file feeds Tax calc"
    - "B2B clinic-org Checkout sessions ALSO set tax_id_collection: { enabled: true }"
    - "Consumer (web/lifetime) sessions DO NOT set tax_id_collection (avoid unnecessary B2C friction)"
    - "Existing tests + 4 new tests pass — verifying the param shape across web/lifetime/clinic flows"
  artifacts:
    - path: "supabase/functions/stripe-checkout/index.ts"
      provides: "automatic_tax + customer_update on every session; tax_id_collection on clinic sessions"
      contains: "automatic_tax"
    - path: "supabase/functions/stripe-checkout/index.test.ts"
      provides: "Test cases asserting session params include automatic_tax (all flows) + tax_id_collection (clinic only)"
      contains: "automatic_tax"
  key_links:
    - from: "stripe-checkout/index.ts handleSession"
      to: "stripe.checkout.sessions.create"
      via: "sessionParams object construction at line ~568"
      pattern: "automatic_tax.*enabled.*true"
---

<objective>
Extend `stripe-checkout/index.ts` to comply with Stripe Tax: add `automatic_tax`, `customer_update`, and B2B-only `tax_id_collection` to every Checkout session. Mirror collected tax IDs to `org_subscriptions.tax_id` via the existing `checkout.session.completed` webhook event handler (delegated to Plan 65-04).

Purpose: PAY-01/02/03 are the headline pre-launch tax-compliance items. Stripe Tax must be enabled in Dashboard BEFORE these flags take effect — that operator action is documented in close-out Plan 65-10. The CODE side of PAY-01/02/03 is wholly contained here.

Output: Modified `stripe-checkout/index.ts` (session params for web/lifetime/clinic flows) + new test cases in `index.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/PROJECT.md
@leanshot/.planning/phases/65-stripe-tax-payment-resilience/65-CONTEXT.md

# CURRENT checkout entry (Phase 14 base + Phase 43 lifetime extension)
@supabase/functions/stripe-checkout/index.ts
@supabase/functions/stripe-checkout/index.test.ts

<interfaces>
From stripe-checkout/index.ts:
- `handleSession(req: Request): Promise<Response>` at line ~344 — builds `sessionParams` then calls `stripeInstance.checkout.sessions.create(sessionParams)` at line ~568.
- The function branches on `plan` (line ~540): `'lifetime'` → mode='payment' + payment_intent_data, else → mode='subscription' + subscription_data (web/clinic both subscriptions).
- A separate code path handles clinic flows — search for `STRIPE_PRICE_CLINIC_BASE` / `STRIPE_PRICE_CLINIC_OVERAGE` / `ensureClinicCustomer` to find the clinic line_items branch.
- Test harness uses `__setStripeForTest(stub)` + `__setAdminForTest(fakeAdmin)` for DI.

Stripe SDK: pinned `stripe@19?target=denonext`, API version `2026-04-22.dahlia`. `automatic_tax` + `customer_update` + `tax_id_collection` are first-class top-level params on `Stripe.Checkout.SessionCreateParams` in this SDK version (Context7 reference if needed).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add automatic_tax + customer_update to ALL sessions; tax_id_collection on clinic only</name>
  <files>supabase/functions/stripe-checkout/index.ts, supabase/functions/stripe-checkout/index.test.ts</files>
  <behavior>
    - Test "web subscription session includes automatic_tax: { enabled: true }" — stub stripe, call handleSession with plan='plus_monthly', assert `sessionParams.automatic_tax.enabled === true`.
    - Test "web subscription session includes customer_update: { address: 'auto', name: 'auto' }" — same flow, assert both fields.
    - Test "web subscription session does NOT include tax_id_collection" — assert `sessionParams.tax_id_collection === undefined`.
    - Test "clinic subscription session includes tax_id_collection: { enabled: true }" — call handleSession with plan='clinic', assert `sessionParams.tax_id_collection.enabled === true` AND `automatic_tax.enabled === true` AND `customer_update.address === 'auto'`.
    - Test "lifetime payment session includes automatic_tax + customer_update" — call with plan='lifetime' (mode='payment'), assert both fields present. NOTE: for mode='payment', `customer_update` requires a pre-existing Stripe customer (which the flow already creates via ensureWebCustomer); no special handling needed.
  </behavior>
  <action>
Per D-01 + D-02 + D-03 (CONTEXT.md "Stripe Tax + B2B tax_id Collection"):

In `handleSession` in `supabase/functions/stripe-checkout/index.ts`, locate the `sessionParams` construction near line 540-568. Add three modifications:

1. **Unconditional on every session** (web subscription, web lifetime, clinic subscription): set top-level fields on `sessionParams`:
   - `automatic_tax: { enabled: true }`
   - `customer_update: { address: 'auto', name: 'auto' }`

2. **Clinic-only**: when the code path is the clinic line-items branch (gated on `STRIPE_PRICE_CLINIC_BASE` / `ensureClinicCustomer` call), additionally set:
   - `tax_id_collection: { enabled: true }`

3. **Web/consumer + lifetime**: do NOT set `tax_id_collection` (CONTEXT.md: "Consumer flows: customer-address-on-file feeds Tax calc; no extra collection").

Implementation notes:
- Place the unconditional fields BEFORE the plan-branching `if (plan === 'lifetime') { ... } else { ... }` block so both branches inherit them.
- Place `tax_id_collection` assignment inside the clinic-detection branch (NOT inside the lifetime branch; clinic is a `mode='subscription'` flow with the clinic price-IDs).
- Add a comment block above the additions citing PAY-01/PAY-02/PAY-03 + D-01/D-02/D-03 + the Phase 65 CONTEXT.md operator-gate requirement: "Stripe Tax must be enabled in Dashboard BEFORE deploy — Plan 65-10 close-out documents the manual step".
- Do NOT remove or re-arrange existing fields (`payment_method_collection: 'always'`, `discounts`, `subscription_data.trial_period_days: 7` are all Phase 14/43 invariants).

For `index.test.ts`: extend the existing test suite with the 5 cases above. Mirror the existing stub patterns in this file (look for `__setStripeForTest` usage to see how to intercept `sessions.create` params). Capture the `sessionParams` argument via a `vi.fn`-style spy or a stub that stores the arg in a module-scoped variable.

DO NOT touch the webhook handler in this plan — `checkout.session.completed` → `org_subscriptions.tax_id` mirror is Plan 65-04's responsibility.

DO NOT touch the affiliate-payout checkout flow (search for `affiliate-payout` Edge Fn separately; PAY-01 wording "consumer + clinic + affiliate-payout where applicable" — affiliate payouts are Stripe Connect Express payouts, not Checkout sessions, so they are NOT in scope for `stripe-checkout`. Add a clarifying comment in the diff header noting this).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && $HOME/.deno/bin/deno test --no-check --allow-env --allow-net supabase/functions/stripe-checkout/index.test.ts 2>&1 | tail -10 | grep -q "ok"</automated>
  </verify>
  <done>handleSession produces sessionParams with automatic_tax + customer_update on every plan branch; tax_id_collection only on clinic branch; all existing tests pass + 5 new tests pass; no regression to lifetime/web/clinic happy paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user JWT → stripe-checkout | JWT-authed POST to /session creates Stripe session |
| stripe-checkout → Stripe API | sessionParams transit to Stripe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03-01 | Information Disclosure | tax_id transit | mitigate | tax_id_collection is Stripe-hosted UI — Stripe stores the tax_id, we receive only the validated value on `checkout.session.completed`. No tax_id ever transits our Edge Fn from the browser. |
| T-65-03-02 | Tampering | automatic_tax bypass | accept | If Stripe Dashboard "Enable Tax" is OFF, `automatic_tax: { enabled: true }` returns Stripe error — surfaces at deploy-time smoke test (Plan 65-10), not in production. |
| T-65-03-03 | Repudiation | tax calc audit | mitigate (defer) | Tax calc audit lands via `checkout.session.completed` webhook → `tax_collection_log` insert in Plan 65-04. This plan only sends the request flag. |
</threat_model>

<verification>
- `grep -c "automatic_tax" supabase/functions/stripe-checkout/index.ts` ≥ 2 (header comment + sessionParams assignment)
- `grep -c "tax_id_collection" supabase/functions/stripe-checkout/index.ts` ≥ 1 (clinic branch only)
- All existing tests in index.test.ts still pass
- 5 new tests cover web/clinic/lifetime × automatic_tax/customer_update/tax_id_collection presence-absence matrix
</verification>

<success_criteria>
PAY-01/02/03 code-side requirements satisfied: every Checkout session uses Stripe Tax + customer_update; B2B clinic sessions collect tax IDs. Mirror to `org_subscriptions.tax_id` is wired in Plan 65-04. Operator dashboard enablement documented in Plan 65-10.
</success_criteria>

<output>
Create `.planning/phases/65-stripe-tax-payment-resilience/65-03-SUMMARY.md` when done.
</output>
