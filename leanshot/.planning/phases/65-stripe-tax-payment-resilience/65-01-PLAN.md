---
phase: 65-stripe-tax-payment-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20290104000001_org_subscriptions_tax_id.sql
  - supabase/migrations/20290104000002_subscriptions_dunning_state.sql
  - supabase/migrations/20290104000003_dunning_emails_sent.sql
  - supabase/migrations/20290104000004_refunds.sql
  - supabase/migrations/20290104000005_lifecycle_emails_sent.sql
  - supabase/migrations/20290104000006_tax_collection_log.sql
  - supabase/migrations/20290104000007_tax_nexus_thresholds.sql
  - supabase/migrations/20290104000008_tax_nexus_state_revenue_matview.sql
autonomous: true
requirements: [PAY-03, PAY-04, PAY-05, PAY-07, PAY-08, PAY-10, PAY-11]
must_haves:
  truths:
    - "org_subscriptions has tax_id column (nullable text) for B2B Stripe Tax mirror"
    - "subscriptions has dunning_state column with enum-constrained values"
    - "dunning_emails_sent table enforces idempotency by (subscription_id, stage) composite PK"
    - "refunds table records every Stripe refund with surrogate id PK + audit columns"
    - "lifecycle_emails_sent table enforces idempotency by (user_id, stage) composite PK"
    - "tax_collection_log table records Stripe Tax calculations"
    - "tax_nexus_thresholds seeded with US economic-nexus thresholds (≥10 states)"
    - "tax_nexus_state_revenue matview aggregates per-state revenue + threshold-proximity %"
  artifacts:
    - path: "supabase/migrations/20290104000001_org_subscriptions_tax_id.sql"
      provides: "ALTER TABLE org_subscriptions ADD COLUMN tax_id text"
      contains: "alter table public.org_subscriptions"
    - path: "supabase/migrations/20290104000002_subscriptions_dunning_state.sql"
      provides: "subscriptions.dunning_state column + last_dunning_email_at"
      contains: "dunning_state"
    - path: "supabase/migrations/20290104000003_dunning_emails_sent.sql"
      provides: "dunning_emails_sent audit table"
      contains: "create table public.dunning_emails_sent"
    - path: "supabase/migrations/20290104000004_refunds.sql"
      provides: "refunds audit table with surrogate id PK"
      contains: "create table public.refunds"
    - path: "supabase/migrations/20290104000005_lifecycle_emails_sent.sql"
      provides: "lifecycle_emails_sent idempotency table"
      contains: "create table public.lifecycle_emails_sent"
    - path: "supabase/migrations/20290104000006_tax_collection_log.sql"
      provides: "tax_collection_log table"
      contains: "create table public.tax_collection_log"
    - path: "supabase/migrations/20290104000007_tax_nexus_thresholds.sql"
      provides: "tax_nexus_thresholds table + US seed data"
      contains: "create table public.tax_nexus_thresholds"
    - path: "supabase/migrations/20290104000008_tax_nexus_state_revenue_matview.sql"
      provides: "tax_nexus_state_revenue materialized view"
      contains: "create materialized view public.tax_nexus_state_revenue"
  key_links:
    - from: "subscriptions.dunning_state"
      to: "dunning_emails_sent.stage"
      via: "stage value matches dunning_state value for current row"
      pattern: "dunning_state.*first_failed|second_failed|final_warning"
    - from: "tax_nexus_state_revenue"
      to: "tax_nexus_thresholds"
      via: "join on state column"
      pattern: "join.*tax_nexus_thresholds"
---

<objective>
Create all Phase 65 database schema in 8 migration files (timestamps 20290104000001-000008) so downstream Edge Fns + UI in Wave 2 have stable contracts.

Purpose: Phase 65 ships 6 Edge Fns + 3 UI surfaces + 1 cron — they all depend on the same row shapes. One foundation plan unblocks all of Wave 2 in parallel.

Output: 8 migration files (no `IF NOT EXISTS` on CREATE POLICY per project convention); seed data inline for tax_nexus_thresholds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/PROJECT.md
@leanshot/.planning/ROADMAP.md
@leanshot/.planning/STATE.md
@leanshot/.planning/phases/65-stripe-tax-payment-resilience/65-CONTEXT.md

# Analog: Phase 64 migration shape — bare CREATE POLICY, timestamptz, surrogate UUID PK, RLS-enabled
@supabase/migrations/20290103000001_privacy_optout_requests.sql
@supabase/migrations/20290103000003_ad_targeting_exclusion.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-table column additions and audit tables (5 migrations)</name>
  <files>supabase/migrations/20290104000001_org_subscriptions_tax_id.sql, supabase/migrations/20290104000002_subscriptions_dunning_state.sql, supabase/migrations/20290104000003_dunning_emails_sent.sql, supabase/migrations/20290104000004_refunds.sql, supabase/migrations/20290104000005_lifecycle_emails_sent.sql</files>
  <action>
Create 5 migration files in monorepo `supabase/migrations/` (NOT under `leanshot/`). Use exact timestamps `20290104000001` through `20290104000005` — Phase 64 ended at `20290103000005` per [[reference_supabase_back_dated_migration_blocks_push]].

**Migration 20290104000001_org_subscriptions_tax_id.sql** (PAY-03 mirror): `alter table public.org_subscriptions add column if not exists tax_id text;` plus a btree index `idx_org_subscriptions_tax_id` (partial: `where tax_id is not null`). No RLS change — table already has RLS.

**Migration 20290104000002_subscriptions_dunning_state.sql** (PAY-07): Add two columns to `public.subscriptions`:
- `dunning_state text` with CHECK constraint `dunning_state in ('none','first_failed','second_failed','final_warning','cancelled_for_payment') or dunning_state is null` (NULL = never failed, equivalent to 'none')
- `last_dunning_email_at timestamptz`
Add partial index `idx_subscriptions_dunning_state` `where dunning_state is not null and dunning_state <> 'cancelled_for_payment'` so the orchestrator cron can scan only active dunning rows.

**Migration 20290104000003_dunning_emails_sent.sql** (PAY-05): Create table `public.dunning_emails_sent` with composite PK `(subscription_id, stage)` for ON-CONFLICT-DO-NOTHING idempotency per Phase 14 webhook pattern. Columns: `subscription_id uuid not null references public.subscriptions(id) on delete cascade`, `stage text not null check (stage in ('first_failed','second_failed','final_warning'))`, `sent_at timestamptz not null default now()`, `resend_message_id text`. RLS: enable, then bare `create policy "service_role_all" on public.dunning_emails_sent for all to service_role using (true) with check (true);` plus a SELECT policy for staff via `public.is_staff()` per [[reference_supabase_is_staff_helper]].

**Migration 20290104000004_refunds.sql** (PAY-08): Create table `public.refunds` with **surrogate** `id uuid not null default gen_random_uuid() primary key` per [[reference_dual_auth_submission_table_pk_pattern]] (NOT composite — stripe_refund_id can repeat across retries). Columns: `subscription_id uuid not null references public.subscriptions(id) on delete cascade`, `stripe_refund_id text not null unique`, `stripe_payment_intent_id text not null`, `amount_cents integer not null check (amount_cents >= 0)`, `currency text not null default 'usd'`, `status text not null check (status in ('pending','succeeded','failed','canceled'))`, `reason text`, `eligibility_window text not null check (eligibility_window in ('trial','money_back_14d'))`, `created_at timestamptz not null default now()`, `updated_at timestamptz not null default now()`. RLS: service_role all + SELECT-own via `auth.uid() = (select user_id from public.subscriptions where id = subscription_id)`.

**Migration 20290104000005_lifecycle_emails_sent.sql** (PAY-10/11): Create table `public.lifecycle_emails_sent` with composite PK `(user_id, stage)`. Columns: `user_id uuid not null references auth.users(id) on delete cascade`, `stage text not null check (stage in ('trial_ending_t3d','trial_ending_t1d','winback_t30d','winback_t60d','winback_t90d'))`, `sent_at timestamptz not null default now()`, `resend_message_id text`, `copy_variant text` (PostHog A/B variant for trial_ending_*), `promo_code text` (winback-only, Stripe Promotion Code id). RLS: service_role all.

For ALL tables: wrap in `begin; ... commit;` block. Add header comment referencing Phase 65 + PAY-IDs + cross-references to [[reference_supabase_back_dated_migration_blocks_push]] and any other applicable memory references. Add `alter table ... enable row level security;` BEFORE policy creation. Do NOT use `if not exists` on `create policy` (unsupported on remote PG per project convention). Do NOT use `if not exists` on `create table` (clean migrations, no re-runs).
  </action>
  <verify>
    <automated>ls supabase/migrations/20290104000001_*.sql supabase/migrations/20290104000002_*.sql supabase/migrations/20290104000003_*.sql supabase/migrations/20290104000004_*.sql supabase/migrations/20290104000005_*.sql 2>&1 | grep -c "^supabase" | grep -q "^5$" && grep -L "if not exists.*create policy" supabase/migrations/20290104000001*.sql supabase/migrations/20290104000002*.sql supabase/migrations/20290104000003*.sql supabase/migrations/20290104000004*.sql supabase/migrations/20290104000005*.sql</automated>
  </verify>
  <done>5 migration files exist with timestamps 20290104000001-000005; each contains begin/commit; tables created with bare CREATE POLICY; org_subscriptions has tax_id column; subscriptions has dunning_state + last_dunning_email_at; dunning_emails_sent + refunds + lifecycle_emails_sent tables created with correct PKs and RLS.</done>
</task>

<task type="auto">
  <name>Task 2: Tax collection log, nexus thresholds (seeded), and per-state revenue matview (3 migrations)</name>
  <files>supabase/migrations/20290104000006_tax_collection_log.sql, supabase/migrations/20290104000007_tax_nexus_thresholds.sql, supabase/migrations/20290104000008_tax_nexus_state_revenue_matview.sql</files>
  <action>
Create the 3 tax-reporting migrations.

**Migration 20290104000006_tax_collection_log.sql** (PAY-01 audit trail): Create table `public.tax_collection_log` to record every Stripe Tax calculation: `id uuid primary key default gen_random_uuid()`, `subscription_id uuid references public.subscriptions(id) on delete set null`, `org_subscription_id uuid references public.org_subscriptions(id) on delete set null`, `stripe_session_id text`, `stripe_payment_intent_id text`, `customer_state text` (US state code), `customer_postal text`, `tax_amount_cents integer not null check (tax_amount_cents >= 0)`, `subtotal_cents integer not null`, `total_cents integer not null`, `tax_rate_percent numeric(5,3)`, `automatic_tax_status text` (Stripe `automatic_tax.status` field: `requires_location_inputs|complete|failed`), `created_at timestamptz not null default now()`. CHECK constraint: at least one of `subscription_id` or `org_subscription_id` is non-null. Index by `customer_state` for nexus rollup. RLS: service_role all + staff SELECT via `public.is_staff()`.

**Migration 20290104000007_tax_nexus_thresholds.sql** (PAY-04 reference data): Create table `public.tax_nexus_thresholds` with: `state text primary key check (length(state) = 2)`, `state_name text not null`, `threshold_amount_cents bigint not null check (threshold_amount_cents > 0)`, `threshold_period_days integer not null check (threshold_period_days in (365,730))`, `transaction_count_threshold integer` (NULL when state uses revenue-only test), `notes text`, `updated_at timestamptz not null default now()`. RLS: service_role all + authenticated SELECT (public reference data).

Seed inline with at least 12 US economic-nexus state thresholds (representative — CA $500k/yr, TX $500k/yr, NY $500k/yr OR 100 tx, FL $100k/yr, WA $100k/yr OR 200 tx, IL $100k/yr OR 200 tx, PA $100k/yr, GA $100k/yr OR 200 tx, NC $100k/yr OR 200 tx, NJ $100k/yr OR 200 tx, VA $100k/yr OR 200 tx, AZ $100k/yr). Use `insert into public.tax_nexus_thresholds (state, state_name, threshold_amount_cents, threshold_period_days, transaction_count_threshold, notes) values ...`. Each row's `notes` should cite the state DOR rule reference.

**Migration 20290104000008_tax_nexus_state_revenue_matview.sql** (PAY-04 rollup): Create `materialized view public.tax_nexus_state_revenue` that aggregates `tax_collection_log` per-state for the trailing 365 days: `select customer_state as state, sum(total_cents) as revenue_cents, count(*) as transaction_count, max(created_at) as last_transaction_at from public.tax_collection_log where customer_state is not null and created_at >= now() - interval '365 days' group by customer_state;`. Add unique index on `state` (required for `refresh materialized view concurrently`). Grant `select` to `service_role` and `authenticated`. Add comment: matview MUST be refreshed daily by nexus-monitor cron (Plan 65-08). Do NOT register the cron here — that's done at deploy time per [[feedback_fn_deploy_before_cron_db_push]].

All 3 migrations: begin/commit wrapper, RLS-enabled tables, bare CREATE POLICY, header comments referencing PAY-IDs + relevant memory references.
  </action>
  <verify>
    <automated>ls supabase/migrations/20290104000006_*.sql supabase/migrations/20290104000007_*.sql supabase/migrations/20290104000008_*.sql 2>&1 | grep -c "^supabase" | grep -q "^3$" && grep -c "insert into public.tax_nexus_thresholds" supabase/migrations/20290104000007_*.sql | grep -qv ":0$" && grep -q "create materialized view" supabase/migrations/20290104000008_*.sql && grep -q "create unique index" supabase/migrations/20290104000008_*.sql</automated>
  </verify>
  <done>3 migration files exist; tax_collection_log has subscription_id OR org_subscription_id check; tax_nexus_thresholds seeded with ≥12 rows; tax_nexus_state_revenue matview created with unique index on state for concurrent refresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role → DB | Edge Fns write to dunning_emails_sent, refunds, lifecycle_emails_sent, tax_collection_log |
| authenticated user → DB | RLS-gated SELECT of own refunds via subscriptions.user_id |
| staff → DB | RLS-gated SELECT on dunning_emails_sent, tax_collection_log, tax_nexus_state_revenue for /admin/tax dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01-01 | Tampering | dunning_emails_sent | mitigate | Composite PK (subscription_id, stage) + ON CONFLICT DO NOTHING in Plan 65-05 prevents duplicate-send tampering |
| T-65-01-02 | Information Disclosure | refunds | mitigate | RLS SELECT-own via auth.uid() = subscriptions.user_id; service_role required to mutate |
| T-65-01-03 | Information Disclosure | tax_collection_log | mitigate | staff-only SELECT via public.is_staff(); customer_state is not PII alone but tax_amount_cents is sensitive |
| T-65-01-04 | Repudiation | refunds | mitigate | created_at + updated_at timestamps + stripe_refund_id UNIQUE + eligibility_window column records WHY refund was allowed |
| T-65-01-05 | Tampering | tax_nexus_thresholds | accept | authenticated SELECT only; service_role mutation gated by Supabase project access (operator-controlled) |
| T-65-01-06 | Denial of Service | tax_nexus_state_revenue matview | mitigate | concurrent refresh (unique index on state) so reads never block; cron-scheduled refresh in Plan 65-08 |
</threat_model>

<verification>
- All 8 migration files exist at exact timestamps 20290104000001-000008
- No `if not exists` on `create policy` lines (project convention)
- No `if not exists` on `create table` lines (clean migrations)
- Each migration wrapped in `begin; ... commit;`
- RLS enabled on every new table before any `create policy` statement
- tax_nexus_thresholds seeded with ≥12 US state rows inline
- tax_nexus_state_revenue matview has unique index on `state` (required for concurrent refresh)
- subscriptions.dunning_state CHECK constraint includes all 5 enum values + NULL
- refunds.id is surrogate uuid (NOT composite) per [[reference_dual_auth_submission_table_pk_pattern]]
- dunning_emails_sent + lifecycle_emails_sent have composite PKs for idempotency
- DO NOT `supabase db push` here — push happens in close-out Plan 65-10 per [[feedback_fn_deploy_before_cron_db_push]]
</verification>

<success_criteria>
8 migration files committed to repo at `supabase/migrations/20290104*.sql`. Wave 2 plans can reference table shapes deterministically. No migration is pushed to remote in this plan — close-out owns push.
</success_criteria>

<output>
Create `.planning/phases/65-stripe-tax-payment-resilience/65-01-SUMMARY.md` when done.
</output>
