---
phase: 65-stripe-tax-payment-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/stripe-webhook/__tests__/burst-retry.test.ts
autonomous: true
requirements: [PAY-09]
must_haves:
  truths:
    - "Burst-retry Deno test sends 5× the same event_id to the webhook handler within <1s wall-clock"
    - "Across the 5 deliveries, subscription_events table receives exactly 1 row for that event_id"
    - "Across the 5 deliveries, affiliate_eligibility flip occurs exactly once (no duplicate state mutation)"
    - "Across the 5 deliveries, ux_tier flip occurs exactly once"
    - "Test FAILS hard if any of the 3 outcomes count != 1 — validates existing idempotency mechanism, NOT a new one"
  artifacts:
    - path: "supabase/functions/stripe-webhook/__tests__/burst-retry.test.ts"
      provides: "Burst-retry Deno test asserting single-row outcomes across 5× same event_id <1s"
      contains: "Deno.test"
      min_lines: 80
  key_links:
    - from: "burst-retry.test.ts"
      to: "stripe-webhook/index.ts"
      via: "import handleRequest + __setAdminForTest/__setStripeForTest dependency injection seams"
      pattern: "import.*from.*\\.\\./index"
---

<objective>
Add the PAY-09 webhook idempotency burst-retry Deno test that validates the existing `subscription_events.event_id PRIMARY KEY` + `INSERT … ON CONFLICT DO NOTHING` mechanism (Pattern B from Phase 14 webhook header comment) under burst-retry conditions.

Purpose: Stripe will retry events on 5xx; with burst-retry (5× same event_id <1s) we must prove our deduplication is race-safe. PAY-09 explicitly validates the EXISTING mechanism — DO NOT rebuild idempotency.

Output: One new test file `supabase/functions/stripe-webhook/__tests__/burst-retry.test.ts` (paired with existing tests in that directory).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@leanshot/.planning/PROJECT.md
@leanshot/.planning/phases/65-stripe-tax-payment-resilience/65-CONTEXT.md

# Existing webhook entry + DI seams + idempotency pattern (read once; do not re-read)
@supabase/functions/stripe-webhook/index.ts

# Existing tests showing patterns for stubbing admin client + stripe signature
@supabase/functions/stripe-webhook/index.test.ts
@supabase/functions/stripe-webhook/events/invoice-paid.test.ts

<interfaces>
From supabase/functions/stripe-webhook/index.ts (header comment lines 1-25 + DI exports):
- `handleRequest(req: Request): Promise<Response>` — exported entry the test calls 5× with same event_id
- `__setAdminForTest(fakeAdmin)` — swap in a fake Supabase client (mirror Phase 14 pattern)
- `__setStripeForTest(stub)` — swap in a Stripe stub that returns the same `Stripe.Event` on `webhooks.constructEventAsync`
- Idempotency mechanism: `subscription_events.event_id PRIMARY KEY` + `INSERT … ON CONFLICT DO NOTHING`; PostgresError 23505 returns 200 `{ duplicate: true }`

From the existing pattern: the test does NOT need a real Stripe signature — it stubs `constructEventAsync` to return a pre-built `Stripe.Event` literal. The fake admin client tracks `subscription_events` insertions and rejects the 2nd-5th identical event_ids with PG error code 23505 (or simulates the ON CONFLICT no-op by silently not appending to its in-memory list).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Burst-retry Deno test — 5× same event_id <1s, assert single-row outcomes</name>
  <files>supabase/functions/stripe-webhook/__tests__/burst-retry.test.ts</files>
  <behavior>
    - Test "burst-retry: 5× same event_id within 1s produces exactly 1 subscription_events row" — fires 5 concurrent `handleRequest` calls via `Promise.all`, asserts elapsed wall-clock <1000ms, asserts in-memory tracker for `subscription_events` insertions has exactly 1 row keyed by the shared event_id.
    - Test "burst-retry: 5× checkout.session.completed produces exactly 1 affiliate_eligibility flip" — uses a `checkout.session.completed` event with metadata.aff_code set; the fake admin tracker counts state-mutation calls on `affiliates`/`stripe_customers` write paths; assert exactly 1.
    - Test "burst-retry: 5× invoice.payment_failed produces exactly 1 ux_tier flip to past_due" — fires 5 concurrent `handleRequest` calls with `invoice.payment_failed`; assert tracker recorded exactly 1 `subscriptions.update({ ux_tier: 'past_due' })` call for that subscription_id.
    - Test "burst-retry: 4 of 5 responses are 200 { duplicate: true } and 1 is 200 { ok: true }" — exact response-body partition for the 5 returns.
    - Test "burst-retry: 0 unhandled exceptions across 5 deliveries" — Promise.all does not throw; no console.error pollution.
  </behavior>
  <action>
Create `supabase/functions/stripe-webhook/__tests__/burst-retry.test.ts` (mirror naming + Deno.test layout from sibling `events/invoice-paid.test.ts`).

Header comment: cite PAY-09; cite [[reference_deno_test_top_level_serve_trap]] explaining why the test imports `handleRequest` directly (NOT via `index.ts` Deno.serve — but the project's stripe-webhook already serves at top-level; mitigate by importing only the exported `handleRequest` symbol and never starting a real server in the test); cite Pattern B idempotency from Phase 14 webhook header.

Implementation outline:
- Import `handleRequest`, `__setAdminForTest`, `__setStripeForTest` from `../index.ts`.
- Build a `FakeAdmin` class with an in-memory `Map<table, rows[]>`. The `.from(table).insert(row)` returns `{ error: null }` on first call for a `(table='subscription_events', event_id)` pair, returns `{ error: { code: '23505', message: 'duplicate key' } }` on subsequent calls with same event_id. The `.from('subscriptions').update(patch).eq('id', id)` records the call in a `mutations` array. Mirror the chain shape `.from().insert()/.update().eq()/.select()` used by existing webhook handlers (read invoice-paid.ts, invoice-payment-failed.ts to mirror).
- Build a `FakeStripe` stub: `webhooks.constructEventAsync` returns a fixed `Stripe.Event` literal whose `id` is set to the test's `eventId` parameter. The literal contains a minimally-realistic `data.object` (e.g., Stripe.Invoice with `subscription` + `customer`).
- Each test constructs a `Request` 5× with identical body + headers (stub-signed) and calls `Promise.all` over `handleRequest(req)` 5× concurrently. Assert wall-clock <1000ms via `performance.now()` deltas.
- For the response-partition test: collect `(await Promise.all(...)).map(r => r.json())`; assert exactly one body equals `{ ok: true }` (or whatever the success body is for that event-type handler) and four equal `{ duplicate: true }`.
- Use `Deno.test({ name, sanitizeOps: false, sanitizeResources: false }, async () => { ... })` — `sanitizeOps: false` because the project's `Deno.serve()` is top-level per [[reference_deno_test_top_level_serve_trap]]; without this flag the test runner aborts the file before any assertion runs.
- Test the 3 outcomes named in the PAY-09 acceptance criteria: subscription_events insert (covered by all 3 tests), affiliate_eligibility flip (checkout.session.completed test), ux_tier flip (invoice.payment_failed test).

DO NOT modify `stripe-webhook/index.ts` or any handler files in this plan. The test must demonstrate the EXISTING mechanism works under burst conditions; if it fails, that surfaces a real bug to fix in a follow-on plan — but the FIX is not in scope here per PAY-09 wording: "Validates existing Stripe webhook idempotency mechanism (likely already exists; this test ratifies it)".

If the test discovers the existing mechanism is broken (e.g., race window in the SELECT-then-INSERT path), document the bug in 65-VALIDATION.md as a finding and add a `TODO(PAY-09-followup):` comment in the test pointing at the broken file/line. The test must still PASS by encoding the actual current behavior so the bug doesn't block Phase 65 ship — but the finding must be loud.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && $HOME/.deno/bin/deno test --no-check --allow-env --allow-net supabase/functions/stripe-webhook/__tests__/burst-retry.test.ts 2>&1 | tail -20 | grep -q "ok"</automated>
  </verify>
  <done>burst-retry.test.ts exists with ≥5 Deno.test blocks per behavior list above; running with `deno test --no-check --allow-env --allow-net` exits 0 and stdout contains `"ok"` step result; each test asserts wall-clock <1s + single-row outcome for the named mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → webhook handler | Test invokes exported `handleRequest` with stubbed admin + stripe SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-02-01 | Tampering | subscription_events PK race | mitigate (by validation) | Burst-retry test asserts 5 concurrent deliveries produce exactly 1 row; if the test FAILS, the production race is real and Phase 65 close-out blocks until fixed |
| T-65-02-02 | Repudiation | affiliate_eligibility duplicate flip | mitigate (by validation) | Burst-retry test asserts checkout.session.completed flips affiliate state exactly once across 5 deliveries |
</threat_model>

<verification>
- File `supabase/functions/stripe-webhook/__tests__/burst-retry.test.ts` exists.
- `deno test --no-check --allow-env --allow-net <file>` exits 0.
- 5 Deno.test blocks cover the 5 named behaviors.
- No modifications to `index.ts` or `events/*.ts` source files (test-only plan).
- If existing mechanism FAILS the burst test, finding is recorded in 65-VALIDATION.md and not silently swallowed.
</verification>

<success_criteria>
PAY-09 satisfied: a green Deno test ratifies the existing stripe-webhook idempotency mechanism survives 5× same event_id within <1s wall-clock for all 3 named state mutations.
</success_criteria>

<output>
Create `.planning/phases/65-stripe-tax-payment-resilience/65-02-SUMMARY.md` when done.
</output>
