/**
 * Phase 22 plan 22-01 Wave 0 scaffold — lifecycle welcome series e2e (ON-02).
 * Owner of impl: plan 22-07.
 *
 * Behaviors deferred:
 *   - RESEND_API_KEY=test-stub → function returns success without HTTP call
 *   - Verified domain path stubbed via gated send health-check
 */
import { test } from '@playwright/test';

test.skip('lifecycle welcome series stub send [DEFERRED — Wave 0 scaffold; impl in plan 22-07]', async ({ page: _page }) => {
  // Placeholder: invokes welcome series function with test-stub key; asserts no real Resend HTTP.
});

// Wave 0 scaffold per .planning/phases/22-…/22-RESEARCH.md §Validation Architecture — DEFERRED implementation owner: 22-07
