/**
 * Phase 15 Plan 15-07 — LeadFormBlock editor-preview tests.
 *
 * Task 0 scaffold: declared as `it.todo` placeholders. Task 1 fills these in
 * with real assertions on:
 *   - renders heading and button label from `content`.
 *   - renders an optional name input only when `content.collectName` is true.
 *   - renders a `<label>` for the email field.
 */
import { describe, it } from 'vitest';

describe('LeadFormBlock', () => {
  it.todo('renders heading and button label from content');
  it.todo('renders an optional name input only when content.collectName is true');
  it.todo('renders a <label> for the email field');
});
