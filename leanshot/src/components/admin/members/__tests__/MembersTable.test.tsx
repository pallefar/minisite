/**
 * Phase 22 plan 22-01 Wave 0 scaffold — MembersTable (ADMIN-01).
 * Owner of impl: plan 22-02.
 *
 * Behaviors deferred:
 *   - 3-click sort cycle: unsorted → asc → desc → unsorted
 *   - Pagination URL serialization (?page=N&size=M)
 */
import { describe, it } from 'vitest';

import { MembersTable } from '@/components/admin/members/MembersTable';

describe('MembersTable (Phase 22 ADMIN-01)', () => {
  it.skip('sort header 3-click cycle reverts to unsorted [DEFERRED — Wave 0 scaffold; impl in plan 22-02]', () => {
    void MembersTable;
  });
  it.skip('pagination updates ?page= in URL [DEFERRED — Wave 0 scaffold; impl in plan 22-02]', () => {
    void MembersTable;
  });
});

// Wave 0 scaffold per .planning/phases/22-…/22-RESEARCH.md §Validation Architecture — DEFERRED implementation owner: 22-02
