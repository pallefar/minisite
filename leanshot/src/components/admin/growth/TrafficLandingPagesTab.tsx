/**
 * Phase 51 Plan 51-05 — TrafficLandingPagesTab STUB.
 *
 * Placeholder for **Plan 51-08** (Top Landing Pages tab). See sibling
 * TrafficChannelsTab.tsx for the slot-contract rationale + overwrite rules.
 *
 * Plan 51-08 contract:
 *   - Named export: `TrafficLandingPagesTab`
 *   - Signature: `React.FC` (no props)
 *   - Module path: `@/components/admin/growth/TrafficLandingPagesTab`
 */
import { EmptyState } from '@/components/ui/EmptyState';

export const TrafficLandingPagesTab: React.FC = () => (
  <EmptyState
    inline
    title="Landing Pages tab"
    body="Top landing pages + variant join (PAGEAB) ships in Plan 51-08."
  />
);

export default TrafficLandingPagesTab;
