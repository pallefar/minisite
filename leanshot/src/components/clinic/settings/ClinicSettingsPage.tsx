// Phase 9 Plan 09-01 Wave-0 stub. Plan 09-03 OVERWRITES this file with the
// real clinic settings (Workspace / Roles / Members tabs). Note the path:
// `src/components/clinic/settings/` (under clinic/, NOT under
// dashboard/settings/ — keeps the operator-context settings physically
// separate from the patient-context Settings drawer).

export function ClinicSettingsPage() {
  return (
    <div
      role="main"
      className="min-h-screen flex items-center justify-center bg-[var(--color-bg)] text-[var(--color-text-secondary)]"
    >
      <p>Phase 9 stub — Plan 09-03 overwrites this file.</p>
    </div>
  );
}

export default ClinicSettingsPage;
