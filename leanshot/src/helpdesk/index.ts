/**
 * Helpdesk barrel — Phase 37 Plan 06.
 *
 * Only the root widget is re-exported here. Sub-components are loaded via
 * React.lazy() inside HelpdeskWidget so they stay in their own sub-chunks.
 */
export { HelpdeskWidget } from './HelpdeskWidget';
