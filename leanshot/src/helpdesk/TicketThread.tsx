/**
 * TicketThread — Phase 37 Plan 06 Task 3.
 *
 * Stub created in Task 1; real Realtime + composer implementation lands in Task 3.
 */
import type { JSX } from 'react';

export interface TicketThreadProps {
  ticketId: string;
  currentUserId: string;
  onBack: () => void;
}

export default function TicketThread(_props: TicketThreadProps): JSX.Element | null {
  return null;
}
