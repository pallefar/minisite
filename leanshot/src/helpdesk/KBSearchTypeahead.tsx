/**
 * KBSearchTypeahead — Phase 37 Plan 06 Task 2 (HELP-11).
 *
 * Stub created in Task 1; real debounced RPC implementation lands in Task 2 GREEN.
 */
import type { JSX } from 'react';

export interface KBSearchTypeaheadProps {
  onSelectArticle: (id: string) => void;
}

export default function KBSearchTypeahead(
  _props: KBSearchTypeaheadProps,
): JSX.Element | null {
  // Task 2 GREEN replaces this stub with the real debounced RPC + locale picker.
  return null;
}
