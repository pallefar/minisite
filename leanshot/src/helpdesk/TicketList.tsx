/**
 * TicketList — Phase 37 Plan 06 Task 3.
 *
 * Stub created in Task 1; real implementation lands in Task 3.
 */
import type { JSX } from 'react';

export interface TicketListProps {
  onSelect: (ticketId: string) => void;
}

export default function TicketList(_props: TicketListProps): JSX.Element | null {
  return null;
}
