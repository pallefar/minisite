/**
 * TicketForm — Phase 37 Plan 06 Task 3 (HELP-02).
 *
 * Stub created in Task 1; real implementation lands in Task 3 GREEN.
 */
import type { JSX } from 'react';

export interface TicketFormProps {
  onSubmitted: (ticketId: string) => void;
  onCancel: () => void;
}

export default function TicketForm(_props: TicketFormProps): JSX.Element | null {
  return null;
}
