/**
 * ReplyComposer — Phase 37 Plan 06 Task 3.
 *
 * Stub created in RED; real composer + '/' slash-command lands in Task 3 GREEN.
 */
import type { JSX } from 'react';

export interface ReplyComposerProps {
  ticketId: string;
  onSent?: () => void;
}

export default function ReplyComposer(_props: ReplyComposerProps): JSX.Element | null {
  return null;
}
