<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Declare this app as a Wear OS watch app -->
    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:label="@string/app_name"
        android:theme="@style/Theme.Leanshot.Wear">

        <!-- Main watch launcher activity -->
        <activity
            android:name=".WearMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Quick dose-log activity launched from tile or complication -->
        <activity
            android:name=".QuickLogActivity"
            android:exported="false" />

        <!-- Tile service — displays next-dose + streak on the watch face -->
        <service
            android:name=".DoseTileService"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
        </service>

        <!-- Data Layer relay service — receives DataClient events from watch and relays to phone -->
        <service
            android:name=".WatchDataLayerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
            </intent-filter>
        </service>

    </application>

</manifest>
