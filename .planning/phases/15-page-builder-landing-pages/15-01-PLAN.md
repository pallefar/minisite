---
phase: 15-page-builder-landing-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20261001000001_profiles.sql
  - supabase/migrations/20261001000002_page_builder_tables.sql
  - supabase/migrations/20261001000003_page_builder_fk_and_rls.sql
  - supabase/migrations/20261001000004_page_assets_storage.sql
  - leanshot/tests/rls/page-builder-rls.test.ts
autonomous: false
requirements: [PAGE-01, PAGE-07]
user_setup:
  - service: supabase
    why: "supabase db push pushes the 4 Phase 15 migrations to the live cloud DB (project ytnsipxxmzgaebkqmokp). Non-TTY runs need an access token; the push is the [BLOCKING] task."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "macOS keychain (per memory reference_stripe_legacy_key_and_supabase_token) or Supabase Dashboard -> Account -> Access Tokens"

must_haves:
  truths:
    - "A LeanShot staff member (profiles.is_staff = true) can be distinguished from a normal user by the is_staff() SQL helper and by RLS policies on all 4 new surfaces."
    - "A non-staff authenticated user cannot read, insert, update, or delete rows in landing_pages, landing_page_revisions, leads, or site_settings."
    - "An anonymous/public visitor can read only landing_pages rows where is_published = true (for page-render), and nothing in landing_page_revisions / leads / site_settings beyond what page-render needs."
    - "Every page save appends a new landing_page_revisions row; existing revision rows can never be UPDATEd or DELETEd by anyone via RLS (append-only invariant)."
    - "landing_pages.published_revision_id and landing_page_revisions.page_id form a circular FK that resolves cleanly in a single `supabase db push` (deferred FK)."
    - "page-assets Storage bucket exists; only is_staff users can list/insert/update/delete objects; objects are publicly readable by URL for the rendered page."
  artifacts:
    - path: "supabase/migrations/20261001000001_profiles.sql"
      provides: "public.profiles table with is_staff boolean + is_staff() SECURITY DEFINER helper + RLS"
      contains: "create table public.profiles"
    - path: "supabase/migrations/20261001000002_page_builder_tables.sql"
      provides: "landing_pages, landing_page_revisions, leads, site_settings tables (NO forward FK, NO RLS yet)"
      contains: "create table public.landing_pages"
    - path: "supabase/migrations/20261001000003_page_builder_fk_and_rls.sql"
      provides: "deferred circular FK landing_pages.published_revision_id + RLS enable + all policies on the 4 tables"
      contains: "deferrable initially deferred"
    - path: "supabase/migrations/20261001000004_page_assets_storage.sql"
      provides: "page-assets Storage bucket + idempotent storage.objects RLS policies"
      contains: "'page-assets'"
    - path: "leanshot/tests/rls/page-builder-rls.test.ts"
      provides: "live cross-tenant impersonation proofs for all 4 RLS surfaces + bucket + append-only revision invariant test"
      contains: "describeIfLive"
  key_links:
    - from: "is_staff() SQL helper"
      to: "RLS policies on landing_pages / landing_page_revisions / leads / site_settings + page-assets bucket"
      via: "public.is_staff() called in USING / WITH CHECK clauses"
      pattern: "public\\.is_staff\\(\\)"
    - from: "landing_pages.published_revision_id"
      to: "landing_page_revisions.id"
      via: "deferred FK constraint"
      pattern: "references public\\.landing_page_revisions"
    - from: "leanshot/tests/rls/page-builder-rls.test.ts"
      to: "live cloud DB (post db push)"
      via: "supabase-js anon clients impersonating non-staff users against seeded staff-owned rows"
      pattern: "createClient"
---

<objective>
Create the Phase 15 data layer: the `public.profiles` table (home of the `is_staff` gate per D-11), the `is_staff()` SQL helper, the four page-builder tables (`landing_pages`, `landing_page_revisions`, `leads`, `site_settings`), the deferred circular FK, RLS on every surface, and the `page-assets` Storage bucket with listing RLS. Then push all four migrations to the live Supabase cloud DB and prove the RLS surfaces hold with cross-tenant impersonation tests.

Purpose: Every Wave 2+ plan (`page-render`, `page-save`, `page-publish`, editor, asset library) depends on this schema being live. The `is_staff()` helper + `profiles.is_staff` column are the contract consumed by 15-04's route guard and Edge Function gates. The append-only `landing_page_revisions` invariant test is the contract that 15-04's `page-save` (INSERT-only) and 15-09's restore (pointer re-point, never row mutation) must honor.

Output: 4 migration files, 1 RLS impersonation test file, a live-pushed schema on project `ytnsipxxmzgaebkqmokp`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-page-builder-landing-pages/15-CONTEXT.md
@.planning/phases/15-page-builder-landing-pages/15-RESEARCH.md
@.planning/phases/15-page-builder-landing-pages/15-PATTERNS.md
@.planning/phases/15-page-builder-landing-pages/15-PLAN-OUTLINE.md

<interfaces>
<!-- The exact schema contracts later plans import. Defined HERE. Do not redefine downstream. -->

CRITICAL SCHEMA FACT (executor pre-check confirmed 2026-05-14):
- There is NO `public.profiles` table in the live DB. User profile data lives in
  `public.settings` (PK `user_id`, a jsonb `payload` blob). The codebase has no
  `profiles` table anywhere in `supabase/migrations/`.
- D-11 says the gate lives on `profiles.is_staff` "or equivalent". This plan CREATES
  `public.profiles` as the canonical home for `is_staff` — it is the natural future
  home for Phase 22's full staff-admin surface, and keeps the staff gate off the
  per-device-synced `settings.payload` blob. `profiles` is keyed 1:1 on `auth.users(id)`.
- RESEARCH.md and PATTERNS.md both reference `profiles.is_staff` directly — they
  ASSUMED it existed. It does not. This plan makes the assumption true.

Migration timestamp block: latest existing migration is `20260901000007_*`. Phase 15
uses the `20261001*` block. Files MUST be created in this exact order (db push applies
alphabetically):
  20261001000001_profiles.sql
  20261001000002_page_builder_tables.sql
  20261001000003_page_builder_fk_and_rls.sql
  20261001000004_page_assets_storage.sql

`public.profiles` (20261001000001):
  id          uuid primary key references auth.users(id) on delete cascade
  is_staff    boolean not null default false
  created_at  timestamptz not null default now()

`public.is_staff()` helper — SECURITY DEFINER, language sql, STABLE,
  `set search_path = public, extensions` (Pitfall 2 — search_path on SECURITY DEFINER).
  Returns `coalesce((select is_staff from public.profiles where id = auth.uid() limit 1), false)`.
  `revoke all ... from public; grant execute ... to authenticated, service_role, anon`.

`public.landing_pages` (20261001000002 — created WITHOUT the published_revision_id FK):
  id                     uuid primary key default gen_random_uuid()
  slug                   text not null unique
  title                  text not null default ''
  is_published           boolean not null default false
  published_revision_id  uuid                       -- FK ADDED in migration 003
  seo                    jsonb not null default '{}'::jsonb   -- per-page SEO (15-08 consumer)
  created_by             uuid references auth.users(id) on delete set null
  created_at             timestamptz not null default now()
  updated_at             timestamptz not null default now()

`public.landing_page_revisions` (20261001000002 — append-only per D-07):
  id          uuid primary key default gen_random_uuid()
  page_id     uuid not null references public.landing_pages(id) on delete cascade
  blocks      jsonb not null default '[]'::jsonb   -- BlockNode[] (block-schema.ts defined by 15-03)
  created_by  uuid references auth.users(id) on delete set null
  created_at  timestamptz not null default now()
  -- NEVER UPDATEd or DELETEd by anyone — enforced by RLS (no update/delete policy).

`public.leads` (20261001000002 — column shape is Claude's Discretion, per 15-RESEARCH Pattern 6):
  id                uuid primary key default gen_random_uuid()
  page_id           uuid references public.landing_pages(id) on delete set null
  email             text not null
  name              text
  extra_fields      jsonb not null default '{}'::jsonb
  ip_hash           text                    -- SHA-256 of X-Forwarded-For; not raw PII
  honeypot_flagged  boolean not null default false
  source_url        text
  created_at        timestamptz not null default now()

`public.site_settings` (20261001000002 — single-row config per D-15):
  id                   uuid primary key default gen_random_uuid()
  site_name            text not null default 'LeanShot'
  default_description  text
  favicon_url          text
  default_og_image     text
  updated_at           timestamptz not null default now()
  -- Singleton enforced via a partial unique index on a constant: see <action> Task 2.

`page-assets` Storage bucket (20261001000004):
  id/name = 'page-assets', public = true (rendered pages serve image URLs directly),
  file_size_limit = 10485760 (10 MB per D-13), allowed_mime_types =
  array['image/jpeg','image/png','image/webp','image/gif'].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: profiles table + is_staff() helper migration</name>
  <files>supabase/migrations/20261001000001_profiles.sql</files>
  <read_first>
    - supabase/migrations/20260601000019_stripe_subscriptions.sql (the closest table+RLS+SECURITY DEFINER analog; copy the `count_active_patients` function header for the SECURITY DEFINER + `set search_path = public, extensions` + revoke/grant pattern)
    - supabase/migrations/*_settings.sql (the `settings` migration — copy its style of documenting a DELIBERATELY-omitted policy with a clear comment)
    - .planning/phases/15-page-builder-landing-pages/15-RESEARCH.md "RLS policy pattern for landing_pages (is_staff gate)" code block (the recommended is_staff() body)
  </read_first>
  <action>
    Create migration `20261001000001_profiles.sql`. This is a STANDALONE migration file (split per the planner-iter1 Postgres DDL anti-pattern — no forward refs, no enum adds, no circular FKs here).

    Header comment block must state: Phase 15 Plan 15-01; the executor-confirmed fact that no `profiles` table existed before this; that `is_staff` is the D-11 thin staff gate and the canonical future home for Phase 22's staff-admin surface; and the Pitfall 2 annotation for the SECURITY DEFINER search_path.

    Contents in order:
    1. `create table public.profiles` with columns exactly as the `<interfaces>` block specifies (`id` PK referencing `auth.users(id) on delete cascade`, `is_staff boolean not null default false`, `created_at`).
    2. `alter table public.profiles enable row level security;`
    3. RLS policies on `public.profiles`:
       - SELECT: `auth.uid() = id` (a user can read their own profile row) — policy name `profiles_select_own`.
       - INSERT: `auth.uid() = id` with check — policy name `profiles_insert_own` (lets a user self-provision their row; `is_staff` defaults false so this cannot self-escalate).
       - NO UPDATE policy and NO DELETE policy for `authenticated` — `is_staff` is set only by service-role (Phase 22 admin tooling / manual SQL). A normal user can never flip their own `is_staff`. Document this omission in a comment exactly like the `settings` migration documents its omitted DELETE policy.
    4. `create or replace function public.is_staff()` — `returns boolean`, `language sql`, `stable`, `security definer`, `set search_path = public, extensions`. Body: `select coalesce((select is_staff from public.profiles where id = auth.uid() limit 1), false);`
    5. `revoke all on function public.is_staff() from public;` then `grant execute on function public.is_staff() to authenticated, service_role, anon;` (anon needs it because page-render's public path checks may reference it; harmless — anon's `auth.uid()` is NULL so it always returns false for anon).

    Do NOT add any seed row. The first staff user is set by manual SQL / Phase 22 — out of scope here.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -q "create table public.profiles" supabase/migrations/20261001000001_profiles.sql && grep -q "security definer" supabase/migrations/20261001000001_profiles.sql && grep -q "set search_path = public, extensions" supabase/migrations/20261001000001_profiles.sql && grep -cE "create policy" supabase/migrations/20261001000001_profiles.sql | grep -qx 2 && echo OK</automated>
  </verify>
  <done>Migration file exists with the profiles table, exactly 2 RLS policies (select_own, insert_own), no update/delete policy, and the is_staff() SECURITY DEFINER helper with the correct search_path.</done>
</task>

<task type="auto">
  <name>Task 2: page-builder tables migration (no FK, no RLS)</name>
  <files>supabase/migrations/20261001000002_page_builder_tables.sql</files>
  <read_first>
    - supabase/migrations/20260601000019_stripe_subscriptions.sql (table creation + partial-index IMMUTABLE-safe pattern; the `subscription_events` append-only table is the closest analog for `landing_page_revisions`)
    - supabase/migrations/*_settings.sql (the `extensions.moddatetime` trigger pattern — copy the `create trigger ... execute function extensions.moddatetime(updated_at)` shape)
    - .planning/phases/15-page-builder-landing-pages/15-RESEARCH.md "Pattern 1: Block-Tree JSON Schema" + "Pitfall 6: Forward-Ref FK Circular Dependency" (the table-creation-order rule)
    - the `<interfaces>` block above for the EXACT column list of all 4 tables
  </read_first>
  <action>
    Create migration `20261001000002_page_builder_tables.sql`. STANDALONE file — creates the 4 tables ONLY. NO forward FK, NO RLS enable, NO policies (those go in migration 003, deliberately split per Pitfall 6 + the planner-iter1 DDL anti-pattern).

    Header comment: Phase 15 Plan 15-01; explicitly state the circular-FK split — `landing_pages` is created here WITHOUT `published_revision_id`'s FK constraint (the column exists but is a bare `uuid`), and the FK is added in migration 003 as `deferrable initially deferred` per Pitfall 6.

    Create tables in this order (FK dependency order):
    1. `create table public.landing_pages` — all columns from `<interfaces>`. The `published_revision_id` column is declared as a bare `uuid` (NULLable, NO `references` clause here).
    2. `create table public.landing_page_revisions` — all columns from `<interfaces>`. `page_id` HAS its `references public.landing_pages(id) on delete cascade` here (landing_pages already exists). Add a comment: `-- append-only per D-07; RLS in migration 003 grants no UPDATE/DELETE`.
    3. `create table public.leads` — all columns from `<interfaces>`. `page_id references public.landing_pages(id) on delete set null`.
    4. `create table public.site_settings` — all columns from `<interfaces>`.

    Indexes:
    - `create index idx_landing_page_revisions_page on public.landing_page_revisions(page_id, created_at desc);` (no WHERE clause needed — page_id is `not null`; if a partial index is used the predicate must be IMMUTABLE-safe per Pitfall 1, but a plain index is fine here).
    - `create index idx_leads_page on public.leads(page_id) where page_id is not null;` (IMMUTABLE-safe column-only predicate).
    - `create unique index site_settings_singleton on public.site_settings ((true));` — enforces exactly one row (Open Question 4 in RESEARCH.md). Add a comment explaining the constant-expression singleton trick.

    Add a `moddatetime` trigger on `landing_pages.updated_at` and `site_settings.updated_at` mirroring the `settings` migration's `extensions.moddatetime` trigger pattern (the `moddatetime` extension is already created by the Phase 6 settings migration — do NOT re-create the extension; just `create trigger`).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && for t in landing_pages landing_page_revisions leads site_settings; do grep -q "create table public.$t" supabase/migrations/20261001000002_page_builder_tables.sql || { echo "MISSING $t"; exit 1; }; done && ! grep -q "references public.landing_page_revisions" supabase/migrations/20261001000002_page_builder_tables.sql && ! grep -qi "enable row level security" supabase/migrations/20261001000002_page_builder_tables.sql && grep -q "site_settings_singleton" supabase/migrations/20261001000002_page_builder_tables.sql && echo OK</automated>
  </verify>
  <done>Migration 002 creates all 4 tables, the singleton unique index, and the supporting indexes. It contains NO forward FK to landing_page_revisions and NO RLS statements — those are deliberately deferred to migration 003.</done>
</task>

<task type="auto">
  <name>Task 3: deferred circular FK + RLS for all 4 page-builder tables</name>
  <files>supabase/migrations/20261001000003_page_builder_fk_and_rls.sql</files>
  <read_first>
    - supabase/migrations/20260601000019_stripe_subscriptions.sql (the RLS policy block — `subscription_events` having NO authenticated policies = deny-all is the analog for `leads` deny-all-to-authenticated-except-staff)
    - .planning/phases/15-page-builder-landing-pages/15-RESEARCH.md "Pitfall 6" (deferred FK syntax) + "RLS policy pattern for landing_pages" code block + "Security Domain" threat table
    - supabase/migrations/20261001000001_profiles.sql (the is_staff() helper this migration depends on — same db push run, forward reference resolves at plan time per the org_logos_storage precedent)
  </read_first>
  <action>
    Create migration `20261001000003_page_builder_fk_and_rls.sql`. STANDALONE file — adds the circular FK + all RLS. Split from table creation per Pitfall 6 + planner-iter1 DDL anti-pattern.

    Header comment: Phase 15 Plan 15-01; state the circular-FK resolution (Pitfall 6) and the append-only RLS invariant for `landing_page_revisions`.

    Section 1 — deferred circular FK:
    - `alter table public.landing_pages add constraint fk_landing_pages_published_revision foreign key (published_revision_id) references public.landing_page_revisions(id) on delete set null deferrable initially deferred;`
    - Comment: this closes the circle (`landing_pages` <-> `landing_page_revisions`); `deferrable initially deferred` lets a single transaction insert a revision then point `published_revision_id` at it (15-04 `page-save` / 15-09 restore). `on delete set null` = deleting the published revision unpublishes the page rather than cascading.

    Section 2 — enable RLS on all 4 tables: `alter table public.<t> enable row level security;` for `landing_pages`, `landing_page_revisions`, `leads`, `site_settings`.

    Section 3 — `landing_pages` policies:
    - SELECT `landing_pages_select_published` — `using (is_published = true)` — public/anon can read published pages (page-render).
    - SELECT `landing_pages_select_staff` — `using (public.is_staff())` — staff read all pages incl. drafts.
    - INSERT `landing_pages_insert_staff` — `with check (public.is_staff())`.
    - UPDATE `landing_pages_update_staff` — `using (public.is_staff()) with check (public.is_staff())`.
    - DELETE `landing_pages_delete_staff` — `using (public.is_staff())`.

    Section 4 — `landing_page_revisions` policies (APPEND-ONLY — this is the security-critical invariant):
    - SELECT `landing_page_revisions_select_staff` — `using (public.is_staff())`.
    - SELECT `landing_page_revisions_select_published` — `using (exists (select 1 from public.landing_pages p where p.published_revision_id = landing_page_revisions.id and p.is_published = true))` — lets page-render read the one published revision per page for anon visitors.
    - INSERT `landing_page_revisions_insert_staff` — `with check (public.is_staff())`.
    - **NO UPDATE policy. NO DELETE policy.** Add a prominent comment: `-- APPEND-ONLY (D-07). The deliberate absence of UPDATE/DELETE policies makes every existing revision row immutable to every authenticated caller. Service-role (Edge Functions) bypasses RLS but 15-04/15-09 are contractually INSERT-only / pointer-re-point-only. The append-only invariant test in tests/rls/page-builder-rls.test.ts proves this.`

    Section 5 — `leads` policies:
    - INSERT `leads_insert_service_role` — `with check (auth.role() = 'service_role')` — only the `lead-capture` Edge Function (service role) writes leads.
    - SELECT `leads_select_staff` — `using (public.is_staff())` — staff read leads.
    - NO UPDATE / NO DELETE policy for authenticated. Comment: leads are append-only operational data; mutation/cleanup is service-role-only.

    Section 6 — `site_settings` policies:
    - SELECT `site_settings_select_all` — `using (true)` — global SEO defaults are public (page-render injects them into every page head; anon must read them).
    - INSERT `site_settings_insert_staff` — `with check (public.is_staff())`.
    - UPDATE `site_settings_update_staff` — `using (public.is_staff()) with check (public.is_staff())`.
    - NO DELETE policy. Comment: singleton config — never deleted.

    Every policy targets `to authenticated` except the public-read ones (`landing_pages_select_published`, `landing_page_revisions_select_published`, `site_settings_select_all`) which target `to authenticated, anon`. Match the `org_logos_storage` precedent for `to authenticated, anon` on public reads.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -q "deferrable initially deferred" supabase/migrations/20261001000003_page_builder_fk_and_rls.sql && grep -cE "enable row level security" supabase/migrations/20261001000003_page_builder_fk_and_rls.sql | grep -qx 4 && ! grep -iE "create policy[^;]*landing_page_revisions[^;]*for (update|delete)" supabase/migrations/20261001000003_page_builder_fk_and_rls.sql && grep -cE "public\.is_staff\(\)" supabase/migrations/20261001000003_page_builder_fk_and_rls.sql | awk '$1>=10{print "OK"}'</automated>
  </verify>
  <done>Migration 003 adds the deferred circular FK, enables RLS on all 4 tables, has the full policy set, and contains ZERO update/delete policies on landing_page_revisions (append-only invariant). is_staff() is referenced in 10+ policy clauses.</done>
</task>

<task type="auto">
  <name>Task 4: page-assets Storage bucket + listing RLS migration</name>
  <files>supabase/migrations/20261001000004_page_assets_storage.sql</files>
  <read_first>
    - supabase/migrations/20260801000003_org_logos_storage.sql (the EXACT analog — public bucket + idempotent `do $$ ... pg_policies` policy guards; copy this structure)
    - supabase/migrations/20260514000010_storage_bucket.sql (the photos bucket — the `on conflict (id) do update` bucket-insert pattern + the 4-policy CRUD set)
    - .planning/phases/15-page-builder-landing-pages/15-CONTEXT.md D-13 (asset library — listing RLS, 10 MB cap, required alt-text is a UI concern not a DB concern)
  </read_first>
  <action>
    Create migration `20261001000004_page_assets_storage.sql`. STANDALONE file.

    Header comment: Phase 15 Plan 15-01; `page-assets` is the D-13 asset-library bucket. Bucket is `public = true` because `page-render` serves image URLs directly into rendered HTML for anonymous visitors (same rationale as `org-logos`). Listing/write RLS is gated on `public.is_staff()` — only staff use the asset library; visitors only ever read by direct URL.

    Section 1 — bucket insert:
    `insert into storage.buckets (id, name, public, file_size_limit, allowed_mime_types) values ('page-assets', 'page-assets', true, 10485760, array['image/jpeg','image/png','image/webp','image/gif']) on conflict (id) do update set file_size_limit = excluded.file_size_limit, allowed_mime_types = excluded.allowed_mime_types, public = excluded.public;` — copy the idempotent `on conflict do update` shape from the photos bucket migration.

    Section 2 — storage.objects RLS policies, EACH wrapped in an idempotent `do $$ begin if not exists (select 1 from pg_policies where schemaname='storage' and tablename='objects' and policyname='<name>') then create policy ... end if; end $$;` block (copy the exact guard structure from `org_logos_storage`):
    - SELECT `page_assets_select_public` — `for select to authenticated, anon using (bucket_id = 'page-assets')` — anyone reads (bucket is public; the policy is still required because storage.objects RLS applies regardless of the bucket.public flag — same note as org_logos_storage).
    - INSERT `page_assets_insert_staff` — `for insert to authenticated with check (bucket_id = 'page-assets' and public.is_staff())`.
    - UPDATE `page_assets_update_staff` — `for update to authenticated using (bucket_id = 'page-assets' and public.is_staff()) with check (bucket_id = 'page-assets' and public.is_staff())`.
    - DELETE `page_assets_delete_staff` — `for delete to authenticated using (bucket_id = 'page-assets' and public.is_staff())`.

    The "listing RLS" requirement from D-13 is satisfied because the asset-library UI lists objects via the storage API, which the `page_assets_select_public` policy permits. There is no per-folder isolation here (unlike photos/org-logos) because all assets are staff-shared across any page (D-13: "upload once, pick from a grid across any page"). Add a comment stating this deliberate difference from the per-user-folder photos bucket.
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && grep -q "'page-assets'" supabase/migrations/20261001000004_page_assets_storage.sql && grep -q "10485760" supabase/migrations/20261001000004_page_assets_storage.sql && grep -cE "do \\\$\\\$" supabase/migrations/20261001000004_page_assets_storage.sql | awk '$1>=4{print "OK"}' && grep -q "public.is_staff()" supabase/migrations/20261001000004_page_assets_storage.sql && echo DONE</automated>
  </verify>
  <done>Migration 004 creates the page-assets public bucket (10 MB, image MIMEs) and 4 idempotent storage.objects policies (public select, staff insert/update/delete) gated on is_staff().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: RLS cross-tenant impersonation + append-only invariant test</name>
  <files>leanshot/tests/rls/page-builder-rls.test.ts</files>
  <behavior>
    - T-15-01 (landing_pages): a non-staff authenticated user gets 0 rows when selecting a staff-created DRAFT (is_published=false) landing_pages row by id.
    - T-15-02 (landing_pages): a non-staff authenticated user's INSERT into landing_pages is denied (error is non-null OR 0 rows returned).
    - T-15-03 (landing_page_revisions): a non-staff authenticated user gets 0 rows selecting a revision belonging to a staff draft page.
    - T-15-04 (landing_page_revisions APPEND-ONLY): an authenticated user (even a staff user, via the anon-key client = RLS-enforced) CANNOT UPDATE an existing revision row and CANNOT DELETE it — both operations return an error or affect 0 rows. This proves the append-only invariant at the RLS layer.
    - T-15-05 (leads): a non-staff authenticated user gets 0 rows selecting a leads row; and a non-staff INSERT into leads is denied (only service_role may insert).
    - T-15-06 (site_settings): a non-staff authenticated user CANNOT UPDATE the site_settings singleton row (error or 0 rows); SELECT IS allowed (site_settings_select_all = true) so assert SELECT returns the row.
    - T-15-07 (page-assets bucket): a non-staff authenticated user's upload to the page-assets bucket is denied (storage error).
    - Gating: the whole describe block is `describe.skip` when SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY are not all present (mirrors the Phase 14 test exactly). A separate always-passing gating test warns when skipped.
  </behavior>
  <read_first>
    - leanshot/tests/rls/subscriptions-impersonation.test.ts (the EXACT template — copy the env-var gating, the `describeIfLive`, the `beforeAll` user-creation via `admin.auth.admin.createUser`, the per-test fresh anon client with unique `storageKey`, the `afterAll` best-effort cleanup, the gating-test-at-the-bottom pattern)
    - leanshot/vite.config.ts (the `test.include` already covers `tests/**/*.test.ts` — this file will run under `npm run test:unit`; confirm no config change needed)
    - the `<interfaces>` block above for the seed-row column shapes
  </read_first>
  <action>
    Create `leanshot/tests/rls/page-builder-rls.test.ts`. Structurally clone `subscriptions-impersonation.test.ts`.

    Env + gating: identical to the Phase 14 file — `URL`, `ANON`, `SERVICE` from `process.env` (accept the `VITE_` fallbacks), `SHOULD_RUN = Boolean(URL && ANON && SERVICE)`, `describeIfLive = SHOULD_RUN ? describe : describe.skip`.

    `beforeAll` (120s timeout):
    1. Create `admin` service-role client.
    2. Create one STAFF user and one NON-STAFF user via `admin.auth.admin.createUser({ email, password, email_confirm: true })`. Capture ids/emails/passwords.
    3. For the staff user: `admin.from('profiles').insert({ id: staffUserId, is_staff: true })`. For the non-staff user: `admin.from('profiles').insert({ id: normalUserId, is_staff: false })` (insert it explicitly for clarity even though `is_staff` defaults false).
    4. Seed via service-role `admin`:
       - one DRAFT `landing_pages` row: `{ slug: 'rls-draft-<ts>', title: 'RLS draft', is_published: false, created_by: staffUserId }` — capture `draftPageId`.
       - one `landing_page_revisions` row for it: `{ page_id: draftPageId, blocks: [], created_by: staffUserId }` — capture `revisionId`.
       - one `leads` row: `{ email: 'rls-lead-<ts>@leanshot.test', page_id: draftPageId }` — capture `leadId`.
       - ensure a `site_settings` row exists: SELECT the singleton first; if none exists, INSERT one and capture `siteSettingsId`. (The singleton unique index means a second blind insert fails — fetch-or-insert.) If a row already existed (seeded elsewhere), record that and SKIP deleting it in afterAll.
    5. Each test creates its own fresh anon client with a unique `storageKey` (copy the Phase 14 per-test client pattern) to avoid session bleed.

    Tests T-15-01 .. T-15-07 exactly as the `<behavior>` block describes. For the assertion style:
    - For denied SELECT: RLS returns `{ data: [], error: null }` (Postgres RLS filters silently) — assert `expect(error).toBeNull()` AND `expect(data).toHaveLength(0)` (the Phase 14 pattern).
    - For denied INSERT/UPDATE/DELETE: assert EITHER `error` is non-null OR (for UPDATE/DELETE) `data` is empty / count is 0. Write the assertion as `expect(error !== null || (data ?? []).length === 0).toBe(true)` so it is robust to whichever way RLS rejects.
    - For the append-only T-15-04: use the NON-staff anon client to attempt `.from('landing_page_revisions').update({ blocks: [{ tampered: true }] }).eq('id', revisionId)` and `.delete().eq('id', revisionId)` — both must be no-ops. ALSO use a STAFF anon-key client (sign in the staff user via the anon client) and attempt the same UPDATE/DELETE — because there is no UPDATE/DELETE policy AT ALL, even staff are denied at the RLS layer. Assert both staff and non-staff are denied. Add an inline comment: this is the contract 15-04 page-save (INSERT-only) and 15-09 restore (pointer re-point only) must honor.
    - For T-15-06 site_settings: assert the non-staff client CAN select the singleton (`data` length 1) but CANNOT update it.
    - For T-15-07 page-assets: sign in the non-staff anon client, attempt `.storage.from('page-assets').upload('test-<ts>.png', <small Blob/Uint8Array>)`, assert `error` is non-null.

    `afterAll` (60s, best-effort, FK order): delete seeded `leads`, `landing_page_revisions`, `landing_pages`, the `site_settings` row ONLY if this test created it, `profiles` rows, then `admin.auth.admin.deleteUser` for both users. Wrap each in try/catch like the Phase 14 file.

    Bottom: the always-passing gating `describe` that `console.warn`s when `SHOULD_RUN` is false — copy verbatim from the Phase 14 file (adjust the message to name this test).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite/leanshot && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep -v "page-builder-rls" || true; npm run test:unit -- tests/rls/page-builder-rls.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>The test file typechecks and runs under `npm run test:unit`. When SUPABASE_* env vars are absent it reports as skipped (gating test passes with a warning). When run against the live DB post-push, T-15-01..07 all pass — proving every RLS surface holds and the append-only invariant is enforced even against staff.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: [BLOCKING] supabase db push — apply the 4 Phase 15 migrations to the live cloud DB</name>
  <files>supabase/migrations/20261001000001_profiles.sql, supabase/migrations/20261001000002_page_builder_tables.sql, supabase/migrations/20261001000003_page_builder_fk_and_rls.sql, supabase/migrations/20261001000004_page_assets_storage.sql</files>
  <action>
    [BLOCKING] task. Runs AFTER Tasks 1-5 (all 4 migration files written + verified locally, RLS
    test file written) and BEFORE this plan's verification. `autonomous: false` because
    `supabase db push` against the live project may surface an interactive confirmation prompt
    that cannot always be suppressed in non-TTY — a live-DB push prompt is the one truly
    unavoidable interactive step.

    FALSE-POSITIVE GUARD: `npx tsc` / `npm run build` pass WITHOUT this push — the app reads no
    types from the live DB (Vite SPA; Supabase types are not codegen-pinned here). A green
    typecheck does NOT mean the schema is live. This task is the only thing that makes Wave 2
    (page-render / page-save / page-publish) actually runnable.

    Run from the repo ROOT `/Users/karstenhaldan/minisite` — the migrations dir is repo-root
    `supabase/migrations/`, NOT under `leanshot/`:

    1. Make the access token available for non-TTY: `export SUPABASE_ACCESS_TOKEN=<token>` —
       from the macOS keychain (per memory `reference_stripe_legacy_key_and_supabase_token`) or
       Supabase Dashboard -> Account -> Access Tokens.
    2. `supabase db push`. If an interactive confirmation prompt appears and cannot be
       suppressed, answer it. Expected: all 4 migrations apply in timestamp order; the deferred
       circular FK in migration 003 resolves cleanly (Pitfall 6); no "relation does not exist" error.
    3. If the push fails on the circular FK: confirm migration 002 has NO
       `references public.landing_page_revisions` and migration 003 has
       `deferrable initially deferred` — the split is the fix.
    4. Smoke-confirm: re-running `supabase db push` reports "Remote database is up to date".
    5. Re-run the RLS test against the live DB with secrets injected:
       `cd leanshot && SUPABASE_URL=... SUPABASE_ANON_KEY=... SUPABASE_SERVICE_ROLE_KEY=... npm run test:unit -- tests/rls/page-builder-rls.test.ts`
       — T-15-01..07 must all PASS (not skip).
  </action>
  <verify>
    <automated>cd /Users/karstenhaldan/minisite && supabase db push 2>&1 | grep -qiE "up to date|applying migration|finished|already applied" && echo PUSH-OK</automated>
  </verify>
  <done>All 4 migrations are applied to project `ytnsipxxmzgaebkqmokp`; `supabase db push` reports the remote DB is up to date; the RLS impersonation test T-15-01..07 passes against the live DB (not skipped). Resume signal: "schema-pushed".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/public visitor → Postgres (via PostgREST + RLS) | Unauthenticated reads of `landing_pages` / `landing_page_revisions` / `site_settings` for `page-render`. Must see ONLY published content. |
| non-staff authenticated user → Postgres | A logged-in LeanShot patient must NOT reach any page-builder write path or any draft/lead/settings data. |
| service_role (Edge Functions) → Postgres | `lead-capture` (writes `leads`), `page-save`/`page-publish` (writes `landing_pages` + `landing_page_revisions`). RLS-bypassed by design; contract-bound to honor append-only. |
| staff user → page-assets Storage bucket | Only `is_staff` users may upload/list/mutate assets; visitors read by public URL only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-E-01 | Elevation of Privilege | `profiles.is_staff` self-escalation | mitigate | `profiles` has SELECT+INSERT-own policies only — NO UPDATE policy for authenticated. A user can create their profile row (defaults `is_staff=false`) but can never flip the flag. Only service-role / manual SQL sets `is_staff=true`. Proven indirectly: T-15-02 shows non-staff cannot write page-builder data. |
| T-15-I-01 | Information Disclosure | non-staff reads draft `landing_pages` / `landing_page_revisions` | mitigate | RLS: public SELECT gated on `is_published = true`; staff SELECT gated on `public.is_staff()`. Draft pages and their revisions are invisible to anon + non-staff. Proven by T-15-01, T-15-03. |
| T-15-I-02 | Information Disclosure | non-staff reads captured `leads` (emails) | mitigate | `leads` has SELECT policy gated on `public.is_staff()` only; no policy grants non-staff read. Proven by T-15-05. |
| T-15-T-01 | Tampering | mutation of an existing `landing_page_revisions` row (breaks version-history integrity) | mitigate | `landing_page_revisions` has NO UPDATE and NO DELETE policy for ANY authenticated role — append-only at the RLS layer. Even staff (via the RLS-enforced anon client) are denied. Proven by T-15-04 against both staff and non-staff. Service-role callers (15-04/15-09) are contractually INSERT-only / pointer-re-point-only per the Cross-Plan Contract Notes. |
| T-15-T-02 | Tampering | forged `leads` rows / spam inserted by an authenticated user bypassing the honeypot+rate-limit in `lead-capture` | mitigate | `leads` INSERT policy is `auth.role() = 'service_role'` ONLY — an authenticated user cannot INSERT directly; all lead writes route through the `lead-capture` Edge Function (15-07) which owns the honeypot + per-IP rate limit. Proven by the non-staff INSERT-denied half of T-15-05. |
| T-15-T-03 | Tampering | non-staff overwrites global SEO defaults in `site_settings` | mitigate | `site_settings` UPDATE/INSERT policies gated on `public.is_staff()`; SELECT is public (page-render needs it) but write is staff-only. Proven by T-15-06. |
| T-15-S-01 | Spoofing | slug hijacking — a page with slug `admin`/`clinic`/`share` shadows a real app route | accept (here) / mitigate (downstream) | The `RESERVED_SLUGS` denylist is defined in `block-schema.ts` (15-03) and enforced at save time in `page-save` (15-04). The `slug` column here has only a UNIQUE constraint — denylist enforcement is intentionally an Edge Function concern, not a DB CHECK, so the list can evolve without a migration. Out of scope for this plan's surface. |
| T-15-I-03 | Information Disclosure | non-staff uploads to / lists the `page-assets` bucket | mitigate | Storage RLS: INSERT/UPDATE/DELETE gated on `public.is_staff()`; SELECT is public (rendered pages serve image URLs to anon). Proven by T-15-07. |
| T-15-D-01 | Denial of Service | circular FK makes `supabase db push` fail, blocking the whole phase | mitigate | Pitfall 6: tables split across migration 002 (no FK) and 003 (`deferrable initially deferred` FK). The DDL-split anti-pattern memo is honored — enum/forward-ref DDL never shares a migration file with table creation. |
</threat_model>

<verification>
- All 4 migration files exist under `/Users/karstenhaldan/minisite/supabase/migrations/` with the `20261001*` timestamp block, in the documented order.
- Migration 002 contains zero RLS statements and zero forward FK to `landing_page_revisions` (the split is real, not cosmetic).
- Migration 003 contains the `deferrable initially deferred` FK and zero UPDATE/DELETE policies on `landing_page_revisions`.
- `is_staff()` is `security definer` with `set search_path = public, extensions` (Pitfall 2).
- `leanshot/tests/rls/page-builder-rls.test.ts` typechecks and runs under `npm run test:unit`; skips cleanly without env vars.
- `[BLOCKING]` `supabase db push` applied all 4 migrations to project `ytnsipxxmzgaebkqmokp` without a circular-FK error.
- Post-push, T-15-01..07 all PASS against the live DB.
</verification>

<success_criteria>
- PAGE-01 (partial — the data layer half): `landing_pages` + `landing_page_revisions` + `leads` + `site_settings` tables and the `page-assets` Storage bucket exist on the live DB with RLS on every surface. (The asset-library UI half of PAGE-01 is 15-09.)
- PAGE-07 (partial — the append-only foundation): `landing_page_revisions` is provably append-only at the RLS layer — the invariant test (T-15-04) is green. (The version-history UI + restore is 15-04 + 15-09.)
- D-11 satisfied: `profiles.is_staff` column + `public.is_staff()` helper exist and gate all 4 RLS surfaces + the bucket — the contract 15-04 consumes for its route guard and Edge Function gates.
- Project rule satisfied: every RLS surface (4 tables + 1 bucket) has a live cross-tenant impersonation proof test.
</success_criteria>

<output>
After completion, create `.planning/phases/15-page-builder-landing-pages/15-01-SUMMARY.md`.

The SUMMARY must record, for downstream plans:
- Confirmation that `public.profiles` was NET-NEW in this plan (no prior `profiles` table existed) — 15-04 and any Phase 22 plan must know `profiles` is owned/created here.
- The exact final column names of all 4 tables + `profiles` (in case the executor deviated from the `<interfaces>` block — downstream plans import these).
- The migration timestamps actually used (if the executor had to bump past a collision).
- Whether `supabase db push` needed an interactive prompt answered, and the access-token source used (for the next plan's push, if any).
- The `is_staff()` helper signature and grant set.
</output>
